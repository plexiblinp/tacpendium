# SECURITY — Tacpendium

## 1. ★★本アプリの前提: LAN 内での利用

**Tacpendium は自宅内 LAN での利用を前提として設計されている。**
**⇒ インターネットへ直接公開しないこと。**

この前提のうえで、一部のセキュリティ要件は意図的に緩い。

| 項目 | 現状 |
|---|---|
| 通信路 | **平文 HTTP を前提とする**（TLS 終端を持たない） |
| ログイン | 簡易ログイン。LAN 内の家族・同居人を想定した強度である |
| CORS / CSRF | **LAN 前提の設定**である |
| API | LAN モードには**認証を要求しない経路が残っている**（既知の制約。下記 §3） |

**★「安全である」とは書かない。** 上記は設計上の前提であって、保証ではない。

### インターネットへ公開したい場合に、自分で塞ぐべきところ

本ソフトウェアは `AGPL-3.0-or-later` であり、改変してネットワーク越しに他人へ
使わせる場合、その利用者へ改変後のソースを提供する義務が生じる（AGPL §13）。
**ただし §13 が義務づけるのはソースの提供であって、安全な修正ではない。**
公開運用を行うなら、少なくとも次を自分で手当てすること。

1. **認証** — 未認証で到達できる API を塞ぐ
2. **通信路の暗号化** — TLS 終端を前に置く
3. **公開範囲** — 到達できるネットワークを絞る

---

## 2. 脆弱性の報告

**公開の場に書かないこと。** 公開リポジトリの
**private vulnerability reporting**（Security タブ →「Report a vulnerability」）
を使う。Issues は閉じている。

報告に含めてほしいもの:

- 影響（何ができてしまうか）
- 再現手順（最小の形で）
- 版（リリース成果物のファイル名、またはアプリの起動ログに出る版）
- 環境（OS / ブラウザ / LAN 構成）

**★個人が開発する OSS であり、SLA は無い**（`SUPPORT.md`）。返答までの時間は
保証できないが、致命的なものから見る。

---

## 3. 既知の制約

| # | 制約 |
|---|---|
| 1 | **LAN モードに未認証の API 経路が残っている。** 同一 LAN からは、ログインせずに到達できるものがある |
| 2 | **平文 HTTP である。** 同一 LAN 上の第三者が通信を観測しうる |
| 3 | **改変版は本窓口の対象外である**（`SUPPORT.md`）。改変して運用しているものの不具合は、その改変者が責任を持つ |

---

## 4. 対象範囲

- **対象**: 本リポジトリの公開リリースに含まれるコードと、その配布バイナリ
- **対象外**: 改変版 ／ 第三者が再配布したもの ／ 同梱するゲームデータの内容の誤り
  （データの誤りは脆弱性ではない。通常の報告経路を使うこと）

---

## 5. 権利者・データの由来に関する問い合わせ

**ここは脆弱性の窓口ではない。** §2 の private vulnerability reporting は脆弱性報告の
体裁しか受け付けられず、GitHub アカウントも要る。**権利者からの申し出や、データの
由来についての問い合わせは、そちらでは受けられない。**

    plexiblinp.contact@gmail.com

この窓口で受けるもの:

- **権利者からの取り下げの申し出**（`NOTICE` §3 の撤退ポリシー）
- **同梱するゲームデータの由来についての問い合わせ**
- **公開スナップショットに含めていない資料**（`docs/seed-data/` 等）の取り扱いについての問い合わせ

**★応答までの時間は約束しない**（個人が開発する OSS であり、`SUPPORT.md` のとおり SLA は
無い）。**ただし権利者からの取り下げの申し出は、他に優先して見る。**
