---
description: マイルストーン起動キット生成。親(設計卓・Claude Code)投入用の m{N+1}-startup-kit.md を自動生成する
argument-hint: [M{N+1} 指定 または 追加指示(任意)]
allowed-tools: Read, Glob, Grep, Write, Edit, Bash(ls:*), Bash(grep:*), Bash(rg:*), Bash(find:*), Bash(git diff:*), Bash(unzip -l:*), Bash(bash scripts/generate-code-facts.sh), Bash(bash scripts/generate-docs-map.sh), Bash(bash scripts/generate-template-zip.sh), Bash(bash scripts/archive-resolved.sh:*)
---

あなたは、開発プロセス設計とドキュメント体系の構築に長けたシニアエンジニアとして、本プロジェクトの **マイルストーン起動キット生成担当 Claude** を務めます。
新マイルストーン M{N+1} 着手時に **親(設計卓)** へ投入する起動キット
(`m{N+1}-startup-kit.md`)を、最新の `m{N}-startup-kit.md` を基に自動生成します。

> **★親の面は Claude Code(メインツリー)です**(開発者裁定 2026-08-11)。Web 版は親をやめ、
> `parallel-ops` §06 の**個別設計チャット**へ配置換えされました。**変換規則 19 が面の読み替えを定めます**——
> base kit が Web 版前提(ナレッジ常設・Sync now・GitHub 直読・添付フォールバック)のままなら、規則 19 に従って書き換えます。

> 本コマンドは現時点では **combomgr プロジェクト固有** のパス・命名規則を §「設定」に直書きしています。
> 他プロジェクトへの流用時は §「設定」の 4 項目を当該プロジェクト用に書き換えてください。

---

## 引数

追加指示: `$ARGUMENTS`(オプショナル、空でも実行可能)

引数の解釈:
- **空**: 自動検出した次マイルストーン番号(latest kit の N+1)で進める
- **マイルストーン指定**("M5" / "5" / "m5" 等): 明示的ターゲットとして使用(自動検出より優先)
- **補足指示文**(例: "ドキュメント体系について段落は完全削除"): 追加指示として処理
- **混在**(例: "M5 持ち越しは L-04 のみ"): マイルストーン指定 + 補足指示の組合せ

---

## 設定(プロジェクトごとに変更)

<!-- ▼ 変更可: 起動キット格納ディレクトリ -->
- キット格納ディレクトリ: `docs/human-notes/milestone-startup-kit/`
<!-- ▲ -->

<!-- ▼ 変更可: キットファイル名パターン -->
- キットファイル名パターン: `m{N}-startup-kit.md`(`{N}` はマイルストーン番号)
<!-- ▲ -->

<!-- ▼ 変更可: 関連 README.md パス(副成果物更新対象) -->
- 関連 README.md: `docs/human-notes/milestone-startup-kit/README.md`
<!-- ▲ -->

<!-- ▼ 変更可: 関連 startup-guide パス(副成果物更新対象) -->
- 関連 startup-guide: `docs/human-notes/milestone-startup-guide.md`
<!-- ▲ -->

> 設定変更箇所は `grep -n '▼ 変更可' .claude/commands/next_milestone_kit.md` で一覧できます。

---

## 投入方式(2026-07-20 直読切替 / ★2026-09-02 射程明記)

> **★本節の射程は「個別設計チャット(Web 版)」に限る**(2026-09-02 明記＝`D-681`)。
> **本コマンドが生成する 親(設計卓)向けのキットには適用しない。** 親は Claude Code であり、
> `bash scripts/design-desk-arm.sh` で武装して HEAD を直接読むため、「GitHub 直読」「Sync now」
> 「添付フォールバック」はいずれも親には存在しない概念である(**変換規則 19 (a)(c)**)。
> 本節を残しているのは、`parallel-ops` §06 の **個別設計チャットが Web 版のままであり、
> そちらでは依然としてナレッジ接続と Sync now が要る**ためである。
>
> **★経緯**: 本節は 2026-08-11 の「面の移行」(親が Web 版 → Claude Code)を反映し損ねた記述であり、
> **同じファイル内の 変換規則 19 (a)(c) と正面から矛盾したまま 2026-09-02 まで残っていた**。
> **削除ではなく射程明記を選んだのは、個別設計チャット向けには内容が現役だからである**(開発者判断)。

**個別設計チャット(Web 版)へ資料を渡す場合**の投入方式は **GitHub 直読を既定** とする: Web 版設計チャット(Projects)の GitHub ナレッジに
combomgr リポジトリを接続し、§1 の必須パスを直読させる。鮮度は「開発者が push し、Web 版側で
Sync now を実行した時点」。従来の **手動添付はフォールバック**(直読不能時・PC 手動投入時)として存続し、
変換規則 7 / 13〜16 の各フォールバック手順を用いる。運用手順の正本は `docs/process/remote-ops.md` を参照。

---

## あなたの役割と前提

### 役割

- 最新の `m{N}-startup-kit.md` を読み、§「変換規則」に従って書き換えた新 kit を生成
- 必要事実は他資料(change-number-registry, handover §4, playbook ヘッダ等)から取得
- Plan Mode で変換案を開発者に提示・承認を得てから新ファイルを作成

### Plan Mode 必須

本コマンドは新規ファイル作成と既存ファイル編集を伴うため **Plan Mode 必須**。
承認なしに新ファイルを作成しない / 既存ファイルを編集しない。

### セッション揮発一時ファイル(コンパクション対策)

digest 再蒸留の未適用提案一覧・progress-summary 付随更新案・Step 3 で取得した事実など、
**後段(Step 5 提示やキット完了後の別工程)までしばらく保持してからマージ・反映する中間情報**は、
長いフローの途中でコンテキスト圧縮(コンパクション)が起きると見失う恐れがある。これを防ぐため、
以下のルールで一時ファイルへ書き出して管理してよい。

- **置き場所・命名**: `tmp/ephemeral-{YYYYMMDD}-{内容}.md`(例: `tmp/ephemeral-20260716-digest-unapplied-proposals.md`)。
  リポジトリ直下の `tmp/` は `.gitignore` 済み(commit 対象外)。接頭語 `ephemeral-` により
  **該当セッション限りで揮発する資料**であることを示す。この命名・場所以外に一時ファイルを作らない。
- **永続化したくなった場合**: 揮発資料のはずだったものを永続資料(docs/ 配下等)へ昇格したくなった場合は、
  勝手に移動・改名・転記せず **必ず開発者に伝えて承認を得る**。
- **最終報告に一覧**: 作業完了時、作成した一時ファイルの一覧を最終報告に含める(開発者が破棄/昇格を判断できるように)。

---

## 実施フロー

### Step 1: 検出

§「設定」の **キット格納ディレクトリ** を `ls` で確認し、**キットファイル名パターン** に
合致するファイル一覧から最大の N を **base milestone** として特定する。

例(combomgr 現状): `m4-startup-kit.md` → base = M4。

### Step 2: ターゲット決定

- `$ARGUMENTS` にマイルストーン指定があればそれをターゲット M{N+1} として使用
- なければ Step 1 の base milestone + 1 を仮の M{N+1} として使用
- Plan Mode で開発者に M{N+1} の値を確認(誤検出の可能性を排除)

### Step 3: 事実取得

**Step 3-0(最初に実行): `code-facts.md` の再生成。** 事実取得に入る前に
`bash scripts/generate-code-facts.sh` を実行し、`docs/handover/code-facts.md`(設計担当への
引き継ぎパケットに同梱する機械的事実資料)を現行コードと一致させる。これにより生成キットには
常に最新版が同梱される。スクリプトは grep/awk/sed のみで Git 操作を含まず、frontmatter の
allowed-tools で許可済みのため承認プロンプトなしで実行できる(Plan Mode 中でも実行してよい。
生成物の commit は開発者が行う)。
**失敗時(抽出破損ガードで `exit 1`)は停止し、stderr の内容をそのまま開発者に報告する**
(`code-facts.md` を手編集して取り繕わない)。

**Step 3-0b(Step 3-0 の直後に実行): `retrospective-digest.md` の再蒸留。**
`.claude/commands/retrospective-digest-update.md` を Read し、その手順に厳密に従って実行する
(引数なし=現行 retrospective-log 全体から再蒸留)。これにより設計担当へ渡す digest(現役教訓の
蒸留版)が現状の機構化状況に最新化される。
- **前提**: 直前マイルストーンの新教訓が `retrospective-log.md` に **記録済み** であること(設計担当が
  当該マイルストーン完了時に記録する)。未記録なら記録完了を待ってから実行する(未記録のまま走らせない)。
- **権限境界の継承(重要)**: 当該サブコマンドは **`retrospective-digest.md` のみ編集**。retrospective-log
  本体・playbook への改訂は **提案として出力するだけ**(適用しない)。本キットコマンドもこの 2 ファイルを編集しない。
- **提案の繰り込み**: サブコマンドが出力した提案(log §1 昇格 / §8 圧縮 / playbook §4.x 等)は破棄せず、
  本キットの **Step 5 Plan Mode 提示**に「digest 再蒸留結果 + 未適用の提案一覧」として繰り込み、開発者の
  承認/保留を仰ぐ(キットが勝手に適用しない)。
- digest の書き込みは Step 3-0(code-facts 生成)と同様、ハンドオフ派生資料のリフレッシュとして Plan Mode
  中でも可(commit は開発者)。

**Step 3-0c(Step 3-0b の直後に実行): `progress-summary.md` の更新。**
`.claude/commands/update_progress_summary.md` を Read し、その手順に厳密に従って実行する
(ターゲット = base milestone M{N} = 直前に完了したマイルストーン。引数なしで「要約済み最終 + 1」が
M{N} に解決される)。これにより設計担当へ渡す progress-summary(後続マイルストーン担当の索引)が
直近マイルストーンまで要約済みの状態に最新化される。
- **前提**: ターゲット M{N} が `progress-log.md` に **完了記録済み**(完了マーカー存在)であること。
  未完了なら走らせず Plan Mode で停止・報告する(未完了のまま要約しない)。
- **権限境界の継承(重要)**: 当該サブコマンドは **`progress-summary.md` のみ編集**(新節追加 /
  §「持ち越し課題」更新 / §「更新履歴」更新 / 節番号シフト)。`progress-log.md` は **読み取り専用**。
  本キットコマンドもこれらを直接編集しない。
- **提案の繰り込み**: サブコマンドが出力する付随節の更新案(§「持ち越し課題」/ §「更新履歴」)は、
  本キットの **Step 5 Plan Mode 提示**に繰り込み、開発者の個別承認/保留を仰ぐ(キットが勝手に適用しない)。
- progress-summary の書き込みは Step 3-0 / 3-0b と同様、ハンドオフ派生資料のリフレッシュとして Plan Mode
  中でも可(commit は開発者)。

**Step 3-0d(Step 3-0c の直後に実行): `docs-map.md` の再生成。** 事実取得に入る前に
`bash scripts/generate-docs-map.sh` を実行し、`docs/handover/docs-map.md`(設計担当への
引き継ぎパケットに同梱する **文書ID ⇄ 実パス・役割マップ**)を現行 docs 構成と一致させる。
これにより設計担当が指示書で `DES-002` 等の文書ID で参照した資料の実パスを引けるようになり、
実装担当(コード/構成は見られるが)・設計担当双方の取り違えを防ぐ。スクリプトは grep/awk/sed のみで
Git 操作を含まず、frontmatter の allowed-tools で許可済みのため承認プロンプトなしで実行できる
(Plan Mode 中でも実行してよい。生成物の commit は開発者が行う)。
**失敗時(抽出破損ガードで `exit 1`)は停止し、stderr の内容をそのまま開発者に報告する**
(`docs-map.md` を手編集して取り繕わない)。

**Step 3-0f(Step 3-0d の直後に実行): 解決済み記録のアーカイブ退避。** マイルストーン境界は
**退避の単位**である。`bash scripts/archive-resolved.sh --milestone M{N+1}` を **まず dry-run で**実行し、
退避対象の一覧を開発者へ提示する。

- **なぜここでやるか**: 反映担当の廃止(2026-08-11)により、`parallel-board.md` ／ `followup-backlog.md` ／
  `M{N}-overview.md` は設計担当へ **その回の資料として渡される**運用になった。**渡す量が直接コストになる。**
  ところが両資料は「古い行を消さない」ことを規則にしており構造的に成長する
  (2026-08-11 実測: board 517 KB のうち §3 裁定ログが 322 KB=62%、backlog の §J は 15 行中 11 行が解決済み)。
- **退避は削除ではない。** 退避先に全文が残り、退避元には索引行が残る。退避先は
  `docs/handover/archive/followup-backlog-M{N}.md` ／ `docs/process/archive/parallel-board-rulings-M{N}.md` で、
  **M20 以降はマイルストーン単位**に分ける(アーカイブ確認時に全部を読まないで済むため)。
- **★board の裁定ログは `--board-through <D 番号>` を渡したときだけ対象になる。** 上限は**毎回判断する**——
  直近の裁定は次のマイルストーンでも参照されるため、**全部を退避しない**。省略すれば backlog だけが対象になる。
- **★マイルストーンが想定より拡大した場合の排除ルート**: `完了` 扱いだが実はまだ動いている項目があれば、
  **設計担当の宣言**として `--exclude <ID,ID,…>` を付けて退避対象から外す。dry-run の一覧を見て判断する。
- **dry-run の結果を Step 5 の Plan Mode 提示に含め、承認後に `--apply` を付けて実行する。**
  退避対象が 0 件なら何もせず次へ進む。実行後は `git diff` で確認し、検査(`bash scripts/check-doc-refs.sh` 等)を
  回してからコミットする(commit は開発者)。
- 退避が不要・不適切と判断したらスキップしてよい(**キットの生成自体はブロックしない**)。その旨を報告に書く。

**Step 3-0e(任意・添付フォールバック時のみ): `design-templates.zip` の再生成。** キットの既定投入方式は
GitHub 直読(§「投入方式」・変換規則 7/16)であり、テンプレ実体 `docs/instructions/templates/` はコミット済み
のため **zip の再生成は通常不要(本 Step はスキップしてよい)**。開発者が手動添付(フォールバック)で投入する
場合のみ、`bash scripts/generate-template-zip.sh` を実行して zip を現行テンプレ本体と一致させる。スクリプトは
zip 生成のみで Git 操作を含まず、frontmatter の allowed-tools で許可済みのため承認プロンプトなしで
実行できる(Plan Mode 中でも実行してよい。zip は .gitignore 済み・commit 対象外)。
**失敗時(`exit 1`)は停止し、stderr の内容をそのまま開発者に報告する**(zip を手作業で作らない)。

以下からは **読み取りのみ** で事実を取得する。**ファイルパスが combomgr 固有のため、
他プロジェクトへの流用時は要書き換え**:

| # | 取得元 | 取得内容 |
|---|--------|---------|
| a | `docs/design/supp-001-detailed-design.md §4.1`(マイルストーン分割表) | M{N+1} のスコープ名(例: M5 = 比較系、M6 = 初期体験系、M7 = 仕上げ) |
| b | `docs/handover/change-number-registry.md` 最新行 | 直近で起票された CHANGE 番号範囲、改訂された設計書 + バージョン |
| c | `docs/handover/m{N}-to-m{N+1}-handover.md §4`(または最新の handover §4) | 持ち越し課題 ID 一覧と概要 |
| d | `docs/handover/design-instruction-playbook.md` ヘッダ | playbook 最新バージョン |
| e | `docs/design/supp-001-detailed-design.md` ヘッダ | SUPP-001 最新バージョン |
| f | `docs/instructions/M{N}-*.md` 一覧(M{N} = base milestone) | M{N} の最後のサブマイルストーン指示書ファイル名(複数なら直近 2 件)。※ 書式手本はテンプレ直読(変換規則 7)が代替するため個別投入の対象外。実物の参照が別途要るときは直読パス候補として把握する |
| g | `docs/instructions/reviews/M{N}-*-review-checklist.md` 一覧 | f に対応するレビューチェックリストファイル名。※ f と同様、個別投入の対象外 |
| h | `docs/progress/progress-summary.md` 最新節 | Step 3-0c で更新済みのはずなので、対象範囲が M0〜M{N} になっていることを確認する(後続マイルストーン担当の索引) |
| i | `docs/handover/code-facts.md` ヘッダ(生成日・対象コミット) | Step 3-0 で再生成済みのはずなので、ヘッダの生成日・対象コミットが現行 HEAD と一致することを確認する。設計担当(コード閲覧不可)向け機械的事実資料(変換規則 13 参照) |
| j | `docs/handover/docs-map.md` ヘッダ(生成日) | Step 3-0d で再生成済みのはずなので、ヘッダの生成日が当日であることを確認する。設計担当(ファイル構成閲覧不可)向けの文書ID ⇄ 実パス・役割マップ(変換規則 15 参照) |
| k | `docs/instructions/templates/` の内容 | `ls docs/instructions/templates/` で収録テンプレ(instruction / review-checklist / milestone-overview / phase-overview / research / change-notification / change-report の各 `*.template.md` + README = 8 件)が揃っていることを確認する(直読対象の実在確認)。設計担当へ渡す「各成果物の書式・粒度の手本」(変換規則 7 参照)。添付フォールバック時のみ Step 3-0e の zip を `unzip -l` で確認 |

取得した事実は **Plan Mode で開発者に明示** する(誤認識が混入しないよう逐一確認)。

### Step 4: 変換案作成

§「変換規則(20 項目)」に従い、base kit の内容を機械的に書き換えた新 kit 案を
**メモリ上に** 生成する(ファイル書き込みは Step 6 まで保留)。項目 12(シニアエンジニアのロール指定)は
機械置換ではなく当該マイルストーンの技術領域に合わせた文言で必ず付与すること。

### Step 5: Plan Mode 提示

> **Plan Mode タイムアウト対策（未回答事項の扱い）**
>
> Claude Code の仕様変更により、Plan Mode は一定時間で自動タイムアウトし、開発者の回答を得ないまま Plan Mode を抜けて「編集承認」扱いになることがある。これを防ぐ手段は無いため、以下で代替する。
>
> 1. **未回答・未決の事項は、本 Step 5 提示（プラン）の末尾に「■ 未決事項（要回答）」として箇条書きで必ず明示し、開発者の返答を待つ。** 各項目に (a) 何が未決か (b) こちらの推奨案・暫定案 (c) 回答が無いと何が困るか を書く。
> 2. **重要な未決事項を抱えたまま承認された場合（タイムアウトによる自動承認を含む）は、Step 6 以降（成果物生成・既存ファイル編集）に進まない。** 代わりに「重要な未決事項が残っているため作業を保留しました。以下について回答をお願いします」と連絡し、未決事項を再掲して停止する。
> 3. 推奨案どおり進めても実害の無い軽微な事項に限り、推奨案を採用した旨を明記した上で続行してよい。重要度の判断に迷う場合は「重要」側に倒し、停止して確認する。

以下 8 項目を開発者に提示し、承認を得る:

1. **ターゲット M{N+1} の確認**(自動検出または $ARGUMENTS 指定)
2. **Step 3 で取得した事実の一覧**(CHANGE 番号範囲、改訂された設計書 + バージョン、持ち越し課題 ID 一覧、直近指示書ファイル名、各ファイルのバージョン)
3. **「ドキュメント体系について」段落の処理方針**:
   - 削除(M5 以降、ドキュメント体系の変更がない場合)
   - 簡略化(「恒久情報は B グループに分離済み」程度の定常文に短縮)
   - 維持(再度ドキュメント体系の大改変があった場合)
4. **副成果物の処理**(承認は個別):
   - `README.md §1`(「現在は最新...」)と `§6 改訂履歴` の更新
   - `milestone-startup-guide.md §3.{N+1}` 雛形追加(節タイトル + 投入推奨資料スタブ)
5. **digest 再蒸留結果と未適用の提案**(Step 3-0b 由来):
   - `retrospective-digest.md` の更新差分サマリ(現役教訓の増減 / 機構化済みへ移動した項目)
   - サブコマンドが出した **未適用の提案**(retrospective-log §1 昇格 / §8 圧縮 / playbook §4.x 等)を一覧提示し、開発者の承認/保留を仰ぐ。**承認されたものは本キット完了後に別工程で反映**(本コマンドは log/playbook を編集しない)
6. **progress-summary 更新結果と提案**(Step 3-0c 由来):
   - `progress-summary.md` に追加された新節 §{N} の内容サマリ
   - サブコマンドが出した付随節の更新案(§「持ち越し課題」/ §「更新履歴」)を個別承認可で提示(不要なら skip)
7. **プロンプト本文の必須要素の反映確認**(脱落の再発防止チェック):
   - 変換規則 7: 設計テンプレ集(`docs/instructions/templates/` 直読)が **(A) §1 必須表・(B) §2 プロンプト本文の一文 の 2 箇所** に入っているか(個別実物の添付運用・旧 zip 必須運用に戻っていないか)
   - 変換規則 16: §2 プロンプト末尾の **「投入資料の確認」ブロック(直読方式)**(直読マニフェスト照合・パス単位の不足報告・必須揃うまで成果物生成ゲート・添付フォールバック手順)が入っているか
   - 変換規則 12: 設計担当ロール指定が §2 冒頭に入っているか
   - 変換規則 17: CLAUDE.md が **必須表・受領マニフェストに残っていないか**(任意へ降格済みか・必須件数を再計算済みか)
   - **変換規則 20: 節限定の読み方が機構になっているか**(★2026-08-12 新設) — **(A)** §1 の各行に **読む単位(全文 / 節限定)** が書いてあるか **(B)** 節限定の行が **`grep -n '^## '` → `Read(offset/limit)` の 2 手**で書かれているか(**行番号の直書きは不可**。伸びた瞬間に失効する) **(C)** **履歴節 5 本**(`parallel-board` §9 ／ `change-number-registry` §4 ／ `architecture-patterns` §12 ／ `playbook` 更新履歴 ／ `progress-summary` §20)が **「読まない」と明記**されているか **(D)** 節番号を base kit から**写さず実査したか**(`grep -n '^## '`) **(E)** キット本文が効果を**「量が減る」で説明していないか**(裁定 15。書いてよいのは **起動時のコンテキスト占有**と**現在値と履歴の混在による正確性**の 2 つだけ)
   - **変換規則 19: 面の読み替えが済んでいるか**(★2026-08-11 新設) — **(a)** §1 の「ナレッジ常設 / 直読」二分割が**「起動時に必ず読む / 必要時に引く」へ置き換わっているか** **(b)** §2 冒頭に **`bash scripts/design-desk-arm.sh`(武装)が最初の 1 手として**入っているか **(c)** 「Sync now」「GitHub 直読」「添付フォールバック」が**親向けの手順として残っていないか**(個別設計チャット向けの注記としてなら可) **(d)** 並列時に**親を 2 つ立てるキットになっていないか** **(e)** §2 冒頭の**武装の直後**に **`bash scripts/check-derived-docs.sh`(規則 19 (I))**が入っているか——コマンドだけでなく **3 要素が揃っていること**を見る: **出力の読み方**(「源泉が新しい」= 丸ごと無効ではない。**変化量**が小さければ変わった領域だけ実物確認)／**成果物生成のゲートにしない**(古い資料があっても止めない)／**`code-facts` は武装中に再生成できない**(源泉が作業ツリーに無く `generate-code-facts.sh` が exit 2。武装していないセッションか開発者へ回す)
8. **ドキュメント棚卸の結果**(★2026-08-13 新設。**マイルストーン境界が棚卸の自然な機会である**):
   - `bash scripts/check-doc-inventory.sh` を実行し、**型に無いファイル**の一覧を提示する(**常に exit 0。ゲートにしない**)
   - **M{N} 期に作られた作業用ファイルで役目を終えたもの**を挙げ、**`docs/handover/phase{P}/` への移設**を提案する(**削除ではない**)。判定材料は **(a) 実質の参照元**——**`docs-map.md` は自動生成で `docs/handover/` 直下を全件列挙するため数に入れない** **(b) 本文が自ら宣言した寿命**(例「本書の寿命は M{N} クローズまで」)
   - **★参照ゼロで判定しない**——滞留するファイルは複数箇所から参照されており、**参照元が「これは消す予定」という行である**ことが多い。**参照されている ≠ 生きている**(2026-08-13 実測)
   - **移設・削除の実行は開発者の手番**(D-196 境界条件 3)。**キットは候補と根拠を出すところまで**

### Step 6: 書き込み(主成果物)

Step 5 で承認後、以下を **新規作成**:

- `<キット格納ディレクトリ>/m{N+1}-startup-kit.md`

既存の `m{N}-startup-kit.md`(および過去キット)は **編集しない**(コピー元として履歴保持)。

### Step 7: 副成果物(Step 5 で承認された場合のみ)

- `<関連 README.md>` を編集:
  - §1 の「現在は `m{N}-startup-kit.md` ... が最新」を `m{N+1}-startup-kit.md` に更新
  - §6 改訂履歴に新エントリ追加(日付 = 実行日、内容 = 新 kit 作成の旨)
- `<関連 startup-guide>` を編集:
  - §3.{N+1} 雛形を追加(節タイトル「M{N+1} 着手時(<スコープ名>)」+ 投入推奨資料スタブ)
  - 内容詳細(重点節等)は開発者と Web 版設計担当の協議で後埋め

---

## 変換規則(20 項目)

base kit (`m{N}-startup-kit.md`) → target kit (`m{N+1}-startup-kit.md`) の書き換え規則。

> **★まず規則 19 を読むこと(2026-08-11 新設)。** 親(設計卓)の面が Web 版 → Claude Code へ変わったため、
> **規則 7 / 13 / 14 / 15 / 16 / 17 の前提(GitHub 直読・プロジェクトナレッジ・Sync now・添付フォールバック・
> 「コード閲覧不可」「ファイル構成閲覧不可」)が部分的に失効している**。規則 19 が読み替えを定める。

| # | 項目 | 変換内容 |
|---|------|---------|
| 1 | マイルストーン番号・スコープ名 | base 内の `M{N}` → `M{N+1}`、`M{N-1}` → `M{N}` 等を文脈に応じて差し替え。スコープ名(セットプレイ系 等)は Step 3-a で取得した M{N+1} のスコープ名に差し替え |
| 2 | handover ファイル名 | `m{N-1}-to-m{N}-handover.md` → `m{N}-to-m{N+1}-handover.md`(両方の N を変換) |
| 3 | 「読む順序」参照 | `m{N-1}-to-m{N}-handover.md §0` → `m{N}-to-m{N+1}-handover.md §0` |
| 4 | 持ち越し課題 ID | base の持ち越し課題列挙 → Step 3-c で取得した最新の ID 一覧に差し替え。**0 件の場合は「持ち越し課題なし」と明示** |
| 5 | CHANGE 起票・設計書改訂事実 | base の「M{N-1} 期間で CHANGE-0XX を起票、DES-00X を vX.Y.Z に改訂」相当を Step 3-b の最新事実に差し替え。**改訂なしの場合は「M{N} 期間で CHANGE 起票なし、設計書本体改訂なし」と明示** |
| 6 | progress-summary 対象範囲 | `M0〜M{N-1}` → `M0〜M{N}` |
| 7 | 設計テンプレ集(テンプレ実体の直読・**2 箇所・必須・欠落時は復元**) | 設計担当が M{N+1} の各成果物を書く際の **書式・粒度の手本** として、テンプレ実体 `docs/instructions/templates/`(instruction / review-checklist / milestone-overview / phase-overview / research / change-notification / change-report の各 `*.template.md` + README = 各成果物の骨格 + 記入指針 + ミニ実例)を **GitHub 直読で参照させる**。**以下 2 箇所に必ず反映する**: **(A)** §1「必須(直読)」表に「設計テンプレ集 `docs/instructions/templates/`(各成果物のテンプレを収録・GitHub 直読)」行、**(B)** §2 最初のプロンプト本文に「M{N+1} の指示書・レビューチェックリスト・CHANGE 通知書等は、`docs/instructions/templates/` 内の対応テンプレの章立て・記入指針に従って作成してください」相当の一文。**従来の「直近指示書 + レビューチェックリストを個別添付してテンプレ例にする」運用は行わない**(実物の参照が要る場合は直読パスを示す)。base kit が旧運用(zip 添付・個別実物添付)のままなら直読方式へ置換する。**置換だけで済ませず、(A)(B) のいずれかが脱落していれば追加して復元する**(過去 m7→m9 で (B) の一文が脱落し例示プロセスが失われた再発防止)。「テンプレは各成果物の型で、最終的な正は Playbook §2 等の運用ルール」の注記を付す。加えて **§2 プロンプト本文に「テンプレで書式・粒度が不十分な場合は、書こうとしている成果物と性質の近い過去の実物(ファイル名・マイルストーンを特定し、どのテンプレのどの節が不足かを添えて)を直読で読む。直読で参照できない場合のみ開発者に投入を請求してよい」旨の一文を含める**(templates/README の該当節と対応)。**フォールバック(添付投入時)**: 手動添付で投入する場合のみ、Step 3-0e で再生成した `design-templates.zip` を 1 件として添付する(2026-07-20 直読切替・旧 zip 運用は後方互換) |
| 8 | バージョン注記 | playbook vX.Y.Z, SUPP-001 vX.Y.Z 等を Step 3-d / e の最新値に差し替え。M{N} 期間で改訂がなければ現行版を維持 |
| 9 | 「ドキュメント体系について」段落 | base に M3→M4 一度きりの記述があれば、Step 5-3 の開発者判断(削除 / 簡略化 / 維持)に従って処理 |
| 10 | 「M{N-1} プロンプトからの主な改訂点」表(末尾) | 表のタイトルを `M{N} プロンプトからの主な改訂点` に変更し、行内容を本コマンドが実施した変換項目(マイルストーン番号進捗、持ち越し更新、CHANGE/設計書改訂反映、ドキュメント体系処理 等)に差し替え。一度きり項目(役割の根拠変更 等、M3→M4 特有)は **削除** |
| 11 | 改訂履歴節(キット内に存在する場合) | 新エントリ追加(日付 = 実行日、内容 = "M{N+1} 用に M{N} 起動キットから流用作成") |
| 12 | 最初のプロンプトのロール指定(必須) | §2「最初のプロンプト」の冒頭に、設計担当 Claude の出力品質を高める**シニアエンジニアとしてのロール指定を必ず含める**。当該マイルストーンの技術領域(例: UI・画面設計 / API 設計 / データモデル・スキーマ設計 / 状態管理 / バリデーション設計、まれに HTML パース / データ変換 等)に精通したシニアソフトウェアエンジニアである旨に加え、(a)妥当性・保守性を多角的に検討する、(b)自明な前提でも一度立ち止まって検証する、(c)トレードオフと判断根拠を明示する、(d)不確かな点は推測で埋めず確認事項として挙げる、(e)品質に妥協しない、の姿勢を明記する。base kit に既存のロール指定があれば当該マイルストーンの技術領域に合わせて文言調整し、無ければ新規に追加する。playbook §1 の「設計担当 Claude」の役割定義と矛盾しない範囲で、品質向上のための上乗せとして付与する |
| 13 | 投入推奨資料への code-facts.md 追加(必須) | 生成するキットの**投入推奨資料(設計担当へ渡す資料リスト)に `docs/handover/code-facts.md` を必ず含める**。同資料は設計担当 Claude(コード閲覧不可)向けの **機械的事実の参照元**(React コンポーネント Props / queryKey / react-router ルート / Go ハンドラ↔ルート / config 構造体 / 共通ナビリンク)であり、retrospective-log §1 パターン A/C「実コード確認の省略」を防ぐ目的。併せて「**投入直前に `/regen_code_facts` で再生成し、開発者が commit+push した版を直読させること(Web版側の Sync now も忘れない。添付フォールバック時は再生成版を貼る)**」「最終的な正は常に実コードで、本資料は静的抽出ゆえの限界(冒頭『本資料の限界』節)がある」旨の注記を付す。base kit に既出なら鮮度・注記のみ更新、無ければ追加する |
| 14 | 投入推奨資料への retrospective-digest.md 追加(必須) | 生成するキットの**投入推奨資料に `docs/handover/retrospective-digest.md` を必ず含める**。これは設計担当が指示書執筆時に毎回読む **現役教訓の蒸留版**(設計担当ミスの再発防止)。併せて「**投入直前に `/retrospective-digest-update` で再蒸留(Step 3-0b)し、開発者が commit+push した版を直読させること(添付フォールバック時は最新版を貼る)**」「機構化済みの教訓は本書から除外され code-facts / playbook が自動で強制する」旨を注記する。**`docs/handover/retrospective-log.md` は起動時の必読から外す(任意参照のまま)**、設計担当の **起動時必読** は digest とする(log は全期間の全量記録で毎回通読しない)。ただし単に『アーカイブ参照』とだけ書かず、キットには以下 3 点を必ず明記して **更新責任の所在を示す**: **(1) retrospective-log の更新責任は設計担当(Web 版)にある**(マイルストーン完了時に当該期間のミス・教訓を追記)、**(2) digest は設計担当にとって読み取り専用で、その更新は Claude Code が `/retrospective-digest-update` で行う**、**(3) log は更新が必要になったとき/事例を遡るときに開発者へ投入を要求して読む・追記する**。base kit に既出なら鮮度・注記のみ更新、無ければ追加する |
| 15 | 投入推奨資料への docs-map.md 追加(必須) | 生成するキットの**投入推奨資料に `docs/handover/docs-map.md` を必ず含める**。同資料は設計担当 Claude(リポジトリのファイル構成を直接見られない)向けの **文書ID ⇄ 実パス・役割マップ**(`DES-002` 等の文書ID → 実パス逆引き + docs 配下のディレクトリ別役割)で、設計担当が指示書で文書ID 参照した資料の実パス確認に使い、実装担当の無駄な探索・「読まずに着手」による品質低下を防ぐ目的。併せて「**投入直前に `/regen_docs_map` で再生成し、開発者が commit+push した版を直読させること(添付フォールバック時は再生成版を貼る)**」「最終的な正は常に実ファイルで、本資料は静的抽出ゆえの限界(冒頭『本資料の限界』節)がある」旨の注記を付す。base kit に既出なら鮮度・注記のみ更新、無ければ追加する |
| 16 | 投入資料の確認ブロック(直読方式・必須) | §2 最初のプロンプトの **末尾に「投入資料の確認」ブロックを必ず置く**。既定は GitHub 直読方式で、手順を定義する: **(1)** §1 投入資料一覧の **必須パスを「直読マニフェスト」とみなし**、GitHub 連携(プロジェクトナレッジ等)で各パスが実際に読めるかを確認し、読めたものを列挙する(読めた分は読み込み・内容理解を始めてよい)。**(2)** 読めないパスは即「欠落」と断定せず、**開発者側の Sync now 未実施・ナレッジ選択漏れ・push 漏れを疑い、パス単位で具体的に** 報告して同期・追加を求める(「いくつか読めない」等の曖昧報告は禁止)。**(3)** **必須パスが全て読めるまで CHANGE 通知書・指示書・設計判断などの成果物生成を開始しない**(不足のまま想定で進めない=本プロジェクトが最も警戒する「実態を確認せず想定で進める」アンチパターン。この段階は読み込み・内容理解・確認に留める。任意資料は未読でも着手してよい)。**(4)** 必須分が揃ったら「直読版の鮮度=最終 push+Sync 時点」の認識を含め一行で報告し、当該マイルストーン M{N+1} の **着手前確認事項リストの提案**へ進む。**必須パス件数・着手前確認の具体項目は生成するキットの実値に置換する**。設計テンプレ集は `docs/instructions/templates/` **1 件(ディレクトリ)** として数える(収録テンプレを個別に数えない)。併せて §1 見出し・本文の「Web チャットへアップロード」「添付」文言を直読前提(「GitHub 直読対象パス」等)へ改める。**フォールバック(添付投入時)**: 手動添付で投入する場合は従来の「添付ファイルの受領確認」手順(受領マニフェスト照合→添付の再走査→ファイル名単位の不足報告→必須揃うまで成果物生成ゲート。`m10-startup-kit.md` §2 末尾を範とする。テンプレ集は `design-templates.zip` 1 件として数える)を用いる。base kit が添付方式のままなら直読方式へ置換する(2026-07-20 切替) |
| 17 | CLAUDE.md の任意化(必須→任意へ降格・必須) | 生成するキットで **CLAUDE.md を必須投入から外し、任意(要望時投入)へ降格する**。base kit の §1 必須表に CLAUDE.md があれば任意側へ移動し、「Web 版設計担当が CLAUDE.md を改訂する運用は終息・安定したため任意化(2026-07-16 開発者方針)。概要把握・改訂支援等で必要になったら開発者へ投入を請求する」旨の注記を付す。§2 プロンプト本文のマニフェスト列挙からも必須扱いを外し、**起動時未投入=請求時投入** とする(retrospective-log と同じパターン)。**投入資料の確認ブロック(規則 16)の必須件数を再計算する**(CLAUDE.md 分 −1)。base §2 依頼事項に「CLAUDE.md・settings.json は整備済み。改訂が要れば改訂支援」相当の記述がある場合は維持してよいが、CLAUDE.md 本体は起動時未投入である前提に文言を整合させる |

| 19 | **親の面が Claude Code になったことによる読み替え(必須・2026-08-11)** | **親(設計卓)は Claude Code(メインツリー)で動く**。Web 版は親をやめ `parallel-ops` §06 の**個別設計チャット**へ配置換えされた(開発者裁定 2026-08-11。正本 `docs/progress/20260811-design-desk-surface-study.md` §9)。**生成するキットは次の形にする**——**(A) §1 の「Ⅰ プロジェクトナレッジ常設 / Ⅱ セッション固有・直読」の二分割を廃止する**。Code 版にナレッジも Sync now も存在せず、**線引きの根拠だった「検索は取りこぼす」(`parallel-ops` §10)が消えるため**。代わりに **「起動時に必ず読むパス」と「必要時に引くパス」** の 2 列にする(**取りこぼしではなく、読ませすぎを防ぐのが新しい理由**)。**(B) §2 プロンプトの冒頭に武装手順を置く**＝`bash scripts/design-desk-arm.sh` を最初の 1 手として実行させ、`--status` の出力を報告させる。**武装しないまま成果物を作らせない**(規則 16 のゲートを、資料の受領確認から**武装の確認**へ置き換える)。**(C) 規則 13 は有効のまま維持する**——`code-facts.md` の根拠「設計担当はコード閲覧不可」は**武装により今も真**である(見えるようになったのは `docs/` だけ)。**ただし「commit + push して直読させる / Sync now」の手順は削る**(Code は HEAD をそのまま読む)。**(D) 規則 15 の根拠を書き換える**＝`docs-map.md` は「ファイル構成を直接見られない」ためではなく、**文書ID ⇄ 実パスの逆引きと、docs 配下の役割マップ**として要る。**(E) 規則 7 は「GitHub 直読」を「Read で読む」に読み替える**(テンプレ実体 `docs/instructions/templates/` を渡す趣旨は不変。**(A)(B) の 2 箇所必須は維持**)。**(F) 規則 17(CLAUDE.md 任意化)は維持する**——Code では読めるが、**読ませないことが目的**なので任意のままにする。**(G) 添付フォールバックの記述は個別設計チャット向けとして残す**(Web 版は今も添付・直読で動くため)。**(H) 並列マイルストーンでも親は 1 つ**——契約の発行者が 1 人であることが DES 所有権の排他性を構造的に保証している(`parallel-ops` §04)ため、**M{N} 用と M{N+1} 用に親を分けるキットを作らない**。**(I) ★§2 プロンプトの冒頭(武装の直後)に `bash scripts/check-derived-docs.sh` を置く**——**派生資料のメンテはこれまで本コマンドの Step 3-0 に埋まっており、トリガがマイルストーン境界に固定されていた**。M18/M19/M14 残りを並列で進めた期間は境界が来ず、`retrospective-digest` 等が長期間止まって**設計卓が「完全に陳腐化した」と誤解し、資料を使わなくなった実害が出ている**。**セッション起動ごとに回せば境界から切り離せる。** 出力の読み方も §2 に書く: **「源泉が新しい」は資料が丸ごと無効という意味ではない。変化量(例 10/875 件)が小さければ、変わった領域だけ実物で確認し他の節はそのまま使う**。**★ただし `code-facts` は武装中に再生成できない**(源泉が作業ツリーに無いため。`generate-code-facts.sh` が exit 2 で停止する)——**再生成が要ると分かったら、武装していないセッション(製造・改善)または開発者へ回す**。`docs-map` は `docs/` だけを読むので武装中でも再生成できる |
| 20 | **節限定の読み方を機構にする(必須・2026-08-12)** | §1 の必読表は `change-number-registry.md §1(counter)` のように**節を限定して指示している**が、**`Read` は既定でファイル全体を読む**——**節限定が散文にとどまっており、機構になっていない**。生成するキットは次の 3 つを満たすこと。**(A) §1 の各行に「読む単位」を明示する**——**全文**か**節限定**かを行内に必ず書く(既定は全文。節限定にするのは、その資料に**現在値と履歴・経緯が同じ平面に並んでいる**場合)。**(B) 節限定の行には実行可能な手順を書く**——**★行番号を直書きしない**(資料が伸びた瞬間に失効し、本規則が直そうとしている陳腐化そのものを持ち込む)。代わりに **2 手**で書く: **`grep -n '^## ' <path>` で節の開始行を得る → `Read(<path>, offset=<当該節の行>, limit=<次節の行 − 当該節の行>)`**。**(C) 履歴節を「読まない」と明記する**——起動時必読のうち**履歴節を持つのは 5 本**(`parallel-board` §9 改訂履歴 ／ `change-number-registry` §4 更新履歴 ／ `architecture-patterns` §12 更新履歴 ／ `design-instruction-playbook` 更新履歴 ／ `progress-summary` §20 更新履歴)。**必要になったら §「必要時に引くパス」側から引く**。**★効果を「量が減る」で説明しないこと**——読ませる量と総トークンの相関は A/B 実測で **+0.03%／+1.9% しか動かない**(開発者裁定 15・`docs/progress/20260811-context-load-ab-pilot.md`。エージェントは指示どおりに読まず検索で引くため)。**本規則の効果は 2 つに限られる**: **(a) 必読指定されたファイルは全体が読まれうるため、起動時のコンテキスト占有が実コストになる** **(b) 現在値と履歴が同じ平面に並ぶため、検索で引いた 1 行が「今もそう」か「当時そうだった」か構造的に区別できない**——**(b) は 2026-08-12 のセッションで実際に 2 回踏んでいる**(`m20-kit-derived-docs-line` の原因記述が「同時並行だったから」となっていたが実際は構造的原因だった件 ／ `.codex/hooks.json` の「同期済み」が**一度書かれて再検証されなかった記録**で、実際は一度も発火していなかった件)。**★生成時に節番号を実査する**——base kit の節指定をそのまま写さず、`grep -n '^## ' <path>` で**当該節が今もその番号で存在すること**を確認してから書く(節番号は資料の改訂で動く。写すだけだと誤った節を読ませる) |
| 18 | progress-summary の必須投入を維持(必須) | **`docs/progress/progress-summary.md` を必須投入から外さない**(2026-08-11 開発者裁定①)。規則 17 で CLAUDE.md を任意へ降格した前例があるため、**同じ流れで progress-summary を落とさないこと**を明示規則にする。あわせて生成キットの当該行へ注記を付す: 「**起動時に読む資材である**(親が読む progress 資料はこれだけ。`progress-log.md` と個別の完了報告・レビュー報告は読まない)。**進行中の現在地は `docs/process/parallel-board.md` を見る**——同じ状態が両方にあるときは board を優先し、本書は**マイルストーン境界時点の記録**として読む」。**鮮度の注記(「M0〜M{X} まで要約済み」等)は base kit の実測値を引き継がず、生成時点の `progress-summary.md` を実査して書き直す**(base kit の注記をそのまま写すと未要約範囲の記述が失効したまま渡る)。裁定の全文は `docs/progress/20260811-progress-doc-responsibility-rulings.md` |

### 単純置換できないケース(重要)

機械的 sed 置換で壊れるため、**文脈を踏まえて変換**:

- `M1〜M{N-1} の全指示書` → `M1〜M{N} の全指示書`(過去範囲表現、境界は N+1 ではなく N)
- `M{N-1} まで完了 → M{N} から引き継ぎ` → `M{N} まで完了 → M{N+1} から引き継ぎ`(両側変換)
- `M{N-1} のサブマイルストーン M{N-1}-04 / M{N-1}-05` → `M{N} のサブマイルストーン M{N}-XX / M{N}-YY`(M{N} の実際の末尾サブマイルストーン番号で差し替え)

### 一度きりコンテンツの判定

base が `m4-startup-kit.md` の場合、以下は M3→M4 特有の一度きりコンテンツ:
- 「ドキュメント体系について: M3→M4 移行時にドキュメント整理を行いました...」段落
- 改訂点表の「役割の根拠を `handover_1.md` から `playbook §1` に変更」「ファイル群を A〜E に再編」等

これらは Step 5-3 で開発者に処理方針を確認する。M5 以降の連続実行では、base に既に簡略化済みの内容が
入っているため、この判定は省略可能(`base 内に対応コンテンツがなければスキップ`)。

---

## $ARGUMENTS の解釈ルール

| パターン | 例 | 動作 |
|---------|------|------|
| **空** | `(なし)` | 自動検出 + Plan Mode 確認 |
| **マイルストーン指定** | "M5" / "5" / "m5" | ターゲット M{N+1} として使用、自動検出より優先 |
| **補足指示** | "ドキュメント体系について段落は完全削除して" | Step 5 の方針判断に反映 |
| **複数情報の混在** | "M5 持ち越しは L-04 のみ" | マイルストーン指定 + 補足指示として分解処理 |
| **その他/曖昧** | 解釈一意でない | Plan Mode で開発者に確認 |

**$ARGUMENTS でも以下は突破不可**:
- 既存 kit ファイル(`m{N}-startup-kit.md` 等)への編集
- 編集禁止ファイル(下記)への編集
- Git 操作・データ破壊系コマンド

---

## 権限境界・禁止事項

### Git 操作の禁止

- commit / push / merge / rebase / tag / reset --hard / push --force 等は **すべて開発者が行う**
- Claude は変更提案・編集に留め、Git コマンドを直接実行しない
- ブランチ作成・切替も開発者(例外: 開発者から明示的な指示があった場合のみ)

### データ破壊系コマンドの禁止

- `rm -rf` 系の致命的削除
- `git reset --hard` / `git clean -fd` 等の作業破壊
- `chmod 777` の再帰適用
- データベースファイル等の直接削除
- `curl ... | bash` / `wget ... | sh` 等の外部スクリプト直接実行
- `sudo` の使用

### 編集可能ファイル(本コマンドが新規作成・編集する)

- **新規作成のみ**(本コマンドの主成果物):
  - `<キット格納ディレクトリ>/m{N+1}-startup-kit.md`
- **既存ファイル編集**(Step 5 で個別承認された場合のみ):
  - `<関連 README.md>`(§1 と §6 改訂履歴の追記)
  - `<関連 startup-guide>`(§3.{N+1} 雛形追加)
- **委譲サブコマンド経由の更新**(本コマンド本体は直接編集しない):
  - `docs/handover/retrospective-digest.md` は Step 3-0b で `/retrospective-digest-update` を実行することで更新される。同サブコマンドの権限境界(digest のみ編集・log/playbook は提案のみ)に従う。
  - `docs/progress/progress-summary.md` は Step 3-0c で `/update_progress_summary` を実行することで更新される。同サブコマンドの権限境界(progress-summary.md のみ編集・progress-log.md は読み取り専用)に従う。
- **スクリプト生成物(commit 対象外)**:
  - `docs/instructions/templates/design-templates.zip` は Step 3-0e で `bash scripts/generate-template-zip.sh` により再生成される(テンプレ本体からの決定論生成・.gitignore 済み)。テンプレ本体(`docs/instructions/templates/*.template.md` / `README.md`)は設計担当・整備担当が保守し、本コマンドは編集しない。
- **セッション揮発一時ファイル(commit 対象外)**:
  - `tmp/ephemeral-*.md` の新規作成・編集可(§「セッション揮発一時ファイル(コンパクション対策)」の運用ルールに従う。永続化への昇格は必ず開発者承認)。

### 編集禁止(読み取りのみ)

- 既存の `m{N}-startup-kit.md` および過去キット(コピー元・履歴保持)
- 設計書本体(`docs/design/requirements.md`, `docs/design/01-tech-stack.md` 〜 `docs/design/06-validation.md`)
- `docs/design/supp-001-detailed-design.md`(ヘッダ・§4.1 参照のみ)
- `docs/handover/` 配下の継続更新ファイル(`retrospective-log.md`, `architecture-patterns.md`, `change-number-registry.md`)
- `docs/handover/design-instruction-playbook.md`(ヘッダ参照のみ)
- `docs/handover/m{N}-to-m{N+1}-handover.md` 系(§4 / §0 参照のみ)
- `docs/handover/archive/` 配下、`docs/handover/cleanup-assistant-prompt.md`
- `docs/instructions/` 配下、`docs/instructions/reviews/` 配下、`docs/change-notes/` 配下(例外: `docs/instructions/templates/design-templates.zip` の再生成のみ Step 3-0e で許可。テンプレ本体 `*.template.md` / `README.md` は編集しない)
- `docs/progress/` 配下(progress-summary.md を除く、参照のみ。progress-summary.md は Step 3-0c で `/update_progress_summary` 経由でのみ更新)
- `docs/postmortem/` 配下、`.claude/settings.json`、`.claude/settings.local.json`
- 本コマンドに明示的に列挙されていないすべてのフォルダ・ファイル

### 判断保留時の停止

- 取得した事実が不明確、$ARGUMENTS の解釈が曖昧、変換規則の適用判断に迷う場合は **Plan Mode で停止** して開発者確認
- 推測で進めず、必ず開発者承認を得る
- 設計担当 Claude Web 版が受け取るキットの品質はプロジェクト進行に直結するため、**事実取得の正確性は特に慎重に**

---

## 進め方の最初の一歩

引数 `$ARGUMENTS` の有無に関わらず、以下から開始:

1. §「設定」の **キット格納ディレクトリ** を `ls` で確認、最新 `m{N}-startup-kit.md` を特定(Step 1)
2. `$ARGUMENTS` の解釈(空 / マイルストーン指定 / 補足指示 / 混在)を整理
3. Step 2(ターゲット M{N+1} 決定)、Step 3(派生資料リフレッシュ 3-0〜3-0e + 読み取り事実取得)を実施
4. Step 4 で変換案をメモリ上に作成
5. **Plan Mode で Step 5 の確認事項 8 項目(ターゲット / 事実 / 一度きり段落処理 / 副成果物処理 / digest 再蒸留結果 / progress-summary 更新結果 / プロンプト本文の必須要素〔テンプレ直読 2 箇所・投入資料確認ブロック・ロール指定・CLAUDE.md 任意化〕/ ドキュメント棚卸の結果)を開発者に提示**
6. 承認後 Step 6(主成果物の新規作成)、必要に応じて Step 7(副成果物の編集)を実施

不明点があれば遠慮なく開発者に確認してください。**親(設計卓＝Claude Code)が受け取るキットの品質に直結する**ため、
事実取得の正確性は特に慎重に進めてください。

---

## 経緯メモ

### 2026-09-02: 「面の移行」の反映漏れを是正(射程明記)＝`D-681`

**何が起きていたか**: §「投入方式(2026-07-20 直読切替)」は、**親(設計卓)が Web 版だった時期の前提**
(GitHub ナレッジへ接続し Sync now で鮮度を取る)で書かれていた。**2026-08-11 に親は Claude Code へ移り**、
同じファイルの **変換規則 19** が「Sync now / GitHub 直読 / 添付フォールバックが**親向けの手順として**残っていないか」
を検査項目に掲げているにもかかわらず、**§「投入方式」自身はその検査の対象外だった**
(検査は**生成するキット**を見るものであり、**コマンド定義そのもの**は見ない)。

**なぜ発火しなかったか**: 面の移行のとき、**規則側だけを直して前提側を直さなかった**。
規則は「生成物がこうなっているか」を問う形をしており、**コマンド定義の地の文が同じ前提に乗っていることを
拾う仕掛けが無い**。`check-doc-refs.sh` は dead file reference しか見ないため、
**参照先が生きている限り、内容が失効していても緑になる**。

**どう直したか**: **削除ではなく射程明記**。個別設計チャット(Web 版)向けには内容が現役であるため、
節の頭に「**本節の射程は個別設計チャットに限る／親向けキットには適用しない**」を置き、
本文の主語を「個別設計チャットへ資料を渡す場合」へ限定した。
変換規則 19 の「**個別設計チャット向けの注記としてなら可**」に沿う形である。

**同じ前提に乗っていた記述をもう 1 件見つけた**: 末尾の
「設計担当 Claude Web 版が受け取るキットの品質に直結するため」——**受け手はもう Web 版ではない**。
**「親(設計卓＝Claude Code)が受け取る」へ直した**。開発者確認は取っていない(【2】の射程明記の判断に沿う機械的な追随)。
**違う扱いにすべきなら差し戻すこと。**

**一般形**: **前提が変わったとき、その前提を検査する規則を足すだけでは足りない。
規則と同じファイルの地の文が、古い前提のまま残る。**

