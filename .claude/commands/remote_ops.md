---
description: リモート運用の導線。出発前チェックリストを表示し、必要に応じて正本 docs/process/remote-ops.md をオンデマンド読込する軽量コマンド
argument-hint: [checklist | full | <質問>]
allowed-tools: Read, Bash(git status:*), Bash(git log:*)
---

あなたはリモート運用(外出先・夜間)の案内担当です。**本コマンドは軽量導線であり、常駐コンテキストを増やさないため正本はオンデマンドで読みます。**

## 引数の解釈

- **空 または `checklist`**: 下記「出発前チェックリスト」をそのまま表示し、`git status` でクリーン確認だけ補助する(正本は読まない)
- **`full`**: 正本 `docs/process/remote-ops.md` を Read して全文の要点(§1 不変条件・§3 外出中・§5 クラウド注意・§6 push 統制・§8 検証状況)を要約提示する
- **質問文**: 正本 `docs/process/remote-ops.md` を Read して該当箇所を引いて答える(推測で答えない)

## 出発前チェックリスト(表示用・正本 §2 と同期)

- [ ] 進行中の devContainer セッションをキリの良い所で終了
- [ ] `git status` クリーン確認 → 開発者 push
- [ ] **個別設計チャットを使う予定があれば** Web 版プロジェクトの GitHub ナレッジで **Sync now** を実行(直読の鮮度=push+Sync 時点)。※ 親(設計卓)は HEAD を直接読むため Sync 不要
- [ ] 未マージ `claude/` ブランチ・未処理 PR の棚卸し
- [ ] PC は電源 OFF でよい

> 正本: `docs/process/remote-ops.md`(**v1.4.0** / 2026-08-15 改訂②)。本チェックリストと正本 §2 がズレていたら正本を正とし、本コマンドの改訂を開発者に提案する。
>
> **★本脚注の版数は、正本を改訂したときに同じ手番で直すこと。** 2026-09-02 の是正まで `v1.0.0` のまま 4 版ぶん失効していた。

## 禁止事項

- 本コマンドは表示・案内のみ。ファイル編集・Git 書込(add/commit 含む)は行わない。

## 経緯メモ

- **2026-09-02 是正(面の移行の反映漏れ)**: **2026-08-11 に親(設計卓)の面が Claude Web → Claude Code へ移った**が、本コマンドの出発前チェックリストに移行前の文言が残っていた。**2 点を直した** — (1) `- [ ] Web 版プロジェクトの GitHub ナレッジで Sync now`(無条件)を、正本 `docs/process/remote-ops.md` §2 の条件付きの形(「**個別設計チャットを使う予定があれば**」+「※ 親は HEAD を直接読むため Sync 不要」)へ揃えた。(2) 脚注の正本版数 `v1.0.0` → **`v1.4.0`**(4 版ぶん失効していた)。**出所は `M26-01` 担当からの引き継ぎメモ**(スコープ外の発見)。**先例は `.claude/commands/design_handover_report.md` の 2026-09-02 是正(4 か所)**。
  - **★★なぜ発火しなかったのか**: **本コマンドは同じ脚注で「本チェックリストと正本 §2 がズレていたら正本を正とし、本コマンドの改訂を開発者に提案する」と自分で定めていた。⇒ 規則は在ったが、発火の契機が「本コマンドを実行して、かつ正本 §2 と読み比べたとき」だけだった。** 本コマンドは §引数の解釈のとおり、既定(`checklist`)ではチェックリストを表示するだけで**正本を読まない**設計である。⇒ **既定の使い方をしている限り、比較そのものが起きない。★「ズレていたら直す」という規則は、ズレを見る手順を伴わないと発火しない。**
  - **★機械検査の射程外でもあった**: `scripts/check-doc-refs.sh` はファイルの実在を見るが、**参照先の版数までは見ない**。`scripts/check-derived-docs.sh` の PAIRS にも本コマンドは入っていない。⇒ **どの検査も「脚注の版数が古い」を検出できない。**
- **★2026-09-02 に残り 2 件も処理した**(`D-681` / `D-682`)。**`next_milestone_kit.md` / `next_phase_kit.md` の「投入方式」節は、削除ではなく射程明記**——「本節の射程は個別設計チャット(Web 版)に限る／親向けキットには適用しない」を頭に置き、本文の主語を限定した(**変換規則 19 (c) / 生成規則 17 (F) が「個別設計チャット向けの注記としてなら可」と定めているため**)。**`resume_milestone_kit.md` は同じ射程明記を当てたうえで `.claude/commands/old/` へ退避した**(**他プロジェクトへの流用可能性があるため消さない**。**★サブディレクトリは名前空間になるので、起動名が `/old:resume_milestone_kit` へ変わっただけで無効化はされていない**)。**⇒「面の移行」の反映漏れは 3 件とも処理済みである。**
