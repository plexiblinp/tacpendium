# CONTRIBUTING — Tacpendium

## 0. ★現在の受け入れ状況

**Pull Request は、現在は受け付けていない。公開リポジトリ側でも機能を無効化している。**

公開の狙いは (1) 致命的な欠陥を外から検出してもらうこと、(2) 透明性、(3) 証跡であって、
共同開発ではない。**⇒ 外部だけでなく、リポジトリ所有者自身の PR も現在は作成できない。**

**コードの貢献として受け付けているのは、脆弱性の報告だけである**（`SECURITY.md` §2）。
**あわせて、権利者からの申し出とデータの由来についての問い合わせを受けている**（`SECURITY.md` §5）。
Issues と Discussions も閉じている（理由は `SUPPORT.md` §3）。

この方針は恒久のものではない。**開発者が一人で進めたい期間が終わったら、PR を開くつもりである。**
**⇒ ただし時期は約束しない。** 進捗を外へ約束する意図は無い。

本ファイルは、**将来この方針を開いたときのための規約**である。以下はその時点で適用する。

---

## 1. DCO（Developer Certificate of Origin）

貢献には **DCO への同意を必須とする。** コミットに署名行を付けること。

    git commit -s

これは `Signed-off-by: 名前 <メール>` の 1 行を足し、
「この貢献を提出する権利を自分が持っている」ことを表明するものである。
CLA（著作権の譲渡や広い許諾）は求めない。

---

## 2. inbound = outbound

**送られたものは、その部分に適用されているライセンスと同じ条件で受け取る。**

| 貢献先 | 適用されるライセンス |
|---|---|
| アプリ本体（層 A） | `AGPL-3.0-or-later` |
| ゲームデータ（層 B。`character_data/` と `migrations/` の該当分） | `CC-BY-SA-4.0` |
| 開発運用の文書・道具（層 C。`scripts/` / AI 運用ルール群 / 引継ぎ資料） | `MIT` |

どの層かは `REUSE.toml` が正本である。判断が付かない場合は先に尋ねること。

---

## 3. ★素材の由来についての注意

**他人の動画・記事・攻略サイトの内容を、そのまま書き写して投稿しないこと。**

同梱するゲームデータは、**開発者およびコミュニティによる手入力と観測に基づく事実情報**
として集めている（`NOTICE` §2）。第三者が作った表現をそのまま持ち込むと、
その権利者が主張しうる状態になり、**層 B 全体の位置づけが崩れる。**

データを寄せる場合は、次を明記すること。

- **どう測ったか**（トレーニングモードでの手順・使用したキャラと条件）
- **いつ測ったか**（ゲーム側のアップデートで値は変わる）

測定の記録と手順は `character_data/` にある——`chain-cancel-measurements.md`
（チェーンキャンセル実消費フレームの正本）／ `command-correction-history.md` ／
各キャラの再計測記録。

> **★詳細な入力手順書（`docs/seed-data/`）は公開物に含まれていない。** 公式データの
> 抜粋を含むため、公開範囲を再判定するまで公開スナップショットから外してある。
> 必要になったら Issue ではなく **`SECURITY.md` §5 の問い合わせ窓口**へ連絡すること
> （§2 の脆弱性報告の窓口ではない）。

---

## 4. データ更新の受け入れ口

**ゲーム側のアップデートでデータは陳腐化する。** コミュニティによる更新は歓迎する。

- **正本は `character_data/` の CSV と測定記録である**（`DATA-LICENSE.md` §4）。
  `migrations/` 側は生成物・投入手段であり、そちらを直接直さない。
- 新しいマイグレーションを起こすときは、**層 B なら連番の直後に `data_` を入れる**
  （例 `NNNNNN_data_seed_moves_xxx.up.sql`。★実際の連番は開発者が払い出す）。この命名でライセンスの割当が決まる。
  検査は `bash scripts/check-migration-license.sh`。
- **既存のマイグレーションは書き換えない**（適用済みのため）。新しい連番で足す。

---

## 5. 依存ライブラリを増やすとき

- 許可: `MIT` / `Apache-2.0` / `BSD-2-Clause` / `BSD-3-Clause` / `ISC` /
  `Unlicense` / Public Domain / `CC0`
- **`AGPL-3.0-or-later` の配布物に取り込めないものは採らない。**
  とくに `GPLv2-only` は取り込めない。
- 重複機能のライブラリは足さない。最終更新が 2 年以上前のものは原則使わない。
- **新規の依存は、実装より先に提案すること。**

---

## 6. コードの規約

Go は `gofmt` / `go vet` を通すこと。TypeScript は `strict` 前提で `any` を使わない。
コミットメッセージは Conventional Commits 形式（`feat:` / `fix:` / `docs:` など）。
日本語で構わない。
