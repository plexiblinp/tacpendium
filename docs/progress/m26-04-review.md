# M26-04 レビュー報告書

| 項目 | 内容 |
|------|------|
| 対象サブ | M26-04 セキュリティ実査と公開前スキャン（`A6` / `A9` / `A7`） |
| 対象指示書 | `docs/instructions/M26-04-security-survey-and-prerelease-scan.md` v1.1.0 |
| チェックリスト | `docs/instructions/reviews/M26-04-review-checklist.md` v1.0.0 |
| レビュー対象 | `docs/progress/M26-04-completion-report.md` ／ `SECURITY.md` ／ `CONTRIBUTING.md` ／ `git diff 2e35bbd` |
| 実施日 | 2026-09-11 |
| レビュー方式 | 報告が引く `file:line` を全数開き直す ／ `gosec` と `gitleaks` を再実行して数値を突合 ／ 常設検査を再実行 |

---

## 総評

本サブの核心は 2 つ——束 A「設計書にそう書いてあることを実装の証拠と読まないこと」と束 B-1「走査したことを安全であると読まないこと」であり、**この 2 つはどちらも高い水準で満たされている。**
報告が引く `file:line` を 30 か所あまり全数開き直したが、**1 件の誤りも無かった。** `gosec` を再実行したところ検出 59 件・rule 別内訳・走査規模（207 files / 33961 lines）が 1 件違わず一致し、`gitleaks` も検出ファイルと件数が完全に再現した。**測定の捏造も転記ミスも無い。**
一方で、**本サブ自身の書き込みが作った失効記述が 2 件残っている**——`SECURITY.md` §5 を新設したことで `CONTRIBUTING.md` §0 と `SUPPORT.md` §2 の「受け付けているのは脆弱性の報告だけ」が偽になった。前者は本サブが編集した当のファイルの 2 行上である。
加えて `progress-log.md` の索引行が無く、`check-progress-log-index.sh` が現時点で赤い。`gosec` の低重大度 44 件のトリアージは、SQL 11 件で見せた「実物の行を引く」水準に届いていない。

---

## 設計準拠性レビュー結果

### 束 A — `A6` の実査 3 件

| # | 評価 | 判定根拠（本レビューでの実測） |
|---|---|---|
| A-1 | ◎ | 警告の実装を実物で示している。`LanModeConfirmDialog.tsx:60-71` の `step === "warn"` 分岐、`:67-69` の `passwordSet` 出し分け、`:78-80` の `lan-confirm-set-password`、`SettingsSectionNetwork.tsx:112-114` の常時注記——**すべて書かれたとおりであった。** さらに「強く推奨します」が実装に 0 件であることを `grep` で示し、機能は充足・文言は不一致と分けて判定している。**これが束 A が求めていた形である。** |
| A-2 | ◎ | `LanModeConfirmDialog.tsx:27` の `type Step`、`:97-127` の consent 段、`:109-113` のチェックボックス、`:118-121` の `disabled={!consented}`、`:50-55` の同意持ち越し防止——全数一致。コンポーネント名も参照元 1 本という実測も正しい。`DES-002:118` の「押すだけで通さない」まで満たしていることを示した点が良い。 |
| A-3 | ◎ | `SUPP-001:1495` を「過去記録／現行規定」の二択に押し込まず、**前半は失効・後半は現存という混在と判定した。** 判定材料 5 件（見出し階層・文体・同ブロック他行・付記の不在・版の由来）を並べたうえで、`auth/service.go:74` ／ `middleware/auth.go:62-64` ／ `main.go:470` ／ `config.go:131` で M22-01 の配線を実装から裏取りしている。**4 か所とも行番号まで一致。** さらに「無認証経路が残るのは事故ではなく `DES-002:1225` が選んだ設計」という結論へ繋げており、公開判断に効く形になっている。 |
| A-4 | △ | CHANGE を自採番せず §7.1 で請求している点は正しい。**ただし請求が片肺である**——中-2 を参照。 |

### 束 B — 走査の読み方

| # | 評価 | 判定根拠 |
|---|---|---|
| B-1 | ◎ | 「検出 0 件ではない」「安全であるとは書かない」「道具が見ないもの 5 件」を分けて書いている。**浅いクローンを自分で見つけ、走査していないことと到達できないことを分けた**のは E-84 の型を正しく避けている。`.git/shallow` の実在と最古コミット 2026-09-02 は本レビューでも確認した。 |
| B-2 | ◎ | `git archive HEAD` で追跡ファイル全数を展開し、件数で母集団を裏取りしている。本レビューでも展開し 2984 件（報告時点 2983 ＋ 本報告書 1）を確認。除外 29 件が入っていることは `check-public-snapshot.sh` の母数 2984 ／ 公開 2955 ／ 除外 29 と整合する。 |
| B-3 | ◎ | `A2` は `M26-01` で決着済みとして手を出さず、SCANOSS をコード片の由来として別扱いしている。どちらも他方で代替していない。 |
| B-4 | ◎ | 検出物は 1 件も削除されていない。`sa2_lowkey_sunrise_festival` が 6 ファイルに残存することを本レビューでも `grep` で確認した。 |
| B-5 | △ | 外部へ渡したものが無いことは適合。**ただし投げるべき問い 1 本の記録が無い**——中-4 を参照。 |

### 束 C — `A7` の実務設定

| # | 評価 | 判定根拠 |
|---|---|---|
| C-1 | ◎ | 書き込みは `SECURITY.md` ／ `CONTRIBUTING.md` ／ 完了報告の 3 本のみ。`.github/` は 1 件も作られておらず、`dependabot.yml` を作らなかった理由まで書いてある。**GitHub 側の設定変更の形跡はツリーに無い。** |
| C-2 | ◎ | `SECURITY.md:74` が `<権利者・一般問い合わせ窓口のアドレス>` の伏せ字。実アドレスは 1 件も無い。 |
| C-3 | ◎ | `git diff 2e35bbd` の対象 3 ファイルに `README.txt` と `NOTICE` は含まれない。**1 バイトも触っていない。** |
| C-4 | △ | (1) PR を当面受けない＝`CONTRIBUTING.md:5` に既存、(2) 窓口導線＝`:65` へ差し替え済みで、形式上は両方入っている。**ただし §0 の記述が本サブの書き込みで失効した**——高-2 を参照。 |
| C-5 | ◎ | `SECURITY.md:82-83` が応答期限を約束していない。「取り下げの申し出は他に優先して見る」は優先順位の宣言であって期限ではなく、C-5 に反しない。 |

### 束 D — 射程の境界

| # | 評価 | 判定根拠 |
|---|---|---|
| D-1 | ◎ | 法務ポスチャへ手を出していない。`NOTICE` §3 は参照するだけで編集していない。 |
| D-2 | ◎ | `A2` をやり直していない。`go.mod` ／ `go.sum` ／ `pnpm-lock.yaml` に差分 0。 |
| D-3 | ◎ | **束 D で最も見たかったところであり、満点である。** §2.4 で不足を並べ、直さずに開発者へ返している。「フロント実装 ＋ Vitest ＋ E2E が要り、サブの性格と規模が変わる」という理由づけも妥当。 |

### 束 E — 共通

| # | 評価 | 判定根拠 |
|---|---|---|
| E-1 | ◎ | `check-doc-refs.sh` 再実行で exit 0、dead reference なし。 |
| E-2 | ◎ | `check-doc-inventory.sh` 再実行で exit 0、型に無いファイルなし。出力を読んだ形跡も §5.2 に在る。 |
| E-3 | ◎ | `check-md-emphasis.sh docs/progress/M26-04-completion-report.md` を再実行し検出 0 行。541 行でこれは良い。 |
| E-4 | × | **`check-progress-log-index.sh` が exit 1。** 高-1 を参照。 |
| E-5 | ○ | §2.1 の `CHANGE-113` 追随漏れ推定に「推測」と明示し、根拠と「確定はしていない」を併記している。§6 の 8 件も未確認事項として正しく分離されている。 |

---

## 設計準拠性以外の指摘事項

- **`gosec` の検出 59 件は本レビューで再実行し、rule 別内訳まで完全一致した**（G101 1 ／ G104 17 ／ G115 1 ／ G124 1 ／ G201 8 ／ G202 3 ／ G204 1 ／ G301 7 ／ G304 11 ／ G306 9、files 207 ／ lines 33961 ／ nosec 0）。**数値は信用してよい。** 問題は読み方であり、中-3 に書く。
- **`gitleaks` の検出も再現した。** 本レビューで `git archive HEAD` を展開して走査したところ検出 12 件、うち 11 件が報告の列挙と完全一致（`000084.down` 2 ／ `000086.down` 1 ／ `000089.up` 1 ／ `000090.down` 1 ／ `000091.down` 1 ／ `01-migration-details.md` 5）、一致文字列も 1 種類のみ。**12 件目は完了報告そのものである**——低-1 に書く。
- コーディング規約面の指摘は無い。本サブはコードを 1 行も変更していない（`go.mod` ／ `go.sum` ／ `web/` に差分 0）。
- ライブラリ選択について。`gosec` と `gitleaks` を `go install` した実行ファイルとして使い、`go.mod` を汚していない判断は `CLAUDE.md` §6 と `D-620` の線に沿う。ライセンスを一次情報で確認し、実行ファイルの自己申告が版の証拠にならないことまで書いた点は良い。
- セキュリティ面の実質について。本レビューでも `resolveSortClause`（`internal/repository/combo/repository.go:728-739`）を読み、許可リスト引きで外れた値が既定へ落ちること、方向がハードコードの `ASC` / `DESC` へ解決されることを確認した。**報告の「利用者の文字列が SQL へ入る経路は無い」は正しい。**

---

## 推奨修正（優先度別）

### 高（M26 完了前に修正必須）

**高-1. `progress-log.md` への索引行が無い。検査が赤い。**

本レビューで `bash scripts/check-progress-log-index.sh` を実行した結果:

```
NG  作業 ID `m26-04` が docs/progress/progress-log.md に現れない(完了報告: docs/progress/M26-04-completion-report.md)
結果: 違反 1 件
```

完了条件 ★10 ／ チェックリスト E-4 ／ `CLAUDE.md` §8 の未達である。完了報告 §5.4「完了時の検査」と §9「レビュー結果と取り込み」も空欄のまま残っている。
**Phase C / Phase D で埋める予定と本文に書かれていることは承知している**が、現時点で検査が赤いことは事実であり、ここを緑にしないと完了とは言えない。索引行には、横断課題として §7.2 の失効 2 件と §2.4 の不足 1 件を必ず載せること。

**高-2. `CONTRIBUTING.md` §0 の記述が、本サブの書き込みで失効した。**

`CONTRIBUTING.md:10` の逐語:

> **受け付けているのは脆弱性の報告だけである**（`SECURITY.md` §2）。

本サブが `SECURITY.md` §5 を新設した時点で、この文は偽になった。**同じ §0 に本サブが 2 行を追記しているのに、その 2 行上が更新されていない。**
これが効く相手は権利者である。`NOTICE:33` の撤退ポリシーから `SECURITY.md` へ来る前に `CONTRIBUTING.md` §0 を読んだ権利者は「脆弱性以外の窓口は無い」と判断する。`rights-holder-contact-channel-missing` が閉じない。
**動作は何も壊れないためテストも lint も型検査も緑のまま通る。人が読む以外に見つける経路が無い。** 優先度較正の定めるとおり「高」とする。
修正案は 1 行の追記でよい（例——脆弱性の報告に加え、権利者・データの由来についての問い合わせを `SECURITY.md` §5 で受けている旨）。**本サブの射程内のファイルであり、本サブで直せる。**

**高-3. `SUPPORT.md` §2 の受付表にも `SECURITY.md` §5 が無い。報告にも 1 行も出てこない。**

`SUPPORT.md:15-19` は「何を受け付けるか」の表であり、脆弱性報告／Issues／PR の 3 行しか持たない。**`SECURITY.md` §5 の新設で、この表は不完全になった。** `SUPPORT.md` を先に読んだ権利者は、やはり窓口が無いと判断する。
`SUPPORT.md` は指示書が本サブへ割り当てたファイルではない（本サブの射程は `SECURITY.md` と `CONTRIBUTING.md`）。**したがって直すこと自体は別の手番でよい。** しかし**失効させたのは本サブであり、報告に一言も無いことが問題である**——報告 §7.3 の候補表にも、§8 の「併せて更新が要るもの」にも `SUPPORT.md` の行が無い。
**⇒ §7.3 へ 1 行足し、設計伝達レポート §4 から設計卓へ回すこと。** 高-2 と同型（記述だけが旧のまま残る形）であるため「高」に置く。

### 中（M27 着手と並行可）

**中-1. §2.4 の不足の記述が、as-built の半分を落としている。**

報告 §2.4 の表は初回起動ウィザードについて「警告＝無い／同意ダイアログ＝無い」とだけ書く。**これは正しいが、不足ではない部分が書かれていない。**
実物——`web/src/pages/WizardPage.tsx:116-117` は `lanEnabled` のとき `Step07Password` を段として挿入し、`web/src/features/wizard/Step07Password.tsx:13` の逐語コメントは「LAN 共有を有効にしたときだけ出す（`DES-005` §5.1 の『任意、LAN共有有効時のみ表示』）」である。
`DES-002:1160` が求めるのは「警告する」と「パスワード設定へ誘導する」の 2 要素であり、**ウィザードは後者を持ち、前者を欠いている。** 報告の書き方だと、読んだ開発者が「ウィザード経路は完全に無防備」と受け取りうる。
**⇒ §2.4 の表を 2 要素に割り、`Step07Password` の実在を明記すること。** 直すかの判断（ウィザードへ `LanModeConfirmDialog` を差すのか、`Step07Password` の前に警告を 1 枚足すので足りるのか）が、この 1 行で変わる。

**中-2. `A6` の食い違いに対する CHANGE 請求が片肺である。**

報告 §7.1 は `SUPP-001:1495` の 1 本だけを請求し、`DES-002:1160` の文言不一致は「設計卓の判断」として回している。**しかし §2.4 の不足そのものが `DES-002` §8 と as-built の食い違いである。**
`DES-002:101` ／ `:1160` ／ `:1161` はいずれも経路を限定していない（報告自身がそう書いている）。**一方 as-built は設定画面経路にしか無い。** これは指示書 §2.1 の「実査の結果、`SUPP-001` / `DES-002` §8 の記述と as-built が食い違ったら、CHANGE を請求すること」に直接当たる。
報告は「直すかの判断は開発者の手番」とだけ書き、**直さないと決めた場合に `DES-002` §8 への as-built 追記が要ることを書いていない。** ⇒ 実装でも設計書でもなく、どちらにも倒れないまま残る形になっている。
**⇒ 分岐を明記すること。** 直すなら別サブ、直さないなら `DES-002` §8 へ as-built 注記の CHANGE を請求する、の 2 択である。指示書ヘッダの「`DES-002` §8 の as-built 追記が要る可能性」はまさにこれを指している。

**中-3. `gosec` の低重大度 44 件のトリアージが、本サブ自身の水準に届いていない。**

SQL の 11 件（G201 / G202）では実物の行を引き、`resolveSortClause` まで開いて見せている。**同じ報告の G304 11 件 ／ G301 7 件 ／ G306 9 件 ／ G104 17 件は、行を引かずカテゴリ単位の理由で畳まれている。** しかも書かれた理由が実物と合っていない箇所が 2 つある。

- **G304 の「設定経由のパスは `appconfig.ValidateDataPath` で封止済み」。** 本レビューで `gosec` を再実行して 11 件の実体を特定した——`internal/config/config.go:147, 206, 243, 276`（`Load` ／ `replaceAtomically` ／ `PersistPort` ／ `RewriteRelocatedPaths` が扱う**設定ファイルそのもののパス**）と `internal/infra/datadir/{copy.go:74, 83, 104, lock.go:42, notice.go:86}`（**パッケージが組み立てた内部パス**）、`internal/seedgen/csv.go:15`、`internal/testutil/dbtest/dbtest.go:239`。**`ValidateDataPath` が縛るのは `database.path` と `logging.file` であって、これらではない。** 結論（無害）は本レビューの読みでも一致するが、**理由が実物と違う。**
- **G104 の「ベースラインの無い初回実行のため出る型」。** これは安全性の説明になっていない。実体は `internal/repository/setup/repository.go:922, 928, 934, 1011, 1017, 1022` の `rows.Close()` の戻り値と、`internal/config/config.go:216, 220, 224, 229` の `os.Remove(tmpPath)` の後始末、`cmd/genicon/main.go:127-134` である。**無害だが、書かれた理由からはそれが分からない。**

**⇒ 少なくとも `internal/` に落ちる 3 群（G304 / G301 / G306）は、代表 1 行ずつ実物を引き、なぜ無害かを実物の性質で書くこと。** 本サブが §4.1 冒頭で自ら掲げた「設計書にそう書いてあることは実装の証拠にならない」の裏返しであり、**「カテゴリがそうだから」も実装の証拠にならない。**

**中-4. `/ask_external_ai` の記録が無い。回したのか回していないのかが区別できない。**

指示書 §0.4 ／ §2.2-3 ／ §7-1 は「Gitleaks か betterleaks かだけは `/ask_external_ai` で 1 本だけ確認し、結果を添えて開発者へ聞く」と定めている。**報告に `betterleaks` の語が 1 度も出ない**（`grep` 実測でヒット 0）。§1 の裁定表は「gitleaks ＋ gosec を一時導入」という結果だけを書き、**betterleaks を比較検討した形跡が無い。**
`followup-backlog` の `secret-scan-before-release` は「設計卓は `betterleaks` について確かな知識を持たない。着手時に実在・保守状況・ライセンスを確認する」と明記しており、この確認は本サブの仕事であった。`gitleaks` の版・ライセンス・最終更新を §1.1 で実測している点は半分それに当たるが、**比較対象を確認していない。**
**⇒ 回したなら結果を §1 へ添えること。回していないなら §6「確かめられなかったもの」へ 1 行足すこと。** 外部へ渡したものが無いこと自体は適合であり、越権ではない。

**中-5. `rights-holder-contact-channel-missing` の内訳の写しがずれている。**

報告 §7.3 は「残るのは `README.txt` と `NOTICE` §3 の 2 本＝`M26-03`」と書く。**同 followup 行の実測内訳はそうではない**——逐語は「`README.txt:136` はセキュリティに関わる不具合で**適合**。`NOTICE:33` は権利者からの撤退申し出で**不適合**。`CONTRIBUTING.md:62` は `docs/seed-data/` の入手希望で**不適合**」である。
**⇒ 不適合は 2 本で、本サブが `CONTRIBUTING.md` を片づけたため、残るのは `NOTICE:33` の 1 本である。** このまま `M26-03` へ渡すと、既に適合している `README.txt:136` を不要に書き換える誘導になる。
**なお、この書き方の出どころは指示書 §2.3-3b の「`README.txt` と `NOTICE` §3 の 2 本は `M26-03` が持つ」である。⇒ 報告だけの責ではない。設計伝達レポートで設計卓へ返し、`M26-03` 側で判断させること。**

### 低（将来対応）

**低-1. 完了報告そのものが gitleaks の 12 件目になっている。**

本レビューで `git archive HEAD` を展開して `gitleaks dir` を回したところ、検出は 11 件ではなく **12 件**であった。増えた 1 件は `docs/progress/M26-04-completion-report.md` であり、**報告 §3.3 が偽陽性の実例として `sa2_lowkey_sunrise_festival` を引用したことによる。**
実害は無い（同じ技コードであり、報告自身がそう書いている）。**ただし次に走査する人は 12 件を見ることになり、報告の「11 件」と合わない。** `docs/progress/` は公開スナップショットに含まれるため、公開物側でも 1 件増える。
**⇒ §3.3 へ「本報告自身が同じ文字列を含むため、次回の走査では 1 件増える」を 1 行足すこと。**

**低-2. `git diff --stat 2e35bbd` の引用が、いまは再現しない。**

報告 §5.1 は同コマンドの出力として「2 files changed, 24 insertions(+), 1 deletion(-)」を引くが、完了報告自身がコミットされた現在の実測は「3 files changed, 565 insertions(+), 1 deletion(-)」である。**コマンド名を添えて実測を引く節は、再実行で違う値が出ることを注記するか、対象ファイルを限定して書くこと。**
なお `E-225` 観点の確認自体は妥当であり、**本レビューでも `git diff 2e35bbd` を全文読み、新規追加のはずのファイルに deletions が付いていないこと、`CONTRIBUTING.md` の 1 deletion が §3 の導線 1 行を 2 行へ書き換えたことによるものであることを確認した。上書きで消えた行は無い。**

**低-3. 参照の微細なずれが 2 件。**

- §2.3 は `SUPP-001:1495` を「無名の小見出しブロック（`:1490`）の 3 箇条目」と書くが、実測は 4 箇条目である（`:1492` / `:1493` / `:1494` / `:1495`）。あわせて「同ブロック内の他の行 `:1491`-`:1494`」の `:1491` は空行である。**判定そのものには影響しない。**
- §5.2 は `scripts/check-doc-refs.sh:46` を引くが、`PATH_RE` の定義行は `:45` である。

**低-4. `check-doc-refs.sh` の射程の説明が半分しか書かれていない。**

報告 §5.2 は「リポジトリ直下のファイル名は `PATH_RE` が拾わない」と書く。これは正しいが、**同スクリプトはそもそも `CLAUDE.md` ／ `web/CLAUDE.md` ／ `.claude/rules/*.md` ／ `.claude/commands/*.md` のルール面 37 ファイルしか走査しない**（実行出力の「対象範囲」行）。`SECURITY.md` ／ `CONTRIBUTING.md` ／ `docs/` 本文は最初から対象外である。
**⇒ 指示書 §5-3 の前提のずれは二重ではなく三重である。** §7.2 の失効記録へ足すと、次に同じ検査を当てにする担当が助かる。

---

## 良かった点

- **束 A が満点である。** 報告が引く `file:line` を 30 か所あまり全数開き直したが、**1 件の誤りも無かった**——`LanModeConfirmDialog.tsx` の 27 / 50-55 / 58 / 60-71 / 67-69 / 78-80 / 97-127 / 109-113 / 118-121、`ja.json` の 647 / 649 / 651 / 654 / 655 / 656、`SettingsSectionNetwork.tsx:112-114`、`DES-002` の 101 / 118 / 1160 / 1161 / 1213 / 1225、`SUPP-001` の 62 / 1472-1479 / 1495、`auth/service.go:74`、`middleware/auth.go:62-64`、`main.go:470`、`config.go:131`、`routes.go:22`、`combo/repository.go:728-731` と `736-739`。**この精度は、公開の判断を託せる水準である。**
- **「強く推奨します」が実装に 0 件であることを `grep` で示し、機能の充足と文言の不一致を分けて判定した。** 「実装されている」で終わらせなかったことが `A6` の値打ちである。
- **`A6`(c) の判定が、二択を疑ったところが良い。** 「過去記録か現行規定か」という問いに対し、**どちらでもなく両方が混ざった行だと答え、前半の失効を実装 4 か所で裏取りした。** そのうえで「未了の是正が残っている」と読むと公開を止める判断になりうるが実体は設計であると書き、公開判断への効き方まで通している。
- **`gosec` と `gitleaks` の数値が完全に再現した。** `gosec` は 59 件・207 files・33961 lines・rule 別内訳がすべて一致。`gitleaks` は検出ファイルと件数、一致文字列が 1 種類であることまで一致。**実測を装った推計が 1 件も無い。**
- **束 B-1 の書き方が手本になる。** 「探したもの」と「探せていないもの」を分け、後者を 5 件の表にした。とくに**浅いクローンを自分で見つけ、走査していないことと到達できないことを区別した**のは、`E-84` の型を正しく避けている。本レビューでも `.git/shallow` の実在と最古コミット 2026-09-02 を確認した。
- **`golangci-lint` の実行不可を回避せず、そう書いて止めた。** `D-659` の「到達できなければ実行不可と書いて止まり、回避策を独自に作らせない」の正しい適用である。`go` ディレクティブを下げるような回避は 1 行も無い。
- **SCANOSS を「回さない」と決めて止まった判断。** WFP がソース本文でないことまで理解したうえで、「内容に当たるかの解釈が分かれる」として `D-671` で止めた。**外部送信を伴う道具について、製造が独自に線を引かなかったのは正しい。**
- **束 D-3 が満点。** 不足を 1 件見つけて直さず、並べて返した。指示書 §0.1 が最も警戒していた形を正面から避けている。
- **G124 の扱いが良い。** 道具が独立に指摘した `Secure` 属性の不在を、`DES-002:1213` の明示的な決定と突き合わせ、「未決の穴ではなく記録された判断」だと確認した。**走査を回す意味がここに出ている。**
- 541 行の報告で `check-md-emphasis.sh` が 0 行。`D-775` の手順が実際に機能している。

---

## 不明点

- **不明: GitHub 側の設定を実際に変えていないかは、コード上では最終的に確認できない。** `gh` や MCP の実行痕跡はツリーに残らないためである。判定できる範囲では、`git log` 上の本サブのコミットは 2 本ともドキュメントのみ、`.github/` に差分 0、`git status` は clean であり、**変更の形跡は無い。**
- **不明: `/ask_external_ai` を実際に回したかどうか。** 報告に記録が無く、判断できない（中-4）。
- **不明: 全履歴走査の未完了を、公開前にどこまで必須とするか。** 報告は開発者のローカル手番として返しているが、**「(b) が未完了のまま公開してよいか」は開発者の判断であり、本レビューでは判定しない。**

---

## 制約事項

- 本レビューはコード上で判定可能な範囲のみ対象。実際の動作確認・パフォーマンス・実機テストは別途実施が必要。
- `gosec` ／ `gitleaks` ／ `check-progress-log-index.sh` ／ `check-doc-refs.sh` ／ `check-doc-inventory.sh` ／ `check-artifact-integrity.sh` ／ `check-public-snapshot.sh` ／ `check-md-emphasis.sh` は本レビューで再実行した。`go test` ／ `pnpm test` は本サブがコードを 1 行も変更していないため再実行していない。
- 全履歴の `gitleaks git .` は再実行していない。本環境が浅いクローンであることと、報告の (b) が未完了と自認していることは確認済み。

---

## 取り込み結果（自動トリアージ）

**実施**: 2026-09-11 ／ `implement_plan_full` Phase C ／ 再レビュー往復 **0 回**

**★指摘 12 件（高 3 / 中 5 / 低 4）をすべて採用した。⇒ 「高」の不採用は 0 件であり、開発者へのエスカレーションは発生していない。**

| # | 優先度 | 採否 | 理由と対応 |
|---|---|---|---|
| 高-1 | 高 | **採用** | `progress-log.md` へ索引行を追記し `check-progress-log-index.sh` **exit 0** を確認。横断課題 8 件を載せた |
| 高-2 | 高 | **採用** | **本サブが作った失効である。** `CONTRIBUTING.md:10` を「コードの貢献として受け付けているのは」へ限定し、`SECURITY.md` §5 の窓口を 1 行追記して是正 |
| 高-3 | 高 | **採用（記録として）** | `SUPPORT.md` §2 の失効を完了報告 §7.3 / §8 と progress-log 横断課題 3 へ計上。**★ファイルは直していない**——指示書がどちらのサブにも割り当てていないため。設計卓へ回す |
| 中-1 | 中 | **採用** | §2.4 の表を 3 要素へ割り、`WizardPage.tsx:116-117` / `Step07Password.tsx:13` で「誘導は在る」ことを明記 |
| 中-2 | 中 | **採用** | §2.4 へ (A) 実装を直す ／ (B) `DES-002` §8 へ as-built 注記の CHANGE を請求する、の 2 択を明記 |
| 中-3 | 中 | **採用（事実誤りの是正）** | §3.2.4 を新設。**G304 の理由は `ValidateDataPath` ではなく「利用者入力が到達しない」ことである**と実物で書き直し、G104 も 3 群へ分けた |
| 中-4 | 中 | **採用** | §1.3 を新設し、プロンプト生成のみで**回答未取得**・`betterleaks` 未確認であることを明記。§6 へ 9 件目として計上 |
| 中-5 | 中 | **採用** | §7.3 を訂正。**残る不適合は `NOTICE:33` の 1 本**であり、`README.txt:136` は既に適合 |
| 低-1 | 低 | **採用** | §3.3 へ「次回の走査は 12 件になる」を申し送り |
| 低-2 | 低 | **採用** | §5.1 を対象 2 ファイル限定の形へ直し、再現性の注記を追加 |
| 低-3 | 低 | **採用** | `:1495` は 4 箇条目、`PATH_RE` は `:45` へ訂正 |
| 低-4 | 低 | **採用** | §5.2 を書き直し、**ずれが三重である**ことと**本検査が成果物を見ていない**ことを明記。§7.2 へ反映 |

**★不採用は 0 件である。** したがって不採用理由の記載は無い。

**★本レビューの独立性について 1 点記録する。** レビュー担当は `gosec` と `gitleaks` を再実行し、検出 59 件・rule 別内訳・走査規模、および `gitleaks` の検出ファイル・件数・一致文字列を突合している。**⇒ 完了報告の実測値は独立に再現された。** あわせて **12 件目の検出（完了報告自身）を見つけたのはレビュー側である**（低-1）。
