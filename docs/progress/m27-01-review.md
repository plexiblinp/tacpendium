# M27-01 レビュー報告書

| 項目 | 内容 |
|------|------|
| 対象 | 指示書 `docs/instructions/M27-01-hit-type-and-size-labels.md` v1.0.0 |
| 着手基点 | `78f4981` |
| レビュー時点 HEAD | `ac006bd`（作業ツリーはクリーン） |
| 対象コミット | `80ec1c7` / `4de7ee5` / `f2c4d2f` / `6614258` / `ac006bd`（5 本・38 ファイル・+1,679 / -71） |
| レビュー担当 | 品質レビュー担当 Claude Code（read-only。コード変更 0） |
| 実査 | `go build ./...` ／ `go test ./...` 全数緑 ／ `pnpm test` 210 ファイル 2,339 件緑 ／ `tsc --noEmit` 緑 ／ `gofmt -l internal/` 0 件 ／ `go vet` 緑 ／ 常設検査 8 本（artifact-integrity / stop-discipline / progress-log-index / doc-refs / md-emphasis / import-order / enum-sync / browser-storage-keys / doc-inventory）緑 ／ E2E 2 本（`m19-03` 8 件・`m24-12` 5 件）緑 |

---

## 総評

**実装そのものの質は高い。** `medium` の置換は技の強度（light/**medium**/heavy）を 1 か所も巻き込んでおらず、opponent_size 側の取り残しも実測で 0 件。マイグレ `000081` は WHERE で値を絞り、NULL・`large1`・`large2` を巻き込まないことを新規テスト 2 本が母数付きで固定している。指示書に無い実査由来の発見（`punish.ts` の位置インデックス参照・`InfoMark` を `hint` に置かないと数字キーが死ぬこと）を先回りで潰した点も良い。

**一方で、工程としては完了していない。** **`docs/progress/M27-01-completion-report.md` が存在せず、`progress-log.md` への追記も無い**（完了条件 9・§3.2「★必須」）。指示書が §0.3 で「別名だと索引行が落ちても永久に緑になる」と名指しした型そのものに現在なっている。これに引きずられて、**§3.2 が求めた 6 項目（母数付き件数／確定列挙の逐語／数えた数と通した数／やらないと決めた項目／enum-sync ベースラインの理由／確かめられなかったこと）が 1 つも提出されていない**。段 1 成果物 §9.4 が自ら「詳細は完了報告に記録する」と書いた §4-2 逸脱の経緯も、行き先が無いまま宙に浮いている。

**機能面では 2 点が未着地。** ① 完了条件 6 の「保存でき／絞り込みで拾え」を見るテストが無い（在るのは CSV の往復と定数の形の固定だけ）。② `drive_impact_punish_counter` を PC ファミリーから外した帰結——**当該コンボに「パニッシュカウンター版を作る」導線が出て、BE の materialize が `default` 分岐（＝未知値＝normal 相当）で通り、始動技ダメージ ×0.2 を加算した複製を作る**——が、記録も固定もされていない。

**マイグレ台帳が更新されていないのも実害の芽である。** `progress-log.md` の現在の記述は「次の払い出しは `000081` のまま」であり、`M14-03f` / `M28-01` が同じ帯を使う見込みだと指示書 §4-6 が明記している。

---

## 設計準拠性レビュー結果

### 指示書 §2 やること

| # | 項目 | 評価 | 所見 |
|---|---|:--:|---|
| §2.1 | 段 1・ヒット種別の確定案 | **◎** | `docs/progress/M27-01-hit-type-proposal.md`（491 行）。母数（375 行 / 74 ファイル）・既存 4 値との重なりの 1 件ずつの判定・源泉 3 行の他候補・識別子案と表示名案の分離・「足すことで壊れるもの 3 件」まで揃っている。ボタン上限 10（DES-005 / CHANGE-138）に照らした余白計算まで出している点は指示書の要求を超えている |
| §2.1.3 | 画面モック | **○** | 単一 HTML・外部依存 0（`D-543` の型）。ただし「変更後」比較表の相手サイズが `大1` の短ラベルのまま（後述 高-5a） |
| §2.2 | 段 1・相手サイズの判定 | **◎** | 「安さではなく正しさ」で (B) を推した理由付けが指示書 §2.2-2 の要求どおり。CSV ホワイトリストとの衝突を先に洗い出して Q7 として開発者へ返している |
| §2.3 | 段 2・両側へ定数 | **○** | `internal/model/combo.go` に `HitType*` 8 値・`OpponentSize*` 4 値。`web/src/constants/combo-list.ts` に `HIT_TYPE_VALUES` 8 値・`OPPONENT_SIZE_VALUES`（新設）4 値。`check-enum-sync.sh` は 4 新値の対応を確認済み（INFO 行なし）。ただし `model.OpponentSize*` の本番参照が 0 件（中-10） |
| §2.3-3 | enum-sync ベースライン | **○** | 24 → 28。実査で増分は `drive_impact_wall_splat_hit` / `_block` / `drive_impact_punish_counter` / `stun` の各 1 ファイル（いずれも `labels.ts`）と確認。スクリプト内の理由記述も正確（`OpponentSize*` が抽出パターン `(Category\|Status\|Type\|Code)` に一致しないことを実際に確認した）。**理由が「報告」ではなくスクリプトのコメントにしか無い**のは §3.2-5 に対して不足 |
| §2.4-1 | 数えた母数へ全数通す | **△** | 実装は通っている（`opponent_size` の残存 `medium` はコード上 0 件・実測）。**しかし「母数と通した数を並べて書く」報告が存在しない**。加えて段 1 の母数（Go テスト 17 ファイル / TS 6 ファイル）と実際に触った数（Go 11 / TS 5）が食い違う（低-13） |
| §2.4-2 | 絞り込みに新値が出る | **△** | `ComboListFilters.tsx:280` が `HIT_TYPE_VALUES` を map しているので実装上は出る。**それを見るテストが 0 件**（`ComboListFilters.test.tsx` に hit type の記述なし） |
| §2.4-3 | CSV 列契約に触れるか判定 | **○** | 列は不変・値のみ。判定結果を書く場（完了報告）が無い |
| §2.4-4 | 新値のテスト | **△** | 往復は `TestHitTypeM2701RoundTrip` / `TestOpponentSizeRoundTrip` で担保。**保存・絞り込みは 0 件**（高-4） |

### 指示書 §4 やらないこと（8 項目・1 件ずつ）

| # | やらないこと | 評価 | 所見 |
|---|---|:--:|---|
| §4-1 | 段 1 の承認を待たずに段 2 へ | **○** | コミット順（`80ec1c7`/`4de7ee5` → `f2c4d2f` → `6614258`）と提案書 §9 の逐語記録は整合。**ただしリポジトリ内の証拠は §9 の自己申告のみ**（不明扱い・後述） |
| §4-2 | 「大1」「大2」の整理 | **△** | **射程を超えている**（ラベルへ例示キャラを追加＋`large` 新設）。開発者が §9.2 / §9.4 で判断を供給しているので逸脱そのものは正当。**しかし「経緯が完了報告に記録されているか」＝ No**（高-3）。`followup-backlog` の `opponent-size-large-variants-defer` は `要判断` / `未着手` のまま据え置かれており、製造は編集できない（`D-382`）ため設計卓へ渡す経路が完了報告しかない |
| §4-3 | `M27-02` / `M27-03` の項目 | **◎** | フィルタ短縮に手を付けていない。絞り込みは長ラベルのまま 8 件出る形で止めてある |
| §4-4 | 既存 `hit_type` の変更・削除 | **◎** | 既存 4 値は識別子・ラベルとも 1 文字も変えていない。新値は必ず末尾。既存データを動かす DML は `opponent_size` の 1 文のみ |
| §4-5 | `docs/design/` の編集 | **◎** | `git diff --stat 78f4981` に `docs/design/` は 1 件も無い。ただし報告先が無いまま（高-5c） |
| §4-6 | CHANGE 番号・マイグレ連番の自採番 | **不明** | `000081` を消費。up ヘッダは「開発者へ 000081 を 1 本だけ請求した」と書くが、リポジトリ側に払い出しの記録が無い（`progress-log.md` は「次の払い出しは 000081 のまま」で止まっている）。**請求の有無は判断できない**（高-2 と併せて対処が要る） |
| §4-7 | `followup-backlog.md` の編集 | **◎** | 未変更 |
| §4-8 | 新しいツールの導入 | **◎** | 新規はテスト 2 ファイルのみ（`enum_sync_test.go` / `migrate_m2701_test.go`）。既存 `check-enum-sync.sh` はベースライン数値のみ変更 |

### 指示書 §5 完了条件

| # | 条件 | 評価 |
|---|---|:--:|
| 1 | 段 1 成果物が在り承認を得ている | **○**（承認は §9 の逐語記録による） |
| 2 | 確定列挙が完了報告に逐語で | **×**（完了報告が無い。逐語は段 1 成果物 §9 に在る） |
| 3 | 両側に定数・`check-enum-sync.sh` が緑 | **◎**（緑・増加なし） |
| 4 | 数えた全箇所へ通っている／母数と通した数を並べて書く | **△**（通っている／書かれていない） |
| 5 | 「中」→「標準」＋ CSV ホワイトリスト同期 | **◎**（`DefaultOpponentSizes` を `medium` → `standard` の 1 語だけ追随。開発者選択 (a) どおり） |
| 6 | 保存でき・絞り込みで拾え・往復で失われないテスト | **△**（往復のみ。保存・絞り込みは 0 件） |
| 7 | `go test` / `pnpm test` / `make e2e` が緑 | **○**（前 2 者は全数実査で緑。`make e2e` 全数は未実施＝抜き取り 2 本のみ確認） |
| 8 | 検査 7 本が緑 | **◎**（8 本実査。すべて緑。`check-doc-inventory` は情報提供 1 件あり＝中-9） |
| 9 | 完了報告が在り `progress-log.md` へ追記 | **×** |

---

## 設計準拠性以外の指摘事項

### コーディング規約（`CLAUDE.md` §4）

- **Go**: 列挙的文字列定数のパッケージ定数化 ○。`OpponentSize*` の godoc も付いている。`combo.go:119` の失効 godoc（`just_parry_punish_counter` が落ちていた 3 値列挙）を今回是正し、「値を列挙し直さないこと」と再発防止の理由まで書いた点は良い。
- **TypeScript**: `any` なし、`strict` 通過（`tsc --noEmit` 緑）。import 順検査もベースラインどおり。DTO は camelCase 維持。
- **リテラル散在**: `web/src/constants/` を正典にする形は守られているが、`features/combo/labels.ts` の日本語直書きマップが 2 本目として残る（既知・`labels.ts` 冒頭が明記）。今回で対象値が 7 → 12 に増え、割れの面積は広がった（中-8）。
- **`console.log` / `fmt.Println` の残存**: 0 件。`nolint` / `eslint-disable` の追加: 0 件。

### 依存・セキュリティ

- 新規依存 0。`go.mod` / `package.json` に変更なし。indirect → direct の昇格もなし。
- データ破壊性: `000081` は `UPDATE ... WHERE opponent_size='medium'` の 1 文のみ。DDL なし・行の増減なし・`recipe_cache` 不変。重複判定キー（VAL-C02）は `opponent_size` を含むが、`standard` は新設値なので改名は全単射であり衝突を作らない。**安全性の判定は妥当**。
- `down` の非対称（`large` は戻らない）を「欠陥ではなく一般的性質」として明示し、テストで固定してある。`down` を打つ担当が驚かない形になっており、扱いとして正しい。

### 命名

- 識別子（`drive_impact_wall_splat_hit` / `_block` / `drive_impact_punish_counter` / `stun`）は開発者確定 ID-1 案どおり。`stun` に `drive_impact` を付けない理由（DI に限らない一般区分）もコメントに残っている。
- `standard` / `large` / `large1` / `large2` は表示名（標準 / 大 / 大1(ザンギエフ等) / 大2(マリーザ等)）と一致。ja / en とも整合（`ja.json` と `labels.ts` を突合し、差分 0 を確認）。

---

## 推奨修正（優先度別）

### 高（M27 完了前に修正必須）

**高-1. `docs/progress/M27-01-completion-report.md` が無い。`progress-log.md` への追記も無い。**
指示書 §0.3 / §3.2 / 完了条件 9。§0.3 が挙げた理由（`check-progress-log-index.sh` は `*completion-report*.md` しか走査しないため、別名だと索引行が落ちても永久に緑になる）が現在そのまま成立している——実際、同検査は緑のままである。§3.2 の 6 規約はいずれも未提出:

1. すべての件数に母数を付ける
2. 段 1 で開発者が確定させた列挙の**逐語**（段 1 成果物 §9 に在るが、完了報告に写す必要がある）
3. §2.1.1-3 で数えた箇所の数と実際に通した数を**並べて**書く（一致しない場合は理由。低-13 参照）
4. **やらないと決めた**項目の 1 行（ガードクラッシュ／インパクトヒット（壁やられ無し）を入れないと決めたこと、軸名 3 通りの統一をやらないと決めたこと）
5. `check-enum-sync.sh` のベースラインが 24 → 28 へ動いた理由
6. 確かめられなかったこと（DB 実データの分布、`P4M-025` 源泉行の続き 等）

**高-2. マイグレ `000081` の消費が台帳に残っていない。**
`docs/progress/progress-log.md` の直近 2 件（`M14-RESEARCH-03` / `M26-01`）が「**次の払い出しは `000081` のまま**」と書いており、これが現在リポジトリ内で唯一の払い出し台帳の記述である。指示書 §4-6 は「`M14-03f` と `M28-01` も同じ帯を使う見込みであり衝突しうる」と明記している。**⇒ `progress-log.md` へ「M27-01 が `000081` を消費した／次の払い出しは `000082`」を追記すること。** 併せて設計卓からの払い出しの有無を確認すること（up ヘッダの「請求した」を裏付ける記録がリポジトリ内に無い）。

**高-3. §4-2（「大1」「大2」の整理）の射程拡大の経緯が、記録の行き先を持っていない。**
段 1 成果物 §9.4 が「**この経緯は完了報告に記録する**」と自ら宣言しているが、その完了報告が無い。あわせて次の 3 行は製造が編集できない（`D-382`）ため、完了報告 → 設計伝達レポート §4 経由でしか設計卓へ渡らない:

| followup ID | 現状の記述 | 実装後の実態 |
|---|---|---|
| `opponent-size-medium-to-standard` | 未着手 | **実施済み**（DB 値変更＋両側同期＋CSV 追随） |
| `opponent-size-large-variants-defer` | **要判断 / 未着手**（「判断が済んでいない」が禁止の理由） | **開発者が判断を供給し、ラベル変更と `large` 新設を実施**。禁止の前提が消滅している |
| `csv-import-opponent-size-whitelist-stale` | 本文が「許容値が `small` / `medium` / `large` のまま」 | **`small` / `standard` / `large` に変わった**。本文が失効 |

**高-4. 完了条件 6 のうち「新しい値が保存でき」「絞り込みで拾え」を見るテストが 1 件も無い。**
実査結果（新 4 値のリテラルを含むファイルの全数）:

| 面 | テストの有無 |
|---|---|
| CSV 往復 | **有**（`csvcore_test.go` `TestHitTypeM2701RoundTrip` / `TestHitTypeUnknownRejected` 8 値化） |
| 定数の形（末尾 4 値・PC 集合非所属・名前参照） | **有**（`punish.test.ts`） |
| i18n キーの 1 対 1 | **有**（`label-keys.test.ts` の `KEY_MAPS`。相手サイズを今回登録したのは良い改善） |
| **DB へ新 hit_type で保存** | **0 件**（repository / service / handler いずれも。`repository_test.go` の hit_type 系テストは `punish_counter` / `just_parry_punish_counter` の 2 値のまま） |
| **一覧の絞り込みで新値が拾える** | **0 件**（`ComboListFilters.test.tsx` に hit type の記述なし。リポジトリの `hit_type` フィルタも新値で通していない） |
| **E2E** | **0 件**（新 4 値・新サイズ値を通す spec なし） |

指示書 §2.4-2 が「本サブでは『新しい値が絞り込みに出る』ところまで」と明示的に射程指定した唯一の面が、機械検査を持たない状態で終わっている。**最低でも「`HIT_TYPE_VALUES` の全値が絞り込みの選択肢として描画される」1 本**（既存 `label-keys.test.ts` と同じ「値域を回して漏れを検出する」形）を足すこと。

**高-5. 失効した記述が 3 か所に残っている。**

- **5a. 画面モックの「変更後」比較表**（`docs/progress/M27-01-mock/m27-01-hit-type-and-size.html:228`）が `相手の大きさ = 大1` を**短いラベルのまま**描いている。開発者確定は「**全面で長くする**」（§9.2-2 の逐語＝「ボタンのラベルに入れる。」＋「全面で長くする」）であり、同 HTML の §4（L265-266）は `大1(ザンギエフ等)` へ更新済み。**同一ファイル内で更新漏れが起きている。** 本 HTML は「変更後」を名乗る承認済みの図であり、後任がここを写す。
- **5b. `migrations/000001_init_schema.up.sql`** の列コメントが失効。L69 `hit_type TEXT, -- normal/counter/punish_counter`（**3 値。`just_parry_punish_counter` 以降 5 値が落ちている**）、L70 `opponent_size TEXT, -- medium/large1/large2`（**`medium` は今回消滅した値**）。適用済みマイグレを編集しない方針自体は妥当だが、**失効を指し示す記録が `000081` のヘッダにも完了報告にも無い**（段 1 成果物 §6.1 にだけ在る）。`000081` のヘッダは他の論点を丁寧に書いているので、1 行足すのが安い。
- **5c. `docs/design/` の 3 か所が失効**（編集禁止＝**報告のみ**だが、報告先が無い）:
  - `05-screen-design.md:1502`「区分を判定できない反撃」の対象＝「`hit_type` が `normal` / `counter` / **NULL**」——実装（`punishlist/service.go:202`）は `!punishHitTypes[*e.HitType]` の否定形なので、**新 4 値もこのセクションに入る**。段 1 成果物 §2.4-2 が「段 3 で実装を確認する」と書いた項目であり、確認結果がどこにも無い。
  - `03-data-model.md:424` `hit_type` が 3 値のまま（`M18-01` の 4 値化すら届いていない）。同 L1041 / L1072 の `OPEN-001`（状況コード値マスタ）は本サブが実質的に閉じている。
  - `supp-001-detailed-design.md:311` `opponent_size` の値マスタ「`medium` / `large1` / `large2` ＝ 中/大1/大2」が完全に失効。
- **5d.（追加）`internal/service/combo/service.go:1389` のコメント**「未知値は取りこぼさず normal 相当で扱う(silent-drop 回避)」。書かれた当時、この分岐に落ちるのはデータ破損だけだった。**いまは「開発者が確定させた既知の 4 値」がここへ落ちる。** コメントが実態を説明しなくなっている（高-6 と同じ根）。

**高-6. `drive_impact_punish_counter` を PC ファミリーから外した帰結が、記録も固定もされていない。**
実装の実体（実査）:

| 層 | 実体 | 新 4 値での挙動 |
|---|---|---|
| FE | `PunishTree.tsx:277` / `PunishList.tsx:167` の `!PUNISH_COUNTER_HIT_TYPES.has(...)` | **「パニッシュカウンター版を作る」ボタンが出る**（`drive_impact_punish_counter` を含む 4 値すべてで） |
| BE | `service.go:1384` の `switch baseHit` | 新 4 値は `case` に無く **`default`（＝未知値・normal 相当）へ落ちる** ⇒ materialize が通る |
| BE | `service.go:1448` `if baseHit != model.HitTypeCounter` | **始動技ダメージ ×0.2 を加算**した `hit_type=punish_counter` の複製を生成する |
| BE | `punishlist/service.go:202` | 新 4 値は `unclassified`（区分を判定できない反撃）へ入る |

つまり、**「名前だけパニッシュカウンター」の値に対して、PC 版生成の導線が開き、PC 補正が上乗せされる。** 確定反撃ロジックの変更が別サブ扱いであること（開発者確定）は理解できるが、**その別サブが着地するまでの間、この挙動が既定になる**。

- FE 側は `punish.test.ts` が「PC 集合に入れない」ことだけを固定した。**その帰結（materialize 可能・+20%）を固定した箇所は無い。**
- **BE 側には新値の materialize 挙動を見るテストが 1 件も無い**（`default` 分岐に落ちること自体が未固定）。
- ⇒ ①現状挙動を Go テストで固定する（`default` 分岐へ落ちて materialize が通ること、`stun` / 壁やられ でも同様であることを、意図として書き留める）②完了報告と設計伝達レポートに「別サブが決めるまでこの挙動になる」と明記する——の 2 点を求める。**ロジックを直すこと自体は射程外**なので、直すのではなく固定と申し送りで閉じること。

### 中（M28 着手と並行可）

**中-7. 改名前に書き出された CSV に `medium` が残る経路が手当てされていない。**
`000081` は DB を直すが、利用者が改名前に export した CSV を取り込むと `opponent_size=medium` がそのまま入る（`ParseAndValidate` は未知値でも値を保持し `VAL-ENUM` WARNING を付けるだけ。`EnumStrict` の本番設定は 0 件）。取り込まれた行は:

- エディタで 4 ボタンのどれにも一致せず、未選択に見える（`OPPONENT_SIZE_OPTIONS` に `medium` が無い）
- 一覧・詳細では `labelFor` のフォールバックにより **生の `medium` が画面に出る**（`utils.ts:47` — 未知値はコードをそのまま返す）
- DB に `medium` が復活する（`000081` は 1 回きりの backfill なので拾わない。`migrations/000062` のヘッダが一般化した「1 回きりの backfill は、その後に INSERT される行を拾えない」と同じ形）

段 1 成果物 §4.2 の「既存データの移行」は DB だけを見ている。**取込側で `medium` → `standard` へ寄せるか、寄せないと決めるか**を判断し、少なくとも完了報告と申し送りへ記録すること。

**中-8. `labels.ts` の日本語直書きと `ja.json` の一致に機械検査が無い。**
今回 `labels.ts:109-111` に「**locale(ja.json)側と 1 文字も違えないこと**」という規約コメントを新設したが、それを見るテストが無い（`labels.test.ts` はゲージ欄ラベル 4 通りのみ）。実査では現在一致しているが、次に値を足す担当が片側だけ直しても `tsc` もテストも緑になる。**`HIT_TYPE_LABEL_JA` ≡ `HIT_TYPE_LABELS` と `OPPONENT_SIZE_LABEL_JA` ≡ `OPPONENT_SIZE_LABELS` を突き合わせる 1 本**で塞げる（`constants/combo-list.ts` 側の `*_LABELS` は `ja.json` から `jaMap` で導出されているので、そのまま比較できる）。置き場は `label-keys.test.ts` が既に「値域 ↔ キー」を見ているので同じ形にできる。今回 12 値へ増えた分だけ、割れたときの面積も増えている。

**中-9. `check-doc-inventory.sh` が `M27-01-hit-type-proposal.md` を「型に無いファイル」として挙げている。**
情報提供型（常に exit 0）なので検査は緑だが、`CLAUDE.md` §10.Y が求める 4 択（① 継続更新ファイルに行として足せなかったか再検討／② 型として `TYPES` へ昇格／③ **開発者の承認を得て** `EXCEPT` へ追加／④ 仕分け済みの置き場へ移す）のいずれも行われていない。指示書 §3.1 が明示的に許した成果物なので `②` か `③` が妥当と思われるが、**製造が黙って通す話ではない**。完了報告でこの出力に触れ、開発者へ扱いを仰ぐこと。

**中-10. `model.OpponentSize*` が本番コードから 1 か所も参照されていない。**
参照は `internal/service/comboio/csvcore/enum_sync_test.go`（テスト）のみ。`rules.go` の `DefaultOpponentSizes` / `DefaultHitTypes` は依然としてリテラルの再定義であり、`model` を参照していない（`rules.go` のコメント自身が「同期はここを直す手番でしか保てない」と認めている）。`CLAUDE.md` §4 の「正典を作る」目的は果たしたが、**リテラルの散在は減っていない**。
ただし `DefaultOpponentSizes` は「本体の値域と意図的にズレている」状態（開発者裁定でスコープ外）を表現する必要があるため、単純に `model.*` から組み立てると裁定の表現が失われる。**直すのは設計判断を伴う**ので、本サブでは「そういう構造である」ことを申し送るところまでで良い。なお `enum_sync_test.go` の「ズレを直せないなら、ズレを固定して動いたら赤くする」設計は妥当な折衷であり、これ自体は評価する。

### 低（将来対応）

**低-11. `web/src/constants/punish.ts` の `byName` は到達不能な実行時検査である。**

```ts
const byName = (name: HitType): HitType => {
  if (!HIT_TYPE_VALUES.includes(name)) { throw new Error(...); }
  return name;
};
```

引数の型が `HitType`（＝`HIT_TYPE_VALUES` からの派生）なので、`HIT_TYPE_VALUES` から値が消えれば `byName("punish_counter")` は**先にコンパイルで落ちる**。この throw が発火する経路は無い。しかも `PUNISH_HIT_TYPE_BY_GUARD` はモジュールのトップレベルで評価されるため、万一発火すれば import 時に投げてアプリ全体が白画面になる（失敗の仕方が悪い方に倒れている）。位置インデックス参照を名前参照へ直した判断そのものは正しく、**`byName` を外して素の文字列リテラルにしても同じ安全性が得られる**。

**低-12. `en.json` の `help.opponentSize` は参照 0 件。**
エディタは `jaLabel("help.opponentSize")` で `ja.json` 固定を引く（M24-07 の設計どおり）ため、en 側は使われない。ja/en の対称性を保つ意図なら妥当だが、その意図がどこにも書かれていない。

**低-13. 段 1 の母数と実際に触った数の食い違いに説明が要る。**
段 1 成果物 §4.2 は「Go テストの固定値 17 ファイル / 約 30 か所」「TS テストの固定値 6 ファイル / 8 か所」と数えたが、実際に変更されたのは **Go 11 ファイル / TS 5 ファイル**。実査では `opponent_size` 文脈の `medium` 残存は 0 件なので**通し漏れではなく、母数の数え方の違い**（`opponent_size` に言及するファイル数 vs `"medium"` を実値として持つファイル数）と判断できる。ただし指示書 §3.2-3 が「一致しないなら理由を書く」と要求しており、その説明が要る。

---

## 良かった点

1. **`medium` の取り違えがゼロ。** 技の強度（`light` / **`medium`** / `heavy`）は `inputResolution.ts` / `HitBoxLayout.tsx` / `SpecialMovePanel.tsx` / seedgen など多数の箇所に在るが、1 か所も巻き込んでいない。逆方向（`opponent_size` なのに置換し漏れ）も実測 0 件。全置換ではなく文脈を見て置換した形跡が明確。

2. **`punish.ts` の位置インデックス参照の是正。** `HIT_TYPE_VALUES[2]` / `[3]` は「途中へ値を挿入すると型検査もテストも緑のまま別の hit_type を指す」形であり、**指示書のどこにも書かれていない**。実査で見つけて、値を足す前に先に潰した。段 1 成果物 §2.4 が「1 と 3 は指示書に書かれていない。実査で見つけたものであり、1 は段 2 で必ず手当てする」と明示的に宣言してから実行しており、判断の筋道も残っている。

3. **`000081` の up / down ヘッダの質。** 「なぜ WHERE で絞るか」「NULL を `standard` に寄せない理由（重複判定キー VAL-C02 が NULL と文字列を別物として扱う）」「`recipe_cache` に触れない理由」「down が `large` を戻せない非対称は欠陥ではない」まで、次に触る担当が困る点を先回りで書いている。**`migrate_m2701_test.go` が 5 点を実測で固定し、破壊確認の手順（WHERE 句を外す／`large1` に変える）まで書き添えている**のも良い。

4. **`enum_sync_test.go` の「ズレを固定する」設計。** 直すのが射程外なら現状を書き留めて、変わったら赤くする——という形は、着手前に `large1` / `large2` を通す経路が Go 0 件・E2E 0 件で「1/3 しか一致していない状態が誰にも気づかれず残った」という実測に基づいており、根拠が具体的。テスト本文に「このテストが赤くなったら『壊れた』ではなく『ズレが動いた』の合図である」と書いてある点も良い。

5. **`InfoMark` の配置理由の突き止め方。** 「`useFieldSequence` の `focusableIn` が `querySelector`（単数）で `[data-seq-stop]` 内の最初の focusable を拾うため、ⓘ をボタン群より前に置くと数字キーが死ぬ」という因果を特定し、`hint` へ渡す形を選び、**DOM 順そのものをテストで固定**した（`compareDocumentPosition` と同じセレクタでの引き直しの 2 重確認）。実査で `m24-12-editor-rebuild.spec.ts`（キーボード操作の E2E）5 件が緑であることも確認した。

6. **相手サイズを `label-keys.test.ts` の `KEY_MAPS` へ登録したこと。** 「状況 4 軸のうち相手サイズだけが値域 ↔ キーの 1 対 1 検査から外れていた」という既存の穴を、本サブのついでに塞いだ。手足しで入っていた重複行も同時に削っており、置き換えとして正しい。

7. **既存 4 値・既存データを 1 つも動かしていない**（§4-4）。新値は必ず末尾。`git diff --numstat` を全 38 ファイル分確認し、**新規追加ファイルに deletions が付いているものは 0 件**（`E-225` の型の事故なし）。既存ファイルの deletion もすべて 1 対 1 の置換であり、上書きで消えたテストは無い。

---

## 確かめられなかったこと（不明）

- **不明: 段 1 成果物の開発者承認の実在。** リポジトリ内の証拠は提案書 §9 の自己申告（逐語の書き写し）のみであり、外部の記録と突合できない。§9 の内容は詳細で、追問への回答や「私が指示で間違えたか？」といった自然な揺れまで含んでおり捏造とは考えにくいが、**レビュー側で裏を取る経路が無い**。
- **不明: マイグレ `000081` が設計卓／開発者から実際に払い出されたか**（高-2）。
- **不明: `make e2e` 全数の結果。** 抜き取り 2 本（`m19-03` 8 件・`m24-12` 5 件）は緑を確認したが、全数は実行していない。
- **不明: DB 実データにおける `opponent_size='medium'` の行数。** 段 1 成果物 §6.3-1 と同じ理由（利用者の環境にしか無い）。マイグレは `WHERE` で絞るため 0 行でも安全に通ることは確認済み。

---

## 制約事項

- 本レビューはコード上で判定可能な範囲のみ対象。実際の動作確認・パフォーマンス・実機テストは別途実施が必要。
- 本レビューでコードは 1 行も変更していない。作成したファイルは本報告書のみ。Git の書き込み操作（コミット含む）は行っていない。

---

*以上*

---

## 取り込み結果（自動トリアージ・2026-09-02）

**採否の総計: 13 件中 13 件を採用（不採用 0 件）。★「高」の不採用は 0 件。**

**★レビューは `ac006bd` 時点に対するもの。** 高-6 に対応する修正は、レビュー完了の直前に開発者判断（「生成の対象外にするだけ入れる」）を得て着手済みだった。報告書末尾の注記が指摘したとおりである。

### 高（6 件・全件採用）

| # | 指摘 | 採否 | 何をしたか |
|---|---|---|---|
| 高-1 | 完了報告が無い／`progress-log.md` 追記も無い | **採用** | `docs/progress/M27-01-completion-report.md` を作成し、§3.2 の 6 規約を全て満たした。`progress-log.md` へ索引行を追記 |
| 高-2 | マイグレ `000081` の消費が台帳に無い | **採用** | 完了報告 §「併せて更新が要るもの」と `progress-log.md` の索引行へ「`000081` を消費／次は `000082`」を明記 |
| 高-3 | §4-2 逸脱の経緯の行き先が無い | **採用** | 完了報告 §「指示書との差分」に経緯を記録。followup 3 行の現状と実態のズレは §「申し送り」へ（★`D-382` により本表は編集していない） |
| 高-4 | 「保存でき／絞り込みで拾え」のテストが 0 件 | **採用** | Go: `TestList_FilterByHitType_M2701`（8 値 × 相手サイズ 4 値を DB へ保存し `hit_type` フィルタで拾う）。FE: 絞り込みの選択肢に値域の全値が出ることを固定。**破壊確認済み**（フィルタ句を無効化すると「1 件のはずが 8 件」で赤） |
| 高-5a | モックの比較表が短ラベルのまま | **採用** | `大1` → `大1(ザンギエフ等)` へ是正。★同ファイルの「変更前」パネル（L250-251）は短ラベルが正しいので触っていない |
| 高-5b | `000001` の列コメント失効の記録先が無い | **採用** | `000081` のヘッダへ節を新設し、失効する 2 行と「値の正典は `internal/model/combo.go`」を明記。★適用済みマイグレ本体は編集していない |
| 高-5c | `docs/design/` 3 か所の失効の報告先が無い | **採用** | 完了報告 §「設計書への波及」へ 9 件を列挙。**`05-screen-design.md:1502` の述語の実装確認結果も書いた**（新 4 値も `unclassified` に入る＝段 1 §2.4-2 が段 3 で確認すると宣言した項目） |
| 高-5d | `service.go:1389` の「未知値」コメントが実態を説明しなくなった | **採用** | `default` 分岐へ「M27-01 で足した壁やられ 2 種と stun がここへ落ちる。PC ではないため生成できてよい」を追記 |
| 高-6 | DI-PC を PC 集合から外した帰結が未固定・未記録 | **採用（開発者判断で「直す」を選択）** | 開発者へ照会し「**生成の対象外にするだけ入れる**」を得た。BE の materialize 対象外分岐と FE の `PUNISH_COUNTER_HIT_TYPES` へ `drive_impact_punish_counter` を追加。**両方向をテストで固定**（PC は弾かれる／壁やられ 2 種・stun は生成できる）。★タブ分け（`punishHitTypes`）は触っていない＝ロジック本体は別サブ |

> **★レビュアーは「ロジックを直すこと自体は射程外なので、直すのではなく固定と申し送りで閉じること」と述べていた。** 本サブでは**開発者に照会して判断を仰ぎ、直す側を選んだ**。レビュー時点では開発者判断が存在しなかったための助言であり、判断が供給されたことで前提が変わっている。**タブ分け・始動技制約というロジック本体は触っていない**ため、レビュアーの懸念（射程の膨張）は満たしている。

### 中（4 件・全件採用）

| # | 指摘 | 採否 | 何をしたか |
|---|---|---|---|
| 中-7 | 改名前の CSV に `medium` が残る経路が未手当て | **採用（判断して記録）** | **取込側で寄せないと決めた。** 理由 2 つ ─ (1) 開発者裁定 (a)「1 語だけ直す」の射程を超える (2) `VAL-ENUM` WARNING が出るため silent ではない。**ただし「生の `medium` が画面に出る」ことは実害**なので、完了報告 §「申し送り」へ具体案つきで登録候補として出した |
| 中-8 | `labels.ts` ↔ `ja.json` の一致に機械検査が無い | **採用** | `label-keys.test.ts` へ 1 本追加（`HIT_TYPE_LABEL_JA` ≡ `HIT_TYPE_LABELS` / `OPPONENT_SIZE_LABEL_JA` ≡ `OPPONENT_SIZE_LABELS`） |
| 中-9 | `check-doc-inventory` の 1 件が §10.Y の 4 択のどれも取っていない | **採用（解消済み）** | **★例外を足すのではなく「型に合わせる」で解消した。** 本報告書が指す `M27-01-hit-type-proposal.md` は **`M27-01-report.md` へ改名済み**（TYPES の「サブ単位のレポート」に収まる）。⇒ `check-doc-inventory` は「型に無いファイル 0 件」。**★`EXCEPT` へは足していない**（`CLAUDE.md` §8＝「検査を通すために足すと、本検査は存在しないのと同じになる」）。**★本報告書の本文中の旧ファイル名は `ac006bd` 時点の記録として残してある。** |
| 中-10 | `model.OpponentSize*` が本番参照 0 件 | **採用（申し送りまで）** | 完了報告 §「申し送り」へ記録。★レビュアー自身が「本サブでは申し送るところまでで良い」としており、それに従った |

### 低（3 件・全件採用）

| # | 指摘 | 採否 | 何をしたか |
|---|---|---|---|
| 低-11 | `byName` は到達不能な実行時検査（発火すると import 時に白画面） | **採用** | 実行時 `throw` を外し、型注釈だけの恒等関数にした。未使用になった `HIT_TYPE_VALUES` の import も外した |
| 低-12 | `en.json` の `help.opponentSize` は参照 0 件 | **採用** | 参照側（`ComboEditorBasicFields.tsx`）へ意図をコメントで明記（既存 `help.*` と同じ構造・ja/en の対称を崩さない） |
| 低-13 | 段 1 母数と実変更ファイル数の食い違いの説明が要る | **採用** | 完了報告 §「母数と通した数」で説明した |

### 再レビューの往復

**0 回。** 上限は 2 回（`CLAUDE.md` §9 停止規律）。高の不採用が 0 件であり、エスカレーションは発生していない。

### 取り込み後の検証

`go test ./...` 緑 ／ `pnpm test` 2343 件緑 ／ `make e2e` 226 件緑 ／ `tsc --noEmit` 緑 ／ `check-enum-sync` `check-import-order` `check-md-emphasis` 緑。
