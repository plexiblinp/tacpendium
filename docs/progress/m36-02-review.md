# M36-02 レビュー報告書

| 項目 | 内容 |
|------|------|
| 対象 | `M36-02`(リリースの門と来歴)。指示書 v1.1.0 ／ チェックリスト v1.1.0 |
| 差分範囲 | `0e50b76` → `5b82bc4`(HEAD)。7 ファイル |
| レビュー日 | 2026-09-19 |
| レビュー担当 | レビュー担当 Claude Code |
| 判定 | **条件付き合格**(高 4 件 ／ 中 5 件 ／ 低 6 件。**高のうち実装の誤りは 2 件で、どちらも数行で直る**) |

> **★レビュー中に完了報告が到着した。** 着手時点の HEAD は `479ab36`(コード 6 ファイルのみ)であり、その時点では完了報告・`progress-log` 索引行・設計伝達レポートのいずれも存在しなかった。**作業中に `5b82bc4`「完了報告(Phase A/B 時点)を書く」が入ったため、報告の内容も判定に含めた。** 本報告書の「実測」と書いた箇所は、**すべて私が独立に回した結果**であり、完了報告の記述を写したものではない。

---

## 総評

**門と検査の作りは本サブの中でも質が高い。** `check-manual-images.sh` は「PNG が 0 枚でないか」を明確に避け、参照 1 件ごとに実体を見る。陰性 3 ／ 陽性 6 の対照に「空集合の罠」「単引用符での抽出漏れ」「2 本目の説明書だけ壊れている」まで入っており、**チェックリストが問う前に自分で潰しにいった**跡がある。私も実物の複製で図を 1 枚消して赤を再現した。射程 7 の破壊確認、鍵未登録時の拒否、配布 3 本に `.gitkeep` / `SCREENSHOT-RULES.md` が 0 件であることも、**すべて独立に再現できた**。

**外部仕様は原則として記憶で書かれていない。** 私は一次情報を独立に引き直し、(a) `attest-build-provenance` の wrapper 化と「新規実装は `actions/attest` を使え」の逐語、(b) `create-storage-record` / `push-to-registry` の既定、(c) **アクション自身の CI が `artifact-metadata` を使っていないこと**、(d) `anchore/sbom-action` の入力とライセンス、(e) `gh attestation verify` の必須オプション、の 5 点が記述と一致することを確認した。

**ただし 4 点のうち 1 点だけ、一次情報ではないものを一次情報として書いている**(高-1)。タグ署名検証の落とし穴の出典が**第三者の個人リポジトリの PR**(`jdmacleod/unbagged#34`。2026-09-08 マージ・Claude との共著)であり、`release.yml` のヘッダにも「★一次情報が指摘する落とし穴」と書かれている。**主張そのものは正しく、対処も妥当**だが、出典の格付けを誤っている点は §0.2 が最も警戒した形に隣接する。

**もう 1 件の実装側の高は、自己検査が永久に回らないことである**(高-2)。`generate-release-notes.sh` は「`自己検査: 合格` は `check-artifact-integrity.sh` が拾うマーカーである」と書いているが、**同検査は `scripts/check-*.sh` しか走査せず、本ファイルを拾わない**(実測: 検査 18 件に不在)。対照を 6 件書いたのに誰も回さない状態であり、かつ `CLAUDE.md` §8 の表へ載せたことで「常設検査が担保している」と読める。

**残りは「開発者の手番」と「Phase D の手番」である。** attestation / タグ署名 / 公開の実走は製造環境では原理的に作れず、完了報告 §8 に停止規律の必須 5 フィールドで正しく記録されている——**これは規律違反ではなく正規の完了形式である。** `progress-log` の索引行と設計伝達レポートは、本レビュー時点でまだ無い。

---

## 設計準拠性レビュー結果

### 段 0 — 外部仕様の実査(チェックリスト A) … ○

| # | 見たこと | 判定 |
|---|---|---|
| A-1 | 一次情報の URL | **△**。4 点すべてに URL と確認日(2026-09-19)が付く。**ただし #3(タグ署名の検証)の主たる出典が一次情報ではない**(高-1) |
| A-2 | 確認日 | **◎**。完了報告 §2.2 の表と `release.yml` ヘッダの両方に在る |
| A-3 | 非推奨を掴んでいないか | **◎**。**私が独立に引き直し、下表のとおり一致を確認した。** とくに「旧アクションは非推奨ではなく wrapper として動き続ける」を掴んでいる点は、§4.2 が警告した「動いたまま古い」の回避そのものである |
| A-4 | SBOM の新規ツールを入れる前に諮ったか | **○**。完了報告 §2.3 に「Plan Mode・実装前に諮り、`anchore/sbom-action` を採る裁定」と記録された。**レビュー側からは Plan Mode のやり取りを検証できない**(制約事項に記載) |

私が独立に引いた一次情報(2026-09-19 確認):

| 完了報告 / コード内の記述 | 私の実査結果 |
|---|---|
| `attest-build-provenance` は v4 以降 `actions/attest` の薄い wrapper。README が「新規実装は `actions/attest` を使え」と明記 | **一致**。逐語 "As of version 4, `actions/attest-build-provenance` is simply a wrapper on top of `actions/attest`" ／ "new implementations should use `actions/attest` instead" |
| `artifact-metadata` は storage record 用。`push-to-registry` を使わないなら不要 | **裏付けあり**。`action.yml` の `create-storage-record` は既定 `true` だが「push-to-registry が true のとき」に限り、`push-to-registry` の既定は `false`。**かつ `actions/attest` 自身の CI の `test-attest` job は `contents: read` / `attestations: write` / `id-token: write` の 3 つだけ**(私が実査)。★README の permissions ブロック自体は `artifact-metadata: write` を挙げているので、**推論は強い証拠に支えられているが、実走までは確定しない**(中-1) |
| `anchore/sbom-action@v0`(Apache-2.0)、入力 `path` / `format` / `artifact-name` / `output-file` / `upload-release-assets` | **一致**。Apache-2.0 は `CLAUDE.md` §6 の許可列 |
| `gh attestation verify <file> --repo <owner>/<repo>`(`--owner` か `--repo` が必須) | **一致** |
| action の版は既存 2 本の流儀(floating major) | **一致**。`checkout@v7` / `setup-go@v7` / `setup-node@v7` / `pnpm/action-setup@v6` は `nightly-crossbuild.yml` ・`pr-checks.yml` と同一 |
| **タグ署名検証の落とし穴の出典** | **不一致(格付け)**。→ 高-1 |

### 門(射程 1・チェックリスト B) … ◎

| # | 判定 |
|---|---|
| B-1 破壊確認 | **◎**。自己検査の陽性 6 件に加え、**私が実物で再現した**——`docs/usermanual` をスクラッチへ複製して `ch21-home-1.png` を消し、`NG … 参照先が在りません` / EXIT=1 を得た(完了報告は `ch05-combo-list-1.png` で同じことを実ツリー上で行い、`git` 差分 0 で戻したことまで示している) |
| B-2 全数実在か | **◎**。参照 1 件ごとに `-f` と `-s` を見る。陰性対照 C(参照されない図が余分に 2 枚でも緑)が「枚数で固定していない」証拠になっている |
| B-3 `make release-archives` を塞いでいないか | **◎**。`build-release-archives.sh` にも `Makefile` にも門を足していない。**私が `make release-archives` の成果物(`dist/release/` 3 本)に対し `check-release-archive.sh` を回し EXIT=0 を確認した** |
| B-4 呼ばれているか | ○。`release.yml` の 2 段目で `if:` 無しに呼ばれる。**ただしワークフロー自体が一度も走っていない**(高-3) |
| B-5 クイックスタートも母集団か | **◎**。ディレクトリ直下の `*.html` 全数走査。実測「説明書 2 本 / 図の参照 29 件」に `qs-first-combo-1.png` が入る。陽性対照 5 が「2 本目だけ壊れている」を潰している |

### 来歴(射程 2〜4・チェックリスト C) … ○

| # | 判定 |
|---|---|
| C-1 attestation の検証が通った出力 | **×(未達・ただし停止規律で記録済み)**。製造環境には `gh` が無く `git tag` は deny。**Sigstore の短命証明書は実行中の Actions でしか生まれない**ため原理的に作れない。完了報告 §8 原稿 1 が必須 5 フィールドで記録しており、**記録して止めることは正規の完了形式である**(`CLAUDE.md` §9)。⇒ 開発者の手番(高-3) |
| C-2 利用者が打てるコマンドか | **◎**。`gh attestation verify <archive> --repo <slug>` を 3 本分そのまま出す。私が生成して確認した |
| C-3 SBOM の中身が配布物と一致するか | **△**。`path: .` はワークスペース全体の走査(中-2) |
| C-4 ライセンス層の 3 分と矛盾しないか | ○(判定不能)。SBOM を実際に出していないため中身を見られない。`REUSE.toml` 側は無変更 |
| C-5 鍵を作っていないか | **◎**。`.github/allowed_signers` は全 37 行がコメント。鍵 0 件 |
| C-6 未登録で明示的に落ちるか | **◎**。**5 段の明示的拒否**(タグ不在 / 軽量タグ / 台帳不在 / 実効登録 0 件 / `git verify-tag` 失敗)。**私が (4) の述語を再現し、現状 `EFFECTIVE=0`・鍵 1 行で `1` になることを確認した。** `${RUNNER_TEMP}/tag-verified` の哨兵で公開段を二重に守り、**将来 `if:` を緩めても署名なしでは公開されない** |
| C-7 鍵を用意する側の手順 | **◎**。完了報告 §7 に 4 段(鍵作成 → 台帳登録 → rc タグで実走 → `gh attestation verify`)。**さらに §7.4 で「鍵を登録する*前に*打って赤を見る」という否定側の実測まで依頼している**——素通りしないことの証拠を残す発想であり、指示書 §4.5 の要求を超えている |

### CI(射程 5〜6・チェックリスト D) … ○

| # | 判定 |
|---|---|
| D-1 CI から出るか | **◎**。`gh release create` は runner 上。手元からのアップロード経路は無い |
| D-2 nightly を格上げしていないか | **◎**。`git diff --numstat` で `nightly-crossbuild.yml` ・`pr-checks.yml` とも差分 0。**別ファイル・別契機で構造的に分離**している |
| D-3 nightly が壊れていないこと | ○。1 バイトも触っていないため出力は不要(§2.6-3 は「触ったなら」)。**私も YAML パースで 3 本とも job 構成が壊れていないことを確認した** |
| D-4 検証手順の正本が 1 つか | **◎**。SHA-256 の節は正本から**抽出**、アーカイブ名は `release-targets.sh` から引く。**手で 2 か所に書いていない。** 抽出が空振りしたら `exit 2` で止まる(陽性対照で確認) |
| D-5 `DES-002` §11.3 の CHANGE 原稿 | **未了**。§11.3 は「**2 本立てとする**」と明記しており、本サブで 3 本目(Release)が増えた。**設計伝達レポートがまだ無い**(高-4) |

### 射程(チェックリスト E) … ◎

`release-targets.sh` 差分 0 ／ `docs/usermanual/` 差分 0 ／ `docs/design/` ・`followup-backlog.md` 差分 0 ／ マイグレ 0 本 ／ `B1` を取りにいっていない(むしろリリース本文の表に「❌ コード署名は付いていません」と明記し、代替であることを利用者へ隠していない)。**すべて `git diff --numstat` で確認した。**

### 射程 7(チェックリスト G-1〜G-3) … ○

- **G-1 ◎**: `check_archive_names()` が `RELEASE_TARGETS` の 1 列目を全数照合する。
- **G-2 ◎**: **私が再現した**——`✅ 陽性対照(射程7): アーカイブ名を 1 つ書き換えたら検出した(tacpendium-windows-amd64.zip / 2 箇所)`。完了報告はさらに**正本 `release-targets.sh` 側を書き換える**破壊確認(`linux-amd64` → `linux-x86_64`)を実ツリーで行い、`git` 差分 0 で戻している。
- **G-3 ◎**: 置き場の理由が「そのスクリプト自身が宣言していた限界」と結び付けて書かれている。**残る穴(2 列目＝実行ファイル名)を限界の節へ自分で書き足し、停止時記録の原稿 2 にも回している。**
- **△**: 照合は**片方向**である(中-3)。

### 射程 8(チェックリスト G-4 / G-5) … ◎(判断は妥当。独立に検証した)

**製造の「足さなかった」判断を支持する。** 私が独立に確認した事実:

| 確かめたこと | 結果 |
|---|---|
| `docs/usermanual/images/.gitkeep` の存在 | **存在しない**(`ls -a` 実測) |
| 消失点 | **`3bc5c71`(M38-03・開発者指示)が `.gitkeep` → `SCREENSHOT-RULES.md` へ改名し、同じコミットで `RELEASE_MANUAL_EXCLUDE` へ `images/SCREENSHOT-RULES.md` を足している** |
| 配布 3 本の中身(G-4) | **`gitkeep` / `SCREENSHOT` の該当 0 件**(`tar -tzf` × 2 ／ `unzip -l` × 1 で私が実測) |
| 参考 | `manual/images/*.png` は 3 本とも **29 枚**入っている |

⇒ 指示書 §2.7b-1 をそのまま実施すると、**存在しないファイル名の除外エントリ**が 1 行増える。除外配列は「在るものを抜く」ためのものであり、死んだ行は次に読む人へ「まだ `.gitkeep` が在る」と誤読させる。**足さない判断は妥当である。** 完了報告 §0.1 が「★この判断は開発者が覆せる」と明記し、§8 原稿 3 で停止時記録にもしている点も適切(**`followup-backlog.md` は 1 文字も編集していない**＝`D-838` を守っている)。

**G-5 ◎**: `has_dir`(陰性対照 E)が `.gitkeep` に依存しないことは、`check-release-archive.sh --self-test` が EXIT=0(陰性 5 / 陽性 6)であることで裏付けられる。**私も回して確認した。**

### 報告(チェックリスト F) … △

| # | 判定 |
|---|---|
| F-1 CHANGE 原稿が設計伝達レポート §1 / §6 に | **未了**(高-4)。完了報告 §9 は「原稿は設計伝達レポート §1 と §6 へ」と正しく書いているが、**レポート本体がまだ無い** |
| F-2 完了報告 ＋ progress-log 索引行 | **△**。完了報告は在る(482 行)。**`progress-log.md` の `M36-02` 節は無い**(grep の当たり 1 件は `M36-01` 側からの参照)。完了報告 §6 は Phase D で回す予定と読める |
| F-3 報告を入力に取る検査の回し直し | **未了**。§6 の 4 行がすべて「Phase D で記入」。**設計として正しい**(`D-890` を踏まないため報告を書いた後に回す)が、**現時点では空欄である** |
| F-4 全数テストの `EXIT=` と `FAIL` 件数 | **◎**。§5.1 に Go / Vitest / E2E の 3 行。**私は Go を独自に回し `EXIT=0` / `ok` 60 件 / `FAIL` 0 件で一致を確認した**(ただし私の実行は 60 件中 58 件が `(cached)`。低-5 参照) |
| F-5 版ゲート 6 点 | **◎**。§1 に 5b を含む 7 行 |

---

## 設計準拠性以外の指摘事項

1. **「一次情報」の格付けを誤っている**(高-1 で詳述)。
2. **`check-manual-images.sh:39` に韓国語が混入**。逐語「指示書 §3-2 が**명시적**に禁じている」。`明示的` の誤り。
3. **`src` 抽出の母集団が `<img` に限定されていない。** `src_attrs` は `grep -oE 'src="[^"]*"'` で**ファイル中の全 `src=`** を拾う一方、`img_tags` は `<img` の個数だけを数える。現状は `<img` 29 ／ `src="` 29 で一致するが、**`<script src="…">` や `<source src="…">` を 1 つ足すと件数が食い違い、「★src 属性が二重引用符でない img があります」という*誤った診断*で赤になる。**
4. **`extract_section` の終端パターンが、同じ正本に実在する見出し形と噛み合っていない。** 終端は `/^■ /`(■ + 半角空白)だが、`dist-readme.txt` には `■方法 A(推奨): …` ・`■方法 B: …` のように**空白の無い見出しが実在する**(107 / 116 行目)。いまは SHA-256 節の直後が `■ 常駐と終了 (Windows)` なので正しく止まる(陰性対照 B が見ているのはこの 1 ケースだけ)。**将来ここに空白なしの見出しが入ると、抽出が静かに文書末尾まで走る**——抽出行数の上限が無いため気付けない。
5. **SBOM の「中身がある」確認が `grep -o '"name"' | wc -l` である。** SPDX-JSON の `packages` 配列長でも、対象バイナリが載っているかでもない。`jq` は allow リストに在る(`CLAUDE.md` §11)ので `jq '.packages | length'` にすると同じ行数で意味のある検査になる。
6. **`check-dist-readme.sh` の `victim_line` は行番号ではなく出現行数**(`grep -cF` の結果)。出力の `(… / 2 箇所)` は正しいが、変数名が誤解を招く。`victim_hits` が適切。
7. **`release.yml` の `PRERELEASE` が裸展開**。`set -u` 下で定義済みのため動くが、空文字の意図的な語分割に頼っている。なお**本リポジトリに `shellcheck` は未インストール**であり、私は 714 行の新規シェルに静的検査を掛けられなかった。
8. **完了報告 §3 の数え違い**。「新規 3 ファイル(`allowed_signers` / `release.yml` / `check-manual-images.sh` / `generate-release-notes.sh`)」——**括弧内は 4 件**である。
9. **完了報告 §5.5 の結果がどこにも無い。** 「結果は §6 の表に記す」と書いているが §6 は全行が「Phase D で記入」。**私が `check-md-emphasis.sh` を完了報告へファイル引数モードで回し、検出 0 行(EXIT=0)を確認した**ので、値そのものは緑である。
10. **`dist-readme.txt` の記述が本サブで片手落ちになった。** 同ファイルは「※これは『ファイルが壊れていないこと』の確認です。**『配布元が正しいこと』の証明ではありません**」で終わり、attestation への導線が無い(`attestation` / `SmartScreen` の語は 0 件。実測)。**本サブ以後は「配布元を確かめる手段」が実在するのに、アーカイブを展開した利用者だけがそれを知らない。** `docs/usermanual/` は編集禁止(§3-8)なので申し送りが正しい経路だが、**完了報告 §8 の原稿 3 件はどれもこれを扱っていない。**

---

## 推奨修正(優先度別)

### 高(M36 完了前に修正必須)

- **高-1: 「一次情報」でないものを一次情報として書いている。出典を差し替えるか、格付けを書き直すこと。**
  完了報告 §2.2 の実査 #3(タグ署名の検証)の主たる出典は **`https://github.com/jdmacleod/unbagged/pull/34`** であり、**私が実査したところ、これは第三者の個人リポジトリの PR** である(タイトル "Commit the signers file, and verify every tag against it"、2026-09-08 マージ、**Claude との共著と明記されている**)。逐語として引かれた "refusing an empty tag set, since a checkout without tags would otherwise verify nothing and report success" は、**その PR の説明文**であって git / GitHub の文書ではない。**同じ誤りが `.github/workflows/release.yml` のヘッダにも入っている**——「★一次情報が指摘する落とし穴＝タグを持たない checkout は『検証対象 0 件』で緑になる」。
  **★指摘の重さ**: 主張そのものは正しく、5 段の拒否という対処も妥当である。**問題は出典の格付けであり、これはチェックリスト A-1(★★★「一次情報の URL が付いているか」)と §0-2(★★★)が守ろうとしたものそのものである。** かつ**コード内に残った記述は後任が読んで複製する**——「一次情報が言っている」と書いてあれば、次の人は裏を取らない。
  **⇒ 対応**: (a) 出典を `git-verify-tag` / `git config gpg.ssh.allowedSignersFile` の公式ドキュメント(git-scm.com)と GitHub Docs へ差し替える、または (b) 「一次情報」を「第三者の実装例(格付け＝二次情報)」と書き直し、**空集合の罠は実装例からの示唆であって公式の記述ではない**と明示する。**(a) を推す。** 他の 3 点は公式リポジトリの README / ソースを引いており、ここだけが浮いている。

- **高-2: `generate-release-notes.sh` の失効した記述を直し、自己検査が実際に回るようにする。**
  同ファイル末尾付近の逐語「**★文言 "自己検査: 合格" は check-artifact-integrity.sh が拾うマーカーである。変えないこと。**」は**事実に反する**。`check-artifact-integrity.sh` の走査対象は `find scripts -maxdepth 1 -name 'check-*.sh'` ＋ 明示 3 本(`archive-resolved.sh` / `design-desk-arm.sh` / `design-desk-guard.sh`)であり、**`generate-release-notes.sh` は対象外である**。**私の実測**: 同検査は「検査 18 件 / ALLOW 除外 1 件」で緑になるが、その 18 件に本ファイルは現れない(`check-manual-images.sh` / `check-dist-readme.sh` は現れる)。完了報告 §5.2 の「新設 **2 本**の `--self-test` が実際に走ったことも確認」という書き方は正確だが、**`CLAUDE.md` §8 の表には 3 本目として載っている**ため、表だけを読むと「常設検査が担保している」と読める。
  **⇒ 対応**: (a) `check-artifact-integrity.sh` の明示リストへ 1 行足す(**`archive-resolved.sh` ・`design-desk-arm.sh` が同じ理由で足された前例がある**)、または (b) コメントから当該文を削り、`CLAUDE.md` の行にも「自動走査の対象外・手動で回す」と書く。**(a) を推す。**
  **★この指摘を「低」に落とさないこと**——動作は正しいままなのでテストも lint も型検査も緑であり、人が読む以外に見つける経路が無い。**対照を 6 件書いたのに誰も回さない**状態は、本サブが一貫して避けてきた「在るが効かない検査」そのものである。

- **高-3: attestation / タグ署名 / 公開の実走を、開発者の手番として実施する**(製造の修正事項ではない)。
  完了条件 §6-3 / §6-5 は「検証が通った出力」を ★★★ で求めるが、**製造環境では `gh` が無く `git tag` が deny であり、Sigstore の短命証明書は実行中の Actions でしか生まれない。⇒ 原理的に作れない。** 完了報告 §8 原稿 1 が必須 5 フィールドで記録しており、**記録して停止することは正規の完了形式である**(`CLAUDE.md` §9)。**⇒ レビューとしては規律違反とは扱わない。** ただし **`M36` を閉じるには実走が要る**ため、優先度は高に置く。手順は完了報告 §7(とくに **§7.4 の「鍵を登録する前に打って赤を見る」**)。**中-1 の権限確認もこの実走で同時に片付く。**

- **高-4: `progress-log.md` の索引行と設計伝達レポートを出す**(Phase D)。
  本レビュー時点で `progress-log.md` に `M36-02` の節は無く(`grep` の当たり 1 件は `M36-01` 側の参照)、`docs/handover/design-reports/` にも `m36-02` のファイルが無い。`CLAUDE.md` §8 は「**サブ完了時の追記は必須**」と定め、完了条件 §6-10 / §6-11 も同じ。**設計伝達レポートには少なくとも次を載せること**: §1 / §6 に **`DES-002` §11.3 の CHANGE 原稿**(「**2 本立てとする**」が 3 本になった。§11.2 側にも来歴の as-built が要る)、§4 に完了報告 §8 の**原稿 3 件**と、**本レビューの指摘 10(=`dist-readme.txt` に attestation への導線が無い)の申し送り**。

### 中(M37 着手と並行可)

- **中-1: `actions/attest@v4` の権限を、本番タグより前に実証する経路を作る。** `artifact-metadata: write` を外した判断は**アクション自身の CI という強い証拠**に支えられている(私も追認した)が、**公式 README の permissions ブロックは 3 つを挙げている**。いま attestation 2 段は `if: github.ref_type == 'tag'` で守られており、**`workflow_dispatch` の dry-run では走らない。⇒ 最も壊れやすい 3 段(署名検証 / attestation / 公開)が dry-run で一度も動かない。** 完了報告 §7.2 の rc タグ実走でこれを兼ねるなら、**「dry-run が確かめるのは門とビルドと SBOM までである」ことを `release.yml` のコメントへ 1 行書くこと**(いまの「公開せずに『門とビルドが通るか』だけを確かめる」は正直だが、attestation が対象外であることまでは読み取れない)。
- **中-2: SBOM の母集団を配布物へ寄せるか、寄せない理由を書く。** `path: .` は `node_modules`(devDependencies) ・`web/e2e` ・ツール類を含むワークスペース全体の走査であり、**配布物(embed 済みバイナリ 1 本 ＋ 説明書 ＋ ライセンス)の部品表とは母集団が違う**。チェックリスト C-3 は「中身が配布物と一致しているか。⇒ 出しただけになっていないか」を ★★ で問う。**syft はビルド済みバイナリ(`dist/tacpendium-*`)を直接走査できる**ので寄せるか、「ソースツリーの SBOM を出す」という選択を理由付きで報告へ書くこと。あわせて検証を `jq '.packages | length'` へ(指摘 5)。
- **中-3: 射程 7 の照合が片方向である。** 「`RELEASE_TARGETS` の名前が `README.txt` に現れるか」しか見ないため、**旧名が残ったまま新名も書かれている状態は緑になる**(改名では旧名が消えるので今回の破壊確認は通る)。`README.txt` 側の `tacpendium-*.(zip|tar.gz)` 形を拾って `RELEASE_TARGETS` に無いものを警告する逆方向を足すと穴が閉じる。
- **中-4: 門が見ているのは源泉であって、組み上がったアーカイブではない。** `check-manual-images.sh` は `docs/usermanual/` を見る。`check-release-archive.sh` は `manual/images` を**ディレクトリとして在るか**しか見ない(`CLAUDE.md` §8 の逐語)。**⇒ 「源泉には図が全数在るが、アーカイブには 1 枚も入っていない」状態は、2 つの検査を両方すり抜ける。** 現状は健全である(**私が実測: 3 本とも `manual/images/*.png` が 29 枚**)ため緊急ではないが、`D-890` の問い(「配布してよいか」)に答えるなら、**展開先のディレクトリを `check-manual-images.sh` に渡すだけ**で閉じられる(同スクリプトは既にディレクトリ引数を取れる)。
- **中-5: 指摘 3(`src` 抽出の母集団)と指摘 4(`extract_section` の終端)を直す。** どちらも**いまは緑だが将来静かに壊れる**型。とくに指摘 4 は、**噛み合わない見出し形が同じファイルに既に実在する**点で仮定ではない。

### 低(将来対応)

- **低-1**: `check-manual-images.sh:39` の `명시적` → `明示的`。
- **低-2**: 完了報告 §3 の「新規 3 ファイル」→ 4 件(指摘 8)。
- **低-3**: `victim_line` → `victim_hits`(指摘 6)。
- **低-4**: `PRERELEASE` の裸展開を配列化(指摘 7)。あわせて devContainer へ `shellcheck` を入れる価値がある——本サブは 714 行の新規シェルを足したが、静的検査の経路が無い。
- **低-5**: 全数テストを報告へ貼るときは `-count=1` を付けること。**私の実行は `ok` 60 件のうち 58 件が `(cached)` であった。** `DES-002` §11.3 契約 1 は「キャッシュが効くと、実行していないのに緑になる。所要が 3 桁違い、終了コードはどちらも 0」と明記している。**完了報告 §5.1 の値がキャッシュ込みかどうかは読み取れない。**
- **低-6**: 門を `pr-checks.yml` にも掛ける案(リリース時ではなく PR 時に図のリンク切れへ気付ける)。**`make release-archives` を塞がない**という §3-2 の制約には触れない。採否は設計卓の面。あわせて `allowed_signers` の各行へ `namespaces="git"` を付ける案(鍵の用途を git 署名へ限定できる)。

---

## 良かった点

1. **門の対照が「効く」ことに全振りしている。** 陽性 6 件のうち 3 件(参照 0 件＝空集合の罠 / 単引用符での抽出漏れ / 2 本目の説明書だけ壊れている)は、**チェックリストが問う前に自分で潰しにいった対照**である。とくに「参照を 1 件も読めなかったら赤にする」は、`D-890` が指摘した「検査は全緑なのに中身は壊れていた」の再発を構造で止めている。
2. **破壊確認を実ツリーで行い、戻したことまで `git` 差分 0 で示している。** 合成フィクスチャの対照と実物での再現を**両方**置いた。片方だけで済ませていないのが良い。
3. **置き場の判断を、コードの中に理由ごと残している。** 「`check-release-archive.sh` へ足すと `build-release-archives.sh` が必ず呼ぶため `make release-archives` が塞がる」——次に同じ判断をする人が、判断をやり直さずに済む。射程 7 側も同じ形で、**「そのスクリプトが自ら宣言していた限界」と「足した検査」を結び付けて説明している。**
4. **閉じた穴の隣にある、閉じていない穴を自分から申告している。** `check-dist-readme.sh` の限界へ「実行ファイル名(2 列目)はまだ機械で結ばれていない。同じ型の写しである」と書き、**射程を勝手に広げずに停止時記録の原稿へ回した。** 後任が「この検査が見ているから大丈夫」と誤読するのを防ぐ形であり、他のスクリプトへも広げる価値がある。
5. **外部仕様の裏取りが深い。** README の permissions ブロックと実運用が食い違うことに気付き、**アクション自身の CI ワークフローまで見に行った**。私が独立に引き直しても同じ結論になった。**§0.2 が求めた水準を満たしている**(高-1 の 1 点を除く)。
6. **鍵の未登録が「明示的に落ちる」ことに 5 段 ＋ 多重防御を積んでいる。** 「台帳が在るか」で止めず「**コメントと空行しか無い状態**」まで数え、`${RUNNER_TEMP}/tag-verified` の哨兵で「将来 `if:` を緩めても署名なしでは公開されない」形にした。**さらに完了報告 §7.4 で、開発者へ「鍵を登録する前に打って赤を見てほしい」と否定側の実測まで依頼している。**
7. **境界を構造で守っている。** nightly とリリースを**別ファイル・別契機**にし、`CHANGE-129` の逐語をヘッダへ引いた。差分でも nightly / pr-checks は 0 行、`release-targets.sh` も 0 行。
8. **`E-225` の型を自分で点検している。** 完了報告 §3 が「新規ファイルは deletions 0、唯一の `-4` は限界の節の意図的な書き替え」と明示。**私も `git diff --numstat` で追認した**(新規 4 ファイルすべて deletions 0)。
9. **射程 8 を機械的に実行せず、前提が変わっていることを確かめて止めた。** 死んだ設定を作らない判断であり、**「この判断は開発者が覆せる」と明記して停止時記録へ回した**点まで含めて正しい。`followup-backlog.md` を 1 文字も触っていない(`D-838`)。
10. **利用者に対して誠実である。** リリース本文の最後に「❌ **コード署名(Authenticode)は付いていません** ⇒ SmartScreen の警告が出ます。上の 2 つで出どころを確認してください」を置いた。**`B1` を諦めた事実を隠さず、代わりの手段へ導いている**——指示書 §0.1 が求めた「代替である」という位置づけを、設計書の中だけでなく利用者の目に触れる場所で守っている。

---

## 制約事項

- 本レビューはコード上で判定可能な範囲のみ対象。実際の動作確認・パフォーマンス・実機テストは別途実施が必要。
- **`.github/workflows/release.yml` は一度も実行されていない。** GitHub 上の実行履歴を確認しようとしたが、対象リポジトリ(`plexiblinp/combomgr`)は非公開で HTTP 404 のため到達できなかった。**⇒ 構文・論理はレビューしたが、実行時の挙動(権限・アクションの入出力・`gh release create` の成否)は未検証である。**
- **不明: `anchore/sbom-action` 採用の開発者裁定を、レビュー側から検証できない。** 完了報告 §2.3 は「Plan Mode・実装前に諮り、採る裁定を得た」と記録しているが、Plan Mode のやり取りはリポジトリに残らない。**記録された以上、これ以上は追えない。**
- **不明: 完了報告 §5.1 の `pnpm test`(234 ファイル / 2974 テスト)と `make e2e`(364 passed / flaky 0)は、本レビューでは回していない。** 本サブの差分は Go / TypeScript のプロダクトコードに 1 行も触れていないため費用対効果で省いた。**`go test ./...` のみ独自に回し `EXIT=0` / `ok` 60 件 / `FAIL` 0 件で完了報告と一致することを確認した**(ただし 58 件が `(cached)`。低-5)。
- 本レビューで独自に実行し EXIT=0 を確認したもの: `check-artifact-integrity.sh`(違反なし・検査 18 件) ／ `check-manual-images.sh` 本体・`--self-test` ／ **実物の複製に対する門の破壊確認** ／ `check-dist-readme.sh` 本体・`--self-test` ／ `generate-release-notes.sh --self-test`・本文生成 ／ `check-release-archive.sh` 本体・`--self-test` ／ `check-doc-refs.sh` ／ `check-md-emphasis.sh`(CLAUDE.md ・完了報告 ・本報告書。いずれも検出 0 行) ／ `check-progress-log-index.sh` ／ `check-stop-discipline.sh` ／ `tar -tzf` ・`unzip -l` による配布 3 本の中身の実測 ／ `release.yml` を含む 3 本の YAML パース ／ 一次情報 5 件の独立実査。

---

*以上、M36-02 レビュー報告書。*

---

## 取り込み結果（自動トリアージ）

| 項目 | 値 |
|---|---|
| 取り込み実施日 | 2026-09-19 |
| 取り込みコミット | `b43d446` |
| 指摘件数 | **高 4 ／ 中 5 ／ 低 6**（計 15） |
| **★「高」指摘の不採用** | **0 件** ⇒ **開発者へのエスカレーションは発生していない**（製造 CLI Phase C の安全弁） |
| 再レビュー往復 | **0 回**（上限 2 回） |

**★採否と理由の正本は完了報告 `docs/progress/M36-02-completion-report.md` §10.1 の表である**（全 15 件・1 件ずつ理由つき）。以下は要約。

| 採否 | 件数 | 内訳 |
|---|---|---|
| **採用・是正済み** | **10** | 高-1（出典の格付け）／ 高-2（自己検査が回らない）／ 中-1（dry-run の射程）／ 中-2（SBOM の母集団と検証）／ 中-4（アーカイブ内にも門）／ 中-5（終端と抽出母集団）／ 低-2 ／ 低-3 ／ 低-4 ／ 低-5 |
| **採用（開発者の手番）** | **1** | 高-3（実走）。⇒ 完了報告 §8 原稿 1 に必須 5 フィールドで記録 |
| **採用（Phase D で実施）** | **1** | 高-4（progress-log 索引行 ＋ 設計伝達レポート） |
| **対応済み** | **1** | 低-1（コミット前に修正済み。実測で残存 0 件） |
| **不採用（申し送り）** | **3** | 中-3（射程外＝指示書は「1 列目」と明示。⇒ §8 原稿 4）／ 低-4b（`shellcheck` 導入＝開発環境の変更）／ 低-6（レビュアー自身が「採否は設計卓の面」と明記。⇒ §8 原稿 5） |

**★不採用 3 件はいずれも「中」「低」であり、指摘の妥当性は認めたうえで射程・宛先を理由に見送った。⇒ すべて停止時記録の原稿または本節へ根拠つきで残してある（握り潰していない）。**

**★取り込み後の実測**: `go test -count=1` EXIT=0 ／ FAIL 0 件 ／ ok 60 件 ／ **`(cached)` 0 件**。検査 8 本すべて EXIT=0（`check-artifact-integrity.sh` は **19 件**へ増加）。`make release-archives` EXIT=0。`nightly` / `pr-checks` は依然 0 行。

_（本節は製造担当が Phase C で追記した。レビュー本文はレビュー担当の記述であり、変更していない。）_
