# M19-PHASE2 追補 レビュー報告書

| 項目 | 内容 |
|---|---|
| レビュー対象 | `0af9cf0..HEAD`（3 コミット `589f9eb` / `7a9fa3e` / `b12e739`）のうち **DB マイグレーションが関わる部分に限定** |
| 対象成果物 | `migrations/000066_*` ／ `migrations/000067_*` ／ golden 再生成（`000055` / `000056`）／ `character_data/jamie.csv` ／ `internal/infra/migration/migrate_m19p2b_test.go` ／ 対応する報告記述（設計伝達レポート §1-4 / §1-5 / §2-4 / §3-2 / §4、完了報告 §13） |
| 対象外 | `docs/seed-data/check-criteria.md`（開発者指示） |
| 参照した先行資料 | `CLAUDE.md` ／ `SUPP-001` §5.5 ／ `docs/process/m18-m19-contract.md` §2 ／ `M19-PHASE2-instruction-v1.0.0.md` ／ `migrations/000049` `000061` `000063` `000064` `000065` ／ `migrate_m1904c_test.go` `migrate_m19p2_test.go` `canary_punish_scan_test.go` |
| 実施日 | 2026-08-08 |
| 判定 | **受理可**（マージ前修正必須の指摘はゼロ。指摘は報告記述の失効・掃き出し漏れ・テストの薄い箇所） |

---

## 総評

**マイグレーション本体（`000066` / `000067`）の実装は正しい。** 三段階 swap の 4 経路（新規 DB・既存 DB・二重適用・down→re-up）をレビュー側で**実際にマイグレを走らせて独立検証**し、いずれも意図どおり着地することを確認した。とりわけ **既存 DB 経路（旧 golden から再構成。開発者の dev DB が実際にこの状態＝v65・旧 code であることを read-only で確認済み）と新規 DB 経路が、`moves` 全 1653 行 × 16 列＋alias、`move_derivations` 88 行のレベルで完全に同一の最終状態へ着地する。**

**`000064` との相互作用（新規 DB で入れ替わった相手の行に `startup_basis` が入る＝エラーにならない静かな破壊）を製造側が自力で発見し、`000066` 末尾で是正した判断は本追補の核心である。** レビュー側でも「末尾の是正が無ければ新規 DB が壊れる」ことを実測で再現した。取り残しも無い（飲酒版 8 行は元から正しく、swap の対象外）。`000065`（フレーム是正 21 行）が jamie 流酔拳の code を一切参照していないことも確認済みで、フレーム列への波及は無い。

**契約 F-1 / F-2 / F-3 / F-6 は守られている。** `internal/service/` と `internal/seedgen/` の diff は 0。フレーム 7 列は v65→v67 で 1 バイトも動かない（全数射影の突合）。確定反撃の走査射影は **v65 = v66 = v67 で 29655 行が完全一致**（`go test -overlay` でリポジトリを汚さずに版固定測定を自作して再現）。設計伝達レポート §2-4 の主張「旧 code を機械変換すると完全一致＝物理的な候補集合は不変」も**独自に再測定して裏を取った**（不一致 0 行）。

**一方、報告成果物には失効した数字が複数残る。** とくに設計伝達レポート **§6 が「消費した連番は `000064` / `000065` の 2 本」「次に払い出すべき連番は `000066`」のまま**であり、ヘッダ表だけが「次は `000068`」に更新されている。設計卓が §6 を読んで採番すると連番衝突を起こす。また §3-4 に**本コミットで新規追加された「差し替えの文案」が `35 行` のまま**で、`000067` の +4 行（→ 39 行）を織り込んでいない。加えて、今回是正した「接尾辞と `name_ja` の反転」と**同型の行が既存データに 6 件残っており**、その掃き出しが行われていない。

---

## 設計準拠性レビュー結果

### 1. `000066` の三段階 swap の正しさ — ◎

`t.TempDir()` 相当の使い捨て DB に対し、**リポジトリ非改変**で 4 経路を実走させた（検証手段は §制約事項）。

| 観点 | 検証方法 | 結果 |
|---|---|---|
| **(a) 新規 DB で 0 行に当たるか** | 新 golden で v65 まで構築 → `000066.up` 適用 | **改名 3 段の 12 文すべて 0 行**。`tmp_swap_%` 残骸 0。末尾の `startup_basis` 是正 8 行のみが動く（`through`↔`standalone` が正位置へ） |
| **(b) 既存 DB で正しく入れ替わるか** | 旧 golden（`git show 0af9cf0:` で復元）で v65 まで構築 → `000066.up` → `000067.up` | **8 行の code が正しく入れ替わり**、`name_ja` との対応が一致。`tmp_swap_%` 残骸 0、FK 違反 0 |
| **(a)(b) の合流** | 両経路の `moves` 全数射影（`character_code, code, category, startup, active, recovery, total, on_hit, on_block, damage, is_aerial, is_projectile, is_derived, startup_basis, chain_cancel_total, fastest_unreachable, alias_text`）1653 行と `move_derivations` 88 行を突合 | **byte 単位で完全一致**。再 up 後も一致 |
| **(c) 途中で落ちたとき一時 code が残らないか** | `golang-migrate` の `database/sqlite` ドライバ実装を確認（`sqlite.go:196-215`） | **構造的に残らない。** `Config{}`（`NoTxWrap = false`）なので 1 ファイルが `tx.Exec` 1 発で実行され、失敗時は `tx.Rollback()`。**部分適用が起きえない。** ★この根拠がマイグレのコメントにもテストにも書かれていない点だけ指摘（後述 L-7） |
| **(d) down が正しく逆順か** | v67 → down 67 → down 66 → 状態確認 → re-up | **旧 code（反転状態）へ正確に戻る。** ガードの向きも正しく反転している。さらに **down を 64 まで進めても `000064.down` が期待する前状態（`freeflow_strikes_*` = standalone / `freeflow_strikes_1hit_*` = through）を正しく復元**し、新規 DB 経路・既存 DB 経路の双方で down 64 直後の件数が一致（unknown 326 / through 273 / standalone 1054）。**down 連鎖が壊れていない** |
| **ガードの実効性** | `alias_text LIKE '%(単発)'`（`official_ja_move` 限定）が両経路で意図どおり働くか | **働く。** 新規 DB では step(1) の `EXISTS` が偽・step(2) の `NOT EXISTS` が偽で双方 0 行。既存 DB では双方 1 行。**さらに `000066.up` を 2 回適用しても状態不変・エラー無し**（冪等）を実測 |

**ガードの選択（フレーム値ではなく `name_ja`）は妥当。** `preset_aliases` はランタイムに書き込む経路が存在しない（`internal/` 全体で `INSERT/UPDATE/DELETE preset_aliases` は `seedgen` の生成 SQL のみ）ため、ガードは実質的に不変データに乗っている。`000063` が使った構造ガード（`NOT EXISTS` で改名先の空きを見る）は**双方向 swap では原理的に使えない**という up ヘッダの説明も正しい。

### 2. `000064` との相互作用 — ◎

| 観点 | 結果 |
|---|---|
| 新規 DB で `000064` が相手の行に当たること | **再現した。** v65 の実測で `freeflow_strikes_1hit_*`（単発）に `through`、`freeflow_strikes_*`（フル）に `standalone` が入っている。**エラーは出ない**。`000066` 末尾が無ければ静かに壊れたまま出荷される |
| 既存 DB 経路での冪等性 | **無害。** 既存 DB では `000064` が同じ意味の行に正しく当たっているため、末尾の 8 文は**同値の再書き込み**になる。実測で状態不変 |
| 取り残しの有無 | **無い。** 対象は非飲酒版 8 行のみで、飲酒版 8 行（`drink_level_4_freeflow_strikes_*`）は元から正しく、`000064` も正しい行に当たっている（CSV・DB の双方で確認） |
| `000065`（フレーム 21 行是正）への波及 | **無い。** `000065` が触る 21 code を全列挙して確認したところ、jamie 流酔拳の 8 code は **1 つも含まれない**。フレーム列のクロスコンタミは起きない |
| `000057`（move_commands）への波及 | **無い。** 流酔拳系は `is_derived = 1` のため `000057` に元から非搭載（grep ヒット 0）。golden 再生成が `000057` に及ばないのは正しい |
| `000062`（機械 backfill 追随）への波及 | **無い。** `000062` は `is_derived` / `category` の規則ベースで code を参照しない |
| ほかのテーブルが code をテキストで持っていないか | **持っていない。** 全テーブル・全列を `freeflow_strikes_light` / `freeflow_strikes_1hit_light` で機械走査してヒット 0（`combo_steps.move_id` は id 参照、`combos.recipe_cache` は表示文字列、`moves.original_move_id` は id）。**改名がユーザーデータを壊す経路は無い** |

### 3. golden 再生成の差分 — ◎

| ファイル | 変更行 | 内容 |
|---|---|---|
| `000055_*.up.sql` | 16 行 | moves の `UNION ALL SELECT` 8 行＋`preset_aliases` の 8 行。**code 文字列のみが入れ替わり、フレーム・damage・`notes_tool` は 1 文字も動いていない** |
| `000055_*.down.sql` | 2 行 | `code IN (...)` の並び替えのみ。**code 集合は機械照合で完全一致**（並びのみ変化） |
| `000056_*.up/.down.sql` | 各 1 行 | 同上。**集合一致を機械照合で確認** |

- ほかのキャラ・ほかの列への波及は**ゼロ**。
- `internal/seedgen/` の diff は **0**。
- `go test ./internal/seedgen/` **green**（`000055` / `000056` / **`000057`** の 3 golden を含む）。`go run ./cmd/seedgen -check` **OK（golden 10 stem）**。
- CSV 側も 8 行の `move_code` 入替のみ（`name_ja` / フレーム / `command` / `startup_basis` は各行に付いたまま移動しており、行内の整合が保たれている）。

### 4. `000067` の 3 件が指示書・裁定どおりか — ○（内容は正・コメントに 1 箇所の誤り）

| 項目 | 検証 | 結果 |
|---|---|---|
| **A** `fastest_unreachable` の極性 | `000049` DDL コメント「単独で最速入力しても地上の相手に当てられない技（スカラー S の target にできない）」＝ `1` | **正しい。** 4 行に `1` を投入。既存値保護 `AND fastest_unreachable = 0` あり。総数 144 → **148**（実測一致） |
| A の対象 4 行 | `is_aerial = 0`（B 型一覧に載らない）であることを実測 | **一致。** ヘッダの説明（D-222 と同型の抽出漏れ）は事実 |
| A のコメント「非 OD 2 行は `through` なので D-227 の除外述語に掛からない」 | 実測で D-227 述語（`startup_basis IN ('standalone','unknown')` かつ親参照あり）を満たす 16 行を全数列挙 | **正しい。** `devil_reverse` / `head_press` は `through` で非該当（＝`fastest_unreachable` が唯一のガード）、`devil_reverse_od` / `head_press_od` は `unknown` ＋親参照ありで該当 |
| **B** kimberly `elbow_drop` | `standalone` → `through`。既存値保護 `AND startup_basis = 'standalone'` あり | **裁定どおり。** ただし「既定値ではなく既存値の上書き」である点は契約との関係を報告に書くべき（後述 M-5） |
| **C** 親子 16 対 | 親＝同強度・同酔いレベルの `_1hit_`（単発・`standalone`）であることを 16 対すべて実測 | **すべて正しい。** 子＝`_2hits_` とフル、親＝`_1hit_`。飲酒版の親は swap 対象外の `drink_level_4_..._1hit_*` で正しい |
| C の投入形 | `INSERT ... SELECT` の自己結合。down は子・親の両方を明示列挙 | **`000061` の流儀に忠実**（表を空にしない・スコープを子×親で限定） |
| C のヘッダコメント | 「(16 対 / 子 8 行)」 | **誤り。** 実測で **子 16 行 / 親 8 行**（jamie 全体では 20 対 / 子 20 / 親 10。うち 4 対は `000064` 由来）。後述 L-1 |

### 5. 契約 F-1 / F-2 / F-3 / F-6 — ◎

| 契約 | 検証方法 | 結果 |
|---|---|---|
| **F-1**（確定反撃の走査列） | `is_projectile` / `is_aerial` / `on_block` / `recovery` を v65 / v66 / v67 で実測。さらに `punishfinder` の全キャラ総当たり射影を**版固定で自作測定** | **不変。** proj 135 / aerial 123 / on_block<0 838 / recovery非NULL 1473 の 3 版一致。射影は **v65 = v66 = v67 で 29655 行が完全一致** |
| **F-2**（`startup` / `total` の既存値） | 全 1653 行 × フレーム 7 列（`startup, active, recovery, total, on_hit, on_block, damage`）の射影を v65 と v67 で diff | **完全一致（0 行差分）。1 行も変わっていない** |
| **F-3**（新列の非露出） | `internal/service/` `internal/repository/` `internal/api/` `internal/model/` で 4 名称を grep | **消費コードは存在しない。** ヒットは `internal/repository/move/rush.go` の INSERT 側（書き込みのみ・M19-04 既存）のみ |
| **F-6**（`combos` の INSERT 列） | `internal/service/` の diff | **diff 0。触れていない** |
| 補足 | `internal/service/` / `internal/seedgen/` の diff | **どちらも 0**（`git diff 0af9cf0..HEAD --stat` で確認） |

### 6. `migrate_m19p2b_test.go` の妥当性 — ○

| `SUPP-001` §5.5 の規約 | 適合 |
|---|---|
| **(1) 比較区間を自サブに閉じる** | **適合。** `m19p2bBase = 65` / `m19p2bTerminus = 67`。`m.Up()` は**不使用**（`m.Migrate(version)` のみ） |
| **(2) 名前空間の分離** | **適合。** `TestRun_M19P2b_*` / `m19p2b*` の専用名前空間。HEAD スコープの主張を混ぜていない |
| **(3) 投入行と非投入行を対で固定** | **概ね適合。** 投入 4 行＋総数 148、親子 16 対＋総数 88、`startup_basis` の 3 値分割 1125/342/186、D-227 除外述語 16。ただし down 側と v65 側の固定が薄い（後述 L-4） |
| **(4) 1 行 1 UPDATE・provenance** | **適合。** A/B は `c.code` と `code` を明示、C も 1 対 1 文。一次源コメントあり |
| **(5) down は「up が付与した値」を条件にする** | **適合。** `000067.down` は `= 1` / `= 'through'` を条件にしており、up の保護条件を反転させていない |
| down → re-up の往復 | **あり**（`TestRun_M19P2b_DownUpRoundTrip`）。改名の実効経路を実際に動かしている |
| v65 の「壊れた中間状態」の明示固定 | **良い判断。** `TestRun_M19P2b_BeforeState` が `startup_basis` の反転そのものを固定しており、`000066` が何を直したかが後から読める |

**テストの期待値はレビュー側の独立実測とすべて一致した**（88 / 148 / 1125 / 342 / 186 / 16 / 37 / 16 / 135）。

### 7. 報告の主張が事実か — ○（主張の実質は正・数字に失効あり）

| 主張 | 検証 | 結果 |
|---|---|---|
| 「canary は v65 = v66 = v67 で完全一致」 | 版固定で `punishfinder` 射影を自作測定（`go test -overlay`） | **事実。** 3 版とも 29655 行で完全一致 |
| 「旧 code を機械変換すると完全一致＝物理的な候補集合は不変」 | 旧 golden ツリー v65 を再構成して射影を取り、8 code を完全一致置換で機械変換して突合（位置依存・多重集合の両方） | **事実。不一致 0 行。** 主張の実質は裏が取れた |
| 「**29656 行**が完全一致」 | 同上 | **数字が 1 ずれる。** レビュー側の再測定は一貫して **29655 行**（`canary_punish_scan_test.go` と同一の出力形式）。実質に影響は無いが、報告値としては要訂正（L-2） |
| 「有利フレームが変化した節点 244・始動技が入れ替わった節点 1451」 | 位置揃えの素朴 diff を分類して再測定 | **再現できず＝未検証。** レビュー側の実測は「code が違う 258 行 / 始動技のみ違う 1207 行 / 有利フレームのみ違う 0 行（計 1465 行）」。**比較方法（キー結合か位置揃えか、比較相手が v65 か v67 か）が報告に書かれていないため第三者が追試できない** |
| 「新規 DB / 既存 DB の双方で同一の最終状態・一時 code 残骸 0」 | 両経路を独自に再構成して突合 | **事実**（§1 のとおり） |
| 「ユーザーデータ（combos 41 / steps 171 / setups 7 / punishes 6）不変」 | dev DB を read-only で参照 | **件数一致を確認。** dev DB は **v65・旧 code のまま**であり、実 DB へは適用されていない（＝「コピーに適用」という記述と整合）。**コピーへの適用そのものは追試していない**が、旧 golden から再構成した等価な既存 DB 経路で同一の着地を確認した |

---

## 設計準拠性以外の指摘事項

### (1) 同型欠陥の掃き出しが行われていない

設計伝達レポート §1-4 は「`DES-004` / seed 規約に『接尾辞と `name_ja` の対応を系統間で一致させる』を明文化してほしい」「検出観点は `check-criteria.md` §3 に追加済み」と書くが、**その観点を既存データへ初回適用した結果がどこにも無い**。レビュー側で全 CSV を機械走査したところ、**同型の不一致が 6 行残っている**。

| # | ファイル | `move_code` | `name_ja` | 所見 |
|---|---|---|---|---|
| 1 | `jamie.csv` | `intoxicated_assault_1hit` | `[酔いレベル3]酩酊襲(2発止め)` | **今回是正したものと同型の直撃。** code は `_1hit`、名前は「2発止め」。対の `drink_level_3_intoxicated_assault`（total 60）に対し本行は total 44 ＝実体は 2 発止め。**接尾辞が誤っている疑いが濃い**（あわせて `drink_level_3_` 接頭辞も欠けている） |
| 2 | `jamie.csv` | `phantom_sway_2hits` | `幻酔舞` | `_2hits` が「非飲酒版」を指しており、対の `phantom_sway` は `幻酔舞(飲酒)`。接尾辞の意味が hit 数ではない |
| 3 | `jamie.csv` | `full_moon_kick_2hits` | `円月脚` | 同上（対は `円月脚(飲酒)`） |
| 4 | `lily.csv` | `rush_desert_storm_1hits` | `デザートストーム(ラッシュ)` | 非ラッシュの `desert_storm_1hits` は `デザートストーム(単発)`。**ラッシュ版だけ `(単発)` が名前から落ちている** |
| 5-6 | `marisa.csv` | `novacula` / `rush_novacula` | `ノバキュラ(単発)` / `ノバキュラ(単発)(ラッシュ)` | 名前に `(単発)` があるが code に `_1hit` が無い（フル版が存在しないため実害は薄い） |

**#1 は「反転」ではなく「片側だけ誤り」なので今回の swap 型の是正では拾えず、放置すると `move_derivations` の親決定で同じ罠を踏む。**

### (2) CSV 正本と DB の乖離が 5 セル増えたことが未記録

`000067` が入れた値は CSV に反映されていない。

| CSV | 行 | 列 | CSV の値 | DB（v67） |
|---|---|---|---|---|
| `kimberly.csv` | `elbow_drop` | `startup_basis` | 空欄 | `through` |
| `m_bison.csv` | `devil_reverse` | `fastest_unreachable` | 空欄 | `1` |
| `m_bison.csv` | `devil_reverse_od` | `fastest_unreachable` | 空欄 | `1` |
| `m_bison.csv` | `head_press` | `fastest_unreachable` | 空欄 | `1` |
| `m_bison.csv` | `head_press_od` | `fastest_unreachable` | 空欄 | `1` |

設計伝達レポート §4 の 9 番が「CSV 正本と DB の乖離」を将来の整理事項として挙げているが、**本追補が乖離を 5 セル増やしたことは書かれていない。** なお `internal/seedgen` は `startup_basis` / `fastest_unreachable` を **SQL 非投入（保全のみ・`model.go:79-81`）** なので、**CSV を直しても golden は 1 バイトも動かない**（＝安価に解消できる。ただし CSV 変更は開発者の diff 承認待ちが要る運用）。

### (3) 設計伝達レポート §6 の連番案内が失効

§6 本文は **「消費したマイグレ連番は `000064` / `000065` の 2 本」「次に払い出すべき連番は `000066`」** のまま。ヘッダのメタ表だけが「`000066`・`000067`（追補）／次は `000068`」に更新されている。**§6 は「CHANGE 起票のたたき台（設計担当向けチェックリスト）」であり、設計卓が直接読んで採番する節**なので、放置すると `000066` を二重に払い出す。

### (4) §3-4 の「差し替えの文案」が投入直後に失効

本コミットで新規追加された `M19-DESIGN-07` 向けの差し替え文案が **「Phase 2 の実投入は 35 行（B 型一覧 8 ＋ 13 系統 27）」** のまま。`000067` A の 4 行を加えると **39 行**（`fastest_unreachable = 1` の総数も 144 → 148。実測確認済み）。**この文案は設計正本へ貼られる前提の文字列**なので、失効したまま貼られると同じ「資料が実装に追随していない」問題を再生産する。

### (5) 設計伝達レポート §4 の他の数字も v65 のまま

§4 の 9 番「DB は `standalone` 1126 / `through` 341」は v67 では **1125 / 342**。完了報告 §13 には「§2〜§9 の数値は①②（v65）時点のもの」という総括注記があるが、**設計伝達レポート側には同等の注記が無い**（§4 の 2 番だけが「追補でも 186 のまま」と個別に追記されている）。

### (6) 契約 F-4 (2') (c) との関係が §2 に書かれていない

`m18-m19-contract.md` §2 F-4 (2') (c) は「`WHERE <当該列> IS NULL` で既存値を保護する」を求める。本追補は 2 箇所でこれを外れる。

- `000066` 末尾の `startup_basis` 8 文：**ガード無しの無条件 UPDATE**（既存値の上書き）。
- `000067` B：`AND startup_basis = 'standalone'` ＝**既定値（`'unknown'`）ではなく、`000050` の機械規則が付けた既存値を意図的に上書き**。

**どちらも「是正」なので実行として正当**であり、up ヘッダには理由が書かれている。しかし設計伝達レポート **§2（契約・設計に反する独自判断）に 1 行も無い。** ②が F-2 の解除を §2 に明記した前例に照らすと、ここも §2 に置くのが家風に合う。

### (7) 設計伝達レポート §4 の番号列の乱れ

追補で `10` / `11` を **`2` と `3` の間**に挿入したため、番号列が `1, 2, 10, 11, 3, 4, 5, 6, 9, 7, 8, 9` になった（**`9` の重複は追補以前からの既存問題**）。読み手が「10 番」を探すと表の中ほどに現れる。

---

## 推奨修正（優先度別）

### 高（マージ前に修正必須）

**なし。** マイグレーション・golden・CSV・テストのいずれにも、データを壊す／契約を破る欠陥は見つからなかった。

### 中

| # | 指摘 | 対応案 |
|---|---|---|
| **M-1** | 設計伝達レポート §6 の「消費した連番は 2 本」「次は `000066`」が失効（§(3)） | §6 本文を「`000064` / `000065` / `000066` / `000067` の 4 本。次は `000068`」へ更新。**設計卓が直接採番に使う節なので優先度が高い** |
| **M-2** | §3-4 の差し替え文案が「35 行」のまま（§(4)） | 「Phase 2 の実投入は **39 行**（B 型一覧 8 ＋ 13 系統 27 ＋ 追補 `000067` の m_bison 4）」へ。総数 148 も併記 |
| **M-3** | 同型欠陥の掃き出し未実施（§(1)） | 少なくとも `jamie / intoxicated_assault_1hit` を**開発者へ確認事項として起票**する（`_1hit` か `_2hits` か）。残り 5 件は「観点の初回適用結果」として設計伝達レポート §4 に一覧で申し送る。**是正自体は本追補の範囲外でよいが、「見つかっているのに書かれていない」状態は避ける** |
| **M-4** | CSV↔DB 乖離が 5 セル増えたことが未記録（§(2)） | 設計伝達レポート §4 の 9 番に「追補で 5 セル増（内訳は表）」を追記。`seedgen` が両列を SQL 非投入である事実も併記すると、後続が「直すと golden が動く」と誤解しない |
| **M-5** | F-4 (2') (c) との関係が §2 に無い（§(6)） | §2 に「`000066` 末尾の無条件 UPDATE と `000067` B の既存値上書きは F-4 (2') (c) の形を外れる。理由＝機械規則が付けた誤値の是正であり、保護すべき人手値ではない」を 1 段落 |
| **M-6** | §4 の 9 番「1126 / 341」が v65 のまま（§(5)） | 「（v65 時点。追補後は 1125 / 342）」を添えるか、完了報告 §13 と同型の総括注記を §0 付近に置く |

### 低（将来対応）

| # | 指摘 | 対応案 |
|---|---|---|
| **L-1** | `000067.up` のヘッダ「C: jamie 流酔拳の親参照 (16 対 / **子 8 行**)」が誤り | **「16 対 / 子 16 行 / 親 8 行」** へ。実測で確認済み |
| **L-2** | 報告の「29656 行」が実測と 1 ずれる | **29655 行**へ訂正（設計伝達レポート §2-4、完了報告 §13、`progress-log`） |
| **L-3** | 「有利フレーム 244 / 始動技 1451」が追試不能 | 数字の横に**測り方**（比較相手の版・キー結合か位置揃えか）を 1 行添える。`SUPP-001` §5.5 の「説明の射程を明示する」（§7 教訓 4）と同じ趣旨 |
| **L-4** | テストの down 側と v65 側の固定が薄い | (a) down 後に `kimberly/elbow_drop` が `standalone` へ戻ることを固定 (b) `m19p2bExclusionSet = 16` を **v65 でも**測る（「増えない」の主張には両端が要る） (c) 飲酒版 8 code の `code`↔`name_ja` が**不変であること**を対で固定（swap のスコープ外れを検出できる） (d) `TestRun_M19P2b_RenameAndBasis`（v66）でも `startup_basis` の 3 値分割を固定すると、末尾 8 文の過剰適用を検出できる |
| **L-5** | 設計伝達レポート §4 の番号列の乱れ・`9` の重複（§(7)） | 追補分を末尾へ移すか、通し番号を振り直す |
| **L-6** | `000067.up` C の `pa.code IN ('単一要素')` | `=` で足りる（`000061` は複数要素だったので `IN` が適切だった）。可読性のみ |
| **L-7** | swap の原子性の根拠がコメントに無い | 「途中で落ちても一時 code は残らない」は `golang-migrate` の `database/sqlite` が `NoTxWrap = false` で 1 ファイル 1 トランザクションであることに依存する。**この前提が変わると壊れる**ので、up ヘッダに 1 行残すと後続が安全に読める。あわせて「ガードが `preset_aliases`（ランタイム書き込み経路なし）に乗っている」ことも明記すると、ガードの安全性の根拠が自足する |
| **L-8** | `_1hit_` = 基底 という今回の裁定が全キャラ規約ではない | marisa `tonitrus_1hit`（単発）は `through` ＋親参照ありで、`tonitrus`（無印）が `standalone`。**jamie の裁定を全キャラへ一般化してはいけない**ことを、`DES-004` へ明文化する際の注記として残すとよい |

---

## 良かった点

1. **`000064` との相互作用を自力で発見し、末尾で是正した。** 三点更新（CSV → golden → 追随マイグレ）を採ると、**過去の backfill マイグレが「入れ替わった相手の行」に当たる**。これはエラーにならず、新規 DB だけが静かに壊れる。レビュー側で末尾の 8 文を外した状態を再現したところ、確かに反転したまま v67 へ着地した。**この 8 文が本追補で最も価値のある部分である。**
2. **ガードの選び方が良い。** 「フレーム値は再計測で動きうるが `name_ja` は技の同一性を指す」という判断は正しく、しかも `preset_aliases` は実行時に書き込む経路が存在しないため、実質的に不変データに乗っている。`000063` の構造ガードが双方向 swap では使えない理由も正確に説明されている。
3. **v65 の「壊れた中間状態」を意図的にテストで固定した。** `TestRun_M19P2b_BeforeState` が無ければ、`000066` が何を直したのかは半年後に読めない。`SUPP-001` §5.5 (3) の精神をよく汲んでいる。
4. **down が `000064.down` の期待する前状態を正しく復元する。** ここが崩れると down 連鎖全体が壊れるが、実測で両経路とも一致した。設計時に意識されていたことが `down` のガード反転コメントから読み取れる。
5. **golden 再生成の差分が最小。** 8 code の入替以外に 1 文字も動いていない（`000055.down` / `000056` は集合一致の並び替えのみ）。`internal/seedgen/` diff 0・`seedgen -check` OK。
6. **報告が「測り方の失敗」まで自己申告している。** 設計伝達レポート §2-4 の「基準ファイルが三点更新で陳腐化した」という自戒は、次に同じ形を踏む人を確実に助ける。
7. **`000067` C が `000061` の流儀（表を空にしない・子×親でスコープ限定）に忠実。**

---

## 制約事項

1. **コードは一切変更していない。** 本ファイル以外の新規作成・編集は無し。`git status` は clean。Git の書き込み操作は行っていない。
2. **検証手段**——(a) `/tmp/migchk/go`（使い捨て Go モジュール）で `modernc.org/sqlite` を直接叩き、`migrations/*.sql` を版指定で適用。(b) 版固定の `punishfinder` 射影測定は **`go test -overlay`** でテストファイルを `/tmp` から注入（**リポジトリのファイルは 1 バイトも触っていない**）。(c) 旧ツリーの golden は `git show 0af9cf0:` で `/tmp` へ復元。**検証用一時ファイルはすべて `/tmp/migchk/` 配下に置き、レビュー完了後に削除した。**
3. **開発者の dev DB へは書き込んでいない。** `mode=ro&immutable=1` で読み取ったのみ（version 65 / dirty 0 / 旧 code のまま＝実 DB は未適用であることを確認）。
4. **`docs/seed-data/check-criteria.md` はレビュー対象外**（開発者指示）。したがって「追加した検出観点そのものの妥当性」は評価していない。§(1) は**その観点を既存データへ当てた結果が報告に無い**という点の指摘である。
5. **未検証**——設計伝達レポート §2-4 の「有利フレームが変化した節点 **244** ／ 始動技が入れ替わった節点 **1451**」は、比較方法が報告に書かれていないため再現できなかった。レビュー側の位置揃え比較では 258 / 1207 / 0（計 1465）。**どちらが正しいかは判断できない**（測り方が違えば両方成立しうる）。
6. **未検証**——完了報告 §13 の「dev DB のコピーに `000066` / `000067` を実適用」という手順そのものは追試していない。旧 golden から再構成した等価な既存 DB 経路で同一の着地を確認することで代替した。
7. `go test ./...` **全パッケージ green**、`go vet ./...` 出力なし、`gofmt -l internal/ cmd/` 出力なし、`go run ./cmd/seedgen -check` **OK** をレビュー側でも実行して確認済み。

---

*以上*

---

## 取り込み結果（自動トリアージ）

**高（マージ前修正必須）はゼロ。中 6 件・低 8 件をすべて採用**（不採用ゼロ）。

| 指摘 | 優先度 | 採否 | 対応 |
|---|---|---|---|
| **M-1** 設計伝達レポート §6 の連番が失効 | 中 | **採用** | 「`000064`/`000065`（本体）＋`000066`/`000067`（追補）の 4 本。**次は `000068`**」へ更新。設計卓が直接採番に使う節なので最優先で直した |
| **M-2** §3-4 の差し替え文案が「35 行」のまま | 中 | **採用** | 「**39 行**（B 型一覧 8 ＋ 13 系統 27 ＋ 追補 4）／`fastest_unreachable = 1` の総数 **148**」へ。設計正本に貼られる文字列が投入直後に失効する、という指摘は的確 |
| **M-3** 同型欠陥の掃き出し未実施（6 行残存） | 中 | **採用（申し送り。是正はしない）** | 設計伝達レポート §4 に **12 番**を新設し 6 行を全数列挙。特に jamie `intoxicated_assault_1hit`（`[酔いレベル3]酩酊襲(2発止め)`）は**今回是正したものと同型の直撃**で、「反転」ではなく「片側だけ誤り」のため swap 型の是正では拾えない旨も明記。`_1hit` か `_2hits` かの裁定を依頼。**是正は本追補の範囲外**（一次源＝開発者判断が要る） |
| **M-4** CSV↔DB 乖離が 5 セル増えたことが未記録 | 中 | **採用** | §4 の 9 番へ内訳（kimberly `elbow_drop` の `startup_basis` ／ m_bison 4 行の `fastest_unreachable`）と、**`seedgen` が両列を SQL 非投入なので CSV を直しても golden は動かない**ことを併記 |
| **M-5** F-4 (2') (c) との関係が §2 に無い | 中 | **採用** | §2-0b を新設。`000066` 末尾の無条件 UPDATE（**保護すると目的を達成できない**）と `000067` B の既存値指定上書き（**保護ではなく機械付与値からの遷移の明示**）を表で説明 |
| **M-6** §4 の 9 番が v65 の数値のまま | 中 | **採用** | 「1125 / 342（追補後。v65 時点は 1126 / 341）」へ |
| **L-1** `000067.up` ヘッダ「子 8 行」が誤り | 低 | **採用** | 「16 対 / 子 16 行 / 親 8 行」へ |
| **L-2** 「29656 行」が実測と 1 ずれる | 低 | **採用** | **29655** へ訂正（設計伝達レポート §2-4 ／ 完了報告 §13 ／ progress-log）。原因は末尾改行を 1 行として数えていたこと |
| **L-3** 「244 / 1451」が追試不能 | 低 | **採用** | 測り方（**キー結合**での比較）を併記し、**レビューの位置揃え比較では 258 / 1207 と出る**ことも書いた。数え方で数字が変わる旨を明示 |
| **L-4** テストの down 側・v65 側の固定が薄い | 低 | **採用（4 項目すべて）** | (a) down 後に `kimberly/elbow_drop` が `standalone` へ戻ることを固定 (b) `m19p2bExclusionSet = 16` を **v65 でも**測る（「増えない」の主張に両端を与える）(c) **飲酒版 8 code の `code`↔`name_ja`↔`basis` が不変**であることを v65 / v66 / down 後で固定（swap のスコープ外れは件数では検出できない）(d) v66 で `startup_basis` の 3 値分割を固定（末尾 8 文の過剰適用を検出） |
| **L-5** §4 の番号の乱れ | 低 | **採用** | 追補分を 12・13 として末尾へ追加し、既存番号は据え置き（参照が張られているため） |
| **L-6** 単一要素の `IN` | 低 | **採用** | `000067.up` の 16 文を `pa.code = '...'` へ |
| **L-7** swap の原子性の根拠がコメントに無い | 低 | **採用** | `000066.up` のヘッダに「`golang-migrate` の `database/sqlite` が `NoTxWrap = false` で 1 ファイル 1 トランザクション。★この設定が変わると残骸が次回の UNIQUE 違反を招く」と、ガードが乗る `preset_aliases` に**ランタイム書き込み経路が無い**ことを追記 |
| **L-8** 「`_1hit_` ＝ 基底」は全キャラ規約ではない | 低 | **採用** | §4 に **13 番**を新設。marisa は `tonitrus_1hit`（単発）が `through`、`tonitrus`（無印）が `standalone` で **jamie とは逆**。`DES-004` へ明文化する際は「系統内で一貫させる」に留め、接尾辞の意味を全キャラで固定しないよう注記 |

**取り込み後の再検証**: `go test ./...` 全パッケージ green ／ `go run ./cmd/seedgen -check` OK ／ `go vet`・`gofmt -l internal/ cmd/` 出力なし ／ `internal/service/`・`internal/seedgen/` の diff 0。**既存 DB 経路（dev DB のコピーに `000066`/`000067` を実適用）を修正後にもう一度通し**、`move_derivations` 88 ／ `fastest_unreachable=1` 148 ／ `standalone` 1125 ／ `through` 342 ／ 一時 code 残骸 0 ／ jamie 単発の code が `freeflow_strikes_1hit_light` であることを確認した。
