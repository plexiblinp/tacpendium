# M31-03 完了報告: 一覧の列の並び ＋ 詳細への行内リンクの位置

| 項目 | 内容 |
|------|------|
| 作業ID | M31-03 |
| 指示書 | `docs/instructions/M31-03-combo-list-columns-and-row-link.md` **v1.1.0** |
| チェックリスト | `docs/instructions/reviews/M31-03-review-checklist.md` **v1.0.0** |
| 着手基点 | `4c0dcb2` |
| 状態 | **★完了**（段 1〜4 ／ Phase B レビュー ／ Phase C 取り込み ／ 開発者の実機確認と裁定まで。2026-09-09） |
| CHANGE 消費 | **0 本**（★請求のみ。設計卓が起票する＝`D-293`） |
| マイグレ消費 | **0 本** |

> **本報告は段階的に書いた**（`D-510`）。**実装から書ける欄を Phase A の直後に埋め、レビュー結果を参照する §7 には `Phase C で埋める` のプレースホルダを置いて、Phase C の後に実測で置き換えた。⇒ 断定を先に書いていない。**

> **コミットは 2 件に分かれている**（指示書 §6-9）: `cad14c9`（是正＝行内リンク）／ `da2dd45`（決めごと＝列の並び）。

---

## 1. 段 1 — 実査（★コードを書く前に実施し、開発者へ報告した）

### 1-1. 現在の列の並び（表示列カスタマイズ対象は **9 列**）

| # | key | 見出し(ja) | 既定 |
|---|---|---|---|
| 1 | `hitType` | ヒット種別 | ON |
| 2 | `position` | 始動位置 | ON |
| 3 | `opponentStance` | 相手の状態 | ON |
| 4 | `damage` | ダメージ | ON |
| 5 | `recipe` | ルート | ON |
| 6 | `tags` | タグ | ON |
| 7 | `draftStatus` | 登録状態 | ON |
| 8 | `memo` | 備考 | **OFF** |
| 9 | `setupCount` | セットプレイ数 | **OFF** |

カスタマイズ対象外の列＝選択チェックボックス（選択モード時）／展開アイコン／専用画面の 3 列／マイコンボのステータス列／操作列。

> **★★並びの正本は 1 か所ではない**（チェックリスト §6-4「列の定義が 1 か所とは限らない」の実物）**。**
> `COLUMN_DEFINITIONS`（`web/src/constants/combo-list.ts`）が持つのは **表示列メニューの並びと `colSpan` の計数だけ**であり、**表の並びは `ComboTable.tsx` の見出し JSX と `ComboTableRow.tsx` のセル JSX に別々にハードコードされている。**
> **⇒ 3 か所を同時に直さないと見出しとセルがずれるが、`tsc` もテストも検出しない。**

### 1-2. 行の部品を使っている画面 = **3 面**

| 面 | DES-005 | 列設定の渡し方 |
|---|---|---|
| `/combos` コンボ一覧 | §5.4 | 利用者の設定（`useColumnVisibility`） |
| `/mycombo` マイコンボ | §5.5 | **同じキーを共有** ＋ ステータス列 |
| `/game-update/combos` 影響コンボ専用 | **§5.19b** | **`DEFAULT_COLUMN_VISIBILITY` 固定**（利用者設定を読まない）＋ 3 列 |

母集団の取り方＝`ComboTable` の import 元を全数走査した（`grep -rn "components/ComboTable" web/src`）。**「トグルを置いた面」ではなく「共通部品を使っている面」で数えている**（`M24-03` の作法）。

### 1-3. リンクがいつ移ったか → **★見つかった**

**commit `b1e89f9`**「`feat(M27-03/combo-list): 始動状況を 3 列へ割る`」（2026-09-05）。

- **着手前**＝`visibility.starterSituation` の **1 列**（testid `combo-starter-situation`）がリンクを持ち、中身は `formatSituationStatus` ＝ **ヒット種別 / 始動位置** の連結。
- **その手番で** 3 列へ分割し、コミット本文と行内コメントが「**詳細への導線は先頭の 1 セルだけに置く**（3 セルすべてをリンクにすると同じ行き先が 3 本並ぶ）」と明記している。

> **★記録は在る。ただし記録が述べているのは「1 セルだけにする」であって「ヒット種別に置く」ではない。** ヒット種別が受け取ったのは、**それが分割後の先頭に並んだから**にすぎない。**⇒ 意図した移動ではなく、置き場が並び順に従属した副作用である。**
>
> **★★裏付け＝testid が `combo-starter-situation` のまま残っていた。** 「始動状況」という列は `M27-03` 以降 **存在しない**。**旧名がそのまま残っていたこと自体が、誰も置き場を選び直していない証拠である。**

### 1-4. `combo-list-columns-v1` に何が保存されるか → **★指示書 §0.2 の前提が実装事実と食い違う**

保存値は **`ColumnVisibility` そのもの＝boolean 9 個の JSON オブジェクトだけ**であり、**並びの情報を一切持たない**（利用者が列を並べ替える手段も無い）。

> **⇒ 既定の並びを変えても、保存済みの列設定には当たらない。**
> 指示書 §0.2 の 3 案〔(a) 保存済みを尊重 ／ (b) 上書き ／ (c) キーをバージョンアップ〕は、**どれを選んでも結果が同じ**である——**並びは全員に即座に届き、ON/OFF の設定は無傷のまま残る。**
> **★★(c) はむしろ有害である**——**並びと無関係な ON/OFF 設定だけを捨てることになる。**
> **⇒ キーのバージョンアップは行わない。`web/CLAUDE.md` §1 台帳への新規キー追記も不要。**（設計卓の推しは (c) だったが、実装事実がその前提を否定した。開発者へ報告済み。）

**付随して見つかった失効記述**（開発者承認のうえ是正済み・§2 参照）:

- `web/CLAUDE.md` §1 脚注 2 か所 ／ `useRecipeFullView.ts` ／ `useFilterPanelCollapsed.ts` が `ColumnVisibility` を「**7 個の boolean**」と書いていた。`M27-03` で 7 → 9 になっており失効。
- testid `combo-starter-situation` が、存在しない列の名を指していた。

**★残渣を 1 件観測した（本サブでは触っていない）**: `useColumnVisibility` の `{ ...DEFAULT, ...loaded }` マージのため、**旧キー `starterSituation` が保存値に残っている環境がある**。`colSpan` の水増しは `M27-03` が対策済み（`COLUMN_DEFINITIONS` から数える形）であり、実害は現時点で無い。

### 1-5. 列ごとの幅・折り返しの扱い

列幅の明示指定は **無い**。`damage` / `setupCount` が `tabular-nums`（後者は右寄せ）、`recipe` と `memo` が `max-w-xs`（320px）＋ `truncate`。表全体は `overflow-x-auto`。

> **⇒ 並べ替えても列幅は動かない。** `truncate`（`white-space:nowrap` + `overflow:hidden`）が効いて列幅が値で伸びないためであり、`M24-03` の実測（「崩れの原因はレシピ長ではなく列数 × 各列の最小幅」）と一致する。

---

## 2. 段 2 — 行内リンクの位置を戻した（**コミット 1**: `cad14c9`）

**★開発者の回答（2026-09-09）＝リンクの置き場は「ルート列」／見た目は「リンク色（青）」。**

指示書 §2.2-1 は「始動状況の列へ戻す」と書いているが、**「始動状況」列は `M27-03` で 3 列へ割れており、そのままの戻し先が存在しない**。§2.2-2 に従って**止めて報告し**、開発者が **ルート列**を選んだ。

| # | やったこと |
|---|---|
| 1 | ヒット種別セルから `<Link>` を外し、素のラベルへ戻した |
| 2 | ルートセルの **`RecipeText` だけ**を `<Link to={/combos/:id}>` で包んだ |
| 3 | **★置き場を `DETAIL_LINK_COLUMN`（`constants/combo-list.ts`）で宣言した** |
| 4 | testid を `combo-starter-situation` → `combo-hit-type` へ改名し、状況 3 列＋ルート＋セットプレイ数を `COLUMN_TEST_IDS` 表経由へ寄せた |
| 5 | 失効記述 4 か所を是正した（§1-4） |

> **★★(3) が本コミットの核である。** 今回の事故は「置き場が並び順に従属していた」ことで起きた。**定数で宣言し、テストで固定すれば、並べ替えでリンクが黙って動く経路そのものが無くなる。**
>
> **★`MemoFirstLine` はリンクの外に置いた。** 同部品は `role="note"` を持つ**メモ由来の別情報**であり、`<a>` の中へ入れると支援技術がレシピの一部として読み上げる（`M24-03` §4.4 の読み分けが崩れる）。
>
> **★リンクは 1 列だけである。** 操作列の「詳細」は列設定に関わらず従来どおり在り、**ルート列を OFF にしても導線は失われない。**

### 2-1. ★★`DES-005` §5.4 との食い違い（**CHANGE 請求へ回す。全 5 件を名指しする**）

**★★着手時に申告できていたのは 1 件だけだった**（レビュー 高-1 / 高-2 で不足を指摘され、実物を読んで数え直した）。**⇒ 設計卓は請求の本文を読んで §5.4 を畳むため、名指しが無ければ畳み残しが出る。**

| # | §5.4 の箇所（`docs/design/05-screen-design.md`） | 現在の記述 | なぜ成り立たないか |
|---|---|---|---|
| **1** | 「アクション」欄 | **「ルートのホバー/タップ → 全文表示」** | **ルート列をリンクにするとタップは詳細への遷移である。**（★着手時に申告できていた唯一の件） |
| **2** | 「レスポンシブ」欄（スマホ） | **「ルートはタップで全文モーダル」** | **#1 とまったく同型である。⇒ 片方だけ直すともう片方が残る** |
| **3** | 「各コンボの表示項目（横並び）」の列挙 | 展開アイコン → 状況 3 列 → ダメージ → **ルート** → タグ → 登録状態 → 備考 | **この列挙が列の並びの契約そのものである。案C は ルート → ダメージ → 状況 3 列 → …** |
| **4** | 「表示項目の『既定』」 | **「既定で出るのは 始動状況 ／ ダメージ ／ ルート ／ タグ ／ 登録状態」** | **旧順のまま。★加えて「始動状況」という列は `M27-03` 以降 存在しない**（3 列へ割れた） |
| **5** | 「セットプレイ展開時の表示例」の ASCII 図 | **`[始動状況/ダメージ/ルート/タグ/備考]`**（2 行） | **同上。旧順 ＋ 存在しない列名** |

**★★別件でもう 1 件ある（実装が §5.4 より進んでいる側）。**

| # | 事実 | §5.4 の状態 |
|---|---|---|
| **6** | **コンボ一覧にも「ステータス」列が出る。** `web/src/pages/ComboListPage.tsx` が `onStatusChange` を渡し、`ComboTable` が列を 1 本生やす | **§5.4 の表示項目に**ステータス列**の記述が無い**（同列は §5.5 マイコンボ側の概念として書かれている） |

> **★#6 は `M31-03` が作った差ではない。** しかし**本サブは §5.4 の列の記述へ CHANGE を請求する唯一の手番**であり、ここで一緒に出さないと、次に §5.4 を読む担当は「一覧の列は 7 本 ＋ 操作」と読んで数え直す。**★実際に本サブの製造がその取り違えを踏んでいる**（§5-5 の注記）。
>
> **★★#1〜#6 はいずれも「実装が変わったのに記述が旧のまま」の型である。⇒ 動作は正しいままなのでテストも `tsc` も緑になり、人が読む以外に見つける経路が無い。**

**★開発者の後発の指示が上位であるため実装は回答どおり進めたが、黙って越えていない**（`D-782` の一般形）。**⇒ 設計伝達レポート §4 の CHANGE 請求へ上表をそのまま載せる。★番号は自採番しない**（`D-293`）。

---

## 3. 段 3 — 列の並びの案（**提示 → 開発者が案C を選んだ**）

**★開発者の指示＝「言葉ではイメージが難しいのでモックを作ってください」。** ⇒ 実装の見た目をそのまま写した**モックを作って提示した**。

- **モック**: Artifact `https://claude.ai/code/artifact/2150be14-d7e4-4bb3-bf7f-611c1cce0e05`
- **★現状と並べて示している**（指示書 §2.3-2）。**同じデータで 5 つを縦に並べ、幅（1280 / 768 / 390）・既定 OFF の列・面（一覧 / マイコンボ / 専用画面）を切り替えられる。**

| パネル | 並び | 何を優先したか |
|---|---|---|
| 着手前 | ヒット種別 / 始動位置 / 相手の状態 / ダメージ / ルート / タグ / 登録状態 | （リンクはヒット種別。是正の対象） |
| **基準** | 同上 | **並びはそのまま、リンクだけルートへ。★3 案はこれと比べる**（変数が並びだけになる） |
| **案A** | ヒット種別 / 始動位置 / 相手の状態 / **ルート / ダメージ** / タグ / 登録状態 | 状況で当てて、その場でルートを読む。**現状との差分は 1 か所** |
| **案B** | **ルート** / ヒット種別 / 始動位置 / 相手の状態 / ダメージ / タグ / 登録状態 | 行の識別を左端へ。リンクの左端が揃う |
| **案C** | **ルート / ダメージ** / ヒット種別 / 始動位置 / 相手の状態 / タグ / 登録状態 | 「何を・どれだけ」を先に、「どんな条件で」を後ろへ |

**★★開発者の回答（2026-09-09）＝案C。**

**★`combo-list-columns-v1` の扱いの案**（指示書 §6-4）＝ **§1-4 のとおり「何もしない」が正解である。** 3 案のどれを選んでも結果が同じであり、キーのバージョンアップは有害である。

> **★★モックを作って初めて見えたこと**: **ルート列を青にすると、一覧で最も幅のある列が全行青くなる**（案B・案C では左端）。**⇒ 見た目の影響が事前の想像より大きい。** **★併せて開発者へ確認し、「現行のまま」＝青を維持する回答を得た**（2026-09-09）。
>
> **★モックを作ったこと自体が段 3 の要求であった。** **開発者の逐語＝「言葉ではイメージが難しいのでモックを作ってください」。⇒ 案を文章で並べるだけでは選べない、という指摘である。**

---

## 4. 段 4 — 並びを入れた（**コミット 2**: `da2dd45`）

**★開発者の回答を得てから着手した**（指示書 §2.3-4 / §3-1 / §6-5）。

**案C の並び** ＝ ルート / ダメージ / ヒット種別 / 始動位置 / 相手の状態 / タグ / 登録状態 / 備考 / セットプレイ数。

**★★§1-1 の 3 か所を同時に直した**（`COLUMN_DEFINITIONS` ／ `ComboTable` の見出し JSX ／ `ComboTableRow` のセル JSX）。**転記ミスを避けるため機械的に組み替えている。**

| # | 併せて起きたこと |
|---|---|
| 1 | **`ColumnVisibilityMenu.test.tsx` が赤くなった**——先頭の列を `"hitType"` と**ベタ書き**していたためである。**⇒ ベタ書きをやめ `COLUMN_DEFINITIONS[0].key` から引く形へ直した。★`M27-03` のときも同じ理由で `"starterSituation"` から書き換えられている。⇒ 次の並べ替えでは赤くならない** |
| 2 | **並べ替えで宙に浮いた `M27-03` のコメントを両ファイルで書き直した**（並びの説明が別の列の頭に残る状態になっていた） |
| 3 | **失効を 1 件是正した**——「3 列化は **2 面**に及ぶ」→ **3 面**。`M28-02c` が影響コンボの専用画面を足して以降 失効していた（段 1-2 の実査で判明） |

**★列は足していない・減らしていない**（指示書 §3-2）。**`ColumnVisibility` の型・キー名・`DEFAULT_COLUMN_VISIBILITY` の値・保存キーは一切変えていない**（§1-4 より、並びは保存値と無関係）。

**★`DES-005` §5.4 の列の記述と食い違う。⇒ CHANGE を請求する**（指示書 §2.4-3。**★自採番しない**＝`D-293`）。

---

## 5. テスト・検査の実測

### 5-1. 走査（**★「片方を消した」ではなく「もう片方が残っていない」ことを示す**＝指示書 §5-2）

`ComboTableRow.tsx` の `` to={`/combos/${combo.id}`} `` は **2 件のみ**:

| 行 | 場所 |
|---|---|
| 154 | **ルート列**（＝`DETAIL_LINK_COLUMN`） |
| 290 | 操作列の「詳細」（列設定に関わらず常に在る） |

**⇒ 状況 3 列からは 0 件である。**（**★行番号は段 4 の並べ替え後に取り直した値である**——並べ替え前は 172 / 278 だった。）

**★同じことを 2 経路で押さえている**——**走査**（上記）と、**`ComboTableRow.test.tsx` が行の `td` を全数走査する単体テスト**（§5-2）、および **E2E が状況 3 列のリンクを 0 件で確かめる**（§5-5 の test 1）。**⇒ 「片方を消した」だけの報告にならないようにしている**（チェックリスト A-4）。

### 5-2. 単体テスト（新規 `ComboTableRow.test.tsx` 4 本）

**行の `td` を全数走査**して、行内リンクが `DETAIL_LINK_COLUMN` の列にだけ在ることを固定する（テスト 1）。ほかに「ヒット種別の欄はリンクを持たない」「メモの 1 行目はリンクの外」「ルート列 OFF で行内リンクが消え、操作列の詳細は残る」。

> **★★陽性対照を実測した**（チェックリスト A-2 / 指示書 §5-1）。**着手前の形（リンクをヒット種別へ戻す）へ一時的に patch して実行し、4 本中 3 本が落ちることを確認した**（残る 1 本は `MemoFirstLine` の位置を見るテストで、着手前も真であるため落ちない）。**patch は同じ手番で復旧し、復旧を照合した。**

### 5-3. 検査の出力

| 検査 | 結果 |
|---|---|
| `cd web && pnpm test` | **2,628 件 green / 223 ファイル** |
| `pnpm exec tsc --noEmit -p tsconfig.json` | **exit 0** |
| **`make e2e`（全数）** | **282 passed (4.5m)**。**★共通部品を触るため全数で回した** |
| `make e2e-only P=m31-03` | **5 passed**（新規 spec。§5-5） |
| `make e2e-only P=m27-03` | **8 passed**。★うち test 7 は**ルート列のリンクを実際に踏む**導線である |
| `bash scripts/check-artifact-integrity.sh` | **違反なし**（★1 本目に回した。検査 15 件の自己検査 ＋ 生成物 4 件） |
| `bash scripts/check-browser-storage-keys.sh` | **違反なし**（台帳 11 件 / 本番 10 件・一致） |
| `bash scripts/check-import-order.sh` | **違反なし**（現在 100 / ベースライン 101） |
| `bash scripts/check-enum-sync.sh` | **ベースラインどおり（増加なし）** |
| `bash scripts/check-doc-refs.sh` | **dead reference なし** |

> **★上表は「ファイルを足し終えたあと」に回し直したものである**（チェックリスト E-1。**先例＝`M30-01` が `check-import-order` を「違反なし」と報告したが、回したのは新規テストを作る前だった**）。
>
> **★★`check-import-order` の「BASELINE を 100 へ下げること」に従っていない。**
> **⇒ ベースライン固定型の検査を、走行中のレーンで下げない**（`D-388` ／ `md-emphasis-baseline-per-lane-drift` ／ ボード `D-787` (1)）**。レーンごとに測って下げると、マージした瞬間にずれる。**
> **★`M30-01` が同じ 100 / 101 で正しく踏みとどまった先例がある。⇒ 同じ数であり、本サブが減らしたものではない。**

### 5-4. 変更統計（`git diff --stat 4c0dcb2`・コミット 1 時点）

```
 web/CLAUDE.md                                      |   4 +-
 web/e2e/m27-03-list-and-detail.spec.ts             |  10 +-
 web/src/constants/combo-list.ts                    |  25 ++++
 .../combo/components/ComboTableRow.test.tsx        | 134 +++++++++++++++++++++
 .../features/combo/components/ComboTableRow.tsx    |  55 ++++++---
 .../combo/hooks/useFilterPanelCollapsed.ts         |   2 +-
 web/src/features/combo/hooks/useRecipeFullView.ts  |   2 +-
 7 files changed, 207 insertions(+), 25 deletions(-)
```

> **★新規ファイル `ComboTableRow.test.tsx` は `+134` で deletions が 0 である**（教訓 `E-225`）**。⇒ 同名の既存ファイルを上書きしていない。** 作る前に `ls` で同名の不在を確かめてもいる。

### 5-5. ★★破壊確認（新規 spec `web/e2e/m31-03-column-order-and-row-link.spec.ts`・**5 本すべて green**）

**★区分の割り方**＝**「`ComboTable` を import している面」で数えた 3 面から 1 件ずつ**（指示書 §4.3 ／ `SUPP-001` §5.5.4 (10)）。**★`§5.19b` の専用画面を含めている**——同画面は**列設定の扱いが違う**（利用者の設定を読まず `DEFAULT_COLUMN_VISIBILITY` を渡す＝`CHANGE-164` §3）**。⇒ 区分が別である。**

| # | 何を見たか | 結果 |
|---|---|---|
| 1 | **区分 1/3** コンボ一覧＝並びが案C ／ 行内リンクがルート列に 1 本 ／ **状況 3 列に 0 本** ／ 踏むと詳細へ行く | green |
| 2 | **区分 2/3** マイコンボ＝同じ並び（共通部品を共有している） | green |
| 3 | **区分 3/3** 影響コンボ専用画面＝同じ並び ＋ 専用の 3 列が後ろに続く | green |
| **4** | **【破壊確認・本命】保存済みの列設定を持つ環境** | green |
| 5 | **キーボードの順送り**でルート列のリンクへ到達し、Enter で詳細へ行ける | green |

> **★★test 4 が本サブで唯一重かった面である。** **`localStorage` を実際に汚してから開いている**——**「新規の利用者では正しい」だけでは示せない**（チェックリスト B-2 / §6-2）。**★旧キー `starterSituation` を混ぜた保存値で確かめた**（`M27-03` がキーを上げなかったため、実環境にはこの形が残っている）。**★実測＝ON/OFF は保存値どおり**（利用者が消したタグ列は出ず、出した備考列は出る）**、並びだけが新しい既定になる。保存値そのものも壊れていない。**
>
> **★test 5 は `focus()` を直接呼んでいない**——**それでは「順送りの停止点になっているか」を確かめたことにならない。⇒ `Tab` を押して到達するまで辿る形にした。**
>
> **★実測で 1 件、母集団の取り違えを自分で踏んだ**（`M-145` の一般形）。**コンボ一覧にも「ステータス」列が在る**——同画面も `onStatusChange` を渡しており、**マイコンボ専用の列ではなかった。⇒ 最初の期待値が誤っていた。** 表示列カスタマイズの対象は 9 列であり、**ステータス列と操作列はその外側である。**

---

## 6. ■ 併せて更新が要るもの

| # | 対象 | 状態 |
|---|---|---|
| 1 | **CHANGE 番号の登録**（`change-number-registry.md` §1 ほか 4 か所） | **★該当なし**——**本サブは番号を消費していない。請求のみであり、起票は設計卓である**（`D-293`） |
| 2 | **マイグレ連番**（ボード §2.2） | **★該当なし**（消費 0 本） |
| 3 | **版を上げた文書の参照元** | **★該当なし**（指示書・チェックリストの版を上げていない） |
| 4 | **`web/CLAUDE.md` §1 台帳** | **★キーの増減なし**（§1-4）。**脚注 2 か所の失効記述のみ是正した** |
| 5 | `docs/progress/progress-log.md` の索引行 | **★Phase D で追記する**（未追記） |

---

## 7. レビュー（Phase B / C の結果）

| 欄 | 値 |
|---|---|
| レビュー報告書 | `docs/progress/m31-03-review.md`（**fresh subagent**。`fork` は使っていない） |
| 指摘の件数・優先度別内訳 | **高 2 ／ 中 5 ／ 低 5**（計 12 件） |
| 各指摘の採否と理由 | **全 12 件採用 ／ 不採用 0 件**。内訳は同報告書末尾の「取り込み結果（自動トリアージ）」 |
| **「高」指摘の不採用が 0 件か** | **0 件である**（実測。⇒ 開発者へのエスカレーションは発生していない） |
| 再レビュー往復の回数 | **0 回**（上限 2 回・`CLAUDE.md` §9） |

> **★採用の前に、指摘そのものを実物で検証した。** 高-1 が挙げる `DES-005` §5.4 の 3 か所と、高-2 の「§5.4 にステータス列の記述が無い」は、いずれも `docs/design/05-screen-design.md` を実際に読んで確かめている。
>
> **★「高」2 件はどちらも実装の変更を要さなかった**——CHANGE 請求の本文の不足であった。

---

## 7-2. ★★開発者による実機確認（2026-09-09）

**★★本節は製造の実測を 1 件 上書きしている。⇒ 実機が正しい。**

### 7-2-1. リンクとテキスト選択（レビュー 中-4 の裁定材料）

| # | 確認 | 実機の結果（開発者） |
|---|---|---|
| **A-1** | レシピの文字の上でドラッグして離す | **★遷移する**（離した先が文字の上のとき）。**★文字の外へドラッグすると、リンクのドラッグ画像 ＋ 禁止カーソル（〇に斜線）になり、離しても遷移せず消える** |
| **★A-1b** | **文字の外を左クリックして、文字の上へドラッグする** | **★★レシピを選択できる。⇒ 選択手段はゼロではない** |
| **A-2** | ダブルクリック ／ トリプルクリック | **どれだけ速く押しても遷移する**（＝語・行の選択は取れない） |
| **A-3** | 右クリック | **リンクのメニュー**（新しいタブで開く／リンクのアドレスをコピー ほか）。**★「コピー」「切り取り」は出ない** |
| **A-4** | Shift ＋ 矢印 | **選択できない** |
| **A-5** | 「レシピを縦に表示」ON | **同じ挙動。★リンクの当たり判定が複数列分の幅に広がって見える** |
| **A-6** | タッチ（タップ） | **遷移する** |

> **★★製造の事前実測は誤っていた。** 単体 HTML で `<a>` を測ったときは「離しても `click` は発火しない」であったが、**実アプリでは文字の上で離すと遷移する**。**⇒ 単体の再現は実物の代わりにならない。実機の観測を正とする。**
>
> **★★A-1b は事前の実測に無かった事実である。** 「`<a>` の中の文字は一切選択できない」ではなく、**「リンクの外から始めたドラッグなら選択できる」**。**⇒ コピー手段が完全に失われたわけではない。**

**★A-5 の正体を製造が実測した**（Chromium・要素の矩形と `elementFromPoint`）:

| | 見えている文字の幅 | 当たり判定 | 他の列へ及ぶか |
|---|---|---|---|
| 省略表示（既定） | **320px**（`max-w-xs`） | 文字ブロックの幅 | **及ばない** |
| **縦表示（`fullView`）** | **872px ＝ セルほぼ全幅** | **セルほぼ全幅** | **★及ばない**（ダメージ列の中心を叩いてもリンクに当たらない＝実測） |

> **★「複数列分に広がって見える」は正しい観測であり、かつ他の列は侵していない。** 原因は **`RecipeText` が `fullView` のとき `compactClassName`（`max-w-xs`）を適用しない**ことであり、**縦表示ではレシピ列そのものが広い**。**★これは `M24-03` 以来の挙動であり `M31-03` が作った差ではない**——**本サブが変えたのは「その広い帯が押せるようになった」ことである。**

### 7-2-2. 見た目・波及（B / C / D）

| # | 確認 | 結果 |
|---|---|---|
| B-1 | 案C の並びを実データ・幅 1280 / 768 / 390 で見る | **違和感なし** |
| B-2 | 青の分量（ルート列が左端で全行青くなる） | **許容** |
| B-3 | 「表示列」メニューの並びと表の並びの一致 | **一致** |
| B-4 | ルート列を OFF にすると行内リンクが消える | **許容** |
| B-5 | `/mycombo` ／ `/game-update/combos` でも同じ並び | **同じ並びだった** |
| **C** | **自環境の `combo-list-columns-v1`** | **★旧キー `starterSituation` は無かった**（⇒ 残渣は「`M27-03` 以前に列を触った環境だけ」であり、開発者の環境には無い） |
| D | レシピが空のコンボでの読み上げ | **該当データが無いため未確認**（★`M24-13` 以降は仮登録でもレシピ 1 ステップが必須のため、新規には発生しない） |

### 7-2-3. ★★裁定

**開発者の裁定（2026-09-09）＝ (a) 許容する。**

**⇒ 実装の変更は行わない。** 根拠は次の 3 点である。

1. **選択手段はゼロではない**（A-1b＝リンクの外から始めたドラッグで選択できる）。
2. **導線は失われていない**——操作列の「詳細」は列設定に関わらず常に在る。
3. **回避を入れると「ドラッグしたときは遷移しない」判定が要り**、素の `<a>` の挙動から離れる。

**★`followup-backlog.md` §J の `combo-list-recipe-link-blocks-text-selection` は「完了（許容）」へ更新した。★本文は書き換えず、規律の実績として残す。**
---

## 8. 開発者への確認事項（**すべて決着**）

| # | 事項 | 状態 |
|---|---|---|
| **1** | **列の並びをどれにするか**（基準 / 案A / 案B / 案C） | **★決着＝案C**（2026-09-09） |
| **2** | **ルート列のリンクを青のままにするか** | **★決着＝現行のまま**（＝`text-blue-600 hover:underline`。2026-09-09） |
| 3 | 保存済みの列設定の扱い | **★決着**——§1-4 より「何もしない」。3 案のどれを選んでも結果が同じであり、キーのバージョンアップは有害 |
| 4 | 行内リンクが「意図した移動」だった場合 | **★決着**——§1-3 より副作用と判明し、開発者が置き場（ルート列）を選んだ |

---

*以上、M31-03 完了報告（**完了**・2026-09-09）。* **★開発者の実機確認と裁定（§7-2）まで反映済み。★残る未決は `followup-backlog.md` §J の 1 件**〔旧キー残渣の畳み方＝設計卓の手番〕**のみである。**
