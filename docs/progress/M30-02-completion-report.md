# M30-02 完了報告: 仮想コントローラの技の選び方と見た目（強度・変種 ＋ ボタン名・配置）

| 項目 | 内容 |
|------|------|
| 文書ID | M30-02-completion-report |
| バージョン | **2.0.0**（2026-09-09・**★開発者の実機確認 6 件を反映。マイグレ 1 本を消費した**） ／ 1.2.0 ／ 1.1.0 ／ 1.0.0 ／ 0.1.0 |
| 対象指示書 | `docs/instructions/M30-02-controller-selection-and-labels.md` **v1.0.0** |
| 着手基点 | `4c0dcb2` |
| 作成日 | 2026-09-09 |
| CHANGE 消費 | **0 本**（自採番していない＝`D-293`。要る箇所は §6 に一覧） |
| マイグレ消費 | **1 本（`000107`）**。**★★着手時点の宣言は「0 本」だった。** 開発者のインゲーム確認（2026-09-09）で `P4M-016` (b) の決着条件が揃い、**開発者の明示的な指示で切った。⇒ 自採番ではない**（`D-293`）。詳細は §8.4 |

---

## 1. §2.1 契約の棚卸し（**最初の成果物**）

**★本節は実装より先に作った。** 指示書 §0.1 の一般形＝「その機構の挙動を固定しているテストを、実装前に `grep` で拾う」に従う。

### 1.1 仮想コントローラ配下の `data-testid` 全数（着手時点）

| 面 | 部品 | `data-testid` |
|---|---|---|
| タブ（**8 枚**） | `VirtualController.tsx` | `recipe-tab-normal` ／ `recipe-tab-unique` ／ `recipe-tab-target-combo` ／ `recipe-tab-special` ／ `recipe-tab-super-art` ／ `recipe-tab-unclassified` ／ `recipe-tab-character-state` ／ `recipe-tab-common` |
| 通常技 | `HitBoxLayout.tsx` | `recipe-dir-{1..9}` ／ `recipe-normal-{light\|medium\|heavy}-{punch\|kick}`（6 種） ／ `recipe-rush-toggle` |
| 特殊技 | `DirectSpecPanel.tsx`（既定接頭辞） | `recipe-direct-{code}` ／ `recipe-direct-rush-{code}` ／ `recipe-unique-rush-toggle`（`VirtualController.tsx`） |
| 必殺技 | `SpecialMovePanel.tsx` | `recipe-special-family-{family}` ／ `recipe-special-strength-{light\|medium\|heavy}` ／ `recipe-special-od-plain` ／ `recipe-special-od-{lm\|mh\|lh}` |
| ターゲットコンボ | `DirectSpecPanel.tsx` | `recipe-target-combo-{code}` |
| 未分類 | 同上 | `recipe-unclassified-{code}` |
| キャラ固有状態 | 同上 | `recipe-character-state-{code}` |
| 共通技 | `CommonMovePanel.tsx` | `recipe-common-{code}` ／ `recipe-system-parry-drive-rush` |
| タブ外 | `DeleteRow.tsx` | `recipe-system-delete` |
| 全技一覧（コントローラの外・同じ面） | `RecipeBuilder.tsx` | `recipe-pulldown-toggle` ／ `recipe-category-filter` ／ `recipe-move-select` ／ `recipe-add-step` ／ `recipe-omit-surfaced-toggle` |

### 1.2 数字キーの割当（`M24-12` が定めたもの）

**★★仮想コントローラは数字キーを 1 つも消費していない。**

| 事実 | 実体 |
|---|---|
| 割当の正本 | `web/src/features/combo/optionButtons.ts`。`MAX_BUTTONIZED_OPTIONS = 10` ／ `shortcutKeyForIndex`（`0..8` → `"1".."9"`、`9` → `"0"`） ／ `indexForShortcutKey`（逆写像） |
| 購読しているのは | **`OptionButtonGroup.tsx` だけ**。その群にフォーカスが在る間だけ効く |
| 使用面 | `ComboEditorBasicFields.tsx`（エディタの**基本情報タブ**） |
| 仮想コントローラとの関係 | **同居しない**。仮想コントローラは**レシピタブ**に在る。タブ自体は shadcn/Radix の `Tabs` であり数字キーを購読しない |

**⇒ `SM-080`（ボタン名）／ `SM-149`（ボタン配置）は数字キーの割当に触れない。** 触れうるのは表示ラベルと DOM の並びだけである。**指示書 §2.6-3 が想定した「配置を変えると数字キーの割当がどう動くか」は、動く割当が存在しないため該当が無い。**

### 1.3 ★★上記を固定しているテストの全数（単体・E2E の両方）

| # | 種別 | ファイル | 何を固定しているか |
|---|---|---|---|
| 1 | 単体 | `VirtualController.test.tsx` | タブ 8 枚の**存在と並び順** ／ **`★M24-12 の入力の骨格に触れていないこと`**〔`recipe-tab-normal` へフォーカスして `1`〜`5` を押しても既定タブのままであること〕 ／ 方向・攻撃ボタンを **aria 名**〔`ニュートラル` ／ `弱パンチ` ／ `強パンチ` ／ `中パンチ`〕で選択している |
| 2 | 単体 | `moveSurfacing.test.ts` | `isOnSpecialTab` の述語が `deriveSpecialFamilies` と一致すること ／ 出る側 12 経路 ／ 出ない側を区分ごとに 1 件 |
| 3 | 単体 | `moveSurfacing.roster.test.ts` | **seed 全 31 キャラの実測固定**〔未分類の件数・区分内訳・キャラ別表〕 |
| 4 | 単体 | `inputResolution.test.ts` | `parseSpecialCode` ／ `deriveSpecialFamilies` ／ `OD_VARIANT_FLAG` |
| 5 | 単体 | `optionButtons.test.ts` ／ `OptionButtonGroup.test.tsx` ／ `ComboEditorBasicFields.test.tsx` | **数字キーの割当そのもの**（★本サブは 1 行も触らない） |
| 6 | 単体 | `gamepadRecipeInput.test.tsx` | `recipe-dir-{2,5}` ／ `recipe-normal-{heavy-punch,light-kick,light-punch}` ／ `recipe-common-throw_{forward,back}` ／ `recipe-system-delete` ／ `recipe-tab-common` |
| 7 | 単体 | `gamepadShortcut.test.tsx` | `recipe-system-delete` ／ 読取表示 |
| 8 | 単体 | `RecipeBuilder.test.tsx` ／ `SetupRecipeEditor.test.tsx` | `SM-100` のトグル ／ 全技一覧 |
| 9 | E2E | `m15-03-recipe-input.spec.ts` | **`recipe-special-family-hadoken`** ／ **`recipe-special-strength-light`** ／ `recipe-dir-2` ／ `recipe-tab-{normal,special}` |
| 10 | E2E | `m17-03-stage2-input.spec.ts` | `recipe-dir-{2,5,6}` ／ `recipe-tab-{normal,unique,special,super-art}` |
| 11 | E2E | `m30-01-controller-surfacing.spec.ts` | タブ 8 枚 ／ **未分類・共通技・キャラ固有状態の実 DB 件数**〔jamie 11 ／ mai 3 ／ blanka 12 ／ manon 0〕 ／ `recipe-omit-surfaced-toggle` |
| 12 | E2E | `m12-05` ／ `m12-06` ／ `m21-05` ／ `m21-06` ／ `m21-07` ／ `m23-05` ／ `m23-09` ／ `m27-02b` ／ `support/editor-input.ts` | `recipe-tab-normal` ／ `recipe-tab-common` 等（**タブの存在のみ**） |

### 1.4 ★「壊してよい契約か」の結論

| 判断 | 内容 |
|---|---|
| **既存 `data-testid` は 1 つも変えない** | §1.3 の 12 本が名指ししている。**表示名を変えても `data-testid` は据え置くのが既定である**（指示書 §2.6-2） |
| **aria 名も変えない** | **★これは指示書が挙げていない契約である。** `VirtualController.test.tsx` は方向・攻撃ボタンを `getByRole("button", { name: "ニュートラル" })` で選んでおり、**aria を変えると単体テストが落ちる**。`HitBoxLayout.tsx:25` の逐語注記〔「5/2/8 の aria は既存値を維持する（unit テストセレクタ非破壊）」〕とも一致する |
| **主ラベルと DOM の並びは、どのテストも名指ししていない** | ⇒ `SM-080` / `SM-149` が触れる範囲はここに限られる |

---

## 2. 実装

### 2.1 `P4M-016` — 強度なし技の選択

#### (b)「テリーのラウンドウェーブだけは強が押せる」の原因＝**データである**

**★指示書 §2.2-2 は「原因が分かるまで直さない」と定めている。⇒ 原因は特定できた。**

`character_data/terry.csv:27`:

```
terry,round_wave_heavy,special,ラウンドウェイブ,...,d dr r plus p_h,...
```

| # | 実測 |
|---|---|
| 1 | `parseSpecialCode` は末尾 `_heavy` を強度接尾辞として剥がす。**⇒ ファミリー `round_wave` ／ 強度 `heavy` に分解され、ファミリー行に出て「強」だけが活性になる** |
| 2 | **★述語の差ではない。** テリーの `power_wave` は弱・中・OD しか持たない〔236HP が別技のラウンドウェイブであるため強が無い〕／ `burning_knuckle` は中・強・OD しか持たない〔236LP が別技のクイックバーン〕 |
| 3 | **★★`name_ja` は「ラウンドウェイブ」であり強度語を持たない。⇒ 強度の概念が無い技に `_heavy` が付いている** |

**★★同型の先例が 2 件あり、いずれもデータ側を直している。**

| 先例 | 直し方 | 出所 |
|---|---|---|
| `sonic_break_light` → `sonic_break` | マイグレ `000063` 節 A | `M19-04c` |
| `quick_burn_light` → `quick_burn` | マイグレ `000106` | `M28-05`。**開発者の逐語＝「`quick_burn_light` には強度は存在しません…`quick_burn` に直す必要がある」** |

**★★【2026-09-09 解消】開発者のインゲーム確認を得て、本サブで是正した**（マイグレ `000107`。§8.4）。**⇒ ラウンドウェイブは「強度なし通常版」へ合流した。**

**★以下は着手時点の記述である**： 本サブでは直さない。マイグレ連番の自採番は禁止であり（`D-293`）、本サブのヘッダも「マイグレ消費 0 本」である。§6 へデータ是正の請求として出した。

**★E2E に対照を 1 本置いた** — 「ラウンドウェイブは `move_code` が原因でまだ『強』のままである」。**⇒ 直っていないことを記録として残す。**

#### (a)「強度のない技」の母集団と実装

**★★母集団は `moves` テーブルである**（`character_data/*.csv` ではない＝`SUPP-001` §5.5.3.2 ／ `M-145` の取り違え）。**一時テストで `internal/infra/migration` の `newMigrator` から使い捨て DB を作り、`moves` × `characters` × `preset_aliases` を全数（3026 行）落として数えた。**

| 母集団 | 値 |
|---|---:|
| `moves` 総行 | **3026** |
| `category='special'` | **1024** |
| **A1**（強度語がどこにも無い） | **186** |
| **A2**（強度語が `code` の途中に在る） | **105** |
| ファミリー（着手時点の規則） | 273 |

**★`special` の部分集合については CSV と `moves` が完全に一致した**（1024 / 186 / 105）。**差の 283 行はマイグレが直接投入する移動系 system 技等であり、`special` を 1 行も含まない。⇒ `M-145` と同じ取り違えは本サブでは起きていない。**

**実装**（`web/src/features/combo/inputResolution.ts`）

| # | 内容 |
|---|---|
| 1 | `SpecialStrength` に `"none"` を足した。表示順は `none → light → medium → heavy → od` |
| 2 | `parseSpecialCode` を 3 段にした。**(i)** 末尾が強度接尾辞 → 従来どおり ／ **(ii)** 強度語が `code` の途中に在る → **`null` のまま** ／ **(iii)** 強度語がどこにも無い → `{ family: code, strength: "none" }` |
| 3 | **★★強度語の集合は既存の接尾辞表から導いた。書き起こしていない**（2 か所に書くと片方だけ直って静かにずれる） |
| 4 | `SpecialMovePanel` に「**強度なし通常版**」ボタン（`recipe-special-strength-none`）。文言は開発者が決めた（`roles-and-routing` ハード列） |
| 5 | **★弱中強の行は、そのいずれかが在るときだけ描く。** 強度なししか持たないファミリーで、常に押せない 3 つを並べても意味が無い |
| 6 | **★代表名の優先順に `"none"` を `"light"` の次へ置いた。** 強度を持たない技の `name_ja` は強度語を含まないため、最も素直な代表名になる |

**★★(ii) を `null` のままにしたのが `M30-02` と `M30-03` の境界である。** A2 の 105 件は「弱・中・強・OD が揃っている状態版の必殺技」であり、1 技 1 ファミリーではない。**⇒ ここで `"none"` にすると 105 件が「強度の無い別々の技」として並び、ファミリーの意味が壊れる。** `M30-03` §3-1 も「A1 は射程外」と明記しており、境界は両側から一致している。**この境界を機械で固定する主張を 1 本置いた**（`moveSurfacing.roster.test.ts`＝未分類に残る `special` はすべて「強度語が `code` の途中に在る形」である）。

**波及（意図した変化。★テストは「緑にするため」ではなく新しい実測へ書き直した）**

| 観測 | 着手時点 | 本サブ完了時点 |
|---|---:|---:|
| 未分類（CSV 基準・固有状態タブ無し） | 330 | **144** |
| その `special` 内訳 | 291 | **105**（＝A2 のみ） |
| 未分類（**実 DB**）jamie / mai / blanka / manon | 11 / 3 / 12 / 0 | **2 / 1 / 2 / 0** |
| **`_od` だけが押せる形** | **95** | **0** |

**★★「`_od` だけが押せる 95 件」の解消は副次的な帰結である。** 素の版（強度なし）が同じファミリーへ合流するため、「ファミリー名は出るが OD しか押せない」形が消えた。**設計書が名指ししていた `guile/sonic_break` と `terry/quick_burn` もこのうちの 2 件であった**（`M30-01` §2.5）。

**★★【2026-09-09 訂正】逆向きの形を 7 組作った。上の記述は片側しか書いていなかった。**

開発者の実機確認で見つかった（逐語＝「`[電刃錬気]OD波動拳` が必殺技タブのボタンから入力できないがこれは既存バグ?」）。**答え＝既存バグではないが、本サブが作った非対称である。** `denjin_charge_od_hadoken` は A2 であり着手時点から必殺技タブに載っていないが、**素の `denjin_charge_hadoken` は A1 であり本サブで新たに載るようになった。⇒ 「素は押せるが OD が押せない」形になる。**

| キャラ | 素（A1・載る） | OD（A2・載らない） |
|---|---|---|
| ryu | `denjin_charge_hadoken` ／ `denjin_charge_hashogeki` | 各 `_od_` 版 |
| blanka | `lightning_beast_electric_thunder` ／ 同 `_holding` | 各 `_od_` 版 |
| lily | `windclad_condor_dive` | `windclad_od_condor_dive` |
| m_bison | `mine_set_devil_reverse` | `mine_set_od_devil_reverse` |
| mai | `flame_musasabi_no_mai` | `flame_od_musasabi_no_mai` |

**★入力手段は 1 つも消えていない** —— 6 組はキャラ固有状態タブから押せる（`denjin_charge` ／ `lightning_beast` ／ `windclad` ／ `flame`〔`flame_stock` の語幹〕）、`m_bison` の 1 組は未分類タブに残る（状態 code `psycho_mine_is_set` の語幹 `psycho_mine` が `mine_set_*` と一致しないため）。**⇒ 一貫性と見つけやすさの問題であり、機能の欠落ではない。**

**★7 組を `moveSurfacing.roster.test.ts` の主張として固定した。★`M30-03` が A2 を載せれば 0 件になる。⇒ 動くこと自体が検出したい変化である**（開発者判断 2026-09-09＝`M30-03` へ委ねる）。

### 2.2 `P4M-018` — ホールド／ジャスト版（**案 B を採った**）

#### どちらの案で直したか、と理由

**★開発者判断（2026-09-09）＝案 B。** 逐語の追加指示＝「**ジャストや最大ホールドも射程。ただし必殺技以外は対象外。通常技や特殊技など**」。

**★★製造が単独で決めていない**（指示書 §4-6）。**案を 3 つ出した。**

| 案 | 内容 | 採否 |
|---|---|---|
| A | 表示名だけ直す（ファミリー名から強度語を落とす） | 不採用 |
| **B** | **強度の外からホールドを消し、修飾として選ばせる** | **★★採用** |
| C | ホールドファミリーの強度行を出さない | 不採用 |

**★実測で分かったこと** — 逐語の「同じものが 2 か所に見える」の実体は、**ファミリー行に `gladius_holding` が別ファミリーとして並び、その中でまた弱中強が出ていた**ことである。**ファミリー名が「【ホールド】弱グラディウス」のままだったのは、`stripStrengthLabel` が接頭・末尾しか見ず、`【…】` の直後の強度語を落とせなかったためである。**

#### 実測（`moves` テーブル・全 31 キャラ）

**★★件数は 2 つある**（2026-09-09 レビュー 中-4 で是正）。

| 時点 | 変種マーカーで終わるファミリー | 孤児 |
|---|---:|---:|
| **着手時点の規則**（強度接尾辞を持つファミリーだけ） | **19 本** | **0** |
| **`P4M-016` 適用後**（A1 もファミリーになる） | **22 本** | **0** |

**★増えた 3 本は基底が A1 であり、`P4M-016` と合流して初めて畳める** — `blanka/lightning_beast_electric_thunder_holding` ／ `cammy/razors_edge_slicer_holding` ／ `luke/aerial_flash_knuckle_holding`。**⇒ 下表は着手時点の 19 本である。**

| キャラ | 変種ファミリー | 基底 |
|---|---|---|
| akuma | `gou_hadoken_holding` ／ `gou_hadoken_max_holding` | `gou_hadoken` |
| blanka | `electric_thunder_holding` | `electric_thunder` |
| c_viper | `focus_force_holding` ／ `focus_force_max_holding` | `focus_force` |
| cammy | `cannon_spike_holding` ／ `hooligan_combination_holding` ／ `spiral_arrow_holding` | 各基底 |
| dhalsim | `yoga_fire_holding` | `yoga_fire` |
| ed | `psycho_flicker_holding` | `psycho_flicker` |
| ingrid | `sun_shot_holding` | `sun_shot` |
| luke | `flash_knuckle_holding` ／ **`flash_knuckle_perfect`** | `flash_knuckle` |
| mai | `kachousen_holding` | `kachousen` |
| marisa | **`gladius_holding`** ／ `scutum_max_holding` | `gladius` ／ `scutum` |
| rashid | `whirlwind_shot_holding` ／ `whirlwind_shot_max_holding` | `whirlwind_shot` |
| sagat | `tiger_uppercut_holding` | `tiger_uppercut` |

**畳んだ結果、変種を持つファミリーは 18 本になった**（`razors_edge_slicer` / `aerial_flash_knuckle` / `lightning_beast_electric_thunder` を含む。**★これらは A1 側にも同名の変種が在り、`P4M-016` と合流して初めて畳める**）。

#### ★ジャスト版の同型（指示書 §2.3-3 への回答）

**実在した** — `luke/flash_knuckle_perfect`。**★★ただしガイルのジャストは別形である**：`perfect_timing_light_sonic_boom` のように**強度語が `code` の途中に在る A2** であり、着手時点も本サブ完了時点もファミリー UI に載っていない。**⇒ `M30-03` の射程であり、本サブでは扱わない。**

#### ★`move_derivations` との関係（指示書 §2.3-4 への回答）

**関係が無い。** ホールド版は `moves` の独立行であり、`move_derivations` の親子ではない。**⇒ 出し分け（`M30-01` の規則）の話ではなく、ファミリーの束ね方の話である。**

#### 実装

| # | 内容 |
|---|---|
| 1 | `SpecialVariant = "plain" \| "holding" \| "max_holding" \| "perfect"` |
| 2 | **★★照合は長い接尾辞から行う** — `_max_holding` は `_holding` でも終わる。**順序を誤ると「最大ホールド」が「ホールド」として畳まれ、静かに 1 変種消える**（テストで固定した） |
| 3 | **★★畳むのは、剥がした基底が同じキャラの必殺技ファミリーとして実在するときだけ。** 実在しなければ独立ファミリーのまま残す。**⇒ 実測では孤児 0 件だが、データが動いたときに基底の消えた変種を到達不能にしない** |
| 4 | `SpecialFamily` を `byStrength` から `byVariant` ＋ `variants` へ変えた |
| 5 | ファミリー行と強度行の**間**に変種行を挟む（`recipe-special-variant-*`）。**変種が 1 つのファミリーでは行ごと出さない** |
| 6 | **★ファミリーを変えたら変種は通常へ戻す**（押した覚えのない変種で確定させない） |
| 7 | **★代表名は plain から採る** — 変種の `name_ja` は「【ホールド】弱グラディウス」の形であり、ファミリー名としては装飾も強度語も残る |
| 8 | **★★射程は `category='special'` だけ。** 通常技・特殊技の `_holding` 17 件（`M30-01` 区分 D）には 1 行も触っていない |

**★★確定するステップは着手時点とまったく同じである。** ホールド版は従来どおり自分の `move_id` で入り、修飾フラグは足していない。**⇒ レシピ表示・保存・比較は 1 行も変わらない。**

### 2.3 `SD-020` — OD 強度組合せ（**2 面 ＋ 射程拡大**）

#### ★★§2.5 の調査結果（指示書 §2.5・followup `od-variant-active-without-od`）

**開発者の指摘（2026-09-05・`A-6`）＝「OD 非活性で強度組み合わせだけ活性みたいになっているパターンがないかは調査が必要」。**

| 面 | 実在するか |
|---|---|
| **仮想コントローラ側**（`SpecialMovePanel`） | **★実在しない。** 変種 3 つは `{odId != null && …}` の内側に在り、OD 本体が無いファミリーでは描かれない。**⇒ 構造的に起こらない** |
| **修飾フラグ側**（`MODIFIER_FLAGS`） | **★★常に実在していた。** `od_lm` / `od_mh` / `od_lh` は**そのステップの move が OD かどうかを一切見ていない**。**しゃがみ弱 P にも「OD(弱中)」を付けられた** |

**⇒ 開発者判断（2026-09-09）＝射程を広げる。活性条件も直す。**

#### ★★2 面それぞれの変更（指示書 §2.4-2 ／ `sd-020-od-variants-two-surfaces`）

| 面 | 変更 |
|---|---|
| **仮想コントローラ側** | 非プレーン OD 3 種を**既定非表示・展開で全部出す**（`D-722` の (b)）。**★展開の流儀は既存の `CollapsibleFieldset` に揃えた**（「隠す」/「表示」＋ `ChevronDown`）。**新しい流儀を作っていない**。**★プレーン OD は畳まない**（既定で押せる） |
| **修飾フラグ側** | (a) 既定非表示（同じ `CollapsibleFieldset`） ＋ (b) **活性条件**＝`isOdVariantApplicable(move)`（`category==='special'` かつ `code` が `_od` で終わる） |

**★描画箇所は 2 つある** — `RecipeBuilder.tsx`（これから足すステップのインライン行）と `ModifiersEditor.tsx`（既存ステップのダイアログ。**コンボ登録とセットプレイの 2 面が共有**）。**⇒ 条件を画面側へ散らさず、共通部品 `ModifierOdVariantFields` 1 つに持たせた。**

**★★既存データの保護** — 既に `od_lm` 等が入っているステップでは**節を自動展開し、チェック済みの項目は外せるまま残す**（`disabled={!applicable && !checked}`）。**抑止するのは新規付与だけである。⇒ 「開いたら消えていた」を作らない。**

**★`OD_VARIANT_ORDER` の値の集合は変えていない**（指示書 §4-4）。変えたのは「いつ出すか」だけである。

**★`ModifiersSummary.tsx` は触っていない**（表示専用のラベル引きであり、選択肢を描かない）。

### 2.4 `SM-080` / `SM-149` — ボタン名と配置

**★開発者の指示は「まず全てモックを出してください」であった。⇒ 案を全数（ラベル 3 案 ／ レイアウト 3 案）提示したうえで選んでもらった。**

| identity | 決定 | 内容 |
|---|---|---|
| **`SM-080`**（ボタン名） | **案 A＝現行維持** | `controller.direction.*.label` を ja / en とも 1 値も変えていない。**★「未着手」ではなく「判断済み」である**——`followup` `G-2` は 2026-07-21 から「実機確認できるタイミング」のまま止まっていた |
| **`SM-149`**（配置） | **案 X′** | 左＝方向パッド（3×3） ／ 右＝攻撃ボタン（2×3）＋ **その下に空く 1 行ぶんへラッシュトグル**。**⇒ 縦が着手時点の約半分になる** |

**提示して採らなかった案**（記録）:

| 案 | 内容 |
|---|---|
| ラベル案 B | 姿勢で揃える（立ち後ろ / 立ち / 立ち前 ／ しゃがみ後ろ / しゃがみ / しゃがみ前） |
| ラベル案 C | 方向で揃える（上後ろ / 上 / 上前 ／ 後ろ / ニュートラル / 前 ／ 下後ろ / 下 / 下前） |
| レイアウト案 X | 横並び・ラッシュは下に全幅 |
| レイアウト案 Y | 現行維持（縦並び） |

**実装上の約束**

- **★ボタンの大きさ（`ControllerButton` の `size`）は 1 つも変えていない。** 横へ広げただけである（逐語＝「現在の仮想コントローラのサイズ感はいいので」）。
- 狭い画面（`sm` 未満）は従来どおり縦へ折り返す。
- **2 面（コンボ登録・セットプレイ）へ同時に効く**（同じ部品を共有しているため）。

#### ★`CO-020` が同一クラスタとされている根拠（指示書 §2.6-5 への回答）

**ledger（`M25-RESEARCH-01` §2.2 の 374 / 376 / 377 行、`M24-RESEARCH-01` §417）は「同じ共通部品 `HitBoxLayout.tsx` を触るから」を根拠にしている。要求そのものは別である**（i18n ／ 名前 ／ 配置）。**★`CO-020`（`HitBoxLayout` の i18n 未対応）は `M24-07` で解消済み**であり、文言は locale が正典になっている。**⇒ 本サブで新たにやることは無い。**

---

## 3. テスト

### 3.1 変更統計（着手基点 `4c0dcb2`）

**★数える範囲を `web/` に限る**（完了報告そのものを含めると、報告を書き足すたびに数字が古くなる）。`git diff --numstat 4c0dcb2 HEAD -- web/`:

| 追加 | 削除 | ファイル |
|---:|---:|---|
| 47 | 12 | `web/e2e/m30-01-controller-surfacing.spec.ts` |
| **140** | **0** | `web/e2e/m30-02-controller-selection.spec.ts`（**新規**） |
| **90** | **0** | `web/src/features/combo/components/ModifierOdVariantFields.tsx`（**新規**） |
| 72 | 15 | `web/src/features/combo/components/ModifiersEditor.test.tsx` |
| 12 | 2 | `web/src/features/combo/components/ModifiersEditor.tsx` |
| 88 | 6 | `web/src/features/combo/components/RecipeBuilder.test.tsx` |
| 22 | 2 | `web/src/features/combo/components/RecipeBuilder.tsx` |
| 75 | 54 | `web/src/features/combo/components/VirtualController/HitBoxLayout.tsx` |
| 118 | 24 | `web/src/features/combo/components/VirtualController/SpecialMovePanel.tsx` |
| 184 | 6 | `web/src/features/combo/components/VirtualController/VirtualController.test.tsx` |
| 120 | 5 | `web/src/features/combo/inputResolution.test.ts` |
| 147 | 20 | `web/src/features/combo/inputResolution.ts` |
| 24 | 5 | `web/src/features/combo/labels.ts` |
| 42 | 24 | `web/src/features/combo/moveSurfacing.roster.test.ts` |
| 33 | 8 | `web/src/features/combo/moveSurfacing.test.ts` |
| 5 | 3 | `web/src/features/setup/components/SetupRecipeEditor.test.tsx` |
| 8 | 1 | `web/src/locales/en.json` |
| 8 | 1 | `web/src/locales/ja.json` |
| **1235** | **188** | **18 ファイル** |

**★新規のつもりのファイルが `+` だけであることを確認した**（教訓 `E-225` ／ CLI Phase A 補足 (b)）。**新規 2 ファイルはいずれも deletions が 0 である**（上表で太字にした行）。**作る前に `ls` / `find` で同名の不在も確認済みである**（`ModifierOdVariantFields.tsx` ／ `m30-02-controller-selection.spec.ts` とも、`git log` に履歴が無く `git status` で `??` であることまで確かめた）。188 件の deletions はすべて既存 16 ファイルの書き換えによるものである。

### 3.2 実行結果

| 対象 | 結果 |
|---|---|
| `go test ./...` | **緑**（exit 0・失敗パッケージなし） |
| `cd web && pnpm test` | **緑**（222 files / **2653 passed**） |
| `cd web && pnpm exec tsc --noEmit` | **緑** |
| `make e2e-only P=m30-02` | **7 passed** |
| `make e2e-only P=m30-01` | **9 passed** |
| `make e2e`（全数・Phase A 時点） | **緑（285 passed / 0 failed・exit 0）**。**★`M30-01` 完了時に 1 件赤だった `m24-12-editor-rebuild.spec.ts` も緑である**（同レポート §4.4 の申し送り。本サブの着手基点で既に解消していた） |
| `make e2e`（全数・Phase C 取り込み後） | **緑（284 passed / 1 flaky / 0 failed・exit 0）**。**★flaky は `m31-02-tag-field-drag.spec.ts:49`「下へ枠外までドラッグしても文字選択が消えない」1 件で、本サブの射程外である**——`git diff --name-only 4c0dcb2 HEAD` に tag / text-drag / character 関連は **1 ファイルも無い**。**★念のため `make e2e-only P=m31-02-tag-field-drag` を単独で回して 6 passed を確認した**（再試行で緑になり、単独でも緑）。**★「flake」を根本原因として片付けていない** —— 本サブの差分と無関係であることを差分の実査で切り分けたうえで、`M31-02` 側の申し送りとして §6 へ残す |
| `bash scripts/check-artifact-integrity.sh` | **緑**（★1 本目に実行。検査 14 件の自己検査 ＋ 生成物 4 件） |
| `make e2e`（全数・§8 の追補後） | **緑（283 passed / 2 flaky / 0 failed・exit 0）**。**★flaky 2 件は `m31-02-tag-field-drag.spec.ts:49` で、全数実行のときだけ起こる**（単独 5 回は 30/30 緑）。§6-10 に実測と次の一手を残した |
| 常設検査（`check-stop-discipline` / `check-doc-refs` / `check-browser-storage-keys` / `check-enum-sync` / `check-import-order` / `check-doc-inventory`） | **すべて緑**（ベースライン超過なし）。**★`check-import-order` は「101 → 100 へ 1 件減った」と出るが、ベースラインは下げていない**（理由は §5-7） |
| `bash scripts/check-progress-log-index.sh` | **緑**（★Phase D の追記後に実走。追記前は違反 1 件だった） |
| `bash scripts/check-md-emphasis.sh docs/progress/M30-02-completion-report.md` | **緑（検出 0 行）**。★1 行検出されたため自分で直した（`**A（B）**は` の形を `**A**（B）は` へ） |

**新規・改訂テストの内訳**

| ファイル | 何を主張するか |
|---|---|
| `inputResolution.test.ts` | `isOdVariantApplicable` 6 件 ／ `parseSpecialCode` の 3 段（`none` ／ **A2 が `null` のまま** ／ 部分一致で拾わない） ／ **変種の畳み 6 件**（畳み・代表名・`_max_holding` の順序・ジャスト・**孤児は畳まない**・必殺技以外は対象外） |
| `moveSurfacing.test.ts` | A1 が出る側へ移ったこと ／ A2 が未分類のままであること ／ **`_od` の兄弟と 1 ファミリーへ合流すること** |
| `moveSurfacing.roster.test.ts` | 実測の書き直し（144 / 105 / jamie 4 / `_od` だけ 0 / キャラ別表） ＋ **`M30-02` と `M30-03` の境界を機械で固定する主張 1 本** |
| `VirtualController.test.tsx` | 強度なし通常版 ／ 弱中強の行を描かない条件 ／ **変種行 4 件** ／ **配置 3 件**（横並びの兄弟関係・DOM 順・**`data-testid` と aria 名を動かしていないこと**） ／ OD 変種の既定非表示 |
| `ModifiersEditor.test.tsx` | **SD-020 のダイアログ側 5 件**（既定非表示 ／ 展開 ／ 通常技では不活性 ／ OD 必殺技では選べる ／ **既存データは外せる**） |
| `RecipeBuilder.test.tsx` | **SD-020 のインライン側 4 件**（★もう片面。片面だけだと退行が緑で通る） |
| `m30-02-controller-selection.spec.ts`（新規） | 実 seed での 7 件（変種行 ／ 最大ホールド・ジャスト ／ **変種を持たないファミリーの対照** ／ 強度なし通常版 ／ **逐語 (b) の対照** ／ OD の既定非表示 ／ 修飾フラグ側の活性条件） |

### 3.3 ★★破壊確認

**3 件とも、対象の区分を名指しして復元した。**

| 破壊 | 内容 | 結果 |
|---|---|---|
| **A** | 変種の畳みを無効化（`splitSpecialVariant` が常に `plain` を返す） | **6 failed / 1083 passed。** 落ちたのは畳み 3 本 ＋ 変種行 3 本。**★「変種行が在ること」だけでなく「ファミリー行から消えること」「強度がホールド版へ切り替わること」も落ちた** |
| **B** | `isOdVariantApplicable` を常に `true` にする | **7 failed / 1082 passed。★★2 面とも落ちた**（`ModifiersEditor` 1 本 ＋ `RecipeBuilder` 2 本 ＋ 単体 4 本）。**⇒ 片面だけのテストになっていない** |
| **C** | `parseSpecialCode` の `"none"` を `null` へ戻す | **17 failed / 1072 passed。** 実測固定 5 本 ／ 述語 4 本 ／ 必殺技タブ 2 本 ／ 未分類タブ 2 本 ／ `SM-100` 1 本 ／ `parseSpecialCode` 2 本 ／ 空状態 1 本 |

**★どの破壊でも「要素が存在すること」だけを見ているテストは落ちなかった。** 落ちたのは**並び・母集団・遷移**を見ているテストである。**復元後は全数緑に戻ることを確認済み**（`git diff` が空であることも確認した）。

---

## 4. レビュー結果

**レビュー報告書**: `docs/progress/m30-02-review.md`（Phase B・fresh subagent。メイン会話文脈を継承しない独立エージェント）

| 項目 | 実測 |
|---|---|
| 指摘の総数 | **13 件**（高 3 / 中 5 / 低 5） |
| **「高」指摘の不採用** | **0 件**（3 件すべて採用）。**⇒ 安全弁（開発者エスカレーション）の発動は無い** |
| 「中」の採否 | **5 件すべて採用**（中-3 は Phase D で実施） |
| 「低」の採否 | **4 件採用 / 1 件不採用**（低-3＝import 順。**着手時点からの持ち越しでベースライン内。本サブの差分に無関係な整形を混ぜない**。指摘者も同じ結論） |
| 再レビューの往復 | **0 回**（初回のみ。停止規律の上限 2 回に達していない） |
| チェックリストの中心 | **束 A ◎ / 束 B ○ / 束 C ○ / 束 D ◎ / 束 E ○ / 束 F △**（束 F の × は `progress-log` 未追記＝Phase D で解消） |

**採否と理由の全数は、レビュー報告書末尾の「## 取り込み結果（自動トリアージ）」に表で置いた**（事後監査可能にするため。CLI Phase C の品質補償）。

**★★レビューが捕まえた最も重い型は 2 つある。**

1. **新設した活性条件が、実在する入力を弾いていた**（高-2）。`isOdVariantApplicable` が `_od` 末尾だけを見ていたため、**`od` が `code` の途中に在る OD 必殺技 37 行**（`mai/flame_od_kachousen` ／ `ryu/denjin_charge_od_hadoken` 等）で、**着手時点にできた入力ができなくなっていた**。**⇒ 機能後退である。★自分のテストは `hadoken_od` しか見ておらず、この形が 1 件も入っていなかった。**
2. **画面の `disabled` は入口の 1 つでしかなかった**（高-3）。「既存データを外せるようにする」ための緩和（`disabled = !applicable && !checked`）が、**選び直しの経路では抑止をすり抜けていた**。**⇒ 「OD 技で選ぶ → 通常技へ変える → 追加」で、`SD-020` が塞いだはずの状態が復活した。★テストは「選ぶ → 判定を見る」で止まっており、この順序を通っていなかった。**

**★あわせてレビューの数字を 1 件訂正した** — 高-2 の件数は 31 ではなく **37** である（自分で実測し直した）。**指摘の中身は完全に正しい。**

## 5. ■ 併せて更新が要るもの

| # | 項目 | 状態 |
|---|---|---|
| 1 | **消費した CHANGE 番号の登録**（`docs/handover/change-number-registry.md` §1） | **なし。★CHANGE を 1 本も起票していない**（自採番禁止＝`D-293`）。**⇒ 登録すべき番号が無い。** 要る箇所は §6 に一覧した |
| 2 | **番号の写し先 4 か所**（registry §1 ／ 契約 §4 ／ ボード §2.1 ／ ボード §2.4） | **なし**（#1 が無いため） |
| 3 | **消費したマイグレ連番** | **なし（0 本）。★実査値は `ls migrations/` の最大が `000106` である。⇒ 次に払い出す番号は `000107` である**（`M30-01` 完了報告 §7-3 と一致） |
| 4 | **版を上げた文書の参照元** | **なし**（版を上げた文書は無い） |
| 5 | **ブラウザストレージ台帳**（`web/CLAUDE.md` §1） | **追記なし。** 本サブが足した UI 状態（選択中の変種 ／ OD 節の開閉）は**いずれも component-local の `useState` であり、ブラウザストレージへ置いていない**。`check-browser-storage-keys.sh` 緑 |
| 6 | **`docs/progress/progress-log.md` への索引行** | **追記した**（Phase D）。**★`check-progress-log-index.sh` の緑を追記後に実走して確認した**（追記前は「作業 ID `m30-02` が現れない」で違反 1 件だった）。**★`check-md-emphasis.sh` を同ファイルへ掛けると 11 行検出されるが、いずれも行 3152〜4483 の既存の歴史記録であり、本手番で足した行（5670 以降）は 0 行である**（継続更新ファイルの既存行は直さない＝`D-274` (3)） |
| 7 | **`scripts/check-import-order.sh` の `BASELINE`** | **★下げていない。** 本サブで違反ファイルが 101 → 100 へ 1 件減り、スクリプトは「`BASELINE` を 100 へ下げること」と出力する。**★★しかし同スクリプトの冒頭注記が「本定数は作業ツリー全体の総数である。レーン（worktree / ブランチ）ごとに測って下げてはならない」と明記している**（並走レーンが足したぶんが見えず、マージした瞬間にずれる＝`check-md-emphasis.sh` が 2026-08-14 に踏んだ型）。**⇒ `main` へマージ後、改善レーンか開発者の手番で下げる** |
| 8 | **`docs/design/testid-convention.md` の付与済み一覧** | **★製造は編集しない**（`CLAUDE.md` §8 ／ 指示書 §4-11）。**⇒ §6 へ登録依頼として出した** |

---

## 6. `docs/design/` に反映が要る箇所（**製造は直さない**）

**★本サブは `docs/design/` を 1 文字も編集していない。**

| # | 対象 | 内容 | 区分 |
|---|---|---|---|
| **★★1** | **`character_data/terry.csv` ＋ マイグレ 1 本** | **★★【2026-09-09 解消】開発者のインゲーム確認を得て本サブで是正した**（マイグレ `000107`。§8.4）。**⇒ 請求は不要になった。** 以下は解消前の記述： **【データ是正の請求】`terry` の `move_code` `round_wave_heavy` を `round_wave` へ。** **★★根拠＝`name_ja` が「ラウンドウェイブ」で強度語を持たず、`power_wave` が弱・中・OD しか持たない**（236HP が別技であるため）。**⇒ 強度の概念が無い技に `_heavy` が付いている。** **★先例 2 件と完全に同型である**〔`sonic_break_light → sonic_break`（`000063`） ／ `quick_burn_light → quick_burn`（`000106`）〕。**★是正すれば本サブの「強度なし通常版」へ自動で合流する。★本サブはマイグレ 0 本・自採番禁止のため実施していない** | **設計卓／開発者への請求** |
| **★★2** | **`DES-004` §2.1 ／ `DES-005` §5.7** | **as-built ＋ 規則の変更: 必殺技ファミリー UI が「強度なし」を持つようになった。** `CHANGE-166` が明文化した「強度接尾辞を持たない `move_code` はファミリー UI に載らない」は、**A1 の 186 件について失効した**（A2 の 105 件については不変＝`M30-03` の射程）。**★これは `M30-01` §6-1 の「ファミリー UI の拡張」請求に対する開発者判断（2026-09-09・案 A）の実施である** | **CHANGE 起票が要る** |
| **★★3** | **`DES-004` §2.1 ／ `DES-005` §5.7** | **as-built ＋ 規則の追加: 必殺技ファミリーに「変種」の軸ができた**（通常 / ホールド / 最大ホールド / ジャスト）。**★`<基底>_holding` 等は独立ファミリーではなく基底の変種として畳む。★畳むのは基底が実在するときだけ。★射程は `category='special'` のみ** | **CHANGE 起票が要る** |
| **★★4** | **`DES-005` §5.7 ／ `SUPP-001` §3.3.1**（`modifiers.flags`） | **as-built ＋ 規則の追加**: OD 強度組合せ（`od_lm` / `od_mh` / `od_lh`）は **(a) 既定非表示**、**(b) そのステップの move が必殺技の OD のときだけ選択可**。**★(b) は `SD-020` の射程拡大であり、開発者判断による**（2026-09-09）。**★既存データの値は消さない**（外せるまま残す） | **CHANGE 起票が要る** |
| **★5** | **`DES-005` §5.7** | **as-built: 通常技タブの配置が縦並びから横並びになった**（左＝方向パッド ／ 右＝攻撃ボタン ＋ ラッシュトグル）。**★ボタンの大きさは不変。★狭い画面では従来どおり縦へ折り返す** | as-built 反映 |
| **★6** | **`docs/design/testid-convention.md`** | **★★登録義務は無い**（`CHANGE-171` ／ `D-782` が「付与済み一覧」運用を畳んだ。**同書は「新規付与時に本表を更新する義務は無い」「実装が正本である」と明記している**）。**⇒ 本欄は「登録依頼」ではない。** **★ただし同書は「判断が要った付与だけは残す価値がある」とも書いている。⇒ 本サブの新設 8 種のうち 2 件がそれに当たる**——`recipe-special-variant-max-holding`〔**列挙値 `max_holding` をそのまま埋めず、ケバブケース規約へ合わせた**〕 ／ `recipe-od-variant-section-inline` ・ `-dialog`〔**2 面が同じ部品を描くため id を割った。割らないと同時表示で strict mode が落ちる**〕。**★足すか否かは設計卓の任意である**（製造は `docs/design/` を編集しない）。**★消えた id も 1 種ある**——`recipe-special-family-{holding 系 19 本}`（変種として畳まれたため） | 情報提供（**義務なし**） |
| **★★6.5** | **`DES-005` §5.7（⑤ ファミリー行の並びと母集団）** | **★★【請求・別サブ】開発者の指摘**（2026-09-09）＝「魔身がたくさん並んでいるのは確かに煩雑。魔身（酔い+1）以外の魔身は未分類とかへ入れられたらベスト。また魔身は攻撃技ではないので、必殺技の最後に置きたい」。**実測は §8.5 に置いた。⇒ リスト DTO の拡張が要り、`M30-01` §2.9 の判断を覆すため、別サブが要る**（開発者判断 2026-09-09） | **設計卓への請求** |
| **★7** | **`M30-03` との境界** | **`parseSpecialCode` は本サブが 3 段へ広げた。** `M30-03` は (ii) の枝〔強度語が `code` の途中に在る → `null`〕**だけ**を広げればよい。**★境界は `moveSurfacing.roster.test.ts` の主張が機械で固定している。★`M30-03` §2.1 の段 1 で測り直したとき、`_od` だけ押せる形は 0 件・未分類 144（CSV 基準）から始まる**。**★★あわせて「素は押せるが OD が押せない」7 組を引き継ぐ**（§2.1 の訂正）。**`M30-03` が A2 を載せれば 0 件になり、同名のテストが赤で知らせる** | 境界の申し送り |
| **★8** | **ファミリー行の密度**（2026-09-09 レビュー 低-5） | **`P4M-016` によりファミリーの総数が seed 全体で 273 → 342 に増えた。** 3 列グリッドに並ぶ数はキャラ最大 21（`jamie`） ／ 20（`akuma`） ／ 17（`ingrid` / `blanka`）。**★とくに `jamie` は `the_devil_inside` ／ `_up2` ／ `_up3` ／ `_up4` ／ `_reach_drink_lv4` 系が「魔身（酔い+N）」として横並びになる。⇒ `P4M-018` が畳んだのと同型の「軸に落とせる並び」であり、`SD-020` の目的**〔煩雑さを減らす〕**とは逆方向に効く面がある。★開発者判断（案 A）どおりの実装であり違反ではない。⇒ 次サブ以降の候補として挙げる** | 申し送り（候補） |
| **10** | **`m31-02-tag-field-drag.spec.ts:49` が全数実行でのみ揺れる** | **★実測（2026-09-09）**: **単独実行 5 回はすべて 6 passed（30/30）／ 全数実行 2 回はどちらも flaky**（1 件目＝「下へ」 ／ 2 件目＝「右へ」「下へ」）。**⇒ 全数実行のときだけ起こる。** **★本サブ由来ではない** —— (a) 本サブの差分は `TagSelector` ／ `web/src/lib/text-drag-capture.ts` ／ `SearchableSelect` に 1 行も触れていない、(b) **本追補より前の全数実行**（コミット `2335d59` 時点）**で既に同じ spec が flaky だった**。**★★ただし「無関係」とは言い切れない** —— 同 spec は `/combos/new` を使い、**レシピ面は `hidden` で常時マウントされている**（`ComboEditor.tsx:1060-1067`）。⇒ 本サブの変更も同じページに乗る。**`hidden` は `display:none` でレイアウト箱を持たないため座標はずれないが、描画の重さには効きうる。** **★次に見るなら**: 同 spec は座標ドラッグ（`popoverBox` ＋ `EDGE`）で測っており、**4 方向のうち「右」「下」だけが揺れる**。`M31-02` 完了報告の横断課題 4 が「原因ではなく回避を入れた」と書いており、**揺れはその回避の境界に在る可能性がある。★「flake」で片付けない** | 申し送り（`M31-02`） |
| **11** | **`followup-backlog.md` の 2 項目** | `od-variant-active-without-od`（**★調査完了。修飾フラグ側で常に実在していた。射程を広げて是正済み**） ／ `sd-020-od-variants-two-surfaces`（**★2 面とも直した**）。**★製造は §J 以外を編集できない**（`D-382`）ため、設計卓が畳む | 設計卓が畳む |

---

## 7. ★★開発者の手動確認（**機械では詰められない範囲**）

**★本節は「やってください」の一覧ではなく「何を見れば何が分かるか」である。** 判定基準を書かないと、見ても判断できない。

**★★先に、機械が確かめた範囲を書く**（重複して確かめないため）。`go test ./...` 緑 ／ `cd web && pnpm test` 222 files・2657 passed ／ `make e2e` 284 passed・1 flaky・0 failed ／ 常設検査 10 本緑 ／ 破壊確認 3 件。**⇒ 下記 A の①〜④は E2E が機能的な経路をすべて通っている。人が見る必要があるのは「正しく動くか」ではなく「これでいいか」の側である。**

**起動**: `make run-server`（`config.toml` の `[server] port`・既定 47318） ＋ `make run-web`（Vite 5173）。**★E2E 専用スタック**（47390 / 5273）**とは別であり、dev DB を使う。**

### 7.1 A — 実機で見るもの（5 件）

| # | 対象 | 操作 | 見るもの・判定 |
|---|---|---|---|
| **①** | **`SM-149` 横並び** | 任意キャラでコンボ新規登録 → レシピタブ → 通常技タブ | 方向パッドが左・攻撃ボタンが右・ラッシュトグルが攻撃の下。**★縦が着手前の約半分になっているか。★ボタンの大きさは 1 つも変えていない**（逐語「サイズ感はいい」が保たれているか）。**ブラウザ幅を狭めると縦へ折り返す**（`sm` 未満） |
| **②** | **`P4M-018` 変種行** | **マリーザ** → 必殺技タブ | ファミリー行に「【ホールド】弱グラディウス」が**無い**こと。「グラディウス」→ 変種行に［通常］［ホールド］→ ホールドで弱を押すと **【ホールド】弱グラディウス** が積まれること。**★3 変種の例**＝`rashid/whirlwind_shot`（通常/ホールド/最大ホールド） ／ `luke/flash_knuckle`（通常/ホールド/ジャスト）。**該当は全 18 ファミリー**（§2.2 の表） |
| **③** | **`P4M-016` 強度なし通常版** | **テリー** → 必殺技タブ → 「クイックバーン」 | ［強度なし通常版］だけが出て弱中強の行が出ないこと。**★★この文言が実機の感覚に合うか**（変えるなら locale 1 値の差し替えで済む）。**★同じ画面で「ラウンドウェイブ」も見ること** —— こちらは**まだ「強」のまま**である（§7.2） |
| **④** | **`SD-020`** | **リュウ** → 必殺技タブ → 「波動拳」／ 続けて全技一覧 | OD の下が「OD 強度組合せ ▼表示」で畳まれ、展開すると 3 つとも出ること（**★「在るキャラだけ出す」ではない**）。全技一覧の補足で**しゃがみ弱P では OD(弱中) が押せず、OD 波動拳では押せる**こと。**★★`[電刃錬気]OD波動拳` でも押せること**（レビュー 高-2 が見つけた 37 行の後退を直した箇所） |
| **⑤** | **ファミリー行の密度** | **ジェイミー** → 必殺技タブ | ファミリーが 21 個並ぶ（`the_devil_inside` 系 7 本が「魔身（酔い+N）」として横並び）。**★煩雑すぎないか。** 案 A どおりの実装で違反ではないが、`SD-020` の目的と逆に効く面がある（§6-8 に申し送り候補として記載） |

### 7.2 B — ドメイン知識が要る判断（**1 件。★これだけは製造が代われない**）

**★★テリーの「ラウンドウェイブ」に強度は存在するか。**

| 実測 | 値 |
|---|---|
| `move_code` | `round_wave_heavy` |
| `name_ja` | 「ラウンドウェイブ」（**強度語なし**） |
| `command` | `d dr r plus p_h`（236HP） |
| 兄弟 | `power_wave` は弱・中・OD のみ（**強が欠けている**） |

**⇒ 「236HP は強パワーウェイブではなく別技」であれば `round_wave` へ是正すべきである。** **★★先例 2 件と完全に同型であり、いずれも一次源は開発者のインゲーム確認である** —— `sonic_break_light → sonic_break`（`M19-04c` / `000063`） ／ `quick_burn_light → quick_burn`（`M28-05` / `000106`。逐語＝「`quick_burn_light` には強度は存在しません」）。

**★是正すればマイグレ 1 本で、実装済みの「強度なし通常版」へ自動で合流する。★E2E に「まだ『強』のまま」の陽性対照を置いてあるため、是正した瞬間そこが赤で知らせる。**

### 7.3 C — 既存データの確認（**1 件。該当が無ければそれで足りる**）

**dev DB に「OD 技でないステップに `od_lm` / `od_mh` / `od_lh` が付いたコンボ」が在るか。**

在れば、そのステップの［編集］を開く。**期待＝節が自動で開いた状態で出て、チェックを外せる**（消えていたら不具合）。**★抑止したのは新規付与だけである。⇒ 既存の値は消さない。**

**★該当データが無ければ「無い」で確認完了である。** 製造側からは開発者の dev DB が見えないため、ここだけは実機に依存する。

### 7.4 D — 事務の手番

| 何を | 誰が |
|---|---|
| `claude/m30-02-implementation-plan-2wa8l9` を main へマージ | **開発者**（push 済み。**★Claude は push までしかできない**＝`CLAUDE.md` §7） |
| **CHANGE 起票 3 本**（`DES-004` §2.1 ／ `DES-005` §5.7 ／ `SUPP-001` §3.3.1） | **設計卓**（自採番禁止＝`D-293`。内容は §6-2 / §6-3 / §6-4） |
| **ラウンドウェイブ是正のマイグレ `000107` 払い出し** | **設計卓**（§7.2 の結論待ち。**★実査値＝`migrations/` の最大は `000106`**） |
| `scripts/check-import-order.sh` の `BASELINE` を 101 → 100 | **マージ後**に改善レーンか開発者（**★同スクリプトが「レーンごとに測って下げてはならない」と明記**＝§5-7） |

---

## 8. ★★開発者の実機確認とその反映（2026-09-09）

**§7 の手動確認に対する開発者の回答と、そこから出た 6 件の是正。**

| # | 開発者の回答 | 着地 |
|---|---|---|
| A① 横並び | **違和感なし。★ただし最低幅まで縮めるとタブ表示が崩れる**（未分類 / キャラ固有状態 / 共通技）。規定サイズで縦並びになるのは PC では許容だが、スマホは要確認 | **是正した**（§8.1） |
| A② 変種行 | **マリーザ・ラシード・ルーク すべて問題なし** | — |
| A③ 強度なし通常版 | **感覚と一致** | — |
| A④ OD 組 | **「そもそも必殺技以外なら OD 強度組合せを出さない事はできませんか?」** | **是正した**（§8.2） |
| ⑤ ファミリー行の密度 | **煩雑。魔身（酔い+1）以外は未分類へ入れたい。魔身は攻撃技ではないので必殺技の最後に置きたい。（DB 登録順?）** | **★別サブへ回す**（§8.5。開発者判断） |
| B ラウンドウェイブ | **強度はなし。`round_wave` へ是正するべき** | **是正した**（§8.4） |
| C 既存データ | **なし** | — |
| その他1 | **`[電刃錬気]OD波動拳` が必殺技タブから入力できないが既存バグ?** | **★既存バグではないが本サブが作った非対称。`M30-03` へ委ねる**（§2.1 の訂正） |
| その他2 | **OD 強度組合せが押下可能なエリアと分かりにくい** | **是正した**（§8.3） |

### 8.1 A① タブの折り返しが箱からはみ出す（**★表示が壊れていた**）

**原因＝共通部品 `web/src/components/ui/tabs.tsx` の `TabsList` が `h-10`**（高さ 40px 固定）**を持つこと。** `M30-01` がタブを 4 → 8 枚にしたとき `flex-wrap` だけを足し、**高さを開放していなかった。⇒ 折り返した 2 行目以降が箱の外へ出る。崩れるのが末尾 3 枚なのはそのためである。**

**★`M30-01` 由来であり `SM-149` の横並びとは無関係である**（横並びは通常技タブの中身であって、タブ行ではない）。

**★共通部品は触らない。** `h-10` は 1 行に収まる他画面のタブが依存する既定であり、**変えると波及先が読み切れない。⇒ 呼び出し側で `h-auto flex-wrap justify-start gap-1` を上書きした**（`cn()` が後勝ちで解決する）。

**★テストの判定はクラスで行う**（`h-10` を持たず `h-auto` を持つ）。**jsdom はレイアウトを持たないため、実際にはみ出すかどうかは測れない。⇒ その限界をテスト本文に書いた。**

**★★スマホは「確認した」と書かない。** 製造側でできたのは **390px 幅のビューポートでのスクリーンショット**までであり、**実機ではない。** その幅ではタブが 3 行へ折り返して箱に収まり、通常技は縦並びへ折り返すことを確認した。

### 8.2 A④ 必殺技の OD でなければ OD 強度組合せを節ごと出さない

**開発者の逐語への答え＝できる。** 着手時点は「常に節を出して中身を `disabled`」だった。**⇒ `applicable` でなく、かつ選択済みフラグも 0 件なら節ごと描かない形へ変えた。**

**★★例外は 1 つだけ** —— **既に値が入っているときは非適用でも出す。** 出さないと外せなくなり、**「開いたら消えていた」を作る。** そのときは自動で展開し、チェックも操作できるままにする。**★抑止しているのは新規付与だけ、という方針は着手時点から変えていない。**

**★使われなくなった locale キー `controller.special.odVariantsUnavailable`（ja / en）を削除した。** 残すとレビュー 高-1 が指摘した「失効した記述」そのものになる。

### 8.3 その他2 押下可能と分かる見た目へ

**★同じ画面に在る全技一覧のトグル**（`recipe-pulldown-toggle`＝「▶ 全技一覧から選ぶ」。枠・白背景・ホバー）**と同じ見た目へ揃えた。⇒ 新しい流儀は作っていない。画面内の 2 つ目の流儀へ寄せただけである。**

**★`CollapsibleFieldset` は置き換えない** —— 同部品は登録画面の 5 節とコンボ一覧のフィルタ欄が使っており、**見た目を変えると 6 面へ波及する。**

**★2 面が同じ `DisclosureToggleButton` を描く。★既存 2 か所のプルダウン**（`RecipeBuilder` / `SetupRecipeEditor`）**は本部品へ寄せていない。⇒ 見た目を変えるときは 3 か所を同じ手番で動かすことを部品の godoc に書いた**（寄せる整理は別の手番の候補）。

### 8.4 B ラウンドウェイブのデータ是正（**マイグレ `000107`**）

**★開発者のインゲーム確認**（2026-09-09）＝「ラウンドウェイブの強度はなしです。`round_wave` へ是正するべきです」。

**★★傍証がリポジトリ内に既に在った** —— `character_data/command-correction-history.md` の terry の行は、`round_wave_heavy` に対する **「対応 dist code」欄に `round_wave` と記録している**（`M14-03b` 投入前チェック・2026-07-13）。**⇒ 公式データ側の識別子は `round_wave` であり、本アプリ側だけが `_heavy` を持っていた。** 同ファイルは `quick_burn` について**まったく同じ形の注記**を持つ。

**三点更新（`000106` と同型）**: `character_data/terry.csv` ／ golden 4 本の up・down 計 10 か所（`000026` は `moves` seed と `name_ja` の 2 か所） ／ 新マイグレ `000107` ／ `internal/infra/migration/migrate_m3002_test.go`（v107 の最終状態・`down` → re-up の往復・紐付きの生存） ／ 履歴資料への注記。

**★件数ではなく状態で固定した** —— golden も直したため**新規 DB では `000107` の `UPDATE` が 0 行に当たる。**「1 行に当たった」で固定すると CI では常に 0 行になり何も守れない。**⇒ `down` → re-up の往復だけが「適用済み DB を追随させる」経路を実際に動かす。**

**★`moveSurfacing.roster.test.ts` の固定値は動かなかった** —— 是正の前後どちらでも必殺技タブに載るためであり、見込みどおりである。**E2E の対照は「まだ『強』のまま」から「強度なし通常版で押せる」へ書き直した。★緑にするために合わせたのではなく、直したから動く。**

### 8.5 ⑤ 魔身（**★別サブへ回す。実測を残す**）

**並び順の答え＝DB 登録順である**（開発者の推測どおり）。`deriveSpecialFamilies` は `moves` 配列の初出順を保持し、その配列は `moves.id` 昇順＝seed CSV の行順。**⇒ 並びを決める規則は実質無い。**

**「（酔い+1）以外」の正体＝`is_derived=true` と完全に一致する。** 魔身 7 行のうち base だけが `false`、残り 6 行が `true`（全行 `damage=0` ／ `command` も同一の `d d plus p`）。

**★★効果は大きい** —— `is_derived=true` の A1 をファミリー行から外すと**ファミリー総数 342 → 276**。着手前が 273 なので**密度はほぼ元へ戻り、かつ `P4M-016` の目的は達成される。** jamie 21 → 15 ／ blanka 17 → 8 ／ dhalsim 16 → 7 ／ akuma 20 → 15。

**★★しかし本サブでは実装できない。障害は 3 つとも実測で確かめた。**

| # | 障害 |
|---|---|
| 1 | **`is_derived` はフロントへ届いていない**（`MoveResponse` にも `Move` にも無い）。**⇒ リスト DTO の拡張が要る** |
| 2 | **★`code` だけの代理判定は使えない。** 「同キャラに自分の接頭辞となる別の A1 が在る」で代用できるか測ったところ**取りこぼし 90 / 116・誤爆 7**（`akuma` の百鬼系は `demon_dash` 派生だが接頭辞で拾えない） |
| 3 | **「非攻撃技を末尾へ」も `damage = 0` が要る。`damage` もリスト DTO に無い**（`MoveDetailResponse` のみ）。`on_hit` / `on_block` は攻撃技でも空になるため代理にならない |

**★★そして `M30-01` §2.9 が「`is_derived` を入力面の可否に使わない」を 2 つの理由で決めている** —— (a) `M19-DESIGN-08` §0 の述語は filler 候補から**除く**側であり裏返すと `target_combo` 117 行が入力できなくなる ／ (b) `is_derived` は既に 3 義を兼ねており、第 4 の用途は同書 §7 の列追加審査規約が禁じる形。**⇒ 覆すのは設計判断であり、本サブ指示書 §4-1「『何を出すか』の規則を変えること」に正面から当たる。**

**⇒ 開発者判断（2026-09-09）＝別サブへ回す。** §6-6.5 に請求として置いた。
