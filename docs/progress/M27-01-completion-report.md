# M27-01 完了報告: ヒット種別の追加と区分名の整理

| 項目 | 内容 |
|------|------|
| 作業ID | `M27-01` |
| 指示書 | `docs/instructions/M27-01-hit-type-and-size-labels.md` v1.0.0 |
| 実施日 | 2026-09-02 |
| 着手基点 | `78f4981af07730f5f265c506973deb3b4963b28b` |
| ブランチ | `claude/m27-01-implementation-plan-3bw6yc` |
| 成果物 | 本報告 ／ 段 1 成果物 `docs/progress/M27-01-report.md` ／ 画面モック `docs/progress/M27-01-mock/m27-01-hit-type-and-size.html` ／ レビュー `docs/progress/m27-01-review.md` |
| CHANGE 消費 | **0**（★成果物の承認後に設計卓が起票する。次番は `151` の見込み） |
| マイグレ消費 | **1 本**（`000081`。**⇒ 次の払い出しは `000082`**） |

---

## 1. 結果（母数付き）

### 1.1 ヒット種別 — 4 値 → **8 値**

| # | 識別子 | 表示名 ja | 表示名 en |
|---|---|---|---|
| 5 | `drive_impact_wall_splat_hit` | インパクト壁やられ(ヒット) | Impact wall splat (hit) |
| 6 | `drive_impact_wall_splat_block` | インパクト壁やられ(ガード) | Impact wall splat (block) |
| 7 | `drive_impact_punish_counter` | パニッシュカウンター(インパクト) | Punish counter (impact) |
| 8 | `stun` | スタン | Stun |

**既存 4 値は 1 文字も変えていない。既存データも動かしていない**（`hit_type` に CHECK 制約・DEFAULT が 0 件であることを `migrations/` 全 160 本の走査で確認済み ⇒ マイグレ消費 0）。

**ボタン数 = 8 値 ＋「不問」= 9 / 上限 10**（`DES-005` の逐語＝「上限 10 の根拠は数字キーが 10 個であること」）。**★余白は 1 しかない。**

### 1.2 相手の大きさ — 3 値 → **4 値**

| # | 識別子 | 表示名 ja | 表示名 en | 移行 |
|---|---|---|---|---|
| 1 | `standard` | 標準 | Standard | **`medium` から改名**（マイグレ `000081`） |
| 2 | `large` | 大 | Large | 新規追加（既存行なし） |
| 3 | `large1` | 大1(ザンギエフ等) | Large 1 (e.g. Zangief) | 値は不変・**ラベルのみ変更** |
| 4 | `large2` | 大2(マリーザ等) | Large 2 (e.g. Marisa) | 値は不変・**ラベルのみ変更** |

**ボタン数 = 4 値 ＋「不問」= 5 / 上限 10。**

**★「大1＝ザンギエフ／大2＝マリーザ」はリポジトリ内に記録が 0 件だった**（`ザンギエフ|マリーザ|体格|大きい相手` で `docs/` `character_data/` `web/` `internal/` `migrations/` を全文検索してヒット 0）。**本サブが初めて明文化した。**

### 1.3 注意書き（インフォメーションボタン）

「相手の大きさ」の欄に **既存部品 `InfoMark`**（ⓘ + Popover）で「**この区分で表現できない場合は、タグかメモを使ってください。**」を出した。

- **新規部品は作っていない。** `DES-005` が info-mark を正規のヘルプ機構として明記済み（M15-02 / `CHANGE-056`）。本番 3 箇所で稼働・ユニットテスト 4 ケース付き。
- **内容も新方針ではない。** `DES-003` の逐語＝「**代表的な数種類のみアプリ管理対象とし、細かいニュアンスはユーザーが `memo` 欄で補足する運用とする**」。⇒ **既存方針の画面への可視化**である。

---

## 2. ★★段 1 で開発者が確定させた列挙（**逐語**）

**★指示書 §3.2-2 は「★『承認された』とだけ書かない」と定めている。以下は開発者の回答をそのまま写したものである。全文は段 1 成果物 §9 にある。**

| # | 問い | **開発者の回答（逐語）** |
|---|---|---|
| Q1 | 入れる値はどれか | **「入れる値はスタン」** |
| Q2 | DI パニカンは既存 PC の仲間か | **「面1で示されたパニッシュカウンター(インパクト)と同義」** ／ 追問＝**「名前だけパニッシュカウンター。確定反撃のロジックは変更が必要で始動技がインパクトの時だけ使う」** |
| Q3 | 表示名 | **「案A」** |
| Q4 | 識別子 | **「ID-1」** |
| Q5 | 持続当ての別コンボ扱い | **「対象外。完了時に設計卓には連絡。phase4中には対応したいが、これは複雑な見込み。」** |
| Q6 | 相手サイズの DB 値 | **「B」** |
| Q7 | CSV ホワイトリスト | **「a」** |
| 追加 | 区分名 | **「通常 / 大 / 大1(ザンギエフなど) / 大2(マリーザなど) と分けてください」** ＋ **「そもそもモック通り標準にしてほしいが、私が指示で間違えたか？」**（⇒ **標準**を採用）＋ **「ボタンのラベルに入れる。」**＋**「全面で長くする」**＋**「『等』という言葉を付けてください。ザンギエフ等、マリーザ等といった形。」** |
| 追加 | PC 版生成の導線 | **「生成の対象外にするだけ入れる」** |
| 追加 | 確定反撃ロジック | **「別サブへ回す」** |

---

## 3. ★母数と、実際に通した数

**★指示書 §3.2-3 は「数えた箇所の数と、実際に通した箇所の数を並べて書く。★一致しないなら理由を書く」と定めている。**

### 3.1 なぜ一致しないのか（★先に結論）

**段 1 が数えた母数は「`hit_type` / `opponent_size` に**触れている**箇所」であり、「値を足したときに**変更が要る**箇所」ではない。** 両者は構造的に一致しない。

**理由**——本アプリの大半の層は **値に依存しない**。リポジトリ層の SQL は `hit_type` を `*string` として通すだけ、DTO も同様、画面は `*_LABEL_KEYS` を引いて描くだけである。**⇒ 値を足しても変更が要るのは「値を列挙している箇所」だけ。**

### 3.2 実測

| 区分 | 段 1 で数えた母数 | 実際に変更した数 | 説明 |
|---|---:|---:|---|
| ヒット種別に触れる箇所（全体） | **375 行 / 74 ファイル** | — | 母数。ほとんどが値非依存 |
| うち **値を列挙している本番コード** | **10 ファイル** | **7 ファイル** | 下表 |
| 相手サイズに触れる箇所（全体） | **197 出現 / 65 ファイル** | — | 母数 |
| 実装差分（`docs/` 除く） | — | **39 ファイル / +1,071 / −75** | テスト・E2E の固定値更新を含む |

**値を列挙している本番コード 10 ファイルの内訳（★全数）**

| # | ファイル | 変更 | 理由 |
|---|---|---|---|
| 1 | `internal/model/combo.go` | **変更あり** | 定数を追加・新設 |
| 2 | `internal/service/comboio/csvcore/rules.go` | **変更あり** | ホワイトリスト |
| 3 | `web/src/constants/combo-list.ts` | **変更あり** | 値域・キー写像 |
| 4 | `web/src/constants/punish.ts` | **変更あり** | 位置インデックス是正 ＋ PC 対象外集合 |
| 5 | `web/src/features/combo/labels.ts` | **変更あり** | エディタ専用ラベル |
| 6 | `web/src/locales/ja.json` | **変更あり** | 文言 |
| 7 | `web/src/locales/en.json` | **変更あり** | 文言 |
| 8 | `internal/service/combo/service.go` | **変更あり** | materialize の対象外分岐 |
| 9 | `internal/service/punishlist/service.go` | **★変更なし** | タブ分けの集合。**触らないと決めた**（ロジック本体は別サブ）⇒ 新 4 値は「区分を判定できない反撃」に入る |
| 10 | `internal/service/comboio/csvcore/combo.go` | **★変更なし** | 列名の定義のみで値を列挙していない（誤検出） |

**⇒ 値を列挙している本番コード 10 のうち 8 を変更し、2 は意図的に変更していない。通し漏れは 0。**

**★通し漏れが 0 であることの立証**——`opponent_size` 文脈の `medium` 残存を全数検索して **0 件**（技の強度 `light/medium/heavy` は 1 件も触っていないことも確認済み）。

---

## 4. ★★指示書との差分（開発者の指示で射程が広がった 3 件）

**★これは「契約違反の独自判断」ではない。** すべて開発者が明示的に指示したものである。**★ただし記録しないと次の担当が読めないため、ここに残す。**

| # | 指示書の条項（逐語） | 何が起きたか |
|---|---|---|
| 1 | **§4-2「『大1』『大2』の整理」**（★`要判断` である） | **★開発者が 4 区分を確定させた。** 禁止の理由は §2.2-4 の逐語「**判断が済んでおらず、一緒に直そうとすると止まる**」であり、**判断が供給されたことで禁止の前提が消滅した。** ⇒ `large` の新設と `large1`/`large2` のラベル変更を実施 |
| 2 | **§5-5 完了条件「相手サイズの『中』→『標準』が反映されている」** | **★この文言では 4 区分化を覆えない。** 実際に行ったのは「改名 1 件 ＋ 新設 1 件 ＋ ラベル変更 2 件」である |
| 3 | **注意書き / InfoMark の追加** | **★指示書のどの節にも無い。** 開発者の追加指示。`DES-005` §5.7 の変更にあたる（★`CHANGE` の対象。番号は自採番せず設計卓へ請求） |

---

## 5. ★やらないと決めたこと（`D-675`。**「触れなかった」と区別する**）

| # | 項目 | 決めた理由 |
|---|---|---|
| 1 | **ガードクラッシュ を入れない** | Q1 で「スタン」のみが選ばれた。★候補として並べた上で**選ばれなかった** |
| 2 | **インパクトヒット（壁やられ無し）を入れない** | 同上 |
| 3 | **持続当ての別コンボ扱い** | Q5 の逐語＝「対象外。完了時に設計卓には連絡。」★`hit_type` の値ではなく別機能である |
| 4 | **確定反撃ロジック本体の変更** | 開発者選択＝「別サブへ回す」。⇒ タブ分け（`punishHitTypes`）・「始動技がインパクトのときだけ使う」制約は触っていない |
| 5 | **相手サイズの軸名 3 通りの統一**（「相手サイズ」／「相手の体格」／「相手の大きさ」） | 指示書のどの節にも無く、`M27-02`（コンボ編集の入力品質）と面が重なる |
| 6 | **ラベルマップ 2 本の統合**（`constants` と `labels.ts`） | `labels.ts:5-21` 自身が「横断リファクタであり本サブでも行わない」と明記。**同期はしたが統合はしていない**（一致は機械検査で担保） |
| 7 | **CSV ホワイトリストの既存ズレの是正**（`small` / `large1` / `large2`） | Q7 の「a」。開発者裁定でスコープ外。★`medium` → `standard` の 1 語だけ追随させた |
| 8 | **改名前 CSV の `medium` を取込側で寄せること** | 上記 7 の射程を超える。`VAL-ENUM` WARNING が出るため silent ではない。**★ただし実害があるので §8 へ登録候補として出した** |

---

## 6. ★`check-enum-sync.sh` のベースラインが動いた理由

**24 → 28（+4）。** 理由は同スクリプト内にも記載した。

- 増えた 4 件は新しい 4 値が各 1 ファイルで当たったもの。**当たり先はいずれも `web/src/features/combo/labels.ts`**（エディタ専用のラベル表）。
- **★既存 4 値もまったく同じ形で当たっている**（`counter`=1 / `punish_counter`=2 / `just_parry_punish_counter`=1）。**⇒ 新しい同期漏れではなく、既知の 2 本立て構造が値の数だけ増えたものである。**

**★★同スクリプトは `opponent_size` を 1 件も見ていない。** 抽出パターンが接尾辞 `Category|Status|Type|Code` にしか一致せず、`model.OpponentSize*` を拾わない。**⇒ 指示書 §5 完了条件 3「`check-enum-sync.sh` が緑」は、相手サイズについては担保にならない。** 相手サイズの値域とキーの対応は `web/src/constants/label-keys.test.ts` の `KEY_MAPS` が見ている（**本サブで登録した。それまで 4 軸のうち相手サイズだけ外れていた**）。

---

## 7. ★設計書への波及（**編集していない・報告のみ**＝指示書 §4-5）

| # | 箇所 | 内容 |
|---|---|---|
| 1 | `DES-005:716` | 「相手の大きさ（3）」→ **（4）**。ヒット種別（4）→ **（8）** |
| 2 | `DES-005:544` | 重複メッセージの逐語例「相手の大きさ：中」→ **「標準」** |
| 3 | `DES-005:730` | Popover が開いている間は順送りを無効化する規定の**適用面が 1 つ増えた**（基本情報タブに info-mark が入ったため） |
| 4 | `DES-005` §5.7 | **注意書き / info-mark の追加そのもの**（★`CHANGE` の対象） |
| 5 | **`DES-005:1502`** | 「確定反撃の区分になっていない採用」の対象＝「`hit_type` が `normal` / `counter` / NULL」。**★実装を確認した結果、実体は否定形（`!punishHitTypes[...]`）であり、新 4 値もこのセクションに入る。** ⇒ 設計書の列挙が実装と食い違っている（段 1 §2.4-2 が「段 3 で実装を確認する」と宣言した項目の答え） |
| 6 | `SUPP-001:311` | **値と意味の対応表（唯一の正典）**。`opponent_size` 行が完全に失効。`hit_type` 行も 3 値のまま失効している |
| 7 | `DES-003:425` / `:466` | 列定義 ／ **コード値の採用方針**（★注意書きの根拠。逐語は §1.3） |
| 8 | `DES-003:1041` / `:1072` | **`OPEN-001`（状況コード値マスタ）は本サブが実質的に閉じた** |
| 9 | `DES-006` §2.7 | `hit_type` の許容値が 4 値のまま失効。**★`opponent_size` の enum 節は存在しない**（`hit_type` §2.7 が先例） |
| 10 | `DES-006:267` | 「`model.HitType*` 定数は参照 0 件」が失効（現在 8 行以上が参照） |
| 11 | `migrations/000001:69,70` | 列コメントが失効（★適用済み履歴のため編集していない。**`000081` のヘッダへ失効する旨と「値の正典は `internal/model/combo.go`」を明記した**） |

---

## 8. ★申し送り（設計卓・開発者の手番）

### 8.1 ★完了時に設計卓へ連絡すること（開発者の明示指示）

| # | 項目 | 開発者の逐語 |
|---|---|---|
| 1 | **持続当ての別コンボ扱い**（`SM-116`） | **「対象外。完了時に設計卓には連絡。phase4中には対応したいが、これは複雑な見込み。」** |
| 2 | **確定反撃ロジックの変更** | **「名前だけパニッシュカウンター。確定反撃のロジックは変更が必要で始動技がインパクトの時だけ使う」** ＋ **「別サブへ回す」** |

**★2 の現状**——本サブは**生成の対象外にすることだけ**を入れた（開発者確定＝「生成の対象外にするだけ入れる」）。**タブ分けは触っていないため、`drive_impact_punish_counter` は引き続き「区分を判定できない反撃」に出る。** 別サブが決めるべきは (a) どのタブに置くか (b)「始動技がインパクトのときだけ使う」制約をどこで掛けるか、の 2 点である。

### 8.2 `followup-backlog.md` 本表への登録候補（**★本表は 1 文字も編集していない**＝`D-382`。設計卓が畳むこと）

| # | 対象 | 現状の記述 | 実装後の実態 |
|---|---|---|---|
| 1 | `opponent-size-medium-to-standard` | 未着手 | **★解消**（DB 値変更 ＋ 両側同期 ＋ CSV 追随） |
| 2 | `opponent-size-large-variants-defer` | **要判断 / 未着手**（禁止の理由＝「判断が済んでいない」） | **★開発者が判断を供給し、`large` 新設とラベル変更を実施。禁止の前提が消滅している** |
| 3 | `csv-import-opponent-size-whitelist-stale` | 本文が「許容値が `small` / `medium` / `large` のまま」 | **★本文が失効。** 現在は `small` / `standard` / `large` |
| 4 | **新規候補** `opponent-size-legacy-medium-on-import` | — | **改名前に書き出した CSV を取り込むと `medium` が復活する。** 一覧・詳細では `labelFor` のフォールバックで**生の `medium` が画面に出る**。`000081` は 1 回きりの backfill なので拾わない（`migrations/000062` ヘッダが一般化した型と同じ）。**対処案＝取込時に `medium` → `standard` へ正規化する** |
| 5 | **新規候補** `check-enum-sync-misses-size-suffix` | — | `scripts/check-enum-sync.sh` の抽出パターンが接尾辞 `Category\|Status\|Type\|Code` にしか一致せず、`OpponentSize*` を拾わない（§6） |
| 6 | **新規候補** `opponent-size-constants-unused-in-production` | — | `model.OpponentSize*` の参照はテスト 1 本のみ。`rules.go` は依然リテラルの再定義。**ただし `DefaultOpponentSizes` は「本体と意図的にズレている」状態を表現する必要があるため、単純に `model.*` から組み立てると裁定の表現が失われる**（直すのは設計判断を伴う） |

### 8.3 ★開発者へ仰ぐこと

**★解消した（2026-09-02）。** 当初 `M27-01-hit-type-proposal.md` としたため `check-doc-inventory.sh` が「型に無いファイル」として挙げていたが、**既存の型に合わせて `M27-01-report.md` へ改名した**（TYPES の `^M[0-9]+-[0-9]+-report\.md$` ＝「サブ単位のレポート」）。

**★例外表（`EXCEPT`）へは足していない。** `CLAUDE.md` §8 の逐語＝「**★例外表（EXCEPT）へ足して黙らせるのは、開発者の承認を得たときだけにすること。検査を通すために足すと、本検査は存在しないのと同じになる**」。**⇒ 例外を足すのではなく、型に合わせるほうが正しい。**

根拠 3 つ ── (1) `phase4-overview` §8.5.2 が逐語で「**全数表など大きな成果物は `M{N}-{NN}-report.md` へ分けてよいが、完了報告は別に必ず作る**」と**その名前を名指ししている** ／ (2) `M26-01` が `M26-01-report.md` ＋ `M26-01-completion-report.md` の同じ 2 本立て ／ (3) 指示書 §3.1 が「**★名前は任意**」としている。

**⇒ `check-doc-inventory.sh` は「型に無いファイル 0 件」になった。開発者へ仰ぐことは残っていない。**

### ★あわせて確かめたこと ── 過去モックは例外表に 1 件も入っていない

`check-doc-inventory.sh:184` の走査は `find "$d" -maxdepth 1 -type f -name '*.md'` の 1 行であり、**4 ディレクトリ直下の `.md` しか見ない。** 過去モック 9 本（`docs/progress/M24-NN-mock/*.html`）はサブディレクトリ配下かつ `.html` なので**構造的に対象外**である。本サブの `M27-01-mock/m27-01-hit-type-and-size.html` も同じ理由で 1 件も鳴っていない。**⇒ モックのために例外表へ足された行は 0 件であり、本サブでも足していない。**


---

## 8.4 ★★開発者の手動確認（**設計卓へ連絡する前に**）

**★状態: 実施済み（2026-09-02・開発者）。** 開発者の報告＝**「A,B,Cの確認は成功。見た目も問題なし。★改名前に書き出した古い CSV があれば取り込むは残っていなかったので未実施。」**

**★1 件だけ「未実施」がある**——C-2-3（古い CSV の取込）。**対象の古い CSV が環境に残っていなかったため実施できなかった。** ⇒ **「確認して問題なかった」ではない。区別すること。**

### C-0. ★これだけは必ず（唯一、製造が確かめられなかったこと）

**実 DB の `opponent_size` の分布を、マイグレ適用の前後で数える。**

```sql
-- 適用前（アプリを起動する前に）
SELECT COALESCE(opponent_size,'(NULL)') AS v, count(*) FROM combos GROUP BY 1 ORDER BY 2 DESC;
```

| # | 見ること | 期待 | 済 |
|---|---|---|---|
| 1 | **想定外の値が無いか** | `medium` / `large1` / `large2` / `(NULL)` の 4 通りのみ。**★これ以外が出たら、そこで止めて連絡すること。** マイグレは `WHERE opponent_size='medium'` のみを触るので壊れはしないが、**画面に生の値が出る** | **☑ 済**（想定外 **0 件**） |
| 2 | `medium` の件数を控える | 適用後の照合に使う | **☑ 済（15 件）** |

**★★実測（2026-09-02 13:56:32 UTC・開発者。読み取り専用で実行）**

| 値 | 件数 |
|---|---:|
| `(NULL)` | **76** |
| `medium` | **15**（← 移行対象） |
| `large1` | **5** |
| `large2` | **1** |
| **合計** | **97** |

- **DB**: `/home/node/.local/share/combomgr/combomgr.db`（既定）。**マイグレは `version 80` / `dirty 0`**（＝`000081` 適用前）。DB への変更なし。
- **★想定外の値は 0 件。** 4 通りのみであり、**マイグレの前提（`medium` だけを触れば足りる）が実測で裏付けられた。** ⇒ 段 1 §6.3 と本報告 §12-1 の「確かめられなかった」は**解消した**。
- **★削除済み行を除外していないのは正しい照合である。** マイグレ `000081` も `WHERE opponent_size = 'medium'` だけで絞り、ゴミ箱行を除外しない ── 論理削除されたコンボは**復元されうる**ため移行対象に含めるのが正しい（`M23` のゴミ箱・復元）。**⇒ SQL の母集団とマイグレの母集団が一致している。**

```sql
-- 適用後（アプリを 1 度起動すると 000081 が自動適用される）
SELECT COALESCE(opponent_size,'(NULL)') AS v, count(*) FROM combos GROUP BY 1 ORDER BY 2 DESC;
```

| # | 見ること | **期待（★実測から確定した具体値）** | 済 |
|---|---|---|---|
| 3 | `medium` | **0 件** | **☑ 成功** |
| 4 | `standard` | **15 件**（適用前の `medium` と一致） | **☑ 成功** |
| 5 | `large1` | **5 件**（不変） | **☑ 成功** |
| 6 | `large2` | **1 件**（不変） | **☑ 成功** |
| 7 | `(NULL)` | **76 件**（不変） | **☑ 成功** |
| 8 | 合計 | **97 件**（不変。★行が増減しないこと） | **☑ 成功** |
| 9 | マイグレ版数 | `version 81` / `dirty 0` | **☑ 成功** |

> **★記録の限界**——上記 3〜9 は**開発者の「成功」報告に基づく**。適用前（1〜2）と違い、**raw な SQL 出力は本報告に提出されていない。** ⇒ 個々の件数を製造が観測したわけではない。

### C-1. 画面（`go run ./cmd/combomgr` ＋ `cd web && pnpm run dev`）

| # | 面 | 見ること | 済 |
|---|---|---|---|
| 1 | **コンボ編集 / ヒット種別** | **9 ボタン**（不問 ＋ 8 値）の折り返し。**★数字キー 1〜9 がすべて効くか**（上限 10 に対し余白 1） | **☑ 成功** |
| 2 | **コンボ編集 / 相手の大きさ** | **5 ボタン**と **ⓘ の位置**。ⓘ を押して「この区分で表現できない場合は、タグかメモを使ってください。」が出るか | **☑ 成功** |
| 3 | **★順送り** | Enter / ↓ で「相手の大きさ」へ来たとき、**フォーカスが ⓘ ではなくボタン群に載るか**。**★載らないと数字キーが死ぬ**（自動テストで固定したが実機で確認したい） | **☑ 成功** |
| 4 | **一覧の絞り込み** | ヒット種別に 8 値が出るか。**★横長さ**（`M27-03` `P4M-021` の判断材料。これ以上短くする必要があるか） | **☑ 成功** |
| 5 | **既存コンボの表示** | 詳細・比較で「中」が **「標準」** に、「大1」が **「大1(ザンギエフ等)」** になっているか | **☑ 成功** |
| 6 | **重複警告** | 保存前の 1 行に長いラベルが入ったときの読みやすさ（`… / 相手の大きさ：大1(ザンギエフ等) / …`） | **☑ 成功** |
| 7 | **PDF / 画像出力** | 「状況」行に長いラベルが入ったときの折り返し | **☑ 成功** |

### C-2. 往復（データが失われないこと）

| # | 手順 | 期待 | 済 |
|---|---|---|---|
| 1 | 新しいヒット種別でコンボを保存 → 一覧で絞り込む | 拾える | **☑ 成功** |
| 2 | CSV エクスポート → そのまま取込 | 値が失われない・警告が出ない | **☑ 成功** |
| 3 | **★改名前に書き出した古い CSV があれば取り込む** | **`medium` が復活し、画面に生の `medium` が出る**（＝レビュー 中-7。**寄せないと決めた**箇所）。**★実害の程度を判定してほしい。** ひどければ `opponent-size-legacy-medium-on-import` を起票する | **☐ 未実施（対象なし）**——開発者の逐語＝「**改名前に書き出した古い CSV があれば取り込むは残っていなかったので未実施**」。**★「確認して問題なかった」ではない。** 経路は構造的に残っており、**改名前にエクスポートした CSV が後から出てきた場合には再現する** |

### C-3. 番号の裏取り（設計卓の手番との境界）

| # | 確認 | 現状 | 済 |
|---|---|---|---|
| 1 | **マイグレ `000081` が本サブへ払い出されたか** | 実測（最大 `000080`）と `CHANGE-150` の「★次は `000081`」を根拠に採番した。**★払い出しの記録そのものはリポジトリに無い** | **☑ 成功** |
| 2 | **`M14-03f` / `M28-01` と衝突していないか** | 指示書 §4-6 が「同じ帯を使う見込みであり衝突しうる」と明記。**⇒ 次は `000082`** | **☑ 成功** |

---

## 9. ★併せて更新が要るもの（`E-114` / `D-277` / `D-297`）

| # | 項目 | 状態 |
|---|---|---|
| 1 | **消費したマイグレ連番** | **`000081` を消費した。⇒ 次の払い出しは `000082`。** ★`progress-log.md` の直近 2 件（`M14-RESEARCH-03` / `M26-01`）は「次の払い出しは `000081` のまま」と書いており、**その記述は本サブで失効した**。索引行で更新した。**★ボード §2.2 の「次に払い出す番号」は設計卓の手番** |
| 2 | **消費した CHANGE 番号** | **0 本**（本サブでは起票しない＝指示書 §4-6・`D-293`）。**次番は `151` の見込み**。★注意書き / info-mark の追加が `DES-005` §5.7 の変更にあたるため、設計卓が起票する際の対象に含めること |
| 3 | **版を上げた文書の参照元** | **該当なし**（`docs/design/` を 1 バイトも編集していない） |
| 4 | **`followup-backlog.md`** | **★編集していない**（`D-382`）。登録候補は §8.2 |

---

## 10. 検証

| 検証 | 結果 |
|---|---|
| `go test ./...` | **緑** |
| `cd web && pnpm test` | **緑（210 ファイル / 2343 件）** |
| `make e2e` | **緑（226 件）** |
| `cd web && pnpm run lint`（`tsc --noEmit`） | **緑** |
| `gofmt -l internal/` | **空** |
| `bash scripts/check-artifact-integrity.sh` | **緑**（★1 本目に実施） |
| `bash scripts/check-enum-sync.sh` | **緑**（ベースライン 24 → 28。理由は §6） |
| `bash scripts/check-import-order.sh` | **緑**（ベースライン 101 のまま） |
| `bash scripts/check-md-emphasis.sh` | **緑**（ベースライン 436 のまま） |
| `bash scripts/check-doc-refs.sh` | **緑** |
| `bash scripts/check-stop-discipline.sh` | **緑** |
| `bash scripts/check-progress-log-index.sh` | **緑** |
| `bash scripts/check-doc-inventory.sh` | **情報提供 1 件**（§8.3） |

### 10.1 破壊確認（3 件・いずれも赤くなることを確認済み）

| # | 壊したもの | 赤くなったもの |
|---|---|---|
| 1 | `000081` の `WHERE opponent_size = 'medium'` を外す | `TestRun_M2701_OpponentSizeMediumToStandardUpDown` の **5 assert**（`standard` 件数・`large1`・`large2`・NULL の巻き込み・down の戻り） |
| 2 | `InfoMark` を `hint` からボタン群の**前**へ移す | `ComboEditorBasicFields.test.tsx` の DOM 順 assert（`expected +0 to be truthy`） |
| 3 | `repository.go` の `hit_type` フィルタ句を無効化 | `TestList_FilterByHitType_M2701`（「1 件のはずが 8 件」） |

### 10.2 変更範囲

```
git diff --stat 78f4981
  42 files changed, 2162 insertions(+), 75 deletions(-)

git diff --stat 78f4981 -- internal/ web/ migrations/ scripts/
  39 files changed, 1071 insertions(+), 75 deletions(-)
```

**★新規追加した 7 ファイルの deletions はすべて 0**（`E-225`。`git diff --numstat --diff-filter=A` で確認）。

---

## 11. レビュー

| 項目 | 内容 |
|---|---|
| 報告書 | `docs/progress/m27-01-review.md`（★fresh subagent で実施。`fork` は使っていない） |
| 指摘件数 | **13 件（高 6 / 中 4 / 低 3）** |
| 採否 | **13 件すべて採用。★「高」の不採用は 0 件** ⇒ エスカレーションなし |
| 再レビュー往復 | **0 回**（上限 2 回） |
| トリアージの記録 | レビュー報告書末尾の「取り込み結果（自動トリアージ）」節 |

**★レビューは `ac006bd` 時点に対するもの。** 高-6 に対応する修正は、レビュー完了の直前に開発者判断を得て着手済みだった（報告書末尾の注記が指摘したとおり）。

**★レビューが検出した最も重い指摘（高-6）は、計画段階の私の想定が誤っていた点である。** 計画は「新値は述語に入らないので変換導線には自動的に出ない（安全側）」と書いたが、**実体は「対象外リスト」方式であり、未知値は既定で対象になる。** ⇒ 名前上すでにパニッシュカウンターであるものに PC 版生成の導線が開いていた。開発者へ照会し「生成の対象外にするだけ入れる」を得て是正した。

---

## 12. ★確かめられなかったこと

| # | 内容 | 理由 |
|---|---|---|
| 1 | ~~**`opponent_size` の DB 実データの分布**~~ **★2026-09-02 に開発者の実測で解消した** | 実測＝`(NULL)` 76 / `medium` **15** / `large1` 5 / `large2` 1（合計 97）。**想定外の値は 0 件**であり、マイグレの前提が裏付けられた。詳細は §8.4 C-0 |
| 2 | **`P4M-025` の源泉行の続きに何が書かれる予定だったか** | `phase4-memo.txt` の最終行で文が切れている（括弧が閉じていない）。**★推測していない。** 段 1 で開発者に提示し、「スタン」を採るという回答を得た |
| 3 | **`000081` が設計卓から実際に払い出されたか** | 実測で `migrations/*.up.sql` の最大が `000080` であること、`CHANGE-150` 通知書が「★次は `000081`」と書いていることを根拠に採番した。**★払い出しの記録そのものはリポジトリ内に無い。** 設計卓の確認が要る |
| 4 | ~~**実機での見え方**~~ **★2026-09-02 に開発者が確認して解消**（「見た目も問題なし」） | — |
| 5 | **改名前の CSV を取り込んだときの実害の程度** | **★対象の古い CSV が環境に残っていなかったため実施できなかった**（開発者報告）。**⇒ 「問題なかった」ではなく「確かめられなかった」である。** 経路は構造的に残っており、改名前にエクスポートした CSV が後から出てきた場合には再現する |

---

## 13. 制約事項

- 本報告はコード上で判定可能な範囲を対象とする。実機テスト・パフォーマンスは別途。
- **★`docs/design/` は 1 バイトも編集していない**（指示書 §4-5）。§7 は報告のみである。
- **★`followup-backlog.md` は 1 文字も編集していない**（`D-382`）。§8.2 は候補である。
- **★CHANGE 番号は自採番していない**（指示書 §4-6・`D-293`）。

---

*以上*
