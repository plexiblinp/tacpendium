# M30-01 レビュー報告書

| 項目 | 内容 |
|------|------|
| 文書ID | m30-01-review |
| 対象指示書 | `docs/instructions/M30-01-controller-move-surfacing.md` **v1.1.0** |
| チェックリスト | `docs/instructions/reviews/M30-01-review-checklist.md` v1.0.0 |
| 対象完了報告 | `docs/progress/M30-01-completion-report.md` v1.0.0 |
| 着手基点 / HEAD | `50235ce` / `8e43e62` |
| 作成日 | 2026-09-08 |
| 指摘件数 | **11 件**（重大 0 / 高 4 / 中 5 / 低 7 のうち、高・中を本文で詳述） |

---

## 総評

**本サブの中心である「実測が最初の成果物」は満たされている。** 実測はコードを読んだだけの記述ではなく、現物の述語を逐語で写したうえで seed 全 31 キャラ・2743 行に同じ述語を当てた算出であり、`moveSurfacing.roster.test.ts` で回帰として固定されている。当方で §2.3・§2.4・§2.7 と roster test の数値を独立に再計算したところ、`456 = 291+126+5+17+17` ／ `330 = 456−126` ／ 全 31 キャラの「出ない − 区分B」がテストの固定値と 1 件のずれもなく一致した。**チェックリスト §6 の重大 8 件は 1 つも当たらない。** 既知の設計（`CHANGE-166`）を不具合として直しかけておらず、むしろ「`_od` だけが押せる 95 件・24 キャラ」という新事実を出して設計卓への請求に畳んでいる。順序（実測 → 規則 → タブ）もコミット `105e273` が docs のみであることで機械的に確認できる。

**いっぽう、成果物の外側の締めが 2 か所で落ちている。** `docs/progress/progress-log.md` への `### M30-01` 追記が無く（`check-progress-log-index.sh` が実際に赤）、完了報告 §7-6 は「追記した」と書いているが事実と異なる。あわせて §4.4 が「行き先は §7 の横断課題へ 1 行残す」と宣言した `make e2e` の失敗 1 件は、§7 にも progress-log にも実在しない。**「失敗を切り分けたのに、切り分けた結果の置き場が無い」状態である。**

**コード側では「実装が変わったのに記述が旧のまま」の型が 2 件残った。** `moveSurfacing.ts` の冒頭注記が実在しないテストを安全網として挙げており、`VirtualController.tsx` のヘッダコメントは 4 タブ時代のままである。本プロジェクトの較正でこの型は「高」であり、いずれもテスト・lint・型検査では検出できない。

**加えて、SM-100 のトグルには 1 つ実害のある挙動がある**——技を選んだ状態でトグルを ON にすると、セレクトは空欄になるのに `selectedValue` が残り、［追加］が有効なまま「画面に出ていない技」を積む。両面（コンボ／セットプレイ）に同型で入っている。

---

## 設計準拠性レビュー結果

### 束 A — 実測（★★最初の成果物）

| # | 見ること | 判定 | 所見 |
|---|---|---|---|
| **1-1** | キャラ単位・技単位で実測されているか。コード読みと DB 算出を区別しているか | **◎** | §2 冒頭が「タブ側の述語は現物のコードから逐語で写し、出ない技の全数は `character_data/*.csv` 全 31 キャラ 2743 行に同じ述語を当てて算出した」と明示。§2.2 は行番号付きで条件式を写しており、推測で書かれた規則ではない。BE 側（`internal/service/inputresolve/service.go` の 3 段フィルタ・`internal/moveindex`）まで遡っている点も適切。当方で `internal/api/move/dto.go` を実査し、§2.9 の「`is_derived` は DTO に無い」も事実であることを確認した |
| **1-2** | §0.2 の既知の設計で説明が付く分が分けて示されているか | **◎** | 区分 A（291 件）を「意図的に除いている（`CHANGE-166`＝既知の設計）」として他区分と明確に分離。混ざっていない |
| **1-3** | 説明が付かない分の原因が特定されているか | **◎** | 「理由が読めない」区分は 0 件。区分 C（`ryu/rush_axe_kick`）は「規則が正しく働いた結果としてデータ側の命名ずれで落ちている」と結論づけ、設計変更ではなくデータ是正候補として §6-6 へ回している。**「規則で説明が付く ≠ 望ましい」を §3.2 で書き分けている点は特筆に値する** |
| **1-4** | ジェイミーが実測の対象に入っているか | **◎** | §2.4 に 27 行の全数。roster test で顔ぶれごと固定（13 行）。E2E も jamie を使う。当方で `character_data/jamie.csv` に `tenshin` / `the_devil_inside` / `drink_level_3_hermits_elbow` / `full_moon_kick` / `freeflow_strikes_1hit_light` / `bitter_strikes` が実在することを確認した |
| **1-5** | 実測が規則の変更より前に報告されているか | **◎** | コミット `105e273` は `docs/progress/M30-01-completion-report.md` のみ 209 行追加でコードは 1 行も動いていない。順序違反なし |

### 束 B — 射程 5 件

| # | 見ること | 判定 | 所見 |
|---|---|---|---|
| **2-1** | `P4M-017`（技の表示基準） | **○** | 実測を踏まえたうえで「規則そのものは 1 行も変えず、述語を 1 本へ集約する」に着地。`DES-004` §2.1 / `DES-005` §6.4 の必殺技クイック入力の規則は不変であり、変更は §6-1 の請求として設計卓へ回している（`D-293` 準拠）。**減点は実装ではなく注記**——高-2 / 高-3 参照 |
| **2-2** | `P4M-007`（ターゲットコンボタブ）と `DES-004` §2.1 の `tc_<番号>` の扱い | **◎** | `DES-004` §2.1 は「段数付き行は所属セグメントの code で採番／`tc_<番号>` は FR703 手動分類用に温存／`category=target_combo` の付与は入力支援ツールで人が行い本体は取込値を信頼」と定めており、**母集団を `category` で採った本実装と食い違わない**。`is_derived` を使わない理由（`M19-DESIGN-08` §0 の述語は「除く」側であり、裏返すと 126 行中 117 行が入力不能になって `SM-135` と真逆になる）が §2.9・§3.3・コード注記の 3 か所で一貫している |
| **2-3** | `P4M-008`（非表示技・キャラ別技タブ）。「非表示」の定義が実測に基づいているか | **○** | (a) の母集団を `isControllerSurfaced` が false を返す行**そのもの**とし、「非表示」を別途定義していない。定義が 2 つになるのを構造で防いでおり、`surfaceBuckets` と未掲載タブが同じ関数から出ることをテストが固定している。(b) は読み2 を開発者が選んだうえで、判定軸の限界（0 件の 5 キャラ・偽陽性 4 件）を実測して逐語注記に残している。**減点は中-3（`characterStateMoves` の直接テストが無い）** |
| **2-4** | `SM-100`。ファミリー UI に載らない技を省いていないか | **△** | **チェックリスト §6-3 には当たらない**——述語は `isControllerSurfaced` の裏返し 1 本であり、区分 A（`sonic_break`）が残ることを Vitest（`RecipeBuilder.test.tsx`）と E2E（「点辰」）の両方が名指しで主張している。既定 OFF も守られている。**ただし高-4（選択状態のデスync）・中-1（i18n 未対応）・中-2（文言と挙動の不一致）・中-4（セットプレイ面が無観測）で減点** |
| **2-5** | `SM-135` | **◎** | `P4M-007` に畳まれ、別部品は作られていない。E2E も「ターゲットコンボタブから 1 ステップ積める」で射程どおり |

### 束 C — 触ってはいけない面

| # | 見ること | 判定 | 所見 |
|---|---|---|---|
| **3-1** | `M24-12` の入力の骨格（ボタン群・数字キー・順送りの停止点） | **◎** | `git diff --numstat 50235ce HEAD` の 16 ファイルに `OptionButtonGroup` / `fieldSequence` / `ComboEditorBasicFields` / `HitBoxLayout` / `SystemRow` / `SpecialMovePanel` はいずれも現れない。当方で `OptionButtonGroup.tsx` を実査し、数字キーが要素スコープの `onKeyDown`（window 購読ではない）であることを確認した。⇒ 完了報告 §5 の「タブを 4 → 7 枚にしても割当は動かない」は事実 |
| **3-2** | `data-testid` と数字キーの割当に手を出していないか | **○** | 既存 id は 1 つも変わっていない（`recipe-tab-normal` / `recipe-dir-*` / `recipe-direct-*` / `recipe-system-*` はすべて不変）。新タブ 3 枚には既存規約に沿った新規採番のみ。**「`M30-02` の射程は既存の割当を変えること」という読みを §5 で明示して差し戻しの可能性まで書いている**のは正しい返し方 |
| **3-3** | `M27-02b` の必須・任意の印 | **◎** | 1 行も触れていない（差分に該当ファイルなし） |
| **3-4** | `DES-004` §2.1 / `DES-005` の必殺技クイック入力の規則そのものを変えていないか | **◎** | `parseSpecialCode` を再利用しているだけで規則は不変。`isOnSpecialTab` が `deriveSpecialFamilies` と同じ 2 条件であることをテストが構造で主張している（`moveSurfacing.test.ts:153`）。変更が要る箇所は §6-1 の請求として止めている |

### 破壊確認（チェックリスト §4）

| # | 見ること | 判定 | 所見 |
|---|---|---|---|
| **4-1** | 規則を 1 か所壊してテストが赤になること。「要素の存在だけ」を見ていないか | **◎** | 破壊 A（`isOnSpecialTab` から `parseSpecialCode` の条件を外す）で Vitest 13 failed / E2E 2 failed、破壊 B（`isOnTargetComboTab` を常に false）で Vitest 13 failed。**落ちたのは並びと母集団を見るテストであり、存在だけを見るテストは落ちなかったことまで書かれている**。さらに「`tsc` の赤は `TS6133` によるものであり、条件を残したまま無効化すれば型検査は緑になる。⇒ その形でも改めて確認した」と、**偽の安心を与える経路を自分で潰している**。復元後の全数緑も記載 |
| **4-2** | 壊す対象区分に §0.2 の族が含まれているか（`M-139`） | **◎** | 破壊 A の対象がまさに区分 A（強度接尾辞を持たない必殺技）であり、赤になったテストの内訳に「区分 A の 2 本」「未掲載タブ 2 本」「`SM-100` 1 本」が名指しで挙がっている。E2E の失敗も「未掲載タブから `tenshin` が消える／`SM-100` で『点辰』が省かれてしまう」——**入力手段が消える方向の退行を捕まえる形になっている** |

### テストの妥当性（チェックリスト §5）

| # | 見ること | 判定 | 所見 |
|---|---|---|---|
| **5-1** | `go test ./...` ／ `pnpm test` を出力で判定しているか | **○** | §4.2 に件数付きで記載（219 files / 2557 passed）。**ただし `make e2e` は 257 passed / 1 failed であり「全数緑」ではない。** 着手基点へ戻して同一エラーを再現させる切り分けは適切だが、その結論の置き場が実在しない（高-1） |
| **5-2** | `make e2e-only P=<パターン>` を使っているか | **◎** | §4.2 に `make e2e-only P=m30-01` と記載。`playwright` の直叩きは差分・報告のどちらにも現れない |
| **5-3** | 実測の結果がテストとして固定されているか | **◎** | `moveSurfacing.roster.test.ts` が seed 実データで 7 本。総行 2743 / 未掲載 330 / category 別内訳 / ターゲットコンボ 126 / jamie の顔ぶれ 13 行 / `_od` のみ 95 件 / **キャラごとの未掲載件数 31 個**を固定。実測がそのまま回帰になっている |
| **5-4** | キャラ横断で試されているか | **◎** | roster test が 31 キャラ全数。ファイル数が 31 であること自体も 1 本のテストで固定してある |
| — | 出ない側を理由の区分ごとに 1 件ずつ（指示書 §2.6-2） | **◎** | `moveSurfacing.test.ts` のフィクスチャが区分 A / C / D / E を 1 件ずつ持ち、それぞれ `isControllerSurfaced` が false であることを主張。区分 B は本サブでタブを得たため出る側へ移っており、これも正しい |
| — | 純粋関数の単体テスト（`CLAUDE.md` §5） | **△** | `characterStateMoves` に直接の単体テストが無い（中-3） |

**チェックリスト §6「重大な問題」8 件はいずれにも当たらない。** §7「軽微」に該当するものも見当たらない（説明の付かない分は 0 件、タブ並び順は逐語どおり、`SM-135` は畳まれ、実測の粒度は要求を上回る）。

---

## 設計準拠性以外の指摘事項

### 高-1（プロセス）: `progress-log.md` への追記が無く、完了報告の記述と実際が食い違う。切り分けた E2E 失敗の行き先も実在しない

- **事実**: `grep "^### M30" docs/progress/progress-log.md` は 0 件。`bash scripts/check-progress-log-index.sh` を実走した結果は **`NG 作業 ID m30-01 が docs/progress/progress-log.md に現れない` / 結果: 違反 1 件**。
- **にもかかわらず完了報告 §7-6 は「追記した（Phase D）」と断定している。** 指示書 §3-7 ／ 完了条件 8 ／ `CLAUDE.md` §8 未達。
- **さらに連鎖がある**: §4.4 が `make e2e` の失敗 1 件（`m24-12-editor-rebuild.spec.ts:31`）について「⇒ 行き先は §7 の横断課題へ 1 行残す。本サブでは直さない」と宣言しているが、**§7 は「併せて更新が要るもの」の 6 行だけで、この 1 行は無い**。progress-log にも無い。⇒ **着手基点から赤だったと切り分けた貴重な事実が、どこにも記録されないまま消える。**
- 完了報告 §4.2 の常設検査リストにも `check-progress-log-index.sh` が挙がっていない。**回していれば赤に気づけた。**

### 高-2（撤回済み・失効した記述）: `moveSurfacing.ts` が実在しないテストを安全網として挙げている

```
web/src/features/combo/moveSurfacing.ts:33-36
// 通常技タブの入力面が持つ全組み合わせ(方向 9 × 強度 3 × ボタン 2)。
// ★HitBoxLayout の DIRECTIONS / ATTACKS と同じ母集合である。片方だけ増えると
//   「押せるのに未掲載扱い」または「未掲載なのに押せる」がずれるため、
//   moveSurfacing.test.ts が両者の一致を検査する。
```

- **`moveSurfacing.test.ts` に両者の一致を検査するテストは 1 本も無い**（全文確認）。
- **そもそも書けない**: `HitBoxLayout.tsx` の `DIRECTIONS` / `ATTACKS` は module-private の `const` であり `export` されていない。`grep -rn "DIRECTIONS\|ATTACKS" web/src/features/combo` の結果、両者を参照しているのは `HitBoxLayout.tsx` 自身と、**同じ値を独立に書き直した** `moveSurfacing.ts:37-39` と `inputResolutionStage2.test.ts:53` だけである。
- **帰結**: 母集合が 2 か所（3 か所）に重複して存在し、片方だけ増えても何も鳴らない。**注記はその危険を正しく述べているのに、対策として挙げた検査が無い。** ⇒ 次の担当は「検査がある」と読んで安心する。本プロジェクトの較正でこの型は「高」。
- **なお同ファイル 198-199 行の「同じ 2 条件であることは `moveSurfacing.test.ts` が検査する」は事実である**（`moveSurfacing.test.ts:153`）。**片方だけが真であるため、読み手は両方を真だと信じる。**

### 高-3（撤回済み・失効した記述）: `VirtualController.tsx` のヘッダコメントが 4 タブ時代のまま

```
web/src/features/combo/components/VirtualController/VirtualController.tsx:65-69
// 仮想コントローラ = カテゴリタブ式クイック入力面(M15-03、FB④)。
// 通常技=段階2(解決表)＋段階1 一様フォールバック(方向パッド×ボタン、M17-03)/ 特殊技=直接指定
// / 必殺技=ファミリー＋弱中強＋OD4種 / SA=直接指定。
// システム行(DI/DP/投げ/ラッシュ/削除)はタブ外に常設(既存挙動温存・E2E 非回帰)。
// 「全技から選ぶ」網羅入力は RecipeBuilder / SetupRecipeEditor のプルダウン(残置)側で担保する。
```

- **同じファイルが直下で 7 枚を描いている**のに、ヘッダはタブを 4 枚として列挙している。個々の `TabsTrigger` の直前には丁寧な逐語注記が入っているのに、**ファイルを開いて最初に読む記述だけが旧のまま**である。
- 最終行も不正確になった。未掲載タブができたことで、**「全技から選ぶプルダウン側で担保する」は唯一の担保ではなくなった**。完了報告 §6-3 は同趣旨の失効を `CHANGE-166` の帰結文について正しく検出しているが、**同じ失効が自分のコードにも起きていることは拾えていない。**

### 高-4（実害）: `SM-100` の ON で、画面に出ていない技が［追加］で積める

- **再現**: 全技一覧を展開 → 技を 1 つ選ぶ（`selectedValue` に `moveId` が入る）→「ボタンで入力できる技を省く」を ON にする。
- `grouped` は `visibleMoves` から作り直されるため、その技の `<option>` は消える。**controlled な `<select>` の `value` に一致する option が無くなると `selectedIndex = -1` になり、セレクトは空欄を表示する。** しかし React の state `selectedValue` は空にならない。
- `RecipeBuilder.tsx:322` の `disabled={selectedValue === ""}` は false のままなので、**［追加］は有効なまま**である。押すと `handleAdd()` が `Number(selectedValue)` をそのまま積む。
- **⇒ 利用者が「何も選んでいない」と見ている状態で、直前に選んでいた技がステップとして入る。** これは編集中のコンボへ書き込まれる値である。
- **同型が `SetupRecipeEditor.tsx` にもある**（`selectedValue` 89 行 / `disabled={selectedValue === ""}` 288 行）。**2 面とも同じ**。
- `filterCategory` も同型のずれを起こす（ある区分の技が全部省かれると、その区分の `<option>` が消えて絞り込みセレクトが空欄になり、技セレクトが 1 件も出なくなる）。こちらは復帰可能なので実害は小さい。
- **修正は軽い**: トグルの変化時に「いま選んでいる値が `visibleMoves` に居なければ `setSelectedValue("")`（`filterCategory` は `"all"`）へ戻す」の 1 か所で済む。既定 OFF のため既定経路では踏まないが、**トグルは利用者が明示的に押すものであり、押した直後がまさにこの状態である。**

### 中-1: 新規トグルの文言が i18n を通っていない

- `ControllerInputOmissionToggle.tsx:29-30` が `ボタンで入力できる技を省く` / `（{omittedCount} 件）` を日本語ハードコードしている。`useTranslation` を使っていない。
- **同じサブで `ja.json` / `en.json` の両方へ 6 キーを足している**（タブ名・空状態）のに、この 1 文だけ en ロケールで日本語のまま出る。
- 周囲の `RecipeBuilder.tsx` が未 i18n（「区分で絞り込み:」等）である事情は理解できるが、**本サブが新規に作った部品であり、`VirtualController` 側は `M24-07`/`CO-020` で i18n 化済みである**。同じ画面の上下で流儀が割れている。

### 中-2: トグルの文言が、同じサブで足した未掲載タブと矛盾する

- 文言は「**ボタンで入力できる技**を省く」。しかし本サブで未掲載タブができた結果、**残る側の技もボタンで押せる**（`recipe-hidden-{code}`）。
- 実際に表しているのは「入力できるか」ではなく「**通常技・特殊技・ターゲットコンボ・必殺技・SA・共通技行のいずれかに載っているか**」である。
- 利用者から見ると「省く」を押しても押せる技が残るため、**トグルが何をしたのか説明できない**。コード側は `isControllerSurfaced` の非対称（未掲載タブは経路であって掲載ではない）を注記で正しく説明しているが、**その説明は画面に出ていない**。
- 較正上「旧の記述が残った」型ではなく「新しい文言が同じ手番の別変更と食い違った」型のため中に置くが、**利用者に見える取捨の説明**であり `roles-and-routing` のハード列（ユーザー体験に影響する選択）に触れうる。⇒ 文言案を出して開発者に選ばせるのが筋。

### 中-3: `characterStateMoves` に直接の単体テストが無い（`CLAUDE.md` §5）

- `moveSurfacing.ts` の export のうち、`characterStateMoves` だけが `moveSurfacing.test.ts` に 1 件も現れない。観測は `VirtualController.test.tsx` のコンポーネントテスト 4 本のみ。
- **`CLAUDE.md` §5 は「フロント純粋関数のテストは必須」**であり、かつ本関数は本サブで最も判定が緩い軸（`m.code.includes(code)` の**アンカー無し部分一致**）を持つ。
- **実測で分かっている境界がテストに載っていない**: (a) `stateCodes.length === 0` の早期 return、(b) 5 キャラで 0 件になること、(c) SA 行を拾う偽陽性 4 件を**意図的に落とさない**という判断。**(c) は判断であって偶然ではないのに、テストが無いので次の担当が「バグだ」として落としても何も鳴らない。**

### 中-4: `SM-100` はセットプレイ面が完全に無観測

- `SetupRecipeEditor.test.tsx` の差分は `useCommandIndex: () => ({ data: undefined })` のスタブ 3 行だけで、トグルの主張は 1 本も無い。E2E もコンボ側のみ。
- `ControllerInputOmissionToggle.tsx:3-4` は「**2 面が同じ形を出す。部品を 1 つにしておかないと、片方だけ文言や既定が変わって静かにずれる**」と書いている。**その「静かにずれる」を検出する観測が、まさに片面に無い。**
- 完了報告 §3.5 も「対象は 2 面」と書いているが、§4.2 の新規テスト内訳に setup 面は 1 件も無い。

### 中-5: `M24-12` の骨格を守る回帰テストは、同居時の競合を見ていない

- `VirtualController.test.tsx:438-455` は `VirtualController` 単体を描画し、タブへフォーカスを置いて `1`〜`5` を押しても既定タブのままであることを主張している。**主張自体は正しい**（Radix Tabs は矢印キー駆動で数字キーを購読しない）。
- ただし `M24-12` の数字キーの持ち主である `OptionButtonGroup` は**この描画に存在しない**。⇒ 「実画面で両者が同居したときにタブが数字キーを奪わない」ことは示せていない。
- 実装を読む限り `OptionButtonGroup` の `onKeyDown` は要素スコープであり実害は無いと判断できるが、**テストが主張していることと、完了報告 §5 が主張していること（「タブを 4 → 7 枚にしても割当は動かない」）の射程が一致していない。**

---

## 推奨修正（優先度別）

### 高（M30 完了前に修正必須）

1. **`docs/progress/progress-log.md` へ `### M30-01` の索引行を追記する**（高-1）。あわせて、**§4.4 で切り分けた `make e2e` の失敗 1 件（`m24-12-editor-rebuild.spec.ts:31`・着手基点から赤）を横断課題として 1 行残す**。完了報告 §7-6 の「追記した」は追記が入ってから真になる。**`bash scripts/check-progress-log-index.sh` を回して緑を確認すること**（現状は赤）。あわせて完了報告 §4.2 の常設検査リストへ同スクリプトを加える。
2. **`moveSurfacing.ts:33-36` の注記を事実へ直す**（高-2）。取りうる形は 2 つ——(a) `HitBoxLayout` の `DIRECTIONS` / `ATTACKS` を `export` し、`moveSurfacing.test.ts` で `ALL_DIRECTIONS` / `ALL_STRENGTHS` / `ALL_BUTTONS` との一致を実際に検査する（注記が言っているとおりの形にする）。(b) 検査を作らないなら、**「検査する」を消して「両者は独立に定義されており、片方だけ増えても検出されない」と正直に書く**。★(a) を推す——母集合が 3 か所（`HitBoxLayout` / `moveSurfacing` / `inputResolutionStage2.test.ts`）に重複しており、本サブが増やした 1 か所は「未掲載か否か」の判定そのものを決めるため、ずれたときの被害が最も大きい。
3. **`VirtualController.tsx:65-69` のヘッダコメントを 7 タブの as-built へ直す**（高-3）。最終行「全技から選ぶ網羅入力はプルダウン(残置)側で担保する」も、未掲載タブが第 2 の担保になったことを書き足す。**完了報告 §6-3 が `CHANGE-166` の帰結文について検出したのと同じ失効が、自分のコードにも起きている。**
4. **`SM-100` のトグル切替時に、消えた選択を残さない**（高-4）。`useControllerInputOmission` 側に「`visibleMoves` に居ない選択を通知する」口を出すか、`RecipeBuilder` / `SetupRecipeEditor` の両方で `omitSurfaced` の変化時に `selectedValue`（および空になった `filterCategory`）を既定へ戻す。**2 面に同じ形で入れること**——片面だけ直すと `E-76` の型になる。あわせて「トグル ON 後に［追加］が前の選択を積まない」ことを主張するテストを 1 本足す（現状の 3 本はいずれも選択を持たない状態で切り替えている）。

### 中（M31 着手と並行可）

5. **トグルの文言を i18n へ移す**（中-1）。`ja.json` / `en.json` に既に足した `controller.panel.*` と同じ名前空間へ置くのが自然。**★`M29-02` の教訓（同名キーが複数の名前空間に在る JSON の機械編集）に留意し、名前空間ごとアンカーして追加すること。**
6. **文言そのものを見直す**（中-2）。「ボタンで入力できる技を省く」は未掲載タブの追加で不正確になった。案として「タブに載っている技を省く」「未掲載の技だけを表示」等を提示し、**利用者に見える取捨であるため開発者に選ばせる**（`roles-and-routing` ハード列）。実装は変えず文言だけで済む。
7. **`characterStateMoves` の単体テストを足す**（中-3）。最低限 (a) `stateCodes` 空 → 空配列、(b) 部分一致で拾うこと、(c) **SA 行の偽陽性を意図的に落とさないこと**（判断をテストで固定する）。(c) が無いと、次の担当が「無関係な SA が混ざっている」として静かに落とす。
8. **セットプレイ面の `SM-100` に観測を付ける**（中-4）。`SetupRecipeEditor.test.tsx` に「トグルが出る／ON でボタンで押せる技が省かれる／未掲載の技は残る」の 3 本、または E2E を 1 本。**部品コメントが「静かにずれる」と警告している面である。**
9. **完了報告 §5 の主張と回帰テストの射程を揃える**（中-5）。`OptionButtonGroup` を同居させた描画でタブが数字キーを奪わないことを 1 本足すか、§5 の記述を「タブ自体が数字キーを購読しないこと」に限定する。

### 低（将来対応）

10. **完了報告 §4.1 の変更統計が古い**。記載は `209` 行 / `1503 insertions` だが、HEAD 時点の実測は `425` 行 / **`1719 insertions, 11 deletions`**。新規 6 ファイルに deletions が無いという主張（`E-225` 対応）自体は `git diff --numstat` で当方も確認でき事実である。数値だけ差し替えれば足りる。
11. **`moveSurfacing.roster.test.ts` が seed CSV の実値へ 5 か所ハードコードしている**（2743 / 330 / 291・5・17・17 / 126 / 95 / 31 キャラの表）。意図（「この前提が崩れたら件数がずれる。それが検出したい変化そのものである」）は明記されており妥当だが、**DLC キャラ追加や seed 是正のたびに手で数え直す運用が発生する**。将来 seed が動く手番では、更新手順を完了報告に 1 行残す運用を推奨。
12. **`ControllerInputOmissionToggle` が素の `<input type="checkbox">`**。周囲の同種 UI は shadcn `Switch`（`recipe-unique-rush-toggle` 等）。E2E の `check()` / `not.toBeChecked()` が使えるという実利はあるが、見た目の流儀が割れる。
13. **`useControllerInputOmission.ts:33` の doc「0 のときはトグルを出さなくてよい判断材料」に対し、呼び出し側は常に描画している**。判断材料という書き方なので誤りではないが、`omittedCount === 0` のときに「（0 件）」と出るのは情報として無意味。
14. **`DirectSpecPanel.tsx:11` の注記が古い。** 同行は「SA タブはラッシュ非対象のため rushOn を渡さない」とだけ書いており、新 3 タブ（ターゲットコンボ／未掲載／キャラ固有状態）に触れていない。実装上はいずれも渡していないので挙動は正しいが、列挙が古い。
15. **コミット `6ec2376` の時点で `isOnTargetComboTab` が true を返すのにタブは未実装**（次コミット `507f59b` で解消）。同コミットで入った roster test のコメント「ターゲットコンボタブ(P4M-007)を足した分 126 行が面を得て 330 になった」も、そのコミット単体では未成立。指示書 §2.7 の「2〜5 がそれぞれ単独で revert できる形」は逆順 revert では成り立つため実害は無いが、**中間コミットが一時的に「述語は載ると言うが面が無い」状態を作っている**。
16. **`docs/design/testid-convention.md` の付与済み一覧更新を §6-5 で設計卓へ回した判断は正しい**（`CLAUDE.md` §8 ／ 指示書 §4-9 が優先）。なお当方の実査では、**同一覧には既存の `recipe-tab-*` / `recipe-direct-*` / `recipe-dir-*` も載っていない**（`grep "recipe-"` は 3 行のみ）。本サブ由来ではないが、設計卓が畳むときに併せて棚卸しする価値がある。

---

## 良かった点

- **実測が本物である。** 「コードを読んだだけの記述と、DB から算出した記述を区別すること」（チェックリスト §1-1）に対し、§2 冒頭で明示的に区別を宣言し、述語は行番号付きの逐語、件数は seed 全数の算出、そして**その算出を回帰テストとして固定**という三段構えになっている。当方が §2.3 / §2.4 / §2.7 と roster test の数値を独立に再計算したところ、**列の合計（A 291 / B 126）も、31 キャラすべての「出ない − B = テストの固定値」も、1 件のずれもなく一致した。** 数表を手で組んだのではなく機械で出したことが、数値の整合そのものから裏付けられる。
- **既知の設計を直しかけていない。** それどころか `CHANGE-166` が名指ししていた 2 件（`sonic_break` / `quick_burn`）が**同型 95 件・24 キャラのうちの 2 件にすぎない**ことを突き止め、「ファミリー名は出るが押せるのは OD だけ」という利用者に見える形まで言語化した。**これは設計卓が持っていなかった材料であり、本サブの最大の副産物である。**
- **「規則で説明が付く ≠ 望ましい」を §3.2 で書き分けた。** 区分 C（`ryu/rush_axe_kick`）を「規則が正しく働いた結果」として設計変更ではなくデータ是正候補へ回している。説明が付いたことで満足せず、望ましさを別軸で評価している。
- **述語を 1 本にする設計判断が構造的である。** `DirectSpecPanel` から `categories: string[]` を取り上げて `list: Move[]` に変えたのは、「何を出すか」が呼び出し引数へ散る経路そのものを塞ぐ変更であり、指示書 §2.5-2 の「2 か所に書かない」を規律ではなく構造で満たしている。
- **未掲載タブの母集団を `isControllerSurfaced === false` **そのもの**にした**のも同型の判断で、「非表示」の第 2 定義が生まれる余地を消している。しかも「未掲載タブに載ることは判定を変えない」という非対称を、**なぜそうしないと全技一覧が空になるかまで含めて**注記している。
- **破壊確認が「偽の緑」まで潰している。** 破壊 A で `tsc` が赤くなったことを成果にせず、「条件を残したまま無効化すると型検査は緑になる。⇒ その形でも改めて確認した」と、**自分の破壊のやり方が甘くないかを自分で疑っている。** `M-139` への対応としても、対象区分が §0.2 の族そのものである。
- **`is_derived` を使わなかった理由が、`M19-DESIGN-08` §7 の列追加審査規約まで遡って書かれている。** 「同じ軸を裏返すと `SM-135` と真逆になる」「事実列に第 4 の用途を載せることになる」の 2 段で、**使わない判断を積極的に根拠づけている。** 指示書 §2.2-2 が求めた「在る列で導けるかを先に確かめる」に正面から答えている。
- **`CHANGE-166` の帰結文が失効したことを自分で見つけて §6-3 に置いた。** レビュー較正が「高」に置く型を、レビューを受ける前に自分で拾っている。**この目が自分のコードのヘッダコメント（高-3）にも向いていれば完璧だった。**
- **`M30-02` との境界の読みを書いて、差し戻しの可能性まで明記している**（§5 の `data-testid` ／ §6-7）。射程の解釈を自分で確定させず、判断の材料として置いている点が正しい。
- **`SM-100` の保持先をブラウザストレージにしなかった判断と、その根拠**（台帳へキーを足さずに済む・面を離れると既定へ戻るので「消えたまま戻せない」状態にならない）が明快。当方で `bash scripts/check-browser-storage-keys.sh` を実走し、台帳 11 件 / 本番 10 件で一致・違反なしを確認した。

---

## 制約事項

- 本レビューはコード上で判定可能な範囲のみ対象。実際の動作確認・パフォーマンス・実機テストは別途実施が必要。
- **本レビューで実走して確認した検査**: `check-progress-log-index.sh`（**赤・違反 1 件**）／ `check-import-order.sh`（緑・101/101）／ `check-md-emphasis.sh docs/progress/M30-01-completion-report.md`（緑・0 行）／ `check-browser-storage-keys.sh`（緑）／ `check-doc-refs.sh`（緑）。**`check-artifact-integrity.sh` ／ `check-stop-discipline.sh` ／ `check-enum-sync.sh` ／ `check-doc-inventory.sh` ／ `go test ./...` ／ `pnpm test` ／ `make e2e` は再実行していない**（完了報告 §4.2 の記載を採った）。
- **不明: 開発者判断 3 件（`P4M-008` (b) の読み2 ／ `SM-100` の既定 OFF ／ ファミリー UI 拡張を請求へ回す判断。いずれも 2026-09-08）について、リポジトリ上に一次資料が無く、完了報告の記述以外に照合できない。** 指示書 §7 が「実装の途中で返すもの」と定めた運用どおりであれば問題ないが、レビューとしては裏取りできない。
- **不明: `make e2e` の失敗 1 件が着手基点でも赤であるという §4.4 の切り分けは、作業ツリーを一時的に戻して確認したと記載されているが、その痕跡は git 履歴に残らないため再現できない。** 記述の手順（`git show 50235ce:<path>` で戻す／新規 6 ファイルを削除／同一エラー）は妥当であり、疑う理由は見当たらない。
- 本レビューはコードを 1 行も変更していない。作成したファイルは本報告書のみ。

---

*以上、M30-01 レビュー報告書。* **★★本サブの核（実測を先に出し、既知の設計を直さず、規則を 1 本にする）はすべて満たされている。高 4 件はいずれも「成果物の締め」と「記述の鮮度」であり、設計の骨格に触れる修正ではない。**

---

## 取り込み結果（自動トリアージ）

**トリアージ実施日**: 2026-09-08 ／ **実施**: 製造担当 Claude Code（`/implement_plan_full` Phase C）
**★「高」指摘の不採用は 0 件である**（4 件すべて採用）。**⇒ 安全弁（開発者エスカレーション）の発動は無い。**
**再レビューの往復**: 0 回（初回レビューのみ。停止規律の上限 2 回に達していない）

### 高（4 件・すべて採用）

| # | 指摘 | 採否 | 対応と理由 |
|---|---|---|---|
| 高-1 | `progress-log.md` へ `### M30-01` の追記が無い（`check-progress-log-index.sh` 赤）。完了報告 §7-6 の「追記した」が事実と異なる | **採用** | Phase D を実行して追記。横断課題 8 件（`make e2e` の失敗 1 件の切り分けを含む）を置いた。**同スクリプトの緑を確認。**完了報告 §7-6 も「レビュー時点では未追記だった」と訂正した。**★指摘のとおり、`D-510` と同型の誤り（未実施のことを断定）を §8 では避けながら §7 でやっていた** |
| 高-2 | `moveSurfacing.ts:33-36` が実在しないテストを安全網として挙げている | **採用（推奨された案 (a)）** | `HitBoxLayout` の `DIRECTIONS` / `ATTACKS` を `export` し、`moveSurfacing.test.ts` に一致検査 2 本を足した。**注記も「実際に両方を import して検査する」へ直し、旧版が注記だけだった事実を明記した** |
| 高-3 | `VirtualController.tsx:65-69` のヘッダコメントが 4 タブ時代のまま | **採用** | 7 枚の as-built（各タブの母集団つき）へ書き換え、「網羅入力はプルダウンで担保」も未掲載タブが第 2 の担保になったことを追記。**増減させたら同じ手番で直す旨も本文に入れた** |
| 高-4 | `SM-100` の ON で、画面に出ていない技が［追加］で積める | **採用** | `useClearHiddenSelection` を `useControllerInputOmission.ts` に 1 本置き、2 面が同じ形で使う（`E-76` 回避）。**破壊確認で 2 面とも赤になることを確認した。★セットプレイ面は `select.value` だけを見ると option 消滅で自動的に `""` になり緑のままだったため、「積めなくなる」まで主張する形へ改めた**——指摘の「現状の 3 本はいずれも選択を持たない状態で切り替えている」と同型の穴が、こちらの新規テストにも生まれかけていた |

### 中（5 件・4 件採用 / 1 件は開発者へ）

| # | 指摘 | 採否 | 対応と理由 |
|---|---|---|---|
| 中-1 | トグルの文言を i18n へ | **採用** | `controller.panel.omitSurfacedLabel` / `omitSurfacedCount` を ja / en へ追加。名前空間ごとアンカーして追加した |
| 中-2 | 文言そのものの見直し（「ボタンで入力できる技を省く」は未掲載タブの追加で不正確） | **不採用（開発者へ回す）** | **★利用者に見える取捨であり `roles-and-routing` のハード列。製造が単独で決めない。** 指摘のとおり実装は変えずに済むため、i18n 化（中-1）だけを先に入れた。**⇒ 文言案の選択は開発者へ**（§下記「開発者への確認事項」） |
| 中-3 | `characterStateMoves` の単体テスト | **採用** | 4 本（空 `stateCodes` ／ 部分一致 ／ **SA の偽陽性を落とさないことの固定** ／ 複数 code）。(c) は指摘どおり「次の担当が静かに落とさない」ための固定である |
| 中-4 | セットプレイ面の `SM-100` に観測が無い | **採用** | `SetupRecipeEditor.test.tsx` に 3 本（トグルが出る・既定 OFF ／ ON で省かれ未掲載は残る ／ 消えた選択が積めない） |
| 中-5 | 完了報告 §5 の主張と回帰テストの射程がずれている | **採用（記述側で揃えた）** | §5 に「このテストの射程はタブ自体が数字キーを購読しないことまで」と明記。**★`OptionButtonGroup` と仮想コントローラは同じ画面に同居しない**（前者はエディタの基本情報タブ、後者はレシピタブ）**ため、同居描画のテストは足していない** |

### 低（7 件・3 件採用 / 2 件は記録のみ / 2 件不採用）

| # | 指摘 | 採否 | 対応と理由 |
|---|---|---|---|
| 低-10 | 完了報告 §4.1 の変更統計が古い | **採用** | **数える範囲を `web/` に限る形へ改めた**——完了報告そのものを含めると、報告を書き足すたびに数字が古くなるため。per-file の numstat 表に置き換え、新規 6 ファイルの deletions が 0 であることを行ごとに示した |
| 低-11 | roster test が seed の実値を 5 か所ハードコード | **採用（記録のみ）** | 完了報告 §7 に「seed が動く手番では 5 か所を数え直す」を常設項目として足し、progress-log の横断課題にも 1 行残した。**★値そのものは変えない**（指摘も意図は妥当としている） |
| 低-12 | 素の `<input type="checkbox">` が周囲の `Switch` と流儀が割れる | **不採用** | **理由: `Switch`（Radix）は `role="switch"` の `<button>` であり、E2E の `check()` / `not.toBeChecked()` と Vitest の `.checked` がそのまま使えなくなる。** 本サブのトグルは「既定 OFF であること」を 3 層（Vitest 2 面 ＋ E2E）で主張しており、観測の素直さを優先した。**★流儀を揃えるなら周囲の `Switch` 群ごと 1 手番で見るのが筋であり、片方だけ寄せると差が残る** |
| 低-13 | `omittedCount === 0` でも「（0 件）」と出る | **採用** | 0 件のときは件数を描かない形にした。フックの doc も「0 のときトグルは件数の表示を落とす」へ直した |
| 低-14 | `DirectSpecPanel.tsx:11` の列挙が古い | **採用** | 「SA タブはラッシュ非対象」→「`rushOn` を渡すのは特殊技タブだけ。SA / ターゲットコンボ / 未掲載 / キャラ固有状態の 4 タブは非対象」へ。**★失効した記述であり、本プロジェクトの較正では本来「高」に当たる型である**（指摘は低に置いていたが、採用の判断は同じ） |
| 低-15 | 中間コミット `6ec2376` が「述語は載ると言うが面が無い」状態を作っている | **不採用（記録のみ）** | **理由: 履歴の改変は製造の権限外である**（`CLAUDE.md` §7・§10。rebase / amend は deny）。**指摘自身が「逆順 revert では成り立つため実害は無い」と述べており、直す手段が禁止操作しかない。** ⇒ 事実として本欄に残す |
| 低-16 | `testid-convention.md` に既存の `recipe-*` も載っていない | **採用（記録のみ）** | 完了報告 §6-5 と progress-log の横断課題 4 に「併せて棚卸しする価値がある」を追記。**★`docs/design/` の編集は製造の権限外**（`CLAUDE.md` §8 ／ 指示書 §4-9） |

### ★開発者への確認事項（中-2 由来・ハード列）

**全技一覧のトグルの文言を選んでください**（実装は変えずに済みます）。現行は **「ボタンで入力できる技を省く」**。

| 案 | 文言 | 備考 |
|---|---|---|
| A（現行） | ボタンで入力できる技を省く | **★未掲載タブができたため厳密には不正確**（未掲載タブのボタンでも入力できる） |
| B | タブに載っている技を省く | 省かれる集合と一致する。**「タブ」が何を指すか分かる利用者向け** |
| C | 未掲載の技だけを表示 | **残る側を主語にする。⇒ 何が見えるかが先に分かる** |

**★決めていただければ locale の 1 値を差し替えるだけです**（`controller.panel.omitSurfacedLabel`）。
