# M37-02 完了報告: 仮想コントローラ UI の 10 件（`B04` `B05` `B07`〜`B14`）

| 項目 | 内容 |
|------|------|
| 作業 ID | M37-02 |
| 指示書 | `docs/instructions/M37-02-virtual-controller-vertical.md` v1.0.0 |
| チェックリスト | `docs/instructions/reviews/M37-02-review-checklist.md` v1.0.0 |
| 実施日 | 2026-09-13 |
| 枝元コミット | **`38517ef`**（`Merge pull request #215`） |
| ブランチ | `claude/dreamy-ramanujan-w5h1vc` |
| マイグレ消費 | **0**（見込みどおり） |
| CHANGE 消費 | **未採番**（原稿は設計伝達レポート §4。★自採番しない＝`D-293`） |

---

## 0. 着手前の版ゲート（指示書 §0.7）

| # | 確認事項 | 結果 |
|---|---|---|
| 1 | チェックリスト v1.0.0 の存在 | ✅ 11,289 bytes |
| 2 | `M37-RESEARCH-01-report.md` の存在 | ✅ 67,333 bytes |
| 3 | `M37-01` の着地 | ✅ ボード §2.1「`M37-01` はマージ済み」／ `a885546` で受理済み |
| 4 | `M32-02` のスクリーンショット撮影が未着手 | ✅ ボード「`M32-02` ＝ スクリーンショットは `M37` の全着地後」 |
| 5 | 枝元 | ✅ `38517ef` |

---

## 1. ★★★10 件の判定（指示書 §0.1・完了条件 1）

**★本サブは 10 件である。8 件ではない。**

| ID | 要求 | 判定 | 証拠 |
|---|---|---|---|
| **`B04`** | modifier を整ったデザインで表示（生の `{}` 表現を出さない） | **★一部完了** | 内部識別子の露出は解消した（`flagText` を 4 → **10 値**。`ModifiersEditor.tsx:198` も是正。破壊確認は両方向で実測＝§4）。**★★しかし開発者の具体的要望は「新規登録・編集の段階で modifier をつけた時の見た目と一致させたい」であり、これは未達である。⇒ 別サブへ送る**（§7.2・設計伝達レポート §2-1 / §4-6） |
| **`B05`** | modifier の整理（必要・不足を探す） | **★一部完了** | **不足**は **0 値**で確定（`DES-004` §2.3 と `labels.ts` の集合差）。`low_jump` の表示語食い違いも解消（§3）。**★★しかし「必要」の側＝項目の要否と文言の最終チェックは未完である。⇒ 開発者が次マイルストーンで実施する**（2026-09-13 の逐語。§7.5） |
| **`B07`** | ラッシュ版のボタン位置を統一 | **完了** | `RushToggleRow` へ切り出し、特殊技を通常技側（枠の中・末尾）へ揃えた |
| **`B08`** | 必殺技の強度選択を右側へ | **完了** | `sm` 以上で左右 2 カラム。実測は §2 |
| **`B09`** | 必殺技部分を縦スクロール可能なグループへ | **完了** | family リストに高さ上限＋局所スクロール。**`DirectSpecPanel` を使う 5 タブにも入れた** |
| **`B10`** | クイック入力と入力説明欄を縮小 | **完了** | 初回のみ自動表示 ＋ 明示操作で開く ＋ 既読フラグ。**3 文は 1 文字も変えていない** |
| **`B11`** | 仮想コントローラの説明・情報量を整理 | **一部完了** | 案内文 2 か所を短縮。**★区画は 1 つも消していない**——実装コメントと床が「状態として出す」ことを要件として持つため（§5） |
| **`B12`** | 長いレシピを縦に表示 | **完了** | サマリ行を sticky ＋ 全文トグル。★`...` で要約されない形を明示操作で出せる |
| **`B13`** | 強度ボタンの横幅を縮める | **完了** | 「なし/弱/中/強/OD が 1 行」（開発者の逐語）。項目数＝列数の grid ＋ `whitespace-nowrap` |
| **`B14`** | 全体のボタンを小さくする | **完了** | 寸法は開発者がモックを見て選択（中庸）。**★★達成判定は開発者が実機で行った**（2026-09-13・逐語「達成（スクロールせず入力できた）」）**＝`D-855` のとおり製造は判定していない。⇒ 受入条件「縦スクロールせずに全レシピを入力できたか」が満たされた** |

---

## 2. ★★★縦の実測（完了条件 2）

**★段 1 は コードを 1 行も変える前に取った。** 計測アンカー（`recipe-controller-root` / `recipe-special-panel`）の追加だけを先に入れてある——描画には影響しない追加であり、これが無いと段 1 と段 6 が同じ要素を測れない。

- 手段: **Playwright**。`make e2e-only P=m37-02-controller-height`（★`pnpm exec playwright test` を直接叩いていない＝`D-599`）
- spec: `web/e2e/m37-02-controller-height.spec.ts`
- キャラ: **blanka**（family **21**）／ **ryu**（**9**）／ **manon**（**6**）。★family 数はすべて**実測**である（`character_data/*.csv` の `special` 行数は family 集約後の数ではないため転記しない）
- 単位はすべて px。**すべて実測**であり算出値は含まない。

### 2.1 主系列 A（`inputActive=false`）— **段 1 と段 6 の前後 2 点**

**★これが比較の正本である。** 告知・読取欄が描画されないため、`B10` の畳みが混ざらず
`B08`/`B09`/`B13`/`B14` の効きが純粋に出る。

| キャラ | family | 幅 | コントローラ全体 前→後 | Δ | 必殺技パネル 前→後 | Δ | ページ overflow 前→後 |
|---|---:|---:|---:|---:|---:|---:|---:|
| **blanka** | **21** | 390 | **1330 → 631** | **−699（−53%）** | **1086 → 387** | **−699（−64%）** | 2021 → 1334（−687） |
| ryu | 9 | 390 | 641 → 533 | −108（−17%） | 397 → 289 | −108（−27%） | 1332 → 1236（−96） |
| manon | 6 | 390 | 601 → 487 | −114（−19%） | 357 → 243 | −114（−32%） | 1292 → 1190（−102） |
| **blanka** | **21** | 1280 | **848 → 434** | **−414（−49%）** | **724 → 310** | **−414（−57%）** | 1507 → 1105（−402） |
| ryu | 9 | 1280 | 509 → 278 | −231（−45%） | 385 → 154 | −231（−60%） | 1168 → 949（−219） |
| manon | 6 | 1280 | 449 → 252 | −197（−44%） | 325 → 128 | −197（−61%） | 1108 → 923（−185） |
| blanka | 21 | 1366×600 | 848 → 434 | −414 | 724 → 310 | −414 | 1627 → 1225（−402） |
| ryu | 9 | 1366×600 | 509 → 278 | −231 | 385 → 154 | −231 | 1288 → 1069（−219） |
| manon | 6 | 1366×600 | 449 → 252 | −197 | 325 → 128 | −197 | 1228 → 1043（−185） |

**★★読み方の注意 —— 390px と 1280px で効きが違う。**

- **390px では左右分割が効かない**（`sm` 未満は従来どおり縦積み）。⇒ ryu / manon の −108 / −114 は
  **ボタン寸法とチップ化だけ**の効果である。
- **blanka の 390px だけ −699 と桁が違う**のは、**family リストの上限（`11.5rem`）が効いているから**である。
  ⇒ **上限は技数の多いキャラでしか働かない。** ryu（9）/ manon（6）では 1 行も溢れない。
- **1280px では全キャラで 44〜49% 減る。** 左右分割で 種類・強度・OD 組合せが右カラムへ移るためである。

### 2.1b ★段 1 と段 6 で計測手続きが変わった点（**開示**）

**★チェックリスト A-2 は「同じ条件か」を ★★★ で問うている。⇒ 変わった点を隠さない。**

`2c3f3ca` で計測 spec に 3 点の変更を入れた。

| # | 変更 | 比較への影響 |
|---|---|---|
| 1 | 計測前に `window.scrollTo(0, 0)` を入れた | **無し。** 比較している `controllerRoot` / `specialPanel` は `boundingBox().height`、`pageOverflow` は `scrollHeight - clientHeight` であり、**いずれもスクロール位置に依存しない**。★依存していたのは `stepListTop` だけであり、同列は前後比較に使っていない |
| 2 | 系列 B を B（初回）と C（既読）へ分割した | **無し。** 段 1 の系列 B は「初回」しか作れていなかった（E2E は毎回まっさらなコンテキストで始まる）。⇒ 段 6 の系列 B が段 1 の系列 B に対応する |
| 3 | `recipeVisibleWhileInputting` を足した | **段 6 のみの指標**（§2.3 に明記） |

**★1 は段 1 のあとに入れた是正である。** 初版は自動スクロールが残って系列 A と B が逆の結果を返しており、**段 1 の `stepListTop` 列は信頼できない**。⇒ 同列を前後比較へ使っていないのはそのためである。

### 2.2 区画ごとの内訳（完了条件 2 の「どこが効いたか」）

| 区画 | 390px 前→後 | 1280px 前→後 | 効いた要因 |
|---|---:|---:|---|
| 必殺技パネル（blanka） | 1086 → 387 | 724 → 310 | `B08` 左右分割 ／ `B09` 上限 ／ `B13` チップ化 ／ `B14` 寸法 |
| タブ | 112 → 112 | 40 → 40 | **不変**（触っていない） |
| 告知（初回・系列 B） | 116 → **116** | 77 → **77** | **不変**。★初版は閉じるボタンで 1 行増え 140px になっていた。⇒ 右上へ絶対配置して行を足さない形に是正した |
| 告知（2 回目以降・系列 C） | 116 → **0** | 77 → **0** | **`B10`**。畳んだラベル 1 行（約 24px）だけが残る |
| 読取欄（系列 B） | 139 → **123** | 123 → **123** | **`B11`**。★**390px でしか効かない** ——案内文は見出しの隣の inline `<span>` であり、折り返す幅でしか行数が減らない |

**★`B10` の実効**（系列 B 初回 → 系列 C 既読、コントローラ全体）: **390px で −92px ／ 1280px で −53px**。

### 2.3 ★★★「レシピが見えたまま入力できるか」（`B09` / `B14` の目的そのもの）

| 指標 | 段 6 の結果 |
|---|---|
| **必殺技パネルまでスクロールした状態でレシピのサマリ行が viewport 内にあるか** | **★27 条件すべて `true`** |

**★これは床である**——**27 条件すべてに `expect(row.recipeVisibleWhileInputting).toBe(true)` を置いてある。**

**★★初版は系列 A の 9 件にしか assert していなかった**（レビュー 中-3 の指摘）。
**押し下げが最も強いのは告知と読取欄が出ている系列 B である。⇒ 守りたい条件をいちばん危ない側で守っていなかった。**
**⇒ 27 件へ広げた。**

**★★段 1 では同じ指標を取っていない**（段 6 で指標そのものを足したため）。**⇒ 前後 2 点は無い。**
段 1 が偽であったことは**実測ではなく構造からの推論**である——当時のサマリ行は `sticky` を持たない
静的な `<p>` であり、その下に 387〜1086px のパネルが在る以上、パネルを視界へ入れれば必ず外へ出る。
**★「実測」と書けるのは段 6 の 27 条件だけである。**

**★★ステップ一覧（`recipe-step-list`）は前後とも viewport 外である**（`stepListTop` 列が全条件 `false`）。
これは**縮め方の失敗ではない**——同一覧はエディタ下部に在り、その上にセットプレイ節・タグ節も載るため、
コントローラを 0px にしても入らない。**⇒ 視界に残す役目は sticky なサマリ行（`B12`）が持つ。**


---

## 3. `B04` / `B05` — modifier

（本節は §4 の破壊確認と対である）

### 3.1 `B04` サーバ側（本体）

`internal/service/notation/resolver.go` の `flagText` は **4 値しか持たず、残り 6 値は `applyFlags` のフォールバックで `{` ＋ 内部識別子 ＋ `}` になっていた**（`{od_lm}` `{first_hit_cancel}` 等）。10 値すべてを埋めた。表示語は `DES-004` §2.3（短縮表記が正）＝ `labels.ts` と同一。

### 3.2 `B05` — 「消す」ではなく「揃える」だった

- **不足＝設計書に在って実装に無いもの ⇒ 0 値。** `micro_walk` は不足に数えない（`DES-004` §2.1 が独立 move として不採用）。`high_jump` も同（`CHANGE-062` で撤去済みの幽霊仕様）。
- **不要の判定に DB 件数 0 を使っていない**（`D-857` ＝利用者はまだいない）。未使用 4 値（`link` / `low_jump` / `od_lh` / `cancel_drive_rush`）は**削らずに残した**。
- **⇒ `B05` の実体は表示語の食い違いの解消であった。** `low_jump` は UI「低ジャンプ」／ サーバ「最低空」で割れており、**`DES-004` §2.3 が「低ジャンプ」を正としているためサーバ側が仕様からの逸脱**であった。

### 3.3 ★`recipe_cache` の扱い（完了条件 7・§4.3 の危険）

**採った形＝既存の再生成経路に委ねる。データを黙って書き換えない。**

| 経路 | 実体 |
|---|---|
| API | `POST /api/presets/:id/recipe-cache/rebuild`（`internal/api/preset/routes.go:21`） |
| サービス | `notation.RecomputePresetCache`（`cache.go:106-164`）＝当該プリセット分を**全 active コンボ ＋ 全 active セットプレイ**へ作り直す。1 トランザクション・全か無か |
| UI | 設定画面「キャッシュ再構築」（`SettingsSectionData.tsx:19-77`）が**全プリセットへ直列にファンアウト**する（`M24-08` 第 2 部 B） |

**理由 3 つ。**

1. **マイグレで直すのは既存の床が禁じている。** `internal/infra/migration/migrate_m2408_test.go:130` の `TestRun_M2408_RecipeCacheUntouched` が「マイグレは `recipe_cache` を書き換えない」を主張しており、`000022` / `000080` / `000081` も同じ姿勢を明記している。
2. **lazy な解決では直らない。** `ResolveComboRecipe`（`cache.go:38-42`）は**欠けているキーを埋めるだけで既存値を上書きしない**。⇒ 古い `{od_lm}` は放っておいても消えない。
3. **既存行の書き換えは設計判断である**（指示書 §4.3）。⇒ 勝手にやらない。

**★★開発者への申し送り: 本サブの着地後、設定画面の「キャッシュ再構築」を 1 回押してください。** 押すまで、保存済みの `recipe_cache` は古い `{od_lm}` 等を持ち続けます（`M37-RESEARCH-01` §3.3 の実測＝combos 2 行・setups 1 行）。**新規に作る文字列は押さなくても直っています。**

---

## 4. 破壊確認（指示書 §5-3）

**★「効いた」ことの証拠は逆向きが無いと成立しない。** 全数検査だけでは「表示語がすべて埋まっている」のか「検査が何も見ていない」のかを区別できない。

- 追加した床: `internal/service/notation/flagtext_internal_test.go`（`package notation` の内部テスト。`flagText` / `applyFlags` が非公開のため）
  - `TestFlagText_CoversAllTenValues` … 母数 10 を固定し、全値に表示語が在り、内部識別子を含まないこと
  - `TestApplyFlags_NoInternalIdentifierLeaks` … 出力に `{<flag>}` が出ないこと（全 10 値）
  - `TestApplyFlags_MissingFlagFallsBack` … **逆向き**。表示語を 1 つ外すと再びフォールバックが出ること
- **★実際に破壊して両方向を確かめた**（実測・2026-09-13）: `od_mh` の表示語を外すと

```text
--- FAIL: TestFlagText_CoversAllTenValues
    flag 数の不一致: DES-004 §2.3 は 10 値, flagText は 9 値
--- FAIL: TestApplyFlags_NoInternalIdentifierLeaks
    flag "od_mh" が内部識別子のまま出ている: "立ち弱P {od_mh}"
```

  復旧後は緑。**⇒ 検査は実際に赤くなる。**

---

## 5. ★★床について（完了条件 8・チェックリスト束 C）

### 5.1 既存の床は 1 行も外していない

**7 ファイル / 31 行**（`recipe-gamepad-(notice|readout)|GAMEPAD_INPUT_NOTICE_POINTS` の直接参照）。**★数え直した**（`M37-RESEARCH-01` の値を転記していない＝計測点 `M-157`）——実測で研究報告と一致した。

```
web/e2e/m21-06-command-motion.spec.ts
web/src/features/combo/components/VirtualController/VirtualController.test.tsx
web/src/features/gamepad/gamepadRecipeInput.test.tsx
web/src/features/gamepad/gamepadShortcut.test.tsx
web/src/features/keyboard/keyboardRecipeInput.test.tsx
web/src/features/physical-input/commandMode.test.tsx
web/src/features/physical-input/modalSuppression.test.tsx
```

### 5.2 ★★★`B11` で形を取り違えかけた（§0.4 の合図が効いた）

**最初は「未登録／未取得のときは区画ごと出さない」形を検討した。⇒ 誤りである。**

実装コメントが、この 2 つの表示を**意図的な要件**として名指ししている:

- `GamepadRecipeReadout.tsx:186` 「★『割り当てられるボタンが無い』ことを失敗ではなく状態として出す（§4.4-3）」
- 同 `:204` 「★『索引を取得できていない』ことを失敗ではなく状態として出す（§4.6-6）」

床のテスト名も同じことを言っている:

- `gamepadShortcut.test.tsx:634` 「★未登録であることが**状態として画面に出る**（失敗として扱わない）」
- `commandMode.test.tsx:644` 「モードに入れないことが**状態として出る**（エラーにしない）」

**⇒ 区画ごと消せばこの 2 本が落ちる。指示書 §0.4 の合図そのものである**——「床を外す必要が出たら、それは形を取り違えたか…のどちらかである」。

**⇒ 採った形＝状態は残し、説明句だけを落とす。**

| 区画 | 変更前 | 変更後 |
|---|---|---|
| ショートカット | `未登録（コントローラ設定で「ショートカット前置き」を登録すると使えます）` | **`未登録`** |
| コマンド技入力 | `このキャラクターのコマンド表を取得できていないため使えません` | **`コマンド表を取得できていないため使えません`** |

**⇒ 床 2 本はそのまま緑である。書き換えていない。**

### 5.3 ★意図した床の更新は 1 件だけ

`internal/service/notation/resolver_test.go` の `TestRenderSteps_WithFlags` が `"立ち弱P {最低空}"` を assert していた。`low_jump` を `DES-004` §2.3 の正（低ジャンプ）へ直したため `"立ち弱P {低ジャンプ}"` へ更新した。**理由はコード内コメントと設計伝達レポート §4 に在る。**

### 5.4 ★★床が 1 本落ちて欠陥を 1 件見つけた（黙って直していない）

スクロール容器へ `${testIdPrefix}-scroll` の testid を付けたところ、`VirtualController.test.tsx:499` の並び順の床が落ちた——`[data-testid^="recipe-common-"]` の**接頭辞走査**が非ボタン要素まで 1 件多く数えたためである。

**★同型の走査はリポジトリに 5 か所ある**（実測）。とくに `recipe-special-family-scroll` は `m30-04-derived-to-row-end.spec.ts:21` の E2E と**本サブの計測 spec の family 数**まで狂わせるところだった（21 → 22）。

**⇒ 是正は実装側である。`recipe-scroll-*` の別名前空間へ移した。床は 1 つも書き換えていない。**

---

## 6. ★足した床（引いていない）

| ファイル | 件数 | 何を主張するか |
|---|---:|---|
| `internal/service/notation/flagtext_internal_test.go` | 3 | `B04` の全数 ＋ 逆向き |
| `web/src/features/gamepad/components/GamepadInputNotice.test.tsx` | 8 | `B10`＝初回展開 / 2 回目は畳む / 明示操作で開く / 保存失敗時は隠す / 2 面共有 |
| `web/src/features/gamepad/components/GamepadRecipeReadout.test.tsx` | 11 | `B11` の非退行＝3 区画の残存 ／ 未解決 **6 理由** ／ ラッシュ版を「技が無い」へ丸めていないこと |
| `web/src/features/combo/components/ModifiersEditor.test.tsx` | +2 | `B04` の内部 type 露出 ＋ 未知 type のフォールバック |
| `web/src/features/combo/components/RecipeBuilder.test.tsx` | +5 | `B12`＝既定 1 行 / 展開 / 上限 / `textContent` 不変 / 空なら非表示 |
| `web/e2e/m37-02-controller-height.spec.ts` | 27 | 縦の実測 ＋ **「レシピ可視（入力中）」** |

---

## 7. ★★実機確認の結果（完了条件 3）

**2026-09-13 に開発者が実機で確認した。★製造は判定していない**（`D-855`）。

### 7.1 判定

| # | 事項 | 結果 |
|---|---|---|
| **1** | **`B14`＝「縦スクロールせずに全レシピを入力できたか」** | **★★達成**（開発者の逐語＝「達成（スクロールせず入力できた）」）。**⇒ `B14` を「完了」とした** |
| 2 | 見た目 | **問題なし**（開発者の逐語＝「見た目はこれで問題ありません」） |
| 3 | **タップ領域**（技ボタン 52px → **40px**。iOS 推奨 44px を下回る） | **★実機で問題は報告されていない。** 見た目の承認と 1 件の登録が通っている |
| 4 | 畳んだ告知の見た目（`P-59` (2)） | **既定の「ラベル 1 行」で承認。**（`GAMEPAD_NOTICE_COLLAPSED_FORM` を `"hidden"` へ 1 行変えれば完全に消す形も試せる状態は維持） |
| **5** | **★★告知を閉じたら次から出ないこと**（下記 7.4-A＝`B10` の本体） | **★確認済み**（2026-09-13）。**⇒ E2E が通れなかった唯一の経路が実機で通った** |
| **6** | **★設定画面の「キャッシュ再構築」**（§3.3） | **★実施済み**（2026-09-13）。**⇒ 保存済みの `recipe_cache` 3 行が新しい表示語へ更新された** |

### 7.2 ★★`B04` — 要望が具体化し、**別サブへ送る**

**開発者の逐語**（2026-09-13）＝**「新規登録、編集の段階で modifier をつけた時の見た目と一致させたい、というのが具体的な要望だった。厳しいなら申し送りにして別サブも検討していい」。**

**⇒ 「波括弧を残すか外すか」の二択ではなかった。** 実査の結果は次のとおり。

**★語彙は既に完全一致している。** 編集画面のバッジ（`ModifiersSummary.tsx:47-52`）は `MODIFIER_FLAGS` のラベルを描き、サーバ文字列は `flagText` を使う。**本サブで 10 値すべてを揃えたため食い違いは 0 である**（`low_jump` の「最低空」→「低ジャンプ」の是正を含む）。

```text
編集画面   立ち強P  [ジャスト] [ディレイ] ※      ← [ ] は青いバッジ、※ はメモ(hover で本文)
一覧・詳細 立ち強P {ジャスト} {ディレイ} (ヒット確認)
```

**★★★一覧ではバッジにできない。** `internal/api/combo/dto.go:295`:

```go
Steps []StepResponse `json:"steps,omitempty"` // 一覧 API では nil(N+1 防止、§4.11)
```

**一覧 API は steps を意図的に返さない。⇒ 修飾の構造がクライアントに無く、バッジを描けない。** 詳細 API は steps を返すため詳細だけならバッジ化できるが、**一覧と詳細で見た目が割れる**——`B04` が消そうとしている不統一を別の形で作ることになる。

**⇒ 完全な一致には `§4.11`（N+1 回避）の再検討が要る。設計判断であり本サブの射程外である。**

**★波括弧だけを外すことは推奨しない。** バッジは**色**で「これは修飾である」と伝えているが、平文には色が無く、**波括弧がその唯一の代替である。** 外すと `立ち強P ジャスト ディレイ` となり技名と修飾の境目が空白だけになる。**⇒ バッジ化とセットでなければ情報が減る。**

**★往復の安全性は確認済み**（判断材料として）: 表示文字列は**解析されない**。CSV の `recipe` 列は**セル内 JSON `[{move_code,modifiers}]`** であり（`csvcore/contract.go:56`）、`recipe_cache` の表示文字列とは別物である。**⇒ 撤去してもインポートは壊れない。壊れないが、読みにくくなる。**

**残る食い違い 3 点**（申し送りへ）:

| # | 項目 | 編集画面 | 一覧・詳細 |
|---|---|---|---|
| 1 | flags | 青バッジ・区切り記号なし | `{ジャスト}` |
| 2 | メモ | `※` ＋ hover で本文 | ` (本文)` を展開 |
| 3 | **非技ステップ** | **「パリィドライブラッシュ」** | **「生ラッシュ」**★語が違う |

**★3 は設計伝達レポート §4-4 で既に起票していた**（`parry-drive-rush-label-differs-go-and-ui`）。**⇒ 同じ面であり 1 本に畳んで起票する**（同 §4-6）。

### 7.3 ★スマホ幅の実機確認は**省略**（開発者判断・2026-09-13）

**開発者の逐語**＝**「スマホ確認は省略します。おまけ機能に近いため。リリース後に課題として再浮上する可能性はあるので注視します。」**

- **★E2E は 390×844 を測っているが、それは実機ではない**（Chromium のビューポート指定である）。⇒ 実機のタッチ操作・慣性スクロール・`overscroll-contain` の効き・40px のタップ領域は**未確認のままである**。
- **⇒ 申し送りへ起票した**（設計伝達レポート §4-7 の `virtual-controller-narrow-width-unverified-on-device`）。**割付＝リリース後。**

### 7.4 ★★手動確認の結果（**自動テストが構造的に届かない範囲**）

**★★開発者が確認完了を宣言した**（2026-09-13・逐語＝「私の確認は終わった認識です」）。**⇒ 本サブの手動確認は閉じる。**

経緯として、開発者から「1 件登録もできましたが、手動動作確認としてはそれだけで充分ですか？」と問われ、製造は「**足りない**」と答えて以下 4 点を挙げた。1 件登録で確かめられたのは主に見た目と保存の一周であり、これらは自動テストで代替できないためである。

| # | 確認 | なぜ自動テストで代替できないか | 結果 |
|---|---|---|---|
| **A** | 実機のパッド／キーボード登録で **告知が出る → 閉じる → 再読込で出ない** | **★`B10` の本体である。** E2E に実機のパッドは無く、既読状態は `localStorage` へ直接書いて作っている。**⇒ 「実際に閉じたら次から出ない」経路そのものを一度も通っていない** | **★★確認済み**（2026-09-13） |
| **B** | 技数の多いキャラで**必殺技一覧の中だけ**をスクロールし、**末尾でページ側が動かない**こと | `overscroll-contain` の効き目。**高さは測っているがスクロールの連鎖は測っていない。** ここが効かないと `B09` が防いだレシピの画面外流出が戻る | **★明示の報告なし。** 開発者の確認完了の宣言に含めて閉じる |
| **C** | レシピ行の「全文」トグル ＋ **貼り付きの見え方**（角の白帯） | 単体テストは className を見るだけ。レビュー低-8 と同件 | **★明示の報告なし。** 同上 |
| **D** | **修飾を付けたコンボ**を 1 件登録し、一覧・詳細で表示を確認 | **★`B04` はここでしか実地確認できない。** 修飾なしの登録では `B04` の変更点に一度も触れていない | **★次マイルストーンへ**（§7.5） |

**★★B / C は「確認した」とは書かない。** 開発者は確認完了を宣言したが、この 2 点について明示の言及は無かった。**⇒ 「開発者の判断で閉じた」と「確認して問題が無かった」は別である。** 後任が前者を後者と読み違えないよう、ここで区別しておく。

**★キャッシュ再構築（§3.3）は実施済み**（2026-09-13）。保存済みの 3 行（combos 8 / 78・setups 16）の `{od_lm}` 等が `{OD(弱中)}` 等へ更新された。

### 7.5 ★★modifier の項目確定は**次マイルストーンで開発者が実施する**

**開発者の逐語**（2026-09-13）＝**「modifier の項目確定はまだ完遂できていないので、次のマイルストーンで実施します。（この modifier が本当に必要か、文言がおかしくないか？の最終チェック）」**

**⇒ `B05` の判定を「完了」から「一部完了」へ改めた**（§1）。内訳は次のとおり。

| `B05` の要求 | 本サブでの状態 |
|---|---|
| **不足しているものを探す** | **★確定した＝0 値。** `DES-004` §2.3 と `labels.ts` の集合差が 0（`micro_walk` / `high_jump` は設計上の不採用であり不足に数えない） |
| **必要なものを見定める**（項目の要否） | **★未完。⇒ 開発者が次マイルストーンで最終チェックする** |
| **文言** | **★Go と UI の食い違いは解消した**（10 値を一致・`low_jump` の是正）。**⇒ ただし「文言そのものが適切か」の最終チェックは未完** |

**★★これは製造が判断できない類の事項である。** 「この modifier が本当に必要か」は **SF6 のドメイン知識**を要する判断であり、`roles-and-routing.md` のハード列（開発者のドメイン知識が要る）に当たる。**⇒ 製造が推測で削らなかったのは正しい**（`D-857`＝利用者はまだいないため DB 件数 0 を不使用の証拠にしない）。

**⇒ 申し送りへ起票した**（設計伝達レポート §4-8 の `modifier-set-and-wording-final-review`）。**★上記 D（修飾を付けたコンボの実地確認）は、この最終チェックの中で自然に通る。**

---

## 8. ■ 併せて更新が要るもの

| # | 対象 | 状態 |
|---|---|---|
| 1 | **CHANGE 番号の登録** | **★消費していない。** 原稿を設計伝達レポート §4 へ置いた。⇒ 採番と `change-number-registry.md` §1 への登録は**設計卓の手番**である（`D-293`） |
| 2 | **マイグレ連番** | **消費 0。** ボード §2.2 の「次に払い出す番号」は動かない |
| 3 | **`web/CLAUDE.md` §1 台帳** | **✅ #12 を追加済み**（`gamepad-input-notice-seen-v1`）。`check-browser-storage-keys.sh` 緑 |
| 4 | **版を上げた文書の参照元** | **なし**（設計書本体は 1 バイトも触っていない） |
| 5 | **`docs/usermanual/`** | **★追随の対象は 0 件である**（§9） |

---

## 9. `docs/usermanual/` の追随（完了条件 9）— **対象なし（実査済み）**

`docs/usermanual/tacpendium-readme.html` の「コンボ登録・編集（`DES-005` §5.7）」章（行 350-369）は **本文が全部プレースホルダ**である:

```html
<h3>この画面は何のためにあるか</h3>
<p class="todo">本文は M32-02 で記述します。</p>
```

**⇒ 陳腐化する本文がまだ存在しない。追随の対象は 0 件。**（「なし」と書くことと、節を落とすことは違う）
**★スクリーンショットは撮っていない**（開発者の手番・`M37` の全着地後＝overview §4.2）。

---

## 10. 開発者とのやりとりで確定した点

| # | 事項 | 決着 |
|---|---|---|
| 1 | ボタン寸法 | **モックを提示して選択いただいた**（開発者の指示＝「モックでまず示して欲しいです」）。⇒ 技ボタン＝中庸 / 種類・強度・OD 組合せ＝控えめ |
| 2 | `B12` の形 | **トグルで展開 ＋ sticky**（開発者選択） |
| 3 | 数字キーによる強度入力 | **★取りやめ**（開発者が再検討のうえ「マウスで基本的に入力するのでキーボード機能は付ける必要がなかった」）。⇒ 調査結果だけ §11 に残す |
| 4 | 強度ボタンの色 | **★現行を維持**（開発者の指示）。弱＝水色 / 中＝黄 / 強＝赤 / OD＝灰 |
| 5 | 対象タブ | **★必殺技だけでなく他のタブも**（開発者の指示）。⇒ `DirectSpecPanel` を使う 5 タブ ＋ 共通技 ＋ 通常技へ及ぼした |

---

## 11. 実施しなかったことと、その調査結果

### 11.1 数字キーによる強度・種類の選択（取りやめ）

開発者から可否の調査を求められ、**実現可能・衝突は解決済み**という結果を得たが、**開発者が再検討のうえ取りやめた**。判断材料として結果を残す。

- **衝突は `M24-12` が既に解いていた。** `useKeyboardInput` は `window` の **capture 段**で動き（`useKeyboardInput.ts:137`）、**利用者が割り当てたキーにしか触らず**（同 `:84`。キーボード割当は既定を持たない＝`D-370`）、自分のキーなら `preventDefault()` する（同 `:95`）。選択肢群の側は `event.defaultPrevented` を見て手を引く（`OptionButtonGroup.tsx:109`）。
- **専用の床もある**（`OptionButtonGroup.test.tsx:126`）。コメントに実測が残っている——「2 を弱パンチへ割り当てた状態で、ガード前はステップが増え**かつ**選択肢が変わる。ガード後はステップだけ」。
- **Enter は競合しない。** 数字群は Enter を扱わず、ネイティブ `<button>` の既定動作のままである。順送りの Enter（`useFieldSequence.ts:176`）も同じガードを持つ。

**⇒ 将来また要望が出たら、この節から再開できる。**

### 11.2 射程外として触っていないもの

`A`（運び量 UI）／ `B01` `B02` `B03` `B06` `B15` ／ 設計書本体 ／ `docs/handover/followup-backlog.md`（`D-838`）／ スクリーンショット ／ DB 列・マイグレ。

**★サブを自分で割っていない**（`D-855`）。段 4 は想定内に収まった。

---

## 12. 検査の結果

| 検査 | 結果 |
|---|---|
| **`bash scripts/check-artifact-integrity.sh`**（★1 本目に回した） | **OK** |
| `go test ./...` | **OK**（全パッケージ） |
| `pnpm test`（Vitest） | **OK 2,886 件 / 233 ファイル** |
| **`make e2e`（全数）** | **OK**（2 回実施。1 回目 344 passed / 2 回目 343 passed ＋ **flaky 1**＝`m31-02-tag-field-drag.spec.ts:49`。★既登録の flaky であり `D-863` が本サブへ再測を割り付けていた。⇒ §12.1） |
| `make e2e-only P=m37-02-controller-height` | **OK 27 件**（段 1 / 段 6 の実測） |
| `bash scripts/check-browser-storage-keys.sh` | **OK**（台帳 12 件 / 本番 11 件・ズレなし） |
| `bash scripts/check-import-order.sh` | **OK**（99 / ベースライン 99・増加なし） |
| `bash scripts/check-enum-sync.sh` | **OK**（ベースラインどおり） |
| **`bash scripts/check-progress-log-index.sh`** | **OK**（★レビュー 高-2 で欠落を指摘され追記した。**着手時は NG であった**） |
| `bash scripts/check-md-emphasis.sh docs/progress/M37-02-completion-report.md` | **OK 0 行**（`D-775`） |
| `bash scripts/check-md-emphasis.sh docs/progress/m37-02-review.md` | **OK 0 行** |
| `pnpm exec tsc -b` | **OK** |

**★★`make e2e` の全数が緑であることが `B12` の sticky にとって重い。** sticky なバーは
`scrollRectIntoViewIfNeeded` が対象をバーの真下へ停めたときにクリックを遮り、
"element intercepts pointer events" を広範囲に撒く形の欠陥になりうる（着手前に最大のリスクとして
挙げていた）。**⇒ 344 件が緑であり、その形では起きていない。**

**★上の `make e2e` 全数は 1 つ前のコミットで回している。** その後の変更は「告知の閉じるボタンを右上へ絶対配置した」1 点のみである。
**⇒ 同変更のあと `m37-02-controller-height`（27 件）と Vitest 全数（2,891 件）を回し直して緑を確認した。**


### 12.1 ★★`m31-02-tag-field-drag` の再測（`D-863` が本サブへ割り付けた作業）

`followup-backlog.md` の `m31-02-tag-field-drag-flaky-in-full-run` に **「⇒ `M37-02` は同じ面へさらに手を入れる。★★同サブで再測させる（`D-863`）」** とある。**⇒ 実施した。**

| 条件 | 試行 | flaky |
|---|---:|---:|
| **単独**（`make e2e-only P=m31-02-tag-field-drag`・6 件） | **10 回** | **3 回（30%）** |
| 全数（`make e2e`・344 件） | 2 回 | 1 回 |
| **対照＝本サブの sticky を外した状態**（単独・6 件） | 4 回 | **0 回** |

**★★★「sticky を外したら 0 回だった」を原因の特定に使わないこと。** 30% の発生率の下でも **4 回連続で出ない確率は約 24%** であり、`n=4` の 0 件は無効果と完全に整合する。**⇒ 有意ではない。**

**★根拠にしているのは機構のほうである。** レシピ面は `ComboEditor.tsx:1075` で `hidden`（＝`display:none`）であり、**sticky 要素はレイアウト箱を持たない。⇒ 座標にもポインタイベントにも影響しない。** 同 spec が測るのは基本情報タブのタグ欄のドラッグ座標である。

**⇒ 本サブの差分が原因とは言えない。面は既存行どおり `M31-02` である。**（既存行の更新原稿は設計伝達レポート §4-5。**★`followup-backlog.md` は製造が編集しない**＝`D-838`）

---

## 13. レビューと取り込み（Phase B / C）

| 項目 | 内容 |
|---|---|
| レビュー報告書 | `docs/progress/m37-02-review.md`（292 行） |
| 指摘件数 | **18 件**＝高 4 ／ 中 5 ／ 低 9 |
| **★「高」指摘の不採用** | **0 件**（4 件すべて採用）。⇒ エスカレーション事由なし |
| 採否 | **採用 15 件 ／ 不採用 3 件**（いずれも低） |
| 再レビュー往復 | **0 回**（初回のみ。上限 2 回に達していない） |

### 13.1 高（4 件・**すべて採用**）

| # | 指摘 | 採否 | 対応 |
|---|---|---|---|
| **高-1** | `GAMEPAD_NOTICE_COLLAPSED_FORM = "hidden"` に開く導線が無く、コメントは在ると書いている | **採用** | `GamepadNoticeOpenButton` を新設し `VirtualController` の見出し行（横に空きがあり縦を増やさない）へ載せた。**両分岐に単体テスト 4 本を追加**（着手前は `"hidden"` 分岐のテストが 0 本だった）。コメントも実物に合わせた |
| **高-2** | `progress-log.md` への索引行が無い（`check-progress-log-index.sh` が NG） | **採用** | Phase D で追記（§12 の検査表にも本検査を追加） |
| **高-3** | 「未解決 6 理由」の床が空振り（`not.toContain("ありません」")` は必ず通る） | **採用** | **★指摘のとおり無意味な assertion であった。** 実装の空表示は `ありません` であり `」` を含まない。⇒ 6 理由の期待文字列を**テスト側へ独立に書き写し**（実装の `UNRESOLVED_LABEL` を import しない＝実装が実装と一致するだけになるため）、`toContain` で照合する形へ書き直した。**★破壊確認も実施**（下記） |
| **高-4** | `resolver.go` のコメントが破壊確認の場所を誤指し | **採用** | `flagtext_internal_test.go` へ訂正。非公開の `flagText` に外部テストパッケージからは触れられない理由も添えた |

**★高-3 の破壊確認**（`B04` と同じ作法を適用した。指摘の主旨そのもの）——`rush_not_available_in_air` の理由文を `move_not_found` と同一（＝`DES-005` §6.4.3 が名指しで禁じた「技が無い」への丸め）にすると:

```text
× ★rush_not_available_in_air に固有の理由文が出る
× ★★6 理由がすべて互いに異なる（丸めていないことの対照）
  Tests  2 failed | 10 passed (12)
```

復旧後は 12 件緑。**⇒ 書き直した床は実際に赤くなる。**

### 13.2 中（5 件・**すべて採用**）

| # | 指摘 | 対応 |
|---|---|---|
| 中-1 | `B04` の受入条件（overview「生の `{}` 等で囲んだ表現をそのまま出さない」）と実装（波括弧は残す）の読みの割れを開発者へ上げていない | **§7 の請求へ 1 行追加。判定「完了」は暫定と明記した** |
| 中-2 | 段 1 と段 6 で `measure()` の手続きが変わったことを開示していない／段 1 の生 stdout が残っていない | **§2.1b を新設して 3 点を開示。付録E へ段 1 の生出力を貼った** |
| 中-3 | 「27 条件すべて床」は実際には系列 A の 9 件のみ assert | **★床を 27 件へ広げた**（主張を弱めるのではなく実態を主張に合わせた）。§2.3 に経緯も書いた |
| 中-4 | 区画 3/4 は「畳んだ」ではなく**復旧手順の恒久削除**（得た縦は 390px で 16px） | **★`title` 属性へ逃がした。縦は 1px も増えない。** 「どうすれば使えるようになるか」は残る |
| 中-5 | `getSnapshot` から `localStorage` へ書いている（描画中の副作用） | **★書込みを `persistGamepadNoticeSeen()` へ分離し、`useEffect` から呼ぶ形にした。`getSnapshot` は読むだけの純関数になった。** 保存失敗時に畳む挙動は `autoOpenedRef` で維持（手動で開いたものは閉じない） |

### 13.3 低（9 件・採用 6 / 不採用 3）

| # | 採否 | 理由 |
|---|---|---|
| 低-1（共通技のラッシュボタンが全幅になった） | **採用** | §7 の実機確認へ 1 行追加 |
| 低-2（特殊技の補足が `(…)` で囲まれるようになった） | **採用** | 本報告へ記載。通常技側と統一された副作用であり意図どおり |
| 低-3（`DirectSpecPanel` のインデント） | **採用** | 整形した |
| 低-4（`repeat(0, …)` のガード） | **採用** | `Math.max(1, count)` |
| 低-5（`flagText` を壊すテストと将来の並列化） | **採用** | 「このパッケージを `t.Parallel()` で並列化しないこと」をコメントで明示 |
| 低-6（`rows` がワーカーごとの状態） | **採用** | spec 側で `test.describe.configure({ mode: "serial" })` を宣言 |
| 低-7（`ODifiedVariants` の命名） | **採用** | `odVariants` へ改名 |
| 低-8（sticky の `-mx-4` が角に白帯を作る） | **不採用（実機確認へ回す）** | コード上では判定できない。⇒ §7 の実機確認へ 1 行追加した。**指摘を握り潰してはいない** |
| 低-9（保存できない環境では初回から畳まれる） | **不採用（申し送りへ）** | 意図的なトレードオフでありコメントに明記済み。⇒ 付録C へ申し送りとして追加 |

### 13.4 レビューが認めた点（抜粋）

段 1 を本当に先に取ったこと（コミットグラフで裏が取れる形）／ `B04` の破壊確認の作法／ `B11` で §0.4 の合図に従って引き返した判断／ 床を書き換えず `recipe-scroll-*` へ逃がした是正／ `recipe_cache` を黙って書き換えなかった判断／ `B14` を自己判定せず不利な事実（40px < 44px）を添えて請求したこと。

---

## 付録A. CHANGE 原稿（**★設計伝達レポート §4 へ移す**）

**★製造は自採番しない**（`D-293`）。以下は設計卓が採番・起票するためのたたき台である。
**★設計伝達レポートを作る手番で §4 へ移すこと**（`CLAUDE.md` §9 の停止規律）。

### A-1 `DES-005` §6.4.3 — 告知の出しかた（`B10`）

**変更の要点**: 「告知と読取表示は、機体を観測できているときだけ出す」に **表示回数の規定**を足す。

> **★告知は初回のみ自動表示する。** 以降は明示操作で開く。既読フラグはブラウザに保持する
> （DB 列を足さない・§10.X 公認キー `gamepad-input-notice-seen-v1`）。
> **★型は §5.9 の「制約の告知」と同じである。**
> **★文言 3 点は変えない**（`D-347` が守っているのは 3 点目であり、**畳むことは落とすことではない**——
> 明示操作で開ける限り、伝える場所は在る）。
> **★2 面（コンボのレシピ入力面 ／ セットプレイ入力面）で既読を共有する。**

**あわせて失効させる記述**: `GamepadInputNotice.tsx` の旧コメント「常設の注記にした（§9.2-2 で形は自由）。初回のみの説明にしなかったのは…」——**実装側は本サブで是正済み**である。

### A-2 `DES-005` §6.4 / §5.7 — 仮想コントローラの配置（`B07`〜`B14`）

> **★必殺技パネルは `sm` 以上で左右 2 カラムに分ける**（左＝ファミリー一覧 ／ 右＝種類・強度・OD 組合せ）。
> **★ファミリー一覧は高さ上限と局所スクロールを持つ。** 上限は固定寸法で置く（ビューポート相対にしない
> ——モバイルで URL バー開閉に合わせて伸縮し、操作中にボタンが動く）。
> **★同じ扱いを `DirectSpecPanel` 系の 5 タブにも及ぼす。**
> **★強度は「なし・弱・中・強・OD」が 1 行に並ぶ。** 強度色（弱＝水色 / 中＝黄 / 強＝赤 / OD＝灰）は維持する。
> **★ラッシュ版トグルは枠の中・末尾に置く**（通常技・特殊技で統一）。
> **★レシピのサマリ行は sticky であり、明示操作で全文を縦に展開できる。**

### A-3 `web/CLAUDE.md` §1 台帳 — 新キー 1 件（`B10`）

**★台帳への追記は本サブで実施済み**（#12 `gamepad-input-notice-seen-v1`）。**CHANGE 番号だけが未採番**である。

---

## 付録B. 停止時記録（§J 行の原稿）— **★該当なし**

**★★`docs/handover/followup-backlog.md` は 1 文字も編集していない**（`D-838`）。

停止規律の上限（再レビュー往復 2 回 / タイムボックス / 開発者の終了指示）には**達していない**。
**⇒ 未解消のまま停止した項目は現時点で 0 件である。** Phase C で発生したらここへ 5 フィールド付きで追記する。

---

## 付録C. 設計卓への申し送り（レポート §4 候補）

| # | 事項 |
|---|---|
| 1 | **★Go と UI で表示語が割れている 2 件目が在る（直していない）**。`resolver.go` の `nonMoveTypeText` は `parry_drive_rush` → **「生ラッシュ」**、`labels.ts:225-232` は **「パリィドライブラッシュ」**。`DES-004` §2.3 は「パリィドライブラッシュ（生ラッシュ）」と両方を書いており、`M37-RESEARCH-01` §3.2 は「生ラッシュ」を as-built として記録している。**⇒ 指示書は `low_jump` しか名指ししていないため射程外として残した。** |
| 2 | **★`modifiers.flags` に Go 側の定数集合が無い**（`model.Modifiers.Flags` は `[]string`）。`check-enum-sync.sh` は flags を見ていない——フロント側の定義が `web/src/constants/` ではなく `features/combo/labels.ts` に在るためである。**⇒ 表示語は Go / labels.ts / `DES-004` の 3 か所にあり、機械検査が 1 つも無い。** 本サブでは doc コメントの是正までに留めた（定数の新設は射程外）。 |
| 3 | **★`[data-testid^="..."]` の接頭辞走査が 5 か所ある。** 同じ接頭辞を持つ非ボタン要素を足すと**静かに 1 件多く数える**。本サブで実際に踏み、`recipe-scroll-*` の別名前空間へ逃がした。**⇒ testid 規約（`DES-005` §6.8）へ「走査に使われる接頭辞の下へ非ボタン要素を置かない」を足す価値がある。** |
| 4 | **★保存できない環境（プライベートブラウジング等）では告知が初回から畳まれる**（レビュー 低-9）。⇒ `D-347` の「事前に伝える」がその環境だけ弱くなる。**意図的なトレードオフであり**（既読を書けないまま表示すると毎回自動表示になり `B10` を満たさない）、実装コメントにも明記してあるが、`D-347` の射程に関わるため申し送る。 |
| 5 | **★`docs/usermanual/` §5.7 は本文が未記述である**（`M32-02` 待ち）。**⇒ 本サブの UI 変更は「追随の対象 0 件」で閉じたが、`M32-02` が書く時点では本サブの as-built を反映する必要がある。** |

---

## 付録E. 段 1 の生出力（**★もう取り直せない値である**）

レビュー 中-2 の指摘により追加。**段 1 は枝元の状態でしか取れず、再取得は不可能である。**
⇒ 後任が要約表を再計算できるよう、stdout をそのまま残す。

```text
=== M37-02 CONTROLLER HEIGHT ===
series	character	viewport	families	root	special	tabs	notice	readout	overflow	stepListVisible
A(inputActive=false)	blanka	390x844	21	1330	1086	112	0	0	2021	false
B(inputActive=true)	blanka	390x844	21	1599	1086	112	116	139	2289	false
A(inputActive=false)	ryu	390x844	9	641	397	112	0	0	1332	false
B(inputActive=true)	ryu	390x844	9	910	397	112	116	139	1600	false
A(inputActive=false)	manon	390x844	6	601	357	112	0	0	1292	false
B(inputActive=true)	manon	390x844	6	870	357	112	116	139	1560	false
A(inputActive=false)	blanka	1280x720	21	848	724	40	0	0	1507	false
B(inputActive=true)	blanka	1280x720	21	1062	724	40	77	123	1720	false
A(inputActive=false)	ryu	1280x720	9	509	385	40	0	0	1168	false
B(inputActive=true)	ryu	1280x720	9	723	385	40	77	123	1381	false
A(inputActive=false)	manon	1280x720	6	449	325	40	0	0	1108	false
B(inputActive=true)	manon	1280x720	6	663	325	40	77	123	1321	false
A(inputActive=false)	blanka	1366x600	21	848	724	40	0	0	1627	false
B(inputActive=true)	blanka	1366x600	21	1062	724	40	77	123	1840	false
A(inputActive=false)	ryu	1366x600	9	509	385	40	0	0	1288	false
B(inputActive=true)	ryu	1366x600	9	723	385	40	77	123	1501	false
A(inputActive=false)	manon	1366x600	6	449	325	40	0	0	1228	false
B(inputActive=true)	manon	1366x600	6	663	325	40	77	123	1441	false
=== END ===

  ✓  18 [chromium] › e2e/m37-02-controller-height.spec.ts:194:7 › M37-02 仮想コントローラの縦寸法(実測) › 系列B manon @ 1366x600 (1.5s)
8e154e8 feat(M37-02/notation): modifier flag 10 値すべてに表示語を与える(B04/B05)
```

**★`stepListVisible` 列は信頼できない**（当時はスクロール位置に依存していた。§2.1b の 1）。
**⇒ 同列は前後比較へ使っていない。**

---

## 付録D. 変更統計（`E-225` の新規ファイル上書き確認）

**★「新規のつもりのファイルが `+` だけか」の観点で読むこと。** deletions のある新規ファイルは
それ自体が矛盾している——**上書きは `go test` を緑のまま通す**（消えたテストは走らないだけであり、
テストも lint も型検査も何も言わない）。**気づける唯一の経路が変更統計の deletions である。**

### 新規ファイル 7 件（すべて `+` のみ・deletions 0）

```text
docs/progress/M37-02-completion-report.md                              +396 -0
internal/service/notation/flagtext_internal_test.go                    +87 -0
web/e2e/m37-02-controller-height.spec.ts                               +277 -0
web/src/features/combo/components/VirtualController/RushToggleRow.tsx  +52 -0
web/src/features/gamepad/components/GamepadInputNotice.test.tsx        +128 -0
web/src/features/gamepad/components/GamepadRecipeReadout.test.tsx      +124 -0
web/src/features/gamepad/gamepad-notice-storage.ts                     +93 -0
```

**⇒ 上書きで消えたファイルは 0 件である。**

### 全体

```text
docs/progress/M37-02-completion-report.md          | 396 +++++++++++++++++++++
 internal/model/combo.go                            |  12 +-
 .../service/notation/flagtext_internal_test.go     |  87 +++++
 internal/service/notation/resolver.go              |  26 +-
 internal/service/notation/resolver_test.go         |   6 +-
 web/CLAUDE.md                                      |   4 +
 web/e2e/m37-02-controller-height.spec.ts           | 277 ++++++++++++++
 .../combo/components/CollapsibleFieldset.tsx       |  13 +-
 .../combo/components/ModifiersEditor.test.tsx      |  28 ++
 .../features/combo/components/ModifiersEditor.tsx  |  10 +-
 .../combo/components/RecipeBuilder.test.tsx        |  75 ++++
 .../features/combo/components/RecipeBuilder.tsx    |  53 ++-
 .../VirtualController/CommonMovePanel.tsx          |  49 +--
 .../VirtualController/ControllerButton.tsx         |  29 +-
 .../VirtualController/DirectSpecPanel.tsx          |  44 ++-
 .../components/VirtualController/HitBoxLayout.tsx  |  31 +-
 .../components/VirtualController/RushToggleRow.tsx |  52 +++
 .../VirtualController/SpecialMovePanel.tsx         | 307 ++++++++++------
 .../VirtualController/VirtualController.tsx        |  40 ++-
 .../gamepad/components/GamepadInputNotice.test.tsx | 128 +++++++
 .../gamepad/components/GamepadInputNotice.tsx      |  69 +++-
 .../components/GamepadRecipeReadout.test.tsx       | 124 +++++++
 .../gamepad/components/GamepadRecipeReadout.tsx    |  16 +-
 web/src/features/gamepad/gamepad-notice-storage.ts |  93 +++++
 web/src/locales/en.json                            |   9 +-
 web/src/locales/ja.json                            |   9 +-
 26 files changed, 1774 insertions(+), 213 deletions(-)
```

### 射程外へ触れていないことの証跡

```text
$ git diff --name-only 38517ef..HEAD | grep -E "followup-backlog|docs/design/|migrations/"
（0 件）
```

**⇒ `followup-backlog.md`（`D-838`）・設計書本体（`CLAUDE.md` §8）・マイグレは、いずれも 1 バイトも触っていない。**

---

*以上、M37-02 完了報告。*
