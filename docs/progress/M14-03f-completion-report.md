# M14-03f 完了報告: 第四波 seed（残り 14 キャラ）

| 項目 | 内容 |
|------|------|
| 対象指示書 | `docs/instructions/M14-03f-fourth-wave-seed.md` v1.0.0 |
| 実施日 | 2026-09-02 |
| 実施担当 | 製造担当 Claude Code（`/implement_plan_full`） |
| 実行ブランチ | `claude/m14-03f-implementation-plan-gi9q9y` |
| 消費マイグレ | **10 本**（`000082`〜`000091`）。**★`000081` は使っていない**（並列タスクが消費・開発者連絡 2026-09-02） |
| CHANGE 消費 | **なし**（設計書を 1 文字も動かしていない） |

---

## 0. 要約

**14 キャラ（未 seed 12 ＋ 仮登録 2）を 1 波で投入した。`characters` は 19 → 31 になり、全ロースターが充足した。**

**★実測が要る 4 群は投入していない**（開発者の運び方＝二段構え）。請求用紙は `docs/progress/20260902-M14-03f-manual-input-list.md`。

---

## 1. ★§0.2 の判定結果（`c_viper` / `dhalsim` の UNIQUE 衝突経路）

### 判定 = **成立しない。「必ず最終波」の制約は失効している。**

**手順**（すべてスクラッチ領域。リポジトリは 1 バイトも変更していない）:

1. `c_viper` / `dhalsim` を**含む** 14 キャラで seedgen を回し、生成物 7 本をスクラッチへ作成
2. `migrations/*.up.sql` 全 80 本（**`000075_m20_preset_aliases_unique` を含む**）を適用したクリーン DB を作成
3. そこへ前提 2 本（`characters` 行・移動 9 種）＋ 生成物 7 本を順に適用

**結果 — 9 本すべて適用成功。UNIQUE 違反 0 件。**

```
base applied: 80
OK 000081_chars / 000082_mov / 000083_seed_moves / 000084_is_derived
OK 000085_move_commands / 000090_aliases_numeric / 000091_aliases_srk
OK 000092_p34_numeric / 000093_p34_srk
characters 31 / moves 3025 / preset_aliases 7587 / move_commands 1635
```
（※ この判定時の連番は仮のものである。確定した連番は §3 を参照）

**⇒ `m20-contract.md` L99 の「先例＝`c_viper` / `dhalsim` が『UNIQUE 衝突で起動不能の経路』として最終波指定されている」は失効している。**

**★波の切り方**: 開発者承認のうえ **14 キャラを 1 波**とした（分割すると段の数が波の数だけ掛かり 26 本前後になる）。

### 仮登録 2 体は削除せず再利用した

開発者の指示は「問題があれば一度削除してから再登録。再利用可能ならそのまま」であった。**再利用可能と判定した。**

| 判断材料 | 実測 |
|---|---|
| UNIQUE 衝突 | **起きない**（上記） |
| 両者の CSV に移動 9 種の行があるか | **0 件**（seedgen の `drop 移動move = 0`）⇒ `UNIQUE(character_id, code)` に当たらない |
| 削除の副作用 | **`characters` を消す down は利用者コンボを orphan にする**（`architecture-patterns.md` §11.Y の実測）。`c_viper` は `custom_states`（`limit_decoupler`）を持ち、削除すると失われる |

---

## 2. 段ごとの結果（★1 段ずつ「投入した／落とした／不要だった」）

**母数 = `M14-RESEARCH-03` §2.4-3 の 13 段 ＋ 条件付き 3 段。**

| 段 | 内容 | 結果 | 実測（母数つき） |
|---:|---|---|---|
| 1 | `characters` 行 | **投入した** | **12 行**（母数 14。仮登録 2 体は既存のため対象外） |
| 2 | 移動 system move 9 種 ＋ `official_ja_move` alias | **投入した** | **108 moves / 108 alias**（母数 12 キャラ × 9 code） |
| 3 | `moves` 本体 ＋ `official_ja_move` alias ＋ `original_move_id` | **投入した** | **1264 moves**（母数 = 14 CSV の全行 1264） |
| 4 | `is_derived` backfill | **投入した** | **519 code**（母数 1264） |
| 5 | `move_commands` | **投入した** | **737 行**（母数 1264 → `command` 非空 **1005** → 非派生 **739**〔派生技は索引に載せない〕→ 語彙外トークンで索引に載らない **2 行**〔`blanka/blanka_chan_bomb_activated` と `_od`〕を除いて **737**） |
| 6 | `is_projectile` backfill | **投入した** | **53 行**（母数 1264）。**0 件のキャラが 6 体**（`alex` `cammy` `dee_jay` `e_honda` `elena` `dhalsim`）＝既定値 0 が正 |
| 7 | 移動 5 code の `total` | **★第二段で投入した**（`000092`・2026-09-02） | **70 行**（14 キャラ × 5 code）。§17 |
| 8 | `chain_cancel_total` | **投入した（一部）** | **2 行**（`elena` の `crouching_light_punch`=10 / `crouching_light_kick`=12）。**残り 13 キャラは未実測のため NULL のまま** |
| 9 | frame_cost（`startup_basis` / `fastest_unreachable`） | **投入した** | 下記 §4 |
| 10 | エイリアス `numeric` | **投入した** | **906 行**（キャラ別）＋ 移動系 108 行 |
| 11 | エイリアス `srk` | **投入した** | **919 行**（キャラ別）＋ 移動系 108 行 |
| 12 | `P-34` `numeric`（`-noinput-only`） | **★不要だった** | 下記 §5 |
| 13 | `P-34` `srk`（同） | **★不要だった** | 同上 |
| +1 | `custom_states` | **★第二段で投入した**（`000093`・2026-09-02） | 第四波 **4 キャラ / 6 状態**。**不要 9 キャラ ＋ 既存 `c_viper`** で 14 キャラすべての要否が確定。§17 |
| +1 | `fastest_unreachable` の人手判断分 | **★段 9 に内包した** | CSV が機械規則の上位集合であったため別段にならなかった（§4） |
| +1 | `move_derivations` | **★投入しなかった** | **0 行**。判断材料が足りない（§6） |

---

## 3. 消費した連番の全数と 1 本ずつの内容

| 連番 | 内容 | 生成手段 | 投入行数 |
|---|---|---|---|
| `000082_seed_characters_fourth_wave` | 未 seed 12 キャラの `characters` 行 | 手書き | 12 |
| `000083_seed_movement_system_moves_fourth_wave` | 移動 9 種 ＋ `official_ja_move` alias（12 キャラ） | 手書き | 108 + 108 |
| `000084_seed_moves_fourth_wave` | `moves` ＋ alias ＋ `original_move_id`（14 キャラ） | seedgen `-mode moves` | 1264 |
| `000085_backfill_moves_is_derived_fourth_wave` | `is_derived` backfill | seedgen `-mode derived-backfill` | 519 |
| `000086_seed_move_commands_fourth_wave` | `move_commands` 索引 | seedgen `-mode move-commands` | 737 |
| `000087_backfill_moves_is_projectile_fourth_wave` | `is_projectile` backfill | 手書き（一次源＝CSV） | 53 |
| `000088_backfill_moves_chain_cancel_total_fourth_wave` | `chain_cancel_total` | 手書き（一次源＝`chain-cancel-measurements.md`） | 2 |
| `000089_backfill_moves_frame_cost_fourth_wave` | `startup_basis` / `fastest_unreachable` | 手書き（一次源＝**CSV**） | **1344** / 118（★`startup_basis` は 964＋380。**`unknown` の 46 行は UPDATE していない**＝DDL 既定値のまま） |
| `000090_seed_aliases_numeric_fourth_wave` | 表記プリセット `numeric` | seedgen `-mode aliases` | 906 + 108 |
| `000091_seed_aliases_srk_fourth_wave` | 表記プリセット `srk` | seedgen `-mode aliases` | 919 + 108 |

### 第二段で消費した連番（2026-09-02・受領データの投入）

| 連番 | 内容 | 投入行数 |
|---|---|---|
| `000092_backfill_movement_total_fourth_wave` | 移動 5 code の `total`（14 キャラ） | **70** |
| `000093_seed_custom_states_fourth_wave` | 第四波 4 キャラの `custom_states` | **6 状態 / 4 キャラ** |
| `000094_seed_custom_states_missing_seeded_chars` | **★第四波スコープ外**。`jamie` / `m_bison` の投入漏れ是正 | **3 状態 / 2 キャラ** |
| `000095_correct_kimberly_shuriken_bomb_max` | **★第四波スコープ外**。`kimberly` の値域 2 → 3（アップデート追随） | 1 セル |

**⇒ 本サブの消費連番は合計 14 本（`000082`〜`000095`）。次に払い出すのは `000096`。**

**★`000081` は使っていない。** 並列タスクが消費するため（開発者連絡 2026-09-02）。`golang-migrate` は連番の穴を許容する（`Next()` はソート済み index の次要素を返す）。

**★ただし 1 点、注意が要る**——**払い出した番号より小さい番号を後から作ると、その 1 本が永久にスキップされ、しかもエラーにならない**（ボード `D-119` / `D-120`）。本波を先に適用した DB に、後から `000081` が現れると `000081` は二度と走らない。**⇒ 開発者の dev DB へ本波を適用する前に、`000081` を先に取り込むこと。** 新規クリーン DB では起きない。

---

## 4. 段 9 の値の出所を CSV に変えた（開発者裁定 2026-09-02）

### 何が違うのか

| 波 | CSV の `startup_basis` / `fastest_unreachable` | 使った一次源 |
|---|---|---|
| 既 seed 17 キャラ | **ほぼ空**（新列 3 本は 2026-08-19 以降の precheck バッチで導入） | `000050` / `000062` の**機械規則** ＋ 人手判断（`000064` / `000067` / `000068`） |
| **第四波 14 キャラ** | **完全に埋まっている**（空欄 **0 件**・実測） | **CSV** |

### 機械規則との突合（★規則を捨てたのではなく、上位集合であることを確かめた）

**★全 1264 行を 1 行ずつ突き合わせた。**

| 列 | 実測 |
|---|---|
| `fastest_unreachable` | **CSV は機械規則（code に `jumping_` を含む）の厳密な上位集合**。`jumping_` を含むのに CSV が false の行は **0 件**。CSV が true で `jumping_` を含まない行が **23 件**あり、これが B 型（`000064` B / `000068` が人手判断で扱っていた類）である |
| `startup_basis` | **★こちらは「上位集合」ではない。CSV は機械規則を *是正* している**（下記） |

**`startup_basis` が機械規則と食い違う行 = 279 行。内訳:**

| 規則 → CSV | 行数 | 意味 |
|---|---:|---|
| `unknown` → `standalone` | **131** | 規則が埋められない行を CSV が埋めている |
| `unknown` → `through` | **110** | 同上 |
| **`standalone` → `through`** | **28** | **★規則が *誤る* 行** |
| `standalone` → `unknown` | **10** | CSV が「自動提案に安全に使えない」と判定した行 |

**★★28 行は「規則を当てると誤った値が入る」型である。内訳は 3 つに割れる:**

| 型 | 行数 | 顔ぶれ |
|---|---:|---|
| **ジャンプから通して測った値** | **23** | `akuma/zanku_hadoken` 4 ／ `blanka/aerial_rolling_attack` 4 ／ `cammy/cannon_strike` 4 ＋ `sa2_aerial_killer_bee_spin` 1 ／ `chun_li/yoso_kick` 1 ＋ `aerial_hundred_lightning_kicks` 4 ／ `dee_jay/knee_shot` 1 ／ `dhalsim/yoga_comet` 4 |
| **地上必殺技からの派生の通し値** | **4** | `ed/psycho_shoot_{light,medium,heavy,od}`（`notes_tool`「サイコスパーク派生」） |
| **★根拠を確かめられなかった** | **1** | `chun_li/wall_jump`（`notes_tool` が空欄）。**値は CSV に従った**。§13 に再掲する |

**★`is_aerial` では説明できない。** 28 行のうち **26 行は `is_aerial=false`** であり、true は 2 行だけ（`chun_li/yoso_kick` ／ `dee_jay/knee_shot`）。**「空中技だから」ではない。** いずれも `is_derived=0` のため機械規則は `standalone` と判定するが、実際は通し値である。

**★誤ってもエラーにはならない。** `M19-05` の費用規則が静かに誤った分類で回るだけである。

**⇒ 機械規則だけを踏襲していたら、241 行が `unknown` のまま取り残されたうえに、28 行に誤った値が入っていた。**

### 投入結果（母数 = 第四波の全 moves 1390 行）

| 値 | 行数 |
|---|---:|
| `startup_basis = 'standalone'` | **964**（CSV 由来 838 ＋ 移動 system move 126） |
| `startup_basis = 'through'` | **380** |
| `startup_basis = 'unknown'`（★CSV が明示的に unknown と書いた行。埋めるのが正ではない） | **46** |
| **合計** | **1390**（3 値で全行を分割している） |
| `fastest_unreachable = 1` | **118**（機械規則の 95 ＋ B 型 23） |

### `c_viper` / `dhalsim` の既存移動 18 行も `standalone` にした

`000050` が両者を明示除外したのは「移動 system move のみを持ちフレーム列が全 NULL で、その startup がどちらの意味かを語る対象が存在しない」ためである（同マイグレのヘッダの逐語）。**本波で両者が実技を持つため、この理由は消える。** `000062` の逐語も「実技を持つキャラは除外に当たらない」と書いている。**この判断は `000089` のヘッダに書いた。**

---

## 5. 段 12/13（`P-34`・層 C-3）が不要だった理由

**★0 行だったのは「データが空だから」ではない。** 層 C-3 の対象は `noInputDerivedTargets`（`generate_m2002.go`）という**固定表**であり、**新 14 キャラの行が 1 つも無かった**ためである。

**第四波の CSV には条件（`command` 空欄 かつ `is_derived=true`）に当たる行が 11 件ある。** 開発者判断（2026-09-02）で **5 件を固定表へ足した**。

| 追加した 5 件 | 根拠（`notes_tool` の逐語） |
|---|---|
| `cammy/razors_edge_slicer` / `_od` / `_holding` | **「フーリガンから何もしなかったら出る技。フーリガン強度不問」** |
| `akuma/demon_low_slash` / `_od` | 「弱中強百鬼襲から派生」／「OD百鬼襲から派生」 |

| 外した 6 件 | 理由 |
|---|---|
| `akuma/gou_hadoken_max_holding_{light,medium,heavy}` | ボタンを押し続けている（何も押さずに待つのとは違う） |
| `aki/sinister_slide_cancel` ／ `chun_li/serenity_stream_cancel` | **上入力が要る** |
| `alex/prowler_stance_cancel` | 解除操作であり `notes_tool` に入力の記述が無い |

**⇒ 外した 6 件は `numeric` / `srk` で `official_ja_move`（日本語技名）へフォールバックする。画面は壊れない（`DES-004` §5.3）。**

### ★★`-noinput-only` の 2 本が要る条件を確定させた

**`000076` / `000077` は、`000072` / `000073` が `M20-06` より前に生成されたことの追補である。** 生成器は通常実行でも層 C-3 を解決して出力し、`-noinput-only` は**出力を絞るだけ**である（`generate_m2002.go` の `runAliases`）。

**⇒ `M20-06` 以降に起こす波では、層 C-3 は `-mode aliases` の通常実行に含まれる。別マイグレは要らない。** 実測でも `000090` / `000091` に C-noinput 5 行が入っている。

**この条件は `character_data/seed-progress.md` へ書き足した。**

---

## 6. `move_derivations` を投入しなかった理由と、代わりに出したもの

**開発者の問い**＝「新投入分だと `notes` に書いているが、そこから作れないか」。**実測で答える。**

| 事実 | 実測 |
|---|---:|
| 対象（非 rush の派生技） | **277 件** |
| rush_variant（`original_move_id` で解決済み＝対象外） | 242 件 |
| `notes_tool` に親らしき記述がある | **132 件（48%）** |
| **手がかりがゼロ** | **113 件** |
| **抽出結果が自分自身になった**（`notes_tool` の技名が誤っている可能性） | **4 件** |

**★機械では確定できない理由は 2 つある。**

1. **親は日本語の技名で書かれている**（例「Sinister Slideから派生」「阿修羅閃空(前方)から派生」）。`move_code` への写像は機械では決まらず、**誤っても SQL はエラーにならない**（`SUPP-001` §5.5 (4)）。
2. **`move_derivations` は「許可される親をすべて」登録する表である**（`moves-input-background.md` §3.3）。「Xから派生」は「X が親の 1 つ」を示すが「**X だけが親**」かどうかを示さない。既存 88 行はいずれも開発者の「〜からのみ派生」という**断定**から作られている（`000064` の D ブロック）。

**⇒ 候補表（277 件・確信度つき）を `docs/progress/20260902-M14-03f-manual-input-list.md` 群4 に出した。**

> **【2026-09-04 解消】第四段 `000101` で投入した**（§19）。**候補表 277 件のうち投入は 169 件（61%）で、108 件は「行を作らない」と裁定された。** ★「機械提案をそのまま入れなくてよかった」ことが数字で確認できた——**ターゲットコンボ・強化版の別技・ホールド版の独立技が候補に紛れており、`notes_tool` と `move_code` 接頭辞だけでは弁別できていなかった。** ◯×を返してもらってから投入する（開発者選択 2026-09-02）。

---

## 7. ★★§2.3 の取り残し検査（列ごと・母数つき）

**母数 = 第四波が INSERT した `moves` 1390 行（CSV 由来 1264 ＋ 移動 108 ＋ 仮登録の既存 18）。**

| 列 | 既定値のまま残った行数 | **意図しない取り残しか** |
|---|---:|---|
| `is_projectile` | 1337（＝1390 − 53） | **いいえ（意図どおり）**。CSV で false の行であり、既定値 0 が正 |
| `is_derived` | 871（＝1390 − 519） | **いいえ（意図どおり）**。同上 |
| `startup_basis = 'unknown'` | **46** | **いいえ（意図どおり）**。CSV が明示的に unknown と書いた行（`elena` のリンクスワール系ほか）。埋めるのが正ではない |
| `fastest_unreachable = 0` | 1272（＝1390 − 118） | **いいえ（意図どおり）** |
| **移動 5 code の `total` が NULL** | **0**（第一段の時点では 70） | **いいえ（★第二段 `000092` で解消）**。**全 31 キャラで移動 5 code が埋まった** |
| `chain_cancel_total` が NULL | 1352（第一段の時点では 1388） | **いいえ（★第三段 `000099` で解消）**。**第四波 13 キャラの 38 行が入り、全 31 キャラ 93 行が揃った**。残る NULL はチェーングループ外の技であり、NULL のままが正（非 NULL であること自体がグループ員の意味を持つ） |
| `custom_states` が NULL（`characters`） | **9**（第一段の時点では 13） | **いいえ（★第二段 `000093` で解消）**。**残る 9 キャラは開発者が「不要」と確定した側**であり、NULL のままが正 |
| `move_derivations` の行 | **0**（第一段の時点） | **いいえ（★第四段 `000101` で解消）**。**子 169 件 / ペア 313 行**。残る 108 件は「行を作らない」と裁定された側であり、0 行が正 |
| `preset_aliases.character_id` が NULL | **0** | **いいえ（0 件が正）**。手書き `000083` も含め全行に入っている |
| **`numeric` / `srk` の alias を持たない move** | numeric 358 / srk 345 | **いいえ（意図どおり）**。生成器が投入しないと判定した行（内訳＝`derived` 266 / `collision` 52・44 / `rush-original-unfilled` 26・21 / `derived-no-command` 6 / `no-command` 6 / `index-skip` 2）。`official_ja_move` へフォールバックする |

**⇒ 意図しない取り残しは 0 件である。**

---

## 8. ★★実装中に見つけた欠陥（自分で作り、自分で見つけた）

### `000083` が `000080` の是正を巻き戻していた

**`000083`（移動 9 種の alias）を `000054`（第三波）から写したところ、`000080`（`M24-08` / `D-615`）が是正した旧表記「前ダッシュ / 後ろダッシュ」を 12 キャラへ入れ直していた。**

- **`000080` は 1 回きりの UPDATE であり、後から INSERT される行を拾わない。** `000080` は `000083` より前に走るため、**二度と直らない。**
- **★取り残してもエラーにならない。** alias 行そのものは在るのでフォールバックも起きず、**画面に古い表記が出るだけ**である。テストも lint も型検査も緑のままだった。
- **見つけた経路**: 追加した対テスト（`c_viper` / `dhalsim` が本 seed 済みになったことを主張する側）が「移動系エイリアスが無い」で落ち、実データを引いて気づいた。

**★一般形 — `000062` ヘッダの一般化は backfill だけでなく「是正マイグレ」にも当たる。**

指示書 §2.3-1 は「**既存の backfill マイグレ**を 1 本ずつ読み、その内容を第四波の行にも適用すること」と書いており、私は backfill だけを数えていた。**是正マイグレ（`*correct*` / `*fix*`）は数えていなかった。**

**是正マイグレの全数実査**（`grep -l "UPDATE preset_aliases\|UPDATE moves" migrations/*.up.sql`）:

| マイグレ | 第四波への当たり |
|---|---|
| **`000080_fix_ground_dash_label`** | **★当たる**（キャラを絞っていない）⇒ 是正した |
| `000079_fix_character_display_names` | 当たらない（`mai` を名指し） |
| `000063` / `000065` / `000066` / `000068` | 当たらない（いずれも既 seed キャラを名指しで絞っている） |
| `000018_cleanup_moves_unobservable_columns` | 当たらない（`raw_data` の一時整理。seedgen は `{"notes_tool":...}` を意図して書いており、既 seed 17 キャラにも 68 行ある） |
| `000074_m20_preset_aliases_add_character_id` | 当たらない（新規行は `character_id` を明示投入済み。NULL 0 件で確認） |

**⇒ `seed-progress.md` へ「是正マイグレも再適用の対象である」と、この実査手順を書き足した。**

**⇒ 31/31 のキャラで「前方ステップ / 後方ステップ」に揃っていることを `migrate_m1403f_test.go` で固定した**（旧表記が 0 行であることも対で主張する）。

---

## 9. `seed-progress.md` を補った内容（指示書 §2.4）

1. **手順に含まれていなかった 8 件**の表（`characters` 行 ／ 移動 9 種 ／ `is_projectile` ／ 移動 `total` ／ `chain_cancel_total` ／ frame_cost ／ `custom_states` ／ `P-34` の 2 本）と、第四波での実績。
2. **★#8 の条件**——`-noinput-only` の 2 本が要るのは `M20-06` より前に生成した波だけである（§5）。**あわせて「固定表に自分の波のキャラが載っているか」は別問題であることを明記した。**
3. **★#6 の値の出所は波によって違う**——次の波では、まず CSV の両列が埋まっているかを実測すること（§4）。
4. **★★是正マイグレも再適用の対象である**——第四波が実際に踏んだ事例と、全数実査の手順（§8）。
5. **`seed_imported` を 14 キャラぶん 未 → 済 へ更新**（31 キャラすべてが 済/済 になった）。

---

## 10. down マイグレの判断（指示書 §2.5 / `architecture-patterns.md` §11.Y）

**手書き 5 本の down すべてに、§11.Y の 3 点を本文へ明示した。**

| # | 明示したこと | 本波での判断 |
|---|---|---|
| 1 | 子行を明示 DELETE するか | **`000082`（characters）はしない**——seed 由来の子行は `000083`〜`000091` の down が先に除去する（golang-migrate は連番の降順）。**`000083` はする**——FK=OFF で CASCADE が発火しないため `preset_aliases` を先に消してから `moves` を消す。`000087` / `000088` / `000089` は UPDATE のみで行を削除しない |
| 2 | **利用者データを消すか、残すか** | **★残す。** `000082.down` は `characters` の 12 行だけを消す。**⇒ 当該 12 キャラの利用者コンボは親を失った orphan として残る。これは意図である**（down で利用者データを消すほうが被害が大きい）。**★注記に書いた**——書かないと、次の読み手は既存の「依存行は除去済み」を利用者データまで含むと読む |
| 3 | up と同じキーで消す | `000082` は `(game_id, code)`、`000083` は `(c.code IN 12 キャラ) × category='system' × 移動 9 code`。**★`c_viper` / `dhalsim` を混ぜていない**——混ぜると `000025` が入れた前の波の行まで消える |

### `move_derivations` の明示削除責務（指示書 §2.5-3）

**本波は `move_derivations` を 1 行も入れない。⇒ `000084.down` は触れない。この判断理由を down のヘッダに書いた。**
第二段で投入する場合、そのマイグレ自身の down が自分の入れた行を消す（`000064` / `000067` と同型）。head からの順次ロールバックでは、そちらが先に走る。

---

## 11. テスト

### 追加したもの

| ファイル | 内容 |
|---|---|
| `internal/seedgen/generate_m1403f_test.go`（新規 322 行） | **golden 5 本**（moves / derived / move-commands / aliases numeric / srk）＋ 変換不変条件 ＋ 生成の実測値（**投入行と非投入行を対で固定**） |
| `internal/infra/migration/migrate_m1403f_test.go`（新規 406 行） | 段ごとの投入件数固定（キャラ別 14 ＋ 合算）／**★§2.3 の取り残し検査**／**★`000080` 是正の再適用検査**／層 C-3 の 5 件と外した 6 件／`elena` の値そのもの／既 seed 17 キャラ非波及／down → re-up |

### キャラ数 19 → 31 に伴う既存テストの追随（Go 8 ファイル ＋ E2E 3 spec ＝ 11 件）

**★いずれも母数の変化であり、規則は変わっていない。**

| ファイル | 変更 |
|---|---|
| `internal/infra/migration/migrate_test.go` | characters 19→31 ／ `aki` 再追加で 2→3 ／ numeric alias 1258→2272 ／ srk 1273→2300 |
| `internal/aliasindex/m2207_baseline_test.go` | 母集合 X 76→124（確定 53→98 / 候補 15→26 / **なし 8→0**）／ 緩和衝突組 17→31 |
| `internal/aliasindex/m2207b_third_stage_test.go` | `preset_aliases` 4184→7597 ／ super_art 79→138 / critical_art 17→32 |
| `internal/repository/movecommand/repository_test.go` | CharKeys 17→31 |
| `internal/service/punishfinder/service_test.go` | legacy 37→65 / 案 C 39→71 / unique 空中技 16→32。**★案 C の増分は列挙で固定した**（件数だけだと入れ替わりを検出できない） |
| `internal/seedgen/generate_m2006_test.go` | 層 C-3 の対象 13→18。**★追加 5 件と、外した 6 件の不在を対で主張する** |

### ★前提そのものが消えた 2 件は「消す」のではなく fixture で作り直した

**全 31 キャラが本 seed 済みになったため、配布 DB から「moves を持たないキャラ」が消えた。** 2 つのテストがその状態を実データ（`c_viper`）に頼っていた。

| テスト | 対応 |
|---|---|
| `internal/api/intake/handler_test.go` の `TestResolve_UnseededCharacter_AllUnresolved` | **`TestResolve_MovelessCharacter_AllUnresolved` へ改名し、テスト側で `characters` 行だけのキャラを作る。** 守っている不変条件（moves が無くても 500 にせず全行未解決で 200 を返す）は今も要る——ゲーム更新で新キャラが増えると、`characters` 行だけが先に入る期間が必ずできる（先例＝`c_viper` / `dhalsim` が `000014` から本波まで仮登録だった） |
| `internal/repository/preset/list_alias_entries_by_character_test.go` の `...MovesetlessCharacterStillHasMovementAliases` | **同じく fixture で作る**（`characters` 行 ＋ 移動 2 code ＋ その alias）。**あわせて `c_viper` / `dhalsim` が本 seed 済みになったことを主張する対テストを追加した** |

---

## 11.5 変更統計（着手基点 `78f4981`）

```
44 files changed, 10293 insertions(+), 111 deletions(-)
```

**新規 25 ファイル**（マイグレ 20 ／ テスト 2 ／ 文書 3）**・既存 19 ファイルの編集**。

**★`git diff --numstat` で「新規のつもりのファイルが `+` だけか」を確認した**（教訓 `E-225`）。
**新規 25 ファイルはすべて deletions 0 である。** deletions を持つ 19 ファイルはいずれも意図して編集した既存ファイル（テストの追随 8 ／ `generate_m2002.go` ／ `cmd/seedgen/main.go` ／ `character-sort.ts` ／ E2E spec 3 ／ `seed-progress.md` ／ `code-facts.md` ／ `progress-log.md`）である。

---

## 12. 検証の実測

### `go test ./...`

| 時点 | 結果 |
|---|---|
| **着手前ベースライン** | **55 パッケージ ok / FAIL 0**（`internal/infra/migration` 49.0 秒） |
| 第四波 投入後 | **55 パッケージ ok / FAIL 0** |
| レビュー取り込み後 | **55 パッケージ ok / FAIL 0** |
| **第二段投入後** | **55 パッケージ ok / FAIL 0** |

### クリーン DB への全マイグレ適用

```
applied 90 migrations OK
characters 31 / moves 3025 / preset_aliases 7597 / move_commands 1635
```

**★完了条件 6 が求めた `preset_aliases` の投入前後の差:**

| 時点 | 行数 |
|---|---:|
| 投入前（`000080` まで） | **4184** |
| 投入後（`000091` まで） | **7597** |
| **差** | **＋3413** |

**内訳**: `official_ja_move` 1390（＝第四波の全 moves。`000083` の 108 ＋ `000084` の 1282）／ `numeric` 1014（キャラ別 906 ＋ 移動系 108）／ `srk` 1027（キャラ別 919 ＋ 移動系 108）＝ 3431。**★差 3413 との開き 18 は `c_viper` / `dhalsim` の移動系 alias**（`numeric` 9 ＋ `srk` 9）で、これは `000072` / `000073` が既に投入済みのため新規に増えていない。

### `-alias-report`

2 プリセットとも出力し、投入しない行の内訳を確認した（§7 の最終行）。**`-alias-report` に出ない衝突（同一キャラ内で 2 技が同表記）は `go test ./internal/infra/migration/` まで回して緑を確認した。**

### `make e2e`

| 時点 | 結果 |
|---|---|
| **着手前ベースライン** | **226 passed / 0 failed**（60 spec・5.9 分） |
| 第四波 投入直後 | **223 passed / 3 failed**（母数 226 は不変）。**★3 件とも前提の消失であり、seed の欠陥ではない** |
| spec 是正後 | **226 passed / 0 failed**（5.3 分） |
| レビュー取り込み後 | **226 passed / 0 failed**（5.4 分） |
| **第二段（`000092`〜`000095`）投入後** | **226 passed / 0 failed**（4.0 分） |

**★既知の flakiness の判定は不要だった**——落ちた 3 件はいずれも 2 回（初回 ＋ retry #1）とも同じ理由で落ちており、単独実行でも再現した。**⇒ flaky ではなく実際の前提消失である。**

**落ちた 3 件と対応:**

| spec | 何が失効したか | 対応 |
|---|---|---|
| `m12-05-seed-cleanup-and-controller-resolve` | 「`aki` は出ず」——`aki` は `000082` で再追加された | **主張を「`000017` で除いた 3 体（`aki` / `guile` / `jamie`）は後続 seed で再追加され、ロースターは 31 体」へ書き換えた。** 重複が無いことも同時に見る |
| `m17-03-stage2-input` | 「未 seed キャラ（`c_viper`）: 解決表が空でも画面が壊れない」 | **「第四波で本 seed 済み: 解決表が効き、しゃがみ通常技が出る」へ書き換えた**（seed が画面まで届いたことの主張になる） |
| `m17-04-intake-helper` | 「未投入キャラは全行未解決として提示される」 | **「照合が通り、未投入の案内は出ない」へ書き換えた** |

**★`m17-03` / `m17-04` は画面側の被覆を 1 つ失った。** 「moves を持たないキャラでも壊れない」を E2E で組むには**キャラを差し込む手段**が要るが、E2E スタックは `-tags=debug` を使わないため spec からは差し込めない。**バックエンド側の同じ不変条件は Go のテストが fixture 付きで守っている**（§11）。**⇒ §15-7 に followup 候補として記録した。**

---

## 13. ★確かめられなかったこと（推測で埋めていない）

| # | 項目 | 理由 |
|---|---|---|
| 1 | ~~**12 キャラの表示名がインゲーム表示と一致するか**~~ **【2026-09-05 解消】開発者がインゲーム表示と突合して 12/12 合格。** `characters.name_ja` / `name_en` を書くのは migrations だけであり、リポジトリ内に正本が無い（`000079` の本文）という**構造の問題は残る**——**次の波でも同じ目視突合が要る**（§14-25） |
| 2 | ~~**`move_derivations` の親子**~~ **【2026-09-04 解消】** 第四段 `000101` で投入（§19）。**候補表を出したのみ**だった状態は終わった |
| 2b | ~~**`chun_li/wall_jump` の `startup_basis='through'` の根拠**~~ **【2026-09-04 是正・根拠はある】** ★本報告の初版は「`notes_tool` が空欄で機械規則（`standalone`）と食い違う理由を説明できない」と書いたが、**`rashid/wall_jump` が `through` ＋親なしで先行投入されている as-built の先例**であり、`chun_li/wall_jump` はそれに揃っているだけである。**「確かめられなかったこと」ではない。** 値は CSV に従った（CSV が一次源という裁定どおり）。★ただし「壁ジャンプが `through` である設計上の理由」そのものは依然どこにも書かれていない——これは §14 の設計卓の手番として残す |
| 3 | ~~**移動 5 code の `total`** / **`chain_cancel_total` の残り 13 キャラ** / **`custom_states`**~~ **【解消】** 移動 `total` と `custom_states` は第二段（2026-09-03）、`chain_cancel_total` は第三段（2026-09-04）で投入済み。**残るのは `move_derivations` の候補表 277 行の ○/× 回答だけ**（＝上の 2） |
| 4 | **開発者の dev DB・配布済み DB の状態** | 本サブの実測はすべて新規クリーン DB である |
| 5 | **`pre-dist-e2e-clean`（クリーン DB での全 E2E）** | **指示書 完了条件 9 で本サブの範囲外と明記されている** |

---

## 14. ■ 併せて更新が要るもの

| # | 項目 | 状態 |
|---|---|---|
| 1 | **消費した CHANGE 番号の登録** | **なし**（本サブは CHANGE を起票していない）⇒ `change-number-registry.md` §1 への登録は不要 |
| 2 | **消費したマイグレ連番** | **`000082`〜`000091` の 10 本。★`ls migrations/` の実査値は末尾 `000091`。** ⇒ **`change-number-registry.md` §1 の「次に払い出すマイグレ連番」／ボード §2.2 ／ボード §1 冒頭【採番の現在地】の 3 か所が `000081` のまま失効する**（`E-114` / `E-191`）。**★製造は同書を編集しない**（`D-382`）⇒ 設計卓の手番として §15 に候補を出す。**★`000081` は並列タスクが使うため、次の空きは `000092` である** |
| 3 | **版を上げた文書の参照元** | **なし**（版を持つ文書を上げていない） |
| 4 | **`character_data/seed-progress.md`** | **更新した**（§9） |
| 5 | **`docs/progress/progress-log.md`** | **索引行を追記した** |
| 6 | **`docs/handover/code-facts.md`**（派生資料） | **再生成した**（`scripts/generate-code-facts.sh`）。マイグレ棚卸しが `000080` で止まっていたため、`000082`〜`000091` を反映 |
| 7 | **`web/src/lib/character-sort.ts` の「既知の例外 2 つ」の実測値** | **更新した**。19 体時点の「漢字 1 件 / ラテン始まり 2 件」→ **31 体時点の「漢字 4 件 / ラテン始まり 4 件」**（`e_honda` は両方に当たるため実体 6 件）。**★挙動は変わっていない。変わったのは母数であり、2026-08-26 の受容裁定を維持するかは開発者の手番** |

| 8 | **★★`DES-003` §3.2 の「設置技 → subject: self（JP・ベガ・ブランカ 等）」** | **★是正が要る（設計卓の手番）**。2026-09-02 の開発者裁定でベガ「サイコマイン付与」は **`opponent`** と確定した（§17）。**★CHANGE 起票の候補。製造は `docs/design/` を編集しない**（`CLAUDE.md` §8） |
| 9 | **★第四波スコープ外を 2 件直した（設計卓未連絡）** | `000094`（`jamie` / `m_bison` の `custom_states` 投入漏れ）と `000095`（`kimberly` の値域 2→3）。**★開発者が後日連絡する**（開発者の逐語）。⇒ 設計卓が受け取るまで台帳に載らない |
| 10 | **消費したマイグレ連番（第二段ぶん）** | **`000092`〜`000095`。⇒ 本サブの合計は 14 本（`000082`〜`000095`）。次に払い出すのは `000096`**（`000081` は並列タスク用に空けたまま） |
| 11 | **消費したマイグレ連番（第三段ぶん）** | **`000097`〜`000099`。★`000096` は並列タスクが消費した（開発者連絡 2026-09-04）。⇒ 本サブの合計は 17 本。次に払い出すのは `000100`** |
| 12 | **★★`M19-DESIGN-07` が 2 系統に分岐していた** | **★設計卓の手番。** 設計卓から受領した版は自称 **v1.0.9** で、版歴が **v1.0.6** 〔2026-08-01〕→ **v1.0.7** 〔08-31〕と繋がっており、本リポジトリが辿った **v1.0.7 → v1.3.0 →…→ v1.8.1**（2026-08-03〜08-10 の反映レーン）を知らない**別系統**だった。そのまま上書きすると **是正 8 件が巻き戻る**——§2-1 の `G = N − active`（**D-24／D-38**。ボード **M-13** が一度潰した撤回済み定義 `G = 1 − N` の復活）／§4-1 の「CSV に列を持つ」（**D-36**）／§4-2 の人手 308 行／§4-3・§10-5 の B 型 16 行と人手判断結果（**D-207／D-236**）／§5 項目 3 の M19-05 errata（**D-274**）／§5 項目 4 の述語 `IN ('standalone', 'unknown')`（**CHANGE-093／D-227**）／§9-3 の向き付け注記（**D-224**）／§10 項目 1 の決着。⇒ **開発者裁定（2026-09-04）により、製造が新規差分 4 箇所だけを v1.8.1 へ移植して v1.9.0 とした。** **設計卓は以後 v1.9.0 を基底にすること**（`parallel-board.md:468` と `M19-overview.md:214` も v1.8.1 系で追跡していた） |
| 13 | **受領版 `M19-DESIGN-07` §9 の読み数が内部不整合** | **★設計卓の手番。製造は逐語で移し、直していない。** 当該項目は「全 **13** 読み（クロス 6・自己 3・3 連 4）」と書くが、同 §10・成果表 v2.6.1・別紙はいずれも「**14** 読み」であり、別紙の表も 14 行ある（B1–B3／C1–C6／R1–R4／D）。**§9 側が R1（同一構成再読）を数え落としている** |
| 14 | **★設計卓の「§9-6」は本系列では「§9 の項目 8」** | 受領版の §9 は 5 項目までしか無く、本系列は既に 7 項目あったため採番がずれた。**`SF6セットプレイ-ドメイン知識集成.md` §3-5 の参照 1 箇所を「§9 の項目 8」へ直した**。設計卓側の他資料にも同じ参照があれば要追随 |
| 15 | **`chain-cancel-worklog.md` は参照のみで実体が無い** | 成果表の冒頭が「検証の過程・棄却した読み・判断根拠は `chain-cancel-worklog.md` に全記録」と書き、v2.6.1 では §4-1／§5-6／§6／§6-2／§7-4 と参照が増えたが、**同ファイルはリポジトリに一度も存在したことがない**（設計卓の手元資料）。開発者に確認済みで、**配置対象外**と確定（2026-09-04）。リンク切れの参照が残ることを承知のうえで現状維持 |
| 16 | **`M19-OPS-01` の配置** | **`docs/seed-data/` へ置いた**（開発者裁定 2026-09-04 の選択肢のうち製造が性質で判断）。同ディレクトリ README が「手入力 seed データの…**入力支援**…に使う参照資料の正本置き場」と自己宣言しており、OPS-01 は**値を持たない実行手順書**（値の正本は成果表）なので一致する。`character_data/` は seed データ本体と値の正本の場所。**README の収録ファイル表へも 1 行足した**（置くだけでは台帳から漏れる）。**`check-doc-inventory.sh` の走査対象（docs 直下 4 ディレクトリ）外なので §10.Y の新種検出には掛からない** |
| 17 | **`chun_li/wall_jump` が `through` である設計上の理由** | **★設計卓の手番。** as-built の先例（`rashid/wall_jump`）はあるが（§13-2b で是正）、**「壁ジャンプが `through` である理由」そのものはどの設計書にも書かれていない**。第五波以降で同型が出たとき機械規則（`standalone`）との食い違いを毎回説明できない |
| 18 | **消費したマイグレ連番（第四段ぶん）** | **`000100`〜`000101`。⇒ 本サブの合計は 19 本。次に払い出すのは `000102`** |
| 19 | **★★`M19-DESIGN-07` の `csv-edit-vs-golden-divergence` が 2 例目を得た** | **★followup 本表の更新候補（設計卓の手番）。** 同 followup は「M19-04c がその最初の実例」と書いているが、**本サブが 2 例目**である。**M19-04c で得た 5 つの実務知見はいずれも有効だった**——①`NOT EXISTS` ガード（本件では新規 DB で 0 行・既存 DB で 1 行になり衝突しなかったが、ガードは付けた）／②golden に載る列と載らない列の切り分け（本件は `startup_basis` が CSV も DB も `unknown` で追随不要と実査で確定）／③「grep ヒット × seedgen が生成していない」の 2 条件（手書き 3 本に旧 code は 0 件）／④件数ガードの版（既存 `move_derivations` は **74 ではなく 90** だった。§19-5）／⑤down の非対称性。**★加えて本サブで判別述語が明文化された**——**`D-336`＝「適用済みマイグレは改変しない」と三点更新は上下ではなく適用対象が違う。判別の述語＝そのマイグレは生成物か手書きか。** |
| 20 | **★製造が自己参照 3 件を外した（裁定の逐語には無い判断）** | **★報告事項。** `elena/lynx_whirl_{light,medium,heavy}` の親リスト（「リンクスワール系・スピンサイズ派生版含む」9 code）に、各子が自分自身を含んでいた。**開発者裁定「`elena/lynx_whirl_od` は自己参照なので行を作らない」と同じ原理**で、自己参照の 1 組だけを外し残る 8 親は投入した。**親リスト全体を捨ててはいない。** ⇒ 316 − 3 = **313 行** |
| 21 | **★「通常ジャンプ攻撃に `jump_*` 親を付けない」の根拠が資料のどこにも無い** | **★設計卓の手番。** 2026-09-04 に開発者が裁定したが、**`M19-DESIGN-07` にも `DES-003` §3.3 にも「ジャンプ攻撃を `move_derivations` の子にしない」根拠は書かれていない。** 第四波では②（ジャンプ前提の必殺技）に `jump_*` 親を付けたので、**「①は付けず②は付ける」という線引きが資料に無いまま実装だけ在る状態**になった。**★①は `is_derived=false` のため候補表にも載らない**——**「候補表に無い＝判断済み」ではない**という穴でもある。製造は `TestRun_M1403f_JumpNormalsHaveNoParent` と `character_data/seed-progress.md` #9 に落としたが、**設計正本側の記述が要る** |
| 22 | **消費したマイグレ連番（第五段ぶん）** | **`000102`。⇒ 本サブの合計は 20 本。次に払い出すのは `000103`** |
| 23 | **★★`chain_cancel_total` を CSV に書く裁定へ変わった（`D-94` / `D-99` を覆す）** | **★設計卓の手番。** `000052` のヘッダは逐語で「**CSV にも書くと同じ値が 2 か所に存在してどちらが正か分からなくなるため転記しない。CSV では列だけ用意して値は素通しする**」と書いている。**2026-09-04 の開発者裁定でこれを覆した**——理由は「アップデート対応は CSV を外部ツールで取込 → 修正 → **差分のみマイグレで取込**となるため、CSV 側だけが陳腐化していると問題」。**整理**＝値の正本は引き続き `character_data/chain-cancel-measurements.md`（測定と多重検算の記録を持つのはこちら）、**CSV は運搬役**。**2 か所に存在する問題は `TestCSVAndDBAgreeOnFrameCostColumns` で解いた**。**★適用済みの `000052` のヘッダは改変していない**（`D-535`）ので、**マイグレのヘッダと現行運用が食い違ったまま残る** |
| 24 | **★3 列が陳腐化していた理由＝テストが無かった** | **★followup 本表の候補。** `startup_basis` 1186 セル・`fastest_unreachable` 1445 セル・`chain_cancel_total` 89 セルが CSV 側で空だった。**`seedgen` はこの 3 列を「読むだけで SQL へ出力しない」**（`csv.go`「保全のみ・SQL 非投入」）ため、**golden が守ってくれない領域**である。⇒ `TestCSVAndDBAgreeOnFrameCostColumns` を新設した。**値を揃えるだけでは再発する** |
| 25 | **★開発者の手動確認 4 件がすべて合格した（2026-09-05）** | （i）**実アプリのセットプレイ提案**＝C.ヴァイパー（KA=54・target を「しゃがみ弱K」へ固定）で提案 146 件・除外 12 件は 1 つも出ず／イングリッド（KA=68）で **`solar_burst` 14 行が 1 行も出なかった**＝`000102` の直接の検証。（ii）**12 キャラの表示名**をインゲーム表示と突合＝12/12 合格（§13-1 を解消）。**ただし構造の問題は残るので followup は開いたままにする**——`characters.name_ja` の正本がリポジトリに無い以上、**次の波でも目視が要る**。**⇒ 波の指示書の完了条件へ「表示名の開発者突合」を入れてほしい（設計卓の手番）**。（iii）**外部ツールでの CSV 往復＝3 列が保持されることを確認**。⇒ §14-23 の書き戻しは目的を果たす（**次の取込で DB を NULL 化する事故を実際に防ぐ**）。（iv）ダルシム連打版（`damage`/`on_hit`/`on_block` が NULL）の画面表示に崩れなし |

### ★★§0.2 の判定で失効した記述（設計卓の手番。★製造は編集していない）

**`c_viper` / `dhalsim` の「必ず最終波」制約が失効した**（§1）。**同じ前提を書いている箇所が 5 か所ある。**

| # | 場所 | 何が失効したか |
|---|---|---|
| 1 | **`docs/process/m20-contract.md` L99** | 「先例＝`c_viper` / `dhalsim` が『UNIQUE 衝突で起動不能の経路』として最終波指定されている」。**★UNIQUE の投入順序に条件があること自体は失効していない**——失効したのは「その先例が現に成立している」という部分である |
| 2 | **`docs/process/parallel-board.md` L212** | 「c_viper / dhalsim は必ず最終波（UNIQUE 衝突で起動不能の経路）」 |
| 3 | **同 L208 / L214** | 「必ず最終波の制約は失効判定ができていない」「c_viper・dhalsim は攻撃技 0 の仮登録＝最終波候補」。**★判定は済んだ**（成立しない） |
| 4 | **`docs/instructions/M14-overview.md` §3** | 同趣旨の「最終波」サブの記述 |
| 5 | **`docs/handover/m17-to-m18-handover.md`** ／ **`docs/human-notes/model-allocation.md`** | 同趣旨の記述（レビュー実測） |

**★あわせて配布 blocker の残条件が変わった。** `D-43` の「解除は全 31 キャラ充足時点」は**充足した**。**⇒ 残るのは `pre-dist-e2e-clean`（クリーン DB での全 E2E）だけである**（本サブの範囲外＝指示書 完了条件 9）。

**★これらはいずれも設計卓・開発者が持つ文書であり、製造は 1 文字も編集していない**（`D-382` / `CLAUDE.md` §8）。

---

## 15. followup-backlog への登録候補（★本報告は登録しない。`D-382`）

| # | 候補 | 根拠 |
|---|---|---|
| 1 | **`viper-dhalsim-unique-collision-undetermined` は解消。閉じる候補** | §1（判定＝成立しない） |
| 2 | **`elena-chain-cancel-two-rows-unseeded` は解消。閉じる候補** | §2 段 8 |
| 3 | **`seed-progress-procedure-misses-p34-aliases` は解消。閉じる候補**（★ただし内容が変わった＝「P-34 の 2 本を足す」ではなく「M20-06 以降の波では不要」だった） | §5 / §9 |
| 4 | **`movement-total-needs-measured-values-for-14` は継続**（70 行 NULL のまま） | §2 段 7 |
| 5 | **`yasmine-custom-states-definition-missing` は継続** | §2 |
| 6 | **★新規: 是正マイグレも seed 波の再適用対象である** | §8。`backfill-migration-does-not-catch-later-rows` の射程を広げる形 |
| 7 | **★新規: 全キャラ seed 済みになり「技データ未投入キャラ」の E2E が組めなくなった** | §11 / §12。Go 側は fixture で守れたが、E2E スタックは debug ビルドを使わないためキャラを差し込めない |
| 8 | **★新規: `characters` の表示名の正本がリポジトリに無い** | §13-1。`000079` が「migrations だけが書く」と明記しているが、突合の材料は開発者の手元にしかない |
| 9 | **★新規: `move_derivations` の親子は `notes_tool` からは 48% しか手がかりが無い** | §6 |
| 10 | **★新規: `000081` を後から作る場合の順序**（先に取り込まないと永久にスキップされる） | §3 |
| 11 | **★新規: seedgen が生成する down には波ごとの判断を書けない**（golden が byte 一致を守るため手編集できない）。⇒ `move_derivations` の明示削除責務のような「その波固有の判断」は手書きの down に置くしかない。**次の波も同じ制約を受ける** | §16 高-1 |
| 12 | **★新規: `character-sort.ts` の「既知の例外」の母数が 3 → 6 へ倍増した**。挙動は不変だが、2026-08-26 の受容裁定を維持するかは開発者の手番 | §14-7 |
| 13 | **★新規: `seedgen-header-provenance` の緩和策が指示書テンプレートに乗っていない**。「波の指示書へ『ヘッダの投入元表記を信じないこと』を注記する」が緩和策だが、`M14-03e` にはあって `M14-03f` には無かった。⇒ 波ごとに落ちる | §16 中-2 |

---

## 17. 第二段（受領データの投入・2026-09-02）

**指示書の運び方（二段構え）の後半。** 落としていた 4 群のうち **2 群の回答が届いたので投入した**。

### 群1: 移動 5 code の `total`（`000092`・70 行）

**値の出所**: 開発者提供・実測値・2026-09-02 受領（`000059` と同じ性格＝DB/CSV/既存マイグレに存在しない一次源）。

**★CSV の `notes_tool` が独立に 3 件を裏づけた**（値の一次源ではないが対照になる）:

| キャラ | CSV の逐語 | 受領値 | 判定 |
|---|---|---:|---|
| `aki` | 「通常飛びが **45** と他キャラクターより **2** 多い」 | 45 | **一致**（基準 43 ＋2） |
| `chun_li` | 「通常飛びが **47** と他キャラクターより **4** 多い」 | 47 | **一致**（基準 43 ＋4） |
| `dhalsim` | 「ダルシムはジャンプが **73F** のキャラクター」 | 73 | **一致** |

**★裏取りできたのはこの 3 件だけである。** 残り 11 キャラのジャンプ値と、全 14 キャラのダッシュ値はリポジトリ内に対照が無く、開発者提供が唯一の根拠である。**★「他キャラクターより N 多い」の基準が 43 であることは、14 キャラ中 10 キャラが 43 であることとも整合する。**

**★`c_viper` / `dhalsim` を対象に含めた。** `000041` は両者を明示除外しており理由は逐語で「仮登録キャラ・攻撃技 0 件で候補生成に寄与しない」だった。**第四波で実技を持ち実測値も受領したため、除外の理由は消えた**（`000089` が `startup_basis` の除外を解除したのと同じ形）。

**⇒ 全 31 キャラで移動 5 code の `total` が埋まった**（第四波が最後の穴だった）。対象外 4 code（`forward` / `back` / `micro_*`）の 56 行は NULL のままが正。

### 群3: `custom_states`（`000093` / `000094` / `000095`・9 状態）

**開発者裁定 4 件**（2026-09-02）:

| 論点 | 裁定 |
|---|---|
| `code` の作り方 | **`name_en` からの機械生成を優先**（既存の as-built / m11 案より優先） |
| ベガ「サイコマイン付与」の `subject` | **`opponent`**（★下記の設計書との食い違い） |
| ブランカちゃん人形の値域 | **0〜3**（受領表の「0〜2」は転記ミスと確定） |
| ジェイミー酔いレベルの値域 | **0〜4** |

**投入した 9 状態**:

| 連番 | キャラ | `code` | `type` | 値域 | `subject` | `scope` |
|---|---|---|---|---|---|---|
| `000093` | `aki` | `poisoned` | flag | — | **opponent** | persistent |
| `000093` | `blanka` | `blanka_chan_bomb` | stock | **0–3** | self | persistent（`show_delta: true`） |
| `000093` | `blanka` | `lightning_beast` | flag | — | self | conditional / `sa2_active` |
| `000093` | `e_honda` | `sumo_spirit` | flag | — | self | persistent |
| `000093` | `yasmine` | `bayani_mode` | flag | — | self | persistent |
| `000093` | `yasmine` | `nakatagong_lakas` | flag | — | self | conditional / `sa2_active` |
| `000094` | `jamie` | `drink_level` | level | **0–4** | self | persistent（**★`show_delta` なし**） |
| `000094` | `jamie` | `the_devils_song` | flag | — | self | conditional / `sa2_active` |
| `000094` | `m_bison` | `psycho_mine_is_set` | flag | — | **opponent** | persistent |

**不要と確定した 9 キャラ**: `akuma` / `alex` / `cammy` / `chun_li` / `dee_jay` / `ed` / `elena` / `sagat` / `dhalsim`。**NULL のままが正である。** `c_viper` は既存（`limit_decoupler`）。⇒ **14 キャラすべての要否が確定した。**

### ★★`show_delta` の規則を私が取り違えていた

**記入依頼（`20260902-M14-03f-manual-input-list.md` 群3）で「`show_delta` は integer 型なら自動で `true` にします」と書いたが、これは誤りだった。**

正しい規則は `web/src/features/combo/customStates.ts` の逐語＝**「方向可変（増える/減る両方あり）= true。一方向のみ（単調増加/減少）のキャラは false / 省略」**。

⇒ **`drink_level` は開発者が「減少はしない」と明記しているので `show_delta` を付けない。** `blanka_chan_bomb` は「使うと 1 減る」＝消費型なので `true`。**同じ int 系でも扱いが違う。** 記入依頼の説明文も是正した。

### ★★設計書との食い違い 1 件（設計卓の手番）

**`DES-003` §3.2 は 2026-08-25 の開発者方針（`CHANGE-131` 追補）として逐語で次を書いている**:

> **設置技が置かれた状態から始まるコンボ（JP・ベガ・ブランカ 等） → subject: self, type: flag（設置物の有無）**

**★ベガが名指しで `self` とされている。** 一方 2026-09-02 の開発者裁定は **`opponent`**（「特定の技を当てると、相手が、この状態になる」）。

**⇒ 本サブは 2026-09-02 の裁定に従い `opponent` で投入した。`DES-003` §3.2 の当該行の是正が要る。** **★製造は `docs/design/` を編集しない**（`CLAUDE.md` §8）。§14 へ設計卓の手番として記録した。

### ★★第四波のスコープ外を 2 件直した（設計卓未連絡）

**`000094`（`jamie` / `m_bison` の投入漏れ）と `000095`（`kimberly` の値域）は、いずれも既 seed 17 キャラであり `M14-03f` の対象外である。** 開発者の明示依頼により本サブで直した。**★設計卓への連絡は開発者が後日行う**（開発者の逐語＝「設計卓には後程連絡」）。

**★`jamie` の過去の as-built との違い**: `000009` は `jamie` に `code='drunk_level'` / `type='composite'` の定義を seed していたが、`000017` が「先行リリース対象外」として削除した。**本サブの `drink_level` は別の code・別の型である。** `composite` はフロントの描画対象外（`customStates.ts` の `isSupportedState` は `flag` / `level` / `stock` のみ通す）であり、`level` が現行の正しい型である。**`code='drink_level'` は `character_data/jamie.csv` の `move_code`（`drink_level_1` / `_3` / `_4`）とも一致し、値域 0–4 を裏づける。**

### 第二段の検証

| # | 検査 | 結果 |
|---|---|---|
| 1 | クリーン DB へ全 94 マイグレ適用 | **成功** |
| 2 | 第四波 移動 5 code の `total` NULL | **0**（was 70） |
| 3 | 対象外 4 code の `total` 非 NULL | **0**（56 行が NULL のまま） |
| 4 | **全 31 キャラ**の移動 5 code の `total` NULL | **0** |
| 5 | 70 行の値の照合（キャラ × code） | **NG 0 件** |
| 6 | `custom_states` 非 NULL のキャラ数 | **14**（was 8） |
| 7 | `json_valid = 0` の行 | **0** |
| 8 | 「不要」9 キャラが NULL | **9** |
| 9 | `kimberly` の `max` = 3・他フィールド温存 | **一致** |
| 10 | 既存 7 定義（`kimberly` 以外）が不変 | **一致** |
| 11 | `go test ./...` | **55 ok / 0 FAIL** |
| 12 | `make e2e` | §12 の表に併記 |
| 13 | down → re-up | 群ごとに元へ戻り復帰 |

**★破壊確認を 1 件実施した**——`drink_level` の `show_delta` 期待値をわざと `true` に反転させたところ、テストが `show_delta = "", want "1"` で赤くなった。**⇒ 本アサーションは空回りしていない。**

### 第二段で残っているもの

| 群 | 状態 |
|---|---|
| 群2 `chain_cancel_total` | **未受領**（13 キャラ。★ロースター判定を含む） |
| 群4 `move_derivations` | **候補表の回答待ち**（277 件） |

---

## 16. レビューと取り込み

**レビュー報告書**: `docs/progress/m14-03f-review.md`（fresh subagent による独立レビュー）
**再レビュー往復**: **0 回**（上限 2 回）
**指摘 16 件**（高 5 / 中 5 / 低 6）。**採用 15 件 / 不採用 1 件。★「高」の不採用は 0 件。**

### 採用した「高」5 件

| # | 指摘 | 対応 |
|---|---|---|
| 高-1 | **`move_derivations` の down 判断がどのヘッダにも書かれていないのに、報告 §10 は「書いた」と述べている**（指示書 §2.5-3 / 完了条件 8 の未達） | **`000082.down` へ実際に書いた。★当初 `000084.down` へ書こうとして golden が落ちた**——同ファイルは seedgen の生成物で手編集できない。**⇒ 生成物の down には波ごとの判断を載せられないという制約が分かったので、その理由ごと `000082.down` に書いた** |
| 高-2 | **段 9「28 行」の理由が 23/28 にしか当たらない。しかも 4 か所へ複製済み** | **4 か所すべて是正した**（`000089` ヘッダ ／ 完了報告 §4 ／ `seed-progress.md` ／ `progress-log.md`）。**★内訳を 3 つに割った**（ジャンプから通した値 23 ／ 地上必殺技からの派生 4＝`ed/psycho_shoot` ／ **根拠を確かめられなかった 1＝`chun_li/wall_jump`**）。**★あわせて「空中必殺技」という記述も誤りだった**——28 行のうち 26 行は `is_aerial=false` である。`wall_jump` は §13 へ回した |
| 高-3 | **seedgen の生きたコメント 5 か所が「CSV を持たないキャラ（`c_viper` / `dhalsim`）」を分離の理由として書いており、本サブで失効した** | **5 か所すべて是正した**（`generate_m2002.go` 3 ／ `cmd/seedgen/main.go` 1 ／ golden test 2）。**★分離の必要は残るので、理由を「移動 9 code はどの CSV にも無く、投入先はその波で `characters` 行が増えたキャラであり `-chars` と一致しない」へ書き換えた**（第四波が実例＝`-chars` 14 / `-movement-chars` 12） |
| 高-4 | **`character-sort.ts` の「既知の例外 2 つ」の実測値が 19 体時点のまま** | **31 体時点へ更新した**（漢字 1→**4** ／ ラテン始まり 2→**4**。`e_honda` は両方に当たる）。**★受容裁定の維持は開発者の手番として §14 へ回した** |
| 高-5 | **§0.2 の判定で失効した 5 か所が §14 にも §15 にも載っていない** | **§14 へ「設計卓の手番」として名指しで足した**（`m20-contract` L99 ／ ボード L208・L212・L214 ／ `M14-overview` §3 ／ `m17-to-m18-handover` ／ `model-allocation`）。**あわせて配布 blocker の残条件が `pre-dist-e2e-clean` だけになったことを書いた** |

### 採用した「中」4 件・「低」6 件

`internal/service/intake/service_test.go` の `c_viper` 依存を索引ゼロ fixture の意図が読める名前へ（中-1）／ `code-facts.md` を再生成（中-3）／ §7 の列見出しを「意図しない取り残しか」へ（中-4）／ `movecommand` テストの「仮データキャラ」注記（中-5）／ **`000087.down` を up と同じキー（キャラごとの code リスト）へ**（低-11）／ `000083.down` へ FK 枝の判断を注記（低-12）／ コメントの `?` 残り（低-13）／ **記入依頼の「前ダッシュ」を「前方ステップ」へ**（低-14）／ `preset_aliases` の差 ＋3413 を明記（低-15）／ 「9 件」の内訳を実数へ（低-16）。

### ★不採用 1 件（中-2）

**指摘**: `internal/seedgen/generate.go` L173 / L184 のヘッダテンプレートから「移動 system move 9 種は drop 済（投入元は `000025`）」の断定を外す。

**不採用の理由**: **既に裁定済みの項目であり、「波の実装サブでは触れない」と決まっている。** followup `seedgen-header-provenance`（`M14-03d` 由来・2026-08-01 起票）の逐語＝「**なぜ今直さないか**＝文字列を変えると生成物が変わり、**golden 4 stem が byte 単位で全部変わる**。`M14-03c` §4.3.1 の 3 条件を破るため、**波の実装サブでは触れない**（**製造が触らなかった判断は正しい**）」「**恒久対応**＝波ごとの投入元をパラメータ化する。**既存 4 stem の再生成と golden の全面更新を伴うため独立サブを推奨**」。

**★ただし緩和策は本サブでも要る。** 同 followup の緩和策は「波の指示書に『ヘッダの投入元表記は既知の誤り。信じないこと』を注記する」だが、**`M14-03f` の指示書にはその注記が無い**（`M14-03e` §4.1 にはあった）。**⇒ ここに書く**——

> **★本波の生成マイグレ 5 本（`000084` / `000085` / `000086` / `000090` / `000091`）のヘッダは「移動 system move 9 種は drop 済（投入元は `000025`）」と出力しているが、実際の投入元は新 12 キャラについては `000083` である**（`c_viper` / `dhalsim` の分だけが `000025` 由来）。**ヘッダの投入元表記を信じないこと。**

---

## 18. 第三段（連打キャンセル実測の受領・2026-09-04）

**二段構えの残り。** 落としていた 4 群のうち **群2（`chain_cancel_total`）の実測が完了し、開発者から文書 5 枚を受領した。**
**群4（`move_derivations` 候補表 277 行の ○/×）は未受領のまま残る。**

### 18-1. 受領文書の検証（★上書き前に全数照合した）

| 検査 | 結果 |
|---|---|
| 既存 55 行（18 キャラ）が黙って書き換わっていないか | **55/55 一致**（`000052`／`000060`／`000088` の UPDATE 文を全パースして突合） |
| 新規 38 行（13 キャラ）の単発値 su/act/rec/total が CSV と一致するか | **89/93 一致・不一致 0**。突合できない 4 行は CSV に行を持たない連打版（zangief 3＝`000051` で登録済・CSV 非搭載が正／dhalsim 1＝本段で新規登録） |
| `dhalsim/agile_kick`（1LK）の写像 | CSV に `agile_kick` `[unique]` アジャイルキック `5/3/10/17` が実在。成果表と一致 |
| dhalsim 連打版 `4/3/9/15` の総和検算 | `4+3-1+9 = 15` ＝ seedgen が CSV に課すのと同じ式を満たす |

**★この照合は「上書き先が分からない／diff に不整合がある／配置先が分からない場合は教えてください」という開発者の依頼への回答である。**
**1 件の不整合を検出し、上書きを止めて報告した**（§14-12＝`M19-DESIGN-07` の系統分岐）。

### 18-2. 文書の反映（5 枚）

| ファイル | 扱い |
|---|---|
| `character_data/chain-cancel-measurements.md` | **上書き**（v2.3.0 系 55 件/18 キャラ → **v2.6.1 93 件/31 キャラ**）。純加算・既存行の改変なし |
| `docs/handover/SF6セットプレイ-ドメイン知識集成.md` | **上書き**（v1.1.2 → v1.3.0）＋ **参照 2 箇所を本系列の採番へ是正**（§14-14） |
| `docs/handover/M19-DESIGN-07-frame-cost-model.md` | **上書きせず、新規差分 4 箇所だけを移植して v1.9.0**（§14-12） |
| `docs/seed-data/M19-OPS-01-chain-measurement-manual.md` | **新規配置**（§14-16） |
| `character_data/dhalsim-joint-remeasurement-2026-09-04.md` | **新規配置**。成果表 §保留A が相対名で参照する別紙のため同一ディレクトリへ |

### 18-3. 群2: `chain_cancel_total`（`000097`〜`000099`・38 行 ＋ 新規 1 技 ＋ 親参照 2 行）

**★`000096` は並列タスクが消費した**（開発者連絡 2026-09-04）ため 1 つずらしてある。

| 連番 | 内容 | 生成手段 | 投入行数 |
|---|---|---|---|
| `000097_seed_moves_dhalsim_rapid` | ダルシムの屈弱P 連打版 1 技 ＋ `official_ja_move` alias | 手書き | 1 + 1 |
| `000098_seed_move_derivations_dhalsim_rapid` | 同技の親参照（クロス限定＝2 親） | 手書き | 2 |
| `000099_backfill_moves_chain_cancel_total_fourth_wave_second_stage` | 第四波 13 キャラの `chain_cancel_total` | 手書き | 38 |

**値の出所**: `character_data/chain-cancel-measurements.md` v2.6.1（CSV ではない。同書が値の正本を自己宣言している）。
ダルシムの根拠は別紙 `dhalsim-joint-remeasurement-2026-09-04.md` の生読み 14 本。

**★ロースターは非対称である**（圏外はいずれも開発者が実測に基づき明示回答した）:

| キャラ | グループ員 | 圏外 |
|---|---|---|
| `e_honda` | Crouch LP 10 / Crouch LK 9（**2 技のみ**） | Stand LP |
| `ed` | Stand LP 9 / Crouch LP 10（**2 技のみ**） | Crouch LK |
| `blanka` | **Stand LK** 9 / Crouch LP 11 / Crouch LK 11 | Stand LP |
| `dhalsim` | Stand LP 12 / Crouch LP 11 / **`agile_kick`（特殊技）11** ＋ **連打版 10** | Crouch LK |
| 他 9 キャラ | Stand LP / Crouch LP / Crouch LK の 3 技 | — |

**圏外 4 件は `NULL` のままであることをテストで対の主張として固定した**（投入し過ぎの検出）。

### 18-4. ★ダルシムの連打版はザンギエフと出現条件が違う（直積にしなかった理由）

| | ザンギエフ（`000051` / `000061`） | ダルシム（`000097` / `000098`） |
|---|---|---|
| 出現条件 | **2 打目以降なら常に**連打版 | **直前技が Stand LP または 1LK のときだけ**（直前が屈弱P なら素が出る） |
| 判定の基準 | 位置 | **技の同一性**（同ボタンの Stand LP から出るので、ボタンではない） |
| `move_derivations` | 親集合＝グループ全行 ⇒ 3 親 × 3 子 = **9 行** | 親集合＝`{standing_light_punch, agile_kick}` ⇒ 2 親 × 1 子 = **2 行** |
| 連打版の行数 | 3 | 1（屈弱P のみ。Stand LP・1LK に変種はない） |

**⇒ `crouching_light_punch` を親に入れてはならない。** 入れると「屈弱P ＞ 屈弱P」で連打版が出る挙動を意味し、実測（別紙 R2／R3／R4）と食い違う。
**テストで「親がちょうど 2 つ」かつ「`crouching_light_punch` が親でない」ことを対で主張している。**

**★設計は変わっていない。** 同じ `move_derivations` の親集合で両者を表現できる（`M19-DESIGN-07` v1.9.0 §9 項目 8＝親集合駆動の差し込み規則）。新列もキャラ分岐も要らない。

### 18-5. `agile_kick` は `category='unique'`（特殊技）だがグループ員である

グループ員資格は**技単位の列**（`chain_cancel_total` 非 NULL）で表現され、**`category`・ボタン種別に非依存**である（`M19-DESIGN-07` v1.9.0 §3・§9 項目 8）。
「通常技扱いへ再登録」は消費者都合で事実列を曲げる反パターンとして設計卓が**不採用**にしている。⇒ **`category` は `unique` のまま値だけ入れる。**

### 18-6. 検証の実測（第三段）

| # | 検査 | 結果 |
|---|---|---|
| 1 | クリーン DB へ全マイグレ適用 | **99 本すべて成功** |
| 2 | `chain_cancel_total` 非 NULL 件数 | **93**（第二段時点 55） |
| 3 | 非 NULL を持つキャラ数 | **31**（第二段時点 18） |
| 4 | 既存 55 行が不変 | **55/55**（総数 ＋ 代表 3 行の値を固定） |
| 5 | 圏外 4 行が NULL のまま | **4/4 NULL** |
| 6 | ダルシム連打版の moves 1 行 ＋ alias 1 行 ＋ 親参照 2 行 | **1 / 1 / 2** |
| 7 | `crouching_light_punch` が親に**居ない** | **0 件** |
| 8 | `zangief` の連打版 3 行・親参照 9 行が不変 | **3 / 9** |
| 9 | `go test ./...` | **緑**（55 ok / 0 FAIL） |
| 10 | `make e2e` | **緑**（226 passed / 0 failed） |
| 11 | down → re-up（v99 → v95 → v99） | 元へ戻り、再投入で復帰 |

### 18-7. 波及した件数固定テスト（実査して直した・1 本）

| ファイル | 変更 | 理由 |
|---|---:|---|
| `internal/aliasindex/m2207b_third_stage_test.go` | `preset_aliases` 行数 **7597 → 7598** | ダルシム連打版に対の `official_ja_move` alias が 1 行増えたため。**同テストのコメント自身が「seed 波のたびに更新する」と書いている**（守っているのは「案 E が辞書に触らない」であって行数の永続性ではない） |

**★動かなかったもの（実査で確認）**: `migrate_test.go` の numeric 2272 / srk 2300（`official_ja_move` は別プリセット）／`punishfinder` の件数（連打版は `is_derived=1` ＋ 親ありで候補に入らない）／
**seedgen の golden 全数**（CSV を 1 行も触っていないため。★動いたら CSV を汚した証拠なので止めて調べる、という判定にしていた）。

### 18-8. 第三段で残ったもの

| # | 項目 | 状態 |
|---|---|---|
| 1 | **`move_derivations` 候補表 277 行の ○/×** | **未受領**。§6 の候補表のまま |
| 2 | **ジャンプを親にできるかの方針** | **未回答**。開発者が既存キャラを確認中（29 行の一覧を提出済み） |

**⇒ 第四段があるとすれば群4 の 1 群だけである。**

---

## 19. 第四段（`move_derivations` の裁定投入 ＋ 入力ミスの是正・2026-09-04）

**二段構えの最後。** 群4 の裁定 277 件を受領し、**同時に名指しで報告された入力ミス 1 件**を先に是正した（開発者裁定＝**入力ミスの是正を先**。誤 code は裁定表で 4 件の親候補に使われており、後回しにすると二度手間になる）。

### 19-1. 消費した連番

| 連番 | 内容 | 生成手段 | 投入行数 |
|---|---|---|---|
| `000100_rename_cammy_hooligan_combination_holding` | `move_code` と表記の是正 | 手書き | UPDATE 2 文 |
| `000101_seed_move_derivations_fourth_wave` | `move_derivations` | 手書き | **313** |

**★`000096` は並列タスクが消費した**（開発者連絡 2026-09-04）。

### 19-2. 入力ミスの是正（三点更新の 2 例目）

| 項目 | 現 | 正 |
|---|---|---|
| `move_code` | `hooligan_combination_holding` | `hooligan_combination_holding_heavy` |
| 表記（`official_ja_move` の `alias_text`） | 【ホールド】フーリガンコンビネーション | 【ホールド】強フーリガンコンビネーション |

**新 code は同キャラの既存命名と整合する**（`spiral_arrow_holding_heavy` / `cannon_spike_holding_heavy` が既にある）。

**★三点は不可分**（followup `csv-edit-vs-golden-divergence`。手順の正本は `m19-04c-completion-report.md` §12）:

| 点 | やったこと |
|---|---|
| **CSV（正本）** | `character_data/cammy.csv` の 1 行（`move_code` と `name_ja`） |
| **golden（生成マイグレ）** | **`000084` / `000085` を seedgen で再生成**。**★`D-336` により生成物の再生成は「適用済みマイグレの改変」に当たらない**（逐語＝「判別の述語＝そのマイグレは生成物か手書きか」） |
| **既存 DB** | `000100`。改名 UPDATE に **`NOT EXISTS` ガード**（m19-04c §12.1） |

**★再生成の実測**: `seedgen -check` で 5 本すべてを事前測定した。**差分が出たのは `000084`（up/down）と `000085`（up/down）だけ**で、`000086` / `000090` / `000091` は **byte 一致**。行数も不変（moves 1264 / is_derived 519 / move_commands 737 / numeric 906 / srk 919）。
**⇒ `_heavy` 接尾辞が強度トークンとして拾われて `hooligan_combination_heavy` と衝突する懸念は成立しなかった。** 先例の `spiral_arrow_holding_heavy` / `cannon_spike_holding_heavy` も numeric / srk alias を 0 件しか持たない。

**★golden に載らない列の取り残し（m19-04c §12.2）は本件では発生しない**（実査）:

| 列 | 状態 |
|---|---|
| `startup_basis` | CSV が `unknown`。手書きの `000089` は当該 code を列挙していない ⇒ 新規 DB・既存 DB とも `unknown` で一致 |
| `is_derived` | `000085`（生成物）が再生成で新 code に当たる。既存 DB は改名で値を保持 |
| `is_projectile` / `chain_cancel_total` / `fastest_unreachable` | 手書き 3 本（`000087` / `000089` / `000099`）に旧 code は **0 件**（着手時に再 grep） |

### 19-3. `move_derivations` の裁定（277 件 → 投入 169 件・313 行）

| 区分 | 子の件数 |
|---|---:|
| **投入した** | **169**（61%） |
| 行を作らない（派生なし／ターゲットコンボ／強化版の別技／ホールド版の独立技／複雑すぎて扱わない） | 98 |
| 行を作らない（開発者裁定）＝`elena/lynx_whirl_od` の自己参照 | 1 |
| 行を作らない（開発者裁定）＝キャミィ OD フーリガン派生 9 件 | 9 |
| **計** | **277** |

**★候補表の 39% が「行を作らない」だった。** `notes_tool` と `move_code` 接頭辞だけでは、ターゲットコンボ・強化版の別技・ホールド版の独立技を弁別できていなかった。**機械提案をそのまま投入しなかったことが、数字で正当化された。**

**キャラ別のペア数**: elena 72 ／ c_viper 45 ／ akuma 32 ／ yasmine 32 ／ dhalsim 27 ／ cammy 22 ／ alex 17 ／ blanka 15 ／ dee_jay 15 ／ sagat 15 ／ chun_li 7 ／ aki 6 ／ e_honda 6 ／ ed 2。

### 19-4. ★`jump_*` を親にする行が初めて入った（21 子 / 52 行）

| キャラ | 子 | 親 |
|---|---|---|
| `akuma` | `tenmaku_blade_kick` ／ `aerial_tatsumaki_zanku_kyaku` ／ `_od` | `jump_forward` |
| `akuma` | `sa1_tenma_gozanku` | `jump_neutral` ＋ `jump_forward` |
| `blanka` | `lightning_beast_{light,medium,heavy,od}_aerial_rolling_attack` | `jump_neutral` ＋ `jump_forward` ＋ `jump_back` |
| `c_viper` | `aerial_burning_kick_{light,medium,heavy,od}` | `jump_neutral` ＋ `jump_forward` |
| `dhalsim` | `yoga_mummy` ／ ドリルキック 3 種 ／ `aerial_yoga_float` ／ 空中ヨガテレポート 4 種 | `jump_neutral` ＋ `jump_forward` ＋ `jump_back` |

**⇒ §13 で「先例 0 件」と報告した状態が解消した。** ただし **既存 17 キャラのジャンプ前提技は本サブの範囲外**（開発者が目視確認中）。テストで「第四波以外に `jump_*` 親が 0 行であること」を対の主張として固定した。

#### ★★論点が 2 つに割れていた（2026-09-04・開発者の問い返しで判明）

**製造が「区分A / 区分B」と呼んで一覧に出したものは、性質の違う 2 種類だった。** 開発者から「これは何の判断ですか？ 派生元がジャンプの特殊技。必殺技の話とは別？」と問い返されて分離できた。

| | 中身 | 件数 | 候補表への載り方 |
|---|---|---:|---|
| **①ジャンプ攻撃そのもの** | `jumping_{light,medium,heavy}_{punch,kick}` ＋ `neutral_jumping_heavy_kick` | **全 31 キャラで 186** | **一度も載っていない**（`is_derived=false` のため候補抽出の対象外） |
| **②ジャンプ前提の必殺技・特殊技** | 空中竜巻旋風脚・ドリルキック・エアフラッシュナックル 等 | 第四波 21 子／既存 17 キャラ 36 件 | ②が裁定表の対象だった |

**★開発者裁定（2026-09-04）＝①には付けない**（全 31 キャラで現状維持）。**②の既存 17 キャラ分だけが目視確認の対象である。**

**★「付いていたら消してほしい」との指示を受けて実査した結果、削除対象は 0 行だった。**
`jumping_*P/K` と `neutral_jumping_*` を子に持つ `move_derivations` は、**マイグレ全 101 本を通して 1 行も無い**。`000101` の `jump_*` 親 52 行はすべて②である。

**★線引きを 1 件明記する**——`c_viper/high_jumping_{light,medium,heavy}_{punch,kick}` ＋ `high_jumping_neutral_heavy_kick`（**7 件**）には `high_jump` を親として投入済みで、これは①に見えるが**①ではない**。
親の `high_jump` は `character_data/c_viper.csv` で **`category=unique`・`command='d u'` を持つ入力技**であり、**移動 system move の `jump_*`（`000025` / `000083` が投入する 9 code の一部）とは別物**である。⇒ **非対称ではなく性質が違う。残す。**

**★裁定は機械で守る。** ①は候補表に載らないため、「候補表に無い＝判断済み」ではない。次の波が抽出条件を変えると静かに破れる。⇒ `TestRun_M1403f_JumpNormalsHaveNoParent` を新設し、①が 0 行であること・②の 52 行が残ること・`high_jumping_*` の親が `high_jump` であって `jump_*` でないことを対で固定した。

### 19-5. ★既存 `move_derivations` は 74 行ではなく **90 行**だった（実測で是正）

着手時にマイグレの SQL をパースして「`000061` 9 ＋ `000064` 63 ＋ `000098` 2 ＝ **74**」と見積もったが、**実測は 90** だった。

**原因**: **`000067` の 16 行（すべて `jamie`）を数え落としていた。** `000064` は `AND pa.code IN ('...')` と書くのに対し、**`000067` は `AND pa.code = '...'` の単数形**で書かれており、同じ正規表現では拾えない。

**⇒ 教訓は m19-04c §12.4 と同じ形である**——**件数ガードは実測で置く。SQL のパースで見積もった値をそのまま期待値にしない。** テストが 1 発で落として捕まえた。

### 19-6. ★製造が自己参照 3 件を外した（報告事項）

`elena/lynx_whirl_light` / `_medium` / `_heavy` の親リストは「リンクスワール系（スピンサイズ派生版含む）」の 9 code で、**各子が自分自身をその中に含んでいた**。

**開発者裁定「`elena/lynx_whirl_od` は自己参照なので行を作らない」と同じ原理**で、**自己参照の 1 組だけを外し、残る 8 親はそのまま投入した**（親リスト全体は捨てていない）。⇒ 316 − 3 = **313 行**。

**★自己参照を入れると何が起きるか**: 導出 `startup_basis IN ('standalone','unknown') ∧ 親参照あり → 単独入力不可` が**自分自身で成立**し、単独で出せる技がセットプレイ提案の filler / target から**静かに落ちる**。**テストで「自己参照が 0 行であること」を固定した。**

### 19-7. ★候補集合の増減を実データで測った（`D-274`）

**「候補集合を動かす変更は、実装を確定する前に実データで増減を測ること。設計文書の読解だけを根拠にしない」**（`M19-DESIGN-07` §5 項目 3 の errata。M19-05 の Plan Mode 素案で実際に過剰解禁が起きた型）。

| | 投入前（v100） | 投入後（v101） |
|---|---:|---:|
| **単独入力不可**（`startup_basis IN ('standalone','unknown')` ＋ 親参照あり） | **17** | **86** |

**⇒ +69。** 投入した子 169 件のうち **69 件（41%）が filler の先頭（head）と target から外れた**。

**★「提案から消える」ではない**（2026-09-05 の手動検証で判明・是正）。**`buildFillerUnits`（`service.go:530`）は連鎖の 2 番目以降にはむしろ単独入力不可の技を *優先して* 使う**——逐語＝「2 番目以降の候補。**単独入力不可の員があればそれを使う**」。実測すると該当は **4 行だけ**（`zangief` 連打版 3 ＋ `dhalsim/crouching_light_punch_rapid`）で、**連打版が「2 発目にしか出ない技」であることと噛み合っている**。残る 100 件は `through` か、もともと候補外だった行である。

**★この数字は既存のテストを 1 本も動かさなかった**（`setplay` / `punishfinder` の件数固定テストはいずれも fixture ベースで実データを引かない）。**⇒ 実データの増減はテストが自動では守らない。専用の測定テスト `TestRun_M1403f_FourthStage_SoloUnavailableDelta` を新設して固定した。**

### 19-8. 検証の実測（第四段）

| # | 検査 | 結果 |
|---|---|---|
| 1 | クリーン DB へ全マイグレ適用 | **101 本すべて成功** |
| 2 | 旧 code = 0 行 ／ 新 code = 1 行 | 0 / 1 |
| 3 | 改名の巻き添え（子 3 件 ＋ `hooligan_combination_heavy`） | 4/4 無傷 |
| 4 | `move_derivations` 総行数 | 90 → **403** |
| 5 | `jump_*` を親にする行 ／ 子 | **52** ／ **21** |
| 6 | 第四波以外に `jump_*` 親が付いていないこと | 0 件 |
| 7 | 「行を作らない」と決めたものが 0 行（代表 14 件） | 0 件 |
| 8 | 自己参照（`child = parent`） | **0 件** |
| 9 | 既存 90 行が不変 | 90/90 |
| 10 | 単独入力不可の増減 | **17 → 86** |
| 11 | `go test ./...` | **緑**（55 ok / 0 FAIL） |
| 12 | `make e2e` | **緑**（226 passed / 0 failed） |
| 13 | down → re-up（v101 → v99 → v101） | 元へ戻り、再投入で復帰。**down の非対称性も記録** |

**★波及した件数固定テストは 0 本だった**（第三段は 1 本動いた）。`alias_text` の 1 件変更は `preset_aliases` の行数を動かさず、`move_derivations` の件数を固定しているテストは本サブが新設した分だけである。

### 19-9. 第四段で残ったもの（＝第五段の材料）

| # | 項目 | 状態 |
|---|---|---|
| 1 | ~~**既存 17 キャラのジャンプ前提技への親付与**~~ **【2026-09-04 解消】** ①は「付けない」で決着（§19-4）、②は第五段 `000102` で投入（§20）。**⇒ 既存キャラのジャンプ派生は完了** |
| 2 | キャミィ OD フーリガン派生 9 件 | 親の弱中強 OD フーリガンが `hooligan_combination_od` 1 行に畳まれており指せない。登録するなら実測が要る |
| 3 | ヤスミン `mukha_ng_langit_od` の弱中強版が未登録 | `ulan_od` / `kulog_od` は `mukha_ng_langit_od` で投入済み。本来は強度別の親がある |
| 4 | エレナ `lynx_whirl_od` の `notes_tool` が自己参照 | AKI `nightshade_chaser` ／ アレックス `low_rush` `low_retreat` と同型の notes 側入力ミスの可能性。正しい親が判明したら追加投入 |
| 5 | ブランカ `rolling_cannon` の `name_ja` が「ローリングキャノン（後方斜め下）」 | 基底 code に方向名が入っており、`_down`＝（下）・`_down_forward`＝（前方斜め下）と並ぶと崩れている（**製造が発見**。裁定は「対象外」） |

---

## 20. 第五段（既存キャラのジャンプ派生 ＋ CSV 3 列の陳腐化是正・2026-09-04）

**二段構えの完全な終わり。** 第四段で残した「既存 17 キャラのジャンプ前提技」を開発者が全件目視で裁定し、**あわせて CSV の陳腐化是正という新しい要求**を受けた。

### 20-1. 消費した連番

| 連番 | 内容 | 生成手段 | 投入行数 |
|---|---|---|---|
| `000102_seed_move_derivations_existing_chars` | 既 seed 11 キャラのジャンプ派生 | 手書き | **56** |

**CSV の書き戻しはマイグレを起こしていない**——DB が既に正しい値を持っており、CSV を DB に合わせる向きだから。

### 20-2. ★「機械で候補を挙げる」だけでは取り落とすことが実証された

**製造が出した棚卸しは 36 件だったが、開発者の全件目視で構図が変わった。**

| | 件数 |
|---|---:|
| 棚卸し 36 件のうち **「対象外」と裁定された** | **21**（`terry/jumping_lariat`・`jumping_knee`・`lily/great_spin`＝名前がジャンプで始まるが別技 ／ `kimberly/step_up_*` 3＝jump 無関係 ／ `air_throw` 2 ／ ザンギエフ 4 ／ ラシード 6 ／ マリーザ 4） |
| 棚卸し 36 件のうち **採用された** | **15** |
| **棚卸しに載っていなかったのに開発者が見つけた** | **28**（6 キャラ分） |
| **投入した子の合計** | **43**（ペア **56 行**） |

**★棚卸しに無かった 28 件の内訳**: `lily/condor_dive` 系 4 ／ `juri/shiku_sen` 2 ／ `zangief/borscht_dynamite` 2 ／ `rashid/arabian_skyhigh_*` 4 ／ `jamie/luminous_dive_kick_*` 4 ／ **`ingrid/solar_burst_*` 14**。

**⇒ 製造の抽出条件（`is_aerial` / code に `aerial|jump|air` / 名前や `notes_tool` に「ジャンプ・空中・エリアル」）は、`condor_dive`（コンドルダイブ）・`shiku_sen`（疾空閃）・`borscht_dynamite`（ボルシチダイナマイト）・`arabian_skyhigh`・`luminous_dive_kick`（無影蹴）・`solar_burst`（ソーラーフレア）のどれにも当たらない。** 技名からジャンプ前提と分かるのは**ドメイン知識であって文字列ではない**。

### 20-3. `ingrid/solar_burst` の方向対応（自然言語指示の展開）

開発者の逐語＝「**14 種類が `jump_forward` と `jump_neutral`。ただし垂直や前方と名前に含まれるものは対応した方向のジャンプのみ**」。
**実査すると 14 種すべてが（垂直）か（前方）を持っていた**ため、例外規定が全件にかかり、**結果は 1 親ずつ 14 行**（開発者に確認済み）。

| 名前 | code | 親 |
|---|---|---|
| （垂直）7 件 | `solar_burst_{light,lv1,lv2,lv3}_neutral{,_od}` | `jump_neutral` のみ |
| （前方）7 件 | `solar_burst_{light,lv1,lv2,lv3}_forward{,_od}` | `jump_forward` のみ |

**テストで方向の取り違えを検出する**（`*neutral*` ← `jump_neutral` が 7 行、`*forward*` ← `jump_forward` が 7 行、総数 14 行）。

### 20-4. ★候補集合の増減を実データで測った（`D-274`）

| | 投入前（v101） | 投入後（v102） |
|---|---:|---:|
| **単独入力不可** | **86** | **125** |

**+39。投入した子 43 件のうち、`standalone` の 39 件がちょうど落ちた**（`through` の 4 件＝`kimberly/elbow_drop`・`mai/flame_sa2_…`・`lily/windclad_condor_dive` / `_od` は落ちない）。**事前予測と実測が一致した。**

### 20-5. CSV 3 列の陳腐化是正

**開発者要求（2026-09-04）**＝「アップデート対応は CSV を外部ツールで取込 → 修正 → **差分のみマイグレで取込**となるため、CSV 側だけが陳腐化していると問題」。

**実測すると、実際に陳腐化していた**（空セル数）:

| 列 | 既 seed 17 キャラ | 第四波 14 キャラ |
|---|---|---|
| `startup_basis` | **1186 / 1479 が空** | 0 / 1264 |
| `fastest_unreachable` | **1445 / 1479 が空** | 0 / 1264 |
| `chain_cancel_total` | **1479 / 1479 が空** | **1264 / 1264 が空** |

**書き戻したセル＝2720**（`startup_basis` 1186 ／ `fastest_unreachable` 1445 ／ `chain_cancel_total` 89）。**上書きした既存値は 0 件**——すべて空セルを埋めただけで、CSV が持っていた値と DB が食い違う箇所は無かった。

**★DB を唯一の出所にした。SQL のパースで再導出していない。** 第四段で実際に失敗した型である（`000064` は `pa.code IN (...)`、`000067` は `pa.code = '...'` の単数形で、同じ正規表現では拾えず、既存 90 行を 74 行と見積もった）。

**★CSV に書けない行**（意図的な欠落・同期テストの対象外）:

| 行 | 理由 |
|---|---|
| `zangief` の連打版 3 行（`000051`）／`dhalsim` の連打版 1 行（`000097`） | CSV に足すと生成物が変わって golden が壊れ、値が 2 か所に存在する（`D-94` / `D-99`） |
| 移動 system move 9 code × 31 キャラ（`000025` / `000083`） | CSV は攻撃技のみを持つ |

DB 3026 行 − CSV 2743 行 = **283**（＝ 279 移動 ＋ 4 連打版）。**過不足なく説明がつく。**

**★golden は 1 本も動かなかった（実測）。** `seedgen` はこの 3 列を**読むだけで SQL へ出力しない**（`internal/seedgen/csv.go`「新列 3 つ(000049)。保全のみ・SQL 非投入」）。**書式の制約は守った**——`startup_basis` は enum 検証あり（`generate.go`）、`chain_cancel_total` は整数 or 空、`fastest_unreachable` は `true` / `false`。

**★CSV の往復忠実性を先に確認した。** `csv.reader` → `csv.writer(lineterminator='
')` の往復が **31 ファイルすべてで byte 一致**することを確かめてから書いた（`marisa.csv` に 1 件だけ**改行を含む引用フィールド**があり、行単位の編集では壊れる）。

### 20-6. ★恒久テストを新設した（陳腐化はテストが無いから起きた）

`internal/infra/migration/csv_db_sync_test.go` の **`TestCSVAndDBAgreeOnFrameCostColumns`**。

- クリーン DB へ全マイグレを適用し、`moves` の 3 列を CSV と突合（**2743 行**）
- **不一致 0 件**を主張。差分が出たら**どちらが新しいかは判定せず両方を出す**（CSV が正なら backfill マイグレ、DB が正なら書き戻し、と人が決める）
- 突合行数 2743 も固定する（CSV 行が減ってテストが空回りするのを防ぐ）

**★この 3 列は golden が守らない領域である**（SQL 非投入なので、CSV をどう書いても生成物は変わらない）。**⇒ 本テストが唯一のガードである。**

### 20-7. 検証の実測（第五段）

| # | 検査 | 結果 |
|---|---|---|
| 1 | クリーン DB へ全マイグレ適用 | **102 本すべて成功** |
| 2 | `move_derivations` 総行数 | 403 → **459** |
| 3 | `jump_*` を親にする行 | 52 → **108** |
| 4 | `ingrid/solar_burst` の方向一致 | `neutral` 7 ／ `forward` 7 ／ 総数 14 |
| 5 | 「対象外」12 代表が 0 行 | 0 件 |
| 6 | 既存の親が無傷（`hisen_kick` 3 ／ `side_flip` 1 ／ zangief 連打版 9 ／ 第四波 52） | 4/4 一致 |
| 7 | 通常ジャンプ攻撃を子に持つ行 ／ 自己参照 | **0** ／ **0**（不変） |
| 8 | **CSV と DB の 3 列が一致** | **不一致 0 件・突合 2743 行** |
| 9 | **seedgen golden 全数** | **0 差分** |
| 10 | 単独入力不可の増減 | **86 → 125（+39）** |
| 11 | `go test ./...` | **緑**（55 ok / 0 FAIL） |
| 12 | `make e2e` | **緑**（226 passed / 0 failed） |
| 13 | down → re-up（v102 → v101 → v102） | 元へ戻り、再投入で復帰。既存行の巻き添えなし |

**★波及した件数固定テストは 0 本だった。**

### 20-8. 第五段で残ったもの

| # | 項目 | 状態 |
|---|---|---|
| 1 | キャミィ OD フーリガン派生 9 件 | 親が `hooligan_combination_od` 1 行に畳まれており指せない。登録するなら実測が要る |
| 2 | ヤスミン `mukha_ng_langit_od` の弱中強版が未登録 | `ulan_od` / `kulog_od` は `mukha_ng_langit_od` で投入済み |
| 3 | エレナ `lynx_whirl_od` の `notes_tool` が自己参照 | 正しい親が判明したら追加投入 |
| 4 | ブランカ `rolling_cannon` の `name_ja` が「（後方斜め下）」 | 基底 code に方向名が入っている（製造が発見・裁定は「対象外」） |
| 5 | **`000052` のヘッダと現行運用の食い違い** | `chain_cancel_total` を CSV に書かない、と書いてあるが覆った。**適用済みなので改変しない**（`D-535`）＝§14-23 |

**⇒ `M14-03f` の二段構えはこれで完了。** 未受領のデータは無い。

---

*以上*
