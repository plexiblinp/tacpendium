# M24-09a レビュー報告書

| 項目 | 内容 |
|------|------|
| 対象指示書 | `docs/instructions/M24-09a-ci-distribution-and-dependency-audit.md` v1.0.0 |
| チェックリスト | `docs/instructions/reviews/M24-09a-review-checklist.md` v1.0.0 |
| レビュー対象 | `3aaf3e5..HEAD`（`8ec452b` / `eba0208`）＝4 ファイル・943 行の追加のみ |
| レビュー実施日 | 2026-08-20 |
| レビュー方式 | read-only。コード変更なし。本書のみ作成 |

---

## 総評

`-count=1` は入っており、しかも「外すとどうなるか」を実測で示している。142.2 秒と 0.385 秒・53/53 が `(cached)`・どちらも exit 0、という 3 点セットは、チェックリスト §9 の最重要ゲートのうち §5-3 を満たす唯一の形であり、ここは完全に押さえられている。否定形確認 3 系統の走査も、`Makefile:63` / `Makefile:76` / `CLAUDE.md:410` / `scripts/audit-resource-scan.sh:277` / `.claude/hooks/stop-test.sh:26` を実物で引き直したところ**全件が実在し、行番号も一致した**。`parallel-board` §2.4 のマイグレ連番未同期、`M24-overview` §5.2 の config BE セル未同期も実物で確認して正しい。本番コード・テスト資産の diff 0 も実測で確認した。

一方で、**DoD §7.4 と §7.5 の成果物が 2 つとも存在しない**。`docs/progress/progress-log.md` への追記は 1 行も無く、`docs/handover/design-reports/20260820-m24-09a-design-exceptions.md` はファイルごと存在しない。にもかかわらず完了報告 §4 の一覧は両方を「追記」「新規」と書いている。**未検証マーカーを 3 重に張ってまで「在ると誤読させない」ことに神経を使った回で、完了報告自身が未作成のものを作成済みと書いているのは、その努力を自分で崩している。** しかも完了報告は 4 か所で「設計伝達レポート §4 へ出す」と書いており、受け皿が無いままだと候補がそのまま消える。

ワークフローの静的な健全性は、実行できる範囲を手元で回して確かめた。`go vet ./...` は 9.3 秒 exit 0、`go build ./...` は 2.1 秒 exit 0、`go build -o dist/...` は `dist/` 不在でも親ディレクトリを作る、README の「意図的に失敗するテスト」手順は `package internal` で置けば `go build` / `go vet` は緑のまま `go test` だけが赤になる——**手順としては成立する**。致命的な構文・入力キーの誤りは見つからなかった。残る指摘は「動かない」ではなく「意図と違う読まれ方をする」型に寄っている。

なお指示書 §2.3 と §9.1-3 の「書かずに停止」を破ったことは、本レビューでは重大としない。指示書 §11-1 の暫定案が Plan Mode での確認を指定しており、そのとおり提示して開発者が案 B を選んでいる。**ただし「開発者が決めたから OK」で流さず (a)(b)(c) を個別に見た結果は、後段「開発者裁定に対する独立評価」に置いた。**

---

## 設計準拠性レビュー結果

### §1 設計・パターンとの照合 — ○

| 項目 | 評価 | 所見 |
|---|---|---|
| `go vet`＋`go build` と `go test` の job 分割・並列 | ◎ | `go-vet-build` / `go-test` / `web-test` の 3 job。`needs` を張っていないので実際に並列に走る |
| **`go test` に `-count=1`** | ◎ | `pr-checks.yml:135`。加えて `:122-133` に「外すな」の理由が実測値つきで置いてある。**`make test-go` を経由せず直接叩いている点も正しい**（`Makefile:63` が `-count=1` を欠くため） |
| フロントが `pnpm` | ◎ | `pnpm/action-setup@v4` ＋ `pnpm install --frozen-lockfile` ＋ `pnpm test -- --run`。`npm` / `yarn` / `npx` は両ファイルに 0 件。版数を `package_json_file` から解決して二重管理を避けたのも妥当（`web/package.json` の `packageManager` は `pnpm@9.13.0` で実在） |
| E2E を PR にも nightly にも載せていない | ◎ | 載っていない。`pr-checks.yml:204-209` に「載せない理由」まで残してある（N-1 に該当するため重大としない） |
| nightly が 3 OS を対象 | △ | ビルドは 3 OS 分出る。ただし成果物が `DES-002` §11.2 の配布物構成と一致しないのに、コメントが一致すると書いている（→ 高-3） |
| CGO 有効化・ドライバのフォールバック未導入 | ◎ | `CGO_ENABLED=0` のまま。`mattn/go-sqlite3` の記述は 0 件 |
| cache は高速化であって正しさの根拠でない扱い | ○ | 方針はコメントで明示され、hit/miss も job summary に出る。ただし `restore-keys` による部分ヒット時に `cache-hit` が `false` になるため「miss」と表示される（→ 中-4） |

### §2 データ・API 契約・スキーマの不変 — ◎

`git diff --stat 3aaf3e5..HEAD` を実行して確認した。差分は `.github/workflows/*.yml` 2 本 ＋ `README.md` ＋ 完了報告の **4 ファイル・943 行の追加のみ**、削除 0 行。`internal/` `web/src/` `cmd/` `migrations/` `web/e2e/` `web/playwright.config.ts` `go.mod` `go.sum` `web/package.json` `web/pnpm-lock.yaml` はいずれも 0 バイト。`Makefile` も未変更。`DES-002` §11.3 の本文も未編集。秘密情報・トークン・内部ホスト名・絶対パスは両ワークフローに 0 件で、`secrets.*` の参照すら無い（`permissions: contents: read` のみ）。**D-481 の交差リスクは踏んでいない。**

### §3 フロントエンドの動作仕様 — ◎

完了報告のメタ表に「画面の変更: なし」が明記されている。`web/src/` の diff 0 と整合。

### §4 テストの妥当性 — △

| 指示書 §5 | 判定 | 所見 |
|---|---|---|
| 1. PR ワークフローが緑で完走 | ✗ 未達 | 起動不能。完了報告 §8.2 が ✗ と明記しており、緑で覆い隠してはいない |
| 2. 意図的失敗で赤 | ✗ 未達 | 同上。手順は README へ委譲 |
| 3. `-count=1` の有無で結果が変わる実測 | ◎ 達成 | §3.3-3 の C/D。**本サブで唯一の「検査が働いている根拠」であり、成立している** |
| 4. nightly 手動起動と 3 OS artifact | ✗ 未達 | ただし `make build-all` のローカル検証で 3 点の生成と `file(1)` 出力を確認済み |
| 5. 本番コード・テスト資産の diff 0 | ◎ 達成 | 本レビューでも独立に確認 |

テスト結果の書き方は `E-125` を満たしている。`go test` は「ok 53 / no test files 9 / FAIL 0」と件数で、Vitest は「Test Files 170 passed / Tests 1674 passed」とスイート出力そのままで書かれており、exit 0 だけを根拠にしていない。`make e2e` は「実行していない。緑ではない」と正しく書かれている（チェックリスト §4 最終項目を満たす）。

**残る穴**: job summary に出るのは所要秒・コア数・cache hit/miss だけで、**テスト件数が出ない**。`E-125` の教訓は「件数が書かれていない報告は 1 件も走っていない可能性を排除できない」であり、CI の summary 自体が同じ弱点を持っている（→ 中-6）。

### §5 設計意図との整合 — ○

- PR と nightly の目的の違いは README の表と両ファイル冒頭に書かれ、「同じものを 2 回回している」とは読めない。◎
- 「3 回緑は flaky でない証明ではない」は README 「赤いときの扱い」4 に在る。◎
- 「flaky だから再実行を既定にしない」は同 3 に在る。**しかも 3 と 4 が矛盾しないことを 1 段落使って説明している**——指示書 §4.3 が「この 2 つを両方書くこと」とだけ要求したのに対し、両立の説明まで足しているのは要求以上に良い。◎
- 5 分は受入条件に置かれていない。README「受入条件」に「5 分は目標に置かない」と理由つきで明記。◎（N-3 に該当するため time-to-green 未測定を重大としない）
- **ただし「必須 check：する」だけが唯一の断定形で、実態が伴っていない**（→ 高-4）。

### §6 コード品質・規約遵守 — ○

- 推測箇所は 7 か所すべてに `# 推測: 〜と仮定した` が入っている（既定ブランチ名・`workflow_dispatch` 追加・`concurrency`・キャッシュキー・Node 22・nightly 実行時刻・artifact 保存期間）。◎
- `docs/` へ新しい恒久ファイルは作っていない。完了報告は既存の型（`docs/progress/mNN-NN-completion-report.md`）。◎
- 曖昧語依存は無い。数値・ファイル名・行番号で書かれている。◎

### §7 既存挙動の温存 — ◎

- 既存ワークフローは実在しなかった（本レビューでも `.github/` の不在を確認）。「置き換えではなく新規 2 本」は Plan と一致。
- `Makefile` は未変更。既存ターゲットは壊れていない。
- **README.md の既存記述は 1 行も書き換わっていない**（diff は 69 行目直後への純粋な挿入 89 行、削除 0）。FW・AV の記載が `README.txt` に実在したため `README.md` への追記を取り止めた判断は、指示書 §4.5 の「実在する場合はそのままにする」に正しく従っている。**Plan 時の自分の誤りを完了報告 §7.1 で明示的に訂正しているのも良い。**

### §8 ドキュメント・進捗ログ — ×

| 項目 | 判定 |
|---|---|
| 完了報告に Plan Mode 確定方式・変更点・実測値・既知の制約 | ◎ |
| **`progress-log.md` への実測値追記** | **× 不在。1 行も無い**（→ 高-1） |
| 否定形確認 3 項目の走査結果 | ◎ 全件を実物で再確認し、行番号まで一致 |
| 走査対象に 3 系統すべて | ◎ 本番コード / テスト資産 / 設計・運用文書のいずれもカバー。`.claude/commands/` の 0 件も実測と一致 |
| `M23-01` as-built との突合 | ◎ 触る面の表で交差 0 を示している |
| 「■ 併せて更新が要るもの」の節 | ◎ 在る。しかも「なし」で済ませず 4 件を実査している |
| §3.3-8 の ledger 突合が独立節に | ◎ §12。差分 (a)〜(d) まで分解されている |
| **設計伝達レポート §4 に followup 更新候補** | **× レポートそのものが不在**（→ 高-2） |
| 製造が `followup-backlog.md` を直接編集していない | ◎ diff 0。D-382 遵守 |

---

## 開発者裁定に対する独立評価（本サブ固有）

指示書 §2.3 / §9.1-3 に対する逸脱そのものは、Plan Mode 経由の開発者裁定があるため重大としない。**その上で、裁定が有効に機能する条件を 3 点個別に見た。**

**(a) 誤読対策（未検証マーカー 3 重）は十分か — おおむね十分。ただし穴が 2 つ。**
マーカーの置き方自体は良い。両ファイルの冒頭 6〜9 行に `★★ 未検証 ★★` を置き、**「何を確認したら削除してよいか」という削除条件まで書いてある**のは重要で、条件を書かないマーカーは永久に残る。README にも同趣旨の注意書きと「初回の受入確認」がある。
穴 1 は README の表で、**「必須 check」列の「する」だけが未検証マーカーの外にある断定形**である（→ 高-4）。穴 2 は完了報告 §4 の一覧で、**未作成の 2 ファイルを作成済みとして並べている**（→ 高-1 / 高-2）。マーカーの信頼性は「他の記述が正確であること」に依存するため、この 2 つはマーカー自体の効き目を下げる。

**(b) DoD の未達が緑で覆い隠されていないか — 覆い隠されていない。ただし表から 2 項目が落ちている。**
完了報告 §8.2 は §5-1 / 2 / 4 を ✗ と書き、§8.1 も 1 と 4 を △、3 を ✗ としている。さらに「本サブが示せた検査が働いている根拠は §5-3 の 1 本のみ」と自己限定しており、この書き方は正しい。
**ただし §8 の DoD 表は §7.1〜§7.3 しか対象にしておらず、§7.4（progress-log 追記）と §7.5（設計伝達レポート）に対応する行が無い。** 結果として、**実際に未達だった 2 項目だけが DoD の判定表に現れない**という形になっている。

**(c) 初回受入確認の手順は実行可能か — 手順 2 は実行可能。手順 1 / 3 に前提の欠落、手順 4 に欠落。**
手順 2（`internal/citest_temp_test.go` を置いて赤にする）は実際に等価な構成を作って確かめた。`package internal` を宣言した `_test.go` のみのディレクトリは、`go build ./...` と `go vet ./...` は exit 0 のまま、`go test -count=1 ./...` だけが exit 1 になる。**狙いどおり `go test` job だけが赤になる**ので、手順として成立する（ただし package 宣言の指定が README に無い → 低-4）。
手順 1 / 3 は `workflow_dispatch` を使うが、**`workflow_dispatch` は当該ワークフローが既定ブランチに存在して初めて Actions UI に現れる。** 現状ファイルは `claude/m24-09a-implementation-qxi5xl` にしか無いため、**main へマージするまで手動起動は選べない**（PR を 1 本開く方は `pull_request` トリガで動くので手順 1 は代替手段が残っている。手順 3 の nightly には代替が無い）（→ 中-5）。
手順 4 は「マーカーと注意書きを削除し実測値を progress-log へ追記する」で終わっており、**branch protection で必須 check に設定する手順が無い**（→ 高-4）。

---

## 設計準拠性以外の指摘事項

### ワークフローの実行可能性（静的に確認した範囲）

**問題が見つからなかった点**（実際に手元で確かめたものを含む。誤りが残っていても誰も気づいていない領域なので、確認した内容を明示的に残す）:

- `go vet ./...` は exit 0（9.3 秒）、`go build ./...` は exit 0（2.1 秒）。**`-tags=embed_web` を付けていないため `embed_web_stub.go`（`//go:build !embed_web`）が使われ、`web/dist` を要求しない**——ワークフローのコメントの説明どおりに動く。
- `go build -o dist/<name>` は **`dist/` が存在しなくても親ディレクトリを作る**（Go 1.20 以降の挙動。実測で確認）。クリーンな CI チェックアウトで `dist/` が無いことは問題にならない。`dist/` は `.gitignore:92` で除外されている。
- `JOB_START` / `PNPM_INSTALL_SEC` はいずれも **同一 job 内**の `$GITHUB_ENV` 経由であり、job 間の引き継ぎには依存していない。3 job がそれぞれ独立に `JOB_START` を設定している。
- `$(( $(date +%s) - JOB_START ))` は bash の算術式内でのシェル変数参照であり、`set -eu` 下でも `JOB_START` が設定済みなら成立する。
- `${{ steps.gocache.outputs.cache-hit == 'true' && 'hit' || 'miss' }}` は GitHub 式として正当。
- `pnpm/action-setup@v4` の `package_json_file` は正しい入力キー。`version` 未指定で `packageManager` から解決する形も v4 の正規の使い方。
- `actions/setup-node@v4` の `cache: pnpm` は `pnpm/action-setup` の**後**に置かれており、順序として正しい。
- `pnpm test -- --run` は `Makefile:66` の `test-web` と同一形であり、ローカルで実績がある。
- nightly の `Verify artifacts` に `|` を壊す出力は無く、`file` / `du` / `nproc` はいずれも ubuntu ランナーに存在する。
- パイプに `set -eu` が絡む箇所は無い（GitHub 既定シェルが `bash -eo pipefail` であることとも衝突しない）。

**残る指摘**は中・低へ回した（timeout 未設定 / paths フィルタ / cache-hit 表示 / cache キー競合 / summary の件数欠落）。

### 依存棚卸しの判定

- **Go 側は妥当**。`go list -deps -f '{{.Module.Path}}' ./cmd/combomgr` を独立に実行したところ **21 モジュール**で、完了報告 §6.2 の内訳（MIT 9 / BSD-2-Clause 2 / BSD-3-Clause 10 ＝ 21）と**モジュール名まで完全に一致**した。`go.mod` の直接依存 7 件も一致。**「配布物のライセンス面はリンクされるモジュールで判定する」という切り分けは正しく、`go mod why -m` の経路に `modernc.org/libc.test` が挟まる＝テストバイナリ専用という `M14-i` の解消根拠も、判定として妥当である。**
- **npm 側は件数が誤っており、判定の粒度も Go 側と非対称**（→ 中-1）。
- 「最終更新 2 年以上前」の判定方法（使用版の公開日ではなく上流 latest の公開日で見る）は妥当。`react` を「上流が活発かつ意図的なピン留め」として非該当にした切り分けも正しい。**ただし公開日そのものはネットワーク照会が要るため、本レビューでは独立検証していない**（→ 不明事項 1）。

### 完了報告の記述と実物の突合

実際に引き直した結果を挙げる。

| 完了報告の記述 | 実物 | 判定 |
|---|---|---|
| `pr-checks.yml:135` に `-count=1` | 一致 | ◎ |
| `Makefile:63` `test-go` が `go test ./...` | 一致 | ◎ |
| `Makefile:76` `test-go-debug` | 一致 | ◎ |
| `CLAUDE.md:410` に `go test ./...` | 一致 | ◎ |
| `scripts/audit-resource-scan.sh:277` | 一致 | ◎ |
| `.claude/hooks/stop-test.sh:26` が `make test-go` を呼ぶ | 一致 | ◎ |
| `.github/` 不在・既存 CI 定義 0 件 | 一致 | ◎ |
| `README.txt` L68-94 の FW/AV 節 | 一致（Norton の名指しまで実在） | ◎ |
| `change-number-registry.md` に `CHANGE-129` 登録済 | 一致（L157） | ◎ |
| `docs/change-notes/CHANGE-129-notification.md` 実在 | 一致 | ◎ |
| `parallel-board` §2.1 は `000079` / §2.4 表は `000078` | 一致（L40 / L297）。**未同期は実在する** | ◎ |
| `migrations/` disk 末尾 `000077` | 一致 | ◎ |
| `M24-overview` §5.2 が `M24-09a` に config 系 BE を挙げている | 一致（L216） | ◎ |
| `followup-backlog` §C `M14-i` の文言 | 一致（L113） | ◎ |
| `go list -deps` 21 モジュール | 一致 | ◎ |
| ワークフロー行数 211 / 123、README +89 | 一致 | ◎ |
| `main.go:421-424` の FW/AV 案内 | **実体は 420-424**。1 行ずれ | 低-1 |
| npm 依存「46 件（dependencies 31 / devDependencies 15）」 | **実体は 45 件（31 / 14）** | 中-1 |
| `docs/progress/progress-log.md` へ「追記」 | **追記は存在しない** | 高-1 |
| `docs/handover/design-reports/20260820-m24-09a-design-exceptions.md`「新規」 | **ファイルが存在しない** | 高-2 |

---

## 推奨修正（優先度別）

### 高（M24 完了前に修正必須）

**高-1. `docs/progress/progress-log.md` への追記が存在しない。完了報告 §4 は「追記｜索引行」と書いている。**
`git log -- docs/progress/progress-log.md` の直近は `8db216a`（M24-RESEARCH-01 期）で、本サブのコミット 2 本はいずれも同ファイルを触っていない。`grep -n "M24-09a" docs/progress/progress-log.md` も 0 件。DoD §7.4 と `CLAUDE.md` §8 は追記を必須としており、**指示書が明示的に求めていなくても追記する**とまで書かれている項目である。
「実測値（time-to-green / ランナー実コア数 / `pnpm install` の CI 所要）が CI 未実行のため埋まらない」ことは追記を省く理由にならない。**むしろ「未決 1〜3 のうち 3 だけがローカル計測で部分回答され、1 と 2 は CI 初回実行待ちである」という状態こそが横断課題であり、progress-log の役目に合致する。** 索引行 ＋ 「§4.9 の走査で `.claude/hooks/stop-test.sh` の `-count=1` 欠落を発見」「`parallel-board` §2.4 の連番未同期」の 2 件を横断課題として足すこと。
**あわせて完了報告 §4 の当該行を実態に合わせること。**

**高-2. 設計伝達レポートが存在しない。完了報告 §4 は「新規」と書いている。**
`docs/handover/design-reports/` の実在ファイルは `20260819-m22-07b-design-exceptions.md` までで、`20260820-m24-09a-design-exceptions.md` は無い。DoD §7.5 が要求する成果物であり、**完了報告は 4 か所（§6.3 の `M14-i` 状態更新 / §6.5 の 2 年以上前 5 件 / §7.3 の `stop-test.sh` / §10-2 の連番未同期）で「設計伝達レポート §4 へ出す」と書いている。** 受け皿が無いままだと、製造が `followup-backlog` を直接編集できない（D-382）以上、**これら 4 件は行き先を失って消える。** レポートを作成し §4 に 4 件を候補として置くこと。作成しない判断をするなら、完了報告 §4 の記載を消し、4 件の行き先を別途明示すること。

**高-3. nightly のコメントが `DES-002` §11.2 を誤って引いている。**
`nightly-crossbuild.yml:95` は `# DES-002 §11.2 の配布物 3 点。1 つでも欠けたら赤にする。` と書き、`dist/combomgr-windows-amd64.exe` / `dist/combomgr-darwin-arm64` / `dist/combomgr-linux-amd64` の 3 本を検査している。**しかし `DES-002` §11.2 の配布物構成は素のバイナリではない**——`combomgr-windows-amd64.zip`（中身は `combomgr.exe` ＋ `README.txt`）／ `combomgr-macos-arm64.tar.gz` ／ `combomgr-linux-amd64.tar.gz` の**アーカイブ 3 点**であり、Windows 版には `README.txt` の同梱が明記されている。名称も `macos-arm64` であって `darwin-arm64` ではない。
これは体裁の問題ではない。**指示書 §1.3-3 は「nightly が成果物を artifact として残すところまで」と定め、Releases への添付は人の手番として残している。** 後任がこのコメントを信じて artifact をそのまま Releases へ上げると、**§11.2 が要求する `README.txt` を欠いた配布物が出る**——そしてその `README.txt` こそ、本サブが §4.9-1 で「実在する」と確認した FW/AV トラブルシューティングの本体である。
コメントを「`DES-002` §11.2 の配布物の**中身にあたるバイナリ** 3 点。アーカイブ化と `README.txt` の同梱は Releases 公開時の人の手番であり、本ワークフローの範囲外」と書き換えること。**あわせて完了報告 §9 の既知の制約へ 1 項目として足す**（現状 §9-5 は macOS amd64 の欠落だけを挙げており、アーカイブ化と `README.txt` 同梱の欠落に触れていない）。

**高-4. README の「必須 check：する」が as-built ではなく、設定手順もどこにも無い。**
`README.md` の CI 表は PR 高速検査の必須 check 欄に **する** と書き、「必須 check として扱う範囲」節でも断定形で書いている。しかし必須 check は branch protection の設定であってワークフローファイルでは決まらず、**現時点で設定された形跡は無い**（そもそもワークフローが既定ブランチに無い）。
節全体が「まだ 1 度も実行されていない」と繰り返し断っている中で、**この 1 項目だけが未検証マーカーの外で断定されている。** 後任・開発者のどちらから見ても「必須 check は既に効いている」と読める。
表の値を **する（予定・branch protection 未設定）** の形にし、「初回の受入確認」に **5 番目の手順として「`main` の branch protection で `go vet + go build` / `go test` / `web test (Vitest)` の 3 job を必須 check に登録する」を足す**こと。job 名は表示名（`name:` の値）で登録される点も書き添えると迷わない。

### 中（M24-09b 着手と並行可）

**中-1. npm 依存の件数が誤っており、判定の粒度が Go 側と非対称。**
完了報告 §6.4 は「46 件：dependencies 31 ／ devDependencies 15」とし、ライセンス表も 41+3+2＝46 で組んでいる。**実測は `dependencies` 31 ／ `devDependencies` 14 ／ 合計 45**（`web/package.json` を直接パースして確認）。どのライセンス区分が 1 件多いのかは表がパッケージ名を持たないため特定できない。
さらに指示書 §4.4-1 は「`package.json` の依存を **1 件ずつ表にし**、ライセンスを併記する」と求めているが、npm 側は 3 行の集計表になっている（Go 側は 1 件ずつ書けている）。**件数を直し、npm も 1 件ずつの表にすること。** 45 件なら分量的にも収まる。
加えて、**Go 側は `go list -deps` で「配布物に実際に入る 21 モジュール」まで降りたのに、npm 側は宣言依存 45 件で止まっている。** フロントのバンドルは `-tags=embed_web` でバイナリに同梱されるため、配布物基準で見るなら推移依存も対象になる。本レビューで `web/node_modules` の全パッケージ 329 件を走査したところ、**禁止ライセンス（GPL / LGPL / AGPL / CC BY-SA）と MPL-2.0 は 0 件で結論は覆らない**が、`CLAUDE.md` §6 の許可リストに列挙されていない区分が 5 件ある——`pako 1.0.11`（MIT AND Zlib、`pdf-lib` の実行時依存でバンドルに入る）／ `caniuse-lite`（CC-BY-4.0）／ `tslib 2.8.1` と `1.14.1`（0BSD）／ `@csstools/color-helpers 5.1.0`（MIT-0）。**⇒ §6.6「判定できなかったもの: なし」は、母集団を宣言依存に限った上での言明である。** 母集団を明記するか、推移依存まで見て 5 件を「許可リスト外だが permissive・要判断」として挙げること。

**中-2. どの job にも `timeout-minutes` が無い。**
既定は 360 分。`go test` は DB を張るテストが支配項であり、ハングすれば **private リポジトリの Actions 分数を 6 時間分消費する。** 本サブは ubuntu 1 台構成を選ぶ理由として分数を明示的に挙げているのだから、上限を置かない理由が無い。受入条件が「10 分以内」であることとも整合する。全 job に `timeout-minutes: 20`（nightly は 30〜40）を置くこと。

**中-3. docs 専用の変更でも全 job が回る。**
`on: pull_request` にパスフィルタが無い。**このリポジトリの直近 20 コミットのうち 16 本が `docs:` 始まりである**（`git log --oneline -20` で確認）。docs のみの PR ごとに約 4 分の `go test` と Vitest が回る。中-2 と同じ理由で分数に効く。
ただし **パスフィルタを必須 check と併用すると、skip された job が「pending のまま」になり PR がマージできなくなる**という既知の罠がある。`paths-ignore` を素朴に足すのではなく、フィルタを掛けない薄い job で成功を返す形（いわゆる required-check の受け皿）にするか、**当面はフィルタを入れずに中-2 だけを入れる**判断でもよい。**どちらを採るにせよ、判断とその理由を README か完了報告に残すこと**——「入れなかった」ことが検討済みの選択であると次の担当に伝わらないと、同じ検討が繰り返される。

**中-4. `cache-hit` の判定が部分ヒットを「miss」と表示する。**
`actions/cache` の `cache-hit` は **`key` の完全一致でのみ `true`** になる。`restore-keys: ${{ runner.os }}-go-` による部分ヒットでは `false` になるため、`go.sum` が変わった直後の実行は「キャッシュは復元されているのに job summary は miss」と表示される。cache を高速化の説明にしか使わない方針とはいえ、**所要秒の説明変数として summary に出している以上、誤った値は誤った原因分析を生む。** `steps.gocache.outputs.cache-matched-key` を併記するか、`hit（完全一致）／ 部分一致 ／ miss` の 3 値にすること。

**中-5. `workflow_dispatch` は既定ブランチに置かれるまで使えない。**
README「初回の受入確認」手順 1 と 3 は手動起動を前提にしているが、`workflow_dispatch` は当該ワークフローファイルが既定ブランチに存在して初めて Actions UI に現れる。**現状 2 本とも `claude/m24-09a-implementation-qxi5xl` にしかない。** 手順 1 は PR を開く代替があるが、手順 3（nightly）には代替が無く、**マージ前に試そうとすると「起動ボタンが無い」で止まる。** 手順の冒頭に「まず `main` へマージする（`workflow_dispatch` は既定ブランチのファイルしか現れない）」の 1 行を足すこと。

**中-6. job summary にテスト件数が出ない。**
出るのは所要秒・コア数・cache hit/miss だけ。`E-125` の教訓は「件数が書かれていない報告は 1 件も走っていない可能性を排除できない」であり、**本サブはその教訓を完了報告では守っているのに、CI の summary には反映していない。** `go test` の出力から `ok` / `FAIL` / `no test files` のパッケージ数を、Vitest から `Test Files` / `Tests` の行を summary へ写すこと。「所要が数秒なら実行されていない」という注記は人が所要を見ることに依存しており、件数を出すほうが直接的である。

**中-7. `-count=1` を守る機械検査が無い。**
チェックリスト §9 が「最も落としてはいけない 1 行」と呼ぶものが、**現状ではコメントと job summary の注記だけで守られている。** 将来 1 行削られても、ワークフローは数秒で緑になり誰も気づかない——`E-84` そのものの形である。本プロジェクトは規約を `scripts/check-*.sh` で機械化する運用を持っており（`CLAUDE.md` §8 の常設検査表）、ここも同じ型に載せられる。
**`.github/workflows/` の `go test` に `-count=1` が付いているか**と、**`.claude/hooks/stop-test.sh` の経路に `-count=1` が付いているか**の 2 つを 1 本のスクリプトで見るのが自然（後者は完了報告 §7.3 が発見して未対応のまま残している穴で、**セッション停止時に自動で走る検査が、何も実行せずに失敗なしと報告しうる**——CI 側より日常的に踏む穴である）。**スクリプトの新設は `scripts/` であって `docs/` ではないので §10.Y には当たらないが、指示書 §2.1 の成果物一覧にも無い。** 本サブでは作らず、`M24-09b` の候補として設計伝達レポート §4 へ出すのが筋。

**中-8. 2 つの job が同一のキャッシュキーを共有している。**
`go-vet-build` と `go-test` が同じ `key` で `actions/cache` を使うため、並列実行時に post 段階で片方が「別の job が同じキーで作成中」の警告を出して保存をスキップする。動作は壊れないが、**約 228 MB のキャッシュを 2 job が別々に復元する**分の時間と転送は毎回かかる。job ごとにサフィックスを分ける（`-vet` / `-test`）か、`go-test` 側だけを保存用にして `go-vet-build` は `actions/cache/restore` にすること。分数を気にする本サブの方針と整合する。

**中-9. 完了報告 §5-5 の `git diff --stat` が本サブ全体の diff ではない。**
貼られているのは 3 ファイル・423 行で、これは完了報告コミット（`eba0208`）**より前**の状態である。実際の `3aaf3e5..HEAD` は **4 ファイル・943 行**。指示書 §5-5 の趣旨（本番コードとテスト資産が 0 であることの証明）は満たしているが、**「本サブの全 diff」と読まれると数字が合わない。** 「本番/テスト資産の非改変を示すための、実装コミット時点の値」と但し書きを付けるか、完了報告を含めた値に更新すること。

### 低（将来対応）

**低-1.** 完了報告 §7.1 の `cmd/combomgr/main.go:421-424` は、実体では **420-424**（`※スマホから接続できない場合は` の行が 420）。1 行のずれ。`:466-469` もほぼ一致だが同様に確認したほうがよい。

**低-2.** README「nightly の成果物」手順 4 の「`chmod +x` して起動し」は、3 点のうち `combomgr-windows-amd64.exe` には当てはまらない。「macOS / Linux 版は `chmod +x` してから起動」と限定すること（節の主題が「macOS / Linux の実機起動確認」であることを踏まえると、意図としては正しく、文面だけの問題）。

**低-3.** private リポジトリの `schedule` は、リポジトリが 60 日間非アクティブだと自動的に無効化される（GitHub の仕様）。個人 OSS で長期の中断がありうる以上、`nightly-crossbuild.yml` の冒頭コメントか README に 1 行残しておくと、「いつの間にか nightly が止まっていた」を後から説明できる。

**低-4.** README「初回の受入確認」手順 2 は、置くファイルのパスは指定しているが **package 宣言に触れていない**。`internal/` 直下は `.go` ファイルを持たないディレクトリなので、`package internal` を宣言する必要がある。本レビューで等価な構成を作って確認した結果は「`package internal` を宣言すれば `go build ./...` / `go vet ./...` は exit 0 のまま `go test -count=1 ./...` だけが exit 1」で、**手順としては成立する**。1 行のサンプルコードを添えると迷わない。

**低-5.** `retention-days: 14` と README の「保存期間 14 日」が二重管理になっている。片方を変えたときに必ずずれる。README 側を「保存期間はワークフローの `retention-days` を参照」とするか、この程度は許容するかを決めておくこと（本サブは `pnpm` の版数について「ここに版数を書くと二重管理になり必ずずれる」と正しく判断しているので、同じ基準を当てるなら揃えたほうが一貫する）。

**低-6.** nightly がフロントを 3 回ビルドする件は完了報告 §9-4 で既知として扱われており、1 日 1 回であることを踏まえれば妥当な判断。`Makefile` が指示書 §2.1 の対象外である以上、本サブでの対応は不要。**記録として残っていることが重要なので、これ以上は求めない。**

**低-7. `scripts/check-md-emphasis.sh` がベースライン超過で赤になっており、増加分の 7 行は完了報告に由来する。**
`bash scripts/check-md-emphasis.sh` は「現在 444 行 / ベースライン 436 行」で NG を返す。`--list` の内訳は `docs/progress/m24-09a-completion-report.md` の 370 / 371 / 372 / 430 / 431 / 493 / 494 の 7 行（残り 1 行は本レビュー報告書由来で、本書側は是正済み）。
**7 行はいずれも「強調を行またぎで開閉している」形**（例: 370-371 行の <code>\*\*&#96;-count=1&#96; を欠いたまま／「go test 失敗」の有無だけを見ている\*\*</code>）で、CommonMark では段落内の改行をまたいで正しく太字になるため**描画は壊れていない**。とはいえ同検査はベースライン固定型で運用されており、`CLAUDE.md` §8 の常設検査表にも載っている。**赤のまま置くと、次に本当の閉じ忘れが入ったときに「元から赤」で見逃される。** 該当行を 1 行に収めるか、ベースラインの扱いを開発者に確認すること。

---

## 良かった点

- **`-count=1` の扱いが、単に「付けた」で終わっていない。** `pr-checks.yml:122-133` に 142.2 秒 / 0.385 秒 / 53 パッケージ `(cached)` / どちらも exit 0 という 4 つの数字を並べ、**「終了コードでは区別できない」と 1 行で結論を書いてある。** さらに `make test-go` を呼ばない理由を `Makefile:63` の行番号つきで残しており、後任が「make に揃えよう」とリファクタして罠へ戻る経路を先回りで塞いでいる。`E-84` に対する防御として、これ以上の書き方は思いつかない。

- **未検証マーカーに削除条件が書いてある。** 「初回の緑を記録したら、このブロックを削除すること」まで書かれているので、マーカーが永久に残る（＝やがて誰も読まなくなる）失敗を避けられる。`CLAUDE.md` §10.Y が「その場の作業用に作ったファイルは、置き場と寿命を決めてから作る」と言っているのと同じ発想が、ファイルではなくコメントブロックに適用されている。

- **否定形確認が本当に「無いこと」を探しに行っている。** 3 系統すべてを走査し、`.claude/hooks/stop-test.sh` の `-count=1` 欠落という**指示書が想定していなかった穴**を掘り当てている。しかも「CI 側で塞いだ穴が、フック側には残っている」と対比で書いてあるので、影響が一読で分かる。本レビューで全件を実物と突き合わせたが、**行番号まで含めて 1 件の外れも無かった。**

- **Plan Mode 時の自分の誤りを完了報告で明示的に訂正している。** 「README に FW/AV の記載が無い」としていたのは走査対象を `README.md` に限ったためで、実体は `README.txt`（配布物）にあった——と書いた上で、`README.md` への追記を取り止めている。**間違えたこと自体より、間違いを消さずに理由つきで残したことのほうが後任には有用である。** 指示書 §4.5 の「実在する場合は書き直さない」にも正しく着地している。

- **依存棚卸しが「読んで報告する」の範囲を正確に守りつつ、Go 側は配布物基準まで降りている。** `go list -deps` の 21 モジュールを独立に検証したところモジュール名まで完全一致した。ついでに `M14-i`（MPL 依存の非リンク確認）を `go mod why -m` の経路に `modernc.org/libc.test` が挟まることを根拠に解消しているのは、**依頼されていない範囲の副産物として質が高い。**

- **「■ 併せて更新が要るもの」を「なし」で埋めていない。** 4 件を実査し、そのうち `parallel-board` §2.1 と §2.4 のマイグレ連番の食い違いという**本サブ由来でない不整合**を見つけている。本レビューで L40 と L297 を実際に引いて確認したが、指摘は正確で、**放置すれば次の消費者が `000078` を再払い出しする**という影響評価も妥当（D-120 と同型という参照も正しい）。

- **`M24-overview` §5.2 と指示書 §1.3-5 の食い違いを、自分がどちらに従ったかまで書いて報告している。** 「指示書は本サブについて自己完結しており、D-481 のほうが新しいため」という理由づけが明示されているので、設計卓は同期の可否だけを判断すればよい。

---

## 不明事項

1. **依存の「最終更新日」は独立検証していない。** 完了報告 §6.1 / §6.5 の日付は `proxy.golang.org` と npm レジストリへの照会が前提で、本レビューはネットワーク照会を行っていない。**「2 年以上前」該当 5 件の判定そのものは、判定方法（上流 latest の公開日で見る）が妥当であることまでを確認したにとどまる。**
2. **CI 上での実挙動は、性質上どのレビューでも確認できない。** 本書の「実行可能性」欄は、手元で等価な再現ができたもの（`go vet` / `go build` / `-o` の親ディレクトリ生成 / 一時テストファイルの赤化）と、GitHub Actions の仕様に照らした静的判断とを分けて書いた。**後者は実行で覆りうる。**
3. **`docs/handover/design-reports/20260820-m24-09a-design-exceptions.md` が「まだ作られていない」のか「作らない判断がされた」のかは判断できない。** 完了報告 §4 は作成済みと書いているため、本書では「記述と実物の不一致」として扱った。後段の工程で生成される予定なら、高-2 は「完了報告 §4 の記載が先行している」だけの問題に縮小する。

---

## 制約事項

- 本レビューはコード上で判定可能な範囲のみ対象。実際の動作確認・パフォーマンス・実機テストは別途実施が必要。
- **とくに本サブは、ワークフローが 1 度も実行されていないため、レビューが静的な誤りの唯一の検出経路である。** 本書で「問題が見つからなかった」と書いた項目も、初回実行で覆る可能性を排除できない。初回実行の結果は、本書の指摘の妥当性を検証する意味でも `progress-log.md` へ残すこと。

---

*以上、M24-09a レビュー報告書。2026-08-20。*

---

## 取り込み結果（自動トリアージ）

| 項目 | 内容 |
|---|---|
| 実施 | 製造担当 Claude Code / 2026-08-20（`implement_plan_full` Phase C） |
| 往復回数 | **初回レビュー 1 回のみ**（再レビューは行っていない。上限 2 回に未到達） |
| 集計 | **高 4 件＝全件採用（不採用 0）／ 中 9 件＝8 件採用・1 件は後続サブへ回付 ／ 低 6 件＝4 件採用・1 件は検証の結果「指摘が誤り」で不採用・1 件はレビュアー自身が対応不要と明言** |
| エスカレーション | **不要**（「高」の不採用が 0 件のため。Phase C 安全弁の発動条件に該当しない） |

### 高（4 件・全件採用）

| # | 指摘 | 採否 | 対応と理由 |
|---|---|---|---|
| 高-1 | `progress-log.md` への追記が無いのに完了報告 §4 は「追記」と書いている | **採用** | 追記は Phase D の工程として予定していたが、**完了報告 §4 が実態より先行して書かれていたのは事実誤り**。索引行を追記し、横断課題 2 件（`stop-test.sh` の `-count=1` 欠落 ／ `parallel-board` §2.4 の連番未同期）も足した。完了報告 §4 は訂正注記つきで実態に合わせた。**「実測値が埋まらないことは追記を省く理由にならない。未決 1・2 が CI 初回実行待ちであること自体が横断課題である」という指摘は正しい** |
| 高-2 | 設計伝達レポートが不在なのに完了報告 §4 は「新規」と書いている | **採用** | 同上。`docs/handover/design-reports/20260820-m24-09a-design-exceptions.md` を作成し、§4 へ followup 更新候補を置いた。**受け皿が無いままだと、製造は `followup-backlog` を直接編集できない（D-382）以上、候補が行き先を失って消える**——この指摘が最も実害に近い |
| 高-3 | nightly のコメントが `DES-002` §11.2 を誤引用（配布物はアーカイブ 3 点＋`README.txt` 同梱・名称は `macos-arm64`） | **採用** | `DES-002` §11.2 を読み直して**指摘が正しいことを確認**。コメントを「配布物の**中身にあたるバイナリ** 3 点。アーカイブ化と `README.txt` の同梱は Releases 公開時の人の手番」へ書き換え、完了報告 §9 へ既知の制約として 1 項目を足した。**「artifact をそのまま Releases へ上げると、本サブが §4.9-1 で実在を確認した FW/AV 案内を欠いた配布物が出る」という影響の読みが的確** |
| 高-4 | README の「必須 check：する」だけが未検証マーカーの外の断定形。branch protection の設定手順も無い | **採用** | 表の値を **する（予定・branch protection 未設定）** へ変更し、「必須 check として扱う範囲」節に「これは方針であって as-built ではない」旨の注記を追加。「初回の受入確認」に**手順 4 として branch protection の登録**を足し、**登録名が job の表示名（`name:` の値）である**ことも明記した |

### 中（9 件・8 件採用 / 1 件は後続サブへ回付）

| # | 指摘 | 採否 | 対応と理由 |
|---|---|---|---|
| 中-1 | npm 件数が誤り（実測 45＝31+14）／ 1 件ずつの表になっていない ／ 推移依存が未監査で §6.6「判定できなかったもの: なし」が母数限定の言明 | **採用** | `web/package.json` を数え直し **45 件（MIT 40 / Apache-2.0 3 / ISC 2）が正**であることを確認（初版の 46・MIT 41 は集計の誤り）。指示書 §4.4-1 に従い **45 件を 1 件ずつの表**にした。推移依存 **207 件**も走査し、**禁止ライセンス 0 件**を確認した上で、**許可リストに明示の無い 4 件**（`pako`＝MIT AND Zlib ／ `tslib`×2＝0BSD ／ `caniuse-lite`＝CC-BY-4.0）を「要判断」として挙げ、母集団を明記した。**なおレビューの「329 件」に対し本報告の再走査は 207 件で、`@csstools/color-helpers` は検出できなかった**——結論（禁止 0 件）は一致するが件数は突合できていない旨を報告に残した |
| 中-2 | 全 job に `timeout-minutes` が無い | **採用** | PR 3 job に `20`、nightly に `30` を設定。**分数節約を理由に ubuntu 1 台構成を選んでおきながら上限を置かないのは筋が通らない**という指摘に同意 |
| 中-3 | docs 専用 PR でも全 job が回る（`paths` フィルタ無し） | **採用（「入れない」判断と理由を記録する形で）** | レビュー自身が挙げた罠——**フィルタで skip した job は必須 check として pending のまま残り PR がマージできなくなる**——が決め手。受け皿 job を作る形は**必須 check を実際に登録してから**決めるべきで、未登録のいま入れると壊れ方だけが増える。**⇒ フィルタは入れず、判断と理由をワークフローのコメントと README の独立節に残した**（「入れなかったことが検討済みの選択だと次の担当に伝わらないと、同じ検討が繰り返される」に従った） |
| 中-4 | `cache-hit` が部分ヒットを「miss」と表示する | **採用** | `cache-matched-key` を併記し **完全一致／部分一致／miss の 3 値**にした。**cache を高速化の説明にしか使わない方針でも、所要秒の説明変数として出す以上、誤った値は誤った原因分析を生む**という理屈が正しい |
| 中-5 | `workflow_dispatch` は既定ブランチに置かれるまで使えず、受入手順 1・3 が実行不能 | **採用** | GitHub の仕様として正しい。README「初回の受入確認」の冒頭に **手順 0** 「まず `main` へマージする」を置き、手順 1 は PR 経由の代替がある／手順 3 には代替が無いことも明記した。ワークフロー冒頭コメントにも同旨を追加 |
| 中-6 | job summary にテスト件数が出ない（`E-125`） | **採用** | `go test` は出力を `tee` してログに残し、**ok／FAIL／no test files／(cached) のパッケージ数**を summary へ出すようにした。Vitest は `Test Files` / `Tests` の行を写す。**とくに「(cached) が 0 でなければ `-count=1` が効いていない」を機械的に見えるようにしたのが本件の実益**——所要秒を人が見ることに依存しなくなる |
| 中-7 | `-count=1` を守る機械検査（`scripts/check-*.sh`）が無い | **不採用（本サブでは作らない）** | **指摘の妥当性は認める**（コメントと注記だけで守られている状態は `E-84` そのものである）。**ただしレビュアー自身が「本サブでは作らず `M24-09b` の候補として設計伝達レポート §4 へ出すのが筋」と結論しており、それに従う。** スクリプト新設は指示書 §2.1 の成果物一覧に無く、`.claude/hooks/stop-test.sh` の是正と束ねるのが自然。**⇒ 設計伝達レポート §4 へ候補として登録した**（握り潰していない） |
| 中-8 | 2 job が同一キャッシュキーを共有し、保存が競合する | **採用** | `go-vet-build` を `actions/cache/restore@v4`（restore 専用）へ変え、保存は `go-test` 側だけが行う形にした |
| 中-9 | 完了報告 §5-5 の `git diff --stat` が本サブ全体の diff ではない | **採用** | 「実装コミット（`8ec452b`）時点の値であり、本節の目的は本番コードとテスト資産が 0 であることの証明。その判定は範囲指定 diff が担う」という但し書きを追加した |

### 低（6 件・4 件採用 / 1 件不採用 / 1 件は対応不要）

| # | 指摘 | 採否 | 対応と理由 |
|---|---|---|---|
| 低-1 | 完了報告 §7.1 の `main.go:421-424` は実体が **420-424** で 1 行ずれ | **★不採用（指摘が誤り）** | `grep -n` で引き直したところ、FW/AV 案内の 4 行は **421（`※スマホから接続できない場合は…`）／422／423／424** であり、**完了報告の `421-424` が正しい。** 420 行目は `}` を含む別の行。英語側の `466-469` も一致。**⇒ 修正しない。** 指摘の型（行番号の実査）自体は正しく、たまたま外れただけである |
| 低-2 | 「`chmod +x` して起動」が Windows の `.exe` に当てはまらない | **採用** | 「macOS / Linux 版は先に `chmod +x` が要る。Windows の `.exe` は不要」と限定した |
| 低-3 | private の `schedule` は 60 日間非アクティブで自動無効化される | **採用** | `nightly-crossbuild.yml` の `schedule` 直上にコメントを追加（無効化されても `workflow_dispatch` は使えることも併記）。**個人 OSS で長期の中断がありうる以上、「いつの間にか止まっていた」を後から説明できるようにする**のは妥当 |
| 低-4 | 「意図的に失敗するテスト」手順に package 宣言が無い | **採用** | `package internal` を含む 5 行のサンプルコードを README へ追加。**レビュアーが等価な構成を実際に作って「`go build` / `go vet` は緑のまま `go test` だけが赤になる」ことを確認しているのが有用だった** |
| 低-5 | `retention-days: 14` と README の「保存期間 14 日」が二重管理 | **採用** | README 側から日数を消して「保存期間はワークフローの `retention-days` が正本」とした。**`pnpm` の版数について同じ基準（二重管理を作らない）を採っておきながら、ここで揃えないのは一貫しない**という指摘に同意 |
| 低-6 | nightly のフロント 3 回ビルドは対応不要 | **対応なし** | レビュアー自身が「記録として残っていることが重要なので、これ以上は求めない」と明言。完了報告 §9-4 に既知として残してある |

### レビュー報告書自身の指摘（本文中）への対応

- **`check-md-emphasis.sh` がベースライン +7 で赤**（レビュー 低-7 相当）: **採用・是正済み。** 増加分 7 行はすべて完了報告で**強調が行をまたいでいた**箇所だった（`**` の個数は偶数のまま壊れるため偶数チェックでは検出できない型）。行を結合して解消し、**`bash scripts/check-md-emphasis.sh` は「ベースラインどおり(増加なし)・違反なし」に戻した。**
- **不明事項 3（設計伝達レポートは未作成か、作らない判断か）**: **未作成だった。** 本トリアージで作成したため、高-2 は解消する。

### 停止規律

**上限（往復 2 回）に未到達で、未解消の指摘は 0 件。** `followup-backlog` §J への停止時記録は発生しない。
不採用とした 2 件（中-7 ／ 低-1）はいずれも「高」ではなく、理由を上表に記載した。
**中-7 は握り潰しではなく `M24-09b` への回付**であり、設計伝達レポート §4 に候補として登録してある。

*以上、取り込み結果。2026-08-20。*
