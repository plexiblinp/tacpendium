# M24-03 完了報告: 比較・詳細・レシピ可読性

| 項目 | 内容 |
|------|------|
| 対象指示書 | `docs/instructions/M24-03-compare-detail-recipe-readability.md` **v1.1.0** |
| チェックリスト | `docs/instructions/reviews/M24-03-review-checklist.md` v1.1.0 |
| CHANGE | **`CHANGE-134`**（設計卓が起票済み。**製造は番号を消費していない**） |
| 着手基点 | `62b0240` |
| マイグレーション | **消費 0 本**（`ls migrations/*.sql \| wc -l` = 158。最新は `000079`。**次に払い出す番号は `000080` のまま**） |
| モック | `docs/progress/M24-03-mock/m24-03-screens.html`（**Plan Mode の提示に含めた**。§7-5 参照） |
| 実施日 | 2026-08-26 |

---

## 0. 要約

**9 項目すべてに手当てした。** うち 8 項目を実装し、**1 項目（`SM-096`）は実測の結果「現行コードでは症状が再現しない」ことが分かったため実装せず、成立を E2E で固定した**（指示書 §9.3 の形）。

**核心は「レシピの見せ方を 4 面へ 1 つの規則で通した」ことである。** `DES-005` §5.4 に 6 つ並んだままだった省略方式のうち **6 番目（通常は末尾省略、トグルで全文表示モードへ切替）を採り、1 つへ畳んだ。**

---

## 1. §3.3 着手前の実査 14 件（**母数付き**）

**★実査 3 と 14 はブラウザ実測である。** 使い捨ての Playwright 計測 spec（`web/e2e/` に一時作成）で採り、**計測後に spec は削除した**（計測器であって成果物ではない）。他の 12 件は静的実査である。

| # | 系統 | 結果 |
|---|---|---|
| **1** | II | **4 面は別々の実装で、共通部品を通っていなかった。** 一覧 `ComboTableRow.tsx:110-117` = `<TableCell className="max-w-xs truncate" title>` ／ 詳細 `ComboDetailRecipe.tsx:67-71` = `<p className="whitespace-pre-wrap break-words">`（**truncate も title もステップ改行も無し**） ／ 比較 `CompareTable.tsx:218-224` = `<span className="max-w-xs block truncate" title>` ／ モーダル `AddComboToCompareModal.tsx:119-124` = `<span className="truncate sm:max-w-[200px]" title>`。**⇒ 規則が 4 通りあった** |
| **2** | II | **共通部品は存在しなかった。** 空表示の文言 `"（レシピなし）"` は**実装 9 ファイル**に散在（`git grep -ln "（レシピなし）" 62b0240 -- 'web/src/**/*.tsx' 'web/src/**/*.ts' \| grep -v '\.test\.' \| wc -l` → **9**）。**⇒ 新設した**（§2） |
| **3** | **I** | **★★実測。§1.1 に別掲**（memo の見立てと機序が違った） |
| **4** | I | **ledger の記述は正しかった。** レシピ列は `ColumnVisibility.recipe` で ON/OFF 可（`constants/combo-list.ts:56`・既定 `true`）、行内で truncate、`title` に全文 |
| **5** | I | **「見にくい」の実体は表示幅の不足だった。** `CompareTable.tsx:220` は `max-w-xs`（=320px）で truncate、全文は `title` のみ。**ホバーの挙動ではなく「一発で見えないこと」が症状**（`SM-018` と同型） |
| **6** | **II** | **★共通ではない。コンボと別経路である。** セットプレイのレシピは **5 か所で個別描画**——`SetupAccordionItem.tsx:113`（全文折返し） ／ `SetupCandidateList.tsx:52`（裸のテキスト） ／ `LinkExistingSetupModal.tsx:62` ／ `SetupTreeRow.tsx:39`（`title` のみ） ／ `SetupSelectorModal.tsx:77`。**⇒ §1.3-6 のとおり本サブでは触っていない**（§6 に残課題として記載） |
| **7** | I | **`SM-003` は無かった（確認済み）。** `/compare` → `/combos/:id` のリンクは `ComparePage.tsx` / `CompareTable.tsx` / `CompareTargetList.tsx` に **0 件**。存在した導線は `ComparePage.tsx:141` の「← コンボ一覧へ」のみ |
| **8** | I | **`SM-061` は無かった（確認済み）。** ヘッダは `t("compare.comboHeader", { id })` + `formatStarterStatus` のみで、キャラ名は **0 件**。**`CompareTable` は既に `useCharacters()` を呼んでいた**（`:78`・custom_states 解決用）ため、取得経路は増やしていない |
| **9** | **I** | **★狭さが数で出た（実測）。** モーダルの候補レシピ幅 = **200px**、一覧 = **320px**、比較 = **320px**。**⇒ モーダルは一覧の 62.5%（200/320）。** モーダルの器は **489px**（**実測値**。クラスの公称は `max-w-lg` = 512px で、内側の余白ぶん実測が小さい。**以降も実測値と公称値を区別して記す**）。**40 文字のレシピが、一覧では 62%、モーダルでは 48% しか見えていなかった。** **★一覧から比較へ入る導線は既に在った**——`ComboListPage.tsx:133-136`（選択モード → 「比較」→ `/compare?ids=...`）、`MyComboPage.tsx:221` も同型。**⇒ モーダルは唯一の経路ではなく「狭い場所での妥協」である**（§1.1.4.2 の見立てを裏付けた） |
| **10** | **II** | **★混ざらない。`M15-05` の技名 1 行サマリは「読む面」に出ていなかった。** `formatRecipeLine`（`features/combo/utils.ts:78-95`）の呼び元は**編集面 3 本のみ**——`RecipeBuilder.tsx:144` / `SetupInputRow.tsx:52` / `SetupRegistrationSection.tsx:21`。`/combos` と `/combos/:id` には **0 件**。**⇒ 現状では衝突しない。それでも将来の混同を防ぐため、メモ 1 行目に ✎ 記号・専用 testid・`aria-label` を付けた**（§2.4） |
| **11** | I | **ledger の記述は正しかった（裏取り済み）。** `OKI_NO_GAUGE_LABEL = "ノーゲージ"` は `constants/oki.ts:33` に**定義 1 箇所・参照 0 件**だった |
| **12** | II | **★★本項の初出の答えは不完全だった（M24-03 レビュー 高-2 で是正）。** 初出は `okiOptionLabel(` の**呼び元 3 件**（`export-model.ts:185` / `ComboDetailMetadata.tsx:101` / `CompareTable.tsx:300`）だけを数えており、**`okiOptionLabel` を経由せずインラインで同じラベルを組み立てている 1 件を取りこぼしていた**——`ComboEditorBasicFields.tsx:501-502`。**⇒ 起き攻めラベルを組み立てている箇所は 4 件である。** 数え直し（**組み立ての材料そのものを引く形へ変えた**）: `grep -rn "OKI_USES_DR_LABEL\|OKI_TECH_TYPE_LABELS\|OKI_ATTACK_TYPE_LABELS\|OKI_NO_GAUGE_LABEL\|okiOptionLabel(" web/src --include=*.tsx --include=*.ts \| grep -v '\.test\.' \| grep -v '^web/src/constants/oki.ts'`。**★「関数の呼び元」だけを数えると、関数を経由しない組み立てが見えない。** 是正内容は §11 |
| **13** | II | **`M24-02` の新設キー = `combo-list-filters-collapsed-v1`（localStorage・boolean 1 個）。** 実装は `useFilterPanelCollapsed.ts:17-38`——`createLocalStorageHelper` 経由・`-v1` 接尾辞・`useState` 初期化で `load()`・setter で `save()`。**本サブの新キーはこの形に倣った**（ただし §2.2 の理由で購読機構を足した） |
| **14** | **I** | **★★実測。§1.2 に別掲**（変更前の基準値） |

### 1.1 §3.3-3 `SM-096`「一定以上の長さで画面が崩れる」——**★★memo の見立てと機序が違った**

**実測条件**: リュウのコンボをステップ数 1 / 2 / 3 / 4 / 5 / 6 / 7 / 8 / 12 / 16 / 24 / 32 で作成（実文字数 5〜263 文字）。画面幅 1280 / 768 / 390 の 3 種。Chromium。

**(a) 一覧のレシピ列は幅 320px 固定で、4 ステップ・31 文字から切れ始める**

| ステップ数 | 1 | 2 | 3 | **4** | 5 | 6 | 7 | 32 |
|---|---|---|---|---|---|---|---|---|
| 実文字数 | 5 | 14 | 23 | **31** | 40 | 49 | 58 | 263 |
| セル幅 / 内容幅 (px) | 320/320 | 320/320 | 320/320 | **320/407** | 320/514 | 320/602 | 320/690 | 320/2957 |
| 見えている割合 | 100% | 100% | 100% | **79%** | 62% | 53% | 46% | **11%** |

**(b) ★★ページ全体は、どの幅・どの長さでも横へ溢れなかった**

| 画面幅 | 1280 | 768 | 390 |
|---|---|---|---|
| 一覧（省略表示）でページが横溢れ | **なし** | **なし** | **なし** |
| 詳細（263 文字）でページが横溢れ | **なし** | **なし** | **なし** |
| 展開行（`SetupTreeRow`・197 文字）でページが横溢れ | **なし** | **なし** | **なし** |
| 表が容器を超える量（表内スクロールで吸収） | 0px | 421px | 799px |

**★★機序の判定**：**レシピを 5 文字から 263 文字まで伸ばしても、表の幅は 1155px のまま変わらなかった。** `truncate`（`white-space:nowrap` + `overflow:hidden`）が効いて**列幅が動かない**ためである。表が容器を超える分は `overflow-x-auto` が**表内スクロール**として吸収しており、**ページ本体は 390px 幅でも横へ溢れない。**

**⇒ 崩れの原因はレシピ長ではなく「列数 × 各列の最小幅」である。全文表示モードでは直らない——というより、直すべき崩れが観測されなかった。**

**⇒ 指示書 §9.3 の「その症状は既に解消していた」に当たるものとして扱い、実装せず、成立を E2E ケース (6) で固定した**（3 幅 × 省略/全文 × 一覧/詳細）。**★これは欠陥ではない**（チェックリスト §0.3-3 / 5-9）。

> **★`SetupTreeRow` は truncate も max-width も持たない唯一の面**であり、静的な読みでは最有力の崩れ候補だった。**実測では溢れなかった**——flex の子が縮んで折返すためである（197 文字で `scrollWidth == clientWidth`、行高 65px）。**読みだけで手を選んでいたら、ここを直して「直った」と報告するところだった。**

### 1.2 §3.3-14 変更前の基準値（**★切り詰めずに採った**）

**実測条件**: 一覧 `/combos`・幅 1280 × 高さ 720・Chromium。

| 項目 | 実測値 |
|---|---|
| 1 行の高さ | **93px**（9 行すべて同一） |
| 表の上端 y | 385px（ヘッダ帯・フィルタ欄が上を占める） |
| ヘッダ行の高さ | 67px |
| ヘッダ下の可視領域 | 269px |
| **画面に収まる行数** | **2 行** |
| 幅 768 / 390 のときの 1 行の高さ | **213px**（＝ この幅では現状すでに 1 行しか入らない） |

**★この「2 行」が既定を選ぶ材料になった**（§4 の開発者裁定を参照）。

---

## 2. 実装したもの

### 2.1 レシピ表示の共通部品（§4.1・**本サブの核心**）

| 新設 | 内容 |
|---|---|
| `web/src/features/combo/recipeDisplay.ts` | 純粋関数。`splitRecipeSteps` / `firstMemoLine` ＋ 定数 3 本（`RECIPE_STEP_CONNECTOR` / `RECIPE_EMPTY_LABEL` / `MEMO_FIRST_LINE_MAX_LENGTH`） |
| `web/src/features/combo/components/RecipeText.tsx` | **4 面が通る唯一の描画部品**（＋ `MemoFirstLine`） |
| `web/src/features/combo/components/RecipeViewToggle.tsx` | **トグル。4 面に置くが、切り替える値は 1 つ** |
| `web/src/features/combo/hooks/useRecipeFullView.ts` | 状態の共有 |

**★ステップの区切りは `" > "`。出所は `internal/service/notation/resolver.go:11` の `connector` である**（Go 側は非公開定数のため import できない。値を変えるときは両側を同時に動かす旨をコメントに書いた）。

**★空表示の文言を単一の正典へ寄せた。** 実装側のリテラルは **9 → 6**（本サブが触った 3 面から消え、残る 6 はすべてセットプレイの面と定数定義そのもの）。数え直し: `grep -rn "（レシピなし）" web/src --include=*.tsx --include=*.ts | grep -v "\.test\." | wc -l` → **7**（うち 1 件は `recipeDisplay.ts` の定数定義、**残り 6 件がセットプレイの 5 面と `SetupRegistrationSection`**）。

**★★全文表示は `<span>` で組んだ（`<ol>/<li>` ではない）。** 理由は「4 面のうち 1 面が `<button>` の中だから」である——比較の追加モーダルは候補カード全体が `<button>`（`disabled` で選択済みを表す）であり、`button` の内容モデルは phrasing content のため `ol`/`li` を入れると不正な入れ子になる。**番号は自前で振っている。**

### 2.2 状態の共有（§4.1・`E-76`）

- 保持先: **localStorage 新キー `recipe-full-view-v1`**（`boolean | null`）。`web/CLAUDE.md` §1 台帳 **#11** へ登録済み。`bash scripts/check-browser-storage-keys.sh` 緑（§5）。
- **★★`useState` ではなく `useSyncExternalStore` + モジュールレベルの購読者集合にした。**
  **理由は「読み手が同時にマウントされるから」である**——**`/compare` では比較表とコンボ追加ダイアログが同時に画面へ出る。** 面ごとに state を持つと、比較表で切り替えてもダイアログ側は再マウントまで古い値を見る。**⇒ 「4 面が 1 つの値を共有している」を満たさない。**
  **★`M24-02` の #10（`useFilterPanelCollapsed`）は素の `useState` だが、あちらは読み手が 1 つしか無い。形を釣られて揃えていない**（台帳の脚注にも書いた）。
- **面ごとの既定はトグルとは別の仕組み**（`useRecipeFullView(surfaceDefault)`）。**トグルは 1 つしか無い。**
  一覧・比較・追加モーダル = `false`（**既定は現状のまま**）、詳細 = `true`。

### 2.3 比較画面の 2 件（§4.2）

- **`SM-061`**: 各コンボ列の見出しへキャラ名を出す。値は **`name_ja`**（`useCharacters()` の `nameJa`。`DES-005` §4.3 の並び順と同じ語彙。**第 2 の語彙を作っていない**）。取得経路は増やしていない（`CompareTable` は既に同フックを呼んでいた）。
- **`SM-003`**: 見出しセルの最下段へ「詳細を開く →」リンク。**★「削除」と取り違えられない位置にした**（`M23-07` の教訓）。
  **分離は 4 点**——**位置**（✕ は右上、リンクはセル最下段。あいだにキャラ名と始動状況の 2 行が入る） ／ **文言**（「詳細を開く」対 `aria-label` **「比較から外す」**） ／ **色**（青リンク対 灰・ホバーで赤） ／ **形**（テキストリンク対 アイコンボタン）。
  **★実査で分かったこと**: そもそも比較表の ✕ の `aria-label` は「削除」ではなく「**比較から外す**」であり、押しても比較リストから外れるだけでデータは消えない。**E2E で両者の縦の距離を機械で固定した**（8px 超）。

### 2.4 `SM-089` メモ 1 行目（§4.4）——**詳細と一覧の両方**

- 共通の小部品 `MemoFirstLine` で 1 か所に持つ。**メモが空・空白のみなら要素自体を出さない。**
- 1 行目の取り方＝最初の改行まで（CRLF / LF 両対応）。**長い場合は 40 文字で省略**（§9.2 の裁量。**`formatMemo` の 30 文字は一覧の「備考」列専用で用途が違うため流用していない**）。
- **★読み分け**: ✎ 記号 ＋ `data-testid="memo-first-line"` ＋ `aria-label="メモの1行目"`。レシピ側は `data-testid="recipe-text"`。**両者を並べたときに混ざらないことをテストで固定した。**

### 2.5 `SM-097` ノーゲージのラベル（§4.5）

`constants/oki.ts` の `okiOptionLabel` の中で **1 回だけ**付与（`const suffix = o.usesDr ? '・ドライブラッシュ' : '・ノーゲージ'`）。**呼び元ごとに足していない。** 日本語のまま（i18n 化は `M24-07` の手番）。

---

## 3. ★★推測で進めた箇所（§9.2 の裁量内・その旨を明示する）

| # | 事項 | 採った案 |
|---|---|---|
| 1 | トグルの見た目・置き場所 | 4 面共通のアイコン付きトグル。一覧＝操作ボタン列の先頭、詳細＝レシピ見出し行、比較＝ページ見出し行、モーダル＝ヘッダ直下 |
| 2 | `SM-003` の導線の形 | 見出しセル最下段の「詳細を開く →」テキストリンク（位置の安全性は裁量外として §2.3 の 4 点で分離した） |
| 3 | メモ 1 行目の省略の閾値 | **40 文字** |
| 4 | `web/e2e/support/` のヘルパ名 | `recipe-view.ts` ／ `setRecipeFullView(scope, on)` ほか |
| 5 | モーダルの器の幅 | `max-w-lg`（公称 512px / 実測 489px）→ `max-w-2xl`（公称 672px）。**★§4.1 の規則が第一手であり、幅はその補助である**（§4.3。規則の適用を省いてはいない） |
| 6 | 候補行の目印 | 比較の追加モーダルの `<li>` へ `data-testid="compare-candidate"` / `data-combo-id` を付与。**理由＝表示モードを切り替えると行の文字列が変わり、文字列では行を特定できなくなる**（E2E で実際に踏んだ） |

---

## 4. ★開発者裁定（Plan Mode・2026-08-26）

**§4.1 の「既定をどちらにするか」をモックで提示し、回答を得た。**

> **案 A（既定は末尾省略・指示書の記述どおり）を採用。**

**⇒ 一覧・比較・追加モーダルの既定は現状のまま（末尾省略＋ホバー）。詳細のみ全文表示を既定にした。** 判断材料として §1.2 の基準値（**現状すでに 1 画面 2 行**／全文にすると 1 行）を添えて提示した。

---

## 5. 自己テスト結果

### 5.1 テスト

| 対象 | コマンド | 結果 |
|---|---|---|
| Go（**着手前と同じであることの対照**） | `go test ./... -count=1` | **全パッケージ ok**（終了コード 0）。**本サブは Go を 1 バイトも触っていない**（§7-1） |
| フロント単体 | `cd web && pnpm test -- --run` | **191 ファイル / 1995 テスト すべて緑**（着手前は 187 ファイル / 1952 テスト。**+4 ファイル / +43 テスト**） |
| 型検査 | `cd web && pnpm exec tsc --noEmit` | **エラー 0** |
| E2E | `make e2e` | **187 passed / 0 failed**（3.5 分） |

**★`make e2e` 実行直前のポート確認**（§2.4・`D-548`。**毎回とった**）:

```
=== ★make e2e 実行直前のポート確認(D-548) ===
2026-08-26T17:20:35+00:00
47390 / 5273 ともに空き
```

### 5.2 ★★既存テストへの波及（**全数 grep の結果**・§5.1 末尾 / チェックリスト 5-7）

**`M24-02` の教訓（表示の DOM 種別を変えると assert が壊れる）が実際に効いた。** レシピ文字列とメモ文字列を assert している箇所を全数洗い出し、**8 ファイルを後状態へ更新した。**

| 種別 | 何が起きたか | 直したもの |
|---|---|---|
| **ラベルの変化**（`SM-097`） | `okiOptionLabel` にノーゲージが付き、**出力（エクスポート）にも効いた** | `export-model.test.ts:50-52` ／ `CompareTable.test.tsx:114-118`。**母集合（12 変種）は変えず、ラベル文字列だけを後状態へ**（§2.2 末尾が許す範囲） |
| **Router 文脈**（`SM-003`） | `CompareTable` が `<Link>` を持ったため Router が要る | `CompareTable.test.tsx` に `MemoryRouter` の shim を 1 本置いた（13 箇所の `render` へ複製しない） |
| **★★メモの二重表示**（`SM-089`） | **メモの 1 行目がレシピの上にも出るようになり、同じ文字列が 1 画面に 2 か所ある。** `getByText(memo)` が strict mode violation になった | **E2E 6 ファイル**を `{ exact: true }` へ（1 行目の表示は ✎ を伴うため exact では一致しない）。洗い出しコマンド: `grep -rn "getByText(memo" e2e/ \| grep -v exact` → 現在 **0 件** |

**★これは実装の欠陥ではなく、`SM-089` が意図した表示そのものである。** ただし「**同じメモが 2 か所に出る**」ことは**開発者の目に留まる可能性がある**ため、§6 に残課題として挙げた。

#### レシピ文字列側の全数 grep（**★M24-03 レビュー 中-4 で追記**）

```
$ grep -rn "（レシピなし）" web/src --include=*.tsx --include=*.ts | grep -v "\.test\." | wc -l
7      （着手前 9。うち 1 件は新設の定数定義 recipeDisplay.ts、残り 6 件はすべてセットプレイの面＝スコープ外）

$ grep -rn "defaultRecipe" web/e2e --include=*.spec.ts | wc -l
2      （m20-05-recipe-cache-wiring.spec.ts の API 応答の読み取り 2 件。★DOM を assert していない）

$ grep -rln "（レシピなし）" web/src --include=*.test.tsx --include=*.test.ts
web/src/features/combo/components/SetupTreeRow.test.tsx      ← スコープ外の面(セットプレイ)
web/src/features/combo/components/RecipeText.test.tsx        ← 本サブが新設

$ grep -rn "compare.row.route" web/src --include=*.tsx --include=*.ts | grep -v "\.test\." | wc -l
1      （CompareTable.tsx の行ラベル定義のみ）
```

**⇒ 本サブが描画方法を変えた 4 面について、レシピ文字列を DOM で assert していた既存箇所は 0 件だった。** 残るのはスコープ外のセットプレイの面のみである。

### 5.3 ★★破壊確認 4 件（**指示書 §5.3**）

**各件で (a) 壊す対象が実在するか (b) その壊し方で本当に観測が変わるか を先に確かめた**（計測点 `M-78` ／ playbook §4.32）。**復旧は `cp` で行った**（Git の `checkout` / `restore` は `.claude/settings.json` の deny 対象のため）。**4 件とも復旧後の差分 0 を確認済み。**

| # | 壊したもの | (a) 対象の実在 | (b) 観測の変化 | **赤くなった経路** |
|---|---|---|---|---|
| **1** | 全文表示モードのトグルを「常に既定」へ固定（`useRecipeFullView.ts:70` の `stored ?? surfaceDefault` → `surfaceDefault`） | **実在**（`:70`） | **変わった** | **単体 5 ＋ E2E 3 ＝ 8 経路**。単体＝`useRecipeFullView.test.tsx` の共有テスト 5 本。E2E＝**(1) / (5) / (6)** |
| **2** | 4 面が見る値を面ごとに分ける（購読機構をやめ、面ごとの `useState` へ） | **実在**（`useSyncExternalStore` + `listeners`） | **変わった** | **単体 4 ＋ E2E 2 ＝ 6 経路**。単体＝共有テスト 4 本（**「ダイアログの中で切り替えても他の 3 面へ伝わる」を含む**）。E2E＝**(1) / (5)** |
| **3** | メモ 1 行目の取り出しを「全文」へ変える（`recipeDisplay.ts:55`） | **実在**（`:55`） | **変わった** | **単体 8 ＋ E2E 1 ＝ 9 経路**。単体＝`firstMemoLine` 6 本 ＋ `MemoFirstLine` 2 本。E2E＝**(2)** |
| **4** | ノーゲージのラベル付与を外す（`oki.ts:75`） | **実在**（`:75`） | **変わった** | **単体 5 経路 / 3 ファイル**——`constants/oki.test.ts`（純粋関数 3 本）＋ **`CompareTable.test.tsx`（表示）** ＋ **`export-model.test.ts`（出力）** |

**★★指示書の予測と実際が食い違った点（1 件・隠さず記す）**

**破壊確認 1 について、指示書 §5.3 は「E2E (1) と (2) が赤くなる」と予測していた。実際には (2) は緑のままだった。**
理由：**(2) が見ているのは「詳細の既定が全文であること」であり、トグルを既定へ固定しても詳細は全文のままだから**である。**⇒ 壊し方が (2) の観測を動かさない。**
**代わりに (5)（追加モーダルでモードが効く）と (6)（全文表示にしても横溢れしない）が赤くなった**ため、**破壊確認 1 の趣旨（トグルが効かなくなったら気づける）は 8 経路で成立している。**

**★全件が緑のままだった破壊確認は 1 件も無い**（`M24-02` の破壊確認 3 の型＝「値を読むだけ」の壊し方、には当たらなかった）。

#### 実施コマンドと出力（**★M24-03 レビュー 高-3 で追記**）

**共通の手順**: `cp` で対象ファイルを退避 → 壊す → 下記コマンドを実行 → `cp` で復旧 → `git diff --stat` が空であることを確認。

**破壊確認 1**（`useRecipeFullView.ts:70` の `stored ?? surfaceDefault` → `surfaceDefault`）

```
$ cd web && pnpm test -- --run
 Test Files  1 failed | 190 passed (191)
      Tests  5 failed | 1990 passed (1995)

   × 全文表示モードは 4 面で 1 つの値を共有する > ★★トグルを 1 回押すと 4 面すべてが全文表示になる
   × 全文表示モードは 4 面で 1 つの値を共有する > ★★もう一度押すと 4 面すべてが省略表示へ戻る
   × 全文表示モードは 4 面で 1 つの値を共有する > ★一度切り替えたら、面ごとの既定より共有された値が優先する(トグルは 1 つ)
   × 全文表示モードは 4 面で 1 つの値を共有する > ★★比較の追加ダイアログの中で切り替えても、他の 3 面へ同時に伝わる
   × 全文表示モードは 4 面で 1 つの値を共有する > ★保存済みの値は次回の描画で 4 面すべてに効く(面ごとの既定を上書きする)

$ pnpm e2e e2e/m24-03-recipe-readability.spec.ts
  ✘  (1) 一覧でモードを切り替えると、省略されていたレシピが全部読める(SM-130)
  ✓  (2) 詳細でレシピがステップごとに縦に読める(SM-022)        ← ★予測と違い緑のまま
  ✓  (3)(4) 比較にキャラ名が出て、そこから詳細へ行ける
  ✘  (5) 比較の追加モーダルでも全文表示モードが効く(SM-018)
  ✘  (6) ★長いレシピでも画面は横へ溢れない(SM-096 の成立を固定する)
```

**破壊確認 2**（購読機構をやめ、面ごとの `useState` へ）

```
$ cd web && pnpm test -- --run
      Tests  4 failed | 1991 passed (1995)

   × ★★トグルを 1 回押すと 4 面すべてが全文表示になる
   × ★★もう一度押すと 4 面すべてが省略表示へ戻る
   × ★一度切り替えたら、面ごとの既定より共有された値が優先する(トグルは 1 つ)
   × ★★比較の追加ダイアログの中で切り替えても、他の 3 面へ同時に伝わる

$ pnpm e2e e2e/m24-03-recipe-readability.spec.ts
  ✘  (1) ／ ✘  (5)   ✓  (2) (3)(4) (6)
```

**破壊確認 3**（`recipeDisplay.ts:55` の 1 行目取り出し・省略をやめ、メモ全文を返す）

```
$ cd web && pnpm test -- --run
      Tests  8 failed | 1987 passed (1995)

   × MemoFirstLine > メモの 1 行目を出す
   × MemoFirstLine > ★空白だけのメモでも要素を出さない
   × firstMemoLine > 空白だけのメモも空文字を返す(空の行を作らない)
   × firstMemoLine > 改行があるときは 1 行目だけを返す(LF)
   × firstMemoLine > CRLF でも 1 行目だけを返す
   × firstMemoLine > 長文は既定の閾値で切って … を付ける
   × firstMemoLine > 閾値は呼び手から変えられる
   × firstMemoLine > ★1 行目が長いときも 1 行目だけを見る(2 行目を混ぜない)

$ pnpm e2e e2e/m24-03-recipe-readability.spec.ts e2e/combo-crud.spec.ts
  ✘  (2) 詳細でレシピがステップごとに縦に読める(SM-022)
      Error: expect(locator).not.toContainText(expected) failed
  1 failed / 6 passed
```

**破壊確認 4**（`oki.ts:75` のノーゲージ付与を外す）

```
$ cd web && pnpm test -- --run
 Test Files  3 failed | 188 passed (191)
      Tests  5 failed | 1990 passed (1995)

   × CompareTable > 各行のラベルが表示される                                    ← 表示面
   × buildComboFields 表示項目選択 > oki を選ぶと 12 変種の個別行へ展開される     ← 出力面
   × okiOptionLabel のゲージ区分ラベル > ノーゲージ版に「・ノーゲージ」が付く      ← 純粋関数
   × okiOptionLabel のゲージ区分ラベル > ★12 変種すべてがゲージ区分を名乗る
   × okiOptionLabel のゲージ区分ラベル > 未知のコード値はそのまま出す(フォールバックは不変)

 FAIL  src/constants/oki.test.ts
 FAIL  src/features/combo-io/export-model.test.ts
 FAIL  src/features/combo/components/CompareTable.test.tsx
```

**★取り込み時に追加した破壊確認 5**（`ComboEditorBasicFields.tsx` を旧のインライン組み立てへ戻す）

レビュー 高-2 の是正を守る網が実在することを確かめた。

```
$ cd web && pnpm test -- --run src/features/combo/components/ComboEditorBasicFields.test.tsx
      Tests  2 failed | 35 passed (37)

   × ★起き攻めのラベルは constants/oki.ts の正典(okiTechAndGaugeLabel)を通る
   × ★ノーゲージ版も入力面でゲージ区分を名乗る(読む面と表記が割れない)
```

---

## 6. ★併せて更新が要るもの

| 項目 | 状態 |
|---|---|
| **消費した CHANGE 番号の登録** | **★該当なし。** `CHANGE-134` は**設計卓が起票済み**であり、**製造は番号を消費していない**（指示書ヘッダ）。`change-number-registry.md` §1 への追記は不要 |
| **その番号の写し先（registry §1 ／ 契約 §4 ／ ボード §2.1 ／ §2.4）** | **★該当なし**（上記のとおり番号を払い出していない） |
| **消費したマイグレ連番** | **★該当なし。0 本。** 実査: `ls migrations/` の最新は `000079_fix_character_display_names`。**ボード §2.2 の「次に払い出す番号 `000080`」は動かない** |
| **版を上げた文書の参照元** | **★該当なし。** 設計書・指示書の版は上げていない（設計書本体は製造が編集しない＝`CLAUDE.md` §8） |
| **ブラウザストレージ台帳** | **★更新した。** `web/CLAUDE.md` §1 へ **#11 `recipe-full-view-v1`** を追記（脚注 4 本つき）。`CHANGE-134` の射程内（§7-7） |
| **`docs/progress/progress-log.md`** | **★追記した**（§8） |
| **`docs/handover/design-reports/`** | **★作成した**（`/design_handover_report` 相当。§8） |

---

## 7. §7.4.1 CHANGE 要否の判定（**8 条件を 1 件ずつ**）

**★判定した結果「射程は広がらなかった」であっても、判定したこと自体を書く**（§9.4）。

| # | 条件 | 判定 | 根拠 |
|---|---|---|---|
| **1** | `DES-002` §4.2 の経路表に載る API を新設・変更したか | **該当なし** | **`internal/` / `cmd/` / `migrations/` の差分は 0 バイト。** `git diff --stat 62b0240 -- internal/ cmd/ migrations/` の出力が空。**§2.3 の例外条項は発動していない**（分岐 A で着地） |
| **2** | `DES-003` のスキーマに触れたか | **該当なし** | マイグレーション 0 本。`migrations/` の差分 0 |
| **3** | `DES-004` のレシピ文字列の生成規則に触れたか | **該当なし** | `internal/service/notation/` の差分 0（契約 F-1）。**フロントは組み立て済み文字列を `" > "` で分解して描くだけ**であり、`recipe_cache` の中身は変えていない |
| **4** | `DES-006` の VAL の判定結果が変わったか | **該当なし** | バリデーションに触れていない。VAL 関連テストは全緑 |
| **5** | `SUPP-001` §5.2 のパッケージ構成に増減があるか | **該当なし** | Go 側のパッケージは増減なし。フロントの新設は既存ディレクトリ（`features/combo/` 配下）内 |
| **6** | `SUPP-001` §5.5 のテスト方針が変わるか | **該当なし** | 既存の方針どおり（純粋関数＝必須、コンポーネント＝主要ロジック持ちのみ、E2E＝主要フロー） |
| **7** | **ブラウザストレージのキーを新設したか** | **★★該当する** | **`recipe-full-view-v1` を新設**。`web/CLAUDE.md` §1 台帳 #11 へ登録済み。**`DES-005` §5.4 への記載は設計卓の手番**（設計書本体は製造が編集しない）。**`CHANGE-134` の射程内である** |
| **8** | マイグレーションを消費したか | **該当なし** | **0 本。** ボード §2.2 の連番セルの更新は不要 |

### 7.1 ★★上記 8 条件に無いが、設計卓へ渡すべきこと（**射程の広がり 1 件**）

**`SM-097`（ノーゲージのラベル）が `DES-005` §5.6 項目6 の「ラベル生成規則」へ及ぶ。**

- `DES-005` §5.6 項目6 は生成規則を**逐語で**書いている——`"投げ重ね(その場受け身)"` / `"シミー(後ろ受け身・ドライブラッシュ)"`。
- **as-built は `"投げ重ね(その場受け身・ノーゲージ)"` に変わった。**
- **★さらに、呼び元 3 件のうち `export-model.ts:185` は出力（エクスポート）面である。⇒ `DES-005` §5.13 のエクスポート出力にも「ノーゲージ」が載る。**
- **⇒ `CHANGE-134` の射程へ `DES-005` §5.6（および §5.13 のラベル参照）を加える判断を設計卓へ仰ぐ。**

---

## 8. 品質チェック

| 検査 | 結果 |
|---|---|
| `bash scripts/check-artifact-integrity.sh`（**★1 本目に回した**） | **違反なし**。検査 11 件の自己検査が通り、派生資料 4 本の生成物も健全 |
| `bash scripts/check-browser-storage-keys.sh`（**新キーを作ったため必須**） | **違反なし**（台帳 11 件 / 本番コード 10 件・一致） |
| `bash scripts/check-md-emphasis.sh` | **違反なし**（436 行 / ベースライン 436 行・増加なし） |
| `bash scripts/check-doc-refs.sh` | **dead reference なし** |
| `bash scripts/check-enum-sync.sh` | **ベースラインどおり（増加なし）** |
| `bash scripts/check-progress-log-index.sh` | **違反なし**。**★偽の緑を返しうる**（followup §AH）ため、**索引行を目で確かめた**——`progress-log.md` 末尾に `### M24-03:` の節が実在することを `tail` で確認済み |
| `bash scripts/check-doc-inventory.sh`（情報提供） | 型に無いファイル **3 件**。**いずれも `IMPROVE-01` の既存ファイル**（`improve-01-completion-report.md` / `improve-01-review.md` / `IMPROVE-01-clean-clone-toolchain.md`）であり、**本サブが増やした新種は 0 件**。`docs/progress/M24-03-mock/` は `M24-01-mock` / `M24-02-mock` と同じ型 |

---

## 9. ★E-225（新規ファイルの上書き防止）の確認

**(a) 作る前に確かめた。** 新規 12 ファイルすべてについて、Write の前に `ls` / `find` で同名の不在を確認した。

**(b) 変更統計を「新規のつもりのファイルが `+` だけか」の観点で読んだ。**

```
$ git diff --numstat --diff-filter=A 62b0240
docs/progress/M24-03-mock/m24-03-screens.html                +409 -0
web/e2e/m24-03-recipe-readability.spec.ts                    +208 -0
web/e2e/support/recipe-view.ts                               +121 -0
web/src/constants/oki.test.ts                                 +56 -0
web/src/features/combo/components/RecipeText.test.tsx        +120 -0
web/src/features/combo/components/RecipeText.tsx             +124 -0
web/src/features/combo/components/RecipeViewToggle.tsx        +45 -0
web/src/features/combo/hooks/useRecipeFullView.test.tsx      +272 -0
web/src/features/combo/hooks/useRecipeFullView.ts             +80 -0
web/src/features/combo/recipeDisplay.test.ts                  +99 -0
web/src/features/combo/recipeDisplay.ts                       +59 -0
```

**★新規ファイル 11 件すべて deletions が 0 である**（`docs/progress/M24-03-completion-report.md` は本ファイルのため上記に含まれない）。**上書きで消したファイルは無い。**

全体の変更統計:

```
$ git diff --stat 62b0240
 29 files changed, 1818 insertions(+), 55 deletions(-)
```

**55 deletions の内訳**はすべて既存ファイルの置換（4 面のレシピ描画の差し替え・既存テストの assert 更新・locale の末尾カンマ）であり、**ファイルごと消えたものは無い。**

---

## 10. 残課題（**設計卓・後続サブへ**）

| # | 事項 | 宛先 |
|---|---|---|
| **1** | **★★セットプレイのレシピ表示は本サブの共通部品を通っていない**（§1 実査 6）。5 か所で個別描画のままであり、**空表示の文言リテラルもそこに残っている**（6 件）。**指示書 §1.3-6 でスコープ外と明示されているため触っていない。** 寄せるなら別サブ | 設計卓 |
| **2** | **★`SM-089` により、詳細と一覧でメモが 1 画面に 2 か所出る**（レシピの上の 1 行目 ＋ 既存の「備考」欄／列）。**要求どおりの実装だが、二重表示そのものが気になる可能性がある。** 既存の備考欄を畳むかは開発者の判断 | 開発者 |
| **3** | **★`SM-097` が `DES-005` §5.6 のラベル生成規則と §5.13 の出力に及ぶ**（§7.1）。`CHANGE-134` の射程へ加えるか | 設計卓 |
| **4** | `ComboDetailRecipe` の `PresetSwitcher` が「uncontrolled → controlled」の React 警告を出す（`selectedPresetId` が `undefined` から始まるため）。**本サブの変更ではなく既存の挙動**だが、テストで面を描くようになって表面化した | 後続（`M24-08` 相当） |

---

## 11. ★レビュー取り込みで変わったこと（**Phase C・自動トリアージ**）

**レビュー報告書**: `docs/progress/m24-03-review.md`（**重大 0 件 / 高 3 件 / 中 6 件 / 低 7 件**）。
**採否**: **高 3/3 採用 ／ 中 6/6 採用 ／ 低 7/7 採用（不採用 0 件）。**
**★「高」指摘の不採用は 0 件であるため、開発者エスカレーション（Phase C 安全弁）は発火していない。** 往復は 1 回。

### 11.1 実装が変わったもの

| 指摘 | 何を直したか |
|---|---|
| **高-1** | **撤回済みの記述がコードとモックに残っていた。** `CompareTable.tsx` のコメントが ✕ の `aria-label` を「削除」と書いていたが、実装は **「比較から外す」**（`t("compare.removeCombo")`）である。**完了報告と設計伝達レポートは自ら是正済みで、コード側とモックだけが旧のままだった。** 両方を as-built へ揃えた |
| **★★高-2** | **★実査 §3.3-12 の答えが不完全で、入力面と読む面でラベルの語彙が割れていた。** `ComboEditorBasicFields.tsx:501-502` が `okiOptionLabel` を経由せず `spec.usesDr ? "・ドライブラッシュ" : ""` をインラインで組み立てており、`SM-097` の「ノーゲージ」が**エディタにだけ付かなかった**。**⇒ `constants/oki.ts` へ `okiTechAndGaugeLabel()` を切り出し、`okiOptionLabel` とエディタの両方がそこを通るようにした。** エディタが攻撃種別を見出しへ出すため `okiOptionLabel` をそのまま呼べない、という事情も同関数のコメントに残した |
| **中-1** | **`/mycombo` が `ComboTable` / `ComboTableRow` を一覧と共有する 5 面目だった。** レシピもメモ 1 行目も一覧と同じ規則で描かれるのに、**切り替えの口だけが無かった。** `MyComboPage` へ `RecipeViewToggle` を 1 つ置いた（**値は 4 面と同じ 1 つを共有する。トグルが増えるだけで値は増えない**） |
| **中-2** | `recipeDisplay.ts` のコメントの件数「8 本」を実測どおり **9 本**へ |
| **中-3** | **未使用の i18n キー `compare.characterLabel` を削除した**（ja / en）。使う予定が無いまま先置きすると `M24-07` が探す手間になる。**⇒ 本サブの新設キーは 4 本で確定** |
| **中-5** | `MemoFirstLine` へ **`role="note"`** を付けた。`aria-label` はロールを持つ要素にしか確実に効かず、ロール無しの `div` では読み上げられない実装があり得るため |
| **中-6** | **E2E (1) へ一覧側のメモ 1 行目の assert を足した。** 詳細側はケース (2) が見ていたが、**一覧側を見ているものが無かった**（次のサブが一覧の行を触って落としても気づけない） |
| **低-1** | `ComparePage.tsx` の字下げ |
| **低-2** | `MemoFirstLine` の `title` を**切り詰める前**の 1 行目に。切り詰め後を入れるとホバーしても続きが読めない |
| **低-3** | 空レシピの分岐にも `data-recipe-view="empty"` を付けた |
| **低-4** | キャラ名が空（一覧の取得前）なら要素を描かない |
| **低-5** | `firstMemoLine` の切り詰めを **コードポイント単位**へ（`Array.from`）。サロゲートペアを途中で割らない |

### 11.2 報告が変わったもの

| 指摘 | 何を直したか |
|---|---|
| **高-3** | **破壊確認 4 件に実施コマンドと失敗出力が無かった**（指示書 §7.2 / チェックリスト 6-1）。§5.3 へ貼った。**あわせて取り込みで足した破壊確認 5（高-2 の是正を守る網の実在確認）も載せた** |
| **中-4** | **レシピ文字列側の全数 grep のコマンドと件数が無かった**（メモ側だけ載っていた）。§5.2 へ追記。**4 面のレシピ文字列を DOM で assert していた既存箇所は 0 件**である |
| **低-6** | モーダルの器の幅の数値が実測値（489px）と公称値（512px）で 2 通り出ていた。**区別して記す形へ統一** |
| **低-7** | `progress-log.md` の索引行へレビュー結果欄（重大 0・高 3・中 6・低 7 / 採否 / 往復 1 回）を追記 |
| **実査 12** | **高-2 に伴い §1 の実査 12 の答えを是正**（呼び元 3 件 → 組み立て箇所 4 件）。**数え直しのコマンドも「関数の呼び元」から「組み立ての材料」を引く形へ変えた**——**★関数の呼び元だけを数えると、関数を経由しない組み立てが見えない** |

### 11.3 ★取り込み後の再検証

| 対象 | 結果 |
|---|---|
| `cd web && pnpm exec tsc --noEmit` | **エラー 0** |
| `cd web && pnpm test -- --run` | **191 files / 1997 tests 全緑**（取り込み前 1995。**高-2 の再発防止テスト 2 本を追加**） |
| `make e2e`（ポート確認済み: `18:06:53` / 47390・5273 空き） | **187 passed / 0 failed** |
| 破壊確認 5（高-2 の網の実在確認） | **2 件が赤**（§5.3 に出力を掲載） |
| 各 `check-*.sh` | §8 を参照（**取り込み後に再実行して全件緑を確認**） |

---

## 12. ★開発者の手動確認（**★2026-08-26 実施済み**）

### 12.1 要否の判定

**★必要である。** `M24-overview` §4.4 が **`M24-01`〜`M24-06` を「★必要（各 1 回・短い）」** と定めている。逐語——**「本 MS の中身は『使いにくい』への対処であり、直ったかどうかは体験でしか判定できない」**（種別 **(a)** ＝人間の感覚でしか判定できない。`M22-overview` §4.4.1 / `D-398`）。

**★本サブ固有の事情が 2 つある。**

| # | 事情 |
|---|---|
| **1** | **★★モック承認後に面が 2 つ増えた。** 開発者が承認したモックは **4 面**（一覧・詳細・比較・追加モーダル）だが、**レビュー取り込みで `/mycombo`（中-1）と新規登録／編集画面（高-2）が加わった。** 実査: モック HTML に `マイコンボ` / `エディタ` は**各 0 ヒット**。**⇒ この 2 面は開発者が一度も見ていない** |
| **2** | **`M24-overview` §1.5 が「モックが承認されても実装が正しいことの根拠にはならない」と明記している。** モックは「形の妥当性を判断するための材料」であり、実装の証拠ではない |

**★`M24-02` の手動確認は援用できない**（`D-429` の 3 条件）。同じ `/combos` 画面だが——

- **条件 1（同一ビルドで確認されている）が破れる**: 本サブが `ComboTableRow.tsx` を書き換えた。
- **条件 3（その確認がいま主張したい命題を実際に検証している）が破れる**: `M24-02` の命題はフィルタ・タグ・キャラ選択であり、**レシピの見せ方を見ていない。**

### 12.2 確認手順（**通し 10〜15 分**）

**起動**: 端末 1 で `make run-server`（`http://localhost:47318`） ／ 端末 2 で `make run-web`（`http://localhost:5173`）。

**下ごしらえ（1 回だけ）**——**4 ステップ以上（約 31 文字以上＝省略が始まる長さ）のレシピ**を持ち、**2 行以上のメモ**が入ったコンボが 1 本要る。無ければ新規登録で作る。

> **★表示モードは localStorage `recipe-full-view-v1` に残る。** 既定へ戻すには DevTools → Application → Local Storage で当該キーを削除するか、トグルをもう一度押す。

| STEP | 面 | 操作 | 見るところ・判定してほしいこと |
|---|---|---|---|
| **1-1** | **一覧 `/combos`** | トグルを押す前 | レシピが「…」で切れ、ホバーで全文が出る。**これが既定でよいか** |
| **1-2** | 同上 | **`▤ レシピ全文表示` を押す** | ステップごとに `1. 2. 3.` と縦へ展開。**★行が高くなった一覧を実際に見て、「やはり既定は省略でよい」か「既定を全文にしたい」か。** **★モックでの案 A 採用は実測値だけ（1 画面 2 行 → 1 行）を材料にした判断であり、実物を見ていない。既定の変更は 1 行である** |
| **1-3** | 同上 | レシピ列の上の `✎` 付きの薄い行 | メモの **1 行目だけ**が出ている（2 行目は出ない）。**「薄く」の度合いは適切か** |
| **1-4** | 同上 | 同じ行の「備考」列 | **★同じメモが 1 画面に 2 か所出る**（レシピの上 ＋ 備考列）。**この二重表示を許容するか、備考列を畳むか**（§10-2 の残課題） |
| **2** | **★マイコンボ `/mycombo`**（**モックに無かった面**） | 開く | トグルが増えている（置き場所が浮いていないか） ／ レシピとメモ 1 行目が一覧と**同じ**見え方か ／ **STEP 1 で全文にしたまま来ると、こちらも全文になっている**（値は共有）——挙動として自然か |
| **3** | **詳細 `/combos/:id`** | 長いコンボの詳細を開く | **詳細は全文表示が既定**（縦並び）。読みやすいか ／ レシピ見出し行のトグルがプリセット切替の隣で邪魔でないか ／ `✎` 行が下の「メモ」欄と二重に出る（1-4 と同じ論点） |
| **4** | **比較 `/compare`** | **キャラ違いを含む 2 件**を選んで比較 | 見出しセルが `コンボ #N` ＋ `✕`（右上）→ **キャラ名** → 始動状況 → **「詳細を開く →」**（最下段）の順になっているか ／ **★`✕` と「詳細を開く →」を取り違えて押しそうにないか**（**E2E は縦 8px 超を固定しているが、「取り違えにくいか」は判定していない**） ／ 全文表示でツールチップが消えるか |
| **5** | **比較の追加モーダル** | 「コンボを追加」を押す | **器を `max-w-lg` → `max-w-2xl` へ広げた**（公称 512px → 672px）。広すぎ／狭すぎないか ／ **全文表示にしたとき候補が縦に伸びすぎて選びにくくなっていないか**（`SM-018` の元の不満は「短くしか出ない」だった） |
| **★6** | **★★新規登録／編集の「起き攻め」節**（**モックに無かった面・最重要**） | 任意のコンボの「編集」→ 起き攻め節 | **12 個中 6 個のラベルが「・ノーゲージ」ぶん長くなった**（「その場受け身」→「**その場受け身・ノーゲージ**」）。**2 列グリッドに収まるか／折り返して窮屈でないか。** **★★あわせて採否そのものの回答が要る**——§12.3 |
| **7** | **エクスポート出力** | 1 件選んで CSV かクリップボード（PDF も可） | 起き攻め 12 行の項目名が長くなった。**過密でないか。** **★PDF のページ数は `m17-05c-pdf-pagination.spec.ts`（「単独コンボは 1 ページのまま」）が固定済みであり、見るのは体裁だけでよい** |

### 12.2.1 ★★実施結果（**2026-08-26・開発者**）

| 面 | 結果 |
|---|---|
| **一覧・マイコンボ** | **修正依頼 2 件**——**逐語＝「失念していたが備考欄に同じくメモが出ている。こちらの列は消してもらえますか？」** ／ **「ついでにセットプレイ数も既定 OFF にしてもらえますか？」**。**⇒ どちらも「既定 OFF」で決着**（列そのものは残す） |
| **比較** | **逐語＝「こちらで問題ない。」** ただし違和感 1 件——**逐語＝「（比較画面）詳細を開く⇒（詳細画面）戻る⇒（一覧画面）という遷移になっているのは違和感があった。対応難しければ申し送りでもいい。」** ⇒ **申し送り**（§13.2） |
| **追加モーダル** | **逐語＝「こちらで問題ない。」** |
| **新規登録／編集の起き攻め節** | **「これは何を変えましたか？」→ 回答のうえ 案 A で決着**（§12.3） |
| **エクスポート出力** | **「これは何を変えましたか？」→ 回答**（実装は 1 バイトも触っておらず、`okiOptionLabel` 1 か所からの波及。§12.3.1） |

### 12.3 ★★STEP 6 の決着（**案 A**）

**論点**: **指示書 §1.3-5 は新規登録／編集画面をスコープ外と宣言していた。** レビュー高-2 で「`okiOptionLabel` を経由しないインライン組み立てが 1 件あり、入力面と読む面で表記が割れている」ことが判明したため、**取り込みで正典（`okiTechAndGaugeLabel`）へ寄せた。**

| 案 | 内容 | 帰結 |
|---|---|---|
| **A（現状）** | **編集画面も正典へ寄せたまま** | 入力面・読む面・出力の表記が揃う。ただし**スコープ外と宣言された面を変えている** |
| **B** | **編集画面は元の短いラベルへ戻す** | 編集画面は攻撃種別でグループ化しているため、ゲージ区分を省いても文脈で読める。**採る場合は「意図的な差分」として `constants/oki.ts` へ明記する**（`constants/oki.ts` が自ら「表記揺れを防ぐ単一の正典」と宣言しているため、無記録の差分にはしない） |

**⇒ ★★2026-08-26 決着＝案 A（このまま正典へ寄せたまま）。**

**★根拠は `Memo_Someday.txt` の原文である**（`SM-097` line 227・逐語は設計伝達レポート §2-3.1）。**要望本体は 1 文で面の指定が無く、不満の実体は「画面に「ノーゲージ」は出ない」である。編集画面も「画面」であるため案 A は原文の趣旨に沿う。**

**★★あわせて分かったこと**——**memo の 2026-08-11 注記自体が「実際にラベルを組む」のは `okiOptionLabel` だと断定していた（誤り）。** 編集画面はインラインで組んでおり、**memo → ledger → 指示書 §3.3-12 → 初回実査の全員が同じ盲点を共有していた。** レビュー 高-2 が唯一それを拾った。

**★ledger の要約列は `／【2026-` で切れていた**（`SM-018` = `M-82` と同型）。**今回は要旨が無傷で実害は無かった**が、**`M-82` の教訓「要約列から確認事項を立てない。原文へ当たる」が再び有効だった。**

### 12.3.1 ★エクスポート出力について（開発者質問への回答）

**★エクスポートの実装コードは 1 バイトも触っていない。** `web/src/features/combo-io/` の差分は `export-model.test.ts`（期待値）のみである（`git diff --stat 62b0240 -- web/src/features/combo-io/` → 1 file / +4 -2）。

変えたのは `constants/oki.ts` の `okiOptionLabel` **1 か所**で、`export-model.ts:185` がそれを呼んでいるため**自動で波及した**。

| 項目 | 変化 |
|---|---|
| 起き攻めの項目名 | **12 行中 6 行**が「投げ重ね(その場受け身)」→「投げ重ね(その場受け身・**ノーゲージ**)」 |
| 値（✓/✗）・行数・列構成・出力形式 | **不変** |
| 影響する出力 | **CSV ／ クリップボード ／ PDF のすべて**（いずれも `buildComboFields` を通る） |

### 12.4 ★確認が不要なもの（**無駄足を防ぐため明示**）

**以下は機械で固定済みであり、手で確かめる必要はない。**

| 事項 | 固定している機構 |
|---|---|
| **4 面（＋`/mycombo`）が同じ値を見ていること** | `useRecipeFullView.test.tsx`（**本物の 4 コンポーネントを同一ツリーへ並べている**）。**破壊確認 2 で赤になることも確認済み** |
| **長いレシピで画面が横に溢れないこと** | E2E (6)。1280 / 768 / 390 × 省略/全文 × 一覧/詳細 |
| **全文表示でツールチップが消える／ステップが全部出る** | E2E (1) ／ `RecipeText.test.tsx` |
| **12 変種すべてがゲージ区分を名乗る** | `constants/oki.test.ts` |
| **編集画面のラベルが正典を通っている** | 取り込みで足した再発防止テスト 2 本（**旧のインライン組み立てへ戻すと赤**＝§5.3 の破壊確認 5） |
| **`internal/` 差分 0・マイグレ 0 本** | `git diff --stat 62b0240 -- internal/ cmd/ migrations/` が空 |

---

## 13. ★手動確認の取り込み（**2026-08-26**）

### 13.1 実施したもの — 一覧・マイコンボの列 2 件を既定 OFF

**`web/src/constants/combo-list.ts` の `DEFAULT_COLUMN_VISIBILITY` の 2 行のみ。**

| 列 | 変更 | 理由 |
|---|---|---|
| **備考**（`memo`） | `true` → **`false`** | `SM-089` でメモの 1 行目がレシピ列の直上へ出るようになり、**同じ文字列が 1 画面に 2 か所並んだ**（開発者逐語＝「備考欄に同じくメモが出ている。こちらの列は消してもらえますか？」） |
| **セットプレイ数**（`setupCount`） | `true` → **`false`** | 一覧の情報量を絞るため（開発者逐語＝「ついでにセットプレイ数も既定 OFF にしてもらえますか？」） |

**★★「列を消した」のではない。** `ColumnVisibility` の型（7 フィールド）・`COLUMN_DEFINITIONS`・ブラウザストレージキー `combo-list-columns-v1` は**一切変えていない**。**⇒ 表示列カスタマイズから従来どおり戻せる。保存済み設定との互換も崩れず、キーのバージョン上げ（`-v2`）も不要。**

> **★保存済みの設定を持つ環境では、その保存値が優先される**（既定は初回のみ効く）。**一度でも列を触った環境では、備考列が出たままになる。** これは `useColumnVisibility` の既存のマージ仕様どおりである。

### 13.2 申し送り（実施しないもの）

**比較→詳細→「戻る」→一覧 の遷移**（開発者逐語＝「対応難しければ申し送りでもいい」）。

- **★原因は既存実装である。** `ComboDetailPage.tsx:97` の「戻る」は `<Link to="/combos">` のハードコードで、**本サブでは触っていない**（同ファイルの差分は memo prop を渡す 4 行のみ）。
- **★`SM-003` の「詳細を開く」導線が、既存のこの挙動を初めて露出させた。**
- 直すなら `navigate(-1)` 系（先例＝`ComboEditor.tsx:781` / `SetupEditorPage.tsx` の 3 箇所）だが、**一覧→詳細・直接 URL・マイコンボ→詳細のすべての経路に及ぶ**ため別サブが妥当。
- **記録先＝設計伝達レポート §4-8。** **★`followup-backlog.md` §J へは書いていない**——**§J は「上限に達して止めた項目」の受け皿**であり、本件は**開発者判断による意図的な先送り**で停止記録ではないため。**`D-382` の正規の口（設計伝達レポート §4 → 設計卓が畳む）へ回した。**

### 13.3 既存テストへの波及（**実査済み・5 箇所**）

**★「既定が `true` であること」に乗っていたテストだけが落ちた。母集合は変えていない。**

| # | 対象 | 直し方 |
|---|---|---|
| **1** | `ComboTable.test.tsx` の「セットプレイ数の列」describe | **`{ ...DEFAULT_COLUMN_VISIBILITY, setupCount: true }` を明示して渡す。** 命題は「列が機能するか」であって「既定で出るか」ではない |
| **2** | **★`useColumnVisibility.test.ts`「merges old structure with defaults」**（**計画時に予測できていなかった 1 件**） | **`expect(visibility.memo).toBe(true)` と値をベタ書きしていた。** 命題は「保存に無かったキーは既定へ落ちる」であるため、**`toEqual({ ...DEFAULT_COLUMN_VISIBILITY, ...partial })` へ書き直した**（`M24-01` §7-8「数ではなく定義から導く」と同じ型） |
| **3** | `e2e/m24-01-character-and-list.spec.ts`「C: セットプレイ数の列」 | **「表示列」から明示的に ON にしてから確認する。** ヘルパは `e2e/support/column-visibility.ts` へ（`E-232`）。**★あわせて「既定では出ない」ことも同ケースで確かめている** |
| **4** | `e2e/m17-05b-export-flow.spec.ts`（一覧・マイコンボの 3 箇所） | **`getByText(memo, { exact: true })` → `page.locator("tr", { hasText: memo })` へ。** ⇒ **どの列がメモを表示していても成立する形**にした（命題は「fixture が一覧に出ている」） |
| **5** | `e2e/m24-03-recipe-readability.spec.ts` (1) | **「備考列が既定では出ていない」を 1 行追加**（破壊確認 6 の赤経路） |

**★影響しないことを実査済み**——`combo-crud` / `combo-custom-states` / `m14-03b` / `m12-03` の `getByText(memo, { exact: true })` は**すべて詳細画面**（`saveNewComboAndOpenDetail` 経由）であり、一覧の備考列に依存していない。

### 13.4 ★破壊確認 6（**追加**）

**壊し方**: `DEFAULT_COLUMN_VISIBILITY` の `memo` / `setupCount` を `true` へ戻す。

- **(a) 対象の実在**: `web/src/constants/combo-list.ts:76,80`
- **(b) 観測の変化**: **変わった。赤くなった経路は 3**

```
$ cd web && pnpm test -- --run
      Tests  1 failed | 1997 passed (1998)
   × ComboTable > セットプレイ数の列(M24-01 §4.5 / SM-012) > ★既定では出ない(M24-03 の開発者判断)

$ pnpm e2e e2e/m24-03-recipe-readability.spec.ts e2e/m24-01-character-and-list.spec.ts
  ✘  (1) 一覧でモードを切り替えると、省略されていたレシピが全部読める(SM-130)
  ✘  C: 一覧にセットプレイ数の列が出る(0 件と 1 件以上の両方)
```

**★復旧は `cp` で行った**（Git の `checkout` / `restore` は deny 対象）。**復旧後の差分 0 を確認済み。**

### 13.5 ★取り込み後の再検証

| 対象 | 結果 |
|---|---|
| `cd web && pnpm exec tsc --noEmit` | **エラー 0** |
| `cd web && pnpm test -- --run` | **191 files / 1998 tests 全緑**（取り込み前 1997。**既定 OFF の網を 1 本追加**） |
| `make e2e`（ポート確認済み: `15:26:01` / 47390・5273 空き） | **187 passed / 0 failed** |
| `internal/` `cmd/` `migrations/` の差分 | **0 バイトのまま**（`git diff --stat` が空） |
| 各 `check-*.sh` | §8 の再実行で全件緑 |
| **★★取り込み後の開発者確認**（2026-08-26） | **実施済み。逐語＝「既定で消えていました。一覧とマイコンボの両方を見ました。」** **⇒ `D-429` 条件 1「同一のビルドで確認されている」を満たすのはこの確認である**——**列 2 件を直した後のビルドで見てもらっているため。** §12.2.1 の 1 回目だけでは条件 1 が破れていた（確認の後に穴埋めをすると、確認は確認でなくなる） |

---

*以上、M24-03 完了報告。*
