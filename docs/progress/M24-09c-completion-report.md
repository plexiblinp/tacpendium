# M24-09c 完了報告: E2E スイートの安定化

| 項目 | 内容 |
|---|---|
| 対象指示書 | `docs/instructions/M24-09c-e2e-suite-stabilization.md` **v1.2.0** |
| チェックリスト | `docs/instructions/reviews/M24-09c-review-checklist.md` **v1.2.0** |
| 着手基点 | `c8c7330`（`#107`＝M24-01 / `#108`＝M24-09b の両マージを祖先に含む） |
| 実施日 | 2026-08-26 |
| CHANGE 消費 | **0 本**（§7.4.1 で 6 条件を判定した結果） |
| マイグレ消費 | **0 本** |
| 新規依存 | **0 本** |

> **★本報告が実測の正本である。** progress-log には索引行だけを置く（`CLAUDE.md` §8）。

---

## 0. 先に読んでほしい 4 点

**★★(1) 本番コードのバグを 1 件見つけた。直していない（§1.3-3）。** VAL-C02 の重複判定と INSERT が不可分でないため、**同一識別キーの本登録コンボが 2 件同時に存在した**。§9 に決定論的な再現手順を書いた。

**★★(2) 指示書 §1.1 の分類が実測と食い違っている。** 同表は `M24-09b` を `flaky` としているが、**実際は `fail` である**（§2.6）。

**★★(3) 同じ症状の未着手記録は 8 件ではなく 9 件ある。** `e2e-shared-global-resource-parallel` が §AH の束ねに入っていない（§6）。**開発者の裁定により本サブの射程に含めた。**

**★★(4) 自分の計測器が壊れていた。実測 1・2 のポート証跡は無効である。** 当初のポート確認は `ss` 前提だったが**本環境に `ss` は無く、常に「空き」と誤答していた**（§2.7）。**⇒ 実測 1・2 の 5 回分について「ポートが空いていた」とは主張しない。**

---

## 1. 結論

**機序（1 文）**: `fullyParallel: false` が直列化するのは 1 ファイルの中だけで、**spec ファイル同士は worker 数だけ並列に走る**のに、E2E スタックは**バックエンド 1 本・使い捨て DB 1 個・`config.toml` 1 個の共有資源**であり、ファイルをまたいだ干渉を防ぐ仕組みが無い。

**採った手**: `web/playwright.config.ts` に **`workers: 1` を 1 行**。既存 spec の書き換えは **0 本**。

---

## 2. 実測（§4.1）

### 2.0 母数の宣言

| 量 | 値 | 数え方 |
|---|---|---|
| spec ファイル | **51 本**（対策後 53 本） | `find web/e2e -name '*.spec.ts'` |
| テストケース | **178 件**（対策後 180 件） | **Playwright 自身の `Running N tests` を正とする** |
| 既定 worker 数 | **2** | `os.cpus().length`=4 ／ 既定 `"50%"` ／ `Math.max(1, floor(4*0.5))` |
| `retries` | **1** ⇒ `flaky` 区分は存在する | `web/playwright.config.ts:78` |

### 2.1 `passed` / `flaky` / `fail` の定義（§4.1.1）

本報告では指示書 §4.1.1 の定義に厳密に従う。**`passed`=1 回目で通った ／ `flaky`=1 回目で落ち retry で通った ／ `fail`=retry でも通らなかった。**

### 2.2 実測 1 —— 既定 worker（2）で 3 回

| 回 | passed | flaky | fail | 総経過 | Playwright 計測 | 落ちた spec |
|---|---:|---:|---:|---|---|---|
| 1 | 178 | **0** | **0** | 303s | 4.9m | — |
| 2 | 178 | **0** | **0** | 248s | 4.1m | — |
| 3 | 178 | **0** | **0** | 250s | 4.1m | — |

**★一発で緑になった回数 / 実行回数 = 3/3。** **★スイート全体の flake は 3 回とも再現しなかった**（`E-219`）。1 回目が長いのは Go のビルドキャッシュが空だったためである。

### 2.3 実測 2 —— `--workers=1` で 2 回

| 回 | passed | flaky | fail | 総経過 | Playwright 計測 |
|---|---:|---:|---:|---|---|
| 1 | 178 | **0** | **0** | 382s | 6.3m |
| 2 | 178 | **0** | **0** | 385s | 6.4m |

**★一発で緑になった回数 / 実行回数 = 2/2。**

### 2.4 実行時間の内訳（§3.3-9 / §3.3-10）

**起動時間はポートが listen を始めた時刻を実測した。**

| 量 | 実測 |
|---|---|
| **バックエンド起動**（47390 が listen） | **3〜4 秒** |
| **Vite 起動**（5273 が listen） | **4〜5 秒** |
| **⇒ 起動時間** | **約 5 秒（worker 数に依らずほぼ一定）** |
| テスト実行（既定 2 worker） | 約 243 秒 |
| テスト実行（直列） | 約 377 秒 |
| **直列化による倍率** | **約 1.54 倍** |

**★★指示書 §4.2.1 の「`起動時間 + テスト実行時間 × N`」は上限であって推定値ではない。** N=2 なら 2 倍のはずが**実測は 1.54 倍**だった。**理由＝`fullyParallel: false` は「ファイル単位」でしか並列化しないため、長いファイルが律速して並列度を使い切れない。** ⇒ **worker 数が多い環境ほど、直列化の代償は N 倍より小さくなる。**

### 2.5 実測 3 —— 9 件が名指しする spec の隔離実行（母数付き）

| spec | 母数 | 結果 |
|---|---:|---|
| `m14-03b-custom-states-realdata` | 1 | 1 passed |
| `m17-01-media-fields` | 2 | 2 passed |
| `moves-edit` | 1 | 1 passed |
| `combo-custom-states` | 1 | 1 passed |
| `m12-06-draft-promotion` | 1 | 1 passed |
| `m19-03-setup-results` | 8 | 8 passed |
| `m18-03c-drainage` | 6 | 6 passed |
| `m14-03e-third-wave-seed` | 1 | 1 passed |
| `m20-04-preset-management` | 9 | 9 passed |
| `m18-03b-materialize` | 4 | 4 passed |
| `m17-05c-pdf-pagination` | 3 | 3 passed |

**★11 本すべて緑。9 件の記録はいずれも単独では再現しない**（`E-219`）。**⇒ これは「記録が誤っていた」という意味ではなく、「単独実行では出ない＝干渉である」という §4.1 の見立てと整合する。**

### 2.6 ★実測 4 —— 干渉の実在の証明（**本サブの核心**）

**★★先に、この実測が何を示し何を示さないかを固定する**（レビュー M-1）。

| | |
|---|---|
| **示すもの** | **E2E スタックの構造が、2 つの spec ファイルによるデータ空間の奪い合いを許すこと** |
| **★示さないもの** | **§1.1 の 9 件が、既存 spec 間のデータ干渉で起きていたこと。** 既存 spec 間の干渉として立てた仮説 3 件はいずれも否定され（§10.1）、実測 1 でもスイート全体の flake は 1 件も再現しなかった |

**★既存 spec 間で干渉が実際に観測された事例は別に 1 件ある**——`followup-backlog.md:348` が記録する `M20-05` の実測（**プリセットを作る spec を別ファイルへ置くと `m20-04` の上限 8 件テストが落ちる。当該 spec 単独では 4/4 緑であり、組み合わせでしか出ない**）。**⇒ 「構成した干渉」（本サブの probe）と「観測された干渉」（M20-05）の両方が揃っている。**

**同じコード・同じデータで、並列度だけを変えると結果が 100% 反転する。**

| 条件 | 実行回数 | 結果 |
|---|---:|---|
| `--workers=2` | **10** | **10 回とも赤**（9 回は 1 failed、1 回は 2 failed） |
| `--workers=1` | **3** | **3 回とも緑** |

**★実測時の `retries` は `0` である**（全回 `--retries=0` を明示。レビュー H-2）。**★★取り込み後は probe 側が `test.describe.configure({ retries: 0 })` を宣言しており、スイート既定（`retries: 1`）に依らず「落ちたら赤」が構造的に保証される**——**retry を許すと `workers` を戻す変更が入っても「1 回目は赤・retry で緑」＝ flaky 扱いになり終了コードが 0 になるためである。** 既定経路での検証結果は §5 の破壊確認 1 に記載した。

**⇒ `E-216` の基準（結果が変わるなら timing ではなく競合）を満たす。**

**観測された 2 つの落ち方:**

| # | 頻度 | 内容 |
|---|---|---|
| **1** | 10 回中 9 回 | 後から作った側が **VAL-C02（ERROR）で弾かれる**。逐語＝`400 {"code":"VAL-C02","message":"同一キャラ・同一レシピ・同一状況のコンボが既に存在します(id=1)"}` |
| **2** | 10 回中 1 回 | **両方の作成が通り、双方が「同じ識別キーの生きた本登録コンボが 2 件」を観測した**（`Expected: 1 / Received: 2`）。**⇒ §9 の本番コードのバグ** |

**★干渉が成立する理由（現物）**: 識別キーは `characterId + starterMoveId + position + opponentStance + hitType + opponentSize` の 6 項で、**DB 全体で一意**である（`internal/service/combo/service.go:1020` `duplicateKeyOf`）。**親コンボ等でスコープされていない。** VAL-C02 は本登録の重複を **ERROR** で止める（`internal/service/validation/combo.go:153`）。

### 2.7 ★★実測の証跡についての自己申告（**汚染された実測は実測が無いより悪い**）

**当初のポート確認は `ss -ltn | grep` で書いていたが、本環境に `ss` は存在しない。** ⇒ grep が常に空振りし、**実際の状態に関わらず「OK: 空き」と出力していた。**

- **これは「走査の前に対象が実在することを確かめる」を怠った形である**（`.claude/commands/add_e2e_spec.md` の同趣旨の注記＝**描画されていなければ走査は常に真を返す**）。
- **⇒ 実測 1（3 回）と実測 2（2 回）の計 5 回について、「ポートが空いていた」という証跡は無効である。** 実際には 1 本のバックグラウンドジョブで直列に流しており他レーンも動いていないため汚染の可能性は低いが、**検証はしていないので主張しない。**
- **★残リスクの上限**（レビュー M-2）: `web/playwright.config.ts` の `webServer` は **両方とも `reuseExistingServer: false`** である。**⇒ ポートが埋まっていれば Playwright は明示エラーで止まり、「既存プロセスへ接続して気付かず緑になる」経路は設定上ふさがれている。** **⇒ 無効な証跡が隠しうるのは「実行が失敗して止まる」ケースまでであり、「汚染された緑」は生じない。** 実測 1・2 はいずれも正常終了しているため、結果自体は使える。

**是正**: `/dev/tcp` + `/proc/net/tcp` による判定へ差し替え、**listener を実際に立てて検知できることを対照実験で確認した**（`D-375` の「対照実験を添える」）。**実測 3・実測 4・§5.3・§5.4 の証跡は有効である。**

---

## 3. 手の選定（§4.2）

**判定基準を上から順に適用した。**

| 系統 | 採否 | 理由 |
|---|---|---|
| **(A) worker ごとに実行環境を分ける** | **不採用** | **`configPath` が `cmd/combomgr/main.go:81` で `"config.toml"` にハードコードされ、env override が無い**（存在するのは `COMBOMGR_LOG_LEVEL` / `COMBOMGR_DB_PATH` / `COMBOMGR_PORT` の 3 つのみ＝`internal/config/config.go:22,28,34`）。**⇒ worker ごとにバックエンドを立てても全員が同じ `config.toml` を奪い合う。分離には本番コードの変更が要り、§2.2 が禁じている** |
| **(B) 直列化（`workers: 1`）** | **★採用** | **判定基準 1（spec 書き換え 0 本）を最上位で満たす**。新規依存 0（基準 3）。変更は 1 行 |
| **(B') 環境非依存の小さい固定値** | **不採用** | **並列度が残る以上、干渉も残る**。得られるのは「環境間で症状が揃う」ことだけで、開発者の要望（一発で緑）を満たさない |
| **(C) 名前空間分離／後片付けの追加** | **不採用** | **原理的に埋められない**——教訓 `E-232`（`retrospective-log.md:1611`）の逐語＝**「並行実行中は『まだ消していない時間』が必ず在るため、後片付けでは原理的に埋められない」**。加えて `config.toml` は singleton で名前空間を切れず（`E-142`）、spec 書き換えも要るため基準 1 に劣る |

**★判定基準 1 を満たすのは (A) と (B) だけであり、(A) は本番コード変更が要るため脱落した。⇒ 残ったのが (B) である。**

**★判定基準 2（§4.2.1）**: 実行時間の伸びは **1.54 倍**。10 倍の保険には遠く、報告のみで進めた。**(a) 1 回あたり 248s → 386s ／ (b) 一発で緑になった回数は既定 3/3・直列 2/2・対策後 5/5。**

**★判定基準 4**: 実測 4 は取れた（§2.6）ので、対策を実装した。**★実装は実測 4 を取った後に行った**（チェックリスト 1-3）。

---

## 4. 決定論 spec（§4.3）

`web/e2e/aa-interference-probe-a.spec.ts` ／ `-b.spec.ts` ／ 共通ヘルパ `web/e2e/support/interference-probe.ts`。

| 条件 | 実行回数 | 結果 |
|---|---:|---|
| **対策後（`workers: 1`）** | **10** | **10 回とも緑** |
| **対策前（`--workers=2`）** | **10** | **10 回とも赤** |

- **★冪等性ではない。** 1 本を 2 回走らせる形ではなく、**2 本の別ファイルが同じデータ空間を奪い合う形**である（チェックリスト 4-2）。
- **★DB を直接書いていない。** 作成は `POST /api/combos`、判定は**アプリ自身が保存前重複ダイアログで使う** `POST /api/combos/check-duplicate` を通す（チェックリスト 4-3 / `E-217`）。
- **★対象キャラに `zangief` を選んだ理由**: 全 51 spec を走査して**どの spec も触っていない**ことを確認済み。seed 済みでもある。⇒ probe が他 spec を巻き添えにせず、他 spec も probe を壊さない。
- **★ファイル名が `aa-` で始まるのは意図である**（Playwright はパス順に割り当てるため、先頭 2 本なら worker が 2 つ以上のとき必ず同時に走り出す）。

---

## 5. 破壊確認（§5.3・3 件）

**★壊す対象が実在することを先に確かめた**（`M-78`）。**★ただし 3 件目は指示書の想定とは意味が違う。正確に書く**（レビュー M-3）。

- **1 と 2 は対象が実在した**（`workers: 1` の行 ／ probe の干渉部分）。
- **★3 は「寄せ元の古い形」が歴史上存在しない。** 共通ヘルパ `interference-probe.ts` は probe と同時に新設したものであり、「既存の重複していた形を寄せた」わけではないためである。**⇒ 指示書 §5.3-3 が意図する「寄せたことで挙動が変わっていないことの対照」は、本サブでは成立しない。** **代わりに「ヘルパから逸脱させるとその spec だけが赤くなる」＝ヘルパが荷重を担っていることを確かめた。** 有用ではあるが、**指示書の対照とは別物である。**

| # | 壊したもの | 期待 | 実測 | 判定 |
|---|---|---|---|---|
| **1** | `workers: 1` を 1 行だけ無効化 | 決定論 spec が赤 | **赤**（`Running 2 tests using 2 worker` ／ VAL-C02 で 1 failed） | ✅ |
| **1'** | **同上を「既定経路」で再確認**（`--retries` を渡さない＝スイート既定 `retries: 1` の文脈。レビュー H-2） | **flaky 緑ではなく `fail` で赤** | **3 回とも `1 failed`**（`using 2 worker`）。対策ありでは 3 回とも `2 passed`（`using 1 worker`） | ✅ |
| **2** | probe B の識別キーを片割れとずらす（干渉部分の無害化） | 対策前の状態でも緑 | **`--workers=2` で 2 passed** | ✅ |
| **3** | probe A だけ共通ヘルパを使わず古い形へ戻す | **その spec だけ赤・他は緑** | **probe A のみ赤（`Expected: 1 / Received: 0`）／ probe B は緑** | ✅ |

**★赤くなった経路の数**: 破壊確認 1 では 2 本中 1 本が赤（もう 1 本は「先に作れた側」であり緑が正しい）。破壊確認 3 では 2 本中 1 本が赤。**赤くならない経路が残っていないことは、破壊確認 2（無害化すると全部緑）で対照が取れている。**

**★3 件とも実施後にファイルを復元し、復元されたことを実際に grep で確認した。**

**★★破壊確認 1' が H-2 の核心である。** `test.describe.configure({ retries: 0 })` を probe へ入れる前は、**`workers` を戻す変更が入っても「1 回目は赤・retry で緑」＝ flaky 扱いになり、スイートの終了コードが 0 になりえた。** ⇒ `web/playwright.config.ts` の「本行を消す変更は、その 2 本が赤くなることで検出される」という断言が、既定経路では保証されていなかった。**取り込みで構造的に保証した上で、既定経路で 3 回実測して確かめた。**

---

## 6. 9 件の結末（§4.4）

**★指示書は 8 件だが、実査で 9 件目が見つかった**（`e2e-shared-global-resource-parallel`。`followup-backlog.md:348`・**未着手**・**5 サブ連続で観測**・**docs 配下 26 ファイルから参照＝9 件中で最多**）。**開発者の裁定により射程に含めた。**

| # | スラッグ | 現在の再現性 | 本サブの対策で説明が付くか | 結末 |
|---|---|---|---|---|
| 1 | `CO-021` | 隔離 1/1 緑・フル 5 回で 0 回 | **付く**。`m14-03b:23` はキャラ一覧 API の初回応答を見る。並列時は他ファイルの書き込みと同じ DB を奪い合う | **解消見込み（対策後 5 回で 0 回）** |
| 2 | `CO-022` | 隔離 2/2 緑・フル 5 回で 0 回 | **付く**。保存後の遷移待ちが、他ファイルの書き込みで遅延する形 | **解消見込み** |
| 3 | `CO-025` | 3 spec とも隔離緑・フル 5 回で 0 回 | **付く**（`M24-09b` の実測でも 4 回とも 0 回） | **解消見込み** |
| 4 | `e2e-flaky-isolation` | — | **★最もよく説明が付く**。同記録の逐語が「3 回走らせると毎回異なる spec が 1 件落ちる／単独実行すると green」であり、**本サブの実測 4 と同一の形** | **解消見込み** |
| 5 | `e2e-sqlite-write-contention`(a) | — | **★一部のみ**。並列書き込みが消える点は説明が付く。**ただし `busy_timeout` は `M23-10` で既に是正済み**（`internal/infra/db/db.go:38` が DSN で 5000 を指定）であり、**本記録が書かれた当時の前提は既に変わっている** | **未解消（別の機序の可能性）。§10 へ申し送り** |
| 6 | `e2e-flaky-combo-post-500` | 隔離 8/8 緑 | **★断定しない**。`POST /api/combos` の 500 は干渉だけでは説明しにくい（チェックリスト 7-3）。**当時サーバ側ログが採取されておらず、原因は未断定のままである** | **未解消（再現したらまずサーバログを採る）** |
| 7 | `e2e-flaky-m18-03c-drainage` | 隔離 6/6 緑 | **付く**（観測記録であり、単独では再現しない点が一致） | **解消見込み** |
| 8 | `e2e-flaky-one-per-run-persists` | 隔離 1/1 緑 | **付く**。「毎回 1 件・毎回別のテスト」は**特定 spec の欠陥ではない**ことを意味し、干渉の形と一致 | **解消見込み** |
| **9** | **`e2e-shared-global-resource-parallel`** | `m20-04` 9/9・`m18-03b` 4/4・`m17-05c-pdf` 3/3 とも隔離緑 | **★付く。しかも同記録自身が恒久策候補として `workers: 1` を名指ししていた**（逐語＝「残るのは恒久策の選択——`workers: 1` ／ 資源ごとの排他フィクスチャ ／ 上限テストの相対化」） | **解消見込み** |

### 6.1 ★機序の書き分け（レビュー M-4）

**「9 件が 1 つの構造から出た」の中身は一様ではない。** `workers: 1` はどちらにも効くため結末の判定は変わらないが、**主張の強さが違う**ので分けて書く。

| 機序 | 該当 | 実証の度合い |
|---|---|---|
| **(i) データ空間の奪い合い** | 4（`e2e-flaky-isolation`）／ 9（`e2e-shared-global-resource-parallel`） | **本サブが決定論的に実証（§2.6）＋ `M20-05` の実測** |
| **(ii) 並列度そのもの（遅延・書き込み競合）** | 1（`CO-021`）／ 2（`CO-022`）／ 3（`CO-025`）／ 7 ／ 8 | **一般論に留まる。** 「他ファイルの負荷で待ちが延びる」形であり、本サブは個別には実証していない |
| **(iii) 未特定** | 5（`e2e-sqlite-write-contention`）／ 6（`e2e-flaky-combo-post-500`） | **未解消。§10 へ申し送り** |

**⇒ `M24-CLOSE` で残件を判定するときは、(ii) の 5 件が「対策後に再現しないこと」でしか裏付けられていない点に留意されたい。**

**★★9 件すべてを「解消」とは書かない。** 5 と 6 は未解消である。**とくに 6（`POST /api/combos` の 500）を干渉へ安易に束ねなかった**——サーバ側の 500 は干渉では説明しにくく、当時ログが無いため断定できない。

---

## 7. 否定形確認（§4.5）

### 7.1 走査 1 —— 「flaky は仕様である」「retry で吸収する」趣旨

**母集合: 1,241 ファイル**（`docs/` 配下 1,185 ＋ `web/e2e/*.ts` 55 ＋ `Makefile` 1）。

| 種別 | 件数 |
|---|---|
| **「flaky は仕様／正常／想定内」と明言** | **2 件**（本サブの指示書自身の走査指示行を除く） |
| **「retry で吸収」趣旨** | **25 ファイル** |
| `web/e2e/` のコメント | **0 件**（retry 前提の防御的記述はあるが、是認の明言は無い） |
| `Makefile` | **0 件** |

**★★うち 2 件は「制度化された場所」に入っている**——`docs/instructions/phase3/M17-03-stage2-ui.md:182`（**受入条件**）と `docs/instructions/phase3/reviews/M17-03-review-checklist.md:81`（**レビュー合格基準**）。**次の担当が「落ちても retry で通るから正常」と読む経路がここに残る。**

**★完了済みマイルストーンの指示書・完了報告は当時の記録として書き換えていない**（`D-535`）。**挙がったことだけを報告する。**

**★あわせて**: `web/playwright.config.ts:78` の `retries: 1` には**理由コメントが 1 行も無い**。上記 25 件はすべて、この無注釈の設定を事後的に正当化する形で書かれている。**本サブでは `workers: 1` の側に理由を書いた**（同じ轍を踏まないため）。

### 7.2 走査 2 —— 9 件のスラッグ名の写し

**母集合: `docs/` 配下 1,185 ファイル。**

| スラッグ | 出現ファイル数 | うち「未着手」と併記 |
|---|---:|---:|
| `e2e-shared-global-resource-parallel` | **26** | 14 |
| `e2e-flaky-isolation` | 15 | 8 |
| `e2e-sqlite-write-contention` | 13 | 9 |
| `CO-022` / `CO-025` | 12 | 9 |
| `CO-021` | 11 | 9 |
| `e2e-flaky-combo-post-500` | 9 | 7 |
| `e2e-flaky-m18-03c-drainage` | 8 | 7 |
| `e2e-flaky-one-per-run-persists` | 6 | 5 |

**★畳むときは写し先も見ること**（`E-114`）。**★`e2e-flaky-one-per-run-persists` だけが followup に独立行を持たない**（束ね行 `:800` の中に埋め込まれているのみ）。

---

## 8. 完了の証明（§5.4）

### 8.1 必要条件 —— `make e2e` 5 回連続

| 回 | passed | flaky | fail | 総経過 | ポート確認 |
|---|---:|---:|---:|---|---|
| 1 | 180 | **0** | **0** | 386s | OK×2（有効な証跡） |
| 2 | 180 | **0** | **0** | 391s | OK×2 |
| 3 | 180 | **0** | **0** | 392s | OK×2 |
| 4 | 180 | **0** | **0** | 391s | OK×2 |
| 5 | 180 | **0** | **0** | 399s | OK×2 |

**★★上記 5 回はレビュー取り込み前のコードで測ったものである**（取り込みで probe を改修したため）。**⇒ 取り込み後のコードでフルスイートを 1 回追試した——`Running 180 tests using 1 worker` ／ `180 passed` ／ 417 秒 ／ ポート確認 OK×2。** **「直す前に測った数字を、直した後の証拠として使わない。」**

**★5 回とも `Running 180 tests using 1 worker`** ＝ `workers: 1` が実際に効いていることの証跡。母数が 178 → 180 なのは probe 2 本を足したためである。

### 8.2 ★★十分条件 —— 緑の回数を根拠にしない

**★対策前も既定 worker で 3/3 緑だった。⇒ 緑の回数だけでは何も言えない。** 根拠は次の 2 つに置く。

1. **機序が特定され、構造的に起こり得なくなった**: 干渉は「spec ファイルが並列に走ること」に依存する。`workers: 1` はその前提を消す。**⇒ 残る並列度がゼロなので、ファイル間干渉は構造的に起こり得ない。**
2. **破壊確認 3 件が通った**（§5）。とくに**破壊確認 1 で対策を外すと決定論 spec が赤くなる**＝対策が効いていることの直接の証拠。

---

## 9. ★★本番コードのバグ（**直していない・報告のみ**）

**§1.3-3 / §9.3 に従い、直さずに報告する。**

| 項目 | 内容 |
|---|---|
| **症状** | **同一識別キーの本登録コンボが 2 件同時に存在した。** VAL-C02 が防ぐはずの状態である |
| **決定論的な再現手順** | `git revert` 等で `workers: 1` を外し、`pnpm e2e e2e/aa-interference-probe-a.spec.ts e2e/aa-interference-probe-b.spec.ts --workers=2 --retries=0` を繰り返す。**10 回中 1 回の頻度で、両方の `POST /api/combos` が 201 を返し、双方が `duplicates` を 2 件観測する** |
| **機序（推定）** | **VAL-C02 の重複判定と INSERT が同一トランザクションで直列化されていない。** 2 つの要求が同時に判定を通過し、双方が INSERT できる（check-then-act の競合） |
| **なぜ今まで踏まなかったか** | **単独利用では同時要求が起きない。** **★ただし `M22` が LAN・多人数モードを入れたため、2 クライアントから到達しうる** |
| **なぜ本サブで直さないか** | **§2.3 の 3 条件を満たさない。** 条件 2（`DES-002` §4.2 の経路表に載る API の挙動に該当しない）を**満たさない**——`POST /api/combos` はまさに経路表の API であり、**CHANGE が要る**。⇒ 止めて報告する |
| **設計卓への申し送り** | **`DES-006` の VAL-C02 の判定が、並行要求下では保証されないことになる。** 判定を「保証」と読める記述があるなら、そこも同時に見ていただきたい |

---

## 10. 実査で見つかった食い違い（**報告対象**）

| # | 何が | どの根拠で | どう違うか |
|---|---|---|---|
| **1** | **指示書 §1.1 の表の分類** | `M24-09b-completion-report.md:101` の逐語＝run 2 で落ちた 2 本は「**両方とも retry #1 でも赤**」 | 同表は `M24-09b` を **`flaky`** としているが、§4.1.1 の定義では **`fail`** である。**⇒ 「開発者は `fail`、製造は `flaky` を見ていた」という `D-557` の見立ては成り立たない。クラウド実行環境でも `fail` は出ている** |
| **2** | **束ねの件数** | `followup-backlog.md:348` | **9 件目 `e2e-shared-global-resource-parallel` が §AH の束ねに入っていない**（§6） |
| **3** | **束ね行 §AH `:800` の内部矛盾** | `parallel-board.md:712` では `P-41` は解消済み | 割付列は `★開発者の承認待ち`（`P-41`）のままだが、内容列冒頭は `【2026-08-25 決着・D-554】…承認した`。**同一セル内で状態が食い違っている** |
| **4** | **`CLAUDE.md` §5 の E2E 記述**（§7.4.1 条件 6） | `Makefile:97-101` が自認、および本サブの実査 | `CLAUDE.md:158` は「使い捨て DB + 専用ポート…で実行され、**dev DB・dev サーバに影響しない**」とするが、**E2E スイート自身が `PUT /api/config` を踏み、リポジトリ直下の `config.toml`（dev と同じ実ファイル）を再シリアライズしてコメントを落とす**。**★本サブの変更が原因ではなく既存の齟齬である。★`CLAUDE.md` は開発者のファイルなので編集していない** |
| **5** | **`e2e-flaky-one-per-run-persists` の行** | `grep -c` | 9 件中この 1 件だけが followup に**独立行を持たない** |

### 10.1 ★製造側の見立てのうち、実測で否定されたもの（**自己申告**）

**Plan Mode で「干渉の証拠」として挙げた 3 件は、裏を取った結果いずれも誤りだった。報告に載せる前に取り下げる。**

| # | 取り下げた主張 | 否定した根拠 |
|---|---|---|
| 1 | `"e2e-c-setup"` を `m19-01` と `m19-03` が奪い合う | **VAL-S04 はコンボ単位である**（`internal/service/setup/validate.go:133`「同一コンボ内の同一レシピ重複チェック」／`restore.go:339`「親が違えば同一レシピは正当である」）。**2 本は別々の親コンボへ付けており衝突しない** |
| 2 | `memo:"m20-05"` を `m20-04` と `m20-05` が奪い合う | **memo はラベルにすぎず、両者とも作成応答の `id` で引いている**。検索キーではない |
| 3 | `m20-04` と `m20-05` が `defaults.presetId` を奪い合って落ちる | **2 本だけを `--workers=2` で 5 回走らせたが 5 回とも緑**。予測は外れた（干渉の窓が狭いと見られる） |

**★これらを裏取りせずに書いていれば、虚偽の主張を 3 件出していた。** チェックリスト 7-3（**安易に束ねていないかを見る**）は、まさにこの型を戒めている。

---

## 11. 自己テスト結果（§7.2）

| 対象 | 結果 |
|---|---|
| `make e2e` 5 回連続 | **5 回とも 180 passed / 0 failed / 0 flaky**（§8.1） |
| 決定論 spec 10 回 | **10 回とも緑**（対策前は 10 回とも赤） |
| 破壊確認 3 件 | **3 件とも期待どおり**（§5） |
| `go test ./... -count=1` | **FAIL 0 件**（着手前と同じ。本サブは Go を 1 バイトも触っていない） |
| `pnpm test -- --run` | **184 ファイル / 1,881 テスト passed**（同上） |

### 11.1 非破壊性の証明（チェックリスト 2-1）

```
$ git diff --stat c8c7330..HEAD
 web/e2e/aa-interference-probe-a.spec.ts |  53 ++++++++++++
 web/e2e/aa-interference-probe-b.spec.ts |  53 ++++++++++++
 web/e2e/support/interference-probe.ts   | 137 ++++++++++++++++++++++++++++++++
 web/playwright.config.ts                |  19 +++++
 4 files changed, 262 insertions(+)
```

- **`web/src` / `internal` / `cmd` / `migrations` / `.github` / `CLAUDE.md` / `.claude/commands` はいずれも変更ファイル数 0。**
- **★262 insertions・0 deletions。** 新規のつもりのファイルが `+` だけであることを確認した（`E-225`。**deletions のある新規ファイルはそれ自体が矛盾している**）。
- **専用ポートは不変**（`BASE_E2E_BACKEND_PORT = 47390` / `BASE_E2E_VITE_PORT = 5273`）。
- **既存 spec 51 本は 1 本も書き換えていない**（判定基準 1 を 0 本で満たした）。

---

## 12. 品質チェック（§7.3）

| 検査 | 結果 |
|---|---|
| `check-artifact-integrity.sh`（**1 本目**） | **exit 0・違反なし** |
| `check-md-emphasis.sh` | **exit 0・436 行／ベースライン 436 行（増加なし）** |
| `check-doc-refs.sh` | **exit 0・dead reference なし** |
| `check-progress-log-index.sh` | **Phase D で索引行を追記した後に回す。★偽の緑を返しうるため索引行は目視でも確認する** |

---

## 13. CHANGE 要否の判定（§7.4.1・**6 条件を 1 件ずつ**）

| # | 条件 | 判定 | 理由 |
|---|---|---|---|
| 1 | `DES-002` §4.2 の経路表に載る API の挙動が変わったか | **不要** | 本サブは API を 1 バイトも触っていない。**★ただし §9 のバグは同経路表の API の挙動に該当するため、そちらは設計卓の起票対象である** |
| 2 | `DES-003` のスキーマ・論理削除の契約に触れたか | **不要** | `migrations/` 差分 0 |
| 3 | `DES-006` の VAL の判定結果が変わったか | **不要** | 本サブの変更では変わらない。**★ただし §9 のバグは「VAL-C02 の判定が並行要求下で保証されない」ことを意味する** |
| 4 | `SUPP-001` §5.2 のパッケージ構成に増減があるか | **不要** | §5.2 は **Go** パッケージ構成の節であり、`web/` は §5.3 へ委譲されている。追加した 3 ファイルは `web/e2e/` 配下で、`web/e2e/support/` は `M24-01` が新設し `D-553` が運用の型として承認済み |
| **5** | **`SUPP-001` §5.5 のテスト方針が変わるか**（E2E の走らせ方は同節の射程） | **不要（ただし要判断）** | **§5.5 は worker 数・並列度について何も書いていない**ため、失効する記述は無い。**★「E2E は直列で走る」を設計書に明記すべきと設計卓が判断するなら、そのときは CHANGE が要る。判断は設計卓の手番として申し送る** |
| **6** | **`CLAUDE.md` §5 の E2E 記述が実態と食い違うようになるか** | **★既に食い違っている（本サブの変更が原因ではない）** | §10-4 のとおり「dev DB・dev サーバに影響しない」が実態と違う。**`CLAUDE.md` は開発者のファイルなので編集せず報告する**（条件 6 の定めどおり）。**★あわせて「使い捨て DB ＋ 専用ポート」の記述自体は本サブ後も真である**（worker ごとの DB 分離は採らなかったため失効しない） |

**★判定した結果「不要」であっても、判定したこと自体を記録した**（§9.4）。

---

## 14. ■ 併せて更新が要るもの（Phase D-0 常設項目）

| 項目 | 該当 |
|---|---|
| **消費した CHANGE 番号の登録** | **なし**（CHANGE 消費 0 本。§13） |
| **「次の番号」の写し先の全数是正** | **なし**（番号を払い出していない） |
| **消費したマイグレ連番** | **なし**（`migrations/` 差分 0） |
| **版を上げた文書の参照元** | **なし**（本サブは設計書の版を上げていない） |
| **★その他** | **`docs/progress/progress-log.md` への索引行**（Phase D で追記）／ **設計伝達レポート**（レビュー後の工程） |

> **★工程順として「まだ無い」ものを過去形で書かない**（`M24-09b` §7-2）。**索引行と設計伝達レポートはレビュー後の工程であり、本報告の作成時点では未作成である。**

---

## 15. 完了後の次ステップ（設計卓へ）

1. **§9 の本番コードのバグ**の扱いを決めていただきたい（CHANGE 起票の要否）。
2. **§13 条件 5** ——「E2E は直列で走る」を `SUPP-001` §5.5 へ明記するかどうか。
3. **§10-4** —— `CLAUDE.md:158` の「dev に影響しない」の是正（開発者のファイル）。
4. **followup §AH の束ね行と 9 件それぞれの指し先を同時に畳む**（**片方だけ残さないこと**）。**★9 件目 `e2e-shared-global-resource-parallel` を束ねへ加える判断が要る。**
5. **§10-3 の束ね行の内部矛盾**（割付列が `P-41` のまま）の是正。
6. **§6 の未解消 2 件**（`e2e-sqlite-write-contention` / `e2e-flaky-combo-post-500`）は followup に残し、`M24-CLOSE` で行き先を確認する。

---

---

# 追補（指示書 **v1.4.0** ／ チェックリスト **v1.4.0** ／ `CHANGE-135` **v1.1.0** ／ 裁定 `D-569`）

| 項目 | 内容 |
|---|---|
| 実施日 | 2026-08-27 |
| 追加コミット | 段 1 + 段 2 = `777ee1f`（push 済み）／ 段 3 = 本追補で作成 |
| 指示書の版 | **リポジトリ内は v1.2.0 のまま**。v1.3.0 / v1.4.0 と `CHANGE-135` は**別ブランチで反映される**ため、製造は**参照のみ**で本ブランチにコミットしていない |

> **★上の「参照のみ」は開発者の明示指示である。** 本追補が実測の正本であることは §0 の宣言と同じ。

---

## 16. 段 1・段 2 —— dev の `config.toml` からの切り離し（`CHANGE-135`）

### 16.1 事実 A の機序

E2E は test ごとに新規ブラウザコンテキストを使うため、既定キャラの解決順
（`web/src/features/combo/defaultCharacter.ts:55-62`）の**段 1（URL `?character=`）と段 2（sessionStorage）が常に空**になり、
**段 3b（設定ファイルの `defaults.characterId`）が単独で決める**。
バックエンドの config パスは `cmd/combomgr/main.go` にハードコードで、**dev と同じ実ファイル**を読んでいた。
⇒ 開発者が設定画面で既定キャラを変えた瞬間に「リュウが出ること」を前提にした spec がまとめて落ちる。

- **開発者ローカルの実測**: **35 failed / 145 passed / 17.8 分**（`[defaults] character_id` が juri）。
- **`workers` とは無関係**。同じ 35 件が**従来 worker 数のブランチでも落ちた**（開発者の実測）。
- **★35 件が 1 つの署名を共有するという当初の見立ては誤り**だった。**26 件が `getByText("リュウ")`、残り 9 件は別ロケータ**である（§10.1 と同じ型の自己申告）。

### 16.2 依存は双方向だった

| 向き | 何が起きていたか | 消し方 |
|---|---|---|
| dev → E2E | 既定キャラが段 3b として E2E の画面に効く | E2E 専用 config を `COMBOMGR_CONFIG_PATH` で渡す |
| E2E → dev | スイート自身の `PUT /api/config` が dev の `config.toml` を再シリアライズし**コメントを全部落としていた** | 書き込み先が `.tmp` 側になる |

### 16.3 段 1 の実装（本番コード 2 か所・開発者承認済み）

- `internal/config/config.go` —— `EnvConfigPath = "COMBOMGR_CONFIG_PATH"` を既存 3 定数と同じ形で追加。
- `cmd/combomgr/main.go` —— `resolveConfigPath()` を追加。**env 未設定・空文字・空白のみなら `"config.toml"`**。
- `cmd/combomgr/main_test.go` —— 上記 4 ケースを Go テストで固定した。
- `web/playwright.config.ts` —— 使い捨て DB と同じ `.tmp` 配下へ `config.toml.example` を複製し env で渡す。**`rm -rf` の「後」に作る**。
- `Makefile` —— dev の `config.toml` を作る処理を削除。**失効した注記も同じ手番で書き換えた**（「不在だと全 spec が落ちる」「E2E が `config.toml` を再シリアライズする」はどちらも成り立たなくなったため）。

**★既定の挙動は不変である。** `SUPP-001` §5.8 の「配置場所＝アプリ実行ディレクトリ直下」という契約は変わらない。env はそれを一時的に外すだけである。

> **★CHANGE 要否の判定を製造は誤った（自己申告）。** §13 で「設計文書を無効化しない」と結論したが、**走査したのが env 変数名だった**。正しい対象は **`SUPP-001` §5.8 の「配置場所」の記述**である。
> **さらに「既存 3 つの env が文書化されていないのだから 4 つ目も要らない」という理由付けは、過去の欠落を根拠にしている**（設計卓 `CHANGE-135` §3-2 の指摘。**受理**）。

### 16.4 段 2 —— 既定キャラへの暗黙依存を消す（14 spec）

`web/e2e/support/character.ts` を新設し、既存の型を引用して寄せた（m17-03 の `?character=` 明示 ／ m24-01 の `?character_id=` ／ m20-04 の「id を直書きしない」）。

- **★`ryuCharacterId` は既に 13 ファイルへ複製されていた**（`E-114` の型）。**さらに 12 本増やさないため共通ヘルパにした。**
- キャラ名の `toBeVisible()` は「画面が出来た待ち」なので、**待ちの意味は残したまま要求したキャラの名前で待つ形**へ変えた（**アサーションは変えていない**）。
- **`character-default.spec.ts` は触っていない** —— 同 spec は**段 3b が単独で効くこと自体を主張**しており、書き換えると主張が消える。
- **検証**: E2E 専用 config へわざと `character_id = 5` を書いて回すと **179 passed / 1 failed** で、落ちたのは `character-default` だけだった。**＝ 14 本が依存を脱し、かつ検出器は生きている。**

**★この事故は 2 度目である。** 1 度目は 2026-08-13（開発者機・`character_id = 5`）で、`m20-04-preset-management.spec.ts:52-66` が再発防止のヘルパと注記を置いたが、**その 1 ファイルに閉じたまま横へ広がらなかった**。段 2 はその横展開である。

---

## 17. 段 3 —— 配信方式を `vite preview` にする（`D-569`）

### 17.1 何を変えたか（**3 か所だけ**）

| ファイル | 変更 |
|---|---|
| `web/vite.config.ts` | `preview.proxy` を追加。**ターゲット解決は同ファイルの `resolveApiTarget()` をそのまま呼ぶ**（裁定 §1。**第 2 の解決規則を書き起こさない**） |
| `web/playwright.config.ts` | `webServer[1].command` を `pnpm dev …` → **`pnpm build && pnpm exec vite preview …`**。ビルドが同じコマンドに入るため `timeout` を 300 秒へ |
| `Makefile` | **recipe は変えていない。注記のみ**（「ビルドを Makefile 側へ移さないこと」を明記） |

**★構成（webServer 2 本）もポート（47390 / 5273 を基準に worktree オフセット）も変えていない。**

**★★`pnpm build` をハーネス側に置いてあるのは意図である。** `Makefile` 側へ出すと **`cd web && pnpm e2e` を直接叩いた経路が古い `dist` を検査する**。**それは緑で通るため気づけない**（13-3 が「重大」と定める事故そのもの）。**条件付きにもしない**（`dist` の有無や更新時刻で分岐させない）。

### 17.2 `embed_web` を採らなかった理由（**速度ではない**）

裁定 `D-569` は**影響範囲**で決めた。製造の実測でも **`embed_web` 156〜158 秒 / `vite preview` 156〜159 秒**で差が無かった。

- `launchBrowser`（`cmd/combomgr/main.go:447`）が E2E のたびにブラウザを開こうとする**運用障害**。**抑止手段が無い。**
- 抑止するには**本番コードの 3 か所目**が要る（§12.9・13-6b で「**重大**」）。
- 「安定化」のサブが **E2E の挙動を 11 か所変える**。とくに **静的要求が全ミドルウェアを通り、ページ遷移ごとに余計な DB 参照とアクセスログが出る**こと（＝**flake の診断を難しくする側の変化**）。

> **★訂正（レビュー M-8）。** 裁定が挙げた **「React dev→prod で StrictMode の二重実行が消える」は、`vite preview` でも同じく起きる**。
> `vite preview` が配るのは `vite build` の成果物であり、React は production モードで動くためである。
> **⇒ これは `embed_web` と `vite preview` を分ける論点ではない。採った案でも失う検査であり、§23 に代償として計上した。**
> **★製造は裁定の理由付けをそのまま書き写しており、1 件ずつ裏を取っていなかった。**

### 17.3 §12.1.3-4 の撤回

製造の §6-1 が受理され、**Playwright は dev の `config.toml` を読み続ける**（現行実装のまま）。
`readDevBackendPort()` が読むのは **worktree のポートオフセットを決めるため**であり、「どの worktree に居るか」は dev の `config.toml` にしか無い情報である。
バックエンドのポートは `COMBOMGR_PORT` で明示的に渡すのでズレない。**★読み先を専用 config へ移すと `scripts/wt-new.sh` の worktree ごとのポート分離が壊れる。**
**13-2 は判定が反転し、揃えていたら「重大」だった。**

---

## 18. 段 3 の実測

### 18.1 §12.5.1 —— `make e2e` を 3 回（**母数つき**）

| 回 | 結果 | 一発で緑か | 実時間 |
|---|---|---|---|
| 1 | **180 passed** | ○ | 4m12.9s＝**253 秒** |
| 2 | **180 passed** | ○ | 3m45.3s＝**225 秒** |
| 3 | **180 passed** | ○ | 3m43.3s＝**223 秒** |

**一発で緑になった回数 ／ 実行回数 ＝ 3 / 3。** `flaky` 0 件・`fail` 0 件。

**内訳**

| 何 | 実測 |
|---|---|
| `pnpm build` 単体 × 3 | 26.3 / 27.2 / 27.4 秒 |
| ビルドをハーネスから外した 1 回（破壊確認 3-(b) と同一実行） | 3m24.1s＝**204 秒** |

⇒ **ビルドの上乗せは約 20〜27 秒**で、単体計測と整合する。

### 18.2 ★★母数 307〜309 秒は再現しなかった（**自己申告**）

破壊確認 2（`vite preview` → `pnpm dev` へ戻す）の実測は **180 passed / 6m42.4s＝402 秒**である。
**指示書が母数として与えた 307〜309 秒は、同じ環境の別時点の値であり、今日は出なかった。**

**⇒ 根拠に使えるのは同一セッション内の背中合わせ比較だけである。**

| 配信方式 | 実測（同一セッション・同一 HEAD・`workers: 1`・180 テスト） |
|---|---|
| `pnpm dev` | **402 秒** |
| `pnpm build` + `vite preview` | **223 / 225 / 253 秒** |

⇒ **約 1.6〜1.8 倍速。** **★「307〜309 秒 → 223 秒」とは書かない**（測った時点が違う値どうしを引き算しない）。

> **★以前の報告に載せた「`vite preview` 156〜159 秒」も再現しなかった。** 当時の測り方（ビルドを含んでいたか、`go run` のコンパイルキャッシュが温まっていたか）の記録が残っていない。**⇒ 母数として採るのは本節の値だけとする。**

### 18.3 §12.4-8 —— SPA fallback とプロキシが全 spec に足りるか

**180 passed を 3 回**で示した（§18.1）。`vite preview` は `server.proxy` を読まないため `preview.proxy` を足したが、**`/api` 以外の追加設定は要らなかった**。

### 18.4 §12.4-6 —— `reuseExistingServer` の実値

| 何 | 値 |
|---|---|
| 実装（`web/playwright.config.ts:138` / `:161`） | **両方 `false`** |
| `SUPP-001` §4.5:573 | 「dual webServer array + `reuseExistingServer: true`」 |

**⇒ 食い違い。報告のみ（`SUPP-001` は製造が編集しない）。**

**同 §4.5 にもう 1 件ある。** 同節は `resolveBackendPort()` が「起動時に `config.toml` を読み、**バックエンドの実ポートを動的取得**」と書くが、
実装の関数名は **`readDevBackendPort()`** で、役割も **worktree のポートオフセット決定**に変わっている（バックエンドのポートは `COMBOMGR_PORT` で明示的に渡す）。

---

## 19. 段 3 の破壊確認（3 件）

### 19.1 破壊確認 1 —— 段 1 の対照

**手順**: ハーネスから `COMBOMGR_CONFIG_PATH` を外し、dev の `config.toml` を `character_id = 5`（ken）にして `make e2e`。

**結果**: **1 failed（`character-default.spec.ts:9`）/ 179 passed**。⇒ **赤。段 1 の対照は効いている。**

> **★副作用（E2E → dev の汚染が残っているか）は判定できなかった。** 比較の基準（バックアップ）を**自分の `sed` の前**に取ってしまったため、実行後の差分が「E2E が書いた」のか「自分が書いた」のか分けられない。
> **★これは本サブで 2 度目の同じミスである**（1 度目は `config.toml` の md5 を自分の `sed` より前に取った件）。**汚染の有無は §16.2 の段 1 時点の実測で既に取れているため取り直していないが、本件を証拠として数えない。**

### 19.2 破壊確認 2 —— 配信方式を戻す

**手順**: `webServer[1].command` を `pnpm dev --port … --strictPort` へ戻して `make e2e`。

**結果**: **180 passed / 402 秒**。⇒ **赤/緑ではなく数で示す**（§18.2 の表）。

### 19.3 ★破壊確認 3 —— 「古い `dist` を検査する」事故（13-3 は**重大**）

**★1 回目の検査は設計が失敗しており、証拠にならなかった。**

`web/src/pages/PunishSearchPage.tsx:45` の `確定反撃サーチ` の**末尾に `Z` を足して** `make e2e` → **180 passed**。
原因は **`getByRole` の `name` が既定で部分一致**であること。**`確定反撃サーチZ` は `確定反撃サーチ` を含むので、対照が動かなかった。**
⇒ **「壊した対象が実在する」だけでなく「壊すと観測が動く」まで確かめる**（`M-78` ／ playbook §4.32）。**追記ではなく置換でやり直した。**

**2 回目（有効な検査）**

| # | 手順 | 結果 |
|---|---|---|
| (a) | 同 45 行を `確定反撃サーチ` → **`確定反撃サーナ`** （1 文字置換）して `make e2e` | **1 failed（`m18-02-punish-search.spec.ts:74`）/ 179 passed** ⇒ **通常経路では変更が反映される** |
| (b) | **ソースを元へ戻し**、ハーネスから `pnpm build &&` だけを外して `make e2e` | **同じ 1 件が落ち続けた**（`web/dist/assets/index-*.js` に `確定反撃サーナ` が残っていることを `grep` で確認済み） ⇒ **古い画面が観測できる** |

**⇒ 「その事故を検出する手が無い」ではない。** **ビルドをハーネスに束ねてある限り起きず、外した瞬間に起きる。**
**★だからこそ `pnpm build` を `Makefile` へ移してはならない**（`Makefile` の注記にも書いた）。

---

## 20. 段 3 後の非破壊性（13-6b は**重大**）

**基点 `c8c7330` からの差分（M24-09c の全レンジ）**

| 対象 | 差分 |
|---|---|
| `cmd/combomgr/main.go` | **`configPath` の 1 か所のみ**（`const` → `defaultConfigPath` ＋ `resolveConfigPath()` ＋ `run()` 冒頭の 1 行） |
| **`launchBrowser`** | **`c8c7330` の定義と逐語一致（13 行）。1 文字も触っていない。** |
| `web/src` | **0 ファイル** |
| `migrations` | **0 ファイル** |
| CI（`.github`） | **0 ファイル** |
| `CLAUDE.md` | **0 ファイル** |
| `.claude` | **0 ファイル** |
| `docs/design/`（`SUPP-001` 含む） | **0 ファイル** |

**対照（着手前と同一であること）**

| 何 | 結果 |
|---|---|
| `go test ./... -count=1` | **ok 55 パッケージ・失敗 0** |
| `pnpm test -- --run` | **184 files / 1881 tests passed** |
| `pnpm build`（＝ `tsc -b && vite build`） | 成功（型検査を含む） |

> **★`web/tsconfig.json` の `include` は `["src", "vite.config.ts"]` であり `e2e/` を覆わない**（レビュー L-5）。**⇒ 追補が変えた 14 spec と新設ヘルパ 2 本は上の 3 つの対照の外側だった。** **`pnpm exec tsc --noEmit -p e2e/tsconfig.json` を別に回して exit 0 を確認した**（§26.1）。

**probe（配信方式を変えたので取り直した）**

| 条件 | 結果 |
|---|---|
| `--workers=2` | **1 failed**（`VAL-C02` 衝突で本登録が 400）⇒ **赤** |
| 既定（`workers: 1`） | **2 passed** ⇒ **緑** |

**⇒ 配信方式を変えても決定論は保たれている。`workers: 1` を消す変更は、この 2 本が赤くなることで検出される。**

---

## 21. 裁定 §8 の宿題 —— 386 秒 → 307〜309 秒 の差

**切り分けを 1 回だけ測った**（段 2 の spec 書き換えだけを一時的に `777ee1f` の親へ戻し、他は同一）。

| 条件（どちらも `pnpm dev` ・ `workers: 1` ・ 180 テスト） | 実測 |
|---|---|
| 段 1 のみ（**段 2 を外した**） | **180 passed / 6m39.8s＝399.8 秒** |
| 段 1 ＋ 段 2（＝破壊確認 2 と同一実行） | **180 passed / 6m42.4s＝402.4 秒** |

**⇒ 差は 2.6 秒（0.6%）。段 2 は実行時間の要因ではない。**

**段 1 にも機序が無い。** 起動時に読む設定ファイルのパスが変わるだけで、**変わるのは 1 回の読取だけ**である。

**⇒ 約 20% の短縮を本サブの変更に帰属させる根拠は無い。**
**★同じ環境が別時点で 307〜309 秒と 400 秒を出しており、環境側の変動幅のほうが差より大きい**（§18.2）。
**⇒ 理由は「分からない」。** 裁定 §8 がそれを許している。**推測を断定形では書かない。**

---

## 22. 開発者のファイル 2 つ（13-10。**製造は編集しない**）

**★`vite preview` 採用により、`M24-09b` 時点の案から内容が変わる。ポート 5273 は残る。**

### 22.1 `CLAUDE.md:158`（§5 テスト規約の表）

**現行（逐語）**

```
| E2E(Playwright) | **導入済み**(`web/e2e/`・`make e2e`)。使い捨て DB + 専用ポート(バックエンド 47390 / Vite 5273)の独立スタックで実行され、dev DB・dev サーバに影響しない。新規 spec は `/add_e2e_spec` 参照 |
```

**提案（逐語）**

```
| E2E(Playwright) | **導入済み**(`web/e2e/`・`make e2e`)。使い捨て DB + 専用 config + 専用ポート(バックエンド 47390 / vite preview 5273)の独立スタックで実行され、dev DB・dev の config.toml・dev サーバのいずれにも影響しない。フロントは毎回ビルドし直した dist を配る(dev サーバではない)。worker は 1 に固定(`workers: 1`)。新規 spec は `/add_e2e_spec` 参照 |
```

**何が変わるか**: (1) 「専用 config」を足す（`CHANGE-135`）、(2) 「dev の `config.toml` にも影響しない」を足す、(3) `Vite` → `vite preview` とし配信物が `dist` であることを書く、(4) `workers: 1` を書く。

### 22.2 `.claude/commands/add_e2e_spec.md:91`

**現行（逐語）**

```
  // 前提: ウィザード完了済み DB。バックエンド(:47318) および Vite dev(:5173) が起動していること。
```

**提案（逐語）**

```
  // 前提: ウィザード完了済み DB。make e2e が使い捨て DB・専用 config で
  //       バックエンド(:47390) と vite preview(:5273) を起動する。
```

**何が変わるか**: 現行の `:47318` / `:5173` は **dev スタックの値**であり、E2E スタック（`47390` / `5273`）とも、配信方式（`vite preview`）とも一致しない。**現行の記述は 2 重に古い。**

---

## 23. 追補の射程外（**明示**）

| # | 何 | なぜ本サブでやらないか |
|---|---|---|
| 1 | **`VAL-C02` の check-then-act 競合**（§9） | **§12.6 で射程外。別サブへ回すと裁定済み。** 直していない |
| 2 | **`SUPP-001` §4.5 の齟齬 2 件**（§18.4） | **§6-2 受理済み。設計卓が直す。製造は編集しない** |
| 3 | **CI** | **§6-3 受理・§12.9 追加分。1 ファイルも触っていない** |
| 4 | **`e2e-does-not-exercise-production-static-serving`**（裁定 §7） | **本サブでは直さない。** `vite preview` は本番の静的配信経路（Go の embed）を通らないため、その経路は E2E で検査されないままである |
| 5 | **`D-548`（E2E ポートはホスト単位）** | **消えないことが確定済み。** **★「重ねられるようになった」とは書かない** |
| 6 | **★React の StrictMode 二重実行の検査**（レビュー M-8） | `vite preview` は production ビルドを配るため **StrictMode の二重実行が起きない**。**dev サーバでしか踏めない不具合は E2E で検査されなくなる。** **⇒ 採った案の代償であり、`embed_web` を選んでも同じだった** |
| 7 | **★`make e2e` が `web/dist` を毎回作り直す**（レビュー L-7） | **新しい副作用である。** `make build` などが直前に作った `dist` は E2E の後には残っていない（内容は同じソースから作られるので実害は無いが、**「dev に影響しない」の列挙に `web/dist` は含まれない**）。**§22.1 の提案文にも「毎回ビルドし直した dist を配る」と書いた** |

**13-6 の事実（§7 で受理済み）**: E2E 専用 config に `[defaults] character_id = 1` が入るのは**スイート自身の `PUT /api/config`** による。**毎回作り直され、値も常に 1** である（`config.toml.example` に `[defaults]` が無いため、解決順は段 4 の `INITIAL_CHARACTER_ID = 1` へ落ちる）。

---

## 24. ★★教訓 —— 「製造の環境では踏めない」が 2 度起きた

| # | 事象 | なぜ踏めなかったか |
|---|---|---|
| 1 | **事実 A**（dev の `config.toml` が E2E を汚す） | クリーンな clone には `config.toml` が無く、example から生成されるため |
| 2 | **`launchBrowser`** | クラウドでは `xdg-open` が失敗し warn ログだけになるため |

**⇒ どちらも「3 回緑だった」ことが「問題が無い」ことの証拠になっていなかった。**

**★緑は環境の証明にしかならない。**

**本追補で同じ型をもう 2 件出した**（どちらも自己申告）:

| # | 何 | 何が起きたか |
|---|---|---|
| 3 | **破壊確認 3 の 1 回目** | 対象は実在したが**壊しても観測が動かなかった**（部分一致）。**緑を「問題なし」と読みかけた** |
| 4 | **破壊確認 1 の副作用判定** | **基準を自分の変更の前に取ってしまい**、実行後の差分を誰が書いたか分けられなくなった（本サブ 2 度目）。**★§25.1 で測り直した** |
| 6 | **★実機確認の依頼に HEAD の確認が無かった** | 開発者の 1 回目の実機確認が赤になった原因は**実装ではなく pull 漏れ**だった（§25.0）。**★`claude/` 名前空間への push は、開発者のローカルを自動で追随させない。** **⇒ 依頼文の 1 行目に `git log --oneline -3` を置くこと** |
| 5 | **★撤回した数字がコード注記に残った**（レビュー H-5） | **完了報告 §18.2 で「再現しなかった」と申告した数字が、同じコミットで入れたコード注記に「実測・同一環境」の断定形で残っていた。** **★本サブで 2 度目である**（1 度目＝取り込み前のレビュー高-1）。**教訓を自分で書いた直後に、自分でもう一度破った** |

**⇒ 検査は「回したか」ではなく「壊したときに動くか」で価値が決まる。**

**⇒ そして「撤回した」は、本文・コード注記・索引のすべてを同じ手番で直して初めて成立する。**


---

---

## 25. ★§12.5.1-3 の対照 —— **事実 A の根治の証明**（13-5。**レビュー H-3 で追加**）

**★これは破壊確認 1 の裏返しであり、13-5 が「根治の唯一の証明」と定める項目である。**
段 1 の時点で取っていたが**完了報告に載っておらず、段 3 後の HEAD で追試もしていなかった**（レビュー H-3。**指摘は妥当**）。**段 3 適用後の HEAD（`03fc37a` ＋ レビュー取り込み）で取り直した。**

| 条件 | 実測 |
|---|---|
| dev の `config.toml` を **`[defaults] character_id = 5`（ken）** にしたまま `make e2e`（`COMBOMGR_CONFIG_PATH` は**渡したまま**） | **180 passed / 0 failed / 0 flaky。3m43.0s** |

**⇒ 緑。dev の既定キャラを変えても E2E は一切影響を受けない。**

**対照が効いていることは §19.1（破壊確認 1）が示す** —— **同じ状態で `COMBOMGR_CONFIG_PATH` を外すと 1 failed になる。**
**⇒ 「緑だった」ではなく「外すと赤くなる」まで揃っている。**

### 25.0 ★★【決着】開発者の実機確認（2026-08-27）—— **製造の環境では取れなかった証拠**

| 項目 | 内容 |
|---|---|
| 条件 | **画面から既定キャラを「ガイル」・既定プリセットを「2」に設定した状態**のまま `make e2e` |
| 結果 | **180 passed。`flaky` 0 件 ／ `failed` 0 件** |
| 実行時間 | **3 分以内**（**★開発者機（24 コア）は製造のクラウド環境の 223〜253 秒より速い**） |
| 開発者の判定 | **「これぐらいなら許容可能」** |

**⇒ 事実 A の根治が、以前 35 件がまとめて落ちた条件と同種の状態で証明された。**
**⇒ `defaults.presetId` も切り離されている**（プリセット 2 は本サブで一度も試していない条件だった）。
**⇒ 実行時間の受容可否は製造が決められない項目だったが、`vite preview` 化によって開発者の受容線を下回った。**

> **★製造の 3 回緑は、この証拠の代わりにならなかった。** クリーンな clone には `config.toml` が無く、`config.toml.example` に `[defaults]` も無いため、**製造は事実 A を踏めない。** **§24 の「緑は環境の証明にしかならない」がそのまま実演された。**
>
> **★1 回目の実機確認は赤だったが、原因は実装ではなく `777ee1f` 以降の pull 漏れだった。** **⇒ 実機確認の依頼文には「`git log --oneline -3` で該当 hash が出ることを確認してから回す」を 1 行目に置くこと**（§24 の 6 件目として計上）。

### 25.1 ★E2E → dev の汚染も、今度は正しく測れた

§19.1 の注記で「基準を自分の `sed` より前に取ってしまい判定できなかった」と自己申告した件を、**本節でやり直した。**

- **基準（`md5sum`）を、`sed` の後・`make e2e` の直前に取った。**
- 実行後に `md5sum -c` → **`config.toml: OK`（変化なし）**。

**⇒ スイートの `PUT /api/config` はもう dev の `config.toml` へ届かない。双方向とも切れている。**

---

## 26. レビュー結果と取り込み（Phase B / C）

**追補分のレビュー**（別セッションの独立したレビュー担当）: **重大 0 / 高 3 / 中 3 / 低 3**。詳細は `docs/progress/m24-09c-review.md` の「追補分のレビュー（2026-08-27）」。

**★9 件すべて採用した。不採用は 0 件（エスカレーション不要）。**

| # | 指摘 | 取り込み |
|---|---|---|
| **H-3** | §12.5.1-3 の対照が実測の正本に無く、段 3 後に追試もされていない | **§25 を新設し、段 3 後の HEAD で取り直した（180 passed）。§25.1 で汚染の測り直しも済ませた** |
| **H-4** | `progress-log.md:4969-4980` が追補で偽になった記述を抱えたまま | **Phase D で索引行を追記し、同時に偽になった 4 点を是正した**（§27） |
| **H-5** | **撤回した数字がコード注記に断定形で残っている**（`vite.config.ts` / `playwright.config.ts`） | **両方の注記を書き換えた。** 同一セッションの背中合わせの値へ差し替え、**「別時点の値どうしを引き算しない」ことを注記自体に書いた** |
| **M-7** | `playwright.config.ts` の「config パスがハードコードで env で動かせない」が段 1 で偽になった | **書き換えた。** 採らない理由を**現在成り立つ理由**（バックエンドを worker 数だけ起こす必要があり別サブの規模になる）へ差し替えた |
| **M-8** | **StrictMode の二重実行が消えるのは `vite preview` でも同じ**で、案を分ける論点ではない | **§17.2 に訂正を入れ、§23 の代償表へ 6 行目として計上した** |
| **M-9** | 13-12（`CHANGE-135` の射程が広がったか）の判定が無い。**`SUPP-001` §5.5 が追補で一度も再判定されていない** | **§27 を新設して 1 件ずつ判定した** |
| **L-5** | 対照に `e2e/` の型検査が無い（`web/tsconfig.json` は `e2e/` を覆わない） | **`pnpm exec tsc --noEmit -p e2e/tsconfig.json` を回した ⇒ exit 0。§20 の対照へ追加した** |
| **L-6** | `m17-05b-export-flow.spec.ts` の `characterIdOf` がループ内で同じ API を 6 回叩く | **ループの外へ出した**（6 件とも同じキャラのため） |
| **L-7** | `make e2e` が `web/dist` を毎回作り直す副作用が「影響しない」の列挙に無い | **§23 の代償表へ 7 行目として計上し、§22.1 の提案文にも書いた** |

> **★H-5 は本サブで 2 度目の同じ型である**（1 度目＝取り込み前のレビュー高-1）。**撤回は本文とコード注記を同じ手番で直す**という教訓を、**自分でもう一度破った。** §24 の表へ 5 件目として数える。

### 26.1 §20 の対照（**L-5 を反映した最新**）

| 何 | 結果 |
|---|---|
| `go test ./... -count=1` | **ok 55 パッケージ・失敗 0** |
| `pnpm test -- --run` | **184 files / 1881 tests passed** |
| `pnpm build`（＝ `tsc -b && vite build`） | 成功 |
| **`pnpm exec tsc --noEmit -p e2e/tsconfig.json`**（**新規**） | **exit 0** |

---

## 27. ★13-12 —— `CHANGE-135` の射程が広がったかの判定（**レビュー M-9**）

**判定対象は `SUPP-001` の 3 節である。1 件ずつ判定する。**

| 節 | 逐語（要旨） | 段 1・段 2 で | **段 3（`vite preview`）で** | 判定 |
|---|---|---|---|---|
| **§5.8** | 設定ファイルの**配置場所＝アプリ実行ディレクトリ直下** | **射程内**（`CHANGE-135` が既に扱う） | 変化なし | **既に射程内。広がらない** |
| **§4.5** | E2E 基盤の実装メモ（`resolveBackendPort()` ／ **dual webServer array + `reuseExistingServer: true`**） | 齟齬 2 件が顕在化（§18.4） | **★広がる。** 配信方式が **dev サーバ → `vite preview`（ビルド済み `dist`）** に変わり、**同節の「実装メモ」が扱う対象そのものが変わる** | **★射程が広がる。設計卓の手番** |
| **§5.5** | テスト最低ライン（レイヤー別方針。「フロント: ページは原則テストしない。E2E で補完」） | **無効化しない** | **無効化しない** | **★広がらない。** ただし **§13 条件 5 の宿題（「E2E は直列で走る」を明文化するか）は未決のまま残る**。段 3 で足すなら **「フロントはビルド済み `dist` を配る」** も同じ手番になる |

**⇒ `CHANGE-135` の射程は §4.5 へ広がる。§5.5 は広がらないが、明文化の要否は未決である。**

**★どちらも `SUPP-001` は製造が編集しない**（§6-2 受理済み・13-11）。**本節は判定の報告である。**


---

*以上、M24-09c 完了報告（追補まで）。*
