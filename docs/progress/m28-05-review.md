# M28-05 レビュー報告書

| 項目 | 内容 |
|------|------|
| 対象サブ | **M28-05**（terry `quick_burn_light` の `move_code` 是正） |
| 対象指示書 | `docs/instructions/M28-05-terry-quick-burn-code-fix.md` **v1.0.0** |
| チェックリスト | `docs/instructions/reviews/M28-05-review-checklist.md` **v1.0.0** |
| 完了報告 | `docs/progress/M28-05-completion-report.md` |
| レビュー日 | 2026-09-07 |
| 着手基点 | `233f48e` ／ レビュー時点の HEAD＝`5fece40`（`84da594` 実装 ＋ `5fece40` 完了報告） |
| 判定 | **重大 0 件**（チェックリスト §6 の 8 項目はいずれも該当なし）。**高 2 件 / 中 3 件 / 低 3 件** |

---

## 総評

**チェックリスト §6 の重大 8 項目は 1 件も該当しない。** 動く golden 4 本はすべて再生成されており（`migrations/` 全数を走査して「`quick_burn` を含む golden は 4 本しか無い」ことをレビュー側でも確認した）、`UPDATE` は `character_id` ＋ `code` の対で絞られ `NOT EXISTS` ガードを持ち、ファイル名は `000106_data_*` で層 B へ落ちている。CSV 正本も直っている。

**本サブ固有の危険（`-check` は `000026` しか守らない）を、製造は実測で正面から潰している。** 破壊確認 2（`000035` の索引だけを戻す）で `go test` が赤・`-check` が **`OK` / exit 0** を返すことを対照として記録しており、`CHANGE-163` §1 の主張を最初に裏付けた記録として質が高い。指示書が求めていない 4 件目（自テストの空振り確認）も自発的に実施している。

**残る指摘は、コードではなく「記録が事実とずれている／請求が届く場所に無い」ことに集中している。** とくに (a) 完了報告 §13-7 が `progress-log` 追記を **○** と書いているが実測では `check-progress-log-index.sh` が **NG**、(b) 設計卓への請求 3 件が `docs/progress/` にしか無い（設計卓は同ディレクトリを読まない＝2026-08-11 開発者裁定①）——の 2 点は、いずれも「テストでは検出できず、人が読む以外に見つける経路が無い」型である。

レビュー側で再実行した検証は末尾「本レビューで実際に回したもの」に列挙した。**`go test ./...` は 58 ok / 0 FAIL / 9 no test files で完了報告の記載と一致した。**

---

## 設計準拠性レビュー結果

### 束 A — 母数（チェックリスト §1）

| # | 観点 | 評価 | 所見 |
|---|---|---|---|
| A-1 | `M28-04` の実測と一致しているか／不一致なら止めて報告したか | **◎** | **レビュー側で着手基点 `233f48e` に対して独立に測り直し、完了報告の表と完全に一致した**（`git grep -c "quick_burn_light" 233f48e` ＝ `character_data` 2 ／ `migrations` 10 ／ `internal` 0 ／ `web` 0 ／ `scripts` 0 ／ **`docs/design` 0** ／ `docs` 45）。`docs` の 16 → 45 の差異で**止めて Plan Mode で報告し、開発者が続行を裁定**しており、指示書 §2.1 の「一致しなければ止めて報告」を満たしている。29 件の内訳（`M28-04` 完了後に書かれた文書＋本指示書・本チェックリスト自身）も、レビュー側の走査結果と矛盾しない |
| A-2 | 改名先 `quick_burn` の空き | **◎** | 着手基点で `^terry,quick_burn,` は 0 件。`UNIQUE (character_id, code)` の衝突・順序制約とも無し。完了報告 §1.2 の記載どおり |
| A-3 | terry 以外の同型の報告 | **◎** | **レビュー側で `_light` 孤児走査を独立に再実行し、完了報告 §10 と完全一致した**（`_light` 総数 **160** ／ 孤児は `ingrid,sun_flare` の **1 件のみ**）。`sun_flare_light` は 2026-09-06 に開発者が「正しい」と確認済みであり是正対象でない。**直さず報告のみ**（`D-161`）で正しい。§10 が「目安が 2 件当たったことは目安が正しい証明ではない」と留保を添えている点も良い |

**★低-1（伝達候補）**: 指示書 §2.1 の括弧書き「`character_data/` **2 件**（`terry.csv` の `quick_burn_light` と `quick_burn_od` の対）」は**内訳が実物と違う**。実物は `terry.csv` 1 件 ＋ `character_data/command-correction-history.md` 1 件である（`quick_burn_od` は `quick_burn_light` を含まないので走査に当たらない）。**数字は 2 で一致するため母数照合は通るが、括弧書きだけを写すと `command-correction-history.md` の存在を見落とす。** 製造は両方を直しており実害は出ていないが、この食い違いは報告に書かれていない。

### 束 B — 是正（チェックリスト §2）

| # | 観点 | 評価 | 所見 |
|---|---|---|---|
| B-1 | `character_data/terry.csv` が直っているか | **◎** | L32 が `terry,quick_burn,special,クイックバーン,…` へ。**24 列の列契約も維持**（レビュー側で `awk -F, NF` を確認。ヘッダ 24 ／ L31〜L34 とも 24）。`CHANGE-159` の 24 列目（`last_changed_game_version`）は空のままで、seed 波の生成物にマーカーを入れない規約とも整合 |
| B-2 | DML 1 本であること・DDL が無いこと | **◎** | `000106` は `UPDATE` 1 文（down も 1 文）のみ。DDL なし |
| B-3 | `WHERE` が `character_id` ＋ `code` か | **◎** | `character_id IN (SELECT … c.code='terry' AND game_id …)` ＋ `AND code = 'quick_burn_light'`。`code` 単独ではない。**先例 `000063` 節 A と同型** |
| B-4 | `NOT EXISTS` ガード | **◎** | up / down とも `NOT EXISTS (SELECT 1 FROM moves m2 WHERE m2.character_id = moves.character_id AND m2.code = '<改名先>')`。**新規 DB（golden 由来で既に `quick_burn` を持つ）で `UNIQUE` 衝突を起こさない**という `SUPP-001` §5.5.4 (8) の要求を満たす。ガードの意味（「改名先が埋まっている＝済んでいる」）がコメントで現場に残っている |
| B-5 | ファイル名が `NNNNNN_data_*.sql` か | **◎** | `000106_data_correct_terry_quick_burn_code.{up,down}.sql`。**レビュー側で `bash scripts/check-migration-license.sh` を実走して「違反なし」を確認**。`REUSE.toml` の `migrations/*_data_*.sql` にグロブで当たるため同ファイルの編集は不要、という §3.1 の判断も正しい（`REUSE.toml` L82 を確認） |
| B-6 | 連番が `000106` で自採番していないか | **◎** | 指示書ヘッダの払い出しどおり。**レビュー側で `migrations/` の連番を走査し、`000106` が最大・重複なしを確認** |
| B-7 | down の中身と失われるものが報告に在るか | **◎** | §3.3 に 3 行の表。**失われるのは是正そのものだけ／利用者データは失われない**（すべて `moves.id` 参照）／**三点更新に固有の非対称**（新規 DB を v105 へ down した状態は、その DB の本来の v105 ではない）まで書けている。しかも `000063` が同じ非対称を持つ先例であることに触れている |
| B-8 | `quick_burn_od` を改名していないか | **◎** | CSV・golden 4 本とも `quick_burn_od` は不変。**さらにテストで `quick_burn_od` = 1 件を対照として固定**しており、巻き添え改名が起きたら赤になる |

**★中-1**: `character_data/command-correction-history.md` の追随に**注記が無い**。同ファイルは冒頭で「**M14-03b 投入前チェック・2026-07-13** の全件記録」と宣言しているスナップショット型の履歴であり、`move_code` セルを現行 code へ書き換えたことで、**「2026-07-13 の時点で既に `quick_burn` だった」と読める形になった**。開発者裁定（2026-09-07＝「改名して追随させる」）に基づく変更なので**撤回は不要**だが、当時の登録が `quick_burn_light` であった事実がこの表から消えている。**1 行の追随注記**（例: 「★`quick_burn` は 2026-09-07 の `M28-05` で `quick_burn_light` から改名。当時の登録は `quick_burn_light`」）を足すことを推奨する。

> **参考（指摘ではない）**: 誤りの発生源そのものは `docs/seed-data/official-data-edge-cases.md` §9.2 に「`quick_burn` → **`quick_burn_light`**、`round_wave` → **`round_wave_heavy`** へ改名（強度サフィックスを人手で付与）」として残っている。**同書は `docs/` の歴史記録であり、直さないのが正しい**（チェックリスト §0.3-2）。完了報告 §2.2 の補強根拠（「対応 dist code 欄は元から `quick_burn`」）より直接的な出所なので、記録として本レビューに残す。

**★低-2**: `SUPP-001` §5.5.4 (8) の「**転記キーを `move_code` 単独にしない。`name_ja` との対で確認する**」について。`moves` テーブルに JP 名の列は無く（`000001` のスキーマ。JP 名は `preset_aliases` の `official_ja_move` 側）、**SQL の `WHERE` へ `name_ja` を対で入れることは構造上できない**。転記レベルでは完了報告 §2.2 が「日本語名一致（`command-correction-history.md`）」で対を取っており、規約は満たしていると判断する。**より強く同定したい場合は `EXISTS (… preset_aliases … alias = 'クイックバーン')` を足す選択肢がある**が、先例 `000063` も採っておらず、必須とは考えない（採否は製造判断でよい）。

### 束 C — golden（チェックリスト §3）

| # | 観点 | 評価 | 所見 |
|---|---|---|---|
| C-1 | **4 本すべて再生成されているか** | **◎** | **レビュー側で「取りこぼした 5 本目が無いこと」を独立に確認した**——`grep -rln "quick_burn" migrations/` の結果は `000026` / `000035` / `000072` / `000073` の up・down と `000106` の 10 ファイルのみであり、**terry の code を列挙する他の golden（派生 backfill・`is_projectile` 列挙等）に `quick_burn` は 1 件も無い**。⇒ 動く golden は 4 本で全数である |
| C-2 | 動いた理由が報告に書かれてから更新されているか | **◎** | §5 の表に 4 本それぞれの「何が載っているか／動く理由／固定テスト」があり、**引用している行番号はすべて実物と一致した**（`000026` up L42・L140 ／ down L57・L59、`000035` up L19 ／ down L46、`000072` up L1211 ／ down L18、`000073` up L1223 ／ down L18）。`000035` が動く理由（`is_derived=0` かつ `command` 非空で索引に載る）も正しい |
| C-3 | 4 本の golden テストが緑か | **◎** | レビュー側で `go test ./internal/seedgen/ -run TestGolden_ -v` を実走し、**14 本すべて PASS**。対象 4 本（`CommittedMigration` / `MoveCommands` / `M2002NumericAliases` / `M2002SRKAliases`）を目視で確認した |
| C-4 | 差分の健全性（`E-225`） | **◎** | `git diff 233f48e` を全数読んだ。**変更 10 ファイルの deletions 12 行はすべて `quick_burn_light` → `quick_burn` の 1 対 1 置換**であり、行の増減・並び替え・他キャラへの波及は無い。**新規 3 ファイル（テスト 182 / up 46 / down 25）と完了報告（376）は `+` のみで deletions を持たない**（`git diff --stat 233f48e` で確認）。⇒ 「新規のつもりで既存を上書きした」形の事故は無い |

### §4 — 破壊確認

| # | 観点 | 評価 | 所見 |
|---|---|---|---|
| D-1 | CSV を戻して `-check` が `DIFF` / exit 1 | **○** | §6-1 に実走記録。**出力文面がリポジトリの実物と一致する**ことをレビュー側で照合した（`cmd/seedgen/main.go:87` の「生成物と既存ファイルに差分あり。★… は適用済みマイグレであり再生成してはならない」）。復元後 `-check` = `OK` / exit 0 も記録あり |
| D-2 | **`000035` の索引だけを戻して `TestGolden_MoveCommandsMatchesRegeneration` が赤**（本サブ固有） | **◎** | §6-2 に実走記録。**`generate_m1702_test.go:165` が実際に `assertGolden(t, "000035_seed_move_commands", …)` の行であることを照合した。**さらに**同じ壊れた状態で引数なし `-check` が `OK` / exit 0 を返すことを対照として実測**しており、`CHANGE-163` §1 ／ `SUPP-001` §5.5.4 (10) の主張に**リポジトリ初の実測証拠**を与えている。本サブで最も価値の高い成果 |
| D-3 | ファイル名から `data_` を外して `check-migration-license.sh` が赤 | **○** | §6-3 に実走記録。**スクリプト側に「★静かな漏れ:」の文面が実在する**ことを照合（`scripts/check-migration-license.sh:236`）。up / down の 2 件で赤、復元後 exit 0 |
| D-4 | （追加）新設テスト自身の空振り確認 | **◎** | 指示書は求めていないが、down を当たらない値へ書き換えて `DownUpRoundTrip` / `LinksSurvive` の 2 本が赤になることを確認済み。**`V106State` が緑のままである理由（v106 の最終状態しか主張していない）まで説明している**のが良い |

**★1 だけで済ませていない**（チェックリスト §4 の最重要点）。**3 件とも実走され、うち 2 が本サブ固有である。**

> **注**: レビュー担当は指示により**破壊確認の再現（一時的な書き換え）を行っていない**。上記の判定は、記録の内部整合と、記録が引用する行番号・エラー文面が実物と一致することの照合による。**照合したものはすべて一致した。**

### §5 — テストの妥当性

| # | 観点 | 評価 | 所見 |
|---|---|---|---|
| E-1 | `go test ./...` が緑（golden 4 本を含む） | **◎** | **レビュー側で実走。58 ok / 0 FAIL / 9 no test files** で完了報告 §7-1 の記載と一致。`TestRun_M2805_*` 3 本も個別に実走して PASS |
| E-2 | テスト設計が `SUPP-001` §5.5.4 (6) に適合 | **◎** | **件数で固定していない。**「v106 の最終状態」＋「down → re-up の往復」＋「`move_id` の同一性」で固定しており、規約 (6) の要求どおり。とくに `LinksSurvive` が **`move_commands` / `preset_aliases` の件数だけでなく `move.id` の同一性**を見ている点は、「別の行に付け替わった」形の事故を捕まえる正しい設計。区間終端を `const m2805Terminus = 106` に閉じて HEAD 終端を避けている点も規約どおり |
| E-3 | `cd web && pnpm test` ／ `make e2e`（リポジトリルート） | **○（未再現）** | 完了報告 §7 に「212 files / 2454 tests 緑」「`make e2e` をリポジトリルートで実行し 247 passed」と実測が記載されている。**レビュー側では再実行していない**（後述「制約事項」）。ただし **`web/src` / `web/e2e` に `quick_burn` の参照は 0 件**であることを確認しており、FE 側へ波及する経路は無い |
| E-4 | **「常設検査は緑だった」と書いた時点が完了時点か** | **×** | **本サブで唯一の × である。**詳細は「高-1」。§7.1 が 3 検査を「本報告を書いた後に測る（→ §11）」としているが、**§11 は「確かめられなかったこと・解釈したこと」であり測定値は 1 つも無い**。⇒ 参照先が空で、`M28-04` 教訓 4 の要求（完了時点の値を残す）が実質的に未達 |

---

## 設計準拠性以外の指摘事項

### ★高-1（記録が事実とずれている）— `progress-log` 未追記のまま完了条件が **○** になっている

- **実測**: `bash scripts/check-progress-log-index.sh` → **`NG 作業 ID m28-05 が docs/progress/progress-log.md に現れない(完了報告: docs/progress/M28-05-completion-report.md)` / 違反 1 件**。
- にもかかわらず完了報告 §13-7 は「`docs/progress/progress-log.md` へ追記されている ＝ **○**（Phase D）」と書いている。**Phase D はレビュー（Phase B/C）の後であり、レビュー時点では未実施である。**
- **同型の前例が本プロジェクトに在る**——`M28-02b` レビュー 高-1「**変更統計を手書きしたために、やっていないことが『やった』として記録に残りかけた**」（`progress-log` 参照）。**今回は統計ではなく完了条件表で同じ形が出ている。**
- **是正**: §13-7 を **「未（Phase D で追記）」** に改めるか、Phase D の追記を済ませてから **○** にすること。**あわせて §7.1 の 3 行（`check-progress-log-index` / `check-md-emphasis` / `check-doc-inventory`）に実測値を書き、参照先を §11 以外の実在する節にすること。**
- **参考（レビュー時点の実測値）**: `check-progress-log-index.sh` = **NG 1 件**（上記）／ `check-md-emphasis.sh` = **495 行 / ベースライン 436（NG）。ただし `--list` を走査した結果、M28-05 の成果物は 1 行も含まれていない＝本サブ寄与 0**（既知の `followup` `check-md-emphasis-baseline-stale`）／ `check-doc-inventory.sh` = **型に無いファイルなし（違反なし）**。

### ★高-2（請求が届く場所に無い）— 設計卓宛の 3 件が `docs/progress/` にしかない

完了報告 §8 は設計卓の手番として 2 件を挙げている（**§8-2** マイグレ連番を `000107` へ ／ **§8-3** `parallel-board.md` §2.2 が **`次は 000092`** のままで 14 本ぶん古い）。**レビュー側でボードを実査し、記載が `000092` であること・`migrations/` の実測末尾が `000106` であることを確認した。§8-3 の指摘は事実である。**

問題は宛先である。**設計卓は `docs/progress/` を読まない**（2026-08-11 開発者裁定①）。**`M28-02b` は同じ形で最重要の CHANGE 請求を落としかけ、`progress-log` に「『請求を書いた』と『請求が届く場所に置いた』は別である」「`implement_plan_full` の Phase 構成には設計伝達レポートの生成が入っていないため、一気通貫で回すと落ちる」と申し送っている。** 本サブの完了報告にはその手当（`docs/handover/design-reports/` への配置、または Phase D の `progress-log` 横断課題への転記）への言及が無い。

- **とくに §8-3 は放置すると実害が出る**——ボードが 14 本ぶん古いまま次のサブへ連番を払い出すと、**既存ファイルと衝突する番号が配られうる**（`SUPP-001` §2.7「危険なのは穴ではなく後から若い番号が現れること」）。
- **是正**: Phase D の `progress-log` 索引行の**横断課題**へ 3 件（`000107` の払い出し ／ ボード §2.2 の取り直し ／ 下記 中-2 の `followup` 行の閉じ）を書くこと。**必要なら `/design_handover_report` で設計伝達レポートを起こす。**

### ★中-2 — `followup` 行を閉じる請求が §8 に無い

`docs/handover/followup-backlog.md` §BS の **`terry-quick-burn-light-move-code-error`** は状態が **「起票済み・投入可」** のままである。**§J 以外を製造が編集できない（`D-382`）ため、行を書き換えていないこと自体は正しい。** しかし完了報告 §8「併せて更新が要るもの」に**この行を閉じる請求が 1 行も無い**。§8 は CHANGE 登録・連番・ボード・版数・`docs/design/` の 5 行を持つが、**本サブの起票元そのものが抜けている。**

### ★中-3 — §3.2 の実測記述が事実と違う（旧 code の残存箇所）

完了報告 §3.2 末尾:

> **実測: リポジトリ全体の `quick_burn_light` 残存は `000106` の up 4 行 / down 4 行のみ**（コメントを含む）。

**誤りである。** レビュー側の走査では、**新設テスト `internal/infra/migration/migrate_m2805_test.go` に 2 件**（L10 のコメント ／ **L32 の `m2805OldCode = "quick_burn_light"`**）が在り、加えて `docs/` に 45 件（§1 で自認済み）ある。⇒ **§1 の母数表が「`internal/` 0 件」としている区分は、本サブの完了時点で 0 → 2 に変わっている。**

- **実装側の 2 件はいずれも必要なもの**（`SUPP-001` §5.5.4 (8)「旧 code の文字列は実装経路から完全には消せない」）**であり、コードの是正は不要である。** 直すべきは記述のほうである。
- **なぜ「高」ではなく「中」か**: 誤りが `docs/progress/` の完了報告に閉じており、コード側のコメント・DTO・i18n には伝播していないため。ただし**後任がこの 1 行を根拠に「実装側に旧 code は無い」と読むと、次の走査結果と食い違う。**

### ★低-3 — 完了報告 §2.1 の `git diff --stat` が自身のコミット前の値

§2.1 は 13 files / 265 insertions を貼っているが、完了報告自身がコミットされた現在は **14 files / 641 insertions** である。**`E-225`（新規ファイルに deletions が付いていないか）の目的は満たしており実害は無い**——完了報告も `+376` のみで deletions は 0 であることをレビュー側で確認した。追記するなら 1 行の注記で足りる。

### ★低-4（情報提供）— `DES-004` §2.1 の命名パターン表に「強度の区別が無い必殺技」の形が無い

§2.1 の命名パターン表は必殺技を **`<技名>_<強度>`** と規定しており、`sonic_break` / `quick_burn` のような**強度サフィックスを持たない必殺技の形は表に書かれていない**。間接的な根拠は変種サフィックス表の `_od` 行の実例が **`sonic_break_od`**（＝強度なしの base が在ることを含意）である点のみ。**本サブが `CHANGE` を起こす必要は無い**（`docs/design/` に `quick_burn` / `quick_burn_light` の名指しは実査で 0 件。レビュー側でも再確認した）が、**同型の是正が 3 例目に達したときに設計卓が §2.1 へ 1 行足せる材料**として記録する。完了報告 §11-1 が「`DES-004` §2.1 からの導出」と書いている箇所は、厳密には**表そのものではなく先例と `_od` 実例からの類推**である。

### ★低-5（情報提供）— 派生資料の陳腐化

`bash scripts/check-derived-docs.sh`（常に exit 0 の情報提供型）は `code-facts.md`（**106 / 1161 件＝9%**）／ `docs-map.md`（79 / 1378 件＝6%）／ `custom-commands.md`（7 / 34 件＝21%）を「陳腐化疑い」と出す。**本サブ着手前からの状態であり、本サブの責任ではない**（本サブが増やしたのは新設テスト 1 本ぶん）。CLAUDE.md §8 の注記どおり、**「源泉が新しい」は資料が丸ごと無効という意味ではない。**

---

## 推奨修正（優先度別）

### 高（M28 完了前に修正必須）

1. **完了報告 §13-7 と §7.1 を事実に合わせる。**（高-1）
   - Phase D で `docs/progress/progress-log.md` へ索引行を追記し、`bash scripts/check-progress-log-index.sh` が緑になることを確認してから §13-7 を **○** にする（未実施のままなら **「未（Phase D）」** と書く）。
   - §7.1 の 3 行（`check-progress-log-index` / `check-md-emphasis` / `check-doc-inventory`）へ**完了時点の実測値**を書く。`check-md-emphasis` は **495 / 436 で赤だが本サブ寄与 0 行**（`--list` に M28-05 の行が 1 件も無い）という内訳まで書くこと——`M28-04` 教訓 4 が求めているのはこの形である。**参照先「→ §11」は §11 に測定値が無いので、実在する節へ差し替えること。**
2. **設計卓宛の請求を、届く場所へ置く。**（高-2）
   - Phase D の `progress-log` 横断課題（必要なら `/design_handover_report`）へ **(a) マイグレ連番の次は `000107`**、**(b) `parallel-board.md` §2.2 が `次は 000092` のままで 14 本ぶん古い（実測末尾 `000106`）**、**(c) `followup-backlog.md` §BS の `terry-quick-burn-light-move-code-error` を閉じてよい**——の 3 件を書く。
   - **とくに (b) は、放置すると次のサブへ既存ファイルと衝突する連番が配られうる。**

### 中（次サブ着手と並行可）

3. **完了報告 §3.2 の「残存は `000106` の up 4 行 / down 4 行のみ」を訂正する。**（中-3）実際は **＋ `internal/infra/migration/migrate_m2805_test.go` の 2 件（L10 コメント / L32 定数）**。コードは直さない（規約 (8) どおり必要な残存である）。
4. **`character_data/command-correction-history.md` へ追随注記を 1 行足す。**（中-1）例:「★`quick_burn` は 2026-09-07 の `M28-05` で `quick_burn_light` から改名（当時の登録は `quick_burn_light`）」。**現状は 2026-07-13 のスナップショットが、当時と違う値で静かに書き換わった状態である。**
5. **`followup-backlog.md` §BS の行を閉じる請求を完了報告 §8 に足す。**（中-2。§J 以外を製造が編集しないのは正しい）

### 低（将来対応）

6. 指示書 §2.1 の括弧書き（`character_data/` 2 件の内訳）が実物と違うことを、設計卓へ伝達候補として 1 行残す。（低-1）
7. `SUPP-001` §5.5.4 (8) の「`name_ja` との対」を SQL 側でも取りたい場合の選択肢（`preset_aliases` の `EXISTS`）を、次に同型の改名を行うサブへの申し送りとして残す。**採否は製造判断でよい**（先例 `000063` も採っていない）。（低-2）
8. `DES-004` §2.1 に「強度の区別が無い必殺技」の形が無い件を、設計卓への情報提供として残す。（低-4）
9. 完了報告 §2.1 の `git diff --stat` が自身のコミット前の値である旨を 1 行注記。（低-3）

---

## 良かった点

1. **★破壊確認 2 が、本サブ固有の穴に初めて実測の証拠を付けた。** `000035` を壊した状態で **`go test` は赤・引数なし `-check` は `OK` / exit 0** という対照を並べて記録している。`CHANGE-163` §1 は「素直に実行すると偽の証拠が作れる」と警告した規約であり、**本サブはその最初の適用先として、警告の内容を再現して見せた。** この記録は次に同型を扱うサブがそのまま引用できる。
2. **★指示書が求めていない 4 件目の破壊確認を自発的に実施した。** しかも `V106State` だけが緑のまま残る理由まで説明しており、「赤にならなかった＝テストが弱い」と即断していない（`SUPP-001` §5.5.4 (10′) の姿勢）。
3. **★golden を上書きする前に、4 本それぞれの「動く理由」を特定して表にした。** 引用行番号がすべて実物と一致しており、**後任が「なぜこの 4 本なのか」を再調査せずに済む。**
4. **★テストが件数ではなく状態・往復・`move_id` の同一性で固定されている。** とくに `LinksSurvive` の「`move_commands` / `preset_aliases` の件数だけでなく `move.id` が同一であること」は、指示書も設計書も明示していない観点であり、**「別の行に付け替わった」形の事故を捕まえる。**
5. **★マイグレのコメントが、読み手の疑問に先回りしている。** 「なぜ新規 DB では 0 行に当たるのか」「なぜガードが要るのか」「down で何が失われるのか」「三点更新に固有の非対称」が**すべて現場（SQL ファイル）に在る。** 完了報告を読まないと分からない状態になっていない。
6. **★母数の差異（`docs` 16 → 45）で止めて開発者裁定を仰ぎ、29 件の内訳を全数示した。** さらに「母数を `docs/` の総件数で置くと、その母数を出した報告自身が母数を押し上げる」という**一般形の教訓**まで抽出している。
7. **★`E-225` の確認を明示的に行っている**（新規ファイルに deletions が付いていないこと・Write の前に `ls` で同名の不在を確認）。レビュー側の `git diff --stat` でも裏が取れた。
8. **★同型走査（`_light` 孤児）の結論に留保を添えた。** 「terry と ingrid の 2 件で目安が当たったことは、目安が正しいことの証明ではない」——`D-161`（全キャラ点検はしない）の趣旨を正しく守りつつ、断定に化けさせていない。

---

## 本レビューで実際に回したもの（読み取り系のみ）

| # | コマンド | 結果 |
|---|---|---|
| 1 | `git diff 233f48e` ／ `git diff --stat 233f48e` ／ `git log --oneline 233f48e..HEAD` | 全数読了。deletions 12 行はすべて 1 対 1 置換／新規 4 ファイルは `+` のみ |
| 2 | `git grep -c "quick_burn_light" 233f48e -- <各ディレクトリ>` | 母数を独立に再測。完了報告の表と完全一致 |
| 3 | `grep -rln "quick_burn" migrations/` | **動く golden が 4 本で全数であること**を確認（5 本目は無い） |
| 4 | `go test ./...` | **58 ok / 0 FAIL / 9 no test files**（完了報告と一致） |
| 5 | `go test ./internal/seedgen/ -run TestGolden_ -v` | 14 本 PASS（対象 4 本を目視確認） |
| 6 | `go test ./internal/infra/migration/ -run TestRun_M2805 -v` | 3 本 PASS |
| 7 | `go run ./cmd/seedgen -check` | `OK: 生成物は既存ファイルと一致` / exit 0 |
| 8 | `bash scripts/check-migration-license.sh` | 違反なし |
| 9 | `bash scripts/check-artifact-integrity.sh` | 違反なし（自己検査 14 件 ／ 生成物 4 件とも OK） |
| 10 | `bash scripts/check-progress-log-index.sh` | **NG 1 件**（`m28-05` が `progress-log` に無い） |
| 11 | `bash scripts/check-md-emphasis.sh` ／ `--list` | 495 / 436 で NG。**M28-05 の行は 0 件（本サブ寄与 0）** |
| 12 | `bash scripts/check-doc-inventory.sh` ／ `check-stop-discipline.sh` ／ `check-public-snapshot.sh` | いずれも違反なし |
| 13 | `bash scripts/check-derived-docs.sh` | 情報提供。3 資料が陳腐化疑い（本サブ着手前から） |
| 14 | `_light` 孤児の独立走査（`awk` ／ `comm`） | **160 件中 孤児 1 件＝`ingrid,sun_flare`**（完了報告 §10 と一致） |
| 15 | `awk -F, '{print NF}' character_data/terry.csv` | ヘッダ・該当行とも **24 列**（`CHANGE-159` の列契約を維持） |
| 16 | 記録の照合（`cmd/seedgen/main.go:87` ／ `internal/seedgen/generate_m1702_test.go:165` ／ `scripts/check-migration-license.sh:236`） | 完了報告 §6 が引用する行番号・文面はすべて実物と一致 |

---

## 制約事項

- 本レビューはコード上で判定可能な範囲のみ対象。実際の動作確認・パフォーマンス・実機テストは別途実施が必要。
- **破壊確認（`§6-1`〜`§6-4`）はレビュー側で再現していない。** 指示により一時的な書き換えを行わないため、判定は**記録の内部整合と、記録が引用する行番号・エラー文面が実物と一致することの照合**による（照合結果はすべて一致）。
- **`cd web && pnpm test` と `make e2e` はレビュー側で再実行していない。** 完了報告 §7 の実測記載（212 files / 2454 tests、247 passed）を採った。**`web/src` / `web/e2e` に `quick_burn` の参照が 0 件であることは確認済み**であり、FE へ波及する経路は無い。
- **開発者の手元 DB（`quick_burn_light` を持つ既存 DB）での `000106` の実適用は確認できない。** 完了報告 §11-3 が同じ留保を書いており、`down` → `re-up` の往復が等価な検証であるという説明は妥当と判断した。
- **不明**: 完了報告 §1.1 の「開発者裁定 2026-09-07＝続行」および §2.2 の「開発者裁定 2026-09-07＝改名して追随させる」は、リポジトリ内に記録が無く（`parallel-board.md` / `followup-backlog.md` / `progress-log.md` に該当記述なし）、**レビュー側では裁定の存在そのものを検証できない。** 完了報告の記載を前提として判定した。

---

*以上、M28-05 レビュー報告書。* **★重大は 0 件であり、束 A〜C ／ §4 破壊確認はいずれも要求を満たしている。** **残る「高」2 件はどちらもコードではなく記録の問題である**——**完了条件が未実施のまま ○ になっていること**と、**設計卓宛の請求が設計卓の読まないディレクトリにしか無いこと**。**どちらもテスト・lint・型検査では緑のままであり、人が読む以外に見つかる経路が無い。**

---

## 取り込み結果（自動トリアージ）

**実施**: 2026-09-07 / `implement_plan_full` Phase C（**判断は自動。人間トリアージ承認は外し、採否理由の事後監査可能化で補償する**）。
**再レビュー往復**: **0 回**（初回レビューのみ。上限 2 回に未達）。
**★「高」指摘の不採用は 0 件**——エスカレーションは発生していない。

| # | 優先度 | 指摘（要約） | 採否 | 理由・対応 |
|---|---|---|---|---|
| 高-1 | 高 | `progress-log` 未追記のまま完了報告 §13-7 が **○** ／ §7.1 の 3 検査が「→ §11」を指すが §11 に測定値が無い | **採用** | **正しい指摘。`D-510` と同型（観測していないことの断定）である。** Phase D で `progress-log.md` へ索引行を追記し、`bash scripts/check-progress-log-index.sh` が **exit 0・違反なし**であることを確認してから §13-7 を実測付きの ○ にした。§7.1 の 3 行へ完了時点の実測値を記入（`check-progress-log-index` = 緑 ／ `check-md-emphasis` = **495 / 436 で赤だが本サブ寄与 0 行** ／ `check-doc-inventory` = 型に無いファイルなし）。壊れた参照「→ §11」は削除 |
| 高-2 | 高 | 設計卓宛の請求が `docs/progress/` にしかなく届かない。とくにボード §2.2 が 14 本ぶん古い | **採用** | **正しい指摘（`M28-02b` 横断課題 1 と同型）。** `progress-log.md` の索引行 **★横断課題 2** へ 3 件を明記した——**(a)** 次に払い出す連番は `000107`、**(b)** **`parallel-board.md` §2.2 が `000092` のままで実測末尾 `000106` と 14 本ぶんずれている（放置すると衝突する連番が配られうる）**、**(c)** `followup-backlog.md` §BS の `terry-quick-burn-light-move-code-error` は閉じてよい。あわせて完了報告に **§8.1「請求の届け先」** を新設。**★製造は `parallel-board.md` / §J 以外の `followup-backlog.md` を編集できない**（`D-382`）ため、実際に畳むのは設計卓の手番である |
| 中-3 | 中 | 完了報告 §3.2 の「旧 code 残存は `000106` の up 4 / down 4 のみ」が誤り | **採用** | **実測で確認した——`grep -rn "quick_burn_light"`（`docs/` 除く）は 10 件**（`000106` up 4 / down 4 ＋ `migrate_m2805_test.go` の L10 コメント・L32 定数）。§3.2 を訂正し、**`internal/` の母数が 0 → 2 になったこと**と、**改名を固定するテストは旧 code を名指しせざるを得ない**ことを明記。**コードは直していない**（規約 (8) が言う必要な残存である） |
| 中-1 | 中 | `command-correction-history.md` に追随注記が無く、2026-07-13 のスナップショットが静かに書き換わった状態 | **採用** | **正しい指摘。** terry 表の直下へ注記 3 行を追加（改名日 2026-09-07・作業 ID `M28-05`・**当時の登録は `quick_burn_light` だった**・一次源は開発者のインゲーム確認）。**★`move_code` セルを改名して追随させること自体は 2026-09-07 の開発者裁定であり、注記はそれと両立する**（値は現行に追随させ、履歴は注記で保つ） |
| 中-2 | 中 | `followup-backlog.md` §BS の起票元行を閉じる請求が §8 に無い | **採用** | §8 の表へ 6 行目として追加。**行を製造が編集しないこと自体は `D-382` どおり正しい**とレビュアーも認めている |
| 低-1 | 低 | 指示書 §2.1 の括弧書き（`character_data/` 2 件の内訳）が実物と違う | **採用** | §8.2 の伝達候補 1 として記載。**件数 2 は合っているが内訳が違う**（実際は `terry.csv` L32 と `command-correction-history.md` L15。`quick_burn_od` は `quick_burn_light` を含まない） |
| 低-4 | 低 | `DES-004` §2.1 に「強度の区別が無い必殺技＝`<技名>` 単体」の形が無い | **採用** | §8.2 の伝達候補 2 として記載。**本型の誤りは `sonic_break` に続いて 2 例目である** |
| 低-2 | 低 | `SUPP-001` §5.5.4 (8) の「`name_ja` との対」を SQL 側でも取る選択肢 | **採用（申し送りとして）** ／ **SQL への追加は不採用** | **指摘は §8.3 へ申し送りとして記載した。SQL を変えなかった理由を同節に明記**——(i) **先例 `000063` 節 A も同じ規約の下で採っていない**、(ii) `moves` に `name_ja` 列が無く `preset_aliases` / `official_ja_move` 経由の `EXISTS` になり読みにくくなる、(iii) **`character_id` ＋ `code` の対 ＋ `NOT EXISTS` ガード ＋ 往復テストで「当たったこと」を三重に固定しており、規約の趣旨（別の技を静かに上書きしない）は満たされている**。**★レビュアー自身が「採否は製造判断でよい」と明記している** |
| 低-3 | 低 | §2.1 の `git diff --stat` が自コミット前の値である旨の注記 | **採用** | §2.1 へ 1 行追加（コミット `84da594` 時点の値であり、本報告書・Phase D の追記・Phase C の取り込み分を含まない） |

**★総括: 指摘 9 件のうち 8 件が「記録」に対するものであり、コードへの指摘は 0 件だった。** 中-3 も記述の訂正であってコードは変えていない。**⇒ 本サブで壊れやすかったのは実装ではなく、「やったこと」と「書いたこと」の一致である。** とくに **高-1 は `D-510` とまったく同型**（観測していないことを断定した）であり、**同じ工程で 2 度目**である。
