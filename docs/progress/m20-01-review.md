# M20-01 レビュー報告書

| 項目 | 内容 |
|------|------|
| 対象 | `docs/instructions/M20-01-initial-presets-three.md` v1.0.0 ／ `docs/instructions/reviews/M20-01-review-checklist.md` v1.0.0 |
| 対象コミット | `b14828f` / `6f63421` / `f97191e`（差分基準 `eb472b4..HEAD`） |
| レビュー日 | 2026-08-12 |
| 前提 | **設計卓の追加裁定 A-1〜A-4（2026-08-12・リポジトリ未反映）を指示書 v1.0.0 本文に優先して適用した。** |
| 実行した検証 | `go test ./...`（全緑）／ `pnpm exec tsc --noEmit`（exit 0）／ `bash scripts/check-progress-log-index.sh`（違反なし）／ `bash scripts/check-enum-sync.sh` ／ `name` リテラルのバイト単位突合（Python）／ `git diff` による適用済みマイグレ非改変確認 |

## 総評

**最重要ゲート 3 つはいずれも合格である。** `presets.id` は詰めておらず（欠番 2・4 をテストで明示的に固定）、`down` の `name` リテラルは `migrations/000005_seed_presets.up.sql` とバイト単位で一致し（括弧は全件半角 `0x28`/`0x29`、中黒 `・` の有無も一致）、`000005` / `000006` / `000011` は 1 バイトも変わっていない。
設計卓の追加裁定 4 件（A-1 の `srk` 付け替え ／ A-2 の専用ファイル分離 ／ A-3 の追随 7 件 ／ A-4 の godoc 是正）はすべて裁定どおり実装されており、**本レビューではこれらを問題として扱っていない**。A-4 の前提（「10 を強制する実装は無い」）はレビュー側でも独立に実査し、**正しいことを確認した**（後述）。
テスト設計の水準は高い。比較区間が自サブに閉じ、欠番の明示・母数の併記・orphan 検査まで対で固定してあり、コミットメッセージにはミューテーションで検査力を裏取りした記録まである。
一方で**「実装は正しいが記述が旧のまま残っている」型の残骸が設計書側に 4 か所ある**。うち 3 か所は設計卓の是正スコープ（§1.3）に入っておらず、このままでは誰の手番にも載らない。本プロジェクトの較正規則によりこれを「高」に置く。
また **完了報告・progress-log・model-allocation が未作成**であり、チェックリスト §0.1 が要求する「§3.3 実査 7 項目の結果」がリポジトリ上で確認できない（Phase C/D 未実施のため想定内だが、完了承認の前提として残っている）。

## 設計準拠性レビュー結果

### 0.2 最重要ゲート 3 つ

| # | 観点 | 評価 | 根拠 |
|---|------|------|------|
| 1 | `presets.id` を詰めていない | ◎ | `000069_*.up.sql` に `id` を書き換える `UPDATE` / `VACUUM` / テーブル再作成は無い。削除は `code` 条件、改称も `code` 条件で `id` に触れない。`migrate_m2001_test.go` U-3 が `id=1/3/5` の対応に加え **`id=2` / `id=4` が 0 行であること**を明示的に固定している。 |
| 2 | `down` が `000005` の投入値へ忠実に戻る | ◎ | 下表のとおり**バイト単位で一致**を確認した。`INSERT` は `id` を明示、6 列すべて列挙、`down` の条件は `code='numeric'`（反転になっていない）。 |
| 3 | `000005` / `000006` / `000011` 非改変 | ◎ | `git diff eb472b4..HEAD -- migrations/000005* migrations/000006* migrations/000011*` が空。`000006:9` / `000011:4` の「残り 4 プリセット」コメントも据え置かれている（N-2 のとおり正しい）。 |

**`name` リテラルの逐語突合（チェックリスト §1.2 が「1 文字ずつ」を要求している項目）**

| 対象 | `000005_seed_presets.up.sql` | `000069_*.down.sql` | 判定 |
|---|---|---|---|
| id=2 | `公式表記(日本語・コマンド表示)改善版` | 同左 | 一致（括弧 `0x28`/`0x29` ＝半角、中黒 `・` あり） |
| id=3 | `ナンバリング記法(日本語版)` | 同左 | 一致（括弧半角） |
| id=4 | `ナンバリング記法(英語版)` | 同左 | 一致（括弧半角） |

> 検証方法: 両ファイルからシングルクォート内文字列を機械抽出し、日本語（かな・漢字）以外の全コードポイントを 16 進で出力して比較した。目視ではなく機械比較である。

### §4.1 マイグレ up

| 観点 | 評価 | 備考 |
|---|---|---|
| 削除が `code` 条件（`id` 決め打ちでない） | ◎ | `DELETE FROM presets WHERE code = '...'` |
| 1 行 1 文（`IN (...)` でまとめていない） | ◎ | 2 文に分けてある |
| `numeric` への改称と `name = 'ナンバリング記法'` | ◎ | `UPDATE` 1 文。テスト U-5 が固定 |
| `is_builtin` / `base_preset_code` / `user_id` 不触 | ◎ | `SET` 句は `code` / `name` のみ。U-5 が id=3 の `is_builtin=1` / `base_preset_code IS NULL` も併せて固定 |
| サービス層の組み込みプリセット保護を緩めていない | ○ | サービス層の差分は 0。**ただし `internal/service/preset/` は現時点で `doc.go` のみで、SUPP-001 §7.3 の 403 保護はまだ実装されていない。** up.sql の「⇒ サービス層の保護は緩めていない」というコメントは、読者に「保護が実装済みである」と誤読させうる（→ 中の指摘 3） |

### §4.2 マイグレ down

◎。`id` 明示・6 列明示・逐語 `name`・`code='numeric'` 条件のすべてを満たす。
加えて `presets.id` は `INTEGER PRIMARY KEY AUTOINCREMENT` であるため、`id` を明示した `INSERT` は `sqlite_sequence` を動かさず（up 後も 5 のまま）、`000005` 直後の状態と `sqlite_sequence` まで含めて一致する。**復元後の `ORDER BY id` も 1〜5 の元順序に戻る**（`ListAllPresets` は `ORDER BY id`）。

### §4.3 `id` を詰めない

◎。§0.2 ゲート 1 のとおり。テストが「詰めていないこと」を能動的に検出する形になっている点が良い（`count(*) WHERE id IN (2,4) = 0`）。

### §4.4 / §4.5 定数

| 観点 | 評価 | 備考 |
|---|---|---|
| Go 定数が 3 種 | ◎ | `PresetCodeOfficialJaMove` / `PresetCodeNumeric` / `PresetCodeSRK`。既存の綴り慣習（`SRK` は大文字のまま）を維持しており、§4.4 の「命名規則を変える場ではない」に沿う |
| TS `BUILTIN_PRESET_CODES` が 3 キー | ◎ | `officialJaMove` / `numeric` / `srk` |
| Go 値・TS 値・`presets.code` の三者一致 | ◎ | `official_ja_move` / `numeric` / `srk` で一致 |
| `ComboDetailRecipe.tsx` 不変 | ◎ | 差分 0。参照は `BUILTIN_PRESET_CODES.officialJaMove` の 1 か所のみで、`tsc --noEmit` も exit 0 |
| `web/src/constants/` へ移設していない | ◎ | N-4 のとおり移設しないのが正しい |

### §4.6 既存テストの追随（設計卓裁定 A-1 適用後）

| # | 指示（A-1 適用後） | 実装 | 評価 |
|---|---|---|---|
| 1 | `numeric` へ書き換え | `TestRepository_FindAlias_NotFound` が `numeric` を引く | ◎ |
| 2・3 | **関数を存置し、対象を `srk` へ付け替える** | `TestRepository_ListAliasesByPreset_Empty` を存置し `srk` へ付け替え。理由（`numeric` だと #1 と主張が重複する／`srk` は重複しない）と、M20-02 で落ちるのが意図であることをコメントで明記 | ◎ |
| 4・5 | `numeric` へ書き換え | `cache_test.go` / `resolver_test.go`。変数名も `numericJaID` → `numericID` へ改めてある | ◎ |
| 6 | code だけ `numeric` へ。「0 件」は触らない | `migrate_test.go:934` が `code='numeric'` へ。0 件の主張は据え置き、削除もされていない（§9-5 の重大に該当しない） | ◎ |

### §4.6.2 追随 7 件（設計卓裁定 A-3）

◎。7 件を実査した結果、すべて「プリセット総数」を根拠にした期待値で、5 → 3 への更新が妥当である。

1. `internal/infra/migration/migrate_test.go:925`（`presets = 3`）
2. 同 `:926`（`builtin presets = 3`）
3. `internal/repository/preset/repository_test.go:42`（`ListAllPresets` = 3）
4. `internal/api/preset/handler_test.go:39`（API 応答 = 3）
5. `internal/service/notation/cache_test.go:94`（recipe_cache キー数 = 3）
6. 同 `:169`（Restore 後 = 3）
7. `internal/service/notation/setup_resolver_test.go:187`（setup 側 recipe_cache = 3）

**取りこぼしが無いことをレビュー側でも独立に確認した。** `internal/service/combo/service_test.go:248-253` は `SELECT count(*) FROM presets` で動的に数えており追随不要（製造の判断どおり）。ほかに固定値 5 でプリセット数を主張している箇所は Go / TS / E2E に存在しない。

### §4.7 マイグレ契約テスト（A-2 適用後）

| 観点 | 評価 | 備考 |
|---|---|---|
| 配置（A-2: 新規 `migrate_m2001_test.go`） | ◎ | 既存慣習（`migrate_m1904d_test.go` 等）と一致。`migrate_test.go` は §4.6 #6 と A-3 の期待値更新のみ |
| 比較区間が自サブに閉じている | ◎ | `m.Migrate(68)` → `m.Migrate(69)` → `m.Migrate(68)`。`m.Up()` 不使用 |
| HEAD スコープの主張を混ぜていない | ◎ | HEAD の主張は `migrate_test.go` の `TestRun_SeedRowCounts` に残したまま |
| U-1〜U-7 / DN-1〜DN-3 の 10 件 | ◎ | サブテスト 10 本として実在。U-2 は列挙＋列挙外 0 件の双方向、U-3 は欠番明示、U-6 は母数 3 を併記、U-7/DN-3 は行数不変＋orphan 0、DN-2 は 5 行 × 5 列（`code`/`name`/`is_builtin`/`base_preset_code`/`user_id`）を対で固定 |
| FK 挙動の固定（§3.3-7） | ◎ | `preset_aliases.preset_id` は `REFERENCES presets(id) ON DELETE CASCADE`（`000001_init_schema.up.sql:145`）。U-7 が「波及していないこと」を行数と orphan の両面で固定している |

### §1.3 スコープ外 6 項目

◎。エイリアス投入・一意制約・プリセット管理 UI・`recipe_cache` キー変更・`DefaultPresetID`・`config` の実在検査、いずれも差分 0。
`internal/config/config.go:225` と `internal/service/config/service.go:227` は `>= 1` の範囲検査のみで `presets` の実在を見ていない状態が維持されている（N-5 のとおり本サブでは正しい）。

### 設計卓裁定 A-4 の前提検証（「10 を強制する実装は無い」は正しいか）

**正しい。** レビュー側で独立に実査した結果は次のとおり。

- `internal/service/preset/` は `doc.go` のみ（サービス実装が存在しない）。
- `MaxPreset` 等の上限定数は Go / TS のいずれにも存在しない（`grep -niE "maxpreset|preset.*(limit|max|上限)"` で 0 件）。
- `DES-006` VAL-P05（プリセット総数上限 10 件）に対応する実装・テストは 0 件（`VAL-P05` の grep はコード側 0 ヒット）。
- したがって `internal/model/preset.go` の godoc を「8 件（組み込み 3 + カスタム 5）」へ直したことは、A-4 の「10 を強制している実装が無ければ直す」に合致する。**SUPP-001 §7.3 との一時的な食い違いは指摘しない**（設計卓が承認済みの想定内の状態）。

### テスト実行結果

- `go test ./...` — **全パッケージ緑**（失敗 0）。
- `pnpm exec tsc --noEmit` — **exit 0**（`numericJa` / `officialJaCommand` / `numericEn` への残存参照は無い）。
- `bash scripts/check-progress-log-index.sh` — 違反なし。
- `bash scripts/check-enum-sync.sh` — ベースラインから 2 件減少（→ 中の指摘 2）。

## 設計準拠性以外の指摘事項

### 1. 失効した設計書記述のうち、設計卓の是正スコープに入っていないものが 4 か所ある（高）

指示書 §1.3 が設計卓の手番として挙げているのは **`SUPP-001` §7.3** と **`DES-004` §3.1・§3.4・§4.2（CHANGE-098）** の 2 系統だけである。しかし本サブで失効する記述は、その外側にもある。

| # | 所在 | 記述 | 是正スコープ |
|---|---|---|---|
| a | `docs/design/06-validation.md:134` | **VAL-P05**「システム全体のプリセット総数（組み込み**5**件＋カスタム）が上限（**10**件）を超えていないか」 | **どの CHANGE にも入っていない** |
| b | `docs/design/06-validation.md:138` | 「組み込み**5**件＋カスタム5件の合計**10**件を上限とする」＋その根拠段落 | **同上** |
| c | `docs/design/04-notation-spec.md:405`（**§8**、§3.1/§3.4/§4.2 ではない） | 「組み込み**5**プリセットの全エイリアス表記」 | CHANGE-098 の対象節に**含まれていない** |
| d | `docs/design/03-data-model.md:889`（§6） | 「プリセットのエイリアス全件（組み込み**5**プリセット分）」 | **どの CHANGE にも入っていない** |
| e | `docs/design/supp-001-detailed-design.md:1291`（**§7.5.5**） | 「プリセット数上限 **10** 件の制約内、§7.3 の方針通り」 | §1.3 が挙げるのは **§7.3** のみ。§7.5.5 は明示されていない |

**製造が `DES` / `SUPP` を編集していないのは正しい**（§9-10 の重大に該当しない）。問題は「誰の手番にも載っていない」ことである。この状態で完了すると、`internal/model/preset.go` の godoc だけが「8 件」になり、設計書 5 か所が「10 件 / 組み込み 5」のまま残る。**次の担当は設計書をコピーするので、旧の数が M20-04（プリセット管理 UI）・M20-05 の前提として複製される**（本レビュー較正の 2 番目の理由に該当）。

**⇒ 完了報告の §4.9「見つけたが直さない」節に (a)〜(e) を所在つきで列挙し、設計卓へ「CHANGE-098 のスコープ外に 4 か所ある」と名指しで伝達すること。** 指示書 §4.9 の走査キーワード（`組み込み5` / `組み込み5+カスタム5`）で当たる場所であり、§4.9 (c) の免除（`DES-004` 本体と `SUPP-001`）は (a)(c)(d) を覆わない。

### 2. `docs/handover/architecture-patterns.md:616` の「残り 4 プリセット」が失効している（高）

当該行は歴史記録ではなく **「フェーズ3 の前提」** として書かれた前向き参照である。

> - **フェーズ3 の前提**: 残り 4 プリセットのエイリアス整備 / プリセット管理 UI 着手時に SUPP-001 §7 の再計算を実装する。

本サブ完了後の実体は **残り 2**（`numeric` / `srk`）である。同じ事実を持つ `docs/handover/followup-backlog.md:93` は 2026-08-12 に設計卓が「残り 4 → 残り 2」へ更新済みであり、**片側だけが直っている状態**になっている。指示書 §4.9 (b) の免除対象は「完了報告・review・change-notes・progress・**過去の** handover」であり、`architecture-patterns.md` は現役の横断参照資料（同ファイルは過去にもフェーズ番号再設定で前向き参照を追随させた実績がある＝更新履歴 v1.0.15）なので免除に当たらない。

**⇒ 是正するか、少なくとも完了報告に「失効を検出したが、資料の所管が設計卓／改善レーンのため直さない」と所在つきで書くこと。** 無記録のまま残すのが最も悪い。

### 3. `scripts/check-enum-sync.sh` のベースラインが実測とずれた（中）

`BASELINE_SCATTER=26` に対し実測 24。本サブが `PresetCodeNumericJa` / `PresetCodeNumericEn` の 2 定数を削除した結果であり、スクリプト自身が「BASELINE_SCATTER を 24 へ更新してよい」と出力している。
**更新しないと、次に別の列挙値が 2 件散在しても「ベースライン以内」で検知されない**（減った分が緩衝材になる）。本サブが減らした分なので、本サブで詰めるのが筋である。

### 4. up.sql のコメントが未実装の保護を実装済みのように読ませる（中）

`migrations/000069_*.up.sql` の

> `⇒ サービス層の保護は緩めていない。`

は事実（差分 0）だが、**`internal/service/preset/` は現在 `doc.go` のみで 403 保護は未実装**である。「緩めていない」は「保護が存在する」を含意するため、後任が「組み込みプリセットは削除できないはず」と誤認しうる。`SUPP-001 §7.3 の保護は M20-04 以降の実装対象であり、現時点では未実装である（本マイグレはそれを前倒しも先送りもしない）` 程度の 1 行を足すと誤読が消える。

### 5. U-2 の許容 code 集合がリテラル二重管理になっている（低）

`migrate_m2001_test.go:115-116` の

```
WHERE code NOT IN ('official_ja_move','numeric','srk')
```

は同ファイル `:30-38` の `m2001PresetAfterUp` と同じ集合をリテラルで持ち直している。片方だけ直す事故の余地がある（`strings.Join` 等で `m2001PresetAfterUp` から組むか、`count(*) WHERE code IN (...)` を 3 と突き合わせる形にできる）。本サブの契約テストは値を凍結するのが目的なのでリテラル直書き自体は許容範囲だが、二重管理である点は記録に値する。

### 6. `preset.go` godoc の「8 件」に強制主体が書かれていない（低）

`internal/model/preset.go:18` は「全体上限は 8 件（組み込み 3 + カスタム 5）」と書くが、**現時点でこの上限を強制する実装は存在しない**（VAL-P05 未実装）。A-4 に沿った是正として正しいが、`（VAL-P05 は未実装。強制はプリセット作成機能の実装時＝M20-04 以降）` と添えると、後任が「8 を強制する検査がどこかにある」と探す無駄が消える。

### 7. 規約準拠（コーディング規約・命名・ライブラリ）

| 観点 | 評価 |
|---|---|
| `console.log` / `fmt.Println` の残存 | ◎ なし |
| 不要な `nolint` / `eslint-disable` | ◎ なし |
| 依存追加 | ◎ なし |
| マイグレ slug（`m20_initial_presets_three`） | ◎ 指示書の推奨どおり。既存慣習（`m19p2b` 等の短縮形もあるが `m20_*` は読める）と衝突しない |
| テスト関数名（`TestRun_M2001_*`） | ◎ 既存の `TestRun_M1904D_*` 系と整合 |
| 連番 `000069` | ◎ disk 末尾 `000068` ／ ボード §2.2「次に払い出す番号＝000069」と一致。自採番ではない（§9-9 に該当しない） |
| i18n | ◎ `ja.json` / `en.json` にプリセット表示名の定義は存在せず（表示名は DB の `presets.name`）、削除したプリセット名の残骸なし。parity 影響なし |
| E2E | ◎ `web/e2e/*.spec.ts` に旧 3 code の参照 0 件。差分 0 |

## 推奨修正（優先度別）

### 高（M20 完了前に修正必須）

1. **完了報告 `docs/progress/M20-01-completion-report.md` を作成し、§3.3 の実査 7 項目の結果を全件載せる**（チェックリスト §0.1・§9-7）。現時点でリポジトリ上に痕跡があるのは 1・2・3・7 の一部（コミットメッセージと up.sql コメント）だけで、**4（`config` の `preset_id` 実値と実在検査の有無）／ 5（旧 3 code の系統別参照件数と C-5 との差分）／ 6（`BUILTIN_PRESET_CODES` の定義位置と参照全数）は確認できない**。
2. **`docs/progress/progress-log.md` への索引行追記**（`CLAUDE.md` §8・DoD §7.4）と **`docs/human-notes/model-allocation.md` への実使用モデル追記**。いずれも未実施。
3. **設計書側の失効記述 5 か所（指摘 1 の a〜e）を完了報告に所在つきで列挙し、設計卓へ「CHANGE-098／SUPP-001 §7.3 のスコープ外」と明示して伝達する。** 製造は編集しない（それが正しい）が、伝達しないと誰の手番にも載らない。
4. **`docs/handover/architecture-patterns.md:616` の「残り 4 プリセット」（指摘 2）を是正するか、直さない理由と所在を完了報告に記録する。**
5. **完了報告に §7.4.1「■ 併せて更新が要るもの」節を置く**（該当なしでも「なし」と書く）。消費した連番＝`000069` 1 本、CHANGE-098 の registry 登録は設計卓済みで製造の追加登録は不要、という実査結果を含めること。

### 中（M21 着手と並行可）

6. `scripts/check-enum-sync.sh` の `BASELINE_SCATTER` を 26 → 24 へ更新する（指摘 3）。
7. `000069_*.up.sql` のコメントに「サービス層の 403 保護は現時点で未実装」を 1 行追記する（指摘 4）。
8. `internal/model/preset.go:18` の「8 件」に強制主体（VAL-P05 未実装であること）を添える（指摘 6）。

### 低（将来対応）

9. `migrate_m2001_test.go` U-2 の code 集合リテラル二重管理を解消する（指摘 5）。
10. `docs/handover/phase2-to-phase3-handover.md:64`（「残り 4 プリセットのエイリアス実データ整備（numeric_ja / srk 等）」）も失効しているが、フェーズ間 handover は歴史記録寄りであり、§4.9 (b) の免除に当たると解釈できる。**是正は不要だが、完了報告の走査結果に所在として挙げておくと、次の担当が同じ走査をやり直さずに済む。**

## 良かった点

- **`name` リテラルを一次源から機械的にコピーしている。** レビュー側でバイト単位比較をしても差分 0 だった。教訓 `E-80` の型（記憶で書く）を実際に回避できている。
- **ミューテーションで検査力を裏取りしている**（`f97191e` のコミットメッセージ）。「down の `INSERT` から `id` を落とす → DN-2 が FAIL」「up に id 詰めの `UPDATE` を足す → U-3 が FAIL」を実際に走らせ、撤去後に `git diff` で差分 0 を確認している。**テストが書いてあることと、テストが効くことは別である**という点を自分で潰しているのは、本プロジェクトで最も価値の高い作法の一つ。
- **設計卓裁定 A-1 の「なぜ `srk` か」をコード内コメントに残している。** 「`numeric` へ付け替えると `FindAlias_NotFound` と主張が重複する」「`srk` は M20-02 で落ちるが、それは『まだ空である』を守る意図した仕掛けである（D-308）」まで書いてあるため、M20-02 の担当が失敗を見たときに「期待値を緩める」という誤った対応へ流れにくい。
- **A-3 の追随 7 件を、文字列 grep ではなく「その 5 は何の 5 か」で 1 件ずつ判定している。** その結果 `service/combo/service_test.go` の動的カウントを「追随不要」と正しく除外できている（機械置換なら誤って触っていた箇所）。
- **U-7 で行数不変だけでなく orphan（参照先の消えた `preset_aliases`）も見ている。** `ON DELETE CASCADE` は行数が変わらなければ発火していないとは限らない（例えば削除と挿入が相殺する場合）ため、両面で見るのは正しい設計。
- **変数名まで追随している**（`numericJaID` → `numericID`）。値だけ変えて名前を残すと、次の読者が「numeric_ja がまだあるのか」と誤読する。
- **`dbtest.go` のパッケージ godoc（「組み込みプリセット 5 種」）まで拾って直している。** テストヘルパの説明文は grep 母数から漏れやすい場所で、これを拾えたのは A-3 の趣旨をよく理解した動きである。

## 不明・確認できなかった事項

- **不明: `make e2e` の実行結果について判断できない。** リポジトリ上に実行痕跡が無く、本レビューでは E2E スタックを起動していない（DoD §7.2）。既知 flaky（ボード P-14 / P-22）との切り分けを含め、完了報告での報告を待つ。
- **不明: 着手前実査の `preset_aliases = 1653 行 / すべて preset_id=1` の裏取りができない。** 環境に `sqlite3` CLI が無く、レビュー側で一時 DB を作って確認していない。ただし `moves` 総数が 1653（`migrate_m1904d_test.go` の `m1904dMovesTotal`）であり、`official_ja_move` が全 moves にエイリアスを持つ設計と整合するため、値としては矛盾しない。
- **不明: Plan Mode の提示内容そのものは確認できない**（リポジトリに残らないため）。§3.3 の 7 項目が Plan で確定されたかは完了報告で判定する必要がある。

## 制約事項

- 本レビューはコード上で判定可能な範囲のみ対象。実際の動作確認・パフォーマンス・実機テストは別途実施が必要。
- 本レビューは設計卓の追加裁定 A-1〜A-4（リポジトリ未反映）を指示書 v1.0.0 本文に優先して適用している。**リポジトリ上の指示書・チェックリストだけを読む後続担当は、`srk` への付け替え（§4.6 #2・#3）・`migrate_m2001_test.go` の新設（§2.1）・追随 7 件（§4.6.2）・`preset.go` godoc の是正を「指示書違反」と誤読する。** 指示書とチェックリストのリポジトリ反映（v1.1.0 化）を設計卓へ依頼することを推奨する。

---

*以上、M20-01 レビュー報告書。重大（§9）判定 0 件。高 5 件はいずれも記述・報告の欠落であり、実装そのものの欠陥ではない。*

---

## 取り込み結果（自動トリアージ）

`implement_plan_full` Phase C。**製造担当が本報告書を読み、各指摘の採否と理由を以下に記録する（事後監査用）。**

**★「高」指摘の不採用は 0 件。したがって開発者へのエスカレーションは発生していない。**

### 前提: レビュー実行時点と現時点の差

**レビュアーは実装 3 コミット（`b14828f` / `6f63421` / `f97191e`）の時点のツリーを読んでいる。**
**完了報告 `M20-01-completion-report.md` と `model-allocation.md` の M20 節は、レビュアー起動後に作成した `a189bdc` に含まれる。**
**⇒ 高 1・高 2 の一部・高 5 は、レビュアーが見た時点では確かに存在しなかったが、指摘を受けた時点では既に充足していた。** 指摘は正当である。

### 高（5 件）

| # | 指摘 | 採否 | 理由・対応 |
|---|---|---|---|
| 1 | 完了報告を作成し §3.3 実査 7 項目を全件載せる | **採用（既に充足）** | `a189bdc` の `docs/progress/M20-01-completion-report.md` §1 に **7 項目すべて**を記載済み。レビュアーが「確認できない」とした **4（`config` の実値と実在検査の有無）／ 5（旧 3 code の系統別件数と C-5 との差分）／ 6（`BUILTIN_PRESET_CODES` の定義位置と参照全数）** も同 §1.3.3-4 / §1.3.3-5 / §1.3.3-6 に実値で記載してある |
| 2 | `progress-log.md` 索引行追記 ／ `model-allocation.md` 追記 | **採用** | `model-allocation.md` は `a189bdc` で M20 節を新設済み（**実使用モデル識別子はハーネス規約によりリポジトリ成果物へ書けないため、割当区分のみ記録し、モデル名欄は開発者記入待ちとした**旨を明記）。**`progress-log.md` は Phase D で追記した** |
| 3 | 設計書側の失効記述 5 か所（a〜e）を完了報告に所在つきで列挙し、設計卓へ「CHANGE-098／SUPP-001 §7.3 のスコープ外」と明示して伝達 | **採用** | **5 か所すべてを実査で確認**（`DES-006:134` / `:138` ／ `DES-004:405`＝§8 ／ `DES-003:889`＝§6 ／ `SUPP-001:1291`＝§7.5.5）。**完了報告に §4.4 を新設**し、所在・失効内容・現在の是正スコープを表で列挙した上で、設計卓へ判断を依頼した。**(a)(b) の VAL-P05 は M20-04 のプリセット作成機能が直接参照する検証仕様であり優先度が高い旨も添えた。** 製造は 5 か所とも編集していない |
| 4 | `architecture-patterns.md:616`「残り 4 プリセット」を是正するか、直さない理由と所在を記録 | **採用（記録側を選択）** | **失効していることに同意**（前向き参照であり §4.9 (b) の免除に当たらない。対になる `followup-backlog.md:93` は設計卓が「残り 2」へ更新済みで片側だけ直っている）。**ただし同書は冒頭で「対象読者＝設計・指示書作成担当 Claude」「本書へ追記するのは設計担当 Claude」と所管を明記しており、製造が編集する資料ではない。** 指摘が代替として明示した「所在つきで記録する」側を採り、**完了報告 §4.5 に記録して設計卓へ是正を依頼した** |
| 5 | 完了報告に §7.4.1「■ 併せて更新が要るもの」節を置く | **採用（既に充足）** | `a189bdc` の完了報告 §6 に設置済み。**消費した連番＝`000069` 1 本 ／ CHANGE-098 の registry 登録は設計卓が実施済みで製造の追加登録は不要（`change-number-registry.md:129` を実査）／ 版を上げた文書なし** を含む |

### 中（3 件）

| # | 指摘 | 採否 | 理由・対応 |
|---|---|---|---|
| 6 | `check-enum-sync.sh` の `BASELINE_SCATTER` を 26 → 24 へ | **採用** | **当初は「検査スクリプトの改変は単一の変更に別の変更を混ぜる」として見送り、申し送りに回していた。** レビューの「**減った分が緩衝材になり、次に別の列挙値が 2 件散在しても検知されない。本サブが減らした分なので本サブで詰めるのが筋**」という理由に同意して**採用へ変更**した。更新後に再実行し「ベースラインどおり（増加なし）」を確認済み |
| 7 | `000069_*.up.sql` のコメントに「403 保護は未実装」を追記 | **採用** | 「緩めていない」が「保護が存在する」を含意し後任を誤読させうる、という指摘は妥当。`internal/service/preset/` が `doc.go` のみで作成・削除 API 自体が無いこと、実装対象が M20-04 以降であることを 4 行で明記した |
| 8 | `preset.go` の「8 件」に強制主体（VAL-P05 未実装）を添える | **採用** | 「後任が 8 を強制する検査を探す無駄が消える」という指摘は妥当。「この上限を強制する実装は現時点で存在しない（DES-006 VAL-P05 は未実装で、プリセット作成機能自体が無い）。強制は M20-04 以降の実装対象である」を追記した |

### 低（2 件）

| # | 指摘 | 採否 | 理由・対応 |
|---|---|---|---|
| 9 | U-2 の code 集合リテラル二重管理を解消 | **採用** | 指摘は「リテラル直書き自体は許容範囲」としていたが、**片方だけ直す事故の余地を残す理由が無い**ため採用。許容集合を `m2001PresetAfterUp` からプレースホルダで組む形へ変更し、**凍結する値の定義箇所を 1 つに寄せた**。変更後も 10 件すべて green |
| 10 | `phase2-to-phase3-handover.md:64` は是正不要だが所在を走査結果に記録 | **採用** | 「フェーズ間 handover は歴史記録寄りであり §4.9 (b) の免除に当たる」という判断に同意。完了報告 §4.5 に**是正不要である旨と所在**を記録した |

### 不明として挙げられた 3 件への回答

| レビューの「不明」 | 回答 |
|---|---|
| `make e2e` の実行結果 | **完了報告 §3.1 に全文記載した。****本環境では完走しない**——`playwright install` がネットワークポリシーで 403 拒否され、プリインストール Chromium はビルド 1194 に対しプロジェクトは 1223 を要求する。`PW_EXECUTABLE_PATH` 経由で起動すると 34 failed / 39 passed になるが、**失敗した 34 件のうち 10 件を小バッチで再実行すると全部通る**。加えて `official_ja_move` のエイリアス解決を直接検証する `m14-03d` / `m14-03e` / `m12-05` を狙って実行し pass を確認した。**★フルスイートの green は本環境では確認できていない**（開発者機・CI での完走が残課題） |
| `preset_aliases = 1653 行 / すべて preset_id=1` の裏取り | **一時 DB（`migration.Run` + `t.TempDir`）で実測した値である**（プローブは実測後に撤去済み）。レビューが指摘したとおり `moves` 総数 1653 と一致し、`official_ja_move` が全 moves にエイリアスを持つ設計と整合する。**実測ログは完了報告 §1.3.3-3 に転記してある** |
| Plan Mode の提示内容 | **§3.3 の 7 項目は Plan Mode で提示し、開発者と設計卓の双方が受領済みである**（連番 `000069` の一致と停止条件 1・2 の不発動について設計卓から明示の受領回答がある）。**内容は完了報告 §1 が正本** |

### 本トリアージで新たに変更したファイル

- `migrations/000069_m20_initial_presets_three.up.sql`（中 7）
- `internal/model/preset.go`（中 8）
- `internal/infra/migration/migrate_m2001_test.go`（低 9）
- `scripts/check-enum-sync.sh`（中 6）
- `docs/progress/M20-01-completion-report.md`（高 3・高 4・低 10 ＝ §4.4 / §4.5 の新設）
- `docs/progress/progress-log.md`（高 2 ＝ Phase D）

**取り込み後の再検査**: `go test ./...` 全緑 ／ `check-enum-sync.sh` ベースラインどおり ／ `check-artifact-integrity.sh` 違反なし。
