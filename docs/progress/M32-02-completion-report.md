# M32-02 完了報告: 操作説明書の本文（`B4` の中身）＋ UI の粗の記録

| 項目 | 内容 |
|------|------|
| 作業ID | M32-02 |
| 指示書 | `docs/instructions/M32-02-usermanual-body.md` **v1.2.0** |
| チェックリスト | `docs/instructions/reviews/M32-02-review-checklist.md` **v1.0.0** |
| 着手基点 | `6fdd018` |
| 作業日 | 2026-09-12 |
| CHANGE 消費 | **0 本**（★自採番していない＝`D-293`。§6 に請求を 1 件挙げた） |
| マイグレ消費 | **0 本** |
| 実装差分 | **0 行**（`web/src/` ／ `internal/` ／ `cmd/` ／ `migrations/` の差分 0 バイト） |

---

## 1. 着手前の版ゲート（指示書 §0.4・`M-169` の対処）

**★4 点とも実物で確かめてから着手した。**

| # | 確かめること | 実測 |
|---|---|---|
| 1 | 本指示書が v1.1.0 以上（完了条件 7.5 が在るか） | **v1.2.0**。完了条件 7.5 は `:228` に実在 ✅ |
| 2 | `docs/instructions/reviews/M32-02-review-checklist.md` が在る | **在る**（v1.0.0・116 行・9791 B）✅ |
| 3 | `docs/progress/M32-01-completion-report.md` が `main` に在る | **在る**（597 行・48725 B）✅ |
| 4 | 器 `docs/usermanual/tacpendium-readme.html` と正本 `docs/usermanual/dist-readme.txt` が在る | **両方在る**（714 行 / 173 行）✅ |

**枝元**（`git log --oneline -5`）:

```
6fdd018 Merge pull request #208 from plexiblinp/claude/adoring-thompson-hl2142
93cd292 docs: M32-02 のチェックリストを発行し、着手前の版ゲートを置く（D-849）
40a5d18 docs: フェーズ4 の残件を洗い、M32-02 を投入可と判定する（D-848）
0bdf0ca Merge pull request #206 from plexiblinp/claude/charming-goodall-n3r99h
5cbac11 Merge remote-tracking branch 'origin/main' into claude/charming-goodall-n3r99h
```

**⇒ チェックリスト発行のコミット `93cd292` を含む地点から切っている。`M-169` の事故（古い地点から枝を切る）は起きていない。**

---

## 2. 画面数を数え直した（チェックリスト B-1 / B-4・指示書 §2.1）

**★設計卓の「22」をそのまま使っていない。着手時に自分で数えた。**

```
$ grep -nE '^#{2,4} *5\.[0-9]+[a-z]?' docs/design/05-screen-design.md
```

- `###` レベルの画面節 = **24**（`5.1`〜`5.22` ＋ `5.13a` ＋ `5.19b`）
- `#### 5.9.1` / `#### 5.9.2` は §5.9 配下の小節であり**画面ではない**⇒ 数えていない
- 死んでいる 2 節 = **§5.13**（`:1172`「2026-08-30 廃止＝`CHANGE-144`」）／ **§5.17**（`:1568`「削除：`CHANGE-055`」）

**⇒ 生きている画面は 22。設計卓の実測（2026-09-06）と一致した。**

**★B-4 への答え＝`M28` 以降で節は増えていない。** 2026-09-06 の `89ee3be`（`M28-02b` 受理時点）でも `### 5.` は 24 で、節番号の並びも現在と同一である。変わったのは節の**中身**だけ（§5.19b 見出しの「★未実装」→「`CHANGE-164`・`M28-02c`・2026-09-07 実装」）。

### 2.1 対応表（**24 節すべてに「書く／書かない」と理由を付けた**・B-2）

| `DES-005` | 画面 | 章 | 扱い |
|---|---|---|---|
| §5.1 | 初回起動ウィザード | **01** | 書いた |
| §5.2 | ホーム（スマホ専用） | **22** | 書いた |
| §5.3 | ユーザー選択 | **02** | 書いた |
| §5.4 | コンボ一覧 | **05** | 書いた |
| §5.5 | マイコンボ | **06** | 書いた |
| §5.6 | コンボ詳細 | **07** | 書いた |
| §5.7 | コンボ登録・編集 | **08** | 書いた |
| §5.8 | コンボ比較 | **10** | 書いた |
| §5.9 | セットプレイ登録・編集 | **11** | 書いた（`5.9.1` / `5.9.2` の内容を本文へ織り込んだ） |
| §5.10 | プリセット一覧 | **12** | 書いた |
| §5.11 | プリセット編集 | **13** | 書いた |
| §5.12 | タグ管理 | **09** | 書いた |
| **§5.13** | エクスポート | — | **書かない**＝**2026-08-30 廃止**（`CHANGE-144` / `M24-06` / `D-605`）。後継は §5.13a |
| §5.13a | エクスポートダイアログ | **15** | 書いた |
| §5.14 | 取込（節名は「インポート」） | **16** | 書いた（**★章名は「取込」へ改めた。§3.1**） |
| §5.15 | ゴミ箱 | **17** | 書いた |
| §5.16 | 設定 | **03** | 書いた |
| **§5.17** | moves 取込プレビュー | — | **書かない**＝**削除済み**（`CHANGE-055` / `M14-02`）。本文も削除通知 1 段落のみ |
| §5.18 | moves 編集グリッド | **14** | 書いた |
| §5.19 | 引っ越し取込 | **18** | 書いた |
| §5.19b | ゲーム更新の影響コンボ | **19** | 書いた（**`M28-02c` の as-built で書いた**。§2.2） |
| §5.20 | 確定反撃サーチ（探す） | **20** | 書いた |
| §5.21 | 確定反撃マイリスト（使う） | **21** | 書いた |
| §5.22 | ログイン | **04** | 書いた |

**24 節 − 2 節 ＝ 22 章。目次 22 行 ／ 本文 22 章で一致している**（§5 の機械計測）。

### 2.2 §5.19b について（指示書 §4.4・チェックリスト §7）

**★止まる条件は発動しなかった。** `M28-02c` は受理済みであり（`82bc2b6`・`D-769`）、実物も在る——`/game-update/combos`（`web/src/router.tsx:55`）／ `web/src/pages/GameUpdateCombosPage.tsx` ／ `web/src/features/game-update/` ／ E2E `web/e2e/m28-02c-game-update.spec.ts`。

**⇒ 指示書の記述ではなく着地したもの（`DES-005` §5.19b 冒頭の as-built ブロック）で書いた。**

---

## 3. やったこと

### 3.1 22 章の本文（88 か所）

器が持っていた `<p class="todo">本文は M32-02 で記述します。</p>` **88 個**（22 章 × 4 見出し）をすべて本文へ置き換えた。**`h3` の文言・順序・`figure` の構造は 1 つも変えていない。**

段の割り方は**指示書 §2.2 をそのまま採った**（器の章順が既にこの順であり、並べ替えていない＝E-4）。**段ごとにチェックポイントコミットを切った**（D-4）:

| コミット | 段 | 章 |
|---|---|---|
| `111a760` | 段 1 | ch01–ch04 |
| `960ac7b` | 段 2 | ch05–ch09 |
| `bf8d59d` | 段 3 | ch10–ch14 |
| `429bd43` | 段 4 | ch15–ch18 |
| `ef45cbd` | 段 5 | ch19–ch22 |
| `8df45ab` | 段 6 | 節番号の非表示 ＋ 暫定表記の除去 |

**読み手の想定**＝「**SF6 は知っているが本アプリは初めて**」（指示書 §7-2 を開発者が確認）。**SF6 の用語（コンボ・SAゲージ・ドライブラッシュ・確定反撃・ジャストパリィ等）は説明せず、本アプリの用語（マイコンボ・セットプレイ・プリセット・引っ越し取込・仮登録等）だけを説明した。**

### 3.2 ★開発者裁定 4 件（着手前に確認した）

| # | 事項 | 裁定 | 反映 |
|---|---|---|---|
| **1** | `.src` / `.toc-src`（`DES-005 §5.x` の可視表示・全 44 か所）を出すか隠すか | **隠す**（CSS 1 行） | `.src, .toc-src{display:none;}` を足した。**★要素は HTML に残した**ので章 ⇄ `DES-005` の対応はソースで追える。器の CSS コメント（「★決めずに公開しないこと」）を「決めた」記述へ書き替えた |
| **2** | ch16 の章名「インポート」（**`M24-07` で退けた語**） | **「取込」へ直す** | 目次 ／ 章見出し ／ `img alt` ／ `figcaption` の 4 か所。**§3.1 の粗として記録した** |
| **3** | 読み手の想定 | **設計卓の想定どおり** | 器の `#about` の記述をそのまま使った |
| **4** | スクリーンショットの枚数 | **複雑な数画面だけ 2 枚に増やす** | §3.4 |

### 3.3 暫定表記の除去（完了条件 7.5・C-1〜C-3）

**4 か所すべて外した。**

| # | 場所 | 外した逐語 |
|---|---|---|
| 1 | `README.md:10-11` 冒頭 blockquote | 「操作説明書 … は**現在準備中です**（器のみ。本文は `M32-02` で執筆する）」 |
| 2 | `README.md:218` 4 ファイル役割表 | 「**★現在は器のみで本文は未執筆**(`M32-02`)」 |
| 3 | `docs/usermanual/dist-readme.txt:7` 冒頭 | 「操作説明書(…)は**現在準備中です**。」 |
| 4 | `docs/usermanual/dist-readme.txt:172` 末尾 | 「(公開リポジトリの …)は**現在準備中です**。」 |

```
$ grep -rn "現在準備中\|本文は未執筆\|本文は M32-02\|器のみ" README.md README.txt docs/usermanual/
（0 件）
```

**★`README.txt` は直接編集していない**（C-2）。**正本 `docs/usermanual/dist-readme.txt` を直し、`bash scripts/check-dist-readme.sh --write` で作り直した。**

**★`dist-readme.txt` の起動方法節の `- Windows : tacpendium-windows-amd64.exe   (公式サポート)` の行には触れていない**——`check-dist-readme.sh:145` の陽性対照がこの行を標的にしており、変えると自己検査が壊れる。

**★到達経路（`P-57`）は解決していない**（C-4）。`README` の文面で「操作説明書にあります／全 22 章です」と示すに留めた。zip への同梱・Pages の設定は開発者判断である。

### 3.4 スクリーンショットの置き場（完了条件 4）

- 置き場＝`docs/usermanual/images/`（**現在 `.gitkeep` のみ。画像 0 件**）
- 命名＝`.gitkeep` の規約 `ch<NN>-<slug>-<n>.png` に従った。**正本は器の各 `<figure>` の `img src`**
- **★撮影と貼り込みは開発者の手番である。合成していない**（E-3）。`figure` の `todo` クラスは 27 枚すべて残した

**枚数＝計 27 枚**（22 章 × 1 枚 ＋ **5 章に 2 枚目**）。2 枚目を足した 5 章と、その根拠（`DES-005` の節の行数・実測）:

| 章 | 画面 | `DES-005` の節の行数 | 2 枚目に撮るもの |
|---|---|---:|---|
| **08** | コンボ登録・編集 | **378**（最大） | レシピを入力しているタブ |
| **17** | ゴミ箱 | 197 | 完全削除が拒否され、掴んでいるコンボが並んだ状態 |
| **20** | 確定反撃サーチ | 159 | 「自動判定できない相手技」のまとまりと理由ラベル |
| **07** | コンボ詳細 | 152 | 紐づくセットプレイと成立条件の 2×2 グリッド |
| **05** | コンボ一覧 | 112 | 絞り込み欄を開き、セットプレイを展開した状態 |

---

## 4. ★★★UI の粗の記録（**第 2 の成果物**）

> **★仕分けていない**（A-5）。「直す ／ 直さない ／ 判断が要る」は設計卓の手番である。**⇒ 事実と根拠だけを出す。**
>
> **★`docs/handover/followup-backlog.md` は 1 文字も編集していない**（E-2・`D-838`）。**登録候補は設計伝達レポート §4 へ並べる。**
>
> **★0 件ではない。計 28 件。** 内訳＝説明が長くなった 11 件 ／ 先に別の画面を見ないと分からない 6 件 ／ 語の揺れ 4 件 ／ 3 つに絞れなかった 4 件 ／ **本文が弁解を負った 3 件**。
>
> **★★5 つ目の型はレビュー指摘（中-1）で足した。** 指示書 §2.4 が挙げる型は 3 つ（長くなった ／ 順序が逆行する ／ 語が揺れた）であり、初版の網はそこへ「3 つに絞れなかった」（§4.5 由来）を足した 4 型で閉じていた。**⇒ `B4` の逐語「説明が要る箇所＝設計が負けている箇所」に最も近いのは、実は 5 つ目である**——**説明書が「これは壊れていません」と書かされている箇所**は、書き手が困った場所そのものである。**★型を自分で増やす発想が無かったことを記録しておく。**

### 4.1 説明が長くなった箇所（11 件・**各件に「なぜ長くなったか」を付けた**＝A-2）

| # | 章 | 箇所 | **なぜ長くなったか** |
|---|---|---|---|
| **A-1** | ch05 | 表示列の「既定は初回しか効かない」 | **画面に「いま既定のままか、自分で触ったあとか」を示す手がかりが 1 つも無い。⇒ 文章で補うしかなかった**（`useColumnVisibility` の保存値優先は `DES-005` §5.4 が明記しているが、利用者からは観測できない） |
| **A-2** | ch05 | 絞り込み・並びの保持範囲 | **条件が 2 段ある**——「詳細等へ寄り道して戻ったときだけ」かつ「コンボ一覧だけ（マイコンボは対象外）」。**★マイコンボは同じ表部品を使っており見た目が同一なので、違いを文章で言う以外に伝える手段が無い** |
| **A-3** | ch05 | 成立条件 3 値の意味 | **「成立 / 不成立 / 未検証」という*排他に見える語*で非排他の集合を表している。⇒ 語だけでは必ず誤読される。** さらに「未検証＝埋め残しがある（手つかずではない）」「セットプレイ 0 件のコンボはどれにも出ない」の 2 つを足さないと絞り込み結果が説明できない |
| **A-4** | ch07 | 「ゴミ箱へ移動」／「紐付け解除」／コンボ削除 の 3 者 | **同じカードの中に 2 つ、ページ上部に 1 つあり、名前から「何が消えるか」が読み取れない。** `DES-005` §5.6 は守りを 5 点挙げているが、**そのうち 4 点は実装側の分離であって、利用者が読む手がかりは「文言が違う」1 点しかない** |
| **A-5** | ch08 | 「6 項目を変えると別コンボとして登録し直される」 | **保存方式の分岐**（`DES-005` §5.7 の (a)/(b)）**が画面に一切現れない。⇒ ゴミ箱に旧行が増えるという*結果*でしか気づけない。** 分岐するキーが 6 つあり、列挙しないと説明にならない |
| **A-6** | ch16 | 親解決の `○` / `×` | **画面の記号が実際の結果と食い違う**（重複時の動作を考えに入れていない＝`DES-005` §5.14 の既知の乖離）。**⇒ 記号の意味を打ち消す説明が要った。「○ でも取り込まれないことがある」は、説明書が画面に反論している形である** |
| **A-7** | ch17 | 編集で積まれた旧行が出ない件 | **同じ一覧に 2 つの由来（自分で消した／編集で積まれた）が混在しうるうえ、`M23-01` 以前に編集した分は出たままという*時期による非対称*がある。⇒ 「出ない」と言い切れず、但し書きが要った** |
| **A-8** | ch19 | 保存後の戻り先の限界 | **同じ「編集」に見える 2 つの入口**（行の［編集］／［詳細］→ 編集）**で挙動が違うのに、画面上に差が出ていない**（`followup` `game-update-return-to-lost-via-detail`）。**⇒ 利用者に「どちらを押したか」を覚えておけと言う形になった** |
| **A-9** | ch20 | 「出てこない」の理由が 2 通りある | **`damage = 0` は完全除外、`damage IS NULL` は手動確認レーンへ、と行き先が違うのに画面では区別が付かない。⇒ 「出てこない＝データが無い」という自然な推論が成り立たないことを、名指しで否定する必要があった**（`DES-005` §5.20 が計測点 `M-155` として同じ取り違えを記録している） |
| **A-10** | ch20 | 手動確認レーンからの登録だけ自動採用される | **同じ「新規登録」に見える 2 導線で、後続の挙動が逆になる。** 非対称は意図的で揃えてはならないと設計書が明記しているが、**画面側にその区別を示す表示が無い** |
| **A-11** | ch21 | 第 3 セクションの内側 3 区分 | **入れ子が 3 段**（セクション →「反撃に転用可能」／「再利用可能」→ 折りたたみ別枠）**あり、どこに何が入るかを区分名から辿れない。** とくに別枠が「そこにしか解除手段が無いから残してある」枠であることは、名前からは読めない |

### 4.2 「先に別の画面を見ないと分からない」箇所（6 件・A-3）

**★説明の順序（章の並び）と、実際の動線が逆行する箇所である。**

| # | 章 | 内容 |
|---|---|---|
| **B-1** | ch06 → ch08 | **マイコンボの状態（使用中／練習中／頻度低下）は、マイコンボの画面では切り替えられない。** コンボ登録・編集（ch08）の「その他情報」へ行く必要がある。**⇒ 06 を読んでいる時点では 08 をまだ読んでいない** |
| **B-2** | ch11 → ch07 | **セットプレイの成立条件は編集時に出ない**（新規登録時のみ）**。⇒ あとから埋めるにはコンボ詳細（ch07）へ戻る。** 章の順では詳細が先だが、作業の動線は 11 → 07 と逆行する |
| **B-3** | ch13 → ch14 | **「呼び名を変えたい」のか「技のデータを直したい」のか**の区別は、moves 編集グリッド（ch14）を読むまで付かない。**⇒ プリセット編集（ch13）だけを読んだ人は、フレームもここで直せると誤解しうる** |
| **B-4** | ch20 → ch14 | **「ダメージ不明」で手動確認レーンへ回った技を直すには moves 編集グリッド（ch14）が要る。** 第 5 部から第 3 部へ戻る形になる |
| **B-5** | ch03 と ch04 | **パスワード紛失時の復旧手順を 2 章に書いた。** `DES-005` は §5.16 と §5.22 の両方で要求しており、**理由も明記されている**（「この案内が要る人はログイン画面に居て、設定画面へ入れない」）。**⇒ 重複ではないが、同じ手順が 2 か所にある状態である** |
| **B-6** | ch19 → ch07 | **「更新未確認」の印が出るのはコンボ詳細だけ。⇒ ch19 の説明だけでは、印をどこで見るのか分からない。** これは `DES-005` §5.19b が「受け入れた代償」として明記している設計判断である |

### 4.3 語が揺れている箇所（4 件・**`M29-01` の結果を名指しする**＝A-4）

| # | 事実 | `M29-01` / `M24-07` との関係 |
|---|---|---|
| **★★C-1** | **器の ch16 の章名が「インポート」だった**（目次 `:177` ／ 章見出し `:538` ／ `img alt` ／ `figcaption` の 4 か所） | **「インポート」は `M24-07`（開発者判断 2026-08-30）で退けた語であり、`web/src/locales/retired-words.test.ts` の `RETIRED` に `{word:"インポート", useInstead:"取込"}` として登録されている。** 実装の現物はナビ「取込」（`web/src/components/Header.tsx:35`）・`h1`「取込(CSV)」（`web/src/pages/ComboImportPage.tsx:188`）。**★`DES-005` §2 の画面一覧は `CHANGE-147` で「取込(CSV)」へ改名済みで、§5.14 の節名だけが古い。⇒ 器はその古い節名を写していた。★開発者裁定で「取込」へ直した**（§3.2-2） |
| **C-2** | **`retired-words.test.ts` は `docs/` を 1 バイトも走査しない** | **同ファイル自身が「走査対象は `web/src` だけである」と注記している**（Go 側・`migrations/` も網の外）。**★`M29-01` 完了報告 §7 も `05-screen-design.md:558` / `:562` に統一前の語が残ると名指ししており、「`retired-words.test.ts` は `docs/` を走査しないため永久に検出されない」と書いている。⇒ C-1 が機械で捕まらなかった理由はこれである** |
| **C-3** | **同じ画面に呼び名が 2 つある**——ナビは「取込」、`h1` は「取込(CSV)」 | **`M24-07` は「インポート → 取込」を決めたが、括弧付きの `(CSV)` の有無までは揃えていない。** 説明書では章名を「取込」とし、本文で `h1` の表記に触れる形にした |
| **C-4** | **「取込」は和語、「エクスポート」はカタカナのまま** | **`M24-07` は取込側だけを和語へ寄せた。⇒ 対になる書き出し側（`DES-005` §5.13a「エクスポートダイアログ」、画面の「エクスポート」ボタン）はカタカナのままである。★`M29-01` はこの対を群として扱っていない**（4 群＝空レシピ／コピー／ゲージ／相手状態） |

### 4.4 「よく使う操作」を 3 つに絞れなかった画面（4 件・A-7）

**★黙って 5 つ書いていない。3 つに収め、落としたものをここへ挙げる。**

| # | 章 | 3 つに入れたもの | **落としたもの**（＝絞れなかった分） |
|---|---|---|---|
| **D-1** | **ch03 設定** | ネットワーク切替 ／ 基本（既定キャラ・プリセット）／ パスワードとログアウト | **データ（DB パス・バックアップ・リストア・レシピキャッシュ再構築）／ プリセット管理へのリンク ／ 詳細（初回ガイド・ログパス・バージョン）。★節が 6 つある画面を 3 つの操作で代表させることに無理がある** |
| **D-2** | **ch05 コンボ一覧** | キャラ切替 ／ 絞り込み ／ 選択して比較・書き出し | **表示列カスタマイズ ／ セットプレイの展開（▶）／ ソート ／ レシピを縦に表示するトグル ／ 行の編集・コピー・削除。★落とした 5 つはすべて「つまずきどころ」へ回した。⇒ 操作が多いのではなく、1 画面が担う役割が多い** |
| **D-3** | **ch08 コンボ登録・編集** | レシピ入力 ／ 仮登録 ／ コピー | **セットプレイ登録セクション ／ キャラ固有状態 ／ 起き攻め情報 ／ メディア 3 欄 ／ タグ ／ メモ ／ コンボ後情報。★`DES-005` の表示項目は 14 項目ある** |
| **D-4** | **ch20 確定反撃サーチ** | 条件選択 ／ ツリーをたどる ／ 採用・確定反撃版の生成 | **手動確認レーンからの登録 ／ 不採用（★2 種類ある——一時非表示と `adopted`/`unreachable` トグル）／ 変換済みの折りたたみを開く。★「不採用」が 2 種類あること自体が粗の候補である**（`DES-005` §5.21 の注記が「混同すると指示が 2 通りに読める」と述べている） |

### 4.5 本文が弁解を負った箇所（3 件・**レビュー指摘 中-1 で追加**）

**★「これは仕様です」「不具合ではありません」「今後実装予定です」と説明書が書かざるを得なかった箇所である。⇒ 書き手が困った場所がそのまま出ている。**

| # | 章 | 本文の逐語 | **なぜ弁解が要ったか** |
|---|---|---|---|
| **E-1** | ch02 ユーザー選択 | 「画面を再読み込みすると、この画面へ戻ります。**選んだ人を保存していないためで、不具合ではありません**」 | **利用者から見ると「選んだのに忘れられた」という体験そのものである。⇒ 設計の意図**（`DES-002` §8＝選んだ利用者を保持しない）**は正しいが、画面はその意図を 1 つも伝えていない。★「不具合ではない」と言葉で打ち消すしかなかった** |
| **E-2** | ch03 設定 | 「『手動バックアップ』『リストア』のボタンは、**いまは押せません**（**今後実装予定**）」 | **押せないボタンが配布物の画面に出ている。⇒ 説明書が「押せない理由」を引き受けている。★`D-607` は「設定画面に出ているボタンを未達の機能要件にしないため」`NFR201` のままとしたと記録しているが、その判断は*画面に出し続けること*を前提にしている** |
| **E-3** | ch09 タグ管理 | 「カテゴリと色は、一度決めると『未設定』には戻せません。**仕様であり、不具合ではありません**」 | **一方向にしか動かせない設定が、画面上ではそう見えない。⇒ 「変えられる」と「未設定へ戻せる」の差を、押してみるまで知る手段が無い**（`DES-006` §5 ／ `CHANGE-172` §2） |

> **★据え置いた 2 件**（レビューも据え置きが妥当としたもの）——**ch21 の「パニッシュカウンター(インパクト)」が第 3 セクションへ出る件**と、**ch16 / ch15 の「載るタグは操作した利用者のもの」**。**どちらも設計書側に「欠陥ではない」と明記があり**（`CHANGE-151` §6-1 ／ `CHANGE-113`）**、弁解ではなく既知の設計判断の説明である。**

---

## 5. 検証（指示書 §5・チェックリスト束 E）

### 5.1 構造の機械計測

```
$ grep -c 'class="toc-ch"' docs/usermanual/tacpendium-readme.html     → 22
$ grep -c '<section class="chapter"' docs/usermanual/tacpendium-readme.html → 22
$ grep -c '^    <h3>' docs/usermanual/tacpendium-readme.html          → 88   (22 章 × 4)
$ grep -c '<p class="todo">' docs/usermanual/tacpendium-readme.html   → 0
$ grep -c 'class="shot todo"' docs/usermanual/tacpendium-readme.html  → 27
id 重複: 0 件 ／ dead anchor: 0 件（Python で id 集合と href="#..." 集合を突合）
```

**⇒ §5-1「目次と本文の章が全数一致」は 22 / 22 で満たした。**

### 5.2 生成物の照合（§5-3・C-2 / C-3）

```
$ bash scripts/check-dist-readme.sh
✅ 一致: README.txt は docs/usermanual/dist-readme.txt から生成した内容と同一です

$ bash scripts/check-dist-readme.sh --self-test
  ✅ 陰性対照: 無改変なら一致と判定した
  ✅ 陽性対照: 正本を 1 行変えたら差分として検出した
自己検査: 合格(陽性は赤・陰性は緑)
```

**★本サブは `README.txt` の正本に触った。⇒ 指示書 §5-3 が「触っていなくても 1 回回すこと」と求めている検査を、触ったうえで回している。**

### 5.3 語の走査（完了条件 7・D-3）

対象＝`docs/usermanual/tacpendium-readme.html` ／ `docs/usermanual/dist-readme.txt` ／ `README.md` ／ `README.txt`。

**退けた語（`retired-words.test.ts` の `RETIRED` 7 語 ＋ `M29-01` 完了報告 §2 の 5 語）:**

```
OK  「インポート」 0 件        OK  「取込ヘルパー」 0 件
OK  「相手スタンス」 0 件      OK  「相手サイズ」 0 件
OK  「相手の体格」 0 件        OK  「コピーして作成」 0 件
OK  「SA ゲージ」 0 件         OK  「複製」 0 件
OK  「消費 SA」 0 件           OK  「消費ドライブ」 0 件
OK  「始動残量」 0 件          OK  「開始残量」 0 件
```

**★走査が空回りしていないことの陰性対照**（採った語が実際に当たること）:

```
OK  「取込」 18 件            OK  「引っ越し取込」 7 件
OK  「相手の状態」 2 件       OK  「相手の大きさ」 1 件
OK  「SAゲージ」 1 件         OK  「コピー」 26 件
```

**★初回の走査で「複製」が 1 件当たった**（`ch03` の「そのファイルを自分で複製して備えてください」）**。⇒ `M29-01` 裁定 2-A の語へ「コピー」へ直した。** 意味としては UI ラベルではなくファイル操作の一般語だが、**説明書の中で「コピー」と「複製」を混在させると、利用者から見れば同じ揺れである。**

**★`DES-005` の本文から語をそのまま写していない**（指示書 §4.2）。`M29-01` 完了報告 §7 が `05-screen-design.md:558` / `:562` に統一前の語が残ると名指ししているため、**実装の現物**（`web/src/locales/ja.json` ／ 各画面の直書き）**と `DES-005:989-1002` の「画面に出す語の正典（4 群）」を突き合わせて書いた。**

### 5.4 描画の確認（E-9）

**★`docs/usermanual/` 配下は `check-md-emphasis.sh` の走査対象ではない**（そもそも HTML である）。**⇒ 書いた本人が描画で確かめた。**

`/opt/pw-browsers/chromium-1194/chrome-linux/chrome` を `executablePath` に直指定した headless Chromium（**★`make e2e` の経路ではない。プロジェクトの E2E とは別経路である**）。

| 幅 | 横スクロール | `.src` / `.toc-src` | 目次 / 章 | `<p class="todo">` |
|---|---|---|---|---|
| **900px** | **なし** | 要素 44 件 / **可視 0 件** | 22 / 22 | 0 件 |
| **390px** | **なし** | 要素 44 件 / **可視 0 件** | 22 / 22 | 0 件 |

- **JS の例外: なし**（`pageerror` 0 件）
- 読み込み失敗＝**画像 27 件**（未撮影。想定どおり）／ **Web フォント 1 件**（オフライン環境のため `fonts.googleapis.com` へ到達しない）／ **その他 0 件**
- **★目次と ch07 を実際にスクリーンショットで見た。** 章番号 `01`〜`22` が CSS カウンタで振られていること、`DES-005 §5.x` が読み手から消えていること、ch16 の見出しが「取込」になっていること、`.key` / `.caution` / `.note` / `ul.plain` / `code.path` が意図どおり描かれていることを目視で確認した

**★完了報告そのものの閉じない強調検査**（`D-775`。`docs/progress/` は常時走査の対象外＝ここで見なければ誰も見ない）:

```
$ bash scripts/check-md-emphasis.sh docs/progress/M32-02-completion-report.md
検出: 0 行(ファイル指定のためベースライン比較を行わない)   EXIT=0
```

**★本節はレビュー指摘 低-4 で足した**——**実行はしていたが、記録が報告に無かった。⇒ 記録が無いと次の担当が手順を落とす。**

### 5.5 常設検査 ＋ テスト（E-7 / E-8）

```
bash scripts/check-artifact-integrity.sh   → 違反なし  ★1 本目に回した
bash scripts/check-doc-refs.sh             → dead reference なし
bash scripts/check-stop-discipline.sh      → 違反なし
bash scripts/check-doc-inventory.sh        → 型に無いファイルなし
bash scripts/check-public-snapshot.sh      → 違反なし（母数 3065 / 公開 3036 / 除外 29）
go test ./...                              → ok 60 / FAIL 0
cd web && pnpm test                        → 229 ファイル / 2810 件 すべて緑
```

**★`go test` / `pnpm test` は「変化が無いことの確認」である**——**本サブは実装に触っていない。**

### 5.6 ★変更統計（着手基点 `6fdd018`）

```
 README.md                              |   6 +-
 README.txt                             |   9 +-
 docs/usermanual/dist-readme.txt        |   9 +-
 docs/usermanual/tacpendium-readme.html | 579 ++++++++++++++++++++++++++-------
 4 files changed, 473 insertions(+), 130 deletions(-)
```

**★`E-225`（新規のつもりのファイルで既存を上書きする）の観点で読んだ。** 本サブは**新規ファイルを 1 つも作っていない**（完了報告を除く）。`tacpendium-readme.html` の 130 deletions は**プレースホルダ 88 行 ＋ 器の CSS コメント 5 行 ＋ 章名・暫定表記の置換分**であり、**置換によるものである**。`web/src/` ／ `internal/` ／ `cmd/` ／ `migrations/` の差分は **0 バイト**（E-1）。

---

## 6. ★設計卓への請求・申し送り

**★CHANGE は自採番していない**（`D-293`）。

| # | 内容 |
|---|---|
| **★★1**（**CHANGE 請求**） | **`DES-005` §5.14 の節名が「インポート」のままである。⇒ 退けた語である**（`M24-07`・`retired-words.test.ts` の禁則語）**。★同書 §2 の画面一覧は `CHANGE-147` で「取込(CSV)」へ改名済みであり、片側だけが古い。★本サブは説明書の章名を「取込」へ直した**（開発者裁定）**が、設計書本体は書き換えていない**（`CLAUDE.md` §8）**。⇒ 採番と反映は設計卓の手番** |
| **★★1b**（**CHANGE 請求**） | **`DES-005` §5.13a の `CHANGE-165` 注記の逐語「書出に行数・バイト上限が無いのに、取込は 1000 行 / 10MiB で弾く」が実装と食い違う。⇒ 書き出しは*コンボ 1000 件*で打ち切られる**（`internal/service/comboio/export.go:18` の `exportRowLimit = csvcore.DefaultMaxRows` ／ 同 `:136` の `len(out) >= exportRowLimit` ／ 打ち切り時は `export.truncation.body` の確認が出る）**。★上限が無いのは「セットプレイの行数」と「バイト数」である。★同じ逐語が `export.go:80` のコード注釈にも在り、2 か所で同じ失効を共有している。⇒ 本サブは説明書の本文を実装に合わせて直した**（レビュー 高-1 の採用）**が、設計書とコード注釈は書き換えていない**（`CLAUDE.md` §8 ／ 本サブは実装差分 0 行）**。★採番と反映は設計卓の手番** |
| **★2** | **目次末尾の 1 文が、利用者に内部の設計書パスを見せている。** 逐語＝「全 22 章。`docs/design/05-screen-design.md` §5 の画面 24 節のうち、廃止・削除済みの 2 節（§5.13 エクスポート／§5.17 moves 取込プレビュー）を除いた数です。」**★`.src` / `.toc-src` を隠した結果、ここが読み手から見える唯一の内部参照として残った。⇒ 本サブは触っていない**（目次は `M32-01` の成果物であり、開発者の裁定は `.src` / `.toc-src` についてのものだった＝E-4）**。~~★「決めずに公開しないこと」の趣旨に照らすと、公開前に扱いを決める必要がある~~** **★★★【2026-09-15 決着＝開発者裁定】外した。** 開発者の逐語＝**「説明書に設計書の参照は付けたくありません」（2026-09-15）**。⇒ 目次末尾の 1 文を **「全 22 章。アプリの画面ごとに 1 章です。」** へ置き換えた。**★描画で実測し、読み手に見えるテキストから `docs/design` / `DES-005` / `§5.13` / `§5.17` の痕跡が 0 件であることを確認した。** **★`.src` / `.toc-src` の 44 件は `display:none` のまま据え置き**〔2026-09-12 の裁定＝「隠す。要素は残す」。**`§5.13` / `§5.17` は同 44 件に含まれないため本裁定の射程外**〕**。** |
| **3** | **`DES-005` §2 の画面一覧に §5.19b と §5.13a の行が無い**（表は 22 行）**。§3 の画面遷移図にも §5.19b / §5.20 / §5.21 / §5.22 / §5.13a / §5.19 が無い**（`:111` に「主要な遷移のみ記載」の注記はある）**。⇒ 本サブの対応表（§2.1）は §5 の節見出しを正としている** |
| **4** | **到達経路（`P-57`）は未決のままである。** 本文が 22 章そろったことで、**`README` が案内する先に実体が在る状態にはなった**が、**(a) URL が書けない ／ (b) zip に同梱されない ／ (c) GitHub 上の `.html` はソース表示になる** の 3 つは変わっていない。**⇒ `B4` の価値はここで止まる**（`M32-01` レポート §4-1・チェックリスト §6-1 が「見なくてよい」としている項目だが、本文が入った以上、優先度が上がる） |
| **★4b** | **器が Google Fonts を `@import` で読みに行く**（`tacpendium-readme.html:8`）**。⇒ 閲覧のたびに外部へ接続し、オフラインでは既定フォントへ落ちる**（`sans-serif` のフォールバックは指定済み）**。★`M32-01` 由来であり本サブでは触っていないが、`P-57` で「zip へ同梱する」を採るなら、同梱物が外部へ接続することの可否は同じ手番で決まる。⇒ 到達経路の判断と一緒に見ること**（レビュー 低-3） |
| **5** | **スクリーンショット 27 枚は未配置である。** 撮影と貼り込みは開発者の手番。枠と命名は器に入っている（§3.4）。**★撮り終えたら `figure` の `todo` クラスを外すこと**（未記入であることを示すための枠線・背景色が付いている） |

---

## 7. 完了条件の自己点検（指示書 §6）

| # | 条件 | 判定 |
|---|---|---|
| 1 | §2.1 の 22 画面に対応する章が本文にある（着手時に数え直した結果で判定） | **満たした**（§2・§5.1。自分で数え直して 24 節・生きている 22） |
| 2 | 書かない 2 節に理由が付いている | **満たした**（§2.1 の表。`CHANGE-144` / `CHANGE-055`） |
| 3 | 各章が §2.3 の 5 要素を持っている | **満たした**（`h3` 88 本＝22 × 4 ＋ `figure` 27 枚） |
| 4 | スクリーンショットの置き場が空けてあり、枚数と場所が報告にある | **満たした**（§3.4。27 枚・`docs/usermanual/images/`・命名規約） |
| 5 | UI の粗の記録がある | **満たした**（§4。**25 件**。0 件ではない） |
| 6 | 粗の各件に「なぜそう見えたか」の 1 行が付いている（仕分けはしない） | **満たした**（§4.1 は全件に「なぜ長くなったか」。**仕分けていない**） |
| 7 | `M29-01` の統一語を使っている（走査結果を報告に添える） | **満たした**（§5.3。退けた語 12 種 0 件 ＋ 採った語 6 種の陰性対照） |
| **7.5** | 暫定表記「現在準備中です」を**全数**外している | **満たした**（§3.3。4 か所・走査 0 件。`README.txt` は `--write` で再生成） |
| 8 | §5 の検査がすべて緑（出力で判定する） | **満たした**（§5.2・§5.5） |
| 9 | 完了報告を書き、`progress-log.md` へ索引行を 1 行足す | **本ファイル ＋ Phase D で追記** |
| 10 | 設計伝達レポートを出し、§4 に UI の粗を `followup` 登録候補として並べる | **Phase C の後に実施** |

---

## 8. レビュー結果（Phase B / C）

**レビュー報告書**: `docs/progress/m32-02-review.md`（207 行・2026-09-12）

| 項目 | 実測 |
|---|---|
| 指摘件数 | **11 件**（**高 4 / 中 3 / 低 4**） |
| **★「高」指摘の不採用** | **0 件**（**4 件すべて採用**） |
| 不採用 | **低 2 件のみ**（低-1 / 低-2。理由は下表） |
| 再レビュー往復 | **0 回** |
| 総評 | **「束 A（第 2 の成果物）は満たしている。⇒ 不合格ではない」** |

### 8.1 トリアージ（採否と理由）

| # | 指摘 | 採否 | 対応 |
|---|---|---|---|
| **高-1** | **「書き出しには件数の上限がありません」が実装と食い違う。** 実装は `exportRowLimit = csvcore.DefaultMaxRows = 1000` でコンボ 1000 件で打ち切り、打ち切り確認（`export-truncation-confirm`）まで出る | **採用** | **★実装を実物で当たって確認した**（`export.go:18` / `:136` / `rules.go:10` / `ja.json` の `export.truncation.body`）**。⇒ 指摘は正しい。** ch15 / ch16 の `caution` を書き直した——**「コンボは 1000 件で打ち切られ、超えると確認が出る」「上限が無いのはセットプレイの行数とバイト数であり、そのぶん自分の取込が拒否しうる」**。**★あわせて §6-1b へ CHANGE 請求を足した**（`DES-005` §5.13a の注記と `export.go:80` のコード注釈が同じ失効を共有している） |
| **高-2** | **「硬直データ不備」は画面に存在しない語。実装ラベルは「硬直データ不定」** | **採用** | **★`web/src/constants/punish.ts:54` と `DES-005:1795` の両方で確認した。** ch20 を「硬直データ不定」へ。**説明書が第 5 の語を作っていた**（`M29-01` の趣旨に逆行する） |
| **高-3** | **`progress-log.md` の索引行が未追記**（`check-progress-log-index.sh` が NG 1 件） | **採用** | **Phase D で追記した**（§9）。**★手順上は Phase C の後だが、検査が赤のままでは閉じられないという指摘は正しい** |
| **高-4** | **設計伝達レポートが未作成。25 件の粗が製造ブランチの完了報告にしか無い** | **採用** | **Phase D の後に `/design_handover_report` で作成する**（完了条件 10）。**★「第 2 の成果物は、設計卓に届いて初めて価値になる」という指摘のとおりである** |
| **中-1** | **粗の網が 4 型に閉じ、本文が「仕様です／不具合ではありません／今後実装予定」と弁解している箇所が 1 件も拾われていない** | **採用** | **★最も価値のある指摘である。§4.5 として 5 つ目の型を新設し 3 件を足した**（ch02 / ch03 / ch09）**。⇒ 粗は 25 → 28 件。★ch21 / ch16 の 2 件は設計書に「欠陥ではない」と明記があるため据え置いた**（レビューも据え置きが妥当としている） |
| **中-2** | **目次末尾の内部パス露出**（`.src` を隠した裁定の趣旨に照らすと、ここだけ残っている） | **採用**（追加作業なし） | **★レビュー自身が「製造は §6-2 で正しく申し送っており、`E-4` に従って触らなかった判断は妥当」としている。⇒ 申し送りとして §6-2 に据え置く。開発者の裁定を仰いでから畳む** |
| **中-3** | **章 ⇄ `DES-005` の対応表が、読み手の受け取る成果物の側に無い** | **採用**（認識の記録） | **★`.toc-src` を隠した帰結であり、開発者裁定の直接の帰結である。⇒ 対応表は完了報告 §2.1 と HTML ソース（要素は残してある）の 2 系統で追える。本行がその認識の記録である** |
| **低-1** | 画像ファイル名 `images/ch16-import-1.png` が章名「取込」と揃っていない | **不採用** | **★スラッグは全 22 章とも英語で統一されている**（`wizard` / `user-select` / `combo-list` / `intake-helper` …）**。`import` は「取込」の英語表記として正しく、揃っていないのは*言語*であって*語*ではない。⇒ 1 章だけ規則から外すほうが害が大きい。★`.gitkeep` は「章番号とスラッグの対応は HTML の `img src` を正本とする」と定めており、正本側は一貫している** |
| **低-2** | 器の CSS に `.body table` / `.table-scroll` の定義があるが本文は表を 0 個 | **不採用** | **★`M32-01` が「本文でこう使う」と明記して置いた部品である**（器の CSS コメント）**。⇒ 使わなかっただけであり、消すのは器の設計判断を巻き戻す**（`E-4`）**。★スクリーンショットを入れたあとに表を足したくなる余地も残る** |
| **低-3** | `@import url('https://fonts.googleapis.com/...')`。オフライン閲覧・外部接続の観点 | **採用**（申し送り） | **★`M32-01` 由来で本サブの射程外だが、`P-57` で「zip へ同梱する」を採るなら同じ手番で決まる。⇒ §6-4b へ申し送りを足した** |
| **低-4** | 完了報告に `check-md-emphasis.sh <file>` の実行記録が無い | **採用** | **★実行はしていたが記録が無かった。⇒ §5.4 の末尾へ実行コマンドと出力を足した**（`D-775`） |

### 8.2 ★「高」指摘の不採用は 0 件である

**⇒ Phase C の安全弁（重大指摘の自動棄却）は発動していない。開発者へのエスカレーションは不要。**

### 8.3 ★レビューが独立に確かめたこと（製造の主張の裏取り）

| # | 内容 |
|---|---|
| 1 | **`DES-005` の失効した記述を写していないか、6 点を名指しで実物確認**（段数 8/6 ／ プリセット 3 つ ／ レシピトグルの新しい軸 ／ 既定表示列 ／ ドライブダメージの符号 ／ ゴミ箱の無期限）**。⇒ 6 点すべて新しい側で書けていた** |
| 2 | **画面数を独立に数え直し、`###` の画面節 24・生きている 22 で一致** |
| 3 | **退けた語 13 種を独自走査して全 0 件** |
| 4 | **`E-225`（消えた行）を `git diff` で独立検証**——130 deletions はプレースホルダ 88 行 ＋ CSS コメント ＋ 章名の置換のみ。**`<h3>つまずきどころ</h3>` が削除側に 1 行現れるのは差分整列の産物であり、`h3` は 22 章すべて 4 本（計 88）そろっている。⇒ 失われた行は無い** |
| 5 | **章ごとの密度を実測**——**段 5 の平均 1012 字 > 段 1 の 961 字**。**⇒ 指示書 §0.3 / D-5 が警告する「後半が薄くなる」型は踏んでいない** |

### 8.4 ★レビューが判断できないとした 1 件

**逐語**＝「**高-1 について、`DES-005` §5.13a の `CHANGE-165` 注記が『書出に上限が無い』と述べている意図が、バイト数とセットプレイ行数だけを指した省略表現なのか、`exportRowLimit` を見落とした記述なのかは、本レビューでは判断できない。設計卓の確認を要する。**」

**⇒ 製造も判断していない。§6-1b の CHANGE 請求として設計卓へ回した**（`D-293`＝自採番しない）。

---

*以上、M32-02 完了報告。*
