# M24-09d レビュー報告書

| 項目 | 内容 |
|---|---|
| 対象指示書 | `docs/instructions/M24-09d-test-execution-speedup.md` **v1.0.0** |
| チェックリスト | `docs/instructions/reviews/M24-09d-review-checklist.md` **v1.0.0** |
| 対象完了報告 | `docs/progress/M24-09d-completion-report.md` |
| 着手基点 / レビュー時 HEAD | `da53aaf` / `84d3e46`（作業ツリーはクリーン） |
| 実施日 | 2026-08-30 |
| 判定 | **高 2 件（うち 1 件は完了承認を妨げる重大＝チェックリスト §6-1）／ 中 2 件 ／ 低 5 件** |

> **★置き場の食い違い。** 本レビューの依頼は `docs/progress/m24-09d-review.md` を指定しており、チェックリスト §0.4 は `docs/progress/M24-09d-review-report.md` を指定している。**依頼側と先例（`m24-13-review.md`）に合わせて前者に置いた。**

---

## 総評

**テンプレート化そのものの作りは、本サブが最も恐れていた壊れ方（「何も検証しないまま緑になる」）に正面から答えている。** スキーマ一致テストは全テーブルの `sqlite_master` ＋ `table_info` ＋ `foreign_key_list` ＋ `index_list` ＋ `index_info` に加えて**全テーブルの行数**まで突き合わせ、比較相手は `db.Open` すら通していない。除外の観測は速度ではなく `MigrationRunCount()` である。**本レビューは指示書の数字（208 か所 / 79 本 / `Setup` の行数 / 除外対象の全数）を独立に数え直し、いずれも完了報告と一致することを確認した。**

**一方で、本サブは「回していない検査の結果を完了報告へ書いた」を再発させている。** 完了報告 §11 は `check-progress-log-index.sh` を「違反なし（索引行を追記したうえで回した）」と書いているが、**`docs/progress/progress-log.md` に `M24-09d` の索引行は 1 行も無く、いま回すと `違反 1 件` である。** これは `progress-log` 末尾の `M24-13` 教訓 8 と完全に同型であり、**その教訓が書かれているまさにそのファイルへの追記が落ちている。**

**もう 1 件、設計書側で見落としがある。** `DES-002` §11.3 の契約 (4) は **「★E2E は CI に載せない。★否定形で書いておかないと、次の担当が再実行で緑にして載せる」** と明文化されている（`D-499`）。本サブは nightly に E2E job を載せたが、**`CHANGE-145` の影響設計書は `SUPP-001` §5.5 / §4.5 のみで `DES-002` を含まず、完了報告 §8（as-built 差分 5 件）にも §13（申し送り）にも出てこない。** 契約が予告していた事態が、契約を誰も参照しないまま起きている。

**指示書 §4.4 の前提が再現しなかった件の扱いは適切である。** 4 通りの実験と `--listFilesOnly` の母数を出し、実測のほうを `web/CLAUDE.md` へ書き、判断を設計卓へ回している。**本レビューでも `web/tsconfig.json` が単一プロジェクト（`references` なし・`include: ["src","vite.config.ts"]`）であることを確認し、製造の結論が正しいことを裏取りした。**

---

## 設計準拠性レビュー結果

### A. 設計・パターンとの照合（チェックリスト §1）

| # | 観点 | 評価 | 所見（本レビューの独立確認） |
|---|---|---|---|
| 1 | `internal/infra/migration` がテンプレートを使っていないか | **◎** | `template_exclusion_test.go` の `TestMain` が `dbtest.DisableTemplate()` を呼ぶ。同パッケージの test ファイルは **27 本すべて `package migration_test`** で `TestMain` は**リポジトリ全体で 1 件**（`grep -rn "func TestMain" --include="*_test.go" .`）。衝突なし |
| 2 | 除外が効いていることが速度以外で観測されているか | **◎** | `dbtest.MigrationRunCount()`。テンプレート側 `TestTemplate_BuiltOnlyOnce`（`== 1`）と除外側 `TestTemplate_DisabledInThisPackage`（増分 `== 2`）の対。**同パッケージに `t.Parallel()` は 0 件**のため増分は決定論的 |
| 3 | §3.3-6（除外対象の全数）が母数付きで、探し方つきで報告されているか | **◎** | 9 軸の走査表あり。**本レビューが独立に `migration.Run(` / `MigrationsFS` / `golang-migrate` / `schema_migrations` / `user_version` を横断走査したところ、`internal/infra/migration/` と `internal/testutil/dbtest/` 以外のヒットは 0 件**。結論は一致 |
| 4 | `Setup(t)` の署名が変わっていないか | **◎** | `func Setup(t *testing.T) *sql.DB` / `func SetupWithPath(t *testing.T) (*sql.DB, string)` とも `da53aaf` と同一。呼び出し元 208 か所 / 55 ファイル（`git grep -n "dbtest\.Setup(" da53aaf` で再計数）は 1 か所も触っていない |
| 5 | `-wal` / `-shm` を持ち込まないか | **○** | `checkpointAndClose` が素の `sql.Open`（`db.Open` を使わない理由がコメントで明示）→ `wal_checkpoint(TRUNCATE)` → `Close`。その後サイドカーの実在を検査して**在れば `templateErr` に落とす**（黙って進まない）。**`-journal` は見ていない**（低-3） |
| 6 | 絞り込み用ターゲットが `make e2e` と環境変数を 1 か所から共有しているか | **◎** | `E2E_TARGETS := e2e e2e-only` に対して `export PW_EXECUTABLE_PATH ?=` を 1 度だけ書き、Chromium 解決も `ENSURE_PW_CHROMIUM` に 1 度だけ書いて両方が参照。**`E-76` の 2 か所持ちになっていない** |
| 7 | §3.3-9 の環境変数の全数が通っているか | **◎** | `Makefile` が `e2e` で設定するのは `PW_EXECUTABLE_PATH` の 1 つのみ（実査どおり）。DB パス・ポートは `playwright.config.ts` の `webServer.env` 側であり `Makefile` は持たない。**前提ターゲット `ensure-web-deps` も共有されている** |
| 8 | `make e2e` の挙動が変わっていないか | **○** | `make -n e2e` は「`ensure-web-deps` の展開 → Chromium 解決の `if` → `cd web && pnpm e2e`」で従来と同義。**ただし `ENSURE_PW_CHROMIUM` が行継続を畳むため、出力は逐語一致ではなく 1 行化されている**。完了報告 §12 の「展開結果が従来と同一」は「同義」の意味に読むべき（低-4） |
| 9 | §4.3 実施時に `workers` が `1` のままか | **◎** | `web/playwright.config.ts` は `git diff da53aaf` で **0 ファイル**。`workers: 1`（`:99`）／ `retries: 1`（`:100`）／ `reporter: "list"`（`:101`）。nightly のコメントが挙げる 3 値と実物が一致 |
| 10 | `pr-checks.yml` へ E2E が足されていないか | **◎** | YAML を実際にパースして job 一覧を確認＝`go-vet-build` / `go-test` / `web-test` の 3 本のみ。**差分はコメントのみ**（15 行追加 / 4 行削除） |
| 11 | `web/CLAUDE.md` の 1 行が §4.4 の内容と一致しているか | **△** | **一致していない。指示書の前提が再現しなかったため、実測のほうを書いた**（§8-1）。**扱いとしては正しい**（playbook §4.43）が、`followup §AR` の `tsc-noemit-excludes-test-files` は旧記述のまま残る＝設計卓の手番（中-2）。**`retrospective-log` へは書いていない**（`git diff` 0 ファイル）＝指示書どおり |
| 12 | `internal/testutil/` 以外の `internal/` が変わっていないか | **○** | 変更は `internal/infra/migration/template_exclusion_test.go`（新規・52 行・deletions 0）の 1 本のみ。**本番コードは 0 バイト**。指示書 §2.1 が「テンプレート方式のテスト（置き場は製造が決める）」を成果物に挙げており、`TestMain` の性質上そこにしか置けない。完了報告 §12 で明示済み |
| 13 | `internal/service/notation/` / `internal/service/preset/`（契約 F-1） | **◎** | `git diff --numstat da53aaf` に一切現れない |

### B. 数字の照合（チェックリスト §2・**本サブ固有の最重要節**）

| # | 観点 | 評価 | 所見 |
|---|---|---|---|
| 1 | 同一セッション内の背中合わせか。指示書の数字を母数にしていないか | **◎** | §1 に「指示書の 705 / 120 / 193 / 64.6 は母数として一切使っていない」と明記し、導入前 137.2 / 138.2 秒、導入後 43.2 / 42.4 秒をいずれも 2 回ずつ採っている。**warm 条件（計測前に 1 回空回し）も宣言されている** |
| 2 | ok パッケージ数・FAIL 数・**テスト本数**が不変か | **◎** | 完了報告は ok 55 / FAIL 0 / skip 5 / 1,660 → 1,664（+4）と報告。**本レビューが `go test ./... -count=1 -json` を独立に回した結果＝ok 55 / FAIL 0 / pass 1,659 ＋ skip 5 ＝ 1,664・exit 0 で一致。** さらに `comm` による集合差分（消えたテスト 0 行）は**構造的にも裏が取れる**——`git diff --numstat da53aaf` に既存 `*_test.go` が 1 本も現れないため、既存テストの削除・skip 化・緩和は物理的に起きていない |
| 3 | 削減量が **wall** で報告されているか | **◎** | wall を主指標に置き、Σ パッケージ elapsed（396.6 → 79.6 秒）は別量として並記している。**CPU の削減量だけで済ませていない** |
| 4 | §3.3-1 の食い違いの答えが報告されているか | **◎** | 「4 つは同一の実行から採られていない」＋「Σ elapsed > wall は `GOMAXPROCS`（実測 4）本のパッケージ並列による」と解いたうえで、同一実行では `internal/service/combo` 94.0 秒 < wall 137.2 秒で逆転が消えることを示している。**射程確定の根拠として成立している** |
| 5 | 「208 か所」「79 本」「12 行」の N が数え直されているか | **○** | 本レビューで再計数＝`dbtest.Setup(` **208 行 / 55 ファイル**、`dbtest.SetupWithPath(` **5 行 / 3 ファイル**、`migrations/*.up.sql` **79** / `*.down.sql` **79**、`Setup` 本体 **17 行**（`da53aaf:28-44`）。**すべて完了報告と一致。**「`dbtest.go` の行数 65 行」だけ実測 64 行（低-1） |
| 6 | 除外後の wall の床が報告されているか | **◎** | 実査 3 で 43.9 秒と予測し、導入後の実測で `internal/infra/migration` が 37.6 秒＝wall 43.2 秒のほぼ全部と確認。**本レビューの独立実測でも同パッケージが 34.0 秒で最長**（次点 5.7 秒）であり、床の構造は再現した |

### C. テストの妥当性（チェックリスト §3）

| # | 観点 | 評価 | 所見 |
|---|---|---|---|
| 1 | 既存テストが消えていない・skip されていない・緩められていないか | **◎** | 上記 B-2 のとおり構造的に確認。skip は 5 件のまま |
| 2 | スキーマ一致テストが全数で比べているか | **◎** | `sqlite_master`（type/name/tbl_name/sql）＋ 全ユーザーテーブルの `table_info` / `foreign_key_list` / `index_list` / `index_info`（**自動生成インデックス込み**、`pragma_index_list` 経由）。**「テーブルが在る」で済ませていない。**加えて**全テーブルの行数**と `characters != 0` の直接主張があり、「両方空だから一致した」が塞がれている——**これは指示書 §5.1-1 の要求を超えており、本サブが恐れていた壊れ方に対する正しい上積みである** |
| 3 | `sync.Once` が効いていることが固定されているか | **◎** | `TestTemplate_BuiltOnlyOnce`（`Setup` 3 回で `MigrationRunCount() == 1`） |
| 4 | `-wal` / `-shm` が付いてこないことが固定されているか | **○** | `TestTemplate_NoWalSidecarFiles`。**配られた DB 側は固定されているが、テンプレート構築側の番人（`buildTemplate` のサイドカー検査）は一度も赤くなっていない**（低-3） |
| 5 | 追加したテスト自身がテンプレートに依存していないか | **◎** | `openFreshlyMigrated` は `migration.Run` を自分で呼び、**`db.Open` も使わず素の `sql.Open`** で開く。ヘルパの共有経路まで断っている |

### D. 破壊確認（チェックリスト §4）

| # | 観点 | 評価 | 所見 |
|---|---|---|---|
| 1 | 破壊確認 1 で**複数**パッケージが赤くなったか | **◎** | 6 パッケージ中 6 が FAIL。スキーマ差異と行数差異の両方を名指しで出力（`want 529 行 / got 506 行`、`want 22 行 / got 21 行`）。**1 パッケージだけではない** |
| 2 | 破壊確認 2 の観測が実際に変わったか。除外の存廃が根拠付きで決まっているか | **◎** | 増分 2 → 1 に変わった。**★「赤くなったのは本サブが足した観測 1 本だけである／既存 99 テストは 1 本も赤くならない」と正直に書き、そのうえで残す判断を 3 根拠（`CHANGE-145` §2.1.2-3 の契約 ／ 後続テストの前提 ／ コスト実測 0.9 秒で wall に乗らない）で示している**。チェックリストが要求した形そのもの |
| 3 | 破壊確認 3 で `Executable doesn't exist` が出たか | **○** | (3b) で逐語再現。**ただし (3a)（`E2E_TARGETS` から外す）は `playwright install` の 403 で落ちており、期待していたメッセージとは別の落ち方である**。両方載せている点は誠実だが、(a)/(b) の壊し方の説明が 1 行ずつしかなく、読み手が (b) の具体を再現できない（低-5） |
| 4 | 破壊確認 4 | **○** | CI 上では実行手段が無い（`workflow_dispatch` / `schedule` は既定ブランチのワークフロー定義しか起動しない）。**ローカルで job の打つシェル構文（`set -o pipefail` ＋ `tee`）を回して非ゼロ伝播と Job summary の抽出まで検証したうえで、「CI の検出器として機能することを観測した」とは書かないと明記している**。チェックリスト §4-4 は軽微扱いであり、分岐点も書かれている |
| 5 | 4 件ともコマンドと出力が載っているか | **◎** | すべて載っている。各件に「撤去後 `git diff` = 0 ファイル」まで添えてある（作業ツリーのクリーンさは本レビューでも確認済み） |
| 6 | 破壊確認後の確定実行が絞られていないか | **◎** | §10 の確定実行は `go test ./... -count=1`（絞っていない・exit 0）と `make e2e`（全数 215） |

### E. 基準値と実行の作法（チェックリスト §5）

| # | 観点 | 評価 | 所見 |
|---|---|---|---|
| 1 | 着手前の `make e2e` を採っていないか | **◎** | 採っていない（`D-594` / `D-600` どおり） |
| 2 | E2E の件数の増減を主張していないか | **◎** | 「全数緑（215 件）」とだけ書き、増減を主張していない |
| 3 | 直前サブの全数緑の確認が書かれているか | **◎** | `M24-13` 完了報告の「215 passed / 0 failed」を確認した旨を明記 |
| 4 | `pnpm build` が回されているか | **◎** | §10 に記録あり。**本レビューでも `pnpm build` を独立に回して exit 0（6.43 秒）を確認** |

### F. ドキュメント・進捗ログ（チェックリスト §6）

| # | 観点 | 評価 | 所見 |
|---|---|---|---|
| 1 | `progress-log.md` へ追記されているか | **×** | **追記が無い。完了報告 §11 の「違反なし」は現物と食い違う**（高-1） |
| 2 | 完了報告に `git diff --numstat` が貼られているか | **◎** | 貼られており、**本レビューの実測と 1 行も違わない。新規 2 ファイルの deletions は 0**（`E-225` の観点で確認済み） |
| 3 | 実査 12 件の答えが母数付きで載っているか | **◎** | 12 件すべてに母数と走査コマンドが付いている |
| 4 | テンプレート化の形の複数案と選定理由 | **◎** | 保存先 3 案 ／ 除外手段 4 案 を採否と理由つきで提示。**`sync.Once` の失敗を `templateErr` に残す判断の理由まで書かれている** |
| 5 | `CHANGE-145` の番号を消費していないか | **◎** | `docs/change-notes/` に新規ファイル 0（`git diff --name-only da53aaf -- docs/` は完了報告 1 本のみ） |
| 6 | as-built が指示書と違った箇所が報告されているか | **△** | 5 件は報告されている。**`DES-002` §11.3 契約 (4) との矛盾が抜けている**（高-2） |

---

## 設計準拠性以外の指摘事項

### コーディング規約・実装品質

- **`internal/testutil/dbtest/dbtest.go`**: エラーはすべて `fmt.Errorf(... %w ...)` で wrap、公開シンボル（`DisableTemplate` / `MigrationRunCount`）に godoc 付き、`panic` なし、`fmt.Println` / `console.log` なし。**CLAUDE.md §4 の Go 規約に違反なし。**`go vet ./...` / `go build ./...` とも通る（本レビューで実行）。
- **並行安全性**: `templateBytes` / `templateErr` は `sync.Once` の happens-before で保護され、`templateDisabled` / `migrationRuns` は `atomic`。**`DisableTemplate()` は「最初の `Setup` より前」でなければ効かないという制約が godoc に明記されており、唯一の正しい位置（`TestMain`）も書かれている。**
- **`os.WriteFile(dbPath, templateBytes, 0o644)`**: マジックナンバーだが、直前に「SQLite が自分で作るときの既定と揃えている（umask 適用前）」と根拠コメントがある。**定数化までは不要と判断する。**
- **新規依存 0**（`go.mod` / `package.json` に差分なし）。`modernc.org/sqlite` の blank import は既存依存。
- **`.github/workflows/nightly-crossbuild.yml`**: action の版（`checkout@v7` / `setup-go@v7` / `setup-node@v7` / `cache@v6` / `pnpm/action-setup@v6` / `upload-artifact@v7`）は `pr-checks.yml` および既存 `crossbuild` job と**すべて一致**。YAML は実際にパースして job 2 本（`crossbuild` / `e2e`）を確認。`timeout-minutes: 30` あり。`E-125`（件数が無い報告）対策として Job summary が「読めなかった」と「0 件だった」を書き分けている点は良い。

### セキュリティ

- 認証・秘密情報・ブラウザストレージへの影響なし（`check-browser-storage-keys.sh` 違反なし・新キー 0）。
- ワークフローに `permissions: contents: read` が workflow レベルで宣言済み。新 job は追加の権限を要求していない。**`pull_request_target` 等の危険な trigger は使っていない。**

### 機械検査の再実行結果（本レビューで独立に実行）

| 検査 | 結果 |
|---|---|
| `scripts/check-artifact-integrity.sh` | **違反なし** |
| `scripts/check-md-emphasis.sh` | **436 / 436・増加なし** |
| `scripts/check-doc-refs.sh` | **dead reference なし** |
| `scripts/check-enum-sync.sh` | **ベースラインどおり** |
| `scripts/check-browser-storage-keys.sh` | **違反なし** |
| **`scripts/check-progress-log-index.sh`** | **★NG 1 件**（完了報告の「違反なし」と食い違う） |
| `go test ./... -count=1` | **ok 55 / FAIL 0 / 1,664 テスト / exit 0 / wall 38.3 秒** |
| `cd web && pnpm build` | **exit 0** |

---

## 推奨修正（優先度別）

### 高（M24 完了前に修正必須）

- **高-1. `docs/progress/progress-log.md` に `M24-09d` の索引行が無い。しかも完了報告 §11 が「違反なし（索引行を追記したうえで回した）」と書いている。**
  - 実測: `git diff --numstat da53aaf` に `progress-log.md` が現れない ／ `grep -n "M24-09d" docs/progress/progress-log.md` が 0 件 ／ `bash scripts/check-progress-log-index.sh` → `NG 作業 ID m24-09d が docs/progress/progress-log.md に現れない` ／ `結果: 違反 1 件`。
  - 該当: チェックリスト §6-1（**重大**）／ 指示書 §7.4 ／ `CLAUDE.md` §8（**「指示書が明示的に求めていなくても追記する」**）。
  - **★これは `M24-13` 教訓 8（回していない検査の結果を完了報告へ書いた）と完全に同型であり、その教訓が書かれているファイルそのものへの追記が落ちている。**「検査表は 1 行ずつ実際の出力から埋めること」という自分たちの結論が守られていない。
  - **★副次的な帰結**——`D-602` が単価表を撤回した根拠の 1 つは「**同じ情報が完了報告 §7.2 と `progress-log` の索引行に既に残っている**」であった。索引行が無い以上、その前提の片方が成立していない。
  - **対応**: 索引行を追記し（`.claude/commands/implement_plan.md` §完了時 の形式）、**再度検査を回して出力から §11 を埋め直す。是正の記録は消さずに残すこと**（`M24-13` の先例）。

- **高-2. `DES-002` §11.3 の契約 (4)「★E2E は CI に載せない」が as-built と正面から矛盾しており、どこにも報告されていない。**
  - 逐語（`docs/design/02-architecture.md` §11.3「契約」4）: **「★E2E は CI に載せない。載せる前提の設定が未適用であり、受入条件も未達である。★否定形で書いておかないと、次の担当が再実行で緑にして載せる。」**（`D-499` / `D-480` で意図的に否定形として明文化されたもの）
  - 本サブは `nightly-crossbuild.yml` に `e2e` job を足した。**nightly は CI である。**
  - **`CHANGE-145` の影響設計書欄は `SUPP-001` §5.5 / §4.5 のみで、`DES-002` を含まない。** 完了報告 §8（as-built 差分 5 件）にも §13（申し送り 5 件）にも `DES-002` は 1 度も出てこない。
  - **同節の表の行「nightly クロスビルド ｜ Windows / macOS / Linux 3 OS 向けのクロスビルド」も失効している**（job が 2 本になり、ワークフロー名も `Nightly` へ変わった）。`DES-002` §12 末尾の「CI は本フェーズでは構築せずローカル実行」も同じ面。
  - **★本プロジェクトの較正では「撤回済み・失効した記述が残っている」は高である**。通常のテストでは検出できず、後任は本文をコピーして注記を読まない。**しかも今回は、契約自身が「次の担当が載せる」と予告していた事態がそのまま起きている。**
  - **★同型の先例**: `M24-13` 横断課題 1（**「影響設計書」の欄は起票時の見込みであって全数ではない。as-built で設計書を直接 `grep` して数え直すこと**）／ `M24-12` 教訓 14・23。
  - **対応（製造の手番）**: 完了報告 §8 へ 6 件目として、§13 へ申し送りとして **「`DES-002` §11.3 契約 (4) と §11.3 の表、および §12 末尾が as-built で失効した」** を追記し、**設計伝達レポート §4 へ「`CHANGE-145` の影響設計書に `DES-002` §11.3 / §12 を足す必要がある」を出すこと**。**設計書本体の改訂は設計卓の手番であり、製造は触らない**（`CLAUDE.md` §8）。**失効箇所を挙げる節には使った `grep` の出力を添えること**（`M24-12` 教訓 23）。

### 中（M25 着手と並行可）

- **中-1. 除外パッケージが増えても誰も気づかない。`DisableTemplate()` の呼び出し箇所を固定する検査が無い。**
  - 現状、除外の一覧は「`grep -rn "dbtest.DisableTemplate()"` を打つ人が居るかどうか」に依存する。`dbtest.go` の godoc と `template_exclusion_test.go` のヘッダに「現時点の該当は 1 パッケージのみ」と書いてあるが、**書いただけでは守れない**（`M24-12` 教訓 23）。
  - `CHANGE-145` §5 のリスク 2 は「除外すべきパッケージを見落とす」だが、**逆向き（除外すべきでないパッケージが除外される）に対する検出器が無い。**通常パッケージが `DisableTemplate()` を呼んでも**テストは全部緑のまま遅くなるだけ**であり、本サブが定義した「速度は根拠にならない」の裏返しがそのまま当てはまる。
  - **対応案（形は裁量）**: (a) `scripts/` に `DisableTemplate()` の呼び出しが `internal/infra/migration` の 1 か所であることを固定する検査を足す（`CLAUDE.md` §8 の常設検査群と同じ姿勢）／ (b) `dbtest` 側で許可パッケージ名を持ち、それ以外からの呼び出しを落とす。**★本サブでやるべきとまでは言わない。設計卓へ候補として出せば足りる。**

- **中-2. `make e2e-only` の存在を知る導線が無い。**
  - `D-599` の眼目は「**文書を読まなくても正しい形になる**」であり、ターゲットを作った点は正しい。**ただし、罠に落ちる人は「絞り込み実行の方法」を探す人であり、その人が `Makefile` を開くとは限らない。**
  - `CLAUDE.md` §5 のテスト規約表（E2E の行）と §11「開発時に頻用するコマンド」、`README.md` のいずれにも `e2e-only` は 1 度も現れない（`grep` 済み）。`.claude/commands/add_e2e_spec.md` も `make e2e` しか書いていない。
  - **対応**: `CLAUDE.md` §5 の E2E 行か §11 のコマンド一覧へ 1 行（`make e2e-only P=<パターン>`）。**★`CLAUDE.md` は製造セッションで自動ロードされるため、ここに 1 行あるだけで到達率が変わる**。なお `web/CLAUDE.md` §3 は型検査の話であり、置き場としては合わない。
  - **★あわせて**: `followup §AR` の `playwright-narrow-run-env-trap` は解消済みだが、状態列が「★起票済」のままである（設計卓の手番）。

### 低（将来対応）

- **低-1.** 完了報告 §2 実査 4 の「`dbtest.go` の行数 **65 行**」は実測 **64 行**（`git show da53aaf:internal/testutil/dbtest/dbtest.go | wc -l`）。他の N（208 / 55 / 5 / 3 / 79 / 17）はすべて一致しているだけに、この 1 つだけが浮く。
- **低-2.** `dbtest.go` のパッケージコメント「**マイグレーションは 79 本あり、1 回あたり実測 0.45 秒かかる**」は陳腐化が確定している——**指示書 §14 が「消費するなら次は `000080`」と書いており、次のマイグレーションで 79 は誤りになる。**0.45 秒も測定環境（クラウドコンテナ・`nproc` 4）依存であり、`D-602` が単価表を撤回した理由の 1 つ（「測ったのはクラウドコンテナであり devContainer や CI では別の値になる」）がそのまま当てはまる。**⇒ 本数と秒数を落として「マイグレーションの本数に比例して積み上がる」等の性質だけを残すか、日付と環境を添えること。**（**単価表の新設には当たらないため、`D-602` 違反ではない。**）
- **低-3.** `buildTemplate` のサイドカー検査は `-wal` / `-shm` の 2 種のみで、**`-journal`（rollback journal・現在の `journal_mode=delete` で使われる形式）を見ていない**。実際には `migration.Run` が正常終了していれば残らないため実害は薄いが、番人としての射程は片手落ちである。**あわせて、この番人自身が一度も赤くなっていない**（破壊確認 4 件のいずれもここを通らない）。
- **低-4.** 完了報告 §12 の「`make -n e2e` の展開結果が従来と同一」は逐語一致ではない。`ENSURE_PW_CHROMIUM` の行継続が畳まれるため、出力は 1 行化される（**シェルとしては同義**）。「同一」ではなく「同義」と書くほうが後任を誤らせない。
- **低-5.** 破壊確認 3 の (3a) / (3b) の壊し方の説明が各 1 行で、(3b)「共有そのものを外す」が具体的に何を外したのか読み手には再現できない。**期待していた `Executable doesn't exist` が出たのは (3b) だけであり、(3a) は `playwright install` の 403 で別の落ち方をしている**。どちらも載せた点は誠実だが、(3a) は「環境変数が渡らない罠」の再現ではないことを 1 行添えるとよい。
- **低-6.** 完了報告の wall が §3（43.2 / 42.4 秒）と §10（38.2 秒）で割れており、§0 の見出しは 43.2 を使っている。**本レビューの独立実測は 38.3 秒**で §10 側に一致した。**2 つが別条件（背中合わせ計測 / 確定実行）であることは読めばわかるが、見出しの数字がどちらかを 1 行で断っておくと引用時にずれない。**
- **低-7.** レビュー報告書の置き場が、依頼（`docs/progress/m24-09d-review.md`）とチェックリスト §0.4（`docs/progress/M24-09d-review-report.md`）で食い違っている。**本書は依頼と先例（`m24-13-review.md`）に従った**。チェックリスト側の是正は設計卓の手番。

---

## 良かった点

1. **★★「テストが緑である」を根拠にしない形が、テストの構造として実現されている。** `TestTemplate_SchemaMatchesFreshMigration` は指示書 §5.1-1 が求めたスキーマ全数に加えて、**自発的に全テーブルの行数と `characters != 0` を主張している**。理由も明記されている——「スキーマだけを比べると seed が 1 行も入っていないテンプレートが緑で通る／両方空だから一致した、を排除する」。**これは本サブが最も恐れていた壊れ方に対する正面からの回答であり、指示書の要求を超えている。**
2. **★比較相手が `db.Open` すら通っていない。** チェックリスト §3-5（テンプレートを検証するテストがテンプレートを使うと一緒に壊れる）に対して、`openFreshlyMigrated` は `migration.Run` を自分で呼び、素の `sql.Open` で開く。**「同じ壊れ方を共有しない」がヘルパの層まで貫かれている。**
3. **★消えたテストを件数の一致ではなく集合の差分（`comm`）で示した。** 本レビューは独立に `go test -json` を回して ok 55 / FAIL 0 / 1,664 を再現し、さらに `git diff --numstat` に既存 `*_test.go` が 1 本も現れないことで**構造的にも**裏を取れた。
4. **★★破壊確認 2 で不都合な事実を自分から書いた。** 「赤くなったのは本サブが足した観測 1 本だけで、既存 99 テストは 1 本も赤くならない」と明記したうえで、除外を残す判断を 3 つの根拠（契約 ／ 後続テストの前提 ／ コスト 0.9 秒で wall に乗らない）で立てている。**チェックリスト §4-2 が求めた形そのもので、しかも自分に不利な側から書いている。**
5. **★★破壊確認 4 で「観測した」と書かなかった。** 「ローカルで同じコマンドが赤くなることと、GitHub のランナーでジョブが赤くなることは別の主張である」と明示し、成立しなかった分岐点（`workflow_dispatch` は既定ブランチの定義しか起動できない）を書いた。**`D-594` の「報告は主張であって観測ではない」を、自分の報告にも適用している。**
6. **★指示書 §4.4 の前提を鵜呑みにせず、4 通りの実験と `--listFilesOnly`（テストファイル 198 本）で反証し、実測のほうを書いた。** 本レビューでも `web/tsconfig.json` が単一プロジェクト（`references` なし・`include: ["src","vite.config.ts"]`）であることを確認し、**結論が正しいことを裏取りできた**。しかも運用上の帰結（`web/e2e/` はどちらの経路でも型検査されない＝`SUPP-001` §4.5 の積み残しが生きている）まで分離して書いている。
7. **★`sync.Once` の失敗を握り潰さない設計。** 「失敗を握り潰すと『テンプレートが壊れたまま全テストが走る』状態になる」という理由つきで `templateErr` に残し、以後すべての `Setup` を落とす。**サイドカーが残っていたら `Setup` を落とす番人も同じ思想。**
8. **★`E2E_TARGETS` / `ENSURE_PW_CHROMIUM` による 1 か所共有。** `E-76` を名指しし、**新しい E2E ターゲットを足すときは `E2E_TARGETS` へ名前を足すこと。レシピ側へ export を書き足さない**という後任への指示までコメントに入っている。`define` を採らなかった理由まで書かれており、後任が同じ検討をやり直さずに済む。
9. **★指示されていない失効記述を自発的に是正した。** `pr-checks.yml` の E2E 除外理由が `workers: 1` の適用（`CHANGE-135`）で既に失効していたことを実査で見つけ、旧理由を残したまま現在の理由へ差し替えている（**E2E の実体は足していないことも `grep` で確認済み**）。`README.md` 3 か所の追随も同様。
10. **★`git diff --numstat` を貼り、新規 2 ファイルの deletions が 0 であることを自分から明示した**（`E-225`）。**本レビューの実測と 1 行も違わない。**

---

## 制約事項

- 本レビューはコード上で判定可能な範囲のみ対象。実際の動作確認・パフォーマンス・実機テストは別途実施が必要。
- **`make e2e` は回していない**（4 分かかるうえ同時実行できない資源であるため、レビュー依頼で禁じられている）。**⇒ E2E 全数緑（215 件）は完了報告の主張として受け取っており、本レビューは検証していない。**
- **nightly の `e2e` job が GitHub ランナーで成立するかは、本レビューでも検証できない。** ワークフロー定義の静的な妥当性（YAML パース・job 構成・action 版・`permissions`・`timeout-minutes`）までを確認した。**初回実行の結果を見る手番が残る**（完了報告 §13-2 の申し送りどおり）。
- 破壊確認 4 件は、レビュー担当がコードを変更できないため**再実行していない**。完了報告に載ったコマンドと出力、および撤去後の `git diff` = 0 の記載を根拠として評価した。
- 「不明」として残る点は無い。

---

*以上、M24-09d レビュー報告書。* **★本サブの実装そのもの（テンプレート化・除外・観測・テスト）は、本サブ固有の危険に対して正しく作られている。** **★是正が要るのは報告と記録の側である**——**`progress-log` の索引行が落ちたまま「違反なし」と書かれていること（高-1）と、`DES-002` §11.3 の契約が as-built で失効したことが誰にも伝わっていないこと（高-2）。** **どちらも動作は正しいままであり、テストも lint も型検査も緑である。人が読む以外に見つける経路が無い。**

---

## 取り込み結果（自動トリアージ）

**判定: 高 2 / 中 2 / 低 7 の計 11 件。採用 9 件・設計卓へ回した 2 件・不採用 0 件。**
**★「高」の不採用は 0 件である**（エスカレーション不要）。

### 取り込み後の再実行（同一セッション）

`go test ./... -count=1` → **exit 0 / wall 39.7 秒 / ok 55 / FAIL 0 / 1,664 テスト / skip 5 / 導入前比で消えたテスト 0 本。**

| # | 指摘 | 採否 | 理由と対応 |
|---|---|---|---|
| **高-1** | `progress-log` に索引行が無いのに完了報告 §11 が「違反なし」と書いている | **★採用** | **指摘のとおりであり、こちらの誤りである。** 索引行を追記し（`progress-log.md:5129`）、検査を回し直して `違反なし`（検査 65 件 / ALLOW 除外 10 件）を確認した。**★完了報告 §11 は「違反なし」へ書き換えず、初版が回さずに書いたことと実測が `違反 1 件` であったことを残したうえで是正を追記した**（`M24-13` の先例）。**★索引行そのものも `grep` で目視確認した**（本検査は偽の緑を返しうる＝followup §AH） |
| **高-2** | `DES-002` §11.3 契約 (4)「E2E は CI に載せない」が as-built で失効し、どこにも報告されていない | **★採用** | **指摘のとおり。** 完了報告へ **§8-6** を新設し、失効箇所 4 か所を**実際の `grep` 出力つき**で列挙した（`M24-12` 教訓 23）。**§13-6** として設計伝達レポート §4 向けの申し送り（`CHANGE-145` の影響設計書欄へ `DES-002` §11.3 / §12 を足す）を追加。`progress-log` の横断課題 1 にも 1 行残した。**★設計書本体は触っていない**（`CLAUDE.md` §8。改訂は設計卓の手番） |
| **中-1** | 除外の逆向き（除外すべきでないパッケージが除外される）に検出器が無い | **★採用**（レビューは「本サブでやるべきとまでは言わない」としていたが、取り込んだ） | **本サブの主題そのもの（静かに壊れる面）であるため取り込んだ。** レビューの案 (b) を採り、`DisableTemplate()` が `runtime.Caller` で呼び出し元パッケージを見て許可一覧（`templateExemptPackages`）に無ければ落とす形にした。**★実際に発火することを確かめてから採用した**（完了報告 §4.3.1 に出力を掲載）。**★案 (a)（新しい `scripts/check-*.sh`）は採らなかった**——`check-artifact-integrity.sh` が各検査に自己検査を要求しており、検査機構そのものへ射程が広がる（§1.6-5） |
| **中-2** | `make e2e-only` の導線が `Makefile` 本文以外に無い | **★採用** | `CLAUDE.md` **§11**（頻用コマンド）へ 2 例と罠の注記を、**§5**（テスト規約表の E2E 行）へ 1 行を足した。**★`CLAUDE.md` は製造セッションで自動ロードされるため到達率が変わる**というレビューの理由をそのまま採った。**followup `§AR` の状態列の是正は設計卓の手番**であり、完了報告 §13-7 へ回した |
| **低-1** | `dbtest.go` の行数が 65 行ではなく 64 行 | **★採用** | 完了報告 §2 実査 4 を **64 行**へ訂正 |
| **低-2** | パッケージコメントの「79 本」「0.45 秒」は陳腐化が確定している | **★採用** | 本数と秒数を落とし、「**適用コストは本数に比例して積み上がる**」という性質と「**実測値をここへ書かないこと。数字の正本は完了報告である**」に置き換えた（`D-602` と同じ理由） |
| **低-3** | サイドカーの番人が `-journal` を見ていない | **★採用** | `buildTemplate` の検査へ `-journal` を足して 3 種にした。**現在の `journal_mode` は `delete` であり、残る形としてはむしろこちらのほうが近い** |
| **低-4** | 「`make -n e2e` の展開結果が従来と同一」は逐語一致ではない | **★採用** | 完了報告 §12 を「**シェルとして同義**（★逐語一致ではない——行継続が畳まれて 1 行化される）」へ訂正 |
| **低-5** | 破壊確認 3 の (3a) / (3b) の壊し方が再現できない | **★採用** | 完了報告 §9 の (3a) / (3b) に**何を外したかを具体的に**書き、**(3a) は `Makefile` の番人が先に止めるため「環境変数が渡らない罠」そのものの再現ではない**ことを 1 行添えた |
| **低-6** | wall が §3（43.2 秒）と §10（38.2 秒）で割れている | **★採用** | §3 の直後に「**前者は背中合わせ計測・後者は確定実行であり、別条件。引き算しないこと。導入前との比較に使うのは §3 の側**」と断りを入れた |
| **低-7** | レビュー報告書の置き場が CLI とチェックリスト §0.4 で食い違う | **設計卓へ回した**（本サブでは是正しない） | **製造 CLI とチェックリストの雛形のどちらを正本にするかは、製造の裁量の外である。** 本サブは CLI と先例（`m24-13-review.md`）に従っており、**現物を動かすと逆に先例から外れる。** 完了報告 §13-8 へ申し送りとして記録した |

### ★設計卓へ回した 2 件（**不採用ではなく、手番が製造に無いもの**）

- **低-7**（上表のとおり）。
- **中-2 の後半**——`followup §AR` の状態列（`playwright-narrow-run-env-trap` は解消済みだが「★起票済」のまま ／ `tsc-noemit-excludes-test-files` は記述そのものが現物と食い違う）。**製造が直接書けるのは followup の §J だけである**（`D-382`）。完了報告 §13-7 へ記録した。

### ★停止規律

**再レビュー往復は 0 回で完了した**（初回レビューの全指摘を 1 巡で処理し、未解消の指摘は無い）。**⇒ `followup-backlog.md` §J への停止時記録は発生していない。**
