# M31-04 完了報告 — 確定反撃でドライブリバーサルを扱う（`SM-098`）

| 項目 | 内容 |
|------|------|
| 作業 ID | **M31-04** |
| 指示書 | `docs/instructions/M31-04-drive-reversal-punish.md` **v1.0.0** |
| チェックリスト | `docs/instructions/reviews/M31-04-review-checklist.md` **v1.0.0** |
| 実施日 | 2026-09-10 |
| 着手基点 | `0660fa2` |
| 消費マイグレ | **`000109`**（層 B＝`000109_data_seed_drive_reversal_system_move`） |
| 消費 CHANGE | **0 本**（★製造は自採番しない＝`D-293`。請求は設計伝達レポート §4 へ） |
| レビュー報告 | `docs/progress/m31-04-review.md`（**指摘 9 件＝高 3 / 中 3 / 低 3。★不採用 0 件**） |

---

## 0. 結論

**ドライブリバーサルを全キャラ 1 行ずつの system move として投入し、確定反撃のブロックタブに有利フレーム `+6` で出るようにした。ジャストパリィタブからは明示除外した。入力面（仮想コントローラ）へは出していない。**

---

## 1. ★★段 1 — 実査（コードを書く前に実施）

**母集団＝`moves` テーブル 3026 行 ／ sf6 キャラ 31 ／ うち `category='system'` は 310 行**（10 code × 31）**。**
**★`character_data/*.csv` ではなく DB 実測である**（`SUPP-001` §5.5.3.2）**。測り方＝マイグレ HEAD を適用した新規 DB へ問い合わせた。**

| # | 測ったこと | 結果 |
|---|---|---|
| **1** | **`moves` に `drive_reversal` 相当の行が在るか** | **0 件**（`code LIKE '%reversal%'` = 0）**。⇒ category も無い。段 2 の作業は減らなかった** |
| **2** | **確定反撃の走査が、どの述語で技を拾っているか** | **リポジトリ側は絞っていない**——`listMovesForScanSQL`（`internal/repository/punish/queries.go`）は当該キャラの `moves` を全投影する。**除外はサービス層 `punishfinder.isNonPunishableTarget` の 2 条件だけ**＝`is_aerial=1` ／ `category='system' ∧ code ∈ movementSystemCodes`（移動 9 code）**。⇒ system を一律には除かない**（`drive_parry` が既に走査対象として通っている） |
| **3** | **`M30-01` の共通技タブ 13 code に D リバが入っていないこと** | **入っていない**（実物 `COMMON_MOVE_CODES` が `CHANGE-170` §2.2 と一致）**。⇒ 帰結を §2 に書いた** |
| **4** | **`preset_aliases` に「ドライブリバーサル」の別名が在るか** | **0 件**（`alias_text LIKE '%リバーサル%'` = 0）**。★`official_ja_move` は投入前の時点で `moves` と 1:1 だった**（3026 / 3026） |
| **5** | **母集団を 1 行書くこと**（`M-145` の対処） | **上の見出しに書いた** |

### 1.1 ★★指示書に書かれていなかった関門が 4 つ在った（**§4.3 が警告していた形そのもの**）

**★述語（1-2）を通っても、そのあとに 4 つのふるいが在る**（`punishfinder` の pass 1）**。**

| ふるい | 落ちると |
|---|---|
| `damage = 0` | **完全除外**（画面に出ない） |
| `damage IS NULL` | 手動確認レーン（`unknown_damage`） |
| `is_projectile = 1` | 手動確認レーン（`distance_dependent`） |
| `on_block IS NULL`（block タブ） | 手動確認レーン（`data_missing`） |

**⇒ `on_block = -6` を入れただけでは成立レーンに出ない。`damage` を非 NULL かつ ≠0 にする必要があった。**

### 1.2 ★`damage` の一次源はプロジェクト内に在った（チェックリスト E-1）

**`DES-002` §4**（`docs/design/02-architecture.md:1000`）**が、取込判別の記述として次を持っている。**

> ドライブリバーサルはダメージ 500>0 のためダメージ値では非攻撃と判別できず、名称・位置で除外する。

**⇒ `damage = 500` の根拠はこれである。SF6 の値を推測で入れていない。**

### 1.3 ★★段 1-3 の帰結＝行を入れると自動的に「未分類」タブへ並ぶ

**`isControllerSurfaced()`**（`web/src/features/combo/moveSurfacing.ts`）**は 7 面の論理和であり、未分類タブはその否定である**（`CHANGE-170` §2.4）**。**

**★`drive_reversal` はどの面にも当たらない。⇒ 行を入れた時点で未分類タブへ並び、コンボのステップとして押せるようになる。**

**★★現状 `category='system'` の 10 code はすべて共通技タブの 13 に含まれており、未分類へ落ちる system move は 1 件も無い。⇒ D リバが最初の 1 件になる。**

**⇒ 「何もしない」は「出さない」にならない。これを段 4 の判断材料として開発者へ出した。**

### 1.4 ★`SM-098` は「要件」なのか「画面」なのか（指示書 §4.4 ／ チェックリスト D-2）

**`REQ-001` で確定反撃を扱っているのは `NFR406`**（`docs/design/requirements.md:370`）**＝「相手の技をガード／ジャストパリィした後の隙に当てる技と、そこから発展するコンボの記録」。**

**★D リバは「相手の技」であり、要件文を変えずに収まる。⇒ `REQ-001` への CHANGE 請求は要らない。**

**⇒ 請求するのは `DES-002` §4 の追随だけである**（`CHANGE-169` が既に「必要が生じた」を記録済みであり、本サブはその as-built に当たる）**。**

### 1.5 その他の実測

| # | 実測 |
|---|---|
| 1 | **`csv_db_sync_test` は CSV 行を起点に突合する**（`checked = 2743` は CSV 由来）**。⇒ DB 側にだけ在る行は対象外であり、CSV 不可触の方針と整合する** |
| 2 | **作業ツリーの HEAD は `000107` であり、`000108` は本ツリーに存在しない**（並列サブが消費する番号として払い出されている）**。⇒ 契約テストの比較区間の始端は実在する `000107` を使った** |
| 3 | **前例の `drive_parry`**（全キャラ 1 行の system move）**の持ち方＝`category='system'` / `on_block=NULL` / `recovery=33` / `startup=1` / `total=45` / `damage=0`** |

---

## 2. 開発者判断（2026-09-10・段 1 の報告と一緒に 1 回で出した＝指示書 §7）

| # | 事項 | 回答 |
|---|---|---|
| **1** | **段 3＝ジャストパリィ（27F）の表現** | **案 (b) 入れるが走査から外す** |
| **2** | **段 4＝入力面に出すか** | **出さない＝明示除外を入れる** |

**★★段 3 で案 (a)**（列に入れない）**を採らなかった理由を実測で示した**——**(a) は「走査に出ない」にならない。`recovery` を NULL のまま残すと、JP タブの手動確認レーン（`reason=data_missing`）に出る。⇒ 事実を捨てたうえに画面へ出る、という組み合わせになる。**

**★★製造は独断で決めていない**（チェックリスト D-1）**。⇒ 実測を添えて案を出し、回答を得てから実装した。**

---

## 3. 段 2 — 投入の形

**★★既存の system move と同じ「全キャラ 1 行ずつ」の形に揃えた＝31 行**（`migrations/000025_seed_movement_system_moves_all.up.sql` と同型）**。設計卓の案どおりであり、段 1 の実測でも変える理由は出なかった。**

**★`000025` は 9 code を入れるため値リストを `CROSS JOIN` しているが、本サブは 1 code なので値リストが要らず `characters` を直接走査している。⇒ 結果の形は同じだが、`git grep "CROSS JOIN"` では `000109` は出ない**（レビュー 低-1 で是正）**。**

| 列 | 値 | 一次源 |
|---|---|---|
| `code` | `drive_reversal` | — |
| `category` | `system` | **`drive_parry` の前例に揃えた**（全キャラ一律の共通システム技） |
| `damage` | **500** | **`DES-002` §4**（`02-architecture.md:1000`） |
| `on_block` | **-6** | **開発者の逐語**（2026-09-08）「一律ガードフレームが-6になる」 |
| `recovery` | **27** | **開発者の逐語**「ジャストパリィは27F」**。★JP 後の有利フレームは `recovery` 列そのものである**（`internal/model/punish.go`） |
| `startup` / `active` / `total` / `on_hit` | **NULL** | **一次源が無いため入れていない。★走査には不要であり、推測値を入れると後から「実測か推測か」が分からなくなる** |
| `is_projectile` / `is_aerial` / `setup_only` / `fastest_unreachable` | **0**（DEFAULT） | — |

**alias**: `official_ja_move` の「ドライブリバーサル」を**対で**投入した（31 件）。**★入れないと表示が生 code へフォールバックする**（000025 の注記。確定反撃の相手技名は `officialJaPresetSubquery` 経由で解決される）**。**

**マイグレ名**: **`000109_data_seed_drive_reversal_system_move`**。**★`_data_` を入れた**（層 B＝`CC-BY-SA-4.0`。`CHANGE-158`）**。★`down` も書いた**（`SUPP-001` §5.5.4。alias → moves の逆順） **。★番号は自採番していない**（`D-800` の払い出し）**。**

### 3.1 ★★`000025` を写して踏んだ穴（**テストで拾った**）

**★`preset_aliases.character_id` を入れ忘れた。**

**★★`000025` は `000074`**（`character_id` を非正規化で追加）**より前のマイグレであり、同列を持たない。⇒ その形をそのまま写すと NULL の行が 31 件生まれる。**

**★NULL でも `INSERT` は通り、alias は表示にも出る。⇒ 動作では気づけない。** **★`000075` の UNIQUE 索引 2 本の構成列でもあり、NULL だと一意性が効かない。**

**★気づいた経路は `internal/repository/preset` の「`character_id` が NULL の行が 0 件」テストだけであった。⇒ 是正し、本サブの契約テストにも同じ主張を足した**（次に写す人が同じ穴を踏まないため）**。**

---

## 4. 段 3 — ジャストパリィ（案 b の実装）

- `internal/service/punishfinder/constants.go`: **`justParryExcludedCodes`** を新設（`drive_reversal` の 1 件）
- `internal/service/punishfinder/service.go`: **`just_parry` 分岐の先頭で `continue`**

**★成立レーンにも手動確認レーンにも出さない。⇒ 開発者の逐語は「取り扱わなくてもいい」であって「判断を人に回す」ではないためである。**

**★ブロックタブからは外していない。★除外は `code` 単位である**（`category='system'` を丸ごと外していない）**。⇒ `drive_parry` は JP タブに出続ける。これを対照テストで固定した。**

**★「27F」という事実は `moves.recovery` に残っている。⇒ 捨てると次に誰かが同じことを調べ直す。**

---

## 5. 段 4 — 入力面（明示除外）

- `web/src/constants/move-code.ts`（**新規**）: `MOVE_CODE_DRIVE_REVERSAL`。**バックエンド `internal/model/move.go` の `MoveCodeDriveReversal` と 1:1**（`CLAUDE.md` §4 の同期規約）
- `web/src/features/combo/moveSurfacing.ts`: **`INPUT_EXCLUDED_MOVE_CODES` / `isInputExcluded()`** を新設し、**未分類タブ**（`surfaceBuckets`）から外した
- `web/src/features/combo/hooks/useControllerInputOmission.ts`: **全技一覧のプルダウン**からも外した（トグルの状態に関係なく常に）

**★★2 面をまたぐ必要があった。⇒ 未分類タブから外しても、全技一覧のプルダウンから選べれば「入力面に出さない」にならない。**

**★`omittedCount`（トグルが省いた件数）には数えていない。⇒ 常時除外はトグルの効き目ではないためである。**

**★★共通技タブの固定 13 code には 1 文字も触っていない**（指示書 §3-2）**。⇒ テストで固定した。**

**★`setup_only` 列は流用しなかった。⇒ あちらは `P4M-005` の射程であり、付与口も非表示の範囲もまだ決まっていない**（`M31-RESEARCH-01`）**。**

### 5.1 ★★「入力面」は 2 面である（**3 面目は対象外と決めた。塞ぎ忘れではない**）

**★レビュー 高-3 が 3 面目を指摘した。⇒ 事実であることを製造側でも確かめた。**

| 経路 | 現状 |
|---|---|
| **引っ越し取込の逆引き**（`internal/service/intake/service.go` の別名照合） | **`ax.Lookup` は `category` で絞らない。★`Lookup` は「別名テキスト → code 集合」でありキャラ横断で code を重複排除する。⇒ 31 キャラ共通の「ドライブリバーサル」は 1 件に解け、`Resolved=true` になる** |
| **引っ越し取込の技セレクタ**（`web/src/pages/IntakeHelperPage.tsx` の `moveOptions`） | **`useMovesByCharacter` の全件から組むため、そのまま並ぶ** |

**★★2026-09-10 開発者判断＝(b) 含めない・明記。逐語＝「ユーザーがあり得ないコンボを入れたりとかは考えなくていい」。**

**⇒ 取込経路は「入力面」に含めない。★これは塞ぎ忘れではなく、対象外と決めた結果である。** **★同じ趣旨を `moveSurfacing.ts` の `INPUT_EXCLUDED_MOVE_CODES` と `internal/model/move.go` の 2 か所へ記述として残した。⇒ 次に触る人が「穴だ」と読んで勝手に塞がないため**（塞ぐなら判断のやり直しである）**。**

**★alias 投入そのものは正しい。⇒ 確定反撃の相手技名が `officialJaPresetSubquery` 経由で解決されるため、無いと生 code へ落ちる。**

---

## 6. テストと実測

### 6.1 ★★陽性対照（指示書 §4.3 ／ チェックリスト C-1）

**`internal/infra/migration/migrate_m3104_test.go` の `TestRun_M3104_PunishScanPositiveControl` が、同一 DB を `v107` → `v109` と進めながら 2 回測る。**

| 時点 | ガード | 実測 |
|---|---|---|
| **`v107`（投入前）** | block ／ just_parry | **成立レーン・手動確認レーンのどちらにも `drive_reversal` は出ない**（全 31 相手キャラ） |
| **`v109`（投入後）** | **block** | **全 31 相手キャラの成立レーンに出る。有利フレーム＝`+6`**（`-(on_block)`） |
| **`v109`（投入後）** | **just_parry** | **成立レーン・手動確認レーンのどちらにも出ない**（全 31 相手キャラ） |

**★1 キャラで済ませていない**（チェックリスト C-3）**。⇒ `CROSS JOIN` の取りこぼしは 1 組だけ見ていれば緑になる。**

### 6.2 契約テスト

`TestRun_M3104_UpContract` ／ `TestRun_M3104_DownRoundTrip`（比較区間 `v107` → `v109`。**HEAD を終端にしていない**＝`SUPP-001` §5.5 規約 (1)）。

**★件数リテラルで固定していない**（`SUPP-001` §5.5.4 (6)）**。⇒ 期待値は「sf6 キャラ数」から引いており、キャラ追加の波で本テストを直す作業が生じない。**

主張: 投入前 0 行 ／ 投入後は全キャラに 1 行 ／ `category`・`on_block`・`damage`・`recovery` が全行そろっている ／ 一次源の無い列は NULL のまま ／ `is_projectile`・`is_aerial` が 0 ／ alias が対で在る ／ **alias の `character_id` が `moves` と一致する** ／ **`drive_parry` の行数が変わっていない**（追加であって置換でない＝チェックリスト E-3） ／ down で投入分だけが消える。

### 6.3 ユニットテスト

`internal/service/punishfinder/m31_04_drive_reversal_test.go` — ブロックタブに `adv=6` で出る ／ JP タブの成立・手動確認のどちらにも出ない ／ **除外が `code` 単位であることの対照**（`drive_parry` は JP タブに出続ける）。

`web/src/features/combo/moveSurfacing.test.ts` — 入力面から外れている ／ **未分類タブにも並ばない**（除外が無ければ落ちる形であることを同じテスト内で先に確認している） ／ 共通技 13 code 不変の対照 ／ **同じ `category='system'` の共通技は除外されない対照**。

### 6.4 検査の実行結果（★出力で判定）

| 検査 | 結果 |
|---|---|
| `go test ./...` | **59 パッケージ ok ／ FAIL 0** |
| `cd web && pnpm test` | **225 files / 2677 tests passed**（★Phase C 後の再実行値。取り込み前は 224 / 2675） |
| `cd web && pnpm run lint`（`tsc --noEmit`） | **緑** |
| **`bash scripts/check-migration-license.sh`** | **違反なし。`--list` で `000109` の up / down とも `B 新規 CC-BY-SA-4.0` と判定** |
| `bash scripts/check-import-order.sh` | 違反なし（**§6.6 に注記**）**。★Phase C で足したテストが 1 件当たった**（`React` をサードパーティの後に置いていた）**⇒ 直して 99 へ戻した。★型検査もテストも緑のまま通る型であり、本検査が唯一の検出経路であった** |
| `bash scripts/check-enum-sync.sh` | ベースラインどおり（増加なし） |
| `bash scripts/check-browser-storage-keys.sh` | 一致（**新しいキーは足していない**） |
| `bash scripts/check-doc-refs.sh` | dead reference なし |
| `make e2e-only P=m18-02` | **2 passed** |
| `make e2e-only P=m18-03` | **13 passed** |
| **`make e2e-only P=m30-0`** | **16 passed。★このうち「未分類タブの件数が実 DB で固定である」が通ったことが、入力面の除外が効いている実測である** |
| **`make e2e-only P=m30-01`**（Phase C 後） | **9 passed。★中-1 で足したプルダウンの `toHaveCount(0)` を含む** |
| `bash scripts/check-progress-log-index.sh` | **Phase D で回す** |
| `bash scripts/check-artifact-integrity.sh` | **Phase D で回す（1 本目）** |

**★`playwright` を直接叩いていない**（`CLAUDE.md` §11 ／ `D-599`）**。**

### 6.4.1 ★★開発者の実機確認（2026-09-10・**コード上では判定できない範囲**）

| # | 確認したこと | 結果 |
|---|---|---|
| **1** | **ブロックタブに D リバが出るか。★有利フレーム `+6` が実際のゲームの体感と合っているか** | **★★合っている。⇒ `-6` を `-(on_block)` に乗せて `+6` とする既存の算出式が、この技にも当てはまることが実機で確かめられた。★`+6` は開発者から受け取った値そのものではなく実装側の解釈だったため、ここが確かめられた意味は大きい** |
| **2** | **`+6` で出る始動技候補に、距離的に届かない技が混じっていないか** | **混じるが仕様どおりで問題なし**（走査は距離判定を持たない）**。⇒ 実用上の邪魔にはなっていない** |
| **3** | **ジャストパリィタブから本当に消えているか。★消えることが気持ち悪くないか** | **消えている。★★「気持ち悪さもなかった」。⇒ レビュー 低-3 の懸念は実機で成立しなかった**（§8-6 を取り下げた） |
| **4** | **入力面に出ていないこと ／ 技名が生 code になっていないこと** | **どちらも良好**（未分類タブ・全技一覧プルダウンとも出ない ／ 表示は「ドライブリバーサル」） |
| — | **引っ越し取込** | **判断済みのため確認していない**（§5.1 の開発者判断）**。⇒ 決着済み・対象外という記録はそのまま有効** |

### 6.5 変更統計（`git diff --stat 0660fa2`）

```
 internal/aliasindex/m2207b_third_stage_test.go     |   5 +-
 internal/infra/migration/migrate_m3104_test.go     | 273 +++++++++++++++++++++
 internal/model/move.go                             |  16 ++
 internal/service/punishfinder/constants.go         |  20 ++
 .../punishfinder/m31_04_drive_reversal_test.go     |  93 +++++++
 internal/service/punishfinder/service.go           |   6 +
 ...9_data_seed_drive_reversal_system_move.down.sql |  12 +
 ...109_data_seed_drive_reversal_system_move.up.sql |  67 +++++
 web/src/constants/move-code.ts                     |  18 ++
 .../combo/hooks/useControllerInputOmission.ts      |  22 +-
 web/src/features/combo/moveSurfacing.test.ts       |  32 +++
 web/src/features/combo/moveSurfacing.ts            |  31 ++-
 12 files changed, 590 insertions(+), 5 deletions(-)
```

**★新規 5 ファイル**（`migrate_m3104_test.go` / `m31_04_drive_reversal_test.go` / `move-code.ts` / `000109` の up・down）**はいずれも `+` だけである**（教訓 `E-225`）**。deletions 5 行はすべて既存ファイルの編集分**〔`m2207b_third_stage_test.go` の行数定数 1 行 ／ `useControllerInputOmission.ts` の `visibleMoves` 3 行 ／ `moveSurfacing.ts` の未分類判定 1 行〕**である。**

**★`character_data/*.csv` は 1 バイトも触っていない**（チェックリスト A-1。上の一覧に 1 件も出ていないことがその実測である）**。⇒ golden の再生成は要らず、差分は 0 である**（A-3）**。同時に走る `M35-02` と衝突しない。**

### 6.6 ★`check-import-order.sh` のベースライン差（**本サブ由来ではない**）

**同検査は「現在 99 ファイル / ベースライン 101 ファイル」と出し、`BASELINE` を 99 へ下げるよう促す。**

**★本サブの変更ファイル 3 本**（`moveSurfacing.ts` / `useControllerInputOmission.ts` / `move-code.ts`）**は違反一覧に 1 件も出ていない。⇒ 差は着手前から在ったものである。**

**★スクリプトの `BASELINE` は触っていない。⇒ 共有の検査スクリプトであり、並列サブとの衝突面を増やさないためである。★下げるのは別の手番とした**（§8 へ記録）**。**

---

## 7. ■ 併せて更新が要るもの

| # | 対象 | 状態 |
|---|---|---|
| **1** | **消費した CHANGE 番号の登録**（`docs/handover/change-number-registry.md` §1） | **★該当なし。⇒ 本サブは CHANGE を 1 本も起票していない**（自採番しない＝`D-293`）**。請求は設計伝達レポート §4 へ書く** |
| **2** | **「次の番号」の写し先の全数** | **★該当なし**（同上。CHANGE 番号を消費していない） |
| **3** | **消費したマイグレ連番** | **`000109` を消費した。⇒ ボード §2.2 の「次に払い出す番号」を進める必要がある。★ただし `000108` は本ツリーに存在せず並列サブが消費予定であり、実査値はマージ後でないと確定しない。⇒ 設計伝達レポート §4 へ回す**（**★製造はボードを直接編集しない**＝`D-382`） |
| **4** | **版を上げた文書の参照元** | **★該当なし**（設計書・指示書の版を上げていない。`docs/design/` は編集していない＝指示書 §3-4） |
| **5** | **`web/CLAUDE.md` §1 のブラウザストレージ台帳** | **★該当なし**（新しいキーを足していない。`check-browser-storage-keys.sh` が一致を確認） |

---

## 8. 残課題・横断課題

| # | 事項 | 宛先 |
|---|---|---|
| **1** | **`DES-002` §4 への追随**（本サブの as-built）**。★`REQ-001` は不要**（§1.4 の実査） | **設計伝達レポート §4 で CHANGE を請求** |
| **2** | **ボード §2.2 の「次に払い出すマイグレ番号」**（`000109` を消費。★`000108` の帰趨はマージ後に確定） | **設計伝達レポート §4** |
| **3** | **`scripts/check-import-order.sh` の `BASELINE` が 101 で、実測 99**（**本サブ由来ではない**＝§6.6） | **別の手番。⇒ 改善レーンか、次に同スクリプトを触るサブ** |
| **4** | **入力面から明示除外する仕組みが本サブで 1 件目になった**（`INPUT_EXCLUDED_MOVE_CODES`）**。★`P4M-005`**（`setup_only` の活用）**が同じ「入力から外す」面を持つ。⇒ 2 通りの仕組みが並ぶ前に、どちらへ寄せるかを決める必要がある** | **設計伝達レポート §4**（`M31-RESEARCH-01` の続き） |
| **★★5** | **★キャラ追加波では `000109` の追随マイグレが要る**（レビュー 中-2）**。⇒ 移動 system move が `000025` → `000044`（マノン）→ `000054`（第三波）と追随してきたのと同じ性質である。★これを検出する常設テストは無い**——本サブの契約テストは `v107 → v109` に閉じており（規約どおり）、`migrate_head_test.go` は FK の宙吊りしか見ない。**⇒ 思い出さない限り、そのキャラだけ確定反撃に D リバが出ない。動作は落ちず、テストも緑である** | **次のキャラ追加波の担当。⇒ 設計伝達レポート §4 にも書く** |
| **★6** | **~~JP タブから黙って消えることが画面に何も残らない~~**（レビュー 低-3）**。★★2026-09-10 の実機確認で取り下げた**——**開発者の逐語＝「消えている。気持ち悪さもなかった」**（§6.4.1 の 3）**。⇒ 懸念は実機で成立しなかった。★`DES-005` §5.20 への画面表現の追随は要らない。★除外の理由は `justParryExcludedCodes` の注記に残っており、次に調べる人はそこへ辿り着ける** | **★対応不要（決着）** |

---

## 9. 完了条件の充足（指示書 §6）

| # | 条件 | 充足 |
|---|---|---|
| 1 | 段 1 の実査 5 件に結果があり、母集団が 1 行書かれている | **§1**（母集団＝`moves` 3026 行） |
| 2 | 段 2 の投入の形が理由つきで決まり、実装されている | **§3** |
| 3 | マイグレ名に `_data_` が入り、層 B と判定される | **§3 / §6.4**（`--list` で `B` を確認） |
| 4 | 段 3 が開発者の回答を得てから実装されている | **§2 / §4** |
| 5 | 段 4 の案が報告に書かれている | **§1.3 / §5**（★案の提示にとどめず、回答を得て実装した） |
| 6 | 陽性対照 2 件が実測で示されている | **§6.1** |
| 7 | §5 の検査がすべて緑 | **§6.4** |
| 8 | 完了報告 ＋ `progress-log.md` へ索引行 | **本ファイル ／ Phase D** |
| 9 | 完了報告を `check-md-emphasis.sh` に通す | **Phase D で回す** |
| 10 | 設計伝達レポートを出す | **§8 の 4 件を §4 へ書く。⇒ 本サブ完了後の手番** |

---

## 10. レビュー結果（**Phase C 実施後に記入**）

- **レビュー報告書**: `docs/progress/m31-04-review.md`（**fresh subagent が作成。メイン会話文脈を継承していない**）
- **指摘の件数・優先度別内訳**: **9 件＝高 3 ／ 中 3 ／ 低 3**
- **チェックリストの最重要 4 点**（A-1 / B-1 / C-1 / D-1）: **すべて充足**とレビューが判定。**B-2 / C-1 / 常設検査はレビュー側で独立に再実行して再現した**
- **各指摘の採否と理由**: **レビュー報告書末尾「取り込み結果（自動トリアージ）」に全 9 件ぶん記載した**
- **★「高」指摘の不採用**: **0 件**。高-1 / 高-2 は採用して修正した。**高-3 は「不採用」ではなく、指摘を事実として認めたうえで対応方針を開発者が (b)「含めない・明記」と決めたものである**（§5.1）
- **再レビュー往復の回数**: **0 回**（初回のみ。上限 2 回に達していない）
- **停止時記録**: **無し**。未解消のまま止めた項目は 0 件であり、`followup-backlog.md` §J への転記は要らない

### 10.1 レビュー取り込みで変わったもの

| 変更 | 由来 |
|---|---|
| `useControllerInputOmission.ts` の godoc（`visibleMoves` / `omittedCount` / 冒頭の解説ブロック） | 高-1 |
| `moveSurfacing.ts` の `isInputExcluded` の godoc | 高-2 |
| `moveSurfacing.ts` / `internal/model/move.go` へ「取込は対象外と決めた」旨を記述 | 高-3 |
| `useControllerInputOmission.test.ts`（**新規**） ＋ E2E `m30-01` へ 1 件 | 中-1 |
| 完了報告 §8-5 ／ `progress-log.md` 横断課題 | 中-2 |
| 「`CROSS JOIN characters`」の記述 3 か所 | 低-1 |

---

*以上、M31-04 完了報告。*
