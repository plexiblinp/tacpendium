# M19 独立監査レポート: 設計書バージョン整合 ＋ M19-01 実装レビュー

| 項目 | 内容 |
|------|------|
| 監査プロンプト | `tmp/M19-監査プロンプト-版整合と実装レビュー.md` |
| 実施日 | 2026-07-25 |
| 実施者 | 独立監査担当 Claude Code(製造・レビュー・設計のいずれとも別セッション) |
| 対象 | Task A = 設計書(REQ-001 / DES-001〜006 / SUPP-001)版整合 / Task B = M19-01(PR #10)実装レビュー第二パス |
| 監査ブランチ | `chore/docupd-and-auditM19-01`(HEAD `cd331f0`) |
| 検証方式 | **完全 read-only**。既存の完了報告・レビュー・指示書の主張は仮説として扱い、実ファイル・実コード・実 seed CSV・`git diff` を一次情報として裏取りした。DES/コード/CLAUDE.md は編集せず、CHANGE 番号・マイグレ連番も採番していない |

---

## 1. サマリ

- **反映漏れは実在する。しかも開発者申告の 1 件だけではない。** DES-003 §3.4 の `knockdown_advantage` errata②(KA 意味論)は**本文に一文字も入っていない**(A-1)。加えて **CHANGE-083 の `combo_punish_starters` が DES-003 に不在**(A-7)＝**新テーブルが実在するのにデータモデル正典に載っていない**状態で、こちらは A-1 と同格以上の重さがある。
- M19-01 関連(DES-002 提案エンドポイント・DES-005 提案 UI・CLAUDE.md §10.X キー登録)は**すべて未反映**だが、実装完了直後・CHANGE-085 反映待ちとして**想定内の状態**。ただし **CHANGE-085 通知書の本体がリポジトリに存在しない**(addendum 2 本のみ)ため、反映の拠り所が repo 内に無い(A-2)。
- **M19-01 の実装に重大問題は発見できなかった。** 高リスク不変条件(受理帯・KA+1 一貫性・スキーマ不変・FR011 非破壊)はすべて OK、golden 4/4 PASS、`go build`/`go vet`/`go test ./...`(45 パッケージ)/`tsc --noEmit`/Vitest 815 件/locales parity すべて緑。
- 実装で報告すべきは**残留リスク 5 件**(rush の +11 フレーム基準が既定結果に混入・`is_derived` の filler/target 非対称・`alreadyAdopted` の打ち切り前 N+1・明示指定時の system 非除外・handler コメントの陳腐化)。いずれも**指示書に準拠しており実装欠陥ではない**が、仕様の穴・運用上の注意として設計側の判断を要する。
- 版ヘッダ・台帳の整合は**局所的に破れている**: change-number-registry が 083 未起票のまま停止(A-10)、DES-004 の版番号が重複(A-11)、SUPP-001 の v1.27.0 が更新履歴に無い(A-15)、前提文書欄が全書で旧版参照(A-12)。一方 `docs-map.md` の版対応は**全 7 文書で実ヘッダと完全一致**(異常なし)。

---

## 2. Task A: 設計書バージョン整合監査

### 2.1 A-2 版ヘッダ棚卸し

各文書の**ヘッダ記載**(＝主張)をそのまま転記した表。「直近で反映したと主張している CHANGE」はステータス欄冒頭の記述。

| 文書ID | 実パス | ヘッダ版 | 版数表記 | 直近で反映したと**主張** | 前提文書欄の参照版 |
|--------|--------|---------|---------|------------------------|-------------------|
| REQ-001 | `docs/design/requirements.md` | **2.17.0** | 第27版 | CHANGE-077(§7 で NFR406 をフェーズ4→3 へ移設) | (欄なし) |
| DES-001 | `docs/design/01-tech-stack.md` | **1.4.0** | 第6版 + 2026-07-06 errata | CHANGE-053 / errata=前提文書欄追従 | REQ-001 v2.16.0 |
| DES-002 | `docs/design/02-architecture.md` | **1.34.0** | 第36版 | CHANGE-071(M17-04 取込ヘルパー) | REQ-001 v2.16.0 / DES-001 v1.4.0 |
| DES-003 | `docs/design/03-data-model.md` | **1.33.0** | 第36版 | CHANGE-078/079/080/081(M18-01 スキーマ基盤) | REQ-001 v2.16.0 / DES-002 v1.30.0 |
| DES-004 | `docs/design/04-notation-spec.md` | **1.15.0** | 第16版(※重複あり・A-11) | CHANGE-081(§2.5 `is_projectile` 正典) | REQ-001 v2.16.0 / DES-003 v1.28.0 |
| DES-005 | `docs/design/05-screen-design.md` | **2.48.0** | 第59版 | CHANGE-082(§5.4 hit_type 4 値化) | REQ-001 v2.16.0 / DES-002 v1.30.0 / DES-003 v1.26.0 / DES-004 v1.8.0 |
| DES-006 | `docs/design/06-validation.md` | **1.21.0** | 第23版 | CHANGE-082(§2.7 hit_type 4 値化) | REQ-001 v2.16.0 / DES-003 v1.26.0 |
| SUPP-001 | `docs/design/supp-001-detailed-design.md` | **1.27.0** | 第20版改訂 | (CHANGE 非連動・自由改訂) | REQ-001 v2.16.0 / DES-001 v1.4.0 / DES-002 v1.30.0 / DES-003 v1.26.0 / DES-004 v1.8.0 / DES-005 v2.37.0 / DES-006 v1.17.0 |
| (規約) | `docs/design/testid-convention.md` | (版ヘッダなし) | — | — | — |

**台帳との照合(異常なし)**: `docs/handover/docs-map.md:36-40,112,116` の版番号は上表の実ヘッダと **7/7 一致**(DES-002 1.34.0 / DES-003 1.33.0 / DES-004 1.15.0 / DES-005 2.48.0 / DES-006 1.21.0 / REQ-001 2.17.0 / SUPP-001 1.27.0)。`docs-map.md:131-137` の役割マップ側の版表記も同一。**台帳は正しく追従している**。

### 2.2 A-3 反映の実在検証(本監査の核心)

#### 【最優先】A-1: DES-003 §3.4 `knockdown_advantage`(errata②)

**現行本文の全文引用**(`docs/design/03-data-model.md:354`。§3.4 のカラム表の 1 行):

```
| knockdown_advantage | INTEGER | NULL可 | ダウン後の有利フレーム数 |
```

§3.4 内で `knockdown_advantage` に言及する他の箇所は `:439` の 1 文のみ:

```
`knockdown_advantage`（INTEGER）は combos に残置・不変（正規化対象外）。
```

**個別判定(errata② が明文化したと表明された 5 項目)**:

| 判定項目 | 期待される記述 | 実ファイル | 判定 |
|---------|--------------|-----------|------|
| (a) 「無敵時間」相当 | KA＝ダウン中の無敵時間 | `grep -rn "無敵" docs/design/` → **0 件** | **no** |
| (b) 起き上がり＝KA+1 | 相手の起き上がりは KA+1 フレーム目 | `grep -rn "KA+1\|KA + 1" docs/design/` → **0 件**。「起き上がり」の語自体は `03-data-model.md:563`(セットプレイ例文)・`02-architecture.md:391`(DR リバーサル)の 2 件のみで、いずれも KA と無関係 | **no** |
| (c) `S = Σfiller.total + startup` | S の定義 | `grep -rn "Σ" docs/design/03-data-model.md` → 該当なし。§3.4 に数式なし | **no** |
| (d) `N = KA+2−S` | N の定義 | `grep -rn "KA+2\|KA + 2" docs/design/` → **0 件** | **no** |
| (e) `1 ≤ N ≤ active` | 成立条件 | 同上・**0 件** | **no** |

**不整合の明示**:

- **(a)〜(e) すべて no。** DES-003 §3.4 は「ダウン後の有利フレーム数」という**フレーム数の一般的説明のみ**で、M19 が依存する意味論(無敵時間としての解釈・起き上がり位置・S/N の定義・成立条件)は**一切入っていない**。
- 加えて DES-003 のステータス欄(`:8`)にも **errata② の見出し・記載が無い**。第36版(CHANGE-078〜081)以降の追記が無く、「反映済みと書いてあるのに本文に無い」型ではなく、**履歴にも本文にも無い**(＝差し替え版が repo に到達していない)状態。
- **実装側は errata② を存在するものとして参照している**: `internal/service/setplay/setplay.go:9`

  ```go
  // # 確定した意味論(DES-003 §3.4 errata②・M19-01 §1.1)
  //   - KA(knockdownAdvantage) = ダウン中の無敵時間。相手の起き上がりは KA+1 フレーム目。
  //   - S = Σ(filler.total) + target.startup = target の第 1 active の絶対フレーム。
  //   - N = KA + 2 − S = 起き上がりに重なる持続フレーム番号。
  //   - 成立条件(窓方式) = NMin ≤ N ≤ target.active
  ```

  指示書 `docs/instructions/M19-01-setplay-suggestion-engine-and-ui.md:26`(v1.4.0 更新履歴)も「KA 意味論が **DES-003 §3.4 errata② で明文化**された旨へ更新」と記載。**つまりコードとコメントと指示書は errata② を正典として参照しているが、参照先が存在しない**。→ **反映漏れ確定(開発者申告どおり)**。

#### 【最優先】A-7: CHANGE-083 `combo_punish_starters` が DES-003 に不在

開発者申告に無かったが、**A-1 と同格以上の反映漏れ**を検出した。

- 通知書は実在: `docs/change-notes/CHANGE-083-notification.md:1`「combo_punish_starters 新設（M18-02・始動技レベルの検証結果を保存）」。ステータス欄は「**起票（着手前ゲート・実装待ち）**。DES-003・DES-005 → 実装後に改訂」。
- **実装は完了している**: `migrations/000040_create_combo_punish_starters.up.sql` が存在し、`internal/model/punish.go` / `internal/repository/punish/queries.go` / `internal/repository/punish/repository.go` / `internal/service/punishfinder/constants.go` / `internal/infra/migration/migrate_m1802_test.go` が当該表を参照。M18-02 完了報告 `docs/progress/phase3/M18-02-completion-report.md` も存在し、PR #11(`390dfed`)で main へマージ済み。
- **しかし DES-003 に無い**: `grep -rn "combo_punish_starters" docs/design/` → **0 件**。DES-003 §3 のテーブル定義は §3.17 `combo_punish_curations` で終わっており(`03-data-model.md:649`)、新表の節が無い。ステータス欄も第36版(CHANGE-078〜081)止まり。
- → **反映漏れ確定**。DES-003 は「データモデルの正典」であり、**実 DB に存在するテーブルが正典に載っていない**のは A-1 より波及が大きい(後続マイルストーンが DES-003 を読んで「その表は無い」と判断し得る)。

#### A-8: CHANGE-084(移動 system move の `total` 注記)が DES-003 §3.3 に不在

- 通知書 `docs/change-notes/CHANGE-084-notification.md` は「影響設計書 = **DES-003 §3.3**（移動 system move における `total` の意味の注記）。**反映は実装後**」。
- 実装済み: `migrations/000041_backfill_movement_total.up.sql` が存在。
- DES-003 §3.3(`03-data-model.md:266-333`)に当該注記なし。→ **反映漏れ**。

#### A-3(プロンプト §A-3 の DES-002 §4.2 項目): 提案エンドポイント

- `docs/design/02-architecture.md:127-153` の §4.2「主要エンドポイント（概要）」表に **`setplay-suggestions` の行が無い**。`grep -rn "setplay-suggestion\|setplay_suggestion" docs/design/` → **0 件**。
- したがって query パラメータ(`n_min` / `sort` / `target_types` / `include_zero_damage` / `target_move_id`)も**すべて未記載**。
- **併せて判明した既存の穴**: 同表には FR011 の既存セットプレイ系エンドポイント(`GET /api/setups/candidates`・`GET /api/combos/:comboId/setup-candidates`・setup CRUD)も**載っていない**(`grep -n "setup" 02-architecture.md` のヒットは §7.5/§7.6 の CSV 契約と `setup_only` 列のみ)。§4.2 冒頭が「ここでは代表的なエンドポイントのみ示す」と断っているため規約違反ではないが、**M19 の未反映は「M19 だけの遅れ」ではなく §4.2 の網羅方針の帰結**でもある。
- 判定: **未反映**(CHANGE-085 反映待ち＝実装直後として想定内)。

#### A-4(プロンプト §A-3 の DES-005 項目): 提案 UI

- `grep -n "SetupSelectorModal\|セットプレイ提案\|提案" docs/design/05-screen-design.md` → **ヒット 0 件**(「提案」の 2 文字すら本文に無い)。
- したがって「相乗り先」「種別フィルタ」「damage 既定除外」「制約告知」「採用/不採用」いずれも未記載。
- **`SetupSelectorModal` の誤記が残っているか**という問い: **残っていない**。ただし理由は「是正された」からではなく「**提案 UI の節そのものが DES-005 に存在しない**」ため。誤記の混入余地は反映時に生じるので、反映時に相乗り先を **ComboDetailPage** と書くこと(実装は `web/src/pages/ComboDetailPage.tsx:178`)。
- 判定: **未反映**(想定内)。**誤記残存は無し**。

#### A-5(プロンプト §A-3 の CLAUDE.md §10.X 項目): 公認 localStorage キー表

- `CLAUDE.md:315` まで(#1 `combo-list-columns-v1` / #2 仮想コントローラ(取消線) / #3 `onboarding-seen-v1`)。キー命名規則表は `:334` の #1 のみ。
- `grep -n "setplay-notice-seen-v1" CLAUDE.md` → **0 件**。
- 実装側は `web/src/features/setplay/setplay-notice-storage.ts:6` で `setplay-notice-seen-v1` を定義済み。addendum `docs/change-notes/CHANGE-085-addendum-setplay-localstorage-key.md` が「§10.X 公認キー表への追記可否」を確認依頼中。
- 判定: **未反映**(addendum のステータス「確認待ち」と整合＝手続き上は正しい状態)。

#### A-6(プロンプト §A-3 の DES-003 §3.3 項目): move フラグの用途注記に setplay consumer が入っているか

`docs/design/03-data-model.md` §3.3(`:266-333`)の該当行を引用して個別判定。

| 列 | DES-003 §3.3 の現行記述(要点) | 実装での setplay 消費 | 判定 |
|----|------------------------------|---------------------|------|
| `is_projectile` | 「**役割＝確定反撃の自動走査から飛び道具を除外する唯一の一次源**（DES-004 §2.5・M18-02 finder）」 | `internal/repository/setplay/repository.go:48-49` で SELECT、`internal/service/setplay/service.go:93-96` で `special_projectile` 種別の判定に使用 | **反映漏れ**。しかも「**唯一の**一次源」という排他的断定が、第 2 の消費者(setplay 種別分類)の存在と**矛盾** |
| `is_derived` | 「**役割はただ一つ＝段階2 のコマンド入力解決の索引から除外すること**（DES-004 §2.4）」 | `service.go:286` で target 列挙の除外条件に使用 | **反映漏れ**。「**役割はただ一つ**」が実装と**明確に矛盾** |
| `original_move_id` | 「ラッシュ版moveの場合、対応する通常技のmove_idを参照。通常技・システム技ではNULL」(`:274`) | `service.go:99-109` で rush 種別(`normal_rush`/`unique_rush`)の元技カテゴリ解決に使用 | **反映漏れ**(用途追加の注記なし) |
| `setup_only` | 「**将来のセットプレイ自動提案機能**（フレーム消費の算出）のための予約列。**本フェーズではフラグ保持のみで利用ロジックは未実装**（CHANGE-022）」(`:285`) | M19-01 は **意図的に絞り込みに使わない**と確定(指示書 §11-1)。repo は SELECT するが(`repository.go:49`)service は未使用 | **注記が陳腐化**。自動提案機能は**実装済み**であり、かつ「フレーム消費の算出のための予約列」という位置づけは撤回された(実セマンティクス＝コンボ登録 UI から隠す行区分) |

→ 4 列すべてで **setplay consumer が未反映**。特に `is_projectile` の「唯一」・`is_derived` の「ただ一つ」は**排他的断定が実装により破られている**ため、単なる追記漏れより強い不整合として報告する。

#### A-9: REQ-001 に M19 の FR が無い(追加検出)

- `grep -rn "自動提案" docs/design/*.md` → ヒットは DES-003 §3.3 `setup_only` 行の「**将来の**セットプレイ自動提案機能」**1 件のみ**。
- REQ-001 のセットプレイ関連 FR は **FR011(転用支援＝同一 KA の他コンボから既存セットプレイを流用)** のみ(`requirements.md:126`)。**「フレーム計算で新規セットプレイを提案する」機能の FR は存在しない**。
- 要件レベルの根拠は `docs/instructions/phase3-overview.md:123,200`(マイルストーン表と M19 節)のみ。
- 判定: **要件反映漏れ**(実装済み機能に要件番号が無い＝トレーサビリティの穴)。CHANGE-085 が DES-002/005 のみを対象としているため、この穴は 085 反映でも埋まらない。

### 2.3 A-4 逆方向チェック(履歴漏れ・版矛盾)

| # | 種別 | 内容 | 根拠 |
|---|------|------|------|
| A-10 | **台帳の停止** | `docs/handover/change-number-registry.md:114` が「**083 以降 空き**／次回起票は **083 から採番**」のまま。実際は **CHANGE-083・084 の通知書が実在**(起票日とも 2026-07-23)、**085 も addendum 2 本から「中央起票済み」と参照**されている。registry §1 の更新履歴の最新行は **1.74.0 / 2026-07-22**(`:297`)で、083 以降の起票が 1 件も記録されていない | `ls docs/change-notes/` / `grep -n "08[345]" change-number-registry.md` |
| A-11 | **版番号の重複** | DES-004 の版数表記で **第13版・第14版・第15版・第16版がそれぞれ 2 回**出現。「第16版（CHANGE-081 反映：M18-01）」と「第16版（CHANGE-066反映：M16-06）」が併存し、第13〜16版が二重に採番されている | `grep -o "第[0-9]*版" docs/design/04-notation-spec.md \| sort \| uniq -c` → 第13〜16版が各 2 |
| A-12 | **前提文書欄の版ズレ(全書横断)** | DES-005 前提 = DES-003 **v1.26.0**(実 1.33.0)/ DES-002 **v1.30.0**(実 1.34.0)/ DES-004 **v1.8.0**(実 1.15.0)。DES-003 前提 = DES-002 **v1.30.0**。DES-004 前提 = DES-003 **v1.28.0**。DES-006 前提 = DES-003 **v1.26.0**。DES-001/DES-002 前提 = REQ-001 **v2.16.0**(実 2.17.0)。SUPP-001 前提 = 全 7 文書とも旧版。**過去に「2026-07-06 errata（前提文書欄追従）」で一括追従した前例がある**(DES-001 `:8`)ため、本欄は維持対象の運用と解される | 各文書 `| 前提文書 |` 行 |
| A-15 | **履歴漏れ(版番号が更新履歴に無い)** | SUPP-001 はヘッダ **バージョン 1.27.0**(`:6`)だが、§更新履歴の最終行は **1.26.0 / 2026-07-05**(`:45`。全 28 行)。**1.27.0 のエントリが存在しない**。`grep -n "1\.27\.0"` のヒットはヘッダ 1 箇所のみ | `docs/design/supp-001-detailed-design.md:6,45` |
| A-16 | (参考)派生資料の停止 | `docs/progress/progress-summary.md` の最新は **§16(M16)・1.13.0 / 2026-07-09**。M17・M18・M19 が未要約。CLAUDE.md §1 が「現在地は progress-summary を参照」と定めているため、**現在地の一次情報が 3 マイルストーン分遅れている** | `tail docs/progress/progress-summary.md` |

**逆方向で「本文にあるのに履歴に無い」型は検出されなかった**(A-15 は版番号自体の履歴漏れ)。**「履歴が反映済みと書くのに本文に無い」型も検出されなかった**——A-1/A-7/A-8 はいずれも**履歴にも本文にも無い**(＝差し替え・改訂が repo に到達していない)型である。この区別は重要で、「中央が送ったつもりの差し替え版が repo に着いていない」という搬送段の問題を示唆する。

### 2.4 A-5 全設計書スイープの結果

| 文書 | 反映漏れ | 備考 |
|------|---------|------|
| REQ-001 | **有**(A-9) | CHANGE-077 反映は本文で確認(§7 の NFR406 フェーズ移設) |
| DES-001 | 無 | 前提文書欄の版ズレのみ(A-12) |
| DES-002 | **有**(A-3) | CHANGE-071 反映は §4.2 に `POST /api/intake/resolve`・`/api/intake/csv` として実在(`:139-140`)＝**主張どおり反映済みを確認** |
| DES-003 | **有(最多)**(A-1 / A-6 / A-7 / A-8) | CHANGE-078〜081 反映は実在確認: §3.15 `combo_punishes`(`:615`)・§3.16 prunings(`:633`)・§3.17 curations(`:649`)・§3.4 `materialized_from_combo_id`(`:340`)・§3.3 `is_projectile`(§3.3 内)。**082 以降が入っていない** |
| DES-004 | 版矛盾(A-11) | CHANGE-081 反映は §2.5 として実在 |
| DES-005 | **有**(A-4) | CHANGE-082 反映は §5.4 hit_type 4 値化として実在 |
| DES-006 | 無 | CHANGE-082 反映は §2.7 として実在。VAL-C10(`:47`)に `knockdown_advantage` の範囲 -600〜+600 が定義されており、**A-1 の意味論(KA≥0 前提)と整合していない可能性**は §4-6 に記載 |
| SUPP-001 | 履歴漏れ(A-15) | 補足資料のため CHANGE 非連動 |

M16-2 型(古い版の別コピーが出回る)の再発は**検出されなかった**。`docs/design/` 配下に重複コピー・旧版ファイルは無く、`docs-map.md` の版対応も全一致(§2.1)。

---

## 3. Task B: M19-01 実装レビュー(独立第二パス)

### 3.1 B-1 高リスク不変条件の再検証

| # | 不変条件 | 判定 | 根拠(引用) |
|---|---------|------|-----------|
| 1 | 受理条件が「帯」である(等値でない) | **OK** | `internal/service/setplay/setplay.go:227` `if remaining >= 0 && remaining <= band`。`band` は `:144` `band := target.Active - nMin`。**`remaining == 0` / `== KA` / `== KA+1` の等値判定はファイル内に存在しない**(`grep -n "== 0\|== ka\|== KA" setplay.go` → 該当なし) |
| 2 | 受理帯 `0 ≤ remaining ≤ Active − NMin` | **OK** | 同上。`budgetMax = knockdownAdvantage + 2 - nMin - target.Startup`(`:151`) |
| 3 | `N = KA+2−S`(= `remaining + NMin`) | **OK** | `:235-236` `TotalFrames: ka + 2 - nMin - remaining`(= S)/ `HitActiveFrame: remaining + nMin`(= N)。代数的に `N = KA+2−S` と恒等(`S = ka+2-nMin-remaining` を代入すると `KA+2−S = nMin+remaining = N`) |
| 4 | 起き上がり＝KA+1 の一貫使用 | **OK** | `internal/service/setplay/service.go:192` `landing := ka + 1` が**唯一の生成点**。以降 `buildProposal`(`:298`)へ引数で渡し DTO へ素通し(`internal/api/setplay/dto.go:46`)。`grep -n "ka + 1\|ka+1\|+ 1" service.go` の該当は `:192` のみ。**オフバイワン(KA と KA+1 の取り違え)は無し** |
| 5 | スキーマ不変(migrations 増えていない) | **OK** | `git diff --stat e608022 398b465` の 30 ファイルに **`migrations/` が 1 件も含まれない**。新テーブル・新列ゼロ |
| 6 | `go.mod` が stdlib のみ(新規 require ゼロ) | **OK** | 同 diff に `go.mod` / `go.sum` が**含まれない**。エンジンの import は `errors`・`sort` のみ(`setplay.go:29-32`) |
| 7 | 別リポ不変(`autopilot-combomgr` 未変更) | **未確認** | 本ワークスペースに当該リポが存在しない(`ls /workspaces/` → `combomgr` のみ)。**間接証拠**: PR #10 の差分は combomgr 配下のパスのみで、他リポへの書き込みは本リポ履歴からは発生し得ない。§5 に未確認として再掲 |
| 8 | 既存 FR011 非破壊 | **OK** | `git diff --stat e608022 398b465` に `internal/service/setup/`・`internal/api/setup/`・`internal/repository/setup/`・`internal/model/`・`web/src/features/setup/`(コンポーネント)が**一切含まれない**。追加は新規パッケージ + `cmd/combomgr/main.go`(DI 9 行)・`web/src/pages/ComboDetailPage.tsx`(9 行)・`web/playwright.config.ts`(`PW_EXECUTABLE_PATH` 任意上書き 10 行)・locales(各 57 行追加) |

### 3.2 B-2 golden-case の実測

```
$ go test ./internal/service/setplay/... -v -run 'TestGoldenCases'
--- PASS: TestGoldenCases (0.00s)
    --- PASS: TestGoldenCases/GC-1_Ryu_立ち弱K>鎖骨割り (0.00s)
    --- PASS: TestGoldenCases/GC-3_Juri_立ち中P>前投げ (0.00s)
    --- PASS: TestGoldenCases/GC-4_Terry_しゃがみ弱K>立ち中K (0.00s)
    --- PASS: TestGoldenCases/GC-5_Guile_ニーバズーカ>立ち中P (0.00s)
PASS
ok  github.com/plexiblinp/combomgr/internal/service/setplay  0.002s
```

テストは N・着弾(landing)・S の**3 値すべてを等値アサート**している(`golden_test.go:97-105`)ため、PASS は下表の実測一致を意味する。

| # | キャラ | KA | 期待レシピ | N | 着弾 | S | 実測 |
|---|--------|----|-----------|---|------|---|------|
| GC-1 | リュウ | 40 | 立ち弱K > 鎖骨割り | 4 | 41 | 38 | **一致** |
| GC-3 | ジュリ | 27 | 立ち中P > 前投げ | 3 | 28 | 26 | **一致** |
| GC-4 | テリー | 27 | しゃがみ弱K > 立ち中K | 3 | 28 | 26 | **一致** |
| GC-5 | ガイル | 34 | ニーバズーカ > 立ち中P | 2 | 35 | 34 | **一致** |

**golden データの忠実性を独立検証(監査独自)**: `golden_test.go` はフレーム値をリテラルで持つため、「`character_data/*.csv` の実フレーム値」という主張(`golden_test.go:11`)自体を seed CSV と突合した。

| move | test の値 | CSV の値 | 一致 |
|------|----------|---------|------|
| ryu `standing_light_kick` | st=5 ac=3 tot=18 dmg=300 | st=5 ac=3 tot=18 dmg=300 | ✅ |
| ryu `collarbone_breaker` | st=20 ac=4 tot=42 dmg=600 | st=20 ac=4 tot=42 dmg=600 | ✅ |
| juri `standing_medium_punch` | st=6 ac=4 tot=21 dmg=600 | st=6 ac=4 tot=21 dmg=600 | ✅ |
| juri `throw_forward` | st=5 ac=3 tot=30 dmg=1200 | st=5 ac=3 tot=30 dmg=1200 | ✅ |
| terry `crouching_light_kick` | st=5 ac=2 tot=17 dmg=200 | st=5 ac=2 tot=17 dmg=200 | ✅ |
| terry `standing_medium_kick` | st=9 ac=3 tot=29 dmg=700 | st=9 ac=3 tot=29 dmg=700 | ✅ |
| guile `knee_bazooka` | st=8 ac=5 tot=27 dmg=500 | st=8 ac=5 tot=27 dmg=500 | ✅ |
| guile `standing_medium_punch` | st=7 ac=3 tot=24 dmg=600 | st=7 ac=3 tot=24 dmg=600 | ✅ |

**8/8 完全一致**。恒等式も手計算で確認(GC-1: `40+2−38=4`・GC-5: `34+2−34=2`・landing はいずれも `KA+1`)。

### 3.3 B-3 納品契約 vs 実装

| 契約項目 | 実装 | 判定 |
|---------|------|------|
| エンドポイント | `GET /api/combos/:comboId/setplay-suggestions`(`internal/api/setplay/routes.go`) | **一致** |
| query `n_min` | `handler.go:42-48`(非整数は 400) | **一致** |
| query `sort` = `n\|target` | `handler.go:50-57`(`""`→`n`。それ以外は 400) | **一致**(旧 `steps` は拒否) |
| query `target_types` | `handler.go:60-64`(省略時 `defaultTargetTypes()`) | **一致** |
| query `include_zero_damage` | `handler.go:66-72`(bool 以外 400) | **一致** |
| query `target_move_id` | `handler.go:74-80`(正整数以外 400) | **一致** |
| DTO `s`/`n`/`landing`/`targetActive`/`alreadyAdopted` | `dto.go:14-21`(すべて camelCase JSON) | **一致** |
| `role ∈ {filler,target,parent}` | `service.go:19-22`(`parent` は M19-02 予約・本サブ未出力)・`dto.go:9` | **一致** |
| `counted` | `dto.go:10`。全ステップ `true`(`service.go:307,314`) | **一致** |
| 既定 ON = `normal`/`unique`/`special_projectile` | `handler.go:100-106` | **一致** |
| 既定 OFF = `special`/`throw`/`normal_rush`/`unique_rush` | 上記に含まれない＝OFF。種別キーは `service.go:39-46` | **一致** |
| `damage>0` 既定除外 | `service.go:289-291` `if !params.IncludeZeroDamage && (m.Damage == nil || *m.Damage <= 0)` | **一致**(NULL も除外) |
| `category=system` 常時除外 | `service.go:110-112` の `default: return ""` → `:279` で `ttype == ""` を除外 | **一致**(自動列挙時。明示指定時は §3.4 B-4 参照) |
| `include_zero_damage` の緩和(system は含めない) | 種別 `""` は type 判定で先に落ちるため、`IncludeZeroDamage=true` でも system は入らない | **一致** |
| `setup_only` で絞り込まない | `service.go:203-208` の filler 収集条件は `m.Total != nil && *m.Total >= 1` **のみ**。`grep -rn "SetupOnly" internal/service/` → **0 件**(repo は SELECT するが service は未使用) | **一致**(撤回済み仕様を実装していないことを確認) |
| VAL-S04(同一親コンボ・同一レシピ→ブロック) | 採択保存は FE `setupApi.create(comboId, ...)` → 既存 setup サービス経路。`internal/service/setup/validate.go:18` `CodeS04Duplicate = "VAL-S04"`、`internal/service/setup/service.go:119` で `CalcSetupRecipeHash` | **一致** |
| VAL-S05(親コンボID必須) | `internal/service/setup/validate.go:19` `CodeS05ParentRequired = "VAL-S05"` | **一致** |
| `alreadyAdopted` が名前非依存のレシピ一致 | `service.go:319-320` `hash := setupsvc.CalcSetupRecipeHash(setupSteps)` → `FindDuplicateInCombo(...)`。同関数(`internal/repository/setup/repository.go:620-656`)は **setups.name を参照せず** `combo_id`+`character_id`+レシピハッシュのみで判定 | **一致** |
| `setups.name` を FE 側で i18n から生成 | `SetplaySuggestionSection.tsx:313-316` `t("setplay.adoptNameFormat", {move, n})`。ja=`{{move}} 持続{{n}}F目重ね` / en=`{{move}} meaty on active frame {{n}}` の**両キー存在** | **一致** |
| 技名は `moves.nameJa` 固定 | `SetplaySuggestionSection.tsx:59` `map.set(m.id, m.nameJa ?? m.code)`。**`nameEn` 相当の分岐が無い**ため、**英語 UI でも技名は日本語**(書式のみ英語)。例: `かかと落とし meaty on active frame 3` | **一致(仕様どおり)**。§4-7 に運用注意として再掲 |
| `M19-LIMITATION-NOTICE` 最低 4 箇所 + 相互参照 | **6 箇所 / 5 ファイル**: `service.go:185`(d KA NULL)・`:204`(c filler 規則)・`:275`(b target 規則)・`web/src/locales/ja.json:424`・`web/src/locales/en.json:424`・`web/src/features/setplay/components/SetplayLimitationNotice.tsx:14`。Go 側は i18n キー `setplay.limitations.*` を、i18n 側は `internal/service/setplay/service.go` を**相互に名指し**している | **一致** |

### 3.4 B-4 残留リスクの独立評価

#### (e) ラッシュ版 target のフレーム基準

**実データの実測**: `character_data/ryu.csv` の `rush_*` 全 17 件で、`startup` と `total` がともに元技 **+11 で完全一定**。

```
rush_standing_light_kick   startup=16  base standing_light_kick=5   diff=11 | total 29 vs 18  diff=11
rush_collarbone_breaker    startup=31  base collarbone_breaker=20   diff=11 | total 53 vs 42  diff=11
rush_axe_kick              startup=21  base axe_kick_2=10           diff=11 | total 54 vs 43  diff=11
（17/17 すべて diff=11）
```

使用箇所: `service.go:221` `target := Target{Name: tc.Code, Startup: *tc.Startup, Active: *tc.Active}` → `Suggest(ka, fillers, target, opt)` → `setplay.go:151` `budgetMax = KA + 2 - nMin - target.Startup`。つまり**格納 `startup` がそのまま S の材料**になる。

**評価**: +11 の一定オフセットは「ドライブラッシュ発動分を含む絶対発生」として**内部的に一貫している**(オフセットが技ごとにブレていない=フレームメーター表示由来と整合)。ただし**この 11F が実機の raw ドライブラッシュ発動から攻撃発生までの実測値と一致するか、および前段 filler 直後に DR を仕込めるか(先行入力の可否)は、本監査では確認できなかった**。addendum 自身も「フレーム基準はフレームメーターに準拠＝実機確認前提」と明記している。→ **未確認・実機検証要**。

**rush 種別が既定 OFF であること**: **確認**(`handler.go:100-106` の既定 3 種別に rush が含まれない)。

**ただし監査独自の指摘**: **rush 版は既定でも `filler` に入る**。filler の条件は `total ≥ 1` のみ(`service.go:206`)で `is_derived` も種別も見ない。ryu の filler 候補 84 件のうち **17 件が rush 版**。実データで再現した既定 3 種別・KA=40 の探索では、以下のような**rush filler を含む提案が既定条件で生成される**:

```
rush_crouching_medium_punch > standing_light_punch
standing_light_punch > rush_standing_light_punch > standing_light_punch
rush_standing_medium_punch > standing_medium_punch
```

「未検証のフレーム基準を既定から外す」という rush target 既定 OFF の趣旨に対し、**filler 側は同じ基準を既定で使っている**＝**非対称**。加えて空振り DR はドライブゲージを消費し前進するため、間合い・ゲージの前提が変わる(エンジンはいずれもモデル化しない)。→ **設計判断を要する残留リスク**。

#### B 型 air-only 技(`is_derived=false ∧ is_aerial=false` の「最速でも当たらない空中技」)

**機械除外されず target に出得ることを確認**。`collectTargets`(`service.go:250-295`)の除外条件は「startup/active NULL」「種別不一致」「`is_aerial=true`」「`is_derived=true`(非 rush)」「damage 0/NULL」のみで、**「地上の相手に当たるか」の判定は無い**。指示書 §7.4 の既知割り切りどおり＝**仕様通りを確認**(実装欠陥ではない)。

#### 弾(`special_projectile`)既定 ON

**実装が既定 ON であることを確認**(`handler.go:104` `setplaysvc.TargetTypeSpecialProjectile`)。妥当性は運用判断(弾は着弾距離依存のため重ね概念が成立するかはケース次第)。

#### 追加検出(監査独自)

| # | 内容 | 根拠 | 評価 |
|---|------|------|------|
| B-add-1 | **`is_derived` の扱いが target と filler で非対称** | target は `is_derived=true`(非 rush)を「単独では出せない」として除外(`service.go:286`)。一方 filler は除外しない。ryu の filler 候補には**非 rush 派生技 11 件**が含まれ、既定 3 種別 KA=40 で実際に `high_double_strike > standing_medium_punch`(上段二連撃＝`category=target_combo`・`is_derived=true`)や `standing_light_punch > fuwa_triple_strike_2hits > standing_light_punch`(不破三連撃(2発止め))といった提案が生成される。DES-003 §3.3 の `is_derived` 判定原理は「**コントローラでこの技だけをいきなり出せるか？出せない → true**」であり、その定義に従えば**これらを単独の空振りとして先頭・中間に置く提案は入力不可能**になり得る | 指示書 §4.5「全技を filler 候補」に**準拠**しているため実装欠陥ではない。**仕様の穴**として設計判断を要する。`total` が「派生部分のみ」か「連携全体」かも DES-003 に定義が無く未確認 |
| B-add-2 | **`alreadyAdopted` を打ち切り前の全提案で算出(N+1)** | `service.go:231` が提案ごとに `buildProposal` を呼び、その中で `FindDuplicateInCombo`(`internal/repository/setup/repository.go:620-656`)を実行。同関数は候補 setup 1 件ごとに `FindStepsBySetupID` を**追加発行**する。`maxProposals=200` の打ち切りは `:241`＝**その後**。**実データで再現した実測**: ryu KA=40・既定 3 種別 → target 21 件・提案 **210 件**(＝DB 往復 210 回 ×(1+setup 件数))/ juri KA=40 → **323 件** / ryu 全種別 ON → **459 件** / ryu KA=27 → 42 件 | ローカル SQLite で数百往復のため**実害は軽微**と評価。ただし NFR001(100ms)を明示根拠にしている実装(`service.go:24-26`)としては、**打ち切り後の 200 件に対してのみ判定する**方が素直。**あわせて判明**: ryu KA=40 の既定条件は 210 件＝`maxProposals=200` を**超えるため `truncated=true` になる**。完了報告 §1「golden-case・E2E とも truncated=false(MaxResults=200 未到達)」は**検証シナリオ限定の事実**であり、通常運用では打ち切りが起こり得る |
| B-add-3 | **明示 `target_move_id` は BE で system を除外しない** | `service.go:266-272` が明示指定時に種別/damage/`is_derived`/`is_aerial` を**全バイパス**。`category=system`(ドライブパリィ等)の除外は **FE ピッカーのみ**(`SetplayTargetPicker.tsx:20,28` `EXCLUDED_CATEGORY = "system"`) | API 直叩きで system を target にできる(害は「意味の無い提案が出る」程度)。**多層防御が片側**という指摘 |
| B-add-4 | **handler の doc comment が旧仕様のまま** | `internal/api/setplay/handler.go:30` `&sort=n\|steps         // 省略時 n(N 降順)`。実装は `n`/`target` のみ受理し `steps` は 400(`:50-57`)。addendum §2 は「`sort` は `n\|target`(`steps` を廃止)」 | **コメントのみ陳腐化**。製造担当の軽微是正 |
| B-add-5 | (参考)`MoveCandidate.SetupOnly` が未使用 | `internal/repository/setplay/repository.go:28,49,65` で SELECT・Scan するが `internal/service/setplay/` で参照ゼロ | 撤回済み仕様の残骸。将来の絞り込み再検討に備えた保持とも読めるため**指摘としては最軽微** |
| B-add-6 | **負の KA では理由が出ず「提案なし」になる** | DES-006 VAL-C10(`06-validation.md:47`)は `knockdown_advantage` の範囲を **-600〜+600・WARNING** とし**負値を保存できる**。一方エンジンは `KA < 0` を `ErrNegativeAdvantage` で弾く(`setplay.go:104,125`)。ただしグルー層が `Suggest` のエラーを `continue` で飲む(`service.go:223-226`)ため、**API は 500 にならず 200 + 0 件**を返す。UI は `setplay.empty`「提案が見つかりませんでした」を表示 | **クラッシュ・500 は起きない**(防御的スキップが効いている)ので実装欠陥ではない。ただし**負 KA が「条件が悪い」ではなく「未対応」であることがユーザーに伝わらない**。KA NULL には専用文言(`setplay.noKnockdown`)があるのと非対称 |

### 3.5 B-5 テスト・回帰

| 検査 | 結果 |
|------|------|
| `go build ./...` | **PASS**(exit 0) |
| `go vet ./...` | **PASS**(exit 0・出力なし) |
| `go test ./...` | **PASS**。`ok` **45 パッケージ**・FAIL **0** |
| `go test ./internal/service/setplay/... -run TestGoldenCases` | **PASS 4/4**(§3.2) |
| `pnpm exec tsc --noEmit` | **PASS**(exit 0) |
| `pnpm test -- --run`(Vitest) | **PASS**。**114 ファイル / 815 テスト**すべて green(setplay 分: `SetplayLimitationNotice.test.tsx` 5 件・`SetplaySuggestionSection.test.tsx` を含む) |
| locales parity | **PASS**。`src/locales/locales.test.ts` 2 件 green。監査側でも独立に集合比較し **ja 337 キー / en 337 キー・差分 0** を確認 |
| E2E(`make e2e`) | **本監査では未再実行**(開発者判断 2026-07-25)。M19-01 完了報告の A/B/C 3/3 PASS を再現していないため、E2E 観点は**未確認**として扱う |

**落ちたものは無し。** 完了報告の「Vitest 794 PASS」は当時の値で、現在は 815(M18-02 等の追加分を含む)。

---

## 4. 修正が必要と思われるもの(担当の当たり付き)

> 監査は修正しない。以下は**発見の報告**であり、実施判断は開発者・中央(設計担当)・製造担当に委ねる。

| # | 内容 | 重要度 | 担当の当たり |
|---|------|-------|-------------|
| **1** | **DES-003 §3.4 に KA 意味論(errata②)を反映**((a)無敵時間 /(b)起き上がり=KA+1 /(c)S 定義 /(d)N 定義 /(e)成立条件)。現状はコード・指示書が**存在しない節を正典として参照**している(§2.2 A-1) | **最高** | **中央**(三点セット反映)or **開発者**(受領済み差し替え版の適用)。差し替え版が repo に届いていないのが根因なので、**搬送の確認から**入るのが妥当 |
| **2** | **DES-003 に `combo_punish_starters`(CHANGE-083)を反映**。実 DB に存在する表がデータモデル正典に無い(§2.2 A-7)。あわせて CHANGE-084 の移動 system move `total` 注記(A-8) | **最高** | **中央**(M18-02 実装完了済み＝M17-D10 の「実装後反映」トリガは既に成立) |
| **3** | **change-number-registry の再開**: 083/084/085 の起票を §1 と更新履歴へ記録し「次番号」を是正(§2.3 A-10)。**CHANGE-085 通知書本体を repo へ格納**(§2.2 A-2。現在 addendum 2 本のみで本体が無い) | **高** | **中央**(採番台帳は中央が正)。**監査は採番しない** |
| **4** | **CHANGE-085 の反映**: DES-002 §4.2 へ提案エンドポイント + query 5 種、DES-005 へ提案 UI(相乗り先 = **ComboDetailPage**・種別フィルタ・damage 既定除外・生成ステップ・技ピッカー・S 非表示・sort=n/target)。addendum 2 本の内容を含める(§2.2 A-3/A-4) | 高 | **中央** |
| **5** | **CLAUDE.md §10.X へ `setplay-notice-seen-v1` を #4 として追記**(§2.2 A-5) | 中 | **開発者**(CLAUDE.md 本体は製造担当が編集しない) |
| **6** | **DES-003 §3.3 の排他的断定を是正**: `is_projectile` の「**唯一の**一次源」・`is_derived` の「**役割はただ一つ**」を setplay consumer 追加後の実態に合わせる。`original_move_id` の rush 種別分類用途、`setup_only` の「将来の自動提案のための予約列/利用ロジック未実装」の陳腐化も併せて(§2.2 A-6)。**あわせて DES-006 VAL-C10 の `knockdown_advantage` 範囲 -600〜+600(負値を保存可)と、負を弾く提案エンジン(`setplay.go:104,125`)の整合を確認されたい**。現状の挙動は 500 ではなく「提案 0 件」(§3.4 B-add-6)＝安全側だが、負 KA が「未対応」であることが UI に出ない | 高 | **中央** |
| **7** | **REQ-001 に M19(セットプレイ自動提案)の FR を追加**。現状 FR011(転用支援)のみで、フレーム計算による新規提案の要件番号が無い(§2.2 A-9) | 中 | **中央**(CHANGE-085 のスコープ外＝別途要否判断) |
| **8** | **rush 版 filler の扱いを決める**: rush target を既定 OFF にした趣旨(未検証のフレーム基準)に対し、filler は既定で rush を含む(§3.4 (e))。「filler も既定除外」「明示 opt-in」「現状維持＋制約告知に明記」のいずれかを選択 | 中 | **中央**(仕様判断)→ 決定後に**製造** |
| **9** | **`is_derived` 技を filler に含めるかを決める**: DES-003 §3.3 の判定原理(単独で出せない→true)に照らすと入力不可能な提案が出得る(§3.4 B-add-1)。あわせて `target_combo` の `total` が「派生部分のみ/連携全体」どちらかを DES-003 で定義 | 中 | **中央**(仕様判断)→ 決定後に**製造** |
| **10** | **`alreadyAdopted` を打ち切り後の 200 件に限定**(N+1 の実測 210〜459 往復。§3.4 B-add-2)。あわせて完了報告の「truncated 未到達」記述に「検証シナリオ限定」の但し書き | 低〜中 | **製造** |
| **11** | **`internal/api/setplay/handler.go:30` の doc comment を `n\|target` へ是正**(§3.4 B-add-4) | 低 | **製造** |
| **12** | **明示 `target_move_id` 時の system 除外を BE にも入れるか判断**(現状 FE ピッカーのみ。§3.4 B-add-3) | 低 | **中央**(仕様)or **製造**(多層防御として) |
| **13** | **DES-004 の版番号重複(第13〜16版が各 2 回)を是正**(§2.3 A-11)。**SUPP-001 v1.27.0 の更新履歴エントリを追加**(A-15)。**前提文書欄の版ズレを一括追従**(A-12。2026-07-06 errata の前例あり) | 低 | **中央** |
| **14** | (参考)`progress-summary.md` を M17〜M19 分まで前進させる。CLAUDE.md §1 が「現在地の一次情報」と定めている資料が 3 マイルストーン遅れ(§2.3 A-16) | 低 | **開発者**(`/update_progress_summary`) |

---

## 5. 確認できなかった項目(未確認)

| # | 項目 | 理由 |
|---|------|------|
| 1 | **別リポ不変**: `autopilot-combomgr/projects/setplay-suggestion` が変更されていないこと | 本ワークスペースに当該リポが存在しない(`ls /workspaces/` → `combomgr` のみ)。**間接証拠**として PR #10 の差分は combomgr 配下のみ。移植元との差分比較(移植の忠実性)も同理由で未実施 |
| 2 | **rush 版の +11 フレームが実機の絶対発生と一致するか** | seed CSV 内では 17/17 一定で内部整合するが、実機(フレームメーター)照合は本監査の手段外。**実機検証要**(addendum も同旨) |
| 3 | **rush を filler として空振りする際の実入力可能性**(前段 filler 直後に raw DR を仕込めるか・ゲージ消費・前進距離) | ドメイン実測が必要 |
| 4 | **`target_combo` の `total` の意味**(派生部分のみ / 連携全体) | DES-003 に定義が無く、seed CSV からも弁別できない |
| 5 | **E2E(A/B/C)の再現** | 開発者判断により `make e2e` を実行していない。完了報告の 3/3 PASS は**本監査では未裏取り** |
| 6 | **errata② の差し替え版が実際に送付されたか** | 中央側の送信記録は repo 外。repo 内には痕跡が無い(DES-003 本文・ステータス欄・change-notes いずれにも errata② の記載なし)ことのみ確認 |
| 7 | **CHANGE-085 通知書本体の内容** | repo に存在しないため参照不能。反映範囲は addendum 2 本の記述からのみ把握 |

---

## 6. 「異常なし」を確定させた項目(一致の記録)

監査プロンプト §0 に従い、差分だけでなく一致も事実として記録する。

- `docs/handover/docs-map.md` の版対応表は **7/7 文書で実ヘッダと一致**(§2.1)。
- DES-002 の CHANGE-071 反映(§4.2 の intake API 2 本)、DES-003 の CHANGE-078〜081 反映(§3.15/§3.16/§3.17/§3.4/§3.3)、DES-004 の CHANGE-081 反映(§2.5)、DES-005/DES-006 の CHANGE-082 反映(§5.4/§2.7)、REQ-001 の CHANGE-077 反映(§7)は、**いずれも本文に実在することを確認**(＝主張どおり)。
- `docs/design/` に旧版の別コピー・重複ファイルは**無し**(M16-2 型の再発なし)。
- M19-01 の高リスク不変条件 **8 項目中 7 項目 OK・1 項目のみ未確認**(別リポ)。受理帯・KA+1・スキーマ不変・FR011 非破壊はいずれも引用付きで OK。
- golden データのフレーム値は seed CSV と **8/8 一致**。golden テストは N・landing・S の 3 値を等値アサートし **4/4 PASS**。
- 納品契約 vs 実装は **20 項目すべて一致**(§3.3)。撤回済み仕様(`setup_only` 絞り込み)を**実装していない**ことも確認。
- `M19-LIMITATION-NOTICE` は要件「最低 4 箇所」に対し **6 箇所 / 5 ファイル**、Go ↔ i18n の**相互名指し**あり。
- locales parity **ja 337 / en 337・差分 0**。
- 提案 UI の相乗り先は **`ComboDetailPage.tsx:178`**(契約どおり。`SetupSelectorModal` ではない)。
- localStorage は `createLocalStorageHelper` 経由・`-v1` suffix・try-catch(helper 側)＝CLAUDE.md §10.X の**実装ガイドラインに適合**(表への登録のみ未了)。
- B 型 air-only 技が target に出得ることは**仕様どおり**(既知割り切り)。`special_projectile` 既定 ON も**実装が契約どおり**。
- `go build` / `go vet` / `go test ./...`(45 パッケージ)/ `tsc --noEmit` / Vitest 815 件 / locales parity **すべて緑・落ちたものゼロ**。

---

## 7. 監査の read-only 遵守

- 本レポート(`docs/progress/M19-audit-20260725.md`)**以外のファイルを一切変更していない**(`git status` で確認)。
- DES/REQ/SUPP・ソース・マイグレ・seed・CLAUDE.md は**未編集**。CHANGE 番号・マイグレ連番は**採番していない**。
- §3.4 B-add-2 の定量化に用いた Python 再現スクリプトはセッションのスクラッチパッド配下に置き、**リポジトリには追加していない**。

---

*本書は発見の報告であり、DES/コードの修正・CHANGE 採番は行っていない。修正の実行は開発者・中央(設計担当)・製造担当の判断による。*
