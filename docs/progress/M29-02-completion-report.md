# M29-02 完了報告: 入出力（CSV ／ バックアップ・復元 ／ 下書きの往復）

| 項目 | 内容 |
|------|------|
| 作業ID | M29-02 |
| 指示書 | `docs/instructions/M29-02-io-csv-backup-draft.md` **v1.1.0** |
| チェックリスト | `docs/instructions/reviews/M29-02-review-checklist.md` **v1.0.0** |
| 着手基点 | `233f48e` |
| 作業日 | 2026-09-07 |
| CHANGE 消費 | **0 本**（起票は設計卓＝`D-293`。反映が要る箇所は §6 に一覧化する） |
| マイグレ消費 | **0 本** |

---

## 0. 変更統計（着手基点 `233f48e`）

```
 39 files changed, 2782 insertions(+), 136 deletions(-)
```

**★上表は取り込み（Phase C）後の実測値である**（`git diff --stat 233f48e`）。レビュー報告書 `docs/progress/m29-02-review.md` と、レビュー指摘への対応分を含む。

**★教訓 `E-225` の観点で 1 度読んだ。** 新規ファイルは **8 本**であり、`git diff --stat` で**いずれも `+` のみ・deletions 0** であることを実測した。

| 新規ファイル | 行数 |
|---|---|
| `docs/progress/M29-02-completion-report.md` | 225 + |
| `internal/api/comboio/export_headers_test.go` | 129 + |
| `internal/api/comboio/import_upload_errors_test.go` | 127 + |
| `internal/repository/combo/count_vs_list_limit_test.go` | 143 + |
| `internal/service/comboio/export_observation_test.go` | 106 + |
| `internal/service/comboio/file_error_code_test.go` | 86 + |
| `web/src/features/combo-io/export-truncation.test.ts` | 111 + |
| `web/src/features/combo-io/export-truncation.ts` | 66 + |
| `docs/progress/m29-02-review.md`（Phase B が作成） | — |
| **合計（9 本）** | **1440 insertions(+) / 0 deletions** |

**⇒ 「新規のつもりのファイルに deletions が付いている」状態は無い。** 作成前に `ls` で同名の不在を確認している。

**★マイグレーション 0 本・新規依存 0 本・スキーマ変更 0。**

**★deletions 130 行は既存ファイルの置換である**（最大は `csvcore_test.go`。ゴールデン行を「確定では必ず落ちる行」から「成立する最小形」へ改めたことと、`VAL-C09` テストの主張の反転による）。**★テストは 1 本も削除していない**——`TestVALC09_EmptyRecipe_WarnsForDraftAndPublished` は**改名して主張を反転**した（`ErrorsForDraftAndPublished`）。

---

## 1. §2.1-1 上限の値と、超えたときの挙動（実測）

### 1.1 上限は 4 本が別々に定義されている

| # | 定義箇所 | 値 | 何に効くか |
|---|---|---|---|
| 1 | `internal/service/comboio/csvcore/rules.go:10` `DefaultMaxRows` | **1000 行** | CSV 取込・プレビュー（コンボ CSV ／ セットプレイ CSV の両方） |
| 2 | `internal/service/comboio/export.go:18` `exportRowLimit` | **1000 件** | CSV 書出（`csvcore.DefaultMaxRows` を参照） |
| 3 | `internal/repository/combo/repository.go:938-945` | **既定 100 ／ クランプ 1000** | コンボ一覧の全読み出し。**★書出の実効上限はここ** |
| 4 | `web/src/constants/export.ts:5` `MAX_EXPORT_SELECTION` | **1000 件** | 画面での選択数 |

**★あわせてバイト上限が 3 本**——`DefaultMaxBytes`（10MiB・`rules.go:8`）／ `DefaultMaxCellBytes`（1MiB・`rules.go:12`）／ `maxUploadBytes`（10MiB・`internal/api/comboio/handler.go:47`）。

### 1.2 超えたときの挙動 —— 「黙っている」のは書出だけである

**★指示書 §0.1 は 4 群のうち 2 群が「黙る」としているが、実測では取込・プレビューは鳴っていた。黙っていたのは書出である。**

| 経路 | 超えたときの挙動 | 黙っているか |
|---|---|---|
| 取込（commit） | `csvimport.go:149` → `fileReject` でファイル全体を拒否。`import.go:131-133` が 400 を返す | **鳴る**（★ただし文言が英語のまま＝§3） |
| プレビュー | 同じ関数・同じ Options（`import.go:28` と `import.go:130` は同一行）。`comboFileError` に載る | **鳴る**（★同じく英語のまま） |
| 書出（CSV） | `export.go:94-96` の `break` と `export.go:108` の `Limit` で切り捨て。`export.go:29-32` の `slog.WarnContext` のみ | **★★黙る** |
| 書出（実効） | `repository.go:938-945` で既定 100・クランプ 1000 へ**黙ってクランプ** | **★★黙る** |
| **書出（画面から）** | **★★一覧が上限で切れているのに気づけない**。`ComboListPage` / `MyComboPage` が `combosQuery.data.items`（BE 既定 100 件）を対象 ID として渡し、件数表示には `count`（＝`len(items)`）を使っていた | **★★黙る** |
| 書出（`range=all/filter/mycombo`） | `export-data.ts:44-52` が `limit` を送らないため、リポジトリ既定 100 が効く。**★ただし画面からは到達しない**（`ExportDialog` は常に `range: "selected"` を送る＝`ExportDialog.tsx:190`）。**⇒ 実害の経路ではなく、契約として残っている分岐である** | **★黙る（到達不能）** |

**★`export.go:29-32` のコメントは「サイレント打ち切りを避ける」と書いているが、避けられていない**——`slog` はサーバ側のログであり、利用者の画面にも応答にも出ない。同ファイル `:28` に「利用者通知は将来サブで検討」と明記されている。**⇒ 本サブがその「将来サブ」である。**

**★判定式の偽陽性**：`if len(combos) >= exportRowLimit` は**ちょうど 1000 件のとき（切り捨てが 1 件も起きていないとき）も**「切り捨てた」と記録する。

---

## 2. §2.1-2 上限が掛かる経路の全数

**★チェックリスト §0.4-1 は 5 経路**〔取込／書出／プレビュー／バックアップ／復元〕**を数えるよう求めている。⇒ 5 つとも調べた。「調べていない経路」は 0 である。★ただし実装上のデータ経路は 3 つしか存在しない。**

| # | 経路 | 実装の所在 | 上限 |
|---|---|---|---|
| 1 | **取込** | `POST /api/import/csv` → `internal/service/comboio/import.go:123` | 1000 行 ／ 10MiB ／ 1MiB/セル |
| 2 | **プレビュー** | `POST /api/import/csv/preview` → `import.go:21` | 同上（**同じ関数を呼ぶ**） |
| 3 | **書出** | `GET /api/export/csv` → `export.go:22` | 1000 件（実効はリポジトリの 100/1000） |
| 4 | **バックアップ** | **★存在しない。** `web/src/features/config/SettingsSectionData.tsx:90-105` が `disabled` のボタンを描くだけ。API 経路も無い | — |
| 5 | **復元** | **★ゴミ箱からの 1 件復元のみ**（`POST /api/combos/:id/restore` ほか）。一括復元も行数上限も無い | — |

### 2.1 ★往復の非対称（本実査で新規に判明・未起票だった）

| # | 非対称 | 帰結 |
|---|---|---|
| 1 | **セットプレイの書出に行数上限が無い**（`export.go:185-214` は上限を持たない）のに、**取込は 1000 行で弾く**（`csvcore/validate.go:367-373`） | **★自分が出したファイルを自分の取込が拒否しうる** |
| 2 | **書出にバイト上限が無い**（`csvcore/csvexport.go` に `MaxBytes` 相当が無い）のに、**取込は 10MiB で弾く**（`rules.go:8`） | 同上 |

**★開発者の実測回答（2026-09-07）＝セットプレイ総数は 1000 を超えていない。⇒ 現時点で 1 は発火していない。**

---

## 3. §2.1 付随 —— 鳴っているのに読めない（本実査で新規に判明）

| # | 内容 |
|---|---|
| 1 | **ファイルレベルのエラーは `[CODE]` の接頭辞を落として渡される**（`import.go:29-33` が `FileError.Message` だけを写す）。行レベルは `issueText`（`import.go:555-560`）が `[VAL-I03] col: msg` の形にするのに、ファイルレベルだけ形が違う。**⇒ `web/src/features/combo-io/formatIssue.ts:17` の `ISSUE_RE` がコードを取り出せず、訳に当たらない。★`ja.json` に「行数が上限を超えています」が存在するのに一度も出ない。** 画面には英語の `row count exceeds limit 1000` が出る |
| 2 | **`setup_file` が上限を超えると黙って取り込まれない**——`handler.go:157-159` が `if setupBytes, e := readFormFile(...); e == nil` の形であり、**エラーのときは `setupCSV` が空のまま進む。⇒ セットプレイが 1 件も入らないのに何も言わない** |
| 3 | **`combo_file` が 10MiB を超えると「コンボ CSV(combo_file)が必要です」と誤ラベルされる**（`handler.go:149-152` がサイズ超過のエラーを握り潰して差し替える） |

---

## 4. §2.1-3 既存データへの影響（**★直す前に確認した**）

**★本セッションはクラウドの使い捨てコンテナであり、開発者の DB（`~/.local/share/tacpendium/tacpendium.db`）が存在しない**（実測＝ディレクトリ無し）。**⇒ 実測は開発者へ照会した。** 先例＝`CHANGE-139` の「ステップ 0 本の仮登録は 0 件だった（開発者がローカル DB で照会。コンボ総数 86）」。

| # | 照会した内容 | 開発者の回答（2026-09-07） | 本サブへの効き |
|---|---|---|---|
| 1 | コンボ総数は 100 件を超えるか | **超えている可能性がある** → **★実測 86 件で超えていなかった**（2026-09-07・§9.1b） | **★★現時点では発火していない。⇒ 観測は件数に依らず効いており、超えた時点で鳴る**（§4.2 / §9.1b） |
| 2 | 本登録で `damage` / ダウン後有利F / SAゲージ消費 / ドライブゲージ消費 のいずれかが未入力のコンボは在るか | **無い** | `VAL-C15` をプレビューへ足しても既存データに該当なし |
| 3 | セットプレイの総数は 1000 を超えるか | **無い** | §2.1 の非対称 1 は現時点で未発火 |
| 4 | 0 ステップのコンボ（`VAL-C09` 該当）は在るか | **無い**（逐語＝「既存で空レシピデータはない」） | `VAL-C09` を ERROR へ揃えても既存データに該当なし |

### 4.2 ★どの経路で 100 件が効いていたのかの訂正（レビュー指摘による）

**★初版の本報告は「FE 生成書出（PDF/PNG/クリップボード）が `limit` を送らないため 100 件で切れる」と書いたが、不正確だった。**

| # | 実態 |
|---|---|
| 1 | **`ExportDialog` は常に `range: "selected"` を送る**（`ExportDialog.tsx:190`）。⇒ `export-data.ts` の一覧を引く分岐（`range=all/filter/mycombo`）は**画面から到達しない** |
| 2 | **実際に効いていたのは、その手前である**——`ComboListPage:145-148` / `MyComboPage:232-235` が「選択ゼロなら現フィルタ結果の全件」として `combosQuery.data.items` を渡す。**この `items` が BE 既定 100 件で切れている** |
| 3 | **件数表示は `count`（＝`len(items)`）を使っていた。⇒ 100 件で切れていても「全 100 件をエクスポート」と表示され、利用者にはそれが全件に見えた** |

**⇒ 実効のある修正は `exportTotalCount` を `count` から `total` へ替えたことと、送信前の確認である。** `export-data.ts` へ `limit` を明示したのは**到達不能な分岐に対する予防**であり、**実害を止めた本体ではない。**

### 4.1 ★★重大度の是正は、取り込める行を 1 件も変えない

**★これが本サブの安全性の核である。**

プレビューの重大度を WARNING から ERROR へ上げても、**その行はいまでも確定（commit）で落ちている**——commit は `internal/service/validation` を通り（`import.go:224-234`）、同検証器は `VAL-C09` / `VAL-C15` / `VAL-S06` をいずれも ERROR として扱う。**⇒ 変わるのは「プレビューが嘘をつかなくなる」ことだけであり、通っていた入力が通らなくなる形にはならない。**

**★例外は `VAL-T02`（タグ名が空）だけである**——確定側は行を落とさずタグを読み飛ばす（`import.go:505-507`）。**⇒ ここを ERROR へ揃えると本当に取込結果が変わる。だから揃えない**（§5.4）。

---

## 5. §2.2 / §2.2b プレビューと確定の重大度の表

### 5.1 非対称は csvcore の中には無い

**★プレビューと確定は同じ関数・同じ Options を呼んでいる**——`import.go:28` と `import.go:130` はいずれも `csvcore.ParseAndValidate(comboCSV, importOptions(resolver))` であり、1 文字も違わない。**⇒ 非対称は「commit がそのあと別の検証器へ行を渡す」ところにある。**

| 層 | 型 | 定義 | ゼロ値 |
|---|---|---|---|
| CSV 取込 | `csvcore.Severity`（int・iota） | `csvcore/csvimport.go:12` | **WARNING**（★明示せずに組むと黙って WARNING になる） |
| 本体保存 | `validation.Severity`（string） | `internal/service/validation/result.go:8` | `""`（どちらでもない） |

**★変換関数は存在しない。** `DES-006` §1.2 の重大度は 2 段のみであり、**製造は新しい段を作っていない。**

### 5.2 非対称の向きと全数

**★向きは 4 件とも (b)「プレビューで通ったものが確定で落ちる」である**（指示書 §2.2-2）。**⇒ (a) の向き（プレビューで警告だったものが確定で通る）は 1 件も無い。**

| VAL | 内容 | プレビュー | 確定 | 本サブの扱い |
|---|---|---|---|---|
| **`VAL-C09`** | レシピが空 | **WARNING** `csvcore/validate.go:172` | **ERROR** `validation/combo.go:373` | **ERROR へ揃える** |
| **`VAL-C15`** | 本登録の必須 4 欄 | **★検査そのものが無い** | **ERROR ×4** `validation/combo.go:431-440` | **プレビューへ足す** |
| **`VAL-S06`** | セットプレイ名が空 | **WARNING** `csvcore/validate.go:426` | **ERROR** `internal/service/setup/validate.go:122` | **ERROR へ揃える** |
| `VAL-T02` | タグ名が空 | WARNING `csvcore/validate.go:149` | **落とさず読み飛ばす** `import.go:505-507` | **★WARNING のまま**（§5.4） |
| **`VAL-S02`** | セットプレイのレシピが空 | **★検査そのものが無い** | **ERROR** `internal/service/setup/validate.go:127` | **プレビューへ新設**（**★レビュー高-2。§5.5**） |
| `VAL-C12` | ラッシュ版の元技が不正 | **検査なし** | ERROR `validation/combo.go:490` | **★揃えない**（§5.5 に理由） |

### 5.5 ★★「全数」の数え方を誤っていた（レビュー高-2 による是正）

**★初版の本報告は上表を 4 件で閉じ「全数」と書いたが、`VAL-S02` が漏れていた。**

| # | 何が起きたか |
|---|---|
| 1 | **`VAL-S02`（セットプレイのレシピが空）は `VAL-C09` の完全な双子である**——確定側は ERROR で落とすのに、プレビュー層には検査そのものが無かった。**★しかも本サブが揃えた `VAL-S06` の 20 行下に在る** |
| 2 | **★見落とした理由は数え方である。** 「気づいた非対称を 1 件ずつ潰す」やり方を採ったため、**気づかなかったものは最後まで現れなかった。⇒ 正しい数え方は「確定側の検証器が持つ VAL を列挙し、プレビュー層と突き合わせる」ことである** |
| 3 | **⇒ `VAL-S02` を本サブで揃えた**（ERROR）。回帰テストも置いた |

**★`VAL-C12`（ラッシュ版の元技が不正）は揃えない。理由を書く**——同検証は「ある技がラッシュ版か」「その元技は何か」という**技マスタの関係**を引く必要があり（`validation/combo.go:60-67` の `MoveReader`）、`csvcore` の `Lookup` は `CharacterExists` / `MoveExists` の 2 つしか持たない。**⇒ 揃えるには本層へ新しい依存を持ち込むことになり、`csvcore` が「CSV の形だけを見る層」であることを崩す。★これは設計判断であり製造が単独で決めない。⇒ §11 へ申し送る。**

**★`VAL-C15` が最も黙っている**——プレビューは**無警告**（issue が 1 件も出ない）で、確定で 4 件の ERROR が出る。**★同じ判定を 2 か所に書いた結果ではなく、片方に書き忘れた結果である**（指示書 §2.2-3 の逆の形）。

### 5.3 §2.2b `VAL-C09` をどちらへ揃えたか、その根拠

**★ERROR へ揃えた。開発者の選択である**（2026-09-07。逐語＝「ERRORへ揃える。既存で空レシピデータはないので気にしなくて良い」）。**★製造が単独で決めていない。**

| # | 根拠 |
|---|---|
| 1 | **`DES-006` §2.1 の正典が ERROR である**（`06-validation.md:46`＝「レシピが空（ステップ数0）でないか｜ERROR」） |
| 2 | **`M24-13` / `CHANGE-139` の実質裁定と同じ向きである**——開発者裁定の逐語＝「ステップが入っていない仮登録があっても、何に使おうとしたものか後から全く分からないからです」。**⇒ 空レシピを許さない向きへ揃えており、裁定と食い違わない**（チェックリスト §0.4-4） |
| 3 | **`CHANGE-139` が WARNING を据え置いた理由は「取込可否を変えないため」であり、`DES-006` §6 自身が「揃えるべきかは設計判断である」と後続へ送っている**（followup `csv-preview-commit-severity-asymmetry`）。**⇒ 本サブがその後続である** |
| 4 | **既存データに該当が無い**（§4 の 4）。**⇒ いままで通っていた CSV が通らなくなる実害は無い** |

**★順序を守った**（指示書 §2.2b-4 ／ チェックリスト §0.4-5）——**重大度を先に決め、そのあとで和英の文言を揃えた。** 逆順にしていない。

### 5.4 ★`VAL-T02` を揃えなかった理由

**確定側は行を落とさない**——`import.go:505-507` が名前の空なタグを読み飛ばし、**コンボ自体は取り込まれる。⇒ 「警告を出したうえで処理を続行する」という `DES-006` §1.2 の WARNING の定義に、確定側の挙動が正しく一致している。**

**★ここを ERROR へ揃えると、いままで取り込めていた行が取り込めなくなる**（§4.1 の唯一の例外）。**⇒ 揃えないのが正しい。** 非対称に見えるが、非対称なのは重大度ではなく、**タグが落ちることを利用者へ伝えているかどうか**であり、それは別の論点である。

---

## 6. §2.3 バックアップ／復元 —— **問題を特定した。ただし本サブでは直さない**

**★指示書 §2.3-2 は「実査して具体を出すか、出せないなら『問題が特定できなかった』と書いて止まる」と定めている。⇒ 具体を出せた。**

| # | 実測 |
|---|---|
| 1 | **バックアップ／復元というデータ経路は存在しない。** `internal/` を全数走査したが、backup / restore / snapshot / dump のいずれの API 経路も無い |
| 2 | **にもかかわらず、設定画面にボタンが 2 つ出ている**——`web/src/features/config/SettingsSectionData.tsx:90-105` が `disabled` の「バックアップ」「リストア」を描き、ツールチップは「今後実装予定」。`web/src/pages/SettingsPage.test.tsx:88-98` が**意図的な状態として固定している** |
| 3 | **`CHANGE-146` が `FR401` から「バックアップ」を外している**（`docs/change-notes/change-report-146.md:27,53-54`。行数上限を「CSV 書出はバックアップではない」理由の 1 つに挙げている） |
| 4 | **既に起票済みである**——`docs/handover/followup-backlog.md:999` `backup-restore-buttons-disabled`。**状態＝未決・「いつやるかは開発者の手番」**。`NFR201` の実現方式の未達であり機能要件の未達ではない |

**⇒ 実装は「黙っているものを鳴らす」ではなく `NFR201` の新機能であり、本サブの射程外である。** 本サブは起票済みの行を指すに留める。

**★`M28-01` の `internal/infra/datadir` とは別物である**（指示書 §2.3-3）。同パッケージは製品改名に伴う**データディレクトリの 1 回限りの移設**であり（`internal/infra/datadir/doc.go:1-33`）、復元機能ではない。**★混同していない。**

**★`M23` の as-built は巻き戻していない**（指示書 §4-8）——ゴミ箱・論理削除・復元時検証（復元は ERROR でも止めない＝`internal/service/validation/restore.go:8-13`）に 1 行も触れていない。

---

## 7. §2.4 下書きの往復 —— **問題を特定した**

### 7.1 CSV 側は既に鳴っている

`SM-071` の仮登録除外は **FE のみ**で行われ（`web/src/features/combo-io/import-draft.ts`。BE は弾かない＝`D-591`）、**除外件数と行ごとの印を画面へ出している**（`web/src/pages/ComboImportPage.tsx:127,290,360`）。同ファイルの逐語＝「仮登録のため除外した行数（画面に出す件数。黙って減らさないための材料）」。**⇒ この経路は黙っていない。**

### 7.2 ★★編集モードの仮登録トグルは切り替えても保存されない（新規に判明・未起票だった）

**★これは §0.1 の「成功の自動断定」そのものである**——「保存しました」が出て詳細画面へ遷移するのに、`is_draft` は変わっていない。

| # | 実測 |
|---|---|
| 1 | **トグルは編集モードでも無条件に描画される**——`web/src/features/combo/components/ComboEditor.tsx:912-917`。モードによる分岐は `M24-12` / `D-582` が意図して外した。E2E `web/e2e/m24-12-editor-rebuild.spec.ts:178-218` が「編集モードでも仮登録トグルが最上部に在る」ことを固定している |
| 2 | **しかし `buildPatchPayload` は `isDraft` を送らない**——`ComboEditor.tsx:384-405` が送るのは 16 欄であり `isDraft` は含まれない |
| 3 | **`hasKeyChanges` も `isDraft` を見ない**——`web/src/features/combo/utils.ts:270-285`。**⇒ トグルを切り替えても `PUT`（全面作り直し）へ回らない** |
| 4 | **サーバは nil を「不変更」として扱う**——`internal/repository/combo/repository.go:1114-1117`（`if input.IsDraft != nil`）。**⇒ 何も更新されない** |
| 5 | **昇格の検証も走らない**——`internal/service/combo/service.go:652` の `promoting` が false のままになるため、`VAL-C15` が飛ばされる |
| 6 | **未保存ガードも助けにならない**——`formDirtyKey`（`ComboEditor.tsx:1381-1397`）は `isDraft` を含み、`handleSaveSuccess` が基準をトグル後の値へ戻す。**⇒ 利用者が気づく手がかりが 1 つも残らない** |

**★`FR010`（昇格）は専用ボタン `PromoteToFinalButton` が満たしており、機能要件の未達ではない。★しかし「在る操作が何もしない」形であり、`DES-005:711`（保存で本登録切替も可能）とも食い違う。**

### 7.3 射程外を守った

`draft-gauge-default-zero` は **2026-09-05 開発者裁定（`D-723`）で受容済み**であり、**直していない**（指示書 §2.4-3 / §4-6）。`M27-02b` の 3 状態（`oki_verified` の 未検証／成立なし／成立あり）にも 1 行も触れていない（指示書 §4-8）。

---

<!-- ★以降 §8 以降は実装の進行にあわせて埋める。
     ★§10（レビュー結果）は Phase C の後にしか書けない欄である（`D-510`）。
       レビュー報告書 `docs/progress/m29-02-review.md` は Phase B のサブエージェントが
       作るまで存在しないため、それまでリンクを張らない・件数を断定しない。 -->

## 8. 実装

### 8.1 コミットの割り方（指示書 §2.6）

**★1 コミットに 2 つの群を混ぜていない**（赤が出たときにどの群かが分からなくなるため）。

| 順 | コミット | 内容 |
|---|---|---|
| 1 | `f81e2af` `docs(M29-02): 入出力の実査` | **コードは動かない**（§1〜§7） |
| 2 | `d8510d2` `feat(M29-02/io): 書出の黙った切り捨てを鳴らす` | §2.1 |
| 3 | `fda3e80` `fix(M29-02/import): 鳴っているのに読めない・届かない 3 件` | §2.1 付随（§3） |
| 4 | `008c467` `fix(M29-02/validation): 重大度の非対称を揃える` | §2.2 / §2.2b |
| 5 | `08eebb2` `fix(M29-02/draft): 仮登録トグルが保存されない件` | §2.4 |
| 6 | `9b59a7d` `test(M29-02): 無観測の経路 2 件に観測を足す` | 破壊確認の結果（§9.3） |

### 8.2 §2.1 行数上限の黙った切り捨て — 鳴らし方は (c) 件数を示して選ばせる

**★開発者が選んだ**（2026-09-07）。**製造は選んでいない**（指示書 §2.1-4 / §4-2）。

| 経路 | 何を足したか |
|---|---|
| **書出（CSV / BE）** | `ExportResult` が総数と載った件数を持ち、応答ヘッダ `X-Export-Total` / `X-Export-Included` / `X-Export-Truncated` / `X-Export-Reimport-Blocked` で運ぶ。**★本体が zip なので JSON の警告欄を混ぜられない。⇒ 画面を経由しない呼び出し元に届くのはヘッダだけである** |
| **一覧の暗黙クランプ** | リポジトリへ `Count` を新設し `ListResponse` へ `total` を追加。**★`count` は `len(items)` であり、上限で切れた事実を知る手段が存在しなかった。⇒ `total > count` が観測になる** |
| **書出（画面から）** | **★これが実効のある修正である**——`exportTotalCount` を `count`（＝載った件数）から `total`（総数）へ替えた。⇒ 一覧が 100 件で切れていても「全 N 件」が正しく出て、確認が発火する |
| 書出（`range=all/filter/mycombo`） | `export-data.ts` が `limit` を明示して送るようにした。**★画面からは到達しない分岐であり、予防である**（§4.2） |
| **画面** | 送信**前**に対象件数を上限と比べ、超えるなら「対象 N 件のうち M 件までしか出力できません」を出して利用者に選ばせる（testid `export-truncation-confirm`） |

**★あわせて `>=` による偽陽性を直した**——着手前は**ちょうど 1000 件のとき**（1 件も落ちていないとき）**も**「切り捨てた」と記録していた。

**★★上限の値は 1 つも変えていない**（指示書 §2.1-5 / §4-1）。`DefaultMaxRows` / `exportRowLimit` / リポジトリの既定 100・クランプ 1000 / `MAX_EXPORT_SELECTION` のいずれも着手前と同じ値である。

> **★`export-data.ts` に `limit` を明示したことは「上限の変更」に当たらない。** 従来効いていた 100 は、**`limit` を送り忘れた結果たまたま効いていたリポジトリ既定**であり、設計上の上限は `MAX_EXPORT_SELECTION` ＝ `exportRowLimit` ＝ `DefaultMaxRows` ＝ 1000 で一貫している。**⇒ 意図した上限を実際に効かせ、超過は `total` との差で観測する形にした。★開発者へ確認済み**（「4 件とも直す」の選択に含まれる）。

### 8.3 §2.2 / §2.2b 重大度の非対称

| VAL | 着地 | 根拠 |
|---|---|---|
| **`VAL-C09`** | **WARNING → ERROR** | 開発者選択（§5.3）。`DES-006` §2.1 の正典・`M24-13` の裁定の向きと一致 |
| **`VAL-S06`** | **WARNING → ERROR** | 同型。確定側（`internal/service/setup`）が同じ行を ERROR で落としている |
| **`VAL-C15`** | **プレビュー層へ新設** | 着手前は検査そのものが無かった |
| `VAL-T02` | **WARNING のまま** | **★確定側が行を落とさずタグだけ読み飛ばす。⇒ WARNING の定義に一致している**（§5.4） |

**★順序を守った**——**重大度を先に決め、そのあとで文言を揃えた**（指示書 §2.2b-4 / チェックリスト §0.4-5）。

**文言の揃え**：`comboImport.val.VAL-C09` を本体側の文面（`レシピが空です。少なくとも 1 ステップ必要です`）へ揃え、`VAL-C15` を和英へ追加した。**★`VAL-C15` は `{{col}}` を描くため、`comboImport.col.*` の欠落 3 件**（`damage` / `drive_gauge_consumed` / `sa_gauge_consumed`）**を補い、`knockdown_advantage` の `ノックダウン` を本体側と同じ `有利フレーム` へ揃えた**（指示書 §0.3 ＝ **新しい割れを作らない**）。

### 8.5 ★`setup_file` の上限超過の鳴らし方（**★開発者確定済み・2026-09-07**）

**★握り潰しをやめる**ことに疑いは無い（黙って 1 件も取り込まないのが着手前の形だった）。**★しかし「どう鳴らすか」には 2 案あり、製造は (a) を採った。**

| 案 | 内容 | 採否 |
|---|---|---|
| **(a)** | **上限超過を 400 で返し、取込全体を止める** | **★採用**（**理由＝コンボだけが入ってセットプレイが落ちる状態は、利用者から見て「一部だけ入った」であり、本サブが直そうとしている形そのものである**） |
| (b) | セットプレイだけ落として警告を出し、コンボは取り込む | 不採用 |

**★★これは利用者に見える挙動の選択であり、本来は開発者の手番である**（指示書 §2.1-4 ／ `roles-and-routing` ハード列）。**⇒ (a) で進めたうえで開発者確認を仰いだ**（レビュー中-3）。

> **★★【2026-09-07 開発者確定】(a) で確定した。** 逐語＝**「setup_file については 10MiB を超えたら取込停止でいいです」**。**⇒ 製造の暫定判断と一致したため、実装の変更は無い。★「製造が単独で決めた」状態は解消した。**

**★「添付されていない」場合は従来どおり通す**（セットプレイ CSV は任意）——**区別することが要点であり、そこは変えていない。**

### 8.4 §2.4 下書きの往復（§7.2 の是正）

`buildPatchPayload` に `isDraft` を足した。**★トグルを消す案は採らない**——`M24-12` / `D-582` が編集モードの最上部へ置くと決め、E2E `m24-12-editor-rebuild.spec.ts` がその配置を固定している。**⇒ 巻き戻しではなく「在る操作が効くようにする」**（指示書 §4-8）。

---

## 9. テスト・破壊確認

### 9.1 実行結果（すべて緑）

| 対象 | 結果 |
|---|---|
| `go test ./...` | **緑** |
| `cd web && pnpm test` | **緑**（213 files / **2480 tests**） |
| **`make e2e`（リポジトリルート・全数）** | **緑**（**250 passed / 4.4m**。★確認ダイアログの spec 3 件を足したあとの実測）。**★`web/` では実行していない**（チェックリスト §5-5） |
| `scripts/check-artifact-integrity.sh`（**1 本目に実行**） | **緑**（検査 14 件の対照が実走・派生資料 4 本とも健全） |
| `check-doc-refs` / `check-browser-storage-keys` / `check-enum-sync` / `check-import-order` / `check-doc-inventory` | **緑** |
| `check-md-emphasis` | **★着手前から NG**（現在 495 行 / ベースライン 436）。**★本サブのファイルの寄与は 0 行**（`--list` で実測）。`M29-01` 完了報告 §8-9 と同じ状態である |

**★既定上限（`DefaultMaxRows` ＝ 1000）そのものを踏むテストは、着手前 1 本も無かった**（既存は `Options{MaxRows: 2}` の注入のみ）。本サブで新設した。

### 9.1b ★実機での確認（**開発者実測・2026-09-07**）

**★本セッションは実 DB を持たないため、ここは製造が測れない箇所である。**

| # | 確認 | 実測値 |
|---|---|---|
| 1 | `GET /api/combos?limit=2` の `count` と `total` | **`count: 2` / `total: 86`**。**★着手前は `count` が `len(items)` しか無く、上限で切れた事実を知る手段が存在しなかった。⇒ 総数の観測が効いていることの直接の確認である** |
| 2 | `GET /api/export/csv?range=all` の観測ヘッダ | **4 本とも出た** —— `X-Export-Total: 86` / `X-Export-Included: 86` / `X-Export-Truncated: false` / `X-Export-Reimport-Blocked: false`。**★切り捨てが無くてもヘッダを出す設計どおり**（「ヘッダが無い」と「切り捨てていない」を呼び出し元が区別できるようにするため） |
| 3 | 実データの往復（書出 → 取込） | **問題なし**（開発者確認）。**⇒ `VAL-C09` / `VAL-C15` を ERROR へ揃えた影響は、手持ちの 86 件に 1 件も出ていない。§4.1 の「取り込める行を 1 件も変えない」が実データで裏づけられた** |

**★コンボ総数は 86 件であり 100 件を超えていなかった**（§4 の照会 1 は「超えている可能性がある」だったが、実測は 86）。**⇒ 一覧の暗黙 100 件は現時点では発火していない。★ただし観測は上記 1 のとおり件数に依らず効いており、超えた時点で鳴る。**

### 9.2 破壊確認（チェックリスト §6・すべて実走して赤を見た）

**★★§6-1 は「5 経路それぞれに観測が付いていることの唯一の証拠」である。⇒ 5 経路すべてで実走した。**

| # | 殺した観測 | 赤くなったもの | 他の経路 |
|---|---|---|---|
| 1 | 書出（CSV/BE）の `Truncated()` を常に false へ | `TestExportResultTruncatedBoundary` ／ `TestExportHeaders_TruncatedIsReported` | **緑のまま**（局在を確認） |
| 2 | 取込・プレビューの `[CODE]` 付与を外す | `TestPreviewFileErrorCarriesValCode` ／ `TestCommitFileErrorCarriesValCode` | **緑のまま** |
| 3 | `Count` を `List` と同じ上限でクランプ | `TestCountIsNotClampedByListLimit` | **緑のまま** |
| 4 | FE 側 `isTruncated` を常に false へ | `export-truncation.test.ts` 3 件 ／ `ExportDialog.test.tsx` 3 件 | **緑のまま** |
| **★★5** | **セットプレイ書出↔取込の非対称の判定を殺す** | **★★1 本も赤くならなかった**（§9.3） | — |
| 6 | `VAL-C09` を WARNING へ戻す | `TestVALC09_EmptyRecipe_ErrorsForDraftAndPublished` | **緑のまま** |
| 7 | `VAL-C15` の検査を殺す | `TestVALC15_RequiredFields_ErrorsForPublishedOnly` | **緑のまま** |
| 8 | `buildPatchPayload` から `isDraft` を外す | `ComboEditor.test.tsx` の新設 3 件 | **緑のまま**（他 65 件は緑） |

**★★§6-2（境界＋1）** は Go / TypeScript の双方で主張している——`isTruncated` ／ `ExportResult.Truncated()` ／ `ExceedsImportRowLimit` ／ `VAL-I03` ／ `Count` のいずれも、**「上限をちょうど 1 件超える」と「ちょうど上限（1 件も落ちていない）」の両方**を置いた。**★「上限の 2 倍」では試していない。**

### 9.2b ★確認ダイアログの E2E（開発者要望・2026-09-07 追加）

**★レビューが「E2E がこの確認を通っていない」と指摘した穴を埋めた**（`web/e2e/m29-02-export-truncation.spec.ts`）。

| # | 内容 |
|---|---|
| **★★1** | **101 件を実際に投入していない。** 確認は「一覧が上限で切れている」ときにだけ出るため素直にやると 101 件必要だが、**それは共有 DB を 100 件で埋め、一覧を見る他ファイルの spec を道連れにする**。**⇒ `page.route` で `GET /api/combos` の応答だけをブラウザコンテキスト単位で差し替えた**（`D-466` / 教訓 `E-142` の指示どおり。**サーバ状態を 1 バイトも書き換えないため復元も要らない**） |
| 2 | **主張は 3 本**——(1) 切り捨てるとき**確認が出て出力が始まらない** ／ (2) **対照**＝切り捨てが無ければ確認を出さずそのまま出力する ／ (3) 「このまま出力する」で続行できる |
| **★3** | **対照 (2) が要る理由**——**これが無いと「常に確認を出す」実装でも (1) は緑になる** |
| **★★4** | **破壊確認を実走した**——`exportTotalCount` を `total` から `count`（着手前の形）へ戻すと**切り捨て側 2 件が赤・対照 1 件は緑のまま**。**⇒ 局在も正しい** |
| 5 | **書出応答も stub している**——一覧を差し替えた以上その id は実在せず、実 API では 400 になるため。**★BE 側の観測は Go テストが担保しており、本 spec の主題は「確認を出すか」である** |

**★`make e2e` は 247 → 250 件になり、全数緑。** 他ファイルへの巻き添えが無いことを実測した。

### 9.3 破壊確認で見つかった無観測（本サブでいちばん価値のある発見）

**★破壊確認 5 が 1 本も赤くならなかった。⇒ セットプレイ書出↔取込の非対称は「観測が付いているつもり」で無観測だった。**

| # | 何が起きていたか |
|---|---|
| 1 | `ReimportBlocked()` の単体テストは在った（構造体のフィールドを手で立てて呼ぶもの）。応答ヘッダのテストも在った（同じく手で値を入れるもの）。**⇒ しかし「実際に計算しているか」は 1 度も観測されていなかった** |
| 2 | 判定が `len(dtoSetups) > csvcore.DefaultMaxRows` というインラインの比較だったため、**境界で正しいかを主張するテストが書けなかった** |
| 3 | **⇒ `ExceedsImportRowLimit` / `ExceedsImportByteLimit` へ抽出し、境界＋1 とちょうど上限を主張した**（コミット `9b59a7d`）。抽出後に同じ破壊確認をやり直し、**赤くなることを確認した** |

**★同じ形の無観測がもう 1 件在った**——**FE 生成書出の「配線」**。`ExportDialog` のテストは `runExport` をモックしているため、`run-export.ts` の中で `onTruncated` を呼び忘れても緑のまま通っていた。**⇒ `run-export.test.ts` へ観測を足した。**

> **★★一般形：「判定関数の単体テスト」と「呼び出し側のモックテスト」が両方在っても、その 2 つを繋ぐ配線は無観測でありうる。** 破壊確認 §6-1 は、まさにそこを撃つためのものである。

---

## 10. レビュー結果

**レビュー報告書**: `docs/progress/m29-02-review.md`（Phase B・fresh subagent。**メイン会話の文脈を継承しない独立エージェント**で実施。`fork` は使っていない）

### 10.1 件数と優先度別内訳

| 優先度 | 件数 | 採用 | 不採用 |
|---|---:|---:|---:|
| **高** | **3** | **3** | **0** |
| 中 | 6 | 6（うち 1 件は記録で対応） | 0 |
| 低 | 6 | 1 | 5（将来対応） |
| **合計** | **15** | **10** | **5** |

**★★「高」指摘の不採用は 0 件である**（実測）。⇒ 開発者へのエスカレーション（Phase C 安全弁）は発生していない。

### 10.2 各指摘の採否と理由

**★正本はレビュー報告書末尾の「取り込み結果（自動トリアージ）」節である**（事後監査のため同じ内容をそちらへも記録した）。要点のみ再掲する。

| # | 指摘 | 採否 | 要点 |
|---|---|---|---|
| **高-1** | `en.json` の `export.maxReached` が `compared` へ書き換わっていた | **採用** | **★本サブが入れた回帰だった。** i18n 編集スクリプトがファイル先頭から `"maxReached"` を検索し、`compare.maxReached` の文字列を `export.maxReached` へ書き込んでいた。着手基点の値へ戻した |
| **高-2** | 重大度の非対称が全数に達していない（`VAL-S02`） | **採用** | **★`VAL-C09` の完全な双子を見落としていた**（§5.5）。ERROR で揃え回帰テストを置いた。`VAL-C12` は揃えない理由を明記した |
| **高-3** | 書出の切り捨て観測の「配線」が無観測 | **採用** | **★破壊確認で再現した。** 継ぎ目テストを追加し、追加後に同じ破壊確認をやり直して赤を見た |
| 中-1〜中-6 | 確認のリセット漏れ ／ 実 `ja.json` 不使用 ／ `setup_file` の鳴らし方の選定者 ／ 100 件の診断ずれ ／ `VAL-C10` の旧語 ／ 後方互換の主張欠落 | **6 件とも採用** | **★中-4 により §1.2 / §4.2 / §8.2 / §12 の記述を実態へ訂正した**（初版の診断が不正確だった） |
| 低-1〜低-6 | 型と `??` の食い違い ほか | 1 件採用 / 5 件将来対応 | **★低-5（`COUNT(*)` のコスト実測）は実データを持たない本セッションでは測れない。⇒ 測らずに「問題ない」と書かないため不採用とした** |

### 10.3 再レビューの往復

**0 回**（初回レビューのみ）。**★停止規律の上限（往復 2 回）に達していない。** 未解消のまま停止した項目は無く、`followup-backlog.md` §J への記録は発生していない。

### 10.4 ★レビューが本サブから引き出した最大の価値

**★★「全数を数えたつもり」が 2 件とも崩されたことである。**

| # | 内容 |
|---|---|
| 1 | **§5.2 の「非対称の全数」は 4 件で閉じていたが、`VAL-S02` が漏れていた**（高-2）。**★見落とした理由は数え方である**——「気づいた非対称を 1 件ずつ潰す」やり方では、気づかなかったものは最後まで現れない |
| 2 | **§9.2 で「5 経路すべてに観測を付けた」と書いた直後に、その中心機能の配線が無観測だった**（高-3）。**★製造自身が §9.3 で一般形として書いた型に、製造自身が掛かっていた** |

**⇒ 一般形＝「全数を数えた」という主張は、数え方を書いて初めて検証可能になる。** 数え方を書かなければ、レビューも「漏れているかどうか」を判定できない。

## 11. `docs/design/` に反映が要る箇所の一覧（**製造は直さない**）

**★CHANGE 起票は設計卓の手番**（`D-293`。自採番していない）。

| # | ファイル:行 | 内容 |
|---|---|---|
| **★★1** | `docs/design/06-validation.md` §6（L374-380） | **★★「severity は `WARNING` のまま据え置いた ⇒ 取込可否は変わらない」が失効した。** 本サブが `VAL-C09` を ERROR へ揃えたためである。**★同段落の「ただし非対称は残る」「揃えるべきかは設計判断である（followup `csv-preview-commit-severity-asymmetry`）」も同時に失効する。★これは「撤回済み・失効した記述がコード上に残っている」型であり、放置すると次の担当が旧の前提を複製する** |
| **★★2** | `docs/design/06-validation.md` §2.1（`VAL-C15` の行）／ §6 | **`VAL-C15` を CSV 取込プレビューでも検査するようになった。** 着手前は「本登録の必須 4 欄」が本体保存経路にしか無かった |
| **★★3** | `docs/design/06-validation.md` §6（`VAL-S06`） | **`VAL-S06` をプレビューでも ERROR にした**（着手前は WARNING） |
| **★★4** | `docs/design/02-architecture.md` §4.2 | **`GET /api/export/csv` の応答ヘッダ 4 本を新設**（`X-Export-Total` / `X-Export-Included` / `X-Export-Truncated` / `X-Export-Reimport-Blocked`）。**★zip 本体に警告欄を混ぜられないための契約である** |
| **★★5** | `docs/design/02-architecture.md` §4.2 | **`GET /api/combos` の応答へ `total` を追加**（絞り込みに一致する総数）。**★既存の `count` は `len(items)` のままであり意味を変えていない** |
| **★6** | `docs/design/05-screen-design.md` §5.13a（エクスポートダイアログ） | **上限で切り捨てるときの確認を出す**（鳴らし方 (c)）。開発者選択 |
| **★7** | `docs/design/02-architecture.md` §7.6 | **★★後方互換の記述と `VAL-C15` の緊張関係。** 同節は「旧 CSV（消費列なし）を後方互換で受理する」と定めるが、**`M27-02b` 以降、消費ゲージが空の本登録行は確定で必ず落ちる**（本サブの実測で確認）。**⇒ 列の不在は受理するが、値の不在は本登録では受理しない。★本サブが作った状態ではなく、`M27-02b` 以降そうだった。⇒ 明記が要る** |
| **★8** | `docs/design/06-validation.md` §6 の VAL 一覧 | **ファイルレベルのエラーが英語のまま出ていた件**（`[CODE]` 接頭辞が落ちていたため訳に当たらなかった）。**★同節は「file レベルエラーは現状 英語のまま〔将来 BE 小改修で日本語化＝保留〕」と書いている。⇒ 本サブで解消したので失効する** |
| **★9** | `docs/design/05-screen-design.md` / `docs/design/06-validation.md` | **`knockdown_advantage` の呼称。** 取込画面の列ラベルとメッセージ（`VAL-C10`）が `ノックダウン` だったのを本体側と同じ `有利フレーム` へ揃えた |
| **★10** | `docs/design/06-validation.md` §6 | **★`VAL-C12` はプレビュー層で揃えていない**（§5.5）。**⇒ 揃えるには `csvcore` の `Lookup` へ技マスタの関係（ラッシュ版と元技）を引く責務を足すことになり、同層が「CSV の形だけを見る層」であることを崩す。★設計判断であり製造が単独で決めない** |
| **★11** | `docs/design/05-screen-design.md` §5.14（取込画面） | **★`setup_file` が上限を超えたら取込を止める**（§8.5）。**★★2026-09-07 開発者確定済み**（逐語＝「10MiB を超えたら取込停止でいい」）。**⇒ 確認は済んでおり、設計書への反映だけが残っている** |

---

## 12. ■ 併せて更新が要るもの

| # | 項目 | 状態 |
|---|---|---|
| 1 | **消費した CHANGE 番号の登録** | **なし。** 本サブは CHANGE を 1 本も起票していない（起票は設計卓 ＝ `D-293`）。⇒ `change-number-registry.md` §1 への登録は不要 |
| 2 | **「次の番号」の写し先 4 か所** | **なし**（上と同じ理由。番号を消費していない） |
| 3 | **消費したマイグレ連番** | **なし。** マイグレーション 0 本。`ls migrations/` の実査値（最新 `000105`）とボード §2.2 はずれていない |
| 4 | **版を上げた文書の参照元** | **なし。** 設計書・指示書の版を 1 本も上げていない（`docs/design/` は編集せず §11 に一覧化した） |
| 5 | **`web/CLAUDE.md` §1 のブラウザストレージ台帳** | **なし。** 新規キーを使っていない（`check-browser-storage-keys.sh` 緑） |
| 6 | **`docs/design/testid-convention.md` の「付与済み一覧」** | **★対応済み**（2026-09-07）。本サブが新設した testid **1 本**＝`export-truncation-confirm` を登録した |
| 7 | **★横断課題（`followup` の状態更新候補）** | **`csv-export-row-limit-silent-truncation`（解消）** ／ **`csv-preview-commit-severity-asymmetry`（解消）** ／ **`backup-restore-buttons-disabled`（据え置き・開発者の手番。本サブでは触っていない）**。**★`followup-backlog.md` は §J 以外を製造が編集できない**（`D-382`）**ため、本欄を設計卓への申し送りとする** |
| 8 | **★横断課題（新規に判明・本サブで解消済み）** | **(a) 一覧が BE 既定 100 件で切れたまま書出の対象になり、件数表示も `count` だったため気づけない**（**★「解消」ではなく「書出の入口に観測を付けた」である。⇒ 一覧画面そのものは依然 100 件で黙って切れており、`total` は取得できるようになったが画面には出していない。★上限の値を変えないという射程の帰結であり、一覧のページングは別の判断である**） ／ **(b) 編集モードの仮登録トグルが PATCH で送られない** ／ **(c) 書出↔取込の上限の非対称**（セットプレイ行数・バイト数）／ **(d) `setup_file` の上限超過の握り潰し** ／ **(e) `combo_file` の上限超過の誤ラベル**。**★いずれも未起票だった。⇒ 「解消済みとして」起票が要る** |
| 9 | **★横断課題（`M29-01` から引き継いだもの・解消）** | **`VAL-C09` の重大度の非対称**（`M29-01` 完了報告 §8-6 が `M29-02` へ送ったもの）。**⇒ 本サブで ERROR へ揃えて解消した** |
| 10 | **★横断課題（着手前から NG・本サブの寄与 0）** | **`check-md-emphasis.sh` が着手前から NG**（現在 495 行 / ベースライン 436）。**★本サブのファイルの寄与は 0 行**であり、差 59 行は既存ファイル由来である（`M29-01` 完了報告 §8-9 と同じ状態） |
| 11 | **★横断課題（実装の作法として残すもの）** | **★★「判定関数の単体テスト」と「呼び出し側のモックテスト」が両方在っても、その 2 つを繋ぐ配線は無観測でありうる**（§9.3）。本サブでは 2 件見つかった。**⇒ 破壊確認 §6-1 を経路ごとに実走する運用は、この型を撃つ唯一の手段である** |
| 12 | **★横断課題（設計の緊張・開発者判断が要る）** | **`DES-002` §7.6 の後方互換の約束と `VAL-C15` の関係**（§11-7）。**★本サブが作った状態ではないが、明記が無いままだと「旧 CSV は取り込める」と読んだ担当が実際には取り込めない行を作りうる** |
