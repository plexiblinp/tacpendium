# M26-01 レビュー報告書

| 項目 | 内容 |
|------|------|
| 対象 | `docs/progress/M26-01-report.md`（`M26-01` 依存ライセンスの棚卸しと三層のパス割当） |
| 観点の正本 | `docs/instructions/M26-01-license-inventory-and-three-layer.md` v1.0.0 |
| チェックリスト | **存在しない**（`docs/instructions/reviews/M26-01-review-checklist.md` は無い）。指示書 §2〜§5 を観点として用いた |
| 実施日 | 2026-09-02 |
| 対象 commit | `6a28af5`（着手基点 `e1cd553cbb0940e1bc20f31c69ae9e7283a2a130`） |
| 実施形態 | **コード・文書とも一切変更していない。** 本ファイルの作成のみ。Git は読み取り系のみ使用 |

---

## 総評

**本報告が主張する数字は、母数・表の行数とも 1 件残らず再実測して一致した。** トップレベル 27 件・`migrations/*.up.sql` 80 本の 2 表は、実ファイル一覧との `diff` で欠落 0・余剰 0。ライセンス列 52 件（Go direct 7 ＋ フロント direct 45）は `go list -m -json` ／ モジュールキャッシュの `LICENSE` 実文 ／ `pnpm licenses list --json` と突合して**食い違い 0 件**、`pnpm audit` の 11 件も advisory ID 単位で完全一致した。`SUPP-001` §5.7 の逐語引用 3 ブロックも原文と一致する。指示書 §4「やらないこと」8 項はすべて遵守されている（差分は本報告 1 ファイル・**864 挿入 / 0 削除**）。

**一方で、下流（`CHANGE-151` の起票と `LICENSE` の配置）へそのまま複製される欠落を 3 件見つけた。** (1) §2.2.2「1 ファイル内で層が割れるもの（2 本）」に **3 本目 `000029_clear_ryu_legacy_seed.up.sql` が抜けている**——同ファイルは `000017` より**多い 7 つのアプリ側テーブル**を明示 DELETE している。(2) §3.4 の `CC BY 4.0` → GPLv3 一方向互換の「公表」は、CC が実際に宣言しているのは `CC BY-SA 4.0` → GPLv3 であり、**未検証かつ内容が不正確な外部事実**を根拠に置いている。(3) §2.2 / §2.2.2 の `000017` 根拠欄「FK 連鎖」は、当該ファイルのヘッダが逐語で「連鎖削除されない」と書いている内容と**逆**である。

法務のヘッジ規律は徹底されている（「問題ない」型の記述 0 件／「解される」31・「可能性がある」7・「確かめられなかった」12）。「確かめられなかった」2 件（Go の脆弱性列・Dependabot alerts）の理由も妥当で、安易な諦めではない——`/root/.ccr/README.md` が「organization policy denials（403/407）は再試行せず報告せよ」と明示しており、`mcp__*` と `gh api` は `.claude/settings.json` で deny されている（実測確認済み）。

---

## 設計準拠性レビュー結果

### 開発者指定の重点 4 観点

| 観点 | 評価 | 内容 |
|---|---|---|
| **(a) すべての件数に母数が付いているか／母数と実測が一致するか**（§3.1-1） | **○** | **母数の値は全件一致した**（下表）。ただし `scripts/` の母数の**性質**の記述が誤っており（指摘 6）、`SUPP-001` §5.7 の 2 年ルールの照合が Go 側 7 件に及んでいない（指摘 7） |
| **(b) 層割当の全数性**（§5-3 / §5-4） | **○** | トップレベル 27 行・`.up.sql` 80 行とも `diff` で完全一致・欠落 0。**ただし「層が割れる」ファイルの数え落としが 1 件**（指摘 2） |
| **(c) 法務の断定の混入**（§3.1-6 / §4-7） | **△** | 「これで問題ない」型は 0 件で規律は良い。**ただし §3.4 に未検証かつ不正確な外部事実の断定が 1 件**（指摘 3） |
| **(d) 「確かめられなかった」を推測で埋めていないか**（§3.1-4） | **○** | 2 件（§1.3 Go 脆弱性・§1.8 Dependabot）とも理由が妥当で、環境側の実測でも裏づけられた。**唯一の例外が指摘 3** |

#### 母数の再実測（本レビューが自分で数え直した結果）

| 対象 | report の主張 | 本レビューの実測 | 判定 |
|---|---|---|---|
| トップレベル（`.git` のみ除外） | 27 件 | **27 件** | 一致 |
| `migrations/*.up.sql` | 80 本 | **80 本** | 一致 |
| うち名に `seed` を持つもの | 35 本 | **35 本** | 一致 |
| `go.mod` direct 依存 | 7 件 | **7 件** | 一致 |
| `go.mod` `// indirect` | 15 件 | **15 件** | 一致 |
| `web/package.json` `dependencies` | 31 件 | **31 件** | 一致 |
| `web/package.json` `devDependencies` | 14 件 | **14 件** | 一致 |
| `character_data/*.csv` | 31 件 | **31 件** | 一致 |
| `character_data/` の非 CSV | 3 件（md） | **3 件**（`chain-cancel-measurements.md` / `command-correction-history.md` / `seed-progress.md`） | 一致 |
| フロント推移込み全パッケージ | 315 件 | **315 件**（`pnpm licenses list --json` の集計） | 一致 |
| ツリー全体のライセンス種別 | 9 種 | **9 種**（`(MIT AND Zlib)` `0BSD` `Apache-2.0` `BSD-2-Clause` `BSD-3-Clause` `CC-BY-4.0` `ISC` `MIT` `MIT-0`） | 一致 |
| `internal/` の Go パッケージ | 13 件 | **13 件** | 一致 |
| `docs/` 直下のディレクトリ | 10 件 | **10 件** | 一致 |
| `docs/seed-data/` | 7 ファイル | **7 ファイル** | 一致 |
| `internal/seedgen/` | 15 ファイル | **15 ファイル** | 一致 |
| 画像・ブランド資産 | 0 件 | **0 件** | 一致 |
| マイグレの最大連番 | `000080` | **`000080`**（`.up.sql` 80 ＋ `.down.sql` 80 ＝ 160、他ファイル 0） | 一致 |

#### 表の 1:1 突合（欠落検出）

| 表 | 実測 | 結果 |
|---|---|---|
| **§2.1 トップレベル表** | 表の行から抽出したパス名（27 件）を `ls -A \| grep -v '^\.git$' \| sort` と `diff` | **完全一致（欠落 0・余剰 0）** |
| **§2.1 層の内訳** | 表の層欄を数え直し | A **18** ／ 未割当 **7** ／ 層が割れる **2** ＝ **27**。report の申告と一致 |
| **§2.2 マイグレ表** | 表の行から抽出したファイル名（80 件）を `ls migrations/*.up.sql` と `diff` | **完全一致（欠落 0・余剰 0）** |
| **§2.2 層の内訳** | 表の層欄を数え直し | A **28** ／ B **52** ＝ **80**。report の申告と一致 |
| **§5 の「25 本を取り違えていた」** | seed 名 × 層 A ＝ **4 本**（`000002` / `000005` / `000007` / `000012`）／ 非 seed 名 × 層 B ＝ **21 本**。合計 **25** | report と一致 |
| **§1.5 / §1.6 依存表** | 45 行のパッケージ名・版指定を `web/package.json` と `diff` | **完全一致**（名前・版指定とも） |

### 指示書の各要求への充足

| 指示書 | 評価 | 所見 |
|---|---|---|
| **§2.1-1** Go direct 全数と一覧・母数 | **◎** | 7 件・母数明記。モジュール名と版が `go.mod` と完全一致 |
| **§2.1-2** npm の 2 種を分けて数える | **◎** | 31 / 14 に分離。両表とも `package.json` と完全一致 |
| **§2.1-3** 3 列（SPDX / 最終更新 / 脆弱性）を全行埋める | **◎** | **52 行すべて埋まっており、空欄 0。**（a）は Go 7 件をモジュールキャッシュの `LICENSE` 実文で、フロント 45 件を `pnpm licenses list --json` で本レビューが再検証し、**食い違い 0 件**。（b）も Go 7 件を `go list -m -json` で、npm はサンプル 7 件を `pnpm view time.modified` で再取得して一致 |
| **§2.1-4** `// indirect` の全数 | **◎** | 15 件 |
| **§2.1 走査手段の固定**（`D-659`） | **◎** | `go-licenses` の失敗を実出力付きで記録し、**指示書が定めたフォールバック**（`go mod download` ＋ キャッシュの `LICENSE` 読取）へ落ちている。新ツール導入 0 |
| **§2.2-1** トップレベル全件を層へ | **◎** | 27 件・欠落 0（上表） |
| **§2.2-2** `migrations/` を 1 本ずつ判定・「seed 名＝層 B」を採らない | **○** | 80 本を中身で判定し 28 / 52 に分割。**割れているファイルの数え落とし 1 件**（指摘 2）。逆方向（層 A 側にゲームデータが混ざる）は本レビューで機械走査し、`000018`（`UPDATE moves` ＝ スキーマ整理）と `000074`（`UPDATE preset_aliases` ＝ 構造 backfill）の 2 件のみで、いずれも層 A 判定が妥当 |
| **§2.2-3** `character_data/` の層 | **◎** | 34 件を 33（B）/ 1（A）へ分離。`seed-progress.md` の同居を正しく検出。CSV ヘッダ列の引用も原文と一致 |
| **§2.2-4** `seedgen` の境界 | **◎** | 「生成コード＝層 A／入力と出力＝層 B」を明示 |
| **§2.2-5** 層 C の実体・0 件なら 0 件 | **◎** | 「ソースコードとしては 0 件」と書き、**どう探したか**（母集団 13 件 → 5 件の godoc 確認）も記載。「開発運用資産は当てはまりうる」という留保も適切 |
| **§2.2-6** 層 D の実体 | **◎** | `find` の出力 0 行を根拠に 0 件。`web/index.html` に favicon 参照が無いことも実測どおり |
| **§2.2-7** `docs/` の層 | **◎** | 判断材料 5 点 ＋ 3 案（長短付き）。**案を選ばず開発者へ回している**（指示書 §7 に忠実） |
| **§2.2-8** AI 運用ルール群の層 | **◎** | 「公開可否」と「層の割当」を分離して論じている |
| **§2.2-9** `.devcontainer` の層 | **◎** | 層 A ＋「公開しないことと層は別」を明記 |
| **§2.2-10** 利用者生成データ | **◎** | ファイル表に現れないことを明示。`.gitignore` 実測も一致。`000012` の合成コンボとの混同注意まで書いている |
| **§2.3-1〜3** AGPL 互換性判定 | **○** | 52 件を 1 件ずつ／種別ごとに判定。判定の向きも取り違えていない。**`CC-BY-4.0` の根拠が不正確**（指摘 3） |
| **§2.3-4** 判定できないものは「判定できなかった」 | **◎** | §3.6 に 3 件 |
| **§2.4-1** §5.7 が inbound であることを**文面で**確認 | **◎** | **本レビューの評価が最も高い節。** 「文面からは『文脈上そう読める』までしか言えない」「同節は inbound という語も限定句も一度も書いていない」と、**指示書が期待した答えより弱い結論を正直に出している**。逐語引用は原文と完全一致（後述） |
| **§2.4-2** 違反するなら逐語で示す | **◎** | 「逐語で示すべき違反箇所は無い」＋ 反対の読み方の余地も併記 |
| **§2.4-3** 事実だけ書きポリシーは緩めない | **◎** | 「広がる」事実のみ。緩和提案なし |
| **§2.4-4** 撤回か明確化か | **◎** | 「明確化」と判定し、**加えて `DES-001` §5/§6 は実質的変更が要る**ことを分離して指摘。原文と照合して正確 |
| **§3.1-2**「無い」ときはどう探したか | **◎** | §2.7 / §2.8 / §1.8 いずれも探索手順を記載 |
| **§3.1-3** 設計卓の 3 数字を数え直す | **◎** | 3 つとも数え直し「合っていた」と明記。加えて「数が合うことと判定が正しいことは別」と踏み込んでいる |
| **§3.1-5** 実行コマンドと出力を貼る | **○** | §0.2 に走査コマンド全文。**ただし §3.4 の外部事実の主張だけコマンドが無い**（指摘 3）。§9 の `git diff --stat` が実測とずれる（指摘 5） |
| **§3.2** 成果物 2 本 | **◎** | §1（依存棚卸し）／§2（三層パス割当）とも report 内に在る |
| **§3.3** `CHANGE-151` を起票しない | **◎** | 起票していない。`change-number-registry` 未編集（実測: 差分 1 ファイルのみ） |
| **§4-1** コードの変更 | **◎** | `git diff --stat e1cd553…` ＝ **1 file changed, 864 insertions(+), 0 deletions**。`migrations/` `internal/` `web/` `cmd/` `character_data/` `docs/design/` 未変更。`git status` はクリーン（untracked 0） |
| **§4-2 / §4-3** `LICENSE` / SPDX ヘッダ | **◎** | 作成・挿入とも 0（差分から確認） |
| **§4-4** `docs/design/` 編集 | **◎** | 未編集 |
| **§4-5** `followup-backlog.md` 編集 | **◎** | 未編集。§7 に候補 4 件を挙げるにとどめている（`D-382` に忠実） |
| **§4-6** 新ツール導入 | **◎** | 0。`pnpm view` の使用理由も明記されており、`pnpm` は `CLAUDE.md` §11 の正規パッケージマネージャで妥当 |
| **§4-7** 法的断定 | **○** | 「問題ない」型 0 件。**§3.4 の 1 か所のみ問題**（指摘 3） |
| **§4-8** `.claude/commands/` 編集 | **◎** | 未編集 |
| **§5-6** 実装差分が空 | **◎** | 実測どおり。**新規ファイルに deletions 0**（`E-225` の観点でも問題なし） |
| **§5-7** `progress-log.md` への追記 | **△** | **現時点で未追記**（`grep -in 'M26' docs/progress/progress-log.md` → 0 件）。ただし `implement_plan_full` の Phase 順は **A → B → C → D** で、索引行（Phase D）はレビュー（Phase C）の**後**に実施するのが正しい運用であるため、**規律違反ではない**。指摘 1 は「取り込み時に必ず実施すること」という確認事項として挙げる |
| **§5-8** すべての件数に母数 | **○** | 上記 (a) のとおり |

### `SUPP-001` §5.7 逐語引用の照合（開発者指定・「引用の改変は高」）

**3 ブロックとも原文と完全一致した。改変・省略・要約は無い。**

| report §4.1 の引用 | 原文の位置 | 判定 |
|---|---|---|
| 節題 `### 5.7 依存ライブラリ追加ポリシー` | `docs/design/supp-001-detailed-design.md:1111` | **一致** |
| 禁止リスト `GPL-2.0 / GPL-3.0 / LGPL-2.1 / LGPL-3.0 / AGPL-3.0 / CC BY-SA / 独自プロプライエタリライセンス / デュアルライセンス(GPL or 商用)` | `:1124` | **一致**（report が示す行番号も正しい） |
| 追加ルール 5 項 | `:1128`〜`:1132` | **5 項すべて一致**（読点・括弧内表記まで） |
| §4.2 根拠 3 の `MPL-2.0` 備考「原則不使用、代替がない場合のみ開発者確認の上採用」 | `:1116` の表 | **一致**（`CLAUDE.md` §6 側の「上で採用」ではなく `SUPP-001` 側の表記を正しく引いている） |
| §4.5 の `DES-001` §5「本プロジェクト自体のライセンスは **MIT License** とする（確定。2026-06-28 開発者確定）」 | `docs/design/01-tech-stack.md` §5 | **一致**。§6 に対応する 2 行（「ライセンス確定」「文書整合是正」）が在ることも実測どおり |

### §8 想定外の発見の事実性

| 節 | 判定 | 実測 |
|---|---|---|
| **§8-1** checklist `A1` の「AGPL §13 の解釈整理を骨子に 1 節足すこと」が骨子に無い | **正しい** | checklist の逐語は原文（`:32`）と一致。骨子への `§13` grep は **0 件**（本レビューでも再現） |
| **§8-2** 骨子 §8 と `M26-overview` §3 の食い違い | **正しい** | 骨子 `:146`（「撤廃し」）と overview `:77`/`:79`（「撤回ではなく明確化」「据え置く」）の逐語とも一致。「設計書間の矛盾には当たらない」という切り分けも `CLAUDE.md` §8 に照らして妥当 |
| **§8-3** `000072` が「層 A / 層 B / 層 C」を別の意味で使っている | **正しい** | `migrations/000072_m20_seed_aliases_numeric.up.sql:7`「層順の原理: command が表記を決める行は層 A、move_code の構造が決める行は層 B。」／ `:12`「`-- ===== 層 C-1: 移動系 9 code x 19 キャラ =====`」いずれも逐語一致。**下流の SPDX 配置サブで実害が出る指摘であり、価値が高い** |
| **§8-4** `go-licenses` が本サブの目的で失敗する循環 | **正しい** | 失敗の 2 要因の説明は出力と整合 |
| **§8-5** 配布禁止 HTML はリポジトリに無い | **概ね正しい** | `work_html/` は `.gitignore:98` で除外・`000010` ヘッダの「work_html から抽出」も一致。**html の列挙が 2 本漏れ**（指摘 12） |
| **§8-6** `README.txt` は配布物そのもの | **正しい** | `README.txt` 冒頭「配布版 README」・123 行、`README.md` 199 行とも一致 |

---

## 設計準拠性以外の指摘事項

### 1. 「確かめられなかった」2 件の理由の妥当性（開発者指定の確認項目）

**両件とも妥当であり、安易な諦めではない。本レビューが独立に検証した。**

- **Go の脆弱性列（§1.3）**: `govulncheck ./...` を実行して `Forbidden` を再現した。プロキシ側の記録（`$HTTPS_PROXY/__agentproxy/status` の `recentRelayFailures`）にも `vuln.go.dev:443` への `connect_rejected` が残っており、**`/root/.ccr/README.md` は「organization policy denials（403/407）は再試行せず報告せよ」と明示している**。指示書 §2.1 も「回避策を独自に作らない」と定めている。⇒ **report の対応が唯一の正解。** リポジトリ内の代替経路（`scripts/app-build-supplychain-check.sh`）も内部で `govulncheck` を呼ぶため同じ壁に当たる。
- **`govulncheck -version` の偽陽性の対照実験（§1.3）**: 本レビューでも再現した（403 環境で `No vulnerabilities found.` を返す）。**この落とし穴を明示的に潰した記述は、次に同じ環境で作業する担当を確実に救う。**
- **Dependabot alerts（§1.8）**: `mcp__*` と `Bash(gh api:*)` が `.claude/settings.json` の `deny` に在ることを実測確認。`dependabot.y*ml` は 0 件、`.github/` は workflow 2 本のみ。**「設定ファイルの有無」と「alerts の有効・無効」を混同していない**点も正確。

### 2. 層割当そのものの妥当性（開発者指定の確認項目）

**80 本のうち疑わしいものを機械走査 ＋ 実ファイル読解で検証した。**

- **層 B 判定のうちアプリ側テーブルへ書き込むもの**（`combos` / `combo_steps` / `combo_tags` / `combo_setups` / `combo_oki_options` / `setups` / `setup_steps`）を全数走査 → **`000017` と `000029` の 2 本**。report は `000017` のみを「割れている」として挙げている（**指摘 2**）。
- **層 A 判定のうちゲームデータ表へ書き込むもの**（`moves` / `characters` / `preset_aliases` / `custom_states` / `move_commands` / `move_derivations`）を全数走査 → `000018`（`UPDATE moves` ＝ 観測不能列の整理）と `000074`（`UPDATE preset_aliases` ＝ `character_id` の構造 backfill）の 2 本のみ。**いずれも構造変更であり層 A で正しい。**
- **指示書 §2.2-2 が名指しした 4 本**を実際に開いて確認 → `000002`（`games` 1 行・SF6 のタイトル名のみ・全 5 行）／`000005`（表記プリセット 5 種）／`000007`（既定ユーザー ＋ 予約カテゴリのタグ 3 件、ヘッダに「本来は M6 初回起動ウィザードで生成」）／`000012`（ヘッダに「SF6 的正確性は不問」・合成コンボ 36 件）。**4 本とも層 A で正しい。**
- **`000039`** を実読 → `ALTER TABLE` 1 文 ＋ 8 キャラ分（`terry` / `guile` / `ingrid` / `kimberly` / `juri` / `ken` / `mai` / `ryu`）の飛び道具判別。**「8 キャラ分」は正しい。**「1 行目が ALTER TABLE」は不正確（実際は 13 行目）＝ **指摘 9**。
- **`000022` / `000028` / `000021` / `000069` / `000070` / `000074` / `000079` / `000080`** のヘッダを実読 → **すべて report の根拠欄と整合。**

### 3. コーディング規約・機械検査

**新規コードは無いため規約観点の指摘は無い。** 常設の機械検査を全数回した結果は次のとおり。

| 検査 | 結果 |
|---|---|
| `scripts/check-artifact-integrity.sh` | 違反なし（自己検査 12 件 OK・生成物 4 件 OK） |
| `scripts/check-md-emphasis.sh` | 違反なし（436 行 ＝ ベースラインどおり・**増加 0**）。commit `6a28af5` で「閉じない強調 4 行を是正」しており、実際に増やしていない |
| `scripts/check-doc-refs.sh` | dead reference なし |
| `scripts/check-stop-discipline.sh` | 違反なし（§J の必須 5 フィールド OK） |
| `scripts/check-progress-log-index.sh` | 違反なし。**★ただし本検査は `*completion-report*.md` しか見ない**ため、`M26-01-report.md` の索引行の有無は**原理的に検出できない**（指摘 1 の前提） |
| `check-doc-inventory.sh` の型 | `docs/progress :: ^M[0-9]+-[0-9]+-report\.md$`（report §「併せて更新が要るもの」の主張どおり）。本レビュー報告も `^[Mm][0-9]+-[0-9a-z-]+-review(-[0-9]+)?\.md$` に合致 |

### 4. セキュリティ

- `pnpm audit` の 11 件を本レビューで再実行し、**深刻度内訳（high 6 / moderate 4 / low 1）・パッケージ・脆弱な版・修正版・GHSA ID がすべて report の表と一致**した。`react-router-dom` の `Patched versions: <0.0.0` も実測どおりで、**v6 系に修正版が無い**という読みは正しい。§6-5 として要判断へ回した処理も妥当（依存更新は指示書 §4-1 に当たる）。
- 「direct か」列（`postcss` = direct/dev、`react-router-dom` = direct/prod、他は推移）も `package.json` と整合。

---

## 推奨修正（優先度別）

### 高（M26 完了前に修正必須）

- **[高-1] §2.2.2 の「1 ファイル内で層が割れているもの（2 本）」に 3 本目が抜けている。`000029_clear_ryu_legacy_seed.up.sql` を追加すること。**
  - 実測: 層 B と判定されたファイルのうちアプリ側テーブルへ書き込むのは `000017` と `000029` の 2 本。**`000029` は `combos` / `combo_tags` / `combo_oki_options` / `combo_setups` / `combo_steps` / `setups` / `setup_steps` の 7 表を明示 DELETE**しており、`000017`（4 表）より**触る範囲が広い**。
  - 同ファイルのヘッダも逐語で「(1) ryu の combos+従属行(combo_tags/combo_oki_options/combo_setups/combo_steps)を全削除」「(2) ryu の setups+setup_steps を全削除」と、層 A 側の表への操作を主目的の一部として宣言している。
  - **影響**: §2.2.2 の件数（2 → 3）／ §3.5 の「残る問題は 1 ファイル内で層が割れる 2 本だけ」／ §6-4 の要判断／ §7 の followup 候補 `spdx-header-cannot-split-within-file` の 4 か所へ複製される。**開発者が承認する表そのものの欠落**である。
  - 層の第一候補（B）自体は変えなくてよい。**「割れている」一覧に載っていないことが問題。**

- **[高-2] §3.4 の `CC BY 4.0` に関する外部事実の断定を撤回し、「確かめられなかった」へ書き換えること。**
  - 該当（`M26-01-report.md:590`）: 「Creative Commons は `CC BY 4.0` から `GPLv3` 系への一方向互換を**公表しており**」
  - **問題 1（内容）**: Creative Commons が一方向互換を宣言している（ShareAlike Compatible Licenses）のは **`CC BY-SA 4.0` → GPLv3** である。`CC BY 4.0` は ShareAlike 条項を持たないため互換宣言の対象ではない。**`BY` と `BY-SA` の取り違えと解される。**
  - **問題 2（手続き）**: この主張には走査コマンドも出力も無い（§0.2「走査コマンド全文」にも該当行が無い）。本環境から一次情報へは到達できない。**§3.1-4「確かめられなかったものは『確かめられなかった』と書く。推測で埋めない」と §3.1-5「実行したコマンドと出力を貼る」の双方に反する。**
  - **問題 3（射程）**: `CC-BY-4.0` は本報告が唯一「判定に注意が要る」とマークしたライセンスであり、**その 1 件の結論を支える唯一の根拠**が未検証である。
  - **推奨する書き換えの形**: (i) `caniuse-lite` が `CC-BY-4.0` の推移的 devDependency であるという**実測できる事実**は残す。(ii) 互換性の結論は「**確かめられなかった。一次情報（Creative Commons の互換ライセンス宣言）へ本環境から到達できないため**」とし、§6 の要判断へ移す。(iii) 「配布バイナリに同梱されるものではないと解される」という射程の議論は事実に基づくので残してよい。

- **[高-3] §2.2 行 17 および §2.2.2 の `000017` 根拠欄「FK 連鎖」は事実と逆。訂正すること。**
  - report の記述: 「FK 連鎖で `combos` / `combo_steps` / `combo_tags` / `combo_setups`（層 A 側の表）にも触れる」
  - 原文（`migrations/000017_cleanup_ajg_seed_and_unify_ryu_move_code.up.sql` ヘッダ・逐語）: 「マイグレーション接続は foreign_keys=OFF(migrate.go が pragma 無しで接続。アプリ接続のみ db.go で FK=ON)。**よって combos を DELETE しても子テーブル(combo_steps 等の ON DELETE CASCADE)は連鎖削除されない。** orphan を残さないため、**子行を先に明示 DELETE する。**」
  - **⇒ 連鎖しないからこそ明示 DELETE している。「FK 連鎖で触れる」は因果が逆。** 層 B という第一候補は変わらないが、**なぜ割れているのかの説明が誤っている**ため、この行を読んで SPDX を配置する後任が誤解する。正しくは「FK=OFF のため CASCADE に頼れず、層 A 側の表を明示 DELETE している」。

### 中（M27 着手と並行可）

- **[中-4] §9 の `git diff --stat` の貼付が実測と一致しない（858 vs 864）。**
  - 実測: `git diff --stat e1cd553cbb0940e1bc20f31c69ae9e7283a2a130` → `docs/progress/M26-01-report.md | 864 +++…` / `1 file changed, 864 insertions(+)`。report §9 は `858` と貼っている。
  - §9 は「実装への差分が空であることの**立証**」節であり、貼った出力が現物と一致しないと立証として機能しない（`E-225` の観点そのもの）。**deletions が 0 である点・変更ファイルが 1 本である点は実測でも正しい**ため、結論は揺らがない。**最終の実行結果へ貼り替えること。**

- **[中-5] §2.1 行 26 の「30 本のシェルスクリプト」は不正確。**
  - 実測: `scripts/` は 30 エントリだが、`.sh` は **28 本**で、残りは `clear-validation-data.sql`（SQL）と `dev-throwaway-db-guide.md`（Markdown 文書）。
  - 層 A の判定は変えなくてよいが、**母数の性質の記述が誤っている**（§3.1-1 の趣旨）。§5 の実測表の行「`scripts/` のスクリプト｜30 本」も同様に読み替えられる。「30 エントリ（`.sh` 28 ／ `.sql` 1 ／ `.md` 1）」とすること。

- **[中-6] `SUPP-001` §5.7「最終更新 2 年以上前」の照合が Go の direct 依存 7 件に及んでいない。**
  - §1.6 の注記は「direct 依存 **45 件**のうち 4 件が 2025-01 より前」としているが、この 45 はフロントのみの母数である。**direct 依存の全体は 52 件**（Go 7 ＋ フロント 45）。
  - 本レビューが `go list -m -json` で再取得した Go 側の日付: `gopkg.in/natefinch/lumberjack.v2 v2.2.1` = **2023-02** ／ `github.com/pkg/browser` = **2024-01**。**同じ基準に当たる 2 件が照合対象から外れている。**
  - 「違反を主張するものではない」という留保は Go 側にも同じく当てはまるが、**判断材料としての網羅性が欠ける**。母数を 52 にするか、「フロントのみを対象にした」と明示すること。

- **[中-7] `migrations/*.down.sql` 80 本が層割当の対象外のまま、下流への申し送りが無い。**
  - §0.1 が除外を明記しており、指示書 §5-4（「全 `.up.sql`」）には抵触しない。**判断は正しい。**
  - ただし SPDX ヘッダを配置する別サブ（指示書 §3.3 手順 5）は `.down.sql` 80 本にも同じ判定を要する。**§6 の要判断にも §7 の followup 候補にも、この 80 本の存在が挙がっていない。**
  - `up` と同じ層を機械的に当てるのか、down は全て層 A（構造の巻き戻し）とするのかは判断を要する。**§6 へ 1 行足すこと。**

- **[中-8]（確認事項）`docs/progress/progress-log.md` への索引行が未追記。**
  - 実測: `grep -in 'M26' docs/progress/progress-log.md` → **0 件**（最終見出しは `### M14-RESEARCH-03 …（2026-09-01）`）。
  - **規律違反ではない**——`implement_plan_full` の Phase 順は A → B → C（レビュー）→ D-0 → **D（索引行）** であり、レビュー時点で未追記なのは正しい状態である。
  - **ただし `check-progress-log-index.sh` は `*completion-report*.md` しか対象にしないため、`M26-01-report.md` の索引行が落ちても永久に緑のままになる。**（本レビューで実測確認）**⇒ 人が読む以外に検出経路が無いため、取り込み時に必ず実施すること。** report ヘッダ行 9 が「本報告と `docs/progress/progress-log.md` の索引行…」と書いている以上、書かれていない状態でクローズすると自己申告と実態が食い違う。

### 低（将来対応）

- **[低-9]** §2.2.2 の `000039` 割れ方欄「**1 行目**が `ALTER TABLE`」——実際は **13 行目**（1〜11 行目はコメント、12 行目は空行）。「8 キャラ分」「残り約 50 行」（全 53 行）は正しい。
- **[低-10]** §2.8「`web/public/` は**空ディレクトリ**」——実際は `.gitkeep` が 1 件在る。「画像 0 件」「favicon 参照なし」という結論は正しい。
- **[低-11]** §2.4 の `internal/seedgen/`「15 ファイル：`csv.go` / `format.go` / `generate*.go` ＋ テスト」——**件数 15 は正しい**が、列挙が `model.go` を落としている（実体は `csv.go` / `format.go` / `model.go` / `generate.go` / `generate_m1702.go` / `generate_m2002.go` ＋ テスト 9 本）。
- **[低-12]** §8-5 の html 列挙が 2 本漏れ——`docs/handover/codex-sandbox-rebuild-checklist.html` と `docs/process/parallel-ops.html`。「いずれもカプコン由来の配布禁止物ではない」という結論は変わらない。`A3` へ引き継ぐ材料としての網羅性のみの問題。
- **[低-13]** §1.6 注記の閾値の揺れ——`SUPP-001` §5.7 の逐語は「最終更新が2年以上前(**2024年以前**)」であり、report は後半の括弧に従って「2025-01 より前」で数えている。前半の文字どおり（2026-09 起点で 2024-09 より前）だと該当は 2 件になる。**どちらの読みを採ったかを 1 行添えること。**
- **[低-14]** §1.4 の「third-party notices の分量は、この 315 件と Go の 15 件が主たる母数になる」——Go 側は `// indirect` 15 件だけでなく **direct 7 件も notices の対象**である（合計 22 件）。フロント側の 315 は direct を含む総数なので、Go 側だけ direct を除いた数を並べると非対称になる。

---

## 良かった点

- **全数性の主張が本当だった。** 「表の行名と `ls` の突合を `diff` で行い完全一致を確認した」という自己申告を本レビューが独立に再現したところ、トップレベル 27 行・マイグレ 80 行とも**欠落 0・余剰 0**だった。層の内訳（18/7/2、28/52）も表の行から数え直して一致した。**「数えた」と書いてあるものが実際に数えられている報告は、そう多くない。**
- **ライセンス列 52 件が一次源と 1 件も食い違わなかった。** Go 7 件はモジュールキャッシュの `LICENSE` / `COPYING` 実文（BSD-2 と BSD-3 の弁別を "endorse or promote" の有無で行った手順まで再現可能）、フロント 45 件は `pnpm licenses list --json` と完全一致。**手動法へ落ちた後もこの精度を保っている。**
- **`govulncheck -version` の偽陽性を対照実験で潰した（§1.3）。** 「この出力を到達性の証拠に使ってはならない。パッケージ引数が無く走査対象ゼロのため DB を引かない」——本レビューでも再現した。**次に同じ環境に入る担当が同じ穴に落ちるのを確実に防ぐ記述であり、指示書が要求していない付加価値。**
- **「名に seed が付く＝層 B」で分けたら 25 本を取り違えた、という定量化（§5）。** 本レビューで再現し 4 ＋ 21 ＝ 25 を確認した。**指示書の警告に「はい従いました」で終わらせず、従わなかった場合の被害を数字で示している。**
- **§2.4-1 で、指示書が期待した答えより弱い結論を正直に出した。** 「同節は inbound という語も限定句も一度も書いていない」「文面だけを切り出せば無限定に読める」——**指示書は「inbound であることを文面で確認する」と書いており、確認できたことにする誘因があった場面**で、確認できた範囲を正確に区切っている。この節が `CHANGE-151` を「撤回ではなく明確化」へ導く根拠になっており、**弱く書いたことがかえって結論を強くしている。**
- **§8-3 の用語衝突の発見。** `migrations/000072` のヘッダが「層 A / 層 B」を、本文の見出しが「層 C」を、それぞれライセンス三層と無関係な意味で既に使っている——逐語まで確認して正しかった。**SPDX ヘッダを配置する別サブで実際に事故になる箇所を、誰も指示していないのに先回りして潰している。**
- **§8-1 の「`A1` 承認の前提が 1 つ欠けている」。** checklist が「AGPL §13 の解釈整理を骨子に 1 節足すこと」と要求しているのに骨子に該当節が無い（grep 0 件を本レビューでも再現）。**これは調査の副産物ではなく、承認ゲートを止めうる発見である。**
- **法務のヘッジ規律。** 「これで問題ない」型の記述 0 件。「解される」31 ／「可能性がある」7 ／「確かめられなかった」12。指摘 [高-2] の 1 か所を除き徹底している。
- **`E-225` の観点（新規ファイルに deletions が付いていないか）を §9 で自発的に確認している。** 数字は古かった（指摘 [中-4]）が、**観点そのものを持ち込んだ判断は正しい。**
- **`D-382` の遵守。** followup 本表を 1 文字も編集せず §7 に候補 4 件を挙げるにとどめ、さらに「既存の登録で足りるもの（`cloud-env-cannot-reach-vuln-go-dev`）」を分けて「台帳の更新も新規登録も要らない」と結論している。**候補を増やす方向へ流れず、既存の受け皿を先に確認している。**

---

## 制約事項

- 本レビューはコード上・文書上で判定可能な範囲のみ対象。実際の動作確認・パフォーマンス・実機テストは別途実施が必要。
- **本レビューも法的助言ではない。** 指摘 [高-2] は「Creative Commons の互換宣言の対象が `CC BY-SA 4.0` であって `CC BY 4.0` ではない」という**一次情報の所在についての指摘**であり、`caniuse-lite` を AGPL 配布物へ取り込めるか否かの結論を述べるものではない。本環境から一次情報へは到達できないため、**この点の最終確認は開発者または専門家へ回す必要がある。**
- **`migrations/*.up.sql` 80 本のうち、中身を開いて確認したのは 15 本**（`000002` / `000005` / `000007` / `000012` / `000017` / `000021` / `000022` / `000028` / `000029` / `000039` / `000069` / `000070` / `000074` / `000079` / `000080` のヘッダ含む）。**残りは書き込み先テーブルの機械走査（層 A ⇔ 層 B の相互混入の検出）でのみ検証した。** 走査は「どの表に INSERT / UPDATE / DELETE するか」を見るものであり、**個々の値が SF6 の事実か否かまでは判定していない。**
- **`.up.sql` の層判定のうち、`backfill_*` / `correct_*` / `fix_*` 系（本レビューが個別に開いていない約 20 本）は、report の根拠欄の記述とファイル名の整合のみで妥当と判断した。** 全数の中身確認は行っていない。
- **`docs/` 配下・`web/` 配下・`internal/` 配下の個別ファイルの層は、指示書 §3.2-2 の成果物の対象外**（トップレベル ＋ `migrations/` ＋ `character_data/`）であるため、レビュー対象外とした。
- Go の既知脆弱性は本レビュー環境でも `vuln.go.dev` へ到達できず、**report と同じく確認できていない。**
- Dependabot alerts の有効・無効は、`mcp__*` と `gh api` が deny されているため**本レビューでも確認できていない。**

---

*以上*

---

## 取り込み結果（自動トリアージ）

`/implement_plan_full` Phase C。**14 件すべてを採用した。不採用は 0 件である。**

| # | 優先度 | 指摘の要旨 | 採否 | 理由・対応 |
|---|---|---|---|---|
| 高-1 | 高 | §2.2.2 に `000029_clear_ryu_legacy_seed.up.sql` が抜けている | **採用** | 実測で確認（層 A 側 7 表を明示 DELETE）。§2.2.2 を 3 本へ。波及先 §3.5 / §6-4 / §7 も是正 |
| 高-2 | 高 | §3.4 の `CC BY 4.0` → GPLv3 互換の断定が未検証かつ誤り | **採用** | `BY` と `BY-SA` の取り違えであった。断定を撤回し「確かめられなかった」へ。§6-8 の要判断と §7 の followup 候補 `cc-by-4-agpl-compat-unverified` へ移した |
| 高-3 | 高 | `000017` の根拠「FK 連鎖」は因果が逆 | **採用** | 原文ヘッダで確認（FK=OFF ゆえ CASCADE 不発火 → 明示 DELETE）。表の根拠欄と §2.2.2 の注記を訂正 |
| 中-4 | 中 | §9 の `git diff --stat` が実測とずれている | **採用** | 最終の実行結果へ貼り替えた |
| 中-5 | 中 | 「30 本のシェルスクリプト」が不正確 | **採用** | 「30 エントリ（`.sh` 28 ／ `.sql` 1 ／ `.md` 1）」へ。§5 の実測表も同様に是正 |
| 中-6 | 中 | 2 年基準の照合が Go の direct 依存に及んでいない | **採用** | 母数を direct 全体の **52 件**へ、該当を **6 件**（フロント 4 ＋ Go 2）へ是正 |
| 中-7 | 中 | `*.down.sql` 80 本が下流へ申し送られていない | **採用** | §6-9 の要判断と §7 の followup 候補 `down-sql-layer-unassigned` を新設 |
| 中-8 | 中 | progress-log の索引行が未追記 | **採用** | 指摘のとおり Phase D で実施する（レビュー時点で未追記なのは Phase 順として正しい）。★`check-progress-log-index.sh` は `*completion-report*.md` しか走査しないため機械では検出できない点も、報告書側に記録済み |
| 低-9 | 低 | `000039` の `ALTER` は 1 行目でなく 13 行目 | **採用** | 是正 |
| 低-10 | 低 | `web/public/` は空でなく `.gitkeep` が 1 件 | **採用** | 是正（「画像 0 件」の結論は不変） |
| 低-11 | 低 | `internal/seedgen/` の列挙が `model.go` を落としている | **採用** | 非テスト 6 本を全数列挙する形へ是正 |
| 低-12 | 低 | §8-5 の HTML 列挙が 2 本漏れ | **採用** | 2 本を追加（結論は不変） |
| 低-13 | 低 | 2 年基準の閾値の読みが未明示 | **採用** | 「括弧の側（2024 年以前＝2025-01 より前）を採った」と明記し、別の読みでの件数も併記 |
| 低-14 | 低 | notices 母数が Go 側だけ direct を除いており非対称 | **採用** | Go 22 件（direct 7 ＋ indirect 15）へ是正 |

**★「高」指摘の不採用は 0 件**であるため、Phase C の安全弁（重大指摘の自動棄却時に開発者へエスカレーション）は作動していない。
**再レビュー往復は 0 回**（上限 2 回に対して未使用）。未解消のまま停止した項目は無いため、`followup-backlog.md` §J への停止時記録は発生していない。

*以上*
