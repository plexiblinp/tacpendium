# M24-01 レビュー報告書

| 項目 | 内容 |
|---|---|
| 対象指示書 | `docs/instructions/M24-01-list-navigation-and-default-character.md` **v1.3.0** |
| チェックリスト | `docs/instructions/reviews/M24-01-review-checklist.md` **v1.3.0** |
| 対象完了報告 | `docs/progress/M24-01-completion-report.md` |
| 着手基点 | `210ab49` ／ レビュー時 HEAD `92bc523` ＋ **未コミット 3 ファイル** |
| レビュー実施日 | 2026-08-25 |
| 判定 | **重大 0 件 ／ 高 3 件 ／ 中 6 件 ／ 低 7 件** |

---

## 総評

実装の骨格は指示書の意図どおりである。既定キャラの解決順は純粋関数 1 本へ畳まれ、段の列挙は配列 1 か所にあり、段 3a は列挙にも材料にも入っていない。`INITIAL_CHARACTER_ID` の実使用点が着手前 9 か所であることは `git grep 210ab49` で独立に数え直して一致し、完了時 0 か所（解決関数内 1 か所を除く）も確認した。分岐 A の判定は `internal/api/combo/handler.go:305-341` のバッチ取得を実物で確かめて正しく、Go / `migrations/` / `docs/design/` の差分は 0 バイトである。`SM-002` / `SM-004` / `SM-044` の「既に解消／既に充足」判定は 3 件とも実物で裏が取れた。`SM-083` は 1 バイトも入っていない。`SM-084` の E2E 波及は補助ファイル 1 本へ集約され、id を POST 応答から取る形は教訓 `E-232` に正しく答えている。vitest を再実行し、報告の **184 ファイル / 1867 テスト全 pass** と一致することも確認した。

一方で、**レビュー中に作業ツリーが動いており、完了報告が現物を記述していない**。`web/src/pages/ComboListPage.tsx` ／ `web/e2e/m24-01-character-and-list.spec.ts` ／ モック HTML の 3 ファイルが未コミットで、キャラ名の大見出し撤去という画面変更と、それに追随した E2E ロケータの差し替えが報告のどこにも書かれていない。報告 §9 の変更統計（`40 files / 1571 insertions / 153 deletions`）は現物（`41 files / 1979 / 163`）と一致せず、報告 §7 の `make e2e 178 passed` はこの変更**より前**の測定である。あわせて、失効した記述の是正が 1 経路だけ取り残されており（確定反撃サーチ経路の保存後ダイアログ文面）、§4.9 の「全数チェックリスト」も本番コードのコメント 3 行を落としている。いずれもテスト・lint・型検査では緑のままの型である。

---

## 設計準拠性レビュー結果

### 1. 設計・パターンとの照合（チェックリスト §1）

| 観点 | 評価 | 所見 |
|---|---|---|
| 解決が 1 か所へ集約・順序どおり | **◎** | `web/src/features/combo/defaultCharacter.ts:56-62` の `CHARACTER_RESOLUTION_STAGES` が `1-url → 2-session → 3b-config` の 1 配列。`resolveCharacterIdWithStage` は配列を回すだけで、段ごとの `if` は無い。 |
| **段 3a が未実装か・1 行で差し込めるか** | **◎（未実装は確認済み）／○（「1 行」は正確には 2 か所）** | `git diff 210ab49 \| grep main_character_id` の全ヒットがコメント・テスト名・報告文のみで、実装経路は無い。差し込み位置のコメントも `:59-60` に置かれている。ただし実際に効かせるにはフック `useResolvedCharacterId.ts` 側で材料を 1 つ増やす必要があり、**「1 行足せば済む」は解決関数側だけの話**である（フック内コメント `:23-24` はそう正しく書いてあり、完了報告 §4.1 の書き方だけが強い）。指示書 §4.1-2a の要求（段の列挙が 1 か所）は満たしている。 |
| **マイコンボが「キャラだけ」を読むか** | **◎** | `readSessionCharacterId()`（`useComboListFilters.ts:46-62`）は `URLSearchParams(saved).get("character_id")` **のみ**を取り出し、戻り値は `number \| null`。`MyComboPage.tsx` は `useResolvedCharacterId()` の戻り値（数値 1 個）しか受け取らない。タグ・状況・仮登録トグル・ソートを読む経路は構造的に存在しない。**チェックリスト §9-9 に該当しない。** |
| 段 3b の実在検査 | **○** | `verifyExists: true` が段 3b にだけ付き、`existsAsCharacter` で確認している。ただし**検査が効かない経路がある**（中-2）。 |
| 新規ストレージキーを作っていないか | **◎** | 新キー 0。`bash scripts/check-browser-storage-keys.sh` を再実行し「台帳 9 件 / 本番コード 8 件・違反なし」を確認。`localStorage` へも移していない。 |
| 台帳の実査と無断追記の不在 | **◎** | `web/CLAUDE.md` §1 #6 に `combo-list-filters-v1` が登録済み（実物で確認）。本サブによる台帳追記は 0。 |
| 読めない・壊れているとき段 3 へ落ちるか | **◎** | `createSessionStorageHelper` が try-catch 済みで失敗時 `null`。`readSessionCharacterId` にも二重の try-catch。`{壊れた JSON` の入力で `null` が返ることをテストで固定（`useComboListFilters.test.tsx`）。 |
| M23 成果物・`/trash` 非接触 | **◎** | `TrashListRow.tsx` / `TrashPage.tsx` / trash 系 API に差分なし（`git diff --stat` で確認）。 |
| `FR013` 凍結 | **◎** | 作成者概念の追加なし。 |

### 2. データ・API 契約・スキーマの不変（チェックリスト §2）

| 観点 | 評価 | 所見 |
|---|---|---|
| スキーマ変更・マイグレ連番 | **◎** | `git diff --stat 210ab49 -- internal/ cmd/ migrations/` が空。0 本消費。 |
| 分岐 A の Go 差分 0 バイト | **◎** | 同上。例外条項 §2.3 は未発動で正しい。 |
| `queryKey` の rename | **◎** | `web/src/features/combo/api.ts` に差分なし。 |
| i18n の追加のみ・parity | **◎** | `comboList.characterScope.label` / `.note` / `comboList.column.setupCount` / `comboEditor.characterField` の 4 キーが ja / en 両方へ追加。既存キーの rename なし。 |

### 3. フロントエンドの動作仕様（チェックリスト §3）

| 観点 | 評価 | 所見 |
|---|---|---|
| **`SM-119` の解き方** | **○（指定からの逸脱は正当だが記録が不完全）** | 指示書 §4.1-4 は「新規登録画面側に明示的な入力を置く」と指定していたが、開発者の途中指示（逐語が完了報告 §4.1-4 に引用されている）により**一覧画面のヘッダ帯へキャラ選択を移す**形へ差し替えられている。`?character=` の伝播は切られておらず（`ComboListPage.tsx` の `newComboHref` が健在、`ComboEditorPage.tsx:28-36` の読み手も不変）、**`SM-044` との衝突は起きていない**。新規登録画面側の見出し「このコンボのキャラ」も残置されており、両方が入っている。**⇒ 差し替えそのものは妥当。** ただし `CHANGE-130` の本文がこの差し替えで失効しており、報告の失効一覧に載っていない（中-5）。 |
| 保存後の遷移先が保存キャラを対象にするか | **◎** | `ComboEditor.tsx` の `saveDestination()` が `mode === "new"` のときだけ `/combos?character_id=<保存キャラ>` を返し、段 1（URL）で保つ。Vitest（`ComboEditor.test.tsx` の 3 本）と E2E ケース A の両方で固定されている。編集・コピー・確定反撃サーチ復帰の 3 経路には対照テストが置かれている。 |
| セットプレイ数の列がカスタマイズ対象か | **◎** | `ColumnVisibility` / `DEFAULT_COLUMN_VISIBILITY` / `COLUMN_DEFINITIONS` の 3 か所に `setupCount` を追加。非表示テストあり。 |
| `ComboSortControls` の一覧側挙動 | **◎** | 部品に差分なし（新規テストのみ）。一覧側の挙動は 1 バイトも変わっていない。 |

### 4. テストの妥当性（チェックリスト §4）

| 観点 | 評価 | 所見 |
|---|---|---|
| §5.1 の 1〜3 が実在するか | **◎** | 解決関数 11／`ComboSortControls` 6／セットプレイ列 4。件数は報告に記載あり。 |
| 「段 3 が実在しないとき段 4」「ストレージが読めないとき段 3」 | **◎** | `defaultCharacter.test.ts:55-64` と `:73-81` に実在。 |
| §5.2 の E2E 3 ケース | **◎** | `web/e2e/m24-01-character-and-list.spec.ts` に A / B / C が実在。判定キーは `m2401-${Date.now()}` で spec 固有（`E-232`）。件数での存在判定はしていない。 |
| テスト結果が件数で書かれているか | **○** | 報告 §7.1 はスイート名と件数つき。vitest は再実行で一致を確認した。**ただし `make e2e` の数値は未コミット変更より前の測定**（高-1）。 |
| 実アプリと同じ状態遷移で描画しているか（`E-217` / `E-223`） | **◎** | `ComboEditor.test.tsx` の新規 3 本は `mockCreateMutate.mock.calls[0]` から実際の `onSuccess` を取り出して呼んでおり、no-op モック化していない。E2E / Vitest とも否定形の前に「描画されたこと」を 1 行アサートしている。 |
| **テスト名・コメントが実際の主張と一致しているか** | **△** | 3 本が名乗りより弱い（中-3）。 |
| 破壊確認 3 件 | **○** | コマンド・出力・狙ったアサーション文言つきで 3 件とも記載。全 184 ファイルを毎回回して巻き添えが無いことも確認している（`E-230` に正しく答えている）。**#2 の差し替えは妥当**——両画面が同一部品・同一 `SORT_FIELD_VALUES` を使う以上、指示書 §5.3-2 の破壊は真に no-op であり、実物（`MyComboPage.tsx` / `ComboListFilters.tsx` が同じ `ComboSortControls` を呼ぶ）で裏が取れた。差し替え先（部品が全数を出さなくなる）は「集合がずれたら赤くなる」という趣旨を保っている。 |

### 5. 設計意図との整合（チェックリスト §5）

| 観点 | 評価 | 所見 |
|---|---|---|
| A 群 4 件が 1 つの規則へ収束したか | **◎** | 5 面（一覧・マイコンボ・エディタ・比較・比較追加モーダル）がすべて `useResolvedCharacterId` を通る。画面ごとの分岐は増えていない。 |
| `SM-002` の形と理由 | **◎** | 実装せずと判定。裏を取った：件数は `MyComboStatusTabs.tsx:58` でタブのラベル内（押せばタブが切り替わる＝空振りしない）、タブ外の件数は `CharacterInfoBar.tsx:36-40` の合計 1 個で非クリック、同ファイルには `C-07: …タブと役割を分離して紛らわしさを解消` のコメントが残っている。**不変条件「利用者がタブだと思って件数を押さない」は現状で成立している。** |
| `SM-004` の「使える項目を渡せる形」 | **◎** | 実装せずと判定。裏を取った：`MyComboPage.tsx:73`（`isSortField` が `SORT_FIELD_VALUES`）と `ComboListFilters.tsx` が同一部品・同一定数を使っており、集合は元から一致している。**揃えに行っていないのが正しい。** |

### 6・7・8（コード品質／既存挙動の温存／ドキュメント）

| 観点 | 評価 | 所見 |
|---|---|---|
| `console.log` / `eslint-disable` / `any` | **◎** | 追加行に 1 件も無い（差分 grep で確認）。 |
| **「N 件」の実数一致（`E-220` / `E-236`）** | **△** | §4.9 の是正件数が実数と違う（高-3）。一方で `ColumnVisibilityMenu.test.tsx` のベタ書き `6` を機械的に `7` にせず `COLUMN_DEFINITIONS.length` 由来へ書き直したのは、まさに `E-220` の罠を避けた良い判断である。 |
| `INITIAL_CHARACTER_ID` の godoc | **◎** | `web/src/lib/constants.ts:15-22` が「呼び出し元は `defaultCharacter.ts` の 1 か所だけ」と書いており、実物と一致する（grep で確認）。`E-224` の指示どおり「増えたときにしか壊れない形」になっている。 |
| 既存テストの全数（3 系統） | **◎** | 報告 §2 #9 に Vitest / E2E / Go を別々に記載。実測 E2E 10 spec への波及も §5 に別表で書かれている。 |
| `colSpan`（`E-192`） | **◎** | `ComboTable.tsx:100` が `Object.values(visibility).filter(Boolean).length` で自動追随。`TrashListRow.tsx:141` の `colSpan={6}` は独立テーブルで非接触（実物で確認）。「非表示で 1 減る」テストまで置いてある。 |
| `AddComboToCompareModal` の挙動 | **○** | 文脈優先（`defaultCharacterId` 最優先）のロジックは不変で、`ComparePage.character.test.tsx` に対照テストが 2 本ある。フォールバック値だけが定数から解決順へ移った——これは §4.1-5 の要求（参照の置換）と §1.3 の要求（挙動不変）が原理的に両立しない箇所であり、**採った形（文脈ロジック不変 ＋ 落ち先だけ解決順）が妥当**と判断する。 |
| モック HTML | **◎** | `docs/progress/M24-01-mock/m24-01-screens.html`。外部 CSS / JS / 画像の参照ゼロ（grep で確認）。パネル 1・2 は変更前 / 変更後を並置。パネル 3 は「現状の写し」1 枚のみで変更案を描いていない——`D-543` 逐語（「直っている可能性がある」）に照らして妥当。**未コミット編集で実装（大見出し撤去）に追随済み。** |
| `progress-log.md` 索引行（`E-208`） | **○（未了・正しい順序）** | 現時点で M24-01 の行は無い。指示書 §7.4「索引行はレビューの後に埋める」に従っており、**レビュー前に書かれていないこと自体は正しい**。取り込み時に必ず追記すること。 |
| 設計伝達レポート | **未了** | `docs/handover/design-reports/` に M24-01 分が無い。DoD §7.5 の残作業。 |

---

## 設計準拠性以外の指摘事項

- **コーディング規約**: `any` なし、class コンポーネントなし、import 順も既存に揃っている。JSON / DTO の camelCase も維持。列挙定数の新設は無く `check-enum-sync.sh` の対象外。
- **命名**: `resolveCharacterIdWithStage` / `CHARACTER_RESOLUTION_STAGES` / `CHARACTER_FALLBACK_STAGE_ID` は段の識別子を戻り値に含める設計で、テストが段を名指しできる。良い形。
- **ライブラリ**: 新規依存 0。
- **セキュリティ**: ブラウザストレージへ機密情報・DB 永続化対象データを入れていない。段 2 が読むのは整数 1 個のみ。
- **機械検査**: `check-browser-storage-keys.sh`（違反なし）・`check-md-emphasis.sh`（ベースラインどおり 436 行）・`check-doc-inventory.sh`（新種 3 件はいずれも IMPROVE-01 由来で本サブと無関係）をレビュー側で再実行し、報告と一致することを確認した。

---

## 推奨修正（優先度別）

### 高（M24 完了前に修正必須）

**高-1. 未コミット変更 3 件が完了報告に載っておらず、E2E の再実行証拠が無い**（チェックリスト §8 ／ 教訓 `E-189` / `E-193` / `E-242`）

`git status` が次の 3 ファイルを未コミットで返す。

```
 M docs/progress/M24-01-mock/m24-01-screens.html
 M web/e2e/m24-01-character-and-list.spec.ts
 M web/src/pages/ComboListPage.tsx
```

内容は「一覧ヘッダ帯からキャラ名の大見出しを外し、登録件数をプルダウンの右へ移す」という**画面変更**と、それに追随した E2E ロケータの差し替え（`getByText(dhalsimName)` → `getByTestId("combo-list-character-scope")` の `toHaveValue`）である。ロケータを差し替えざるを得なかった理由はコメントに正しく書かれている（大見出しを外した結果、キャラ名が閉じた `<select>` の `<option>` にしか現れず hidden 扱いになる）。

問題は次の 3 点。

1. **完了報告 §4.1-4 / §12-2 のどこにもこの変更が書かれていない。** 設計卓へ渡る as-built は「ラベル『対象キャラ』＋補足」までで、**大見出しの撤去と件数の移動が落ちる。**
2. **報告 §9 の変更統計が現物と違う。** 報告は `40 files changed, 1571 insertions(+), 153 deletions(-)`、現物は `41 files changed, 1979 insertions(+), 163 deletions(-)`。§9 は「新規のはずのファイルに deletions が付いていないか」（`E-225`）を確かめる節であり、**その節の数値自体が現物と対応していない。**
3. **報告 §7.1 の `make e2e 178 passed / 0 failed / 0 flaky` は、この変更より前の測定である。** 変更されたのは E2E spec とその対象画面そのものであり、**いま緑である証拠が存在しない。**

**やること**: (a) 3 ファイルをコミットする、(b) `make e2e` を現物で回し直して件数を報告 §7.1 へ差し替える、(c) 報告 §4.1-4 と §12-2 に「キャラ名の大見出しを外し、アイコン → 対象キャラ → 登録件数の順にした（2026-08-25 開発者判断）」を追記する、(d) §9 の統計を取り直す。

**高-2. 保存後の警告ダイアログ文面が、確定反撃サーチ経路で失効したまま残っている**（指示書 §2.2「失効した記述の是正」／ 本レビューの優先度較正）

`web/src/features/combo/components/ComboEditor.tsx`:

```ts
const pendingNavigateLabel = pendingNavigateTo?.startsWith("/combos?")
  ? "一覧"
  : "詳細画面";
```

遷移先は 3 通りある（`punishReturn` ／ `/combos?character_id=N` ／ `/combos/:id`）が、判定は 2 分岐である。**`punishReturn`（`/punish/search?self=…`）のとき、ダイアログは「警告を確認した上で詳細画面へ移動しますか?」「続行（詳細画面へ移動）」と表示しながら、確定反撃サーチへ戻る。**

完了報告 §4.4 は「固定文言だったため…遷移先から呼び名を導くようにした」と書いているが、**導けているのは 2 経路だけである。** この経路は `hasWarning === true` かつ確定反撃サーチからの登録という組み合わせで実際に到達する（`handleSaveSuccess` の `dest = punishReturn ?? saveDestination(data)` → `setPendingNavigateTo(dest)`）。

なお本件は M24-01 以前から誤っていた（元は全経路が「詳細画面」固定）。**それでも「高」に置く**——報告が「是正した」と宣言した箇所の一部が旧のまま残っており、後任は宣言のほうを読む。動作は正しいままなのでテストも lint も型検査も緑である。

**やること**: `punishReturn` の分岐にも呼び名を与えるか、判定を「`/combos?` で始まる → 一覧 ／ `/combos/` で始まる → 詳細画面 ／ それ以外 → 元の画面」の 3 分岐にする。

**高-3. §4.9 否定形確認の「全数チェックリスト」が全数でない（是正 5 件 → 実数 8 件）**（チェックリスト §6 / §8 ／ 教訓 `E-220` / `E-236` / `E-193`）

`git grep -n INITIAL_CHARACTER_ID 210ab49 -- web/src` で着手前のヒットを数え直したところ、**本番コードのコメント 3 行**が報告 §6 の仕分け表に載っていない。

| 着手前の位置 | 逐語（要旨） | 現状 |
|---|---|---|
| `AddComboToCompareModal.tsx:21` | 「不在時は `INITIAL_CHARACTER_ID`。」 | 是正済み（表に無い） |
| `ComboEditor.tsx:78` | 「不在時は `INITIAL_CHARACTER_ID`。」 | 是正済み（表に無い） |
| `ComparePage.tsx:86` | 「空なら `INITIAL_CHARACTER_ID`」 | 是正済み（表に無い） |

**是正そのものは 3 件とも行われている**（差分で確認）。壊れているのは記録のほうで、報告は「**★是正 5 件は**いずれも動作を変えない記述である」と断定している。実数は 8 件である。あわせて、チェックリスト §8 が求める「『検出した箇所』と『手当てした箇所』が**別の表**になっているか」（`E-193`）を満たしておらず、1 つの表に混ざっている。

**やること**: §6 の仕分け表へ 3 行を追加し、件数の断定を実数へ直す。検出表と手当て表を分ける。

### 中（M25 着手と並行可）

**中-1. キャラ一覧未取得時のフォールバック `<option>` が常に「リュウ」表記**（`web/src/pages/ComboListPage.tsx`）

```tsx
<option value={selectedCharacterId}>{t("comboList.characterRyu")}</option>
```

`value` は解決済み ID（段 3b で 1 以外になりうる）だが、表示名は固定で「リュウ」である。移設前は `value={INITIAL_CHARACTER_ID}` と対だったため整合していた。**大見出しを外した（高-1）ことで、一覧上でキャラ名が出る唯一の場所がこの `<select>` になり、食い違いの影響が増している。**`t("comboList.characterRyu")` はフェーズ 1 の 1 キャラ運用の名残でもある。

**中-2. 段 3b の実在検査が、`ComboEditor` の初期化子経路では効かない**（チェックリスト §1「無いと、設定が実在しないキャラを指したとき段 4 へ落ちずに壊れる」）

`existsAsCharacter()` は `knownCharacterIds == null` のとき **`true` を返す**（検査をスキップ）。`useCharacters()` は React Query の取得であり、`App.tsx` のゲート（`useConfig` の `isLoading` のみ）には入っていない。

`ComboEditor` は解決値を `useState(() => initialBasic(...))` の**遅延初期化子で 1 回だけ**読む（`ComboEditor.tsx:130-132`）。初回描画時点で `characters` は未取得なので、**`config.toml` が実在しないキャラ ID を指していると、そのまま新規コンボのキャラとして採用され、段 4 へ落ちない。** `ComboListPage` / `MyComboPage` は毎描画で解決し直すため後から自己修復するが、`ComboEditor` は修復しない。

スキップ自体は「検査できないことを不在と読まない」という妥当な判断であり、コメントにも書かれている。問題は**それが `ComboEditor` では恒久的になる**点である。テストにも E2E にも出ない（E2E 環境は `config.toml.example` に `[defaults]` が無く既定 1 のため、段 3b と段 4 が同値）。

**やること**: `ComboEditor` 側で `characters` 到着後に再解決するか、`useResolvedCharacterId` に「未取得のあいだは解決を保留する」旨を明示して呼び出し側の判断材料にする。少なくとも制約をコメントへ残す。

**中-3. 名乗りより弱いテスト／コメントが 3 か所**（チェックリスト §4「レビューの静的読解だけが検出経路である」）

| 位置 | 名乗り | 実際に見ているもの |
|---|---|---|
| `useComboListFilters.test.tsx`「★キャラ以外は読まない(DES-005 §5.5 の除外を破らないこと)」 | 「他のキーを読んでいない」 | `expect(typeof readSessionCharacterId()).toBe("number")` **だけ**。戻り値型は TypeScript のシグネチャで既に保証されており、実装が他のキーを読んでも緑になる。 |
| `defaultCharacter.test.ts`「段 3a を 1 行足せば差し込める形になっている」 | 「差し込める形である」 | ローカルで組んだ配列 `withUserStage` の順序を確かめるだけで、`resolveCharacterIdWithStage` を 1 度も通していない。**実装が段の列挙を無視していても緑になる。** |
| `web/e2e/m24-01-character-and-list.spec.ts` ケース B のコメント「一覧側だけが持つ軸(**仮登録トグル・並び順**)が混ざっていないことで判定する」 | 2 軸を見る | 実際のループは `get("is_draft")` の 1 軸だけ。**並び順は見ていない**（マイコンボは自前の `sort` を常に送るため、そのままでは書けない）。 |

否定形の実質は `MyComboPage.test.tsx` の「★キャラ以外は持ち込まない」（`tag_ids` / `is_draft` / `sort` / `order` を `URLSearchParams` で解いて比較）が担っており、**仕様は守られている**。直すべきは名乗りとコメントである。

**中-4. 削除後の URL 主張を必要以上に緩めた（10 か所 / 7 spec）**（指示書 §2.2「更新後も同じかそれ以上の主張を持つこと」）

`toHaveURL("/combos")` → `toHaveURL(/\/combos(\?.*)?$/)`。**任意のクエリを許容する**ため、更新前より弱い。段 2 の復元で `?character_id=` が付きうるという理由は正しいが、`/\/combos(\?character_id=\d+)?$/` まで絞れば母集合を変えずに主張を保てる。

`m23-05-duplicate-collision.spec.ts` も同型で、`new RegExp("/combos/" + comboID + "$")`（**どのコンボか**まで主張）から `/\/combos\?character_id=\d+/`（キャラ ID の値を問わない）へ弱まっている。ここは `character_id=${TRASH_CHARACTER_ID}` まで書ける。

**中-5. `CHANGE-130` の本文が `SM-119` の差し替えで失効しているのに、報告の失効一覧に無い**

`docs/change-notes/CHANGE-130-notification.md:78`:

> **`SM-119`（フィルタが新規登録のキャラ選択を兼ねていて不自然）は、新規登録側に明示的な入力を置き、その初期値を解決順から取ることで解く。**

実装は一覧側のヘッダ帯へ移す形になった。報告 §6 は「生きたルール面で旧定義を述べているのは **2 行**」（`DES-005:447` / `:560`）と断定しているが、**この 1 行が抜けている**。設計書と同じく製造は編集しない（`CLAUDE.md` §8）ので、**設計伝達レポート §1 へ逐語で回すこと。**

**中-6. `combo-list-filters-v1` の読み手が広がったが、台帳の用途欄が旧のまま**

`web/CLAUDE.md` §1 #6 の用途は「コンボ一覧のフィルタ・ソート状態（in-app ナビゲーション中の一時保持）」。本サブで**マイコンボ・エディタ・比較の 3 面が読み手として加わった**。台帳への追記は CHANGE 経由なので製造が直さないのは正しいが、**設計伝達レポート §4 の候補として挙がっていない。** `DES-005` §5.4 / §5.5 を 1 文字も変えないという建付けは維持されているため、必要なのは台帳側の用途欄の更新だけである。

### 低（将来対応）

- **低-1**: `MyComboPage.test.tsx` / `useComboListFilters.test.tsx` / `ComboTable.test.tsx` の 3 ファイルが末尾改行なし（`\ No newline at end of file`）。
- **低-2**: `pendingNavigateLabel` の `"一覧"` / `"詳細画面"` が i18n を通っていない。周囲のダイアログ文面が元から素の日本語なので整合はしているが、指示書 §2.1-7 の parity 要求からは外れる。既存キーの rename ではないので `M24-07` へ回してよい。
- **低-3**: `m21-05-keyboard-input.spec.ts` の保存後待ちから明示 `{ timeout: 15000 }` が落ちた。`playwright.config.ts` は `expect.timeout` を設定しておらず既定 5s である。実質の待ちは `waitForResponse` が担うため実害は小さいが、**当時 15s を積んだ理由の記録が消えている**。将来ここが flaky になったら真っ先に疑う場所として記録を残すこと。
- **低-4**: `setupCount` 列は `useColumnVisibility` を共有するため**マイコンボ画面にも出る**。指示書は禁じていないが、報告に記載がない。意図なら as-built に 1 行足すこと。
- **低-5**: `internal/api/combo/handler.go:311-314` はセットプレイのバッチ取得に失敗すると warn ログだけ出して `setups` を空にする。**この経路では新しい列が全行 `0` になり、「本当に 0 件」と区別できない。** フロント単独では直せない（分岐 B＝契約変更が要る）ため、設計卓へ回す材料として記録するにとどめる。
- **低-6**: `m17-01-media-fields.spec.ts` に、削除した `toHaveURL` の跡の空行が残っている（コメント直後に空行 → `const anchor`）。
- **低-7**: `docs/progress/M24-01-mock/` は指示書が指定した置き場だが、`CLAUDE.md` §10.Y が求める**寿命の宣言**が無い。役目を終えたときに誰も消さない形になる。

---

## 良かった点（Claude Code へのフィードバックとして残す）

1. **実査 14 件を「数と行番号つき」で全件返している。** とくに `INITIAL_CHARACTER_ID` の参照点を独立に数え直し、**ledger の「4 面」が誤りで実測 6 ファイル / 9 か所だった**ことを根拠つきで報告している。私が `git grep 210ab49` で数え直した結果と 1 件も違わなかった。「N か所は着手時に数え直す」がそのまま効いた例である。
2. **「実装しない」を 3 件とも実物で裏付けている。** `SM-002` は `MyComboStatusTabs.tsx:58` と `CharacterInfoBar.tsx:35`（`C-07` のコメントが現存）、`SM-004` は両画面が同じ `SORT_FIELD_VALUES` を参照している事実、`SM-044` は `useComboListFilters.ts` の復元経路。**どれも「memo にそう書いてあるから」で押し切らず、症状が現在も成立するかを確かめている**（`E-219` の型）。しかも「実装しない」で終わらせず、成立をテストで固定している（`ComboSortControls` は従来テスト 0 本だったところへ 6 本）。
3. **破壊確認 #2 が実行不能であることを、逃げずに書いている。** 「指示書どおりにやったら no-op になる」を隠さず報告し、破壊の趣旨（集合がずれたら赤くなる）を保つ等価な破壊へ差し替えている。差し替えの理由が「両画面が 1 本の部品と 1 本の定数へ統合されている以上、ずれを起こす唯一の経路は部品が全数を出さなくなること」と機序で書かれており、判断の再現ができる。
4. **E2E の波及 22 か所を「同じ 3 行を 10 ファイルへ複製する」で解かず、補助ファイル 1 本へ集約した。** しかも id を「一覧の一番新しい行」ではなく **`POST /api/combos` の応答**から取っている。`fullyParallel: false` でもファイル単位では並行に走るという教訓 `E-232` に、コメントごと正しく答えている。
5. **`ColumnVisibilityMenu.test.tsx` のベタ書き `6` を機械的に `7` へ +1 せず、`COLUMN_DEFINITIONS.length` 由来へ書き直した。** 「本来の主張は『定義した列がすべて出る』であり数ではない」という判断は、`E-220`（元のずれが温存される）を構造的に断っている。
6. **`check-browser-storage-keys.sh` が赤で止めたとき、検査を緩めずコメント側を直した。** 報告にその経緯まで残っている。検査を通すために例外を足す方向へ行かなかったのは正しい。
7. **`colSpan` を「自動追随するから大丈夫」で終わらせず、非表示にすると展開行の `colSpan` が 1 減ることをテストで固定した**（`E-192`）。`TrashListRow` が独立テーブルであることも実査済みで、触っていない。
8. **確定反撃サーチ経路のキャラ解決に回帰が無い。** `PunishTree.tsx` が常に `?character=${self}` を付けるため段 1 が勝ち、`basic.characterId !== punishSelfCharacterId` による「登録されませんでした」への転落は起きない。ここは段 2（セッション）が割り込めば静かに壊れる場所だったが、対照テストも置かれている。
9. **判断が割れうる点を §13 に 3 件、開発者向けに明示している。** `mode === "copy"` を広げるかを「推測で `new` に絞った・分岐は `saveDestination()` の 1 行なので戻せる」と、**戻し方まで添えて**書いているのは良い。

---

## 制約事項

- 本レビューはコード上で判定可能な範囲のみ対象。実際の動作確認・パフォーマンス・実機テストは別途実施が必要。
- **`make e2e` はレビュー側で実行していない**（`Makefile` §e2e の注記どおり開発環境の初期化状態を変えるため、read-only レビューの範囲外と判断した）。高-1 のとおり、現物に対する E2E の緑は**まだ誰も確認していない**。
- vitest は再実行した（`pnpm exec vitest run` → **184 ファイル / 1867 テスト全 pass**、報告 §7.1 と一致）。`check-browser-storage-keys.sh` / `check-md-emphasis.sh` / `check-doc-inventory.sh` も再実行し、報告と一致することを確認した。
- **レビュー中に作業ツリーが動いた。** `web/e2e/m24-01-character-and-list.spec.ts` と `docs/progress/M24-01-mock/m24-01-screens.html` はレビュー開始後に更新されている。本報告は**更新後の状態**に対する判定である。

---


---

## 取り込み結果（自動トリアージ）

実施日 2026-08-25 ／ 製造担当 Claude Code（`implement_plan_full` Phase C）。
**「高」の不採用は 0 件**（安全弁の発動なし）。**再レビュー往復は 0 回**（上限 2 回。未消化）。

### 高（3 件・**全件採用**）

| # | 採否 | 対応 |
|---|---|---|
| **高-1** 未コミット変更 3 件が報告に無く、E2E の再実行証拠が無い | **採用** | (a) 全ファイルをコミットした。(b) `make e2e` を最終状態で回し直し **178 passed / 0 failed / 0 flaky・exit 0** を報告 §7.1 へ差し替えた。(c) 報告 §4.1-4 へ「キャラ名の大見出しを撤去し、アイコン → 対象キャラ → 登録件数の順にした（開発者逐語つき）」と、E2E ロケータを差し替えた理由を追記。§12-2 の as-built にも項目 6 として追加。(d) §9 の統計を **43 files / 2609 / 175** で取り直し、**「初版の数値が現物と対応していなかった」ことを節の中に残した**（数値だけ直すと同じ落ち方を繰り返すため） |
| **高-2** 保存後ダイアログ文面が確定反撃サーチ経路で失効 | **採用** | `pendingNavigateLabel` を 3 分岐へ（`/combos?` → 一覧 ／ `/combos/` → 詳細画面 ／ それ以外 → 元の画面 ／ 未設定 → 次の画面）。**指摘のとおり本件は M24-01 以前から誤っていたが、報告が「是正した」と宣言した箇所の一部であるため「高」の扱いに同意する。** |
| **高-3** §4.9 の全数チェックリストが全数でない（5 件 → 実数 8 件） | **採用** | §6 を**検出表（15 行）と手当て表（是正 8 ／ 残す 4 群）の 2 表へ分割**した（`E-193`）。件数の断定を実数 8 へ直し、**「初版が 5 件と書いていた」ことも残した**——これ自体が `E-220`/`E-236`（自分が書く数も疑う）の実例だったため |

### 中（6 件・**採用 6 / 不採用 0**）

| # | 採否 | 対応 |
|---|---|---|
| **中-1** 未取得時のフォールバック `<option>` が常に「リュウ」表記 | **採用** | `{selectedCharacterName \|\| t("common.unknown")}` へ。**★新しい i18n キーは作らず既存の汎用キーを使った。** 未使用になった `comboList.characterRyu` は**撤去しない**（既存キーの整理は `M24-07` の手番＝§2.2）。**なお本件は `followup-backlog`（phase2）§115 と M7-05 C-1 が同じ defect を挙げており、そこへ合流する形になった** |
| **中-2** 段 3b の実在検査が `ComboEditor` の初期化子経路で効かない | **採用** | `ComboEditor` に採り直しの `useEffect` を置いた。条件は「新規モード」「段 1（`?character=`）が無い」「利用者がキャラに触れていない」の 3 つ。**★`isFormDirty` は `characterId` を除いて判定するため、dirty だけでは守れない**——`characterTouchedRef` を別に持たせた。**テスト 4 本（採り直す／触れたら採り直さない／段 1 が在れば採り直さない／編集モードでは採り直さない）と、破壊確認 1 件を追加**（§8 の破壊 4） |
| **中-3** 名乗りより弱いテスト／コメント 3 か所 | **採用** | (a) `useComboListFilters.test` の「キャラ以外は読まない」は型を見ているだけだったため、**「他の軸が同居していても返るのは character_id の値」＋「character_id を外すと他の軸が残っていても null」の対照**へ書き直した。(b) `defaultCharacter.test` の「1 行足せば差し込める」を**実際に見ているものへ改名**し、「resolveCharacterIdWithStage を通していない」「フック側でも材料が要る」を注記。(c) E2E ケース B のコメントを**実際に見ている 1 軸**へ直し、4 軸の否定形は `MyComboPage.test` が担うと明記 |
| **中-4** 削除後の URL 主張を必要以上に緩めた | **採用** | 10 か所を `/\/combos(\?character_id=\d+)?$/` へ絞った。`m23-05` は `character_id=${TRASH_CHARACTER_ID}$` まで書いた |
| **中-5** `CHANGE-130:78` の失効が報告の一覧に無い | **採用** | 報告 §12-1 を「2 行」から**3 行**へ改め、逐語つきで追加。**設計書と同じく製造は編集しない**（`CLAUDE.md` §8）ため、設計伝達レポートへ回す |
| **中-6** 台帳 #6 の用途欄が旧のまま | **採用** | 報告 §12-3 へ追記。**台帳の追記は CHANGE 経由のため製造は直さない**（指示書 §4.1-3 / `CLAUDE.md` §10.X） |

### 低（7 件・**採用 6 / 不採用 1**）

| # | 採否 | 対応 |
|---|---|---|
| **低-1** 末尾改行なし 3 ファイル | **採用** | 該当 2 ファイル ＋ 本サブで触った他 3 ファイルも確認して付与 |
| **低-2** `pendingNavigateLabel` が i18n を通っていない | **★不採用（`M24-07` へ）** | **理由**: 周囲のダイアログ文面（「保存完了 — 警告があります」「このページに留まる」等）が**元から素の日本語**であり、この 1 語だけ i18n へ通すと**同じダイアログ内で 2 系統が混在する**。指示書 §1.3 は「i18n の語彙見直しは `M24-07` の面」と明示しており、**ダイアログごとまとめて移すのが正しい単位**である。レビュアー自身も「`M24-07` へ回してよい」としている |
| **低-3** `m21-05` の明示 `{ timeout: 15000 }` が落ちた | **採用** | **当時 15s を積んだ理由と、いま落としてよい理由（待ちは `waitForResponse` が担う）、将来 flaky になったら真っ先に疑う場所であること**をコメントで残した |
| **低-4** `setupCount` 列がマイコンボにも出る | **採用（as-built の明記）** | 意図である（列カスタマイズを一覧と共有する既存の建付け）。報告 §12-2 の項目 7 として明記 |
| **低-5** バッチ取得失敗時に列が全行 `0` になる | **採用（設計卓へ回す材料として記録）** | フロント単独では直せず、区別には `DES-002` §4.2 の契約変更が要る。報告 §12-3 へ記録 |
| **低-6** `m17-01` に空行の跡 | **採用** | 2 か所とも整形 |
| **低-7** モック置き場の寿命が未宣言 | **採用** | 報告 §12-3 で寿命を宣言（「`CHANGE-130` が `DES-005` へ反映され、設計卓が形を確認し終えた時点まで」）。**★「消す」は開発者の手番**（D-196）のため `M24-CLOSE` の観点として挙げる |

### 取り込み後の検査

| 検査 | 結果 |
|---|---|
| `pnpm exec tsc --noEmit` | exit 0（エラー 0 件） |
| `pnpm exec vitest run` | **184 ファイル / 1871 テスト全 pass** |
| `make e2e` | **178 passed / 0 failed / 0 flaky**・exit 0・3.7 分 |
| `bash scripts/check-artifact-integrity.sh` | 違反なし |
| `bash scripts/check-browser-storage-keys.sh` | 違反なし |
| `bash scripts/check-md-emphasis.sh` | ベースラインどおり |
| `bash scripts/check-progress-log-index.sh` | 緑（索引行の追記後に確認） |

### 未解消のまま停止した項目

**なし。** 高・中は全件採用し、低の不採用 1 件は行き先（`M24-07`）が決まっている。
**⇒ `docs/handover/followup-backlog.md` §J への停止時記録は不要。**

---

*以上、M24-01 レビュー報告書 ＋ 取り込み結果。*
