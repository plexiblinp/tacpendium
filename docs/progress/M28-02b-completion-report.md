# M28-02b 完了報告: ゲーム更新の影響コンボを見せる画面（**設計まで**）

| 項目 | 内容 |
|------|------|
| 作業 ID | **M28-02b** |
| 指示書 | `docs/instructions/M28-02b-game-update-impact-screen.md` **v1.2.0** |
| チェックリスト | `docs/instructions/reviews/M28-02b-review-checklist.md` v1.0.0 |
| 実施日 | 2026-09-06 |
| ブランチ | `claude/m28-02b-implementation-8nnuz3` |
| 着手基点 | `25c9285` |
| 消費マイグレ | **0 本**（★次に払い出すのは `000106` のまま。動かしていない） |
| 消費 CHANGE | **0 本**（★起票は設計卓＝`D-293`。§6 に請求内容を列挙した） |
| 実装 | **★★0 行。** `web/` ／ `internal/` ／ `migrations/` ／ `docs/design/` に差分なし |
| テスト | **★実施しない**（指示書 §5 は「実装まで進んだ場合のみ」）。理由と、実装時の確かめ方は §9 |

---

## 0. 変更統計（着手基点 `25c9285`）

**★実際に `git diff --stat 25c9285` を走らせた出力の写しである**（**第 2 版・追補まで完了した時点**。★行数は本ファイル自身を含むため、写した時点を明記する）:

```
 .../20260906-m28-02b-design-exceptions.md          | 275 +++++++++
 docs/progress/M28-02b-completion-report.md         | 579 +++++++++++++++++++
 docs/progress/M28-02b-mock/index.html              | 615 +++++++++++++++++++++
 docs/progress/m28-02b-review.md                    | 312 +++++++++++
 docs/progress/progress-log.md                      |  25 +
 5 files changed, 1806 insertions(+)
```

**★新規ファイルはいずれも `+` だけであり deletions が 0 である**（教訓 `E-225` の確認。**deletions のある新規ファイルはそれ自体が矛盾している**）。
書く前に `ls` / `git status` で同名の不在を実査した——**`docs/progress/M28-02b-mock/` ／ 本ファイル ／ `docs/progress/m28-02b-review.md` はいずれも着手時点で存在しなかった。**

> **★★初版の本節は手書きだった**（`progress-log.md` の行を、まだ追記していないのに載せていた）。**レビュー高-1 で指摘され、実測の写しへ置き換えた。⇒ 変更統計を手で書くと、やっていないことが「やった」として記録に残る。**
> **★★本節を 2 度直している。** 1 度目は手書きを実測の写しへ置き換えたとき、2 度目は Phase D の追記とレビュー報告書が加わって数字が動いたときである。**⇒ 変更統計は「いつの時点か」を書かないと、正しく写しても後から嘘になる。**

---

## 1. ★★本サブは「設計まで」で完了している

**開発者の逐語（`D-668`）＝「Phase4 での必達はバックエンド、DB 構造などの決定。画面などのユーザーが触れる部分はイメージ（設計）まで出来れば実装は後でも良い」。**

| # | 必達成果物（指示書 §0.1） | 本報告での所在 |
|---|---|---|
| **1** | **モック HTML** | **`docs/progress/M28-02b-mock/index.html`**（**第 2 版**・単一ファイル・**外部依存 0 件**・JS 無し。`grep -ciE "https?://|<script|src="` が 0） |
| **2** | **画面設計の記述**（`DES-005` へ入れる文面の素） | **§4**（決めごと全数）＋ **§5**（幅 3 つの選択と理由） |
| **3** | **設計卓への CHANGE 請求** | **§6** |

**★Plan Mode は通した**（指示書 §0.1 の注記＝「『設計まで』は Plan Mode のことではない。2 つは別の軸である」）。**⇒ 計画を提示し承認を得たうえで、実装へ進まずに 3 つの成果物を出す形＝本サブの既定どおりである。**

### 1.1 ★実装へ進まなかった理由（**2 本立てで書く**）

**★「§2.9 の条件が欠けた」だけでは不正確なので分けて書く。**

| # | 内容 | 判定 |
|---|---|---|
| **1** | **§2.9-1「モックが開発者に承認されていること」** | **★未充足。** 本報告と同時にモックを初めて提出した段階であり、承認はこれからである。**⇒ 実装へ進まない直接の理由はこれ 1 点である** |
| 2 | §2.9-2「`M28-02a` が完了し API が在ること」 | **充足**（`CHANGE-159`） |
| 3 | §2.9-3「§2.2-4 ／ §2.5-3 ／ §2.6-3 の 3 つに答が出ていること」 | **答は出した**（§5.1 / §5.2 / §4 の §2.6-3 行）。**★ただし開発者の承認は未取得** |
| **★別件** | **§2.3-4（どの技が変わったか）が現行 API では満たせない** | **★これは §2.9 の条件ではない。独立した事実として並記する**（根拠は §6.1）。**⇒ 仮にモックが承認されても、この請求が通るまで本機能の目的は達成できない** |

**★止めることは失敗ではない**（指示書 §0.1・チェックリスト §0.2）。

---

## 2. ★★調査で判明した最大の穴 — 主動線が PC で消える

**指示書にも設計卓の見立てにも無かった事実である。実査で出た。**

`web/src/App.tsx:33-34`:

```tsx
if (location.pathname === "/" && !isMobile) {
  return <Navigate to="/combos" replace />;
}
```

`useIsMobile` の閾値は `(max-width: 639px)`（`web/src/hooks/useIsMobile.ts:3`）。
**⇒ 640px 以上では `HomePage` が一度も描画されない。**

そして既存の移行告知バナー `DataMigrationBanner` の**唯一のマウント先は `HomePage.tsx:31`** である。

| 帰結 | 内容 |
|---|---|
| **★1** | **いまの移行告知（`M28-01`・`CHANGE-156`）は PC で一度も出ていない。**〔**★本サブ由来ではない既存の欠陥である。⇒ 直していない。§8-1 で返す**〕 |
| **★★2** | **指示書 §2.2-2 は「押すと (b) へ飛ぶ。★これが主動線である」と書く。⇒ 「移行告知と同型」を素直に採ると、本機能の主動線が PC で丸ごと消える** |

**⇒ 決めた: 告知は `HomePage` ＋ `ComboListPage` の 2 か所に出す。**

| 案 | 判定 |
|---|---|
| (i) `AppRouter` の外（全画面に出す） | **採らない。** 「しょっちゅう使う機能ではない」（開発者の逐語）に対してうるさすぎる |
| **(ii) `HomePage` ＋ `ComboListPage`** | **★採る。** PC の実質的な入口は `/combos` である（上のリダイレクト先そのもの）。スマホは `HomePage` で受ける |
| (iii) `ComboListPage` だけ | **採らない。** 一覧を開かないと告知が来ない。**`M28-01` の告知が解こうとした「開かなければ誰にも伝わらない」が再発する** |

**★モック面 1 では、スマホ版と PC 版の 2 枚を並べてこの穴を可視化してある**（開発者が形として判断できる唯一の場所であるため）。

---

## 3. ★語彙 — 「要確認」は既に別の意味で使われていた（当初案を差し替えた）

**当初は「要確認」1 語で統一する案だった。`web/src/locales/ja.json` を grep して 0 件だったため衝突なしと判断しかけたが、実物を当たると衝突していた——ハードコードだったためである。**

| 所在 | 既存の「要確認」の意味 |
|---|---|
| `web/src/pages/ComboImportPage.tsx:329` / `:426` | `<TableHead>要確認 / エラー</TableHead>`（取込プレビューの列見出し） |
| `internal/service/movewarning` パッケージ全体 ／ `web/src/constants/move-warning.ts:1` | **「moves の要確認コード」**。技編集グリッドが `bg-amber-50` ＋ バッジで表示している |

**★既存＝「行のデータそのものが怪しい」。本サブ＝「データは正しいがゲームが変わった」。意味が違う。**

**★★既存語に別の意味で相乗りするのは、語を割るのと同じだけ後段の仕事を増やす**——`M29-01`（語彙統一）は「2 つを分離する」仕事を背負うことになる。指示書 §2.7-4 の狙い〔語を割ると `M29-01` の仕事を増やす〕は、**相乗りにも同じだけ当たる。**

**⇒ 採る語＝「更新未確認」。** 4 面すべてで 1 語。**「影響可能性あり」と「未確認」を別語にしない**（§2.7-4）。
**★★2026-09-06 に開発者が「更新未確認」を採用して決着した**（対案「更新の確認待ち」は不採用。モックからも落とした）。

**★差し替え先にも同じ衝突検査を当てた**（**★これを記録しないと、本サブの教訓「`ja.json` の grep だけでは足りない」が効かない**＝レビュー低-1）:

| 検査した先 | 「未確認」／「更新未確認」／「更新の確認待ち」 |
|---|---|
| `web/src/locales/ja.json` ／ `en.json` | **0 件** |
| `web/src/`（`*.test.*` を除く本番コード。**★ハードコードを拾うため**） | **0 件** |
| `internal/`（`*_test.go` を除く） | **0 件** |

**⇒ 衝突なし。** 〔**★「要確認」を見落とした原因は、`ja.json` だけを見たことである。実物はハードコードで存在していた。⇒ 差し替え先には本番コード全体を当てた。**〕

| 面 | 語 |
|---|---|
| 起動時告知 | 「ゲームの更新後、**更新未確認**のコンボが N 件あります」 |
| 一覧のボタン | 「⚠ **更新未確認** N 件」 |
| 専用画面 | 見出し「**更新未確認**のコンボ」／各行のバッジ「⚠ **更新未確認**」 |
| コンボ詳細 | 「前提バージョン: 2026.08.03.01（⚠ **更新未確認**）」 |
| 操作 | 「**問題なし**」（**★★2026-09-06 開発者確定。初版の「確認した」から変更した**。⇒ §5.4） |

**★守ったこと**: 語を「破綻」にしない（`FR307` 整合。「このコンボは繋がらなくなりました」と書かない）／ **色だけで表さない**（記号 ⚠ と語を必ず併記）。

---

## 4. 画面設計の記述（`DES-005` へ入れる素）— 決めごと全数

**★「こういう画面にします」ではなく「何を出し、何を出さないか」で書く。**

| 節 | 決めたこと |
|---|---|
| **§2.2-1** 告知の条件 | 更新未確認が 1 件以上あるときだけ出す。**件数と現在のゲームバージョンを出す** |
| **§2.2-2** 主動線 | 押すと `/game-update/combos` へ飛ぶ |
| **★マウント先** | **`HomePage` ＋ `ComboListPage` の 2 か所**（§2） |
| **§2.2-3** 延期 | 「次のバージョンアップまで表示しない」チェックボックス。**★「消す」ではなく「次の版まで延期する」。⇒ 次の版が来たらまた出る** |
| **§2.2-4** 延期の保存先 | **サーバ側のファイル**（選択理由は §5.1） |
| **§2.2-5** 延期の取り消し | **持たない**（移行告知と同じ）。**★袋小路にならない**——専用画面という別の入口が常に在り、しかも**一覧のボタンは延期と無関係に出る**。**⇒ 移行告知（取り消せず入口も 1 つ）との決定的な違いはここである** |
| **§2.2-6** 判定に失敗したとき | **黙って出さないことにしない。★★バナーの位置にも、一覧のボタンの位置にも、失敗として出す**（モック 1-c / 2-c）。**★「一覧のボタン位置にだけ出す」にはしない**——**スマホの入口はホーム画面（バナー）しか無いため、バナー側で黙るとコンボ一覧を開くまで失敗に気づけない**。これは §2.2-6 と危険 2 が名指しした罠の再発そのものである〔**★レビュー高-4 で指摘され訂正した。初版は「バナーは出さず、一覧のボタン位置に出す」と書いていた**〕。**★★「1 か所」なのは出どころ（取りに行く経路）であって出し先ではない**——経路は 1 本にまとめる（件数の出どころを 2 つに割らないため）が、**出し先は 2 か所である** |
| **§2.3-1** 一覧の再利用 | **★既存の `ComboTable` / `ComboTableRow` を「表そのものとして」再利用する**（取得・絞り込み・ソートだけでなく**行の描画も**。指示書 §2.3-1 が「フィルタ・ソート・**行の描画**を持つ」と名指ししているため）。**新しい一覧の仕組みを作らない。**〔**★レビュー高-3 で「報告は再利用と書きモックは別物の表を描いている」と指摘され、モック面 3 を再利用の形へ描き直した**〕 |
| **★足す 3 列** | **★専用画面のときだけ `ComboTable` へ渡す列とする**〔変わった技 ／ 前提バージョン ／ 操作〕。**★★表示列カスタマイズの対象にしない。⇒ `COLUMN_DEFINITIONS` ／ `ColumnVisibility` 型 ／ 保存キー `combo-list-columns-v1` に触れない**——触るとコンボ一覧とマイコンボの列設定にまで影響が出る（`web/CLAUDE.md` §1 の台帳も動く） |
| **§2.3-2** ルート | **`/game-update/combos`**。**★先例＝`/trash/combos/:id`**（接頭辞ディレクトリ ＋ 複数形リソース）。`/punish/list` `/import/combo/helper` とも整合。**★`/combos/affected` は採らない。理由＝この画面はコンボ一覧の別名ではなく独立した面だからである**——`/combos/*` の下はコンボという 1 件の資源を操作する経路（詳細・編集・複製）で埋まっており、「更新の影響を横断で見る」面はそこに属さない。**⇒ 機能グループの接頭辞ディレクトリを立てる流儀に合わせた**〔**★★当初は「ルータの宣言順で `/combos/:id` に食われる」と書いていたが、これは事実として誤りだったので取り下げた**（レビュー高-2）。**`react-router-dom` `^6.30.6` の `<Routes>` は宣言順ではなく specificity で照合する**——実装（`@remix-run/router@1.23.3` の `rankRouteBranches`）を実測すると `staticSegmentValue = 10` / `dynamicSegmentValue = 3` であり、静的セグメント `affected` は `:id` より高くランクされる。**同じ仕組みで `/combos/new` が既に共存している。⇒ 宣言順を入れ替えても何も変わらない**〕 |
| **§2.3-3** 軸を足さない | **既存の一覧にフィルタ軸を足さない**（開発者の逐語＝「一覧に常に 1 軸増えると邪魔」）。専用画面が `affected_by_game_update=true` を内部で固定する。**⇒ 一覧に増えるのはボタン 1 個だけである** |
| **§2.3-4** どの技が変わったか | **チップで出す**（§6.1 の請求が前提。**★モック面 3 に「サーバ側の追加が必要」と注記した**） |
| **§2.3-5** 複数の技で当たるとき | **全部出す**（案 ア）。**件数だけにしない**——「影響がある」とだけ出しても直せない（`D-729`）。**★3 件を超えたら「他 N 件」で畳む案 イ もモックに並べた** |
| **§2.3-6** 空の状態 | **文面を 2 通りに分ける**——初回「更新未確認のコンボはありません」／全部確認し終えた後「すべて確認済みです」。**★初回は必ずここに来る**（§9-1） |
| **★キャラのスコープ** | **★告知・ボタン・専用画面をすべてキャラ横断で統一する。** 理由＝コンボ一覧は**必ずキャラを 1 体選んでいる**（`ComboListPage.tsx:51-59`）が、件数はアプリ全体の合計である。**⇒ ボタン文面に「全キャラ」を明記する**（モック 2-e の案 イ）。**★専用画面は「見出し ＋ 表」をキャラの数だけ縦に並べる**〔表の中にキャラ列を足さない ／ グループ見出し行という新しい概念を表部品へ持ち込まない〕——`COLUMN_DEFINITIONS` にキャラ列は無く、足すと共有部品・`ColumnVisibility` 型・localStorage 台帳 #1 まで波及する。**★キャラ名は既存のキャラ一覧から引く**（コンボの応答が持つのは `characterId` だけであり、キャラ名は入っていない＝レビュー低-2） |
| **§2.4-1** ボタンの条件 | 更新未確認が 1 件以上のときだけ。**常設しない。★延期中でも出す**（§5.1 の許容条件そのもの） |
| **★バナーとボタンが同時に出る状態** | **★★同時に出す。** PC で `/combos` を開いた既定の状態はこれであり、**利用者が最も長く見る状態である**（モック 2-d に 1 枚足した）。理由＝2 つは役割が違う〔**バナー＝気づかせる・延期できる** ／ **ボタン＝延期したあとも残る常設の入口**〕。**★「バナーが出ている間はボタンを隠す」は採らない**——**延期した瞬間にボタンが現れる形になり、「消したのに別のものが出てきた」と読まれる**。⇒ 延期のチェックを入れるとバナーだけが消え、ボタンは残る（**これが §2.2-5 の「袋小路にならない」の実体である**）〔**★レビュー中-3 で「合成状態がどちらの成果物にも無い」と指摘され、決めて足した**〕 |
| **§2.4-2** 件数 | 出す（告知と同じ形・全キャラ合計） |
| **§2.4-3** 取得失敗 | **★★0 件として扱わない。ボタンを消さず「件数を取得できませんでした（再試行）」を出す。** 先例が実在する——`combo-list-setup-count-hides-fetch-failure`。**現場は `ComboTableRow.tsx:167-176` の `{combo.setups?.length ?? 0}` と `ComboTable.tsx:158` の `canExpand`**（後者は列設定に関係なく全員に及ぶ）。**BE 側の握り潰しは `internal/api/combo/handler.go:349-353`** |
| **§2.4-4** マイコンボ | **出さない。** 理由＝マイコンボは「使用中／練習中／頻度低下」で絞った作業ビューであり、**「アプリ全体に未確認がある」という全体状態の入口を 2 つ置く意味が薄い**。開発者の逐語も「一覧に」だけを名指ししている。**★ただし行のバッジは出る**——`ComboTableRow` を一覧と共有しているため。**これは意図した挙動として書く**〔面の数え上げは「トグルを置いた面」ではなく「共通部品を使っている面」で行う＝`M24-03` の実測〕 |
| **§2.5-1** 置き場・粒度 | **専用画面の各行 ＋ コンボ詳細。1 件ずつ**（選択理由は §5.2）。**★語は「問題なし」**（§5.4）。**★★あわせて各行から［編集］へ行けること**（§5.5）。**★★「コンボ詳細」は開発者が示した 2 択〔一覧／新画面〕の外である。⇒ 1 つ増やした**〔理由＝§2.6-1 で詳細に「前提バージョン」を出すと決めた以上、**そこから直接確認できないと「印は見えるが操作は別画面にある」形になる**。⇒ 印と操作を同じ場所に置く。**★一覧には置いていない**——一覧に置くと §2.3-3（軸を常設しない）に近づくため〕〔**★レビュー中-2 で「幅の外へ出たことが書かれていない」と指摘され明記した**〕 |
| **§2.5-4** 押した直後 | **★行を消さず、印を「⚠ 更新未確認」→「✓ 確認済み」へ変えて残す**（消すと「いま何をしたか」が見えない）。件数だけ即座に減らす |
| **★再取得したときの見え方** | **★★既定ソートは更新日時の降順**（`constants/combo-list.ts` の `DEFAULT_SORT_FIELD = "updated_at"` / `DEFAULT_SORT_ORDER = "desc"`）**であり、`acknowledge-version` は `updated_at` を進める**（`CHANGE-159` §1.1-5）。**⇒ 素直に `invalidateQueries` すると、再取得の瞬間に専用画面から行が消え、コンボ一覧では先頭へ飛ぶ。⇒ 押下時はキャッシュ内の当該 1 件だけを楽観更新し、`invalidate` は画面を離れるまで遅らせる。★`queryKey` の正本は `web/src/lib/query-keys.ts` であり、前方一致依存が実測 39/58 なので狙ったキーだけに当てる**（`query-keys.invalidation.test.ts` の表への追記も要る） |
| **★編集して保存したときの挙動** | **★★実査した。2 経路に分かれる**（`ComboEditor.tsx:449-471` の `hasKeyChanges()` 分岐）。**(a) レシピ・始動技・状況など重複判定キー 7 項目**（`utils.ts:254-285`）**を直す → `PUT`**（`runPut` :722-735）**⇒ 旧行を論理削除して新しい行を INSERT する**（`service.go:840-871`）**ため `insertComboSQL` の `COALESCE(?, (SELECT current_data_version …))`**（`repository.go:486`）**が効き、基準が自動で最新になる。⇒ 専用画面から消える。** **(b) メモ・ダメージ・タグなど → `PATCH`**（`runPatch` :709-720）**⇒ `UpdateMetadata` の SET 句に `baseline_version` が無い**（`repository.go:1104-1168`）**ため据え置き。⇒ 残る。** **★★この非対称のままでよい（2026-09-06 開発者確定）**——**「直す」＝レシピを直すことなので消える。メモを 1 文字直しただけで警告が消えないのは `FR307`（アプリが勝手に無事と断定しない）と整合する。⇒ 返却事項 6 はこれで決着した**（§8-6） |
| **★保存後の戻り先** | **★★専用画面へ戻す**（2026-09-06 開発者確定）。**⇒ 出発点を `location.state` で編集画面へ渡し、保存後の遷移先に使う**（既存の `fromSave` と同じ仕組みに 1 値足す形）。**★★履歴の形は 1 枚も変えないこと**——`M24-12` (3) が「戻る 1 回で編集画面、2 回目でその前へ抜ける」を契約として押さえており、`NavigationGuardProvider.tsx:255-305` が `history.go` の勘定を持っている。**★副次的な利点＝`PUT` は 201 Created で id が変わる**（`handler.go:470`）**ため、「元のコンボの詳細へ戻る」形だと id の差し替えが要る。専用画面へ戻す形ならその問題が起きない** |
| **§2.5-5** 楽観排他 | **★競合の分岐を画面に作らない。** `acknowledge-version` は `version` を受けず 409 を持たない（`CHANGE-159` §1.1 の「持たせなかった分岐 2」＝基準の前進はコンボの内容の変更ではなく「利用者が見た」ことの記録であるため）。**★実査は不要になった**（指示書 §0.5-2）。**★ただし 404 は起きうる**——画面を開いている間に別の端末が削除した場合。**⇒ 文面「このコンボはゴミ箱に移動されています」を決めた**（§0.5-3 の「空の状態と 404 の出し分け」への答え。**空の状態は §2.3-6 の 2 文面、404 はこの 1 文面**） |
| **§2.6-1** 詳細の 1 行 | 「前提バージョン: 2026.08.03.01（現在 2026.09.10.01）」＋ 該当時のみ ⚠ とバッジ ＋「確認した」 |
| **§2.6-2** **出る面の実査** | **★出る面は 1 つではなく 2 つである。** `ComboDetailMetadata.tsx` が **コンボ詳細**（`ComboDetailPage.tsx:199`）と **ゴミ箱のコンボ詳細**（`TrashComboDetailPage.tsx:192`）の両方から呼ばれる。**★`drive_damage` の先例が 3 面だったのは、整形関数を `features/combo/utils.ts:188-227` に置いて `combo-io/export-model.ts:155-168` からも呼んでいるからである。⇒ 本件が 2 面で止まるのは「エクスポートへ足さない」という選択の結果であり、数の違いに理由が付く**（`CompareTable` は `drive_damage` の符号整形を通しておらず 4 面目ではない） |
| **§2.6-3** エクスポート | **★出さない**（`baselineVersion` もバッジも）。`CHANGE-159` §6 が「`baseline_version` は CSV に載せない（DB 管理列であって利用者の入力ではない）」と決めており、出すと CSV の列契約（`DES-006`）に波及して本サブの射程を大きく超える。**★エクスポートは `combo-io/export-items.ts` の項目台帳を通る別経路であり、足さなければ出ない** |
| **★ゴミ箱詳細** | **バッジは出す。ただし「確認した」ボタンは出さず「ゴミ箱の項目は確認できません。復元すると確認できます。」と添える。** 理由＝判定式は `deleted_at` を見ないので `affectedByGameUpdate: true` はゴミ箱の行にも載る一方、`acknowledge-version` の母集団は `WHERE id = ? AND deleted_at IS NULL` で **404 になる**（`CHANGE-159` §1.1）。**★ボタンだけ黙って消すと「確認できないのに警告だけ出る」形になる。⇒ 情報を隠さず理由を書く方が §2.4-3 の思想に沿う** |
| **§2.6-4** 未設定のコンボ | **★実査済み。抜ける経路は無い**（下記 §4.1）。**それでも表示は「不明」＋ 更新未確認のバッジとする**（下記の理由） |
| **★`affectedByGameUpdate` の欠落** | **★★FE の型では必須（`boolean`・`?` を付けない）とし、`?? false` を書かない。** 理由＝`CHANGE-159` §1.3 が `omitempty` を付けなかったのは「**キーが無い＝判定していない**」を `false` と区別するためであり、**FE 側で `?? false` を書くとその区別が 1 段下で潰れる**。**★先例が同じ形をしている**——`ComboTableRow.tsx:167-176` の `{combo.setups?.length ?? 0}` と `ComboTable.tsx:158` の `canExpand`。**⇒ 現行の `web/src/features/combo/types.ts` は 3 分岐とも `affectedByGameUpdate?: boolean`（任意）であり、必須へ直す必要がある**〔**★`ComboSummary` / `ComboDetail` / `Combo` の 3 つすべて**＝`web/CLAUDE.md` §2〕。**⇒ §6.4 の請求に含めた**〔**★レビュー中-4**〕 |

### 4.1 §2.6-4 の実査結果 —— `materialize` 経由でも基準は抜けない

**指示書 §2.6-4 は「`materialize` 経由の生成コンボに入らない経路が残っている可能性がある（`M28-02a` §2.3-5）」を確かめよと求めている。⇒ 確かめた。抜けない。**

| # | 実査した事実 |
|---|---|
| 1 | **本番コードの `INSERT INTO combos` は 1 本しか無い**（`internal/repository/combo/repository.go:456`。他のヒットはすべて `*_test.go`） |
| 2 | **その 1 本が `baseline_version` を `COALESCE(?, (SELECT current_data_version FROM games WHERE code = 'sf6'))` で入れている**。⇒ 呼び出し側が nil を渡せば構造的に埋まる |
| 3 | **`games.current_data_version` は `NOT NULL DEFAULT '2026.08.03.01'`**（`migrations/000104_add_game_update_tracking.up.sql:64`）。⇒ 副問い合わせが NULL を返す状況が無い |
| 4 | **製造側も同じ理由を明記している**（`repository.go` の `insertComboSQL` 上のコメント＝「`materialize-bypasses-required-fields` の轍を踏まないよう、サービス層ではなく INSERT に置いた」） |

**⇒ `M28-02a` §2.3-5 の懸念は解消している。**

**★それでも「不明」の表示を用意する理由**——`baselineVersion` は `omitempty` 付きであり、**NULL ならキーごと消える**。そして `CHANGE-159` §2.1 は「**基準が NULL ＝影響可能性あり（安全側）**」と定める。**⇒ 「—」と出すと「不明だから更新未確認として出ている」という因果が画面から消える。**〔**★これは `affectedByGameUpdate` に `omitempty` を付けなかった理由（「キーが無い」＝「判定していない」と `false` を区別する）と同じ向きの配慮である。**〕

---

## 5. 埋めた「幅」3 つ — 選んだ案と理由

**★`D-742` の決着には幅が 3 つ残っていた。⇒ 埋めるのが本サブの仕事であり、埋めた理由を書くことが成果物である。**

### 5.1 幅1: 告知の「延期した」の保存先（§2.2-4）

**採った案＝(i) サーバ側のファイル。**
`filepath.Dir(dbPath)` 直下（＝移行告知 `.migration-notice.json` と同じ場所。`cmd/tacpendium/main.go:512` が `noticehandler.NewHandler(filepath.Dir(dbPath))` を渡している実物に合わせた表現）へ `.game-update-notice.json` を置き、`{"postponedForVersion": "2026.09.10.01", "at": ...}` を持つ。
**★記録するのは「延期した時点の現在版」である**（1 世代前の版ではない。モックの各面が前提にしている現在版と同じ値になる＝レビュー低-4）。
**`games.current_data_version` と一致する間だけバナーを抑止する。**

| # | 理由 |
|---|---|
| 1 | **「消す」ではなく「次の版まで延期」という意味そのものが、版の文字列を保存する形に一致する。** 次の版が来ると値が食い違い、**何もしなくても再表示される**（真偽フラグ＋手動クリアだと消し忘れが起きる） |
| **2** | **★指示書が示した判断材料＝「延期は端末ごとか、アプリ全体か」。** **★★コンボは利用者ごとの所有物ではない**〔`combos` に `user_id` が無い。所有者を `user_id` で持つのは `tags` であり、コンボは `combo_tags` → `tags.user_id` を辿る形。サービス層の `UserID` にも「**誰として保存するか**。コンボ本体の所有者ではない」とコメントが在る〕**。⇒ 影響コンボの集合はアプリ全体で 1 つであり、その告知の延期もアプリ全体が素直である。** 端末ごとにすると「同じ DB を見ている 2 人が別の件数の告知を見る」形になり、そちらの方が不自然である |
| 3 | **ブラウザストレージ案 (ii) は台帳（`web/CLAUDE.md` §1）への追記請求が先に要る**（`CLAUDE.md` §10.X。**未記載キーは `scripts/check-browser-storage-keys.sh` が赤にする**＝指示書 §4 危険 7）**。しかも端末ごとに割れて上の 2 と噛み合わない** |
| 4 | **DB 列案 (iii) はマイグレを消費する**（次の払い出しは `000106`。`D-293` により自採番不可＝止めて請求する事項）。**版の文字列 1 個のためにスキーマを動かす理由が無い** |
| 5 | **移行告知の作法をそのまま使える**（tmp+rename の原子的書き込み ／ **壊れた JSON は「無し」に倒す**＝`internal/infra/datadir/notice.go` の `ReadNotice`）。**★後者は本サブでも安全側に働く——ファイルが壊れたら「延期していない」＝告知が出る**（§2.2-6 の「黙って出さないことにしない」と整合） |

**★★LAN 共有で 1 人の延期が全員に効くことを許容する。★ただし条件が 1 つある——延期が抑止するのは「バナーだけ」であり、一覧のボタンは抑止しない。**

理由＝A さんが延期しても B さんはボタンから専用画面へ行ける。**これは指示書 §2.2-5 が用意した論法（別の入口があるから袋小路にならない）と同じ形をそのまま使ったものである。**
**★逆に、延期がボタンまで消す設計にすると LAN では許容できない。ここが分水嶺なので、`DES-005` の文面へ 1 行として残すこと。**

**★他の選択肢も検討して落とした**:

| 案 | 落とした理由 |
|---|---|
| (iv) セッション中だけ保持 | 「次のバージョンアップまで」という逐語を満たさない |
| (v) `.migration-notice.json` へ相乗り | 意味の違う 2 つの告知を 1 ファイルに混ぜることになる。**`UpsertNotice` の「解決済みは 1 回だけ／未解決は毎起動で上書き」という規則が、本サブの「版ごと」と噛み合わない** |
| (vi) `config.toml`（`PUT /api/config`） | config はアプリ設定であって告知状態ではない。**`restartRequired` の判定にも絡む** |

### 5.2 幅2: 「確認した」の粒度と取り消し（§2.5-3）

**採った案＝一括を出さない。1 件ずつだけ**（指示書 §2.5-3 の候補 (c)）。**置き場＝専用画面の各行 ＋ コンボ詳細。**

| # | 理由 |
|---|---|
| 1 | **`M28-02a` は 1 件ずつの経路しか作っていない**（`CHANGE-159` §1.1 の「持たせなかった分岐 3」＝「一括操作は無い。1 件ずつだけである」）。**⇒ 一括を採るなら経路の追加請求が要る。★請求せずに一括を描くことはしていない** |
| **2** | **★★この論点の中身は「一括で誤爆したときに戻せないこと」である**（指示書 §2.5-3）。**基準を進めると進める前の値がどこにも残らない。⇒ 一括を出さなければ論点そのものが消える** |
| 3 | **本機能の目的は「見せて直しやすくする」ことである**（`D-729`）。**一括で全部確認済みにする操作は目的の逆**——見ずに消す動線になる |
| 4 | **頻度が低い**（SF6 のバランス調整は年に数回＝`CHANGE-159` §7.2）。1 件ずつでも運用が破綻しない |

**★逐語の幅の中に収まっている**——開発者の逐語「一括、または、1 つずつ」は選択肢の提示であり、指示書 §2.5-1 が「幅の中から選び、理由を書くこと」、§2.5-3 が候補 (c)「一括を出さない。1 つずつだけにする」を明文で挙げている。**⇒ これは問いへの差し戻しではなく、幅を埋めた形である。**

**★取り消しは持たない。** 理由＝候補 (b)「直前の基準を 1 世代だけ持つ」は列を足すためマイグレを消費し、本サブの射程を超える（設計卓の見立ても同じ＝§7-2）。**誤爆の被害は 1 件に限られ、しかも押した行を見ている最中の操作である。** 緩和として §4 の「押しても行を消さない」を採った。

**★ただし「押しやすさの実害」は取り消しとは別軸として認める**——マーカーは技単位であり、1 回のバランス調整で複数の技が動く。**1 キャラのコンボが数十件まとめて当たる形は普通に起きる。⇒ 「実運用で耐えられない件数が出たら一括経路の追加を請求する」を §8-5 に残した**（いま出さないことと、将来も出さないことは別である）。

#### 5.2.1 ★★「確定反撃の不採用に近いイメージ」の読み方（**指示書 §2.5-2 / 危険 3**）

**逐語は操作感の話であって永続性の話ではない**（`M-128`＝短い逐語の意味が 1 つに定まると思わない）。**⇒ 「一時的に隠すだけでよい」とは読んでいない。**

**★as-built の「不採用」を実査した。実物は 2 つあり、本サブの「確認した」はどちらとも違う。**

| # | 実物 | 性質 |
|---|---|---|
| (i) | **一時非表示**（`PunishTree.tsx:141-149` の `hideStarter` / `hideCombo` / `restoreAll`） | **クライアント state。リロードで戻る。★ボタンに「この端末のみ・画面更新で戻ります」と添えてある** |
| (ii) | **`adopted` / `unreachable` トグル**（`PunishTree.tsx:151-174` の `toggleVerdict`） | **DB へ書く。★同じ verdict を再クリックで未検証へ戻せる**（`deleteVerdict`） |
| **本サブ** | **「確認した」** | **DB へ書き、しかも解除できない**（進める前の基準が残らない） |

**⇒ モックでは「一時的に隠す」形にしていない**（面 3-b＝押しても行は残り、印だけ「✓ 確認済み」に変わり、次の再取得で絞り込みから外れる）。

> **★実査で見つけた `DES-005` と as-built のずれ（本サブの判断には影響しないが事実として残す）。**
> **指示書 §2.5-2 は `DES-005` に沿って「(i) は §5.20 の探す側 ／ (ii) は §5.21 のマイリスト側」と書いているが、実装では (i) (ii) とも探す側 `/punish/search` の `PunishTree.tsx` に同居している。**
> マイリスト側（`/punish/list`）が持つのは **curation / pruning の永続 hide とその解除**（`PunishList.tsx` / `HiddenItemsPanel.tsx`）である。
> **⇒ 「2 つの不採用がどちらとも違う」という指示書の結論そのものは変わらない。**

### 5.3 幅3: 「影響可能性あり」の見せ方（§2.7）— **★★開発者の判断で決着した**

**★初版は 3 案（A＝行の先頭にバッジ ／ B＝行全体を淡く塗る ／ C＝専用画面にだけ状態列）をモックへ並べて開発者へ返した**（`D-742` の問い 4＝「決められないので製造時にモックで決定」）。

**⇒ 2026-09-06、開発者が 3 案とも採らないと判断した。逐語＝「専用画面なのだから、そもそもバッジも状態列もいらないと思うがどう思いますか？」**

**★★製造もこの判断が正しいと考える。⇒ 3 案を撤回し、印を出す面を数え直した。**

| 面 | 印を出すか | 理由 |
|---|---|---|
| **専用画面** | **★出さない** | **全行が該当するので冗長である。** 要る情報は「どれが該当か」ではなく **「どの技が変わったか」** であり、それは専用の列で出している |
| **コンボ一覧・マイコンボ** | **★出さない** | 開発者が「一覧に常に 1 軸増えると邪魔」と明確に否定している。**★気づく経路はバナーとボタンで足りる**（それが (c)＋(b) を選んだ理由そのものである） |
| **コンボ詳細** | **★★出す（ここだけ）** | **1 件だけを見ている画面であり、バナーもボタンも無い。** §2.6-1 で「基準バージョンを見せる」と決めた行に添えるのが素直 |

**★受け入れた代償**——**一覧を眺めていて個々のコンボの状態は分からない。** ⇒ 知りたくなったら詳細を開けば分かり、「何件あるか」はボタンが常に出している。

**★例外が 1 つだけある**——**「問題なし」を押した直後の「✓ 問題なしにしました」。これは状態の印ではなく操作結果の表示である**（押したことが見えないと困るため、次の再取得まで残す）。

> **★★撤回にあたって落としたもの**——3 案の比較そのものと、**レビュー中-1 で足した「3 案の波及表」**（一覧・マイコンボへの波及 ／ 列の台帳・保存キー ／ 列表示をオフにしたら印が消えるか）。**★モックからは消し、ここには「撤回した」という事実だけを残す**——**同じ案が再提案されたときに「一度否定済みである」と分かるようにするため。**
>
> **★★撤回した記述をそのまま残さないことは、本プロジェクトで「高」扱いの規律である**〔実装・判断が変わったのに旧の記述が残る形は、テストも lint も型検査も検出せず、後任が本文をコピーして下流へ複製する〕。

### 5.4 ★操作の語＝「問題なし」（**2026-09-06 開発者確定・初版から変更**）

**初版は「確認した」だった。⇒ 開発者の逐語＝「『確認した』ボタンには違和感。『問題なし』の方がしっくりくる」。**

| # | 製造としても「問題なし」が良いと考える理由 |
|---|---|
| 1 | **誰が何を判断したのかがはっきりする。** 「確認した」は行為だけを言うが、「問題なし」は**利用者が下した判断**を言う。**★判断するのは利用者でありアプリではない**——これは `FR307`（アプリは「影響可能性」を出すだけで破綻の自動断定はしない）の裏返しである |
| 2 | **「更新未確認」との対がそのまま成立する**（未確認 → 見て判断した → 問題なし） |

**★ヒント文を添える**——**「このコンボはゲームの更新後も問題ないと記録します」。** 理由＝**語だけだと「このコンボに問題は一切ない」とも読める。⇒ 文脈（ゲームの更新）を 1 行で固定する。**

**★衝突検査を実施した**（**★「要確認」で踏んだ轍を繰り返さないため、`ja.json` だけでなく本番コード全体に当てた**）:

| 検査した先 | 「問題なし」／「問題な」 |
|---|---|
| `web/src/locales/ja.json` ／ `en.json` | **0 件**（「問題」は 2 件あるが `dataMigration` の一文と `validationError` であり別文脈） |
| `web/src/` 本番コード（`*.test.*` を除く） | **1 件・コメントのみ**（`features/combo/types.ts:48`） |
| `internal/` 本番コード（`*_test.go` を除く） | **2 件・コメントのみ**（`validation/combo.go:123` に完全一致が 1 件あるが Go コメント内であり表示に出ない） |

**⇒ UI 表示文字列としては未使用。衝突なし。**

**★API の名前は変えない**（`POST /api/combos/:id/acknowledge-version`）。**⇒ 画面のラベルと API 名がずれるため、設計書側に「画面のラベルは『問題なし』である」と併記を請求する**（§6.5）。

### 5.5 ★★「影響があった場合」の導線が初版に無かった（**開発者の指摘**）

**開発者の逐語＝「また影響がある場合のボタンが抜けているように見える。影響があった場合は、専用画面⇒編集画面⇒編集＋問題なしに変えて？⇒専用画面（問題なくなったものは消える）となるイメージ」。**

**★★指摘のとおりである。初版のモックと報告は「問題なし」しか置いておらず、「直しに行く」導線が無かった。**
**⇒ `D-729` の逐語「どのコンボに影響があるかをユーザーに見せ<u>修正しやすいように</u>する事が今回の目的」の後半に応えていなかった。**

**⇒ 決めた流れは 2 本である。**

| どういう場合 | 流れ |
|---|---|
| **影響があった（直す）** | **専用画面 →［編集］→ 編集画面 → レシピを直して保存 → 専用画面へ戻る（その行は消えている）**。**★「問題なし」を押す必要は無い**——保存（`PUT`）が基準を最新にするため |
| **影響が無かった（見て平気）** | **専用画面 →［問題なし］→「✓ 問題なしにしました」→ 次の再取得で消える** |

**★★［編集］は新しく作らない。** `ComboTableRow.tsx:186-212` の操作セルが**無条件で**「詳細 ／ 編集（`/combos/:id/edit`） ／ コピー ／ 削除」を出しており、**出す・出さないを切り替える props は無い**。**⇒ 専用画面が `ComboTable` を再利用する以上、［編集］は自動的に付いてくる。足すのは［問題なし］1 つだけである。**

**★これはレビュー高-3 で決めた「`ComboTable` を再利用する」を、さらに強く支持する事実である**（再利用しない設計を採っていたら、この導線を自分で作り直すことになっていた）。

## 6. 設計卓への CHANGE 請求（§2.8）

**★製造は `docs/design/` を編集していない**（`CLAUDE.md` §8）。**★CHANGE 番号もマイグレ連番も自採番していない**（`D-293`）。

### 6.1 ★★最重要: `ComboResponse` へ「変わった技」の欄を足す請求

**指示書 §2.3-4 は「1 行に『どの技が変わったか』を出すこと。★★これが本機能の目的である」と書いている。⇒ 現行 API では原理的に取れない。実物で二重に塞がっている。**

| # | 実査した事実 |
|---|---|
| **1** | **`GET /api/moves` からマーカーは取れない。** `internal/model/move.go:79` に `LastChangedGameVersion *string` は在るが、**API は DTO へ詰め替えており `internal/api/move/dto.go` の応答（一覧・詳細とも）にこの欄が無い** |
| **2** | **`games.current_data_version` を返す HTTP 経路が 1 本も無い**（ルート登録に `games` が無く、`GET /api/config` にも含まれない）。**⇒ 比較の片側が FE に存在しない** |
| 3 | **`ComboSummary` に `steps` が無い**（`steps?` を持つのは `ComboDetail` だけ＝`web/src/features/combo/types.ts:124-128`）。**⇒ 一覧応答からステップの `moveId` を辿れない** |
| 4 | **行ごとに `GET /api/combos/:id` を引く代替も成立しない**——`ComboStep` が持つのは `moveId` / `moveCode` / `modifiers` だけであり、**N 回引いても答が入っていない**（N+1 の代償を払う以前の問題） |
| **5** | **仮に取れても FE で自前比較するのは §0.3 違反。** 判定条件を画面側で書き直すと 2 か所に割れて静かにずれる。**しかも 2 種類の NULL の向き**（`CHANGE-159` §2.1＝基準 NULL は「影響あり」／マーカー NULL は「影響なし」で**向きが逆**）**を画面側で再現することになり、これは設計卓が「設計書に書くことが唯一の防御である」と書いた当のものである** |

**⇒ 請求内容**:

| # | 内容 |
|---|---|
| 1 | **`ComboResponse` へ `affectedMoves`（`[{moveId, code, nameJa, lastChangedGameVersion}]`）を足す。** 判定式（`CHANGE-159` §2 の `EXISTS`）と**同じ 1 か所から導出**させる |
| 2 | **一覧の N 件ぶんはバッチ取得で作る。★先例が在る**——`internal/api/combo/handler.go:344` の `ListSetupsByComboIDs` による N+1 回避 |
| 3 | **★`nameJa` は NULL 可**（`MoveResponse.NameJa *string`）。**⇒ 引けないときは `code` へ落ちる**作法を明記する（先例＝`starterMoveNameJa` の `types.ts:110-112` のコメント） |
| **4** | **★★`affectedMoves.length > 0` で判定させないこと。** 画面は `affectedByGameUpdate` を見る。**長さで判定すると真偽が 2 か所で表せてしまい、それ自体が §0.3 の再発である** |

**★★この請求が通らないと本機能の目的が達成できない。** 専用画面は「影響がある」としか言えず、利用者は直せない。

### 6.2 告知の経路（§2.2 / §2.4 の土台）

**請求＝`GET /api/notices/game-update`（`{currentDataVersion, affectedCount, postponedForVersion?}` を**常に 200 で**返す）／ `POST /api/notices/game-update/postpone`。**

| # | 内容 |
|---|---|
| **1** | **★204 の作法から外れることを承知で外す。** 指示書 §0.4 の逐語「流用できるのはバナーの形と **204 で『無い』を表す作法まで**である」は**上限の宣言**であって「必ず流用しろ」ではない。**しかも本経路が返すのは「有無」ではなく件数と現在版であり、204 では表現できない**。**★一覧のボタンは延期中でも件数が要るため、告知の有無と件数を同じ 1 本から取ることで出どころを 1 つに保つ** |
| **2** | **★代替（`GET /api/combos?affected_by_game_update=true&limit=1` の `count`）は原理的に不可能である**——`handler.go:383` が `Count: len(items)`（そのページの件数）であり、`repository.go` が `limit <= 0 → 100` / `limit > 1000 → 1000` に丸める。**⇒ 総数は取れない。この代替の検討は打ち切ってよい** |
| **3** | **★判定式を共有した `COUNT` の新設が要る。★式を書き写さないこと**——現行 repository は `LIMIT` 付きの `SELECT` しか持たない。**`CHANGE-159` §2 が「判定式は 1 か所しかなく、SELECT の派生列と一覧の WHERE が同じ式を共有する」と明言しており、ここを崩すと §0.3 の再発になる** |
| **4** | **★`games.current_data_version` の初の HTTP 露出である**（独立した請求項目として書くこと）。副産物として、コンボ詳細に「基準 2026.08.03.01（現在 2026.09.10.01）」と出せるようになる |
| **5** | **★`POST .../postpone` は要求本文を取らない。** サーバが `games.current_data_version` を読んで書く。**`acknowledge-version` が「版数をクライアントから受けない ⇒ 任意の版へ進めたことにできない」を明示的に選んだ**（`CHANGE-159` §1.1）**のと同型にする** |
| 6 | **★語を `/ack` ではなく `/postpone` にする**（「永久に既読」と「次の版まで延期」は意味が違う） |
| 7 | **★1 本にまとめる代償を明記する**——落ちると告知とボタンが同時に失敗表示になる。**許容する**（再試行 UI が 1 か所で済む利点の裏側） |

### 6.4 `affectedByGameUpdate` を FE 型で必須にする（**★レビュー中-4**）

**現行の `web/src/features/combo/types.ts` は 3 分岐とも `affectedByGameUpdate?: boolean`（任意）である。⇒ 必須（`boolean`）へ直す。**

| # | 内容 |
|---|---|
| 1 | **`ComboSummary` / `ComboDetail` / `Combo` の 3 つすべてを直す**（`web/CLAUDE.md` §2＝「バックエンド `ComboResponse` は同一型。フィールド追加時は該当するすべてに追加すること」） |
| **2** | **★★画面側で `?? false` を書かない。** `CHANGE-159` §1.3 が `omitempty` を付けなかったのは「キーが無い＝判定していない」を `false` と区別するためであり、**FE で既定値へ倒すとその区別が 1 段下で潰れる**（先例＝`ComboTableRow.tsx:167-176` の `?? 0`） |
| 3 | **`baselineVersion` は `omitempty` 付きのままでよい**（欠落＝「不明」であり、§4.1 のとおり表示で拾う） |

### 6.5 画面のラベルと導線（**★2026-09-06 の開発者フィードバックで確定した分**）

| # | 内容 | 宛先 |
|---|---|---|
| 1 | **画面のラベルは「問題なし」である旨を併記する**（API 名は `acknowledge-version` のまま。`CHANGE-159` §1.1 と `REQ-001` `FR702` の説明文が「確認した」で書かれている） | `REQ-001` ／ `DES-002` §4.2 |
| 2 | **専用画面の各行に［編集］が出ること**（既存の行アクションの再利用であり新設ではない）と、**「影響あり ⇒ 直す ⇒ 消える」の流れ** | `DES-005`（新節） |
| **3** | **★保存後の戻り先を出発点から決める**（専用画面から編集へ入った場合は専用画面へ戻す）。**★★履歴の形は変えないこと**（`M24-12` (3) の契約） | `DES-005`（新節 ＋ §5.7 コンボ登録・編集） |
| 4 | **レシピを直して保存すると基準が最新になる（`PUT`）／ メモだけ直しても進まない（`PATCH`）という非対称は、意図された仕様である**旨 | `REQ-001` `FR702` ／ `DES-002` §4.2 |

### 6.3 請求の宛先（**★絞り込んで書く**）

| 宛先 | 内容 |
|---|---|
| **`DES-002` §4.2** | 新経路 2 本（§6.2）／ `ComboResponse` へ `affectedMoves`（§6.1） |
| **`DES-005` §2 画面一覧** | **新画面 1 行**（画面名「ゲーム更新の影響コンボ」・`/game-update/combos`） |
| **`DES-005`（新節）** | 専用画面の定義（§4 の該当行がそのまま素になる） |
| **`DES-005` §5.4** | 一覧のボタン（**★フィルタ軸は足さないこと**を併記） |
| **`DES-005` §5.6** | コンボ詳細の 1 行（**★出る面は 2 つ**であることを併記） |
| **`REQ-001` `FR702`** | 画面側の as-built（語は 1 語。破綻の自動断定はしない）／ **画面のラベルは「問題なし」**／ **`PUT` と `PATCH` の非対称は意図された仕様である**（§6.5） |
| **`DES-005` §5.7** | **保存後の戻り先を出発点から決める**（§6.5-3） |
| **`DES-003`** | **★不要**（影響コンボを保存する表を作らない＝導出のまま） |
| **`DES-006`** | **★不要**（CSV に出さない＝列契約を動かさない） |
| **`web/CLAUDE.md` §1** | **★不要**（サーバ側ファイルを採ったため。ブラウザストレージを 1 キーも使わない） |
| **マイグレ** | **★消費 0 本**（次に払い出すのは `000106` のまま） |

---

## 7. やらないこと（§3）の遵守

| # | 禁止事項 | 本サブでの状態 |
|---|---|---|
| 1 | 判定条件を画面側で書き直すこと | **★書き直していない。** そもそも FE から判定できないことを §6.1 で示し、サーバ側の追加を請求した |
| 2 | 一覧に影響コンボの軸を常設すること | **★していない。** 一覧に増えるのはボタン 1 個だけであり、絞り込みは専用画面が内部で固定する |
| 3 | 影響コンボを保存するテーブルを作ること | **★していない**（`DES-003` への請求も出していない） |
| 4 | バージョン文字列の形を触ること | **★触っていない**（`YYYY.MM.DD.NN` のまま） |
| 5 | 「破綻した」と断定する文面を書くこと | **★書いていない。** モックの文面は「影響を受けている可能性があります」で統一 |
| 6 | `M29-01` の語彙統一を先取りすること | **★していない。** 本サブが作る 4 面の中で語を 1 つに保っただけである |
| 7 | CSV の列契約を動かすこと | **★動かしていない**（§2.6-3 で「出さない」を選んだ） |

---

## 8. ★開発者へ返す事項（指示書 §7 の 6 件 ＋ 追加 3 件）

**★答えられないものは「答えられなかった」と書く**（`M-128` の運用・完了条件 §6-4）。

| # | 事項 | 製造の答 |
|---|---|---|
| **1** | **「影響可能性あり」の見せ方**（§2.7） | **★★決着した（2026-09-06）。** 開発者の判断により **3 案とも撤回し、印はコンボ詳細にだけ出す**（§5.3）。**語は「更新未確認」で確定**（§3） |
| **2** | **「問題なし」の粒度と取り消し**（§2.5-3） | **★答えた。一括を出さない・取り消しを持たない**（§5.2）。**⇒ 経路の追加請求は出していない。★「実運用で耐えられない件数が出たら一括経路の追加を請求する」は §8-5 として残す。★★語は「確認した」→「問題なし」へ変更した**（2026-09-06 開発者確定＝§5.4） |
| **3** | **告知の「延期した」の保存先**（§2.2-4） | **★答えた。サーバ側のファイル**（§5.1）。**設計卓の見立て（サーバ側ファイルを推す）と一致した。★ブラウザストレージ案を採らなかったので台帳への追記請求は無い** |
| **4** | **基準バージョンをエクスポートへ出すか**（§2.6-3） | **★答えた。出さない。** 設計卓の見立てと一致（列契約に及ぶため） |
| **5** | **専用画面のルート名**（§2.3-2） | **★答えた。`/game-update/combos`。** 先例＝`/trash/combos/:id`。**★`/combos/affected` を採らない理由は「この画面はコンボ一覧の別名ではなく独立した面だから」である**〔**★当初書いた「ルータの宣言順で食われる」は誤りであり取り下げた**——v6 は specificity で照合する。§4 の該当行に経緯を残した〕 |
| **6** | **`PATCH` が基準を進めないこと**（`followup` `combo-baseline-not-advanced-on-patch`） | **★★決着した（2026-09-06 開発者確定）＝「このままでよい」。** 理由＝**「直す」＝レシピを直すことであり、それは `PUT` 経路を通って基準が自動で最新になる**（§4 の「★編集して保存したときの挙動」）。**⇒ 利用者から見た流れは成立する。一方メモを 1 文字直しただけで警告が消えないのは `FR307` と整合する。⇒ 設計卓への請求から外した。★`followup-backlog.md` の当該行は製造が編集できない**（§J 以外＝`D-382`）**ので、設計伝達レポート §4 へ「決着したので畳んでよい」と書いて回す** |
| **★7**〔追加〕 | **既存の移行告知が PC で一度も出ていない**（§2） | **★本サブ由来ではない既存の欠陥であり、直していない。** 対処は「バナーのマウント先を増やす」だけで済むが、**`M28-01` の射程であり本サブが勝手に触ると `CHANGE-156` の as-built がずれる。⇒ 設計卓の割付を仰ぐ** |
| **★8**〔追加〕 | **`ComboResponse` へ `affectedMoves` を足す請求を通すか**（§6.1） | **★通すことを推す。★通らないと `FR702` の目的（どの技が変わったか）が達成できない** |
| **★9**〔追加〕 | **`M28-02a` 完了報告と `M28-02b` 指示書で射程が食い違っている** | **★★`M28-02a` 完了報告 §8 は「3 つの入力方式の UI は作っていない（`M28-02b` の射程）」と書くが、`M28-02b` 指示書 v1.2.0 の §2「やること」に始動位置・運び量の 3 方式入力 UI は 1 行も無い。⇒ 本サブでは作っていない**（指示書が正である＝`CLAUDE.md` §8 の参照優先順位）。**★帰結＝`followup` `duplicate-precheck-ignores-position-mass` と `position-bands-duplicated-go-and-ts` は、いずれも割付条件が「3 方式入力を実装するなら／FE 側で導出を使い始めるなら」であり、本サブでは条件が成立せず未着手のままである。⇒ 割付先の見直しが要る** |

---

## 9. テストと破壊確認 —— **実施していない**

**★指示書 §5 の見出しは「★実装まで進んだ場合のみ」であり、§6 も「進まなかった場合は §2.9 のどの条件が欠けたかを書くこと」である。⇒ チェックリスト §4（束 D）／ §5 ／ §6 は飛ばした。飛ばしたことをここに書く。**

**★「動かして 0 件だった」とは書かない。** そもそも動かしていない。

### 9.1 ★「初回は必ず 0 件」への対処（**実装時の確かめ方**）

**`D-725` により既存コンボの基準は「列を足した時点の最新」であり、`games.current_data_version` は `2026.08.03.01`、既存コンボの基準も同じ値である。⇒ 素のデータでは何も出ない。★「0 件だったから正しい」は正しさの証拠にならない。**

**⇒ 実装するときの確かめ方（本報告で先に決めておく）**:

1. **使い捨て DB を作る**（`make e2e` が使う独立スタックと同じ考え方）。**★`D-399` (1) により、開発サーバや共有 DB を書き換えない。**
2. その DB で `UPDATE moves SET last_changed_game_version = '2026.09.10.01' WHERE ...`（**対象を数件に絞る**）と `UPDATE games SET current_data_version = '2026.09.10.01'` を当て、**基準が古いコンボを人工的に作る**。
3. **対照実験を置く**——マーカーを当てなかったキャラのコンボが 1 件も出ないことを同時に主張する（照合パターンが対象以外に当たっていないことの確認）。
4. 破壊確認 4（**画面を開いただけで基準が進まないこと**）は、**画面を開く前後で `baseline_version` が不変であることを直接見る**。**★進むと利用者が見る前に警告が消える。**
5. Vitest は **実 `ja.json` を引く `t`** で書く（**キーをそのまま返すモックでは文面の主張が空振りする**＝`M23-04` 教訓 2）。
6. E2E の絞り込みは **`make e2e-only P=<パターン>`**（**`playwright` を直接叩くと `PW_EXECUTABLE_PATH` が渡らず UI を使う spec が全滅する**＝`D-599`）。

### 9.2 着手時点のベースライン（**★緑の由来を後から追えるように残す**）

| 対象 | 結果 |
|---|---|
| `go build ./...` ／ `go test ./...` | **緑** |
| `cd web && pnpm exec tsc --noEmit -p tsconfig.json` | **緑** |
| `cd web && pnpm exec vitest run` | **緑**（212 ファイル・2440 件） |
| `check-artifact-integrity.sh` | **緑**（検査 14 件の自己検査 ＋ 生成物 4 件） |
| `check-doc-refs.sh` ／ `check-progress-log-index.sh` ／ `check-stop-discipline.sh` ／ `check-doc-inventory.sh` | **緑** |
| **`check-md-emphasis.sh`** | **★★着手前から赤**（現在 494 行 / ベースライン 436 行）。**★自サブ由来でないドリフトであり床にしない**（チェックリスト §0.3-3）。**⇒ 本サブの目標は「494 を増やさないこと」であり、§11 で確認する** |

**★`check-doc-inventory.sh` は docs 直下 4 ディレクトリの直下のみを走査するため、`docs/progress/M28-02b-mock/` は検出対象外である**（型を増やしたわけではない）。**★例外表へ足して黙らせることはしていない。**

---

## 10. ■ 併せて更新が要るもの

| # | 項目 | 判定 |
|---|---|---|
| 1 | **消費した CHANGE 番号の登録**（`change-number-registry.md` §1） | **★なし。** 製造は自採番していない（`D-293`）。**⇒ 番号は 1 つも消費していない** |
| 2 | **その番号の写し先を全数直したか**（registry §1 ／ 契約 §4 ／ ボード §2.1 ／ §2.4 の 4 か所） | **なし**（同上） |
| 3 | **消費したマイグレ連番** | **★なし。`ls migrations/` の最新は `000105` であり、次に払い出すのは `000106` のまま**（実査した） |
| 4 | **版を上げた文書の参照元** | **なし**（設計書を 1 冊も編集していない） |
| 5 | **ブラウザストレージ台帳**（`web/CLAUDE.md` §1） | **★なし**（サーバ側ファイルを採ったため、キーを 1 つも増やしていない） |

---

## 11. 完了条件の充足（指示書 §6）

| # | 条件 | 判定 |
|---|---|---|
| **1** | モック HTML が `docs/progress/M28-02b-mock/` に在り、4 面 ＋ 見せ方の案 2〜3 を含むこと | **★充足。** 4 面 ＋ **見せ方 3 案** ＋ **語 2 案** ＋ 複数技の見せ方 2 案 ＋ 件数文面 2 案 |
| **2** | §2.2〜§2.7 の決めごとに、選んだ案と理由が報告に在ること | **★充足**（§4 ／ §5） |
| **3** | §2.8 の CHANGE 請求が報告に在ること | **★充足**（§6） |
| **4** | §7 の返却事項に答が並んでいること（答えられないものは「答えられなかった」と書く） | **★充足**（§8。**★6 番は「答えられなかった」と明記**） |
| **5** | `progress-log.md` へ 1 行追記されていること | **★Phase D で実施**（§12 に記録） |
| **6** | 実装まで進んだ場合は §5 が緑。進まなかった場合はどの条件が欠けたかを書くこと | **★充足**（§1.1 で 2 本立てに分けて記述。§9 でテストを飛ばしたことを明記） |

---

## 12. レビュー結果（**★Phase C で実測を書いた**）

> **★本節は Phase A/B の時点ではプレースホルダだった**（`D-510` / `M23-04` の事故＝レビュー実施前に「『高』指摘の不採用は 0 件」と断定し、当時存在しないレビュー報告書へリンクしていた）。**⇒ 以下はレビュー実施後の実測である。**

| 欄 | 内容 |
|---|---|
| レビュー報告書 | `docs/progress/m28-02b-review.md`（248 行） |
| 指摘の件数 | **重大 0 ／ 高 4 ／ 中 4 ／ 低 4**（計 12 件） |
| 各指摘の採否と理由 | **全 12 件を採用。** 全件の理由はレビュー報告書末尾「## 取り込み結果（自動トリアージ）」に記載 |
| **「高」指摘の不採用** | **0 件**（4 件すべて採用）**⇒ 開発者へのエスカレーションは発生していない** |
| 中・低の不採用 | **0 件**（8 件すべて採用） |
| 再レビュー往復 | **1 回**（初回のみ。上限 2 回に対して余裕あり） |

### 12.1 ★★レビューが検出した最大の欠陥（高-2）

**`/combos/affected` を却下した根拠が事実として誤っていた。**
初版は「ルータの宣言順で `/combos/:id` が先に来るため `affected` を id として拾う」と書いていたが、
**`react-router-dom` v6 の `<Routes>` は宣言順ではなく specificity で照合する。**

**★製造側でも独立に実物で確かめた**——`web/node_modules/.pnpm/@remix-run+router@1.23.3/.../router.cjs.js` に
`rankRouteBranches` が在り、**`staticSegmentValue = 10` / `dynamicSegmentValue = 3`** である。
⇒ 静的セグメント `affected` は `:id` より高くランクされ、**宣言順を入れ替えても何も変わらない**。
同じ仕組みで `/combos/new`（`router.tsx:28`）が `/combos/:id`（`:31`）と共存している。

**★結論（`/game-update/combos` を採る）は変わらないが、根拠の文は `DES-005` の新節へそのまま写る位置に在った。**
**⇒ 「テストも lint も型検査も検出せず、後任は注記を読まずに本文をコピーする」という較正基準どおりの「高」である。**
**★さらに実害がある**——「宣言順が効く」と読んだ後任が `router.tsx` の行順を意味なく入れ替える。

### 12.2 2 つの必達成果物が食い違っていた（高-3 / 高-4）

**★モックと完了報告は「どちらも `DES-005` の素」である。⇒ 食い違うと、片方だけが静かに写される。**

| # | 食い違い | 直した向き |
|---|---|---|
| **高-3** | 報告「既存の `ComboTable` を再利用」 ⇄ モック「別構成の表」 | **モックを再利用の形へ描き直した**（左 7 列は一覧と同じ ＋ 網かけ 3 列だけを専用画面で足す）。**キャラは「見出し ＋ 表」を縦に並べる形へ変更** |
| **高-4** | モック「バナー位置に失敗を出す」 ⇄ 報告「一覧のボタン位置**だけ**に出す」 | **報告をモック側へ揃えた。★報告側の形はスマホで失敗が黙る**（入口がホーム画面しか無いため）＝§2.2-6 の罠の再発だった |

---

## 13. 判断が分かれうる箇所（レビューへの申し送り）

| # | 箇所 | 製造の判断と、分かれうる理由 |
|---|---|---|
| 1 | **告知の経路を「常に 200」にしたこと**（§6.2） | 移行告知の 204 の作法から外れる。**★外してよいと読んだ根拠は §0.4 の「流用できるのは…204 の作法まで」が上限の宣言であることと、返す情報が「有無」ではないことの 2 点である** |
| 2 | **告知と件数を 1 本の経路にまとめたこと** | 落ちると告知とボタンが同時に失敗する。**★分けると件数の出どころが 2 つになりドリフトするため、まとめる方を採った** |
| 3 | **マイコンボにボタンを出さないこと**（§2.4-4） | 開発者の逐語は「一覧に」だけを名指ししている。**★ただしマイコンボこそ「使っているコンボ」であり、そこに出す読み方もありうる。⇒ 出さない理由を §4 に書いた上で、覆されたら 1 行で足せる形にしてある** |
| 4 | **語を「更新未確認」に変えたこと**（§3） | 当初案「要確認」からの変更である。**★変更の根拠は実物の衝突 2 か所であり、モックに両案を並べて開発者が戻せる形にしてある** |
| 5 | **キャラ横断で統一したこと**（§4 の「キャラのスコープ」） | 一覧は必ずキャラを 1 体選んでいるため、ボタンの件数と飛び先が食い違う。**★キャラごとに絞る案も採れるが、告知（全体）とボタン（キャラ）で数が割れるため統一を採った** |

---

---

## 14. ★★追補: 開発者フィードバックの反映（2026-09-06・レビュー取り込みの後）

**Phase D まで終えて push した後（`13b8c0c`）、開発者から 4 件のフィードバックが入った。⇒ 本報告とモックを第 2 版へ更新した。★実装は引き続き 0 行である。**

| # | フィードバック | 反映先 | 判定 |
|---|---|---|---|
| ① | **語は「更新未確認」を採用** | §3 | **決着。** 対案をモックから落とした |
| **②** | **「専用画面なのだから、そもそもバッジも状態列もいらないのでは？」** | **§5.3** | **★★指摘が正しい。3 案とも撤回した**（本報告の欠陥） |
| **③** | **「確認した」→「問題なし」／ 影響がある場合のボタンが抜けている** | **§5.4 / §5.5** | **★★後半は本報告の欠陥。［編集］の導線が無かった** |
| ④ | **保存後は専用画面へ戻す** | §4 の「★保存後の戻り先」 | **決着。** 新しい設計判断として §6.5-3 で請求 |

### 14.1 ★★自分の成果物の欠陥として認めた 2 件

| # | 何が欠けていたか | なぜ自分で気づけなかったか |
|---|---|---|
| **②** | **専用画面は全行が該当するのに、行ごとに「該当している」という印を置いていた。** 3 案とも一覧側にも印を出す前提で組み立てており、**「そもそも要るのか」を問い直していない** | **★指示書 §2.7 が「見せ方を決めよ」と書いていたため、「どう見せるか」だけを考えて「出さない」を選択肢に入れていなかった。⇒ 一般形＝「どう作るか」を問われたら、その前に「作る必要があるか」を 1 度だけ問うこと** |
| **③** | **「影響あり ⇒ 直しに行く」の導線が、モックにも報告にも無かった。** 置いていたのは「問題なし」だけである | **★★`D-729` の逐語の後半に応えていなかった**——「どのコンボに影響があるかをユーザーに見せ<u>修正しやすいように</u>する事が今回の目的」。**⇒ 目的の文を 2 つの要求に分けて読むこと。★しかも［編集］は既存の行部品が既に出しており、作る必要すら無かった** |

### 14.2 ★決着したことで請求から落としたもの

**返却事項 6（`combo-baseline-not-advanced-on-patch`）。** ④ の確認の過程で `PUT` / `PATCH` の実挙動を実査し、**「レシピを直すと消える／メモだけ直しても残る」という非対称が、開発者の望む流れをそのまま満たすことが分かった。⇒ 開発者確定「このままでよい」。**

**★`followup-backlog.md` の当該行は製造が編集できない**（§J 以外は `D-382`）**ので、設計伝達レポート §4 へ「決着したので畳んでよい」と書いて設計卓へ回した。**

### 14.3 ★設計伝達レポートを作った（**この追補で最も効く 1 件**）

`docs/handover/design-reports/20260906-m28-02b-design-exceptions.md` を新規作成した。

**★★理由＝設計卓は `docs/progress/` を読まない**（2026-08-11 開発者裁定①。同ディレクトリの既存レポートが逐語で明記している）。
**⇒ §6 の CHANGE 請求は完了報告にしか無く、設計卓の目に入らない場所に置かれていた。★本サブの最重要の請求（`affectedMoves`）が届かないままになるところだった。**
`roles-and-routing.md:105` も製造の出力として `design-report` を挙げている。

*以上、M28-02b 完了報告。* **★★本サブの成果物は「決めたこと」と「決めた理由」であり、コードではない。** **★最も重かった判断は、語を当初案から差し替えたこと**（`ja.json` に無いことを衝突なしと読みかけたが、**実物はハードコードで存在していた**）**と、`FR702` の目的そのものが現行 API では達成できないと分かったこと**（§6.1）**である。**
