# M32-03 完了報告: 操作説明書を `M37` の着地へ追随させる ＋ クイックスタートの新設

| 項目 | 内容 |
|------|------|
| 作業 ID | `M32-03` |
| 指示書 | `docs/instructions/M32-03-usermanual-revision-after-m37.md` **v1.1.0** |
| チェックリスト | `docs/instructions/reviews/M32-03-review-checklist.md` **v1.1.0** |
| 実施日 | 2026-09-15 |
| 着手基点 | **`96b01d6`** |
| CHANGE 消費 | **0 本**（起票のたたき台は設計伝達レポートへ回す） |
| マイグレ消費 | **0 本** |

---

## 1. 着手前の版ゲート（指示書 §0.6・チェックリスト §F）

| # | 確かめたこと | 実測 |
|---|---|---|
| 1 | チェックリストの存在 | `docs/instructions/reviews/M32-03-review-checklist.md` **v1.1.0**・存在 |
| 2 | `M32-02` 完了報告が `main` に在る | `docs/progress/M32-02-completion-report.md`（45,149 B） |
| 3 | `docs/usermanual/` に本文が在る | `tacpendium-readme.html` 102,234 B・**22 章**・`<figure>` **27**。器だけではない |
| 4 | `M37-07` の着地 | `M37-07-completion-report.md` ／ `m37-07-review.md` の双方あり。**`ls migrations/` の最新＝`000117_add_combos_starter_meaty`** |
| 5 | `DES-005` が 2.121.0 以上 | **2.122.0** |
| 6 | 枝元（`git log --oneline -5`） | `96b01d6` / `d4e692b` / `cbf1eb4` / `6b413ce` / `31d1ce9` |

---

## 2. 段 1 — 現物を読み、数え直した（指示書 §2.1）

| # | 数えたもの | `M32-02` の記述 | **本サブの実測** | 一致 |
|---|---|---|---|---|
| 1 | 本文の章数 | 22 章 | **22 章**（`<section class="chapter"` の実数） | ○ |
| 2 | 撮影の置き場 | 27 枚（同報告 **§3.4**） | **27 枚**（`<figure>` の実数。`figure.todo` も 27） | ○ |

**★指示書 §2.3-1 は枚数の出所を「`M32-02` レポート §1-1」としているが、実際は §3.4 である。⇒ 枚数 27 そのものは正しい。**

---

## 3. ★★★指示書 §2.2 の対応表に無かった食い違い（§4.5 ／ チェックリスト §H）

**★★指示書 §2.2 は `CHANGE-195`〜`209` の 15 本を前提に 8 本を名指ししている。⇒ 実際に `M32-02` 本文執筆後に着地したのは `CHANGE-192`〜`211` の 20 本であった。**

**★境界の確定方法**＝本文の最終コミット `8df45ab`（2026-09-12 18:03）以降に**新規追加**された `docs/change-notes/` を列挙した。

```
git log --format="%h|%ad|%s" --date=short --diff-filter=A --name-only 8df45ab..HEAD -- docs/change-notes/
```

**★`CHANGE-188`〜`191` は本文執筆より前であり、`M32-02` の射程である。⇒ 本サブでは扱わない。**

### 3.1 指示書が名指ししていない 12 本 — 全数を実読して判定した

| CHANGE | 主題 | 判定 |
|---|---|---|
| **★★192** | 操作説明書を配布アーカイブへ同梱（`P-57` の決着・`D-850`） | **★★★影響あり。§5 (b) で是正した** |
| 193 | 【ジャスト】版の命名の as-built（guile の seed 是正） | **影響なし** — 本文は技名・ファミリーを 1 つも列挙していない |
| 194 | マイグレーション本数の as-built ／ スキーマ同一性の固定 | **影響なし** — 内部規約 |
| 195 | `PATCH /api/combos/{id}` がマス数 2 欄を運ぶ | **小。`ch07` の「メタデータ」の欄**。`CHANGE-196` の改稿に含めた |
| 198 | `hit_type` の値域の正本を `SUPP-001` §3.2 へ寄せる | **影響なし** — **`hit_type` は 8 値のまま**（`B15` は差し戻し・`D-866`）。本文は値を列挙していない |
| 202 | 重複判定キーの節の**例**の差し替え | **影響なし** — `SUPP-001` 内の例示 |
| 204 | `moves` へ `first_hit_startup` を足す | **影響なし** — **`GET /api/moves` に露出しない**。`ch14` は CSV 列数も当該列も書いていない（実読） |
| 206 | golden 再生成と未適用マイグレの差し替えの規約化 | **影響なし** — 製造手順 |
| **207** | 重複判定キーを 7 → 8 要素へ | **★★影響あり＝`ch08` の「大事」box**（§3.2） |
| 208 | `VAL-C02` の判定条件へ `starter_meaty` を足す | **影響なし** — 内部バリデーション |
| 210 | `DES-005` §5.14 の節名を「取込(CSV)」へ | **影響なし・むしろ裏付け** — `ch16` は既に「取込」。`M32-02` の判断を設計卓が受理した形 |
| 211 | `DES-005` §5.13a の `CHANGE-165` 注記は**省略表現**である | **影響なし・むしろ裏付け** — `ch16` の「書き出しも 1000 件で打ち切られる／セットプレイ行数とファイルの大きさには上限が無い」は裁定と逐語で一致 |

### 3.2 ★★★`ch08` の「大事」box が**現に誤っていた**（最も重い 1 件）

| | |
|---|---|
| 本文の記述（着手前） | 「レシピ・始動技・始動位置・相手の状態・ヒット種別・相手の大きさ——**この 6 つ**」 |
| 実測 | `internal/service/combo/duplicate_keys.go:33-42` は **8 要素**。`character_id` を除くと利用者から見て **7 つ** |
| 由来 | `CHANGE-207` ／ `CHANGE-209`（`M37-07`・`D-874`）が `starter_meaty` を足した |
| 是正 | **「持続当て（始動技）」を足して「7 つ」へ** |

**★指示書 §2.2 の表はこれを名指ししていない。⇒ 表は `CHANGE` の反映先から引いたものであり、本文を読んで作られていないためである（同 §4.5 の警告どおり）。**

### 3.3 ★★★黒窓は既に出ない — `M34-02` のトレイ常駐

**★開発者の指摘により判明した。⇒ 本サブの起点は `M37` だが、`M34-02` の着地も本文へ届いていなかった。**

- `combmgr-friend-readme.html` は `M32-01`（`582106f`）で `tacpendium-readme.html` へ改名され、中身は `M32-02` が全面改稿した。**⇒ 指示書 §2.5.4-1「黒い窓の注意は同書に在るので参照する」は参照先を失っている。**
- **`CHANGE-187` §5 は「`M32` への申し送り 2 件」**〔移行手順 ／ **「終了は通知領域のアイコンから」**〕**を明記している。⇒ `M32-03` 指示書はこれを拾っていない。**

| 場所 | 着手前の記述 | 実態 |
|---|---|---|
| `dist-readme.txt:22` | 「起動時に**コンソール**/ログへ表示される URL」 | Windows 配布版に**コンソールは無い**（`make build-windows` の `-H=windowsgui`） |
| `dist-readme.txt:74` | 「起動時に**コンソール画面**と logs/ 配下のログに」 | 同上 |
| `tacpendium-readme.html:263`（`ch03`） | 「**ウィンドウをタスクトレイへしまってある状態から**」 | **しまうウィンドウが無い。常に常駐している** |

**★★プラットフォーム条件つきである**——`internal/desktop/desktop_other.go` は `//go:build !windows` のスタブであり、**トレイ常駐は Windows のみ**。macOS / Linux（非公式対応）は端末で動く。**⇒ 「Windows では」と限定して書いた。**

### 3.4 ★指示書 §2.5.4-2「キャラクターを取り込む」も失効していた

`migrations/` がキャラクター・技を seed で投入する（`000003_seed_characters` 以降。`character_data/*.csv` は 31 本）。**⇒ 初回起動時に取込は要らない。** 22 章に「キャラクターデータ取込」の画面は無い（`ch16` は**コンボの CSV 取込**であって別物）。**⇒ クイックスタートの節 2 を差し替えた**（§6）。

### 3.5 `ch11` と `CHANGE-199` — 当て先は `ch08` に収めた

`CHANGE-199` のゲームパッド告知は「**コンボ / セットプレイの 2 面で既読と開閉を共有**」であり、表は `ch11` に `CHANGE-203` しか割り当てていない。**⇒ `ch08` の本文に「コンボとセットプレイのどちらで開け閉めしても、もう一方にも効きます」と書いて 1 か所に収めた。⇒ `ch11` は差分 0 とした**（同じ情報を 2 か所に持たないため＝`E-76`）。

---

## 4. 段 2 — 8 章それぞれの判定（完了条件 1 ／ チェックリスト A-1）

> **★「直した」か「読んだが直す所は無かった」のどちらかを、根拠つきで全 8 章について書く。**

### 4.1 改稿した 4 章

| 章 | `DES-005` | 当てた差分 | 由来 |
|---|---|---|---|
| **`ch08`** コンボ登録・編集 | §5.7 | **(1) 「大事」box を 6 → 7 つ**（§3.2） ／ (2) 「持続当て（始動技）」と step の修飾「持続当て」の書き分けを `caution` で ／ (3) 入力方式〔始動位置 3 択・既定は通常入力／運び量 2 択・既定はマス目〕と値域クランプ ／ (4) 空欄のまま欄を離れたときの代表値補完と、運び量には掛からないこと ／ (5) 仮想コントローラの左右 2 列化・高さ上限・告知は初回のみ ／ (6) 修飾の表示語（低ジャンプ → **低空** ほか）とキャンセルが既定であること | `207` / `209` / `196` / `200` / `199` / `201` |
| **`ch07`** コンボ詳細 | §5.6 | 「状況」の欄に「持続当て（始動技）」が並ぶこと ／ 始動位置が `80 マス (50%)` 形式で出ること ／ **運び量は「状況」ではなく「メタデータ」の欄**であること | `209` / `196` / `195` |
| **`ch05`** コンボ一覧 | §5.4 | 絞り込みの軸へ「持続当て（始動技）」を追加し、選択肢が**指定なし / はい / いいえ**であること・**列は増えていない**こと ／ 修飾が技の左に札で出ること ／ **キャッシュ再構築を一度押すまで札と波括弧が混ざる**こと | `209` / `203` |
| **`ch17`** ゴミ箱 | §5.15 | 並びが**消した日時の新しい順**であること | `197` |

**★`ch17` は「直す」ではなく「足す」判断である。** 着手前の本文に**旧い並び順の記述は 1 行も無かった**（チェックリスト A-5 は「旧い並び順の説明が残っていないか」を見るので、厳密にはそのままでも通る）。**⇒ よく使う操作が「戻す」であり、「さっき消したもの」に辿り着けることが実益であるため足した。**

**★修飾の札（バッジ）の説明は `ch05` にだけ置いた。** 読む面 5 つ〔一覧 / マイコンボ / 詳細 / 比較 / セットプレイ〕で見た目は同じであり、**読者が最初に出会うのが `ch05` である**。5 か所へ書くと同期先が 5 つになる（`E-76`）。

### 4.2 ★読んだが直す所が無かった 4 章（**根拠つき**）

**★判定は本文の語の実測で行った。⇒ 「影響が小さそうだから」ではない。**

| 章 | 当たるはずの差分 | **本文の実測** | 判定 |
|---|---|---|---|
| **`ch06`** マイコンボ | `CHANGE-203`（修飾のバッジ） | **「修飾」0 件・「波括弧」0 件・「バッジ」0 件。** 本文は「表はコンボ一覧と<b>同じ部品を共有しています</b>」「表の作りはコンボ一覧と同じです」と 2 度述べている | **差分 0**。当て先の記述が無く、`ch05` の説明が効く |
| **`ch10`** コンボ比較 | `CHANGE-203` | **上記 3 語すべて 0 件。** 本文は項目を列挙せず「記録してある項目をすべて、縦に揃えて出します」と述べる | **差分 0** |
| **`ch11`** セットプレイ登録・編集 | `CHANGE-203`（＋ §3.5 の `CHANGE-199`） | **上記 3 語すべて 0 件。** `CHANGE-199` の告知共有は `ch08` に 1 か所で収めた（§3.5） | **差分 0** |
| **`ch20`** 確定反撃サーチ | **`CHANGE-205`（★最も落としやすい）** | **「発生」0 件。** 「候補」は 4 件あるが、いずれも**件数を述べていない**〔「ダメージが 0 の技は候補に出ません」「元のコンボは候補からたたまれます」「たたまれるのはこの画面の候補だけ」「ほかの候補と見比べてから」〕 | **差分 0**（下記の裁定による） |

**★★★`ch20` を「影響なし」で通してはいない**（チェックリスト §0-3 / A-3）。**中身が変わったこと**〔候補から 17 行が恒久的に落ちた ／ 88 行が緩い有利フレームで出る ／ **「発生」の意味が「単独で出したときの発生」→「判定に使った発生」へ変わった**〕**を確認したうえで、本文が候補の数も「発生」も説明していないことを実測で確かめた。**

**★★開発者裁定（2026-09-15・本セッション）＝「触れない」**（指示書 §7-1 の設計卓の暫定案どおり）。**⇒ 技一覧の「発生」との食い違いは `followup` の `punish-scan-response-startup-means-first-hit` と同じ面であり、設計伝達レポート §4 へ回す。**

---

## 5. ★開発者の明示指示による追加 2 件（指示書 §2.2 の 8 章の外）

**★★★本節は射程の拡大であり、開発者の明示指示（2026-09-15・本セッション）による。⇒ 独自判断ではない。**

### (a) トレイ常駐と黒窓（§3.3）

- `dist-readme.txt` の**コンソール参照 2 か所**を実態へ是正した
- **「■ 常駐と終了 (Windows)」を新設**——トレイのメニュー **6 項目**（`cmd/tacpendium/tray.go:34-40` の逐語）／ **終了はトレイのみ**であること ／ 画面内に終了ボタンを作らない理由（`D-834`＝LAN の他端末から認証なしで止められる口を作らない） ／ 簡易パスワードの復旧導線（`CHANGE-187` §2(f)） ／ **macOS・Linux にはトレイが無い**こと
- **`ch03` の 1 文**「ウィンドウをタスクトレイへしまってある状態から」を是正した

**★★★これが 14 章への唯一の差分である。1 文に限定した。**

### (b) 操作説明書の到達経路（`CHANGE-192`）

`CHANGE-192` §1 は、着手前に経路が **1 つも無かった**ことを記録している〔URL は `P-46` 未確定 ／ zip の構成が exe と `README.txt` のみ ／ **GitHub 上では `.html` がソース表示になる**〕。**開発者裁定（`D-850`）で zip 同梱に決着した。**

| 行 | 着手前 | 是正後 |
|---|---|---|
| `dist-readme.txt:7` | 「操作説明書(**`docs/usermanual/tacpendium-readme.html`**)に全 22 章あります」 | **`manual/tacpendium-readme.html`** ＋ クイックスタートへの案内 |
| `dist-readme.txt:171-172` | 「(**公開リポジトリの** `docs/usermanual/...`)にあります」 | **同梱の `manual/...`** ＋ 同上 |

**★`M32-02` 完了報告 §3.3 は「★到達経路（`P-57`）は解決していない」と*正しく留保*していた。⇒ その後 `CHANGE-192` が解決し、追随だけが落ちていた。**

**★`README.txt` は直接編集していない。** 正本 `docs/usermanual/dist-readme.txt` を直し、`bash scripts/check-dist-readme.sh --write` で生成し直した（`D-755` の生成方式 (b)。`M32-02` と同じ手順）。

---

## 6. 段 5 — クイックスタートの新設（**第 2 の成果物**）

| 項目 | 内容 |
|---|---|
| 置き場・命名 | **`docs/usermanual/tacpendium-quickstart.html`** |
| 根拠 | `M32-01` が定めた **`tacpendium-<種別>.html`** の流儀（`tacpendium-readme.html` と対になる）。`docs/usermanual/` は `check-doc-inventory.sh` の走査対象 4 ディレクトリの外 |
| 節数 | **5**（上限どおり） |
| 図 | **1 枚**（`images/qs-first-combo-1.png`・`figure.todo`） |
| 導線 | 本編の**前付け「本書の読み方」**へ 1 行リンク（開発者裁定）。**★22 章の本文には 1 行も触れていない** |

### 6.1 節の並びと送り先

| # | 節 | 送り先 |
|---|---|---|
| 1 | 入れる → 起動する | `README.txt`（★**通知領域に常駐・終了はそこから**を 1 行で） |
| 2 | **最初に聞かれることに答える** | 本編 第 1 章 |
| **3** | **★最初のコンボを 1 本 登録する**（芯・図はここだけ） | 本編 第 8 章 |
| 4 | 見つける／比べる | 本編 第 5 章 ／ 第 10 章 |
| 5 | 困ったら → 本編の該当章へ | 目次 |

**★★節 2 は指示書 §2.5.4 の案「キャラクターを取り込む」から差し替えた**（§3.4 のとおり失効しているため）。

### 6.2 ★上限に当たって落としたもの（チェックリスト I-4）

**★黙って削っていない。**

| 落としたもの | なぜ |
|---|---|
| **節 2〜5 の図 4 枚** | §2.5.3 は「各節 3 行程度 ＋ **図 1 枚まで**」だが、**同時に「全体が 1 画面に収まること」も課している。⇒ 5 枚置くと 1 画面に収まらない。** 芯である節 3 の 1 枚だけを残した |
| **タグ・セットプレイ・確定反撃サーチの紹介** | **道順ではなく機能の一覧になる**（§2.5.2）。「最初の 1 本まで」に要らない |
| **スマホからの接続・バックアップ・LAN** | **`README.txt` の側にある。⇒ 重ねない**（チェックリスト I-5） |

### 6.3 ★要約を書いていないこと（チェックリスト I-1）

**書いたのは道順だけである。** 各節は画面を名指しして本編へ送り、**その画面に何の欄があるかは 1 つも書いていない**。声は `M32-01` 以前の `combmgr-friend-readme.html`（`582106f^` の git 履歴から引いた）の調子〔「短く読めます」「気づいたこと・不満は何でも言ってください（怒りません）」〕に寄せたが、**内容は重ねていない**。

**★本編は 1 行も縮めていない**（指示書 §3-2b。§8 の変更統計で示す）。

---

## 7. 段 3 — 撮影の置き場（完了条件 3 ／ チェックリスト §D）

### 7.1 最終の枚数と場所

| 文書 | 枚数 |
|---|---|
| 本編 `tacpendium-readme.html` | **28**（22 章 × 1 ＋ 2 枚目を持つ 5 章＝`ch05` / `ch07` / `ch08` / `ch17` / `ch20` ＋ **`ch07` の 3 枚目**） |
| **クイックスタート**（新規） | **1** |
| **合計** | **★29** |

> **★★着手時は 27 枚であり、本サブで 2 枚増えた**〔クイックスタートの 1 枚 ／ **`ch07` のセットプレイ自動提案**〕**。⇒ 後者は 2026-09-16 の開発者指示による追加である**（§7.4）。

**★置き場は `docs/usermanual/images/`（現在 `.gitkeep` のみ・画像 0 件）。正本は各 `<figure>` の `img src` である。**

| # | ファイル名 | 撮るもの |
|---|---|---|
| 1 | `ch01-wizard-1.png` | **初回起動ウィザード（パスワードを決める段）**。★段 7。LAN を有効にしないと出ない |
| 2 | `ch02-user-select-1.png` | ユーザー選択 |
| 3 | `ch03-settings-1.png` | 設定 |
| 4 | `ch04-login-1.png` | ログイン |
| 5 | `ch05-combo-list-1.png` | コンボ一覧 |
| 6 | `ch05-combo-list-2.png` | 絞り込み欄を開き、セットプレイを ▼ で展開した状態 |
| 7 | `ch06-my-combo-1.png` | マイコンボ |
| 8 | `ch07-combo-detail-1.png` | コンボ詳細 |
| 9 | `ch07-combo-detail-2.png` | 紐づくセットプレイと、成立条件の 2×2 グリッド |
| — | （`ch07` の 3 枚目は下の ★28 行） | |
| 10 | `ch08-combo-editor-1.png` | コンボ登録・編集 |
| 11 | `ch08-combo-editor-2.png` | レシピを入力しているタブ |
| 12 | `ch09-tag-manage-1.png` | タグ管理 |
| 13 | `ch10-combo-compare-1.png` | コンボ比較 |
| 14 | `ch11-setplay-editor-1.png` | セットプレイ登録・編集 |
| 15 | `ch12-preset-list-1.png` | プリセット一覧 |
| 16 | `ch13-preset-editor-1.png` | プリセット編集 |
| 17 | `ch14-moves-grid-1.png` | moves 編集グリッド |
| 18 | `ch15-export-dialog-1.png` | エクスポートダイアログ |
| 19 | `ch16-import-1.png` | 取込 |
| 20 | `ch17-trash-1.png` | ゴミ箱 |
| 21 | `ch17-trash-2.png` | 完全削除が拒否され、掴んでいるコンボが並んだ状態 |
| 22 | `ch18-intake-helper-1.png` | 引っ越し取込 |
| 23 | `ch19-game-update-1.png` | ゲーム更新の影響コンボ |
| 24 | `ch20-punish-search-1.png` | 確定反撃サーチ（探す） |
| 25 | `ch20-punish-search-2.png` | 「自動判定できない相手技」のまとまりと理由ラベル |
| 26 | `ch21-punish-list-1.png` | 確定反撃マイリスト（使う） |
| 27 | `ch22-home-1.png` | ホーム（スマホ専用） |
| **★28** | **`ch07-combo-detail-3.png`** | **【新規】セットプレイの自動提案（採用・不採用を決める前の状態）** |
| **★29** | **`qs-first-combo-1.png`** | **【新規】クイックスタート — 最初の 1 本を入れているところ** |

### 7.2 ★増やさなかった面と、その理由（チェックリスト D-2 / D-4）

**`ch05` にフィルタが 1 軸増えたが、置き場は増やさなかった。** 2 枚目が既に「**絞り込み欄を開いた状態**」であり、増えた軸はその同じ枠に収まる。**⇒ 減らした置き場は 0 件である。**

### 7.4 ★★2026-09-16 の開発者指示による追加 3 件

**★★★いずれも本サブの当初射程の外であり、開発者の明示指示による。**

| # | 何を | どこ | 理由 |
|---|---|---|---|
| **1** | **撮影方針を書き残した** | `docs/usermanual/images/.gitkeep`（次の担当者向け） ＋ 本編の**前付け**「本書の読み方」（読者向け 1 文） | **★画面全体を写さないことを決めた。**〔判断基準＝「本文が名指ししている語を、読者が画面上で見つけられるか」〕**。⇒ 記録しないと、次に説明書を触る人が「写真が不完全だ」と判断して撮り直す。★前付けであり 22 章の本文には触れていない** |
| **2** | **`ch01` の figcaption / alt に段を名指しした** | `ch01`（★**14 章のひとつ**） | 「初回起動ウィザード」→ **「初回起動ウィザード（パスワードを決める段）」**。★段 7 を撮ると決めたため、段を書かないと読者が「どの段か」を取り違える |
| **3** | **`ch07` に 3 枚目の置き場を足した** | `ch07`（★**8 章のひとつ**） | **`ch07-combo-detail-3.png` ＝ セットプレイの自動提案。**★本文の最終段落が「出しただけでは保存されない」「採用を押したときに初めて残る」「不採用は一時的に隠すだけ」と 3 つの挙動を述べており、**文章だけでは伝わりにくい面である** |

**★★2 により、14 章のうち差分がある章は `ch03`（§5 (a)）と `ch01` の 2 章になった。⇒ §8.2 の表を更新した。**

**★撮影方針の根拠**——本書は「**全機能の一覧ではありません**」と自ら宣言しており（前付け）、`M32-02` も「よく使う操作は 3 つまで」で縛っている。**⇒ 本文が網羅を放棄しているのに写真だけ網羅する理由が無い。** あわせて**手順の流れは後日の動画とクイックスタートが担う**。**★ただし「動画が来るから写真は薄くてよい」ではない**——写真の役目は「本文の名詞と画面を結びつけること」であり、**動画が最も苦手な面である**（動画は走査できない）。**⇒ 動画が出来ても写真の役目は減らない。**

**★★例外を 1 つ明記した**——**本文が画面の「構造」を主張している箇所は、その主張が写真で裏切られないこと**（`ch08`「入力欄は 2 枚のタブ」→ タブ見出し 2 つが写る ／ `ch01`「全部で 8 段」→「ステップ 7 / 8」が写る）**。⇒ 読者は本文より写真を信じるためである。**

**★あわせて撮影の設定を `.gitkeep` へ実測つきで書いた**——**表示幅が 712px 固定であるため、見かけの倍率は `712 ÷ ビューポート幅` で決まる**（DPR ではない）**。⇒ フル HD の全画面撮影は 0.37 倍になり 14px の文字が 5px 相当で潰れる。推奨は DevTools の `1100 × 800 / DPR 2`。**

---

### 7.3 ★★★開発者の手番に含まれる作業（指示書 §2.3-5）

- **撮影と貼り込みは開発者の手番である。本サブでは撮っていない**（チェックリスト D-3）
- **★★撮り終えたら `figure` の `todo` クラスを外すこと。** 未記入であることを示す**点線の枠と背景色**が付いており、外さないと配布物にそのまま残る（`M32-02` レポート §1-1 と同趣旨）。**現在 28 枚すべてが `todo` である**

---

## 8. ★変更統計（着手基点 `96b01d6`）

```
 README.txt                                 |  41 ++++++++--
 docs/usermanual/dist-readme.txt            |  41 ++++++++--
 docs/usermanual/tacpendium-quickstart.html | 127 +++++++++++++++++++++++++++++
 docs/usermanual/tacpendium-readme.html     |  26 +++++-
 4 files changed, 220 insertions(+), 15 deletions(-)
```

### 8.1 ★新規ファイルの deletions（教訓 `E-225`）

```
  +35    -6     README.txt
  +35    -6     docs/usermanual/dist-readme.txt
  +127   -0     docs/usermanual/tacpendium-quickstart.html   ← ★新規。deletions 0
  +23    -3     docs/usermanual/tacpendium-readme.html
```

**★`tacpendium-quickstart.html` は `+127 / -0` である。⇒ 新規のつもりのファイルで既存を上書きしていない。** 作成前に `ls docs/usermanual/` と `find . -name "*quickstart*"` で同名の不在を確かめた。

### 8.2 ★★★章ごとの差分（完了条件 2 ／ チェックリスト §0-1）

**★機械判定した。**〔各 `<section class="chapter">` の中身を着手基点と文字列比較〕

| 区分 | 章 | 結果 |
|---|---|---|
| **★8 章のうち改稿** | `ch05` / `ch07` / `ch08` / `ch17` | 変更あり |
| **★8 章のうち差分 0** | `ch06` / `ch10` / `ch11` / `ch20` | **差分 0**（§4.2 に根拠） |
| **14 章** | `ch02` `ch04` `ch09` `ch12` `ch13` `ch14` `ch15` `ch16` `ch18` `ch19` `ch21` `ch22` の **12 章** | **差分 0** |
| **★14 章のうちの例外 2 章** | **`ch03`** ／ **`ch01`** | **どちらも開発者の明示指示による。**`ch03` は 1 文（§5 (a)）、`ch01` は figcaption と `alt` のみ（§7.4-2） |

**★★本編の実質の削除は 3 行であり、いずれも*置き換え*の片側である**〔`ch03` の 1 文 ／ `ch08` の「大事」box の 1 行 ／ `ch05` の絞り込みの 1 行〕**。⇒ 本編を縮めていない**（チェックリスト I-6）。

---

## 9. 段 4 — 走査（完了条件 4 ／ チェックリスト §E）

**★件数を出す。「走査した」だけにしない。**

| # | 走査 | 結果 |
|---|---|---|
| **1** | **「現在準備中」** | **本編 0 件 ／ クイックスタート 0 件。**⇒ `M32-02` 完了条件 7.5 が外した結果を復活させていない |
| **2** | **「持続当て」** | **計 10 件。**〔「持続当て（始動技）」**5 件** ＝ `ch05` の絞り込み / `ch05` の 3 値 / `ch07` の状況 / `ch08` の「大事」box / `ch08` の `caution` ／ 素の「持続当て」**5 件** ＝ 動詞としての用法 3 件 ＋ **step の修飾としての用法 2 件**〕**。★`ch08` の `caution` が「状況の欄の『持続当て（始動技）』」と「レシピの技ごとに付ける修飾の『持続当て』」を*別々の項目*として明示的に書き分けている**（`ch07` にも同趣旨の 1 文） |
| **3** | **`M29-01` / `M24-07` の統一語** | **退けた語「インポート」は本編 0 件 ／ クイックスタート 0 件。**（「エクスポート」は本編 9 件 / QS 1 件だが、**`M24-07` は取込側だけを和語へ寄せており書き出し側はカタカナのままが正**＝`M32-02` レポート §4.3 C-4） |
| **4** | **波括弧** | **本編 2 行**（`:366` / `:367`）**。★どちらも「廃止していない」側の記述である**——`:366` の逐語は「**波括弧の書きかたが無くなったわけではありません**——マウスを載せたときの表示・エクスポート・コピーは、今までどおり `{目押し}` のような波括弧付きの文字のままです」。**⇒ 「波括弧が無くなった」と読める記述は 0 件**（チェックリスト B-1） |

---

## 10. 検査（指示書 §5 ／ 完了条件 6）

**★出力で判定した。**

| 検査 | 着手前 | 完了後 |
|---|---|---|
| `bash scripts/check-artifact-integrity.sh`（**★1 本目**） | `EXIT=0` | **`EXIT=0`** |
| `bash scripts/check-doc-refs.sh` | `EXIT=0` | **`EXIT=0`** |
| `bash scripts/check-doc-inventory.sh` | `EXIT=0` | **`EXIT=0`** |
| `bash scripts/check-dist-readme.sh` | `EXIT=0` | **`EXIT=0`**（`--write` で再生成後に照合。逐語＝「✅ 一致: README.txt は docs/usermanual/dist-readme.txt から生成した内容と同一です」） |
| `bash scripts/check-stop-discipline.sh` | — | **`EXIT=0`** |
| `bash scripts/check-md-emphasis.sh <本サブの新規 .md>` | — | §13 に記す |

**★★`go test` / `pnpm test` / `make e2e` は回していない**（チェックリスト C-4）。**実装差分が 0 行だからである。⇒ 回す必要が出たら射程が広がった合図であり、止まって報告するのが正しい。**

### 10.1 ★HTML の構造検証（`M32-02` §5.4 と同じ型）

| 見たもの | 結果 |
|---|---|
| 本編の章数 | **22** |
| 本編の `<figure>` ／ うち `todo` | **27 / 27** |
| クイックスタートの節数 ／ `<figure>` ／ うち `todo` | **5 / 1 / 1** |
| クイックスタート → 本編のアンカー解決 | **未解決 0 件**（`#ch01` `#ch05` `#ch08` `#ch10` `#toc` がすべて実在） |
| 本編 → クイックスタートのリンク | **1 件**（前付けのみ） |
| `<section>` の開閉 | **全 22 章が同じ入れ子の深さで開閉。釣り合い OK** |

> **★`<div>` の素朴な計数は 1 件ずれるが、これは着手基点でも同じずれ方をする**（着手前 差 1 → 完了後 差 1）**。⇒ 原因は `:70` の CSS コメント内の文字列 `<div class="table-scroll">` であり、実タグではない。★本サブが持ち込んだものではなく、本物の不整合でもない。**

---

## 11. 完了条件の自己点検（指示書 §6）

| # | 条件 | 結果 |
|---|---|---|
| 1 | 8 章すべてに「直した」か「読んだが直す所は無かった」が**根拠つきで**ある | **○**（§4.1 / §4.2） |
| 2 | 残る 14 章の差分が 0（`git diff --stat` で示す） | **△ → ○（条件付き）**。**13 章が差分 0。`ch03` のみ 1 文の差分があり、これは開発者の明示指示による**（§5 (a) / §8.2） |
| 3 | 置き場の最終の枚数と場所が一覧で報告にある | **○**（§7.1。**28 枚**） |
| 4 | §2.4 の走査 4 本の結果が報告にある | **○**（§9。件数つき） |
| 5 | `M32-02` の `UI の粗` が残っている（上書きしていない） | **○**。同記録は `docs/progress/M32-02-completion-report.md` §4（28 件）にあり、**本サブは同ファイルを 1 バイトも触っていない**（§8 の変更統計に現れない） |
| 6 | §5 の検査がすべて緑（出力で判定） | **○**（§10） |
| 7 | 完了報告 ＋ `progress-log.md` へ索引行 1 行 | **○**（Phase D で実施） |
| 8 | 設計伝達レポートを出す。CHANGE 原稿は §1 と §6 へ（**§4 ではない**） | **Phase D で実施** |
| 9 | 着手前の版ゲート 6 点を通した | **○**（§1） |

---

## 12. ■ 併せて更新が要るもの（教訓 `E-114` ／ `D-277`・`D-297`）

| # | 項目 | 結果 |
|---|---|---|
| 1 | **消費した CHANGE 番号の登録** | **なし**。本サブは CHANGE を 1 本も起票していない。**⇒ `change-number-registry.md` §1 への登録は不要**（起票のたたき台は設計伝達レポート §6 へ出し、採番は設計卓の手番） |
| 2 | **「次の番号」の写し先 4 か所** | **触っていない**（registry §1 ／ 契約 §4 ／ ボード §2.1 ／ ボード §2.4） |
| 3 | **消費したマイグレ連番** | **0 本。**`ls migrations/` の最新は `000117` のままで、着手前から動いていない |
| 4 | **版を上げた文書の参照元**（`E-118`） | **版を上げた文書は無い**。`docs/design/` 本体は 1 バイトも触っていない（`CLAUDE.md` §8） |
| **★5** | **生成物の再生成** | **★あり。`README.txt` を `check-dist-readme.sh --write` で再生成した**（正本＝`docs/usermanual/dist-readme.txt`）。照合も緑 |
| **★6** | **配布アーカイブの構成** | **★手を入れていないが、変更が要る。** `CHANGE-192` §2 の `manual/` はクイックスタートを含んでいない。**⇒ CHANGE 原稿を設計伝達レポート §1・§6 へ出す**（§14-1） |

---

## 13. `check-md-emphasis.sh`（`D-775`）

**★この手番で `docs/progress/` へ新規に作ったファイルだけを、ファイル引数モードで見る。**

```bash
bash scripts/check-md-emphasis.sh docs/progress/M32-03-completion-report.md
```

**結果＝`EXIT=0` / 検出 0 行**（1 度目は `:100` を検出したため、閉じ `**` の直前の `」` を強調の外へ出して是正した。逐語＝`**「A」**であり` → `「**A**」であり`）。

**★継続更新ファイル（`docs/progress/progress-log.md`）は渡していない。**——同ファイルは歴史記録としての既存の検出行を持ち、それらは直さない（`D-274` (3)）。

---

## 14. 設計卓への請求・申し送り

> **★★`docs/handover/followup-backlog.md` は 1 文字も編集していない**（`D-838`）**。⇒ 登録候補は設計伝達レポートへ並べる。**

### 14.1 CHANGE 原稿のたたき台（→ 設計伝達レポート **§1・§6**。★§4 ではない＝`M-183`）

| # | 内容 |
|---|---|
| **1** | **`CHANGE-192` §2 の配布アーカイブ構成へ `manual/tacpendium-quickstart.html` を足す。** ★`nightly-crossbuild.yml:109` のコメント「中身 = tacpendium.exe + README.txt」も `CHANGE-192` 後は失効している。**⇒ アーカイブ化は Releases 公開時の人の手番でスクリプトが無いため、本サブでは CI を触っていない** |

### 14.2 `followup` 登録候補（→ 設計伝達レポート **§4**）

| # | 内容 |
|---|---|
| 1 | **`CHANGE-187` §5 の「`M32` への申し送り 2 件」が `M32-03` 指示書へ届いていなかった**〔移行手順＝ショートカットの作業フォルダを変えていた利用者は `config.toml` を exe の隣へ ／ 終了導線〕**。★終了導線は本サブで書いたが、移行手順は書いていない**（射程外・利用者が既に居るかは開発者の情報） |
| 2 | **`ch03` / `ch04` にトレイ由来の追随余地が残る**——簡易パスワードの復旧導線として `CHANGE-187` §2(f) がトレイの「設定ファイルのフォルダを開く」を用意したが（**★唯一の手段ではない**——`config.toml` は `AppBaseDir()`＝配布では exe の隣にあり、exe のフォルダからも辿れる）、両章は `config.toml` を「開いて直す」としか書いていない。**⇒ 14 章不干渉のため本サブでは当てていない** |
| 3 | **`ch20` の「発生」の食い違い**（`punish-scan-response-startup-means-first-hit` と同じ面）。**開発者裁定＝触れない。⇒ 判断の記録として残す** |
| 4 | **指示書 §2.3-1 の引用先が誤り**——「`M32-02` レポート §1-1」は実際には **§3.4**（枚数 27 そのものは正しい） |
| 5 | **指示書 §2.2 の対応表の欠落 3 件**——`CHANGE-192` / `CHANGE-207` / `ch11` × `CHANGE-199`。**⇒ 表を直すのは設計卓の手番である**（指示書 §4.5） |
| 6 | **`M32-02` 本文執筆後に着地した CHANGE は 20 本であり、指示書が前提とした 15 本より多い。**⇒ 次に同型の追随サブを起票するときは、`git log --diff-filter=A -- docs/change-notes/` で境界を機械的に引くとよい |

### 14.3 新たに気づいた `UI の粗`（→ 設計伝達レポート §4。`D-382`）

**★`M32-02` の 28 件は上書きしていない。以下は*追記*の候補である。**

| # | 章 | 粗 |
|---|---|---|
| **F-1** | `ch05` | **修飾の札と波括弧付き文字列が混ざる状態を、利用者が「キャッシュ再構築を押す」以外に解消できない。**⇒ 画面側に「古い行が残っています」を示す手掛かりが 1 つも無く、**説明書が note で肩代わりしている**（`M32-02` §4.5「本文が弁解を負った箇所」と同じ型） |
| **F-2** | `ch08` | **「持続当て（始動技）」と修飾の「持続当て」が同じ画面に出るのに、画面上で区別が付かない。**⇒ 括弧書きの 1 語だけが手掛かりであり、**説明書が `caution` を 1 つ立てる必要があった**（同上の型） |
| **F-3** | `ch08` | **入力方式の選択が保存もブラウザ保持もされない**（`CHANGE-196`）**。⇒ 毎回「通常入力」へ戻る。マス目で入れ続ける利用者は毎回 1 回多く押す** |

---

## 15. レビュー結果（Phase B / C）

**レビュー報告書＝`docs/progress/m32-03-review.md`**（Phase B の fresh subagent が作成。トリアージ結果は同書末尾の「## 取り込み結果（自動トリアージ）」にある）

### 15.1 指摘の件数と優先度別の内訳

| 優先度 | 件数 | 採用 | 不採用 |
|---|---:|---:|---:|
| **高** | **2** | **2** | **0** |
| 中 | 7 | 6 | 1 |
| 低 | 5 | 1 | 4 |
| **計** | **14** | **9** | **5** |

> **★「中」を 7 件と数えている**。レビュー本文は「中 — 6 件」と見出しに書いているが、**5 番目が (i) / (ii) の 2 件に分かれており、採否も割れた**（(i) 採用 / (ii) 不採用）。⇒ 独立に数えた。

### 15.2 ★★★「高」指摘の不採用は **0 件**である

**⇒ Phase C の安全弁（重大指摘の自動棄却時の開発者エスカレーション）は発動していない。**

| 指摘 | 内容 | 措置 |
|---|---|---|
| **高-1** | **`ch05` の絞り込みの中立ラベル「指定なし」が実装と違う。**実装は `comboList.filter.all` ＝ **「全て」**（`ComboListFilters.tsx:451`）。`CHANGE-209` §4 の*状態の説明語*を表示語として写した誤り。**★同じ `ch05` の 4 段落下で同じラベルを「全て」と書いており、本文内で 2 通りになっていた** | **採用**。「全て」「持続当て（始動技）」「通常始動」の 3 つへ是正 |
| **高-2** | **`README.txt` が `CHANGE-192` §2 の構成図に無いファイル（クイックスタート）を名指ししている**。アーカイブは人手で組むため dead reference になりうる | **採用**。配布 README が名指しするのは `manual/tacpendium-readme.html` だけにし、クイックスタートへはその冒頭のリンク経由で送る形にした |

**★★高-1 は指示書 §4.5 の警告（「設計卓が出した対応表を信じきるな」）が*製造の側でも*当たった例である。⇒ 製造は `CHANGE-209` の記述を実 UI ラベルとして写しており、実装を引いていなかった。**

### 15.3 採否の一覧

**★各指摘の採否と理由は、レビュー報告書末尾の「## 取り込み結果（自動トリアージ）」に全 14 件ぶん記してある**。要約:

- **採用 9 件**＝高-1 / 高-2 / 中-1（`onBlur` の前提が成立しない） / 中-2（「キャッシュ再構築」） / 中-3（クイックスタートのフォルダ違い） / 中-4（分量を詰めた） / 中-5(i)（`ch20` は札を通らない） / 中-6（索引行＝Phase D） / 低-4（報告の「唯一の到達手段」）
- **不採用 5 件**＝中-5(ii)（`ch07`/`ch11` への送り） / 低-1（章数「22」の散在） / 低-2（CSS の二重化） / 低-3（退役 modifier 3 値） / 低-5（修飾位置の非対称の 1 文）。**いずれも理由を報告書へ記した**

### 15.4 ★★取り込みで新たに分かったこと — **指示書 §2.5.3 の 3 条件は同時に満たせない**

| 条件 | 状態 |
|---|---|
| 節 5 つまで | **満たす** |
| 各節 3 行程度 ＋ 図 1 枚まで | **満たす**（最長の節で約 5 行、図は芯の節に 1 枚） |
| **全体が 1 画面に収まること** | **★満たさない。概算 約 1,389px**（ノート PC のビューポート高 ≒ 800〜900px） |

**★算術上の下限**＝ 5 節 ×（3 行 × 26.6px ＋ 見出し・余白 70px）＋ ヘッダ 約 150px ＋ フッタ 約 90px ＝ **図が 0 枚でも約 990px**。**⇒ 「節 5 つ」「各節 3 行」「図 1 枚」を満たしたまま 1 画面へ収めることはできない。**

**★これ以上詰めると指示書が求める節そのものを落とすことになる**（芯の節 3 は `I-3` が保護し、残る 4 節はいずれも道順の一部）。**⇒ 製造の判断では閉じない。設計伝達レポート §4 へ「指示書の上限の内部矛盾」として出す。**

### 15.5 再レビューの往復

**0 回**（初回レビューのみ）。**⇒ 停止規律の上限 2 回には達していない。**

### 15.6 ★レビューが独立に確かめたこと（製造の主張の裏取り）

**★以下はレビューが実物を引いて確認し、**製造の主張が正しいと判定した**ものである。**

- **`ch20` を「影響なし」で通していないこと**——本文の「発生」0 件・「候補」4 件がいずれも件数を述べていないことを再計数。`DES-005` §5.20 も「除外を利用者へ見せる表現は持たせていない」と明記している
- **`ch08` の「7 つ」**——`duplicate_keys.go:33-42` の 8 要素から `character_id` を除いた 7 語と、本文の 7 語が 1 対 1 で一致
- **トレイが Windows のみであること**——`desktop_other.go` の `//go:build !windows` スタブ。メニュー 6 項目も `tray.go:34-40` と逐語一致
- **クイックスタート節 2 の差し替えの妥当性**——キャラ・技は seed 投入済み。`ch16` はコンボの CSV 取込であって別物
- **射程**——14 章の差分は `ch03` の 1 文のみ、本編は `+23/-3` で削除 3 行はすべて置換の片側 ＝ **縮んでいない**。新規ファイルは `+127/-0`
- **走査 4 本の件数を独立に再計数して全一致**

### 15.7 ★レビューが判断できないとした 2 件

| # | 内容 | 製造からの回答 |
|---|---|---|
| 1 | `ch03` の 1 文と `dist-readme.txt` の是正が「開発者の明示指示による」ことをリポジトリ上で検証できない | **そのとおりである。**指示は本セッションの対話で与えられており、ボード・コミットに裁定行は無い。**⇒ 設計伝達レポート §4 で受理の手番に記録を請求する** |
| 2 | クイックスタートが 1 画面に収まるかはレンダリングしていないため断定できない | **製造も描画していない**。§15.4 の概算は CSS の寸法からの計算であり実測ではない。**⇒ 開発者の目視を要する**。ただし算術上の下限（図 0 枚でも約 990px）は描画に依らない |

---

*以上、M32-03 完了報告。*
