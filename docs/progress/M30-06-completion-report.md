# M30-06 完了報告: ファミリー行の代表名が 1 メンバーの名前になっている

| 項目 | 内容 |
|------|------|
| 作業ID | **M30-06** |
| 指示書 | `docs/instructions/M30-06-family-label-representative-name.md` **v1.0.0** |
| チェックリスト | `docs/instructions/reviews/M30-06-review-checklist.md` **v1.0.0** |
| 実施日 | 2026-09-12 |
| 着手基点 | `6cd4ce5` |
| ブランチ | `claude/quirky-pasteur-g7ol0x` |
| CHANGE 消費 | **0 本**（**★製造は自採番しない**＝`D-293`。**請求は §6**） |
| マイグレ消費 | **0 本** |
| スキーマ変更 | **0** ／ 新規依存 **0** |

---

## 1. 何をしたか

**弱/中/強が選べるファミリー行なのに、行の名前が最弱メンバーの名前で固定されていた**（cammy 4 行）。代表名の選択が `light → none → medium → heavy → od` の優先で **1 メンバーの `nameJa` をそのまま採る**ためである。

**⇒ ファミリーに属するメンバーの表示名を突き合わせ、差が強度語だけのときにその強度語を落とす**ようにした。

---

## 2. ★★★段 1 — 対象の全数を実測で起こす

**★指示書 §4.3 の指示どおり、`M30-05` の「4 行」は写しとして扱わず数え直した。⇒ 結果は一致した。**

### 2.1 メンバー間で表示名が割れている raw ファミリー（実測 10 件 / 母集団 raw 397 件）

| # | キャラ/ファミリー | 差分の中身 | 強度語の位置 | 畳んだか |
|---|---|---|---|---|
| 1 | akuma/`demon_swoop_side_switch` | `""` ／ `中強百鬼襲・` | 丸括弧の中 | **畳まない** |
| 2 | c_viper/`focus_force_forward_step` | `""` ／ `OD` | 先頭装飾の中 | **畳まない** |
| 3 | cammy/`razors_edge_slicer` | `""` ／ `OD` | 丸括弧の中 | **畳まない** |
| **4** | cammy/`cannon_strike_hooligan_combination` | `弱` ／ `中` ／ `強` | **丸括弧の中** | **★畳む** |
| **5** | cammy/`reverse_edge_hooligan_combination` | `弱` ／ `中` ／ `強` | **丸括弧の中** | **★畳む** |
| **6** | cammy/`fatal_leg_twister` | `弱` ／ `中` ／ `強` | **丸括弧の中** | **★畳む** |
| **7** | cammy/`silent_step` | `弱` ／ `中` ／ `強` | **丸括弧の中** | **★畳む** |
| 8 | guile/`sonic_cross` | `""` ／ `１` | 強度語ではない | **畳まない** |
| 9 | guile/`perfect_timing_sonic_cross` | `""` ／ `１` | 強度語ではない | **畳まない** |
| 10 | luke/`flash_knuckle_holding` | `""` ／ `(ホールド)` | 強度語ではない | **畳まない** |

- **判別できない行は 0 件である**（§2.1-4）。10 件すべて差分の中身を 1 件ずつ確認した。
- **★1 件は行にならない**——`luke/flash_knuckle_holding` は `flash_knuckle` の `holding` 変種として基底へ畳まれる。代表名は `plain` から採られるため表に出ない。**⇒ 握り潰さず、テストで件数として固定した。**
- **★★畳む 4 件だけが「差分が強度語そのもの」である。** 残り 6 件は差分に空文字か強度語でない語を含むため、規則が成立しない。

### 2.2 対象 4 行（実測・変更前 → 変更後）

| キャラ/ファミリー | 変更前 | 変更後 |
|---|---|---|
| cammy/`cannon_strike_hooligan_combination` | キャノンストライク(**弱**フーリガンコンビネーション派生) | キャノンストライク(フーリガンコンビネーション派生) |
| cammy/`reverse_edge_hooligan_combination` | リバースエッジ(**弱**フーリガンコンビネーション派生) | リバースエッジ(フーリガンコンビネーション派生) |
| cammy/`fatal_leg_twister` | フェイタルレッグツイスター(**弱**フーリガンコンビネーション派生) | フェイタルレッグツイスター(フーリガンコンビネーション派生) |
| cammy/`silent_step` | サイレントステップ(**弱**フーリガン派生) | サイレントステップ(フーリガン派生) |

### 2.3 ★★★反例群の全数（指示書 §2.1-3）

**★指示書は「cammy の OD 兄弟 3 行が既知だが、他に無いかを実測で確かめる」と指示していた。⇒ 他に在った。**

**素朴な案 (a)** を当てると同一表示になる組は、**実測 5 組 13 ファミリー**である。

| # | キャラ | ファミリー | 素朴な案 (a) での表示 |
|---|---|---|---|
| 1 | cammy | `cannon_strike_{light,medium,heavy}_od_hooligan_combination` | キャノンストライク(ODフーリガン派生) |
| 2 | cammy | `fatal_leg_twister_{light,medium,heavy}_hooligan_combination` | フェイタルレッグツイスター(ODフーリガンコンビネーション派生) |
| 3 | cammy | `silent_step_{light,medium,heavy}_od_hooligan_combination` | サイレントステップ(ODフーリガン派生) |
| 4 | cammy | `reverse_edge_hooligan_combination` ／ `reverse_edge` | リバースエッジ(フーリガンコンビネーション派生) |
| 5 | elena | `lynx_whirl_spinning_scythe` ／ `lynx_whirl_od_spinning_scythe` | リンクスワール(スピンサイズ派生) |

**★★★既知だったのは #1 だけである。⇒ 実測で 4 組 10 ファミリーが追加で見つかった。**

**★★★これらが採った方式で潰れない理由は 2 つある。「メンバーが 1 件だから」ではない**——**13 件のうち 3 件は 3 メンバーを持つ**（実測。`cammy/reverse_edge_hooligan_combination` ／ `elena/lynx_whirl_spinning_scythe` ／ `elena/lynx_whirl_od_spinning_scythe`）。

| # | 理由 |
|---|---|
| **1** | **畳む条件が見るのは「メンバー数」ではなく strip 後の*相異なる*名前の数である。⇒ elena の 2 件は弱/中/強が接頭形であり strip で揃うため、相異なる名前が 1 つしかない** |
| **2** | **反例の兄弟どうしは別ファミリーに居る。⇒ 畳む判定はファミリーの中しか見ないため、ファミリーをまたぐ区別**（`OD` 等）**に触れない。畳んだ後も区別が残る** |

**★★★したがって `cammy/reverse_edge_hooligan_combination` は「畳む対象」でありながら反例表にも載る。矛盾ではない**——**同行は畳まれるが、隣の `reverse_edge`**（リバースエッジ(ODフーリガンコンビネーション派生)）**とは `OD` で区別が残る。**

**★★★「落としすぎないこと」を担保しているのは畳む条件そのものではない。⇒ 372 行全数の重複組テストである**（§4-5）**。**

> **★★本欄は 1 巡目レビュー「高-2」で是正した。** 初稿は 4 か所で「13 ファミリーはいずれも 1 メンバー」と書いていた。**結論は正しかったが機序が違う。⇒ そのまま残すと後任が「多メンバーのファミリーは反例になりえない」という誤った不変条件を受け取る。**

---

## 3. ★★段 2 — 方式の選択

### 3.1 ★★★素朴な案 (a) は成立しない（実測）

指示書 §0.4 は **(a) を先に試せ**とし、§0.3 の反例に当たらないかを実測で確かめよと言う。**⇒ 試した。当たる。**

| 観測 | 着手時点 | 素朴な案 (a) を当てたとき |
|---|---:|---:|
| 同一表示になるファミリーの組 | **1 組** | **6 組** |
| 区別できなくなるファミリー | 2 件 | **15 件** |

**⇒ 新たに 5 組 13 ファミリーが区別できなくなる**（着手時点の 1 組は既存＝§6-1）**。★`M30-05` の実機確認 C が確認した形を壊す。**

**★機序＝丸括弧の中の強度語は `DES-004` §2.1 の規約どおり派生元の強度であり、これらの行ではそれが「どの派生元から出る技か」を示す唯一の情報である。⇒ 位置で落とすと情報そのものが消える。**

### 3.2 採った方式 — (a′) 判定を位置ではなくデータに置く

**★★(b)（代表名を別フィールドで持つ）へは行かなかった。⇒ (a) の判定条件だけを替えれば足りる。**

> **代表名を作るとき、ファミリーに属するメンバーの表示名を突き合わせ、差が強度語だけのときに限ってその強度語を落とす。**

**★★★丸括弧という位置を一切見ない。⇒ 見るのは「そのファミリーの中で何が変わるか」だけである。**

| なぜ両立するか | |
|---|---|
| **対象 4 行** | `(弱…)` `(中…)` `(強…)` の 3 メンバーを持つ。差は強度語だけ → **畳む** |
| **反例 13 件** | strip 後の相異なる名前が 1 つ（10 件は 1 メンバー、3 件は接頭形で揃う）→ **畳まれない**。畳まれる 1 件（`reverse_edge_hooligan_combination`）も兄弟とは `OD` で区別が残る |

**★落とすのは「その行の中で既にボタンとして選べる情報」だけである。行を区別している情報は、定義上ファミリーをまたぐので本規則には見えない。**

### 3.3 指示書の禁止事項との関係

| # | 禁止事項 | 本サブでの扱い |
|---|---|---|
| 1 | **丸括弧の中の強度語を表示名そのものから落とす**（§3-1） | **★していない。** `stripStrengthLabel` の差分は **0 行**。落としたのは代表名を組み立てる瞬間だけであり、**ステップに入る技名は不変である**（E2E 2 本目で実測） |
| 2 | `M30-05` の 3 形の規則を変える（§3-2） | **★していない。** `DES-005` §6.1 の 3 形は 1 つも変えていない |
| 3 | `family` 識別子を変える（§3-3） | **★していない。** digest 同一（§4） |
| 4 | `name_ja` / `move_code` / `preset_aliases` を触る | **★していない。** `character_data/` `migrations/` `internal/` `cmd/` の差分は **0 バイト**（実測） |

**⇒ 止まって請求する 2 条件（指示書 §7）はどちらも発動しなかった。**

---

## 4. ★★段 3 — 入れて測る

**★下表はすべて実測である**（seed CSV 31 キャラを実装で読ませて取得。算出値は 1 つも無い）。

| # | 観測 | 変更前（実測） | 変更後（実測） | 判定 |
|---|---|---|---|---|
| 1 | **ファミリー識別子 digest** | `b54d118393a8f981…` | `b54d118393a8f981…` | **★★★同一**（完了条件 5・B-1） |
| 2 | ファミリー行の総数 | 372 | **372** | 不変（完了条件 6） |
| 3 | `is_derived` の並び | 194:178 | **194:178** | 不変（完了条件 6） |
| 4 | 丸括弧内に強度語を持つ行 | 22 | **18** | 減ったのは対象 4 行ちょうど |
| 5 | 同一表示になるファミリーの組 | 1 組 | **1 組** | **増えていない** |
| 6 | 表示名ラベル digest | `906378a0ae1efdea…` | `a7ee6db54b6a94c5…` | 動いたのは 4 行のみ（目視確認済み） |

- **★★陽性対照**（§2.3-2・C-5）: 入れる**前**に対象 4 行の代表名が強度語を含んでいたことを実装で測ってから入れた（上表の「変更前」列）。**さらに床としては、seed CSV の `name_ja` そのもの 12 行が強度語つきで登録されていることを、実装と独立な軸で固定した。**
- **★★対の主張**（§4.1・A-2）: 「対象 4 行が変わった」と「反例群が区別できるまま」を**両方**測っている。**片方だけでは過剰除去が緑になる。**
- **★★★さらに床自身が反例の実在を証明する**——新規テストの 1 本が「素朴な案 (a) を当てると 5 組が同一表示になる」ことを測る。**⇒ 反例が机上の懸念ではないことと、(a) を採らなかった理由が、床として残る。**

---

## 5. 変更したファイル

```
web/e2e/m30-06-family-label-representative-name.spec.ts        +86  -0
web/src/features/combo/inputResolution.test.ts                 +111 -0
web/src/features/combo/inputResolution.ts                      +73  -0
web/src/features/combo/specialFamilyLabel.roster.test.ts       +21  -8
web/src/features/combo/specialFamilyRepresentativeName.roster.test.ts +270 -0
```

**★新規 4 ファイルはいずれも deletions が 0 である**（`E-225` の観点で読んだ）**。⇒ 上書きで既存の行を消していない。** 唯一の deletions 8 行は、意図して更新した共有テストの期待値である。

### 5.1 ★★共有テストを何をどう変えたか（完了条件 9・E-1）

**`specialFamilyLabel.roster.test.ts`**（`M30-05` の床 10 本）**——10 本すべて残っている。本サブが意図して動かす数値だけを直した。**

| テスト | 変更 | 理由 |
|---|---|---|
| (B) 群の件数 | **22 → 18** | 対象 4 行が代表名に「弱…派生」を採らなくなったため。**★丸括弧の中を見る規則は 1 文字も変えていない** |
| `EXPECTED_PRESERVED` | cammy/`cannon_strike_hooligan_combination` の 1 件を**外した** | 同行は本サブの対象である。新しい表示名は新規テストが逐語で固定する |
| 表示名 digest | `906378a0…` → `a7ee6db5…` | **★差分を目で見て、動いたのが対象 4 行だけで他 368 行が 1 文字も動いていないことを確認してから貼り替えた**（テスト自身の警告に従った） |
| 識別子 digest ／ 総数 372 ／ 194:178 | **変えていない** | **赤にならずに緑のまま通った** |

**`moveSurfacing.roster.test.ts` は 1 文字も触っていない**（ラベルを 1 件も見ていないため）。

---

## 6. ★★併せて更新が要るもの

| # | 項目 | 状態 |
|---|---|---|
| 1 | **CHANGE 番号の請求** | **★★未消費。設計卓へ請求する**（下記 §6.2 に中身） |
| 2 | 消費したマイグレ連番 | **0 本。** ボード §2.2 の「次に払い出す番号」は動かない |
| 3 | 版を上げた文書の参照元 | **なし。** 設計書を 1 冊も編集していない |
| 4 | 番号の写し先 4 か所 | **今回は該当なし**（番号を消費していないため）。**★設計卓が `190` を消費する手番で 4 か所を揃えること** |

### 6.2 ★★★CHANGE 請求の中身（`DES-005` §6.1 へ 1 本）

**★製造は自採番しない**（`D-293`）**。レジストリは `190` を `M30-06` 用に見込んでいる**（`D-840`）**。設計書本体は編集していない。**

**★★新規則を足すだけでは足りない。同節に失効が 2 か所できる。⇒ 併せて直さないと残る。**

| # | 箇所 | 現在の記述 | 要る手当て |
|---|---|---|---|
| **A** | **`DES-005` §6.1 末尾** | 「残っている課題が 2 つある。**(1) 代表名が 1 メンバーの名前そのものになる行が 4 件ある**〔cammy〕⇒ `M30-06` の射程」 | **★解消済み。⇒ 撤回が要る**（残る課題は `P-56` の 1 つになる） |
| **B** | **`DES-005` §6.1 の位置別の表** | 「丸括弧の中 ／ 派生元の強度 ／ **22 行** ／ 落とさない」 | **★ファミリー行としては 18 行**。**⇒ ただし `stripStrengthLabel` の出力としては 22 行のままである。どちらを指すかを書き分けること** |

**★★足す規則の中身**: 代表名は、ファミリーに属するメンバーの表示名の差が強度語だけのときに限りその強度語を落とした共通形とし、それ以外は優先順の 1 件とする。**★丸括弧という位置は見ない。⇒ `DES-004` §2.1 の規約は 1 文字も変わらない。**

### 6.1 ★★申し送り — lily に着手前からの同一表示が 1 組ある

**本サブの射程外であり、本サブが作った差でもない。⇒ 実測の過程で見つけたので残す。**

- lily `windclad_condor_dive_follow_up` と `condor_dive_follow_up` が**どちらも「コンドルダイブ(派生)」と表示される**（着手前から）。
- **★機序＝seed の `name_ja` が「ODコンドルダイブ(派生)」と「コンドルダイブ(派生)」であり、`[風纏い]` が入っていない。** 前者は `stripStrengthLabel` の接頭形規則で `OD` が落ちるため、両者が同一になる。
- **★同キャラの `windclad_condor_dive` には `[風纏い]` が入っている。⇒ 派生行だけ抜けている。** 直すなら seed 側であり、`name_ja` を触るので番号の請求が要る。
- **★本サブでは「増えていないこと」だけを床で固定した。**

---

## 7. 検査結果

| # | コマンド | 結果 |
|---|---|---|
| 1 | `bash scripts/check-artifact-integrity.sh` | **違反なし**（検査 15 件 ／ 生成物 4 件） |
| 2 | `cd web && pnpm test` | **229 ファイル 2810 本 green** |
| 3 | `cd web && pnpm tsc --noEmit` ／ `pnpm lint` | **0**。**★両者は同一である**（`web/package.json` の `lint` は `tsc --noEmit`。⇒ eslint は回っていない）**。`eslint-disable` ／ `nolint` の追加は diff 上 0 件で確認した** |
| 4 | **`make e2e`（全数）** | **307 passed**。`m30-05-family-label-strength-word.spec.ts` の 3 本を含めて緑 |
| 5 | `bash scripts/check-import-order.sh` | **違反なし。★ベースラインは下げていない**（`D-388`。現在 99 / ベースライン 101） |
| 6 | `bash scripts/check-md-emphasis.sh`（本手番の新規 md） | §7 末尾に記載 |

---

## 8. ★★★実機確認 — **返った（2026-09-12・問題なし）**

**★★開発者が実機で確認し、下記 5 項目について「問題なかった」と判定した。⇒ 本節は閉じている。**

**★判定の粒度は一括である。⇒ 項目ごとの内訳は得ていないため書かない。**

**★★とくに項目 ⑤**（`(フーリガン派生)` という残し方が読みやすいか）**は設計判断であった。⇒ これが OK になったことで、`DES-005` §6.1 への反映に保留事項は無くなった**（設計伝達レポート §4-6）**。**

以下は確認した内容の証跡として残す。**本件は開発者が実機で見つけた問題であり**（指示書 §7・`CLAUDE.md` §9 のハード列）**、直った形の確認も開発者の手番であった。⇒ 製造は「直った」と判定していない。**

1. 仮想コントローラで **cammy** を選び、必殺技タブを開く。
2. **対象 4 行の名前から「弱」が消えていること**を見る。
   - サイレントステップ(フーリガン派生)
   - キャノンストライク(フーリガンコンビネーション派生)
   - リバースエッジ(フーリガンコンビネーション派生)
   - フェイタルレッグツイスター(フーリガンコンビネーション派生)
3. **★同じ画面で、次の行が区別できるままであること**を見る。
   - サイレントステップ(弱ODフーリガン派生) ／ (中ODフーリガン派生) ／ (強ODフーリガン派生)
   - リバースエッジ(ODフーリガンコンビネーション派生)
4. **対象行をクリックし、中や強を選ぶと、ステップには従来どおり強度語つきの技名が入ること**を見る（例: サイレントステップ(中フーリガン派生)）。
5. **★「これは違う」と思ったら知らせてほしい。** 特に **(フーリガン派生)** という残し方が読みやすいかどうかは実機の判断による。

---

## 9. レビュー結果と取り込み

**レビュー報告書**: `docs/progress/m30-06-review.md`（1 巡目）
**再レビュー往復**: **0 回**（上限 2 回。**高 5 件をすべて採用したため往復は発生していない**）

| 優先度 | 件数 | 採用 | 不採用 |
|---|---:|---:|---:|
| **高** | **5** | **5** | **0** |
| 中 | 3 | 3 | 0 |
| 低 | 4 | 3 | 1 |
| **計** | **12** | **11** | **1** |

**★★★「高」指摘の不採用は 0 件である**（実測。⇒ エスカレーションは発生していない）**。**

### 9.1 採否と理由

| # | 優先度 | 指摘 | 採否 | 理由・対応 |
|---|---|---|---|---|
| 高-1 | 高 | `progress-log.md` の索引行が無い | **採用** | Phase D で追記。`check-progress-log-index.sh` の緑を確認 |
| 高-2 | 高 | 「反例 13 件はいずれも 1 メンバー」は事実ではない | **採用** | **★実測で確認した。3 件が多メンバーであり、うち 1 件は畳んだ対象行そのものであった。⇒ 結論は正しいが機序が違う。** 4 か所（本報告 §2.3 / §3.2、実装 doc、テスト doc）を正しい機序へ書き換えた |
| 高-3 | 高 | `stripStrengthLabel` の doc が失効（例・22 行） | **採用** | 例を `_od_` 側の行へ差し替え、件数を 18 へ。**★併せて「本関数の規則は変えていない」を明記した** |
| 高-4 | 高 | `SpecialFamily.nameJa` の注記が失効 | **採用** | 「plain 変種の最弱強度の `nameJa`」は畳めた場合に成り立たない。正しい定義へ書き換えた |
| 高-5 | 高 | CHANGE 請求が失効箇所を名指ししていない | **採用** | §6.2 を新設し、`DES-005` §6.1 の失効 2 か所（末尾の残課題 (1) ／ 位置別表の 22 行）を名指しした |
| 中-1 | 中 | 落とした強度語とメンバー自身の強度の対応を誰も測っていない | **採用（案 b）** | roster テストへ 12 行の対応を固定する床を追加。**★案 (a)**（実装で強度一致を要求）**は採らなかった**——将来「派生元の強度と行の強度軸が別物」の形が出たとき窮屈になるため。レビューの推奨とも一致 |
| 中-2 | 中 | 「素朴な案 (a)」の定義が 1 種類に固定されていない | **採用** | テストの `NAIVE_DROP` に定義を明記。**★全部落とす定義なら 6 組 17 件になることも併記した** |
| 中-3 | 中 | 畳み直しループが変種 raw ファミリーも対象 | **採用（コメント）** | **★実装は変えなかった**——`plain` に限ると `entry.nameJa == null` の経路で挙動が変わりうる。**現状その経路に入る割れた変種は 1 件のみで畳まれない**ことを実測し、射程をコメントで固定した |
| 低-9 | 低 | `STRENGTH_WORDS` が `STRENGTH_LABELS` の形に暗黙依存 | **採用** | 4 語すべてが畳めることを測る単体テストを追加。**★変異注入で検証した**——`STRENGTH_LABELS` を `"弱|中|強|(?:OD)"` にすると当該 1 本だけが赤になる（roster テストは緑のまま） |
| 低-10 | 低 | `pnpm lint` の実体は `tsc --noEmit` | **採用** | §7-3 の記述を是正した |
| 低-11 | 低 | `roles-and-routing.md:116` が `CLAUDE.md` §9（`D-838`）と食い違う | **★不採用** | **★本サブが作った差ではなく、射程外である**（指示書 §3）。**⇒ 握り潰さず、§10 と progress-log の横断課題へ申し送る。** 設計卓の手番 |
| 低-12 | 低 | §4 の表に実測/算出の書き分けが無い | **採用** | 表の全行が実測であることを明記した |

### 9.2 ★不採用 1 件の扱い

**低-11 のみ不採用。⇒ 優先度「低」であり、`CLAUDE.md` §9 の「進めて報告する」に従い理由を記載して進めた。** 内容は `docs/handover/roles-and-routing.md` の記述が 2026-09-12 の `D-838`（停止時記録の記録先を設計伝達レポート §4 へ一本化）に追随していないというもので、**レビュー担当が最初に読む資料であるため実害がありうる。⇒ 設計伝達レポート §4 の候補として出す。**

## 10. 停止時記録（§J 行の原稿）

**★★該当なし。** 上限（往復上限 2 回 ／ タイムボックス ／ 終了指示）に達しておらず、未解消の項目も無い。**⇒ §6-1 の lily は「未解消」ではなく射程外の申し送りであり、設計伝達レポート §4 の新規登録候補として出す。**

---

*以上、M30-06 完了報告。*
