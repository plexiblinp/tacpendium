# M31-02 レビュー報告書

| 項目 | 内容 |
|------|------|
| 対象 | `M31-02`（共通画面部品 — キャラ選択の入力挙動） |
| 指示書 | `docs/instructions/M31-02-shared-character-picker.md` v1.0.0 |
| チェックリスト | `docs/instructions/reviews/M31-02-review-checklist.md` v1.0.0 |
| 完了報告 | `docs/progress/M31-02-completion-report.md` |
| 対象差分 | `git diff 50235ce`（コミット `b838017`）＝ 2 ファイル / 269 insertions / 1 deletion |
| レビュー実施日 | 2026-09-08（初回） |

---

## 総評

指示書が `M27-02a` の既存裁定を引き継いでいないことを着手時に見抜き、実装へ入る前に停止して開発者裁定を取り直した判断は正しい。`CLAUDE.md` §8 の設計書間矛盾の扱いそのものであり、盤面（`D-686`）を実際に照合したところ製造の主張はすべて裏付けが取れた。`P4M-013` は「正しい挙動」を開発者の逐語で先に言語化してから直しており、チェックリストが最も警戒していた 2 つの形（推測で直す・部品が複数あるのに片方だけ直す）はいずれも回避されている。修正そのものは 1 関数・28 行の追加に留まり、見た目にも部品構造にも触れていない。

一方で、**修正前にどの方向が壊れていたかについて、コード内コメントと E2E spec のヘッダコメントが完了報告およびコミットメッセージと食い違っている**。これは陽性対照の記述そのものの食い違いであり、動作は正しいままなのでテストも型検査も緑になる。読む以外に見つける経路が無く、後任が spec をコピーして注記だけ引き継ぐ形で複製される。優先度「高」として扱う。

もう 1 件、2026-09-08 の開発者裁定がリポジトリ上のどこにも記録されておらず、`parallel-board.md` には `D-686` の「`M31` から `P4M-012` / `P4M-013` を引く」だけが生きたまま残っている。完了報告 §5 の請求リストにも「裁定そのものの登録」が入っていない。

重大の判定基準 8 件のいずれにも当たらない。完了承認を妨げる問題は無い。

---

## 設計準拠性レビュー結果

### 束 A — 段 1 の実査 5 件

| # | 判定 | 内容 |
|---|---|---|
| 1-1 部品の数 | ◎ | 「2 層・各 1 実体」（ラッパ＝`CharacterSelector.tsx` ／ 実体＝`SearchableSelect.tsx`）と数で書かれている。レビュー側でも `grep -rn "<SearchableSelect"` を再実行し、実体を通らないキャラ選択が 0 件であることを確認した。さらに波及先がキャラ選択 14 に留まらず 17 コントロールであることを自分で数え直し、それを破壊確認の区分 5 の根拠にしている点は、チェックリストの要求水準を超えている |
| 1-2 画面の全数 | ◎ | 14 コントロール / 12 ファイル / 12 節。呼び出し元をファイル名と行番号で列挙している。レビュー側で 14 件すべての行番号を照合し、`DES-005` の節見出し（§5.1 / §5.4 / §5.5 / §5.7 / §5.8 / §5.11 / §5.15 / §5.16 / §5.18 / §5.19 / §5.20 / §5.21）とも突き合わせて一致を確認した |
| 1-3 `P4M-012` の再現手順 | ○ | `M27-02a` §1.1 の実測を引いており、本サブでは測り直していない。裁定がクローズであるため妥当。ただし引用のまま終わらせず、順送りの根が `ComboEditorBasicFields.tsx:327`、キャラ欄が `ComboEditor.tsx:948` へずれた現在も構造が変わっていないことを実査している。レビュー側で両行を照合し一致を確認した |
| 1-4 `P4M-013` の言語化 | ◎ | 開発者の逐語を引用し、そこから「正しい挙動＝枠内で押下して始めたドラッグは、枠外へ出ても文字選択が維持されること」を導いている。実装より先に得ていることが §0 と §1-4 の記述順で示されている。チェックリスト §0.2-1 の最重要項目を満たす |
| 1-5 `M31-01` との交点 | ◎ | 交点無しと結論し、根拠として「`P4M-015` は `PunishListPage` / `PunishSearchPage` の呼び出し側、本サブは `SearchableSelect.tsx` の 1 ファイルのみ」を挙げている。差分が実際に 2 ファイルであることで裏が取れる |

### 束 B — `P4M-012`

**前提**: 本 identity は 2026-09-02 の開発者裁定（`M27-02a` §1 表 #4・逐語＝「キャラ欄については対象外でいい。修正しなくて良い。」）で対応不要となり、`parallel-board.md` `D-686` が「`M31` から `P4M-012` / `P4M-013` を引く」と明記していた。指示書・overview・チェックリストのいずれもこれを引き継いでいない。レビュー側で以下を確認済み。

- `docs/instructions/M27-overview.md:105` に「`M31` は `P4M-012` / `P4M-013` を持たない。⇒ `M31` の起票時に引き算すること」が実在する
- `M31-overview.md` §2.1 は identity 7 / 8 として両者を数えたままである
- `M31-overview` / `M31-02` 指示書 / レビューチェックリストの `M27-02a` 言及は 0 件（`grep -c` で各 0）

**⇒ 指示書どおりに読めば「`P4M-012` が直っていない」は重大な問題に見えるが、これは開発者裁定によるものである。判定の対象は「裁定に沿っているか」であって「直っているか」ではない。**

| # | 判定 | 内容 |
|---|---|---|
| 2-1 「効く」の定義 | ◎（裁定により無効化） | 定義を決めずに直す、という事故は起きていない。2026-09-08 裁定で恒久クローズとなり、コード変更は 0 行。完了報告 §0 の表に裁定と帰結が対で書かれている |
| 2-2 フォーム送信との二重発火 | — （該当なし） | `Enter` の経路に手を入れていないため二重発火は原理的に発生しない。差分に `onKeyDown` 系の変更が 1 行も無いことを確認した |
| 2-3 キーボード操作の到達性 | ○（据置） | 真の非対称は followup `editor-character-field-outside-field-sequence` にあり未解消のままだが、これは 2026-09-02 と 2026-09-08 の裁定で対象外。本サブが構造を悪化させていないことは §1-3 で実査されている |

### 束 C — `P4M-013`

| # | 判定 | 内容 |
|---|---|---|
| 3-1 言語化と実装の一致 | ◎ | 逐語＝「ポインタがテキストボックスの外に出ると文字選択が消える」に対し、実装は検索欄で始まったドラッグのポインタイベントを検索欄へ束ねる方向であり、狙いと手段が一致している。E2E は `selectionStart` / `selectionEnd` を直接読んでおり、言語化された量をそのまま測っている |
| 3-2 閾値の定数化 | ◎ | 閾値を導入していない。判定は `pointerType` と `button` の 2 つだけであり、新たなマジックナンバーは 0 件。E2E 側の `EDGE` / `OUT` は定数化されている |
| 3-3 タッチ操作 | △ | `e.pointerType !== "mouse"` の早期 return により、タッチとペンの経路は構造上 1 行も通らない。設計としては正しい。**ただしそれを検証する試験が 1 件も無い**。E2E は `playwright.config.ts` の projects が chromium 1 本のみでタッチ文脈を持たず、指示書 §5-5 が挙げた「タッチ操作」の実測は報告にも無い。既存の `SearchableSelect.test.tsx`（27 件）へガードの単体テストを足せば低コストで塞がる（推奨修正 中-3） |
| 3-4 推測箇所の明示 | ○ | 「なぜ左と上でだけ起きるのか」が未特定であること、原因ではなく回避であることをコード内コメントと完了報告 §2.3 の双方に明示している。`CLAUDE.md` §9-3 が求める逐語形式（「推測: 〜と仮定した」）ではないが、開示の実質は満たしている。むしろ問題は開示の形式ではなく、開示された実測値そのものが 2 か所で食い違っていることである（推奨修正 高-1） |

### 破壊確認

| # | 判定 | 内容 |
|---|---|---|
| 4-1 区分から 1 件ずつ | ◎ | 区分の割り方が完了報告 §4.3 と spec の 148〜158 行に書かれており、**なぜその区分を立てたか**まで各行にある。画面名ではなく呼び出しの形で割った理由（`M-139` の再演回避）も明記されている。とくに区分 5（`CharacterSelector` を経由しない直接利用）は、キャラ選択 14 の外にある 3 コントロールを射程へ引き込むために立てたものであり、`SUPP-001` §5.5.4 (10) の趣旨に正面から答えている。「代表を選んだ」で止めていない |
| 4-2 陽性対照 | △ | 対照の実施そのものは行われている（修正を一時的に無効化して同じ spec を回し、修正前 2 件赤 / 修正後 10 件緑）。加えて spec 内部にも「枠内では選択が出来ていること」を先に assert する形で対照が埋め込まれており、これは良い設計である。**しかし修正前に赤だった方向の記録が 2 通り存在し、食い違っている**（推奨修正 高-1）。対照の存在は確認できるが、対照の内容がどちらであるかを読み手が判定できない |

### テストの妥当性

| # | 判定 | 内容 |
|---|---|---|
| 5-1 `pnpm test` を出力で判定 | ◎ | `Test Files 217 passed (217) / Tests 2506 passed (2506)` と件数まで引用している。終了コードだけを見ていない |
| 5-2 `make e2e-only` の使用 | ◎ | `make e2e-only P=m31-02` / `P=m27-02a`。`playwright` の直叩きは無い |
| 5-3 「E2E が緑」を根拠にしていないか | ◎ | 本サブの spec は `input.selectionStart` / `selectionEnd` を `evaluate` で直接読んでおり、要素の存在ではなく入力の効き方を測っている。さらに `M27-02a` の spec が同じ identity について何を測っていなかったか（文字選択を 1 度も読んでいない）を特定し、両者が矛盾しない理由を言語化している。チェックリスト §0.3 が警戒した形を正面から扱った、本サブで最も質の高い部分である |
| 5-4 閾値の境界の両側 | — （該当なし） | 閾値を導入していないため対象外。なお実測で「popover 左端の 1px 外側」という実在の境界を特定しているが、spec は `OUT = 60` の 1 点しか踏んでいない（推奨修正 低） |

### 重大な問題の判定基準（8 件）

いずれにも該当しない。個別に確認した結果は次のとおり。

1. 言語化せずに直した → 該当なし（逐語を先に得ている）
2. 部品の数を数えていない → 該当なし（数え、レビュー側でも再確認した）
3. 区分の割り方が書かれていない → 該当なし（理由付きで 5 区分）
4. 陽性対照が無い → 該当なし（ただし記述の食い違いあり＝高-1）
5. 共通部品を作り替えた → 該当なし（1 関数追加と 1 属性追加のみ）
6. 見た目を変えた → 該当なし（`className` / レイアウトへの変更 0 行）
7. testid 未登録 → 該当なし（新規 testid 0 件。spec が使う `combo-list-character-scope` / `combo-list-tag-filter` / `combo-list-tag-filter-count` はいずれも既存）
8. `M31-01` の 9 件に手を出した → 該当なし（差分は `SearchableSelect.tsx` の 1 ファイルのみ）

### 教訓 `E-225`（新規ファイルの deletions）

`git diff --stat 50235ce` および `git diff 50235ce` を実読して確認した。

- `web/e2e/m31-02-character-picker-drag.spec.ts` = 241 insertions / **0 deletions**。上書きで消えた既存 spec は無い
- `web/src/components/SearchableSelect.tsx` = 29 insertions / 1 deletion。deletion は 1 行目の `import` 文（`type PointerEvent as ReactPointerEvent` を足すための書き換え）1 行のみであり、削られたロジックは無い

---

## 設計準拠性以外の指摘事項

### 1. 実測記述の食い違い（コーディング規約・トレーサビリティ）

同じ実測について、リポジトリ内に 2 通りの記述が存在する。

| 出所 | 記述 |
|---|---|
| `web/src/components/SearchableSelect.tsx:133` | 「**左へ出たときだけ**選択が anchor へ collapse する。右・上・下では維持される」 |
| `web/e2e/m31-02-character-picker-drag.spec.ts:16-19` | 「着手前の実測（修正前は左方向だけが赤）… 右/上/下へ60px: 維持」 |
| 完了報告 §2.2 表 #1 | 「**左と上でだけ選択が消える**。右・下は維持される」 |
| 完了報告 §2.3 | 「なぜ**左と上でだけ**起きるのか」 |
| 完了報告 §4.1 | 修正前は **2 件赤**（「左へ…」「上へ…」） |
| コミット `b838017` のメッセージ | 「左・上で再現。右・下は元から維持」「『なぜ左と上だけか』は特定できていない」 |

完了報告 2 か所とコミットメッセージ 2 か所が「左と上」で一致しており、**コード内コメントと spec ヘッダの 2 か所が失効した記述である**と判断できる。挙動は正しいままなので `tsc` もテストも E2E も緑であり、読む以外に検出経路が無い。

### 2. 2026-09-08 裁定の記録先

`parallel-board.md` を検索した範囲では、2026-09-08 の 2 件の裁定はどこにも記録されていない。盤面には `D-686`（2026-09-02）の「⇒ `M31` から `P4M-012` / `P4M-013` を引く」「部品は `M27-02a` が所有し、`M31` は利用側の追随だけを見る」が生きたまま残っている。本サブは実際には `M31` 側で部品本体を直しているため、盤面の正本と as-built が食い違う。

完了報告 §5 の請求 4 件は `followup-backlog` と `M31-overview` の是正を挙げているが、**裁定そのものを盤面へ登録する請求が無い**。番号の自採番は `D-293` で禁じられているので製造が採番するのは正しくないが、「新しい裁定番号を 1 本起こして `D-686` の該当部分を上書きしてほしい」という請求は §5 に置ける。

### 3. 同型の第 2 実装への波及が測られていない

`web/src/features/tag/components/TagSelector.tsx` は `Popover` ＋ `Input` ＋ `ul` という `SearchableSelect` と同一構造の検索欄付きドロップダウンであり（`SearchableSelect.tsx` のヘッダコメント自身が「検索欄付きの実績は付与側 TagSelector にある」と書いている）、`onPointerDown` を持たない。完了報告 §2.3 の結論が「消える条件は popover の枠の外へポインタが出ること」である以上、同じ欠陥が残っている可能性が高い。

これは `P4M-013` の射程（キャラ選択）外であり、指示書 §3-3「共通部品を作り替える」に触れないためコードへ手を出さない判断は正しい。**ただし完了報告 §7 の申し送りにも一言も無い**。射程外の同型欠陥は、申し送りに載らなければ誰も知らないまま残る。

### 4. E2E spec の脱出点が枠外である保証

`DIRECTIONS` の「下」は `exit: b.y + b.height + OUT * 2`（入力欄の下端から 120px）で脱出する。現在は検索語 `ダルシム` で候補が 1 件に絞られるため popover 高さが概ね 100px 前後に収まり、脱出点は確実に枠外へ出る。しかし popover の高さは候補件数に依存し、`ul` は `max-h-48`（192px）まで伸びる。候補が増える変更（ロケール・キャラ追加・絞り込み条件の変更）が入ると、脱出点が popover の内側に留まり、**この test は何も測らずに緑になる**。

「左」「右」「上」は popover 幅が `w-64` 固定であるため脱出点が枠外に出ることが決まっており、この問題を持たない。「下」だけが件数依存である。

### 5. スマホ専用画面の根拠の当て方

コード内コメントと完了報告が「スマホ専用画面がある＝`DES-005` §5.2」をタッチ配慮の根拠にしているが、`DES-005` §5.2 はホーム画面であり、完了報告 §1-2 が数えた 12 節に含まれていない。つまり §5.2 にキャラ選択は無い。タッチを気にすべき実際の根拠は「§5.4 以下の各画面がレスポンシブでタッチ端末から触れること」である。ガードの実装は結論として正しいので実害は無いが、根拠として引いている節は誤っている。

### 6. spec 内のリテラル

`CLAUDE.md` §4 のマジックナンバー定数化の観点。`EDGE` / `OUT` を定義しておきながら、134〜143 行（`M27-02a` の固定を壊していないことを見る test）では `b.x + b.width - 8` / `b.x - 260` / `b.y + 300` / `b.y + 340` が直値で書かれている。`8` は `EDGE` そのものである。テストコードであり指摘としては軽微。

### 7. 利用箇所の件数の食い違い（既存記述）

`SearchableSelect.tsx:19-21` のヘッダコメントは「`CharacterSelector`（14 コントロール / 13 ファイルが使う）」、完了報告 §1-1 は「14 コントロール / 12 ファイル（コメント行 1 を除く）」。レビュー側で `grep -rln` を実行したところ 13 ファイルが返り、うち 1 件は `CharacterSelector.tsx` 自身のコメント行だった。**どちらの数え方も間違ってはいない**が、同じ量に 2 つの値が並んでいる。ヘッダコメント自身が「件数を本文へ書かないこと。書くと必ず失効する」と警告している箇所でもある。本サブが持ち込んだものではないため、直すかどうかは任意。

### 8. その他

- `console.log` / `eslint-disable` / `nolint` / `any` の混入は無い
- import 順は React → サードパーティ → `@/` → 相対で規約どおり。`type` import の位置も既存行内への追記であり区分を後退させていない
- 空 `catch` にはコメントが入っており、意図的な握り潰しであることが読める
- コミットメッセージは Conventional Commits ＋ スコープ `M31-02/共通部品` で規約どおり
- 未検証の相互作用が 1 つある。選択済みテキストの上で押下してドラッグすると Chromium はネイティブのテキスト drag-and-drop を開始する。ポインタキャプチャとの相互作用（`pointercancel` による暗黙解放に落ちるか）は測られていない。実害の可能性は低い

---

## 推奨修正（優先度別）

### 高（M31 完了前に修正必須）

- **高-1: 失効した実測記述を 2 か所直す。** `web/src/components/SearchableSelect.tsx:133` の「左へ出たときだけ／右・上・下では維持される」と、`web/e2e/m31-02-character-picker-drag.spec.ts:16-19` の「修正前は左方向だけが赤／右・上・下は維持」を、完了報告 §2.2・§4.1 およびコミットメッセージと同じ「左と上」へ揃える。両者が別の測定（着手前の探索的計測と、press / exit を対にした spec の計測）を指しているなら、**どちらの計測についての記述かを 1 行で書き分ける**。いま同じ量に 2 つの答えが並んでおり、後任は spec をコピーしてヘッダの注記を読まないまま複製する。
- **高-2: 2026-09-08 裁定の盤面登録を請求リストへ足す。** 完了報告 §5 の「設計卓へ請求するもの」へ 1 行を追加し、`D-686` の「`M31` から `P4M-012` / `P4M-013` を引く」「部品は `M27-02a` が所有し `M31` は利用側の追随だけを見る」が 2026-09-08 の裁定で部分的に更新されたことを、新しい裁定番号で盤面へ記録するよう求める。番号は自採番しない（`D-293`）。あわせて完了条件 8 の設計伝達レポート §4 にも同内容を載せる。現状、この裁定はリポジトリ内では完了報告 1 か所にしか存在しない。

### 中（M31-03 着手と並行可）

- **中-3: タッチ／ペンのガードに試験を 1 件足す。** `web/src/components/SearchableSelect.test.tsx` へ、`HTMLElement.prototype.setPointerCapture` を差し替えたうえで `pointerType: "touch"` / `button: 2` では呼ばれず、`pointerType: "mouse"` / `button: 0` では呼ばれることを見る単体テストを追加する。E2E は chromium 1 プロジェクトのみでタッチ文脈を持たないため、ガードが外れても現在は誰も気付けない。指示書 §5-5 とチェックリスト 3-3 が要求した観測点をここで塞げる。
- **中-4: 「下」方向の脱出点が popover の外であることを assert する。** `document.elementFromPoint` で popover の外であることを確かめるか、popover の `boundingBox` から脱出点を算出する。現状は候補件数に依存して静かに vacuous になりうる。これは本サブ自身が警戒した `M-139` の形（守られている区分ばかりを試す）と同型である。
- **中-5: `TagSelector` の同型欠陥を申し送りへ足す。** 完了報告 §7 へ 1 行。射程外であるためコードには手を出さず、`followup-backlog` §J もしくは設計伝達レポート §4 の候補として出す（本表への登録は設計卓の手番＝`D-382`）。

### 低（将来対応）

- 低-6: `DES-005` §5.2 を根拠に引いている箇所（コード内コメントと完了報告 §2.4）を、実際の根拠（キャラ選択を持つ各画面がレスポンシブであること）へ改める。
- 低-7: spec 134〜143 行の直値 `8` を `EDGE` へ寄せる。
- 低-8: 実測で特定した「popover 左端の 1px 外側」という境界を spec で 1 点踏む（枠内側 1px では維持、枠外 1px でも維持）。閾値をコードへ入れていないので必須ではないが、回避が効かなくなったときの検出が早くなる。
- 低-9: `SearchableSelect.tsx` ヘッダの「13 ファイル」と完了報告の「12 ファイル」の数え方の差を、どちらかへ寄せるか数を消す。

### 残作業（欠陥ではないが完了条件に含まれるもの）

- 完了報告 §4.4 の `make e2e` 全数が「Phase C で記入する」のまま。完了条件 5 は全数の緑を求めている（`D-694`）。
- `docs/progress/progress-log.md` への索引行が未追記（完了条件 7・`CLAUDE.md` §8）。完了報告は Phase D で追記するとしている。
- 完了報告 §6 のレビュー結果欄が未記入。本報告書の受領後に埋めること。

---

## 良かった点

- **止めるべきところで止めた。** 指示書の射程 2 件が既に別サブで決着済みであることに着手時点で気付き、`M27-overview.md:105` という決定的な 1 行まで辿って実装前に開発者へ回した。`CLAUDE.md` §8 の「設計書間の矛盾は止めて確認」が、進めて報告する原則より優先されることを正しく適用している。指示書・overview・チェックリストが揃って見落としていた事実を、製造工程で拾い上げている。
- **「再現しなかった」を測定対象とセットで否定した。** `M27-02a` の結論を覆すのではなく、あちらが `selectionStart` / `selectionEnd` を 1 度も読んでいなかったという事実を特定し、両者が同時に真である理由を言語化したうえで、`make e2e-only P=m27-02a` を回して先行サブの固定が今も成立していることまで確かめている。§7-3 の一般形（逐語の語が実装のどの量に当たるかを先に決めて書き残す）は、他サブへ持ち出せる教訓になっている。
- **原因が特定できなかったことを隠していない。** 「なぜ左と上でだけか」が未特定であること、入れたのは原因の除去ではなく回避であることを、コード内コメント・完了報告・コミットメッセージの 3 か所で明示している。加えて退けた案（透明の受け皿）を実測値付き（枠外 8px で悪化）で残しており、次に同じ問題へ触る者が同じ袋小路へ入らない。
- **破壊確認の区分を画面名で割らなかった。** 呼び出しの形で割り、各区分に「なぜその区分を立てたか」を添えている。とくに区分 5 は、`SearchableSelect` の波及先がキャラ選択 14 に留まらないことを自分で数えた結果として立てられており、指示書が名指ししていなかった範囲を自発的に射程へ入れている。`M-139` の再演を避けるという趣旨に、手続きではなく理由の側から答えている。
- **陽性対照を二重に持たせた。** 修正を無効化した状態での実測（外側の対照）に加え、枠外を見る前に「枠内では選択が出来ていること」を assert する（内側の対照）。後者は spec が恒久的に持ち続けるため、将来「もともと選択できていない」状態で緑になる事故を防ぐ。
- `E-225` のチェックを、`--stat` の数値を引用する形で実際に行っている。作る前の同名ファイル不在確認まで書かれている。

---

## 制約事項

- 本レビューはコード上で判定可能な範囲のみ対象。実際の動作確認・パフォーマンス・実機テストは別途実施が必要。
- 指示に従い、テストおよび E2E は実行していない（別セッションで全数実行中）。完了報告に記載された実測値（`pnpm test` 2506 件・`make e2e-only` の緑・修正前の陽性対照）は、報告の記述と差分の内容から矛盾が無いことを確認したに留まる。
- 2026-09-08 の開発者裁定は、リポジトリ内では完了報告 §0 にのみ存在する。レビュー側で確認できたのは 2026-09-02 の裁定（`M27-02a` 完了報告 §1 および `parallel-board.md` `D-686`）までである。2026-09-08 裁定そのものの存在は本レビューの前提として受け取っており、独立に検証していない。これが推奨修正 高-2 の理由でもある。
- 「なぜ左と上でだけ壊れるのか」の原因究明は本レビューの範囲外。回避策の妥当性は、実測記録と実装の対応関係の範囲でのみ判定した。
