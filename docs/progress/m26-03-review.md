# M26-03 レビュー報告書

| 項目 | 内容 |
|------|------|
| 対象指示書 | `docs/instructions/M26-03-legal-posture-recheck.md` v1.1.0 |
| チェックリスト | `docs/instructions/reviews/M26-03-review-checklist.md` v1.0.0 |
| 着手基点 | `2e35bbd` |
| 対象成果物 | `README.txt` ／ `NOTICE` ／ `docs/progress/M26-03-completion-report.md` |
| レビュー実施日 | 2026-09-11 |
| レビュー方式 | read-only。**完了報告が主張する実測を、レビュー担当が全件回し直して突合した** |

---

## 総評

本サブの成果物は、**指示書が最も見たいと言った 2 点（束 A-1 の「判定できない」と束 B-1 の「手順」）の両方で合格である**。判定できない 6 件を `U-1`〜`U-6` として立て、「埋めるには何が要るか」まで添えた設計は、本プロジェクトの完了報告として質が高い。
**さらに、完了報告が主張する実測をレビュー担当が全件回し直したところ、1 つも食い違わなかった**——母数 2954 の突合、24 列の充填数（2743 行・全 24 列）、凍結表（層 A 30 / 層 B 72）、CSV の最終変更日、`notes` 4 セルの分布まで、数値がすべて再現した。実測を装った推測は検出できなかった。
一方で **「高」が 2 件ある**。1 つは機械検査が赤のまま残っていること（`check-progress-log-index.sh` が違反 1 件）、もう 1 つは **本サブ最大の発見である §4.2 の欠陥を、完了報告自身が再生産していること**である。後者は報告書を書いた瞬間に §4.2 の推奨と §5.2 の件数を失効させており、本プロジェクトの較正で言う「撤回済み・失効した記述が成果物に残っている」型にあたる。
指示書の例文が一次記録と逆向きであることを §3.4 で覆した点は、本サブで最も価値のある副産物である。

---

## 設計準拠性レビュー結果

### 束 A — 来歴の切り分け

| # | 評価 | 判定の根拠と問題点 |
|---|---|---|
| **A-1** | **◎** | **合格。全件が 3 値に埋まっていない。** §3.5 に `U-1`〜`U-6` の 6 件、§3.3 の 24 列表に `condition_ja` / `condition_en` の 2 列が「判定できない」として立っている。さらに **3 値に収まらないものを `X` として別枠で立てた**（§2.3 の `LICENSES` 4 件 ／ `web/src/components/ui/` 21 件）のは、指示書が与えた 3 値の軸がカプコンとの関係しか切らないという構造的な指摘であり、埋めずに構造を疑った点を評価する。U 表に「埋めるには何が要るか」列があるのも良い。**★この列を「埋めろ」という指摘はしない**——本サブの品質そのものである |
| **A-2** | **◎** | **合格。列単位どころか行単位まで降りている。** §3.3 が 24 列全数を判定し、さらに `name_ja`（通常技 A ／ 固有技 C）と `damage`（TC 行だけ累積方式で意味が違う）の 2 列について **列の中で行が割れる**ことを示した。§3.2 が「列で切っても足りない箇所がある」と明示している。指示書 §4.3 の要求を超えている |
| **A-3** | **◎** | **合格。再現した。** `bash scripts/check-public-snapshot.sh --list` を回し直し、トップレベル別の件数がレビュー側でも一致した（`web` 681 ／ `internal` 423 ／ `migrations` 218 ／ `.claude` 42 ／ `scripts` 35 ／ `character_data` 35 ／ `cmd` 6 ／ `LICENSES` 3 ／ `.codex` 3 ／ `.github` 2 ／ `.agents` 2 ／ `human-notes` 1 ／ 単独 21 本）。合計も 2954 に着地した。**`M26-02` 完了報告の写しを使わず `--list` の実出力を正本にした判断は正しい**。同報告の 2706 件は既に古い |
| **A-4** | **○** | **確認結果は在る。ただし結論の書き方に問題がある**（→ 中-3）。`migrations/` は機械検査が緑であること、`character_data/` は 35 件全数を見たことを示しており、手続きは足りている。しかし §5.2 の太字結論「**層 B に入るべきものが層 A へ落ちている例は 1 件も無かった**」は、同じ報告書の §4.2 が見つけた `L-1` / `L-2`（公式の生フレーム値を含み、層 A として公開される 2 ファイル）と正面から読み合わせにくい。三層の軸が「SF6 の事実か否か」である以上、公式の生値を含む文書が層 A に在ることは、素直に読めば「落ちている」側に見える。報告書はこれを「逆向き」と分類して整理したが、**読者が持ち帰るのは太字の絶対文のほうである** |
| **A-5** | **◎** | **合格。** ヘッダ注記で実査日 2026-09-11 を明示し、指示書 §0.2 の「2026-09-10 時点」との差を **CSV 31 本の最終コミット日の実測**で埋めている。レビュー側で `git log -1 --date=short` を回して `ryu` 09-10 ／ `terry` 09-09 ／ 残り 09-06 以前を確認した。**「これで確定」と書かず、`D-811` の次波で古くなることを明記している**点も要求どおり |

### 束 B — 手順が残っているか

| # | 評価 | 判定の根拠と問題点 |
|---|---|---|
| **B-1** | **◎** | **合格。結論だけで終わっていない。** §7 が独立した節として在り、§7.1 が引き金 4 種、§7.2 がそのまま貼って実行できるコマンド列 6 ブロック、§7.4 が差分の取り方である。**レビュー側で §7.2 の [1] [3] [4] [5] を実際に貼って実行し、すべて動いた**（[4] の Python は 24 列の充填を報告書と同一の値で再出力した）。手順が動く形で残っている |
| **B-2** | **◎** | **合格。手順の中身が言語化されている。** §7.3 の「判断の分かれ目」8 件は、コマンドではなく判断規則である。特に **(1) 列の名前で判断しない ／ (2) 情報と表記を分ける ／ (3) 列で切っても足りない場合がある** の 3 つは、本サブが実際に踏んだ判断であり、次の担当が同じ結論へ再到達できる形になっている。「1 件ずつ見た」で終わっていない |

### 束 C — 書き換えていないもの

| # | 評価 | 判定の根拠 |
|---|---|---|
| **C-1** | **◎** | `git diff --stat 2e35bbd -- docs/human-notes/future-notes/` が空。2 本の決定文書は 0 バイト差分 |
| **C-2** | **◎** | `git diff --stat 2e35bbd -- character_data/` が空。CSV 31 本・md 4 本とも 0 バイト差分 |
| **C-3** | **◎** | `git diff --stat 2e35bbd -- CONTRIBUTING.md` が空。`D-814` の分離が守られている |
| **C-4** | **◎** | `git diff --stat 2e35bbd -- REUSE.toml` が空。§4.3 は案 A / B / C を並べて **推奨だけを返し、実施していない**。`REUSE.toml:73-77` が本サブを名指ししていた「暫定である」注記に対し、**推奨を返して申し送りに置いた**のは正しい畳み方である |
| **C-5** | **◎** | 法務の判断を 1 件も決めていない。§1.4 の外部照会プロンプトは生成のみで、**回答が無い限り前提 1 / 3 / 4 / 5 の現在値は判定できないと明記**している。§1.6 の `name_ja` の扱いも「3 つの記述が同居している」という事実提示に留め、§8-2 で開発者へ返している |

**★変更総量の確認**（教訓 `E-225`）: `git diff --stat 2e35bbd` は **3 ファイル / 696 insertions / 1 deletion**。唯一の deletion は `NOTICE` §3 の差し替えられた 1 行であり、**新規追加ファイルに deletions は付いていない**。上書きで消えたものは無い。

### 束 D — 窓口導線

| # | 評価 | 判定の根拠 |
|---|---|---|
| **D-1** | **◎** | 差し替えは `README.txt` と `NOTICE` の 2 本のみ。3 本目に手は入っていない |
| **D-2** | **◎** | 両ファイルとも `<権利者・一般問い合わせ窓口のアドレス>` の伏せ字。実アドレスの混入なし。`NOTICE` 側に「実アドレスは公開の手番で差し替えること」の注記あり |
| **D-3** | **◎** | 両ファイルとも DMCA / GitHub 申し立て経路への言及が 0 件。`D-805` の撤回が守られている |
| **D-4** | **◎** | `README.txt`＝「返答の期限はお約束できません」／ `NOTICE`＝「返答の期限は約束しない」。両方に明記 |
| **D-5** | **◎** | 両ファイルとも「脆弱性の報告は本窓口ではない」旨を明記して分離。`SECURITY.md` は 0 バイト差分で `M26-04` の射程を侵していない。**`README.txt` は敬体・半角括弧、`NOTICE` は常体という既存の文体差に合わせてある**のも良い |

### 束 E — `docs/seed-data/` の再判定と外部照会

| # | 評価 | 判定の根拠と問題点 |
|---|---|---|
| **E-1** | **◎** | §4.1 が 8 件全数を 1 ファイルずつ再判定し、行数・最終変更日・該当行番号つきで示している。レビュー側で `grep -c 公式` を回し、`setplay-move-notes.md` と `M19-OPS-01-chain-measurement-manual.md` が 0 件であること、`moves-input-background.md` に生値の記述が無いことを確認した。**`D-742` のディレクトリ単位を維持する理由**〔ファイル単位にすると後から足されたファイルが黙って通る〕**まで再確認している** |
| **E-2** | **◎** | §4.1 の結論 2 が「除外は『公開しない』であって『無い』ではない」を明示し、公開スナップショット方式が前提であることを `NOTICE:47-48` に紐づけている |
| **E-3** | **◎** | 外部へ投げた問いは 0 本である。`.claude/commands/ask_external_ai.md` を確認したところ、同コマンドは **プロンプトを生成するだけで外部へ問い合わせない**。§1.4 はプロンプトを 1 本だけ提示しており、その本文にもリポジトリ固有の内容が入っていない。上限 1 本を超えていない |

### 束 F — 共通

| # | 評価 | 判定の根拠と問題点 |
|---|---|---|
| **F-1** | **○** | レビュー側で `bash scripts/check-doc-refs.sh` を回して **緑**（dead reference なし）。実体は問題なし。**ただし完了報告に出力の証跡が無い**（§11 が空欄のまま）→ 中-2 |
| **F-2** | **△** | `bash scripts/check-doc-inventory.sh` は **緑**（型に無いファイルなし）。報告書 §9-5 が「完了報告とレビュー報告は既存の型に合う」と §10.Y の観点で論じてはいるが、**出力そのものは貼られていない**。「出力を読んでいるか」という設問に対し、読んだ証跡が無い → 中-2 |
| **F-3** | **◎** | `bash scripts/check-md-emphasis.sh docs/progress/M26-03-completion-report.md` を回して **検出 0 行** |
| **F-4** | **×** | **落ちている。`docs/progress/progress-log.md` に `M26-03` の索引行が 1 行も無い。** `bash scripts/check-progress-log-index.sh` が **違反 1 件**を返す（逐語＝「作業 ID `m26-03` が docs/progress/progress-log.md に現れない」）。`grep -n 'M26-03' docs/progress/progress-log.md` の唯一のヒットは `M26-02` の記述内の言及（`:5494`）であり、本サブの索引行ではない。**指示書 §6-11 と `CLAUDE.md` §8 が必須としている** → 高-1 |
| **F-5** | **○** | §4.3 に「**推測: 三層の『割当そのもの』とは層の定義とパスの層への振り分けを指し、除外範囲の表現形式は含まない、と仮定した**」が在り、最も必要な箇所（指示書 §3-2 の禁止範囲の解釈）に置かれている。**ただし §2.3 の `X-2` で「21 ファイルが shadcn/ui 由来である」と断定した根拠が `components.json` の自己宣言とファイル名一致の 2 点のみである**点は、同じ形のマーカーが在ってよかった。**ただし直後に「言えること／言えないこと」を分けて書いてあるため、実害は小さい** |

---

## 設計準拠性以外の指摘事項

### 1. 完了報告自身が §4.2 の欠陥を再生産している（**最重要**）

**§7.2 の [5] の走査をレビュー側で回し直したところ、ヒットが 3 件になった。**

```
HIT docs/progress/M14-RESEARCH-02-report.md
HIT docs/progress/M26-03-completion-report.md      ← ★新しく増えたのは本サブの成果物である
HIT docs/progress/progress-log.md
```

`docs/progress/M26-03-completion-report.md` は **公開スナップショットの許可リストに入っている**（`--list` に 1 件でヒットする）。そこへ次の 2 系統の公式生値が転記された。

- **§4.2 の `L-1` 行**——`M14-RESEARCH-02-report.md` の `:372` / `:374` / `:376` から**約 26 個の公式値を逐語で再掲した**（`active` ／ `damage` ／ `startup` ／ `recovery` ／ `on_block` の 5 種）。
- **★§4.1 の表**——**DENY されている `docs/seed-data/` から公式生値を引き写した**。該当は `input-notes.md:31` / `:107-108`（**3 技分**）、`check-criteria.md:64`（**4 技分**）、`target-combo-and-derived-flag-rules.md:112`（**TC 1 技分**）。**いずれも「公式値 → 実測値」の対、または「実測値（公式 N）」の形である。**

**⇒ 後者は `D-742` の除外を部分的に無効化している。** 除外の理由は「公式の生フレーム値・元データ抜粋を含む」であり、その値を公開される文書へ書き写せば、除外が守っていたものが漏れる。§4.1 の再判定は **値を引かずに行番号と件数だけで書ける**内容であり、転記は避けられた。

**⇒ これは体裁の問題ではない。書いた瞬間に、同じ報告書の 2 か所が失効した。**

- **§4.2 (e) の選択肢 (i)「`public-snapshot-manifest.txt` に DENY を 2 行足す」は誤りになった。** 3 行である。
- **§5.2 の表の 5 行目「`docs/progress/` の `L-1` / `L-2` | **2**」は 3 である。**
- **§8-6 の申し送りが開発者へ渡す件数も 2 件のままである。**

**⇒ 開発者はこの申し送りを読んで「2 ファイル」を前提に判断する。本サブの成果物そのものが 3 件目であることは、報告書のどこにも書かれていない。**

**★なお「完了報告は歴史記録だから書き換えない」**（`D-274` (3)）**は、本件の免責にならない。** 歴史記録の原則が守るのは **既に在る記録**であって、いま書く新しい記録の内容ではない。§4.2 (d) が挙げた 3 つの理由（射程・歴史記録・「消す」は開発者の手番）は `L-1` / `L-2` に対しては正しいが、**自分自身に対しては成立しない**。

### 2. §3.3 の集計行の列数が合っていない（合計だけが偶然 24 に着地している）

§3.3 末尾の逐語＝「**A 10 列 ／ S 8 列 ／ 判定できない 2 列 ／ 行で割れる 2 列 ／ C（情報）＋A（表記）1 列 ／ 値なし 1 列**（合計 24）」。

**表を数え直すと合わない。**

| 区分 | 表の実数 | 集計行 |
|---|---:|---:|
| **S** | **9**〔`startup` `active` `recovery` `total` `on_hit` `on_block` `notes` `notes_tool` `chain_cancel_total`〕 | 8 |
| **A**（`last_changed_game_version` を除く） | **9** | 10 |
| 判定できない | 2 | 2 |
| 行で割れる | 2 | 2 |
| C＋A | 1 | 1 |
| 値なし（`last_changed_game_version`。表では `A` 表記） | 1 | 1 |

**⇒ `A` を +1、`S` を −1 したため、合計だけが 24 に着地して整合して見える。**
**★これは本プロジェクトが繰り返し警戒している「緑でも壊れている」形そのものである**——合計が合うので検算では捕まらない。`S`（開発者の実測）と `A`（本アプリ独自）は法務上の意味が違う区分であり、件数が入れ替わったまま次のサブへ写されると誤読される。

### 3. §5.2 の絶対文が、同じ報告書の §4.2 と読み合わせにくい

指摘事項 1 と同根。§5.2 の「**層 B に入るべきものが層 A へ落ちている例は 1 件も無かった**」は、**三層の軸が「SF6 の事実か否か」である**という §5.2 自身の説明と衝突する。`L-1` / `L-2` が含むのは公式のフレーム値・ダメージ値、すなわち SF6 の事実そのものであり、それが `AGPL-3.0-or-later` ／ `SPDX-FileCopyrightText = "2026 plexiblinp"` の宣言下で公開される。
報告書は「これは第三者素材の軸の問題であって層の問題ではない」と整理し、§8-8 で「三層は軸が 1 本足りない」と申し送っている。**整理としては筋が通っている。問題は、絶対文のほうが引用されやすいことである。**

### 4. 検査の証跡が完了報告に残っていない

指示書 §5 は検査を 5 件挙げている。完了報告に出力が貼られているのは **2 件だけ**である。

| 指示書 §5 | 完了報告の証跡 | レビュー側の実測 |
|---|---|---|
| `go test ./...` | **なし** | 未実施（本サブは Go コードを 1 行も触っていないため実害なし） |
| `check-migration-license.sh --list` | **非 `--list` 形の出力のみ**（§2.4 / §5.1） | `--list` を回して層の解決結果が変わっていないことを確認。緑 |
| `check-doc-refs.sh` | **なし** | 回して緑 |
| `check-doc-inventory.sh` | **なし**（§9-5 で観点は論じている） | 回して緑 |
| `check-md-emphasis.sh <file>` | **なし** | 回して 0 行 |

§11「検査結果」が「（Phase C で埋める）」のまま残っている。**実体はすべて緑であり、品質そのものに問題は無い。** 指摘は証跡の欠落である。
**★加えて `CLAUDE.md` §8 が「1 本目に回す」とした `check-artifact-integrity.sh` に言及が無い。** レビュー側で回して違反なし（検査 15 件の自己検査がすべて OK、生成物 4 件も OK）。

### 5. §2.2 の母数が「自己コミット前の値」であることが書かれていない

完了報告 §2.2 の母数 2954 は、**完了報告そのものがコミットされる前の値**である。現在は `2984 / 2955 / 29` になる（差の 1 件は完了報告自身）。
これ自体は測定時点の正しい写しである。問題は **§7.4 が「本書の §2.2 が前回値である」と宣言している**ことで、次にこの手順を回す担当は `docs/**` が 1482 → 1483 に増えた理由を報告書から復元できない。§7.4 に「本書自身が 1 件を占める」の 1 行があれば足りた。

### 6. §9-5 が、書いた時点で存在しないファイルを「作った」と書いている

§9-5 の逐語＝「作ったのは `docs/progress/M26-03-completion-report.md` … と `docs/progress/m26-03-review.md` … の 2 本」。
**§10 は同じ報告書の中で「`docs/progress/m26-03-review.md` は Phase B のサブエージェントが作るまで存在しない。⇒ 存在しないファイルへのリンクを断定で書かない（`D-510`）」と正しく書いている。** §9-5 と §10 が矛盾している。`D-510` の先例を引いておきながら、別の節で同じことをしている。

### 7. `NOTICE` に日付入りの暫定注記が出荷される

追加された `NOTICE:40` の逐語＝「★上記は伏せ字である。実アドレスは公開の手番で差し替えること（2026-09-11 時点で未確定）。」
**伏せ字アドレス本体の差し替えは §8-9 で申し送られているが、この注記行そのものの撤去は申し送られていない。** `NOTICE` は配布物に同梱される法務文書であり、★付きの開発メモと日付が残ったまま公開されると体裁を損なう。`grep -rn '権利者・一般問い合わせ窓口のアドレス'` では **この行は拾えない**（アドレス文字列を含まないため）。

### 8. `README.txt` が参照する `NOTICE` は zip に入らない

追加ブロックが「撤退ポリシーは NOTICE に記載しています」と案内しているが、同ファイル `:131` は「**本 zip に入っているのは実行ファイルと本 README.txt だけです**」と書いている。
**既存の `SECURITY.md` / `SUPPORT.md` への参照と同じ形であり、本サブが持ち込んだ問題ではない。** ただし、権利者が読む導線としては「公開リポジトリのタグにある」旨が近くに無いと辿れない。

---

## 推奨修正（優先度別）

### 高（M26 完了前に修正必須）

- **高-1: `docs/progress/progress-log.md` へ `M26-03` の索引行を 1 行追記する。**
  `bash scripts/check-progress-log-index.sh` が **違反 1 件**で赤である。指示書 §6-11 ／ `CLAUDE.md` §8 の必須事項。形式は `.claude/commands/implement_plan.md` の「完了時」節に従い、日付・作業 ID・結果・報告書リンク＋横断課題を 1 行で置く。**横断課題としては §8 の 12 件のうち設計卓が畳む必要があるもの（8-6 / 8-7 / 8-11）を落とさないこと。**

- **高-2: 完了報告に「本書自身が `L-3` である」ことを追記し、失効した 2 か所を是正する。**（指摘事項 1）
  本サブは公開される文書へ、`L-1` の公式生値と **DENY 済み `docs/seed-data/` の公式生値**を転記した。**是正は報告書全体の書き直しではなく、次の 3 点で足りる。**
  1. **§4.2 の表へ `L-3`＝`docs/progress/M26-03-completion-report.md` の行を足す**（`§4.1` の再判定表と `§4.2` の `L-1` 引用が該当箇所である旨）。
  2. **§4.2 (e) の選択肢 (i) を「DENY を 2 行」から「3 行」へ直し、§5.2 の表 5 行目の件数 2 を 3 へ直す。**
  3. **§8-6 の申し送り本文へ「本書自身を含めて 3 件である」を 1 行足す。**
  **★併せて、§4.1 の表から公式の生数値の逐語引用を落とし、行番号と件数だけの記述へ置き換えることを推奨する。** 再判定の結論（除外を続ける）は値を引かずに成立する。
  **★これを「低」に置かないこと。** 動作は何も壊れておらずテストも検査も緑だが、**開発者が §8-6 を読んで下す判断の前提が 2 件のままになっている。**

### 中（M27 着手と並行可）

- **中-1: §3.3 の集計行を `A 9 列 ／ S 9 列 ／ …` へ直す。**（指摘事項 2）合計が偶然 24 に着地しており検算で捕まらない。`S` と `A` は法務上の意味が違う区分であり、入れ替わったまま §7.4 の「前回値」として次の波へ渡る。
- **中-2: §11「検査結果」を埋め、指示書 §5 の 5 件と `check-artifact-integrity.sh` の出力を貼る。**（指摘事項 4）実体はすべて緑であることをレビュー側で確認済みなので、転記のみで閉じる。`check-migration-license.sh` は **`--list` 形**で貼ること（指示書 §5-2 の指定）。
- **中-3: §5.2 の絶対文に §4.2 への相互参照を足す。**（指摘事項 3）「層 B に入るべきものが層 A へ落ちている例は 1 件も無かった」の直後に「**ただし §4.2 の `L-1` / `L-2` / `L-3` は SF6 の事実を含んだまま層 A で公開される。三層の軸では切れないため別枠にした**」の 1 文を置けば、太字だけを持ち帰る読者にも届く。
- **中-4: §7.4 へ「本書自身が公開 1 件を占める」を 1 行足す。**（指摘事項 5）次に §7.2 を回す担当が `docs/**` の +1 を説明できるようにする。
- **中-5: §9-5 の `m26-03-review.md` の記述を §10 と整合させる。**（指摘事項 6）同じ報告書が `D-510` を引きながら破っている。
- **中-6: `NOTICE:40` の暫定注記の撤去を §8-9 へ追記する。**（指摘事項 7）`grep` では拾えない行であることも併記すること。
- **中-7（設計卓宛・製造の手番ではない）**: **チェックリスト `A-2` と指示書 §2.3-3 / §4.3 の例「フレーム値は事実、`command` は開発者の補完」が一次記録と逆向きである。** 完了報告 §3.4 が `moves-input-background.md:51` ／ `check-criteria.md:64,66` ／ `M14-RESEARCH-02-report.md` §C-3-4 ／ `command-correction-history.md:3` の逐語で正しく覆し、§8-5 で申し送っている。**製造の対応は正しい。** ただし **チェックリスト v1.0.0 の `A-2` にも同じ逆向きの例が写っており、次のレビュー担当が同じ誤りを前提に見る。** 指示書側とチェックリスト側の両方を直す手番が要る。

### 低（将来対応）

- **低-1**: `README.txt` の新規ブロックが案内する `NOTICE` は zip に同梱されない（指摘事項 8）。既存の `SECURITY.md` 参照と同じ形であり本サブ起因ではないが、権利者向けの導線としては「公開リポジトリのタグにある」旨が要る。`M26-04` か公開手番で扱う。
- **低-2**: `M26-01` 完了報告の `character_data/` 件数 34 と実測 35 の差（§8-4 で申し送り済み）。宣言に穴は無く、記述だけが古い。歴史記録のため直さない判断も妥当。
- **低-3**: `.claude/commands/precheck_seed_data.md:40` の「23 列」が実体 24 列（§8-12 で申し送り済み）。射程外の判断は正しい。改善レーンの手番。
- **低-4**: 伏せ字アドレスの差し替え箇所が 2 か所（`M26-04` 次第で 3 か所）になる件（§8-9）。`grep` で拾える形にしてある旨が書かれており、対応としては足りている。

---

## 良かった点

- **★実測がすべて再現した。** レビュー担当が `check-public-snapshot.sh --list` の件数分解、`check-migration-license.sh` の凍結表、24 列の充填数（Python で再計算）、CSV 31 本の最終変更日、`notes` 4 セルの所在（`terry` 2 ／ `zangief` 2）、公開 `.html` 31 本と `.csv` 31 本の内訳、`web/src/components/ui/` の 21 件、`components.json` の shadcn スキーマ宣言を回し直した。**1 件も食い違わなかった。** 実測を装った推測が無い報告書である。
- **★§4.2 は本物の発見である。** 「`D-742` の除外は場所で切っており、内容では切れていない」という指摘は、公開ゲート `A` の設計そのものに効く。レビュー側で該当行（`M14-RESEARCH-02-report.md:370-376` ／ `progress-log.md:3742`）を実際に開いて事実であることを確認した。**指示書が「やること」として挙げていない範囲で、自分で走査軸を立てて見つけている。**
- **★§3.4 は指示書の誤りを一次記録で覆した。** 「フレーム値は事実、`command` は開発者の補完」という指示書の例文に対し、`moves-input-background.md:51` の「公式データを転記しない」／ `M14-RESEARCH-02-report.md` の「`command` は公式と完全一致」を並べ、**「情報」と「表記」に分ければ両立する**という第三の整理を出した。指示書に従って誤った表を作る道が在ったのに、そちらへ行かなかった。
- **★「判定できない」の書き方が良い。** `U-1`〜`U-6` に「なぜ判定できないか」と「埋めるには何が要るか」の 2 列を付け、さらに `U-1` / `U-2` / `U-6` が **公式 dist 不在という 1 つの原因に還元される**ことまで示した。次の担当が importer を接続した瞬間に 3 件がまとめて閉じることが分かる。
- **★§7.3 の「判断の分かれ目」8 件。** 手順を「コマンド列」で終わらせず、**機械化できない判断規則**を言語化した。特に「列の名前で判断しない」は、本サブが実際に踏みかけた穴から抽出されている。
- **★射程を守った。** 3 ファイル・696 insertions・1 deletion。`character_data/` ／ 決定文書 2 本 ／ `CONTRIBUTING.md` ／ `REUSE.toml` ／ `SECURITY.md` のいずれも 0 バイト差分。**`REUSE.toml` は自分が名指しされた注記に答える必要が在った場面でも触っていない**（推奨を返して申し送りに置いた）。
- **★§6.3 で製造判断を 1 件、自分から報告している。** `followup-backlog:1399` の案 A（`SECURITY.md` §5 へ回す）が指示書 §2.4-5 と衝突して採れないことを示し、代わりに 2 ファイル自己完結を選び、**副作用（差し替え箇所が 2 か所になる）まで自分で挙げて §8-9 へ送った**。`CLAUDE.md` §9 共通原則 3 の形どおりである。
- **★`followup-backlog:1399` が `D-814` を反映していないこと**（§8-11）**を見つけ、製造が §J 以外を編集できない制約**（`D-382`）**を踏まえて設計卓へ回した。** 制約の中で正しく処理している。

---

## 制約事項

- 本レビューはコード上で判定可能な範囲のみ対象。実際の動作確認・パフォーマンス・実機テストは別途実施が必要。
- **外部の一次情報（robots.txt / EULA / 利用規約 / shadcn/ui のライセンス条件）は本レビューでも確認していない。** 完了報告が `U-3` / `U-4` として「判定できない」に置いた扱いは妥当であり、レビュー担当もこれを埋めていない。
- **不明: `docs/progress/M26-03-completion-report.md` を公開スナップショットから DENY すべきかどうかは判断できない。** 高-2 で求めたのは「3 件目であることを申し送りへ反映する」ことであって、除外の可否ではない。除外の可否は `A3` の適用範囲の判断であり、完了報告 §4.2 (e) が言うとおり開発者の手番である。
- **不明: `L-1` / `L-2` / `L-3` の公式値の量が、法務上どの水準で問題になるかは判断できない。** 完了報告 §4.2 (b) の「量は `docs/seed-data/` より小さい」という観測は事実として正しいが、閾値は製造もレビューも決められない。

---

## 取り込み結果（自動トリアージ・2026-09-11）

> **★`/implement_plan_full` の Phase C。人間トリアージ承認を外す代わりに、採否と理由をここへ残して事後監査可能にする。**

**指摘 13 件（高 2 ／ 中 7 ／ 低 4）は、13 件すべて採用した。★不採用は 0 件であり、「高」指摘の自動棄却は発生していない。⇒ 開発者へのエスカレーションは不要であった。** 再レビュー往復は 0 回（上限 2 回に未達）。**`followup-backlog.md` §J への停止時記録は 0 件**（未解消の指摘が無いため）。

| # | 指摘 | 採否 | 理由と対応 |
|---|---|---|---|
| **高-1** | `progress-log.md` の索引行が無く `check-progress-log-index.sh` が赤 | **採用** | 指摘のとおり。Phase D で索引行を追記し、検査が緑になったことを完了報告 §11-7 に記した。横断課題には設計卓が畳む必要のある 3 件（8-6 / 8-7 / 8-11）を落とさず入れた |
| **高-2** | **完了報告自身が §4.2 の欠陥を再生産している** | **採用（★推奨より踏み込んだ）** | **指摘は正しい。** 初版の §4.1 と §4.2 は、除外の根拠を示すために DENY 済み `docs/seed-data/` と `L-1` から公式の生値を逐語で引き写しており、公開される本書が 3 件目になっていた。**★レビューの推奨は「`L-3` として数え、件数を 2 → 3 へ直す」であったが、それだと漏れた値は漏れたままである。⇒ 転記そのものを落とした**——値の逐語引用を、行番号・件数・値の形だけの記述へ置き換えた（再判定の結論「除外を続ける」は値を引かずに成立する）。**★置き換え後に §7.2 [5] を回し直し、ヒットが 2 件へ戻ることを確認した。⇒ 件数 2 は是正後も正しいため §4.2 (e) / §5.2 / §8-6 は変更していない。★顛末は §4.2 の注記と §7.3 の「分かれ目 9」に残した**（是正して痕跡を消すと、次の担当が同じ穴を踏む） |
| **中-1** | §3.3 の集計が `A 10 / S 8`（実数 `9 / 9`） | **採用** | 数え直して指摘のとおりと確認。`A 9 / S 9` へ是正し、**あわせて内訳の列名を書き下した**——合計だけが 24 に着地する形だったため、数だけ直しても同じ誤りが再発しうる |
| **中-2** | 指示書 §5 の検査の証跡が無い | **採用** | **§11 を新設**し、`check-artifact-integrity.sh` を含む 8 件の出力を貼った。`check-migration-license.sh` は指定どおり `--list` 形 |
| **中-3** | §5.2 の絶対文が §4.2 と読み合わせにくい | **採用** | §5.2 の結論の直後へ相互参照を追加。「軸が違うだけで放置してよいという意味ではない」まで書いた |
| **中-4** | §2.2 の母数が自己コミット前の値である旨が無い | **採用** | §7.4 へ追記（次に数えると `docs/**` が 1483、合計 2955 になる旨） |
| **中-5** | §9-5 が存在しないレビュー報告を「作った」と断定 | **採用** | 指摘のとおり `D-510` を引きながら破っていた。文言を是正し、§10 は本ファイルの実在を確認してから書いた |
| **中-6** | `NOTICE:40` の暫定注記の撤去が申し送られていない | **採用** | **§8-9b を新設**。`grep` で拾えない旨と、同型の先例 `NOTICE:44-45` も併記した |
| **中-7** | チェックリスト `A-2` にも逆向きの例が写っている | **採用（設計卓宛）** | **§8-5 へ追記**。★製造は指示書もチェックリストも編集しない |
| **低-1** | `README.txt` が案内する `NOTICE` は zip に同梱されない | **採用** | **§8-10b を新設**（`M26-04` または公開の手番へ）。本サブ起因ではないが導線としては要る |
| **低-2** | `M26-01` の件数 34 と実測 35 の差 | **採用済み** | §8-4 で申し送り済み。追加対応なし |
| **低-3** | `precheck_seed_data.md:40` の「23 列」 | **採用（★拡張）** | **§8-12 を拡張。製造が独自に見つけた 2 件目を併記した**——`internal/seedgen/model.go:21` が同じ「23 列」と書いており、**同じ `var csvColumns` の要素は 24 個である**。**★本番コードのコメントであり `go test` / `go vet` / `tsc` が一切検出しない**（`D-250` と同型）。正本側（`SUPP-001` §3.2 ／ `DES-003`）は 24 列で正しい |
| **低-4** | 伏せ字の差し替え箇所が 2 か所 | **採用済み** | §8-9 で申し送り済み。追加対応なし |

**★製造が射程外として実施しなかったもの**（不採用ではなく、手番が違うもの）: 中-7（指示書・チェックリストの是正＝設計卓）／ 低-3（`precheck_seed_data.md` と `model.go` の是正＝改善レーン）／ 低-1（`M26-04` または公開の手番）。**いずれも §8 の申し送りへ入れてある。⇒ 落としていない。**

---

## 追走の記録（2026-09-11・`D-819` ／ `D-820` ／ `D-821` 反映時）

**★本報告書そのものが 3 件目の漏れになっていた。⇒ 製造が是正した。**

指摘 高-2 を書くにあたり、本報告書の `:98` / `:99` は**公式の生フレーム値・ダメージ値を逐語で引いていた**（`M14-RESEARCH-02-report.md` 由来のものと、**DENY 済み `docs/seed-data/` 由来のもの**）。**★レビュー報告書 `docs/progress/m26-03-review.md` も `docs/**` の ALLOW で公開される。⇒ 指摘の内容と同じことを、指摘した文書自身がしていた。**

**★是正**＝`:98` / `:99` から値の逐語引用を落とし、**ファイル・行番号・件数・値の形**だけの記述へ置き換えた。**指摘の力は落ちていない**——`D-742` の除外が部分的に無効化されているという主張は、値を引かずに成立する。

**★★あわせて、走査そのものの欠陥が分かった。** 完了報告 §7 が定めていた走査は**本件を 1 行しか拾わなかった**——`:99` の引用は「実測値（公式 N）」「`NN→NN`」の形で、**英語の列名も 2 桁以上の数値も伴わない。⇒ 狭い網を素通りする。**

**⇒ 走査を 2 本立てへ改めた**（完了報告 §7.A (c) ／ §7.B [5a] [5b]）。

| 走査 | 対象 | 網 |
|---|---|---|
| 全体 | 公開される `docs/` 全数 | **狭い**。広げると版数の矢印で数百件に当たり、合否に使えない（実測 150 件超） |
| **★自分の差分** | **その手番で足した行だけ** | **広い**。件数が小さく、当たった行を人が見られる |

**★★結論は「走査を直せば済む」ではない。⇒ 狭い網は取りこぼし、広い網は使い物にならない。★機械で決められないのだから、書き方の側**（値を引用しないで書く）**で守るしかない。⇒ 完了報告 §7.A の分かれ目 9 をその形へ書き換えた。**

---

## 再レビュー（往復 2 回目・2026-09-11）

### 総評

**原稿化そのものは良い。** `D-819` / `D-820` / `D-821` の 3 つを取り違えずに分けて受け、束 B-1〜B-6 と `D-821` の挿入位置の指示は**全件が形として満たされている**。原稿は 72 行で集積場の系統に収まり、`SUPP-001` §5.5.5 / §5.5.6 と見出し・日付行・前置き・表・末尾の置き場の規約まで型が一致する。
**★しかし「切り出したときに成立するか」で見ると 3 点が壊れる。** (1) **実行コマンドの逐語が原稿の外にしか無く、原稿から辿る線が 1 本も無い**。(2) **節を組み替えたのに `§7.2` / `§7.3` / `§7.4` / `§7.0` を指す行が本文に 4 か所残った**。(3) **初回レビュー 中-4 で採用した「次に数えると件数が動く」注記が、組み替えの過程で消えている**——`§10` の取り込み表は「採用・§7.4 へ追記」のままである。
**⇒ いずれも 1〜2 行で直る。** 原稿の中身そのものを書き直す必要は無い。

### 束 B の評価

| # | 判定 | 根拠 |
|---|---|---|
| **B-1** | **◎** | **結論だけになっていない。** 原稿は (a) 引き金 4 種 ／ (b) 母集団の起こし方 ／ (c) 3 階層と 2 本立ての走査 ／ (d) 分かれ目 9 件 ／ (e) 差の見方 ／ (f) 何を書き残すか、の 6 段で構成されている。**★ただし実行コマンドの行き先に問題がある**（下の `R-1`） |
| **B-2** | **◎** | **分かれ目が言語化されている。** (d) の 9 行はいずれも「どちらに転ぶか」と「何を見て決めるか」の対になっている。**★9 の「走査の母集団に自分の文書が入る」は今回の追走で足された行であり、初回レビュー 高-2 の実害から起こしたもので、机上の項目ではない** |
| **B-3** | **◎** | **§7 の見出しが逐語で「設計卓が `SUPP-001` §5.5.9 へ移すための原稿」**。§7.A の見出しも「この範囲をそのまま `SUPP-001` §5.5.8 の直後へ」。**★`暫定` の語は §7 の全域から消えている**（`grep` 実測 0 件）**。⇒ 裁定前の形は残っていない** |
| **B-4** | **◎** | **新規ファイルは 0 件。** `git diff --stat 0055cfc` の対象は `docs/progress/` の既存 3 本のみ。`docs/process/` / `scripts/` への追加は無い。`bash scripts/check-doc-inventory.sh` も「型に無いファイルなし」 |
| **B-5** | **◎** | **`DRAFT` マーカー間は 72 行**（60〜80 の範囲内）**。分かれ目は 9 行の表のまま原稿に載っており、完了報告へ逃がしていない。★(d) は原稿の中で最も行数を使っている部分であり、圧縮の犠牲になっていない** |
| **B-6** | **◎** | **見出しは `#### 5.5.9`。** 実物と突き合わせて `§5.5.7` が `:1230`、`§5.5.8` が `:1291` で埋まっていることを確認した。**見出しレベルも兄弟節と同じ `####`** |

**`D-821` の挿入位置の指示**: **◎。** 原稿ブロックの**内側**（`:536`）に「★挿入位置＝§5.5.8 の直後（2 つ目の §5.5.4 の手前）…★この引用行は反映時に落としてよい」が在る。実物で `§5.5.8` が `:1291`、2 つ目の `§5.5.4` が `:1309` であることを確認済みで、記述は正しい。**★「外に書くだけでは落ちる」と製造自身が理由を書いている点を評価する。**

### 原稿を切り出したときに成立するか（★本再レビューの主眼）

**まず成立している側を確認する。**
原稿内の参照を全数抽出したところ、**完了報告内の相対参照は 1 件も残っていない**。`§4.2` は `M26-02` 完了報告 §4.2、`§1.3` / `§2` は `moves-input-background.md` / `check-criteria.md`、`§4` は `DATA-LICENSE.md` §4 と、**すべて文書名で修飾されている**。「本書」は 0 件。唯一の「本節」は (d) の「本節の値打ちである」で、**移した後にこそ正しく読める**。`D-742` / `D-821` の裸の裁定 ID は `SUPP-001` 自身が `D-2` 〜 `D-3xx` を裸で使っており、慣行に合っている。

**壊れる側は次のとおり。**

| # | 壊れ方 | 詳細 |
|---|---|---|
| **★★`R-1`** | **実行コマンドの逐語が、原稿から到達できない場所にだけ在る** | 原稿 (c) は「`--list` をトップレベルで集計する」「31 本のヘッダを `sort -u` して」「全体走査は狭く・自分の差分は広く」と**方針だけ**を書き、実体の `awk` / `python3` / `grep` は **§7.B にしか無い。★そして原稿の中に §7.B を指す行が 1 行も無い**（実測。原稿内の「完了報告」の語は 3 件ともすべて別の意味での言及）**。⇒ §7 の前置きは「移した後に本節を引かないこと」と明記している。★`D-819` が案 A を退けた理由は「引かれる先が歴史記録になる」ことであり、⇒ コマンド側だけが案 A の形で残った** |
| **★★`R-2`** | **組み替え後に消えた節番号を指す行が 4 か所残っている** | `:13`「3 つの裁定が何を決めたかは **§7.0** の表」——**★`### 7.0` という見出しは存在しない**（現行の見出しは `7` / `7.A` / `7.B` のみ。表は §7 前置きの引用ブロック内に在る）**。★この行は今回の作業ツリー編集で新しく入ったものである。** ／ `:393` と `:399`「**§7.2** [5] に手順として残した」「**§7.2** [5] を回し直した」——`§7.2` は削除済みで、現在の該当は §7.B [5a] [5b]。**★同じ段落の `:401` / `:405` は新番号**（§7.A (d) ／ §7.A (c)）**へ直っており、直し漏れである。** ／ `:835` 末尾「**★本書の値打ちは §3.5 の「判定できない 6 件」と §7.3 の「判断の分かれ目 8 件」である**」——**`§7.3` は削除済み、かつ分かれ目は 9 件へ増えている。⇒ 本書の結語が、本書の値打ちを古い番号と古い件数で名指している** |
| **★★`R-3`** | **初回レビュー 中-4 で採用した注記が、組み替えで消えている** | 旧 §7.4 に在った逐語＝「**§2.2 の 2954 件は本書がコミットされる前の値である。★本書自身が公開物 1 件を占めるため、次に数えると `docs/**` は 1483 件、合計は 2955 件になる**（レビュー報告書を数えるとさらに +1 件）」が、**現行の全文から消えている**（`grep '2955\|1483'` が 0 件。`2954` は §2.2 側に残存）**。★一方 §10 の取り込み表 `:804` は「中-4 … 採用 ／ §7.4 へ追記」のままである。⇒ 取り込んだと記録されている修正が、成果物には無い。** **★★さらに悪いのは、原稿 (e) が「差の理由を前回の完了報告に書き残しておくこと」と次の人へ指示していることである**——**⇒ その指示の出どころである本完了報告が、いま書き残していない** |
| **`R-4`** | **原稿に「31 本」という、その原稿自身の引き金で失効する数が埋まっている** | (c) 階層 3 の「**31 本のヘッダを `sort -u` して**」。**★同じ原稿の (a) 引き金 2 が「キャラが追加された ⇒ CSV が 1 本増える」と書いている。⇒ 節が想定している最初の出来事で、節の中の数が古くなる。** 実測は `character_data/*.csv` が 31 本（ディレクトリ全体は 35 ファイル）で**今日は正しい**。**★先例が本書 §8-4 に在る**——`M26-01` 完了報告の「34 件」が実測 35 件へずれた件と同じ形である。§7.B の実コマンドは `character_data/*.csv` のグロブで書かれており**数を持たない。⇒ 原稿側だけが数を持ってしまっている** |
| **`R-5`** | **原稿が「読んでから決める」と指示する 2 ファイルは、公開スナップショットから除外されている** | (d) 1 の「`docs/seed-data/moves-input-background.md` §1.3 と `check-criteria.md` §2 の類型を読んでから決める」。**★`docs/seed-data/` は `D-742` で DENY されており**、しかもそれを説明しているのは同じ原稿の (d) 7 である。**`SUPP-001` は `docs/**` として公開される側なので、⇒ 公開物を読む人には辿れない参照になる。** 開発リポジトリでのみ読める旨が 1 句あれば解ける。**★値の漏洩ではない**（ファイル名と節番号だけ）**ので法務上の問題ではなく、導線の問題である。** なお `check-criteria.md` はディレクトリを伴わない表記で、直前の兄弟ファイルのパスから推測させる形になっている |
| **`R-6`** | **原稿に由来が無い** | 移した後の §5.5.9 には、**どのサブの成果かを示す語が 1 つも無い**（`M26-03` の語が原稿内に 0 件）**。★兄弟節は持っている**——§5.5.8 は見出しに `CHANGE-103・M20-06 の as-built`、§5.5.7 は本文 1 行目に「M19 の 5 サブで実際に起きた形である」。**⇒ 「完了報告を引かない」と決めた以上、由来が無いと実測値の在り処も辿れない。** `CHANGE` 番号は `D-293` により製造が書かないので正しいが、**サブ名まで落とす必要は無い** |
| **`R-7`** | **挿入位置の引用行が見出しの前に在る** | `:536` の引用行は `#### 5.5.9` の**手前**に置かれている。**⇒ 設計卓が落とし忘れると、§5.5.8 の末尾に付いた注記として読める。** 見出しの直後（「（2026-09-11 追加）」の後）へ置けば、落とし忘れても §5.5.9 の中に収まり無害である。**★指示の効き目は変わらない** |

### 束 C の回帰 ／ `E-225` の確認

- **束 C: 回帰なし。** `git diff --stat 0055cfc -- docs/design/ docs/process/ scripts/ character_data/ docs/human-notes/ docs/instructions/ CONTRIBUTING.md REUSE.toml SECURITY.md NOTICE README.txt` は**空**。**★`docs/design/` は 1 バイトも触られていない。⇒ `CLAUDE.md` §8 の「設計書本体は製造が直接編集しない」を守っている。**
- **`E-225`: 問題なし。** `git diff --numstat 2e35bbd` は `M26-03-completion-report.md` が `836 / 0`、`m26-03-review.md` が `272 / 0`、`progress-log.md` が `13 / 0`。**新規追加の 2 本に deletions は付いていない。** 全体の 1 deletion は `NOTICE`（窓口導線の差し替え）ただ 1 件で、これは §2.4 の意図した変更である。
- **常設検査**: `check-md-emphasis.sh`（完了報告 ／ レビュー報告の 2 本）・`check-progress-log-index.sh`・`check-doc-inventory.sh` をレビュー側で回し直し、**いずれも緑**。`progress-log.md` の索引行 7 は 3 裁定を分けて書く形へ更新されている。

### 推奨修正

#### 高

- **高-A（`R-2`）: 失効した節番号を指す 4 行を直す。** `:13` の `§7.0` → 「§7 冒頭の表」。`:393` / `:399` の `§7.2 [5]` → `§7.B [5a] [5b]`。`:835` の `§7.3 の「判断の分かれ目 8 件」` → `§7.A (d) の「判断の分かれ目 9 件」`。**★本書の結語が古い番号と古い件数を名指している状態は、後任が最初に読む行であるだけに重い。** 型検査も lint も見ない種類の誤りである。
- **高-B（`R-3`）: 消えた 中-4 の注記を §7.B へ戻す。** 逐語は旧 §7.4 の 1 段落をそのまま使えばよい（`git show 0055cfc:docs/progress/M26-03-completion-report.md` の `:589`）**。★原稿の 60〜80 行には影響しない**（§7.B は原稿の外）**。⇒ 戻さないなら §10 の `:804` を「採用したが組み替えで消えた」へ直すこと。★どちらか一方は必須である**——現状は取り込み表と本文が食い違っている。
- **高-C（`R-1`）: 原稿の末尾に、実行コマンドの在り処を 1 行だけ置く。** 例＝「**★由来＝`M26-03`。実行コマンドの逐語と前回の実測値は同サブの完了報告 §7.A / §7.B に在る**（再現用。判断の中身は本節が正本）」。**`SUPP-001` は `:1094` / `:1187` で既に「完了報告に貼ること」と書いており、参照の形として異物ではない。⇒ この 1 行で `R-1` と `R-6` が同時に閉じる。** 行数は 73 行になり `D-820` の範囲内に収まる。

#### 中

- **中-A（`R-4`）: 原稿 (c) の「31 本のヘッダ」を数を持たない表記へ。** 例＝「`character_data/*.csv` の全ヘッダを `sort -u` して 1 種類であることを確かめる」。**★§7.B の実コマンドは既にグロブで書かれているので、原稿側を合わせるだけである。**
- **中-B（`R-5`）: (d) 1 の参照先 2 本に「開発リポジトリでのみ読める」旨を 1 句添える。** あわせて `check-criteria.md` を `docs/seed-data/check-criteria.md` とフルパスで書く。**★切り出し後は兄弟行からの推測が効かない。**

#### 低

- **低-A（`R-7`）: 挿入位置の引用行を `#### 5.5.9` の直後へ移す。** 落とし忘れたときの被害が §5.5.9 の内側に閉じる。
- **低-B**: 本ファイルの「追走の記録」節の見出しが `D-819` ／ `D-821` の 2 つだけを挙げており、**`D-820` が入っていない**。完了報告 `:13` は 3 つを挙げる形へ直っているので、こちらも揃えると読み手が数を取り違えない。
- **低-C**: §8.13 に保存したリレー文の本文に「7.2 が実行できるコマンド列、7.3 が判定の分かれ目 8 件、7.4 が差分の取り方」が残る（`:699` / `:721`）**。★これは逐語の歴史記録であり書き換えるべきではない。⇒ 直上の「役目を終えた」注記に「★本文中の §7.2 / §7.3 / §7.4 は当時の節番号である」を 1 句足すだけでよい。**

### 良かった点

- **★3 つの裁定を「まとめて決着済み」と畳まなかった。** §7 冒頭の 3 行表は `D-819` ＝置き場 ／ `D-820` ＝番号と長さ ／ `D-821` ＝挿入位置と重複処理、と**何がどれで決まったかを分けて書いている。⇒ 次に `SUPP-001` を触る人が、どの裁定を引けばよいか迷わない。** §8-10 / §8.13 / §8.14 / `progress-log` の索引行まで同じ粒度で揃えている。
- **★★挿入位置を「原稿の外に書くだけでは落ちる」と見抜いて、ブロックの内側にも置いた。** `D-821` の指示は「1 行添えること」だけで、**内側か外側かは書かれていなかった。⇒ 切り出し後の状態を想像して置き場を決めている。**
- **★節番号を実物で検算し、`D-819` の `§5.5.7` を差し戻した**（§8.14）**。裁定をそのまま写さなかった。⇒ 写していれば、既に埋まっている番号の上へ新節が乗るところだった。**
- **★★自分の初回レビュー報告書自身が同じ欠陥を持っていたことを見つけ、是正して記録した**（本ファイルの「追走の記録」）**。しかも「走査を直せば済む」で終わらせず、「狭い網は取りこぼし、広い網は合否に使えない ⇒ 書き方の側で守るしかない」という結論まで出し、原稿 (d) 9 へ畳んでいる。⇒ 指摘の再生産を、規則ではなく判断として残した。**
- **原稿の型が兄弟節と一致している。** 見出しレベル `####` ／ 日付行「**（2026-09-11 追加）**」／ 動機を述べる太字の前置き 1 段落 ／ 判断は表 ／ 末尾に「★記載先はここであり、`DATA-LICENSE.md` §4 ／ `REUSE.toml` ではない」の置き場の規約。**★最後の 1 文は §5.5.5 の末尾と同じ形であり、実物を見て書いたことが分かる。★兄弟節はコードフェンスを 1 つも持たないので、コマンドを原稿へ入れなかった判断そのものは型として正しい**（在り処の 1 行だけが足りない＝高-C）。

### 制約事項

- **★チェックリスト束 B の実物を読んでいない。** 製造の作業ツリーには `v1.0.0` しか無く、設計卓が発行した `v1.2.0` は別ブランチに在る。**⇒ 本再レビューはタスク指示に貼られた束 B v1.2.0 の逐語を全文として扱った。** `B-5` / `B-6` の判定はその逐語に対するものである。
- **★`D-819` / `D-820` / `D-821` の原文を読んでいない。** 参照できたのは完了報告 §7 の表と §8.13 / §8.14 の記述、およびタスク指示の要約のみ。**⇒ 「3 つの裁定が何を決めたか」の正しさは検証していない。** 不明: `D-820` が「チェックリストへ束 B-5・B-6 を追加」以外に原稿へ求めた事項があるかは判断できない。
- **本体の調査結果**（完了報告 §1〜§6）**は初回レビューで確認済みのため再確認していない。** 本再レビューは `0055cfc` 以降の差分と、原稿の切り出し可能性に限定している。
- **`SUPP-001` への実際の反映は見ていない**（未反映であることが正しい）**。⇒ 反映後に節番号・挿入位置・引用行の除去が意図どおりになったかは、設計卓側で確認が要る。**
- **初回レビューの 13 件の取り込み内容は再確認していない**（今回 `R-3` で 1 件の消失を見つけたのは、組み替えの差分を読んだ副産物である）**。⇒ 他の 12 件が組み替えを生き延びたかは全数では見ていない。**

---

## 取り込み結果（再レビュー・自動トリアージ・2026-09-11）

**指摘 8 件**（高 3 ／ 中 2 ／ 低 3）**を 8 件すべて採用した。★不採用 0 件。⇒ 「高」指摘の自動棄却は発生しておらず、エスカレーションは不要であった。**
**★これで往復は 2 回（初回 ＋ 再レビュー 1 回）＝上限到達である**（`CLAUDE.md` §9）**。⇒ 未解消の指摘は 0 件のため、`followup-backlog.md` §J へ記録すべき項目は無い。**

| # | 採否 | 対応 |
|---|---|---|
| **高-A**（`R-2`） | **採用** | 失効した節番号 4 か所を是正〔`§7.0` → 「§7 冒頭の表」／ `§7.2 [5]` ×2 → `§7.B [5a] [5b]` ／ 結語 → `§7.A (d) の「判断の分かれ目 9 件」`〕。**★結語が古い番号と古い件数で本書の値打ちを名指していた点は、指摘のとおり重い** |
| **★高-B**（`R-3`） | **採用** | 消えていた初回 中-4 の注記を **§7.B の末尾へ戻した**。**★実測値も現在へ更新**（`docs/` 1484 件 ／ 合計 2956 件）**。★§10 の取り込み表も「一度消えて戻した」経緯つきへ直した。⇒ 取り込み表と成果物の食い違いを解消した。** あわせて「**節を組み替えるときは、その節が抱えていた取り込み済みの指摘を数え直すこと**」を注記として残した |
| **高-C**（`R-1` ＋ `R-6`） | **採用** | 原稿の末尾へ 1 行〔由来＝`M26-03` ／ 実行コマンドと前回の実測値の在り処 ／ **判断の中身は本節が正本**〕。**⇒ `R-1` と `R-6` が同時に閉じた。** 原稿は **76 行**で `D-820` の 60〜80 に収まる |
| **中-A**（`R-4`） | **採用** | 「31 本のヘッダ」をグロブ表記へ。**★本数を書かない理由も添えた** |
| **中-B**（`R-5`） | **採用** | フルパスへ揃え、**「開発リポジトリでのみ読める」を明記** |
| **低-A**（`R-7`） | **採用** | 挿入位置の引用行を見出しの直後へ移した |
| **低-B** | **採用** | 本ファイルの「追走の記録」の見出しへ `D-820` を追加 |
| **低-C** | **採用** | §8.13 の直上へ「本文中の節番号は当時のもの」を 1 句。**★逐語の本文は直していない** |

**★製造が自分で見つけて直したものが 1 件**——§8-10 が `DRAFT` マーカーの文字列を逐語で書いていたため、`grep` での原稿抽出が **2 件に当たり範囲指定が壊れた**（本追走の検証で実際に踏んだ）。**⇒ 逐語をやめ、理由つきで注記した。**

**★レビュアーの制約事項を 1 件埋めた。** 逐語＝「初回レビューの 13 件の取り込み内容は再確認していない。⇒ 他の 12 件が組み替えを生き延びたかは全数では見ていない」。**⇒ 製造が 13 件を全数照合した。★生き延びていなかったのは 高-B の 1 件のみで、残り 12 件は現存する。**
