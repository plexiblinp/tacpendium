# M30-04 レビュー報告書（1 巡目）

> **★2 巡目は `docs/progress/m30-04-review-2.md` に在る。** 本書はチェックリスト未発行の時点で、指示書 §4 / §6 を代用の観点束として実施したものである（チェックリストは `D-816` で事後発行された）。**本文は歴史記録として書き換えない。**

| 項目 | 内容 |
|------|------|
| 対象 | M30-04（必殺技ファミリー行の派生変種を末尾へ回す） |
| 指示書 | `docs/instructions/M30-04-derived-variants-to-row-end.md` v1.1.0 |
| 完了報告 | `docs/progress/M30-04-completion-report.md`（Phase A 完了時点） |
| 着手基点 | `23b8a08` ／ 対象コミット 5 本（`67b6be2` / `acc06a8` / `528c241` / `2067d17` / `8b69224`） |
| レビュー実施日 | 2026-09-10 |
| チェックリスト | 未発行（`D-736` の運用だが本サブでは起票されず）。⇒ 指示書 §4 / §6 を観点束として使用 |
| 独立実行した検査 | `go test ./...` ／ 対象 2 spec の Vitest ／ `make e2e-only P=m30-04` ／ 機械検査 8 本（本文 §検査の再実行に記載） |

---

## 総評

**実装は堅い。** 判定をファミリー単位の AND に畳んだこと、`sort` を使わず 2 群 filter 連結にしたこと、陽性対照を move id からの独立復元で作ったことは、いずれも指示書 §4 の危険（件数の消失・群内順序の破壊・対照の欠如）に正面から答えている。私の側で `go test ./...`・Vitest 61 本・`make e2e-only P=m30-04`（4 passed）を独立に回して全部緑を確認した。完了報告の数値（372 / 178 / 47・混在 5 件）も、そのテストが実データから計算した値であることを確認した。

**指摘の主軸は 3 つである。** ①「失効した記述」の走査がコードだけで止まっており、`docs/handover/` に 1 か所生き残っている ／ ② `progress-log.md` への索引行が無く、常設検査 `scripts/check-progress-log-index.sh` が実際に赤である ／ ③ `getByIDSQL` へ足した列に回帰テストが 1 本も無く、次に落とされても全部緑で通る。

**設計上の帰結は正しく認識され、正しく回されている。** 372 のうち 178（48%）が末尾へ回り、その中に jamie の流酔拳・流酔脚のような主力必殺技が含まれる点を完了報告 §3 が自分から書いて設計卓へ回しているのは、本プロジェクトが繰り返し落としてきた「片側だけ解消して完了と書く」型を避けている。ただし開発者承認の逐語がボードに残っていないため、設計伝達レポートで残す必要がある。

---

## 設計準拠性レビュー結果

### 指示書 §2.1（段 1・`is_derived` をリスト DTO へ届ける）: ◎

| # | 条件 | 判定 | 確認内容 |
|---|---|---|---|
| 1 | G-12 の 4 点同期 | ◎ | `model.Move.IsDerived` / `MoveListItem.IsDerived` / `listByCharacterSQL` の `m.is_derived` / Scan 位置 の 4 点が揃う。`divergence_test.go` は `MoveListItem` ⊆ `model.Move` を同名・同型で強制する反射ガードで、`go test ./internal/repository/move/...` が緑 |
| 2 | JSON タグ camelCase | ◎ | `json:"isDerived"`。`omitempty` を付けていない判断も正しい（`isAerial` / `setupOnly` と同扱い。false でキーが消えると受け側が未定義と区別できない）。`TestHandler_List_IsDerived` が JSON レベルで false キーの存在まで見ている |
| 3 | フロントの API 型へ追加 | ◎ | `web/src/features/moves/types.ts` の `Move.isDerived: boolean`（必須）。`MoveDetail` へ足していない理由も型定義の冒頭に書いてある |
| 4 | `damage` を足さない | ◎ | `MoveResponse` / `MoveListItem` に `damage` は入っていない |

**契約の広がりが最小に抑えられていることを別経路で確認した。** `model.Move` は JSON タグを持つが、production の API で `model.Move` をそのまま返す経路は無い（`internal/api/` / `internal/service/` の非テストコードに `model.Move` の出現ゼロ）。`MoveDetailResponse` は明示フィールド列挙で埋め込みではないため、`GET /api/moves/:id` の契約は実際に不変である。⇒ 増えた外部契約は `GET /api/moves` の 1 本だけ。

### 指示書 §2.2（段 2・並べ替え）: ◎

| # | 条件 | 判定 | 確認内容 |
|---|---|---|---|
| 1 | derived を末尾へ | ◎ | `inputResolution.ts` 段 3。ロスターテストが 31 キャラ全数で「`firstDerived` 以降が全部 derived」かつ「末尾ブロック長 == derived 件数」を主張 |
| 2/3 | 群内の相対順を変えない | ◎ | `sort` を使わず 2 回の filter を連結。`Array.prototype.filter` は順序保存が仕様で保証されているため、安定ソートの有無に依存しない。読んで分かる形になっている |
| 4 | 1 件も消さない | ◎ | 連結なので件数は構造的に保存される。加えてロスターテストがファミリー名の集合一致を 31 キャラ全数で主張し、E2E が jamie 21 件・blanka 21 件を全数固定している |

**AND の畳み方に穴は見つからなかった。** 段 1 で raw ファミリー単位に `(get() ?? true) && m.isDerived`、段 2 で基底へ畳むとき `entry.isDerived && (rawDerived.get(family) ?? false)`（`entry` の初期値 true）。基底ファミリーは必ず `order` に載った raw ファミリーから作られるため、AND から漏れる move は存在しない。「plain が true・変種が false」という逆向きの混在も段 2 の AND で false になり、ロスターテストの混在検査が全 372 ファミリーを走査してこれを捕まえる形になっている。

**混在 5 件の実測が独立に再計算されている。** `moveSurfacing.roster.test.ts` の最後の it は、`fam.byVariant` の move id 集合から元 move を引き直して `some` / `every` を計算しており、実装の `isDerived` を鵜呑みにしていない。私が独立に走らせて緑を確認した。

### 指示書 §2.3（段 3・測る）: ○

| # | 条件 | 判定 | 内容 |
|---|---|---|---|
| 1 | キャラ別件数（前後で総数不変） | ◎ | 完了報告 §1.2。テストが 31 キャラの連想配列を丸ごと固定。合計 372 |
| 2 | 末尾へ回った件数 | ◎ | 完了報告 §1.3。合計 178。`M30-02` の 66 件との母集団差（A1 のみ vs A1+A2）を明示している |
| 3 | 陽性対照 | ○ | 下記参照 |
| 4 | 魔身が 1 件も消えていない | ◎ | 単体（7 ファミリー名を全数固定）と E2E（`the_devil_inside*` が 7 件）の 2 経路 |
| 5 | `M30-03` の A2 105 件が消えていない | ◎ | blanka `lightning_beast_rolling_attack` の 4 強度と jamie `drink_level_4_*` 3 件を名指しで固定 |

**陽性対照の独立性 ── 順序については独立、分類については独立でない。**

- **順序**: `originalOrder()` は各ファミリーの move id 最小値で並べ直しており、実装の `outOrder` 構築ロジックを読み直したものではない。着手時点の出力順が「基底ファミリーの初出順」だったこと、および `loadCharacter` が CSV 行順で id を振ることから、この 2 つが一致することは独立に検証できる。**対照として機能している。** 加えて「jamie では着手時点の derived の最上位が index 1」という単一の具体値まで固定しており、47 という集約値が空回りしていないことを担保している。
- **分類**（あるファミリーが derived か）: 47 も 178 も `fam.isDerived`（＝実装の出力）を数えている。**分類そのものの独立オラクルは、混在 5 件を捕まえる 1 本だけである**（後述の中-1）。

### 指示書 §3（やらないこと）: ◎ ── 全 5 項目に抵触なし

| # | やらないこと | 判定 | 確認 |
|---|---|---|---|
| 1 | 削る・未分類へ移す | ◎ | 件数保存をテスト・E2E の両方で固定。E2E の 3 本目が「末尾へ回った魔身（酔い+2）を実際にクリックしてステップが 1 件増える」ことまで見ている |
| 2 | `is_derived` を入力面の可否に使う | ◎ | `isDerived` の参照箇所は `deriveSpecialFamilies` の段 3（並べ替え）のみ。`grep` で全数確認。`isOnSpecialTab` は `category==='special'` のままで、可否の述語に `is_derived` は 1 か所も入っていない。`VirtualController.tsx:270-271` の「ターゲットコンボタブを `is_derived` で絞らない」も維持されている（`M30-01` §2.9 (a) / `D-807` に整合） |
| 3 | CSV 行順の並べ替え・id の振り直し | ◎ | `character_data/` と `migrations/` に差分ゼロ |
| 4 | `code` からの代理判定 | ◎ | 判定は `m.isDerived` のみ。`code` を見る分岐は無い |
| 5 | `damage` をリスト DTO へ | ◎ | 追加なし |

### マイグレ消費 0 本 / CHANGE 消費 0 本: ◎

`git diff --stat 23b8a08 -- migrations/` は空。CHANGE の自採番もしていない（`D-293` 遵守）。設計書 3 本への CHANGE 請求は完了報告 §6 で設計伝達レポートへ回す形になっており、`CLAUDE.md` §8（設計書は製造が直接編集しない）を守っている。

### `getByIDSQL` への追加（完了報告 §2.2 の射程外判断）: ○ ── 判断は妥当だが検査が無い

**判断そのものは妥当であり、射程外ではない。** `MoveDetail` は `model.Move` を埋め込むため、本サブが `model.Move` に列を足した時点で「SELECT しないと `MoveDetail.IsDerived` が常に false になる」罠が新設される。これは本サブが作った罠であり、本サブで塞ぐのが正しい。`MoveDetailResponse` は明示列挙なので契約も動いていない。「本サブが作った罠は本サブで塞ぐ」という完了報告の説明に同意する。

**ただし回帰テストが 1 本も無い**（中-2 として後述）。

### 教訓 `E-225`（新規追加のはずのファイルに deletions が付いていないか）: ◎

`git diff --numstat 23b8a08` を全行読んだ。

- 新規ファイルは 2 本（`web/e2e/m30-04-derived-to-row-end.spec.ts` = `129 / 0` ／ 完了報告 = `253 / 0`）。どちらも deletions 0
- deletions 60 行は、Go 側 44 行が構造体の gofmt 再整列とコメント書き換え、TS 側 4 行が 1 行フィクスチャの書き換え（`isDerived: false` を同じ行へ挿入）、残りが `handler_test.go` のコメント 2 行とキー一覧 2 行の書き換え
- **消えたテスト関数・消えた `it` / `expect` は 0 件**。`handler_test.go` は `TestHandler_List_IsDerived` が純増（84 / 4）で、既存 2 本のキー一覧に `isDerived` が足されただけ

完了報告 §5 の主張（deletions 60 行の内訳・消えたテスト 0 本）は実物と一致する。

### 完了報告の数値と実物の一致: ◎

| 主張 | 検証 | 結果 |
|---|---|---|
| ファミリー 372 / 末尾 178 / 陽性対照 47 / 混在 5 | `pnpm exec vitest run` で当該 2 spec を実行 | 61 tests passed。数値はテストが実データから計算して assert している |
| E2E 21 件（jamie）・21 件（blanka）の顔ぶれと順序 | `make e2e-only P=m30-04` | 4 passed（29.5s） |
| `go test ./...` 緑 | 独立実行 | 失敗 0 |
| §5 の変更統計 33 files / 677 insertions | `git diff --stat 23b8a08` は 34 files / 930 insertions | 差は完了報告自身の 253 行。930 − 253 = 677 で一致 |
| §1.5「相対順テストは並べ替えを外しても緑」 | コードから確認 | 正しい。`originalOrder(fams)` は並べ替えを外すと `fams` と同一列になり、両辺が恒等的に一致する。**自己申告どおり、この 1 本は並べ替えの有無を捕まえない** |

---

## 設計準拠性以外の指摘事項

### コーディング規約（`CLAUDE.md` §4）

- **Go**: JSON タグ camelCase ◎ ／ godoc ◎（`model.Move.IsDerived` に errata③ の含意まで書いてある）／ `fmt.Println` 等の混入なし ◎
- **TypeScript**: `any` なし ◎ ／ `strict` 通過 ◎ ／ import 順 ◎（`check-import-order.sh` 違反なし）
- **列挙定数の同期**: 本サブは列挙値を増やしていない。`check-enum-sync.sh` ベースラインどおり ◎
- **ブラウザストレージ**: 使用なし ◎（`check-browser-storage-keys.sh` 違反なし）

### 機械検査の再実行（私が独立に回した結果）

| 検査 | 結果 |
|---|---|
| `scripts/check-artifact-integrity.sh` | 違反なし |
| `scripts/check-progress-log-index.sh` | **違反 1 件**（後述 高-2） |
| `scripts/check-doc-refs.sh` | dead reference なし |
| `scripts/check-import-order.sh` | 違反なし（BASELINE 引き下げ勧告は据え置きで正しい。後述 低-2） |
| `scripts/check-enum-sync.sh` | ベースラインどおり |
| `scripts/check-stop-discipline.sh` | 違反なし |
| `scripts/check-doc-inventory.sh` | 型に無いファイルなし |
| `scripts/check-derived-docs.sh` | `code-facts.md` / `docs-map.md` / `custom-commands.md` が陳腐化疑い（後述 中-3） |
| `scripts/check-md-emphasis.sh docs/progress/M30-04-completion-report.md` | 0 行 |

**完了報告 §4 の検査表に `check-progress-log-index.sh` が載っていない。** 12 本を挙げたうちに、実際に赤になる 1 本だけが欠けている。表に無い検査は回されないので、抜けたこと自体を指摘対象とする。

### セキュリティ・ライブラリ

- 新規依存の追加なし。`go.mod` / `package.json` に差分なし ◎
- 露出した列は派生技フラグであり、機密性・個人情報の観点で問題なし ◎

---

## 推奨修正（優先度別）

### 高（M30 完了前に修正必須）

**高-1: 失効した記述が `docs/handover/M19-DESIGN-01-central-requirements.md:47` に残っている**

```
**R7（引き当て可能性）**: … UI 表示に使う場合の DTO 追加が既存 `GET /api/moves` の
後方互換で足せること（現状 `is_derived` は model 非搭載＝API 非露出、RESEARCH-02 R-C-1）。
```

**「現状 `is_derived` は model 非搭載＝API 非露出」は本サブで失効した。** 完了報告 §2.4 の走査はコード（`internal/` ＋ `web/src/`）で止まっており、`docs/` 側を `grep` していない。私が `docs/` 全体を走査した結果、**「露出していない」系の生きた記述で失効しているのは以下の 3 か所である。**

| 場所 | 状態 | 誰の手番か |
|---|---|---|
| `docs/design/03-data-model.md` §3.3 `is_derived` 行「model の `Move` 構造体には持たせていない＝`GET /api/moves` のレスポンスに出ない」 | 失効 | 設計卓（完了報告 §6-6 で捕捉済み・CHANGE 請求へ） |
| `docs/handover/followup-backlog.md` G-12 | 失効（本サブが実現した項そのもの） | 設計卓（完了報告 §6-4 で捕捉済み） |
| **`docs/handover/M19-DESIGN-01-central-requirements.md:47`（R7）** | **失効・未捕捉** | **設計卓（設計伝達レポート §4 へ追加が要る）** |

**なぜ「高」か。** 本プロジェクトの較正どおり、この型はテストでも lint でも型検査でも赤にならず、後任は本文をコピーして注記を読まない。とくに R7 は「要件の引き当て可能性」を述べた行であり、次に `is_derived` の UI 露出を検討する担当が「まだ露出していない」を前提に費用見積もりをする直接の経路になる。製造は同書を編集できない（設計卓の成果物）ため、**設計伝達レポート §4 に「R7 の括弧書きが失効した」を明記して回すこと**が是正の内容である。

`docs/change-notes/` 配下の 4 件（`change-report-069.md` / `CHANGE-069` / `CHANGE-070` / `CHANGE-085-addendum`）にも同趣旨の記述があるが、**これらは発行済みの通知書＝歴史記録であり書き換え対象ではない。** 走査対象から外してよい。

**高-2: `docs/progress/progress-log.md` への索引行が無い（常設検査が赤）**

```
$ bash scripts/check-progress-log-index.sh
NG  作業 ID `m30-04` が docs/progress/progress-log.md に現れない
    (完了報告: docs/progress/M30-04-completion-report.md)
結果: 違反 1 件
```

指示書 §6 の完了条件 8 と `CLAUDE.md` §8（「サブ完了時の追記は必須」）に対する未達である。Phase D で足す運用だとしても、**完了報告 §4 の検査表に本スクリプトが挙がっていない**ため、このまま完了扱いにすると気づかれずに残る。`M30-03` の索引行（`progress-log.md:5751`）と同じ形式で 1 行足すこと。あわせて完了報告 §4 の表へ本検査を追加すること。

### 中（M31 着手と並行可）

**中-1: `isDerived` の分類そのものに、全数の独立オラクルが無い**

`moveSurfacing.roster.test.ts` の混在検査は、`fam.byVariant` の id から元 move を引き直して `anyD` / `allD` を独立に計算しているのに、**`anyD && !allD` のときしか `expect(fam.isDerived).toBe(false)` を主張していない。** 残り 367 ファミリーについては、`fam.isDerived` を「そういうものだ」として数えているだけである。

```ts
// 現状（混在のときだけ主張する）
if (anyD && !allD) expect(fam.isDerived).toBe(false);

// 提案（全 372 ファミリーで AND の畳み方そのものを独立に主張する）
expect(`${code}/${fam.family}`).toBe(
  fam.isDerived === allD ? `${code}/${fam.family}` : `${code}/${fam.family}(isDerived=${fam.isDerived} want ${allD})`,
);
```

1 行の変更で、178 / 47 という 2 つの集約値が依存している分類ロジックに全数の床が入る。**現状は「AND を OR に取り違える」以外の壊れ方（例：段 2 の畳み込みから特定の変種が漏れる）を、5 件の混在ファミリーが偶然その形を含んでいるかどうかに賭けている。**

**中-2: `getByIDSQL` へ足した `is_derived` に回帰テストが 1 本も無い**

完了報告 §2.2 の判断（列を足す）は妥当だが、**`GetByID` が `IsDerived` を載せることを主張するテストが存在しない。** `internal/repository/move/` に `GetByID` を直接叩くテストは `rush_test.go:43` の 1 か所だけで、そこは rush 生成の前段として呼んでいるにすぎず `IsDerived` を見ていない。`MoveDetailResponse` へ露出していないので E2E からも見えない。

⇒ **次に誰かが `getByIDSQL` から `m.is_derived` を落としても、テストも lint も型検査も全部緑のまま通る。** これは完了報告 §2.2 が「静かに壊れる形を塞いだ」と述べた罠を、塞いだ側に作り直しているのと同じである（同 §2.2 が自ら挙げた `LastChangedGameVersion` の先例がまさにこの形で残ったものである）。リポジトリ層に、`is_derived=1` の行を入れて `GetByID(...).IsDerived == true` を見る 10 行のテストを 1 本足すこと。

**中-3: `docs/handover/code-facts.md` の DTO 定義が失効した**

```
522: **MoveResponse** … / isAerial `bool` / setupOnly `bool` / nameJa,omitempty `*string` …
669: **MoveListItem** … / IsAerial `bool` / SetupOnly `bool` / NameJa `*string`
```

どちらも `isDerived` / `IsDerived` を欠く。code-facts は「実コードを引かずに想定で書く」を防ぐための派生資料であり、次の担当がここを見て「まだ露出していない」と読む経路そのものである。

**「高」の型だが「中」に置いた理由を明示する。** ① 同書は本サブ着手前から陳腐化していた（生成 2026-09-05 / 源泉の変化量 282 件 = 23%）ため、本サブが作った失効ではない ／ ② 再生成（`/regen_code_facts`）は本サブ射程外の 282 件を巻き込むため、いま回すかマイルストーン境界で回すかは判断を要する。**⇒ 「いま回す / 境界で回す」のどちらでもよいが、決めずに放置しないこと。** 境界送りにするなら設計伝達レポート §4 へ 1 行残すこと。

**中-4: 178 / 372（48%）が末尾へ回る帰結について、開発者承認の逐語が記録に無い**

完了報告 §3 は「着手前に開発者へ提示し、案 A で進める判断を得ている（2026-09-10）」と書いているが、`docs/process/parallel-board.md` の最終更新（`D-814` 時点）にこの応答の記録が無い。ボードは設計卓の手番なので製造の不備ではないが、**この判断はハード列（ユーザー体験に影響する）そのものである** ── 開発者の逐語は「魔身がたくさん並んでいるのは煩雑」「魔身は攻撃技ではないので必殺技の最後に置きたい」であり、jamie の流酔拳・流酔脚のような主力必殺技まで末尾へ回ることは逐語の射程を超えている。

⇒ 設計伝達レポート §4 に **① 178 / 372 という規模 ② 末尾へ回る主力技の具体例 ③ 案 A を選んだ経緯の逐語** の 3 点を残し、設計卓が裁定できる形にすること。完了報告 §3 が既に ①② を書いているので、残るのは ③ と回付先の明示である。

**中-5: 段 1 と段 2 で欠損時の既定値の向きが逆である**

```ts
// 段 1
rawDerived.set(family, (rawDerived.get(family) ?? true) && m.isDerived);
// 段 2
entry.isDerived = entry.isDerived && (rawDerived.get(family) ?? false);
```

段 1 の `?? true` は AND の単位元として正しい。段 2 の `?? false` は防御であり、`order` に載った family は必ず `rawDerived` を持つため到達不能だが、**読み手には「欠損時の向きが逆」に見える。** 到達不能であることをコメントに 1 行書くか、`?? true` に揃えること（揃えても挙動は変わらない）。動作影響なしのため優先度は中の下。

### 低（将来対応）

**低-1: `families[0]` が非 derived であることの根拠がコメントにしか無い**

`SpecialMovePanel.tsx:99-100` と E2E の 4 本目に「31 キャラの実測で index 0 が derived になるキャラは 0 件」とあるが、E2E は jamie 1 キャラしか見ていない。

**ただしこれは実質的に固定されている** ── §1.2 の総数表と §1.3 の derived 件数表が 31 キャラ全数を assert しており、どのキャラも「derived 件数 < 総数」だからである。あるキャラが全 derived になれば §1.3 の表が赤になる。**⇒ 主張は破れないが、その連関がどこにも書かれていない。** ロスターテストに `expect(fams[0].isDerived).toBe(false)` を 1 行足すか、コメントで「§1.3 の表が床である」と書くのが望ましい。

**低-2: `check-import-order.sh` の「BASELINE を 99 へ下げること」勧告への対応**

完了報告 §4 の注のとおり、`claude/` レーンでは下げてはならない（スクリプト冒頭の禁止事項）。**据え置いた判断は正しい。** ただし「実行時メッセージと冒頭コメントが食い違っている」状態が `M30-03` から本サブまで 2 サブ連続で再現しており、次のレーンも同じ迷いを踏む。スクリプト側にレーン判定を入れるか、メッセージへ「レーンで作業中なら下げないこと」を足すことを、改善レーンの候補として `followup` §J 経由で出すのが妥当（本サブの手番ではない）。

**低-3: `inputResolution.test.ts` の追加 `describe` の直前に空行が無い**

`});` の直後に `describe(` が続いている（既存ブロックとの区切りが無い）。Prettier は通っているので機械検査は赤にならない。可読性のみ。

---

## 良かった点

1. **陽性対照を「実装の読み直し」ではなく別軸（move id の最小値）から作った。** 指示書 §4.2 が求めたのはここで、しかも「実装をコピーすると実装が間違っていても同じように間違える」という理由まで書いてある。集約値 47 だけでなく「jamie の derived 最上位は index 1」という具体値を併記していて、集約が空回りしていないことまで担保している。

2. **`sort` を使わなかった理由が明示されている。** 「安定ソートの有無に依らず、群の中の相対順が初出順のまま残ることが読んで分かる形にする」── 挙動が同じでも読み手の検証コストが違う、という選択理由を書いているのは良い。

3. **検査自体が効くことを確かめている（完了報告 §1.5）。** 並べ替えの 6 行を外して 2 本のテストが赤になることを確認し、そのうえで「相対順テストは外しても緑である」ことまで自分から書いている。**自分の床の弱いところを自分で申告している** ── 私がコードから独立に検証したところ、この申告は正確だった（`originalOrder(fams)` は並べ替えを外すと `fams` と恒等になるため、両辺が必ず一致する）。

4. **判定をファミリー単位の AND にした判断と、その理由が実データで裏付けられている。** 混在 5 件を名指しで固定し、「素が押せるファミリーを末尾へ沈めない」という理由を `SpecialFamily.isDerived` の JSDoc に書いている。`M30-02` が「素は押せるが OD が押せない」を 7 組作った先例を踏まえた設計になっている。

5. **`is_derived` の意味の誤読を、コードのあらゆる層で先回りして塞いでいる。** `model.Move` の godoc・`MoveListItem` のコメント・`MoveResponse` のコメント・TS の `Move`・`SpecialFamily` の JSDoc の 5 か所すべてに「単独入力が不可能ではない（errata③）」「入力面の可否に使わない（`D-807`）」が書いてある。指示書 §4.3 の「派生技だけが末尾へ行く、と説明しないこと」への応答として過不足がない。

6. **`getByIDSQL` の罠を自分で見つけて塞ぎ、同型の未修正（`LastChangedGameVersion`）を射程外として申告した。** 「本サブが作った罠は本サブで塞ぐ／着手前から在る同型は射程外として回す」という線の引き方が明快である。

7. **失効した記述をコード側では全数直している。** `setplay/repository.go`（パッケージ doc ＋ SQL コメント）・`rush.go`・`punish/repository.go` の 3 か所は、いずれも「消す」ではなく「どの部分が失効し、どの部分が引き続き有効か」を書き分けている。とくに `punish` の「`damage` / `is_projectile` については引き続き有効」は、全消ししていたら次の担当が専用投影を持つ理由を見失うところだった。あわせて「失効しなかったもの」を 2 件名指しで残しているのも良い（読んだ証拠になる）。

8. **`getByIDSQL` のコメントから、実在しない `upsertMoveSQL` への参照が結果的に消えた。** 旧コメントは「model.Move の全列を upsertMoveSQL と同じ列順で SELECT し」だったが、`upsertMoveSQL` はリポジトリ内に存在せず（`grep` で 0 件）、`model.Move` の全列でもない（`LastChangedGameVersion` を欠く）。書き換え後の「model.Move の列を SELECT し」は両方の不正確さを解消している。

---

## 不明点

- **不明: 完了報告 §3 の「着手前に開発者へ提示し、案 A で進める判断を得ている（2026-09-10）」の逐語**。`docs/process/parallel-board.md`（`D-814` まで）・`docs/handover/followup-backlog.md` のいずれにも該当する記録が見当たらず、コード上からも判定できない。応答が実在したかどうかを私は判定できないため、指摘ではなく「記録に残すこと」（中-4）として扱った。

---

## 制約事項

- 本レビューはコード上で判定可能な範囲のみ対象。実際の動作確認・パフォーマンス・実機テストは別途実施が必要。
- 本レビューで独立に実行したのは読み取り専用の検査のみである（`go test ./...` ／ Vitest 2 spec ／ `make e2e-only P=m30-04` ／ 機械検査 9 本）。`make e2e` の全数は実行していない（完了報告 §4-7 が Phase C 送りとしている）。
- 「末尾へ回った 178 件が UI として妥当か」は実機での見え方に依存するため、コード上では判定できない。中-4 は「規模と具体例を設計卓へ回せているか」だけを見た指摘である。

---

## 取り込み結果（自動トリアージ）

`/implement_plan_full` Phase C。**10 件すべて採用・不採用 0 件。「高」の不採用は 0 件**（安全弁の発火なし）。再レビュー往復 0 回。

| # | 指摘 | 採否 | 対応と理由 |
|---|---|---|---|
| **高-1** | `M19-DESIGN-01-central-requirements.md:47`（R7）の「現状 `is_derived` は model 非搭載＝API 非露出」が失効 | **採用** | 指摘のとおり。着手時点の走査が `internal/` ＋ `web/src/` で止まっており `docs/` を見ていなかった。**同書は設計卓の成果物であり製造は編集しない**（`CLAUDE.md` §8）。⇒ **完了報告 §6-9 に載せ、設計伝達レポート §4 で回付する。** `docs/change-notes/` 4 件を対象外とする判定にも同意（発行済み通知書＝歴史記録） |
| **高-2** | `progress-log.md` の索引行が無く `check-progress-log-index.sh` が赤。しかも完了報告 §4 の検査表に本検査だけが載っていない | **採用** | Phase D で索引行を追記し、緑を確認した。**表から漏れていたことのほうが本体の指摘である**ため、完了報告 §4 へ本検査を含む 4 本を追加した（12 → 15 本） |
| **中-1** | `isDerived` の分類に全数の独立オラクルが無い（混在 5 件のときしか主張していない） | **採用** | ロスターの当該 it を書き換え、**全 372 ファミリー**で「`fam.byVariant` の id から元 move を引き直した `allD`」と `fam.isDerived` を突き合わせるようにした。`checked === 372` も固定。**変異注入で確認**＝段 2 の AND を OR にすると 4 本が赤になる |
| **中-2** | `getByIDSQL` へ足した列に回帰テストが 0 本 | **採用** | `internal/repository/move/get_by_id_test.go` を新設（**作る前に `ls` で同名不在を確認**＝`E-225`）。陽性対照つき（`is_derived=1` の行と `=0` の行の両方）＋列ずれの検出。**変異注入で確認**＝SQL と Scan を両方落とすと `IsDerived = false, want true` で赤 |
| **中-3** | `code-facts.md` の `MoveResponse` / `MoveListItem` が失効 | **採用（境界送りと決定）** | 「いま回す／境界で回す」のどちらでもよいが決めずに放置しない、という指摘に従い**境界送りと決めた**。理由＝同書は着手前から陳腐化（生成 2026-09-05・源泉の変化量 282 件＝23%）しており、`/regen_code_facts` は本サブ射程外の 282 件を巻き込む。**完了報告 §6-10 と設計伝達レポート §4 に決定として残す** |
| **中-4** | 178/372 の帰結についての開発者承認の逐語が記録に無い | **採用** | **完了報告 §3 へ問いと回答の逐語を引用で残した**（提示した 3 択と、回答「指示書どおり A（推奨）」）。設計伝達レポート §4 でも回付する。★レビュアーが「応答が実在したか判定できない」と不明点に挙げたのは正しい判断であり、記録が無かったことが問題だった |
| **中-5** | 段 1 `?? true` / 段 2 `?? false` の向きの不一致 | **採用** | 段 2 を `?? true` に揃え、**到達不能であること**と**なぜ揃えたか**を 3 行のコメントで明示した。挙動は変わらない |
| **低-1** | `families[0]` が非 derived であることの床がコメントだけ | **採用** | ロスターの分類オラクル内で **31 キャラ全数**に `expect(fams[0].isDerived).toBe(false)` を追加した（コメントではなく検査にした） |
| **低-2** | `check-import-order.sh` の実行時メッセージと冒頭コメントの食い違い | **採用（回付先を変更）** | 本サブの手番ではないという判定に同意。**ただし回付先を `followup` §J から設計伝達レポート §4 へ変えた** —— §J は「停止時記録」の面であり、上限に達していない本サブの改善候補を置く場所ではない（`CLAUDE.md` §9・§10.Y） |
| **低-3** | 追加 `describe` の直前に空行が無い | **採用** | 空行を入れた |

### ★取り込み中に、レビューの想定より 1 段深い穴が出た

**中-1 を取り込んだあと変異注入で床を測ったところ、段 1 の AND を OR に変えても全テストが緑のままだった。** 原因は seed 実データに「**同一 raw ファミリーの中で `is_derived` が割れる**」組が **1 件も無い**ことである（混在 5 件はいずれも「plain と holding 変種のあいだ」で割れており、raw ファミリーの中では割れていない）。

⇒ 実データ由来の床では原理的に塞げないため、**合成データのテストを 1 本足した**（`inputResolution.test.ts` の「同じファミリーの中で強度ごとに割れていたら derived にしない（AND であって OR ではない）」）。段 1 を OR にすると本 1 本だけが赤になることを確認した。

**★中-1 の指摘そのものは正しく、指摘に従って初めてこの穴が見えた。**

### 取り込み後の検査

| 検査 | 結果 |
|---|---|
| `go test ./...` | 緑（`get_by_id_test.go` を含む） |
| `cd web && pnpm test` | 緑（combo 系 1133 tests を含む全数） |
| `cd web && pnpm exec tsc --noEmit -p tsconfig.json` | 緑 |
| `make e2e-only P=m30-0` | 21 passed |
| `bash scripts/check-progress-log-index.sh` | **違反なし**（Phase D 後） |
| `bash scripts/check-md-emphasis.sh`（本報告書 ／ 完了報告） | どちらも 0 行 |

