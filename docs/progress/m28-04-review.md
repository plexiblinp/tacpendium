# M28-04 レビュー報告書

| 項目 | 内容 |
|------|------|
| 対象サブ | `M28-04`（`docs/instructions/M28-04-move-code-and-command-corrections.md` v1.1.0） |
| チェックリスト | `docs/instructions/reviews/M28-04-review-checklist.md` v1.0.0（**★§6 / §7-1〜8 は対象不在のため読み替え適用**） |
| 対象成果物 | `docs/progress/M28-04-completion-report.md`（459 行・コミット `5fe2210`） |
| 着手基点 | `25c9285` |
| レビュー日 | 2026-09-06 |
| 差分 | `docs/progress/M28-04-completion-report.md` **+459 / −0**。**deletions 0 を確認**（教訓 `E-225`）。コード・CSV・マイグレの差分ゼロ |

---

## 総評

**中心的な判定——「射程 9 行はすべて `M19-04c`（`000063`・2026-08-03）で是正済み」——は正しい。** 追試で確定した:
`000063` の節 A / B-2 / B-3 / B-3' / C-1〜C-6 が指示書 §2.2 の 3 件と §2.6 の 6 行を**過不足なく覆っており**、CSV（`guile.csv:61-64`・6 行分の各 CSV）・golden（`000026:248-251` ほか）・DB（`csv_db_sync_test` 経由で HEAD 一致を確認）・回帰テスト（`migrate_m1904c_test.go`）の 4 点すべてが是正後の値である。§2.2-3 を「(a) と (b) の両方」とした結論も `D-142` 逐語・`M19-04c` §1.1 B-3・`000063` の 2 文で裏づく。**母数を先に出すという指示書 §2.1 の設計がそのまま効いた好例であり、コード変更ゼロで完了させた判断そのものは支持する。**

**一方で、報告書が「実装を読んで確定させた」「実測した」と断定した箇所に、追試で覆る記述が 4 件あった。** いずれもテストでも lint でも検出できず、次のサブが本文をコピーする経路にある。最も重いのは (1) `startup_basis='unknown'` 48 行の扱い——**件数が自身の内訳と合わず（正は 51）、根拠に挙げた `M19-DESIGN-07` §4-1 が当該行を覆っていない**、(2) `punishfinder` の除外規則の列挙漏れ——**飛び道具分岐を落としており、mai の 2 行についての結論が逆**、(3) 破壊確認の治具が **`seedgen -check` の射程外（manon）を検出できない**ことを scratchpad 実測で確認、(4) 常設検査の実測が**自身の成果物をコミットする前**に取られており、`check-md-emphasis` の増加 65 行のうち **7 行が本報告書自身**、`check-progress-log-index` は現在**赤**である。

**⇒ 判定は「結論は採用、根拠の 4 件は是正を要する」。** 是正はいずれも報告書本文の書き換えのみで、コード・データには波及しない。

---

## 設計準拠性レビュー結果

### 束 1: 「是正済み」判定の正しさ

| # | 観点 | 評価 | 所見 |
|---|---|---|---|
| **1** | **`000063` が §2.2（3 件）／§2.6（6 行）の射程を過不足なく覆うか** | **◎** | 全数照合した。**漏れ 0 件。** §2.2-1 → 節 A（`NOT EXISTS` ガード付き改名）／§2.2-2 → マイグレ対象外＋CSV 正本修正（ヘッダに逐語の根拠あり）／§2.2-3 → 節 B-3（`10/29/0/38 → 15/46/11/71`）＋ B-3'（`unknown → through`）／§2.6 の 6 行 → 節 C-1〜C-6（**指示書が挙げる 6 行と完全同一**）。附随の B-2（順序制約を解く前置改名）・A'/B'（`is_projectile` 中間コホート救済）も確認 |
| **1-b** | **§2.2-2「`command` の入れ替わり」の CSV 側是正の根拠が実物と一致するか**（★自分で照合） | **◎** | **一致した。** `M19-04c` 指示書 §1.1 の B 表（B-1/B-2/B-3）と現行 `character_data/guile.csv:61 / :62 / :63` の `command` を 3 行とも逐語照合し、**完全一致**。加えてボード **`D-167`**（`parallel-board-rulings-M20.md:151`）が同内容を独立に記録しており（開発者から現行と是正後の CSV 行を受領・2026-08-02）、B 表が後年の再構成ではないことも裏づく |
| **2** | **CSV / golden / DB / 回帰テストの 4 点追試** | **○** | CSV＝`guile.csv:61-64` および 6 行の各 CSV を実読、全数是正後の値。golden＝`000026:248-251` が新 code・新フレーム、**旧 code のヒット 0 件**。DB＝`go test ./internal/infra/migration/ -run TestCSVAndDBAgreeOnFrameCostColumns` 緑（HEAD で CSV↔DB 一致）。回帰テスト＝`migrate_m1904c_test.go` の `m1904cFrames` / `m1904cLegacyFrames` を実読、9 行分の是正前後を直書き固定。**ただし §2.7 の「(HEAD)」という帰属が誤り**（下記 中-1） |
| **3** | **§2.2-3 を「(a) と (b) の両方」とした根拠** | **◎** | **裏づく。** `D-142` 逐語「`through` のフレームであるべきところに `standalone` のフレームが入っている」は値と basis の両方を指す読みが自然であり、`M19-04c` §1.1 B-3 が**数値是正と basis 指定の両方**を書き、`000063` が**節 B-3 と B-3' の 2 文**に分けて実装している。三点が一致する。**指示書 §2.2-3 の二択そのものが後年の読み直しで生じた**という §2.2 の指摘も妥当 |
| **4** | **「6 行の値の一次源は `D-158`」の当否** | **◎** | **正しい。**`docs/process/archive/parallel-board-rulings-M20.md:160` を実読し、6 行の値が**全数逐語で記録**されていることを確認した（例「(1) kimberly `bushin_prism_strikes`(現行 startup 52 → 正は startup 26 / active 3 / recovery 19 / **total 47**)」）。**⇒ 指示書 §2.6-1 の「どこにも記録されていない」を失効と判定した §3-2 は正しく、本サブで最も価値のある発見である** |

### 束 2: 見落とした残作業

| # | 観点 | 評価 | 所見 |
|---|---|---|---|
| **5** | **3 code の走査結果の再現** | **◎** | **完全再現。** 報告書の 47/91/46（`docs/`）は、**本報告書自身を除外**して数えた値と 1 件も違わない。他ディレクトリの 2/3/1（`character_data/`）・5/25/9（`migrations/`）・1/5/1（`internal/`）・`web/` `scripts/` `docs/design/` 各 0 もすべて一致。**`web/` 0 件**（`migration-version-literals-in-tests` 型の危険不在）も確認 |
| **6** | **CSV と DB のドリフト** | **○** | 残っていない。`csv_db_sync_test.go` は `m.Up()`＝**HEAD** で走り、`startup_basis` / `chain_cancel_total` / `fastest_unreachable` / `last_changed_game_version` の 4 列を CSV↔DB 突合する（緑を実測）。**⇒ `guile.csv:63` の `through` は HEAD で機械的に守られている。** ただし同テストは `startup` / `active` / `recovery` / `total` / `code` を**見ない**——フレームと code 側の CSV↔DB 担保は golden 経由のみである。この非対称は報告書 §2.7 が触れていない |
| **7** | **`is_derived=1` かつ `startup_basis='unknown'` 48 行の扱い** | **×** | **3 点で誤っている。** 下記「推奨修正 高-1」。結論（射程外）は維持されるが、**開発者へ差し出した §7-5 の根拠が誤りであり、そのまま次サブへ複製される** |
| **8** | **guile 以外の同型の軽走査（型 1〜3）** | **△** | **走査自体は完全再現**（`_light` 総数 **161**・孤児 **2 件**——terry `quick_burn_light` / ingrid `sun_flare_light`。件数まで一致）。**直さない判断も `D-161` に照らして正しい。** ingrid を「誤りとは言えない」とした判定も、`sun_flare_lv1`〜`lv3` / `_lv*_od` の実在（`ingrid.csv:39-45`）から妥当。**ただし terry の根拠が `name_ja` の形だけに留まっており、リポジトリ内にあるより強い証拠を引いていない**（下記 中-4） |

### 束 3: 報告の品質

| # | 観点 | 評価 | 所見 |
|---|---|---|---|
| **9** | **設計卓への請求（§3）の具体性** | **○** | 8 行の表で「どの文書のどの節が、なぜ失効したか」を指しており、**設計卓が動ける水準にはある**。`D-158` は行番号（`:160`）まで示していて良い。**ただし `D-201` を引いていないため、設計卓が同じ調査をやり直す**（下記 中-3） |
| **10** | **`D-510` の遵守** | **○** | **§9 は正しい。** プレースホルダのみで、**存在しないレビュー報告書へのリンクも、レビュー結果の断定も無い**。「Phase B のサブエージェントが作るまで存在しないため、リンクも件数も先に書かない」と明記している。**ただし §0 が §9 と矛盾する**（下記 中-2） |
| **11** | **§8「確かめられなかったこと」の分離** | **△** | **5 項目の分離自体は誠実で、質が高い**（★1 の「開発者が現在も同じ値を正しいと考えているかは確かめていない」／★4 の「§3.1 の経緯は再構成であり当時の意図は確かめていない」は的確）。**しかし §8 が拾い損ねた断定が 2 件ある**——§2.6(b) の「★入る。両側とも。実装を読んで確定させた。」（実際は列挙漏れ）と §1.4 型 2 の 48 行（実際は 51・根拠が別物）。**どちらも §8 へ回すべきだった、あるいは §8 へ回さずに済むよう追試すべきだった** |
| **12** | **`progress-log.md` への索引行** | **Phase D で確認** | レビュー時点で**未追記**（`bash scripts/check-progress-log-index.sh` が `m28-04` の不在で赤）。**指摘ではなく Phase D の残作業。** ただし §4.1 が同検査を「緑」と記載している点は別途 高-4 |

### 束 4: 検証の実測

| # | 観点 | 評価 | 所見 |
|---|---|---|---|
| **13** | **§4.2 破壊確認（チェックリスト §6-1 の代替）** | **△** | **趣旨は半分しか満たしていない。** 「リポジトリを壊さず scratchpad の複製で陽性対照を取る」という**運び方は正しく、称賛に値する**。陰性対照（`OK` / exit 0・752 moves / 9 chars）と破壊 A・B（`DIFF` / exit 1）の実測も再現した。**しかし治具 `seedgen -check` の射程が 9 キャラ・752 行しかなく、射程 9 行のうち manon `temps_lie` を構造的に検出できない**（下記 高-3。scratchpad 実測で確認） |
| **14** | **§4.4 `check-md-emphasis.sh` の赤が着手基点で既存か** | **×** | **既存分は事実だが、免責は成立しない。** HEAD で **501 行 / ベースライン 436**（増加 65）。`--list` で内訳を取ったところ、**うち 7 行が `docs/progress/M28-04-completion-report.md` 自身**（`:377` `:381` `:390` `:429` `:430` `:431` `:432`）。報告書が測ったのは自身をコミットする前の 494 行である（下記 高-4） |
| **15** | **テスト・常設検査の実測値** | **○** | **自分で回して一致を確認した。**`go test ./...` **全パッケージ緑**／`go run ./cmd/seedgen -check` **`OK: 生成物は既存ファイルと一致` / 752 moves / 9 chars**（報告書の陰性対照と同値）／`make e2e`（**リポジトリルートで実行**）**247 passed (3.8m)**——**報告書の「247 passed / 3.8m」と完全一致**／`scripts/check-artifact-integrity.sh` **「結果: 違反なし」**（検査 14 件の自己検査 ＋ 生成物 4 件 OK）。**`check-md-emphasis` と `check-progress-log-index` の 2 本のみ記載が HEAD で成立しない** |

---

## 設計準拠性以外の指摘事項

| # | 項目 | 所見 |
|---|---|---|
| 1 | **禁止事項の遵守** | **違反なし。** `docs/instructions/` / `docs/design/` / `docs/handover/followup-backlog.md` / `docs/process/parallel-board.md` はいずれも無変更（`git diff --stat 25c9285` が完了報告 1 ファイルのみ）。**チェックリスト §7-9 は完全に緑** |
| 2 | **`E-225`（新規ファイルの deletions）** | **確認済み。** `+459 / −0`。新規追加ファイルに deletions が付いていない |
| 3 | **`D-382`（製造が書けるのは §J だけ）** | **遵守。** `followup-backlog.md` は §J を含め 1 行も編集していない。クローズは §3-6 / §3-7 で請求に留めている——正しい運び |
| 4 | **`D-293`（連番の自採番）** | **遵守。** `000106` を未消費のまま返している。`ls migrations/` の実測末尾が `000105` であることも追試で一致 |
| 5 | **`*.db` の扱い** | **遵守。** 使い捨て DB を作って `SELECT` し、**削除していない**旨を明記（`CLAUDE.md` §10） |
| 6 | **`move_commands` の制約記述** | **軽微な不足。** §2.4-1 が列（`move_id` / `character_id` / `token_key`）と明示 INDEX を挙げるが、`000033` が持つ **`PRIMARY KEY (move_id, token_key)`** に触れていない。結論（索引は command 生文字列を持たない＝波及なし）は正しく、実害なし |
| 7 | **`character_data/command-correction-history.md` の旧 code 残置** | **報告のみ。** `:392` `:407` が `sonic_break_light` を**現況の記述として**書いている（`:407`＝「command 空のまま確定」）。同ファイルの改名ログ（`:420`「move_code 改名(2026-07-14…)」）には **2026-08-03 の `sonic_break_light → sonic_break` / `sonic_cross_3_meter_od → sonic_cross_2_meter_od` が載っていない**。**ただし同ファイルは日付とバッチで閉じた記録文書であり、報告書が「歴史記録として据え置く」と分類したのは指示書 §2.3-4・先例 `P-07` に照らして妥当。⇒ 製造の欠陥ではなく、開発者・設計卓の判断材料として挙げる** |

---

## 推奨修正（優先度別）

### 高（M28 完了前に修正必須）

いずれも**報告書本文の書き換えのみ**で完了する。コード・CSV・マイグレへの波及は無い。

---

**高-1: §1.4 型 2 ／ §7-5 の `startup_basis='unknown'` の記述が 3 点で誤っている**

| # | 誤り | 実測 |
|---|---|---|
| **(a)** | **件数「48 行」が自身の内訳と合わない** | 報告書が列挙する内訳〔elena 10 / blanka 8 / chun_li 7 / alex 4 / m_bison 4 / yasmine 4 / ken 3 / marisa 3 / dee_jay 2 / cammy 1 / guile 1 / ingrid 1 / jamie 1 / lily 1 / rashid 1〕は**合計 51 である**。全 CSV 走査（`is_derived=true` かつ `startup_basis='unknown'`）でも **51 行 / 15 キャラ**で、キャラ別内訳は報告書と 1 件も違わない。**⇒ 総数の 48 だけが誤り** |
| **(b)** | **guile の 1 件を「取り残し」と性格づけている** | 当該行は `sonic_break_od`。`docs/progress/20260802-M19-04-manual-input-list-developer-decision.md:113` の備考欄が**逐語で**「**`unknown, 扱いが難しいのでunknownでいい。`**」。`D-138` が定めた `unknown` の意味は「**判断済みで語れないと確定**」（対して `保留`＝未判断）。**⇒ 取り残しではなく、開発者が下し終えた判断である** |
| **(c)** | **根拠に挙げた `M19-DESIGN-07` §4-1 が当該行を覆っていない** | 同節（`:96`）の逐語は「**改名した他の 2 行**〔`sonic_break_light` → `sonic_break` ／ `sonic_cross_2_meter_od`（旧）→ `perfect_timing_sonic_cross_od`〕**は `startup_basis` を確定させていないため、記入値 `standalone` を持ったまま Phase 2 の対象に残る**」。**対象は `standalone` の 2 行**であり、**`unknown` の `sonic_break_od` は含まれない**。さらに §4-1 が定義する Phase 2 母数は **308 行＝11 キャラ 159 ＋ 第三波 6 キャラ 149** であり、**elena / blanka / chun_li / alex / yasmine / dee_jay / cammy の 7 キャラ・37 行はこの母数の外**（いずれも `000053` 以降の seed 波）。**⇒ §7-5 の「既知（`M19-DESIGN-07` §4-1 の Phase 2 対象）」は 51 行中 37 行について成立しない** |

**修正**: 件数を **51** へ。guile の 1 件の根拠を `M19-DESIGN-07` §4-1 から **人手判断リスト `:113` ＋ `D-138`** へ差し替え。「取り残し」という節見出しを改める。§7-5 の「既知」の射程を **11 キャラ＋第三波の分に限定**し、第 4 波以降の 37 行は**別枠（未記入コホート）**として開発者へ差し出す。
**理由**: 根拠は**報告書が自ら「値の一次源」と名指しした同じファイル**の中に、1 回の `grep` で届く位置にあった。テストも lint も検出できず、§7 は開発者が読んで判断する面である。

---

**高-2: §2.6(b) の `punishfinder` 除外規則の列挙が漏れており、mai 2 行についての結論が逆**

§2.6(b) は相手技側の除外を「`isNonPunishableTarget`（`is_aerial` ／ 移動 system move）／ `damage = 0` ／ pruning 済み」の 3 つと書き、「**★入る。両側とも。実装を読んで確定させた。**」「6 行とも `is_aerial=0`・非 system・`damage>0`。⇒ 除外されない＝候補に入る」と断定している。

**`internal/service/punishfinder/service.go:247-251` に、有利フレーム算出より前に走る分岐がある。**

```go
// c: 飛び道具は手動確認(距離依存)。
if om.IsProjectile {
    tree.ManualReviewNodes = append(tree.ManualReviewNodes, manualNode(om, model.PunishReasonDistanceDependent, registeredIdx[om.ID]))
    continue
}
```

**mai `flame_midare_kachousen` / `midare_kachousen` は `is_projectile=true`**（`mai.csv` 実読・報告書自身の SELECT D も `is_projectile 1` を出している）。**⇒ この 2 行は成立レーンに載らず、`ManualReviewNodes` へ落ちる。フレーム値は判定に使われない。** 続く `d`（`on_block` 欠落）・`e`（`recovery` 欠落）・`f`（`recovery = 0`）の 3 分岐も列挙から落ちている。

**始動技側の結論は正しい。** `listMovesForScanSQL`（`internal/repository/punish/queries.go:10`）は `is_derived` / `startup_basis` / `category` を**投影すらせず**、`buildStarters`（`service.go:345`）の絞りは `damage > 0` と `grounded = !is_aerial` のみ。6 行とも `damage > 0`・`is_aerial = false`（CSV 実読）。**⇒ 6 行はすべて始動技候補に入る。**

**修正**: 相手技側の表に飛び道具分岐（および d/e/f）を足し、**「6 行とも」を「4 行が相手技候補に入り、mai の 2 行は飛び道具として手動確認へ回る（フレームは判定に使われない）」へ改める**。始動技側は「6 行とも入る」のまま。
**理由**: これは `D-158` が「**推測で『影響なし』と書かない**」と明示的に未確認へ回した論点への回答であり、**ボードへ返る記述**である。断定の形で残ると、次に確定反撃側へ触れるサブがこれを前提として複製する。

---

**高-3: §4.2 の破壊確認の治具が、射程 9 行のうち 1 行を構造的に検出できない**

`seedgen -check` が再生成するのは **`FirstWaveOrder` の 9 キャラのみ**（`internal/seedgen/generate.go:20`＝terry / guile / lily / ingrid / kimberly / juri / ken / mai / zangief）。**報告書自身が印字した「投入 moves 合計 = 752 / 索引キャラ数 = 9」がその証拠である**（DB は 3026 行・32 キャラ）。

**⇒ manon `temps_lie` は治具の射程外。** scratchpad に `character_data/` を複製し、`manon.csv:30` を `M19-04c` 是正前の値（`5,1,23,28`＝`m1904cLegacyFrames` と同値）へ戻して実測した:

```
manon,temps_lie,target_combo,タン・リエ,5,1,23,28,...
$ go run ./cmd/seedgen -data <複製> -migrations migrations -check
OK: 生成物は既存ファイルと一致
投入 moves 合計 = 752 / 索引キャラ数 = 9 / 索引非搭載(記録)= 276 / drop 移動move = 0
EXIT=0
```

**★9 行のうち 1 行を是正前へ戻しても、報告書が「陽性対照」と呼んだ治具は緑のままである。**（リポジトリ内のファイルは 1 行も触っていない。複製は削除済み。）

**実際の担保は存在する**——`TestGolden_ManonMovesMatchesRegeneration`（`internal/seedgen/generate_m1403d_test.go:34`）が `manon.csv` から再生成した SQL を `000045` と byte 比較する。**しかし §2.7 はこのテストを一度も名指ししておらず、`TestGolden_CommittedMigrationMatchesRegeneration`（`000026` 比較）だけを挙げて「CSV が戻れば golden が落ちる」と書いている。** 名指しされたテストは manon を読まない。

**あわせて、破壊 A・B はどちらも `guile.csv` に対して行われており、指示書が最も危険視した §2.6 の 6 行に対する陽性対照が 1 件も無い。**

**修正**: §2.7 の golden 行を **2 本立て**（`000026` 系＝guile 3 件・kimberly・lily×2・mai×2 ／ `000045` 系＝manon `temps_lie`）へ改める。§4.2 へ **§2.6 の 6 行のうち少なくとも 1 行を戻す破壊確認**を足し、manon については治具を Go テストへ替える必要があることを明記する。
**理由**: チェックリスト §6-1 は「**この確認が本サブの中心である**」と書いている。中心の確認が、射程の 1/9 について空振りする治具で行われていた。

---

**高-4: §4.1 / §4.4 / §10 完了条件 5 の常設検査の実測が、自身の成果物をコミットする前に取られている**

| 検査 | 報告書の記載 | HEAD（`5fe2210`）での実測 |
|---|---|---|
| `check-md-emphasis.sh` | 「現在 **494** 行 / ベースライン 436」「**★増加分の所在は** retrospective-digest(41) / M26-02-completion-report(40) / M21-RESEARCH-01-report(40) 等であり、**本サブが触る範囲に無い**」 | **現在 501 行 / ベースライン 436（増加 65）。`--list` の内訳に `docs/progress/M28-04-completion-report.md` が 7 行**（`:377` `:381` `:390` `:429` `:430` `:431` `:432`） |
| `check-progress-log-index.sh` | **緑**（73 件すべてが progress-log に現れる） | **赤**——`NG 作業 ID 'm28-04' が docs/progress/progress-log.md に現れない` |

**⇒ §4.4 の免責「本サブの変更ではない」は 58 行については正しいが、7 行については成立しない。** チェックリスト §0.3-4 の免責が「ベースラインが**動いていない**こと」である以上、自身が動かした 7 行はこの免責に入らない——**これは報告書 §4.4 自身が立てた論法である。**

**修正**: 本報告書自身の 7 行の閉じない強調を是正（`**A（B）**は` → `**A**（B）は` の形）。§4.4 を「既存 58 行 ＋ 本報告書 7 行」へ書き分ける。§4.1 の `check-progress-log-index.sh` は **Phase D の追記後に再測**し、その結果を記載する。**以後、常設検査は成果物をコミットした後に測る。**
**理由**: 「着手基点で既に赤」という一文が、完了条件 5 の △ 判定と §7-4 の改善レーンへの申し送りの両方を支えている。その一文が部分的に偽であると、免責の範囲を読み違えたまま次サブへ渡る。

---

### 中（M29 着手と並行可）

**中-1: §2.7 が v63 スコープのテストを HEAD の担保として帰属させている**

`migrate_m1904c_test.go` の比較区間は **`m1904cTerminus = 63`** で閉じている（`:56`。`SUPP-001` §5.5 規約 (1) に基づく**意図的な設計**で、コメントに「HEAD(m.Up())を終端にしない」と明記）。`assertV63StateM1904c` は `m.Migrate(63)` 後に走る。**⇒ §2.7 の「`startup_basis` の `through`（**HEAD**）」という帰属は誤り。** `migrate_head_test.go` に置かれた HEAD スコープの検査は `TestRun_HEAD_NoDanglingForeignKeys` のみで、**9 行の code / フレームを HEAD で固定するテストは存在しない**。

**HEAD での成立を実際に立証しているのは、報告書自身の §1.2 の DB 実測（`schema_migrations = 105`）と、`csv_db_sync_test`（`m.Up()` で走る・ただし `startup_basis` 等 4 列のみ）である。** §2.7 はそう書くべきだった。**結論は変わらないが、担保の所在の記述が誤っている。**

**中-2: §0 が §9 と矛盾する（`D-510` の趣旨）**

§9 は「レビュー報告書は Phase B のサブエージェントが作るまで存在しないため、リンクも件数も先に書かない」と正しく宣言している。**一方 §0 は過去形で**「追加したのは本報告書と、**Phase B のレビュー報告書 `docs/progress/m28-04-review.md`**、および `progress-log.md` への索引行だけである」「**★新規ファイルは作成前に `ls` で同名の不在を確認した**…`M28-04-completion-report.md` / `m28-04-review.md` はいずれも**不在だった**」と書く。**コミット `5fe2210` の時点でどちらも存在せず、`ls` で確認した対象も片方しか無い。** 予定を過去形の実測として書いている。§0 を「本報告書のみ。レビュー報告書と索引行は Phase B / D で追加される」へ改めること。

**中-3: §3 が `D-201` を引いていないため、設計卓が同じ調査をやり直す**

`docs/process/archive/parallel-board-rulings-M20.md:117` の **`D-201`（2026-08-07）** は、**本サブが 1 か月後に到達した結論を既に記録している**:

> 【followup `existing-frame-value-corrections` は起票しない】★否定形の記録。**`M19-04c`（`000063`）で `P-19` の 6 行は解消済みであり、起票と同時にクローズするだけの項目になる。** …★**「起票が要る」という記述がボード `D-158`・保留 `P-19`・引き継ぎ資料 §4 の 3 か所に残っており、未決と読まれる。**「起票しないと決めた」と書く

**⇒ §3.1 が「board の記述と `000063` の日付から再構成した」（§8-4 で推測と明示）とした経緯は、推測ではなく board に明記されていた。** かつ `D-201` は**失効箇所を 3 つ**挙げており、報告書 §3-6 / §3-7 が挙げた 2 つ（`followup` §BK / `P-17`・`P-19`）に加えて **`引き継ぎ資料 §4`** という第 3 の所在を名指ししている。**§3 へ `D-201` を引き、§3.1 の推測を確定へ格上げし、第 3 の所在を請求へ足すこと。** あわせて §8-4 は「確かめられなかったこと」から外れる。
（同様に **`D-167`**（`:151`）は §1.1 の B 表を board 側から独立に裏づける記録であり、§2.2 / §1.1 の出典として引く価値がある。）

**中-4: §1.4 型 1 の terry の所見が、リポジトリ内にあるより強い証拠を使っていない**

報告書は terry `quick_burn_light` を「`name_ja` に強度の語が付いていない」ことだけを根拠に「強い候補」としている。**`character_data/command-correction-history.md:15` に、より硬い証拠がある**:

> `quick_burn_light` | クイックバーン | `d dl l plus p_l` | 対応 dist code **`quick_burn`** | 日本語名一致（**英名改名で code 突合が外れたもの**） | 高

**⇒ 公式 dist 側の識別子が強度サフィックスを持たない `quick_burn` であり、突合が `move_code` ではなく日本語名で成立した**——これは `sonic_break` / `sonic_break_od` が合流した形（`X` ＋ `X_od`）と同一で、CSV も `quick_burn_light` / `quick_burn_od` の対だけを持つ（`terry.csv:32-33`）。**`name_ja` の形よりも一段強い、リポジトリ内で完結する証拠である。**

**直さない判断は `D-161` に照らして正しい**（本項は §1.4 の性格づけどおり仮説の提示であり、実機確認は開発者の手番）。**§7-2 で開発者へ判断を仰ぐなら、この 1 行を添えること**——添えるかどうかで、開発者が実機を起動する優先度が変わる。

**中-5: §2.7 が CSV↔DB 担保の非対称に触れていない**

`csv_db_sync_test` が守るのは `startup_basis` / `chain_cancel_total` / `fastest_unreachable` / `last_changed_game_version` の**4 列のみ**であり、`code` と `startup` / `active` / `recovery` / `total` は**見ない**。フレームと code の CSV↔DB 担保は golden 経由のみ（=「CSV↔生成 SQL」であって「CSV↔DB」ではない）。**§2.7 の「3 面すべてを固定している」は、列によって担保の形が違うことを均してしまっている。** 列ごとにどのテストが守るかを 1 行で書き分けること。

### 低（将来対応）

**低-1: §1.3 の走査結果に測定基準時点が無い**
再現コマンド `grep -rn "<code>" <dir> | wc -l` を HEAD で回すと **58 / 101 / 53**（報告書は 47 / 91 / 46）。差は**本報告書自身のヒット 11 / 10 / 7** による。追試で「本報告書を除外すれば 47 / 91 / 46」を確認済みで、**数字は正しい**。「本報告書をコミットする前の測定である」の一文を足せば再現できる。

**低-2: §2.4 の `move_commands` 制約の記述不足**
`PRIMARY KEY (move_id, token_key)`（`migrations/000033_create_move_commands.up.sql`）が挙がっていない。結論（索引は command 生文字列を持たない）に影響なし。

**低-3: `character_data/command-correction-history.md` の旧 code（開発者・設計卓向けの報告事項）**
`:392` `:407` が `sonic_break_light` を現況として書き、`:420` の改名ログに 2026-08-03 の改名 2 件が載っていない。**同ファイルは日付・バッチで閉じた記録文書であり、据え置きの判定は妥当**（指示書 §2.3-4 / `P-07`）。**製造の欠陥ではない。** 将来どちらの性格（生きた台帳 / 歴史記録）で運用するかを設計卓が決めるときの材料として記録する。

---

## 良かった点

- **★母数を先に出す設計に忠実に従い、それが決定的に効いた。** 末尾の総括「母数を出さずに実装へ入っていたら、`000063` と同じ是正をもう 1 本書いて `000106` を空振りさせていた——`NOT EXISTS` ガードも `AND startup_basis = 'unknown'` も無い素の `UPDATE` なら、**0 行に当たって成功し、エラーにはならない**」は、本サブで最も価値のある一文である。実装へ飛び込む誘惑に負けなかったこと自体を評価する。
- **★`D-158` の再発見（§3-2）は本サブの最大の成果。** 指示書 §2.6-1 / `D-742` / `D-753` / チェックリスト §0.4-7 の 4 か所が揃って「値はどこにも記録されていない」と書いていたものを、**逐語を突きつけて失効させた**。追試で `parallel-board-rulings-M20.md:160` に 6 行全数が記録されていることを確認した。**指示書の前提を疑って一次源へ戻る**という、最も難しい動きができている。
- **★リポジトリを壊さずに陽性対照を取った運び方が正しい。** scratchpad へ複製してから破壊し、`-check`（書き込まないモード）を使い、「リポジトリ内のファイルは 1 行も触っていない」と明記している。射程の穴（高-3）はあるが、**方法論としては後続が真似すべき形である。**
- **★`D-510` を §9 で正しく守った。** 存在しないレビュー報告書へのリンクも件数も断定も書いていない。§0 との矛盾（中-2）は残るが、肝心の §9 は規律どおり。
- **★§8 を実際に機能させている。** 「開発者が現在も同じ値を正しいと考えているかは確かめていない」「`M19-04c` 以降に SF6 のアップデートで値が変わっていれば別の課題」（§8-1）は、`M28-02a` の `last_changed_game_version` へ正しく接続する、質の高い留保である。
- **★禁止事項が完全に守られている。** `docs/instructions/` / `docs/design/` / `followup-backlog.md` / `parallel-board.md` すべて無変更。`000106` を未消費で返却。使い捨て DB を削除していない旨も明記。**チェックリスト §7-9 は緑。**
- **★追試して一致した実測が多い。** `_light` 孤児走査（161 / 2 件）、3 code の全走査（47/91/46 ほか全ディレクトリ）、`seedgen -check` の陰性対照（752 / 9）、`make e2e`（**247 passed / 3.8m** が完全一致）、`go test ./...` 緑、`check-artifact-integrity.sh` 緑。**数字を書いた箇所は、射程の穴を除いてほぼすべて再現できた。**

---

## 制約事項

- 本レビューはコード上で判定可能な範囲のみ対象。実際の動作確認・パフォーマンス・実機テストは別途実施が必要。
- **★インゲームの実値は本レビューでも確認していない。** 「9 行が是正済み」は CSV / golden / DB / 回帰テストの 4 点が `M19-04c` の是正後の値であることの確認であり、**その値が現在の SF6 で正しいかは実機でしか確かめられない**（報告書 §8-1・§8-2 の留保に同意する）。terry `quick_burn_light` / ingrid `sun_flare_light` も同様で、本レビューが足した dist 側の証拠（中-4）も**リポジトリ内の記録の突合**であって実機確認ではない。
- 高-3 の scratchpad 実測は `seedgen -check` の射程についてのものである。`TestGolden_ManonMovesMatchesRegeneration` が manon の是正前値を検出することは**コードの読みから確実だが、リポジトリのファイルを壊さない方針のため実走はしていない**（同テストはデータディレクトリを差し替えられず、`repoRoot()/character_data` を直読するため）。
- **不明: `is_derived=1` かつ `startup_basis='unknown'` の 51 行のうち、11 キャラ・第三波の分（14 行）が「判断済みの `unknown`」か「未記入」かは、本レビューでは guile `sonic_break_od` の 1 件しか一次源で確認していない。** 残りの性格づけは人手判断リストと第三波の記入状況を突き合わせる必要があり、本サブの射程外と判断した。

---

*以上、M28-04 レビュー報告書。* **★本レビューの結論は「コード変更ゼロで完了させた判断は正しい。ただし、その判断を支える根拠の書き方に是正が要る」である。** 高の 4 件はいずれも**報告書本文の書き換えだけで閉じ**、コード・データ・マイグレへは波及しない。

---

## 取り込み結果（自動トリアージ）

**実施**: 2026-09-06（Phase C）。**「高」の不採用は 0 件**——**4 件すべて採用し、いずれも追試で再現を確認してから直した。**

### 高

| # | 指摘 | 採否 | 理由・対応 |
|---|---|---|---|
| **高-1** | **`startup_basis='unknown'` の 48 行は 3 点とも誤り**（実数は 51／guile の 1 件は確定判断であり取り残しではない／`M19-DESIGN-07` §4-1 の典拠が別の行を指す） | **★採用** | **3 点とも再現した。** (i) 内訳の合計は `10+8+7+4+4+4+3+3+2+1+1+1+1+1+1 = 51`。**初版の 48 は単純な足し算の誤りで、内訳の側が正しかった。** (ii) `20260802-M19-04-manual-input-list-developer-decision.md:113` の逐語＝「`unknown, 扱いが難しいのでunknownでいい。`」を確認。**開発者が確定させた `unknown` であり `DES-003` §3.3 の三値目そのものである。** (iii) `M19-DESIGN-07` §4-1 の「改名した 2 行」は `sonic_break`／`perfect_timing_sonic_cross_od` であり、**どちらも `standalone`**（DB 実測 SELECT B）。`sonic_break_od` は含まれない。**⇒ 完了報告 §1.4 型 2 を全面差し替えし、(iv) として「51 行のうち 36 行は第四波キャラ（`000082`/`000084`）であり §4-1 の母数〔308 行・基準 2026-08-04〕はこれらを含みようがない」を自分で数えて足した** |
| **高-2** | **`punishfinder` の除外リストが不完全。mai の 2 行は `is_projectile=1` で手動確認レーンへ回るため「6 行とも候補に入る」は相手技側で偽** | **★採用** | **`internal/service/punishfinder/service.go:247-251` を実読して再現**（「c: 飛び道具は手動確認(距離依存)」→ `ManualReviewNodes` へ append して `continue`。**有利フレームの算出に到達しない**）。**mai の 2 行は DB 実測で `is_projectile=1`**（SELECT D）。**⇒ 完了報告 §2.6 (b) を「始動技側は 6 行とも／相手技側は 4 行が成立レーン・mai の 2 行は手動確認レーン」へ差し替え。★`D-158` が「推測で『影響なし』と書かない」と釘を刺した当の問いであり、こちらが逆向きに雑な断定をしていた** |
| **高-3** | **破壊確認が 9 行のうち 1 行（manon `temps_lie`）を見られない。`seedgen -check` の既定モードは `FirstWaveOrder` の 9 キャラしか再生成しない** | **★採用** | **指摘どおり実走して再現した。** **破壊 C**＝manon `temps_lie` を `5,1,23,28` へ戻して既定モード `-check` → **`OK` / exit 0（捕まらない）**。**破壊 C'**＝`-chars manon -out 000045_seed_moves_manon` を明示 → **`DIFF` up・down 2 本 / exit 1**。**破壊 D**＝kimberly（第一波）を `26,3,24,52` へ戻して既定モード → **`DIFF` / exit 1**。**⇒ 完了報告 §4.2 へ C / C' / D を追加し、§2.7 の回帰ガード表へ `TestGolden_ManonMovesMatchesRegeneration` を 1 行として足した。9 行の内訳は「`000026` の golden が 8 行 ／ `000045` の golden が 1 行」である。★初版の破壊 A/B は 2 件とも `guile.csv` に対するもので、§2.6 の 6 行を 1 行も試していなかった** |
| **高-4** | **常設検査を完了報告のコミット前に測っており、報告書自身が母数へ入っている**（`check-md-emphasis` は 501/436 で 7 行が本報告書由来／`check-progress-log-index` は赤） | **★採用** | **再現した。** 本報告書由来の違反行は `:377 :381 :390 :429-432` の **7 行**。`check-progress-log-index` は **`NG 作業 ID m28-04 が docs/progress/progress-log.md に現れない` / exit 1**。**⇒ (1) 自分の 7 行（是正後は 5 行）を直して本報告書由来の違反を 0 に戻した。(2) §4.1 の見出しから「着手前・完了後とも同一」を外し、Phase D 完了後に測り直した値へ差し替えた。(3) §4.4 を測定時点つきの内訳表へ書き直し、「成果物そのものが検査の母数に入る種類の検査では、書く前に測った値は完了時の値ではない」を教訓として明記した** |

### 中

| # | 指摘 | 採否 | 理由・対応 |
|---|---|---|---|
| 中-1 | **`migrate_m1904c_test.go` は `m1904cTerminus = 63` で閉じており、HEAD を固定していない** | **★採用** | `grep` で `const m1904cTerminus = 63` を確認。**⇒ §2.7 の表へ「固定しているのは v63 時点の状態」と注記し、HEAD を守っているのは golden 2 本と `csv_db_sync_test.go` である旨を分けた** |
| 中-2 | **§0 が「追加した」と過去形で書いており、§9 のプレースホルダと矛盾する** | **★採用** | **⇒ 「追加するのは〜」へ改め、レビュー報告書と索引行が初版時点では存在しないことを `D-510` の参照つきで明記した** |
| **中-3** | **`D-201`（2026-08-07）が未引用。ボードは既に「`M19-04c`（`000063`）で `P-19` の 6 行は解消済み」と裁定しており、未決と読まれる 3 か所（`D-158` / `P-19` / 引き継ぎ資料 §4）まで名指ししていた** | **★採用（本レビュー最大の追加）** | `docs/process/archive/parallel-board-rulings-M20.md:117` を実読して確認。**⇒ §3.1 の経緯表へ `D-201` を 1 段として挿入し、「裁定を書くことと、裁定が指す行を直すことは別の手番である。⇒ 『◯◯に古い記述が残っている』と書いた裁定には、それを直す割付が要る」を横断課題として立てた。あわせて §3 の請求へ 7-b（引き継ぎ資料 §4）を、§7 の確認事項へ ★6 を、§8 の ★4 の書き方（再構成 → `D-201` の明示裁定）を直した。★本サブの再起票の重さが 1 段上がった——気づく機会は在り、記録も在ったのに 1 か月後に再起票された** |

### 低・所見

| 指摘 | 採否 | 理由 |
|---|---|---|
| 3 code の走査は「本報告書自身を除けば 47/91/46 で一致」 | **★採用**（注記のみ） | **⇒ §1.3 へ「本表は着手基点 `25c9285` 時点の値である。成果物そのものが母数へ入る」と注記した** |
| 再現できた項目（`_light` 孤児 161/2 ／ `go test ./...` 緑 ／ `seedgen -check` 緑 752/9 ／ `check-artifact-integrity.sh` 緑 ／ `make e2e` 247 passed・3.8m の完全一致） | — | **指摘ではないが、独立に再現された事実として記録する** |

### 不採用

**★0 件。**

---

*以上、Phase C 自動トリアージ。* **★★本レビューは「実装の欠陥」を 1 件も出していない**（コード変更が 0 行なので当然である）**が、報告の断定を 4 件覆した。★4 件とも「テストも lint も型検査も緑のまま通り、人が読む以外に見つける経路が無い」型であり、優先度較正の基準そのものに当たる。** **★とくに 高-3 は、こちらが「機構が守っている」の証拠として提示した実測が、守っていない 1 行を守っていることにしていた**——**陽性対照を張った対象が偏っていた**（2 件とも guile）**という、対照実験の設計そのものの穴である。**
