# M36-01 完了報告: 配布アーカイブの組み立てとチェックサム（射程 1 ＋ 2）

| 項目 | 内容 |
|------|------|
| 作業ID | M36-01 |
| 指示書 | `docs/instructions/M36-01-release-archive-and-checksums.md` **v1.2.0** |
| チェックリスト | `docs/instructions/reviews/M36-01-review-checklist.md` **v1.2.0** |
| 着手基点 | `1cfbc07` |
| 作業日 | 2026-09-15 |
| CHANGE 消費 | **0 本**（**★自採番していない**＝`D-293`。§8 に請求を 2 件挙げた） |
| マイグレ消費 | **0 本**（`migrations/` の差分 0 バイト） |
| 新規依存 | **0 件**（`go.mod` ／ `web/package.json` の差分 0 バイト） |

> **★★★本報告の版について。** 指示書とチェックリストは **v1.2.0** を適用したが、**作業ツリーの `docs/instructions/` 配下は v1.1.0 のままである**。v1.2.0 は開発者が製造チャットへ直接提示したもので、リポジトリへはまだ入っていない。**⇒ 指示書は設計卓の成果物であり製造が作業ツリーへ書き戻す筋ではないと判断し、2 ファイルとも編集していない。** **★Phase B のレビュアーへは v1.2.0 の実体パスを明示して渡した**（渡さないと、是正前の広い `P-4` で `dist-readme.txt` の編集が射程外と誤判定される）。
>
> **★★コミットの範囲について。** 開発者指示（2026-09-15・逐語＝「本ブランチに対するコミットはしないでください」）は、**翌日の確認で「コミットしないのは*組み立ての成果物だけ*である」と明確化された**（2026-09-16・逐語＝「コミットしないのは『組み立ての成果物』だけです — zip / tar.gz そのものとチェックサムファイルです」）。**⇒ 作業一式は通常どおりコミットする。**
>
> **★★★着手時は前者を*作業一式への禁止*と読み違えていた。⇒ その結果、本報告と `progress-log` とレビュー報告の 3 か所に「1 度もコミットしていない」という記述を作り、コミットした瞬間に失効させた**（2026-09-16 に是正）。**★教訓＝禁止の範囲を広く取ると、成果物の中に失効記述が生まれる。⇒ 範囲が読み取れないときは、広く取る前に確かめること**（設計伝達レポート §7-3）。
>
> **★コミットしないもの＝`dist/release/` の zip ／ tar.gz ／ `.sha256`。⇒ `.gitignore` の `/dist/` が既にカバーしており、`git status` に 1 件も現れない**（完了条件 8 / C-6）。

---

## 1. 着手前の版ゲート（指示書 §0.7・`M-169` の対処）— **5 点すべて通過**

| # | 確かめたこと | 実測 | 判定 |
|---|---|---|---|
| 1 | チェックリストの存在 | 作業ツリーに v1.1.0 が在り、**適用したのは開発者提示の v1.2.0** | 通過 |
| 2 | `DES-002` ≥ 1.93.0 ／ §11.2 に `manual/` と `CHANGE-192` | **1.96.0**（実測）。`manual/` は `docs/design/02-architecture.md:1490-1492`、`CHANGE-192` は `:1498` | 通過 |
| 3 | `M32-02` の着地（`docs/usermanual/` に本文） | `tacpendium-readme.html` **102,234 バイト / 1,057 行**（実測）。`<h2>` **25**＝前置き 3 ＋ 章 22 | 通過 |
| **3b** | `images/` は**ディレクトリが在れば足りる** | `docs/usermanual/images/` **在り**。**画像 0 枚**（`.gitkeep` 1 本のみ・実測）。`<img>` 27 箇所は現状すべてリンク切れ | **通過**（撮影は開発者の手番であり本サブの後。**★画像の不在を理由に止まっていない**） |
| 4 | `exe` 名が `tacpendium-windows-amd64.exe` | `Makefile:70` の `-o dist/tacpendium-windows-amd64.exe`（実測）＝`CHANGE-191` と**一致**。食い違いなし | 通過 |
| 5 | 枝元 | `git log --oneline -5` を §1.1 に貼った | 通過 |

### 1.1 枝元（`git log --oneline -5`・実測）

```
1cfbc07 Merge pull request #227 from plexiblinp/claude/adoring-thompson-hl2142
fc06f23 docs(M36-01): M32-03 と並列で投入可と判定し、検査の衝突を防ぐ（D-886）
96b01d6 Merge pull request #226 from plexiblinp/claude/adoring-thompson-hl2142
d4e692b Merge pull request #225 from plexiblinp/claude/exciting-cerf-vtmoia
cbf1eb4 Merge remote-tracking branch 'origin/main' into claude/exciting-cerf-vtmoia
```

---

## 2. 段 1 — 実査 5 点（指示書 §2.1・チェックリスト束 D）

**★数値はすべて「実測」である。算出値は 1 つも使っていない**（計測点 `M-157` / D-5）。

### 2.1 `Makefile` の全ターゲット（実測 18 件・`.PHONY` の宣言数と一致）

`ensure-web-deps` ／ `run-server` ／ `run-web` ／ `build` ／ `build-windows` ／ `build-darwin` ／ `build-linux` ／ `build-all` ／ `check-windows` ／ `test` ／ `test-go` ／ `test-web` ／ `run-server-debug` ／ `build-debug` ／ `test-go-debug` ／ `e2e` ／ `e2e-only` ／ `tidy`

クロスビルドの出力先とファイル名（実測・逐語）:

| ターゲット | 行 | 出力 |
|---|---|---|
| `build-windows` | `Makefile:70` | `dist/tacpendium-windows-amd64.exe` |
| `build-darwin` | `Makefile:74` | `dist/tacpendium-darwin-arm64` |
| `build-linux` | `Makefile:78` | `dist/tacpendium-linux-amd64` |
| `build-all` | `Makefile:80` | 上記 3 本の集約のみ |

**★★zip / tar.gz / チェックサムを作る行は 1 つも無かった**（実測）。**⇒ 本サブが最初の 1 本である。**

### 2.2 `.github/workflows/` の全ワークフローと契機（実測 2 本）

| ファイル | 契機（`on:`） | 内容 |
|---|---|---|
| `pr-checks.yml` | `pull_request` ／ `push: branches:[main]` ／ `workflow_dispatch` | `go vet` ／ `gofmt` ／ `go build` ／ `go test` ／ フロント単体テスト |
| `nightly-crossbuild.yml` | `schedule: "0 18 * * *"` ／ `workflow_dispatch` | `make build-all` ＋ E2E。`dist/` を artifact として 14 日保存 |

**★★`tags:` 契機を持つワークフローは 1 本も無い**（実測）。**⇒ `CHANGE-129` の「CI の artifact は配布物ではない」という境界は、契機の面でも保たれている。**

### 2.3 ★★★リリースを打つ手順が**どこにも無いこと**（**否定形の確認**・`E-117` / D-2）

**「無いはず」で済ませていない。⇒ 実際に探して 0 件であることを確かめた。**

| 探した語 | 走査範囲 | ヒット |
|---|---|---:|
| `goreleaser` ／ `.goreleaser.yml` | リポジトリ全体 | **0** |
| `softprops`（`action-gh-release`） | リポジトリ全体 | **0** |
| `actions/create-release` ／ `ncipollo` | リポジトリ全体 | **0** |
| `gh release` | リポジトリ全体 | **0** |
| `on: push: tags:` ／ `on: release:` | `.github/workflows/` | **0** |
| `sha256sum` ／ `shasum`（成果物向け） | `docs/` を除く追跡ファイル | **0**（`.devcontainer/Dockerfile` の Go ツールチェーン検証 1 件は成果物向けではない） |
| `zip` ／ `tar -c` で成果物を固める行 | `docs/` を除く追跡ファイル | **0**（`scripts/generate-template-zip.sh` は設計テンプレ集であり配布物ではない） |

**★アーカイブ名 3 件が唯一現れるのは `nightly-crossbuild.yml:105-107` の*コメント内*である**（実測）。同コメント自身が「アーカイブ化と `README.txt` の同梱は Releases 公開時の人の手番であり、本ワークフローの範囲外」と書いている。

**★両ワークフローとも `permissions: contents: read` である**（実測）。**⇒ リリースを作る権限すら与えられていない。**

**★近いが別物＝`scripts/make-public-snapshot.sh`。** タグを引数に取るがソースの公開ミラーを作るスクリプトであり、**バイナリもアーカイブもチェックサムも作らず、リモートへ一切触れない**（実測）。

**⇒ 結論: リリースを打つ手順はリポジトリのどこにも存在しない。土台にできる既存物は無かった。**

### 2.4 `docs/usermanual/` の実体（実測）

| パス | サイズ | 備考 |
|---|---:|---|
| `tacpendium-readme.html` | 102,234 B | 1,057 行。`<h2>` **25**（前置き 3 ＋ 章 **22**）／ `<figure>` **27** ／ `<img>` **27** |
| `dist-readme.txt` | 11,396 B | 174 行。**`README.txt` の正本**（本サブが編集した。§4） |
| `images/.gitkeep` | 504 B | 撮影の命名規約メモ。**画像ファイルは 0 枚** |

**★`<img>` は 27 箇所あり、画像が 0 枚であるため現状は全図がリンク切れである**（実測）。**⇒ 版ゲート 3b / P-3 により、これを不合格としていない。撮影は開発者の手番であり本サブの後である。**

**★★「22 枚の PNG」という記述が `DES-002` §11.2 と `CHANGE-192` §2.1 に在るが、実測は 27 である**（章 22 と図 27 の混同）。**⇒ §8 へ申し送った。**

### 2.5 ライセンス関係ファイルの全数（実測・**数え方で 4 と 7 に割れる**）

| 数え方 | 件数 | 内訳 |
|---|---:|---|
| **ルート直下のみ**（チェックリスト §8 対照値と同じ数え方） | **4** | `LICENSE`(34,020 B) ／ `DATA-LICENSE.md`(3,835 B) ／ `NOTICE`(4,027 B) ／ `REUSE.toml`(8,234 B) |
| **`REUSE.toml` が指す `LICENSES/` を展開**（B-3 の「`REUSE.toml` が指すもの」に素直に従った数え方） | **7** | 上記 4 ＋ `LICENSES/AGPL-3.0-or-later.txt`(34,020 B) ／ `LICENSES/CC-BY-SA-4.0.txt`(18,375 B) ／ `LICENSES/MIT.txt`(1,067 B) |

**★★`LICENSE` と `LICENSES/AGPL-3.0-or-later.txt` は md5 が一致し完全同一である**（実測 `216109e2c1c3eaf57de2bfc18c11c9f8`）。**⇒ 両方を同梱すると同じ本文が 2 部入る。★同梱を判断する材料である。**

**★否定形の確認**: `COPYING` ／ `COPYING.LESSER` ／ `.reuse/` ／ `AUTHORS` ／ `THIRD-PARTY-NOTICES` は**いずれも存在しない**（実測）。

**★★★着手時点では数えただけであり、入れるかは決めていなかった**（§2.4-1 / B-1 / B-2）。**⇒ 判断事項として §8-1 へ上げた。**

**★★【2026-09-16 追補】判断が下りた＝同梱する。範囲は全 7 件・パス保持**（開発者判断）。**⇒ §3.5 と §8-1 を更新した。**

---

## 3. やったこと

### 3.1 新規 3 本（**すべて `+` のみ。deletions 0**＝`E-225`）

| ファイル | 行数 | 役割 |
|---|---:|---|
| `scripts/release-targets.sh` | 90 | **構成の正本**（アーカイブ名 ／ 中の実行ファイル名 ／ 形式 ／ 同梱物 ／ 追加同梱物）。定義のみで単体では何もしない |
| `scripts/check-release-archive.sh` | 351 | **中身の検査 ＋ `--self-test`（＝破壊確認）** |
| `scripts/build-release-archives.sh` | 165 | 組み立て ＋ SHA-256 生成 |

**★正本を 1 か所にした理由**＝組み立てと検査が同じ表を知る必要があり、**2 か所に書くと必ずドリフトする**（`E-76`）。**⇒ `Makefile` にも名前を書いていない**（書くと 3 か所目になる）。

**★★同名確認を作る前に行った**（`E-225` の (a)）。3 本とも `find` で 0 件＝新規であることを確かめてから書いた。

### 3.2 `Makefile` — ターゲット 1 本（`release-archives`）

```make
release-archives: build-all
	bash scripts/build-release-archives.sh
```

`.PHONY` へ 1 語追記。**`build-all` を前提に置いたのは、完了条件 1 が「コマンド 1 本で 3 OS ぶん」だからである**（E-5）。**★スクリプト単体でも動く**（`dist/` の既存バイナリから組む）ため、**`M36-02` が CI から呼べる**（E-4 / §7-3）。

**採った形と理由**（§7-3 は実装判断でよいとしている）: **`Makefile` にターゲット 1 本、実体はスクリプト**。理由は 2 つ——(1) 既存のクロスビルド（`build-all`）と同じ入口に揃うので、利用者が探す場所が 1 か所で済む ／ (2) 実体をスクリプトにしておくと CI から `bash scripts/...` で直接呼べ、`make` の前提（`ensure-web-deps` 等）を巻き込まずに済む。

### 3.3 `docs/usermanual/dist-readme.txt`（**正本**）＋ `README.txt`（**生成物**）

**★`README.txt` は直接編集していない。⇒ 正本を直し `bash scripts/check-dist-readme.sh --write` で生成し、引数なしで照合した**（`CHANGE-191` §2.4 / §4.1c）。

| # | 直した箇所 | 逐語（着手前 → 着手後） |
|---|---|---|
| 1 | 同梱物の点数（**A-6 の当のもの**） | **着手前＝「※ 本 zip に入っているのは実行ファイルと本 README.txt だけです。」**（`dist-readme.txt:154` / `README.txt:154`・実測）。**★いったん「実行ファイル・本 README.txt・操作説明書(manual/ フォルダ)の 3 点です。上記のライセンス関係のファイルは含まれていません。」へ直した。** **★★2026-09-16 のライセンス同梱（§8-1）で後半が真逆になったため、さらに直した。** ⇒ 現在は「ライセンスは三層構成です…関係するファイルは本アーカイブに同梱しています。見るものは次のとおりです。／ アプリ本体のライセンス本文 ... `LICENSE` ／ ゲームデータについての説明 ... `DATA-LICENSE.md` ／ 告知(帰属・撤退ポリシー) ... `NOTICE` ／ どのファイルがどの層かの正本 ... `REUSE.toml` ／ 各ライセンスの本文 ... `LICENSES/` フォルダ」 |
| 2 | 冒頭の説明書の所在（`:7`） | `操作説明書(docs/usermanual/tacpendium-readme.html)` → `本アーカイブに同梱の操作説明書(manual/tacpendium-readme.html)` |
| 3 | 末尾の説明書の所在（`:172-173`） | `(公開リポジトリの docs/usermanual/tacpendium-readme.html)` → `(manual/tacpendium-readme.html)` |
| **4** | **新設＝「■ ダウンロードしたファイルの破損確認(SHA-256)」**（**E-3**） | Windows は `Get-FileHash ... -Algorithm SHA256`、macOS は `shasum -a 256 -c`、Linux は `sha256sum -c` を 1 行ずつ。**★末尾に「これは『ファイルが壊れていないこと』の確認であり『配布元が正しいこと』の証明ではない」と明記した**（来歴は `M36-02` の射程であり、混同させない） |

**★★指示書 §0.4 と `followup` が引く「本 zip の中身は 2 点だけ」は要約である。⇒ 実ファイルの逐語は上表の着手前の文であった**（実測）。

**触っていないもの**:

- **章数「全 22 章」**（`:7` / `:193`）——**現時点では失効していない**（実測 22 章）。**★`M32-03` がクイックスタートを足すと失効するが、それは `M32-03` の面である。⇒ §8 へ申し送った**
- **`:19` の macOS 起動方法 `tacpendium-darwin-arm64`**——**Q-2 により勝手に改名しない**
- **ライセンス本文の所在案内**（「公開リポジトリの本バージョンのタグにあります」）——**既定で同梱しないため記述は真のままである**
- **`tacpendium-readme.html` ／ `images/` ／ クイックスタート**——**読むだけ**（§4.1c / P-4。`M32-03` の面）

### 3.4 `.gitignore`

`/dist/` が既に `dist/release/` をカバーしていた（実測）。**⇒ 判定を変える行は足さず、意図を明示するコメント 2 行のみ追加した。★成果物はコミットしていない**（C-6 / 完了条件 8）。

### 3.5 ★★★ライセンス関係ファイルの同梱＝**「選べる形」に作り、既定は入れない**（§2.4 / B-1・B-2・B-4）

`scripts/release-targets.sh` の末尾に `RELEASE_EXTRA_FILES` を置いた。**着手時点の既定は空＝1 つも入れない。**

**★入れると決まったら配列へ 1 行足すだけでよい**（B-4 の「足すのが 1 行で済むか」）。同じ場所に**候補 7 件を実測サイズ付きで列挙**し、`LICENSE` と `LICENSES/AGPL-3.0-or-later.txt` が完全同一であること、`NOTICE` の連絡先が伏せ字で公開リポジトリ URL が未開設であることも注記した。

**★★着手時点では勝手に入れても落としてもいない。⇒ 判断は §8-1 へ上げた。**

**★★★【2026-09-16 追補】開発者判断が下り、全 7 件を同梱した**（`LICENSE` ／ `DATA-LICENSE.md` ／ `NOTICE` ／ `REUSE.toml` ／ `LICENSES/` 配下 3 本）。**⇒ 詳細は §8-1。**

**★★あわせて組み立てを「basename 平坦化」から「パス保持」へ変えた**——**`NOTICE:4-6` が「ライセンス本文は `LICENSE` と `LICENSES/` 配下…を参照すること」と書いており、平坦化すると `LICENSES/MIT.txt` が `MIT.txt` として直下に落ちて、同梱した `NOTICE` の参照先がアーカイブ内に存在しなくなるためである。**

---

## 4. ★★★破壊確認（指示書 §5-3・チェックリスト A-1 / A-2 / P-2）

**「検査を足した」と「検査が効いている」は別である。⇒ 一過性のデモにせず、常設の機械検査に載せた。**

`scripts/check-artifact-integrity.sh` は `scripts/check-*.sh` が `--self-test` を持ち、それが通ることを**機械強制している**（`SELFTEST_ALLOW` 以外は必須）。**⇒ 本サブの破壊確認は、以後すべての手番で自動的に回る。**

### 4.1 対照 10 件の実測（`bash scripts/check-release-archive.sh --self-test`）

**★当初は 9 件だった。⇒ レビュー 低-12 を採り、陰性対照 E（`.gitkeep` すら無い真に空の `images/`）を足して 10 件（陰性 5 / 陽性 5）にした**（§10.1）。

```
OK  self-test: 陰性対照 A = 完全な zip (期待 0 / 実際 0)
OK  self-test: 陰性対照 B = 完全な tar.gz(中は tacpendium-darwin-arm64) (期待 0 / 実際 0)
OK  self-test: 陰性対照 C = manual/ に余分 2 本(★件数で固定していない証拠) (期待 0 / 実際 0)
OK  self-test: 陽性対照 = exe を抜いた zip (期待 1 / 実際 1)
OK  self-test: 陽性対照 = readme を抜いた zip (期待 1 / 実際 1)
OK  self-test: 陽性対照 = manualbody を抜いた zip (期待 1 / 実際 1)
OK  self-test: 陽性対照 = manualimages を抜いた zip (期待 1 / 実際 1)
OK  self-test: 陰性対照 E = 真に空の images/(★.gitkeep に依存していない証拠) (期待 0 / 実際 0)
OK  self-test: 陽性対照 = チェックサム不一致 (期待 1 / 実際 1)
OK  self-test: 陰性対照 D = チェックサム一致 (期待 0 / 実際 0)

自己検査: 合格(陽性は赤・陰性は緑。陰性 5 / 陽性 5)
```

**終了コード 0**（実測）。

### 4.2 ★抜いたのは「あるべきもの」である（**P-2**）

**★件数を 1 つ減らして落ちる形にはしていない。** 抜いたのは 4 つとも*あるべきもの*である——実行ファイル ／ `README.txt` ／ `manual/tacpendium-readme.html` ／ `manual/images/` ディレクトリ。**⇒ `M32-03` が `manual/` へクイックスタートを足しても、この対照は 1 つも壊れない。**

### 4.3 ★★落ちた理由が正しいことまで確かめた（**A-2**）

「赤くなった」だけでは、**別の理由で落ちている可能性**が残る。**⇒ `manual/images/` を抜いたアーカイブを単体で検査し、出力を読んだ**（実測）:

```
OK  tacpendium-windows-amd64.zip: 実行ファイル tacpendium-windows-amd64.exe
OK  tacpendium-windows-amd64.zip: README.txt
OK  tacpendium-windows-amd64.zip: manual/tacpendium-readme.html (15 バイト)
NG  tacpendium-windows-amd64.zip: manual/images/ がディレクトリとして在りません
違反 1 件
```

**★他の 3 点は緑のままで、抜いたものだけが赤い。⇒ 検査が識別できている証拠である。**

### 4.4 ★★★`manual/` を件数・ファイル名で固定していない（**P-1 / 指示書 §4.1b**）

| 面 | 実装 |
|---|---|
| **同梱** | `cp -R docs/usermanual/. → manual/` の**ディレクトリごとコピー 1 行**。ファイルを 1 つずつ列挙していない。★除いているのは `dist-readme.txt` **1 本だけ**であり、これは説明書の中身ではなく `README.txt` の生成元である（同梱すると生成物と二重になる） |
| **検査** | `RELEASE_REQUIRED_FILES` ／ `RELEASE_REQUIRED_DIRS` で「**あるべきものが在るか**」だけを見る。**エントリ総数も、ファイル名の一覧も見ていない** |
| **`images/`** | **ディレクトリとして在ることだけ**を見る（版ゲート 3b）。**★中身の枚数は見ない。⇒ 画像 0 枚でも 27 枚でも緑である** |

**★★陰性対照 C がこれを機械で証明している**——`manual/` にクイックスタート相当の HTML 1 本と PNG 1 枚を足したアーカイブが、**緑のまま**であることを毎回確かめる。**⇒ `M32-03` の着地で本サブの検査が赤くなることはない。**

**★★★陰性対照 E は、版ゲート 3b の保証が `.gitkeep` に依存していないことを示す**（レビュー 低-12）。`zip -r` も `tar -c` も**空ディレクトリをディレクトリエントリとして格納する**ため、`.gitkeep` が将来消えて画像に置き換わっても、逆に画像も `.gitkeep` も無くなっても、判定は変わらない。**⇒ レビューが独立に同じことを手作業で確かめ、その観測が本対照の追加理由になった**（§10.3-3）。

### 4.5 ★追加同梱物の検査（**レビュー 中-5 で足した**）

**★着手時点では、ライセンス関係の「選べる形」が*組み立て側にしかなかった*。⇒ 同梱すると決めて `RELEASE_EXTRA_FILES` へ 1 行足しても、`cp` が失敗したりファイル名が変わったりしたときに、検査は緑のままだった。**

**★★本サブの看板（「zip ができること」と「中身が正しいこと」は別）が、追加同梱物にだけ効いていなかった。⇒ 検査へ `(4b)` を足し、配列が空でないときだけ各 basename がアーカイブに在ることを見る形にした。**

**★★★足しただけで終えず、効くことを実測した**（A-2 と同じ姿勢）:

```
（一時的に RELEASE_EXTRA_FILES へ "LICENSE" を 1 行足し、LICENSE 未同梱の既存アーカイブを検査）
NG  tacpendium-windows-amd64.zip: 追加同梱物が在りません: LICENSE (正本 RELEASE_EXTRA_FILES の LICENSE)
NG  tacpendium-macos-arm64.tar.gz: 追加同梱物が在りません: LICENSE (正本 RELEASE_EXTRA_FILES の LICENSE)
NG  tacpendium-linux-amd64.tar.gz: 追加同梱物が在りません: LICENSE (正本 RELEASE_EXTRA_FILES の LICENSE)
違反 3 件 / EXIT=1

（正本を既定＝空へ復元）
復元後 EXIT=0
```

**⇒ 同梱を決めた瞬間に検査が付いてくる形になった。**

**★★【2026-09-16 追補】その「瞬間」が実際に来た。⇒ 開発者判断で全 7 件を配列へ足した。**

**★あわせて `--self-test` へ陽性対照を 1 件足した**（追加同梱物を 1 本抜くと赤になること）。**⇒ 対照は 10 件から 11 件（陰性 5 / 陽性 6）になった。**

**★同対照は配列が空なら自動で省略される。⇒ 将来ふたたび空へ戻しても壊れない。**

---

## 5. 組み立ての実測（指示書 §5-1 / §5-2・チェックリスト E-1 / E-5 / A-3〜A-5）

### 5.1 コマンド 1 本で 3 OS ぶん（E-5）

`make release-archives`（**終了コード 0**・実測）。出力は `dist/release/`。

| アーカイブ | サイズ（バイト・実測） |
|---|---:|
| `tacpendium-windows-amd64.zip` | 12,385,884 |
| `tacpendium-macos-arm64.tar.gz` | 11,819,393 |
| `tacpendium-linux-amd64.tar.gz` | 12,125,812 |

### 5.2 中身の一覧（**全量**。`head` / `tail` で切っていない）

```
=== tacpendium-windows-amd64.zip（zipinfo -1）===
DATA-LICENSE.md
tacpendium-windows-amd64.exe
NOTICE
LICENSE
REUSE.toml
README.txt
manual/
manual/tacpendium-readme.html
manual/images/
manual/images/.gitkeep
LICENSES/
LICENSES/MIT.txt
LICENSES/AGPL-3.0-or-later.txt
LICENSES/CC-BY-SA-4.0.txt

=== tacpendium-macos-arm64.tar.gz（tar -tzf）===
./
./DATA-LICENSE.md
./LICENSE
./LICENSES/
./LICENSES/AGPL-3.0-or-later.txt
./LICENSES/CC-BY-SA-4.0.txt
./LICENSES/MIT.txt
./NOTICE
./README.txt
./REUSE.toml
./manual/
./manual/images/
./manual/images/.gitkeep
./manual/tacpendium-readme.html
./tacpendium-darwin-arm64

=== tacpendium-linux-amd64.tar.gz（tar -tzf）===
（macOS と同じ構成。実行ファイル名のみ ./tacpendium-linux-amd64）
```

**★本節は 2026-09-16 のライセンス同梱（§8-1）後の実測である。** サイズは zip 12,421,939 B ／ macOS 11,853,722 B ／ Linux 12,158,426 B。

**★★★ライセンス関係 7 件が `LICENSES/` を保ったまま入っている**（`LICENSE` ／ `DATA-LICENSE.md` ／ `NOTICE` ／ `REUSE.toml` ／ `LICENSES/` 配下 3 本）。**⇒ 同梱した `NOTICE` が名指しする参照先がすべてアーカイブ内に在る。**

**★zip は格納順が入力順に依らない**（Info-ZIP の挙動）。**⇒ 検査はエントリの順序を見ていない。**

**★★3 OS すべてに `manual/` と `manual/images/` が入っている**（A-4 / A-5。**非公式対応でも落としていない**）。**★`manual/tacpendium-readme.html` は 3 本とも 102,234 バイト**＝正本と同一サイズであり、**空ではない**（A-3。実物で確かめた）。

**★正本 `dist-readme.txt` は 3 本とも入っていない**（意図どおり。`README.txt` が生成物として入っているため）。

### 5.3 中身の検査（`bash scripts/check-release-archive.sh`）

15 項目すべて `OK`、**違反 0 件・終了コード 0**（実測）。組み立てスクリプトが最後にこの検査を呼ぶため、**「組めた」が常に「中身を検査した」を含む。**

### 5.4 チェックサム（E-1 / E-2）

**採った形＝アーカイブと同じディレクトリに `<アーカイブ名>.sha256` を 1 本ずつ並べる**（§7-2 の設計卓案どおり）。**理由**＝`sha256sum` / `shasum -a 256` がそのまま `-c` で読める形であり、利用者に追加の道具を要求しない。ファイル名は basename で書いてあるため、そのディレクトリで `sha256sum -c` が通る。

生成値（実測）:

```
9b556da0068369f387626779530c72775986367636b49c3c2c103736f3d1a95f  tacpendium-windows-amd64.zip
52b43d96bf307c265f402b8ee46e8ce163f4f0188cbbf14e5e747f232df623f3  tacpendium-macos-arm64.tar.gz
c4a608a22eacc23dae74c050c8e0c8b0a0f28d3b140dab1cccc8a5db76ca3de7  tacpendium-linux-amd64.tar.gz
```

**★★実際に検証した記録**（E-2。**出しただけで終わっていない**）:

```
$ cd dist/release && sha256sum -c ./*.sha256
tacpendium-linux-amd64.tar.gz: OK
tacpendium-macos-arm64.tar.gz: OK
tacpendium-windows-amd64.zip: OK
EXIT=0
```

**★陽性対照も取った**——アーカイブへ 1 バイト追記すると:

```
tacpendium-linux-amd64.tar.gz: FAILED
sha256sum: WARNING: 1 computed checksum did NOT match
改変後 EXIT=1
```

**⇒ 検証が実際に効いている。**（追記は検証後に元へ戻し、再び一致することも確かめた。）

---

## 6. ★★★macOS の名前の非対称（指示書 §4.1d・チェックリスト §Q）

**揃えていない。⇒ 設計書どおりに組み、中のバイナリは `Makefile` が出す名前のまま入れた**（Q-1 / Q-2）。

| 面 | 実測値 | 出所 |
|---|---|---|
| **アーカイブ名** | **`tacpendium-macos-arm64.tar.gz`** | `docs/design/02-architecture.md:1494` |
| **中のバイナリ名** | **`tacpendium-darwin-arm64`** | `Makefile:74` の `-o dist/tacpendium-darwin-arm64` |
| **`README.txt` の記載** | **`tacpendium-darwin-arm64`**（`README.txt:19` ／ `dist-readme.txt:19`） | 生成物。本サブでは触っていない |

**⇒ `macos` という名の tar.gz の中に `darwin` という名のファイルが入る状態が、そのまま残っている。**

**★本サブは非対称を*作った*のではなく*可視化した*。⇒ 黙って揃えると、設計卓が判断する機会が消える**（Q-3）。**★CHANGE 請求として §8 へ上げた。★自採番していない**（Q-4 / `D-293`）。

> **★★指示書 §4.1d の行番号に 1 件ずれがある**（実測）。同表は `README.txt:14` と書くが、**実測は `README.txt:19`** である（`dist-readme.txt:19` と同じ行）。**★食い違いを勝手に直さず報告する**という §4.4 の趣旨に従い、ここに記録する。**⇒ 内容（どの名前がどこに在るか）は指示書のとおりで正しい。**

---

## 7. 検証（指示書 §5・チェックリスト束 F）

### 7.1 非退行＝nightly のワークフローが 1 バイトも変わっていないこと（§5-5 / C-1 / 完了条件 7）

```
$ git diff 1cfbc07 -- .github/
（出力なし）
```

**⇒ `.github/` の差分は 0 バイトである**（実測）。`pr-checks.yml` も `nightly-crossbuild.yml` も触っていない。

### 7.2 変更統計（着手基点 `1cfbc07`・`E-225` の (b)）

**★本節の値はコミット後の実測である。** **⇒ ただし本節自身を直すコミットは、その時点ではまだ存在しないため下の統計に載らない。★載るのは 1〜8 本目までである**（自己参照で嘘にしないための but 書き）。

**★★本節は 2 度更新した**——1 度目は 1〜4 本目の時点、2 度目が本版（ライセンス同梱の 3 本が増えた後）。**⇒ 数を書いた欄は作業が伸びるたびに静かに古くなる**（設計伝達レポート §7-4）。**★数え直せるように、数を得たコマンドを併記してある。**

```
$ git rev-list --count 1cfbc07..HEAD
8

$ git log --oneline 1cfbc07..HEAD
13ae77a docs(M36-01): ライセンス同梱の判断が下りたことへ報告 3 点を追随させる
e2e4260 docs(M36-01): ライセンス同梱を README.txt へ反映する
f22b429 feat(M36-01/release): ライセンス関係ファイル 7 件を配布物へ同梱する（開発者判断）
5471954 docs(M36-01): 完了報告の変更統計をコミット後の実測へ更新
62403f4 docs(M36-01): 設計伝達レポートを作成
61c5e93 docs(M36-01): 完了報告とレビュー報告を追加し progress-log へ索引行を足す
37573df docs(M36-01): 配布物が 3 点になったことへ README.txt を追随させ SHA-256 の検証手順を足す
93cca55 feat(M36-01/release): 配布アーカイブの組み立てとチェックサムを新設

$ git diff --stat 1cfbc07..HEAD
 .gitignore                                         |   2 +
 Makefile                                           |  24 +-
 README.txt                                         |  38 +-
 .../20260916-m36-01-design-exceptions.md           | 312 +++++++++
 docs/progress/M36-01-completion-report.md          | 719 +++++++++++++++++++++
 docs/progress/m36-01-review.md                     | 258 ++++++++
 docs/progress/progress-log.md                      |  15 +
 docs/usermanual/dist-readme.txt                    |  38 +-
 scripts/build-release-archives.sh                  | 188 ++++++
 scripts/check-release-archive.sh                   | 398 ++++++++++++
 scripts/release-targets.sh                         | 120 ++++
 11 files changed, 2095 insertions(+), 17 deletions(-)
```

**★★★成果物は 1 件も入っていない**（完了条件 8 / C-6）。**実測＝全 8 本の `git show --stat` を走査して `dist/release` の出現 0 件**（`git log --format=%H 1cfbc07..HEAD | while read h; do git show --stat --format= "$h"; done | grep -c dist/release` → `0`）。 `git check-ignore` も zip 3 本と `.sha256` の全てに当たる（`.gitignore:100` の `/dist/`）。

**★新規 6 本はすべて `+` のみで deletions 0 である**（`E-225`）。行数は `wc -l` の実測（**★本表も 1 度古くなった**——ライセンス同梱で 3 本が伸びた。**⇒ 数を書いた欄は作業が伸びるたびに静かに古くなる**＝設計伝達レポート §7-4）:

| 新規ファイル | 行数 |
|---|---:|
| `scripts/release-targets.sh` | 120 |
| `scripts/check-release-archive.sh` | 398 |
| `scripts/build-release-archives.sh` | 188 |
| `docs/progress/M36-01-completion-report.md` | 728 |
| `docs/progress/m36-01-review.md` | 258 |
| `docs/handover/design-reports/20260916-m36-01-design-exceptions.md` | 330 |

**★11 件の deletions はすべて `README.txt` と `dist-readme.txt` の*置き換えた行*である**（§3.3 の 3 箇所）。**⇒ 新規ファイル側の deletions は 0 である。**

**★`progress-log.md` は追記のみ（`+14`・deletions 0）である。⇒ 既存の過去節を 1 行も編集していない**（`CLAUDE.md` §8）。

**★触っていないことを実測で示す**（境界の確認。**いずれも `git diff 1cfbc07..HEAD` が 0 行**）:

| 対象 | 差分 | 根拠 |
|---|---:|---|
| `.github/` | **0 行** | C-1 / 完了条件 7（`CHANGE-129` の境界） |
| `docs/design/` | **0 行** | C-8（`CLAUDE.md` §8） |
| `docs/handover/followup-backlog.md` | **0 行** | C-7（`D-838`） |
| `migrations/` | **0 行** | マイグレ消費 0 本 |
| `internal/` ／ `cmd/` ／ `web/src/` | **0 行** | Go / TS のコードを 1 行も変えていない |
| `go.mod` | **0 行** | 新規依存 0 件 |

**★`docs/usermanual/` のうち変更したのは `dist-readme.txt` のみ**（§4.1c / P-4）。`tacpendium-readme.html` と `images/` は `git status` に現れない。

### 7.3 機械検査（§5-7 / F-2）

| 検査 | EXIT | 結果 |
|---|---:|---|
| `check-artifact-integrity.sh`（**★1 本目に回した**） | **0** | 違反なし（**★初回は赤だった。§7.5 参照**） |
| `check-dist-readme.sh` | 0 | 一致（正本と生成物） |
| `check-doc-refs.sh` | 0 | dead reference なし |
| `check-migration-license.sh` | 0 | 違反なし（層 A 30 本 / 層 B 72 本） |
| `check-doc-inventory.sh` | 0 | 型に無いファイルなし |
| `check-md-emphasis.sh` | 0 | ベースラインどおり（増加なし） |
| `check-stop-discipline.sh` | 0 | 違反なし |
| `check-progress-log-index.sh` | **0** | **113 件**すべて progress-log に在り（**★Phase D の追記後に回し直した実測値**。**⇒ 着手直後の 1 回目は本完了報告が存在しなかったため 112 件で緑であり、完了報告を書いた時点で赤（違反 1 件）になっていた。レビュー 高-1 / 高-2 が実測で検出した**） |

### 7.4 全数テスト（§5-6 / F-1）

**★★終了コードはパイプを挟まずに読み、出力は全量をファイルへ落とした**（`head` / `tail` で切っていない）。

| 対象 | コマンド | 終了コード | 件数（実測） |
|---|---|---:|---|
| Go | `go test ./... > /tmp/gotest.txt 2>&1` | **EXIT=0** | **`^--- FAIL` 0 件** ／ `^ok ` **60 件** |
| フロント単体 | `( cd web && pnpm test -- --run ) > /tmp/webtest.txt 2>&1` | **EXIT=0** | **234 ファイル passed (234)** ／ **2950 テスト passed (2950)** |
| E2E 全数 | `make e2e > /tmp/e2e.txt 2>&1` | **EXIT=0** | **352 passed (7.1m)** ／ **failed 0** ／ **flaky 0**（`✓` 行の実測も 352、`✘` 行 0） |

**★終了コードは `echo "EXIT=$?"` で*パイプを挟まずに*読んだ**（`M37-05` の `go test ... | head -20` と同型の穴を避けるため）。**★出力は 3 本とも全量をファイルへ落とした**（E2E は 407 行）。**⇒ `head` / `tail` で切っていない。⇒ どの spec が走ったかを後から示せる。**

**★E2E の件数は `M37-07`（2026-09-14）の実測 352 と一致する。⇒ 「緑だが 1 件も走っていない」ではない**（`E-125`）。

**★本サブは Go / TS のコードを 1 行も変えていない**（差分は `Makefile` ／ `.gitignore` ／ `scripts/*.sh` ／ `docs/usermanual/dist-readme.txt` ／ `README.txt` のみ）。**⇒ 3 本が緑であることは「壊していない」の確認であり、本サブの成果を積極的に示すものではない。★本サブの成果を示すのは §4 の破壊確認と §5 の実測である。**

### 7.5 ★★検査機構が実際に本サブを捕まえた（記録として残す）

**`check-artifact-integrity.sh` の初回実行は赤であった**（実測）:

```
NG  check-release-archive.sh の --self-test が「自己検査: 合格」を出力しない
  宣言だけで実装が無い／対照が落ちている可能性。再現: bash scripts/check-release-archive.sh --self-test
```

**★対照そのものは 9 件すべて通っていた。⇒ 落ちていたのは「合格をどう名乗るか」の規約である**（同検査が拾うマーカー文言が決まっている）。是正して緑になった。

**★記録する理由**＝**「他の検査が緑でも、その緑が信用できるとは限らない」という同検査の存在理由が、本サブで実際に働いた実例だからである。** 新しい `check-*.sh` を足す手番では、**`--self-test` を書いただけでは足りず、マーカー文言まで合わせる必要がある**。

---

## 8. ★設計卓への請求・申し送り（**設計伝達レポート §4 の原稿**）

> **★★本節は設計伝達レポート §4 の源泉である。⇒ レポートは作成済み＝`docs/handover/design-reports/20260916-m36-01-design-exceptions.md`**（`CLAUDE.md` §9 / `D-838`）。
>
> **★設計卓は完了報告を読まない**（2026-08-11 開発者裁定①）**。⇒ 本節に置いたままでは届かないため、レポート §4 へ「設計卓が迷わず追加できる形」で転記してある**（スラッグ ／ 何が起きるか ／ 根拠 ／ 割付の候補 ／ 新規か更新か）。
>
> **★`docs/handover/followup-backlog.md` は 1 文字も編集していない**（C-7 / `D-838`）。

### 8-1 ★★★【決着】ライセンス関係ファイルを配布物へ同梱する（**2026-09-16 開発者判断**）

**★着手時は「判断事項」として上げた。⇒ 判断が下りたので実施した。★実装が判断を下してはいない**（§2.4-3 / B-1 / B-2）。

| 項目 | 内容 |
|---|---|
| **判断** | **同梱する。範囲は全 7 件・パス保持**（開発者判断・2026-09-16） |
| **入れたもの** | `LICENSE` ／ `DATA-LICENSE.md` ／ `NOTICE` ／ `REUSE.toml` ／ **`LICENSES/AGPL-3.0-or-later.txt` ／ `LICENSES/CC-BY-SA-4.0.txt` ／ `LICENSES/MIT.txt`**（**★`LICENSES/` のディレクトリ名ごと保つ**） |
| **着手前** | **1 つも入っていなかった**（`followup` `release-zip-missing-license-files` の当のもの） |

#### ★★★なぜ 7 件で 1 セットなのか（**実測**）

**部分的に入れると、同梱した `NOTICE` の参照先が欠ける。**

| 実測 | 内容 |
|---|---|
| **`NOTICE:4-6` の逐語** | **「本ファイルは Tacpendium の配布物に付随する告知である。ライセンス本文は `LICENSE`（アプリ本体＝AGPL-3.0-or-later）と `LICENSES/` 配下、パス単位の割当は `REUSE.toml` を参照すること。データ層の説明は `DATA-LICENSE.md` にある。」** **⇒ `NOTICE` 自身が「配布物に付随する」と名乗り、4 つすべてを名指ししている** |
| **`embed_migrations.go`** | `//go:embed migrations/*.sql`。**⇒ 層 B（`CC-BY-SA-4.0`）のゲームデータのマイグレ 20 本（`*_data_*.sql`）が配布バイナリへ実際に埋め込まれる。★ライセンス本文が要る実体がある** |
| **`DATA-LICENSE.md:22`** | 「パス単位の割当の正本は `REUSE.toml` である」 |

#### ★★なぜパスを保つのか

**`NOTICE` が「`LICENSES/` 配下」と書いているためである。⇒ basename で平坦化すると `LICENSES/MIT.txt` が `MIT.txt` として直下に落ち、その参照先がアーカイブ内に存在しなくなる。**

**⇒ 組み立てを「basename 平坦化」から「パス保持」へ変えた**（§3.5）。**★あわせて basename 衝突ガードは不要になったため、「同じパスを 2 回書いた」場合の重複検出へ置き換えた。★リポジトリ外を指すパス（`/` 始まり・`..` を含む）は exit 2 で止める。**

#### ★重複を承知で入れたもの

**`LICENSE` と `LICENSES/AGPL-3.0-or-later.txt` は md5 一致で完全同一である**（`216109e2c1c3eaf57de2bfc18c11c9f8`・34,020 B × 2）。**⇒ 重複 34KB は 12MB のアーカイブの 0.3% であり、リポジトリと同じ形を保つ利益のほうが大きい**（開発者判断）。**★REUSE の慣行でも `LICENSES/` 配下に全本文が揃っている前提のツールがあり、欠けると `REUSE.toml` の宣言と実態がずれる。**

#### ★★★減らすときの注意（**次の担当へ**）

**先に `NOTICE` の参照先が欠けないかを見ること。⇒ 「重複しているから `LICENSE` を落とす」のような一見素直な削減が、同梱した `NOTICE` を壊す。★法務の断定は開発者の手番である**（`roles-and-routing` のハード列）**。⇒ 実装で減らさない。**

#### ★未確定のまま同梱されたもの（**開発者へ申し送り**）

**`NOTICE` の連絡先は伏せ字であり、記載の公開リポジトリ URL（`https://github.com/plexiblinp/tacpendium`）は未開設である**（`NOTICE` 内に明記）。**⇒ 同梱したことで、この 2 つが利用者の手元へ届くようになった。★公開の手番までに差し替えが要る。**

### 8-2 ★★CHANGE 請求 1: `DES-002` §11.2 の「組む仕組みは存在しない」が失効した

**★自採番していない**（`D-293`）。

| 項目 | 内容 |
|---|---|
| **対象** | `docs/design/02-architecture.md` §11.2 の注記ブロック（`:1506` / `:1508`） |
| **失効した逐語** | **「★★★zip / tar.gz を組む仕組みは存在しない（2026-09-12 開発者回答で確定）。⇒ 本節が定める配布物の構成は、いまだ一度も実現されていない。★設計と as-built の乖離である。」** ／ 同 **「★`Makefile` にも zip ターゲットは無い見込みである（開発者回答）。」** |
| **as-built** | `make release-archives`（`Makefile:101`）で 3 OS ぶんが 1 コマンドで組める。実体は `scripts/build-release-archives.sh`、構成の正本は `scripts/release-targets.sh`、中身の検査は `scripts/check-release-archive.sh`（`--self-test` 付き）。SHA-256 は `<アーカイブ名>.sha256` として並ぶ |
| **失効しない部分** | **「★CI は組まない」は真のままである**（`CHANGE-129` の境界は保たれ、nightly は 1 バイトも変わっていない）。**★「CI からリリースを打つ」は依然として未実装である**（射程 6＝`M36-02`） |
| **あわせて** | `followup` `release-packaging-does-not-exist` の射程 (1)(2) が満たされた。**★(3)〜(6) は未着手のままである。⇒ 同行は閉じない** ／ `usermanual-no-reachable-path` は**同梱が実現したため閉じられる見込み**（実際にリリースが打たれるまで待つなら開いたまま） |

### 8-3 ★★CHANGE 請求 2: macOS の名前の非対称（**§Q-4**）

**★自採番していない。★製造は揃えていない**（Q-1 / Q-2）。

| 項目 | 内容 |
|---|---|
| **事象** | アーカイブ名は `tacpendium-macos-arm64.tar.gz`（`DES-002` §11.2）、中のバイナリと `README.txt` の記載は `tacpendium-darwin-arm64`（`Makefile:74` ／ `README.txt:19`） |
| **数** | **`macos` と書いてあるのは設計書の 1 か所だけ**であり、**実装と配布文書の 2 か所は `darwin` で揃っている**（実測） |
| **原因** | 命名規則が 2 つ並んでいる。`Makefile` は 3 つとも GOOS 名で出す（`windows` / `darwin` / `linux`）。`windows` と `linux` は利用者向けの語とたまたま一致して隠れ、**macOS だけが露出した** |
| **判断が要る点** | (a) 設計書のアーカイブ名を `darwin` へ寄せるか ／ (b) `Makefile` の出力名を `macos` へ寄せるか（**★`CHANGE-191` で固定された面であり、`windows_gui_link_test.go` や nightly の `Verify artifacts` にも波及する**） ／ (c) 非対称のまま明文化するか |
| **実装側の備え** | **`scripts/release-targets.sh` の表を 1 行直せば、どちらへでも寄せられる**（アーカイブ名と中のバイナリ名が同じ行に並んでいる） |

### 8-4 ★実査で出た記載の食い違い 3 件（**いずれも勝手に直していない**）

| # | 事項 | 実測 |
|---|---|---|
| 1 | **指示書 §4.1d の表が `README.txt:14` と書く** | **実測は `README.txt:19`**（`dist-readme.txt:19` と同じ行）。**★内容は正しい。行番号だけがずれている** |
| 2 | **チェックリスト §8 対照値「ライセンス関係ファイル 4」** | **ルート直下だけを数えれば 4 で正しい。⇒ ただし B-3 は「`REUSE.toml` が指すもの」と書いており、素直に従うと `LICENSES/` 3 本を含めて 7 になる**。**⇒ 対照値に数え方を添えると取り違えが消える** |
| 3 | **`DES-002` §11.2 ／ `CHANGE-192` §2.1 の「22 枚の PNG」** | **実測は `<img>` 27 箇所**（`<figure>` も 27）。**★章数 22 と図数 27 の混同である**。**⇒ 本サブの判断（同梱の形）には影響しない**が、data URI 案を落とした理由の数字であるため記録する |

### 8-5 ★既知だが本サブの射程外（触っていない）

| # | 事項 |
|---|---|
| 1 | **`nightly-crossbuild.yml:105-107` のコメントが `CHANGE-191` で失効したまま**（逐語「中身 = tacpendium.exe + README.txt」）。**★既存の `followup` `readme-exe-name-ci-comment-unsynced` である。⇒ 本サブは `.github/` を 1 バイトも触らないため直していない**（C-1）。**★★本サブの着地で、同コメントは「2 点」という点数の面でも失効した**（配布物は 3 点になった）。**⇒ 失効の根拠が 1 つ増えた** |
| 2 | **`followup` `release-zip-missing-license-files` の根拠文面**（同じく nightly の逐語を引用）も、上と同じ理由で古くなっている |
| 3 | **`dist-readme.txt` の「全 22 章」は `M32-03` の着地で失効する。⇒ `M32-03` がクイックスタートを足すため**。**★本サブでは触っていない**（現時点では実測 22 章で正しく、説明書の中身は `M32-03` の面である）。**⇒ `M32-03` の手番で `dist-readme.txt:7` と `:193` の 2 か所を見ること**（**★直すのは正本であって `README.txt` ではない**） |
| 4 | **`des002-macos-arch-mismatch`**（§11.1 は macOS amd64 / arm64 の両方を非公式対応と書くが §11.2 と `Makefile` は arm64 のみ）。**★既存の `followup` であり、本サブは arm64 の 1 本だけを組んでいる** |

### 8-6 ★★★「組める」ことは「配布してよい」ことではない（**リリース可否の門がどこにも無い**・レビュー 中-9）

**★現時点で `make release-archives` を打つと、`<img>` 27 箇所がすべてリンク切れの説明書が入ったアーカイブ 3 本が、検査全緑で出来上がる**（`docs/usermanual/images/` は実測 0 枚）。

**★★版ゲート 3b がこれを不合格にしないのは正しい**（撮影は開発者の手番であり本サブの後である）**。⇒ しかしその帰結として、「撮影前の成果物を配布してはならない」を止める仕組みは、機械にも手順にも 1 つも無い状態になった。**

| 面 | 状態 |
|---|---|
| 本サブの成果 | **「コマンド 1 本で組める」ことである** |
| 本サブが保証しないこと | **「組んだものを配布してよい」こと。⇒ 画像 0 枚でも緑である** |
| いま門が無い場所 | **実際にリリースを打つ手番**（開発者。射程外＝指示書 §3-8） |

**⇒ リリースを打つ前に、撮影の完了を人が確かめること。★機械では止まらない。**（門を置くべきかは設計卓の判断。**`M36-02` が CI からリリースを打つ形を作るなら、そこが自然な置き場になる**。）

### 8-7 ★★SHA-256 の検証手順が、検証したい時点では読めない場所にしかない（**`M36-02` への従属**・レビュー 中-7）

**★手順は `README.txt` に足した**（E-3 の「`README.txt` か説明書に 1 行」は満たしている）**。⇒ しかしその `README.txt` はアーカイブの中にある。**

**★★ダウンロード直後にチェックサムを照合したい利用者は、まだ展開していない。⇒ 展開してしまった後では、壊れたファイルを展開した後である。**

**⇒ 同じ 1 行が*リリースページ側*にも要る。★これは射程 6（CI からリリース成果物を出す＝`M36-02`）への従属である。⇒ `M36-02` がリリース本文を組むときに、本サブが `README.txt` へ書いた 3 行を同じ内容で載せること。**

### 8-8 ★`README.txt` 末尾の「リポジトリを扱う人向けの注記」が、本サブで初めて利用者の手元へ届く（レビュー 中-8）

**★逐語**（`README.txt` 末尾）**＝「※ ここから下はリポジトリを扱う人向けの注記です(利用者の方は読み飛ばしてください)。本ファイルは docs/usermanual/dist-readme.txt から生成されています。修正はそちら(正本)を直し、bash scripts/check-dist-readme.sh --write で作り直してください。」**

| 時点 | `README.txt` の所在 |
|---|---|
| `CHANGE-191`（2026-09-12） | **リポジトリの中だけ。⇒ 注記の宛先（リポジトリを扱う人）と読者が一致していた** |
| **本サブの着地後** | **配布アーカイブにも入る。⇒ 内部の生成手順書が配布物へ載る形になった** |

**★実害は小さい**（「読み飛ばしてください」と断ってあり、内容も機密ではない）**。⇒ ただし状態が変わったこと自体を記録する。★配布時に落とすか残すかは `CHANGE-191` を起こした面の判断であり、製造が決める事項ではない。**

**★落とす場合の注意＝落とすと `check-dist-readme.sh` の照合が壊れる**（同スクリプトは「正本の全文 ＋ 生成注記」を生成物と定義している）**。⇒ 素直な形は「組み立て時に末尾ブロックを落とす」ではなく「注記の文面を配布物に載ってよいものにする」のほうである。**

### 8-9 ★新設の検査を `CLAUDE.md` §8 の表へ載せるか（**製造が編集する面ではない**・レビュー 低-13）

**★`scripts/check-release-archive.sh` は `CLAUDE.md` §8「常設の機械検査（まとめ）」の表に載っていない。**

**★★機械的な強制はすでに効いている**——`check-artifact-integrity.sh` が `scripts/check-*.sh` の `--self-test` を拾うため、**本検査が壊れれば同検査が赤くなる**（実測＝`OK check-release-archive.sh の自己検査が通る`）。

**⇒ 表へ載せるかどうかは「人が迷ったときに回す先」として案内するかの判断であり、`CLAUDE.md` は製造が勝手に編集する面ではない。★設計卓・開発者の手番として上げる。**

**★あわせて `check-derived-docs.sh` の先例に注意すること**——同スクリプトは表に載っていなかったため、**回すきっかけが失われて派生資料が長期間止まり、設計卓が資料を使わなくなる実害が出た**（`CLAUDE.md` §8 の注記）。

### 8-10 ★停止時記録（必須 5 フィールド）

**★未解消のまま停止した項目は 0 件である。⇒ 再レビューの往復上限にも達していない。**

本サブで開いたままの事項は §8-1（開発者の判断待ち）と §8-2 / §8-3（設計卓の CHANGE 採番待ち）であり、**いずれも「製造が解けずに止まった」ものではなく、はじめから他者の手番として設計された事項である。⇒ 停止規律の §J 行としてではなく、上記の請求として上げる。**

---

## 9. 完了条件の自己点検（指示書 §6）

| # | 条件 | 判定 | 根拠 |
|---|---|---|---|
| 1 | §11.2 の構成どおりの zip / tar.gz が**コマンド 1 本で 3 OS ぶん**作れる | **満たす** | §5.1（`make release-archives` EXIT=0） |
| 2 | 中身が実物で確かめられている（`manual/` が空でない ／ `README.txt` が生成物と一致） | **満たす** | §5.2（本文 102,234 B × 3 本）／ §7.3（`check-dist-readme.sh` EXIT=0） |
| 3 | **破壊確認が在る**＝中身を 1 つ抜くと検査が落ちる | **満たす** | §4（対照 9 件 ＋ 落ちた理由の確認） |
| 4 | チェックサムが出て、**実際に検証した記録**が在る | **満たす** | §5.4（`sha256sum -c` EXIT=0 ＋ 陽性対照） |
| 5 | 段 1 の実査 5 点が報告に在る（**3 は否定形**） | **満たす** | §2（とくに §2.3） |
| 6 | ライセンス関係の全数と同梱の判断事項が §4 原稿に在る（**勝手に入れていない**） | **満たす** | §2.5（全数 4 / 7 の両方）／ §8-1。**★着手時は入れず、判断を上げた。⇒ 2026-09-16 に開発者判断が下りてから同梱した。★実装が判断を下してはいない** |
| 7 | nightly が 1 バイトも変わっていない | **満たす** | §7.1（`git diff -- .github/` が空） |
| 8 | 成果物をコミットしていない | **満たす** | §3.4（`/dist/` は `.gitignore` 済み）。**★実測＝`git check-ignore` が `dist/release/*.zip` と `*.sha256` の両方に当たり、`git status` に 1 件も現れない。⇒ コミット後も `git show --stat` に `dist/release` は 0 件**（§7.2） |
| 9 | 着手前の版ゲート 5 点を通した | **満たす** | §1 |
| 10 | `progress-log.md` へ 1 行追記した | **満たす** | 追記済み（横断課題 8 件つき）。**`check-progress-log-index.sh` を追記後に回し直して EXIT=0 / 113 件**（§7.3）。**★本欄は当初「Phase D で実施」という*未来形*で「満たす」と書いており、根拠が存在しなかった。⇒ レビュー 高-3 の指摘で実測へ是正した**（`D-510` と同型） |
| 11 | CHANGE 原稿を §4 へ書いた（**自採番していない**） | **満たす** | §8-2 ／ §8-3 |
| 12 | 完了報告を `check-md-emphasis.sh` で見た | **満たす** | §7.3 |

---

## 10. レビュー結果（Phase B / C）

| 項目 | 値 |
|---|---:|
| レビュー報告書 | `docs/progress/m36-01-review.md`（215 行） |
| 指摘の総数 | **13 件** |
| 高 | **4 件** |
| 中 | **5 件** |
| 低 | **4 件** |
| **★「高」指摘の不採用** | **0 件**（**⇒ 全件採用したため、Phase C 安全弁のエスカレーションは発生していない**） |
| 再レビューの往復 | **0 回**（初回のみ。**⇒ 上限 2 回に達していない**） |

### 10.1 トリアージ（**採否と理由**・全 13 件）

| # | 優先度 | 指摘 | 採否 | 理由・対応 |
|---|---|---|---|---|
| 1 | **高** | `progress-log.md` へ索引行が追記されていない（`check-progress-log-index.sh` が EXIT=1） | **採用** | **指摘のとおりであった。** Phase D で追記し（横断課題 8 件つき）、**回し直して EXIT=0 / 113 件**を確認した |
| 2 | **高** | §7.3 の同検査の行が実測（赤）と食い違う | **採用** | **1 回目を回したのは完了報告を書く前であり、その時点では 112 件で緑だった。⇒ 完了報告が生まれた瞬間に赤へ変わっていた。** 追記後の実測値へ直し、**経緯（いつ緑でいつ赤だったか）も併記**した |
| 3 | **高** | §9 完了条件 10 の根拠が「Phase D で実施」という**未来形**である | **採用** | **`D-510` と同型の誤りであり、弁解の余地がない。** 断定を書いた時点で、それが観測ではなく予測であったことは記録から消える。実在する根拠（追記済み ＋ 再実行の実測値）へ直し、**是正した事実自体も欄に残した** |
| 4 | **高** | §7.4 が `<!-- TEST-RESULTS-PLACEHOLDER -->` のまま | **採用** | **レビュー実行中に先行して埋めた**（E2E の完走を待っていたため。`COMPLETION-REPORT-WRITE-TIMING` に従い、結果が出るまでプレースホルダを置いていた）。**⇒ レビュアーが読んだ時点では未記入であり、指摘は正しい。** 実測（EXIT ＋ 件数）で記入済み |
| 5 | 中 | 検査側が `RELEASE_EXTRA_FILES` を 1 度も参照していない | **採用** | **本サブの看板（「組めた」と「中身が正しい」は別）が追加同梱物にだけ効いていなかった。** 検査へ `(4b)` を足した。**★足しただけで終えず、`LICENSE` を 1 行足した状態で既存アーカイブを検査して 3 本とも赤になることを実測し、既定へ戻して緑に戻ることも確かめた** |
| 6 | 中 | `RELEASE_EXTRA_FILES` の basename 平坦化が未明示。同名は無警告で上書き | **採用** | `release-targets.sh` へ平坦化と衝突の注意を明記し、**組み立て側で `[ -e ]` による衝突検出を足して exit 2 で止める形**にした。**★候補 7 件の basename は現状すべて異なるため 7 件すべて足しても衝突しないことも実測して注記した** |
| 7 | 中 | SHA-256 の検証手順がアーカイブの中にしか無い | **採用** | §8-7 として申し送った（**`M36-02` 射程 6 への従属**として明記） |
| 8 | 中 | `README.txt` 末尾の生成注記が本サブで初めて配布物へ載る | **採用** | §8-8 として申し送った。**★落とすと `check-dist-readme.sh` の照合が壊れる**ことまで書き添えた |
| 9 | 中 | 「画像 0 枚でも緑」がリリース可否の申し送りとして上がっていない | **採用** | §8-6 として申し送り、**`progress-log.md` の横断課題 1 番にも置いた**（後任は progress-log から辿るため） |
| 10 | 低 | `check_archive` の未使用変数 `src` | **採用** | 削除した |
| 11 | 低 | GNU coreutils 前提が「限界」節に無い | **採用** | `build-release-archives.sh` の限界節へ 1 行足した |
| 12 | 低 | 自己検査の `images/` 対照が常に `.gitkeep` を含む | **採用** | **陰性対照 E（`.gitkeep` すら無い真に空の `images/`）を足した。⇒ 対照は 9 件から 10 件（陰性 5 / 陽性 5）になった。** あわせて `has_dir` のコメントへ「`.gitkeep` に依存していない」を明記した |
| 13 | 低 | `CLAUDE.md` §8 の常設機械検査表への掲載 | **採用**（申し送りとして） | §8-9 として上げた。**★`CLAUDE.md` は製造が勝手に編集する面ではない。⇒ 設計卓・開発者の手番。** `check-derived-docs.sh` が表に無かったために実害が出た先例も添えた |

### 10.2 ★「高」指摘の不採用は **0 件**である

**★本節は Phase C の完了後に書いている**（`COMPLETION-REPORT-WRITE-TIMING`）。**⇒ 上の 0 件は観測であって予測ではない。★皮肉なことに、高-3 はまさにこの規律を破ったことへの指摘であった。**

### 10.3 ★レビューが独立に確かめたこと（製造の主張の裏取り）

**レビュアーは報告を読むだけで済ませず、自分で回している。**

| # | レビューが独立に実施したこと | 結果 |
|---|---|---|
| 1 | `--self-test` の再実行 | 陽性 5 / 陰性 4（当時）すべて期待どおり・EXIT=0 |
| 2 | **破損アーカイブを自作して単体検査**（exe 欠落 / `README.txt` 欠落 / `manual/` 丸ごと欠落の 3 本） | **抜いたものだけが NG、残りは緑。⇒ 巻き添えで落ちていないことを独立に確認** |
| 3 | **★`.gitkeep` すら無い真に空の `images/`** で zip / tar.gz を自作して検査 | **どちらも EXIT=0。⇒ 版ゲート 3b が実体としても満たされていることを、製造より踏み込んで確かめた**（この観測が低-12 の根拠になった） |
| 4 | `sha256sum -c ./*.sha256` ／ `zipinfo -1` ／ `tar -tzf` | 3 本とも OK。本文 102,234 B を実測 |
| 5 | `Makefile` 18 ターゲット ／ `<img>` 27 ／ `<figure>` 27 ／ `<h2>` 25 ／ ライセンス 4 件・7 件 ／ `LICENSE` の md5 | **報告の数値と完全一致**（§8 対照値の「数え直させる」に応えた） |
| 6 | 指示書 §4.1d の `README.txt:14` | **実測 `:19` であることを独立に確認** |
| 7 | 常設検査 7 本の再実行 | **`check-progress-log-index.sh` のみ EXIT=1** であることを検出（＝高-1〜3 の根拠） |

**★★7 番が本レビューの最大の値打ちである。⇒ 製造は自分が回した時点の緑を信じて書き、その緑が完了報告の生成によって赤へ変わったことに気づいていなかった。**

### 10.4 ★失効記述は 1 件も作り込まれていない（レビュー判定）

射程外と指定された 3 件（nightly のコメント ／ `dist-readme.txt` の「全 22 章」 ／ macOS の非対称）は、**いずれも報告に申し送りとして記録済み**であることをレビューが確認した。

---

*以上、M36-01 完了報告。*
