# M23-01 レビュー報告書

| 項目 | 内容 |
|------|------|
| 対象作業 | M23-01（ゴミ箱の実態を仕様へ揃える — `superseded_by_combo_id` の新設と残日数の撤回） |
| 対象指示書 | `docs/instructions/M23-01-trash-as-built-and-superseded-rows.md` v1.0.0 |
| チェックリスト | `docs/instructions/reviews/M23-01-review-checklist.md` v1.0.0 |
| 対象差分 | `3aaf3e5..25d97c7`（コミット 4 本 / 17 ファイル / +1480 -45） |
| 完了報告 | `docs/progress/m23-01-completion-report.md` |
| 設計伝達レポート | `docs/handover/design-reports/20260820-m23-01-design-exceptions.md` |
| レビュー実施日 | 2026-08-20 |
| 判定 | **条件付き合格**（§9 重大ゼロ。**ただし「高」2 件の修正を M23 完了前に要する**） |

---

## 総評

**最重要ゲート（手動で消した行がこれまでどおりゴミ箱に出る）は満たされている。** レビュー側でも `TestSuperseded_ManualDelete_StaysVisible` / `TestSuperseded_TrashHidesOldRowButKeepsManualDelete` を再実行して green を確認し、述語が `OnlyDeleted` の 1 分岐だけに閉じていること・`scanCombo` を通る SELECT 5 か所すべてに列が足されていることを実物で確認した。スコープ外 8 件への踏み込みは 0 件、`setups` への列追加なし、設計書本体の編集なし、`version` 不変、マイグレ連番 `000078` の実査も正しい。
**Plan Mode の実査 8 件（指示書 §3.3）は完了報告 §1 に全件そろっており、§0.1 の「確認できない」送りは発生しなかった。** とくに §3.3-7 を FK=ON / FK=OFF の 2 系で実測してテストへ固定した点、§4.9 の 3 系統走査で `CHANGE-121` の射程外だった `DES-003` §5 の残骸を掘り当てた点は、指示書の要求水準を上回る。
一方で **成果物としての欠落が 1 件ある**——`docs/progress/progress-log.md` への索引行が存在しないのに、完了報告 §14 が「Phase D で追記済み」と書いている。**機械検査（`check-progress-log-index.sh`）はこれを緑と判定するため、人が読む以外に見つける経路が無い。**
あわせて、本サブが自ら as-built で「`PUT` は `201 Created`」と確定させた直後に、同じ `internal/api/combo/dto.go` の godoc が「PATCH/PUT 200」のまま残っている。**as-built 化を目的としたサブで、目的そのものに反する失効記述がコード上に残った形**であり、優先度較正の指定に従い「高」に置く。

---

## 設計準拠性レビュー結果

| # | 項目（対応するチェックリスト節） | 評価 | 所見 |
|---|---|---|---|
| 1 | **マイグレ `000078` / 列定義**（§1・§2） | ◎ | `ALTER TABLE combos ADD COLUMN superseded_by_combo_id INTEGER REFERENCES combos(id) ON DELETE SET NULL`。**`ADD COLUMN` のみでテーブル再構築なし**、既定値なし、`materialized_from_combo_id`（`000038`）と同型の self-FK。連番は disk 末尾 `000077` を実査して `000078`（再請求条件に当たらず・自採番なし・番号飛ばしなし）。`down` は `DROP COLUMN`（`000038` の流儀と逐語で一致）。`TestRun_M2301_SchemaUpDown` が up → down → 再 up を実際に流し、`pragma_table_info` / `pragma_foreign_key_list` で各段を検算している。**レビュー側でも 3 本を再実行し green を確認。** |
| 2 | **`materialized_from_combo_id` の不可侵**（§1・§7） | ◎ | 意味・書き込み箇所ともに無改変。`M23-overview` §4.4.1 案 2（汎用由来列への読み替え）は採っていない。マイグレテストが down 時の巻き添え落ちも検算している。 |
| 3 | **`setups` へ列を足していない**（§1） | ◎ | 足していない。**§3.3-6 の実査結果も報告にある**（`UpdateSetup` は `FindByID` → 同一行 `UPDATE` のみで `InsertSetup` 相当の呼び出しが無い／API は `PATCH` 1 本）。「無い」と断ぜず 1 回確認せよという指定どおり。マイグレテストが `setups` に本列が無いことを否定形で検算している点も良い。 |
| 4 | **`PUT` の書き込み位置・トランザクション**（§1・§9） | ◎ | `internal/service/combo/service.go` の `UpdateWithKeyChange` 内、`combo.ID = newID` の直後・`InsertSteps` の前。`BeginTx`〜`Commit` の内側であり、`defer` の `Rollback` が効く。**論理削除の時点では新行 id が未確定であるため 1 文にまとめられない**という制約も §1-4 で正しく報告されている。`TestSuperseded_NotWrittenOnRollback` が存在しないタグ ID で tx 後半を失敗させ、旧行が汚れないことと論理削除も巻き戻ることを対で見ている。 |
| 5 | **`version` / `updated_at` を動かしていない**（§2・§9） | ◎ | 書き込み文は `SET superseded_by_combo_id = ?` のみ。`TestSuperseded_PutWritesSuccessorID` が旧行の `version` 不変を検算。 |
| 6 | **`PATCH` では書いていない**（§1） | ◎ | `PATCH` 経路に差分なし。 |
| 7 | **ゴミ箱の絞り込み**（§1・§9） | ◎ | `OnlyDeleted` 分岐へ `superseded_by_combo_id IS NULL` を追加。**レビュー側で `grep -n "OnlyDeleted"` を当て、判定箇所が `repository.go:690` の 1 か所しか無いことを確認した。** 一覧と件数（`count = len(items)`）・一括操作（`TrashBulkActions.tsx` は表示行から選ばれた id に対して個別 API を並べるだけ）が同一集合を通ることも実物で確認。`TestSuperseded_BulkRestoreMatchesVisibleSet` / `_BulkPermanentDeleteMatchesVisibleSet` が「画面に出ていない行が戻らない・消えない」を否定形で固定している。 |
| 8 | **SELECT の列追加漏れ**（非破壊性） | ◎ | `scanCombo` を通る SELECT は `FindByID` / `FindByIDAllowDeleted` / `List` / `FindActiveByDuplicateKey` / `ListAllActiveCombosTx` の 5 本で、**レビュー側で `FROM combos` / `scanCombo` を全走査して 5 本以外が無いことを確認した。** 全数に列が足されており、`TestRepository_SupersededByComboID_RoundTrip` が既存列（`hit_type`）の健全性まで見て scan ズレを検出できる形になっている。INSERT のプレースホルダ数も整合（`go build ./...` 成功・実 DB テスト green）。 |
| 9 | **残日数の撤去（本番コード）**（§3） | ◎ | `TRASH_RETENTION_DAYS` / `calculateRemainingDays` / 列 7 のセル / ヘッダ「残日数」/ 画面本文の 90 日告知がすべて消えている。**`colSpan={8}` → `{7}` の追随も取れている**（列削減の巻き添えで最も落ちやすい箇所）。文言だけ変えて列を残す形は採っていない。代わりの告知も足していない（§4.4-5 遵守）。**レビュー側で `TRASH_RETENTION\|残日数\|期限切れ\|90 日\|retention` を本番コード全域へ当て、残骸 0 件を確認。** |
| 10 | **i18n の ja/en parity**（§3） | ◎ | ゴミ箱画面は i18n を通っていない（生の日本語文字列）ため対象キーが両ロケールとも 0 件。**「片側だけ消えている」状態は構造的に発生しない**ことを完了報告 §1-5 #9 と設計伝達レポート §3-3 が明示している。レビュー側でも `web/src/locales/{ja,en}.json` に該当キーが無いことを確認。 |
| 11 | **コンボ型 3 分岐の一貫性**（§3） | ○ | `ComboSummary` と `Combo` へ追加。`ComboDetail` は `ComboSummary` を継承するため自動的に整合しており、3 つの間で食い違いは無い（`tsc --noEmit` も緑）。**ただし型定義ファイル冒頭の規約コメントが「両方に追加すること」のままで、3 分岐という実態と食い違う**（下記 M-1）。 |
| 12 | **DTO へ出す判断**（§10-2 の軽微枠） | ◎ | 出す側を採り、理由を完了報告 §3・設計伝達レポート §1-2・`dto.go` のコード内コメント（`// 推測: 〜と仮定した`）の 3 か所に残している。`omitempty` により既存クライアントへの影響も無い。**「読む本番コードは述語 1 か所だけ」という状態を明示しており、チェックリスト §0.3 N-3 の誤判定を招かない書き方になっている。** |
| 13 | **CSV / dup / recipe_hash 非対象**（§2） | ◎ | `DuplicateKey` / `CalcRecipeHash` はいずれも明示フィールドで構成され本列を含まない。CSV 入出力（`internal/service/comboio/`）にも本列は 0 ヒットで、`materialized_from_combo_id` と同じ扱いになっている。 |
| 14 | **テスト（指示書 §5 の 7 件）**（§4） | ◎ | 7 件すべてに対応があり、追加分を含め 20 本。**§5-1 は `dbtest.Setup` による実 DB**（`SUPP-001` §5.5.4 規約 14 を満たす）。§5-7 の E2E は同一 spec 内で「編集で積んだ行は出ない」と「手動で消した行は出る」を対で確認しており、UI 判定と API 判定の両方を持つ。**レビュー側で Go 側 12 本を再実行して全 PASS を確認。** |
| 15 | **テスト結果の書き方**（§4・`E-125`） | ◎ | `go test ./...` は `53 packages ok / FAIL 0`＋新規 14 本の `--- PASS` 行を転記、`pnpm test` は `170 files / 1676 tests`、`make e2e` は `1 flaky / 154 passed` と新規 2 本の緑行を転記。**終了コードだけを根拠にしていない。flaky 1 件も隠さず、本サブ起因でないと判断した根拠 2 点（単独走で 8/8 green・失敗箇所が本サブの経路を通らない）を添えている。** 「flake と断定はしない」という留保も適切。 |
| 16 | **as-built の報告（§3.3-2 / §4.5）**（§8） | ○ | 完了報告 §10 に 4 経路（`PUT` / `restore` / `permanent` / ゴミ箱一覧）を独立節として置き、ハンドラ・クエリ引数・成功／異常応答・副作用まで列挙している。**`count` が総件数でなく `len(items)` である点、`only_deleted` が `include_deleted` に優先する点、`character_id` が画面側で 1 固定である点まで拾っており、`CHANGE-121` §2 へ写す材料として十分。** 実装は 1 行も変えていない。**減点は下記 H-2**（同じ as-built が `dto.go` の godoc と矛盾したまま）。 |
| 17 | **否定形走査の 3 系統**（§8） | ◎ | 本番コード／テスト資産／設計文書の 3 系統を走査し、系統③で **`CHANGE-121` の起票文に無い 2 件目の残骸（`DES-003:946`「ゴミ箱に90日以上保持されたレコード（設定で変更可能）」）を発見**している。**レビュー側でも同じ走査を当て、残骸が `DES-003:946` と `DES-005:738` の 2 件で、いずれも設計卓の手番であることを確認した。** `PUT` 後にゴミ箱の件数を数えるテストの実査も 4 分類で行われ、期待値を書き換えたテストが 0 本であることが示されている。 |
| 18 | **スコープ遵守**（§5） | ◎ | 切り替え導線なし／変更履歴ビューなし／自動完全削除の機構なし／`FR013` 不可侵（誰が消したかを出していない）／`restore`・`permanent` の契約不変／`DELETE` の楽観排他は据え置き。**`M23-03` / `M23-06` へ踏み込みたくなった箇所 4 件を §9 に明記して踏み込んでいない**のは指示書 §2.3 の要求どおり。 |
| 19 | **既存挙動の温存**（§7） | ◎ | `character_id` 絞り込みは `TestRepository_ListOnlyDeleted_KeepsCharacterFilter` で固定。生成元バッジは `materialized_from_combo_id` 無改変により無傷。復元の現状挙動（印に触れない）は `TestRepository_Restore_LeavesSupersededMarkUntouched` で固定。 |
| 20 | **品質検査**（§6） | ◎ | レビュー側で再実行し、`check-artifact-integrity.sh`（検査 11 件の自己検査＋派生資料 4 件 OK）／`check-doc-refs.sh`／`check-md-emphasis.sh`（436 行＝ベースライン）／`check-enum-sync.sh`（増加なし）／`check-browser-storage-keys.sh`（台帳 9 / 実装 8 で一致）／`check-stop-discipline.sh` がいずれも違反なし。`go build ./...` 成功、`gofmt -l internal/` 0 件、`tsc --noEmit` エラーなし。`console.log` / `fmt.Println` 0 件。JSON タグは `supersededByComboId`（camelCase）で DB 列名と分離。 |
| 21 | **ドキュメント（§7.4 progress-log）**（§8） | **×** | **`docs/progress/progress-log.md` に M23-01 の索引行が存在しない。** 詳細は下記 H-1。 |
| 22 | **完了報告・設計伝達レポートの構成**（§8） | ○ | 「■ 併せて更新が要るもの」（§13）11 項目・並列相手の明記・消費 CHANGE / マイグレの記載・as-built 独立節・followup 候補の §4 送り（`followup-backlog.md` は未編集＝**D-382** 遵守）と、要求されたものは揃っている。**「次の番号」の写し先が実査で 3 か所であり 4 か所ではないこと（M23 に契約ファイルが無い）を、決め打ちせず実査して報告した点は評価できる。** 減点は §14 の事実と異なる記述（H-1 / M-2）。 |

---

## 設計準拠性以外の指摘事項

### H-1（高）`progress-log.md` への索引行が無い。完了報告 §14 はそれを「追記済み」と書いている

- **事実**: `docs/progress/progress-log.md`（4772 行）に M23-01 の索引行が無い。最終エントリは `M23-RESEARCH-01`（2026-08-20）のまま。`git status` はクリーンで、未コミットの追記も無い。差分 17 ファイルにも同ファイルは含まれない。
- **`grep -n "M23-01"` のヒットは 1 件だけで、それは `M23-RESEARCH-01` のエントリ本文中の「`M23-01` の要決定事項」という言及**である。
- **にもかかわらず完了報告 §14 の見出しは**「progress-log への索引行（Phase D で追記済み）」と書かれている。**存在しない成果物を「済み」と記載している。**
- **機械検査では捕まらない**: `scripts/check-progress-log-index.sh` は「作業 ID が本文のどこかに現れるか」を `grep -qiF` で見る床であり、上記の言及ヒットで緑になる。実際に回すと `OK 検査した 45 件すべてが progress-log に現れる` と出る。**スクリプト自身が冒頭の「限界」節でこの穴を明記している**（「言及はあるが索引行として書かれていない形は検出できない。内容の妥当性は人間が読む」）。
- **違反している規定**: 指示書 §7.4 ／ `CLAUDE.md` §8（「**サブ完了時の追記は必須**」「指示書が明示的に求めていなくても追記する」）。
- **害**: 完了報告 §14 は横断課題 2 件（ボード §2.2 のマイグレ連番のズレ ／ `CHANGE-121` の射程から漏れた `DES-003` §5）を「progress-log へ載せた」前提で書いている。**索引行が無いということは、この 2 件が横断インデックスに載っていない**ということであり、`CLAUDE.md` §8 が索引を置く目的（完了報告に閉じない事項を時系列・横断で拾えるようにすること）が達成されていない。とくにマイグレ連番のズレは「次のサブが `000078` を再払い出しして衝突する」型の事故（**D-120**）に直結する。
- **修正**: `progress-log.md` の末尾へ M23-01 の索引行（日付・作業 ID・結果・報告書リンク＋横断課題 2 件）を追記する。**あわせて完了報告 §14 の「（Phase D で追記済み）」という記述を、実際に追記した後の状態に合わせること。**

### H-2（高）`internal/api/combo/dto.go:194` の godoc が失効している——`PUT` は `200` ではなく `201 Created`

```go
// ComboResponse は単一コンボの API レスポンス(POST 201、GET 200、PATCH/PUT 200)。
type ComboResponse struct {
```

- **実装**: `internal/api/combo/handler.go` の `UpdateWithKeyChange` は `return c.JSON(http.StatusCreated, toComboResponse(combo, &result))` で **`201 Created`** を返す。
- **本サブ自身がこれを as-built として確定させている**——完了報告 §10-1 の「成功応答」欄に「**`201 Created`** ＋ `ComboResponse`。**★返る `id` は新しく採番された id である**」と明記されている。
- **にもかかわらず、同じ `dto.go` の 40 行下（`ComboResponse` の構造体本体）を編集しながら、その直上の godoc は「PATCH/PUT 200」のまま残された。**
- **これは既存の誤りであって本サブが壊したものではない。** ただし本サブの目的の 1 つが「**ゴミ箱まわりの API 契約の as-built を揃えること**」であり、**実態と食い違った契約記述をコード上に残したまま「as-built を報告した」と結論するのは、本サブが畳もうとしている食い違いと同型の負債**である（完了報告 §3 が DTO 判断の理由として自ら述べている論理がそのまま当てはまる）。
- **優先度較正**: 動作は正しいままなのでテストも lint も型検査も緑であり、**人が読む以外に見つける経路が無い**。かつ **後任は本文をコピーして注記を読まない**——`ComboResponse` の godoc は「この DTO を返す経路とステータス」の一次記述であり、次に `PUT` 系を触る担当がここを前提にする。本プロジェクトの基準に従い「高」に置く。
- **修正**: 1 行。`(POST 201、GET 200、PATCH 200、PUT 201)` の形に直す（`PUT` が `201` を返す理由＝新 id で採番し直すため、を一言添えるとなお良い）。

### M-1（中）`web/src/features/combo/types.ts` 冒頭の規約コメントが 3 分岐という実態と食い違う

```ts
// 注: ComboSummary と ComboDetail / Combo は別 interface だが、バックエンド ComboResponse は同一型。
//     フィールド追加時は両方に追加すること。
```

- **`web/CLAUDE.md` §2 が置くよう指定している文面は**「フィールド追加時は該当するすべてに追加すること」である。現物は「**両方に**」＝ 2 つを指す語のまま。
- **実態は 3 interface**（`ComboSummary` / `ComboDetail` / `Combo`）で、本サブは正しく「`ComboSummary` と `Combo` の 2 つ（`ComboDetail` は継承）」へ追加している。**判断は正しいが、コメントを字句どおり読むと「2 つに足せばよい」と読め、継承関係を知らない後任が `Combo` を落とす**——`web/CLAUDE.md` §2 が「`Combo`（編集用）への追加を忘れると、コンボ編集画面で型エラーまたは表示崩れが発生する」と名指しで警告している事故そのもの。
- **既存の記述であり本サブが導入したものではない**が、本サブは当該ファイルにフィールドを足しており、規約コメントの正しさが問われる位置にある。
- **修正**: 冒頭コメントを `web/CLAUDE.md` §2 の指定文面へ揃え、`ComboDetail` が `ComboSummary` を継承する旨を 1 行足す。

### M-2（中）完了報告 §14 がレビュー実施前に「レビューの結果」を断定している

- §14 末尾に「**★優先度「高」の指摘を不採用にした件は無い**（Phase C のトリアージ結果は `docs/progress/m23-01-review.md` 末尾）」とある。
- 完了報告のコミット（`25d97c7`）時点で `docs/progress/m23-01-review.md` は存在しない（本書が初出）。**レビューも取り込みも実施前の断定であり、記述としては空手形である。**
- 害は H-1 より小さいが、**「完了報告に書いてあるからレビューは通った」と読める形**であり、後段の取り込み判断で確認を省略させうる。
- **修正**: 当該行を削除するか、「（Phase C 実施後に追記）」のプレースホルダに改める。取り込み完了後に事実で埋めること。

### M-3（中）新規 E2E spec に後片付けが無く、後継の現役コンボが共有 E2E DB に残る

- `web/e2e/m23-01-trash-superseded.spec.ts` の 1 本目は `POST` → `PUT`（キー変更）を行い、**採番し直された後継コンボ（`isDraft: true`・`character_id = 1`）を削除せずに残す**。2 本目も削除済み行を 1 件残す。`afterEach` / `afterAll` での片付けは無い。
- 指示書 §5 の注記は「**件数を絶対値で数える既存 spec と衝突しないことを確認すること**」を求めており、完了報告は「**ゴミ箱の**件数を絶対値で数える spec は 0 件」と答えている。**答えているのはゴミ箱の側だけで、通常一覧に増える現役コンボの側は確認範囲に入っていない。**
- **レビュー側で `web/e2e/*.spec.ts` の `toHaveCount(n)` / `tbody tr` 系を全走査し、コンボ一覧の行数を絶対値で数える spec が 0 件であることを確認した。⇒ 現時点で実害は無い。**
- ただし `make e2e` は使い捨て DB とはいえ **1 回の走行内では全 spec が同一 DB を共有する**ため、今後「一覧の件数を数える spec」が足された時点で、実行順に依存した不安定さが入る。既存の後片付け慣行（`m22-03` / `m22-04` が `DELETE /api/combos/:id/permanent` を後片付けに使っている）にも揃っていない。
- **修正**: `afterAll` で作成した id を `permanent` で落とすか、少なくとも spec 冒頭に「後継行を残す」ことと、その根拠（件数を数える spec が現時点で 0 件であること）をコメントで明記する。

### L-1（低）`include_deleted=true` の経路では旧行が依然として可視である

- 述語を足したのは `OnlyDeleted` 分岐のみで、`GET /api/combos?include_deleted=true` は `deleted_at` の絞り込み自体を持たないため、**`PUT` が積んだ旧行も返る**。
- **指示書 §4.3-1 の指定範囲（`OnlyDeleted` の分岐へ足す）どおりであり違反ではない**。通常一覧側へ足さない判断も設計伝達レポート §1-3 に理由付きで書かれている。
- ただし完了報告 §10-4 の as-built 表には `include_deleted` の欄が「`only_deleted` が優先される」としか書かれておらず、**`include_deleted=true` 単独指定時に旧行が出ることは書かれていない**。`CHANGE-121` §2 へ写す際に落ちる。
- **対応**: 完了報告 §10-4 の絞り込み欄へ 1 行足す（`include_deleted=true` 単独時は旧行も返る）。実装変更は不要。

### L-2（低）`TrashListRow.test.tsx:70` のアサーションが無効化されている（既存・本サブ範囲外）

```ts
expect(link.closest("td")?.onclick).toBeDefined;
```

- `toBeDefined` が**呼び出されていない**ため、この行は常に成功する（何も検証していない）。
- 本サブの差分外のファイルだが、ゴミ箱の行コンポーネントを触った作業の直近であり、`M23-06`（ゴミ箱の利用者体験）が同ファイルを触る前に直すのが安い。

### L-3（低）コード内の `DES-003 §3.4` 参照が前方参照になっている

- `internal/model/combo.go` / `internal/api/combo/dto.go` / `migrations/000078_*.up.sql` がいずれも「`DES-003` §3.4: dup / recipe_hash の非対象」を根拠として引くが、**同節にはまだ `superseded_by_combo_id` の記述が無い**（設計卓が `CHANGE-121` で追記予定）。
- 反映されるまでの間、コメントを辿った担当が「§3.4 に無い」と混乱しうる。**`CHANGE-121` 反映後に自然解消する種類のもの**であり、実装の修正は不要。設計伝達レポート §4-7 に反映項目として載っているので追跡はできている。

### L-4（低）印を持つ旧行は API を直接叩けば現役へ復元できる

- `POST /api/combos/:id/restore` は `deleted_at` のみを NULL に戻し、`superseded_by_combo_id` に触れない。**印を持ったまま現役へ戻った行は、後継と同じキーを持つ現役コンボが 2 行並ぶ状態を作りうる。**
- **指示書 §2.2-3 が「復元の契約を変えない」を固定しており、本サブでの対処は正しく見送られている。** 完了報告 §9-2 に「判断は `M23-04`」として送られており、`TestRepository_Restore_LeavesSupersededMarkUntouched` が現状挙動を固定している。**指摘としては「記録済みであることの確認」にとどまる。**

---

## 推奨修正（優先度別）

### 高（M23 完了前に修正必須）

1. **H-1** `docs/progress/progress-log.md` へ M23-01 の索引行を追記する（日付・作業 ID・結果・報告書リンク＋横断課題 2 件＝ボード §2.2 のマイグレ連番のズレ／`CHANGE-121` の射程外の `DES-003` §5 残骸）。**あわせて完了報告 §14 の「（Phase D で追記済み）」を実態に合わせる。**
2. **H-2** `internal/api/combo/dto.go:194` の godoc を実態へ揃える（`PATCH/PUT 200` → `PATCH 200、PUT 201`）。1 行。

### 中（M24 着手と並行可）

3. **M-1** `web/src/features/combo/types.ts` 冒頭の規約コメントを `web/CLAUDE.md` §2 の指定文面（「該当するすべてに追加すること」）へ揃え、`ComboDetail` の継承関係を 1 行明記する。
4. **M-2** 完了報告 §14 の「優先度『高』の指摘を不採用にした件は無い」を、取り込み実施後の事実で埋めるかプレースホルダへ改める。
5. **M-3** `web/e2e/m23-01-trash-superseded.spec.ts` に後片付け（`afterAll` での `permanent`）を足すか、後継行を残す旨と衝突しない根拠を spec 内へ明記する。

### 低（将来対応）

6. **L-1** 完了報告 §10-4 の as-built へ「`include_deleted=true` 単独時は旧行も返る」を 1 行追記（`CHANGE-121` §2 へ写す材料の欠落を防ぐ）。
7. **L-2** `TrashListRow.test.tsx:70` の `toBeDefined` を `toBeDefined()` へ（`M23-06` に同梱でよい）。
8. **L-3** `CHANGE-121` 反映後に、コード内 `DES-003 §3.4` 参照が実在の記述を指すことを確認する（設計卓の反映作業の一部）。
9. **L-4** 印を持つ行の復元の扱いは `M23-04` の判断へ（本サブでは対処しない、で正しい）。

> **設計卓の手番として設計伝達レポートが挙げている 2 件は、レビュー側でも実物で裏取りした。**
> - **ボード §2.2 のズレ**: `docs/process/parallel-board.md` §2.2 の「次に払い出す番号」＝`000078`・「disk 末尾」＝`000077` はいずれも 2026-08-14 の値で失効。**§2.1 は `000079` へ更新済みであり、同じ数字の写し先 2 か所のうち片方だけが直っている**（`E-114` の同型）。実査値は disk 末尾 `000078` ／ 次は `000079`。**製造は `parallel-board` を編集できないため、設計卓が §2.2 を戻すこと。**
> - **`DES-003:946`**: 「ゴミ箱に90日以上保持されたレコード（設定で変更可能）」が現存。`CHANGE-121` の起票文は `DES-003` **§3.4** しか挙げておらず射程外。**`DES-005` §5.15 だけを撤回すると生き残る。**

---

## 良かった点

1. **★§3.3-7（実測に委ねられた 1 件）の扱いが模範的である。** 「列が無いと測れない」ことを認識してマイグレ適用後に専用テストを新設し、**FK=ON / FK=OFF の 2 系で測って結果が割れることを実測で示し、両系ともデータは壊れないことまで確認したうえで、テストに実測値を固定した**（`TestRun_M2301_SuccessorHardDelete_FKOn` / `_FKOff`）。**「効かないこと」を赤にするためのテストではなく、実測値を記録に固定するためのテストであると明記している**のも正しい（チェックリスト §0.3 N-5 の趣旨どおり）。実装は変えず、判断は設計卓へ返している。
2. **★§4.9 の 3 系統走査が本当に 3 系統やられており、指示書も `CHANGE-121` も知らなかった残骸を掘り当てた。** `DES-003` §5 の「90日以上保持（設定で変更可能）」は、本番コードだけを見ていれば見つからず、**`DES-005` §5.15 だけを撤回した場合に生き残って次の担当の前提になる**性質のもの。指示書の走査キーワード指定（「`90` と保持・期限が同居する行」）が効いた場面であることまで自己分析されている。
3. **★「次の番号」の写し先を 4 か所と決め打たず実査した。** M23 に契約ファイル（`docs/process/m23-contract.md`）が存在しないため実在は 3 か所である、という結論を根拠付きで出している。**「4 と決め打つと 1 か所見つからないで止まる」という指摘は、次のサブにそのまま効く。**
4. **★ボード §2.2 のマイグレ連番のズレを、自分の作業の外側で検出して報告した。** 「同じ数字の写し先が 2 か所あり片方だけ直っている」という `E-114` の型を正しく同定し、**製造が触れない共有直列リソースであることを理由に手を出さず、設計卓へ返している**——境界の引き方が正確。
5. **★テスト設計が「対で見る」を徹底している。** Go 側・Vitest・E2E のすべてで「隠れること」と「隠れすぎていないこと」を同じテスト／同じ spec の中で確認している。とくに `TestSuperseded_ManualDelete_StaysVisible` は、失敗メッセージが「ゴミ箱から消えてしまう」と害の側を書いており、後任が意図を読み違えない。
6. **★SELECT 5 か所への追加漏れを、`scanCombo` 共通化という構造から先回りして潰している。** 「1 か所でも漏らすと全経路の scan がずれる」という認識のもと、round-trip テストで既存列（`hit_type`）の健全性まで見る形にしたのは、症状（列ずれ）が出る場所と原因の場所が離れる不具合への正しい備え。
7. **★`E-125` への対応が形式的でない。** テスト結果を件数で書いただけでなく、**flaky 1 件を隠さず記載し、本サブ起因でないと判断した根拠を 2 点（単独走の結果・失敗箇所が本サブの経路を通らないこと）挙げたうえで「flake と断定はしない」と留保**している。都合の悪い観測を丸めない態度が守られている。
8. **★as-built の粒度が高い。** 完了報告 §10 は 4 経路それぞれの異常応答コードまで列挙しており、`count` が `len(items)` である・`character_id` が画面側で 1 固定である等、**表に載せないと必ず失われる細部**を拾っている。設計卓が `CHANGE-121` §2 へそのまま写せる。
9. **★推測で進めた 3 件が、指示書 §9.2 の許可範囲に正確に収まっている。** DTO 判断はコード内コメントに `// 推測: 〜と仮定した` の形で残され、残り 2 件（ファイル名・跡地レイアウト）は報告に理由付きで置かれている。§9.1 の「推測してはいけない 4 件」への抵触が 0 件であることも項目ごとに確認されている。

---

## 制約事項

- 本レビューはコード上で判定可能な範囲のみ対象。実際の動作確認・パフォーマンス・実機テストは別途実施が必要。
- レビュー側で再実行したのは Go の該当テスト 12 本（`TestSuperseded*` 6 ／ `TestRepository_*Superseded*` 3 ／ `TestRun_M2301_*` 3）・`go build ./...`・`gofmt -l internal/`・`tsc --noEmit`・機械検査 6 本（`check-artifact-integrity` / `check-doc-refs` / `check-md-emphasis` / `check-enum-sync` / `check-browser-storage-keys` / `check-stop-discipline` ／ 参考として `check-doc-inventory` / `check-progress-log-index`）である。**`go test ./...` 全数・`pnpm test` 全数・`make e2e` は本レビューでは再走していない**——完了報告の転記内容（スイート名・件数）を一次証跡として扱った。
- **`make e2e` を再走していないため、新規 spec 2 本の green はレビュー側で独立に確認していない**（完了報告の転記による）。M-3 で指摘した spec 間の状態共有の影響も、静的走査（既存 spec に絶対件数アサーションが無いこと）までの確認である。
- **不明: `PUT` を短時間に連続実行したときの `datetime('now')` 秒精度に起因する挙動**は、本サブが `updated_at` / `deleted_at` の規則に触れていないため対象外とし、判断していない。
- **不明: 実 DB（開発者手元の dev DB）に既に積まれている旧行の実件数**は判断できない。`M23-RESEARCH-01` 横断課題 3 のとおり、クラウド実行環境に dev DB が構造的に存在しない。§4.1-5 の非対称（適用前の旧行は隠れない）が利用者にどの程度見えるかは、開発者の手元でのみ測れる。

---

*以上、M23-01 レビュー報告。最重要ゲート（手動で消した行がこれまでどおりゴミ箱に出る）は満たされており §9 重大ゼロ。**「高」2 件——`progress-log` への索引行の欠落（かつ完了報告がそれを「済み」と記載）と、`ComboResponse` godoc の失効した `PUT 200`——を M23 完了前に是正すること。***

---

## 取り込み結果（自動トリアージ）

`/implement_plan_full` Phase C。**判断は製造担当（自動）。「高」の指摘を不採用にしたものは 1 件も無く、Phase C 安全弁のエスカレーションは発生していない。**

| 指摘 | 採否 | 理由 / 加えた変更 |
|---|---|---|
| **H-1** `progress-log.md` に索引行が無い（完了報告 §14 は「追記済み」と書いていた） | **採用** | **指摘が正しい。** Phase D として索引行を追記した（結果 1 行・報告リンク・**横断課題 6 件**）。**あわせて完了報告 §14 の「（Phase D で追記済み）」という空手形を、実際に追記した後の事実へ書き直した。** `check-progress-log-index.sh` が false-pass する穴を指摘した点も含め、指摘の精度が高い。 |
| **H-2** `dto.go:194` の godoc が `PATCH/PUT 200` のまま | **採用** | **指摘が正しい。** `(POST 201、GET 200、PATCH 200、PUT 201)` へ是正し、**`PUT` が 201 を返す理由（旧行を論理削除して新 id で採番し直すため＝`D-413`）を godoc に添えた。** 既存の誤りではあるが、**as-built を揃えることを目的としたサブが、同じファイルを編集しながら失効記述を残すのは筋が通らない**という指摘に同意する。 |
| **M-1** `types.ts` 冒頭の規約コメントが「両方に」のまま | **採用** | `web/CLAUDE.md` §2 の指定文面（「該当するすべてに追加すること」）へ揃え、**`ComboDetail` が `ComboSummary` を継承すること**と、**2 つと覚えないこと**を明記した。本サブが同ファイルにフィールドを足しており、規約コメントの正しさが問われる位置にあるという指摘に同意。 |
| **M-2** 完了報告 §14 がレビュー前に結果を断定 | **採用** | §14 を実施後の事実へ書き直し、トリアージの全結果は本節を参照する形にした。 |
| **M-3** 新規 E2E に後片付けが無い | **採用** | `test.afterEach` で作成行を論理削除 → 完全削除する形にした。**`PUT` が採番し直した後継 id も対象に含めている**（これを落とさないと現役コンボが 1 行残る）。**冒頭コメントの根拠も「ゴミ箱の件数を数える spec が 0 件」から「一覧の行数を数える spec も 0 件だが、後片付けまでやって初めて将来足されても壊れないと言える」へ書き直した。** 後片付け追加後に spec を再実行し `2 passed (9.7s)` を確認。 |
| **L-1** `include_deleted=true` 単独時の as-built が欠落 | **採用** | 完了報告 §10-4 の as-built 表へ 1 行追加（`include_deleted=true` 単独では `deleted_at` の絞り込み自体が掛からず旧行も返る）。**実装は変更していない**（指示書 §4.3-1 の指定範囲は `OnlyDeleted` の分岐のみ）。 |
| **L-2** `TrashListRow.test.tsx:70` のアサーションが無効（`toBeDefined` が未呼び出し） | **不採用（見送り・followup へ）** | **本サブの差分外のファイルであり、かつ「1 行で直る」指摘ではない。** `toBeDefined()` を呼ぶだけでは実質的に直らない——**`element.onclick` は未設定時 `null` であり、`null !== undefined` なので通ってしまう。**「直したように見えて何も検証しない」状態が残り、かえって悪い。意図（`tr` の `onClick` が `stopPropagation` で発火しないこと）を実際に検証するには `useNavigate` のモック化が必要で、テストの作り替えになる。**⇒ 同ファイル・同コンポーネントを扱う `M23-06` へ回す**（設計伝達レポート §4-6 の followup 候補へ登録した）。**★スコープを広げないための見送りであり、指摘そのものは妥当と認める。** |
| **L-3** コード内の `DES-003` §3.4 が前方参照 | **不採用（対応不要）** | **`CHANGE-121` の反映で自然解消する。** レビュー自身が「実装の修正は不要」としており、設計伝達レポート §4-7 に反映項目として載っているため追跡もできている。 |
| **L-4** 印を持つ旧行は API 直叩きで現役へ復元できる | **不採用（対応不要）** | **指示書 §2.2-3 が「復元の契約を変えない」を固定**しており、判断は `M23-04`（復元時のバリデーション）。`TestRepository_Restore_LeavesSupersededMarkUntouched` が現状挙動を固定済み。レビュー自身が「記録済みであることの確認にとどまる」としている。 |

### 取り込み後の再検証

| コマンド | 結果 |
|---|---|
| `go build ./...` ／ `gofmt -l internal/` | 成功 ／ 0 件 |
| `go test ./internal/api/combo/` | `ok github.com/plexiblinp/combomgr/internal/api/combo 1.135s` |
| `pnpm vitest run src/features/combo` | `Test Files 49 passed (49)` ／ `Tests 536 passed (536)` |
| `pnpm exec tsc --noEmit -p e2e/tsconfig.json` | エラーなし |
| `playwright test e2e/m23-01-trash-superseded.spec.ts` | `2 passed (9.7s)` |
| `bash scripts/check-progress-log-index.sh` | `OK 検査した 45 件すべてが progress-log に現れる` ／ 違反なし |
| `bash scripts/check-md-emphasis.sh` | ベースラインどおり（増加なし） |

**往復回数: 1 回（初回レビューのみ）。** 再レビューは要していない（`CLAUDE.md` §9 の上限 2 回に対して 1 回で収束）。**未解消のまま停止した項目は無いため、`followup-backlog.md` §J への記録は発生していない。**
