# M26-03 完了報告: 法務ポスチャの再点検とデータ来歴の切り分け（`A4` ＋ `A3`）

| 項目 | 内容 |
|------|------|
| 対象指示書 | `docs/instructions/M26-03-legal-posture-recheck.md` **v1.1.0** |
| チェックリスト | `docs/instructions/reviews/M26-03-review-checklist.md` v1.0.0 |
| CHANGE | **消費 0 本**（指示書ヘッダの見込みどおり。**三層のパス割当は変わらなかった**＝§5.3） |
| 消費マイグレ | **0 本**（次に払い出す番号は変わらない） |
| 着手基点 | `2e35bbdb1c44b645b4dbed6517d6f667391df76a` |
| 実施日 | **2026-09-11** |
| 実装差分 | **2 ファイル**〔`README.txt` ／ `NOTICE`〕**のみ。18 insertions / 1 deletion**（§6） |
| レビュー | `docs/progress/m26-03-review.md`（結果は §10） |
| **★追走**（2026-09-11） | **`D-819` ／ `D-820` ／ `D-821` を受け、§7 を `SUPP-001` §5.5.9 の原稿へ仕立て直した**（§7.A。**3 つの裁定が何を決めたかは §7 冒頭の表**）。**★本追走の時点で手元の指示書は `v1.1.0`、チェックリストは `v1.0.0` である**——**設計卓が発行した `v1.3.0` / `v1.2.0` は設計卓ブランチ `claude/design-desk-d697-log-shj48l` 上に在り、`CLAUDE.md` §7 により製造は `fetch` できない。⇒ 本追走はリレー文の指示本文を指示として扱った**（リレー文が但し書きでそれを許している） |

> **★判定の基準時点について。** 指示書 §0.2 は「2026-09-10 時点」と書くよう求めているが、**実査日は 2026-09-11**（対象 commit `2e35bbd`）である。
> **⇒ 本書は実査日を書く。** そのうえで **`M14` 完了報告の日（2026-09-10）から `character_data/` に変更が無いこと**を実測で添える——
> 31 本の CSV の最終コミット日は `ryu.csv` が 2026-09-10、`terry.csv` が 2026-09-09、残り 29 本が 2026-09-06 以前である。
> **⇒ 2026-09-10 時点と 2026-09-11 時点の内容は同一である。**
>
> **★「これで確定」とは書かない。** 開発者の逐語は「`custom states` への追加などこまごまとしたモノ、ひょっとしたら追加の修正等が入る可能性はゼロではないが、いったん完了」であり、
> **`D-811` はリリース数週間後に大きめの SF6 アップデートが来ることを初出情報として記録している。⇒ 本書の判定は、次の波で必ず古くなる。**

---

## 0. 要点（先に読む 6 行）

1. **`A4` の 2 本の決定文書は、リポジトリ内から判定できる範囲では前提が崩れていない。** ただし **robots / EULA の「現在値」はリポジトリからは判定できない**。⇒ 外部照会のプロンプトを 1 本用意した（§1.4）。
2. **`A3` の切り分けは 3 階層で起こした。** 母集団 **2954 件**（許可リストの実出力）→ 混在領域だけファイル単位 → CSV は **24 列**単位。
3. **★3 値に収まらないものが 2 群あった。** `LICENSES/**` 3 件と `web/src/components/ui/` 21 件は、**第三者由来であって「カプコン由来」でも「手入力 seed」でも「本体のコード・文書」でもない**（§2.3）。
4. **★層 B に入れるべきものが層 A へ落ちている箇所は、`migrations/` には無かった**（機械検査が緑）。**一方、逆向きの問題が 2 件ある**（§5）。
5. **★最大の発見＝`docs/seed-data/` を除外した理由（公式の生フレーム値を含む）と同じ内容が、公開される `docs/progress/` の 2 ファイルに在る**（§4.2）。**⇒ `D-742` の除外は「場所」で切っており、「内容」では切れていない。**
6. **窓口導線 2 本を差し替えた。** アドレスは伏せ字、返答の期限は約束しない、DMCA は案内しない（§6）。

---

## 1. 段 1（`A4`）— 法務ポスチャの前提の再点検

### 1.1 ★本節の限界を先に書く

**2 本の決定文書はどちらも「外部の一次情報を読んで下した判断」の記録である。⇒ その前提が今も成立しているかは、外部を読まないと分からない。**
指示書 §2.1 は **外部へ投げてよい問いを 1 本に限っている**。**⇒ 本節がリポジトリ内から言えるのは次の 2 つだけである。**

- **(a) 決定文書が「前提」として何を挙げていたか**（出所つきで再掲できる）
- **(b) その前提から導かれた実務ルールが、いまの配布物で守られているか**（＝当てはめ。実測できる）

**★(c)「前提そのものが変わったか」は判定できない。⇒ §1.4 のプロンプトで開発者が確かめる。**

### 1.2 決定文書が挙げていた前提（出所つき）

| # | 前提 | 出所 | 判断日 |
|---|---|---|---|
| 1 | **公式サイトの利用規約**——文字情報を含む各種素材の知的財産権を同社が保有し、法律で許された範囲を除き無断の複製・翻案・改変・頒布・公衆送信を禁止する | `combmgr-framedata-legal-decision.html:138-143` | **★記載なし**（下記 1.3-A） |
| 2 | **公式フレームデータの HTML は配布禁止** | `docs/instructions/M14-RESEARCH-01-distribution-fix-schema-cleanup-survey.md:28` の逐語＝「開発者確定 2026-06-28」 | **2026-06-28** |
| 3 | **`cid.capcom.com` は robots.txt レベルで自動アクセスを拒否**（実際に自動取得を試みてブロックされた） | `combmgr-battlelog-source-decision.md:27` | **★記載なし**（下記 1.3-A） |
| 4 | **ゲーム EULA**——通信プロトコル／データの解析と他用途への使用、複製・改変・解析・リバースエンジニアリング、サービスの妨害を禁止 | `combmgr-battlelog-source-decision.md:28` | **★記載なし** |
| 5 | **CAPCOM ID 系の規約**——規約違反や「手段のいかんを問わずサービス運営を妨害した場合」、事前通知なく利用を停止できる | `combmgr-battlelog-source-decision.md:29` | **★記載なし** |
| 6 | **中核原則「事実は入れ、表現は持ち込まない」** | `combmgr-framedata-legal-decision.html:185-207`（**定義本体**）／ 短縮表記の明文化は `combmgr-license-strategy-outline.md:155` | **★記載なし** |

### 1.3 ★リポジトリ内から言える「前提の状態」

**(A) ★2 本の決定文書はどちらも判断日を持たない。**
`combmgr-framedata-legal-decision.html:119` のメタ行は `Tacpendium ／ ステータス: 決定 ／ 種別: 決定記録（DR）` のみで、**日付が無い**。
`combmgr-battlelog-source-decision.md:3` も `ステータス: 決定（取り込み方式）／一部要確認` のみである。
**⇒ 「前提が変わっていないか」を確かめる基準日が、文書自身からは決まらない。**
**★これは本サブで直せない**（開発者のファイルである＝§3-1）。**⇒ 申し送る**（§8-1）。

**(B) ★引用元 URL が保存されていない。**
`combmgr-framedata-legal-decision.html:140` は利用規約を **「逐語ではなく要約。原文は別途保管」** とし、**URL を書いていない**。
`battlelog-source` 側で URL に相当する具体名は `cid.capcom.com`（`:27`）**の 1 つだけ**である。
**⇒ 次に再点検する人は、どのページを見に行けばよいかを文書から復元できない。**

**(C) ★`battlelog-source` は自分で「未確認」を 3 件残している**（`:55-57`）。**⇒ 前提が「固まっていない」ことを文書自身が認めている。**
1. CAPCOM ID 利用規約の「禁止事項」条項の特定（第 21 条ではないことまでは判明済み）
2. SF6 オンラインサービスの個別規約に同種条項がないか
3. 公式エクスポート（CSV／公式 DL）の有無
**★ただし同書は「この条文確認は補強であって決定の前提ではない」と自ら書いている**（`:55`）。⇒ 未確認 3 件が残っていても、実務上の決定（自動スクレイプしない／手動保存パースに寄せる）は揺らがない。

**(D) ★`battlelog` 側の機能は、そもそもまだ配布物に無い。**
バトルログ取り込みは実装されておらず、**本サブが見ている配布物 2954 件に関係する記述は存在しない。⇒ `battlelog-source` の前提は、いま配布物のリスクにはなっていない**（将来機能の決定記録である）。

### 1.4 ★外部照会（指示書 §2.1 の「1 本だけ」）

**`/ask_external_ai` は、外部 AI へ貼るプロンプトを 1 本生成するコマンドであって、外部へ問い合わせるコマンドではない**（`.claude/commands/ask_external_ai.md:5-11` ／ 出力節）。
**⇒ 製造は外部へ 1 バイトも送っていない。** 下記を開発者が Perplexity か Microsoft Copilot へ貼る。

```
あなたはゲーム関連の利用規約・robots.txt・EULA の調査を専門とするシニアリーガルリサーチャーです。
最新かつ正確な情報で、今日（2026-09-11）時点で答えてください。

# 質問
格闘ゲームのフレームデータを扱う個人の非営利ツールの作者として、次の4点の「現在の状態」を知りたい。

1. カプコン公式サイト（capcom.co.jp およびストリートファイター6 の公式サイト）の利用規約は、
   サイト掲載の文字情報・素材の無断複製／頒布／公衆送信を禁止しているか。
   直近1年以内に、その条項に実質的な変更があったか。
2. カプコンのプレイヤー向けID系サイト（CAPCOM ID / Buckler's Boot Camp）の robots.txt は、
   現在、一般のクローラーの自動アクセスを拒否しているか。直近1年以内に変更があったか。
3. ストリートファイター6 のゲーム内 EULA（エンドユーザー使用許諾契約）は、
   通信プロトコルやゲームデータの解析・抽出を禁止しているか。直近1年以内に変更があったか。
4. カプコンが、フレームデータ等のゲーム内数値データについて、
   公式のエクスポート機能や公式のデータ配布（CSV・API 等）を提供しているか。

# 前提
- 質問者は個人開発者であり、法律の専門家ではない。法的助言を求めているのではなく、
  「規約や robots.txt の現在の記載がどうなっているか」という事実を知りたい。
- ツール側は既に「自動クロールはしない／公式データは配布しない／
  作者が実機で観測した数値のみを同梱する」という方針で運用している。
  その方針の前提が今も成立しているかを確かめたい。

# 回答の条件
- 最新の情報に基づくこと。いつ時点の情報かを明記すること。
- 主張には必ず情報源（URL）と、その情報源の日付を添えること。
- 分からないこと・確認できなかったことは、埋めずに「不明」と明記すること。
- 「変更があったか」が確認できない場合は「変更の有無は不明」と書くこと（推測しない）。
- 日本語で回答すること（英語の情報源を参照してよい）。
- 簡潔に、4つの質問ごとに箇条書きで。
```

**★製造は法務の判断をしない**（§3-4 ／ 束 C-5）。**⇒ 以下は開発者が外部 AI から受け取った回答の記録であり、評価ではない。**

#### 1.4.1 ★外部照会の回答（**開発者が実施・調査基準日 2026-09-11**）

**★★結論を先に書く。⇒ 「現在値」は一部だけ確認できた。「直近 1 年で変わったか」は 4 件すべて不明のままである。**

| 前提 | 現在値 | 変化の有無 | 出所 |
|---|---|---|---|
| **1. 利用規約**（`capcom.co.jp` ／ SF6 公式） | **★不明。** 共通規約ページが **403 Forbidden** で本文を取得できず、一次資料として照合できなかった | **不明** | `capcom.co.jp` ／ `capcom-games.com/ja-jp/site/`（確認日 2026-09-11） |
| **1'. 参考**（別サイト） | **同趣旨の禁止が現存する。** 文字情報を含む素材について、法令上認められた範囲を除き無断の複製・翻案・改変・頒布・公衆送信・転載等を拒否 | — | `mobile.capcom.com/site.html`（確認日 2026-09-11）**。★SF6 サイトへの適用を証明する資料ではない** | 
| **3. robots**（CAPCOM ID） | **★制限的であることが現在も確認できる。** `User-agent: *` に対し `Disallow: /`、そのうえで トップ・guide・support・policy・assets・sitemap 等だけを個別に `Allow` | **不明** | `cid.capcom.com/robots.txt`（2026-09-11 取得） |
| **3'. robots**（Buckler's Boot Camp） | **★不明。⇒ §1.4.2 の発見 1 を参照** | **不明** | `c.cid.capcom.com/info/servicelist/ja/`（確認日 2026-09-11） |
| **4. EULA** | **★禁止条項は現存する。** 第 5 条①＝解析・リバースエンジニアリング・コード抽出の禁止 ／ 第 5 条②＝**通信プロトコルその他のデータの解析と、それらの他用途への使用の禁止** | **不明**（ページ末尾は「2023 年 6 月 1 日制定」表示のまま。**★サイレント変更が無かったことの証明にはならない**） | `game.capcom.com/eula/denuvo/jpn.html`（確認日 2026-09-11） |
| **5. 公式エクスポート** | **★確認できない。** 公式に公開・文書化されたフレームデータの CSV / API / エクスポートは見当たらない。Buckler 内部の経路は**非公式・undocumented・非 REST** と第三者が明記 | — | `c.cid.capcom.com/info/servicelist/ja/`（確認日 2026-09-11）／ 非公式の参考＝`sfstats.app/buckler` |

#### 1.4.2 ★回答から出た新しい事実 2 件（**★決定文書が書いていない**）

| # | 事実 | 効き方 |
|---|---|---|
| **★★1** | **`combmgr-battlelog-source-decision.md:27` は「`cid.capcom.com` は robots.txt レベルで自動アクセスを拒否」を根拠に挙げているが、Buckler's Boot Camp は `www.streetfighter.com` 側の別ホストである。⇒ 決定文書が根拠にしたホストと、バトルログを実際に取りに行くホストが一致していない可能性がある** | **★実務判断は揺るがない**——同書 `:55` は「実務的な決定は robots 拒否 ＋ EULA ＋ 運営妨害条項で既に成立しており、条文確認は補強であって決定の前提ではない」と自ら書いている。**⇒ 根拠が 1 本弱くなるだけで、結論（自動スクレイプしない・手動保存パースへ寄せる）は変わらない。★ただし「根拠として挙げたホストが違う」ことは記録に残す価値がある** |
| **★2** | **`cid.capcom.com/robots.txt` に `Content-Usage: ai=n` と `DisallowAITraining: /` が記載されている** | **★決定文書には無い前提である。** 本アプリは AI 学習をしないため実害は無いが、**⇒ 「AI 学習拒否を明示している」という事実は、次に同じ再点検をする人が知っておくべき前提である** |

#### 1.4.3 ★`U-4` の状態

**★★部分的に閉じた。⇒ 完全には閉じていない。**

- **閉じた分**＝robots（CAPCOM ID）と EULA の**現在の記載**は確認できた。**⇒ 制限的な条項は現存する。**
- **★閉じていない分**＝**(a) 利用規約の本文**（403 で取得できず）／ **(b) 4 件すべての「直近 1 年で変わったか」**（過去版との差分を取れていない）／ **(c) Buckler 側の robots**。
- **★★「変わっていない」とは書けない。⇒ 検証可能な過去版との比較ができていないためである。** 本書は**現在値の確認できた範囲**だけを事実として記録する。

**★★製造の観測**（判断ではない）**——照会の結果は、決定文書の実務ルールを緩める材料を 1 件も含んでいない。⇒ 「自動クロールをしない」「Buckler 内部の非公開経路を使わない」「公式サイトからデータセットを複製・再配布しない」「実機で自ら観測した数値だけを扱う」という現行の保守的な前提を変える理由は、本照会では見つからなかった。★逆に強める材料**（robots の AI 学習拒否の明示）**が 1 件出た。**

### 1.5 ★「事実 in ／ 表現 out」の当てはめ（実測）

**★線そのものは引き直さない**（§4.5）。**当てはめだけを見る。**

| # | 線の項目 | 当てはめの実測 | 判定 |
|---|---|---|---|
| 1 | **公式の HTML を配らない** | 公開される `.html` は **31 本**。内訳は `docs/human-notes/future-notes` 12 ／ `docs/human-notes/go-react-learning-guide` 11 ／ その他 8 で、**すべて開発者・設計卓の自作資料と `web/index.html` ／ `docs/usermanual/` である。公式由来の HTML は 0 本** | **◎ 守られている** |
| 2 | **公式由来の検証 CSV を配らない** | 公開される `.csv` は **31 本**で、**全数が `character_data/` である**。`combomgr-importer/dist/*.csv` は追跡下に 0 件、実体もこの作業ツリーに存在しない（`.gitignore:110`） | **◎ 守られている** |
| 3 | **画像・アイコン・ロゴを持ち込まない** | `M26-01` 完了報告 §1 の実測＝**リポジトリ全域に画像 0 件**。本サブでも公開 2954 件に画像拡張子は現れなかった | **◎ 守られている** |
| 4 | **公式の説明文・分類の文言を持ち込まない** | `notes` 列は 2743 行中 **4 セル**のみで、内容は開発者の日本語注記である（§3.3 の実測）。`notes` が空なのは**法的リスク回避のための仕様**である旨が開発者回答として記録されている（`M14-RESEARCH-02-report.md` §H-4 ／ `official-data-edge-cases.md:30`） | **◎ 守られている** |
| 5 | **★公式ローカライズ名称の文字列を持ち込まない** | **★ここだけ当てはめが割れる。** `name_ja` 列は 2743 行 100% 充填で、**通常技は本アプリの表記ベース命名**〔`立ち弱P` 等〕**だが、固有技は公式の日本語技名そのものである**〔`鎖骨割り` ／ `波動拳` ／ `パワーウェイブ` 等〕 | **△ §1.6 で扱う** |
| 6 | **ページの選択・配列を持ち込まない** | `move_code` は本アプリの独自採番であり、**importer の機械採番とは別体系である**（`M14-RESEARCH-02-report.md:21` の逐語＝共通 15 / 67 件）。並び順も CSV 側の入力順である | **◎ 守られている** |

### 1.6 ★`name_ja` と「表現 out」の関係（**書いて返す。決めない**）

**★事実として、3 つの記述が同居している。**

1. `combmgr-framedata-legal-decision.html:200` は **「公式ローカライズ名称の文字列」を「持ち込まない（表現）」側に置いている**。
2. **同じ文書の `:206` は逃げ道を示している**——「技の識別子は**表記ベース**〔`5HP`・`立強P` 等〕**を志向する。中立性が上がる**」。**⇒ 通常技はこの方針どおりになっている。**
3. **`NOTICE:12-14` は固有技名を別の枠で受けている**——「ゲームタイトル名・キャラクター名・**技名**は各権利者の商標またはそれに準ずるものである」。**⇒ 商標表示として扱い、「表現の複製」としては扱っていない。**

**★★【2026-09-11 開発者の訂正】製造の当初の整理は不正確であった。⇒ 逐語＝「正確にはインゲームで表示されるものなので、アプリ独自の中立表記とは言えない。インゲームで出ている公然の事実であり、公式サイトの技表をパーサーで取ったものではないだけ。」**

**⇒ 分かれ目は「独自表記か公式表記か」ではない。★取得経路である。**

| | 当初の製造の整理（**誤り**） | **開発者の訂正**（正） |
|---|---|---|
| 固有技名 | 「公式のローカライズ名称そのもの」＝表現を持ち込んでいる | **インゲームで表示される公然の事実を、開発者が観測して手入力したもの。★公式サイトの技表をパーサーで取得したものではない** |
| 分ける軸 | 独自表記 ／ 公式表記 | **★観測か、転記か** |

**★★これはフレーム値とまったく同じ扱いである。** `moves-input-background.md:51` の逐語＝「**公式データを `character_data` へ転記しない。** 本作業は開発者が実機を見て手入力する方針である」。**⇒ `name_ja` だけが例外なのではなく、列をまたいで同じ原則が通っている。**

**⇒ `framedata-legal:200` の「公式ローカライズ名称の文字列」を持ち込まない、が禁じているのは公式の資料からの転記である。★インゲームの公然の表示を観測することは、その禁止の対象ではない**（開発者の整理）**。⇒ そのうえで固有技名は `NOTICE:12-14` の商標の枠で受ける。**

**★ただし、この整理がどこにも明文化されていない。⇒ `DATA-LICENSE.md` にも `NOTICE` にも「`name_ja` はインゲーム表示の観測であって公式資料からの転記ではない」とは書いていない。**
**★これは「線の当てはめ」ではなく「線の書き方」の問題であり、開発者の手番である**（§3-4）。⇒ §8-2 で申し送る。

**★製造は `name_ja` を 1 バイトも変えていない。**

---

## 2. 段 2（`A3`）— 配布物の来歴の切り分け

### 2.1 母集団の起こし方（**★束 A-3**）

**母集団は「公開されるもの」である。⇒ 許可リストの実出力から起こした。**

```
$ bash scripts/check-public-snapshot.sh --list | wc -l
2954
$ bash scripts/check-public-snapshot.sh
母数 2983 件 / 公開 2954 件 / 除外 29 件
結果: 違反なし
```

> **★`M26-02` 完了報告 §4.2 の貼り付けを母集団に使わないこと。** 同節は 2026-09-06 時点の写しであり、
> **その後 `docs/progress/20260906-squash-research/**` の DENY 1 行が足されている**（`public-snapshot-manifest.txt:27` ／ 開発者確定 2026-09-08）。
> **⇒ 除外は当時の 13〜14 件から 29 件へ、公開は 2706 件から 2954 件へ動いている。★正本は `scripts/public-snapshot-manifest.txt` と `--list` の実行結果である。**

### 2.2 階層 1 — 許可リストのグロブ単位（母数 2954 件・**全数**）

**判定は 3 値**〔**C**＝カプコン由来の事実 ／ **S**＝手入力 seed（開発者が作った）／ **A**＝本体のコード・文書〕。
**★3 値に収まらないものは `X` と書く**（§2.3）。

| # | グロブ | 件数 | 来歴 | 次の階層へ降りるか |
|---|---|---:|---|---|
| 1 | `docs/**` | **1482** | **A**（★2 ファイルに **C** が混在＝§4.2） | **★降りる** |
| 2 | `web/**` | **681** | **A**（★21 ファイルが **X**＝§2.3-2） | **★降りる** |
| 3 | `internal/**` | **423** | **A** | 降りない |
| 4 | `migrations/**` | **218** | **★混在**（A 64 ／ C＋S 154） | **★降りる**＝§2.4 |
| 5 | `.claude/**` | **42** | **A**（層 C） | 降りない |
| 6 | `scripts/**` | **35** | **A**（層 C） | 降りない |
| 7 | `character_data/**` | **35** | **★混在**（C＋S 34 ／ A 1） | **★降りる**＝§3 |
| 8 | `cmd/**` | **6** | **A** | 降りない |
| 9 | `LICENSES/**` | **3** | **★X**＝第三者のライセンス本文（§2.3-1） | — |
| 10 | `.codex/**` | **3** | **A**（層 C） | 降りない |
| 11 | `.github/**` | **2** | **A** | 降りない |
| 12 | `.agents/**` | **2** | **A**（層 C） | 降りない |
| 13 | `human-notes/**` | **1** | **A**（層 C） | 降りない |
| 14 | 単独ファイル 21 本〔`LICENSE` ／ `NOTICE` ／ `REUSE.toml` ／ `DATA-LICENSE.md` ／ `CONTRIBUTING.md` ／ `SECURITY.md` ／ `SUPPORT.md` ／ `README.md` ／ `README.txt` ／ `CLAUDE.md` ／ `AGENTS.md` ／ `Makefile` ／ `go.mod` ／ `go.sum` ／ `config.toml.example` ／ `embed_*.go` 3 本 ／ `.gitignore` ／ `.gitattributes` ／ `.editorconfig`〕 | **21** | **A**（★`LICENSE` 1 本は **X**＝FSF の AGPL 全文） | — |
| | **合計** | **2954** | | |

**★件数の突合**: 1482＋681＋423＋218＋42＋35＋35＋6＋3＋3＋2＋2＋1＋21 ＝ **2954**。`--list` の出力行数と一致する。

**★降りない 8 群（internal / .claude / scripts / cmd / .codex / .github / .agents / human-notes ＝ 514 件）を束で `A` と判定した根拠**:
いずれも Go / TypeScript / シェル / 運用文書であり、**SF6 の数値そのものを保持しない**。
数値を保持しうるのは (a) `character_data/`（正本）と (b) `migrations/` の投入 SQL の 2 系統だけである、と `DATA-LICENSE.md:58-66` が宣言しており、本サブの実測（§1.5-2＝公開 CSV は `character_data/` の 31 本だけ）とも合う。
**★例外が 1 つある**——`internal/seedgen/` は CSV を読んで SQL を書く生成器だが、**値を持たず、列の定義**（`model.go:22-34` の `csvColumns`）**だけを持つ。⇒ `A` で正しい。**

### 2.3 ★★3 値に収まらなかったもの（**束 A-1**）

**★指示書が与えた 3 値は「カプコンとの関係」を切る軸である。⇒ カプコン以外の第三者が作ったものは、3 値のどこにも入らない。**
**★埋めずに `X` として立てる。**

| # | 対象 | 件数 | 何者か | 現在の宣言 | 判定 |
|---|---|---:|---|---|---|
| **X-1** | `LICENSES/AGPL-3.0-or-later.txt` ／ `LICENSES/CC-BY-SA-4.0.txt` ／ `LICENSES/MIT.txt` ／ ルートの `LICENSE` | **4** | **FSF ／ Creative Commons が著作権を持つライセンス本文である**。本体のコードでも SF6 の事実でもない | **REUSE 仕様上 `LICENSES/` と `LICENSE` は Covered File ではない**（`REUSE.toml:20-21` の逐語）。⇒ **宣言の対象外であり、問題は無い** | **X。実害なし** |
| **X-2** | `web/src/components/ui/` の **21 ファイル** | **21** | **★shadcn/ui 由来のソースである。** `web/components.json` が `"$schema": "https://ui.shadcn.com/schema.json"` と宣言しており、shadcn/ui は**依存として入れるのではなくソースをコピーして使う**設計である。⇒ **`go.mod` にも `package.json` にも現れない** | **`REUSE.toml` の既定**（`path = "**"`）**により層 A＝`AGPL-3.0-or-later` ／ `SPDX-FileCopyrightText = "2026 plexiblinp"`。⇒ 第三者が書いたソースに、本体の著作権表示が付いている** | **★§5.2 で扱う** |

**★X-2 について製造が言えることと言えないこと。**
- **言えること**＝(a) 21 ファイルが shadcn/ui 由来であること（`components.json` の自己宣言 ＋ ファイル名が shadcn/ui のコンポーネント名と一致）、(b) **`NOTICE` §5 にも `CONTRIBUTING.md` にも `DATA-LICENSE.md` にも shadcn への言及が 1 件も無いこと**（`grep` 実測 0 件）、(c) `NOTICE` §5 の第三者ライブラリ一覧は `go.mod` / `package.json` を源泉としており、**コピーして持ち込んだソースは構造上そこに載らないこと**。
- **★言えないこと**＝**shadcn/ui の現在のライセンス条件がどうなっているか。⇒ 外部を読まないと確かめられず、本サブの外部照会 1 本は §1.4 に使った。★判定できない。**
- **★製造は「違反である」とは書かない。** shadcn/ui はコピーして使う前提で配られているものであり、**問題があるとすれば許諾ではなく帰属表示の側である。⇒ 事実だけを置き、判断は開発者へ返す**（§8-3）。

### 2.4 階層 2A — `migrations/` 218 件（**★束 A-4**）

```
$ bash scripts/check-migration-license.sh
凍結表: 層 A 30 本 / 層 B 72 本
結果: 違反なし
```

| 層 | 由来 | 件数 | 来歴 |
|---|---|---:|---|
| **A** | 凍結 | **60** | **A**＝スキーマ定義（`CREATE TABLE` / `ALTER TABLE`） |
| **A** | 新規 | **4** | **A**＝`000104_add_game_update_tracking` ／ `000105_add_combos_position_mass`（up/down 各 2） |
| **B** | 凍結 | **144** | **★C＋S の混在**（下記） |
| **B** | 新規 | **10** | **★C＋S の混在**＝`000106`〜`000110` |
| | **合計** | **218** | |

**★層 B の 154 件の来歴は「C か S か」では切れない。⇒ 切れない理由が構造にある。**
`DATA-LICENSE.md:63` の逐語＝「`migrations/` の層 B 分 … **上記から生成・投入したもの**。`cmd/seedgen` が CSV から生成した SQL、および人手で起こした是正 SQL」。
**⇒ 層 B のマイグレは `character_data/` の写しであり、来歴は写し元が持つ。★したがって層 B 154 件の来歴判定は §3 の列単位判定に還元される。⇒ ここで別に判定しない**（二重に判定すると食い違う）。

**★命名規約の当てはめで 2 件の観測。**

| # | 観測 | 事実 |
|---|---|---|
| 1 | **`_data_` グロブだけを数えると層 B を 6 組と誤認する** | 層 B 154 件のうち `migrations/*_data_*.sql` に当たるのは **12 件**（6 組）だけで、**残り 142 件は `REUSE.toml:95-118` の凍結列挙で層 B に解決している**。⇒ 命名規約は**新規分にしか効かない** |
| 2 | **`000063_correct_moves_data_m1904c` は `_data_` を意図せず含む** | 同ファイルの `data` は `moves_data` の一部であり、層 B 宣言のために付けたものではない。⇒ 凍結列挙（`REUSE.toml:106` の `migrations/000063_*.sql`）と `_data_` グロブの**両方で層 B に解決する**。結果は正しいが、**規約の当てはめとしては偶然である** |

**★層 B に入れるべきものが層 A へ落ちている例は `migrations/` には無い**（機械検査が緑）。
**★なお `migrations/000109_data_seed_drive_reversal_system_move.up.sql` のヘッダは、この穴を自分で文書化している**——逐語＝「`_data_` を落とすと REUSE の既定（層 A ＝ AGPL）へ静かに落ちる（`M28-05` が `000106` で踏んだ形）」。**⇒ 過去に 1 度踏んで、ヘッダのコメントで塞いである。**

---

## 3. 段 3 — `character_data/` の来歴を 1 ファイルずつ・**1 列ずつ**判定する

### 3.1 ファイル単位（**35 件・全数**。2026-09-11 実査）

| # | ファイル | 行数 | 最終変更 | REUSE の層 | 来歴 |
|---|---|---:|---|---|---|
| 1〜31 | **`*.csv` 31 本**（下表） | **データ 2743 行** | 2026-09-06〜09-10 | **B** | **★列ごとに違う＝§3.3** |
| 32 | `chain-cancel-measurements.md` | 283 | 2026-09-05 | **B** | **S**＝開発者がインゲームのフレームメーターを読んだ実測記録。**公式に対応する値が存在しない**（`M19-OPS-01-chain-measurement-manual.md` §1 の逐語＝「値は規則から導出できず … **実測**でのみ確定する」） |
| 33 | `command-correction-history.md` | 1782 | 2026-09-09 | **B** | **★C と S の混在**。自称は「**公式データ**（FR701 / `combomgr-importer/dist/`）**との突合で command を補完した全件記録**」（`:3`）。**「対応 dist code」欄は公式データ抜粋の `move_code` である**（`:7`）。**ただし同ファイルは「公式データのフレーム値・ダメージ値は転記しない」と自分で宣言し**（`:8`）**、実測でも転記は無い** |
| 34 | `dhalsim-joint-remeasurement-2026-09-04.md` | 43 | 2026-09-05 | **B** | **S**＝開発者との共同再計測の生読み記録 |
| 35 | `seed-progress.md` | 333 | 2026-09-05 | **★A**（`REUSE.toml:124-128` で層 A へ差し戻し済み） | **A**＝seed 投入の手順書であってゲームデータではない |

**★`M26-01` 完了報告は同ディレクトリを「34 件」と数えていた**（層 B 33 ＋ 層 A 1）。**⇒ 実測は 35 件である。**
差の 1 件は `dhalsim-joint-remeasurement-2026-09-04.md` で、**`M26-02` 完了報告 `:54` に「指示書の列挙から漏れていたが、グロブ `character_data/**` で自動的に層 B に入っている」と既に記録がある。⇒ 宣言に穴は無い。件数の記述だけが古い**（§8-4）。

**CSV 31 本の内訳**（データ行数 ／ 最終変更日。**★すべて 2026-09-10 以前であり、指示書 §0.2 の「2026-09-10 時点」と本書の実査日 2026-09-11 で内容は同一である**）:

| ファイル | 行 | 最終変更 | ファイル | 行 | 最終変更 | ファイル | 行 | 最終変更 |
|---|---:|---|---|---:|---|---|---:|---|
| `aki` | 72 | 09-06 | `akuma` | 107 | 09-06 | `alex` | 98 | 09-06 |
| `blanka` | 118 | 09-06 | `c_viper` | 89 | 09-06 | `cammy` | 99 | 09-06 |
| `chun_li` | 87 | 09-06 | `dee_jay` | 84 | 09-06 | `dhalsim` | 97 | 09-06 |
| `e_honda` | 78 | 09-06 | `ed` | 75 | 09-06 | `elena` | 99 | 09-06 |
| `guile` | 88 | 09-06 | `ingrid` | 91 | 09-06 | `jamie` | 127 | 09-06 |
| `jp` | 75 | 09-06 | `juri` | 73 | 09-06 | `ken` | 80 | 09-06 |
| `kimberly` | 95 | 09-06 | `lily` | 81 | 09-06 | `luke` | 83 | 09-06 |
| `m_bison` | 79 | 09-06 | `mai` | 96 | 09-06 | `manon` | 72 | 09-06 |
| `marisa` | 108 | 09-06 | `rashid` | 99 | 09-06 | **`ryu`** | 84 | **09-10** |
| `sagat` | 77 | 09-06 | **`terry`** | 70 | **09-09** | `yasmine` | 84 | 09-06 |
| `zangief` | 78 | 09-06 | | | | **合計** | **2743** | |

**★CSV の種類は 1 種類だけである。** 31 本のヘッダは**バイト単位で同一**（`sort -u` の結果が 1 行）。列数は **24 列**で、正本は `internal/seedgen/model.go:22-34` の `csvColumns`。

### 3.2 ★ファイル単位の判定では足りない（**束 A-2 / 指示書 §4.3**）

**★31 本の CSV はどれも「1 ファイル＝1 来歴」ではない。⇒ 列で切らないと、層 B に入れるべきものが層 A へ落ちる。**
**★さらに本サブの実測では、1 つの列の中で行によって来歴が違う例が 2 件あった**（`name_ja` と `damage`）。**⇒ 列で切っても足りない箇所がある。**

### 3.3 ★列単位の判定（**24 列・全数**。母数 2743 行）

**「充填」は全 31 本を通した非空セル数の実測である。**

| # | 列 | 充填 | 率 | 来歴 | 根拠（逐語のあるもの） |
|---|---|---:|---:|---|---|
| 1 | `character_code` | 2743 | 100% | **A** | 本アプリの識別子。ファイル名と 1:1 |
| 2 | `move_code` | 2743 | 100% | **A** | **本アプリの独自採番である。** `M14-RESEARCH-02-report.md:21`＝「importer CSV の `move_code` は現行 ryu seed の正準採番と**体系が異なる**（共通 15 / 67 件）」 |
| 3 | `category` | 2743 | 100% | **A** | **本アプリの独自区分。** 同 §C-3-2＝手入力側の `target_combo` 7 / `rush_variant` 13 に対し公式側は **0** |
| 4 | **`name_ja`** | 2743 | 100% | **★行で割れる** | **通常技＝A**（`立ち弱P` 等の表記ベース命名。`framedata-legal:206` の方針どおり）／ **固有技＝C**（`鎖骨割り` ／ `波動拳` ／ `パワーウェイブ`＝公式の日本語技名）。★整形が入る場合がある（`04-notation-spec.md:324` の正規化規則 ／ `check-criteria.md:67` の表記統一） |
| 5 | `startup` | 2692 | 98.1% | **S** | **★公式の転記ではない。** `moves-input-background.md:51`＝「**公式データを `character_data` へ転記しない。** 本作業は開発者が実機を見て手入力する方針である」。`check-criteria.md:66`＝空中技・派生技の発生は**公式基準ではなく**派生元から最速キャンセル起点。「公式より系統的に大きいのは意図的」 |
| 6 | `active` | 2691 | 98.1% | **S** | 同上。`check-criteria.md:64`＝実測が公式と違う箇所は実測値へ修正済み |
| 7 | `recovery` | 2690 | 98.1% | **S** | 同上 |
| 8 | `total` | 2690 | 98.1% | **S** | 同上 |
| 9 | `on_hit` | 1197 | 43.6% | **S** | 同上 |
| 10 | `on_block` | 2094 | 76.3% | **S** | 同上（`check-criteria.md:64` にジュリ SA3 の実測差の例） |
| 11 | **`damage`** | 2740 | 99.9% | **★行で割れる** | **原則 S**（実測）。**ただし TC の `damage` は本アプリ独自の「累積」方式へ置換されている**——`target-combo-and-derived-flag-rules.md:112-113`＝公式方式は各段の単発値、**開発者方式は累積値**。⇒ **値の意味そのものが公式と違う行がある** |
| 12 | `is_aerial` | 2743 | 100% | **A** | `official-data-edge-cases.md:30`＝「ラッシュ版生成の判定用（空中技フラグではない・**手入力 CSV が正**）」＝本アプリ独自の意味 |
| 13 | `is_projectile` | 2743 | 100% | **A** | `M14-RESEARCH-02-report.md` §C-3-6＝「**importer に該当列なし**」 |
| 14 | `is_derived` | 2743 | 100% | **A** | `target-combo-and-derived-flag-rules.md:98`＝「**公式データ・手入力データから機械判別できない**」＝開発者の判断で付与 |
| 15 | `notes` | **4** | 0.1% | **S** | **★2 ファイル 4 セルのみ**（`terry` 2 ／ `zangief` 2）。内容は開発者の日本語注記。**空なのは法的リスク回避の仕様**（`M14-RESEARCH-02-report.md` §H-4） |
| 16 | `notes_tool` | 338 | 12.3% | **S** | **24 ファイルに存在。** 手入力ツール上で**開発者が技ごとに残した注記**（`precheck_seed_data.md:32`＝「公式差分OK」「〜で計測」等の宣言が行単位で書かれている） |
| 17 | `original_move_code` | 514 | 18.7% | **A** | ラッシュ版・派生版の親を指す本アプリ独自の参照 |
| 18 | **`command`** | 2185 | 79.7% | **★C（情報）＋ A（表記）** | **§3.4 で扱う** |
| 19 | `condition_ja` | **2** | 0.1% | **★判定できない** | **2 セルのみ**（`ingrid` 1 ／ `lily` 1）。**設計上の出どころは公式の備考欄である**（`precheck_seed_data.md:105`＝「公式備考欄由来の `condition_ja`」／ `official-data-edge-cases.md:220`）。**★ただし現存の 2 セル**〔`（体力25%以下で）` ／ `（ジャンプ中に）`〕**が公式の文言の写しか、開発者が書いたかは、公式 dist が無いため確かめられない**（§3.5） |
| 20 | `condition_en` | **2** | 0.1% | **★判定できない** | 同上〔`(when under 25% vitality)` ／ `(During a jump)`〕 |
| 21 | `startup_basis` | 2743 | 100% | **A** | セットプレイ探索のための本アプリ独自概念。公式に対応列なし |
| 22 | `chain_cancel_total` | **89** | 3.2% | **S** | **★値の正本は CSV ではない**（`precheck_seed_data.md:53` ／ ボード `D-99`）。正本は `character_data/chain-cancel-measurements.md`＝**開発者のフレームメーター実測**。公式に存在しない値 |
| 23 | `fastest_unreachable` | 2743 | 100% | **A** | 同じく本アプリ独自概念 |
| 24 | `last_changed_game_version` | **0** | 0.0% | **A**（★現時点で値なし） | **31 ファイル 2743 行すべて空。** `03-data-model.md:438`＝「**立てるのは配信者**（開発者）**である**」「NULL の意味は『まだ一度も変わっていない』という既知の状態であり、『不明』ではない」。**⇒ 次の SF6 アップデートで初めて値が入る列である** |

**★集計**: **A 9 列 ／ S 9 列 ／ 判定できない 2 列 ／ 行で割れる 2 列 ／ C（情報）＋A（表記）1 列 ／ 値なし 1 列**（合計 24）。
**★内訳を書き下す**——**A**＝`character_code` `move_code` `category` `is_aerial` `is_projectile` `is_derived` `original_move_code` `startup_basis` `fastest_unreachable` ／ **S**＝`startup` `active` `recovery` `total` `on_hit` `on_block` `notes` `notes_tool` `chain_cancel_total`。
**★初版は `A 10 / S 8` と書いていた**（2026-09-11・レビュー指摘 中-1）**。⇒ +1 と −1 が打ち消し、合計だけが 24 に着地していた。★検算では捕まらない形である。⇒ `S`**（開発者の実測）**と `A`**（本アプリ独自）**は法務上の意味が違うため、内訳を書き下す形へ変えた。**
**★純粋な「C＝カプコン由来の事実」だけの列は、24 列のうち 0 列である。**

### 3.4 ★★`command` 列——指示書 §4.3 の例と一次記録が食い違う（**開発者確認事項**）

**★指示書 §2.3-3 / §4.3 は、列ごとに来歴が違う例として次を挙げている。**

> **フレーム値は事実、`command` は開発者の補完**

**★リポジトリ内の一次記録は、この 2 つを逆向きに述べている。⇒ 実測と逐語を並べる。**

| | 指示書の例 | リポジトリ内の一次記録 |
|---|---|---|
| **フレーム値**（`startup` 等） | 「事実」＝カプコン由来と読める | **`moves-input-background.md:51`＝「公式データを `character_data` へ転記しない。開発者が実機を見て手入力する」。`check-criteria.md:64`＝公式値と実測が違う箇所は実測値へ修正済み**〔ザンギ立ち強P 22→24F 等〕**。`check-criteria.md:66`＝発生の基準が公式と違い「公式より系統的に大きいのは意図的」** |
| **`command`** | 「開発者の補完」＝開発者由来と読める | **`M14-RESEARCH-02-report.md` §C-3-4＝「`command` は公式と完全一致（索引源として成立する）… importer CSV と共通する 55 move すべてで文字列が完全一致（差分 0）」。`command-correction-history.md:3`＝「公式データ**（FR701 / `combomgr-importer/dist/`）**との突合で command を補完した全件記録」** |

**★製造の判定＝どちらも「補完」と「事実」の二分では捉えられない。⇒ 情報と表記を分けると整合する。**

- **`command` の情報内容はカプコン由来である。** 何を入力するとその技が出るかは、ゲームが決めている事実であり、値は公式 dist との突合で埋めた。
- **★`command` の表記は本アプリ側の設計である。** 実データは `d dr r plus p_h` ／ `p_l` ／ `charge_l r plus p_l` のようなトークン列であり、**これは公式サイトの表記ではなく、importer の「コマンドトークン語彙表」に従う正規化形である**（`M14-RESEARCH-02-report.md:44`＝「トークン語彙は `combomgr-importer/README.md`『コマンドトークン語彙表』を正とした」）。
- **⇒ `command` はまさに「事実 in ／ 表現 out」が効いている列である。** 情報は取り込み、公式の表現は持ち込んでいない。

**★フレーム値の側は逆で、情報そのものが開発者の実測である。⇒ 公式値と一致しない箇所が意図的に存在する。**

**★★したがって、指示書の例文をそのまま使うと来歴を取り違える。** 本書は**一次記録の逐語に従って判定した**。
**★指示書は編集しない**（製造の範囲外）。**⇒ 開発者へ返す**（§8-5）。**★どちらが正しいかは製造が決めない。**

### 3.5 ★判定できないもの（**束 A-1**）

**★「判定できない」を書けることが本サブの品質である**（指示書 §4.1）。**⇒ 埋めなかったものを全部挙げる。**

| # | 判定できないもの | なぜ判定できないか | 埋めるには何が要るか |
|---|---|---|---|
| **U-1** | `condition_ja` / `condition_en` の現存 2 セルが、公式の文言の写しか開発者の記述か | **公式 dist（`combomgr-importer/dist/`）がこの作業ツリーに存在しない。** `.gitignore:110` で除外された別リポジトリであり、追跡ファイルに 0 件、`find / -maxdepth 4 -type d -name combomgr-importer` も 0 件 | **importer を接続したセッションでの突合**（`precheck_seed_data.md:64-68` の参照元解決） |
| **U-2** | `name_ja` の固有技名のうち、どれが公式表記そのままで、どれが正規化後か | 同上。加えて `04-notation-spec.md:324` の正規化は**エイリアス側の規則**であり、`name_ja` 本体にどこまで適用されているかは記述が無い | 同上、または開発者の記憶 |
| **U-3** | **shadcn/ui のライセンス条件**（§2.3 X-2） | 外部の一次情報が要る。**本サブの外部照会 1 本は §1.4 に使った**（指示書 §2.1 は 1 本に限っている） | 開発者の確認、または `M26-04` / `M26-01` 系の手番 |
| **U-4** | **2 本の法務決定文書の「前提の現在値」**（robots / EULA / 利用規約）**。★★2026-09-11 に部分的に閉じた**（§1.4.1〜1.4.3）**。⇒ robots**（CAPCOM ID）**と EULA の現在の記載は確認でき、制限的な条項の現存が分かった** | **★残る不明は 3 つ**——**(a) 利用規約の本文**（共通規約ページが **403** で取得できず）／ **(b) 4 件すべての「直近 1 年で変わったか」**（検証可能な過去版との差分が取れない）／ **(c) Buckler's Boot Camp 側の robots**（`www.streetfighter.com` の別ホストであり未取得） | **(a)** 別経路での規約本文の取得 ／ **(b)** 過去版アーカイブとの差分 ／ **(c)** `www.streetfighter.com` の robots の取得 |
| **U-5** | **2 本の法務決定文書の判断日** | **文書自身が日付を持たない**（§1.3-A） | 開発者の記憶、またはボード側の記録との突合 |
| **U-6** | `command` 未充填 558 行（2743 − 2185）が「公式側も空」なのか「移植漏れ」なのか | 同 U-1（公式 dist 不在）。**なお `followup-backlog` の `seed-precheck-batch7-residuals` が「公式 dist 未接続（degrade）のため、公式突合を要する検査を実施できなかった」として 33 件を未着手で抱えている** | importer 接続 |

**★U-1 / U-2 / U-6 は同じ 1 つの原因に還元される**——**公式 dist がこの作業ツリーに無い。⇒ 本サブは「公式との突合」による裏取りができない。★これは本サブの手落ちではなく、`docs/seed-data/` を含む公式由来資料を配布物から外す方針の裏返しである。**

---

## 4. `docs/seed-data/` の除外の再判定（`D-742` ／ **束 E-1・E-2**）

### 4.1 除外理由は今も当てはまる（**除外を続ける**）

**除外理由の逐語**（`scripts/public-snapshot-manifest.txt:26`）＝「同ディレクトリの README が『OSS 公開版リポジトリには含めない方針』と自己宣言している（**公式の生フレーム値・元データ抜粋を含む**）」。

**★8 件の全数を再判定した**（2026-09-11 実査。**`D-742` の確定日 2026-09-06 以降、8 件のいずれも 1 バイトも変わっていない**——最終コミットは 2026-09-05 が 2 件、2026-09-02 が 6 件）。

| # | ファイル | 行 | 公式の生値を含むか | 判定 |
|---|---|---:|---|---|
| 1 | `input-notes.md` | 316 | **★含む**。`:31` ／ `:107-108` ／ `:121-122` ／ `:147` の **4 か所**。いずれも「実測値（公式 N）」の形で公式のフレーム値を並記している | **除外を続ける** |
| 2 | `check-criteria.md` | 180 | **★含む**。`:64`（**意図的差分の類型 (a)**。4 技分の「公式値→実測値」の対）／ `:71`（同 (h)。3 技分） | **除外を続ける** |
| 3 | `official-data-edge-cases.md` | 734 | **★含む**。§9.5 は公式値と手入力値を**表形式で並記**している（テリーのジャンプ通常技 2 技分） | **除外を続ける** |
| 4 | `target-combo-and-derived-flag-rules.md` | 194 | **★境界事例**。`:109` ／ `:112` に、TC 1 技分のダメージ値が「公式方式の値」として現れる。**★公式データ表の抜粋ではなく 1 技分の作業例であるが、「公式の値」として明示された数値である** | **除外を続ける**（下記 4.3） |
| 5 | `moves-input-background.md` | 359 | **含まない**。`:54` / `:359` で**自ら宣言している**——「この資料にも、公式由来の生フレーム値やダメージ値を保存しない」。`grep` 実測でも生値の記述は無い | ★単体では除外不要 |
| 6 | `M19-OPS-01-chain-measurement-manual.md` | 124 | **含まない**。`公式` の出現が **0 件**。内容は開発者のフレームメーター実測の手順書 | ★単体では除外不要 |
| 7 | `setplay-move-notes.md` | 70 | **含まない**。`公式` の出現が **0 件** | ★単体では除外不要 |
| 8 | `README.md` | 51 | **含まない**（他ファイルの性格を述べる記述のみ。`:31` が名指しの根拠を持つ） | ★除外の宣言そのもの |

**★結論 1＝ディレクトリごとの除外を続ける。** 3 件（＋境界 1 件）が該当し、**`D-742` がディレクトリ単位にした理由**〔ファイル単位にすると後から足されたファイルが黙って通る＝denylist と同じ穴〕**は今も有効である。⇒ 5〜7 が単体では除外不要であることは、ディレクトリ単位をやめる理由にならない。**

**★結論 2＝除外は「公開しない」であって「無い」ではない**（束 E-2 ／ 指示書 §4.4）。**8 件は開発リポジトリの追跡下に在り、履歴にも残る。⇒ 開発リポジトリを丸ごと公開する運用へ変えると、この防壁は消える。★公開スナップショット方式であることが前提である**（`NOTICE:47-48`）。

### 4.2 ★★最大の発見——同じ内容が、**公開される** `docs/progress/` に在る

**★`D-742` の除外は「場所」で切っている。⇒ 「内容」では切れていない。**
公開される 1482 件の `docs/**` を、`docs/seed-data/` と同じ判定基準——**公式の生フレーム値・ダメージ値の転記**——で走査したところ、**2 ファイルが該当した。**

| # | ファイル | 公開 | 転記されている公式値 |
|---|---|---|---|
| **L-1** | **`docs/progress/M14-RESEARCH-02-report.md`** §C-3-7（`:370-376` の表） | **★公開される** | **約 26 個**。テリー 1 キャラ・27 move の範囲で、`active` ／ `damage` ／ `startup` ／ `recovery` ／ `on_block` の**公式値が「公式値→実測値」の対の左側として逐語で載っている**。該当は `:372`（ジャンプ通常技 5 技）／ `:374`（多段合計への置換 7 技）／ `:376`（単独の数値差 9 技） |
| **L-2** | **`docs/progress/progress-log.md:3742`** | **★公開される** | **4 個**。`L-1` の `:372` と同じジャンプ通常技 2 技の `active` ／ `damage` |

**★走査の方法は §7.B [5a] [5b] に手順として残した。⇒ 次の波でも同じ走査ができる。**

> **★★本書自身が 1 度 3 件目になった**（2026-09-11・レビュー指摘 高-2）。
>
> **⇒ 初版の §4.1 と §4.2 は、除外の根拠を示すために `docs/seed-data/` と `L-1` から公式の生値を逐語で引き写していた。★本書は公開される。⇒ 除外が守っていたものを、除外について書く文書が漏らしていた。**
>
> **★是正＝値の逐語引用を落とし、行番号・件数・値の形だけの記述へ置き換えた。⇒ 再判定の結論**（除外を続ける）**は値を引かずに成立する。★置き換え後に §7.B [5a] を回し直し、ヒットが 2 件へ戻ることを確認した。**
>
> **★★教訓は §7.A (d) の分かれ目 9 へ入れた**——**走査の母集団には、いま書いている自分の文書が入る。⇒ 「公式値が在るか」を調べる文書は、その値を引用しないで書けるかを先に考えること。**
>
> **★★★同じことが 2 巡目にも起きた**（2026-09-11 の追走）**。⇒ レビュー報告書 `docs/progress/m26-03-review.md` が、指摘 高-2 を書くために公式値を逐語で引いていた。★レビュー報告書も公開される。**
>
> **★★そして、そのとき走査は 1 行しか拾わなかった。⇒ `docs/seed-data/` 由来の引用は「実測値（公式 N）」「NN→NN」の形であり、英語の列名も 2 桁以上の数値も伴わないため、狭い網を素通りした。** **★是正＝レビュー報告書からも値の逐語引用を落とし、走査を 2 本立て**〔全体は狭い網 ／ 自分が足した行は広い網〕**へ改めた**（§7.A (c) ／ §7.B [5a] [5b]）**。**
>
> **★★ここから引くべき教訓は「走査を直せば済む」ではない。⇒ 狭い網は取りこぼし、広い網は使い物にならない。★機械で決められないのだから、書き方の側で守るしかない。**

**★製造の観測。**

- **(a) これは `docs/seed-data/` を外した判断と整合しない。** 外した理由は「公式の生フレーム/ダメージ値や元データ抜粋を含み、OSS リポへそのまま載せられない」（`docs/seed-data/README.md:31`）であり、**L-1 / L-2 はまさにそれを含んで公開される。**
- **(b) 量は `docs/seed-data/` より小さい。** テリー 1 キャラ・27 move の範囲に閉じており、`input-notes.md` のような全キャラ横断の蓄積ではない。**⇒ 同じ性格だが同じ規模ではない。**
- **(c) `D-637`「docs はアーカイブ含め全部公開する」の判断は、この族を対象にしていない。** `D-742` の逐語＝「`D-637` の『全部公開』は『隠す価値のあるものが無いから』という判断である。**⇒ 第三者の権利が絡むものは、その判断の対象外である**」。**⇒ L-1 / L-2 は `D-637` の例外ではなく、`D-742` と同じ `A3` の適用対象である。**
- **(d) ★製造は直さない。** 理由は 3 つ。**(i) 指示書 §0.1＝本サブの書き込みは §2.4 の 1 件だけである。(ii) 完了報告と `progress-log.md` は歴史記録であり書き換えない**（`D-274` (3)）**。(iii) 「消す」変更は開発者の手番である**（`CLAUDE.md` §10.Y ／ `D-196` 境界条件 3）。
- **(e) ★選択肢は 3 つある**（**決めない**）。**(i) `public-snapshot-manifest.txt` に DENY を 2 行足す**〔ただし `progress-log.md` は横断インデックスであり、丸ごと外すと公開版から索引が消える〕**／ (ii) 該当箇所だけを要約・抽象化した派生版へ置き換える**〔`docs/seed-data/README.md:44` が自分について挙げている案と同じ形〕**／ (iii) このまま公開する**〔量が小さく、いずれも「公式が誤っていた」ことの記録であるという評価〕。
- **⇒ §8-6 で申し送る。**

### 4.3 `REUSE.toml` の `docs/seed-data/**` 宣言の再判定（**`REUSE.toml:73-77` が本サブを名指ししている**）

**名指しの逐語**＝「`docs/seed-data/**` の著作権表示は暫定である … 同ディレクトリは公開スナップショットから DENY されており、その理由は『公式の生フレーム値・元データ抜粋を含む』——つまり第三者素材を含みうると自分で判定した範囲である。**そこへ自分の著作権表示と CC BY-SA を宣言するのは整合しない**。★公開物には出ないため実害は無いが、**`M26-03`（`A3`＝データ来歴の切り分け）で再判定すること**」。

**★製造の判定＝整合していないという指摘は正しい。⇒ 4.1 の再判定でも、3 件（＋境界 1 件）に第三者素材が現存することを確認した。**

**★ただし `REUSE.toml` は触らない**（指示書 §3-2 ／ 束 C-4）。**⇒ 推奨だけを書いて返す。**

| 案 | 内容 | 製造の評価 |
|---|---|---|
| **A** | `docs/seed-data/**` を層 B から外し、**既定の層 A へ落とす** | **★悪化する。** 層 A も「2026 plexiblinp」の著作権表示であり、**第三者素材へ自分の表示を付ける問題は解消しない**。⇒ 層が変わるだけである |
| **B** | `docs/seed-data/**` 専用の `[[annotations]]` を立て、`SPDX-License-Identifier` を **`LicenseRef-` 形式の「混在・再配布不可」**にする | **★製造の推奨。** REUSE は独自ライセンスを `LicenseRef-<名前>` で表現でき、「配らない範囲である」ことを宣言そのものに書ける。**⇒ 公開スナップショットの DENY と宣言が同じことを言う** |
| **C** | 現状維持（暫定の注記のまま） | **★次に読む人が同じ違和感を踏む。** 注記は既に 1 度「再判定せよ」と送っており、**本サブがそれに答えないと注記だけが残る** |

**★案 B は `REUSE.toml` の三層の割当そのものには触れない**〔層 A / B / C の定義も、`character_data/**` と `migrations/*_data_*.sql` の割当も変えない〕**。⇒ 指示書 §3-2 の禁止には当たらないと解した。★ただし `REUSE.toml` の編集は CHANGE の対象になりうるため、本サブでは実施せず申し送る**（§8-7）。**推測: 三層の「割当そのもの」とは層の定義とパスの層への振り分けを指し、除外範囲の表現形式は含まない、と仮定した。**

---

## 5. 層 B に入るべきものが層 A へ落ちていないか（**束 A-4 ／ 指示書 §2.2-4**）

### 5.1 機械で守れている範囲

```
$ bash scripts/check-migration-license.sh
凍結表: 層 A 30 本 / 層 B 72 本
結果: 違反なし
```

**★ただし `scripts/check-migration-license.sh:39` の逐語＝「対象範囲: `migrations/` と `REUSE.toml` のみ。ライセンスの妥当性そのものは見ない」。⇒ 機械が守っているのは 218 件だけである。**

### 5.2 人が見るしかない範囲——実査の結果（**落ちているものは無い。逆向きが 2 件ある**）

| # | 対象 | 件数 | 宣言 | 判定 |
|---|---|---:|---|---|
| 1 | `character_data/**` | 35 | 層 B（`seed-progress.md` 1 件のみ層 A へ差し戻し） | **◎ 正しい。** §3.1 の全数判定で、層 A へ落ちているものは無い。差し戻された 1 件も「seed 投入の手順書でありゲームデータではない」という理由と合う |
| 2 | `migrations/**` | 218 | 層 A 64 ／ 層 B 154 | **◎ 正しい**（機械検査が緑。§2.4） |
| 3 | **`docs/seed-data/**`** | 8 | 層 B | **△ 逆向き。** **層 B に「入れるべきでないもの」が層 B に在る**（§4.3） |
| 4 | **`web/src/components/ui/`** | 21 | 層 A（既定の `**`） | **△ 逆向き。** **第三者が書いたソースに本体の著作権表示が付いている**（§2.3 X-2）。**★層 B の問題ではない**（SF6 とは無関係）**が、来歴の切り分けとしては同じ形である** |
| 5 | `docs/progress/` の L-1 / L-2 | 2 | 層 A（`docs/progress/**` は層 C の列挙に入らないため既定の層 A） | **△ 逆向き。** **公式の生値を含む記述が、本体の著作権表示つきで公開される**（§4.2） |

**★★まとめると、「層 B に入るべきものが層 A へ落ちている」例は 1 件も無かった。⇒ 見つかったのはすべて逆向き——「自分のものとして宣言している範囲に、他人のものが混じっている」形である。**

**★★ただしこの 1 文だけを持ち帰らないこと**（2026-09-11・レビュー指摘 中-3）**。⇒ §4.2 の `L-1` / `L-2` は SF6 の事実**（公式のフレーム値・ダメージ値）**を含んだまま層 A として公開される。★素直に読めば「落ちている」側に見える。⇒ 本書が「落ちていない」と書いたのは、三層の軸が「SF6 の事実か否か」であり、`L-1` / `L-2` の問題は「第三者素材か否か」という別の軸に属するからである**（§8-8）**。★軸が違うだけで、放置してよいという意味ではない。**
**★これは `M26-01` が決めた三層の割当の欠陥ではない。⇒ 三層は「SF6 の事実か否か」で切る軸であり、「第三者素材か否か」を切る軸を持っていない。★軸が 1 本足りない、というのが本サブの観測である**（§8-8）。

---

## 6. 段 4（`§2.4`）— 窓口導線の差し替え（**本サブで唯一の書き込み**）

### 6.1 変更した 2 ファイル

```
$ git diff --stat 2e35bbd
 NOTICE     | 11 ++++++++++-
 README.txt |  8 ++++++++
 2 files changed, 18 insertions(+), 1 deletion(-)
```

**`README.txt`**——`■ ライセンスと問い合わせ` 節に 1 項を追加した（従来の 136-137 行は残し、その下へ挿入）。

```
- 権利者の方からのお申し出、および同梱しているゲームデータの出所について
  のお問い合わせは、下記の窓口へお願いします(撤退ポリシーは NOTICE に
  記載しています)。

      <権利者・一般問い合わせ窓口のアドレス>

  ※ 個人が開発している OSS のため、返答の期限はお約束できません。
  ※ 脆弱性の報告は、この窓口ではなく SECURITY.md の経路をお使いください。
```

**`NOTICE` §3**——「連絡先は `SECURITY.md` に記載の窓口を用いること。」の 1 行を、専用窓口の記述へ差し替えた（撤退ポリシーの本文 `:32` はそのまま）。

### 6.2 チェックリスト束 D への対応

| # | 見ること | 本サブの結果 |
|---|---|---|
| **D-1** | 差し替えたのが 2 本か | **◎ `README.txt` と `NOTICE` の 2 本のみ。** `git diff --stat 2e35bbd -- CONTRIBUTING.md` は **空**（`D-814`） |
| **D-2** | アドレスが伏せ字か | **◎ `<権利者・一般問い合わせ窓口のアドレス>`。** 実アドレスは書いていない。`NOTICE` 側には「★上記は伏せ字である。実アドレスは公開の手番で差し替えること」の注記を、**同ファイル `:44-45` の既存の★注記と同じ形で**置いた |
| **D-3** | DMCA を案内していないか | **◎ 両ファイルとも DMCA / GitHub の申し立て経路に一切触れていない**（`D-805` で推奨撤回済み） |
| **D-4** | 返答の期限を約束しないことが明記されているか | **◎ 両方に明記**（`README.txt`＝「返答の期限はお約束できません」／ `NOTICE`＝「返答の期限は約束しない」） |
| **D-5** | `SECURITY.md` の窓口と混ざっていないか | **◎ 両方に「脆弱性は本窓口ではない」旨を明記して分離した。** `SECURITY.md` 自体は 0 バイト差分（`M26-04` の射程） |

### 6.3 ★製造判断を 1 件した（報告する）

**★指示書 §2.4 は窓口の「置き場」を指定していない。**
`followup-backlog:1399` の案 A は「`SECURITY.md` に §5 を新設し、そこへ回す」だったが、**指示書 §2.4-5 は `SECURITY.md` と混ぜるなと明示し、同ファイルは `M26-04` の射程である。⇒ 回し先を作れない。**

**⇒ 伏せ字アドレスを 2 ファイルへ自己完結で書いた**（回し先を作らず、それぞれが窓口を持つ形）。
**★副作用が 1 つある**——実アドレスが決まったとき、**差し替える箇所が 2 か所になる**（`M26-04` が `CONTRIBUTING.md` にも書くなら 3 か所）。**⇒ §8-9 で申し送る。**

**★文体を既存に合わせた**——`README.txt` は敬体・半角括弧・プレーンテキスト、`NOTICE` は常体・`**` 強調あり。

---

## 7. ★★判定の手順（**設計卓が `SUPP-001` §5.5.9 へ移すための原稿**。指示書 §2.2-5 ／ 束 B-1・B-2・B-3）

> **★★本節は原稿である。** **判定手順の正本は `SUPP-001` の新節 §5.5.9 であり、⇒ 下の §7.A をそのまま移せばよい。**
>
> **★決めたのは 3 つの裁定であり、それぞれ別のことを決めている**（2026-09-11）**。⇒ まとめて「決着済み」と読まないこと。**
>
> | 裁定 | 決めたこと |
> |---|---|
> | **`D-819`** | **置き場**＝`SUPP-001` §5.5 配下の新節とする。**★製造が出した案 A / B / C はいずれも採らない** |
> | **`D-820`** | **節番号＝§5.5.9** ／ **節の長さ＝集積場の系統**（60〜80 行）／ チェックリストへ束 `B-5` `B-6` を追加 |
> | **`D-821`**（`CHANGE-181`） | **`SUPP-001` の節番号の重複の処理**（§8.14）／ **規則 A**（プライム形を使わない）／ **規則 B**（挿入位置の明示） |
>
> **★★挿入位置＝§5.5.8 の直後**（`D-821` 規則 B）。**`SUPP-001` の節の並び順は番号順ではなく、末尾に 2 つ目の §5.5.4 が在る。⇒ 位置を明示しないと末尾へ付く。★同じ 1 行を原稿の中にも置いた**——**設計卓は `DRAFT` ブロックを切り出して貼るため、外に書くだけでは落ちる。**
>
> **★完了報告は歴史記録であり、正本にはならない。⇒ 移した後に本節を引かないこと。** 反映と `CHANGE` の採番は設計卓の手番である（`D-293`＝製造は自採番しない）。
>
> **★なぜ手順が要るか。** `D-811` の初出情報＝リリース数週間後に大きめの SF6 アップデート〔1 キャラ追加 ＋ バトルバランス調整〕が来る。
> **⇒ 来歴の判定は繰り返される。★結論だけを書くと、次の波で全数を最初から起こすことになる**（指示書 §4.2）。

### 7.A ★原稿（**この範囲をそのまま `SUPP-001` §5.5.8 の直後へ**）

<!-- DRAFT-5.5.9-BEGIN -->

#### 5.5.9 ゲーム側アップデートに追従するときの配布物のデータ来歴の判定

**（2026-09-11 追加）**

> **★挿入位置＝§5.5.8 の直後**（2 つ目の §5.5.4 の手前）**。⇒ `SUPP-001` の節の並び順は番号順ではないため、末尾へ付けないこと**（`D-821` 規則 B）**。★この引用行は反映時に落としてよい。⇒ 落とし忘れても §5.5.9 の内側に収まる。**

**アップデートのたびに「配布物のうち何がカプコン由来か」を判定し直すことになる。書かれていないと、次の波で全数を最初から起こすことになる。**

**(a) 引き金は 4 つある。**

| 引き金 | 何が動くか |
|---|---|
| **`character_data/*.csv` を更新した** | フレーム値が動く。**列が増えることもある**（先例＝`M28-02a` が 23 列 → 24 列へ） |
| **キャラが追加された** | CSV が 1 本増え、`migrations/` に seed が 1 組増える |
| **`docs/` に調査レポートが増えた** | **★公式値の転記が混ざりうる。⇒ (c) の走査が要る** |
| **公開スナップショットの許可リストを変えた** | 母集団そのものが動く |

**(b) 母集団は「公開されるもの」である。**

`bash scripts/check-public-snapshot.sh --list` の実出力から起こす。**★完了報告の写しを母集団に使わないこと**——`M26-02` 完了報告 §4.2 の貼り付けは作成時点のスナップショットであり、その後 DENY が 1 行足されて件数がずれた。**⇒ 正本は `scripts/public-snapshot-manifest.txt` と `--list` の実行結果である。**

**(c) 3 階層で降りる。全数をファイル単位で見ない。**

| 階層 | 何を見るか | 道具 |
|---|---|---|
| **1** | 許可リストのグロブ単位で 3 値〔カプコン由来の事実 ／ 手入力 seed ／ 本体のコード・文書〕を割る | `--list` をトップレベルで集計する |
| **2** | 混在する領域だけファイル単位へ降りる〔`character_data/` ／ `migrations/` ／ `docs/seed-data/`〕 | `bash scripts/check-migration-license.sh --list` |
| **3** | CSV は列単位へ降りる。**★先に列が増えていないかを見る** | `character_data/*.csv` の全ヘッダを `sort -u` して **1 種類であること**を確かめる（**★本数を書かない。⇒ (a) の引き金 2 で増える**） |

**★あわせて、公式の生値が公開物へ混ざっていないかを走査する。⇒ 走査は 2 本立てにすること。**

| 走査 | 対象 | 網の幅 |
|---|---|---|
| **全体走査** | `--list` の `docs/` 配下（**千件規模**） | **狭くする。**`公式` と英語の列名・数値が近接する形だけを拾う。**★広げると版数の矢印**（`23 列 → 24 列` 等）**で数百件に当たり、合否の判定に使えなくなる** |
| **★自分の差分の走査** | **その手番で足した行だけ**（`git diff <着手基点> \| grep '^+'`） | **広くする。**`数値→数値` の対まで拾う。**★件数が小さいので、当たった行を人が 1 件ずつ見られる** |

**★★全体走査は網であって証明ではない。⇒ 狭い網は必ず取りこぼす**（実例は (d) の 9）**。★本当の歯止めは「値を引用しないで書く」という書き方のほうである。**

**(d) ★判定の分かれ目。ここが機械化できない部分であり、本節の値打ちである。**

| # | 分かれ目 | 判定のしかた |
|---|---|---|
| **1** | その列は公式の値の写しか、開発者の実機実測か | **★列の名前で判断しない。** `startup` のような公式にもある列名でも、本プロジェクトでは**実測が正であり、公式と系統的に食い違う。**⇒ `docs/seed-data/moves-input-background.md` §1.3 と `docs/seed-data/check-criteria.md` §2 の類型を読んでから決める。**★この 2 本は公開スナップショットから除外されている**（(d) の 7）**。⇒ 開発リポジトリでのみ読める** |
| **2** | その列は「情報」と「表記」のどちらが公式由来か | **2 つに割る。** `command` は**情報内容が公式由来で、表記は本アプリのトークン語彙である。**⇒ 「事実 in ／ 表現 out」はこの分割で判定する |
| **3** | 列で切れば足りるか | **★足りないことがある。** `name_ja` は通常技と固有技で来歴が違い、`damage` は TC 行だけ値の意味が違う（単発 → 累積）。⇒ **列の中で行が割れる可能性を必ず一度疑う** |
| **4** | 「判定できない」と書いてよいのはどんなときか | **公式 dist が作業ツリーに無いとき**は突合による裏取りができない。⇒ **埋めずに「判定できない」と書き、埋めるには何が要るかを添える** |
| **5** | 3 値に収まらないものをどうするか | **★カプコン以外の第三者由来は 3 値のどこにも入らない**（ライセンス本文・コピーして持ち込んだ UI ソース等）。⇒ 別枠に立て、**「許諾の問題」と「帰属表示の問題」を分けて書く** |
| **6** | 除外されているものを「無い」と扱ってよいか | **★だめである。** 除外は「公開しない」であって「無い」ではない。**開発リポジトリの履歴には残る。**⇒ 公開がスナップショット方式であることを毎回確かめる |
| **7** | `docs/seed-data/` の除外を外してよいか | **公式の生値を含むファイルが現存する限り外さない。★ディレクトリ単位を維持する**——ファイル単位にすると後から足されたものが黙って通る（`D-742`） |
| **8** | 判定に日付をどう添えるか | **★実査日を書く。** そのうえで判定対象の最終変更日を実測して添える（`git log -1 --format=%ad --date=short`）。**⇒ 「これで確定」とは書かない** |
| **9** | 走査の母集団に自分の文書が入ることを忘れていないか | **★入る。** 判定結果を書いた文書は `docs/` に置かれ、**公開される。**⇒ 「公式値が公開物に在るか」を調べる文書がその値を引用すれば、自分が該当件数を 1 つ増やす。**★根拠は行番号と件数と値の形で示せる。⇒ 値そのものを引かずに書けるかを先に考える。** **★★そして「走査が緑だから引用していない」と読まないこと**——**狭い網は、英語の列名を伴わない引用**〔`実測値（公式 N）` や `NN→NN` の対だけの形〕**を静かに取りこぼす。⇒ 自分が足した行は (c) の広い網で別に見ること** |

**(e) 前回値との差だけを見る。全部を起こし直さない。**

前回の実測値は、その回の完了報告が持つ〔階層 1 の件数表 ／ `character_data/` のファイル一覧 ／ 列ごとの充填 ／ `docs/seed-data/` の判定〕。**⇒ 出力と突き合わせ、動いた行だけを再判定する。動いていない行は前回の判定が生きている。**

**★件数には、その回の完了報告とレビュー報告そのものが含まれる。** 判定した文書は `docs/` へ置かれて公開物になるため、**次に数えると `docs/` が増えている。⇒ 差の理由を前回の完了報告に書き残しておくこと。**

**(f) 何を書き残すか。⇒ (e) が突き合わせる相手はこれである。**

| 残すもの | なぜ要るか |
|---|---|
| **階層 1 の件数表**（グロブごとの件数と 3 値。**合計が `--list` の行数と一致すること**） | 次回、どの領域が動いたかが**引き算で分かる。**⇒ 動いた領域だけ降りればよい |
| **列ごとの判定と充填数** | **★列の来歴は年単位で変わらないが、充填は毎回動く。**⇒ 判定を書き写し、充填だけ測り直す |
| **★「判定できない」の一覧と、埋めるのに何が要るか** | **★これが最も価値がある。**⇒ 条件が揃った瞬間（公式 dist を接続した等）に**まとめて閉じられる** |
| **実査日と、判定対象の最終変更日** | 「いつ時点の判定か」が復元できる。**⇒ 「これで確定」と書かない**（(d) の 8） |

**★逆に、公式の生値そのものは書き残さない**（(d) の 9）**。⇒ 行番号・件数・値の形で足りる。**

**★記載先はここであり、`DATA-LICENSE.md` §4 ／ `REUSE.toml` ではない。** 前者は**層とデータの正本**を書く場所、後者は**パス単位の宣言**を書く場所であり、**運用手順**を書くと同じ情報が 2 か所に増える。

**★由来＝`M26-03`。** 実行コマンドの逐語と前回の実測値は、同サブの完了報告に在る（**再現用**）。**★判断の中身は本節が正本であり、⇒ 迷ったときに完了報告を引き直さないこと。**

<!-- DRAFT-5.5.9-END -->

### 7.B 原稿に入れなかった補足（**完了報告に残す実行記録**）

**★判断の中身はすべて §7.A に在る。⇒ ここに置くのは再現用のコマンドだけである。**

```bash
# [1] 母集団を起こす
bash scripts/check-public-snapshot.sh                 # 母数 / 公開 / 除外 の件数
bash scripts/check-public-snapshot.sh --list > /tmp/pub.txt
bash scripts/check-public-snapshot.sh --list-excluded  # 除外の理由つき一覧

# [2] 階層 1 — トップレベルで件数を割る
awk '{ if ($0 ~ /\//) { split($0,a,"/"); g=a[1] } else { g=$0 } c[g]++ }
     END { for (k in c) printf "%6d  %s\n", c[k], k }' /tmp/pub.txt | sort -rn

# [3] 階層 2 — 層の解決結果
bash scripts/check-migration-license.sh                # 違反 0 であること
bash scripts/check-migration-license.sh --list | awk '/^  [AB?]/{print $1,$2}' | sort | uniq -c

# [4] 階層 3 — 列契約と充填の実測
for f in character_data/*.csv; do head -1 "$f"; done | sort -u | wc -l   # 1 でなければ列契約が割れている
python3 - <<'PY'
import csv, glob
files = sorted(glob.glob('character_data/*.csv')); cols=None; rows=0; ne=None
for f in files:
    with open(f, newline='', encoding='utf-8') as fh:
        r=csv.reader(fh); h=next(r)
        if cols is None: cols=h; ne=[0]*len(h)
        for row in r:
            if not row: continue
            rows+=1
            for i,v in enumerate(row[:len(cols)]):
                if v.strip(): ne[i]+=1
print(f"files={len(files)} rows={rows} cols={len(cols)}")
for i,c in enumerate(cols): print(f"{c:<28}{ne[i]:>7}{ne[i]/rows*100:>7.1f}%")
PY

# [5a] 全体走査（★狭い網。広げると版数の矢印で数百件に当たり使えなくなる）
grep '^docs/' /tmp/pub.txt | while read -r f; do
  grep -qE '公式[^。]{0,40}(active|damage|startup|recovery|on_block|on_hit)[^。]{0,20}=?[0-9]|公式(側)?(は|が|の)?[^。]{0,15}[0-9]{2,4}[^。]{0,10}(→|、)' "$f" && echo "$f"
done

# [5b] ★自分が足した行だけを広い網で見る（★[5a] は必ず取りこぼす）
git diff <着手基点> | grep '^+' | grep -vE '^\+\+\+' \
  | grep -nE '公式[^。]{0,40}(active|damage|startup|recovery|on_block|on_hit)|公式[^。]{0,10}[0-9]|[0-9]+→[0-9]+'
# ★ファイル単位ではなく行単位にすること。progress-log のような継続更新ファイルは
#   ファイル全体を見ると既存の歴史記録が大量に当たる

# [6] 公式由来の HTML / CSV が混ざっていないか
grep -i '\.html$' /tmp/pub.txt | sed 's|/[^/]*$||' | sort | uniq -c   # すべて自作資料か
grep -i '\.csv$'  /tmp/pub.txt                                        # character_data/ の 31 本だけか
```

**★本サブの実測値**（2026-09-11・`--list` は 2954 件）**は §2.2 ／ §3.1 ／ §3.3 ／ §4.1 に在る。⇒ 次回はそれと突き合わせる。**

**★★前回値を読むときの注意が 1 つある**（2026-09-11・初回レビュー指摘 中-4）**。⇒ §2.2 の 2954 件は、本書とレビュー報告書がコミットされる前の値である。★判定結果を書いた文書自身が公開物になるため、書いた後に数え直すと増える**——**実測で `docs/` は 1482 → 1484 件、合計は 2954 → 2956 件になった**（本書 ＋ `docs/progress/m26-03-review.md` の 2 件ぶん）**。⇒ 次に数える人が差の理由を復元できるよう、ここに残す。**

> **★この注記は 1 度失われた**（2026-09-11・再レビュー指摘 高-B）**。§7 を `7.1〜7.4` から `7 / 7.A / 7.B` へ組み替えたとき、旧 §7.4 ごと消えていた。⇒ §10 の取り込み表は「採用」のままであり、取り込んだ記録と成果物が食い違っていた。★節を組み替えるときは、その節が抱えていた取り込み済みの指摘を数え直すこと。**
---

## 8. ★開発者への確認事項・申し送り（**製造は決めない**）

| # | 事項 | 段 | 宛先 | 製造の推奨 |
|---|---|---|---|---|
| **8-1** | **2 本の法務決定文書が判断日を持たない**（§1.3-A）。⇒ 「前提が変わっていないか」を確かめる基準日が文書から決まらない | 段 1 | **開発者**（★開発者のファイルである） | 次に開くときヘッダへ日付を 1 行。**製造は触らない** |
| **★8-1b** | **★★`combmgr-battlelog-source-decision.md:27` が根拠に挙げたホストと、対象サービスのホストが一致していない可能性**（§1.4.2 の 1）**。同書は `cid.capcom.com` の robots 拒否を根拠にしているが、Buckler's Boot Camp は `www.streetfighter.com` 側の別サービスである。⇒ `www.streetfighter.com` の robots は未取得** | 段 1 | **開発者**（★開発者のファイルである。**製造は書き換えない**） | **★結論は変わらない**——同書 `:55` が「条文確認は補強であって決定の前提ではない」と自ら書いており、**実務判断は robots 単独に依存していない。⇒ 次に同書を開くとき、根拠のホストを 1 行直せば足りる** |
| **★8-1c** | **★`cid.capcom.com/robots.txt` が `Content-Usage: ai=n` と `DisallowAITraining: /` を明示している**（§1.4.2 の 2）**。⇒ 決定文書のどちらにも書かれていない前提である** | 段 1 | **開発者**（法務の前提の追記） | **★本アプリは AI 学習をしないため実害は無い。⇒ ただし「AI 学習拒否を明示している」ことは、次に再点検する人が知っておくべき前提である。★決定文書へ 1 行足すかは開発者の判断** |
| **8-2** | **★`name_ja` の取得経路が明文化されていない**（§1.6。**2026-09-11 に開発者が製造の当初の整理を訂正した**）**。⇒ 固有技名は「公式のローカライズ名称の転記」ではなく、インゲームで表示される公然の事実の観測である。★フレーム値と同じ原則が通っているのに、`DATA-LICENSE.md` にも `NOTICE` にもその旨が無い** | 段 1 | **開発者**（法務の判断） | **★`NOTICE` §2 が既に「開発者およびコミュニティによる手入力と観測に基づく事実情報」と書いており、⇒ `name_ja` もそこに含まれることを 1 句足せば足りる。★「独自表記 ／ 公式表記」の軸で書かないこと**——**観測か転記かが分かれ目である**（§1.6）**。★本サブの射程外** |
| **8-3** | **`web/src/components/ui/` 21 件が shadcn/ui 由来**で、帰属表示がどこにも無い（§2.3 X-2）。**`NOTICE` §5 は `go.mod` / `package.json` を源泉とするため、コピーして持ち込んだソースは構造上載らない** | 段 2 | **開発者 → `M26-01` 系または `M26-04`** | **★ライセンス条件の確認が先**（判定できない＝U-3）。**許諾ではなく帰属の問題である可能性が高い** |
| **8-4** | **`M26-01` 完了報告の `character_data/` の件数が 34 件**だが**実測 35 件**（§3.1）。宣言に穴は無く、件数の記述だけが古い | 段 3 | **設計卓**（歴史記録のため直さない判断もありうる） | 直さなくてよい。**次に数えるときの注意として残す** |
| **8-5** | **★指示書 §2.3-3 / §4.3 の例「フレーム値は事実、`command` は開発者の補完」が、リポジトリ内の一次記録と向きが逆である**（§3.4）**。★★同じ逆向きの例が `docs/instructions/reviews/M26-03-review-checklist.md` の `A-2` にも写っている**（2026-09-11・レビュー指摘 中-7）**。⇒ 次のレビュー担当が同じ誤りを前提に見る** | 段 3 | **設計卓**（指示書とチェックリストの起草元） | **情報と表記に分けると整合する。⇒ 指示書側とチェックリスト側の両方を直す手番が要る**（★製造はどちらも編集しない） |
| **8-6** | **★公式の生フレーム/ダメージ値が、公開される 2 ファイルに在る**（§4.2 の L-1 / L-2）。`docs/seed-data/` を外した理由と同じ内容である | 段 2 | **開発者**（★`A3` の適用範囲の判断） | **選択肢 3 つを §4.2 (e) に並べた。★製造は決めない。「消す」変更は開発者の手番である** |
| **8-7** | **`REUSE.toml:73-77` の「`docs/seed-data/**` の宣言は暫定」への回答**（§4.3）。指摘は正しく、3 件（＋境界 1 件）に第三者素材が現存する | 段 3 | **設計卓 → CHANGE 起票の要否** | **案 B**（`LicenseRef-` 形式で「配らない範囲」を宣言に書く）**。★三層の割当そのものは変えない** |
| **8-8** | **★三層は「SF6 の事実か否か」の軸しか持たず、「第三者素材か否か」の軸が無い**（§5.2）。⇒ 見つかった問題 3 件（`docs/seed-data/` ／ shadcn/ui ／ L-1・L-2）はすべて後者の軸で、**三層のどこに置いても解けない** | 段 2 | **設計卓**（公開ゲート `A` の設計） | **軸を足すか、除外で切るかの判断。⇒ 現状は「除外で切る」運用**（許可リストの DENY）**であり、それで足りるなら軸は要らない** |
| **8-9** | **伏せ字アドレスの差し替え箇所が 2 か所になった**（`README.txt` ／ `NOTICE`。`M26-04` が `CONTRIBUTING.md` にも書くなら 3 か所） | 段 4 | **開発者**（公開の手番） | `grep -rn '権利者・一般問い合わせ窓口のアドレス'` で全数を拾える形にしてある |
| **8-9b** | **★`NOTICE` に足した暫定注記そのものの撤去**（2026-09-11・レビュー指摘 中-6）**。逐語＝「★上記は伏せ字である。実アドレスは公開の手番で差し替えること（2026-09-11 時点で未確定）。」⇒ `NOTICE` は配布物に同梱される法務文書であり、★付きの開発メモと日付が残ったまま公開されると体裁を損なう** | 段 4 | **開発者**（公開の手番） | **★★この行は `grep -rn '権利者・一般問い合わせ窓口のアドレス'` では拾えない**（アドレス文字列を含まない）**。⇒ アドレスを差し替えるときに本行も消すこと。★同じ形の先例が `NOTICE:44-45` に在り**（公開予定 URL の★注記）**、そちらも公開の手番で見直す対象である** |
| **8-10** | **★判定手順の恒久的な置き場。★★決着済み**（2026-09-11・`D-819` ＝置き場 ／ `D-820` ＝節番号と長さ ／ `D-821` ＝挿入位置）**。⇒ 正本は `SUPP-001` の新節 §5.5.9 であり、挿入位置は §5.5.8 の直後である**（`D-821` 規則 B。**`SUPP-001` の節の並び順は番号順ではなく、位置を明示しないと末尾へ付く**）**。★製造が出した案 A / B / C はいずれも採られなかった**——**`D-819` の決め手は「§5.5.5 と §5.5.6 が同じ動機の節として既に在り、そこは既に読まれる場所である」ことであった。⇒ 新規ファイルは索引を人の手番で張る必要がある分だけ弱い** | 全段 | **設計卓**（**★反映は受理時の設計卓の手番**） | **★原稿は §7.A に置いた**（HTML コメントのマーカーで挟んである）**。⇒ そのまま移せる。★マーカーの文字列を本欄へ逐語で書かないこと**——**書くと `grep` が 2 件に当たり、範囲指定が壊れる**（本追走で実際に踏んだ）**。★`CHANGE` 番号は書いていない**（`D-293`＝製造は自採番しない。**設計卓が受理時に採番する**） |
| **8-10b** | **★`README.txt` の新規ブロックが案内する `NOTICE` は、リリース zip に同梱されない**（2026-09-11・レビュー指摘 低-1）**。同ファイル `:131` の逐語＝「本 zip に入っているのは実行ファイルと本 README.txt だけです」。★既存の `SECURITY.md` / `SUPPORT.md` への参照と同じ形であり本サブが持ち込んだ問題ではないが、権利者が読む導線としては「公開リポジトリの本バージョンのタグにある」旨が近くに要る** | 段 4 | **`M26-04` または公開の手番** | **★`README.txt:127-131` が既に「公開リポジトリの本バージョンのタグにあります」と書いており、そこへ `NOTICE` を足すだけで足りる。★本サブでは触っていない**（§2.4 は窓口導線の差し替えのみ） |
| **8-11** | **`followup-backlog:1399`**（`rights-holder-contact-channel-missing`）**が `D-814` を反映しておらず**「導線 3 本」「`M26-03` v1.0.0」のまま。指示書 v1.1.0 と不整合 | 段 4 | **設計卓**（★製造は本表を編集できない＝`D-382`） | 本サブの完了に合わせて「導線 2 本を差し替え済み・`CONTRIBUTING.md` は `M26-04`」へ更新 |
| **8-12** | **★「CSV は 23 列」という失効した記述が 2 か所に残っている。⇒ 実体は 24 列である**（`last_changed_game_version` を `M28-02a` が追加＝`CHANGE-159`）。**(a) `.claude/commands/precheck_seed_data.md:40` ／ (b) ★`internal/seedgen/model.go:21` の逐語＝「CSV 列順(23 列・character_data/*.csv の現行契約)」——★同じ `var csvColumns` の要素は 24 個である**（本サブの実測。**`:33` で 24 個目を足したときにコメントが追随しなかった**） | 段 3 | **設計卓 / 改善レーン** | **★(b) は本番コードのコメントであり、`go test` も `go vet` も `tsc` も検出しない。⇒ 人が読む以外に見つける経路が無い**（`D-250` と同型）**。★正本側は正しい**——`SUPP-001` §3.2 と `DES-003` はどちらも 24 列である。**⇒ 失効しているのは上記 2 か所だけ。★どちらも 1 行の是正だが、本サブの射程外のため触っていない**（指示書 §0.1） |

### 8.13 ★設計卓へのリレー（**判定手順の置き場・開発者の依頼により作成**）

> **★★本項は役目を終えた**（2026-09-11）**。⇒ 結論＝`D-819` は案 A / B / C のいずれも採らず、`SUPP-001` §5.5 配下の新節と決めた。★節番号 §5.5.9 と節の長さは `D-820` が確定した**（当初の `D-819` は §5.5.7 としていたが、**同番号は既に埋まっており製造が差し戻した**＝§8.14）**。★挿入位置は `D-821` が定めた。**
>
> **★以下のプロンプト本文は経緯の記録として残す。⇒ 消さない。★本文中の `§7.2` / `§7.3` / `§7.4` は執筆当時の節番号であり、現行は `§7.A` / `§7.B` である**（2026-09-11 の組み替え）**。⇒ 逐語であるため本文は直していない。**

```
リレー事項: M26-03 の「来歴再判定の手順」を恒久化してよいか

【背景】
D-811 の初出情報により、SF6 のアップデート（1 キャラ追加＋バランス調整）は
リリース数週間後に来る。M26-03 §2.2-5 / §4.2 とチェックリスト束 B-1 は、
判定結果だけでなく「判定の手順」を残すことを完了条件にしている。
理由は「結論だけ書くと、次のアップデートで全数を最初から起こすことになる」である。

製造は手順を docs/progress/M26-03-completion-report.md §7 に書いた。
7.2 が実行できるコマンド列、7.3 が判定の分かれ目 8 件、7.4 が差分の取り方である。

【問い】
この手順の恒久的な置き場をどうするか。3 案を見ている。

案 A — 完了報告の中に置いたままにする（＝現状）。
  承認不要。ただし完了報告は歴史記録であり書き換えない（D-274 (3)）。
  ⇒ 次のアップデートのとき、担当が 1,482 件の公開 docs から本書を見つけられる保証が無い。
  docs/progress/ は check-doc-refs.sh の走査対象外でもある。

案 B — docs/process/data-provenance-recheck-ops.md を新設して手順の正本にする。
  dependency-pin-ops.md（D-764 / D-766 で例外表へ追加・開発者承認済み）と同じ
  「ops 手順書」の形。CLAUDE.md §10.Y により開発者承認が要る。
  REUSE.toml の層 C（MIT）に自動で入る。
  followup-backlog の sf6-major-update-after-release から索引を張れば、
  M32 投入時に必ず引かれる。

案 C — scripts/ に一覧生成スクリプトを足し、判断の分かれ目は完了報告に書く。
  機械で繰り返せるが、新規スクリプトは指示書の射程外の追加であり、
  M26-03 は「調べて記録するサブ」である。

【製造の推奨】案 B。理由 2 つ。
(1) 本サブが残すのは §7.3 の「判断の分かれ目」であり、機械化できない。
    ⇒ 案 C は主要部を運べない。実際、本サブで最も効いたのは
    「列の名前で判断しない」「情報と表記を分ける」という 2 つの判断規則であった。
(2) followup-backlog の sf6-major-update-after-release は既に
    「M32-01 / M32-02 の投入時に本行を引く」と書いており、引かれる先が要る。
    ⇒ 案 A だと引かれる先が歴史記録になる。

【確認したいこと】
案 B を採るなら (a) docs/ への恒久ファイル新設の承認（CLAUDE.md §10.Y）と、
(b) check-doc-inventory.sh の例外表へ足すかどうかの判断が要る。
承認が出るまで、手順は完了報告 §7 に「★暫定の置き場」と明記して置いてある。
```

---

### 8.14 ★`SUPP-001` の節番号の重複（**製造が差し戻し、設計卓が処理済み**）

**★本サブの原稿を置く先を実査したとき、`SUPP-001` に番号の重複が 2 件あることが分かった。⇒ 製造は直さず差し戻した**（`docs/design/` は設計卓の手番＝`CLAUDE.md` §8）。

| # | 事実 | 扱い |
|---|---|---|
| 1 | **`#### 5.5.4` が 2 回使われている**〔`:1045` マイグレ契約テストの規約 (6)〜(9) ／ `:1309` テスト DB のテンプレート方式〕 | **★設計卓が処理済み**（2026-09-11・`D-821` ／ `CHANGE-181`。**★あわせて `D-820` が新節の番号を §5.5.9 と確定した**） |
| 2 | **同 §5.5.4 の内部でも規約番号 `(10)` が 2 回使われている**〔`:1089` 非破壊条件 ／ `:1160` golden の破壊確認〕 | 同上 |

**★処理の形は「注記 4 か所」であり、番号は振り直していない。⇒ 理由＝`§5.5.4` の外部参照が 124 件あり**（設計卓の実測）**、どちらを指すかを 1 件ずつ判定することになる。★先例は `SUPP-001` 自身の履歴 `:67` の `D-217`「節番号 5.5 は変えない」である。**

**★あわせて `D-821` が新しい規則を 2 つ立てた。⇒ 次に §5.5 を触る担当はこれを引くこと。**

| 規則 | 内容 |
|---|---|
| **A** | **§5.5.4 へ規約を足すときはプライム形**（`(10′)` `(11′)` `(11″)`）**を使わず、次の空き番号を使う。⇒ 現在の最大は `(16)` なので次は `(17)`** |
| **B** | **次に小節を足すのは §5.5.9 であり、§5.5.8 の直後へ置く。⇒ 番号順の並びを、末尾の §5.5.4 の手前まで保つ** |

**★製造は `SUPP-001` を 1 バイトも触っていない**（`git diff --stat` で 0 行）。

---

## 9. ■ 併せて更新が要るもの

| # | 観点 | 本サブの状態 |
|---|---|---|
| 1 | **消費した CHANGE 番号を登録したか** | **★該当なし。CHANGE は 0 本消費**（三層のパス割当は変わらなかった＝§5.2。指示書 §1-13 の「割当が変わると分かったら請求」には至っていない）。⇒ `docs/handover/change-number-registry.md` §1 への登録は不要 |
| 2 | **「次の番号」の写し先 4 か所** | **★該当なし**（CHANGE を払い出していないため、registry §1 ／ 契約 §4 ／ ボード §2.1 ／ ボード §2.4 のいずれも動かない） |
| 3 | **消費したマイグレ連番** | **★該当なし。0 本。** `ls migrations/` の最大は `000110` のままで、ボード §2.2 の「次に払い出す番号」は動かない |
| 4 | **版を上げた文書の参照元** | **★該当なし。** 本サブは版を持つ文書を 1 本も更新していない（指示書・チェックリスト・設計書のいずれも編集していない） |
| 5 | **新設した恒久ファイル** | **★なし。** 本サブが `docs/progress/` へ置くのは **完了報告**（本書。型＝`*-completion-report.md`）と、**Phase B のレビュー報告**（型＝`*-review.md`）**の 2 本であり、どちらも `check-doc-inventory.sh --list-allow` の既存の型に合う**。⇒ §10.Y の承認事項に当たらない。**★初版は 2 本を「作った」と断定していたが、執筆時点でレビュー報告は存在しなかった**（2026-09-11・レビュー指摘 中-5。**§10 で `D-510` を引きながら本欄で同じことをしていた**）**。⇒ 実在を確認したうえで §10 に記す。** |
| 6 | **★他サブへ波及する申し送り** | **§8 の 12 件。** うち **8-11**（`followup-backlog:1399` の `D-814` 未反映）と **8-7**（`REUSE.toml` の暫定注記への回答）は**設計卓が畳む必要がある**（製造は `followup-backlog` を §J 以外編集できない＝`D-382`） |

---

## 10. レビュー結果（Phase B / Phase C）

| 項目 | 値 |
|------|---|
| レビュー報告書 | `docs/progress/m26-03-review.md`（225 行。**★本欄は同ファイルの実在を確認してから書いた**＝`D-510`） |
| レビュー方式 | **fresh subagent**（メイン会話文脈を継承しない。`fork` は使っていない）。**★レビュー担当は完了報告の実測を全件回し直して突合した** |
| 指摘件数 | **初回 13 件**（高 2 ／ 中 7 ／ 低 4）**＋ 再レビュー 8 件**（高 3 ／ 中 2 ／ 低 3）**＝計 21 件** |
| 採否 | **21 件すべて採用。★不採用は 0 件。⇒ 「高」指摘の不採用も 0 件であり、エスカレーションは発生していない** |
| 再レビュー往復 | **1 回**（初回 ＋ 再レビュー 1 回＝**上限到達**）**。★2 巡目の結果は §10.3** |
| 停止時記録 | **★`followup-backlog.md` §J への登録は 0 件**。**往復上限に達したが、未解消の指摘が 0 件のため記録すべき項目が無い**（`CLAUDE.md` §9） |

### 10.1 束別の評価（レビュー担当による）

| 束 | 結果 |
|---|---|
| **A**（来歴の切り分け） | A-1 ◎ ／ A-2 ◎ ／ A-3 ◎ ／ A-4 ○ ／ A-5 ◎ |
| **B**（手順が残っているか） | B-1 ◎ ／ B-2 ◎ |
| **C**（書き換えていないもの） | C-1〜C-5 すべて ◎ |
| **D**（窓口導線） | D-1〜D-5 すべて ◎ |
| **E**（`docs/seed-data/` と外部照会） | E-1 ◎ ／ E-2 ◎ ／ E-3 ◎ |
| **F**（共通） | F-1 ○ ／ F-2 △ ／ F-3 ◎ ／ **F-4 ×** ／ F-5 ○ |

**★チェックリストが最も見たいと言った `A-1`**（判定できないと書けているか）**と `B-1`**（手順が残っているか）**は両方 ◎ であった。★`F-4` の × は `progress-log.md` の索引行の欠落**（高-1）**であり、Phase D で解消した。**

### 10.2 トリアージ（採否と理由）

| # | 優先度 | 指摘 | 採否 | 対応 |
|---|---|---|---|---|
| **高-1** | 高 | `progress-log.md` に `M26-03` の索引行が無く `check-progress-log-index.sh` が赤 | **採用** | **Phase D で追記**。検査が緑になったことを §11 で示す |
| **高-2** | 高 | **完了報告自身が §4.2 の欠陥を再生産している**——`docs/seed-data/`（DENY 済み）と `L-1` から公式の生値を逐語で引き写し、公開される本書が 3 件目になっていた | **採用** | **★レビューの推奨（「`L-3` として数え、件数を 3 へ直す」）より踏み込んだ。⇒ 転記そのものを落とした。** §4.1 と §4.2 の値の逐語引用を、行番号・件数・値の形だけの記述へ置き換え、**§7.2 [5] を回し直してヒットが 2 件へ戻ることを確認した。⇒ 件数 2 は是正後も正しいため §4.2 (e) / §5.2 / §8-6 は変更していない。★顛末は §4.2 の注記と §7.3 の分かれ目 9 に残した**（教訓を消さないため） |
| **中-1** | 中 | §3.3 の集計が `A 10 / S 8`（実数は `9 / 9`。+1 と −1 が打ち消し合計だけ 24 に着地） | **採用** | `A 9 / S 9` へ是正し、**内訳の列名を書き下した**（検算で捕まらない形だったため、数だけでなく中身を置いた） |
| **中-2** | 中 | 指示書 §5 の検査の証跡が完了報告に無い | **採用** | **§11 を新設して 6 件の出力を貼った**（`check-migration-license.sh` は指定どおり `--list` 形） |
| **中-3** | 中 | §5.2 の絶対文が §4.2 と読み合わせにくい | **採用** | §5.2 へ相互参照の 1 段を追加 |
| **中-4** | 中 | §2.2 の母数が自己コミット前の値である旨が §7.4 に無い | **採用** | **当初は §7.4 へ追記した。★その後 §7 を組み替えたときに旧 §7.4 ごと消え、再レビュー 高-B で検出された。⇒ §7.B の末尾へ戻し、実測値も現在の値へ更新した**（`docs/` 1484 件 ／ 合計 2956 件） |
| **中-5** | 中 | §9-5 が、執筆時点で存在しないレビュー報告を「作った」と断定（§10 で `D-510` を引きながら同じことをしていた） | **採用** | §9-5 の文言を是正 |
| **中-6** | 中 | `NOTICE:40` の暫定注記そのものの撤去が申し送られていない（`grep` でも拾えない） | **採用** | **§8-9b を新設**。同型の先例 `NOTICE:44-45` も併記 |
| **中-7** | 中 | チェックリスト `A-2` にも指示書 §4.3 と同じ逆向きの例が写っている | **採用**（**★設計卓宛**） | **§8-5 へ追記**。★製造は指示書もチェックリストも編集しない |
| **低-1** | 低 | `README.txt` が案内する `NOTICE` は zip に同梱されない | **採用** | **§8-10b を新設**（`M26-04` または公開の手番へ） |
| **低-2** | 低 | `M26-01` の `character_data/` 件数 34 と実測 35 の差 | **採用済み** | **§8-4 で申し送り済み**。追加対応なし |
| **低-3** | 低 | `precheck_seed_data.md:40` の「23 列」 | **採用** | **§8-12 を拡張。★製造が独自に見つけた 2 件目**（`internal/seedgen/model.go:21` の同じ失効コメント。**本番コードであり機械検査が一切検出しない**）**を併記した** |
| **低-4** | 低 | 伏せ字の差し替え箇所が 2 か所 | **採用済み** | **§8-9 で申し送り済み**。追加対応なし |

**★不採用は 0 件である。⇒ 「高」指摘の自動棄却は発生しておらず、開発者へのエスカレーションは不要であった。**

### 10.3 再レビュー（往復 2 回目・**上限到達**）のトリアージ

**指摘 8 件**（高 3 ／ 中 2 ／ 低 3）**。⇒ 8 件すべて採用。★不採用 0 件。**
**★主眼は「原稿を切り出したときに成立するか」であった。⇒ 束 B-1〜B-6 と `D-821` の挿入位置は全件 ◎ だったが、切り出しの観点で 3 件の高が出た。**

| # | 指摘 | 採否 | 対応 |
|---|---|---|---|
| **高-A** | **組み替えで消えた節番号を指す行が 4 か所残っている。** 特に結語が `§7.3 の「判断の分かれ目 8 件」` と、**削除済みの節番号 ＋ 古い件数**（実際は 9 件）**を名指していた** | **採用** | 4 か所を是正〔ヘッダ表の `§7.0` → 「§7 冒頭の表」／ `§7.2 [5]` ×2 → `§7.B [5a] [5b]` ／ 結語 → `§7.A (d) の「判断の分かれ目 9 件」`〕。**★§8.13 の保存リレー文に残る旧番号は逐語の歴史記録のため直さず、直上の注記へ「当時の節番号である」を 1 句足した**（低-C） |
| **★高-B** | **初回レビュー 中-4 で採用した注記が、組み替えで消えていた。** §10 の取り込み表は「採用」のままで、**取り込んだ記録と成果物が食い違っていた** | **採用** | **§7.B の末尾へ戻した。★実測値も現在の値へ更新した**（`docs/` 1482 → **1484** 件 ／ 合計 2954 → **2956** 件）**。⇒ 数え直すと増える理由が本書自身とレビュー報告書の 2 件であることまで書いた。★あわせて §10 の該当行を「一度消えて戻した」経緯つきへ直し、「節を組み替えるときは、その節が抱えていた取り込み済みの指摘を数え直すこと」を注記として残した** |
| **高-C** | **実行コマンドの逐語が原稿の外にしか無く、原稿から辿る線が 1 本も無い。⇒ `D-819` が案 A を退けた理由がコマンド側だけ再現していた**（あわせて **`R-6` 原稿に由来が無い**） | **採用** | **原稿の末尾へ 1 行**〔`★由来＝M26-03`。実行コマンドの逐語と前回の実測値は同サブの完了報告に在る（再現用）。**判断の中身は本節が正本であり、迷ったときに完了報告を引き直さない**〕**。⇒ `R-1` と `R-6` が同時に閉じた。** 原稿は 76 行で `D-820` の 60〜80 に収まる |
| **中-A** | 原稿 (c) の「**31 本**のヘッダ」が、**その原稿自身の引き金**（キャラ追加で CSV が 1 本増える）**で失効する** | **採用** | `character_data/*.csv` のグロブ表記へ。**★「本数を書かない」理由も添えた** |
| **中-B** | 原稿が「読んでから決める」と指示する 2 本は `docs/seed-data/` 配下で、**公開スナップショットから除外されている**。⇒ 公開物を読む人には辿れない | **採用** | フルパスへ揃え、**「開発リポジトリでのみ読める」を明記**。**★値の漏洩ではなく導線の問題である** |
| **低-A** | 挿入位置の引用行が見出しの**手前**に在り、落とし忘れると §5.5.8 の末尾に付いた注記として読める | **採用** | 見出しと由来行の**直後**へ移した。**⇒ 落とし忘れても §5.5.9 の内側に収まる** |
| **低-B** | レビュー報告書の「追走の記録」の見出しが `D-820` を落としている | **採用** | 3 裁定を挙げる形へ |
| **低-C** | §8.13 の保存リレー文に旧節番号が残る（**★逐語の歴史記録であり書き換えるべきではない**） | **採用** | 直上の注記へ 1 句。**本文は直していない** |

**★製造が自分で見つけて直したものが 1 件ある**——**§8-10 が `DRAFT` マーカーの文字列を逐語で書いていたため、`grep` での原稿抽出が 2 件に当たり範囲指定が壊れた。⇒ 本追走の検証で実際に踏んだ。** 逐語をやめ、**同じ穴を踏まないよう理由つきで注記した。**

**★レビュアーが「確認していない」と申告した箇所を製造が埋めた。** 制約事項の逐語＝「**初回レビューの 13 件の取り込み内容は再確認していない。⇒ 他の 12 件が組み替えを生き延びたかは全数では見ていない**」。**⇒ 製造が 13 件を全数 `grep` で照合した。★生き延びていなかったのは 高-B の 1 件のみで、残り 12 件は現存する。**

---

## 11. 検査結果（指示書 §5・**Phase C 後の最終実測**）

| # | コマンド | 結果 |
|---|---|---|
| **0** | `bash scripts/check-artifact-integrity.sh`（**★`CLAUDE.md` §8 が「1 本目に回す」としたもの**） | **`結果: 違反なし`**（検査 15 件の自己検査がすべて OK ／ 生成物 4 件も OK） |
| 1 | `go test ./...` | **緑**（`EXIT=0`。**★本サブは Go コードを 1 行も触っていない**＝現状維持の確認） |
| **2** | `bash scripts/check-migration-license.sh --list` | **`結果: 違反なし`**。**層の判定は変わっていない**（凍結表 層 A 30 本 / 層 B 72 本。218 件の解決結果＝A 64 / B 154） |
| **3** | `bash scripts/check-doc-refs.sh` | **`結果: dead reference なし`** |
| **4** | `bash scripts/check-doc-inventory.sh` | **`結果: 型に無いファイルなし`**（情報提供型。**新設した恒久ファイルは 0 件**＝§9-5） |
| **5** | `bash scripts/check-md-emphasis.sh docs/progress/M26-03-completion-report.md` | **`検出: 0 行`** |
| **5b** | `bash scripts/check-md-emphasis.sh docs/progress/m26-03-review.md` | **`検出: 0 行`**（**★この手番で `docs/progress/` へ新規に作った 2 本目**＝`D-775`） |
| **6** | `bash scripts/check-public-snapshot.sh` | **`結果: 違反なし`**（窓口導線の編集で許可リストの判定は壊れていない） |
| **7** | `bash scripts/check-progress-log-index.sh` | **Phase D の追記後に `結果: 違反なし`**（★着手時は違反 1 件＝レビュー高-1） |

**★`docs/progress/progress-log.md` は `check-md-emphasis.sh` へ渡していない**——**継続更新ファイルであり、歴史記録としての既存の検出行を持つため**（`D-274` (3) ／ `CLAUDE.md` §8）。

---

*以上、`M26-03` 完了報告。* **★本書の値打ちは §3.5 の「判定できない 6 件」と §7.A (d) の「判断の分かれ目 9 件」である。**
**★前者を埋めれば表全体の信用が落ち、後者が無ければ次のアップデートで全数を最初から起こすことになる。**
