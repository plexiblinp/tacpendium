# M37-06 レビュー報告書

| 項目 | 内容 |
|------|------|
| 対象 | `M37-06`（modifier の項目確定と表現形式・`B04` / `B05` の未了分） |
| 指示書 | `docs/instructions/M37-06-modifier-set-and-display.md` **v1.1.0** |
| チェックリスト | `docs/instructions/reviews/M37-06-review-checklist.md` **v1.1.0** |
| 着手基点 | `a3fe6a2` ／ 対象コミット `c16e581` / `ce1480d` / `f4962fe` |
| 実施日 | 2026-09-14 |
| 判定 | **条件付き合格**（「高」1 件の是正後にマージ可） |

---

## 総評

束 A〜H を実物のコードで当て直した結果、**本サブの芯である「削除 3 値の表示語を残す」「flags を 14 値にする」「既存行を書き換えない」は、いずれも完了報告の主張どおりに成立している**。とりわけ破壊確認は、指示書 §4.1 が名指ししていた Go 側の `{just}` 形だけでなく、実査で見つけたフロント側の「波括弧すら付かない生コード」形の**両方**に置かれており、逆向き（表示語を外すとフォールバックが出る）まで固定されている。指示書の要求を上回っている。

問い (ii) の「案 C」も、`DES-002` §4.11 に触れていないことを差分で確認できた——`internal/api/` / `internal/repository/` / `migrations/` の差分は**すべて 0 バイト**であり、追加クエリも `recipe_cache` の形式変更も無い。`RecipeText` が一覧・詳細を含む全面の唯一の描画経路であるため、束 F-3 は構造から満たされている。

一方で「高」が 1 件ある。**本サブは Go ⇄ フロントの表示語一致を機械検査にしたのに、`resolver.go` と `labels.ts` のコメントは「機械検査は無い」と断言したまま残っている。** 差し替え原稿（完了報告 §7-1）の側だけが正しく直っており、実体のコードコメントが旧のままという非対称になっている。動作は正しいのでテストも型検査も緑であり、人が読む以外に見つける経路が無い。

再現検証も行った: `go test ./...` **EXIT=0 / `--- FAIL` 0 件**、`pnpm test` **EXIT=0 / 234 files・2931 tests passed**、`make e2e`（全数）**EXIT=0 / 346 passed + 1 flaky**〔`m31-02-tag-field-drag` ＝ followup に既登録の flaky。本サブとは無関係〕、`make e2e-only P=m37-06-modifier-height` **EXIT=0**。機械検査は `check-artifact-integrity.sh` / `check-import-order.sh`（99/99）/ `check-enum-sync.sh` / `check-md-emphasis.sh`（完了報告・0 行）/ `check-stop-discipline.sh` / `check-progress-log-index.sh` / `check-doc-refs.sh` がすべて緑。`gofmt -l .` も 0 件。

---

## 設計準拠性レビュー結果

### 束 A — `B04` を逆行させていないこと（最重要）: **◎**

| # | 判定 | 根拠（**実物で確認した**） |
|---|---|---|
| **A-1** | **◎** | 削除 3 値の表示語は両側に残っている。`internal/service/notation/resolver.go:73-75` に `just` / `neutral_jump` / `forward_jump` の 3 行が在り、フロントは `web/src/features/combo/labels.ts:252-256` の `MODIFIER_FLAGS_RETIRED` として分離保持。`MODIFIER_FLAGS`（引き当て専用・17 件）が RETIRED を合成しており、`ModifiersSummary.tsx:43` の `?? f` フォールバックに落ちない |
| **A-2** | **◎** | `flagtext_internal_test.go:111` `TestApplyFlags_RetiredFlagsStillRender` が retired 3 値を名指しで描画。フロント側は `ModifiersSummary.test.tsx` の「外した 3 値でも内部コードではなく表示語が出る」。**Go とフロントで露出の形が違う**（Go は `{just}`、フロントは波括弧なしの `just`）ことを段 1 実査で見つけて両方に置いた点は、指示書の要求を超えている |
| **A-3** | **◎** | 逆向きが 3 系統在る。①`TestApplyFlags_MissingFlagFallsBack`（`od_lm`＝選択肢側と `just`＝retired 側の**両方**で `flagText` を一時破壊）②`ModifiersSummary.test.tsx` の未知値で生コードが出ること ③`RecipeText.test.tsx` の未知 `{od_lm}` がバッジにならないこと |
| **A-4** | **◎** | `TestApplyFlags_NoInternalIdentifierLeaks` が 17 件全数を回す。母数の床（`len(flagText) != len(want)` で `t.Fatalf`）も在る |
| **A-5** | **◎** | データ移行・マイグレは 0。`git diff --stat a3fe6a2 -- migrations/ internal/api/ internal/repository/` は**空**。書き換え経路も無い——既存ステップの編集は `ModifiersEditor` のみを通り、同部品は `localFlags` を `step.modifiers?.flags ?? []` で初期化して保存時にそのまま返すため、選択肢に出ない retired flag も落ちない（`ModifiersEditor.test.tsx` の「選択肢から外した flag は、編集して保存しても落ちない」が床）。`RecipeBuilder` の `draftFlags` は**新規追加専用**であり既存行に触れない（`handleAdd` のみが消費） |
| **A-6** | **◎** | `internal/recipehash/recipehash.go` は 1 文字も変わっていない。`modifiers` の値を書く経路が無い以上、既存全行の `recipe_hash` は不変である |
| **A-7** | **○** | 差し替え原稿は完了報告 §7-2 に在る。**ただし引用が原文と表記が違う**（後述「低-2」） |

### 束 B — flags の全数と内容: **◎**

`MODIFIER_FLAGS_COMMON`（11）＋ `MODIFIER_OD_VARIANT_FLAGS`（3）＝ **選択肢ちょうど 14 値**。指示書 §2.2 の表と 1 値ずつ突き合わせた結果、**増減も差し替えも 0**。

- **B-1 ◎**: 14 値。`labels.test.ts` が値の集合を sorted 配列で固定している
- **B-2 ◎**: `just` / `neutral_jump` / `forward_jump` は `MODIFIER_FLAGS_COMMON` から消え、選択肢に出ない（`ModifiersEditor.test.tsx` の `queryByText` 3 件が床）
- **B-3 ◎**: 追加 7 値がすべて在る（`no_cancel` / `late_cancel` / `whiff` / `cross_under` / `meaty` / `juggle_high` / `juggle_low`）
- **B-4 ◎**: `od_*` は 3 値のまま。畳んでいない
- **B-5 ◎**: `delay` / `link` / `first_hit_cancel` / `low_jump` すべて残存
- **B-6 ◎**: `low_jump` の表示語は「低空」、**コードは `low_jump` のまま**。`grep 低ジャンプ` の残りはコメントと「旧記述」注記だけであり、生きた表示語には 1 件も無い
- **B-7 ◎**: type は `parry_drive_rush` / `cancel_drive_rush` の 2 値
- **B-8 ◎**: 正規化する実装は足していない
- **B-9 ◎**: 「最速キャンセル」「連打キャンセル」「溜め」は無い（`labels.test.ts` が名指しで禁止している）

### 束 C — `link` と `no_cancel`: **◎**

- **C-1 ◎**: 別の flag として並存
- **C-2 ◎**: 排他化していない。3 面で固定（Go `TestApplyFlags_LinkAndNoCancelAreNotExclusive` ／ `ModifiersEditor` の両方チェック→保存 ／ `ModifiersSummary` の両方描画）。加えて `recipeDisplay.test.ts` が `{目押し} {ノーキャン}` を両方バッジ化することを見ている
- **C-3 ◎**: 完了報告 §7-1 の `DES-004` §2.3 原稿に「`link` は*難易度*の注記であり、接続がキャンセルでないことは `no_cancel` が表す」が在る。**コード側にも同趣旨の注記が `resolver.go:48-49` と `labels.ts:214-215` に置かれている**——原稿が採用されるまでの間も次の担当に届く形になっており良い
- **C-4 ◎**: 「キャンセルの既定はキャンセルである」も原稿とコードコメントの両方に在る
- **C-5 ◎**: 「キャンセルした」を表す flag は作っていない

### 束 D — 表示語の 3 か所: **○**（★「高-1」が在る）

- **D-1 ○**: Go（`flagText` 17 件）とフロント（`MODIFIER_FLAGS` 17 件）は一致しており、**新設した `modifier-flag-labels.sync.test.ts` が両向きで機械検査している**。3 か所目の `DES-004` §2.3 は原稿どまりだが、これは `CLAUDE.md` §8 が設計書本体の編集を製造に禁じているためであり、構造上正しい
- **D-2 △**: 「33」は報告に在る（算出）。**ただし内訳表の「Go `flagText` 動かした件数 11」は実測と違う**（後述「低-1」）
- **D-3 ○**: 「生ラッシュ」へ寄せたことが報告に在る。**ただし副作用が 1 つ生まれている**（後述「中-1」）
- **D-4 ◎**: 設計卓案（`juggle_*`）を採用し、開発者承認の逐語が報告に在る

### 束 E — ボタン群と縦: **◎**

- **E-1 ◎**: 「対象ではない」を事実として書いている。**自分でも確認した**——`MAX_BUTTONIZED_OPTIONS` の参照は `web/src/features/combo/optionButtons.ts` とその利用面だけで、`RecipeBuilder:353` は素の `<input type="checkbox">`、`ModifiersEditor:97` は shadcn `Checkbox` であり `OptionButtonGroup` を通っていない
- **E-2 該当なし**: 対象外のため「やめる」対象が存在しない。`od_*` を畳んで凌ごうとしていない
- **E-3 ◎**: **測り方が健全である。** 段 1 の計測アンカーは `c16e581` で先入れされており、その差分は `data-testid` の追加のみ（`className` は 1 文字も変えていない）——`git show c16e581` で確認した。⇒ 段 1 の値は「変更前の実物」を測ったものである。**段 4 は本レビューで再実行して再現した**: 390x844 → inline **64** / dialog **134**、1280x720 → inline **16** / dialog **135**。選択肢が 7 → 11 に増えて inline は ±0、dialog は −22〜23px。`M37-02` の成果を食っていない
- **E-4 ◎**: 分類を 4 → 5（「当て方・位置系」を新設）へ増やした割り当てが報告と `labels.ts` の並び順に在る

### 束 F — 表現形式（問い (ii)・案 C）: **○**

- **F-1 ◎**: 実査が在り、**内容も正しい**。`internal/model/recipe.go:11` の逐語は「recipe_cache は `{presetId: displayString}` の JSON」であり、ステップ配列も修飾配列も持たない
- **F-2 ○**: 止まらず案 C へ進んだが、**これは開発者裁定（2026-09-14・逐語「3、案C」）であり、逸脱の是非は問わない**。**`DES-002` §4.11 に触れていないことは差分で裏が取れた**——`internal/api/` / `internal/repository/` / `internal/service/combo/` のいずれも差分 0、追加クエリ 0、`recipe_cache` の形式変更 0。変わったのは `RecipeText` の描画内部だけである
- **F-3 ◎**: `RecipeText` が唯一の描画経路であり、面ごとの分岐が存在しない。compact / full の 2 モード両方でバッジが出ることを `RecipeText.test.tsx` が固定している。「片方だけ変えた」は構造的に起こらない
- **F-4 ◎**: 波括弧は残っている。①compact の `title`（テストで固定）②エクスポート・クリップボード（`export-model.ts` / `clipboard.ts` は `RecipeText` を通らず `recipe_cache` 由来文字列を直接使う）③`getSetupDisplayName` 経由の 3 面（`CompareTable` / `TrashSetupListRow` / `TrashBulkActions`）。**`D-867` の理由**〔平文には色が無いから波括弧が唯一の代替〕**と整合している**——色が付く面ではバッジ、付かない面では波括弧、という分かれ方になっている

**★文字列を正規表現で解析する形の脆さについて（依頼事項）**

`splitStepSegments` は `/\{([^{}]*)\}/g` で波括弧を拾い、**中身が既知の表示語 17 件のいずれかに一致するときだけ**バッジにする。実害になりうる経路を洗った結果は次のとおり。

| # | 経路 | 実害の度合い |
|---|---|---|
| 1 | **Go とフロントの表示語がずれる** | **塞がれている。** `modifier-flag-labels.sync.test.ts` が両向きで一致を強制し、しかも「Go 側から 17 件読めたこと」の床（母数チェック）まで置いてある。正規表現が拾えなくなったら**緑ではなく赤**になる |
| 2 | **表示語どうしが重複する** | **塞がれている。** `labels.test.ts` の「表示語が 17 件すべて相異なる」 |
| 3 | **未知の `{...}` を誤ってバッジ化する** | **塞がれている。** 既知語だけを対象にし、未知は素で残す。「壊れているのに整って見える」を避ける判断は正しい |
| 4 | **保存済み `recipe_cache` が古い表示語を持つ** | **残る。**`{低ジャンプ}` はもう既知語でないためバッジにならず、素の `{低ジャンプ}` で出る（後述「中-3」） |
| 5 | `notes`（自由入力）に既知表示語を波括弧付きで書く | 理論上バッジ化する。実害は小（後述「低-6」） |
| 6 | 技名・プリセットエイリアス（自由入力）が既知表示語を波括弧付きで含む | 同上。実害は小 |
| 7 | グローバル正規表現の `lastIndex` 持ち回り | **塞がれている。** 呼び出しごとに 0 へ戻し、連続呼び出しの回帰テストまで置いてある |

⇒ **脆さの主因は「文字列を解析すること」そのものではなく「表示語という*語*に依存すること」である。** そこには機械検査が新設されたので、現時点で実害になりうるのは 4（キャッシュの古さ）だけである。

### 束 G — 触っていないもの: **◎**

`git diff --stat a3fe6a2` を全数で読んだ。**19 ファイルすべてが本サブの射程内**であり、`docs/handover/`（`followup-backlog.md` を含む）/ `docs/design/` / `migrations/` / `go.mod` / `web/package.json` / `web/src/locales/` の差分は**すべて 0 バイト**。

- **G-1 ◎**: `combos` の列に手を出していない（`meaty` は step の flag のみ）
- **G-2 ◎**: flag コードは 1 つも変えていない
- **G-3 ◎**: 画像の追加 0
- **G-4 ◎**: `followup-backlog.md` は 1 文字も編集していない（`D-838`）
- **G-5 ◎**: 設計書本体の編集 0
- **G-6 ◎**: マイグレ消費 0

### 束 H — 実査と検査: **○**

- **H-1 ◎**: 段 1 の 6 点すべてが完了報告 §2 に在る
- **H-2 ◎**: **数え直せなかったことを、数え直せなかったと書いている。** `M37-RESEARCH-01` の「各 1 件」を転記していない（計測点 `M-157` の要求どおり）。加えて「行数に依存しない形で担保した」（テスト側で当該 flag を持つ行を作って描画する）という代替が実際に置かれており、結論が件数に依存していない
- **H-3 ◎**: 「実測」「算出」の書き分けが在る
- **H-4 ◎**: 版ゲート 5 点が表になっている。`M37-DESIGN-01` 1.2.0 / `DES-004` 1.34.0 を自分でも確認した
- **H-5 ◎**: パイプを挟まない判定で `EXIT=` と `FAIL` 件数が貼られている。**本レビューでも再現した**（前述）
- **H-6 ◎**: `check-import-order.sh` 99/99、`check-enum-sync.sh` ベースラインどおり。着手時の実測値も控えられている
- **H-7 ◎**: `docs/usermanual/` に該当章が無いことを実測で示している。**自分でも確認した**——`grep -c "修飾\|modifier"` は `tacpendium-readme.html` / `dist-readme.txt` とも **0**
- **H-8 △**: `progress-log.md` への追記は在る（索引行 ＋ 横断課題 8 件）。**CHANGE 原稿・§J 原稿の置き場が完了報告のままである**（後述「中-4」）

---

## 設計準拠性以外の指摘事項

### コーディング規約（`CLAUDE.md` §4）

- **Go**: godoc・エラー wrap・命名いずれも問題なし。`gofmt -l .` は 0 件（着手前 2 件が本サブで解消。§0.3 に経緯が記録されている）
- **TypeScript**: `any` 0、class コンポーネント 0、`console.log` は E2E spec の計測出力のみ（テストコードは規約の除外対象）。import 順は `check-import-order.sh` が 99/99 で増加なし
- **マジックストリング**: `RECIPE_STEP_CONNECTOR` / `KNOWN_FLAG_LABELS` / `BRACED` いずれも定数化されている
- **`TODO(` コメント**: 追加 0

### 命名

- `MODIFIER_FLAGS_RETIRED` / `splitStepSegments` / `RecipeSegment` / `StepContent` はいずれも役割が名前から読める。**とくに「選択肢（14）」と「引き当て（17）」を別の定数名に割ったことが本サブの設計上の芯**であり、名前でその区別が伝わっている
- `data-testid="recipe-modifier-badge"` / `modifier-flags-inline` / `modifier-flags-dialog` の命名は既存の流儀（`recipe-controller-root` 等）に揃っている

### ライブラリ

新規依存 0（`go.mod` / `web/package.json` とも差分 0）。`modifier-flag-labels.sync.test.ts` は `node:fs` / `node:path` のみを使っており依存を増やしていない。

### セキュリティ

- ブラウザストレージの新規キー 0（`check-browser-storage-keys.sh` 緑・`web/CLAUDE.md` §1 台帳の変更不要）
- 描画は React の通常の子要素経由であり `dangerouslySetInnerHTML` を使っていない。`recipe_cache` 由来の文字列がバッジ化されるが、テキストノードとして描かれるため XSS 経路は増えていない

---

## 推奨修正（優先度別）

### 高（M37 完了前に修正必須）

**高-1. 「機械検査は無い」という*失効した*記述が、コードコメント 2 か所に残っている**

| 場所 | 逐語 |
|---|---|
| `internal/service/notation/resolver.go:42-44` | 「⇒ flag を増やすときは 3 か所すべてを同時に直すこと(**機械検査は無い**。check-enum-sync.sh は flags を見ていない…)」 |
| `web/src/features/combo/labels.ts:191-197` | 「⇒ 片側だけ直すと表示は壊れるが、テストも型検査も緑のままである。**★機械検査は無い**(check-enum-sync.sh は flags を見ていない…)」 |

**本サブは `web/src/features/combo/modifier-flag-labels.sync.test.ts` を新設し、①Go の `flagText` ⇄ ②フロントの `MODIFIER_FLAGS` を両向きで機械検査するようにした。⇒ 上記 2 つの断言は、その手番で失効した。**

**★これを「高」に置く理由は 3 つある。**

1. **通常のテストでは検出できない。** 動作は正しく、テストも型検査も `check-*.sh` もすべて緑である。人が読む以外に見つける経路が無い
2. **後任が本文をコピーして注記を読まない。** 「機械検査は無い」は**行動を規定する記述**である——次に flag を足す担当は、これを読んで「検査が無いから 3 か所を手で確かめよう」と考える。**逆向きの事故も起きる**: 検査が在ることを知らなければ、`modifier-flag-labels.sync.test.ts` が落ちたときに「なぜこんな検査があるのか」と読み解けず、消される余地が残る
3. **原稿だけが直っていて実体が旧のままという非対称になっている。** 完了報告 §7-1 の `DES-004` §2.3 差し替え原稿には「★★★機械検査の状況が変わった（`M37-06`）」が**正しく書かれている**。⇒ 直すべきことは認識されており、書き換え先が 1 か所漏れている形である

**是正の形**（コメントのみの変更であり、実装・テストには触れない）:

- `resolver.go` / `labels.ts` の両方について、「機械検査は無い」を **「Go ⇄ フロントの一致は `web/src/features/combo/modifier-flag-labels.sync.test.ts` が機械検査する。★`DES-004` §2.3（3 か所目）は依然として検査の外である」** に置き換える
- 「`check-enum-sync.sh` は flags を見ていない」の部分は**事実として生きているので残す**（同スクリプトは今も flags を見ていない）
- **★設計卓の手番として**: `docs/design/04-notation-spec.md` の**ヘッダ ステータス行**（`:8`）にも「★★★機械検査は無い」が在る。§2.3 本文（`:259`）は完了報告 §7-1 の原稿が差し替えるが、**ヘッダ行は原稿の射程外**であり、このままだと同じ失効記述が正典に残る。⇒ 完了報告 §7-1 へ 1 行足して請求すること

### 中（M38 着手と並行可）

**中-1. `parry_drive_rush` を「生ラッシュ」へ寄せた結果、同じラジオ 2 択の中で語彙が非対称になった**

`web/src/features/combo/labels.ts:294-300` の `MODIFIER_NON_MOVE_TYPES` は現在 **「生ラッシュ」／「キャンセルドライブラッシュ」** である。着手前は「パリィドライブラッシュ」／「キャンセルドライブラッシュ」で**同じウィジェット内では揃っていた**。

- 開発者の「そのままでよい」裁定（2026-09-14）は、完了報告 §4.5 の記述によれば **`cancel_drive_rush` のサーバ⇄UI の割れ**についてのものである。⇒ **同一ウィジェット内の非対称は、本サブが新しく作ったものである**
- §J 原稿（完了報告 §10）も「サーバ『キャンセルラッシュ』／ UI『キャンセルドライブラッシュ』」という**サーバ⇄UI 軸**でしか記録していない。**UI 内で 2 択の語彙が揃っていないという軸が記録から落ちている**
- **是正は不要**（指示書 §2.6-3 の指定どおりに寄せたことは正しく、勝手に `cancel` 側も動かすのは §3 の射程外である）。**⇒ §J 原稿へ「UI の 2 択の語彙が非対称になった」という観点を 1 行足すこと。** 設計卓が「サーバ⇄UI が割れているだけ」と読むと、この面が視野から落ちる

**中-2. followup `modifier-flag-labels-triplicated-without-check` の状態更新を、名指しで請求していない**

同 followup（`docs/handover/followup-backlog.md:2171`）は状態「**一部手当て済み**（`CHANGE-199`）」であり、本文は「残るのは*定数の移設*である」と書いている。**本サブは移設こそしていないが、3 か所のうち 2 か所を機械検査にした**——これは同 followup の記述を実質的に更新する事実である。

完了報告 §8-3 は「`DES-004` §2.3（3 か所目）は依然として機械検査の外である」とだけ書いており、**followup 行そのものの更新を設計卓へ名指しで請求していない**。`D-838` により製造は同ファイルを編集できないのだから、**請求は設計伝達レポート §4 で明示的に行う必要がある。**

⇒ 完了報告 §7-3（製造の独自判断）または §8 へ、「followup `modifier-flag-labels-triplicated-without-check` の本文と状態を更新すること（Go ⇄ フロントは機械検査になった／残るのは `DES-004` §2.3 と定数の移設）」を 1 行として足すこと。

**中-3. 保存済み `recipe_cache` の `{低ジャンプ}` は、本サブ後は「バッジにならない素の波括弧付き平文」で出る**

`low_jump` の表示語を「低ジャンプ」→「低空」へ変えたため、**`KNOWN_FLAG_LABELS` に「低ジャンプ」は無い**。⇒ キャッシュ再構築前の行は `{低ジャンプ}` のまま素で描かれる。

- 申し送りは在る（完了報告 §8-1・progress-log 横断課題 4・開発者の手番）。**キャッシュ再構築の導線も実在する**ことは確認した（`POST /api/presets/:id/recipe-cache/rebuild` ／ `SettingsPage` の `settings-cache-rebuild` ／ `PresetEditPage` の `preset-cache-rebuild`）
- **ただし申し送りの記述が `M37-02` のときと同じ強さで書かれている**——`M37-02` の申し送りは「押し忘れても壊れない。**表示が古いだけ**」であった。**本サブでは意味が一段変わっている**: 押すまでの間、**同じ画面の中でバッジ（新しい行）と波括弧付き平文（古い行）が混在する**。開発者の要望「編集画面と見た目を一致させたい」に対して、キャッシュ未再構築の行だけが要望を満たさない状態になる
- ⇒ 完了報告 §8-1 と §J 原稿へ、**「押すまで同一画面でバッジと平文が混在する」** を明記すること。「表示が古いだけ」と読むと優先度を取り違える

**中-4. CHANGE 原稿と §J 原稿の置き場が、規定の「設計伝達レポート §4」ではなく完了報告のままである**

指示書 完了条件 12 は「**CHANGE 原稿を設計伝達レポート §4 へ書いた**」、`CLAUDE.md` §9 は停止時記録について「**設計伝達レポート §4 へ『§J 行の原稿』として書く**」と定める。現状は原稿が完了報告 §7 / §10 に在り、`docs/handover/design-reports/` に `M37-06` のレポートが**存在しない**（最新は `20260913-m37-03-design-exceptions.md`）。

- 完了報告 §7 冒頭に「レポート作成時に §4 へ移すこと」と明記されており、`/design_handover_report` を回す Phase が未了なだけと読める。**規律違反ではないが、完了条件 12 は現時点では未充足である**
- ⇒ 取り込み手番の後に `/design_handover_report` を回し、§7-1 / §7-2 / §7-3 / §10 を §4 へ移すこと。**移す際に「高-1」「中-1」「中-2」の是正を反映した版を移すこと**（先に移すと二度手間になる）

### 低（将来対応）

**低-1. 完了報告 §5.2 の内訳表「Go `flagText` 動かした件数 11」が実測と違う（実際は 8）**

Go 側では retired 3 値（`just` / `neutral_jump` / `forward_jump`）の**行は 1 文字も動いていない**——元から在り、そのまま残しただけである。⇒ Go で動いたのは 追加 7 ＋ 表示語変更 1 ＝ **8**。11 が正しいのはフロント（3 値を `COMMON` → `RETIRED` へ移設したため）と `DES-004`（表の行が移るため）の 2 か所である。

合計 33 はチェックリスト D-2 が「算出＝11 値 × 3 か所」と定義しているのでそのままでよいが、**内訳表は「実測」の見出しの下に在る**ため、数字の性格が混ざっている。

**低-2. §7-2 の `SUPP-001` 引用が原文と表記が違う**

原文（`docs/design/supp-001-detailed-design.md:121`）は

> `modifiers` (JSON) も比較対象に含めるため、「同じmove列だがジャスト入力の有無が違うコンボ」は**別コンボとして扱う**

であり、**「同じmove列」に空白が無い**。原稿は「同じ move 列だが**ジャスト入力**の有無が違うコンボ」と書いており、設計卓が原文検索で当てるときに引っかかる。⇒ 原稿の「現行（失効）」欄は**逐語で写す**こと。

**低-3. §7-1 の `DES-004` §2.3 差し替え原稿が、現行表の最終行の扱いに触れていない**

現行表には「**その他 ｜ 言語化が難しい技の当て方を表すコード ｜ － ｜ －**」という行が在る。追加 7 値（`whiff` / `meaty` / `cross_under` / `juggle_*`）はまさに「言語化が難しい技の当て方」であり、この行を残すのか畳むのかを原稿が言っていない。⇒ 設計卓が判断できるよう、原稿に 1 行の言及を足すこと。

**低-4. `RecipeBuilder` の見出し短縮で、インライン面から欄の意味の手掛かりが消えた**

「補足(入力のコツ・状況):」→「補足:」。ダイアログ側は「補足(入力のコツ・状況、複数選択可)」のまま残るため、**同じ概念が面によって説明量の異なる名前で出る**。縦 ±0 を成立させるための意図的な代償として §4.6 に記録されており、判断としては妥当。⇒ 将来、`InfoMark` か `title` 属性で手掛かりを戻せるか検討する余地がある（縦を消費しない形で置ける）。

**低-5. バッジの `mx-0.5` が、サーバ文字列側の半角スペースと二重に効く**

`applyFlags` は `base + " " + flags` を組むため、`{目押し}` の直前には既に半角スペースが在る。`RecipeText.tsx:62` の `mx-0.5` はその上に 2px を足す。またバッジは inline 要素に `py-0.5` を持つため、compact 面（`truncate` = `white-space:nowrap`）では**行ボックスの高さに算入されず背景が上下へはみ出しうる**。いずれも表示上の微差であり機能に影響しない。

**低-6. `splitStepSegments` は `notes` 部の中の既知表示語もバッジ化する**

`applyFlags` は notes を ` (notes)` として同じ文字列へ連結する。⇒ 利用者が notes に `{目押し}` と書くとバッジになる。自由入力なので理論上起こるが、実害は「意図せず色が付く」だけである。

**低-7. 完了報告 §5.6 の dialog @1280 が 1px ずれている**

報告は 390 / 1280 とも **134px** とするが、本レビューの再実行では 390 が 134、1280 が **135** であった（同一 spec・同一コマンド）。フォント描画の丸めと思われ、結論（−23px / 縦は増えていない）は変わらない。

---

## 良かった点

1. **★★★破壊確認の「露出の形が 2 系統ある」ことを、実査で自分で見つけた。** 指示書 §4.1 は Go 側の `{just}` 形だけを名指ししていたが、フロント側（`ModifiersSummary` の `?? f`）は**波括弧すら付かない生コード**になることを段 1 で実測し、**両側に破壊確認を置いた**。指示書の記述を鵜呑みにせず実物を見た結果である。

2. **★★★逆向きの確認を、全数検査と同じ本数だけ置いた。** `TestApplyFlags_MissingFlagFallsBack` は `flagText` を実際に破壊してフォールバックが出ることを確かめ、しかも**選択肢側（`od_lm`）と retired 側（`just`）の両方**で回している。さらに「このパッケージを `t.Parallel()` で並列化しないこと」という副作用の注記まで添えてある。**「緑が何を意味しているか」を証明する形になっている。**

3. **★★★新設した機械検査に「母数の床」を置いた。** `modifier-flag-labels.sync.test.ts` は Go のソースを正規表現で読むが、**「17 件読めたこと」を先に主張する**。正規表現が 1 件も拾えないと全数ループが 0 回まわって全部緑になる——という、この種の検査の典型的な落とし穴を先回りしている。加えて**検査そのものを実際に壊して落ちることを確かめた記録**（完了報告 §5.4）まで在る。

4. **★★★「選択肢 14 / 引き当て 17」を別の定数へ分けた設計判断。** 同じ配列を使い回すと「選択肢に外した値が戻る」か「既存行が内部コードで出る」のどちらかが**必ず**起きる、という理由づけが正しい。両方の数を Go・フロント・テストの 3 面で同じ言葉（選択肢／引き当て）で書いており、読み手が混同しない。

5. **★★段 1 の計測アンカーを、描画不変の別コミットで先入れした。** `c16e581` の差分を実際に読んで確認したが、`data-testid` の追加だけで `className` を 1 文字も変えていない。⇒ 段 1 の値が「変更前の実物」であることが**差分から検証できる**。前後比較の再現性を担保する形として正しい。

6. **★★「未知の `{...}` はバッジにしない」判断。** 「表示語の埋め忘れをバッジで飾ると、壊れているのに整って見える」という理由づけが本質を突いている。`B04` が消したかった状態を隠さないという一貫性がある。

7. **★★既存行の retired flag が編集・保存で落ちないことを UI テストで固定した。** `ModifiersEditor.test.tsx` の「選択肢から外した flag は、編集して保存しても落ちない」は、束 A-5 / A-6（`recipe_hash` の不変）を**コードの構造ではなく振る舞いで**守る床になっている。指示書は「書き換えないこと」としか言っておらず、この床は製造側の上積みである。

8. **★測れなかったことを、測れなかったと書いた。** 削除 3 値の実 DB 行数について `M37-RESEARCH-01` の値を転記せず（`M-157`）、代わりに「行数に依存しない形で担保した」という代替を実際に置いた。**数を根拠に使った判断が 1 つも無い**ことまで明示している。

9. **★指示書の前提が実物と食い違っていた点（`MAX_BUTTONIZED_OPTIONS` の対象でない）を、実測を添えて報告し、指示書側が用意していた分岐（§2.4-4）へ正しく入った。** 一般形（「上限に当たる」を根拠に形を変える指示は、その欄が本当に上限の仕組みに乗っているかを先に実査すること）まで progress-log の横断課題へ残している。

---

## 制約事項

- 本レビューはコード上で判定可能な範囲のみ対象。実際の動作確認・パフォーマンス・実機テストは別途実施が必要。
- **実 DB での確認はしていない**（クリーンクローンに `*.db` が 0 件）。削除 3 値を持つ既存行が実際に「ジャスト」等と表示されることは、開発者の手元での確認が要る（完了報告 §8-5 と同じ）。
- **キャッシュ再構築後の表示**（`{低ジャンプ}` → バッジ「低空」）は、DB が無いため確認していない。導線の存在（`settings-cache-rebuild` / `preset-cache-rebuild` / `POST /api/presets/:id/recipe-cache/rebuild`）だけをコードで確認した。
- **配色の一致**（`ModifiersSummary` のバッジと `RecipeText` のバッジ）はクラス名の一致（`bg-blue-50` / `text-blue-700` / `rounded` / `px-1.5` / `py-0.5`）でのみ確認しており、実画面での見た目は比較していない。
- 段 1（変更前）の縦寸法は、コードが既に変わっているため再測できない。計測アンカーの先入れコミット（`c16e581`）が描画不変であることを差分で確認したことをもって、報告値の妥当性を担保とした。

---

*以上、M37-06 レビュー報告書。* **★★★束 A・B・C・E・F・G は問題なし。⇒ 唯一の「高」は束 D に付随するもので、コード 2 か所のコメントが「機械検査は無い」と断言したまま失効している点である。** **★★実装・テストには触れずコメントだけで直る。⇒ ただし本プロジェクトの較正では「高」であり、`M37` 完了前に直すこと。**

---

## 取り込み結果（自動トリアージ）

**実施**: 2026-09-14・製造担当（`/implement_plan_full` Phase C）。**★人間トリアージ承認を外した代償として、採否と理由をここへ残す（事後監査用）。**

**★★★「高」指摘の不採用は 0 件である。⇒ Phase C の安全弁（重大指摘の自動棄却時のエスカレーション）は発動していない。**

**再レビュー往復: 0 回**（初回のみ。上限 2 回に達していない）。

| # | 指摘 | 採否 | 理由 | 反映先 |
|---|---|---|---|---|
| **高-1** | 「機械検査は無い」の失効記述がコードコメント 2 か所に残存 | **★採用・是正済み** | 指摘のとおり。本サブ自身が機械検査を新設したのに断言が旧のまま残っていた。**「`check-enum-sync.sh` は flags を見ていない」は今も事実なので残した**（指摘の指示どおり） | `internal/service/notation/resolver.go` ／ `web/src/features/combo/labels.ts` ／ `DES-004` ヘッダ `:8` は設計卓へ請求（完了報告 §7-1） |
| **中-1** | UI 内の 2 択の非対称は本サブが新しく作った軸である | **採用（記録）** | 指摘が正しい。開発者の裁定はサーバ⇄UI 軸であり、この軸を見ていない。**実装は変えない**（`cancel` 側を動かすのは指示書 §3 の射程外） | 完了報告 §10（§J 原稿へ「軸が 2 つある」行） |
| **中-2** | followup の状態更新を名指しで請求していない | **採用（記録）** | `D-838` により製造は同ファイルを編集できない。⇒ 請求しなければ誰も直さない | 完了報告 **§7-4 を新設**（更新対象 3 件） |
| **中-3** | キャッシュ再構築まで同一画面でバッジと平文が混在する | **採用（記録）** | 指摘のとおり `M37-02` の「表示が古いだけ」より一段悪い。**実装では直せない**（古い文字列に新しい表示語は入っていない） | 完了報告 §8-1 |
| **中-4** | CHANGE 原稿・§J 原稿が設計伝達レポート §4 に無い | **★採用・是正済み** | 完了条件 12。指摘の助言どおり「高-1 / 中-1 / 中-2 を反映した版」を移した | `docs/handover/design-reports/` |
| **低-1** | §5.2 の内訳「Go 11」が実測と違う（実際は 8） | **採用・訂正済み** | retired 3 値の行は Go では 1 文字も動いていない。算出（33）と実測（内訳）の性格が混ざらないよう注記も足した | 完了報告 §5.2 |
| **低-2** | §7-2 の `SUPP-001` 引用が原文と表記が違う | **採用・訂正済み** | 設計卓が原文検索で当てるための引用である | 完了報告 §7-2（`:121` を逐語で） |
| **低-3** | 原稿が現行表の最終行「その他」の扱いに触れていない | **採用・追記済み** | 追加 7 値がまさにその枠を埋めた | 完了報告 §7-1 |
| **低-4** | 見出し短縮でインライン面から意味の手掛かりが消えた | **不採用（将来対応として記録）** | **レビュー自身が「判断としては妥当」と認めている**。縦 ±0 を成立させるための意図的な代償であり §4.6 に記録済み。`InfoMark` で戻す案は本サブの射程外（指示書 §2.4 は縦を増やさないことだけを求めている） | 完了報告 §4.6（既存） |
| **低-5** | バッジの `mx-0.5` が半角スペースと二重に効く | **採用・是正済み（一部不採用）** | `mx-0.5` は外した。**`py-0.5` の行ボックス外へのはみ出しは採らない**——外すとバッジが潰れて可読性が落ち、`M37-02` が同型のバッジで既に採っている形でもある | `web/src/features/combo/components/RecipeText.tsx` |
| **低-6** | `notes` の中の既知表示語もバッジ化する | **不採用（記録）** | 実害は「意図せず色が付く」だけ。**切り分けるには文字列から notes の範囲を特定する必要があり、それ自体が同じ脆さを持つ**（`(` は技名にも現れる）。⇒ 脆さを 1 つ増やして得るものが色の正確さだけであり割に合わない | — |
| **低-7** | §5.6 の dialog @1280 が 1px ずれている | **採用・追記済み** | 134 / 135 の揺れ。結論は変わらない。**数値を床にしていないのはこのためである** | 完了報告 §5.6 |

**不採用は 2 件（低-4 / 低-6）。いずれも上表に理由を記した。**

### 取り込み後の再検査（★パイプを挟まずに判定）

```
$ go test ./... > /tmp/.../gotest-after.txt 2>&1; echo "EXIT=$?"
EXIT=0
$ grep -cE "^--- FAIL" /tmp/.../gotest-after.txt
0
```

```
$ cd web && pnpm test > /tmp/.../vitest-after.txt 2>&1; echo "EXIT=$?"
EXIT=0
 Test Files  234 passed (234)
      Tests  2931 passed (2931)
```

```
$ make e2e-only P=m37-06-modifier-height ...; echo "EXIT=$?"
EXIT=0
390x844   inline 64 / dialog 134 / 11 件
1280x720  inline 16 / dialog 134 / 11 件
```

`gofmt -l .` **0 件** ／ `pnpm exec tsc --noEmit` **エラー 0**。

**★`make e2e` の全数は再実行していない。⇒ 取り込みで変えたのはコードコメント 2 か所とバッジの CSS クラス 1 か所であり、レビュー担当が取り込み前の版で全数 EXIT=0（346 passed ＋ 既登録 flaky 1）を実測している。**
