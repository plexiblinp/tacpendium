# M33-02 レビュー報告書

| 項目 | 内容 |
|------|------|
| 対象作業 | **M33-02**（9 群の生成と同一性の検証） |
| 指示書 | `docs/instructions/M33-02-generate-and-verify-groups.md` v1.1.0 |
| チェックリスト | `docs/instructions/reviews/M33-02-review-checklist.md` v1.0.0 |
| 完了報告 | `docs/progress/M33-02-completion-report.md` |
| レビュー実施日 | 2026-09-19 |
| レビュー対象 commit | **`1960328`**（`b90df21` からの差分 247 ファイル） |
| レビュー形態 | fresh subagent（read-only。コードは 1 バイトも変更していない） |

---

## 総評

**比較 4 面を独立に再現した。⇒ ハッシュ 2 本・21 表の行と件数・`sqlite_sequence` 9 行のすべてが完了報告の値と一致し、合わせ込みの形跡は無い。** 層 A 4 群と層 B 5 群の割当も、`GAME_TABLES` と各ファイルの書き込み先を機械的に突き合わせて独立に確認した。**とくに `REUSE.toml` の連番グロブ `migrations/000009_*.sql` が新系列の `users` / `tags` を静かに `CC-BY-SA-4.0` へ落としていた件を、違反 0 件の状態から掘り当てて是正した点は本サブ最大の成果である。** これはチェックリスト §0 の不合格条件 2 そのものが*既に発生していた*ということであり、見逃していればリリース後に戻せなかった。

**★不合格 4 条件はいずれも当たらない。**〔1 合わせ込み＝無し ／ 2 層 A の `_data_`＝無し ／ 3 比較が何も見ていない＝破壊確認 2 件と独立再現で否定 ／ 4 `sqlite_sequence`＝`characters` 34・`combos` と `combo_oki_options` の `seq=0` を再現〕

**一方、成果物そのものではなく「引き継ぎの記述」に問題が集中している。** 失効済みの `SUPP-001` §2.7 の旧本文を現状として引用しており、また本サブの `REUSE.toml` 改変が失効させた設計書 2 か所と、置き換えが失効させた本番ソース 38 か所を棚卸しから落としている。**⇒ どれも動作は正しいままで検査も緑であるため、人が読む以外に見つかる経路が無い。**

---

## 設計準拠性レビュー結果

### 束 A — 同一性の検証

| # | 評価 | 内容 |
|---|---|---|
| **A-1** | **◎** | 4 面すべてが独立の節で出ている。**さらに補助面（CHECK 節）を自発的に足しており、1 面の一致で「全一致」と書いていない。** |
| **A-2** | **◎** | `pragma table_info` / `foreign_key_list` / `index_list` のみ。`sqlite_master.sql` のテキスト比較は 1 か所も無い。`SUPP-001` §5.5.4 規約 (18) の逐語どおり。 |
| **A-3** | **◎** | **完了報告 §10.1 のスクリプトを `M33-01` §5.2.1 と 1 行ずつ突き合わせた。⇒ DB パスの引数化と `行数 =` の 1 行追加を除き同一である。** 値だけの引き継ぎではない。レビュー側で同スクリプトを回し、`92e924e8bf1e58aab59bb8f0e63412b58777730f29515159f4858c6a3333d760` と `59448da12ba91732f3008151750777e962629bd7ac510a8e259b77384126ba26` を再現した。 |
| **A-4** | **○** | 破壊確認は在り、レビュー側でも独立に再現した〔`000005` の先頭データ行を落とす → 件数 1634 / 1633・行ハッシュ不一致・面 1 と面 3 は緑〕。**ただし面 1 を狙った破壊確認が無い**（下記 中-3）。 |
| **A-5** | **◎** | §4.6 に「1 度目で一致」の経緯が段 3 つで残っている。**レビュー側が旧 115 本を `b90df21` から取り出して独立に構築し直した結果も一致した。⇒ 合わせ込みの余地が無いことを外から確認できた。** |
| **A-6** | **◎** | 旧・新とも `go run ./cmd/tacpendium` 経由で構築（`modernc.org/sqlite` ＋ `golang-migrate`）。さらに §1.7 で適用器とアプリ経路の等価性を裏取りしている。 |
| **A-7** | **◎** | 21 表すべてに件数が出ている。レビュー側の独立集計と全表一致。 |

**レビュー側の独立実測（参考）:**

```
旧(b90df21 の 115 本)   正規化 92e924e8…  seq 59448da1…  252 行 / 21 表
新(HEAD の 9 本)        正規化 92e924e8…  seq 59448da1…  252 行 / 21 表
行比較 21 表すべて一致（users.created_at の 1 セルのみ除外）／ mismatched tables: []
```

### 束 B — ライセンスの層

| # | 評価 | 内容 |
|---|---|---|
| **B-1** | **◎** | `000001` / `000002` / `000007` / `000009` に `_data_` は無い。 |
| **B-2** | **◎** | `000003` / `000004` / `000005` / `000006` / `000008` に `_data_` が在る。 |
| **B-3** | **◎** | §5.2 の破壊確認が在り、**さらに §5.2.1 で「凍結表に無い新番号なら緑のまま層 B へ落ちる」死角を一時ファイルで実測している。** レビュー側で `scripts/check-migration-license.sh` の規則 (4) を読み、`has_data` の枝が `writes_game_table()` を一度も呼ばないことを確認した。**⇒ 死角の申し送りは正確である。** |
| **B-4** | **◎** | §6.4 の表をレビュー側で機械的に再構成した。**層 A の 8 ファイルは `GAME_TABLES` との積が空、層 B の 10 ファイルは自分の表だけへ書く。⇒ 逆向き（層 B の名で層 A の表へ書く）も 0 件である。** |
| **B-5** | **◎** | `000001` に `moves.raw_data` と `preset_aliases.character_id` への `INSERT` / `UPDATE` が無いことを実物で確認。`preset_aliases.character_id` の NULL は 0 件（レビュー側でも実測）。 |

### 束 C — `sqlite_sequence`

| # | 評価 | 内容 |
|---|---|---|
| **C-1** | **◎** | `characters` = 行 31 / `seq` 34。レビュー側の独立構築でも 34。**廃止した `000009` の差 3 が失われていない。** |
| **C-2** | **◎** | `combos` / `combo_oki_options` の `seq=0` の行が在る。`000001` の末尾に `INSERT INTO sqlite_sequence` が明示されている。 |
| **C-3** | **◎** | `preset_aliases` = 行 7635 / `seq` 7814（差 179）。**機序を前提にせず値で合わせている。⇒ 指示書 §2.4 の注記どおりである。** |
| **C-4** | **◎** | 面 3 として独立に実測され、ハッシュも別建てで出ている。 |

### 束 D — 生成の中身

| # | 評価 | 内容 |
|---|---|---|
| **D-1** | **◎** | `000001`〜`000009`。`000118` も `000114` も取っていない。 |
| **D-2** | **◎** | 廃止 8 本の内容は 1 行も入っていない。 |
| **D-3** | **◎** | 群の順序は `S01` → `S02a` → `S02b` → `S03` → `S04` → `S05` → `S06a` → `S06b` → `S07`。`combo_setup_results` は `combo_setups` の後に `CREATE` されている。 |
| **D-4** | **◎** | `UPDATE moves SET original_move_id = …` が 514 行。`DEFERRABLE` の語は新系列に 1 件も無い。 |
| **D-5** | **◎** | `foreign_key_check` 0 件 / `integrity_check` `ok`（レビュー側でも再確認）。 |
| **D-6** | **◎** | 空 DB へ 9 本適用して `version=9` / `dirty=0`。 |
| **down 9 本** | **△** | 8 本は妥当。**`000002_seed_games.down.sql` だけが `sqlite_sequence` の掃除を持たない**（下記 中-2）。 |

### 束 E — 越境と前提

| # | 評価 | 内容 |
|---|---|---|
| **E-1** | **○** | 製造から実測できない事実であることを明示したうえで開発者回答を記録している。**この扱いは正しい。** |
| **E-2** | **◎** | 増分を数えただけでなく、**既存 6 本の書き換え（golden 再生成）まで拾っている。⇒ 指示書が求めた以上の精度である。** |
| **E-3** | **◎** | `find migrations -mindepth 2` は空。`internal/` の差分 0 行。 |
| **E-4** | **◎** | `internal/seedgen/` / `cmd/seedgen/` の差分 0 行（レビュー側でも `git diff --numstat` で確認）。 |
| **E-5** | **◎** | 述語を決めていない。むしろ「時刻では見分けられない」という材料を 1 つ足している。 |
| **E-6** | **◎** | `docs/design/` の差分 0 行。 |
| **E-7** | **◎** | `followup-backlog.md` の差分 0 行。**非マイグレの変更は `REUSE.toml` / `scripts/check-migration-license.sh` / 完了報告の 3 本だけである**（レビュー側で `git diff --numstat` により全数確認）。 |
| **E-8** | **◎** | 6 点とも通過。 |
| **E-9** | **◎** | 使い捨て DB はリポジトリ直下の `scratch-m33-02-*.db`。 |

### 束 F — 報告の形

| # | 評価 | 内容 |
|---|---|---|
| **F-1** | **—** | 設計伝達レポートは本レビュー時点で未作成（Phase E）。**⇒ 判定できない。** |
| **F-2** | **◎** | **レビュー側で `go test ./...` を全量ファイルへ落として再実測した。⇒ `--- FAIL` 158 / `ok` 58 / `no test files` 9 / FAIL パッケージ 2 / `go list` 69 / 605 行。完了報告の数値と 1 つも食い違わない。** 列挙も 17 本を全数、141 本を再現手順つきで残している。 |
| **F-3** | **○** | 「実測 / 算出 / 引用」の書き分けが §0 で宣言され、おおむね守られている。**ただし §9.1 の 2 行が実測値と食い違う**（下記 中-1）。 |
| **F-4** | **△** | §8 の材料そのものは有用。**ただし引き継ぎ材料に失効した引用が混ざっている**（下記 高-1）。 |

---

## 設計準拠性以外の指摘事項

### 高-1 ★★★`SUPP-001` §2.7 の「現状」の引用が失効している

**完了報告 §11 原稿 4 と §13 項 4 が、`SUPP-001` §2.7 の現行本文を次のように引用している。**

> `SUPP-001` §2.7 は「`migrations/*.up.sql` は 102 本、`000001`〜`000103`、欠番は `000012` の 1 件」と書いている

**★実物は違う。** `docs/design/supp-001-detailed-design.md:336` の現行本文は次である（レビュー側で実測）。

```
★現在の欠番はこの 1 件だけである（★★2026-09-13 実測へ更新＝CHANGE-194・M33-01。
migrations/*.up.sql は 111 本、000001〜000112 のうち欠けるのは 000012 のみ）。
```

**⇒ 「102 本 / `000001`〜`000103`」は `CHANGE-194`（`SUPP-001` 1.68.0）で既に是正された*旧記述*であり、同節 340 行目に「★旧記述＝…」として歴史記録の形で残っているだけである。** 本サブ自身の版ゲート（§1.1 の #4）が `SUPP-001` を **v1.74.0** と実測しているにもかかわらず、原稿は `M33-01` 時点の文面をそのまま写している。

**★これが「高」である理由は 2 つある。**

1. **原稿 4 は設計卓が §J へ転記し、`SUPP-001` を as-built 化する根拠として使う。⇒ 起点が違えば「102 → 9」という誤った差分が正本へ入る。** 正しくは **111 → 9 ／ `000112` → `000009` ／ 欠番 1 件 → 0 件**である。
2. **同じ誤りが §13 項 4 にも複製されている。⇒ 1 か所の写し間違いではなく、確かめずに前サブの本文を引き継いだ形である。**

**⇒ 原稿 4 と §13 項 4 の両方で、起点を「111 本 / `000001`〜`000112`」へ直すこと。**

### 高-2 ★★★`REUSE.toml` の凍結表撤去が失効させた設計書 2 か所が棚卸しに無い

**本サブは `REUSE.toml` の表 (4)（層 B ＝ 既存マイグレ 72 本の連番列挙）を撤去した。⇒ 開発者裁定を得た正当な変更である。★しかし、その列挙の*存在を前提にした設計書の記述*が 2 か所在り、完了報告 §13 にも §11 の原稿 4 本にも 1 件も挙がっていない。**

| # | 場所 | 逐語 | なぜ失効したか |
|---|---|---|---|
| **1** | `docs/design/01-tech-stack.md:241`（`DES-001` §5.1 の三層表・層 B の行） | 「`character_data/**`（`seed-progress.md` を除く）／ `docs/seed-data/**` ／ `migrations/*_data_*.sql` ／ **既存マイグレ 72 本（凍結・一度だけ列挙）**」 | **その列挙は `REUSE.toml` から消えた。⇒ 層 B に入るのは `migrations/*_data_*.sql` だけになった** |
| **2** | `docs/design/supp-001-detailed-design.md:371`（§2.7「マイグレーションのライセンスと命名規約」項 2） | 「**既存 102 本（層 A 30 / 層 B 72）は凍結・一度だけ列挙してある。★適用済みマイグレは書き換えないので、この列挙は以後変わらない**」 | **同上。★しかも「以後変わらない」という予言つきであり、本サブがそれを破った当事者である** |

**★あわせて `docs/design/supp-001-detailed-design.md:342` の「★★★本数はこれからも増えるが、`M33-02` が 111 本を 7 群前後へ潰す」も、群数が `D-858` で 9 へ変わったため失効している。**

**★製造が設計書を直接編集しないのは正しい**（`CLAUDE.md` §8）**。⇒ だからこそ、失効させた当事者が §11 の原稿として残すのが本サブの手番であった。** 現状では、この 3 か所は誰の TODO にもなっていない。**⇒ 次に `DES-001` §5.1 を引く担当は「既存マイグレ 72 本が層 B に在る」と読む。★その 72 本はもう 1 本も存在しない。**

### 高-3 ★★★失効した連番参照が本番ソース 38 か所に残っており、棚卸しが「1 件」になっている

**完了報告 §11 原稿 2 は `M33-03` の作業量を「歴史テスト 141 本 / golden 17 stem / dead reference 1 件 / `code-facts` 37% 陳腐化」と実測値として提示している。⇒ 「dead reference 1 件」は `check-doc-refs.sh` の出力であるが、同スクリプトの対象範囲は `CLAUDE.md` / `web/CLAUDE.md` / `.claude/rules/*.md` / `.claude/commands/*.md` の 37 ファイルだけである**（レビュー側でスクリプト出力のヘッダを確認）**。⇒ 本番ソースは 1 バイトも見ていない。**

**★レビュー側で実測した結果を置く**（テストファイルを除いた `internal/` / `cmd/` / `web/src/`）**。**

| 形 | 件数 | 例 |
|---|---|---|
| **パス形**（`migrations/NNNNNN…` を名指し） | **13 か所** | 下表 |
| **番号形**（「マイグレ NNNNNN」「列は NNNNNN」） | **25 か所** | `internal/model/combo.go:44` `マイグレ 000081` ／ `internal/model/move.go:88` `マイグレ 000013` ／ `internal/seedgen/model.go:85` `列は 000039` ほか |

**★★このうち危険なのは「死んだ参照」ではなく「生きているが別の物を指す参照」である。**

| 場所 | 逐語 | 何が起きたか |
|---|---|---|
| `internal/model/tag.go:32` | `// ★値は migrations/000007_seed_initial_tags_user1.up.sql の逐語と一致させてある。` | **そのファイルは存在しない。★同じ内容は `000009_seed_initial_users_tags.up.sql` へ移った** |
| `internal/api/middleware/user.go:30` | `// (既存環境の users は migrations/000007 が入れる id=1 の 1 行だけである)。` | **★`000007` は現在 `seed_presets` である。⇒ 参照先は実在するが、`users` を 1 行も入れない別物である** |
| `internal/service/user/service.go:61` / `:91` | `// (users の初期行は migrations/000007 が入れる id=1 の 'default' のみ)。` ほか | **同上。★同じ誤りが 1 ファイル内で 2 か所** |
| `cmd/tacpendium/main.go:702` | `// presets.id は 1 / 3 / 5 と欠番があり(migrations/000069)、` | **`000069` は存在しない。★`presets` は `000007` へ移った** |
| `internal/repository/combo/repository.go:1560` | `// superseded_by_combo_id は ON DELETE SET NULL を宣言しているが、migrations/000078` | 同左（**存在しない**） |
| `internal/repository/preset/queries.go:129` ／ `internal/service/preset/service.go:276` | `migrations/000075 の「★DB では守れないもの」` | 同左 |
| `cmd/seedgen/main.go:9` | `go run ./cmd/seedgen  # character_data → migrations/000026_*.sql を書く` | 同左 |
| `web/src/features/combo/inputResolutionStage2.ts:40` ／ `web/src/constants/field-requirement.ts:81` | `(migrations/000035)` ／ `(oki_verified / migrations/000096 と同じ形)` | 同左 |
| `internal/repository/tag/repository.go:196` | `(migrations/000001)` | **★たまたま今も正しい**（`000001` は今も `init_schema`）**。⇒ 正しさが偶然である点が、かえって危うい** |

**★完了報告 §13 が挙げた失効箇所は 4 件だけである**〔`code-facts.md` §10 ／ `.claude/commands/precheck_seed_data.md:138` ／ `scripts/release-targets.sh:87` ／ `cmd/seedgen/main.go:142` と `format.go`〕**。⇒ 上の 38 か所は 1 件も含まれていない**（`cmd/seedgen/main.go` は別行が挙がっているだけである）**。**

**★「高」である理由は較正の定義そのものである**——**動作は正しいままで `go test` も `tsc` も `check-doc-refs.sh` も緑になり、人が読む以外に見つかる経路が無い。⇒ そして後任は本文をコピーして注記を読まない。**

**⇒ 求める対応は修正そのものではない**（`M33-03` の射程でよい）**。★原稿 2 の「dead reference 1 件」という実測値の提示を改め、「`check-doc-refs.sh` の対象範囲は 37 ファイルであり、本番ソースの連番参照 38 か所は別勘定である」と書き足すこと。⇒ この数字を信じて `M33-03` の規模を見積もると外れる。**

### 中-1 §9.1 の変更統計が実測と食い違う（`E-225` の観点そのもの）

**完了報告 §9.1 は `git diff --numstat b90df21` の内訳を次のように書いている。⇒ レビュー側で同じコマンドを回した結果と 2 行が食い違う。**

| 区分 | 完了報告 | **実測（`1960328` vs `b90df21`）** |
|---|---|---|
| `REUSE.toml` | 17 insertions / **76 deletions** | **16 insertions / 29 deletions** |
| `scripts/check-migration-license.sh` | 28 / 16 | **29 / 15** |
| `migrations/` 追加（新）14 本 | 13829 insertions | **13829 は 18 本の合計である。⇒ 新規 14 本だけなら 13575** |

**★検算が通らないことで自己検出できた。** 完了報告の総計は `31453 deletions` であるが、内訳を足すと `31409 + 76 + 16 = 31501` になる（実測の内訳は `31409 + 29 + 15 = 31453` で総計と一致する）。

**★★本サブは `E-225` を自ら引いて「`git diff --numstat` にしか痕跡が残らない」と書いた手番である。⇒ その `numstat` の値を写し違えている。** 結論（226 削除 / 14 追加 / 4 置き換え ／ 同名 4 本の意図的再利用）は正しく、**判断は 1 つも変わらない**が、**同じ節の他の数値も引用時に検算されることを期待できなくなる。**

### 中-2 `000002_seed_games.down.sql` だけが `sqlite_sequence` を掃除しない

**新系列の down 9 本のうち、`sqlite_sequence` に行を持つ表を扱う 6 本を見ると、掃除の有無が 1 本だけ割れている。**

| down | `DELETE FROM sqlite_sequence` |
|---|---|
| `000003` characters / `000004` moves / `000007` presets / `000008` preset_aliases / `000009` users・tags | **在る** |
| **`000002` games** | **★無い** |
| `000005` move_commands / `000006` move_derivations | 不要（複合 PK・AUTOINCREMENT 無し） |

**★レビュー側で往復を実測した。**

```
up 9 本適用後        : characters=34 combo_oki_options=0 combos=0 games=1 moves=3236 …
down を 9→2 まで適用 : combo_oki_options=0 combos=0 **games=1**   ← 000001 直後の姿は combos と combo_oki_options の 2 行だけ
再度 up              : 全 9 行が復元（値は一致）
```

**⇒ `Migrate(1)` まで戻したとき、`games|1` が残る。★`000001` 直後の状態と一致しない。** 再 up すれば `games.id` を明示投入するため最終値は変わらず、**現時点で壊れる経路は無い**。**★それでも、これは指示書 §4.3 が名指しした「行数が合っていても `seq` がずれる」型そのものであり、9 本のうち 1 本だけ扱いが違うのは意図ではなく漏れに見える。⇒ `m.Down()` の保証範囲の議論**（`followup` の `three-point-update-down-below-golden-version`）**とは独立に、群の内部で揃えられる。**

### 中-3 正規化ハッシュの死角は CHECK だけではない。**⇒ 実測で示す**

**完了報告 §4.4 は「`pragma table_info` は CHECK 制約を返さない」として補助面を足した。★判断は正しい。⇒ しかし死角はそれだけではなく、報告はそれ以上を列挙していない。**

**★レビュー側で新旧 2 つの DB について、面 1 に現れない属性を網羅的に取って突き合わせた。⇒ 結果は全項目 SAME であり、成果物そのものは正しい。★問題は「見ていない面が在ることが記録されていない」ことである。**

| 面 1 に現れない属性 | 本サブでの実測 | 見えない理由 |
|---|---|---|
| **`AUTOINCREMENT`** | 新旧とも 16 表で一致 | **`pragma table_info` は `pk=1` を返すだけで `AUTOINCREMENT` を返さない** |
| **部分索引の `WHERE` 式** | 1 本（`alias_text_en IS NOT NULL`）で一致 | `index_list` は `partial=1` の旗だけを返す |
| 索引の `DESC` / 照合順序 | 非既定 0 件で一致 | `index_info` は `desc` / `coll` を返さない（`index_xinfo` が要る） |
| 列の `COLLATE` | 0 件で一致 | `table_info` は照合順序を返さない |
| トリガ / ビュー | 0 件で一致 | 射影が `type='table'` しか列挙しない |
| `WITHOUT ROWID` / `STRICT` | 0 件で一致 | 同上 |
| 生成列 | 0 件で一致 | `table_info` は隠し列を返さない（`table_xinfo` が要る） |
| FK の `DEFERRABLE` | — | `foreign_key_list` が返さない |

**★★とくに `AUTOINCREMENT` は実害の形まで再現できたので置く。** レビュー側で `000001` の `setups.id` から `AUTOINCREMENT` を外した DB を作り、比較 4 面と補助面へ通した。

```
面1 正規化スキーマ   92e924e8…  → 一致
面3 sqlite_sequence  59448da1…  → 一致
面2 全行 / 面4 件数  mismatched tables: []
補助 CHECK 節        CHECK equal: True
ground truth         old.db   -> id INTEGER PRIMARY KEY AUTOINCREMENT,
                     probe.db -> id INTEGER PRIMARY KEY,
```

**⇒ 5 面すべてが緑のまま、手書き `S01` の `AUTOINCREMENT` が 1 つ落ちた。**

**★★なぜこの死角が効くのかというと、被覆が最も薄い場所と重なっているからである。**

- **21 表のうち 12 表は行 0 である。⇒ 面 2 / 面 4 はこの 12 表について `0 == 0` しか見ていない。★実質の被覆は 9 表である。**
- **`AUTOINCREMENT` を持つ 16 表のうち 7 表**〔`setups` / `combo_steps` / `setup_steps` / `combo_punishes` / `combo_punish_curations` / `combo_punish_prunings` / `combo_punish_starters`〕**は `sqlite_sequence` に行を持たない。⇒ 面 3 でも見えない。**
- **⇒ この 7 表の DDL を見ているのは面 1 だけであり、その面 1 が `AUTOINCREMENT` を見ない。**

**⇒ 求める対応は 2 つである。**〔1 完了報告 §3.2 / §4.4 へ「面 1 が見ない属性」を上表の形で列挙し、本サブでは全項目を実測して一致を確認したと書くこと ／ 2 `SUPP-001` §5.5.4 規約 (18) 自体がこの死角を持つため、注記を足す候補として原稿へ挙げること。**★規約 (18) は `table_info` / `foreign_key_list` / `index_list` の 3 つしか指定していない。⇒ 次に同じ比較をする担当も同じ死角を踏む。**〕

### 中-4 破壊確認が面 1 を狙っていない

**§5-4 の破壊確認は `move_commands` の 1 行落としであり、報告自身が「面 2 と面 4 だけが落ち、面 1 / 面 3 は緑のままになる」ことを*狙い*として書いている。⇒ 面の独立性を示す設計としては良い。**

**★しかし §3.2 の自認どおり、面 2 / 面 4 は「ほぼトートロジー」である。⇒ 破壊確認が生かしたのは、最も情報量の少ない 2 面だけということになる。**

**★★トートロジーでないと報告自身が言っている面 1**（手書き `S01` と 115 本の `ALTER` の突き合わせ）**に対する破壊確認が無い。⇒ 「面 1 が生きている」ことは実測されていない。** 中-3 の実測は、まさにその面 1 を狙って壊したときに何も起きない例を 1 つ示している。

**⇒ `S01` の 1 列の型・1 本の索引・1 つの FK のいずれかを壊して面 1 が赤くなることを 1 度だけ確かめれば足りる。**

### 中-5 9 本すべてを凍結表へ入れた結果、規則 (4) が現ツリーでは空回りする

**`check-migration-license.sh` の規則は `num in frozen` で分岐する。⇒ `FROZEN_A` ＋ `FROZEN_B` が `000001`〜`000009` を全数覆ったため、現ツリーの 18 ファイルはすべて規則 (3) の枝へ入る。★規則 (4) は 1 度も走らない。**

**⇒ 現ツリーに対する検査の実体は「手書きの凍結表と `REUSE.toml` の解決が一致するか」だけになった。★その凍結表を手で書いたのが本サブである。** §6.1 が暴いた事故は、まさに「`REUSE.toml` と凍結表が*揃って*間違っていたため違反 0 件で通った」形であった。**⇒ 同じ形の事故に対する耐性は、置き換え後も回復していない。**

**★本サブの §6.4（表ごとの割当を目で確かめる）がその代替であり、レビュー側でも機械的に再構成して正しさを確認した。⇒ 判定は「今回は正しい」である。★指摘は「次回も同じ手作業に依存する」という構造の申し送りであり、`M33-03` のガード設計（原稿 1）とまとめて扱うのが安い。**

### 中-6 `check-migration-license.sh` の凍結表の見出しが失効している

`scripts/check-migration-license.sh:71` は置き換え後も次のままである。

```
# 凍結表(★適用済みマイグレは凍結されている＝D-535。⇒ この表は以後変わらない)
```

**★この表は本サブで変わった。⇒ 直下に 2026-09-19 の差し替え理由が 20 行付いているため読み落とす危険は小さいが、見出しの断定が事実と逆である。** 「★例外は `M33` のような系列そのものの置き換えである（下記）」の 1 行で足りる。

### 中-7 `progress-log.md` への索引行が未追記

**`bash scripts/check-progress-log-index.sh` が「作業 ID `m33-02` が `docs/progress/progress-log.md` に現れない」で違反 1 件を出す**（レビュー側で実測）**。⇒ 完了条件 §6-10 が未達である。**

**★完了報告 §13 項 12 が「Phase D で追記する」と明示し、`D-510` に従って「追記済み」と書いていない。⇒ 手順としては正しい。★本レビューの時点では未達である、という事実のみを記録する。**

### 中-8 §3.2 の「半ばトートロジー」という自認の精度

**レビューの観点として「その自認は正確か」を問われたので、両方向で答える。**

| 向き | 内容 |
|---|---|
| **過大評価している側**（トートロジーと言いすぎ） | **面 2 は escaping と型親和性を実際に検査している。⇒ `S01` の列型を取り違えれば、書き出した値が別の型で格納されて行ハッシュが落ちる。★報告自身も「書き出した行が手書きの `S01` を通ること自体が検査である」と補っているため、結論としては釣り合っている** |
| **過小評価している側**（トートロジーの範囲を狭く言いすぎ） | **★21 表のうち 12 表は行 0 であり、面 2 / 面 4 の値は `0 == 0` である。⇒ この 12 表については「ほぼ」ではなく*完全な*トートロジーである。★さらに `REAL` 列は `combos` の 3 列だけであり、同表が 0 行のため浮動小数の往復は 1 度も試されていない** |

**⇒ 自認の向きは正しいが、粒度が粗い。★「9 表については意味が在り、12 表については無い」と分けて書けば、中-3 の死角がどこに集中するかも同時に説明できる。**

### 低-1 §9.1 の本文と表で旧ファイル数が 230 と 226 に割れる

本文は「旧 230 ファイルを `rm` で一括削除してから」、表は「`migrations/` 削除（旧）226」。**★どちらも正しい**（実ファイルは 230 本、うち 4 本は同名で再利用されたため `git` は 226 削除 ＋ 4 変更と表示する）**が、説明が無いと数え違いに見える。** 表へ「＋ 同名再利用 4 ＝ 230」の 1 語で足りる。

### 低-2 `writes_game_table()` の偽陽性

`000001` の `ON UPDATE CASCADE` が `UPDATE(?:\s+OR\s+\w+)?\s+(表名)` に当たり、`cascade` を表名として拾う。**★報告が §6.4 の副産物として自力で発見し、原稿 1 へ申し送っている。⇒ 指摘ではなく確認である。** 害が無い（見落としではなく余分な違反しか生まない）という判断にも同意する。

### 不明

- **不明: `REUSE.toml` の凍結表撤去に関する 2026-09-19 の開発者裁定の逐語が、リポジトリ上に 1 件も無い。** `docs/process/parallel-board.md` の次の裁定番号は `D-898` であり、`M33-02` の項は未起票である（ボードの更新は設計卓の手番であるため、これは製造の落ち度ではない）。**⇒ 裁定の*範囲*が「表 (4) の撤去」までなのか「凍結表の運用そのものの見直し」まで及ぶのかを、レビュー側からは判定できない。** 受理の手番で設計卓が範囲を確認すること。**★成果物の側から見る限り、変更は最小である**〔表 (4) をコメントへ畳んだだけ。既定の層 A と `*_data_*` → 層 B の 2 表は 1 文字も動いていない。層の割当も結果として 1 つも変わっていない〕**。⇒ 範囲を超えた形跡は見当たらない。**

---

## 推奨修正（優先度別）

### 高（M33 完了前に修正必須）

| # | 内容 |
|---|---|
| **高-1** | **`M33-02-completion-report.md` §11 原稿 4 と §13 項 4 の起点を是正する。⇒ 「102 本 / `000001`〜`000103`」は `CHANGE-194` で失効済みの旧記述である。正しくは「111 本 / `000001`〜`000112` / 欠番 `000012` の 1 件」→「9 本 / `000001`〜`000009` / 欠番 0 件」** |
| **高-2** | **`REUSE.toml` の表 (4) 撤去が失効させた設計書 3 か所を §11 の原稿として起こす**〔`DES-001` §5.1 三層表・層 B の行 ／ `SUPP-001` §2.7 ライセンス項 2 ／ `SUPP-001` §2.7 の「7 群前後へ潰す」〕**。★製造は設計書を編集しない。⇒ 原稿として残すところまでが本サブの手番である** |
| **高-3** | **§11 原稿 2 の「dead reference 1 件」に、`check-doc-refs.sh` の対象範囲が 37 ファイル（ルール面のみ）であることと、本番ソースの失効した連番参照 38 か所（パス形 13 ／ 番号形 25）が別勘定であることを書き足す。⇒ とくに `000007` を指す 4 か所は、死んだ参照ではなく*別のファイルを指す生きた参照*であり、静かに誤読される** |

### 中（M33-03 着手と並行可）

| # | 内容 |
|---|---|
| **中-1** | §9.1 の `REUSE.toml` 行と `scripts/check-migration-license.sh` 行を実測値へ是正し、内訳の合計が総計と一致することを示す |
| **中-2** | `000002_seed_games.down.sql` へ `DELETE FROM sqlite_sequence WHERE name = 'games';` を足し、9 本の down の扱いを揃える |
| **中-3** | §3.2 / §4.4 へ「面 1 が見ない属性」を列挙し、本サブでは全項目を実測一致させたことを記す。**★あわせて `SUPP-001` §5.5.4 規約 (18) への注記を原稿として起こす** |
| **中-4** | `S01` の構造を 1 か所壊して面 1 が赤くなることを 1 度確かめ、§5 へ足す |
| **中-5** | 9 本全数を凍結表へ入れたことで規則 (4) が現ツリーで空回りすることを、原稿 1 へ併記する |
| **中-6** | `scripts/check-migration-license.sh:71` の「この表は以後変わらない」へ例外の 1 行を足す |
| **中-7** | Phase D で `progress-log.md` の索引行を追記し、`check-progress-log-index.sh` を緑に戻す |
| **中-8** | §3.2 のトートロジー判定を「行を持つ 9 表 / 行 0 の 12 表」で分けて書く |

### 低（将来対応）

| # | 内容 |
|---|---|
| **低-1** | §9.1 の 230 と 226 の関係を 1 語で補う |
| **低-2** | `writes_game_table()` の `CASCADE` 偽陽性（**既に原稿 1 へ申し送り済み。⇒ 追加対応は不要**） |

---

## 良かった点

**1. ★★★`REUSE.toml` の `000009` の罠を、違反 0 件の状態から掘り当てた。**
これはチェックリスト §0 の不合格条件 2 が*既に起きていた*ということである。**⇒ `users` / `tags` が `CC-BY-SA-4.0` へ静かに解決し、しかも `check-migration-license.sh` の旧凍結表も `000009` を層 B としていたため規則 (3) が黙っていた。** 検査の緑を根拠にせず表ごとの割当を目で確かめる、という指示書 §4.2 の要求を額面どおり実行しなければ、絶対に見つからなかった。**★そして戻せない種類の事故である。**

**2. ★★「一致した」を自分で値引きした。**
§3.2 が面ごとにトートロジー性を分類し、末尾の締めでも「全行と件数はほぼトートロジー」と繰り返している。**⇒ 「全一致しました」を最も強く疑え、というチェックリストの前置きに対して、疑う材料を報告の側から差し出している。★これは本レビューが最初に探した形そのものである。**

**3. ★★`M33-01` のハッシュと食い違ったとき、実装を疑う前にレシピの引き継ぎを裏取りした。**
§1.7 が凍結点 `5576126` の 222 ファイルを取り出して再適用し、`M33-01` の 3 値を再現したうえで「食い違いは土台が動いたせいである」と機序（`000114` / `000117` が足した 2 列）まで示している。**⇒ 規約 (18) の注記が警告する「値だけを引き継いで誤診する」型を、正面から回避した。**

**4. ★増分の数え方が指示書より精密だった。**
指示書 §0.3 は「増えていたら対応表へ追記する」としか求めていないが、**§1.3 は新規 4 本だけでなく golden 再生成による既存 6 本の書き換えまで拾い、「本数だけ数えると足りない」と明示した。⇒ 凍結点の寿命という `M33-01` §0.5 の趣旨を、本数ではなく内容で解釈している。**

**5. ★`users.created_at` を固定しない判断。**
「値を写せば一致する」場面で、**旧の意味論を変えることになるから写さない、と選んでいる。⇒ これこそが合わせ込みの反対である。** レビュー側で時刻既定を持つ列を全数走査したが、行を持つのは `users.created_at` の 1 列だけであり、**除外が必要かつ最小であることを確認した。**

**6. ★赤を隠さなかった。**
`go test ./...` が 2 パッケージ・158 テストで赤である事実を、件数・パッケージ・17 本の全数列挙・141 本の再現手順まで添えて出している。**⇒ レビュー側の再実測と 1 つも食い違わなかった。**

**7. ★`followup-backlog.md` を 1 文字も触らず、原稿 4 本として設計伝達レポートへ回す形を守った**（`D-838`）**。**

---

## 制約事項

- 本レビューはコード上で判定可能な範囲のみ対象。実際の動作確認・パフォーマンス・実機テストは別途実施が必要。
- 並列 0 本（束 E-1）は製造と同様にレビュー側からも実測できない。開発者回答を前提として扱った。
- 2026-09-19 の開発者裁定（`REUSE.toml` の凍結表撤去）の逐語はリポジトリ上に無いため、裁定の範囲の妥当性は判定していない（上記「不明」）。
- 使い捨て DB は本レビューのスクラッチ領域（`/tmp/claude-0/.../scratchpad`）に作成しており、リポジトリ直下へは 1 件も置いていない。`CLAUDE.md` §10 に従い、既存の `scratch-m33-02-*.db` は 1 件も削除していない。

---

*以上、`M33-02` レビュー報告書。* **★★★不合格 4 条件はいずれも当たらず、比較 4 面はレビュー側の独立構築でも再現した。⇒ `M33-overview` §6.2 段 3 の条件は満たされていると判定する。** **★★ただし「高」3 件はいずれも引き継ぎ記述の失効・不足であり、`M33-03` と設計卓の受理がそこを起点にする。⇒ 動作は正しいまま検査も緑であるため、直さなければそのまま次のサブの前提として複製される。**
