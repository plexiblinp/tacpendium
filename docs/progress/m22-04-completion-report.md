# M22-04 完了報告: 競合したときの利用者体験（409 の見せ方・編集を失わせない導線）

| 項目 | 内容 |
|------|------|
| 対象指示書 | `docs/instructions/M22-04-conflict-ux.md` **v1.5.0** ／ チェックリスト `M22-04-review-checklist.md` **v1.3.0**（**★どちらもリポジトリ未反映＝§13-7**） |
| 実施日 | 2026-08-16 |
| ブランチ | `claude/m22-04-implementation-plan-lawbud` |
| CHANGE | **`CHANGE-115`**（**2026-08-16 承認済み＝D-415**。着手前ゲート通過を確認。**未反映**） |
| 関連裁定 | **`D-412`**（三点セット発行） ／ **`D-413`**（`M22-08` との並列可） ／ **`D-415`**（差分の自動提示は作らない） ／ **`D-416`**（着手前実査からの差し戻し 2 件への裁定＝指示書 v1.4.0） ／ **★`D-417`**（**追補 §4.7＝404 の行き止まりを塞ぐ**。指示書 v1.5.0） |
| マイグレ消費 | **0 本**（`ls migrations/` の最大は `000077` のまま。**次に払い出す番号は `000078`** で不変） |
| 状態 | **本体＝完了（開発者の手動確認 3 項目は 2026-08-16 に通過）。★追補 §4.7 を実施済み**（§15） |

> **★本サブはサーバを触らない画面のサブである。** `internal/` の diff は **0 行**（§8）。**守るべきものは 1 つ——利用者が書いたものを消さないこと。**

---

## 1. §3.3 の実査 9 項目の結果

### 1-1 ★409 の応答本文に何が載るか → **案 (A) で確定**

`internal/model/api_error.go` の `NewAPIError(code, message)` が返す形は次のとおりで、**`Details` は `omitempty` かつ版不一致の 3 経路では渡していない**。

```json
{"error":{"code":"version_conflict","message":"…"}}
```

| 経路 | 実装位置 | メッセージ |
|---|---|---|
| `PATCH /api/combos/:id` | `internal/api/combo/handler.go:332` | このコンボは他の処理で更新されました。再取得してください |
| `PUT /api/combos/:id` | 同 `:379` | 同上 |
| `PATCH /api/setups/:id` | `internal/api/setup/handler.go:194` | セットプレイが他で更新されています。最新版を取得してから再実行してください |

- **⇒ `M22-03` 設計伝達レポート §6 の申告どおりであった**（写しではなく実装で確認した＝`E-56`）。**相違なし。**
- **メッセージは資源ごとに異なる**（コードのみ統一）。**⇒ 画面はメッセージを表示に使わず、資源種別を props で受けて自分の文言を出す形にした**（§4）。

### 1-2 コンボ編集・セットプレイ編集の実体と、入力が保持されるか

| 画面 | 実パス | 着手前の `handleError` | 入力の保持 |
|---|---|---|---|
| コンボ編集 | `web/src/features/combo/components/ComboEditor.tsx`（835 行。`handleError` は 437 行付近） | `err.status === 409` でトースト 1 本 | **★保持されていた** |
| セットプレイ編集 | `web/src/pages/SetupEditorPage.tsx`（`handleError` は 77 行付近） | `parsed.isConflict` でインラインバナー ＋ トースト | **★保持されていた** |

- **どちらも `navigate` も state リセットも行っておらず、自動再読込も無かった。** **⇒ 最重要ゲート 1 の現状はセーフ側であり、本サブの仕事は「壊さないこと」＋「伝えること」だった。**
- **★ただしテストが無かった。** 保持は実装の副作用として成立していただけで、契約として固定されていなかった。**⇒ §5.1-1 / §5.1-11 とその破壊確認 A で固定した**（§5）。

### 1-3 ★一意制約違反の 409 の実体と、`isConflict` に巻き込まれていた全数

**409 を返すコードの全数は 11 種**（`version_conflict` を含む）。走査＝`grep -rn 'http.StatusConflict' internal/ --glob '!*_test.go' -A3` を各候補の直前 3 行と突き合わせ。

| コード | 出どころ |
|---|---|
| `version_conflict` | `internal/api/combo/handler.go:332` `:379` ／ `internal/api/setup/handler.go:194` |
| `duplicate_setup` | `internal/api/setup/handler.go:56` `:201` |
| `alias_conflict` | `internal/api/preset/handler.go:218` |
| `preset_name_duplicate` | `internal/api/preset/handler.go:192` |
| `preset_limit_exceeded` ／ `preset_in_use_by_config` | `internal/api/preset/` |
| `tag_name_duplicate` | `internal/api/tag/handler.go:96` `:129` |
| **`tag_in_use`** | **`internal/api/tag/handler.go:160`**（**★レビュー指摘で追記＝当初の表から欠落していた**） |
| `user_name_duplicate` | `internal/api/user/handler.go:82` |
| `combo_not_in_trash` ／ `rush_variant_exists` | `internal/api/combo/` ／ `internal/api/move/handler.go:138` |

> **★当初この表は 10 種しか列挙しておらず、本文の「11 種」と食い違っていた**（レビュー指摘 中 2-b）。`grep -rn 'StatusConflict' internal/ --include=*.go | grep -v _test.go` は **15 行**で、コードの異なり数は **11 種**である。**`saveError.test.ts` の網も 8 コードであり `tag_in_use` を含まない**——ただし同テストの目的は「版不一致でないコードが版不一致にならない」ことの例示であり、全数の網羅ではない。

**⇒ 版不一致と確実に区別できる**（§9.3-2 の停止条件に当たらない）。

#### ★`isConflict` に巻き込まれていた 409 コードの全数（**指示書 §3.3-3 が数え直しを求めた項目**）

**着手前の `web/src/features/setup/errors.ts:26` は `err.status === 409` だけで判定していた。** ただし **`parseSetupApiError` を通るのはセットプレイ系ルートの応答だけ**であり、そこが返す 409 は実測 **2 種類しかない**。

| コード | 巻き込まれていたか | 本サブでの扱い |
|---|---|---|
| `version_conflict` | — | 版不一致として分離（本来の対象） |
| **`duplicate_setup`** | **★巻き込まれていた** | **§4.2-5 に従い分離した**（下記 §3） |
| **上記以外の 9 コード** | **★巻き込まれていない（実測 0 件）** | **直していない** |

- **⇒ 「直さずに残す」対象は 0 件である**（`D-416` の枠を超えていない）。
- 根拠: `internal/api/setup/routes.go` が登録する 12 ルートのうち `http.StatusConflict` を返すのは `CreateSetup`（`:56`）と `UpdateSetup`（`:194` `:201`）のみ。**プリセット別名・タグ・利用者名の 409 はセットプレイ編集画面に届く経路が無い。**

### 1-4 `version` を送っている画面の全数 → **★指示書 §1.3.2 の 4 画面と一致。5 つ目は無かった**

| # | 画面 | 実パス | 本サブの扱い |
|---|---|---|---|
| 1 | コンボ編集 | `ComboEditor.tsx:245`（PATCH）／ `:428`（PUT） | **対象** |
| 2 | セットプレイ編集 | `SetupEditorPage.tsx:110` | **対象** |
| 3 | 本登録昇格 | `PromoteToFinalButton.tsx:49` | **スコープ外**（§1.6-8） |
| 4 | マイコンボ状態変更 | `useUpdateMyComboStatus.ts:23` | **条件付き → 不発動**（§1-9） |

**走査**: `grep -rn "version" web/src --include=*.ts --include=*.tsx | grep -v '\.test\.'` の全ヒットを目視。**5 つ目は無かった**（`E-84` に従い明記する）。`gamepad/profile.ts` ・ `keyboard/keyboard-storage.ts` の `version: 1` はブラウザストレージのキーバージョニングであり、楽観排他とは無関係。

### 1-5 既存のエラー表示の仕組み → **再利用できる。新しい仕組みは作っていない**

- `web/src/components/ui/alert-dialog.tsx`（shadcn）を `PutConfirmDialog` / `CharacterChangeConfirmDialog` が既に使っている。**`DES-006` §11.2 の「保存実行時にモーダルダイアログで表示」を満たす。**
- **★`AlertDialogContent` は `web/src/lib/modal-presence.ts` の共有プリミティブ経路を通るため、`M21-07` の物理入力抑止に自動で参加する。** **⇒ 自作オーバーレイにしてはならない**（同ファイルの注記が反例 2 件を挙げている）。**本サブは自作していない。**

### 1-6 `M22-03` 設計伝達レポートの実パスと HEAD 妥当性

- 実パス: **`docs/handover/design-reports/20260816-m22-03-design-exceptions.md`**（`design-handover` ではなく **`design-exceptions`** 命名）。
- **HEAD で成り立つ**: `git log --oneline -- internal/ web/src/` の最新は `298fafb`（M22-03 取り込み）。`git diff --stat 9101181..HEAD -- internal/ web/` は **0 行**（着手時点）。**⇒ 同レポートの記述は着手時点の HEAD でそのまま成立していた。**

### 1-7 ★`M22-08` との交差点 → **4 点あり。並列は可**

**§10 に全文。** 要点は **(1) i18n が 1 ロケール 1 ファイル ／ (2) `progress-log.md` の末尾 ／ (3) `make e2e` の固定ポート ／ (4) `components/ui/` は双方とも編集しない**。

### 1-8 ★キー変更編集に負けた側（404）にいま何が出るか → **「コンボが見つかりません」だけ**

**★コードを読むだけでなく、E2E スタックで実際に再現させた**（指示書 §3.3-8 の要求）。

再現手順: コンボを作る → 画面で `/combos/:id/edit` を開く → API 側で `PUT /api/combos/:id`（ステップを 2 件へ増やすキー変更）→ 画面側で memo を書いて保存。

```
[probe] 画面テキスト末尾 = … キャンセル 保存 コンボが見つかりません
[probe] memo 入力値 = "A がじっくり書いた内容"
[probe] URL = http://localhost:5273/combos/1/edit
```

| 観測 | 結果 |
|---|---|
| 表示 | **「コンボが見つかりません」**（`internal/api/combo/handler.go:329` の生メッセージがトーストに出るだけ） |
| 入力の保持 | **★保持されていた** |
| 画面遷移 | **無し** |

**⇒ §1.2-2〔エラーだけ出して、そのあと何をすればよいか分からない〕に当たる。§4.3-7 の対象。**

**★404 の 2 つの由来を画面側で区別する手段は無い。** サーバは「ほかの人が作り直した」場合も「本当に削除された」場合も、**同じ `not_found` ＋ 同じメッセージ**を返す（`handler.go:329` / `:376` の単一分岐）。**⇒ 区別しない文言にした**（§4）。

### 1-9 ★マイコンボ状態変更が版不一致で失敗したあと、画面は何を表示しているか → **「元の値へ戻る」。⇒ §4.2-6 は不発動**

**★`page.route` で `PATCH /api/combos/:id` だけを 409 `version_conflict` に差し替え、実際に失敗させて画面を見た**（指示書 §3.3-9 の要求）。

```
[probe] 変更前のプルダウン表示 = "マイコンボから外す"
[probe] PATCH 迎撃回数 = 1
[probe] 失敗後のプルダウン表示 = "マイコンボから外す"
[probe] 判定 = 元の値へ戻る/変わらない
[probe] トースト等の痕跡 = false
```

**⇒ 発動条件（「変えた値のまま残る」）に当たらない。本サブでは塞がない。** followup `mycombo-status-update-fails-silently` へ回す（設計伝達レポート §4 へ候補を書く）。

**構造的な裏付け**（実行結果と一致する）:

1. `MyComboStatusSelect.tsx:29` は `value={currentStatus || "__remove__"}` の**完全な制御コンポーネント**であり、ローカル state を持たない。
2. `useUpdateMyComboStatus.ts` は**楽観更新（`onMutate`）を持たず**、`onSuccess` でのみ `invalidateQueries` する。
3. **⇒ 失敗時にキャッシュが動かないため、表示は元の値のまま。**

**★所見**——裁定が置いた基準「嘘をつかない」で見ると、**この画面は嘘をついていない**。表示は「変わっていない」という真実を示しており、DB の状態と一致している。**懸念された「変えた値が残って保存されたと誤認させる」状態は構造的に起きない。** 無言であること自体は残るが、それは followup の射程である。

---

## 2. ★破壊確認 A / B の結果

### 2.1 破壊確認 A（入力を保持する処理を外す）→ **赤 6 本**

**注入内容**: 両画面の `handleError` で競合を検出したときに、**フォーム state を初期値へリセットする**（「安全側に倒す」つもりの実装を模した）。

```
× §5.1-1 版不一致で拒否されたとき入力が保持されている > 利用者が打った memo がそのまま残る
× §5.1-1 同 > 2 本目の入力(damage)も消えない
× §5.1-2 自動で読み込み直さない > 初期値へ戻らない
× §5.1-11 キー変更編集に負けたとき(404)も入力が保持されている > memo が消えない
× §5.1-1 (セットプレイ編集) > 利用者が打った名前がそのまま残る
× §5.1-1 (セットプレイ編集) > 初期値へ戻らない
Tests  6 failed | 22 passed (28)
```

撤去後、**本番コードの `git diff --stat` は 0 行**。

#### ★A の 1 回目は空振りした（**赤にならなかった項目とその是正**）

**1 回目の A では「レシピ(ステップ)も消えない」が緑のまま通った。** 主張が `recipe-steps-count === "1"` であり、**初期値へ戻す実装ではステップ件数が変わらないため、リセットを検出できなかった**。

- **何が代わりに守っていたか**——**何も守っていなかった。** 主張そのものが鈍く、この失敗モードに対して無力だった（`SUPP-001` §5.5 (10′) の「別の機構が守っていた」型ではない）。
- **是正**——**リセットの検出は、キー項目でない `memo` / `damage` の 2 本が受け持つ形へ作り替えた。** 件数の主張は **「レシピが空にならない」という別の失敗モードの網**として残し、**検出できないことを現場のコメントに明記した**。
- **★あわせて分かったこと**——**ステップを触るとキー項目の変更になり、`hasKeyChanges` が真になって PUT 経路（再登録の確認ダイアログ）へ分岐する。** ⇒ PATCH 経路のテストで入力を作るときは**キー項目を避ける必要がある**。1 回目はこれに気づかず 15 本を巻き込んで落とした。

### 2.2 破壊確認 B（判定をステータス 409 だけへ戻す）→ **赤 13 本・4 ファイル**

**注入内容**: `classifySaveError` の `code === API_ERROR_CODE_VERSION_CONFLICT` を `err.status === 409` へ戻した。

```
× classifySaveError > ★同じ 409 でも一意制約違反は versionConflict にならない > (8 コード分)
× classifySaveError > duplicate_setup は duplicateSetup であって versionConflict ではない
× parseSetupApiError > ★duplicate_setup は版不一致と別の分類になる
× ★エラーコードで分岐している > version_conflict 以外の 409 で競合モーダルを出さない
× §5.1-12 ★duplicate_setup が版不一致の文言で表示されない > 「ほかの人が変更しました」と出ない
× §5.1-12 同 > 「同じ手順のセットプレイが既に登録されている」旨が出る
Tests  13 failed | 38 passed (51)
```

**★分類関数の層と画面の層の両方で赤くなる。** 撤去後、**本番コードの `git diff --stat` は 0 行**。

---

## 3. ★§4.4 のどれを採ったかと理由 → **(A)**

**採った案: (A) エラーコードだけが載る。**

- **根拠**——§1-1 の実査どおり、応答本文は `code` と `message` だけで `details` を持たない。
- **⇒ §4.3 の 1〜3 と 6 だけを作った。**

## 4. ★上書きの道を出したか → **出していない**

- **理由**——案 (A) の帰結（指示書 §4.4）。**相手が何を変えたかを知る手段が「別枠で開く」しか無く、上書きの判断材料が弱い。** 判断材料が無いまま上書きさせると `FR501` の目的（誤上書きの防止）がその場で消える。
- **固定**——`ConflictDialog.test.tsx` と `ComboEditor.conflict.test.tsx` に「上書きの道が無い」ことの主張を置いた（`§5.1-8`）。E2E でもモーダルに「上書き」の文字が出ないことを見ている。

## 3-1. ★`duplicate_setup` の分離の結果（§4.2-5）

| 項目 | 内容 |
|---|---|
| **採った文言** | **「同じ手順のセットプレイが、このコンボにすでに登録されています。手順を変えるか、すでにあるセットプレイをお使いください。」**（i18n キー `conflict.duplicateSetup`。ja/en 両方） |
| **旧文言** | 「他のタブで更新されています。ページを再読み込みしてやり直してください。」（**版不一致の文言だった**） |
| **触った範囲** | `web/src/features/setup/errors.ts`（判定を `classifySaveError` へ委譲） ／ `web/src/pages/SetupEditorPage.tsx`（インラインバナーを分離・`data-testid="setup-editor-duplicate-setup"`） ／ i18n 2 ファイル |
| **巻き込まれていた他の 409 コードの全数** | **★0 件**（§1-3）。**⇒ 直さずに残したものは無い。** セットプレイ系ルートが返す 409 は `version_conflict` と `duplicate_setup` の 2 種類だけである |
| **見え方の変化** | **★変わった。**`CHANGE-115` §2-e の反映対象（`DES-005` §5.9）。§7 に as-built 差として再掲する |

**★`errors.test.ts` は主張を反転させた。** 旧主張「409 の分類はエラーコード文字列に依存しない」は `M22-03` の線引き（見せ方は `M22-04` の担当）を守るものだったが、**`M22-04` でその線引き自体が動いた**。**期待値を緩めるのではなく主張を作り替えた**（`Header.test.tsx` / `D-308` と同じ扱い）。旧テストが持っていた対照実験（ステータスが 409 でなければ衝突にしない）は形を変えて残してある。

## 3-2. ★§4.2-6 を発動させたか → **発動させていない**

- **判定材料**: §1-9 の実査（プルダウンは**元の値へ戻る**）。
- **⇒ 発動条件に当たらないため、`web/src/features/mycombo/` に diff は 1 行も無い。** (a)(b) いずれも足していない。
- followup `mycombo-status-update-fails-silently` へ回す。

## 4-1. ★キー変更編集の 404 をどこまで扱ったか（§4.3-7）

**採った形: 「404 を版不一致と同じ導線に寄せる」。**

| 項目 | 内容 |
|---|---|
| **理由** | 入力の保持だけを担保する案では、§1-8 の実測どおり、**「コンボが見つかりません」で終わったまま**になり、§1.2-2 が残る。**モーダルと文言はすでに作ってあるため、寄せる追加コストは資源種別と同じく props 1 つぶんだった。** |
| **出す導線** | **「閉じる」のみ。** **★「ほかの人の内容を見る」も「読み込み直す」も出さない**——**旧行が論理削除されており、指す先も読み込み直す先も無いため**（`CHANGE-115` §7-1 の射程 3 と同じ理由） |
| **★区別したか・区別せず書いたか** | **★区別せず書いた。** サーバは「作り直された」場合と「本当に削除された」場合に**同じ `not_found` ＋ 同じメッセージ**を返し、画面側に区別する材料が無い（§1-8）。**⇒ 「ほかの人がこのコンボを作り直したか、削除した可能性があります」と両方を並べた。** |
| **固定** | `ComboEditor.conflict.test.tsx` §5.1-11（4 本。うち 1 本は「『ほかの人が変更しました』と断定しない」） ／ E2E シナリオ B' |
| **★適用範囲はコンボ編集だけである** | **★セットプレイ編集では 404 を寄せない**（レビュー指摘 高 3 で是正）。**セットプレイには `PUT`（キー変更編集）が無い**ため、この画面の 404 は **(a) 親コンボが見つからない**（`POST /combos/:comboId/setups`＝`internal/api/setup/handler.go:49`） ／ **(b) 本当に削除された** の 2 つだけで、**「作り直された」は構造的に起こらない**。**⇒ 当初は寄せてしまい、新規登録でも「このセットプレイを作り直したか、削除した可能性があります」と出る形になっていた**——**まだ 1 件も作られていないものについてそう述べる、チェックリスト §9-15 が名指しした型である。** 従来どおりサーバのメッセージをインライン表示する形へ戻し、**4 本のテストで固定した**（`SetupEditorPage.conflict.test.tsx`） |

---

## 5. テスト

### 5.1 §5.1 の 15 項目の対応（**★追補で 14・15 を追加**）

| # | 項目 | 置き場 |
|---|---|---|
| 1 | 版不一致で入力が保持されている（両画面） | `ComboEditor.conflict.test.tsx` ／ `SetupEditorPage.conflict.test.tsx` |
| 2 | 自動で読み込み直さない | `ComboEditor.conflict.test.tsx` |
| 3 | 「保存しました」が出ない | 両ファイル |
| 4 | **破壊確認 A** | §2.1 |
| 5 | 一意制約違反の 409 が「ほかの人が変更しました」と出ない | `saveError.test.ts`（8 コード） ／ `ComboEditor.conflict.test.tsx` |
| 6 | **破壊確認 B** | §2.2 |
| 7 | 相手の内容を見る／読み込み直すの導線。押す前に伝わる | `ConflictDialog.test.tsx` ／ 両画面 ／ E2E B |
| 8 | 上書きの道（出していないことの確認） | `ConflictDialog.test.tsx` ／ `ComboEditor.conflict.test.tsx` |
| 9 | **非回帰**（版が一致するときは保存できる） | 両画面 ／ **E2E シナリオ A** |
| 10 | i18n parity | 既存 `src/locales/locales.test.ts`（両方向を機械検査） |
| 11 | 404 でも入力が保持されている | `ComboEditor.conflict.test.tsx` ／ **E2E B'** |
| 12 | `duplicate_setup` が版不一致の文言で出ない | `SetupEditorPage.conflict.test.tsx` ／ `errors.test.ts` |
| 13 | §4.2-6 発動時のみ | **★不発動のため無し**（§3-2） |
| **14** | **★【追補】404 のモーダルから保存する道と脱出路が出ている** | `ComboEditor.conflict.test.tsx` §5.1-14（4 本） ／ `ConflictDialog.test.tsx`（部品層 3 本） ／ **E2E B' / B''** |
| **15** | **★【追補】「この内容で新しく登録する」で保存が成立する（到達可能性）** | `ComboEditor.conflict.test.tsx` §5.1-15（5 本） ／ **E2E B''**（サーバの行まで突き合わせ） |

### 5.2 E2E（`web/e2e/m22-04-conflict-ux.spec.ts`）

| # | シナリオ | 結果 |
|---|---|---|
| A | 版が一致する通常の保存は、いままでどおり通る | ✓ |
| B | 後から保存した側に「ほかの人が変更した」と出る。★入力が消えていない | ✓ |
| B' | キー変更編集に負けた側（404）にも導線が出て、入力が消えていない | ✓ |
| **B''** | **★【追補】404 から「この内容で新しく登録する」を通すと、書いた内容が実際に保存される**（`GET /api/combos/<新 id>` の `memo` まで突き合わせ） | ✓ |

**★サーバの共有状態を書き換えていない**（`D-399 (1)`）。触るのは自分で作ったコンボだけで、`config` ・プリセット・タグには一切触れない。

---

## 6. §7.2 自己テスト結果（**コマンド自身の出力**）

```
$ go test ./...
ok  52 パッケージ / FAIL 0
（ほかに no test files が 8 件＝cmd/seedgen, internal/api/debug, internal/api/health,
  internal/api/user, internal/infra/log, internal/repository/setplay,
  internal/repository/user, internal/testutil/dbtest。★合計 60 パッケージ）
```

```
$ cd web && pnpm test
 Test Files  163 passed (163)
      Tests  1561 passed (1561)
   Duration  66.44s
```

```
$ make e2e   # レビュー取り込み前
  1 flaky
    [chromium] › e2e/m19-03-setup-results.spec.ts:331:3 › M19-03 セットプレイ成立条件の記録 ›
      E: 全 4 セル未検証ならグリッドを表示しない(hidden-when-empty)
      Error: combo 作成失敗: 500 {"error":{"code":"internal_error", …}}
  135 passed (2.4m)

$ make e2e   # レビュー取り込み後
  136 passed (2.4m)
```

**★取り込み前の flaky 1 件は既知の `e2e-shared-global-resource-parallel`。** `POST /api/combos` が 500 を返す形で、**`M22-03` が観測した `m18-03b-materialize`（`POST /api/combo-punishes` が 500）と同型**。リトライで緑・**failed は 0 件**。**本サブの変更とは無関係**（本サブは E2E で共有状態を書き換えていない）。**⇒ 4 サブ連続の観測になった**（`M22-01` / `M22-02` / `M22-03` / `M22-04`）。**★取り込み後の実行では flaky 0 件**——**同じコードで出たり出なかったりすることが、ここでも確認できた。**

---

## 7. ★`DES-005` §5.7・§5.9 ／ `DES-006` §11 と as-built の差（**全数**）

**設計卓が `CHANGE-115` の反映で使う。**

| # | 対象 | as-built | 備考 |
|---|---|---|---|
| 1 | `DES-006` §11.2 | **版不一致の ERROR は専用モーダル `ConflictDialog` で出す。入力を保持したまま画面に残し、自動で読み込み直さない** | `CHANGE-115` §2-a のとおり |
| 2 | `DES-006` §11.2 | **★分岐はエラーコードで行う。ステータス 409 では分岐しない。** 判定は `web/src/features/combo/saveError.ts` の `classifySaveError` 1 本に集約 | `CHANGE-115` §2-c |
| 3 | `DES-006` §11.2 | **409 を返すコードは実測 11 種**（§1-3）。うち版不一致は `version_conflict` のみ | 反映時の根拠値 |
| 4 | `DES-005` §5.7 | 導線は **[ほかの人の内容を見る]（別枠・`target="_blank"`）／[読み込み直す]（2 段確認）／[閉じる]** の 3 つ。**上書きの道は無い** | §4.4 案 (A) |
| 5 | `DES-005` §5.7 | **★「読み込み直す」は画面ごとの再読込である**（`window.location.reload()`）。部分的な state 再初期化ではない | §11-1 に理由 |
| 6 | **★`DES-005` §5.7** | **★404（キー変更編集の負け側）も同じモーダルで出す。導線は「閉じる」のみ。文言は作り直し／削除を区別しない** | **`CHANGE-115` に無い項目。§1.3.1 由来** |
| 7 | **★`DES-005` §5.9** | **★同一レシピ重複（`duplicate_setup`）の 409 は、版不一致と別の文言でインライン表示する** | **`CHANGE-115` §2-e の反映対象（`D-416` で追加）** |
| 8 | `DES-005` §5.9 | セットプレイの「ほかの人の内容を見る」は**親コンボの詳細画面**を開く（`/combos/:parentComboId`）。**`/setups/:id` は編集画面であり読み取り専用の詳細画面が無いため** | 実装上の制約 |
| **11** | **★`DES-005` §5.9** | **★セットプレイ編集では 404 を競合モーダルへ寄せない。従来どおりサーバのメッセージをインライン表示する** | **★レビュー指摘 高 3 で是正した。** 理由——**セットプレイにはキー変更編集（`PUT`）が無い**（`internal/api/setup/routes.go` は `PATCH /setups/:id` のみ）。この画面の 404 は **(a) `POST /combos/:comboId/setups` が親コンボを見つけられない**（`internal/api/setup/handler.go:49`。「コンボが見つかりません」） ／ **(b) 対象が本当に削除された** の 2 つで、**「作り直された」は構造的に起こらない**。⇒ 寄せると、まだ 1 件も作られていないセットプレイについて「作り直したか、削除した」と述べる形になった |
| **12** | **★`DES-005` §5.9** | **★版不一致ではトーストを出さない**（モーダルのみ）。`DES-005` §5.9 の「保存失敗時はトースト通知（409 重複／バリデーション／通信エラーで出し分け）」は、**版不一致については失効した** | **レビュー指摘 中 2-a。`SetupEditorPage.tsx` が競合分類で `return` しトーストを通らない** |
| **13** | **★`DES-006` §11.2 との不整合（設計卓の判断が要る）** | **★`duplicate_setup` は ERROR でありながら**インライン表示**であり、§11.2 の「ERROR はモーダルダイアログで表示」と整合しない** | **★着手前からの状態を踏襲したものであり本サブが壊したのではない。`CHANGE-115` で §11.2 を触る手番であるため明示して渡す**（レビュー指摘 中 4）。**⇒ 次の担当が「モーダルにすべきだ」と読んで直す余地を残さないこと** |
| **14** | **★`DES-006` §11 の範囲外（as-built としての差）** | **★非競合エラーのフォールバック文言を変えた**——サーバ由来のメッセージが無いとき（通信断）は「通信エラーが発生しました。」の固定文言を出す | **レビュー指摘 高 1 で是正。§11 の表に追記した** |
| 9 | `DES-005` §6.8 | 新規 test-id: `conflict-dialog` ／ `-input-kept` ／ `-view-theirs` ／ `-reload` ／ `-close` ／ `-reload-confirm` ／ `-reload-execute` ／ `-reload-cancel` ／ `setup-editor-duplicate-setup` | ケバブケース。既存 test-id の変更・削除なし |
| 10 | `DES-005` §5.7・§5.9 | **モーダルは `AlertDialogContent` 経由のため、`M21-07` の物理入力抑止に自動で参加する** | `lib/modal-presence.ts` |

---

## 8. §7.3 品質チェック（**実測値**）

基点 = `3494660`（`M22-03` マージ時点）。

| 対象 | diff |
|---|---|
| **`internal/`** | **★0 行**（§2.2-1。本サブは画面のサブである） |
| `migrations/` ／ `character_data/` ／ `docs/design/` | **0 行** |
| `web/src/features/physical-input/` ／ `gamepad/` ／ `keyboard/` | **0 行**（契約 F-4） |
| **`web/src/features/auth/`** | **★0 行**（§2.2-4。`M22-08` との非干渉） |
| `combo_tags` の読み書き（`internal/repository/combo/` ／ `tag/`） | **0 行**（`D-405`） |
| `go.mod` ／ `go.sum` ／ `web/package.json` ／ `web/pnpm-lock.yaml` | **すべて 0 行**（新規依存なし） |
| **`web/src/features/mycombo/`** | **★0 行**（§4.2-6 不発動） |

```
$ bash scripts/check-artifact-integrity.sh   → 結果: 違反なし（検査 11 件の自己検査 OK / 生成物 4 件 OK）
$ bash scripts/check-browser-storage-keys.sh → 結果: 違反なし（台帳 9 件 / 本番コード 8 件・★新規キーなし）
$ bash scripts/check-enum-sync.sh            → 結果: ベースラインどおり(増加なし)
```

**変更ファイルは 14 件・+1558 / -63 行。** うち**テストと E2E が 9 件**である。

---

## 9. ★開発者の手動確認（§7.1・`D-398`）——**未実施。ここで一度止まる**

**★3 項目とも人間の感覚でしか判定できない。** 下記の手順で確認をお願いします。

### 9.1 準備

```bash
cd /home/user/combomgr/web && pnpm install    # 済んでいれば不要
cd /home/user/combomgr && go run ./cmd/combomgr
# 別ターミナル
cd /home/user/combomgr/web && pnpm run dev
```

### 9.2 手順（**2 つのブラウザタブを使う**）

1. コンボを 1 件用意し、その **編集画面を 2 つのタブで開く**（`/combos/<id>/edit`）。
2. **タブ A** でメモ欄に**長めの文章**を書く（消えたら惜しいと感じる量）。**まだ保存しない。**
3. **タブ B** でメモを別の内容に変えて**保存する**（こちらは成功する）。
4. **タブ A** に戻り、**保存を押す。**

### 9.3 確認項目

| # | 確認すること |
|---|---|
| **(1)** | **★タブ A で書いた文章が消えていないこと。** モーダルを閉じたあとも残っていること |
| **(2)** | **何が起きたかと、次に何ができるかが読んで分かること。** 「ほかの人の内容を見る」を押すと**別タブで**詳細が開き、**編集中のタブはそのまま**であること |
| **(3)** | **★「防がれた」と読めること**（単なる失敗・自分のミスに見えないこと） |

**★あわせて確認していただけると助かります**（任意）——**「読み込み直す」を押したとき、実行前に「いま入力している内容は失われます」と出ること。** ここは 2 段階にしてあります。

---

## 10. ★並列相手との突合（`E-121`）——`M22-08`

**★`M22-08` は本サブの実施時点で走っていなかった**（本サブが先に起動し、その交差点報告を見てから投入する段取り＝`D-413`）。**⇒ 突合の相手は「まだ存在しない成果物」であるため、交差点の実査結果を申し送る形で代える。**

| # | 交差点 | 実体 | 重さ | 本サブが打った手 |
|---|---|---|---|---|
| **1** | **i18n** | **★1 ロケール 1 ファイル**（`web/src/locales/ja.json` ／ `en.json`）。名前空間分割ではない | **中** | **新設 `conflict` 名前空間を、ファイル末尾ではなく `comboDetail` と `comboEditor` の間へ入れた**（§4.6-1）。**ja / en 同位置・同順**（§4.6-3）。**⇒ `M22-08` が触る `auth` / `user` / `settings` とは離れている** |
| **2** | **`docs/progress/progress-log.md`** | 両サブが末尾へ索引行を追記する | **中** | **構造的に避けられない。★マージ順を人間が握ること**（衝突しても「両方採る」で解ける） |
| **3** | **`make e2e` の固定ポート** | バックエンド 47390 ／ Vite 5273。**worktree を分けてもポートは分かれない** | **中** | **★同一マシンで 2 本を同時に走らせない。** 時間をずらすか別マシンで回す |
| **4** | **共通のエラー表示部品** | `components/ui/alert-dialog.tsx` | **低** | **本サブは再利用しただけで編集していない**（diff 0）。新規部品は `web/src/components/ConflictDialog.tsx` として独立させた |

**★交差しないことを確認したもの**——`internal/` 全域（本サブ 0 行） ／ `web/src/features/auth/`（同 0 行） ／ `web/src/features/config/`（触っていない） ／ `web/src/components/Header.tsx`（触っていない） ／ `migrations/`（両サブとも消費 0）。

### 10.1 ★`M22-08` §3.3-8 を実施する担当への申し送り

**本節の結論を引き写さないこと**（`D-217`）。**上記は本サブが自分で実査した結果であり、`M22-08` 側も自分で確かめること。** とくに **i18n の追記位置**は、本サブのマージ後に見ると並びが変わっている。

---

## 11. 文言の全数（§4.5）

**★`CHANGE-115` §3 の案から変えた箇所がある。新旧を並べる。**

| # | 場面 | `CHANGE-115` §3 の案 | 採った文言 | 変えた理由 |
|---|---|---|---|---|
| 1 | 版不一致・見出し（コンボ） | ほかの人がこのコンボを変更しました | **同じ** | — |
| 2 | 版不一致・本文（コンボ） | あなたが開いたあとに、ほかの人がこのコンボを変更しています。\nそのまま保存すると、その変更を消してしまうため、いったん止めました。 | **同じ**（改行を除き逐語） | — |
| 3 | 入力の保持 | 入力した内容はこのまま残っています。 | **同じ** | — |
| 4 | ボタン | [ ほかの人の内容を見る ] [ 読み込み直す ] [ 閉じる ] | **同じ** | — |
| 5 | 読み込み直しの確認 | 読み込み直しますか ／ ほかの人の内容を読み込みます。\nいま入力している内容は失われます。 ／ [ 読み込み直す ] [ やめる ] | **同じ** | — |
| 6 | セットプレイ | 「コンボ」を「セットプレイ」に置き換えるだけ | **同じ** | — |
| 7 | **★404（見出し）** | **案が無い**（§3 は 409 だけを想定） | **このコンボは、いまこの形では見つかりませんでした** | **新規。§4.3-7 が「`CHANGE-115` §3 の案を流用してよい」としつつ、404 用の案は無かった** |
| 8 | **★404（本文）** | **案が無い**（指示書 §4.3-7 の線は「ほかの人がこのコンボを作り直したため、保存できませんでした」） | **ほかの人がこのコンボを作り直したか、削除した可能性があります。保存先がなくなっているため、いったん止めました。** | **★指示書の線から変えた。** 指示書の案は「作り直した」と**断定**するが、**404 は本当に削除された場合にも来る**（§4.3-7 自身の但し書き ／ チェックリスト §9-15）。**⇒ 両方を並べる形にした** |
| 9 | **★`duplicate_setup`** | **案が無い**（`D-416` で本サブへ追加された） | **同じ手順のセットプレイが、このコンボにすでに登録されています。手順を変えるか、すでにあるセットプレイをお使いください。** | **新規。§4.5-1〔利用者が自分で決められることを書く〕に従い、次の一手（手順を変える／既存を使う）を添えた** |
| **10** | **★非競合エラーのフォールバック**（コンボ編集） | **案が無い**（`CHANGE-115` の射程外） | **通信エラーが発生しました。**（**サーバ由来のメッセージが無いときだけ**） | **★レビュー指摘 高 1 の是正。** 当初の実装は `parsed.message` を無条件で優先しており、**通信断（`fetch` が `TypeError` を投げる）のとき `Failed to fetch` が利用者へ出る**形になっていた。**§4.5〔文言を一般の利用者に向けて書く〕を掲げるサブでの逆行だったため戻した** |
| **11** | **★英語ロケールの「見る」導線と再読込確認** | **案が無い**（`CHANGE-115` §3 は日本語のみ） | **旧 `See their version` / `This loads their version.` → 新 `See their copy` / `This loads their copy.`** | **★§4.5-5 の en 版。`version` は楽観排他の「版」と読めるため、`copy`（その人の手元の内容）へ改めた。本サブで新設した機械検査（下記）が検出した** |

**★§4.5 の 5 方針の遵守**——「バージョン」「排他」「競合」「コンフリクト」／「安全」／「エラー」「失敗」は **`conflict.*` の全文言で 0 件**。

**★固定の範囲を広げた**（レビュー指摘 中 1）。当初は `ConflictDialog.test.tsx` の `textContent` 走査だけで、**実際に見ていたのは `versionConflict` × `combo` の 1 組だけ**だった——2 段目のダイアログは別の test-id のため `textContent` に含まれず、`notFound` ・ `setup` ・ `duplicateSetup` の文言は 1 度も走査されていなかった。**⇒ 「機械的に固定」という当初の記載は、実際の固定範囲より広く読める形だった。**

**⇒ `web/src/locales/forbidden-words.test.ts` を新設した。** `ja.json` / `en.json` の `conflict.*` を**再帰的に平坦化して走査する**（`locales.test.ts` の parity 検査と同じ形）。**新しい文言を足せば自動で網に入る。** 陽性対照（走査対象が空でないこと）も置いてある（`E-84`）。**★この検査が実際に en の `version` を検出し、§11-11 の是正につながった。**

### 11-1 ★推測で進めた点（§9.2）

| # | 事項 | 採った案と理由 |
|---|---|---|
| 1 | **「読み込み直す」の実装**（§9.2-1 画面上の挙動） | **画面ごとの再読込（`window.location.reload()`）を採った。** 理由——両画面とも初期化が `useState` の遅延初期化や `useEffect` に散っており、**「読み込み直し用の再初期化」を別に書くと初期化経路が 2 本になって必ずドリフトする**（`E-76`）。読み込み直しは**利用者が入力の消失を承知して選んだ操作**であり、部分的に残す意味も無い |
| 2 | **404 のとき導線を出さないこと** | **旧行が論理削除されており、「見る」先も「読み込み直す」先も無い。** 出すと押しても何も起きない導線になる |
| 3 | **セットプレイの「見る」先** | **親コンボの詳細画面**（`parentComboIds[0]`）。`/setups/:id` は編集画面であり、読み取り専用の詳細画面が存在しないため。親が複数あり得る編集モードでは先頭を採り、**不在なら導線を出さない** |
| 4 | **i18n の挿入位置** | **★`ja.json` / `en.json` は元から名前順に並んでいない**（`app, health, common, auth, user, onboarding, comboList, comboDetail, comboEditor, myCombo, compare, …`）。**厳密な名前順挿入は不可能**なため、**`comboDetail` と `comboEditor` の間**（ファイル中央・combo 系の塊の中）へ ja/en 同位置で入れた。**§4.6 の目的〔末尾衝突の回避〕は満たしている** |

---

## 12. 否定形確認（§4.9）——**走査コマンドと結果**

**★陽性対照を先に置いた**（`E-84`）。

```bash
# 走査 0: 陽性対照(必ず当たる)
LC_ALL=C.UTF-8 grep -rn 'M22-04' docs/instructions/M22-overview.md | grep '担当'
#   → 3 件ヒット(181 / 185 / 289 行)。★走査は生きている
```

### 12.1 走査 1: エラーコード `conflict` の残骸（**★単語境界**）

```bash
# 1a. 単語境界つき(★`\b` は `_` を単語構成文字として扱うため文字クラスで書く)
LC_ALL=C.UTF-8 grep -rnE '(^|[^a-zA-Z_])conflict([^a-zA-Z_]|$)' \
  internal/ web/src/ web/e2e/ migrations/ \
  --include=*.go --include=*.ts --include=*.tsx --include=*.sql | grep -v '\.test\.'
#   → 90 件

# 1b. 対照: 境界を外すと version_conflict まで当たる(分離が効いている証拠)
#   → 202 件  ★90 ≠ 202 であることが、境界指定が機能している証拠である

# 1c. ワイヤ上のエラーコード値として残っていないか(文字列リテラル限定)
LC_ALL=C.UTF-8 grep -rn '"conflict"' internal/ web/src/ web/e2e/ \
  --include=*.go --include=*.ts --include=*.tsx
#   → 3 件

# 1d. NewAPIError の第 1 引数に渡している箇所
LC_ALL=C.UTF-8 grep -rnF 'NewAPIError("conflict"' internal/
#   → 0 件
```

**1c の 3 件を全文で確認した結果、いずれも残骸ではない。**

| 位置 | 中身 | 判定 |
|---|---|---|
| `internal/model/api_error.go:6` | `// ★総称の "conflict" を使わない理由:` | **説明のコメント。正しい** |
| `internal/model/api_error_test.go:112` | `if model.ErrorCodeVersionConflict == "conflict" {` | **★`M22-03` が置いた番人テスト。「conflict でないこと」を主張している。正しい** |
| `internal/service/setup/service_test.go:439` | `name := "conflict"` | **セットプレイ名のテスト用固定値。エラーコードと無関係** |

**⇒ 残骸 0 件。** なお 1a の 90 件は `ErrConflict`（Go のセンチネル変数名）・`AliasConflictError`・テスト用のタグ名・本サブが足した i18n 名前空間 `conflict.*` と test-id `conflict-dialog` である。

### 12.2 走査 2: 「409 の見せ方は未定」旨の記述

```bash
LC_ALL=C.UTF-8 grep -rn '409' docs/ --include=*.md \
  | LC_ALL=C.UTF-8 grep -E '未定|決まっていない|決めていない'
#   → 1 件
```

**唯一のヒットは `docs/instructions/M22-04-conflict-ux.md:253`＝本走査を指示する表の行そのもの**（自己参照）。**⇒ 残骸 0 件。**

### 12.3 走査 3: 「差分を提示する」旨の記述

```bash
LC_ALL=C.UTF-8 grep -rnE '差分|diff|マージ' \
  docs/instructions/M22-overview.md docs/instructions/M22-04-conflict-ux.md \
  docs/change-notes/CHANGE-115-notification.md \
  | LC_ALL=C.UTF-8 grep -E 'M22-04|提示|自動'
#   → 28 件。★当たったファイルを全文で確認した
```

**全ヒットが既に「作らない」と書いている側の記述であった。** とくに **`M22-overview.md:108` の `M22-04` 行は既に「★差分の自動提示は作らない＝`CHANGE-115` §7-1」を含んでいる**（指示書 §11 が名指しした箇所は是正済み）。**⇒ 残骸 0 件。**

---

## 13. ■ 併せて更新が要るもの

| # | 項目 | 状態 |
|---|---|---|
| 1 | **CHANGE 番号の消費** | **★無し。** 本サブは新規採番していない（`CHANGE-115` は設計卓が起票済み）。**⇒ `change-number-registry.md` §1 への追記は不要** |
| 2 | **マイグレ連番の消費** | **★無し。** `ls migrations/` の最大は **`000077`** のまま。**次に払い出す番号は `000078`** で不変 |
| 3 | **版を上げた文書** | **★無し。** 本サブは `docs/design/` を編集していない（diff 0） |
| 4 | **★`M22-overview.md:108` の `CHANGE` 欄が実態と食い違っている** | **★設計卓の手番。** 同行は `115`（**★起票済・未反映**）と書いているが、**`CHANGE-115` は 2026-08-16 に承認済み**（`D-415`。通知書 9 行目は「承認済み」へ更新済み）。**⇒ 「承認済・未反映」へ。** **★`M22-03` の設計伝達レポート §4-1 が `CHANGE-114` で報告したのとまったく同型である**（通知書だけが更新され、overview / ボード / registry が取り残される） |
| 5 | **★`docs/process/m22-contract.md` v1.7.0 §3.3 に `M22-08` の行が無い** | **★設計卓の手番。** 同節は「`M22-01` 〜 `M22-05` は直列必須」と書いたままで、**`M22-08` の行が存在しない**（同サブは `D-408` / `D-409` で後から新設されたため）。**⇒ `D-413` の並列許可は両サブの指示書メタ表にしか無い。** 契約が「ズレ検出の基準」である以上、ここが古いと基準として使えない |
| 6 | `docs/progress/progress-log.md` | **本サブで追記する**（§7.4） |
| 7 | **★指示書 v1.4.0 ／ チェックリスト v1.2.0 ／ 裁定 `D-416` がリポジトリに存在しない** | **★開発者の明示的な指示により、本ブランチへコミットしていない**（2026-08-16。改訂版は本セッションで開発者から直接受領した）。**⇒ 製造側の落ちではないが、状態としては残る。** 実測——`docs/instructions/M22-04-conflict-ux.md` は **v1.3.0** ／ `reviews/M22-04-review-checklist.md` は **v1.1.0** ／ `docs/process/parallel-board.md` は「次の裁定番号 ＝ `D-416`」のままで §3 に `D-416` の裁定ブロックが無い。**⇒ 本報告が根拠として引く §1.3.2 / §1.6-8 / §3.3-9 / §4.2-5 / §4.2-6 / §4.6 / §5.1-12 / §5.1-13 / §7.5-3-1 / §7.5-3-2 を、後任は 1 つも参照できない**（レビュー担当も口頭伝達に依拠して判定せざるを得なかった）。**★`check-instruction-format.sh` は版数の一致を見るが、存在しない改訂は検出対象ですらない。** **⇒ 反映の手番は設計卓・開発者である** |

---

## 15. ★【追補・`D-417`】§4.7 404 の行き止まりを塞ぐ

> **★本節は本体の完了後に実施した追補である**（指示書 v1.5.0 §4.7）。**本体の実装は §1〜§13 が正本であり、本節はその上への差分である。**

### 15.1 §4.7.1 の実査（**1 点**）

**問い**——`POST /api/combos` の要求本文が、編集画面がいま保持している内容から組み立てられるか。

**答え——組み立てられる。§9.3-1 の停止に当たらない。**

| 根拠 | 内容 |
|---|---|
| 組み立て関数 | **`buildCreatePayload()` が既に在る**（`ComboEditor.tsx:210`）。`characterId` ／ `isDraft` ／ ステップ ／ 状況 ／ メタデータ ／ `okiOptions` ／ `tagIds` ／ `situation` を、**いまのフォーム state から**組み立てる |
| 実行経路 | **`runCreate()` が既に在る**（同 `:368`）。`createMut.mutate(buildCreatePayload(), …)` の形で `POST /api/combos` を叩く |
| 成功後の振る舞い | **`handleSaveSuccess` が `navigate('/combos/<新 id>')` ＋ 「保存しました」まで行う**。**⇒ §4.7.2-1 の「保存されたと分かる形」も既存経路で満たせる** |
| **★サーバ変更** | **不要。** `internal/` の diff は **0 行**のまま（§15.8-2） |

### 15.2 足した導線（§4.7.2）

| # | 導線 | test-id | 2 段階 | 実装 |
|---|---|---|---|---|
| 1 | **この内容で新しく登録する** | `conflict-dialog-save-as-new`（確認は `-save-as-new-confirm` / `-execute` / `-cancel`） | **★あり** | `handleConflictSaveAsNew` → `setConflictKind(null)` → **既存の `runCreate()`** |
| 2 | **コンボ一覧へ** | `conflict-dialog-go-to-list`（同 `-go-to-list-*`） | **★あり** | `handleConflictGoToList` → `navigate('/combos')` |

- **★どちらも 404 のときだけ出す。** 版不一致では旧行が生きているため、同じ内容で新しく登録すると重複になるだけであり、進む道は「読み込み直す」が担う。
- **★2 段目の確認ダイアログは 3 操作（読み込み直す／新しく登録する／一覧へ）で 1 つを共用した**。別々に書くと必ずドリフトする（`E-76`）。**既存の `conflict-dialog-reload-confirm` 系の test-id は改名していない**（改名は E2E とコンポーネントテストの回帰になる＝`DES-005` §6.8）。
- **★「上書きの道ではない」ことを現場のコメントに 2 か所残した**（`ConflictDialog.tsx` の冒頭 ／ `ComboEditor.tsx` の `handleConflictSaveAsNew`）。**指示書 §4.7.2 の要求どおり**——書かないと、次の担当が「§4.4 案 (A) に反する」と読んで削る。

### 15.3 §4.7.3 重複の扱い（**新しい見せ方を作っていない**）

**`runCreate()` の `onError` は既存の `handleError` へ入り、`validation` 分類 → `setValidationResult` ＋ `setDuplicateIssue` → 既存の `DuplicateWarning` / `ValidationDisplay` へ落ちる。**

- **§4.7.3-1（新しい見せ方を作らない）は満たした。** 新しいモーダルも新しい文言も作っていない。
- **★そのために競合モーダルを先に閉じる**（`setConflictKind(null)` を `runCreate()` の前に置く）。閉じないと重複の表示がモーダルの裏に隠れる。
- 固定: `ComboEditor.conflict.test.tsx`「★重複（`VAL-C02`）になったら既存のバリデーション表示へ落ちる（新しいモーダルを作らない）」。

#### ★★§4.7.3-2 の前提は、実装上いま成立していない（**レビュー指摘 高 3。設計卓へ申し送り**）

指示書 §4.7.3-2 は「**これは行き止まりではない——「既にあるコンボ」へ行けるためである**」と書くが、**`web/src/features/combo/components/DuplicateWarning.tsx` は `[VAL-C02] <メッセージ>` の文字列と「閉じる」だけを持ち、既存コンボへのリンクを持たない。**

- **⇒ `DES-006` §11.2 の「重複エラーは既存コンボへのリンク」は未実装である**（本サブより前からの状態。本サブが壊したのではない）。
- **★ただし「行き止まり」ではない**——重複で弾かれたあとも**入力は保持されており、競合モーダルへは戻らずエディタに留まる**。利用者は **(a) キー項目を変えて再度「新しく登録する」／(b) いったん保存をあきらめて「コンボ一覧へ」** を選べる。**⇒ 脱出路は §4.7.2-2 が担保している。**
- **★とはいえ §4.7.3-2 が想定した「既にあるコンボへ行ける」形にはなっていない。** **⇒ 設計伝達レポート §4 へ followup 候補として送る**（`duplicate-warning-lacks-link-to-existing-combo`）。

#### ★あわせて 1 件、指示書の記述と実装の食い違いを報告する

**指示書 §4.7.3-1 は「重複の 409」と書くが、`POST /api/combos` の重複は 409 ではなく 400 `validation_failed` である**（`internal/api/combo/handler.go:61-69`。`VAL-C02` は `result.HasError()` の一部として `details.validations` に載る）。

- **⇒ 実装は「409 を落とす」ではなく「400 のバリデーションとして扱う」形であり、既存の `DuplicateWarning` 経路と一致している。** 指示書の意図（既存の見せ方へ落とす）は満たしている。
- **★`DES-006` §11.2 の反映時に、重複が 400 であることを書いてほしい**（409 と書くと、次の担当がステータスで分岐する形を作る＝最重要ゲート 2 の再発）。

### 15.4 ★破壊確認 C の結果（§5.1-14）→ **赤 9 本**

**注入内容**: `onSaveAsNew` と `onGoToList` を `null` で渡す（**＝v1.4.0 の「閉じるだけ」の状態へ戻す**）。

```
× §5.1-14 404 のモーダルから、保存する道と脱出路が出ている > 「閉じる」だけになっていない
× §5.1-14 同 > 「この内容で新しく登録する」は 2 段階である(1 回押しただけでは保存しない)
× §5.1-14 同 > 「コンボ一覧へ」も 2 段階で、入力が失われることを押す前に伝える
× §5.1-15 「この内容で新しく登録する」で保存が成立する > POST 経路(useCreateCombo)が叩かれる
× §5.1-15 同 > ★入力していた内容が要求本文へ載っている(空の payload を送っていない)
× §5.1-15 同 > ★保存が成立すると新しいコンボへ移り、「保存された」ことが分かる
× §5.1-15 同 > 実行後は競合モーダルが閉じている(裏に隠れたまま残らない)
× §5.1-15 同 > ★重複(VAL-C02)になったら既存のバリデーション表示へ落ちる
× §4.7.2-2 ★脱出路が実際に一覧へ着く > 2 段目を承諾すると一覧へ移動する
Tests  9 failed | 20 passed (29)
```

撤去後、**本番コードの `git diff --stat` は 0 行**（`tsc --noEmit` エラーなし）。

#### ★第 2 注入（**レビュー指摘 中で追加**）——「導線は在るが到達できない」

**1 種類の注入では、§5.1-14〔存在〕と §5.1-15〔到達可能性〕が同時に赤くなるため、両者が別の失敗モードを見ていることを証明できない。** ⇒ **導線は出したまま、`handleConflictSaveAsNew` から `runCreate()` の呼び出しだけを外した**（押しても保存されない形）。

```
× §5.1-15 「この内容で新しく登録する」で保存が成立する > POST 経路(useCreateCombo)が叩かれる
× §5.1-15 同 > ★入力していた内容が要求本文へ載っている(空の payload を送っていない)
× §5.1-15 同 > ★保存が成立すると新しいコンボへ移り、「保存された」ことが分かる
× §5.1-15 同 > ★重複(VAL-C02)になったら既存のバリデーション表示へ落ちる
Tests  4 failed | 46 passed (50)
```

**★§5.1-14〔存在〕は緑のままだった。** **⇒ 2 つの主張が別の失敗モードを見ていることが実証された**——存在だけを見ていた v1.4.0 のテストでは、この形を捕まえられない。撤去後 **本番コード diff 0**。

### 15.5 §5.1-15 を「到達可能性」で書いた（**★本サブの反省の核心**）

**★「ボタンが在る」で止めない。** 本サブが行き止まりを全項目緑で通した原因はそこにある。

| 層 | 主張 |
|---|---|
| コンポーネント | **POST 経路が叩かれる ／ 要求本文に利用者の入力が載っている**（`memo` ＝「利用者がじっくり書いた内容」・`damage` ＝ 2800・`version` を**持たない**こと） ／ **成功すると新しいコンボへ移り「保存しました」が出る** ／ 実行後はモーダルが閉じている |
| **E2E（新規シナリオ B''）** | **★サーバに実際に新しい行ができ、利用者が書いた `memo` がそのまま入っていることまで見る。** `PUT` で旧行を消す → 画面で 404 を起こす → モーダルから 2 段階を通す → `waitForURL(/\/combos\/\d+$/)` → **`GET /api/combos/<新 id>` の `memo` を突き合わせる** |

### 15.6 文言（追補分・§4.5 準拠）

| キー | ja | en |
|---|---|---|
| `conflict.saveAsNew` | この内容で新しく登録する | Save this as a new combo |
| `conflict.saveAsNewConfirm.title` | 新しいコンボとして登録しますか | Save as a new combo? |
| `conflict.saveAsNewConfirm.body` | いま入力している内容を、新しいコンボとして登録します。ほかの人が加えた変更は、そのまま残ります。 | What you typed will be saved as a new combo. Anything the other person changed stays as it is. |
| `conflict.saveAsNewConfirm.confirm` / `.cancel` | 新しく登録する ／ やめる | Save as new ／ Cancel |
| `conflict.goToList` | コンボ一覧へ | Go to the combo list |
| `conflict.goToListConfirm.title` | コンボ一覧へ移動しますか | Go to the combo list? |
| `conflict.goToListConfirm.body` | いま入力している内容は失われます。 | What you are typing now will be lost. |
| `conflict.goToListConfirm.confirm` / `.cancel` | 移動する ／ やめる | Go to the list ／ Cancel |

- **★`CHANGE-115` §3 に案が無い**（同節は 404 を想定していない）。**⇒ 全 8 キーが新規である。**
- **★「ほかの人が加えた変更は、そのまま残ります」を入れた**——**これが「上書きではない」ことを利用者へ伝える部分である**（`FR501` の目的と矛盾しないことが読める形）。
- **禁則語 0 件**（`forbidden-words.test.ts` が `conflict.*` を再帰的に走査するため、**追補分も自動で網に入った**）。

### 15.7 §4.6（i18n の位置）を守った

**末尾へ追記していない。** `saveAsNew` / `goToList` は既存の `close` の直後（ラベル群の中）、`saveAsNewConfirm` / `goToListConfirm` は `reloadConfirm` の直後（確認群の中）へ入れた。**ja / en 同位置・同順。**

```
$ git diff -U1 -- web/src/locales/ja.json
@@ -220,2 +220,4 @@      ← ★ファイル末尾(追補後 608 行 / 追補前 594 行)ではない
```

### 15.8 §4.7.4 扱わなかったもの（**指示書どおり**）

| # | 項目 | 状態 |
|---|---|---|
| 1 | ② の生の 404 表示（`ComboDetailPage`） | **触っていない。** followup `raw-error-body-shown-in-generic-error-display` へ。**★§4.7.2-2 の「コンボ一覧へ」を置いたため、②へ入る頻度は減る** |
| 2 | サーバ側の変更 | **`internal/` の diff 0 行** |
| 3 | セットプレイ編集への同じ導線 | **足していない**（§4.7.5 で受理済みのとおり、`setups` にキー変更編集が無くこの行き止まりは構造的に起こらない） |
| 4 | 404 の由来の区別 | **文言は両方を並べたまま**（`conflict.notFound.bodyCombo` は不変） |

### 15.9 追補後の自己テスト結果（**コマンド自身の出力**）

```
$ go test ./...
ok  52 パッケージ / FAIL 0   （★本追補は internal/ を触っていない。非回帰の確認のみ）

$ cd web && pnpm test
 Test Files  163 passed (163)
      Tests  1571 passed (1571)      ← 追補前 1561 本（★+10 本）
   Duration  85.03s

$ make e2e
  137 passed (3.2m)                  ← 追補前 136（★+1＝シナリオ B''）／ failed 0 / flaky 0
```

### 15.10 追補分の as-built 差（**`CHANGE-115` の反映に追加**）

| # | 対象 | as-built |
|---|---|---|
| 15 | **`DES-005` §5.7** | **★404 のモーダルは「この内容で新しく登録する」（2 段階・`POST /api/combos`）と「コンボ一覧へ」（2 段階）を持つ。** 「閉じる」だけにしない |
| 16 | **`DES-005` §5.7** | **★「この内容で新しく登録する」は上書きの道ではない**——別の行を新しく作るため、`FR501` とも §4.4 案 (A) とも矛盾しない。**⇒ この理由を設計書側にも書いてほしい。書かないと次の担当が「案 (A) に反する」と読んで削る** |
| 17 | **`DES-006` §11.2** | **★重複（`VAL-C02`）は既存の見せ方へ落とす。** 競合経路から来た場合も新しいモーダルを作らない |

---

## 14. 次ステップ

1. **★開発者の手動確認 3 項目**（§9）。**⇒ ここで一度止まる。**
2. 設計伝達レポートの生成（`/design_handover_report`）。followup の更新候補は §4 へ書く（**製造は `followup-backlog.md` を編集しない**＝`D-382`）。
3. 設計卓が `CHANGE-115` の三点セットを出す（改訂 `DES-005` §5.7・§5.9・§5.11 ＋ `DES-006` §11.2 ／ `change-report-115.md` ／ registry の「反映済み」化）。**★`DES-005` §5.9 の `duplicate_setup` 分離（§2-e）を含めること。**
4. `M22-05` の三点セットを発行する。

---

*以上、M22-04 完了報告。**守るべきものは 1 つ——利用者が書いたものを消さないこと。** **409 は 2 種類ある。ステータスで分岐しないこと。** **そして、409 が来ない負け方がもう 1 つある。***
