# M24-07 完了報告: 用語・i18n・内部値露出の最終 sweep

| 項目 | 内容 |
|---|---|
| 対象指示書 | `docs/instructions/M24-07-terminology-and-i18n.md` **v1.0.0** |
| チェックリスト | `docs/instructions/reviews/M24-07-review-checklist.md` v1.0.0 |
| CHANGE | **`CHANGE-147`（設計卓が起票済み。★製造は番号を消費していない）** |
| 着手基点 | `d9c1770` |
| 作業日 | 2026-08-30 |
| マイグレ消費 | **0 本**（次に払い出すのは `000080`） |
| 新規依存 | **0** |
| ブラウザストレージ新キー | **0**（`recipe-full-view-v1` は改名していない） |

---

## 1. 実査 14 件の結果（§3.3・すべて母数付き）

### ★★実査 1: レシピ表記の語は `recipe_cache` に焼かれているか → **焼かれている**

生成は `internal/service/notation/resolver.go` の `resolveRecipe`（:36-48）、書き込みは `UpdateRecipeCacheTx`（`cache.go:87`）。**書き込み経路 5 系統 ＋ マイグレ 1 本**（`000012_seed_combos_durability.up.sql:190-203`）。

**表示はキャッシュをそのまま出す。** `internal/service/combo/service.go:438`（詳細）/ `:520`（一覧）が `extractDefaultRecipe(RecipeCache)` を呼ぶだけで、`internal/model/recipe.go:22-31` は JSON をデコードしてキーを引くのみ。**再組み立てするのは論理削除済み行の 2 経路だけ**（`service.go:468-484` / `setup_resolver.go:83-119`）。

焼かれている日本語表示語:

| 種別 | 母数 | 実値 |
|---|---|---|
| 非技ステップ | **2 / 2** | `"生ラッシュ"` / `"キャンセルラッシュ"`（`resolver.go:16-19`） |
| フラグ | **4 / 4** | `{ジャスト}` `{ディレイ}` `{目押し}` `{最低空}`（`resolver.go:21-26`） |
| 技名（`preset_aliases.alias_text`） | `official_ja_move` **1462 件がほぼ全件日本語** | 「前ダッシュ」「立ち弱P」等 |
| 利用者の `Notes` | 自由入力 | 任意の日本語が入りうる（`resolver.go:157-159`） |

**⇒ これらの語は本サブで変えていない。** 根拠は**契約 F-1 ではなく指示書 §1.6-2 後半**（「表示語は写像で解く。内部表現を変えない」）——F-1 は M20/M21 のレーン分離条項であり、両マイルストーン完了により**レーン分離としての効力は失効している**（**D-614**）。

**★`SM-069` の対象語は `recipe_cache` に入らない。** 「投げ重ね」「ドライブラッシュ」は起き攻めオプションのフロント定数（`web/src/constants/oki.ts`）であり、`internal/` 配下に該当リテラルは **0 件**。⇒ §9.1-1 の停止条件には当たらなかった。

### ★★実査 2: CSV 列名・ZIP エントリ名と語彙の重なり → **日本語語彙とは 0 件**

正典は `internal/service/comboio/csvcore/contract.go`。コンボ **33 列**（必須 22 ＋ 任意 11）、セットプレイ **4 列**、distinct **36 件**。**36 件中 0 件**が日本語。ZIP エントリ名も **2 件中 0 件**（`combos.csv` / `setups.csv`）。

**⇒ 日本語表示語の置換は列名に一切当たらない。** 危険なのは英語トークンでの一括置換であり、とくに **`comboImport.col.*` の 15 キー中 14 が実在の CSV 列名**で `features/combo-io/formatIssue.ts:41` が動的に組む（型検査にかからない結合）。**⇒ 同キーの名前は変えていない。**

**★★観測の所在について、指示書の想定と実態が違った**（§4 の as-built 差分 3 を参照）。

### 実査 3: locale の母数 → **着手前 トップレベル 26 / leaf 607**

指示書 §3.3-3 と ledger の「トップレベル 23 / leaf 487」は**失効値**だった。
着手前は `jq -r 'paths(scalars)|join(".")' | wc -l` で ja 607 / en 607、`diff` で集合一致。
**完了時点は トップレベル 30 / leaf 789**（ja/en とも。集合一致を維持）。

### 実査 4: ハードコード日本語の面の全数 → **followup は全数ではない**

着手前: `web/src` の非テスト **332 ファイル**中、非コメント日本語を持つのは **143 ファイル / 1,862 行**。`pages/` 20 面中 **7 面が `useTranslation` 完全 0**。`ComboImportPage` は `t` を持つが**直接呼び出し 0**（本文 79 行は直書き）。

**完了時点: 333 ファイル中 122 ファイル / 1,409 行**（**21 ファイル・453 行を回収**）。

**⇒ 全面 i18n 化は本サブの射程を超える。** 残存の上位は `IntakeHelperPage`（115 行）/ `ComboEditor`（91）/ `ComboEditorBasicFields`（86）/ `intake/prompt.ts`（71）/ `ComboImportPage`（63）で、いずれも §4.2 が名指ししていない面である。

### 実査 5: 一覧フィルタのラベル定数の軸数 → **フィルタ軸 8 本 / 日本語定数を引くのは 4 本**

指示書の「4 軸」は正しかった（`POSITION_LABELS` / `HIT_TYPE_LABELS` / `OPPONENT_STANCE_LABELS` / `OKI_TECH_TYPE_LABELS`）。残り 4 軸は既に i18n を通っていた。
**選択肢とピルは同一定数を引いており整合していた**（ピル生成は `activeFilterValueLabel()` 1 本に集約）⇒ **`E-76` のドリフトは無かった。**
**★他に在った**: `OPPONENT_SIZE_LABELS` / `SORT_FIELD_LABELS` / `OKI_USES_DR_LABEL` / `OKI_NO_GAUGE_LABEL` / `OKI_ATTACK_TYPE_LABELS`。`constants/` 配下の日本語ラベル定数族は **22**。

### 実査 6: `CO-020` 仮想コントローラ → **70 本 / 実装 8 ファイル中 5 ファイル・`useTranslation` 0/8**

`HitBoxLayout` 34 ／ `SystemRow` 17 ／ `VirtualController` 9 ／ `SpecialMovePanel` 8 ／ `DirectSpecPanel` 2。**⇒ 完了時点で 0 本。**
面の数え上げは共通部品で行った（playbook §4.39）: 到達 page は **2**（`ComboEditorPage` / `SetupEditorPage`）、**ルート 4 経路 / マウント点 3**。

### ★★実査 7: キャラ名の英語表示データ → **在った**

`characters.name_en` は **NOT NULL**（`migrations/000001_init_schema.up.sql:30`）、seed **19/19 体**投入済み（`000079` で M24-02 が全数突合）、Go モデル・API・フロント型 `Character.nameEn` まで到達。
**欠けていたのは表示側だけ** —— `nameEn` を出しているコンポーネントは **0 / 148**、唯一の消費は `CharacterSelector.tsx:68` の検索インデックス。**⇒ 訳語を作る必要は無かった。**

### 実査 8: 内部値露出の全数（`SM-006`＝`SM-059`）→ **9 か所**

ledger の「1 か所」は全数ではなかった。`formatStarterStatus`（`utils.ts:47-59`）の**呼び出し 8 か所** ＋ `HomePage.tsx:83` の直参照 1。
**表示名は着手前の DTO からは取れなかった**（`ComboSummary`/`ComboDetail`/`Combo` に `starterMoveNameJa` が無い）。**punish 側は構造として持っており**（`features/punish/types.ts:41-42`）、それが前例になった。

### 実査 9: `SM-133` の構造 → **区別は構造として在った**

相手の技 = `OpponentMoveNode`（`types.ts:26-31`）、自分の技 = `StarterNode`（`:14-24`）。混在フィールド 0。
**欠けていたのは画面上の帰属ラベル**で、描き分けは `font-semibold` / `font-medium` の太さ差のみだった。⇒ ラベル追加で解けた。

### 実査 10: `SM-139` → **1 本の flex 行に区別なく並ぶ / グループ見出し 0 件**

`ComboListFilters.tsx:225` の 1 つの `div` 直下に 8 個の兄弟。**セットプレイ由来 3 項目がコンボ由来 4 項目とタグの「間」に挟まっていた。**
**★既知の衝突**: `setupResult.corner.midScreen`（「画面中央」）と `POSITION_LABELS.mid_screen`（「画面中央」）は**同一文字列**。ピルだけが軸名前置で回避しており、フィルタ本体では回避されていなかった。

### 実査 11: 語彙の出現箇所

| 語 | 母数 | 備考 |
|---|---|---|
| 「ステップ」（`SM-016` 単位語） | **35 か所**（直書き 32 ＋ locale 3） | 前ターンの「38/39」はコメント行を含んでいた誤り。`wizard.step` は初回ウィザードの手順であり対象外 |
| 「手」（第 3 の語彙） | **2 か所** | `ja.json` `comboEditor.duplicateRealtime` ／ `IntakeHelperPage.tsx:506` |
| 「リンク」（`SM-008`） | **7 か所すべて URL の意味** | **`SM-008` は既に実現済みだった**（`link` フラグは `labels.ts:141` で「目押し」） |
| 「DR」（`SM-069`） | **画面表示 0 件** | 定数コメントが略記禁止を明記済み |
| 「ドライブラッシュ」 | 19 行 / 画面到達 6 定数 | 実文字列は `投げ重ね(その場受け身・ドライブラッシュ)` |
| 「インポート」 | **8 行 / 画面 3 件** | ⇒ 完了時点で 0 |
| 「取込 / 取り込」 | **106 行 / 画面約 43 件** | 統一先として残した |

### ★★実査 12: レシピ表示トグルの逐語 → **キーは 2 件のみ / 面は 5 面**

着手前 ja: `label = "レシピ全文表示"` ／ `hint = "レシピを省略表示と全文表示(ステップごとに改行)で切り替えます。設定は一覧・詳細・比較・比較の追加ダイアログで共有されます"`。
トグル設置は **5 面**（`ComboListPage:208` / `MyComboPage:258` / `ComparePage:105` / `AddComboToCompareModal:78` / `ComboDetailRecipe:57`）。**詳細だけ `compactClassName="whitespace-pre-wrap break-words"`**（他 4 面は `max-w-xs` ×3 / `sm:max-w-[380px]` ×1）＝`CHANGE-147` §2.1.1 の記述どおり。
**★★「4 面」という失効記述が 4 か所 ＋ `web/CLAUDE.md` 台帳 #11 に残っていた**（本サブで是正）。

### 実査 13: `setplay.adoptNameFormat` → **触っていない**

ja `"{{move}} 持続{{n}}F目重ね"` ／ en `"{{move}} meaty on active frame {{n}}"`。使用は `SetplaySuggestionSection.tsx:459-461` の 1 か所。
**★既存テスト `features/setplay/adoptName.test.ts:37,41` が ja/en の書式を逐語で固定済み**であり、§5.1-4 は既に満たされていた。

### 実査 14: 空表示の文言 → **レシピ系 4 種 / 5 箇所。ただし意味は 2 群**

| 逐語 | 出所 | 扱い |
|---|---|---|
| `（レシピなし）`（全角括弧） | 定数 `RECIPE_EMPTY_LABEL` | **正典として残した** |
| `(レシピ未入力)`（半角括弧） | 直書き 2 箇所 | **上へ寄せた** |
| `まだステップがありません。技を選んで「追加」してください。` | 直書き | **畳んでいない**（別の意味） |
| `ステップがありません` | 直書き | **畳んでいない**（別の意味） |

**★followup の 4 種目 `t("trash.setup.unnamed")` は実測では別物**（名前の代替表示）。
**★★「4 種を 1 本へ」は正しくなかった** —— 上 2 種は「表示すべきレシピが無い」、下 2 種は「エディタでまだ入力していない」で**意味が 2 群**。判断は `recipeDisplay.ts` のコメントに残した。

---

## 2. ★★開発者判断（いつ ／ どの問いに ／ 逐語で何と答えられたか）

> playbook §4.52。レビュー担当は製造セッションの会話ログを参照できないため、逐語を独立した節として残す。

すべて **2026-08-30・Plan Mode 内**でのやり取りである。

| # | 問い | 開発者の回答（**逐語**） |
|---|---|---|
| 1 | レシピ表示トグルの語彙をどう決め直すか（3 案提示） | **「「レシピを縦に表示」（推奨）」**（選択肢の選択） |
| 2 | 「インポート」と「取込」のどちらへ揃えるか（4 案提示） | **「取込へ統一。ただし取込ヘルパーは第三の案を模索したいです。命名のポイントは別のアプリやメモ、スプレッドシート、Excel等で管理されていたコンボをこのアプリに取り込む事の支援機能である事です。命名を提案してください。」** |
| 3 | `SM-069` 投げ重ねの DR 表記（3 案提示） | **「変更しない」** |
| 4 | 取込ヘルパーの改名案（3 案提示） | **「「引っ越し取込」（推奨）」** |
| 5 | `SM-016` の初回の問い（3 案提示） | **「判断はまだできません。情報が欲しいので、SM-016の全文と、その元ネタになったとみられるhuman-notes/Memo_Someday.txtの文を表示してください。私の記憶だとコンボやセットプレイの技一つ一つをステップと表現しています。が、ステップという表現に違和感がある上、前方ステップ、後方ステップという移動技名とも被っている事が問題です。といっても「手」はそれはそれで不自然です。統一できるより優れた案を探したいです。まず提案をください。」** |
| 6 | `SM-016` の再質問（衝突解消 ＋ 単位語） | **「複数の問題が重なっている。技名はステップ（ただし英語版はDash）としたい。レシピの要素一つ「コンボを構成する任意の1要素」を指す語は私の方で検討中なので次のターンまで待ってください。」** |
| 7 | `SM-016` 単位語（モック §1〜§3 提示後） | **「再検討したが、工程も違和感があった。場所ごとに最適な表現を見つけるのもコスパに合わない。ただしステップ単体だと技の方と混同をするので、コンボステップという表現にいったん統一し、この表現が抱える問題については、後日課題としてください。」** |
| 8 | `SM-016` 単位語（before/after モック提示後・**最終**） | **「ステップ⇒コンボステップ はモックを見たところ、想定よりも影響箇所が多く、手動確認の手間が大きいため、今回は対応不要とします。（どのみち今後直す可能性があるため）あなたが見つけた箇所についてはレポート等に残しておいてください。それだけでいいです。」** |
| 9 | `SM-016` 英語版 | **「英語版ではCombo Step / Stepという表現は自然なため、残してもいいです。」** |
| 10 | フィルタのラベル定数の扱い（衝突を報告し 4 案提示） | **「案 A: ja.json から導出する（推奨）」** |
| 11 | 消す側 16 か所の確認方法 | **「モックに before/after で出す（推奨）」** |
| **12** | **`SM-133` 帰属ラベルの語を「自分の始動技」へ寄せたこと**（**★製造が独断で決め、事後に諮った**。レビュー 中-6 は「新しい語彙の決定ではなく既存ラベルの整合だから諮る必要は無い」と判断したが、**2 つの既存語のうち片方を選ぶことは行っている**ため、ハード列に触れうるとして事後確認した） | **「特に言及していないものはA-1〜A-4まで問題ありませんでした。」**（実機確認の報告に含まれる形で承認） |
| **13** | **`引っ越し取込` h1 の括弧書き「(他のアプリ・メモ・表計算から)」**（**★開発者の逐語に無い製造の裁量**） | **同上（A-3 として提示し、問題なしと回答）** |

### ★設計卓からの差分指示（同日・開発者経由で受領）

- **D-616**: `SM-016` の「ステップ」は 2 つの意味（単位語 / 技名）。衝突を解くなら動かすのは単位語の側。**制約 2 件＝内部表現の `steps` と API の JSON キーは変えない ／ 影響は画面横断**。据え置き案も残し、衝突が残ることを決める前に明示すること。
- **D-615**: 地上ダッシュの表示語は **`M24-08` へ割り付け**。FE 側の「前方ステップ／後方ステップ」は正しいので触らない。en は Dash。**完了報告には「`M24-08` へ送った」とだけ書けばよい** ⇒ **`M24-08` へ送った。**
- **D-614**: 契約 F-1 の効力は M20/M21 完了により**レーン分離としては失効**。本サブが `internal/service/notation/` `preset/` を触らない根拠は指示書 §1.6-2 後半である。**指示書の改版は不要。実査 §3.3-1 はそのまま実施** ⇒ 実施済み（実査 1）。

---

## 3. 実施した内容（コミット単位）

| コミット | 内容 |
|---|---|
| `0853cf9` / `7c9804b` / （見送り記録） | 語彙決定用モック。3 候補 → 「コンボステップ」統一の before/after → **見送りの明示** |
| `—` | E2E 着手前基準値の採取（**226 passed / 4.2 分 / exit 0**） |
| `—`（工程 2） | 失効記述「4 面」→「5 面」の是正（`RecipeText` / `useRecipeFullView` / `web/CLAUDE.md` 台帳 #11） |
| `4599b28` | **タグ管理画面を画面ごと i18n 化**（7 コンポーネント・62 行・`useTranslation` 0/7 だった） |
| `bd1f16b` | **状況・起き攻め・ソートのラベルを locale へ（案 A）** |
| `e9d8679` | **セットプレイ編集の 3 面を画面ごと i18n 化** |
| `899c4e1` | **仮想コントローラ 70 本を i18n 化**（`CO-020` / `G-4`） |
| `7342ac9` | **開発者が決めた語だけを差し替え**（トグル語彙 ＝ `CHANGE-147` の主対象 / 取込語彙 / 空表示） |
| `1a01ec0` | **内部 move code の露出を消す**（`SM-006` ＝ `SM-059`・9 か所） |
| `eccfcfe` | **確定反撃の帰属ラベル ＋ セットプレイ由来フィルタの識別**（`SM-133` / `SM-139`） |
| `4c9d76e` | **キャラ選択のラベルと並べ替えをロケールへ**（`character-selector-label-is-ja-fixed`） |
| `5f5690f` | **テスト 3 本を新設**（en の日本語混入 / ラベルキー写像 / CSV 列名契約） |

### ★★案 A の設計（フィルタのラベル定数）

**着手前に指示書内の衝突が判明したため、手を止めて開発者へ確認した。**

§4.2 は「一覧フィルタのラベル定数を locale へ」と命じるが、その定数は **10 面以上で共有**されており、消費側に **`useTranslation` が 0 の画面が 3 つ**あった（`ComboEditorBasicFields` 236 行 / `PunishList` 60 行 / `export-model.ts` 34 行）。素直に i18n 化すると、**全面直書きのこれらの画面に英語が 1 個混ざる** —— 指示書 §2.3・チェックリスト §2-4 が**重大**と定める「1 画面で 2 系統が混ざる」に正面から当たる。

⇒ **案 A**（開発者裁定）:

- `constants/` は **「値 → i18n キー」の対応表**（`*_LABEL_KEYS`）を持つ。**本ファイルがラベルの単一の正典**である役割は変えていない
- i18n を通らない画面向けには、**同じ `ja.json` から導出した**日本語の写し（`*_LABELS`）を配る。新設ヘルパ `web/src/lib/ja-label.ts` が唯一の経路
- **⇒ 語の源泉は `ja.json` の 1 つだけ。同じ語を 2 か所に書いていない（`E-76`）**
- `labelFor` / `formatStarterStatus` / `okiOptionLabel` / `okiTechAndGaugeLabel` / `buildDuplicateLabel` は引数に文言の解決関数を取る。**既定は日本語固定**なので直書き画面の呼び出しは変えなくてよい

### ★`SM-006` の方式選定（複数案と理由）

**BE の DTO へ `starterMoveNameJa` を加算**した。

| 案 | 採否 | 理由 |
|---|---|---|
| **BE DTO 加算**（採用） | ◎ | `export-model.ts`（PDF/画像出力）は React 外であり FE フェッチ案では届かない ／ punish 側に同型の前例がある ／ 解決経路が 1 本に閉じる |
| FE で `useMovesByCharacter` を引く | × | 上記のとおり非 React の出力経路へ届かない |

名前解決は `internal/repository/punish` が既に持つ経路と同じ（`official_ja_move` プリセットの `alias_text` を LEFT JOIN）。**名前解決を二度実装していない。**

**★フォールバックの形（§4.3 で報告を求められた事項）**: 3 段（表示名 → move code → `始動技#<id>`）。**move code へ落ちる経路は実在する** —— `alias_text` は `moves` の列ではなく `preset_aliases` 由来であり、ラッシュ版（`rush_variant`）は alias を書かない（`internal/repository/move/rush.go:46-47`）。
**★これは「内部値の露出」には当たらない**と判断した。既存規約（`usePreviewNames.ts:11-16`「未投入キャラ・未知 code は引数コードをそのまま返す＝それ自体がエラーの手がかり」）と同じで、**無言で空にするより情報が多い**ためである。

---

## 4. 英語ロケールで日本語のままにした例外（§5.1-2）

**en の leaf 789 件中、日本語文字を含むのは 1 件だけである。**

| キー | 値 | 理由 |
|---|---|---|
| `settings.basic.languageJa` | `日本語` | **言語選択の選択肢そのもの。** 日本語を選ぶ項目は日本語で出すのが正しい（英語話者にも読める必要がある） |

**★例外はコメントではなくテスト内の一覧へ書いた**（`web/src/locales/no-japanese-in-en.test.ts`）。理由の記載を強制し、「消えたキーが例外に残る」「日本語を含まないのに例外にある」も検出する。

**★もう 1 件あった混入は例外にせず直した** —— `comboEditor.duplicateRealtime` の英文中に和文の中黒「・」が 1 個入っていた。スラッシュ区切りへ揃えた。

---

## 5. 落とした・保留した項目とその理由

| 項目 | 扱い | 理由 |
|---|---|---|
| **`SM-016` レシピの単位語** | **★実施しない** | 開発者判断（§2-8 の逐語）。**影響箇所が想定より多く手動確認の手間が大きい。** 調査結果は §6 へ記録 |
| **`SM-069` 投げ重ねの DR 表記** | **変更しない** | 開発者判断（§2-3）。「DR」略記は既に画面から 0 件であり、主要な不満は解消済みと判断された |
| **地上ダッシュの `alias_text`** | **`M24-08` へ送った** | **D-615** |
| **`setplay.adoptNameFormat`** | **触っていない** | §4.7 の推奨どおり。既存テストが ja/en を逐語で固定済み |
| **`SM-110` の残り 19 語** | **変えていない** | `M24-06` §2-C が「変えにくい（ファイル形式の固有名）」「ドメイン語」「アプリ全体の語」と評価済み |
| **エディタ空状態 2 種の統一** | **畳んでいない** | 実査 14 のとおり**意味が 2 群**であり、「4 種を 1 本へ」は正しくなかった |
| **「手」2 か所の寄せ** | **据え置き** | 寄せ先が単位語の決定に依存する。単位語が決まれば 2 行で済む |
| **`features/combo/labels.ts` の `normal` / `counter` 割れ** | **射程外** | エディタ（`ComboEditorBasicFields` 236 行の直書き）を触らずには畳めない |
| **`RecipeText` の `compactClassName`** | **直していない** | `CHANGE-147` 案 B を採らないため（`SM-022` と逆行する） |

---

## 5.4 ★★開発者の実機確認（2026-08-30・レビュー取り込み後）

**★本節を新設した理由**——**英語 UI は、実装・レビューのどちらも実物を見ていなかった。** レビュー報告書 §制約事項の逐語＝**「英語 UI の見え方（高-4 の二重ラベル、中-1 の `・`、中-2 の日本語フォールバック）は静的読解による判定であり、実機での確認はしていない」**。製造も同じで、判定は grep・型検査・テストだけだった。**⇒ 本サブの目的の半分（「英語 UI で日本語が出る」を消す）が未検証のまま手交されるところだった。**

**★手順は `M24-06`（`e881e2c`）と同じ形にした**——レビュー取り込み → 開発者の実機確認 → 結果を報告へ記録 → 設計卓へ手交。

### 5.4.1 通過した項目

| # | 確認したこと | 結果 |
|---|---|---|
| **A-1-1** | 英語 UI・`/combos/new` の仮想コントローラ。**攻撃ボタン 6 個の下に 2 段目が出ていないこと**（高-4 の是正） | **通過** |
| **A-1-2** | 英語 UI・`/combos/:id` と `/compare` の起き攻めラベル。**区切りがスラッシュ**であること（中-1 の是正） | **通過** |
| **A-1-3** | 英語 UI・`/combos` のキャラ選択。**キャラ名が英語で、並び順も英語名の a〜z**（ラベルと並べ替えが同時） | **通過** |
| **A-1-4** | 英語 UI・`/tags/manage`（工程 3 の面） | **通過** |
| **A-2** | 日本語 UI の語彙 3 件——**「レシピを縦に表示」5 面**／**「取込(CSV)」**／**「引っ越し取込」** | **通過** |
| **A-3** | `引っ越し取込` h1 の括弧書き（**製造の裁量**）の可否 | **通過**（そのままでよい） |
| **A-4** | `SM-133` 帰属ラベルを **「自分の始動技」** へ寄せたこと（**製造が独断で決めた語**）の承認 | **通過** |

**★開発者の逐語＝「特に言及していないものはA-1〜A-4まで問題ありませんでした。」**

### 5.4.2 ★★見つかった 2 面（**直さない。開発者判断**）

**★開発者の逐語＝「全画面を見た訳ではないですが、日本語が残っている部分もありました。が、まずはリリースなのでいったんはこれぐらいでいいかと思っています。」**

| 面 | 開発者の観察（逐語） |
|---|---|
| **`/setups/:id`** | **「基本情報、キャラクター、名前、説明(任意)、レシピ、仮想コントローラの通常技とタブ以外の説明等が日本語のまま」** |
| **`/punish/search`** | **「キャラクターのプルダウン内以外は全部日本語だった」** |

#### ★切り分けた——**どちらも本サブが作った回帰ではない**

**当初は「本サブが §2.3 違反を作った」と見立てたが、着手前（`d9c1770`）の実測がそれを否定した。**

| ファイル | 着手前の `useTranslation` | 現在 | 本サブの差分 |
|---|---|---|---|
| `pages/SetupEditorPage.tsx` | **2（既に i18n 済み）** | 2 | 26 行（工程 5） |
| `features/setup/components/SetupBasicInfoForm.tsx` | **0** | **0** | **無し** |
| `features/setup/components/SetupRecipeEditor.tsx` | **2（部分 i18n）** | 2 | **無し** |
| `features/mycombo/components/CharacterSelector.tsx` | **2（既に i18n 済み）** | 2 | 6 行（工程 10） |

**⇒ 混在は着手前から在った。本サブがしたのは「深めた」ことである。**

**(a) `/setups/:id`** —— 工程 5 は指示書 §4.2 の名指しに従って `SetupEditorPage` の文言を locale へ移したが、**子部品 `SetupBasicInfoForm` を連れて行かなかった**。その結果、**画面の一番上に最初に見える 7 行**（`基本情報` / `キャラクター` / `未選択` / `必須` / `セットプレイ名` / `セットプレイ名は必須です` / `(任意)` / `セットプレイの説明`）が日本語のまま残った。**加えて `SetupRecipeEditor` に 10 行**（`レシピ` / `▶ 全技一覧から選ぶ` / `すべて` / `技を読み込み中...` / `編集ボタンについて` / `上に移動` / `下に移動` / `N ステップ`）——**こちらは着手前から部分 i18n であり、本サブは 1 行も触っていない。**

**★開発者の「仮想コントローラの通常技とタブ以外の説明等が日本語」という観察は正確である**——工程 6 が i18n 化したのは `VirtualController/` 配下 5 ファイル（70 本）であり、**その外側の枠（`SetupRecipeEditor`）は射程外だった。**

**(b) `/punish/search`** —— 工程 10 は `CharacterSelector` のキャラ名と並べ替えをロケールへ従わせた。**同部品は `useTranslation` 0 の全面直書き画面 5 つに埋まっている。**

| 面 | `useTranslation` | 日本語リテラル行 |
|---|---|---|
| `pages/PunishSearchPage.tsx` | **0** | 10 |
| `pages/PunishListPage.tsx` | **0** | 10 |
| `pages/PresetEditPage.tsx` | **0** | 10 |
| `pages/MovesEditGridPage.tsx` | **0** | 4 |
| `pages/IntakeHelperPage.tsx` | **0** | 51 |

**★着手前から `CharacterSelector` 自身は `t()` を 2 か所持っていた**（検索プレースホルダ・該当なしメッセージ）ため、**これら 5 面には元から混在が在った**。工程 10 が変えたのは**キャラ名そのもの**——**画面で最も目に入る要素**なので、混在が一気に見えるようになった。**⇒ 開発者の「プルダウン内以外は全部日本語」は、この構造そのものの観察である。**

#### ★直さない判断と、その根拠

**開発者判断＝案 A（直さない・記録のみ）。** 製造から次の 3 点を添えて諮り、承認された。

1. **どちらも回帰ではない**（着手前から在った混在を深めただけ）。
2. **英語ロケール全体が未完成である**——残存 **122 ファイル / 1,409 行**、`useTranslation` 0 の面が 20 面中 7 面。**2 面を直しても en はリリースできる状態にならない**（`CO-026` は開いたまま）。
3. **部分的に直すと再確認の往復が要る**——リリース優先という判断と合わない。

**★示した代案 2 件はいずれも採らなかった**——(B) `/setups/:id` を閉じる〔`SetupBasicInfoForm` 8 語 ＋ `SetupRecipeEditor` 10 語 × ja/en〕／ (C) `CharacterSelector` に `locale` を渡せるようにし、全面直書きの 5 面は `locale="ja"` でキャラ名を日本語のまま据える〔約 25 行・案 A と同型〕。**⇒ 両方とも `CO-026` の候補として設計伝達レポート §4 へ送る。**

### 5.4.3 ★★レビューはこれを見つけられなかった（**教訓**）

レビュー 中-5 は「1 画面 2 系統混在の残存」を扱ったが、**完了報告 §14-12 の上位 5 ファイル（行数順）から読み取れる範囲だけを見て**、`ComboDetail` / `ComboTableRow` / `Header` の 3 種を挙げ **「いずれも本サブが作った混在ではない」** と結論した。**`CharacterSelector` の消費側を走査していない。** 行数で並べると `PunishSearchPage`(10) は埋もれる。

**★★これは工程 3 で 1 度検出した型そのものである。** `TagSelector` を `t()` で i18n 化したところ、その唯一の消費者（`ComboEditorBasicFields`・全面直書き）で英語が混ざることに気付き、`jaLabel` へ差し替えた。**⇒ 同じ問いを `CharacterSelector`（消費側 12 面・うち全面直書き 5 面）へ向けていれば、実機確認の前に見つかっていた。**

**⇒ 教訓: 共有部品を i18n 化したら、消費側の面を全数挙げて「その面は i18n 側か直書き側か」を 1 件ずつ判定する。** 部品の側だけを見ても混在は見えない。

---

## 6. ★`SM-016` 単位語の調査結果（**将来まとめて直すときの材料**）

> 開発者の逐語＝「あなたが見つけた箇所についてはレポート等に残しておいてください」。

**母数 35 か所**（直書き 32 ＋ locale 3）。`wizard.step`（初回ウィザードの手順）は単位語ではないため対象外。

| 群 | 数 | 例 | 語を消せるか |
|---|---|---|---|
| A. 件数を言うだけ | **10** | `{stepCount} ステップ` ×6 ／ 取込プレビューの列見出し ×2 ／「全 N ステップ」 | ✗ 名詞が要る |
| B. 空状態 | 2 | 「まだステップがありません。技を選んで『追加』してください。」 | ✓「まだ何も入っていません」で足りる |
| C. 操作ラベル・placeholder・aria | 7 | 「ステップ追加」「ステップメモ(任意)」 aria「ステップ削除」 | ✓「追加」「メモ(任意)」「削除」で足りる |
| D. 修飾ダイアログ | 2 | 「ステップ編集: 立ち弱P」 | ✓ 技名が隣にあり語が働いていない |
| E. 解決できないときの代替 | 3 | 「(不明なステップ)」 | ✓「(不明)」で足りる |
| F. バリデーション | 1 | 「レシピを 1 ステップ以上入力してください」 | ✓「1 つ以上」で足りる |
| G. 入力系の説明 | 2 | 「…1 ステップにまとまることがあります」 | ✗ 名詞が要る |
| H. AI プロンプト・取込の説明 | 5 | 出力表の列名「ステップ」ほか | ✓ 変えてよい（下記） |
| I. locale の help / hint | 3 | 「各ステップに付けられる補足設定です」 | △ help 2 は名詞が要る |

**⇒ 語を消せるのは 16 / 35、名詞が要るのは 14 / 35、AI プロンプト 5 / 35。**

### ★★H 群（AI プロンプト）は機械契約ではなかった

パーサは `internal/service/intake/parse.go` の `ParseInput` **1 本のみ** （FE は生テキストを送るだけ）。**列は `cols[0]`〜`cols[6]` の位置参照だけで引いており、ヘッダ文字列との突合は 1 つも無い**（`parse.go:35-59`）。ヘッダ行の読み飛ばしも「**第 1 セルが整数に変換できるか」だけ**で決まる。
**★実証済み** —— `parse_test.go:10` の TSV 版テストは既に **2 列目のヘッダを英語 `step`** にしている。

**⇒ 列名は変えてよい。ただし条件 3 件**: (1) 列数・列順を変えない（index 参照が総崩れになる） (2) `IntakeHelperPage.tsx:309` の UI サンプル表が逐語で二重管理されているため同じ手番で直す (3) **「半角数字。1 から」の指示を必ず残す** —— `parse.go:43` は `strconv.Atoi(cols[1])` の**エラーを捨てている**ため、AI が非数値を書くと `StepOrder` が黙って 0 になる。

### ★E2E の「ステップ」依存 25 行の判定（playbook §4.45 規約 2）

grep 総ヒット **120 行 / 43 spec**。うち**コメント 95 行**、技名由来 0 行。**⇒ 実効 25 行。**

- **要変更 9 行**（UI 文字列に依存）: 空状態の文言 3 件（完全一致）／ `getByLabel` の aria セレクタ 6 件。いずれも「変えてはいけないから固定してある」側
- **変更不要 16 行**: test/describe のタイトル 12、アサーションの失敗メッセージ 2、行末コメント等 2。UI の引用ではなく概念の説明

### ★★落とし穴（着手するときに必ず読むこと）

**「ステップ」は「コンボステップ」の部分文字列である。** ⇒ `toContain("ステップ")` 形のアサーションは**置換後も緑のまま通る**。**「テストが緑だから壊れていない」が成り立たない場面**であり、チェックリスト §0.2 の警告そのものである。

### 後日課題（「コンボステップ」を採る場合に残る論点）

| 論点 | 中身 |
|---|---|
| 件数表示が長い | 「3 コンボステップ」（10 か所）。バッジ・列見出しに収まりにくい |
| 語が働いていない 16 か所 | `DES-004` §2.2 の「内部の生表現は rendered UI に出さない」の延長として将来まとめて畳める |
| 技名との語幹共有 | 「コンボステップ」と技名「前方ステップ」は語幹を共有したまま |

---

## 7. テスト

### 7.1 追加したテスト

| # | 内容 | 所在 |
|---|---|---|
| 1 | **英語ロケールに日本語が出ない**（例外一覧つき・5 本） | `web/src/locales/no-japanese-in-en.test.ts` |
| 2 | **ラベル定数 → i18n キーの写像**（14 本） | `web/src/constants/label-keys.test.ts` |
| 3 | **CSV 列名の契約**（列名・列順の逐語固定 ＋ 非 ASCII 検出・2 本） | `internal/service/comboio/csvcore/column_contract_test.go` |
| 4 | `jaLabel` の解決と補間（5 本） | `web/src/lib/ja-label.test.ts` |
| 5 | 始動状況の 3 段フォールバック（3 本） | `web/src/features/combo/utils.test.ts` |
| 6 | キャラ表示名と並べ替えの対（7 本） | `web/src/lib/character-sort.test.ts` |
| 7 | `StarterMoveNameJa` が返ること | `internal/repository/combo/repository_test.go` |
| 8 | `SM-133` の帰属ラベルが両方に出ること | `web/src/features/punish/components/PunishList.test.tsx` |

### 7.2 既存で足りていたもの（重複を作らない）

| # | 内容 | 所在 |
|---|---|---|
| §5.1-1 | ja/en の leaf キー集合一致（**双方向**・全 789 件対象） | `web/src/locales/locales.test.ts`（既存） |
| §5.1-4 | `setplay.adoptNameFormat` のプレースホルダ | `web/src/features/setplay/adoptName.test.ts:37,41` が **ja/en の書式を逐語で固定**（既存） |

### 7.3 ★★§5.1-5（CSV 列名）は「所在の報告」では足りなかった

**実査で分かったこと**: 既存の Go 往復テスト 7 本は**列名を変えても赤くならない**。`ExportCSV` と `ParseAndValidate` が**同じ `CSVColumns` を読む**ため、両側が対称に変わる。**⇒ 「往復が通る」は列名が変わっていないことの証拠にならない。**

これまで検出できたのは E2E だけ（`web/e2e/support/combo-io.ts:10-33` が 22 列を逐語で持つ）だが、**E2E は nightly であり PR チェックに載らない**（`DES-002` §11.3）。**⇒ 契約の観測を PR で必ず走る側へも置いた。**

### 7.4 既存テストの扱い（playbook §4.45 規約 2 で 1 件ずつ判定）

| 対象 | 判定 | 扱い |
|---|---|---|
| `ComboListFilters.test.tsx` の 4 軸の日本語（3 件） | **たまたま現状を写しただけ** | キー主張へ更新。意図（同じ式から採る）は保たれる |
| **`ComboListFilters.test.tsx`「軸名の前置が『画面中央』の衝突を解消する」** | **★変えてはいけないから固定してある** | 両者がキーになると**衝突が再現せず観測が消える**。⇒ **ja の実値が同一であることを `jaLabel` で源泉に固定し直した** |
| `SetupResultGrid` / `VerifiedConditionsField`（3 件） | たまたま現状を写しただけ | キー主張へ更新（第 3 の語彙を作っていないことは「同じキーを引く」で示す） |
| `SetupEditorPage` 系のタイトル・保存（5 件） | たまたま現状を写しただけ | 各ファイルの作法（キー返しモック）に合わせキー主張へ。**モックに補間値を通した**（コピー元 ID が渡っていることの観測を落とさないため） |
| **`SetupEditorPage.test.tsx`「キャンセルが 2 つあり両方を押す」** | **★変えてはいけないから固定してある** | 2 つが別キーになったため正規表現を 2 キーの選択にした。**両方を数えて両方を押す**という主張は不変 |
| `VirtualController` / `keyboardRecipeInput`（13 件） | 変えていない | **日本語を 1 文字も変えていない**ため、実 `ja.json` を引く既存作法を足しただけ。主張は 1 つも変えていない |
| `RecipeBuilder` / `SetupInputRow` の空表示（2 件） | 意図的に変えた語 | **リテラルではなく定数 `RECIPE_EMPTY_LABEL` を主張する形**へ。正典が動けばテストも動く |
| `PunishList` / `HiddenItemsPanel` の「始動」（2 件） | 意図的に変えた語 | 新ラベルへ追随。**主張（始動技が行の属性として出る＝階層を切らない）は不変** |
| `oki.test.ts` の `.map(okiOptionLabel)` | 署名変更 | **point-free は index を解決関数として渡す罠**。型検査が捕まえた。本番コードに同型は 0 件 |

**★テストは 1 本も減らしていない・skip していない。** 着手前 **202 files / 2244 tests** → 完了時 **205 files / 2278 tests**（増加のみ）。

---

## 8. 破壊確認 5 件（★コマンドと出力）

### 1. `en.json` の 1 キーを日本語へ戻す → **赤**

```
壊した: en comboList.filter.position = ポジション
$ pnpm exec vitest run src/locales/no-japanese-in-en
     57|     expect(violations).toEqual([]);
       |                        ^
 Test Files  1 failed (1)
      Tests  1 failed | 4 passed (5)
```

### 2. `en.json` から 1 キーを消す → **赤**

```
壊した: en から comboList.filter.position を削除
$ pnpm exec vitest run src/locales/locales
 Test Files  1 failed (1)
      Tests  1 failed | 1 passed (2)
```

### 3. ★★CSV 列名を 1 つ新語彙へ置き換える → **既存の往復テストは緑のまま / 新設の契約テストと E2E が赤**

```
壊した: ColRecipe = "recipe" -> "レシピ"

--- (a) 既存の Go 往復テスト ---
$ go test ./internal/service/comboio/... -count=1 -run "RoundTrip"
ok  	github.com/plexiblinp/combomgr/internal/service/comboio	0.481s
ok  	github.com/plexiblinp/combomgr/internal/service/comboio/csvcore	0.003s
        ↑★★緑のまま通る。両側が同じ定数を読むため対称に変わる

--- (b) 本サブで足した列名契約テスト ---
$ go test ./internal/service/comboio/csvcore/... -count=1 -run "TestCSVColumn"
--- FAIL: TestCSVColumnNamesAreFrozen (0.00s)
    column_contract_test.go:42: コンボ CSV の列名/列順が変わっている(DES-002 §7.6 の往復契約)
         got=...,tags,レシピ,sa_gauge_consumed,...
        want=...,tags,recipe,sa_gauge_consumed,...
--- FAIL: TestCSVColumnNamesAreASCII (0.00s)
    column_contract_test.go:75: 列名 "レシピ" に非 ASCII 文字 'レ' が入っている(表示語の置換に巻き込まれた可能性)
FAIL

--- (c) E2E ---
$ make e2e-only P=combo-csv-io
  ✘  3 [chromium] › e2e/combo-csv-io.spec.ts:149:3 › ... (5.4s)
  ✘  4 [chromium] › ... (retry #1) (5.5s)
  2 failed
```

**★★これが本サブで最も重要な観測である。** チェックリスト §1-2 が「緑のままなら契約を守る観測が存在しない」と警告していたとおりの状態が実在した。

### 4. `setplay.adoptNameFormat` から `{{n}}` を落とす → **赤**

```
壊した: ja setplay.adoptNameFormat から {{n}} を削除
$ pnpm exec vitest run src/features/setplay/adoptName
 Test Files  1 failed (1)
      Tests  2 failed | 10 passed (12)
```

### 5. 始動状況を move code 先頭へ戻す → **赤**

```
壊した: 表示名の分岐を never true にして move code 先頭へ戻した
$ pnpm exec vitest run src/features/combo/utils
    134|     expect(formatStarterStatus(combo)).toBe("立ち弱P / 通常 / 画面中央");
       |                                        ^
 Test Files  1 failed (1)
      Tests  1 failed | 41 passed (42)
```

**★5 件とも復旧を確認した**（各回のあと `git status --short` が 0 件）。**破壊確認後の確定実行は絞っていない**（`D-599`）。

---

## 9. 自己テスト結果（§7.2）

| コマンド | 結果 |
|---|---|
| `go test ./... -count=1` | **全 55 パッケージ ok**（exit 0） |
| `cd web && pnpm test` | **207 files / 2291 tests 全数緑**（着手前 202 / 2244） |
| `cd web && pnpm build` | **緑**（型検査はこちら＝`web/CLAUDE.md` §3） |
| `make e2e`（**確定実行・絞っていない**） | **226 passed (3.8m)**・exit 0 |

**★★上表はレビュー取り込み（工程 C）後の最終値である。** レビュー時点の値は 205 files / 2278 tests / 226 passed (3.7m) だった。**⇒ テストは増えただけで、1 本も減っていない。**

**★E2E の着手前基準値**（`D-600` 規約 2・**同一セッション内の背中合わせ**）:

**★★【M24-07 レビュー（質問-1 / 質問-2）を受けて、実行したコマンド行ごと貼る形に改めた】** 初版は結果の要約だけを書いており、レビューからは「パイプで切り詰めていないか」を判定できなかった。**これは報告の欠陥である。**

```
$ make e2e          # 着手前（基点 d9c1770）
226 passed (4.2m)
EXIT=0

$ make e2e          # レビュー取り込み後（確定実行）
226 passed (3.8m)
EXIT=0
```

- **⇒ 件数一致（226）。減っていない。★パイプ・`tail`・`-g` による絞り込みは一切かけていない**（`D-599`。上のコマンド行がそのまま実行したものである）。

**★★確定実行の途中で 1 件赤くなり、直してから採り直している**（隠さず書く）。`e2e/m18-03a-punish-mylist.spec.ts:113` が **strict mode 違反**で落ちた——中-6 の取り込みで帰属ラベルをバッジへ揃えた結果、**始動技名が単独の `span` になり、1 手コンボではレシピ文字列と完全一致で衝突した**。同 spec は「始動技名の span が『自分の始動技 立ち弱P』という連結文字列だから完全一致では当たらない」という**偶然の前提**に依存していた。**⇒ spec 側のセレクタを緩めるのではなく、実装側に `data-testid="punish-mylist-recipe"` を足して「これがレシピの span である」と名乗らせた。主張（レシピが行に出ており、その値が API の返却値と一致する）は変えていない。** 絞り込み再実行（`make e2e-only P=m18-03a-punish-mylist` = 3 passed）で直ったことを見てから、**確定実行を絞らずに採り直した**。

**★ポートの空き確認に `ss` / `netstat` / `lsof` を使っていない**（この環境に無く必ず「空き」と誤答する＝`M24-06` 横断課題 4）。python の `connect_ex` で代替し、**毎回「自前 listener を立てて USED と答えるか」の対照実験を通してから**本番ポートを測った。

---

## 10. 品質チェック（§7.3・★1 行ずつ実際の出力から）

| 検査 | exit | 出力（最終行） |
|---|---|---|
| `scripts/check-artifact-integrity.sh` | **0** | `結果: 違反なし` |
| `scripts/check-stop-discipline.sh` | **0** | `結果: 違反なし` |
| `scripts/check-md-emphasis.sh` | **0** | `結果: 違反なし`（★取り込み中に一度 **exit 1 / ベースラインから 2 行増加** になった。閉じない強調 2 行を直して緑に戻した） |
| `scripts/check-doc-refs.sh` | **0** | `結果: dead reference なし(例示 8 件は ALLOW 表で除外。--list-allow で一覧)` |
| `scripts/check-progress-log-index.sh` | **0** | `結果: 違反なし` |
| `scripts/check-enum-sync.sh` | **0** | `結果: ベースラインどおり(増加なし)` |
| `scripts/check-browser-storage-keys.sh` | **0** | `結果: 違反なし` |

**★上表はレビュー取り込み後に 7 本すべてを回し直した実出力である**（回していない検査の結果は書いていない）。

---

## 11. 変更統計（`git diff --numstat d9c1770 b801840`・教訓 `E-225`）

**★★【M24-07 レビュー（中-4）で採り直した】** 初版は **87 files / +2032 / −386** と書いていたが、これは**完了報告を書く前に採った値**であり最終ツリーと一致していなかった。`E-225` が numstat の貼付を求めているのは「**新規追加のはずのファイルに deletions が付いていないか**をレビューが読むため」であり、**貼った数字が最終ツリーと合わないとその照合が成立しない**（レビュー側は「どちらが正か」を判断できない）。

**★採取の基準を明示する**——下記は **`d9c1770`（着手基点）から `b801840`（本節を含む報告コミット）まで**である。**⇒ 本節の数字は、本節を書いたコミット自身を含む。**

**★★「報告執筆後に採り直す」だけでは同じ問題が再発する**——採り直した瞬間に、その編集分がまた外れるためである（無限後退）。**⇒ 範囲の両端を書いて、読み手が `git diff --numstat d9c1770 b801840` でそのまま再現できる形にした。**

**★★`b801840` より後にあるのは、本節そのものの訂正だけである**（`docs/progress/M24-07-completion-report.md` §11 の 1 か所。**コード・テスト・他の節は 1 行も動かない**）。**⇒ 後退はここで止まる。** 教訓 `E-225` が読みたいもの——**新規追加のはずのファイルに deletions が付いていないか**——は、下の一覧で全数が確認できる。

**112 files changed, 3513 insertions(+), 480 deletions(-)**

**★新規ファイル 11 本はすべて `+` のみ（deletions 0）**——上書きで既存を消していないことの確認:

```
554	0	docs/progress/M24-07-completion-report.md
239	0	docs/progress/M24-07-mock/m24-07-step-vocabulary.html
377	0	docs/progress/m24-07-review.md
 80	0	internal/service/comboio/csvcore/column_contract_test.go
128	0	web/src/constants/label-keys.test.ts
 41	0	web/src/features/punish/components/PunishAttributionBadge.tsx
 34	0	web/src/lib/ja-label.test.ts
 46	0	web/src/lib/ja-label.ts
107	0	web/src/locales/composed-en.test.ts
 79	0	web/src/locales/no-japanese-in-en.test.ts
113	0	web/src/locales/retired-words.test.ts
```

**★削除・リネームは 0 件**（`git diff --name-status --diff-filter=DR d9c1770 b801840` が空）。

主な変更（抜粋）:

```
303	  8	web/src/locales/ja.json
304	  9	web/src/locales/en.json
 59	 26	web/src/constants/combo-list.ts
 54	 13	internal/repository/combo/repository.go
 48	 15	web/src/constants/oki.ts
 48	  8	web/src/features/combo/utils.ts
 42	 33	web/src/features/combo/components/VirtualController/HitBoxLayout.tsx
 37	  5	web/src/lib/character-sort.ts
 10	  2	web/src/features/punish/components/PunishList.tsx
  9	  4	web/e2e/m18-03a-punish-mylist.spec.ts
```

**★最後の 2 行は、確定 E2E で赤くなった 1 件の是正である**（§9 に経緯を書いた）。**新規ファイル 11 本に deletions が付いていないこと**と併せて、**「新規のはずのものを上書きで消していない」「E2E を通すために既存の主張を削っていない」** の両方が読める形にしてある。

---

## 12. ■ 併せて更新が要るもの

| 項目 | 状態 |
|---|---|
| **CHANGE 番号の登録** | **不要**。`CHANGE-147` は設計卓が起票済みであり、**製造は番号を消費していない** |
| **「次の番号」の写し先** | **不要**（番号を消費していないため） |
| **マイグレ連番** | **消費 0 本**。`ls migrations/` の最大は `000079` であり、**次に払い出すのは `000080`**（ボード §2.2 と一致） |
| **版を上げた文書の参照元** | **なし**（指示書・チェックリスト・通知書の版は上げていない） |
| **`web/CLAUDE.md` §1 台帳** | **更新した**（#11 の「4 面」→「5 面」是正。`check-browser-storage-keys.sh` 緑） |
| **`docs/progress/progress-log.md`** | **索引行を追記した** |
| **新規依存** | **0**（`go.mod` / `package.json` 不変） |
| **ブラウザストレージ新キー** | **0** |

---

## 13. §7.4.1 CHANGE 要否の判定（as-built で 1 件ずつ）

| 設計書 | 判定 |
|---|---|
| **`DES-005` §5.4**（レシピ表示トグルの軸） | **★`CHANGE-147` の主対象。as-built が確定した** —— 軸は「省略/全文」から **「1 行にまとめる / ステップごとに改行した縦表示」** へ移り、ラベルは **「レシピを縦に表示」** 。**案 B は採らなかった**。あわせて**適用範囲は 5 面**（旧記述の「4 面」は失効）。⇒ 設計卓が反映する |
| **`DES-005` §5.9.2**（`adoptNameFormat`） | **条件不成立**。触っていない ⇒ CHANGE 不要 |
| **`DES-004` §2.2**（表示語の写像） | **条件成立の可能性あり** —— 表示語の正典が「定数の文字列」から「値 → i18n キーの対応表 ＋ locale」へ移った。**内部表現は 1 つも変えていない**ため §2.2 の原則自体は不変だが、**「表示語をどこが持つか」の記述があるなら更新が要る**。⇒ 設計卓の判断を仰ぐ |
| **`DES-002` §7.6**（CSV 契約） | **変更なし**。列名・ZIP エントリ名とも不変。**★ただし「往復テストでは列名変更を検出できない」という事実は §7.6 に書かれていない**（§14-3 参照） |

---

## 14. ★設計卓へ回す as-built の差分

1. **指示書 §3.3-3 の「leaf 487 / トップレベル 23」は誤り。** 着手前の実測は **607 / 26**（ledger `M24-RESEARCH-01` §2 の記載も同じく失効）。
2. **指示書 §1.1 の「followup §AS 2 件」は誤り。** 実測は **§AS 4 件 ＋ §AT 1 件**（`M24-overview` §3 の「9 件」が正しい）。
3. **★★`DES-002` §7.6 の往復契約に、観測の穴があった。** 既存の Go 往復テストは**列名を変えても緑のまま**であり（export/import が同じ定数を読むため）、検出できるのは nightly の E2E だけだった。**⇒ 本サブで PR チェック側へ観測を足した**（`column_contract_test.go`）。§7.6 へ「往復テストは列名の契約を守らない」旨を 1 行残すか、設計卓の判断を仰ぐ。
4. **`SM-069` は `recipe_cache` に触れない**（起き攻めのフロント定数）⇒ §9.1-1 の停止条件には当たらなかった。
5. **`SM-008` は既に実現済みだった**（`link` フラグ → 「目押し」）。残るのは**内部値 `link` が URL と目押しの両方で使われている**点のみで、表示語は割れていない。
6. **「トグルは 4 面」の失効記述が残っていた**（本サブで是正）。**★★【M24-07 レビュー（高-1）で母数を訂正した】** 初版は「4 か所 ＋ 台帳」と書いたが、これは数え直していない値だった。実測は `web/src` + `web/e2e` で 31 行、うち無関係 1 行と是正マーカー 2 行を除く **28 行**が失効したままで、**是正したファイル自身の中に 4 行**（`RecipeText.tsx`）と**公開 godoc 1 行**（`useRecipeFullView.ts`）、**トグル部品の冒頭で 4 面を名指し列挙**（`RecipeViewToggle.tsx`）が残っていた。⇒ 全数を掃いた。**★「N 面」という数を書かず、面の列挙か「コンボ側の面」という言い方へ寄せた**——数だけを書くと、面が増えたときに同じ失効が起きるためである。**★あわせて `useRecipeFullView.test.tsx` に 5 面目（マイコンボ）を足した**——マイコンボは一覧と同じ行部品を使うが自前のトグルを置くため、「2 つ目のトグルから押しても 5 面すべてへ伝わる」を新しい観測として固定した（それまで「5 面共有」に対応する観測は無かった）。
7. **`RecipeText` の `compactClassName` は prop 契約上「幅制御クラス」だが、詳細面だけ折返し制御クラスを渡している。** `truncate` と同時適用され、設計意図と渡している値が矛盾している。**本サブでは直していない**（案 B を採らないため）。
8. **地上ダッシュの表示語が 2 系統**（ボタン「前方ステップ」＝正 ／ `alias_text`「前ダッシュ」＝誤）。**`M24-08` へ送った**（**D-615**）。
9. **ZIP エントリ名の定数が 2 パッケージに重複**（既知 followup `zip-entry-name-constants-duplicated`）——本サブでは触っていない。
10. **`features/combo/labels.ts` とフィルタ定数で `normal` / `counter` が割れたまま**（「通常」vs「通常ヒット」／「カウンター」vs「カウンターヒット」）。エディタを触らずには畳めない。
11. **`GamepadStatusBadge.tsx` は依然として直書きである。** 仮想コントローラの i18n 化により同ファイルの依存コメントが失効したため書き換えた。**gamepad / keyboard 系の面ごとの i18n 化は射程外**として据え置いた（1 画面で 2 系統を混ぜないため）。
12. **残存のハードコード日本語は 122 ファイル / 1,409 行**（着手前 143 / 1,862）。**★★【M24-07 レビュー（中-5）で書き分けた】残存は 2 種類あり、性質が違う。行数だけの一覧では次の担当が読み違える。**

    | 種類 | 例 | 英語 UI での見え方 |
    |---|---|---|
    | **(a) 全面直書きの面**（行数が多い） | `IntakeHelperPage`(115) / `ComboEditor`(91) / `ComboEditorBasicFields`(86) / `intake/prompt.ts`(71) / `ComboImportPage`(63) | **画面まるごと日本語**。★「混ざっていない」ため §2.3 の意味では健全であり、面ごとにまとめて i18n 化すればよい |
    | **★★(b) 1〜2 行だけ混ざる面**（行数が少なく一覧では埋もれる） | コンボ詳細 `ComboDetailMetadata.tsx:112`「タグ」・`ComboDetailPage.tsx:167`「セットプレイ追加」／ コンボ一覧 `ComboTableRow.tsx:69` の `aria-label`（支援技術にだけ日本語）／ 全画面共通 `Header.tsx` のナビ 12 本（`t()` を 2 か所で併用） | **英語 UI でそこだけ日本語**。★★見え方はこちらのほうが悪い。**いずれも本サブが作った混在ではない**（`§4.2` の名指しにも無い）が、`CO-026` を閉じるときは (a) より先に見るべきである |
    | **★★(c) 共有部品が、全面直書きの面へ英語を持ち込む面**（**2026-08-30 の実機確認で判明。レビューも本節の初版も挙げていなかった**） | **`CharacterSelector`**（`t()` 2 か所 ＋ 工程 10 でキャラ名もロケール従属）**が埋まる全面直書き 5 面**＝`PunishSearchPage`(10 行) / `PunishListPage`(10) / `PresetEditPage`(10) / `MovesEditGridPage`(4) / `IntakeHelperPage`(51)。**★`SetupEditorPage`**(i18n 済み) **×** **`SetupBasicInfoForm`**(0) **の親子も同型** | **日本語の画面のなかで、その部品だけが英語**。★★**行数で並べると埋もれる**——`PunishSearchPage` は 10 行しかない。**★(b) より重い**——(b) は「面の中の 1〜2 語」だが、(c) は**面をまたいで増える**（消費側の面の数だけ生まれる） |

    **⇒ `CO-026`（英語ロケール整備）は「大きな面を片付ければ閉じる」ではない。**

    **★★(c) は「共有部品を i18n 化する」たびに新しく生まれる。** 工程 3 で `TagSelector` の同型を検出して `jaLabel` へ差し替えた（§3 の案 A）のに、**工程 10 の `CharacterSelector` へ同じ問いを向けなかった。** ⇒ **共有部品を i18n 化したら、消費側の面を全数挙げて「その面は i18n 側か直書き側か」を 1 件ずつ判定すること。** 部品の側だけを見ても混在は見えない（詳細は §5.4.3）。

13. **★実査 4 の記述を 1 点訂正する**（レビュー 中-5）。`ComboImportPage` を「`t` を持つが**直接呼び出し 0**」と書いたが、同ファイルは `formatIssue(raw, row, t)`（`:251,256`）へ `t` を渡しており、**取込検証エラーのバッジは locale 由来で描かれている**。「直接呼び出し 0」は事実だが、「locale を通っていない」という含意は成立しない。

14. **★★画面名の改名が `DES-005` へ届いていない**（レビュー 高-3）。本サブは開発者判断で **2 つの画面名**を変えた。

    - 画面14: `<h1>インポート(CSV)</h1>` → **`取込(CSV)`**（`ComboImportPage.tsx`）
    - 画面19: `<h1>取込ヘルパー(既存メモ/エクセルから)</h1>` → **`引っ越し取込(他のアプリ・メモ・表計算から)`**（`IntakeHelperPage.tsx`）＋ ナビゲーション（`Header.tsx`）

    **⇒ `DES-005` は旧名のままである**——画面一覧 **row 19**（`:44`）／ 見出し **§5.19**（`:1277`）／ **§5.14** の連携口記述（`:989`）。**★`playbook` §4.44「影響設計書の欄は起票時の見込みであって全数ではない。as-built で足りなければ足す」がまさにこの型である。⇒ 設計卓が反映する。**
    **★コード内コメント 14 か所の旧名は、本サブで改名した**（レビュー取り込み時）。
    **★★`IntakeHelperPage` の h1 に足した括弧書き「(他のアプリ・メモ・表計算から)」は開発者の逐語には無い製造の裁量である**（逐語は「引っ越し取込」まで）。旧 h1 も同形の括弧書きを持っていたため踏襲したが、不要なら設計卓が落として差し支えない。

15. **★指示書 §4.8「並び順を勝手に変えないこと」と as-built に差異がある**（レビュー 中-3）。本サブは `sortCharactersByLocale(characters, i18n.language)` として **en では `Intl.Collator("en")` × `name_en` で並べ替える**ようにした（`lib/character-sort.ts`）。**⇒ 指示書の 1 行とは逆に見えるが、正本（followup `character-selector-label-is-ja-fixed`）は「ラベルの i18n が実現したときに、並べ替えのキーも `name_en` へ揃えること。★★片方だけ変えないこと」と明記しており、実装はそちらと完全に整合する。** 指示書 §4.8 が正本を圧縮しすぎて逆の読みを与えていた。**★この記録が無いと、レビュー担当は「指示書が禁じた並び替えを勝手にやった」と読む**（実際に一度そう判定されかけた）。

---

*以上、M24-07 完了報告。* **★★最大の発見は「CSV 列名の契約を守る観測が、PR チェック側に存在しなかった」ことである** —— 往復テストは列名を変えても緑のまま通り、検出できるのは nightly の E2E だけだった。**★★次に大きいのは、指示書 §4.2 と §2.3 が正面から衝突していたこと**（共有ラベル定数を i18n 化すると全面直書きの 3 画面が「2 系統混在」になる）。**⇒ 手を止めて開発者へ確認し、案 A（`ja.json` から導出）の裁定を得た。** **★語彙はハード列であり、決まった語だけを入れた。単位語（`SM-016`）は開発者判断で見送り、調査結果のみ §6 へ残した。**
