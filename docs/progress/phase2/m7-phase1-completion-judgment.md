# フェーズ 1 完了判定書(M7 完了判定)

> 注: 本資料中のフェーズ番号は CHANGE-020(2026-06-07 整理工程)で再設定される前の旧体系。新旧対応・新フェーズ定義は REQ-001 §7、現行の持ち越し対応フェーズは m7-phase1-completion-handover.md 第2層カタログ / progress-summary §8 を参照。本資料は当時の記録として旧番号のまま据え置く。

| 項目 | 内容 |
|------|------|
| 文書 | フェーズ 1 完了判定(= M7 全体完了判定)。M7-overview §13 DoD の逐条確認 + 補完した追加完了基準 |
| 判定者・判定日 | 設計担当 Claude(M7 期間担当)、2026-06-05 |
| 判定結論 | **フェーズ 1 完了(条件: §13.5 ドキュメント整備のうち handover / retrospective / 設計書追記は本完了処理の後続ターンで実施中。それ以外の機能・配布・回帰・持ち越し整理はすべて充足)** |
| 根拠資料 | M7-01〜M7-06 各 progress-log / 各 connection-memo-handling / retrospective-log v1.0.28 §6.6 / architecture-patterns v1.0.13 / M7-05・M7-06 E2E 結果 |

---

## 0. 判定サマリ

M7-overview §13 の DoD を逐条確認した結果、**機能要件(§13.2)・持ち越し課題解消(§13.3)・回帰なし(§13.4)・完了後作業の前提(§13.6)はすべて充足**。§13.5 ドキュメントのうち handover / retrospective / 設計書追記は**本完了処理の後続ターン(2〜4)で実施中**で、これらは「完了を確定させるための事務処理」であり機能・品質上の未達ではない。

加えて、**§13 DoD に項目として欠けていた embed / 単一バイナリ成立 / 起動 UX / 設定画面 LAN トグル(いずれも DES-002 / DES-005 / NFR303-304 の定義済み要件)を M7-05 / M7-06 で識別・実装・検証**し、補完した(§3)。

よって **フェーズ 1 完了と判定する**。

---

## 1. §13 DoD 逐条確認

### 13.1 サブマイルストーン完了承認

- [x] M7-RESEARCH-01 / 02 完了承認済み
- [x] M7-01 / M7-02 / M7-03 / **M7-04(M7-04-1 + M7-04-2 に分割、両完了承認)** / M7-05 完了承認済み
- [x] **(追加)M7-06 完了承認済み**(embed + 単一バイナリ + 配布構成 LAN 検証。§13 制定時に無かったサブマイルストーン、§3 で補完)

### 13.2 機能要件

- [x] shadcn/ui ベース統一 UI へ移行完了(M7-03、ハイブリッド解消)
- [x] Header 共通化 + ハンバーガー(Sheet スマホナビ)完了(M7-03)
- [x] 全画面が実用可能レベルで PC / タブレット / スマホ動作(NFR306、横スクロール + Header ハンバーガー + 機能動作確認。本格スマホ UI はフェーズ 3 以降送り = §13.3 で記録)。M7-05 / M7-06 E2E + 開発者実機で確認
- [x] フェーズ 1 スコープのキャラ耐久確認(案の確定 = **リュウ + AKI + ジェイミー + ガイル の 4 キャラ**、M7-04-2)で全コンボ管理機能が動作
- [x] 残り 4 プリセットの切替動作確認済み(M7-04-2 A 系統 = 空プリセット選択時のフォールバック、M7-06 で「プルダウン動作 + 日本語表記フォールバック」を実機確認)
- [x] スキーマ耐久実証(複数キャラ + 複数コンボで検索・フィルタ・ソートが NFR001 100ms 内、M7-04-2 C 系統 = 正常)
- [x] スマホ LAN 実機検証完了(M7-06 U-1 = Norton 許可後に接続成立・全機能操作確認、A-1/A-2 込みの再ビルド後最終確認も実施済み)
- [x] R-2 / R-3 解消確認(ComboTable のシミー DR 有列 + AddComboToCompareModal のキャラフィルタ動作、M7-03。なお M7-15 由来の比較モーダルキャラ切替 B-2/B-4 も M7-04-2 で解消)

### 13.3 持ち越し課題解消(解消 or 明示的フェーズ送り確定)

- [x] **R-1(新基準 = Header 共通化 + ハンバーガー + 横スクロールで実用可能レベル)**: M7-03 で充足(※ M7-06 連携メモの「R-1 = LAN IP 事前提示」とは別物。後者は §3.4 に記載)
- [x] R-2 / R-3: M7-03 解消
- [x] **M-1〜M-4**: M7-05 で**全件フェーズ 2 送り確定**(M7-05 連携メモ。DoD は「明示的にフェーズ 2/3 送りで確定」を許容するため充足)。handover に送り確定を明記(後続ターン)
- [x] C-2 / C-3 / C-4: M7-02 解消
- [x] L-02 / L-03 / L-04: M7-05 解消(L-03 = model 層正準コンストラクタ集約、architecture-patterns v1.0.13 §3 で正本化)
- [x] 残課題 3: progress-log で解消済み(整理工程で最終確認推奨)
- [x] 本格スマホ UI 整備(DES-005 §5.6 / §5.8 / §5.9 / §5.10 / §7)を handover でフェーズ 3 以降送りとして記録 → **後続ターン(handover)で実施**

### 13.4 既存機能の回帰なし

- [x] M1〜M6 機能の回帰なし。shadcn/ui + Header 共通化 + キャラ複数化の 3 系統交差点を含め、M7-05 統合 E2E + M7-06 配布構成スモークで確認。`go test ./...` / `pnpm test`(435)全パス

### 13.5 ドキュメント

- [ ] `m7-phase1-completion-handover.md`(旧称 m7-to-phase2-handover.md から改称、宛先 = 整理工程)作成 → **後続ターン 2 で実施**
- [ ] フェーズ 2 / 3 送り持ち越し課題の一元整理 → **後続ターン 2(handover)で実施**
- [ ] フェーズ 3 先行意向の記録 → **後続ターン 2(handover)で実施**
- [ ] retrospective-log M7 期間反省完了(M7-16 / M7-17 は v1.0.28 反映済み、M7-18 + 完了処理反省は後続ターン 3) → **後続ターン 3 で実施**
- [x] architecture-patterns 改訂(v1.0.13 まで反映)。残る C-1 / C-2 追記は **後続ターン 4**。playbook 改訂候補(節番号変更時の全参照 grep 等)は整理工程で扱う
- [-] progress-summary 改訂(M7 セクション)= 開発者管理。整理工程で扱う想定
- [x] change-number-registry: CHANGE-019 まで反映済み。**M7-04 / M7-05 / M7-06 で新規 CHANGE 起票なし**(embed/LAN/起動案内/L-02 等はすべて既定要件への準拠 or 補足資料改訂)。registry 改訂不要

### 13.6 M7 完了承認後の作業(開発者責任 / 整理工程)

- [ ] `web/prototypes/` 削除(開発者責任、handover に注意として再掲)
- [ ] フェーズ 3 着手判断 → 開発者構想では **整理工程 → 開発者要望取込 → FR701 先行**(handover §フェーズ先行意向に記録)

---

## 2. 判定: §13.5 の未チェック項目の扱い

§13.5 の未チェック項目(handover / retrospective / 設計書追記)は、**いずれも本完了処理の後続ターンで実施中の事務処理**であり、フェーズ 1 の機能・品質・配布・回帰の達成を妨げるものではない。したがって**フェーズ 1 完了の判定を保留する理由にはならず**、これら事務処理の完了をもって §13 DoD は形式的にも全充足となる。

---

## 3. §13 DoD に欠けていた追加完了基準(M7-05 / M7-06 で補完)

§13 DoD は **embed / 単一バイナリ配信 / 起動 UX / 設定画面 LAN トグル**という、DES-002 / DES-005 / NFR303-304 に定義済みの配布アーキテクチャ要件を**項目として含んでいなかった**。これらは M7-05 の F-5 検証で顕在化し、M7-06(+ 追補 A-1 / A-2)で実装・検証した。

| 追加基準 | 根拠 | 状態 |
|----------|------|------|
| フロント embed + 単一バイナリ成立(SPA フォールバック /api 非干渉、静的キャッシュ/MIME) | DES-002 §5.1 / §13、NFR303/304 | ✅ M7-06(`-tags=embed_web`、catch-all で /api 明示 404) |
| 起動 UX(ブラウザ自動起動 + 実ポート + 起動案内 stdout) | DES-002 §11.1、NFR303/304 | ✅ M7-06 + A-1(`printStartupNotice`) |
| 設定画面 LAN 共有モード切替トグル(確認ダイアログ・再起動要) | DES-005 §16 / DES-002 §3.4 | ✅ A-2(config 手編集なしで LAN 化可能) |
| 配布構成でのスマホ LAN 実機検証 | DES-002 §14 | ✅ M7-06 U-1 |
| Windows 単一バイナリ起動 | DES-002 §11.1(公式サポート) | ✅ M7-06 B-1(migration WithInstance 化) |

> **判定への含意**: これらは「§13 に書かれていなかったが満たすべき定義済み要件」であり、結果的にすべて充足した。**§13 DoD がこれらを取りこぼしていた事実は、完了判定基準が設計書本体の要件を網羅監査して導出されていなかったことを示す**(後続ターン 3 の retrospective に反省として記録)。

---

## 3.4 フェーズ 2 / 3 送り確定項目(判定上「明示的送り確定」を満たすことの確認、詳細は handover)

以下は「未実装だが**意図的にフェーズ 2/3 送りで確定**」= §13.3 の許容範囲。詳細列挙と前提作業は後続ターン 2 の handover で行う。

- custom_states 消費・表示(保存/API のみ、architecture-patterns §9.1)
- recipe_cache プリセット/エイリアス変更時の無効化(SUPP-001 §7 既定だが未実装、§9.2)
- ComboEditor キャラ選択 = 他キャラ登録 UI(B-1)/ 新規登録のリュウ固定 UX(E-2)
- M-1〜M-4(M5-RESEARCH-01 由来 UI 層リファクタ)
- 残り 4 プリセットのエイリアス実データ整備 / プリセット管理 UI
- 本格スマホ UI(DES-005 §5.6 / §5.8 / §5.9 / §5.10 / §7)
- i18n 英語ロケール整備
- R-1(A-2 確認ダイアログでの LAN IP 事前提示、M7-06 連携メモ)/ R-2(mac/Linux 実機起動)/ R-3(CI 整備)
- 検証用データ全クリア後の 000012 耐久 seed 再 seed 注意(新規 DB 構築時)

---

## 4. 本完了処理の残ターン

1. **(本ターン)フェーズ 1 完了判定** ← 完了
2. **handover 作成**(`m7-phase1-completion-handover.md`、宛先 = ドキュメント・プロジェクト整理工程、番号非依存カタログ)
3. **retrospective まとめ**(M7-18 + 完了処理反省 = 完了判定基準の設計書要件監査不足 / バグ#6 真因レイヤ双方向取り違え 等)
4. **設計書追記**(SUPP-001 §5.6 = 起動案内 stdout / architecture-patterns 既知制約 = FW/AV)

---

*以上、フェーズ 1 完了判定書*
