# ドキュメント整理レポート(/cleanup_docs)— 2026-06-27

| 項目 | 内容 |
|------|------|
| 文書ID | CLEANUP-REPORT-20260627 |
| 実行コマンド | `/cleanup_docs`(2 回目。引数 = 「コマンド自体の自己改善 + 増えた実ドキュメント整理。docs-map を活用、usermanual は未着手」)|
| 対象読者 | 開発者、後続の整理工程担当 |
| 位置づけ | `/cleanup_docs` の Stage 1(再構成計画)+ Stage 3(検証結果)の記録。本実行は **(A) コマンド自己改善(実施済み)** + **(B) 実ドキュメント整理(重複編集なし)** の 2 部構成 |
| 前回 | `cleanup-report-20260607.md`(2026-06-07、初回、M7/フェーズ1完了時。「ほぼ編集不要」と結論)|

---

## 0. スコープ前提(開発者確認の逐語転記)

Stage 0 ステップ 2 で開発者に確認した結果(AskUserQuestion、2026-06-27):

- **前回クリーンアップ**: 2026-06-07(初回・M7/フェーズ1完了時)。**その通り** 確認済み。
- **差分**: M8〜M12 完了 + フェーズ2→3 移行。CHANGE-020→049、M8-M12 指示書群、handover 12 ファイル増、
  新規コマンド管理ファイル(`code-facts.md` / `docs-map.md` / `retrospective-digest.md` / `followup-backlog.md`)。
- **M12・フェーズ2は完了扱い**(指示書・レビュー・CHANGE は編集禁止の歴史資料)。**その通り** 確認済み。
- **docs-map 活用** = インベントリ起点に再設計(コマンド自己改善)。
- **usermanual** = 登録のみ・中身不触。

本クリーンアップで重点的に見る範囲:
1. **コマンド自己改善**(`.claude/commands/cleanup_docs.md`):M0-M4 前提ハードコードの陳腐化是正。
2. **実ドキュメント**:M8〜M12 期間で蓄積した編集可能 6 ファイル + 現行 handover の重複検出。

---

## A. コマンド自己改善(実施済み・commit `31a6999`)

引数で明示依頼されたため、通常は暗黙編集禁止(P)の本コマンドファイルを編集した。変更点:

| # | 変更 | 狙い |
|---|------|------|
| A1 | Stage 0 ステップ 1 に **docs-map.md 起点のインベントリ取得** を追加。未反映ファイルは `/regen_docs_map` 提案 | マイルストーン番号列挙でなくインベントリ駆動で陳腐化を防ぐ |
| A2 | 編集可能リスト #4 を「**現行(進行中/直近)移行 handover**(Stage 0 で確定)」へ。完了済み handover は直下でも凍結=編集禁止と注記 | `m{N-1}-to-m{N}` 直書きの陳腐化解消 |
| A3 | 編集禁止リストの `M{0-4}` を「**完了済み全マイルストーン**(境界は Stage 0 で確認)」へ規則化。`phase{N}/` アーカイブ明記。**新カテゴリ Q(自動生成)/ R(継続更新)/ S(usermanual 登録のみ)** 追加 | 番号ハードコード撤廃 + 新コマンド管理ファイルの分類 |
| A4 | 原則 4 のコマンド管理ファイル一覧を 3 → 7 件(自動生成2 + 継続更新5)に拡張、各専用コマンドを明記 | 継続更新/自動生成ファイルの取りこぼし防止 |
| A5 | §ARGUMENTS に「**自己改善**」パターン追加(本コマンドファイル編集を明示依頼時のみ許可)。突破不可リストに唯一の例外として注記 | 将来の自己改善依頼を first-class 化 |

> コマンド管理ファイル(F〜H / Q / R = 7 件)は本コマンドでは編集しない。各専用コマンド
> (`/regen_docs_map` / `/regen_code_facts` / `/retrospective-digest-update` 等)で更新する。

---

## 1. 重複対応表と処置(実ドキュメント)

docs-map.md を起点に編集可能 6 ファイル + 現行 handover(`phase2-to-phase3-handover.md`)を検査。
**結論: M8〜M12 期間で新たに発生した「整理すべき横断重複」は無し**。ポインタ運用が維持されている。

| ID | 重複/参照箇所 | 種別 | 処置・根拠(grep 実証) |
|----|---------------|------|------------------------|
| D1 | CLAUDE.md §5(テスト方針)⇔ supp-001 §5.5 | 概念重複 | **敢えて残す**(前回 Q3、原則1例外=安全規則)。不変 |
| D2 | CLAUDE.md §6(依存ライセンス)⇔ supp-001 §5.7 | 概念重複 | **敢えて残す**(前回 Q3)。不変 |
| D3 | camelCase/列挙定数同期: CLAUDE.md §4(正本)⇔ supp-001 §6.4 ⇔ playbook §4.8/§17.2 | — | **解消済み維持**。supp-001 §6.4 は「CLAUDE.md を正本」、playbook L44-45/L1281 は「CLAUDE.md/SUPP-001 に正本、本書は運用観点で参照」=ポインタ |
| D4 | supp-001 §6.1〜§6.7(運用詳細)⇔ playbook §1〜§11 | — | **解消済み維持**(2026-05-16、supp-001 L921-991 は全ポインタ。「1 ルール 1 箇所の原則」明記)|
| D5 | 現行 handover §3 残作業 ⇔ followup-backlog / retrospective-log §6.6.5 / change-number-registry / docs-map / code-facts | — | **重複でない(模範的差分運用)**。handover は「**living 版は followup-backlog**、本書 §3 はキックオフ用スナップショット」と明記しポインタ参照(L13/L41/L61/L135)|
| — | move_code: supp-001 §(API/CSV/UI 利用表)⇔ handover §3(M12-05 movecode 統一フォローアップ)| 文脈差 | **真の重複でない**(技術仕様 vs 後続課題、目的が異なる)|
| — | settings.json ⇔ CLAUDE.md §10 | 文脈差 | **真の重複でない**(機械強制 vs お願い、前回と同じ)|

---

## 2. 役割別参照分離(既存設計を維持・現状確認のみ)

| 役割 | 主資料 | 読まない資料 |
|------|--------|-------------|
| 設計・指示書作成担当 Claude(Web) | playbook / SUPP-001 / 設計書本体 / docs-map / 現行 handover / followup-backlog / retrospective-digest | 製造・レビューのチェックリスト |
| 製造担当 Claude Code | CLAUDE.md / 指示書本体 / SUPP-001 / code-facts | playbook・レビューチェックリスト |
| レビュー担当 Claude Code | CLAUDE.md / 指示書本体 + レビューチェックリスト / SUPP-001 | playbook |
| 開発者 | 全資料 + human-notes | — |

役割明示ヘッダは編集可能ファイルに既存(supp-001 L60「本書の対象読者」、playbook §0.0、CLAUDE.md 冒頭、
progress-summary/log の「対象読者」行、現行 handover の前提資料/読み方節)→ **追加編集不要**。

---

## 3. 別オーナーへ routing(本コマンドのスコープ外)

| 項目 | routing 先 | 根拠 |
|------|-----------|------|
| **progress-summary.md の M12 セクション欠落**(現状 §11/M11 まで)| **`/update_progress_summary` + 開発者** | handover L42「progress-summary は開発者が M12 完了時に更新予定」。本コマンドは追記しない |
| **docs-map.md の staleness**(2026-06-24 生成、`phase2-to-phase3-handover.md` 未反映)| **`/regen_docs_map`** | 自動生成=手編集禁止(編集禁止 Q)|
| **M12/フェーズ2 反省・パターンの継続更新ファイル追記** | **`/retrospective-digest-update` 等** | F〜H/R = コマンド管理(原則 4)。handover L120「教訓は retrospective-digest へ繰り込み」既済 |
| **`m12-retrospective-notes.md` の破棄** | **開発者** | handover L41「転記完了につき破棄可」。本コマンドはファイル削除しない |
| **CLAUDE.md §1 フェーズ状態の陳腐化**(「フェーズ1完了、次=フェーズ2」→ 実際はフェーズ2完了・3 着手)| **`/next_phase_kit` + 開発者(下記 Q1)** | 重複でなく**事実の更新** = 本コマンド本来スコープ外。便乗改善回避(前回方針踏襲)|

---

## 4. 開発者判断が必要な質問

1. **CLAUDE.md §1「開発フェーズ」の陳腐化**: 現状「現在のフェーズ: フェーズ1(MVP)完了。次フェーズ: フェーズ2」
   だが実態はフェーズ2完了・フェーズ3着手。これは重複でなく事実更新のため `/cleanup_docs` スコープ外と判断し
   **編集していない**。`/next_phase_kit` 側で更新するか、本整理に便乗して直すか? → **要判断**。
2. 上記以外に、実ドキュメントの内容編集は **発生しない**(重複は全て「解消済み維持」または「敢えて残す」)。
   この結論(= 前回 2026-06-07 と同じく内容編集ゼロ)で確定してよいか? → **要確認**。

---

## 5. 「絶対に欠落させない情報」の保全保証(整理後も全て現所在で不変)

| 情報 | 現所在(正本)| 本実行での変化 |
|------|--------------|----------------|
| コーディング/JSON camelCase/列挙定数同期規約 | CLAUDE.md §4(supp-001 §6.4・playbook §4.8 はポインタ)| 不変 |
| テスト方針・依存ライセンスポリシー | CLAUDE.md §5/§6(+ supp-001 §5.5/§5.7 に重複保持)| 不変(D1/D2)|
| PATCH 省略可能フィールド送信ポリシー(3 状態 / presence-detection)| supp-001 §5.9(+ CHANGE-042/043)| 不変 |
| move_code 分類(system/official/custom)・API/CSV/UI 利用 | supp-001 §(移動コード表)| 不変 |
| E2E 方針(Playwright 段階導入・test-id 規約)| supp-001 + `testid-convention.md` | 不変 |
| 禁則表現・モデル配分判断軸・Plan Mode 基準 | playbook §4/§7/§8 | 不変 |
| 役割定義 | playbook §1 / §0.0 | 不変 |
| CHANGE 採番(欠番 006-009 / 014 含む、最新 049)| change-number-registry(編集禁止 H)| 不変 |
| ブラウザストレージ許容範囲 | CLAUDE.md §10.X | 不変 |
| 持ち越し課題の最新状態(フェーズ3 繰越)| followup-backlog §F12(living 正本)+ handover §3(スナップショット)| 不変 |

---

## 6. Stage 3 検証結果

- **編集差分**: (A) `.claude/commands/cleanup_docs.md`(commit `31a6999`、自己改善)+ 本レポート新規 1 件。
  **編集可能 6 ファイル・現行 handover・編集禁止ファイル群は変更なし**(実ドキュメント重複編集ゼロ)。
- **重複処置の一貫性**: D1〜D5 + 既解消分が「解消済み維持/敢えて残す/真の重複でない」で一貫(grep 実証)。
- **保全保証**: §5 の各情報が現所在に存在することを実ファイル grep で確認済み。
- **役割分離**: §2 のとおり既存設計を維持、各役割が主資料のみで作業可能。
- **コマンド整合**: 自己改善後の cleanup_docs.md で docs-map 起点ステップ・カテゴリ規則化・新カテゴリ Q/R/S・
  §ARGUMENTS 自己改善行を確認。

---

## 7. 結論

フェーズ2→3 移行時点でも、本プロジェクトのドキュメントは **専用コマンド群(update_progress_summary /
regen_docs_map / regen_code_facts / retrospective-digest-update 等)+ ポインタ運用 + 差分のみ handover** により
**十分整理されている**。M8〜M12 期間で新規発生した横断重複は無く、実ドキュメントの内容編集は前回同様ゼロ。

本実行の主成果は **(A) コマンド自己改善**(陳腐化したファイル分類を docs-map 起点 + カテゴリ/ライフサイクル
規則へ刷新し、将来のマイルストーン進行で壊れない構造化)と、**本レポート**(重複の現状所在保証 + routing)。

次に意味のある実ドキュメント整理が必要になるのは、フェーズ3 完了時 or 新たな横断重複が蓄積した時点。
routing 項目(progress-summary M12 追記 / docs-map 再生成 / CLAUDE.md §1 フェーズ状態)は各専用工程で対応。

---

## Stage 2 コミット粒度(提案)

1. **コマンド自己改善**(`.claude/commands/cleanup_docs.md`)— **実施済み(commit `31a6999`)**
2. **Stage 1 レポート新設**(本ファイル)— commit 予定
3. **実ドキュメント重複削除**— **不要**(§1/§7 のとおり編集ゼロ)

> §4 Q1(CLAUDE.md §1 フェーズ状態)を「本整理で直す」と開発者が判断した場合のみ、3 件目の commit を追加する。

---

*以上、cleanup-report-20260627*
