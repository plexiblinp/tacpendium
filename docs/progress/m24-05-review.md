# M24-05 レビュー報告書

| 項目 | 内容 |
|---|---|
| 対象サブ | M24-05（セットプレイ導線・紐付け UI 統合） |
| 対象指示書 | `docs/instructions/M24-05-setup-navigation-and-link-ui.md` **v1.1.0** |
| チェックリスト | `docs/instructions/reviews/M24-05-review-checklist.md` **v1.0.0**（★指示書 v1.0.0 対象・版ずれあり＝後述） |
| 対象完了報告 | `docs/progress/M24-05-completion-report.md` |
| 着手基点 / HEAD | `ad42c4a` / `2b53229` |
| レビュー実施日 | 2026-08-30 |
| レビュー方式 | コード読取のみ（変更なし）＋ 数値・検査の実測再現 |

---

## 総評

**核心（`CO-002`）は正しく満たされている。** チェックリスト §8 が最大の危険とした「母集団が同じことを確かめずに『既存から紐付け』を消す」は、API・SQL・`queryKey` のレベルで実測されており、レビュー側で独立に追試しても同一だった（削除済み `useLinkExistingSetupForm` は `useSetupCandidates(parentComboId)` を呼んでおり、候補表示とキャッシュごと同一）。**重大 0 件。**

`SM-088` を実装せず「成立を固定して報告する」形に留め、規則を逐語で記録し、複製した書式が静かにずれたら赤くなるテストまで置いた点は、指示書 §9.3 の型を正確に踏んでいる。`SM-052` を「`RecipeText` では解けない・実体は modifiers 要約の 2 重実装」と機序で切り分けた判定も的確である。

一方で、**報告書に書いた数値・状態が HEAD の実物と合っていない型の欠落が 3 件ある**。**`progress-log.md` へ 1 行も追記されていないのに「追記した」と書かれている**。**`check-md-emphasis.sh` が赤なのに「違反なし」と書かれている**。**設計伝達レポートが存在しないのに「渡した」「申し送った」と過去形で書かれている**。いずれも M24-13 の教訓 8 / 11（「回していない検査の結果を書いた」「数値は書く前に採る」）が同じ形で再発したものである。

加えて **E2E (5) のテスト名が中身と一致していない**（「コピー先で保存すると別のセットプレイとして増える」と名乗りながら、保存を 1 度も行っていない）。後任は名前を読んで「コピー保存は E2E で守られている」と信じるため、これは体裁ではなく観測の欠落である。

---

## 設計準拠性レビュー結果

### 1. `CO-002`——「既存から紐付け」の撤去（指示書 §4.1 ／ チェックリスト 1-1・1-3・1-5・4-1）: ◎

- **母集団の同一性（§3.3-2 のゲート）**: 満たされている。レビュー側の追試——
  - 旧 `useLinkExistingSetupForm.ts`（`git show ad42c4a:...`）が `useSetupCandidates(enabled ? parentComboId : null)` を呼んでいた。
  - `web/src/features/setup/hooks/useSetupCandidates.ts` の `queryKey` は `["setupCandidates", comboId]` の 1 本のみ。候補表示（`ComboDetailPage` → `useSetupCandidates`）と**同一キー**であり、キャッシュまで共有していた。
  - サーバ側も `FindCandidateSetups(characterID, knockdownAdvantage, excludeComboID)` の 1 本（`internal/repository/setup/repository.go:1077`）。
  - **⇒ 「役割が被っている」は事実であり、消してよい根拠がある。** 完了報告 §1.1-2 の記述は実物と一致する。
- **入口の全数**: 母数付きで報告されており（`grep` 12 ヒット / 4 ファイル・本番 1 か所）、撤去後の残存参照は 0 件であることをレビュー側でも確認した（`grep -rn "既存から紐付け\|LinkExistingSetupModal\|useLinkExistingSetupForm" web/src web/e2e` の残りは説明コメントとテスト名のみ）。
- **撤去そのものの固定**: `ComboDetailPage.test.tsx`「「既存から紐付け」ボタンが存在しない」＋ E2E (1) の 2 層で押さえている。

### 2. 候補 0 件の見せ方（指示書 §4.1 ／ チェックリスト 1-4）: ◎

`SetupCandidateList` に空表示ブロックと `data-testid="setup-candidates"` / `"setup-candidates-empty"` を置き、`ComboDetailPage` の `length > 0` を外している。コンポーネントテスト 1 本 ＋ E2E (1b) で両側から固定されており、破壊確認 2 が 3 経路を赤にしている。i18n キーは `ja` / `en` の両方に足されている（チェックリスト 4-7 ○）。**旧実装が `defaultValue` 頼りで両ロケールに実キーが無かったことを発見して埋めた**のは、指示された範囲を超えた良い拾い方である。

### 3. `SM-052`——modifiers 要約の共通化（指示書 §4.2 ／ チェックリスト 1-12）: ○

- 「`RecipeText` では解けない・実体は 2 重実装」という機序の切り分けは正しい。`ModifiersEditor` は元から共有で、要約側だけが割れていたという診断もコードと一致する。
- **`RecipeText` は 1 行も作り替えていない**（差分はヘッダコメントのみ。チェックリスト 1-6 の「重大」は該当しない）。
- △ **「挙動を 1 文字も変えずに切り出した」は厳密には正しくない**。`ModifiersSummary` には `data-testid="modifiers-summary"` が新設されており、`StepRow` の DOM は変わっている。害はないが、報告の言い切りは実物と食い違う。
- △ **セットプレイ側で `modifiers.type` の表示が消えたこと**が、完了報告では「内部コード露出が消えた」としか書かれていない。実際には `SetupRecipeEditor` の `renderStepLabel`（同ファイル 51 行目、`MODIFIER_NON_MOVE_TYPES` 経由）が主ラベルとして type を出しているため情報は失われないが、**「消した情報が別の場所で出ていること」を報告に書いていない**。後任が「情報が減った」と読む余地がある。

### 4. レシピ表示を `RecipeText` へ寄せる（指示書 §4.2 / §7.1-4 ／ チェックリスト 1-6）: ○

- 母数 12 は追試で一致した（`grep -rn "defaultRecipe" web/src` の setup 系の内訳と一致）。**followup / `RecipeText.tsx` ヘッダの「5 か所」が失効していたことを見つけ、`SetupRegistrationSection` の取り残しを防いだ**のは実査の成果である。
- △ **「12 か所」は setup 側限定の母数である。** combo 側にも `RecipeText` を通っていない面が 2 つ残る（`web/src/pages/HomePage.tsx:83` ／ `web/src/pages/TrashComboDetailPage.tsx:181-183`）。報告の書き方は「未経由の全数 12」と読めるため、単位（setup 側／combo 側）を明示すべきである（`M24-13` の教訓 6「母数は見る単位で割れる」と同型）。
- △ **新たに通した 5 か所のうち、テストで固定されたのは 2 か所だけ**（`SetupCandidateList` の「レシピを `RecipeText` で描く」／ `SetupTreeRow` の `data-testid` ＋ `data-recipe-view`）。**`SetupAccordionItem` / `SetupSelectorModal` / `SetupRegistrationSection` は元の直書きへ差し戻しても全テストが緑のまま**である（`SetupAccordionItem.test.tsx` はレシピ本文を 1 度も掴んでおらず、見出し文字列「レシピ」だけを見ている）。
- △ **`SetupAccordionItem` は `fullView` 固定 `true` により、利用者に見える表示が変わっている。** `RecipeText` の全文モードは**ステップごとに `1.` `2.` と番号を振って縦へ展開する**実装であり（`RecipeText.tsx` の full 分岐）、従来の「`>` 区切りの 1 本の文字列を折り返す」表示とは別物である。`DES-005` §5.6 項目10 の「全文〔複数行折返し〕表示」には反しないが、**モックにも完了報告の as-built 記述にも「番号付きで縦展開になる」とは書かれていない**。

### 5. `SM-088`——自動命名の成立固定（指示書 §4.4 ／ チェックリスト 1-8・1-9）: ◎（1 点だけ △）

- **実装していない**（`SetplaySuggestionSection` の差分は式の切り出しと不要変数の削除のみ）。規則は逐語で報告され、設計卓の暫定案と一致しないことも明記されている。
- **`VAL-S06` は撤回されていない**——フロント側 `disabled={... || name.trim() === ""}`（`SetplaySuggestionSection`）と `SetupEditorPage` の `saveDisabled`、BE 側 `internal/service/setup/validate.go:151`（`CodeS06NameRequired`）がいずれも無変更。`internal/` の差分は 0 である（`git diff --stat ad42c4a -- internal/ cmd/ migrations/` が空）。
- **入力済みの名前を上書きしていない**——`setName` の呼び出しは `startAdopt`（採用ボタン押下）1 か所のみで、以後は `onChange` だけである。**チェックリスト 1-8 の「重大」には該当しない。**
- △ ただし **「上書きしない」を固定するテストは 1 本も無い**。指示書 v1.1.0 が破壊確認 5（チェックリスト 6-2）を落としたため必須ではないが、**チェックリストが最大の危険 2 件のうち 1 件として挙げていた性質が、現在どの層にも観測を持たない**。
- 純粋関数テストの作りは良い。**locales の書式そのものを 3 ケースで固定し、複製した `jaFormat` がずれたら赤くなる形**にしてあるのは、複製テストの弱点を自覚した設計である。

### 6. `SM-011`——セットプレイ編集のコピー（指示書 §4.5 ／ チェックリスト 1-11・4-5）: ○

- 作法はコンボ側と同型（`?copyFrom=`）。新しい作法を作っていない。`mode` を 3 値にせずフラグ 1 本で済ませ、既存 6 分岐を動かさなかった判断は妥当で、理由も報告されている。
- **成立条件を引き継いでいない**（コピー投入の `useEffect` は `verifiedConditions` に触れず、`SetupEditorPage.copy.test.tsx` が 4 セルすべて未チェックであることを確かめている）。チェックリスト 1-11 の「高」は該当しない。
- **`/setups/:setupId`（編集モード）へ成立条件の入力を足していない**（チェックリスト 2-6 の「重大」は該当しない）。コピー先は `/combos/:comboId/setups/new` であり、`DES-005` §5.9 項目5 が新規登録時のみ許している面である。
- 親コンボの決め方（コピー元の先頭の親・不在なら導線を出さない）は既存規則の再利用であり、報告もある。
- × **E2E (5) が名前どおりのことを 1 つも確かめていない**（後述「高-4」）。
- △ **離脱ガードの基準値採り直しにテストが無い**。完了報告 §3.4 が「採らないと何も触っていないのに離脱確認が出る」と明記している性質だが、`SetupEditorPage.copy.test.tsx` の 8 ケースはいずれも `dirtyBaseline` を観測していない。**設定行（`setDirtyBaseline(...)`）を消しても全テストが緑のままである。**

### 7. スコープ外の遵守（`SM-146` ／ レイアウト ／ `VAL-S04` ／ 自動提案）: ◎

- `SM-146` は 1 行も触れていない（導線・「最後の技」の一覧フィルタ・持続当ての扱い、いずれも差分なし）。
- `DES-005` §5.6 項目12（自動提案）は**ファイルに触れているが挙動は不変**であり、§7.4.1 判定 #6 として自主的に報告している。エンジン・条件パネル・ソート・採用／不採用・保存経路はいずれも無変更。**チェックリスト 1-2 の「重大」（消していないか）／ 2-5 の「高」（触っていないか）は、報告があるため「高」に当たらないと判断する。**
- △ 1 点だけ挙動差がある: 旧実装は `steps` が空なら `target.moveId` で例外になる形、新実装は `""` を返す。`SetplaySuggestionStep` の型と生成経路から空にはならないため実害はないが、「1 文字も変えていない」ではない。

### 8. `M24-12` / `M24-04` の成果（チェックリスト 1-14）: ◎

`SetupEditorPage` の離脱ガード 3 フック（`useUnsavedChangesGuard` / `useRequestLeave` / `useLeaveWithoutConfirm`）はすべて残っており、`handleCancel` の一本化も維持されている。コピー導線は `<Link>` であり click キャプチャに掛かる。**出所が `M24-12` ではなく `M24-04`（`1e41985`）であることを実測で訂正した**のは、指示書と followup の記録の誤りを 1 件潰す成果である。

### 9. データ・API 契約・スキーマ・依存・ストレージ（チェックリスト §2 / §3）: ◎

- `internal/` / `cmd/` / `migrations/` の差分 **0**（実測）。契約 F-1 のパッケージも無変更。
- 新規依存 0。マイグレ消費 0（`migrations/` 最新は `000079`）。
- `bash scripts/check-browser-storage-keys.sh` **緑**（台帳 11 / 実装 10・一致）。**`recipe-full-view-v1` の読み手を増やさず面ごとの固定値で渡した判断は妥当**で、理由（台帳と `DES-005` の変更を伴う）も正しい。

### 10. テストの妥当性（チェックリスト §5）: ○

- レビュー側で再実測した結果——
  - `cd web && pnpm test` → **199 files / 2187 tests passed**（完了報告と一致）。
  - `make e2e` → **221 passed（4.4m）/ exit 0**（完了報告と一致。実行前にポート 47390 / 5273 が空きであることを確認）。`m19-03` / `m19-07` / `m24-12` の spec はすべて走って緑（チェックリスト 5-6 の「重大」は該当しない）。
  - 内訳の算術（`2166 − 8 + 2 + 7 + 12 + 8 = 2187`）は、各ファイルのケース数（`adoptName.test.ts` 12 / `ModifiersSummary.test.tsx` 7 / `SetupEditorPage.copy.test.tsx` 8 / 削除 2 ファイル 3+5=8）と一致する。**着手前の 2166 / 198 は checkout を伴うため再現できない（不明）が、内訳と現値は整合している。**
- **既存 spec を緩めていない**——`SetupTreeRow.test.tsx` の書き換えは `data-testid` ＋ `data-recipe-view` ＋ 包み要素の `textContent` の 3 点へ増えており、`SetupCandidateList` / `ComboDetailPage` も検証が増えている。チェックリスト 5-5 の「重大」は該当しない。
- **E2E の撤去確認（チェックリスト 5-2）**: `toHaveCount(0)` の前に `setup-candidates` の `toBeVisible()` を立てているため、「ページが真っ白でも緑」の空振りは塞げている。**不存在の主張に対しては `toHaveCount(0)` のほうが `not.toBeVisible()` より強い**（隠れた要素も数に入るため）。**○ と判定する。**
- **KA をテストごとに割り当てて母集団の混線を避けている**のは、使い捨て DB を spec 間で共有する本プロジェクトの E2E では正しい作法であり、支援モジュール（`web/e2e/support/setup-link.ts`）に理由まで書かれている。

### 11. 破壊確認（指示書 §5.3・3 件）: ○

- 3 件とも構造を変える側で壊しており（クラス追加ではない）、赤くなる経路が数えられている。破壊確認 3 が「純粋関数 4 ＋ E2E 1」で赤くなるという記述は、`adoptName.test.ts` の内容（`buildAdoptedSetupName` の 7 ケースのうち完全一致・`toContain("重ね")` を持つ 4 ケース）と整合する。
- 破壊確認 1 で「コンポーネントテストは落ちない」という**観測の限界を自分から書いている**のは良い。
- △ 破壊確認は本サブの成果 5 群（撤去 / 0 件表示 / `SM-052` / `RecipeText` 集約 / コピー）のうち **3 群にしか当たっていない**。`SM-052` の共通化とコピーは、どちらも壊しても赤にならない可能性が高い（前項 4・6 の観測欠落と同根）。指示書は 3 件しか求めていないため違反ではないが、守られていない経路が残っていることは記録に値する。

### 12. チェックリスト v1.0.0 と指示書 v1.1.0 の版ずれの扱い: ◎（判定は正しい）

製造の突合はレビュー側の追試と一致した——
- §3.3 の実査行は **9 行**（`1/2/3/6/7/8/9/11/12`。同じ `sed | grep -c` を再実行して 9 を確認）。§7.1-1 の「12 件」は失効。
- §7.1 は **8 が欠番**（7 → 9）であり、チェックリスト §0.3-6 の参照先は存在しない。
- 指示書 §5.1 は **E2E 3 ケース**、§5.3 は **破壊確認 3 件**。チェックリストの「4 ケース」「5 件」は v1.0.0 由来。
- チェックリスト 1-7 / 1-10 / 4-2〜4-4 / 5-4 / 6-2 は `SM-146`・`SM-088` 暫定案に紐づく項目で、`D-588` により本サブに該当しない。
- **⇒ 版ずれの解決は妥当であり、指示書 v1.1.0 を正としたのは正しい。** ただしチェックリスト側の是正は設計卓の手番であり、**現状のチェックリストは次に読む人を誤らせる**ため §4 の推奨に挙げる。

---

## 設計準拠性以外の指摘事項

### コーディング規約

- **import 順（`CLAUDE.md` §4 TypeScript「React → サードパーティ → エイリアス（`@/`） → 相対パス」）に反する挿入が 3 件**——
  - `web/src/features/setplay/components/SetplaySuggestionSection.tsx`: `import { buildAdoptedSetupName } from "../adoptName";` が `@/features/setup/api/setupApi` などの `@/` 群の**前**に入っている。
  - `web/src/features/combo/components/SetupSelectorModal.tsx`: `import RecipeText from "./RecipeText";` が `@/features/setup/hooks/...` の前。
  - `web/src/features/combo/components/SetupTreeRow.tsx`: 同上（`@/features/setup/types` の前）。
  - `web/` に ESLint 設定が存在しないため機械検出されない規約であり、**人が読む以外に見つける経路が無い**。
- マジックストリング・`console.log` / `fmt.Println`・不要な `eslint-disable` は無し。
- 新設の公開関数（`buildAdoptedSetupName` / `lastMoveStep` / `ModifiersSummary`）には目的・切り出し理由・非自明な前提（非技ステップが入ってこない理由）まで書かれており、水準は高い。

### 命名

- `data-testid` の付け方（`setup-candidates` / `setup-candidates-empty` / `setup-editor-copy` / `modifiers-summary`）は既存の語彙と一貫している。
- △ E2E `m24-05-setup-link-and-copy.spec.ts` の **(5) のテスト名だけが中身と乖離**（後述）。

### ライブラリ・セキュリティ

- 新規依存 0。外部入力の取り扱いに変更なし。`copyFrom` は `Number(...) > 0` で検査されたうえで既存 API のパス生成にのみ使われており、注入面は増えていない。

### ドキュメント・記録

- 完了報告は網羅的で、実査・母数・逸脱・CHANGE 要否 7 件をすべて 1 件ずつ扱っている。**書式としては本プロジェクトで最良の部類。** 問題は「書いた内容が HEAD の実物と一致しない箇所が 3 つある」ことに限られる（§推奨修正 高-1〜3）。

---

## 推奨修正（優先度別）

### 高（M24 完了前に修正必須）

**高-1. `docs/progress/progress-log.md` へ 1 行も追記されていない。完了報告は「追記した」と書いている。**
- 根拠（実測）:
  - `grep -n "M24-05" docs/progress/progress-log.md` → **0 ヒット**。
  - `git diff --stat ad42c4a -- docs/progress/progress-log.md` → **空**。
  - `bash scripts/check-progress-log-index.sh` → `NG 作業 ID 'm24-05' が docs/progress/progress-log.md に現れない` / `結果: 違反 1 件`。
  - 完了報告 §10「`docs/progress/progress-log.md` | **追記した**（§7.4）」／ §13「末尾へ追記した。…緑の確認だけでなく追記した行を目で確かめた。」
- `CLAUDE.md` §8「サブ完了時の追記は必須」／ チェックリスト 7-1「未追記なら『高』」に該当する。
- **さらに悪いのは「目で確かめた」と書かれていること**——同検査は偽の緑を返しうるという注意書き（followup §AH）に対応する文まで書かれているのに、実際には検査が赤で、追記も無い。**M24-13 教訓 8「回していない検査の結果を完了報告へ書いた」の再発である。**
- 対応: 索引行を追記し、§13 の記述を実測へ合わせる。

**高-2. `bash scripts/check-md-emphasis.sh` が赤。完了報告は「違反なし（436 行 / ベースライン 436 行）」と書いている。**
- 根拠（実測）: `現在 439 行 / ベースライン 436 行` → `NG ベースラインから 3 行増加` / `結果: 違反 1 件`。
- 増えた 3 行はすべて**完了報告そのもの**である:
  - `docs/progress/M24-05-completion-report.md:200`（`**挙動が反転したため「0 件でもセクションを出す」へ書き換え**、`）
  - `docs/progress/M24-05-completion-report.md:252`（`**★製造判断。**同画面が…`）
  - `docs/progress/M24-05-completion-report.md:318`（`**★確認した**: 新規追加した…`）
- 検査を回した時点では緑だったものが、報告書を書いた時点で赤に転じている。**M24-13 教訓 11「数値は書く前に採る」と同型。** チェックリスト 7-8「赤を残していたら『高』」に該当。
- 対応: 3 行の閉じ `**` の直前の約物を強調の外へ出し、§12 の表を実測で埋め直す。

**高-3. 設計伝達レポートが存在しないのに、完了報告が「渡した」「申し送った」と過去形で書いている。**
- 根拠（実測）: `docs/handover/design-reports/` に `m24-05` を含むファイルは**無い**（最新は `20260829-m24-13-design-exceptions.md`）。
- 過去形の箇所: §2「⇒ 設計伝達レポート §4 へ開発者の逐語つきで**申し送った**」／ §3.4「⇒ 設計伝達レポート §4 へ**渡した**」／ §8-2「⇒ 設計伝達レポート §4 へ申し送り」。
- 指示書 §7.5 の逐語「**★工程順として『まだ無い』ものを過去形で書かないこと**」に正面から反する。
- **害が具体的である**——`SetupSelectorModal` を残した判断（開発者の逐語つき）と `SetupEditorPage` の i18n 未整備は、**現在どこにも登録されていない**。`followup-backlog` は §J 以外を製造が書けないため、設計伝達レポートが唯一の受け皿である。過去形で書かれていると、後任もレビューも「もう渡っている」と読んで取りこぼす。
- 対応: 未作成であることを明記して現在形／予定形へ直すか、`/design_handover_report` を実行してから報告を確定する。

**高-4. E2E (5) のテスト名と中身が一致しておらず、「コピーして保存できる」ことがどの層でも検証されていない。**
- 根拠: `web/e2e/m24-05-setup-link-and-copy.spec.ts:139-157`。
  - テスト名: `"(5) コピー先で保存すると別のセットプレイとして増える"`。
  - 実際の中身: `createSetup(...)` を **API で** 1 本作り、`expect(otherSetupId).not.toBe(f.setupId)`（API が別 ID を返したことの確認）→ `/setups/{otherSetupId}` を開いて `expect(page.getByTestId("setup-editor-copy")).toBeVisible()`。
  - **コピー導線を 1 度もクリックしておらず、保存も行っていない。** 直前のコメント「コピーが新規作成として通る」ことを見る」も同様に中身を説明していない。
- 本サブの `SM-011` は「コピーして保存できる」ことが機能要件（指示書 §7.1-6）であり、**その最終形（保存が通り、別のセットプレイとして増える）を確かめている観測が、E2E にもコンポーネントテストにも 1 つも無い**。`SetupEditorPage.copy.test.tsx` は投入までしか見ていない。
- **優先度較正の観点でも「高」である**——テスト名は後任にとって「この経路は守られている」という記述として機能し、動作は正しいままなのでテストも lint も型検査も緑になる。
- 対応: (5) を実際に「コピー → 名前を変える → 保存 → 一覧／API で 2 本になっている」まで運ぶか、名前とコメントを中身どおり（例: 「別の親配下のセットプレイでもコピー導線が出る」）へ直す。**名前を直す側を選ぶなら、保存経路の観測をどこかに 1 本足すこと**（現状は誰も見ていない）。

### 中（M25 着手と並行可）

**中-1. 新たに `RecipeText` へ通した 5 か所のうち 3 か所が、差し戻しても全テストが緑のまま。**
- `SetupAccordionItem` / `SetupSelectorModal` / `SetupRegistrationSection`。`SetupAccordionItem.test.tsx` はレシピ本文を掴んでおらず、見出しの「レシピ」だけを見ている。
- 少なくとも `data-testid="recipe-text"` の存在を 1 行足せば固定できる（`SetupCandidateList.test.tsx` が既に同じ形の 1 ケースを持っている）。

**中-2. `SetupAccordionItem` の表示が「番号付き縦展開」へ変わったことが、モックにも as-built にも書かれていない。**
- `RecipeText` の `fullView` は `1.` `2.` の連番を振って `block` で積む実装であり、従来の「`>` 区切り 1 本の折り返し」とは見え方が違う。`DES-005` §5.6 項目10 には反しないが、**コンボ詳細の常時表示部の見た目が変わる**のは利用者に見える変更である。
- モック HTML（§4.7）は `CO-002` の 3 面 ＋ コピー導線だけで、この面を含んでいない。**開発者の目視確認でも見えない**（`M24-13` 教訓 10 と同型の落ち方）。
- 対応: 完了報告の as-built へ 1 行足すか、モックへ 1 枚足す。

**中-3. コピー投入時の離脱ガード基準値の採り直しに観測が無い。**
- `SetupEditorPage.tsx` のコピー用 `useEffect` から `setDirtyBaseline(...)` の 1 行を消しても、`SetupEditorPage.copy.test.tsx` の 8 ケースはすべて緑のままである。完了報告が「採らないと何も触っていないのに離脱確認が出る」と明記している性質が、どこにも守られていない。
- `dirtyBaseline` を直接見るのは難しいので、「コピー直後に離脱してもガードが立たない」を 1 ケース置くのが現実的（`M24-04` の既存テストに先例がある）。

**中-4. 「入力済みのセットプレイ名を上書きしない」を固定するテストが無い。**
- 実装は正しい（`setName` は `startAdopt` の 1 か所のみ）。しかしチェックリスト §8 が最大の危険 2 件のうち 1 件として挙げた性質であり、現在どの層にも観測を持たない。`SetplaySuggestionSection` に「名前を書き換えたあと再描画されても値が残る」1 ケースを置けば固定できる。
- 指示書 v1.1.0 が破壊確認 5 を落とした結果として生じた穴であり、**指示書の改版で観測が消えた**ことを設計伝達レポートで報告するのが望ましい。

**中-5. `docs/handover/code-facts.md` が削除済みファイルを 2 件指している。**
- `code-facts.md:128`（`LinkExistingSetupModal` の Props 行）／ `:207`（`useLinkExistingSetupForm` の行）。どちらも HEAD には存在しないファイル。
- `bash scripts/check-derived-docs.sh` は `code-facts.md` を「⚠ 陳腐化疑い（源泉の変化量 163 / 1049 件＝16%）」と出している。
- 派生資料であり `/regen_code_facts` で解消できる。**次に `code-facts` を索引として使う担当が、存在しない部品を前提に計画を立てうる**ため、M24 のうちに再生成しておくのが安い。

**中-6. 完了報告の「新規追加した 6 ファイル」が、直後の括弧内で 9 個列挙されている。**
- `docs/progress/M24-05-completion-report.md:318`。列挙は `ModifiersSummary.tsx` / `ModifiersSummary.test.tsx` / `adoptName.ts` / `adoptName.test.ts` / `SetupEditorPage.copy.test.tsx` / `m24-05-setup-link-and-copy.spec.ts` / `support/setup-link.ts` / モック HTML / 本報告書 の **9 件**。
- 指示書 §9.3 / playbook §4.40 が「N 個あると書かれた列挙は数え直す」と定めており、**その規律を自分の報告書で破っている**形になる。

**中-7. チェックリスト v1.0.0 が指示書 v1.1.0 と食い違ったまま残っている（設計卓の手番）。**
- 製造の突合は正しい（前掲 §12）。しかしチェックリスト本体は未改版であり、`bash scripts/check-instruction-format.sh` は緑のまま（同検査は指示書内の版数一致しか見ず、チェックリストと指示書の版ずれを検出しない）。
- **本サブの再レビューや `M24-CLOSE` で同じチェックリストを読む人が、存在しない `§3.3-4` / `-5` / `-10` や失効した「E2E 4 ケース」「破壊確認 5 件」を根拠に判定しうる。**
- 対応: チェックリストを v1.1.0 対象へ改版する（設計卓）。あわせて `check-instruction-format.sh` に「チェックリストの対象指示書欄と指示書の版数の一致」を足すかを検討する。

**中-8. `internal/service/setup` の記述と実装の食い違い（本サブでは触れないのが正しい）。**
- 完了報告 §9-1 の指摘をレビュー側で追試し、**事実であることを確認した**——`validate.go:149-150` のコメントは「レシピ未変更でも実行する」と書いているが、呼び手 `service.go:481` は `if input.Steps != nil` の内側でしか `ValidateSetupUpdate` を呼んでいない。**`steps` を含めず `name: ""` だけを `PATCH` すると `VAL-S06` が走らない。**
- 画面は常に `steps` を同送するため UI からは踏めない。`§1.6-4` / `§2.2` / `§9.1` が `internal/` の VAL に触ることを禁じているため、**触らなかった判断は正しい**。
- **これは「撤回済み・失効した記述がコード上に残っている」型そのもの**であり、設計卓へ確実に届ける必要がある（高-3 の受け皿が未作成である点と連動する）。

### 低（将来対応）

**低-1. import 順が `CLAUDE.md` §4 に反する箇所が 3 件**（前掲）。機械検出手段が無いため、規約として直すか、規約側を実態へ合わせるかを決めるのが望ましい。

**低-2. 「挙動を 1 文字も変えずに切り出した」は厳密には正しくない。**
- `ModifiersSummary` に `data-testid="modifiers-summary"` が新設されている（`StepRow` の DOM が変わる）。
- `buildAdoptedSetupName` は `steps` が空のとき、旧実装が例外になったのに対し `""` を返す。どちらも実害は無いが、言い切りは実物と食い違う。

**低-3. 完了報告 §11 の `git diff --stat ad42c4a` の貼り付けが、現 HEAD の出力と一致しない。**
- 実測（HEAD `2b53229`）: `29 files changed, 1451 insertions(+), 433 deletions(-)`。報告の貼り付け: `28 files changed, 1110 insertions(+), 433 deletions(-)`。
- 差は完了報告ファイル自身（341 行）ちょうどであり、**採取時点では正しかった**。ただし「§11 は `git diff --stat ad42c4a` の出力である」と書いてある以上、後任が同じコマンドを打つと合わない。1 行の但し書きで足りる。

**低-4. 未経由 12 か所の母数の単位が明示されていない。**
- setup 側限定の数であり、combo 側には `HomePage.tsx:83` / `TrashComboDetailPage.tsx:181-183` が残る。`M24-13` 教訓 6 の作法（数を訂正するのではなく単位を明示する）を適用するのが望ましい。

**低-5. `SetupRecipeEditor` で `modifiers.type` の要約表示が消えた件の説明。**
- 情報は `renderStepLabel`（`MODIFIER_NON_MOVE_TYPES` 経由）で出続けるため実害は無いが、完了報告にその 1 行が無い。

---

## 良かった点

1. **消してよい根拠を、要求された層より深く取った。** 指示書 §3.3-2 は「母集団が同じか」を求めただけだが、実際には API 経路・サービス関数・リポジトリの SQL・react-query の `queryKey` まで下りて「同一である」を示している。レビュー側が独立に追試しても同じ結論になり、**チェックリスト §8 の最大リスクが実測で潰れている**。
2. **「N 個ある」を実際に数え直し、その結果 1 か所の取り残しを防いだ。** followup と `RecipeText.tsx` ヘッダの「5 か所」を信じていれば `SetupRegistrationSection` は残っていた。**しかも失効した「5 か所」をヘッダから消して現状へ書き換えている**——撤回済みの記述をコードに残さない規律を、指示されずに実行している。
3. **memo の見立てと機序の差を、実装を根拠に切り分けた。** `SM-052` は「`RecipeText` を通せば解ける」という指示書の見立てを検証し、実体が modifiers 要約の 2 重実装であることを 5 点の食い違いとして特定している。指示書 §9.3 が求めた形そのものである。
4. **既に成立している機能を「実装しない」と決め、規則を逐語で記録し、複製がずれたら赤くなる形でテストした。** `adoptName.test.ts` の「locales の書式そのものを固定する 3 ケース」は、複製テストの弱点を自覚した設計で、他サブでも真似する価値がある。
5. **観測の限界を自分から書いている。** 破壊確認 1 の「コンポーネントテストは disabled 化では落ちない」は、書かなければ誰も気づかない情報である。
6. **記録の誤りを 2 件、実測で訂正した。** 離脱ガードの出所（`M24-12` → `M24-04`）と、指示書 §7.1-1 の「実査 12 件」（正 9 件）。どちらも根拠コマンド／コミットハッシュ付きである。
7. **E2E の使い捨て DB の性質を理解した fixture 設計。** KA をテストごとに割り当て、その理由（spec をまたいで DB を共有するため母集団が混ざる）を support モジュールに残している。
8. **スコープの線を正確に守った。** `internal/` 差分 0・マイグレ 0・依存 0・ストレージキー 0。`internal/` に見つけた実バグ（`VAL-S06` が走らない経路）も、禁止されている以上「直さずに報告」で止めている。

---

## 制約事項

- 本レビューはコード上で判定可能な範囲のみ対象。実際の動作確認・パフォーマンス・実機テストは別途実施が必要。
- 着手前基準値（フロント `198 files / 2166 tests`）は checkout を伴うため再現していない。**不明: 着手前の実測値そのものは検証できない**（現値 `199 / 2187` と内訳の算術が一致することのみ確認）。
- 完了報告 §2 に引用された「本ターンの開発者回答」（詳細は消す・編集側 `SetupSelectorModal` の削除は任せる・`SM-088` は現状のまま）は、レビュー担当からは検証できない。**不明: 開発者回答の実在と逐語の正確さについては判断できない。** ただしその内容に依存する判断（`SetupSelectorModal` を残す・`SM-088` を通常登録へ広げない）は、いずれも母集団が別であること・指示書 §4.4 の記述と整合することから、回答が無かった場合でも「暫定案で進めた」として説明可能な範囲に収まっている。
- 破壊確認の再実行は行っていない（コードを変更しないため）。破壊確認の結果は、壊す対象の実在と、赤くなると主張された経路のテスト内容から整合性のみを確認した。

---

*以上、M24-05 レビュー報告書。* **重大 0 / 高 4 / 中 8 / 低 5。** **★核心（`CO-002` の母集団検証・`SM-088` の非実装と固定・`RecipeText` を作り替えないこと・`VAL-S06` の維持・`M24-12`/`M24-04` の成果・成立条件を足さない／引き継がない）はすべて満たされている。** **★高 4 件のうち 3 件は「報告書に書いた状態が HEAD の実物と一致していない」型であり、実装ではなく記録の問題である。残る 1 件（E2E (5)）は、名前が守っていると称する経路を誰も見ていない問題である。**

---

## 取り込み結果（自動トリアージ）

**採用 15 件 / 不採用 2 件。★不採用はいずれも「高」ではない**（＝安全弁のエスカレーションは発動していない）。

### 高（4 件・**全件採用**）

| # | 指摘 | 採否 | 対応 |
|---|---|---|---|
| **高-1** | `progress-log.md` へ 1 行も追記されていないのに完了報告が「追記した」「目で確かめた」と書いている | **採用** | 索引行を追記し、`check-progress-log-index.sh` を回して **違反なし** を確認。`grep` で追記行を目視。完了報告 §13 を実測へ書き直し、**初版が食い違っていた事実も残した** |
| **高-2** | `check-md-emphasis.sh` が赤（439/436）なのに完了報告 §12 が「違反なし」 | **採用** | 増えた 3 行（いずれも完了報告自身）の閉じ `**` の直前の約物を強調の外へ出し、**436/436 の緑を確認**。§12 に経緯を残した |
| **高-3** | 設計伝達レポートが存在しないのに「渡した」「申し送った」と過去形 | **採用** | `docs/handover/design-reports/20260830-m24-05-design-exceptions.md` を**実際に作成**（§1 独自確定仕様 4 件 / §2 契約違反の独自判断 2 件 / §3 製造判断 5 件 / §4 残課題 9 件 / §6 CHANGE たたき台 / §7 教訓 9 件）。完了報告の該当箇所を実態へ更新 |
| **高-4** | E2E (5) のテスト名と中身が不一致。「コピーして保存できる」観測がどの層にも無い | **採用** | (5) を **実際にコピー → 名前変更 → レシピを 1 手追加 → 保存 → 親コンボのセットプレイが 2 本になる**まで運ぶ形へ書き直した。**★実装中に判明した重要事項**——レシピを変えずに保存すると `VAL-S04` で止まる（コピー先の親に同一レシピが既に紐付いているため）。**仕様どおりの挙動なので、対照ケース (6) として固定し、設計伝達レポート §1-3 へ申し送った** |

### 中（8 件・**6 件採用 / 2 件不採用**）

| # | 指摘 | 採否 | 対応 / 理由 |
|---|---|---|---|
| **中-1** | 新たに `RecipeText` へ通した 5 か所のうち 3 か所が差し戻しても緑のまま | **採用** | `SetupAccordionItem` / `SetupSelectorModal` / `SetupRegistrationSection` の各テストへ `data-testid="recipe-text"` と `data-recipe-view` を確かめる 1 ケースずつ追加 |
| **中-2** | `SetupAccordionItem` が「番号付き縦展開」へ変わったことがモックにも as-built にも無い | **採用** | 完了報告 §3.3 と設計伝達レポート §3-3 へ as-built として明記（**開発者の目視では見えない面であることも書いた**） |
| **中-3** | コピー時の離脱ガード基準値の採り直しに観測が無い | **採用** | 「コピー直後は離脱ガードが張られない」を 1 ケース追加。**`setDirtyBaseline` を外すと赤くなることを実測**（破壊確認 追-1） |
| **中-4** | 「入力済みの名前を上書きしない」を固定するテストが無い | **採用** | `SetplaySuggestionSection.test.tsx` へ 1 ケース追加。**`value={generatedName}` にすると赤くなることを実測**（破壊確認 追-2）。**★指示書の改版で観測が消えていた件は設計伝達レポート §4-7 へ** |
| **中-5** | `code-facts.md` が削除済みファイル 2 件を指している | **採用** | `bash scripts/generate-code-facts.sh` を実行。2 行が消え `ModifiersSummary` が載った |
| **中-6** | 完了報告の「新規追加した 6 ファイル」の列挙が 9 件 | **採用** | **9 件へ是正**。「N 個ある」を自分の報告書で数え違えた事実も残した |
| **中-7** | チェックリスト v1.0.0 が指示書 v1.1.0 と食い違ったまま | **★不採用** | **製造の手番ではない。** `CLAUDE.md` §8 が「設計書本体は Claude Code が直接編集しない」と定めており、指示書・チェックリストも設計担当の管轄である。**⇒ 設計伝達レポート §2-1 へ、突合表と `check-instruction-format.sh` への追加提案つきで申し送った** |
| **中-8** | `internal/service/setup` の記述と実装の食い違い（`VAL-S06` が走らない経路） | **★不採用** | **指示書 §1.6-4 / §2.2 / §9.1 が `internal/` の VAL に触ることを禁じている。** レビュー自身も「触らなかった判断は正しい」としている。**⇒ 設計伝達レポート §4-2 へ、レビューの追試結果つきで申し送った**（高-3 の受け皿が作られたため失われない） |

### 低（5 件・**全件採用**）

| # | 指摘 | 対応 |
|---|---|---|
| **低-1** | import 順が `CLAUDE.md` §4 に反する 3 件 | 3 件とも相対 import を `@/` 群のあとへ移した（`SetplaySuggestionSection` / `SetupSelectorModal` / `SetupTreeRow`） |
| **低-2** | 「挙動を 1 文字も変えずに切り出した」が厳密には正しくない | 完了報告 §3.2 / §3.5 の言い切りを外し、`data-testid` の新設と `steps` 空時の差を明記 |
| **低-3** | §11 の `git diff --stat` が現 HEAD と一致しない | 採り直したうえで、但し書きを追加した——**「本報告書を含んだ時点の出力である」「見るべきは総数ではなく新規ファイルの deletions」** |
| **低-4** | 未経由 12 か所の母数の単位が未明示 | §1.1-6 に「単位＝セットプレイのレシピを描く面」と、コンボ側に 2 面残ることを明記 |
| **低-5** | `modifiers.type` の要約表示が消えた件の説明が無い | §3.2 へ「情報は `renderStepLabel` が user 語彙で出し続ける。消えたのは内部コードでの重複表示だけ」を追加 |

### 取り込み後の再実行

| 対象 | 結果 |
|---|---|
| `go test ./... -count=1` | **全パッケージ ok**（`internal/` の差分は依然 0） |
| `cd web && pnpm test` | **199 files / 2192 tests passed**（取り込み前 2187 ＋ 5） |
| `make e2e` | **222 passed**（取り込み前 221 ＋ 対照ケース 1） |
| `bash scripts/check-artifact-integrity.sh` | 違反なし |
| `bash scripts/check-progress-log-index.sh` | **違反なし**（取り込み前は `NG 違反 1 件`） |
| `bash scripts/check-md-emphasis.sh` | **違反なし・436/436**（取り込み前は `NG 439/436`） |
| `bash scripts/check-doc-refs.sh` / `check-enum-sync.sh` / `check-browser-storage-keys.sh` | いずれも違反なし |

**★再レビューの往復は 1 回目で完了。上限（2 回）には達していない。** **★「高」の不採用は 0 件のため、開発者へのエスカレーションは発生していない。**
