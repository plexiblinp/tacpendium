# 完了報告: 改善レーン C1〜C3（2026-09-07）

| 項目 | 内容 |
|------|------|
| 作業 ID | `20260907-improvement-lane-c1-c3` |
| 指示 | `docs/handover/session-prompts/20260907-improvement-lane-c1-c3.md` v1.0.0（`D-768`） |
| 由来 | `D-761`（C3・案 A）／ `D-764`（C2）／ `D-766`（C1）／ `D-335`（`scripts/` を触る窓） |
| 着手基点 | `2468099` |
| 成果コミット | `03a36d8` |
| 結果 | **C3 の三部・C2・C1 をすべて実施。常設検査 7 本は出力で判定して緑。破壊確認 2 件は実測で成立。** |

---

## 0. 前提の確認（着手前）

`git log --oneline -15` で 3 本のマージを実測した。

| PR | サブ | マージコミット |
|---|---|---|
| #163 | `M28-02c` | `bac44c1` |
| #164 | `M29-02` | `12e0c58` |
| #165 | `M28-05` | `77cc0c4` |

**3 本ともマージ済み。`M31` は未投入。⇒ 並列 0 本の窓であることを確認して着手した。**

---

## 1. C3 — `check-md-emphasis.sh` の案 A（三部すべて実施）

### 1.1 (1) 常時走査を 5 ディレクトリへ狭めた

`collect_targets()` の `find` 引数から `docs/progress` を外した。除外は現行どおり `/archive/` と `/phase[0-9]+/` で、**触っていない**。

冒頭コメントの「対象範囲」を 5 ディレクトリへ更新し、**2026-08-15 の記述 (1)(2) は消さずに残した**うえで 2026-09-07 の追補を足した。追補の主旨は次のとおり。

- **上の (1) を取り消したのではない。** 当時の理由（読めない行が残ると次の担当が事実を取り違える。`M20-07` と `M21-06` が独立に申し送った）**は今も生きている。変わったのは検査の時点だけである。**
- 外した理由は、床が「増やした本人が自分の追加分を直す」を前提とするのに対し、`docs/progress/` の主部は完了報告・レビュー報告であり**歴史記録として書き換えない**（`D-274` (3)）ため、**2 つの規則が正面から食い違って誰も直さないまま床だけが上がっていたこと**。

### 1.2 (2) `docs/progress/` を「書き込み時に見る」形にした

#### ★指示と実物のズレ 1 件 — ファイル引数モードは既に実装されていた

投入プロンプトは「スクリプトに**ファイル引数モード**を足してください」と述べているが、**同モードは着手時点で既に実装済みだった**（`scripts/check-md-emphasis.sh` の引数解釈と、ベースライン比較を行わない終了処理）。仕様も要求どおりで、**引数で渡したファイルだけを検査し、ベースラインと比較せず、1 行でも検出したら非ゼロで終わる**。

**⇒ 新規実装をせず、既存の挙動を実測で確かめ（§3 の破壊確認）、冒頭の「使い方」へ「これが `docs/progress/` の書き込み時検査を担うモードである」ことと、手順の所在を明記した。** 二重に実装すると、同じ判定を持つ経路が 2 つになる。

#### 完了条件への追記は **5 本入った**

| # | ファイル | 入った | 何を渡させるか |
|---|---|---|---|
| 1 | `.claude/commands/implement_plan.md` | ○ | 新規に作ったファイル（完了報告・その他の新規レポート） |
| 2 | `.claude/commands/implement_plan_full.md` | ○ | 同上 ＋ Phase B のレビュー報告書 |
| 3 | `.claude/commands/incorporate_plan.md` | ○ | 新規 ＋ **この手番で書き足したファイル**（取り込みは既存ファイルを編集する手番であり、新規だけを見ると漏れる） |
| 4 | `.claude/commands/review_plan.md` | ○ | レビュー報告書 `docs/progress/<タスクID>-review.md` |
| 5 | `docs/instructions/templates/M{N}-{NN}-{slug}.template.md` §7.4 | ○ | 完了条件の定型行として（記入指針コメント付き） |

**★足せなかったものは無い。指示の名指し 4 本はすべて実在し、すべて入った。**

**★4 番は投入プロンプトの名指しの外である。** レビュー報告書は `docs/progress/` へ新規ファイルを作る残りの経路であり、**実測の先例もここである**——`M28-02c` のレビュー報告書がコードを引用した 1 行で床が 495 から 496 になった（`followup` の `md-emphasis-check-fenced-block-false-positive` に記録）。**⇒ ここを空けたまま常時走査から外すと、実際に踏んだことのある穴がそのまま残る。開発者の判断を得て追加した**（§6-4 で事後承認を請う）。

節にはマーカー `<!-- COMPLETION-REPORT-MD-EMPHASIS -->` を置き、既存の `<!-- PROGRESS-LOG-INDEX -->` / `<!-- STOP-DISCIPLINE -->` と同じ体裁にした（grep で所在を数えられる形）。

#### 編集が不要だったもの（実物で確認）

- **`_wt` 版 4 本**（`implement_plan_wt` / `implement_plan_full_wt` / `incorporate_plan_wt` / `review_plan_wt`）は、いずれも基底コマンドを Read して従う薄いラッパであり、本文を複製していない。**⇒ 追記不要。**
- **Codex 派生物**。`human-notes/codex/README.md` §5 の同期トリガー表が「製造系コマンド本文の変更 → **原則同期不要**（Skill が同じファイルを直接読む）」と定めている。**⇒ 同期作業なし。**

### 1.3 (3) ベースラインを測り直した — **436 → 247**

| 測ったもの | 値 |
|---|---|
| 旧範囲（6 ディレクトリ・778 ファイル） | **496 行** ／ 旧床 436 ＝ **+60 で着手時点から赤だった** |
| うち `docs/progress/` | **249 行** |
| **新範囲（5 ディレクトリ・563 ファイル）** | **247 行 ← 新しい床** |

- **下がった。** 投入プロンプトの「上がったら狭め方が効いていないので報告」には当たらない。
- **「増える方向の更新はしない」という同スクリプトの注記に反しない**（436 → 247 は下げ方向）。
- **部分的なツリーで測っていない。** 2026-08-14 に `M21-05` レーンが自分のツリーで測って下げ、マージした瞬間にずれた先例があるが、本件は**3 本のマージ後・`M31` の投入前で、並列 0 本の全部入りツリー**で測っている（`D-335` の窓）。

#### ★増加 +60 の所在を数えた

**ほぼ全量が `docs/progress/` にあった。**

| 領域 | 2026-08-15 | 2026-09-07 | 差 |
|---|---|---|---|
| `docs/progress/` | 186 行 | 249 行 | **+63** |
| 残り 5 ディレクトリ | 250 行 | 247 行 | **−3** |

**⇒ 案 A の前提（増分の主部は完了報告であり、書き換えないため誰も直せない）は実測で裏づけられた。**

#### ★新しい床 247 行の内訳を数えた（`followup` が求めていた件）

`md-emphasis-check-fenced-block-false-positive` は「A 案で対象を絞った後に、内訳を一度数えること」を求めていた。数えた結果:

| 領域 | 検出 | うちフェンス内（偽陽性） | フェンス外 |
|---|---|---|---|
| **新しい床（5 ディレクトリ）** | 247 | **0** | 247 |
| 外した `docs/progress/` | 249 | **68** | 181 |

**⇒ 新しい床にフェンス内偽陽性は 1 件も入っていない。偽陽性は `docs/progress/` 側に集中していた。⇒ 案 A は「偽陽性の主部を常時走査から外す」副次効果を持つ**（§6-2）。

以上を `BASELINE_BROKEN` の直上コメントへ、既存の追補と同じ形（いつ・なぜ・いくつからいくつへ）で残した。**測定環境（markdown-it-py 4.2.0 / Python 3.11.15）も併記した**——理由は §6-1。

---

## 2. C2 / C1

### 2.1 C2 — `check-doc-inventory.sh` の EXCEPT

`docs/process/dependency-pin-ops.md` を「恒久資料」群へ理由付きで追加した。理由の文は投入プロンプトの指定どおり「依存の版固定と、その解除の手順。2026-09-06 開発者要求により新設（設計卓の裁定 `D-764`）」とし、**例外表への追加が開発者承認済みであること**（`D-764` / `D-766`）と、**TYPES へ昇格させていないこと**（1 本のファイルであり、まだ「型」ではない）を同じ行へ書いた。

**結果**: 同検査は着手前「型に無いファイル **1 件**」→ 実施後「**型に無いファイルなし**」。既知の例外は 28 → 29 件。

### 2.2 C1 — `app_build_check.md`

「回したら `docs/process/dependency-pin-ops.md` §2 の台帳も見る」を、同ファイルの既存の平文段落と同じ体裁で足した。`D-766` と、同書 §3.1 が本コマンドを**主たる契機**と定めている理由（既に依存を全部見ている手番であり追加コストがほぼ無い）、および見たときの 4 段（同書 §4）への導線を含めた。台帳自体の編集は本コマンドの射程外である旨も書いた。

#### ★指示と実物のズレ 2 件目 — `allowed-tools` に `Read` が無かった

同ファイルの `allowed-tools` は **Bash 4 種のみ**で、`Read` を含んでいなかった。**⇒ 「台帳も見る」の 1 行を足すだけでは、実行時に台帳を読めず、追記が空文になる。`Read` を追加した**（§6-5 で事後承認を請う）。

あわせて補足の 1 項へ、**直接実行**（`! bash …`）**では台帳確認が回らない**旨を足した。同ファイルは「トークンを使わず実行したい場合は直接実行しても同じ出力が得られます」と案内しており、**そのままでは新しい 1 行が黙って迂回される。**

---

## 3. C3 の破壊確認（**2 件そろって成立**）

**「回したら緑だった」は仕組みが生きている証拠にならない**ため、実測で示す。

### 3.1 狭めた範囲の外に置いても、常時走査は反応しない

`docs/progress/tmp-md-emphasis-probe.md` に閉じない強調を 1 行入れて置いた。

```
現在 247 行 / ベースライン 247 行
OK  ベースラインどおり(増加なし)
結果: 違反なし          （exit=0）
```

`--list` の出力を `tmp-md-emphasis-probe` で数えた結果は **0 件**。**⇒ 常時走査は届いていない。**

### 3.2 同じファイルをファイル引数モードへ渡すと落ちる

```
docs/progress/tmp-md-emphasis-probe.md:3
検出: 1 行(ファイル指定のためベースライン比較を行わない)      （exit=1）
```

**陰性対照**として、約物を強調の外へ出した正しい形（`**D-1〜D-259**（256 行）は退避した。`）だけを持つファイルを同モードへ渡すと **検出 0 行・exit=0**。**⇒ 何を渡しても落ちるわけではない。**

### 3.3 後始末

プローブ 2 本は削除済み。`git status` に残っていないこと、および `check-doc-inventory` / `check-md-emphasis` の再実行で現れないことを確認した。

### 3.4 自己検査

`bash scripts/check-md-emphasis.sh --self-test` は狭めた後も **4 対照すべて OK**（陽性＝赤 ／ 正しい形＝緑 ／ コードスパン内の `**`＝緑 ／ 依存欠落＝未実行として非ゼロ）。

---

## 4. 検査（**終了コードでなく、コマンド自身の出力で判定**）

| 検査 | 出力 |
|---|---|
| `check-artifact-integrity.sh`（★1 本目） | **結果: 違反なし** |
| `check-md-emphasis.sh` | **現在 247 行 / ベースライン 247 行 → 結果: 違反なし** |
| `check-doc-inventory.sh` | **結果: 型に無いファイルなし**（着手前は 1 件） |
| `check-doc-refs.sh` | **結果: dead reference なし** |
| `check-progress-log-index.sh` | **検査した 79 件すべてが progress-log に現れる → 結果: 違反なし** |
| `check-stop-discipline.sh` | **結果: 違反なし** |
| `check-instruction-format.sh` | **結果: 違反なし** |

**本完了報告自身も、新運用のとおりファイル引数モードへ通している**（ドッグフーディング）。

**★★その 1 回目で、本報告の 1 行が実際に落ちた。** 検出は `…-completion-report.md:122`、内容は
`**「直接実行（! bash …）では台帳確認が回らない」**旨を足した。` ——**閉じ `**` の直前が全角の鉤括弧**という、
まさに本検査が狙っている型である。約物を強調の外へ出して直し、再実行で **検出 0 行・exit=0** を確認した。

**⇒ 新しい 1 行は、入れた当日に自分自身の欠陥を 1 件捕まえた。** これは案 A の (2) を落とせない理由の実測でもある
——**この行は常時走査の対象外のファイルにあり、(2) が無ければ誰も見ないまま残っていた。**

**★★ドッグフーディングは、足した文言そのものの欠陥も 1 件出した。** `progress-log.md` を同モードへ渡すと
**11 行**が落ちるが、**行番号で切り分けると 11 件すべてが既存行**（本手番の追記は 5599 行目以降、検出は 3152〜4483 行目）
であり、**歴史記録として直さない行**である（`D-274` (3)）。ところが `incorporate_plan` へ足した初版の文言は
「新規 ＋ **書き足したファイル**」であり、**継続更新ファイルまで渡させる形になっていた。**
**⇒ 入れた直後から「回すと必ず赤いので、誰も回さなくなる」経路を作りかけていた。**

**⇒ 4 本すべての節へ 2 つの但し書きを足して直した。**
(a) **継続更新ファイル**（`progress-log.md` 等）**は渡さない。**
(b) **検出が出たら行番号で「自分が書いた行か」を判定し、自分の行だけを直す。**
指示書テンプレート側にも (a) を足した。

**★一般形＝「書いた本人が見る」型の検査は、渡す対象を「本人が作ったもの」に限らないと、**
**既存の汚れごと本人へ請求してしまい、初回から死ぬ。**

**★★あわせて 2 つめの偽陽性の型が出た＝強調が行をまたぐ形。** 上の 4 行はいずれも `**` で始めて
次の行で閉じており、**Markdown としては正しく太字になる**が、本検査は「1 行ずつ描画」するため
両方の行で未閉じに見える。
**⇒ 既知の `md-emphasis-check-fenced-block-false-positive`（フェンス内）とは別の型である。**
本手番では**報告の側を 1 行に収める形へ直して回避した**（検査は変えていない）。
**★実務上の含意＝この検査を通す文書では、1 つの強調を 1 行に収めること。** 節に足した文言にも
「行番号で自分の行かを判定する」但し書きがあり、この型に当たった行はそこで拾える。

### 変更統計（`git diff --stat 2468099 HEAD`）

```
 .claude/commands/app_build_check.md                | 11 +++++-
 .claude/commands/implement_plan.md                 | 18 ++++++++++
 .claude/commands/implement_plan_full.md            | 18 ++++++++++
 .claude/commands/incorporate_plan.md               | 18 ++++++++++
 .claude/commands/review_plan.md                    | 18 ++++++++++
 .../templates/M{N}-{NN}-{slug}.template.md         | 10 ++++++
 scripts/check-doc-inventory.sh                     |  1 +
 scripts/check-md-emphasis.sh                       | 42 +++++++++++++++++++---
 8 files changed, 131 insertions(+), 5 deletions(-)
```

**新規ファイルは本完了報告のみで、`+` だけである**（`NEW-FILE-OVERWRITE-GUARD` (b)）。作成前に `docs/progress/` の同名有無を確認済み。

---

## 5. ■ 併せて更新が要るもの

| 項目 | 状況 |
|---|---|
| 消費した番号の登録 | **なし。** CHANGE / マイグレ連番 / 裁定 / 計測点 / 保留のいずれも消費していない（投入プロンプトの「やらないこと」に自採番が挙がっている） |
| 番号の写し先 | **該当なし**（上記のとおり消費が無い） |
| 版を上げた文書の参照元 | **該当なし。** 版番号を持つ文書は触っていない |
| `CLAUDE.md` §8 の検査表 | **更新不要。** 同表の `check-md-emphasis.sh` の行は「CommonMark 実装で 1 行ずつ描画して判定・ベースライン固定型」とだけ書いており、走査範囲も床の値も書いていない。**⇒ 陳腐化しない書き方になっていた** |
| `docs/handover/followup-backlog.md` | **本手番では編集しない。** `check-md-emphasis-baseline-stale` と `md-emphasis-check-fenced-block-false-positive` の 2 行は本作業で状態が動くが、**§J 以外への製造の直接記入は `D-382` で禁じられている。⇒ §6 の請求事項として設計卓へ回す** |
| `docs/process/parallel-board.md` | **編集しない**（書き手は設計卓のみ） |
| `docs/process/dependency-pin-ops.md` §2 の台帳 | **本手番では埋めない。** 同表の 2 行は「未実査」であり、実物（`web/package.json` / `go.mod`）を読める手番で埋める必要がある。**⇒ §6-6** |
| Codex 派生物 | **同期不要**（§1.2） |

---

## 6. 設計卓への請求事項（判断を仰ぐ事項）

> **★★設計卓が読むのは本節ではない。** 設計卓は完了報告を読まない（2026-08-11 開発者裁定①）。
> **⇒ 本節の内容は設計伝達レポートへ移してある。**
> **正本**: `docs/handover/design-reports/20260907-improvement-lane-c1-c3-design-exceptions.md` の §1〜§4。
> 本節は同レポートの下敷きとして残す。**内容が食い違った場合は設計伝達レポートが正である。**


### 6-1. ★★床の値がレンダラの版に依存する（**最も重い 1 件**）

**開発者が 2026-09-06 に実測した 1,052 行（747 ファイル / 320 ファイル）は、本環境で再現しない。** 本環境の旧範囲は **496 行 / 778 ファイル**である。

いっぽう、**リポジトリ側に記録されている値とは完全に一致する。**

| 記録 | 記録値 | 本環境の実測 |
|---|---|---|
| `M28-04` の着手基点（`followup`） | 494 | — |
| `M28-02c`（`followup`・`D-769`） | 495 → 496 | **496** |
| `docs/handover/retrospective-digest.md` | 41 | **41** |
| `docs/progress/M21-RESEARCH-01-report.md` | 40 | **40** |
| `docs/progress/M26-02-completion-report.md` | 40 | **40** |

**⇒ 外れ値は 1,052 のほうである。** 同じ理由で、**保留 `P-52`（`main` 側の `parallel-board.md` に閉じない強調が 105 行）も本環境では再現しない**——本環境の `parallel-board.md` は **0 行**であり、設計卓ツリーの実測（0 行）と一致する。

**★疑われるのは markdown-it-py の版差である。** 本環境は **markdown-it-py 4.2.0 / Python 3.11.15**。devContainer は `.devcontainer/Dockerfile` の `python3-markdown-it`（Debian パッケージ）を使っており、**版が異なりうる**。CommonMark の版が上がると、全角約物まわりの flanking 判定は変わりうる。

**⇒ 請う判断は 2 つ。**

1. **開発者の devContainer で `bash scripts/check-md-emphasis.sh` と `python3 -c 'import markdown_it; print(markdown_it.__version__)'` を 1 度実測すること。** そこで 247 と出れば版差ではない別の原因であり、大きく違えば版差が確定する。
2. **`P-52` の扱い。** 本件が版差で説明できるなら、`P-52` は「マージ解決で board の内容が落ちたか」ではなく**測定環境の問題**であり、性質が変わる。**★ただし本レーンは board を編集しないため、畳むのは設計卓の手番である。**

なお、**版差が確定した場合に本検査をどうするか**（版を固定するか / 出力へ版を印字するか / 床を版ごとに持つか）は本手番の射程外とした。**版を固定する判断は `CLAUDE.md` §6 の追加ルールに当たり、事前提案が要る**ため。

### 6-2. フェンス内偽陽性の内訳を数えた（`md-emphasis-check-fenced-block-false-positive`）

**新しい床 247 行に偽陽性は 0 件**、外した `docs/progress/` の 249 行に **68 件**（27%）。**⇒ 走査を行単位からブロック単位へ作り替える件の緊急度は下がった**——偽陽性の主部が常時走査の外へ出たため、床を押し上げる作用が消えている。**★ただし消えてはいない**: 書き込み時モードでは `docs/progress/` を 1 ファイルずつ見るため、**コードを逐語引用したレビュー報告書は今後も偽陽性で落ちる**。CLI へ足した節には「逐語引用が理由で直せない行は、直さずに完了報告へ 1 行残す」という逃げ道を明記してある。**⇒ 同 followup 行を閉じるか、性質を書き換えるかの判断を請う。**

### 6-3. 新マーカーに機械検査が無い

`<!-- COMPLETION-REPORT-MD-EMPHASIS -->` は 5 本に入ったが、**黙って消えても誰も気づかない**。`<!-- PROGRESS-LOG-INDEX -->` は `check-progress-log-index.sh` が、`<!-- STOP-DISCIPLINE -->` は `check-stop-discipline.sh` が、それぞれ**マーカーの存在**を見ている。**本手番では検査の新設・改造を射程外とした**（投入プロンプトの射程は 3 件であり、検査の新設は別の判断）。**⇒ 足すか否かの判断を請う。** 足すなら `check-progress-log-index.sh` の `CLI_FILES` と同じ形で 1 ブロック増やすだけで済む（`review_plan.md` と指示書テンプレートが対象に加わる点が違う）。

### 6-4. `review_plan.md` への追加は開発者判断で行った（事後承認を請う）

投入プロンプトの名指しは 4 本だった。理由は §1.2 のとおり。**不要なら 1 節を削るだけで戻せる。**

### 6-5. `app_build_check.md` の `allowed-tools` へ `Read` を足した（事後承認を請う）

**足さないと C1 の 1 行が機能しない**（§2.2）。**⇒ 「1 行足す」という指示の射程を、動く形にするために最小限だけ広げた。**

### 6-6. `dependency-pin-ops.md` §2 の台帳が「未実査」のまま残っている

同表の 2 行（`nwsapi` の `pnpm.overrides` ／ `go.mod` の `toolchain`）は状態が **未実査**である。**本手番の射程は C1〜C3 の 3 件であり、台帳を埋めることは含まれていない**ため触っていない。**⇒ 次に `/app_build_check` を回す手番、または境界セッションで埋めること**（`D-766` が「非武装のセッションでしか埋められない」と述べている）。**★本手番で C1 を入れたことにより、その契機は自動的に回るようになった。**

### 6-7. `followup-backlog.md` の 2 行の状態更新

`check-md-emphasis-baseline-stale`（案 A が入り、床が 247 になった）と `md-emphasis-check-fenced-block-false-positive`（§6-2 のとおり内訳を数えた）は、いずれも本手番で状態が動いた。**製造が直接書けるのは §J だけである**（`D-382`）ため、**本報告を出所として設計卓が畳むこと。**

### 6-8. ★偽陽性の 2 つめの型が出た — **強調が行をまたぐ形**

既知の `md-emphasis-check-fenced-block-false-positive` はフェンス内のグロブを数える型だが、
**本手番の実測で別の型が出た**——`**` で始めて**次の行**で閉じる強調である。
**Markdown としては正しく太字になる**が、本検査は「1 行ずつ描画」するため**両方の行で未閉じに見える。**

**★これは新しい床 247 行にも入りうる。★本手番ではフェンス内の内訳だけを数えており、この型は数えていない。**
**⇒ `md-emphasis-check-fenced-block-false-positive` を「行単位走査に由来する偽陽性」として一般化するか、**
**別行として立てるかの判断を請う。**
どちらにせよ**走査を行単位からブロック単位（ないし段落単位）へ変えれば 2 つとも同時に消える**ため、
作り替えの設計は 1 本で足りる。

**★本手番では検査を変えず、報告の側を 1 行に収める形で回避した**
（射程は 3 件であり、検査の作り替えは別の判断であるため）。

---

---

## 7. やらなかったこと（射程外として意識的に外したもの）

- **既存の検出行の是正**（247 行のいずれも直していない）。**本手番の射程は仕組みだけである。**
- `docs/process/parallel-board.md` の編集 ／ 番号の自採番 ／ `docs/design/` の編集
- ファイルの削除（破壊確認のプローブ 2 本を除く。**候補の提示までが製造の手番である**＝`D-196` 境界条件 3）
- `M31` 以降のサブの実装
- **`check-md-emphasis.sh` の出力へレンダラ版を印字すること**（§6-1 の判断待ち。有用だが、本手番の 3 件の外である）

---

*以上、改善レーン C1〜C3 完了報告。*
