# M24-RESEARCH-01 調査報告: `Memo_Someday` 未完行 と 繰越項目の二系統 ledger

| 項目 | 内容 |
|------|------|
| 指示書 | `docs/instructions/M24-RESEARCH-01-someday-and-carryover-ledger.md` v1.0.0（2026-08-20） |
| 実施日 | 2026-08-20 |
| 実施者 | 実装調査担当 Claude Code（`/research_plan` 自動モード） |
| 性格 | **read-only・judgement-free**。実装差分 0 |
| 成果物 | 本報告 ＋ `docs/progress/progress-log.md` への索引行 |

---

## 0. 冒頭に必ず置くもの

### 0.1 baseline commit

| 対象 | 短縮 hash | 日付 | 件名 |
|---|---|---|---|
| **`docs/human-notes/Memo_Someday.txt` の最終更新** | **`5b775a1`** | **2026-08-19** | `docs(M23/kit): Memo_Someday を再仕分けする（★[予定 M23] 7 行を [予定 M24] へ改番）` |
| リポジトリ HEAD（本調査の実行点） | `e3db6a5` | 2026-08-20 | `Merge pull request #79 from plexiblinp/claude/m22-close-m23-m24-kickoff-s4pyd3` |
| ドラフト作成時点の `Memo_Someday.txt` | **`da5be16`** | **2026-08-13** | `chore: e2e向けの修正`（**§2 の locator 読み替えに使用**） |
| `code-facts.md` の生成元 | `e3634d9` | 2026-08-19 | — |

**★本報告の `locator` 列はすべて `5b775a1:{行番号}` である。** **locator は便宜であって identity ではない**——`Memo_Someday.txt` は行の追加で必ずずれる（ドラフトの `Lnn` が 1 行ずれた実例が §2.0 にある）。**再照合は `identity`（マーカーを除いた本文の先頭 40 文字・逐語）で行うこと。**

### 0.2 走査に使ったコマンドの全文

```bash
# A-1: 行頭マーカー付き項目の全数とマーカー別内訳
grep -oE '^・?\[[^]]+\]' docs/human-notes/Memo_Someday.txt | sort | uniq -c | sort -rn
grep -cE '^・?\[[^]]+\]' docs/human-notes/Memo_Someday.txt          # → 211
wc -l < docs/human-notes/Memo_Someday.txt                            # → 349

# A-2: 未完集合 と 調査 の行番号列
grep -nE '^・\[(未|一部|予定 [^]]+|不明)\]' docs/human-notes/Memo_Someday.txt | wc -l   # → 150
grep -nE '^・\[調査\]' docs/human-notes/Memo_Someday.txt | wc -l                        # → 10

# B-4: baseline commit
git log -1 --format=%h -- docs/human-notes/Memo_Someday.txt          # → 5b775a1

# A-4 / C-5 / E-4: ドラフト作成時点の blob を復元して Lnn を読み替える
git log --format='%h %ad' --date=short --until=2026-08-14 -1 -- docs/human-notes/Memo_Someday.txt   # → da5be16 2026-08-13
git show da5be16:docs/human-notes/Memo_Someday.txt | grep -oE '^・?\[[^]]+\]' | sort | uniq -c | sort -rn

# D-1: followup-backlog から M24 割付を抽出
grep -n 'M24' docs/handover/followup-backlog.md          # → 17 行（内訳は §3.0）

# F-3: queryKey の全数（本番コードのみ・test 除外）
grep -rnoE 'queryKey:\s*\[' web/src --include=*.ts --include=*.tsx | grep -v '\.test\.' | wc -l   # → 69
grep -rnE 'queryKey:\s*[A-Z_]+\s*[,}]' web/src --include=*.ts --include=*.tsx | grep -v '\.test\.'  # → 15
grep -rlE 'queryKey:' web/src --include=*.ts --include=*.tsx | grep -v '\.test\.' | wc -l         # → 39 ファイル
grep -rhoE 'queryKey:\s*\[\s*("[^"]+"|[A-Z_]+)' web/src --include=*.ts --include=*.tsx | grep -v '\.test\.' | sed -E 's/.*\[\s*//' | sort | uniq -c | sort -rn

# C-2 の証拠取り（実コード実査・抜粋）
grep -n 'formatStarterStatus' -A6 web/src/features/combo/utils.ts
grep -rn 'OKI_NO_GAUGE_LABEL' web/src                                # → 定義 1 件のみ・参照 0
grep -rn '0〜20本\|0〜6本\|(0〜3)\|(0〜6)' web/src --include=*.tsx --include=*.ts --include=*.json
grep -rn 'INITIAL_CHARACTER_ID' web/src
grep -rln 'useRestoreCombo\|usePermanentDelete\|useTrashCombos\|/trash' web/src
grep -rn '既存から紐付け' web/src
grep -rn 'DeleteComboConfirm\|useDeleteCombo' web/src --include=*.tsx --include=*.ts | grep -v test
ls migrations/*.up.sql | tail -4                                     # → 000077 が末尾

# DoD 7: read-only の立証
git status --porcelain                                               # → 出力なし（本報告作成前の時点）
```

**★「当たること」の確認**（`E-84`）。`grep -oE '^・?\[[^]]+\]'` は **211 件を返した**（0 件ではない）。マーカーの実体は全件が `・[マーカー]` 形式（行頭 `・` 付き）であり、`・` を任意にした正規表現でも同数になることを確認済み。

### 0.3 本報告が数えた範囲（`E-93`）

| 対象 | 範囲 |
|---|---|
| **系統 1** | `docs/human-notes/Memo_Someday.txt` の **1〜349 行の全体**。うち **行頭に `・[マーカー]` を持つ 211 行**を項目とみなす |
| **系統 2** | (A) `docs/handover/followup-backlog.md` の **全 596 行**から「割付」欄または集約文に `M24` を含む箇所 ／ (B) `docs/instructions/phase3-overview.md` **§M24（256〜265 行）の箇条書き** |
| **実コードの実査** | `web/src/**`（FE）・`internal/api/*/routes.go`・`migrations/` に限る。**BE の全レイヤ実査は行っていない** |
| **数えていないもの** | `docs/human-notes/future-notes/` 配下 ／ `docs/human-notes/milestone-startup-kit/` ／ `sf6_app_naming_shortlist.txt` ／ `docs/seed-data/setplay-move-notes.md`（いずれも memo 行から参照されるだけで、本 ledger の母集団ではない） |

---

## §1 母集団（軸 A）

### 1.1 A-1 / A-2: マーカー別の内訳（実測）

| マーカー | 実測 | 未完集合か |
|---|---:|---|
| `[未]` | **104** | ○ |
| `[済]` | 51 | — |
| `[一部]` | **35** | ○ |
| `[調査]` | **10** | 種別 = 調査（**A-5 により ledger に含める**） |
| `[予定 M24]` | **9** | ○ |
| `[予定 M23]` | **1** | ○（**ただし M24 の母集団外**。§1.4） |
| `[不明]` | **1** | ○ |
| **合計（マーカー付き項目）** | **211** | — |
| **未完集合**（`[未]`＋`[一部]`＋`[予定 *]`＋`[不明]`） | **150** | — |
| **マーカーを持たない `・` 行** | **10 行**（**4〜13 行目**） | **項目ではない**（memo の使い方の説明と追記の履歴） |
| ファイル総行数 | 349 | — |

### 1.2 A-3: 設計卓の §3.2 実測値（2026-08-20）との突合

**結論: 7 マーカーすべて・合計・未完集合・マーカー無し `・` 行の行数まで、完全に一致した。**

| # | 設計卓 §3.2 | 本調査 | 判定 |
|---|---|---|---|
| 1 | 総数 211 | 211 | **一致** |
| 2 | `[未]`104 / `[済]`51 / `[一部]`35 / `[調査]`10 / `[予定 M24]`9 / `[予定 M23]`1 / `[不明]`1 | 同左 | **一致** |
| 3 | 未完集合 150 | 150 | **一致** |
| 4 | マーカー無し `・` 行 10 行（4〜13 行目） | 10 行（4〜13 行目） | **一致** |
| 5 | `[予定 M20]` 0 件 | 0 件 | **一致** |
| 6 | `[予定 M23]` は line 91 の 1 件 | line 91 の 1 件 | **一致** |

**⇒ 不一致は 0 件のため、両論併記は不要である。**

### 1.3 A-4: ドラフト §1.3（131 行・2026-08-13 スナップショット）との差分

**★ドラフト作成時点のファイルが git に残っている**（`da5be16`・2026-08-13）。同 blob のマーカー内訳は **ドラフト §1.3 と完全に一致**した。**⇒ ドラフトの母集団は `da5be16` を基準としており、差分をマーカー別に機械算出できる。**

| マーカー | ドラフト（`da5be16`） | 現行（`5b775a1`） | 差分 |
|---|---:|---:|---:|
| `[未]` | 88 | 104 | **+16** |
| `[一部]` | 32 | 35 | **+3** |
| `[予定 M23]` | 7 | 1 | **−6** |
| `[予定 M24]` | 0 | 9 | **+9** |
| `[予定 M20]` | 3 | 0 | **−3** |
| `[不明]` | 1 | 1 | ±0 |
| **未完集合 計** | **131** | **150** | **+19** |
| `[調査]` | 8 | 10 | **+2** |
| `[済]` | 51 | 51 | ±0 |
| **マーカー付き項目 計** | **190** | **211** | **+21** |

**+19 の内訳（実測）**:

- **+18 件** = 2026-08-19 に開発者が素で書き足していた 21 行のうち、実装要求のもの（`SM-140`〜`SM-160` の 18 件。残り 3 件は `[調査]`）
- **+1 件** = `SM-036`（line 91）。**`[調査]` から `[予定 M23]` へ上がったため未完集合に入った**

**`[予定 M23]` −6 / `[予定 M24]` +9 の内訳**: 2026-08-19 の改番で `[予定 M23]` 7 行が `[予定 M24]` へ移り（−7）、line 91 が `[調査]` → `[予定 M23]` へ上がった（+1）。`[予定 M24]` 9 件 = 改番 7 件 ＋ 新規 2 件（`SM-150`・`SM-159`）。

**`[予定 M20]` −3 の内訳**: `SM-013`（→`[一部]`）／ `SM-017`（→`[未]`）／ `SM-045`（→`[一部]`）。memo の 2026-08-19 追記 (3) と一致。

### 1.4 `[予定 M23]` 1 件（`SM-036`）の扱い

指示書 §3.2 #6 は「**M24 の母集団に含めない**」とし、DoD 2 は「**未完集合の全行に `SM-ID` と disposition**」を求める。**両立させるため、150 行すべてに `SM-ID` を振ったうえで `SM-036` の disposition を `返却（M23）` とした。**

- 照合先: `M23-overview` §3 の `M23-04`（復元時のバリデーション）が**主参照に `Memo_Someday` line 91 を明記**している ／ **D-463**
- **⇒ M24 の実装候補の母集団は 149 行**（150 − `SM-036`）。

---

## §2 系統 1 ledger（軸 B / C）

### 2.0 凡例と、locator に関する注意

| 列 | 意味 |
|---|---|
| `SM-ID` | **`SM-001` から、ファイルの出現順**。**未完集合 150 行 ＋ `[調査]` 10 行の計 160 行**に、`5b775a1` 上の出現順で 1 度だけ採番した。**以後変えない** |
| identity | **マーカーを除いた本文の先頭 40 文字（逐語）**。**これが再照合の照合キーである** |
| locator | **`5b775a1:{行番号}`**。**★locator は便宜であって identity ではない**——行の追加で必ずずれる |
| 元マーカー | `5b775a1` 時点のマーカー |
| 種別 | `実装要求`（150 行）／ `調査`（10 行・**A-5 により含める**） |
| disposition | `実装` / `調査` / `返却` / `却下` / `要判断` の 5 種（指示書 §4 C-1） |
| 代表 ID | 重複を束ねた先。**束ねた側の行も残してある**（C-3） |

> **★ドラフトの `Lnn` は「失効」ではなく、決定論的に読み替えられる**（§1.3 の発見）。**ドラフトの 131 個の `Lnn` は、`da5be16` 上の行 → 逐語本文 → 現行行 の経路で 131/131 が解決し、重複も `[調査]` の紛れ込みも 0 件だった。** 対応は §2.2 と §4.4 に置く。**なお現行行番号はドラフトの `Lnn` より 1 大きい**（line 1〜325 の範囲。それ以降は新規 21 行が末尾に付いただけ）。

### 2.1 disposition の集計

| disposition | 実装要求（150） | 調査（10） | 計（160） |
|---|---:|---:|---:|
| **`実装`** | 60 | 0 | **60** |
| **`返却`** | 49 | 3 | **52** |
| **`要判断`** | 34 | 2 | **36** |
| **`調査`** | 5 | 3 | **8** |
| **`却下`** | 2 | 2 | **4** |
| **未付与** | **0** | **0** | **0** |

**★DoD 2 を満たす**——未付与 0 件。**照合先を示せなかった行は `要判断` に倒した**（§0.3 の指示どおり）。

**`返却` 52 件の返却先内訳**:

| 返却先 | 件数 | 主な `SM-ID` |
|---|---:|---|
| **フェーズ4** | 30 | `SM-023`〜`SM-035`・`SM-042`・`SM-046`・`SM-050`・`SM-081`・`SM-087`・`SM-100`・`SM-105`・`SM-106`・`SM-116`・`SM-123`〜`SM-125`・`SM-134`・`SM-135`・`SM-138`・`SM-147` |
| **M14 / update wave** | 12 | `SM-055`・`SM-056`・`SM-063`・`SM-070`・`SM-082`・`SM-091`・`SM-102`・`SM-108`・`SM-113`〜`SM-115`・`SM-132`・`SM-137`・`SM-140` |
| **正式リリース前後** | 5 | `SM-029`(※フェーズ4 と重複計上せず)・`SM-152`・`SM-153`・`SM-158`・`SM-160` |
| **M23** | 1 | `SM-036` |
| **利用者フィードバック運用（followup §A）** | 2 | `SM-038`・`SM-107` |
| **方針（future-notes）／将来課題** | 2 | `SM-154`・`SM-156` |

### 2.2 ledger 本体（160 行）

| `SM-ID` | identity（本文先頭 40 字） | locator | 元マーカー | 種別 | disposition | 証拠 | 触る面 | 代表 ID | 備考 |
|---|---|---|---|---|---|---|---|---|---|
| `SM-001` | 詳細の起き攻め情報がおかしい（いつのまにか中段攻めになる）／比較画面に表示方式を | `5b775a1:19` | `[一部]` | 実装要求 | **調査** | memo line19 の 2026-07-21 注記（M16-03・CHANGE-064 系で起き攻めモデル刷新／表示方式は正規化で一本化済み） | 画面: `/combos/:id`・`/compare` | — | 何が分かれば実装できるか＝**M16-03 正規化後に「中段攻めになる」が再現するか**。ドラフト M23-05 の gate と同旨 |
| `SM-002` | マイコンボの件数表示がタブ切替ボタンと紛らわしい　／【2026-07-21】未対 | `5b775a1:29` | `[未]` | 実装要求 | **実装** | 現行コード `web/src/features/mycombo/components/MyComboPage.tsx` 実在 | 画面: `/mycombo` | — | ドラフト M23-01 |
| `SM-003` | コンボ比較から比較中コンボの詳細画面へ遷移できるように　／【2026-07-21 | `5b775a1:36` | `[未]` | 実装要求 | **実装** | 現行コード `web/src/pages/ComparePage.tsx` / `features/combo/components/CompareTable.tsx` | 画面: `/compare` → `/combos/:id` | — | ドラフト M23-03 |
| `SM-004` | マイコンボのソートがコンボ一覧と不一致（ソート部分は部品化した方がよい？）　／【 | `5b775a1:39` | `[未]` | 実装要求 | **実装** | 現行コード `features/combo/components/ComboSortControls.tsx`（一覧側）と `MyComboPage.tsx`（マイコンボ側）が別実装 | 画面: `/mycombo`・`/combos` ／ 共通部品: ソート部品の抽出 | — | SM-129 (c) の読みと同件になりうる |
| `SM-005` | 一覧のソート項目から相手スタンスを消す ／【2026-06-28 M12-01( | `5b775a1:40` | `[一部]` | 実装要求 | **要判断** | memo line40 の 2026-06-28 注記（M12-01/CHANGE-044 で一覧本体は控えめ表示化。ソート選択肢からの削除の要否は未確定） | 画面: `/combos`（ComboSortControls.tsx） | — | 論点＝**相手スタンスをソート項目として残すか**。SM-065 と対 |
| `SM-006` | 内部値の画面表示を消す（例：「始動技(starter_move_id)」→「始動 | `5b775a1:42` | `[一部]` | 実装要求 | **実装** | 実査: `web/src/features/combo/utils.ts:49-52` が `starterMoveCode`（英語 move code）を先頭に push | 画面: `/combos`・`/moves/edit` ／ locale | — | SM-059 が具体例。技編集の保存トーストが move code を出す件も memo が記録 |
| `SM-007` | ドライブゲージ/SAゲージの「(0〜6)」を「(0〜6本)」に（日本語版のみ）　 | `5b775a1:43` | `[予定 M24]` | 実装要求 | **実装** | 実査: `ComboEditorBasicFields.tsx:307`「(0〜3)」・`:339`「SAゲージ消費(0〜6)」／ followup B §F12-2（B-9・**M24 割付**） | 画面: `/combos/new`・`/combos/:id/edit` ／ locale | — | ドライブ始動側は既に「(0〜6本・0.5刻み)」＝済。SM-101 と同一ファイル |
| `SM-008` | linkを日本語版では「目押し」と表示したい（今はリンク）　／【2026-07- | `5b775a1:44` | `[未]` | 実装要求 | **実装** | memo line44 の 2026-07-21 注記（i18n/表記） | locale（ja） | — | ドラフト M23-07 |
| `SM-009` | 何らかのボタンを押すのにスクロールを多用するものは見つけ次第直す　／【2026- | `5b775a1:45` | `[一部]` | 実装要求 | **調査** | memo line45 の 2026-07-21 注記（登録画面は M15-05 で軽減・全画面横断は継続 TODO） | 画面横断 | — | 何が分かれば実装できるか＝**どの画面のどのボタンがスクロール必須か**の棚卸し |
| `SM-010` | タグが増えたり長くなると一覧のフィルタ表示が崩れる（ただ並べるだけの形式を見直し | `5b775a1:47` | `[予定 M24]` | 実装要求 | **実装** | followup B §F12-2「G-3 タグ表示崩れ＋タグフィルタのドロップダウン化」＝**M24 割付・未着手** ／ 実査: `ComboListFilters.tsx` に折りたたみ・ドロップダウン機構 0 件 | 画面: `/combos` ／ 共通部品: タグ UI | — | memo 自身が SM-064 と同件と明記 |
| `SM-011` | セットプレイ編集にもコピー機能（途中までレシピが同じ場合がある）　／【2026- | `5b775a1:51` | `[未]` | 実装要求 | **実装** | 現行コード `web/src/pages/SetupEditorPage.tsx` 実在 | 画面: `/setups/:setupId` | — | ドラフト M23-05 |
| `SM-012` | 一覧に紐づいているセットプレイ数を表示　／【2026-07-21】未対応 | `5b775a1:53` | `[未]` | 実装要求 | **実装** | 現行コード `features/combo/components/ComboTable.tsx`（列は表示列カスタマイズ制） | 画面: `/combos` ／ API 契約: 件数の取得経路 | — | ドラフト M23-05。件数取得が既存 API で賄えるかは未確認 |
| `SM-013` | カスタムプリセットをキャラ毎に設定したい（プリセット方式の考え直しが必要）　…B | `5b775a1:54` | `[一部]` | 実装要求 | **却下** | memo line54 の 2026-08-19 注記＝**D-291**「カスタムプリセットは横断 1 枚。キャラ単位は UI とコピー時の実体化で満たす」／`M20-04` がその形で着地（`DES-004` §6） | — | — | 却下理由＝**方針と衝突**（採らないという裁定が済んでいる）。ドラフト §5.1「M20 へ返す」は M20 未完了時の記述 |
| `SM-014` | コンボ比較のルート省略＋ツールチップが見にくい（行を広げて全部表示したい）　／【 | `5b775a1:55` | `[未]` | 実装要求 | **実装** | 現行コード `features/combo/components/CompareTable.tsx` | 画面: `/compare` | — | ドラフト M23-03 |
| `SM-015` | コンボの各ステップをドラッグして入れ替え　／【2026-07-21】未対応 | `5b775a1:56` | `[未]` | 実装要求 | **要判断** | ドラフト §3 M23-04 gate「L55 の drag は RESEARCH または小 PoC で採否を確定する」 | 画面: `/combos/new`・`/combos/:id/edit`（RecipeBuilder） | — | 論点＝**ドラッグ並べ替えを M24 の必須にするか PoC ゲートに置くか**。followup `controller-complete-input-reorder`（`DES-005` §6.5）が同じ操作面を要求する |
| `SM-016` | 「ステップ」という表現の再検討（画面表示上）　／【2026-07-21】M16- | `5b775a1:57` | `[一部]` | 実装要求 | **実装** | memo line57 の 2026-07-21 注記（M16-04 で step taxonomy 明文化・画面表示語の再検討は継続） | locale ／ 画面横断のラベル | — | ドラフト M23-07 |
| `SM-017` | アプリスタートアップのプリセット選択の表記例を示したい　／【2026-07-21 | `5b775a1:58` | `[未]` | 実装要求 | **要判断** | memo line58 の 2026-08-19 注記（`DES-005` 初回起動ウィザード Step 4 に表記例の記述は無い＝第 82 版で実査） | 画面: `/wizard` | — | 論点＝**各プリセットの表記例として何を出すか**（要追加仕様） |
| `SM-018` | 比較機能のコンボ追加が使いにくい（子ウィンドウ見直し、または「コンボを追加」ボタ | `5b775a1:59` | `[未]` | 実装要求 | **実装** | 現行コード `features/combo/components/AddComboToCompareModal.tsx` | 画面: `/compare` ／ 共通部品: モーダル | — | ドラフト M23-03 |
| `SM-019` | 既存セットプレイの紐付けが分かりにくい（量が増えると特に）　／【2026-07- | `5b775a1:60` | `[予定 M24]` | 実装要求 | **実装** | followup B §F12-3/E-1（**M24 割付**）／ 実査: `web/src/pages/ComboDetailPage.tsx:144`「既存から紐付け」・`features/setup/hooks/useLinkExistingSetupForm.ts` | 画面: `/combos/:id` ／ API 契約: setup candidate/link | — | memo 自身が SM-062 と同件と明記。**`ComboDetailPage.tsx` は M23 の削除導線と同一ファイル**（§5 交差面） |
| `SM-020` | コンボ一覧等のスクロールを、リンク/ボタン/プルダウン以外の場所クリックでも動か | `5b775a1:61` | `[未]` | 実装要求 | **要判断** | ドラフト §3 M23-03 gate「click-drag scroll（L60）はアクセシビリティと pointer 操作を PoC してから採否を決める」 | 画面: `/combos` | — | 論点＝**採用可否** |
| `SM-021` | コンボ比較の表示スタイルを切替可に（例：レシピを縦に表示）　／【2026-07- | `5b775a1:62` | `[未]` | 実装要求 | **実装** | 現行コード `CompareTable.tsx` | 画面: `/compare` | — | SM-022 / SM-096 / SM-130 と同根（レシピの改行・縦表示） |
| `SM-022` | コンボ詳細のレシピも縦表示できるように（ステップごと改行）　／【2026-07- | `5b775a1:63` | `[未]` | 実装要求 | **実装** | 現行コード `features/combo/components/ComboDetailRecipe.tsx` | 画面: `/combos/:id` | — | memo が SM-096 と同根と明記 |
| `SM-023` | モダン対応（取り扱い方の決定）　…クラシックのみ＝先行前は不要。ISSUE-00 | `5b775a1:67` | `[未]` | 実装要求 | **返却** | `phase3-overview` §2（フェーズ3 はモダンを明示的に排除・配布対象はクラシック） | — | — | 返却先＝**フェーズ4** |
| `SM-024` | ゲームテーブルはもう要らない／別ゲー対応は別プロジェクト化したい　…NFR407 | `5b775a1:68` | `[未]` | 実装要求 | **返却** | memo line68 の 2026-07-21 注記（NFR407 はフェーズ4 据え置き・`phase3-overview` §2） | — | — | 返却先＝**フェーズ4** |
| `SM-025` | セットプレイの重ねた技から始められるコンボの管理　…複雑・要打合せ・要設計 | `5b775a1:69` | `[未]` | 実装要求 | **返却** | ドラフト §5.3（Phase 4・新製品機能）／ memo 本文「複雑・要打合せ・要設計」 | — | — | 返却先＝**フェーズ4** |
| `SM-026` | 連携も管理したい　／【2026-07-21】未対応(フェーズ4寄り) | `5b775a1:71` | `[未]` | 実装要求 | **返却** | memo line71 の 2026-07-21 注記（フェーズ4 寄り） | — | — | 返却先＝**フェーズ4**。SM-050 と同群 |
| `SM-027` | セットプレイ自動発見機能（独立モジュール化、規格を守れば単独で他アプリ組込可）　 | `5b775a1:72` | `[一部]` | 実装要求 | **返却** | memo line72 の 2026-08-11 注記（M19 クローズ済。未達は「独立モジュール化して他アプリ組込可」＝本アプリ内 `internal/service/setplay` として実装） | — | — | 返却先＝**フェーズ4／別プロジェクト化**（SM-024 と同方向） |
| `SM-028` | 全体一括で仮登録にする機能（アプデ後にユーザーが検証して本登録に直す等）　／【2 | `5b775a1:74` | `[未]` | 実装要求 | **返却** | memo line74 の 2026-07-21 注記（フェーズ4 寄り） | — | — | 返却先＝**フェーズ4** |
| `SM-029` | ダークモード　／【2026-07-21】未対応 | `5b775a1:78` | `[未]` | 実装要求 | **返却** | memo の配置節「正式リリース前後」／ ドラフト §5.3 | — | — | 返却先＝**正式リリース前後** |
| `SM-030` | 中国語対応　…NFR307は日英のみ。拡張（フェーズ4寄りでも可）　／【2026 | `5b775a1:79` | `[未]` | 実装要求 | **返却** | memo line79（NFR307 は日英のみ） | — | — | 返却先＝**フェーズ4**。★`D-2` 英語ロケール整備（M24 スコープ）とは別物 |
| `SM-031` | コンボ/セットプレイに画像や動画も添付可能に　／【2026-07-21】M17- | `5b775a1:80` | `[一部]` | 実装要求 | **返却** | memo line80 の 2026-07-21 注記（M17-01/CHANGE-068 で参照 3 フィールドは実装済。動画実体の配布・容量設計は reorg §3 でスコープ外。セットプレイ側は対象外） | — | — | 返却先＝**フェーズ4**。残件は 2 つ＝(1) 実体配布・容量設計 (2) セットプレイ側への展開 |
| `SM-032` | 始動状況や始動技を細かく指定したい（中Pで表示、その他の始動状況で表示等）　…射 | `5b775a1:84` | `[未]` | 実装要求 | **返却** | ドラフト §5.3（schema/domain 拡張）／ memo 本文「射程が読みにくくデータ拡張を含む」 | — | — | 返却先＝**フェーズ4** |
| `SM-033` | ヒット状況をもっと拡充（壁やられ(ガード)、壁やられ(ヒット)、ガークラ等）／区 | `5b775a1:85` | `[一部]` | 実装要求 | **返却** | memo line85 の 2026-08-11 注記（ジャスパ反撃のみ M18-01 で 4 値化。壁やられ（ガード/ヒット）・ガークラ・インパクト壁やられ は未） | — | — | 返却先＝**フェーズ4**。SM-106 / SM-116 の代表 |
| `SM-034` | 微妙な当て方で起き上がりが変わる奴（今はフレーム一定扱い、ユーザー説明もない）　 | `5b775a1:86` | `[未]` | 実装要求 | **返却** | memo 本文「方針未決定」／ ドラフト §5.3 | — | — | 返却先＝**フェーズ4** |
| `SM-035` | 連キャンを扱いたい（弱P-弱P と 弱P(キャンセル)-弱P で全体フレームが変 | `5b775a1:87` | `[未]` | 実装要求 | **返却** | memo line87 の注記（フェーズ4 寄り） | — | — | 返却先＝**フェーズ4** |
| `SM-036` | ゴミ箱から復元するデータのバリデーションが十分かチェック　…調査　／【2026- | `5b775a1:91` | `[予定 M23]` | 実装要求 | **返却** | `M23-overview` §3 の `M23-04`（復元時のバリデーション）が**主参照に `Memo_Someday` line 91 を明記** ／ **D-463** | — | — | 返却先＝**M23**。★指示書 §3.2 #6 のとおり **M24 の母集団に含めない**（DoD 2 に従い ID と disposition のみ与える） |
| `SM-037` | 画面項目の一貫性の調査（ボタン配置、項目名等）　…調査 | `5b775a1:92` | `[調査]` | 調査 | **調査** | memo 本文「…調査」 | 画面横断（ボタン配置・項目名） | — | 何が分かれば実装できるか＝**現行の不揃いの棚卸し**。M24 の UX 整理の直接の補助入力 |
| `SM-038` | 画面項目に違和感があるので利用者と共に修正していきたい　…利用者フィードバック運 | `5b775a1:93` | `[調査]` | 調査 | **返却** | memo line93 の 2026-07-21 注記（friend FB 運用として消化・以降は followup §A で追跡） | — | — | 返却先＝**利用者フィードバック運用（followup §A）** |
| `SM-039` | セットプレイ編集の仕様調査（編集したら全部変わらないか／紐づく他コンボに影響しな | `5b775a1:94` | `[調査]` | 調査 | **調査** | memo 本文「…フェーズ2着手前の地ならし候補」 | — | — | 何が分かれば実装できるか＝**セットプレイを編集したとき紐づく他コンボへ影響するか**。★M23 の復元単位（**D-459** 集約単位）と隣接する |
| `SM-040` | 他にも追加構想は別メモにまとめている　…認識のみ | `5b775a1:97` | `[調査]` | 調査 | **却下** | memo 本文「…認識のみ」 | — | — | 却下理由＝**行動要求ではない**（別メモの存在の記録） |
| `SM-041` | 初回起動ウィザードのデフォルト表示キャラは未実装？　…M6で実装済みの可能性大（ | `5b775a1:101` | `[一部]` | 実装要求 | **実装** | followup B §F12-1/E-2（**M24 割付**・`INITIAL_CHARACTER_ID=1` 固定）／ 実査: `INITIAL_CHARACTER_ID` は `MyComboPage.tsx` / `ComboListFilters.tsx` / `AddComboToCompareModal.tsx` / `ComboEditor` の 4 面で参照 | 画面: `/wizard`・`/settings`・`/combos`・`/mycombo`・`/compare` ／ 共通部品: 既定キャラ解決 ／ config 契約 | — | **E-2 クラスタの代表**（SM-044 / SM-058 / SM-119 を束ねる） |
| `SM-042` | ノーキャン（通常技ヒット後にキャンセルせずに必殺技を出して当てる事）とキャンセル | `5b775a1:104` | `[未]` | 実装要求 | **返却** | ドラフト §5.3（metadata/domain 拡張） | — | — | 返却先＝**フェーズ4**。SM-046 と関連 |
| `SM-043` | キャラクター選択方式、プルダウンだと多分たいへん。他にもプルダウン系は再検討した | `5b775a1:105` | `[一部]` | 実装要求 | **実装** | 実査: `features/mycombo/components/CharacterSelector.tsx` は平坦な Select（検索欄・グループ化なし）／ `ComboListFilters.tsx` は native `<select>` | 画面: `/combos`・`/mycombo`・`/compare` ／ 共通部品: CharacterSelector | — | **キャラ選択クラスタの代表**（SM-120 / SM-136 を束ねる） |
| `SM-044` | 編集や登録から戻った時にキャラクターのフィルターが編集や登録してたキャラを維持し | `5b775a1:106` | `[予定 M24]` | 実装要求 | **実装** | followup B §F12-1/E-2（**M24 割付**） | 画面: `/combos`（編集・登録からの復帰） | `SM-041` |  |
| `SM-045` | コマンドアイコン名からコマンドを逆算して、プリセット作成に役立てれるのでは？　… | `5b775a1:109` | `[一部]` | 実装要求 | **要判断** | memo line109 の 2026-08-19 注記（`M20-07`＋`M22-07b` で逆引き機構は実運用に載ったが、**プリセット作成支援の導線は未整備**＝カスタムプリセット作成は組み込みからのコピーが起点・`DES-004` §6） | 画面: `/presets/:id/edit` | — | 論点＝**逆引きをプリセット作成支援へどう出すか**（要追加仕様） |
| `SM-046` | modifyあたりを再検討 | `5b775a1:112` | `[未]` | 実装要求 | **返却** | ドラフト §5.3（metadata/domain 拡張） | — | — | 返却先＝**フェーズ4**。SM-042 と関連 |
| `SM-047` | ラッシュ技の扱い、本当に今の仕様でいいものか....　／【2026-07-21】 | `5b775a1:113` | `[未]` | 実装要求 | **要判断** | **D-305**（ラッシュ命名は 272 件中 268 件が 1 形へ統一済み・揺れ 0 件）／ followup B 群 B-1 命名クラスタ | — | — | 論点＝**ラッシュ技の現行表現を維持するか**（ドメイン知識が要る） |
| `SM-048` | キャンセルラッシュと、生ラッシュ技の違いの表現について、再検討した方がいいかも. | `5b775a1:114` | `[未]` | 実装要求 | **要判断** | memo line114（注記なし＝2026-07-21 以降の実査対象外のまま） | — | — | 論点＝**キャンセルラッシュと生ラッシュ技を表現上分けるか**。SM-047 と同クラスタ |
| `SM-049` | ユーザーにmovesいじれるようにする場合、ユーザー間の差異はどうする？インポー | `5b775a1:115` | `[未]` | 実装要求 | **要判断** | followup B 群 `B-2-heavy`（moves 手動 CRUD・**要判断**・M14 確定後にスコープ再評価） | — | — | 論点＝**moves をユーザーが編集できるようにするか**。★SM-126 と**方向が逆**（memo line313 が明記） |
| `SM-050` | 連携を割る（無敵、小技、昇竜、インパクト、ドライブリバーサル等）も管理したい。　 | `5b775a1:116` | `[未]` | 実装要求 | **返却** | memo line116 の 2026-07-21 注記（フェーズ4 寄り・連携管理と同群） | — | — | 返却先＝**フェーズ4** |
| `SM-051` | キャラクターが増えた時に簡単に更新できるカスタムコマンド（or何か？）を整備した | `5b775a1:117` | `[一部]` | 実装要求 | **要判断** | memo line117 の 2026-07-21 注記（M17-04 完了・**importer 側の見直しはフェーズ3 完了時目途のまま**） | — | — | 論点＝**`combomgr-importer` 側の見直しを M24 で行うか、SM-158（別リポジトリ化）と束ねるか**。★対象がリポジトリ外 |
| `SM-052` | セットプレイのmodify（メモの方）の表示がコンボと不一致 | `5b775a1:118` | `[未]` | 実装要求 | **実装** | 現行コード `web/src/pages/SetupEditorPage.tsx` ／ コンボ側は `ComboDetailMetadata.tsx` | 画面: `/setups/:setupId`・`/combos/:id` | — | ドラフト M23-05 |
| `SM-053` | 正式名称の案、sf6_app_naming_shortlist.txt参照 ／【 | `5b775a1:119` | `[調査]` | 調査 | **要判断** | memo line119 の 2026-08-19 注記（**開発者が前倒しを明言**＝2026-08-19「一部（正式名称決定など）は早くやるべき」） | — | — | **正式名称クラスタの代表**（SM-157 を束ねる）。論点＝**正式名称を何にするか**（開発者判断） |
| `SM-054` | パリィドライブラッシュの扱い、考え直した方がいいかも？　／【2026-07-21 | `5b775a1:120` | `[一部]` | 実装要求 | **要判断** | memo line120-122 の引用（CHANGE-038 で役割分担確定・M16-04/CHANGE-064 でも `parry_drive_rush` は `modifier.type` のまま正と再確認） | — | — | 論点＝**`parry_drive_rush` の現行表現を維持するか**。★memo 自身が「二重定義ではない」と結論している |
| `SM-055` | ジャンプのフレームを自動入力させたい（ジャンプのフレームはほぼ共通項。違うのはご | `5b775a1:124` | `[一部]` | 実装要求 | **返却** | memo line124 の 2026-08-11 注記（マイグレ 000041/000048/000059 で移動 5 code の total を **17 キャラ**へ backfill 済・**残 14 キャラは未 seed**） | — | — | 返却先＝**M14 / update wave**。**キャラ別フレーム差クラスタの代表**（SM-082 / SM-113 を束ねる） |
| `SM-056` | セットプレイ用の技の計算式を確定させて、それらは自動で取込時に作らせたい。（ラッ | `5b775a1:125` | `[一部]` | 実装要求 | **返却** | memo line125 の 2026-08-11 注記（計算式は M19-05 で確定。**自動生成は未**＝ザンギエフ連打版 3 行はマイグレ 000051 の手書き INSERT・ラッシュ空振りは手動の生成ボタン） | — | — | 返却先＝**M14 / update wave（seed 生成側）**。ドラフト §5.2 と一致 |
| `SM-057` | 詳細でのmodiferやメモの表示の仕方がちょっと不自然に感じる。{}とか | `5b775a1:129` | `[未]` | 実装要求 | **実装** | 現行コード `features/combo/components/ComboDetailRecipe.tsx` / `ComboDetailMetadata.tsx` | 画面: `/combos/:id` | — | ドラフト M23-05 |
| `SM-058` | マイコンボ遷移時にコンボ一覧での選択キャラ維持したい　／【2026-07-21】 | `5b775a1:130` | `[予定 M24]` | 実装要求 | **実装** | followup B §F12-1/E-2（**M24 割付**） | 画面: `/mycombo` | `SM-041` |  |
| `SM-059` | 一覧の始動状況が英語技名からスタートなのおかしい。　／【2026-07-21】比 | `5b775a1:131` | `[未]` | 実装要求 | **実装** | 実査: `web/src/features/combo/utils.ts:49-52`（`starterMoveCode` を先頭に push） | 画面: `/combos` | `SM-006` |  |
| `SM-060` | 保存成功時等のダイアログがしばらく消えないため、ヘッダでの画面遷移を一時的に邪魔 | `5b775a1:132` | `[未]` | 実装要求 | **実装** | memo line132（注記なし）／ 共通トースト機構は M12-02・CHANGE-045 C-04 で導入 | 共通部品: トースト ／ 共通部品: Header | — | ドラフト M23-04。★M23-06（一括操作の確認）と同じ共有部品面 |
| `SM-061` | 比較画面でキャラ名も出したい。複数キャラのコンボを出せるがその時によくわからなく | `5b775a1:133` | `[未]` | 実装要求 | **実装** | 現行コード `CompareTable.tsx` | 画面: `/compare` | — | 比較画面クラスタ（SM-003 / SM-014 / SM-018 / SM-021 と同面） |
| `SM-062` | 編集の既存から紐付けボタンもういらない。起き攻めフレームが一致するものだけ候補と | `5b775a1:134` | `[予定 M24]` | 実装要求 | **実装** | followup B §F12-3/E-1（**M24 割付**） | 画面: `/combos/:id` | `SM-019` |  |
| `SM-063` | ジェイミーやブランカ等、特殊状態が複数あるキャラについてちゃんと動くか確認が必要 | `5b775a1:136` | `[一部]` | 実装要求 | **返却** | memo line136 の 2026-08-11 注記（`custom_states` 定義 seed はマイグレ 000015＋000027 のみ。第三波は「custom_states は本サブのスコープ外」と明記。followup `M14-03b-custom-states-stock` 未着手） | — | — | 返却先＝**M14 / update wave**。SM-102 の代表 |
| `SM-064` | フィルター欄でのタグは増えたり伸びると相変わらず表示が変。これも表示列と同じよう | `5b775a1:137` | `[予定 M24]` | 実装要求 | **実装** | followup B §F12-2（G-3・**M24 割付**） | 画面: `/combos` ／ 共通部品: タグ UI | `SM-010` |  |
| `SM-065` | 相手スタンスの表示はさらに別のものがないか検討したい。　／【2026-07-21 | `5b775a1:140` | `[未]` | 実装要求 | **要判断** | ドラフト §3 M23-02 gate「L139 の stance 表示仕様…を RESEARCH で確定する」 | 画面: `/combos` | — | 論点＝**相手スタンスの表示形式をどうするか**。SM-005 と対 |
| `SM-066` | 【M12-02 C-09 で不採用にした代替案】登録/編集の「戻る/キャンセル」 | `5b775a1:141` | `[未]` | 実装要求 | **要判断** | ドラフト §3 M23-04 gate「L140/L141 の sticky UI …は RESEARCH または小 PoC で採否を確定する」／ memo line141 の 2026-07-21 注記「将来案の記録（保留）」 | 共通部品: アクションバー／エラー表示 | — | ★本行は**要求ではなく M12-02 C-09 で不採用にした代替案の記録**である。論点＝**sticky 化を採るか** |
| `SM-067` | エラーメッセージの位置について、ひとまず保存ボタンの上にも表示する形にしたが、先 | `5b775a1:142` | `[未]` | 実装要求 | **要判断** | memo line142 の 2026-07-21 注記（M12-02 で保存直上＋トーストへ統一済・自動スクロール方式は保留） | 共通部品: エラー表示 | — | 論点＝**先頭へ自動スクロールする方式へ変えるか** |
| `SM-068` | 重複時のエラーがちょっと不自然。既存コンボ（#40）と重複: ケン・立ち強K / | `5b775a1:143` | `[未]` | 実装要求 | **実装** | memo line143（未対応）／ 実装面は `useCheckDuplicate`（followup §E の M24 リファクタ対象と同一） | 画面: `/combos/new`・`/combos/:id/edit` ／ 共通部品: 重複検出表示 | — | **重複表示クラスタの代表**（SM-074 を束ねる）。★followup §E `useCheckDuplicate` の TanStack 化と**同じ実装面**＝束ねる余地 |
| `SM-069` | 投げ重ねのドライブラッシュ表記がまだ気に入らないかも。分かりにくい。　／【202 | `5b775a1:144` | `[未]` | 実装要求 | **実装** | memo line144（未対応・表記） | 画面: レシピ表記 ／ locale | — | ドラフト M23-07 相当 |
| `SM-070` | notationの記載見直し必要かも。 ／【2026-06-28】§2.1「移動 | `5b775a1:147` | `[一部]` | 実装要求 | **返却** | memo line147 の 2026-07-21 注記（M16-04/CHANGE-064 で canonical は system move の `dash_forward`/`dash_back` に確定。frame 表・取込側の掃き取りは M14-03b seed remap で追従。`SUPP-001` §3.3.3 の是正は followup **G-9**） | — | — | 返却先＝**M14 / update wave ＋ followup G-9（次の SUPP-001 自由改訂時）** |
| `SM-071` | インポート機能、仮登録は除外したい　／【2026-07-21】未対応。M17-0 | `5b775a1:154` | `[未]` | 実装要求 | **実装** | memo line154 の 2026-08-11 注記（**M17-05 の対象外＝未対応**。取込プレビューに除外 UI・フィルタは無く既定チェックも仮登録行を含めて ON・BE も素通し。除外フィルタが有るのはエクスポート側の `isDraft` のみ） | 画面: `/import/combo` ／ API 契約: 取込 | — | ドラフト M23-06 |
| `SM-072` | エクスポート機能、現状のキャラID入力、タグID入力、コンボID指定は使いにくす | `5b775a1:155` | `[一部]` | 実装要求 | **実装** | memo line155 の 2026-08-11 注記（**第 2 段は未実行**＝`ComboExportPage` は router `/export/combo`・ヘッダのナビ項目とも健在）／ 実査: `web/src/pages/ComboExportPage.tsx` 実在・`code-facts` §3/§6 と一致 | 画面: `/export/combo` ／ 共通部品: Header ナビ | — | **エクスポート画面クラスタの代表**（SM-073 を束ねる）。★route 削除を伴うなら破壊的＝別 CHANGE（ドラフト M23-06 gate） |
| `SM-073` | エクスポート画面、マイコンボタグ(マイコンボ)で絞り込んで出力だけで指定はちょっ | `5b775a1:157` | `[一部]` | 実装要求 | **実装** | memo line157 の 2026-08-11 注記（一覧/マイコンボからの選択エクスポートで代替はできたが**エクスポート画面本体は未改修**） | 画面: `/export/combo` | `SM-072` |  |
| `SM-074` | コンボ登録時に被った時の表示を見直したい。遷移しなくてもどこが差分だったかわかる | `5b775a1:159` | `[未]` | 実装要求 | **実装** | memo line159（未対応・登録時重複の差分表示） | 画面: `/combos/new` | `SM-068` |  |
| `SM-075` | エクスポート時のファイル名に気を使う。自動でエクスポート対象＋日付とか付ける？い | `5b775a1:160` | `[未]` | 実装要求 | **実装** | memo line160 の 2026-08-11 注記（**未対応**。全経路が固定名＝CSV zip `combomgr-export.zip` / 画像・PDF `combomgr-combo(s).png`・`.pdf`。M17-05 棚卸し A-1〜A-4 に未計上） | 画面: `/export/combo` ／ 一覧のエクスポートダイアログ | — | ドラフト M23-06 |
| `SM-076` | インポート、0件ファイル時に何もでない　／【2026-07-21】M17-05  | `5b775a1:162` | `[一部]` | 実装要求 | **実装** | memo line162 の 2026-08-11 注記（**プレビュー段は依然無反応**＝`ComboImportPage` が `preview.combos.length > 0` のときだけ描画し 0 行の else 分岐が無い。BE も空ファイルは拒否対象外） | 画面: `/import/combo` | — | ドラフト M23-06 |
| `SM-077` | エクスポートしたファイルやクリップボードの形式については少し見直しが必要そう。　 | `5b775a1:163` | `[一部]` | 実装要求 | **実装** | memo line163 の 2026-08-11 注記（未実施＝ファイル名・エクスポート画面の除去。**CSV 契約 `DES-002` §7.6 は不変**） | 画面: `/export/combo` | `SM-072` | SM-075 と相互参照。★CSV 契約の変更はスコープ外 |
| `SM-078` | 区分系まだちゃんと決めれてない　／【2026-07-21】未対応(opponen | `5b775a1:165` | `[未]` | 実装要求 | **要判断** | memo line165 の 2026-07-21 注記（`opponent_size` 等の区分・未対応） | — | — | **区分クラスタの代表**（SM-094 を束ねる）。論点＝**`opponent_size` 等の区分値を何にするか**（ドメイン知識が要る） |
| `SM-079` | 起動中タスクトレイ常駐にして黒窓消す、代わりにUI停止ボタンを設定とかに用意する | `5b775a1:167` | `[未]` | 実装要求 | **要判断** | ドラフト §3 M23-09 gate「task tray は単なる polish ではなく OS integration なので、設計・実装コストを別 decision packet で承認する」 | 配布・起動（`cmd/combomgr`） | — | 論点＝**タスクトレイ常駐を M24 で採るか** |
| `SM-080` | 仮想コントローラのボタン名見直し　／【2026-07-21】followup G | `5b775a1:168` | `[未]` | 実装要求 | **要判断** | followup G §G-2（**割付＝「実機確認できるタイミング」・未着手**）／ memo line168 の 2026-08-19 注記（M21 は実機コントローラとキーボードのみで仮想コントローラのラベルは触っていない） | 共通部品: `HitBoxLayout.tsx` ／ E2E（aria name 由来のセレクタ） | — | 論点＝**M24 で拾うか、実機確認の手番を待つか**。★(a) 確定時は E2E 追従が要る（followup G-2 が明記） |
| `SM-081` | 仮想コントローラを子ウィンドウ化？　／【2026-07-21】未対応 | `5b775a1:169` | `[未]` | 実装要求 | **返却** | ドラフト §5.3（controller 子ウィンドウ＝新しい product surface） | — | — | 返却先＝**フェーズ4／新機能** |
| `SM-082` | 前ステ、後ろステ、飛びのフレームがキャラにより違う件への対応　／【2026-07 | `5b775a1:170` | `[一部]` | 実装要求 | **返却** | memo line170 の 2026-08-11 注記（移動 5 code の total は 17 キャラへ投入済・残 14 キャラは未 seed） | — | `SM-055` |  |
| `SM-083` | マイコンボで誰のコンボか見えるようにしたい　／【2026-07-21】未対応(マ | `5b775a1:171` | `[未]` | 実装要求 | **実装** | 現行コード `MyComboPage.tsx` | 画面: `/mycombo` | — | ドラフト M23-01 |
| `SM-084` | 新規登録の遷移先は一覧にしたい。　／【2026-07-21】未対応 | `5b775a1:172` | `[未]` | 実装要求 | **実装** | 現行コード `web/src/pages/ComboEditorPage.tsx`（保存後遷移） | 画面: `/combos/new` | — | ドラフト M23-01 |
| `SM-085` | キーボードショートカットの用意　／【2026-07-21】未対応(将来) | `5b775a1:173` | `[未]` | 実装要求 | **要判断** | ドラフト §5.4（feedback gate） | — | — | 論点＝**ショートカットの要望が実在するか**。★M21 のキーボード入力（技入力）とは別物 |
| `SM-086` | 画面遷移を減らすための施策（future-noteに入れたもの参照、combmg | `5b775a1:174` | `[未]` | 実装要求 | **要判断** | ドラフト §5.4（feedback gate）／ memo line174（future-note `combmgr-inline-edit-design-memo.md`） | — | — | 論点＝**inline edit の操作モデルを採るか** |
| `SM-087` | なっちゃってレバー機能を仮想コントローラにつける　／【2026-07-21】未対 | `5b775a1:175` | `[未]` | 実装要求 | **返却** | memo line175 の 2026-08-19 注記（M21 は全 7 サブ完了したが本項は対象外のまま）／ ドラフト §5.3 | — | — | 返却先＝**フェーズ4／新機能** |
| `SM-088` | セットプレイ名は自動で〇〇重ねと最後の技から生成されるようにしたい。ユーザーは入 | `5b775a1:176` | `[未]` | 実装要求 | **実装** | memo line176（M12 で必須化済だが自動生成は別 TODO） | 画面: `/combos/:comboId/setups/new`・`/setups/:setupId` | — | ドラフト M23-05 |
| `SM-089` | コンボのメモの1行目をコンボレシピの上に薄く出したい　／【2026-07-21】 | `5b775a1:177` | `[未]` | 実装要求 | **実装** | memo line177 の 2026-07-21 注記（M15-05 の技名 1 行サマリはレシピ由来でメモ由来ではない） | 画面: `/combos/:id`・`/combos` | — | ドラフト M23-03 |
| `SM-090` | 登録画面、左右の余白無駄じゃないか問題。スマホ対応や画面サイズ縮小考えてこうなっ | `5b775a1:179` | `[一部]` | 実装要求 | **調査** | memo line179 の 2026-07-21 注記（M15-05 で PC 2 カラム・セクション折りたたみへ再構成。**余白最適化の要否は再確認**） | 画面: `/combos/new`・`/combos/:id/edit` | — | 何が分かれば実装できるか＝**2 カラム再構成後に余白最適化が要るか**（目視） |
| `SM-091` | modifier、仮想コントローラで扱い悩む奴　／【2026-07-21】この直 | `5b775a1:181` | `[一部]` | 実装要求 | **返却** | memo line181 の 2026-07-21 注記（キャラ固有の別技ボタン面はデータ駆動で **M14-03b 後**（全キャラ seed 後）に構築＝followup「M15-03 派生フォローアップ・別技ボタン面」） | — | — | 返却先＝**M14 / update wave** |
| `SM-092` | ポジションはデフォルトで不問であるべき　／【2026-07-21】未対応(要確認 | `5b775a1:220` | `[未]` | 実装要求 | **要判断** | memo line220 の 2026-08-11 注記（**`position` の列挙値に「不問」が存在しない**＝画面中央/自分画面端/自分画面端寄り/相手画面端/相手画面端寄り の 5 つ。「不問」= any を持つのは相手スタンスのみ） | — | — | ★**実装には値そのものの新設が要る**＝小改善ではない（playbook §4.12）。論点＝**`position` に「不問」を新設するか** |
| `SM-093` | 状態はデフォルトで不問であるべき　／【2026-07-21】未対応(要確認・既定 | `5b775a1:221` | `[未]` | 実装要求 | **実装** | memo line221 の 2026-08-11 注記（相手スタンスには `any` が既にある。**新規登録の初期値が空文字**で `any` ではない） | 画面: `/combos/new`（ComboEditorBasicFields の初期値） | — | ★SM-092 と違い**値の新設は不要**＝初期値の変更で足りる |
| `SM-094` | 相手の大きさの区分は見直しが必要　／【2026-07-21】未対応(oppone | `5b775a1:222` | `[未]` | 実装要求 | **要判断** | memo line222 の 2026-07-21 注記（`opponent_size` 区分） | — | `SM-078` |  |
| `SM-095` | ヒット種別はデフォルトでノーマルであるべき。未指定は禁止。（DB的にはNullや | `5b775a1:223` | `[未]` | 実装要求 | **要判断** | memo line223 の 2026-08-11 注記（初期値は空文字のまま。**M18 で `hit_type` が 4 値化**し確定反撃側が「区分を判定できない反撃」を専用セクションで拾うため、既定値変更は M18 の入力キュー挙動と併せて見る必要がある） | — | — | 論点＝**既定値を `normal` にすると M18 の入力キューがどう変わるか** |
| `SM-096` | ある一定以上の長さのコンボ、セットプレイを入力すると画面が崩れる。恐らくレシピを | `5b775a1:225` | `[未]` | 実装要求 | **実装** | memo line225 の 2026-07-21 注記（レシピ縦表示で解決余地） | 画面: `/combos`・`/combos/:id` | `SM-022` |  |
| `SM-097` | 起き攻めについて、ノーゲージ版は、ノーゲージとラベルにつけたい。　／【2026- | `5b775a1:227` | `[未]` | 実装要求 | **実装** | 実査: `web/src/constants/oki.ts:33` に `OKI_NO_GAUGE_LABEL = "ノーゲージ"` があるが**参照 0 件**（定義 1 箇所のみ） | 画面: `/combos/:id`・`/combos` ／ 共通部品: `okiOptionLabel` | — | memo 2026-08-11 の実査と一致 |
| `SM-098` | 確定反撃でDリバの扱いどうしよう　／【2026-07-21】M18(確定反撃記録 | `5b775a1:229` | `[未]` | 実装要求 | **要判断** | memo line229 の 2026-08-11 是正（M18 はクローズしたが**本件は扱われていない**＝M18 の指示書・完了報告のいずれにも「リバーサル」の記載がゼロ。`CHANGE-023/026` で「ドライブリバーサルは moves 取込対象外」のまま） | — | — | ★**割付先が消えている**。論点＝**ドライブリバーサルを moves 取込対象に加えるか** |
| `SM-099` | ドライブゲージの消費量について、全消費という選択肢を付けたい。　／【2026-0 | `5b775a1:231` | `[一部]` | 実装要求 | **要判断** | memo line231 の 2026-08-11 注記（専用選択肢は未実装＝「全消費」という文字列はコード上に存在しない。数値入力のみ） | — | — | 論点＝**「全消費」をどの値・どの表現で表すか**（要追加仕様） |
| `SM-100` | 全技一覧について、仮想コントローラで入力できる技は省いてもいいかも　／【2026 | `5b775a1:233` | `[未]` | 実装要求 | **返却** | ドラフト §5.3（技一覧省略＝個別新機能） | — | — | 返却先＝**フェーズ4／新機能** |
| `SM-101` | ドライブゲージ消費(0〜20本・0.5刻み)、これ不自然な気がしてきたので直して | `5b775a1:235` | `[未]` | 実装要求 | **実装** | 実査: `ComboEditorBasicFields.tsx:353`「ドライブゲージ消費(0〜20本・0.5刻み)」（対応テストは `:227-228`） | 画面: `/combos/new`・`/combos/:id/edit` | — | ★始動側は max=6 で「(0〜6本・0.5刻み)」なのに消費側の表示だけ 20＝**上限の見え方が不揃い**（memo が記録）。SM-007 と同一ファイル |
| `SM-102` | JPとベガとブランカなど、設置状態から始まるコンボを持つキャラにカスタム状態持た | `5b775a1:237` | `[一部]` | 実装要求 | **返却** | memo line237 の 2026-08-11 注記（JP・ベガ（m_bison）はキャラ自体は seed 済だが `custom_states` は未投入。ブランカはキャラ自体が未 seed） | — | `SM-063` |  |
| `SM-103` | マノンの扱い。技は変わらないから他のキャラほどstateが重要ではない、。　／【 | `5b775a1:239` | `[一部]` | 実装要求 | **却下** | memo line239 の 2026-07-21 注記（マノン seed は M14-03d で投入済み。「state 重要度のドメイン判断は記録として保持」） | — | — | 却下理由＝**行動要求ではない**（ドメイン記録の保持）。ドラフト §5.2 の「非 action の close 候補」と一致 |
| `SM-104` | 一覧のフィルターまでを閉じれるようにしたい。（新規登録みたいに、閉じたり展開した | `5b775a1:241` | `[未]` | 実装要求 | **実装** | 実査: `ComboListFilters.tsx` に折りたたみ機構 0 件 | 画面: `/combos` | — | **フィルタ折りたたみクラスタの代表**（SM-117 を束ねる） |
| `SM-105` | タグについてタイプ列みたいなものを作って、コンボ以外の用途にも拡張したい。個別の | `5b775a1:243` | `[未]` | 実装要求 | **返却** | ドラフト §5.3（tag type＝schema/domain 拡張）／ ドラフト §3 M23-02 gate「tag データモデル拡張は行わず、既存 tag の UI 改善に限定する」 | — | — | 返却先＝**フェーズ4** |
| `SM-106` | ヒット区分、ジャスパ反撃、インパクト壁やられ、インパクトヒット壁やられも追加した | `5b775a1:245` | `[一部]` | 実装要求 | **返却** | memo line245 の 2026-08-11 注記（ジャスパ反撃は M18-01 で実現。インパクト壁やられ / インパクトヒット壁やられ / ガークラ は未） | — | `SM-033` |  |
| `SM-107` | プロや上級者はより自動化したい調べものを抱えている可能性があるのでヒアリング | `5b775a1:247` | `[調査]` | 調査 | **返却** | memo line247（別軸・ヒアリング） | — | — | 返却先＝**利用者フィードバック運用**。SM-038 と同群 |
| `SM-108` | 強度なしの技が仮想コントローラから現在入力できないので対処が必要　／【2026- | `5b775a1:249` | `[未]` | 実装要求 | **返却** | followup G §G-13「強度なし `p`/`k` 単独形（例 `6P`）の非対応＝**実データ 0 件のため展開規則を作っていない**」→ **第二波 precheck の観点** ／ memo line249 の 2026-08-19 注記（M20 完了後も未対応） | — | — | 返却先＝**M14 / update wave（第二波 seed precheck）**。**強度なしクラスタの代表**（SM-140 を束ねる） |
| `SM-109` | 仮想コントローラについてテンキーやLP等の表示からパンチ、矢印アイコンを用意して | `5b775a1:251` | `[未]` | 実装要求 | **要判断** | followup G §G-3（**将来（未割付）・据え置き**）。★「方向の矢印記号は M15-03『指摘6＝記号除去』で意図的に外したもので、アイコンで戻すのは**過去判断の反転**」 | 共通部品: `HitBoxLayout.tsx`（`DIRECTIONS`/`ATTACKS` config）／ icon assets | — | 論点＝**過去判断（記号除去）を反転するか**。★SM-150 とは**別件**だが**同じ icon asset を要求しうる**（memo・followup 双方が明記） |
| `SM-110` | エクスポート、インポート等、エンジニア向けかもしれない表現を見直し。PCで仕事を | `5b775a1:253` | `[未]` | 実装要求 | **実装** | memo line253 の 2026-07-21 注記（用語・表現見直し・未対応） | locale ／ 共通部品: Header ナビラベル | — | ドラフト M23-06/07 |
| `SM-111` | セットプレイ用として初期登録したいデータや、セットプレイで扱うには特殊な技が見つ | `5b775a1:255` | `[調査]` | 調査 | **却下** | memo line255 の 2026-07-21 注記（データ収集〔別ファイル〕＝**機能 TODO ではない**） | — | — | 却下理由＝**行動要求ではない**（`docs/seed-data/setplay-move-notes.md` への記載運用） |
| `SM-112` | 新規作成、システムと、共通システム（移動・その他）が意味が被りすぎててわかりにく | `5b775a1:257` | `[未]` | 実装要求 | **実装** | memo line257 の 2026-08-11 注記（**現存する**＝`RecipeBuilder`／`SetupRecipeEditor` に「共通システム（移動・その他）」optgroup と `category=system` の optgroup が併存） | 画面: `/combos/new`・`/setups/:setupId` ／ 共通部品: RecipeBuilder | — | M16-04 で振り分け原則は決まったが**画面上の呼び名の被りは未手当** |
| `SM-113` | ステップがキャラクターによってフレームが違うことを今表現できていない気がする。な | `5b775a1:259` | `[一部]` | 実装要求 | **返却** | memo line259 の 2026-08-11 注記（M19-04 で費用モデル列を追加・移動 5 code の total は第三波まで投入済・**残 14 キャラは未 seed**。派生元ごとに active/recovery が変わる技は現行スキーマで表現できない＝followup `parent-dependent-frame-variance` 未着手） | — | `SM-055` |  |
| `SM-114` | ザンギの技、【ホールド】じゃなくて技名(ホールド)になっているので訂正したい。舞 | `5b775a1:261` | `[未]` | 実装要求 | **返却** | memo line261 の 2026-08-11 注記（`character_data/zangief.csv` 4 件・`ingrid.csv` 1 件が旧形式、`luke.csv` は両形式混在。**seed 投入済みキャラは CSV 修正だけでなく後続の UPDATE マイグレが要る**） | — | — | 返却先＝**M14 / update wave**。**ホールド表記クラスタの代表**（SM-137 を束ねる） |
| `SM-115` | 派生技のフレームを全体見直ししないとセットプレイ自動発見の精度が落ちる。見　現在 | `5b775a1:263` | `[一部]` | 実装要求 | **返却** | memo line263 の 2026-08-11 注記（枠組みは M19-04 系＋Phase 2 で導入済。**`chain_cancel_total` の追加投入は「M19 では閉じない」裁定＝D-265**・followup `tp02-chain-cancel-total-remaining` 未着手） | — | — | 返却先＝**M14 / update wave（実測パス）** |
| `SM-116` | ヒット区分にインパクト壁やられ、インパクトヒット壁やられ、パニッシュカウンター（ | `5b775a1:265` | `[一部]` | 実装要求 | **返却** | memo line265 の 2026-08-11 注記（パニッシュカウンター〔ジャストパリィから〕は M18-01 で実現。インパクト壁やられ / インパクトヒット壁やられ / **持続当ての別コンボ扱い** は未） | — | `SM-033` |  |
| `SM-117` | 検索のフィルタを非表示可能にする（隠す）　／【2026-08-11】未対応。Co | `5b775a1:303` | `[未]` | 実装要求 | **実装** | memo line303 の 2026-08-11 注記（`ComboListFilters` に折りたたみ機構は無い） | 画面: `/combos` | `SM-104` |  |
| `SM-118` | 新規登録の隠すボタンについて、現在は縦方向に縮めるだけだが、左辺の情報（メタデー | `5b775a1:305` | `[未]` | 実装要求 | **実装** | memo line305 の 2026-08-11 注記（折りたたみは**縦方向に縮むだけ**で他方のカラムを広げる処理は無い） | 画面: `/combos/new`・`/combos/:id/edit` ／ 共通部品: `CollapsibleFieldset.tsx` | — | ドラフト M23-04 |
| `SM-119` | 新規登録したいキャラの選択をフィルタのキャラプルダウンが兼ねているのが不自然　／ | `5b775a1:306` | `[未]` | 実装要求 | **実装** | memo line306 の 2026-08-11 注記（M10-02 で一覧の選択キャラを `?character=` で新規登録へ伝播。文脈が無ければ `INITIAL_CHARACTER_ID`） | 画面: `/combos/new` | `SM-041` |  |
| `SM-120` | 検索のキャラプルダウン、キャラの増加によってちょっと選ぶのがめんどくさくなってき | `5b775a1:307` | `[未]` | 実装要求 | **実装** | memo line307 の 2026-08-11 注記（`CharacterSelector` は平坦なリストの Select・検索欄もグループ化も無い。31 キャラ中 17 が seed 済で今後さらに増える） | 共通部品: CharacterSelector | `SM-043` |  |
| `SM-121` | 色々な操作に確認ダイアログが不足していないかチェックが必要。削除、更新、非表示（ | `5b775a1:308` | `[未]` | 実装要求 | **実装** | memo line308 の 2026-08-19 注記（`M22-04` で 409/404 の競合モーダルと 2 段階確認が増えたが、**インポート／エクスポート／取込には依然として確認ダイアログが無い**） | 画面: `/import/combo`・`/export/combo`・`/import/combo/helper` ／ 共通部品: 確認ダイアログ | — | ★**M23-06（ゴミ箱の一括操作・完全削除の確認）と同じ共有部品**＝§5 交差面 (c) |
| `SM-122` | トースター上だけ出すのが残っていないか、わざわざトップまでスクロールしてみたいな | `5b775a1:309` | `[未]` | 実装要求 | **調査** | memo line309 の 2026-08-11 要確認（M17〜M19 で増えた画面〔取込ヘルパー・エクスポート・確定反撃サーチ/マイリスト・セットプレイ提案〕が同じ流儀になっているかは**未点検**） | 画面横断 ／ 共通部品: エラー表示・トースト | — | 何が分かれば実装できるか＝**新しい画面が「保存直上＋トースト」の流儀になっているか**。★実機確認が要る |
| `SM-123` | セットプレイ。いまは持続当てと汚連携対応。ただ普通に4F暴れに勝てる重ねも対象に | `5b775a1:310` | `[未]` | 実装要求 | **返却** | memo line310 の 2026-08-11 注記（M19-02 に meaty/gap の 2 モード＋受理帯はあるが、**専用区分として名前が付いていない**） | — | — | 返却先＝**フェーズ4／セットプレイ拡張**。SM-124 と同クラスタ |
| `SM-124` | セットプレイが何に勝つのかも管理したい。無敵暴れにも勝つとかもあるし。　／【20 | `5b775a1:311` | `[未]` | 実装要求 | **返却** | memo line311 の 2026-08-11 注記（記録できる「成立条件」は受け身種別 × 画面端の 2×2 で、「何に勝つか」の軸は無い） | — | — | 返却先＝**フェーズ4／データモデル拡張** |
| `SM-125` | セットプレイに汚インパクト、各種詐欺飛び等、頻出の連携を探す事に特化したボタンも | `5b775a1:312` | `[未]` | 実装要求 | **返却** | followup `M19-safejump-constant`（セーフジャンプ定数のデータ形が「並行 MS 待ち」で保留＝**詐欺飛びの前提データが未確定**） | — | — | 返却先＝**フェーズ4** |
| `SM-126` | 技編集は技閲覧に変えて閲覧限定にしたい。setplay_onlyのみコピーして作 | `5b775a1:313` | `[未]` | 実装要求 | **要判断** | memo line313 の 2026-08-11 注記（**followup `B-2-heavy` と方向が逆**＝あちらは編集を増やす、こちらは閲覧限定にする。どちらを採るかは開発者裁定が要る） | — | — | 論点＝**技編集を閲覧限定にするか、手動 CRUD を増やすか**。SM-049 と相互参照 |
| `SM-127` | タグは想定よりも重要そうでプレイヤーによっては大量につけうるので、検索機能が欲し | `5b775a1:314` | `[一部]` | 実装要求 | **実装** | memo line314 の 2026-08-11 注記（付与側 `TagSelector` には検索欄がある。**タグ管理画面には検索が無く**、一覧のタグフィルタも並べるだけ） | 画面: `/tags/manage`・`/combos` ／ 共通部品: タグ UI | — | SM-010 と同クラスタだが「タグ管理画面の検索」は別要求 |
| `SM-128` | コンボの分岐登録をもう少し簡単にすべき？複製で充分なのか検討が必要。（複数同時登 | `5b775a1:315` | `[未]` | 実装要求 | **要判断** | memo line315 の 2026-08-11 注記（既存の手段はコピー登録〔FR012・`?copyFrom=`〕のみ。本文どおり「**ユーザー反応みて決定**」＝判断保留） | — | — | 論点＝**分岐登録の専用画面を作るか、複製で足りるか**（feedback gate） |
| `SM-129` | 一覧の並び替えとともに絞り込みも欲しい　／【2026-08-11 要確認】一覧に | `5b775a1:316` | `[不明]` | 実装要求 | **要判断** | memo line316 の 2026-08-11 要確認（一覧には既にフィルタとソートの両方があり、**この行が指す不足を特定できなかった**） | — | — | ★**指すものを特定できない**。論点＝**(a) ソート項目ごとの絞り込み (b) 並び替え UI からの絞り込み (c) マイコンボ側の不一致〔SM-004〕のどれか** |
| `SM-130` | 一覧画面で一発で全ルート見えるようにしたい。始動技というよりレシピが見たい。　／ | `5b775a1:317` | `[一部]` | 実装要求 | **実装** | memo line317 の 2026-08-11 注記（レシピ列自体はある〔表示列カスタマイズで ON/OFF〕が**行内で truncate される**。マウスオーバーの title には全文が入る） | 画面: `/combos`（ComboTableRow） | `SM-022` |  |
| `SM-131` | 特定の状況限定のコンボを引ける仕組み。ただこれはタグでもいいかも？タグで充分かは | `5b775a1:318` | `[未]` | 実装要求 | **要判断** | memo line318 の 2026-08-11 注記（M19-06 の絞り込みはセットプレイ成立条件の軸であって「状況限定コンボ」一般ではない。本文どおり「**タグで充分かはユーザー反応を見る**」） | — | — | 論点＝**タグで足りるか**（feedback gate） |
| `SM-132` | ラッシュ必殺技もmoveに足したい。通常・特殊技と違って発生が遅くなり、トータル | `5b775a1:319` | `[未]` | 実装要求 | **返却** | memo line319 の 2026-08-11 注記（ラッシュ版生成 `POST /api/moves/:id/rush-variant` は**通常技・特殊技かつ非空中技のみ**に制限され必殺技は対象外） | — | — | 返却先＝**M14 / update wave（moves データ）**。★ドラフト §5.1 が「**1 行を二つに分解**する」と指示した行＝(1) ラッシュ必殺技の表示命名 (2) move 追加と setplay filler |
| `SM-133` | 確定反撃について現状の表記だとどっちがどっちの技か見失う　／【2026-08-1 | `5b775a1:320` | `[未]` | 実装要求 | **実装** | memo line320 の 2026-08-11 注記（構造的には区別できる形だが「相手の技」「自分の技」という**明示ラベルは無く**技名がそのまま並ぶ。docs 側にも本件に対応した記録は無い） | 画面: `/punish/search`・`/punish/list` ／ locale | — |  |
| `SM-134` | 確定反撃ない技についても対策をメモしたい需要はある。これをどうするか？ガードした | `5b775a1:321` | `[未]` | 実装要求 | **返却** | memo line321 の 2026-08-11 注記（M18 は「フレーム上 確定する反撃」を探して記録する作りで、**自由記述の対策メモを置く場所は無い**） | — | — | 返却先＝**フェーズ4／新機能** |
| `SM-135` | ターゲットコンボも入力に欲しい　／【2026-08-11】未対応。M16-05  | `5b775a1:322` | `[未]` | 実装要求 | **返却** | memo line322 の 2026-08-11 注記（M16-05 で `target_combo` 区分は doc 限定で運用正典化〔実データ 0 件〕。**入力面は未整備**） | — | — | 返却先＝**フェーズ4／入力面の新設**。ドラフト §5.3 |
| `SM-136` | キャラ増えてプルダウンでは大変　／【2026-08-11】未対応。上部「検索のキ | `5b775a1:323` | `[未]` | 実装要求 | **実装** | memo line323 の 2026-08-11 注記（`CharacterSelector` が平坦な Select・検索欄なし） | 共通部品: CharacterSelector | `SM-043` |  |
| `SM-137` | ホールド、強化技系の画面表記を見直す。（【ホールド】弱xxx）みたいになってる系 | `5b775a1:324` | `[未]` | 実装要求 | **返却** | memo line324 の 2026-08-11 注記（seed 側の表記が揺れている＝rashid / marisa は「【ホールド】〜」、zangief 4 件と ingrid 1 件は「〜(ホールド)」、luke は 1 件が両形式混在） | — | `SM-114` |  |
| `SM-138` | セットプレイについて特定の有利フレームを取るためのレシピ。を探せるようにしたい。 | `5b775a1:325` | `[未]` | 実装要求 | **返却** | memo line325 の 2026-08-11 注記（M19 の提案エンジンは有利フレームを**入力**とする向きで、目標フレームを与えて空振りレシピを逆引きする経路は無い。ただし合成 filler 単位〔M19-05〕は素材としてそのまま使える） | — | — | 返却先＝**フェーズ4／セットプレイ拡張** |
| `SM-139` | セットプレイのフィルターがセットプレイ用である事がわかりにくい　／【2026-0 | `5b775a1:326` | `[未]` | 実装要求 | **実装** | memo line326 の 2026-08-11 注記（M19-06 の絞り込みがコンボ一覧側に増えたため**どのフィルタがセットプレイ由来か読み取りにくい**） | 画面: `/combos` ／ locale | — | ★followup `setup-result-unverified-label`（「未検証」の語感ずれ・i18n ja/en 両方に触るため M19 では変えない裁定）と同クラスタ |
| `SM-140` | 強度がない技が選べない（バグ）　／【2026-08-19】上部 line 248 | `5b775a1:329` | `[未]` | 実装要求 | **返却** | memo line329 の 2026-08-19 注記（上部 line 248 と同件。followup **G-13** が正本で第二波 precheck の観点のまま未着手） | — | `SM-108` |  |
| `SM-141` | 強化版のナンバリング、SRK表記を見直したい。【強化】236P、または、buff | `5b775a1:330` | `[未]` | 実装要求 | **要判断** | memo line330 の 2026-08-19 注記（`M20-06` で命名規則は正典化されたが「**強化版**」を表す表記の規約は `DES-004` に無い＝実査で 0 件。`M20-02` の別名生成規則〔`numeric`/`srk`〕は強化状態を表す接頭辞を持たない） | — | — | 論点＝**「強化版」をどう表記するか**（値の決めは開発者のドメイン知識が要る） |
| `SM-142` | 前投げは5/6LP+LKより5LP+LKの方が自然かも。これで固定していると、6 | `5b775a1:331` | `[未]` | 実装要求 | **要判断** | memo line331 の 2026-08-19 注記（`M20-02` の別名生成規則の対象だが、**値の決めは開発者のドメイン知識が要る側**＝`P-34` と同じ形） | — | — | 論点＝**前投げの別名を `5LP+LK` にするか** |
| `SM-143` | 被った時は強化版じゃない方を解決して欲しい（コマンド入力）　／【2026-08- | `5b775a1:332` | `[未]` | 実装要求 | **要判断** | memo line332 の 2026-08-19 注記（`DES-004` §5.6.1 は「**曖昧な入力は候補として人へ返す**」を採っており自動で一方へ寄せない。⇒ **現行の設計判断を反転させる要望**であり、採るなら再承認が要る） | — | — | 論点＝**曖昧解決を自動で一方へ寄せるか**。SM-144 の代表 |
| `SM-144` | SA3とCAが被った時だけSA3優先解決にしてほしい　／【2026-08-19】 | `5b775a1:333` | `[未]` | 実装要求 | **要判断** | memo line333 の 2026-08-19 注記（`DES-004` §3.4.2 の実測＝SA と CA が同じ `command` を持つ組が **23 組**・うち **17 組が SA3 vs CA**〔全 17 キャラに 1 組ずつ〕。§5.6.1 は `236236K` を「未解決＋候補 2 件」と定める） | — | `SM-143` |  |
| `SM-145` | プリセット選択の自由度を上げて欲しい。いま参照でプルダウン変えると、コンボのレシ | `5b775a1:334` | `[一部]` | 実装要求 | **調査** | memo line334 の 2026-08-19 **要確認**（`M20-05` で `recipe_cache` の再計算は eager に配線され、経路は `combos` 側 3 本＋`setups` 側 3 本の計 6 本〔`DES-003` §3.4/§3.11・**D-413**〕。ただし**実挙動を裏取りできなかったため断定していない**） | 画面: `/combos/:id`（参照プリセットの切替） | — | 何が分かれば実装できるか＝**参照画面でプルダウンを変えたときセットプレイ側の表示も追随するか**（実挙動の確認） |
| `SM-146` | セットプレイからそれが重なった時に始まるコンボ（セットプレイの最後の技始動のコン | `5b775a1:335` | `[未]` | 実装要求 | **実装** | memo line335 の 2026-08-19 注記（`M19-06`/`M19-07` は入ったが「**セットプレイの最後の技を始動とするコンボ**」へ辿る導線は無い） | 画面: `/setups/:setupId` → `/combos` | — | ★ドラフトの母集団に無い新規行 |
| `SM-147` | クラウドストレージへのエクスポート対応（これはかなり先でもいい）　／【2026- | `5b775a1:336` | `[未]` | 実装要求 | **返却** | memo line336（本文どおり「これはかなり先でもいい」） | — | — | 返却先＝**フェーズ4** |
| `SM-148` | 新規登録画面について、基本情報が当初より増えてしまったので、基本情報とレシピのタ | `5b775a1:337` | `[未]` | 実装要求 | **実装** | memo line337 の 2026-08-19 注記（`M15-05` で一度手当てしたが**その後さらに項目が増えた**。★memo 自身が「M24＝UX 整理の候補」と明記） | 画面: `/combos/new`・`/combos/:id/edit` ／ 共通部品: ComboEditor | — | SM-118 / SM-090 と同じ登録画面レイアウト面 |
| `SM-149` | 仮想コントローラについて、移動ボタンと技ボタンが縦並びで直感的ではない。実物のコ | `5b775a1:338` | `[未]` | 実装要求 | **要判断** | memo line338 の 2026-08-19 注記（M21 は物理入力であり仮想コントローラのレイアウトは触っていない。**上部 line 167〔G-2〕・line 250〔G-3〕と同クラスタでいずれも未割付**） | 共通部品: `HitBoxLayout.tsx` | — | 論点＝**仮想コントローラのレイアウト刷新を M24 で拾うか**。SM-080 / SM-109 と相互参照 |
| `SM-150` | 各技にアイコンをつけたい。アイコンはフリー素材から。ゲームのものは流用しない。　 | `5b775a1:339` | `[予定 M24]` | 実装要求 | **実装** | followup §K `command-icon-annotation`（**割付＝M24〔UX 整理。旧 M23〕・未着手・状態「画面未確定・CHANGE 見込み」**・ボード **D-298**） | 画面: 未確定（技名を出す一部画面）／ icon assets ／ `DES-005`（CHANGE 見込み） | — | ★系統 2 の **CO-024 と同一**（D-4 の重なり）。★SM-109〔G-3〕とは別件だが同じ icon asset を要求しうる |
| `SM-151` | SCANOSSで問題のあるコードが混入していないかチェックしたい。特にReact | `5b775a1:340` | `[調査]` | 調査 | **要判断** | memo line340 の 2026-08-19 注記（別軸〔ライセンス・コンプライアンス調査〕。**followup §F の「依存ライブラリ棚卸し」〔M24〕と隣接するが混入検査は別作業**） | — | — | 論点＝**混入検査を M24 の依存棚卸し〔CO-018〕と束ねるか、別作業とするか** |
| `SM-152` | コード署名つける　／【2026-08-19】未対応（正式リリース前後・配布運用） | `5b775a1:341` | `[未]` | 実装要求 | **返却** | memo line341 の 2026-08-19 注記（followup §F は LAN IP 事前提示／クロスビルド実機／GitHub Actions CI／依存棚卸しまでで、**コード署名は含まれていない**） | — | — | 返却先＝**正式リリース前後（配布運用）** |
| `SM-153` | avastへ登録？誤検知対策。　／【2026-08-19】未対応（正式リリース前 | `5b775a1:342` | `[未]` | 実装要求 | **返却** | memo line342 の 2026-08-19 注記（followup §F の「LAN の FW・AV 既知制約」は AV が LAN 通信を塞ぐ話であり、**本行の「配布物が誤検知される」対策とは別**） | — | — | 返却先＝**正式リリース前後** |
| `SM-154` | ライセンスについて整理。future-notesのcombmgr-license | `5b775a1:343` | `[調査]` | 調査 | **返却** | memo line343 の 2026-08-19 注記（別軸〔方針〕。一次源は `docs/human-notes/future-notes/` の当該メモ） | — | — | 返却先＝**方針（future-notes）** |
| `SM-155` | future-notesをINDEX.mdベースに棚卸。新機能は後回しとして、リ | `5b775a1:344` | `[調査]` | 調査 | **調査** | memo line344 の 2026-08-19 注記（★本文の「M23」は旧番号。「リリース前にやりたかった系」の**受け皿は M24**） | — | — | 何が分かれば実装できるか＝**future-notes のうち「リリース前にやりたかった系」がどれか** |
| `SM-156` | 新しいキャラを登録した時にやるべき作業をチェックリスト化して残す。プリセット、逆 | `5b775a1:345` | `[未]` | 実装要求 | **返却** | followup `character-nickname-aliases-need-per-wave-human-input`（**将来課題・時期未定**＝**D-443**。値の正本が用意できる目処が立ってから） | — | — | 返却先＝**将来課題（時期未定）**。★`CHANGE-118`〔`M22-07`〕が `DES-004` §5.4 へ「導けない別名は人が値を用意しなければ埋まらない」を書かせている |
| `SM-157` | リリース時に名前を正式名にする。　／【2026-08-19】未対応。上部 lin | `5b775a1:346` | `[未]` | 実装要求 | **要判断** | memo line346 の 2026-08-19 注記（上部 line 118 と同件。**開発者が前倒しを明言**） | — | `SM-053` |  |
| `SM-158` | リリース時に別リポジトリ化する。　／【2026-08-19】未対応（正式リリース | `5b775a1:347` | `[未]` | 実装要求 | **返却** | memo line347 の 2026-08-19 注記（正式リリース前後・**開発者作業**） | — | — | 返却先＝**正式リリース前後（開発者作業）** |
| `SM-159` | リリース時にCIの仕組みを作る　／【2026-08-19】followup §F | `5b775a1:348` | `[予定 M24]` | 実装要求 | **実装** | followup §F（**M24〔旧 M23。旧 M20〕へ割付済み**・GitHub Actions CI）／ ベースライン計測は `docs/progress/20260810-ci-baseline-measurement.md` に実施済み（`go test` 245.6 秒＝支配項・flaky 0 件） | CI（GitHub Actions） | — | ★系統 2 の **CO-017 と同一**（D-4 の重なり）。★前倒しは 2026-08-10 の開発者裁定で見送り済み |
| `SM-160` | リリース時に不要ファイルを取り除く　／【2026-08-19】未対応（正式リリー | `5b775a1:349` | `[未]` | 実装要求 | **返却** | memo line349 の 2026-08-19 注記（`docs/` 側の棚卸しは `scripts/check-doc-inventory.sh` が候補を出しており、**削除・移設の実行は開発者の手番**＝`CLAUDE.md` §10.Y ／ **D-196** 境界条件 3） | — | — | 返却先＝**正式リリース前後（開発者の手番）** |

---

## §3 系統 2 ledger（軸 D）

### 3.0 D-1 / D-2: 抽出の範囲と、抽出条件で弁別したもの

`grep -n 'M24' docs/handover/followup-backlog.md` は **17 行**を返した。うち **項目行は 11 行**であり、残る 6 行は次のとおり項目ではない。

| 行 | 何か | 扱い |
|---|---|---|
| 22 | §0 運用の **D-386 裁定文**（番号再編の説明） | 項目ではない |
| 166 | §E の**集約文**（「→ M24 に集約。…」）＝ **7 項目 ＋ 重複 1** | **★D-1 の指示どおり展開して拾う** |
| 184 | §F の**集約文**（「→ M24。…」）＝ **5 項目** | **★同上** |
| 186 | §F の**注記**（CI 前倒し見送りの裁定文） | 項目ではない（`CO-017` の根拠として引用） |
| 212 | §G-11 のメモ本文中の `M24` 言及 | 項目ではない |
| 582 | §J の停止時記録本文中の言及 | 項目ではない |

`phase3-overview` §M24 の**箇条書きは 6 行**である（指示書 §4 D-2 は `E-1`/`E-2`/`E-3`/`D-2`/`D-3`/G 群/H 群 の **7 項目**を挙げるが、実ファイルでは **`D-2` と `D-3` が 1 行に同居**している。項目数としては 7、行数としては 6）。

**★条件付き割付の弁別**: §G の **G-4 / G-5 / G-6 / G-15** は割付欄が「**M24 or ○○**」であり、**M24 への確定割付ではない**。ledger では `条件付き` と明記した。

### 3.1 CO ledger（26 項目）

| `CO-ID` | identity（スラッグ / 既存 ID） | 出所 | disposition | 証拠 | 触る面 | 対応する `SM-ID` | 備考 |
|---|---|---|---|---|---|---|---|
| `CO-001` | `F12-1 / E-2` デフォルト表示キャラ配線漏れ | followup §B 表 ／ `phase3-overview` §M24 `E-2` | **実装** | 割付「M24（旧 M23。旧M20。or M15 前出し）・専用サブ」・未着手・`[CHANGE見込み]` ／ 実査: `INITIAL_CHARACTER_ID` が 4 面で参照 | 画面: `/combos`・`/mycombo`・`/compare`・`/wizard`・`/settings` ／ config 契約 | **`SM-041`**（`SM-044`/`SM-058`/`SM-119`） | 優先 URL/セッション ＞ 設定既定 ＞ リュウ |
| `CO-002` | `F12-3 / E-1` セットプレイ紐付け 2 系統の UI 統合 | followup §B 表 ／ `phase3-overview` §M24 `E-1` | **実装** | 割付「M24（旧 M23。旧M20。M19 提案 UI と整合）」・未着手・`[要設計][CHANGE見込み]` `DES-005` §5.6/§5.7 ／ 実査: `ComboDetailPage.tsx:144`「既存から紐付け」 | 画面: `/combos/:id` ／ API 契約: setup candidate/link | **`SM-019`**（`SM-062`） | **★`ComboDetailPage.tsx` は M23 の削除導線と同一ファイル**（§5） |
| `CO-003` | `F12-3 / E-3` dirty 確認ダイアログ欠如・編集中の画面遷移時データ保持 | followup §B 表 ／ `phase3-overview` §M24 `E-3` | **実装** | 割付「M24（旧 M23。旧M20）」・未着手。`DES-005` §5.7 ／ `REQ` §7「画面遷移時データ保持」と同基盤 | 画面: `/combos/new`・`/combos/:id/edit`・`/setups/:setupId` ／ 共通部品: router ガード | **なし** | **★memo 側に対応行が無い**。`SM-066`/`SM-067`（編集事故 UX）と隣接するが dirty confirm そのものではない |
| `CO-004` | `F12-2` G-3 タグ表示崩れ＋タグフィルタのドロップダウン化 | followup §B 表 ／ `phase3-overview` §M24 `D-3` | **実装** | 割付「M24（旧 M23。旧M20。i18n/UX）」・未着手 ／ 実査: `ComboListFilters.tsx` にドロップダウン機構 0 件 | 画面: `/combos` ／ 共通部品: タグ UI | **`SM-010`**（`SM-064`） | 先行リリースは残存崩れ許容 |
| `CO-005` | `F12-2` SA ゲージ「(0〜3本)」日本語のみ・情報バー頭文字 i18n（B-9） | followup §B 表 ／ `phase3-overview` §M24 `D-3` | **実装** | 割付「M24（旧 M23。旧M20。i18n）」・未着手・**純 i18n＝CHANGE 不要** ／ 実査: `ComboEditorBasicFields.tsx:307`「(0〜3)」・`:339`「(0〜6)」 | 画面: `/combos/new`・`/combos/:id/edit` ／ locale | **`SM-007`** | ドライブ始動側は既に「本」付き＝済 |
| `CO-006` | `F12-6` テスト inline INSERT パターン化 | followup §B 表 | **実装** | 割付「M24（旧 M23。旧M20）リファクタ（低）」・**据え置き** | テストコード | なし | 低優先 |
| `CO-007` | `F12-6` `000012` down orphan 判定 | followup §B 表 ／ §E 集約文（重複記載） | **調査** | 同上。**「既存マイグレ編集禁止」の但し書き付き** ／ ドラフト §3 M23-09 gate「migration cleanup は orphan 判定と rollback 方針を先に確定する」 | `migrations/` | なし | 何が分かれば実装できるか＝**`000012` の down が orphan か否かと rollback 方針** |
| `CO-008` | `recipeCache` API 露出（G 群リファクタ） | followup §E 集約文 ／ `phase3-overview` §M24 G 群 | **実装** | 「→ M24 に集約」・状態「据え置き」 | API 契約: `recipeCache` の露出 | なし | **★`M20-05` の as-built（`combos` 3 本＋`setups` 3 本の 6 経路・`D-413`）を前提に読むこと** |
| `CO-009` | **`queryKey` object・flat 統一**（G 群リファクタ） | followup §E 集約文 ／ `phase3-overview` §M24 G 群 | **実装** | 同上 ／ **実測: 本番コードの `queryKey:` 出現 84 箇所 / 39 ファイル**（§5.2） | **FE 全域（39 ファイル）** | なし | **★M23 との最大の交差面**（§5.2） |
| `CO-010` | `useCheckDuplicate` の TanStack 化（G 群リファクタ） | followup §E 集約文 | **実装** | 同上 | 画面: `/combos/new`・`/combos/:id/edit` | `SM-068`・`SM-074`（**同じ実装面**） | **★束ねる余地がある**——`SM-068`（重複エラーにリンク）が同じフックを触る |
| `CO-011` | `SetupSummary` 型集約（G 群リファクタ） | followup §E 集約文 | **実装** | 同上 | FE 型定義 | なし | — |
| `CO-012` | TOML 全再エンコード＋アトミック書込の重複（G 群リファクタ） | followup §E 集約文 | **実装** | 同上 | BE: `internal/api/config/`・`internal/service/config/` | なし | **★followup §K の `e2e-suite-rewrites-config-toml-comments`（未割付）が同じファイル面を触る** |
| `CO-013` | 検証 fetch 共通 lib 昇格（G 群リファクタ） | followup §E 集約文 | **実装** | 同上 | FE: 検証 fetch | なし | — |
| `CO-014` | `RecipeBuilder` の `console.warn`（G 群リファクタ） | followup §E 集約文 | **実装** | 同上 ／ **`CLAUDE.md` §10 が「`console.log` を本番コードに残す」を禁止事項に挙げている** | 共通部品: `RecipeBuilder` | `SM-112`（同ファイル） | — |
| `CO-015` | LAN IP 事前提示（H 群配布運用） | followup §F 集約文 ／ `phase3-overview` §M24 H 群 | **実装** | 「→ M24」・状態「据え置き」 | 配布・起動（`cmd/combomgr`）／ README | なし | — |
| `CO-016` | クロスビルド mac・Linux 実機起動（H 群） | followup §F 集約文 ／ `phase3-overview` §M24 H 群 | **要判断** | 同上 ／ ドラフト §6.2 が「**開発者手番あり**」と観測 | ビルド | なし | 論点＝**実機起動の確認を誰がいつ行うか**（開発者の手番） |
| `CO-017` | GitHub Actions CI（H 群） | followup §F 集約文 ／ `phase3-overview` §M24 H 群 | **実装** | 同上 ／ **前倒しは 2026-08-10 の開発者裁定で見送り済み**（工程改善 第一波 F-1）。**ベースライン計測は実施済み**＝`docs/progress/20260810-ci-baseline-measurement.md`（`go test` は 2 コア相当で **245.6 秒＝支配項**・全検査 3 回とも緑で flaky 0 件・`-count=1` を付けないと結果キャッシュで「実行せず緑」になる） | CI | **`SM-159`** | **★PR 高速検査 と nightly クロスビルドの分離は未裁定**（`DES-002` §589 は「3 OS クロスビルドを毎回実行」用途で規定） |
| `CO-018` | 依存ライブラリ棚卸し（H 群） | followup §F 集約文 ／ `phase3-overview` §M24 H 群 | **実装** | 同上 | `go.mod` / `package.json` | `SM-151`（隣接・別作業） | — |
| `CO-019` | LAN の FW・AV 既知制約（README/log 記載済）（H 群） | followup §F 集約文 | **調査** | 同上。ドラフト §6.2 が「as-built 再確認」と観測 | README / log | `SM-153`（**別件**） | 何が分かれば実装できるか＝**現行の記載が as-built と合っているか**。★`SM-153`（配布物の誤検知対策）とは別の話 |
| `CO-020` | **`G-4`** 仮想コントローラの i18n 未対応 | followup §G 表（**条件付き**） | **要判断** | 割付「**M24 or 英語ロケール整備**」・据え置き。UI 文字列は全てハードコード日本語・M17-03 でもキーを追加していない | 共通部品: `HitBoxLayout.tsx` ／ locale | `SM-080`・`SM-109`・`SM-149`（仮想コントローラ群） | 論点＝**M24 で拾うか、英語ロケール整備（`CO-026`）と束ねるか** |
| `CO-021` | **`G-5`** flaky `m14-03b-custom-states-realdata.spec.ts:23` | followup §G 表（**条件付き**） | **調査** | 割付「**M24 or fe-e2e 安定化サブ**」・**要観察**。2026-07-16 に 2 回観測 | E2E | なし | 何が分かれば実装できるか＝**再発頻度**（`CO-025` と合流する可能性） |
| `CO-022` | **`G-6`** `m17-01` spec の初回 attempt flaky | followup §G 表（**条件付き**） | **調査** | 割付「**M24 or fe-e2e 安定化サブ**」・要観察。M17-01 で 1 回観測 | E2E | なし | 同上 |
| `CO-023` | **`G-15`** M17-04 実装上の脆さ（低・情報共有） | followup §G 表（**条件付き**） | **要判断** | 割付「**M24 or 改善レーン**」・据え置き。(a) 連携プレビューの StrictMode 対策の router 層リファクタ余地 / (b) 「候補:」先頭提示が実質フォールバック | 画面: `/import/combo` | `SM-071`（同ファイル） | 論点＝**M24 で拾うか改善レーンへ回すか**。★`SM-071`（仮登録の除外）が同じ `ComboImportPage` を触る |
| `CO-024` | **`command-icon-annotation`** コマンドのアイコン併記 | followup §K 表 | **実装** | 割付「**M24（UX 整理。旧 M23）**」・未着手・状態「**画面未確定・CHANGE 見込み**」・ボード **D-298** ／ **`DES-005` に該当記述が無い**（設計卓が実査） | 画面: 未確定（技名を出す一部画面）／ icon assets ／ `DES-005`（CHANGE） | **`SM-150`** | **★前提 2 つ**＝(1) `DES-004` §3.2「公式アイコンは借用できない」＝**自前アイコンの調達が前提** (2) **`G-3`（`CO-020`/`SM-109`）が同じ icon asset を要求する**＝束ねるのが経済的。**★データ源の制約**＝コマンドは `move_commands`（`token_key`）の index-only で、**コマンドを持たない技では併記できない** |
| `CO-025` | E2E timing flake クラスタ（`moves-edit` / `combo-custom-states` / `m12-06-draft-promotion`） | followup §E 本文（M15 期に観測・追加） | **調査** | **★割付が「M23（旧M20 表記を同期）or fe-e2e 安定化サブ」のまま**＝**D-386 (5) の改番が本行に適用されていない**（§6 想定外の発見 ①）。いずれも `retries:1` で緑 | E2E | なし | **★`grep 'M24'` では拾えない行である**。何が分かれば実装できるか＝**恒久対策（タイムアウト延長 or 待機ポーリング化）の要否** |
| `CO-026` | `D-2` 英語ロケール整備 | `phase3-overview` §M24 のみ | **実装** | 「D-2 英語ロケール／D-3 i18n 細部」 ／ 実測: `ja.json` / `en.json` は**ともに leaf キー 487・トップレベル 23 でキー数は一致** | locale（`web/src/locales/en.json`） | `SM-030`（**別件**＝中国語） | **★`followup-backlog` に独立行を持たない**（`G-4` のメモが「英語ロケール整備」と参照するのみ）。**キー数は一致しているため、残るのは訳文の実データ整備である**（memo line79 が「英語ロケール実データ整備(D-2)すら正式リリース時送り」と記録） |

### 3.2 D-4: 系統 1 と系統 2 の重なり（相互参照。**片方を消していない**）

| `CO-ID` | 対応する `SM-ID` | 重なりの性質 |
|---|---|---|
| `CO-001` | `SM-041`（代表）・`SM-044`・`SM-058`・`SM-119` | **同一要求**。memo 側に 4 行、followup 側に 1 行 |
| `CO-002` | `SM-019`（代表）・`SM-062` | **同一要求**。memo 側に 2 行 |
| `CO-004` | `SM-010`（代表）・`SM-064` | **同一要求**。memo 側に 2 行 |
| `CO-005` | `SM-007` | **同一要求** |
| `CO-010` | `SM-068`（代表）・`SM-074` | **実装面が同一**（`useCheckDuplicate`）。要求そのものは別（リファクタ vs UX 改善） |
| `CO-014` | `SM-112` | **ファイルが同一**（`RecipeBuilder`）。要求は別 |
| `CO-017` | `SM-159` | **同一要求** |
| `CO-020` | `SM-080`・`SM-109`・`SM-149` | **同じ共通部品**（`HitBoxLayout.tsx`）。要求は別（i18n / アイコン化 / レイアウト） |
| `CO-023` | `SM-071` | **ファイルが同一**（`ComboImportPage`）。要求は別 |
| `CO-024` | `SM-150` | **同一要求** |

**★系統 2 のうち memo 側に対応行が無いもの**: `CO-003`・`CO-006`〜`CO-009`・`CO-011`・`CO-013`・`CO-015`〜`CO-019`・`CO-021`・`CO-022`・`CO-025`・`CO-026`（**16 項目**）。**⇒ memo だけを母集団にすると、これらは検査の外に落ちる。**

---

## §4 分離の一覧（軸 E）

### 4.1 E-1: `既に解消` に分類した項目

**0 件である。**

**理由（事実）**: 未完集合は定義上 `[済]` を除いた集合であり、その仕分けは **baseline commit `5b775a1`（2026-08-19）** で行われている。同日の再仕分けは「一次資料だけでなく実コードを直接実査し」と memo 自身が方式を記録しており、`[済]` へ落とすべき行は既に落ちている。**本調査でも実コードを実査したが（§0.2 の C-2 系コマンド）、未完集合の中に「実は解消済み」と言える行は見つからなかった。**

**近い性格の 1 件を別枠で示す**（`既に解消` ではなく **`却下（方針と衝突）`**）:

| `SM-ID` | 内容 | 却下理由 | 証拠 |
|---|---|---|---|
| `SM-013` | カスタムプリセットをキャラ毎に設定したい | **方針と衝突**——「キャラ毎に別プリセットを持つ」データモデルは**採らないと裁定済み** | **D-291**「カスタムプリセットは横断 1 枚。キャラ単位は UI とコピー時の実体化で満たす」／`M20-04` がその形で着地（`DES-004` §6） |

**★参考**: memo は 2026-08-11 に「前回パスが `[未]` のまま残していた既済項目 **6 件**」を `[済]` へ是正した実績を持つ（フィルタリセット / タグ新規登録の文言 / 一覧のポジション・ヒット種別表示 / 表示列メニューの閉じ挙動 / tsconfig baseUrl / タグ「+N」のツールチップ）。**これらは既に未完集合の外にある。**

### 4.2 E-2: `再現不能` に分類した項目

**厳密な意味での「再現不能」は 0 件である。** 近い性格として **「指すものを特定できない」1 件**がある。

| `SM-ID` | 内容 | なぜ特定できないか |
|---|---|---|
| `SM-129` | 一覧の並び替えとともに絞り込みも欲しい（`[不明]`） | **一覧には既にフィルタ（`ComboListFilters`）とソート（`ComboSortControls`）の両方がある**ため、この行が指す不足を特定できない。想定される読みは 3 つ——**(a)** ソート項目ごとの絞り込み（例 ダメージ範囲・ヒット種別）が無い ／ **(b)** 並び替え UI の中から絞り込みたい ／ **(c)** マイコンボ側のソート/フィルタが一覧と不一致（＝`SM-004` と同件）。**memo 自身が 2026-08-11 に「断定を避けて `[不明]` のまま残す」と記録している** |

**★あわせて、`却下（行動要求ではない）` 3 件を挙げる**（画面が無い／経路が変わったのではなく、**そもそも実装要求ではない**）:

| `SM-ID` | 内容 | 却下理由 |
|---|---|---|
| `SM-040` | 他にも追加構想は別メモにまとめている | memo 本文「…認識のみ」＝**別メモの存在の記録** |
| `SM-103` | マノンの扱い（state が他キャラほど重要でない） | memo 2026-07-21「**state 重要度のドメイン判断は記録として保持**」。マノン seed は M14-03d で投入済み。**ドラフト §5.2 も「seed 再作業ではない・非 action の close 候補」と一致** |
| `SM-111` | セットプレイ用の特殊技を `setplay-move-notes.md` に記載 | memo 2026-07-21「**データ収集（別ファイル）＝機能 TODO ではない**」 |

### 4.3 E-3: `要判断` 36 件の内訳（**`要追加仕様` はこの中の 6 件**）

| 小分類 | 件数 | `SM-ID` |
|---|---:|---|
| **要追加仕様**（実装できるが、何を作るかが決まっていない） | **6** | `SM-017`（プリセットの表記例）／`SM-045`（逆引きをプリセット作成支援へ出す導線）／`SM-092`（`position` に「不問」を新設）／`SM-095`（既定値を `normal` にすると M18 の入力キューがどう変わるか）／`SM-099`（「全消費」をどの値で表すか）／`SM-141`（「強化版」の表記規約が `DES-004` に無い） |
| **PoC / 採否ゲート** | 7 | `SM-005`・`SM-015`・`SM-020`・`SM-065`・`SM-066`・`SM-067`・`SM-079` |
| **ドメイン値の決め**（開発者の知識が要る） | 6 | `SM-047`・`SM-048`・`SM-054`・`SM-078`・`SM-094`・`SM-142` |
| **★割付先が消えている／未定** | **4** | `SM-051`（importer 側の見直し）／`SM-080`（followup `G-2` が「実機確認できるタイミング」のまま）／**`SM-098`（M18 クローズ後も未処理）**／`SM-149`（`G-2`/`G-3` と同クラスタでいずれも未割付） |
| **feedback gate**（ユーザー反応後に採否） | 4 | `SM-085`・`SM-086`・`SM-128`・`SM-131` |
| **★設計判断の反転**（採るなら再承認が要る） | **3** | `SM-109`（`M15-03`「指摘6＝記号除去」の反転）／`SM-143`・`SM-144`（`DES-004` §5.6.1「曖昧な入力は候補として人へ返す」の反転） |
| **方向が逆の 2 行**（どちらを採るかの裁定が要る） | 2 | `SM-049`（moves 手動 CRUD＝編集を増やす）⇔ `SM-126`（技編集を閲覧限定にする） |
| **開発者判断**（正式名称） | 2 | `SM-053`（代表）・`SM-157` |
| **束ねるかの判断** | 1 | `SM-151`（SCANOSS 混入検査を `CO-018` 依存棚卸しと束ねるか） |
| **特定不能** | 1 | `SM-129`（§4.2） |
| **計** | **36** | — |

### 4.4 E-4 / C-5: ドラフト §5 の仕分けを安定 ID へ読み替え、現在の本文と 1 件ずつ確認した

**結論: ドラフト §5 の全 58 行（§5.1 4 ＋ §5.2 11 ＋ §5.3 38 ＋ §5.4 4 ＋ §5.5 1）は、`da5be16` 経由で 131/131 が現行本文へ解決した。「ドラフトの locator が失効している」行は 0 件である。**

> **★指示書 §4 E-4 は §5.3 を「36 行前後」と書いているが、実数は 38 行である**（ドラフト §6.1 の集計欄も 38 と記載）。

| ドラフト §5 の節 | 行数 | 全件解決 | 本調査の disposition との一致 |
|---|---:|---|---|
| §5.1 M20 へ返す／M20 完了時に再判定 | 4 | **4/4** | **M20 は完了済みのため「返す」先が存在しない**。再判定の結果＝`却下` 1（`SM-013`）／`要判断` 3（`SM-017`・`SM-045`・`SM-047`） |
| §5.2 M14/update wave へ返す | 11 | **11/11** | **11/11 が `返却（M14 / update wave）`** で一致 |
| §5.3 Phase 4・formal release・新製品機能 | **38** | **38/38** | **`返却` 27 ／ `要判断` 10 ／ `実装` 1**（下記★） |
| §5.4 ユーザーフィードバック後に採否 | 4 | **4/4** | **4/4 が `要判断`（feedback gate）** で一致 |
| §5.5 controller `G-2` の割付待ち | 1 | **1/1** | `要判断`（`SM-080`。followup `G-2` は 2026-08-19 時点でも「実機確認できるタイミング」のまま） |

**★§5.3 で仕分けが分かれた 1 件（事実として記録する。判断はしない）**

| `SM-ID` | locator | 内容 | ドラフト §5.3 の仕分け | 本調査の disposition | 分かれた根拠 |
|---|---|---|---|---|---|
| `SM-093` | `5b775a1:221` | 状態はデフォルトで不問であるべき | Phase 4 / scope expansion | **`実装`** | **ドラフト作成（2026-08-13）より前の 2026-08-11 実査で「相手スタンスには `any` が既にある。新規登録の初期値が空文字で `any` ではない」ことが確定している。⇒ 値の新設は不要で、初期値の変更で足りる。** なお**直前の行 `SM-092`（ポジション）は `position` の列挙値に「不問」が存在しないため値の新設が要り、本調査でも `要判断` としている**——**2 行は同じ形に見えるが実態が違う** |

**★ドラフト §5.3 のうち、`要判断` へ倒した 10 件**: `SM-049`・`SM-078`・`SM-092`・`SM-094`・`SM-095`・`SM-098`・`SM-099`・`SM-109`・`SM-126`（＋`SM-093` は上記のとおり `実装`）。**いずれも「Phase 4 へ返す」と断定できるだけの照合先を示せなかったため**、§0.3 の指示に従って `要判断` に倒した。

### 4.5 C-3: 重複の代表 ID（**束ねた側の行も ledger に残してある**）

| 代表 `SM-ID` | 束ねた `SM-ID` | クラスタ |
|---|---|---|
| `SM-041` | `SM-044`・`SM-058`・`SM-119` | **デフォルト表示キャラ**（followup `F12-1 / E-2`） |
| `SM-043` | `SM-120`・`SM-136` | **キャラ選択方式**（`CharacterSelector` が平坦な Select） |
| `SM-019` | `SM-062` | **セットプレイ紐付け UI 統合**（followup `F12-3 / E-1`） |
| `SM-010` | `SM-064` | **タグフィルタの表示崩れ**（followup `F12-2 / G-3`） |
| `SM-072` | `SM-073`・`SM-077` | **エクスポート画面** |
| `SM-068` | `SM-074` | **登録時の重複表示** |
| `SM-055` | `SM-082`・`SM-113` | **キャラ別フレーム差** |
| `SM-063` | `SM-102` | **複数 `custom_states` キャラ** |
| `SM-033` | `SM-106`・`SM-116` | **ヒット区分の拡充** |
| `SM-022` | `SM-096`・`SM-130` | **レシピの改行・縦表示・長文崩れ** |
| `SM-104` | `SM-117` | **一覧フィルタの折りたたみ** |
| `SM-114` | `SM-137` | **ホールド表記** |
| `SM-108` | `SM-140` | **強度なし技** |
| `SM-078` | `SM-094` | **区分（`opponent_size` 等）** |
| `SM-143` | `SM-144` | **曖昧解決の自動化** |
| `SM-053` | `SM-157` | **正式名称** |
| `SM-006` | `SM-059` | **内部値の画面露出** |

**束ねた行数 = 24。⇒ 実装要求 150 行のうち、独立した要求は 126 個である**（ドラフトが「行数は実装タスク数ではない」と観測したとおり）。

### 4.6 C-4: `返却` のうち **M23 宛**（別枠）

| `SM-ID` | locator | 内容 | 返却先の実在 |
|---|---|---|---|
| `SM-036` | `5b775a1:91` | ゴミ箱から復元するデータのバリデーションが十分かチェック | **★`M23-overview` §3 の `M23-04`（復元時のバリデーション）が主参照に `Memo_Someday` line 91 を明記**。**D-463** で M23 のスコープへ取り込み済み。**⇒ 返却先は実在する** |

**ゴミ箱・復元・削除の取り消しに触れる memo 行は、`SM-036` の 1 件のみである**（`5b775a1` 全体を「ゴミ箱」「復元」「削除の取り消し」で走査した結果）。**⇒ M24 側から M23 のスコープへ踏み込む行は無い。**

---

## §5 交差面の実測（軸 F）

> **★本節は判定材料であり、判定ではない**（**D-461**）。並列可否の判定は設計卓と開発者の手番である。

### 5.1 F-1: `実装` 60 件が触る面（4 単位）

#### (i) 画面（`code-facts` §3 のルートと対応づけた）

| route | `実装` 件数 | `SM-ID` |
|---|---:|---|
| **`/combos`** | **18** | `SM-004`・`SM-006`・`SM-010`・`SM-012`・`SM-041`・`SM-043`・`SM-044`・`SM-059`・`SM-064`・`SM-089`・`SM-096`・`SM-097`・`SM-104`・`SM-117`・`SM-127`・`SM-130`・`SM-139`・`SM-146` |
| **`/combos/new`** | **10** | `SM-007`・`SM-068`・`SM-074`・`SM-084`・`SM-093`・`SM-101`・`SM-112`・`SM-118`・`SM-119`・`SM-148` |
| **`/combos/:id`** | **9** | `SM-003`・`SM-019`・`SM-022`・`SM-052`・`SM-057`・`SM-062`・`SM-089`・`SM-096`・`SM-097` |
| `/compare` | 7 | `SM-003`・`SM-014`・`SM-018`・`SM-021`・`SM-041`・`SM-043`・`SM-061` |
| `/mycombo` | 6 | `SM-002`・`SM-004`・`SM-041`・`SM-043`・`SM-058`・`SM-083` |
| `/combos/:id/edit` | 5 | `SM-007`・`SM-068`・`SM-101`・`SM-118`・`SM-148` |
| `/setups/:setupId` | 5 | `SM-011`・`SM-052`・`SM-088`・`SM-112`・`SM-146` |
| `/export/combo` | 5 | `SM-072`・`SM-073`・`SM-075`・`SM-077`・`SM-121` |
| `/import/combo` | 3 | `SM-071`・`SM-076`・`SM-121` |
| `/moves/edit` | 1 | `SM-006` |
| `/wizard` | 1 | `SM-041` |
| `/settings` | 1 | `SM-041` |
| `/combos/:comboId/setups/new` | 1 | `SM-088` |
| `/import/combo/helper` | 1 | `SM-121` |
| `/tags/manage` | 1 | `SM-127` |
| `/punish/search` | 1 | `SM-133` |
| `/punish/list` | 1 | `SM-133` |
| **`/trash`** | **0** | — |
| `/health` / `/presets` / `/presets/:id/edit` | 0 | — |

**★`code-facts` §3 が挙げる 22 ルートのうち、M24 の `実装` が触るのは 17 ルートである。`/trash` は 0 件。**

#### (ii) 共通コンポーネント

| 共通部品 | 件数 | `SM-ID` |
|---|---:|---|
| タグ UI | 3 | `SM-010`・`SM-064`・`SM-127` |
| `CharacterSelector` | 3 | `SM-043`・`SM-120`・`SM-136` |
| `Header`（ナビ・ナビラベル） | 3 | `SM-060`・`SM-072`・`SM-110` |
| **確認ダイアログ** | 1 | `SM-121`（＋`CO-003` の router ガード） |
| **トースト** | 1 | `SM-060`（＋`SM-122` は `調査`） |
| `RecipeBuilder` | 1 | `SM-112`（＋`CO-014`） |
| `ComboEditor` | 1 | `SM-148` |
| `CollapsibleFieldset.tsx` | 1 | `SM-118` |
| モーダル | 1 | `SM-018` |
| 重複検出表示（`useCheckDuplicate`） | 1 | `SM-068`（＋`CO-010`） |
| `okiOptionLabel` | 1 | `SM-097` |
| 既定キャラ解決 | 1 | `SM-041` |
| ソート部品の抽出 | 1 | `SM-004` |

#### (iii) locale キー

**`実装` 8 件が locale に触る**: `SM-006`・`SM-007`・`SM-008`・`SM-016`・`SM-069`・`SM-110`・`SM-133`・`SM-139`。
**系統 2 では `CO-005`・`CO-020`・`CO-026` が locale に触る。**

**実測**: `web/src/locales/ja.json` と `en.json` は**ともにトップレベル 23・leaf キー 487 で一致**している。**⇒ キーの欠落による parity 崩れは現時点で無く、`D-2`（`CO-026`）が扱うのは訳文の実データである。**

#### (iv) DB / API 契約

| `SM-ID` / `CO-ID` | 触る契約 |
|---|---|
| `SM-012` | 一覧に紐づくセットプレイ**件数の取得経路**（既存 API で賄えるかは未確認） |
| `SM-019`（＝`CO-002`） | **setup candidate / link API**（`DES-005` §5.6/§5.7・CHANGE 見込み） |
| `SM-041`（＝`CO-001`） | **config 契約**（既定キャラの設定キー。`Config.Defaults.CharacterID` と `INITIAL_CHARACTER_ID` の関係） |
| `SM-071` | **取込 API**（仮登録の除外を BE で行うか FE で行うか） |
| `CO-008` | **`recipeCache` の API 露出** |
| `CO-012` | `internal/api/config/` ／ `internal/service/config/`（TOML 書込） |

**⇒ DB スキーマ（テーブル・列）を変更する `実装` 項目は 0 件である。**

### 5.2 F-3: `queryKey` の実測（**独立小節**）

**★M24 の G 群「`queryKey` object・flat 統一」（`CO-009`）が触る対象は、本番コードの `queryKey` 全数である。**

| 指標 | 実測値 |
|---|---:|
| **本番コードの `queryKey:` 出現総数**（test 除外） | **84** |
| ── うち インライン配列リテラル（`queryKey: [ …`） | 69 |
| ── うち 変数参照（`queryKey: PRESETS_KEY` 等） | **15** |
| **触るファイル数**（test 除外） | **39** |
| 名前空間（第 1 要素）の異なり | **18** |
| 第 2 要素がオブジェクトリテラルの形（`["combos", filter]` 等の "object 形"） | 14 |
| `code-facts` §2-2 の掲載行数（重複排除済・test 除外） | 64 |

**名前空間の内訳（インライン配列の第 1 要素・出現数）**:

`"combo"` 15 ／ `"combos"` 13 ／ `"setupCandidates"` 10 ／ `"tags"` 9 ／ `"setup"` 4 ／ `"moves"` 4 ／ `PUNISH_LIST_KEY` 2 ／ `PUNISH_FINDER_KEY` 2 ／ `"setups"` 2 ／ `"setplaySuggestions"` 2 ／ `"presets"` 2 ／ `"move"` 2 ／ `"motion-commands"` 2 ／ `"command-index"` 2 ／ `"punish-finder"` 1 ／ `"characters"` 1
**＋ 変数参照**: `PRESETS_KEY`（4）／ `AUTH_STATUS_KEY`（6）／ `USERS_KEY`（3）／ `CONFIG_KEY`（1）／ `PUNISH_FINDER_KEY`・`PUNISH_LIST_KEY`（上記に計上済）

**★M23 が新設する `queryKey` について——as-built では既に存在する。**

`M23-overview` §5.3 (b) は「**ゴミ箱の一覧が新設する `queryKey`**」と述べるが、**実測では `["combos", "trash", characterId]` が `web/src/features/combo/hooks/useTrashCombos.ts:7` に既に存在する**。さらに `usePermanentDelete.ts:17` と `useRestoreCombo.ts:17` が `["combos"]` を invalidate している。**⇒ M23 が触るゴミ箱系の `queryKey` は「新設」ではなく「既存 3 箇所の拡張」である。**（§6 想定外の発見 ②）

**★それでも交差は残る（事実の記録）**: `CO-009` は `["combos"]` 系 13 箇所を含む**全 84 箇所**を触るリファクタであり、`useTrashCombos` / `usePermanentDelete` / `useRestoreCombo` の 3 ファイルはその内側にある。**M23 が同じ 3 ファイルへ行を足す期間と `CO-009` の実施期間が重なると、片方がやり直しになる。**

**★`code-facts` §2-2 の限界（実測で確認）**: 同表は **`PUNISH_FINDER_KEY` / `PUNISH_LIST_KEY` を const 定義行として持たない**。実体は `web/src/features/punish/api.ts:14-15` の **文字列定数**（`= "punish-finder"` / `= "punish-list"`）であり、`_KEY = [` のパターンに合致しないため抽出から漏れている。**また変数参照の 15 箇所も §2-2 には現れない。⇒ `CO-009` の対象数を `code-facts` の 64 行で見積もると 20 箇所を取りこぼす。**

### 5.3 F-2: `M23-overview` §5.3 が挙げた 3 つの交差面の実測

| # | 交差面 | M24 側が実際に触る件数（実測） | M23 側の as-built |
|---|---|---|---|
| **(a)** | **一覧画面・詳細画面** | **一覧 `/combos` = `実装` 18 件 ／ 詳細 `/combos/:id` = `実装` 9 件**。うち `CO-001`（`SM-041`）と `CO-002`（`SM-019`）が両画面に載る | **★ゴミ箱の一覧そのものは交差しない**——`/trash` の実装は `pages/TrashPage.tsx`（61 行）・`TrashList.tsx`・`TrashListRow.tsx`・`TrashBulkActions.tsx`・`useTrashCombos.ts`・`useRestoreCombo.ts`・`usePermanentDelete.ts`・`Header.tsx`・`router.tsx` に閉じており、**`ComboListPage.tsx` / `ComboDetailPage.tsx` を参照していない**。**★ただし削除の入口は交差する**——`useDeleteCombo` ＋ `DeleteComboConfirm` は **`ComboDetailPage.tsx`（`useDeleteCombo` を import・`DeleteComboConfirm` を描画）と `MyComboPage.tsx`** にあり、**`SM-019`（`ComboDetailPage.tsx:144`「既存から紐付け」）・`SM-041`（`MyComboPage.tsx` の `INITIAL_CHARACTER_ID`）と同一ファイルである** |
| **(b)** | **`queryKey`** | **`CO-009` が本番コードの `queryKey` 84 箇所 / 39 ファイルを全数触る** | ゴミ箱系は **3 ファイル・4 箇所が既存**（`["combos","trash",characterId]` ＋ `["combos"]` invalidate 3 箇所）。**⇒ 「新設との衝突」ではなく「既存 4 箇所の同時改変」である**（§5.2） |
| **(c)** | **共通コンポーネント・locale キー** | **確認ダイアログ**: `SM-121`（インポート／エクスポート／取込に確認ダイアログが無い）＋ `CO-003`（dirty 確認）。**トースト／エラー表示**: `SM-060`・`SM-066`・`SM-067`・`SM-122`。**locale**: `実装` 8 件 ＋ `CO-005`・`CO-020`・`CO-026` | `M23-06`（ゴミ箱の利用者体験＝**一括操作・完全削除の確認**）が **`TrashBulkActions.tsx`** と**確認ダイアログの共有部品**を触る。**★`M22-04` で 409/404 の競合モーダルと 2 段階の確認ダイアログが既に増えている**（memo 2026-08-19 実査） |

### 5.4 F-4: マイグレーションを要する項目の在否

**M24 側で `実装` に分類した 60 件のうち、マイグレーションを要する項目は 0 件である。**

- DB スキーマを変更する `実装` 項目は無い（§5.1 (iv)）。
- スキーマ拡張を含む要求（`SM-092` の `position` に「不問」を新設 ／ `SM-105` のタグ タイプ列 ／ `SM-033` 系のヒット区分拡充）は、いずれも **`要判断` または `返却（フェーズ4）`** に落ちている。
- **★`返却（M14 / update wave）` 12 件のうち `SM-114`（ホールド表記）は「seed 投入済みキャラは CSV 修正だけでなく後続の UPDATE マイグレが要る」**（memo 2026-08-11 実査）。**⇒ M24 では実施しないが、update wave 側でマイグレ番号を消費する。**
- **マイグレ連番の現況**: disk 末尾は **`000077_m20_preset_aliases_add_character_id`〜`000077_m20_seed_aliases_p34_srk`** で、**次の空きは `000078`**。`M23-overview` §3 の注記（「マイグレ連番の次の空きは `000078`」）と**一致した**。
- **⇒ M23 と M24 がマイグレ番号を取り合う場面は、現時点の分類では発生しない。**

### 5.5 F-5: `parallel-ops` `E-14` 相当の横断表（**7 列**）

> 列は `parallel-ops` §04 契約必須 6 項目 と 指示書 §4 F-5 の指定に合わせた。**★本表は材料であり、投入の指示ではない。**

| owner | touched area | read-only area | migration 有無 | shared file の単独所有期間 | required predecessor と handoff artifact | 同時実行を禁止する組合せ |
|---|---|---|---|---|---|---|
| **M23**（`M23-01`〜`M23-06`） | `pages/TrashPage.tsx` ／ `features/combo/components/Trash{List,ListRow,BulkActions}.tsx` ／ `features/combo/hooks/use{TrashCombos,RestoreCombo,PermanentDelete}.ts` ／ **`pages/ComboDetailPage.tsx`・`features/mycombo/components/MyComboPage.tsx`（削除の入口＝`useDeleteCombo` / `DeleteComboConfirm`）** ／ `components/Header.tsx`・`router.tsx` ／ `internal/api/combo/routes.go`（`restore` / `permanent`） ／ `DES-005` §5.15・`DES-002` §4.2・`DES-003` §3.3・`DES-006` §7 | M24 の `/export`・`/import`・`/compare`・`/moves/edit`・`/punish/*`・`/presets/*` | **有**（`M23-04` の VAL 新設ほか。**ブロック `121`〜`128` 払い出し済・消費 0**。マイグレは `000078` から） | **`ComboDetailPage.tsx` / `MyComboPage.tsx` / `Header.tsx` / `router.tsx` を M23 が単独所有する期間が要る**（`M23-01`／`M23-06` 期） | 先行＝`M23-RESEARCH-01`。handoff＝`M23-01` 完了時点の「ゴミ箱の一覧が何を返すか」の契約（`M23-overview` §5.2） | **`CO-009`（`queryKey` 統一）** ／ **`SM-019`・`SM-062`（`ComboDetailPage.tsx`）** ／ **`SM-041`・`SM-058`（`MyComboPage.tsx`）** ／ **`SM-121`・`CO-003`（確認ダイアログの共有部品）** |
| **M24-高衝突レーン**（画面・共有フック） | `/combos` 18 件・`/combos/new` 10 件・`/combos/:id` 9 件・`/compare` 7 件・`/mycombo` 6 件・`/setups/:setupId` 5 件 ／ 共通部品 = タグ UI・`CharacterSelector`・`Header`・確認ダイアログ・トースト・`RecipeBuilder`・`ComboEditor`・`CollapsibleFieldset` | `/trash` ／ `migrations/` ／ `internal/api/combo/routes.go` の `restore`/`permanent` | **無**（§5.4） | `Header.tsx` と `router.tsx` は **M23 と競合する**ため単独所有期間の宣言が要る | 先行＝本 ledger の承認。handoff＝`CO-001`（既定キャラ解決）の一箇所実装が済むまで `SM-044`/`SM-058`/`SM-119` を開始しない | **M23 の全サブ**（上記） ／ **`CO-009` は M24 内でも全 UI レーンと衝突する** |
| **M24-`queryKey` レーン**（`CO-009` 単独） | **本番コードの `queryKey` 84 箇所 / 39 ファイル**（`features/{combo,combo-io,setup,setplay,tag,moves,preset,punish,mycombo,character,config,auth,user}` ＋ `hooks/useRecentCombos.ts`） | なし（**全域に触るため read-only 領域を宣言できない**） | 無 | **★repo 全体の FE データ層を単独所有する期間が要る** | 先行＝**M23 のゴミ箱系 `queryKey` 確定**（`["combos","trash",…]` を M23 が拡張し終えてから統一するか、統一後に M23 が追随するかの順序決めが要る） | **M23 の全サブ** ／ **M24 の全 UI レーン** |
| **M24-低衝突レーン**（i18n・配布運用・CI） | `web/src/locales/{ja,en}.json`（`CO-026`・`CO-005`・`CO-020`・`実装` 8 件） ／ CI（`CO-017`＝`SM-159`） ／ 配布（`CO-015`・`CO-016`・`CO-018`） ／ `go.mod`・`package.json` | FE コンポーネント全般（表示語の写像は境界で行う） | 無 | **locale の大規模 rename は同時に一人だけ** | 先行＝**M24 の UI レーンが新語彙を確定してから**（先に部分着手すると locale conflict と手戻りが増える＝ドラフト §3 M23-07 の観測） | **M24 の全 UI レーン**（locale 横断） |
| **M24-テスト安定化レーン**（`CO-021`・`CO-022`・`CO-025`・`CO-006`・`CO-007`） | `web/e2e/` ／ テストコード ／ `migrations/`（`000012` down の判定のみ・**編集禁止**） | 本番コード | **無**（`CO-007` は判定のみ） | なし | 先行＝なし（read-only 調査から始められる） | **full E2E は他レーンの負荷試験と同時実行しない**（ドラフト §3 M23-09 gate） |

**★`parallel-ops` §12 が旧 M23 に与えた振り分け**——「**小修正多数・衝突高低混在。高衝突の小修正だけ 1 レーンに束ねて直列、低衝突は並列。契約の『触る領域』で振り分ける**」——**は、本実測と整合する**。高衝突＝上表の第 2・第 3 レーン、低衝突＝第 4・第 5 レーンにあたる。

**★`parallel-ops` §04 の契約必須 6 項目のうち、本 ledger で埋まったのは 4 項目である**（① スコープ＝§2/§3 ／ ④ 触る領域＝§5.1 ／ ⑤ 凍結インターフェース＝上表 read-only 列 ／ ⑥ 通信規約＝本報告と `progress-log`）。**② DES 所有権 と ③ 予約レンジ は設計卓が埋める**（M24 の CHANGE ブロックは未払い出し。M23 は `121`〜`128` を払い出し済で消費 0）。

---

## §6 想定外の発見

> **§0.3 の例外 2 つ**（想定外の発見 ／ 明らかな矛盾）**に該当するもののみ。事実として記録し、対処は書かない。**

### ① `followup-backlog` §E の 1 行に旧番号 `M23` が残存している（**改番漏れ**）

`docs/handover/followup-backlog.md:169`（§E「M15 期に観測・追加」の E2E timing flake 行）が、割付を

> 「→ **M23（旧M20 表記を同期）** or fe-e2e 安定化サブ」

としている。**しかし同じ §E の見出し行（166 行目）は「→ M24（旧 M23。旧M20）に集約」へ改番済みである。** **D-386 (5)（2026-08-15）の番号再編**により、旧 M23（UX 整理・i18n・繰越・リファクタ）は **M24** へ繰り下がり、**M23 はデータバージョン管理（`FR601`）になった**。

**⇒ 本行を字句どおり読むと「データバージョン管理のマイルストーンで E2E flake を直す」ことになる。**

**★あわせて記録する**: `phase3-overview` v1.2.0 の改訂履歴は「**一括置換はしていない——「M23」は再編後も実在する番号であり、機械置換すると新 M23 まで巻き込む**」と明記している。**本件はその方針の副作用にあたる。** また、**この行は `grep 'M24' docs/handover/followup-backlog.md` では拾えない**——指示書 §4 D-1 の抽出条件（「割付」欄に `M24` を含む行）を字句どおり適用すると**落ちる**。本報告では `CO-025` として ledger に収容した。

### ② `M23-overview` §5.3 (b) の「ゴミ箱の一覧が**新設**する `queryKey`」は as-built と食い違う

`docs/instructions/M23-overview.md:289` は

> 「**M24 の G 群「`queryKey` object・flat 統一」は既存の全キーを触るリファクタである。⇒ ゴミ箱の一覧が新設する `queryKey` と正面から衝突する。**」

と述べるが、**実測では既に存在する**:

| ファイル:行 | `queryKey` |
|---|---|
| `web/src/features/combo/hooks/useTrashCombos.ts:7` | `["combos", "trash", characterId]`（**query 定義**） |
| `web/src/features/combo/hooks/useRestoreCombo.ts:17` | `["combos"]`（invalidate） |
| `web/src/features/combo/hooks/usePermanentDelete.ts:17` | `["combos"]`（invalidate） |

**⇒ 交差の性質は「新設キーとの衝突」ではなく「既存 4 箇所の同時改変」である。** **なお同じ `M23-overview` の別箇所（`phase3-overview` §M23 の 254 行目）は「`/trash` 画面・`restore` / `permanent` API・`deleted_at` 2 表はいずれも実在する」と正しく記録している**——**同一の文書群の中で as-built の認識が割れている。**

### ③ 指示書 §4 E-4 の「36 行前後」は実数と 2 件ずれる

指示書 §4 E-4 は ドラフト §5.3 を「**36 行前後**」とするが、**実数は 38 行**である（ドラフト §5.3 の本文列挙も、§6.1 の集計欄も 38）。**本報告は 38 行すべてを確認した。**

### ④ `code-facts` §2-2 は `queryKey` の 20 箇所を取りこぼす

`docs/handover/code-facts.md` §2-2 は 64 行を掲載するが、**本番コードの `queryKey:` は実測 84 箇所である**。差の内訳は **(a) 変数参照 15 箇所**（`queryKey: PRESETS_KEY` 等。同表は const 定義行を 4 件挙げるのみで使用箇所は挙げない）と **(b) `PUNISH_FINDER_KEY` / `PUNISH_LIST_KEY` の const 定義 2 件**（実体は**配列ではなく文字列定数**で、抽出パターン `_KEY = [` に合致しない）ほか。

**★これは同資料 冒頭「本資料の限界」節が予告している性質の範囲内である**（「変数参照された queryKey（`queryKey: PRESETS_KEY` の中身は §2-2 const 表を参照）」）。**ただし `PUNISH_*` は const 表にも載っていない**ため、同節の案内どおり辿っても実体に届かない。

### ⑤ `SM-098`（確定反撃での D リバの扱い）は割付先が消えたまま残っている

memo が 2026-08-11 に是正記録として残しているとおり——**M18（確定反撃記録）は 2026-07-31 にクローズしたが、本件は扱われていない**。M18 の指示書（`docs/instructions/phase3/M18-*`）・完了報告（`docs/progress/phase3/M18*`）の**いずれにも「リバーサル」の記載がゼロ**であり、設計上は フェーズ2 の `CHANGE-023`/`026` で「ドライブリバーサルは moves 取込対象外」のままである。**⇒ 「マイルストーンがクローズした」ことと「そのマイルストーンへ割り付けた行が処理された」ことが一致していない実例である。** 同型は `SM-080`（followup `G-2`）にもある。

### ⑥ ドラフト §5.3 が同じ枠へ入れた 2 行は、実態が違う

`SM-092`（ポジションはデフォルトで不問）と `SM-093`（状態はデフォルトで不問）は**文面がほぼ同型**でドラフトも同じ §5.3 に入れているが、2026-08-11 の実査では:

- `position` の列挙値は 画面中央／自分画面端／自分画面端寄り／相手画面端／相手画面端寄り の **5 つで「不問」が存在しない** ⇒ **値そのものの新設が要る**
- 相手スタンス（`SM-093`）には **`any`（不問）が既にある** ⇒ **新規登録の初期値が空文字なだけ**

**⇒ 一方は列挙値の追加、他方は初期値の変更である。** 本報告は `SM-092` を `要判断`、`SM-093` を `実装` とした（§4.4）。

---

## §7 `M24-overview` 起票のための要決定事項

> **★推奨案は書くが、決定はしない。**

### 1. 母集団の正本を ledger 側に置くか、`Memo_Someday.txt` 側に置くか

- **何を決めるか**: `SM-ID` と disposition の正本をどこに持つか。
- **なぜ決める必要があるか**: 本報告は ledger 側に持つ設計で作った（指示書 §0.2 が memo の編集を禁じているため）。**同じ情報を 2 か所に持つと必ずドリフトする**（`E-76`）。
- **決めないと何が止まるか**: 次に memo へ行が足されたとき、`SM-ID` を誰が採番するかが決まらない。
- **推奨案**: **ledger を正本とする。** memo には後日 `/memo_someday_cleanup` で「ledger を参照」と 1 行だけ入れる（指示書「開発者への確認事項」1 の暫定案どおり）。**★なお本報告は指示書 §0.2 に従い `Memo_Someday.txt` を一切編集していない。**

### 2. `要判断` 36 件のうち、どこまでを M24 の起票前に決めるか

- **何を決めるか**: §4.3 の 10 小分類のうち、**M24 のサブ分割の前に潰す必要があるもの**の線引き。
- **なぜ決める必要があるか**: 36 件のうち **「要追加仕様」6 件と「設計判断の反転」3 件と「方向が逆の 2 行」は、決まらないとサブの受入条件が書けない**。一方 **「feedback gate」4 件と「PoC / 採否ゲート」7 件は、決めないことが正しい状態**である。
- **決めないと何が止まるか**: サブの Definition of Done が書けない。
- **推奨案**: **「要追加仕様」6 ＋「設計判断の反転」3 ＋「方向が逆」2 ＋「開発者判断（正式名称）」2 ＝ 13 件を起票前に、残る 23 件は各サブの gate として持ち越す。**

### 3. M23 ∥ M24 の並列可否と、並列にする場合のレーン境界

- **何を決めるか**: §5.5 の 5 レーンのうち、M23 と並列に置くもの。
- **なぜ決める必要があるか**: **D-461** が「ledger を得てから判定する」と手順を固定している。本報告がその ledger である。
- **決めないと何が止まるか**: M24 のサブ投入そのもの。
- **材料の要点**: **(a) 一覧・詳細画面の交差は「ゴミ箱の一覧」ではなく「削除の入口」で起きる**——`ComboDetailPage.tsx` と `MyComboPage.tsx` が M23 の `useDeleteCombo`/`DeleteComboConfirm` と M24 の `SM-019`/`SM-041` の双方に載る（§5.3）。**(b) `CO-009` は 84 箇所 / 39 ファイルに触り、read-only 領域を宣言できない**——**M23 と並列に置けるレーンではない**（§5.2）。**(c) 低衝突レーン（i18n・配布運用・CI・テスト安定化）は M23 と面を共有しない。**
- **推奨案**: **`parallel-ops` §12 の振り分けどおり、低衝突レーン（第 4・第 5）だけを M23 と並列に置き、高衝突レーンと `CO-009` は M23 の後へ直列に置く。**

### 4. `CO-009`（`queryKey` 統一）と M23 のゴミ箱系キーの順序

- **何を決めるか**: **M23 がゴミ箱系 `queryKey` を拡張し終えてから統一するか、統一後に M23 が追随するか。**
- **なぜ決める必要があるか**: **順序を決めないと片方が必ずやり直しになる**（`M23-overview` §5.3 (b) の指摘そのもの）。**★ただし前提は補正が要る**——キーは新設ではなく既存 4 箇所である（§6 想定外の発見 ②）。
- **決めないと何が止まるか**: `CO-009` の着手時期。
- **推奨案**: **M23 のゴミ箱系が確定してから `CO-009` を単独レーンで回す。** 既存 4 箇所という規模なら、M23 側の追随コストは小さい。

### 5. `SM-098` と `SM-080` の受け皿（**割付先が消えた 2 件**）

- **何を決めるか**: **M18 クローズ後も未処理の `SM-098`（D リバの扱い）** と、**followup `G-2` が「実機確認できるタイミング」のまま止まっている `SM-080`** を、M24 で拾うか別の受け皿を作るか。
- **なぜ決める必要があるか**: **どちらも「割り付けた先がクローズしたのに項目が残った」形**であり、放置すると次のクローズでも同じことが起きる（§6 想定外の発見 ⑤）。
- **決めないと何が止まるか**: 止まるものは無い。**それが問題である**——止まらないまま検査の外に居続ける。
- **推奨案**: **M24 の ledger に `要判断` として載せたまま、クローズ条件に「`要判断` 36 件すべてに行き先がある」を含める。**

### 6. 系統 2 のうち **memo に対応行が無い 16 項目** の追跡

- **何を決めるか**: `CO-003`・`CO-006`〜`CO-009`・`CO-011`・`CO-013`・`CO-015`〜`CO-019`・`CO-021`・`CO-022`・`CO-025`・`CO-026` の追跡先。
- **なぜ決める必要があるか**: **memo だけを母集団にすると、これらは検査の外に落ちる**（§3.2）。ドラフトが「二系統の ledger」を求めた理由がここにある。
- **決めないと何が止まるか**: M24 のクローズ判定。**片方の ledger だけでは close しない**（ドラフト §0-7）。
- **推奨案**: **`M24-overview` に本報告の §3 を表として取り込み、サブ完了ごとに両 ledger を更新する。**

### 7. `CO-024`（`command-icon-annotation`）と `CO-020`/`SM-109`（`G-3` アイコン化）を束ねるか

- **何を決めるか**: **同じ icon asset を要求しうる 2 件を 1 サブにするか。**
- **なぜ決める必要があるか**: `DES-004` §3.2 が「公式アイコンは借用できない＝自前調達が前提」と定めており、**アイコン資産の調達は二度やる作業ではない**。**★ただし `G-3` には「方向の矢印記号は `M15-03`『指摘6＝記号除去』で意図的に外したもので、アイコンで戻すのは過去判断の反転」という論点が付いており、`CO-024` にはその論点が無い**（新規追加のため）。
- **決めないと何が止まるか**: `CO-024` の指示書スコープ。
- **推奨案**: **アイコン資産の調達だけを共通の先行サブにし、適用面（技名横の併記 / 仮想コントローラ）は別サブに分ける。** 過去判断の反転は `G-3` 側の gate に閉じる。

### 8. `CO-017`（GitHub Actions CI）の PR / nightly 分離

- **何を決めるか**: **PR＝高速検査 ／ nightly＝3 OS クロスビルド** の分離を採るか。
- **なぜ決める必要があるか**: **`DES-002` §589 は CI を「3 OS クロスビルドを毎回実行」用途で規定**しており、PR 高速検査（DORA 推奨 10 分以内）とは目的が違う。**両者の分離は未裁定である**（followup §F の注記）。**★ベースライン実測では `go test` が 2 コア相当で 245.6 秒＝支配項**。
- **決めないと何が止まるか**: `CO-017` の指示書。**分離を採るなら `DES-002` §589 の改訂＝CHANGE が要る。**
- **推奨案**: **`docs/progress/20260810-ci-baseline-measurement.md` §4 の提案 A0-1〜A0-6 から読んで決める**（followup §F の指示どおり）。

### 9. `SM-013` を `却下` として閉じてよいか（**唯一の裁定済み却下**）

- **何を決めるか**: 「カスタムプリセットをキャラ毎に設定したい」を **`却下`（方針と衝突）** として ledger を閉じてよいか。
- **なぜ決める必要があるか**: **D-291 が「キャラ単位は採らない」と裁定済み**だが、**memo の原文は開発者が書いた要望である**。裁定の存在と要望の取り下げは別である。
- **決めないと何が止まるか**: 止まるものは無いが、**却下の記録が開発者の合意を経ていないと、後で同じ要望が再登場する。**
- **推奨案**: **`却下` のまま閉じ、`M24-overview` の「作らないもの」表へ D-291 とともに 1 行載せる。**

---

## 付記: 完了条件（DoD）の充足状況

| # | DoD | 状況 |
|---|---|---|
| 1 | §4 の軸 A〜F を全項目、実値で報告した | **充足**（§1〜§5） |
| 2 | 未完集合の全行に `SM-ID` と disposition（未付与 0 件） | **充足**（§2.1。150 行＋`[調査]` 10 行の計 160 行に採番。未付与 0） |
| 3 | `既に解消` に証拠が 1 件ずつ | **充足**（**該当 0 件**であること自体を §4.1 で根拠付きで報告） |
| 4 | 系統 2 の全項目に `CO-ID` と disposition | **充足**（§3.1。26 項目） |
| 5 | ドラフト §5 の仕分けを安定 ID へ読み替え、現在の本文と 1 件ずつ確認 | **充足**（§4.4。58 行すべて。**失効 0 件**） |
| 6 | 交差面の横断表が `parallel-ops` の 7 列をすべて持つ | **充足**（§5.5） |
| 7 | read-only を逸脱していない（`git status --porcelain` を転記） | **充足**。本報告作成前の時点で `git status --porcelain` は**出力なし**（差分 0）。作成後の差分は **本報告 と `docs/progress/progress-log.md` の 2 ファイルのみ**（§0.2 の許可された例外 2 つ） |
| 8 | `progress-log.md` へ索引行を追記 | **充足** |
| 9 | §7 の要決定事項が `M24-overview` の起票に足る粒度 | **充足**（§7。9 項目・各項目に「何を決めるか／なぜ／決めないと何が止まるか」） |

### 指示書「開発者への確認事項」への回答（推奨案を採用した旨の明記）

| # | 確認事項 | 本報告の扱い |
|---|---|---|
| 1 | `Memo_Someday.txt` の状態マーカーを本調査の結果で更新するか | **暫定案どおり「memo は触らない」を採用した**。**指示書 §0.2 が編集を明示的に禁じているため、これは推奨案の採用ではなく禁止事項の遵守である。** ledger を正本とする形は §7-1 で要決定事項として提示した |
| 2 | `[調査]` 10 件の扱い | **暫定案どおり「含める。ただし `種別 = 調査` として区別する」を採用した**（§4 A-5 の指示と一致）。**`SM-037`〜`SM-040`・`SM-053`・`SM-107`・`SM-111`・`SM-151`・`SM-154`・`SM-155` の 10 件が ledger に載っている** |

---

*以上*
