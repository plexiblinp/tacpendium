# M37-03 レビュー報告書

| 項目 | 内容 |
|------|------|
| 対象 | `docs/instructions/M37-03-list-mycombo-trash-fixes.md` v1.0.0 |
| チェックリスト | `docs/instructions/reviews/M37-03-review-checklist.md` v1.0.0 |
| 完了報告 | `docs/progress/M37-03-completion-report.md` |
| 着手基点 | `38517ef` ／ 対象コミット `422c919` `b8b29fb` `82a1c0b` `fa52fd7` |
| レビュー日 | 2026-09-13 |
| レビュー担当 | 品質レビュー Claude（サブエージェント・コード変更なし） |

---

## 総評

3 件を段に割り、`B15` / `B01` / `B03` を別々に判定した完了報告であり、チェックリスト §0 の「まとめて実装しました」で通さない条件は満たしている。重心の `B15` は同期 6 面すべてが実装に在ることを独立に確認でき、破壊確認（束 A-6）は「回したら緑だった」で済ませず、**1 回目の破壊確認が空振りした事実**まで記録して検査スクリプトの盲点を 1 件掘り出している。`B01` は「表示で閉じる」罠を回避し、旧実装で赤くなることを実測したうえで 2 ケースで並びを一意に特定しており、質が高い。越境（束 D）は `git diff --stat` で機械的に確認し、設計書・`followup-backlog.md`・マイグレ・射程外の面いずれにも 1 行も触れていない。

一方で、**本サブが自ら是正した型の失効記述を 2 箇所拾い漏らしている**。`ListFilter.Sort` の doc コメントは追加した `deleted_at` を含まないまま残り、`ComboListFilters.test.tsx` は「8 値」の記述を 3 箇所（うち 1 つは `it()` のテスト名）抱えたまま緑である。どちらも動作は正しく、テスト・lint・型検査では検出できない型であるため「高」に置く。

---

## 設計準拠性レビュー結果

### 束 A — `B15`（本サブの重心）

| # | 観点 | 評価 | 根拠・問題点 |
|---|---|---|---|
| A-1 | 出発点が実測か | ◎ | `DES-003` §3.2 の 3 値を出発点にしていない。完了報告 §2.1 が 5 面（Go 定数 / TS 定数 / ラベル ja・en / エディタ専用の第 2 ラベル表 / CSV の受け口）を全数実査し **8 値**と確定している。レビューでも `internal/model/combo.go` および `web/src/constants/combo-list.ts` の着手前の値を独立に確認した |
| A-2 | 設計書との食い違いが報告に在るか | ◎ | 完了報告 §2.3。**食い違いは「設計書 vs 実装」ではなく設計書どうし**であるという切り分けが正しい。レビューで実測して裏を取った——`docs/design/03-data-model.md:462` は今日も 3 値、`docs/design/05-screen-design.md:791` は「選択肢は 4 値」、`SUPP-001` §3.2:373 と `DES-006` §2.7 が 8 値。報告の記述と一致する |
| A-3 | 同期の全数 | ○ | 実装面の同期は**欠落なし**（下表）。ただし **doc コメントの追随に漏れが 1 件**（後述・高-2）。`grep` で `stun` / `drive_impact_punish_counter` を全文検索し、値域を列挙している本番ファイルが `model/combo.go` / `csvcore/rules.go` / `combo-list.ts`（3 マップ）/ `labels.ts`（2 マップ）/ `ja.json` `en.json`（各 2 ブロック）/ `check-enum-sync.sh` に限られることを確認した。`punish.ts` の 2 集合へ入れないのは意図的な判断である（§4.1） |
| A-4 | `check-enum-sync.sh` の出力が読まれているか | ◎ | レビューでも実行。`WARN 29 件（ベースライン 29 件）` ／ `"air_hit"（1 ファイル）` ／ `結果: ベースラインどおり(増加なし)`。完了報告の記述と一致。**終了コードでは判定できない**という指摘（§5.3）も実装を読んで確認した（`--strict` でも散在件数の超過でしか `exit 1` しない） |
| A-5 | 5 面すべての実測 | ◎ | `web/e2e/m37-03-list-and-trash.spec.ts` が 保存 / 一覧 / フィルタ / 詳細 / CSV 往復 を分けて見ている。**フィルタは「選択肢が出る」で止めず、`air_hit` の行が 1 件・`normal` の行が 0 件になるまで**見ている。CSV は `[VAL-ENUM]` 0 件を判定に使っており、「取り込めた」だけで閉じていない |
| A-6 | 破壊確認 | ◎ | 完了報告 §5.3。**1 回目の破壊確認が空振りした事実**（`punish.test.ts` の記述が定数の不在を覆い隠していた）を記録し、テスト記述も外して取り直している。これは「機構が生きている証拠」を取りに行った結果としてしか出てこない実測であり、横断課題 §8.3 として登録した判断も妥当 |
| A-7 | 既存データ差分 0 | ◎ | `git diff --stat 38517ef -- migrations/` が空であることをレビューでも確認。DML は 1 本も無く、変更は値域の定義側だけ |
| A-8 | コード・ラベルの選定理由 | ○ | `air_hit` は設計卓案どおりで、既存命名（`stun` が素の名詞、`_hit` 終端の先例）との整合の理由も在る。**短縮ラベルと英語ラベルは設計卓案に含まれていない製造判断**であり、`HIT_TYPE_SHORT_LABEL_KEYS` のコメントが掲げる「案A（英略記中心）」からは外れる。理由（`stun`＝「スタン」の先例）は書かれているが、宛先の扱いとして 1 点補足が要る（低-1） |

**同期の実測（レビュー側の独立確認）**

| 面 | 実測 |
|---|---|
| Go 定数 | `internal/model/combo.go` に `HitTypeAirHit` が**末尾**に在る |
| CSV 受け口 | `csvcore/rules.go` の `DefaultHitTypes` に在り、`enum_sync_test.go` が model との**双方向一致**を要求している |
| TS 定数 | `combo-list.ts` の `HIT_TYPE_VALUES` / `HIT_TYPE_LABEL_KEYS` / `HIT_TYPE_SHORT_LABEL_KEYS` の 3 マップすべて |
| ラベル ja / en | `ja.json` / `en.json` の `situation.hitType` と `situation.hitTypeShort` の計 4 ブロック |
| エディタ専用表 | `labels.ts` の `HIT_TYPE_LABEL_JA` / `HIT_TYPE_OPTIONS` |
| フィルタ選択肢 | `ComboListFilters.tsx:140` が `HIT_TYPE_VALUES` から生成しており自動追随。E2E で実画面を確認済み |
| `CHECK` 制約 | **存在しない**。`grep -i check` で `migrations/*.sql` の `hit_type` 行 0 件。`000001` / `000016` / `000019` いずれも `hit_type TEXT,` |

### 束 B — `B03`（マイコンボのタグ固定表示）

| # | 観点 | 評価 | 根拠 |
|---|---|---|---|
| B-1〜B-5 | 全項目 | ◎（N/A 妥当） | 2026-09-13 の開発者判断で本マイルストーンから除外。完了報告 §0.1 に逐語、§7.2 に 5 項目の N/A 対応表が在る |

**「実装が面倒だから落とした」という体裁になっていないか**——**なっていない**。判断の根拠は 3 つ。

1. **除外が決まる前に下ごしらえが済んでいる**。指示書 §2.1-4 / §2.1-5 の実査（保存先の先例・マイコンボのタグ表示の作り）は完了しており、完了報告 §2.5 / §2.6 に **11 キーの台帳の内訳・`useColumnVisibility.ts` の先例の形・`ComboTableRow.tsx:202-210` の `TagBadgeList` の作り**まで具体に書かれている。着手を避けた者はここまで実査しない
2. **取りやめの帰結が閉じている**。新キーを作っていないため台帳（`web/CLAUDE.md` §1）に 1 文字も足しておらず、`check-browser-storage-keys.sh` は台帳 11 件 / 本番 10 件で緑（レビューでも実行し確認）
3. **追跡を捨てていない**。`M37-overview` §1.2 と followup の `release-pre-request-batch-b01-b15` の状態更新を設計卓へ請求している（§6.3-3）。「実装しなかった」ではなく「取りやめの判断があった」として残している

### 束 C — `B01`（ゴミ箱の並び）

| # | 観点 | 評価 | 根拠・問題点 |
|---|---|---|---|
| C-1 | 「並び順」を見ているか | ◎ | 「削除日時が表示されている」で閉じていない。着手前の実測が **`starter_move_id ASC, position ASC, ...`＝削除日時と無関係な始動状況順**であったことを、`useTrashCombos.ts` が `sort` を送らない → `resolveSortClause` の default 分岐へ落ちる、という経路で示している。レビューでも同フックを読んで確認した |
| C-2 | 既定が削除日時降順・2 件以上の実測 | ◎ | Go 側 5 本（`trash_order_test.go`）＋ E2E 1 本。**2 ケースで挟んで並びを一意に特定**している点が良い——ケース 1 が `id ASC` を、ケース 2 が `id DESC` を排除する。さらに `List` の呼び出しを旧実装へ戻して**実際に赤くなること**を実測している（§5.2 に失敗出力）。`UsesDeletedAtNotID` が旧実装でも通ることを自分から明示しているのも誠実 |
| C-3 | 「既に降順であった」なら証拠付きで閉じているか | ◎ | セットプレイ側は着手前から `deleted_at DESC, id DESC`（`repository/setup/restore.go:216-220`）であることをレビューでも確認。**そちらは変更せず「既に満たしていた」として閉じている**——`M37-overview` §0.2 の要求どおりの形 |
| C-4 | 並び替えを足さない理由 | ◎ | 2026-09-13 の開発者判断（選択肢の提示に対する選択）。理由（ゴミ箱にソート操作面が 1 つも無く、新設は `M27-03` / `P-21` の射程へ近づく）も添えられている |

### 束 D — 越境と自採番

| # | 観点 | 評価 | 実測 |
|---|---|---|---|
| D-1 | マイグレの自採番 | ◎ | `git diff --stat 38517ef` に `migrations/` が 1 件も無い。`ls migrations/` の最終は `000112` で着手前と同一 |
| D-2 | `000113` 消費の明記 | ◎ | 完了報告ヘッダ・§2.2・§6.1 の 3 箇所で「未消費・返す」と明記。`CHECK` 制約が無いため消費しないという判断は、レビュー側の `grep` でも裏が取れた |
| D-3 | 既存値を変えていないか | ◎ | diff 上、既存 8 値の文字列・順序ともに変更なし。追加はすべて末尾 |
| D-4 | 設計書本体の書き換え | ◎ | `git diff --stat` に `docs/design/` が 1 件も無い。CHANGE 請求 3 件は §6.3 に列挙されており起票していない（`D-293` 準拠） |
| D-5 | `followup-backlog.md` の編集 | ◎ | diff に**現れない**。停止時記録は完了報告 §12 に「§J 行の原稿」として 3 件（必須 5 フィールド付き）が置かれており、`D-838` の運用どおり |
| D-6 | `B02` / `B04`〜`B14` | ◎ | 変更ファイル 19 件はすべて `hit_type` の値域 ／ ゴミ箱の `ORDER BY` ／ 報告書に限られる |
| D-7 | 一覧の横幅・列の並び | ◎ | `ComboTable*` / 列設定の面に 1 行も触れていない |
| D-8 | 着手前の版ゲート | ◎ | 完了報告 §1 に 4 点すべて（チェックリスト存在 / overview v1.4.0 / `ls migrations/` 最新 `000112` / 枝元 `38517ef`）が記録されている |

**新規追加ファイルの deletions（教訓 `E-225`）**——`git diff --numstat 38517ef` を確認。新規 3 件（完了報告 568/0 ／ `trash_order_test.go` 151/0 ／ E2E spec 309/0）はいずれも **deletions 0** であり、上書きで消えた行は無い。既存ファイルの deletions も最大 3 行で、すべてコメント・件数表記の置換に対応している。

### 束 E — 検査と説明書

| # | 観点 | 評価 | レビュー側の実測 |
|---|---|---|---|
| E-1 | 検査 3 本 | ◎ | `check-enum-sync.sh`＝ベースラインどおり（29/29・`air_hit` が 1 ファイル）／ `check-browser-storage-keys.sh`＝違反なし（台帳 11 / 本番 10）／ `check-migration-license.sh`＝マイグレ追加なしのため対象外。加えて `check-artifact-integrity.sh`（違反なし・自己検査 16 件）／ `check-stop-discipline.sh`（違反なし）／ `check-progress-log-index.sh`（106 件すべて追記あり）／ `check-md-emphasis.sh docs/progress/M37-03-completion-report.md`（検出 0 行）も緑 |
| E-2 | テスト・型・lint | ○ | レビューで `go test ./...` を全数実行し **green**。`pnpm vitest run` を関連 4 ファイル（`punish.test.ts` / `label-keys.test.ts` / `optionButtons.test.ts` / `ComboListFilters.test.tsx`）で実行し 123 件 green。`+` 行に `eslint-disable` / `nolint` / `console.log` / `fmt.Println` は **1 件も無い**（diff 全文を grep）。**`pnpm test` 全数と `tsc --noEmit` / `pnpm lint` はレビュー側では再実行していない**（完了報告の記載を採る） |
| E-3 | `make e2e` 全数 | △（未検証） | 完了報告は 320 passed / 1 flaky と記載。**レビュー側では再実行していない**。flaky は `m19-03-setup-results.spec.ts` であり、本サブが `setups` の面に 1 行も触れていないことは diff で確認できるため、無関係という切り分け自体は妥当 |
| E-4 | `check-import-order.sh` | ◎ | レビューで実行。**99 / ベースライン 99**。増加なし |
| E-5 | 説明書の追随 | ◎ | `docs/usermanual/tacpendium-readme.html` は `class="todo"` が 88 箇所で `M32-02` 未着地。レビューでも確認した。完了報告 §6.2 が §5.4 / §5.7 / §5.15 / §5.5 の 4 章分の「変わった点」を列挙しており、分岐どおりの対応。**数字キーの割当が `0` になった点まで渡している**のは良い |
| E-6 | `data-testid` | ◎（N/A） | 新規 testid は足していない。E2E が使う `combo-editor-hit-type-air_hit` は `OptionButtonGroup` の `` `${testIdPrefix}-${value}` `` から自動生成される既存規約 |

### 3 件が別々に判定されているか（チェックリスト §0 の差し戻し条件）

◎。完了報告 §10 が 3 行の判定表を持ち、`B01`＝完了／`B03`＝未着手（開発者判断で除外）／`B15`＝完了と別々に判定し、それぞれに別の証拠が付いている。受入条件も §5.1（`B15` の 5 面）／ §5.2（`B01` の並び）／ §7.2（`B03` の N/A）で分かれている。

---

## 設計準拠性以外の指摘事項

### 高-1: `ListFilter.Sort` の doc コメントが追加値 `deleted_at` を含んでいない（失効記述）

`internal/repository/combo/repository.go:73`

```go
Sort           string // ホワイトリスト参照: "default" / "updated_at" / "damage" / "starter_move_id" / "step_count"
```

本サブは同ファイルの `sortFieldWhitelist` へ `"deleted_at"` を足した。**この行はホワイトリストの内容そのものを列挙する doc コメントであり、今日から実物と食い違っている。**

- 通常のテストでは検出できない。`ListFilter` の利用者はコメントを読んで sort 値を選ぶため、`deleted_at` が使えることが構造的に見えない
- 後任がこのコメントを正本として読むと、「ゴミ箱の並びは API から指定できない」と誤読する。完了報告 §4.2 が「後から UI を足すときにサーバ側の変更は要らない」と書いた利点が、コメント側で打ち消されている

**修正**: 当該行へ `/ "deleted_at"` を足す（1 行）。

### 高-2: `ComboListFilters.test.tsx` に「8 値」の失効記述が 3 箇所（うち 1 つはテスト名）

`web/src/features/combo/components/ComboListFilters.test.tsx`

| 行 | 記述 | 状態 |
|---|---|---|
| 542 | `it("★開いた一覧はフル表記である(HIT_TYPE_VALUES の 8 値すべて)", ...)` | **失効**。テスト名であり、テストランナーの出力にそのまま出る |
| 552 | `// 「全て」＋ 8 値。余分な選択肢が紛れていないこと。` | **失効**（今日は 9 値） |
| 561 | `// 短縮キーがフルキーと同一の値は無い(全 8 値で別キーである)。` | **失効**（同上） |
| 533-534 | `// 実測で 9 件のはずが 39 件になった。` | 実測の記録としては歴史だが、**9 件は当時の「全て＋8 値」である**。今日の同条件は 10 件であり、次に読む者が現在値と取り違える |

**assertion は `expect(options.length).toBe(HIT_TYPE_VALUES.length + 1)` と値域ループで書かれているため、テストは緑のまま通る。** 型検査も lint も何も言わない。**人が読む以外に見つける経路が無い。**

本サブは同じ型の失効を `optionButtons.test.ts` で自ら見つけて是正し（完了報告 §7.3）、`punish.test.ts` の「8 値」も直している。**同じ網でこのファイルだけが漏れた**——値域を参照する形で書かれていたためテストが赤くならず、目に留まらなかったものと見られる。放置すると、次にヒット種別へ値を足す担当がこのファイルの「8 値」を現状の記述として複製する（`M19-DESIGN-08` §3.1 の `D-250` と同型）。

**修正**: 3 箇所を 9 値へ、533-534 行は「実測当時は 9 件」と分かる書き方へ。

### 中-1: `HIT_TYPE_OPTIONS` の並びを `HIT_TYPE_VALUES` と突き合わせる検査が無い

`web/src/features/combo/labels.ts:116-118` 自身が **「並びは `constants/combo-list.ts` の `HIT_TYPE_VALUES` と同じにする——エディタの数字キーは配列の順に割り当たるため、他面と並びが違うと『一覧では 5 番目なのにエディタでは 7 番目』が起きる」** と警告している。ところが、

- **値の集合**は `label-keys.test.ts` の `["HIT_TYPE", HIT_TYPE_LABEL_JA, HIT_TYPE_LABELS]` が `toEqual` で守っている
- **`HIT_TYPE_OPTIONS` の並び**を守る検査は無い。`POSITION` には `it("★★POSITION_OPTIONS の並びが POSITION_VALUES と一致する")`（`label-keys.test.ts`）が在るのに、hit_type には同等物が無い

本サブは両方の末尾へ正しく足しているが、**守っているのは人の注意だけ**である。`POSITION` の先例をそのまま写せば 3 行で塞げる。

```ts
expect(HIT_TYPE_OPTIONS.map((o) => o.value)).toEqual([...HIT_TYPE_VALUES]);
```

本サブの射程内（値を足した当のサブ）であり、`M38` 着手と並行で入れられる。

### 中-2: ゴミ箱で `sort=default` を明示されると既定が静かに戻る

`internal/repository/combo/repository.go:752-759`

```go
func resolveListSortClause(filter ListFilter) string {
	if filter.OnlyDeleted {
		if _, ok := sortFieldWhitelist[filter.Sort]; !ok {
			return trashDefaultOrderClause
		}
	}
	return resolveSortClause(filter.Sort, filter.Order)
}
```

`sortFieldWhitelist` には **`"default"` というキーが実在する**。したがって `only_deleted=true&sort=default` を送ると `ok == true` となり、ゴミ箱でも始動状況順へ落ちる。

- **現時点の実害は 0**。`useTrashCombos.ts` は `sort` を送らず、他に `OnlyDeleted` を立てる経路は `internal/api/combo/handler.go:318` の 1 本だけである（レビューで確認）
- **将来の再発経路が在る**。完了報告 §4.2 は「後から並び替え UI を足すときサーバ側の変更は要らない」と書いており、その担当が選択肢に「既定」を置けば `sort=default` が飛ぶ。そのとき**削除日時降順が静かに消える**。テストも `TestList_TrashExplicitSortStillWins` が `deleted_at` を明示する形しか見ていない

**修正案**: `filter.Sort == "default"` もゴミ箱既定へ落とす（`if filter.Sort == "" || filter.Sort == "default"` の形へ寄せる）か、現状を意図として選ぶならその旨をコメントとテストで固定する。どちらでもよいが、**今は「決めていない」状態である**。

### 中-3: `sort=deleted_at` の受理は API 契約の追加であり、as-built の列挙から漏れている

`sortFieldWhitelist` への `deleted_at` 追加は、`GET /api/combos` が受け付ける `sort` の値を 1 つ増やす**外部契約の変更**である。完了報告は §3.2-1 で実装として記載しているが、

- §6.3-2 の CHANGE 請求は「`DES-005` §5.15 へ**並び順**を明記する」であり、**パラメータの受理値が増えたことは含んでいない**
- §11（設計伝達レポートへ回すもの）の `B01` as-built も「§5.15 に並び順の規定が無かった」までで、契約の追加に触れていない

設計卓が §5.15 の追記だけを行うと、**API が受ける値だけがどの設計書にも載らない状態**になる。設計伝達レポート §4 へ 1 行足すこと（実装の修正は不要）。

### 低-1: 短縮ラベルと英語ラベルは設計卓案に含まれない製造判断である旨を、設計伝達レポートで明示すること

指示書 §7-1 が確定していたのは **コード `air_hit` ／ ラベル「空中ヒット」** までである。今回はそれに加えて ja 短縮「空中」／ en 全「Air hit」／ en 短縮「Air」を製造判断で決めている。理由（`stun`＝「スタン」の先例に倣う）は §3.1 に在り、`roles-and-routing` 共通原則1・3 の「進めて報告する」に沿っている。ただし、

- `HIT_TYPE_SHORT_LABEL_KEYS` のコメントは **「語は案A（英略記中心・2026-09-05 開発者確定）」** と書いており、「空中」はそこから外れる（`stun`＝「スタン」という先行例外に倣った形）
- 表示文言はハード列の「ユーザー体験に影響する選択」に触れうる

完了報告 §11-2 に as-built として列挙されているため手続きは満たしているが、**設計卓が「full ja ラベルだけを承認した」と読まないよう、3 語が製造判断である旨を明示する**ことを勧める。実装の修正は不要。

### 低-2: `migrations/000001_init_schema.up.sql:69` のコメントは 3 値のまま（対応不要）

```sql
hit_type  TEXT,   -- normal/counter/punish_counter (CHANGE-006: counter_type → hit_type)
```

失効しているが、**適用済みマイグレは書き換えない**のが本プロジェクトの前提である。本サブの責任ではなく、対応も不要。値域の正本が `SUPP-001` §3.2 に在ることが分かる状態を保てば足りる（完了報告 §2.3 が既にそう書いている）。情報として記録する。

### 低-3: 実 DB の distinct 値が未実測（指示書 §2.1-1 の一部未達）

完了報告 §7.1 が理由（クラウド実行のクリーンクローンに dev DB が無く、`find . -name '*.db'` が 0 件）と、代替（値域を定義する 5 面の全数実査）と、開発者が手元で確かめる `sqlite3` のコマンドまで書いている。**未達を隠さず、判断に必要な情報が揃っている理由まで示している**ため、本サブとしてはこれ以上求めない。

---

## 推奨修正（優先度別）

### 高（M37 完了前に修正必須）

1. **高-1**: `internal/repository/combo/repository.go:73` の `ListFilter.Sort` コメントへ `"deleted_at"` を足す
2. **高-2**: `web/src/features/combo/components/ComboListFilters.test.tsx` の「8 値」3 箇所（542 / 552 / 561）を 9 値へ是正し、533-534 行の「9 件」を実測当時の値と分かる書き方へ

### 中（M38 着手と並行可）

3. **中-1**: `label-keys.test.ts` へ `HIT_TYPE_OPTIONS` の並びと `HIT_TYPE_VALUES` の一致検査を足す（`POSITION_OPTIONS` の先例をそのまま写す）
4. **中-2**: `resolveListSortClause` の `sort=default` の扱いを決め、コメントとテストで固定する
5. **中-3**: `sort=deleted_at` が受理値として増えたことを設計伝達レポート §4 へ 1 行足す

### 低（将来対応）

6. **低-1**: 短縮・英語ラベル 3 語が製造判断である旨を設計伝達レポートで明示する
7. **低-2**: `migrations/000001` のコメントは対応不要。記録のみ
8. **低-3**: 実 DB の distinct 値は開発者の手元で確認（完了報告 §7.1 のコマンド）

---

## ★3 件の判定表（チェックリスト §7・レビューはこの形で答えること）

| ID | 完了 / 一部完了 / 未着手 | 証拠 |
|---|---|---|
| `B01` | **完了** | 着手前は `starter_move_id ASC, position ASC, ...`＝**削除日時と無関係な始動状況順**であった（`useTrashCombos.ts` が `sort` を送らず default 分岐へ落ちる経路をレビューでも確認）。`resolveListSortClause` を新設し `deleted_at DESC, id DESC` へ。Go 5 本＋E2E 1 本で固定し、**2 ケースで挟んで並びを一意に特定**（`id ASC` と `id DESC` の双方を排除）。**旧実装へ戻すと赤くなることを実測済み**（完了報告 §5.2 に失敗出力）。通常一覧の既定が不変であることも別テストで固定。セットプレイ側は着手前から正しく、変更していない（`restore.go:216-220` をレビューで確認）。並び替え UI 不採用は 2026-09-13 開発者判断＋理由あり |
| `B03` | **未着手（開発者判断で本 MS から除外・妥当）** | 2026-09-13 の開発者逐語が完了報告 §0.1 に在り、§7.2 が束 B 全 5 項目の N/A 対応を個別に示す。**「面倒だから落とした」体裁ではない**——除外前に §2.1-4 / §2.1-5 の実査（台帳 11 キーの内訳・`useColumnVisibility.ts` の先例の形・`ComboTableRow.tsx` の `TagBadgeList`）が完了している。新キーを作っていないため `web/CLAUDE.md` §1 の台帳は無編集で、`check-browser-storage-keys.sh` は緑（レビューで実行）。追跡は `M37-overview` §1.2 と followup へ戻す請求が §6.3-3 に在る |
| `B15` | **完了** | 値域 8 → 9。同期はレビュー側の全文検索でも欠落なし——Go 定数 ／ `DefaultHitTypes` ／ TS 3 マップ ／ ja・en 各 2 ブロック ／ エディタ専用 2 マップ ／ フィルタは `HIT_TYPE_VALUES` 由来で自動追随。**`CHECK` 制約なしを 3 通りで確認しマイグレ 0 本**（`000113` 未消費・自採番なし）。**5 面すべて実測**（保存 / 一覧 / フィルタ / 詳細 / CSV 往復。フィルタは絞り込みの結果まで、CSV は `[VAL-ENUM]` 0 件まで）。**破壊確認あり**（片側を消すと `INFO` が出る。1 回目の空振りも記録）。既存データ差分 0（`migrations/` に 1 行も触れず DML なし）。許可リスト・対象外リストのどちらにも入れない判断を明示的に選び、理由がコードコメントとテストの両方に残っている。**★ただし doc コメントの追随に漏れ 2 件**（高-1 / 高-2） |

---

## 良かった点

1. **破壊確認を「機構が生きている証拠」として取りに行った**。1 回目が空振りした事実（`punish.test.ts` の記述が定数の不在を覆い隠した）を隠さず記録し、そこから `check-enum-sync.sh` の `in_constants()` がテストファイルを含むという盲点を掘り出して横断課題（§8.3）に登録している。チェックリスト A-6 が求めた水準を超えている
2. **並びの判定を 2 ケースで挟んだ**。1 ケースでは `id ASC` と `deleted_at DESC` を区別できないという構造を理解したうえでテストを設計しており、さらに **`UsesDeletedAtNotID` は旧実装でも通る**という不利な事実を自分から書いている。テストを「通すため」ではなく「特定するため」に書いている
3. **旧実装へ戻して赤くなることを実測した**。`List` の呼び出しを一時的に戻し、失敗出力をそのまま報告へ貼っている。「テストを足した」と「テストが効いている」を別物として扱えている
4. **設計書の食い違いの切り分けが正確**。「設計書 vs 実装」ではなく「設計書どうし」であり、正本は `SUPP-001` §3.2 と `DES-006` §2.7 側である、という整理はレビュー側の実測とも一致した。さらに `CHANGE-151` が 4 書を改訂した記録を持ちながら 2 箇所が漏れていた事実まで辿っており、§8.4 の「正本を 1 書へ寄せるか」という提起は設計卓にとって価値が高い
5. **横断課題 §8.2 の質**。`air_hit` の追加でヒット種別の欄が `MAX_BUTTONIZED_OPTIONS`＝10 に**張り付いた**（余白 0・割当キーが `0`）ことを発見し、機械では検出されない理由（個数がリテラル）まで示して `optionButtons.test.ts` の注記を書き換え、停止時記録の原稿（§12.1）にも起こしている。レビューで `shouldButtonizeOptions` が `count <= 10` であることを確認し、**今回は線を越えていない**が次の 1 値で越えるという判断が正しいことを裏付けた
6. **越境が 1 件も無い**。設計書・`followup-backlog.md`・マイグレ・射程外の面（`B02` / `B04`〜`B14` / 一覧の横幅）いずれにも触れず、CHANGE も自採番せず請求のみ。停止時記録は `D-838` どおり完了報告 §12 へ原稿として置かれている
7. **E2E の判定の作り方**。件数ではなく**自分が作った 2 件の相対順序**で判定し（他 spec の残置行を掴まない）、CSV は「取り込めた」ではなく `[VAL-ENUM]` 0 件で判定している。仮登録では取込対象から外れるという `M24-06` の制約に当たって本登録へ切り替えた経緯も残っている

---

## 制約事項

- 本レビューはコード上で判定可能な範囲のみ対象。実際の動作確認・パフォーマンス・実機テストは別途実施が必要
- レビュー側で再実行したのは `go test ./...`（全数 green）／ 関連 4 ファイルの `pnpm vitest run`（123 件 green）／ `check-enum-sync.sh`・`check-import-order.sh`・`check-browser-storage-keys.sh`・`check-artifact-integrity.sh`・`check-stop-discipline.sh`・`check-progress-log-index.sh`・`check-md-emphasis.sh`（いずれも緑）。**`pnpm test` 全数・`tsc --noEmit`・`pnpm lint`・`make e2e` 全数は再実行しておらず、完了報告の記載を採っている**
- **不明: 2026-09-13 の開発者判断 2 件**（`B03` の除外 ／ ゴミ箱の並び替え UI を足さない選択）**は完了報告に逐語・要旨として記録されているのみであり、レビュー側で独立に裏を取る経路が無い。** 記録の形式（逐語・日付・帰結の列挙）は適切だが、判断そのものの成否は判定していない
- **不明: `m19-03-setup-results.spec.ts` の flaky が既知か新規か**は判断できない。本サブが `setups` の面に 1 行も触れていないことは diff で確認できるため無関係という切り分けは妥当だが、既知項目との同定は設計卓の手番である

---

## 取り込み結果（自動トリアージ）

**実施**: 2026-09-13 ／ `/implement_plan_full` Phase C ／ 取り込みコミット `後述`

**★「高」指摘の不採用は 0 件である。** ⇒ 開発者へのエスカレーションは発生していない。

| # | 優先度 | 指摘 | 採否 | 理由・対応 |
|---|---|---|---|---|
| 高-1 | 高 | `ListFilter.Sort` の doc コメントに `deleted_at` が無い | **採用** | **指摘のとおりであった。** 本サブで whitelist へ足しながらコメントを直していない＝失効記述である。⇒ コメントを書き換え、**「値を足したら whitelist と本コメントの両方を直すこと」の一文を添えた**（同じ漏れの再発を止めるため） |
| 高-2 | 高 | `ComboListFilters.test.tsx` の「8 値」3 箇所が失効 | **採用** | **指摘のとおりであった。★本サブは同型の失効を `optionButtons.test.ts` と `punish.test.ts` では自ら是正しており、このファイルだけが網から漏れていた。** ⇒ テスト名と注記を値域非依存の表現へ改め、533-536 行の「9 件」は**当時の実測の記録**と分かる書き方にした（assertion は元から `HIT_TYPE_VALUES.length + 1` 由来で緑のままである＝判定は変えていない） |
| 中-1 | 中 | `HIT_TYPE_OPTIONS` の並びを `HIT_TYPE_VALUES` と突き合わせる検査が無い | **採用** | `POSITION_OPTIONS` の先例をそのまま写して `label-keys.test.ts` へ追加。**★破壊確認済み**——`air_hit` を 1 つ手前へ動かすと当該テストだけが赤くなることを実測した。**★この検査は本サブの `§8.2`（数字キーの割当が並びで決まる）と直結する** |
| 中-2 | 中 | `sort=default` がゴミ箱でも「明示指定」として扱われる | **採用（挙動は変えず、固定した）** | **挙動は意図どおりと判断した**——`default` は whitelist の 1 エントリであり「始動状況順で並べてくれ」という明示の要求である（一覧の並び替えで「始動状況順」を選ぶと同じ値が飛ぶ）。⇒ **変えるとそちらが壊れる。** 代わりに `resolveListSortClause` へ理由を書き、`TestList_TrashSortDefaultIsTreatedAsExplicit` で固定した。**★将来ゴミ箱へ並び替え UI を足す担当が「既定へ戻す」に `sort=default` を使うと削除日時降順にならない**——その注意もコメントへ書いた |
| 中-3 | 中 | `sort=deleted_at` の受理が CHANGE 請求・as-built から漏れている | **採用** | 完了報告 §6.3 へ **2b（`DES-002` API 契約の追加）** を、§11 へ as-built の 1 行を追加した |
| 低-1 | 低 | 短縮・英語ラベル 3 語が製造判断である旨を明示 | **採用** | 完了報告 §11 へ **3b** として追加。**設計卓の案はコード `air_hit` と ja 全ラベルまでであり、ja 短縮「空中」／ en「Air hit」「Air」の 3 語は製造が既存の語形に倣って決めた**ことを明記した |
| 低-2 | 低 | `migrations/000001` のコメントは対応不要。記録のみ | **対応不要（採用の必要なし）** | レビュー自身が「対応不要」としている。**★マイグレ済みファイルは書き換えない**（適用済みの履歴であり、`golang-migrate` のチェックサム対象でもある） |
| 低-3 | 低 | 実 DB の distinct 値は開発者の手元で確認 | **採用（申し送りのみ）** | 完了報告 §7.1 に未達の理由（クラウド実行の作業領域に dev DB が存在しない）と確認コマンドを記載済み。**⇒ 追加の対応は開発者の手番である** |

### ★再レビュー往復

**0 回**（初回レビューの指摘をすべて取り込み、未解消の指摘は無い）。⇒ 上限 2 回に達していない。

### ★取り込み後の検査

`go build ./...` / `go vet` / `go test ./...` / `pnpm test` / `pnpm tsc --noEmit` / `pnpm lint` /
`make e2e`（全数）／ `check-artifact-integrity.sh` / `check-enum-sync.sh` / `check-import-order.sh` /
`check-browser-storage-keys.sh` / `check-progress-log-index.sh` / `check-stop-discipline.sh` /
`check-md-emphasis.sh` —— **いずれも緑**（実測は完了報告 §5.5 と §9 を参照）。
