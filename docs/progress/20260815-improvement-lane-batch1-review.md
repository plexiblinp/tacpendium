# 改善レーン 第 1 束 レビュー報告書（クリーンルーム）

| 項目 | 内容 |
|------|------|
| 実施 | Claude Code（品質レビュー担当・クリーンルーム） / 2026-08-15 |
| 対象 | `claude/improvement-lane-batch1-rkky3y` の 3 コミット（起点 `ebcd5dd` → `13793a7`） |
| 要求の正本 | `docs/handover/session-prompts/20260814-improvement-lane-batch1.md` |
| 方針 | 完了報告の数字は一切採用せず、全数を自分で回し直した |

---

## 総評

完了報告の主要な数値主張は、独立に回し直した結果としてほぼすべて再現した。とくに §1.4 のベースライン 1 行ずれの原因分析は、`git archive` で `734cd9b` のツリーを取り出す手順まで含めて完全に再現でき、因果の主張にも飛躍が無い。`Makefile` の target-specific 変数も 6 パターンの実測で意図どおりに動く。指示書 §4 のやらないこと 5 件は実測で違反ゼロ。
一方で、指摘は残る。最も重いのは `remote-ops.md` §5.1 に残った失効記述で、本束が直下の §5.1.1 だけを更新した結果、直前の節が「クラウドでは `make e2e` が完走しない」と断定したまま残った。私自身が同じクラウド環境でフルスイートを exit 0 で完走させており、この断定は実測に反する。
次に、`Makefile` のコメントが devContainer の実態と食い違う。`.devcontainer/devcontainer.json` は既に `PW_EXECUTABLE_PATH` を設定しており、コメントの「devContainer では空のまま」は誤りである。結論は変わらないが理由が逆で、後任が読み違える。
残りは軽微である。マージ前に直すべきは文書 2 か所と `Makefile` のコメント 1 か所で、いずれも実装ロジックの変更を伴わない。

---

## 独立検証の記録

★以下はすべて自分で実行した実測値である。完了報告の記述は「主張」として並記し、一致したかを対で示す。

### 1. 検査本体とベースライン

| # | 私が回したコマンド | 私の実測 | 報告の主張 | 判定 |
|---|---|---|---|---|
| 1 | `bash scripts/check-md-emphasis.sh` | 現在 438 行 / ベースライン 438 行 / 対象 383 ファイル / exit 0 | 438 行・違反なし・対象 382 ファイル | 行数は一致。ファイル数のみ 382 と食い違う（下記 低-1） |
| 2 | `bash scripts/check-md-emphasis.sh --list` のディレクトリ別集計 | change-notes 52 ／ design 14 ／ handover 131 ／ instructions 45 ／ process 10 ／ progress 186 | 同一 | 全 6 分類が一致 |
| 3 | `git archive ebcd5dd docs` を展開し、改修前の絞り込み条件で計数 | 392 ファイル / 373 行 | 392 ファイル / 373 行 | 一致 |
| 4 | 同ツリーへ改修後の絞り込み条件で計数 | 382 ファイル / 438 行 | 382 ファイル / 438 行 | 一致 |
| 5 | 旧対象のうち `phase[0-9]+/` に該当する分だけを計数 | 121 行（handover/phase3 が 20 ／ instructions/phase3 が 101） | 121 行（同内訳） | 一致 |
| 6 | 内訳の検算 | 373 − 121 + 186 = 438 | 同式 | 一致 |
| 7 | `bash scripts/check-md-emphasis.sh --self-test` | 4 対照すべて OK・自己検査 合格 | 合格 | 一致 |

### 2. 陽性対照 3 件（自分で数え直したもの）

| # | 私が回したこと | 私の実測 | 報告の主張 | 判定 |
|---|---|---|---|---|
| 対照1 | 検体 `docs/progress/20260802-whiff-discriminator-search-report.md` を単独検査 | 検出 1 行（`:186`） | 検出 1 行（`:186`） | 一致 |
| 対照1 | 同検体が改修前の対象一覧に含まれるか（`ebcd5dd` ツリーで確認） | 0 件（含まれない） | 0 件 | 一致。改修の効果であることが対で示せている |
| 対照2 | `docs/*/phase[0-9]*/` 配下の md を全数計数 | 481 ファイル / 175 行（9 ディレクトリに分布、親ディレクトリは 11） | 481 ファイル / 175 行（11 ディレクトリ） | 一致 |
| 対照2 | 同内訳 | change-notes/phase2 が 3 ／ handover/phase1 が 7 ／ handover/phase2 が 2 ／ handover/phase3 が 20 ／ instructions/phase1 が 15 ／ instructions/phase2 が 6 ／ instructions/phase3 が 101 ／ progress/phase2 が 8 ／ progress/phase3 が 13 | 同一 | 9 分類すべて一致 |
| 対照2 | `--list` 出力に `phase[0-9]+/` のパスが混入していないか | 0 行 | 0 行 | 一致。実在 175 行に対して 0 なので空振りではない |
| 対照3 | `--list` 出力の `/archive/` パス | 0 行 | 0 件 | 一致 |
| 対照3 | `docs/*/archive/` 配下の閉じない強調 | 10 ファイル / 14 行 | 10 ファイル / 14 行 | 一致（母集合の定義は 低-6 参照） |

★対照 2 について、報告が「先に実在を確かめた」と書いていることが実際に成立しているかを見た。175 行という母数を私自身が独立に出せたので、主張は成立している。「除外したら 0 になった」ではなく「実在 175 行が 1 行も数えられていない」という形になっており、対照として妥当である。

### 3. ベースライン 1 行ずれの原因分析（報告 §1.4）

★`git archive` と `git show` で自分で再現できた。因果の主張にも飛躍は無い。

| # | 私が回したこと | 私の実測 | 報告の主張 | 判定 |
|---|---|---|---|---|
| 1 | `git archive 734cd9b docs` を展開し旧条件で計数 | 376 ファイル / 372 行 | 376 ファイル / 372 行 | 一致 |
| 2 | `git show 734cd9b:scripts/check-md-emphasis.sh` の定数 | `BASELINE_BROKEN=372` | 同ツリーの実測と一致 | 一致 |
| 3 | `ebcd5dd` での旧条件の実測 | 373 行 | 373 行 | 一致 |
| 4 | 両ツリーの検出行集合を差分 | 追加 13 行・削除 12 行。うち 12 組は同一ファイル内の行番号ずれで対応が付き、対応の付かない行は 1 行だけ | 差分の正体は 1 行だけ | 一致 |
| 5 | その 1 行 | `docs/handover/design-reports/20260814-m20-05-design-exceptions.md:29` | 同一 | 一致 |
| 6 | 当該ファイルの履歴 | `7ea1877`（07:03 UTC）で作成、`fdba3ea`（07:53 UTC）で改訂 | 持ち込みは `7ea1877` | 一致 |
| 7 | ★私が追加で確かめたこと。壊れた行が `7ea1877` の時点で既に在ったか | `git show 7ea1877:<file>` を検査 → 1 行検出（当時は 20 行目）。`fdba3ea` 時点でも 1 行（29 行目へずれただけ） | 報告は commit の特定のみ | 報告の帰属は正しい。改訂で混入したのではなく作成時から在った |
| 8 | `git merge-base 7ea1877 734cd9b` | `6d86e562bb712667955386d2e6da3b4589b976f7` | `6d86e56` | 一致 |
| 9 | `git merge-base --is-ancestor` を両方向 | どちらも非祖先 | 互いに祖先ではない | 一致 |
| 10 | `734cd9b` のツリーに当該ファイルが在るか | 存在しない | 視界に無かった | 一致 |

★因果の主張は「`734cd9b` のツリーでは 372 が正しく、並走レーンの 1 行が合流時に加わって 373 になった」というものだが、上記 1・3・4・5・10 が揃っているため飛躍は無い。「測り間違いではない」という否定側の主張も、同ツリーでの再現によって支えられている。

### 4. `Makefile` の target-specific 変数（`make` の実挙動）

★`make --version` は GNU Make 4.3。`Makefile` の 73〜74 行目と同一の書き方を持つ検証用 makefile を scratchpad に作り、6 パターンを実測した。

| # | 条件 | `e2e` の recipe が見る値 | 判定 |
|---|---|---|---|
| a | 環境変数 `PW_EXECUTABLE_PATH=/custom/path` あり | `/custom/path` | (a) 環境変数は尊重される |
| b | 環境変数なし・同梱パス実在 | `/opt/pw-browsers/chromium` | 自動採用される |
| c | 環境変数なし・`other` ターゲットを実行 | 空 | (c) 他ターゲットへ漏れない |
| d | 環境変数あり・`other` ターゲットを実行 | `/custom/path`（環境変数そのもの） | 追加の影響なし |
| e | コマンドライン `make e2e PW_EXECUTABLE_PATH=/cli/path` | `/cli/path` | コマンドライン指定が勝つ |
| f | 環境変数が空文字 | 空 | 従来経路へ落ちる |
| g | 同梱パスが実在しない（`PW_PREINSTALLED_CHROMIUM` を差し替え） | 空 | (b) 実在しなければ空になり従来経路へ落ちる |

- `ls -ld /opt/pw-browsers/chromium` は `chromium-1194/chrome-linux/chrome` へのシンボリックリンクで実在する。
- `web/playwright.config.ts:74` は `process.env.PW_EXECUTABLE_PATH ? ... : {}` の真偽判定なので、`make` が空文字を export しても従来経路になる。空文字が `undefined` と区別されて壊れる形にはなっていない。
- `.PHONY` は 1 行目に既に `e2e` を含む。宣言漏れは無い。
- `PW_PREINSTALLED_CHROMIUM` はリポジトリ内で `Makefile` の 2 か所にしか現れず、変数名の衝突は無い。
- 全ターゲットに `make -n` を掛けたが、警告・パースエラーは 1 件も出なかった。

### 5. 破壊確認と対照（報告 §2.3）

★`make e2e` を実際に 1 回、単一 spec を 1 回回した。`config.toml` は退避してから回し、終了後に復元した。

| # | 私が回したこと | 私の実測 | 報告の主張 | 判定 |
|---|---|---|---|---|
| 1 | `config.toml` を退避 → `env -u PW_EXECUTABLE_PATH make e2e` | exit 0。`102 passed` ＋ `1 flaky`（総数 103）／ 1.7 分 | 緑・103 passed・exit 0・1.7 分 | 完走は一致。内訳は 低-2 参照 |
| 2 | 同実行の先頭出力 | `Created config.toml from config.toml.example (required: isInitialized)` ／ `Using preinstalled Chromium: /opt/pw-browsers/chromium` | 同一 | 一致 |
| 3 | 対照。`config.toml` 不在のまま前段を経由せず単一 spec を直接実行 | exit 1・2 failed。`error-context.md` に `heading "初期設定"` と `heading "ようこそ"` | 前段を無効化すると赤・同じ画面証拠 | 赤くなること・原因がウィザード遷移であることは一致（件数は未検証。制約事項参照） |
| 4 | 実行後の `config.toml` | コメントが全部落ち `[defaults] character_id = 1 / preset_id = 1` が追加されていた | 同一 | 一致。スイート自身が書き換えるという観察は正しい |
| 5 | 実行後の値の保存 | `port = 47318` ／ `path = ""` が保たれ、E2E の env override（47388 など）は焼き付いていない | 値は保たれる | 一致 |
| 6 | 前段の分岐を切り出したシェル片で 3 ケース | 既存 config ありは上書きされず「Using existing」／ 不在は生成 ／ example 不在は `cp` 失敗を検出できず「Created」と出して exit 0 | ケース 3 は報告に記載なし | 中-2 として指摘 |

### 6. 完了条件のコマンド（すべて自分で回した）

| コマンド | 私の実測 | 報告の主張 | 判定 |
|---|---|---|---|
| `bash scripts/check-artifact-integrity.sh` | 違反なし・自己検査 11 件 / ALLOW 除外 1 件 / 生成物 4 件・exit 0 | 同一 | 一致 |
| `go test ./...` | exit 0・FAIL 0（59 パッケージ） | FAIL 0 | 一致 |
| `cd web && pnpm test -- --run` | `Test Files 148 passed (148)` ／ `Tests 1377 passed (1377)` | 148 ファイル / 1,377 件 | 一致 |
| `cd web && pnpm lint` | exit 0 | exit 0 | 一致（実体は `tsc --noEmit`） |
| `bash scripts/check-doc-refs.sh` | dead reference なし・exit 0 | 同一 | 一致 |
| `bash scripts/check-progress-log-index.sh` | 違反なし・exit 0 | 同一 | 一致 |
| `bash scripts/check-stop-discipline.sh` | 違反なし・exit 0 | 同一 | 一致 |
| `bash scripts/check-doc-inventory.sh` | 型に無いファイル 2 件（`docs/handover/m20-desk-startup-prompt.md` ／ `docs/process/m21-measurement-scope-assessment.md`）・本束の新規報告は非該当 | 同一 | 一致。2 件とも本束以前からのもの |
| `bash scripts/check-instruction-format.sh` | ベースラインどおり・exit 0 | 記載なし | 追加で確認、問題なし |
| `bash scripts/check-browser-storage-keys.sh` | 台帳と実装が一致・exit 0 | 記載なし | 追加で確認、問題なし |

### 7. やらないこと（指示書 §4）の実測

| # | 私が回したこと | 実測 |
|---|---|---|
| 1 | `git diff --stat ebcd5dd..HEAD -- docs/design` | 出力 0 行。設計書の差分ゼロ |
| 2 | `git diff --stat ebcd5dd..HEAD -- docs/handover/followup-backlog.md` | 出力 0 行。差分ゼロ |
| 3 | `git diff --name-status ebcd5dd..HEAD` | 変更は 5 ファイルのみ。`docs/progress/` は新規報告の追加と `progress-log.md` の追記だけ |
| 4 | `progress-log.md` の差分位置 | `@@ -4507,3 +4507,16 @@` の純追記。既存行の改変なし |
| 5 | 第 2 束の改名 3 件の痕跡 | `go.mod` ／ `package.json` ／ `internal/` ／ `web/src/` に差分なし。着手されていない |
| 6 | ベースライン定数を通すために動かしていないか | 内訳 373 − 121 + 186 = 438 を私が独立に再現できたため、走査対象の変更に由来する取り直しであると確認 |

### 8. コード引用の照合（報告と `Makefile` コメントが挙げる根拠）

| 引用元 | 私が確認したこと | 判定 |
|---|---|---|
| `internal/service/config/service.go` の `Get` | 137 行目に定義があり、136 行目のコメントが「isInitialized は configPath ファイルの存在有無で判定する」と明記 | 正しい |
| `scripts/wt-new.sh` が port と database.path を書き換える | 56 行目 `PORT=47330`、65 行目 `cp`、67 行目と 69 行目の `sed` で `port` と `path = "./combomgr-dev.db"` を書き換えている | 正しい（行番号の表記のみ 低-4） |
| `config.toml` が `.gitignore` 対象 | `.gitignore:72` で除外、73 行目で example のみ再包含 | 正しい |
| `config.toml.example` の port と `BASE_DEV_PORT` の不一致 | example は `port = 47318`、`web/playwright.config.ts:35` は `BASE_DEV_PORT = 47320` | 正しい。報告 §2.4 の副作用の記述は事実 |
| `SUPP-001` §5.5 の (10′)(11′) | 810 行目に (10′)、819 行目に (11′)。(11′) の末尾が「走査そのものに陽性対照を 1 つ混ぜる」を要求している | 引用は正しく、対照 3 件はこの要求を満たす |

---

## 設計準拠性レビュー結果

| # | 項目 | 評価 | 内容 |
|---|---|---|---|
| 1 | 指示書 §1.2 の 4 項目（対象追加・アーカイブ除外・phase 一般化・ベースライン取り直し） | ◎ | 4 項目すべて実施済み。ベースラインの内訳を両方向で示しており、私の独立計測と完全に一致した |
| 2 | 指示書 §1.3 の陽性対照 3 件 | ◎ | 3 件とも成立。とくに対照 2 は「先に実在を確かめる」という要求の形を守っており、175 行という母数を私も独立に再現できた |
| 3 | 指示書 §1.4 のベースラインずれの原因究明 | ◎ | 再現手順が具体的で、私が `git archive` と `git show` で完全に追試できた。因果に飛躍なし |
| 4 | 指示書 §2.2 の `config.toml` 自動生成 | ○ | 動作は正しい。上書きしない逸脱の根拠（`wt-new.sh`）も実コードと合致。ただし `cp` 失敗が検出されない（中-2） |
| 5 | 指示書 §2.2-3 の `PW_EXECUTABLE_PATH` 吸収 | ○ | 6 パターンの実測で意図どおり。ただしコメントの devContainer 記述が事実と食い違う（中-1） |
| 6 | 指示書 §2.2-4 の `remote-ops.md` 更新 | △ | §5.1.1 は要求どおり。手順を消さず状態列を足しており、版数も整合。しかし直前の §5.1 に失効した断定が残った（高-1） |
| 7 | 指示書 §2.3 の破壊確認と対照 | ○ | 3 条件とも報告されており、条件 1 と 3 の性質は私も再現できた。条件 2 は件数まで再現していないが、赤くなることと原因の画面証拠は再現した |
| 8 | 指示書 §3 の完了条件 | ◎ | 10 項目すべて自分で回して一致。数値の食い違いは低優先の 2 件のみ |
| 9 | 指示書 §4 のやらないこと 5 件 | ◎ | 実測で違反ゼロ |
| 10 | `CLAUDE.md` §6 依存ライブラリ | ◎ | `go.mod` ／ `package.json` に差分なし。indirect からの昇格もなし |
| 11 | `CLAUDE.md` §7 コミット規約 | ◎ | 3 コミットとも Conventional Commits 形式でスコープ付き。作業の節目で区切られている |
| 12 | `CLAUDE.md` §8 progress-log への追記 | ◎ | 純追記で、日付・結果・報告書リンク・横断課題 6 件を備える。相対リンクも正しい |
| 13 | `CLAUDE.md` §9 停止規律 | ◎ | 未解消項目なし。`check-stop-discipline.sh` も緑 |
| 14 | `CLAUDE.md` §10.Y の新設ファイル | ◎ | 新規は報告書 1 本のみで既存の型に収まる。`check-doc-inventory.sh` も新種として検出していない |
| 15 | `SUPP-001` §5.5 (10′)(11′) | ◎ | (11′) の陽性対照要求、(10′) の破壊確認要求とも満たしている |
| 16 | 除外規則が意図しないパスを巻き込まないか | ○ | 実測で確認。詳細は下記 |

### 16 の詳細（除外規則の巻き込み検証）

★私が実際に走査対象と除外結果を突き合わせて確かめたこと。

- 6 ディレクトリ配下で `/phase[0-9]+/` に該当するディレクトリは 16 個（親としては 11 個）で、すべて phase1 ／ phase2 ／ phase3 のアーカイブ置き場だった。
- 名前に `phase` を含むが除外されなかったファイルは 9 件で、いずれも除外されるべきでないものだった。具体的には `docs/instructions/phase3-overview.md`、`docs/handover/phase2-to-phase3-handover.md`、`docs/instructions/templates/phase{N}-overview.template.md` など。ディレクトリ区切りを両側に要求する形なので、ファイル名の途中に `phase3` を含んでも当たらない。
- ご指摘のあった `docs/human-notes/` 配下は、そもそも `find` の起点 6 ディレクトリに含まれないため関係しない。仮に含めても `phase-startup-kit` のようにハイフンが続く形は数字が無いので当たらない。
- ★phase3 は現行フェーズだが、除外されるのは問題ないと判断した。稼働中の指示書は `docs/instructions/` 直下にあり（M19 から M21 まで）、`docs/instructions/phase3/` には M18 以前しか入っていない。同ディレクトリへの最終コミットは 2026-08-11 である。`CLAUDE.md` §10.Y も `docs/handover/phase{N}/` を仕分け済みの置き場として挙げている。つまり phase3 配下はアーカイブ用途で運用されており、コメントの「アーカイブ済みの歴史記録」という説明と実態が合う。ただしこの前提はスクリプトに書かれていない（低-3）。

---

## 設計準拠性以外の指摘事項

1. `make e2e` に永続的な副作用が入った。生成された `config.toml` は実行後も残り、しかも E2E スイート自身がコメントを落として書き換える。私の実測でも、退避して回した後の `config.toml` はコメントが全消しで `[defaults]` 付きになっていた。クリーンな clone の利用者が最初に `make e2e` を回すと、以後アプリの初期設定ウィザードが出なくなり、設定ファイルの説明コメントも読めなくなる。指示どおりの実装だが、`Makefile` にも `remote-ops.md` にも「テストを回すと開発環境の初期化状態が変わる」という一文が無い。
2. 報告書の §2.3 に「対照 2 の `Makefile` はリポジトリ外に生成し `make -f` で回した」と明記されており、リポジトリを汚さずに対照を取る方法として妥当である。私も同じ方式（scratchpad の検証用 makefile とシェル片）で追試した。
3. 作業後の後片付けが行われている。私が検査を始めた時点の `config.toml` は `config.toml.example` とバイト単位で同一で、`web/test-results/` も残っていなかった。E2E を 3 条件回した痕跡を戻してから終えている。
4. `progress-log.md` の横断課題 6 件は、いずれも本束の作業で実際に観測された事実に基づいており、推測で書かれた項目が無い。私が独立に確認できた範囲（1・3・4・5）はすべて正しかった。

---

## 推奨修正（優先度別）

### 高（マージ前に修正必須）

- 高-1. `docs/process/remote-ops.md` §5.1 に失効した断定が残っている。冒頭の一文が「クラウド実行環境では `make e2e` が完走しない」と無条件に述べ、節末の運用結論が「クラウド実行時の E2E は小バッチでの切り分けと開発者機での完走を前提とする」と結んでいる。しかし私は本日、同じクラウド環境でフルスイートを exit 0 で完走させた（102 passed ＋ 1 flaky ／ 1.7 分）。同節が根拠として挙げる実測（73 件・14 分・34 failed）は 2026-08-12 のもので、その後 §5.1.1 の手順 0 が残プロセスという原因を特定し、本束が config という原因を潰している。M20-04 が exit 0 で 79 passed、M21-04 が 80 passed を報告しているのも同じ方向である。本束は §5.1.1 だけを更新したため、直前の節が古い結論のまま残った。動作は正しく、テストも lint も緑になるため、人が読む以外に見つける経路が無い。後任は「クラウドでは完走しない」を出発点に切り分けを始め、本文をコピーして旧記述を複製する。修正は §5.1 の断定へ日付と条件を付し、節末の運用結論を現況へ改めるだけで足り、実装の変更を伴わない。指示書 §2.2-4 の文言は §5.1.1 のみを対象にしていたので範囲外との整理もありうるが、失効記述であるため本項は優先度較正に従って高に置いた。
- 高-2. `docs/handover/followup-backlog.md` の `cloud-e2e-setup-preconditions` 行（355 行目）が失効した。同行は「残るのは恒久対策——`Makefile` で不在時にコピー ／ セットアップスクリプト」「未割付」「手順は明文化済み。恒久対策は未着手」と書いているが、本束で恒久対策が入っている。★修正の手番は設計卓である（D-382 により製造もレビューも同ファイルを編集できない）。製造は完了報告 §4 へ更新候補として正しく列挙しており、本束側の不備ではない。ただし畳まれるまでは、次に同ファイルを読んだ担当が「未着手」と受け取る。したがって本束の反映と同じ手番で畳むこと。

### 中（次サブと並行可）

- 中-1. `Makefile` 70〜72 行目のコメントが devContainer の実態と食い違う。「実在しないローカル・devContainer では空のままとなり、従来どおり `playwright install chromium` へ落ちる」と書いてあるが、`.devcontainer/devcontainer.json:157` が `PW_EXECUTABLE_PATH` へ `/usr/bin/chromium` を設定している。したがって devContainer では変数は空にならず、`?=` によって同値が尊重される経路を通る。挙動不変という結論は保たれるが、理由が逆である。しかも devcontainer.json 側のコメントは「実行時ダウンロードを避ける」と書いており、`Makefile` のコメントを信じた後任は逆の理解を持つ。
- 中-2. `cp` の失敗が検出されない。前段は `if [ ! -f config.toml ]; then cp ...; echo "Created ..."; fi` の形で、分岐の終了ステータスは末尾の `echo` のものになる。私がシェル片を切り出して確かめたところ、`config.toml.example` が無い状態では `cp` が標準エラーへ失敗を出したうえで「Created config.toml from config.toml.example」と表示し、exit 0 で E2E へ進んだ。config は作られていない。これは本束が潰そうとした「全 spec が落ちて自分の変更を疑う」形そのものであり、しかも出力が「作った」と嘘をつく分だけ悪い。`cp ... || exit 1` を足すか、`cp` の直後に生成物の存在を確かめれば済む。
- 中-3. クリーンな clone における E2E ポートが、宣言された前提からずれた。`web/playwright.config.ts` は「E2E 帯は 47390 以上・5273 以上」「ルート dev ポートが 47320 のときオフセット 0 で単一スタック E2E と CI の挙動は不変」と述べ、`config.toml` を読めないときは基準値へフォールバックすると書いている。本束以降、クリーンな clone でも `make e2e` が必ず `config.toml` を作るためフォールバックは効かず、example の `port = 47318` によりオフセットが −2 となって 47388 ／ 5271 になる。報告書 §2.4 はこれを開示しており、実測では 3 条件とも完走し衝突も起きていないので実害は無い。ただし宣言された不変条件と実態がずれた状態が残るので、example の port を 47320 へ揃えるか、`playwright.config.ts` のコメントを現況へ改めること。報告書は followup 候補 `config-toml-example-port-mismatch` として挙げており、判断は設計卓の手番でよい。
- 中-4. `make e2e` の永続的な副作用が文書化されていない（設計準拠性以外の指摘 1）。`Makefile` のコメントか `remote-ops.md` §5.1.1 の注記に一文を足すだけでよい。

### 低（将来対応）

- 低-1. 報告書 §1.2 の表にある「改修後の対象 382 ファイル」は `ebcd5dd` 時点の値で、HEAD では 383 である（報告書自身が `docs/progress/` へ加わったため）。行数 438 は変わらない。スクリプトのコメントは `ebcd5dd` 実測と明記しているので齟齬は無いが、報告書の表には測定時点が書かれていない。
- 低-2. 「103 passed」の内訳が私の実測と違う。私の実行は 102 passed ＋ 1 flaky で、総数が 103 である。flaky は `m18-03c-drainage.spec.ts` の 1 件で、retry で通っている。回ごとに揺れるため報告が誤りとは言えないが、「passed の件数」なのか「総数」なのかが読み取れない。既知の flaky があることは記録しておくほうがよい。
- 低-3. 除外の一般化が成立する前提がスクリプトに書かれていない。`phase[0-9]+/` を除外してよいのは「稼働中の文書は各ディレクトリ直下にあり、`phaseN/` は仕分け済みの置き場である」という運用が続く限りである。将来 phase4 の稼働中文書を `phase4/` へ置く運用に変わると、無検査の領域が静かに生まれる。コメントへ一行足せば足りる。
- 低-4. 報告書 §2.2 の `scripts/wt-new.sh:62-69` という行番号は実際には 65 から 69 である（`cp` が 65、`sed` が 67 と 69。`PORT=47330` は 56）。内容は正しい。
- 低-5. `remote-ops.md` §5.1.1 の手順 2 の「なぜ要るか」列に残る「`Makefile` は既に同変数を受ける形になっている」は、いまや受けるだけでなく設定もするため、状態列と重複気味で古い。
- 低-6. 報告書の対照 3 が使った母集合の定義が書かれていない。「`docs/*/archive/` に実在する 14 行（10 ファイル）」は、`docs/handover/archive/`、`docs/human-notes/archive/`、`docs/process/archive/` の 3 か所を数えた値である。私が最初に `docs` 配下の archive 全体で数えたときは 18 行・17 ファイルになり、差は `docs/human-notes/future-notes/archive/` の 4 行だった。同ディレクトリは走査対象外なので結論は変わらないが、母集合を書かないと追試した側が食い違いを疑う。

---

## 良かった点

1. ★§1.4 の原因分析が完全に追試可能な形で書かれていた。`git archive` で対象コミットのツリーを取り出し、当時の絞り込み条件で数え直すという手順まで書いてあるため、私は迷わず同じ値へ到達できた。しかも「測り間違いではない」という否定側の主張が、同ツリーでの再現によって支えられている。数字を合わせて済ませず原因を先に確かめよ、という指示書 §1.4 の要求に正面から答えている。
2. ベースラインの取り直しを両方向の内訳で示した点。373 − 121 + 186 = 438 という式が独立に再現でき、「赤いから動かした」のではないことを検算可能にしている。指示書 §4-5 が最も警戒していた形を、検算で否定できる形にしてある。
3. 対照 2 の作り方が正しい。「除外したら 0 になった」ではなく「実在 175 行が 1 行も数えられていない」という形にしており、そもそも 0 件だった可能性と区別できている。実在の確認を先に置くという §1.3 の要求どおりである。
4. 指示書 §2.2-2 が上書きを許可していたのに、`wt-new.sh` という具体的な根拠を挙げて安全側へ逸脱し、開発者確認を取っている。「許可は要求ではない」という整理も妥当で、根拠として挙げたコードを私が確認したところ主張どおりだった。
5. `PW_EXECUTABLE_PATH` の吸収が安全側に作られている。環境変数の尊重と実在確認の 2 段構えで、私が試した 6 パターンすべてで既存環境の挙動を変えなかった。
6. `remote-ops.md` の更新方針が指示書 §2.2-4 の主旨を正しく捉えている。手順を消さず状態列を足し、「自動化されたのは前提を満たす操作であって前提そのものではない」という理由まで残している。版数の記述も v1.3.0 として過去分を保持したまま積んでおり整合している。
7. 完了報告に自身の踏んだ失敗（フェンス付きコードブロックへ壊れた形を引用して自分の検査に引っかかったこと）を記録している。同じ形は後任も必ず踏む。
8. 作業痕跡の後片付けがされている（設計準拠性以外の指摘 3）。

---

## 未解消のまま停止した項目

- 報告書 §2.3 の対照 2 について、フルスイートでの件数（58 failed / 2 passed）は再現していない。retry を含めて 10 分を超えるため、単一 spec による縮小再現へ代替した。赤くなること、および原因が初期設定ウィザードへの遷移であること（`error-context.md` の `heading "初期設定"` と `heading "ようこそ"`）は再現できたので、対照としての成立は確認できている。件数そのものは未検証である。
- 報告書 §2.3 の対照 3 のうち、worktree 相当の `config.toml`（port 47330 ／ 専用 DB パス）を置いた場合の完走は再実行していない。上書きされないことと値が保たれることは、port 47318 ／ path 空の実測で確認した。

---

## 制約事項

1. クリーンルーム再現のため、`make e2e` を 1 回と単一 spec の実行を 1 回だけ行った。`config.toml` は事前に scratchpad へ退避し、終了後に復元している。復元後の `config.toml` は `config.toml.example` とバイト単位で同一で、検査開始時に見つけた状態と一致する。
2. 私の実行が生成した `web/test-results/` は削除した。`web/e2e/.tmp/combomgr-e2e.db` は使い捨ての E2E 用 DB で、私の実行により上書きされている。いずれも `.gitignore` 対象である。
3. コード・文書は一切変更していない。作成したのは本レビュー報告書 1 本のみである。Git は `status` ／ `diff` ／ `log` ／ `show` ／ `archive` ／ `merge-base` の読み取り系しか使っていない。`docs/handover/followup-backlog.md` は編集していない。
4. `docs/handover/` の全読込はしていない。必要な節（`followup-backlog.md` の該当行、`m21-to-m22-handover.md` §2.3、`design-reports/` の該当 3 本）のみを開いた。
5. 判定に使った計数ロジックは、`check-md-emphasis.sh` に埋め込まれた Python と同一のものを scratchpad へ写して用いた。スクリプト自体の判定規則が正しいかどうか（CommonMark の解釈が妥当か）は本レビューの範囲外で、`--self-test` の 4 対照が通ることの確認に留めている。
6. `markdown-it-py` は 4.2.0 が導入済みの状態で検査した。未導入時に exit 2 を返す経路は `--self-test` の陽性対照 4 で確認しただけで、実環境での再現はしていない。
7. 高-1 の判断根拠となる完走の実測は、本日 1 回の実行である。同環境で繰り返し完走することまでは確かめていない。ただし M20-04 と M21-04 の完走報告と合わせて 3 例目にあたる。
