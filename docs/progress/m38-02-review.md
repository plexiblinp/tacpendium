# M38-02 レビュー報告書

| 項目 | 内容 |
|------|------|
| 対象作業 | **M38-02**（技編集の導線除去 ＋「引っ越し取込」→「他から引っ越し」改名） |
| 指示書 | `docs/instructions/M38-02-moves-grid-hide-and-rename.md` **v1.0.0** |
| チェックリスト | `docs/instructions/reviews/M38-02-review-checklist.md` **v1.0.0** |
| 着手基点 | `7ce1393` |
| 対象コミット | `23e5bee`（実装） ／ `2b3e03d`（完了報告。**レビュー開始後に増えた**） |
| レビュー実施日 | 2026-09-17 |
| レビュー担当 | 品質レビュー担当 Claude Code |

> **★レビュー中に HEAD が動いた。** 着手時点の HEAD は `23e5bee` だったが、検査の途中で `2b3e03d`（完了報告の追加）が積まれた。**本レビューは `2b3e03d` を対象とする。** 差分の実体は `23e5bee` のままで、`2b3e03d` は `docs/progress/M38-02-completion-report.md` の新設 342 行のみである（`git diff --diff-filter=A/D` で確認済み。**削除ファイルは 0 件**）。

---

## 総評

**射程の 2 件はどちらも、求められた形で着地している。** 技編集は導線だけが外れ、画面・ルート・単体テスト・E2E は 1 行も消えていない。改名は 31 箇所の射程内出現に対して当たり、残る 13 箇所はすべて「経緯コメント」「`RETIRED` の `word`」「否定アサート」のいずれかであることを 1 件ずつ再検証して確認した。

**とくに良い点が 3 つある。** ①「表示語の正本は `ja.json` ではなく実装の直書き 2 箇所である」ことを実測で突き止め、指示書 §1-3 の前提を鵜呑みにしなかったこと。②「設定画面から導線を外す」という指示書の前提が as-built と食い違っていたことを実査で捕まえ、止めずに進めて CHANGE 原稿へ回したこと。③ `retired-words.test.ts` の **陰性対照を同じ手番で移した** こと。陰性対照を置き去りにすると改名した瞬間にテストが赤くなるか、逆に空回りが検出できなくなる。

**不合格には至らないが、「高」が 3 件ある。** いずれも「実装が変わったのに記述が旧のまま残っている」型であり、テストも lint も型検査も緑のまま通る。①`migrations/000033` に 2 度退けた語「取込ヘルパー」が 1 件残っている（射程の切り方が `migrations/` を落とした）。②説明書 ch20 が、いまメニューに無い画面へ利用者を送り続けている。③`progress-log.md` の索引行が無く、`check-progress-log-index.sh` が **EXIT=1** で赤い（完了条件 §6-8 が未達）。

**製造の報告の「緑」は、自分で回して裏が取れた。** Go・フロント・常設検査 11 本・E2E 抜粋 5 本をレビュー側で再実行し、報告の記載と一致することを確認した（§4）。

---

## 設計準拠性レビュー結果

### §A 技編集（消しすぎていないか）

| # | 評価 | 判定の根拠（**★レビュー側で自分で確かめた**） |
|---|---|---|
| **A-1 コードが残っているか** | **◎** | `git diff 7ce1393 2b3e03d` を全量読んだ。`MovesEditGridPage.tsx` は `+17 / -2` で、消えた 2 行はいずれも **コメントの置換の片側**（`// 取込プレビュー(画面17)とは別画面・別導線。` と `// 取込画面からの導線(?character=<code>)で、…`）。**ロジックは 1 行も動いていない。** `features/moves/{MoveEditGrid.tsx, api.ts, types.ts}` ／ `constants/move-warning.ts` は差分ゼロ。`git diff --diff-filter=D` は 0 件 |
| **A-2 ルートが残っており URL 直打ちで着けるか** | **◎** | `router.tsx` は `+2 / -0`（JSX コメントのみ）。`<Route path="/moves/edit" element={<MovesEditGridPage />} />` は無変更。**机上判定で止めず `make e2e-only P="moves-edit"` を自分で回した → `EXIT=0` / 1 passed。** `page.goto("/moves/edit")` で実際に着き、キャラ選択・保存・ラッシュ版生成まで通る |
| **A-3 テストが残っており落ちていないか** | **◎** | `features/moves/MoveEditGrid.test.tsx`（14 本）は差分ゼロ。E2E 4 本（`moves-edit` / `m14-03d-manon-seed` / `m14-03e-third-wave-seed` / `m31-02-character-picker-drag`）のうち `moves-edit` のみ `+11 / -0` の assert 追加で、他 3 本は差分ゼロ。`go test ./...` / `pnpm test` をレビュー側で再実行して全緑 |
| **A-4 「なぜ外したか」の 1 行があるか** | **◎** | 指示書は「1 行」だが **3 ファイルへ置いた**（`Header.tsx:39-44` ／ `MovesEditGridPage.tsx:11-19` ／ `router.tsx:44-45`）。**これは過剰ではなく正しい。** Header を読む人が知りたいのは「なぜ消えたか」、`MovesEditGridPage` を読む人が知りたいのは「なぜ残っているか」であり、片方だけでは A-4 の目的（次の担当が「使われていないから消そう」と判断しないこと）を満たさない。`MovesEditGridPage.tsx` は `followup` の `setup-only-column-has-no-input-path` を名指しで引いており、**フェーズ5 の射程であることが追跡できる** |
| **A-5 設定画面／ヘッダから導線が消えているか** | **◎** | `NAV_LINKS` から `{ to: "/moves/edit", label: "技編集" }` が削除されている。**コメントアウトではなく削除**（`CLAUDE.md` §4 準拠）。`grep -rn "moves/edit" web/src` の結果、残る参照はコメント・`router.tsx`・テストのみで、リンクを描く経路は 0 件。**PC ナビとモバイル Sheet が同じ `NAV_LINKS` を描くため 1 行で両面から消える**ことも実物で確認した。守りとして `Header.test.tsx` に新規 2 本（`+30 / -0`）、`moves-edit.spec.ts` に 3 行の assert が入っている |
| **A-6 E2E を消さずに URL 直打ちへ寄せたか** | **◎** | **寄せ替えは no-op であった。** 実測で `/moves/edit` を踏む E2E 4 本はすべて着手前から `page.goto` の直打ちであり、リンク経由は 0 本。**消した spec は 0 本**（`git diff --diff-filter=D` が 0 件）。`moves-edit.spec.ts` の `goto` に「導線が無いので仕方なくではなく仕様である」という注記を足しているのは適切で、**この spec が緑であること自体がルート温存の証拠**になる設計になっている |

### §B 改名（直し漏れていないか）

| # | 評価 | 判定の根拠 |
|---|---|---|
| **B-1 「引っ越し取込」の出現が 0 件か** | **○** | **レビュー側で `git grep` を再実行した。** リポジトリ全体 `102`（`7ce1393`）→ `84`（`23e5bee`）で報告と一致。射程内（`web/` `internal/` `cmd/` `docs/usermanual/`）は `31` → `13`。**残る 13 件を 1 件ずつ独立に判定し、全件が許容 3 分類のいずれかであることを確認した**（下表）。**⇒ 呼び名としての残存は 0 件である。** ただし **姉妹語「取込ヘルパー」に 1 件の直し漏れ**がある（後述「高-1」）ため ◎ とはしない |
| **B-2 全数と直した数が突き合わせてあるか** | **◎** | 完了報告 §3-1 が **着手前 / 着手後 / 差** の 3 列で表にし、さらに **残る 13 件を 1 行ずつ「残す理由」付きで列挙**している。チェックリストが警告する「『全部直した』だけ」には陥っていない。§3-2 で「取込ヘルパー」側も Go 19 → 0 として別建てで突き合わせてある |
| **B-3 表示語を実物から引いたか** | **◎** | **本サブで最も評価できる判断。** `ja.json` / `en.json` に intake・helper 系のキーが **0 件**であることを実測し、「正本は `Header.tsx` の `label` と `IntakeHelperPage.tsx` の h1 の 2 箇所である」と結論している。レビュー側でも `grep -n "intake\|helper\|引っ越し" web/src/locales/ja.json` で 0 件を確認した。**指示書 §2.3-2 は「`ja.json` から引け」と書いているが、その `ja.json` に無かったという実測を報告しているのが正しい。** 設計書 §5.19 の節名を写した形跡は無い |
| **B-4 ルートを変えていないか** | **◎** | `/import/combo/helper`（`router.tsx:47`）は差分ゼロ。`/moves/edit` も同様。**`INTAKE_USER_RULES_STORAGE_KEY = "intake-helper-user-rules-v1"` も無変更**で、`check-browser-storage-keys.sh` をレビュー側で回して `EXIT=0`（台帳 12 件 / 本番 11 件が一致）。画像ファイル名 `images/ch18-intake-helper-1.png` も据え置きで、`M32` 系の撮影運用と衝突しない |
| **B-5 説明書の該当章も直っているか** | **○** | `tacpendium-readme.html` は 7 箇所すべて置換済みで、`grep -c "引っ越し取込"` は **0**、`grep -c "他から引っ越し"` は **7**。ch18 の h2・TOC・`<code class="path">`（ナビ名）・`alt`・figcaption まで届いている。ch14 の「共通ヘッダから開きます。」も導線除去に合わせて書き換えられ、**URL を載せない判断**も §0.1 の目的（利用者に触らせない）と整合する。**ただし ch20 に失効した誘導が 1 行残っている**（後述「高-2」）ため ◎ とはしない |
| **B-6 走査で `--include` を絞っていないか** | **△** | **`grep` そのものは絞っていない**（`--exclude-dir` は `.git` / `node_modules` / `dist` のみ、全量をファイルへ落として計数）。ここは指示書どおりである。**問題はその後の「射程」の切り方にある**——完了報告 §3-1 が射程を `web/` `internal/` `cmd/` `docs/usermanual/` と定義し、**`migrations/` と `scripts/` を無言で落とした。** その結果「高-1」を取りこぼしている。**⇒ 走査を広く取っても、集計の箱を狭く切れば同じ穴が開く** |

**「引っ越し取込」の残存 13 件 — レビュー側の独立判定（全件 OK）**

| # | 箇所 | 分類 | レビュー判定 |
|---|---|---|---|
| 1 | `Header.test.tsx:65` | 改名の履歴コメント | OK |
| 2 | `Header.test.tsx:70` | 否定アサート（`queryByText(...)` が `null`） | OK |
| 3 | `Header.tsx:33` | 履歴コメント（1 度目の改名） | OK |
| 4 | `Header.tsx:35` | 履歴コメント（2 度目の改名） | OK |
| 5 | `retired-words.test.ts:9` | 履歴コメント | OK |
| 6 | `retired-words.test.ts:13` | 履歴コメント | OK |
| 7 | `retired-words.test.ts:76` | 履歴コメント（`useInstead` を書き換えた理由） | OK |
| 8 | `retired-words.test.ts:83` | `RETIRED` の `word` フィールド | OK。**ここに書いてあるからこそ機械が見張る** |
| 9 | `retired-words.test.ts:152` | 履歴コメント（陰性対照を移した理由） | OK |
| 10 | `IntakeHelperPage.tsx:42` | 履歴コメント | OK |
| 11 | `IntakeHelperPage.tsx:43` | 履歴コメント | OK |
| 12 | `m17-04-intake-helper.spec.ts:4` | 履歴コメント（2 度の改名を 1 行に） | OK |
| 13 | `moves-edit.spec.ts:78` | 否定アサート（`toHaveCount(0)`） | OK |

**★★固有の危険点として指示された「`retired-words.test.ts` が本当に 0 件で通るのか」を、コードを読んで自分で追跡した。⇒ 通る。理由は 2 段である。**

1. **`collectSources` はテストファイルを走査対象から外す**（`retired-words.test.ts:41` の `!/\.test\.tsx?$/.test(e.name)`）。**⇒ 上表の #1・#2・#8・#9 など、テストファイル内の非コメント出現は最初から当たらない。** #13 は `web/e2e` にあり、走査根 `web/src` の外である。
2. **残る非テストファイルの出現（#3・#4・#10・#11）はすべて行頭の `//` 行コメント**であり、`stripComments` の `.filter((l) => !/^\s*(\/\/|\*)/.test(l))` で落ちる。**実物の行を確認し、4 件とも先頭が空白 ＋ `//` であることを確かめた。**

**⇒ 机上判定で終わらせず `pnpm test` を自分で回した。EXIT=0 / Test Files 234 passed / Tests 2953 passed。`retired-words.test.ts` の 8 件（`it.each` の 8 語）もすべて緑である。**

**★陰性対照についても自分で検証した。⇒ 正しく機能している。** `sources.filter((f) => f.body.includes("他から引っ越し")).length > 0` が依存する実体は `Header.tsx:37` の `label: "他から引っ越し"` と `IntakeHelperPage.tsx:162` の h1 であり、**どちらもコメントではない実コード**である。`stripComments` を通っても残るため、対照は空回りしない。**逆に言えば、3 度目の改名で対照を移し忘れればここが赤くなる。⇒ それが狙いどおりの設計である。**

### §C 射程

| # | 評価 | 判定の根拠 |
|---|---|---|
| **C-1 `setup_only` の付与口を作っていないか** | **◎** | 差分に `setup_only` の文字列は 1 件も無い。`MovesEditGridPage.tsx` のコメントで「フェーズ5 の射程である」と明記しているのみで、実装には手を出していない |
| **C-2 `M38-01` の面を触っていないか** | **○** | 新規登録・編集の実装ファイルは差分ゼロ。**唯一の接触は `web/e2e/combo-crud.spec.ts` の `+6 / -1`**（`exact: true` の根拠コメントの書き換え）で、**assert・セレクタ・手順は 1 文字も変わっていない**。失効した根拠を放置せず直したのは正しい判断だが、**同じ spec を `M38-01` が触るとマージで当たる**——並列可のサブであるため、取り込み時に確認すること |
| **C-3 `dist-readme.txt` を触っていないか** | **◎** | `git diff --stat` に `docs/usermanual/dist-readme.txt` は現れない（**0 行**）。完了報告も「`M36-01` の手番＝`D-887`」と明記している |
| **C-4 `docs/design/` 本体・`followup-backlog.md` の差分が 0 行か** | **◎** | どちらも `git diff --stat` に現れない。**`CLAUDE.md` §8 と `D-838` を両方守っている。** CHANGE 原稿・停止時記録はいずれも設計伝達レポート側へ回す形になっている |
| **C-5 マイグレを 1 本も作っていないか** | **◎** | `migrations/` への追加は 0 本（差分に現れない） |

### §D 報告

| # | 評価 | 判定の根拠 |
|---|---|---|
| **D-1 段 1 の実査 4 点が件数で書かれているか** | **◎** | §1-1（31 件を 6 区分に分解）／ §1-2（導線 全数 1 件・設定画面 0 件・Footer 0 件・確定反撃サーチ 0 件）／ §1-3（ルート 1・E2E 4 本・単体 14 本）／ §1-4（章と行番号）。**すべて件数で書かれている。** 計画段階の見積 30 件を実測 31 件へ、Go 側 17 件を実測 19 件へ**自分で訂正して報告している**のは特筆に値する |
| **D-2 完了報告 ＋ `progress-log` の索引行** | **×** | 完了報告は在る（`2b3e03d`）。**`progress-log.md` の索引行が無い。** レビュー側で `bash scripts/check-progress-log-index.sh` を回すと **EXIT=1**、`NG 作業 ID m38-02 が docs/progress/progress-log.md に現れない`。`grep -n "M38" docs/progress/progress-log.md` も 0 件。**完了条件 §6-8 が未達である**（後述「高-3」） |
| **D-3 CHANGE 原稿が設計伝達レポート §1 と §6 にあるか** | **未判定** | `docs/handover/design-reports/` に M38-02 の資料がまだ無い（最新は `20260916-m36-01-design-exceptions.md`）。**完了報告 §6-1 が「原稿を設計伝達レポート §1 / §6 へ置く」と宣言しており、自採番もしていない（`D-293` 準拠）。⇒ 未実施であって誤りではない。** Phase D で必ず出すこと |
| **D-4 `DES-005` §2 の画面一覧も原稿に含まれているか** | **未判定** | 同上。**なお対象行は実在する**——`05-screen-design.md:44` の行 19 が「**引っ越し取込(他のアプリ・メモ・表計算から)**」であり、**§5.19 の節名と §2 の一覧の 2 面に旧名が在る。⇒ 原稿は 2 面を含む必要がある** |
| **D-5 報告を書いた後に検査を回し直したか** | **△** | §8 が **4 検査すべて「Phase D で記載」の空欄**である。**書いたこと自体は `D-890` / `M36-01` §7-1 を正しく踏まえており、「先に回して緑と書く」偽装は犯していない**（§4-2 でも `check-progress-log-index.sh` を「§8 に記載」として保留し、断定を置いていない）。**しかしレビュー時点では未実行であり、いま回すと 1 本が赤い。⇒ D-2 と同じ 1 件が根である** |
| **D-6 全数テストの出力と `EXIT=` / `FAIL` の件数** | **◎** | Go は `EXIT=0` / `--- FAIL` 0 件 / 出力 90 行、フロントは `EXIT=0` / 234 files / 2953 tests、E2E は `EXIT=0` / 352 中 351 passed / 1 flaky と、**すべて件数で貼ってある。さらに本サブに関係する spec 8 本を個別行で抜粋**しており、「全体の結果だけで済ませない」（`M37-05` の教訓）を守っている |

---

## 設計準拠性以外の指摘事項

### 1. `migrations/000033_create_move_commands.up.sql:10` に「取込ヘルパー」が残っている（**高-1**）

```
--   - 索引は広く持つ＝必殺技・空中技も載せる（M17-04 取込ヘルパーが使う）。
```

**この語は 2 度退けられている**——`M24-07`（`CHANGE-147`）で「引っ越し取込」へ、`M38-02` でさらに「他から引っ越し」へ。**⇒ 現行の呼び名を 2 世代さかのぼった名前が、`M17-04` を名指ししながらコード資産の中に残っている。**

**開発者裁定 ② の逐語は「Go 側 19 箇所」であり、この 1 件はその 19 に入っていない**（レビュー側で `git grep -c "取込ヘルパー" 7ce1393 -- internal cmd` を回して **19** を確認。`migrations/` は別の箱にある）。**⇒ 裁定の履行漏れではなく、`git grep` の射程を集計する段で `migrations/` が箱から落ちた結果である**（B-6）。

**なぜ「高」か。** 本プロジェクトの較正どおり、**テストも lint も型検査も緑のまま通る**。`retired-words.test.ts` の走査根は `web/src` だけであり、`migrations/` を 1 バイトも見ない。**人が読む以外に見つける経路が無い。** しかも同ファイルは `M17-04` の設計意図を説明する一次的なコメントであり、**次に索引の範囲を検討する担当が最初に読む場所**である。

> **★ただし「直す」判断そのものは自明ではない。** 適用済みマイグレーションの本文を後から編集することの可否は、本レビューでは判断材料を持たない（`golang-migrate` はチェックサムを持たないためコメント編集で壊れないが、運用方針としての可否は別）。**⇒ 「直すか、直さないと決めて記録するか」を選ぶこと。放置は選択肢に入れない。**

### 2. 説明書 ch20 が、メニューに無い画面へ利用者を送り続けている（**高-2**）

`docs/usermanual/tacpendium-readme.html:983`（第 5 部・確定反撃サーチの章）:

> ダメージが `0` の技は候補に出ません。ダメージが入っていない技は「ダメージ不明」として手動確認の欄へ回ります。この 2 つは別の扱いです。**技のデータを直したいときは moves 編集グリッドへ。**

**ch14 側は正しく直っている**（「この画面はいまメニューに出していません」）。**しかし ch20 のこの 1 行は、そのことを知らない読者を、いま到達手段の無い画面へ送る。⇒ 利用者は探して見つからない。**

**これは `followup` の `punish-unknown-damage-fix-requires-moves-grid`（「確定反撃サーチに導線が無い」）と同じ面である。** 同項目は「導線が無い」ことを課題として記録しているが、**本サブで唯一の導線が消えたことで、その課題の深刻度が一段上がった**。完了報告 §1-2 は「`followup` を実測で確認した（確かに 0 件である）」と書いているが、**確認で止まっており、説明書側の誘導文が失効することまでは繋げていない。**

**⇒ 選択肢は 3 つある。** ①ch20 の該当文を ch14 と同じトーン（いまは開発者側で整えている）へ書き換える。②ch14 への内部リンク（`#ch14`）を張り、ch14 の注記に責任を持たせる。③`followup` へ「説明書 ch20 の誘導が失効している」として登録し、`M32` 系の手番へ回す。**★本レビューはどれが正しいか決めない**（説明書の文面は開発者の面である）**が、いずれかを選んで記録すること。**

### 3. `docs/handover/code-facts.md` の失効は **2 行**であり、報告は 1 行しか挙げていない（**中**）

```
501:| Header | `/import/combo/helper` | "引っ越し取込" | active |
502:| Header | `/moves/edit` | "技編集" | active |
```

完了報告 §6-6 は **`:501` だけ**を挙げて「ナビ表示語がハードコードされており失効する」としている。**`:502` はもっと悪い**——**`active` と書いてあるが、そのナビ項目は存在しない。** 派生資料を索引として引く後任は、**在りもしない導線を前提に作業を始める。**

`check-derived-docs.sh` をレビュー側で回すと `code-facts.md` は **源泉の変化量 159 / 1270 件（13%）で「陳腐化疑い」** と出る。**生成物であり `generate-code-facts.sh` の再生成で直るため、製造が手で書き換えないという判断は正しい。⇒ 申し送りの本文を `:501` から `:501-502` へ直し、「`:502` は消えた導線を `active` と主張している」ことまで書くこと。**

### 4. `Header.test.tsx` の 1 本目のテスト名が、いまも実態より広く名乗っている（**中**）

```
it("PC サイズで NAV_LINKS の全リンクを表示", () => {
```

**本サブはこの問題を自分で見つけている**——新規テストのコメントに「上の 1 本目は 12 本のうち 6 本しか見ていない」と書いてある。**しかし題は直していない。** 現在 `NAV_LINKS` は **11 本**で、このテストが主張するのは **6 本**（`コンボ一覧` / `マイコンボ` / `コンボ比較` / `タグ管理` / `ゴミ箱` / `設定`）である。

**⇒ 見つけたのに直さなかったのは、射程の外だからという判断としては理解できる。しかし「全リンクを表示」という題は、次の担当に「ここは守られている」と誤読させる。** 本サブで新設した 2 本は `技編集` と `他から引っ越し` だけを守るため、**残り 9 本は依然として無防備である**（`確定反撃サーチ` / `確定反撃マイリスト` / `取込` / `プリセット管理` を含む）。

**推奨は 2 案。** ①題を実態に合わせる（「主要 6 リンクを表示」）。②`NAV_LINKS` を `export` して `it.each` で駆動し、題に追いつかせる。**②なら以後の増減が自動で守られる**が、`Header.tsx` の export 追加を伴うため射程判断が要る。**⇒ `M38` 完了前に必須ではない。**

### 5. h1 とナビの 2 つの呼び名 — **増やしてはいないが、台帳に登録されていない**（**中**）

- ナビ: `他から引っ越し`
- h1: `他から引っ越し(他のアプリ・メモ・表計算から)`

**★結論から言うと、本サブがこの型を「増やした」わけではない。** 着手前も同じ非対称（ナビ `引っ越し取込` / h1 `引っ越し取込(他のアプリ・メモ・表計算から)`）であり、**括弧書きは `DES-005` §2 の行 19 が元から持っている**（`05-screen-design.md:44` を実読して確認。同行には「h1 の括弧書きは旧 h1 の形を踏襲した製造の裁量であり、実機確認で承認済み」とまで書いてある）。**⇒ 開発者が今回あらためて選んだという完了報告 §5-4 の記載は、設計書の記述と整合する。指示書 §4.1 の「1 か所落とす」型ではない。**

**問題は別のところにある。** `followup` の `import-screen-has-two-names` は **`Header.tsx` と `ComboImportPage.tsx` の対だけ**を登録している（実読で確認）。**⇒ 本画面の同型は台帳に無い。** 語彙レーンがいつか括弧の有無を揃えにいくとき、**本画面は視界に入らない。**

**⇒ 設計伝達レポート §4 へ「`import-screen-has-two-names` の対象に `IntakeHelperPage` も含める」ことを候補として書くこと**（`D-838` により製造は `followup-backlog.md` を直接編集しない）。**★同項目の本文を書き換えるのではなく、行の追加候補として出すこと。**

### 6. `web/CLAUDE.md` をルール面として製造が編集した点（**低**）

裁定 ① により `web/CLAUDE.md` §1 台帳 #4 の用途欄を書き換えている。**キーは変えておらず `check-browser-storage-keys.sh` は `EXIT=0`**（レビュー側で確認。台帳 12 件 / 本番 11 件が一致）で、実害は無い。**また「2 度退けた語が台帳に残っている」状態の解消であり、目的は正しい。**

**ただし `web/CLAUDE.md` はルール面のファイルであり、`CLAUDE.md` §10.X は「台帳への追記は CHANGE 通知書（または addendum）経由」と定めている。** 今回は**キーの新設ではなく語の是正**であるため §10.X の想定とは異なるが、**製造が単独で判断する面ではない。⇒ 実装前に開発者裁定を取った処理そのものは正しい**（`roles-and-routing` のハード列運用どおり）。**設計伝達レポート §1 で「裁定を得て編集した」ことを明示し、設計卓が追認できる形にすること。**

### 7. 射程を広げた 3 件は、**射程逸脱として問うべきではない**（**判断の記録**）

指示された論点への回答を明示する。**3 件とも妥当であり、射程逸脱には当たらない。** 根拠は 3 つ。

1. **いずれも「本サブの変更によって新たに失効する記述」ではなく「本サブの対象語そのものが、本サブ以前から失効したまま残っていた記述」である。** ②の 19 箇所は `M24-07` が `web/src` しか直さなかった結果であり、③は `CHANGE-055` / `M14-02` で画面 17 を消したときの直し漏れである。**⇒ 同じ語を触る手番で直さなければ、次に誰が直すかの当てが無い。**
2. **本プロジェクトの優先度較正が、この型を「高」と定めている。** 「実装が変わったのに記述が旧のまま」は人が読む以外に検出経路が無く、後任が本文をコピーして複製する（`D-250` と同型）。**⇒ 見つけた手番で直すのが方針である。**
3. **実装前に開発者へ確認し、裁定を得ている。** `roles-and-routing` のハード列運用として正しい経路であり、**推測で広げていない。**

**★むしろ評価すべきは、②の件数を事前見積 17 から実測 19 へ自分で訂正した点である**（`cmd/tacpendium/main.go` の 2 件を取りこぼしていた、と自己申告している）。**⇒ ただし同じ数え直しが `migrations/` までは届かなかった（高-1）。**

### 8. コーディング規約・その他（**指摘なし**）

- **`console.log` / `fmt.Println` の混入**: 差分に無し。
- **`eslint-disable` / `nolint`**: 差分に無し。
- **`any` の混入**: 差分に無し（型定義は 1 行も触っていない）。
- **import 順**: `check-import-order.sh` を回して `EXIT=0`（99 ファイル / ベースライン 99・増加なし）。
- **JSON タグ / DTO の camelCase**: 対象外（DTO のフィールドは 1 つも変えていない）。
- **列挙定数の同期**: `check-enum-sync.sh` は `EXIT=0`（ベースラインどおり）。
- **マジックストリング**: 表示語 2 箇所は定数化していないが、**`retired-words.test.ts` が機械の網として代替している**ため妥当。i18n キーを新設しない判断（完了報告 §5-5）も `DES-005` §5.19 の明文に沿う。
- **セキュリティ**: 認証・権限・ストレージの取り扱いに変更なし。ストレージキーも無変更。
- **依存追加**: 0 件（`go.mod` / `package.json` に差分なし）。

---

## 推奨修正（優先度別）

### 高（M38 完了前に修正必須）

1. **`migrations/000033_create_move_commands.up.sql:10` の「取込ヘルパー」を処理する。** 直すか、直さないと決めて理由を記録するかのいずれか。**放置は不可**（2 度退けた語が一次コメントに残る）。**あわせて完了報告 §3-2 の「取込ヘルパー」突き合わせ表へ `migrations/` の行を足し、射程が `web/` `internal/` `cmd/` `docs/usermanual/` の 4 箱に切られていたことを明示すること**（同じ穴を次のサブが再現しないため）。
2. **説明書 `tacpendium-readme.html:983`（ch20）の「技のデータを直したいときは moves 編集グリッドへ。」を処理する。** ch14 と同じトーンへ書き換える／`#ch14` へリンクする／`followup` 候補として設計伝達レポート §4 へ出す、のいずれか。**いまメニューに無い画面へ利用者を送る文が、利用者向け成果物に残っている。**
3. **`docs/progress/progress-log.md` へ M38-02 の索引行を追記する。** 完了条件 §6-8 が未達であり、`check-progress-log-index.sh` が **EXIT=1** で赤い。**追記後に §8 の 4 検査を回し直し、完了報告 §8 の空欄を実測値で埋めること**（これが `D-890` / `M36-01` §7-1 の手順そのものである）。
4. **完了報告 §6-6 の派生資料の申し送りを `code-facts.md:501-502` の 2 行へ直す。** とくに `:502` が「消えた導線を `active` と主張している」ことを本文に書くこと。

### 中（M39 着手と並行可）

5. **設計伝達レポート §4 へ、`import-screen-has-two-names` の対象へ `IntakeHelperPage` を加える候補を書く。** `followup-backlog.md` は編集しない（`D-838`）。
6. **`Header.test.tsx` の 1 本目の題を実態へ合わせる**（「主要 6 リンク」へ改題、または `NAV_LINKS` 駆動へ作り替え）。現在 11 本中 6 本しか守っていない。
7. **`retired-words.test.ts` の走査根を広げるかどうかを、設計卓で決める。** 本サブが「走査根は `web/src` だけである」と明記して申し送ったのは正しい対応だが、**その結果として高-1 が実際に発生した。⇒ 申し送りだけでは 3 度目の同型が起きる。** 対象は `web/e2e` ／ `internal` ／ `cmd` ／ `migrations` ／ `docs/usermanual`。
8. **`web/e2e/combo-crud.spec.ts` のコメント変更が `M38-01` とマージで当たらないか、取り込み時に確認する。** 並列可のサブで同じファイルを触っている。

### 低（将来対応）

9. **`stripComments` の 2 つの弱点を、次に同ファイルを触る手番で見直す。** ①行末コメントの判定が `/\s\/\/.*$/` であり、**文字列リテラル中の `" // "` を含む行を途中で切る**（誤って「無い」と判定しうる）。②ブロックコメントの除去が非貪欲一括置換であり、**文字列リテラル中の `*/` で早期に閉じうる**。**いずれも現状の実データでは発火していない**（`pnpm test` が緑であることで確認済み）ため、**いま直す必要は無い。**
10. **`moves-edit.spec.ts` がヘッダのナビ内容まで見るようになった点。** 「導線の有無を見る spec を別に増やさないため」という注記どおりで妥当だが、**将来ナビが変わったときに moves 編集グリッドの spec が落ちる**。責務の混在として記憶しておくこと。

---

## 良かった点

**Claude Code へのフィードバックとして残す。**

1. **★★★指示書の前提が as-built と食い違っていたことを、実装前に実測で捕まえた。** 指示書 §0.1 は「設定画面から技編集への導線を外す」と書き、`DES-005` §3（`05-screen-design.md:131`）も「主要ナビゲーションには含めず『設定』配下へ」と定めていた。**実装はヘッダのメインナビにあった。⇒ 設計書どうしの矛盾ではなく設計書と実装の乖離であると切り分け、止めずに進めて CHANGE 原稿へ載せた判断は正確である**（`CLAUDE.md` §8 の「止める」対象は設計書*間*の矛盾であり、本件は該当しない）。**指示書 §2.1-2 の「★設定画面だけとは限らない」という先回りに、実査で正しく応えている。**
2. **★★★「表示語の正本は `ja.json` である」という指示を、実測で否定して報告した。** `ja.json` に intake 系のキーが 0 件であることを確かめ、正本が `Header.tsx` の `label` と h1 の 2 箇所であると結論している。**⇒ 指示書に書いてあるからそう書いた、で済ませていない。** これは `M32-03` §7-2 の教訓（`CHANGE` 通知書は表示語の正本ではない）を、**指示書自身にも適用した**形である。
3. **★★陰性対照を同じ手番で移した。** `retired-words.test.ts:154-156` の「採った語が 1 件以上当たる」を「引っ越し取込」から「他から引っ越し」へ移している。**移し忘れれば改名した瞬間に赤くなる。⇒ 移し忘れなければ、走査の空回りを検出する能力が保たれる。** この対照を手番の中で動かせた例は多くない。
4. **★★既存エントリの `useInstead` が「今も生きた語か」を確認して直した。** `取込ヘルパー` の `useInstead` が「引っ越し取込」のまま残れば、**禁則の代替語として退けた語を指す**という自己矛盾になる。**連鎖改名に固有の罠を自分で見つけている。** ファイル冒頭にその旨を注記して次の担当へ渡しているのも良い。
5. **★★「なぜ外したか」を 1 行ではなく 3 ファイルへ置き、その理由を説明した。** 指示書は「1 行」としか書いていない。**Header を読む人と `MovesEditGridPage` を読む人では知りたいことが逆である**という分析に基づいて置き場を決めており、**指示を超えた分の説明責任を果たしている**（完了報告 §5-3）。
6. **★★見積と実測の食い違いを 2 回、自分から訂正して報告した。** 改名の足し先 30 → 31、Go 側 17 → 19。**「ファイルへ全量落として数え直したから捕まった」と手段まで書いている。⇒ 次の担当が同じ手順を再現できる。**
7. **★★E2E の寄せ替えが no-op であったことを「やらなかった」ではなく「実測の結果 no-op であった」と書いた。** 4 本すべてが元から `page.goto` の直打ちであることを示し、**消した spec が 0 本であることを `git diff --diff-filter=A/D` で裏付けている**（`E-225` への正しい応答）。
8. **★★レビュー結果欄（§7）に予測を書かなかった。** `D-510` / `M23-04` の前例（レビュー前に「『高』指摘の不採用は 0 件」と断定した）を引いて、**5 欄すべてを「Phase C で埋める」としている。⇒ 断定を置かない規律が守られている。**
9. **★`exact: true` を「根拠が消えたから外す」ではなく「根拠を書き換えて残す」とした。** 緩めると「どれを押したか」が実行時まで分からない指定に戻る、という理由まで書いてある。**⇒ 失効したのは根拠であって、その設定の妥当性ではない、という切り分けができている。**
10. **★報告の「緑」が、レビュー側の再実行と一致した。** Go・フロント・常設検査・E2E 抜粋のすべてで報告どおりの結果が出た。**`D-890` が警戒する偽の緑は無い。**

---

## レビュー側で自分で回した検査（`D-890` / `M36-01` §7-1）

**★製造の報告を鵜呑みにせず、以下をすべてレビュー担当が実行した。出力は全量をファイルへ落とし `head` / `tail` で切っていない（`D-881`）。**

| 検査 | 実測 | 報告との一致 |
|---|---|---|
| `go test ./...` | **EXIT=0** ／ `grep -cE "^--- FAIL"` → **0** | 一致 |
| `cd web && pnpm test` | **EXIT=0** ／ **Test Files 234 passed (234)** ／ **Tests 2953 passed (2953)** | 一致 |
| `scripts/check-artifact-integrity.sh` | **EXIT=0**（検査 17 件 / 生成物 4 件すべて OK） | 一致 |
| `scripts/check-browser-storage-keys.sh` | **EXIT=0**（台帳 12 件 / 本番 11 件・ズレなし） | 一致 |
| `scripts/check-doc-refs.sh` | **EXIT=0**（37 ファイル・dead reference なし） | 一致 |
| `scripts/check-import-order.sh` | **EXIT=0**（99 / ベースライン 99・増加なし） | 一致 |
| `scripts/check-md-emphasis.sh`（全数） | **EXIT=0**（247 行 / ベースライン 247・増加なし） | 一致 |
| `scripts/check-md-emphasis.sh docs/progress/M38-02-completion-report.md` | **EXIT=0**（検出 0 行） | — |
| `scripts/check-stop-discipline.sh` | **EXIT=0** | 一致 |
| `scripts/check-enum-sync.sh` | **EXIT=0**（ベースラインどおり） | 一致 |
| `scripts/check-doc-inventory.sh` | **EXIT=0**（型に無いファイルなし） | 一致 |
| `scripts/check-derived-docs.sh` | **EXIT=0**（`code-facts` 13% / `docs-map` 5% が陳腐化疑い） | 報告 §6-6 と整合（**ただし挙げた行が 1 行足りない**＝中-3） |
| **`scripts/check-progress-log-index.sh`** | **EXIT=1** ／ `NG 作業 ID m38-02 が progress-log.md に現れない` | **報告は「Phase D で記載」として保留。⇒ 偽の緑ではないが未達** |
| `make e2e-only P="moves-edit"` | **EXIT=0** ／ 1 passed | 一致（**A-2 / A-5 を実物で確認**） |
| `make e2e-only P="m17-04-intake-helper"` | **EXIT=0** ／ 2 passed（`test.describe` の題が「他から引っ越し(M17-04)」で出る） | 一致 |
| `make e2e-only P="combo-crud"` | **EXIT=0** ／ 2 passed | 一致 |

> **★`make e2e` の全数は回していない。** 6 分かかること、および本サブが触った面（ヘッダのナビ ／ 技編集 ／ 他から引っ越し ／ コンボ CRUD）を抜粋 3 本 5 テストで直接押さえられたことによる。**製造側の全数実測は EXIT=0 / 351 passed / 1 flaky（`m31-02-tag-field-drag.spec.ts` の右ドラッグ文字選択・リトライで緑）であり、本サブはタグ欄にもドラッグ挙動にも触れていない。⇒ 因果を持たないという製造の判断に同意する。**

---

## 不明事項

1. **不明: `migrations/` 配下の適用済みマイグレーション本文（コメント行）を後から編集してよいかについて判断できない。** `golang-migrate` はチェックサムを持たないため機械的には壊れないが、運用方針としての可否を定めた記述を本レビューでは見つけられなかった。**⇒ 高-1 の修正方法（直す／直さないと決めて記録する）の選択は、開発者または設計卓の面である。**
2. **不明: 説明書 ch20 の誘導文（高-2）をどう書き換えるべきかについて判断できない。** 説明書の文面は開発者の面であり、ch14 で「URL を載せない」と決めた判断との整合も本レビューの権限外である。**⇒ 3 つの選択肢を挙げるにとどめた。**

---

## 制約事項

- 本レビューはコード上で判定可能な範囲のみ対象。実際の動作確認・パフォーマンス・実機テストは別途実施が必要。
- `make e2e` の全数は実行していない（上記の理由による）。本サブに関係する 3 spec / 5 テストのみレビュー側で再実行した。
- 実機のブラウザでの見た目（h1 の折り返し、ナビの幅の収まり）は確認していない。**とくにナビ項目は 12 本から 11 本へ減っており、`followup` の `global-nav-item-limit`（1280px での破綻）に対しては好転しているはずだが、実測していない。**
- `docs/handover/` 配下は索引（`docs-map.md`）経由で必要な節のみを参照した。全読込はしていない（実測 87 ファイル・約 3.0MB）。

---

*以上、M38-02 レビュー報告書。* **★★★本サブは「消しすぎ」と「直し漏れ」の両方で落ちる構造だった。⇒ 消しすぎは 0 件である。直し漏れは 1 件**（`migrations/000033` の「取込ヘルパー」）**あり、加えて説明書 ch20 の誘導と `progress-log` の索引行が未達である。**

---

## 取り込み結果（自動トリアージ）

> **★本節は製造（Phase C）が追記したものであり、レビュアーの記述ではない。**
> 手順は `implement_plan_full` の Phase C。**人間トリアージ承認を外す代償として、採否理由をここへ残して事後監査可能にしている。**

| # | 優先度 | 採否 | 対応 |
|---|---|---|---|
| 1 | **高** | **採用** | `migrations/000033_create_move_commands.up.sql:10` のコメントを「他から引っ越し」へ。**★スキーマ・データは変えていない。** 適用済みマイグレのコメント編集は `M29-01` の `SAゲージ` 統一に先例あり（`migrations/000020_...up.sql:2`）。完了報告 §3-2 へ `migrations/` 行と「射程を 4 箱に切ったため落ちた」旨を明記 |
| 2 | **高** | **採用** | `tacpendium-readme.html:983`（ch20）を ch14 と同じトーンへ。**`#ch14` への内部リンク ＋「この画面はいまメニューに出していません」**。★URL は載せない（ch14 と同じ扱い＝開発者裁定） |
| 3 | **高** | **採用** | `progress-log.md` へ索引行を追記（Phase D）。**★これは欠陥ではなく工程順である**——索引行の追記は Phase D であり、レビューは Phase B に走った。追記後に §8 の 4 検査を回し直して実測値で埋めた |
| 4 | **高** | **採用** | 完了報告 §6-6 を `code-facts.md:501-502` の **2 行**へ是正。**`:502` が消えた導線を `active` と主張している**旨を本文へ |
| 5 | 中 | **採用（送付）** | `import-screen-has-two-names` へ `IntakeHelperPage` を加える候補を設計伝達レポート §4 へ。**`followup-backlog.md` は 1 文字も編集しない**（`D-838`） |
| 6 | 中 | **採用** | `Header.test.tsx` 1 本目を**全 11 リンクの名指し ＋ 件数**へ作り替えた。**★題が実態を偽っていた＝本プロジェクトが「高」に較正する型**であり、かつ「技編集を外しても赤くならなかった」原因の一端でもある |
| 7 | 中 | **送付** | `retired-words.test.ts` の走査根拡大は**本サブの射程外**。`.sql` / `.html` のコメント構文を知らない `stripComments` の設計判断が要り、広げると経緯コメントまで赤くなる。⇒ 設計卓へ。**「申し送りだけでは 3 度目が起きる」という警告もそのまま伝える** |
| 8 | 中 | **送付** | `M38-01` とのマージ衝突確認は**本サブの手番では実施できない**（並列ブランチ・取り込み系 git は `deny`）。変更は 44 行目のコメント 5 行だけで解決は自明である旨を §4 へ |
| 9 | 低 | **不採用** | **レビュー自身が「現状の実データでは発火していない・いま直す必要は無い」と書いている。** 発火していない正規表現を投機的に直すと、直したこと自体が検証できない。次に同ファイルを触る手番へ委ねる |
| 10 | 低 | **不採用** | **意図した設計である。** 専用 spec を新設すると全数の実行時間が伸びる割に主張は 2 行。**将来ナビが変わったら落ちるのは望ましい**（技編集の導線が戻ったら知りたい）。理由は spec 内のコメントに明記済み |

### ★「高」指摘の不採用は **0 件**

**⇒ Phase C の安全弁（重大指摘の自動棄却時のみ開発者エスカレーション）は発動していない。**

### ★レビュアーの「不明」2 件への回答

| # | 不明事項 | 回答 |
|---|---|---|
| ① | 適用済みマイグレーション本文（コメント行）の後からの編集可否 | **★先例がある。** `M29-01` が `SAゲージ` の統一で**適用済みマイグレのコメントを直している**（実物＝`migrations/000020_add_gauge_consumed_columns.up.sql:2`。経緯は `retired-words.test.ts` の `SA ゲージ` エントリの `reason` に逐語）。**⇒ コメント行に限り可。** 本サブもスキーマ・データは 1 バイトも変えていない |
| ② | 説明書 ch20 の誘導文の書き換え方針 | **★新しい方針を作らず、既に開発者が選んだ ch14 の文面へ揃えた**（「いまメニューに出していない」と書き、URL は載せない）。⇒ 開発者の面へ踏み込んでいない |

### ★再レビュー往復: **0 回**（上限 2 回）

「高」4 件はいずれも**製造側が実測で裏を取ってから**修正しており、争点は残っていない。
