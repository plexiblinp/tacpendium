# M35-01 レビュー報告書

| 項目 | 内容 |
|------|------|
| 対象作業 | M35-01（タグまわりの是正 3 件・公開前） |
| 指示書 | `docs/instructions/M35-01-tag-fixes.md` v1.0.0 |
| チェックリスト | `docs/instructions/reviews/M35-01-review-checklist.md` v1.0.0 |
| 完了報告 | `docs/progress/M35-01-completion-report.md`（コミット `2e0dece`） |
| 着手基点 | `4c0dcb2` |
| 対象コミット | `6408514`（段 1）／ `d4a1fb2`（段 2）／ `2e0dece`（完了報告） |
| レビュー実施日 | 2026-09-09 |
| レビュー方法 | Read によるコード読取 ＋ 独立の再実測（`go test` 全数 ／ `pnpm test` 全数 ／ `make e2e-only` ／ 常設検査 4 本 ／ **スクラッチ複製上での破壊確認の追試** ） |

---

## 総評

3 段とも、指示書とチェックリストが警戒していた「落ちる形」を正面から回避できている。段 2 は 4 か所と両端の試験を 1 コミット（`d4a1fb2`）に収め、段 1 は二重ガードという指示書の前提外の事実を実測で見つけ、試験を 2 層に置き直した。完了報告 §1 の F-1〜F-4 は **4 件とも実物で裏が取れた**（当方でスクラッチ複製を作り、B-1 / B-2 / B-3 を独立に再現した。出力は報告の逐語と一致する）。

一方で、段 1 の試験対には **service 層のガードを固定するものが 1 本も無い**。当方の追試では、報告自身が「B-2＝両方緑」と記録したその状態で、他人の「使用中」タグへの `DELETE` が `409` ＋ `usageCount: 3` を返す。**他人のタグの存在と使用件数が漏れる形が、新しい試験 2 本とも緑のまま通る**。これは実装の欠陥ではなく試験の穴だが、本サブが「他人のデータに触れないことを誰も確かめていない」という理由で公開前に上げられた経緯からすると、閉じるべき穴である（実装を変えずテスト 1 本で閉じる）。

もう 1 件、段 2 が「失効した記述の是正」として `handler.go:34-35` の godoc を直しながら、**逐語で対になる `tagApi.ts:58` のコメントを旧表記のまま残している**。同じ手番で片方だけ直すのは、本サブの主題そのものである。

そのほかは完了報告の記述精度・走査・破壊確認とも高水準で、指摘の大半は完了前の仕上げ（設計伝達レポート・索引行・§7 の見出し）に属する。

---

## 設計準拠性レビュー結果

### 束 A — 段 1（破壊確認が本体）

| # | 判定 | 内容 |
|---|------|------|
| A-1 | ◎ | 試験の向きは正しい。`TestDelete_CannotTouchAnotherUsersTag` は user B が user 1 のタグへ `DELETE` を撃ち、(1) 204 でないこと (2) 404 であること (3) 行が残っていることの 3 点を主張する。repo 層の `TestDelete_IsScopedToUser` も `ErrNotFound` ＋ 行残存 ＋ 所有者からは消せる対照の 3 点。「自分のタグを消せる」だけを見る形にはなっていない |
| A-2 | ◎ | 破壊確認は実測されている。**当方でも独立に再現した**（スクラッチ複製 `/tmp/.../probe` に Go 側一式を複写して実行）。B-1＝`repository.go:207` の絞りを外すと repo 層が 3 assertion とも赤・API 層は緑のまま。B-3＝二重ガードを両方外すと API 層が赤。**完了報告 §5.1 に貼られた出力と、当方の出力は逐語で一致する** |
| A-3 | ◎ | `TestUpdate_CannotTouchAnotherUsersTag`（`scope_test.go:128`）と同じ形。`newScopedRouter` / `addUser` / `request` を再利用し、tag の取り方（`SELECT id FROM tags WHERE user_id = 1 LIMIT 1`）まで揃えている。むしろ 404 の明示検査ぶん厳しい |
| A-4 | ◎ | 実装は 1 行も変えていない。`git diff --numstat 4c0dcb2` で段 1 のコミット `6408514` は `+35 / -0` と `+36 / -0` のテスト 2 ファイルのみ |
| ★追加 | × | **service 層のガードを固定する試験が無い**（詳細は下記「高-1」） |

**★A-2 の補足（当方の追試で分かったこと）**。報告 §5.1 の B-2 行「service の `repo.Get` ガードを外す → 両方緑」は、単に冗長ガードの実証にとどまらない。`repo.CountUsage`（`repository.go:220`）は **tag リポジトリで唯一 `user_id` を取らないメソッド**であり、`service.DeleteTag` の前段 `repo.Get` だけがそこへ他人の tagID が届くのを止めている。追試の実測は次のとおり。

```
### 現行実装（ガード二重）
  OBSERVED status=404 body={"error":{"code":"not_found",...}}

### service.go:106 の repo.Get ガードだけを外した状態（＝報告の B-2）
  OBSERVED status=409 body={"error":{"code":"tag_in_use",...,"details":{"forceDeleteQuery":"?force=true","usageCount":3}}}
  かつ TestDelete_CannotTouchAnotherUsersTag は PASS（緑のまま）
```

新テストが緑のままなのは、テストが選ぶ tag（`LIMIT 1` で拾う migrations/000007 の seed）が **どのコンボにも使われていない** ため、409 経路へ入らないからである。⇒ 使用中のタグを対象にした assertion を 1 つ足せば B-2 が赤になる。

### 束 B — 段 2（本サブ最大の危険）

| # | 判定 | 内容 |
|---|------|------|
| B-1 | ◎ | 4 か所すべてが単一コミット `d4a1fb2` に入っている（`git show --stat d4a1fb2` で確認：`handler.go` / `types/tag.ts` / `tagApi.ts` ＋ 両端の試験 2 本 ＋ `TagManagementPage.tsx`）。分割の痕跡なし |
| B-2 | ◎ | 走査は完了報告 §5.3 に表で示され、当方の再走査とも一致する。**残存はすべて (a) DB 列別名 `COUNT(c.id) AS usage_count` (b) 旧キーを検出するための逐語 fixture (c) テスト名・コメント** であり、DTO キーとしての snake_case は本番コードから 0 件。**F-2 の訂正（`existing_tag_id` の grep は 0 件でなく `internal/model/api_error_test.go:57,74,75` の 3 件）も実物で確認した** |
| B-3 | △ | 「画面まで届く」は文字どおり未達。**F-3 は正しい**——`TagApiError.usageCount` の getter を読む本番コードは 0 件（当方の全走査でも呼び出し元なし）。`TagManagementPage.tsx:74` が一覧の `Tag.usageCount` から `force` を決めるため 409 経路へ入らず、`useTagManagement` に `onError` も無い。画面を変えずには達成できず、§3-1 がそれを禁じている。代替（BE の生 JSON と FE の逐語同一 fixture）は現実的で妥当だが、**同一性を担保しているのはコメントだけ**であり機械的な歯止めが無い（中-1） |
| B-4 | ◎ | `existing_tag_id` は `web/src/types/tag.ts` から消え、代わりの新設も無い。ハンドラは元から 1 か所も返していない |
| B-5 | ◎ | `force_delete_query` → `forceDeleteQuery` も直っている。取りこぼし無し。**あわせて当方で他ハンドラの `details` キーを全数確認した**——`aliasText` / `existingId` / `combos` / `validations` / `limit` / `field` はいずれも元から camelCase であり、tag だけが唯一の逸脱だった。本修正で API 全体が揃った |

### 束 C — 段 3（差分が無いので見落としやすい）

| # | 判定 | 内容 |
|---|------|------|
| C-1 | ◎ | 機序 3 段が完了報告 §4.1 に表で書かれている。参照行も実物と一致（`useTagFormDialog.ts:57-58` の `\|\| undefined` ／ `tagApi.ts:80` の `JSON.stringify` ／ `model/tag.go:67-71` の `*string` ＋ `repository.go:165-172` の動的 SET） |
| C-2 | ◎ | 「`null` を送っても救えない」理由が §4.2 にある。Go の `encoding/json` が省略と明示的 null を区別できないこと、したがってフロント単独では直らず契約の決めごとが要ることまで書かれている |
| C-3 | 未 | **設計伝達レポートが未発行**（`docs/handover/design-reports/` に M35 分が無い）。完了報告 §11-9 は「Phase D の後に実施する」と正直に書いており断定の誤りは無いが、**本サブの目的そのものが未達のまま終わるリスクがあるため完了前必須**（高-4） |
| C-4 | ◎ | `TagManagementPage.tsx` の `?? undefined` が no-op であることを確認済み。当方でも確認——引数型が既に `string \| undefined` であり、`??` が変換するのは `null` と `undefined` だけなので完全な no-op。外した上で「正規化を足すならフック側であり、ここではない」というコメント 4 行を残した判断は妥当。`pnpm test` 2623 件緑で挙動不変も裏付けられている |

### 束 D — 射程

| # | 判定 | 内容 |
|---|------|------|
| D-1 | ◎ | `category` / `color` を未設定へ戻せるようにする変更は無い。`model/tag.go` / `repository.go` の Update 経路は無改変 |
| D-2 | ○ | 画面ファイル `TagManagementPage.tsx` は触っているが、指示書 §2.3 が「消すかコメントを付けるかは判断してよい」と明示的に授権した範囲であり、挙動は不変（no-op の除去 ＋ コメント）。文言・DOM・i18n は無改変 |
| D-3 | ◎ | `CLAUDE.md` は無改変 |
| D-4 | ◎ | `docs/design/` は無改変（`git diff --stat 4c0dcb2` の対象 9 ファイルに含まれない） |
| D-5 | ◎ | 同乗候補 `tag-val-t01-check-then-act-race` は未着手。順序違反なし。触らない判断の根拠（`tagsvc.New` のシグネチャ変更 ＋ リポジトリ Tx 面の新設が要る）は実物で確認した——`service` 構造体は `repo` しか持たず、先例の combo / setup サービスは `*sql.DB` を持つ。**判断は妥当** |

### 束 E — 検査と報告

| # | 判定 | 内容 |
|---|------|------|
| E-1 | △ | 常設検査は完了報告 §7 が空のプレースホルダであり、Phase C の後に回す設計になっている（ファイルを足し終えてから回す＝指示書 §5-7 に沿う）。**ただし節見出しが「常設検査（★最後に回した）」と過去形で断定している**（高-3） |
| E-2 | ◎ | 「まだやっていないこと」を断定していない。§11 の 6 / 7 / 8 / 9 は「Phase D で追記」「§7 に記す」と明記し、§8 はレビュー報告書が出来るまで書けないことを HTML コメントで明示している。**`D-510` の再発は無い**（§7 の見出しを除く） |
| E-3 | ◎ | 当方でも `bash scripts/check-md-emphasis.sh docs/progress/M35-01-completion-report.md` を実行し **検出 0 行**。結果の §7 への記入は未了 |
| E-4 | ◎ | コミットは 3 本（段 1 ／ 段 2 ／ 完了報告）。段 3 はコミットを持たない。混入なし |

### 観点 1〜4（「緑でも壊れているもの」を探す）

| # | 判定 | 内容 |
|---|------|------|
| 1 | ◎ | 段 2 の試験は DTO と同じ手番で書いたが、**両側ともキー文字列そのものを見る形**になっている。Go 側は構造体へ decode せず生 `map[string]any` で読み、旧キー 3 種の不在も見て、さらに body 全体を golden JSON と `reflect.DeepEqual` する。FE 側は同一 literal を fixture にして getter の戻り値を見る。「両方を `usageCount` に揃えれば型は通る」形は回避できている |
| 2 | ○ | 段 1 の証明は破壊確認だけであるという点は理解され、行列で実測されている（当方でも再現）。**ただし行列の B-2 が「両方緑」で終わっており、その空白を埋める試験が足されていない**（高-1） |
| 3 | ◎ | 段 3 は差分ゼロだが、成果物である完了報告 §4 に機序・`null` 不可の理由・`?? undefined` の始末が書かれている |
| 4 | ◎ | `force_delete_query` の取りこぼしなし。走査は両キーとも示されている |

---

## 設計準拠性以外の指摘事項

### 1. 失効した記述が 1 か所残っている（`tagApi.ts:58`）

```ts
// web/src/features/tag/api/tagApi.ts:58
    // E-3: usage_count を選択キャラのコンボのみで集計する。
    if (params?.characterId !== undefined) {
```

本手番は `internal/api/tag/handler.go:34-35` の godoc を

```
-//   - character_id: 正の整数の場合、usage_count を当該キャラのコンボのみで集計(E-3、任意)
+//   - character_id: 正の整数の場合、usageCount を当該キャラのコンボのみで集計(E-3、任意)
```

と直し、完了報告 §2 でこれを「失効した記述の是正である。本サブの主題そのもの」と分類している。`tagApi.ts:58` は **その godoc と逐語で対になる同じ文**であり、同じ分類が当てはまる。完了報告 §5.3 は「DB 側の集計を指す」として触らない判断を示すが、**同一文の片割れだけを直す形**になっており、次に読む人は 2 つの流儀を比べることになる。API 応答フィールドは `usageCount`（`model/tag.go:56`）である。

### 2. `errors.As` の戻り値破棄（`handler.go:159`・申し送り #1）

```go
case errors.Is(err, tagsvc.ErrTagInUse):
    var inUse *tagsvc.TagInUseError
    errors.As(err, &inUse)          // ← 戻り値を捨てている
    return c.JSON(http.StatusConflict, model.NewAPIErrorWithDetails("tag_in_use", ...
        "usageCount": inUse.UsageCount,   // ← nil なら panic
```

当方でも到達可能性を確認した。`TagInUseError.Is(target)` が `target == ErrTagInUse` を返すため、**センチネル `ErrTagInUse` を裸で返す実装が 1 つでも増えると `errors.Is` は true・`errors.As` は false になり nil 参照で panic する**。現状は `service.DeleteTag` が必ず `*TagInUseError` を返すため起きない（`grep` で裸の `return ErrTagInUse` は 0 件）。開発者判断で「報告のみ」となっているが、**登録先が完了報告本文しか無い**（下記 3 と同じ問題）。

### 3. 申し送り #1 / #2 に恒久の置き場が無い

完了報告 §10 の 5 件のうち、#3 / #4 / #5 は「設計伝達レポート §4 へ」と宛先が書かれているが、**#1（`errors.As`）と #2（`useTagManagement` の `onError` 欠落）は「開発者判断で報告のみ」で止まっており宛先が無い**。`CLAUDE.md` §10.Y の原則（行として足せる先へ足す）に照らすと、`followup-backlog.md` §J（製造が直接書ける唯一の節）か、設計伝達レポート §4 の候補行に載せないと、完了報告の中で滞留する。特に #2 は F-3 の原因そのものであり、`usageCount` を画面へ出す判断と一体で扱われるべきものである。

### 4. FE テストのコメントが、実際に検出するものとずれている

`web/src/features/tag/api/tagApi.test.ts` の 2 本目のコメント：

```
// ★このテストが赤くなるのは「getter が旧キーを読んでいる」ときである。
// ⇒ BE だけ camelCase へ直して FE を直し忘れた状態を、こちら側から検出する。
```

前段は正しいが、後段の「BE だけ直して FE を直し忘れた状態」を捉えるのは **1 本目**（camelCase の body で `usageCount === 3` を主張する側）である。2 本目が固有に捉えるのは「getter が旧キーへフォールバックする形（両対応にしてしまう形）」であり、説明と役割が入れ替わっている。テスト自体は有効なので優先度は低いが、コメントは次の担当が読む。

### 5. import 順（`CLAUDE.md` §4）

`TagManagementPage.tsx` は本手番で編集した対象だが、`react-i18next` が `react` より前に来る既存違反を持つファイルである（`bash scripts/check-import-order.sh --list` に出る）。同スクリプトの方針は「既存分を機械的に並べ替えない。書いた本人が触るときに直していく」であり、触った手番で直せば 1 件減った。副作用 import ではないため安全に動かせる。

なお同検査は現在 `現在 100 ファイル / ベースライン 101 ファイル` を報告し「BASELINE を 100 へ下げること」と出す。**このドリフトは本手番に起因しない**（変更 9 ファイルのうち import 行を動かしたものは無く、新規の `tagApi.test.ts` は規約準拠）。ベースライン更新はスクリプト編集にあたるため本サブの射程外だが、次に触る手番への申し送りに値する。

### 6. golden JSON が日本語メッセージ全文を固定している

`handler_test.go` の `wantBody` と `tagApi.test.ts` の `IN_USE_409_BODY` は、キーだけでなく `"このタグは使用中です。確認の上削除してください"` を含む body 全体を固定する。**文言を 1 文字変えると Go 側が赤くなり、FE 側の fixture も同時に直さないと実物とずれる**（FE 側はキーしか見ないので緑のまま古くなる）。今回はそれが狙いでもあるが、i18n 化・文言修正の手番でここに当たることは予見できるので、コメントに「文言を変えるときは 2 ファイル」と足しておくと安い。

### 7. 参照行番号の軽微なずれ（完了報告 §6）

`_txlock=immediate` の所在を `internal/infra/db/db.go:43-70` としているが、実体の定数は `:78`（`const dsnTxLockParam = "_txlock=immediate"`）、解説コメントが `:58`。結論（`BeginTx` が `BEGIN IMMEDIATE` になる）は正しい。他の参照行（`service.go:106` / `repository.go:207` / `repository.go:115` / `handler.go:163-164` / `useTagFormDialog.ts:57-58` / `model/tag.go:56` / `tagApi.ts:80` / `repository.go:165-172` / `scope_test.go:126`）はすべて実物と一致していた。

### 8. E2E の観測（M35-01 起因ではない）

当方で `make e2e-only P="tag"` を実行したところ **11 passed / 2 flaky（40.2s）**。flaky は `m31-02-tag-field-drag.spec.ts` の「右へ／下へ枠外までドラッグしても文字選択が消えない」の 2 件で、1 回目に `selectionLength` が 0 になり、リトライで pass する。完了報告 §5.4 は同種の spec を「13 件 緑」と記録しており矛盾はしない（Playwright は flaky を最終的に緑として扱う）が、**M35-01 とは無関係の既存 spec の不安定さ**として観測しておく価値がある。

### セキュリティ・規約の一般点

- ブラウザストレージ API の使用なし（`check-browser-storage-keys.sh` 違反なし）
- 新規依存 0 件（`go.mod` / `package.json` 無改変）
- `console.log` / `fmt.Println` の混入なし、`eslint-disable` / `nolint` の追加なし
- エラー wrap（`%w`）・`context.Context` 第一引数・godoc の規約はいずれも維持
- 当方で実行した検査：`go test ./...` 全緑 ／ `pnpm test` 223 ファイル 2623 件緑 ／ `check-artifact-integrity.sh` 違反なし（自己検査 15 件） ／ `check-import-order.sh` 違反なし ／ `check-enum-sync.sh` 増加なし ／ `check-browser-storage-keys.sh` 違反なし ／ `check-md-emphasis.sh`（完了報告）0 行 ／ `check-progress-log-index.sh` **NG 1 件（下記 中-3）**

---

## 推奨修正（優先度別）

### 高（M35 完了前に修正必須）

- **高-1: 段 1 の試験対に、service 層ガードを固定する assertion を 1 つ足す。**
  現状の 2 本は `repository.Delete` の絞りしか押さえておらず、`service.go:106` の `repo.Get` を外しても両方緑になる（報告 §5.1 の B-2 行）。当方の追試では、その状態で **他人の「使用中」タグへの `DELETE` が `409` ＋ `usageCount: 3` を返し、他人のタグの存在と使用件数が漏れる**。原因は `repo.CountUsage` が tag リポジトリで唯一 `user_id` を取らないメソッドであることで、前段の `repo.Get` だけがそれを止めている。
  対処案（実装は変えない・テストのみ）: `scope_test.go` に「user 1 の**使用中**タグ」へ user B から `DELETE` を撃ち、`404` であること（`409` でないこと・body に `usageCount` を含まないこと）を主張するケースを足す。既存の `insertCombo` / `insertComboTag` 相当を api 層に用意するか、`repository_test.go` 側に `CountUsage` 経路の対照を置く。これで B-2 が赤になり、行列の空白が埋まる。
  ★本サブの登録理由（他人のデータに触れられるか・被害が不可逆・公開前）に照らすと、この穴は残して次へ渡すべきものではない。

- **高-2: `web/src/features/tag/api/tagApi.ts:58` のコメントの `usage_count` を `usageCount` にする。**
  本手番が「失効した記述の是正」として直した `handler.go:34-35` の godoc と逐語で対になる同じ文である。片方だけ直した状態で残すと、次の担当がどちらを規範と読むか分かれる（優先度較正: 撤回済み・失効した記述はテストでも lint でも検出できず、後任が本文を複製する）。

- **高-3: 完了報告 §7 の見出し「常設検査（★最後に回した）」を、未実施が分かる文言へ直す。**
  本文は空のプレースホルダであり、見出しだけが過去形で断定している。チェックリスト E-2 が挙げる `D-510`（`M30-01` が未追記の索引行を「追記した」と書いた）と同型で、当時この指摘は「高」で扱われた。`## 7. 常設検査（★Phase C の後に、すべてのファイルを足し終えてから回す）` 等へ。実測値を埋める段でこの但し書きを消せばよい。

- **高-4: 設計伝達レポートを出し、§4 に段 3 の CHANGE 請求を書く（完了条件 9・チェックリスト C-3）。**
  未発行。請求が無いと「カテゴリ・色は未設定へ戻せない」が設計書に書かれないまま終わり、本サブの目的が未達になる。あわせて §4 へ載せるべきものが 3 件ある——F-2 の訂正（`existing_tag_id` の grep は 0 件でなく 3 件。指示書 §2.2-4 と followup §CC の根拠記述が誤り）／ F-3 の事実（`usageCount` は画面まで届いていない・着手前からの状態）／ §2.4 を触らなかった実測コスト。

### 中（M36 着手と並行可）

- **中-1: 両端 fixture の同一性に機械的な歯止めを足す。**
  現在 `handler_test.go` の `wantBody` と `tagApi.test.ts` の `IN_USE_409_BODY` はコメントでしか結ばれていない。安い順に (a) 両ファイルのコメントへ相互の識別子を明記（済）＋ 文言変更時の手順を 1 行 (b) Go 側テストが JSON を `testdata/tag_in_use_409.json` へ書き、FE テストがそれを読む形にする——(b) は本サブの射程を越えるので次の手番で判断すればよい。
- **中-2: 完了報告 §10 の申し送り #1（`errors.As` の戻り値破棄）と #2（`useTagManagement` の `onError` 欠落）に置き場を与える。**
  「報告のみ」の裁定は尊重するが、宛先が無いと完了報告の中で滞留する。`followup-backlog.md` §J へ 5 フィールドで置くか、設計伝達レポート §4 の候補行に載せる。#2 は F-3 の原因そのものであり、`usageCount` を画面へ出すか否かの判断と一体である。
- **中-3: `docs/progress/progress-log.md` へ索引行を 1 行足す（完了条件 7・Phase D）。**
  現時点で `bash scripts/check-progress-log-index.sh` が `NG 作業 ID m35-01 が docs/progress/progress-log.md に現れない` を返す。完了報告 §9-6 / §11-7 が「Phase D で追記する」と明記しており断定の誤りは無いので、予定どおり実施すればよい。
- **中-4: `tagApi.test.ts` の 2 本目のコメントを、実際に検出する対象へ合わせる。**
  「BE だけ直して FE を直し忘れた状態」を捉えるのは 1 本目である。2 本目が固有に捉えるのは「getter が旧キーへフォールバックする形」。

### 低（将来対応）

- **低-1**: `TagManagementPage.tsx` の import 順（`react-i18next` → `react`）を、触った手番のうちに直す。`check-import-order.sh` の違反ファイルが 1 件減る。
- **低-2**: `check-import-order.sh` のベースラインが 101、実測が 100 でドリフトしている（本手番起因ではない）。スクリプト側の `BASELINE` 更新は別手番で。
- **低-3**: golden JSON が日本語メッセージ全文を固定しているため、文言変更時に 2 ファイルの同時修正が要る旨をコメントへ。
- **低-4**: `TagDeleteConfirmDialog.test.tsx:22,37` のテスト名 `usage_count=0` / `usage_count > 0` は概念の略称であり、触らないという完了報告 §5.3 の判断に同意する。将来まとめて直すときの候補に留める。
- **低-5**: 完了報告 §6 の `internal/infra/db/db.go:43-70` は、実体の定数が `:78` にあるため範囲がわずかにずれている。
- **低-6**: `m31-02-tag-field-drag.spec.ts` の 2 件が当方の実行で flaky。M35-01 とは無関係だが、ドラッグ選択 spec の不安定さとして記録に値する。

---

## 良かった点

- **指示書の前提が外れていることを、着手前の実査で 4 件とも掴んだ。** 特に F-1（Delete のガードが二重であること）は、指示書どおりに破壊確認をしていたら「緑のまま」で通ってしまう形であり、チェックリスト §6-2 がまさに警戒していた落とし穴だった。**Update と Delete でサービス層の形が違う**（`UpdateTag` は `Get` を通さない）という差分まで特定して、既存 Update 試験が 1 本で足りている理由も説明している。
- **破壊確認を行列で採り、出力を逐語で貼った。** 当方が独立に再現したところ、B-1 / B-3 の失敗メッセージは報告の記載と 1 文字違わず一致した。「回した」ではなく「何が赤くなったか」を残す形になっている。
- **段 2 のテストが型検査の抜け穴を正面から塞いでいる。** 構造体 decode ではなく生 `map` で読み、旧キー 3 種の不在も見て、body 全体を golden JSON で突き合わせる。FE 側も同一 literal を持つ。「両側同時に変えれば型は通る」という本件固有の危険への対処として的確。
- **F-3 を「達成できない」と正直に書き、代替を提示した。** 完了条件を満たしたように見せる書き方（getter を叩くテストを「画面まで届く試験」と称する等）を採らず、画面を変えない制約と両立する最善を選んでいる。
- **走査の残存を「なぜ残ってよいか」まで分類した**（DB 列別名 ／ 旧キー検出用の逐語 ／ テスト文言）。0 件と言い切らず、残存を数えて種別で説明する形は再現性が高い。
- **`?? undefined` を外すだけで終わらせず、「正規化を足すならフック側であり、ここではない」を残した。** 消した理由ではなく、次に同じ場所を触る人が必要とする情報が書かれている。
- **同乗候補を触らない判断を、実測コスト（`tagsvc.New` のシグネチャ変更 ＋ リポジトリ Tx 面の新設 ＋ 呼び出し 3 か所）で示した。** 「時間が無かった」ではなく形が違うことを示しており、フェーズ5 の見積りにそのまま使える。さらに安い代替案（UNIQUE 違反の翻訳で 409 契約違反だけ消す・約 10 行）まで出して判断を返している。
- **完了報告に「まだやっていないこと」の断定が無い。** §8 を HTML コメント付きのプレースホルダにし、§11 の未了行を明示している（`D-510` の再発なし）。§3 に `--numstat` を貼って `E-225`（新規ファイルの deletions）を自分で確認している点も良い。

---

## 制約事項

- 本レビューはコード上で判定可能な範囲のみ対象。実際の動作確認・パフォーマンス・実機テストは別途実施が必要。
- 破壊確認の追試は、リポジトリを一切変更せずスクラッチ領域へ Go ソースを複写して実施した（本リポジトリの作業ツリーは `git status` で clean を確認済み）。フロント側の破壊確認（完了報告 §5.2 の C-1 / C-2）は再実行しておらず、テスト本体の assertion から論理的に確認したにとどまる。
- 常設検査のうち `check-doc-refs.sh` / `check-stop-discipline.sh` / `check-instruction-format.sh` / `check-doc-inventory.sh` / `check-derived-docs.sh` は本レビューでは回していない。完了報告 §7 の実測（Phase C の後）で確認されたい。
- **不明: 2026-09-09 の開発者判断 3 件**（§2.4 を触らない ／ 申し送り #1 と #2 を報告のみとする）**が、リポジトリ上では完了報告 §6・§10 にしか存在しない。** 盤面（`parallel-board.md`）や followup へ写されるかは本レビューの位置からは確認できない。設計伝達レポート §4 で写されることを想定するが、写されない場合は完了報告本文が唯一の記録になる。

---

## 取り込み結果（自動トリアージ）

`/implement_plan_full` Phase C。**トリアージ日: 2026-09-09。再レビュー往復回数: 0 回**（初回レビューのみ。上限 2 回に達していない）。

| 優先度 | 件数 | 採用 | 不採用 |
|---|---|---|---|
| **高** | **4** | **4** | **0** |
| 中 | 4 | 4（うち 1 件は部分採用） | 0 |
| 低 | 6 | 3 | 3 |
| **計** | **14** | **11** | **3** |

**★★「高」指摘の不採用は 0 件である。** ⇒ 安全弁（高を不採用にする場合のみ開発者へエスカレーション）は発動していない。

### 採否と理由

| # | 採否 | 理由・対応 | 反映先 |
|---|---|---|---|
| **高-1** | **採用** | **本サブの登録理由そのものに当たる穴であった。** `TestDelete_DoesNotLeakAnotherUsersTagUsage` を `scope_test.go` へ追加し、対象タグを `combo_tags` で「使用中」にした上で、他人から `DELETE` を撃つと 404 であり応答に `usageCount` が含まれないことを主張する。**破壊確認 B-2 が赤になることを再実測した。★実装は 1 行も変えていない** | `520f88b` ／ 完了報告 §2・§5.1 |
| **高-2** | **採用** | **指摘のとおり。** `tagApi.ts:58` は本サブが直した `handler.go:34-35` の godoc と逐語で対になる同じ文であり、片方だけ直した状態になっていた。**⇒ 失効した記述の是正という本サブの主題に照らして、最も避けるべき形である** | `520f88b` ／ 完了報告 §5.3 |
| **高-3** | **採用** | 見出しを「常設検査（★Phase C の後に、すべてのファイルを足し終えてから回す）」へ改め、実測後に本節を埋めた。**`D-510` と同型という指摘は正しい** | 完了報告 §7 |
| **高-4** | **採用** | 完了報告のコミット後に `/design_handover_report` を実行する。§4 へ載せるもの 4 件（段 3 の CHANGE 請求 ／ F-2 の訂正 ／ F-3 の事実 ／ §2.4 の実測コスト）はレビューの指摘どおり。**★あわせて中-2 の 2 件も §4 の候補行へ載せる** | 完了報告 §11-9 |
| 中-1 | **部分採用** | (a) 双方の fixture コメントへ「文言を変えるときは 2 ファイルである」を明記した。**(b) の `testdata/*.json` 共有は本サブの射程を越えるため見送り**——レビュー自身も「次の手番で判断すればよい」としている | `520f88b` ／ 完了報告 §10-8 |
| 中-2 | **採用** | 完了報告 §10 の #1 / #2 へ**宛先＝設計伝達レポート §4 の候補行**を明記した。**★`followup-backlog.md` §J ではなく §4 を選んだ理由**——§J は停止時記録であり、本件は停止に伴う未解消項目ではない。`CLAUDE.md` §10.Y が「本表への登録は設計伝達レポート §4 へ候補を書き、設計卓が畳む」としている | 完了報告 §10 |
| 中-3 | **採用** | `progress-log.md` へ M35-01 の索引行を追記した（横断課題 7 件を含む）。`check-progress-log-index.sh` 緑で確認 | `progress-log.md` |
| 中-4 | **採用** | **指摘のとおり。** 2 本目が固有に捉えるのは「getter が旧キーも読む形」であり、「BE だけ直して FE を直し忘れた状態」を捉えるのは 1 本目である。コメントを実態へ合わせた | `520f88b` |
| 低-1 | **採用** | `TagManagementPage.tsx` の `react-i18next` が `react` より前に来ていた。副作用 import ではないため安全に動かせる。`check-import-order.sh` の実測が 100 → 99 へ減った | `520f88b` |
| 低-2 | **不採用** | **スクリプトの `BASELINE` 編集は本サブの射程外である**（レビューも同判断）。**★本サブの是正でドリフトはむしろ 1 件広がった**（ベースライン 101 / 実測 99）。次に触る手番へ申し送った | 完了報告 §10-6 |
| 低-3 | **採用** | 中-1 (a) と同じ変更で満たした | `520f88b` |
| 低-4 | **不採用（対応不要）** | **レビュー自身が完了報告の判断に同意している。** `TagDeleteConfirmDialog.test.tsx` のテスト名は概念の略称であり、失効した記述ではない | ——|
| 低-5 | **採用** | 「定数は `internal/infra/db/db.go:78`、解説コメントは同 `:43-70`」へ改めた | 完了報告 §6 |
| 低-6 | **不採用（記録のみ）** | **M35-01 とは無関係の既存 spec である**（本サブは対象コードを触っていない）。記録として残した | 完了報告 §10-7 ／ `progress-log.md` 横断課題 6 |

### ★レビューへの応答（1 件）

**制約事項の「不明」について** ——2026-09-09 の開発者判断 3 件（§2.4 を触らない ／ 申し送り #1 と #2 を報告のみとする）が完了報告にしか存在しない、という指摘は正しい。**⇒ 設計伝達レポート §4 へ写す**（高-4 / 中-2 の対応）。**★`parallel-board.md` と `followup-backlog.md` 本表への反映は設計卓の手番である**（`D-382` により製造が直接書けるのは §J のみ）。
