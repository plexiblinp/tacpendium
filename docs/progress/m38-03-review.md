# M38-03 レビュー報告書

| 項目 | 内容 |
|------|------|
| 作業 ID | M38-03（説明書へ画像を貼り込み、macOS の名前を寄せ、検査を表へ載せる） |
| 指示書 | `docs/instructions/M38-03-manual-images-and-macos-naming.md` v1.1.0 |
| チェックリスト | `docs/instructions/reviews/M38-03-review-checklist.md` v1.1.0 |
| 完了報告 | `docs/progress/M38-03-completion-report.md`（Phase A 時点） |
| 着手基点 | `654f4a85` |
| 対象コミット | 射程 A `dcaceda7` ／ 射程 B `3f81eb68` ／ 射程 C `b7db3573` ／ 完了報告 `285688bb` |
| レビュー日 | 2026-09-19 |
| レビュー方式 | サブエージェント分離。コード変更なし。読み取り系の検査のみ実行 |

---

## 総評

3 つの射程はいずれも指示書と開発者裁定（完了報告 §0）の範囲内で着地しており、本サブ固有の不合格 5 点（チェックリスト §0）はすべて回避されている。射程 A は母集団を現物から数えており（着手基点で本編 27 ＋ QS 1 ＝ 28 を実測で再確認）、`img src` 29 件と `images/` の PNG 29 枚は `diff` で 1:1、`todo` 0 件、HTML のタグ対応にも崩れが無い。射程 B は `GOOS=darwin` 等のツールチェーン値に触れず、正本 1 行の改名と、それに追随できなかった自己検査を「正本から引く」形へ直した点が良い（着手基点の版が新正本に対して赤になることも再現した）。射程 C の 1 行は実装から書かれている。

指摘は「失効した記述の残り」が中心である。本サブの成果で `README.md:218` の「スクリーンショットは未配置」が偽になったのに残っており、同じ行と `:11` の「全 22 章」も `M38-02` の 4 面追随の外に残った第 5 面である。また本サブが書き換えた `nightly-crossbuild.yml` の同じコメントブロックに、失効した行番号参照（`README.txt L68-94`）が残った。いずれも 1〜2 行の是正である。

---

## 設計準拠性レビュー結果

凡例: ◎ 完全準拠 ／ ○ 準拠（軽微な注記あり） ／ △ 未了・条件付き ／ × 不合格

### §0 本サブ固有の不合格 5 点

| # | 項目 | 判定 | 根拠（レビュー担当の実測） |
|---|---|---|---|
| 0-1 | 枚数を現物から引いたか | ◎ | `git show 654f4a85:` で本編 `<img` 27 ／ QS 1、`figure … todo` 27 ／ 1 を再計数。報告 §2.1 の 28 と一致。`M32-03` の 29（ch14 込み）を引いていない |
| 0-2 | `src` が空を指したまま `todo` が外れていないか | ◎ | `grep -o 'src="images/…"'` 29 件と `ls images/` 29 枚を `sort` して `diff` ＝ 差分なし。`todo` は両ファイルとも要素 0 件（CSS 定義のみ残る） |
| 0-3 | 画像を Claude 側で作っていないか | ◎ | `git diff --stat 654f4a85 HEAD -- docs/usermanual/images/` に PNG 0 件。29 枚はすべて `654f4a85`（開発者コミット）で追加 |
| 0-4 | `macos` / `darwin` を機械的に全置換していないか | ◎ | `Makefile:74` `GOOS=darwin` ／ `build-darwin` ／ `dist/tacpendium-darwin-arm64` ／ `release-targets.sh` の 2〜4 列目はすべて不変。`macos-arm64` の残りは `docs/design/02-architecture.md:1503` の 1 件のみ（設計書本体＝原稿対応） |
| 0-5 | `CLAUDE.md` の行を実装から書いたか | ◎ | `check_archive` (1)〜(5)・`self_test` の対照数・冒頭「限界」と突き合わせて一致（細部の 2 点は 中-1） |

### §A 射程 A — 説明書の画像貼り込み（`dcaceda7`）

| # | 項目 | 判定 | 根拠 |
|---|---|---|---|
| A-1 | 段 1-1 の `img` 全数が件数で報告に在るか | ◎ | 報告 §2.1「28（本編 27 ＋ QS 1）」。着手基点で再計数し一致 |
| A-2 | `src` の指す先が全数実在・突き合わせ | ◎ | 29 / 29・`diff` 差分なし（上記 0-2）。配布物 `dist/release/tacpendium-darwin-arm64.tar.gz` の `manual/images/*.png` も 29 |
| A-3 | `figure` の `todo` が 0 件 | ◎ | 要素側 0 件。CSS の `.todo` 定義（本編 `:107-110` ／ QS `:46-48`）は残るが要素に使用者なし（低-2） |
| A-4 | 外したのが `todo` クラスだけか | ◎ | `html.parser` で本編 `figure` 28 ／ `img` 28 ／ `figcaption` 28、QS 1 ／ 1 ／ 1。タグ対応の崩れ 0。`alt` は 29 件すべて非空・既存分は不変。`figcaption` は制作注記「（撮影は開発者の手番。ファイル名は …）」だけを外し本文を残している（28 件を diff で全数確認） |
| A-5 | 画像を作っていないか | ◎ | 上記 0-3 |
| A-6 | 本文を書き換えていないか | ○ | 書き換えは ch08 `:477` と QS `:88` の 2 文（開発者裁定 §0.2-4）と、ch18 入口の図の追加（§0.2-2）と、注記 28 件の除去（§0.2-5）に限られる。2 文の内容は実装と整合＝`web/src/features/combo/schema.ts:139-149`（published は `damage` / `knockdownAdvantage` のみ `required`、draft は無し）／ `internal/service/validation/combo.go:442-449` 同 2 欄／ `ja.json:265` の表示名「有利フレーム」 |
| A-7 | 「現在準備中です」0 件 | ◎ | 両ファイル 0 件 |
| A-8 | QS 側も母集団に入っているか | ◎ | `qs-first-combo-1.png` を含めて 29 |
| A-9 | 章数表記「全 21 章」を触っていないか | ◎ | 本編 `:191` ／ `dist-readme.txt:8,223` ／ `README.txt:8,223` 不変。ただし `README.md:11,218` は「全 22 章」のまま（`M38-02` の 4 面の外＝高-1 で扱う。本サブが触っていないこと自体は正しい） |

**射程 A の追加確認**

- 章番号の追従（追加指示②）: `section id` は `ch01`〜`ch21` の連番 21 個、TOC の `href` 21 件と 1:1。`ch22` 以上・旧 `ch15`〜`ch22` の参照は `docs/usermanual/` に 0 件。CSS セレクタ `figure img[src*="ch21-"]` は `ch21-home-1.png` だけに当たる。`.gitkeep` の `01〜22`→`01〜21`、横長表の例外から旧 `ch14` の除去、スマホ専用章 `ch22`→`ch21` ×2 も追従済み。
- QS の「第 N 章」リンク: `#ch01` 初回起動ウィザード ／ `#ch05` コンボ一覧 ／ `#ch08` コンボ登録・編集 ／ `#ch10` コンボ比較。いずれも改番の影響範囲外（ch14 以降のみ動いた）で、本編の `<h2>` と一致。
- 追加指示①「始動技」: `grep -n 始動技` で本編 8 行 ／ QS 1 行 ／ dist-readme 0 行を自分でも全量確認。編集画面に関わるのは `:485`「1 つめに置いた技が始動技になります（始動技を選ぶ欄はありません）」と `:494` の同一性キー列挙のみ。`M38-01` 追補で消えたのは読み取り専用の表示（`M38-01` 報告 §10-2）であり「選ぶ欄」は元から無い。「食い違い無し」の判定は妥当。
- ch18 の入口の図: `ch18-game-update-0.png` を実際に開いた。コンボ一覧上部の帯「ゲームの更新後に確認していないコンボがあります」と「更新未確認のコンボ(1 件・全キャラ)」ボタンが写っており、`alt` ／ `figcaption` と一致。本文「ホーム（スマートフォン）とコンボ一覧（PC）の両方にお知らせの帯とボタンが出ます。そこから開きます」を裏付ける位置（入口 → 一覧の順）で、他章と同じく本文末尾に置かれている。適切。

### §B 射程 B — macOS の名前寄せ（`3f81eb68`）

| # | 項目 | 判定 | 根拠 |
|---|---|---|---|
| B-1 | (a)＝アーカイブ名を `darwin` 側へ、か | ◎ | `release-targets.sh:34` の 1 列目のみ `tacpendium-darwin-arm64.tar.gz` へ。2〜4 列目は不変。(b) ではない |
| B-2 | 正本 1 か所で済んでいるか | ○ | 正本は 1 か所。自己検査は `_first_target_field` で正本から引く形になり、ハードコードが消えた。ただし `dist-readme.txt:47`（`.sha256` 名）は利用者案内として避けられない 2 か所目であり、機械検査で正本と結ばれていない（中-2） |
| B-3 | 段 1-4 / 1-5 の件数 | ◎ | 着手基点で `release-targets.sh` 6 行、4 面 48 件（scripts 14 ／ Makefile 7 ／ .github 9 ／ docs/design 18）を再計数し一致 |
| B-4 | 全置換していないか | ◎ | 報告 §3.1 の判定表どおり。ツールチェーン値・OS 名の一般用法は不変（上記 0-4） |
| B-5 | `check-release-archive.sh` が緑か | ◎ | `--self-test` EXIT=0（陰性 5 ／ 陽性 6）。単体 EXIT=0・違反なし（3 本）。加えて着手基点の版を新正本に対して回し `NG 陰性対照 B … (期待 0 / 実際 1)`・EXIT=1 を再現＝自己検査の改修が必要だったことの実証 |
| B-6 | `make release-archives` の出力が報告に在り `darwin` 側か | ◎ | 報告 §3.3 に貼付。`dist/release/` に `tacpendium-darwin-arm64.tar.gz`（＋`.sha256`）が実在し、`README.txt:47` の案内名と一致 |
| B-7 | amd64 の食い違いを触っていないか | ◎ | `git diff 654f4a85 HEAD -- docs/design/` 空 |
| B-8 | `DES-002` 本体を書き換えていないか | ◎ | 同上。原稿は設計伝達レポート（Phase D）へ |

**射程 B のシェル健全性**（`check-release-archive.sh:279-294`）

- `_first_target_field` は `self_test` 内の入れ子関数で、既存の `_stage` 等と同じ流儀。`"${RELEASE_TARGETS[@]}"` の展開は `check_archive` / `main` と同じ形であり、`set -u` 下でも新たな露出は無い（配列が空なら `return 1` → `ERROR … return 2` → `exit 2`）。
- `local wz wb mt mb` を宣言と代入で分けているため、`$(…)` の終了コードが `local` に隠れない。`A && B || C` の連鎖は A・B のどちらが落ちても C に入る。良い。
- 形式ごとの「先頭の定義」を使う設計なので、配列の並び替えや targz の追加に対しても自己検査が追随する。

### §C 射程 C — `CLAUDE.md` §8 の表（`b7db3573`）

| # | 項目 | 判定 | 根拠 |
|---|---|---|---|
| C-1 | 足したのが 1 行か | ◎ | `git diff --stat` ＝ `CLAUDE.md | 1 +`。`:260` の 1 行のみ。他の行は不変 |
| C-2 | 「何を見るか」が実装から書かれているか | ○ | (1) 実行ファイル ／ (2) `README.txt` ／ (3) 本文 html サイズ非 0 ／ (4) `manual/images/` ディレクトリのみ ／ (4b) 追加同梱物パス保持 ／ (5) `.sha256` 在るときだけ ／ 陽性 6・陰性 5 ／ 「限界」の 2 点。すべて `check_archive` と `self_test` の実装に対応する。細部 2 点が実装とずれる（中-1） |
| C-3 | `check-doc-refs.sh` が緑か | ◎ | EXIT=0・dead reference なし（再実行） |

### §D 共通

| # | 項目 | 判定 | 根拠 |
|---|---|---|---|
| D-1 | `dist-readme.txt` を触っていないか | ○ | `:47` の 1 行を開発者裁定（§0.2-3）で改名し、`README.txt` を `--write` で再生成。`check-dist-readme.sh` EXIT=0（再実行）。改名で存在しないファイル名を案内する状態を避けるための最小変更であり妥当 |
| D-2 | `docs/design/` ／ `followup-backlog.md` の差分 0 | ◎ | `git diff --stat` 空。`migrations/` ／ `web/src` ／ `internal/` も 0 |
| D-3 | マイグレ 0 本 | ◎ | 同上 |
| D-4 | `M38-01` / `M38-02` の面を触っていないか | ○ | `web/src` ／ `internal/` は不変。`tacpendium-readme.html` は `M38-02` の面だが、章 id の詰め直しは開発者裁定（§0.2-1・追加指示②）によるもので、`M38-02` 報告 §10-2「詰め直していない」が失効することは報告 §8-1 に記録されている |

### §E 報告

| # | 項目 | 判定 | 根拠 |
|---|---|---|---|
| E-1 | 射程 A ／ B ／ C に分かれているか | ◎ | §2 ／ §3 ／ §4 |
| E-2 | 完了報告 ＋ `progress-log` の索引行 | △ | 報告は在る（新規 312 行・deletions 0＝`E-225` の観点で健全）。索引行は未追記＝`check-progress-log-index.sh` が `m38-03` で NG（EXIT=1）。Phase D で足す手順どおりであり、この時点の赤は正常。なお同検査は `m19-04` でも NG を出すが着手前からの既存 |
| E-3 | CHANGE 原稿が設計伝達レポート §1 / §6 に在るか | △ | `docs/handover/design-reports/` に `m38-03` は未作成（Phase D）。報告 §7-4 に原稿の要点（アーカイブ名・「全 22 章」・「27 枚」）が挙がっており、書く材料は揃っている |
| E-4 | 報告を書いた後に検査を回し直したか | △ | Phase D 予定（報告 §5.2 末尾に明記） |
| E-5 | 全数テストの `EXIT=` と `FAIL` 件数 | ○ | 報告 §5 に go 60 パッケージ ok ／ Vitest 2974 ／ E2E 363 passed・flaky 1（retry 通過）が貼られている。本レビューでは再実行していない（Go・TS の変更は 0 行のため） |
| E-6 | 版ゲート 6 点 | ◎ | 報告 §1。3（「全 21 章」4 面・TOC 21・`section.chapter` 21）と 5（29 枚）は本レビューでも一致 |

---

## 設計準拠性以外の指摘事項

1. **失効した記述の残り（`README.md`）** — `README.md:218` の「★スクリーンショットは未配置(撮影は開発者の手番)」は本サブで偽になった。同じ行と `:11` の「全 22 章」は `M38-02` が追随した 4 面（本編・目次・`dist-readme.txt`・`README.txt`）の外に残った第 5 面である。`followup` の `usermanual-chapter-count-22-scattered` が「4 か所」と数えているが、実際は 5 か所目があった。
2. **失効した行番号参照（`nightly-crossbuild.yml:112`）** — 「README.txt L68-94」は現物では「■ 常駐と終了 (Windows)」〜「■ 操作タイプについて」に当たり、FW/AV のトラブルシューティングは `README.txt:134-178`「■ スマホから接続できないとき(トラブルシューティング)」にある。本サブが同じコメントブロック（`:104-111`）を書き換え、`followup` 2 件の「実体を是正した」と報告しているが、その 1 行下の失効が残った。行番号は改稿のたびに動くため、見出し名で指すのが安全である。
3. **`CLAUDE.md:260` の細部 2 点** — (a) 「アーカイブが無ければ exit 2」は実装では `dist/release/` ディレクトリが無いときだけ exit 2 であり、ディレクトリはあるが個別アーカイブが欠けるときは NG → exit 1（`main():392-395` と `check_archive():119-122`）。(b) 「ライセンス関係 7 件」は正本 `RELEASE_EXTRA_FILES` の要素数の写しであり、配列が変わると本行がずれる（スクリプト自身は件数を持たず配列を回すだけ）。
4. **`dist-readme.txt:47` は正本の 2 か所目である** — 今回の改名で実際に手直しが要ったことが、機械検査で正本と結ばれていない写しが 1 つ在ることの証拠になっている。`check-release-archive.sh` は「README の文面が実態と合うかは見ない」と自ら限界を書いており、`check-dist-readme.sh` は正本と生成物の一致しか見ない。⇒ 「`README.txt` に `RELEASE_TARGETS` の全アーカイブ名が現れる」対照を足す案が考えられる（本サブの射程外・設計卓宛の候補）。
5. **`.gitkeep` の冒頭行** — `docs/usermanual/images/.gitkeep:1`「撮影と貼り込みは開発者の手番」は、`D-893` で貼り込みが Claude 側になった後も残る。`:60-61` の追記と読み合わせれば履歴と分かるが、冒頭で現在形に読める。
6. **配布物に `.gitkeep` が同梱される** — `build-release-archives.sh:121` は `docs/usermanual/` を丸ごとコピーし、`RELEASE_MANUAL_EXCLUDE` は `dist-readme.txt` だけを除く。⇒ `manual/images/.gitkeep`（開発内部の撮影メモ・裁定 ID・マイルストーン ID を含む）が利用者向けアーカイブに入る（`tar -tzf` で実在を確認）。`M36-01` 由来で本サブの射程外だが、本サブが同ファイルへ `M38-03` の注記を足したため利用者に届く内部情報が増えている。
7. **報告の件数の精度** — 報告 §2.2-1「37 トークン」は機能する参照（id 8 ／ href 8 ／ src 10 ／ figcaption 10 ／ CSS セレクタ 1）の数であり、着手基点の `ch1[5-9]|ch2[0-2]` の出現は 39（CSS コメント内の `ch22` 2 件を含む）。コメント 2 件も diff で `ch21` へ直されており取りこぼしは無い。数の書き方の問題に留まる。
8. **残る CSS の `.todo` 定義** — 本編 `:107-110`（`.todo` ／ `figure.todo` ／ `figure.todo img`）と QS `:46-48` は使用要素 0 件のまま残る。`.gitkeep:58` が「撮り終えたら todo クラスを外す」規約を将来にも適用する前提なら残す判断でよいが、本編 `:107` の注記「M32-02 が埋めたら .todo を外す」は歴史である。
9. **旧アーカイブ名を持つ生きた資料（設計卓の面）** — `docs/process/parallel-board.md` ／ `docs/handover/followup-backlog.md` ／ `docs/instructions/M36-01-*.md` ／ 同チェックリストに `tacpendium-macos-arm64` が残る。製造の射程外であり、CHANGE 採番時に設計卓が拾う対象。

セキュリティ上の懸念は無い（シェルの追加は読み取り専用の配列走査のみ。`rm -rf` 等の対象は変えていない）。

---

## 推奨修正（優先度別）

- **高（M38 完了前に修正必須）**:
  - 高-1: `README.md:218` の「★スクリーンショットは未配置(撮影は開発者の手番)」を外す（本サブで失効）。同じ行と `README.md:11` の「全 22 章」を「全 21 章」へ。`README.md` は説明書でも設計書でもなく、指示書 §3-3 が章数表記を禁じた理由（4 面が追随済み）はこの第 5 面には当てはまらない。射程外と判断するなら、設計伝達レポート §4 へ「`usermanual-chapter-count-22-scattered` に 5 か所目（`README.md` ×2）が在る」と書いて設計卓へ回す（どちらでもよいが、いずれかは必須）。
  - 高-2: `.github/workflows/nightly-crossbuild.yml:112` の「README.txt L68-94」を見出し名（「■ スマホから接続できないとき(トラブルシューティング)」）で指す形へ。既存の失効だが、本サブが同じブロックを書き換えて「失効文を是正した」と報告した手番で残ったもの。実行内容（`for f in …`）には触れないこと。
- **中（M39 着手と並行可）**:
  - 中-1: `CLAUDE.md:260` の 2 点。「アーカイブが無ければ exit 2」→「`dist/release/` が無ければ exit 2（個別の欠落は NG → exit 1）」。「ライセンス関係 7 件」→ 件数を書かず「`RELEASE_EXTRA_FILES` の全件」等、正本を指す形へ（`E-76`）。
  - 中-2: `dist-readme.txt` のアーカイブ名（`.sha256` の案内）が正本と機械で結ばれていない件を、設計伝達レポート §4 へ followup 候補として書く（対照の案＝`check-dist-readme.sh` か `check-release-archive.sh --self-test` に「`README.txt` に `RELEASE_TARGETS` の 1 列目が全数現れる」を足す）。本サブでは実装しない。
- **低（将来対応）**:
  - 低-1: `docs/usermanual/images/.gitkeep:1` の「撮影と貼り込みは開発者の手番」を「撮影は開発者の手番」へ（`D-893`）。
  - 低-2: 本編 `:107` の CSS 注記「M32-02 が埋めたら .todo を外す」を、将来の図の追加時の規約として読める文へ（または `.todo` 定義ごと整理するかを設計卓へ問う）。
  - 低-3: `manual/images/.gitkeep` が配布物に入る件を設計伝達レポート §4 へ（`RELEASE_MANUAL_EXCLUDE` へ `images/.gitkeep` を足す案。ただし `has_dir` は `.gitkeep` に依存しないため検査は影響を受けない＝陰性対照 E）。
  - 低-4: 報告 §2.2-1 の「37 トークン」に CSS コメント内 2 件を添える（合計 39・取りこぼし 0）。
  - 低-5: `build-release-archives.sh:78-79` ／ `check-release-archive.sh:16-17` の「撮影は開発者の手番であり M36-01 の後である」は履歴として残してよいが、現在形で読まれないよう「（M36-01 当時）」等を添えると誤読が減る。任意。

---

## 良かった点

- 母集団を現物から数え、`M32-03` の一覧（章 1 つぶん古い）を引かなかった。過不足（名前ずれ 10 ／ 余剰 1）を先に表で出し、勝手に付け替えず開発者へ諮った（`M-187` の型を避けた）。
- 章番号の追従で CSS セレクタ `figure img[src*="ch22-"]` まで拾った。名前だけを追うと落とす箇所であり、`grep` の全量走査を実際にやった証拠になっている。昇順で置換する理由（降順だと連鎖する）も明記されている。
- 射程 B で、改名した瞬間に自己検査が赤になることを着手基点の版で実証し、ハードコードを「正本から引く」形へ直した。`CHANGE-213` の「正本は 1 か所」を自己検査にも通した判断は筋がよい。`local` の宣言と代入を分けている点もシェルとして正しい。
- 48 件を 1 件ずつ「アーカイブ名として書かれているか」で判定した表が報告に在り、ツールチェーン値・OS 名の一般用法・設計書本体を分けている。
- 2 文の本文修正を、実装（`schema.ts` ／ `combo.go` ／ `ja.json`）と突き合わせて書いており、仮登録の例外まで正確である。
- 報告が射程ごとに分かれ、各射程の変更統計と検査の EXIT が独立に読める。「37 トークン」のような数字が再計数で追える粒度で書かれている。

---

## 制約事項

- 本レビューはコード上で判定可能な範囲のみ対象。実際の動作確認・パフォーマンス・実機テストは別途実施が必要。
- `go test ./...` ／ `pnpm test` ／ `make e2e` ／ `make release-archives` は再実行していない（Go・TS の変更 0 行・`dist/release/` の成果物は残っていたため `check-release-archive.sh` 単体と `tar -tzf` で確認した）。
- `check-progress-log-index.sh` の `m38-03` NG は Phase D で索引行を足せば消える見込みであり、本レビューでは不合格に数えていない。
- 設計伝達レポート（CHANGE 原稿 §1 / §6）は Phase D で作成されるため未確認。報告 §7-4 の要点（アーカイブ名・「全 22 章」→ 21・「27 枚」→ 本編 28 ＋ QS 1）が原稿へ載ることを Phase D 側で確かめること。

*以上、M38-03 レビュー報告書。*

---

## 取り込み結果（自動トリアージ）

製造担当（`implement_plan_full` Phase C）による採否。取り込みコミット `c0f13f53`。**「高」指摘の不採用は 0 件**（安全弁の発動なし）。再レビュー往復 **0 回**（初回のみ）。

| 指摘 | 採否 | 理由・対応 |
|---|---|---|
| **高-1** `README.md:11,218` の「全 22 章」／「★スクリーンショットは未配置」 | **採用（修正）** | `:218` は本サブで偽になった記述。同じ行と `:11` の「全 22 章」も同時に「全 21 章」へ。`README.md` は説明書でも設計書でもなく、指示書 §3-3 の禁止理由（4 面が追随済み）はこの第 5 面に当たらない。**あわせて `followup` の `usermanual-chapter-count-22-scattered` が「4 か所」と数えていた件に 5 か所目が在ったことを設計伝達レポート §4 へ** |
| **高-2** `nightly-crossbuild.yml:112`「README.txt L68-94」の失効した行番号 | **採用（修正）** | 見出し名「■ スマホから接続できないとき(トラブルシューティング)」で指す形へ。実行内容（`for f in …`）は不変 |
| **中-1** `CLAUDE.md:260` の細部 2 点 | **採用（修正）** | 「`dist/release/` が無ければ exit 2・個別の欠落は exit 1」／「`RELEASE_EXTRA_FILES` の全件」へ。ルール面の記述であり、実装からずれた行を残す理由が無い（`E-76`） |
| **中-2** `dist-readme.txt:47` の `.sha256` 名が正本と機械で結ばれていない | **採用（報告のみ・実装しない）** | 設計伝達レポート §4 へ followup 候補として書く。対照の設計（`check-dist-readme.sh` か `check-release-archive.sh --self-test` のどちらに置くか）は設計卓の判断 |
| **低-1** `.gitkeep:1`「撮影と貼り込みは開発者の手番」 | **採用（修正）** | 「撮影は開発者の手番・貼り込みは Claude 側＝D-893」へ。本サブが同ファイルへ注記を足した手番で冒頭だけ旧のままにしない |
| **低-2** CSS 注記「M32-02 が埋めたら .todo を外す」 | **採用（修正）** | 本編・QS とも「図を足すときは撮影が済むまで .todo を付け、撮り終えたら外す(M38-03 時点で 0 件)」へ。`.todo` の定義は将来の図の追加に備えて残す（`.gitkeep:58` の規約と整合） |
| **低-3** 配布物に `manual/images/.gitkeep` が同梱される | **採用（報告のみ・実装しない）** | `RELEASE_MANUAL_EXCLUDE` へ足すかは `M36-01` の面（`D-886` の設計）であり本サブの射程外。設計伝達レポート §4 へ候補として書く |
| **低-4** 報告の「37 トークン」に CSS コメント内 2 件を添える | **採用（報告の追記）** | 完了報告 §2.2-1 に「CSS コメント内の `ch22` 2 件も `ch21` へ。出現の総数は 39・取りこぼし 0」を添えた |
| **低-5** `build-release-archives.sh:78-79` ／ `check-release-archive.sh:16-17` の「撮影は開発者の手番であり M36-01 の後である」に「（当時）」を添える | **不採用** | 同注記は「`images/` の枚数を検査で数えない」設計（`D-886`）の根拠を述べたもので、撮影が終わった今も根拠として成立している（枚数は今後も増減する）。レビュー自身も「履歴として残してよい・任意」としており、書き換えると `M36-01` 当時の判断の出所が読めなくなる |
| 設計準拠性以外 9「旧アーカイブ名が生きた資料（parallel-board ／ followup ／ `M36-01` 指示書・チェックリスト）に残る」 | **採用（報告のみ）** | 設計卓の面。CHANGE 採番時に拾う対象として設計伝達レポート §4 へ |
