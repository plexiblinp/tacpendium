# M20-01 完了報告: 初期プリセットの 3 種化

| 項目 | 内容 |
|------|------|
| 作業 ID | M20-01 |
| 対象指示書 | `docs/instructions/M20-01-initial-presets-three.md` v1.0.0 |
| レビューチェックリスト | `docs/instructions/reviews/M20-01-review-checklist.md` v1.0.0 |
| 実施日 | 2026-08-12 |
| ブランチ | `claude/initial-presets-implementation-dowb2s` |
| コミット | `b14828f`（マイグレ・定数）／ `6f63421`（既存テスト追随）／ `f97191e`（契約テスト） |
| 消費したマイグレ連番 | **`000069`**（1 本） |
| 消費した CHANGE 番号 | **なし**（`CHANGE-098` は設計卓が起票・registry 登録済み） |

---

## 0-0. ★★ 開発者・設計卓の手番として残るもの（**本報告の他節から集約**）

**本サブの実装・テスト・記録はすべて完了している。以下は製造の手を離れる項目である。**
**★詳細は各節にあるが、拾い集めなくて済むようここに一覧する。**

| # | 残る手番 | 宛先 | 詳細の所在 | 状態 |
|---|---|---|---|---|
| **1** | **指示書 `M20-01-initial-presets-three.md` とレビューチェックリストが v1.0.0 のままで、設計卓の裁定 A-1〜A-4 が未反映。** 設計卓が言及した §4.6.2 / §4.8 の新設、チェックリスト §9-6 の差替え、§0.3 への N-10 追加も入っていない。**指示書だけを読む担当は、`srk` への付け替え・`migrate_m2001_test.go` の新設・追随 7 件・godoc 是正を「指示書違反」と誤読する** | **設計卓** | **§0** ／ **§7-1** ／ 設計伝達レポート **§2-1**（★親の裁定事項） | **未着手** |
| **2** | **本サブで失効する設計書記述が、現在の是正スコープ（`CHANGE-098` ＝ `DES-004` §3.1・§3.4・§4.2 ／ `SUPP-001` §7.3）の外側に 5 か所ある。** `DES-006:134`（**VAL-P05**）／`DES-006:138`／`DES-004:405`（**§8**）／`DES-003:889`（§6）／`SUPP-001:1291`（**§7.5.5**）。**どの CHANGE にも載っていない。** **特に VAL-P05 は M20-04 のプリセット作成機能が直接参照する検証仕様** | **設計卓** | **§4.4** | **未着手** |
| **3** | **`docs/handover/architecture-patterns.md:616` の「残り 4 プリセット」が失効**（実体は残り 2）。**対になる `followup-backlog.md:93` は設計卓が更新済みで、片側だけ直っている状態。** 同書は所管が設計担当と明記されているため製造は編集していない | **設計卓** | **§4.5** | **未着手** |
| **4** | **`CHANGE-098` の三点セット**（改訂 `DES-004` ＋ `change-report-098` ＋ registry の「反映済み」化）。**as-built が確定したので出せる状態である** | **設計卓** | **§7-7** ／ 設計伝達レポート **§6** | **未着手**（起票・registry 登録は済み） |
| **5** | **消費したマイグレ連番の反映**——`000069` を消費したため、**ボード §2.2 と `change-number-registry.md:132` の「次マイグレ連番は 000069」が更新対象**（次は `000070`、disk 末尾は `000069`） | **設計卓** | **§6** | **未着手** |
| **6** | **`docs/human-notes/model-allocation.md` の M20-01 行にモデル名を記入**。**製造は書けない**（ハーネス規約が設定モデルの識別子をリポジトリ成果物へ含めることを禁じている）。**モデル名は製造セッションのチャットで伝達済み** | **開発者** | **§8** | **未着手** |
| ~~7~~ | ~~`make e2e` のフルスイート green 未確認~~ | — | §3.1 (e) ／ §9.2 | **解消**（2026-08-12・**開発者のローカル環境で成功**） |

**★上記のうち製造が判断してよいものは 1 つも無い。** 1〜5 は設計資料・設計書・ボードの所管であり（`CLAUDE.md` §8 ／ 指示書 §1.3）、6 はハーネス規約による制約である。

---

## 0. ★本サブ実行中に下りた設計卓の追加裁定 4 件（**リポジトリ上の指示書は未反映**）

Plan Mode の着手前実査で**指示書の誤り 3 件**が見つかり、設計卓へ差し戻した。以下の裁定が下りている。

> **★注意（次の担当へ）**: **設計卓は自分の手元でしか指示書・チェックリストを更新していない。**
> **本報告の執筆時点で、リポジトリ上の `M20-01-initial-presets-three.md` は v1.0.0 のまま**であり、
> A-1〜A-4 も、設計卓が言及した「指示書 §4.6.2 の新設」「§4.8 の新設」「チェックリスト §9-6 の差替え」
> 「同 §0.3 への N-10 追加」も**ファイルには入っていない**。**指示書だけを読むと本実装が誤りに見える。**

| ID | 裁定 | 設計卓の自己申告 |
|---|---|---|
| **A-1** | 指示書 §4.6 #2・#3 は**削除しない**。`TestRepository_ListAliasesByPreset_Empty` を**存置し、対象を `numeric_en` → `srk` へ付け替える**。チェックリスト §9-6 には触れない（同項の趣旨は「`numeric` へ書き換えて『消した事実』を覆い隠すこと」の禁止であり、付け替え一般の禁止ではない） | **M-43**。「`:157` と `:163` を独立した 2 件と読んだが、行番号は行の同一性を示さない。現物を持たない側が行番号から構造を推定した **E-117 の再演**」 |
| **A-2** | §4.7 の契約テスト 10 件は**新規 `internal/infra/migration/migrate_m2001_test.go`** へ `TestRun_M2001_*` で置く。**指示書 §2.1 のファイル指定が誤り** | **M-42**。「`SUPP-001` §5.5 (2) を §3.1 の必読に挙げ、チェックリスト §2 に (1)(2) を書き写しておきながら、§2.1 の指定でそれに反していた」 |
| **A-3** | 「個数」だけでプリセット数を主張していた**追随必須 7 件**（本報告 §4）を **5 → 3** へ更新してよい。**条件: 1 件ずつ「その 5 は何の 5 か」を実査して報告する** | **M-44**。「§4.6 の母数は RESEARCH C-5（＝旧 code の文字列 grep）だった。これらの行は旧 code の文字列を 1 つも含まないので grep に出ない。**削除・改称の波及は『旧識別子の文字列』だけでなく『個数』にも及ぶ**——調査の軸を 1 本立て忘れていた」 |
| **A-4** | `internal/model/preset.go` の失効 godoc は**実査で分岐**する。10 を強制する実装が無ければ**直す**（組み込み 3 ＋ カスタム 5 ＝ 8）。強制していれば触らず所在と実値を報告する | 「『`SUPP-001` §7.3 は設計卓の手番』をこの godoc へ広げてはならない。**設計卓が持つのは設計資料であって実装ファイルの godoc ではない**」 |

**★設計卓の総括**: 「Plan Mode の実査 3 件が、いずれも設計卓の誤りを拾った（M-42 / M-43 / M-44）。3 件とも『設計卓が現物を持たない』ことに由来し、3 件とも実物を見た瞬間に出ている。**§3.3 を 7 項目に増やして Plan Mode を必須にした判断は、この形で回収された**」

---

## 1. §3.3 着手前実査 7 項目（**Plan Mode で提示・設計卓が受領済み**）

### 3.3-1 マイグレ連番の disk 末尾

`ls migrations/` の末尾＝**`000068_correct_fastest_unreachable_addendum`**。
ボード記録「次に払い出す番号＝`000069` / disk 末尾＝`000068`」（2026-08-08 同期）と**一致**。
**⇒ `000069` を使用した。D-293 の再請求条件（不一致時）には該当しない。** 設計卓も受領済み。

### 3.3-2 `presets` の現況

**本環境（リモート実行環境・fresh clone）に dev DB は存在しない**ため、`migration.Run` + `t.TempDir` で
**一時 DB を作って実測した**（プローブは実測後に撤去済み。リポジトリには残していない）。

| id | user_id | code | name | base_preset_code | is_builtin |
|---|---|---|---|---|---|
| 1 | NULL | `official_ja_move` | `公式表記(日本語・技名表示)改善版` | NULL | 1 |
| 2 | NULL | `official_ja_command` | `公式表記(日本語・コマンド表示)改善版` | NULL | 1 |
| 3 | NULL | `numeric_ja` | `ナンバリング記法(日本語版)` | NULL | 1 |
| 4 | NULL | `numeric_en` | `ナンバリング記法(英語版)` | NULL | 1 |
| 5 | NULL | `srk` | `SRK 記法` | NULL | 1 |

- **検査した全行数 = 5**。**`user_id` 非 NULL（カスタムプリセット）行 = 0 件 / 検査した全行数 5**（教訓 `E-84` の併記）。
- `base_preset_code` は全 NULL、`is_builtin` は全 1。**C-1 / C-2 の見込みと一致。**
- 静的にも裏取りした: **`000005` 以降に `presets` の行を増減・改変するマイグレは 1 本も存在しない**（`presets` を含む全 40 本を走査）。

### 3.3-3 削除対象を指す `preset_aliases` の件数

```
preset_id=1 (code="official_ja_move") alias 件数 = 1653
preset_aliases 全行数 = 1653
★削除対象（official_ja_command / numeric_en）を指す alias 行数 = 0
```

**⇒ 削除対象を指す行は 0 件。指示書 §9.3-2 の停止条件は不発動**（設計卓も受領済み）。
C-5 の「エイリアスは `official_ja_move` のみ実データ投入済み」と一致する。
静的な裏取り: エイリアス投入マイグレは `000006` / `000011` の 2 本のみで、**両方とも
`WHERE code='official_ja_move'` のサブクエリで `preset_id` を引いている**（生成規則である
`internal/seedgen/generate.go:259` も同じ）。

### 3.3-4 `config` の `[defaults] preset_id` の実値と実在検査の有無

| 経路 | 実値 |
|---|---|
| `internal/config/config.go:113` | `PresetID: 1`（`Default()` の既定値） |
| `internal/config/config.go:225-226` | `if c.Defaults.PresetID < 1 { ... "want >= 1" }` |
| `internal/service/config/service.go:227-230` | `if cfg.Defaults.PresetID < 1 { ... "want >= 1" }` |

**⇒ 2 経路とも「`>= 1` の範囲検査だけで `presets` の実在を見ていない」。穴は実在する。**
**本サブでは直していない**（**D-313** ＝ M20-05 が持つ。指示書 §2.2 の指示どおり報告のみ）。
なお既定値 `1` ＝ `official_ja_move` であり、**本サブが削除した id 2 / 4 を指していない**ため
本サブのブロッカーではない（軸 C-3 の実測と一致）。

### 3.3-5 旧 3 code の参照全数（`node_modules` 除外・**着手前**の値）

| 系統 | 件数 | 所在 |
|---|---|---|
| Go 本番 | **3** | `internal/model/preset.go:7,8,9` |
| Go テスト | **9** | `migrate_test.go:928` ／ `repository/preset/repository_test.go:127,157,163` ／ `notation/cache_test.go:259,261,277,283` ／ `notation/resolver_test.go:72,79` |
| TS 本番 | **3** | `web/src/features/preset/types.ts:15,16,17` |
| TS テスト | **0** | — |
| E2E spec | **0** | 同じ検査で `official_ja_move` が 3 件ヒットしており、**検査器が `web/e2e/` を見ていることは裏取り済み** |
| マイグレ | **7** | `000005` up 3・down 3 ／ `000006:9` ／ `000011:4` |
| i18n JSON | **0** | **表示名（`ナンバリング記法` 等）の残存も 0 件**（チェックリスト §3 の観点。プリセット名は DB 由来でロケールに持たない） |
| `docs/` | **270** | `official_ja_command` 77 ／ `numeric_ja` 102 ／ `numeric_en` 91 |

**⇒ レポート C-5 の実測との差分は無い**（基準時点 commit `1972d82` 以降に増えたものは無い）。

### 3.3-6 `BUILTIN_PRESET_CODES` の定義位置

**`web/src/features/preset/types.ts:13`**（C-4 の実測どおり。**`web/src/constants/` 配下に preset 関連ファイルは無い**）。

**参照箇所の全数 = 2 か所**、いずれも `ComboDetailRecipe.tsx`:
- `:10` — `import { BUILTIN_PRESET_CODES } from "@/features/preset/types";`
- `:30` — `(p) => p.code === BUILTIN_PRESET_CODES.officialJaMove`

**⇒ 参照はどちらも `officialJaMove` であり、本サブの変更で `ComboDetailRecipe.tsx` は 1 バイトも変わっていない**（見込みどおりだったことを実査で確定した）。

### 3.3-7 `preset_aliases` の FK 制約と `ON DELETE` の挙動

```sql
CREATE TABLE preset_aliases (
    id          INTEGER PRIMARY KEY AUTOINCREMENT,
    preset_id   INTEGER NOT NULL REFERENCES presets(id) ON DELETE CASCADE,
    move_id     INTEGER NOT NULL REFERENCES moves(id),
    alias_text  TEXT    NOT NULL,
    UNIQUE (preset_id, move_id)
)
```

- **`preset_id` に FK があり、`ON DELETE CASCADE` が付いている。⇒ 削除は `preset_aliases` へ波及しうる。**
- **実測: `PRAGMA foreign_keys = 0`**（一時 DB の当該接続）。**ボード P-04 の既知課題（PRAGMA が接続プール全体に効いていない）を実測で確認した。本サブでは根治していない。**
- **本サブでは、削除対象を指す alias が 0 件であることと相まって、いずれにせよ波及しない。**
  **その事実を §4.7 の U-7 / DN-3 で固定した**（行数不変＋ orphan 0 件）。

### §3.4 並列相手（`E-14` 交差の確認）

- 並列相手は **M21 レーン（`M21-RESEARCH-01` の PoC）**。同指示書 §0.2' が探索モード＝**本体リポを 1 バイトも変更しない**と定めている。
- **実際に確認した**: 着手時の作業ツリーが clean であること、`git log --oneline` 直近（`eb472b4` / `c1f37ee` / `3051352`）に M21 由来の実装差分が無いこと（3 件とも設計卓の docs 更新とマージコミット）。
- **M20 レーン内の並列は無い**（本サブが M20 の実装 1 本目。M20-02 以降は本サブ完了後）。
- **★「触るファイルが違うから大丈夫」で終わらせていない**（教訓 `E-121`）。上記の実査を行った記録として本節を残す。

---

## 2. 変更内容

### 2.1 マイグレ `migrations/000069_m20_initial_presets_three.{up,down}.sql`（新規）

**up**（§4.1 の規則 4 点をすべて充足）:
- `code` を条件にした（`id` 決め打ちにしていない）。
- **1 行 1 文**で書いた（`IN (...)` でまとめていない）。
- `numeric_ja` → `code='numeric'` / `name='ナンバリング記法'`。
- `is_builtin` / `base_preset_code` / `user_id` は触っていない。

**down**（§4.2 の規則 4 点をすべて充足）:
- **`id` を明示**（2 と 4）。
- **`name` は `migrations/000005_seed_presets.up.sql` からの逐語コピー**。**括弧はすべて半角、中黒あり。**
  機械的に突き合わせて一致を確認済み（3 リテラルとも `000005` と `000069.down` の双方に 1 件ずつヒット）。
- **6 列すべて明示**（`id, user_id, code, name, base_preset_code, is_builtin`）。
- **条件は `code='numeric'`**（up の反転である `code='numeric_ja'` ではない。反転にすると 0 行に当たる）。

**★`id` を詰めていない**（§4.3）。振り直し `UPDATE` / `VACUUM` / テーブル再作成は 1 行も書いていない。
**適用後は id が 1 / 3 / 5 になり、2 と 4 は欠番になる。これは仕様である。**

**★サービス層の組み込みプリセット保護は緩めていない。** そもそも本サブはサービス層を 1 行も触っていない
（付言すると、**現時点でプリセットの作成・削除 API は未実装**であり、`internal/service/preset/` は `doc.go` のみである）。

### 2.2 定数

| ファイル | 変更 |
|---|---|
| `internal/model/preset.go` | `PresetCodeOfficialJaCommand` / `PresetCodeNumericEn` を削除。`PresetCodeNumericJa` → **`PresetCodeNumeric = "numeric"`**。**`PresetCodeSRK` は不変** |
| `web/src/features/preset/types.ts` | `BUILTIN_PRESET_CODES` を `officialJaMove` / **`numeric`** / `srk` の 3 キーへ |

**★指示書 §4.4 の表は識別子を `PresetCodeSrk` と書いていたが、実際の綴りは `PresetCodeSRK` だった。**
§4.4 の「綴りが違う場合、既存の綴りに合わせる——本サブは命名規則を変える場ではない」に従い、**既存綴りを維持した。**

**Go 定数の値・TS 定数の値・`presets.code` の三者は一致している**（`official_ja_move` / `numeric` / `srk`）。

### 2.3 失効記述の是正

| ファイル | 変更前 | 変更後 |
|---|---|---|
| `internal/testutil/dbtest/dbtest.go:4` | 「組み込みプリセット **5 種** … seed 済み」 | 「組み込みプリセット **3 種**」 |
| `internal/model/preset.go:17` | 「全体上限は **10 件(組み込み 5 + カスタム 5**、SUPP-001 で確定)」 | 「全体上限は **8 件(組み込み 3 + カスタム 5)**」＋ SUPP-001 との一時的不一致の注記 |

#### ★A-4 の実査結果（分岐の根拠）

**「10 を強制している実装は無い」と判定した。根拠は 3 点。**

1. **`internal/service/preset/` は `doc.go` のみ**で、サービス実装が無い。プリセットの作成 API が存在しない。
2. `internal/api/preset/` は `GET /api/presets` と `GET /api/presets/:id` の**読み取り 2 本のみ**（`routes.go:7-8`）。
3. `MaxPresets` 等の上限定数・上限検査・バリデーションを `internal/` 全体で走査したが **0 件**
   （`handler.go:41` の `10` は `strconv.ParseInt(..., 10, 64)` の**基数**であり上限ではない）。

**⇒ A-4 の分岐表の「10 を強制している実装が無い（godoc の記述だけ）」に該当するため、直した。**

**★`SUPP-001` §7.3 は「10 件(組み込み 5 + カスタム 5)」のままであり、本サブ完了時点で実装コードと一時的に食い違う。**
**これは設計卓が承認した想定内の状態である**（設計卓が本サブの as-built を見てから §7.3 を是正する）。
**食い違いを読み手が誤解しないよう、`preset.go` の godoc 内に「SUPP-001 §7.3 は 10 件のままであり一時的に食い違う」旨を明記した。**

### 2.4 既存テストの追随（指示書 §4.6 の 6 件）

| # | 場所 | 処置 | 結果 |
|---|---|---|---|
| 1 | `repository/preset/repository_test.go`（`TestRepository_FindAlias_NotFound`） | `numeric_ja` → `numeric` | 済 |
| **2・3** | `repository/preset/repository_test.go`（`TestRepository_ListAliasesByPreset_Empty`） | **★A-1 により削除せず、対象を `numeric_en` → `srk` へ付け替えて関数を存置** | 済 |
| 4 | `notation/cache_test.go`（`TestRecomputePresetCache`） | `numeric_ja` → `numeric`、変数 `numericJaID` → `numericID` | 済 |
| 5 | `notation/resolver_test.go`（`TestRenderSteps_FallbackToOfficialJaMove`） | 同上 | 済 |
| 6 | `migration/migrate_test.go` | **code だけ** `numeric` へ。**「0 件」の主張は据え置き、削除していない** | 済 |

**#2・#3 について**: 関数内に**「`srk` は M20-02 でエイリアスが投入されると落ちる。それは意図した仕掛けであり、
`migrate_test.go` の『0 件』と同型の『まだ空である』を守る資産である（D-308）。落ちたときの正しい対応は、
期待値を緩めることではなく M20-02 側で処遇を決めることである」**というコメントを残した。
**未使用になったヘルパ・fixture は発生していない**（関数を存置したため）。

**#6 について**: 「本サブはエイリアスを 1 行も投入しないため落ちない。落ちるのは M20-02 のときであり、
その扱いは M20-02 の指示書が持つ」旨のコメントを添えた上で、**削除していない。**

### 2.5 ★追随必須 7 件（**A-3 の §4.6.2**。1 件ずつ「その 5 は何の 5 か」を実査した）

**A-3 の条件に従い、7 件それぞれについて「その `5` が何を数えているか」をコードで確認した。
結果、7 件すべてが「プリセット総数」であり、意味の違うものは混じっていなかった。**

| # | 場所 | 「5」の意味（**実査で確定**） | 判定根拠 | 処置 |
|---|---|---|---|---|
| 1 | `migration/migrate_test.go:922`（`TestRun_SeedRowCounts`） | `SELECT count(*) FROM presets` ＝ **プリセット総数** | クエリそのもの | 5 → 3 |
| 2 | `migration/migrate_test.go:923`（同上） | `WHERE is_builtin=1` の件数 ＝ **組み込みプリセット数** | 本サブ後も残る 3 行は全て `is_builtin=1`（U-4 / U-5 で固定） | 5 → 3 |
| 3 | `repository/preset/repository_test.go:41`（`TestRepository_ListAllPresets`） | `ListAllPresets()` の戻り件数 ＝ **プリセット総数** | `repository.go:53` が `presets` を全件返す | 5 → 3 |
| 4 | `api/preset/handler_test.go:38`（`TestHandler_List_200`） | `GET /api/presets` の要素数 ＝ **プリセット総数** | `handler.go:25,32-33` が `ListAllPresets` の結果をそのまま DTO 化 | 5 → 3 |
| 5 | `notation/cache_test.go:92` | `recipe_cache` の JSON キー数 ＝ **プリセット総数** | `cache.go:63,73-80` の `RecomputeComboCache` が `ListAllPresets()` を回し **プリセット 1 件 = キー 1 つ**を作る | 5 → 3 |
| 6 | `notation/cache_test.go:166` | 同上（`Restore` 後） | 同上 | 5 → 3 |
| 7 | `notation/setup_resolver_test.go:185` | 同上（セットプレイ側） | `setup_resolver.go:68` の `RecomputeSetupCache` も `ListAllPresets()` を回す | 5 → 3 |

**★`migrate_test.go` の 2 件は HEAD スコープのテストである**（`migration.Run` で全適用）。
`SUPP-001` §5.5.2 (2) の「HEAD テストが落ちたら期待値を更新するのが正しい対応になり得る」に該当するため、
期待値を更新した。**本サブの契約は `migrate_m2001_test.go` が別に持っており、両者を混ぜていない。**

**★追随不要と判定したもの**: `internal/service/combo/service_test.go:249` は
`SELECT count(*) FROM presets` で**プリセット数を動的に数えてから** `len(cache)` と突き合わせている。
**ハードコードされた 5 ではないため追随不要**（実際に無改変で green）。

### 2.6 本サブのマイグレ契約テスト（指示書 §4.7 / **A-2 により新規ファイル**）

**`internal/infra/migration/migrate_m2001_test.go`（新規）に `TestRun_M2001_InitialPresetsThree` として
サブテスト 10 件**（up 7 ＝ U-1〜U-7 / down 3 ＝ DN-1〜DN-3）。

**規約の充足**:
- **(1) 比較区間を自サブに閉じた**: `m.Migrate(68)` → `m.Migrate(69)` → `m.Migrate(68)`。**`m.Up()` を使っていない。**
- **(2) HEAD スコープの主張を本サブの名前空間に置いていない**（HEAD の主張は `migrate_test.go` 側に残した）。
- **(3) 投入した行と投入しなかった行を対で固定した**（U-2 は 3 件の列挙**＋列挙外 0 件**、U-6 は旧 3 code の 0 件**＋検査した全行数 3 の併記**）。
- **(5) down のスコープ条件を up の反転にしていない。**

| # | 主張 | 実装上の要点 |
|---|---|---|
| U-1 | 全行数が 3 | |
| U-2 | code 集合が `official_ja_move` / `numeric` / `srk` に一致 | **列挙で固定＋列挙外の code が 0 件**であることも対で見る |
| U-3 | id が 1 / 3 / 5 | **★欠番 2・4 が埋まっていないことを明示的に固定**（§4.3 の非破壊条件をテストで守る） |
| U-4 | `official_ja_move`(id=1) の `name` / `is_builtin` / `base_preset_code` が **up 前と同一** | v68 時点の実値を採ってから比較 |
| U-5 | `numeric`(id=3) の `name` が `ナンバリング記法` | `is_builtin=1` / `base_preset_code` NULL も併せて確認 |
| U-6 | 旧 3 code の行数がいずれも 0 | **検査した全行数 3 を併記**（`E-84`） |
| U-7 | `preset_aliases` の行数が up 前後で同一 | **orphan 行 0 件も併せて固定**（FK CASCADE の非波及を実測） |
| DN-1 | 全行数が 5 | |
| DN-2 | **5 行 × 5 列**（id / code / name / is_builtin / base_preset_code）が `000005` の投入値と一致 | `user_id` の NULL も併せて確認 |
| DN-3 | `preset_aliases` の行数が down 前後で同一 | |

#### ★ミューテーションで検査力を裏取りした（**検査が実際に効くことの確認**）

**テストが green であることは、テストが効いていることを意味しない。** 2 件のミューテーションで確認した。

| ミューテーション | 期待 | 結果 |
|---|---|---|
| down の `INSERT` から `id` を落とす（チェックリスト §9-2 の重大条件） | DN-2 が検出 | **`FAIL: DN-2_5行5列が000005の投入値と一致`** |
| up に `id` を詰める `UPDATE` を足す（同 §9-1 の重大条件） | U-3 が検出 | **`FAIL: U-3_idが1_3_5で2と4が欠番`**（副次的に U-5 も検出） |

**両方とも確認後に撤去し、`git diff migrations/` が空であること（＝コミット済みの内容と完全一致）を確認済み。**

---

## 3. テスト結果（§7.2 自己テスト）

| 検査 | 結果 |
|---|---|
| `go build ./...` / `go vet ./...` | **通る** |
| `go test ./...` | **通る**（57 パッケージ green・**FAIL 0 件**） |
| `internal/infra/migration` の §4.7 テスト | **10 件すべて実在し、すべて通る**（サブテストとして個別に列挙される） |
| `cd web && pnpm test` | **通る**（**120 ファイル / 949 テスト全 pass**） |
| `cd web && pnpm exec tsc --noEmit` | **通る（exit=0）** |
| `make e2e` | **本サンドボックスでは完走しない。§3.1 で切り分けた** |

### 3.1 ★E2E の切り分け（§7.2 の「原因かどうかを切り分けて報告する」）

**結論: 本サブの変更が原因ではない。環境要因である。**

**(a) `make e2e` はブラウザ取得の時点で失敗する**（本サブと無関係）。
`playwright install chromium` が**ネットワークポリシーに拒否される**:
`Download failed: server returned code 403 body 'request rejected: host not permitted'`。
プロジェクトの playwright-core 1.60.0 が要求する chromium ビルドは **1223** だが、
本環境にプリインストールされているのは **1194** でありバージョンが一致しない。

**(b) `PW_EXECUTABLE_PATH` でプリインストール Chromium を使うと起動はする**が、
フルスイート（73 テスト・14.0 分）で **34 failed / 39 passed** になる。

**(c) ★同じテストを小バッチで再実行すると全部通る。** 失敗した 34 件のうち **10 件**を再実行して確認した。

| 再実行したバッチ | 結果 |
|---|---|
| `combo-crud.spec.ts`（失敗リストの 2 件） | **2 passed** |
| `character-default` + `m15-02-info-mark` + `m12-06-draft-promotion`（失敗リストの 5 件） | **5 passed** |
| `m14-03d-manon-seed` + `m14-03e-third-wave-seed` + `m12-05-seed-cleanup` + `moves-edit`（うち失敗リストは 3 件） | **5 passed** |

**⇒ 再現しない。フルスイート実行時の資源競合・タイムアウトによるものと判断する**（ボード **P-14 / P-22** の既知 flaky と同型）。

**(d) ★本サブが壊しうる面を狙って確認した。**「flaky だから」で流していない。
- 失敗の分布が**プリセット面に集中していない**（`character-default` / `combo-crud` / `csv-io` など全面に散っている）。本サブ起因なら特定面に寄るはずである。
- **エイリアス解決面を直接見る spec を実行して通した**——`m14-03d-manon-seed`（「画面18 で alias 欠落（生 code フォールバック）が無い」）／`m14-03e-third-wave-seed`（「6 キャラの moves/alias が対で入る」）／`m12-05`（仮想コントローラ回帰）。
  **これらは `official_ja_move` のエイリアス解決を実際に検証しており、本サブの削除・改称で壊れるなら落ちる面である。**
- **プリセット選択肢が 5 → 3 に減ることによる回帰**（チェックリスト §3）についても、上記および `moves-edit` が通ることで、
  表示面の回帰が無いことを確認した。**そもそも削除した 2 種はエイリアス空でフォールバック表示されていたため、表示の変化は無い**（指示書 §1.1）。

**(e) ★開発者のローカル環境で `make e2e` が成功した（2026-08-12・開発者報告）。**
**⇒ 本サブによる E2E 回帰は無いことが実環境で確認された。** 上記 (a)〜(d) は
**「本サンドボックスでフルスイートが完走しない理由と、その切り分け」の記録として残す**
（本環境固有のブラウザ取得制約であり、コード側の問題ではないことが実環境の成功で裏付けられた）。

---

## 4. §4.9 否定形確認（**4 項目すべて走査**）

### 4.1 走査結果（**適用後**の値）

| # | 撤回・変更した定義 | 走査キーワード | 本番/テスト | マイグレ | docs |
|---|---|---|---|---|---|
| 1 | `official_ja_command` の提供 | `official_ja_command` ／ `officialJaCommand` ／ `PresetCodeOfficialJaCommand` ／ `公式表記(日本語・コマンド表示)` | **3 + 0 + 0 + 1 = 4** | 8 + 0 + 0 + 2 = 10 | 77 + 6 + 3 + 7 = 93 |
| 2 | `numeric_en` の提供 | `numeric_en` ／ `numericEn` ／ `PresetCodeNumericEn` ／ `ナンバリング記法(英語版)` | **5 + 0 + 0 + 1 = 6** | 8 + 0 + 0 + 2 = 10 | 91 + 6 + 3 + 7 = 107 |
| 3 | `numeric_ja` という code | `numeric_ja` ／ `numericJa` ／ `PresetCodeNumericJa` ／ `ナンバリング記法(日本語版)` | **3 + 0 + 0 + 1 = 4** | 10 + 0 + 0 + 2 = 12 | 102 + 7 + 3 + 8 = 120 |
| 4 | 「組み込みは 5 種」という数 | `組み込み5` ／ `組み込み 5` ／ `5プリセット` ／ `5 プリセット` ／ `他4プリセット` ／ `残り 4 プリセット` ／ `組み込み5+カスタム5` ／ `組み込みプリセット 5` | **1** | 4 | （歴史記録・未走査集計） |

**★TS 側（`officialJaCommand` / `numericEn` / `numericJa`）は本番・テストとも 0 件**。
**Go 定数名（`PresetCodeOfficialJaCommand` 等）も 0 件。** **E2E spec も 0 件**（着手前から 0）。

### 4.2 ★本番/テストに残った 7 箇所の内訳（**すべて構造上必要なもの**）

```
internal/model/preset.go:7                        ← 「official_ja_command と numeric_en は提供しない。
                                                     numeric_ja は numeric へ改称した」＝廃止を説明する注記
internal/repository/preset/repository_test.go:154  ← 「numeric_en から srk へ付け替えた」＝A-1 の経緯の説明
internal/repository/preset/repository_test.go:155  ← 同上
internal/infra/migration/migrate_m2001_test.go:49  ← DN-2 の復元期待値テーブル（id=2 official_ja_command）
internal/infra/migration/migrate_m2001_test.go:50  ← 同上（id=3 numeric_ja）
internal/infra/migration/migrate_m2001_test.go:51  ← 同上（id=4 numeric_en）
internal/infra/migration/migrate_m2001_test.go:56  ← U-6 の「消えたことを確認する code」リスト
```

**いずれも「旧仕様が生きている記述」ではない。** 前 3 者は**廃止したという事実を説明する注記**であり、
後 4 者は**「消えたこと」「down で戻ること」を検査するために旧 code を名指しする必要がある箇所**である。
チェックリスト §12.1 が「**旧 code の文字列がリポジトリに残っていないことを重大にしていない**——
down マイグレ自身が名指しする必要があり達成不能である」と明記しているとおりの構造である。

`internal/model/preset.go:19` に残る「組み込み 5」は、**`SUPP-001` §7.3 が 10 件のままであることを
読み手へ知らせる注記**（§2.3 参照）であり、失効した主張ではない。

### 4.3 ★見つけたが直さない場所（**直さない記録**）

**この記録が無いと、次の担当が同じ走査をやり直す。**

| 系統 | 所在 | 直さない理由 |
|---|---|---|
| **(a) 適用済みマイグレ** | `migrations/000005_seed_presets.{up,down}.sql`（旧 3 code ＋「組み込みプリセット 5 種」）／ **`000006_seed_aliases_official_ja_move.up.sql:9`**（「残り 4 プリセット(official_ja_command、numeric_ja、numeric_en、srk)は…」）／ **`000011_seed_aliases_official_ja_move_aki_jamie_guile.up.sql:4`**（同旨） | **適用済みマイグレは改変しない**（指示書 §2.2 ／ **D-308** ／ チェックリスト N-2）。**`000006:9` と `000011:4` のコメントは本サブで内容が失効するが、実データではないため据え置く。** **★据え置いたことを本節に明記する**（記録が無いと次の担当が「直し忘れ」と読む） |
| **(b) `docs/` 配下の歴史記録** | **計 270 件超**（完了報告・review・`change-notes/`・`progress/`・過去の handover・RESEARCH レポート） | **当時どうだったかの記録である**（§4.9 (b) ／ チェックリスト N-7） |
| **(c) 設計書本体** | `docs/design/04-notation-spec.md`（DES-004 §3.1・§3.4・§4.2）／ `docs/design/supp-001-detailed-design.md`（§3.4・§7.3） | **設計卓が扱う**（§1.3）。**`CHANGE-098` で `DES-004` を改訂し、`SUPP-001` は自由改訂で是正する。製造は 1 文字も編集していない** |

### 4.4 ★★ 設計卓への名指し伝達: **是正スコープに入っていない失効記述が 5 か所ある**

**指示書 §1.3 が設計卓の手番として挙げているのは `SUPP-001` §7.3 と `DES-004` §3.1・§3.4・§4.2（CHANGE-098）の 2 系統だけである。**
**しかし本サブで失効する記述は、その外側にもある。レビューで検出された（レビュー報告書 指摘 1）。**

| # | 所在 | 失効する記述 | 現在の是正スコープ |
|---|---|---|---|
| **a** | `docs/design/06-validation.md:134` | **VAL-P05**「システム全体のプリセット総数（組み込み **5** 件＋カスタム）が上限（**10** 件）を超えていないか」 | **どの CHANGE にも入っていない** |
| **b** | `docs/design/06-validation.md:138` とその根拠段落 | 「組み込み **5** 件＋カスタム 5 件の合計 **10** 件を上限とする」 | **同上** |
| **c** | `docs/design/04-notation-spec.md:405`（**§8**） | 「組み込み **5** プリセットの全エイリアス表記」 | **`CHANGE-098` の対象節（§3.1・§3.4・§4.2）に含まれていない** |
| **d** | `docs/design/03-data-model.md:889`（§6） | 「プリセットのエイリアス全件（組み込み **5** プリセット分）」 | **どの CHANGE にも入っていない** |
| **e** | `docs/design/supp-001-detailed-design.md:1291`（**§7.5.5**） | 「プリセット数上限 **10** 件の制約内、§7.3 の方針通り」 | **§1.3 が挙げるのは §7.3 のみ。§7.5.5 は明示されていない** |

**製造は上記 5 か所を編集していない**（§1.3 ／ チェックリスト §9-10）。**問題は「誰の手番にも載っていない」ことである。**
**このまま完了すると、`internal/model/preset.go` の godoc だけが「8 件」になり、設計書 5 か所が「10 件 / 組み込み 5」のまま残る。**
**次の担当は設計書をコピーするので、旧の数が M20-04（プリセット管理 UI）・M20-05 の前提として複製される。**

**⇒ 設計卓へ: `CHANGE-098` のスコープ拡大か、別 CHANGE の起票か、自由改訂かの判断をお願いしたい。**
**(a)(b) の `DES-006` VAL-P05 は、M20-04 でプリセット作成機能を作るときに直接参照される検証仕様であり、優先度が高い。**

### 4.5 ★ 失効を検出したが、資料の所管が製造でないため直さないもの

| 所在 | 失効する記述 | 直さない理由 |
|---|---|---|
| **`docs/handover/architecture-patterns.md:616`** | 「**フェーズ3 の前提**: **残り 4 プリセット**のエイリアス整備 / プリセット管理 UI 着手時に SUPP-001 §7 の再計算を実装する」 | **歴史記録ではなく前向き参照であり、§4.9 (b) の免除に当たらない**（本サブ後の実体は**残り 2**＝`numeric` / `srk`）。**ただし同書は冒頭で「対象読者＝設計・指示書作成担当 Claude」「本書へ追記するのは設計担当 Claude」と所管を明記しており、製造が編集する資料ではない。** **★対になる `docs/handover/followup-backlog.md:93` は 2026-08-12 に設計卓が「残り 4 → 残り 2」へ更新済みであり、片側だけが直っている状態である。** ⇒ **設計卓へ是正を依頼する。** |
| `docs/handover/phase2-to-phase3-handover.md:64` | 「**残り 4 プリセット**のエイリアス実データ整備（numeric_ja / srk 等）」 | **フェーズ間 handover は歴史記録寄りであり §4.9 (b) の免除に当たる**（レビュー指摘 10 の判断に同意）。**是正不要。次の担当が同じ走査をやり直さないよう所在だけ記録する。** |

---

## 5. スコープ外に手を出していないことの確認（指示書 §1.3・§2.2）

| 項目 | 状態 |
|---|---|
| `numeric` / `srk` のエイリアス実データ投入と生成規則（M20-02） | **手を出していない。`preset_aliases` は 1 行も触っていない**（U-7 / DN-3 で行数不変を固定） |
| 移動系 9 code・`rush_variant` の表記文字列（M20-02 / P-33） | 手を出していない |
| `preset_aliases` の一意制約（M20-03・**スキーマ承認ゲート未承認**） | **手を出していない。DDL は 1 行も書いていない** |
| プリセット管理 UI / カスタムプリセット作成（M20-04） | 手を出していない。**露出面を先に作っていない** |
| `recipe_cache` の読み出しキー（**D-313** / M20-05） | **`internal/model/recipe.go` の `DefaultPresetID = "1"` は無改変** |
| `presets` への実在検査の追加（D-313 / M20-05） | **追加していない。§3.3-4 で実査して報告するにとどめた** |
| `SUPP-001` §7.3 の是正 | **製造は `SUPP-001` を編集していない**（§2.3 の godoc 是正は実装ファイル側であり、A-4 で明示的に製造の担当とされた） |
| `DES-004` §3.1・§3.4・§4.2 の改訂 | **製造は `DES` 本体を 1 文字も編集していない**（`CHANGE-098` で設計卓が行う） |
| `config` の `[defaults] preset_id` | **触っていない**（既定値 `1` のまま） |
| `official_ja_move`（id=1）の行 | **`code` / `name` / `is_builtin` / `base_preset_code` のいずれも触っていない**（U-4 で固定） |
| バックエンドのサービス層・ハンドラ層・API 契約 | **追加も変更もしていない**（§2.3 例外条項＝該当なし） |

---

## 6. ■ 併せて更新が要るもの

| 対象 | 状態 |
|---|---|
| **消費した CHANGE 番号の registry 登録** | **不要だった（実査済み）。** `docs/handover/change-number-registry.md:129` に **`CHANGE-098` が「起票済（未反映）」で登録済み**（同 §4 の v1.107.0 履歴行も対で存在）。**設計卓が起票と登録を同じ手番で行っている**（D-277 / E-114）。**製造の追加登録は不要。** |
| **「次の番号」の写し先 4 か所** | **設計卓が扱う**（registry §1 ／ `m20-contract` §3・§4 ／ board §2.1 ／ board §2.4）。**製造は編集していない。** |
| **消費したマイグレ連番** | **`000069` を 1 本消費した**（`000069_m20_initial_presets_three.{up,down}.sql`）。**⇒ 次に払い出す番号は `000070`、disk 末尾は `000069` になる。** **ボード §2.2 と `change-number-registry.md:132`（「次マイグレ連番は 000069」）が更新対象である。** |
| **版を上げた文書の参照元** | **本サブでは `docs/` の版を 1 つも上げていない。** ⇒ 参照元の追随（教訓 `E-118`）は該当なし。 |
| **`docs/human-notes/model-allocation.md`** | **M20 節を新設して追記した**（§8 参照）。 |
| **`docs/progress/progress-log.md`** | 索引行を追記した。 |

---

## 7. 気づいたこと・申し送り

1. **★リポジトリ上の指示書 `M20-01-initial-presets-three.md` は v1.0.0 のままで、設計卓の裁定 A-1〜A-4 が入っていない**（§0 参照）。設計卓が言及した §4.6.2 / §4.8 の新設、チェックリスト §9-6 の差替え、§0.3 への N-10 追加も未反映。**as-built とファイルが食い違っているため、設計卓に反映をお願いしたい。**

2. **`BUILTIN_PRESET_CODES` の位置が `CLAUDE.md` §4 の規約から外れている。**
   規約は「バックエンド列挙定数と同期する定数は `web/src/constants/` 配下へ作成する」と定めているが、
   現在位置は `web/src/features/preset/types.ts` である。
   **指示書 §4.5 が移設を明示的にスコープ外としているため、移設していない**（チェックリスト N-4）。
   **移設の要否は M20-04（プリセット管理 UI で `presets.code` が画面へ露出するサブ）で判断するのが自然である**——
   露出面を作るときに import 元をまとめて整理できる。

3. **`check-enum-sync.sh` のベースラインが 2 件緩んだ。**
   本サブが `official_ja_command` / `numeric_en` を消したことで散在数が **26 → 24** へ減り、
   同スクリプトが「`BASELINE_SCATTER` を 24 へ更新してよい」と出力している。
   **本サブでは更新していない**——検査スクリプトの改変は本サブの「削除・リネーム」という単一の変更に別の変更を混ぜるため。
   **ただし更新しないとラチェットが 2 件分緩んだままになる。** 改善レーンか M20-02 で締め直すことを推奨する。

4. **`recipe_cache` の既存行に、消えた id `2` / `4` のキーが残る。**
   本サブは `recipe_cache` を再計算しない（スコープ外＝**D-313** / M20-05）。
   既存 DB では `combos.recipe_cache` / `setups.recipe_cache` の JSON に `"2"` / `"4"` のキーが残るが、
   **読み出しキーは `model.DefaultPresetID = "1"` 固定であり、表示には影響しない。**
   新規 DB では最初から 3 キーになる。**M20-05 が `recipe_cache` を扱うときの前提として記録しておく。**

5. **プリセットの作成・削除 API は現時点で未実装である**（`internal/service/preset/` は `doc.go` のみ、
   `internal/api/preset/` は読み取り 2 本のみ）。`SUPP-001` §7.3 の「削除不可（403 応答）」も
   **まだ実装されていない**（保護すべき経路自体が無いため矛盾はしない）。**M20-04 で作成 UI を作るときに、
   §7.3 の保護と上限検査を同時に入れる必要がある。**

6. **`PRAGMA foreign_keys = 0` が実測で確認された**（§3.3-7）。ボード **P-04** の既知課題は生きている。
   **本サブでは削除対象を指す alias が 0 件だったため実害は無かった**が、
   **将来エイリアスが入った状態でプリセットを削除する経路（M20-04 のカスタムプリセット削除）を作るときは、
   この PRAGMA が効いていないと孤児行が残る。** M20-04 の設計時に必ず確認されたい。

7. **DES CHANGE の要否判定**: **本サブは `DES-004` §3.1・§3.4・§4.2 の改訂を要する。**
   **`CHANGE-098` として設計卓が起票済み**（registry 登録済み・ステータス「起票済（未反映）」）。
   **製造は `DES` 本体を編集していない。** as-built が確定したので、設計卓は
   「改訂 `DES-004` ＋ `change-report-098.md` ＋ registry の『反映済み』化」の三点セットを出せる状態である。

---

## 8. 使用モデルの記録について

`docs/human-notes/model-allocation.md` に **M20 節を新設**し、本サブの行を追加した。

**★実使用モデルの正確な識別子は、本報告書には記載していない。**
本セッションのハーネス規約が、**設定モデルの識別子をリポジトリへ push される成果物
（コミットメッセージ・コード内コメント・その他の成果物）に含めることを禁じている**ためである。
**実使用モデル名は製造セッションのチャット上で開発者へ伝えた。**
`model-allocation.md` の当該行には**割当区分（指示書ヘッダの「高速実装」枠 ／ Plan Mode 必須で実行）を記録し、
モデル名の欄は開発者が記入する形にしてある。**

---

## 9. 完了条件（DoD）の充足状況

### 9.1 機能要件

- [x] `presets` が 3 行（`official_ja_move` / `numeric` / `srk`）である（U-1 / U-2）
- [x] `id` が `1` / `3` / `5` で、`2` と `4` が欠番である（U-3）
- [x] `numeric` の `name` が `ナンバリング記法` である（U-5）
- [x] `down` で 5 行へ忠実に戻る（DN-1 / DN-2 の 5 行 × 5 列）
- [x] Go 定数が 3 種、フロント定数 `BUILTIN_PRESET_CODES` が 3 キーである
- [x] §4.6 の 6 件が処置されている（**#2・#3 は A-1 により削除ではなく `srk` へ付け替え／#6 は削除していない**）

### 9.2 自己テスト結果

- [x] `go test ./...` が通る（57 パッケージ green / FAIL 0）
- [x] `cd web && pnpm test` と型チェックが通る（949 テスト pass / `tsc --noEmit` exit=0）
- [x] **`make e2e` が通る** — **開発者のローカル環境で成功（2026-08-12・開発者報告）。** 本サンドボックスでは完走しないが、それはブラウザ取得がネットワークポリシーで拒否されプリインストール版がバージョン不一致であるという**環境固有の制約**であり、§3.1 で切り分け済み
- [x] §4.7 のテストが 10 件（up 7・down 3）実在し、すべて通る（**ミューテーション 2 件で検査力も裏取り済み**）

### 9.3 品質チェック

- [x] §4.9 の否定形確認を 4 項目すべて走査し、結果（件数と所在）を報告している（§4）
- [x] 「見つけたが直さない」場所を明示している（§4.3 の (a)(b)(c)）
- [x] `console.log` / `fmt.Println` を残していない（本サブは追加していない）
- [x] 不要な `eslint-disable` / `nolint` を追加していない

### 9.4 ドキュメント

- [x] `docs/progress/progress-log.md` への追記
- [x] DES CHANGE の要否判定結果を記載（§7-7）
- [x] `docs/human-notes/model-allocation.md` へ追記（§8 の但し書きあり）

---

*以上、M20-01 完了報告。マイグレ `000069` を消費し、組み込みプリセットを 5 種から 3 種へ整理した。`id` は詰めておらず、`2` と `4` は欠番のまま残る。*
