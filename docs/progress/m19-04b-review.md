# M19-04b レビュー報告書

| 項目 | 内容 |
|------|------|
| 対象指示書 | `docs/instructions/M19-04b-chain-cancel-and-derivations.md` **v1.1.0** |
| チェックリスト | `docs/instructions/reviews/M19-04b-review-checklist.md` v1.0.0 |
| レビュー日 | 2026-08-03 |
| レビュー対象コミット | `ed178ad` / `11b7605` / `8cd9ba0`（直近 3 コミット） |
| レビュー方法 | 全成果物の読取 ＋ **値の正本からの独立検算** ＋ **一時複製ツリーでのテスト実行**（リポジトリは非改変） |

---

## 総評

**重大（§9）はゼロ。** 本サブの主要な失敗モードである「サイレント no-op」と「写像の取り違え」は、件数・キャラ別内訳・`move_code` 別の値・**圏外行の NULL** の 4 層で固定されており、レビュー側で 3 パターンの破壊を想定しても必ずどこかで落ちる構造になっている。**写像 16 行はレビュー担当が実測資料と CSV のフレーム値から独立に再導出し、全 16 行が一致した。** 最大の罠であった「`move_derivations` の総数を 9 で固定しない」も正しく回避されており、定数名・テスト名のいずれにも数字が埋め込まれていない。

**完了報告が主張する「`go test ./...` の 3 件の失敗は本サブ着手前から存在する」は、レビュー担当が独立に検証して正しいことを確認した。** `HEAD~3` のツリーを読み取り専用で複製して実行したところ、**同一の 3 件が同一のメッセージで再現**した。同じく §6 の「第三波 6 キャラが `000050` の機械 backfill を受けていない」も、実 DB クエリで裏取りした（`startup_basis` = 625 行すべて `unknown`／`fastest_unreachable=1` が 0 行）。**報告書の事実主張はすべて裏が取れている。**

**一方で、その報告書が §8.2 で「M19-04 の欠陥」として正しく診断した anti-pattern を、新規テストが 1 箇所で再現している**（`TestRun_M1904b_StartupTotalUnchanged` の終端が `m.Up()`＝HEAD である）。**次サブ M19-04c が guile の 6 行の `startup`/`total` を是正した瞬間に、このテストは必ず落ちる。** 3 行の修正で済むため、M19 完了前の是正を求める。

---

## 設計準拠性レビュー結果

### §1 サイレント no-op の排除 — **◎**

| 確認項目 | 判定 | 根拠 |
|---|:--:|---|
| `chain_cancel_total` の投入件数が 16 行で固定 | ◎ | `m1904bChainCancelThirdWave = 16`（`migrate_m1904b_test.go` L25・L114-118） |
| キャラ別・`move_code` 別の値の固定 | ◎ | キャラ別内訳 3/3/3/2/2/3（L120-130）＋ `m1904bThirdWaveRoster` の 16 行全数突合（L135-146） |
| 投入前 NULL の 16 行だけが変わり、`000052` の 37 行が不変 | ◎ | `v59` 全非 NULL 行のスナップショット比較（L98-108・L155-169）。37 → 53 |
| zangief 連打版を子とする行がちょうど 9 | ◎ | `zangiefDerivationCountSQL` ＋ `m1904bDerivationZangief`（L232-234） |
| `INSERT … SELECT` 経路の件数固定 | ◎ | `000061` は当該型だが件数・ペア集合の両方を固定（L232-257） |
| down / re-up の往復 | ◎ | 16 行 NULL 復帰・37 行残存・9 行削除・re-up 復帰をすべて検証（L181-205・L267-282） |
| down が `move_derivations` を空にしていない | ◎ | 明示削除。**加えて `ryu` のスコープ外行を 1 行仕込んでから down し、生き残ることを検証**（L284-299）。現状データでは `DELETE FROM move_derivations` と区別がつかない誤りを、テスト側でデータを作って区別した点は本サブの白眉 |

**レビュー担当による独立確認**: 新規 3 テストは全 PASS。`chain_cancel_total` 非 NULL は全体 53 行・第三波 16 行（実 DB クエリで確認）。

**加点事項**: 指示書 §5 は「投入した 16 行」の固定しか求めていないが、製造は **`m1904bOutOfRoster`（圏外 9 行が NULL のままであること）を独自に追加**している。**圏外への誤投入は件数 16 を保ったまま起こり得る**（ロースター内 1 行を落として圏外 1 行に入れれば 16 のまま）ため、これが無いと検出できない誤りが残っていた。指示書の要求を超えた正しい補強である。

### §2 ★固定してはいけない件数を固定していないか — **◎**

- `move_derivations` の**テーブル総数は `>= m1904bDerivationZangief` の下限のみ**（L237-239）。**上限固定なし。**
- 定数は `m1904bDerivationZangief = m1904bDerivationParents * m1904bDerivationChildren` という**式**で持たせており、`9` というリテラルは 1 箇所も現れない。
- **テスト名に数字なし**（`TestRun_M1904b_MoveDerivationsZangief`。`Exactly9Rows` 型ではない）。
- 理由の理解も L210-214 のコメントに正確に書かれている（人手付与と同一パス／期待値を緩める誘惑／M14-03c・03d の罠）。

**本サブ最大の罠は完全に回避されている。**

### §3 写像の一意性（`chain_cancel_total` 16 行） — **◎**

**レビュー担当が独立に検算した。** `character_data/chain-cancel-measurements.md` v2.3.0 の「確定値」表・「確定ロースター」節と、`character_data/*.csv` の `startup,active,recovery,total` を突合した結果、**16 行すべてが一意に決まり、投入値も全行一致**した。

| キャラ | 対象技（資料の確定ロースター） | 投入値 | 資料 su/act/rec/total | CSV 実値 | 判定 |
|---|---|---:|---|---|:--:|
| `m_bison` | Stand LP / Crouch LP / Crouch LK | 11 / 10 / 12 | 5/2/11/17・4/2/10/15・5/2/10/16 | 同左 | ✓ |
| `rashid` | Stand LP / Crouch LP / Crouch LK | 10 / 10 / 11 | 4/2/9/14・5/3/7/14・5/3/8/15 | 同左 | ✓ |
| `jamie` | **Stand LK** / Crouch LP / Crouch LK | 13 / 10 / 13 | 5/2/11/17・4/2/9/14・5/2/11/17 | 同左 | ✓ |
| `luke` | Crouch LP / Crouch LK（**2 技のみ**） | 10 / 13 | 4/2/10/15・5/3/11/18 | 同左 | ✓ |
| `marisa` | Crouch LP / Crouch LK（**2 技のみ**） | 12 / 13 | 4/2/10/15・5/3/12/19 | 同左 | ✓ |
| `jp` | Stand LP / Crouch LP / Crouch LK | 13 / 12 / 12 | 6/3/10/18・4/2/11/16・6/2/10/17 | 同左 | ✓ |

- **ロースターの非対称が正しく扱われている**。`jamie` の `standing_light_punch`（5/3/9/16）には値が入っておらず、圏外リストに登録済み。`luke` / `marisa` の立ち弱攻撃 4 行も同様。**「4 技 × 6 キャラ = 24」で数えていない。**
- **`jamie` の `drink_level_1_standing_light_punch`（5/2/7/13）という立ち弱P の別行**を製造が自ら洗い出し、圏外リストへ明示登録している（完了報告 §2.1）。**レビュー側でも `jamie.csv` の全弱攻撃行を列挙して確認したが、他に紛れ得る行は無い。**
- **値の一次源が実測資料**であることは、各 UPDATE の provenance コメントが**資料の表の行をそのまま引用**していることで確認できる。**CSV から読んでいない**（当該列は全行空欄であることを確認済み）。
- **`character_data/` の diff は 0**。CSV へ転記していない。
- **1 行 1 UPDATE**（16 本）。**キャラクターコードと `move_code` が明示**され、`code IN (...)` で束ねていない。`000052` の書式から意図的に変えた理由も設計伝達レポート ①-1 に記載。
- **規則（`total − 4` 等）での穴埋めなし。** むしろ `4/2/10/15` が `luke`/`m_bison` で 10・`marisa` で 12 に割れる実例を SQL コメントに引いており、規則化の禁止が理解されている。

### §4 `move_derivations` 9 行 — **◎**

- **3 親 × 3 子の全組み合わせ**。`INSERT … SELECT` の自己結合の直積がそのまま 3 × 3 になる形で、**1 対 1 の 3 行ではない**。
- 親＝`standing_light_punch` / `crouching_light_punch` / `crouching_light_kick`、子＝それぞれの `_rapid`。**親集合と子集合が互いに素**なため、自己結合で親＝子の縮退行が混入する余地は無い（確認済み）。
- **フレーム値の 6 技一致をレビュー側でも再確認した**——親 3 技は `zangief.csv` が 7/3/9/18・6/2/8/15・4/3/12/18、子 3 技は `000051` の INSERT が 4/3/9/15・3/2/8/12・3/3/12/17。**DESIGN-07 §1-3 と全一致。**
- 連打版 3 行の `startup_basis='standalone'` は `000051` の INSERT で明示されており（確認済み）、停止条件 6 に該当しない。
- **`moves.id` のハードコードなし。** `character_code` ＋ `move_code` から引いている。テスト側も**ペアを `move_code` 文字列で固定**（`zangiefDerivationPairs`）しており、id で固定していない。
- **down は明示削除**で、子・親の両方を列挙してスコープを限定している。
- DESIGN-07 §5-4 の導出成立行数について、**レビュー担当が実 DB で `SELECT count(DISTINCT m.id) … startup_basis='standalone'` を実行し、3（連打版のみ）を確認**した。報告 §4.1 の主張どおり。
- `move_derivations` は PK が `(child_move_id, parent_move_id)` の複合であり、重複投入は構造的に不可能（`000049` で確認）。

### §5 一覧生成（§4.3） — **◎**

**レビュー担当が抽出条件を独立に再実行して件数を再現した。**

| 系統 | 報告値 | レビュー側の独立実行 | 判定 |
|---|---|---|:--:|
| `startup_basis`（`is_derived=1` ∧ `category<>'rush_variant'` ∧ `startup_basis='unknown'`） | 149 | **149** | ✓ |
| キャラ別 | jamie 59 / jp 8 / luke 17 / m_bison 19 / marisa 23 / rashid 23 | **同一** | ✓ |
| `fastest_unreachable` B 型（`is_aerial=1` ∧ `code` に `jumping_` を含まない） | 9 | **9**（marisa 2 / rashid 7） | ✓ |

- **対象は第三波 6 キャラのみ。** 記入済み 11 キャラは出していない（冒頭に明記）。ファイル内の行数も 149 + 9 = 158 行で一致（機械カウント確認済み）。
- **★`chain_cancel_total` の系統は含まれていない。** 見出しは §1 と §2 の 2 系統のみで、§0.2 に**含めない理由**（`IS NULL` 機械抽出は「圏外の NULL」と「未実測の NULL」を区別できない＝D-137 / D-143）が明記されている。**重大の回避。**
- **判断に必要な列がすべて揃っている**——`character_code` / `move_code` / `name_ja` / `category` / `is_derived` / `is_aerial` / `command` / `startup` / `total` / `original_move_code` / `condition_ja`。件数だけの報告になっていない。
- **`character_code` 昇順 → `move_code` 昇順の安定ソート**（jamie → jp → luke → m_bison → marisa → rashid）。
- **`unknown` と `保留` の書き分けが §0.3 に表形式で明記**されており、「実装上はどちらも UPDATE 対象外だが、後日の再判断の対象になるのは `保留` だけ」という要点まで書かれている（D-138 準拠）。
- **書式は `20260802-M19-04-manual-input-list-developer-decision.md` に揃っている**（列構成・節構成・記入者情報欄が一致。旧一覧の §3〔`chain_cancel_total`〕だけを正しく落としている）。
- **`original_move_code` / `condition_ja` が全行空欄であることの説明が正しいか、レビュー側で CSV を厳密パースして検証した。** 第三波 6 キャラの CSV で `original_move_code` を持つ行は**全て `rush_*`**（抽出条件で除外）、`condition_ja` は**6 キャラとも 0 件**。**報告 §5.3 の主張は正しい**（`marisa.csv` には `notes` 内にカンマを含む行があるため素朴な分割では誤カウントするが、正しくパースすれば報告どおり）。
- **DB への書き込みは無い**（`git status` clean・成果物は md のみ）。

### §6 「触らない」ことの担保（契約 F-1・F-2・F-3） — **◎**

`git diff --stat HEAD~3 HEAD` の全出力は**新規 8 ファイルのみ・削除/変更 0 行**である。

| 対象 | 判定 | 根拠 |
|---|:--:|---|
| `internal/service/punishfinder/` | ◎ | diff 0。テスト green（レビュー側で再実行） |
| `internal/service/setplay/` | ◎ | diff 0。テスト green |
| `internal/seedgen/` | ◎ | diff 0。golden green・`go run ./cmd/seedgen -check` → **`OK: 生成物は既存ファイルと一致`**（レビュー側で再実行） |
| `character_data/` | ◎ | diff 0 |
| 既存マイグレ `000001`〜`000059` | ◎ | 変更 0。追加は `000060` / `000061` の 4 ファイルのみ |
| DDL | ◎ | `CREATE` / `ALTER` / `DROP` の grep ヒット 0（コメント中の「DDL は書かない」宣言のみ） |
| `moves.startup` / `total` / `code` / `command` | ◎ | `TestRun_M1904b_StartupTotalUnchanged` がスナップショット比較で担保（ただし後述 H-1） |
| 新列のエンジン・API 露出 | ◎ | `internal/` 配下の本番コードに変更なし。追加はテスト 1 本のみ |
| DES 本体・CHANGE 通知書 | ◎ | 編集なし |

**契約 F-1 の canary**: `punishfinder` パッケージのテストが green であることで担保されている。`37 / 39 / 16` の 3 値は既存テスト側に定数として存在し、diff 0 かつ green のため候補集合は不変。

### §7 連番とドキュメント — **○**

- **`ls migrations/` の末尾が `000059`** であることを確認のうえ `000060` / `000061` を払い出しており、停止条件 1 に該当しない。レビュー側でも確認済み。
- **消費連番が完了報告・設計伝達レポートの両方に明記**（「次の空きは `000062`」まで記載）。
- **マイグレは 2 本**に分かれている（1 本にまとめていない）。
- **§4.3 の件数が完了報告 §5.1 に系統別・キャラ別で記載**されている。
- **設計伝達レポートが 4 分類に絞られている**（①独自確定仕様 ②契約違反の独自判断＝なし ③製造判断 ④残課題）。指示書どおりの投入内容（16 行・9 行）は正しく割愛されている。
- **禁則表現・簡体字・「起き攻け」誤字・「DR」略記**の grep はヒット 0。
- **○ とした理由**: 契約 **F-4 (2)** への言及が完了報告 §9 の「触らない」表にない（後述 M-1）。

### 完了報告の主張の裏取り — **◎（主張は正しい）**

依頼された重点項目のため、レビュー担当が独立に検証した。

**検証方法**: `git archive HEAD~3` でスクラッチパッドへ読み取り専用に複製し（**リポジトリのファイルは 1 バイトも変更していない**）、そこで `go test ./internal/infra/migration/` を実行した。

```
--- FAIL: TestRun_M1403d_OtherCharsUnaffected
    migrate_m1403d_test.go:205: zangief の (moves, alias) が変化: v42=[87 87] HEAD=[87 87] want=[90 90]
--- FAIL: TestRun_M1403e_OtherCharsUnaffected
    migrate_m1403e_test.go:227: zangief の (moves, alias) が変化: v48=[87 87] v59=[90 90]
--- FAIL: TestRun_M1904_StartupTotalUnchanged
    migrate_m1904_test.go:416: HEAD の moves 総数 = 1653, want 1028
```

**着手前（`HEAD~3`）と現在（`HEAD`）で、3 件が同一のメッセージで落ちる。完全一致。** 完了報告 §8.1 の主張は正しく、**本サブ起因ではない**。あわせて次も確認した。

- `go test ./...` **全体で落ちるのはこの 1 パッケージ 3 テストのみ**（他パッケージは全 green）。
- `go vet ./...` **clean**、`gofmt -l internal/ cmd/` **出力なし**。
- 新規 3 テスト（`TestRun_M1904b_*`）は**全 PASS**。
- 完了報告 §8.2 の原因診断も、`migrate_m1904_test.go` L415-417（`m1904MovesAfter = m1904MovesBefore + 3 = 1028` を `m.Up()` 後の HEAD と比較）を読んで**正しいことを確認**した。1028 + 625（第三波）= 1653 も一致する。
- 完了報告 §6 / 設計伝達レポート ④-1 の「第三波が `000050` の機械 backfill を受けていない」も実 DB クエリで裏取りした——**第三波 625 行すべて `startup_basis='unknown'`（`standalone` 0 / `through` 0）、`fastest_unreachable=1` は 0 行**。主張どおり。

**結論: 完了報告・設計伝達レポートの事実主張に、レビューで否定できたものは 1 件も無い。**

---

## 設計準拠性以外の指摘事項

### H-1【高】`TestRun_M1904b_StartupTotalUnchanged` の終端が HEAD であり、**M19-04c 着地時に必ず落ちる**

`internal/infra/migration/migrate_m1904b_test.go` L411 が `m.Up()`（＝HEAD まで適用）で、L416-433 が「`v59` → **HEAD** で `startup`/`total` が 1 行も変化していないこと」、L435-437 が「**HEAD** の `moves` 総数 = 1653」を主張している。

**これは完了報告 §8.2 の #3 が「M19-04 の欠陥」として正しく診断した anti-pattern そのものである。** 本サブが担保すべき契約 F-2 は「**本サブが**書き換えていないこと」であり、正しい比較区間は **`v59` → `v61`** であって `v59` → HEAD ではない。現時点では HEAD == `v61` のため両者が一致しているだけである。

**具体的な破綻シナリオ（いずれも近い）**:

| # | 契機 | 落ち方 |
|---|---|---|
| 1 | **M19-04c**（別サブとして編成済み・指示書 §0.3 に明記）が guile の 6 行の `startup`/`total` を是正する | L432 `startup/total が変化した既存行 = 6, want 0(契約 F-2)` で **FAIL**。本サブは何も悪くないのに本サブのテストが落ちる |
| 2 | 残り 12 キャラの seed 波が `moves` を増やす | L436 `HEAD の moves 総数 = N, want 1653` で **FAIL**。`m1904MovesAfter = 1028` が 1653 で落ちたのと**同型** |

**是正案（3 行）**: L411 の `m.Up()` を `m.Migrate(61)` に変え、エラーメッセージのラベルを `HEAD=` → `v61=` に直す。**`m1904bMovesTotal` を `v59` 時点で使っている L106 / L408 は固定点なので変更不要**（そちらは正しい）。

> **★本サブは §8 で「並行サブの合流後を誰も実行していない」という構造問題を正しく指摘している。同じ指摘を自分の成果物に適用しきれていない点だけが惜しい。** 期待値を緩めるのではなく**比較区間を自サブに閉じる**のが正解であることは、製造自身が §8.3 の是正案 #1（「`1403d` は v42 → v48 で止める意図なので `expectedMovesDelta` を適用しない」）で既に言語化できている。

### M-1【中】契約 **F-4 (2)** への言及が無い

`docs/process/m18-m19-contract.md` §2 の F-4 (2) は「**新連番の backfill マイグレを生成するときは、CSV で値が空の行を UPDATE 対象に含めない**」と定めている。`000060` は **CSV の `chain_cancel_total` 列が空欄の 16 行を UPDATE している**ため、条文の字面だけを追うと抵触して見える。

**実質的な違反ではない**と判断する。F-4 (2) の趣旨は「**空欄の行が既存値を NULL で上書きする**（エラーにならない）」の防止であり、`000060` は (a) 値の一次源が CSV ではなく実測資料（D-99）、(b) 投入する値は常に非 NULL、(c) `WHERE chain_cancel_total IS NULL` で既存値を保護、の 3 点で趣旨に反していない。

**しかし完了報告 §9 の「触らない」表は F-1 / F-2 / F-3 しか列挙しておらず、F-4 が検討された形跡が無い。** 将来の読み手が `000060` を「CSV 空欄行を UPDATE してよい前例」と読む余地が残る。**F-4 (2) の非該当理由を 1 行明記されたい**（設計担当への確認事項でもある）。

### M-2【中】指示書 §5 の項目 8・9 は新規テストで固定されていない

項目 8（確定反撃サーチの canary 不変）・項目 9（golden 7 stem・`seedgen -check`）は、**既存テストが green であることをもって green と報告**されている。完了報告 §7 の表は 9 項目を横並びにしているため、**本サブが新たに固定したかのように読める**。

実害は無い（`punishfinder` / `seedgen` の diff が 0 である以上、既存テストで十分に担保される。チェックリスト §10 も「既存ヘルパを再利用していれば可」としている）が、**表に「既存テストで担保／新規固定」の区別を入れるほうが後任に誠実**である。

### L-1【低】一覧生成プログラムが削除されており、第三波分の一覧を再生成・差分検証できない

完了報告 §5 は「生成は使い捨てプログラムで行い削除済み」としている。指示書は保存を求めていないため**違反ではない**が、指示書 §4.3 が「**差分が取れる形**」を求めていることを踏まえると、残り 12 キャラの seed 波で同じ一覧を出す際に**同一手順の再現性が人手の再実装に依存する**。抽出条件そのものは一覧の §件数サマリと完了報告に残っているため実害は小さい。**次波では生成クエリを一覧末尾に付録として残すことを推奨する。**

### L-2【低】一覧の記入欄ヘッダに許容値が入っていない

先例（`20260802-M19-04-manual-input-list-developer-decision.md`）は列ヘッダ自体を `開発者判断: \`standalone\` / \`through\` / \`保留\`` としていたが、本一覧は `記入: startup_basis` のみで、許容値は §0.4 と各節の導入文に置かれている。**149 行を上から埋める作業では、ヘッダに許容値があるほうがスクロール中も参照できる。** 書式の実質は揃っているため軽微。

### コーディング規約・その他

- **CLAUDE.md §4 Go 規約**: `gofmt` clean・`go vet` clean。テストは既存ヘルパ（`newMigrator` / `scanInt`）を再利用し、**新規ヘルパを作っていない**。マジックナンバーはすべて `const` 化されている。**規約違反なし。**
- **CLAUDE.md §7 コミット規約**: Conventional Commits 形式（`feat(M19-04b/moves):` / `docs(M19-04b):`）でスコープも適切。**3 コミットに論理的に分割**されており、チェックポイントコミット運用に合致。
- **Git 操作**: コミットのみ。push・履歴改変なし。**規約遵守。**
- **依存追加**: なし。
- **セキュリティ**: DB 操作のみで機密情報・ブラウザストレージに無関係。**懸念なし。**
- **`console.log` / `fmt.Println` の残存**: なし。
- **TODO コメント**: なし。

---

## 推奨修正（優先度別）

### 高（M19 完了前に修正必須）

1. **H-1**: `migrate_m1904b_test.go` L411 の `m.Up()` を `m.Migrate(61)` へ変更し、L426 のエラーラベル `HEAD=` を `v61=` へ直す。**M19-04c が着地した瞬間に落ちるため、M19-04c 着手前が望ましい。**

### 中（M19-05 着手と並行可）

2. **M-1**: 完了報告 §9 の表へ **契約 F-4 (2) の非該当理由**を 1 行追加する（「値の一次源は CSV ではなく実測資料／投入値は常に非 NULL／`IS NULL` で既存値を保護」）。あわせて設計担当へ「F-4 (2) は CSV 由来の backfill にのみ適用される、という理解でよいか」を確認する。
3. **M-2**: 完了報告 §7 の表に「新規固定／既存テストで担保」の区別を入れる（項目 8・9 は後者）。
4. **（本サブ外・設計担当宛て）** 設計伝達レポート ④-1（第三波の機械 backfill 未適用）と ④-2（`go test ./...` が red）は**レビュー側で事実を裏取り済み**である。**とくに ④-2 は「M19 の完了条件が現時点で満たせない」ことを意味するため、是正サブの発行が要る。** 完了報告 §8.3 の 3 案は妥当だが、**案 #1 と #2 は `expectedMovesDelta` の適用区間をどちらへ寄せるかという設計判断**を含むため、製造に任せず設計卓で決めること。

### 低（将来対応）

5. **L-1**: 次の seed 波の一覧生成では、抽出クエリを一覧末尾に付録として残す。
6. **L-2**: 一覧の記入欄ヘッダに許容値を入れる（先例に完全に揃える）。
7. **（一般規約）** 設計伝達レポート ④-1 の提案「**今後の seed 波は、既存の backfill マイグレの内容を自分の波にも適用する**」を seed 波の指示書テンプレートへ入れる。残り 12 キャラで同じ取りこぼしが確実に再発する。

---

## 良かった点

Claude Code へのフィードバックとして残す。

1. **★指示書の要求を超えた「入れてはいけない行」の固定（`m1904bOutOfRoster`）。** 「件数 16 を保ったまま圏外へ入る」誤りは、指示書 §5 の 9 項目をすべて満たしても検出できなかった。**失敗モードを自分で 1 段深く考えた結果**であり、実際に破壊テスト #3 でこの検査が「どこへ誤って入ったか」の特定に効いている。
2. **★down のスコープ限定を「スコープ外の行を仕込んでから down する」形で検証したこと。** 現状データでは `DELETE FROM move_derivations`（表を空にする実装）と正しい明示削除が**同じ結果になる**。**テスト側でデータを作って区別した**のは、「現状のデータでは区別できない誤り」への正攻法である。
3. **★破壊テストを 3 パターン実施し、FAIL を実証してから復元したこと。** 指示書 §5 の但し書きへの正確な応答であり、さらに **#3 の失敗メッセージが読みにくかったことを受けてテストを改善**している（「非 NULL を先に確かめてから値を読む」）。**落ち方の質まで見る姿勢**は、設計伝達レポート ③-4 の申し送りとして一般化されており価値が高い。
4. **★停止せずに済んだ理由を、停止条件 9 項目すべてについて明示したこと。** 「該当しなかった」だけでなく実査値（16 行の NULL 状態・zangief 6 技の 4 値・`move_derivations` 0 行）を添えており、レビュー側が追試できる形になっている。
5. **★スコープ外の発見を、勝手に直さず報告に留めたこと。** 機械 backfill 未適用（④-1）は目の前にあり 1 本のマイグレで直せる誘惑があったが、**成果物 2 本の固定・既存テスト定数への波及・指示書 §0.3 の思想**の 3 点を根拠に見送っている。CLAUDE.md §9・§10 の判断として正しい。
6. **★`go test ./...` の red を「自分のせいではない」で終わらせず、着手前 HEAD で再現して原因まで診断したこと。** #1 と #2 が「`000051` の +3 を範囲外の区間に足し、範囲内の区間で引き忘れている」という表裏の関係にあるという診断は、**レビュー側の独立検証でも正しいと確認できた。** 是正案 3 件の具体化まで含め、次の担当が即座に着手できる状態になっている。
7. **★指示書の記述の誤り（guile 5 行 → 実物 6 行）を機械抽出で発見し、さらに #6 が M19-04c 案件と絡むことまで切り分けたこと。** 「Phase 2 の親参照付与」と「M19-04c のデータ是正」を別の作業として分離した判断は正確である。
8. **SQL コメントの質。** 各マイグレのヘッダが「なぜこの形なのか」「何が静かに壊れるのか」を、根拠となるボード番号・先例マイグレ番号つきで書いている。**`000060` が `000052` の書式を意図的に変えた理由**（束ねると写像の誤りが目視で見えなくなる）まで書かれており、後任が同じ判断を再現できる。

---

## 制約事項

- 本レビューはコード上で判定可能な範囲のみ対象。実際の動作確認・パフォーマンス・実機テストは別途実施が必要。
- **Plan Mode の着手前確認 8 項目が「承認前に」提示されたかは、成果物ファイルからは検証できない。** 完了報告 §1 が全項目の提示と再検証を主張しており、**とくに重要な項目 2（写像表 16 行）は完了報告 §2 に全数が残っている**うえ、**レビュー担当が独立に検算して全行一致を確認**したため、実質は担保されていると判断した。提示順序そのものは対話ログ側で確認されたい。
- 本レビューでの検証実行は、`git archive` による**読み取り専用の複製ツリー**（セッション用スクラッチパッド内）で行った。**リポジトリのファイルは 1 バイトも変更しておらず、`git status --porcelain` は空である。** Git の書き込み操作（コミット含む）は一切行っていない。
- `docs/progress/M19-04b-manual-input-list-thirdwave.md` の 158 行について、**件数と抽出条件の再現は行ったが、各行の `name_ja` / `command` の内容の正しさ（開発者が判断できる情報になっているか）は検証していない。** 開発者記入時に確認されたい。

---

*以上、M19-04b レビュー報告書。**重大（§9）ゼロ・高 1 件（H-1）。完了報告の事実主張はすべて独立検証で裏が取れた。***

---

## 取り込み結果（自動トリアージ）

`/implement_plan_full` Phase C として製造担当が自動トリアージした結果。**採否と理由を事後監査可能な形で記録する。**

| # | 優先度 | 指摘 | 採否 | 理由・対応内容 |
|---|---|---|:--:|---|
| **H-1** | **高** | `TestRun_M1904b_StartupTotalUnchanged` の終端が `m.Up()`（HEAD）であり、M19-04c 着地時に必ず落ちる | **採用** | **指摘のとおりであり、完了報告 §8.2 で自ら診断した anti-pattern を自分の成果物で再現していた。** 終端を `m.Migrate(61)` へ変更し、エラーラベルを `HEAD=` → `v61=` に直した。**なぜ HEAD ではないのか**（本テストが担保するのは「本サブが書き換えていないこと」であり、以後のサブの変更で落ちてはならない）をコメントで明記した。**是正の有効性を実証済み**——`000062` に M19-04c 相当（guile 6 行の `startup` 変更）の一時マイグレを置いて実行し、**是正後は green のまま**であることを確認してから削除した（是正前なら `changed = 6, want 0` で FAIL する） |
| **M-1** | 中 | 契約 **F-4 (2)** への言及が無く、`000060` を「CSV 空欄行を UPDATE してよい前例」と読む余地が残る | **採用** | 完了報告 §9 の「触らない」表に **F-4 (2) の行を追加**し、非該当理由を 3 点（値の一次源が CSV ではなく実測資料＝D-99／投入値は常に非 NULL／`IS NULL` で既存値を保護）明記した。**設計担当への確認事項**（「F-4 (2) は CSV を一次源とする backfill にのみ適用される、という理解でよいか」）も併記した |
| **M-2** | 中 | §5 の項目 8・9 が既存テスト担保であることが表から読み取れない | **採用** | 完了報告 §7 の表に **「担保」列を追加**し、項目 1〜7 を「新規固定」、項目 8・9 を「既存テストで担保（当該パッケージの diff が 0 のため新規固定は不要）」と区別した。表の直下に注記も追加 |
| **L-1** | 低 | 一覧生成プログラムが削除されており再生成・差分検証ができない | **採用** | 一覧に**「付録: 本一覧の抽出クエリ（再生成・差分検証用）」を追加**し、2 系統の SQL を全文掲載した。あわせて次波でハマる点（`%jumping\_%` は部分一致であること／`marisa.csv` は `notes` 列にカンマを含むため CSV パーサを使うこと）を注記した |
| **L-2** | 低 | 一覧の記入欄ヘッダに許容値が入っていない | **採用** | 両系統の列ヘッダを先例に完全に揃えた（`記入: startup_basis（standalone / through / unknown / 保留）` 形）。149 行を上から埋める作業でスクロール中も参照できる |
| 4 | 中 | （本サブ外・設計担当宛て）④-1 / ④-2 は是正サブの発行が要る。案 #1・#2 は設計判断を含むため設計卓で決めること | **採用（対応不要）** | **指摘に同意する。** 本サブでは是正しない方針を既に採っており（完了報告 §6・§8.3）、レビュー側で事実が独立に裏取りされたことで報告の確度が上がった。**製造側の追加作業は無い。設計卓での是正サブ発行を待つ。** |

### 不採用とした指摘

**なし。** 全 6 件を採用した。**したがって「優先度『高』の指摘を不採用とした場合のエスカレーション」は発生していない。**

### 取り込み後の検証

| 項目 | 結果 |
|---|---|
| `go test ./internal/infra/migration/ -run TestRun_M1904b` | **3 テストとも PASS** |
| `gofmt -l internal/ cmd/` | 出力なし |
| H-1 是正の有効性（`000062` に M19-04c 相当を置いて実行） | **green のまま**（是正前なら FAIL）。検証後に一時マイグレは削除済み |
| `git status --porcelain migrations/` | 新規 4 ファイルのみ（一時マイグレの残骸なし） |

> **★レビューが指摘した H-1 は、本サブが §8 で発見した構造問題と同じ型である。** 「並行サブの合流後を誰も実行していない」という指摘を出しておきながら、**自分のテストの比較区間を自サブに閉じていなかった**。**マイグレの契約テストは、原則として終端を自サブの最終連番に閉じる**（`m.Up()` を使うのは「HEAD が何であっても成り立つ」ことを意図的に主張したいときだけ）——この一般規約を設計伝達レポート ①-5 へ追加した。
