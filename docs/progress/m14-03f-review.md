# M14-03f レビュー報告書

| 項目 | 内容 |
|------|------|
| 対象 | `docs/instructions/M14-03f-fourth-wave-seed.md` v1.0.0 / 完了報告 `docs/progress/M14-03f-completion-report.md` |
| 着手基点 | `78f4981`（`git diff --stat 78f4981` = 38 files / 9719 insertions / 89 deletions） |
| 実施日 | 2026-09-02 |
| 実施担当 | レビュー担当 Claude Code（`/review_plan`・コード変更なし） |
| 観点の出所 | **対のレビューチェックリスト `docs/instructions/reviews/M14-03f-review-checklist.md` は存在しない**（指示書ヘッダ「着手承認後に設計卓が起こす」・未作成）。⇒ 指示書 §5 完了条件と §3 出力規約を観点として使った |
| 実測環境 | スクラッチ領域に使い捨て SQLite を作り、`migrations/*.up.sql` 90 本を連番順に適用して照合（python3 `sqlite3`）。リポジトリは 1 バイトも変更していない |

---

## 総評

**数字の再現性が非常に高い。** 完了報告の主要な件数（`characters` 31 ／ 第四波 `moves` 1390 ／ `preset_aliases` 7597 ／ `move_commands` 737 ／ `is_projectile` 53 ／ `is_derived` 519 ／ `startup_basis` 964/380/46 ／ `fastest_unreachable` 118 ／ 取り残し 70・13・0・0）を独立に作った DB ですべて再現でき、機械規則との突合 279 行・内訳 131/110/28/10 も CSV から規則を再実装して完全に一致した。**down → re-up も実測で健全**（19 キャラ / 1653 moves / 4184 aliases / 898 move_commands へ正確に戻り、orphan は 0、仮登録 2 体の資産は残る）。§0.2 の判定も支持できる。

**一方で「実装は正しいが、書かれた根拠が実物と食い違う」型が 5 件ある。** 本プロジェクトの較正では「高」に当たる型であり、しかも本サブは seed 波＝**マイグレのヘッダが実質的な設計記録**であるため影響が大きい。とくに (1) `move_derivations` の down 判断がどのヘッダにも書かれていないのに「書いた」と報告している（完了条件 8 未達）、(2) 段 9 の「28 行」の理由が 23/28 にしか当たらないまま 4 か所へ複製済み、の 2 件は次の波が確実に踏む。

**テストは弱まっていない。** 前提が消えた 2 件は「消す」ではなく fixture 化で守り、対テストまで足している。E2E 3 本の書き換えも失った被覆を明示して followup へ回しており、扱いは正しい。ただし **19 → 31 の波及調査に 3 か所の取りこぼし**（`service/intake` のテスト、`character-sort.ts` の受容範囲注記、`code-facts.md`）がある。

---

## 設計準拠性レビュー結果

### 完了条件 1: §0.2 の判定結果 — ◎

**判定「成立しない（必ず最終波の制約は失効）」は妥当。レビュー側で再現できた。**

| 検証したこと | 実測 |
|---|---|
| 14 CSV に移動 9 code の行があるか | **0 行**（`forward` / `back` / `micro_*` / `dash_*` / `jump_*` すべて）⇒ `UNIQUE(character_id, code)` に当たらない |
| `000075`（`UNIQUE(preset_id, character_id, alias_text)` ほか）を含む全マイグレ適用 | **90 本すべて成功。UNIQUE 違反 0 件** |
| `drive_parry` の扱い | CSV に 14 行あり、`000084` を通過（ヘッダの記述どおり） |

報告 §1 の手順（生成物をスクラッチに作り、`000075` 込みのクリーン DB へ適用）は判定として十分であり、結論も正しい。仮登録 2 体を削除せず更新で足した判断（指示書 §4-7）も守られている。

### 完了条件 2: 13 段 ＋ 条件付き 3 段が 1 段ずつ埋まっているか — ◎

16 段すべてに「投入した／落とした／不要だった／段 9 に内包した」が付いている。落とした段（7 / `custom_states` / `move_derivations`）は理由と NULL 行数が名指しで書かれており、指示書 §0.3 の「★『あとで』と書かない」を満たす。**段 12/13 が「不要だった」である根拠（`-noinput-only` は出力を絞るだけで、`M20-06` 以降の波では通常実行に含まれる）も `generate_m2002.go` の `runAliases` を読んで確認した。正しい。**

### 完了条件 3: 取り残しの列ごとの計数 — ○

**数字はすべて実測と一致した。**

| 列 | 報告 | レビュー実測 |
|---|---:|---:|
| `is_projectile=1` | 53 | **53** |
| `is_derived=1` | 519 | **519** |
| `startup_basis='unknown'` | 46 | **46** |
| `fastest_unreachable=1` | 118 | **118** |
| 移動 5 code の `total` NULL | 70 | **70** |
| `chain_cancel_total` 非 NULL | 2 | **2** |
| `custom_states` NULL（第四波 14 キャラ） | 13 | **13** |
| `move_derivations`（第四波） | 0 | **0** |
| `preset_aliases.character_id` NULL | 0 | **0** |
| alias 不在 move（numeric / srk） | 358 / 345 | **358 / 345** |

「意図した未投入」と「取り残し」の弁別も、判定としては正しい。**ただし表の書き方が結論と噛み合っていない**（下記 中-4）。列見出しが「意図した数か」なのに 4 行が「★いいえ（意図した未投入）」と答え、直後に「意図しない取り残しは 0 件」と締めるため、表と結論が矛盾して読める。

### 完了条件 4 / 5: 対象キャラの moves ／ `elena` の `chain_cancel_total` — ◎

14 キャラすべてが `moves` を持つ（`aki` 81 〜 `blanka` 127。仮登録 2 体も `c_viper` 98 / `dhalsim` 106 で攻撃技を獲得）。`elena` の 2 行（`crouching_light_punch`=10 / `crouching_light_kick`=12）も投入済みで、`migrate_m1403f_test.go` が**値そのもの**を固定している。`000088` のヘッダが「一次源は `chain-cancel-measurements.md` であって CSV ではない」「技名→`move_code` の写像を単発値の一致で確認した」まで書いており、質が高い。

### 完了条件 6: 3 プリセット ＋ P-34 の再適用 — ○

`official_ja_move` 3025 ／ `numeric` 2272 ／ `srk` 2300 を実測。層 C-3 の 5 件が `000090` / `000091` に含まれることも確認した（別マイグレを起こしていないのは正しい判断）。**ただし完了条件が明示的に求めた「投入前後で数えて差を書くこと」の“差”が数値として書かれていない**（4184 と 7597 が別々の節に散っている。差 = 3413）。下記 低-5。

### 完了条件 7: `seed-progress.md` の 8 件補記 — ◎

8 件の表 ＋ #8 の成立条件 ＋ #6 の値の出所 ＋ 是正マイグレの節を追加。**指示書が求めた 8 件を超えて「是正マイグレも再適用対象」を自力で足しており、次の波への効きが最も大きい追記である。** `seed_imported` も 14 キャラ 未 → 済 へ更新済み。

### 完了条件 8: down が §11.Y の 3 点を満たすか — ×

**手書き 4 本（`000082` / `000083` / `000087` / `000088` / `000089`）の down は §11.Y の 3 点を本文へ明示しており、質が高い。**

- `000082.down` は **(2) 利用者データを残す＝当該 12 キャラのコンボは orphan として残る、これは意図である** を逐語で書いている（指示書がとくに求めた点）。実測でも down 後に user combo 1 件が orphan として残り、`moves` / `preset_aliases` / `move_commands` の orphan は 0 だった。
- `000083.down` は **`c_viper` / `dhalsim` を混ぜていない**（up・down とも 12 キャラ固定）。`000090` / `000091` の層 C-1 も 12 キャラ固定。実測で down 後も両者の移動 9 種・27 alias・`custom_states` が残ることを確認した。**指示書 §2.5 の最大の地雷は踏んでいない。**

**しかし 3 点目の `move_derivations` が満たされていない。** 指示書 §2.5-3 は「第四波でどうするかを決めて、**決めた内容を down のヘッダに書くこと**」を求めており、完了条件 8 も「その判断が down のヘッダに書かれている」を条件にしている。

```
$ grep -n "move_derivations" migrations/0000{82..91}*.sql
（出力なし）
```

**`000082`〜`000091` の 20 本すべてに `move_derivations` の文字列が 1 度も現れない。** 完了報告 §10 は「**`000084.down` は触れない。この判断理由を down のヘッダに書いた**」と書いているが、事実ではない。⇒ 下記 高-1。

### 完了条件 9: `go test` / `make e2e` — ○（E2E は検証不能）

- `go test ./...` を本レビューで実行し **全パッケージ緑**を確認（`go list ./...` = 63、テストを持つのは 55 で報告と整合）。
- **`make e2e` の 226 passed は本レビューでは検証できなかった**（本環境にブラウザが無い）。E2E spec の差分は読んだうえで、書き換えの妥当性のみ判定した（下記「テストの追随」）。

### 完了条件 10: `progress-log.md` への追記 — ◎

索引行 ＋ 横断課題 10 件。`scripts/check-progress-log-index.sh` も緑（65 件すべて追記あり）。「レビューは Phase C 後に追記（まだ実施していないためリンクを張らない＝`D-510`）」の扱いも正しい。

### 指示書 §3 の 7 規約 — ○（規約 3 は満たすが根拠に難、規約 5 は満たす）

| # | 規約 | 判定 |
|---|---|---|
| 1 | すべての件数に母数 | ○（主要な件数に母数が付いている。§11 の「9 件」だけ内訳が 8 件で合わない＝低-6） |
| 2 | 段ごとに投入／落とした／不要だった | ◎ |
| 3 | §0.2 の判定結果 | ◎（結論は正しい） |
| 4 | 取り残しを列ごとに | ○（数字は正。表の語が結論と不整合＝中-4） |
| 5 | 消費連番の全数と 1 本ずつ | ◎（`000081` を使わない理由と、後から `000081` が現れたときの永久スキップ（`D-119`/`D-120`）まで書いてある） |
| 6 | `seed-progress` を補った内容 | ◎ |
| 7 | 確かめられなかったことを書く | ○（5 件明記。ただし `chun_li/wall_jump` の `through` の根拠は書けたはずで、漏れている＝高-2 の一部） |

### 機械検査 — ◎

`check-artifact-integrity` / `check-progress-log-index` / `check-doc-inventory` / `check-md-emphasis` / `check-doc-refs` / `check-browser-storage-keys` / `check-stop-discipline` をすべて実行し、**全件「違反なし」**。`check-derived-docs` のみ ⚠（下記 中-3）。

### E-225（新規追加ファイルの deletions）検査 — ◎

`git diff --numstat 78f4981` を確認。**新規 14 ファイル（マイグレ 20 本・テスト 2 本・docs 2 本）はすべて deletions = 0**。上書きで消えた行は無い。

---

## 設計準拠性以外の指摘事項

### 高-1: `move_derivations` の down 判断がどこにも書かれていないのに「書いた」と報告している

**実物**: `migrations/000082`〜`000091` の up/down 20 本に `move_derivations` の文字列が 1 度も現れない（grep 実測）。
**報告**: `docs/progress/M14-03f-completion-report.md` §10「`move_derivations` の明示削除責務（指示書 §2.5-3）」に

> 本波は `move_derivations` を 1 行も入れない。⇒ `000084.down` は触れない。**この判断理由を down のヘッダに書いた。**

**なぜ「高」か**: 指示書 §2.5-3 と完了条件 8 が明示的に求めた成果物が欠けており、かつ**報告が「在る」と述べている**。次の波の担当は「前波が判断を書いた」と信じて `000084.down` を開き、見つけられない。followup `move-derivations-explicit-delete-on-reseed` は「第三波の down は `move_derivations` に触れていない」という状態のまま、第四波でも解消されない。動作は正しい（本波は 1 行も入れないので触れないのが正）ため、テストも lint も何も言わない。

**修正の形**: `000084.down`（seedgen 生成物のため手編集不可なら `000085` 以降の手書き down、あるいは `000082.down`）のヘッダへ「本波は `move_derivations` を 1 行も入れないため触れない。第二段で投入する場合はそのマイグレ自身の down が自分の入れた行を消す（`000064` / `000067` と同型）」を 3 行で書き、報告 §10 の主張をそれに合わせる。

### 高-2: 段 9「28 行」の根拠が実物の 23/28 にしか当たらない（4 か所へ複製済み）

**実物**: CSV から機械規則（`original_move_code` あり → `through` ／ `is_derived=false` → `standalone` ／ 他 → `unknown`）を再実装し、全 1264 行を突き合わせた。**279 行・内訳 131 / 110 / 28 / 10 は完全に再現した。** `standalone → through` の 28 行の顔ぶれも一致する。

**しかし 28 行のうち 5 行は、書かれた理由に当たらない。**

| 行 | `is_aerial` | `notes_tool` | 書かれた理由（非派生の空中必殺技・ジャンプから通した値）に当たるか |
|---|---|---|---|
| `ed/psycho_shoot_{light,medium,heavy,od}`（**4 行**） | **false** | 「サイコスパーク派生」／「ODサイコスパーク派生」 | **当たらない。空中技ではなく、地上必殺技（`psycho_spark`）からの派生である** |
| `chun_li/wall_jump`（**1 行**） | **false** | **空欄** | **当たらない。空中技でもジャンプ派生でもなく、`through` の根拠がリポジトリ内に無い** |

残る 23 行（`akuma/zanku_hadoken` 4 ／ `blanka/aerial_rolling_attack` 4 ／ `cammy/cannon_strike` 4 ＋ `sa2_aerial_killer_bee_spin` ／ `chun_li/aerial_hundred_lightning_kicks` 4 ＋ `yoso_kick` ／ `dee_jay/knee_shot` ／ `dhalsim/yoga_comet` 4）は `notes_tool` に「ジャンプから」が逐語で在り、説明が当たる。

**なぜ「高」か**: **値そのものは CSV に忠実で正しい**（実測で `ed/psycho_shoot_light` = `through` を確認）。誤っているのは**書かれた理由**である。そしてこの理由は既に **4 か所へ複製されている** ——

1. `migrations/000089_backfill_moves_frame_cost_fourth_wave.up.sql` のヘッダ（★seed 波では**ヘッダが実質的な設計記録**）
2. `docs/progress/M14-03f-completion-report.md` §4
3. `character_data/seed-progress.md`「#6 の値の出所は波によって違う」節
4. `docs/progress/progress-log.md` 横断課題 2

次の波の担当は「28 行は空中必殺技の話」と読み、自分の波で `ed/psycho_shoot` 型（**地上必殺技からの派生で `through`**）が出たときに規則の適用範囲を誤る。しかも `M19-05` の費用規則が静かに誤った分類で回るだけなので、テストは緑のままである（報告自身がそう書いている）。

**修正の形**: 4 か所の記述を「**28 行の大半（23 行）は非派生の空中技で、フレームがジャンプから通した値である。残る 5 行は別の型である——`ed/psycho_shoot`×4 は地上必殺技 `psycho_spark` からの派生（`is_aerial=false`）／`chun_li/wall_jump` は `notes_tool` が空欄で `through` の根拠がリポジトリ内に無い**」へ直す。`chun_li/wall_jump` は報告 §13「確かめられなかったこと」へ 1 行足すのが正しい扱い。

### 高-3: 「CSV を持たないキャラ（`c_viper` / `dhalsim`）」という**生きた理由づけ**が 5 か所で失効している

本サブで `character_data/c_viper.csv` / `dhalsim.csv` を一次源として使い（`fourthWaveOrder` に両者が入っている）、両者は本 seed 済みになった。**⇒ 「CSV を持たない」は失効した。**

| ファイル | 行 | 記述 |
|---|---|---|
| `internal/seedgen/generate_m2002.go` | 409 | 「移動系 9 code は CSV に存在せず…、**CSV を持たないキャラ**（`c_viper` / `dhalsim`）**にも投入先がある**。⇒ charOrder では表せない」 |
| 同 | 433 | 「キャラを列挙しないため、**CSV を持たないキャラ**（`c_viper` / `dhalsim`）にも自動で当たり」 |
| 同 | 776 | 「移動系は CSV に無いため charOrder では表せない。**CSV を持たないキャラ**（`c_viper` / `dhalsim`）にも投入先があるので、専用の集合を受け取る」 |
| `cmd/seedgen/main.go` | 166 | 「移動系は CSV に無く、**CSV を持たないキャラ**（`c_viper` / `dhalsim`）にも投入先がある」 |
| `internal/seedgen/generate_m2002_golden_test.go` | 16-17 / 25-27 | 「**CSV を持たない** `c_viper` / `dhalsim` にも投入先があるため」 |

**`movementChars` を `charOrder` と分ける必要そのものは今も在る。** しかし理由が入れ替わった——正しくは「**移動 9 code は 31 CSV すべてに存在せず（実測 0 行）、`c_viper` / `dhalsim` の移動行は前の波（`000025`）由来だから、`-movement-chars` は『その波で増えたキャラ』でなければならない**」である。

**なぜ「高」か**: 本サブは `generate_m2002.go` を**実際に編集している**（層 C-3 の固定表へ 5 件追加）にもかかわらず、同ファイル内の失効記述を直していない。次の波の担当がこのコメントを読むと「`c_viper` / `dhalsim` は CSV が無いから `-chars` に入れなくてよい」と誤読しうる（第五波以降で両者の CSV が更新されたとき、実際に落ちる）。動作は変わらないためテストも型検査も緑である。

### 高-4: 19 → 31 の波及が `web/src/lib/character-sort.ts` の「受容範囲の根拠」まで届いていない

同ファイルは「**既知の例外 2 つ（2026-08-26 実測・開発者確認済み）。これは欠陥ではない**」として次を書いている。

| # | 記述（現状） | レビュー実測（31 キャラ） |
|---|---|---|
| (1) | 「name_ja に漢字を含む…**実測: 現行 19 体のうち漢字を含むのは 1 件（code=mai）**」 | **4 件**（`mai` 舞 / `akuma` 豪鬼 / `chun_li` 春麗 / `e_honda` E.本田） |
| (2) | 「name_ja がラテン文字で始まる…**実測: 2 件（code=c_viper の "C.ヴァイパー" / code=jp の "JP"）**」 | **4 件**（＋`aki` "AKI" / `e_honda` "E.本田"） |

**なぜ「高」か**: この注記は**開発者が「漢字キャラは一番下でよい」と裁定した根拠の母数**であり、「1 件だから受容してよい」と「4 件だから受容してよい」は別の判断である。並び順は利用者に見える挙動（キャラ選択・一覧のスコープ）であり、19 → 31 でおかしな位置に来るキャラが **3 体 → 6 体**へ倍増した。`character-sort.test.ts` は合成 fixture（`{ nameJa: "舞" }` 等）で書かれているため**テストは緑のまま**であり、人が読む以外に見つける経路が無い。同ファイルは「後任へ: これを『バグ』と読んで読み仮名のマップをフロントへ持たせないこと」とまで書いており、後任が最も参照する注記である。

**修正の形**: 実測値を 31 キャラ時点へ更新し、更新日と契機（M14-03f）を添える。母数が増えたことが受容判断を変えるかは**開発者の手番**であり、製造は数字の更新と報告に留める。

### 高-5: §0.2 の判定で失効した記述の是正が、どの手番へも渡されていない

判定「成立しない」により、次の記述が失効した。

| ファイル | 箇所 | 記述 |
|---|---|---|
| `docs/process/m20-contract.md` | L99 | 「**先例＝`c_viper` / `dhalsim` が『UNIQUE 衝突で起動不能の経路』として最終波指定されている**」 |
| `docs/process/parallel-board.md` | L212 | 「**c_viper / dhalsim は必ず最終波**（UNIQUE 衝突で起動不能の経路）」 |
| `docs/instructions/M14-overview.md` | §3 の表 | 「**M14-03（最終波）** | c_viper / dhalsim ＋ 配布 blocker 解除の判定」 |
| `docs/human-notes/model-allocation.md` | L319 / L331 | 「M14-03（最終波）… **c_viper / dhalsim**」 |
| `docs/handover/phase3/m17-to-m18-handover.md` | L133 | 「**c_viper/dhalsim は必ず最終波**」 |

完了報告は §1 で `m20-contract.md` L99 の失効に触れているが、**§14「併せて更新が要るもの」にも §15「followup 登録候補」にも 1 件も載っていない**（§14 はマイグレ連番レジストリの 3 か所のみ、§15 は followup の開閉のみ）。

**なぜ「高」か**: 製造がこれらを直さないのは正しい（`D-382` / `CLAUDE.md` §8）。しかし**手番の宛先へ渡す責務は残る**。指示書が §0.2 の判定を最優先に置き「そう報告する」と求めたのは、この是正を起こすためである。判定だけして宛先を書かないと、`M14-overview` §3 に「最終波」サブが残り続け、次の担当が「まだ 2 キャラ残っている」と読む（既にロースターは 31/31 で充足している）。

**修正の形**: §14 へ「設計卓の手番」として上記 5 か所を名指しで足す。あわせて「**配布 blocker 解除の DoD（全キャラ充足）のうち、キャラ充足の条件は本サブで満たされた。残るのは `pre-dist-e2e-clean` である**」を書くと、`M14-overview` §4 の判定サブの再定義がそのまま起票できる。

---

### 中-1: `internal/service/intake/service_test.go` が `c_viper` を「未投入キャラ」として使い続けている

`TestResolve_UnseededCharacter_AllUnresolved`（L144〜）:

```go
// 未投入キャラ(索引に居ない)→ 全行未解決・エラーで落ちない・MovesAvailable=false。
res, err := svc.Resolve(context.Background(), "c_viper", text)
```

**対に当たる `internal/api/intake/handler_test.go` は本サブで `TestResolve_MovelessCharacter_AllUnresolved` へ改名し、fixture でキャラを作る形に直している。** service 側だけが手つかずで、テスト名・コメント・引数が「c_viper = 未投入」を主張したままである。`newService(nil)` の fake index を使うため**テストは緑のまま**で、守っている不変条件も壊れていない。しかし「追随 9 件」のリストから漏れており、同じ判断が同じサブの中で片側にしか適用されていない。

**修正の形**: `handler_test.go` と同じ扱い（キャラ code を `test_moveless` 等の中立な値へ変え、テスト名から「Unseeded」を外す）。fake index なので fixture は不要。

### 中-2: seedgen 生成 5 本のヘッダが「移動 9 種の投入元は `000025`」と書くが、12 キャラの投入元は `000083` である

`migrations/000084` / `000085` / `000086` / `000090` / `000091` の up・down 10 本すべてが

```
-- 移動 system move 9 種は drop 済(投入元は 000025)。
```

を持つ。**第四波 14 キャラのうち 12 キャラの移動 9 種を入れたのは `000083`（本波の手書き）であり、`000025` ではない。** テンプレートは `internal/seedgen/generate.go` L173 / L184 のハードコードで、第一波・第三波から引き継いだ既存の不正確さである（`000055` にも同じ行がある）。

**なぜ本サブで挙げるか**: **本サブはまさにこの点を `seed-progress.md` §2.4 へ「#2 移動 system move 9 種 — `000025` は遡って埋めない。波ごとに要る」と書き足している。** 同じ事実がヘッダでは逆に書かれており、手順書とヘッダが正面から食い違う状態になった。次の波は 10 本のヘッダを新しく生成するので、テンプレートを直せば以後は出なくなる。

**修正の形**: `generate.go` のテンプレートを「移動 system move 9 種は drop 済（投入元は当該波の移動 seed マイグレ。第一波以前のキャラは `000025`）」へ。**既に適用済みの `000055` 等の本文は書き換えない**（`D-535`）。

### 中-3: `docs/handover/code-facts.md` のマイグレーション棚卸しが `000080` で止まっている

`grep "^| 0000[78][0-9]" docs/handover/code-facts.md | tail` の末尾は `| 000080 | fix_ground_dash_label |`。**本波の 10 本が載っていない。** `scripts/check-derived-docs.sh` も ⚠ 陳腐化疑い（源泉の変化量 **31 / 1094 件 = 3%**）と判定している。完了報告 §14「併せて更新が要るもの」に再生成（`/regen_code_facts`）が挙がっていない。指示書 §1 は「`code-facts.md` は★引く。想定で書かない」を前提に置いており、次の波が引く資料である。

### 中-4: 完了報告 §7 の表の見出しと結論が噛み合っていない

列見出しは「**意図した数か**」。ところが 4 行が「**★いいえ（意図した未投入）**」と答え、直後に「⇒ **意図しない取り残しは 0 件である**」と締める。「いいえ」が 4 つあるのに「0 件」なので、表と結論が矛盾して読める。指示書 §5-3 の判定語は「**意図しない残り**」であり、表の列も「意図しない取り残しか」へ揃えれば、4 行とも「いいえ」で結論と一致する。件数そのものは全件正しいので、語の問題である。

### 中-5: `internal/repository/movecommand/repository_test.go` の同一ファイル内で新旧が同居している

- L81: 「**仮データキャラ(classic5 の `c_viper` 等)へは依存しない(将来の整理で削除され得るため)**」← 失効（`c_viper` は本 seed 済みの正規キャラであり、削除され得る仮データではない）
- L113: 「★M14-03f(第四波)で全 31 キャラが index を持つようになった…」← 更新済み

L113 を直したときに L81 が視野に入っていない。

---

## 推奨修正（優先度別）

### 高（M14 完了前に修正必須）

1. **`move_derivations` の down 判断を実際に down のヘッダへ書く**（高-1）。あわせて完了報告 §10 の「書いた」という記述を実物に合わせる。**指示書 §2.5-3 / 完了条件 8 の未達である。**
2. **段 9「28 行」の理由を 4 か所すべてで是正する**（高-2）。`ed/psycho_shoot`×4 と `chun_li/wall_jump` が別の型であることを書き、`wall_jump` は §13「確かめられなかったこと」へ回す。**★`000089` のヘッダは実質的な設計記録であり、ここが最優先。**
3. **seedgen の「CSV を持たない `c_viper` / `dhalsim`」5 か所を是正する**（高-3）。分離の必要は残るので、理由だけを「移動 9 code は全 CSV に無く、両者の移動行は `000025` 由来」へ書き換える。
4. **`web/src/lib/character-sort.ts` の実測値 2 件を 31 キャラ時点へ更新する**（高-4）。母数が 3 → 6 に増えたことが受容判断を変えるかは開発者の手番として §14 へ回す。
5. **失効した 5 か所を §14「併せて更新が要るもの」へ設計卓の手番として名指しで足す**（高-5）。`m20-contract` L99 ／ ボード L212 ／ `M14-overview` §3 の「最終波」サブ ／ `model-allocation` ／ `m17-to-m18-handover` L133。あわせて配布 blocker の残条件が `pre-dist-e2e-clean` だけになったことを書く。

### 中（次サブ着手と並行可）

6. `internal/service/intake/service_test.go` の `TestResolve_UnseededCharacter_AllUnresolved` を `handler_test.go` と同じ扱いへ揃える（中-1）。
7. `internal/seedgen/generate.go` L173 / L184 のヘッダテンプレートから「投入元は 000025」の断定を外す（中-2）。**適用済みマイグレの本文は書き換えない**（`D-535`）。
8. `/regen_code_facts` を回し、`code-facts.md` のマイグレ棚卸しへ `000082`〜`000091` を反映する（中-3）。§14 へも項目を足す。
9. 完了報告 §7 の列見出しを「意図しない取り残しか」へ揃える（中-4）。
10. `internal/repository/movecommand/repository_test.go` L81 の「仮データキャラ」注記を是正する（中-5）。

### 低（将来対応）

11. **`000087.down` が up と同じキーで戻していない**。up はキャラごとの code リスト（8 ブロック）だが、down は 8 キャラ × 53 code の**直積**で `is_projectile=0` に戻す。**今日は実害 0**（実測: 直積が当たる 53 行は up が立てた 53 行と完全一致し、`is_projectile=0` の行は 1 つも巻き込まない）。将来同名 code が同 8 キャラの別キャラへ増えると静かに巻き込む。§11.Y の 3 点目の趣旨からは外れる。
12. **明示 DELETE を持つ `000083.down` の子行削除を `fk_on` / `fk_off` の 2 枝で回していない**（`architecture-patterns.md` §11 / `E-226`）。マイグレ接続は常に FK=OFF なので実害は無いと解せるが、その判断がどこにも書かれていない。1 行の注記で足りる。
13. `internal/service/punishfinder/service_test.go` L387 のコメントに「**案 C 39→?**」と `?` が残っている（const は 71）。
14. `docs/progress/20260902-M14-03f-manual-input-list.md` 群1 の説明が「**前ダッシュ / 後ろダッシュ**」と書いている。本サブの §8 が是正した旧表記であり、正は「前方ステップ / 後方ステップ」。開発者へ渡す用紙で旧語を使っている。
15. 完了条件 6 が求めた「`preset_aliases` の投入前後の**差**」を数値で書く（4184 → 7597 / 差 3413）。現状は 2 つの値が別々の節に散っている。
16. 完了報告 §11 の見出し「既存テストの追随（**9 件**）」に対し、本文の内訳が 8 ファイル（表 6 ＋ fixture 2）。件数と内訳が合わない（指示書 §3-1「すべての件数に母数を付ける」）。

---

## 良かった点

- **数字が全部合う。** レビュー側で独立にスクラッチ DB（`migrations/*.up.sql` 90 本を連番順に適用）を作り、報告のほぼ全数字を照合した。`characters` 31 ／ `moves` 3025（第四波 1390）／ `preset_aliases` 7597 ／ `move_commands` 1635（第四波 737）／ `is_projectile` 53 ／ `is_derived` 519 ／ `startup_basis` 964/380/46 ／ `fastest_unreachable` 118 ／ `chain_cancel_total` 2 ／ 移動 `total` NULL 70 ／ `custom_states` NULL 13 ／ alias 欠落 numeric 358 / srk 345 ／ `alias_text_en` 160 ／ `move_derivations` 277 / 242 / 132 — **すべて一致**。さらに CSV から機械規則を再実装して 279 行・内訳 131/110/28/10 まで再現できた。**この密度で「母数つきの件数」を書ける完了報告は多くない。**

- **down → re-up を実際に回して健全性を確認できた。** `000091` → `000082` の順次 down で 19 キャラ / 1653 moves / 4184 aliases / 898 move_commands へ**正確に**戻り、`moves` / `preset_aliases` / `move_commands` の orphan は 0。仮登録 2 体の移動 9 種・27 alias・`custom_states` は残り、**user combo だけが意図どおり orphan（1 件）として残った**。§11.Y の (2)「利用者データを残す。その結果 orphan になることを注記へ書く」が実装と本文の両方で成立している。指示書がとくに警戒した `-movement-chars` への `c_viper` / `dhalsim` 混入も、`000083` / `000090` / `000091` の up・down すべてで 12 キャラ固定になっていた。

- **§8 の自己申告（`000083` が `000080` の是正を巻き戻していた）は本物の発見。** しかも「alias 行は在るのでフォールバックも起きず、画面に古い表記が出るだけ／テストも lint も型検査も緑のまま」という**最も見つけにくい型**である。見つけただけで終わらせず、(a) 是正マイグレの全数実査手順（`grep -l "UPDATE preset_aliases\|UPDATE moves"`）を `seed-progress.md` へ規約として昇格させ、(b) 31/31 で新表記に揃うことと**旧表記が 0 行であること**を対で固定する検査を置いた。実測で「前方ステップ 31 / 後方ステップ 31 / 前ダッシュ 0 / 後ろダッシュ 0」を確認した。**指示書 §2.3-1 は「backfill マイグレ」としか書いていなかった。射程を自力で広げている。**

- **テストを弱めていない。前提が消えた 2 件の扱いが特に良い。** `intake` / `preset` の 2 件は「前提が消えたから消す」ではなく、守っている不変条件（moves を持たないキャラでも 500 にしない／技未投入でも逆引き辞書は空でない）を**今も要る理由つきで**書き、fixture で作り直した。さらに `c_viper` / `dhalsim` が**本 seed 済みになったことを主張する対テスト**まで足しており、仮登録のまま取り残されていたら赤くなる。

- **母数の変化を「増えてよい増え方か」で説明している。** `m2207_baseline_test.go` は「なし 8 → 0」が起きた理由（旧記述『c_viper / dhalsim は SA / CA を 1 つも持たない』の失効）を書き、「減っていたら重大」という向きまで明示した。`m2207b` は「本テストが守っているのは『案 E が辞書に触らない』であって『4184 行が永久』ではない」と役割を再定義し、**seed 波が増えても 0 のままでなければならない対照**（素の SA 番号 0 行）を指している。`punishfinder` は件数だけでなく**増分を列挙で固定**（「件数だけだと入れ替わりを検出できない」）。**母数の追随でここまで書く例は稀。**

- **落とした段の扱いが規律的。** 「あとで」で済ませず、何が何行 NULL かを数え（70 / 13 / 0 / 1388 のうち確定 2）、**推測値が紛れ込んだら赤くなるテスト**（移動 5 code の `total` 非 NULL = 0 を主張）を置き、開発者への記入用紙まで作った。指示書 §4-6「実測値が無い `total` を推測で埋めない」を、報告だけでなくテストで守っている。

- **`000088`（`chain_cancel_total`）のヘッダの質が高い。** 一次源が CSV ではなく `chain-cancel-measurements.md` であること、CSV へ転記しない理由（同じ値が 2 か所にあると正が分からなくなる）、「`total - 4` 等の規則で導かない」根拠（同一単発値が最大 3 通りに割れる）、技名→`move_code` の写像を**単発値の一致で確認した**手順、残り 13 キャラの未実測がロースター判定を含むこと——**次の担当が必要とする判断材料が過不足なく書かれている。**

- **`000090` / `000091` に `-noinput-only` を起こさなかった判断と、その根拠の確定。** `generate_m2002.go` の `runAliases` を読んで「生成器は通常実行でも層 C-3 を解決し、`-noinput-only` は出力を絞るだけ」を確かめ、「`M20-06` 以降の波では別マイグレは要らない」という**条件つきの規則**へ畳んで `seed-progress.md` へ書き足した。followup `seed-progress-procedure-misses-p34-aliases` の内容が「2 本を足す」から「不要である」へ変わったことも報告に明記している。**指示書の前提（§2.4-8「手順どおりに実行すると落ちる」）を実物で検証し直し、結論が違ったことを報告している。**

---

## 制約事項

- 本レビューはコード上で判定可能な範囲のみ対象。実際の動作確認・パフォーマンス・実機テストは別途実施が必要。
- **`make e2e`（226 passed / 0 failed）は本環境にブラウザが無いため再現できなかった。** E2E spec 3 本の差分は読んだうえで、書き換えの妥当性（失った被覆の明示と followup への回付）のみ判定した。
- **`go test ./...` は本レビューで実行し全緑を確認した**（`go list ./...` = 63 パッケージ、テストを持つ 55 が ok。報告の「55 パッケージ ok / FAIL 0」と整合）。
- **不明: 12 キャラの `name_ja` / `name_en` がインゲーム表示と一致するかは判断できない。** リポジトリ内に正本が無く（`000079` 本文）、突合材料は開発者の手元にしかない。**報告 §13-1 がこれを「確かめられなかったこと」として明示し、開発者へ突合を請求しているのは正しい扱いである。**
- **不明: `chun_li/wall_jump` の `startup_basis='through'` の根拠は判断できない。** CSV がそう書いており（`notes_tool` は空欄）、CSV が一次源である以上その値を入れるのは正しいが、なぜ `through` なのかはリポジトリ内に材料が無い（高-2）。
- **不明: 開発者の dev DB / 配布済み DB の状態は確認していない。** 本レビューの実測はすべて新規クリーン DB である。**報告 §3 が指摘した「`000081` を後から作る場合は本波の適用より先に取り込むこと」（`D-119` / `D-120`）は、その意味で dev DB 側の手番として残っている。**

---

*以上*

---

## 取り込み結果（自動トリアージ・2026-09-02）

**指摘 16 件（高 5 / 中 5 / 低 6）。採用 15 件 / 不採用 1 件。★「高」の不採用は 0 件。**
**再レビュー往復 0 回**（上限 2 回）。

| # | 優先度 | 採否 | 理由・対応 |
|---|---|---|---|
| 高-1 | 高 | **採用** | `move_derivations` の down 判断を **`000082.down` へ実際に書いた**。★当初 `000084.down` へ書いて golden が落ちた——同ファイルは seedgen の生成物で手編集できない。**⇒ その制約ごと `000082.down` に記録した**。報告 §10 の記述も実物に合わせた |
| 高-2 | 高 | **採用** | 28 行の内訳を 3 つに割り（ジャンプ通し 23 ／ 地上必殺技からの派生 4 ／ **根拠不明 1＝`chun_li/wall_jump`**）、**4 か所すべて**（`000089` ヘッダ ／ 完了報告 ／ `seed-progress.md` ／ `progress-log.md`）を是正。**★「空中必殺技」も誤りだった**（26/28 は `is_aerial=false`）。`wall_jump` は §13「確かめられなかったこと」へ |
| 高-3 | 高 | **採用** | 5 か所すべて是正（`generate_m2002.go` 3 ／ `main.go` 1 ／ golden test 2）。分離の必要は残るため、理由を「移動 9 code はどの CSV にも無く、投入先は `-chars` と一致しない」へ書き換えた |
| 高-4 | 高 | **採用** | `character-sort.ts` の実測値を 31 体時点へ（漢字 1→4 ／ ラテン始まり 2→4）。**受容裁定の維持は開発者の手番**として §14 へ |
| 高-5 | 高 | **採用** | 失効 5 か所を §14 へ設計卓の手番として名指しで追加。配布 blocker の残条件が `pre-dist-e2e-clean` だけになったことも明記 |
| 中-1 | 中 | **採用** | `service/intake/service_test.go` を `TestResolve_CharacterNotInIndex_AllUnresolved` へ。索引ゼロ fixture であることを注記 |
| **中-2** | 中 | **★不採用** | **既に裁定済みで「波の実装サブでは触れない」と決まっているため**（followup `seedgen-header-provenance`＝「文字列を変えると golden 4 stem が byte 単位で全部変わる」「恒久対応は独立サブを推奨」「製造が触らなかった判断は正しい」）。**★ただし同 followup の緩和策（波の文書へ『ヘッダの投入元表記を信じないこと』を注記）は `M14-03f` 指示書に欠けていたため、完了報告 §16 に書いた**——本波の生成 5 本のヘッダは「投入元は 000025」と出すが、新 12 キャラの実際の投入元は `000083` である |
| 中-3 | 中 | **採用** | `scripts/generate-code-facts.sh` を実行し `000082`〜`000091` を反映。§14 へも項目追加 |
| 中-4 | 中 | **採用** | §7 の列見出しを「意図しない取り残しか」へ揃え、各行の答えも「いいえ（意図どおり）／いいえ（意図して落とした段）」へ統一 |
| 中-5 | 中 | **採用** | `movecommand` テストの「仮データキャラ」注記を是正 |
| 低-11 | 低 | **採用** | `000087.down` を up と同じキー（キャラごとの code リスト）へ書き換え。直積をやめた |
| 低-12 | 低 | **採用** | `000083.down` へ「マイグレ接続は常に FK=OFF であり `fk_on` 枝が存在しない」判断を注記 |
| 低-13 | 低 | **採用** | `punishfinder` コメントの `?` を 71 へ |
| 低-14 | 低 | **採用** | 記入依頼の「前ダッシュ / 後ろダッシュ」を「前方ステップ / 後方ステップ」へ。表示語の経緯も注記 |
| 低-15 | 低 | **採用** | `preset_aliases` の差（4184 → 7597 / **＋3413**）と内訳を §12 へ |
| 低-16 | 低 | **採用** | 「9 件」→「Go 8 ファイル ＋ E2E 3 spec ＝ 11 件」へ |

### 取り込み後の検証

- `go test ./...` = **55 ok / 0 FAIL**
- `make e2e` = **226 passed / 0 failed**
- 機械検査 **9 本**すべて緑（`check-artifact-integrity` / `check-stop-discipline` / `check-progress-log-index` / `check-doc-refs` / `check-browser-storage-keys` / `check-enum-sync` / `check-import-order` / `check-md-emphasis` / `check-doc-inventory`）

*以上*
