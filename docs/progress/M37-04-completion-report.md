# M37-04 完了報告: 確定反撃候補の初段発生（`B06`）／ `B02` の射程判定

| 項目 | 内容 |
|------|------|
| 指示書 | `docs/instructions/M37-04-punish-candidate-first-hit-startup.md` **v1.0.1** |
| チェックリスト | `docs/instructions/reviews/M37-04-review-checklist.md` **v1.0.1** |
| 実施日 | 2026-09-13 |
| ブランチ | `claude/zealous-knuth-uu4ajg` ／ 着手基点 **`9a4d391`** |
| 射程 identity | **2 件**——`B06`（着地）／ `B02`（**請求**。§7） |
| マイグレ消費 | **2 本＝`000114`（層 A・列追加）／ `000115`（層 B・データ修正）**。用途は §6 |

---

## 0. 着手前の版ゲート（指示書 §0.6）

| # | 確かめたこと | 実測 |
|---|---|---|
| 1 | チェックリストの存在 | ✅ `reviews/M37-04-review-checklist.md` v1.0.1 |
| 2 | `docs/progress/M37-RESEARCH-01-report.md` | ✅ 存在（67,333 bytes） |
| 3 | `ls migrations/` の最新 | ✅ **`000112`**。⇒ `000113` は欠番（`D-866`）。**詰めずに `000114` から使った** |
| 4 | `M37-01` / `M37-03` の着地 | ✅ 枝元に**マージコミットが在る**——`9a4d391`（PR #218）／ `0b6f1ac`（PR #217）／ `9b3b0d2`（PR #216）。完了報告 `M37-01` / `M37-03` も `docs/progress/` に在る |
| 5 | 枝元（`git log --oneline -5`） | `9a4d391` / `0b6f1ac` / `cf2e070` / `5e47d56` / `4a002c0` |

---

## 1. 段 1 実査（指示書 §2.1）

### 1-1 件数を数え直した（**実測**。`M37-RESEARCH-01` の値を転記していない）

**式**——CSV 側は `character_data/*.csv` 全 31 ファイルを `csv.DictReader` で集計（全 2,743 行）。DB 側はマイグレ適用済み DB へ SQL を当てた（`internal/infra/migration/migrate_m3704_test.go` の `TestRun_M3704_TargetSetCounts` が同じ式を保持する）。

| 集合 | CSV 実測 | DB 実測 | 対照値 |
|---|---:|---:|---:|
| `category='target_combo'` | 126 | 126 | 126 |
| うち `is_derived` | 117 | 117 | 117 |
| うち非 derived | 9 | 9 | 9 |
| **derived ＋ `standalone`**（★埋める対象） | **109** | **109** | **109** |
| 上記のうち `startup` あり | 106 | 106 | 106 |
| 上記かつ非空中・`damage>0` | 105 | 105 | 105 |
| `basis='through'` | 4 | 4 | 4 |

**★対照値と 1 件の差もなく一致した。**

**★★実測で新たに分かった分解**（対照表に無いもの）——`target_combo ∧ is_derived` の 117 行は **`standalone` 109 ／ `through` 4 ／ `unknown` 4** に割れる。**`through` の 4 行**（jamie 乱酔旋）**は 4 行とも `is_derived=true` である。⇒ これが §2 の述語の形を決めた。**

### 1-2 `punishfinder` の投影に足せるか

**足せた。** 着手時点の `listMovesForScanSQL`（`internal/repository/punish/queries.go:10`）は 12 列を投影し、**`category` は在るが `is_derived` も `startup_basis` も無かった**（`M37-RESEARCH-01` §4.2 の「現投影に情報が足りない」の実物）。3 箇所を同時に直した（§3）。

### 1-3 `B02`（バーンアウト）の現在地

**実装に存在しない。** §7 に判定を書いた。

---

## 2. 判定対象の確定（**開発者裁定**・2026-09-13）

`moves` の行を 3 つに分け、**集合 B の 109 行だけを対象にする**。

| 群 | 定義 | 件数 | 扱い |
|---|---|---:|---|
| **A** | `target_combo` ∧ 非 derived | 9 | 触らない。`startup` のまま |
| **B** | `target_combo` ∧ `is_derived` ∧ **`basis='standalone'`** | **109** | **`first_hit_startup` で判定。NULL なら候補から外す** |
| **C** | `target_combo` ∧ `is_derived` ∧ `through`(4) / `unknown`(4) | 8 | 触らない。`startup` のまま |

**⇒ 除外述語は 4 条件である**: `category='target_combo'` ∧ `is_derived` ∧ `startup_basis='standalone'` ∧ `first_hit_startup IS NULL`。

> **★★★チェックリスト A-1 は述語を「3 つ」と書いている**（`startup_basis` に触れていない）**。本サブは条件を 1 つ*足して* 4 条件にした。⇒ 製造の独自判断ではなく開発者裁定である。**
>
> **★理由＝3 条件では指示書自身のテストが成立しない。** 3 条件にすると集合 C の 8 行まで除外され、**§5-4 と A-4 が要求する「`basis='through'` 4 行が候補から消えていないこと」が構造的に成立しなくなる。**
>
> **★根拠となる指示書の記述は 4 つある**——§0.4「それ以外の行は NULL のままでよい。⇒ 判定側は `startup` をそのまま使う」／ §4.2「判定側は*行の属性*で対象かどうかを決める」／ §4.3「外すのは 109 だけ。広げると偽陰性が増える」／ §5-4 のテスト。**⇒ 4 条件はこの 4 つすべてと整合し、3 条件はどれとも整合しない。**
>
> **⇒ 設計伝達レポート §4 へ、チェックリスト A-1 の文言是正として送る。**

---

## 3. 変更したもの

### 3-1 段 2 — 列を足す（`000114`）

- `migrations/000114_add_moves_first_hit_startup.{up,down}.sql`（新規）。`ALTER TABLE moves ADD COLUMN first_hit_startup INTEGER;`
- **`CHECK` は付けなかった。** 既存の `startup`（`migrations/000013_add_moves_frame_columns.up.sql:4`）が `ALTER TABLE moves ADD COLUMN startup INTEGER;` で `CHECK` を持たないため、それに倣った（チェックリスト B-4）。
- **値は 1 行も入れていない**（B-2）。`TestRun_M3704_FirstHitStartupColumnAddedEmpty` が「`first_hit_startup IS NOT NULL` の行 = 0 件」を固定する。
- down は `DROP COLUMN`（`000049` の down と同型）。

### 3-2 段 2' — CSV に列を足した（**足す**＝開発者裁定）

- `character_data/*.csv` 全 31 ファイルへ `first_hit_startup` を**末尾追加**（契約 24 → **25 列**）。**全行が空セルである。**
- **末尾にした理由**——`internal/seedgen/csv.go` の `parseRow` は位置引き（`g(N)`）であり、途中挿入だと既存 24 列の添字が全部ずれる。⇒ 位置依存を増やさない。
- `internal/seedgen`（`model.go` の `csvColumns` / `MoveRow`、`csv.go` の `parseRow`）を追随。**保全のみ・SQL 非投入**（`startup_basis` 等 `000049` の 3 列と同じ流儀）。
- `internal/infra/migration/csv_db_sync_test.go` を 4 列 → **5 列**へ拡張（`quad` → `quint`）。同テストの doc が「次に列が増えたとき同テストへ足すこと」（followup `csv-db-frame-cost-columns-drift`）を明示的に求めていた。

### 3-3 段 3 — 投影を足した

| ファイル | 内容 |
|---|---|
| `internal/repository/punish/queries.go` | `listMovesForScanSQL` へ `m.startup_basis` / `m.is_derived` / `m.first_hit_startup` |
| `internal/repository/punish/repository.go` | `ScanMove` へ `StartupBasis string` / `IsDerived bool` / `FirstHitStartup *int` |
| `internal/repository/punish/scan.go` | NULL 可の `first_hit_startup` は `sql.NullInt64` → `nullInt64ToIntPtr`（同ファイルの既存パターン）。NOT NULL の 2 列は直受け |

### 3-4 段 4 — 判定を変えた（`B06` 本体）

`internal/service/punishfinder/service.go` にヘルパ 2 本を新設した。

- `usesFirstHitStartup(sm)` ＝ §2 の 3 属性判定。
- `candidateStartup(sm) *int` ＝ 判定に使う発生。対象行では `first_hit_startup` を返し、**NULL ならそのまま `nil`**（＝候補にしない）。対象外は `startup` をそのまま返す。

`buildStarters` の **2 箇所**（地上レーン・ダッシュ経由レーン）をこれ経由へ差し替えた。**ジャンプ経由レーンは `startup` を判定に使わない**（キャラのジャンプ全体で決まる）**うえ `category=normal` を要求するため `target_combo` は到達しない。⇒ 述語は変えていない。**

**★実装判断（§2.3-5）＝応答の `startup` も判定に使った値へ揃えた。** 判定を 6F で行いながら画面へ 5F を出すと読めないためである。**着地時点では `first_hit_startup` が全行 NULL なので応答は 1 バイトも変わらない**（対象行は候補に出ず、対象外の行は `startup` がそのまま渡る）。埋めた行から順に正しくなる。

**★除外されたことを利用者へ見せる形は足さなかった**（§2.3-5 の「実装判断でよい」）。理由——利用者はまだいない（`D-857`）ため実害は開発者のテスト中に限られ、かつ **109 行が埋まれば除外そのものが消える一時状態**である。恒久でない状態に UI 語彙を足すと、埋め終わった後に外す手番が要る。

### 3-5 段 5 — 鷹嘴連拳の `is_aerial` を是正（`000115`）

**★集合 A の実査中に見つけた誤入力である**（§8 に経緯）。

**一次源は seed 行が自分で持っていた注記**（`migrations/000084_seed_moves_fourth_wave.up.sql:1246` の `raw_data.notes_tool`・`character_data/chun_li.csv` にも同文）:

> 「エッジケース。空振りでも出せる**空中ターゲットコンボ**。通常のジャンプ攻撃と同じような性質も持つ。startupとactiveはターゲットコンボの**2段目standalone**を記載。**ジャンプ強Pから派生**。」

**⇒ 注記は「空中ターゲットコンボ」と明言しているのに `is_aerial` だけが `false` だった**（開発者確認済み）。裏づけとして `on_hit` / `on_block` が両方空欄であり、**これは chun_li の `is_aerial=false` 行では本行だけである**（空中技の形）。

- `migrations/000115_data_correct_chun_li_soaring_eagle_aerial.{up,down}.sql`（新規）
- `character_data/chun_li.csv` と golden `000084` にも同じ是正を入れた。**⇒ 新規 DB は最初から `is_aerial=1` で seed され、`000115` の `UPDATE` は 0 行に当たって成功する**（`000112` と同じ性質）。**テストは件数ではなく v115 時点の状態で固定した。**

### 3-6 併せて直したもの — `migrate_m3104_test.go` の陽性対照

**本サブの変更で落ちたため直した。** 旧実装は **v107 の DB へ*現行の* `punishfinder` を当てて**おり、走査の投影へ列が増えるたびに `no such column` で落ちる（実際 `first_hit_startup` で落ちた）。**⇒ サービスコードは常に最新スキーマを要求するのに、DB だけ過去へ戻していた。**

**★先に実測した赤の出力**（`SUPP-001` §5.5.4 規約 (16) 歯止め (c)）:

```
    migrate_m3104_test.go:236: scan(guard=block): list opponent moves: list moves for scan:
        SQL logic error: no such column: m.first_hit_startup (1)
--- FAIL: TestRun_M3104_PunishScanPositiveControl (0.57s)
FAIL	github.com/plexiblinp/tacpendium/internal/infra/migration	76.516s
```

**⇒ スキーマは HEAD まで上げ、「投入前」は当該行の削除で作る形へ直した。** 「v107 に行が無い / v109 に行が在る」は `TestRun_M3104_UpContract` が素の SQL で押さえており、本テストの主張は*走査の挙動*であるため、版ではなく行の有無で作れる。**削除した DB から行が出ないこと・別インスタンス（行あり）で出ることの両方を見ており、主張は弱めていない。**

---

## 4. テスト（指示書 §5）

### 4-1 新設したテスト

| ファイル | 本数 | 何を固定するか |
|---|---:|---|
| `internal/service/punishfinder/m37_04_first_hit_startup_test.go`（新規） | **7** | §5-1 / §5-2 / §5-3 破壊確認（`Scan` 経由）/ §5-4 偽陰性 / 述語表 / **ダッシュ経由レーンのゲート** / 境界 |
| `internal/infra/migration/migrate_m3704_test.go`（新規） | 5 | `000114` 全行 NULL ／ 母集団件数 ／ v115 状態 ／ down-up 往復 ／ `startup` 不変 |

### 4-2 **★★実データでの実測**（canary ハーネス）

**単体フィクスチャだけでは「実データで何が変わったか」を言えない**ため、`internal/infra/migration/canary_punish_scan_test.go` を変更の前後で 2 回走らせて突き合わせた。**「前」は本サブの 2 つの変更**（`B06` のゲート／`is_aerial`）**を一時的に戻して測り、測定後に復元した。**

| 観点 | 実測 |
|---|---|
| 候補 code が**完全に消えた**もの | **106 件** |
| **新たに現れた**もの | **0 件**（⇒ 範囲を広げていない） |
| 消えた 106 件のうち**集合 B に属さない**もの | **`soaring_eagle_punches` の 1 件だけ**（`is_aerial` 由来であり `B06` のゲート由来ではない） |
| ⇒ **`B06` のゲートで消えた件数** | **105 件＝対照値「地上候補の静的条件を満たす行数 105」とちょうど一致** |

**§5-2 の名指し確認**（有利 5F の地上レーン）:

| code | 前 | 後 |
|---|---|---|
| **`fuwa_triple_strike_2hits`** | **出る**（3,912 回） | **出ない** |
| `fuwa_triple_strike` | 出る | 出ない |
| `soaring_eagle_punches` | 出る（3,718 回） | 出ない |

**★再現手順**（低-11 への回答。数値だけが報告に残る形にしないため明記する）:

1. `CANARY_OUT=<path>/canary-after.txt go test ./internal/infra/migration/ -count=1 -run TestCanary_PunishScanProjection`
2. 次の 3 点を一時的に戻す——(a) `candidateStartup` を `return sm.Startup` だけにする ／ (b) golden `000084` の `soaring_eagle_punches` 行の `is_aerial` を `1` → `0` ／ (c) `000115` の `SET is_aerial = 1` を `0` にする
3. 同じコマンドで `canary-before.txt` を取り、**元へ戻す**
4. 両者を突き合わせる（本報告の数値は `ACC` 行から `<code>:<lane>` を抽出して集合差を取ったもの）

**§5-4 の偽陰性確認**（実データ）:

| 集合 | 後も候補に在る | 消えた |
|---|---:|---|
| `basis='through'`（4 行） | **4 / 4** | 0 |
| `basis='unknown'`（4 行） | 1 / 1（**残る 3 行は `startup` が空欄で元から候補ではない**） | 0 |
| 非 derived（9 行） | **8 / 9** | **1＝`soaring_eagle_punches`**（`is_aerial` 由来。意図どおり） |

### 4-3 **★★破壊確認**（§5-3）

**「候補から消えた」だけでは別の理由で消えた可能性を排除できない。** 次の 3 つで歯止めの所在を示した。

1. `TestScan_DestructiveCheck_ReturnsWhenGateRemoved` が、**`Scan` を通して**「ゲートを外すと同じ行が同じ有利 5F で候補に戻る」ことを見る。**外し方は 2 通り**——(a) 同じ行の `startup_basis` だけを `through` にして述語の対象から外す（＝変更前とまったく同じ「`startup` をそのまま使う」判定へ戻る）／ (b) `first_hit_startup` を埋める。**⇒ 除外の理由が「NULL であること」だけだと言える。**
2. **★レビュー 低-8 を受けて `Scan` 経由へ直した。** 旧版は `candidateStartup` の単体確認にとどまっており、**`damage=0` や `is_aerial` など `Scan` の他の除外条件に紛れていないことを示せていなかった。**
3. **変異テストを実測した**——(a) 述語から `startup_basis` を落として 3 条件にすると `TestScan_NonTargetRows_Unchanged` と `TestUsesFirstHitStartup_Predicate` が赤 ／ (b) 除外を外すと **3 本**（`TestScan_TargetComboNullFirstHit_Excluded` ／ `TestScan_DestructiveCheck_ReturnsWhenGateRemoved` ／ `TestScan_DashLane_UsesSameGate`）が赤。**⇒ テストが空回りしていない。**

### 4-4 束 C-1 — `startup` の値が 1 行も変わっていないこと

| 対象 | 実測 |
|---|---|
| `character_data/*.csv` の `startup` 列（着手基点 `9a4d391` と全 31 ファイル突合） | **差分 0 行** |
| `migrations/` の差分 | **`startup` の値を書き換えた行は 0。** 唯一の DML は `000115` の `is_aerial` であり、golden `000084` の変更も `is_aerial` の `0` → `1` だけである（同じ行の `startup=6` は不変） |
| `TestRun_M3704_StartupUntouched` | 鷹嘴連拳の `startup` が seed 値 `6` のまま |

### 4-4b golden を再生成する前の突合（`SUPP-001` §5.5.4 規約 (16) の手順）

**変わる列名＝`is_aerial`。** 同名を参照する `*_test.go` を `grep` し、各ファイルの版固定の終端が golden の版（`000084`）以上かを突き合わせた。

| 版固定を持つテスト | 終端 | 判定 |
|---|---:|---|
| `migrate_m1904c_test.go` | v63 | **v84 より前。⇒ chun_li 第四波の行はまだ存在しない** |
| `migrate_m19p2_test.go` | v65 | 同上 |
| `migrate_m19p2b_test.go` | v68 | 同上 |
| `rules_m1905_test.go` | v68 相当 | 同上 |
| `migrate_m3104_test.go` | **HEAD へ変更した**（§3-6） | 影響なし |
| `migrate_m3704_test.go` | v115（本サブ） | 本サブの対象 |

**残りの `is_aerial` 参照テスト 12 本は版固定を持たず `m.Up()` か fake repo であり、全数緑である。**

**⇒ 結論は白。** ただし**手順の記録が無い状態では次に同じ判断をする人を助けない**ため、結論だけでなく突合表そのものを残す。

### 4-5 検査（すべて**出力で**判定した）

| 検査 | 結果 |
|---|---|
| `bash scripts/check-artifact-integrity.sh`（★1 本目） | **違反なし**（検査 16 件の自己検査 ＋ 生成物 4 件が OK） |
| `bash scripts/check-migration-license.sh` | **違反なし** |
| 〃 `--list`（§5-6 の実測） | **`000114` = 層 A（AGPL-3.0-or-later）／ `000115` = 層 B（CC-BY-SA-4.0）。⇒ 想定どおり。`REUSE.toml` の編集は不要だった** |
| `go test ./...` | **全 60 パッケージ ok** |
| `cd web && pnpm test` | **233 ファイル / 2,898 テスト passed** |
| `make e2e`（全数） | **345 passed（6.2m）** |
| `bash scripts/check-browser-storage-keys.sh` | 違反なし |
| `bash scripts/check-enum-sync.sh` | ベースラインどおり（増加なし） |
| `bash scripts/check-import-order.sh` | ベースラインどおり（増加なし） |
| `bash scripts/check-doc-refs.sh` | dead reference なし |

---

## 5. 変更統計（`git diff --stat 9a4d391`）

```
 50 files changed, 3465 insertions(+), 2841 deletions(-)
```

**★新規ファイルは 6 本で、すべて `+` のみである**（`E-225` の観点で読んだ）:

```
A	internal/infra/migration/migrate_m3704_test.go                      178 +
A	internal/service/punishfinder/m37_04_first_hit_startup_test.go      215 +
A	migrations/000114_add_moves_first_hit_startup.up.sql                 38 +
A	migrations/000114_add_moves_first_hit_startup.down.sql                4 +
A	migrations/000115_data_correct_chun_li_soaring_eagle_aerial.up.sql   42 +
A	migrations/000115_data_correct_chun_li_soaring_eagle_aerial.down.sql 10 +
```

**★`character_data/*.csv` の 31 ファイルが大きな `+`/`-` を持つのは、全データ行の末尾へ空セル 1 つを足したためである**（1 行が `-` と `+` の対で出る）。**上書きではない。** 内容の変更は chun_li 1 行の `is_aerial` だけであり、§4-4 の突合がそれを裏づけている。

---

## 6. **★★マイグレ 2 本の用途**（`D-870` の条件）

| 連番 | 層 | **用途** | `startup` に触れたか |
|---|---|---|---|
| **`000114`** | **A** | **列の追加**（`moves.first_hit_startup INTEGER`）。**DDL のみ・値は 1 行も入れない** | **触れていない** |
| **`000115`** | **B** | **データ修正**（chun_li `soaring_eagle_punches` の `is_aerial` を `0` → `1`）。**DML のみ** | **触れていない** |

**★指示書 §4.1「最大の危険＝データを直す方向へ行くこと」が守っているのは `startup` の値である。** `000115` が直すのは `is_aerial` の boolean 1 セルだけであり、**同じ行の `startup=6`（＝2 段目の値）はそのまま残している。**

**★`000113` は欠番のまま詰めていない**（`D-866`）。実証——`golang-migrate` は v113 へ `Migrate` できず `no migration found for version 113` を返す（`migrate_m3704_test.go` の定数コメントに実測を残した）。

---

## 7. `B02`（バーンアウト中の確定反撃）＝ **請求する**

### 7-1 段 1 の実査結果

| 載せ先の候補 | 載るか | 根拠 |
|---|---|---|
| コード全体の `burnout` / `バーンアウト` の出現 | **実質ゼロ** | 唯一の出現は `internal/service/comboio/csvcore/csvcore_test.go:33` の**架空の JSON フィクスチャ**。`combos.situation` は BE 素通しの opaque JSON であり、どのキャラの `custom_states` 定義にも `burnout` という state code は無い |
| `characters.custom_states` | **載らない** | seed 済み state code 全 35 種に該当が 0 件。かつ設計上「**キャラ固有**状態」の器であり（`DES-003` §3.2）、バーンアウトは全キャラ共通のシステム状態 |
| `combos.opponent_stance` | **載らない** | 値域は体勢 4 値（`standing` / `crouching` / `airborne` / `any`）。**`B15` が `hit_type` へ「空中ヒット」を足して `airborne` と重複し差し戻された先例と同型になる**（計測点 `M-174`） |
| `punishfinder.ScanParams`（`service.go:26`） | **軸が無い** | `SelfCharacterID` / `OpponentCharacterID` / `GuardType` の 3 つだけ |
| `moves` のフレーム列 | **データが無い** | `on_block` は通常時のみ。バーンアウト時の硬直差も、システム共通の増加量も、どの列にも無い |

### 7-2 判定＝**射程が大きい。⇒ 本サブでは実施せず、設計卓へ請求する**（§2.4-2）

**何が大きかったか**——実施には「UI タブ → URL query → API → `ScanParams` → 走査述語 → 有利フレーム算出式」の**6 層すべてに新しい軸を通す**必要がある。**加えて、バーンアウト時の硬直差という一次データがリポジトリに 1 件も無い**ため、軸を通しても計算する材料が無い。

**★請求時に添える差分**——既存の「バーンアウトを独立列にしない」決定（`docs/instructions/phase3-overview.md:73,172` ／ `phase3/M16-02-gauge-consumed-columns.md:49` ／ `phase3/M16-overview.md:68`）は**すべてドライブゲージ消費モデルの文脈**であり、**確定反撃の硬直差という `B02` の文脈で判断した既存決定は 1 件も存在しない。**

---

## 8. **★★横断課題** — 集合 A に「2 段目以降の値を持つ行」が居る

**開発者の求めで集合 A（`target_combo` かつ非 `is_derived` の 9 行）を実査した。**

**ご指摘の観点（初段は通常技か特殊技のはず）は通った**——9 行すべて `command` の先頭入力が `normal` か `unique` の地上技に解決し、**カテゴリ面の異常は 0 件である。**

**★ただし 9 行すべてが `command` に `chain` を含む**＝どれも「2 段止め」か「全段」であって 1 段目の行ではない。**⇒ 記録されている `startup` が 2 段目以降の値である行が混ざる。**

| 行 | 記録 `startup` | 先頭入力の技 | その値 | 所見 |
|---|---:|---|---:|---|
| e_honda `toko_shizume`（地鎮） | **22** | 立中P | 10 | **2 段目の `power_stomp`（力足・`dr plus k_h`）の 22 と完全一致。⇒ 2 段目の値である** |
| aki `hun_dun`（渾沌） | 8 | 立弱P | 5 | 8 はどの地上通常技とも一致しない ⇒ 2 段目固有の値と読める |
| aki `qiong_qi`（窮奇） | 14 | 立強P | 12 | 同上 |
| e_honda `double_slaps`（連ね張り手） | 4 | 立弱P | 5 | **★誤入力ではない**（開発者の逐語＝「2 段目の発生が 1 段目より早くなることは、standalone 扱いの `target_combo` ではありえる」） |
| chun_li `soaring_eagle_punches`（鷹嘴連拳） | 6 | — | — | **`is_aerial` の誤入力。⇒ 本サブで是正した**（§3-5） |
| zangief 4 行 | 9 / 9 / 10 / 9 | 立中P ／ `power_stomps_1hits` | 9 / 9 | 一致（健全） |

**★★値の誤りは 1 件も無い。** 構造の話である——**aki 渾沌も ryu 不破三連撃（2 発止め）も「TC 全体 / 途中止めを 1 行で表す」同じ形なのに、前者は `is_derived=false`、後者は `true` である。⇒ `is_derived` の付け方がキャラ間で不統一であり、新しいゲートは `is_derived` を鍵にしているため、その不統一がそのまま「掛かる / 掛からない」の差になる。**

**⇒ 本サブでは手を出さない**（開発者裁定＝「鷹嘴連拳の `is_aerial` だけ直す」）**。設計伝達レポート §4 へ横断課題として送る。**

> **★手法の限界を明記する。** 上の初段照合は `command` の文字列一致による**一回限りの実査**であり、**指示書 §3-3 がこれを実装へ組み込むことを禁じている**（実測で 126 行のうち 59 行が判定不能）。**実際、自動照合は chun_li の初段を `lotus_fist`（37F）と誤って出した。⇒ 手法として信頼できないことの実例である。** 9 行という小ささゆえに個別に読めただけである。

---

## 9. ■ 併せて更新が要るもの

| # | 対象 | 状態 |
|---|---|---|
| 1 | **CHANGE 番号の登録** | **消費なし。⇒ 製造は自採番しない**（`D-293`）。`DES-003` §3.3 ／ `DES-005` §5.20 への反映は設計卓の手番であり、原稿を設計伝達レポート §4 へ置く |
| 2 | **マイグレ連番** | **`000114` ＋ `000115` を消費した**（`D-870`）。**`000113` は欠番のまま**（`D-866`）。⇒ ボード §2.2「次に払い出す番号」は **`000116`** になる |
| 3 | **`DES-003` §3.3 の列台帳** | **原稿を設計伝達レポート §4 へ置く**（名前／実セマンティクス／消費者一覧＝`M19-DESIGN-07` §7 の列台帳規約）。**製造は設計書本体を編集しない**（`CLAUDE.md` §8） |
| 4 | **CSV 列契約の版** | **24 → 25 列**。`internal/seedgen/model.go` の `csvColumns` コメントが「23 列」のままだった（`last_changed_game_version` 追加時から既に 24 列であり、着手前から陳腐化していた）。**★本サブで「25 列」へ直した**（レビュー 高-1 の採用）——**本サブがこの宣言ブロックへ 1 要素を足した当人であり、触ったブロックの数え違いは触った手番で直すのが正しい。⇒ 横断課題へ送るという当初の判断は撤回する。** |
| 7 | **★`first_hit_startup` を DB へ入れる経路** | **本サブには無い。** 本列は CSV に持つが seedgen が SQL へ出力しないため、**開発者が 109 行を埋めても DB には 1 行も入らず、`csv_db_sync_test` が赤になる。⇒ backfill マイグレが別途要り、その番号の請求も要る**（`D-293`）。先例＝`startup_basis` 等 3 列の `000050` / `000064` / `000067`。**記述は `000114` のヘッダと `internal/seedgen/model.go` のパッケージ doc に置いた**（レビュー 中-4 の採用） |
| 8 | **応答 `startup` の意味変更** | `starterNode` が判定に使った値（対象行では `first_hit_startup`）を応答へ載せるよう変えた。**フロントは `PunishTree.tsx` で `発生 {startup}F` と無条件ラベルで描くため、109 行が埋まった時点で確定反撃サーチと技一覧の「発生」が食い違って見える。⇒ `DES-005` §5.20 への差分として設計伝達レポート §4 へ送る**（レビュー 中-5 の採用） |
| 9 | **`is_aerial` の 2 つの意味** | 本サブが直したのは「空中で出る技か」の意味（確定反撃側の読み）である。**もう一方の実セマンティクス**（ラッシュ版自動生成の除外＝`M19-DESIGN-07` §7 ／ `D-222`）**には影響しない**——ラッシュ可否は `category ∈ {normal, unique}` を要求し（`internal/service/move/service.go:89`）、本行は `target_combo` のため元から対象外。**記述は `000115` のヘッダに置いた**（レビュー 中-7 の採用）。**列の意味の一本化そのものは射程外**（followup `M19-flag-semantics-drift` と同根） |
| 5 | **`docs/usermanual/`** | **該当なし。** §5.20 / §5.21 の章は「本文は M32-02 で記述します」のスタブであり、陳腐化した記述は無い。**⇒ `M32-02` が本文を書くときに本サブの除外規則を前提にすること** |
| 6 | **`fastest_unreachable` の B 型候補** | **★★【2026-09-13・2 回目の訂正】初版「16 → 17 行目」・追補1「25 → 26」はいずれも最終形と合わない。⇒ 追補2 で `is_aerial` を `0` へ戻したため、本サブは B 型候補を 1 行も増やさない（実測 25 行で前後不変・うち `target_combo` は 0 行）。** `DES-003` §3.3 の「16 行」が失効していること自体は変わらないが、**それは第四波（`000084`）でキャラが増えた時点の話であり、本サブが起こしたものではない** |
| 7 | `B02` の判定が明記されている | ✅ §7＝**請求** |
| 8 | §5 の検査がすべて緑（出力で判定） | ✅ §4-5 |
| 9 | 完了報告 ＋ `progress-log.md` への索引行 | 本書 ＋ Phase D で追記 |
| 10 | 設計伝達レポート | **Phase C 後に作成する**（§11） |

---

## 11. レビューと取り込み（Phase B / Phase C）

| 項目 | 値 |
|---|---|
| レビュー報告書 | `docs/progress/m37-04-review.md`（fresh subagent が作成） |
| 指摘の件数 | **全 15 件＝高 3 / 中 4 / 低 8** |
| 採否 | **採用 13 / 不採用 2**（いずれも「低」） |
| **「高」指摘の不採用** | **0 件**（⇒ 安全弁の発動なし） |
| 再レビュー往復 | **0 回**（上限 2） |
| 差し戻し事由 | **0 件**（レビュアーの総評＝「差し戻しに当たる事項は 0 件。合格水準」） |

### 11-1 採否と理由

| # | 指摘 | 採否 | 対応 / 理由 |
|---|---|---|---|
| **高-1** | `csvColumns` の見出しが「23 列」のまま（実際 25 列） | **採用** | **「25 列」へ直し、数え違いの履歴も残した。** 本サブがこの宣言ブロックへ 1 要素を足した当人であり、**触ったブロックの数え違いは触った手番で直すのが正しい。⇒ 当初「横断課題へ送る」としたのは誤りであり撤回した** |
| **高-2** | ジャンプレーンのコメント「未確定なら 0 を渡して省略させる」が実装と食い違う | **採用** | **記述を実装に合わせた。★この行は本サブの途中で直したつもりが、作業中の巻き戻しで直り損ねていた。⇒ レビュアーが拾わなければ残っていた** |
| **高-3** | seedgen パッケージ doc の「SQL 非投入」列挙が 3 列のまま | **採用** | `first_hit_startup` を 4 つ目として足し、**backfill が要る旨**も併記した |
| **中-4** | 値を DB へ入れる経路が設計されていない | **採用** | **`000114` のヘッダと seedgen パッケージ doc へ明記**——CSV を埋めただけでは DB に入らず `csv_db_sync_test` が赤になること、backfill マイグレと**番号の請求**が要ること、先例が `000050` / `000064` / `000067` であること。**設計伝達レポート §4 へも送る** |
| **中-5** | 応答 `startup` の意味変更が設計へ送られていない | **採用** | **§9-8 に起こし、`DES-005` §5.20 への差分として設計伝達レポート §4 へ送る** |
| **中-6** | `SUPP-001` §5.5.4 (16) 歯止め (c) の赤の出力と grep の記録が無い | **採用** | **§3-6 へ赤の実測出力を貼り、§4-4b に `is_aerial` の突合表を足した。結論は白**（版固定の終端はすべて v68 以下＝`000084` より前） |
| **中-7** | `000115` の根拠が `is_aerial` の名目セマンティクス依拠 | **採用** | **`000115` のヘッダへ「どちらの意味で直したか」を明記。** もう一方の意味（ラッシュ版生成の除外）には影響しないことを `internal/service/move/service.go:89` の述語で裏取りした |
| **低-8** | 破壊確認が `buildStarters` を呼んでいない | **採用** | **`Scan` 経由へ書き直し、外し方を 2 通りにした**（§4-3） |
| **低-9** | ダッシュ経由レーンのゲート単体テストが無い | **採用** | **`TestScan_DashLane_UsesSameGate` を追加**（有利 20F・dash total 10 ⇒ slack 10 の境界も） |
| **低-10** | ジャンプレーンだけ `candidateStartup` を再計算 | **採用** | 上の `startup` をそのまま渡す形へ整理（高-2 と同時） |
| **低-11** | canary の実測値を再現する手段が無い | **採用** | **§4-2 へ再現手順 4 ステップを明記** |
| **低-12** | コミット粒度（`752bfbf` に `migrate_m3104_test.go` 修正が同梱） | **不採用** | **直す手段が無い。** 履歴改変（rebase / amend）は `CLAUDE.md` §7 / §10 で機械的に禁止されており、かつ当該コミットは push 済みである。**⇒ 指摘は正しいが製造の手番では解消できない。次サブ以降の粒度で留意する** |
| **低-13** | `000115` の down が新規 DB を golden と食い違う状態へ落とす | **不採用** | **意図的であり、`000112` が作った既存の型に揃えている。** down は「是正前の姿」へ戻すものであり、そこには矛盾も含まれる。**★ただし「同型の借金が 1 件増えた」という指摘自体は妥当なので、横断課題として設計伝達レポート §4 へ送る** |
| **低-14** | 版ゲート E-2-4 の証拠が弱い | **採用** | **§0 の表を枝元のマージコミット名指し**（`9a4d391` / `0b6f1ac` / `9b3b0d2`）**へ強めた** |
| **低-15** | `progress-log.md` の索引行が未追記 | **採用** | **Phase D で追記する**（`CLAUDE.md` §8） |

### 11-2 取り込み後の再検査

| 検査 | 結果 |
|---|---|
| `go test ./...` | **全 60 パッケージ ok** |
| `cd web && pnpm test` | **233 ファイル / 2,898 テスト passed**（取り込みは Go 側と docs のみのため再実行は確認目的） |
| `make e2e`（全数） | **345 passed** |
| `bash scripts/check-migration-license.sh` | 違反なし |
| 変異テスト（再実施） | 除外を外すと **3 本**が赤（低-8 の書き直しで 2 → 3 本に増えた） |

---

## 12. 未解消のまま停止した項目

**製造の作業としては、なし。** 本サブの射程内に未解消の実装項目は無い。§8 と §9 の横断課題は**設計卓の手番へ送るもの**であり、製造の未解消項目ではない（原稿は設計伝達レポート §4 へ置く）。

**★★ただし回答待ちが 2 件ある**（2026-09-13 追記）。**いずれも本サブの完了条件には含まれないが、放置すると下流で詰まる。**

| # | 回答待ちのもの | 待っている相手 | 詰まるもの |
|---|---|---|---|
| 1 | **`fastest_unreachable` の人手判断 1 行**（鷹嘴連拳） | **開発者**（§13-1） | 値が未判断のまま既定 `false` で入っている状態が続く |
| 2 | **backfill 用のマイグレ番号 1 本** | **設計卓**（伝達レポート §4-8。開発者がリレー） | **開発者が 109 行を埋めた時点で `csv_db_sync_test` が赤になる** |

---

## 13. ★★開発者の手動確認（**自動テストが構造的に届かない範囲**）

> **★★★【2026-09-14・完了】開発者が全項目の確認完了を宣言した**（逐語＝「**全て問題なし。設計卓へ連絡します。**」）**。⇒ 本サブの手動確認は閉じる。**
>
> **★あわせて開発者の手元 DB（`/home/node/.local/share/tacpendium/tacpendium.db`）の状態が実測された**——`is_aerial=0 / startup=6 / is_derived=0`。**⇒ `000115` は新旧どちらも未適用**（旧内容なら `is_aerial=1`、新内容なら `startup` が NULL）**＝v114 以下であり、§13-1b の修復は不要だった。** アプリ起動時の `m.Up()`（`internal/infra/migration/migrate.go:95`）で `000115`（新）と `000116` が順に当たる。
>
> **★申し送り（未検証）**——このあと `main` のビルドへ戻すと、**DB は v116 なのにバイナリ内のマイグレは v112 まで**という状態になる。`golang-migrate` は次の版が見つからず `ErrNoChange` で素通りする見込みで余分な列が残るだけと考えられるが、**この経路は実測していない。**

> **★★★本節は 2026-09-13 に追加した。** 着地報告の時点で**請求すべき人手判断を 1 件落としていた**（開発者の問い＝「全ては機械判定できないので私が見ないといけない認識でしたが、特にその請求を受けていない」）。**⇒ 何が機械で判定でき、何ができなかったかを分けて残す。**

### 13-0 機械で判定できた範囲（参考・再掲）

`go test ./...` 全 60 パッケージ ／ `pnpm test` 2,898 ／ `make e2e` 345 ／ `check-artifact-integrity.sh` ほか検査 6 本 ／ **canary による実データ前後比較**（消えた候補 106 件・新規 0 件・うち 105 件が集合 B ちょうど）／ **変異テスト**（除外を外すと 3 本が赤）。

**★これらは「述語が意図どおり効いているか」を判定する。⇒ 「その値でよいか」「その見え方でよいか」は判定しない。**

### 13-1 ★★★空中限定ターゲットコンボの形へ揃えた（**追補2**・実機で見てほしい）

> **★初版の §13-1 は「`fastest_unreachable` の人手判断を請求」だった。⇒ 撤回した。** 開発者指示で `soaring_eagle_punches` を elena `soaring_raid` の形へ揃えた結果、`is_aerial` が `0` へ戻り、**B 型ロースターに入らなくなったため請求自体が不要になった。**

| 項目 | 内容 |
|---|---|
| **何をしたか** | chun_li `soaring_eagle_punches`（鷹嘴連拳）を **elena `soaring_raid` / ingrid `satelite_leap` と同じ形**へ揃えた——**フレーム 4 列を空に ／ `is_aerial=0` ／ `is_derived=1` ／ `notes_tool` を更新** |
| **なぜ機械で判定できないか** | **「この技を確定反撃で扱うか」はゲームの性質であり、データからは決まらない。** 開発者の逐語＝「**空中限定のターゲットコンボで確定反撃ではそもそも扱わない対象外になる**」 |
| **★実測（canary・31 キャラ総当たり）** | **3 行が完全に一致した**——始動技としての出現 **0 件** ／ 相手技として手動確認レーン（`data_missing`）に **62 件**（31 キャラ × 2 タブ） |
| **手順** | 確定反撃サーチ → **相手キャラ＝春麗** → ガード / ジャストパリィの両タブ → **鷹嘴連拳が「自動判定できない相手技」セクションに出ること**を見る（`soaring_raid` / `satelite_leap` と同じ見え方になるのが期待値）。**★始動技としては出ない** |

**★★同型の行が他にもあると気づいたら、`first_hit_startup` を埋めるのではなくこの形へ揃えるほうが正しい**（判断材料は `docs/progress/20260913-M37-04-dropped-starters.md` §5）。

### 13-1b ★`000115` を差し替えたことによる DB への影響（**該当するか確かめてほしい**）

**`000115` は一度別の内容**（`is_aerial` を `1` にするだけ）**を持っていた。★`golang-migrate` は版数を記録するだけでファイルのチェックサムを取らないため、旧内容を適用済みの DB は書き換えても再実行されない。**

**★対象は「本ブランチを checkout してアプリを起動した `tacpendium.db`」だけである**（CI・テスト DB は毎回ゼロから ／ E2E DB は起動時に `rm -rf web/e2e/.tmp` ／ 過去の検証 DB は v6x 世代 ／ `main` 未マージのため配布物なし）。

```sql
-- (1) 該当するか（115 以上なら対象）
SELECT version, dirty FROM schema_migrations;

-- (2) 旧内容が残っているか（is_aerial=1 や startup=6 なら旧のまま）
SELECT m.is_aerial, m.startup, m.is_derived FROM moves m
  JOIN characters c ON c.id = m.character_id
 WHERE c.code = 'chun_li' AND m.code = 'soaring_eagle_punches';
```

**★★直し方＝新 `000115` の `UPDATE` を 1 回そのまま流す。** 同マイグレは**絶対値を書く設計**にしてあるため、旧内容が当たっていても最終状態が揃う。**⇒ 登録済みのコンボ・タグは消えない。**

### 13-2 ★★鷹嘴連拳が「相手技」の一覧からも消えること（**実機で見てほしい**）

| 項目 | 内容 |
|---|---|
| **何が起きるか** | `is_aerial=true` は `isNonPunishableTarget` にも効く。**⇒ 鷹嘴連拳は「反撃したい相手技」としても走査から完全に外れる**（成立レーンにも手動確認レーンにも出ない） |
| **なぜ機械で判定できないか** | **既存方針（相手の空中攻撃は確定反撃の対象にしない）どおりであり機械的には意図どおりだが、それでよいかは製品判断である** |
| **手順** | 確定反撃サーチを開く → **相手キャラ＝春麗** → ガード / ジャストパリィの**両タブ**で、相手技の一覧に**鷹嘴連拳が出ないこと**を見る。**出ないのが期待値** |

### 13-3 ★候補が実際に減ったこと（**画面で見てほしい**）

| 項目 | 内容 |
|---|---|
| **なぜ機械で判定できないか** | **E2E は候補の中身を主張していない**——`web/e2e/m18-02-punish-search.spec.ts` は ryu vs ken で `startup <= 30` の技を**動的に選ぶ**作りであり、特定の行の有無を固定していない。**⇒ 105 件の消失は Go の canary で確かめたもので、画面では見ていない** |
| **手順** | 確定反撃サーチ → **自キャラ＝リュウ** → 相手キャラは任意 → **有利 +5F 前後の相手技**を開く → 始動技に **不破三連撃（2 発止め）が出ないこと**を見る |

### 13-4 ★偽陰性が出ていないこと（**画面で見てほしい**）

| 項目 | 内容 |
|---|---|
| **なぜ機械で判定できないか** | 13-3 と同じ。`basis='through'` 4 行の残存は canary と単体テストで確認済みだが、**画面表示は未確認** |
| **手順** | 確定反撃サーチ → **自キャラ＝ジェイミー** → **ジャストパリィタブ**（有利が大きく取りやすい）→ 有利が 16F 以上の相手技を開く → **乱酔旋系が候補に残っていること**を見る |

### 13-5 ~~109 行の手入力~~ → **★★完了（2026-09-14・追補3）**

> **★★★開発者がルールを提示し、106 行を機械解決して投入した。⇒ 手入力は不要になった。**

| 項目 | 内容 |
|---|---|
| **開発者の指示**（逐語） | 「**commands を見れば全て機械的に対応可能だったので、手入力は省略していいですか？ あなたの方でやってもらえますか？**」＋ 解決ルール 3 点 |
| **結果** | **106 / 106 解決・未解決 0 件**（解決先は `normal` 83 / `unique` 23、`special` や入れ子は 0） |
| **投入** | マイグレ **`000116_data_backfill_moves_first_hit_startup`**（層 B・**2026-09-14 開発者裁定で払い出し**）。CSV と同じ値を 1 行 1 `UPDATE` で投入（先例＝`000064`） |
| **投入しない 4 行** | dee_jay `party_in_the_air` ／ elena `soaring_raid` ／ elena `raptor_range` ／ chun_li `soaring_eagle_punches`。**空中限定でフレームを持たず、確定反撃の対象外** |
| **対応表の全数** | `docs/progress/20260913-M37-04-dropped-starters.md` §6 |

### 13-6 ★★候補が「着手前より増える」こと（**実機で見てほしい・追補3**）

**★埋めたことで挙動が 2 方向に動いた。**

| 関係 | 行数 | 画面での見え方 |
|---|---:|---|
| 初段のほうが記録より**遅い** | **17 行** | **恒久的に候補から落ちる**（記録値が速すぎて偽陽性を出していた） |
| 初段のほうが記録より**速い** | **88 行** | **★着手前より*緩い有利フレームで*候補に出る** |

**例＝dee_jay `funky_dance` は記録 20 だが初段は立中P の 7。⇒ 着手前は有利 20F 以上でしか出なかったものが、有利 7F から出る。**

**★canary 実測**——新たに現れた code は **0 件**、消えた code は `soaring_eagle_punches` の **1 件のみ**、始動技の延べ出現は **+54,462**。

**★★あわせて `soaring_eagle_punches` が「相手技」としても変わった**——着手前は `recovery=41` を持っていたため**ジャストパリィタブに「有利 +41F の相手技」として 31 ノード出ていた**（そこに 40 本以上の始動技がぶら下がっていた）。**⇒ 空中限定の形にしたことで手動確認レーンへ移り、通常技 1,304 種の延べ出現が減った。これは `soaring_raid` と同じ扱いになった結果である。**

**手順**: 自キャラ＝ディージェイ → 有利 7〜19F の相手技を開き、**ファンキーダンスが候補に出ること**を見る（着手前は出なかった）。あわせて相手キャラ＝春麗のジャストパリィタブで**鷹嘴連拳が成立レーンに出ないこと**を見る。


---

*以上、M37-04 完了報告。* **★本サブの芯は「初段を同定しない」ことである**——同定は実測で 59 行が判定不能であり、`command` 列は DB に無い。値を直接持てばその困難がまるごと消えた。 **★★次に重いのは破壊確認である**——実データで 106 件が消えたが、**そのうち 105 件がちょうど集合 B の地上候補数と一致し、新たに現れた候補は 0 件であった。⇒ 範囲を広げずに偽陽性だけを落とせたと言える。**
