# 指示書 M24-07: 用語・i18n・内部値露出の最終 sweep

| 項目 | 内容 |
|------|------|
| 文書ID | M24-07 |
| バージョン | 1.1.0 |
| 推奨モデル | **高精度実装**（**★★語彙の決定が含まれ、しかも `recipe_cache` と CSV 列名という 2 つの契約に隣接する**。実モデル名は `docs/human-notes/model-allocation.md` が正本） |
| Plan Mode | **必須**（**★★実査 14 件 ＋ 語彙の候補出しを Plan Mode に含める**。**★語彙は開発者が決める**＝`CLAUDE.md` §9 のハード列） |
| 機械レビュー | 必須（別チェックリスト: `M24-07-review-checklist.md`） |
| 対象指示書 | （本書は指示書。チェックリスト側が本書を指す） |
| 参照してよい先行資料 | `DES-005` §5.4 / §5.6 / §5.9.2 / §5.13a ／ `DES-004` §2.2 ／ `DES-002` §7.6 ／ `docs/handover/followup-backlog.md` §AI / §AK / §AS / §AT ／ `docs/handover/code-facts.md` ／ ledger `M24-RESEARCH-01` §2 / §3（**★他サブの指示書・チェックリストは含めない**＝D-217） |
| 並列性 | **★★並列にしない**（`M24-overview` §5＝**locale の大規模 rename は同時に一人だけ**。**先に部分着手すると locale conflict と手戻りが増える**） |
| 依存 | **★★M24 の UI レーン 01〜06 の完了**（**済**＝`D-605` / `D-606`。**新語彙が確定してから一括整合する**） |
| CHANGE | **`CHANGE-147` v1.0.0**（**設計卓が起票済み。製造は番号を消費しない**）。影響設計書＝**`DES-005` §5.4**（主対象＝レシピ表示トグルの語彙の決め直し） ／ **`DES-005` §5.9.2**（条件付き） ／ **`DES-004` §2.2**（条件付き＝表示語の写像に触れる場合） |
| マイグレ | **0 本の見込み**。**★消費するなら次は `000080`** |
| 想定所要時間 | 300〜390 分 |
| 作成者・作成日 | 設計担当 Claude（設計卓・M24 期）/ 2026-08-30 |

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| 1.1.0 | 2026-08-30 | **★★完了後の是正である**（**D-618**・計測点 **`M-103`**）。**★§4.8 の 1 行が正本**（followup `character-selector-label-is-ja-fixed`）**を圧縮しすぎて、逆の読みを与えていた**〔正本は「ラベルの i18n が実現したときに、並べ替えのキーも `name_en` へ揃えること。片方だけ変えないこと」を求めている〕。**★製造は正本に従い、ラベルと並べ替えの両方を `name_en` へ揃えた。⇒ 判断は正しい。設計卓が受理した。** |
| 1.0.0 | 2026-08-30 | 初版（**D-608**） |

---

## 1. 背景と目的

### 1.1 扱うもの——**3 つの束**

| 束 | 内容 | 出所 |
|---|---|---|
| **A. 語彙の決定** | **どの語を使うかを決める。★決めるのは開発者である** | `SM-016` / `SM-008` / `SM-069` ／ `SM-110` の 20 語 ／ `recipe-view-toggle-vocabulary` |
| **B. ハードコード日本語の掃き出し** | **英語 UI でも日本語が出る箇所を locale へ移す** | followup §AI 3 件 ／ §AK 1 件 ／ §AS 2 件 ／ `CO-020` |
| **C. 内部値の画面露出とラベルの不足** | **内部コード・英語 move code が画面に出る ／ 帰属が読めない** | `SM-006`（＝`SM-059`） ／ `SM-133` ／ `SM-139` |

### 1.2 ★★本サブの最大の危険——**語彙を変えると 2 つの契約に触れる**

**★★起票前に契約を持つ節を走査した**（playbook §4.51）。**2 件当たった。**

| # | 契約 | なぜ当たるか |
|---|---|---|
| **★★1** | **契約 F-1**（`m21-contract` §5。**`internal/service/notation/` / `internal/service/preset/` は触らない ／ `recipe_cache` の読み書きは M20 所管**） | **★★レシピ表記の語**（`SM-069`）**を変えると、`recipe_cache` に焼かれた既存データと食い違う。** **★`recipe_cache` は DB に保存された文字列であり、表示のたびに組み立て直しているとは限らない** |
| **★★2** | **`DES-002` §7.6**（**CSV の必須列・任意列は列名が契約。ZIP エントリ名も契約**） | **★★CSV の列名は「画面に出る語」ではないが、語彙の一括置換で巻き込まれうる。★巻き込むと往復が壊れる** |

**⇒ §2.2 で両方を「変更しないもの」に置いた。§3.3 で実査させる。§5.3 の破壊確認で「巻き込んでいない」ことを見せさせる。**

### 1.3 ★★もう 1 つの危険——**「英語ロケール整備」は際限なく広がる**

**`CO-026`（`D-2` 英語ロケール整備）は、そのままだと 487 leaf キーの訳文すべてが対象になる。**

**★実測**（ledger）——`ja.json` と `en.json` は**ともにトップレベル 23・leaf キー 487 で一致している。⇒ キーの欠落による parity 崩れは無い。** **★`CO-026` が扱うのは訳文の実データである。**

**⇒ 射程を絞る**（§1.6）。**★既存 487 キーの訳文の全面見直しはしない。**

### 1.4 ★★`SM-068`（＝`SM-074`）は本サブから外した

**`M24-overview` §3 は `SM-068`（＝`SM-074`。登録時の重複エラーの表示改善）を `M24-07` へ割り付けていた。⇒ 本サブから外し、`M24-08` へ移す。**

**★理由 2 つ**——**(1) i18n ではない。「重複した既存コンボのどこが差分か」を見せる UX の話である ／ (2) ledger が「`CO-010`（`useCheckDuplicate` の TanStack 化）と実装面が同一であり、束ねる余地がある」と書いている。★`M24-08` が TanStack の面である。**

**⇒ `M24-overview` §3 の割付を変えた**（`D-608`）。

### 1.5 ★★起票時に設計卓が確かめていないこと

**★設計卓は武装中であり、実装ソースを読んでいない。以下はすべて ledger と followup の記述であって、設計卓が現物で裏を取ったものは 1 つも無い。**

| 対象 | 分からないこと |
|---|---|
| `recipe_cache` | **★★レシピ表記の語が `recipe_cache` に焼かれているか。表示のたびに組み立て直しているか。★これが本サブの分岐点である** |
| locale | **「leaf キー 487・トップレベル 23」**——**N を数え直す**（playbook §4.40） |
| ハードコード日本語 | **★followup が挙げた面の全数。★「4 軸」「20 語」等の N を数え直す** |
| `CO-020` | **仮想コントローラの文字列が何本あるか** |
| キャラ名 | **★英語表示のデータが在るか**（`name_en` 相当）。**無ければ `character-selector-label-is-ja-fixed` は本サブで解けない** |

### 1.6 ★★このサブで作らないもの（スコープ外）

| # | 作らないもの | 理由 |
|---|---|---|
| **★★1** | **既存 487 キーの訳文の全面見直し** | **★際限が無い**（§1.3）。**★見るのは「新語彙に関わるキー」と「日本語が漏れている箇所」だけ** |
| **★★2** | **`internal/service/notation/` / `internal/service/preset/` の変更** | **契約 F-1**（§1.2-1）。**★表示語は写像で解く。内部表現を変えない** |
| **★★3** | **`recipe_cache` の再構築を伴う変更** | **契約 F-1。★必要になったら止めて設計卓へ報告する**（§9.1） |
| **★★4** | **CSV の列名・ZIP エントリ名の変更** | **`DES-002` §7.6 の契約**（§1.2-2）。**★往復が壊れる** |
| **5** | **`DES-004` の内部表現の変更** | **★内部表現仕様書である。★変えるのは表示語だけ** |
| **6** | **`SM-068` / `SM-074`（重複エラーの表示改善）** | **★`M24-08` へ移した**（§1.4） |
| **7** | **画面構造の変更** | **★本サブは語とラベルのサブである。★ラベルを足すことは可、レイアウトを組み替えることは不可** |
| **8** | **中国語等の追加ロケール** | **★`ja` / `en` の 2 つだけ** |
| **9** | **`SM-110` の 20 語のうち、決まらなかった語の実装** | **★開発者が決めた語だけを入れる**（§4.1） |

### 1.7 目的

**英語 UI で日本語が出ないようにし、語彙を 1 本へ揃える。あわせて内部値の画面露出を消し、帰属が読めないラベルを足す。**

---

## 2. 成果物

### 2.1 作成/修正するファイル

| ファイル | 内容 |
|---|---|
| `web/src/locales/ja.json` / `en.json` | **★本サブの主戦場。新規キーと、決まった語への差し替え** |
| `web/src/constants/` 配下のラベル定数 | **★ハードコード日本語を locale 参照へ**（followup §AI / §AK） |
| タグ管理画面 ／ 一覧フィルタ ／ 仮想コントローラ ／ `SetupEditorPage` ／ `SetupCandidateList` ／ `SetupAccordionItem` | **★i18n 化**（**★面の全数は §3.3 で実査**） |
| 一覧の始動状況（`SM-006` / `SM-059`） | **★内部の move code を画面から消す** |
| 確定反撃の表示（`SM-133`） ／ セットプレイのフィルタ（`SM-139`） | **★ラベルを足す** |
| **語彙の純粋関数テスト ／ i18n parity テスト** | §5.1 |
| `docs/progress/progress-log.md` | **★追記**（CLAUDE.md §8） |

### 2.2 変更しないもの（原則）

| 対象 | 何を守るか | 根拠 |
|---|---|---|
| **`internal/service/notation/` / `internal/service/preset/`** | **契約 F-1** | `m21-contract` §5 |
| **`recipe_cache` の中身** | **契約 F-1**（M20 所管） | 同上 |
| **CSV の列名 ／ ZIP エントリ名** | **往復契約** | `DES-002` §7.6 |
| **`DES-004` の内部表現** | **内部表現と表示語の分離** | `DES-004` §2.2 |
| **`ja` / `en` のトップレベル構造** | **★parity**（ともに 23・487 で一致している） | ledger 実測 |
| **画面のレイアウト** | **★本サブは語とラベルのサブである** | §1.6-7 |
| **`web/playwright.config.ts` の `workers`** | 直列実行 | `SUPP-001` §5.5・`D-597` |

### 2.3 例外条項

**★ハードコード日本語を locale へ移すために、対象コンポーネントの `t()` 呼び出しを足すことは可。** **★ただし「画面内で 2 系統が混ざる」形にしない**——**1 画面は全部 i18n か、全部直書きかのどちらかにする**（`M24-02` / `M24-05` / `M24-06` が守った作法）。

**★触ったら報告すること**（§7.4.1）。

### 2.4 ★並列性

**★★並列にしない。** `M24-overview` §5＝**locale の大規模 rename は同時に一人だけ。先に部分着手すると locale conflict と手戻りが増える。**

---

## 3. 前提条件

### 3.1 必読ドキュメント

- `CLAUDE.md` §4（TypeScript 規約・列挙定数の同期） / §5 / §7 / §8 / §10
- `DES-005` §5.4（**★レシピ表示トグルの語彙。本サブで決め直す**） / §5.6 / §5.9.2 / §5.13a
- `DES-004` §2.2（**★内部表現と表示語の分離**）
- `DES-002` §7.6（**★CSV 列名の契約**）
- `m21-contract` §5（**★契約 F-1**）
- `docs/handover/followup-backlog.md` **§AI / §AK / §AS / §AT**（**★本サブの出所**）
- `docs/handover/code-facts.md`（**★引く。想定で書かない**）

### 3.2 参照不要

- 他サブの指示書・レビューチェックリスト（`D-217`）
- `docs/postmortem/`

### 3.3 ★★着手前の確認（Plan Mode 必須）

**★以下はすべて実査項目である。§1.5 のとおり、設計卓が現物で裏を取ったものは 1 つも無い。**

**★★「N 個ある」と書かれた列挙は、その N を数え直すこと**（playbook §4.40）。**★★あわせて「列挙そのものが全数か」を疑うこと**（`M24-09d` の教訓＝**数が合っていても、数える対象の集合が指示書で狭められていることがある**）。

| # | 確かめること | なぜ要るか |
|---|---|---|
| **★★1** | **★★レシピ表記の語が `recipe_cache` に焼かれているか。表示のたびに組み立て直しているか**（**★どちらか断定できる証拠を出すこと**） | **★★§1.2-1。★★焼かれているなら `SM-069` は本サブでは解けない。⇒ 止めて設計卓へ報告する**（§9.1-1） |
| **★★2** | **★★CSV の列名・ZIP エントリ名が、語彙の置換対象と文字列として重なるか**（**母数付き**） | **★★§1.2-2。★重なるなら置換の範囲を先に切る** |
| **3** | **`ja.json` / `en.json` の leaf キー数とトップレベル数**（**★「487 / 23」の N を数え直す**） | **★parity の母数。★本サブの前後で崩さない** |
| **4** | **★ハードコード日本語が残っている面の全数**（**母数付き。★followup が挙げた面だけを母集団にしないこと**） | **★★followup は「見つかったもの」の一覧であって全数ではない。★`t()` 呼び出しが 0 の画面を機械的に洗い出すこと** |
| **5** | **★一覧フィルタのラベル定数の軸数**（**★followup は「4 軸」と書いている。N を数え直す**） | **★`position` / `hitType` / `opponentStance` / `setupTechType` ＋ `OKI_NO_GAUGE_LABEL`。★他に在るか** |
| **6** | **★`CO-020` 仮想コントローラの文字列の全数**（**母数付き**） | **★共通部品 `HitBoxLayout` 系。★面の数え上げは「共通部品を使っている面」で行う**（playbook §4.39） |
| **★7** | **★★キャラ名の英語表示のデータが在るか**（`name_en` 相当の列 or 辞書） | **★★無ければ `character-selector-label-is-ja-fixed` は本サブで解けない。⇒ 落として報告する。★勝手に訳語を作らない** |
| **8** | **★`SM-006` / `SM-059` の内部値露出の全数**（**母数付き**） | **★ledger は `web/src/features/combo/utils.ts` の 1 か所を挙げている。★他に在るか** |
| **9** | **★`SM-133`（確定反撃の帰属）の現在の表示。「相手の技」「自分の技」の区別が構造として在るか** | **★構造が在ればラベルを足すだけで済む。無ければ本サブの射程を越える** |
| **10** | **★`SM-139`（セットプレイのフィルタ）が一覧のどこに出ているか。コンボ由来のフィルタと同じ並びか** | **★ラベルで見分けさせるのか、並びを分けるのか。★並びを分けるのは §1.6-7 に触れる** |
| **11** | **★`SM-016`（「ステップ」）／`SM-008`（link → 目押し）／`SM-069`（投げ重ねの DR 表記）の現在の語が、それぞれ何か所に出ているか**（**母数付き**） | **★§4.1 の候補出しの材料。★置換の影響範囲** |
| **★12** | **★★`recipe-view-toggle-vocabulary` のトグルのラベルとホバー文が、いま何というキーで、何と書かれているか**（**逐語**） | **★★§4.6。★`DES-005` §5.4 の語彙を決め直すため、現物の逐語が要る** |
| **13** | **★`setplay.adoptNameFormat` の現在の ja / en の値**（**逐語**） | **★★§4.7。★このキーは書式の正本であり、翻訳がそのまま命名規則の変更になる**（`DES-005` §5.9.2） |
| **14** | **★空表示の文言 4 種**（`empty-recipe-label-four-variants`）**の現在地と、それぞれの出所**（**母数付き**） | **★§4.2。★1 本へ揃えるとき、`RECIPE_EMPTY_LABEL` 定数を正典にできるか** |

---

## 4. 詳細仕様

### 4.1 ★★語彙の決定（**Plan Mode のゲート。★開発者が決める**）

**★★実装の前に、候補を並べて開発者へ諮ること。** **`CLAUDE.md` §9 のハード列＝「正解がなく好みが決め手」に該当する。**

**★★出す形**——**1 語につき「現在の語 ／ 出ている面と件数 ／ 候補 2〜3 案 ／ 製造の推奨と理由」の 4 列。**

| # | 対象 | 現在の語（**★実査で確かめる**） | 備考 |
|---|---|---|---|
| **1** | **`SM-016`** | **「ステップ」** | **★`M16-04` で step taxonomy が明文化された後も、画面表示語の再検討は残っている** |
| **2** | **`SM-008`** | **「リンク」** | **★memo の要求＝日本語版では「目押し」と表示したい** |
| **3** | **`SM-069`** | **投げ重ねのドライブラッシュ表記** | **★★§1.2-1 の実査 1 の答えが「焼かれている」なら、本件は落とす**（§9.1-1） |
| **★★4** | **`recipe-view-toggle-vocabulary`** | **「レシピ全文表示」** | **★★§4.6。設計卓の案あり** |
| **5** | **`SM-110` の 20 語** | **`M24-06` 完了報告 §2-C の 3 列表** | **★★とくに「インポート」と「取込」がアプリ内で 2 本立てになっている。★語を選ぶ前に統一の要否を決める** |
| **6** | **空表示の文言** | **4 種**（`empty-recipe-label-four-variants`） | **★1 本へ揃える** |

**★★決まらなかった語は実装しない**（§1.6-9）。**★「決まらなかった」ことを完了報告へ書くこと。**

### 4.2 ハードコード日本語の掃き出し

| 対象 | 出所 | 決めたこと |
|---|---|---|
| **タグ管理画面**（`t()` 呼び出し 0） | §AI `tag-management-page-not-i18n` | **★画面ごと i18n 化する。★`M24-02` が足した 4 本の直書きも同時に移す**（**★あの判断は正しかった**＝画面内で 2 系統が混ざるのを避けた） |
| **一覧フィルタのラベル定数** | §AI `filter-value-labels-hardcoded-ja` ／ §AK `oki-no-gauge-label-hardcoded-ja` | **★選択肢でも要約ピルでも同じ定数を引くこと**（**★2 か所に分けない**＝`E-76`） |
| **`SetupEditorPage` / `SetupCandidateList` / `SetupAccordionItem`** | §AS `setup-editor-page-hardcoded-ja` | **★画面ごと i18n 化する**（**`M24-05` が「新規の 2 文字列だけ i18n にすると 2 系統が混ざる」として見送った分**） |
| **仮想コントローラ**（`CO-020`） | ledger §3 | **★共通部品であり、通っている面の全数で効く**（§3.3-6） |
| **空表示の文言 4 種** | §AS `empty-recipe-label-four-variants` | **★§4.1-6 で決めた 1 語へ揃える** |

**★★守ること**——**1 画面は全部 i18n か、全部直書きかのどちらかにする**（§2.3）。

### 4.3 内部値の画面露出（`SM-006` ＝ `SM-059`）

**★一覧の始動状況が英語の move code から始まっている**（ledger の実査＝`starterMoveCode` を先頭に push）。

| 論点 | 決めたこと |
|---|---|
| **形** | **★内部の move code を画面から消し、技の表示名を出す** |
| **★言語** | **★技名は `nameJa` 系である**（`DES-005` §5.9.2 の as-built＝**en ロケールでも技名は日本語**）。**⇒ 本サブで技名の英語化はしない。★それは別の話であり、データが要る** |
| **★フォールバック** | **★表示名が無いときにどうするかを決めて報告すること**（**★内部コードへ戻すなら、それが「露出」でないかを説明すること**） |
| **面** | **★§3.3-8 で数えた全数**（**★ledger は 1 か所を挙げているが、それが全数とは限らない**） |

### 4.4 ラベルの追加（`SM-133` / `SM-139`）

| # | 対象 | 決めたこと |
|---|---|---|
| **`SM-133`** | **確定反撃で「相手の技」「自分の技」が見失われる** | **★★ラベルを足すだけにする。★構造が既に区別できているなら、明示ラベルを付けるだけで済む**（ledger の注記）。**★構造が無ければ落として報告する**（§3.3-9） |
| **`SM-139`** | **セットプレイのフィルタがセットプレイ用だと分からない** | **★★ラベル・見出し・グループ名で見分けさせる。★並びを分けない**（§1.6-7）。**★機序＝`M19-06` の絞り込みがコンボ一覧側に増えたため、どのフィルタがセットプレイ由来か読めなくなった** |

### 4.5 `CO-020`——仮想コントローラの i18n

**★UI 文字列がすべてハードコード日本語であり、`M17-03` でもキーを追加していない**（ledger）。

**★★面の数え上げは「共通部品を使っている面」で行うこと**（playbook §4.39）。**★`HitBoxLayout` 系を通っている面は、部品を直した時点で全部に及ぶ。**

### 4.6 ★★`recipe-view-toggle-vocabulary`——**`DES-005` §5.4 の軸を決め直す**（**★`CHANGE-147` の主対象**）

**★★問題**——**`DES-005` §5.4 は「通常は末尾省略、トグルで全文表示モードへ切替」と定めているが、5 面のうち 4 面でしか成立していない。コンボ詳細だけ `compactClassName` が `whitespace-pre-wrap break-words` であり、トグル OFF でも折り返して全部読める。**

**★開発者の逐語**（2026-08-30 実機確認）＝**「レシピは元から全文表示されており、これは縦に表示するボタンなので、ラベル名が実際の機能を表していない」。**

| 案 | 内容 | 設計卓の評価 |
|---|---|---|
| **★★A（推奨）** | **軸を「省略/全文」から「1 行にまとめる/ステップごとに改行」へ移す** | **★★この軸なら 4 面でも成立する**〔OFF は 1 行で幅により切られ、ON はステップごとに改行して全部見える〕。**★ホバーの説明文の括弧内が既にそう言っている**（`comboCommon.recipeView.hint`＝「(ステップごとに改行)」） |
| **B** | **コンボ詳細の `compactClassName` を他面と揃えて幅で切る** | **★採らない。`SM-022`〔詳細で縦に読みたい〕と逆行する** |

**⇒ 案 A を採る。★ただし具体の文言は §4.1 で開発者へ諮ること。**

**★★`DES-005` §5.4 の本文も同じ軸へ書き換わる。⇒ `CHANGE-147` の主対象である。**

### 4.7 ★★`setplay.adoptNameFormat`——**翻訳ではなく命名規則の変更である**

**★★同キーはセットプレイ名の自動命名の書式の正本である**（`DES-005` §5.9.2。**純粋関数ではなくロケール文字列が正本**）。

| 論点 | 決めたこと |
|---|---|
| **★★扱い** | **★★「文言の翻訳」ではなく「命名規則の変更」として扱う。⇒ 触るなら §4.1 で開発者へ諮ること** |
| **★現状** | **ja＝`"{{move}} 持続{{n}}F目重ね"` ／ en＝`"{{move}} meaty on active frame {{n}}"`**（`DES-005` §5.9.2。**★逐語は §3.3-13 で確かめる**） |
| **★推奨** | **★触らないことを推す。★`SM-088` は `M24-05` で決着したばかりであり、本サブの目的（日本語の漏れを消す）には該当しない** |
| **★プレースホルダ** | **★`{{move}}` / `{{n}}` を落とさないこと。落とすと名前が壊れる** |

### 4.8 ★キャラ選択のラベル（`character-selector-label-is-ja-fixed`）

**★キャラ選択部品は言語に関わらず `name_ja` を表示する**（§AI）。

**★★§3.3-7 の実査で英語表示のデータが無ければ、本項は落として報告すること。★勝手に訳語を作らない。**

**【★★2026-08-30 是正＝`D-618` / 計測点 `M-103`】本行は正本を圧縮しすぎて、逆の読みを与えていた。** **★正本の逐語**（followup `character-selector-label-is-ja-fixed`）:

> **`CHANGE-133` の起票時に置いた「英語なら a〜z の昇順」は、目的**〔英語利用者が探しやすい順〕**を達成できないため撤回した**（`D-562`）。**★並べ替えのキーは `name_ja` 固定で着地している。**
>
> **⇒ ラベルの i18n が実現したときに、並べ替えのキーも `name_en` へ揃えること。★★片方だけ変えないこと**——**表示が ja のまま並びだけ英語順にすると、利用者の目には無順序に見える**（実際に起きた）。

 **⇒ 撤回されたのは「a〜z の昇順にする」という起票時の理由づけであって、「ラベルを i18n したら並びも揃える」は残っている。** **★★正しい指示＝ラベルを `name_en` にするなら、並べ替えのキーも `name_en` へ揃えること。片方だけ変えないこと。**  以下は旧記述（**★誤り。撤回する**）： ~~**★`CHANGE-133` の起票時に置いた「英語なら a〜z の昇順」は撤回済みである**（§AI）。**⇒ 並び順を勝手に変えないこと。**~~

---

## 5. テスト要件

### 5.1 追加するテスト

| # | 内容 |
|---|---|
| **★★1** | **i18n parity**——**`ja.json` と `en.json` の leaf キー集合が一致すること**（**★キー数だけでなく集合で比べる**）。**★本サブの前後で崩さない** |
| **★★2** | **英語ロケールで日本語が出ないこと**——**`en.json` の値に日本語の文字が含まれないこと**（**★例外は「技名」等、意図的に日本語のままのもの。★例外は一覧として持ち、テストへ書くこと**） |
| **3** | **★決めた語の純粋関数テスト**（ラベル定数を引く写像が在れば、そこを固定する） |
| **4** | **★`setplay.adoptNameFormat` のプレースホルダ `{{move}}` / `{{n}}` が ja / en の両方に在ること** |
| **★5** | **★★CSV の列名が語彙の変更を受けていないこと**（**§1.2-2 の契約。★既存テストで担保されているなら、その所在を報告するだけでよい**） |

### 5.2 既存テストの扱い

**★★テストが固定している文字列を変えるときは、「変えてはいけないから固定してある」のか「たまたま現状を写しただけ」なのかを 1 件ずつ判定すること**（playbook §4.45 の規約 2）。

**★★1 本も消さない・skip しない。** **★文字列の変更でテストを緩めない。**

### 5.3 ★★破壊確認（**5 件・必須**）

**★★「壊す対象がその観測に含まれているか」を、期待を書く前に 1 件ずつ確かめること**（playbook §4.32）。

| # | 壊し方 | 期待 |
|---|---|---|
| **★★1** | **`en.json` の 1 キーを日本語へ戻す** | **★§5.1-2 が赤くなる** |
| **★★2** | **`en.json` から 1 キーを消す** | **★§5.1-1 が赤くなる** |
| **★★3** | **CSV の列名を 1 つ、決めた新語彙へ置き換えてみる** | **★★往復のテストが赤くなる。★これが `DES-002` §7.6 の契約が守られていることの観測である。★緑のままなら契約を守る観測が無い** |
| **4** | **`setplay.adoptNameFormat` から `{{n}}` を落とす** | **★§5.1-4 が赤くなる** |
| **5** | **§4.3 の内部値露出を元へ戻す**（move code を先頭に戻す） | **★その面の観測が赤くなる。★赤くならないなら観測が無い** |

**★★破壊確認は絞り込み実行でよい**（`D-599`。**★`make e2e-only P=<パターン>` を使うこと**）。**★破壊確認後の確定実行は絞らないこと。**

---

## 6. レビュー観点（別ファイル参照）

`docs/instructions/reviews/M24-07-review-checklist.md` を参照。

---

## 7. 完了条件（Definition of Done）

### 7.1 機能要件

1. **★★§4.1 の語彙が開発者に諮られ、決まった語だけが実装されている**（**★決まらなかった語は「決まらなかった」と報告されている**）。
2. **★★`recipe_cache` に触れていない**（§1.6-3）。**★§3.3-1 の答えが報告されている。**
3. **★★CSV の列名・ZIP エントリ名が変わっていない**（§1.6-4）。**★破壊確認 3 で観測が在ることが示されている。**
4. **★`internal/service/notation/` / `internal/service/preset/` が変わっていない**（契約 F-1）。
5. **★★`ja.json` と `en.json` の leaf キー集合が一致している**（§5.1-1）。
6. **★★英語ロケールで日本語が出ない**（§5.1-2。**★例外の一覧が報告されている**）。
7. **★§4.2 の面がすべて i18n 化されている**（**★§3.3-4 で数えた全数**）。**★1 画面で 2 系統が混ざっていない。**
8. **★§4.3 の内部値露出が消えている**（**★§3.3-8 で数えた全数**）。
9. **★§4.4 のラベルが足されている**（**★構造が無くて落とした場合は、その旨が報告されている**）。
10. **★§4.6 の語彙が決め直され、`DES-005` §5.4 の軸と一致している。**
11. **★§4.8 が落ちた場合、その理由が報告されている。**
12. **★破壊確認 5 件が実施され、コマンドと出力が載っている。**
13. **★テストが 1 本も減っていない・skip されていない。**

### 7.2 自己テスト結果

- `go test ./... -count=1`
- `cd web && pnpm test`
- **★★`pnpm build`**（**★型検査はこちらで見る**＝`web/CLAUDE.md` §3）
- `make e2e`
- **破壊確認 5 件**（§5.3）。**実施コマンドと出力を載せること。**

**★★E2E の着手前基準値は採ること**（`D-600` 規約 2）。**★理由＝直前に `M24-06` と `M24-09d` の 2 本がまとまって入っており、合流後の main を誰も回していない。** **★採取コマンドにパイプで切り詰めを入れないこと。**

**★★母数は「指示書に書いてあるから使える」ものではない**（playbook §4.37）。**⇒ 比較は同一セッション内の背中合わせで採ること。**

**★★ポートの空き確認に `ss` / `netstat` / `lsof` を使わないこと**（playbook §4.37 規約 7）。**★この環境にはどれも無く、grep が必ず空になって「空き」と誤答する。**

### 7.3 品質チェック

- `bash scripts/check-artifact-integrity.sh`（**★1 本目に回す**）
- `bash scripts/check-md-emphasis.sh` ／ `bash scripts/check-doc-refs.sh` ／ `bash scripts/check-progress-log-index.sh` ／ **`bash scripts/check-enum-sync.sh`**（**★★本サブは列挙定数を触る。ここが効く**） ／ `bash scripts/check-browser-storage-keys.sh`
- **★★検査表は 1 行ずつ実際の出力から埋めること**（`M24-09d` の教訓 2＝**回していない検査の結果を報告へ書くことが 2 サブ連続で起きた**）。**★回していないものは書かない。**

### 7.4 ドキュメント

- **`docs/progress/progress-log.md` への追記**（製造担当が行う。`CLAUDE.md` §8）。**指示書が明示的に求めていなくても追記する。**

#### 7.4.1 ★CHANGE 要否の判定（**as-built で判定する**）

**`CHANGE-147` は起票済みである。⇒ 製造は番号を消費しない。**

**★as-built が指示書と違ったら、違ったほうを完了報告へ書くこと**（playbook §4.43）。

**★★とくに次の 2 つは設計卓が断定できていない**——**(a) `recipe_cache` に語が焼かれているか ／ (b) キャラ名の英語表示のデータが在るか。**

### 7.5 完了報告

- **★★§3.3 の実査 14 件の答えを、母数付きで載せること。**
- **★★§4.1 で開発者に諮った語と、決まった／決まらなかったの別**（**★開発者判断は「いつ ／ どの問いに対して ／ 逐語で何と答えられたか」の 3 つを独立した節として残すこと**＝playbook §4.52）。
- **★英語ロケールで日本語のままにした例外の一覧**（§5.1-2）。
- **★落とした項目（§4.3 のフォールバック ／ §4.4 ／ §4.8 ／ `SM-069`）とその理由。**
- 完了報告に `git diff --numstat` を貼ること（教訓 `E-225`）。

---

## 8. 参照ドキュメント

| ID | パス | 使いどころ |
|---|---|---|
| DES-005 §5.4 | `docs/design/05-screen-design.md` | **★§4.6 の主対象** |
| DES-005 §5.9.2 | 同上 | **★`setplay.adoptNameFormat` が書式の正本であること** |
| DES-004 §2.2 | `docs/design/04-notation-spec.md` | **★内部表現と表示語の分離** |
| DES-002 §7.6 | `docs/design/02-architecture.md` | **★CSV 列名の契約** |
| m21-contract §5 | `docs/process/m21-contract.md` | **★契約 F-1** |
| CHANGE-147 | `docs/change-notes/CHANGE-147-notification.md` | 本サブの通知書 |
| followup §AI / §AK / §AS / §AT | `docs/handover/followup-backlog.md` | **★本サブの出所** |
| ledger | `docs/progress/M24-RESEARCH-01-report.md` §2 / §3 | **★SM / CO の逐語** |
| code-facts | `docs/handover/code-facts.md` | **★引く。想定で書かない** |

---

## 9. 注意事項・判断に迷ったら

### 9.1 推測で進めてはいけない事項

| # | 事項 | 対応 |
|---|---|---|
| **★★1** | **レシピ表記の語が `recipe_cache` に焼かれていると分かった** | **★★止めて設計卓へ報告する。★`recipe_cache` の再構築は契約 F-1 に触れ、本サブの射程を越える** |
| **★★2** | **語彙の置換が CSV の列名・ZIP エントリ名へ及びうる** | **★★止めて設計卓へ報告する**（`DES-002` §7.6 の契約） |
| **★★3** | **§4.1 の語が決まらない** | **★実装しない。決まらなかったと報告する。★勝手に決めない**（`CLAUDE.md` §9 のハード列） |
| **4** | **`internal/service/notation/` を触らないと解けない** | **★止めて設計卓へ報告する**（契約 F-1） |
| **5** | **キャラ名・技名の英語訳を作る必要が出た** | **★★作らない。落として報告する。★訳語はデータであって実装ではない** |
| **6** | **画面のレイアウトを変えないと解けない** | **★止めて設計卓へ報告する**（§1.6-7） |

### 9.2 推測で進めてよい事項（その旨を明示）

- locale キーの命名と階層
- i18n 化するときのコンポーネント内の書き方
- §5.1 のテストの置き場
- §4.3 のフォールバックの形（**★ただし報告すること**）

**⇒ いずれも「推測: 〜と仮定した」をコード内コメントまたは完了報告に明示すること**（`CLAUDE.md` §9）。

### 9.3 不明事項発見時の対応

`CLAUDE.md` §9 に従う。**原則は「止めて聞く」ではなく「進めて報告する」。** **★§9.1 の 6 件はハード列扱いである。**

### 9.4 ★CHANGE 要否

**`CHANGE-147` が起票済みである**（§7.4.1）。**★製造は番号を消費しない。**

---

## 10. 完了後の次ステップ

- 設計伝達レポート（`design_handover_report`）を生成する。
- **★次は `M24-08`（queryKey）である。★`SM-068` / `SM-074` を同サブへ移した**（§1.4）。
- その後 `M24-CLOSE`。

---

## 11. 開発者への確認事項

| # | 事項 | 設計卓の暫定案 | 止まるか |
|---|---|---|---|
| **★★1** | **★★§4.1 の語彙**（`SM-016` / `SM-008` / `SM-069` / `recipe-view-toggle-vocabulary` / `SM-110` の 20 語 / 空表示の文言） | **★暫定案は無い。★製造が候補を出し、開発者が選ぶ** | **★★止まる**（**語彙はハード列**。**★ただし候補出しまでは進めてよい**） |
| **2** | **★`setplay.adoptNameFormat` を触るか** | **★暫定案＝触らない**（§4.7） | **★止まらない**（暫定案で進める） |
| **3** | **★`SM-110` の「インポート」と「取込」を 1 本にするか** | **★暫定案＝1 本にする。★どちらの語を採るかは開発者が決める** | **★★止まる**（1 と同じ手番で諮る） |

---

*以上、指示書 M24-07 **v1.1.0**。* **★★本サブの最大の危険は「語彙を変えると 2 つの契約に触れる」ことである**——**契約 F-1**〔`recipe_cache` / `internal/service/notation/`〕**と `DES-002` §7.6**〔CSV 列名・ZIP エントリ名〕。**⇒ §3.3-1 / -2 の実査が本サブの分岐点である。** **★★次に大きい危険は「英語ロケール整備」が 487 キーの訳文すべてへ広がることである。⇒ 射程は「新語彙に関わるキー」と「日本語が漏れている箇所」に絞った。** **★★語彙は開発者が決める。製造は候補を出すところまでである。** **★`SM-068` / `SM-074` は本サブから外し `M24-08` へ移した**（§1.4）。
