# 指示書 M31-05: 各キャラの設置系 `custom_states` を足す（`P4M-023`）

| 項目 | 内容 |
|------|------|
| 文書ID | M31-05 |
| バージョン | **1.3.0**（2026-09-10・**★★マイグレ番号 `000110` を払い出した＝`D-808`。⇒ 投入時点で番号を持っていなかった**） ／ 1.2.0（2026-09-10・**★★★開発者が案 (vi) を暫定採択した＝`D-807`。⇒ 段 1 で止めて返す運用をやめ、実測が案 (vi) を否定したときだけ止める形にした。★これで本サブは投入できる**） ／ 1.1.0（2026-09-10・**★★開発者が変種の実態を示したため、段 1 の案を 2 本から 4 本へ広げた**＝`D-806`。**★あわせて実査を 1 件足した**〔同時に複数置けるか〕**。★設計卓の推す案が (ii) から (vi) へ変わっている**） |
| 作成日 | 2026-09-09 |
| 作成者 | 設計担当 Claude（設計卓／フェーズ4 期） |
| 対象マイルストーン | **`M31`**（`α②`）。上位＝`docs/instructions/M31-overview.md` v1.6.0 |
| 推奨モデル | **高精度実装 ＋ Plan Mode 必須**（**★★9 キャラぶんの `custom_states` を入れる。⇒ 1 件でも取り違えると利用者の入力が狂う**。実モデル名は `docs/human-notes/model-allocation.md`） |
| Plan Mode | **必須**（`CLAUDE.md` §9＝**データ投入を伴う**） |
| レビュー | **必要**（チェックリストは着手承認の手番で設計卓が起こす＝`D-736`） |
| CHANGE 消費 | **0〜1 本**（**★段 1 で案 (vi) を採るなら `DES-003` §3.2 へ `options` を足す CHANGE が 1 本要る**。★案 (ii) / (v) なら 0 本。**★起票は設計卓。自採番しない**＝`D-293`） |
| マイグレ消費 | **★★★`000110` を払い出した**（2026-09-10・`D-808`）**。⇒ 層 B であり `000110_data_*.sql` である**（`CHANGE-158`）**。★1 本に固定。⇒ 足りないと分かったら自採番せず再請求すること**（`D-293`） |
| 前提サブ | **無し**（**★ただし `M33` より前に入れること**＝§0.4） |
| 射程 identity | **1 件**——`P4M-023`（各キャラ `custom_state` 拡張） |
| 配置 | `docs/instructions/M31-05-custom-states-placement-expansion.md` |
| 出力 | `docs/progress/M31-05-completion-report.md` |

---

## 0. この指示書の特殊性

### 0.1 ★★対象は開発者が確定済みである。⇒ 9 体が新規

**★開発者が 2026-09-08 に対象を確定させ、`M31-01` の製造が実データと突合済みである。**

| キャラ | 開発者の記述 | 実データの `move_code` |
|---|---|---|
| キンバリー | 細工手裏剣設置数 | `shuriken_bomb_light` / `_medium` / `_heavy`（＋`shuriken_bomb_spread_*` 3 件＝乱れ細工手裏剣） |
| ジュリ | 歳破衝／OD歳破衝 | `saihasho` / `saihasho_od`（**★対象は `fuha_saihasho`＝§0.2-1**） |
| ブランカ | ブランカちゃん人形 | `blanka_chan_bomb`（＋`_activated` / `_activated_od`） |
| ダルシム | ヨガアーチ | `yoga_arch_light` / `_medium` / `_heavy` / `_od` |
| ダルシム | ヨガサンバースト（ホールド二段階） | `sa2_yoga_sunburst` / `_holding` / `_max_holding`（**★二段階は既に別 `move_code` である**） |
| **JP** | ヴィーハト／ODヴィーハト | **`departure_light` / `_medium` / `_heavy` / `_od`** |
| JP | ラヴーシュカ発動中 | `sa2_lovushka`（**SA2**） |
| ラシード | イウサール発動中 | `sa2_ysaar`（**SA2**） |
| 舞 | 花蝶扇バウンド中（ホールド版・乱れ） | `kachousen_holding_light` / `_medium` / `_heavy` / `_od` ／ `midare_kachousen`（**★＋`flame_*` の焔版 5 件も含む＝§0.2-2**） |
| イングリッド | サンオーダー（各レベル＋弱中強） | `sa2_order_of_the_sun_lv1` / `_lv2` / `_lv3`（**SA2**） |
| ヤスミン | パンギル・サ・リクラン（弱中強）／OD（弱中強） | `pangil_sa_likuran_light` / `_medium` / `_heavy` / `_od`（**★OD は実データで 1 行＝§0.2-3**） |

**★★`aki` は対象外である**（開発者の逐語＝「いらない事がわかった」）**。⇒ 10 体のうち 9 体が新規である。**

### 0.2 ★★開発者が決着させた 3 件（**確認から始めなくてよい**）

| # | 論点 | 開発者の逐語（2026-09-08） |
|---|---|---|
| **★★1** | ジュリに `fuha_saihasho`（[風破]歳破衝）が第 3 の技として実在する | **「[風破]歳破衝が今回のものを表します。風破が付いていないものは設置等できないので、この表現で使い手には伝わります。[風破]を付けるのは今回に限っては冗長です。表記ゆれ的になることも move と custom state は別物という事で大丈夫です。」** ⇒ **対象は `fuha_saihasho`。★state 名に「[風破]」は付けない** |
| **★2** | 舞に `flame_*` の焔版が同型で 5 件実在する | **「その通りです。焔版も扱います。」** |
| **★★3** | ヤスミンの OD は実データで `pangil_sa_likuran_od` の 1 行しかない | **「moves 上は省略していますが、custom state では弱中強を扱いたいです。」** ⇒ **`moves` を増やすのではなく、state 側で弱中強を持つ** |

### 0.3 ★★既存の `custom_states` と名前が似ている 2 件は、別物である

**★★製造が 1 度外した。⇒ 名前の一致だけで「既存が該当する」と読んだ。**

> **キンバリーとブランカに関しては持っていません。この二体の既存とはコンボを始めた時に残っているストック数、つまりフィールドに設置されたかどうかではなく、残弾です。なので別物として扱ってください。**

| キャラ | 既存 | 意味 | 今回足すもの |
|---|---|---|---|
| **キンバリー** | `shuriken_bomb_stock`(stock) | **コンボ開始時に残っている残弾**（手持ち） | **★フィールドへ設置した数**（別の state） |
| **ブランカ** | `blanka_chan_bomb`(stock・0〜3) | **同上＝残弾** | **★設置したかどうか**（別の state） |

**★★「既存で足りる」は 1 件も無い。⇒ 実測できるのは名前と型までであり、意味は開発者にしか確かめられない**（`CHANGE-169` §2.5）**。**

### 0.4 ★★`M33` より前に入れること

**★マイグレを消費する。⇒ `M33`**（マイグレーションの統合）**の後に入れると、統合済みのベースラインを触ることになる。**

**★★`M31-04`**（確定反撃の D リバ）**／ `M35-02`**（`ryu` の `axe_kick`）**も seed / マイグレを触る。⇒ 同じ手番で走るなら番号をまとめて請求すること。**

---

## 1. 前提として読むもの

| # | 何を | どこ |
|---|---|---|
| **★★1** | **選定基準と一般規則** | **`docs/change-notes/CHANGE-169-notification.md` §2**（**★★「始動技よりも先にやっておくムーブかどうか」／ `custom_state` と `move` は別物 ／ 名前が一致しても意味は一致しない**） |
| **★★2** | **`custom_states` の構造** | `DES-003` §3.2（**★`subject` / `type` / 値の定義**） |
| **★★3** | **フロントが扱える型** | `web/src/features/combo/customStates.ts` の `isSupportedState`（**★★`flag` / `level` / `stock` の 3 種。`composite` は明示的にスキップされる**） |
| **★4** | **`composite` の撤回の経緯** | `migrations/000009`（jamie の `drunk_level`＝`type='composite'`）／ `migrations/000017`（**その削除**） |
| **★5** | **マイグレの規約** | `SUPP-001` §5.5.4 ／ `CHANGE-158`（**層 B の命名**） |
| **★6** | **`M30-01` のキャラ固有状態タブ** | `docs/change-notes/CHANGE-170-notification.md` §2.5（**★状態 code ＋ 語幹で `move_code` を照合する**） |

---

## 2. やること

### 2.1 ★★段 1 — 「どの変種で設置したか」をどう表すかを実測で確かめる（**★★★案 (vi) が暫定採択されている。⇒ 実測が否定したときだけ止めて返す**）

**★★★【2026-09-10 運用が変わった＝`D-807`】開発者の逐語＝「B-2 は新しい推奨案を暫定とし、実測で最終確認」。**

**⇒ 段 1 で必ず止めて返す運用ではなくなった。★案 (vi) を暫定の正として段 2 へ進んでよい。**

| どうなったら | どうする |
|---|---|
| **実測が案 (vi) を支持する** | **★★止めずに段 2 へ進む。⇒ 実数と実測を報告に書くこと** |
| **★★★実査 5**（同時に複数置ける）**が「居る」だった** | **★そのキャラだけ案 (ii) を使い、止めずに進む。⇒ 理由を報告に書くこと** |
| **★★★案 (vi) が成立しないと分かった** | **★★止めて設計卓へ返す。⇒ 「なぜ成立しないか」を実測で示すこと** |
| **★`options` を足すと `sun_crest` が変わると分かった** | **★★止めて返す。⇒ 案 (vi) の前提**（`options` を持たない `level` は変わらない）**が崩れる** |


**★★これが本サブ最大の判断である。⇒ コードを書く前に済ませること。**

**★問題**——**強度・ホールド・レベルは `move_code` が既に区別している**（`yoga_arch_light/medium/heavy` ／ `sa2_yoga_sunburst_holding` / `_max_holding` ／ `sa2_order_of_the_sun_lv1/2/3` ／ `pangil_sa_likuran_*`）**。⇒ 足りないのは「どの変種で設置したか」を `custom_states` が持てないことである。**

**★現行の `type` は `flag` / `level` / `stock` の 3 種であり、「弱で設置した／強で設置した」を表す型が無い。**

**★★★【2026-09-10・`D-806`】開発者が変種の実態を示した。⇒ 逐語＝「弱中強, OD, ODの弱中強。ホールド、最大ホールド、強化状態などで変わる。」「ノーマルとODの区別だけでいい場合は別々におけばいいが、強度の区別が大量になる場合はそうはしたくない」。**

**★これで案 (ii) の代償が測れるようになった。⇒ 1 ファミリーあたり最大 7 〜 8 変種であり、9 体ぶんでは state が 50 前後になる。★開発者の懸念は正当である。**

| 案 | 内容 | 設計卓の見立て |
|---|---|---|
| **(i)** | **新しい `type`**（列挙／enum）**を足す** | **★★推さない。⇒ `composite` が過去に一度採られて撤回されている**（`000009` → `000017`）**。★フロントの `isSupportedState` が明示的にスキップする形になった前例であり、同じ道になりうる** |
| **(ii)** | **変種ごとに別の `code` を持つ**（例 `yoga_arch_set_light` / `_medium` / `_heavy`） | **★★変種が 2 つ以下のキャラにだけ推す。⇒ 既存の `flag` で表せて CHANGE も要らないが、9 体ぶんで state が 50 前後になる。★★★ただし「同時に複数置ける」キャラは、この案でしか表せない**（下の実査 5 を見ること） |
| **(iii)** | **`type: composite` を使い、1 つの `code` で変種を値として持つ** | **★★★採らない**（2026-09-10 に設計卓が提案し、同じ手番で撤回した＝`D-806` / 計測点 `M-152`）**。⇒ `DES-003` §3.2 には残っているが、as-built では `000017` で削除され `isSupportedState` がスキップする。★案 (i) と同じ道である** |
| **(v)** | **`type: level` のまま、数値の意味を state 名で伝える**（例 `ヨガアーチ設置（1弱 2中 3強 4OD）`） | **★推さない。⇒ CHANGE も描画の変更も 0 本で済むが、利用者が数字と変種の対応を覚えることになる** |
| **★★★(vi)** | **`type: level` のまま、`value_definition` にラベル付きの選択肢を持たせる**（下記） | **★★★設計卓はこちらを推す。⇒ 1 キャラ 1 state で足り、画面には「弱 / 中 / 強 / OD」とラベルが出る。★`isSupportedState` は `level` を既に通すので、型の追加も撤回済み型の復活も要らない** |

**★★案 (vi) の形**（**★これは形の例示であり、実データの正本は seed である**）:

```json
{
  "code": "yoga_arch_set",
  "name_ja": "ヨガアーチ設置",
  "subject": "self",
  "scope": "persistent",
  "type": "level",
  "value_definition": {
    "kind": "integer",
    "min": 0,
    "max": 4,
    "options": [
      { "value": 0, "label_ja": "なし" },
      { "value": 1, "label_ja": "弱" },
      { "value": 2, "label_ja": "中" },
      { "value": 3, "label_ja": "強" },
      { "value": 4, "label_ja": "OD" }
    ]
  }
}
```

**★★案 (vi) の代償は 2 つだけである＝(1) `DES-003` §3.2 へ `options` を足す CHANGE が 1 本 ／ (2) 描画に「選択肢が在れば選択、無ければ数値入力」の分岐が 1 つ。⇒ `isSupportedState` は触らない。**

**★★★`options` を持たない `level` は今までどおり数値入力である。⇒ `sun_crest`**（唯一の as-built `level`）**は 1 行も変わらない。★これが `composite` との決定的な差である。⇒ 撤回済みの型を復活させるのではなく、通っている型に任意フィールドを 1 つ足すだけである。**

**★★採否は開発者の判断である**（`roles-and-routing` ハード列＝ユーザー体験に影響する）**。⇒ 段 1 の実数と実査を添えて聞くこと。**

**★段 1 の成果物**:

| # | 何を |
|---|---|
| **★★1** | **案 (ii) / (vi) それぞれで、9 体ぶんの state が何個になるかの実数** |
| **★★2** | **案 (ii) を採ったとき、`M30-01` のキャラ固有状態タブ**（状態 code ＋ 語幹で `move_code` を照合）**が正しく拾えるかの実測。⇒ `yoga_arch_set_light` の語幹が `yoga_arch_set` になると、`yoga_arch_light` に当たらない可能性がある** |
| **★★3** | **案 (vi) を採ったとき、`isSupportedState` と描画に何を足すことになるかの見積もり。⇒ `level` は既に通るので、足すのは「選択肢が在れば選択を出す」分岐だけのはずである。★実物で確かめること** |
| **★4** | **どちらでも変わらない部分**（`subject` はすべて `self` か、など） |
| **★★★5** | **【2026-09-10 追加＝`D-806`】同じファミリーの変種を、同時に 2 つ以上設置できるキャラが居るか。⇒ 居るなら、そのキャラは 1 つの値では表せない。★案 (ii) の `flag` が要る** |
| **★★6** | **【2026-09-10 追加＝`D-806`】`start_min` / `end`**（int state の①始動時／②終了時＝`DES-003` §3.2）**が、変種の値にどう効くか。⇒ 「始動時は弱が置いてある、終了時は強が置いてある」が表現として要るかを確かめること** |

**★★段 1-2 が本サブで最も見落としやすい。⇒ 案 (ii) は「タブに出なくなる」副作用を持ちうる。**

**★★★実査 5 の候補はキンバリーの細工手裏剣である**（`shuriken_bomb_light` / `_medium` / `_heavy` ＋ 乱れ 3 件）**。⇒ 設置数の state が既に `stock` で在ることからも、複数同時が在りうる。★ここが「1 キャラ 1 state」で足りるかどうかの分かれ目である。**

**★★★案 (vi) はキャラ固有状態タブの照合にも効く**（`CHANGE-170` §2.5）**。⇒ 1 つの state の語幹 `yoga_arch_set` が `yoga_arch_light` / `_medium` / `_heavy` / `_od` すべてに当たれば、1 つの state でタブに全部載る。★案 (ii) だと語幹が変種ごとに分かれる。⇒ 実査 2 と同じ測定で両方見えるので、まとめて測ること。**

### 2.2 段 2 — `custom_states` を入れる（**★開発者の回答を得てから**）

| # | やること |
|---|---|
| **★★1** | **9 体ぶんの `custom_states` を `characters.custom_states` へ足す。⇒ 既存の state は 1 つも消さない**（キンバリー・ブランカの残弾は別物として残す） |
| **★★2** | **`subject` は「その状態が誰に付いているか」で決める**（`CHANGE-154` の是正）**。⇒ 設置系はすべて `self` の見込みだが、実測で確かめること** |
| **★★3** | **state 名に技名の装飾を持ち込まない**（`CHANGE-169` §2.4。**ジュリの「[風破]」を付けない**） |
| **★★4** | **ヤスミンは `moves` を増やさず、state 側で弱中強を持つ**（§0.2-3） |
| **★5** | **舞は焔版**（`flame_*` の 5 件）**を含める**（§0.2-2） |
| **★★6** | **マイグレは層 B である。⇒ ファイル名に `_data_` を必ず入れる**（`NNNNNN_data_*.sql`）**。★`down` も書く** |

### 2.3 段 3 — 波及を確かめる

| # | 何を |
|---|---|
| **★★1** | **`M30-01` のキャラ固有状態タブが、足した state を拾うこと。⇒ 段 1-2 の実測どおりになるか** |
| **★★2** | **9 体それぞれでタブが空でないこと。⇒ 陽性対照＝足す前は空**（または少ない）**であることを実測してから足す** |
| **★3** | **エディタの `custom_state` トグル**（`combo-editor-custom-state-{code}`）**が出ること** |
| **★4** | **既存の state が 1 つも消えていないこと** |

---

## 3. やらないこと

| # | 何を |
|---|---|
| **★★1** | **`aki` に足すこと。⇒ 開発者が「いらない事がわかった」と述べた** |
| **★★2** | **既存の `shuriken_bomb_stock` / `blanka_chan_bomb` を書き換えること。⇒ 残弾であり、今回のものとは別物である** |
| **★★3** | **`moves` を増やすこと。⇒ ヤスミンの OD 強度別も `moves` へは足さない**（§0.2-3） |
| **★4** | **段 2 を開発者の回答なしで実施すること**（§2.1） |
| **★5** | **`docs/design/` を編集すること**（`CLAUDE.md` §8） |
| **★★6** | **マイグレ番号を自採番すること**（`D-293`） |
| **★7** | **`M30-01` の `moveSurfacing.ts`**（キャラ固有状態タブの判定）**を書き換えること。⇒ データ側で解けるはずである。★解けないなら、それ自体が報告に値する** |

---

## 4. ★★危険（**先に読むこと**）

### 4.1 ★★最大の危険＝名前の一致で「既存が該当する」と読むこと

**★★製造が 1 度これで外した。⇒ `shuriken_bomb_stock` は「細工手裏剣のストック」であり、開発者の「細工手裏剣設置数」と語がほぼ重なるが、指すものが違った**（手持ち ⇄ フィールド上）**。**

**★★実測できるのは名前と型までであり、意味は開発者にしか確かめられない**（`CHANGE-169` §2.5）**。⇒ 迷ったら聞くこと。**

### 4.2 ★★2 番目＝案 (ii) がタブの照合を壊すこと

**★`M30-01` のキャラ固有状態タブは「状態 code ＋ その語幹」で `move_code` を照合する**（後置語 `_stock` / `_mode` / `_is_set` / `_crest` / `_charge` を落とした形）**。**

**★★`yoga_arch_set_light` のような code を作ると、語幹が `yoga_arch_set` になり `yoga_arch_light` に当たらないことがありうる。⇒ 段 1-2 で必ず実測すること。**

**★★これは「足したのにタブに出ない」という形で現れる。⇒ 検査は緑のままである。**

### 4.3 ★3 番目＝ライセンスの層

**★キャラ固有状態は SF6 の事実である。⇒ 層 B である。★`_data_` を入れ忘れると層 A へ静かに落ちる**（`M28-05` の先例）**。**

### 4.4 ★4 番目＝`subject` を技の分類で決めること

**★`CHANGE-154` が是正した型である。⇒ `subject` は「設置技かどうか」では決まらない。「その状態が誰に付いているか」で決める。**

**★★同じ節が同型の是正を 2 度受けている**（`CHANGE-154` と `CHANGE-169`）**。⇒ 3 度目を作らないこと。**

---

## 5. テスト

| # | 何を | 判定 |
|---|---|---|
| **★★1** | **9 体それぞれのキャラ固有状態タブが空でないこと** | **★★陽性対照とセットで**（§2.3-2） |
| **★★2** | **既存の state が 1 つも消えていないこと** | **★キンバリー・ブランカの残弾を名指しで** |
| **★3** | **`subject` がすべて実測どおりであること** | **★段 2-2 の結果** |
| **★★4** | **マイグレ契約テスト**（`SUPP-001` §5.5.2 / §5.5.4） | **★★件数で固定しないこと**（§5.5.4 (6)） |
| **★★5** | **`bash scripts/check-migration-license.sh`** | **★★層 B として判定されること** |
| 6 | **`go test ./...`** ／ **`cd web && pnpm test`** | **★出力で判定する** |
| **★7** | **`make e2e-only P=<パターン>`**（**★`playwright` を直接叩かない**＝`CLAUDE.md` §11） | **★入力面を含む spec を回す** |
| **★★8** | **常設検査**（1 本目は `check-artifact-integrity.sh`） | **★★最後に回すこと**（`M30-01` の教訓 3） |

---

## 6. 完了条件

| # | 条件 |
|---|---|
| **★★1** | **段 1 の実数と実測が示されている**（**★少なくとも (ii) と (vi) の実数を出すこと**）**。★★案 (vi) が成立しなかった場合のみ、止めて返したことが報告に在る** |
| **★★2** | **段 1-2 の実測**（タブの照合が壊れないか）**が示されている** |
| **★★3** | **9 体ぶんの state が入っている。★`aki` には入れていない** |
| **★★4** | **既存の state が 1 つも消えていない** |
| **★★5** | **マイグレのファイル名に `_data_` が入っており、`check-migration-license.sh` が層 B と判定する** |
| **★6** | **9 体それぞれでタブが空でないことが、陽性対照とセットで示されている** |
| 7 | **§5 の検査がすべて緑**（**★出力で判定。★最後に回す**） |
| **★8** | **`docs/progress/M31-05-completion-report.md` を書き、`docs/progress/progress-log.md` へ索引行を 1 行足す**（`CLAUDE.md` §8） |
| **★★9** | **完了報告を `bash scripts/check-md-emphasis.sh <file>` に通す**（`D-775`） |
| **★★10** | **設計伝達レポートを出す**（`/design_handover_report`）**。★段 1 で案 (i) を採ったなら `DES-003` §3.2 への CHANGE の請求を書くこと** |

---

## 7. ★開発者への確認事項

| # | 事項 | 段 |
|---|---|---|
| **★★1** | **★★★暫定採択済み**（2026-09-10・`D-807`）**＝案 (vi)。⇒ 実測が否定したときだけ返すこと。★以下は経緯である。** **「どの変種で設置したか」をどう表すか。⇒ 設計卓の案＝(vi)**〔`type: level` のまま `value_definition` にラベル付きの選択肢を持たせる〕**。★対抗は (ii)**〔変種ごとに別 `code`〕**。★★案 (iii)**（`composite`）**は 2026-09-10 に撤回済みであり、再提案しないこと**（`D-806`） | **段 1** |

**★★段 1 の実数と、タブの照合の実測と、実査 5**（同時に複数置けるか）**を添えて聞くこと。⇒ 数と実態を見ないと選べない。**

**★★★案 (vi) を推す理由を 1 行で＝開発者が示した変種の実態**〔弱中強 ／ OD ／ OD の弱中強 ／ ホールド ／ 最大ホールド ／ 強化状態〕**は、案 (ii) では 9 体ぶんで state が 50 前後になる。⇒ 画面に並ぶ数が実用の限度を超える。**

**★★§0.2 の 3 件は既に決着している。⇒ 二度聞かないこと。**

---

## 8. 参照ドキュメント

| ID | パス | 参照する箇所 |
|----|------|------------|
| — | `docs/change-notes/CHANGE-169-notification.md` | **★★§2**（選定基準 ／ `custom_state` と `move` は別物 ／ 名前が一致しても意味は一致しない） |
| — | `docs/change-notes/CHANGE-170-notification.md` | **§2.5**（**キャラ固有状態タブの判定軸。★§4.2 の危険**） |
| DES-003 | `docs/design/03-data-model.md` | **§3.2**（`custom_states` の構造） |
| SUPP-001 | `docs/design/supp-001-detailed-design.md` | **§5.5.2 / §5.5.4**（マイグレ契約テスト） |
| — | `docs/change-notes/CHANGE-158-notification.md` | **層 B の命名**（`NNNNNN_data_*.sql`） |
| — | `docs/process/parallel-board.md` | **`D-781`**（開発者の対象確定）／ **`D-788`**（本サブの起票）／ **`CHANGE-154`**（`subject` の是正） |

---

*以上、指示書 M31-05 **v1.3.0**。* **★★本サブの落とし穴は「9 体ぶん入れるだけ」と読むことである。** **★段 1 の判断**（`type` を増やすか、`code` を分けるか）**が決まらないと、入れる形そのものが決まらない。** **★もう 1 つは §4.2 である**——**案 (ii) は「足したのにタブに出ない」を起こしうる。⇒ 検査は緑のままである。**
