# 指示書 M{{N}}-RESEARCH-{{NN}}: {{調査テーマ}}

<!--
  ▼ このファイルの使い方(設計担当 Claude 向け)
  - 本テンプレは read-only 調査指示書の骨格。`{{…}}` を埋め、`<!-- 記入指針 -->` / `<!-- 例 -->` は完成時に削除する。
  - 調査指示書は「実装物なし=機械レビュー不要・judgement-free(判断を含めない・事実列挙のみ)」が核。§5 報告様式を指示書内に埋め込む。
  - 2 系統ある: (簡易型)=§0 なし / (厳格型)=冒頭に「§0 この指示書の特殊性」を置き read-only・judgement-free を強調。下の §0 は厳格運用時のみ残し、簡易運用なら §0 ごと削除する。
  - 禁則表現排除(Playbook §4.1)・行レベル参照(§5)。成果物=調査レポートのみ(コード・DES・seed を一切変更しない)。
-->

| 項目 | 内容 |
|------|------|
| 文書ID | M{{N}}-RESEARCH-{{NN}} |
| バージョン | 1.0.0 |
| 種別 | 調査指示書（read-only。実装・マイグレ作成/編集・コード変更・seed 変更・ファイル移動を一切行わない） |
| 対象 | 製造担当 Claude Code |
| モデル | {{高速実装}}（read-only 調査。grep / view 中心の事実列挙。実モデル名は `docs/human-notes/model-allocation.md` が正本。実使用は開発者判断） |
| レビュー | 不要（read-only。実装物が無いため機械レビュー対象なし） |
| 作成者・作成日 | 設計担当 Claude（{{担当・期}}）/ {{YYYY-MM-DD}} |
| 前提 | {{phase{X}-overview §M{N} / 背景・調査を要する理由}} |
| 主参照 | {{code-facts §… / DES-00X §… / REQ-001 FR… / followup-backlog §… / retrospective-digest §…}} |

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| 1.0.0 | {{YYYY-MM-DD}} | 初版。{{一行要旨}} |

---

<!-- ▼▼▼ 厳格運用時のみ: §0「この指示書の特殊性」。簡易運用なら §0 全体を削除して「## 1. 背景と目的」から始める ▼▼▼ -->

## 0. この指示書の特殊性

### 0.1 製造担当として起動 + 調査だけ実施して閉じる運用
<!-- 記入指針: 製造担当セッションを起動し §4 の事実列挙 → §5 レポート → クローズ。実装は本調査対象外(後続サブで別指示書) -->

### 0.2 read-only 厳守
<!-- 記入指針: view/grep/git log/find/ls/SQL SELECT のみ。編集系・書き込み系 bash は禁止。
     ▼▼▼ ★書き込みの例外は 2 つ。**両方を必ず書くこと**（片方だけだと DoD と衝突する） ▼▼▼
       (1) §5 の調査レポート（`docs/progress/` 配下）
       (2) **`docs/progress/progress-log.md` への索引行の追記**（`CLAUDE.md` §8 の常設要求）

     ★(2) を書き落とすと、DoD と禁止表が同じ指示書の中で正面衝突する。
       DoD 側は `CLAUDE.md` §8 により「progress-log へ索引行を追記した」を常設で要求する一方、
       禁止表が「`docs/` 配下の他ファイルを変更しない」とだけ書いていると、**片方を満たすと
       もう片方に違反する**。**実行者に「read-only 逸脱かどうか」を独断させることになる。**

     ★これは実際に 2 本連続で起きた（ボード M-40 = M20-RESEARCH-01 ／ M-49 = M21-RESEARCH-01）。
       2 本とも実行者が独断で追記側に倒し、2 本とも報告で衝突を指摘してきた。
       **個別の指示書の誤記ではなくテンプレート由来であるため、ここで解く**（2026-08-12）。 -->

### 0.3 判断・提案を含めない（judgement-free）
<!-- 記入指針: 事実列挙のみ。「ここを直すべき」等の修正方針・設計結論は含めない。ただし(1)想定外の発見・(2)明らかな矛盾の事実指摘 は判断でないため記録 -->

### 0.4 本調査の主目的
<!-- 記入指針: 軸ごとの主目的を表で。調査対象外との混同回避も明記 -->

<!-- ▼▼▼ 任意ブロック: 探索モード（PoC 可）。**試作を作る回だけ** §0.2 を下記で置き換える。
       純粋な調査（read-only のみ）なら本ブロックは節ごと削除する ▼▼▼ -->

### 0.2' read-only の範囲（PoC 実行を含むよう明確化）
<!-- 記入指針: ★禁止表に「`docs/` 配下の他ファイルを変更する」と書く場合、
     **許可側へ「`docs/progress/progress-log.md` への索引行の追記」を必ず並べること**（§0.2 の注記と同じ理由）。
     許可側が「`docs/progress/` へ §2.1 のレポートを作る」だけだと、索引行の追記が禁止側に入ってしまう。 -->
<!-- 記入指針（2026-08-11 監査タスク B・提案 B-2）:
  **本プロジェクトの RESEARCH は既定が「read-only の調査モード」であり、試作を作る「探索モード」は既定に無い。**
  ところが実務で必要になった実績がある（`M19-RESEARCH-01`＝別リポエンジンの実体確定と出力品質 PoC）。
  そのとき設計担当は §0.2 をその場で書き換えて許可/禁止の線を引いた。**本ブロックはその再利用版**である
  （1 件だけ手書きで存在し、後続 RESEARCH 3 本は純 read-only に戻ったため再発明が起きる状態だった）。

  **許可/禁止を 2 列で厳密に分けること。** 範として `M19-RESEARCH-01` §0.2 の 3 点を必ず含める:
  (1) **一時的な PoC ハーネスはリポ外の使い捨て領域に限る**（`/tmp` 配下等。入力を渡し出力を集めるだけの薄いドライバ）
  (2) **DB はコピーを作って読む**。原本を開いたまま書き込む操作はしない
  (3) **本体リポのソース／マイグレ／seed／テスト／DES・REQ 本体は 1 バイトも変更しない**

  **★あわせて「移行条件」を必ず書く**（Stage 3 §4.3）——**PoC の結果をそのまま正式実装の受入条件にしない。**
  結果をレビューし、仕様駆動の指示書を別途起こす。`M19-RESEARCH-01` §0.1 は
  「本実装（エンジンの本体統合・提案 UI・保存導線）は本調査の対象外で、PoC 品質確認後に別指示書で扱う」と書いている。

  **★合格線は置かない。** PoC は評価的“事実”（hit/miss・実行時間・再現の有無）を数値と実出力で示すところまで。
  「品質が十分か」の判定は開発者が数値を見て置く（起動キット §2「先に測る→後で置く」）。
-->

**許可**:
- {{read-only の通常範囲（view／grep／git log／find／ls／SQL SELECT）}}
- {{試作の実行範囲（例: 別リポのエンジンを `go run` する）}}
- {{一時的な PoC ハーネスの作成——**リポ外の使い捨て領域に限る**}}

**禁止**:
- {{本体リポのソース／マイグレ／seed／テスト／DES・REQ 本体の変更（1 バイトも変更しない）}}
- {{本体 DB 原本への書き込み（INSERT/UPDATE/DELETE/マイグレ適用）}}
- {{採番（CHANGE・マイグレ連番）——必要が生じたら請求する。自採番しない}}

**移行条件**: {{PoC の結果はそのまま受入条件にしない。レビュー後、仕様駆動の指示書を別途起こす}}

<!-- ▲▲▲ 探索モードの任意ブロックここまで ▲▲▲ -->

<!-- ▲▲▲ 厳格運用ブロックここまで ▲▲▲ -->

---

## 1. 背景と目的

### 1.1 背景
<!-- 記入指針: なぜ着手前に read-only 調査が要るか。後続修正サブ(M{N}-xx)のスコープ確定入力になる旨 -->

### 1.2 目的
<!-- 記入指針: §4 の項目を実 SQL/実コード/実テストの view・grep で確認し §5 様式で報告する、と明記 -->

### 1.3 この調査でやらないこと（read-only 厳守）
<!-- 記入指針: 実装・マイグレ・seed/コード変更を一切しない。最終決定をしない(材料提示のみ)。DES/REQ 本体を変更しない -->

---

## 2. 成果物

### 2.1 作成するファイル
- `docs/progress/M{{N}}-RESEARCH-{{NN}}-report.md`（調査報告。§5 の様式）。
<!-- 記入指針: レポート配置は docs-map の RESEARCH レポート規約に従う -->

### 2.2 変更しないもの
<!-- 記入指針: 全ソース・全マイグレ・全 seed・DES/REQ 本体・テスト(read-only) -->

---

## 3. 前提条件

### 3.1 必読ドキュメント
<!-- 記入指針: code-facts の該当節・DES 行レベル参照・関連 spec を列挙 -->

### 3.2 前提事実（設計担当が code-facts / DES から確認済み = 調査の出発点）
<!-- 記入指針: 調査担当がここを起点に実査する既知事実。断定でなく「実コードで裏取り」を促す。厳格型では §0.5 として置く流儀もある -->

---

## 4. 調査項目
<!-- 記入指針: A. B. C.… の英字ラベルで列挙。各項目「実 SQL/実コードを view し grep で全数を取る」「列名・関数名・件数は実値で報告」「確認不能は未確認と明記」を徹底 -->

### A. {{調査軸A}}
- **A-1**: {{確認項目}}

### B. {{調査軸B}}
- **B-1**: {{確認項目}}

---

## 5. 報告様式（`M{{N}}-RESEARCH-{{NN}}-report.md`）
<!-- 記入指針: レポートの構造(見出しツリー)を指示書内に埋め込む。各項目で「実態/契約との差/後続スコープへの含意/推奨(決定はしない)」を書かせる。末尾に「要決定事項」を番号付きで集約 -->

各調査項目について、以下を記す:
1. **実態**（view / grep の結果。列名・関数名・件数は実値）。
2. **契約・正典との差**（{{DES 節との乖離}}）。
3. **後続スコープへの含意**（{{M{N}-xx 修正のスコープ確定材料}}）。
4. **推奨**（複数案は併記。決定はしない）。

末尾に「**{{後続サブ}} スコープ確定のための要決定事項**」を番号付きで集約する。

---

## 6. 完了条件（Definition of Done）
<!-- 記入指針: §4 全項目を実値で報告した/read-only を逸脱していない/要決定事項が Plan Mode 入力の粒度でそろっている 等 -->

---

## 7. 参照ドキュメント
<!-- 記入指針: code-facts・DES(版付き実パス)・REQ・phase-overview・digest 等 -->

---

## 開発者への確認事項
<!-- 記入指針: 解決済みは注記して記録保持。残る確認事項が無ければ「なし」と明記 -->

---

*以上、M{{N}}-RESEARCH-{{NN}} 調査指示書 v1.0.0。配置 `docs/instructions/M{{N}}-RESEARCH-{{NN}}-{{slug}}.md`。本調査の report が {{後続サブ}} のスコープ確定入力となる。*
