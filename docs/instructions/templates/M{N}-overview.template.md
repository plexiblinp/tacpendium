# M{{N}} マイルストーン全体像（M{{N}}-overview）: {{テーマ}}

<!--
  ▼ このファイルの使い方(設計担当 Claude 向け)
  - 本テンプレは M{N} 概要(各サブ指示書の上位文書)の骨格。`{{…}}` を埋め、`<!-- 記入指針 -->` / `<!-- 例 -->` は完成時に削除する。
  - overview は「補足資料(自由改訂)」= CHANGE 通知書対象外。DES 本体に触れる判断は各サブで CHANGE 起票。
  - §2「スコープ全件処遇一覧」が本 MS の肝。上位 phase-overview の FB/FR を全件、担当サブへ処遇づけする。後続送りは根拠付き(Playbook §4.11)。
  - サブ分割・連番・実装順は開発者承認を得てから確定(Playbook §4.12)。禁則表現排除(§4.1)・行レベル参照(§5)。
-->

| 項目 | 内容 |
|------|------|
| 文書種別 | 補足資料（overview。CHANGE 通知書対象外・自由改訂） |
| バージョン | {{0.1.0（ドラフト）→ 承認後 1.0.0}}<br>**★本セルは現行版と日付だけを持つ。変更の内容をここへ書き足さない（置き場は §改訂履歴）** |
| 作成日 | {{YYYY-MM-DD}} |
| 作成者 | 設計担当 Claude（{{担当・期}}） |
| 位置づけ | {{phase{X}-overview §M{N} の具体化。本書がサブ分割・全 FB 処遇・CHANGE 見込みの正本。各サブ指示書の上位文書}} |
| 前提 | {{phase{X}-overview §M{N} / followup-backlog §… / 関連 request / DES-00X §… / code-facts(着手時実査)}} |
| 実パス規約 | 文書ID ⇄ 実パスの正は `docs/handover/docs-map.md`。本書は実パス併記。 |
| 配置 | `docs/instructions/M{{N}}-overview.md` |

---

## 1. 目的・位置づけ
<!-- 記入指針: この MS の性格(破壊的マイグレの有無・並走可否)と達成目標を箇条書き。上位 MS/次 MS との依存(順序依存があれば明記)を書く -->

---

## 2. スコープ全件処遇一覧（本MSの肝）

### 2.1 {{FB/FR 群}} 処遇（{{出典・出現順}}）
<!-- 記入指針: 上位 overview の項目を全件、処遇(本 MS 先行/後続送り/対象外)と担当サブに割り付ける。番号より項目の中身が正、を明記 -->

| # | 要旨 | 処遇 | 担当サブ |
|---|------|------|---------|
| {{①}} | {{要旨}} | {{M{N} 先行 / M{X} 後 / 対象外}} | {{M{N}-{NN}}} |

### 2.2 {{追加のスコープ群があれば}}
<!-- 記入指針: コマンド解決・繰越タスク等、別枠の処遇表。無ければ節ごと削除 -->

### 2.x 段階導入・後続送り（根拠付き）
<!-- 記入指針: 「将来送り/スコープ外」は帰結を根拠付きで明記(Playbook §4.11)。表: 項目 | 処遇 | 根拠 -->

| 項目 | 処遇 | 根拠 |
|------|------|------|
| {{項目}} | {{M{X}/対象外}} | {{根拠}} |

---

## 3. サブユニット構成（フラット連番・{{分割は承認待ち/承認済み}}）

> {{サブ分割・連番・実装順は設計担当の提案。Playbook §4.12 により開発者承認を得てから確定(確認事項1)。}}

| サブ | 内容 | 主参照（実パス） | モデル | CHANGE 見込み | 依存 |
|------|------|------|------|------|------|
| **M{{N}}-{{01}}** | {{内容}} | {{DES-00X §… `docs/design/…` / code-facts §…}} | {{高速実装}} | {{なし / DES-00X 軽微}} | {{先頭 / 独立 / M{N}-xx 後}} |

> **採番**: {{モデル方針}}。CHANGE 採番は {{次番号}} から（{{欠番}}）。model-allocation へ各サブ着手時に追記。

---

## 4. 主要設計判断・調査結果の反映
<!-- 記入指針: 4.1, 4.2… で個別判断。RESEARCH レポートや friend FB の反映、着手前実査ゲートの根拠を記す -->

### 4.1 {{判断の見出し}}

---

## 5. 依存関係・進行順序
<!-- 記入指針: サブの実装順を番号付きで。順序依存(先行が後続の安全網 等)を根拠付きで。並走可否も -->

<!-- ★必須(並列サブがある場合): E-14 の交差表を置く(教訓 E-121 / ボード D-284・D-297)。
     ★2026-08-12 の改訂で本ブロックが一度失われた(改善レーンの d41add7 が §改訂履歴 を新設した際、
       同日に設計卓が入れた本ブロックを含まない版で置き換わったため)。再適用済み。
     並列サブは互いの E-14 交差相手である。同時に発行した指示書とチェックリストは、
     いずれも「他の並列サブが完了した後の as-built」を知らない。
     ⇒ 各サブの完了時に「他の並列サブの as-built が、自分の指示書・チェックリストの前提を
        崩していないか」を照合する項目を、各サブの完了条件に置く。
     ★「触るファイルが違うから大丈夫」で済ませない。面が交差しないことは編成時の見込みであり、
       追補や開発者要望で後から交差しうる(M19 で実際に起きた。実害が出なかったのは結果であって
       検出の仕組みが働いた結果ではない)。
     交差相手が無い場合は「本 MS に並列サブは無い」と明記する(節の欠落と、対象が無いことは違う)。 -->

<!-- 例:
| サブ | 並列相手 | 交差しうる面 | 完了時の照合 |
|------|---------|-------------|-------------|
| M{N}-01 | M{N}-02 | {{共有コンポーネント / 同一 API のレスポンス契約}} | 02 完了時に 01 の指示書 §4.x の前提を再照合 |
-->

---

## 6. 完了の観点（M{{N}} クローズ）
<!-- 記入指針: 各サブの完了で達成される状態を箇条書き。E2E 非回帰・DES CHANGE 反映済み等の横断条件も -->

---

## 開発者への確認事項

> **状態（{{YYYY-MM-DD}}）**: {{確認事項の承認状況。承認済み/暫定運用の別}}。

<!-- 記入指針: 各項目は「何を/なぜ/暫定案」。承認済みは注記して記録保持 -->
1. **{{確認事項の見出し}}**
   何を: {{}}
   なぜ: {{}}
   暫定案: {{}}

---

## 改訂履歴

<!-- 記入指針:
  ★本節は 2026-08-12 新設。従来テンプレは改訂履歴の置き場を持たず、メタ表の
  「バージョン」セルが唯一の置き場だった。その結果 M19-overview のセルが
  13,094 文字まで肥大した（v1.26.1 まで 26 回超の改訂を 1 セルへ追記し続けたため）。
  同型は parallel-board のメタ表でも起き 17,785 文字に達し、2026-08-12 に是正した。
  ★overview は起動時必読なので、肥大はマイルストーンごとに全セッションのコンテキストを削る。
  1 改訂 1 行。内容が長くなるなら本文側を直し、ここには要約だけを書く。
-->

| 版 | 日付 | 内容 |
|---|---|---|
| {{x.y.z}} | {{YYYY-MM-DD}} | {{1 行で。長くなるなら本文を直して要約だけ置く}} |

---

*以上、M{{N}}-overview v{{x.y.z}}。配置 `docs/instructions/M{{N}}-overview.md`。本書は M{{N}}（{{テーマ}}）の正本であり、各サブ指示書の上位文書。サブ連番・実装順は開発者承認済み（Playbook §4.12）。*
