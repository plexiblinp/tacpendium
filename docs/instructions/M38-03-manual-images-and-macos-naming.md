# 指示書 M38-03: 説明書へ画像を貼り込み、macOS の名前を寄せ、検査を表へ載せる

| 項目 | 内容 |
|------|------|
| 文書ID | M38-03 |
| バージョン | **1.1.0**（2026-09-18・**★★★母集団の動き方を是正した**＝`D-895`。**⇒ `M38-02` は図を「1 枚」ではなく*章ごと*減らした**〔`ch14` 削除・22 章 → 21 章〕**。★§0.2 / §0.3 版ゲート 3 / §1 / §2.1 / §4.1 を直した。★射程・完了条件は変えていない**） ／ **1.0.0**（2026-09-17・発行） |
| 作成日 | 2026-09-17 |
| 作成者 | 設計担当 Claude（設計卓） |
| 対象マイルストーン | **`M38`**（撮影で出たリリース前要望の第 2 束）。上位＝`docs/instructions/M38-overview.md` v1.1.0 |
| 推奨モデル | **標準実装**（実モデル名は `docs/human-notes/model-allocation.md`） |
| Plan Mode | **推奨**（**★★段 1 の実査だけで済むか、貼り込みに判断が要るかを先に見せること**） |
| レビュー | **必要**（**★チェックリスト＝`docs/instructions/reviews/M38-03-review-checklist.md` v1.1.0**） |
| CHANGE 消費 | **1 本の見込み**（`DES-002` §11.2 のアーカイブ名。**★自採番しない**＝`D-293`） |
| マイグレ消費 | **0 本** |
| 前提サブ | **★★★3 本とも着地していること**——`M32-03` ／ `M38-01` ／ `M38-02`（**★理由＝§0.2**）**。★あわせて開発者の撮影が終わっていること** |
| 射程 identity | **3 件**——説明書の画像の貼り込み ／ macOS の名前寄せ ／ `CLAUDE.md` §8 への 1 行 |
| 配置 | `docs/instructions/M38-03-manual-images-and-macos-naming.md` |
| 出力 | `docs/progress/M38-03-completion-report.md` |

---

## 0. この指示書の特殊性

### 0.1 ★★★3 つの射程は互いに無関係である

**★束ねた理由は開発者の逐語**（2026-09-17）**＝「マイルストーンが増えすぎると面倒なので、1 つか 2 つでまとめて直させたい」。⇒ どれも単独では 1 マイルストーンに満たない。**

| # | 射程 | 面 |
|---|---|---|
| **A** | **説明書へ画像を貼り込み、`figure` の `todo` を外す** | `docs/usermanual/` |
| **B** | **macOS の名前を寄せる**（**★案 (a)＝アーカイブ名を `darwin` 側へ**） | `scripts/` ／ `DES-002` の原稿 |
| **C** | **`scripts/check-release-archive.sh` を `CLAUDE.md` §8 の表へ載せる** | `CLAUDE.md` |

**★★順序の制約は無い。⇒ どれから着手してもよい。★ただし報告は 3 つに分けて書くこと**（**★1 つが差し戻されても他の 2 つが巻き添えにならないため**）**。**

### 0.2 ★★★前提サブが 3 本ある理由

| 前提 | なぜ |
|---|---|
| **`M32-03`** | **★説明書の本文と図の置き場を確定させたのが同サブである** |
| **`M38-01`** | **★新規登録・編集の見た目が変わる。⇒ §5.7 の図は撮り直しである** |
| **`M38-02`** | **★★★技編集の章**（`ch14`）**が*章ごと消えた*。⇒ 説明書は 22 章 → 21 章になり、その章の図の置き場もまとめて消えた**（**★開発者裁定 2026-09-17**＝「あまり見せたくない機能について言及を残すのは少し嫌です」）**。⇒ 貼り込みの母集団が本サブの着手前に動いている** |

**★★★だから枚数を指示書へ書かない。⇒ 着手時に説明書の現物から数えること。**

**★★根拠＝計測点 `M-187`**——**`M32-03` の母集団を*前サブの執筆時点*から引いた結果 5 件を取りこぼし、うち 2 件は現に誤っていた。⇒ 母集団は*着地した現物*から引く。**

**★★★本サブでは実際に動いた。⇒ `M38-02` が `ch14` を章ごと消し、章数が 22 → 21 になった。★`M32-03` 完了報告の枚数一覧は、章 1 つぶん古い。**

### 0.3 ★★着手前の版ゲート

| # | 確かめること | 満たさないとき |
|---|---|---|
| **1** | `docs/instructions/reviews/M38-03-review-checklist.md` が存在すること | **★止まって設計卓へ請求する** |
| **★★2** | **`docs/usermanual/tacpendium-readme.html` と `tacpendium-quickstart.html` が両方在ること** | **★★無ければ `M32-03` が未着地である。⇒ 止まって請求する** |
| **★★★3** | **説明書に技編集の章が*無く*、章数が「21 章」になっていること**（**★本編・目次・`dist-readme.txt`・`README.txt` の 4 面**） | **★★「22 章」が残っていれば `M38-02` が未着地である。⇒ 止まって請求する。★★★本サブは章数を直さない**（`M38-02` が 4 面とも追随済みのはずである） |
| **★★4** | **`scripts/release-targets.sh` と `scripts/check-release-archive.sh` が在ること** | **★無ければ `M36-01` が未着地である。⇒ 止まって請求する** |
| **★5** | **開発者が用意した画像ファイルが在ること**（**★置き場所は開発者の連絡による**） | **★★無ければ止まって請求する。⇒ 本サブは画像を*作らない***（§3-1） |
| 6 | `git log --oneline -5` で枝元を報告へ書く | — |

---

## 1. 前提として読むもの

| # | 資料 | どこを |
|---|---|---|
| **★★1** | **`docs/usermanual/tacpendium-readme.html`** | **★★図の置き場の全数。⇒ 本サブの母集団である** |
| **★★2** | **`docs/progress/M32-03-completion-report.md`** | **★★図の枚数と場所の一覧**（**★★★ただし `M38-02` が `ch14` を章ごと消している。⇒ この一覧は章 1 つぶん古い。★現物と突き合わせること**） |
| **★★3** | **`docs/progress/M38-02-completion-report.md`** | **★★`ch14` 削除の範囲**〔節 ／ TOC 行 ／ `ch20` の相互参照 ／ 撮影枠 ／ 章数 4 面〕**。⇒ 差分の出所である** |
| **★★4** | **`scripts/release-targets.sh`** | **★★配布物構成の正本**〔アーカイブ名 ／ 中の実行ファイル名 ／ 形式 ／ ソース〕**。⇒ 射程 B が触るのはここである** |
| **★5** | `docs/change-notes/CHANGE-213-notification.md` | **正本を 1 か所に寄せた経緯**（**★「macOS の名前を寄せるときも 1 行で済む」と書いてある**） |
| **★6** | `docs/design/02-architecture.md` | **§11.1**（対応プラットフォーム） ／ **§11.2**（配布物構成） |
| 7 | `CLAUDE.md` | **§8「常設の機械検査（まとめ）」の表** |

---

## 2. 段取り

### 2.1 ★★★段 1 — 実査（**★手を入れる前に済ませ、報告へ書く**）

| # | 数えること | どう数えるか |
|---|---|---|
| **★★★1** | **説明書 2 ファイルの `img` の全数**（**★本編 ＋ クイックスタート**） | **★出力をファイルへ全量落として `wc -l` で数える。⇒ `head` で切らない** |
| **★★2** | **`figure` の `todo` クラスの全数** | **★同上。★★1 と 2 の数が食い違うなら、その理由を報告へ書く** |
| **★★★3** | **開発者が用意した画像ファイルの全数と、その名前** | **★★`img` の `src` と突き合わせる。⇒ 過不足を*先に*表で出す** |
| **★★4** | **`scripts/release-targets.sh` の中で macOS を指している行の全数** | **★★1 行とは限らない。⇒ 数えてから直す** |
| **★5** | **`macos` / `darwin` の両語の出現の全数**（`scripts/` ／ `Makefile` ／ `.github/` ／ `docs/design/`） | **★`--include` を絞らないこと。⇒ 同じ語が 4 面に居る** |

**★★★段 1-3 の突き合わせで過不足が出たら、そこで止まって開発者へ報告すること。⇒ 勝手に名前を付け替えて辻褄を合わせない。**

### 2.2 ★★★段 2 — 射程 A：説明書へ画像を貼り込む

| # | やること |
|---|---|
| **★★★1** | **画像ファイルを `docs/usermanual/images/` へ置く。⇒ 名前は `img` の `src` に合わせる**（**★本編＝`ch<NN>-<slug>-<n>.png` ／ クイックスタート＝`qs-` 接頭辞**） |
| **★★★2** | **`figure` の `todo` クラスを外す**（**★★外すのは `todo` クラスだけである。⇒ `figure` そのものや `figcaption` を消さない**） |
| **★★3** | **`img` の `src` が指す先が*全数*実在することを確かめる。⇒ 1 枚も欠けていないこと**（§5-1） |
| **★★★4** | **画像を*作らない*。⇒ 撮影は開発者の手番である**（§3-1） |
| **★5** | **「現在準備中です」が 0 件のままであることを確かめる**（`M32-02` 完了条件 7.5 ／ `M32-03` §2.4-1） |

**★★本サブの後、説明書に `todo` の置き場が 1 つも残らないのが正しい着地である。⇒ 残すなら理由を報告へ書くこと。**

### 2.3 ★★★段 3 — 射程 B：macOS の名前を寄せる（**案 (a)**）

**★★★開発者裁定＝案 (a)**（2026-09-17・`D-893`）**。⇒ アーカイブ名を `darwin` 側へ寄せる。**

| # | やること |
|---|---|
| **★★★1** | **`scripts/release-targets.sh` のアーカイブ名を `darwin` 側へ寄せる**（**★段 1-4 で数えた行に対して**） |
| **★★2** | **`DES-002` §11.2 の配布物構成の原稿を、設計伝達レポート §1 と §6 へ書く**（**★★設計書本体は書き換えない**＝`CLAUDE.md` §8） |
| **★★★3** | **`check-release-archive.sh` を回して緑であることを確かめる。⇒ 正本が 1 か所であることの実証である** |
| **★4** | **`Makefile` と `README.txt` は既に `darwin` である見込みだが、段 1-5 の実査で確かめること。⇒ 書いてある数字を信じない** |

**★★★なぜ (a) で (b) ではないか**（`D-893`）**＝(b)〔バイナリ名を `macos` へ〕は `CHANGE-191` で固めた面と `windows_gui_link_test.go` と nightly の 3 つに触る。⇒ 触る面が (a) より広い。★(a) は正本が 1 か所に寄っているため 1 行で済む**（`CHANGE-213`）**。**

**★★`§11.1` と `§11.2` の amd64 の食い違いは本サブの射程*外*である**（`followup` の `des002-macos-arch-mismatch`）**。⇒ 触らない。**

### 2.4 ★★段 4 — 射程 C：`CLAUDE.md` §8 の表へ 1 行

| # | やること |
|---|---|
| **★★★1** | **`scripts/check-release-archive.sh` の行を `CLAUDE.md` §8 の表へ足す**（**★開発者承認済み**・2026-09-17・`D-893`） |
| **★★2** | **「何を見るか」は*スクリプト自身の実装*から書く。⇒ 本指示書の要約を写さない** |
| **★★★3** | **足した後に `bash scripts/check-doc-refs.sh` を回すこと**（**★★これが本射程の全部である**——**設計卓の枝には同スクリプトが無く、載せると赤になるため載せられなかった。⇒ 本サブの枝には在る**） |

**★★★同じ理由で、本射程は `M36-01` の着地後にしか実行できない。⇒ §0.3 の版ゲート 4 がそれを見る。**

### 2.5 ★段 5 — 設計書の原稿

**★CHANGE 原稿を設計伝達レポートの §1 と §6 へ書く。⇒ §4 ではない**（計測点 `M-183`）**。★射程 B のぶん 1 本だけである。⇒ 射程 A と C は設計書へ届かない。**

---

## 3. やらないこと

| # | やらないこと | なぜ |
|---|---|---|
| **★★★1** | **スクリーンショットを撮ること・作ること** | **★★撮影は開発者の手番である。⇒ 用意されていない画像が在れば止まって請求する** |
| **★★2** | **説明書の本文を書き換えること** | **★★本サブは*貼り込み*だけである。⇒ 本文は `M32-03` と `M38-02` の成果である** |
| **★★★3** | **図の枚数を増減させること・章数の表記を直すこと** | **★★母集団は着手時の現物で確定している。⇒ 足りなければ止まって請求する**（§2.1-3）**。★★★章数「21 章」は `M38-02` が 4 面とも追随済みである。⇒ 触らない** |
| **★★4** | **`figure` や `figcaption` を消すこと** | **★★外すのは `todo` クラスだけである** |
| **★★★5** | **バイナリ名を `macos` へ寄せること**（**案 (b)**） | **★★裁定は (a) である**（`D-893`）**。⇒ (b) は触る面が広い** |
| **★★6** | **`DES-002` §11.1 と §11.2 の amd64 の食い違いを直すこと** | **★未決である**（`followup` の `des002-macos-arch-mismatch`） |
| **★7** | **`CLAUDE.md` §8 の表の*他の行*を触ること** | **★足すのは 1 行である** |
| **★8** | **`docs/design/` 本体を編集すること** | **`CLAUDE.md` §8** |
| **★★9** | **`docs/usermanual/dist-readme.txt` を触ること** | **★`M36-01` の成果である**（`D-887`）**。⇒ 本サブの射程外** |

---

## 4. 危険（**★起きやすい順**）

### 4.1 ★★★最大の危険＝**枚数を指示書や過去の報告から引くこと**

**★★★計測点 `M-187` が同じ型である**——**母集団を*前サブの執筆時点*から引いた結果 5 件を取りこぼし、うち 2 件は現に誤っていた。**

**★★本サブでは `M38-02` が `ch14` を章ごと消している。⇒ `M32-03` の報告にある枚数は*着手前の値ではない*。★現物から数えること**（§2.1-1）**。**

### 4.2 ★★2 番目＝**貼り込めなかった置き場を黙って残すこと**

**★★`todo` が残ったまま `img` が空を指すと、説明書はリンク切れの図を出す。⇒ しかも検査は全緑である**（`D-890` の指摘と同じ型）**。**

**★★★残すなら、どの章の何枚を、なぜ残したかを報告へ書くこと。**

### 4.3 ★★3 番目＝**`macos` / `darwin` の置換を機械的に当てること**

**★★同じ語が 4 面に居る**〔`scripts/` ／ `Makefile` ／ `.github/` ／ `docs/design/`〕**。★全置換すると `GOOS=darwin` のようなツールチェーンの値まで動く。⇒ 1 件ずつ「アーカイブ名として書かれているか」を判定する**（先例＝`CHANGE-092` の「`30` / `13` を機械的に全置換しない」）**。**

### 4.4 ★2 番目に地味だが効く＝**`CLAUDE.md` の行を、スクリプトを読まずに書くこと**

**★★`E-76`＝同じ情報を 2 か所に持つと必ずドリフトする。⇒ 表の「何を見るか」は実装から書く。★本指示書の要約を写すと、写した時点でずれる。**

---

## 5. テスト

| # | 見るもの |
|---|---|
| **★★★1** | **説明書 2 ファイルの `img` の `src` が指す先が*全数*実在すること**（**★段 1-1 で数えた全数に対して**） |
| **★★2** | **`figure` の `todo` クラスが 0 件であること**（**★残すなら理由が報告に在ること**） |
| **★★3** | **「現在準備中です」が 0 件であること** |
| **★★★4** | **`bash scripts/check-release-archive.sh` が緑であること**（**★射程 B の実証**） |
| **★★5** | **`bash scripts/check-doc-refs.sh` が緑であること**（**★射程 C の実証**） |
| **★6** | **`make release-archives` が通り、出来たアーカイブ名が `darwin` 側であること**（**★出力を報告へ貼る**） |
| 7 | **`go test ./...` ／ `cd web && pnpm test` ／ `make e2e` を全数**（**★出力をファイルへ全量落とし、`EXIT=` と `FAIL` の件数を報告へ貼ること**） |
| 8 | `bash scripts/check-md-emphasis.sh <触った .md>` ／ `bash scripts/check-artifact-integrity.sh` |

---

## 6. 完了条件

| # | 条件 |
|---|---|
| **★★★1** | **段 1 の実査 5 点が*件数*で報告に在る** |
| **★★★2** | **説明書の `img` が全数実在する。⇒ 数えた全数と貼った数が突き合わせてある** |
| **★★3** | **`figure` の `todo` が 0 件。⇒ 残っているなら章・枚数・理由が報告に在る** |
| **★★★4** | **`scripts/release-targets.sh` のアーカイブ名が `darwin` 側に寄っている。⇒ `check-release-archive.sh` が緑である** |
| **★★5** | **`CLAUDE.md` §8 の表に `check-release-archive.sh` の行が在り、`check-doc-refs.sh` が緑である** |
| **★6** | **`DES-002` §11.2 の原稿が設計伝達レポート §1 と §6 に在る**（計測点 `M-183`） |
| 7 | **§5 の検査がすべて緑**（**★出力で判定する**） |
| **★8** | **`docs/progress/M38-03-completion-report.md` を書き、`progress-log.md` へ索引行を 1 行足した** |
| **★★9** | **報告が射程 A ／ B ／ C の 3 つに分かれている**（§0.1） |
| **★10** | **着手前の版ゲート 6 点を通した**（§0.3） |
| **★★11** | **完了報告を書いた*後に*、報告を入力に取る検査を回し直した**（**★製造 CLI の当該節**） |

---

## 7. ★開発者への確認事項

**★全て確定済みである**（2026-09-17・`D-893`）**。**

| # | 事項 | 確定内容 |
|---|---|---|
| 1 | 貼り付けと `todo` 外しを誰がやるか | **★Claude 側でよい**（開発者逐語＝「私は画像を用意し、張り付けと todo 削除は Claude Code か Codex に任せようかと思います」） |
| 2 | macOS の名前 | **★案 (a)＝アーカイブ名を `darwin` 側へ寄せる** |
| 3 | `check-release-archive.sh` を `CLAUDE.md` §8 へ載せるか | **★承認済み。⇒ 載せる** |

---

## 8. 参照ドキュメント

| ID | ファイルパス | どこを |
|----|-------------|--------|
| M38-overview | `docs/instructions/M38-overview.md` | §3.4（本サブ） ／ §5（撮影の扱い） |
| DES-002 | `docs/design/02-architecture.md` | §11.1 ／ §11.2 |
| CHANGE-213 | `docs/change-notes/CHANGE-213-notification.md` | **配布物構成の正本が 1 か所に寄った経緯** |
| followup | `docs/handover/followup-backlog.md` | `des002-macos-arch-mismatch` ／ `usermanual-chapter-count-22-scattered` |
| ボード | `docs/process/parallel-board.md` | `D-890` / `D-893` / `D-894` |

---

*以上、指示書 M38-03 **v1.1.0**。* **★★★本サブの芯は §0.2 である**——**図の枚数は着手前に*動く*。⇒ 指示書にも過去の報告にも正しい数は書いていない。★現物から数えること。**
