# 指示書 M30-01: 仮想コントローラの技の出し分け（何を出すか）

| 項目 | 内容 |
|------|------|
| 文書ID | M30-01 |
| バージョン | **1.2.0**（2026-09-08・**★★完了後の注記を §0.5 に足した＝`D-782`。★本文は書き換えていない**） ／ 1.1.0（2026-09-07・**★★`CHANGE-166` の as-built を §0.4 として追補した＝`D-773`。⇒ `P4M-017` の答えの一部が、本サブの起票後に判明していた**） ／ 1.0.0（2026-09-06） |
| 作成日 | 2026-09-06 |
| 作成者 | 設計担当 Claude（設計卓／フェーズ4 期） |
| 対象マイルストーン | **`M30`**（`α②` 仮想コントローラ）。上位＝`docs/instructions/M30-overview.md` v1.2.0 |
| 推奨モデル | **高精度実装 ＋ Plan Mode 必須**（**★★「何を出すか」の現行規則が誰にも分かっていない。⇒ 実測が最初の成果物である**。実モデル名は `docs/human-notes/model-allocation.md`） |
| Plan Mode | **必須**（`CLAUDE.md` §9＝全体に影響する変更。**★出し分けの規則を決めてから実装する**） |
| レビュー | **必要**（チェックリストは着手承認の手番で設計卓が起こす。**★`D-736` の運用**） |
| CHANGE 消費 | **1 本の見込み**（`DES-005` §5.7。**★起票は設計卓。自採番しない**＝`D-293`） |
| マイグレ消費 | **0 本の見込み**（**★次に払い出す番号は `000104` 以降。要ると判明したら止めて請求する**） |
| 前提サブ | **無し**（**★`M28` レーンとは面が違う**）。**★★ただし `M30-02` は本サブの後である**（`M30-overview` §3） |
| 射程 identity | **5 件**——`P4M-007` / `P4M-008` / `P4M-017` / `SM-100` / `SM-135` |
| 配置 | `docs/instructions/M30-01-controller-move-surfacing.md` |
| 出力 | `docs/progress/M30-01-completion-report.md` |

---

## 0. この指示書の特殊性

### 0.1 ★★最初の成果物は実装ではなく「現行の出し分け規則」である

**開発者の逐語（`P4M-017`）＝「ジェイミー等、必殺技が一部しか仮想コントローラには出ていない？ 表示基準がよくわからない。」**

**⇒ 課題そのものが「規則が読めない」である。★実装を直す前に、いま何が出て何が出ていないかを実測して示すこと**（§2.1）。

**★★規則が分かってから、変えるかどうかを決める。⇒ 順序を逆にしないこと。**

### 0.4 ★★【2026-09-07 追補＝`D-773`】`P4M-017` の答えの一部が、本サブの起票後に判明した

**★★`CHANGE-166`**（`M28-05` 由来・2026-09-07）**が、次を設計書へ明文化した。⇒ 本サブの起票**（2026-09-06）**より後である。**

> **強度接尾辞**（`_light` / `_medium` / `_heavy` / `_od`）**を持たない `move_code` は、必殺技クイック入力パネルのファミリー UI に載らない。⇒ 「全技一覧から選ぶ」プルダウンからの入力になる。★これは意図された設計である。**

**★★これは `P4M-017` の逐語**〔「ジェイミー等、必殺技が一部しか仮想コントローラには出ていない？ 表示基準がよくわからない」〕**の答えの一部である。⇒ 「出ていない技」の少なくとも 1 種類は、この規則で説明が付く。**

**★実例は 2 件＝`guile` の `sonic_break` ／ `terry` の `quick_burn`**（どちらも `_od` 版だけが別行で在る）**。**

| # | 本サブへの帰結 |
|---|---|
| **★★1** | **§2.1 の実測は、この規則を「既知の説明」として持ったうえで行うこと。⇒ 説明が付く分と、付かない分を分けること。★分けないと、既知の設計を「不具合」として直しかける** |
| **★★2** | **★★この規則そのものを変えるかどうかは、本サブの判断ではない。⇒ 変える必要があると分かったら止めて設計卓へ請求する**（`D-293`）**。★`DES-004` §2.1 と `DES-005` の両方に及ぶ** |
| **★3** | **ジェイミーで実際にこの形が出ているかは未確認である。⇒ §2.1 で確かめること。★`sonic_break` / `quick_burn` 以外にも同型が在りうる** |
| **4** | **参照＝`DES-004` §2.1 の「強度の区別が無い必殺技 ＝ `<技名>` 単体」の行 ／ `DES-005` の必殺技クイック入力の注記 ／ `docs/change-notes/CHANGE-166-notification.md` §1** |

---

### 0.5 ★★【2026-09-08 完了後の注記＝`D-782`】本書の 2 か所が実装と食い違う（**両方とも決着済み**）

> **★★本文は書き換えない**（`D-274` (3)＝**投入済みの指示は歴史記録であり、注記で足す**）**。⇒ 読むときは次の 2 点を上書きして読むこと。**

| 本書の記述 | 実際 | 裁定 |
|---|---|---|
| **§0.2 と §4「やらないこと」-1**＝`M24-12` の入力の骨格に触れない。「本サブが触るのは『タブに何を並べるか』であり『どう押すか』ではない」 | **★★常設行の共通技をタブへ移した。⇒ 「どう押すか」に当たる** | **★★受理**（`CHANGE-170`）**。⇒ 開発者が実機確認を経て明示的に指示した**（逐語＝「共通技のタブが欲しくなってきた。…なお下部の共通技は移動に伴い削除してください」）**。★製造は指示書との衝突を指摘したうえで実施している。★これが受理できる形である** |
| **§2.1-3**＝「`moves` に在って仮想コントローラに出ない行を全数で挙げること」 | **★★製造は `character_data/*.csv` を数えた。⇒ 未分類 330 行と報告したが実際は 551 行だった** | **★★規約化した**（`CHANGE-170` ／ `SUPP-001` §5.5.3.2）**＝「seed の実測は `moves` テーブルに対して行う。`character_data/*.csv` は入力の一部でしかない」。★計測点 `M-145`** |

**★★骨格の実体は保たれている。⇒ 物理入力の点灯は `heldCodes` を通して保った ／ 数字キーの割当は 1 つも消費していない ／ 削除ボタンはタブへ入れず常設のまま残した**（どのタブに居ても直前のステップを消せる必要があるため）**。**

---

### 0.2 ★★`M24-12` の入力の骨格は動かさない

**`M24-12` がボタン群・数字キー・順送りの停止点を定め**（`DES-005` §5.7）**、`M27-02b` がその上に必須・任意の印を足した。**

**⇒ 本サブが触るのは「タブに何を並べるか」であり、「どう押すか」ではない。★`data-testid` と数字キーの割当は `M30-02` の射程である。**

### 0.3 ★射程は 5 件。ledger の `代表 ID` で数える

| identity | 内容 | 名寄せ |
|---|---|---|
| **`P4M-007`** | **ターゲットコンボタブ**（逐語＝「特殊技タブの隣にターゲットコンボタブが欲しい」） | **`SD-010` / `SM-135` を代表する** |
| **`P4M-008`** | **非表示技・キャラ別技タブ**（逐語＝「SA の隣に何らかの理由で表示していない技〔強化版必殺技等〕だけを出すタブが欲しい」＋「キャラ別に特定の技を出すタブ」） | **`SD-011` を代表する** |
| **`P4M-017`** | **技の表示基準**（逐語＝§0.1） | **`SD-021` を代表する** |
| **`SM-100`** | **全技一覧から、仮想コントローラで入力できる技を省く** | — |
| **`SM-135`** | **ターゲットコンボも入力に欲しい** | **`P4M-007` が代表。★別件として数えない** |

**★★実質は 4 つの決めごとである**（`SM-135` は `P4M-007` に畳まれる）。**⇒ `M-116` の教訓＝`代表 ID` 欄を引けば、判断ではなくデータで分かる。**

---

## 1. 前提として読むもの

| # | 資料 | 読む理由 |
|---|---|---|
| 1 | `docs/instructions/M30-overview.md` v1.2.0 §2 | **identity の突き合わせ。★12 → 10 になった経緯** |
| 2 | `DES-005` §5.7 | **★`M24-12` の入力の骨格。本サブは動かさない**（§0.2） |
| 3 | `docs/progress/M25-RESEARCH-01-report.md` §2.2 | **ledger（`代表 ID` 欄が名寄せの正本）** |
| 4 | `docs/handover/code-facts.md` | **★実装の現在地。想定で書かない** |
| 5 | `DES-003` §3.3（`moves`）・§3.20（`move_derivations`） | **★出し分けの材料になる列**（`category` / `is_derived` / `startup_basis`） |
| 6 | `docs/handover/M19-DESIGN-08-exclusion-predicates.md` §0 | **★★「空振りで出せるか」の判別軸は `is_derived` である**（`P-16` の決着）。**⇒ 出し分けの材料として既に決着した軸が在る** |

---

## 2. やること

### 2.1 ★★現行の出し分け規則を実測する（**最初の成果物**）

**実装へ入る前に一覧を提示すること。**

| # | やること |
|---|---|
| **★★1** | **仮想コントローラの現在のタブ構成を出す**（**何タブ在るか。それぞれ何を並べているか**）。 |
| **★★2** | **★★タブごとの述語を、コードから逐語で書き出す**（`moves` のどの列で絞っているか）。**⇒ 「たぶん `category` で絞っている」ではなく、現物の条件式を写す。** |
| **★★3** | **★★出ていない技を数える。⇒ 1 キャラを例に、`moves` に在って仮想コントローラに出ない行を全数で挙げること**（**開発者が挙げたジェイミーを使うのが早い**）。 |
| **★★4** | **★出ない理由を分類する**〔**述語で落ちている ／ データが無い ／ 意図的に除いている ／ 理由が読めない**〕**。★「理由が読めない」が在ったら、それが `P4M-017` の答えの一部である。** |
| **★5** | **`SM-100`（全技一覧）の現状も同じ形で出す**——**「仮想コントローラで入力できる技」を判定する経路が在るか。無いなら、無いと書く。** |

### 2.2 `P4M-017` — 技の表示基準を決める

| # | 内容 |
|---|---|
| **★★1** | **§2.1 の実測を材料に、出し分けの規則を Plan Mode で提案すること。★案を出して止まる。★製造が単独で決めない**（**利用者に見えるものの取捨であり `roles-and-routing` ハード列**）。 |
| **★★2** | **★★既に決着した軸を使えないか先に見ること**——**`is_derived`**〔`M19-DESIGN-08` §0。**「空振りで出せるか」の判別軸として決着済み**〕**／ `category` ／ `startup_basis`。⇒ 新しい列を作る前に、在る列で導けるかを確かめる。** |
| **★★3** | **★新しい列を足したくなったら止まって報告すること**（**スキーマ変更は開発者の個別承認事項**＝`CLAUDE.md` §10）**。⇒ `M28-overview` §3.6 と同じ窓に載せる判断が要る。** |
| **★4** | **規則を決めたら、その規則で「出る／出ない」が全キャラで説明できることを確かめること。⇒ 説明できない行が残るなら、規則が正しくない。** |

### 2.3 `P4M-007` — ターゲットコンボタブ

| # | 内容 |
|---|---|
| **★★1** | **特殊技タブの隣に置く**（開発者の逐語）。 |
| **★★2** | **★母集団は `category = 'target_combo'` である**（`DES-003`）**。⇒ ただし `is_derived` の扱いを決めること**——**`M19-DESIGN-08` §0 が「`NOT (category='target_combo' AND is_derived = 1)` が filler の除外述語」と決めている。⇒ 本タブは逆に「出す」側なので、同じ軸をどう使うかを報告に書く。** |
| **★3** | **`SM-135`（ターゲットコンボも入力に欲しい）は本項に畳まれる。★別のものを作らない。** |
| **★4** | **タブが増えると `M24-12` の数字キーの割当に影響しないかを確かめること**（§0.2。**★影響するなら報告して止まる。割当を変えるのは `M30-02` である**）。 |

### 2.4 `P4M-008` — 非表示技・キャラ別技タブ

| # | 内容 |
|---|---|
| **★★1** | **★★開発者の逐語は 2 つある**——**(a) 「SA の隣に何らかの理由で表示していない技（強化版必殺技等）だけを出すタブが欲しい」 ／ (b) 「キャラ別に特定の技を出すタブ」。** |
| **★★2** | **★(a) は §2.2 の規則に依存する。⇒ 「表示していない技」は、規則が決まって初めて定義できる。★順序を守ること。** |
| **★★3** | **★★(b) の「キャラ別に特定の技」は、何を指すかが決まっていない。⇒ Plan Mode で読みを 2〜3 出して止まること。★製造が解釈で埋めない。** |
| **★4** | **(a) と (b) を 1 タブにするか 2 タブにするかも §7-1 で返すこと。** |

### 2.5 `SM-100` — 全技一覧から入力できる技を省く

| # | 内容 |
|---|---|
| **★★1** | **★これは仮想コントローラではなく「全技一覧」側の変更である。⇒ 面が 1 つ増える。** |
| **★★2** | **★★判定は §2.2 の規則の裏返しになる。⇒ 規則を 2 か所へ書かないこと。1 本の述語を両側から使う形にする**（**★2 か所に書くと、片方だけ直されて静かにずれる**）。 |
| **★3** | **「省く」が既定か、切り替えられるかを Plan Mode で提案すること**（**★既存のコンボが参照している技が一覧から消えると、利用者が探せなくなる面がある**）。 |

### 2.6 テスト

| # | 内容 |
|---|---|
| **★★1** | **★出し分けの述語は純粋関数として書き、単体テストを必須にする**（`CLAUDE.md` §5＝フロント純粋関数は必須）。 |
| **★★2** | **★★「出る」だけでなく「出ない」もテストすること。⇒ 出ない理由の分類**（§2.1-4）**ごとに 1 件ずつ。** |
| **★3** | **タブが増えた E2E を 1 本足すこと**（`/add_e2e_spec`）。**★`make e2e` は全数で回す。** |
| **★4** | **★`M24-12` の骨格を守るテスト**〔数字キー・順送りの停止点〕**が緑のままであることを確かめること**（§0.2）。 |

### 2.7 ★コミットの割り方

| 順 | コミット |
|---|---|
| **1** | **§2.1 の実測**（コードは動かない。**★述語の書き出しと分類だけ**） |
| **2** | **`P4M-017` の規則の実装**（述語 1 本） |
| **3** | **`P4M-007` ターゲットコンボタブ** |
| **4** | **`P4M-008` 非表示技・キャラ別技タブ** |
| **5** | **`SM-100` 全技一覧側** |

**★2〜5 がそれぞれ単独で revert できる形にすること。**

---

## 3. 出力

`docs/progress/M30-01-completion-report.md`。

| # | 書き方の規約 |
|---|---|
| **★★1** | **§2.1 の実測**（**タブ構成 ／ タブごとの述語の逐語 ／ 出ない技の全数と分類**）。**★これが本サブで最も価値のある成果物である。** |
| **★★2** | **決めた出し分けの規則と、その規則で全キャラが説明できること。★説明できない行が残るなら、それを書く。** |
| **★★3** | **`P4M-008` の (b) をどう読んだか。★開発者へ返した案と、選ばれたもの。** |
| **★★4** | **`SM-100` の述語が `P4M-017` の述語と 1 本を共有していること**（§2.5-2）。 |
| **★5** | **`M24-12` の骨格に触れなかったこと**（数字キー・順送り・`data-testid`）。 |
| **★6** | **`docs/design/` に反映が要る箇所の一覧**（**製造は直さない**）。 |
| **★7** | **`docs/progress/progress-log.md` の `### M30-01` へ追記**（`CLAUDE.md` §8）。 |

---

## 4. やらないこと

| # | やらないこと |
|---|---|
| **★★1** | **`M24-12` の入力の骨格に触れること**（ボタン群の押し方・数字キーの割当・順送りの停止点。§0.2） |
| **★★2** | **ボタンの名前と配置を変えること**（`SM-080` / `SM-149`＝**`M30-02` の射程**） |
| **★★3** | **強度・変種の選び方を変えること**（`P4M-016` / `P4M-018` / `SD-020`＝**`M30-02` の射程**） |
| **★★4** | **出し分けの規則を製造が単独で決めること**（§2.2-1。**★Plan Mode で案を出して止まる**） |
| **★★5** | **新しい列を足すこと**（§2.2-3。**★スキーマ変更は開発者の個別承認事項**） |
| **★★6** | **`P4M-008` の (b) を解釈で埋めること**（§2.4-3） |
| **★★7** | **出し分けの規則を 2 か所へ書くこと**（§2.5-2） |
| **★8** | **子ウィンドウ化 / なっちゃってレバー**（`SM-081` / `SM-087`。**★`M30` の射程外**＝`D-722`） |
| **★9** | **`docs/design/` の編集**（`CLAUDE.md` §8） |
| **★10** | **`docs/handover/followup-backlog.md` の編集**（`D-382`。**§J 停止時記録だけは例外**） |
| **★11** | **CHANGE 番号・マイグレ連番の自採番**（`D-293`） |

---

## 5. 完了条件

1. **§2.1 の実測が出ており、出ない技が全数で分類されている。**
2. **出し分けの規則が決まり、その規則で全キャラが説明できる**（説明できない行が在るなら書かれている）。
3. **4 つの決めごと**〔`P4M-017` / `P4M-007` / `P4M-008` / `SM-100`〕**すべてに着地がある**（実装した ／ 案を出して止まった、のいずれか）。
4. **`M24-12` の骨格を守るテストが緑のままである。**
5. **`go test ./...` / `cd web && pnpm test` / `make e2e` が緑。**
6. **常設検査が緑**（`check-artifact-integrity.sh` を 1 本目に回す）。
7. **`docs/design/` に反映が要る箇所が一覧になっている。**
8. **`docs/progress/progress-log.md` へ追記されている。**

---

## 6. ★設計卓が確かめていないこと（実査項目）

| # | 確かめること |
|---|---|
| **★★1** | **仮想コントローラの現在のタブ構成**（**★設計卓は `web/src` を読めない**＝武装中） |
| **★★2** | **タブごとの述語の現物**（§2.1-2） |
| **★★3** | **`SM-100` の「入力できる技」を判定する経路が実在するか**（§2.5-1。**★無いかもしれない**） |
| **★4** | **タブを増やすと `M24-12` の数字キーの割当がどうなるか**（§2.3-4） |
| **★5** | **`is_derived` が仮想コントローラ側で既に使われているか**（§2.2-2。**★`M19-DESIGN-08` は filler の除外述語として決着させたが、コントローラ側での利用は未確認**） |

---

## 7. 開発者への確認事項

**★以下は着手前ではなく、実装の途中で返すものである。⇒ 本サブの投入はこれ待ちにしない。**

| # | 確認事項 | 設計卓の暫定案 |
|---|---|---|
| **★★1** | **`P4M-008` の (b)「キャラ別に特定の技を出すタブ」は何を指すか**（§2.4-3） | **★案なし。★★逐語だけでは形が決まらない。⇒ 製造が §2.1 の実測を出したら、その材料の上で 2〜3 の読みを提示させ、開発者が選ぶ形にする。★(a) と 1 タブにするか 2 タブにするかも同時に決める** |
| **★★2** | **`P4M-017` の出し分けの規則** | **★★製造が §2.1 の実測とともに案を出す。⇒ 設計卓は形を決めない**——**現行の述語が読めないうちに規則を決めると、「いま出ていないもの」を取りこぼす** |
| **★3** | **`SM-100` の「省く」が既定か、切り替えられるか**（§2.5-3） | **★切り替えられる形を推す**——**既存のコンボが参照している技が一覧から消えると、利用者が探せなくなる面がある** |

---

## 8. 参照ドキュメント

| ID | パス | 参照する箇所 |
|----|------|------------|
| — | `docs/instructions/M30-overview.md` | §2（identity）／ §3（サブ構成）／ §5（実査項目） |
| DES-005 | `docs/design/05-screen-design.md` | §5.7（**`M24-12` の入力の骨格 ＋ `M27-02b` の必須・任意の印**） |
| DES-003 | `docs/design/03-data-model.md` | §3.3（`moves`）／ §3.20（`move_derivations`） |
| — | `docs/handover/M19-DESIGN-08-exclusion-predicates.md` | §0（**`is_derived` の決着**） |
| — | `docs/progress/M25-RESEARCH-01-report.md` §2.2 | **ledger（`代表 ID`）** |
| — | `docs/handover/code-facts.md` | 実装の現在地 |

---

*以上、M30-01 指示書 **v1.2.0**。* **★★本サブの落とし穴は「タブを足すサブだ」と読むことである。** **★最も価値のある成果物は §2.1 の実測**〔いま何が出て何が出ていないか、そしてなぜか〕**であり、タブはその後で決まる。⇒ 順序を逆にすると、「いま出ていないもの」を取りこぼしたまま新しいタブを足すことになる。**
