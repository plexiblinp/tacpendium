# M39-overview: `startup_basis` の不変条件の是正（**`D-187` の 132 行 ＋ 常設ガード化**）

| 項目 | 内容 |
|------|------|
| 文書ID | M39-OVERVIEW |
| バージョン | **1.4.0**（2026-09-20・**★★★`M39-02` を受理し `M39` をクローズした**＝`D-916`。⇒ `CHANGE-235` を起票と同時に反映した。★2 サブとも着地。★マイグレ消費は通算 0 本） ／ **1.3.0**（2026-09-19・**★★★`M39-02` を起票した**＝`D-913`〔**開発者承認済み。逐語「M39-02起票してかまいません。リリース前に直したいです。」**〕**。⇒ `M39` は 2 サブ構成になった。★三点セット発行済み・未投入。★`M36-02` と並列可**） ／ **1.2.0**（2026-09-19・**★★★`M39-01` を受理した**＝`D-911`〔マージ可〕**。⇒ `CHANGE-229`〜`232` を起票と同時に反映した。★★`M39` はまだ閉じない**——**`M39-02` の起票を提案しており**（`D-912`）**、開発者の承認待ちである**） ／ **1.1.0**（2026-09-19・**★★★是正の形を「新系列 `000001`〜`000009` への折り込み」へ変えた**＝`D-910`〔**開発者の決定**〕**。⇒ マイグレ消費は 1 本から 0 本になった。★`D-336` のカーブアウトを切った**） ／ **1.0.0**（2026-09-19・起票＝`D-909`） |
| フェーズ | **フェーズ4**（初回全体公開までにやるべきこと） |
| 状態 | **★★★`M39` はクローズした**（2026-09-20・`D-916`）**＝2 サブとも受理・マージ可である。⇒ `CHANGE-229`〜`232` / `235` を反映済み。★対の followup 2 行**〔`d187-...` ／ `rush-variant-...`〕**も対で閉じた。★開発者が検証 DB で実測 0 件を確認済み。** |
| 由来 | **followup §DS** の `d187-startup-basis-invariant-violated-at-head`（`M33-03` レポート §4-1 ／ `D-906` (7)） |
| CHANGE 見込み | **★★実績 4 本**（`CHANGE-229`〜`232`）**。⇒ 見込みは 0〜1 本であった。★4 本とも「値」ではなく*規約*である**〔規約 (23) (24) (25) の新設 ＋ §2.7 の理由の差し替え〕**。⇒ 指示書 §2.5 が見込んでいたのは (24) の 1 本だけであった。** |
| マイグレ消費 | **★★★0 本**（**★`M39-01`＝新系列への折り込み**＝`D-910` ／ **`M39-02`＝実行時の経路であり `migrations/` を触らない**）**。★次に払い出す連番は `000010` のままである。** |
| 新規依存 | **0 件** |

---

## 0. ★★★なぜ `M33` を開き直さず新しい MS を立てるか

**★★`M33` は 3 サブとも着地してクローズした**（2026-09-19・`D-907`）**。⇒ 開き直すと「クローズした」の意味が消える。**

**★先例は `D-892` である**——**`M37` が 8 サブとも着地して閉じた後、撮影で出たリリース前要望を `M37-09` にせず `M38` として新設した。⇒ 同じ形である。**

**★★もう 1 つ理由がある＝`M33` の射程条件は「マイグレ消費 0 本」であった**〔`M33-03` 指示書 §3-1〕**。⇒ 本サブはマイグレを 1 本消費する。★射程条件を破る作業を同じ MS へ入れると、その MS の完了条件が後から読めなくなる。**

---

## 1. ★★射程（**1 件。⇒ followup §DS の 1 行そのものである**）

| # | スラッグ | 面 | なぜ公開前か |
|---|---|---|---|
| **★★★1** | **`d187-startup-basis-invariant-violated-at-head`** | **データ ＋ ガード** | **★★★マイグレを綺麗にした状態で配りたい**（開発者の逐語・2026-09-19）**。⇒ 公開後に直すと、利用者の DB に是正マイグレが 1 本増える。★いま直せば新系列の `000010` として静かに載る** |

---

## 2. ★★サブ構成

| サブ | 射程 | 状態 |
|---|---|---|
| **`M39-01`** | **132 行の是正 ＋ `character_data/` 6 行 ＋ 読む経路の全数 ＋ ガードの `TestRun_HEAD_*` 化** | **★★★完了・受理**（2026-09-19・`D-911`）**。⇒ `CHANGE-229`〜`232` を反映済み。★マイグレ消費 0 本**〔案 A＝`000004` への折り込み〕**。** **★★最大の成果は射程外にあった＝段 1 で「*書く*経路」まで数え、ラッシュ生成経路が実行時に同じ不変条件を破れることを見つけた**〔該当元技が 3 件実在〕**。⇒ `M39-02` の提案へつながった。** |
| **`M39-02`** | **ラッシュ生成経路が実行時に `D-187` を破る穴を塞ぐ** | **★★★完了・受理**（2026-09-20・`D-916`）**。⇒ 案 (a) で着地＝元技の `startup` が NULL なら `'unknown'` を入れる。★置き場は Go 側**〔`CLAUDE.md` §4 の列挙定数へ戻せる〕**。★`rushEligible` は現状維持**（開発者裁定）**。★`CHANGE-235` を反映済み。** |

---

## 3. ★★本 MS で扱うことの中身

### 3.1 ★★★何が壊れているか

**★`D-187` の不変条件＝「`startup` が `NULL` なら `startup_basis` は `unknown`」。⇒ HEAD で 132 行が違反している**（`M33-03` の実測）**。**

| 内訳 | 件数 | 出所 |
|---|---|---|
| **`category='system'` の移動 move** | **126** | **★マイグレ SQL のみが源泉**〔14 キャラ × 9。**★14 キャラは第四波そのもの**〕 |
| **`target_combo`** | **4** | **CSV** |
| **`special`** | **2** | **CSV** |

**★★原因＝旧 `000065` の是正が第三波までの母集団にしか当たらず、第四波の backfill が CSV の `standalone` を入れ直した。**

### 3.2 ★★★なぜ誰も気づかなかったか

**★★旧テストは版 v64 でしか不変条件を見ていなかった。⇒ v64 の時点では成立していた。★その後の seed 波で崩れたことを、誰も見ていない。**

**★★★これが本 MS の半分である。⇒ 132 行を直すだけで帰ると、次の seed 波でまた崩れる。**

### 3.3 ★★実害の扱い

**★`M33-03` は「実害は出ていない」と結論した**〔`usesFirstHitStartup` の 110 行のうち未記入の 4 行が `target_combo` 4 行と完全一致し、`candidateStartup` が `nil` を返して候補から落とす＝fail-closed〕**。**

**★★★ただし根拠は 1 経路だけである。⇒ `M39-01` の段 1 で「`startup_basis` を読む経路」を全数数えさせる。★数えてから是正する。**

---

## 3.4 ★★★是正の形（**案 A＝折り込み**）

**★★★開発者の決定である**（2026-09-19・`D-910`。逐語＝「新マイグレではなく、既存マイグレに含める形はダメですか？」→「案 A にします」）**。⇒ 目的は `M33` と同じであり、マイグレを綺麗にして配ることである。**

**★★★`D-336`**〔適用済みマイグレは改変しない〕**のカーブアウトを切った。⇒ 述語＝「新系列 `000001`〜`000009` は、*公開前*であり、かつ適用済みの DB が*開発者の検証環境 1 つだけ*である間に限り、改変してよい」。★この窓は公開で閉じる。**

| # | 得るもの |
|---|---|
| **★★1** | **マイグレが 9 本のままである。⇒ 誤った値が*最初から入らない*** |
| **★★2** | **`down` の設計問題が消える。⇒ INSERT の値が変わるだけであり、`down` の主張は動かない** |

| # | 代償 |
|---|---|
| **★★★1** | **`M33-02` の「全一致」を意図的に手放す。⇒ 以後は「`startup_basis` の 132 セルを除いて一致」になる** |
| **★★★2** | **開発者の検証 DB を作り直す手番が 1 つ増える。⇒ `schema_migrations` が 9 本を持つ DB では書き換えた版は再実行されない** |
| **★★3** | **レビューしづらい。⇒ 巨大な INSERT 群の中の 132 セルは目で追えない。★機械での立証が要る** |

---

---

## 3.5 ★★★`M39-02` が要る理由（**★実行時の穴である**）

**★★★`M39-01` のガードは `TestRun_HEAD_*` 族であり、*新規 DB の最終状態*を見る。⇒ 利用者が操作して作った行は見ない。**

**★★ラッシュ版の生成経路が、実行時に同じ不変条件を破れる。⇒ `startup` が NULL の元技からラッシュ版を作ると `startup NULL` ＋ `startup_basis='through'` の行ができる。**

**★★★HEAD に該当元技が *3 件実在する*＝`rashid/run` ／ `alex/prowler_stance` ／ `dee_jay/speedy_maracas`。⇒ 仮説ではない。**

| # | リリース前に直す理由 |
|---|---|
| **★★★1** | **3 件が実在し、押せば壊れる** |
| **★★2** | **直しは数行であり `migrations/` を 1 バイトも触らない。⇒ いま入れる費用が最小である** |
| **★★★3** | **公開すると「利用者の DB に違反行が在りうる」が恒久の前提になる。⇒ 以後この不変条件へ機械的な歯止めを置けなくなる** |

**★実害は fail-closed である**〔`candidateStartup` が `nil` を返し候補から落ちる〕**。⇒ 出力は壊れない。★止めるほどではないが、直す費用のほうが小さい。**

**★★`M19-04` §4.5 の「`'through'` をリテラルで書く」設計判断とは衝突しない。⇒ 同判断は「ラッシュ版は通し値である」を述べたものであり、*通し値の元が無い*場合を扱っていない。★覆しではなく、未処理のケースである。**

---

## 4. ★★危険

### 4.1 ★★★`unknown` を「埋めるべき穴」と読むこと

**★`DES-003` §3.3 の `unknown` は三値目であり、未確定の印ではない。⇒ 本 MS は `unknown` へ*寄せる*方向であり、`unknown` を*減らす*方向ではない。**

**★★followup の `startup-basis-unknown-51-rows-mixed-provenance` は逆向きの行である**〔`is_derived=1` かつ `unknown` の 51 行〕**。⇒ 本 MS の射程外である。★巻き込まないこと。**

### 4.2 ★★CSV とマイグレを別コミットに分けること

**★`TestCSVAndDBAgreeOnFrameCostColumns` が CSV と DB の突合で不一致 0 件を固定している。⇒ CSV 6 行だけ先に直すと赤くなる。**

### 4.3 ★是正だけしてガードを置かないこと

**★§3.2。⇒ 直因はガードの版固定である。★そこを直さないと同じことが 3 度目に起きる。**

---

## 5. ★★投入の時機

**★★前提＝`M33-03` のマージ。⇒ 新系列 9 本が `migrations/` に在り、`go test ./...` が緑であること。**

**★★★`M39-02` は `M36-02` と*並列可*である**（2026-09-19 実査＝`D-913`）**。⇒ 交点 0 である。**

| サブ | 触る面 |
|---|---|
| **`M39-02`** | **`internal/repository/move/` ／ `internal/service/move/` ／ テスト** |
| **`M36-02`** | **`scripts/release-*` ／ `.github/workflows/` ／ `docs/usermanual/dist-readme.txt`**（同サブ指示書 §0.4 / §2 / §3 の実査） |

**★★ファイルの重なりが 0 である。⇒ どちらを先にマージしてもよい。★後にマージするほうが相手を取り込むこと。**

**★★★`M39-02` の前提は `M39-01` の `main` マージだけである。⇒ `M36-02` の着地を待たない。**

**★★以下は前版の記述：** `M36-02` は開発者が 2026-09-19 のこの手番で投入した。

| サブ | 交点 | 判定 |
|---|---|---|
| **`M36-02`**（リリースの門と来歴） | **SBOM とライセンス層の 1 点だけ** | **★★0 である。⇒ `000010_data_*.up.sql` は `REUSE.toml` の層 B グロブ `migrations/*_data_*.sql` に*既に*当たる。★層の宣言は 1 行も動かない** |

**★★★設計卓は当初「`M39-01` を*先に*入れること」を推した。⇒ その理由**〔`M36-02` は attestation と SBOM を作るため、データの形が動いた後に来歴を付け直すのは無駄である〕**は成立していなかった**（計測点 `M-207`）**。★attestation も SBOM も CI がリリース時に生成する*機構*であり、コミット済みの凍結物ではない。⇒ 「付け直す」対象が無い。**

**★★⇒ 順序はどちらでもよい。★決めるのは開発者である。**

---

## 6. 参照ドキュメント

| ID | パス | 参照する箇所 |
|----|------|------------|
| — | `docs/handover/followup-backlog.md` | **§DS** の `d187-startup-basis-invariant-violated-at-head` |
| — | `docs/process/parallel-board.md` | **`D-187`** ／ **`D-906`** (7) ／ **`D-909`**（本 MS の起票） |
| DES-003 | `docs/design/03-data-model.md` | **§3.3**（`startup_basis` の三値の述語） |
| SUPP-001 | `docs/design/supp-001-detailed-design.md` | **§5.5 規約 (19)**（三点更新） ／ **§5.5.5**（アップデート追従時の読み方） |
| — | `docs/change-notes/CHANGE-158-notification.md` | **層 B の命名**（`NNNNNN_data_*.sql`） |
| — | `docs/instructions/phase4-overview.md` | §3.1（残数の現在地） |

---

*以上、M39-overview **v1.4.0**。* **★★本 MS の落とし穴は「132 行を直すだけ」と読むことである。** **★直因はガードが版固定だったことであり、そこを直さないと同じことが 3 度目に起きる。** **★もう 1 つ、`unknown` へ寄せる方向と、`unknown` を減らす方向を取り違えないこと。**
