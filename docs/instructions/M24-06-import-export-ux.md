# 指示書 M24-06: 取込・出力の UX

| 項目 | 内容 |
|------|------|
| 文書ID | M24-06 |
| バージョン | 1.2.0 |
| 推奨モデル | **高精度実装**（**★★7 項目のうち 2 件が「監査」であり、面を絞らないと際限なく広がる**。実モデル名は `docs/human-notes/model-allocation.md` が正本） |
| Plan Mode | **必須**（**★実査 14 件を Plan Mode に含める**。CLAUDE.md §9＝**API 契約に触れうる変更**） |
| 機械レビュー | 必須（別チェックリスト: `M24-06-review-checklist.md`） |
| 参照してよい先行資料 | `DES-005` §5.13 / §5.13a / §5.14 / §5.19 ／ `DES-006` §5（取込 VAL） ／ `DES-002` §4.2 ／ `docs/handover/code-facts.md`（**★他サブの指示書・チェックリストは含めない**＝D-217） |
| 並列性 | **★`M24-13` / `M24-05` と並列可の見込み**（本サブは `/import` / `/export` 面）。**★実査で確かめること。★★`make e2e` は重ねられない**（`D-548`） |
| 依存 | **`M24-01` の完了**（済＝**D-553**。**★一覧のフィルタ/選択が出力対象の入口である**） |
| CHANGE | **`CHANGE-144` v1.2.0**（**設計卓が起票済み。製造は番号を消費しない**）。影響設計書＝**`DES-005` §5.13 / §5.13a / §5.14**（主対象） ／ **`DES-006` §5**（条件付き） ／ **`DES-002` §4.2**（条件付き） |
| マイグレ | **0 本の見込み**。**★消費するなら次は `000080`** |
| 想定所要時間 | 300〜390 分 |
| 作成者・作成日 | 設計担当 Claude（設計卓・M24 期）/ 2026-08-29 |

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| 1.2.0 | 2026-08-29 | **★★開発者の問いから、ファイル名の変更が取込を壊しうる面を 1 つ見つけた**（**D-592**）。 **★★`DES-002` §7.6 の逐語＝「export はコンボ CSV（`combos.csv`）+ セットプレイ CSV（`setups.csv`）の 2 ファイルを ZIP 1 個に同梱して配送する。import は…export した ZIP をそのまま受理して自動展開する（往復対称）」。** **⇒ ★★ZIP の中のエントリ名は契約である。自動命名を及ぼしてはいけない。** **★外側のファイル名（ダウンロードされる ZIP / CSV の名前）は取込に無関係な見込みだが、個別アップロードの見分け方が実査項目である**（§3.3-13 / -14）。**⇒ §4.4.1 を新設した。** **★あわせて確認事項 3 が決着した**——**既定は案 B、かつ自動生成値を初期値とした編集可能な欄にする**（§4.4）。 |
| 1.1.0 | 2026-08-29 | **★★設計卓が自分の読み取りの誤りを訂正した**（**D-591**・計測点 **`M-93`**）。 **★`SM-072` は「直すか畳むか決まっていない」のではなく、memo が「エクスポート画面の廃止（第2段）」で方向を確定させていた。** **★機序＝memo の行が長く、設計卓は前半（2026-07-21 注記）だけを読んで判断した。同じ行の後半（2026-08-11 注記）に「第2段は未実行」「迂回路ができただけで本件の解消ではない」と書いてあった。** **⇒ §4.3 の暫定案を「直す」から「廃止する」へ差し替え、確認事項 2 を「実査の結果しだいの分岐」へ改めた。** **★あわせて `SM-071` に「BE は弾かない」を明記した**（§4.1.1。**開発者の問い＝「何らかの原因でFE突破して入ってしまうなんてことはありませんよね？」への回答**）。 |
| 1.0.0 | 2026-08-29 | 初版（**D-590**） |

---

## 1. 背景と目的

### 1.1 扱う 7 項目——**6 クラスタ**

**ledger の正本は `docs/progress/M24-RESEARCH-01-report.md` §2 / §3 である。以下は要旨であり、原文は同報告と `docs/human-notes/Memo_Someday.txt` を見ること。**

**★★要約列は原文ではない**（`M-82`）。**確認事項を立てるときは原文へ当たること。**

| クラスタ | ID | memo の要求（要旨） | 面 |
|---|---|---|---|
| **A. 取込の入口** | **`SM-071`** | **インポートで仮登録を除外したい** | `/import` |
| | **`SM-076`** | **0 件ファイルのとき何も出ない**（**★プレビュー段階の話**） | `/import` |
| **B. 出力の対象指定** | **`SM-072`**（＝`SM-073` / `SM-077`） | **キャラ ID 入力・タグ ID 入力・コンボ ID 指定が使いにくすぎる** | `/export` |
| **C. 出力の後始末** | **`SM-075`** | **ファイル名に気を使う。自動で対象＋日付を付けたい** | `/export`・§5.13a |
| **D. 用語** | **`SM-110`** | **「エクスポート」「インポート」等がエンジニア向けの表現ではないか** | 横断 |
| **E. 確認ダイアログの監査** | **`SM-121`** | **色々な操作に確認ダイアログが不足していないか** | 横断 |
| **F. 技術的負債** | **`CO-023`**（＝`G-15`） | **`M17-04` 実装上の脆さ**（`ComboImportPage` の StrictMode 対策） | `/import` |

### 1.2 ★★本サブの最大の危険——**2 件の「監査」が際限なく広がる**

**`SM-110`（用語）と `SM-121`（確認ダイアログ）は、どちらも「全部を見直す」形の要求である。**

| ID | 原文の逐語 | 本サブでの扱い |
|---|---|---|
| **`SM-110`** | **「エクスポート、インポート等、エンジニア向けかもしれない表現を見直し。PCで仕事をする人には一般的かもしれないが、そうではない人向けには怪しい気がしてきた。★ただ私の感覚なので改めて検討は必要。」** | **★★候補を挙げるまで。語を確定させない。** **★原文が自ら留保している。★`M24-07`（用語・i18n）が新語彙を確定させる手番である**（`M24-overview` §3＝**UI レーン完了後に一括整合**） |
| **`SM-121`** | **「色々な操作に確認ダイアログが不足していないかチェックが必要。削除、更新、非表示（永続、一時的なものは除く）、インポート、エクスポート、取込等。何も出ないと事故や、動作していないのでは？と誤解しそうな部分は対応が必要」** | **★★監査は本サブで行う。ただし面は取込・出力に限る。** **★是正も取込・出力の面のものだけ。他面で見つけたものは是正せず報告する** |

**★★面を絞らないと `M24-06` が全画面監査になる。⇒ §1.6-4 / §1.6-5 で明示的に切った。**

### 1.3 ★★`SM-072` の方向は memo が確定させている——**「エクスポート画面の廃止（第2段）」である**

**memo の逐語（line 155 本文）＝「エクスポート機能、現状のキャラID入力、タグID入力、コンボID指定は使いにくすぎるので修正が必要」**

**★注記の逐語（2026-07-21）＝「M17-05 棚卸し A-3（対象選択の改善＋エクスポート画面の段階廃止）。A-1（一覧/マイコンボからの選択エクスポート）は M17-05b 完了（2026-07-18）＝一覧のフィルタ/選択がそのまま対象指定に。★エクスポート画面自体の廃止は第2段（実態確認後・CHANGE対象）」**

**★★注記の逐語（2026-08-11）＝「M17 はクローズしたが第2段は未実行 — `ComboExportPage` は router(`/export/combo`)・ヘッダのナビ項目とも健在で、ID 入力の経路は残っている（M17-05b の完了報告も『`ComboExportPage.tsx` は差分ゼロ』と明記）。★★迂回路ができただけで本件の解消ではない」**

**★`SM-073`（line 157）の注記（2026-08-11）＝「一覧/マイコンボからの選択エクスポートで実質的な代替はできたが、★エクスポート画面本体は未改修のまま残存」**

**★`SM-077`（line 163）の注記（2026-08-11）＝「未実施＝CSV 契約（`DES-002` §7.6）は不変、ファイル名（…）、★エクスポート画面の除去（…）」**

**⇒ ★★3 行が揃って「エクスポート画面の廃止」を未実施分として指している。方向は確定している。**

> **★★設計卓は v1.0.0 で「直すか畳むかが決まっていない」と書いた。誤りである**（計測点 `M-93`）。**★機序＝memo の行が長く、前半（2026-07-21 注記）だけを読んで判断した。同じ行の後半に答えが書いてあった。**

**★★ただし memo 自身が条件を付けている＝「実態確認後・CHANGE対象」。**

**⇒ 順序は「実態確認 → 廃止 → CHANGE」である。** **★実態確認とは「`/export/combo` でしかできないことが無いか」を数えること**（§3.3-5）。**★`CO-002` と同じ手順である**（`D-587` / `D-588`＝**消す前に母集団が同じことを確かめる**）。

**★暫定案＝廃止する**（§4.3）。**★★§11 確認事項 2 は「実査で『しかできないこと』が見つかった場合にどうするか」である。**

### 1.4 ★★起票時に設計卓が確かめていないこと

**設計卓は実装ソースを読めない。★以下の面の指定はすべて実査項目である**（§3.3）。**★「ある」も「無い」も断定していない。**

| 対象 | 分からないこと |
|---|---|
| 取込プレビュー | **★仮登録の行がどう表示されているか**（ledger は「表示列があるだけ」と書いている）。**★除外 UI・フィルタが本当に無いか** |
| 0 件ファイル | **★プレビュー段階で現在どうなるか** |
| `/export` | **★対象指定の現在の形。★`/export` でしかできないことが在るか** |
| ファイル名 | **★現在どう決まっているか** |
| `ComboImportPage` | **★`CO-023` の StrictMode 対策が入っているか** |

### 1.5 目的

**「取込・出力が使いにくい」を潰す。★ただし「監査」の 2 件は面を絞る。**

### 1.6 ★★このサブで作らないもの（スコープ外）

| # | 作らないもの | 理由 |
|---|---|---|
| **1** | **★★`SM-110` の語を確定させること** | **★原文が「ただ私の感覚なので改めて検討は必要」と留保している。★`M24-07` の手番**（§1.2） |
| **2** | **`M17-05b` で入った「一覧のフィルタ/選択がそのまま対象になる」導線の作り替え** | **★済んでいる**（`CHANGE-072`）。**★本サブは `/export` 側を直す** |
| **3** | **★取込を実行した後のトースト**（成功N/スキップN/エラーN） | **★`M17-05a` の B-5 で入っている。★`SM-076` はプレビュー段階の話である**（§4.2） |
| **★★4** | **★★取込・出力以外の面の確認ダイアログの是正** | **★★`SM-121` の監査は行うが、是正は取込・出力の面だけ。他面は報告に留める**（§1.2 / §4.6） |
| **★5** | **★全画面の用語 sweep** | **★`M24-07` の手番**（§1.2） |
| **6** | **CSV / PDF / PNG の出力形式そのものの変更** | **★`M17-05c` の成果物。★本サブは対象指定とファイル名である** |
| **7** | **取込の VAL（`VAL-I` 系）の判定内容の変更** | **★★確認事項 1 で案 B（BE で除外）を採った場合のみ触れる。★暫定案 A では触れない** |
| **8** | **レシピの内部表現・`recipe_cache`** | **契約 F-1 の凍結パッケージ**（`internal/service/notation/` / `internal/service/preset/`） |
| **9** | **`queryKey` の綴りの統一** | `M24-08` の手番 |

---

## 2. 成果物

### 2.1 作成/修正するファイル

**★本一覧は「予見できた範囲」であって禁止列ではない。一覧に無いファイルでも §2.2 の禁止列に当たらないなら触ってよい。ただし触った理由を完了報告に書くこと。**

**★★以下の「面」は §3.3 の実査で確定する。設計卓は現物を読めないため、断定形で指定していない。**

| ファイル | 内容 |
|---|---|
| **取込画面**（`ComboImportPage.tsx` 相当） | §4.1（仮登録の除外） ／ §4.2（0 件ファイル） ／ §4.7（`CO-023`） |
| **出力画面**（`/export` 相当） | §4.3（対象指定） ／ §4.4（ファイル名） |
| **出力ダイアログ**（§5.13a 相当） | §4.4（ファイル名） |
| **`web/src/locales/{ja,en}.json`** | 新規キーのみ。**★既存キーの整理はしない**（§1.6-5） |
| **テスト**（純粋関数・コンポーネント・E2E） | §5 |
| **モック HTML** `docs/progress/M24-06-mock/` | §4.8 |
| **完了報告** `docs/progress/M24-06-completion-report.md` | §7.5 |
| **設計伝達レポート** `docs/handover/design-reports/2026MMDD-m24-06-design-exceptions.md` | `/design_handover_report` |

### 2.2 変更しないもの（原則）

| 対象 | 何を守るか | 根拠 |
|---|---|---|
| **`internal/service/notation/` ／ `internal/service/preset/`** | **契約 F-1 の凍結パッケージ** | M21 契約 |
| **★取込を実行した後のトースト**（成功N/スキップN/エラーN） | **★`M17-05a` の B-5 の成果物** | §1.6-3 |
| **★`M17-05b` の「一覧のフィルタ/選択がそのまま対象」導線** | **★済んでいる** | §1.6-2 |
| **★出力形式の中身**（CSV / PDF / PNG / クリップボード） | **★`M17-05c` の成果物** | §1.6-6 |
| **★★取込の VAL（`VAL-I` 系）の判定内容** | **★確認事項 1 で案 B を採ったときのみ触れる** | §1.6-7 |
| **★`DES-005` §5.19（取込ヘルパー）の連携口** | **★`CHANGE-071` の契約。★`/import` が router state で受け取る形を壊さない** | `DES-005` §5.14 の冒頭 |
| **★`check-*.sh` の判定ロジック** | **★検査を緩めて通さない** | `M24-01` §7-7 |

### 2.3 例外条項

**§2.2 の「`internal/` を触らない」を破ってよいのは、次の 3 条件がすべて成立するときだけである。**

1. **フロントの変更だけでは要求を満たせないことを、実測で示せる**（**★確認事項 1 で案 B を採った場合が想定される**）。
2. **`DES-002` §4.2 の経路表に載る既存 API の挙動を変えない**（新設のみ。既存の変更なら止めて報告）。
3. **設計卓へ報告し、`CHANGE-144` の射程へ `DES-002` §4.2 と `DES-006` §5 を加える判断を仰ぐ。**

**★3 条件のどれか 1 つでも欠けたら、直さずに報告すること。**

### 2.4 ★並列性

**★`M24-13` / `M24-05` と触る面は交わらない見込み**（本サブは `/import` / `/export`）。**★実査で確かめること**（§3.3-12）。

**★★`make e2e` は重ねられない**（`D-548`）。**★各回の実行直前にポートが空いていることを確認し、その出力を完了報告へ載せること。**

---

## 3. 前提条件

### 3.1 必読ドキュメント

- **★★`DES-005` §5.14**（インポート。**★入口一本化＝`CHANGE-075`/B-1。★セットプレイ CSV は従属**）
- **`DES-005` §5.13 / §5.13a**（エクスポート ／ 一覧・マイコンボからのダイアログ）
- **★`DES-005` §5.19**（取込ヘルパー。**★`/import` への連携口の契約**）
- **`DES-006` §5**（取込の VAL。**★`VAL-I05` の正準形の検証が在る**）
- `docs/handover/code-facts.md`

### 3.2 参照不要

- 他サブの指示書・チェックリスト（`D-217`）
- `DES-003` / `DES-004`（スキーマにもレシピの内部表現にも触れない見込み）

### 3.3 ★★着手前の確認（Plan Mode 必須）

**★以下はすべて実査項目である。§1.1 / §1.3 / §1.4 に書いた内容も含めて、設計卓が現物で裏を取ったものは 1 つも無い。**

**★★実査は 2 系統ある**——**(I) その症状が現在も成立するか** ／ **(II) 触る面がどこか。**

**★★「N 個ある」と書かれた列挙は、その N を数え直すこと**（playbook §4.40）。

| # | 確かめること | 系統 | なぜ要るか |
|---|---|---|---|
| **1** | **★★取込プレビューで仮登録の行が現在どう扱われているか**（**★ledger は「表示列があるだけで除外 UI・フィルタは無い」と書いている。裏取り**） | **I** | **★§4.1。★既に除外できるなら実装せず成立を固定して報告する** |
| **2** | **★★取込 API が `is_draft` をどう扱っているか**（**★弾いているか、そのまま取り込むか**） | **II** | **★§4.1。★確認事項 1（FE / BE のどちらで除外するか）の材料である** |
| **3** | **★0 件ファイルをプレビューしたときに現在どうなるか**（**★何も出ないのか、出るが分かりにくいのか**） | **I** | **★§4.2。★★「何も出ない」と「出ているが気づけない」で手が違う** |
| **4** | **★`/export` の対象指定の現在の形**（**★キャラ ID 入力 ／ タグ ID 入力 ／ コンボ ID 指定がそれぞれどういう UI か**） | **I** | **★§4.3** |
| **★★5** | **★★`/export` でしかできないことが在るか**（**★§5.13a のダイアログと母集団・できることを突き合わせる。母数付きで**） | **I** | **★★確認事項 2（直すか畳むか）の材料である。** **★`CO-002` の先例＝「役割が被っているから消す」は消す前に母集団が同じことを確かめてからである** |
| **6** | **★エクスポートのファイル名が現在どう決まっているか**（**★固定名か、利用者入力か、形式ごとに違うか**） | **I** | **★§4.4** |
| **7** | **★★取込・出力の面で確認ダイアログが無い操作の全数**（**母数付き**）。**★「無いと事故になる」ものと「無くてよい」ものを分けること** | **I** | **★§4.6。★★面を取込・出力に絞ること。他面は数えなくてよいが、目に入ったものは報告する** |
| **8** | **★取込・出力まわりで利用者に見えている用語の全数**（**母数付き**） | **I** | **★§4.5。★★語を変えないこと。候補を挙げるだけである** |
| **9** | **★`ComboImportPage` に `CO-023` の StrictMode 対策が入っているか**（`setTimeout(0)` ＋ cleanup ＋ ref ＋ `previewM.data` 購読） | **II** | **★§4.7。★入っていれば実装せず成立を固定して報告する** |
| **10** | **★★`DES-005` §5.19 の連携口（取込ヘルパー → `/import` の router state）が、通常の取込と同じプレビュー画面を通るか** | **II** | **★★§4.1.1。通らないなら仮登録のフィルタが効かない経路が 1 本残る。⇒ 報告すること。** **★あわせて連携口を壊していないか確かめる手段（E2E など）が在るか**（§2.2。**★壊すと取込ヘルパーからの導線が切れる**） |
| **★★13** | **★★ZIP の中のエントリ名を取込側が何で探しているか**（**名前一致か ／ 拡張子か ／ 順序か**） | **II** | **★★§4.4.1。`DES-002` §7.6 は `combos.csv` / `setups.csv` と名前を明示している。⇒ 名前で探しているなら契約であり、自動命名を及ぼしてはいけない** |
| **★★14** | **★★個別アップロードで combos / setups をファイル名で見分けているか、入力欄で見分けているか** | **II** | **★★§4.4.1。★見込みは「入力欄」である**（`DES-005` §5.14 が欄を 2 つに分けている）。**⇒ 名前で見分けているなら、自動命名が取込を壊す。止めて報告すること** |
| **11** | **★出力の E2E spec の全数**（**母数付き**） | **II** | **★§5.2。★ファイル名を変えると落ちる spec が在りうる** |
| **12** | **★`M24-13` / `M24-05` と触るファイルが交わらないか** | **II** | §2.4 |

---

## 4. 詳細仕様

### 4.1 ★★`SM-071`——**取込で仮登録を除外する**

| 論点 | 決めたこと |
|---|---|
| **★除外する場所** | **★★暫定案＝FE**（取込プレビューでフィルタし、送らない）。**★§11 確認事項 1** |
| **★見せ方** | **★★除外した件数を出すこと。** **★黙って減らさない**——**「取り込んだつもりの行が入っていない」が起きる** |
| **★案 B（BE で除外）を採る場合** | **★★§2.3 の 3 条件を満たし、設計卓へ報告すること。★`DES-006` §5 の取込 VAL に触れる** |
| **★既に除外できたら** | **★実装せず、成立をテストで固定して報告する**（§9.3） |

#### 4.1.1 ★★FE で除外する形の限界を明記する（**開発者の問いへの回答・2026-08-29**）

**開発者の問い＝「何らかの原因でFE突破して入ってしまうなんてことはありませんよね？」**

**★★答え＝突破されうる。** **FE で除外する形は「送らない」だけであり、BE は `is_draft` の行を弾かない。**

| 経路 | 入るか |
|---|---|
| **通常の取込画面** | **★入らない**（FE がフィルタする） |
| **★API を直に叩く** | **★★入る** |
| **★FE のフィルタの条件漏れ** | **★★入る**（**⇒ 純粋関数テストで固定する**＝§5.1） |
| **★★取込ヘルパー（`/import/combo/helper`）からの連携** | **★★同じプレビュー画面を通るなら入らない。通らないなら入る。⇒ §3.3-10 で確かめること** |

**★★害の形＝仮登録として DB に行が増えるだけである。データは壊れず、利用者は削除できる。**

**⇒ ★この形を採るのは「不変条件を守る」ためではなく「うっかり混ざるのを防ぐ」ためである**（開発者裁定 2026-08-29）。

> **★★`M24-11` / `M24-13` と混同しないこと。** **あちらは「FE で塞いでも BE が守らないと意味がない」不変条件の話である**（`VAL-C02` の check-then-act ／ `VAL-C09` の適用範囲）。**本項は不変条件ではない。**
>
> **★★もし「絶対に入れない」が要件になったら、それは BE で弾く形（案 B）であり `DES-006` §5 の取込 VAL に触れる。⇒ 止めて報告すること。**

**★完了報告に「BE は弾かない」ことを明記すること**（§7.1-2）。

---

### 4.2 ★`SM-076`——**0 件ファイルのとき何も出ない**

**★★プレビュー段階の話である**（memo の 2026-08-11 注記）。**★取込を実行した後のトーストは触らない**（§1.6-3）。

| 論点 | 決めたこと |
|---|---|
| **形** | **★0 件だったことが読めるようにする**（**★文言は実装判断**） |
| **★★見分けること** | **★★「何も出ない」のか「出ているが気づけない」のかを §3.3-3 で確かめること。★手が違う** |
| **★0 件の定義** | **★実査で確かめること**（**ファイルが空 ／ ヘッダだけ ／ 全行がエラー**）。**★どれを 0 件とみなすかを報告すること** |

### 4.3 ★★`SM-072`（＝`SM-073` / `SM-077`）——**エクスポート画面を廃止する（第2段）**

| 論点 | 決めたこと |
|---|---|
| **★★暫定案** | **★★`/export/combo`（`ComboExportPage`）を廃止する。** **★memo の 3 行が揃って未実施分として指している**（§1.3） |
| **★★★先に実態確認を行うこと** | **★★memo 自身が「実態確認後・CHANGE対象」と条件を付けている。** **⇒ §3.3-5 で「`/export/combo` でしかできないこと」を母数付きで数えること。★`CO-002` と同じ手順である**（`D-587` / `D-588`） |
| **★★「しかできないこと」が見つかったら** | **★★止めて報告すること**（§9.1）。**★勝手に廃止しない。★勝手に §5.13a 側へ移植もしない**——**移植は「廃止」ではなく「機能追加」であり、射程が変わる** |
| **★廃止するもの** | **★ルート `/export/combo` ／ ヘッダのナビ項目 ／ `ComboExportPage`**（**★§3.3-4 / -5 で全数を確かめてから**） |
| **★残るもの** | **★§5.13a のダイアログ**（一覧・マイコンボからの動線＝`CHANGE-072` / `M17-05b`）。**★★触らない**（§1.6-2） |
| **★キャラ指定** | **★★`SM-073` の原文はキャラ指定を必須にしないことを求めている。** **⇒ 廃止後も §5.13a 側でキャラを絞れる必要があるかを §3.3-5 の「しかできないこと」として数えること** |
| **★`DES-005` §5.13 の扱い** | **★★節の廃止は設計卓が反映時に行う。★製造は設計書を編集しない**（§7.4.1-4） |

### 4.4 ★`SM-075`——**ファイル名の自動命名**

**memo の逐語＝「エクスポート時のファイル名に気を使う。自動でエクスポート対象＋日付とか付ける？いまのままだと連続エクスポート時に名前を一々買えないといけないのでしんどい」**（**★「買えない」は「変えられない」と読める**）。

| 論点 | 決めたこと |
|---|---|
| **★要求** | **★★連続エクスポートでファイル名が衝突しない形にすること。これが本項の核心である** |
| **★★規則** | **★★確定（2026-08-29・`D-592`）＝`combos_{対象の種別}_{件数}_{YYYYMMDD-HHmmss}.{拡張子}`。** **例＝`combos_filtered_42_20260829-143512.zip`** |
| **★対象の種別** | **★`DES-005` §5.13 の 4 値に対応する英字**〔**全コンボ ／ フィルタ中 ／ 選択中 ／ マイコンボ**〕。**★★日本語をファイル名に入れないこと**——**OS・ツールによって化ける** |
| **★件数を入れる理由** | **★★連続出力で「新しいほう」は日時で分かるが「目的のほう」が分からない。件数は 1 つ足すだけでそれが分かる**（開発者裁定 2026-08-29） |
| **★適用面** | **★`/export` と §5.13a のダイアログの両方**（**★片方だけだと作法が割れる**） |
| **★★利用者が変えられる形** | **★★自動生成値を初期値として入れた「編集可能な欄」にする**（2026-08-29・`D-592`）。**★「毎回入力させる」でも「後で OS 上で変えてもらう」でもない** |
| **★★その形を採る理由** | **★★ブラウザのダウンロードは既定で「ダウンロードフォルダへ即保存」であり、「名前を付けて保存」ダイアログが出るかは利用者のブラウザ設定に依る。** **⇒ 「後で変えてもらう」は結局ファイルを探して改名する作業になり、memo の「一々変えないといけないのでしんどい」がそのまま残る。** **★「毎回入力させる」は手間が増え、memo の要求と逆行する** |
| **★作法の一致** | **★★`SM-088`（セットプレイ名）と同じ形である**——**空なら自動で埋め、入力済みなら尊重する。⇒ プロジェクト内で作法が揃う** |

#### 4.4.1 ★★★ZIP の中のエントリ名は契約である。自動命名を及ぼしてはいけない

**開発者の問い＝「ファイル名は import には無関係だったはずですが、名称変更によって取り込めなくなったりしないかは調査したいです。」**

**★★設計書に答えの一部が在る。`DES-002` §7.6 の逐語:**

> **「配送コンテナ（CHANGE-051）：export はコンボ CSV（`combos.csv`）+ セットプレイ CSV（`setups.csv`）の 2 ファイルを ZIP 1 個に同梱して配送する（`archive/zip`・in-memory 生成でサーバ側ディスク一時ファイルを作らない）。import は 2 CSV の個別アップロードに加え、★export した ZIP をそのまま受理して自動展開する（往復対称）。契約上の「別ファイル（2 CSV）」は ZIP 内で維持される（ZIP は配送コンテナであって CSV 統合ではない）。」**

**⇒ ★★名前が関わる層は 2 つある。混同しないこと。**

| 層 | 中身 | 自動命名を及ぼすか |
|---|---|---|
| **外側**（利用者がダウンロードするファイルの名前） | 例＝`combos_filtered_42_20260829-143512.zip` | **★★及ぼす。これが `SM-075` の対象である** |
| **★★内側**（ZIP の中のエントリ名＝`combos.csv` / `setups.csv`） | **`DES-002` §7.6 が名前を明示している。★契約である** | **★★★及ぼしてはいけない。** **変えると「export した ZIP をそのまま受理して自動展開する」往復対称が壊れる** |

**★★これが本項の最大の落とし穴である。** **「ファイル名を自動生成する」を素直に実装すると、ZIP の中身まで改名しかねない。**

**⇒ ★実装は「外側だけ」に及ぼすこと。★★ZIP 内のエントリ名は `combos.csv` / `setups.csv` のまま固定する。**

**★★あわせて 2 つを実査すること**（§3.3-13 / §3.3-14）——**(a) ZIP 内のエントリ名を何で探しているか**（名前一致か、拡張子か、順序か） ／ **(b) 個別アップロードで combos / setups をファイル名で見分けているか、入力欄で見分けているか。**

> **★(b) の見込み＝入力欄で見分けている。** **`DES-005` §5.14 が「コンボCSVファイル（必須）」「セットプレイCSVファイル（任意・従属化）」と欄を 2 つに分けており、送信前ガードも `comboFile` の有無で判定していると書いている。⇒ 外側のファイル名は無関係な見込みである。**
>
> **★★ただし設計卓は現物を読めない。断定していない。⇒ 実査で確かめ、名前で見分けているなら止めて報告すること**（§9.1）。

**★往復の証拠を残すこと**——**自動命名を入れたあとに「export した ZIP をそのまま import できる」ことを E2E で固定する**（§5.1）。

---

### 4.5 ★★`SM-110`——**候補を挙げるまで。語を決めない**

**memo の逐語＝「…★ただ私の感覚なので改めて検討は必要。」**

| 論点 | 決めたこと |
|---|---|
| **★★やること** | **★★取込・出力まわりの用語を全数挙げ**（§3.3-8。母数付き）、**「エンジニア向けに見える」候補に印を付けて報告するだけ** |
| **★★やらないこと** | **★★語を変えること。** **★`M24-07`（用語・i18n）が新語彙を確定させる手番である** |
| **★報告の形** | **★用語 ／ 出ている面 ／ 言い換えの候補（出せた場合のみ）の 3 列で並べること** |

### 4.6 ★★`SM-121`——**監査は本サブ。是正は取込・出力の面だけ**

**memo の逐語＝「色々な操作に確認ダイアログが不足していないかチェックが必要。削除、更新、非表示（永続、一時的なものは除く）、インポート、エクスポート、取込等。何も出ないと事故や、動作していないのでは？と誤解しそうな部分は対応が必要」**

| 論点 | 決めたこと |
|---|---|
| **★監査の面** | **★★取込・出力に限る**（§3.3-7）。**★他面で目に入ったものは報告に留め、是正しない**（§1.6-4） |
| **★★分けること** | **★★「無いと事故になる」ものと「無くてよい」ものを分けること。** **★原文が「永続、一時的なものは除く」と自ら除外を書いている。⇒ 一律に足さない** |
| **★是正の形** | **★共有プリミティブのダイアログを通すこと**（規則9。**自作オーバーレイは物理入力抑止に参加しない**） |
| **★★「動作していないのでは？と誤解しそう」** | **★★これは確認ダイアログとは別の手当てでよい**（トースト・進捗表示）。**⇒ 一律にダイアログにしない** |

### 4.7 ★`CO-023`（＝`G-15`）——**`M17-04` 実装上の脆さ**

| 論点 | 決めたこと |
|---|---|
| **★扱う** | **★★`SM-071` と同じファイル（`ComboImportPage`）を触るため、同じサブで扱う**（**★§11 確認事項 4**） |
| **★形** | **★followup §G の逐語どおり**——**`setTimeout(0)` ＋ cleanup ＋ ref ＋ `previewM.data` 購読**（**dev の二重マウントで `useMutation` の per-call `onSuccess`/observer が落ちて `isPending` が固まる既知事象への標準対処**） |
| **★既に入っていたら** | **★実装せず、成立をテストで固定して報告する**（§3.3-9） |

### 4.8 ★モック HTML（`M24-overview` §1.5）

**次の 2 つについて、変更前と変更後を並べた単一 HTML を `docs/progress/M24-06-mock/` へ置くこと。**

1. **★`/export` の対象指定の前後**（§4.3）
2. **★取込プレビューの仮登録の除外 ＋ 0 件ファイルの見え方**（§4.1 / §4.2）

**★★提示のタイミングは「承認直後・コード変更前」でよい**（followup `mock-presentation-timing-vs-plan-mode`）。**★作り込まないこと。**

**★§3.3 の実測で「既に解消していた」と分かった項目は、その項目のモックは「現状の写し」1 枚でよい。**

---

## 5. テスト要件

### 5.1 追加するテスト

| 層 | 内容 |
|---|---|
| **純粋関数** | **★必須**。**ファイル名の組み立て**（§4.4。**★連続で呼んで衝突しないこと**） ／ **取込プレビューの仮登録フィルタ**（§4.1） |
| **コンポーネント** | **★0 件ファイルのときに 0 件だと読めること**（§4.2） ／ **★除外件数が出ること**（§4.1） |
| **E2E** | **★★4 ケース**——**(1) 仮登録を含む CSV を取り込むと仮登録が除外され、件数が出ること ／ (2) 0 件ファイルをプレビューすると 0 件だと分かること ／ (3) 連続でエクスポートしてファイル名が衝突しないこと ／ ★★(4) 自動命名で出力した ZIP をそのまま取り込めること**〔**往復対称の証拠。§4.4.1**〕 |

**★E2E の共通の下ごしらえは `web/e2e/support/` へ寄せること**（`D-553`）。

**★★`getByRole` の `name` は既定で部分一致である**（`M24-03` の教訓）。**⇒ 文言の破壊確認は「追記」ではなく「置換」で行うこと。**

**★★`toHaveCount` / `toHaveAttribute` は隠れた要素にも通る**（`M24-12` の教訓）。

### 5.2 既存テストの扱い

- **★★ファイル名を変えると落ちる spec を数え**（§3.3-11。母数付き）、**書き換えること。★検証内容を緩めないこと。**
- **★`M17-05a` / `M17-05b` / `M17-05c` の spec を落とさない。**
- **★`DES-005` §5.19（取込ヘルパー）の連携口を守る spec を落とさない**（§3.3-10）。

### 5.3 ★★破壊確認（**5 件・必須**）

**★破壊確認を書く前に 3 つを確かめること**（playbook §4.32）——**(a) 壊す対象が実在するか** ／ **(b) その壊し方で本当に観測が変わるか** ／ **(c) ★期待経路は「その観測が何を見ているか」から立てること。**

**★★クラスを足す形の壊し方は避けること**（`M24-12` の教訓＝**jsdom は Tailwind を読まない**）。**⇒ 構造を変える側で選ぶ。**

| # | 壊すもの | 期待 | 同じ層で書けているか |
|---|---|---|---|
| **1** | **★仮登録のフィルタを外す** | **★E2E (1) と純粋関数テストが赤くなる** | **★どちらもフロント側** |
| **2** | **★除外件数の表示を外す** | **★コンポーネントテストが赤くなる** | 同上 |
| **3** | **★0 件ファイルの表示を外す** | **★E2E (2) とコンポーネントテストが赤くなる** | 同上 |
| **★4** | **★★ファイル名から日時を外す**（固定名へ戻す） | **★E2E (3) と純粋関数テストが赤くなる** | 同上。**★★これが赤くならないなら、衝突しないことはどこにも守られていない** |
| **★★5** | **★★ZIP 内のエントリ名にも自動命名を及ぼす**（`combos.csv` → `combos_filtered_42_....csv`） | **★★E2E (4) が赤くなる** | 同上。**★★これが赤くならないなら、往復対称は守られていない** |

**★赤くなる経路を数えること**——**赤くならない経路があるなら、その経路は守られていない。**

---

## 6. レビュー観点（別ファイル参照）

`docs/instructions/reviews/M24-06-review-checklist.md`

---

## 7. 完了条件（Definition of Done）

### 7.1 機能要件

1. **§3.3 の実査 12 件が実施され、結果が報告されている**（**★母数の要る項目は母数付きで**＝§3.3-5 / -7 / -8 / -11）。
2. **§4.1 の仮登録の除外が動き、除外件数が出ている**（**★どこで除外したかが報告されている**）。**★★あわせて「BE は弾かない」ことと、§3.3-10 の連携口が同じフィルタを通るかが完了報告に明記されている**（§4.1.1）。
3. **§4.2 の 0 件ファイルが読める**（**★「0 件」の定義が報告されている**）。
4. **★★§4.3 の実態確認が行われ、`/export/combo` でしかできないことの母数が報告されている**（§3.3-5）。**★「無い」なら廃止されている。★「在る」なら廃止せず報告されている。**
5. **★★§4.4 のファイル名が連続エクスポートで衝突しない**（**★`/export` と §5.13a の両方**）。**★自動生成値が初期値として入り、その場で直せる。**
5b. **★★§4.4.1 の ZIP 内のエントリ名が `combos.csv` / `setups.csv` のまま固定されている**（**★自動命名を及ぼしていない**）。**★§3.3-13 / -14 の実査結果が報告され、自動命名で出した ZIP をそのまま取り込めることが E2E で固定されている。**
6. **★★§4.5 の用語が全数挙げられ、候補に印が付いている**（**★★語を変えていないこと**）。
7. **★★§4.6 の確認ダイアログの監査が取込・出力の面で行われ、「無いと事故になる」ものと「無くてよい」ものが分けられている**（**★他面は報告に留めていること**）。
8. **§4.7 の `CO-023` が扱われている**（**★既に入っていたら成立を固定して報告**）。
9. **★§4.8 のモック HTML が提示されている。**
10. **★`DES-005` §5.19 の連携口が壊れていない**（§3.3-10）。

### 7.2 自己テスト結果

- `go test ./... -count=1`（**★本サブは Go を触らない見込みなので、着手前と同じであることの対照**）
- `cd web && pnpm test`
- `make e2e`（**★実行直前にポートが空いていることを確認し、その出力も載せる**）
- **破壊確認 5 件**（§5.3）。**実施コマンドと出力を載せること。**

**★★基準値は「変更を入れる前」に、切らずに採ること**（**★採取コマンドにパイプで切り詰めを入れない**）。

**★★母数は「指示書に書いてあるから使える」ものではない**（playbook §4.37）。**⇒ 比較は同一セッション内の背中合わせで採ること。**

### 7.3 品質チェック

- `bash scripts/check-artifact-integrity.sh`（**★1 本目に回す**）
- `bash scripts/check-md-emphasis.sh` ／ `bash scripts/check-doc-refs.sh` ／ `bash scripts/check-progress-log-index.sh` ／ `bash scripts/check-enum-sync.sh` ／ `bash scripts/check-browser-storage-keys.sh`
- **★`check-progress-log-index.sh` は偽の緑を返しうる**（followup §AH）。**緑が出ても索引行を目で確かめること。**

### 7.4 ドキュメント

- **`docs/progress/progress-log.md` への追記**（製造担当が行う。`CLAUDE.md` §8）。**指示書が明示的に求めていなくても追記する。**

#### 7.4.1 ★CHANGE 要否の判定（**as-built で判定する**）

**`CHANGE-144` は起票済みである。** **★以下は「射程が広がったか」の判定である。1 件ずつ判定し、結果を完了報告に書くこと。**

| # | 条件 | 該当したら |
|---|---|---|
| **1** | **★`DES-002` §4.2 の経路表に載る API を新設・変更したか**（**★確認事項 1 で案 B を採った場合**） | 設計卓へ報告。`CHANGE-144` の射程へ加える |
| **2** | **★`DES-006` §5 の取込 VAL に触れたか** | 同上 |
| 3 | `DES-003` のスキーマに触れたか | 同上 |
| **4** | **★`DES-005` §5.13 を廃止したか**（**確認事項 2 で畳む案を採った場合**） | **★★同上。★節の廃止は設計卓の判断である** |
| **5** | **★`SUPP-001` の契約を持つ節に触れたか** | 同上（**`D-564`**） |
| **6** | **★ブラウザストレージのキーを新設・変更したか** | **`web/CLAUDE.md` §1 台帳へ登録し、`DES-005` へも書く** |
| **7** | **★マイグレーションを消費したか**（見込みは 0 本） | **★消費したらボード §2.2 の連番セルの更新が要る。設計卓へ報告** |

### 7.5 完了報告

`docs/progress/M24-06-completion-report.md`。**★工程順として「まだ無い」ものを過去形で書かないこと。**

**★★件数を本文へ書くときは、その場で数え直したコマンドを併記すること。**

**★★as-built を書く節は、指示書からの転記ではなく実装かテストを見て書くこと**（playbook §4.43）。

**★★指示書が挙げた要求を実装しないと決めたら、それは「判断」ではなく「逸脱」として報告すること**（playbook §4.42）。

---

## 8. 参照ドキュメント

| ID | パス | 用途 |
|---|---|---|
| `DES-005` §5.14 | `docs/design/05-screen-design.md` | **★★インポート。主対象** |
| `DES-005` §5.13 / §5.13a | 同上 | エクスポート ／ ダイアログ |
| `DES-005` §5.19 | 同上 | **★取込ヘルパーの連携口。★壊さない** |
| `DES-006` §5 | `docs/design/06-validation.md` | **取込の VAL。★`VAL-I05` の正準形の検証** |
| `DES-002` §4.2 | `docs/design/02-architecture.md` | 条件付き（§2.3） |
| `code-facts.md` | `docs/handover/code-facts.md` | 実装の索引 |
| ledger | `docs/progress/M24-RESEARCH-01-report.md` §2 / §3 | **7 項目の索引。★★要約列は原文ではない**（`M-82`）。**★原文は `docs/human-notes/Memo_Someday.txt`** |
| `CHANGE-144` | `docs/change-notes/CHANGE-144-notification.md` | 本サブの通知書 |

---

## 9. 注意事項・判断に迷ったら

### 9.1 推測で進めてはいけない事項

- **★★`SM-110` の語を変えること**（§4.5。**候補を挙げるまで**）。
- **★★取込・出力以外の面の確認ダイアログを是正すること**（§4.6）。
- **★★`/export/combo` でしかできないことが在るのに廃止すること**（§4.3。**★止めて報告する**）。
- **★★「しかできないこと」を §5.13a 側へ勝手に移植すること**（§4.3。**★移植は「廃止」ではなく「機能追加」であり射程が変わる**）。
- **★★FE の除外を「BE も弾く」と書くこと**（§4.1.1。**★BE は弾かない**）。
- **★★★ZIP の中のエントリ名（`combos.csv` / `setups.csv`）を変えること**（§4.4.1。**`DES-002` §7.6 の契約であり、往復対称が壊れる**）。
- **★個別アップロードがファイル名で見分けていると分かったのに、自動命名を及ぼすこと**（§4.4.1。**★止めて報告する**）。
- **★取込の VAL（`VAL-I` 系）の判定内容を変えること**（§1.6-7。**★§2.3 の 3 条件を満たすときのみ**）。
- **★取込を実行した後のトーストを変えること**（§1.6-3）。
- **★`DES-005` §5.19 の連携口を壊すこと**（§2.2）。
- **★出力形式の中身を変えること**（§1.6-6）。
- **`internal/` を触ること**（§2.3 の 3 条件をすべて満たすときのみ）。
- **新規依存の追加**（`CLAUDE.md` §6）。
- **ブラウザストレージの新キーを台帳へ登録せずに使うこと**（`CLAUDE.md` §10.X）。

### 9.2 推測で進めてよい事項（その旨を明示）

- **0 件ファイルの文言と見せ方**（§4.2）。
- **除外件数の見せ方**（§4.1）。
- **対象指定の選択 UI の形**（§4.3。**★`M24-02` の検索欄付きコンボボックスや `M24-12` のボタン群を流用してよい。★流用した場合はその旨を報告すること**）。
- **`web/e2e/support/` へ寄せるヘルパの命名とシグネチャ。**

### 9.3 不明事項発見時の対応

**★★「その症状は既に解消していた」場合は、実装せず、成立をテストで固定して報告すること。★これは欠陥ではない**（**★とくに §4.1 / §4.7 で起きうる**）。

**★★「memo の見立てが機序と違った」場合も報告すること。** **★とくに §4.2（「何も出ない」のか「気づけない」のか）。**

**★★「実査の答えが指示書と違った」場合も報告すること。** **★とくに §3.3-1 / -2 / -5 は、設計卓が裏を取っていない。**

**★★「N 個ある」と書かれた列挙は、その N を数え直すこと**（playbook §4.40）。

### 9.4 ★CHANGE 要否

§7.4.1 のとおり。**★判定した結果「射程は広がらなかった」であっても、判定したこと自体を報告に書くこと。**

---

## 10. 完了後の次ステップ

- 設計卓が受理し、**`CHANGE-144` を反映する**——**`DES-005` §5.13 / §5.13a / §5.14。**
- **★★§4.5 の用語の候補を `M24-07`（用語・i18n）へ渡す**（設計伝達レポート §4）。
- **★§4.6 で「他面」に見つかった確認ダイアログの不足を followup へ畳む。**
- **★`CO-023` の followup（`G-15`）を閉じる。**

---

## 11. 開発者への確認事項

| # | 問い | 設計卓の暫定案 | 止まるか |
|---|---|---|---|
| ~~**1**~~ | ~~取込での仮登録の除外を FE でやるか BE でやるか~~ | **★★2026-08-29 決着＝FE**（`D-591`）。**★BE は弾かない。⇒ API 直叩きでは入る。★これは不変条件ではなく「うっかり混ざるのを防ぐ」ためである**（§4.1.1） | **決着済み** |
| **2** | **★★`SM-072` の実態確認で「`/export/combo` でしかできないこと」が見つかった場合にどうするか**（**★★方向は memo が「エクスポート画面の廃止（第2段）」で確定させている**＝§1.3） | **★★2026-08-29 決着**（`D-591`）——**開発者の逐語＝「`/export/combo` でしかできないことがあるかないかで廃止要否を決定で良い」。⇒ 無ければ廃止する。★在ったら廃止せず報告する**（**★★§5.13a 側へ移植するのは「機能追加」であり射程が変わるため、その場で決めない**） | **★見つかった場合のみ止まる** |
| ~~**3**~~ | ~~エクスポートのファイル名の自動命名の規則~~ | **★★2026-08-29 決着**（`D-592`）——**`combos_{対象の種別}_{件数}_{YYYYMMDD-HHmmss}.{拡張子}`。★自動生成値を初期値とした編集可能な欄にする。★★ZIP 内のエントリ名には及ぼさない**（§4.4 / §4.4.1） | **決着済み** |
| **4** | **★`CO-023`（`G-15`）を本サブで扱うか**（**割付は「M24 or 改善レーン」・据え置き**） | **★暫定案＝本サブで扱う。★`SM-071` と同じファイルを触るため** | **★止まらない**（暫定案で進める） |

---

*以上、指示書 M24-06 **v1.2.0**。* **★★本サブの最大の危険は「監査」2 件が際限なく広がることである**〔`SM-110` は候補を挙げるまで ／ `SM-121` は面を取込・出力に絞る〕。**★★`SM-072` の方向は memo が「エクスポート画面の廃止（第2段）」で確定させている**（§1.3。**★v1.0.0 の「直すか畳むか決まっていない」は誤りだった＝`M-93`**）。**★ただし memo 自身が「実態確認後」と条件を付けている。⇒ `CO-002` と同じ手順（消す前に母集団を数える）が要る。** **★`SM-071` は FE で除外するが BE は弾かない。★これは不変条件ではなく「うっかり混ざるのを防ぐ」ためである**（§4.1.1）。 **★★★`SM-075` の自動命名は「外側のファイル名」だけに及ぼす。ZIP の中のエントリ名（`combos.csv` / `setups.csv`）は `DES-002` §7.6 の契約であり、変えると往復対称が壊れる**（§4.4.1）。
