# RESEARCH 指示書 M37-RESEARCH-01: リリース前要望の実査（`A` の配線 ／ `B05` ／ `B06` ／ `B11`）

| 項目 | 内容 |
|------|------|
| 文書ID | M37-RESEARCH-01 |
| バージョン | **1.0.0**（2026-09-13） |
| 作成日 | 2026-09-13 |
| 作成者 | 設計担当 Claude（設計卓／フェーズ4 期） |
| 対象マイルストーン | **`M37`**（リリース前要望の束）。上位＝`docs/instructions/M37-overview.md` v1.0.0 |
| 種別 | **★★RESEARCH（`/research_plan`）。⇒ read-only 厳守・事実列挙のみ。★実装しない** |
| 推奨モデル | **標準**（実モデル名は `docs/human-notes/model-allocation.md`） |
| Plan Mode | **不要**（read-only） |
| レビュー | **不要**（RESEARCH レポートは設計卓が読む） |
| CHANGE 消費 | **0 本**（調査である） |
| マイグレ消費 | **0 本** |
| 前提サブ | **無し**（**★`M37` の先頭。⇒ 他 4 サブはこの結果を待つ**） |
| 射程 | **4 件**——`A` の配線状況 ／ `B05`（modifier）／ `B06`（確定反撃候補）／ `B11`（仮想コントローラの情報量） |
| 配置 | `docs/instructions/M37-RESEARCH-01-request-batch-survey.md` |
| 出力 | `docs/progress/M37-RESEARCH-01-report.md` |

---

## 0. この指示書の特殊性

### 0.1 ★★★設計卓が読めないから、この調査が要る

**★設計卓は武装中であり `web/src/` `internal/` `character_data/` を作業ツリーに持たない。⇒ 「いま何が出ているか」を数えられない。**

**★★★`B05` / `B06` / `B11` は、いずれも「実査してから射程を決める」項目である**（`M37-overview` §0.3）**。⇒ 数えないまま指示書を書くと、設計卓が想像で射程を決めることになる。**

### 0.2 ★★本サブは 4 サブの律速である

**⇒ `M37-01` / `M37-02` / `M37-04` は本レポートを待つ。★`M37-03` だけが前提なしで並列に走れる。**

**★したがって、完璧より速さが要る場面がある。⇒ 分からないものは「分からない」と書いて先へ進むこと**（§4-4）**。**

### 0.3 ★★★仕分けをしない

**★本サブは事実を出すところまでである。⇒ 「直す / 直さない / 判断が要る」の仕分けは設計卓の手番である。**

**★★「この modifier は要らないと思う」のような判断を書かないこと。⇒ 代わりに「この modifier は N 件で使われており、画面 X に出る」と書く。**

### 0.4 ★★★着手前の版ゲート（**`M-169` の対処**）

**⇒ 着手の 1 手目に次の 2 点を確かめて報告 §1 へ書くこと。**

| # | 確かめること | 満たさないとき |
|---|---|---|
| **1** | **本指示書が v1.0.0 以上であり、`docs/instructions/M37-overview.md` が `main` に在ること** | **★止まって設計卓へ請求する** |
| **2** | **`git log --oneline -5` で自分の枝元を報告に書く** | — |

---

## 1. 前提として読むもの

| # | 資料 | 読む箇所 |
|---|---|---|
| **★★1** | `docs/instructions/M37-overview.md` v1.0.0 | **§1.1**（`A` の確定済み設計と不変条件）／ **§1.2**（`B01`〜`B15` の表）／ **§1.3**（`B10` の削除対象の逐語）／ §4（危険） |
| 2 | `docs/design/05-screen-design.md` | **§5.7**（コンボ登録・編集）／ **§6**（仮想コントローラの論理構成）／ §5.20（確定反撃サーチ） |
| 3 | `docs/design/03-data-model.md` | `combos.start_position_mass` / `carry_distance_mass` ／ `moves` の発生フレーム系の列 |
| 4 | `docs/handover/code-facts.md` | **§2**（Props / DTO）／ §10（マイグレ）。**★ただし派生資料である。⇒ 最終的な正は実物** |
| 5 | `docs/process/parallel-board.md` | **`D-730` / `D-731`**（3 方式の確定）／ `M-137`（射程の着地漏れ）／ **`D-853`**（受理） |

---

## 2. 調べること（**★read-only。⇒ 1 行も書き換えない**）

### 2.1 ★★`A` — 運び量・始動位置の配線状況

| # | 調べること | 数え方 |
|---|---|---|
| **★★1** | **`startPositionMass` / `carryDistanceMass` を参照している箇所の全数** | **★式で数えること**（例＝`grep -rn "startPositionMass\|carryDistanceMass" web/src/`）**。⇒ ファイル・行・用途**〔型定義 ／ スキーマ ／ 実際の入力・表示〕**を 1 行ずつ** |
| **★★2** | **入力・表示コンポーネントへの配線が本当に無いことの確認**（**★否定形**） | **★「無い」と断定する前に探す**（`E-117`）**。⇒ 探した式と、ヒット 0 件であることを報告に書く** |
| **★★3** | **区分選択（7 区分）の現在の入力 UI が在るか。⇒ 在るならどこか** | `web/src/constants/position.ts` の使われ方から辿る |
| **★4** | **API / CSV 側の受け口の as-built**（受け取る形・単位・範囲） | **★`D-730` の「0〜160 マス」と一致するかを実物で確かめる** |
| **★★5** | **バリデーションの現状**（`DES-006` の VAL コードとの対応） | **★3 方式を連動させるとき、どの検証が既に在るか** |

**★★★注意＝始動位置と運び量は別の値である**（`D-731`）**。⇒ 混ぜて数えないこと。★運び量は区分へ丸めない。**

### 2.2 ★★`B05` — modifier の実査

| # | 調べること | 数え方 |
|---|---|---|
| **★★1** | **いま存在する modifier の全数と、それぞれの意味** | **★式で数えること。⇒ 定義元**（定数・型・DB の列や値）**から辿る。★画面に出るものだけを数えない** |
| **★★2** | **各 modifier が*どの画面に*出るか** | **⇒ `DES-005` の節番号で書く。★出ていないものは「出ていない」と書く** |
| **★★★3** | **画面での*現在の見え方*の逐語** | **★★`B04` の要求は「生の `{}` 等で囲んだ表現をそのまま出さない」である。⇒ 実際に `{}` が出ている箇所を*文字列として*報告に写すこと** |
| **★4** | **各 modifier の使用件数**（実 DB ／ seed のどちらで数えたかを明記） | **★0 件のものは 0 件と書く。⇒ 「使われていない modifier」は `B05` の主対象になりうる** |
| **★★5** | **`B05` の「不足しているもの」の手がかり** | **★★これは推測になる。⇒ 「不足」と断定せず、「`DES-004` / `DES-005` が言及しているのに実装に無いもの」を機械的に拾うところまでにする** |

### 2.3 ★★★`B06` — ターゲットコンボの確定反撃候補

**★★★開発者から仮説が示されている**（2026-09-13・逐語）**＝「ターゲットコンボの 2 段目以降の発生 F に、1 段目の物ではなく、2 段目の発生 F を入れる方式にしていたため候補にでているものと思われる」。**

**⇒ ★これは*仮説*である。⇒ 事実として扱わず、当たっているかを実測で確かめること。**

| # | 調べること | 数え方 |
|---|---|---|
| **★★★1** | **上記仮説の真偽**——**ターゲットコンボの 2 段目以降の行は、`startup` に「その段の発生」を持っているか、「初段の発生」を持っているか** | **★実データで確かめる。⇒ `character_data/*.csv` と DB の両方。★例に挙がった「不破三連撃(2発止め)」を*名指しで*確かめること** |
| **★★2** | **確定反撃サーチが候補を出すときに、どの列を読んでいるか** | **★★実装を辿ること。⇒ `startup` / `startup_basis` / `is_derived` / `category` のどれを見ているか** |
| **★★★3** | **仮説が当たっていた場合、同じ形の行が何件あるか** | **★★★これが射程の大きさを決める。⇒ 1 件なのか 100 件なのかで是正方式が変わる。★式で数えること** |
| **★4** | **`startup_basis` が何を持っているか**（当該行群） | **★`standalone` / `unknown` 等。⇒ 既存の除外述語と関係するか** |
| **★★5** | **是正の選択肢を*事実として*並べる** | **★★仕分けはしない。⇒ 「データを直す」「実装の判定を変える」のどちらが可能かを、実装とデータの形から述べるところまで** |

**★★★注意＝「2 発止め」のような*段を止める*技の扱いは、ゲームの事実である。⇒ 開発者のドメイン知識が要る**（`CLAUDE.md` §9 のハード列）**。★製造は「どちらが正しいか」を決めない。**

### 2.4 ★★`B11` — 仮想コントローラの情報量

| # | 調べること | 数え方 |
|---|---|---|
| **★★1** | **「押している入力〜解決できなかった入力」の 3 区画に、いま何が出ているか** | **★`GamepadRecipeReadout.tsx` を読み、区画ごとに*出る情報の一覧*を作る。⇒ 逐語で** |
| **★★2** | **各情報が「無いと困る」か「あると良い」か**の**手がかり** | **★★★判定しない。⇒ 代わりに「その情報を出すために何を計算しているか」「他の画面で同じ情報が取れるか」を書く。★設計卓が判定する材料を出す** |
| **★★3** | **`B10` の 3 文の現在地**（`GamepadInputNotice.tsx`） | **★逐語が `M37-overview` §1.3 と一致するかを確かめる。⇒ 食い違ったら、それが成果である** |
| **★★★4** | **★★3 文と 3 区画を維持することを要求している既存コメント・テストの全数** | **★★★これが最も重い。⇒ 削除するとき、どの床が落ちるかが分からないと、黙って外すことになる**（`M37-overview` §4.3）**。★式で数えること** |
| **★5** | **仮想コントローラの縦の寸法の内訳**（`B08` / `B09` / `B13` / `B14` の材料） | **★★概算でよい。⇒ 「どの区画が縦を食っているか」が分かる粒度。★正確な px は要らない** |

---

## 3. やらないこと

| # | 内容 | 理由 |
|---|---|---|
| **★★1** | **1 行でも書き換えること** | **★★RESEARCH である。⇒ `git diff` が空であることを報告に書く** |
| **★★2** | **仕分け**（直す / 直さない / 判断が要る） | **★設計卓の手番**（§0.3） |
| **★★3** | **是正方式を決めること** | **★とくに `B06`。⇒ ゲームの事実の判断は開発者のドメイン知識が要る** |
| 4 | **`B01` / `B02` / `B03` / `B07` / `B12` / `B15` の調査** | **★これらは射程が既に決まっている。⇒ 本サブの 4 件だけを見る** |
| **★5** | **`followup-backlog.md` の編集** | **★`D-838`。⇒ 設計伝達レポートも RESEARCH では不要。★本レポートが成果物である** |

---

## 4. ★★出力の形

| # | 節 |
|---|---|
| **1** | **§1 版ゲートの結果**（§0.4 の 2 点 ＋ 枝元） |
| **★★2** | **§2 `A` の配線状況**（§2.1 の 5 項目。**★式と件数を各項目の冒頭に**） |
| **★★3** | **§3 `B05` modifier の実査**（§2.2 の 5 項目。**★`{}` の逐語を含む**） |
| **★★★4** | **§4 `B06` の仮説検証**（§2.3。**★★仮説が当たったか外れたかを最初の 1 行で書く。⇒ 件数を必ず出す**） |
| **★★5** | **§5 `B11` の情報量**（§2.4。**★★とくに 2.4-4 の床の全数**） |
| **★★6** | **§6 ★分からなかったこと**（**★0 件なら「0 件」と書く。⇒ 空欄にしない**） |
| **7** | **§7 使った式の一覧**（**★再現できる形で**） |
| **8** | **`docs/progress/progress-log.md` への索引行**（`CLAUDE.md` §8。**★必須**） |
| **9** | **`bash scripts/check-md-emphasis.sh <本報告のパス>`**（`D-775`） |

---

## 5. ★★本書の落とし穴

| # | 落とし穴 |
|---|---|
| **★★★1** | **★仮説を事実として扱うこと**（§2.3）**。⇒ 開発者の仮説は良質だが、確かめる対象である。★「仮説どおりでした」と書く前に、名指しの行で見ること** |
| **★★2** | **★目視で数えること。⇒ 全項目で式を使う**（`M33-01` §0.3 と同じ型。**設計卓の見立て 3 件に対し実測 28 ファイルという先例がある**） |
| **★★3** | **★判定してしまうこと**（§0.3）**。⇒ 「要らないと思う」と書いた瞬間、設計卓の判断材料が減る** |
| **★★★4** | **★分からないものを埋めること。⇒ §4-6 へ書いて先へ進む。★本サブは 4 サブの律速であり、完璧より速さが要る場面がある** |
| **★5** | **★`code-facts.md` を正として実物を見ないこと。⇒ 派生資料である**（`CLAUDE.md` §8 の注記） |

---

## 6. 参照ドキュメント

| ID | パス | 参照する箇所 |
|----|------|------------|
| — | `docs/instructions/M37-overview.md` v1.0.0 | **§0.2**（全件を新規実装するのではない）／ §1.1 ／ §1.2 ／ **§1.3**（`B10` の逐語）／ §4 |
| DES-005 | `docs/design/05-screen-design.md` | §5.7 ／ **§6**（仮想コントローラ）／ §5.20 |
| DES-003 | `docs/design/03-data-model.md` | `combos` の位置系の列 ／ `moves` の発生フレーム系 |
| DES-004 | `docs/design/04-notation-spec.md` | **§2.1**（`move_code` の命名規約。**★`B05` で modifier の定義元を辿るときに要る**） |
| DES-006 | `docs/design/06-validation.md` | **`A` のバリデーションの現状** |
| — | `docs/process/parallel-board.md` | `D-730` / `D-731` ／ `M-137` ／ `D-853` / `D-854` |
| — | `docs/human-notes/Memo_Someday.txt` | **424〜466 行付近**（`B01`〜`B15` の一次源。**★逐語を確かめたいとき**） |

---

*以上、RESEARCH 指示書 M37-RESEARCH-01 **v1.0.0**。* **★★★本サブの芯は §2.3-3 である**——**`B06` の仮説が当たっていた場合、同じ形の行が何件あるか。⇒ 1 件なら個別是正、100 件なら方式の是正であり、射程がまるごと変わる。★ここを数えずに `M37-04` を起票すると、設計卓が想像で射程を決めることになる。**
