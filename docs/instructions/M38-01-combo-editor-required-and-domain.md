# 指示書 M38-01: 新規登録・編集の必須と値域の是正（矢印 ／ 並び ／ 必須の入れ替え ／ 不問 ／ ヒット種別）

| 項目 | 内容 |
|------|------|
| 文書ID | M38-01 |
| バージョン | **1.1.0**（2026-09-18・**★★★as-built 注記を足した。⇒ 本文は 1 文字も変えていない**〔`VAL-C15` は 2 欄になった ／ 「不問」はトグルではなく空欄そのものになった ／ 画面項目が 2 件動いた〕**。★完了したサブの指示書は歴史記録である**＝`D-896`） ／ **1.0.0**（2026-09-17・発行） |
| 作成日 | 2026-09-17 |
| 作成者 | 設計担当 Claude（設計卓） |
| 対象マイルストーン | **`M38`**（撮影で出たリリース前要望の第 2 束）。上位＝`docs/instructions/M38-overview.md` v1.0.0 |
| 推奨モデル | **標準実装**（実モデル名は `docs/human-notes/model-allocation.md`） |
| Plan Mode | **必須**（`CLAUDE.md` §9＝**バリデーション契約の変更 ＋ 値域の変更 ＋ データ移行の可能性**） |
| レビュー | **必要**（**★チェックリスト＝`docs/instructions/reviews/M38-01-review-checklist.md` v1.1.0**） |
| CHANGE 消費 | **2〜3 本の見込み**（`DES-006` `VAL-C15` ／ `DES-005` §5.7 ／ `SUPP-001` §3.2。**★自採番しない**＝`D-293`） |
| マイグレ消費 | **★★0 本**（**★`000118` は未消費で返した**＝`D-896`）。**★以下は発行時の記述：** `000118` を払い出し済み（0〜1 本。要らなければ未消費で返すこと。先例＝`M28-04`） |
| 前提サブ | **なし**（**★`M38-02` とは並列可**＝`M38-overview` §3.3） |
| 射程 identity | **5 件**——矢印 ／ 並び ／ 必須の入れ替え ／ 不問の明示 ／ ヒット種別 |
| 配置 | `docs/instructions/M38-01-combo-editor-required-and-domain.md` |
| 出力 | `docs/progress/M38-01-completion-report.md` |

---

## ★★★as-built（**2026-09-18 追記＝`D-896`。本サブは完了済みである**）

> **★本節は歴史記録の注記である。⇒ 以降の本文は発行時のまま 1 文字も変えていない。**

| # | 発行時の記述 | 実際にどうなったか |
|---|---|---|
| **★★★1** | **§2.4.1 ／ §3-2 ／ §6-3 が「`VAL-C15` は 4 欄のまま。★『4 欄』という数を動かさない」と 3 箇所で要求**（**★チェックリスト §0-3 / B-1 を含めると 5 箇所**） | **★★開発者裁定で覆った。⇒ `VAL-C15` は 2 欄**〔`damage` / `knockdown_advantage`〕**である**（`CHANGE-216`）**。★★★製造は着手前に衝突を 5 箇所とも逐語で提示し、裁定を得てから実装した。⇒ 規律違反ではない。★正本は `DES-006` §2.1 である** |
| **★★★2** | **§2.4.2 が「『不問』を明示的に選べるようにする。placeholder に薄く『不問』」と定めた** | **★★作って、使って、撤回された。⇒ 着地は「空欄そのものが不問」であり、トグルも `Space` のショートカットも無い。★placeholder は空なら*常に*「不問」。★正本は `DES-005` §5.7**（`CHANGE-217`）**。★★理由＝UI が 3 状態なのに DB は 2 状態であり、余った 1 状態を見分けさせる手段が placeholder の有無しか作れなかった** |
| **★★3** | **§2.3-3 の「上にあるものが必須」という落とせない条件** | **★成立しなくなった。⇒ 必須が 4 → 2 欄になり、先頭 4 欄の 3・4 番目が任意になった。★欄の位置は開発者裁定で維持。⇒ 手掛かりは節の凡例だけが担う** |
| **★4** | **画面項目の並び**（発行時は射程外） | **★2 件動いた**〔運び量を「相手の大きさ」の直下へ ／ 始動技の読み取り専用表示を削除〕**。⇒ 開発者の追加裁定**（2026-09-18）**。★保存される値は 1 バイトも変わらない** |
| **★5** | **マイグレ `000118` を払い出し済み** | **★未消費で返した。⇒ `hit_type` が NULL の既存行を `normal` へ寄せるマイグレは作らない**〔`CHANGE-218`〕 |

**★★★機構化は `playbook` §4.72**〔同じ禁止を複数箇所へ重ねて書かない〕**と §4.73**〔UI の状態数が永続化先を超える設計〕**に入れた。**

---

## 0. この指示書の特殊性

### 0.1 ★★★段 1 の実査が判断の起点である。**先に直さないこと**

**★★射程 5 のうち「ヒット種別の不問」は*正体が未確定*である。⇒ 設計卓は実装を読めない**（武装中）**。**

**★★★設計書には「不問」が無い**（`SUPP-001` §3.2 の `hit_type` は 8 値）**。⇒ しかし開発者は画面で「1 不問」を見ている。★どちらも事実である。**

**⇒ 段 1 で正体を特定してから §2.5 へ進むこと。★特定の結果によってやることが変わる。**

### 0.2 ★★「必須」と「不問」は両立する

**★一見矛盾する**〔必須＝空でないこと ／ 不問＝NULL〕**。⇒ 解けている。**

| 保存時 | 保存後の DB |
|---|---|
| **数値を入れた** | その値 |
| **「不問」を選んだ** | **NULL** |
| **空のまま** | **★保存できない**（必須が止める） |

**★★★保存された NULL は*必ず*「不問」である。⇒ 「空のまま」は保存されないからである。★だから NULL の両義性は生じない。**

**★★既存行は別である。⇒ 必須でなかった時代の NULL は「未入力」かもしれない。★それらも「不問」と表示される。⇒ 実害は表示だけであり、直さない**（§3-4）**。**

### 0.3 ★★★列は 1 つも足さない

**★開発者の当初の見立ては「フラグを新設して不問を入力可能にしたい」であった。⇒ 要らない。**

**★★先例＝`CHANGE-200` / `M37-05` が `start_position_mass IS NULL ⇔ position = 不問` を確立している。**

**★★`M37-07` で「3 値にしない」と決めたのと逆向きに見えるが矛盾しない。⇒ あちらは*重複判定キー*であり、NULL を許すと `NULL != NULL` で「不問どうし」が別コンボへ分かれる。★本件の 2 列はキーに入っていない。**

### 0.4 ★★着手前の版ゲート（**`M-169` の対処**）

| # | 確かめること | 満たさないとき |
|---|---|---|
| **1** | `docs/instructions/reviews/M38-01-review-checklist.md` が存在すること | **★止まって設計卓へ請求する** |
| **2** | **`DES-006` が 1.49.0 以上であること**（`VAL-C15` が 4 欄で在るか） | **★古いなら止まって請求する** |
| **3** | **`SUPP-001` §3.2 の `hit_type` が 8 値であること** | **★本書はこれを正本とする。⇒ 食い違ったら止まる** |
| **4** | **`ls migrations/` の最新を実測し、報告へ書く**（**★数はここに書かない**＝計測点 `M-184`） | **★食い違ったら自採番せず再請求**（`D-293`） |
| **5** | `git log --oneline -5` で枝元を報告へ書く | — |

---

## 1. 前提として読むもの

| # | 資料 | どこを |
|---|---|---|
| **★★1** | `docs/design/supp-001-detailed-design.md` | **§3.2**（**状況 4 軸の値域の正本**。⇒ `hit_type` は 8 値） |
| **★★2** | `docs/design/06-validation.md` | **§2.1 `VAL-C15`**（**必須 4 欄の正典**） ／ §2.3（`VAL-C02` の重複判定） |
| **★3** | `docs/design/05-screen-design.md` | §5.7（コンボ登録・編集） |
| **★4** | `docs/change-notes/CHANGE-200-notification.md` | **NULL を「不問」と読む先例** |
| 5 | `docs/design/03-data-model.md` | §3.3（`combos` の列。**★4 列とも NULL 可であることを確かめる**） |

---

## 2. 段取り

### 2.1 ★★★段 1 — 実査（**★コードを書く前に済ませ、報告へ書く**）

| # | 確かめること | なぜ |
|---|---|---|
| **★★★1** | **ヒット種別の「不問」の正体。⇒ 選択肢の実体はどこに在るか ／ 選ぶと DB へ何が入るか ／ `hit_type` の既定値は何か** | **★射程 5 の判断の起点。⇒ §2.5 の分岐がこれで決まる** |
| **★★2** | **`hit_type` が NULL の既存行が実 DB に何行あるか** | **★★マイグレの要否がこれで決まる。⇒ 0 行ならマイグレは要らない** |
| **★★★3** | **その NULL 行を `normal` へ寄せたとき、重複判定キーが衝突する組があるか** | **★★`hit_type` は重複判定キーの 1 つである**（`VAL-C02`）**。⇒ 寄せると「別コンボだったもの」が同じキーになりうる** |
| **4** | **`VAL-C15` の実装の在り処**（4 欄をどこで見ているか） | 射程 3 の足し先 |
| **5** | **数値欄の矢印上下増減がどこで効いているか**（`<input type="number">` の既定か、独自実装か） | 射程 1 の足し先 |
| **6** | **`drive_available_at_start` / `sa_available_at_start` の現在の入力形** | 射程 4 の足し先 |

**★数値の隣に「実測」「算出」を書き分けること**（計測点 `M-157`）**。**

**★★★2 と 3 は実 DB が要る。⇒ 製造の作業ツリーに DB が無ければ、開発者へ実測を依頼して止まること**（先例＝`M37-07` §4-4）**。**

### 2.2 ★段 2 — 矢印キー（**射程 1**）

| # | やること |
|---|---|
| **1** | **数値欄で矢印キーの上下による増減を止める** |
| **★2** | **矢印キーはフォーカス移動へ返す**（開発者の逐語＝「矢印でフォーカスを移動させれた方がうれしい」） |
| **★3** | **スピナー**〔上下ボタン〕**が描画されているなら、それも消すこと。⇒ 押せるのに効かない状態を作らない** |

### 2.3 ★★段 3 — 必須項目の並び（**射程 2**）

| # | やること |
|---|---|
| **1** | **必須項目を基本情報の上の方へまとめる** |
| **★★2** | **「上の方」の具体は実装判断でよい。⇒ 採った並びと理由を報告へ書くこと** |
| **★★★3** | **必須でないものを上へ混ぜないこと。⇒ 混ぜると「上にあるものが必須」という手掛かりが消える** |

### 2.4 ★★★段 4 — 必須の入れ替えと「不問」（**射程 3 ＋ 4**）

#### 2.4.1 `VAL-C15` の 4 欄を入れ替える

| | 欄 |
|---|---|
| **着手前** | `damage` ／ `knockdown_advantage` ／ **`drive_gauge_consumed`** ／ **`sa_gauge_consumed`** |
| **★着手後** | `damage` ／ `knockdown_advantage` ／ **`drive_available_at_start`** ／ **`sa_available_at_start`** |

**★★4 欄のままである。⇒ 中身が入れ替わるだけである。★「4 欄」という数を動かさない。**

**★★★外した 2 欄は*空でも保存できる*ようになる。⇒ 値域の検査は外さない。★入っているなら範囲を守ること。**

#### 2.4.2 「不問」を選べるようにする

**★対象は入れ替えで*必須になった* 2 欄である**〔`drive_available_at_start` / `sa_available_at_start`〕**。**

| # | やること |
|---|---|
| **★★1** | **「不問」を選ぶと NULL が入る。⇒ 列は足さない**（§0.3） |
| **★★★2** | **placeholder に薄く「不問」と出す**（開発者の逐語＝「画面上、null の場合はテキストボックス上に薄く不問と見えると良いです」） |
| **★★3** | **必須の判定は「数値が入っているか*または*不問が選ばれているか」である。⇒ 空のままは保存できない** |
| **★4** | **既存行の NULL も「不問」として表示される。⇒ 直さない**（§0.2） |

### 2.5 ★★★段 5 — ヒット種別（**射程 5・★分岐がある**）

**★★段 1-1 の実査の結果で分かれる。⇒ 先に直さないこと。**

| 実査の結果 | やること |
|---|---|
| **(A) UI だけの選択肢で、選ぶと NULL が入る** | **★選択肢から「不問」を消す ／ 既定を `normal` にする ／ 既存の NULL 行の扱いを段 1-2・1-3 の実測で決める** |
| **(B) 実装が独自の値**〔`any` 等〕**を入れている** | **★★止まって設計卓へ請求する。⇒ 値域の正本**（`SUPP-001` §3.2）**に無い値が DB に入っていることになり、判断が要る** |
| **(C) 開発者が別の軸**〔`opponent_stance` の「不問」〕**を見ていた** | **★★何もしない。⇒ `opponent_stance` の `any` は正規の値である**（`CHANGE-003`）**。★報告へそう書いて閉じる** |

#### 2.5.1 ★★★既存の NULL 行の扱い（**(A) のとき**）

| 段 1-2 の実測 | やること |
|---|---|
| **0 行** | **★マイグレは要らない。⇒ `000118` を未消費で返す** |
| **1 行以上・衝突なし**（段 1-3） | **★マイグレで `normal` へ寄せる。⇒ `000118` を使う** |
| **1 行以上・衝突あり** | **★★★止まって設計卓へ請求する。⇒ 寄せると重複判定キーが衝突し、「別コンボだったもの」が同じキーになる。★自分で決めない** |

**★★寄せるマイグレは*絶対値を書く* `UPDATE` にすること**（`SUPP-001` §5.5 規約 (20) の (b) と同じ流儀）**。**

### 2.6 ★段 6 — 設計書の原稿

**★CHANGE 原稿を設計伝達レポートの §1**〔確定仕様〕**と §6**〔起票たたき台〕**へ書く。⇒ §4 ではない**（計測点 `M-183`）**。★設計書本体は書き換えない**（`CLAUDE.md` §8）**。**

---

## 3. やらないこと

| # | やらないこと | なぜ |
|---|---|---|
| **★★1** | **列を足すこと** | **★§0.3。⇒ NULL が「不問」を表せる** |
| **★★2** | **`VAL-C15` の欄数を変えること** | **★4 欄のままである。⇒ 中身が入れ替わるだけ** |
| **★3** | **外した 2 欄の値域検査を外すこと** | **★必須を外すのと値域を外すのは別である。⇒ 入っているなら範囲は守る** |
| **★★4** | **既存行の NULL を「未入力」として直すこと** | **★§0.2。⇒ 表示が「不問」になるだけであり実害は小さい。★直すと*開発者が入れた値*を失う危険がある** |
| **★★★5** | **段 1 の実査の前に §2.5 を直すこと** | **★§0.1。⇒ 正体が未確定である** |
| **★6** | **`hit_type` の値域**〔8 値〕**を動かすこと** | **★正本は `SUPP-001` §3.2 である。⇒ 「不問」を消すのは*選択肢*からであって値域からではない**（**そもそも値域に無い**） |
| **★7** | **`M38-02` の面を触ること** | **★技編集 ／ 改名は向こうの射程である** |

---

## 4. 危険（**★起きやすい順**）

### 4.1 ★★★最大の危険＝**「不問」の正体を確かめずに消すこと**

**★設計書に無い値が画面に出ている。⇒ どこから来ているかが分からないまま消すと、消し残しか、消しすぎになる。**

**★★★段 1-1 が終わるまで §2.5 へ進まないこと。**

### 4.2 ★★2 番目＝**既存の NULL 行を寄せて重複を作ること**

**★`hit_type` は重複判定キーの 1 つである**（`VAL-C02`）**。⇒ NULL の行を `normal` へ寄せると、`normal` の行と同じキーになりうる。**

**★★★段 1-3 で衝突を数えてから決める。⇒ 衝突が 1 組でも出たら止まって請求する。**

### 4.3 ★★3 番目＝**「必須」と「不問」を矛盾と読むこと**

**★§0.2 を読むこと。⇒ 「空のまま」は保存できず、「不問を選んだ」は保存できる。★保存された NULL は必ず「不問」である。**

### 4.4 ★4 番目＝**必須でないものを上へ混ぜること**

**★§2.3-3。⇒ 「上にあるものが必須」という手掛かりが消える。**

### 4.5 ★5 番目＝**スピナーを残すこと**

**★§2.2-3。⇒ 押せるのに効かないボタンは、消したはずの機能が残っているように見える。**

---

## 5. テスト

| # | 見るもの |
|---|---|
| **★★1** | **`VAL-C15` の 4 欄が入れ替わったこと**（**★入れ替え前の 2 欄で空のまま保存できること ／ 入れ替え後の 2 欄で空のまま保存できないこと**） |
| **★★★2** | **「不問」を選んで保存すると NULL が入ること。⇒ 読み直すと placeholder に「不問」が出ること** |
| **★3** | **矢印キーで数値が変わらないこと。⇒ フォーカスが動くこと** |
| **★★4** | **ヒット種別の選択肢に「不問」が無いこと**（§2.5 が (A) のとき）**。★既定が `normal` であること** |
| **★5** | **外した 2 欄の値域検査が生きていること**（**★入れたら範囲を見る**） |
| 6 | **`go test ./...` ／ `cd web && pnpm test` ／ `make e2e` を全数**（**★出力をファイルへ全量落とし、`EXIT=` と `FAIL` の件数を報告へ貼ること**） |

---

## 6. 完了条件

| # | 条件 |
|---|---|
| **★★★1** | **段 1 の実査 6 点が報告に在る。⇒ とくに 1〜3 は実測値で書かれている** |
| **★★2** | **§2.5 のどの分岐を採ったかと、その根拠が報告に在る** |
| **★★3** | **`VAL-C15` が 4 欄のままで、中身が入れ替わっている** |
| **★★4** | **「不問」を選ぶと NULL が入り、placeholder に薄く出る** |
| **★5** | **矢印キーがフォーカス移動になっている。⇒ スピナーが残っていない** |
| **★★6** | **列を 1 つも足していない**（`git diff` で示す） |
| **★7** | **マイグレを使ったか未消費で返したかが報告に在る**（**★使ったなら `000118`。★返したならその旨**） |
| 8 | **§5 の検査がすべて緑**（**★出力で判定する**） |
| **★9** | **`docs/progress/M38-01-completion-report.md` を書き、`progress-log.md` へ索引行を 1 行足した** |
| **★★10** | **設計伝達レポートを出した**（`/design_handover_report`）**。★CHANGE 原稿は §1 と §6 へ**（計測点 `M-183`） |
| **★11** | **着手前の版ゲート 5 点を通した**（§0.4） |
| **★★12** | **完了報告を書いた*後に*、報告を入力に取る検査を回し直した**（`check-progress-log-index.sh` 等。**★製造 CLI の当該節**） |

---

## 7. ★開発者への確認事項

| # | 事項 | 設計卓の状態 |
|---|---|---|
| ~~1~~ | 技編集を消すのは新しい判断でよいか | **★★決着＝よい**（2026-09-17）**。⇒ `M38-02` の射程** |
| ~~2~~ | ヒット種別の「不問」は何を見たか | **★★開発者の回答＝新規登録・編集画面のヒット種別に「1 不問」が在る。⇒ 「過去の実装担当が仕込み、発見できなかったバグの可能性」**（逐語）**。★正体は段 1 で特定する** |
| ~~3~~ | 列を足さず NULL で表す形でよいか | **★★決着＝よい**（2026-09-17）**。⇒ あわせて「null の場合はテキストボックス上に薄く不問と見える」ことを求められた** |
| **★4** | **既存の `hit_type` が NULL の行をどう扱うか** | **★段 1-2 / 1-3 の実測が出てから諮る。⇒ 0 行なら諮る必要が無い** |

---

## 8. 参照ドキュメント

| ID | ファイルパス | どこを |
|----|-------------|--------|
| M38-overview | `docs/instructions/M38-overview.md` | §2（**前提が覆った 3 件**） |
| SUPP-001 | `docs/design/supp-001-detailed-design.md` | **§3.2**（値域の正本） ／ §5.5 規約 (20) |
| DES-006 | `docs/design/06-validation.md` | **§2.1 `VAL-C15`** ／ §2.3 `VAL-C02` |
| DES-005 | `docs/design/05-screen-design.md` | §5.7 |
| DES-003 | `docs/design/03-data-model.md` | §3.3（**4 列とも NULL 可**） |
| CHANGE-200 | `docs/change-notes/CHANGE-200-notification.md` | **NULL を「不問」と読む先例** |
| ボード | `docs/process/parallel-board.md` | `D-891` / `D-892` |

---

*以上、指示書 M38-01 **v1.1.0**。* **★★★§0.1 を最初に読むこと**——**「ヒット種別の不問」は正体が未確定である。⇒ 段 1 の実査が判断の起点であり、先に直すと消し残すか消しすぎる。**
