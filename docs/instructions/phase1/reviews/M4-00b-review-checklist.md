# M4-00b レビューチェックリスト

| 項目 | 内容 |
|------|------|
| 対象指示書 | `docs/instructions/M4-00b-modifiers-editor-console-warn.md` v1.0.0 |
| バージョン | 1.0.0 |
| 推奨レビューモデル | Sonnet 4.6 |
| 作成者 | 詳細設計・製造準備担当 Claude(M4 期間担当) |
| 作成日 | 2026-05-16 |

## 更新履歴

| バージョン | 更新日 | 更新内容 |
|-----------|--------|----------|
| 1.0.0 | 2026-05-16 | 初版作成 |

---

## 0. レビュー担当への前置き

### 0.1 レビュー前の準備

レビュー担当 Claude Code は以下を読んでからレビューに着手する:

- M4-00b 指示書本体(`docs/instructions/M4-00b-modifiers-editor-console-warn.md`)
- 本チェックリスト(全節)
- **M4-00 指示書 v1.0.0**(本指示書は M4-00 の双子作業のため、構造比較の参照源として重要)
- CLAUDE.md(§4 TypeScript 規約、§10 禁止事項)
- 製造担当の実装完了報告(`docs/progress/progress-log.md` 末尾)
- 関連既存ファイル(`web/src/features/combo/components/ModifiersEditor.tsx` 修正後 / `ModifiersEditor.test.tsx` 修正後)
- 比較参照(`web/src/features/combo/components/RecipeBuilder.tsx` M4-00 完了後 / `RecipeBuilder.test.tsx` M4-00 完了後)

### 0.2 レビューの基本姿勢

本指示書は **隠れ P-01 相当の清算** であり、M4-00 と同構造のため M4-00 レビュー観点を踏襲する。以下を意識する:

- 製造担当が指示書通り実装したか機械的に確認
- **M4-00 で確立した RecipeBuilder 側のパターンと整合しているか**(useEffect 構造、spy 方式、文言形式の対称性)
- 既存テスト回帰がないか確認(ModifiersEditor.test.tsx と RecipeBuilder.test.tsx 両方)
- §3.4 着手前確認(特に §3.4.1 検出ゼロ、§3.4.2 対象フィールド特定)が報告に含まれているか確認
- M2-04 設計担当ミスの連鎖踏み(retrospective-log §1 パターン A の双子事例)が本指示書で正しく清算されているか確認

### 0.3 レビュー結果の報告フォーマット

レビュー結果は以下のフォーマットで開発者に報告する:

```
## M4-00b レビュー結果

### 重大な問題(検出された場合、§9 判定基準)
- (なし / 列挙)

### 軽微な問題(検出された場合、§10 判定基準)
- (なし / 列挙)

### 機械的チェック通過状況
- §1〜§7 各節の通過項目数 / 全項目数

### M4-00 との整合確認
- §5.2 の各観点で問題なし / 問題あり

### 完了判定
- §12 通り「重大な問題なし → 完了承認待ち」/「重大な問題あり → 修正要求」
```

---

## 1. 指示書本体との照合(retro R-01 対応、最重要観点)

### 1.1 §2.2 ファイル修正内容との一致

- [ ] `web/src/features/combo/components/ModifiersEditor.tsx` に `useEffect` で `console.warn` を発火する処理が追加されている
- [ ] `web/src/features/combo/components/ModifiersEditor.test.tsx` に新規 3 ケースが追加されている
- [ ] 上記 2 ファイル以外への変更が含まれていない(本指示書のスコープ厳守、§2.3)
- [ ] **RecipeBuilder.tsx および RecipeBuilder.test.tsx に変更が入っていない**(M4-00 完了承認済み、本指示書のスコープ外)

### 1.2 §4.1 console.warn 実装の整合

- [ ] `useEffect` の依存配列が §3.4.2 で特定された対象フィールドのみになっている
- [ ] 50 文字超の判定が `<対象フィールド名>.length > 50` で実装されている(`>=` ではないこと、§4.1.1)
- [ ] 警告文言に「50 文字を超えています」が含まれている(§4.1.2)
- [ ] 警告文言に **実際の文字数 N** が含まれている(`${...length}` 等、§4.1.2)
- [ ] 50 文字以下の場合の else 分岐が **存在しない**(余計な処理を追加していない)
- [ ] 視覚的 UI 警告(M2-04 §4.1 で実装済み)が **削除・変更されていない**(§4.1.4、§2.3)
- [ ] 配置位置が既存 `useEffect` 群の末尾、独立ブロックとして追加されている(§4.1.3)
- [ ] 他の既存 `useEffect` ブロックを変更していない

### 1.3 §4.2 テスト 3 ケース実装の整合(M4-00 と同形)

- [ ] ケース 1: 50 文字以下で `console.warn` 未発火を検証している
- [ ] ケース 2: 51 文字以上で `console.warn` が 1 回発火し、文言に「50 文字を超えています」を含むことを検証している
- [ ] ケース 2 の文言確認が **部分一致**(`expect.stringContaining` 等)で行われている
- [ ] ケース 3: 50 文字超を 50 文字以下に減らした際、spy 呼出回数が 1 のまま維持されることを検証している
- [ ] `vi.spyOn(console, 'warn')` 方式でスパイを設定している(M4-00 と同方式、§4.2.2 / §3.4.4)
- [ ] `beforeEach` でスパイ設定、`afterEach` でリセットが実装されている
- [ ] 既存ケース(ModifiersEditor.test.tsx の従前ケース)が変更されていない(§2.3)

### 1.4 §3.4 着手前確認の報告

- [ ] §3.4.1 の console.warn 検出ゼロ確認結果が報告に含まれている
- [ ] **§3.4.2 で特定した警告対象フィールド名が明示的に報告に記載されている**(本指示書の最重要確認)
- [ ] §3.4.3 の ModifiersEditor.test.tsx 既存ケース確認結果が報告に含まれている
- [ ] §3.4.4 の既存 spy 方式確認結果が報告に含まれている(M4-00 と同方式が確認されているか)
- [ ] §3.4.2 の対象フィールド特定について Plan Mode で開発者の合意を得てから実装に着手したか報告に明記されている

---

## 2. API 整合性

本指示書はバックエンド変更を含まないため、本節は **該当なし**。

---

## 3. フロントエンドの動作仕様

### 3.1 既存挙動の温存

- [ ] M2-04 §4.1 で実装された ModifiersEditor の視覚的 UI 警告(赤ボーダー + 文字数メッセージ)が引き続き動作する
- [ ] ModifiersEditor の他のロジック(flags / type / notes 編集)が回帰していない
- [ ] M4-00 で追加された RecipeBuilder の console.warn が引き続き動作する(独立性確認)
- [ ] コンボ登録・編集の保存フローが影響を受けていない

### 3.2 console.warn の発火タイミング

- [ ] 50 文字を超えた瞬間に発火する(useEffect の依存配列で実現、§4.3 確定事項)
- [ ] 50 文字以下に戻った後、再度超えた場合は再発火する
- [ ] 50 文字以下から減らした際の追加発火がないこと(ケース 3 で検証)
- [ ] ModifiersEditor と RecipeBuilder の console.warn が独立して発火する(§5.2 E2E シナリオ B で検証)

---

## 4. テストの妥当性

### 4.1 バックエンドテスト

該当なし(本指示書はバックエンド変更を含まない)。

### 4.2 フロントエンドテスト

- [ ] `cd web && pnpm test` が全通過している
- [ ] `cd web && pnpm build` が成功している
- [ ] TypeScript の型エラーが残っていない
- [ ] ModifiersEditor.test.tsx の総ケース数が **既存ケース数 + 新規 3 ケース** になっていること
- [ ] RecipeBuilder.test.tsx が回帰していない(M4-00 で追加された 3 ケース含む)
- [ ] テストファイル内の console 出力が抑制されている(`vi.spyOn(console, 'warn').mockImplementation(() => {})` 等)

### 4.3 E2E シナリオ動作確認手順書

- [ ] 製造担当の完了報告に「開発者向けの動作確認手順書」が含まれている
- [ ] 手順書に §5.2 シナリオ A(ModifiersEditor の console.warn 手動動作確認)が含まれている
- [ ] 手順書に §5.2 シナリオ B(RecipeBuilder 側との独立性確認)が含まれている
- [ ] 手順書に §5.2 シナリオ C(既存機能の回帰)が含まれている

---

## 5. 設計意図との整合(機械的チェックを超えた観点)

### 5.1 隠れ P-01 清算の本質

本指示書の本質は「**M2-04 設計担当ミスが ModifiersEditor 側にも存在していた事実を清算する**」こと。以下の精神に沿った実装になっているか確認:

- [ ] `console.warn` が **新規に追加** されている(既存コードの修正ではない)
- [ ] M4-00 の RecipeBuilder console.warn と **同等パターン** で実装されている(useEffect 構造、spy 方式、文言形式)
- [ ] 「将来課題 §8.3 警告ヘルパフック統一」を **本指示書で先取りしていない**(handover §8.3 で M5 以降または M7 と確定済み、過剰実装を避ける)

### 5.2 M4-00 との整合確認(最重要)

本指示書は M4-00 の双子作業。両者の実装が対称構造になっているか確認する:

- [ ] **useEffect 構造**: ModifiersEditor の useEffect が RecipeBuilder の useEffect と同型(依存配列 + 50 文字超判定 + console.warn 発火)
- [ ] **spy 方式**: `vi.spyOn(console, 'warn').mockImplementation(() => {})` パターンが両ファイルで一致
- [ ] **文言形式**: 「<フィールド名> が 50 文字を超えています: N 文字」の形式が両ファイルで一致(<フィールド名> 部分のみ異なる)
- [ ] **テスト 3 ケース構造**: ケース名・期待動作・実装パターンが両ファイルで対称
- [ ] **配置位置**: 既存 useEffect 群の末尾に独立ブロックとして追加されている(両ファイル共通)

### 5.3 retrospective-log §1 パターン A の再発防止

本指示書は M2-04 設計担当ミスの双子事例を清算するもの。製造担当が同パターンを再発させていないか確認:

- [ ] §3.4.1 で「console.warn 検出ゼロ」を確認した上で実装に着手している
- [ ] §3.4.2 で対象フィールドを **実態確認** してから実装している(過去指示書の記述を盲信していない)
- [ ] M4-00 のパターンを参考にしているが、コピペではなく ModifiersEditor の実態に合わせて適合している

---

## 6. コード品質・規約遵守

- [ ] CLAUDE.md §4 TypeScript 規約に準拠
- [ ] `console.warn` 以外の console 出力を本番コードに残していない(CLAUDE.md §10)
- [ ] テストファイル以外の場所で `vi.spyOn` 等の Vitest API を呼び出していない
- [ ] import 順序が CLAUDE.md §4 に従う
- [ ] 新規 import に未使用のものが含まれていない

---

## 7. 既存挙動の温存

### 7.1 ModifiersEditor.tsx の既存挙動

- [ ] flags / type / notes 編集が動作する
- [ ] 視覚的 UI 警告(赤ボーダー、文字数メッセージ)が動作する
- [ ] M2-04 で実装された他の機能が回帰していない

### 7.2 ModifiersEditor.test.tsx の既存テスト

- [ ] 既存ケースが全通過する
- [ ] 既存ケースのテスト名・期待値が変更されていない

### 7.3 M4-00 で追加された RecipeBuilder 側への影響

- [ ] `RecipeBuilder.tsx` に変更が入っていない
- [ ] `RecipeBuilder.test.tsx` に変更が入っていない
- [ ] M4-00 で追加された 3 ケースが引き続き全通過する

### 7.4 他コンポーネントへの影響

- [ ] `pnpm test` 全体で他コンポーネントのテストが回帰していない

---

## 8. ドキュメント・進捗ログ

- [ ] `docs/progress/progress-log.md` に M4-00b 完了報告が追記されている
- [ ] 完了報告に **M4-00 §3.4.2 で発見された隠れ P-01 清算の事実** が明記されている
- [ ] 完了報告に **§3.4 着手前確認結果**(特に §3.4.1 検出ゼロ、§3.4.2 対象フィールド名)が含まれている
- [ ] 完了報告に **M2-04 設計担当ミスの双子事例の清算** が記録されている(retrospective-log §1 パターン A の追跡)

---

## 9. 重大な問題の判定基準

以下のいずれかに該当する場合、**重大な問題** と判定し M4-00b 完了承認を保留する:

- §1.2 / §1.3 のチェック項目で 2 件以上の未充足がある
- §3.4.2 で特定した対象フィールド名が完了報告に明記されていない、または開発者の Plan Mode 合意なしに実装着手している
- 視覚的 UI 警告(M2-04 §4.1 実装)が削除・変更されている
- 既存ケース(ModifiersEditor / RecipeBuilder 双方)が回帰している
- M4-00 と異なる spy 方式が採用されている(同方式踏襲が望ましい、§3.4.4 / §4.2.2)
- M4-00 で完了済みの RecipeBuilder 関連ファイルに変更が入っている
- `console.log` などの不要な console 出力が本番コードに残されている

---

## 10. 軽微な問題の判定基準

以下に該当する場合、**軽微な問題** と判定し M4-00b 完了承認は許容するが、記録として残す:

- 警告文言の細部が指示書例と多少異なる(「50 文字を超えています」を含めば許容、§9.2 推測 OK 範囲)
- テストケース名が指示書例と異なるが、検証内容は §4.2.1 通り
- 対象フィールド名のラベル表記(コード内変数名と警告文言上の表記)が完全一致しない
- スパイのリセット方式が `mockRestore` か `restoreAllMocks` かで揺れているが、テストが全通過している

---

## 11. 質問・確認事項のフォーマット

レビュー中に判断が分かれる事項を発見した場合、以下のフォーマットで開発者に質問する:

```
## 質問: M4-00b §X.Y についての判断確認

### 状況
(発見した事象、該当ファイル・行)

### 案 1: 製造担当の実装通り承認
(理由・リスク)

### 案 2: 修正を要求
(修正方針・理由)

### レビュー担当の推奨
(案 1 または案 2、理由)
```

---

## 12. レビュー完了の判定

すべて以下を満たす場合、レビュー完了として開発者に「重大な問題なし」と報告する:

- §1 / §3 / §4 / §6 / §7 / §8 の機械的チェック項目がすべて通過
- §5 設計意図整合(隠れ P-01 清算、M4-00 との対称性、retrospective-log §1 パターン A 再発防止)が確認されている
- §9 重大な問題に該当する項目がゼロ
- §10 軽微な問題が存在する場合は記録されている

不通過の場合は §9 / §10 のどちらに該当するかを明示し、§11 フォーマットで開発者に質問または修正要求を行う。

---

*以上*
