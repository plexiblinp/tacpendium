M7-06 追補（A-1 / A-2）のレビューをお願いします。対象は2件のみ（設定画面 LAN トグル /
起動案内 stdout 出力）。いずれも設計書に定義済みの要件で CHANGE 不要・スキーマ変更なしが前提。
レビューモデル: Sonnet 4.6。

【準備】
- 対象 diff を git で把握。Plan Mode 確定事項（A-2 の永続化経路・確認ダイアログ、
  A-1 の stdout 出力方式）を確認。
- 根拠: DES-005 §16 ネットワーク / DES-002 §3.4（LANモード切替）、NFR303/304（exe起動だけで
  使える）、SUPP-001 §5.6（ログ方針）、CLAUDE.md §10（fmt.Println 禁止の趣旨）。
- grep 系（fmt.Println の使い方）は自分で実行し結果を報告書に明示。

【A-2: 設定画面 LAN 共有モード切替トグル】
- [ ] 設定画面ネットワークから LAN ⇄ ローカルを切替できる（表示のみでない）
- [ ] 切替操作時に確認ダイアログ（DES-002 §3.4 の公開警告文を踏襲、明示同意）。
      M7-05 で AlertDialog 化済みの仕組みを使い、window.confirm を新規に持ち込んでいない
- [ ] mode 変更が再起動要（restartRequired）として扱われ、UI に表示される
- [ ] 永続化がウィザード Step05（server.mode）と同じ経路を再利用（独自経路を新設していない）
- [ ] config.toml 手編集なしで LAN 化できる（A-2 の主目的）
- [ ] DES-005 §16 / DES-002 §3.4 の規定どおり（独自解釈で挙動を変えていない）

【A-1: 起動時案内の stdout 出力】
- [ ] 起動案内専用の stdout 出力関数（明確に命名、例 printStartupNotice）として実装され、
      散発的な fmt.Println をコード各所に撒いていない
- [ ] 出力内容にアクセス URL（PC: localhost:実ポート / LAN 時は LAN URL 一覧）+ FW/AV ヒントを含む
- [ ] L-04 フォールバックの実ポートに追従する（固定ポート決め打ちでない）
- [ ] 既存の slog 運用ログ（ファイル出力）が維持されている（stdout と二重 = ログは記録、
      stdout はユーザー案内）。slog 側を壊していない
- [ ] CLAUDE.md §10 の趣旨（散発的デバッグ出力の残留防止）に反していない
      = 意図的なユーザー向け起動案内に限定され、デバッグ出力の混入がない

【既存非破壊・スコープ】
- [ ] go test ./... / pnpm test 全パス
- [ ] dev 構成（Vite + proxy）が引き続き動作（embed/配布構成・dev の両系統が壊れていない）
- [ ] スキーマ変更なし、スコープ外（新機能 / フェーズ2-3先行 / A-1・A-2 以外）に手が入っていない

【重大問題（完了承認を妨げる）】
- LAN トグルが表示のみで実際に切替できない / 確認ダイアログがない / window.confirm 復活
- 永続化が独自経路で config 不整合を起こす
- 起動案内が実ポートに追従しない（利用者が LAN URL を把握できない）
- 散発的 fmt.Println の撒き散らし / slog 運用ログの破壊
- テスト失敗 / dev 構成の破壊 / スキーマ変更 / スコープ外着手

【軽微問題（持ち越し許容）】
- 起動案内の文言・整形の細部
- restartRequired の表示文言

【質問】判断に迷ったら Plan Mode CLI 経由で設計担当へ（直接 Q&A なし）。
「観点 + ファイル/行 + 事実 + 迷っている点」を明記。特に
(a) LAN トグルの永続化が独自経路、(b) stdout 出力が slog 趣旨/§10 と整合するか疑義、
(c) DES-005/DES-002 規定の解釈が割れる場合 は重大問題候補として明示報告。

【完了判定】上記すべて満たせば「完了承認可」。設計反映（SUPP-001 §5.6 追記等）は
設計担当作業のため、製造側は実装 + progress-log 記録まででよい。
