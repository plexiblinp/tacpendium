# M7-03 機械レビューチェックリスト

| 項目 | 内容 |
|------|------|
| 対応指示書 | M7-03: レスポンシブ仕上げ(R-3 AddComboToCompareModal キャラクターフィルタ追加 + 既存固定幅 3 件対応 + R-1 / R-2 解消確認) v1.0.0 |
| バージョン | 1.0.1 |
| 推奨モデル | Sonnet 4.6(機械レビュー)|
| 役割 | M7-03 の実装が指示書通りか + 構造的問題がないかを機械的に検査する。**実機テスト + E2E シナリオ実行は別途実施が必要**(`m7-03-e2e-scenarios.md` で開発者が実行)|
| 作成日 | 2026-05-31 |

## 更新履歴

| バージョン | 更新日 | 更新内容 |
|-----------|--------|----------|
| 1.0.0 | 2026-05-31 | 初版作成。M7-03 指示書 v1.0.0 に対応。M7-02 機械レビューチェックリスト 14 章を 8 章に圧縮(R-3 単一画面 UI 追加 + 既存固定幅 3 件対応 + R-2 解消済み確認の小規模スコープに合わせ、Toast 移行 / List API 拡張 / ロジックフック分離 / C-2/3/4 解消 / Q10〜Q22 章を削除)|
| 1.0.1 | 2026-06-01 | M7-03 完了承認(2026-06-01)に伴う事後履歴整合修正。製造担当伝達 1(指示書 §4.1.1 Props 誤り = `selectedCharacterId` + `characters` Props 不要)+ Q28 (A) 確定(モーダル再オープン時の前回選択キャラ保持)を §3.1 / §3.3 に反映 |

---

## 0. レビュー実施前の確認

- [ ] M7-03 指示書 v1.0.0 を読了している
- [ ] M7-02 完了承認(2026-05-31)+ M7-RESEARCH-02 完了承認(2026-05-27)+ CHANGE-017 反映(DES-005 v2.10.0)を確認している
- [ ] playbook v1.9.0 §4.6 / §4.9 / §8.4.2 / §17.2 を確認している
- [ ] architecture-patterns v1.0.9 §1(プレ層/ロジック層分離)+ §1.1(queryKey 規約)+ §1.2 shadcn/ui 統一導入パターン + §7 Dialog scroll 制約パターン(正式化済み)を確認している
- [ ] retrospective-log v1.0.25 §1 構造的アンチパターン + §6.6 M7 期間担当総括 + §6.7 / §6.8 / §6.9 / §6.10 運用知見 + §7.2 M7-3 候補クローズ判断を確認している
- [ ] M7-overview v1.0.1 §2.9 R-1 解消基準再定義 + §4.5 M7-03 スコープを確認している
- [ ] **Plan Mode 質問書ファイル(`m7-03-plan-mode-questions.md` 等、必須項目 5 件)+ 開発者一括ご回答ファイル** を確認している(playbook v1.9.0 §8.4.2 = 必須項目 2 件以上で質問書ファイル方式必須、CLI ウィザード逐次方式は本指示書では禁止)

---

## 1. ファイル一覧チェック(指示書 §2.1 / §2.2 / §2.3 / §2.4)

### 1.1 新規作成ファイル(§2.1)

- [ ] **なし**(本指示書は既存ファイル修正のみ、新規ファイル作成は対象外)

### 1.2 修正ファイル(§2.2)

#### A. R-3 解消関連
- [ ] `web/src/features/combo/components/AddComboToCompareModal.tsx` に以下が追加 / 修正されている:
  - [ ] `CharacterSelector` のインポート文(M7-01 で shadcn/ui Select 化済みの既存コンポーネント再利用)
  - [ ] `useCharacters` フックのインポート + 使用
  - [ ] キャラ選択 `useState`(初期値 = `INITIAL_CHARACTER_ID` 定数を温存)
  - [ ] `useCombos(open ? { characterId } : { characterId: -1 })` の `characterId` 引数が選択 state に連動
  - [ ] キャラセレクタ UI 配置(必須項目 1 確定どおり = 推奨「モーダル上部に『キャラクター:』ラベル付き」)
  - [ ] キャラ切替時のローディング表示(必須項目 2 確定どおり = 推奨「TanStack Query 標準ローディング UX 踏襲」)
  - [ ] Props 契約維持(`open` / `onOpenChange` / `onAdd` / その他、呼び出し元 ComparePage への影響なし)
  - [ ] Dialog scroll 制約パターン継続(M7-02 で適用済みの内部リスト要素 `max-h-[60vh] overflow-y-auto` 維持)
- [ ] `web/src/features/mycombo/components/CharacterSelector.tsx` への変更なし(M7-01 確立物、再利用のみ)
- [ ] `useCharacters` フックへの変更なし(M3-04 等で確立済み、再利用のみ)

##### 関連テスト
- [ ] `AddComboToCompareModal.test.tsx`(既存、存在する場合)に以下のテストケースが追加されている:
  - [ ] キャラセレクタが表示される
  - [ ] キャラ切替で `useCombos` クエリ paramsが更新される
  - [ ] 初期キャラ(`INITIAL_CHARACTER_ID`)でモーダルオープン時にコンボ候補が表示される
  - [ ] 他キャラに切替でそのキャラのコンボが表示される
- [ ] 既存テスト(キャラ ID 固定前提のテストがあれば)が修正済み

#### B. 既存固定幅 3 件対応関連
- [ ] §3.4.4 view 確認で特定された 3 件(`max-w-[200px]` × 2 + `min-w-[160px]` × 1)に Plan Mode 必須項目 3 確定どおりの修正が適用されている:
  - [ ] 件 1(`max-w-[200px]`):候補 (A) / (B) / (C) / (D) のいずれかが個別適用
  - [ ] 件 2(`max-w-[200px]`):同上
  - [ ] 件 3(`min-w-[160px]`):同上
- [ ] 候補 (A) 採用箇所: 固定幅削除 + Tailwind ユーティリティ(`flex-1` / `w-full` 等)で柔軟化
- [ ] 候補 (B) 採用箇所: `sm:max-w-[200px]` 等で sm 以上のみ適用
- [ ] 候補 (C) 採用箇所: `min-w-0` + `truncate` で長文時省略表示
- [ ] 候補 (D) 採用箇所: 現状維持(本 M7-03 では触らない)

#### C. R-2 解消済み確認(新規実装なし)
- [ ] **コード変更なし**(M4-04 で既に解消済みの追認のみ)
- [ ] 製造担当の完了報告に「R-2 = M4-04 で既に解消済み、本 M7-03 で新規実装なし、view 確認のみ実施」が記録されている
- [ ] §3.4.9 表 B「R-2 既存実装解消済み確認表」の 6 項目すべてに ○ / × が埋まっている

### 1.3 スコープ外への変更がないこと(§2.3、最重要)

- [ ] **設計書本体(REQ-001 / DES-001〜DES-006)に変更がない**(M7-03 製造担当の対象外、最重要)
- [ ] **補足資料(SUPP-001 / playbook / architecture-patterns / handover / CLAUDE.md / change-number-registry / retrospective-log)に変更がない**(M7-03 製造担当の対象外)
- [ ] **新規 CHANGE 通知書が起票されていない**(本指示書スコープ内では起票見込みなし)
- [ ] **バックエンド変更が含まれていない**(本 M7-03 はフロント変更のみ、`go test ./...` 念のため実行で 0 件変更確認)
- [ ] **本格スマホ UI(カード形式 / アコーディオン / ボトムシート / スワイプ)が実装されていない**(フェーズ 3 以降、M7-overview §2.9 案 Y 採用継続)
- [ ] **`lg:` / `xl:` / `2xl:` ブレークポイントが新規導入されていない**(フェーズ 2 以降、本 M7-03 では `sm:` / `md:` の既存使用範囲内のみ)
- [ ] **テーブル系 4 件の `overflow-x-auto` 横スクロール廃止が実装されていない**(フェーズ 3 以降)
- [ ] **Header 内追加要素(プリセット切替 / 言語切替 / ユーザー表示 / モード表示)が実装されていない**(フェーズ 2 以降)
- [ ] **バックエンドキャッシュ計算(`drive_gauge_consumed_total` 等の自動集計関数)が実装されていない**(M7-04 Q11 γ-2 確定)
- [ ] **VAL-C06 / VAL-C07 のバリデーション復活が実装されていない**(M7-04)
- [ ] **`moves` テーブルに `drive_gauge_increase` / `sa_gauge_increase` カラム追加マイグレーションが含まれていない**(M7-04)
- [ ] **バグ #5 修正(仮登録コンボ本登録昇格時エラー表示)が含まれていない**(M7-04 Q20 (Y))
- [ ] **`window.confirm` 全件 grep + AlertDialog 化が含まれていない**(M7-05 Q19 (W))
- [ ] **バグ #6 修正(レシピなしセットプレイ登録時エラー表示なし)が含まれていない**(M7-05 Q21 (BB))
- [ ] **リファクタ + 統合 E2E + LAN 検証 + フェーズ 1 完了判定が含まれていない**(M7-05)
- [ ] **`m7-to-phase2-handover.md` が作成されていない**(M7-05)
- [ ] **CharacterSelector コンポーネント本体への変更なし**(M7-01 確立物、再利用のみ)
- [ ] **`useCharacters` フックへの変更なし**(M3-04 等で確立済み、再利用のみ)
- [ ] **M7-01 / M7-02 確立物への変更なし**:
  - [ ] Header.tsx / Footer.tsx
  - [ ] shadcn/ui 既存コンポーネント全件(`components/ui/` 配下、M7-01 + M7-02 で導入済みの 20 件程度)
  - [ ] Header 非表示 4 ページ(ComboEditorPage / SetupEditorPage / WizardPage / HomePage)= 案 A 確定継続
  - [ ] システムタグ表示制御(M7-01 追加タスク確立)
  - [ ] 系統 A 後半 9 件(M7-02 確立)
  - [ ] ロジックフック 4 件(D-1 〜 D-4、M7-02 確立)
  - [ ] Dialog scroll 制約パターン(M7-02 確立、AddComboToCompareModal にも継続適用)
  - [ ] react-hook-form + zodResolver + shadcn/ui Form 統合パターン(M7-02 確立)
  - [ ] native `<select>` 13 件維持(M7-02 Q16 (P) 確定、Radix Select の `value=""` 制約による例外)
  - [ ] ComboDetailMetadata 表示項目変更(M7-02 Q10 (X) 確定 = 開始残量表示)
  - [ ] Q22 案 A 修正(`useUpdateComboWithKeyChange` の onSuccess)
- [ ] **バックエンドの `drive_gauge_consumed_total` / `sa_gauge_consumed_total` カラム + API レスポンスフィールド + フロント型定義 = 温存**(Q15 確定継承)
- [ ] **ModifiersEditor の `console.warn` 残存(P-01)が継続温存**
- [ ] **PromoteToFinalButton の独立コンポーネント化が実施されていない**(フェーズ 2 以降検討)
- [ ] **i18n 英語ロケール追加が実装されていない**(本 M7-03 では i18n キー変更も不要)
- [ ] **`INITIAL_CHARACTER_ID` 定数が温存**(削除されていない)
- [ ] 既存 routes 定義の変更なし

### 1.4 例外条項適用箇所のチェック(§2.4)

- [ ] (a) AddComboToCompareModal への CharacterSelector + useCharacters 統合(§2.2 A 既明示の範囲内)
- [ ] (b) AddComboToCompareModal の `useState`(キャラ選択 state)追加(§2.2 A 既明示)
- [ ] (c) 既存固定幅 3 件の Tailwind クラス修正(§2.2 B 既明示、Plan Mode 必須項目 3 確定方針)
- [ ] (d) 関連テストの追加 / 修正(§2.2 A / §2.2 B 既明示)
- [ ] **依存追加なし**(本 M7-03 は既存ライブラリのみで実装可能、依存追加見込みなし、許容しない範囲)

---

## 2. 着手前確認結果のチェック(指示書 §3.4)

製造担当の作業報告に以下の確認結果が **すべて含まれている** こと:

### 2.1 §3.4.1 既存ファイル構造の確認
- [ ] `AddComboToCompareModal.tsx` / `CharacterSelector.tsx` / `useCharacters` / `INITIAL_CHARACTER_ID` 定数の場所 / `ComparePage.tsx` の現状確認結果が記録されている

### 2.2 §3.4.2 R-3 既存実装の view 確認(3 点セット)
- [ ] AddComboToCompareModal の構造 + 責務 + Props の 3 点セット確認結果が記録されている(`useQuery`(`useCombos`)現状実装、`INITIAL_CHARACTER_ID` 固定、UI 構造、内部 `useState` の有無 + 命名)

### 2.3 §3.4.3 R-2 既存実装の view 確認(解消済み事実の追認)
- [ ] ComboDetailMetadata.tsx 行 110-113 / 118-121 / 86-89 / 102-105 の view 確認
- [ ] CompareTable.tsx 行 29-35 / 97-100 / 38-45 / 93-96 の view 確認
- [ ] ComboEditor.tsx 行 148-152 / 171-175 / 515-519 / 548-552 の view 確認
- [ ] M7-RESEARCH-02 §4.4 (a) の事実列挙どおりで動作中であることが報告されている

### 2.4 §3.4.4 既存固定幅 3 件の view 特定
- [ ] `grep -rn "max-w-\[200px\]" web/src/` 結果(2 件特定)
- [ ] `grep -rn "min-w-\[160px\]" web/src/` 結果(1 件特定)
- [ ] 各 3 件のファイル名 + 行番号 + 対応する UI 要素 + 周辺レイアウトコンテキスト + スマホ画面幅シミュレーション結果が記録されている

### 2.5 §3.4.5 Tailwind ブレークポイント使用状況の確認(参考、新規導入なし)
- [ ] `grep -rnE "\b(sm|md|lg|xl|2xl):" web/src/` 結果が記録されている
- [ ] M7-RESEARCH-02 特記事項 1 と整合(`lg:` / `xl:` / `2xl:` 使用ゼロ確認)
- [ ] 本 M7-03 で新規導入なし(`sm:` / `md:` の既存使用範囲内のみ許容)

### 2.6 §3.4.6 ComparePage(呼び出し元)の view 確認
- [ ] AddComboToCompareModal の呼び出し方確認
- [ ] Props 渡しの確認(本 M7-03 では呼び出し元への影響なしを確認)

### 2.7 §3.4.7 関連テストの現状確認
- [ ] `AddComboToCompareModal.test.tsx`(存在する場合)の view 確認
- [ ] `CharacterSelector.test.tsx`(存在する場合)の view 確認(M7-01 確立物、変更不要前提)

### 2.8 §3.4.8 スコープクリープ警戒
- [ ] 本 M7-03 着手前 view 確認時に発見した小規模スマホ表示課題の有無 + 対応要否協議結果
- [ ] 「本格スマホ UI 整備に該当する」課題が発見された場合の **フェーズ 3 以降送り** 確認

### 2.9 §3.4.9 対応表 3 表完全埋め確認(最重要)
- [ ] 表 A: R-3 AddComboToCompareModal キャラクターフィルタ追加対応表(全 8 行埋め)
- [ ] 表 B: R-2 既存実装解消済み確認表(全 6 項目 ○/× で埋め)
- [ ] 表 C: 既存固定幅 3 件対応表(3 件すべての確定方針埋め)

### 2.10 §3.4.10 Plan Mode 必須項目 5 件確定確認(最重要)
- [ ] **質問書ファイル方式で実施されている**(playbook v1.9.0 §8.4.2、必須項目 2 件以上で質問書方式必須)
- [ ] **CLI ウィザード逐次方式が使われていない**(本指示書スコープでは禁止、retrospective-log v1.0.25 §6.6 M7-2 由来の改善試行)
- [ ] 必須項目 1: CharacterSelector の AddComboToCompareModal 統合配置 + UI 詳細
- [ ] 必須項目 2: キャラ切替時のコンボ候補リストの動作
- [ ] 必須項目 3: 既存固定幅 3 件の対応方針確定(各 3 件個別)
- [ ] 必須項目 4: R-2 解消済み確認の Plan Mode 計画提示
- [ ] 必須項目 5: AddComboToCompareModal のテスト追加方針

---

## 3. R-3 解消の妥当性(指示書 §4.1)

### 3.1 CharacterSelector 統合
- [ ] `CharacterSelector` が `web/src/features/mycombo/components/CharacterSelector.tsx` からインポート + 使用されている(M7-01 で shadcn/ui Select 化済みの既存コンポーネント再利用)
- [ ] CharacterSelector への変更なし(新規作成 / 改造なし)
- [ ] Props 契約: `selectedCharacterId={number}` / `onChange={(characterId: number) => void}` の **2 件のみ**(製造担当伝達 1 由来、`characters` Props は不要 = CharacterSelector が内部で `useCharacters()` を自前呼出する設計)
- [ ] **AddComboToCompareModal 側で `useCharacters` の import + 呼出が含まれていない**(CharacterSelector 内部で呼出されるため、AddComboToCompareModal 側での呼出は重複かつ不要)

### 3.2 useCharacters フック使用
- [ ] `useCharacters` フックが正しくインポート + 使用されている
- [ ] フック自体への変更なし(M3-04 等で確立済み、再利用のみ)

### 3.3 useState + useCombos クエリ連動
- [ ] キャラ選択 `useState` の初期値が `INITIAL_CHARACTER_ID` 定数を使用
- [ ] `useCombos` クエリの `characterId` 引数が選択 state に連動(キャラ切替で新たなキャラのコンボが表示)
- [ ] キャラ切替時のローディング表示が必須項目 2 確定どおり
- [ ] **モーダル再オープン時に前回選択キャラが保持される**(Q28 (A) 確定方針 2026-06-01、`useEffect` による `open` トリガーリセット **なし** が正、リセット実装が含まれている場合は逆に方針違反)

### 3.4 UI 配置 + Props 契約維持
- [ ] キャラセレクタ UI 配置が必須項目 1 確定どおり(推奨「モーダル上部に『キャラクター:』ラベル付き」)
- [ ] Props 契約維持: 呼び出し元 ComparePage への影響なし
- [ ] Dialog scroll 制約パターン継続(M7-02 §7 確立、内部リスト要素の `max-h-[60vh] overflow-y-auto` 維持)
- [ ] `INITIAL_CHARACTER_ID` 定数の温存(削除されていない)

---

## 4. 既存固定幅 3 件対応の妥当性(指示書 §4.2)

### 4.1 個別判断の遵守
- [ ] 各 3 件の対応方針が表 C 確定どおり個別適用されている(機械的な同一方針適用はなし、各箇所のドメイン文脈次第)
- [ ] 候補 (A) / (B) / (C) / (D) の採用根拠が表 C で記録されている

### 4.2 候補別の実装妥当性

#### 候補 (A) 採用箇所(固定幅削除 + Tailwind ユーティリティで柔軟化)
- [ ] 固定幅クラス削除済み
- [ ] `flex-1` / `w-full` 等の Tailwind ユーティリティで妥当な形で置換

#### 候補 (B) 採用箇所(sm 以上のみ適用)
- [ ] `sm:max-w-[200px]` 等で sm 以上のみ適用
- [ ] スマホ画面幅(390px 等)で制限なし表示

#### 候補 (C) 採用箇所(min-w-0 + truncate)
- [ ] `min-w-0` + `truncate` で長文時省略表示

#### 候補 (D) 採用箇所(現状維持)
- [ ] 該当箇所のコード変更なし

---

## 5. R-2 解消済み確認の妥当性(指示書 §4.3)

### 5.1 view 確認のみ実施
- [ ] **コード変更なし**(M4-04 で既に解消済みの追認のみ)
- [ ] 製造担当の完了報告に「R-2 解消済み確認、新規実装なし」が明示
- [ ] 表 B 6 項目すべてに ○ / × が埋まっている

### 5.2 動作確認結果
- [ ] ComboDetailMetadata: シミー(`okiShimmyNeutralTech` / `okiShimmyBackTech`)+ DR 有(`okiMeatyNeutralTechThrowDr` / `okiMeatyBackTechThrowDr`)表示が動作中
- [ ] CompareTable: 同上の表示が動作中(`formatOkiDrNone` / `formatOkiDrYes` 関数経由)
- [ ] ComboEditor: 同上の編集可能フィールドが動作中

---

## 6. テスト要件の充足(指示書 §5)

### 6.1 新規追加テスト
- [ ] `AddComboToCompareModal.test.tsx` に §2.2 A 末尾の 4 件のテストケース追加 + 全件 PASS
- [ ] 既存テスト修正後 + 全件 PASS

### 6.2 実行結果
- [ ] `pnpm test --run` 全件 PASS(423〜455 件前後)
- [ ] `pnpm build` + `pnpm dev` エラーなし
- [ ] `go test ./...` 全件 PASS(バックエンド変更なし確認用、念のため実行)
- [ ] `go build ./...` エラーなし(同上)

### 6.3 単体テスト件数推移
- [ ] M7-02 完了時 420〜450 件 → M7-03 完了時 423〜455 件前後(+3〜5 件)

---

## 7. 構造的アンチパターン再発なし(retrospective-log v1.0.25 §1 + §6 + §7.2 クローズ判断遵守)

### 7.1 retrospective-log §1 構造的アンチパターン
- [ ] §1.1 統合エディタの直接編集パターン: 該当なし(本 M7-03 は単一画面 UI 追加のみ)
- [ ] §1.2 実コード確認の省略: §3.4.2〜§3.4.7 で全対象の view 確認完了
- [ ] §1.3 ジェネリック化のしすぎ: CharacterSelector 既存コンポーネントの再利用のみ(過剰汎化なし)
- [ ] §1.4 並行作業の同期不整合: 本指示書は単独着手
- [ ] §1.5 Migration の前置忘れ: 該当なし(本指示書はフロント変更のみ、SQL 変更なし)
- [ ] §1.6 設計書本体・既存実装の整合確認漏れ: 対応表 3 表で完全網羅、M6-3/4/5/6 + M7-5 連続発生の再発なし

### 7.2 retrospective-log §6 各反省踏襲
- [ ] §6.6 M7 期間担当総括: M7-03 配分課題(R-1 / R-2 / R-3 + 既存固定幅 3 件)の処理状況が明示
- [ ] §6.9 CHANGE-018 起票運用知見: 本 M7-03 では新規 CHANGE 起票なし、運用維持
- [ ] §6.10 Plan Mode 反問運用知見: 本 M7-03 でも製造担当からの Plan Mode 反問を歓迎する運用継続(反問があれば本 §16 経由で記録)

### 7.3 retrospective-log §7.2 M7-3 候補クローズ判断遵守
- [ ] M7-02 完了承認時にクローズ判断済み(構造的教訓昇格不要)
- [ ] 本 M7-03 で「マイルストーン間の実装パターン分裂」事象が **発生していない**

### 7.4 M7-5 反省踏襲(設計書本体節タイトル多モード性確認)
- [ ] 本 M7-03 は設計書本体改訂を含まない(M7-5 のような節タイトル多モード性見落としは発生しえない)
- [ ] DES-005 §5.8 コンボ比較の規定整合は M7-RESEARCH-02 + 本 M7-03 で確認完了

---

## 8. 重大判定 / 軽微判定

### 8.1 重大欠陥(完了承認保留)

以下に該当する場合は **重大欠陥** = 機械レビュー完了承認保留 + 製造担当に再実装依頼:

- 設計書本体(REQ-001 / DES-001〜DES-006)が改変されている(CHANGE 通知書未起票での改変)
- 補足資料(SUPP-001 / playbook / architecture-patterns / handover / CLAUDE.md / change-number-registry / retrospective-log)が改変されている
- 新規 CHANGE 通知書が起票されている(本指示書スコープ内では起票見込みなし)
- **バックエンド変更が含まれている**(本 M7-03 はフロント変更のみ、重大なスコープ侵食)
- 既存機能(M1〜M6 + M7-01 + M7-02)の回帰がある
- 本格スマホ UI(カード形式 / アコーディオン / ボトムシート / スワイプ)が実装されている(フェーズ 3 以降侵食、重大)
- `lg:` / `xl:` / `2xl:` ブレークポイントが新規導入されている(フェーズ 2 以降侵食、重大)
- テーブル系 4 件の `overflow-x-auto` 横スクロール廃止 / カード形式変換が実装されている(フェーズ 3 以降侵食、重大)
- Header 内追加要素(プリセット切替 / 言語切替 / ユーザー表示 / モード表示)が実装されている(フェーズ 2 以降侵食、重大)
- バックエンドキャッシュ計算実装 / VAL-C06/C07 復活 / `moves` カラム追加 / バグ #5 修正が含まれている(M7-04 スコープ侵食、重大)
- `window.confirm` 全件解消 / バグ #6 修正 / 統合 E2E / LAN 検証 / `m7-to-phase2-handover.md` 作成が含まれている(M7-05 スコープ侵食、重大)
- M7-01 / M7-02 確立物への変更がある(Header.tsx / Footer.tsx / shadcn/ui 既存コンポーネント本体 / Header 非表示 4 ページ / システムタグ表示制御 / 系統 A 後半 / ロジックフック 4 件 / Dialog scroll 制約パターン本体 / native `<select>` 13 件 / Q22 案 A 修正等、重大)
- バックエンドの `drive_gauge_consumed_total` / `sa_gauge_consumed_total` カラム + API レスポンスフィールド + フロント型定義の削除が含まれている(Q15 温存方針違反、重大)
- CharacterSelector コンポーネント本体への変更(M7-01 確立物変更、重大)
- `useCharacters` フックへの変更(M3-04 等確立物変更、重大)
- `INITIAL_CHARACTER_ID` 定数の削除(温存方針違反、重大)
- 依存追加が含まれている(本 M7-03 は既存ライブラリのみで実装可能、依存追加見込みなしの方針違反)
- `pnpm test --run` または `go test ./...` で失敗テストが存在
- `pnpm build` / `pnpm dev` / `go build ./...` でエラー発生
- 対応表 3 表(§3.4.9 A〜C)が **完全に埋まっていない** 状態で Plan Mode 計画提示
- Plan Mode 必須項目 5 件のいずれかが **未確定** のまま実装着手
- **Plan Mode 質問書ファイル方式が実施されていない**(必須項目 2 件以上で必須、playbook v1.9.0 §8.4.2、CLI ウィザード逐次方式は本指示書では禁止)
- R-2 解消済み確認で M7-RESEARCH-02 §4.4 (a) と異なる事実(M4-04 以降の追加変更で動作しない状態等)が発見されたのに、Plan Mode 停止 → 開発者協議なしで実装着手
- **CharacterSelector に `characters` Props または `selectedId` Props を渡している**(Props 誤り、製造担当伝達 1 由来、retrospective-log v1.0.26 §6.6 M7-14、重大)
- **AddComboToCompareModal 側で `useCharacters` の import + 呼出が含まれている**(CharacterSelector 内部で呼出される設計、二重呼出は無駄、軽微〜重大)
- **モーダル再オープン時に `INITIAL_CHARACTER_ID` にリセットする `useEffect` が含まれている**(Q28 (A) 確定方針違反、前回選択キャラ保持が正、重大)

### 8.2 軽微欠陥(完了承認可、改善推奨)

以下は軽微な問題で、完了承認は可だが次回マイルストーン以降の改善対象として記録:

- §1.4 例外条項適用箇所の理由明記が不十分
- Tailwind クラス順序揺れ
- キャラセレクタのラベル文言が必須項目 1 確定方針と微妙に異なる(機能に影響なければ軽微)
- 既存固定幅 3 件の対応で確定方針と異なる候補が採用されているが結果的に同等の UX が実現されている(機能に影響なければ軽微)
- AddComboToCompareModal のテストの assert メッセージが指示書記載例と異なる(網羅性が確保されていれば軽微)
- 完了報告書の節構成 / 文体が過去マイルストーンと完全一致しない

### 8.3 質問フォーマット(レビュー時の指摘形式)

```
[Major / Minor] <該当節>: <観点>
該当ファイル: <ファイルパス + 行番号>
事実: <コードの状態>
判定根拠: <指示書のどの節 + retrospective-log のどのアンチパターン>
修正案: <具体提案>
```

---

## 9. レビュー報告書フォーマット

機械レビュー完了時に以下を含む報告書を `docs/progress/m7-03-review.md` として出力:

```markdown
# M7-03 機械レビュー報告書

## 1. レビュー実施日
2026-XX-XX

## 2. レビュー結果サマリ
- ✅ / ⚠️ / ❌ §1 ファイル一覧(新規なし + 修正 + スコープ外無変更 + 例外条項)
- ✅ / ⚠️ / ❌ §2 着手前確認結果(§3.4.1〜§3.4.10、特に §3.4.9 対応表 3 表 + §3.4.10 必須項目 5 件)
- ✅ / ⚠️ / ❌ §3 R-3 解消の妥当性
- ✅ / ⚠️ / ❌ §4 既存固定幅 3 件対応の妥当性
- ✅ / ⚠️ / ❌ §5 R-2 解消済み確認(新規実装なし確認)
- ✅ / ⚠️ / ❌ §6 テスト要件
- ✅ / ⚠️ / ❌ §7 構造的アンチパターン再発なし

## 3. 対応表 3 表(§3.4.9)+ Plan Mode 必須項目 5 件(§3.4.10)の確認(最重要)
- 表 A〜表 C: ○ / ×(各)
- Plan Mode 必須項目 1〜5(質問書ファイル方式で実施): ○ / ×
- CLI ウィザード逐次方式の不採用: ○ / ×

## 4. スコープ外への変更がないことの確認(最重要)
- 設計書本体 / 補足資料の無変更: ○ / ×
- バックエンド変更なし: ○ / ×
- 本格スマホ UI / `lg:` / `xl:` / `2xl:` 新規導入なし: ○ / ×
- M7-04 / M7-05 / フェーズ 2 / フェーズ 3 スコープに該当する変更なし: ○ / ×
- M7-01 / M7-02 確立物への変更なし: ○ / ×
- 新規 CHANGE 通知書起票なし: ○ / ×
- 依存追加なし: ○ / ×

## 5. 検出した問題

### 5.1 重大な問題(完了承認保留)
(該当する場合、§8.1 判定基準のどれに該当するか明記)

### 5.2 軽微な問題(完了承認可、改善推奨)
(該当する場合、§8.2 判定基準のどれに該当するか明記)

### 5.3 改善提案(次回マイルストーン以降)
(該当する場合、M7-04 / M7-05 / フェーズ 2/3 のいずれかのスコープへの送り先を明示)

## 6. 制約事項
- 本レビューは静的コードレビュー。**実機テスト + E2E 動作確認は `m7-03-e2e-scenarios.md` で開発者が実施**(本機械レビュー対象外、playbook §14)
- 動作確認シナリオの手動確認は開発者の責任範囲

## 7. 完了承認判定
- ✅ 完了承認可 / ⚠️ 条件付き承認(改善後) / ❌ 完了承認保留(重大な問題あり)
```

---

*以上、M7-03 機械レビューチェックリスト v1.0.0*
