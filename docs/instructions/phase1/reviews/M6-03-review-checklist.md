# M6-03 機械レビューチェックリスト

| 項目 | 内容 |
|------|------|
| 対応指示書 | M6-03: スマホ専用ホーム画面 + スマホフッター(共通 UI 要素)v1.0.0 |
| バージョン | 1.0.0 |
| 推奨モデル | Sonnet 4.6(機械レビュー)|
| 役割 | M6-03 の実装が指示書通りか、構造的問題がないかを機械的に検査する。**実機テストは別途実施が必要**(playbook §14、M4-02 E2E 由来運用知見: レビュー完了承認 ≠ サブマイルストーン完了承認)|
| 作成日 | 2026-05-23 |

## 更新履歴

| バージョン | 更新日 | 更新内容 |
|-----------|--------|----------|
| 1.0.0 | 2026-05-23 | 初版作成。M6-03 指示書 v1.0.0 に対応 |

---

## 0. レビュー実施前の確認

- [ ] M6-03 指示書 v1.0.0 を読了している
- [ ] M6-02 完了報告(2026-05-23、v1.0.2)を確認している(ヘッダ「設定」リンク追加 SettingsPage のみ反映、他ページは別指示扱い)
- [ ] M6-overview v1.0.0 §4.4 を確認している
- [ ] DES-005 v2.8.0 §4.1 ヘッダ / §4.2 フッター / §4.4 ブレークポイント / §5.2 スマホホーム を確認している
- [ ] SUPP-001 v1.14.0 §4.6 レスポンシブ対応タイミングを確認している
- [ ] **architecture-patterns.md v1.0.4 §1 プレゼンテーション層/ロジック層分離パターン + §1.1 TanStack Query queryKey 規約**(`useRecentCombos` 新設時に踏襲)を確認している
- [ ] **retrospective-log.md v1.0.20** §1 構造的アンチパターン + §6.1 M6-1〜M6-6 + §6.2 構造的分類 + §6.3 教訓(対応表のスコープを UI 全範囲に拡張、最重要)を確認している
- [ ] m6-design-session-handover §4(M6-03 指示書作成方針、案 C 採用根拠)を確認している

---

## 1. ファイル一覧チェック(指示書 §2.1 / §2.2)

### 1.1 新規作成ファイル(§2.1)

#### 1.1.1 フロントエンド新規作成

- [ ] `web/src/pages/HomePage.tsx` が新設されている
- [ ] `web/src/components/Footer.tsx` が新設されている
- [ ] `web/src/hooks/useRecentCombos.ts` が新設されている
- [ ] **(条件付き)** `web/src/hooks/useIsMobile.ts` が新設されているか、または Tailwind ブレークポイントのみで完結している(§3.4.8 必須項目 1 で確定)

#### 1.1.2 テスト新規作成

- [ ] `web/src/pages/HomePage.test.tsx` が新設されている
- [ ] `web/src/components/Footer.test.tsx` が新設されている
- [ ] `web/src/hooks/useRecentCombos.test.ts` が新設されている
- [ ] **(条件付き)** `web/src/hooks/useIsMobile.test.ts` が新設されている(useIsMobile を実装した場合のみ)

### 1.2 修正ファイル(§2.2)

- [ ] `web/src/App.tsx` にスマホ判定 + リダイレクト分岐拡張が実装されている(M6-02 既存の `isInitialized: false` → `/wizard` 挙動温存 + スマホ → `/`(or `/home`)、PC → `/combos` の分岐追加)
- [ ] `web/src/App.tsx` に新規ルート(`/` または `/home`、§3.4.8 必須項目 3 で確定)が追加されている
- [ ] **(条件付き)** §3.4.8 必須項目 4 で **案 B(全画面共通配置)** を採用した場合、`App.tsx` のレイアウトラッパーに `<Footer />` が追加されている
- [ ] **(条件付き)** `web/package.json` に `react-responsive` 等のメディアクエリライブラリが追加されている(§3.4.8 必須項目 1 で JS 方式採用時のみ、Plan Mode 承認後)
- [ ] **(条件付き)** `web/src/components/Header.tsx` のスマホサイズ時挙動が調整されている(§3.4.5 で実態確認後、スマホで崩れない範囲の最小調整)

### 1.3 スコープ外への変更がないこと(§2.3、最重要)

- [ ] 既存ヘッダ(`Header.tsx`)の **他ページ反映** が **行われていない**(M6-02 v1.0.2 で SettingsPage のみ反映状態を温存、§1.3、開発者ご判断 2026-05-23「現状維持、別指示があるので待機」、最重要)
- [ ] 既存ルート定義の **変更が新規ルート追加に限定** されている(`/combos`、`/mycombos`、`/combos/:id`、`/combos/new`、`/combos/:id/edit`、`/compare`、`/setups/:id`、`/tags`、`/trash`、`/wizard`、`/settings` 等の既存ルートが温存されている)
- [ ] **(案 B 採用時の例外)既存全画面のフッター重なり回避調整(`padding-bottom` 等)が必要最小限**(§2.4 例外条項 (c))、それ以外の既存画面の内部実装変更は **ない**(本指示書 §1.3、M7 持ち越し)
- [ ] **(案 A 採用時)既存全画面に変更が含まれていない**(フッターは `HomePage.tsx` 内のみマウント)
- [ ] バックエンド全般(handler / service / repository / model / migration / config / netutil)に **変更がない**
- [ ] 設計書本体(REQ-001 / DES-001〜DES-006)が変更されていない
- [ ] SUPP-001 / playbook / architecture-patterns.md / handover / CLAUDE.md が変更されていない
- [ ] **プリセット管理画面 `/presets`(DES-005 §5.10 + §5.11)が実装されていない**(P-1 持ち越し、§1.3、M6 終了間近で再協議)
- [ ] **新規 CHANGE 通知書が起票されていない**(M6-overview §9、§1.3 既定)
- [ ] **shadcn/ui が導入されていない**(playbook §17.2、M7 まで未導入)
- [ ] **下書き自動保存(FR603)が実装されていない**(M6-04 スコープ、§1.3 既定)
- [ ] **複数ユーザー認証 UI が実装されていない**(SUPP-001 §4.2 フェーズ 2 送り、§1.3 既定)
- [ ] **「最近更新したコンボ」用の新規 API が追加されていない**(既存 `GET /api/combos` 流用、§3.4.8 必須項目 5 で確定方針通り)。**例外**: §3.4.3 確認結果で既存 API のクエリパラメータが満たせず、Plan Mode で開発者承認後に「既存 API のクエリパラメータ追加」を採用した場合のみ許容(§2.4 例外条項に明示なし、Plan Mode で承認結果を完了報告に明記)

### 1.4 例外条項適用箇所のチェック(§2.4)

- [ ] (a) 新規ルーティング 1 件追加が `/` または `/home` のいずれかで実装されている
- [ ] (b) **(条件付き)** `web/package.json` に依存追加した場合、CLAUDE.md §6 依存追加ポリシー準拠 + Plan Mode で開発者承認済み(完了報告で承認結果明示)
- [ ] (c) **(案 B 採用時)** `App.tsx` への `<Footer />` 追加 + 既存全画面のフッター重なり回避の最小調整(`padding-bottom` 等)が必要最小限に留まっている
- [ ] (d) **(条件付き)** `useIsMobile` フックの新設が JS でメディアクエリ監視が必要な場合のみ行われている(Tailwind ブレークポイントだけで完結する場合は不要、§3.4.8 必須項目 1)

---

## 2. 着手前確認結果のチェック(§3.4、retrospective-log v1.0.20 §6.1 M6-1 反省踏襲)

製造担当の Plan Mode 計画提示に以下の確認結果が **すべて含まれている** こと:

### 2.1 §3.4.1〜§3.4.5 / §3.4.7 着手前確認

- [ ] §3.4.1 既存ルート定義の網羅確認結果(全件列挙、`/` の現状実装、レイアウトラッパー方式)
- [ ] §3.4.2 既存ヘッダの配置方式確認結果(全画面共通レイアウト or ページ個別)— **案 C 採用根拠**
- [ ] §3.4.3 既存 `GET /api/combos` のクエリパラメータ仕様確認結果
- [ ] §3.4.4 既存スマホ判定実装の有無確認結果
- [ ] §3.4.5 既存 UI コンポーネントの 3 点セット確認結果(`Header.tsx` + 既存ボタンコンポーネント + M3-04 `MyCombosPage` 参照)
- [ ] §3.4.7 言語切替の M6-02 確定状態の確認結果(翻訳キー追加範囲)

### 2.2 §3.4.6 ホーム画面表示項目 + フッター + リダイレクト分岐 ↔ 既存実装実態の対応表(M6-6 教訓踏襲、最重要)

- [ ] **製造担当の Plan Mode 計画提示に「ホーム画面表示項目 + スマホフッター 4 ボタン + アプリ起動時リダイレクト分岐 ↔ 既存実装実態の対応表」が完全に埋められている**(17 項目すべて、M6-3 / M6-4 / M6-5 / M6-6 反省踏襲、最重要、対応表のスコープを UI 全範囲に拡張)
- [ ] 対応表の各行(ホーム表示 10 行 / フッター 4 行 / リダイレクト 3 行)について、表示項目 / 機能 + 対応する既存実装 + 実装有無 + M6-03 でのアクションが明記されている
- [ ] **未確認項目がない**(あった場合は §4 着手不可、M6-3〜M6-6 反省再発防止の最重要観点)
- [ ] **「プリセット管理」ボタンの対応既存実装が「未実装(P-1 持ち越し)」と明記** + アクションが「disabled + ツールチップ『今後実装予定』」と確認されている

---

## 3. Plan Mode 必須項目の確認(§3.4.8、retrospective-log v1.0.20 §6.1 M6-2 反省踏襲、5 項目)

製造担当の完了報告に以下 **5 項目** の **開発者承認結果が明示** されていること:

- [ ] **必須項目 1**: スマホ判定方式の確定(Tailwind ブレークポイント / JS メディアクエリ監視)— 開発者承認方針の明記
- [ ] **必須項目 2**: アプリ起動時リダイレクト分岐の実装位置(`App.tsx` グローバル判定 / ルーター層 / ページ層)— 開発者承認方針の明記
- [ ] **必須項目 3**: スマホ専用ホームのルート設計(`/home` 新設 / `/` 出し分け)— 開発者承認方針の明記
- [ ] **(案 C 採用、最重要)必須項目 4**: スマホフッターの配置方式(案 A 限定実装 / 案 B 全画面共通配置)— 開発者承認方針の明記、§3.4.2 着手前確認結果(既存 `Header` 配置方式)を踏まえた判断根拠も明記
- [ ] **必須項目 5**: 「最近更新したコンボ」取得方式の確定(既存 API クエリパラメータ / 既存 API クエリパラメータ追加 / フロント側ソート + slice)— 開発者承認方針の明記

---

## 4. スマホ専用ホーム画面のチェック(§4.1)

### 4.1 表示構造(§4.1.1、DES-005 §5.2)

- [ ] アプリロゴ / 名称が表示される
- [ ] 現在のユーザー名表示が **単一ユーザー運用のため非表示(または最小プレースホルダ)** で実装されている(本指示書 §4.1.4、YAGNI、フェーズ 2 で有意化)
- [ ] 主要機能ボタン 6 件が **縦並び、大きめのタップ領域** で表示される(DES-005 §5.2 規定、Tailwind の `flex-col gap-4 py-6` 等)
- [ ] 「最近更新したコンボ」セクションが表示される(0 件時はプレースホルダ、ローディング中はスケルトン or スピナー、失敗時はエラーメッセージ)

### 4.2 コンポーネント設計(§4.1.2、architecture-patterns §1)

- [ ] `HomePage.tsx` が **統合ページ**(表示専用コンポーネント + ロジックフックの組み合わせ)で実装されている
- [ ] 内部で使うロジックフック: `useRecentCombos`(新設) + 既存 `useConfig`(M6-01 / M6-02 既存)
- [ ] 主要機能ボタン 6 件 + 「最近更新したコンボ」リストが表示専用コンポーネントとして分離されている

### 4.3 主要機能ボタン 6 件(§4.1.3)

- [ ] 6 件すべてが実装されている(コンボ一覧 / マイコンボ / 新規コンボ登録 / コンボ比較 / プリセット管理 / 設定)
- [ ] 「プリセット管理」ボタンが **disabled + ツールチップ「今後実装予定」**(M6-02 v1.0.2 §4.5.6 / §4.5.7.1 と統一文言、共通 i18n キー `t('common.comingSoon')` 等を使用、M6-02 と統一)
- [ ] 各ボタンクリックで対応ルートに遷移(プリセット管理は遷移しない、disabled)
- [ ] ボタンレイアウトが大きめのタップ領域で実装されている(具体値は Plan Mode で承認後、合理性確認)

### 4.4 「最近更新したコンボ」表示(§4.1.5)

- [ ] 上位 3 件のコンボへのリンクリストが表示される
- [ ] 各リンククリックで `/combos/:id` 詳細画面に遷移
- [ ] 取得方式が §3.4.8 必須項目 5 で開発者承認された方式で実装されている
- [ ] ローディング中表示(スケルトン or スピナー)が実装されている(M5-01 v1.0.2 統合案 Q 踏襲)
- [ ] 0 件時のプレースホルダ表示が実装されている
- [ ] 失敗時のエラーメッセージ表示が実装されている(リトライボタンなし、YAGNI)
- [ ] `useRecentCombos` フックの queryKey が既存 `useCombos` 系の structure(flat tuple / object、§3.4.3 で確認した既存実態)に揃っている(architecture-patterns §1.1)

---

## 5. スマホフッターのチェック(§4.2、DES-005 §4.2)

### 5.1 表示構造(§4.2.1)

- [ ] 4 ボタン構成(コンボ一覧 / マイコンボ / 新規登録 / 設定)が実装されている
- [ ] 中央の「新規登録」ボタンが **大きめ表示**(`highlighted: true` 等)
- [ ] `position: fixed; bottom: 0` または Tailwind `fixed bottom-0 left-0 right-0` でスマホ下部に固定表示
- [ ] スマホ表示時のみ表示(PC / タブレットでは非表示、`md:hidden` 等、§3.4.8 必須項目 1 で確定した方式に従う)

### 5.2 各ボタンの実装(§4.2.2)

- [ ] アイコンと文字ラベルの組み合わせで表示(視認性確保)
- [ ] アイコンライブラリの選定が §3.4.5 で確認した既存利用ライブラリと整合(`lucide-react` 等、新規依存追加なしを優先)
- [ ] 中央の「新規登録」ボタンが他 3 ボタンと比較して視覚的に大きめ
- [ ] 各ボタンクリックで対応ルートに遷移(コンボ一覧 / マイコンボ / 新規登録 / 設定)
- [ ] 現在の画面に応じた active 状態表示(react-router の `useLocation()` または `NavLink` 等)

### 5.3 配置方式(§4.2.3、§3.4.8 必須項目 4、案 C 採用)

- [ ] **(案 A 限定実装採用時)** フッターが `HomePage.tsx` 内のみでマウントされ、他画面では表示されない
- [ ] **(案 B 全画面共通配置採用時)** フッターが `App.tsx` のレイアウトラッパーで全画面共通に配置され、スマホ表示時は全画面で表示される
- [ ] §3.4.8 必須項目 4 の Plan Mode 開発者承認結果と実装が一致している
- [ ] §3.4.2 着手前確認結果(既存 `Header` の配置方式)と整合した方式が採用されている(案 C 採用根拠)

---

## 6. アプリ起動時のリダイレクト分岐拡張のチェック(§4.3)

### 6.1 M6-02 既存実装からの拡張(§4.3.1)

- [ ] M6-02 で実装した「`isInitialized: false` → `/wizard`」分岐が **温存** されている(変更されていない)
- [ ] スマホ判定が追加されている(§3.4.8 必須項目 1 で確定した方式)
- [ ] スマホ + `isInitialized: true` で `/`(または `/home`、§3.4.8 必須項目 3)に遷移する
- [ ] PC/タブレット + `isInitialized: true` で `/combos` に遷移する(`/` 出し分け採用時は `/` アクセス時にリダイレクト、`/home` 新設採用時はホームアクセス時にリダイレクト)
- [ ] 既存ルート(`/wizard`、`/settings`、`/combos`、`/mycombos` 等)に直接アクセスした場合の挙動は変更されていない

### 6.2 ウィザード経由時の遷移先(§4.3.2)

- [ ] M6-02 v1.0.1 §4.4.5 既定の「ウィザード完了時は常にコンボ一覧へ固定遷移(`navigate('/', { replace: true })`)」が温存されている
- [ ] M6-03 でこの挙動を変更していない(YAGNI、M6-overview §2.5 既定)

---

## 7. 設計判断事項のチェック(§4.4)

製造担当の完了報告に §4.4 設計判断事項表との整合確認結果が明記されていること:

- [ ] スマホ専用ホーム画面の新設(DES-005 §5.2)
- [ ] スマホフッターの新設(DES-005 §4.2、M6-6 教訓踏襲)
- [ ] スマホフッターの配置方式(案 C = Plan Mode で既存 `Header` 配置方式に揃える、開発者ご判断 2026-05-23)
- [ ] スマホ判定方式(Plan Mode 確定、Tailwind ブレークポイント推奨)
- [ ] ホーム画面のルート(Plan Mode 確定、`/` 出し分け推奨)
- [ ] アプリ起動時リダイレクト分岐(`App.tsx` グローバル判定、M6-02 既存パターン踏襲)
- [ ] プリセット管理ボタンの扱い(disabled + ツールチップ「今後実装予定」、M6-02 と統一)
- [ ] 既存ヘッダの他ページ反映(本指示書スコープ外、開発者ご判断 2026-05-23「現状維持、別指示があるので待機」)
- [ ] 既存画面のスマホレスポンシブ調整(M7 持ち越し、本指示書スコープ外)
- [ ] 「最近更新したコンボ」取得(Plan Mode 確定、API 拡張なし優先)
- [ ] 「現在のユーザー名」表示(単一ユーザー運用のため非表示、YAGNI)
- [ ] 推奨モデル Opus 4.6(関心数 2 + 案 C 採用、開発者ご判断)
- [ ] Plan Mode 必須(必須項目 5 件、M6-2 反省踏襲)
- [ ] 逸脱が発生した場合は理由を明記

---

## 8. テスト要件のチェック(§5.1)

### 8.1 フロントエンドテスト(§5.1.1、必須 10 件以上)

- [ ] `HomePage.test.tsx` に表示確認 + ボタンクリック動作確認のテストが含まれている
- [ ] `Footer.test.tsx` に表示確認 + 4 ボタンクリック動作確認 + active 状態確認 + スマホ表示時のみ表示確認のテストが含まれている
- [ ] `useRecentCombos.test.ts` に取得成功 / 失敗 + queryKey structure 確認のテストが含まれている
- [ ] **(条件付き)** `useIsMobile.test.ts`(`useIsMobile` 新設時のみ)に判定動作確認のテストが含まれている
- [ ] 合計 10 件以上のテストケースが追加されている

### 8.2 既存テストの回帰確認(§5.1.2)

- [ ] `web/src/pages/` 配下の既存ページテスト(CombosPage / MyCombosPage / ComboDetailPage / CompareePage / SettingsPage / WizardPage 等)が pass
- [ ] M6-02 で確立した `App.tsx` の起動時リダイレクトテスト(`isInitialized: false` → `/wizard`)が pass
- [ ] Go backend テストに変更がない(本指示書スコープ外)

### 8.3 ビルド・型チェック(§5.1.3)

- [ ] `npm run build` が成功している
- [ ] `npm run type-check`(または相当)が成功している
- [ ] `go build ./...` が成功している(backend 変更なし、形式的確認)

---

## 9. E2E シナリオ実行結果のチェック(§5.2)

製造担当の完了報告に以下のシナリオ手順 + 開発者実機検証結果が **すべて含まれている** こと:

- [ ] **シナリオ A**: スマホ表示時のホーム画面表示(主要機能ボタン 6 件 + 最近更新したコンボ + スマホフッター 4 ボタンが表示される、ボタンクリックで遷移)
- [ ] **シナリオ B**: PC 表示時のリダイレクト確認(`/` アクセスで `/combos` 自動遷移、スマホフッターは非表示)
- [ ] **シナリオ C**: アプリ起動時のリダイレクト分岐確認(`config.toml` 削除状態で `/wizard`、存在状態でホーム or コンボ一覧)
- [ ] **シナリオ D**: スマホフッターのナビゲーション確認(**案 B 採用時のみ全画面共通でフッター表示**、案 A 採用時はホーム画面のみ)
- [ ] **シナリオ E**: 「最近更新したコンボ」の動作確認(上位 3 件表示 + 各リンク遷移 + 0 件時プレースホルダ + 失敗時エラーメッセージ)
- [ ] **シナリオ F**: 既存機能の回帰確認(PC / スマホ両方で既存全画面が動作、案 B 採用時のフッター重なり回避確認)

---

## 10. コード品質・規約遵守

- [ ] CLAUDE.md §2 技術スタック遵守(React + TypeScript + Tailwind + TanStack Query)
- [ ] CLAUDE.md §4 規約遵守(camelCase、ハードコード禁止、列挙定数同期、TypeScript 型 1:1 対応)
- [ ] CLAUDE.md §5 テスト規約遵守
- [ ] CLAUDE.md §6 依存追加ポリシー遵守(`react-responsive` 等を追加する場合 Plan Mode 承認後)
- [ ] CLAUDE.md §10 禁止事項に抵触なし(ブラウザストレージ API の不適切使用なし、機微情報のログ出力なし等)
- [ ] **architecture-patterns.md v1.0.4 §1 プレゼンテーション層/ロジック層分離パターン遵守**(`HomePage` 統合 + 表示専用コンポーネント + ロジックフックの組み合わせ、最重要)
- [ ] **architecture-patterns.md v1.0.4 §1.1 queryKey 規約遵守**(`useRecentCombos` の structure が既存 `useCombos` 系と整合、ID 型 number 正規化)
- [ ] **shadcn/ui を使用していない**(playbook §17.2、M7 まで未導入)
- [ ] **設計書本体への影響なし**(M6-overview §9 既定、CHANGE 通知書起票なし)
- [ ] react-i18next 翻訳キーが追加されている(ホーム画面 + フッター分、設定画面 + ウィザード分は M6-02 で追加済み)

---

## 11. ドキュメント・進捗ログ(§7.4)

- [ ] `docs/progress/progress-log.md` に M6-03 完了報告が追記されている
- [ ] **§3.4 着手前確認サマリ(必須、§3.4.1〜§3.4.5 / §3.4.7 + §3.4.6 対応表)** が完了報告に含まれている(M6-01 製造担当伝達事項 3 由来の運用改善 + M6-02 v1.0.1 §3.4.7 対応表運用継続 + M6-6 教訓踏襲で UI 全範囲拡張)
- [ ] §3.4.8 Plan Mode 必須項目 5 件の開発者承認結果が完了報告に含まれている
- [ ] §4.4 設計判断事項表との整合確認結果が明記されている
- [ ] §5.2 E2E シナリオ A〜F の実行結果が明記されている
- [ ] 例外条項適用箇所(§2.4 (a)〜(d))の明示
- [ ] M5-RESEARCH-01 / M6-RESEARCH-01 / M6-01 / M6-02 で確立した「製造担当からの実装完了後連絡事項」(handover §6.2 多段構えフィードバックループ)が含まれている、または「乖離なし」が明記

---

## 12. 重大な問題の判定基準

以下のいずれかに該当する場合、**重大な問題** と判定し M6-03 完了承認を保留する:

- §1.1 / §1.2 新規ファイルが指示書 §2.1 と乖離(主要コンポーネント欠落)
- §1.3 スコープ外への変更が含まれている(**既存ヘッダの他ページ反映実施(M6-02 v1.0.2 状態を温存しない)、既存画面の内部実装変更、バックエンド変更、設計書本体改変、shadcn/ui 導入、プリセット管理画面 `/presets` 実装、下書き自動保存実装、CHANGE 起票等、最重要**)
- §2 着手前確認結果が完了報告に含まれていない(retrospective-log v1.0.20 §6 M6-1 反省踏襲、最重要観点)
- **§2.2 §3.4.6 ホーム画面表示項目 + フッター 4 ボタン + リダイレクト分岐 ↔ 既存実装実態の対応表が Plan Mode 計画提示に含まれていない**(M6-3 / M6-4 / M6-5 / M6-6 反省踏襲の最重要観点)
- **§3 Plan Mode 必須項目 5 件の開発者承認結果が完了報告に含まれていない**(retrospective-log v1.0.20 §6 M6-2 反省踏襲、最重要観点)
- **§3 必須項目 4(スマホフッター配置方式、案 C 採用)の Plan Mode 確定結果と実装が一致していない**(開発者ご判断 2026-05-23 違反、最重要)
- §4 `HomePage` の主要機能ボタン 6 件のいずれかが欠落、または「プリセット管理」ボタンが disabled 化されていない
- §5 スマホフッター 4 ボタンのいずれかが欠落、または中央の「新規登録」ボタンが大きめ表示されていない
- §6 アプリ起動時のリダイレクト分岐拡張が未実装、または M6-02 既存挙動を破壊している
- **§4 / §5 architecture-patterns §1 プレゼンテーション層/ロジック層分離パターン違反**(retrospective-log v1.0.20 §6 M6-1 反省踏襲、最重要)
- §4 `useRecentCombos` の queryKey が既存 `useCombos` 系と乖離(architecture-patterns §1.1 違反)
- §8 必須テスト(フロント 10 件以上)のいずれかが未実装または失敗
- §8.2 既存テスト回帰
- §8.3 ビルド・型チェック失敗
- §9 E2E シナリオ A〜F のいずれかが完了報告に含まれていない
- §10 architecture-patterns.md v1.0.4 違反(特に §1 / §1.1、最重要観点)
- §11 progress-log.md 完了報告に必須項目が含まれていない

---

## 13. 軽微な問題の判定基準(完了承認可、改善推奨)

以下は軽微な問題で、完了承認は可だが次回マイルストーン以降の改善対象として記録する:

- §1.4 例外条項適用箇所の理由明記が不十分
- §4 ホーム画面の UI 細部(色 / 余白 / アニメーション)が DES-005 §5.2 と微妙に異なる(機能には影響なし)
- §5 スマホフッターアイコンの選定が一般的なライブラリ(`lucide-react` 等)と異なる(機能には影響なし)
- §4.3 「プリセット管理」ボタンの disabled 化のツールチップ文言が「今後実装予定」と完全一致しない(M6-02 v1.0.2 と統一されていない、軽微)
- §4.4 「最近更新したコンボ」のローディング表示形式(スケルトン or スピナー)が一般的でない(機能には影響なし)
- §6 アプリ起動時のリダイレクト分岐実装位置が `App.tsx` ではなくルーター層 or ページ層(Plan Mode 承認済みなら許容)
- §8 テストヘルパ関数の重複(M6-01 / M6-02 と HomePage / Footer で似たモック設定パターンが共通化されていない、YAGNI 観点で許容)
- ドキュメント記述の細部不整合(完了報告の節構成、文体等、機能には影響なし)

---

## 14. レビュー報告書フォーマット

機械レビュー完了時に以下を含む報告書を `docs/progress/m6-03-review.md` として出力:

```markdown
# M6-03 機械レビュー報告書

## 1. レビュー実施日
2026-XX-XX

## 2. レビュー結果サマリ
- ✅ / ⚠️ / ❌ §1 ファイル一覧チェック(HomePage / Footer / useRecentCombos + 必要時 useIsMobile + App.tsx 修正)
- ✅ / ⚠️ / ❌ §2 着手前確認結果(retrospective-log v1.0.20 §6 M6-1 反省踏襲 + §3.4.6 ホーム + フッター + リダイレクト対応表)
- ✅ / ⚠️ / ❌ §3 Plan Mode 必須項目 5 件(retrospective-log v1.0.20 §6 M6-2 反省踏襲、最重要)
- ✅ / ⚠️ / ❌ §4 スマホ専用ホーム画面(架構パターン §1 遵守 + 主要機能ボタン 6 件 + 最近更新したコンボ)
- ✅ / ⚠️ / ❌ §5 スマホフッター(4 ボタン + 中央「新規登録」大きめ + 配置方式 案 A or B)
- ✅ / ⚠️ / ❌ §6 アプリ起動時のリダイレクト分岐拡張(M6-02 既存挙動温存 + スマホ判定追加)
- ✅ / ⚠️ / ❌ §7 設計判断事項表との整合
- ✅ / ⚠️ / ❌ §8 テスト要件(フロント 10 件以上)
- ✅ / ⚠️ / ❌ §9 E2E シナリオ A〜F
- ✅ / ⚠️ / ❌ §10 コード品質・規約遵守
- ✅ / ⚠️ / ❌ §11 ドキュメント

## 3. Plan Mode 必須項目 5 件の開発者承認結果確認(最重要)
- 必須項目 1(スマホ判定方式): 承認方針 = ...、実装 = ...
- 必須項目 2(リダイレクト実装位置): 承認方針 = ...、実装 = ...
- 必須項目 3(ホームのルート設計): 承認方針 = ...、実装 = ...
- **必須項目 4(スマホフッター配置方式、案 C 採用)**: 承認方針 = 案 A or 案 B、§3.4.2 着手前確認結果 = ...、実装 = ...
- 必須項目 5(「最近更新したコンボ」取得方式): 承認方針 = ...、実装 = ...

## 4. architecture-patterns.md v1.0.4 準拠確認(M6-1 反省再発防止の最重要観点)
### 4.1 §1 プレゼンテーション層/ロジック層分離パターン
- `HomePage` 統合 + 表示専用コンポーネント + ロジックフック分離: ○ / ×
- `Footer` 表示専用、ロジック(active 状態判定等)はフック化: ○ / ×

### 4.2 §1.1 queryKey 規約
- `useRecentCombos` の queryKey が既存 `useCombos` 系の structure と整合: ○ / ×(flat tuple / object のいずれか確認)
- ID 型 number 正規化(URL パラメータ等を扱う場合): ○ / × / N/A

## 5. ホーム + フッター + リダイレクト ↔ 既存実装実態の対応表確認(M6-3〜M6-6 反省踏襲、最重要)
- 製造担当の Plan Mode 計画提示に対応表 17 項目が完全に埋められているか: ○ / ×
- 未確認項目の有無: あり / なし
- 「プリセット管理」ボタンの P-1 持ち越し状態 + disabled 化の対応が明記されているか: ○ / ×

## 6. 検出した問題
### 6.1 重大な問題(完了承認保留)
(該当する場合、§12 判定基準のどれに該当するか明記)

### 6.2 軽微な問題(完了承認可、改善推奨)
(該当する場合、§13 判定基準のどれに該当するか明記)

### 6.3 改善提案(次回マイルストーン以降)
(該当する場合)

## 7. 制約事項
- 本レビューは静的コードレビュー。**実機テスト(ブラウザでの動作確認、スマホサイズでの表示確認、E2E シナリオ A〜F の実行、案 B 採用時の全画面共通フッター視覚確認、既存画面の回帰確認)は別途実施が必要**(playbook §14)
- 動作確認シナリオの手動確認は開発者の責任範囲

## 8. 完了承認判定
- ✅ 完了承認可 / ⚠️ 条件付き承認(改善後) / ❌ 完了承認保留(重大な問題あり)
```

---

*以上、M6-03 レビューチェックリスト v1.0.0*
