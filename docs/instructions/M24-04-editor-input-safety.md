# 指示書 M24-04: エディタ操作・dirty state・入力事故の防止

| 項目 | 内容 |
|------|------|
| 文書ID | M24-04 |
| バージョン | 1.1.0 |
| 推奨モデル | **高精度実装**（**★★9 項目のうち 2 項目が同じ画面の縦を争っており、片方を決めないともう片方が決まらない**。実モデル名は `docs/human-notes/model-allocation.md` が正本） |
| Plan Mode | **必須**（**★実査 15 件と、モック HTML の提示を Plan Mode に含める**。CLAUDE.md §9） |
| 機械レビュー | 必須（別チェックリスト: `M24-04-review-checklist.md`） |
| 参照してよい先行資料 | `DES-005` §5.7（コンボ登録・編集） ／ `DES-006`（§11 エラー表示 ／ 初期値） ／ `REQ-001` §7 フェーズ3 ／ `web/CLAUDE.md` §1 ／ `docs/handover/code-facts.md`（**★他サブの指示書・チェックリストは含めない**＝D-217） |
| 並列性 | **★`M24-11` と並列可の見込み**（`M24-11` は `internal/`、本サブは `web/src/` のエディタ面）。**★★`make e2e` の実行は重ねられない**（`D-548`。**★`vite preview` を採っても消えていない**） |
| 依存 | **`M24-01` の完了**（済＝**D-553**。**保存後の遷移先を確定させた**） ／ **`M24-03` の完了**（済＝**D-572**。**★★同サブが編集画面を触っている**＝§1.1.1） |
| CHANGE | **`CHANGE-137`**（**設計卓が起票済み。製造は番号を消費しない**）。影響設計書＝**`DES-005` §5.7**（主対象） ／ **`DES-006`**（条件付き） ／ **`REQ-001` §7**（条件付き＝§1.3-1） |
| マイグレ | **0 本の見込み**（本サブはフロントである）。**★消費するなら次は `000080`** |
| 想定所要時間 | 300〜360 分（**★9 項目あり、うち 1 件は画面レイアウトの作り替えである**） |
| 作成者・作成日 | 設計担当 Claude（設計卓・M24 期）/ 2026-08-27 |

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| 1.1.0 | 2026-08-27 | **★★開発者確認事項 2 が決着し、あわせて原文の落とし物を 1 件是正した**（**D-576**）。 **★★`SM-101` は「表示を実際の上限へ揃える」ではなく「ラベルから上限を消す」で確定した**〔開発者の逐語＝**「ドライブゲージ消費(0〜20本・0.5刻み)からドライブゲージ消費(0.5刻み)に変えておいてください。20もあれば特殊なコンボを対象にする場合も足りるはずですが、20の根拠が薄いため、ラベルが違和感を出しているのだと気づきました。」**〕。**★設計卓の暫定案「揃える」は撤回する。** **★★`SM-118` の原文に、要約列から落ちていた指定が 1 件あった**（計測点 **`M-87`**）——**逐語＝「両方に着けるのが変ならコンボレシピを優先。」** **⇒ §4.2.2 へ優先順位を足した。★`M-82` の同型で 3 度目である。** **★あわせて `SM-148` の原文が「タブ分け**も**検討したい」であることを §4.2.1 へ明記した**（**★確定要求ではなく検討の提案である**）。 |
| 1.0.0 | 2026-08-27 | 初版（**D-574**） |

---

## 1. 背景と目的

### 1.1 扱う 9 項目——**4 クラスタに畳める**

**ledger の正本は `docs/progress/M24-RESEARCH-01-report.md` §2 / §3 である。以下は要旨であり、原文はそちらを見ること。**

**★★要約列は原文ではない**（`M-82`）。**確認事項を立てるときは `docs/human-notes/Memo_Someday.txt` の原文へ当たること。**

| クラスタ | ID | memo の要求（要旨） | 面 |
|---|---|---|---|
| **A. 入力事故の防止**（★本サブの核心） | **`CO-003`** | **dirty 確認ダイアログが無い ／ 編集中の画面遷移時データ保持**（**★★1 行に 2 つの別要求が入っている**＝§1.1.2） | `/combos/new`・`/combos/:id/edit`・`/setups/:setupId` |
| **B. 画面レイアウト**（★2 件が同じ縦を争う） | **`SM-148`** | **基本情報が当初より増えたので、基本情報とレシピをタブに分けたい**（**★memo 自身が「M24＝UX 整理の候補」と明記**） | `/combos/new`・`/combos/:id/edit` |
| | **`SM-118`** | **「隠す」ボタンが縦方向に縮むだけで、左辺の情報（メタデータ）が広がらない** | 同上 ／ `CollapsibleFieldset` |
| **C. 語彙・表記の不揃い** | **`SM-007`**（＝**`CO-005`**） | **ゲージの「(0〜6)」を「(0〜6本)」に**（日本語版のみ）。**★ドライブ始動側は既に「本」付き＝済** | 同上 ／ locale |
| | **`SM-101`** | **ドライブゲージ消費「(0〜20本・0.5刻み)」が不自然**（**★始動側は max=6 なのに消費側の表示だけ 20＝上限の見え方が不揃い**） | 同上 |
| | **`SM-112`** | **「システム」と「共通システム（移動・その他）」が意味が被りすぎて分かりにくい** | 同上 ／ `/setups/:setupId` ／ `RecipeBuilder` |
| | **`SM-093`** | **状態はデフォルトで不問であるべき**（**★相手スタンスには `any` が既にあるが、新規登録の初期値が空文字**） | `/combos/new` |
| **D. 通知・エラー表示** | **`SM-060`** | **保存成功時等のダイアログがしばらく消えず、ヘッダでの画面遷移を一時的に邪魔する** | 共通部品: トースト ／ Header |
| | **`SM-067`** | **エラーメッセージの位置**（**★★2 案あり＝§11 確認事項 1**） | 共通部品: エラー表示 |

### 1.1.1 ★★`M24-03` が編集画面を触った直後である

| 何が変わったか | 本サブへの影響 |
|---|---|
| **★★編集画面の起き攻めラベルが `okiTechAndGaugeLabel()` の正典へ寄った**（`M24-03` §2-3。**開発者裁定＝案 A**） | **★`M24-03` は指示書でここをスコープ外と宣言していたが、表記の割れを解消するために触った。** **⇒ 編集画面のラベルは既に正典を通っている。作り直さないこと** |
| **一覧の表示項目の既定が変わった**（備考・セットプレイ数を既定 OFF） | 本サブの面ではないが、**★保存後の遷移先が一覧である**（`CHANGE-130`）ため、`SM-060` の見え方に効く |
| **レシピの見せ方が 5 面で 1 つの規則を通るようになった**（`RecipeText`） | **★★エディタは「読む面」ではないため対象外だった。⇒ 本サブでエディタのレシピ表示に触るなら、その規則と揃えるか、揃えない理由を報告すること**（§3.3-13） |

**⇒ 上記は `DES-005` §5.4 / §5.6 / §5.7 の as-built である。実装の現物は §3.3 で確認すること。**

### 1.1.2 ★★`CO-003` の followup 行は、**2 つの別の要求を 1 行にまとめている**

**followup `F12-3 / E-3` の逐語＝「dirty 確認ダイアログ欠如・編集中の画面遷移時データ保持」。**

| # | 要求 | 中身 |
|---|---|---|
| **1** | **dirty 確認ダイアログ** | **保存していないのに離れようとしたら聞く。** ⇒ **事故の防止** |
| **2** | **編集中の画面遷移時データ保持** | **離れても、戻ったら入力が残っている。** ⇒ **利便性** |

**★★これは別物である。** **1 は「止める」、2 は「覚えておく」。**

**★★2 は `REQ-001` §7 フェーズ3 の項目として独立に載っている**（逐語＝**「編集中の画面遷移時データ保持」**）。

**⇒ 1 行にまとめたまま片方だけ実装すると、`REQ-001` の項目が「済み」に見える状態が生まれる。**

**★本サブでやるのは 1 だけである**（§1.3-1）。**★2 は本サブでやらない。followup を割ることを設計卓が引き受ける。**

### 1.1.3 ★★起票時に設計卓が確かめていないこと

**設計卓は実装ソースを読めない。** 以下は `code-facts.md` **からの引用**であり、それ以上のことは分からない。

| 対象 | code-facts が示すこと | **分からないこと** |
|---|---|---|
| `ComboEditor` | Props に `mode: "new" \| "edit" \| "copy"` / `initial?` / `initialCharacterId?` | **★dirty state を持っているか。離脱をどう扱っているか** |
| `ComboEditorBasicFields` | Props に `value: BasicFieldsValue` / `moves` / `autoStarterMoveId` / `customStates` ほか | **★★項目が何個あるか**（`SM-148` の「増えてしまった」の実体） |
| `CollapsibleFieldset` | Props に `legend: string` / `defaultOpen?` / `summary?` / `children` | **★「隠す」が何を縮めているか**（`SM-118`） |
| `RecipeBuilder` | Props に `characterId` / `steps` / `moves` / `movesLoading` / `onChange` ほか | **★optgroup の呼び名**（`SM-112`） |
| ルート | `/combos/new` と `/combos/:id/edit` は**どちらも `ComboEditorPage`** | **★モードの分岐がどこに在るか** |

**⇒ 上記を断定形として扱わないこと。§3.3 で 1 件ずつ実査し、食い違ったら報告すること**（計測点 `M-77` の再発防止）。

### 1.2 目的

**「入力を失う」と「入力に手間取る」を減らす。**

**★★本サブの 9 項目は、どれも「機能が無い」ではなく「入力の途中で困る」である。** **⇒ 個々の項目を潰すだけでなく、エディタ全体が入力しやすくなったかを見ること。**

### 1.3 ★★このサブで作らないもの（スコープ外）

| # | 作らないもの | 理由 |
|---|---|---|
| **1** | **★★「編集中の画面遷移時データ保持」**（`CO-003` の 2 つ目） | **★別の要求である**（§1.1.2）。**`REQ-001` §7 フェーズ3 の独立項目であり、下書きの永続化という別の設計が要る。** **⇒ 本サブは dirty 確認だけ** |
| **2** | **レシピの内部表現・`recipe_cache` の生成規則** | `DES-004` の領域 ／ **契約 F-1 の凍結パッケージ**（`internal/service/notation/` / `internal/service/preset/`） |
| **3** | **i18n（英語ロケールの整備）** | **`M24-07` の手番。★`SM-007` / `CO-005` は「日本語版のみ」の要求であり、日本語の文言を直すだけである** |
| **4** | **`M24-03` が寄せた起き攻めラベルの正典** | **★作り直さない**（§1.1.1）。**`okiTechAndGaugeLabel()` を通ったままにする** |
| **5** | **セットプレイ編集の作り直し** | **★`SM-112` は `/setups/:setupId` にも及ぶが、直すのは optgroup の呼び名だけである。** 構造は M23 の成果物 |
| **6** | **`queryKey` の綴りの統一** | `M24-08` の手番 |
| **7** | **キーボードショートカット**（`SM-085`） | 本サブの割付に無い（**要判断**のまま followup） |
| **8** | **★M16-04 が決めたレシピステップの振り分け原則** | **★`SM-112` は「画面上の呼び名の被り」であって振り分けの規則ではない。原則に触れないこと** |

---

## 2. 成果物

### 2.1 作成/修正するファイル

**★本一覧は「予見できた範囲」であって禁止列ではない。一覧に無いファイルでも §2.2 の禁止列に当たらないなら触ってよい。ただし触った理由を完了報告に書くこと。**

**★★以下の「面」は §3.3 の実査で確定する。設計卓は現物を読めないため、断定形で指定していない**（テンプレ §2.1 の記入指針）。

| ファイル | 内容 |
|---|---|
| **エディタ本体**（`ComboEditor.tsx` 相当） | §4.1（`CO-003`） ／ §4.2（`SM-148`） |
| **基本情報の欄**（`ComboEditorBasicFields.tsx` 相当） | §4.4（`SM-007` / `SM-101` / `SM-093`） |
| **折りたたみ部品**（`CollapsibleFieldset.tsx` 相当） | §4.3（`SM-118`） |
| **レシピ組み立て**（`RecipeBuilder.tsx` 相当） | §4.5（`SM-112`） |
| **トースト**（共通部品） | §4.6（`SM-060`） |
| **エラー表示**（共通部品） | §4.7（`SM-067`。**★確認事項 1 の回答後**） |
| **`web/src/locales/{ja,en}.json`** | 新規キーのみ。**★既存キーの整理はしない**（§1.3-3） |
| **`web/CLAUDE.md` §1 の台帳** | **★新キーを作る場合に登録**（**`CHANGE-137` がその「CHANGE 経由」を満たす**） |
| **テスト**（純粋関数・コンポーネント・E2E） | §5 |
| **モック HTML** `docs/progress/M24-04-mock/` | §4.8。**★Plan Mode の提示に含める** |
| **完了報告** `docs/progress/M24-04-completion-report.md` | §7.5 |
| **設計伝達レポート** `docs/handover/design-reports/2026MMDD-m24-04-design-exceptions.md` | `/design_handover_report` |

### 2.2 変更しないもの（原則）

| 対象 | 何を守るか | 根拠 |
|---|---|---|
| **`internal/` ／ `cmd/` ／ `migrations/`** | **★分岐 A で着地させる。差分 0 バイトを目指す** | §2.3 の例外条項 |
| **`internal/service/notation/` ／ `internal/service/preset/`** | **契約 F-1 の凍結パッケージ** | M21 契約 |
| **`DES-005` §5.7 の保存後の遷移先の表**（4 モード） | **★1 文字も変えない** | `CHANGE-130` |
| **`DES-005` §4.3.1 の解決順（5 段）** | **★1 文字も変えない** | `CHANGE-130` |
| **`DES-005` §5.4 のレシピの見せ方の規則**（5 面・`RecipeText`） | **★作り直さない。揃えるか、揃えない理由を報告する** | `CHANGE-134` |
| **`M24-03` の `okiTechAndGaugeLabel()`** | **★正典を通ったままにする** | §1.3-4 |
| **M16-04 のレシピステップの振り分け原則** | **★呼び名だけ直す** | §1.3-8 |
| **`/trash` 系** | M23 の成果物 | `M24-overview` |
| **`check-*.sh` の判定ロジック** | **★検査を緩めて通さない** | `M24-01` §7-7 |

**★先行サブの契約テストを落とさない。** ただし**本サブが動かす基準値そのもの**（項目数・初期値など）は、母集合を変えずに後状態へ更新してよい。

### 2.3 例外条項

**§2.2 の「`internal/` を触らない」を破ってよいのは、次の 3 条件がすべて成立するときだけである。**

1. **フロントの変更だけでは要求を満たせないことを、実測で示せる。**
2. **`DES-002` §4.2 の経路表に載る既存 API の挙動を変えない**（新設のみ。既存の変更なら止めて報告）。
3. **設計卓へ報告し、`CHANGE-137` の射程へ `DES-002` §4.2 を加える判断を仰ぐ。**

**★3 条件のどれか 1 つでも欠けたら、直さずに報告すること。**

### 2.4 ★並列性

**`M24-11` と触る面は交わらない見込み**（`M24-11` は `internal/service` / `internal/repository` / `internal/infra/db`、本サブは `web/src/`）。**★実査で確かめること。**

**★`make e2e` は重ねられない**（`D-548`。**★`M24-09c` で `vite preview` を採っても消えていない**——バックエンドの専用ポート 47390 と使い捨て DB が残る）。**★各回の実行直前にポートが空いていることを確認し、その出力を完了報告へ載せること。**

---

## 3. 前提条件

### 3.1 必読ドキュメント

- **`DES-005` §5.7**（コンボ登録・編集。**★`CHANGE-130` で保存後の遷移先が確定している**） ／ **§5.4**（**★`CHANGE-134` でレシピの見せ方が 5 面で 1 つの規則を通るようになった**） ／ **§5.6 項目6**（**★`CHANGE-134` でラベル生成規則が変わった**）
- **`DES-006` §11**（警告の表示と操作の分岐。**★★「選ばないと次へ進めないものはモーダルが正しい」**）
- **`REQ-001` §7 フェーズ3**（**★「編集中の画面遷移時データ保持」が独立項目として載っている**）
- `docs/handover/code-facts.md`

### 3.2 参照不要

- 他サブの指示書・チェックリスト（`D-217`）
- `DES-002` §4.2（分岐 A で着地する見込み。§2.3 が発動したときだけ読む）

### 3.3 ★★着手前の確認（Plan Mode 必須）

**★以下はすべて実査項目である。§1.1 / §1.1.1 / §1.1.3 に書いた内容も含めて、設計卓が現物で裏を取ったものは 1 つも無い。**

**★★実査は 2 系統ある**（`M24-overview` §2.3）——**(I) その症状が現在も成立するか** ／ **(II) 触る面がどこか。**

| # | 確かめること | 系統 | なぜ要るか |
|---|---|---|---|
| **1** | **★★エディタが dirty state を持っているか。離脱（ブラウザバック・ヘッダのリンク・URL 直打ち）を現在どう扱っているか** | **II** | **★`CO-003` の出発点。★「持っていない」なら作る、「持っているが使っていない」なら配線するで、手が違う** |
| **2** | **★★基本情報の項目が現在いくつあるか**（**母数を数えること**） | **I** | **★`SM-148` の「当初より増えてしまった」の実体。★`M15-05` で一度手当てした後にさらに増えている** |
| **3** | **★一覧・詳細と同じ「1 画面に入る量」の基準値**（**エディタで 1 画面に何項目入るか**） | **I** | **★`SM-148` を満たしたかの判定に要る**（`D-551`＝母数を添える） |
| **4** | **★`CollapsibleFieldset` の「隠す」が何を縮めているか。左右のカラムがどう組まれているか** | **II** | **★`SM-118` の「縦方向に縮むだけ」の裏取り** |
| **5** | **★★`SM-148`（タブ分け）と `SM-118`（左辺を広げる）が両立するか** | **II** | **★★タブに分けると「隠す」の意味が変わる。⇒ どちらを先に決めるかで結論が変わる**（§4.2） |
| **6** | **ゲージ欄の表示文言の全数**（**始動側・消費側・ドライブ・SA の 4 通り**）と、**それぞれの実際の上限値**。 **★★あわせて、各上限に「根拠が説明できるか」を判定すること**（例＝**ドライブゲージは 6 本しかないので始動側の 6 には根拠がある**） | **I** | **★`SM-007`。★★`SM-101` は 2026-08-27 に確定した**（§4.3）——**ドライブ消費側はラベルから上限を消す。** **⇒ 本項の残りの用途は「他の 3 通りにも同じ問題があるか」を報告することである。★本サブでは直さない** |
| **7** | **★★`SM-093`——新規登録の「状態」の初期値が実際に何か。`any` と空文字が同じ扱いか** | **I** | **★ledger は「初期値が空文字で `any` ではない」と書いている。裏取り。★同じ扱いなら表示だけの問題である** |
| **8** | **`RecipeBuilder` / `SetupRecipeEditor` の optgroup の呼び名の全数** | **II** | **★`SM-112`。★2 か所に同じ被りが在る見込み** |
| **9** | **★トーストの表示時間と、ヘッダとの重なり方** | **I** | **★`SM-060`。★「しばらく消えない」の実体を秒で測ること** |
| **10** | **★エラー表示が現在どこに出ているか**（保存ボタンの上 ／ トースト ／ 両方） | **I** | **★`SM-067`。★ledger は「M12-02 で保存直上＋トーストへ統一済」と書いている。裏取り** |
| **11** | **★★エディタで「選択肢が少なくても素早く入力を進めたい」の実体**（**★開発者が `D-559` で本サブへ回した項目**）。**入力完了までの操作回数を数えること** | **I** | **★★これは ledger の 9 項目に無い。開発者が `M24-02` の確認事項 2 で本サブへ渡した**〔逐語＝**「新規登録＋編集画面は選択肢が少なくても素早く入力を進めたい」**〕。**⇒ 手数を数えないと手が決まらない** |
| **12** | **★`/setups/:setupId`（セットプレイ編集）が同じ部品を通っているか** | **II** | **★`CO-003` と `SM-112` が及ぶ。★通っていなければ、及ぶ範囲を報告すること** |
| **13** | **★エディタのレシピ表示が `M24-03` の `RecipeText` を通っているか** | **II** | **★通っていないなら、揃えるか揃えないかの判断が要る**（§1.1.1） |
| **14** | **★★「隠す」の状態がブラウザストレージに保持されているか**（`CollapsibleFieldset`） | **II** | **★保持しているなら台帳に載っているはずである。載っていなければ `CLAUDE.md` §10.X 違反であり報告** |
| **15** | **★`M24-11` と触るファイルが交わらないか**（同サブは `internal/` の書き込みトランザクション） | **II** | §2.4 |

---

## 4. 詳細仕様

### 4.1 ★★`CO-003`——**dirty 確認ダイアログ**（**★データ保持はやらない**）

| 論点 | 決めたこと |
|---|---|
| **★★やること** | **保存していない変更がある状態で離脱しようとしたら、確認を出す** |
| **★★やらないこと** | **入力の保持**（§1.1.2 / §1.3-1）。**★「あとで戻ったら残っている」は作らない** |
| **形式** | **モーダル。** **★`DES-006` §11 の「選ばないと次へ進めないものはモーダルが正しい」に当たる**（先例＝`VAL-N02`） |
| **★対象の離脱経路** | **★★§3.3-1 の実査で確定する。** **★少なくとも「ヘッダのリンク」「ブラウザバック」の 2 つは含めること**——**`SM-060` が「ヘッダでの画面遷移」を名指ししている** |
| **★★対象の面** | **`/combos/new` ／ `/combos/:id/edit` ／ `/setups/:setupId`**（**★3 面目は §3.3-12 で確かめる。通っていなければ報告**） |
| **★保存直後は出さない** | **保存が成功した直後の遷移で確認が出ると、毎回邪魔になる。** **⇒ 保存の成功で dirty を落とすこと** |
| **★「変更がある」の判定** | **★初期値と現在値の比較で足りる。** **★形は製造の裁量。ただし「触ったら dirty」（フォーカスしただけで立つ）にしないこと** |

**★★`REQ-001` §7 の「編集中の画面遷移時データ保持」は本サブでは満たされない。** **⇒ 完了報告に「これは満たしていない」と明記すること**（**★followup を割るのは設計卓の手番**）。

### 4.2 ★★`SM-148` と `SM-118`——**同じ縦を争う 2 件。順序を決める**

**★★`SM-148`（タブ分け）を先に決める。`SM-118`（左辺を広げる）はその結論に従う。**

**理由＝タブに分けると「隠す」の意味が変わる。** **逆順で決めると、`SM-118` を直した後に `SM-148` がそれを壊す。**

#### 4.2.1 `SM-148`（基本情報とレシピをタブに分ける）

| 論点 | 決めたこと |
|---|---|
| **★★形——原文は「検討したい」である** | **原文の逐語＝「基本情報とレシピのタブ分け**も**検討したい」**（`Memo_Someday.txt` line 337）。**★★「も」と「検討したい」であり、確定要求ではない。** **⇒ タブが最善かを Plan Mode で判断してよい。★タブ以外の形を採るなら理由を報告すること。★逆に、タブが最善だと判断したなら、その理由も報告すること** |
| **★背景** | **同注記＝「上部 line 267（friend FB①）は `M15-05`（表示項目整理・PC 2 カラム化）で一度手当てしているが、その後さらに項目が増えた形」。** **★★つまり本件は M15-05 の再発である。⇒ 「今回また増えたら同じことが起きる」を防ぐ形を選ぶこと** |
| **★★分ける単位** | **基本情報 ／ レシピ の 2 つ。★3 つ以上に割らないこと**——**memo が言っているのは 2 分割である** |
| **★★保存はタブをまたぐ** | **★どちらのタブに居ても保存できること。** **⇒ タブを切り替えないと保存できない形にしない** |
| **★★エラーがあるタブが分かること** | **★★保存に失敗したとき、エラーが「今見ていないタブ」に在ると、利用者は何が悪いか分からない。** **⇒ タブの見出しにエラーの有無を出すこと。これは落とせない条件である** |
| **★★モードによる差** | **新規 / 編集 / コピー で同じ形にすること**（`DES-005` §5.7 の 4 モード。**★保存後の遷移先だけがモードで違う＝`CHANGE-130`。それは変えない**） |
| **★状態の保持** | **★どのタブを開いていたかを保持するかは製造の裁量。** **保持するなら `web/CLAUDE.md` §1 台帳へ登録すること**（`CLAUDE.md` §10.X） |

#### 4.2.2 `SM-118`（「隠す」が縦に縮むだけ）

| 論点 | 決めたこと |
|---|---|
| **★★`SM-148` の結論に従う** | **タブに分けた結果「隠す」が不要になるなら、無理に残さないこと。** **⇒ その旨を報告する** |
| **残す場合** | **★「隠したぶん、もう一方のカラムが広がる」ようにする**（memo の要求そのもの） |
| **★★優先順位が原文に在る**（**2026-08-27 是正＝`M-87`**） | **原文の逐語＝「左辺の情報（メタデータ）、右辺の情報（コンボレシピ）を折りたたんで、他方のサイズを広げる処理を付けて欲しい。★両方に着けるのが変ならコンボレシピを優先。」**（`Memo_Someday.txt` line 305）。 **⇒ ★片方だけにするなら「コンボレシピ側を広げる」を採ること。** **★★この指定は ledger の要約列から落ちていた**（`M-82` の同型・3 度目） |
| **★★1 カラムのときは何もしない** | **★狭い画面では左右に並んでいない見込みである。** **⇒ 広げる先が無い状態で壊れないこと**（§3.3-4 で確かめる） |

### 4.3 ★`SM-007`（＝`CO-005`）／ `SM-101`——**ゲージ欄の表記を揃える**

| 論点 | 決めたこと |
|---|---|
| **`SM-007`** | **「(0〜6)」→「(0〜6本)」。日本語版のみ。★ドライブ始動側は既に「本」付きであり、それに揃える** |
| **★★`SM-101`（2026-08-27 確定・開発者回答）** | **★実際の上限は 20 のまま変えない。** **★★ラベルを `ドライブゲージ消費(0〜20本・0.5刻み)` → `ドライブゲージ消費(0.5刻み)` へ変える。** **⇒ 上限を書かない。** |
| **★★理由（開発者の逐語）** | **「20もあれば特殊なコンボを対象にする場合も足りるはずですが、20の根拠が薄いため、ラベルが違和感を出しているのだと気づきました。」** **⇒ 直すべきは値ではなく「根拠の薄い数をラベルに書いていること」であった** |
| **★★設計卓の暫定案は撤回する** | **v1.0.0 は「表示を実際の上限へ揃える」としていた。★開発者は「揃える」ではなく「消す」を選んだ。** **⇒ 揃えるべきは数ではなく、「上限を書くかどうかの規則」である** |
| **★★ここから出る原則** | **★上限をラベルに書くのは、その上限に根拠が説明できるときだけにする。** **★説明できない上限は書かない**（`0.5 刻み` のような入力の作法は書く） |
| **★★残り 3 通りは実査して報告するだけ**（**本サブでは直さない**） | **開発者が名指ししたのはドライブゲージ消費の 1 件だけである。** **⇒ 残る 3 通り**〔ドライブ始動側 `(0〜6本・0.5刻み)` ／ SA 始動側 `(0〜3)` ／ SA 消費側 `(0〜6)`〕**について、上の原則に照らして「上限の根拠が説明できるか」を §3.3-6 で確かめ、説明できないものを見つけたら報告すること。★勝手に消さないこと**——**開発者の指示の射程を超える** |
| **★語彙** | **★4 通り（始動側・消費側 × ドライブ・SA）の文言を 1 か所で組み立てられるなら、そうすること。** **★第 2 の語彙を作らない** |

### 4.4 ★`SM-093`——**状態の既定を「不問」にする**

| 論点 | 決めたこと |
|---|---|
| **やること** | **新規登録の「状態」の初期値を `any`（不問）にする** |
| **★値の新設は不要** | **相手スタンスには `any` が既にある**（ledger の実査。**§3.3-7 で裏取り**） |
| **★★空文字と `any` が同じ扱いなら、表示だけの問題である** | **★§3.3-7 で確かめること。** **同じなら「初期値を変える」ではなく「初期値が `any` として選択されて見えるようにする」が正しい手である** |
| **★既存データに触らない** | **★初期値の変更であり、保存済みのコンボの値は変えない。★マイグレは要らない** |

### 4.5 ★`SM-112`——**「システム」と「共通システム（移動・その他）」の呼び名の被り**

| 論点 | 決めたこと |
|---|---|
| **やること** | **★画面上の呼び名の被りを解消する** |
| **★★振り分けの原則には触れない** | **M16-04 が決めている**（§1.3-8）。**★どの技がどちらへ入るかは変えない** |
| **★2 か所ある見込み** | **`RecipeBuilder` と `SetupRecipeEditor`**（ledger）。**★§3.8 で全数を確かめ、両方直すこと** |
| **★呼び名は製造の裁量** | **★ただし「どちらが何を含むか」が呼び名から読めること。** **★選んだ理由を報告すること** |

### 4.6 ★`SM-060`——**保存成功のトーストがヘッダを邪魔する**

| 論点 | 決めたこと |
|---|---|
| **★★実体を測る** | **§3.3-9 で表示時間を秒で測ること。** **★「しばらく」は症状であって機序ではない** |
| **★手は 2 通りある** | **(a) 表示時間を短くする ／ (b) 位置を変えてヘッダと重ならないようにする。** **★★どちらを採るかは実測で決める**——**重なっていないのに長いだけなら (a)、重なっているなら (b)** |
| **★消せること** | **★どちらを採っても、利用者が明示的に消せる経路を 1 つ残すこと** |
| **★トーストの機構は作り直さない** | **M12-02・`CHANGE-045` C-04 で導入されたもの。★調整にとどめる** |

### 4.7 ★`SM-067`——**エラーメッセージの位置**（**★★確認事項 1 の回答を待つ**）

**★★本項は §11 確認事項 1 の回答が出るまで実装しない。** **⇒ 他の項目から着手してよい。**

**ledger の注記＝「M12-02 で保存直上＋トーストへ統一済・自動スクロール方式は保留」。**

**論点＝先頭へ自動スクロールする方式へ変えるか。**

**★★§4.2.1 の「エラーがあるタブが分かること」と隣接する**——**タブに分けると、エラーが別タブに在る場合がある。⇒ 自動スクロールだけでは足りない。**

### 4.8 ★モック HTML（`M24-overview` §1.5）

**次の 4 つについて、変更前と変更後を並べた単一 HTML を `docs/progress/M24-04-mock/` へ置くこと。**

1. **★★`SM-148` のタブ分け**（§4.2.1。**★エラーが別タブに在るときの見え方も出すこと**）
2. **`SM-118` の「隠す」の挙動**（§4.2.2。**★`SM-148` の結論に従った形で**）
3. **dirty 確認ダイアログ**（§4.1）
4. **ゲージ欄の表記**（§4.3。**★4 通りを並べること**）

**★★Plan Mode の提示に含めること。実装後ではない。** **★作り込まないこと。**

**★§3.3 の実測で「既に解消していた」と分かった項目は、その項目のモックは「現状の写し」1 枚でよい**（先例＝`M24-01` の `SM-002` ／ `M24-03` の `SM-096`）。

---

## 5. テスト要件

### 5.1 追加するテスト

| 層 | 内容 |
|---|---|
| **純粋関数** | **★必須**。**dirty の判定**（初期値と現在値の比較。**★空文字と `null` と未設定の扱い**）／ **ゲージ欄の文言組み立て**（4 通り） |
| **コンポーネント** | **★★保存の成功で dirty が落ちること**（§4.1）／ **★エラーがあるタブが見出しから分かること**（§4.2.1）／ **★状態の初期値が `any` であること**（§4.4） |
| **E2E** | **★4 ケース**——**(1) 入力途中でヘッダのリンクを踏むと確認が出ること ／ (2) 保存直後の遷移では確認が出ないこと ／ (3) タブを切り替えても入力が消えないこと ／ (4) 別タブにエラーがある状態で保存すると、そのタブが分かること** |

**★E2E の共通の下ごしらえは `web/e2e/support/` へ寄せること**（`D-553` が運用の型として承認済み。**★同じ数行を複数 spec へ複製しない**＝`E-232`）。

**★★`M24-03` の教訓が効く**——**`getByRole` の `name` は既定で部分一致である。** **⇒ 文言の破壊確認は「追記」ではなく「置換」で行うこと**（`M24-03` で実際に空振りした）。

### 5.2 既存テストの扱い

- **`DES-005` §5.7 の保存後の遷移先を守るテストを落とさない**（`M24-01` の成果物）。
- **`M24-03` の起き攻めラベルの再発防止テスト 2 本を落とさない**（§1.3-4）。
- **★項目数をベタ書きしているテストを見つけたら、数ではなく「定義した項目がすべて出る」形へ書き直すこと**（`M24-01` §7-8）。

### 5.3 ★★破壊確認（**4 件・必須**）

**★破壊確認を書く前に 2 つを確かめること**（計測点 `M-78` ／ playbook §4.32）——**(a) 壊す対象が実在するか** ／ **(b) ★その壊し方で本当に観測が変わるか。**

**★★あわせて 3 つ目**（`D-572` ／ playbook §4.32）——**★期待経路は「壊す対象」からではなく「その観測が何を見ているか」から立てること。**

| # | 壊すもの | 期待 | 同じ層で書けているか |
|---|---|---|---|
| **1** | **dirty の判定を「常に false」へ固定する** | **E2E (1) が赤くなる** | **★どちらもフロント側** |
| **2** | **保存の成功で dirty を落とすのをやめる** | **★E2E (2) が赤くなる** | 同上 |
| **3** | **タブの見出しからエラーの表示を外す** | **★E2E (4) とコンポーネントテストが赤くなる** | 同上 |
| **4** | **状態の初期値を空文字へ戻す** | **★コンポーネントテストが赤くなる** | 同上 |

**★赤くなる経路を数えること**——**赤くならない経路があるなら、その経路は守られていない。**

---

## 6. レビュー観点（別ファイル参照）

`docs/instructions/reviews/M24-04-review-checklist.md`

---

## 7. 完了条件（Definition of Done）

### 7.1 機能要件

1. **§3.3 の実査 15 件が実施され、結果が報告されている**（**★母数の要る項目は母数付きで**＝§3.3-2 / -3 / -6 / -8 / -9 / -11）。
2. **§4.1 の dirty 確認が動いている**（**★保存直後は出ない**）。
3. **★★§4.1 の「編集中の画面遷移時データ保持」は満たしていないことが、完了報告に明記されている**（§1.1.2）。
4. **§4.2 の `SM-148` / `SM-118` が、その順序で決められている**（**★`SM-118` の結論が `SM-148` に従っていること**）。
5. **§4.3 / §4.4 / §4.5 / §4.6 が実装されている**（**★§3.3 で「既に解消していた」と分かった項目は、実装せず成立をテストで固定して報告する**）。
6. **§4.7 は確認事項 1 の回答が出てから実装されている**（**★回答が出なかった場合は未実装で報告する。それは欠陥ではない**）。
7. **★§3.3-11（エディタの入力の手数）が数えられ、手が要るか要らないかが報告されている**（**★開発者が `D-559` で本サブへ回した項目である**）。
8. **§4.8 のモック HTML が Plan Mode で提示されている。**

### 7.2 自己テスト結果

- `go test ./... -count=1`（**★本サブは Go を触らない見込みなので、着手前と同じであることの対照**）
- `cd web && pnpm test`
- `make e2e`（**★実行直前にポートが空いていることを確認し、その出力も載せる**）
- **破壊確認 4 件**（§5.3）。**実施コマンドと出力を載せること。**

**★★基準値は「変更を入れる前」に、切らずに採ること**（`M24-02` §7-2 の教訓＝**採取コマンドにパイプで切り詰めを入れない**）。

**★★母数は「指示書に書いてあるから使える」ものではない**（playbook §4.37）。**⇒ 比較は同一セッション内の背中合わせで採ること。**

### 7.3 品質チェック

- `bash scripts/check-artifact-integrity.sh`（**★1 本目に回す**）
- `bash scripts/check-browser-storage-keys.sh`（**★新キーを作った場合は必ず**）
- `bash scripts/check-md-emphasis.sh` ／ `bash scripts/check-doc-refs.sh` ／ `bash scripts/check-progress-log-index.sh` ／ `bash scripts/check-enum-sync.sh`
- **★`check-progress-log-index.sh` は偽の緑を返しうる**（followup §AH）。**緑が出ても索引行を目で確かめること。**

### 7.4 ドキュメント

- **`docs/progress/progress-log.md` への追記**（製造担当が行う。`CLAUDE.md` §8・playbook §11.1〜§11.3）。**指示書が明示的に求めていなくても追記する。**

#### 7.4.1 ★CHANGE 要否の判定（**as-built で判定する**）

**`CHANGE-137` は起票済みである。** **★以下は「射程が広がったか」の判定である。1 件ずつ判定し、結果を完了報告に書くこと。**

| # | 条件 | 該当したら |
|---|---|---|
| 1 | **`DES-002` §4.2 の経路表に載る API を新設・変更したか**（§2.3 の例外条項） | 設計卓へ報告。`CHANGE-137` の射程へ加える |
| 2 | `DES-003` のスキーマに触れたか | 同上 |
| 3 | **`DES-006` の VAL の判定結果が変わったか**（**★`SM-101` の上限 ／ `SM-093` の初期値**） | **★同上。射程内の見込み** |
| 4 | **★`REQ-001` §7 の項目の状態が変わるか**（**「編集中の画面遷移時データ保持」**） | **★同上。★本サブでは満たさない見込み** |
| 5 | **★`SUPP-001` §5.2 のパッケージ構成に増減があるか** | 同上（**★`SUPP-001` の契約を持つ節は CHANGE を要する**＝`D-564`） |
| 6 | **★`SUPP-001` §5.5 のテスト方針が変わるか** | 同上 |
| **7** | **★★ブラウザストレージのキーを新設したか**（§4.2.1 のタブ状態 ／ §3.3-14） | **`web/CLAUDE.md` §1 台帳へ登録し、`DES-005` §5.7 へも書く。★`CHANGE-137` の射程内である** |
| **8** | **★マイグレーションを消費したか**（見込みは 0 本） | **★消費したらボード §2.2 の連番セルの更新が要る。設計卓へ報告** |

### 7.5 完了報告

`docs/progress/M24-04-completion-report.md`。**★工程順として「まだ無い」ものを過去形で書かないこと**（`M24-09b` §7-2）。

**★★件数を本文へ書くときは、その場で数え直したコマンドを併記すること**（`M24-02` §7-3 の教訓）。

---

## 8. 参照ドキュメント

| ID | パス | 用途 |
|---|---|---|
| `DES-005` §5.7 | `docs/design/05-screen-design.md` | **登録・編集画面。★`CHANGE-130` で保存後の遷移先が確定** |
| `DES-005` §5.4 / §5.6 項目6 | 同上 | **★`CHANGE-134` で改訂されたばかり** |
| `DES-006` §11 | `docs/design/06-validation.md` | **警告の表示と操作の分岐** |
| `REQ-001` §7 フェーズ3 | `docs/design/requirements.md` | **★「編集中の画面遷移時データ保持」** |
| `code-facts.md` | `docs/handover/code-facts.md` | 実装の索引 |
| ledger | `docs/progress/M24-RESEARCH-01-report.md` §2 / §3 | **9 項目の索引。★★要約列は原文ではない**（`M-82`）。**★原文は `docs/human-notes/Memo_Someday.txt`** |
| `CHANGE-137` | `docs/change-notes/CHANGE-137-notification.md` | 本サブの通知書 |

---

## 9. 注意事項・判断に迷ったら

### 9.1 推測で進めてはいけない事項

- **★★「編集中の画面遷移時データ保持」を作ること**（§1.3-1。**別の要求である**）。
- **`SM-067` の解き方**（§4.7。**確認事項 1 の回答を待つ**）。
- **★`SM-101` の実際の上限（20）を変えること**（§4.3。**仕様変更であり本サブの外**）。
- **★M16-04 のレシピステップの振り分け原則を変えること**（§1.3-8）。
- **★`M24-03` が寄せた起き攻めラベルの正典を作り直すこと**（§1.3-4）。
- **`DES-005` §5.7 の保存後の遷移先を変えること**（`CHANGE-130`）。
- **`internal/` を触ること**（§2.3 の 3 条件をすべて満たすときのみ）。
- **新規依存の追加**（`CLAUDE.md` §6）。
- **ブラウザストレージの新キーを台帳へ登録せずに使うこと**（`CLAUDE.md` §10.X）。

### 9.2 推測で進めてよい事項（その旨を明示）

- **dirty の判定の実装の形**（§4.1）。
- **タブの見た目・切替の作法**（§4.2.1）。
- **`SM-112` の新しい呼び名**（§4.5。**★選んだ理由を報告すること**）。
- **トーストの表示時間・位置**（§4.6。**★実測に基づくこと**）。
- **`web/e2e/support/` へ寄せるヘルパの命名とシグネチャ。**

### 9.3 不明事項発見時の対応

**★★「その症状は既に解消していた」場合は、実装せず、成立をテストで固定して報告すること**（`M24-01` の `SM-002` / `SM-004`、`M24-03` の `SM-096` と同じ形）。**★これは欠陥ではない。**

**★★「実査の答えが指示書と違った」場合も報告すること。** **★とくに §3.3-7（`SM-093` の初期値）と §3.3-6（ゲージの上限）は、ledger の実査を写しただけであり、設計卓は裏を取っていない。**

**★★「memo の見立てが機序と違った」場合も報告すること**（`M24-03` の `SM-096` の先例）。**★見立てを実装の根拠にしない。**

### 9.4 ★CHANGE 要否

§7.4.1 のとおり。**★判定した結果「射程は広がらなかった」であっても、判定したこと自体を報告に書くこと。**

---

## 10. 完了後の次ステップ

- 設計卓が受理し、**`CHANGE-137` を `DES-005` §5.7 へ反映する。**
- **★★followup `F12-3 / E-3` を 2 行へ割る**（§1.1.2。**設計卓の手番**）——**dirty 確認（本サブで解消）と、編集中のデータ保持（未着手・`REQ-001` §7）。**
- **followup の該当行を畳む。**
- **`M24-07`（i18n）へ渡すものを設計伝達レポート §4 に並べる。**

---

## 11. 開発者への確認事項

| # | 問い | 設計卓の暫定案 | 止まるか |
|---|---|---|---|
| **1** | **★★`SM-067`（エラーメッセージの位置）。** **ledger の注記＝「M12-02 で保存直上＋トーストへ統一済・自動スクロール方式は保留」。** **論点＝先頭へ自動スクロールする方式へ変えるか** | **★暫定案＝変えない。** 理由＝**`SM-148` でタブに分けると、エラーが「今見ていないタブ」に在る場合が生まれる。⇒ 自動スクロールだけでは届かない。** **★★代わりに「タブの見出しにエラーの有無を出す」を落とせない条件にした**（§4.2.1）。 **⇒ タブ分けの後で、まだ足りないかを見てから決めたい** | **★§4.7 だけ止まる。** 他の 8 項目は先に進められる |
| ~~**2**~~ | ~~`SM-101` のドライブゲージ消費の上限は、実際に 20 でよいか~~ | **★★2026-08-27 決着**（**D-576**）——**上限は 20 のまま。ラベルを `ドライブゲージ消費(0.5刻み)` へ変える**〔**開発者の逐語＝「20の根拠が薄いため、ラベルが違和感を出しているのだと気づきました」**〕。**★設計卓の暫定案「揃える」は撤回した。** **⇒ §4.3 へ反映済み** | **決着済み** |

---

*以上、指示書 M24-04 **v1.1.0**。* **★v1.0.0 からの変更＝確認事項 2 が決着し、確認事項は 1 件になった**（`D-576`）。**★★`SM-101` はラベルから上限を消す形で確定した。設計卓の暫定案「揃える」は撤回した。** **★★あわせて `SM-118` の原文から落ちていた指定「両方に着けるのが変ならコンボレシピを優先」を §4.2.2 へ足した**（計測点 `M-87`＝`M-82` の同型・3 度目）。**★`SM-148` の原文が「検討したい」であることも §4.2.1 へ明記した。**
