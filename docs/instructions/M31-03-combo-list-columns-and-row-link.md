# 指示書 M31-03: 一覧の列の並び ＋ 詳細への行内リンクの位置

| 項目 | 内容 |
|------|------|
| 文書ID | M31-03 |
| バージョン | **1.1.0**（2026-09-09・**★★§1-5 の `testid-convention.md` への登録要求が `CHANGE-171` で失効したため差し替えた＝`D-787`**） ／ 1.0.0（2026-09-07） |
| 作成日 | 2026-09-07 |
| 作成者 | 設計担当 Claude（設計卓／フェーズ4 期） |
| 対象マイルストーン | **`M31`**（`α②`）。上位＝`docs/instructions/M31-overview.md` v1.4.0 |
| 推奨モデル | **標準実装**（**★★ただし共通部品であり、波及先は一覧を出す全画面である**。実モデル名は `docs/human-notes/model-allocation.md`） |
| Plan Mode | **必須**（`CLAUDE.md` §9＝**共通部品への変更であり全画面へ波及する**） |
| レビュー | **必要**（チェックリストは着手承認の手番で設計卓が起こす＝`D-736`） |
| CHANGE 消費 | **1 本の見込み**（`DES-005` §5.4 の列の並び。**★起票は設計卓。自採番しない**＝`D-293`） |
| マイグレ消費 | **0 本** |
| 前提サブ | **★★`M31-02` の後**（**同じ共通部品の族であり、直列にする**＝§0.3） |
| 射程 identity | **2 件**——`combo-list-column-order-unreviewed` ／ `combo-list-detail-link-on-wrong-column` |
| 配置 | `docs/instructions/M31-03-combo-list-columns-and-row-link.md` |
| 出力 | `docs/progress/M31-03-completion-report.md` |

---

## 0. この指示書の特殊性

### 0.1 ★★2 件は性質が違う。⇒ 混ぜないこと

| # | 件 | 性質 |
|---|---|---|
| **1** | **詳細への行内リンクの位置** | **★★明確な是正である。⇒ 「元々は始動状況にあった」ものがヒット種別へ移っている** |
| **2** | **列の並び** | **★★案が無い。⇒ まず現状を実測して案を出し、開発者が選ぶ**（`M32-01` の改名先と同じ形） |

**★開発者の逐語**（2026-09-07）**:**

> **一覧における列の並びを見直したいです。恐らく今の並びは適当な気がして違和感がありますが、まだ案はありません。**
>
> **それと元々は始動状況に詳細へ飛ぶリンクが張られていましたが、今はヒット種別にリンクが張られていて不自然なのでこれも修正したいです。**

**⇒ 1 は直せる。2 は決めるところから始める。★コミットを分けること。**

### 0.2 ★★列の並びを変えると、既に保存されている利用者の列設定に当たる

> **★★★【2026-09-09 errata＝`D-796` / 計測点 `M-150` (a)】本節の前提は実装事実として成立していなかった。⇒ 設計卓の誤りである。**
>
> **★実測**（`M31-03` の製造 ＋ レビューが独立に検証）**＝保存値は `ColumnVisibility` そのもの、つまり boolean 9 個の JSON オブジェクトだけである。⇒ 並びの情報を持たず、利用者が列を並べ替える手段も無い。★読み出しは `{ ...DEFAULT, ...loaded }` のマージであり、キー順は消費側で使われない。**
>
> **⇒ 既定の並びを変えても保存済みの設定には当たらない。★本節の 3 案はどれを選んでも結果が同じである。**
>
> **★★設計卓が推していた (c)**（キーのバージョンアップ）**はむしろ有害である**——**並びと無関係な ON/OFF 設定だけを捨てることになる。⇒ 製造は実施しなかった。★設計卓は受理する。**
>
> **★★★一般形＝「重い点だ」と言われたものほど、まず型を読むこと。⇒ 保存値の型を読めば 5 分で否定できた。★この位置づけを次の同型サブへ複製しないこと。**
>
> **★★本文は書き換えない**（`D-274` (3)＝**完了済みサブの指示書は注記で追随する**）**。**

**★★これが本サブの唯一の重い点である。**

**★利用者の列設定はブラウザストレージのキー `combo-list-columns-v1` に保存される**（`web/CLAUDE.md` §1 の台帳）**。⇒ 既定の並びを変えたとき、保存済みの設定を持つ利用者に何が起きるかを決める必要がある。**

| 案 | 何が起きるか |
|---|---|
| **(a) 保存済みの並びを尊重する** | **★既に使っている利用者の見た目は変わらない。⇒ ただし「並びを直した」効果がその利用者には届かない** |
| **(b) 新しい既定で上書きする** | **★全員に届く。⇒ ただし自分で並べ替えた利用者の設定が消える** |
| **(c) キーをバージョンアップする**（`-v2`） | **★保存済みは読まれなくなり、新しい既定が効く。⇒ (b) と同じ結果だが、古い値が残るので戻せる** |

**★★設計卓は (c) を推す**——**`web/CLAUDE.md` §1 が「キーバージョニング」を実装ガイドラインに挙げている。⇒ 既にその形が用意されている。**

**★★ただし決めるのは開発者である**（§7-2）**。★台帳への追記が要る**（`§10.X`。**⇒ キーを増やすなら `web/CLAUDE.md` §1 の台帳へ足す。★足さずに使わないこと**）**。**

### 0.3 ★★`M31-02` の後に置く（**直列**）

**★どちらも共通部品の族である。⇒ 同じ手番に 2 つの部品を触ると、赤が出たときに切り分けられない**（`M31-02` を分けたのと同じ理由）**。**

**★★`M31-02` はキャラ選択、本サブは一覧の行である。⇒ 部品は別だが、直列にする。**

---

## 1. 前提として読むもの

| # | 資料 | 読む箇所 |
|---|---|---|
| 1 | `docs/handover/followup-backlog.md` §BX | **`combo-list-column-order-unreviewed` ／ `combo-list-detail-link-on-wrong-column`**（**★開発者の逐語が入っている**） |
| 2 | `docs/design/05-screen-design.md` | **§5.4**（コンボ一覧）／ §5.5（マイコンボ）／ **§5.19b**（ゲーム更新の影響コンボ。**★同じ行の部品を使う**） |
| 3 | `web/CLAUDE.md` | **§1**（ブラウザストレージの台帳。**★`combo-list-columns-v1`**）／ §2（コンボ型の 3 分岐） |
| **★4** | `docs/change-notes/CHANGE-164-notification.md` §3.1 | **★★`M28-02c` が同じ行の部品を触っている**——**専用画面は［コピー］を出さず［削除］を出す。⇒ 行アクションの出し分けが既に在る** |
| 5 | `docs/design/testid-convention.md` | **★★命名規則だけを見ること**（`{feature}-{element}`・小文字ケバブケース）**。⇒ 「付与済み一覧」への登録義務は `CHANGE-171`**（2026-09-08）**で無くなった。★同書は全数ではない**——**全数が要るときは `grep -rn "data-testid" web/src --include='*.tsx'` を引く** |

---

## 2. やること

### 2.1 段 1 — 実査（**★★コードを書く前に、これを済ませて設計卓へ報告すること**）

| # | 確かめること | なぜ |
|---|---|---|
| **★★1** | **現在の列の並びを全数で書き出す**（列の定義と、既定の表示・非表示） | **★★案を出す土台である。⇒ 並びを「適当」と評する前に、いまの並びを示す** |
| **★★2** | **一覧の行の部品を使っている画面の全数**（`DES-005` の節と対応させる） | **★★波及の確認の区分の割り方の根拠になる。★`§5.19b` の専用画面も使っている** |
| **★★3** | **詳細へのリンクが、いつ・どの手番で「始動状況」から「ヒット種別」へ移ったか** | **★★意図した移動なら、その理由が記録されているはずである。⇒ 記録が在れば、戻すことが退行になりうる。★記録が無ければ事故である** |
| **★4** | **`combo-list-columns-v1` に何が保存されるか**（保存される値の形） | **★§0.2 の判断に要る** |
| **5** | **列ごとの幅・折り返しの扱い** | **★並びを変えると横幅の収まりが変わる** |

### 2.2 段 2 — 行内リンクの位置を戻す（**★先にこちらをやる**）

| # | やること |
|---|---|
| **★★1** | **詳細へのリンクを、始動状況の列へ戻す**（段 1-3 で「事故」と判明した場合）**。** **★★★【2026-09-09 errata＝`D-796` / 計測点 `M-150` (b)】戻し先が実在しなかった。⇒ 「始動状況」という列は `M27-03`**（2026-09-05）**で 3 列**（ヒット種別 / 始動位置 / 相手の状態）**へ割れており、存在しない。** **★製造は §2.2-2 に従って止め、開発者へ返した。⇒ 開発者は「ルート列」を選んだ**（2026-09-09）**。★止めた判断は正しい。** |
| **★★2** | **段 1-3 で「意図した移動」と判明した場合は、止めて設計卓へ報告する。⇒ 戻すことが退行になりうる** |
| **★3** | **リンクは 1 列にだけ張ること。⇒ 2 列から飛べる形にしない**（**★どちらを押しても同じなら、片方は要らない**） |
| **★4** | **キーボードで到達できること**（**★リンクを移すと tab 順が変わる**） |

### 2.3 段 3 — 列の並びの案を出す（**★開発者が選ぶ**）

| # | やること |
|---|---|
| **★★1** | **並びの案を 2〜3 本出す。★案ごとに「何を優先した並びか」を 1 行付けること**（例＝入力の順 ／ 見る頻度の順 ／ 幅の収まり） |
| **★★2** | **★★段 1-1 の現状と並べて示すこと。⇒ 差分が見えないと選べない** |
| **★★3** | **§0.2 の 3 案**（保存済みを尊重 ／ 上書き ／ キーをバージョンアップ）**についても案を添える** |
| **★4** | **設計卓へ報告して回答を待つ。⇒ 勝手に決めて実装しない** |

### 2.4 段 4 — 並びを入れる（**★開発者の回答を得てから**）

| # | やること |
|---|---|
| **★★1** | **選ばれた並びを既定にする** |
| **★★2** | **`combo-list-columns-v1` の扱いを、選ばれた案どおりにする。★キーを増やすなら `web/CLAUDE.md` §1 の台帳へ足す**（`CLAUDE.md` §10.X。**⇒ 台帳に無いキーを使わない**） |
| **★3** | **`DES-005` §5.4 の列の記述と食い違わないこと。★食い違うなら止めて CHANGE を請求する**（`D-293`） |
| **★4** | **`§5.19b` の専用画面が既定の列設定を渡していることを壊さない**（`CHANGE-164` §3。**⇒ 専用画面は利用者の列設定を読まない**） |

---

## 3. やらないこと

| # | 内容 | 理由 |
|---|---|---|
| **★★1** | **並びを、開発者の回答を得ずに入れる** | **★§2.3-4。⇒ 案が無いことが本件の出発点である** |
| **★★2** | **列を足す ／ 減らす** | **★★本サブの射程は「並び」である。⇒ 列の増減は別の判断であり、`DES-005` の契約に及ぶ** |
| **★3** | **行アクションの出し分けを変える** | **★`CHANGE-164` §3.1 が確定させたばかりである**（専用画面はコピーを出さない） |
| **★4** | **`combo-list-columns-v1` を台帳に無い形で使う** | `CLAUDE.md` §10.X |
| **★5** | **`M31-01` / `M31-02` の射程に手を出す** | 別サブ |
| **★6** | **番号を自採番する** | `D-293` |
| **★7** | **`followup-backlog.md` の本表を編集する** | `D-382`（**★書けるのは §J だけ**） |

---

## 4. ★★危険（**先に読むこと**）

### 4.1 ★★最大の危険＝**保存済みの列設定を黙って壊すこと**

**§0.2。⇒ 既に自分で並べ替えた利用者が居る。★既定を変えるとき、その設定をどう扱うかを決めずに入れると、利用者の手元で見た目が変わるか、変わらないかのどちらかが黙って起きる。**

**★★どちらであっても、決めてから入れること。⇒ 「たまたまこうなった」を作らない。**

### 4.2 ★★2 番目＝**「元々こうだった」を確かめずに戻すこと**

**★段 1-3。⇒ リンクが移ったのが意図した移動なら、戻すことが退行である。**

**★★記録が見つからない場合は「見つからなかった」と書いて、事故として扱ってよい。⇒ ただし「探した」ことを示すこと。**

### 4.3 ★3 番目＝**波及の確認が偏ること**

**★段 1-2 で数えた画面の区分から 1 件ずつ取ること**（`SUPP-001` §5.5.4 (10)。計測点 `M-139`）**。**

**★★`§5.19b` の専用画面を落とさないこと**——**同画面は列設定の扱いが他と違う**（既定の列設定を渡し、利用者の設定を読まない＝`CHANGE-164` §3）**。⇒ 区分が違う。**

### 4.4 ★4 番目＝**2 件を同じコミットに混ぜること**

**§0.1。⇒ 片方は是正、片方は決めごとである。★赤が出たときに切り分けられなくなる。**

---

## 5. テスト

| # | 何を | 判定 |
|---|---|---|
| **★★1** | **行内リンクが始動状況の列に在り、詳細へ飛ぶこと** | **★段 1-3 の再現**（いまはヒット種別に在る）**を陽性対照にする** |
| **★★2** | **リンクが 2 列から張られていないこと** | **★★走査で示すこと。⇒ 「片方を消した」だけでは、もう片方が残っていないことを示せない** |
| **★★3** | **【破壊確認・対象の名指し】段 1-2 の区分から 1 件ずつ**、**列の並びが既定どおりに出ること。★`§5.19b` の専用画面を必ず含めること** | **★§4.3** |
| **★★4** | **【破壊確認】保存済みの列設定を持つ状態を作り、選ばれた案どおりの挙動になること** | **★★案 (a) なら変わらない ／ (b)(c) なら新しい既定になる。⇒ どちらかを実測で示す** |
| **★5** | **キーボードで詳細へ到達できること** | **★§2.2-4** |
| 6 | **`cd web && pnpm test`** | **★出力で判定する** |
| **★7** | **`make e2e-only P=<パターン>`**（**★`playwright` を直接叩かない**＝`CLAUDE.md` §11） | **★一覧を含む spec を回す** |

---

## 6. 完了条件

| # | 条件 |
|---|---|
| **★★1** | **段 1 の実査 5 件に結果がある**（**★1 と 2 は数で。★3 は「見つかった / 見つからなかった」で**） |
| **★★2** | **行内リンクが戻っている**（または「意図した移動だった」ことが示され、止めている） |
| **★★3** | **列の並びの案が 2〜3 本、現状と並べて示されている** |
| **★★4** | **`combo-list-columns-v1` の扱いの案が添えられている** |
| **★★5** | **段 4 は、開発者の回答を得てから実施されている** |
| **★★6** | **§5 の破壊確認 2 件が実測で示され、区分の割り方が報告にある** |
| **★7** | **キーを増やしたなら `web/CLAUDE.md` §1 の台帳へ足し、`bash scripts/check-browser-storage-keys.sh` が緑** |
| 8 | **§5 の検査がすべて緑**（**★出力で判定**） |
| **★9** | **コミットが 2 件で分かれている**（是正 ／ 並び） |
| **★10** | **`docs/progress/M31-03-completion-report.md` を書き、`docs/progress/progress-log.md` へ索引行を 1 行足す**（`CLAUDE.md` §8） |
| **★★11** | **設計伝達レポートを出す**（`/design_handover_report`）**。★§4 に `DES-005` §5.4 への CHANGE の請求を書くこと** |

---

## 7. ★開発者への確認事項

| # | 事項 | 設計卓の状態 |
|---|---|---|
| **★★1** | **列の並びをどれにするか** | **★★製造が段 3 で 2〜3 案を出し、開発者が選ぶ。⇒ 案が無いことが本件の出発点である**（開発者の逐語＝「まだ案はありません」） |
| **★★2** | **保存済みの列設定をどう扱うか** | **★★設計卓は (c) キーをバージョンアップするを推す**（`web/CLAUDE.md` §1 が「キーバージョニング」を実装ガイドラインに挙げている）**。★ただし決めるのは開発者である** |
| **★3** | **行内リンクが「意図した移動」だった場合** | **★段 1-3 で判明したら止めて報告させる。⇒ 戻すことが退行になりうる** |

---

## 8. 参照ドキュメント

| ID | パス | 参照する箇所 |
|----|------|------------|
| — | `docs/instructions/M31-overview.md` v1.4.0 | §3（サブ構成） |
| — | `docs/handover/followup-backlog.md` §BX | **開発者の逐語 2 件** |
| DES-005 | `docs/design/05-screen-design.md` | §5.4 ／ §5.5 ／ **§5.19b** |
| — | `web/CLAUDE.md` | §1（台帳）／ §2 |
| — | `docs/change-notes/CHANGE-164-notification.md` | §3.1（**行アクションの出し分け**）／ §3（専用画面の列設定） |
| SUPP-001 | `docs/design/supp-001-detailed-design.md` | §5.5.4 (10)（**破壊確認の対象の取り方**） |
| — | `docs/process/parallel-board.md` | `D-772`（本件の記録）／ `D-774`（本サブの起票）／ `D-293` ／ `D-382` |

---

*以上、指示書 M31-03 **v1.1.0**。* **★★本サブの落とし穴は「並びを直すだけ」と読むことである。** **★実際には、既に自分で並べ替えた利用者の設定に当たる**（§0.2）**——決めずに入れると、利用者の手元で見た目が変わるか変わらないかが黙って起きる。** **★★もう 1 つは「元々こうだった」を確かめずに戻すことである**（§4.2）**——意図した移動なら、戻すほうが退行である。**
