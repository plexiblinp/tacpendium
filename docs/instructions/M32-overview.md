# M32-overview: `B` 摩擦低減（署名・AV・データ鮮度・操作説明書・宣伝・クラシック前提の明示）

| 項目 | 内容 |
|------|------|
| 文書種別 | 補足資料（overview。CHANGE 通知書対象外・自由改訂） |
| バージョン | **1.8.0**（2026-09-17・**★★`M32-03` を受理した**＝`D-889`。**⇒ `M32` は 3 サブとも完了。★残る開発者の手番は撮影だけである**） ／ **1.7.0**（2026-09-14・**★★`M32-03` を新設した**＝`D-882`・開発者承認〔操作説明書を `M37` の着地へ追随させる。**改稿 8 章 ＋ 撮影の置き場の点検**〕**。★§3.2b を新設。★`M32` は 3 サブになった**） ／ **1.6.0**（2026-09-14・**★★`M32-02` の現在地を確定した**＝`D-879`〔**走行済み・開発者の撮影の手番で停止中・未マージ**〕**。★§3.2 の末尾へ影響章の表を置いた**〔**改稿が要るのは 8 章 ／ `M37-07` を待つのは 3 章**〕） ／ **1.5.0**（2026-09-12・**★§4 の確認事項 3 / 5 に打ち消し線を追随させた**〔**★どちらも 2026-09-06 に決着済みなのに、開かれたままに見えていた**＝`D-848`〕） ／ **1.4.0**（2026-09-12・**★★`M32-01` を受理した＝`D-847`。⇒ §1.1 の `B3` 逐語の日付を `2026-08-03` へ是正**〔`D-764` が片側だけ直していた〕） ／ **1.3.0**（2026-09-06・**★★2 サブを起票した＝`D-763`**〔`M32-01` v1.0.0 ／ `M32-02` v1.0.0〕**。★あわせて §3.1-1 の失効と §5-4 の実測値を反映した**） ／ 1.2.0（2026-09-06・**★★確認事項が全件決着＝`D-757`。⇒ 承認済み・起票可**） ／ 1.1.0（2026-09-06・**★★確認事項 3 件が決着＝`D-755`**〔`README.txt` の生成方式＝(b) ／ 動画は射程外 ／ `B1` / `B2` / `B3` / `B5` は完了条件に含めない〕**。★§1.1 を新設し `B1`〜`B6` の逐語を本書へ埋めた**〔計測点 `M-134`〕） ／ 1.0.0（2026-09-06・起票） |
| 作成日 | 2026-09-06 |
| 作成者 | 設計担当 Claude（設計卓／フェーズ4 期） |
| 対象マイルストーン | **`M32`**（フェーズ4 の最後）。上位＝`docs/instructions/phase4-overview.md` §3 |
| 位置づけ | **`M32` のサブ構成・射程・CHANGE 見込みの正本** |
| 状態 | **★★★2026-09-17 更新＝`D-889`。`M32` は 3 サブとも完了である**〔`M32-01` 着地 ／ `M32-02` 着地 ／ **`M32-03` 受理・マージ待ち**〕**。★残る開発者の手番は*撮影*だけである。** **★★撮影は 27 枚 ＋ クイックスタートの図 1 枚**〔接頭辞 `qs-`〕**。⇒ 撮り終えたら `figure` の `todo` クラスを外すこと。** 以下は前版の記述： **★★★2026-09-14 更新＝`D-879`。`M32-01` は着地、`M32-02` は*走行済みで開発者の撮影の手番で止まっている***〔**未マージ。⇒ `main` に完了報告は無い**〕**。★現在地と影響章の表は §3.2 の末尾にある。** **★★旧記述の「起票済み」は 2026-09-06 の値であり、8 日間失効していた**（計測点 `M-182`）**。** 以下は前版の記述： **★★起票済み**（2026-09-06・`D-763`）**。⇒ `M32-01` / `M32-02` の指示書が発行された。★投入はフェーズ4 の最後である**（§0 の 4 点目）**。★レビューチェックリストは着手承認の手番で起こす**（`D-736`） |
| identity 数 | **★★13 を実測した**（§2）。**`phase4-overview` の「13」と一致する。⇒ 数は合っている** |
| **★★実質の決めごと** | **★★5 つ**（§2.2。**13 のうち 6 件は名寄せ、2 件は既に解消している**） |
| CHANGE 見込み | **★★0 本へ確定**（2026-09-06・`D-763`）**。⇒ 方式 (b) を採ったため `DES-002` §11.2 のアーカイブ構成は変わらない**〔`README.txt` は同じ位置に同じ役割で残り、変わるのは「誰が書くか」だけである〕**。★構成が変わると分かったら止めて請求する**（`D-293`）**。** 以下は前回更新時の記述： **0〜1 本**（**`DES-002` §11.2 のリリースアーカイブ構成**＝`README.txt` の生成方式が変わる場合のみ） |
| マイグレ見込み | **0 本** |
| 配置 | `docs/instructions/M32-overview.md`（active＝直下。フェーズ4 終了時に `docs/instructions/phase4/` へアーカイブ） |

---

## 0. 本書の確度

- **★★identity の数は合っている。** `phase4-overview` §3 の「13」は ledger の `根拠` 欄が `B1`〜`B6` の 12 行 ＋ `FN-02` の 1 行で再現できた。**⇒ `M29` の「29」**（`M-127`＝内訳が引けない）**とは違う。★本 MS は行を特定できている。**
- **★★ただし 13 は「作業の数」ではない。** **6 件が名寄せ**（`SM-152`↔`B1` ／ `SM-153`↔`B2` ／ `SM-023`↔`B6` ／ `readme-md-and-txt-coexist` と `usermanual-not-written` は `B4` の族）**、2 件は既に解消している**（§2.3）**。⇒ 実質は 5 つである**（§2.2）。
- **★★本 MS の最大の特徴は「製造サブに割れる部分が少ない」ことである**（§3）。**⇒ 5 つのうち 3 つは開発者の手番であり、Claude が代われない。**
- **★★`M32` は `M27`〜`M31` の後に置く**（`phase4-overview` §M32。逐語＝「**説明書を作る過程で UI の粗が出たら記録すること**」）**。⇒ 起票はできるが、投入はフェーズ4 の最後である。**

---

## 1. 目的

**公開したときの摩擦を下げる。⇒ 「公開できるか」ではなく「公開して届くか」の話である。**

**★`A` ゲート**（法務・セキュリティ＝`M26`）**とは性質が違う。⇒ `A` は済むまで公開してはいけないもの、`B` は済まなくても公開できるが第一印象で損をするものである**（`checklist` §0）。

---

### 1.1 ★★`B1`〜`B6` とは何か（**逐語。本書を読むのにこれ以上の参照を要らなくする**）

**出所＝`docs/human-notes/combmgr-prerelease-checklist.md` §2「B：公開の摩擦低減」。⇒ 6 項目である。**

| # | 項目 | 逐語の内容 | 誰の作業か |
|---|---|---|---|
| **`B1`** | **コード署名の取得** | 「未署名バイナリは Windows SmartScreen で警告が出る。**インストール不要・exe 起動だけで使える**という DNA の入口を守るために要る」 | **開発者**（証明書の購入と申請） |
| **`B2`** | **ウイルス誤検知対策** | 「Avast 等への申請。**Go の単一バイナリは誤検知されやすい**。署名（`B1`）が先にあると通りやすい」 | **開発者**（各ベンダーへの申請） |
| **`B3`** | **データ鮮度の確認** | 「2026-08-03 のアップデート（ヤスミン追加＝30→31）の反映状況。**公開時のデータが古いと第一印象で終わる**」（**★★2026-09-12 是正＝`D-847`・`M32-01` の実査。旧記述は `2026-08-04`**。**⇒ `D-764` が `checklist` §2 側だけを直し、本欄が取り残されていた。★§5-3 が自ら「実査項目」に挙げていた食い違いである**） | **開発者の実測** |
| **`B4`** | **操作説明書・動画** | 「起票の素通しが売りなので、**説明が要る箇所＝設計が負けている箇所**でもある。説明書を作る過程で UI の粗が出たら記録すること」 | **★製造サブになる**（説明書のみ。**動画は §4-3**） |
| **`B5`** | **宣伝ブログ記事** | 「発信者ピボットの北極星（主要発信者が CSV を出し続けているか）に直結する導線。**誰に読ませるかを先に決める**」 | **開発者の執筆** |
| **`B6`** | **モダン操作の扱いを明示する** | 「**実装ではなく明示。B の中で最も安い割に効く**」 | **★製造サブになる**（`B4` と同じ文書面） |

**★★6 つのうち Claude が代われるのは `B4`（説明書の部分）と `B6` だけである。⇒ これが §2.2 の「実質 5 つ、製造サブは 1 つ」の中身である。**

---

## 2. 射程

### 2.1 identity 13 件（**ledger 実測**）

| identity | 内容 | 根拠 | 名寄せ |
|---|---|---|---|
| `B1` | **コード署名の取得** | `checklist` | `SM-152` が同一物 |
| `SM-152` | コード署名 | `M24 report §2.2` | **★`B1` が代表** |
| `B2` | **ウイルス誤検知対策** | `checklist` | `SM-153` が同一物 |
| `SM-153` | AV 誤検知対策 | `M24 report §2.2` | **★`B2` が代表** |
| `B3` | **データ鮮度の確認** | `checklist` | — |
| `CL-4-3` | `FR702` が要求するスキーマ変更 | `checklist` | **★★解消済み**（§2.3） |
| `B4` | **操作説明書・動画** | `checklist` | `readme-md-and-txt-coexist` / `usermanual-not-written` が族 |
| `readme-md-and-txt-coexist` | `README.md` と `README.txt` の二重 | `followup` | **★`B4` の族** |
| `usermanual-not-written` | 利用者向けマニュアルが未整備 | `followup` | **★`B4` の族。★★`B4` そのものである** |
| `B5` | **宣伝ブログ記事** | `checklist` | — |
| `B6` | **クラシック前提の明示** | `checklist` | `SM-023` が同一物 |
| `SM-023` | モダン対応（取り扱い方の決定） | `M24 report §2.2` | **★`B6` が代表** |
| `FN-02` | 対外向け機能一覧 | `future-notes/combmgr-features.txt` | **★★解消済み**（§2.3） |

### 2.2 ★★実質は 5 つである

| # | 決めごと | 誰が |
|---|---|---|
| **★★1** | **コード署名の取得**（`B1`） | **★開発者**（証明書の購入と申請。**Claude が代われない**） |
| **★★2** | **AV 誤検知対策**（`B2`） | **★開発者**（各ベンダーへの申請。**`B1` が先行条件**） |
| **3** | **データ鮮度の確認**（`B3`） | **★開発者の実測**（§2.4） |
| **★★4** | **操作説明書 ＋ `README` の整理 ＋ クラシック前提の明示**（`B4` ＋ `B6`） | **★★製造サブになる**（§3） |
| **5** | **宣伝ブログ記事**（`B5`） | **★開発者の執筆**（**★Claude は素材を出せるが、書くのは開発者である**） |

**⇒ ★★製造サブに割れるのは 4 だけである。★1・2・3・5 は overview に手番として書き、サブにしない。**

### 2.3 ★★既に解消している 2 件

| identity | なぜ解消したか |
|---|---|
| **`CL-4-3`**（`FR702` が要求するスキーマ変更） | **★★`M28-02a` が済ませた**（`CHANGE-159`。新列 5 本 ＋ マイグレ `000104` / `000105`）**。⇒ `M32` の射程ではない。★本行が `B3`（データ鮮度）を根拠にしていたのは、「鮮度を表せるスキーマ」が要ったからである。⇒ 表せるようになった** |
| **`FN-02`**（対外向け機能一覧） | **★★公開物から外すことに決した**（`D-656`）**。⇒ 開発者が 2026-09-06 に `docs/human-notes/future-notes/combmgr-features.txt` を削除した**（`M26-02` レポート §4-5）**。★★ただし帰結が 1 つ残る**——**`B6`（クラシック前提の明示）は本ファイルでは満たせなくなった。⇒ `README` と操作説明書で満たす**（`phase4-overview` §M32） |

### 2.4 ★`B3`（データ鮮度）の現在地

| # | 事実 |
|---|---|
| 1 | **`M14` 第四波は完了した**（`D-707`）**。⇒ 全 31 キャラが `characters` に居る。★`M25-RESEARCH-01` の実測「DB seed 19/31」は失効している** |
| 2 | **`games.current_data_version` が入った**（`CHANGE-159`）**。現在値＝`2026.08.03.01`**（2026-09-06 開発者確定） |
| ~~**★★3**~~ | **★★【2026-09-06 決着＝`D-764`】食い違いは解消した。⇒ `2026-08-03` が正しく、`checklist` の `2026-08-04` が誤記であった**（開発者の逐語＝「2026.08.03 が正しい。08-04 はミス」）**。★`checklist` §2 の `B3` 行を是正済み。** **⇒ `games.current_data_version` の `2026.08.03.01` は、`B3` が指すアップデートそのものである。** |
| **★★4** | **★★したがって `B3` は事実上満たされている**（2026-09-06）**。⇒ 全 31 キャラが `characters` に居り**（`M14` 第四波完了＝`D-707`）**、版の記録も同じアップデートを指している。★残るのは開発者が現物で 1 度確かめることだけである。** |

**⇒ `B3` は「作業」ではなく「確認」である。★ただし 3 が食い違ったままだと、公開時のデータが古いかどうかを誰も判定できない。**

---

## 3. サブ構成（**★★2 本を提案。直列**）

### 3.1 `M32-01` — 操作説明書の器と `README` の整理（`B4` の構造 ＋ `B6`）

> **★★起票済み＝`docs/instructions/M32-01-usermanual-shell-and-readme.md` v1.0.0**（2026-09-06・`D-763`）**。⇒ 射程の正本は指示書側である。本節は編成の記録として残す。**

| # | 射程 |
|---|---|
| **★★1** | ~~**`docs/usermanual/` を新設し**~~**、そこを正本にする**（`D-743` の裁定）**。★★【2026-09-06 是正＝`D-763`】`docs/usermanual/` は既に存在する**〔設計卓の実査。`combmgr-friend-readme.html` が 1 本入っている〕**。⇒ 新設は済んでおり、残るのは「正本にする」だけである。★指示書 `M32-01` §0.2 が本行を上書きする** |
| **★★2** | **`README.txt` はそこから生成する。★★`docs/usermanual/` へ移してはならない**——**3 つの独立した根拠が配布物の必須成果物と定めている**〔`nightly-crossbuild.yml` ／ `DES-002` のリリースアーカイブ構成 ／ `cmd/tacpendium/main.go`〕**。⇒ 移すとビルドが壊れる** |
| **★★3** | **`combmgr-friend-readme.html` を改名して器として再利用する**（開発者の逐語＝「**M07 頃のもので削除予定だが、正式マニュアルに再利用できるなら改名して残す**」）**。★中身は書き直す前提である** |
| **★4** | **`readme-md-and-txt-coexist` の解消**——**`README.md` と `README.txt` のどちらが正本かを外から判別できるようにする。★「消す」は開発者の手番**（`D-196`） |
| **★★5** | **`B6`（クラシック前提の明示）**——**`README` と操作説明書に書く。★実装ではなく明示である**（`checklist` §3.2。**「B の中で最も安い割に効く」**） |
| **★6** | **章立てと目次を決める**（本文は `M32-02`） |

**★本サブを先に置く理由＝`README.txt` の生成がビルドに関わるからである。⇒ 壊すと nightly が落ちる。★本文は壊れない。**

### 3.2 `M32-02` — 操作説明書の本文（`B4` の中身）

> **★★起票済み＝`docs/instructions/M32-02-usermanual-body.md` v1.0.0**（2026-09-06・`D-763`）**。⇒ 射程の正本は指示書側である。本節は編成の記録として残す。**

| # | 射程 |
|---|---|
| **★★1** | **画面ごとに書く**（`DES-005` の §5.x に対応させる） |
| **★★2** | **★★UI の粗を記録する。⇒ これが本サブの第 2 の成果物である。** `checklist` `B4` の逐語＝「**起票の素通しが売りなので、説明が要る箇所＝設計が負けている箇所でもある。説明書を作る過程で UI の粗が出たら記録すること**」 |
| **★3** | **★記録した粗は直さない。⇒ `followup` への登録候補として報告する**（`D-382`。**製造は本表を編集しない**） |
| **★4** | **`M29-01` で統一した語を使う**（**★`M32` は `M29` の後である。⇒ 統一前の語で書くと二度手間になる**） |

**★2 本を直列にする理由＝器が決まらないと本文の置き場が決まらない。⇒ 並列にすると本文の移動が発生する。**

---

### 3.2b ★★`M32-03` — 操作説明書を `M37` の着地へ追随させる（**2026-09-14 新設＝`D-882`・開発者承認**）

> **★★起票済み＝`docs/instructions/M32-03-usermanual-revision-after-m37.md` v1.0.0 ＋ チェックリスト v1.0.0。⇒ 射程の正本は指示書側である。**

**★★★新設した理由＝`M32-02` の本文は `M37` の大半より前に書かれており、撮り直しだけでなく*改稿*が要る章が 8 あるためである**〔§3.2 末尾の表〕**。**

**★★`M32-02` を開いたまま改稿まで抱えさせない。⇒ 撮影は `M32-02` の完了条件に入っておらず、待たずに閉じるのが正規の完了形である**（`D-880`）**。★閉じることで `M33-02` のゲート「`M32` の全着地」が一度開く。**

| # | 射程 |
|---|---|
| **★★1** | **8 章へ差分を当てる**（**★ゼロから書き直さない**） |
| **★★2** | **撮影の置き場を点検する**（**★撮らない。⇒ 撮影は開発者の手番**） |
| **★3** | **走査 4 本**〔暫定表記 ／ 表示語 ／ `M29-01` の統一語 ／ 波括弧〕 |

**★★★`M32-03` は `M32` を開き直す。⇒ `M33-02` は本サブの着地まで待つ。★これは番号の付け方の帰結ではない**——**面が `docs/usermanual/` である以上どの番号を付けても待つ**〔`M33-02` は `M37` の全着地も要求しており、`M37` へ置いても同じ〕**。**

**★前提サブは 2 本＝`M32-02` の着地 ＋ `M37-07` の着地。★CHANGE 0 本の見込み・マイグレ 0 本。**


---

#### ★★★【2026-09-14 現在地＝`D-879`】`M32-02` は走行済みであり、**開発者の撮影の手番で止まっている**

**★開発者申告**（逐語）**＝「`M32-02` は投入済みです。ただし後から出てきた `M37` サブによる画面変更等の影響で、説明書の大枠は出来たが、スクリーンショットを私が取るところで停止中。というステータスです。なので main に completion-report もありません。マージ前です」。**

**★★★本節の「起票済み」も `phase4-overview` §3.1 の「開発者の手番」も、どちらもこの状態を言い表していなかった**（計測点 `M-182`）**。⇒ 区分に「製造は走ったが開発者の手番で止まっており、未マージ」の枠が無い。**

**★★★本文は `M37` の大半より前に書かれている。⇒ 撮り直しだけでなく*改稿*が要る章がある。**

##### 影響した章（**設計卓の実査。`CHANGE-195`〜`205` の反映先から引いた**）

| `DES-005` | 章 | 影響した `M37` サブ | 何が変わったか | 撮影 |
|---|---|---|---|---|
| **§5.7** | **コンボ登録・編集** | **M37-01 / M37-02 / M37-05 / M37-06 ＋ M37-07** | **★★最も重い**。運び量・始動位置の 3 値入力〔`CHANGE-196`〕／ 仮想コントローラの縦化〔`CHANGE-199`〕／ 保存前の補完と `onBlur`〔`CHANGE-200`〕／ modifier が 14 値へ〔`CHANGE-201`〕／ 状況の欄へ「持続当て（始動）」が 1 つ増える〔`M37-07`〕 | **★M37-07 待ち** |
| **§5.6** | **コンボ詳細** | **M37-01 / M37-06 ＋ M37-07** | 始動位置・運び量の表示〔`CHANGE-196`〕／ 修飾がバッジで出る〔`CHANGE-203`〕／ 持続当て（始動）を必ず出す〔`M37-07`〕 | **★M37-07 待ち** |
| **§5.4** | **コンボ一覧** | **M37-06 ＋ M37-07** | 修飾がバッジで出る〔`CHANGE-203`〕／ **★★2026-09-14 決着＝`M37-07` のフィルタを*足す*。⇒ 実装判断ではなくなった** | **★M37-07 待ち** |
| **§5.5** | **マイコンボ** | **M37-06** | 修飾がバッジで出る〔`CHANGE-203`〕 | **★今すぐ可** |
| **§5.8** | **コンボ比較** | **M37-06** | 同上 | **★今すぐ可** |
| **§5.9** | **セットプレイ登録・編集** | **M37-06** | 同上 | **★今すぐ可** |
| **§5.15** | **ゴミ箱** | **M37-03** | 並び順が削除日時の降順へ ＋ `sort=deleted_at`〔`CHANGE-197`〕 | **★今すぐ可** |
| **§5.20** | **確定反撃サーチ** | **M37-04** | **★★★見た目はほぼ同じだが*中身*が変わった**——候補から 17 行が恒久的に落ち、88 行が緩い有利フレームで出る。**「発生」の意味が「単独で出したときの発生」から「判定に使った発生」へ**〔`CHANGE-205`〕**。⇒ 技一覧の「発生」と食い違って見える。★数字や「発生」の説明を本文に書いているなら、そこは現に誤りである** | **★今すぐ可**〔**`M37-04` のマージ後**〕 |

**★★★改稿が要るのは 8 章。⇒ 残る 14 章は `M37` の影響を受けていない。**

**★★撮影の観点では 19 章が `M37-07` と無関係である**（`D-878`）**。⇒ そのうち 5 章**〔§5.5 / §5.8 / §5.9 / §5.15 / §5.20〕**は本文の改稿も要るが、画面はもう最終形である。** **★★★2026-09-14 更新＝19 は 20 にならなかった。⇒ 開発者が「`M37-07` の一覧フィルタには足したい」と決めたため、§5.4 は `M37-07` 待ちで確定した**（`D-880`）**。**

**★★★撮影は「マージ済みのビルド」で行うこと。⇒ 特に §5.20 は `M37-04` を、バッジ系 5 章は `M37-06` をマージしていないと古い画面が写る。**


### 3.3 ★★サブにしないもの（**overview に手番として書く**）

| # | 内容 | 誰が | 順序 |
|---|---|---|---|
| **★★1** | **コード署名の取得**（`B1`） | 開発者 | **★`B2` より前**（`checklist` §6。**署名があると AV 申請が通りやすい**） |
| **★★2** | **AV 誤検知対策**（`B2`） | 開発者 | `B1` の後 |
| **3** | **データ鮮度の確認**（`B3`） | 開発者の実測 | **★§2.4-3 の食い違いを先に解くこと** |
| **4** | **宣伝ブログ記事**（`B5`） | 開発者の執筆 | **★`B4` の後**（`checklist` §6。**説明書が無いと宣伝の着地先が無い**） |

---

## 4. ★開発者への確認事項

| # | 確認事項 | 設計卓の暫定案 |
|---|---|---|
| ~~**★★1**~~ | **サブ分割 2 本の承認**（§3） | **★★決着＝承認**（2026-09-06・`D-757`）**。⇒ 2 サブ・直列で確定。★`M32-01`＝器 ＋ `README` の整理 ＋ `B6` ／ `M32-02`＝本文 ＋ UI の粗の記録** |
| ~~**★★2**~~ | **`README.txt` の生成方式** | **★★決着＝(b)**（2026-09-06 開発者承認＝`D-755`）**。⇒ スクリプトで生成し、生成物をコミットする。** **★これにより `README.txt` は `docs/usermanual/` の正本から機械的に作られ、ドリフトを検査できる形になる。★スクリプトが 1 本増える。** **★採らなかった案＝(a) 手で写す**〔**必ずずれる**〕**／ (c) ビルド時に生成**〔**ドリフトは構造的に起きないが、`nightly-crossbuild.yml` と `Makefile` に手が入り `DES-002` §11.2 への CHANGE が要る。⇒ コストが釣り合わない**〕**。** |
| ~~**★★3**~~ | **動画を射程に入れるか** | **★★【2026-09-06 決着＝`D-755`】動画は射程外である。⇒ `M32-01` §3-6 / `M32-02` にも「やらないこと」として入っている。★2026-09-12 に打ち消し線を追随させた（`D-848`）。** 以下は起票時の記述： **★★前提＝`checklist` の `B4` は項目名が「操作説明書・動画」であり、2 つを求めている。⇒ 説明書のほかに操作動画を作れ、という意味である。** **★★設計卓の案＝入れない。⇒ Claude は動画を作れない。★入れると、サブが「動画は未着手」を抱えたまま閉じられなくなる。** **★入れない場合の扱い＝開発者の手番として `followup` へ 1 行置く。⇒ 忘れるのではなく、行き先を変えるだけである。** |
| ~~**★4**~~ | **`combmgr-friend-readme.html` の改名先** | **★★決着＝製造に案を出させる**（2026-09-06・`D-757`）**。⇒ `docs/usermanual/` 配下のどの名前にするかは章立て**（§3.1-6）**と一緒に決まるため、`M32-01` の成果物として出させる。★開発者が選ぶ** |
| ~~**★★5**~~ | **`B1` / `B2` / `B3` / `B5` を `M32` の完了条件に含めるか** | **★★【2026-09-06 決着＝`D-757`】含めない。⇒ `M32` は `M32-01` ＋ `M32-02` で完了とする。★`B1` / `B2` / `B3` / `B5` は開発者の手番として §3.3 に残る。★2026-09-12 に打ち消し線を追随させた（`D-848`）。** 以下は起票時の記述： **★★何を聞いているか＝「`M32` は完了した」と宣言するために、これら 4 件の完了も要求するかどうかである。** **★★含めると何が起きるか＝`M32` は開発者の作業**〔証明書の購入 ／ AV ベンダーへの申請 ／ ブログの執筆〕**が終わるまで閉じられない。⇒ フェーズ4 そのものが、Claude 側の作業が全部終わったあとも「未完了」のまま残る。** **★★設計卓の案＝含めない。⇒ `M32` の完了は `M32-01` ＋ `M32-02` の完了とする。★`B1` / `B2` / `B3` / `B5` は公開ゲートの手番として §3.3 と `followup` に残す。** **★これは「やらない」ではなく「`M32` の完了判定に混ぜない」である。⇒ 公開の前には全部要る。** |

---

## 5. ★設計卓が確かめていないこと（実査項目）

| # | 確かめること |
|---|---|
| **★★1** | **`combmgr-friend-readme.html` の現物**（**★設計卓は読めるが、中身の再利用可否は開発者の判断である**） |
| **★★2** | **`README.txt` の現在の中身と、それを必須成果物と定めている 3 か所の実物**（**★`.github/` と `cmd/` は武装中の作業ツリーに実体が無い**＝`M-133` の教訓。**⇒ 起票時に製造へ実査させる**） |
| **★★3** | **§2.4-3 の日付の食い違い**（`2026-08-04` vs `2026.08.03.01`） |
| ~~**★4**~~ | **★★【2026-09-06 実測＝`D-763`】`DES-005` §5 の画面の節は 24 本あり、うち 2 本は死んでいる**〔§5.13 エクスポート＝`CHANGE-144` で廃止 ／ §5.17 moves 取込プレビュー＝`CHANGE-055` で削除〕**。⇒ 生きている画面は 22 である。★対応表は指示書 `M32-02` §2.1 が持つ。★着手時に数え直すこと** |
| **★5** | **`M29-01` の統一結果**（**★`M32-02` はその語を使う。⇒ `M29-01` が終わっていない時点では書けない**） |

---

## 6. 前提と順序

```
M27 / M29 / M30 / M31（α② の是正）
     │
     └─→ M32-01（操作説明書の器 ＋ README の整理 ＋ B6）
              │
              └─→ M32-02（本文 ＋ UI の粗の記録）

M14 第四波 …（完了済み＝D-707）★B3 の前提はこれで満たされた

【開発者の手番・M32 と並行してよい】
  B1（コード署名）→ B2（AV 誤検知）
  B3（データ鮮度の確認）★§2.4-3 を先に解く
  B5（宣伝ブログ）★B4 の後
```

**★★`M32` はフェーズ4 の最後である**（`phase4-overview` §M32）**。⇒ `M29-01` の語の統一が終わっていないと `M32-02` は書けない**（§5-5）**。**

> **★★公開ゲート `A` 側に、`M32` より先に閉じるべきものが 1 件ある**——**`rights-holder-contact-channel-missing`**（`D-752`）**。⇒ `NOTICE` §3 の撤退ポリシーが実質機能しない状態であり、これは `B`（摩擦低減）ではなく `A`（公開ゲート）である。★`M32` の完了条件には入れないが、公開の順序としては先である。**

---

## 7. CHANGE 見込み・採番

| サブ | CHANGE 見込み | マイグレ | 根拠 |
|---|---|---|---|
| **`M32-01`** | **0〜1 本** | **0 本** | **★§4-2 で (c) ビルド時生成を採る場合のみ `DES-002` §11.2**（リリースアーカイブ構成）**に及ぶ。⇒ (a) / (b) なら 0 本** |
| **`M32-02`** | **0 本の見込み** | **0 本** | **★文書の執筆であり設計書の契約に触れない。★ただし「UI の粗」から設計変更が要ると分かったら請求する**（`D-293`） |

**★次に採番するのは `161`**（**★追加ブロック `162`〜`171` を払い出し済み＝`D-748`**）。**★先付けしない**（playbook §4.15）。

---

## 8. 参照ドキュメント

| ID | パス | 参照する箇所 |
|----|------|------------|
| — | `docs/instructions/phase4-overview.md` | §3 の M32 行 ／ §M32 |
| — | `docs/human-notes/combmgr-prerelease-checklist.md` | §2（`B1`〜`B6`）／ §3.2（`B6` の説明）／ §6（順序） |
| — | `docs/progress/M25-RESEARCH-01-report.md` §2.2 | **ledger**（identity の正本） |
| DES-002 | `docs/design/02-architecture.md` | §11.2（**リリースアーカイブ構成。★`README.txt` を必須成果物と定めている**） |
| DES-005 | `docs/design/05-screen-design.md` | §5.x（**`M32-02` が対応させる画面**） |
| — | `docs/handover/followup-backlog.md` | `readme-md-and-txt-coexist` ／ `usermanual-not-written` ／ `rights-holder-contact-channel-missing` |
| — | `docs/process/parallel-board.md` | `D-743`（`README.txt` の射程）／ `D-656`（`FN-02`）／ `D-752`（申し送り） |

---

*以上、M32-overview **v1.8.0**。* **★★本 MS の落とし穴は「13 件ある」と読むことである。** **★実質の決めごとは 5 つで、そのうち製造サブに割れるのは 1 つだけである**（§2.2）**——`B1` / `B2` / `B5` は開発者が代われない作業であり、`B3` は確認である。⇒ サブ数を identity 数から見積もると、必ず多すぎる。** **★★もう 1 つは `README.txt` を `docs/usermanual/` へ移してしまうことである**（§3.1-2）**——3 つの独立した根拠が配布物の必須成果物と定めており、移すとビルドが壊れる。⇒ 「正本の場所」と「配布物としての姿」は別である。**
