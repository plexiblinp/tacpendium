# M17-05d レビューチェックリスト

| 項目 | 内容 |
|------|------|
| 対象指示書 | `docs/instructions/phase3/M17-05d-intake-helper-ops.md` v1.0.0（取込ヘルパーの運用テコ入れ＝§G-14a） |
| 対象指示書ID | M17-05d |
| レビューモデル | Sonnet 4.6（実使用は開発者判断） |
| バージョン | 1.0.0 |
| 作成者・作成日 | 設計担当 Claude（M17 期・セッション3）/ 2026-07-18 |

> **文書ID ⇄ 実パスの正は `docs/handover/docs-map.md`（自動生成）**。

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| 1.0.0 | 2026-07-18 | 初版。 |

---

## 0. レビュー担当への前置き

### 0.1 準備

- 必読: 指示書 v1.0.0 全節・**DES-005 §5.19**（取込ヘルパー・**固定 ja**）・**CHANGE-071**（二段の役割分担）・M17-04 伝達資料・**followup §G-14／§G-14a**・code-facts（`IntakeHelperPage`・`web/src/features/intake/`・`prompt.ts`・**config 構造体**・localStorage 前例 `onboarding-seen-v1`）。
- **Plan Mode 着手前確認結果の確認（必須）**: 指示書 §3.5 の **5 項目すべて**に質問書＋回答が残っているか。**とくに 2（保存先の実査）・4（② 照合への非波及）**。

### 0.2 基本姿勢

- 本サブは **① プロンプト側だけ**を変える。**② 厳密照合（`internal/moveindex`・`csvcore`・`inputresolve`・intake の照合部）に手が入っていたら重大**（§G-14b＝M20 の領域）。
- **「本体から LLM を呼ばない」を破っていないか**（(a) は「ユーザーが自分で複数 AI に投げる」明示＝アプリが AI を呼ぶのではない）。
- **保存先で config（TOML）を触ったら承認ゲート**＝報告なしに config を広げていたら重大。

### 0.3 報告フォーマット

- 各節「OK / 重大（§9）/ 軽微（§10）/ 質問（§11）」。

---

## 1. (a) 複数の AI サービスの明示

- [ ] 「プロンプトをコピー」近傍に**複数 AI を試す旨の注記**があるか。
- [ ] **特定モデルを単一指名していない**か（「GPT 推奨」等でなく「複数を試す」＝Gemini でも動く前提）。

## 2. (b) 入力整形のお願い 3 件

- [ ] **回数展開**（`×2〜3` → `> > >`）・**切れ目の明示**（1 コンボ 1 行/番号）・**分岐記法の行単位化**（具体例つき）の**3 件**が、**プロンプト本文（`prompt.ts`）とユーザー画面の両方**（または開発者確定の配置）にあるか。
- [ ] **分岐の具体例**（ストック/PC/ライズ/フレア/SA1/SA3/ODライズ の before/after）が含まれているか。

## 3. (c) ユーザー独自ルールの自由入力欄

- [ ] **自由入力欄（textarea）**があり、**「プロンプトをコピー」時に本文へ結合**されるか（**空なら結合しない**）。
- [ ] **保存される**か（リロードで復元）。**保存先が Plan Mode 確定どおり**か。
- [ ] **保存先が localStorage** か（推奨）。**config（TOML）を触った場合、承認を得た記録があるか**（**無断で config を広げていたら重大**）。
- [ ] 「開発者プロンプトをそのまま使わなくてよい」旨の注記があるか。

## 4. 【最重要】② 照合への非波及

- [ ] **`internal/moveindex`・`csvcore`・`inputresolve`・intake の照合部が差分ゼロ**か（**`git diff` の結果が完了報告にあるか**）。
- [ ] **`resolve`／`BuildCSV` の既存テストが不変で通る**か（② の実測解決率が変わらない）。
- [ ] 触ったのが **`prompt.ts`＋`IntakeHelperPage`＋（保存先）** に限定されているか。

## 5. 非改変・境界

- [ ] **BE・DTO・スキーマ・CSV 契約が不変**か。
- [ ] **M17-05a が触る `ComboImportPage`（§5.14）に手が入っていない**か（05d は §5.19 のみ）。
- [ ] **§5.19 は固定 ja**（i18n キーを足していない）か。

## 6. テスト・ドキュメント

- [ ] §4 のケース（プロンプト結合・保存往復・② 非波及・i18n）が揃っているか。`make e2e` 非回帰。
- [ ] 完了報告に **§3.5 の 5 項目**・**保存先の最終実装**・**② 非波及の `git diff`**・**DES 反映要点**（§5.19）があるか。**製造が DES を直接編集していない**か。
- [ ] 禁則表現・簡体字・「起き攻け」誤字・「DR」略記の grep 除去。

---

## 9. 重大な問題（完了承認を妨げる）

- **② 厳密照合（`internal/moveindex`・`csvcore`・`inputresolve`・intake 照合部）に手が入った**（§G-14b＝M20 の領域）。**`resolve`／`BuildCSV` の解決率が変わった**。
- **本体から LLM を呼ぶ実装を入れた**（(a) はユーザーが自分で複数 AI に投げる明示）。
- **config（TOML）を無断で広げた**（保存先＝承認ゲートを経ていない）。
- **M17-05a の領域（`ComboImportPage`・§5.14）に手を入れた**。
- **§5.19 に i18n キーを足した**（固定 ja の境界を破った）。
- Plan Mode 5 項目のいずれかが未確認（推測実装）。スキーマ/CSV 契約を変更した。`make e2e` が回帰した。

## 10. 軽微（持ち越し許容）

- (a)(b)(c) の文言（**仮でよい・確定は開発者**）。
- 自由入力欄・注記の配置（スマホ幅で成立すれば可）。
- (b) をプロンプト側/画面側のどちらに厚く出すか（**判断根拠が報告にあれば可**）。

## 11. 質問フォーマット

- 「指示書 §X.X / DES-005（実パス）§5.19 に対し実装が Z。意図確認したい」。

## 12. レビュー完了の判定

- §1〜§6 OK・§9 重大ゼロ・Plan Mode 5 項目（**とくに 2・4**）確認済み。§10 軽微は持ち越し可。

---

*以上、M17-05d レビューチェックリスト v1.0.0。配置 `docs/instructions/phase3/reviews/M17-05d-review-checklist.md`。重点＝**② 照合は差分ゼロ**・**複数 AI 明示（単一指名しない）**・**自由ルール欄の保存先（localStorage 推奨・config は承認ゲート）**・**§5.19 は固定 ja**・**05a の領域に触れない**。*
