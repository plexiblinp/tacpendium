# M15-06 レビューチェックリスト

| 項目 | 内容 |
|------|------|
| 対象指示書 | `docs/instructions/phase3/M15-06-onboarding-advanced-mode.md` v1.2.0（初回オンボーディング FB⑯） |
| 対象指示書ID | M15-06 |
| レビューモデル | Sonnet 4.6（model-allocation v1.27.0 参照。実使用は開発者判断） |
| バージョン | 1.1.0 |
| 作成者・作成日 | 設計担当 Claude（フェーズ3 継続担当・M15 期）/ 2026-07-04 |

> 文書ID ⇄ 実パスの正は `docs/handover/docs-map.md`（自動生成）。

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| 1.0.0 | 2026-07-04 | 初版（指示書 v1.0.0＝⑯＋⑰ と対）。 |
| 1.1.0 | 2026-07-04 | 指示書 v1.2.0 に追従＝**⑯オンボーディングのみ**へ再スコープ（⑰上級者モードは M15-05 折り畳みで充足＝対象外）。⑰関連の観点を削除。 |

---

## 0. レビュー担当への前置き

### 0.1 レビュー前の準備
- 必読: 指示書 v1.2.0、CLAUDE.md §10.X（UI 状態 localStorage 許容）、DES-005 §2/§4.1・§5.4/§6.4.1・§5.7 CHANGE-039（新規登録導線）、code-facts §1/§route/§nav、retrospective-digest §3/§4。
- **Plan Mode 実査結果の確認**: 指示書 §3.3 #1〜#5 が実査で確定してから実装されているか。特に **#1 既存の空状態/ガイドを実査したか（自作再発明防止・playbook §4.6）／初回判定が UI 状態（localStorage・browser-storage.ts 経由）か**。

### 0.2 レビューの基本姿勢
- **⑯は表示制御＋UI 状態のみ**（DB/API/ConfigResponse を触らない）。「初回か」は localStorage（`browser-storage.ts` 経由）。
- **押し付けない**（スキップ/再表示可）。既存遷移・M15-05① レイアウトを壊さない。
- **⑰上級者モードを作っていない**（M15-05 折り畳みで充足・スコープ外）。

### 0.3 報告フォーマット
- 各節「OK / 重大（§9）/ 軽微（§10）/ 質問（§11）」。

---

## 1. 設計・パターンとの照合（最重要）

### 1.1 初回オンボーディング（指示書 §4.1・§3.3）
- [ ] 初回に**新規登録の位置・主要メニュー**が分かる導線か。**スキップ/再表示可・押し付けでない**か。
- [ ] 「初回か」の判定が **UI 状態（localStorage・`browser-storage.ts` 経由）**か（DB 非対象・直接 localStorage を呼んでいない・CLAUDE.md §10.X）。
- [ ] 既存の空状態/ガイドを実査し流用したか（自作再発明していない・playbook §4.6/§4.9）。

### 1.2 メニュー可読性（指示書 §4.2）
- [ ] Header/Footer の可読性が向上し、**既存遷移が不変**か（過度な改変をしていない）。

### 1.3 スコープ・test-id・DES 直接編集禁止（指示書 §1.3・§4.3・§7.4）
- [ ] **⑰上級者モードを作っていない**（M15-05 折り畳みで充足）。
- [ ] 既存 test-id 規約を grep 実値確定してから採用（digest §4）。範囲限定。
- [ ] DES-005 CHANGE 要否の判定が完了報告にあり、**製造が DES を直接編集していない**か（要なら設計担当起票）。

---

## 2. データ・API 契約・スキーマの不変（非破壊性・非スキーマ）
- [ ] DB スキーマ・API・`ConfigResponse` に差分なし。⑯は表示制御＋UI 状態のみ。

---

## 3. フロントエンドの動作仕様（指示書 §4）
- [ ] 初回ガイドが表示・スキップ/再表示可。メニュー可読性向上。既存レイアウト（M15-05①）が崩れない。
- [ ] 既存の空状態/ガイド・`browser-storage.ts` を流用（自作再発明なし）。

---

## 4. テストの妥当性（ケース数で確認。指示書 §5 ↔ §7 DoD）
- [ ] **初回（Vitest）**: 初回でガイド表示・スキップ/再表示・2 回目以降の挙動・localStorage 判定。
- [ ] **可読性（Vitest）**: 既存遷移不変で可読性向上（非回帰）。
- [ ] **既存 E2E 非回帰**: `make e2e` 全通過・永続 dev DB 残渣非依存。

---

## 5. 設計意図との整合（精神の確認）
- [ ] **「⑯は表示制御＋UI 状態・backend 非対象」**: localStorage（browser-storage.ts）で初回判定し ConfigResponse を触らない。
- [ ] **「押し付けない」**: スキップ/再表示可。
- [ ] **「⑰は作らない」**: M15-05 折り畳みで充足・上級者モードを新設していない。

---

## 6. コード品質・規約遵守
- [ ] 曖昧語依存でない（playbook §4.1）。localStorage は `browser-storage.ts` 経由（CLAUDE.md §10.X）。`console.log` 残置なし。
- [ ] i18n 追加時は ja/en 両ロケール parity（playbook §4.13）。

---

## 7. 既存挙動の温存（非破壊性）
- [ ] 既存ナビ遷移・登録画面（M15-05①）・M15-01〜05 の成果が不変。

---

## 8. ドキュメント・進捗ログ
- [ ] 完了報告に Plan Mode 確定（既存ガイド有無・形式/再表示・可読性範囲・test-id・DES CHANGE 要否）、変更点、テストケース数、既知の制約。
- [ ] DES CHANGE が要なら設計担当起票前提で製造は DES を直接編集していない。

---

## 9. 重大な問題の判定基準（完了承認を妨げる）
- **⑯の状態を DB/API/`ConfigResponse` に載せた**、または **DB 対象データを localStorage に載せた**（CLAUDE.md §10.X 禁止）。localStorage を `browser-storage.ts` 非経由で直接呼んだ。
- **⑰上級者モードを新設した**（M15-05 折り畳みで充足・スコープ外）。
- ⑯が押し付け（スキップ/再表示不可）。既存遷移・M15-05① レイアウトを壊した。
- Plan Mode 実査（§3.3 #1〜#5）のいずれか未確認のまま実装。既存ガイドを実査せず自作再発明（playbook §4.6）／test-id を grep 確定せず衝突（digest §4）。
- 既存 E2E が非回帰でない。製造が DES 本体を直接編集している。

## 10. 軽微な問題の判定基準（持ち越し許容）
- ガイド/空状態の文言・見た目の微調整。メニュー可読性の細部（既存遷移不変なら可）。

## 11. 質問・確認事項のフォーマット
- 「指示書 §X.X / CLAUDE.md §10.X / DES-005 §5.4 に対し実装が Z。意図確認したい」の形で根拠節併記。

## 12. レビュー完了の判定
- §1〜§8 が全て OK、§9 重大ゼロ（**特に⑯の UI 状態・押し付けない・⑰を作らない**）、§0.1 の Plan Mode 実査（#1〜#5）が揃っている。§10 軽微は持ち越し可。

---

*以上、M15-06 レビューチェックリスト v1.1.0。配置 `docs/instructions/phase3/reviews/M15-06-review-checklist.md`。指示書 v1.2.0（⑯オンボーディングのみ）と対。⑯の UI 状態・押し付けない導線・⑰非新設を最重要ゲートとする。*
