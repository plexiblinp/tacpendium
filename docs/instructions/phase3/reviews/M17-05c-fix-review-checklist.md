# M17-05c-fix レビューチェックリスト

| 項目 | 内容 |
|------|------|
| 対象指示書 | `docs/instructions/phase3/M17-05c-fix-output-spec.md` v1.0.0（PDF/PNG 実出力レビュー是正） |
| 対象指示書ID | M17-05c-fix |
| レビューモデル | Sonnet 4.6（実使用は開発者判断） |
| バージョン | 1.0.0 |
| 作成者・作成日 | 設計担当 Claude（M17 期・セッション3）/ 2026-07-18 |

> **文書ID ⇄ 実パスの正は `docs/handover/docs-map.md`（自動生成）**。

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| 1.0.0 | 2026-07-18 | 初版（M17-05c レビューチェックリスト v1.0.0 の派生＝**今回は出力内容の一部が「意図的変更」**な点が母体と異なる）。 |

---

## 0. レビュー担当への前置き

### 0.1 準備

- 必読: 指示書 v1.0.0 全節・**CHANGE-073 通知書＋DES-005 §5.13/§5.13a（v2.44.0）**・base 完了報告 `m17-05c-completion-report.md` §2〜§4・引き継ぎ `20260718-M17-05c-design-lead-handover.md` §0/§3-1・code-facts（`export-image/`・`ComboExportDocument`・`ExportDialog`・`export-items.ts`）。
- **Plan Mode 着手前確認結果の確認（必須）**: 指示書 §3.3 の **7 項目すべて**に質問書＋回答が残っているか。**とくに 7（是正後の再実測）・3（メディア除外の CSV 非波及）**。

### 0.2 基本姿勢

- **母体（M17-05c）と決定的に違う点**: 本サブは **出力内容の一部を「意図的に変更」する**（メディア視覚除外・セットプレイ名称のみ・総題削除）。**「出力内容は不変」を機械適用しない**＝**変えてよいのは CHANGE-073 が列挙した箇所だけ**。それ以外（項目の意味・値・CSV）は不変。
- **base の生き残る機構（列分割・見出し再掲・ページ番号・PNG）は非回帰で維持**。**行分割の撤去に巻き込まれて落ちていないか**を最優先で見る。
- **「静かに壊れない」は下限 70% の注記で担保**＝**縮小しすぎて潰れる／あふれて切れる のどちらも不可**。

### 0.3 報告フォーマット

- 各節「OK / 重大（§9）/ 軽微（§10）/ 質問（§11）」。

---

## 1. 【最重要】用紙寸法固定と縮小フィット

- [ ] **単独＝A4 縦／比較＝A4 横に固定**され、**内容幅ページをやめている**か。
- [ ] **行分割が撤去**され、**用紙超過時のみ縮小フィット**（通常は原寸）か。**行分割の残骸で 2 系統が並存していない**か。
- [ ] **縮小率 < 70% で縮小せず、原寸のまま注記して出力**するか（**切らない・潰さない**）。**注記が出るケースの実測がある**か。
- [ ] **是正後の再実測**（§3.3-7）が完了報告にあるか: **単独 26 項目が A4 縦 1 ページ**（2 ページ化しない）・**比較 4 列レシピ 4 行が A4 に原寸**・**縮小発火の最小ケース**。**机上でなく実測**か。

## 2. 【最重要】出力内容の変更は「CHANGE-073 の列挙分だけ」

- [ ] **メディア**: 視覚出力に **link のみ**・**video/image は出ない**か。**CSV は 3 列すべて維持**（往復テスト green）か。**CSV に波及していない**か。
- [ ] **セットプレイ**: 視覚出力で **名称のみ**（複数 `" / "`）か。**名称未設定はレシピ表示**にフォールバックするか。
- [ ] **総題「コンボ比較」が削除**され、**各コンボ列見出しは残っている**か。
- [ ] **これ以外の出力項目の意味・値が不変**か（CHANGE-052/068 の定義）。**superset・状況行畳み込みが不変**か。

## 3. 【最重要】base の生き残る機構が非回帰

- [ ] **列分割（4 列/ページ・5 件以上で列分割）・見出し列/thead 再掲・ページ番号 `N/M`・範囲ラベル**が**維持**されているか。
- [ ] **PNG は 1 枚のまま・22 件超で PNG 選択時のみ事前警告＋PDF 推奨・上限到達でハード例外**が不変か。
- [ ] **`PDF_COLUMNS_PER_PAGE` が名前付き定数**のままか（**将来の列数変更が 1 箇所で済む**形）。

## 4. ExportDialog（§5.13a）の出し分け

- [ ] **視覚形式のみ選択時＝チェックボックス 15**（video/image を出さない）／**CSV 含む選択時＝17** か。
- [ ] **CSV と視覚を同時選択時＝17**（PDF には描かない・CSV には出す）か。**同時選択を禁止していない**か。
- [ ] **M17-05b の動線・排他・選択上限・WYSIWYG が不変**か。

## 5. 非改変

- [ ] **CSV 契約・BE export クエリ・スキーマ が不変**か。**M17-05b（動線・形式選択）に手が入っていない**か。
- [ ] **比較列数の可変 UI（3 列/A3）を実装していない**か（今回スコープ外＝定数のみ）。
- [ ] **縮小下限下回り時の列自動削減を実装していない**か（今回スコープ外＝注記が正）。
- [ ] **DOM ラスタ多重キャプチャの方式を pdf-lib ネイティブ描画へ作り替えていない**か（JP フォント問題の再燃回避）。

## 6. テスト・ドキュメント

- [ ] §4 のケースが揃っているか（単独 A4/比較 A4/メディア視覚除外・CSV 維持/セットプレイ名称のみ/総題なし/出し分け/縮小注記）。`make e2e` 非回帰。
- [ ] 完了報告に **§3.3 の 7 項目**・**再実測値**・**base 非回帰の証跡**・**CSV 非波及**・**DES との drift 有無**があるか。**製造が DES を直接編集していない**か。
- [ ] 禁則表現・簡体字・「起き攻け」誤字・「DR」略記の grep 除去。

---

## 9. 重大な問題（完了承認を妨げる）

- **用紙寸法を固定していない**（内容幅ページのまま）。**行分割が残っている**（縮小フィットと二重）。
- **縮小が下限 70% を無視して潰す／あふれて切れる**（静かに壊れる）。
- **CSV に波及した**（media 3 列が欠けた・往復が壊れた）。
- **メディア除外/セットプレイ名称のみ/総題削除以外の出力内容を変えた**（CHANGE-073 の列挙外）。
- **base の列分割・見出し再掲・ページ番号・PNG 挙動を壊した**（行分割撤去の巻き添え）。
- **ExportDialog の出し分けが違う**（CSV 含む時に 15 になる／視覚のみで 17 のまま）。
- **BE/CSV 契約/スキーマ/M17-05b の動線を変えた**。**pdf-lib ネイティブ描画へ作り替えた**。
- **§3.3-7 の再実測がない**（机上で「収まるはず」で済ませた）。`make e2e` が回帰した。

## 10. 軽微（持ち越し許容）

- 縮小注記の文言（**仮でよい・確定は開発者**）。
- セットプレイ名の連結子（`" / "` 以外でも根拠があれば可）。
- 継続ページ見出しの見せ方。

## 11. 質問フォーマット

- 「指示書 §X.X / DES-005（実パス）§Y に対し実装が Z。意図確認したい」。

## 12. レビュー完了の判定

- §1〜§6 OK・§9 重大ゼロ・Plan Mode 7 項目（**とくに 7・3**）確認済み。§10 軽微は持ち越し可。

---

*以上、M17-05c-fix レビューチェックリスト v1.0.0。配置 `docs/instructions/phase3/reviews/M17-05c-fix-review-checklist.md`。重点＝**用紙固定＋縮小フィット（下限70%・切らない）**・**出力変更は CHANGE-073 の列挙分だけ**・**base の分割機構は非回帰**・**CSV 非波及**・**ExportDialog 出し分け（案B）**。*
