# 指示書 M28-02b: ゲーム更新の影響コンボを見せる画面

| 項目 | 内容 |
|------|------|
| 文書ID | M28-02b |
| バージョン | **1.2.0**（2026-09-06・**★「設計まで」と Plan Mode の区別を §0.1 へ明記した＝`D-752`。★射程は不変**） ／ 1.1.0（2026-09-06・**★★`M28-02a` が完了した＝`D-748`。⇒ §0.5 を新設し、API の形が決まったことによる含み 10 件を書いた。★返却事項が 1 件増えた**） ／ 1.0.0（2026-09-06） |
| 作成日 | 2026-09-06 |
| 作成者 | 設計担当 Claude（設計卓／フェーズ4 期） |
| 対象マイルストーン | **`M28`**（破壊的変更の窓）。上位＝`docs/instructions/M28-overview.md` v1.9.0 |
| 推奨モデル | **高精度設計**（**★成果物は画面設計とモックであり、コード量ではない**。実モデル名は `docs/human-notes/model-allocation.md`） |
| Plan Mode | **必須**（`CLAUDE.md` §9＝API 契約に触れうる変更。**★モックを Plan Mode の提示に含める**＝`D-543`） |
| レビュー | **必要**（チェックリストは着手承認の手番で設計卓が起こす＝`D-736`） |
| CHANGE 消費 | **1 本の見込み**（`DES-005`＝新画面 ＋ 一覧 ＋ 詳細 ／ `DES-002`＝経路を足す場合。**★起票は設計卓。自採番しない**＝`D-293`） |
| マイグレ消費 | **0 本の見込み**（**★次に払い出す番号は `000106` 以降。§2.2-4 の記録先を DB に置く案を採るなら要る。⇒ 止めて請求すること**＝`D-293`） |
| 前提サブ | ~~**`M28-02a` の完了**~~ **★★【2026-09-06 充足＝`D-748`】`M28-02a` は完了・受理した**（`CHANGE-159`）**。⇒ 投入可である。★着手時に §0.5 を読むこと** |
| 射程 identity | **無し**（`FR702` 本体の画面側。ledger の行ではない） |
| 配置 | `docs/instructions/M28-02b-game-update-impact-screen.md` |
| 出力 | `docs/progress/M28-02b-completion-report.md` |

---

## 0. この指示書の特殊性

### 0.1 ★★必達は「設計まで」である。実装は後でよい

**開発者の逐語（`D-668`）＝「Phase4 での必達はバックエンド、DB 構造などの決定。画面などのユーザーが触れる部分はイメージ（設計）まで出来れば実装は後でも良い」。**

**⇒ 本サブの必達成果物は 3 つである。**

| # | 必達成果物 |
|---|---|
| **★★1** | **モック HTML**（§2.1。**`D-543` の運用＝開発者が実画面を見ずに形だけを判断するための材料**） |
| **★★2** | **画面設計の記述**（§2.2〜§2.7 を埋めたもの。**`DES-005` へ入れる文面の素**） |
| **★★3** | **設計卓への CHANGE 請求**（§2.8。**★製造は `docs/design/` を編集しない**） |

**★実装まで進んでよい条件は §2.9 に書いた。★★ただし「実装しなかった」ことは失敗ではない。⇒ 上の 3 つが揃っていれば完了である。**

> **★★「設計まで」は Plan Mode のことではない**（2026-09-06 開発者からの問いに対する明確化＝`D-752`）**。**
>
> | 語 | 何を指すか |
> |---|---|
> | **Plan Mode** | **着手の手順である。⇒ どのサブでも実装の前に計画を出して承認を待つ**（`CLAUDE.md` §9）**。★本サブでも必須である。** |
> | **「設計まで」** | **成果物の範囲である。⇒ 上の 3 つ**〔モック HTML ／ 画面設計の記述 ／ CHANGE 請求〕**を出したら完了してよい、という意味。★コードを 1 行も書かなくてよい。** |
>
> **⇒ 2 つは別の軸である。★Plan Mode を通したあと、実装へ進まずに 3 つの成果物を出して終わる、という形が本サブの既定である。**


### 0.2 ★★4 つの問いはすべて決着している。ゼロから議論しない

**2026-09-06 に開発者が全問へ答えた（`D-742`）。⇒ 案を並べ直さないこと。**

| # | 問い | 決着 | 逐語 |
|---|---|---|---|
| **★★1** | **影響コンボをどこに出すか** | **(c) 起動時告知 ＋ (b) 専用画面。(c) から (b) へ飛ぶ** | 「しょっちゅう使う機能ではないため、別画面を立てたい。一覧に常に 1 軸増えると邪魔」 |
| **★★1-a** | **告知の消し方** | **「次のバージョンアップまで消せる」チェックボックスを付ける** | 同上 |
| **★★1-b** | **告知以外の入口** | **一覧に未確認がある時だけ出てくるボタンで遷移** | 同上 |
| **★★2** | **「確認した」の操作をどこに置くか** | **一覧、または、新画面。一括、または、1 つずつ** | 「確定反撃の不採用に近いイメージ」 |
| **★★3** | **基準バージョンを利用者に見せるか** | **見せる。コンボ詳細に 1 行** | — |
| **★★4** | **「影響可能性あり」の見せ方** | **製造時にモックで決める** | 「決められないので製造時にモックで決定」 |

**★★2 と 4 は「幅を持たせた決着」である。⇒ 幅の中から製造が選び、選んだ理由を報告に書くこと。★★問いへ差し戻さないこと。**

### 0.3 ★★判定する側は `M28-02a` が作る。本サブは作り直さない

**`M28-02a` §2.3 が「コンボが使っている `move` のうち、マーカーがコンボの基準より新しいものが 1 つでもあるか」の導出と、「確認した」＝基準を最新へ進める API までを作る。**

**⇒ 本サブが作るのは、その上に載る画面だけである。★判定条件を画面側で書き直さないこと**——**2 か所に書くと片方だけ直されて静かにずれる**（`M30-01` §2.5-3 と同じ形）。

**★★`M28-02a` の成果物を先に読むこと**（`docs/progress/M28-02a-completion-report.md`）。**★API の形・NULL の扱い・基準列の名前は、そこが正本である。**

### 0.4 ★★「移行告知と同型」は形だけである。既読の意味が違う

**設計卓は `D-742` で「(c) は `M28-01` が作った移行告知と同型であり、経路を 1 つ増やすだけ」と見立てた。★これは形の話であって、契約の話ではない。**

| 軸 | 移行告知（`GET /api/notices/data-migration`） | **本サブの告知** |
|---|---|---|
| 既読の単位 | **1 回きり**（`.migration-notice.json` が正本。既読にしたら二度と出ない） | **★★バージョンごと**（「次のバージョンアップまで」＝次の版が来たらまた出る） |
| 既読の取り消し | **無い**（`DES-002` の記述＝「取り消せない」） | **★決めること**（§2.2-5） |
| 保存先 | **サーバ側のファイル。ブラウザストレージを使わない** | **★決めること**（§2.2-4） |
| 一覧・履歴 | **無い**（常に 0 件か 1 件） | **★飛び先の (b) がその役を担う** |

**⇒ ★★「同型だから流用できる」と読まないこと。★流用できるのはバナーの形と 204 で「無い」を表す作法までである。**

---

### 0.5 ★★`M28-02a` は完了した。API の形は決まっている（**2026-09-06・`D-748`**）

**★`CHANGE-159` を読むこと。⇒ 以下は本サブの前提が変わった点だけである。**

| # | 決まったこと | 本サブへの含み |
|---|---|---|
| **★★1** | **「確認した」＝`POST /api/combos/{id}/acknowledge-version`。★1 件ずつだけであり一括は無い** | **★★§2.5-1 の「一括、または、1 つずつ」のうち、一括は API が無い。⇒ 一括を採るなら経路の追加を請求すること**（設計卓へ。**自分で足さない**） |
| **★★2** | **楽観的排他を要求しない。⇒ 409 は無い** | **★§2.5-5 の実査は不要になった。⇒ 競合の分岐を画面に作らないこと** |
| **★★3** | **ゴミ箱の行は「確認した」にできない**（404）**。★仮登録は対象に含む** | **★空の状態と 404 の出し分けを決めること** |
| **4** | **`updated_at` を進める。⇒ 更新日時順の一覧では並びが動く** | **★「確認した」を押した直後に行が動く。⇒ §2.5-4 の判断材料になる** |
| **★★5** | **絞り込みは `GET /api/combos?affected_by_game_update=true`。★値域外は 400** | **★(b) 専用画面はこの 1 本で作れる。⇒ 新しい一覧の仕組みを作らない**（§2.3-1） |
| **★★6** | **`affectedByGameUpdate` は `omitempty` を付けていない。⇒ `false` が必ず載る** | **★★§2.4-3 の「取得失敗を 0 件にしない」がここで効く。⇒ キーが無いのは「判定していない」であり `false` とは別である** |
| **7** | **`baselineVersion` は `omitempty` あり。★CSV には載せない** | **★§2.6-3 の判断は済んだ。⇒ エクスポートへ出さない。★§2.6-2 の実査だけは残る** |
| **★★8** | **`PATCH` は基準を進めない**（`PUT` だけが進める） | **★★これは本サブが決める事項として `followup` `combo-baseline-not-advanced-on-patch` に登録した。⇒ §7 の返却事項に足した** |
| **★9** | **事前重複チェックがマス数を知らない** | **★3 方式入力を出すとリアルタイム重複警告で同じずれが起きる**（`followup` `duplicate-precheck-ignores-position-mass`）**。⇒ 3 方式入力を実装するなら同じ手番で解く** |
| **10** | **区分表が Go と TS に二重に直書きされている** | **★FE 側で導出を使い始めるなら同じ手番で解く**（`followup` `position-bands-duplicated-go-and-ts`） |

**★★初回が 0 件であることは変わっていない**（§4-1）**。⇒ `games.current_data_version` は `2026.08.03.01` で、既存コンボの基準も同じ値である。**

---

## 1. 前提として読むもの

| # | 資料 | 読む理由 |
|---|---|---|
| 1 | **`docs/change-notes/CHANGE-159-notification.md`** | **★★判定と API の正本**（§0.3 / §0.5）**。★★2026-09-06 に `M28-02a` が完了し、内容はここへ落ちている。⇒ `docs/progress/` の完了報告ではなくこちらを読むこと** |
| 2 | `docs/instructions/M28-overview.md` §3.2 | **★案 (a)〜(e) と 10 軸。読むのは「なぜ (b) か」の理解のためであり、選び直すためではない** |
| 3 | `DES-005` §5.4（コンボ一覧）・§5.6（コンボ詳細）・§5.21（確定反撃マイリスト） | **足す先と、「不採用」の実物**（§2.5） |
| 4 | `DES-002` の `GET /api/notices/data-migration` ／ `POST /api/notices/data-migration/ack` | **告知の作法の先例**（§0.4） |
| 5 | `docs/instructions/M24-01-list-navigation-and-default-character.md` §4.10 | **★モック HTML の運用**（`D-543`） |
| 6 | `docs/handover/followup-backlog.md` `combo-list-setup-count-hides-fetch-failure` | **★★§2.4 で必ず踏む罠の先例** |
| 7 | `web/CLAUDE.md` §1（ブラウザストレージ台帳） | **★§2.2-4 で localStorage 案を採るなら必須** |
| 8 | `docs/handover/code-facts.md` | 実装の現在地。**★想定で書かない** |

---

## 2. やること

### 2.1 ★★最初の成果物はモック HTML（**Plan Mode の提示に含める**）

**`D-543` の運用に従う**（先例＝`M24-01` §4.10）。

| # | やること |
|---|---|
| **★★1** | **`docs/progress/M28-02b-mock/` に単一 HTML を置く。★4 つの面を並べる**〔起動時告知 ／ 一覧のボタン ／ 専用画面 ／ コンボ詳細の 1 行〕 |
| **★★2** | **「影響可能性あり」の見せ方は、案を 2〜3 並べて置くこと**（§0.2 の問い 4。**★開発者が選ぶ**） |
| **★3** | **作り込みすぎないこと。★判断に要る最小限でよい**——**作り込むと、作り込みそのものが実装の約束に見える**（`M24-01` §4.10 の逐語） |
| **★★4** | **これは実装の証拠ではない。⇒ モックが承認されても、実装が正しいことの根拠にはならない** |

### 2.2 (c) 起動時告知

| # | 内容 |
|---|---|
| **★★1** | **出す条件＝「未確認のコンボが 1 件以上ある」。★件数を出すこと**（「3 件のコンボがゲームの更新の影響を受けている可能性があります」の形）。 |
| **★★2** | **押すと (b) 専用画面へ飛ぶ。★これが主動線である。** |
| **★★3** | **「次のバージョンアップまで表示しない」チェックボックスを付ける**（開発者の逐語）。**★★「消す」ではなく「次の版まで延期する」である。⇒ 次の版が来たらまた出る。** |
| **★★4** | **★★消した状態をどこに持つかを決めて、案と理由を報告に書くこと。** **★候補は 3 つある**〔**(i) サーバ側のファイル**＝移行告知と同型 ／ **(ii) ブラウザストレージ**＝`CLAUDE.md` §10.X。**台帳が正本であり、未記載のキーは使えない。⇒ 使うなら CHANGE addendum で台帳へ追記してから使う** ／ **(iii) DB の列**＝マイグレを消費する。**⇒ 止めて請求**〕**。** **★★判断材料＝「延期は端末ごとか、アプリ全体か」である**——**利用者が複数の端末から見る形**（`lan` モード）**では答えが変わる。★どちらが素直かを述べて選ぶこと。** |
| **★★5** | **★取り消せるかを決めること**（延期したあとで「やっぱり見たい」に応えるか）。**★移行告知は取り消せない**（`DES-002`）**。★★ただし本サブは (b) 専用画面という別の入口を持つ。⇒ 告知を取り消せなくても袋小路にならない。★この違いを報告に書くこと。** |
| **★6** | **★★出す条件の判定に失敗したときに、黙って出さないことにしないこと**（§2.4-3 と同じ罠）。 |

### 2.3 (b) 専用画面

| # | 内容 |
|---|---|
| **★★1** | **一覧を再利用する。★新しい一覧の仕組みを作らないこと**——**`DES-005` §5.4 のコンボ一覧が既にフィルタ・ソート・行の描画を持つ。** |
| **★★2** | **ルートを決めること**（既存の流儀＝`/punish/list` / `/import/combo/helper`）。**★決めた名前を報告に書く。** |
| **★★3** | **★★一覧に軸を足さない**（開発者の逐語＝「一覧に常に 1 軸増えると邪魔」）。**⇒ 「影響コンボだけを出すフィルタ」を既存の一覧へ足す形にしないこと。★これは本サブの形を決めた理由そのものである。** |
| **★★4** | **1 行に「どの技が変わったか」を出すこと。★★これが本機能の目的である**——**開発者の逐語**（`D-729`）**＝「どのコンボに影響があるかをユーザーに見せ修正しやすいようにする事が今回の目的」。⇒ 「影響がある」とだけ出しても直せない。** |
| **★5** | **★1 コンボが複数の技で当たる場合の見せ方を決めること**（全部出すか、件数だけか）。 |
| **★6** | **★空の状態の文面を決めること**（`0` 件のとき）。**★★初回は必ず 0 件である**（§4-1）。 |

### 2.4 一覧に出すボタン

| # | 内容 |
|---|---|
| **★★1** | **未確認が 1 件以上ある時だけ出す**（開発者の逐語）。**⇒ 常設しない。** |
| **★2** | **件数を出すこと**（(c) と同じ形）。 |
| **★★3** | **★★取得に失敗したときに「0 件」として扱わないこと。⇒ ボタンが黙って消える。** **★先例が実在する**——**`combo-list-setup-count-hides-fetch-failure`**〔**セットプレイ件数の取得失敗が `0` 件と区別できず、一覧上は「セットプレイ無し」に見える**〕**。★同じ形をしている。** **⇒ 失敗は失敗として出すか、少なくとも `0` と区別できる状態を持つこと。** |
| **★4** | **★マイコンボ**（`DES-005` §5.5）**にも出すかを決めること。⇒ 出さないなら、出さない理由を書く。** |

### 2.5 「確認した」の操作

| # | 内容 |
|---|---|
| **★★1** | **置き場＝一覧、または、新画面。★粒度＝一括、または、1 つずつ**（開発者の逐語）。**⇒ 幅の中から選び、理由を書くこと。** |
| **★★2** | **★★「確定反撃の不採用に近いイメージ」は操作感の話である。永続性の話ではない。** **★★確定反撃の「不採用」は 2 つある**——**(i) `DES-005` §5.20 の探す側＝当該行をクライアント側で一時的に隠すだけで、再検索でリセットされる**〔**永続しない**〕**／ (ii) §5.21 のマイリスト側＝`adopted` / `unreachable` のトグルで、再クリックで解除**〔**DB へ書き、解除もできる**〕**。** **★★本サブの「確認した」はどちらでもない**——**`M28-02a` §2.3-4 により、コンボの基準を現在の最新へ進めることで表す。⇒ DB へ書き、しかも進める前の基準がどこにも残らない。** **⇒ ★★逐語を「一時的に隠すだけでよい」と読まないこと。★これは `M-128` の教訓**〔短い逐語の意味が 1 つに定まると思わない〕**の適用対象である。** |
| **★★3** | **★★取り消しをどうするかを決めて報告に書くこと。** **★候補**〔**(a) 取り消せない。確認ダイアログで守る** ／ **(b) 直前の基準を 1 世代だけ持つ。⇒ 列が要る＝マイグレ請求** ／ **(c) 一括を出さない。1 つずつだけにする**〕**。** **★★一括で誤爆したときに戻せないことが、この論点の中身である。⇒ 一括を採るなら (a) か (b) を同時に決めること。** |
| **★4** | **★「確認した」を押した直後に行が消える形にするか、印だけ変えて残す形にするかを決めること。⇒ 消えると「いま何をしたか」が見えない。** |
| **★★5** | **★★これは書き込みである。⇒ `M22-03` の楽観ロックの対象かを実査すること**（`combos` を更新するため）。**★競合したときの振る舞いを決めて報告に書く。** |

### 2.6 基準バージョンの表示

| # | 内容 |
|---|---|
| **★★1** | **コンボ詳細に 1 行出す**（開発者の逐語＝「(a) 見せる〔コンボ詳細に 1 行〕」）。 |
| **★★2** | **★★出す面が本当に 1 つかを実査すること。** **★先例＝`drive_damage` の符号表示は「詳細・ゴミ箱詳細・テキスト/画像エクスポートの 3 面が 1 本を共有する」**（`DES-005` §5.6。`CHANGE-155`）**。⇒ 「詳細に 1 行」を素直に読むと 1 面だが、詳細の描画を共有している面が他に在りうる。★実査して、出す面を全数で報告すること。** |
| **★★3** | **★★エクスポートへ出すかを決めること。⇒ 出すなら CSV の列契約に波及する**（`DES-006`。`M28-02a` §2.5 の射程）**。★設計卓の見立ては「出さない」である**——**列契約を動かすと取込側の互換に及び、本サブの射程を大きく超える。★ただし判断は報告して開発者へ返すこと。** |
| **★4** | **★未設定のコンボがあるかを確かめること**（`M28-02a` の backfill で全件埋まっているはずだが、`materialize` 経由の生成コンボに入らない経路が残っている可能性がある＝`M28-02a` §2.3-5）。**⇒ 空のときの表示を決めること。** |

### 2.7 「影響可能性あり」の見せ方

| # | 内容 |
|---|---|
| **★★1** | **★★製造時にモックで決める**（開発者の逐語）。**⇒ §2.1-2 で案を 2〜3 出して、開発者が選ぶ。** |
| **★★2** | **★★語を「破綻」にしないこと。★`FR307` 整合**（`M28-02a` の確定事項 4＝**アプリは「影響可能性」を出すだけ。破綻の自動断定はしない**）**。⇒ 「このコンボは繋がらなくなりました」と書かない。** |
| **★3** | **★色だけで表さないこと**（色が見分けられない利用者に届かない）。**⇒ 語か記号を併せること。** |
| **★4** | **★★「影響可能性あり」と「未確認」は同じものである**（基準が古い＝まだ確認していない）。**⇒ 2 つの語を作らないこと。★語を 1 つに決めて、4 つの面で同じ語を使うこと**（`M29-01` の射程と同じ性質。**★本サブで語を割ると `M29-01` の仕事を増やす**）。 |

### 2.8 設計書への反映（**★CHANGE 請求**）

| # | 内容 |
|---|---|
| **★★1** | **★製造は `docs/design/` を編集しない**（`CLAUDE.md` §8）。**⇒ 反映が要る箇所を挙げて設計卓へ返すこと。** |
| **★2** | **見込みの宛先**〔`DES-005`＝新画面の節 ＋ §5.4 のボタン ＋ §5.6 の 1 行 ／ `DES-002`＝経路を足す場合 ／ `web/CLAUDE.md` §1＝ブラウザストレージ案を採る場合〕**。** |
| **★3** | **★CHANGE 番号を自採番しないこと**（`D-293`）。 |

### 2.9 ★実装まで進んでよい条件

**★★3 つがすべて満たされたときに限る。**

| # | 条件 |
|---|---|
| **★★1** | **§2.1 のモックが開発者に承認されていること** |
| **★★2** | **`M28-02a` が完了し、API が在ること**（§0.3） |
| **★★3** | **§2.2-4 ／ §2.5-3 ／ §2.6-3 の 3 つの決めごとに答が出ていること** |

**⇒ 満たされないものが 1 つでも残る場合は、設計までで止めて報告すること。★★止めることは失敗ではない**（§0.1）。

---

## 3. やらないこと

| # | 内容 |
|---|---|
| **★★1** | **判定条件を画面側で書き直すこと**（§0.3） |
| **★★2** | **一覧に影響コンボの軸を常設すること**（§2.3-3。**開発者が明確に否定した**） |
| **★★3** | **影響コンボを保存するテーブルを作ること**（`M28-02a` の確定事項 3＝**導出する**） |
| **★★4** | **バージョン文字列の形を触ること**（`D-728` で確定。`YYYY.MM.DD.NN`） |
| **★5** | **「破綻した」と断定する文面を書くこと**（§2.7-2） |
| **★6** | **`M29-01` の語彙統一を先取りすること**（**★本サブは自分が作る 4 面の中で語を 1 つに保てばよい**＝§2.7-4） |
| **★7** | **CSV の列契約を動かすこと**（§2.6-3。**★設計卓の見立ては「出さない」**） |

---

## 4. ★★危険（**先に読むこと**）

| # | 危険 | なぜ危ないか |
|---|---|---|
| **★★1** | **★★初回は必ず 0 件である** | **`D-725`＝既存コンボの初期値は「列を足した時点の最新」。⇒ 画面を作って動かしても何も出ない。★★「0 件だったから正しい」にならない。⇒ 人工的にマーカーを古くしたデータで確かめること。★確かめ方を報告に書く** |
| **★★2** | **★★取得失敗が「0 件」に見える** | §2.4-3。**先例が実在する**（`combo-list-setup-count-hides-fetch-failure`） |
| **★★3** | **★★「不採用に近い」を「永続しない」と読む** | §2.5-2。**★実物の「不採用」は 2 つあり、どちらとも違う** |
| **★★4** | **★★一括の「確認した」を戻せない** | §2.5-3。**基準を進めると前の値が残らない** |
| **★5** | **★詳細の 1 行が 3 面へ波及する** | §2.6-2。**`drive_damage` の先例** |
| **★6** | **★モックを作り込みすぎる** | §2.1-3。**作り込みが実装の約束に見える** |
| **★★7** | **★★告知の「消した」をブラウザストレージへ台帳未記載のまま置く** | `CLAUDE.md` §10.X。**★`check-browser-storage-keys.sh` が赤になる。⇒ 使うなら台帳への追記を先に請求すること** |

---

## 5. テスト（**★実装まで進んだ場合のみ**）

| # | 対象 | 内容 |
|---|---|---|
| 1 | **フロント（Vitest）** | **★件数の取得失敗が `0` 件と区別されること**（§2.4-3）。**★★実 `ja.json` を引く `t` で書くこと**——**キーをそのまま返すモックでは文面の主張が空振りする**（`M23-04` 教訓 2） |
| 2 | **フロント（Vitest）** | **★未確認が 0 件のときにボタンが出ないこと** |
| 3 | **E2E（Playwright）** | **★★1 本。告知 → 専用画面 → 「確認した」→ 件数が減る、の往復。★人工的に古い基準を作ってから流すこと**（§4-1） |
| 4 | **破壊確認** | **★「確認した」を押していないのに基準が進んでいないこと**（**★★画面を開いただけで進むと、利用者が見る前に消える**） |

**★サーバの共有状態を書き換えないこと**（`D-399` (1)）。**★モックを使う場合は、照合パターンが対象以外に当たらないことを先に確かめ、対照実験を置くこと。**

**★E2E の絞り込みは `make e2e-only P=<パターン>` を使うこと。★`playwright` を直接叩かないこと**（`CLAUDE.md` §11。`D-599`）。

---

## 6. 完了条件

| # | 条件 |
|---|---|
| **★★1** | **モック HTML が `docs/progress/M28-02b-mock/` に在り、4 面 ＋ 「影響可能性あり」の案 2〜3 を含むこと**（§2.1） |
| **★★2** | **§2.2〜§2.7 の決めごとに、選んだ案と理由が報告に在ること** |
| **★★3** | **§2.8 の CHANGE 請求が報告に在ること** |
| **★4** | **§7 の返却事項に答が並んでいること**（**★答えられないものは「答えられなかった」と書くこと**＝`M-128` の運用） |
| **★5** | **`docs/progress/progress-log.md` へ 1 行追記されていること**（`CLAUDE.md` §8。**★指示書が求めていなくても追記する**） |
| **★6** | **実装まで進んだ場合は §5 が緑であること。★進まなかった場合は、§2.9 のどの条件が欠けたかを書くこと** |

---

## 7. ★開発者へ返す事項

| # | 事項 | 設計卓の見立て |
|---|---|---|
| **★★1** | **「影響可能性あり」の見せ方**（§2.7） | **★案なし。★★モックを見てから開発者が選ぶ。⇒ これは決着済みの手順であり、問い直しではない** |
| **★★2** | **「確認した」の粒度と取り消し**（§2.5-3） | **★一括を出すなら確認ダイアログを推す**——**列を足す案 (b) はマイグレを消費し、本サブの射程を超える** |
| **★★3** | **告知の「消した」の保存先**（§2.2-4） | **★サーバ側のファイルを推す**——**移行告知の作法をそのまま使え、端末をまたいでも同じに見える。★ブラウザストレージ案は台帳への追記が先に要る** |
| **★4** | **基準バージョンをエクスポートへ出すか**（§2.6-3） | **★出さないことを推す**——**CSV の列契約に及ぶ** |
| **★5** | **専用画面のルート名**（§2.3-2） | **★製造が案を出す。既存の流儀に合わせること** |
| **★★6** | **`PATCH` が基準を進めないこと**（`followup` `combo-baseline-not-advanced-on-patch`。**★★2026-09-06 追加**） | **★★案なし。★どちらが正しいかが自明でない**——**`FR307`**（自動断定しない）**から見ると `PATCH` 側が正しく、`PUT` 側が「無事の自動断定」に近い。★これは `M28-overview` §3.2.6 が案 (e) を落とした理由そのものである**〔編集したら確認済みになる〕**。⇒ 画面が「確認した」を出す手番に、`PUT` 側の挙動と併せて開発者が決める** |

---

## 8. 参照ドキュメント

| ID | パス | 参照する箇所 |
|----|------|------------|
| — | `docs/instructions/M28-overview.md` | §3.2（案と 10 軸）／ §3.4（保留の期限） |
| — | `docs/instructions/M28-02a-game-update-schema-and-backend.md` | §2.2（マーカーと基準列）／ §2.3（判定の導出） |
| DES-005 | `docs/design/05-screen-design.md` | §5.4（一覧）／ §5.5（マイコンボ）／ §5.6（詳細）／ §5.20・§5.21（**「不採用」の実物**） |
| DES-002 | `docs/design/02-architecture.md` | `GET /api/notices/data-migration` ／ `POST /api/notices/data-migration/ack` |
| DES-006 | `docs/design/06-validation.md` | CSV の列契約（§2.6-3） |
| REQ-001 | `docs/design/requirements.md` | **`FR307`**（破綻の自動断定をしない）／ `FR702` |
| — | `docs/instructions/M24-01-list-navigation-and-default-character.md` §4.10 | **モック HTML の運用**（`D-543`） |
| — | `docs/handover/followup-backlog.md` | `combo-list-setup-count-hides-fetch-failure` ／ `materialize-bypasses-required-fields` |
| — | `web/CLAUDE.md` §1 | ブラウザストレージ台帳 |
| — | `docs/handover/code-facts.md` | 実装の現在地 |

---

*以上、M28-02b 指示書 **v1.2.0**。* **★★本サブの落とし穴は「画面を 1 枚作るサブだ」と読むことである。** **★4 つの問いは決着しているが、決着の中に「幅」が 3 つ残っている**〔告知の保存先 ／ 「確認した」の粒度と取り消し ／ 「影響可能性あり」の見せ方〕**。⇒ 幅を埋めるのが本サブの仕事であり、埋めた理由を書くことが成果物である。** **★★もう 1 つは、初回が必ず 0 件だということである**（§4-1）**——⇒ 動かして何も出ないことは、正しさの証拠にならない。**
