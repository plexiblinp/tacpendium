# 指示書 M33-02: 9 群の生成と同一性の検証

| 項目 | 内容 |
|------|------|
| 文書ID | M33-02 |
| バージョン | **1.2.0**（2026-09-19・**★★★§0.3 の「並列 0 本」を*理由に合わせて*緩めた**＝`D-900`〔開発者の指摘＝「`M38-03` は設計書関連なので衝突しない認識」**。⇒ そのとおりであった**〕**。★段取り・9 群・完了条件は 1 文字も変えていない**） ／ **1.1.0**（2026-09-15・**★★起票時の推測が事実になった箇所を 3 か所つぶした**＝`D-883`〔**`M37` は着地済み ／ マイグレ増分は `000113`〜`000117`**〕**。★射程・段取り・完了条件は 1 文字も変えていない**） ／ **1.0.0**（2026-09-13・起票） |
| 作成日 | 2026-09-13 |
| 作成者 | 設計担当 Claude（設計卓） |
| 対象マイルストーン | **`M33`**（マイグレーションの統合）。上位＝`docs/instructions/M33-overview.md` v2.6.0 |
| 推奨モデル | **★★上位**（**★111 本を 9 本へ潰し、同一性を独立構築で比較する。⇒ 本 MS で最も重い**。実モデル名は `docs/human-notes/model-allocation.md`） |
| Plan Mode | **必須**（`CLAUDE.md` §9＝**スキーマ・データ移行を伴う変更そのものである**） |
| レビュー | **必要**（**★チェックリスト＝`docs/instructions/reviews/M33-02-review-checklist.md` v1.1.0**） |
| CHANGE 消費 | **1〜2 本の見込み**（`SUPP-001` §2.7 の再更新 ／ `DES-002` §11 系。**★自採番しない**＝`D-293`） |
| マイグレ消費 | **★★★0 本**（**⇒ 新系列は `000001` から始まる。★既存の連番体系の外であり、設計卓は番号を払い出さない**＝§0.2） |
| 前提サブ | **★★★`M33-01`**（完了・受理済み）**＋ `M31` 族と `M32` の全着地 ＋ `M37` の全着地 ＋ 並列 0 本**（§0.3） |
| 射程 | **`M33` の 3 段のうち第 2 段**（`M33-overview` §5） |
| 配置 | `docs/instructions/M33-02-generate-and-verify-groups.md` |
| 出力 | `docs/progress/M33-02-completion-report.md` |

---

## 0. この指示書の特殊性

### 0.1 ★★★本サブは「分離ブランチで作る」。**採用は条件を満たしたときだけである**

**★`M33-overview` §6.2 の逐語＝「`M33-02` の比較が全一致した場合のみ `M33-03` へ進む。⇒ 一致しなければ、ここへ進まずに旧系列のままリリースする」。**

**★★★したがって本サブは「失敗してよい」サブである。⇒ 一致しなかったことを正直に出すのが正しい完了形である。**

**★★合わせ込みで一致させないこと。** 差が出たら差を出す。**⇒ 「一致するまで直す」と、何が違ったのかが消える。**

**★リリースの可否を本 MS に握らせない**（同 §6.2 段 4）**。⇒ 旧系列のまま出しても利用者に不都合は無い。★111 本という本数は開発側の見通しの問題である。**

### 0.2 ★★★新系列は `000001` から始まる。**設計卓は番号を払い出さない**

**★`D-791` の決着＝案 (a)。⇒ 開発 repo の `migrations/` そのものを 9 群へ置き換える**（開発者の逐語＝「開発 repo も 7 群へ」。**★群数は `D-858` で 9 へ増えた**）**。**

**⇒ 新系列の連番は既存の `000001`〜`000117` の続きではない。★`000118` を取らないこと。**

> **★★★【2026-09-15 更新＝`D-883`】起票時は「`000112` まで ／ `000114` を取らないこと」と書いていた。⇒ その後 5 本が積まれた**〔`000113`＝**欠番** ／ `000114`〜`000116`＝`M37-04` ／ `000117`＝`M37-07`〕**。**
>
> **★数そのものは着手時に `ls migrations/` で実測すること。⇒ 本注記は「起票時の数を写すな」という意味であって、本注記の数を写せという意味ではない。**

> **★★★これは `D-293`（自採番しない）の例外ではない。⇒ そもそも既存の連番体系の外だからである。**
>
> **★判断に迷ったら止まって設計卓へ請求すること。⇒ 番号の付け方そのものが本サブの成果の一部である。**

### 0.3 ★★★並列の条件（**★v1.2.0 で緩めた＝`D-900`**）

> **★★★【2026-09-19 改訂＝`D-900`】条件は「並列 0 本」から「★走行中のサブが `migrations/` を触らないこと」へ緩めた。**
>
> **★★開発者の指摘＝「`M38-03` は設計書関連なので衝突しない認識」。⇒ 実査したところ、そのとおりであった。**
>
> **★★★根拠＝本要件の理由は `M33-overview` §6.2 段 2 の逐語「ここから `migrations/` を触るため」である。⇒ 理由が `migrations/` を名指ししている以上、`migrations/` を触らないサブは該当しない。★旧文言「1 本も無いこと」は理由より広く書かれていた。**
>
> **★★危険の実体は*ドリフト*である**——**本サブは「旧 111 本が作るスキーマ」と「新 9 本が作るスキーマ」を突き合わせる。⇒ 走行中に別のサブがマイグレを足すと、比べる相手が動き、潰し漏れが静かに生じる。★だから見るべきは「マイグレを足すか」であり、「走っているか」ではない。**
>
> | 確かめること | どう確かめるか |
> |---|---|
> | **★★★走行中のサブの*マイグレ消費*が全部 0 本か** | **★各サブの指示書のヘッダ「マイグレ消費」欄を読む。⇒ 1 本でも消費するなら着手しない** |
> | **★着手の 1 手目に `ls migrations/` を実測したか** | **★下の段落のとおり。⇒ 緩めても*この実測は省かない***（**★★並列相手が誤ってマイグレを足す可能性は 0 ではない**） |
>
> **★★★実例＝`M38-03` は並列に置いてよい**（2026-09-19 実査）**。⇒ 同サブのマイグレ消費は 0 本であり、触る面は `docs/usermanual/` ／ `scripts/release-targets.sh` の 1 行 ／ `CLAUDE.md` §8 の 1 行である。★本サブとの交点は 0 である。**
>
> **★★マージ順はどちらでもよい。⇒ `M38-03` の差分は `migrations/` を 1 バイトも触らないため、先にマージしても潰した 111 本が戻ることは無い。**
>
> **★以下は v1.1.0 までの記述：** 並列 0 本が要件である。`M33-overview` §6.2 段 2 の逐語＝「ここから `migrations/` を触るため並列 0 本が要る」。

| 着地していること |
|---|
| **`M31` 族と `M32` のすべて** |
| **★★`M37` のすべて**（**★★★2026-09-15 時点で*着地済み*である**〔**8 サブとも main へマージ済み。`B02` はフェーズ5 送り＝`D-878`**〕**。⇒ 起票時の「消費しうる」は確定した＝`000113` は欠番 ／ `000114`〜`000116`＝`M37-04` ／ `000117`＝`M37-07`**） |
| **★★走行中のサブが `migrations/` を触らないこと**（**★v1.2.0 で緩めた。⇒ 旧＝「他に走行中のサブが 1 本も無いこと」**） |

**⇒ 着手の 1 手目に `ls migrations/` の最新を実測し、`M33-01` の凍結点 `5576126` からの増分を数えること。★増えていたら、その分を対応表へ追記してから始める**（`M33-01` §0.5＝凍結点には寿命がある）**。**

### 0.4 ★★★9 群である。**7 群ではない**

**★`D-858` で `S02` と `S06` が層をまたいでいることが判明し、層ごとに分割した。**

| 群 | 内容 | 層 | 命名 |
|---|---|---|---|
| **S01** | 最終スキーマ | **A**（`AGPL-3.0-or-later`） | `NNNNNN_*.sql` |
| **★S02a** | **games** | **★A** | `NNNNNN_*.sql` |
| **S02b** | characters・custom_states | **B**（`CC-BY-SA-4.0`） | **`NNNNNN_data_*.sql`** |
| **S03** | moves | **B** | **`NNNNNN_data_*.sql`** |
| **S04** | move_commands | **B** | **`NNNNNN_data_*.sql`** |
| **S05** | move_derivations | **B** | **`NNNNNN_data_*.sql`** |
| **★S06a** | **presets** | **★A** | `NNNNNN_*.sql` |
| **S06b** | preset_aliases | **B** | **`NNNNNN_data_*.sql`** |
| **S07** | 初期 users・tags | **A** | `NNNNNN_*.sql` |

**★★★`games` / `presets` はアプリの設計物であって SF6 の事実ではない。⇒ 層 A である。★`_data_` を入れないこと。**

> **★★機械検査はこの向きを検出しない。** 規則 (4) は「`_data_` を持つなら層 B へ解決すること」しか見ず、**どの表へ書くかを見ない。⇒ `NNNNNN_data_seed_games_characters.up.sql` は緑になる。★`games` が静かに `CC-BY-SA-4.0` へ移る。**
>
> **★★戻せない。⇒ 一度 CC-BY-SA で配ったものを AGPL へ戻すのは、第三者が利用した後は取り消せない。**

### 0.5 ★★★着手前の版ゲート（**`M-169` の対処**）

| # | 確かめること | 満たさないとき |
|---|---|---|
| **1** | `docs/instructions/reviews/M33-02-review-checklist.md` が存在すること | **★止まって設計卓へ請求する** |
| **2** | `docs/instructions/M33-overview.md` が `main` に在り **v2.6.0 以上**であること（**★9 群になっているか**） | **★同上。⇒ 7 群のままなら古い** |
| **3** | `docs/progress/M33-01-completion-report.md` が `main` に在ること | **★本サブはこれを土台にしている。⇒ 無ければ進めない** |
| **4** | **`SUPP-001` が 1.68.0 以上であること**（**★規約 (18) が在るか**） | **★同上** |
| **5** | **`ls migrations/` の最新と、凍結点 `5576126` からの増分** | **★増えていたら対応表へ追記してから始める**（§0.3）**。★★★2026-09-15 時点の設計卓の把握＝増分は 5 本**〔`000113` は欠番のため実ファイルは 4 本〕**。⇒ この数を写さず実測すること**（`M-184`） |
| **6** | `git log --oneline -5` で枝元を報告へ書く | — |

---

## 1. 前提として読むもの

| # | 資料 | 読む箇所 |
|---|---|---|
| **★★★1** | `docs/progress/M33-01-completion-report.md` | **全文**（**★対応表 111 行 ／ 3 つの一覧 ／ 移行の下ごしらえ ／ §5.1 基準 DB の手順 ／ §5.2.1 の正規化ハッシュ*スクリプト本体***） |
| **★★2** | `docs/design/supp-001-detailed-design.md` | **§5.5.4 規約 (18)**（**★スキーマ同一性の固定方法。⇒ 取り違えると正しい実装が赤くなる**）／ 規約 (9)（前コホート救済）／ 規約 (16)（golden 再生成）／ **§2.7**（本数と欠番） |
| **★★3** | `docs/instructions/M33-overview.md` v2.6.0 | **§0.5.3**（**★9 群と層**）／ §2（危険 4 件）／ §4.3（保全しないの条件）／ §4.4（新系列へ移す）／ **§6.2**（投入の時機） |
| **★4** | `docs/handover/followup-backlog.md` | `m33-02-sqlite-sequence-traps` ／ `m33-users-tags-seed-vs-user-predicate-undefined` ／ `three-point-update-down-below-golden-version` |
| 5 | `scripts/check-migration-license.sh` | **`GAME_TABLES` の定義 ／ 規則 (3)（凍結表）／ 規則 (4)** |
| 6 | `docs/process/parallel-board.md` | **`D-858`**（9 群への分割・廃止 8 本・`000018`/`000074` の分割）／ `D-791`（新系列へ移す）／ `D-293` |

---

## 2. やること

### 2.1 ★★段 1 — 前提の再確認（**★コードを書く前に**）

| # | 確かめること | なぜ |
|---|---|---|
| **★★★1** | **並列 0 本であること**（§0.3） | **★1 本でも走っていると `migrations/` が動く** |
| **★★2** | **`M33-01` の対応表が今も有効か**（凍結点からの増分） | **★増分が 1 本でも出たら、追記してから始める** |
| **★3** | **基準 DB を `M33-01` §5.1 の手順で作り直し、`M33-01` のハッシュと一致すること** | **★★これが「土台が同じである」ことの確認である。⇒ 一致しなければ、ここで止まって報告する** |
| **★4** | **`M33-01` §5.2.1 のスクリプト本体を入手して回すこと**（**★値だけを使わない**＝規約 (18)） | **★書式が違えば値も違う。⇒ 誤診の元** |

### 2.2 ★★★段 2 — 9 群の生成

| # | やること |
|---|---|
| **★★1** | **`M33-01` の対応表に従って、旧 111 本の内容を 9 群へ振り分ける** |
| **★★★2** | **層 A の 4 群**（`S01` / `S02a` / `S06a` / `S07`）**に `_data_` を入れない。層 B の 5 群に必ず入れる**（§0.4） |
| **★★3** | **廃止 8 本は書かない**〔`000009` / `000010` / `000011` / `000022` / `000028` / `000029` / `000081` / `000103`〕**。★ただし `000009` の `sqlite_sequence` は再現する**（§2.4） |
| **★★4** | **`000018` / `000074` を層で分割する**——**`000018` は DDL 部を `S01`・`raw_data` の最終値を `S03` ／ `000074` は `ALTER` を `S01`・`character_id` の値を `S06b`** |
| **★5** | **投入順は `M33-01` §4.3 の 21 表のトポロジカル順に従う。⇒ 複合 FK `combo_setup_results` → `combo_setups` を必ず後に置く** |
| **★6** | **自己参照 3 本は「NULL で入れてから UPDATE」の 2 パスで解く。⇒ 遅延制約は使わない**（`M33-01` §1-4） |

### 2.3 ★★★段 3 — 同一性の検証（**★本サブの本体**）

| # | やること |
|---|---|
| **★★★1** | **旧最終状態と新 baseline を*独立に*構築する。⇒ 本番の `modernc.org/sqlite` ＋ `golang-migrate` で** |
| **★★★2** | **正規化スキーマを比較する**（規約 (18)）**。⇒ `pragma table_info` / `foreign_key_list` / `index_list` から構造だけを取る。★`sqlite_master.sql` のテキストで比較しない** |
| **★★3** | **全行を比較する。⇒ 表ごとに件数と内容の両方** |
| **★★★4** | **`sqlite_sequence` を比較する**（§2.4 の罠） |
| **★★5** | **一致しなかった箇所を*そのまま出す*。⇒ 合わせ込まない**（§0.1） |

### 2.4 ★★★段 4 — `sqlite_sequence` の罠 2 件（**★静かにずれる**）

| # | 罠 | 対処 |
|---|---|---|
| **★★1** | **`combos` / `combo_oki_options` は行数 0 でも `sqlite_sequence` に `seq=0` の行を持つ**〔`combos` は `000016`/`000019` の表再構築、`combo_oki_options` は `000021` の 0 行 `INSERT`〕 | **★「空の表は飛ばす」と実装しないこと** |
| **★★★2** | **`000009` を廃止すると `characters` の `seq` 差 3 が失われる**〔実測＝行 31 / `seq` 34〕 | **★`S02b` が `sqlite_sequence` を合わせる責任を持つ** |

**★`preset_aliases` の差 179 について＝`M33-01` は「削除された技 1 件につき official_ja の alias が 1 件消えるため」と*推定*しており、実測していない。⇒ 機序を前提にせず、値を合わせること。**

### 2.5 ★段 5 — 移行手順の材料を渡す（**★手順そのものは `M33-03`**）

**★`M33-01` §4.3 / §4.4 が出した材料**（21 表の投入順 ／ `sqlite_sequence` の扱い ／ 数えるための式）**を、実際に 9 群で通したときの結果で更新すること。⇒ `M33-03` が使う。**

**★★`users` / `tags` の「seed 由来と利用者由来を分ける述語」は未確定である**（`followup` の `m33-users-tags-seed-vs-user-predicate-undefined`）**。⇒ 本サブでは決めない。★`M33-03` の入力として残す。**

---

## 3. やらないこと

| # | 内容 | 理由 |
|---|---|---|
| **★★★1** | **一致しない差を合わせ込むこと** | **★§0.1。⇒ 差を出すのが正しい完了形である** |
| **★★2** | **`migrations/legacy/` を作ること ／ 歴史テストを移すこと** | **`M33-03` の射程**（`M33-overview` §5） |
| **★★3** | **golden / seedgen / CSV 同期テストを整えること** | **同上** |
| **★★★4** | **既存の連番の続きを取ること** | **★§0.2。⇒ 新系列は `000001` から** |
| **★5** | **`users` / `tags` の分け方を決めること** | **★`M33-03` の入力**（§2.5） |
| **★6** | **設計書本体を書き換えること** | `CLAUDE.md` §8。**⇒ as-built は設計伝達レポート §4 へ** |
| **★★7** | **`followup-backlog.md` の編集** | **`D-838`。⇒ 停止時記録も設計伝達レポート §4 へ原稿として書く** |
| 8 | **開発者の手元 DB を触ること** | **★触れない。⇒ 移行手順は `M33-03`** |

---

## 4. ★★危険（**先に読むこと**）

### 4.1 ★★★最大の危険＝テキストで同一性を判定すること

**★規約 (18)。⇒ 適用済み DB の生テキストは、旧系列が 111 本かけて積み上げた文字列そのものである。★意味が同じでも手書きの DDL とは一致しない。**

**★★`M33-01` の実測＝`combos` は表名が引用符付き ／ `moves` は `ALTER ADD COLUMN` の追記が 1 行に連なる ／ 13 表が旧 SQL の `--` コメントを本文として含む。**

**⇒ ここを取り違えると、正しい実装が赤くなる。★「一致しない」と報告する前に、比較の取り方を確かめること。**

### 4.2 ★★★2 番目＝ライセンスの層が静かに混ざること

**§0.4。⇒ 機械検査は「どの表へ書くか」を見ない。★`_data_` を付ければ緑になる。**

**★★★層 A の 4 群に `_data_` を付けないこと。⇒ 付けた瞬間、`games` / `presets` / `users` / `tags` が `CC-BY-SA-4.0` へ移る。**

**★`bash scripts/check-migration-license.sh` は回すが、緑を根拠にしないこと。⇒ 表ごとの割当を目で確かめる。**

### 4.3 ★★3 番目＝`sqlite_sequence` を忘れること

**§2.4。⇒ 行数が合っていても `seq` がずれる。★「空の表は飛ばす」「廃止したから書かない」の 2 つが、どちらも `seq` を落とす。**

### 4.4 ★★4 番目＝並列で走らせること

**§0.3。⇒ `migrations/` 全体を触る。★他サブが 1 本でも走っていると、対応表が着手中に古くなる。**

**★★着手時に確かめ、報告に書くこと。**

### 4.5 ★5 番目＝「一致した」を早く言うこと

**★比較は 4 面ある**〔正規化スキーマ ／ 全行 ／ `sqlite_sequence` ／ 件数〕**。⇒ 1 面の一致で「全一致」と書かないこと。**

**★★`M33-overview` §6.2 段 3 は「全一致した場合のみ `M33-03` へ進む」と定めている。⇒ ここの判定が次段の可否を決める。**

---

## 5. テスト

| # | 確かめること | 判定 |
|---|---|---|
| **★★★1** | **旧最終状態と新 baseline の正規化スキーマが一致すること**（規約 (18) の取り方で） | **★出力そのもので判定する。⇒ ハッシュだけでなく、突き合わせた表数・列数も書く** |
| **★★★2** | **全 21 表の全行が一致すること** | **★表ごとに件数を出す** |
| **★★★3** | **`sqlite_sequence` が一致すること**（§2.4） | **★★とくに `characters` の `seq` 34 と、`combos` / `combo_oki_options` の `seq=0`** |
| **★★4** | **【破壊確認】9 群のどれか 1 本から 1 行を落とすと、比較が*落ちる*こと** | **★★これが「比較が生きている」証拠である。⇒ 「一致した」だけでは、比較が何も見ていない可能性を排除できない** |
| **★★★5** | **【破壊確認】層 A の 1 本に `_data_` を付けると `check-migration-license.sh` が*層 B へ解決する*こと** | **★★§4.2 の歯止め。⇒ 検査がこの向きを見ないことを、実測で確かめておく** |
| **★6** | **`foreign_key_check` が 0 件 ／ `integrity_check` が `ok`** | |
| **★7** | **新系列を空の DB へ最初から適用して通ること**（`version` と `dirty=0`） | |
| 8 | **`go test ./...`**（**★歴史テストは `M33-03` で移すため、ここでは落ちうる。⇒ 落ちたものを列挙して報告する**） | **★「落ちた」を隠さない** |
| 9 | **`bash scripts/check-migration-license.sh`**（**★緑を根拠にしない**＝§4.2） | |

---

## 6. 完了条件

| # | 条件 |
|---|---|
| **★★1** | **§0.5 の版ゲート 6 点と、§2.1 の段 1 が完了報告にある** |
| **★★2** | **9 群が生成されている。⇒ 層 A の 4 群に `_data_` が無く、層 B の 5 群に在る** |
| **★★★3** | **§5 の比較 4 面すべての結果がある**〔正規化スキーマ ／ 全行 ／ `sqlite_sequence` ／ 件数〕**。⇒ 一致しなかった面が在るなら、そのまま書かれている** |
| **★★★4** | **§5-4 / §5-5 の破壊確認 2 件がある** |
| **★5** | **新系列の連番が `000001` から始まっている**（§0.2） |
| **★6** | **廃止 8 本が書かれておらず、`000009` の `seq` 差 3 が再現されている** |
| **★7** | **`000018` / `000074` が層で分割されている** |
| **★8** | **`migrations/legacy/` を作っていない ／ 歴史テストを移していない**（`M33-03` の射程） |
| **★★9** | **`go test ./...` で落ちたものが*列挙*されている**（**★歴史テストは落ちうる。⇒ 隠さない**） |
| **★10** | **`docs/progress/M33-02-completion-report.md` ＋ `progress-log.md` への索引行**（`CLAUDE.md` §8） |
| **★★11** | **設計伝達レポート**（`/design_handover_report`）**。★§4 に「全一致したか」を最初の 1 行で書くこと**（**⇒ `M33-03` の可否を決める**） |

---

## 7. ★開発者への確認事項

| # | 事項 | 設計卓の暫定案 |
|---|---|---|
| **1** | **新系列の連番の付け方** | **★★案＝`000001`〜`000009` の 9 本。⇒ 群の順序は投入順**〔`S01` → `S02a` → `S02b` → `S03` → `S04` → `S05` → `S06a` → `S06b` → `S07`〕**。★別の付け方を採るなら止まって請求すること** |
| **2** | **一致しなかった場合の扱い** | **★★案＝そこで止めて報告する。⇒ `M33-03` へ進まない**（`M33-overview` §6.2 段 3）**。★合わせ込まない** |

**★上記 2 件とも「進めてよいが、判断が変わったら止まる」形である。⇒ 着手前の待ちは無い。**

---

## 8. 参照ドキュメント

| ID | パス | 参照する箇所 |
|----|------|------------|
| — | `docs/progress/M33-01-completion-report.md` | **全文**（**★本書の土台。とくに §4.3 投入順 ／ §5.1 基準 DB ／ §5.2.1 ハッシュのスクリプト**） |
| SUPP-001 | `docs/design/supp-001-detailed-design.md` | **§5.5.4 規約 (18)** ／ 規約 (9) ／ 規約 (16) ／ §2.7 |
| — | `docs/instructions/M33-overview.md` v2.6.0 | **§0.5.3**（9 群と層）／ §2 ／ §4.3 ／ §4.4 ／ **§6.2** |
| — | `scripts/check-migration-license.sh` | `GAME_TABLES` ／ 規則 (3) ／ 規則 (4) |
| — | `docs/handover/followup-backlog.md` | `m33-02-sqlite-sequence-traps` ／ `m33-users-tags-seed-vs-user-predicate-undefined` |
| — | `docs/process/parallel-board.md` | **`D-858`** ／ `D-791` ／ `D-854` ／ `D-293` |

---

*以上、指示書 M33-02 **v1.2.0**。* **★★★本サブは「失敗してよい」サブである**（§0.1）**。⇒ 一致しなかったことを正直に出すのが正しい完了形であり、合わせ込んで一致させるのが最悪の形である。** **★★次に重いのは §4.2 である**——**層 A の 4 群に `_data_` を付けた瞬間、`games` / `presets` / `users` / `tags` が `CC-BY-SA-4.0` へ移り、機械検査は緑のままである。⇒ そして戻せない。**
