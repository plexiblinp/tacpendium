# 指示書 M31-02: 共通画面部品 — キャラ選択の入力挙動

| 項目 | 内容 |
|------|------|
| 文書ID | M31-02 |
| バージョン | **1.1.0**（2026-09-08・**★★完了後の注記を §0.0 に足した＝`D-779`。★本文は書き換えていない**） ／ 1.0.0（2026-09-07） |
| 作成日 | 2026-09-07 |
| 作成者 | 設計担当 Claude（設計卓／フェーズ4 期） |
| 対象マイルストーン | **`M31`**（`α②`）。上位＝`docs/instructions/M31-overview.md` v1.2.0 |
| 推奨モデル | **標準実装**（**★2 件で小さい。★★ただし共通部品であり、波及先が全画面である**。実モデル名は `docs/human-notes/model-allocation.md`） |
| Plan Mode | **必須**（`CLAUDE.md` §9＝**全体に影響するリファクタリング**。⇒ 共通部品は全画面へ波及する） |
| レビュー | **必要**（チェックリストは着手承認の手番で設計卓が起こす＝`D-736`） |
| CHANGE 消費 | **0 本の見込み**（**★入力挙動の是正であり、`DES-005` の契約は変わらない見込み。★変わると分かったら止めて請求する**＝`D-293`） |
| マイグレ消費 | **0 本** |
| 前提サブ | **無し**（**★`M31-01` とは並列可の見込み。ただし 1 か所だけ交わりうる**＝§0.2） |
| 射程 identity | **2 件**——`P4M-012`（キャラ選択後 `Enter` が反応しない）／ `P4M-013`（キャラ選択のドラッグ判定） |
| 配置 | `docs/instructions/M31-02-shared-character-picker.md` |
| 出力 | `docs/progress/M31-02-completion-report.md` |

---

## 0.0 ★★【2026-09-08 完了後の注記＝`D-779`】本書の完了条件 3 は履行されない（**受理済み**）

> **★★本文は書き換えない**（`D-274` (3)＝**投入済みの指示は歴史記録であり、注記で足す**）**。⇒ 読むときは次を上書きして読むこと。**

| 本書の記述 | 実際 | 裁定 |
|---|---|---|
| **§2.2 段 2 ／ §5-1 ／ §5-2 ／ §6-3**（`P4M-012` を直す） | **★★1 行も変えていない** | **★★受理**（2026-09-08 開発者裁定＝**恒久に不要・クローズ**）**。⇒ `M31-overview` 1.5.0 が identity から落とした** |
| **§7**（2 件の「正しい挙動」を開発者へ問う） | **★★両方とも 2026-09-02 に回答済みだった**（`M27-02a` の裁定） | **★本書と `M31-overview` と本書のチェックリストは `M27-02a` にも `SD-015` / `SD-016` にも 1 度も言及していない。⇒ 起票側の落ちである**（計測点 `M-144`） |
| **§3-3**（共通部品を作り替えない） | **★`TagSelector` にも同じガードを当てた** | **★★触れていない。⇒ 部品の作り替えではなく、同じ回避を当てただけである**（2026-09-08 開発者が実機で確認して射程を広げた） |

**★★`P4M-012` の実体＝`Enter` は正しく効いている。⇒ 候補が確定し popover が閉じ、フォーカスがトリガへ戻る。そこで再度 `Enter` を押すと、折りたたまれた combobox の ARIA 標準挙動として候補が開き直す。★「反応しない」の実体は「次へ進まず、同じ欄が開き直すだけ」であり、`SearchableSelect` 単体に欠陥は無い。**

**★真の非対称は followup `editor-character-field-outside-field-sequence` にある**（キャラ欄が順送りの根の外・タブより上に在る）**。⇒ そちらも 2026-09-08 に「対象外（現状維持）」となった。**

---

## 0. この指示書の特殊性

### 0.1 ★★2 件しかないが、単独のサブにしてある（**理由がある**）

**★開発者承認済み**（`D-747`。逐語＝「1,2,3 はあなたの案を承認します」）**。**

**理由＝共通部品は他の全画面に波及する。⇒ セットプレイ・確定反撃の変更と同じコミットに混ぜると、赤が出たときに切り分けられない**（`M31-overview` §3.1）**。**

**★★したがって「小さいから `M31-01` へ寄せる」をしないこと。⇒ 分けてあること自体が設計判断である。**

### 0.2 ★★`M31-01` との交点が 1 か所だけありうる

**★★`M31-01` の `P4M-015`（確定反撃の自キャラ既定値）は、キャラ選択の部品を使う。⇒ 本サブと同じファイルへ入りうる。**

**⇒ 着手時に実査し、重なったら設計卓へ知らせること**（`D-740` の交点表）**。★重なったら本サブを先に通し、`P4M-015` を後へ回す。**

**★他の面は交わらない見込みである。**

### 0.3 ★★入力挙動の是正であり、見た目の変更ではない

**★2 件とも「押しても効かない」「掴んだつもりが選択になる」という種類である。⇒ 画面の絵は変わらない。**

**★★これは E2E で見えにくい**——**`getByRole` は要素の存在を見るが、キー入力やドラッグの効き方は見ない。⇒ §5 で明示的に測ること。**

---

## 1. 前提として読むもの

| # | 資料 | 読む箇所 |
|---|---|---|
| 1 | `docs/instructions/M31-overview.md` v1.2.0 | §2.1（identity 表の 7 と 8）／ §3.1（並列可の根拠） |
| 2 | `docs/design/05-screen-design.md` | **キャラ選択が現れる画面すべて**（**★段 1 で全数を数えること**＝§2.1） |
| 3 | `docs/handover/code-facts.md` | **★共通部品の所在**（**★同書は `main` より古い可能性がある。⇒ 実物で確かめること**） |
| 4 | `web/CLAUDE.md` | §2（コンボ型の 3 分岐）／ §1（ブラウザストレージ台帳） |
| 5 | `docs/design/testid-convention.md` | **★testid を足すなら登録が要る** |

---

## 2. やること

### 2.1 段 1 — 実査（**★★コードを書く前に、これを済ませて設計卓へ報告すること**）

| # | 確かめること | なぜ |
|---|---|---|
| **★★1** | **キャラ選択の共通部品がどれか。★実体が 1 つか、複数あるか** | **★★複数あると「片方だけ直した」が起きる。⇒ 波及先を数える前に、部品の数を数える** |
| **★★2** | **その部品を使っている画面の全数**（`DES-005` の節と対応させる） | **★★§5 の破壊確認で、どの画面を試すかを決めるのに要る**（`SUPP-001` §5.5.4 (10)） |
| **★★3** | **`P4M-012`**（キャラ選択後 `Enter` が反応しない）**の再現手順** | **★直す前に「壊れている」を実測で押さえる。⇒ 陽性対照になる** |
| **★★4** | **`P4M-013`**（キャラ選択のドラッグ判定）**の再現手順**——**何をしたときに、何が起きてほしくて、実際は何が起きるか** | **★★`P4M-013` は現象の記述が 1 行しかない。⇒ 何が「正しい」かを先に言語化して報告すること。★推測で直さない** |
| **★5** | **`M31-01` との交点**（§0.2） | **★`P4M-015` が同じファイルへ入るか** |

### 2.2 段 2 — `P4M-012`（キャラ選択後 `Enter` が反応しない）

| # | やること |
|---|---|
| **★★1** | **キャラを選んだ後に `Enter` が効くようにする** |
| **★★2** | **★★「効く」が何を指すかを段 1 で言語化してから直すこと**——**候補＝選択を確定する ／ 次の入力欄へ進む ／ フォームを送信する。⇒ どれかで挙動が全く違う** |
| **★3** | **フォームの送信と衝突しないこと**（**★`Enter` でフォームが送信される画面がある。⇒ 選択の確定と送信が二重に起きないこと**） |
| **★4** | **キーボードだけで操作できること**（**★`Enter` が効かないのは、キーボード操作の入口が塞がっているという意味である**） |

### 2.3 段 3 — `P4M-013`（キャラ選択のドラッグ判定）

| # | やること |
|---|---|
| **★★1** | **段 1-4 で言語化した「正しい挙動」に合わせる** |
| **★★2** | **★★閾値を導入するなら定数化する**（`CLAUDE.md` §4＝**マジックナンバーは定数化**）**。⇒ 「何 px 動いたらドラッグ」をリテラルで散在させない** |
| **★3** | **タッチ操作でも壊れないこと**（**★スマホ専用画面がある**＝`DES-005` §5.2） |

### 2.4 段 4 — 波及の確認

| # | やること |
|---|---|
| **★★1** | **段 1-2 で数えた画面すべてで、キャラ選択が従来どおり動くこと** |
| **★★2** | **★★全部は試さなくてよい。⇒ 区分から 1 件ずつ取ること**（`SUPP-001` §5.5.4 (10)。**★偏ると、守られていない区分を守っていることにできる**） |
| **★3** | **区分の割り方を報告に書くこと**（**★「代表を選んだ」なら、何を代表にしたかを書く**） |

---

## 3. やらないこと

| # | 内容 | 理由 |
|---|---|---|
| **★★1** | **キャラ選択の見た目を変える** | **★§0.3。⇒ 入力挙動の是正である。★絵を変えると波及の確認が成立しなくなる** |
| **★★2** | **`M31-01` の 9 件に手を出す** | **★別サブ。★特に `P4M-015` は交点でありうる**（§0.2） |
| **★3** | **共通部品を作り替える** | **★2 件の是正が射程である。⇒ 作り替えが要ると分かったら止めて請求する**（**★`M24-12` の先例＝エディタの作り替えは独立のサブになった**） |
| **★4** | **設計書本体を書き換える** | `CLAUDE.md` §8 |
| **★5** | **番号の自採番** | `D-293` |
| **★6** | **`followup-backlog.md` の本表を編集する** | **★製造が書けるのは §J だけである**（`D-382`） |

---

## 4. ★★危険（**先に読むこと**）

### 4.1 ★★最大の危険＝**「正しい挙動」を推測で決めること**

**★`P4M-013`（ドラッグ判定）は現象の記述が 1 行しかない。⇒ 何が起きてほしいのかが書かれていない。**

**★★段 1-4 で言語化し、報告してから直すこと。⇒ 推測で進めるなら「推測: 〜と仮定した」とコード内コメントまたは報告へ明示すること**（`CLAUDE.md` §9-3）**。**

### 4.2 ★★2 番目＝**部品が複数あることを見落とすこと**

**★段 1-1。⇒ 「共通部品」と呼ばれていても、実体が 1 つとは限らない。★片方だけ直すと、直っていない画面が残り、しかもテストは緑になる。**

### 4.3 ★★3 番目＝**波及の確認が偏ること**

**★★全画面を試す必要はないが、区分から 1 件ずつ取ること**（`SUPP-001` §5.5.4 (10)）**。**

**★`M28-04` で実際に落ちた形である**（計測点 `M-139`）**——対象区分を指定しないと、守られている区分ばかりを試して「機構は生きている」の証拠にできてしまう。**

### 4.4 ★4 番目＝**E2E が入力の効き方を見ていないこと**

**§0.3。⇒ `getByRole` は要素の存在を見るが、`Enter` が効いたかは見ない。★「spec が緑」を「直った」と読まないこと。**

---

## 5. テスト

| # | 何を | 判定 |
|---|---|---|
| **★★1** | **`P4M-012`**——**キャラ選択後に `Enter` を押すと、段 2-2 で決めた挙動が起きること** | **★★段 1-3 の再現手順を陽性対照にする。⇒ 直す前は起きないことを先に示す** |
| **★★2** | **`Enter` によるフォーム送信と二重に起きないこと** | **★§2.2-3。⇒ 送信が 2 回走らないことを実測で示す** |
| **★★3** | **`P4M-013`**——**段 1-4 で言語化した挙動になること** | **★閾値を入れたなら、境界の両側を試すこと** |
| **★★4** | **【破壊確認・対象の名指し】段 1-2 で数えた画面の区分から 1 件ずつ**、**キャラ選択が従来どおり動くこと** | **★★区分の割り方を報告に書く。⇒ 「代表を選んだ」だけでは足りない**（`M-139`） |
| **★5** | **タッチ操作**（スマホ専用画面＝`DES-005` §5.2） | **★`P4M-013` はポインタの種別で挙動が変わりうる** |
| 6 | **`cd web && pnpm test`** | **★出力で判定する**（終了コードだけを見ない） |
| **★7** | **`make e2e-only P=<パターン>`**（**★`playwright` を直接叩かない**＝`CLAUDE.md` §11） | **★キャラ選択を含む spec を回す** |

---

## 6. 完了条件

| # | 条件 |
|---|---|
| **★★1** | **段 1 の実査 5 件に結果がある**（**★特に 1**〔部品の数〕**と 2**〔画面の全数〕**は数で書く**） |
| **★★2** | **`P4M-013` の「正しい挙動」が言語化され、報告にある**（**★実装より先に**） |
| **★★3** | **`P4M-012` / `P4M-013` が直っている。★どちらも陽性対照と対で示されている** |
| **★★4** | **波及の確認が、区分から 1 件ずつ取れている。★区分の割り方が報告にある** |
| 5 | **§5 の検査がすべて緑**（**★出力で判定**） |
| **★6** | **testid を足したなら `docs/design/testid-convention.md` へ登録する** |
| **★7** | **`docs/progress/M31-02-completion-report.md` を書き、`docs/progress/progress-log.md` へ索引行を 1 行足す**（`CLAUDE.md` §8） |
| **★★8** | **設計伝達レポートを出す**（`/design_handover_report`）**。★§4 に `M31-01` との交点の実査結果を書くこと** |

---

## 7. ★開発者への確認事項

| # | 事項 | 設計卓の状態 |
|---|---|---|
| **★★1** | **`P4M-012` の「`Enter` が効く」が何を指すか** | **★★製造が段 1 で候補を挙げ、設計卓が畳んで開発者へ回す。⇒ 候補＝選択を確定する ／ 次の入力欄へ進む ／ フォームを送信する。★挙動が全く違う** |
| **★★2** | **`P4M-013` の「正しいドラッグ判定」** | **★同上。⇒ 現象の記述が 1 行しかなく、何が起きてほしいのかが書かれていない** |

---

## 8. 参照ドキュメント

| ID | パス | 参照する箇所 |
|----|------|------------|
| — | `docs/instructions/M31-overview.md` v1.2.0 | §2.1（7 と 8）／ §3.1 |
| DES-005 | `docs/design/05-screen-design.md` | **キャラ選択が現れる画面すべて** ／ §5.2（スマホ専用） |
| — | `web/CLAUDE.md` | §1 ／ §2 |
| — | `docs/design/testid-convention.md` | testid の登録 |
| — | `docs/process/parallel-board.md` | `D-747`（サブ分割）／ `D-740`（交点表）／ `D-293`（自採番しない）／ `D-382` |
| SUPP-001 | `docs/design/supp-001-detailed-design.md` | §5.5.4 (10)（**破壊確認の対象の取り方**） |

---

*以上、指示書 M31-02 **v1.1.0**。* **★★本サブの落とし穴は「2 件だから軽い」と読むことである。** **★共通部品であり、波及先は全画面である**（§0.1）**。⇒ 分けてあること自体が設計判断であり、`M31-01` へ寄せない。** **★★もう 1 つは「正しい挙動」が書かれていないことである**（§4.1）**——`P4M-013` は現象しか記録されていない。⇒ 言語化してから直すこと。推測で直すと、直したかどうかを誰も判定できない。**
