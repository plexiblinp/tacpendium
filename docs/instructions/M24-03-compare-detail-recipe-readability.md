# 指示書 M24-03: 比較・詳細・レシピ可読性

| 項目 | 内容 |
|------|------|
| 文書ID | M24-03 |
| バージョン | 1.1.0 |
| 推奨モデル | **高精度実装**（**★9 項目のうち 6 項目が「レシピの見せ方」という 1 つの根から出ており、面ごとに別々に直すと `DES-005` §5.4 が 6 つの選択肢を並べたまま決めきれなかった状態を再演する**。実モデル名は `docs/human-notes/model-allocation.md` が正本） |
| Plan Mode | **必須**（**★実査 14 件と、モック HTML の提示を Plan Mode に含める**。CLAUDE.md §9） |
| 機械レビュー | 必須（別チェックリスト: `M24-03-review-checklist.md`） |
| 参照してよい先行資料 | `DES-005` §4.3 / §5.4 / §5.6 / §5.8 ／ `DES-004`（内部表現・レシピ文字列） ／ `web/CLAUDE.md` §1 ／ `docs/handover/code-facts.md`（**★他サブの指示書・チェックリストは含めない**＝D-217） |
| 並列性 | **★`M24-09c` と並列可**（触る面が交わらない）。**★★`M24-02` とは並列にしない**（`/combos` の同じ表・同じ共通部品に触る）。**★`make e2e` の実行は重ねられない**（専用ポートの共有＝`D-548`） |
| 依存 | **`M24-01` の完了**（済＝**D-553**）／ **`M24-02` の完了**（済＝**D-562**）。**★★`M24-02` が一覧のフィルタ欄と共通部品を動かした直後である**（§1.1.1） |
| CHANGE | **`CHANGE-134`**（**設計卓が起票済み。製造は番号を消費しない**）。影響設計書＝**`DES-005`**（§5.4 / §5.6 / §5.8） |
| マイグレ | **0 本の見込み**（本サブはフロントの見せ方である）。**★消費するなら次は `000080`** |
| 想定所要時間 | 240〜300 分 |
| 作成者・作成日 | 設計担当 Claude（設計卓・M24 期）/ 2026-08-26 |

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| 1.1.0 | 2026-08-26 | **★★開発者確認事項 2 件が決着し、確認事項が 0 件になった**（**D-565**）。 **★★`SM-018` はクラスタ B からクラスタ A へ移る**——**開発者の説明で機序が判明した**〔逐語＝**「子ウィンドウが小さくて、表示できるレシピが短い。ホバー表示は付けているが、やはり一発で見れるのと比べて、確認に時間がかかる」** ／ **「比較画面ではなく、一覧画面から選ぶ分には不満はない」**〕。**⇒ これは `SM-014` と同型であり、レシピが省略されて読めないという同じ根である。§4.1 の適用範囲を 3 面から 4 面へ広げた。** **★★あわせて ledger の要約が原文を切っていたことが分かった**——**原文（`Memo_Someday.txt` line 59）は「または「コンボを追加」ボタン廃止」であり、ledger は「ボタ」で切れていた。設計卓は §11 でそれをそのまま写し、「廃止」を落としていた**（計測点 **M-82**）。 **★★`SM-089` は一覧にも出す。設計卓の暫定案「詳細だけ」は撤回する**——**根拠にした「一覧は縦幅を争っている」が開発者の逐語で崩れた**〔**「縦幅についてはコンボをみたいのにフィルター等により圧迫される事を嫌がっただけで、コンボ自体の情報量が増えてもそこまで問題視しない」**〕。**⇒ 嫌われていたのはフィルタ等による圧迫であって、コンボ自体の情報量ではない。** **★§4.7 のモックへ「既定をどちらにするかを開発者が選べる形で出す」を足した**（**開発者の逐語＝「モック的に見てみたくはある」**）。 |
| 1.0.0 | 2026-08-26 | 初版（**D-563**） |

---

## 1. 背景と目的

### 1.1 扱う 9 項目——**3 クラスタに畳める**（**★2026-08-26 更新＝A が 6 件、B が 2 件になった**）

**ledger の正本は `docs/progress/M24-RESEARCH-01-report.md` §2 / §3 である。以下は要旨であり、原文はそちらを見ること。**

| クラスタ | ID | memo の要求（要旨） | 面 |
|---|---|---|---|
| **A. レシピの見せ方**（★本サブの核心） | **`SM-021`** | **比較の表示スタイルを切替可に**（例＝レシピを縦に表示） | `/compare` |
| | **`SM-022`** | **詳細のレシピも縦表示できるように**（ステップごと改行） | `/combos/:id` |
| | **`SM-096`** | **一定以上の長さのコンボ・セットプレイを入れると画面が崩れる**（memo が「レシピ縦表示で解決余地」と注記） | `/combos`・`/combos/:id` |
| | **`SM-130`** | **一覧で一発で全ルート見えるようにしたい。始動技よりレシピが見たい**（レシピ列はあるが**行内で truncate される**） | `/combos` |
| | **`SM-014`** | **比較のルート省略＋ツールチップが見にくい**（行を広げて全部表示したい） | `/compare` |
| | **★★`SM-018`** | **比較のコンボ追加の子ウィンドウが小さく、表示できるレシピが短い**（**★2026-08-26 に機序が判明し、クラスタ B から移した**＝§1.1.4） | `/compare` の追加モーダル |
| **B. 比較画面** | **`SM-003`** | **比較中コンボの詳細画面へ遷移できるように** | `/compare` → `/combos/:id` |
| | **`SM-061`** | **比較でキャラ名も出したい**（複数キャラのコンボを並べたときに分からなくなる） | `/compare` |
| **C. 情報の足し** | **`SM-089`** | **コンボのメモの 1 行目をレシピの上に薄く出したい** | `/combos/:id`・`/combos`（**★2026-08-26 確定＝両方に出す**） |
| | **`SM-097`** | **起き攻めのノーゲージ版に「ノーゲージ」とラベルを付けたい** | `/combos/:id`・`/combos` |

**★★クラスタ A の 6 件は「1 つの根」である。** **ledger 自身が `SM-021` / `SM-022` / `SM-096` / `SM-130` を同根と書いており、`SM-014` も同じ症状（省略されて読めない）である。** **★`SM-018` は 2026-08-26 に開発者の説明で機序が判明し、同じ根と分かった**（§1.1.4）。

### 1.1.1 ★★`M24-02` が同じ面を触った直後である

| 何が変わったか | 本サブへの影響 |
|---|---|
| **キャラ選択が全面で共通部品 `CharacterSelector` に揃った** | **★`SM-061`（比較でキャラ名）と `SM-018`（コンボ追加モーダル）は、その直後の面である** |
| **一覧のフィルタ欄が折りたためるようになり、要約ピルが出るようになった** | **★`SM-130`（一覧で全ルート）は同じ画面の縦幅を争う。フィルタを畳んだぶんの余白をレシピが食う形になる** |
| **フィルタの表示列カスタマイズに「セットプレイ数」が増えた**（`M24-01`） | 列カスタマイズの項目が増えている |

**⇒ 上記は `DES-005` §4.3 / §5.4 の as-built である。実装の現物は §3.3 で確認すること。**

### 1.1.2 ★★設計書が 6 つの選択肢を並べたまま、選ばれていない

**`DES-005` §5.4 の「ルート（レシピ文字列）」の項に、省略表示の方式が 6 つ列挙されている。**

> 末尾省略＋ホバー全文表示 ／ 前方優先表示で後方を省略 ／ 始動技＋締め技を残し中略 ／ 行高さ可変ラップ表示 ／ 一覧と詳細でレシピ表示の詳しさを切り替えるトグル ／ 上記の組み合わせ（例：通常は末尾省略、トグルで全文表示モードへ切替）

**★★同じ項に逐語でこう書いてある**——**「具体的な省略方式は実装フェーズで決定する」。**

**⇒ これは「選択肢の列挙」であって「決定」ではない。** **★★設計書は決定を実装フェーズへ委ねており、実装は着地している。しかし、どれを採ったのかが設計書へ戻っていない**（**★設計卓は実装ソースを読めないため、どれで着地しているかも断定できない。§3.3-1 / §3.3-4 で実査する**）。

**⇒ 本 CHANGE でこれを畳む**（§4.1）。**★6 つを並べたまま残さない**——**残すと、次に同じ不満が出たときにまた 6 つから選び直すことになる。**

**★★「実装フェーズで決定する」と書いた項は、決定したら設計書へ戻すこと。** **戻さないと、設計書は永久に「未決」のままになり、as-built だけが答えを持つ。**

### 1.1.3 ★★起票時に設計卓が確かめていないこと

**設計卓は実装ソースを読めない。** 以下は `code-facts.md`（2026-08-26 / `9c42050d`）**からの引用**であり、それ以上のことは分からない。

| 対象 | code-facts が示すこと | **分からないこと** |
|---|---|---|
| `CompareTable` | Props に `combos: (ComboDetail \| undefined)[]` / `errors` / `loadings` / `ids` / `onRemove` | **★中身の構造。レシピをどう描いているか。行クリックの有無** |
| `ComboDetailRecipe` | Props は `comboId: number` の 1 つだけ | **★レシピの取得と描画をどこでやっているか** |
| `AddComboToCompareModal` | Props に `open` / `currentIds` / `defaultCharacterId` / `onAdd` / `onOpenChange` | **★レシピをどう描いているか。モーダルの幅**（§1.1.4 で機序は判明したが、現物は読めていない） |
| `ComboTableRow` | Props に `combo: ComboSummary` / `visibility: ColumnVisibility` ほか | **★レシピ列の truncate の実装** |

**⇒ 上記を断定形として扱わないこと。§3.3 で 1 件ずつ実査し、食い違ったら報告すること**（計測点 `M-77` の再発防止）。

### 1.1.4 ★★`SM-018` の機序が判明した（2026-08-26・開発者回答）

**★★開発者の逐語:**

> **「比較画面のコンボ追加ボタンを押した後に出てくる子ウィンドウについて、子ウィンドウが小さくて、表示できるレシピが短い。ホバー表示は付けているが、やはり一発で見れるのと比べて、確認に時間がかかることを言っているように見える。比較画面ではなく、一覧画面から選ぶ分には不満はない。」**

**⇒ 「使いにくい」の実体は手数ではなく、`SM-014` と同じ「省略されて読めない」である。**

| 分かったこと | 本サブへの影響 |
|---|---|
| **★★モーダル内の候補のレシピが短くしか出ていない** | **★§4.1 の全文表示モードの規則を、このモーダルにも通す**（適用範囲が 3 面 → 4 面） |
| **★ホバーで補っているが一発で見えない** | **`SM-014` と同型。★全文表示モードのときはツールチップを出さない**（§4.1） |
| **★★「一覧画面から選ぶ分には不満はない」** | **★一覧は幅が足りている。⇒ 狭さが効いているのはモーダルの側である** |

**★★設計卓の暫定案「手数を減らす」は撤回する。** **手数は機序ではなかった。**

#### 1.1.4.1 ★★ledger の要約が原文を切っていた（計測点 `M-82`）

| | 逐語 |
|---|---|
| **原文**（`docs/human-notes/Memo_Someday.txt` line 59） | **「・[未] 比較機能のコンボ追加が使いにくい（子ウィンドウ見直し、または「コンボを追加」ボタン廃止）」** |
| **ledger の要約列**（`M24-RESEARCH-01-report.md`:210） | 「比較機能のコンボ追加が使いにくい（子ウィンドウ見直し、または「コンボを追加」ボタ」 |

**★★ledger は「ボタ」で切れている。** **設計卓は §11 でそれをそのまま写し、「廃止」を落としていた。**

**⇒ 落とした結果、「ボタンを足す」とも読める形になり、暫定案が「手数を減らす」＝ボタンを増やす方向へ寄りかけた。** **★原文は正反対（廃止）である。**

**★★教訓＝要約列から確認事項を立てない。原文へ当たる。** **`M24-RESEARCH-01-report.md` §2 / §3 は「ledger の正本」だが、要約列は原文ではない。**

#### 1.1.4.2 ★「ボタン廃止」は採らない

**★理由 2 つ。**

1. **★★開発者が 2026-08-26 に「子ウィンドウ側の問題」として説明している。** **⇒ 原文の 2 案のうち、開発者自身が前者を選んでいる。**
2. **廃止すると比較画面から追加できなくなり、別の不便を生む。**

**★ただし「一覧画面から選ぶ分には不満はない」は残しておく価値がある**——**★一覧から比較へ入れる導線が既にあるなら、モーダルは「狭い場所での妥協」であって唯一の経路ではない。** **⇒ §3.3-9 で一覧からの導線の有無を確かめること。**

---

### 1.2 目的

**「長いレシピが読めない」を終わらせる。**

**★★4 面（一覧・詳細・比較・比較の追加モーダル）で同じ不満が別々に記録されている。** **⇒ 面ごとに直すと、4 通りの見せ方ができて次の不満を生む。** **1 つの規則を決めて 4 面へ通す。**

### 1.3 ★★このサブで作らないもの（スコープ外）

| # | 作らないもの | 理由 |
|---|---|---|
| **1** | **レシピ文字列の生成規則の変更** | **`DES-004`（内部表現仕様）の領域であり、`recipe_cache` の中身は変えない。** 本サブは**見せ方**だけを変える |
| **2** | **`recipe_cache` の再計算・スキーマ変更** | 同上。**★契約 F-1 の凍結パッケージ（`internal/service/notation/` / `internal/service/preset/`）に触れない** |
| **3** | **i18n（ラベルの英語化）** | `M24-07` の手番。**★`SM-097` のラベルは日本語のまま足す**（`constants/oki.ts` の i18n 化は followup `filter-value-labels-hardcoded-ja` ＝ `M24-07`） |
| **4** | **`queryKey` の綴りの統一** | `M24-08` の手番 |
| **5** | **エディタ（新規登録・編集）のレシピ表示** | **★本サブは「読む面」だけである。** 編集中の表示は `M24-04` の面 |
| **6** | **セットプレイのレシピ表示の作り直し** | **★`SM-096` は「コンボ、セットプレイ」と書いているが、セットプレイの一覧・詳細の構造は M23 の成果物である。** **⇒ 崩れの機序が共通なら共通の手で直る。共通でないなら報告する**（§3.3-6） |
| **7** | **`M24-02` が作った折りたたみ・要約ピルの作り直し** | 直後の成果物である。**★形式を借りるのは可** |

---

## 2. 成果物

### 2.1 作成/修正するファイル

**★本一覧は「予見できた範囲」であって禁止列ではない。一覧に無いファイルでも §2.2 の禁止列に当たらないなら触ってよい。ただし触った理由を完了報告に書くこと。**

**★★以下の「面」は §3.3 の実査で確定する。設計卓は現物を読めないため、断定形で指定していない**（テンプレ §2.1 の記入指針）。

| ファイル | 内容 |
|---|---|
| **レシピ表示の共通部品**（**★新設の見込み。§3.3-2 で既存の有無を確認**） | §4.1。**★4 面が同じ規則を通るための 1 か所** |
| **一覧のレシピ列**（`ComboTableRow.tsx` 相当） | §4.1（`SM-130` / `SM-096`） |
| **詳細のレシピ**（`ComboDetailRecipe.tsx` 相当） | §4.1（`SM-022`） ／ §4.4（`SM-089`） |
| **比較表**（`CompareTable.tsx` 相当） | §4.1（`SM-021` / `SM-014`） ／ §4.2（`SM-003` / `SM-061`） |
| **比較のコンボ追加モーダル**（`AddComboToCompareModal.tsx` 相当） | §4.1 ／ §4.3（`SM-018`。**★★クラスタ A の 4 面目である**） |
| **起き攻めラベル**（`constants/oki.ts` 相当 ＋ `okiOptionLabel`） | §4.5（`SM-097`） |
| **`web/src/locales/{ja,en}.json`** | 新規キーのみ。**★既存キーの整理はしない**（§1.3-3） |
| **`web/CLAUDE.md` §1 の台帳** | §4.1 で新キーを作る場合に登録（**★`CHANGE-134` がその「CHANGE 経由」を満たす**） |
| **テスト**（純粋関数・コンポーネント・E2E） | §5 |
| **モック HTML** `docs/progress/M24-03-mock/` | §4.7。**★Plan Mode の提示に含める** |
| **完了報告** `docs/progress/M24-03-completion-report.md` | §7.5 |
| **設計伝達レポート** `docs/handover/design-reports/2026MMDD-m24-03-design-exceptions.md` | `/design_handover_report` |

### 2.2 変更しないもの（原則）

| 対象 | 何を守るか | 根拠 |
|---|---|---|
| **`internal/` ／ `cmd/` ／ `migrations/`** | **★分岐 A で着地させる。差分 0 バイトを目指す** | §2.3 の例外条項 |
| **`internal/service/notation/` ／ `internal/service/preset/`** | **契約 F-1 の凍結パッケージ。`recipe_cache` の計算に触れない** | M21 契約 ／ §1.3-1 |
| **`DES-005` §4.3.1 の解決順（5 段）** | **1 文字も変えない** | `CHANGE-130` |
| **`DES-005` §4.3 の並び順（`name_ja` 固定）** | **1 文字も変えない** | `CHANGE-133` |
| **`M24-02` の折りたたみ・要約ピル** | 直後の成果物。**形式を借りるのは可、作り直しは不可** | §1.3-7 |
| **`M15-05` の技名 1 行サマリ** | **★★これは「レシピ由来」であってメモ由来ではない**（ledger `SM-089` の注記）。**`SM-089` が足すメモ 1 行目と混ざらないこと** | §4.4 |
| **`/trash` 系** | M23 の成果物 | `M24-overview` |
| **`check-*.sh` の判定ロジック** | **★検査を緩めて通さない** | `M24-01` §7-7 |

**★先行サブの契約テストを落とさない。** ただし**本サブが動かす基準値そのもの**（列数・表示項目数など）は、母集合を変えずに後状態へ更新してよい。

### 2.3 例外条項

**§2.2 の「`internal/` を触らない」を破ってよいのは、次の 3 条件がすべて成立するときだけである。**

1. **見せ方の変更だけでは要求を満たせないことを、実測で示せる**（例＝一覧の応答にレシピの全文が載っておらず、詳細を引かないと出せない）。
2. **`DES-002` §4.2 の経路表に載る既存 API の挙動を変えない**（新設のみ。既存の変更なら止めて報告）。
3. **設計卓へ報告し、`CHANGE-134` の射程へ `DES-002` §4.2 を加える判断を仰ぐ。**

**★3 条件のどれか 1 つでも欠けたら、直さずに報告すること。**

### 2.4 ★並列性

**`M24-09c` と触る面は交わらない**（本サブは `web/src/`、`M24-09c` は `web/e2e/` と E2E ハーネス）。

**★★`M24-02` とは並列にしない**——**`/combos` の同じ表と同じ共通部品に触るためである。** **`M24-02` は完了済みなので、実際には直列で済む。**

**★`make e2e` は重ねられない**（専用ポート 47390 / 5273 の共有＝`D-548`）。**★各回の実行直前にポートが空いていることを確認し、その出力を完了報告へ載せること。**

---

## 3. 前提条件

### 3.1 必読ドキュメント

- **`DES-005` §4.3**（共通部品。**★`M24-02` で改訂されたばかり**）／ **§5.4**（コンボ一覧。**★レシピの省略表示の 6 選択肢がここにある**）／ **§5.6**（コンボ詳細）／ **§5.8**（コンボ比較）
- **`DES-004`**（内部表現・レシピ文字列。**★本サブは中身を変えない。読むだけ**）
- **`web/CLAUDE.md` §1**（ブラウザストレージ台帳）
- **`docs/handover/code-facts.md`**（2026-08-26 / `9c42050d`）

### 3.2 参照不要

- 他サブの指示書・チェックリスト（`D-217`）
- `DES-002` §4.2（分岐 A で着地する見込み。§2.3 が発動したときだけ読む）

### 3.3 ★★着手前の確認（Plan Mode 必須）

**★以下はすべて実査項目である。§1.1 / §1.1.1 / §1.1.3 に書いた内容も含めて、設計卓が現物で裏を取ったものは 1 つも無い。**

**★★実査は 2 系統ある**（`M24-overview` §2.3）——**(I) その症状が現在も成立するか** ／ **(II) 触る面がどこか。**

| # | 確かめること | 系統 | なぜ要るか |
|---|---|---|---|
| **1** | **★4 面（一覧・詳細・比較・比較の追加モーダル）のレシピが、現在どう描かれているか。** truncate / ツールチップ / 折返しの実装 | **II** | **★4 面が別々の実装なら、まず 1 か所へ寄せる話になる。共有していれば 1 か所を直せば済む** |
| **2** | **レシピ表示の共通部品が既に在るか** | **II** | **★在れば作らない。無ければ作る**（§4.1） |
| **3** | **★★`SM-096`（長いコンボで画面が崩れる）が現在も起きるか。** **崩れる閾値（ステップ数 or 文字数）を測ること** | **I** | **★★「縦表示で直る」は memo の見立てであって機序ではない。** **崩れの実体が横溢れなら縦表示で直る。別の原因（テーブルの列幅計算・overflow の設定）なら直らない。** **⇒ 測ってから手を選ぶ** |
| **4** | **`SM-130`（一覧で全ルート）が現在どうなっているか。** レシピ列の有無・truncate の有無・`title` 属性の有無 | **I** | ledger は「列自体はある／行内で truncate／`title` には全文」と書いている。**裏取り** |
| **5** | **`SM-014`（比較のツールチップが見にくい）の実体** | **I** | **★「見にくい」の実体を測る。** 表示幅か・ホバーの挙動か・全文が入っていないのか |
| **6** | **★セットプレイのレシピ表示が、コンボと同じ部品を通っているか** | **II** | **★`SM-096` は「コンボ、セットプレイ」と書いている。** **共通なら共通の手で直る。共通でないなら報告する**（§1.3-6） |
| **7** | **`SM-003`（比較 → 詳細への遷移）が現在無いことの確認** | **I** | ledger の裏取り |
| **8** | **`SM-061`（比較でキャラ名）が現在無いことの確認** | **I** | 同上。**★`M24-02` でキャラ選択が共通部品に揃った直後である** |
| **9** | **★★`SM-018`——モーダル内の候補のレシピが現在どう出ているか**〔**何文字まで出るか ／ ホバーの有無 ／ モーダルの幅**〕。**★あわせて「一覧から比較へ入れる導線」が既に在るかを確かめること** | **I** | **★2026-08-26 更新＝機序が判明したため、問いを「手数」から「狭さ」へ変えた**（§1.1.4）。**★★一覧では不満が無いと開発者が述べている。⇒ 一覧とモーダルで何文字ぶん違うかを数えると、狭さが効いていることを示せる**（`D-551`＝母数を添える） |
| **10** | **`SM-089`（メモの 1 行目）——`M15-05` の技名 1 行サマリが現在どこに出ているか** | **II** | **★★混ざらないことが最重要である。** ledger が「同サマリはレシピ由来でメモ由来ではない」と明記している |
| **11** | **`SM-097`——`OKI_NO_GAUGE_LABEL` の定義箇所と参照数** | **I** | **ledger は「定義 1 箇所・参照 0 件」と書いている。裏取り。** **★参照が 0 なら、足すだけで済む** |
| **12** | **起き攻めのラベルを組み立てている関数**（`okiOptionLabel` 相当）**の全数と、その呼び元** | **II** | **★1 か所で組み立てているなら 1 か所を直せば全面に効く** |
| **13** | **`M24-02` が新設したブラウザストレージキーと、台帳の現在の内容** | **II** | **★§4.1 で新キーを作るなら、同じ形に倣う** |
| **14** | **★一覧の 1 行の高さと、画面に収まる行数**（現状の基準値） | **I** | **★★`SM-130` と `SM-089` を満たすと行が高くなる。** **⇒ 基準値が無いと「増えた」を判定できない**（`D-551`＝母数を添える）。 **★2026-08-26 更新——これは「止めるための門」ではない。** **開発者は「コンボ自体の情報量が増えてもそこまで問題視しない」と述べている。** **⇒ §4.7 のモックへ添えて、開発者が既定を選ぶための材料にする** |

---

## 4. 詳細仕様

### 4.1 ★★レシピの見せ方——**6 つの選択肢を 1 つへ畳む**（`SM-021` / `SM-022` / `SM-096` / `SM-130` / `SM-014`）

**★★採るのは `DES-005` §5.4 の 6 番目「組み合わせ（通常は末尾省略、トグルで全文表示モードへ切替）」である。**

**★これは設計卓の発明ではない**——**`SM-021` が「表示スタイルを切替可に」と、切替そのものを名指ししている。**

| 論点 | 決めたこと |
|---|---|
| **既定** | **★現状のまま**（**★現状が何であるかは §3.3-1 / §3.3-4 の実査で確定する。設計卓は「末尾省略＋ホバー」と断定しない**）。 **★★2026-08-26 更新——既定を変えない理由は弱くなった。** **`DES-005` §5.4 の懸念「一覧の縦幅が大きくなり、スクロール量が増える」は、開発者の逐語で重みが下がった**〔**「コンボ自体の情報量が増えてもそこまで問題視しない」**〕。 **⇒ 既定は現状のまま実装するが、§4.7 のモックで既定と全文の両方を出し、開発者が既定を選べる形にすること**（§3.3-14 の基準値を添える） |
| **切替** | **「全文表示モード」へのトグル。** ステップごとに改行して縦に展開する |
| **★★適用範囲** | **★2026-08-26 更新＝4 面である**——**一覧・詳細・比較 ＋ 比較のコンボ追加モーダル**（§1.1.4）。**★1 つの規則を 4 面へ通す。面ごとに違う見せ方を作らない** |
| **★★状態の保持** | **保持する。★★4 面で 1 つの値を共有する**（`E-76`＝同じ情報を 2 か所に持たない）。**★保持先は実査で決める**——**新キーが要るなら `web/CLAUDE.md` §1 台帳へ登録すること**（`CLAUDE.md` §10.X。**本 CHANGE がその要求を満たしている**） |
| **★詳細画面の既定** | **★詳細は「全文表示」を既定にしてよい**（`SM-022` の要求は詳細で縦に読みたい、である）。**★ただし 4 面共通のトグルとは別に「面ごとの既定」を持つ形にすること**——**トグルを 2 つ作らない** |
| **★ツールチップ** | **全文表示モードのときはツールチップを出さない**（同じ情報が 2 か所に出る）。**★`SM-014`（ツールチップが見にくい）はこれで解ける** |

**★★`SM-096`（崩れ）は §3.3-3 の実測で機序を確かめてから手を選ぶこと。**

- **横溢れが原因なら、全文表示モードで直る。**
- **★別の原因（列幅計算・`overflow` の設定・親要素の制約）なら、全文表示モードでは直らない。** **⇒ 直らない場合は、崩れの機序と必要な手を報告すること。本サブで直すかは設計卓が決める。**

**★★「縦表示で解決余地」は memo の見立てであって機序ではない。** **測らずに実装しないこと。**

### 4.2 ★比較画面の 2 件（`SM-003` / `SM-061`）

| 項目 | 決めたこと |
|---|---|
| **`SM-003`（詳細へ遷移）** | **比較表から各コンボの詳細へ行ける導線を 1 つ置く。** **★形は製造の裁量**（列ヘッダのリンク ／ 行クリック 等）。**★ただし「削除」と取り違えられない位置にすること**（`M23-07` の教訓＝**取り違えると利用者のデータが消える操作の隣に置かない**） |
| **`SM-061`（キャラ名）** | **比較表にキャラ名を出す。** **★出す位置は各コンボ列の見出し**（`DES-005` §5.8 の「比較対象コンボ一覧」または比較表の 1 行目）。**★キャラ名の値は `name_ja`**（`DES-005` §4.3 の並び順と同じ語彙。**第 2 の語彙を作らない**） |

### 4.3 ★★`SM-018`（比較のコンボ追加）——**2026-08-26 確定。クラスタ A の一部として直す**

**★★確認事項 1 は決着した。回答待ちは無い。**

| 論点 | 決めたこと |
|---|---|
| **★★やること** | **モーダル内の候補のレシピを、§4.1 の全文表示モードの規則に乗せる**（§1.1.4） |
| **★手数は機序ではなかった** | **設計卓の暫定案「手数を減らす」は撤回する** |
| **★「コンボを追加」ボタン廃止** | **採らない**（§1.1.4.2） |
| **★モーダルの狭さそのもの** | **★幅・高さを広げてよい。★ただし §4.1 の規則より優先しない**——**全文表示モードで読めるようになるなら、それが第一手である** |
| **★ツールチップ** | **全文表示モードのときは出さない**（§4.1 と同じ） |

**★★§3.3-9 は「手数を数える」から「狭さの実体を測る」へ変わった**（§3.3 の表を参照）。

### 4.4 ★`SM-089`（メモの 1 行目をレシピの上に薄く）——**★2026-08-26 確定**

| 論点 | 決めたこと |
|---|---|
| **★★対象** | **★2026-08-26 確定＝詳細 `/combos/:id` と一覧 `/combos` の両方。** **★設計卓の暫定案「詳細だけ」は撤回した**——**根拠にした「一覧は縦幅を争っている」が開発者の逐語で崩れた**〔**「縦幅についてはコンボをみたいのにフィルター等により圧迫される事を嫌がっただけで、コンボ自体の情報量が増えてもそこまで問題視しない」**〕。**⇒ 嫌われていたのはフィルタ等による圧迫であって、コンボ自体の情報量ではない** |
| **★★混ざらないこと** | **`M15-05` の技名 1 行サマリは「レシピ由来」であってメモ由来ではない**（ledger の注記）。**⇒ 同じ場所に 2 種類の 1 行が並ぶことになる。** **★どちらがどちらか読み分けられる形にすること** |
| **メモが空のとき** | **何も出さない**（空の行を作らない） |
| **1 行目の取り方** | **最初の改行まで。** **★長い場合は省略する**（レシピの上に長い行が来ると本末転倒である） |

### 4.5 ★`SM-097`（ノーゲージのラベル）

**`OKI_NO_GAUGE_LABEL = "ノーゲージ"` が定義されているが参照 0 件である**（ledger の実査。**§3.3-11 で裏取り**）。

| 論点 | 決めたこと |
|---|---|
| **やること** | **ノーゲージ版の起き攻めオプションのラベルに「ノーゲージ」を付ける** |
| **★どこで組み立てるか** | **既存のラベル組み立て関数（`okiOptionLabel` 相当）の中で 1 回だけ**（§3.3-12）。**★呼び元ごとに足さない** |
| **★i18n** | **日本語のまま足す。** **`constants/oki.ts` の i18n 化は `M24-07` の手番**（followup `filter-value-labels-hardcoded-ja`）。**★本サブで一部だけ英語化すると、画面内で 2 系統が混ざる** |
| **★語彙** | **`constants/oki.ts` が「起き攻めラベルの単一の正典」と自ら宣言している。** **⇒ 第 2 の語彙を作らない** |

### 4.6 i18n

- **新規に追加する文言は `web/src/locales/{ja,en}.json` へ入れること。**
- **★既存キーの整理・語彙の見直しはしない**（`M24-07` の手番）。
- **★`constants/oki.ts` / `constants/combo-list.ts` のハードコード日本語には触らない**（同上）。

### 4.7 ★モック HTML（`M24-overview` §1.5）

**次の 4 つについて、変更前と変更後を並べた単一 HTML を `docs/progress/M24-03-mock/` へ置くこと。**

1. **レシピの全文表示モード**（§4.1。**★★一覧・詳細・比較 ＋ 比較の追加モーダルの 4 面すべて。既定と全文の両方**）。 **★★あわせて §3.3-14 の基準値（1 行の高さ・画面に収まる行数）を数で添えること**——**開発者が「既定をどちらにするか」を選べる形にする**（**開発者の逐語＝「モック的に見てみたくはある」**）
2. **比較表のキャラ名と詳細への導線**（§4.2）
3. **メモ 1 行目の表示**（§4.4。**★`M15-05` の技名サマリと並んだ状態を出すこと。★一覧と詳細の両方**）
4. **ノーゲージのラベル**（§4.5）

**★★Plan Mode の提示に含めること。実装後ではない。** **★作り込まないこと。**

**★§3.3-3 の実測で「崩れが起きない」と分かった場合は、その項目のモックは「現状の写し」1 枚でよい**（先例＝`M24-01` の `SM-002`）。

---

## 5. テスト要件

### 5.1 追加するテスト

| 層 | 内容 |
|---|---|
| **純粋関数** | **★必須**。レシピの省略・分割（ステップごとの改行位置）／ メモ 1 行目の取り出し（空・改行なし・長文）／ ノーゲージ判定 |
| **コンポーネント** | **★★全文表示モードのトグルが 4 面で同じ値を見ていること**（§4.1）／ **★メモ 1 行目と技名サマリが読み分けられること**（§4.4） |
| **E2E** | **★5 ケース**——**(1) 一覧でモードを切り替えると全ルートが見えること ／ (2) 詳細でレシピが縦に読めること ／ (3) 比較から詳細へ行けること ／ (4) 比較にキャラ名が出ていること ／ (5) ★比較の追加モーダルでもモードが効いていること**（§4.3） |

**★E2E の共通の下ごしらえは `web/e2e/support/` へ寄せること**（`D-553` が運用の型として承認済み。**★同じ数行を複数 spec へ複製しない**＝`E-232`）。

**★★`M24-02` の教訓が効く**——**表示の DOM 種別を変えると `toHaveValue` / `selectOption` ベースの assert が壊れる。** **⇒ レシピの描画方法を変える前に、レシピ文字列を assert している箇所を全数 grep すること。**

### 5.2 既存テストの扱い

- **`DES-005` §4.3.1 の解決順を守るテストを落とさない**（`M24-01` の成果物）。
- **`DES-005` §4.3 の並び順を守るテストを落とさない**（`M24-02` の成果物）。
- **★表示項目数をベタ書きしているテストを見つけたら、数ではなく「定義した項目がすべて出る」形へ書き直すこと**（`M24-01` §7-8）。

### 5.3 ★★破壊確認（**4 件・必須**）

**★破壊確認を書く前に 2 つを確かめること**（計測点 `M-78` ／ playbook §4.32）——**(a) 壊す対象が実在するか** ／ **(b) ★その壊し方で本当に観測が変わるか**〔`M24-02` の破壊確認 3 は「値を読むだけ」の壊し方で 33 件すべて緑だった〕。

| # | 壊すもの | 期待 | 同じ層で書けているか |
|---|---|---|---|
| **1** | **全文表示モードのトグルを「常に既定」へ固定する** | **E2E (1) と (2) が赤くなる** | **★どちらもフロント側。層をまたいでいない** |
| **2** | **4 面が見る値を面ごとに分ける**（共有をやめる） | **★コンポーネントテスト（4 面で同じ値）が赤くなる** | 同上 |
| **3** | **メモ 1 行目の取り出しを「全文」へ変える** | **★純粋関数のテストが赤くなる** | 同上 |
| **4** | **ノーゲージのラベル付与を外す** | **★ノーゲージ判定のテストが赤くなり、該当する表示のテストも赤くなる** | 同上 |

**★赤くなる経路を数えること**——**赤くならない経路があるなら、その経路は守られていない。**

---

## 6. レビュー観点（別ファイル参照）

`docs/instructions/reviews/M24-03-review-checklist.md`

---

## 7. 完了条件（Definition of Done）

### 7.1 機能要件

1. **§3.3 の実査 14 件が実施され、結果が報告されている**（**★母数の要る項目は母数付きで**）。
2. **§4.1 のレシピ表示が 4 面で 1 つの規則を通っている**（**★比較の追加モーダルを含む**）。
3. **§4.2 / §4.4 / §4.5 が実装されている**（**★§3.3 で「既に解消していた」と分かった項目は、実装せず成立をテストで固定して報告する**）。
4. **§4.3（`SM-018`）が §4.1 の規則に乗って実装されている**（**★2026-08-26 に確認事項 1 が決着し、回答待ちは無い**）。
5. **★§3.3-3 で崩れの機序が測られ、全文表示モードで直るか直らないかが報告されている。**
6. **§4.7 のモック HTML が Plan Mode で提示されている。**

### 7.2 自己テスト結果

- `go test ./... -count=1`（**★本サブは Go を触らない見込みなので、着手前と同じであることの対照**）
- `cd web && pnpm test`
- `make e2e`（**★実行直前にポートが空いていることを確認し、その出力も載せる**）
- **破壊確認 4 件**（§5.3）。**実施コマンドと出力を載せること。**

**★★基準値は「変更を入れる前」に、切らずに採ること**（`M24-02` §7-2 の教訓＝**採取コマンドにパイプで切り詰めを入れない**）。

### 7.3 品質チェック

- `bash scripts/check-artifact-integrity.sh`（**★1 本目に回す**）
- `bash scripts/check-browser-storage-keys.sh`（**★新キーを作った場合は必ず**）
- `bash scripts/check-md-emphasis.sh` ／ `bash scripts/check-doc-refs.sh` ／ `bash scripts/check-progress-log-index.sh` ／ `bash scripts/check-enum-sync.sh`
- **★`check-progress-log-index.sh` は偽の緑を返しうる**（followup §AH）。**緑が出ても索引行を目で確かめること。**

### 7.4 ドキュメント

- **`docs/progress/progress-log.md` への追記**（製造担当が行う。`CLAUDE.md` §8・playbook §11.1〜§11.3）。**指示書が明示的に求めていなくても追記する。**

#### 7.4.1 ★CHANGE 要否の判定（**as-built で判定する**）

**`CHANGE-134` は起票済みである。** **★以下は「射程が広がったか」の判定である。1 件ずつ判定し、結果を完了報告に書くこと。**

| # | 条件 | 該当したら |
|---|---|---|
| 1 | **`DES-002` §4.2 の経路表に載る API を新設・変更したか**（§2.3 の例外条項） | 設計卓へ報告。`CHANGE-134` の射程へ加える |
| 2 | `DES-003` のスキーマに触れたか | 同上 |
| 3 | **`DES-004` のレシピ文字列の生成規則に触れたか** | **★同上。本サブは触らない見込み** |
| 4 | `DES-006` の VAL の判定結果が変わったか | 同上 |
| 5 | **★`SUPP-001` §5.2 のパッケージ構成に増減があるか** | 同上 |
| 6 | **★`SUPP-001` §5.5 のテスト方針が変わるか** | 同上 |
| **7** | **★★ブラウザストレージのキーを新設したか**（§4.1） | **`web/CLAUDE.md` §1 台帳へ登録し、`DES-005` §5.4 へも書く。★`CHANGE-134` の射程内である** |
| **8** | **★マイグレーションを消費したか**（見込みは 0 本） | **★消費したらボード §2.2 の連番セルの更新が要る。設計卓へ報告** |

### 7.5 完了報告

`docs/progress/M24-03-completion-report.md`。**★工程順として「まだ無い」ものを過去形で書かないこと**（`M24-09b` §7-2）。

**★★件数を本文へ書くときは、その場で数え直したコマンドを併記すること**（`M24-02` §7-3 の教訓＝**「N 件だけである」は判定の結果から数える**）。

---

## 8. 参照ドキュメント

| ID | パス | 用途 |
|---|---|---|
| `DES-005` §4.3 / §5.4 / §5.6 / §5.8 | `docs/design/05-screen-design.md` | 画面仕様。**★§4.3 と §5.4 は `M24-01` / `M24-02` で改訂されたばかり** |
| `DES-004` | `docs/design/04-notation-spec.md` | レシピ文字列。**★読むだけ** |
| `web/CLAUDE.md` §1 | — | ブラウザストレージ台帳 |
| `code-facts.md` | `docs/handover/code-facts.md` | 2026-08-26 / `9c42050d` |
| ledger | `docs/progress/M24-RESEARCH-01-report.md` §2 / §3 | **9 項目の索引。★★要約列は原文ではない**（§1.1.4.1＝`SM-018` の要約が「ボタ」で切れていた＝`M-82`）。**★原文は `docs/human-notes/Memo_Someday.txt`** |
| `CHANGE-134` | `docs/change-notes/CHANGE-134-notification.md` | 本サブの通知書 |

---

## 9. 注意事項・判断に迷ったら

### 9.1 推測で進めてはいけない事項

- **★「ボタン廃止」を採ること**（§1.1.4.2。**開発者が子ウィンドウ側の問題として説明している**）。
- **★モーダルの幅を広げることで §4.1 の規則の適用を省くこと**（§4.3。**★規則が第一手である**）。
- **`SM-096` の崩れが全文表示モードで直らなかったときの手**（§4.1。**測って報告する**）。
- **`internal/` を触ること**（§2.3 の 3 条件をすべて満たすときのみ）。
- **新規依存の追加**（`CLAUDE.md` §6）。
- **ブラウザストレージの新キーを台帳へ登録せずに使うこと**（`CLAUDE.md` §10.X）。
- **`DES-004` のレシピ文字列の生成規則の変更。**

### 9.2 推測で進めてよい事項（その旨を明示）

- **トグルの見た目・置き場所**（アイコン・ラベル）。
- **`SM-003` の導線の形**（列ヘッダのリンク ／ 行クリック 等。**★「削除」と取り違えられない位置であること**）。
- **メモ 1 行目の省略の閾値。**
- **`web/e2e/support/` へ寄せるヘルパの命名とシグネチャ。**

### 9.3 不明事項発見時の対応

**★★「その症状は既に解消していた」場合は、実装せず、成立をテストで固定して報告すること**（`M24-01` の `SM-002` / `SM-004` と同じ形）。**★これは欠陥ではない。**

**★「memo の見立てが機序と違った」場合も報告すること**（`SM-096` の「縦表示で解決余地」）。**★見立てを実装の根拠にしない。**

### 9.4 ★CHANGE 要否

§7.4.1 のとおり。**★判定した結果「射程は広がらなかった」であっても、判定したこと自体を報告に書くこと。**

---

## 10. 完了後の次ステップ

- 設計卓が受理し、**`CHANGE-134` を `DES-005` へ反映する。** **★★§5.4 の 6 選択肢は本 CHANGE で畳む。** **★★あわせて `DES-005` §5.8 へ「比較の追加モーダルも同じ規則を通る」を書く。**
- **followup の該当行を畳む。**
- **`M24-07`（i18n）へ渡すものを設計伝達レポート §4 に並べる。**

---

## 11. 開発者への確認事項

> **★★2026-08-26 決着（`D-565`）。2 件とも回答が出た。確認事項は 0 件である。**

| # | 問い | 回答 | 反映先 |
|---|---|---|---|
| **1** | **`SM-018`（比較のコンボ追加が使いにくい）の解き方** | **★★機序が判明した**——**「子ウィンドウが小さくて、表示できるレシピが短い。ホバー表示は付けているが、やはり一発で見れるのと比べて、確認に時間がかかる」** ／ **「比較画面ではなく、一覧画面から選ぶ分には不満はない」。** **⇒ `SM-014` と同型であり、クラスタ A の一部である** | §1.1.4 ／ §4.3 ／ §4.1 の適用範囲（3 面 → 4 面） ／ §3.3-9 |
| **2** | **`SM-089` を一覧にも出すか** | **★★出す。** **設計卓の暫定案「詳細だけ」は撤回した**——**「縦幅についてはコンボをみたいのにフィルター等により圧迫される事を嫌がっただけで、コンボ自体の情報量が増えてもそこまで問題視しない」。** **★あわせて「モック的に見てみたくはある」** | §4.4 ／ §4.7-1 / -3 ／ §3.3-14 |

---

*以上、指示書 M24-03 **v1.1.0**。* **★v1.0.0 からの変更＝開発者確認事項 2 件が決着し、確認事項が 0 件になった**（`D-565`）。**`SM-018` はクラスタ A へ移り、§4.1 の適用範囲が 4 面になった。`SM-089` は一覧にも出す。★設計卓の暫定案は 2 件とも撤回した。**
