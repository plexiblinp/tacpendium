# M29-overview: `α②` 用語・ラベルの統一と入出力（CSV／エクスポート／バックアップ）

| 項目 | 内容 |
|------|------|
| 文書種別 | 補足資料（overview。CHANGE 通知書対象外・自由改訂） |
| バージョン | **1.2.0**（2026-09-06・**★★`M29-01` / `M29-02` を起票した＝`D-744`**） ／ 1.1.0（2026-09-06・**★★確認事項 4 件が決着＝`D-742`**） ／ 1.0.0（2026-09-06） |
| 作成日 | 2026-09-06 |
| 作成者 | 設計担当 Claude（設計卓／フェーズ4 期） |
| 対象マイルストーン | **`M29`**（フェーズ4）。上位＝`docs/instructions/phase4-overview.md` §3 |
| 位置づけ | **`M29` のサブ構成・射程・CHANGE 見込みの正本** |
| 状態 | **★★起票済み**（2026-09-06・`D-744`）。**`docs/instructions/M29-01-vocabulary-and-labels.md` v1.0.0 ／ `docs/instructions/M29-02-io-csv-backup-draft.md` v1.0.0。★ただし `M29-01` の射程から「ステップの二義」を外した**（§2.2-4） |
| identity 数 | **★★29 と記録されているが、内訳が機械的に引けない**（§2.1。**⇒ 起票の前提が 1 つ欠けている**） |
| CHANGE 見込み | **2〜3 本**（`DES-005` が主 ／ `DES-002` §7.6（CSV フォーマット）／ `DES-006`（重大度）） |
| マイグレ見込み | **0 本** |
| 配置 | `docs/instructions/M29-overview.md`（active＝直下。フェーズ4 終了時に `docs/instructions/phase4/` へアーカイブ） |

---

## 0. 本書の確度

- **★★`identity` の内訳を、設計卓は列挙できていない**（§2.1）。**⇒ 本書の射程は「面の記述」からの再構成であり、ledger の行との 1 対 1 が取れていない。**
- **★設計卓は実装ソースを読んでいない。** 実装の事実は `code-facts.md` ／ 完了報告 ／ 設計伝達レポートからのみ引いている。**断定していない箇所は §5 の実査項目にした。**
- **★開発者判断（2026-09-06）＝「`M29` は一つの MS のままでよい」。⇒ 面で MS を割ることはしない。サブ分割は本書 §3 で提案する。**

---

## 1. 目的

**同じものを指す語が画面ごとに違う状態と、入出力の経路で「黙って落ちる」箇所を、まとめて揃える。**

**★★「まとめて」が本 MS の要点である**——`phase4-overview` §3 の逐語＝**「語彙の統一は 1 サブにまとめる。面ごとに直すと、直した先で再び割れる。」**

---

## 2. 射程

### 2.1 ★★identity の内訳が機械的に引けない（**先に読む**）

**`phase4-overview` §7.1 は `α②` 91 行の面ごとの内訳として「用語・ラベル・入出力＝29 件 → `M29`」と記録している。**

**★★しかし ledger（`M25-RESEARCH-01` §3）の `触る面` 列に「用語・ラベル・入出力」という値は存在しない。** 設計卓が `触る面` 列を全数集計した結果（フェーズ4 の 96 行）は次のとおりで、**この 29 件を再現する組み合わせが読み取れない。**

| 触る面（上位） | 件数 |
|---|---:|
| 画面・データ | 21 |
| 画面: 仮想コントローラ | 11 |
| リポジトリ運用・文書 | 8 |
| データ・seed | 7 |
| データ・画面 | 6 |
| （以下 28 種が 1〜4 件ずつ） | — |

**⇒ 「29」は `M25` が行なった派生の集計であり、`触る面` 列からは引けない。**

**★★これは `M-116` と同じ型である**〔境界のずれを `代表 ID` 欄の機械突合で見つけた件〕**——数は記録されているが、その数を構成する行が特定できない。**

| # | 帰結 |
|---|---|
| **★★1** | **本書は「29 件の一覧」を持てない。⇒ 射程は §2.2 の面の記述からの再構成である。** |
| **★★2** | **⇒ `M29-01` の最初の成果物を「29 件の内訳の実査」にする**（§3。**`M30-01` §2.1 と同じ形**）。 |
| **★3** | **★数が合わない可能性がある。⇒ 実査の結果が 29 でなかったら、`phase4-overview` §7.1 へ追随注記が要る**（§5-1）。 |

### 2.2 面の記述からの再構成（**`phase4-overview` §3 の逐語**）

**★逐語＝「語彙の割れ〔空レシピのラベルが 4 種／コピーの言い回しが 3 種／SA ゲージの単位「本」の有無／「ステップ」の二義／相手状態の語彙〕と、入出力〔CSV の取込・書出・プレビューと確定の重大度の非対称・行数上限の黙った切り捨て・バックアップ／復元・下書きの往復〕。」**

#### 語彙の割れ — 5 群

| # | 群 | 分かっていること |
|---|---|---|
| **1** | **空レシピのラベルが 4 種** | **★同じ「レシピが空」を 4 通りの語で言っている。⇒ どれかに寄せる** |
| **2** | **コピーの言い回しが 3 種** | 同上 |
| **3** | **SA ゲージの単位「本」の有無** | **★「2 本」と「2」が混在している** |
| ~~4~~ | ~~「ステップ」の二義~~ | **★★【2026-09-06 射程外＝`D-742`】フェーズ5 以降へ送る**（開発者判断）**。** **★★あわせて設計卓の理解が誤っていたので是正する**——**設計卓は「コンボのステップとウィザードのステップ」と書いたが、開発者の逐語は「move としてのステップ（前方ステップなど）と、combo の 1 工程としてのステップ」である。⇒ 衝突しているのは「移動技のステップ」と「コンボの工程」であり、ウィザードは関係ない。★設計卓が `phase4-memo` の逐語「「ステップ」の二義」を推測で埋めた**（計測点 `M-128`）**。** |
| **5** | **相手状態の語彙** | **★`M27-01` が `opponent_size` を 4 区分化し、`M27-03` が一覧の見出しを「相手の状態」にした。⇒ 着手時に as-built を読むこと** |

#### 入出力 — 4 群

| # | 群 | 分かっていること |
|---|---|---|
| **★★6** | **CSV の取込・書出で、プレビューと確定の重大度が非対称** | **★プレビューで警告だったものが確定で通る（またはその逆）。⇒ 利用者が「見たとおりに入る」と思えない** |
| **★★7** | **行数上限の黙った切り捨て** | **★★上限を超えた分がエラーにならずに落ちる。⇒ 本 MS で最も危険な 1 件である**（`FR307`＝破綻の自動断定をしない、の逆向き＝**成功の自動断定**） |
| **8** | **バックアップ／復元** | `M23` が `FR601`（データバージョン管理）とゴミ箱／復元を入れた。**⇒ 着手時に as-built を読むこと** |
| **9** | **下書きの往復** | **★`M27-02b` が仮登録・本登録の 3 状態を入れた。⇒ 同上** |

### 2.3 ★既に `M29` が行き先として挙がっている `followup`

| スラッグ | 内容 | 出所 |
|---|---|---|
| **`combo-list-filter-controls-split-across-two-widgets`** | **一覧のフィルタ 7 本のうち、ヒット種別 1 本だけが `SearchableSelect` である**（他 6 本は native `<select>`）**。★意図的な非対称であり、`M27-03` が「`M29` または フェーズ5」を割付候補に挙げた。★★いま揃えに行く理由は無い** | `M27-03` 設計伝達レポート §4-5 |

**★★これは「用語・ラベル」ではなく「部品の統一」である。⇒ 本 MS の射程に入れるかは §4-2 で確認する。**

### 2.4 ★★`M29` に入らないもの（**誤って引き込まない**）

| # | 何 | どこへ |
|---|---|---|
| **★★1** | **`csv-drive-damage-sign-flipped-across-versions`**（`drive_damage` の符号反転で、旧版の CSV を取り込むと符号が逆になる） | **★`DES-002` §7.6 への注記で足りる可能性が高い。⇒ 開発者が実害を見積もってから決める**（`followup`）**。★入出力の話だが、`M29` の射程ではなく `CHANGE` の話である可能性がある** |
| **★2** | **`FN-08` / `CL-3.5-4`（単一 HTML エクスポート）** | **★★`要判断` として据え置かれている**（`checklist` §3.5 はフェーズ4、開発者はフェーズ6 以降と説明。**時期矛盾**）**。⇒ `M29` に入れない** |
| **★3** | **`SM-147`（クラウドストレージへのエクスポート）** | **フェーズ6 以降**（原文が「かなり先でもいい」と明記） |

---

## 3. サブ構成（**★★開発者承認事項**）

**2 サブを提案する。**

| サブ | 射程 | 性質 |
|---|---|---|
| **M29-01** | **★★29 件の内訳の実査 ＋ 語彙・ラベルの統一**（§2.2 の 1〜5） | **★全面を薄く撫でる。i18n を全面で触る** |
| **M29-02** | **入出力**（§2.2 の 6〜9） | **★特定の経路を深く触る。CSV / バックアップ・復元 / 下書きの往復** |

### 3.1 ★★直列にする。順序は `M29-01` → `M29-02`

**★理由 2 つ。**

| # | 理由 |
|---|---|
| **★★1** | **語彙を先に揃えないと、入出力側で新しい文言を足すときに揃った規則に従えない。⇒ 直した先で再び割れる**（`phase4-overview` §3 の警告そのもの）。 |
| **★★2** | **`M29-01` は i18n を全面で触る。⇒ 並列にすると `M29-02` の文言変更と必ず衝突する**（`D-740` の交点表で (1) が埋まる）。 |

### 3.2 ★★`M29-01` の最初の成果物は「内訳の実査」である

**§2.1 のとおり、設計卓は 29 件を列挙できていない。⇒ 実装を直す前に、語の割れを全数で出させる。**

**★`M30-01` §2.1 と同じ形である**〔**課題そのものが「規則が読めない」なので、実測が最初の成果物になる**〕**。**

**★★ただし `M30-01` と違う点が 1 つある**——**`M30-01` は「現行の述語」を読めば答えが出るが、`M29-01` は「同じものを指す語」を人が判定する必要がある。⇒ 実査の結果に対して、開発者がどれへ寄せるかを選ぶ手番が要る**（§4-3）。

---

## 4. ★開発者への確認事項


**★★4 件すべて決着した**（2026-09-06・`D-742`）。**⇒ 決めることは残っていない。**

| # | 確認事項 | 状態 |
|---|---|---|
| ~~1~~ | ~~サブ分割 2 本の承認~~ | **★★決着＝承認**（`D-742`）。**⇒ `M29-01`（内訳の実査 ＋ 語彙・ラベル）／ `M29-02`（入出力）の 2 本で確定した** |
| ~~2~~ | ~~`combo-list-filter-controls-split-across-two-widgets` を射程に入れるか~~ | **★決着＝入れない**（`D-742`）。**⇒ フェーズ5 へ送る** |
| ~~3~~ | ~~語彙をどれへ寄せるかを誰が決めるか~~ | **★★決着＝開発者が選ぶ**（`D-742`。逐語＝「**基本は私が選びたい**」）。**⇒ `M29-01` が実査の結果とともに案を出し、開発者が選ぶ。★製造が単独で決めない。★対象は §2.2 の 1・2・3・5 の 4 群である**〔**4 は射程外になった**〕 |
| ~~4~~ | ~~「ステップ」の二義をどちら側で改名するか~~ | **★★決着＝フェーズ5 以降へ送る**（`D-742`）。**★あわせて設計卓の理解の誤りを是正した**（§2.2-4） |

以下は決着前の記述：
| # | 確認事項 | 設計卓の暫定案 |
|---|---|---|
| **★★1** | **サブ分割 2 本の承認**（§3） | **★★直列を推す**（§3.1）。**★1 本にまとめる案も在るが、`M29-01` の実査が終わるまで `M29-02` の量が読めないため、分けておくほうが止めやすい** |
| **★★2** | **`combo-list-filter-controls-split-across-two-widgets` を `M29` の射程に入れるか**（§2.3） | **★★入れないことを推す**——**「用語・ラベル」ではなく「部品の統一」であり、`M27-03` 自身が「いま揃えに行く理由は無い」と書いている。⇒ フェーズ5 へ送る** |
| **★★3** | **語彙をどれへ寄せるかを、誰が決めるか**（§3.2） | **★★開発者が選ぶ**（`roles-and-routing` ハード列＝利用者に見える語）**。⇒ `M29-01` が実査の結果とともに案を出し、開発者が選ぶ形にする。★製造が単独で決めない** |
| **★4** | **「ステップ」の二義**（§2.2-4）**をどちらの側で改名するか** | **★案なし。★★コンボのステップは `DES-003` の `combo_steps` に対応する語であり、ウィザードのステップは画面の語である。⇒ 改名するならウィザード側が安いが、利用者にどちらが自然かは開発者の判断** |

---

## 5. ★設計卓が確かめていないこと（実査項目）

| # | 確かめること |
|---|---|
| **★★1** | **★★29 件の内訳**（§2.1）**。⇒ `M29-01` の最初の成果物。★数が 29 でなかったら `phase4-overview` §7.1 へ追随注記が要る** |
| **★★2** | **語の割れの現物**（空レシピ 4 種 ／ コピー 3 種 ／ SA ゲージの「本」／「ステップ」の二義 ／ 相手状態）**。★設計卓は `web/src` を読めない** |
| **★★3** | **CSV のプレビューと確定の重大度の非対称の現物**（§2.2-6）**。★`DES-006` の重大度の定義と実装が一致しているか** |
| **★★4** | **行数上限の値と、超えたときの挙動**（§2.2-7）**。★★「黙って切り捨てる」が本当に黙っているか** |
| **★5** | **`M23` のバックアップ／復元の as-built**（§2.2-8）／ **`M27-02b` の下書きの往復の as-built**（§2.2-9） |
| **★6** | **`M27-01` / `M27-03` が相手状態の語彙をどこまで動かしたか**（§2.2-5。**`CHANGE-151` / `CHANGE-155`**） |

---

## 6. 依存関係・順序

```
M27（α② 画面品質）… ★完了。M27-01 / M27-02a / M27-02b / M27-03 とも受理済み
     │
     └─→ M29-01（内訳の実査 ＋ 語彙・ラベルの統一）… ★i18n を全面で触る
              │
              └─→ M29-02（入出力）… ★語彙が揃ってから
```

**★`M28` レーン ／ `M30` レーンとは面が違う。⇒ 並列可**（`D-740` の交点表で判定すること）。

**★★ただし `M29-01` は i18n を全面で触る。⇒ 同時期に i18n を触るサブが在るなら交点になる**〔`M30-01` / `M30-02` はボタン名やタブ名で i18n を触りうる〕**。**

---

## 7. CHANGE 見込み・採番

| サブ | CHANGE 見込み | マイグレ | 根拠 |
|---|---|---|---|
| **M29-01** | **1〜2 本** | **0 本** | **`DES-005`**（語彙の正典）**が主。★「ステップ」の二義を改名するなら `DES-003` にも及ぶ** |
| **M29-02** | **1 本** | **0 本** | **`DES-002` §7.6（CSV フォーマット）／ `DES-006`（重大度）** |

**⇒ `M29` の見込みは 2〜3 本。★次に採番するのは `157`**（**★`M26-02` が `157` を使う見込みであり、`M29` は `158` 以降になる見込み。★先付けしない**＝playbook §4.15）。

### 7.1 推奨モデル

- **`M29-01`**——**高精度実装 ＋ Plan Mode 必須**（**★i18n を全面で触る。★実査の結果に対して案を出す手番が在る**）
- **`M29-02`**——**高精度実装 ＋ Plan Mode 必須**（**★★「黙って切り捨てる」を直すと、いままで通っていた入力が通らなくなる。⇒ 既存データへの影響を先に見る**）

---

## 8. 参照

| 対象 | 何を読むか |
|---|---|
| `docs/instructions/phase4-overview.md` §3 M29 ／ §7.1 | **射程の逐語 ／ 29 件の集計** |
| `docs/progress/M25-RESEARCH-01-report.md` §3 / §5.1 | **ledger（★`触る面` 列に「用語・ラベル・入出力」は無い**＝§2.1） |
| `DES-005` | **語彙の正典** |
| `DES-002` §7.6 | **CSV フォーマット** |
| `DES-006` | **重大度の定義** |
| `docs/handover/followup-backlog.md` | `combo-list-filter-controls-split-across-two-widgets` ／ `csv-drive-damage-sign-flipped-across-versions` |
| ボード `D-740` | **交点表**（並列の判定） |

---

*以上、M29-overview **v1.2.0**。* **★★本 MS の落とし穴は「29 件の一覧が在る」と思うことである**（§2.1）。**★数は記録されているが、その数を構成する行は ledger から引けない。⇒ 内訳の実査を `M29-01` の最初の成果物に置いた。★実査の結果が 29 でなかったら、`phase4-overview` §7.1 のほうを直す。**
