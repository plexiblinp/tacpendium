# 指示書 M28-02a: `FR702` 追従のスキーマとバックエンド ＋ 運び量の記録

| 項目 | 内容 |
|------|------|
| 文書ID | M28-02a |
| バージョン | **1.6.0**（2026-09-06・**★★マイグレ 2 本目 `000105` を払い出した＝`D-738`。⇒ 消費は `000104` ＋ `000105` で確定**） ／ 1.5.0（2026-09-05・**★★新区分 2 つの名前が確定した＝`D-733`**） ／ 1.4.0（2026-09-05・**★★`M27-03` の受理で 3 点が動いた＝`D-732`。マイグレ連番 `000103` → `000104` ／ §2.4.7 の射程が縮んだ ／ 投入制約が解けた**） ／ 1.3.0（2026-09-05・**★★始動位置の仕様が全件確定＝`D-731`**） ／ 1.2.0（2026-09-05・**★★物差しが 160 マスで確定＝`D-730`**） ／ 1.1.0（2026-09-05・**★★バージョン文字列の形が確定した＝`D-728`**） ／ 1.0.0（2026-09-05） |
| 作成日 | 2026-09-05 |
| 作成者 | 設計担当 Claude（設計卓／フェーズ4 期） |
| 対象マイルストーン | **`M28`**（破壊的変更の窓）。上位＝`docs/instructions/M28-overview.md` v1.6.0 |
| 推奨モデル | **高精度実装**（`CLAUDE.md` §9＝スキーマ・API 契約・データ移行を伴う変更。実モデル名は `docs/human-notes/model-allocation.md`） |
| Plan Mode | **必須** |
| レビュー | **必要**（チェックリストは着手承認後に設計卓が起こす） |
| CHANGE 消費 | **1〜2 本**（`REQ-001` `FR702` の明確化 ／ `DES-003`（列追加）／ `DES-002`（導出の経路）／ `DES-006`（`DuplicateKey`・バリデーション）に及ぶ見込み。**★起票は設計卓。自採番しない**＝`D-293`） |
| マイグレ消費 | **★★2 本＝`000104` ＋ `000105`**（**2026-09-06 確定＝`D-738`**）。**★`000104`＝`FR702` のマーカー列と基準列 ／ `000105`＝運び量と始動位置の列**〔**§2.8 のコミットの割り方と 1 対 1 になる**〕**。★3 本目が要ると判明したら止めて請求すること**（`D-293`） |
| 前提サブ | **`M28-01` の完了**（module path が動いた上にスキーマを足す） |
| 配置 | `docs/instructions/M28-02a-game-update-schema-and-backend.md` |
| 出力 | `docs/progress/M28-02a-completion-report.md` |

---

## 0. この指示書の特殊性

### 0.1 ★★決まっていることが多い。ゼロから議論しない

**本サブは 2026-09-02 から 2026-09-05 にかけて段階的に絞り込まれた。⇒ 着手時に案を並べ直さないこと。**

| # | 確定事項 | 出所 |
|---|---|---|
| **★★1** | **マーカーは `move` に立てる**（コンボにではない）。**立てるのは配信者（開発者）** | `checklist` §5.1 |
| **★★2** | **マーカーはバージョン文字列**（フラグではない） | 同上 |
| **★★3** | **影響コンボは保存せず導出する** | 同上 |
| **★★4** | **アプリは「影響可能性」を出すだけ。破綻の自動断定はしない**（`FR307` 整合） | 同上 |
| **★★5** | **基準の持ち場は (b) コンボ単位**（`combos` に基準列 1 本）。**決め手は「部分消化できるか」である**——**1 件ずつ「確認した」にできるのは (b) だけ** | `M28-overview` §3.2.7 |
| **★★6** | **既存コンボの初期値は (b-2)＝列を足した時点の最新。⇒ 初回に出る件数は 0 件** | **`D-725`** |
| **★★7** | **過去のマイグレへ遡ってゲームバージョンを付ける作業は要らない** | **`D-725`** |
| **★8** | **`M14` 第四波は完了済み。⇒ backfill は 1 回で済む**（全 31 キャラが揃っている） | `D-707` |
| **★★9** | **★★バージョン文字列の形は `YYYY.MM.DD.NN`**〔例＝`2026.08.03.01`〕**。前半 `YYYY.MM.DD` は SF6 のバランス調整の日付と一致させ、後半 `NN` は 2 桁のアプリ固有の版とする** | **`D-728`** |

### 0.2 ★★本当の目的を取り違えないこと

**開発者の逐語（2026-09-05）＝「この差分アップデートが起きた時に、どのコンボに影響があるかをユーザーに見せ修正しやすいようにする事が今回の目的」。**

**⇒ `FR702` は「バージョンを持つ」機能ではない。「差分の影響を受けたコンボを指し示す」機能である。**

**★バージョン列は手段である。★目的から見て要らない列は足さない。**

### 0.3 ★★射程はスキーマとバックエンドだけ

**画面は `M28-02b` が持つ**（`D-668`。開発者の逐語＝「**Phase4 での必達はバックエンド、DB 構造などの決定。画面などのユーザーが触れる部分はイメージ（設計）まで出来れば実装は後でも良い**」）。

**⇒ 本サブで作るのは「判定できる状態」までである。★画面を作らない。**

### 0.4 ★★2 つのスキーマ変更を 1 本の窓で通す

**`FR702` のマーカー ＋ 運び量の記録**（`M28-overview` §3.6）**の 2 つである。**

**★同じサブに置く理由**——`checklist` §4 の「**破壊的変更を 1 回にまとめる**」。**⇒ マイグレを 2 回に割ると、利用者から見て破壊が 2 回起きる。**

**★★ただし §2 の中では面を分けて書いてある。⇒ コミットも分けること**（§2.8）。

### 0.5 ★★スキーマ変更は開発者の個別承認事項である

`CLAUDE.md` §10。**⇒ §2.1 のスキーマ案を出して承認を得るまで、マイグレを書き始めないこと。**

---

## 1. 前提として読むもの

| # | 資料 | 読む理由 |
|---|---|---|
| 1 | `docs/instructions/M28-overview.md` §3.2（案 (a)〜(e) と 10 軸）・§3.6（運び量） | **★案の比較はここで終わっている。読むのは「なぜ (b) か」の理解のためであり、選び直すためではない** |
| 2 | `docs/human-notes/combmgr-prerelease-checklist.md` §5.1 | 確定事項の一次源 |
| 3 | `DES-003` §3.3（`moves`）・§3.5 付近（`combos.position`）・§3.20（`move_derivations`） | 列を足す先 |
| 4 | `DES-006`（`DuplicateKey` と CSV 列契約） | §2.4 / §2.5 |
| 5 | `docs/handover/followup-backlog.md` `csv-db-frame-cost-columns-drift` | **★★本サブで最も踏みやすい罠の先例**（§2.2-4） |
| 6 | `docs/handover/code-facts.md` | 実装の現在地 |

---

## 2. やること

### 2.1 ★★スキーマ案を出す（**最初の成果物。承認を得るまで実装へ入らない**）

| # | やること |
|---|---|
| **★★1** | **足す列の一覧**（テーブル・列名・型・NULL 可否・既定値・マイグレ本数） |
| **★★2** | **既存データの埋め方**（backfill の中身。**★`D-725` により `combos` は「列を足した時点の最新」で埋める**） |
| **★★3** | **判定クエリの形**（§2.3） |
| **★★4** | **`DuplicateKey` と CSV 列契約への波及**（§2.4-3 / §2.5） |
| **★5** | **down マイグレの中身**（**★列削除は破壊である。⇒ down で何が失われるかを書くこと**） |

### 2.2 `FR702` — マーカー列と基準列

| # | 内容 |
|---|---|
| **★★1** | **`moves` にマーカー列を 1 本**（「最後に変わったゲームバージョン」）**。★1 本で足りる**——**飛ばし更新で 2 回変わっていても「基準 < 最終変更」で判定できる**（`M28-overview` §3.2.1）。 |
| **★★2** | **`combos` に基準列を 1 本**（「このコンボが前提としているゲームバージョン」）**。★登録・更新時に当時の最新を書く。** |
| **★★3** | **★★バージョン文字列の形は `YYYY.MM.DD.NN` で確定した**（`D-728`）**。** **★前半 `YYYY.MM.DD`＝SF6 のバランス調整の日付**〔開発者の逐語＝「**プレイヤー目線で見るゲームバランスの変更という意味でのバージョンはここだけ**」〕**／ 後半 `NN`＝2 桁のアプリ固有の版**〔開発者の逐語＝「**手入力である都合上、私の入力ミスの訂正などやサイレント調整の見逃し、機能追加で列が増えるなどが入る可能性もあるので、そのバージョンも触れるようにしたい**」〕**。例＝`2026.08.03.01`。** |
| **★★3-a** | **★★この形は `TEXT` のまま辞書順で正しく並ぶ。⇒ 順序用の追加列は要らない。** **★理由＝全フィールドが固定幅のゼロ埋めであり、区切りが一定だからである**〔`2026.08.03.01` < `2026.08.03.09` < `2026.08.03.10` < `2026.09.01.00`〕**。⇒ v1.0.0 が懸念した `"1.10" < "1.9"` は起きない。** |
| **★★3-b** | **★★ただし固定幅が崩れた瞬間に順序が壊れる。⇒ 形式の検証を必ず入れること。** **`2026.8.3.1` のような値が 1 つ入るだけで比較が狂い、★エラーにはならない。** **★入口を全部塞ぐこと**——**DB 側の `CHECK` 制約 ／ アプリ側のバリデーション ／ マイグレで投入する値そのもの。★どこまで塞いだかを報告に書く。** **★`DES-006` へ検証コードが要るなら設計卓へ請求する**（**製造は `docs/design/` を編集しない**）。 |
| **★★3-c** | **★★「現在のデータバージョン」を持つ場所が要る。** **⇒ `combos` の基準列へ書く値の出どころである。** **★★`MAX(moves.<マーカー列>)` で代用しないこと**——**move を 1 行も変えないアップデート**〔入力ミスの訂正が moves 以外だった ／ 機能追加で列が増えただけ〕**では `MAX` が動かず、版が上がったことを表せない。★後半 `NN` はまさにその型の版である。** **★設計卓の見立ては `games` テーブルに列を 1 本足すことである**——**`games` は 1 行**（`sf6`）**で、DB と一緒に配布され、`NFR407`（他格闘ゲーム対応の余地）とも整合する。⇒ 新しいテーブルを作らずに済む。★案を Plan Mode で出して報告すること。** |
| **★★4** | **★★seed 再生成でマーカーが消えないようにすること。⇒ `seedgen` が SQL へ出力する側に入れること。** **★先例がある**——**`startup_basis` / `chain_cancel_total` / `fastest_unreachable` は `seedgen` が SQL へ出力しないため golden が守らず、DB だけが進んでいる**（`followup` `csv-db-frame-cost-columns-drift`）**。★マーカー列は同じ形をしている。⇒ 同じ罠を踏まないこと。** **★★`moves` の seed には `DELETE` → `INSERT` で `moves.id` が再発番される波が実在する**（`DES-003` §3.20 の「明示削除の責務」）**。** |
| **★5** | **既存 `moves` 行のマーカー初期値を決める**（**★`D-725` により「初回公開より前」は存在しないものとして扱う。⇒ NULL でよいはずだが、NULL が判定でどう振る舞うかを §2.3-3 で決めること**）。 |

### 2.3 `FR702` — 判定の導出

| # | 内容 |
|---|---|
| **★★1** | **影響コンボは保存しない。導出する**（確定事項 3）。**⇒ 影響コンボを持つテーブルを作らない。** |
| **★★2** | **判定は「コンボが使っている `move` のうち、マーカーがコンボの基準より新しいものが 1 つでもあるか」である。** **★`combo_steps` を経由する**（`DES-003` §3.5 の taxonomy＝`move_id` あり＝技 / NULL＝非技）**。★`move_id` が NULL の行は判定に入らない。** |
| **★★3** | **NULL の扱いを決めて、報告に書くこと。** **★★向きは安全側に倒す**（`M28-overview` §3.2.4 の軸 5 ＝ `FR307` と同じ向き）——**分からないものは「影響可能性あり」に出す。出し漏らさない。** |
| **★4** | **「確認した」の操作は、コンボの基準を現在の最新へ進めることで表す**（部分消化＝軸 3）。**★本サブでは API まで。画面は `M28-02b`。** |
| **★5** | **`materialize` で生成した確定反撃コンボ**（`internal/service/combo/service.go` 付近）**にも基準が入ること。★入らない経路が残っていないか実査すること**——**先例＝`materialize-bypasses-required-fields`**（`followup`。**必須 4 欄が空のまま在りうる**）。 |

### 2.4 始動位置と運び量（**★★2026-09-05 に全件確定＝`D-730` / `D-731`**）

#### 2.4.1 ★★物差し＝トレーニングモードの床のマス（0〜160）

**開発者の逐語＝「スト6の画面は 160 マスをベースに感覚を記載する。（トレーニングモードの床のマス）」**

**★★区分は 7 つ。左右対称である**（`D-731` で是正。**★v1.2.0 が載せていた `91〜111` / `112〜134` は開発者の入力ミスであり失効**）。

| 順 | マス | 表示ラベル | 代表値 |
|---|---|---|---|
| **1** | **0〜25** | 自分画面端 | **12** |
| **2** | **26〜47** | 自分画面端寄り | **36** |
| **3** | **48〜69** | **自分中央寄り**（`mid_self`。**★2026-09-05 確定＝`D-733`**） | **58** |
| **4** | **70〜90** | 画面中央 | **80** |
| **5** | **91〜112** | **相手中央寄り**（`mid_opponent`。**★2026-09-05 確定＝`D-733`**） | **102** |
| **6** | **113〜134** | 相手画面端寄り | **124** |
| **7** | **135〜160** | 相手画面端 | **148** |

**★設計卓が検算した**——**連続しており隙間も重複も無い**（26+22+22+21+22+22+26 ＝ 161 ＝ 0〜160 の値の数）**。★鏡像も全区分で一致する**（`mirror(x) ＝ 160 - x`）。

**★★並び順は「不問」を先頭に置いた 8 値である**（`D-731` で是正。**★v1.2.0 が載せていた「7 相手画面端 / 8 相手画面端寄り」は開発者の入力ミスであり失効**）。

`不問 / 自分画面端 / 自分画面端寄り / 自分中央寄り / 画面中央 / 相手中央寄り / 相手画面端寄り / 相手画面端`

#### 2.4.2 ★★代表値は定数表で直書きする（**丸め規則を実装しない**）

| # | 内容 |
|---|---|
| **★★1** | **従来型の入力で区分を選んだとき、上表の代表値が入る**（**画面中央 → `80`**。開発者の逐語と一致）。 |
| **★★2** | **★★中央値を計算して丸める形にしないこと。** **★設計卓が 4 通りを検算した——四捨五入・切り捨て・切り上げはいずれも左右対称にならない**〔例＝四捨五入だと `12.5 → 13` と `147.5 → 148` で、`160 - 13 ＝ 147 ≠ 148`〕**。対称になるのは half-even だけである。** |
| **★★3** | **⇒ 7 個しかないので定数表で直書きする。★丸め規則を実装するより確実で、説明も要らない。** |

#### 2.4.3 入力方式は 3 つ。連動させる

| 方式 | 中身 |
|---|---|
| **★★従来型（既定）** | **区分を選ぶ。⇒ その区分の代表値が入る。★★これが既定である**（開発者の逐語＝「**全てのユーザーがマス数まで調べるとは限らないので、デフォルトは記載通り従来型とする**」） |
| **マス数** | 0〜160 の整数 |
| **パーセント** | — |

**★3 方式は連動する。⇒ どれを触っても他の 2 つが追随する。**

#### 2.4.4 ★★保存する正本はマス数 1 本

| # | 内容 |
|---|---|
| **★★1** | **保存するのはマス数（0〜160 の整数）1 本とする**（2026-09-05 開発者裁定＝`D-731`）**。★パーセントと区分は表示・入力のための変換であり、保存しない。** |
| **★★2** | **★理由＝連動の落とし穴は往復である。** **パーセントの分解能が 1% だと `1% ＝ 1.6 マス` であり、マス → % → マス で値が変わる。⇒ 利用者が触っていないのに数字が動く。** |
| **★★3** | **★これは `M28-overview` §3.6 の確定事項 2「パーセンテージで持つ」の改訂である**（`D-731` で承認済み）**。⇒ 設計卓が overview へ反映する。製造は請求するだけでよい。** |
| **★4** | **区分（`combos.position`）は、マス数から導出できる。★ただし列として残すか導出にするかは Plan Mode で案を出すこと**——**`DuplicateKey` が区分を使うため**（§2.4.6-3）**、毎回導出すると重複判定のたびに計算が要る。** |

#### 2.4.5 ★★始動位置と運び量は別の列である

| # | 内容 |
|---|---|
| **★★1** | **★★`M28-overview` §3.6-4「位置状況は運び量の値と閾値の比較で機械的に決まる」は失効した**（2026-09-05 開発者裁定＝`D-731`）**。** |
| **★★2** | **開発者の逐語＝「そもそも運び量と今の話は別。コンボを始める前の立ち位置です。なお必ずしも画面端にぴったり送るコンボとは限らないので、運び量は別の列として欲しいです。」** |
| **★★3** | **⇒ 列は 2 本である**——**始動位置**〔コンボを始める前の立ち位置。0〜160〕**と 運び量**〔運んだ距離〕**。★同じ 160 マスの物差しを使うが、共有させない。** |
| **★4** | **★運び量は「必ずしも画面端にぴったり送るとは限らない」ため、区分へ丸めない。⇒ 区分を持つのは始動位置だけである。** |

#### 2.4.6 スキーマと既存データ

| # | 内容 |
|---|---|
| **★★1** | **区分が 5 → 7 に増える**（不問を除く）**。★★既存値の移行は要らない**——**既存の 5 区分はそのまま残り、間に 2 つ挿入されるだけである。** |
| **★★2** | **★★ただし「画面中央」の意味は狭くなる。⇒ 既存の「画面中央」の一部は、新しい区分けでは新区分にあたる。★どれがそうかを判別する記録は無い。⇒ 直せない。受容することを報告に明記すること。** |
| **★★3** | **`DuplicateKey` には引き続き区分を使う。★マス数そのものは重複キーに入れない**（`M28-overview` §3.6-3 の趣旨を維持）**。⇒ ★区分が増えた分だけ重複判定が細かくなる。既存コンボの重複判定が変わらないことを確かめること。** |
| **★★4** | **★★既存コンボのマス数の初期値をどうするか。⇒ 区分の代表値を入れるのが素直である**〔例＝「画面中央」の既存コンボには `80`〕**。★Plan Mode で案を出して報告すること。★「入れない（NULL）」を採るなら、従来型の入力へ戻したときに何が起きるかを書くこと。** |

#### 2.4.7 ★★ラベルと並び順は本サブで直す（**画面の作りは変えない**）

| # | 内容 |
|---|---|
| **★★1** | **★★一覧は `M27-03` が済ませた**（2026-09-05・`CHANGE-155`）**。⇒ 本サブが持つのは 詳細・比較・エディタ の 3 面である**〔開発者の逐語＝「**他の画面は後から始動位置に直します。あなたの修正は一覧だけでいいです**」〕**。★一覧をやり直さないこと。** |
| **★★1-b** | **ラベル「ポジション」を「始動位置」へ改める**（詳細・比較・エディタ）**。★★あわせて並び順を §2.4.1 の順にする。★並び順は一覧も含めた全面である**——**`M27-03` が直したのは一覧の「列」であって、`position` の「値の並び順」ではない。⇒ 混同しないこと。** |
| **★★2** | **★★これは §0.3 の例外である。⇒ 直すのは表示文字列と並び順の定数だけであり、画面の作りは変えない。** **★理由＝値域が 7 区分になった瞬間に必要になる。⇒ 後のサブへ送ると、その間ずっと新区分が変な位置に出る。** |
| **★★3** | **★3 つの入力方式そのものは `M28-02b`**（画面）**である。本サブでは作らない。** |
| **★★4** | ~~`M27-03` が一覧・詳細を触っている最中である~~ **★★【2026-09-05 解消＝`D-732`】`M27-03` は完了・受理した。⇒ 投入制約は無くなった。★★ただし着手時に `M27-03` の as-built を読むこと**〔`CHANGE-155` ／ `DES-005` §5.4。**一覧は「始動状況」1 列から ヒット種別 / 始動位置 / 相手の状態 の 3 列に割れている**〕**。** |

### 2.5 CSV / エクスポートへの波及

| # | 内容 |
|---|---|
| **★★1** | **`combos` に列が増えると CSV の列契約に触れる**（`DES-006` ／ `followup` `csv-column-contract-unobserved-in-pr`）**。⇒ 出力するかしないかを決めること。** |
| **★★2** | **★設計卓の見立ては「基準列は出力しない」である**——**先例＝`materialized_from_combo_id` 等の内部列を出していない**（`M28-overview` §3.2.4）**。★運び量は利用者が入れる値なので出力する側が素直だが、判断は Plan Mode で案を出して報告すること。** |
| **★3** | **import 側の後方互換**（**★列が増えた CSV を古い版が読めるか、逆はどうか**）**を報告に書くこと。** |

### 2.6 バックエンド API

| # | 内容 |
|---|---|
| **★★1** | **`DES-002` §4.2 の経路表に沿うこと。★新しい流儀を作らない。** |
| **★2** | **一覧の絞り込みに「影響可能性あり」を足すなら、既存の絞り込みと同じ形にすること**（**★`M27-03` が同じ面を触っている。⇒ 衝突しないか実査すること**）。 |
| **★3** | **`combos.baseline_version` を JSON へ出すなら camelCase**（`CLAUDE.md` §4）。 |

### 2.7 テスト

| # | 内容 |
|---|---|
| **★★1** | **サービス層は必須**（`CLAUDE.md` §5）。**★判定ロジックの網羅**——**マーカーが新しい／古い／同じ／NULL、基準が NULL、`move_id` が NULL のステップ。** |
| **★★2** | **★★マイグレの版数をテストへ直書きしないこと**（`followup` `migration-version-literals-in-tests`。**★実例＝`000012` を消したら `TestRun_MovesFrameColumnsDownRollback` が落ちた**）。 |
| **★3** | **golden があるなら、マーカー列を足した後の golden がどう動くかを報告に書くこと**（§2.2-4 と同じ面）。 |
| **★4** | **E2E は本サブでは足さなくてよい**（画面が無いため）。**★既存 E2E が緑であることは要る。** |

### 2.8 ★★コミットの割り方

| 順 | コミット |
|---|---|
| **1** | **`FR702` のマーカー列と基準列**（マイグレ ＋ backfill） |
| **2** | **`FR702` の判定の導出 ＋ API** |
| **3** | **運び量の列**（マイグレ） |
| **4** | **運び量と `position` の関係**（§7-2 / §7-3 の回答後） |

**★理由は `M28-03` と同じである**——**赤が出たときに、`FR702` 側か運び量側かを切り分けられるようにする。**

---

## 3. 出力

`docs/progress/M28-02a-completion-report.md`。

| # | 書き方の規約 |
|---|---|
| **★★1** | **§2.1 のスキーマ案と、承認を得た版** |
| **★★2** | **バージョン文字列の順序をどう解いたか**（§2.2-3。**★これが本サブの中心である**） |
| **★★3** | **`seedgen` の出力にマーカーを入れたか。★入れていないなら、なぜ守れるのかを書くこと**（§2.2-4） |
| **★★4** | **NULL の扱いと、その向き**（§2.3-3） |
| **★★5** | **CSV 列契約への波及と、出力するかしないかの判断と理由**（§2.5） |
| **★6** | **`docs/design/` に反映が要る箇所の一覧**（**製造は直さない**） |
| **★7** | **`docs/progress/progress-log.md` の `### M28-02a` へ追記**（`CLAUDE.md` §8） |

---

## 4. やらないこと

| # | やらないこと |
|---|---|
| **★★1** | **画面を作ること**（§0.3。**`M28-02b` の射程**） |
| **★★2** | **影響コンボを保存するテーブルを作ること**（確定事項 3＝導出する） |
| **★★3** | **破綻の自動断定**（確定事項 4＝`FR307`）**。★「無事の自動断定」も同じく禁止である**——**`M28-overview` §3.2.6 が案 (e) を落とした理由がこれである** |
| **★★4** | **案 (a) / (c) / (e) を採り直すこと**（§0.1-5。**★比較は終わっている**） |
| **★★5** | **過去のマイグレへ遡ってゲームバージョンを付けること**（`D-725`） |
| **★★6** | **バージョン文字列の形を製造が単独で決めること**（§2.2-3 → §7-1） |
| **★★7** | **運び量の閾値を製造が作ること**（§2.4-4 → §7-3） |
| **★8** | **`combos.position` の既存値を書き換えること**（§2.4-5） |
| **★9** | **マイグレの版数をテストへ直書きすること**（§2.7-2） |
| **★10** | **CHANGE 番号・マイグレ連番の自採番**（`D-293`） |
| **★11** | **`docs/design/` の編集**（`CLAUDE.md` §8） |
| **★12** | **`docs/handover/followup-backlog.md` の編集**（`D-382`。**★§J 停止時記録だけは例外**） |

---

## 5. 完了条件

1. **§2.1 のスキーマ案が承認され、その形で実装されている。**
2. **判定が導出で動く**（保存していない）**。§2.3 の 5 件すべてに着地がある。**
3. **バージョン文字列の順序が解けており、`"1.10" < "1.9"` の型の誤りが起きないことをテストで示している。**
4. **`seedgen` の再生成でマーカーが消えないことを示している**（§2.2-4）。
5. **運び量の列が入っている。★`position` との関係は §7-2 / §7-3 の回答の範囲で着地している**（回答が無いまま作らない）。
6. **`go test ./...` / `cd web && pnpm test` / `make e2e` が緑。**
7. **常設検査が緑**（`check-artifact-integrity.sh` を 1 本目に回す）。
8. **`docs/design/` に反映が要る箇所が一覧になっている。**
9. **`docs/progress/progress-log.md` へ追記されている。**

---

## 6. ★設計卓が確かめていないこと（実査項目）

| # | 確かめること |
|---|---|
| **★★1** | **`seedgen` が `moves` のどの列を SQL へ出力しているか**（**★設計卓は実装ソースを読めない。§2.2-4 の判断はここに依る**） |
| **★★2** | **`materialize` の経路がコンボのどの列を埋めているか**（§2.3-5） |
| **★★3** | **CSV の列契約の現物**（`DES-006` の記述と実装が一致しているか。**★`csv-column-contract-unobserved-in-pr` が「観測されていない」と言っている**） |
| **★4** | **`M27-03` が一覧の絞り込みをどう変えたか**（§2.6-2。**★並列で走っている**） |
| **★5** | **`games` テーブルの現物**（**★設計卓は `DES-003` §3.1 の定義しか読めていない。⇒ §2.2-3-c で列を足せるかは実物で確かめること**） |
| **★6** | **既存のバリデーションが `DES-006` のどのコードまで実装されているか**（§2.2-3-b。**★`audit_validation_coverage` が使える**） |

---

## 7. 開発者への確認事項

**★★確認事項は 0 件である**（`D-728` / `D-730` / `D-731`）**。⇒ 投入前に決めることは残っていない。**

| # | 確認事項 | 状態 |
|---|---|---|
| ~~1~~ | ~~バージョン文字列の形と順序~~ | **★決着＝`YYYY.MM.DD.NN`**（`D-728`）**。★辞書順で正しく並ぶ。★形式の検証**（§2.2-3-b）**と「現在のデータバージョン」の持ち場**（§2.2-3-c）**は実装の中で解く** |
| ~~2~~ | ~~`position` の入力欄を残すか~~ | **★決着＝残す**（`D-730`）**。3 方式で直接入力する** |
| ~~3~~ | ~~区分の並び順~~ | **★決着＝`不問 / 自分画面端 / 自分画面端寄り / 新区分 / 画面中央 / 新区分 / 相手画面端寄り / 相手画面端`**（`D-731`。**★開発者の入力ミスであった**） |
| ~~4~~ | ~~新区分 2 つの名前~~ | **★★決着＝「自分中央寄り」/「相手中央寄り」**（2026-09-05・`D-733`。逐語＝「**「〇〇中央寄り」で構いません**」）**。code は `mid_self` / `mid_opponent` を推す**〔既存の `mid_screen` と並ぶ〕**。⇒ 暫定ラベルは無くなった** |
| ~~5~~ | ~~保存する正本をマス数にしてよいか~~ | **★決着＝マス数 1 本**（`D-731`）**。⇒ `M28-overview` §3.6 の「パーセンテージで持つ」は改訂された** |
| ~~6~~ | ~~ラベルと並び順をどのサブでやるか~~ | **★決着＝本サブで直す**（`D-731`。**★設計卓へ一任された**）**。⇒ 直すのは表示文字列と並び順の定数だけ**（§2.4.7）**。★★`M27-03` の完了後に投入する** |
| ~~7~~ | ~~`M28-overview` §3.6-4 は失効するか~~ | **★★決着＝失効**（`D-731`）**。⇒ 始動位置と運び量は別の列である**（§2.4.5） |
| ~~8~~ | ~~区分の境界が左右非対称~~ | **★決着＝対称に是正した**（`D-731`。**★`91〜112` / `113〜134`**） |

**★★実装の途中で開発者へ返すものは無くなった**（`D-733`）**。⇒ 投入可である。**

---

## 8. 参照ドキュメント

| ID | パス | 参照する箇所 |
|----|------|------------|
| — | `docs/instructions/M28-overview.md` | §3.2（案の比較）／ §3.3（`M14` との順序）／ §3.6（運び量） |
| — | `docs/human-notes/combmgr-prerelease-checklist.md` | §4（破壊的変更の窓）／ §5.1（`FR702` の確定済み方向性） |
| REQ-001 | `docs/design/requirements.md` | `FR702` / `FR307` |
| DES-002 | `docs/design/02-architecture.md` | §4.2（経路表） |
| DES-003 | `docs/design/03-data-model.md` | §3.3（`moves`）／ `combos.position`／ §3.5（`combo_steps` taxonomy）／ §3.20 |
| DES-006 | `docs/design/06-validation.md` | `DuplicateKey` ／ CSV 列契約 |
| — | `docs/handover/followup-backlog.md` | `csv-db-frame-cost-columns-drift` ／ `csv-column-contract-unobserved-in-pr` ／ `migration-version-literals-in-tests` ／ `materialize-bypasses-required-fields` |

---

*以上、M28-02a 指示書 **v1.6.0**。* **★★本サブの落とし穴は 2 つある。** **(1) 形式が崩れた版数が 1 つ入って、辞書順が静かに壊れること**（§2.2-3-b。**★形が `YYYY.MM.DD.NN` である限り正しく並ぶが、固定幅が崩れた瞬間に負ける**）**／ (2) マーカー列を `seedgen` の出力に入れ忘れ、seed 再生成で黙って消えること**（§2.2-4。**★先例が 3 列ある**）**。★どちらもエラーにならない。**
