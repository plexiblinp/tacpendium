# M31-overview: `α②` セットプレイ・確定反撃・共通部品

| 項目 | 内容 |
|------|------|
| 文書種別 | 補足資料（overview。CHANGE 通知書対象外・自由改訂） |
| バージョン | **1.12.0**（2026-09-10・**★★`M31-05` を受理した＝マージ可＝`D-816`。⇒ `CHANGE-180` を起票と同時に反映した。★`P4M-023` は完了。★★`M31` の残りは `M31-06` の 1 本である**〔開発者が懸念により一旦避けた〕**。★あわせて `M26-03` のチェックリストを含む 4 本を発行した**） ／ 1.11.0（2026-09-10・**★★`M31-04` を受理した＝マージ可＝`D-810`。⇒ `CHANGE-176` を起票と同時に反映した。★`SM-098` 行へ「入力面の除外まで含む」を追記し、§5-6 の実査項目を閉じた**〔`REQ-001` への CHANGE は要らない＝`NFR406` の文言に収まる〕**。★★あわせて `M31-06` を v1.1.0 へ改版した＝`M31-04` が新設した機構に乗る形へ**） ／ 1.10.0（2026-09-10・**★★`M31-06` を起票した＝`D-807`**〔`docs/instructions/M31-06-setup-only-input-exclusion.md` v1.0.0。**★`P4M-005` のフェーズ4 分＝除外の経路だけを、データが 0 件のうちに作る**〕**。★あわせて `M31-05` を v1.2.0 へ改版した＝段 1 の案 (vi) が暫定採択され、止めて返す運用をやめた**） ／ 1.9.0（2026-09-09・**★★`M31-03` を受理した＝`D-796`。⇒ `CHANGE-174` を起票と同時に反映した。★設計卓の誤り 2 枝**〔前提の未検証 ／ 実在しない列名〕**を `M-150` として記録した**） ／ 1.8.0（2026-09-09・**★★`M31-RESEARCH-01` を受理した＝`D-793`。⇒ `P4M-005` は「調べる」から「決める」へ移った。★設計卓の前提が 1 つ覆った**〔**目的は決まっていた。失効していたのは `internal/model/move.go:68` のコメントのほうである**〕） ／ 1.7.0（2026-09-09・**★★`M31-05`**（`P4M-023`＝設置系 `custom_states` の拡張）**と `M31-RESEARCH-01`**（`P4M-005`＝`setup_only` は何を出し分ける列か）**を新設した＝`D-788`。⇒ `M31` の identity 8 件はすべて起票済みになった**） ／ 1.6.0（2026-09-08・**★★`M31-04` を新設した＝`D-785`**〔`SM-098`＝確定反撃でドライブリバーサルを扱う。**開発者の回答が揃った**〕**。★マイグレを 1 本消費するので `M33` より前に入れること**） ／ 1.5.0（2026-09-08・**★★identity を 10 → 8 へ是正した＝`D-779`。⇒ `D-686` の引き算が §2.1 で失われていた。★`M31-02` の受理にともなう**） ／ 1.4.0（2026-09-07・**★★`M31-03` を新設して 3 サブになった＝`D-774`**〔一覧の列の並び ＋ 詳細への行内リンク。**開発者が 2026-09-07 に直接挙げた 2 件**〕） ／ 1.3.0（2026-09-07・**★★2 サブを起票した＝`D-768`**〔`M31-01` v1.0.0 ／ `M31-02` v1.0.0〕） ／ 1.2.0（2026-09-06・**★★確認事項 4 が決着＝`D-764`。⇒ 4 件すべて決着し、`M31-01` / `M31-02` を起票できる状態になった。★§4.2〜§4.4 を新設した**） ／ **1.1.0**（2026-09-06・**★★確認事項 1〜3 が決着＝承認**〔`D-747`〕**。★4 は課題の原文を §4.1 へ置いた。開発者が検討中**） ／ 1.0.0（2026-09-06・起票） |
| 作成日 | 2026-09-06 |
| 作成者 | 設計担当 Claude（設計卓／フェーズ4 期） |
| 対象マイルストーン | **`M31`**（フェーズ4）。上位＝`docs/instructions/phase4-overview.md` §3 |
| 位置づけ | **`M31` のサブ構成・射程・CHANGE 見込みの正本** |
| 状態 | **★★起票済み**（2026-09-07・`D-768`）**。⇒ `M31-01` / `M31-02` の指示書が発行された。★投入は走行中 3 本のマージと、改善レーン C1〜C3 の後である。★レビューチェックリストは着手承認の手番で起こす**（`D-736`） 以下は前回更新時の記述： **★★承認済み・起票可**（2026-09-06・`D-764`）**。⇒ 確認事項 4 件がすべて決着した。★`M31-01` / `M31-02` の起票が次の手番である** 以下は前回更新時の記述： **★サブ分割の承認待ち**（§3） |
| identity 数 | **★★10 を実測した**（§2.1）。**`phase4-overview` の「12 見込み」とは 2 件ずれる** |
| CHANGE 見込み | **1〜2 本**（`DES-005` が主 ／ `DES-006`（必須欄の検証経路）） |
| マイグレ見込み | **0 本** |
| 配置 | `docs/instructions/M31-overview.md`（active＝直下。フェーズ4 終了時に `docs/instructions/phase4/` へアーカイブ） |

---

## 0. 本書の確度

- **★★identity を ledger から実測した**（§2.1）。**⇒ `M29` と違い、本 MS は行を特定できている。★ただし `phase4-overview` の「12 見込み」とは 2 件ずれる。**
- **★設計卓は実装ソースを読んでいない。** 実装の事実は `code-facts.md` ／ 完了報告 ／ 設計伝達レポートからのみ引いている。**断定していない箇所は §5 の実査項目にした。**
- **★★本 MS には欠陥が 1 件混じっている**（`materialize-bypasses-required-fields`）。**⇒ 他の 10 件は UX 項目だが、これだけは性質が違う**（§2.3）。

---

## 1. 目的

**セットプレイ・確定反撃・共通画面部品の 3 面に散った小さな課題をまとめて片づける。**

**★これらは「同じ画面の中の話」ではなく「同じ担当が触る面」でまとまっている。⇒ 面が縦に割れていないことが本 MS の性質である。**

---

## 2. 射程

### 2.1 ★★identity の実測（**先に読む**）

**★★【2026-09-08 是正＝`D-779`】`M31` に属する identity は 8 件である。⇒ 1.4.0 まで 10 件としていたのは誤りだった。**

**★★落とした 2 件＝`P4M-012`**（キャラ選択後 `Enter` が反応しない。**旧 identity 7**）**と `P4M-013`**（キャラ選択のドラッグ判定。**旧 identity 8**）**。**

| 落とした identity | 理由 |
|---|---|
| **`P4M-012`** | **★★恒久にクローズ**（2026-09-08 開発者裁定）**。⇒ `M27-02a` が実測で「`Enter` は正しく効いている」ことを突き止めており、逐語の「反応しない」の実体は「次へ進まず、同じ欄が開き直すだけ」だった。★真の非対称は followup `editor-character-field-outside-field-sequence` にある** |
| **`P4M-013`** | **★★`M31-02` で解消した**（2026-09-08）**。⇒ popover の中の検索欄で始めたマウスのドラッグが、枠外へ出ると文字選択を失う欠陥。★`SearchableSelect` と `TagSelector` の 2 部品にあり、両方直った** |

**★★そもそも 2 件は `D-686`**（2026-09-02）**で `M31` から引かれていた。⇒ `M27-overview.md:105` と `phase4-overview.md:136` の 2 か所に「`M31` は `P4M-012` / `P4M-013` を持たない」と書かれている。★それでも §2.1 は 2 件を数えたまま起票した。**

**★★機序＝設計卓が ledger から数え直したためである。⇒ ledger には「この 2 件は引く」が書かれていないので、再導出が手作業の引き算を上書きした。★計測点 `M-144`。**

**★★一般形＝源泉から数え直すと、源泉に書いていない手作業の引き算は必ず消える。⇒ 「2 か所に書いたから大丈夫」は効かない。どちらも源泉ではないからである。**

**★以下は是正後の 8 件である。**

| # | identity | 内容 | 触る面 | 名寄せ |
|---|---|---|---|---|
| **1** | **`P4M-004`** | **セットプレイ編集の戻るボタン位置** | 画面: セットプレイ編集 | `SD-001` を代表する |
| **2** | **`P4M-005`** | **`setplay only` フラグの活用**（**★★実列名は `setup_only` である。⇒ `setplay_only` という列は DB に無い**） | データ・セットプレイ | `SD-003` を代表する。 **★★【2026-09-09 更新＝`D-793`】`M31-RESEARCH-01` 完了・受理**（報告＝`docs/progress/M31-RESEARCH-01-report.md`）**。⇒ 目的は決まっていた**〔`CHANGE-022` §2 ／ `DES-003` §3.3＝**コンボ登録 UI の技選択から除外**〕**。** **★未決は 3 つ＝どの `move_code` に立てるか ／ 非表示の範囲 ／ 付与口**（**★1 つも無い**）**。** **★`custom_states` との重なりは「無い」で決着。⇒ `M31-05` と独立に進めてよい。** **★★開発者判断 6 件が報告 §6 に並ぶ。⇒ 実装サブの起票はその後である** |
| **3** | **`P4M-019`** | **セットプレイ削除時の紐付け解除** | 画面: セットプレイ | `SD-023` を代表する |
| **4** | **`P4M-014`** | **確定反撃マイリストの区分整理** | 画面: 確定反撃 | `SD-018` を代表する |
| **5** | **`P4M-015`** | **確定反撃の自キャラ既定値** | 画面: 確定反撃 | `SD-017` を代表する |
| **6** | **`SM-098`** | **確定反撃での D リバの扱い**（**★★★2026-09-10 完了＝`M31-04`。⇒ 射程は「入力面の除外まで含む」**〔`INPUT_EXCLUDED_MOVE_CODES` の新設〕**。★`D-810` で超過を受理した**） | 画面・要件: 確定反撃 | **★`REQ-001` は不要**（`NFR406` の文言に収まる＝§5-6 は閉じた） |
| **★★7** | **`SD-006`** | **起き攻め DR なし選択時の自動チェック** | 画面・データ | **★代表を持たない＝純増** |
| **★8** | **`P4M-023`** | **各キャラ `custom_state` 拡張** | **データ・seed** | `SD-004`（複雑な状態遷移キャラのセットプレイ対応）を代表する |

### 2.2 ★★`phase4-overview` の「12 見込み」と 4 件ずれる（**★★是正前は 2 件と書いていた＝`D-779`**）

**`phase4-overview` §3 M31 は「★★12 見込み」と書き、「純増は as-built 由来の 3 件だけ」と説明している。**

**★★是正後の実測は 8 件である**（`D-779`）**。⇒ 代表を持たない純増は `SD-006` の 1 件だけである。**

**★★差は 2 件から 4 件へ広がった。⇒ ただし内訳の 2 件は分かっている**〔`P4M-012` / `P4M-013`＝`D-686` で `M27-02a` へ移した分〕**。★残る差 2 件が §5-1 の実査項目である。**

| # | 帰結 |
|---|---|
| **★★1** | **⇒ 差 4 件のうち 2 件は判明した**（`P4M-012` / `P4M-013`＝`D-686` の引き算）**。★残る 2 件の正体は未確認である**（§5-1）**。★`M-127` と同じ族**〔記録された数の内訳が引けない〕**だが、本件は 8 件を特定できているぶん軽い。** |
| **★★2** | **★as-built 由来の 3 件のうち 2 件が ledger に載っていない可能性がある。⇒ `followup-backlog` 側に在る**（§2.3 がその 1 件である） |
| **★3** | **実測が 10 で確定したら `phase4-overview` §3 と §7.1 へ追随注記が要る。** |

### 2.3 ★★`followup` 由来の 1 件（**欠陥であり、UX 項目ではない**）

| スラッグ | 内容 |
|---|---|
| **`materialize-bypasses-required-fields`** | **★★確定反撃から生成した本登録コンボは、必須 4 欄が空のまま在りうる。** `internal/service/combo/service.go` の materialize が `VAL-C15` の経路を通らないためである（`M27-02b` 設計伝達レポート §1-1）。 |

**★★これは他の 10 件と性質が違う。⇒ UX の改善ではなく、`M27-02b` が入れた必須化の穴である。**

**★`M27-03` の教訓 1 と同じ形である**〔**「表示だけ」に見える依頼が、データ移行を伴う変更だった**〕**——「確定反撃の画面」の話に見えるが、実体は検証経路の穴である。**

### 2.4 ★★`P4M-023` は本 MS の射程か（**要確認**）

**`P4M-023`（各キャラ `custom_state` 拡張）の `触る面` は「データ・seed」である。⇒ `M14` レーンの匂いがする。**

**★ただし `SD-004`（複雑な状態遷移キャラのセットプレイ対応）が代表 ID として `P4M-023` を指しており、セットプレイ側の要求が根にある。**

| 読み | 帰結 |
|---|---|
| **(a) `M31` が持つ** | **セットプレイが必要とする `custom_state` を、セットプレイ側の判断で足す** |
| **(b) `M14` レーンが持つ** | **seed データの拡張であり、他の seed 作業と同じ手番でやる** |

**⇒ §4-3 で確認する。★設計卓は (a) を推す**——**「どの状態が要るか」はセットプレイ側にしか分からないため。ただし投入は `M14` レーンと同じ人がやるほうが安い可能性がある。**

### 2.5 ★`SD-006` の「再整理」の射程（**起票前に決める**）

**開発者の逐語**（`D-649`）**＝「修正自体は単純な見込みだが、いったん直すときに再整理はしたい」。**

**⇒ 「再整理」が何を指すかが決まっていない。★設計卓の見立て＝「起き攻めまわりを 1 サブでまとめて触る」**（`phase4-overview` §3 の記述と同じ）**。**

**★★ただし「起き攻めまわり」の範囲が不明である**——**`combo_oki_options` の 12 変種の UI ／ 凡例の注記 ／ `M27-02b` が入れた「起き攻めを調べた」トグル ／ 比較表の 12 変種行。⇒ §4-4 で確認する。**

---

## 3. サブ構成（**★★開発者承認事項**）

**2 サブを提案する。**

| サブ | 射程 | identity | 性質 |
|---|---|---|---|
| **M31-01**〔**起票済み＝`docs/instructions/M31-01-setplay-punish-and-oki.md` v1.0.0**〕 | **セットプレイ ＋ 確定反撃 ＋ 起き攻めの連動**（**★`materialize` の穴を含む**） | `P4M-004` / `P4M-005` / `P4M-019` / `P4M-014` / `P4M-015` / `SM-098` / `SD-006` / `materialize-bypasses-required-fields`（**8**） | **★データと検証に触れる。★★`materialize` の穴が最も重い** |
| **M31-02**〔**起票済み＝`docs/instructions/M31-02-shared-character-picker.md` v1.0.0**〕 | **共通画面部品**（キャラ選択） | `P4M-012` / `P4M-013`（**2**） | **★★キャラ選択の入力挙動だけ。他と面が完全に分かれる** |

**★`P4M-023` は §4-3 の回答で行き先が決まる。⇒ `M31-01` に入るか、`M14` レーンへ出る。**

### 3.0 ★★【2026-09-07 追加＝`D-774`】`M31-03` を新設した（**3 サブになった**）

| サブ | 射程 | identity | 性質 |
|---|---|---|---|
| **M31-03**〔**起票済み＝`docs/instructions/M31-03-combo-list-columns-and-row-link.md` v1.0.0**〕 | **一覧の列の並び ＋ 詳細への行内リンクの位置** | `combo-list-column-order-unreviewed` / `combo-list-detail-link-on-wrong-column`（**2**） | **★★開発者が 2026-09-07 に直接挙げた。⇒ ledger 由来ではない純増である** |

**★★`M31-02` の後に直列で置く**——**どちらも共通部品の族であり、同じ手番に 2 つの部品を触ると赤が出たときに切り分けられない。★部品は別である**〔`M31-02`＝キャラ選択 ／ `M31-03`＝一覧の行〕**。**

**★★2 件は性質が違う**——**行内リンクは明確な是正**〔元々は始動状況にあったものがヒット種別へ移っている〕**、列の並びは案が無い**〔開発者の逐語＝「まだ案はありません」〕**。⇒ コミットを分けさせる。**

**★★唯一の重い点＝並びを変えると、既に保存されている利用者の列設定**（`combo-list-columns-v1`）**に当たる。⇒ 決めずに入れると、利用者の手元で見た目が変わるか変わらないかが黙って起きる。**


### 3.1 ★★並列にしてよい

**`M31-01`**（セットプレイ・確定反撃）**と `M31-02`**（キャラ選択の入力挙動）**は、触るファイルが重ならない見込みである。**

**⇒ `D-740` の交点表で判定すること。★重なったら直列にする。**

**★★ただし `M31-02` は小さい**（2 件）**。⇒ 単独で 1 サブにする価値が在るかは §4-2 で確認する。★設計卓は「分ける」を推す**——**共通部品は他の全画面に波及するため、セットプレイ・確定反撃の変更と同じコミットに混ぜると、赤が出たときに切り分けられない。**

---

## 4. ★開発者への確認事項

| # | 確認事項 | 設計卓の暫定案 |
|---|---|---|
| ~~**★★1**~~ | **サブ分割 2 本の承認**（§3） | **★★決着＝承認**（2026-09-06・`D-747`。逐語＝「1,2,3 はあなたの案を承認します」）**。⇒ 2 サブ・並列で確定。交点表で重なりが出たら直列へ落とす** |
| ~~**★★2**~~ | **`M31-02`（共通画面部品 2 件）を単独のサブにするか** | **★★決着＝承認**（同上）**。⇒ 分ける。理由＝共通部品は他の全画面に波及し、同じコミットに混ぜると赤が出たときに切り分けられない** |
| ~~**★★3**~~ | **`P4M-023`（各キャラ `custom_state` 拡張）を `M31` が持つか、`M14` レーンへ出すか**（§2.4） | **★★決着＝承認**（同上）**。⇒ `M31` が持つ。理由＝「どの状態が要るか」はセットプレイ側にしか分からない** |
| ~~**★★4**~~ | **`SD-006` の「再整理」の射程**（§2.5） | **★★決着＝2026-09-06・`D-764`。⇒ 射程が確定した。詳細は §4.2** 〔**(1) 連動は未実装であり `SD-006` 本体は生きている ／ (2) 「再整理」＝起き攻め周りのバリデーション警告の違和感の解消 ／ (3) 項目数・未検証の記録はフェーズ4 内で解消済みであり射程外**〕**。★`M31-01` を起票できる状態になった** |

### 4.1 ★★確認事項 4 の一次源（**2026-09-06 追加＝`D-747`**）

**★★`SD-006` の一次源は `Memo_Someday.txt:366` の 1 行だけである。⇒ 逐語は次のとおり。**

> 起き攻めについて、ドライブラッシュなしの方にチェックをつけたら、ドライブラッシュ版に自動でチェックがついて欲しい。

**★★「再整理」はこの行に書かれていない。⇒ 出所は `D-649` の開発者回答のほうである。逐語＝「直したい。修正自体は単純な見込みだが、いったん直すときに再整理はしたい」。**

**⇒ したがって射程が不明なのは「再整理」の 3 文字だけであり、`SD-006` 本体は 1 行で閉じている。**

#### ★同じ面に在る隣接行（**参考。射程に入れるかは開発者の判断**）

| # | 行 | 一次源 | 状態 |
|---|---|---|---|
| 1 | **`SD-005`＝相手の状態等の指定をプルダウンより速い方法にしたい** | `Memo_Someday.txt:365`（`SD-006` の**すぐ上の行**） | **★★`D-651` で「`M24` で一番使う新規・編集画面には実施済み。緊急度のあるところは直っている」と決着。⇒ フェーズ5 へ送った** |
| 2 | **`oki-legend-lacks-post-combo-annotation`＝`起き攻め` 節の legend に `（コンボ後情報）` が付いていない** | `followup`（`M24-07` §4-9 由来） | **要判断・据置**（`D-646` 規則 (8)）**。★1 行の文言変更** |
| 3 | **`M27-02b` の「起き攻めを調べた」トグル**（3 状態＝必須・任意・未検証） | `CHANGE-153` | **★実装済み。⇒ 触るなら整合の確認だけ** |

#### ★★「すでに解消されている可能性」の見どころ

**★開発者の見立て**（2026-09-06 逐語＝「これはすでに解消されているものが残っている可能性があります」）**を確かめるには、次の 2 つを実物で見る必要がある。**

| # | 何を見るか | なぜ |
|---|---|---|
| **★★1** | **登録・編集画面の起き攻めチェックボックスが、いま `uses_dr` の対と連動しているか** | **★`M16-03` が `combo_oki_options` を正規化し、`M24` 期に登録・編集画面へ手が入っている。⇒ その過程で連動が入った可能性がある** |
| **★★2** | **`Memo_Someday.txt:366` に状態マーカーが付いていないこと** | **★★同ファイルは「未マーカー＝未処理」ではない**（`D-649` の教訓＝**5 行のうち 2 行は既に実行済みだった**）**。⇒ マーカーの有無を根拠にしないこと** |

**★★設計卓は武装中であり実装を読めない。⇒ 上の 2 点は起票時に製造へ実測させるのが素直である**（`M30-01` §2.1 ／ `M29-01` §2.1 と同じ形＝**最初の成果物を実測に置く**）**。★もし解消済みなら、`M31` の射程から `SD-006` を外して `phase4-overview` の数のほうを直す。**

---

### 4.2 ★★確認事項 4 の決着（**2026-09-06＝`D-764`**）

**★開発者の回答を受けて、「再整理」の射程が 3 つに分かれた。**

| # | 事項 | 開発者の逐語 | 帰結 |
|---|---|---|---|
| **★★1** | **連動の実装**（`SD-006` 本体） | **「連動していない」** | **★★見どころ 1 の答えである。⇒ 未実装であり `SD-006` は生きている。★`M31` の射程に残す** |
| **★★2** | **起き攻め周りのバリデーションの整理** | **「おそらくバリデーションの整理もしたくてこれは書いていた。A にチェックないのに B にチェックがついているのはおかしいみたいな警告で違和感を感じるものが存在した気がするが、気がするレベルで深く見てなかった」** | **★★これが「再整理」の中身である。⇒ `M31` の射程に入れる。★ただし下の 4.3 のとおり、まず違和感の正体を特定させる** |
| **★★3** | **項目数の不足 ／ 未検証が記録されない** | **「そもそも項目数が足りないのでは？とか、未検証が記録されていないのでは？とかもここには含まれていた気がするが、それはフェーズ4 内で解消済み」** | **★★射程外である。⇒ `M31-01` に持ち込まない。★★解消の実体は `M27-02b` の 3 状態トグル**〔必須・任意・未検証〕**であり、`CHANGE-153` で着地している** |

### 4.3 ★★「違和感」の正体の候補（**設計卓が `DES-006` を実査した。2026-09-06**）

**★★開発者の記述は「気がするレベル」である。⇒ 実在するかを先に確かめる必要がある。★設計卓は `DES-006` を読めるので、候補を特定して置く**（**`M-138` の教訓＝「どこにも記録されていない」と書く前に読む**）**。**

**★★候補 1＝`VAL-C11` そのものである。⇒ 開発者の記述と形が一致する。**

> **逐語**（`DES-006` §2 `VAL-C11`）**＝「起き攻めオプション（`combo_oki_options`）の整合性を全 attack_type へ一様適用（uniform）。ドライブラッシュ有（`uses_dr=true`）のオプションがあるのにノーゲージ版（同 attack_type・同 tech_type で `uses_dr=false`）が無い場合に WARNING（例：シミー/打撃重ねでも「ドライブラッシュのみ設定・ノーゲージなし」に発火）」。重大度＝WARNING。**

| # | なぜ違和感が出るか |
|---|---|
| **★★1** | **本 VAL は「A にチェックが無いのに B にチェックがある」で鳴る警告そのものである。⇒ 開発者の記述と一致する** |
| **★★2** | **★★もとは meaty の 2 ペアだけが対象だった。⇒ `CHANGE-063` / `M16-03` の正規化で全 attack_type へ一様適用になった**。**★`DES-006` は「シミー/打撃重ねでも発火する」を例として自分で挙げている。⇒ 発火面が広がったことは設計書自身が認めている** |
| **★★3** | **★★`SD-006` の要望と向きが逆である**——**`SD-006` は「ノーゲージ版にチェック → DR 版に自動でチェック」を求めている。`VAL-C11` は「DR 版だけある → 警告」で鳴る。⇒ どちらも同じ対の話だが、片方は入力の補助、片方は事後の警告である。★連動を入れると `VAL-C11` の母集団が変わりうる** |

**★候補 2＝警告の載る経路が 2 つに分かれていること。**

> **逐語**（`DES-006` §末尾）**＝「登録応答では `validations` と `warnings` の両方に警告が載りうる（`CHANGE-125` で新しく生まれた状態）。`POST /api/combos` は `VAL-C03` / `VAL-C08` / `VAL-C10` / `VAL-C11`〔いずれも WARNING〕を従来どおり `validations` に載せたまま、`VAL-C14` を `warnings` に載せる」。**

**★同じ種別の警告が 2 経路に分かれている。⇒ 画面での出方が揃っていない可能性がある。★`VAL-C11` は `validations` 側である。**

### 4.4 ★★`M31-01` へ渡す形

| # | 渡すこと |
|---|---|
| **★★1** | **`SD-006` 本体**（ノーゲージ版にチェック → DR 版へ連動）**を実装する。★連動が未実装であることは開発者が確認済みである。⇒ 製造に再実測させない** |
| **★★2** | **`VAL-C11` の発火を実測させる**——**シミー / 打撃重ね を含む全 attack_type で、どういう入力のときに鳴るかを 1 通り出させる。★★これが「違和感」の正体かどうかの判定材料になる** |
| **★★3** | **連動を入れた後に `VAL-C11` の母集団がどう変わるかを示させる**（**★連動が入ると「DR 版だけある」状態は手で作らない限り生まれない。⇒ 警告が実質死ぬ可能性がある**） |
| **★★4** | **重大度と文言は変えさせない。⇒ 変える必要があると判明したら止めて設計卓へ請求させる**（`D-293`。**★`M29-02` が `VAL-C09` で同じ形を踏んでいる**） |
| **★5** | **項目数・未検証の記録は射程外である**（§4.2-3）**。⇒ 指示書の「やらないこと」へ明記する** |


---

---

## 5. ★設計卓が確かめていないこと（実査項目）

| # | 確かめること |
|---|---|
| **★★1** | **`phase4-overview` の「12 見込み」と実測 8 件の差 4 件**（§2.2）**。★うち 2 件は `D-686` の引き算と判明した。⇒ 残る 2 件を `followup-backlog` 側の as-built 由来の項目から洗うこと** |
| **★★2** | **`materialize` が `VAL-C15` の経路を通らない現物**（§2.3。**`internal/service/combo/service.go`**）**。★設計卓は実装を読めない** |
| **★★3** | **`SD-006` の現物**（**ドライブラッシュなしにチェックを付けたら DR 版にも自動で付く**）**。★どの層で起きているか**〔UI の連動か、保存の正規化か〕 |
| ~~**★4**~~ | **★★【2026-09-08 決着＝`D-779`】答えは `SearchableSelect` である**（`M27-02a` の成果物＝`D-683`）**。⇒ 加えて `M31-02` が `TagSelector` にも同じ欠陥を見つけて直した。★2 部品は統合していない**（`M27-02a` の開発者裁定。**波及の数が違う**）**。★以下は前回の記述：** `P4M-012` / `P4M-013` が触る部品（`CharacterSelector` か `SearchableSelect` か） |
| **★5** | **`P4M-023` の `custom_state` が既にどこまで在るか**（**`M11` が `custom_states` を入れ、`CHANGE-131` がマノンのメダルを撤回している**） |
| **★6** | **`SM-098`（確定反撃での D リバの扱い）が「要件」なのか「画面」なのか**（**触る面が `画面・要件: 確定反撃` である。⇒ `REQ-001` に及ぶ可能性がある**） |

---

## 6. 依存関係・順序

```
M27（α② 画面品質）… ★完了。★★M27-02b が必須化を入れ、materialize の穴を作った
     │
     ├─→ M31-01（セットプレイ ＋ 確定反撃）… ★materialize の穴を塞ぐ
     │
     └─→ M31-02（共通画面部品）… ★並列可（交点表で判定）
```

**★`M28` レーン ／ `M29` レーン ／ `M30` レーンとは面が違う。⇒ 並列可**（`D-740` の交点表で判定すること）。

**★★ただし `M31-02`（キャラ選択）と `M30`（仮想コントローラ）は、共通部品で重なりうる。⇒ 同時に出すなら交点表を先に書くこと。**

---

## 7. CHANGE 見込み・採番

| サブ | CHANGE 見込み | マイグレ | 根拠 |
|---|---|---|---|
| **M31-01** | **1〜2 本** | **0 本** | **`DES-005`**（セットプレイ・確定反撃の画面）**が主。★★`materialize` の穴を塞ぐと `DES-006` の検証経路に及ぶ**〔`VAL-C15` がどこで掛かるか〕**。★`SM-098` が要件なら `REQ-001` にも及ぶ** |
| **M31-02** | **0〜1 本** | **0 本** | **`DES-005`**（共通部品の入力挙動）**。★挙動の修正だけなら CHANGE は要らない** |

**⇒ `M31` の見込みは 1〜3 本。★次に採番するのは `157`**（**★`M26-02` が `157`、`M29` が `158` 以降を使う見込み。★先付けしない**＝playbook §4.15）。

### 7.1 推奨モデル

- **`M31-01`**——**高精度実装 ＋ Plan Mode 必須**（**★★`materialize` の穴は検証経路の変更であり、既存 83 件のコンボに影響しうる**）
- **`M31-02`**——**標準実装 ＋ Plan Mode 必須**（**★共通部品は他の全画面に波及する**）

---

## 8. 参照

| 対象 | 何を読むか |
|---|---|
| `docs/instructions/phase4-overview.md` §3 M31 ／ §7.1 | **射程の逐語 ／ 12 見込み**（**★実測と 2 件ずれる**＝§2.2） |
| `docs/progress/M25-RESEARCH-01-report.md` §3 | **ledger（`代表 ID` 欄が名寄せの正本）** |
| `M27-02b` 設計伝達レポート §1-1 | **`materialize` が必須 4 欄を迂回する現物** |
| `DES-005` | セットプレイ・確定反撃・共通部品の画面 |
| `DES-006` | **`VAL-C15`（必須 4 欄）の検証経路** |
| ボード `D-649` | **`SD-006` の「再整理」の逐語** |
| ボード `D-740` | **交点表**（並列の判定） |
| `docs/handover/followup-backlog.md` | `materialize-bypasses-required-fields` ／ `sd-020-od-variants-two-surfaces` |

---

*以上、M31-overview **v1.5.0**。* **★★本 MS の落とし穴は「小さな UX 課題が 8 件」と読むことである。** **★1 件だけ性質が違う**（`materialize-bypasses-required-fields`）**——これは `M27-02b` が入れた必須化の穴であり、UX ではなく欠陥である。⇒ 他の 7 件と同じ優先度で扱うと、後回しになる。** **★もう 1 つは `phase4-overview` の「12」と実測 8 件の差である**（§2.2）**——★差 4 件のうち 2 件は `D-686` の引き算と判明した。⇒ 残る 2 件の正体を確かめないまま起票すると、`M31` が閉じたときに 2 件が宙に浮く。**
