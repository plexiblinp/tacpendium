# 指示書 M37-04: 確定反撃候補の初段発生（`B02` / `B06`）

| 項目 | 内容 |
|------|------|
| 文書ID | M37-04 |
| バージョン | **1.0.3**（2026-09-14・**★マイグレ欄のみ改訂＝`000116` を追加払い出し**〔`D-875`〕**。⇒ 射程・段取り・完了条件は 1 文字も変えていない**） ／ **1.0.2**（2026-09-13・**★マイグレ欄のみ改訂＝`000115` を追加払い出し**〔`D-870`〕**。⇒ 射程・段取り・完了条件は 1 文字も変えていない**） ／ **1.0.1**（2026-09-13・**★版ゲート 3 のみ改訂＝`000113` が欠番になったことを明記**〔`D-866`〕**。⇒ 射程・段取り・完了条件は 1 文字も変えていない**） ／ **1.0.0**（2026-09-13・起票） |
| 作成日 | 2026-09-13 |
| 作成者 | 設計担当 Claude（設計卓） |
| 対象マイルストーン | **`M37`**（リリース前要望の束）。上位＝`docs/instructions/M37-overview.md` v1.2.0 |
| 推奨モデル | **標準実装**（実モデル名は `docs/human-notes/model-allocation.md`） |
| Plan Mode | **必須**（`CLAUDE.md` §9＝**スキーマ変更を伴う**） |
| レビュー | **必要**（**★チェックリスト＝`docs/instructions/reviews/M37-04-review-checklist.md` v1.0.0・2026-09-13 発行済み**） |
| CHANGE 消費 | **1〜2 本の見込み**（`DES-003` §3.3 ／ `DES-005` §5.20。**★自採番しない**＝`D-293`） |
| マイグレ消費 | **★★`000114` ＋ `000115` ＋ `000116` を払い出し済み**（**`000115` は 2026-09-13 に＝`D-870`、`000116` は 2026-09-14 に追加払い出し＝`D-875`。★いずれも製造が請求し開発者が承認した。★★`M37-07` は `000117` へ動かした**〕**⇒ 本サブの 3 本は連番で並ぶ**（**★後から下の番号を足すと `Up` が素通りする**＝`SUPP-001` §根拠 2）**。**（**。★★`000113` は欠番である**〔`D-866`〕**。⇒ 詰めない。★さらに足りなければ止めて請求する。★★★用途を報告へ明記すること**——**`startup` の値を書き換えるマイグレなら §4.1 の最大の危険に触れる** |
| 前提サブ | **`M37-RESEARCH-01`**（完了済み）**／ `M37-01` と `M37-03` の着地**（**★同じ `combos` / `moves` 面へ触れうるため**） |
| 射程 identity | **2 件**——`B02`（バーンアウト中の確定反撃）／ `B06`（ターゲットコンボの候補の是正） |
| 配置 | `docs/instructions/M37-04-punish-candidate-first-hit-startup.md` |
| 出力 | `docs/progress/M37-04-completion-report.md` |

---

## 0. この指示書の特殊性

### 0.1 ★★★`B06` はデータの誤りではない。**判定の誤りである**

**★開発者の回答**（2026-09-13・逐語）**＝「ゲームの第二段目発生が入っている事は間違いない。元データは私の手入力であるため、その時に基準は制定している」。**

**⇒ ターゲットコンボの 2 発止め行が「その段の発生」を持っていることは*正しい*。★技の性能表示としてはそれが正である。**

**★★★誤っているのは、確定反撃の候補判定が*その値を初段の発生として使っている*ことである**（開発者が追認・`D-857`）**。**

| 実測（`M37-RESEARCH-01` §4.1） | 値 |
|---|---|
| ryu `fuwa_triple_strike_2hits`（2 発止め） | `startup=5` |
| ryu `fuwa_triple_strike`（全段） | `startup=17` |
| **初段＝ryu `standing_medium_punch`** | **`startup=6`** |

**⇒ 地上で有利 5F のとき、`5 <= 5` で 2 発止めが候補に出る。★しかし実際にはこの技は 6F から始まる。⇒ 入らない。**

### 0.2 ★★★初段を「同定しない」。**発生値を直接持つ**

**★開発者が挙げた落とし穴＝ターゲットコンボの初段には 2 パターンある**〔① `unique_move_1hit` ／ ② `standing_medium_punch`〕**。⇒ ② を機械的に拾うのは現状のデータでは難しい。**

**★★実測が裏づけている**（`M37-RESEARCH-01` §6-2）**＝単純な先頭 `command` 完全一致では、126 行中 64 行が不一致・3 行一致・59 行が判定不能。★しかも `command` 列は CSV にあって DB `moves` に無い。⇒ 実行時に引けない。**

**⇒ 同定が難しいなら、同定しない。⇒ 値を直接持つ。★①/② の区別も要らなくなる。**

### 0.3 ★★★列名は `first_hit_startup` である（**★列追加審査を通した**）

**★`M19-DESIGN-07` §7 の審査規約＝「`moves` への新列は*ゲームの事実の記録*に限る。特定機能の除外・選別は既存事実列からの導出規則として消費者側に書く」。**

| 案 | 審査 |
|---|---|
| ~~`starter_startup`~~ | **★★不可。⇒ 「始動用の」は*確定反撃という特定消費者の用途*を名前に持ち込んでいる。★`is_derived`（実態＝索引除外の判断）と同じ罠である** |
| **`first_hit_startup`** | **★★★可。⇒ 「この技の 1 段目が当たるまでのフレーム」はゲームの事実であり、確定反撃に限らない普遍的な性質である** |

**★★「NULL の間は候補から外す」は*消費者側の導出規則*である。⇒ 列には書かない。★これも審査規約に適合する。**

### 0.4 ★★★埋めるのは 109 行だけである。**全 3026 行ではない**

| 対象 | 扱い |
|---|---|
| **`category='target_combo'` かつ `is_derived` かつ `basis='standalone'` の 109 行** | **★`first_hit_startup` を埋める対象。⇒ 手入力は開発者が行う** |
| **それ以外の行** | **★NULL のままでよい。⇒ 判定側は `startup` をそのまま使う**（現状どおり） |

**★★NULL の意味は「不明」で統一する。⇒ 「1 段技だから NULL」という意味を持たせないこと。★2 つの意味を 1 つの NULL に持たせると、後から区別できない。**

### 0.5 ★★★段階的に正しくなる。**109 行を一度に埋めなくてよい**

**★開発者の判断**（`D-857`）**＝NULL の間は候補から外す。**

| 状態 | 判定 |
|---|---|
| `first_hit_startup` が入っている | **その値で判定する。⇒ 正しい候補になる** |
| **NULL**（対象 109 行のうち未入力） | **★候補から外す。⇒ 偽陽性が消える** |

**★★理由＝偽陽性**〔入らない確定反撃を信じて練習する〕**のほうが偽陰性より害が大きい。★いま既に偽陽性が出ている。⇒ 外せば今すぐ改善し、埋めた行から順に正しく戻る。**

**★前提が 1 つ更新されている＝利用者はまだいない**（開発者の逐語）**。⇒ 実害は「開発者がテスト中に誤った候補を信じる」ことである。**

### 0.6 ★★着手前の版ゲート（**`M-169` の対処**）

| # | 確かめること | 満たさないとき |
|---|---|---|
| **1** | `docs/instructions/reviews/M37-04-review-checklist.md` が存在すること | **★止まって設計卓へ請求する** |
| **2** | `docs/progress/M37-RESEARCH-01-report.md` が `main` に在ること | **★本書はこれを土台にしている** |
| **3** | **★★★`ls migrations/` の最新は `000112` である。⇒ `000113` は*欠番*である**（**2026-09-13 確定＝`D-866`。`M37-03` が払い出しを受けたが `hit_type` に `CHECK` 制約が無く消費しなかったうえ、`B15` 自体が差し戻された**）**。★本サブは `000113` を使わず `000114` を作る** | **★最新が `000112` 以外なら、自採番せず再請求**（`D-293`） |
| **4** | **`M37-01` / `M37-03` が着地していること** | **★同じ面へ触れうる。⇒ 走行中なら止まって請求する** |
| **5** | `git log --oneline -5` で枝元を報告へ書く | — |

---

## 1. 前提として読むもの

| # | 資料 | 読む箇所 |
|---|---|---|
| **★★★1** | `docs/progress/M37-RESEARCH-01-report.md` | **§4 全体**（`B06` の仮説検証。**★§4.1 名指しの実データ ／ §4.2 候補抽出の実装 ／ §4.3 件数 ／ §4.5 構造上の事実**）／ 付録 B（126 行の全数） |
| **★2** | `docs/handover/M19-DESIGN-07-frame-cost-model.md` | **§7**（**★列追加審査の規約。⇒ 本サブが通した当のもの**） |
| **★3** | `docs/design/03-data-model.md` | **§3.3 `moves`**（**★列台帳。⇒ 新列の行を足す先**）／ `startup` の行 |
| 4 | `docs/design/05-screen-design.md` | **§5.20**（確定反撃サーチ）／ §5.21（マイリスト） |
| 5 | `docs/instructions/M37-overview.md` v1.2.0 | §1.2（`B02` / `B06` の受入条件）／ §4.4 |
| 6 | `docs/process/parallel-board.md` | **`D-857`**（`P-58` の決着）／ `D-293` ／ `M19-DESIGN-07` §7 の位置づけ |

---

## 2. やること

### 2.1 ★★段 1 — 実査

| # | 確かめること | なぜ |
|---|---|---|
| **★★1** | **`M37-RESEARCH-01` §4.3 の件数を数え直す**〔`target_combo` 126 ／ derived 117 ／ derived ＋ standalone 109 ／ うち startup あり 106 ／ 非空中 damage>0 が 105〕 | **★他所から持ってきた実測値は測り直す** |
| **★★2** | **`punishfinder` の投影に `first_hit_startup` を足せるか**（`repository/punish/queries.go` ／ `ScanMove`） | **★現在は `startup` / `category` のみ。⇒ 足す先の実物を見る** |
| **★3** | **`B02`（バーンアウト）の現在地** | **★★射程が未確定である。⇒ §2.4 で扱う** |

### 2.2 ★★★段 2 — 列を足す（`000114`）

| # | やること |
|---|---|
| **★★1** | **`moves.first_hit_startup INTEGER NULL` を足す**（**★`CHECK` は既存の `startup` に倣う。⇒ 実物を見て揃えること**） |
| **★★2** | **`DES-003` §3.3 の列台帳へ行を足す原稿を、設計伝達レポート §4 へ書く**（**★製造は設計書を編集しない**＝`CLAUDE.md` §8）**。⇒ 「名前／実セマンティクス／消費者一覧」の 3 点**（`M19-DESIGN-07` §7 の列台帳規約） |
| **★★★3** | **値は 1 行も入れない。⇒ 全行 NULL で足すだけである** |
| **★4** | **CSV の列に足すかは実装判断でよい。⇒ 足すなら往復を確かめ、足さないなら理由を報告に書く** |

> **★★★手入力は開発者の手番である**（`D-857`。逐語＝「109 件なら他のマイルストーンの合間に私が作業出来る範囲ではある」）**。⇒ 本サブは*器*を作るところまでである。**

### 2.3 ★★★段 3 — 判定を変える（`B06`）

| # | やること |
|---|---|
| **★★1** | **`punishfinder` の `buildStarters` で、`category='target_combo'` かつ `is_derived` の行は `first_hit_startup` を使う** |
| **★★★2** | **同じ行で `first_hit_startup` が NULL なら候補から外す**（§0.5） |
| **★3** | **それ以外の行は `startup` のまま。⇒ 挙動を変えない** |
| **★★4** | **投影に必要な列を足す**（`startup_basis` / `is_derived` / `first_hit_startup`。**★`M37-RESEARCH-01` §4.2 が「現投影に情報が足りない」と実測している**） |
| **★5** | **除外されたことが利用者に分かる形にするかは実装判断でよい。⇒ 決めた形と理由を報告に書く** |

### 2.4 ★★`B02` — バーンアウト中の確定反撃

| # | やること |
|---|---|
| **★★1** | **段 1 で現在地を確かめる。⇒ バーンアウトが「状態」としてどこにあるか**（`characters.custom_states` ／ `opponent_stance` ／ 別の表現） |
| **★★★2** | **射程が段 1 で決まる。⇒ 大きいと分かったら、止まって設計卓へ請求すること**（**★本サブは `B06` が本体であり、`B02` を抱えて肥大させない**） |
| **★3** | **小さければ本サブで実施する。⇒ 「小さい」の判定は、既存の状態表現に載るかどうかである** |

---

## 3. やらないこと

| # | 内容 | 理由 |
|---|---|---|
| **★★★1** | **`first_hit_startup` に値を入れること** | **★§2.2-3。⇒ 手入力は開発者の手番である** |
| **★★2** | **既存の `startup` の値を変えること** | **★§0.1。⇒ データは正しい** |
| **★★3** | **初段を機械的に同定しようとすること** | **★§0.2。⇒ 実測で 59 行が判定不能である。★時間を溶かす** |
| **★4** | **列名を `starter_startup` にすること** | **★§0.3。⇒ 審査規約に反する** |
| **★5** | **`B04`〜`B15` に手を出すこと** | **★他サブの射程** |
| **★6** | **設計書本体を書き換えること** | `CLAUDE.md` §8。**⇒ 列台帳の原稿は設計伝達レポート §4 へ** |
| **★★7** | **`followup-backlog.md` の編集** | **`D-838`** |

---

## 4. ★★危険（**先に読むこと**）

### 4.1 ★★★最大の危険＝「データを直す」方向へ行くこと

**★§0.1。⇒ 開発者が「データは正しい」と明言している。★`startup` を書き換えると、技の性能表示が壊れる。**

**★★しかも `startup` は他の機能も読む共通データである**（`M37-RESEARCH-01` §4.5）**。⇒ 確定反撃の都合で書き換えると別の面へ波及する。**

### 4.2 ★★2 番目＝NULL に 2 つの意味を持たせること

**§0.4。⇒ 「不明」と「1 段技だから不要」を同じ NULL で表すと、後から区別できない。**

**★★NULL は「不明」で統一する。⇒ 「対象 109 行のうち未入力」も「対象外の 2900 行」も、どちらも NULL である。★判定側は*行の属性*で対象かどうかを決める。**

### 4.3 ★★★3 番目＝除外の範囲を広げすぎること

**★外すのは「`target_combo` かつ `is_derived` かつ `first_hit_startup` が NULL」の行だけである。**

**⇒ `target_combo` 全部を外さないこと。★非 derived 9 行と `basis='through'` 4 行は 109 の外にあり、現在の候補判定の母集団に含まれる**（`M37-RESEARCH-01` §4.3）**。**

**★★広げると、使えるはずの候補が消える。⇒ 偽陰性が増える。**

### 4.4 ★4 番目＝`B02` で肥大させること

**§2.4-2。⇒ 本サブの本体は `B06` である。★`B02` の射程が大きいと分かったら、抱え込まずに請求すること。**

---

## 5. テスト

| # | 確かめること | 判定 |
|---|---|---|
| **★★★1** | **`first_hit_startup` が入っている行は、その値で判定されること** | **★実データで。⇒ 1 行だけテスト用に埋めて確かめる形でよい**（**★コミットしない**） |
| **★★★2** | **対象 109 行のうち NULL の行が候補から*外れる*こと** | **★★★これが本サブの成果である。⇒ ryu `fuwa_triple_strike_2hits` を名指しで確かめること** |
| **★★3** | **【破壊確認】除外を外すと、`fuwa_triple_strike_2hits` が有利 5F で候補に*戻る*こと** | **★★歯止めが生きている証拠。⇒ 「出なくなった」だけでは、別の理由で出ていない可能性を排除できない** |
| **★★4** | **非 derived 9 行と `basis='through'` 4 行が候補から消えていないこと** | **★§4.3 の偽陰性の確認** |
| **★5** | **`startup` を読む他の機能が壊れていないこと**（差分 0） | |
| **★6** | **`000114` が層 A に解決すること**（`check-migration-license.sh`。**★`moves` は `GAME_TABLES` だが、DDL のみで `_data_` を持たない**） | **★実測で確かめる。⇒ 判定が想定と違ったら報告する** |
| 7 | **`go test ./...` / `pnpm test` / `make e2e`（全数）** | |

---

## 6. 完了条件

| # | 条件 |
|---|---|
| **★★1** | **§2.1 の実査 3 件が完了報告にある**（**★1 は式と件数で**） |
| **★★2** | **`moves.first_hit_startup` が足され、全行 NULL である** |
| **★★★3** | **`DES-003` §3.3 の列台帳の原稿**（名前／実セマンティクス／消費者一覧）**が設計伝達レポート §4 にある** |
| **★★★4** | **§5-2 と §5-3 の実測がある**（`fuwa_triple_strike_2hits` を名指しで） |
| **★5** | **§5-4 の偽陰性の確認がある** |
| **★6** | **`startup` の値が 1 行も変わっていない** |
| **★7** | **`B02` の判定**（本サブで実施した / 請求した）**が明記されている** |
| 8 | **§5 の検査がすべて緑**（**★出力で判定する**） |
| **★9** | **`docs/progress/M37-04-completion-report.md` ＋ `progress-log.md` への索引行** |
| **★★10** | **設計伝達レポート**（`/design_handover_report`）**。★§4 に列台帳の原稿と、`B02` の射程判定を書くこと** |

---

## 7. ★開発者への確認事項

| # | 事項 | 状態 |
|---|---|---|
| **1** | **`B02` の射程** | **★★段 1 で決まる。⇒ 大きければ止まって請求する**（§2.4-2） |
| **2** | **109 行の手入力** | **★★★開発者の手番である**（`D-857`）**。⇒ 本サブは器を作るところまで。★入力の時期は本サブの完了条件に含めない** |

---

## 8. 参照ドキュメント

| ID | パス | 参照する箇所 |
|----|------|------------|
| — | `docs/progress/M37-RESEARCH-01-report.md` | **§4 全体 ／ 付録 B**（**★本書の土台**） |
| — | `docs/handover/M19-DESIGN-07-frame-cost-model.md` | **§7**（列追加審査の規約 ＋ 列台帳） |
| DES-003 | `docs/design/03-data-model.md` | **§3.3 `moves`**（列台帳） |
| DES-005 | `docs/design/05-screen-design.md` | §5.20 ／ §5.21 |
| — | `docs/instructions/M37-overview.md` v1.2.0 | §1.2 ／ §4.4 |
| — | `docs/process/parallel-board.md` | **`D-857`**（`P-58` の決着）／ `D-293` |

---

*以上、指示書 M37-04 **v1.0.3**。* **★★本サブの芯は §0.2 である**——**初段を同定しない。⇒ 同定は実測で 59 行が判定不能であり、`command` 列は DB に無い。★値を直接持てば、その困難がまるごと消える。** **★★★次に重いのは §5-3 の破壊確認である**——**「候補から消えた」だけでは、別の理由で消えている可能性を排除できない。⇒ 除外を外すと戻ることまで見て、初めて歯止めが効いていると言える。**
