# 指示書 M29-01: 語彙・ラベルの統一（★内訳の実査から始める）

| 項目 | 内容 |
|------|------|
| 文書ID | M29-01 |
| バージョン | **1.0.0**（2026-09-06） |
| 作成日 | 2026-09-06 |
| 作成者 | 設計担当 Claude（設計卓／フェーズ4 期） |
| 対象マイルストーン | **`M29`**（`α②` 用語・ラベルの統一と入出力）。上位＝`docs/instructions/M29-overview.md` v1.1.0 |
| 推奨モデル | **高精度実装 ＋ Plan Mode 必須**（**★★i18n を全面で触る。★実査の結果に対して案を出す手番が在る**。実モデル名は `docs/human-notes/model-allocation.md`） |
| Plan Mode | **必須**（`CLAUDE.md` §9＝全体に影響するリファクタリング） |
| レビュー | **必要**（チェックリストは着手承認の手番で設計卓が起こす。**★`D-736` の運用**） |
| CHANGE 消費 | **1〜2 本の見込み**（`DES-005`＝語彙の正典。**★起票は設計卓。自採番しない**＝`D-293`） |
| マイグレ消費 | **0 本** |
| 前提サブ | **無し**（**★`M27` は完了済み**）。**★★ただし `M29-02` は本サブの後である**（`M29-overview` §3.1） |
| 配置 | `docs/instructions/M29-01-vocabulary-and-labels.md` |
| 出力 | `docs/progress/M29-01-completion-report.md` |

---

## 0. この指示書の特殊性

### 0.1 ★★最初の成果物は実装ではなく「語の割れの全数」である

**`phase4-overview` §7.1 は「用語・ラベル・入出力＝29 件」と記録しているが、★★その内訳が ledger から引けない**（`M29-overview` §2.1。計測点 `M-127`）。

**⇒ 本サブの最初の成果物は「29 件の内訳」である。★実装を直す前に、語の割れを全数で出すこと。**

**★★実査の結果が 29 でなかったら、そう報告すること。⇒ `phase4-overview` §7.1 のほうを直す**（設計卓の手番）。

### 0.2 ★★寄せ先は開発者が選ぶ

**開発者の逐語（2026-09-06）＝「基本は私が選びたい」**（`D-742`）。

**⇒ 製造は「どの語がどこに出ているか」を出し、案を添えて止まる。★製造が単独で寄せ先を決めない**（利用者に見える語＝`roles-and-routing` ハード列）。

### 0.3 ★射程は 4 群（**「ステップ」の二義は外れた**）

| # | 群 | 逐語 |
|---|---|---|
| **1** | **空レシピのラベルが 4 種** | `phase4-overview` §3 M29 |
| **2** | **コピーの言い回しが 3 種** | 同上 |
| **3** | **SA ゲージの単位「本」の有無** | 同上 |
| **4** | **相手状態の語彙** | 同上 |

**★★「ステップ」の二義はフェーズ5 以降へ送られた**（2026-09-06 開発者裁定＝`D-742`）。**⇒ 本サブで触らない。**

**★あわせて設計卓の理解の誤りを記録しておく**——**設計卓は「コンボのステップとウィザードのステップ」と読んだが、実際は「move としてのステップ（前方ステップなど）と、combo の 1 工程としてのステップ」である**（計測点 `M-128`）。

### 0.4 ★★i18n を全面で触る

**⇒ 他のサブと並列にするときは交点表を先に書くこと**（`D-740`）。**★`M30-01` / `M30-02` はタブ名・ボタン名で i18n を触りうる。**

---

## 1. 前提として読むもの

| # | 資料 | 読む理由 |
|---|---|---|
| 1 | `docs/instructions/M29-overview.md` v1.1.0 §2 | **★★29 件の内訳が引けないこと ／ 射程の再構成** |
| 2 | `docs/instructions/phase4-overview.md` §3 M29 ／ §7.1 | **射程の逐語 ／ 29 件の集計** |
| 3 | `DES-005` | **語彙の正典** |
| 4 | `docs/change-notes/CHANGE-151-notification.md` ／ `CHANGE-155-notification.md` | **★相手状態の語彙は `M27-01`（`opponent_size` 4 区分化）と `M27-03`（一覧の見出し「相手の状態」）が動かしている。⇒ as-built を読むこと** |
| 5 | `docs/handover/code-facts.md` | **★実装の現在地。想定で書かない** |

---

## 2. やること

### 2.1 ★★語の割れの全数を出す（**最初の成果物**）

**実装へ入る前に一覧を提示すること。**

| # | やること |
|---|---|
| **★★1** | **4 群それぞれについて、実際に使われている語を全数で出す。★どの語が、どの画面の、どのキーで出ているかを書くこと。** |
| **★★2** | **★★i18n のキーから引くこと。「画面に出ている文言」で数えない**（`M27-03` の教訓 7＝**「画面に出ている文言」で有無や着地先を判定しない**）**。⇒ 同じ文言が別のキーから出ている場合がある。** |
| **★★3** | **ja / en の両方を見ること。★片方だけ割れている場合がある。** |
| **★★4** | **★合計が 29 になるかを確かめ、報告に書くこと。⇒ ならなくてよい。★★ならなかったら、その数と内訳をそのまま書く**（`phase4-overview` を直すのは設計卓）。 |
| **★5** | **参照 0 のキーが在れば挙げること**（**★`M27-03` が `comboList.column.starterStatus` を削除した先例が在る**）。 |

### 2.2 ★★寄せ先の案を出して止まる

| # | 内容 |
|---|---|
| **★★1** | **4 群それぞれについて、寄せ先の案を出すこと。★1 群につき 2〜3 案。** |
| **★★2** | **★開発者が選ぶ**（`D-742`）**。⇒ 製造は選ばない。** |
| **★3** | **案には「なぜその語か」を 1 行添えること**（**★語だけ並べても選べない**）。 |
| **★4** | **★★「どれかに寄せる」以外の着地も在りうる**——**例＝「実は別のものを指していた」。⇒ その場合はそう書く。★無理に統一しない。** |

### 2.3 統一の実装

| # | 内容 |
|---|---|
| **★★1** | **★i18n のキーを 1 本へ寄せる。⇒ 参照 0 になったキーは ja / en の両方から削除すること**（`M27-03` の先例）。 |
| **★★2** | **★★`label-keys.test.ts` の「値域 ↔ キーの 1 対 1」検査を活かすこと**（`M27-03` が `HIT_TYPE_SHORT_LABEL_KEYS` で使った形）**。⇒ 統一した語に値域が在るなら、同じ検査へ登録する。★足し忘れが赤になる。** |
| **★3** | **`web/src/constants/` の定数と `internal/model/` の列挙定数の同期を崩さないこと**（`CLAUDE.md` §4。**検査＝`scripts/check-enum-sync.sh`**）。 |
| **★4** | **★相手状態の語彙は `M27-01` / `M27-03` の as-built に合わせること**（§1-4）。**⇒ 巻き戻さない。** |

### 2.4 テスト

| # | 内容 |
|---|---|
| **★★1** | **★★「画面に出ている文言」でテストしないこと**（`M27-03` の教訓 7）**。⇒ `data-testid` かキーで判定する。** |
| **★2** | **参照 0 のキーを削除したなら、それを検出するテストか検査が在るか確かめること。** |
| **★3** | **`make e2e` は全数で回す。★文言を変えると E2E が赤くなる。⇒ 赤くなった箇所は「文言で判定していた」証拠であり、報告に書くこと。** |

### 2.5 ★コミットの割り方

| 順 | コミット |
|---|---|
| **1** | **§2.1 の実査**（コードは動かない） |
| **2〜5** | **4 群を 1 群ずつ**（**★1 コミットに 2 群を混ぜない**——**赤が出たときにどの群かが分からなくなる**） |
| **6** | **参照 0 のキーの削除** |

---

## 3. 出力

`docs/progress/M29-01-completion-report.md`。

| # | 書き方の規約 |
|---|---|
| **★★1** | **§2.1 の全数**（**どの語が、どの画面の、どのキーで出ているか**）。**★これが本サブで最も価値のある成果物である。** |
| **★★2** | **★合計が 29 だったか。★★違ったら、その数と内訳** |
| **★★3** | **4 群それぞれの寄せ先と、開発者が選んだ理由** |
| **★4** | **削除した参照 0 のキー** |
| **★5** | **`make e2e` で赤くなった箇所**（**★「文言で判定していた」証拠である**）。 |
| **★6** | **`docs/design/` に反映が要る箇所の一覧**（**製造は直さない**）。 |
| **★7** | **`docs/progress/progress-log.md` の `### M29-01` へ追記**（`CLAUDE.md` §8）。 |

---

## 4. やらないこと

| # | やらないこと |
|---|---|
| **★★1** | **「ステップ」の二義に触れること**（§0.3。**フェーズ5 以降へ送られた**＝`D-742`） |
| **★★2** | **寄せ先を製造が決めること**（§0.2 / §2.2-2） |
| **★★3** | **「画面に出ている文言」で数える・テストすること**（§2.1-2 / §2.4-1） |
| **★★4** | **無理に統一すること**（§2.2-4。**★「実は別のものを指していた」なら、そう書く**） |
| **★★5** | **相手状態の語彙を `M27-01` / `M27-03` の as-built から巻き戻すこと**（§2.3-4） |
| **★6** | **入出力に触れること**（**`M29-02` の射程**） |
| **★7** | **1 コミットに 2 群を混ぜること**（§2.5） |
| **★8** | **`docs/design/` の編集**（`CLAUDE.md` §8） |
| **★9** | **`docs/handover/followup-backlog.md` の編集**（`D-382`。**§J 停止時記録だけは例外**） |
| **★10** | **CHANGE 番号・マイグレ連番の自採番**（`D-293`） |

---

## 5. 完了条件

1. **§2.1 の全数が出ており、合計が報告に書かれている**（29 でなくてよい）。
2. **4 群すべてに着地がある**（統一した ／ 案を出して止まった ／ 「別のものだった」と結論した、のいずれか）。
3. **参照 0 になったキーが ja / en の両方から消えている。**
4. **`go test ./...` / `cd web && pnpm test` / `make e2e` が緑。**
5. **常設検査が緑**（`check-artifact-integrity.sh` を 1 本目に回す。**★`check-enum-sync.sh` も見ること**）。
6. **`docs/design/` に反映が要る箇所が一覧になっている。**
7. **`docs/progress/progress-log.md` へ追記されている。**

---

## 6. ★設計卓が確かめていないこと（実査項目）

| # | 確かめること |
|---|---|
| **★★1** | **4 群の語の現物**（**★設計卓は `web/src` を読めない**＝武装中） |
| **★★2** | **`phase4-overview` の 29 件が、実際に何件になるか**（§2.1-4） |
| **★3** | **`label-keys.test.ts` の現在の登録内容**（§2.3-2） |
| **★4** | **`M27-01` / `M27-03` が相手状態の語彙をどこまで動かしたか**（§2.3-4） |

---

## 7. 開発者への確認事項

**★以下は着手前ではなく、実装の途中で返すものである。⇒ 本サブの投入はこれ待ちにしない。**

| # | 確認事項 | 設計卓の暫定案 |
|---|---|---|
| **★★1** | **4 群それぞれの寄せ先**（§2.2） | **★★案なし。⇒ 製造が §2.1 の実査とともに 1 群 2〜3 案を出し、開発者が選ぶ**（`D-742`。逐語＝「**基本は私が選びたい**」） |
| **★2** | **「実は別のものを指していた」群が出た場合、統一しないでよいか**（§2.2-4） | **★統一しないことを推す**——**無理に 1 語へ寄せると、区別が必要な場面で言い分けられなくなる** |

---

## 8. 参照ドキュメント

| ID | パス | 参照する箇所 |
|----|------|------------|
| — | `docs/instructions/M29-overview.md` | §2（射程）／ §3（サブ構成）／ §5（実査項目） |
| — | `docs/instructions/phase4-overview.md` | §3 M29 ／ §7.1 |
| DES-005 | `docs/design/05-screen-design.md` | **語彙の正典** |
| — | `docs/change-notes/CHANGE-151-notification.md` ／ `CHANGE-155-notification.md` | 相手状態の語彙の as-built |
| — | `docs/handover/code-facts.md` | 実装の現在地 |
| — | `CLAUDE.md` §4 | **列挙定数の同期**（`check-enum-sync.sh`） |

---

*以上、M29-01 指示書 **v1.0.0**。* **★★本サブの落とし穴は「語を並べて 1 つ選ぶ作業だ」と読むことである。** **★最も価値のある成果物は §2.1 の全数**〔どの語が、どの画面の、どのキーで出ているか〕**であり、統一はその後で決まる。** **★★もう 1 つは「画面に出ている文言」で数えることである**（`M27-03` の教訓 7）**——同じ文言が別のキーから出ている場合がある。⇒ i18n のキーから引くこと。**
