# M31-02 レビューチェックリスト: 共通画面部品 — キャラ選択の入力挙動

| 項目 | 内容 |
|------|------|
| 文書ID | M31-02-review-checklist |
| バージョン | **1.1.0**（2026-09-08・**★★完了後の注記を §0.0 に足した＝`D-779`**） ／ 1.0.0 |
| 対象指示書 | `docs/instructions/M31-02-shared-character-picker.md` **v1.1.0** |
| 推奨モデル | **標準レビュー**（**★★ただし「2 件だから軽い」と読ませないこと。共通部品であり波及先は全画面である**） |
| 作成者・作成日 | 設計担当 Claude（設計卓・フェーズ4 期）/ 2026-09-07 |

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| 1.0.0 | 2026-09-07 | 初版（`D-773`。**★投入の合図と同じ手番で起こした**＝`D-736` の運用） |

---

## 0.0 ★★【2026-09-08 完了後の注記＝`D-779`】束 B は全項目が対象外である

> **★★本文は書き換えない**（`D-274` (3)）**。⇒ 読むときは次を上書きして読むこと。**

**★★§2 束 B**（`P4M-012`＝`Enter` が効かない）**は全項目が対象外になった。⇒ 2026-09-08 開発者裁定＝恒久に不要・クローズ。**

**★理由＝`M27-02a`**（2026-09-02）**が実測で「`Enter` は正しく効いている」ことを突き止めており、本件は起票の時点で既決だった。★本チェックリストも指示書も `M27-02a` に 1 度も言及していない**（計測点 `M-144`）**。**

**★★§3 束 C**（`P4M-013`＝ドラッグ判定）**は有効である。⇒ 加えて `TagSelector` の同型欠陥も同じ手番で直った**（開発者が実機で確認して射程を広げた）**。★回帰は `web/e2e/m31-02-tag-field-drag.spec.ts` が固定する。**

---

## 0. レビュー担当への前置き

### 0.1 ★★2 件しかないが、単独のサブになっている（**理由がある**）

**★共通部品は他の全画面に波及する。⇒ セットプレイ・確定反撃の変更と同じコミットに混ぜると、赤が出たときに切り分けられない。**

**⇒ 「小さいから `M31-01` へ寄せればよかった」という指摘はしないこと。★分けてあること自体が設計判断である。**

### 0.2 ★★最も見落としやすい形 2 つ

| # | 形 | なぜ危ないか |
|---|---|---|
| **★★1** | **「正しい挙動」が推測で決まっている** | **★`P4M-013`**（ドラッグ判定）**は現象の記述が 1 行しかなく、何が起きてほしいのかが書かれていない。⇒ 言語化せずに直すと、直ったかどうかを誰も判定できない** |
| **★★2** | **部品が複数あるのに片方だけ直している** | **★「共通部品」と呼ばれていても、実体が 1 つとは限らない。⇒ 片方だけ直すと、直っていない画面が残り、しかもテストは緑になる** |

### 0.3 ★★E2E は入力の効き方を見ていない

**★`getByRole` は要素の存在を見るが、`Enter` が効いたかは見ない。⇒ 「spec が緑」を「直った」と読まないこと。**

### 0.4 レビュー前の準備

- `docs/instructions/M31-02-shared-character-picker.md`（全文。**とくに §0.2 と §4**）
- `docs/design/05-screen-design.md`（**キャラ選択が現れる画面**）／ §5.2（スマホ専用）
- `docs/design/testid-convention.md`（**testid を足したなら登録が要る**）

---

## 1. 束 A — 段 1 の実査 5 件

| # | 見ること | 判定 |
|---|---|---|
| **★★1-1** | **キャラ選択の共通部品がいくつあるかが、数で書かれているか** | **★★「1 つである」なら、そう確かめた根拠があるか。⇒ §0.2-2** |
| **★★1-2** | **その部品を使っている画面の全数が、`DES-005` の節と対応付けて書かれているか** | **★★これが破壊確認の区分の割り方の根拠になる** |
| **★1-3** | **`P4M-012` の再現手順があるか** | **★直す前に「壊れている」を押さえているか** |
| **★★1-4** | **`P4M-013` の「何が起きてほしいのか」が言語化され、実装より先に報告されているか** | **★★§0.2-1。⇒ ここが無ければ、以降の判定ができない** |
| **1-5** | **`M31-01` との交点**（`P4M-015`）**が実査されているか** | **★重なったら知らせたか** |

---

## 2. 束 B — `P4M-012`（`Enter` が効かない）

| # | 見ること | 判定 |
|---|---|---|
| **★★2-1** | **「効く」が何を指すかが決まってから直しているか**（選択の確定 ／ 次の入力欄へ ／ フォーム送信） | **★★どれかで挙動が全く違う。⇒ 決めずに直していないか** |
| **★★2-2** | **フォーム送信と二重に起きないことが実測されているか** | **★`Enter` でフォームが送信される画面がある。⇒ 送信が 2 回走らないこと** |
| **2-3** | **キーボードだけで操作できるようになっているか** | **★`Enter` が効かないのは、キーボード操作の入口が塞がっているという意味である** |

---

## 3. 束 C — `P4M-013`（ドラッグ判定）

| # | 見ること | 判定 |
|---|---|---|
| **★★3-1** | **段 1-4 で言語化した挙動と、実装が一致しているか** | **★言語化と実装がずれていないか** |
| **★★3-2** | **閾値を入れたなら定数化されているか** | **`CLAUDE.md` §4＝マジックナンバーは定数化。⇒ 「何 px 動いたらドラッグ」をリテラルで散在させていないか** |
| **★3-3** | **タッチ操作で壊れていないか** | **★スマホ専用画面がある**（`DES-005` §5.2）**。⇒ ポインタの種別で挙動が変わりうる** |
| **★3-4** | **推測で進めた箇所に「推測: 〜と仮定した」の明示があるか** | `CLAUDE.md` §9-3 |

---

## 4. 破壊確認（**★★対象区分を名指しで見ること**）

| # | 何を | なぜ |
|---|---|---|
| **★★4-1** | **段 1-2 で数えた画面の区分から 1 件ずつ、キャラ選択が従来どおり動くこと** | **★★区分の割り方が報告に書かれているか。⇒ 「代表を選んだ」だけでは足りない**（`SUPP-001` §5.5.4 (10)。計測点 `M-139`） |
| **★★4-2** | **`P4M-012` / `P4M-013` が、直す前は起きなかったことが示されているか**（陽性対照） | **★★無いと「もともと動いていた」と区別できない** |

**★★偏りを見ること**——**「守られている区分ばかりを試して、機構が生きている証拠にする」形が `M28-04` で実際に起きた**（`M-139`）**。⇒ 区分から 1 件ずつ取れているかを数える。**

---

## 5. テストの妥当性

| # | 見ること |
|---|---|
| 5-1 | **`cd web && pnpm test` を出力で判定しているか** |
| **★5-2** | **`make e2e-only P=<パターン>` を使っているか。⇒ `playwright` を直接叩いていないか**（`CLAUDE.md` §11） |
| **★★5-3** | **★§0.3。⇒ 「E2E が緑」を「入力が効くようになった」の根拠にしていないか** |
| 5-4 | **閾値を入れたなら、境界の両側が試されているか** |

---

## 6. 重大な問題の判定基準（**完了承認を妨げる**）

| # | 内容 |
|---|---|
| **★★1** | **`P4M-013` の「正しい挙動」を言語化せずに直した** |
| **★★2** | **部品の数を数えていない**（**⇒ 片方だけ直している可能性を排除できない**） |
| **★★3** | **破壊確認の区分の割り方が書かれていない**（`M-139` の再演） |
| **★★4** | **陽性対照が無い**（直す前は起きなかったことが示されていない） |
| **★5** | **共通部品を作り替えた**（**⇒ 2 件の是正が射程である。作り替えが要るなら請求する**） |
| **★6** | **キャラ選択の見た目を変えた**（**⇒ 波及の確認が成立しなくなる**） |
| **7** | **testid を足したのに `testid-convention.md` へ登録していない** |
| **8** | **`M31-01` の 9 件に手を出した** |

---

## 7. 軽微な問題の判定基準（**持ち越し許容**）

| # | 内容 |
|---|---|
| 1 | **交点**（`P4M-015`）**が実際には無かった** |
| 2 | 閾値の値そのものの好み（**★定数化されていればよい**） |
| 3 | タッチ操作の細部（**★壊れていなければよい**） |

---

## 8. レビュー完了の判定

- **束 A〜C の全項目に判定が付いている**
- **★★破壊確認 2 件が実測で示され、区分の割り方が書かれている**
- **重大の判定基準 8 件のいずれにも当たらない**
- **★再レビューの往復は 2 回まで**（`CLAUDE.md` §9 の停止規律）

---

*以上、M31-02 レビューチェックリスト **v1.0.0**。* **★★「2 件だから軽い」と読ませないこと**（§0.1）**——共通部品であり、波及先は全画面である。** **★★最も見落としやすいのは、「正しい挙動」が推測で決まっていることと、部品が複数あるのに片方だけ直していることである**（§0.2）**——どちらもテストは緑のまま通る。**
