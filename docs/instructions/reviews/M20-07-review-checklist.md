# M20-07 レビューチェックリスト

| 項目 | 内容 |
|------|------|
| 文書ID | M20-07-REVIEW-CHECKLIST |
| バージョン | 1.0.0 |
| 対象 | `docs/instructions/M20-07-intake-reverse-lookup.md` v1.0.0 |
| 対の CHANGE | `docs/change-notes/CHANGE-104-notification.md` |
| 作成者・作成日 | 設計卓（Claude Code・武装中）/ 2026-08-14 |

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| 1.0.0 | 2026-08-14 | 初版（指示書 v1.0.0 と対）。 |

---

## 0. 読む順（★ここから始める）

1. **§1.1 を最初に見る**（**正規化の範囲**）。**本サブで唯一、緩めすぎても誰も落ちない項目である**
2. **§1.5**（表示を 1 文字も変えていないか）
3. **§4**（テスト。とくに破壊確認）
4. 残りは順不同

> **★本サブは「照合を緩める」作業である。** **緩めた側の失敗は、赤くならない**——**別の技が同じものとして解決されても、テストも型検査も緑のまま利用者のデータへ入る。** **⇒ 判断の根拠が残っているかを見る。**

---

## 1. 設計意図との一致

### 1.1 ★正規化の範囲（§4.1・**最重要**）

| # | 確認 |
|---|---|
| 1 | **★採った範囲と、採らなかった範囲の理由が完了報告にある**（`§4.1-4`）。**「NFKC を掛けた」だけで終わっていない** |
| 2 | **★別の技が同一視される組が 0 であることを実測で確かめている**（`§3.3-2`・`§4.1-5`）。**「たぶん大丈夫」で通していない** |
| 3 | **入力側と辞書側の両方へ同じ関数を掛けている**（`§4.1-2`）。**テストがある**（§5 (h)） |
| 4 | **正規化がデータを書き換えていない**（`§4.1-3`。**照合のときだけ掛かる**） |
| 5 | **`preset_aliases` の行の値が 1 つも変わっていない**（`§2.2-1`） |

### 1.2 両引き（§4.2）

| # | 確認 |
|---|---|
| 1 | **`alias_text` と `alias_text_en` の両方を引いている** |
| 2 | **`alias_text_en` が NULL の行がフォールバックの引き金になっていない**（§5 (d)） |
| 3 | **★両引きで新たに複数件になった組の有無が報告されている**（`§4.2-3`）。**★解けていたものが解けなくなっているなら劣化である** |
| 4 | **決定論の骨格が不変**（1 件のときだけ確定・0 件/複数件は未解決） |

### 1.3 多候補ハッジの分割（§4.3）

| # | 確認 |
|---|---|
| 1 | **分割した結果が「候補」であって「確定」になっていない**（`§4.3-2`） |
| 2 | **区切りを実データで確かめている**（`§4.3-3`。**`" or "` だけと決め打っていない**） |
| 3 | **★「解けなかった」と「そもそも候補に挙がらなかった」が別に出る**（`§4.3-4`。`E-84`） |

### 1.4 一意性の実効確認（§4.4）

| # | 確認 |
|---|---|
| 1 | **カスタムプリセットが逆引きに含まれるかを実査している** |
| 2 | **★「一意制約があるから大丈夫」で終わっていない**（`§4.4-3`。**制約はプリセット内・逆引きは横断であり、1:N は当然成立する**） |
| 3 | **決定論が守られていることをテストで固定している**（§5 (g)） |

### 1.5 ★表示を 1 文字も変えていないか（§4.5）

| # | 確認 |
|---|---|
| 1 | **表示経路に diff が無い**（`§2.2-4`） |
| 2 | **正規化を表示側へ掛けていない**（`§4.5-2`） |
| 3 | **テストがあり、破壊確認済みである**（§5 (f)） |

### 1.6 スコープ外を踏んでいないか（§1.3）

| # | 確認 |
|---|---|
| 1 | **`alias_text` のデータを書き換えていない**（36 行の全角を半角へ揃える等） |
| 2 | **表示ロケール分岐を実装していない**（**逆引きと表示は別である**） |
| 3 | **曖昧一致・編集距離を入れていない** |
| 4 | **辞書を増やしていない**（SA 番号・キャラ通称等）。**★順序を逆にすると効果が測れない** |
| 5 | **`command` 記法を拡張していない** |

---

## 2. 契約・凍結インターフェース

| # | 確認 |
|---|---|
| 1 | **`internal/moveindex/` ／ `move_commands` の diff 0**（契約 F-3） |
| 2 | **`scripts/` の diff 0**（**D-335**。**`M21-06` と並走しうる**） |
| 3 | **`docs/design/` ／ `docs/change-notes/` の diff 0** |
| 4 | **`M20-03` の一意制約 3 本が不変** |
| 5 | **`DES-004` §5.3 の 3 段フォールバックが不変** |
| 6 | **`recipe_cache` の再計算ロジックと呼出契機が不変** |
| 7 | **`DES-002` §4.2 の契約が動いた場合、着手前に設計卓へ上げている**（`§2.3`） |

> **★`§2.2` は挙動の凍結であり、失効した記述の是正を妨げない**（**D-361**）。**とくに「`M20-07` が正規化の具体形を決める」という未来形は、本サブが決めた時点で失効する**（`§4.6-4`）。

---

## 3. 実装品質

| # | 確認 |
|---|---|
| 1 | `any` / `@ts-ignore` / `eslint-disable` / `console.log` の追加 0 件 |
| 2 | エラーの wrap（`fmt.Errorf("...: %w", err)`） |
| 3 | サービス層のメソッドが第一引数に `context.Context` を取る |
| 4 | **★正規化関数が 1 か所にある**（**入力側と辞書側で別々に書かれていない**＝`E-76`） |
| 5 | `TODO` コメントが `// TODO(<対応予定>): <内容>` 形式である |

---

## 4. テスト

| # | 確認 |
|---|---|
| 1 | **§5 の (a)〜(h) が実装されている** |
| 2 | **★(b)(f)(h) の破壊確認が行われ、「どのテストが赤くなったか」まで報告されている** |
| 3 | **★赤にならなかった項目について、「別の機構が代わりに守っている」を先に疑い、守っていたものを特定してから、ガードと同じ層に契約テストを置いた形跡がある**（**D-375** ／ `SUPP-001` §5.5 (10′)） |
| 4 | **(b) が「別の技が同一視されないこと」を主張している**（**単に「解決できること」ではない**） |
| 5 | `go test ./...` FAIL 0 ／ `pnpm test` 全緑 ／ `pnpm lint` exit 0 ／ `make e2e` 全緑 |
| 6 | **E2E spec を足す場合、グローバル資源を触るものは資源ごとに 1 ファイルへ寄せている**（**D-362**）**＋ 破壊確認と対照実験を置いている**（**D-375**） |

---

## 5. ドキュメント

| # | 確認 |
|---|---|
| 1 | **`docs/progress/progress-log.md` へ 1 行追記されている**（CLAUDE.md §8。**必須**） |
| 2 | **完了報告に §7.5 の 8 項目がある** |
| 3 | **§7.5-3**（**正規化の前後で解決できるようになった件数と数え方**）**がある。★数え方の併記が無い件数を通さない** |
| 4 | **`migrations/` の消費本数が報告されている**（**disk 末尾を着手時に実査した記録つき**） |

---

## 6. 否定形確認（§4.6）

| # | 確認 |
|---|---|
| 1 | **2 軸で回している**——**(a) 識別子 ／ (b) 前提を述べている散文**（**D-361**） |
| 2 | **★走査の 4 条件を満たしている**（**D-375**）——**`LC_ALL=C.UTF-8` ／ 行跨ぎまたはファイル単位 ／ 数値パターン ／ 陽性対照** |
| 3 | **★陽性対照が当たったことが報告されている**（**「回した」だけでは不足**） |
| 4 | **§4.6 の 4 項目それぞれについて結果がある** |
| 5 | **★項目 3 は「既に撤回されている」が正解である**（不可視文字は 0 件＝**D-307**）。**まだ残っている記述は残骸である** |

---

## 7. 停止規律

| # | 確認 |
|---|---|
| 1 | **§9.4 の停止条件に当たった場合、`followup-backlog` §J へ必須 5 フィールド付きで記録され、停止している** |
| 2 | **再レビュー往復が 2 回を超えていない** |
| 3 | **記録して停止した場合、それを「失敗」として扱っていない**（**正規の完了形式である**） |

---

## 8. 差し戻し事由（重大）

| # | 事由 |
|---|---|
| **重大 1** | **★正規化で別の技が同一視される組がある**（`§4.1-5`） |
| **重大 2** | **★採った範囲の理由が完了報告に無い**（`§4.1-4`） |
| **重大 3** | **正規化を入力側／辞書側の片方にしか掛けていない**（`§4.1-2`） |
| **重大 4** | **`preset_aliases` の行の値を書き換えた**（`§2.2-1`） |
| **重大 5** | **★表示の見え方が変わった**（`§4.5`） |
| **重大 6** | **正規化を表示側へ掛けた**（`§4.5-2`） |
| **重大 7** | **決定論の骨格を変えた**（1 件のときだけ確定＝`§2.2-7`） |
| **重大 8** | **曖昧一致・編集距離を入れた** |
| **重大 9** | **多候補の分割結果を「確定」にした**（`§4.3-2`） |
| **重大 10** | **辞書を増やした**（`§1.3-5`） |
| **重大 11** | **表示ロケール分岐を実装した**（`§1.3-2`） |
| **重大 12** | **一意制約を足した ／ 落とした** |
| **重大 13** | **`internal/moveindex/` ／ `move_commands` ／ `scripts/` の diff がある** |
| **重大 14** | **設計書（`docs/design/`）を編集した** |
| **重大 15** | **`DES-002` §4.2 の契約を、着手前に上げずに変えた**（`§2.3`） |
| **重大 16** | **(b)(f)(h) のテストが無い、または破壊確認をしていない** |

---

## 9. ★このレビューで判定してはいけないこと

| # | 内容 | 理由 |
|---|---|---|
| 1 | **`alias_text` の 36 行を直すべきかどうか** | **`D-307` で採らないと決めた**（**表示を変えずに照合だけを緩めるほうが影響が小さい**） |
| 2 | **辞書を増やすべきかどうか** | **`§1.3-5`。照合を直してから測る。順序を逆にすると効果が測れない** |
| 3 | **表示ロケール分岐が未実装であること** | **本サブの対象外**（followup `alias-text-en-display-and-reverse-lookup-unimplemented` の (1)） |
| 4 | **正規化の実装の置き場** | **`§9.2-1` で製造へ委ねた**（`§4.1` の制約を満たす限り） |
| 5 | **多候補の提示の見せ方の細部** | **`§9.2-2` で製造へ委ねた**（`§4.3-4` の制約は掛かる） |
| 6 | **逆引きが全プリセット横断であること** | **`DES-003` §3.9 が既に定めた仕様である**（1:N は当然成立する） |
| 7 | **設計書の文案** | **製造が文案を作らないのが正しい** |

---

*以上、M20-07 レビューチェックリスト v1.0.0。*
