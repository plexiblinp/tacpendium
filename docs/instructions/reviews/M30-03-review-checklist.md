# M30-03 レビューチェックリスト（必殺技ファミリーの導き方を「`code` のどこかにある強度語」へ広げる）

| 項目 | 内容 |
|------|------|
| 文書ID | M30-03-REVIEW-CHECKLIST |
| バージョン | **1.0.0** |
| 作成日 | 2026-09-10 |
| 対象指示書 | `docs/instructions/M30-03-special-family-code-anywhere.md` **v1.0.0** |
| 発行 | 設計卓（`D-807`） |
| 使い方 | `/review_plan` の入力。**★本書は指示書の完了条件を写したものではない。⇒ 「緑でも壊れているもの」を探すための束である** |

---

## 0. ★★レビュー担当への前置き

**★★本サブは `parseSpecialCode` を変える。⇒ 必殺技タブの母集団を決める関数であり、105 件が動く。**

**★★★本サブ最大の危険は「載った」だけを測って「壊していない」を測らないことである。⇒ 母集団を広げる変更は、広がった側しか目に入らない。**

---

## 1. 束 A — ★★★7 組の非対称（**指示書ヘッダが完了条件として名指しした点**）

| # | 見ること | 落ちる形 |
|---|---|---|
| **★★★A-1** | **`web/src/features/combo/moveSurfacing.roster.test.ts` の「素は押せるが OD が押せない 7 組」が 0 件を主張するよう更新されているか** | **★★★`M30-02` が `A1` の素を必殺技タブへ載せた結果、その OD が `A2` のままで載らない非対称ができた。⇒ 本サブが `A2` を載せれば 0 件になる。★テストを更新しないと、直ったことが記録に残らない** |
| **★★A-2** | **7 組の全数が実物と突き合わされているか**（`followup` §CL の `special-family-plain-loads-but-od-does-not`） | **★「0 件になった」を件数だけで言っていないか。⇒ 7 組それぞれが載ることを見ること** |
| **★★★A-3** | **0 件になったのが「載せたから」であることを、陽性対照で示しているか** | **★★★テストの期待値を 0 件へ書き換えるだけでも緑になる。⇒ 実装が効いた証拠にならない** |

---

## 2. 束 B — ★★★母集団を広げたことの副作用

| # | 見ること | 落ちる形 |
|---|---|---|
| **★★★B-1** | **105 件が載ったことだけでなく、**「**元から載っていたものが 1 件も落ちていない**」**ことが測られているか** | **★★★`parseSpecialCode` の 2 段目を変えると、1 段目・3 段目の判定順に影響しうる。⇒ 落ちた側はテストに出ない** |
| **★★B-2** | **未分類タブの件数が、105 件ぶん減っているか** | **★★載せた先が必殺技タブなら、未分類は同数だけ減るはずである。⇒ 減っていないなら二重に載っている** |
| **★★★B-3** | **3 列グリッドに並ぶ数が、キャラごとにどう増えたかが示されているか** | **★★★`P4M-016` で既に 273 → 342 に増えており、`jamie` 21 ／ `akuma` 20 が実測されている。⇒ さらに増えると読めなくなる。★`M30-04` の射程だが、本サブが悪化させた分は測ること** |
| **★B-4** | **`A1` を射程外にしたことが守られているか**（指示書 §3-1） | **★A1 に手が入っていたら射程外である** |

---

## 3. 束 C — ★★`M30-02` の上に立っていること

| # | 見ること | 落ちる形 |
|---|---|---|
| **★★C-1** | **`M30-02` の 3 段構造の 2 段目だけを変えているか** | **★1 段目・3 段目に手が入っていたら、`M30-02` の決着を覆している。⇒ 理由が報告に在るか** |
| **★★C-2** | **`M30-02` がマージ済みの状態で作業しているか** | **★前提サブである。⇒ マージ前の木で作ると、衝突が後から出る** |
| **★C-3** | **`terry` の `round_wave`**（`M30-02` が `000107` で是正した）**が壊れていないか** | **★強度語の判定を広げると、`_heavy` を落とした code が別の枝に入りうる** |

---

## 4. 束 D — ★★CHANGE と設計書

| # | 見ること | 落ちる形 |
|---|---|---|
| **★★D-1** | **CHANGE を自採番していないか**（`D-293`） | **★指示書は「1 本の見込み」と書いている。⇒ 番号は設計卓が払い出す** |
| **★★D-2** | **設計伝達レポートに、`DES-004` §2.1 ／ `DES-005` 第6節 への請求が書かれているか** | **★規則そのものを変える変更である。⇒ 設計書に届かないと、次に読む人が旧規則で判断する** |
| **★D-3** | **マイグレを 1 本も足していないか**（指示書＝0 本・**データを直さない**） | **★足していたら案 (ii) を採ったことになる。⇒ §0.2 が採らないと決めている** |

---

## 5. 束 E — ★共通

| # | 見ること |
|---|---|
| **★E-1** | **`bash scripts/check-import-order.sh` がベースラインどおりか**（**★レーンごとに下げない**＝`D-388`） |
| **★E-2** | **`console.log` が残っていないか**（`CLAUDE.md` §10） |
| **★E-3** | **完了報告が `bash scripts/check-md-emphasis.sh <file>` を通っているか**（`D-775`） |
| **★E-4** | **`docs/progress/progress-log.md` へ索引行が 1 行足されているか**（`CLAUDE.md` §8） |
| **★★E-5** | **「推測:〜と仮定した」が要る箇所に書かれているか**（`CLAUDE.md` §9-3） |

---

*以上、M30-03 レビューチェックリスト **v1.0.0**。* **★★本書が最も見たいのは束 A-3 と束 B-1 である**——**どちらも「緑になったが何も直っていない」を通す形だからである。**
