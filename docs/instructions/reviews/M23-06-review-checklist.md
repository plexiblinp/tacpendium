# M23-06 レビューチェックリスト

| 項目 | 内容 |
|------|------|
| 対象指示書 | `docs/instructions/M23-06-trash-columns-and-presentation.md` v1.0.0（ゴミ箱の列と見せ方） |
| 文書ID | M23-06-REVIEW |
| 対象指示書ID | M23-06 |
| レビューモデル | 高精度レビュー（実モデル名は `docs/human-notes/model-allocation.md` が正本。実使用は開発者判断） |
| バージョン | 1.0.0 |
| 作成者・作成日 | 設計担当 Claude（設計卓／M23 起票期）/ 2026-08-22 |

> 文書ID ⇄ 実パスの正は `docs/handover/docs-map.md`（自動生成）。

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| 1.0.0 | 2026-08-22 | 初版（指示書 v1.0.0 と対）。**最重要ゲート＝「一括操作が種別に応じて正しい API を叩くこと」と「ルート列を消しセットプレイのレシピは出すという非対称が保たれていること」。** |

---

## 0. レビュー担当への前置き

### 0.1 レビュー前の準備

1. **★指示書 §1.2 / §1.3 を先に読む**——**ルート列と名無しセットプレイは同じ根から出た症状であり、本サブは逆の答えを出す。**
2. **`architecture-patterns` §11 を読む**（同じ根に逆の答えを出す判断の型）。
3. **完了報告に Plan Mode の実査 9 件（指示書 §3.3）と破壊確認 3 件（§5.2）が揃っていることを先に確認する。**

### 0.2 レビューの基本姿勢

- **★★「ルート列も出せるはずだ」を指摘しないこと。** **技術的には出せる。出さないのは判断である**（指示書 §1.3 / §11-1）。
- **★「一貫していない」を理由に非対称を指摘しないこと。** **軸は「その情報が無いと利用者が困るか」であって「技術的に出せるか」ではない。**
- **★本サブは画面のサブである。** サーバ側の削除の副作用を直していないことを指摘しない。

### 0.3 ★重大でないもの（**誤判定しないこと**）

| # | 誤判定しやすい状態 | 実は仕様である理由 |
|---|---|---|
| N-1 | **ルート列が消えている**（空表示ではなく列ごと） | **指示書 §4.1。★「存在しない概念の列を残さない」**（**D-458** で残日数に適用したのと同じ論理）。**開発者の逐語もある** |
| N-2 | **ルート列は消したのにセットプレイのレシピは出している** | **指示書 §1.3。★意図的な非対称である。** **コンボは `memo` と始動状況で識別できるが、セットプレイは名前が空だと識別手段がゼロになる** |
| N-3 | **論理削除で `recipe_cache` を消すのをやめていない** | **指示書 §4.3-4 / §1.6-4。★採らないと決めた。** 削除の副作用は `DES-002` §4.2 の契約であり、`M23-03` が固定したばかりである |
| N-4 | **`resolve-setup-recipe` が撤去されていない** | **指示書 §4.3-2。★使う側で決着した**（**D-513** の差し戻しの決着）。**撤去しないことが正解である** |
| N-5 | **行クリックが 404 のまま ／ ゴミ箱へ入れる導線が無い ／ 紐付け解除の導線が無い** | **指示書 §1.6-1 〜 3。`M23-07`** |
| N-6 | **サーバ側の削除・完全削除が直っていない**（CASCADE / Tx 位置 / 削除日時） | **指示書 §1.6-5。`M23-08`** |
| N-7 | **タグ列のために新しいデータ取得を追加していない** | **指示書 §4.2。★データは既に応答に載っている。取りに行ったらそれが誤りである** |
| N-8 | **選択状態がブラウザストレージへ保存されていない** | **指示書 §4.4-3。★画面内の一時状態である** |
| N-9 | **警告の VAL コードで分岐していない**（`details` のキーで分岐している） | **指示書 §4.6-3。★意図どおりである。** 警告の種類は増えうる |

### 0.4 報告フォーマット

- 各節「OK / 重大（§9）/ 軽微（§10）/ 質問（§11）」。

---

## 1. 設計・パターンとの照合（最重要）

- [ ] **ルート列が、見出し・セル・i18n キーのすべてで消えている**（指示書 §4.1）。**★空表示に変えただけになっていないこと。**
- [ ] **タグ列が描画されている**（同 §4.2）。**★新しくデータを取りに行っていないこと。**
- [ ] **タグ 0 件の表示が一覧画面と揃っている**（同 §4.2。**★ゴミ箱専用の新しい見せ方を作っていない**）。
- [ ] **★★名無しセットプレイがレシピ文字列で識別できる**（同 §4.3）。
- [ ] **★レシピ文字列がサーバ側で解決されている**（同 §4.3-3）。**★クライアント側で組み立て直していないこと**（表記規則を 2 か所に持つことになる＝`E-76`）。
- [ ] **★N+1 になっていない。方式と問い合わせ回数が報告に在る**（同 §4.3-3）。
- [ ] **★★一括操作が、選んだ種別に応じて正しい API を叩く**（同 §4.4-1）。**★コンボの id でセットプレイの API を叩く経路が存在しないこと。**
- [ ] **「全選択」の対象が表示中のテーブルの中だけである**（同 §4.4-2）。
- [ ] **選択状態がブラウザストレージへ保存されていない**（同 §4.4-3）。
- [ ] **警告の分岐が `details` のキーで行われ、VAL コードがハードコードされていない**（同 §4.6-3）。
- [ ] **単件復元で、警告が無くても完了トーストが出る。警告があるときは 1 枚に畳まれている**（同 §4.6-1）。
- [ ] **一括復元で「どの件に警告が付いたか」が出る**（同 §4.6-3）。**★新しくデータを取りに行っていないこと。**
- [ ] **不活性なテストが作り替えられている**（同 §4.7）。**★括弧を足しただけになっていないこと。**

## 2. データ・API 契約・スキーマの不変（非破壊性）

- [ ] **スキーマを変えていない。マイグレを 1 本も足していない**（指示書 §4.8）。
- [ ] **削除・復元・完全削除の副作用が変わっていない**（同 §2.2 / §4.3-4）。
- [ ] **`M23-04` が作った `warnings` の応答の形が変わっていない**（同 §2.2）。
- [ ] **`VAL-C08` / `VAL-S03` / `VAL-R01`〜`R04` の判定内容が変わっていない**（同 §2.2。**★本サブが触るのは見せ方だけである**）。
- [ ] **削除済みセットプレイ一覧の応答へのフィールド追加が、既存クライアントを壊さない形である**（同 §4.3-3）。

## 3. 画面の仕様

- [ ] **完了報告に最終的な列構成が逐語で書かれている**（指示書 §4.5）。**★`CHANGE-127` の反映がこれ待ちである。**
- [ ] **列見出しの i18n が ja / en の両方に在る。**
- [ ] **消した列の i18n キーが残っていない**（同 §4.1）。
- [ ] **レシピ文字列が長いときに行が壊れない**（同 §9.2。**★省略するなら全文を見る手段が在ること**）。

## 4. テストの妥当性（ケース数で確認。指示書 §5 ↔ §7 DoD）

- [ ] **フロント 12 件が揃っている**（指示書 §5.1）。
- [ ] **★★§5.1-4（一括操作の API の振り分け）が在る。** **★取り違えると別のデータが消える。**
- [ ] **★§5.1-11（未知の VAL コードで壊れない）が在る**（**★ハードコードしていないことの唯一の証拠である**）。
- [ ] **§5.1-7（レシピ文字列が空でも壊れない）が在る。**
- [ ] **★★破壊確認 3 件が実施され、コマンドと出力が完了報告に在る**（同 §5.2）。
- [ ] **★破壊確認 3 が「緑のまま通ること」を確かめている**（同 §5.2-3。**★ここだけ赤くならないことが期待である**）。
- [ ] **★フロントテストが実 `ja.json` を引く `t` で書かれている**（同 §5.1 の末尾。**キー返しモックでは文面の主張が空振りする**＝`M23-04` 教訓 2）。
- [ ] **E2E 2 本が足されている**（同 §5.4）。
- [ ] **テスト結果が件数付きで報告されている**（同 §7.2。**★終了コードだけを根拠にしていない**＝`E-125`）。

## 5. 設計意図との整合（精神の確認）

- [ ] **★否定形確認 3 件が完了報告に理由付きで在る**（指示書 §4.9）。
- [ ] **★「消す」と「出す」の非対称の理由が、実装のコメントか報告に現れている**（同 §1.3）。
- [ ] **★`resolve-setup-recipe` を「使う側で決着させた」と明記されている**（同 §4.3-2）。
- [ ] **★完全削除が不可逆であることが、識別手段の設計に反映されている**（同 §4.3-1）。

## 6. コード品質・規約遵守

- [ ] **`CLAUDE.md` §4 の TypeScript / Go 規約。** JSON タグ camelCase。
- [ ] **`console.log` が本番コードに残っていない。**
- [ ] **選択状態の型が明示されている**（`any` を使っていない）。
- [ ] **`TODO` が `// TODO(<対応予定>): <内容>` 形式である。**

## 7. 既存挙動の温存（非破壊性）

- [ ] **既存のフロントテストが全て緑である**（件数付き）。
- [ ] **`M23-01`〜`M23-05` が足したテストが緑のままである**（指示書 §5.5）。
- [ ] **`go test ./...` / `make e2e` が緑である。**
- [ ] **ゴミ箱以外の画面が変わっていない**（指示書 §1.6-7）。

## 8. ドキュメント・進捗ログ

- [ ] **`docs/progress/progress-log.md` へ 1 行追記されている**（`CLAUDE.md` §8）。
- [ ] **★レビュー結果を参照する記述が、レビュー後に埋められている**（指示書 §7.4。**`M23-04` 教訓 3**）。
- [ ] **完了報告に指示書 §7.5 の 7 点が揃っている。**
- [ ] **★設計書本体（`DES-005` / `DES-002`）が編集されていない**（反映は設計卓が `CHANGE-127` で行う）。
- [ ] **`bash scripts/check-browser-storage-keys.sh` が緑である**（指示書 §7.3）。
- [ ] **`followup-backlog.md` へ書いた場合、§J だけを触っている**（**D-382**）。

---

## 9. 重大な問題の判定基準（完了承認を妨げる）

- **★★一括操作が種別を取り違えて API を叩きうる**（指示書 §4.4-1）。**これが最重要ゲートである。** **★別のデータが消える。**
- **★★§5.1-4 のテストが無い。**
- **★§5.1-11（未知の VAL コードで壊れない）が無い、または VAL コードがハードコードされている**（同 §4.6-3）。
- **★破壊確認 3 件が実施されていない、または出力が報告に無い**（同 §5.2）。
- **ルート列が「空表示」に変えられただけで残っている**（同 §4.1）。
- **名無しセットプレイが依然として識別できない**（同 §4.3）。
- **レシピ文字列をクライアント側で組み立て直している**（同 §4.3-3。**表記規則を 2 か所に持つ**）。
- **タグ列のために新しいデータ取得を追加している**（同 §4.2）。
- **論理削除の副作用を変えている**（同 §4.3-4 / §1.6-4）。
- **`resolve-setup-recipe` を撤去している**（同 §4.3-2）。
- **選択状態をブラウザストレージへ保存している**（同 §4.4-3。**★台帳未記載なら `CLAUDE.md` §10.X 違反**）。
- **不活性なテストが括弧を足しただけで直されている**（同 §4.7。**★何も検証しない状態が残る**）。
- **スキーマ変更・マイグレの追加がある**（同 §4.8）。
- **完了報告に最終的な列構成の逐語が無い**（同 §4.5。**★`CHANGE-127` が反映できない**）。
- **スコープ外 8 件（同 §1.6）のいずれかへ踏み込んでいる。**
- **指示書 §7.5 の 7 件のいずれかが完了報告に無い。**

## 10. 軽微な問題の判定基準（持ち越し許容）

- 一括選択の型の形（指示書 §4.4 の (a) / (b) のどちらでもよい）。
- 列の並び順・幅・見出しの文言。
- レシピ文字列の省略の仕方（**★全文を見る手段が在れば足りる**）。
- 警告のトーストの文面。
- テストのデータ組み立ての作法。

## 11. 質問・確認事項のフォーマット

- 「指示書 §X.X / `DES-00X` §X / `M23-RESEARCH-01` 報告 §X に対し実装が Z。意図確認したい」の形で根拠節併記。

## 12. レビュー完了の判定

- §1〜§8 が全て OK、§9 重大ゼロ（**特に「一括操作の振り分け」「VAL コードのハードコード」「破壊確認 3 件」「列構成の逐語」**）、§0.1 の Plan Mode 実査 9 件が揃っている。§10 軽微は持ち越し可。

### 12.1 ★本チェックリスト自身の検査（**発行前に設計担当が行う**）

- [ ] **本書と対の指示書の間に、節番号の食い違いが無い。**
  → 本書が参照する指示書の節は §1.2 / §1.3 / §1.6 / §2.2 / §3.3 / §4.1 / §4.2 / §4.3 / §4.4 / §4.5 / §4.6 / §4.7 / §4.8 / §4.9 / §5 / §7 / §9 / §11 であり、**指示書側に実在する**。
- [ ] **指示書が禁じていない事項を「重大」として求めている項目が無い。**
  → §9 の各項目は指示書 §1.6 / §2.2 / §4 の禁止・固定と 1 対 1 で対応する。
- [ ] **達成不能な条件を重大として求めていない。**
  → **「ルート列にレシピが出ること」を求めていない**（§0.3 N-1 / N-2。**★消すのが判断である**）。
  → **「コンボとセットプレイで扱いが揃っていること」を求めていない**（§0.3 N-2。**★非対称が意図である**）。
  → **「`recipe_cache` を消すのをやめること」を求めていない**（§0.3 N-3。**採らないと決めた**）。
  → **「行クリックが 404 でなくなること」を求めていない**（§0.3 N-5。**`M23-07`**）。

---

*以上、M23-06 レビューチェックリスト v1.0.0。配置 `docs/instructions/reviews/M23-06-review-checklist.md`。指示書 v1.0.0 と対。「一括操作の振り分け」と「消す／出すの非対称」を最重要ゲートとする。*
