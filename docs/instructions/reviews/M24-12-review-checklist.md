# M24-12 レビューチェックリスト: エディタの作り替え（1 カラム縦積み ＋ タブ ＋ キーボード中心の入力）

| 項目 | 内容 |
|------|------|
| 文書ID | M24-12-review-checklist |
| バージョン | 1.2.0 |
| 対象指示書 | `docs/instructions/M24-12-editor-rebuild.md` **v1.2.0** |
| 推奨モデル | **高精度レビュー**（**★★`M24-04` が入れたばかりの機構を壊していないかの照合が中心である**） |
| 作成者・作成日 | 設計担当 Claude（設計卓・M24 期）/ 2026-08-28 |

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| 1.2.0 | 2026-08-29 | **指示書 v1.2.0 へ追随**（**D-583**）。 **★§0.3 へ 2 件を追加**——**(14) `type=flag` が Switch でなくなったこと**〔**★`DES-005` §5.7 項目6 を撤回した**〕**／ (15) 仮登録トグルが Switch のまま残っていること**〔**★逆に替えていたら「中」**〕。 **★§1 へ 1-23 〜 1-25 を新設。★★1-23（`false` を書いていないか）は「重大」に置いた**——**VAL 非連動であり BE は止めない。⇒ 黙って壊れる。** |
| 1.1.0 | 2026-08-29 | **指示書 v1.1.0 へ追随**（**D-582**）。 **★§0.3 へ 3 件を追加**——**(11) レシピ未入力時の保存ボタンに手が入っていないこと**〔**★別サブ＝`P-45`。手を入れていたら逆に「重大」**〕**／ (12) ゲージ欄に 0.5 刻みでない値が入ること**〔**★開発者が承知のうえで許容と裁定した**〕**／ (13) SA 側のラベルが変わっていないこと。** **★§1 へ 1-18 〜 1-22 を新設**（ゲージ欄 ／ 仮登録トグル ／ タグ欄）。**★§8 へ「上限クランプを外した」「保存ボタンに手を入れた」を重大として追加。** |
| 1.0.0 | 2026-08-28 | 初版（**D-581**） |

---

## 0. レビュー担当への前置き

### 0.1 レビュー前の準備

1. 指示書 **v1.2.0** と `CHANGE-138` 通知書 **v1.2.0** を全文読む。
2. 完了報告 `docs/progress/M24-12-completion-report.md` を読む。
3. **★★`DES-005` §5.7 を読む**——**本サブが置き換える節（「幅で役割が変わる」）と、守る節（エラーの在り処 ／ 離脱ガード）が同じ節に同居している。**
4. **★`docs/change-notes/change-report-137.md` を読む**（**`M24-04` の as-built。★守るものの出所**）。
5. **★`DES-006` §2.4 を読む**（**`opponent_stance` の既定**）。
6. **★★`DES-006` §2.2 / §2.5 と `REQ-001` FR009 / FR305 を読む**（**★仮登録の緩和ルールと、ゲージ消費列の範囲 VAL 非連動。§0.3-11 / -12 の判定に要る**）。

### 0.2 レビューの基本姿勢

コードを変更せず、指摘のみを行う。

### 0.3 ★★重大でないもの（**誤判定しないこと**）

| # | これは欠陥ではない |
|---|---|
| **1** | **★★`M24-04` の「幅で役割が変わる」形が無くなっていること。** **指示書 §1.2 / §4.1 がそれを本サブの目的そのものとしている**（`P-44` の決着＝`D-580`）。**⇒ 「`CHANGE-137` の as-built と違う」を欠陥と判定しないこと** |
| **2** | **★★「メタデータを隠す」トグルが消えていること。** **指示書 §4.1.2 が「消してよい」を明示している。★ただし理由の報告は要る** |
| **3** | **★`CollapsibleFieldset`（折りたたみ）が撤去されていること、または残っていること。** **どちらも指示書 §3.3-5 の実査結果次第である。★理由の報告は要る** |
| **4** | **★★「編集中の画面遷移時データ保持」が実装されていないこと。** **指示書 §1.7-1 がスコープ外としている**（`REQ-001` §7 フェーズ3 の独立項目）。**★ただし「満たしていない」の明記は要る** |
| **5** | **★`/setups/:setupId` が作り替えられていないこと。** **指示書 §1.7-2 がスコープ外としている。★ただし「及ばない範囲」の報告は要る**（7-5） |
| **6** | **★キャラ選択がボタン化されていないこと。** **開発者が例外と明言した**（指示書 §1.3-3 / §1.7-3） |
| **7** | **★選択肢が 10 を超える欄がボタン化されていないこと。** **開発者の線引きである**（**「10を超えるものはボタン化の対象外」**）。**★ただし一覧の報告は要る**（1-6） |
| **8** | **★ハイライトの色、Boolean のトグルキー、ボタンの並べ方が設計卓の想定と違うこと**（指示書 §9.2 で委任済み） |
| **9** | **★既存 E2E spec が多数書き換えられていること。** **★★それが本サブの本命の作業量である**（指示書 §5.2）。**⇒ 差分の大きさそのものを欠陥と判定しないこと** |
| **10** | **`web/CLAUDE.md` の台帳に差分があること**（キーを新設・変更した場合。**`CHANGE-138` の射程内**） |
| **★★11** | **★★レシピ未入力時の保存ボタンに手が入っていないこと。** **指示書 §1.7-10 がスコープ外としている**〔**要件の話であり別サブ＝`P-45`**〕。**★★逆に手が入っていたら「重大」である**（8 を見よ） |
| **★★12** | **★★ゲージ欄に `0.5` 刻みでない値（`1.3` 等）が入るようになったこと。** **★開発者が承知のうえで許容すると裁定した**（`D-582`）。**⇒ 「不変条件が壊れた」と判定しないこと。★指示書 §4.9.1 が「実装が緩い」ではなく「仕様である」と明記している** |
| **★★14** | **★★`type=flag` が Switch でなくなり、ボタン群になっていること。** **指示書 §4.12 が `DES-005` §5.7 項目6 の「type=flag はトグル＝Switch 表記」を撤回している**（`D-583`）。**⇒ 「設計書と違う」を欠陥と判定しないこと** |
| **★15** | **★仮登録トグル（`ComboDraftToggleField`）が Switch のまま残っていること。** **指示書 §4.12.4 がそう定めている**〔**整合の相手が変わっていないため**〕。**★★逆に替えていたら「中」**（1-25） |
| **★13** | **★SA ゲージ 2 欄のラベルが変わっていないこと**（指示書 §4.9.2。**★開発者が名指ししていない**）。**★あわせて followup `sa-gauge-unit-suffix-split` が解消していないこと** |

### 0.4 報告フォーマット

指摘は **重大 / 高 / 中 / 低** で分類し、各件に**根拠**（ファイル:行 または 出力の逐語）を添える。

---

## 1. 設計・パターンとの照合（最重要）

| # | 観点 | 判定 |
|---|---|---|
| **1-1** | **★★離脱ガードの 4 経路がすべて残っているか**〔画面内リンク ／ ブラウザバック ／ リロード・タブを閉じる ／ 画面内のボタン〕（指示書 §4.6-1） | **★★1 つでも落ちていたら「重大」**——**`CHANGE-137` の as-built であり、`DES-005` §5.7 に載っている** |
| **1-2** | **★★保存後の遷移が離脱ガードの補正を通っているか**（§4.6-3） | **★★通っていないなら「重大」**——**「離れたつもりで編集画面自身へ戻る」＝離れられない状態になる。★`M24-04` の手動確認で初めて出た欠陥であり、作り替えで最も再発しやすい** |
| **1-3** | **★保存の成功で dirty が落ちるか**（§4.6-2） | **落ちないなら「重大」** |
| **1-4** | **★離脱の入口が「どこへ」か「戻る」かの形で受け取られているか**（§4.6-4） | **任意の関数を預かる形なら「高」**——**補正の仕方が決まらなくなる** |
| **1-5** | **★★エラーの在り処がタブ見出しから分かるか**（§4.1） | **★★分からないなら「重大」**——**`CHANGE-137` の落とせない条件をそのまま引き継いでいる** |
| **1-5b** | **★エラーの振り分けが添字付きの前方一致か**（**BE は `steps[0].moveId`、FE は `steps.0.moveId`**） | **完全一致だけなら「高」**——**基本情報タブへ誤計上し、直す欄の無いタブへ利用者を誘導する** |
| **1-5c** | **★数えているのが ERROR だけか**（警告を混ぜていないか） | 混ざっていたら「中」 |
| **1-6** | **★★ボタン化の対象の全数と各欄の選択肢の個数が母数付きで報告されているか。★10 を超える欄の一覧があるか**（§3.3-3 / §4.3） | **母数の欠落は「高」。一覧の欠落は「高」**——**開発者の線引きが守られたかを判定できない** |
| **1-7** | **★★タブの切替でアンマウントしていないか**（1 つの DOM を CSS で出し分けているか。§4.1） | **★★アンマウントする形なら「重大」**——**切替で入力が消える** |
| **1-8** | **★どちらのタブに居ても保存できるか**（§4.5） | **タブを切り替えないと保存できない形なら「重大」** |
| **1-9** | **★★畳まれたセクションへハイライトが移るとき、そのセクションが開いてから移っているか**（§4.4.2） | **飛ばしているなら「高」**——**利用者は「入力できない欄」に到達したことに気づけない。★止まるなら「高」**——**順送りが詰まる** |
| **1-10** | **★メモ欄で順送りが無効になっているか。抜けるのが Tab またはクリックか**（§4.4.1） | **無効になっていないなら「高」**——**開発者が明示的に指定した** |
| **1-11** | **★編集モードで順送りが強制されていないか**（§4.4.1） | **強制されているなら「中」**——**開発者は「必要な箇所を直接クリックして変更すればいい」と述べている** |
| **1-12** | **★幅による分岐が消えているか**（§4.1。**lg 未満 / lg 以上で形が違わないか**） | **残っているなら「高」**——**本サブの目的そのものである** |
| **1-13** | **★`opponent_stance` の既定が `any` のままか**（§2.2） | **★空文字へ戻っていたら「重大」**——**保存値が変わり、重複判定キーと一覧フィルタで別の値になる** |
| **1-14** | **★ゲージ欄のラベルが `CHANGE-137` の形のままか**（**上限が書き戻されていないか**） | **書き戻されていたら「高」** |
| **1-15** | **★§3.3 の実査 12 件が実施され、結果が報告されているか** | 実査の欠落は「高」 |
| **1-16** | **★モードによる差が保存後の遷移先だけか**（新規 / 編集 / コピー でタブの形が同じか） | 形が違うなら「中」 |
| **1-17** | **★★ボタンの部品が汎用部品として 1 つ置かれているか**（§4.3。**同じ形を各欄へ複製していないか**） | **複製されているなら「高」**——**選択肢の見た目がすぐ割れる** |
| **★1-18** | **★★上限クランプ（`clampNumericString`）が残っているか**（§4.9.1 / §3.3-13） | **★★外れていたら「重大」**——**`DES-006` §2.5 が「範囲は UI クランプで担保」と定めており、消費列には BE の範囲検証が無い。⇒ 外すと上限がどこにも無くなる。★スピナーとは別物である** |
| **★1-19** | **★★非正準表記（`6.0` / `06` / `.5` / `6.`）の扱いが実査され、CSV 出力 → 再取込が `VAL-I05` に落ちないことが確かめられているか**（§3.3-14） | **★実査が無いなら「高」**——**`VAL-I05` は非正準表記をエラーとする。手入力を主にした時点で前面に出た論点である** |
| **★1-20** | **★ゲージ欄のラベルが §4.9.2 の表のとおりか**（**ドライブ始動＝`ドライブゲージ(0〜6.0本)` ／ ドライブ消費＝`ドライブゲージ消費` ／ SA 2 欄は変更なし**）。**★4 通りが 1 か所で組み立てられているか**（`M24-04` の形） | **表と違うなら「高」。組み立てが散っていたら「高」**——**`M24-04` が 1 か所へ寄せたばかりである** |
| **★1-21** | **★★仮登録トグルが編集モードでも最上部に在り、ラベルが 2 つの版で同一か**（§4.10） | **★位置が末尾のままなら「高」。★★ラベルが 2 つの版で違うなら「高」**——**同じ機能に 2 つの呼び名を作らない**（`opponent-stance-vocabulary-split` の同型） |
| **★1-22** | **★タグ欄が順送りで開き、Tab の素通りでは開かないか**（§4.11）。**★開いた後の操作がクリック時と同一か** | **フォーカスだけで開くなら「中」**——**Tab の素通りで毎回開いて煩い。★作法が割れていたら「高」** |
| **★★1-23** | **★★`type=flag` の「いいえ」で `undefined` が保存されているか**（§4.12.2。**`false` を書いていないか**） | **★★`false` を書いていたら「重大」**——**`CHANGE-040` 以来の消去意味論に反する。★`custom_states` は VAL 非連動であり BE は止めない。⇒ 黙って壊れる。★破壊確認 8 が赤くならないなら、意味論はどこにも守られていない** |
| **★1-24** | **★`type=flag` が数字キーだけで選べ、他の 5 欄と同じ作法になっているか**（§4.12）。**★初期状態で「いいえ」が選択済みに見えるか** | **作法が割れているなら「高」**——**本項の目的そのものである。★「未選択」の第 3 の見た目を出していたら「中」** |
| **★1-25** | **★仮登録トグルの UI 種別が Switch のまま残っているか**（§4.12.4） | **ボタン群に替えていたら「中」**——**指示書が「位置とラベルは変えるが UI 種別は変えない」と分けている** |

---

## 2. データ・API 契約・スキーマの不変（非破壊性）

| # | 観点 | 判定 |
|---|---|---|
| **2-1** | **★★`internal/` ／ `cmd/` ／ `migrations/` の差分が 0 バイトか**（§2.2） | **差分があり §2.3 の 3 条件の報告が無いなら「重大」** |
| **2-2** | **`DES-002` §4.2 の経路表に載る API を新設・変更していないか** | 変更していて報告が無いなら「重大」 |
| **2-3** | **`internal/service/notation/` ／ `internal/service/preset/` に触れていないか**（契約 F-1） | 触れていたら「重大」 |
| **2-4** | **マイグレーションを消費していないか**（見込みは 0 本） | 消費していて報告が無いなら「高」 |
| **2-5** | **`DES-005` §5.7 の保存後の遷移先の表（4 モード）が変わっていないか**（`CHANGE-130`） | 変わっていたら「重大」 |
| **2-6** | **新規依存が追加されていないか**（`CLAUDE.md` §6） | 追加されていて提案の記録が無いなら「重大」 |

---

## 3. ブラウザストレージ（**`CLAUDE.md` §10.X**）

| # | 観点 | 判定 |
|---|---|---|
| **3-1** | **キーを新設・変更した場合、`web/CLAUDE.md` §1 の台帳へ登録されているか** | 未登録なら「重大」 |
| **3-2** | **`web/src/lib/browser-storage.ts` 経由で読み書きしているか**（try-catch ／ キーバージョニング ／ JSON シリアライズ） | 直接 API を叩いていたら「高」 |
| **3-3** | **機密情報・DB 永続化対象データを入れていないか**（**★入力途中のコンボ本体を入れていないか**） | **★入れていたら「重大」**——**「編集中の画面遷移時データ保持」はスコープ外であり、ここで先取り実装しない** |
| **3-4** | **`bash scripts/check-browser-storage-keys.sh` が緑か** | 赤なら「高」 |

---

## 4. フロントエンドの動作仕様

| # | 観点 | 判定 |
|---|---|---|
| **4-1** | **タブ割りが `D-578` の指定どおりか**〔**タブ 1＝基本情報 / キャラ固有状態 / 起き攻め / その他情報 ／ タブ 2＝レシピ / セットプレイ**〕 | 違っていて理由も無いなら「高」 |
| **4-2** | **セクションの区切りが残っているか**（§4.1。**6 セクションを 2 タブへ畳むのであって、セクションを潰すのではない**） | 潰れていたら「中」 |
| **4-3** | **基本情報タブが 1 カラム縦積みか**（§4.2） | 2 カラムが残っているなら「高」 |
| **4-4** | **有効なボタンの背景色が変わるか**（§4.3） | 変わらないなら「中」 |
| **4-5** | **ボタンに数字キーのショートカットが付いているか。★10 個までか**（§4.3） | 11 個以上へ割り当てていたら「高」 |
| **4-6** | **保存ボタンがレシピタブの下にあるか**（§4.5） | 違うなら「中」 |
| **4-7** | **「レシピへ」ボタンがメモの後にあるか**（§4.4.1） | 無いなら「中」 |
| **4-8** | **従来の「一覧に戻る」ボタンがそのままか。★新しい戻るボタンが足されていないか**（§1.3） | 足されていたら「中」——**開発者が撤回した依頼である** |
| **4-9** | **★`SM-067` の前提が確かめられ、報告されているか**（§3.3-9） | 報告が無いなら「高」 |
| **4-10** | **★トーストの見え方が保存ボタンの移動後も確かめられているか**（§3.3-10） | 確かめていないなら「中」 |

---

## 5. テストの妥当性

| # | 観点 | 判定 |
|---|---|---|
| **5-1** | **E2E 7 ケースが揃っているか**（§5.1） | 欠けているなら「高」 |
| **5-2** | **★★E2E (3) が「確認が出ない」だけでなく「編集画面へ戻らない」まで見ているか** | **★見ていないなら「高」**——**`M24-04` の欠陥はまさにそこで、確認が出ないだけでは通ってしまう** |
| **5-3** | **★★書き換えた既存 spec の母数が報告されているか**（§5.2-1） | 母数の欠落は「高」 |
| **5-4** | **★★書き換えで spec の検証内容が緩められていないか**（§5.2-3。**タブを開く手順を足したのか、アサーションを削ったのか**） | **緩められていたら「重大」** |
| **5-5** | **★落とした spec があるなら、本数と理由が報告されているか**（§5.2-4） | 報告が無いなら「高」 |
| **5-6** | **`M24-04` 完了時点の passed 数と突き合わされているか**（§5.2。**★同一セッション内の背中合わせで採られているか**） | 別時点の値どうしを引き算していたら「高」 |
| **5-7** | **純粋関数・コンポーネントのテストが §5.1 のとおり揃っているか** | 欠けているなら「中」 |
| **5-8** | **`getByRole` の `name` の部分一致に足をすくわれていないか**（**破壊確認が「追記」ではなく「置換」で行われているか**） | 追記で行っていたら「高」——**空振りする** |

---

## 6. 破壊確認（**指示書 §5.3・8 件**）

| # | 観点 | 判定 |
|---|---|---|
| **6-1** | **8 件すべて実施され、コマンドと出力が載っているか** | 欠けているなら「高」 |
| **6-2** | **★壊す対象が実在し、その壊し方で観測が変わることが確かめられているか**（playbook §4.32） | 空振りしているなら「高」 |
| **6-3** | **★期待経路が「その観測が何を見ているか」から立てられているか** | **期待と違う経路が赤くなったのに、それが報告されていないなら「中」** |
| **6-4** | **★赤くなる経路が数えられているか**（**赤くならない経路があるなら、その経路は守られていない**） | 数えていないなら「中」 |

---

## 7. ドキュメント・進捗ログ

| # | 観点 | 判定 |
|---|---|---|
| **7-1** | **`docs/progress/progress-log.md` へ追記されているか**（`CLAUDE.md` §8） | 未追記なら「高」 |
| **7-2** | **完了報告に §7.4.1 の CHANGE 要否 8 件の判定結果が載っているか** | 欠けているなら「高」 |
| **7-3** | **★★「編集中の画面遷移時データ保持」を満たしていないことが完了報告に明記されているか**（§1.7-1） | 明記が無いなら「高」——**`REQ-001` の項目が「済み」に見える** |
| **7-4** | **★★3 段が独立したコミットになっているか。段ごとの結果が分けて書かれているか**（§4.7 / §7.5） | **★なっていないなら「高」**——**`M24-04` で同じことが起きた**（`D-580`） |
| **7-5** | **★`/setups/:setupId` へ及ばない範囲が報告されているか**（§3.3-11） | 報告が無いなら「中」 |
| **★7-5b** | **★★「レシピ未入力時の保存ボタンに手を入れていない」ことが完了報告に明記されているか**（§7.1-17） | 明記が無いなら「中」 |
| **7-6** | **★モック HTML が Plan Mode で提示され、変更前と変更後が並んでいるか**（§4.8） | 実装後に出していたら「中」 |
| **7-7** | **★件数を本文へ書いた箇所に、その場で数え直したコマンドが併記されているか** | 併記が無いなら「中」 |
| **7-8** | **★`bash scripts/check-artifact-integrity.sh` を 1 本目に回しているか。他の検査も緑か** | 回していないなら「中」。赤を残していたら「高」 |

---

## 8. 重大な問題の判定基準（完了承認を妨げる）

- **離脱ガードの 4 経路のいずれかが落ちている**（1-1）。
- **保存後の遷移が離脱ガードの補正を通っていない**（1-2）。
- **保存の成功で dirty が落ちない**（1-3）。
- **エラーの在り処がタブ見出しから分からない**（1-5）。
- **タブの切替でアンマウントする**（1-7）。
- **どちらかのタブでしか保存できない**（1-8）。
- **`opponent_stance` の既定が空文字へ戻っている**（1-13）。
- **`internal/` に差分があり、§2.3 の 3 条件の報告が無い**（2-1）。
- **`DES-002` §4.2 の既存 API を変更した**（2-2）／ **契約 F-1 の凍結パッケージに触れた**（2-3）。
- **保存後の遷移先の表（4 モード）が変わっている**（2-5）。
- **新規依存が提案なしで追加されている**（2-6）。
- **ブラウザストレージへ入力途中のコンボ本体を入れている**（3-3）／ **キーが台帳へ未登録**（3-1）。
- **E2E の書き換えで検証内容が緩められている**（5-4）。
- **★★上限クランプ（`clampNumericString`）が外れている**（1-18）。
- **★★レシピ未入力時の保存ボタンに手が入っている**（§0.3-11。**★要件の話であり別サブ＝`P-45`。ここで先取りすると `REQ-001` FR009 と設計が食い違ったまま実装が進む**）。
- **★仮登録トグルのラベルが 2 つの版で違う**（1-21）。
- **★★`type=flag` の「いいえ」で `false` を書いている**（1-23）。

---

## 9. 軽微な問題の判定基準（持ち越し許容）

- ハイライトの色・Boolean のトグルキー・ボタンの並べ方（§9.2 で委任済み）。
- 「メタデータを隠す」トグルの処遇（理由の報告があること）。
- トーストの位置の微調整。
- **★ただし「持ち越す」と決めたものは followup の候補として設計伝達レポート §4 へ並べること**（**★製造は `followup-backlog` を直接編集しない**＝`D-382`。**§J だけが例外**）。

---

## 10. 質問・確認事項のフォーマット

| # | 質問 | 対象ファイル:行 | 影響 |
|---|---|---|---|

---

## 11. レビュー完了の判定

- **重大 0 件**であること。
- **高**が残る場合は、取り込みの可否を製造担当が判断し、不採用なら理由を完了報告に書くこと。
- **★再レビューの往復上限は 2 回**（`CLAUDE.md` §9 停止規律）。**★上限に達したら未解消項目を `followup-backlog` §J へ必須 5 フィールド付きで記録し、直ちに停止すること。**

### 11.1 ★本チェックリスト自身の検査（**発行前に設計担当が行う**）

- **指示書の版数と対象指示書欄が一致しているか**（`bash scripts/check-instruction-format.sh`）。
- **★★§0.3「重大でないもの」が、指示書がスコープ外・委任と定めた項目をすべて拾っているか**（**★拾い漏れると、レビューが仕様どおりの実装を欠陥と判定する**）。
- **★§8 の重大判定が、指示書 §2.2「変更しないもの」と §7.1「機能要件」から漏れなく引かれているか。**

---

*以上、M24-12 レビューチェックリスト **v1.2.0**。* **★★本サブの最大の危険は「`M24-04` が入れたばかりの機構を、作り替えで黙って落とすこと」である。§1-1 〜 §1-5 がその照合にあたる。**
