# M28-02c レビューチェックリスト: `FR702` のバックエンド追補 ＋ 影響コンボ画面の実装

| 項目 | 内容 |
|------|------|
| 文書ID | M28-02c-review-checklist |
| バージョン | **1.0.0** |
| 対象指示書 | `docs/instructions/M28-02c-game-update-backend-remainder.md` **v1.0.0** |
| 推奨モデル | **高精度レビュー**（**★★判定式の共有を崩すと、表示と絞り込みが静かにずれる。⇒ テストは緑のまま通りうる**） |
| 作成者・作成日 | 設計担当 Claude（設計卓・フェーズ4 期）/ 2026-09-06 |

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| 1.0.0 | 2026-09-06 | 初版（`D-760`。**★投入の合図と同じ手番で起こした**＝`D-736` の運用） |

---

## 0. レビュー担当への前置き

### 0.1 レビュー前の準備

1. **`docs/change-notes/CHANGE-162-notification.md` を通読する**——**★★設計の正本である。⇒ 実装が契約どおりかを見るのが本レビューの中心である。**
2. **`CHANGE-159` §1 / §2 を読む**——**★`M28-02a` が作った API と判定式、そして「2 種類の NULL は向きが逆」。**
3. **`DES-005` §5.19b と §5.6 の注記を読む**——画面の正本。

### 0.2 ★★本サブは「設計どおりか」を見るサブである

**設計は `M28-02b` が全部済ませてある。⇒ 案を並べ直した形跡が在れば、それ自体が指摘対象である。**

**★ただし「契約が実装できない」と判明して設計卓へ請求しているなら、それは正しい振る舞いである。⇒ 請求せずに勝手にずらしていることが重大である。**

### 0.3 ★★重大でないもの（**誤判定しないこと**）

| # | 内容 |
|---|---|
| 1 | **`GET /api/moves` の応答 DTO にマーカーを載せていないこと**（指示書 §2.1-6＝**載せなくても成立する。設計卓は載せないことを推している**） |
| 2 | **始動位置・運び量の 3 方式入力 UI を作っていないこと**（**★射程外＝フェーズ5**） |
| 3 | **一括の「問題なし」を作っていないこと**（**★作らないのが正しい**） |
| 4 | **`check-md-emphasis.sh` / `check-import-order.sh` のベースラインが動いていないこと** |

### 0.4 ★★本サブに固有の危険（**最優先で見る**）

| # | 危険 | なぜ緑のまま通りうるか |
|---|---|---|
| **★★1** | **判定式を書き写した** | **★★表示と絞り込みがずれるのは、マーカーと基準が特定の関係になったときだけである。⇒ ふつうのテストデータでは両方同じ答えを返し、緑になる。★`CHANGE-159` §2 が「判定式は 1 か所しかなく、SELECT の派生列と一覧の WHERE が同じ式を共有する」と明言している** |
| **★★2** | **`affectedMoves.length > 0` で影響の有無を判定した** | **★真偽が 2 か所で表せてしまう。⇒ それ自体が「判定条件を 2 か所に書く」の再発である。★正本は `affectedByGameUpdate` の 1 本** |
| **★★3** | **取得失敗を「0 件」として扱った** | **★ボタンが黙って消える**（先例＝`combo-list-setup-count-hides-fetch-failure`）**。★★`affectedByGameUpdate` は `omitempty` を持たないので `false` は必ず載る。「キーが無い」は「判定していない」であり `false` とは別である** |
| **★★4** | **画面側で `?? false` を書いた** | **★上の区別が 1 段下で潰れる**（先例＝`ComboTableRow` の `?? 0`） |
| **★★5** | **初回は 0 件であることを確かめずに「動いた」と書いた** | **`D-725` により既存コンボの基準は最新。⇒ 画面を開いても何も出ない。★「0 件だったから正しい」は証拠にならない** |
| **★★6** | **延期でボタンまで消した** | **★★LAN で他の利用者が入口を失う。⇒ ここが分水嶺である**（`CHANGE-162` §2.3） |
| **★★7** | **`HomePage` だけにマウントした** | **★640px 以上では `HomePage` が描画されない。⇒ 主動線が PC で丸ごと消える。★テストでは気づけない**（既存の移行告知が実際にこれで消えていた） |
| **★8** | **列設定の共有物を触った** | **`COLUMN_DEFINITIONS` ／ `ColumnVisibility` ／ `combo-list-columns-v1` に触ると、コンボ一覧とマイコンボの列設定にまで影響が出る。★`web/CLAUDE.md` §1 の台帳も動く** |
| **★★9** | **`COUNT` を一覧 API の `count` で代用した** | **★★原理的に不可能である**——**ハンドラが `Count: len(items)` であり、`limit` が丸められる。⇒ 総数は取れない。★「取れているように見えた」なら、それはページ内の件数である** |

---

## 1. 束 A — `affectedMoves`（**最重要**）

| # | 見るところ |
|---|---|
| **★★1** | **判定式が 1 か所から導かれているか。★★式が 2 本ある状態を作れないことが、構造で示されているか**（§0.4-1） |
| **★★2** | **一覧の N 件ぶんがバッチ取得で作られているか**（先例＝`ListSetupsByComboIDs`）**。★N+1 になっていないか** |
| **★3** | **`nameJa` が引けないときに `code` へ落ちるか**（先例＝`starterMoveNameJa`） |
| **★★4** | **空のときに `[]` が明示されているか**（キーごと消えていないか） |
| **★★5** | **`affectedMoves.length` で真偽を判定する経路が 1 つも無いか**（§0.4-2）**。★画面・API・テストのすべてで見ること** |

---

## 2. 束 B — 告知の経路 2 本 ＋ `COUNT`

| # | 見るところ |
|---|---|
| **★★1** | **`GET /api/notices/game-update` が常に 200 で `{currentDataVersion, affectedCount, postponedForVersion?}` を返すか。★204 になっていないか** |
| **★★2** | **`COUNT` が判定式を共有しているか。★式が書き写されていないか**（§0.4-1） |
| **★★3** | **`COUNT` を一覧 API の `count` で代用していないか**（§0.4-9） |
| **★★4** | **`postpone` が要求本文を取らないか。★サーバが `games.current_data_version` を読んで書いているか** |
| **★5** | **保存先が `filepath.Dir(dbPath)` 直下の `.game-update-notice.json` か。★マイグレもブラウザストレージ台帳も消費していないか** |
| **★★6** | **★★延期が抑止するのはバナーだけか。一覧のボタンが抑止されていないか**（§0.4-6）**。⇒ ここが分水嶺である** |
| **★7** | **エラー契約の全行が報告に書かれているか**（`CHANGE-159` §1.1 と同じ粒度） |

---

## 3. 束 C — 専用画面

| # | 見るところ |
|---|---|
| **★★1** | **既存の `ComboTable` / `ComboTableRow` が表そのものとして再利用されているか。★新しい一覧の仕組みを作っていないか** |
| **★★2** | **足す 3 列が専用画面のときだけ渡す列になっているか。★`COLUMN_DEFINITIONS` ／ `ColumnVisibility` ／ `combo-list-columns-v1` に触っていないか**（§0.4-8） |
| **★★3** | **取得失敗が「0 件」として扱われていないか**（§0.4-3）**。★バナーの位置にも、一覧のボタンの位置にも失敗として出るか** |
| **★4** | **既存の一覧にフィルタ軸が足されていないか。⇒ 増えたのはボタン 1 個だけか** |
| **★5** | **キャラ横断で統一されているか。★ボタン文面に「全キャラ」が明記されているか** |
| **★6** | **ルートが `/game-update/combos` か** |

---

## 4. 束 D — 告知バナーと詳細

| # | 見るところ |
|---|---|
| **★★1** | **マウント先が `HomePage` ＋ `ComboListPage` の 2 か所か**（§0.4-7） |
| **★★2** | **★★既存の移行告知バナーのマウント先も `ComboListPage` へ増えているか。★コミットが分かれているか**（指示書 §2.4-2 / §2.5-6） |
| **★3** | **バナーとボタンが同時に出るか。★「バナーが出ている間はボタンを隠す」になっていないか** |
| **★★4** | **印を出しているのがコンボ詳細だけか。⇒ 専用画面にも一覧にも置いていないか** |
| **★★5** | **ゴミ箱詳細で印は出るが「問題なし」ボタンが出ず、理由が添えられているか**（**★ボタンだけ黙って消えていないか**） |
| **★★6** | **`affectedByGameUpdate` が FE 型で必須になっているか**（3 分岐すべて）**。★★画面側に `?? false` が無いか**（§0.4-4） |
| **★7** | **`baselineVersion` が無いときに「不明」＋印になっているか**（**★「—」になっていないか**） |
| **★8** | **エクスポートへ前提バージョンや印が出ていないか** |

---

## 5. テストの妥当性

| # | 見るところ |
|---|---|
| **★★1** | **★★人工的に古い基準を作ってから流しているか**（§0.4-5）**。⇒ 素のデータでは何も検証できない** |
| **★★2** | **`COUNT` が一覧の絞り込みと同じ集合を返すことを、実 DB で主張しているか**（**★件数と行数が一致すること**） |
| **★3** | **延期の保存と読み出し、および版が上がったら抑止が外れることのテストが在るか** |
| **★4** | **取得失敗が `0` 件と区別されることを主張するテストが在るか。★実 `ja.json` を引く `t` で書かれているか** |
| **★5** | **E2E の絞り込みは `make e2e-only P=<パターン>` を使っているか**（`D-599`）**。★`make e2e` はリポジトリルートで実行しているか** |

---

## 6. 破壊確認（**★3 件とも必須**）

| # | 壊すもの | 期待 |
|---|---|---|
| **★★1** | **画面を開いただけで基準が進む形にしてみる** | **赤になること。⇒ 進むと利用者が見る前に消える** |
| **★★2** | **延期した状態を作る** | **★★ボタンが消えないこと**（§0.4-6 の分水嶺の陽性対照） |
| **★★3** | **ビューポートを 640px 以上にする** | **★★バナーが出ること**（§0.4-7。**既存の移行告知が実際にこれで消えていた**） |

---

## 7. 重大な問題の判定基準（完了承認を妨げる）

| # | 内容 |
|---|---|
| **★★1** | **判定式が 2 か所に在る**（書き写した） |
| **★★2** | **`affectedMoves.length` で影響の有無を判定している** |
| **★★3** | **取得失敗を「0 件」として扱っている** |
| **★★4** | **画面側に `?? false` が在る** |
| **★★5** | **延期でボタンまで消える** |
| **★★6** | **`HomePage` だけにマウントしている** |
| **★★7** | **`COUNT` を一覧 API の `count` で代用している** |
| **★★8** | **列設定の共有物を触っている** |
| **★9** | **契約と実装がずれているのに設計卓へ請求していない** |
| **★10** | **`docs/design/` を編集した ／ `followup-backlog.md` の §J 以外を編集した ／ CHANGE 番号・マイグレ連番を自採番した** |

---

## 8. 軽微な問題の判定基準（持ち越し許容）

| # | 内容 |
|---|---|
| 1 | 配色・余白・文面の趣味 |
| 2 | **英語側の語選び** |
| 3 | **`GET /api/moves` にマーカーを載せたこと**（**★理由が報告に在る場合に限り軽微とする**） |
| 4 | コミットの粒度（**★ただし移行告知の是正が混ざっているのは §4-2 の対象である**） |

---

## 9. レビュー完了の判定

1. **§7 の重大が 0 件である。**
2. **§6 の破壊確認 3 件が実走されている。**
3. **`go test ./...` / `cd web && pnpm test` / `make e2e` が緑。**
4. **常設検査が緑**（**★`check-artifact-integrity.sh` を 1 本目に回す**）。
5. **`docs/design/` に反映が要る箇所が一覧になっている**（**★契約どおりなら 0 件のはずである**）。
6. **`docs/progress/progress-log.md` へ追記されている。**

### 9.1 ★本チェックリスト自身の検査（発行前に設計担当が行う）

- [x] **対象指示書の版数と一致している**（v1.0.0）
- [x] **`bash scripts/check-instruction-format.sh` が緑**
- [x] **`bash scripts/check-md-emphasis.sh` が緑**

---

*以上、M28-02c レビューチェックリスト **v1.0.0**。* **★★本サブでレビューが最も見落としやすいのは「判定式を書き写したこと」である**（§0.4-1）**——表示と絞り込みがずれるのはマーカーと基準が特定の関係になったときだけであり、ふつうのテストデータでは両方同じ答えを返して緑になる。⇒ 「式が 2 本ある状態を作れないこと」を構造で見ること。** **★★もう 1 つは「初回は必ず 0 件である」ことである**（§0.4-5）**——画面を開いても何も出ないので、「動かして確認した」が証拠にならない。**
