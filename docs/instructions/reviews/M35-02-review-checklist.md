# M35-02 レビューチェックリスト（`ryu` の `axe_kick_2` → `axe_kick`）

| 項目 | 内容 |
|------|------|
| 文書ID | M35-02-REVIEW-CHECKLIST |
| バージョン | **1.0.0** |
| 作成日 | 2026-09-10 |
| 対象指示書 | `docs/instructions/M35-02-ryu-axe-kick-code-fix.md` **v1.0.0** |
| 発行 | 設計卓（`D-802`） |
| 使い方 | `/review_plan` の入力。**★本書は指示書の完了条件を写したものではない。⇒ 「緑でも壊れているもの」を探すための束である** |

---

## 0. ★★レビュー担当への前置き

**★★本サブは小さい。⇒ 射程は 1 件、直す値も 1 つである。★だからこそ「三点のうち 1 つを落とす」形で壊れる。**

| 点 | 内容 | 落とすと |
|---|---|---|
| **1** | **CSV 正本の是正** | **次の seed 再生成で戻る** |
| **2** | **golden の再生成** | **`TestGolden_*MatchesRegeneration` が赤になる**（★気づけるが、気づく前にマイグレを書くと二度手間） |
| **3** | **追随マイグレ** | **★★既存 DB が直らない。⇒ テストは緑のまま** |

**★★★3 を落とすのが一番危ない。⇒ CSV と golden だけ直すと、新規 DB では直るが既存 DB では直らない。★どのテストも赤くならない。**

---

## 1. 束 A — ★★★三点が揃っているか

| # | 見ること | 落ちる形 |
|---|---|---|
| **★★★A-1** | **3 点すべてが在るか**〔`character_data/ryu.csv` の是正 ／ golden の再生成 ／ 追随マイグレ〕 | **★★★1 つでも欠けると、上表のどれかが起きる。⇒ とくに追随マイグレの欠落はテストが 1 本も赤くならない** |
| **★★A-2** | **`character_data/ryu.csv:27` の `axe_kick_2` と `:84` の `original_move_code=axe_kick_2` の**両方**が直っているか** | **★★`:84` を落とすと、`rush_axe_kick` の参照先が消える。⇒ 直したつもりで別の壊れ方をする** |
| **★★A-3** | **golden の再生成が「手で書き換えた」ではなく「生成し直した」か** | **★★手で直すと、生成器の出力と 1 バイトずれたときに気づけない。⇒ 生成コマンドが報告に在るか** |

---

## 2. 束 B — ★★参照の全数

| # | 見ること | 落ちる形 |
|---|---|---|
| **★★★B-1** | **`axe_kick_2` を参照している箇所の全数が、走査で示されているか**〔`moves` ／ `preset_aliases` ／ `combo_steps` ／ `setup_steps` ／ `character_data` ／ docs / テスト〕 | **★★★1 か所残ると、そこだけ古い code を指す。⇒ 「3 ファイルだと思う」では足りない。★式と出力が要る** |
| **★★B-2** | **走査に使った式が報告へ書かれているか** | **★★「`grep` が 0 件」は、書いた時点の走査条件ごと引き継がれる。⇒ `M35-01` の `M-148` (a) と同型である。★式を併記していなければ 1 件** |
| **★★★B-3** | **`rush_axe_kick` を 1 文字も触っていないか** | **★★★開発者の逐語は「`axe_kick_2` 側の方が誤り。`axe_kick`、`rush_axe_kick` が正しい」である。⇒ `rush_` 側を直すと、正しいものを壊すことになる** |
| **★B-4** | **`axe_kick` という code が既に別の技で使われていないか**（**衝突の確認**） | **★衝突していると `UNIQUE` で落ちるが、マイグレを流すまで気づけない** |

---

## 3. 束 C — ★★マイグレの形

| # | 見ること | 落ちる形 |
|---|---|---|
| **★★★C-1** | **ファイル名が `000108_data_*.sql` になっているか**（**層 B**） | **★★★`_data_` を入れないと `REUSE.toml` の既定**（層 A＝`AGPL`）**へ静かに落ちる。⇒ 誰も赤くならない。★SF6 の技名は SF6 の事実であり層 B である** |
| **★★C-2** | **`scripts/check-migration-license.sh` を回して緑であることが示されているか** | **★機構が判定する。⇒ 目視では層を確かめたことにならない** |
| **★★C-3** | **番号が `000108` か。⇒ 自採番していないか** | **★`D-800` で払い出した番号である。★1 本に固定であり、足りないなら再請求** |
| **★★C-4** | **`down` が書かれているか**（`SUPP-001` §5.5.4） | **★`down` が無いと、歴史テストが往復できない** |
| **★★★C-5** | **`preset_aliases` の参照も更新されているか** | **★★★`move_code` は `preset_aliases` の解決キーである**（`DES-004` §2.1 の警告）**。⇒ `moves` だけ直すと、別名の解決が外れる** |

---

## 4. 束 D — ★直ったことを示しているか

| # | 見ること | 落ちる形 |
|---|---|---|
| **★★D-1** | **陽性対照が在るか＝直す前に「`rush_axe_kick` が入力できない」ことを実測し、直した後に「できる」ことを実測しているか** | **★★後だけ見ると、もともとできていたのか本サブで直ったのかが分からない** |
| **★★D-2** | **機序が報告に書かれているか**（`resolveRushByCode` が `rush_<元技>` の形で探し、`axe_kick_2` に対しては `rush_axe_kick_2` を探して外れる） | **★★エラーにならず「ラッシュ版が出ないだけ」である。⇒ 機序を書かないと、次に似た形が出たとき同じ調査をやり直す** |
| **★D-3** | **既存 DB での確認が在るか**（**追随マイグレが効いていること**） | **★新規 DB だけで確かめると、束 A の 3 点目が効いていなくても緑になる** |

---

## 5. ★★見なくてよいもの

| # | 何を | なぜ |
|---|---|---|
| 1 | **是正の向き** | **開発者が確定済みである**（逐語＝「`axe_kick_2` 側の方が誤り」）**。⇒ 迷う余地が無い** |
| 2 | **他キャラの同型** | **本サブの射程は `ryu` の 1 件だけである。⇒ 見つけても直さず、報告に挙げるところまで** |
| 3 | **`data-testid` の登録** | **`CHANGE-171` が一覧運用を畳んでいる** |

---

*以上、M35-02 レビューチェックリスト **v1.0.0**。* **★★★本書で最も重いのは A-1 と C-1 と C-5 である。** **★A-1 の 3 点目**（追随マイグレ）**を落とすと、テストが 1 本も赤くならないまま既存 DB が直らない。** **★C-1 の `_data_` を落とすと、SF6 の事実が `AGPL` で配られる。★どちらも「緑のまま壊れる」形である。**
