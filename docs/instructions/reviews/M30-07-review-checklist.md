# M30-07 レビューチェックリスト（【ジャスト】版の `move_code` を接尾形へ揃える）

| 項目 | 内容 |
|------|------|
| 文書ID | M30-07-REVIEW-CHECKLIST |
| バージョン | **1.0.1**（2026-09-12・**★対象指示書が v1.0.1 へ改版された＝§0.5「意図した赤の一覧」の追加。⇒ 追随して束 B へ 1 行足した**） ／ **1.0.0**（2026-09-12・発行） |
| 対象指示書 | `docs/instructions/M30-07-perfect-variant-code-suffix.md` **v1.0.1** |
| 作成者 | 設計担当 Claude（設計卓） |
| 位置づけ | **レビュー担当の主入力**（`D-736`） |

---

## 0. ★★レビュー担当への前置き

**本サブは「3 行の `move_code` を直すだけ」に見えて、実は*先行 3 サブの床を全部動かす*サブである。**

**★★★ファミリー総数が 372 → 369 になる。⇒ `M30-04` / `M30-05` / `M30-06` はいずれも「372 が不変」を完了条件・テストに書いている。★1 か所でも残ると赤になり、赤を消すために床を緩める誘惑が出る。**

**★最も重い不合格は「床を緩めて緑にした」ことである**（件数のアサートを消す・digest の比較を外す等）**。**

---

## 1. 束 A — ★★★床を緩めていないか（**本サブの本体**）

| # | 観点 |
|---|---|
| **A-1** | **★★★372 / 194:178 / `family` digest を床にしている箇所が*全数*更新されているか。⇒ 完了報告に列挙されているか** |
| **A-2** | **★★★床を緩めた箇所が 1 つも無いか**——**件数のアサートを消した ／ digest の比較を外した ／ `toBe` を `toBeGreaterThan` に変えた等。★1 件でも在れば不合格である** |
| **A-3** | **★新しい値が実測で出されているか。⇒ 「369 になるはず」ではなく「369 だった」か** |
| **A-4** | **★`family` digest は変わるのが正しい。⇒ 新しい digest が報告に書かれ、テストが更新されているか** |
| **A-5** | **★★★減った分が意図した分と一致することが示されているか**——`372 - 369 = 3` **＝ガイルの `perfect_timing_*` 3 行そのものか。⇒ 「赤かったので直した」では通らない**（指示書 v1.0.1 §0.5）**。★消えた 3 行を名指しで挙げているか** |
| **A-6** | **★指示書 §0.5 が挙げた床が*全数*処理されているか**〔`specialFamilyLabel.roster.test.ts` の総数・識別子 digest・表示名 digest ／ `moveSurfacing.roster.test.ts` の 372 と 194:178 ／ `specialFamilyRepresentativeName.roster.test.ts` の反例群〕**。⇒ 3 ファイルとも報告に現れているか** |

---

## 2. 束 B — ★★★データの追随が漏れていないか

| # | 観点 |
|---|---|
| **B-1** | **★★★`preset_aliases` の追随が在るか。⇒ 実測の件数つきか。★`down` も対になっているか**（`M35-02` / `M35-03` の先例） |
| **B-2** | **★`moves.original_move_code` に参照が無いことを確かめたか**（`M35-03` が直した列である） |
| **B-3** | **★`000112` を使っているか**（自採番していないか＝`D-293`）**。★着手時に `ls migrations/` で最新が `000111` であることを実査したか** |
| **B-4** | **★`down` が書かれ、改名の逆になっているか** |
| **B-5** | **★`check-migration-license.sh` の層判定が正しいか。⇒ 破壊確認**（`_data_` を外すと赤になるか）**を回したか** |

---

## 3. 束 C — ★★★golden の追随（**規約 (16) の手順で行われているか**）

| # | 観点 |
|---|---|
| **C-1** | **★★★連番の `grep` で済ませていないか。⇒ 変わる列名**（`code` / `move_code` / `original_move_code`）**で `*_test.go` を探したか**（`SUPP-001` §5.5.4 規約 (16)） |
| **C-2** | **★ヒットした各ファイルの終端が、その golden の版以上かを突き合わせたか。★終端が HEAD のテストも対象である** |
| **C-3** | **★★先に赤を実測してから直したか。⇒ その出力が完了報告に貼られているか**（規約 (16) 歯止め (c)） |
| **C-4** | **★要素数を減らさず、対のまま入れ替える形になっているか**（同 (b)） |
| **C-5** | **★`make e2e` を*全数*回したか。⇒ 件数固定と「最後の技を選ぶ」形の spec を洗ったか**（`SUPP-001` §5.5.7 (i)） |

---

## 4. 束 D — ★★射程を越えていないか

| # | 観点 |
|---|---|
| **D-1** | **★★★`splitSpecialVariant` の接尾辞集合を広げていないか**（指示書 §3-1）**。⇒ (b) を採らないと裁定した。★実装側で吸収したら不合格である** |
| **D-2** | **★【ホールド】/【最大ホールド】の `move_code` に差分が無いか**（指示書 §3-2）**。⇒ 揺れていないものを揃え直していないか** |
| **D-3** | **★ルーク側の `move_code` に差分が無いか**（**ルークが正である**） |
| **D-4** | **★`name_ja` に差分が無いか。⇒ 本サブは識別子だけを直す** |
| **D-5** | **★`DES-005` §6.1 の落とす規則 3 形 ／ `is_derived` の並びのロジックに差分が無いか** |
| **D-6** | **★データ補正**（`name_ja` の逸脱の是正等）**をしていないか**（`D-305`） |

---

## 5. 束 E — ★実査が信用できるか

| # | 観点 |
|---|---|
| **E-1** | **★★【ジャスト】を持つ `move_code` の全数を実物で数え直したか。⇒ `M30-05` の「guile 10 / luke 3」をそのまま使っていたら不合格である**（同サブ教訓 1） |
| **E-2** | **★【ホールド】系に同じ揺れが 0 件であることを実測で確かめたか。⇒ これも写しである** |
| **E-3** | **★接頭形／接尾形の件数が両方出ているか。⇒ 合計が全数と一致するか** |
| **E-4** | **★数値の隣に「実測」「算出」が書き分けられているか**（計測点 `M-157`） |
| **E-5** | **★手元の `DES-004` が 1.32.0 以上であることを着手時に確かめたか**（指示書 §1 の版ゲート ／ `playbook` §4.60） |

---

## 6. 束 F — ★成果が出ているか

| # | 観点 |
|---|---|
| **F-1** | **★★ガイルの 3 ファミリーが畳まれ、「素＋ジャスト」の 2 変種になっているか。⇒ 総数が 369 か。★372 のままなら畳めていない** |
| **F-2** | **★ルークとガイルが同じ形で選べることを、実機で見る手順が完了報告に在るか**（指示書 §2.5） |
| **F-3** | **★陽性対照＝入れる前にガイルが別ファミリー 3 行だったことを測っているか** |

---

## 7. ★★見なくてよいもの

| # | 何を | なぜ |
|---|---|---|
| 1 | **代表名の作り方** | **`M30-06` の射程。⇒ 本サブは `M30-06` の着地後に走る** |
| 2 | **表示名から強度語を落とす規則** | **`M30-05` の成果。⇒ `DES-005` §6.1 が正本** |
| 3 | **`ODサンシュート` が残る仕組み** | **`M30-05` が実装コメントで固定済み** |
| 4 | **CSV ローダの二重化 ／ `roster.test.ts` 冒頭の失効値** | **`followup` の別項。⇒ `M31-06` 着地後の手番** |
| 5 | **CHANGE の起票** | **★製造は自採番しない**（`D-293`）**。⇒ `DES-004` §2.1 の as-built 化は受理の手番で設計卓が行う** |

---

*以上、M30-07 レビューチェックリスト **v1.0.1**。* **★★束 A が本体である。⇒ 「3 行直して緑になった」で通さないこと。** **★総数が動く以上、先行 3 サブの床が全部動く。緩めずに正しい新しい値へ更新されているかを見ること。** **★★もう 1 つは C-1 である**——**連番の `grep` は「やった」という体裁を保ったまま取り落とす。**
