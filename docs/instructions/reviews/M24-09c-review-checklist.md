# M24-09c レビューチェックリスト: E2E スイートの安定化

| 項目 | 内容 |
|------|------|
| 文書ID | M24-09c-review-checklist |
| バージョン | 1.4.0 |
| 対象指示書 | `docs/instructions/M24-09c-e2e-suite-stabilization.md` **v1.4.0** |
| 推奨モデル | **高精度レビュー**（**本サブの成果は「緑の回数」では判定できない。機序と破壊確認で判定する**） |
| 作成者・作成日 | 設計担当 Claude（設計卓・M24 期）/ 2026-08-25 |

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| 1.4.0 | 2026-08-26 | **指示書 v1.4.0 へ追随**（**D-569**）。 **★★配信方式が `embed_web` から `vite preview` へ差し替わった。** **⇒ 13-2 を撤回し、13-6b を新設**〔**`cmd/` の差分が §12.1.3 の 1 か所のままか**〕。 **★★13-7 を「2 か所」から「1 か所」へ改めた**——**`vite preview` は本番コードに触らないため、`internal/config` の `EnvConfigPath` と `cmd/` の `configPath` のうち後者だけが `cmd/` の差分である。★`launchBrowser` の抑止を足していたら「重大」。** **★§0.3 へ 3 件を足した**〔`SUPP-001` §4.5 の齟齬を製造が直していないこと ／ CI 定義を触っていないこと ／ Playwright が dev の `config.toml` を読み続けていること〕。 |
| 1.3.0 | 2026-08-26 | **指示書 v1.3.0 へ追随**（**D-564**）。**★§13 を新設＝追補分（dev の `config.toml` からの切り離し ＋ `embed_web` 化）の観点。** **★★13-3（「古いフロントをテストする」事故の検出）と 13-5（事実 A の対照が緑であること）を「重大」判定にした。** **★§0.3 へ 3 件を足した**〔段 2 の残りを followup へ出したこと ／ `VAL-C02` を直していないこと ／ `D-548` の制約が消えたと書いていないこと〕。 |
| 1.2.0 | 2026-08-26 | **指示書 v1.2.0 へ追随**（**D-557**）。**★6-4 を「2 倍の門」から「一発で緑になった回数 / 実行回数」の報告へ改めた**——**判定基準 2 は撤回され、実行時間の伸びは制約ではなくなった。** **★★新項 1-6 を足した＝`passed` / `flaky` / `fail` の 3 語が定義付きで分けて数えられているか**〔**開発者は `fail` を、製造 2 サブは `flaky` を見ていた。同じ語で違う量を数えると比較が壊れる**〕。 |
| 1.1.0 | 2026-08-25 | **指示書 v1.1.0 へ追随**（**D-556**）。**★6-4 を「2 倍を超えていないか」から「基準 2 がこの環境で門として働くかが報告されているか」へ改めた**——**worker 数によっては 2 倍の線が何も止めないため、超えたかどうかだけを見ても意味がない。** |
| 1.0.0 | 2026-08-25 | 初版（**D-554**） |

---

## 0. レビュー担当への前置き

### 0.1 レビュー前の準備

1. 指示書 **v1.4.0** を全文読む（**★§12 追補と §12.10 を含む**）。
2. 完了報告 `docs/progress/M24-09c-completion-report.md` を読む。
3. **★`git diff` で `web/src/` ／ `migrations/` が 0 バイトであることを自分で確かめる**（指示書 §1.3-3）。 **★★`internal/` ／ `cmd/` は §12 追補で差分が入る**——**`internal/config/config.go` の `EnvConfigPath` と `cmd/combomgr/main.go` の `configPath` の 2 か所だけである**（指示書 §12.1.3）。**それ以外に差分が在るなら「重大」。** **★★とくに `cmd/combomgr/main.go` の `launchBrowser` に手が入っていたら「重大」**——**`vite preview` を採ったのは、まさにその 3 か所目を作らないためである**（`D-569`）。
4. **`CHANGE-135` 通知書を読む**（§12 追補の射程）。

### 0.2 レビューの基本姿勢

コードを変更せず、指摘のみを行う。**★本サブは「実装しない」が正しい結末でありうる**（§0.3）。

### 0.3 ★★重大でないもの（**誤判定しないこと**）

| # | これは欠陥ではない |
|---|---|
| **1** | **★対策を実装せず、調査と報告で終えていること。** **指示書 §4.2 の判定基準 4 が「実測 4（干渉の証明）が取れなかったら報告で終える」を正しい結末として認めている。** **⇒ 実装が無いことを「未達」と判定しないこと** |
| **2** | **8 件すべてが「解消」になっていないこと。** **★指示書 §4.4 が「全部が解消と書かれていたら設計卓はそれを疑う」と明記している。** 別の機序だったものが混じるのが自然である |
| **3** | **`make e2e` の実行時間が伸びていること。** **★★「2 倍以内」の門は v1.2.0 で撤回済みである**（`D-557`＝**開発者が実行時間の伸びを許容した**）。**⇒ 伸びたことを「未達」と判定しないこと。見るのは §6 の「一発で緑になった回数 / 実行回数」である**（**★本行は v1.2.0 の追随で取り残されていた。2026-08-26 是正**） |
| **4** | **既存 spec の下ごしらえ（fixture・待機・データ作成）の形が変わっていること。** 主張が変わっていなければよい（指示書 §2.2） |
| **5** | **`web/e2e/support/` へファイルが増えていること。** **運用の型として承認済みである**（`D-553`） |
| **6** | **★★§12.1.2 の段 2（E2E が既定キャラに依存しないようにする）の残りが followup へ出されていること。** **指示書 §12.8-3 がそれを正しい結末としている**〔**★工数が読めないため、段 1 だけ本サブで行うことを許してある**〕 |
| **7** | **★★`VAL-C02` の競合が直っていないこと。** **指示書 §12.6 が「本サブでは直さない」と定めている**〔別サブ。射程が `DES-006` / `DES-003`（スキーマ変更）/ `DES-002` §4.2 の 3 書に及ぶ〕 |
| **8** | **★`D-548` の制約（`make e2e` を重ねられない）が「消えた」と書かれていないこと。** **★★2026-08-26 に「消えない」ことが確定した**（`D-569`）——**`vite preview` でもポート 5273 を使い続け、47390 と使い捨て DB も残る** |
| **9** | **★`SUPP-001` §4.5 の齟齬 2 件を製造が直していないこと**〔`resolveBackendPort()` → `readDevBackendPort()` ／ `reuseExistingServer` が両方 `false`〕。**★`SUPP-001` は設計卓の手番である**（指示書 §12.10-2） |
| **10** | **★CI 定義（`.github/workflows/pr-checks.yml`）に差分が無いこと。** **`reporter: dot` と retry の扱いが残っていても欠陥ではない**——**CI 定義は `M24-09a` の成果物であり、本サブでは触らない**（指示書 §12.10-3） |
| **11** | **★★`playwright.config.ts` が dev の `config.toml` を読み続けていること。** **指示書 §12.1.3-4（読み先を揃えること）は撤回された**（`D-569`・§12.10-1）——**揃えると worktree のポート分離が壊れる** |

### 0.4 報告フォーマット

指摘は **重大 / 高 / 中 / 低** で分類し、各件に**根拠**（ファイル:行 または 出力の逐語）を添える。

---

## 1. 設計・パターンとの照合（最重要）

| # | 観点 | 判定 |
|---|---|---|
| **1-1** | **★★機序が特定されているか。** 完了報告に「何が何と干渉していたか」が 1 文で書かれているか。**「たぶん競合」で終わっていないか** | **書かれていなければ「重大」** |
| **1-2** | **★★指示書 §4.1 の実測 4 種がすべて実施され、母数付きで報告されているか**〔既定 worker 3 回 ／ `--workers=1` 2 回 ／ 8 件の隔離実行 ／ **干渉の実在の証明**〕 | **母数の欠落は「高」。実測 4（干渉の証明）の欠落は「重大」** |
| **1-3** | **★★実測 4 が取れていない場合、対策を実装していないか。** **取れていないのに実装しているなら、根拠の無い対策である** | **実装していたら「重大」** |
| **1-4** | **★§4.2 の判定基準が上から順に適用されているか。** spec 書き換え 0 本の手が採れない場合、その理由が書かれているか | **理由が無いなら「高」** |
| **1-5** | **採らなかった系統の不採用理由が書かれているか**（`CLAUDE.md` §9 共通原則3。**★系統は 4 つある**＝(A) / (B) / (B') / (C)） | 無いなら「中」 |
| **1-6** | **★★`passed` / `flaky` / `fail` の 3 語が、定義付きで分けて数えられているか**（指示書 §4.1.1）。 **★★開発者は `fail`（retry でも通らない）を、製造 2 サブは `flaky`（retry で通った）を見ていた。** **同じ語で違う量を数えると、実測どうしが比較できない** | **3 語が混ざっているなら「高」** |

---

## 2. データ・API 契約・スキーマの不変（非破壊性）

| # | 観点 | 判定 |
|---|---|---|
| **2-1** | **`web/src/` ／ `internal/` ／ `cmd/` ／ `migrations/` の差分が 0 バイトか** | **0 でなければ「重大」**（§2.3 の 3 条件をすべて満たす例外を除く。満たすなら報告に 3 条件の充足が書かれているはず） |
| **2-2** | **E2E の専用ポート 47390 / 5273 が変わっていないか** | 変わっていたら「重大」 |
| **2-3** | **`internal/service/notation/` ／ `internal/service/preset/` に差分が無いか**（契約 F-1） | 差分を検出したら「重大」 |
| **2-4** | **CI 定義に差分が無いか**（`M24-09a` の成果物） | 差分を検出したら「高」 |
| **2-5** | **`.claude/commands/add_e2e_spec.md` ／ `CLAUDE.md` に差分が無いか**（開発者のファイル） | 差分を検出したら「重大」 |

---

## 3. E2E ハーネスの妥当性

| # | 観点 | 判定 |
|---|---|---|
| **3-1** | **★★既存 spec が主張している内容が変わっていないか。** 下ごしらえの形は変えてよいが、「何を確かめているか」は不変であること | 変わっていたら「重大」 |
| **3-2** | **★実測がポート衝突で汚染されていないか。** 各回の直前のポート確認の出力が載っているか（指示書 §2.4） | **無いなら「高」**（`M24-09b` §7-9＝**汚染された実測は、実測が無いより悪い**） |
| **3-3** | **`check-*.sh` の判定ロジックを緩めていないか** | 緩めていたら「重大」 |
| **3-4** | **`web/e2e/support/` へ寄せた場合、同じ数行が複数 spec へ複製されていないか**（`E-232` / `M24-01` §7-6） | 複製を検出したら「中」 |

---

## 4. テストの妥当性

| # | 観点 | 判定 |
|---|---|---|
| **4-1** | **★★§4.3 の決定論 spec があるか**（対策を実装した場合）。**10 回走らせて 10 回とも同じ結果になることが示されているか** | **無いなら「重大」。10 回の実測が無いなら「高」** |
| **4-2** | **★★その spec は本当に「干渉」を測っているか。** **2 つの spec が同じデータ空間を奪い合う形になっているか**——**単に「片方の spec を 2 回走らせて 2 回目が落ちる」形なら、それは干渉ではなく冪等性の問題である** | **形が違うなら「重大」** |
| **4-3** | **★決定論 spec が、実アプリと同じ経路で干渉を起こしているか**——**テスト内で直接 DB を書いて衝突を作っているなら、E2E の干渉を再現していない**（`E-217` / `E-223` の型＝**結果表示を主張するテストは実アプリと同じ状態遷移で描画する**） | **違うなら「高」** |
| **4-4** | **`go test` / `pnpm test` が着手前と同じか**（本サブは Go も `web/src/` も触らないため、対照として同一であるはず） | 違うなら「高」 |

---

## 5. 破壊確認（**指示書 §5.3・3 件**）

| # | 観点 | 判定 |
|---|---|---|
| **5-1** | **3 件すべてが実施され、コマンドと出力が載っているか** | 欠けていたら「高」 |
| **5-2** | **★破壊確認 1**（対策の中核を 1 行戻す）**で決定論 spec が赤くなったか** | 赤くならないなら「重大」＝**対策が効いていない、または spec が対策を測っていない** |
| **5-3** | **★破壊確認 2**（決定論 spec の干渉部分を無害化する）**で、対策前の状態でも緑になったか** | **これが緑にならないのなら、その spec は干渉以外の何かを測っている**＝「高」 |
| **5-4** | **★★「壊す対象が実在したか」が確かめられているか。** 実在しなかった項目について、その旨と差し替えが報告されているか | **無言で省略されていたら「高」**（計測点 `M-78`） |
| **5-5** | **★赤くなった経路が数えられているか。** 赤くならない経路があるなら、その経路に対策が効いていない | 数が無いなら「中」 |

---

## 6. 完了の証明（**指示書 §5.4**）

| # | 観点 | 判定 |
|---|---|---|
| **6-1** | **`make e2e` 5 回連続の全出力があるか**（pass / fail / flaky と実行時間） | 無いなら「高」 |
| **6-2** | **★★「5 回緑だったから直った」を根拠にしていないか。** **必要条件と十分条件が分けて書かれているか** | **緑の回数だけを根拠にしていれば「高」**——**元の発生率なら 5 回続けて出ないことは偶然でも起こりうる** |
| **6-3** | **十分条件（機序の特定 ＋ 破壊確認 3 件）が満たされているか** | 満たされていないなら「重大」 |
| **6-4** | **★★「一発で緑になった回数 / 実行回数」が、実測 1（既定 worker）と実測 2（直列）の両方について報告されているか**（指示書 §4.2.1 の新しい基準 2）。 **★★実行時間の伸びは制約ではない**（2026-08-26 に開発者が許容＝`D-557`）。**⇒ 「2 倍を超えたか」を欠陥として判定しないこと。** **★測るのは「成功した 1 回の時間」ではなく「緑になるまでの総時間」である**——**1 回あたりが 3 倍でも、一発で緑になる率が `1/3` から `3/3` になれば総時間は短くなる。** **★あわせて worker 数と実行時間の内訳（起動 / テスト実行）が実測されているか**（§3.3-10 / §3.3-11） | **「一発で緑になった回数 / 実行回数」が無いなら「高」。実行時間が伸びたことだけを理由に指摘しないこと** |

---

## 7. 8 件の結末（**指示書 §4.4**）

| # | 観点 | 判定 |
|---|---|---|
| **7-1** | **8 件それぞれに「現在の再現性 ／ 本サブの対策で説明が付くか ／ 結末」の 3 列があるか** | 欠けていたら「高」 |
| **7-2** | **★「説明が付かない」ものについて、理由と残る疑問が書かれているか** | **「付く」と書きながら機序が書かれていないものを検出したら「高」** |
| **7-3** | **★★`e2e-flaky-combo-post-500`（`POST /api/combos` が 500）が「干渉で説明が付く」と書かれている場合、その機序が具体的に書かれているか** | **★書かれていなければ「高」**——**サーバの 500 は干渉では説明しにくい。安易に束ねていないかを見る** |

---

## 8. ドキュメント・進捗ログ

| # | 観点 | 判定 |
|---|---|---|
| **8-1** | **`docs/progress/progress-log.md` へ索引行が追記されているか** | 無いなら「高」 |
| **8-2** | **★`check-progress-log-index.sh` が緑でも、索引行を目で確かめたか。** 同スクリプトは部分文字列一致で偽の緑を返す（followup §AH） | 目視確認の記載が無いなら「中」 |
| **8-3** | **§7.4.1 の CHANGE 要否 6 条件が 1 件ずつ判定されているか**（**★「不要」でも判定したことを書く**） | 欠けていたら「高」 |
| **8-4** | **★★条件 5（`SUPP-001` §5.5）と条件 6（`CLAUDE.md` §5 の E2E 記述）の判定があるか**——**対策が worker ごとの DB 分離なら「1 スイート 1 個」の記述が失効する** | 無いなら「高」 |
| **8-5** | **§4.5 の否定形走査 2 件が、母集合の宣言付きで報告されているか** | 母集合の宣言が無いなら「中」 |

---

## 9. 重大な問題の判定基準（完了承認を妨げる）

1. **機序が特定されていない**（1-1）。
2. **実測 4（干渉の実在の証明）が無い**（1-2）。
3. **実測 4 が取れていないのに対策を実装している**（1-3）。
4. **決定論 spec が無い**（対策を実装した場合。4-1）。
5. **決定論 spec が干渉ではなく冪等性を測っている**（4-2）。
6. **破壊確認 1 で決定論 spec が赤くならない**（5-2）。
7. **十分条件が満たされていない**（6-3）。
8. **本番コードに差分がある**（§2.3 の 3 条件の充足が示されていない。2-1）。
9. **E2E の専用ポートが変わっている**（2-2）。
10. **既存 spec の主張が変わっている**（3-1）。
11. **開発者のファイル（`CLAUDE.md` / `.claude/commands/`）に差分がある**（2-5）。

---

**★★追補分（§11.5）の重大**

13. **`playwright.config.ts` とバックエンドが違う config を読んでいる**（13-2）。
14. **★「古いフロントをテストする」事故の検出が確かめられていない**（13-3）。
14b. **★★`cmd/combomgr/main.go` に 3 か所目（`launchBrowser` の抑止）が足されている**（13-6b）／ **`embed_web` が採られている**（13-13）。
14c. **★`playwright.config.ts` の読み先をバックエンドと揃えている**（13-2 の反転）。
15. **worktree のポート分離が壊れている**（13-4）。
16. **★事実 A の対照が未実施、または赤**（13-5）。
17. **`internal/` ／ `cmd/` に §12.1.3 の 2 か所以外の差分がある／既定の挙動が変わっている**（13-7）。
18. **★破壊確認が全件緑のまま「実施した」で終わっている**（13-8）。
19. **`CLAUDE.md` ／ `.claude/commands/add_e2e_spec.md` ／ `SUPP-001` を製造が編集している**（13-10 / 13-11）。

---

## 10. 軽微な問題の判定基準（持ち越し許容）

- 実測の出力の整形・報告の体裁。
- `web/e2e/support/` のヘルパの命名。
- 8 件のうち「別の機序だった」ものの深追い。

---

## 11. 質問・確認事項のフォーマット

**「何が」「どの根拠で」「どう違うか」の 3 点を書く。** 推測を断定形で書かないこと。

---

## 11.5 ★★追補分の観点（**指示書 §12**）

| # | 観点 | 判定 |
|---|---|---|
| **13-1** | **★★§12.4 の実査 8 件が実施され、結果が報告されているか。母数の要る項目に母数があるか**（§12.4-4＝**既定キャラに依存している spec の数 / 全 spec 数**） | 母数の欠落は「高」。実査の欠落は「高」 |
| ~~**13-2**~~ | ~~**`playwright.config.ts` の読む config と、バックエンドが読む config が同じか**~~ | **★★2026-08-26 撤回**（**D-569**・指示書 §12.10-1）。**★揃っていないのが正しい**——**Playwright が dev の `config.toml` を読むのは worktree のオフセットを決めるためであり、バックエンドのポートは `COMBOMGR_PORT` で渡される。⇒ 揃えると worktree のポート分離が壊れる。** **★揃えていたら「重大」**（判定が反転した） |
| **13-3** | **★★フロントのソースを変えたときに `make e2e` が確実に再ビルドするかが、実測で確かめられているか**（§12.4-5・破壊確認 3） | **★★確かめられていないなら「重大」**——**「古いフロントをテストする」事故は緑で通るため気づけない。★`vite preview` でも `embed_web` でも同じ代償である**（`D-569`） |
| **13-4** | **★`Makefile` の worktree ごとのポート分離が壊れていないか**（§12.1.3-5。**意図的な設計である**） | 壊れていたら「重大」 |
| **13-5** | **★★事実 A の対照が緑か**——**dev の `config.toml` の既定キャラを別キャラへ変えてから `make e2e` を回し、全件緑であること**（§12.5.1-3） | **★★実施されていない、または赤なら「重大」**——**これが根治の唯一の証明である** |
| **13-6** | **★E2E 専用 config に `defaults.characterId` が書かれていないか**（§12.1.2 段 2） | **書かれていて、報告も無いなら「高」**——**書いた時点で「誰かが変えると壊れる」が復活する** |
| **13-7** | **★`internal/` ／ `cmd/` の差分が §12.1.3 の 2 か所だけか。既定の挙動が不変か**（env 未設定なら `"config.toml"`） | **それ以外の差分は「重大」。既定の挙動が変わっていたら「重大」** |
| **13-6b** | **★★`cmd/combomgr/main.go` に `launchBrowser` の抑止が足されていないか**（**3 か所目**） | **★★足されていたら「重大」**——**`vite preview` を採ったのはそれを作らないためである**（`D-569`） |
| **13-13** | **★`vite preview` が採られているか。`embed_web` になっていないか**（§12.2） | **`embed_web` なら「重大」** |
| **13-14** | **★`vite.config.ts` に `preview.proxy` が置かれ、`resolveApiTarget()` を再実装していないか**（§12.2.2） | **★第 2 の解決規則を書き起こしていたら「高」** |
| **13-15** | **★webServer の構成（2 本）とポート（47390 / 5273）が変わっていないか**（§12.2.2） | 変わっていたら「高」 |
| **13-8** | **破壊確認 3 件（§12.5.2）が実施され、コマンドと出力が載っているか。★「壊す対象が実在したか」と「その壊し方で観測が動いたか」の両方が確かめられているか**（`M-78` ／ playbook §4.32） | 欠けていたら「高」。**★全件緑のまま「実施した」なら「重大」** |
| **13-9** | **★破壊確認 2（`embed_web` を外す）の観測が「時間」で数として示されているか**（赤/緑ではない） | 数が無いなら「中」 |
| **13-10** | **★`CLAUDE.md` §5 と `.claude/commands/add_e2e_spec.md` について、変わる内容が逐語で並んでいるか**（§12.3。**★開発者が写すだけで直せる形か**） | 無いなら「高」。**★製造がこの 2 ファイルを編集していたら「重大」** |
| **13-11** | **★`SUPP-001` が編集されていないか**（設計卓の手番） | 編集していたら「重大」 |
| **13-12** | **★`CHANGE-135` の射程が広がったかが判定され、結果が報告されているか**（§12.8-8。**`SUPP-001` §4.5 / §5.5 / §5.8 を含む**） | 欠けていたら「高」 |

---

## 12. レビュー完了の判定

**重大 0 件になるまで往復する。★上限は 2 回**（`CLAUDE.md` §9 停止規律）。**上限に達したら未解消項目を `followup-backlog` §J へ必須 5 フィールドで記録し、直ちに停止する。**

### 12.1 ★本チェックリスト自身の検査（**発行前に設計担当が行う**）

- [x] **指示書の版数と一致しているか**（v1.4.0 ⇄ v1.4.0）
- [x] **§0.3 が「実装していない」を欠陥と判定しない形になっているか**（本サブは調査で終わりうる）
- [x] **破壊確認の観点が「壊す対象が実在したか」を含んでいるか**（`M-78`）
- [x] **完了の証明が「緑の回数」に依存しない形になっているか**（6-2）
- [x] **★実査で決まる面を、断定形で書いていないか**（`M-77`）
- [x] **★★§0.3-3（「2 倍以内」）が撤回済みであることを反映しているか**（**v1.2.0 の追随で取り残されていた。2026-08-26 是正**）
- [x] **★§0.1-3 が「`internal/` / `cmd/` は 0 バイト」のままになっていないか**（**§12 追補で 2 か所だけ差分が入る**）
- [x] **★追補分の重大判定が「古いフロントをテストする」事故を含んでいるか**（13-3）
- [x] **★★13-2 の判定が反転済みか**（`D-569`＝**揃っていないのが正しい**）
- [x] **★★`cmd/` の 3 か所目を「重大」判定に含めているか**（13-6b）
- [x] **★§0.3 が「`SUPP-001` を製造が直していない」「CI 定義を触っていない」を欠陥と判定しない形になっているか**

---

*以上、M24-09c レビューチェックリスト **v1.4.0**。*
