# M27-02a レビューチェックリスト: 届かないものを届かせる（保存の理由・候補選択の操作）

| 項目 | 内容 |
|------|------|
| 文書ID | M27-02a-review-checklist |
| バージョン | **1.0.0** |
| 対象指示書 | `docs/instructions/M27-02a-reachability-save-path-and-selectors.md` **v1.3.0** |
| 推奨モデル | **高精度レビュー**（**★共通部品の改修が利用側の全数へ波及する ／ ★並列レーンの侵食を見る必要がある**） |
| 作成者・作成日 | 設計担当 Claude（設計卓・フェーズ4 期）/ 2026-09-02 |

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| 1.0.0 | 2026-09-02 | 初版（**`D-691`**）。**★指示書 v1.2.0（`D-690` の射程確定）を織り込んだ** |

---

## 0. レビュー担当への前置き

### 0.1 レビュー前の準備

1. 指示書 **v1.2.0** を全文読む。**★とくに §2.1.3.1 / §2.1.3.2（射程の確定）と §4（並列レーン）。**
2. 完了報告 `docs/progress/M27-02a-completion-report.md` を読む。
3. **★`docs/handover/followup-backlog.md` の 4 行を読む**——`editor-validation-errors-unreachable-from-ui` ／ `editor-tab-badge-misroutes-setups-errors` ／ `combo-put-error-message-empty` ／ `tag-field-keyboard-unreachable`。**★これらが本サブの一次情報である。**
4. **★`DES-005` §5.7（順送り契約の表を含む）を読む**——**`M24-12` が作った入力補助と、`CHANGE-151` で増えた info-mark がすでに在る。★本サブがそれを壊していないかの照合先。**
5. **★`DES-006` §2.7 の「許可リストか対象外リストか」を読む**——**`M27-01` の教訓であり、集合へ値を足す変更が本サブに在れば同じ罠に当たる。**
6. **★`docs/process/parallel-board.md` `D-689` / `D-690` を読む**——**並列レーンの境界と射程の確定はここが正本。**

### 0.2 レビューの基本姿勢

コードを変更せず、指摘のみを行う。

### 0.3 ★★重大でないもの（**誤判定しないこと**）

| # | これは欠陥ではない |
|---|---|
| **★★1** | **検証エラーが UI から到達不能のままであること。** **設計卓が (a)「そのまま残す」を採った**（`D-684`・指示書 §2.1.1）。**★逆に (b)〔UI 側の上限を緩めて到達可能にする〕や (c)〔機能を畳む〕をやっていたら「重大」である** |
| **★★2** | **`GET /api/moves` の 500 `internal_error` の空 `message` が直っていないこと**——**ただし完了報告が (b) 判定〔他の 500 経路も `message` を持たない＝方針決定であり射程外〕を根拠付きで示している場合に限る**（`D-690`・指示書 §2.1.3.2）。**★根拠が無いまま直していなければ「高」。★逆に (a) 判定なのに直していなければ「高」** |
| **★★3** | **`SD-015` / `SD-016` / `tag-field-keyboard-unreachable` が 1 つの共通部品へ寄せられて**いない**こと**——**ただし完了報告が「別実装である」ことを実測で示し、寄せる／寄せないの判断と波及の数を書いている場合に限る**（指示書 §2.2.1-3）。**★「寄せるほうが正しい」とは限らない。★根拠が無い場合は「高」** |
| **★★4** | **`drive_impact_punish_counter` が確定反撃の「区分を判定できない反撃」に出ること。** **★`M31` の射程であり本サブは触らない**（`CHANGE-151` §6-1） |
| **★5** | **`M28-01`（正式名リネーム）が未適用でモジュールパスが旧のままであること。** **★順序が変わった**（`D-689`。指示書 §4.3） |
| **★6** | **`docs/design/` が編集されていないこと。** **★設計書は設計卓が改訂する**（指示書 §3-6）。**★★逆に編集されていたら「高」** |
| **★7** | **`docs/handover/followup-backlog.md` の §J 以外が編集されていないこと。** **★製造が直接書けるのは §J だけである**（`D-382`）。**★★逆に編集されていたら「高」** |
| **8** | **`P-44`（エディタ全面テコ入れ）に手が付いていないこと。★保留中である** |

### 0.4 ★★並列レーンの侵食（**本サブに固有・最優先で見る**）

**`M14-03f`（第四波 seed）が同時に走っている**（`D-689`）。**⇒ 面が交わらないことが並列を許した唯一の根拠である。**

| # | 見ること | 判定 |
|---|---|---|
| **★★1** | **`migrations/` に差分が無いか** | **在れば「重大」**（**マイグレ連番は `M14-03f` が `000082` から連番で消費中であり、自採番すると衝突する**＝`D-293`。**要ると判明したときは設計卓へ請求するのが正しい経路**） |
| **★★2** | **`character_data/` に差分が無いか** | **在れば「重大」** |
| **★★3** | **`internal/seedgen/` に差分が無いか** | **在れば「重大」。★`internal/` の中で分かれている点に注意する**——**`internal/api` / `internal/service` は射程内であり、`internal/seedgen` だけが射程外である** |

**★上 3 点は完了報告の記述ではなく、実際の変更ファイル一覧で見ること。**

### 0.5 報告フォーマット

`docs/progress/m27-02a-review.md` へ、**重大 / 高 / 中 / 低**の 4 段で、各件に**根拠のファイル・行**を添えて書く。

---

## 1. 束 A — 保存が通らないときに理由へ届くか（最重要）

| # | 検査 | 重み |
|---|---|---|
| **1-1** | **★★`PUT /api/combos/:id` と `PATCH /api/combos/:id` の両方**で検証エラー応答の `message` が空でなくなっているか。**★`PATCH` は Plan Mode の全数調査で出た新発見であり、追跡行には無い**（`D-690`） | **重大** |
| **1-2** | **★`message` の組み立てが 1 か所へ寄っているか。★呼び出し側で個別に文字列を書いていないか**（`CLAUDE.md` §4 マジックストリングの定数化。指示書 §2.1.3-2） | **高** |
| **1-3** | **★`POST` の既存の文言（「バリデーションエラーがあります」）が変わっていないか。★合わせに行った結果 `POST` 側が動いていたら後退である** | **高** |
| **1-4** | **★空 `message` の全数が報告に書かれているか**（指示書 §2.1.3-3・§5-4b）。**★「直した 2 件」だけを書いて母数を書いていないのは不足** | **中** |
| **1-5** | **タブのバッジが `setups[*]` をレシピタブへ振り分けるか**（`D-593` の裁定どおり） | **高** |
| **1-6** | **★★「編集モードで `setups[*]` の ERROR が出うるか」を先に確かめた記録が在るか**（指示書 §2.1.2-1）。**★出ると分かったのに前方一致を足しただけなら「高」**——**編集モードではセットプレイ登録の欄が出ないため、欄の無いタブへ誘導する形が残る** | **高** |
| **1-7** | **★バッジの計上規則が定数化されているか**（指示書 §2.1.2-3）。**規則が実装に散っていれば「中」** | **中** |
| **1-8** | **★★動線として通ることを実操作で確かめた記録が在るか**（指示書 §2.1.4）。**★「3 件とも直した」は動線が通った証明にならない。★切れている箇所が在るならその事実が書かれているか** | **高** |
| **1-9** | **★(a) の前提**〔BE 検証が発火したとき応答が画面へ届く〕**を発火経路を作って確かめた記録が在るか**（指示書 §2.1.1-2）。**★確かめずに (a) を通したなら「高」**——**備えになっていない備えを残すことになる** | **高** |

---

## 2. 束 B — 候補選択に操作が届くか

| # | 検査 | 重み |
|---|---|---|
| **★★2-1** | **部品の同定結果が報告に在るか**（指示書 §2.2.1・§5-2）。**★3 件が同じ部品か。別実装なら寄せた／寄せなかったの判断と波及の数** | **高** |
| **2-2** | **キャラ選択で `Enter` が効くか。★「反応しない」の正体が特定されているか**（指示書 §2.2.2-1）。**★症状に対処しただけで原因が書かれていなければ「中」** | **高** |
| **★2-3** | **`Enter` がフォームの意図しない送信を起こしていないか**（指示書 §2.2.2-3）。**★候補選択の `Enter` と保存の `Enter` は別である** | **重大** |
| **2-4** | **テキストボックス内のドラッグ選択で候補リストが閉じなくなっているか**（指示書 §2.2.3-2） | **高** |
| **★2-5** | **外側クリックで閉じる挙動が残っているか**（指示書 §2.2.3-3）。**★閉じない部品は、閉じすぎる部品より使いにくい** | **重大** |
| **2-6** | **タグ欄で矢印キーの候補移動と `Enter` の選択が効くか**（指示書 §2.2.4-1） | **高** |
| **★★2-7** | **`Enter` の扱いが `SD-015` と矛盾していないか**（指示書 §2.2.4-2）。**★2 件は同じ鍵の話であり、束 B を 1 本で持っている理由がこれである** | **重大** |
| **2-8** | **複数選択での `Enter` の意味**〔選んで閉じる／選んで開いたまま〕**が決められ、報告に書かれているか**（指示書 §2.2.4-3） | **中** |

---

## 3. 既存の破壊（非破壊性）

| # | 検査 | 重み |
|---|---|---|
| **★★3-1** | **`M24-12` の入力補助が生きているか**——**単一選択 5 欄のボタン群 ／ 起き攻め 12 個の数字キー ／ `type=flag` の数字キー**（`DES-005` §5.7） | **重大** |
| **★★3-2** | **順送り（`useFieldSequence`）が壊れていないか。★とくに `CHANGE-151` で入った info-mark へフォーカスが載っていないか**——**`focusableIn` は `querySelector`（単数）で `[data-seq-stop]` 内の最初の focusable を拾うため、DOM 順が効く**（`DES-005` の順送り契約） | **重大** |
| **3-3** | **`SearchableSelect` を使う他の画面**〔一覧・ゴミ箱・セットプレイ・確定反撃 等〕**が壊れていないか。★波及の全数が報告に在るか** | **重大** |
| **3-4** | **`CHANGE-151` の 8 値 / 4 値の表示が壊れていないか**（`M27-01` の成果物の上に積んでいる） | **高** |
| **3-5** | **API のエラー応答の形**〔ステータス・エラーコード・`issues[]`〕**が変わっていないか。★変えたのは `message` だけか** | **重大** |
| **3-6** | **`recipe_hash` ／ 重複判定キー（`VAL-C02`）に触れていないか** | **重大** |

---

## 4. テストの妥当性

| # | 検査 | 重み |
|---|---|---|
| **4-1** | **`PUT` / `PATCH` の `message` が空でないことがテストで固定されているか。★片方だけなら「高」** | **高** |
| **4-2** | **キーボード操作**〔`Enter` ／ 矢印 ／ ドラッグ中に閉じない ／ 外側クリックで閉じる〕**がテストで固定されているか** | **高** |
| **★4-3** | **★`M27-01` の先例に倣い、位置インデックス（添字）参照を新たに増やしていないか**——**正典配列の途中へ値を挿入すると型検査もテストも緑のまま別の値を指す**（`M27-01` §3-2） | **中** |
| **4-4** | **テストが「実装をなぞるだけ」になっていないか。★壊れる条件を実際に作って落ちることを見ているか** | **中** |

---

## 5. ドキュメント・進捗ログ

| # | 検査 | 重み |
|---|---|---|
| **5-1** | **完了報告 `docs/progress/M27-02a-completion-report.md` が在るか。★ファイル名がこれであるか**（**`check-progress-log-index.sh` は `*completion-report*.md` しか走査しない**） | **重大** |
| **5-2** | **完了報告に §1〜§7 ＋ §4b が揃っているか**（指示書 §5） | **高** |
| **5-3** | **`docs/progress/progress-log.md` へ索引行が追記されているか**（`CLAUDE.md` §8） | **高** |
| **5-4** | **★6 件それぞれの「いまも再現するか」の判定が在るか**（指示書 §0.3）。**★解消済みなら、いつ何で解消したかまで書かれているか** | **高** |
| **5-5** | **CHANGE たたき台が在るか**（`DES-005` / `DES-006` / `DES-002` のどこへ何を書くか）。**★製造は自採番していないか**（`D-293`） | **高** |

---

## 6. 破壊確認（**実際に壊して、検出できることを見る**）

| # | 壊し方 | 期待 |
|---|---|---|
| **6-1** | `PATCH` の `message` を空へ戻す | **4-1 のテストが落ちる** |
| **6-2** | info-mark を選択肢群より **前** の DOM 位置へ移す | **順送りのテストが落ちる**（3-2） |
| **6-3** | タグ欄の矢印キー処理を外す | **4-2 のテストが落ちる** |
| **6-4** | 外側クリックで閉じる処理を外す | **2-5 のテストが落ちる**（**★「閉じない」を検出できるか**） |

**★4 件とも「落ちない」なら、そのテストは何も守っていない。⇒ 「高」。**

---

## 7. 重大な問題の判定基準（完了承認を妨げる）

| # | 重大 |
|---|---|
| **1** | **`migrations/` ／ `character_data/` ／ `internal/seedgen/` に差分が在る**（§0.4） |
| **2** | **マイグレを自採番している**（`D-293`） |
| **3** | **`M24-12` の入力補助または順送りが壊れている**（3-1 / 3-2） |
| **4** | **`SearchableSelect` の改修で他画面が壊れている**（3-3） |
| **5** | **API のエラー応答の形が `message` 以外も変わっている**（3-5） |
| **6** | **`recipe_hash` / `VAL-C02` に触れている**（3-6） |
| **7** | **候補選択の `Enter` がフォームを送信する**（2-3） |
| **8** | **外側クリックで候補リストが閉じなくなった**（2-5） |
| **9** | **`Enter` の扱いが `SD-015` と `tag-field` で矛盾している**（2-7） |
| **10** | **設計卓の裁定 (a) に反して (b) / (c) を実施している**（§0.3-1） |
| **11** | **完了報告が無い、またはファイル名が違う**（5-1） |

---

## 8. 軽微な問題の判定基準（持ち越し許容）

- 文言・表記ゆれのうち、意味が変わらないもの。
- テスト名・コメントの体裁。
- **★ただし「持ち越す」と決めたものは完了報告に書くこと**（`D-675`＝「触れなかった」と「やらないと決めた」を区別する）。

---

## 9. レビュー完了の判定

- **重大 0 件**であること。
- **高**が残る場合は、取り込みの可否を製造担当が判断し、不採用なら理由を完了報告に書くこと。
- **★再レビューの往復上限は 2 回**（`CLAUDE.md` §9 停止規律）。**★上限に達したら未解消項目を `followup-backlog` §J へ必須 5 フィールド付きで記録し、直ちに停止すること。**

### 9.1 ★本チェックリスト自身の検査（**発行前に設計担当が行う**）

- **指示書の版数と対象指示書欄が一致しているか**（`bash scripts/check-instruction-format.sh`）。
- **★★§0.3「重大でないもの」が、指示書がスコープ外・委任と定めた項目をすべて拾っているか。**
- **★§0.4（並列レーンの侵食）が在るか**——**本サブに固有であり、他サブのチェックリストには無い。**

---

*以上、M27-02a レビューチェックリスト **v1.0.0**。* **★★本サブの最大の危険は 2 つある。(1) 共通部品の改修が、指示書に書かれていない画面を壊すこと（3-3）。(2) 並列で走っている `M14-03f` の面へ踏み込むこと（§0.4）。★どちらも完了報告の記述ではなく、実際の変更ファイル一覧で見ること。**
