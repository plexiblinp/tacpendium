# M23-02 レビューチェックリスト

| 項目 | 内容 |
|------|------|
| 対象指示書 | `docs/instructions/M23-02-setup-restore-and-permanent-delete.md` v1.3.0（セットプレイの復元と完全削除） |
| 文書ID | M23-02-REVIEW |
| 対象指示書ID | M23-02 |
| レビューモデル | 高精度レビュー（実モデル名は `docs/human-notes/model-allocation.md` が正本。実使用は開発者判断） |
| バージョン | 1.3.0 |
| 作成者・作成日 | 設計担当 Claude（設計卓／M23 起票期）/ 2026-08-20 |

> 文書ID ⇄ 実パスの正は `docs/handover/docs-map.md`（自動生成）。

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| 1.3.0 | 2026-08-20 | **指示書 v1.3.0 と対**（**D-491**）。**§1 へ §4.1-6 の 2 か所の是正を追加 ／ §4 へテスト 2 本を追加 ／ §0.3 へ N-11・N-12 ／ §9 へ「劣化を出荷していないこと」を追加。** |
| 1.2.0 | 2026-08-20 | **指示書 v1.2.0 と対**（**D-490**）。**§3 へ「`M23-01` の 7 列を崩していないこと」を追加し、§8 の並列相手を `M24-09a` のみへ改めた。** |
| 1.1.0 | 2026-08-20 | **指示書 v1.1.0 と対**（**D-485** / **D-486**）。**§3 の「参照元コンボが示される」を「拒否されたことが伝わる」へ緩め、専用の表示を作っていないことを §5 の確認項目に足した。** **§0.3 へ N-10 を追加。** |
| 1.0.0 | 2026-08-20 | 初版（指示書 v1.0.0 と対）。**最重要ゲート＝「共有されたセットプレイを論理削除しても、他の生きたコンボの紐付けが壊れないこと」。** |

---

## 0. レビュー担当への前置き

### 0.1 レビュー前の準備

1. 指示書 `M23-02` を通読する（**§1.2「案 P1 は事故ではない」と §1.5 のスコープ外 8 件を頭に入れてから差分を見る**）。
2. **完了報告に Plan Mode の実査 9 件（指示書 §3.3）の結果が揃っていることを先に確認する。** **揃っていない項目がある場合、その項目に依存する実装は「確認できない」として §11 へ回す。** **★とくに §3.3-1 は本サブの安全性そのものであり、欠けていたら §9 の重大として扱う。**
3. **`docs/progress/M23-RESEARCH-01-report.md` §C / §E-2 を読む**（本サブの前提の実測）。

### 0.2 レビューの基本姿勢

- **本サブの中心は「新しい API を 2 本足したこと」ではなく、「既存の削除の挙動を変えたこと」である。** **API が動くことだけを見て合格にしないこと。**
- **★共有の状態で見ること。** セットプレイが 1 つのコンボにだけ紐付いている状態では、**本サブが直した壊れ方が再現しない。** テストが 1 対 1 で書かれていたら、それは §9 の重大にあたる。

### 0.3 ★重大でないもの（**誤判定しないこと**）

| # | 誤判定しやすい状態 | 実は仕様である理由 |
|---|---|---|
| N-1 | **論理削除の後も `combo_setups` に行が残っている** | **これが本サブの目的である**（指示書 §4.1。**案 P1 の撤回**＝**D-483**）。**「消し忘れ」と読んで消させないこと** |
| N-2 | **`GetRecipeCache`（setups）に `deleted_at IS NULL` が無いまま** | **指示書 §1.5-1。** **`M23-03` の担当**（`M23-overview` §4.9 の穴 6 か所の 1 つ）。**本サブで塞ぐと 2 サブが同じ関数を触る** |
| N-3 | **`recipe_cache` を論理削除時に消したままである** | **指示書 §2.2-7。** **復元時に作り直す側を採っている**（コンボ側の `Restore` と同型） |
| N-4 | **削除・復元が楽観排他を持たない** | **指示書 §1.5-6。** **コンボ側の `DELETE /api/combos/:id` も持たない**（報告 §D の実測）。**揃えてある。判断は `M23-03`** |
| N-5 | **参照元がすべてゴミ箱に居るときに完全削除が通る** | **指示書 §4.4-6 が明示的にそう決めている。** **★「コンボを復元しても紐付けが欠ける」帰結は既知の非対称であり、`M23-05` の入力である** |
| N-6 | **本サブ適用前に論理削除されたセットプレイは、復元しても紐付けが戻らない** | **指示書 §4.2-6。** **遡って復旧することはできない**（どのコンボに紐付いていたかの情報が残っていない）。**仕様である** |
| N-7 | **完全削除が CASCADE を使わず 4 表を明示的に消している** | **指示書 §4.3-2。** **`P-04` により CASCADE の発火が保証されない。** **冗長ではなく意図である** |
| N-8 | **ゴミ箱画面のセットプレイの見せ方が素朴である** | **指示書 §1.5-2 / §4.5-4。** **作り込みは `M23-06` の担当。** 本サブは「押せるようにする」までである |
| N-9 | **`DES-006` に VAL コードが起きていない** | **指示書 §1.5-5 / §4.4-5。** **`M23-04` がまとめて行う。** 本サブは応答の形を報告するだけである |
| N-11 | **ゴミ箱のセットプレイが、コンボの表とは別のテーブルに置かれている** | **指示書 §4.5-5**（**D-491**）。**混ぜる形は `M23-01` の 7 列の撤去を巻き戻すため採れない。** **★「統一されていない」と読んで 1 つの表にまとめさせないこと** |
| N-12 | **ゴミ箱のセットプレイに一括選択が無い** | **指示書 §4.5-5。** **既存の選択状態はコンボ id の配列であり、混ぜると id が衝突する。** **一括操作は `M23-06`** |
| N-10 | **拒否のときに、参照元コンボの一覧が画面に出ない** | **指示書 §4.4-3b**（**D-485**・開発者回答＝**専用の表示は作らない**）。**★応答の本体には含まれているのが正しい**〔`M23-06` の材料〕**が、画面が列挙しないことは仕様である。** **★ただし「何も起きない」は仕様ではない**——**§4.4-3c が短い文言 1 つを求めている。無反応なら §9 の重大** |

### 0.4 報告フォーマット

- 各節「OK / 重大（§9）/ 軽微（§10）/ 質問（§11）」。

---

## 1. 設計・パターンとの照合（最重要）

- [ ] **`DeleteSetup` から `combo_setups` の物理削除が外れている**（指示書 §4.1-1）。
- [ ] **`// 案 P1: ...` のコメントが、撤回の経緯を残す形へ更新されている**（同 §4.1-4）。**★消しただけになっていないこと。**
- [ ] **`combo_setup_results` の扱いが §3.3-2 の実査結果と一貫している**（同 §4.1-3）。
- [ ] **復元がコンボ側の形に揃っている**（同 §4.2-1。ルート・ハンドラの応答の形）。
- [ ] **復元が `version` に触れていない**（同 §4.2-3。報告 §D-5 のコンボ側の実測と揃っている）。
- [ ] **復元が `recipe_cache` を同一トランザクションで作り直している**（同 §4.2-4）。
- [ ] **復元が `setup_steps` に対して何もしていない**（同 §4.2-5）。**★「確認したうえで何もしない」旨が完了報告に書かれているか。**
- [ ] **完全削除が `combo_setup_results` → `combo_setups` → `setup_steps` → `setups` の順で明示削除している**（同 §4.3-2）。
- [ ] **完全削除が 1 トランザクションで行われている**（同 §4.3-4）。
- [ ] **論理削除されていないセットプレイへの完全削除が拒否される**（同 §4.3-3）。
- [ ] **完全削除の前チェックが `combos.deleted_at IS NULL` との結合を挟んでいる**（同 §4.4-2）。
- [ ] **★★§4.1-6 の 2 か所が是正されている**（**D-491**）——**(a) 引き継ぎオプションの要否を判定する数え上げ ／ (b) 検証結果の書き込みを守るガード。** **どちらも `setups` へ結合し `deleted_at IS NULL` を持つこと。** **★これは「他所の穴を塞いだ」のではなく「本サブが壊した不変条件の後始末」である。**
- [ ] **★§4.1-6 の 2 か所以外へ広げていない**（指示書 §9.1-2。**`M23-03` の担当へ踏み込んでいないこと**）。
- [ ] **★削除済みセットプレイ一覧の経路が足されている**（同 §4.2-8）。**引数の綴りが既存の同経路に揃っていること**（コンボ側の綴りを持ち込んでいないこと）。**★`combo_setups` の件数だけを数えていないこと**——**ゴミ箱に居るコンボからの参照まで数えてしまう。**

## 2. データ・API 契約・スキーマの不変（非破壊性）

- [ ] **スキーマを 1 つも変えていない。マイグレを 1 本も足していない**（指示書 §4.7 / §7.2）。
- [ ] **`PATCH /api/setups/:id` の挙動を変えていない**（同 §2.2-4）。
- [ ] **`DELETE /api/combos/:comboId/setup-links/:setupId` を変えていない。呼ぶ側にも回っていない**（同 §2.2-3）。
- [ ] **`combos` 側の復元・完全削除の挙動を変えていない**（同 §2.2-2）。
- [ ] **JSON タグが camelCase である**（`CLAUDE.md` §4）。
- [ ] **★設計書本体（`DES-002` / `DES-005` / `DES-006`）を製造が直接編集していない**（`CLAUDE.md` §8。反映は `CHANGE-122` で設計卓が行う）。

## 3. フロントエンドの動作仕様

- [ ] **ゴミ箱からセットプレイの復元・完全削除を呼べる**（指示書 §4.5-4）。
- [ ] **拒否の応答を受けたときに、拒否されたことが利用者に伝わる**（同 §4.4-3c）。**★参照元コンボの列挙は求めない**（同 §4.4-3b＝**D-485**）。**★確認するのは「押しても無反応にならないこと」である。**
- [ ] **i18n キーが ja / en の両方に在る**（同 §4.4-7。playbook §4.13）。**★片方だけ足していないこと。**
- [ ] **セットプレイ型の扱いが一貫している**（`web/CLAUDE.md` §2。**★DTO を変えた場合、分岐の全てに反映されていること。変えていない場合はそれでよい**＝指示書 §3.3-8）。
- [ ] **見せ方の作り込みへ踏み込んでいない**（指示書 §1.5-2。**★`M23-06` の領域**）。
- [ ] **★`M23-01` の撤去を巻き戻していない**（指示書 §3.3-9。**D-490**）。**ゴミ箱の一覧が 7 列のままであること ／ 残日数の列・「あと N 日」・「期限切れ」・本文の 90 日告知が復活していないこと ／ 行内エラー行の `colSpan` が 7 のままであること。** **★これは「古い状態を見ながら配線した」ことの検出である。** **配線そのものが正しくても巻き戻りうる。**

## 4. テストの妥当性（ケース数で確認。指示書 §5 ↔ §7 DoD）

- [ ] **指示書 §5 の 10 件がすべて在る**（サービス層 6・リポジトリ層 2・Vitest 1・E2E 1）。
- [ ] **★§5-1 が「複数のコンボが同じセットプレイを共有している状態」で書かれている。** **1 対 1 で書かれていたら §9 の重大である**——**共有時の壊れ方を検出できない。**
- [ ] **★§5-2 が A と B の両方を確認している。** 片方だけで合格にしていないこと。
- [ ] **§5-3 が実 DB で行われている**（`SUPP-001` §5.5.4 規約 14）。**モックだけで「出ない」を主張していないこと。**
- [ ] **★§5-4（拒否される）と §5-5（参照元がすべてゴミ箱なら通る）が対で在る。** **片方だけでは「常に拒否される」状態と区別できない。**
- [ ] **§5-6 が 4 表すべてを確認している**（`combo_setup_results` / `combo_setups` / `setup_steps` / `setups`）。**★`M23-overview` §4.10 が記録した「CASCADE を守るテストが 0 件」という穴を、本サブが繰り返していないこと。**
- [ ] **§5-8（復元が `version` を動かさない）が在る。**
- [ ] **★★§4.1-6 のテスト 2 本が在る**——**(a) 紐付いたセットプレイを全部ゴミ箱へ入れたコンボの `knockdown_advantage` を変更できること ／ (b) ゴミ箱のセットプレイへ検証結果を書けないこと。** **★(a) が無いまま合格にしないこと**——**これは本サブが作りかけた利用者可視の劣化であり、テストが唯一の歯止めである。**
- [ ] **§5-10 の E2E が、紐付け 2 本 → 論理削除 → 復元 → 両方から見える、までを 1 本で通している。**
- [ ] **★完了報告のテスト結果が、コマンド自身の出力（スイート名・pass/fail の件数）で書かれているか**（教訓 **E-125**）。**「`go test ./...` 全緑」「exit 0」だけを根拠にしていないか**——**依存が未インストールでも終了コード 0 を返す経路がある**（M20-06 で `vitest: not found` が exit 0 として報告され、一度「フロントは緑」と誤報告された）。**件数が書かれていない報告は、そのテストが 1 件も走っていない可能性を排除できない。**
- [ ] **E2E を実行しなかった場合、「実行しなかった」と書かれている**（「緑」と書いていない。指示書 §7.2）。
- [ ] **既存 spec のうち、セットプレイやコンボの件数を絶対値で数えているものと衝突していない**（指示書 §5 の注記）。

## 5. 設計意図との整合（精神の確認）

- [ ] **`FR013`（所有者・監査ログを持たない）に触れていない**（指示書 §2.2-6）。**★「削除した本人だけが復元できる」形になっていないこと。**
- [ ] **「紐付けを保つ」を「ゴミ箱のセットプレイを見せる」に読み替えていない**——**除外は参照側に委ねている**（指示書 §4.1-5 / §4.5）。
- [ ] **完全削除を「拒否」ではなく「確認つき強行」に作り替えていない**（指示書 §4.4-3。**D-485** で確定済み）。
- [ ] **★参照元コンボを列挙する専用の表示（ダイアログ・パネル・一覧）を新設していない**（指示書 §4.4-3b。**D-485**）。**★良かれと思って足しやすい箇所である。**
- [ ] **★拒否の判定に `combos.deleted_at IS NULL` 以外の述語を足していない**（指示書 §4.4-6。**D-486**。**ゴミ箱に居るコンボまで拒否の理由に数えていないこと**）。
- [ ] **`P-04` の根治へ踏み込んでいない**（指示書 §1.5-4）。**★セットプレイ側の新経路を明示削除で書くことと、`PRAGMA` の設定を変えることは別である。**
- [ ] **コンボ側の既存 5 表を明示削除へ移していない**（指示書 §1.5-3。**`M23-06` の担当**）。

## 6. コード品質・規約遵守

- [ ] 曖昧語依存でない（Playbook §4.1）。定数化・命名一貫。
- [ ] i18n 追加時は ja/en 両ロケール parity（Playbook §4.13）。
- [ ] `console.log` / `fmt.Println` が残っていない。
- [ ] **エラーが wrap されている**（`CLAUDE.md` §4。`fmt.Errorf("...: %w", err)`）。
- [ ] **推測で進めた箇所に `// 推測: 〜と仮定した` が入っている**（`CLAUDE.md` §9-3。指示書 §9.2 の 4 件が該当しうる）。

## 7. 既存挙動の温存（非破壊性）

- [ ] **セットプレイの作成・編集・紐付け・紐付け解除が、これまでどおり動く。**
- [ ] **論理削除中のセットプレイが、コンボ詳細・セットプレイ一覧・候補取得に出ない**（報告 §E-2 の 13 本の述語が効いたままである）。
- [ ] **`UpsertSetupResult` / `DeleteSetupResult`（成立条件の検証結果）が壊れていない。** **★紐付けを残すようにしたことで、これらが論理削除中のセットプレイに対して通ってしまう状態が生まれていないか**（**指示書 §3.3-1 の走査で列挙されているべき箇所である**）。
- [ ] **コンボ側の手動削除 → ゴミ箱 → 復元 → 完全削除の一連が、これまでどおり動く。**

## 8. ドキュメント・進捗ログ

- [ ] 完了報告に **Plan Mode 確定方式・変更点・テストケース数・既知の制約** が書かれている。
- [ ] DES CHANGE が要なら設計担当起票前提で、製造が DES 本体を直接編集していない。
- [ ] **撤回・変更した挙動の残骸が残っていないか。** 指示書 §4.9 の 3 項目について、**走査キーワードで実際に走査した結果**が完了報告にあるか。
- [ ] 走査対象に **3 系統すべて**が含まれているか——**本番コード／テスト資産（コメント・ヘルパ名・fixture・spec 名）／設計文書・指示書**。**本番コードだけを見て「直っている」と報告していないか**（動作に影響しない残骸はテストでは検出できない）。**★とくに §4.9-2**（**論理削除の後に紐付けが 0 件であることを前提にしたテスト**）**は期待値が反転するため、見落とすとテストが赤くなるか、誤った期待値のまま緑になる。**
- [ ] **★並列サブの as-built との突合が行われているか**（教訓 `E-121` ／ ボード **D-284**・**D-297**）。**★本サブの並列相手は `M24-09a` のみである**（**D-490**。**`M23-01` は並列相手ではなく前提である**——完了済みで、本サブはその上に積む）。**完了報告に相手が名指しで書かれているか。**
- [ ] **★完了報告に「■ 併せて更新が要るもの」の節があるか**（教訓 `E-114` ／ ボード **D-277**）。**該当が無い場合も「なし」と書かれているか。** **★本サブは `CHANGE-122` を消費し、マイグレは消費しない。** 消費した CHANGE 番号の registry 登録・**「次の番号」の写し先**・版を上げた文書の参照元。
- [ ] **★指示書 §7.5 が求める 6 件が、完了報告の独立した節に置かれているか**（設計卓が `CHANGE-122` へ写す材料である）。**1 件でも欠けていたら §9 の重大にあたる**——**設計卓は実装ソースを読めないため、報告に無いものは存在しないのと同じである。**
- [ ] **★案 P1 の撤回であることと、撤回して安全である根拠（指示書 §4.1-5）が完了報告に明記されているか**（同 §1.2）。
- [ ] **設計伝達レポート §4 に followup の更新候補が置かれているか。** **★製造が `followup-backlog.md` を直接編集していないこと**（**D-382**）。

---

## 9. 重大な問題の判定基準（完了承認を妨げる）

- **★★本サブが作った劣化を出荷していないこと**（指示書 §4.1-6。**D-491**）。**紐付いたセットプレイが全部ゴミ箱に居るコンボの `knockdown_advantage` が変更できない状態のまま完了していたら重大である。** **★撤回前は通っていた操作であり、退行にあたる。**
- **★最重要ゲート＝共有されたセットプレイを論理削除しても、他の生きたコンボの紐付けが壊れないこと。** **これが開発者要求そのものである**（**D-484**）。**テストが 1 対 1 の状態でしか書かれていない場合も、この重大に含める**——**守られていることを主張できていない。**
- **完全削除の前チェックが `combos.deleted_at IS NULL` との結合を持たない**（指示書 §4.4-2）。**ゴミ箱のコンボまで拒否の理由に数えると、利用者が完全削除できなくなる。**
- **完全削除が CASCADE に依存している**（指示書 §4.3-2）。**`P-04` により発火が保証されず、`combo_setups` / `combo_setup_results` が残留しうる。**
- **完全削除が 1 トランザクションになっていない**（一部だけ消えた状態が残りうる）。
- **論理削除されていないセットプレイを完全削除できる**（ゴミ箱を経由しない削除の経路。指示書 §4.3-3）。
- **復元が `version` を動かしている**（指示書 §4.2-3。コンボ側と食い違う）。
- **指示書 §3.3-1 の走査結果が完了報告に無い**（**案 P1 の撤回が安全かどうかを誰も確かめていないことになる**）。
- **§3.3-1 で見つかった穴を、報告せずに塞いでいる**（指示書 §9.1-2。**塞ぐ判断は設計卓が行う**）。
- **`setups` 系へスキーマ変更を入れている／マイグレを足している**（指示書 §4.7 ／ §9.1-1）。
- **`DES-006` に VAL コードを起こしている**（指示書 §1.5-5。**`M23-04` と衝突する**）。
- **スコープ外 8 件（指示書 §1.5）のいずれかへ踏み込んでいる。**

## 10. 軽微な問題の判定基準（持ち越し許容）

- ゴミ箱画面のセットプレイの配線の見た目が素朴である（**★作り込みは `M23-06`**）。
- 拒否の応答の本体に載せる項目の選び方（id と名前が在れば足りる）。
- 関数名・ファイル名の語の選び方。
- テストの命名・配置の流儀。
- 既に生きているセットプレイへの復元の扱いが、コンボ側と細部で違う（**理由が報告に在る場合**。指示書 §9.2-4）。

## 11. 質問・確認事項のフォーマット

- 「指示書 §X.X / `DES-00X` §X / `M23-RESEARCH-01` 報告 §X に対し実装が Z。意図確認したい」の形で根拠節併記。

## 12. レビュー完了の判定

- §1〜§8 が全て OK、§9 重大ゼロ（**特に「共有されたセットプレイの論理削除で他のコンボの紐付けが壊れないこと」**）、§0.1 の Plan Mode 実査 9 件が揃っている。§10 軽微は持ち越し可。

### 12.1 ★本チェックリスト自身の検査（**発行前に設計担当が行う**）

- [ ] **本書と対の指示書の間に、節番号の食い違いが無い。**
  → 本書が参照する指示書の節は §1.2 / §1.5 / §2.2 / §3.3 / §4.1 / §4.2 / §4.3 / §4.4 / §4.5 / §4.7 / §4.9 / §5 / §7 / §9 / §11 であり、**指示書側に実在する**（**★§4.9 は §4.7 との衝突を避けて寄せた番号である**）。
- [ ] **指示書が禁じていない事項を「重大」として求めている項目が無い。**
  → §9 の各項目は指示書 §1.5 / §2.2 / §4 の禁止・固定と 1 対 1 で対応する。
- [ ] **達成不能な条件を重大として求めていない。**
  → **`ON DELETE CASCADE` が効くことを重大に据えていない**（`P-04` により保証されないため。**逆に「CASCADE に依存していること」を重大にしてある**）。
  → **本サブ適用前に論理削除されたものの紐付けが戻ることを求めていない**（§0.3 N-6。復旧に要る情報が残っていない）。
  → **`GetRecipeCache`（setups）が塞がれていることを求めていない**（§0.3 N-2。**`M23-03` の担当**）。

---

*以上、M23-02 レビューチェックリスト v1.3.0。配置 `docs/instructions/reviews/M23-02-review-checklist.md`。指示書 v1.3.0 と対。「共有されたセットプレイを論理削除しても、他の生きたコンボの紐付けが壊れない」ことを最重要ゲートとする。*
