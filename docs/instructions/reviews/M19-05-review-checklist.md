# M19-05 レビューチェックリスト

| 項目 | 内容 |
|------|------|
| 文書ID | **M19-05-REVIEW** |
| 対象指示書 | **`docs/instructions/M19-05-consumption-side.md` v1.1.1**（消費側 — 費用規則の実装と filler 規則の変更） <!-- ★配置時の実ファイル名と一致していることを確認すること --> |
| 対象指示書ID | **M19-05** |
| **参照してよい先行資料** | **対の指示書 ／ `M19-DESIGN-07` ／ `M19-DESIGN-08` ／ `m18-m19-contract.md` §2・§3.2 ／ `SUPP-001` §5.5**。**★他サブの指示書・チェックリストは含めない**（**D-217**） |
| レビューモデル | Sonnet 4.6（model-allocation 参照。実使用は開発者判断） |
| バージョン | **1.1.1**（2026-08-10・**クリーンルームレビューの指摘を反映**＝**§13 の「§0.3 に 8 件」が N-9〜N-11 の追加に追随していなかった**（→ 11 件）／**§13 の「対象指示書＝実ファイル名と一致」を実査で確定**（`M19-05-INSTRUCTION-REVISION-PATCH` §11 確認 9 の委任）。**旧記載 1.1.0**＝2026-08-10・**対の指示書 v1.1.0 への追随**＝CL-01〜CL-03。**§0.2 へ R-8〔`punishfinder` に diff がある〕を追加**、**§0.3 へ N-9〜N-11 を追加**、**§6〔段階 C〕に撤回ブロックを置いた**〔**D-260／D-261／D-262**〕。**旧記載 1.0.0**） |
| 作成者・作成日 | 設計担当 Claude（親チャット・設計卓）/ 2026-08-07 |

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| 1.1.0 | 2026-08-10 | **対の指示書 v1.1.0 への追随**（CL-01〜CL-03）。§0.2 へ R-8、§0.3 へ N-9〜N-11、§6 に撤回ブロック。**対象指示書の版を v1.1.0 へ同期**（**D-247**）。 |
| 1.0.0 | 2026-08-07 | 初版（指示書 v1.0.0 と対）。 |

---

## §0. レビュー担当への前置き

### 0.1 このレビューが守るもの

**M19 の中で最も広い変更である。** 費用規則（**提案結果**）と候補集合（**どの技が使えるか**）の両方を動かす。最重要ゲートは 3 点。

| # | ゲート |
|---|---|
| **G-1** | **契約の解除範囲を超えていない**（とくに `moves` のデータに diff 0・`internal/seedgen/` に diff 0） |
| **G-2** | **段階ごとに golden と canary が測られ、動いた分が全数説明されている** |
| **G-3** | **設計正本（`M19-DESIGN-07` / `M19-DESIGN-08`）に根拠が無い変更が入っていない** |

### 0.2 ★重大判定の基準

| # | 重大の条件 |
|---|---|
| **R-1** | **`moves` のデータに diff がある**（本サブはデータを 1 行も変更しない） |
| **R-2** | **`internal/seedgen/` に diff がある**、または **golden を再生成している**（契約 F-6） |
| **R-3** | **指示書 §0.2 (a)(b)(c) で解除していない凍結条項に触れている** |
| **R-4** | **canary の期待値が書き換えられている**（動いた事実を期待値の更新で消している） |
| **R-5** | **`M19-DESIGN-07` / `M19-DESIGN-08` に根拠が無い仕様が実装されている**（設計卓へ確認せずに） |
| **R-6** | **段階 A・B・C が 1 コミットにまとめられており、段階ごとの測定ができていない** |
| **R-7** | **ゲート 1 が「`category` を外す」形で実装されている**（`is_derived` へ**置き換える**のが正。外すと篩が全部外れる＝ボード D-148） |
| **R-8** | **`internal/service/punishfinder/` に diff がある**（**2026-08-09・D-260／D-261**＝段階 C とテストの前方一致是正はいずれも撤回された。**本サブは `punishfinder` を 1 行も触らない**） |

### 0.3 ★重大でないもの（**誤判定しないこと**）

| # | 誤判定しやすい状態 | 実は仕様である理由 |
|---|---|---|
| **N-1** | **filler 候補が +4 行増えている** | **指示書 §2.1 ゲート 1 の緩和。** 空振りでも出るパターン 3（zangief `machine_gun_chops` / `machine_gun_chops_2hits` / `power_stomps` / `power_stomps_2hits`）が入るのが正しい |
| **N-2** | **`category='target_combo'` の行が 63 行まだ除外されている** | **仕様。** ゲート 1 は「外す」ではなく「置き換える」——**パターン 1／2 は引き続き除外される** |
| **N-3** | **状態前提技（`ryu/denjin_charge_hadoken`・`ken/thunder_kick` 等）が除外されていない** | **既知の限界**（`M19-DESIGN-07` §9-6）。`target_combo` 外にあり本述語では取りこぼす。**本サブで解決しない** |
| **N-4** | **除外述語に `'unknown'` が含まれている** | **仕様**（**CHANGE-093 ／ D-227**）。`'standalone'` のみではない |
| **N-5** | **`rush_variant` に何も手が入っていない** | **仕様**（`M19-DESIGN-07` §2-1）。**通し値＋親を別ステップ計上しない現状のまま分類 3／4 で正しく動く。壊れていないものを直さない** |
| **N-6** | **`damage` が NULL のままの相手技がある** | **仕様**（ボード D-121）。**未実測値を捏造しない** |
| **N-7** | **`migrations/` に新規ファイルが無い** | **仕様。** 本サブはマイグレを消費しない見込み |
| **N-8** | **`chain_cancel_total` が NULL の技がチェーングループ員として扱われていない** | **仕様**（`DES-003` §3.3）。**非 NULL であること自体がグループ員であることを兼ねている** |
| **N-9** | **確定反撃の相手技候補に除外述語が足されていない** | **仕様**（**2026-08-09・D-260**）。**段階 C は本サブから外れた。** 述語は setplay 側（§2.1 ゲート 2）には正しいが `punishfinder` には転用できない |
| **N-10** | **`punishfinder` テストの `HasPrefix` がそのまま残っている** | **仕様**（**2026-08-09・D-261**）。**当該 `HasPrefix` は「従来 prefix 方式」の対照群であり、部分一致へ変えると canary と増分検証が無効化される。** D-131(b) の前提が実装と食い違っていた |
| **N-11** | **R6 警告が実装されていない** | **仕様**（**2026-08-09・D-262**）。**言及はあるが仕様が定義されておらず、本サブから外れた**（followup `r6-parent-step-consistency-warning`） |

### 0.4 報告フォーマット

- 各節「OK / 重大（§0.2）/ 軽微（§10）/ 質問（§11）」。

---

## §1. 着手前（Plan Mode）の報告

- [ ] **1.1** 指示書 §3 の **10 項目すべて**が報告されている。
- [ ] **1.2** **指示書 §0.1 の前提 6・7 が充足していることが実査で確認されている**（`DES-003` §3.3 の述語 ／ `M19-DESIGN-07` §4-3 の件数）。**未了なら止まっているか。**
- [ ] **1.3** **契約の凍結条項が全数列挙**され、指示書 §0.2 (a)(b)(c) で覆われているかが**突合されている。**
  → **覆われていない条項に触っていれば R-3。**
- [ ] **1.4** **§3.1 の実査 2 件**（案 α の材料 ／ TP-02 の対象行。**シャドウライズ 4 行を含むか**）が報告されている。
- [ ] **1.5** **§3.2 の実査 1 件**（移動 system move を親に取れるか）が報告され、**裁定を待つ姿勢が示されている。**
  → **裁定を待たずにこの系統へ親参照を付けていれば「要説明」。**
- [ ] **1.6** **想定と食い違った項目があった場合、実装に入る前に報告されている**（`PARENT-E21`）。

---

## §2. 契約・凍結インターフェース

- [ ] **2.1** **`moves` のデータに diff 0。** → あれば **R-1**。
- [ ] **2.2** **`internal/seedgen/` に diff 0。golden を再生成していない。** → あれば **R-2**。
- [ ] **2.3** **`migrations/` に新規ファイルが無い。** 追加した場合、**報告して止まったうえで設計卓の裁定を得ているか。**
- [ ] **2.4** **`internal/service/punishfinder/` の変更が、指示書 §2.4・§2.5 の 2 点に限られている**（相手技候補への述語 1 本 ＋ テストの前方一致の是正）。
  → **本番コードの部分一致ロジックを変更していないか**（**本番は既に正しい。テストだけが誤っていた**）。
- [ ] **2.5** **`is_projectile` / `is_aerial` / `on_block` / `recovery` の意味と値が変わっていない**（**使われ方の変更のみが解除対象**）。

---

## §3. 段階の分離（**★本サブの中心**）

- [ ] **3.1** **段階 A・B・C がそれぞれ別コミットになっている。** → なければ **R-6**。
- [ ] **3.2** **段階の順序が A → B → C。** C（`punishfinder`）が最後にある。
- [ ] **3.3** **段階ごとに golden と canary が測られている**（着手前の基準を含めて 4 点）。
- [ ] **3.4** **段階 A・B で canary が動いていない。**
  → 動いていれば「要説明」——**`punishfinder` に触っていないのに候補集合が変わっている。**
- [ ] **3.5** **canary の期待値が書き換えられていない。** → 書き換えられていれば **R-4**。

---

## §4. filler 候補規則（段階 A）

- [ ] **4.1** **述語が 2 つのゲートの合成として 1 本で書かれている**（指示書 §2.1）。
- [ ] **4.2** **ゲート 1 が `category` の「置き換え」になっている。** → 「外す」形なら **R-7**。
- [ ] **4.3** **ゲート 1 の増分が +4 行ちょうど**で、**`move_code` つきで固定**されている。
- [ ] **4.4** **ゲート 1 が維持する 63 行**が引き続き除外されることが固定されている。
  → **★「入った件数」だけでなく「出たままの件数」も固定されているか**（`SUPP-001` §5.5 (3)）。
- [ ] **4.5** **ゲート 2 の述語が `startup_basis IN ('standalone','unknown')`。** `'standalone'` のみなら**是正が要る**（CHANGE-093／D-227）。
- [ ] **4.6** **ゲート 2 の除外が `move_code` つきで固定**され、**`'standalone'` 由来と `'unknown'` 由来が分けて数えられている。**
- [ ] **4.7** **除外した行が、チェーン合成・表記展開経由では登場する**（除外して終わりにしていない＝`M19-DESIGN-07` §5-4）。
- [ ] **4.8** **ザンギエフ連打版行に特例が書かれていない**（一般則に乗るだけ＝同 §9-1）。

---

## §5. 費用規則と合成単位（段階 B）

- [ ] **5.1** **4 分類が上から順に判定される**形になっている（指示書 §2.2）。
- [ ] **5.2** **分類 1（表記用親の cost 0）と分類 2（`chain_cancel_total`）が、それぞれ 1 ケース以上で固定**されている。
- [ ] **5.3** **恒等式が成立する**——`S = Σcost(i)` ／ `N = KA + 2 − S` ／ **`G = N − active`**。
  → **★`G = 1 − N` が使われていないか**（撤回済みの誤り。ボード D-24／D-38）。
- [ ] **5.4** **分類 2 の判定が決定論**（両技の `chain_cancel_total` 非 NULL ∧ 隣接）で、**`setup_steps` から再計算可能**である。modifiers 等の特記を必要としていない。
- [ ] **5.5** **合成単位の total ＝ Σ非末端の `chain_cancel_total` ＋ 末端の `total`** が固定されている。
- [ ] **5.6** **ザンギエフ連打版が合成単位の 2 番目以降に差し込まれる**ことが固定されている。**差し込んだ行自身の値を §2.2 の規則が読む**（末端の `total` も連打版の値になる）。
- [ ] **5.7** **エンジン・受理帯・恒等式が変わっていない**（区分 2 のまま＝`M19-DESIGN-07` §2-2）。
- [ ] **5.8** **M19-02 の資産に触っていない**（受理帯方式・打ち切りのランキング後化・gap モード・`n_max`）。
- [ ] **5.9** **golden 5/5**（GC-2 を含む）。**★4/4 の内訳と GC-2 の期待値が Plan Mode で確定してから実装されているか。**

---

## §6. 確定反撃（段階 C）

> **★【2026-08-09・D-260／D-261】本節は撤回した。本サブは段階 C を実施しない。**
> **⇒ 本節のチェック項目は適用しない。代わりに「`internal/service/punishfinder/` に diff 0」を §0.2 の重大判定で見ること。**
> **★以下は撤回の記録として残す。**

- [ ] **6.1** **除外述語が §4.5 と同じ**（setplay 側と punishfinder 側で同じ述語）。
- [ ] **6.2** **除外の増分が `move_code` つきで固定**されている（**3 行の見込み。実測値が正**）。
- [ ] **6.3** **`damage` を触っていない。** NULL のままである。
- [ ] **6.4** **成立レーンが不変**（`buildStarters` が `Damage == nil` を最初にスキップする）。**canary で固定されている。**
- [ ] **6.5** **`neutral_jumping_heavy_kick` がテストで取り落とされない**（D-131(b) の是正）。
- [ ] **6.6** **本番コードの部分一致ロジックが変わっていない**（テスト側のみの是正）。
- [ ] **6.7** **canary が動いた場合、増分が全数説明され、変化の方向が報告されている。**
  → **緩める方向の変化は、実機で成立することが個別に説明されているか。**

---

## §7. 版固定 canary 測定手段（**D-237**）

- [ ] **7.1** **版を指定して射影を採取でき、SHA256 で突合できる手段が常設されている。**
- [ ] **7.2** **所在と使い方が完了報告に書かれている**（次のサブが読む）。
- [ ] **7.3** **形は問わない**（公開ヘルパ／テスト用エクスポート／専用テスト）。**要件を満たしていれば可。**

---

## §8. 否定形確認

- [ ] **8.1** 指示書 §5 の **3 項目**について、**走査キーワードで実際に走査した結果**が完了報告にある。
- [ ] **8.2** **走査対象に 3 系統すべて**が含まれている——**本番コード／テスト資産（コメント・ヘルパ名・fixture・spec 名）／設計文書・指示書・overview**。
- [ ] **8.3** **★`startup_basis = 'standalone'` の走査で、用途が判別されている。**
  → **費用計算で「単独値として使う」用途は残ってよい。** 機械的に 0 件にしていないか。
- [ ] **8.4** **除外対象が尊重されている**（本指示書・チェックリスト・`M19-DESIGN-07` §5-3 の経緯記述に旧述語が残るのは正常）。

---

## §9. テストと報告

- [ ] **9.1** **比較区間が自サブに閉じている**（`SUPP-001` §5.5 (1)(2)）。**マイグレを追加しない本サブでは、テストが HEAD 依存になっていないか。**
- [ ] **9.2** **`m1904PrefixMissed = 2`** が期待値そのままで green（**D-189** のガード）。
- [ ] **9.3** **完了報告に指示書 §7.2 の 10 項目**が載っている。
- [ ] **9.4** **既知の限界が明記**されている——**状態前提技はゲート 1 で取りこぼす。**
- [ ] **9.5** **守ったガードの所在**（ファイル名＋テスト名）が明記されている（**D-189**）。
- [ ] **9.6** **`docs/progress/progress-log.md` への追記**が行われている（**D-191**）。
- [ ] **9.7** **設計伝達レポート §2** が「なし」でない場合、**解除範囲を超えた内容が明示**されている。
- [ ] **9.8** **★分類ごとに主張が閉じている。** 「すべて N 行のいずれか」のような文が分類の境界をまたいでいないか（`SUPP-001` §5.5 (d)）。

---

## §10. 軽微な問題の判定基準（持ち越し許容）

- 実装の配置（ファイル分割・関数名・ヘルパの形）。**既存構成に合っていれば可。**
- 版固定 canary 測定手段の形（§7.3）。
- コメント・provenance の表記ゆれ。

---

## §11. 質問・確認事項のフォーマット

- 「**指示書 §X.X / `M19-DESIGN-07` §Y / `SUPP-001` §5.5 (n)** に対し実装が Z。意図確認したい」の形で根拠節を併記する。

---

## §12. 設計卓へ上げる（レビュー担当が確認する）

**次のいずれかがあった場合、完了報告に「設計卓の判断が要る」として明記されているかを確認する。**

- [ ] **12.1** **§3.2 の実査結果**（移動 system move を親に取れるか）。**裁定待ちである旨。**
- [ ] **12.2** **§3.1 (ii) の実査結果**（TP-02 の対象行の全数）。**M19 で閉じるか M20 へ送るかの裁定待ち。**
- [ ] **12.3** **`M19-DESIGN-07` / `M19-DESIGN-08` に根拠が無い変更が要ると判明した場合。**
- [ ] **12.4** **契約の凍結条項のうち、指示書 §0.2 で覆われていないものに触る必要が生じた場合。**
- [ ] **12.5** **マイグレが必要になった場合。**

---

## §13. ★本チェックリスト自身の検査（**発行前に設計担当が行う**）

- [x] **本書と対の指示書の間に、節番号の食い違いが無い。**（2026-08-07 確認）
- [x] **メタ表の「対象指示書」が、配置時の実ファイル名と一致している。**（**2026-08-10 実査で確定**＝メタ表の `docs/instructions/M19-05-consumption-side.md` は実在し、配置名と一致）
  → **★配置後に確認すること。** M19-PHASE2 で食い違った実例がある。
- [x] **指示書が禁じていない事項を「重大」として求めている項目が無い。**（§0.3 に **11 件**を明示。**2026-08-10 に N-9〜N-11 を追加**）
- [x] **達成不能な条件を重大として求めていない。**（§8.3 で「0 件にしない」を明示）
- [x] **§0.3（重大でないもの）が埋まっている。**

---

## §14. レビュー完了の判定

- **§1〜§9 が全て OK、§0.2 の重大ゼロ**（**特に G-1・G-2・G-3**）、**§1 の Plan Mode 実査が揃っている。**
- **§10 軽微は持ち越し可。**

---

*以上、M19-05 レビューチェックリスト v1.1.1。配置 `docs/instructions/reviews/M19-05-review-checklist.md`。指示書 v1.1.1 と対。**契約の解除範囲・段階ごとの測定・設計正本への根拠**を最重要ゲートとする。*
