# M19-04c レビューチェックリスト

| 項目 | 内容 |
|------|------|
| 文書ID | M19-04C-REVIEW |
| 対象指示書 | `docs/instructions/M19-04c-data-corrections.md` **v1.3.0**（**2026-08-08 実査で同期**。旧記載 v1.2.0 は 4 改訂ぶん古かった） |
| 対象指示書ID | M19-04c |
| CHANGE | **起票しない**（2026-08-03 に製造が実査し設計卓が受理。指示書 §0.3） |
| レビューモデル | Sonnet 4.6（model-allocation。実使用は開発者判断） |
| バージョン | **1.2.0** |
| 作成者・作成日 | 親チャット（設計卓）/ 2026-08-02 |

> **文書ID ⇄ 実パスの正は `docs/handover/docs-map.md`（自動生成）。**

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| **1.2.0** | **2026-08-08** | **設計伝達レポート §4-2 の差し戻し 16 件のうちチェックリスト側 4 件を反映**（`m19-04c-fixup-patch-20260807.md` v1.0.0 の FX-13〜FX-16）。**FX-13（C-1）**＝**「既存マイグレの diff が 0」が §1・§10 の「4 本の改変は許容」と正反対**だった 2 か所を「指示書 §2.1 の 4 本に限られているか」へ〔**旧テンプレートの残骸。指示書どおりに実装した成果物が重大判定になっていた**〕／**FX-14（C-2）**＝**「`sonic_break_light` がリポジトリに残っている」は達成不能**（**改名マイグレ自身が旧 code を名指しする必要がある**）ため実装経路に限定し、§3 の除外リストへ改名マイグレと検証テストを追加／**FX-15（C-3）**＝見出しの節参照ずれ 2 か所（§2.3 → **§2.4** ／ §2.4 → **§2.7**）／**FX-16（D-1）**＝**「HEAD の最終状態で固定」は `SUPP-001` §5.5 (1)(2) と衝突する**ため「最終状態と down → re-up の往復」へ改め、**HEAD スコープの検査は `migrate_head_test.go` へ置く**旨を注記。**★(C) 3 件と D-1 の根は旧テンプレの残骸であり、テンプレート側の改訂は別途**（`instruction-template-fixup-patch`）。反映係（Claude Code）による改訂 |
| **1.1.1** | **2026-08-07** | **メタ表へ `文書ID` 行を追加＝ボード **D-203**。`docs-map` 未収録の是正**（`scripts/generate-docs-map.sh` は `文書ID` 行から §1 を抽出するため、行が無い文書は載らない）。**本文の変更なし**（反映係・設計卓の依頼 Part 3 項目 6）。**対象指示書の版は変えていない。** |
| **1.1.0** | **2026-08-03** | 対象指示書を **v1.2.0** へ追従。**§2.5 を新設**（`000039` の書き換え＝案 1 と同時写像の罠）。**既存マイグレ改変の許容 4 本を明記**。**§6 を状態ベース固定へ差し替え**。**§8 を新設**（ガードの所在）。**CHANGE 要否の検査を削除**（確定済み）。**baseline red の読み替えを削除**（M19-04d で解消済み＝`go test ./...` は green が前提）。 |
| 1.0.0 | 2026-08-02 | 初版。 |

---

## 0. レビュー担当への前置き

### 0.1 準備

- 必読: 指示書 M19-04c v1.2.0 **全節**（**とくに §0.2 契約 F-2 の条件付き解除・§2.1 既存マイグレ改変の許容 4 本・§2.5 `000039` の書き換え・§2.6 状態ベース固定・§2.7 Phase 2 への申し送り・§3 停止条件**）、`m18-m19-contract.md` **§2（F-1・F-2）**、`DES-003` **§3.3・§3.14**、`DES-004` **§2.1・§2.4**。
- **Plan Mode 着手前確認の確認（必須）**: **§2.5 条件 3（`migrations/` 全体の旧 3 code の全数 grep）**・§2.3（`move_code` 変更の影響実査）・§1.1 C の内部整合の検算——**この 3 つが提示されずに実装へ進んでいたらその時点で重大**（§10）。

### 0.2 基本姿勢

- **本サブは「既存値を書き換える」唯一のサブである。** **契約 F-2 を条件付きで解除して行う。** **条件（§0.2 の 3 点）を満たしていなければ、実装が正しくても承認しない。**
- **★「UPDATE マイグレを 1 本書けば終わり」ではない。** **CSV・golden・マイグレの三点更新が不可分**である。**どれか 1 つでも欠けていたら重大。**
- **「エラーにならない失敗」を疑う。** 主要な失敗モードは **`WHERE` が 1 行も当たらない UPDATE**（0 件更新でも成功する）と、**改名の順序違反**（こちらは UNIQUE 違反で落ちるので発見できる）である。
- **★対象は 10 行**（A 1 ＋ B 3 ＋ C 6）。**8 件ではない。**

### 0.3 報告フォーマット

- 各節ごとに「OK / 重大（§9）/ 軽微（§10）/ 質問（§11）」で報告。

---

## 1. 【最重要】CSV・golden・マイグレの三点更新

- [ ] **`character_data/guile.csv` / `kimberly.csv` / `lily.csv` / `mai.csv` / `manon.csv` の 5 本が是正されている**か。**CSV が正本である。**
- [ ] **該当 stem（`000026` / `000034` / `000045`）の golden が再生成されている**か。
- [ ] **既存マイグレの改変が指示書 §2.1 の 4 本（`000026` / `000034` / `000045` / `000039`）に限られている**か。**それ以外に diff があれば重大。**
- [ ] **UPDATE マイグレで既存 DB へ届けている**か。**★適用済み連番は再実行されないため、CSV を直しただけでは既存 DB に届かない。**
- [ ] **3 つのうち 1 つでも欠けていないか。** **欠けていたら重大**——CSV と DB が食い違うか、CI が落ちるか、次の再 seed で誤りが復活する。
- [ ] **CSV の変更が §1.1 の該当行だけ**であることが**機械的に検証**されているか。**RFC4180 パーサで検証しているか**（**`marisa.csv:80` にクォート付きフィールドがあるため、カンマ数ベースの検証は誤検知する**）。

## 2. 【最重要】golden 再生成の健全性

- [ ] **再生成の前後で diff を取り、変わった行が §1.1 の 10 行だけであることが行単位で示されている**か。
- [ ] **10 行以外が 1 行でも変わっていないか。** **変わっていたら重大。**
- [ ] **`internal/seedgen` の生成関数（`writeMovesInsert` / `writeAliasInsert` / `rawDataSQL` / `CustomHeader`）の diff が 0** か。**触っていたら重大**（golden が全面的に変わる）。
- [ ] **再生成コマンドがテスト関数のコメントに明記**されているか。
- [ ] **golden 7 stem が green**・**`go run ./cmd/seedgen -check` が OK** か。

## 2.5 【最重要】`000039`（`is_projectile`）の書き換え

- [ ] **`000039` の `code` 列挙が新 code へ書き換えられている**か（**案 1**）。**案 2〔据え置き＋新マイグレで補完〕を採っていたら重大**——**v39 時点で新規 DB（102）と既存 DB（104）が乖離し、「飛び道具なのに `is_projectile=0` の行が 2 行ある DB」を中間状態として作る。**
- [ ] **改変が `code` 文字列リテラルの置換のみ**か。**ロジック・列・件数・順序を変えていないか。**
- [ ] **改変前後で v39 の `is_projectile=1` が 104 件で不変**か。**`TestRun_M1801_IsProjectileBackfill` が期待値を変えずにそのまま green** か（**期待値を書き換えていたら重大**）。
- [ ] **★`migrations/` 全体を旧 3 code で grep し、`000039` 以外に該当が無いことが全数で示されている**か。**示されていなければ重大。**
- [ ] **★同時写像で置換されている**か。**`sonic_cross_2_meter_od` は「消える旧 code」であると同時に「生まれる新 code」である**（`sonic_cross_3_meter_od` の改名先）。**単純な一括置換だと二重置換で行が消える。** **置換後に 3 つの新 code がそれぞれちょうど 1 件ずつ存在することが確認されている**か。
- [ ] **`000039` の diff 全文が完了報告に載っている**か。

## 3. 【最重要】改名の順序制約（UNIQUE 衝突）

- [ ] **B-2（`sonic_cross_2_meter_od` → `perfect_timing_sonic_cross_od`）が B-3（`sonic_cross_3_meter_od` → `sonic_cross_2_meter_od`）より前**に置かれているか。**逆順だと UNIQUE `(character_id, code)` に違反する。**
- [ ] **down が逆順**（B-3 を戻してから B-2 を戻す）になっているか。
- [ ] **改名した 3 行について、改名前の code が 0 件・改名後の code が 1 件**であることが**両方固定**されているか。**片方だけでは不十分**（改名前が残っていても、改名後が増えていても検出できるように）。
- [ ] **`sonic_break_medium` / `sonic_break_heavy` が存在しない**ことが実査されているか（「強度の区別が無い」という前提の裏取り）。
- [ ] **`sonic_break` が既存 `move_code` と衝突しない**ことが確認されているか。
- [ ] **`sonic_break_light` が実装経路（`internal/` `cmd/` `migrations/` `character_data/*.csv` `web/`）で 0 件**か。**歴史記録（`command-correction-history.md`・ボード・followup・`docs/progress/`・指示書とチェックリスト自身）に残っているのは正常である。**
- **【2026-08-07 追記・C-2】除外対象に「改名マイグレ（`000063` up/down）とその検証テスト（`migrate_m1904c_test.go` の code 定数）」を含める。** 旧 code を名指しできなければ改名そのものが書けない。

## 4. 【最重要】`command` 変更の索引・解決表への波及（**§2.4**）

- [ ] **変更前後で `move_commands` の行数と内容がどう変わるかが実査**され、報告されているか。
- [ ] **段階 2 の解決表（畳んだ表）の内容が変わるか**が実査されているか。**変わるなら FE キャッシュの無効化契約（DES-002 §4.2・CHANGE-069／070）に触れる**——**その場合は止まって報告すべき箇所である。**
- [ ] **B-1 と B-2 が変更後も同じ `command` を持つ**ことが確認されているか（両者は元々コマンド衝突しており、それが `is_derived=true` の理由である。**衝突の構造が変わっていないか**）。
- [ ] **`internal/moveindex` の「索引非搭載」件数**の変化が報告されているか（M14-03e の証跡で 252 行＝derived 250 ＋ JP の意図的 empty command 2）。
- [ ] **`internal/moveindex` の diff が 0** か（**データが変わるだけで、コードは触らないはず**）。

## 5. 【最重要】契約 F-2 の条件付き解除（§0.2）

- [ ] **条件 1**——**変更する行が全数列挙され、件数と変更前後の値を固定するテスト**があるか。**「条件式で束ねた UPDATE」になっていないか。**
- [ ] **条件 2**——**確定反撃サーチの canary 判定値が変更の前後で測定され、差分の内訳が報告**されているか。**差分が出た場合、それが本サブの変更で説明できることが行単位で示されているか。**
- [ ] **条件 3**——**`internal/service/punishfinder/` と `internal/service/setplay/` の diff が 0** か。
- [ ] **3 条件すべてを満たしたことが完了報告に記載**されているか。**記載が無ければ、実装が正しくても承認しない。**
- [ ] **説明できない canary の差分が 1 件でもあれば重大。**

## 6. 【最重要】固定の仕方 — **件数ではなく最終状態**

- [ ] **★テストが「UPDATE の件数」ではなく「最終状態と down → re-up の往復」で固定されている**か。**「UPDATE が 10 行に当たった」で固定していたら重大**——**golden 再生成後、新規 DB では UPDATE が 1 行も当たらない**（最初から是正後の値で seed される）。**マイグレは成功し、エラーにならない。**

> **【2026-08-07 是正・D-1】★「HEAD の最終状態で固定されているか」は問わない。** `SUPP-001` §5.5 の規約 (1)(2)——**比較区間は自サブの最終連番に閉じる ／ HEAD スコープの主張をサブ名前空間に置かない**——と衝突する。**自サブの終端（本サブでは v63）へ閉じているのが正しい。** HEAD スコープの検査が要るなら `migrate_head_test.go` の `TestRun_HEAD_*` へ置くこと。
- [ ] 状態ベースの例——**`sonic_break` が 1 件・`sonic_break_light` が 0 件**／**`bushin_prism_strikes` の `startup=26` `total=47`**。
- [ ] **「この UPDATE マイグレは既に適用済みの DB を追随させるためだけに存在し、その経路は CI で再現できない」という構造が完了報告に書かれている**か（`csv-edit-vs-golden-divergence` の最初の実例）。

## 6.5 フレーム値の是正（C・6 行）

- [ ] **6 行すべてで `total == startup + active − 1 + recovery` が成立**することが検算され、報告されているか（**`seedgen` の `validate()` が全カテゴリ一律で検証している**）。
- [ ] 是正後の値が指示書 §1.1 C のとおりか——kimberly `bushin_prism_strikes`＝**26/3/19/47**／lily `condor_dive_follow_up`＝**12/12/24/47**／lily `windclad_od_condor_dive_follow_up`＝**12/10/24/45**／mai **2 行**＝**28/30/39/96**（**active・recovery は不変**）／manon `temps_lie`＝**5/5/17/26**。
- [ ] **B-3 のフレームが 15/46/11/71** で、**`startup_basis` が `through`** になっているか。
- [ ] **10 行以外の `startup` / `total` が 1 行も変わっていない**ことが**スナップショット比較**で示されているか。

## 7. 【重点】Phase 2 への申し送り（**§2.7**）

- [ ] **改名前後の `move_code` 対応表が完了報告に載っている**か。**★これは必須成果物である。** 無ければ重大。
- [ ] 対応表に **3 件すべて**が含まれているか——`sonic_break_light` → `sonic_break`／`sonic_cross_2_meter_od` → `perfect_timing_sonic_cross_od`／`sonic_cross_3_meter_od` → `sonic_cross_2_meter_od`。
- [ ] **改名後の `sonic_cross_2_meter_od`（旧 3_meter_od）が `through` で確定した**ことが明記されているか（**Phase 2 の転記対象から外れる**）。
- [ ] **危険の説明が添えられている**か——**記入用コピーは `move_code` をキーにしており、そのまま突き合わせると `standalone` が `through` の行に上書きされ、しかもエラーにならない。**

## 8. 【重点】ガードの所在（ボード D-189）

- [ ] **★`migrate_m1904_test.go` の `m1904PrefixMissed = 2`（`juri` / `ken` の `neutral_jumping_heavy_kick`）が生きている**か。**本サブは `000039` を改変するため、巻き添えにしていないか。**
- [ ] **`is_projectile` の件数ガード（v39 で 104 件）が生きている**か。
- [ ] **ガードの所在が完了報告に明記されている**か。**★所在が分からなくなると、次に触る人がそれを消す。**
- [ ] **`go test ./...` が全パッケージ green** か（**M19-04d で baseline red 3 件は解消済み。新たに落ちたものがあれば本サブ由来である**）。
- [ ] **製造が DES / CHANGE 通知書を直接編集していない**か。

## 9. 連番とドキュメント

- [ ] **`ls migrations/` の実査結果が Plan Mode に提示**され、**自採番していない**か。
- [ ] **消費したマイグレ連番が完了報告に明記**されているか。
- [ ] **既存マイグレの改変が、指示書 §2.1 の 4 本（`000026` / `000034` / `000045` / `000039`）に限られている**か。**【2026-08-07 是正・C-1】★旧記載「既存マイグレの diff が 0 か」は §1（`:47`）・§10（`:139`）の「4 本の改変は許容」と正反対であり、旧テンプレートの残骸だった。指示書どおりに実装した成果物が重大判定になる。**
- [ ] **down で全 10 行が元の値へ戻り、re-up で復帰する**ことが確認されているか。
- [ ] **設計伝達レポート**が作成され、4 項目に絞られているか。
- [ ] **`csv-edit-vs-golden-divergence` の実例としての所感**（三点更新の手順で何が抜けやすかったか）が報告されているか。**次に同型の是正をするときの指示書に反映する。**
- [ ] 禁則表現・簡体字・「起き攻け」誤字・「DR」略記の grep 除去。

---

## 10. 重大な問題（完了承認を妨げる）

- **CSV・golden・マイグレの三点更新のいずれかが欠けている。**
- **golden 再生成で 10 行以外が変わった**／**変わった行の全数が示されていない。**
- **`internal/seedgen` の生成関数を変更した。**
- **指示書 §2.1 の 4 本以外の既存マイグレを改変した。**
- **改名の順序が B-2 → B-3 でない**／**改名前後の件数が両方固定されていない。**
- **`sonic_break_medium` / `_heavy` の不在を確認せずに `_light` を落とした。**
- **`sonic_break_light` の文字列が実装経路（`internal/` `cmd/` `character_data/*.csv` `web/`）に残っている。** **【2026-08-07 是正・C-2】★旧記載「リポジトリに残っている」は達成不能である**——**改名マイグレ自身が `WHERE code = 'sonic_break_light'` で旧 code を名指しする必要がある**ため（`000063` の up/down とテスト定数で 4 件残る）。**`migrations/` と検証テストは除外する。**
- **`command` 変更による `move_commands` / 段階 2 解決表の変化を実査していない**／**変化したのに止まらずに進んだ。**
- **★`000039` を書き換えず案 2 を採った。**
- **★`migrations/` 全体の旧 3 code の全数 grep が提示されていない。**
- **★同時写像でなく単純な一括置換をした**（二重置換で行が消える）。
- **★`TestRun_M1801_IsProjectileBackfill` の期待値（104）を書き換えた。**
- **★テストを「UPDATE の件数」で固定した**（新規 DB では 0 行になる）。
- **★`m1904PrefixMissed` または `is_projectile` の 104 件ガードを巻き添えにした。**
- **`go test ./...` が green にならないまま完了とした。**
- **契約 F-2 の 3 条件のいずれかを満たしていない**／**満たしたことが完了報告に記載されていない。**
- **確定反撃サーチの canary に、本サブの変更で説明できない差分がある。**
- **`internal/service/punishfinder/` または `internal/service/setplay/` を変更した。**
- **フレーム値の内部整合（`total == startup + active − 1 + recovery`）が成立しない行を投入した。**
- **10 行以外の `moves` 行の `code` / `command` / `startup` / `total` が変わった。**
- **★改名前後の `move_code` 対応表が完了報告に無い**（Phase 2 が誤爆する）。
- **マイグレ末尾が想定と違ったのに自採番して進めた。**
- **指示書 §2.1 の 4 本以外の**既存マイグレを改変した。または **4 本の改変に down 整合が無い**。**【2026-08-07 是正・C-1】**
- **製造が DES / CHANGE 通知書を直接編集した。**

## 11. 軽微な問題（持ち越し許容）

- マイグレの分割数（**消費連番が明記されていれば可**）。
- テストファイルの配置と命名（**既存ヘルパを再利用していれば可**）。
- `lily/condor_dive_follow_up` 系の命名見直しの提案（**本サブでは扱わない。followup へ回っていれば可**）。
- E2E の既知 flakiness による 1 件の落ち（**単独実行で green であることと判定根拠が示されていれば許容**）。

## 12. 質問・確認事項のフォーマット

- 「指示書 §X.X / 設計書 DES-00N（実パス）§Y に対し、実装が Z。意図確認したい」の形で根拠節を併記して設計担当へ。

## 13. レビュー完了の判定

- §1〜§9 が全て OK、§10 重大ゼロ、Plan Mode の 3 項目（**§2.5 条件 3 の全数 grep・§2.3 の影響実査・内部整合の検算**）が揃っている。§11 軽微は持ち越し可。

---

*以上、M19-04c レビューチェックリスト v1.1.0。配置 `docs/instructions/reviews/M19-04c-review-checklist.md`。重点＝**CSV／golden／マイグレの三点更新の不可分性**・**`000039` の同時写像**・**★テストは件数でなく最終状態で固定**・**契約 F-2 の 3 条件**・**ガードの所在**・**Phase 2 への対応表**。*
