# M37-04 レビューチェックリスト（確定反撃候補の初段発生）

| 項目 | 内容 |
|------|------|
| 文書ID | M37-04-REVIEW-CHECKLIST |
| バージョン | **1.0.3**（2026-09-14・**★`000116` の追加払い出しを B-5 / B-5b へ反映**〔`D-875`〕） ／ **1.0.2**（2026-09-13・**★`000115` の追加払い出しを B-5 へ反映**〔`D-870`〕） ／ **1.0.1**（2026-09-13・対象指示書の版数追随） |
| 対象指示書 | `docs/instructions/M37-04-punish-candidate-first-hit-startup.md` **v1.0.3** |
| 作成者 | 設計担当 Claude（設計卓） |
| 位置づけ | **レビュー担当の主入力**（`D-736`） |

---

## 0. ★★レビュー担当への前置き

**★★★本サブは「データを直さない」サブである。⇒ 開発者が「データは正しい」と明言している。**

**★誤っているのは*判定*であり、`startup` の値ではない。⇒ `startup` に差分が 1 行でも出たら、それだけで差し戻す理由になる。**

| # | 本サブ固有の不合格 |
|---|---|
| **1** | **★★★`startup` の値を書き換えた** |
| **2** | **★★`first_hit_startup` に値を入れた**（**★手入力は開発者の手番**） |
| **3** | **★★除外の範囲を広げすぎた**（`target_combo` 全部を外す等） |
| **4** | **★破壊確認が無い**——「候補から消えた」だけで、別の理由で消えている可能性を排除していない |

---

## 1. 束 A — ★★★判定の変更（**本サブの本体**）

| # | 観点 |
|---|---|
| **A-1** | **★★★除外の条件が「`category='target_combo'` かつ `is_derived` かつ `first_hit_startup` が NULL」の 3 つそろっているか。⇒ 1 つでも欠けると範囲が広がる** |
| **A-2** | **★★★`fuwa_triple_strike_2hits` が有利 5F で候補から*外れる*ことが名指しで実測されているか**（§5-2） |
| **A-3** | **★★★§5-3 の破壊確認が在るか＝除外を外すと同じ行が候補に*戻る*こと。⇒ 無ければ「別の理由で出ていない」可能性を排除できない** |
| **A-4** | **★★§5-4 の偽陰性の確認が在るか＝非 derived 9 行と `basis='through'` 4 行が候補から消えていないこと**（`M37-RESEARCH-01` §4.3） |
| **A-5** | **★`first_hit_startup` が入っている行は、その値で判定されるか**（§5-1。**★テスト用に 1 行だけ埋める形でよい。⇒ ★コミットしていないか**） |
| **A-6** | **★それ以外の行は `startup` のままか。⇒ 挙動を変えていないか** |
| **A-7** | **★投影に必要な列が足されているか**（`startup_basis` / `is_derived` / `first_hit_startup`）**。⇒ `M37-RESEARCH-01` §4.2 は「現投影に情報が足りない」と実測している** |

---

## 2. 束 B — ★★★列の追加

| # | 観点 |
|---|---|
| **B-1** | **★★★列名が `first_hit_startup` か。⇒ `starter_startup` になっていないか**（**★「始動用の」は特定消費者の用途であり、`M19-DESIGN-07` §7 の審査規約に反する**） |
| **B-2** | **★★★`first_hit_startup` が全行 NULL か。⇒ 値を 1 行でも入れていないか**（**★手入力は開発者の手番**） |
| **B-3** | **★★`DES-003` §3.3 の列台帳の原稿**（名前／実セマンティクス／消費者一覧）**が設計伝達レポート §4 に在るか**（`M19-DESIGN-07` §7 の列台帳規約） |
| **B-4** | **★`CHECK` 制約が既存の `startup` に倣っているか**（実物を見て揃えたか） |
| **B-5** | **★★マイグレが `000114`〜`000116` の範囲に収まっているか。⇒ 自採番していないか**（`D-293`）**。★`000115` は 2026-09-15 の追加払い出しである**〔`D-870`〕**。★★`000113` は欠番である**〔`D-866`〕**。⇒ 詰めて `000113` にしていないか** |
| **B-5b** | **★★★`000115` と `000116` の*用途*が報告に明記されているか。⇒ `startup` の値を書き換えるマイグレなら束 C-1 の不合格である** |
| **B-6** | **★`000114` が層 A に解決するか**（`check-migration-license.sh`）**。⇒ 判定が想定と違ったら報告されているか** |
| **B-7** | **★CSV へ足したか / 足さなかったかが明記され、足したなら往復が確かめられているか** |

---

## 3. 束 C — ★★★触っていないもの

| # | 観点 |
|---|---|
| **C-1** | **★★★`moves.startup` の値が 1 行も変わっていないか**（**★差分 0 の実測**） |
| **C-2** | **★`character_data/*.csv` の `startup` 列に差分が無いか** |
| **C-3** | **★`startup` を読む他の機能が壊れていないか** |
| **C-4** | **★★初段を機械的に同定しようとしていないか**（指示書 §3-3）**。⇒ `command` の解析や親リンクの推定に手を出していないか。★実測で 59 行が判定不能である** |
| **C-5** | **★`B04`〜`B15` に手を出していないか** |
| **C-6** | **★★★`docs/handover/followup-backlog.md` を 1 文字も編集していないか**（`D-838`） |
| **C-7** | **★設計書本体を書き換えていないか**（`CLAUDE.md` §8） |

---

## 4. 束 D — ★`B02`（バーンアウト）

| # | 観点 |
|---|---|
| **D-1** | **★段 1 でバーンアウトの現在地が実査されているか**（状態としてどこにあるか） |
| **D-2** | **★★射程の判定が明記されているか**——**本サブで実施した / 大きいので請求した のどちらか。⇒ 曖昧にしていないか** |
| **D-3** | **★実施したなら、既存の状態表現に載る形か。⇒ 新しい状態の持ち方を発明していないか** |
| **D-4** | **★請求したなら、何が大きかったかが書かれているか** |

---

## 5. 束 E — ★実査と検査

| # | 観点 |
|---|---|
| **E-1** | **★★件数を数え直しているか**〔126 / 117 / 109 / 106 / 105〕**。⇒ `M37-RESEARCH-01` の値をそのまま使っていたら不合格である** |
| **E-2** | **★★着手前の版ゲートを通したか**（指示書 §0.6 の 5 点）**。⇒ とくに 4＝`M37-01` / `M37-03` が着地していたか**（計測点 `M-169`） |
| **E-3** | **`go test ./...` / `pnpm test` / `make e2e`（全数）が緑か** |
| **E-4** | **`check-migration-license.sh` が緑か** |
| **E-5** | **★数値の隣に「実測」「算出」が書き分けられているか**（計測点 `M-157`） |

---

## 6. ★★見なくてよいもの

| # | 何を | なぜ |
|---|---|---|
| 1 | **109 行の値が入っていないこと** | **★開発者の手番**（`D-857`）**。⇒ 本サブの完了条件に含めない** |
| 2 | **`5F` がゲーム上の第 2 段発生として正しいか** | **★開発者が「間違いない」と明言済み**（`D-857`） |
| 3 | **`preset_aliases` や `move_derivations` の整合** | **★本サブの射程外** |
| 4 | **除外されたことの利用者への見せ方** | **★実装判断でよい。⇒ 決めた形と理由が書かれていれば足りる** |
| 5 | **CHANGE の起票** | **★製造は自採番しない**（`D-293`） |

---

## 7. ★★対照値（**`M37-RESEARCH-01` の実測。⇒ 数え直させる**）

| 集合 | CSV | DB |
|---|---:|---:|
| `category='target_combo'` | 126 | 126 |
| うち `is_derived` | 117 | 117 |
| **`target_combo` ＋ derived ＋ `standalone`**（**★除外の母集団**） | **109** | **109** |
| 上記のうち startup あり | 106 | 106 |
| 上記かつ非空中・damage>0 | **105** | **105** |
| 非 derived | 9 | 9 |
| `basis='through'` | 4 | 4 |

**★★★105 は「誤データ 105 件」ではない。⇒ 「地上候補の静的条件を満たす行数」である。**

---

*以上、M37-04 レビューチェックリスト **v1.0.3**。* **★★★束 C-1 を最初に見ること**——**`startup` に差分が 1 行でも出たら、本サブの前提が崩れている。** **★★次に重いのは A-3 である**——**「候補から消えた」は、除外が効いた証拠にも、別の理由で出ていない証拠にもなる。⇒ 戻ることまで見て初めて歯止めである。**
