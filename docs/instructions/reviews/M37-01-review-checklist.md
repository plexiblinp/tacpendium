# M37-01 レビューチェックリスト（始動位置・運び量の 3 方式入力 UI）

| 項目 | 内容 |
|------|------|
| 文書ID | M37-01-REVIEW-CHECKLIST |
| バージョン | **1.0.0**（2026-09-13・発行） |
| 対象指示書 | `docs/instructions/M37-01-carry-distance-three-way-input.md` **v1.0.0** |
| 作成者 | 設計担当 Claude（設計卓） |
| 位置づけ | **レビュー担当の主入力**（`D-736`） |

---

## 0. ★★レビュー担当への前置き

**★本サブは作る量が少ない。⇒ 「動いた」で通しやすい形である。**

**★★★不合格の中心は 3 つ。⇒ どれも「動いているのに間違っている」型である。**

| # | 不合格の形 |
|---|---|
| **1** | **★★運び量に区分が付いている**（`D-731` の不変条件 2 に反する） |
| **2** | **★★導出が二重になっている**（サーバの `normalizePositionAndMass` と同じことを UI がしている） |
| **3** | **★破壊確認が無い**——「付けなかった」で終わっており、次の担当が付けるのを止められない |

---

## 1. 束 A — ★★★不変条件（**本サブの本体**）

| # | 観点 |
|---|---|
| **A-1** | **★★★運び量に区分の入力が*無い*か**（`D-731`）**。⇒ `POSITION_OPTIONS` が運び量側へ渡っていないか** |
| **A-2** | **★★★§5-2 の破壊確認が在るか。⇒ 「運び量に区分を与えようとしても与えられない」ことが型 or テストで固定されているか。★「付けなかった」だけなら不合格である** |
| **A-3** | **★★運び量から始動位置を導出していないか**（`D-731` の不変条件 1）**。⇒ 逆方向**（始動位置 → 運び量）**も無いか** |
| **A-4** | **★区分の境界が `D-731` の 91〜112 か。⇒ `D-730` の 91〜111 を使っていないか**（**★同裁定は更新されている**） |

---

## 2. 束 B — ★★★導出の二重化

| # | 観点 |
|---|---|
| **B-1** | **★★★サーバ側 `internal/service/combo/service.go` の差分が 0 行か。⇒ とくに `normalizePositionAndMass`** |
| **B-2** | **★★「送信の直前に値を書き換える」コードが無いか**（指示書 §4.2 の見分け方）**。⇒ それは導出である** |
| **B-3** | **★UI が送るのは利用者が入れた値そのままか。⇒ マスから position を埋めて送っていないか**（**★サーバがやる**） |
| **B-4** | **★変換は `constants/position.ts` の既存関数を使っているか。⇒ 新しい変換関数を書いていないか** |
| **B-5** | **★`constants/position.ts` の差分が 0 行か。⇒ 既存テスト 34 行が緑のままか** |

---

## 3. 束 C — ★★連動と境界

| # | 観点 |
|---|---|
| **C-1** | **★★★3 方向すべての連動が実測で示されているか**〔区分 → マス・% ／ マス → 区分・% ／ % → 区分・マス〕**。⇒ 1 方向だけの確認では不足である** |
| **C-2** | **★境界値の実測が在るか**〔`0` / `160` / `161` 拒否 / `-1` 拒否 / 空〕 |
| **C-3** | **★未入力（null）が 3 方式とも表現でき、`null` として送られるか** |
| **C-4** | **★パーセントの丸めの向きが 1 か所に決まり、報告に書かれているか**（**★`percentToMass` は四捨五入 ＋ 0〜160 クランプ**＝実測。⇒ 表示側と食い違っていないか） |
| **C-5** | **★既存の区分選択が壊れていないか**（`ComboEditorBasicFields.tsx:445` の 8 選択肢＝7 区分 ＋ 不問） |

---

## 4. 束 D — ★★実査と越境

| # | 観点 |
|---|---|
| **D-1** | **★★着手時に参照 8 行を*数え直して*いるか**（指示書 §2.1-1）**。⇒ `M37-RESEARCH-01` の「8 行」をそのまま使っていたら不合格である**（`M30-05` 教訓 1） |
| **D-2** | **★★`VAL-C16` / `VAL-RANGE` の食い違いを*報告しているだけ*か。⇒ 直していないか**（指示書 §4.3）**。★片方へ寄せると `DES-006` の他コードとの整合が崩れうる** |
| **D-3** | **★`internal/` `migrations/` `character_data/` の差分が 0 か** |
| **D-4** | **★DB 列・API・CSV の契約を変えていないか。⇒ 変えたなら、止まって請求した記録が在るか**（指示書 §7） |
| **D-5** | **★★★`docs/handover/followup-backlog.md` を 1 文字も編集していないか**（`D-838`） |
| **D-6** | **★★着手前の版ゲートを通したか**（指示書 §0.4 の 4 点）**。⇒ 枝元の `git log --oneline -5` が報告に在るか**（計測点 `M-169`） |
| **D-7** | **★重複事前チェックに手を出していないか**（`duplicate-precheck-ignores-position-mass` は別項目） |

---

## 5. 束 E — ★説明書と検査

| # | 観点 |
|---|---|
| **E-1** | **★説明書の追随が在るか**（指示書 §2.4）**。⇒ `M32-02` が未着地なら「本サブで足した UI」の列挙が完了報告に在るか** |
| **E-2** | **★一覧に出さない判断をしたなら、理由が報告に在るか**（指示書 §2.3-2） |
| **E-3** | **`pnpm test` / `pnpm tsc --noEmit` / `pnpm lint` が緑か。★`eslint-disable` を足していないか** |
| **E-4** | **`make e2e` を*全数*回しているか**（`SUPP-001` §5.5.7 (i)） |
| **E-5** | **`go test ./...` に変化が無いか** |
| **E-6** | **`check-import-order.sh` がベースラインから増えていないか。★レーン内で下げていないか**（`D-388`） |
| **E-7** | **★新しい `data-testid` を足したなら `docs/design/testid-convention.md` に沿っているか** |

---

## 6. ★★見なくてよいもの

| # | 何を | なぜ |
|---|---|---|
| 1 | **`VAL-C16` / `VAL-RANGE` のどちらが正か** | **★設計卓の判断。⇒ 本サブは報告まで** |
| 2 | **重複事前チェックがマス数を見ないこと** | **★`followup` の `duplicate-precheck-ignores-position-mass`。⇒ 別項目** |
| 3 | **区分表が Go と TS に二重にあること** | **★`followup` の `position-bands-duplicated-go-and-ts`。⇒ 別項目** |
| 4 | **一覧の横幅** | **★`M27-03` / `P-21` が扱う面** |
| 5 | **CHANGE の起票** | **★製造は自採番しない**（`D-293`）**。⇒ 請求は報告 §6 へ** |

---

## 7. ★★数の対照（**着手時に数え直させる値**）

| 対象 | `M37-RESEARCH-01` の実測（2026-09-13） |
|---|---|
| `startPositionMass` / `carryDistanceMass` の参照 | **8 行 / 2 ファイル**（型 6・スキーマ 2） |
| `*.tsx` ／ i18n JSON ／ E2E の該当 | **各 0 行** |
| 変換関数の参照 | **47 行**（`constants/position.ts` 13 ＋ 同 test 34） |
| 部品・ページへの該当 | **0 行** |
| 区分入力の本体 | **`ComboEditorBasicFields.tsx:445` の 1 か所** |

**★これらは「着手前の姿」である。⇒ 着手後は当然増える。★レビューは「増えた場所が §2 のとおりか」を見る。**

---

*以上、M37-01 レビューチェックリスト **v1.0.0**。* **★★★束 A-2 を落とさないこと**——**破壊確認が無いと、`D-731` の不変条件は「今回は守った」でしかなくなる。⇒ 次に運び量へ触る担当を止められない。** **★★次に重いのは B-1 / B-2 である**——**導出の二重化は動くので、テストでは出ない。⇒ 人が読む以外に検出経路が無い。**
