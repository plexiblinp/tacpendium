# M29-01 レビューチェックリスト: 用語・ラベルの統一

| 項目 | 内容 |
|------|------|
| 文書ID | M29-01-review-checklist |
| バージョン | **1.0.0** |
| 対象指示書 | `docs/instructions/M29-01-vocabulary-and-labels.md` **v1.0.0** |
| 推奨モデル | **高精度レビュー**（**★★i18n を全面で触る。⇒ 文言で判定しているテストが赤くなり、その赤の読み方を間違えると仕様が壊れる**） |
| 作成者・作成日 | 設計担当 Claude（設計卓・フェーズ4 期）/ 2026-09-06 |

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| 1.0.0 | 2026-09-06 | 初版（`D-753`。**★投入の合図と同じ手番で起こした**＝`D-736` の運用） |

---

## 0. レビュー担当への前置き

### 0.1 レビュー前の準備

1. **指示書 v1.0.0 を通読する**（とくに §0.1 の「最初の成果物は実装ではない」、§0.2 の「寄せ先は開発者が選ぶ」、§0.3 の 4 群）。
2. **`M27-03` の教訓 7 を頭に入れる**——**「画面に出ている文言」で有無や着地先を判定しない。⇒ i18n のキーから引く。**

### 0.2 ★★本サブの最初の成果物は実装ではない

**「語の割れの全数」である**（指示書 §0.1）。**⇒ 実装だけがあって全数が無ければ、順序が逆である。**

**★★`phase4-overview` §7.1 は「用語・ラベル・入出力＝29 件」と記録しているが、その内訳は ledger から引けない**（計測点 `M-127`）**。⇒ 実査の結果が 29 でなくてよい。★29 に合わせようとしていたら、それこそが誤りである。**

### 0.3 ★★重大でないもの（**誤判定しないこと**）

| # | 内容 |
|---|---|
| 1 | **実査の件数が 29 でないこと**（§0.2） |
| 2 | **4 群のうち一部が「実は別のものを指していた」として統一されないこと**（指示書 §2.2-4。**★無理に統一しないのが正しい**） |
| 3 | **寄せ先を製造が選んでいないこと**（**★開発者が選ぶ**＝`D-742`。**製造は 1 群 2〜3 案を出して止まる**） |
| 4 | **`check-enum-sync.sh` のベースラインが動いていないこと** |
| 5 | **`M27-01` / `M27-03` の as-built に合わせた結果、他の語と揃わないこと**（**★巻き戻さないのが正しい**＝指示書 §2.3-4） |

### 0.4 ★★本サブに固有の危険（**最優先で見る**）

| # | 危険 | なぜ緑のまま通りうるか |
|---|---|---|
| **★★1** | **「画面に出ている文言」で全数を数えた** | **★同じ文言が別のキーから出ている場合がある。⇒ 文言で数えると 1 件に見え、実際は 2 キーである。★★逆に、キーは 1 本でも画面によって別の文言に見えることがある。⇒ 数え方を間違えると、以後の作業が全部ずれる** |
| **★★2** | **寄せ先を製造が決めてしまった** | **★報告に「〜へ統一した」と書いてあっても、開発者が選んだ記録が無ければ違反である。⇒ `D-742` の逐語は「基本は私が選びたい」** |
| **★★3** | **参照 0 になったキーを ja だけ消して en を残した** | **★`en` は日常的に見ないため気づかれない。⇒ 検査が無ければ永久に残る** |
| **★★4** | **E2E が赤くなったのを「文言を戻して」直した** | **★★赤くなった箇所は「文言で判定していた」証拠である。⇒ テスト側を `data-testid` かキーへ直すのが正しい。★文言を戻すと、本サブの目的そのものが消える** |
| **★★5** | **1 コミットに 2 群を混ぜた** | **★赤が出たときにどの群かが分からなくなる**（指示書 §2.5） |
| **★★6** | **`internal/model/` の列挙定数との同期を崩した** | **★`check-enum-sync.sh` は既定 warning でありベースライン固定型である。⇒ 崩れても赤にならない場合がある**（`CLAUDE.md` §4） |

---

## 1. 束 A — 語の割れの全数（**最初の成果物**）

| # | 見るところ |
|---|---|
| **★★1** | **4 群それぞれについて、割れている語が全数で挙がっているか** |
| **★★2** | **★★数え方が「i18n のキー」であるか。「画面に出ている文言」で数えていないか**（§0.4-1） |
| **★★3** | **キーごとに「どこから出ているか」（参照元）が添えられているか。★添えられていないと、寄せたときに何が動くか分からない** |
| **★4** | **実査の件数が `phase4-overview` §7.1 の「29」と違う場合、その旨が報告に書かれているか**（**★合わせようとしていないか**） |
| **★5** | **「実は別のものを指していた」群が在れば、そう書かれているか** |

---

## 2. 束 B — 寄せ先の決め方

| # | 見るところ |
|---|---|
| **★★1** | **1 群につき 2〜3 案が出ているか** |
| **★★2** | **各案に「なぜその語か」が 1 行添えられているか**（**★語だけ並べても選べない**） |
| **★★3** | **★★開発者が選んだ記録が在るか**（§0.4-2）**。⇒ 無ければ、製造が決めたことになる** |
| **★4** | **`M27-01` / `M27-03` の as-built と食い違う案が在れば、食い違うことが明示されているか**（**★巻き戻しは射程外**） |

---

## 3. 束 C — 統一の実装

| # | 見るところ |
|---|---|
| **★★1** | **i18n のキーが 1 本へ寄っているか。★参照 0 になったキーが `ja` と `en` の**両方**から消えているか**（§0.4-3） |
| **★★2** | **`label-keys.test.ts` の「値域 ↔ キーの 1 対 1」検査が活かされているか**（`M27-03` の先例）**。★統一した語に値域が在るなら、同じ検査へ載せること。⇒ 足し忘れが赤になる形にする** |
| **★★3** | **`web/src/constants/` と `internal/model/` の同期が崩れていないか**（§0.4-6。**★`check-enum-sync.sh` を回した結果が報告に在るか**） |
| **★4** | **相手状態の語彙が `M27-01`（`hit_type` 8 値 / `opponent_size` 4 値）／ `M27-03` の as-built に合っているか** |

---

## 4. 束 D — テストの妥当性

| # | 見るところ |
|---|---|
| **★★1** | **★★テストが「画面に出ている文言」で判定していないか**（`M27-03` の教訓 7）**。⇒ `data-testid` かキーで判定しているか** |
| **★★2** | **参照 0 のキーを削除したことを検出するテストか検査が在るか**（**★無ければ、次に増えたとき同じことが起きる**） |
| **★★3** | **`make e2e` を全数で回しているか。★赤くなった箇所が在れば、それが報告に書かれているか**（§0.4-4。**★★赤は証拠であって事故ではない**） |
| **★4** | **実 `ja.json` を引く `t` で書かれているか**（**★キーをそのまま返すモックでは文面の主張が空振りする**＝`M23-04` 教訓 2） |

---

## 5. 束 E — コミットの割り方

| # | 見るところ |
|---|---|
| **★★1** | **§2.1 の実査が独立したコミットになっているか**（コードが動かないコミット） |
| **★★2** | **4 群が 1 群ずつ割れているか**（§0.4-5） |
| **★3** | **参照 0 のキーの削除が最後の 1 本になっているか** |

---

## 6. 破壊確認（**★2 件とも必須**）

| # | 壊すもの | 期待 |
|---|---|---|
| **★★1** | **統一した語のキーを 1 本、値域から外す**（または値域に値を 1 つ足してキーを足さない） | **`label-keys.test.ts` の 1 対 1 検査が赤になること**（**★束 C-2 が実際に効いていることの陽性対照**） |
| **★★2** | **参照 0 として削除したキーを `en` だけ復活させる** | **★検出されるか。⇒ されないなら、束 D-2 の検査が無いということである** |

---

## 7. 重大な問題の判定基準（完了承認を妨げる）

| # | 内容 |
|---|---|
| **★★1** | **全数が「画面に出ている文言」で数えられている** |
| **★★2** | **寄せ先を製造が決めた**（開発者が選んだ記録が無い） |
| **★★3** | **参照 0 のキーが `ja` からだけ消えている**（`en` に残っている） |
| **★★4** | **E2E の赤を、文言を戻して直した** |
| **★★5** | **1 コミットに 2 群以上が混ざっている** |
| **★★6** | **`internal/model/` との同期が崩れている**（`check-enum-sync.sh` の結果が報告に無い場合を含む） |
| **★7** | **実査の件数を `29` に合わせるために、群を足したり削ったりした** |
| **★8** | **`docs/design/` を編集した ／ `followup-backlog.md` の §J 以外を編集した ／ CHANGE 番号を自採番した** |

---

## 8. 軽微な問題の判定基準（持ち越し許容）

| # | 内容 |
|---|---|
| 1 | 案の並べ方・報告の節立て |
| 2 | **英語側の語選び**（**★日本語が決まっていれば足りる**） |
| 3 | **1 群だけ統一に至らなかったこと**（**★「実は別のものを指していた」なら正しい着地である**） |
| 4 | **実査の件数が 29 と違うこと**（**★理由が報告に在る場合に限り軽微とする**） |

---

## 9. レビュー完了の判定

1. **§7 の重大が 0 件である。**
2. **§6 の破壊確認 2 件が実走されている。**
3. **`cd web && pnpm test` / `make e2e`（全数）が緑。**
4. **常設検査が緑**（**★`check-artifact-integrity.sh` を 1 本目に回す**＝`CLAUDE.md` §8。**★`check-enum-sync.sh` と `check-import-order.sh` も回す**）。
5. **`docs/progress/progress-log.md` へ追記されている。**

### 9.1 ★本チェックリスト自身の検査（発行前に設計担当が行う）

- [x] **対象指示書の版数と一致している**（v1.0.0）
- [x] **`bash scripts/check-instruction-format.sh` が緑**
- [x] **`bash scripts/check-md-emphasis.sh` が緑**

---

*以上、M29-01 レビューチェックリスト **v1.0.0**。* **★★本サブでレビューが最も見落としやすいのは「文言が揃ったから終わり」と読むことである。** **★見るのは (1) 数え方がキーであること (2) 寄せ先を開発者が選んだこと (3) `en` 側も消えていること の 3 つである**（§0.4-1 / -2 / -3）**。** **★★もう 1 つは、E2E の赤の読み方である**（§0.4-4）**——赤くなった箇所は「文言で判定していた」証拠であり、事故ではない。⇒ テスト側を直すのが正しく、文言を戻すと本サブの目的そのものが消える。**
