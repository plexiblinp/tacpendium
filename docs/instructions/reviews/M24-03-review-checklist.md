# M24-03 レビューチェックリスト: 比較・詳細・レシピ可読性

| 項目 | 内容 |
|------|------|
| 文書ID | M24-03-review-checklist |
| バージョン | 1.1.0 |
| 対象指示書 | `docs/instructions/M24-03-compare-detail-recipe-readability.md` **v1.1.0** |
| 推奨モデル | **高精度レビュー**（**★9 項目のうち 6 項目が「レシピの見せ方」という 1 つの根から出ている。面ごとに別々に直っていないかの照合が中心である**） |
| 作成者・作成日 | 設計担当 Claude（設計卓・M24 期）/ 2026-08-26 |

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| 1.1.0 | 2026-08-26 | **指示書 v1.1.0 へ追随**（**D-565**）。 **★★`SM-018` がクラスタ A へ移り、§4.1 の適用範囲が 3 面から 4 面（＋比較の追加モーダル）になった。** **⇒ 1-1 / 1-2 / 5-3 / 6-2 の「3 面」をすべて「4 面」へ改め、4-11 を新設した。** **★★§0.3-2「`SM-018` が未実装でも欠陥ではない」を撤回した**——**確認事項 1 が決着し、`SM-018` は実装対象になった。** **★★§0.3-4「`SM-089` が一覧に出ていないこと」も撤回した**——**開発者回答で一覧にも出すことが確定した。⇒ 4-6b を新設し、出ていないなら「高」にした。** **★E2E を 4 ケースから 5 ケースへ。★7-4 のモックを 4 面 ＋ 基準値付きへ。** |
| 1.0.0 | 2026-08-26 | 初版（**D-563**） |

---

## 0. レビュー担当への前置き

### 0.1 レビュー前の準備

1. 指示書 **v1.0.0** と `CHANGE-134` 通知書を全文読む。
2. 完了報告 `docs/progress/M24-03-completion-report.md` を読む。
3. **★`DES-005` §5.4 / §5.6 / §5.8 を読む。** **とくに §5.4 の「ルート（レシピ文字列）」の項に省略表示の選択肢が 6 つ並んでいる**——**本サブはそこを 1 つへ畳む。**
4. **★`DES-005` §4.3 / §4.3.1 を読む**（`M24-01` / `M24-02` で改訂されたばかり。**本サブが壊していないかの照合先**）。

### 0.2 レビューの基本姿勢

コードを変更せず、指摘のみを行う。

### 0.3 ★★重大でないもの（**誤判定しないこと**）

| # | これは欠陥ではない |
|---|---|
| **1** | **★★「その症状は既に解消していた」として実装していない項目があること。** 指示書 §9.3 がそれを正しい結末としている。**`M24-01` は 8 件のうち 3 件が既に解消済みだった**（`M24-overview` §2.3）。**⇒ 未実装を「未達」と判定しないこと。ただし「成立をテストで固定した」ことは要る** |
| ~~**2**~~ | ~~**`SM-018`（比較のコンボ追加）が未実装であること**~~ | **★★2026-08-26 撤回**（**D-565**）——**確認事項 1 が決着し、`SM-018` はクラスタ A の 4 面目として実装対象になった。** **⇒ 未実装なら「高」**（4-11） |
| **3** | **★★`SM-096`（崩れ）が全文表示モードで直らず、機序と必要な手の報告だけで終わっていること**（指示書 §4.1 末尾）。**★これは正しい結末である**——**「縦表示で解決余地」は memo の見立てであって機序ではない** |
| ~~**4**~~ | ~~**`SM-089`（メモ 1 行目）が一覧に出ていないこと**~~ | **★★2026-08-26 撤回**（**D-565**）——**開発者回答で一覧にも出すことが確定した**〔**嫌われていたのはフィルタ等による圧迫であって、コンボ自体の情報量ではない**〕。 **⇒ 出ていないなら「高」**（4-6b） |
| **5** | **トグルの見た目・置き場所、`SM-003` の導線の形、メモ 1 行目の省略の閾値が設計卓の想定と違うこと**（§9.2 で委任済み） |
| **6** | **`web/e2e/support/` へファイルが増えていること**（`D-553` が運用の型として承認済み） |
| **7** | **`web/CLAUDE.md` の台帳に差分があること**（§4.1 で新キーを作った場合。**`CHANGE-134` の射程内**） |

### 0.4 報告フォーマット

指摘は **重大 / 高 / 中 / 低** で分類し、各件に**根拠**（ファイル:行 または 出力の逐語）を添える。

---

## 1. 設計・パターンとの照合（最重要）

| # | 観点 | 判定 |
|---|---|---|
| **1-1** | **★★レシピの見せ方が 4 面（一覧・詳細・比較・比較の追加モーダル）で 1 つの規則を通っているか**（§4.1）。**★面ごとに別々の見せ方を作っていないか** | **面ごとに違う実装なら「重大」**——**それは本サブが終わらせようとした状態そのものである**（§1.2） |
| **1-2** | **★★全文表示モードのオン/オフが 4 面で 1 つの値を共有しているか**（§4.1）。**★面ごとに別々の値を持っていないか**（`E-76`＝同じ情報を 2 か所に持たない） | **別々に持っているなら「重大」** |
| **1-3** | **★「面ごとの既定」がトグルとは別の仕組みになっているか**（§4.1。**詳細だけ全文を既定にする**）。**★トグルが 2 つ作られていないか** | トグルが 2 つあるなら「高」 |
| **1-4** | **★既定が「末尾省略＋ホバー」のままか**（§4.1）。**★既定を全文表示へ変えていないか** | **変えていたら「高」**——**`DES-005` §5.4 の懸念「一覧の縦幅が大きくなり、スクロール量が増える」が当たる** |
| **1-5** | **★全文表示モードのときにツールチップが出ていないか**（§4.1。**同じ情報が 2 か所に出る**） | 出ているなら「中」 |
| **1-6** | **★§3.3 の実査 14 件が実施され、結果が報告されているか。母数の要る項目に母数があるか**（§3.3-3 / -11 / -12 / -14） | 母数の欠落は「高」。実査の欠落は「高」 |
| **1-7** | **★★`SM-096` の崩れの機序が実測されているか**（§3.3-3）。**★崩れる閾値（ステップ数 or 文字数）が数で示されているか。「縦表示で直る」を前提に実装していないか** | **測らずに実装していたら「高」**（`M24-01` は 3 件が既に解消済みだった） |
| **1-8** | **★セットプレイのレシピ表示がコンボと同じ部品を通っているかが確かめられ、共通でないなら報告されているか**（§3.3-6 / §1.3-6） | 確認が無いなら「中」 |
| **1-9** | **★キャラ名の値が `name_ja` か**（§4.2。**`DES-005` §4.3 の並び順と同じ語彙**）。**★第 2 の語彙を作っていないか** | 別の値を出しているなら「高」 |
| **1-10** | **★ノーゲージのラベルがラベル組み立て関数の中で 1 回だけ付いているか**（§4.5・§3.3-12）。**★呼び元ごとに足していないか** | 呼び元ごとなら「中」 |

---

## 2. データ・API 契約・スキーマの不変（非破壊性）

| # | 観点 | 判定 |
|---|---|---|
| **2-1** | **`internal/` ／ `cmd/` ／ `migrations/` の差分が 0 バイトか**（分岐 A） | **0 でなく、§2.3 の 3 条件の充足が報告されていないなら「重大」** |
| **2-2** | **`internal/service/notation/` ／ `internal/service/preset/` に差分が無いか**（契約 F-1） | 差分を検出したら「重大」 |
| **2-3** | **★`recipe_cache` の中身・生成規則に触れていないか**（`DES-004` の領域。§1.3-1） | 触れていたら「重大」 |
| **2-4** | **`DES-005` §4.3.1 の解決順（5 段）が 1 文字も変わっていないか**（`CHANGE-130`） | 変わっていたら「重大」 |
| **2-5** | **`DES-005` §4.3 の並び順（`name_ja` 固定）が 1 文字も変わっていないか**（`CHANGE-133`） | 変わっていたら「重大」 |
| **2-6** | **`M24-02` の折りたたみ・要約ピルが作り直されていないか**（§1.3-7。**形式を借りるのは可**） | 作り直しなら「高」 |
| **2-7** | **`/trash` 系に差分が無いか**（M23 の成果物） | 差分を検出したら「高」 |
| **2-8** | **`queryKey` の綴りが変わっていないか**（`M24-08` の手番） | 変わっていたら「高」 |
| **2-9** | **`check-*.sh` の判定ロジックが緩められていないか**（§2.2） | 緩めていたら「重大」 |

---

## 3. ブラウザストレージ（**`CLAUDE.md` §10.X**）

**★§4.1 の状態保持で新キーを作った場合にのみ本節が効く。作っていないなら 3-6 だけを見る。**

| # | 観点 | 判定 |
|---|---|---|
| **3-1** | **★★新キーを作った場合、`web/CLAUDE.md` §1 台帳へ登録されているか** | **未登録なら「重大」** |
| **3-2** | **`bash scripts/check-browser-storage-keys.sh` が緑か。出力が載っているか** | 赤または出力が無いなら「重大」 |
| **3-3** | **★★ヘルパ（`web/src/lib/browser-storage.ts`）を経由しているか。直接 API を呼んでいないか** | 直接呼びを検出したら「重大」 |
| **3-4** | **★保持しているのが UI 状態だけか。** DB へ永続化すべきデータを入れていないか | 入れていたら「重大」 |
| **3-5** | **★キー名が `M24-02` が新設したキーと同じ形（バージョン接尾辞付き）になっているか**（§3.3-13） | 揃っていないなら「中」 |
| **3-6** | **★既存キーへ相乗りした場合、そのキーの既存の読み手が壊れていないか** | 壊れていたら「重大」 |

---

## 4. フロントエンドの動作仕様

| # | 観点 | 判定 |
|---|---|---|
| **4-1** | **★★一覧で全文表示モードにすると、レシピが truncate されずに全部読めるか**（`SM-130` / `SM-096`） | 読めないなら「重大」——**本サブの主目的である** |
| **4-2** | **★詳細でレシピがステップごとに縦に読めるか**（`SM-022`） | 読めないなら「高」 |
| **4-3** | **★比較で表示スタイルが切り替わり、省略された行が全部見えるか**（`SM-021` / `SM-014`） | 見えないなら「高」 |
| **4-4** | **★★比較表から各コンボの詳細へ行けるか**（`SM-003`）。**★その導線が「削除」と取り違えられない位置にあるか**（`M23-07` の教訓） | 導線が無いなら「高」。**削除の隣なら「重大」**——**取り違えると利用者のデータが消える** |
| **4-5** | **★比較表にキャラ名が出ているか**（`SM-061`）。**★複数キャラを並べたときにどの列がどのキャラか分かるか** | 分からないなら「高」 |
| **4-6** | **★★メモ 1 行目と `M15-05` の技名 1 行サマリが読み分けられるか**（§4.4）。**★2 種類の 1 行が混ざって「どちらがどちらか分からない」状態になっていないか** | **混ざっているなら「重大」**——**ledger が「同サマリはレシピ由来でメモ由来ではない」と明記している論点である** |
| **4-7** | **★メモが空のときに空の行が出ていないか**（§4.4） | 出ていたら「中」 |
| **4-8** | **★メモ 1 行目が長い場合に省略されているか**（§4.4。**レシピの上に長い行が来ると本末転倒**） | されていないなら「中」 |
| **4-9** | **★ノーゲージ版の起き攻めオプションに「ノーゲージ」が付いているか**（`SM-097`）。**★日本語のままか**（英語化していないか＝§4.5） | 付いていないなら「高」。**一部だけ英語化していたら「中」** |
| **4-10** | **★★`M24-02` が作った折りたたみ・要約ピル・キャラ選択が壊れていないか**（同じ画面である） | 壊れていたら「重大」 |
| **4-11** | **★★比較の追加モーダルで、候補のレシピが全文表示モードの規則に乗っているか**（`SM-018`・§4.3）。 **★機序は「モーダルが狭くレシピが短くしか出ない」であって手数ではない**（§1.1.4）。 **★「コンボを追加」ボタンを廃止していないか**（§1.1.4.2） | **規則に乗っていないなら「高」。★ボタンを廃止していたら「重大」** |
| **4-6b** | **★★`SM-089` のメモ 1 行目が一覧 `/combos` にも出ているか**（§4.4。**2026-08-26 確定**） | **出ていないなら「高」** |

---

## 5. テストの妥当性

| # | 観点 | 判定 |
|---|---|---|
| **5-1** | **レシピの省略・分割の純粋関数テストがあるか**（§5.1） | 無いなら「高」 |
| **5-2** | **メモ 1 行目の取り出しのテストが 空・改行なし・長文 を確かめているか**（§5.1） | 欠けていたら「高」 |
| **5-3** | **★★全文表示モードのトグルが 4 面で同じ値を見ていることのコンポーネントテストがあるか**（§5.1） | **無いなら「高」**——**1-2 を守る唯一の機械的な網である** |
| **5-4** | **★メモ 1 行目と技名サマリが読み分けられることのテストがあるか**（§5.1） | 無いなら「中」 |
| **5-5** | **E2E 5 ケースがあるか**（§5.1。**★5 件目＝比較の追加モーダルでもモードが効いていること**） | 欠けていたら「高」 |
| **5-6** | **★E2E の共通の下ごしらえが `web/e2e/support/` へ寄せられているか。同じ数行が複数 spec へ複製されていないか**（`E-232`） | 複製を検出したら「中」 |
| **5-7** | **★★レシピ文字列を assert している箇所の全数 grep が行われ、その結果が報告されているか**（§5.1 末尾）。**★描画方法を変えたのに既存 assert を確かめずに通していないか** | **全数の報告が無いなら「高」**——**`M24-02` で DOM 種別の変更が assert を壊した先例がある** |
| **5-8** | **★表示項目数をベタ書きしているテストを「定義した項目がすべて出る」形へ書き直しているか**（§5.2） | ベタ書きが残っているなら「中」 |
| **5-9** | **★「既に解消していた」として実装しなかった項目について、成立をテストで固定しているか**（§9.3） | **固定していないなら「高」**——**次に同じ要望が再登場する** |
| **5-10** | **`DES-005` §4.3 / §4.3.1 を守る既存テストが落ちていないか**（§5.2） | 落ちていたら「重大」 |

---

## 6. 破壊確認（**指示書 §5.3・4 件**）

| # | 観点 | 判定 |
|---|---|---|
| **6-1** | **4 件すべてが実施され、コマンドと出力が載っているか** | 欠けていたら「高」 |
| **6-2** | **★★「壊す対象が実在したか」が確かめられているか**（計測点 `M-78`）。実在しなかった項目について、その旨と差し替えが報告されているか | **無言で省略されていたら「高」** |
| **6-3** | **★★「その壊し方で本当に観測が変わったか」が示されているか**（playbook §4.32）。**★全件が緑のままだった破壊確認が、緑のまま報告されていないか**〔`M24-02` の破壊確認 3 は「値を読むだけ」の壊し方で 33 件すべて緑だった〕 | **緑のまま「実施した」で終わっているなら「重大」**——**それは壊せていない** |
| **6-4** | **★破壊確認 2（4 面が見る値を分ける）で赤くなったか。** 赤くならないなら **1-2 を守るテストが存在しない**＝その旨が報告されているか | **赤くならず、報告も無いなら「重大」** |
| **6-5** | **★赤くなった経路が数えられているか**（§5.3 末尾） | 数が無いなら「中」 |

---

## 7. ドキュメント・進捗ログ

| # | 観点 | 判定 |
|---|---|---|
| **7-1** | **`docs/progress/progress-log.md` へ索引行が追記されているか** | 無いなら「高」 |
| **7-2** | **★`check-progress-log-index.sh` が緑でも、索引行を目で確かめたか**（followup §AH） | 目視確認の記載が無いなら「中」 |
| **7-3** | **§7.4.1 の 8 条件が 1 件ずつ判定されているか**（**★「射程は広がらなかった」でも判定したことを書く**） | 欠けていたら「高」 |
| **7-4** | **★★モック HTML が `docs/progress/M24-03-mock/` にあり、変更前と変更後が並んでいるか**（§4.7）。**★レシピの全文表示は 4 面すべて、既定と全文の両方が出ているか。★★§3.3-14 の基準値（1 行の高さ・画面に収まる行数）が数で添えられているか**（**開発者が「既定をどちらにするか」を選ぶ材料である**） | **変更前が無いなら「高」**——**開発者が「もう直っているのか」を判定できない**。**4 面が揃っていない／基準値が無いなら「中」** |
| **7-5** | **★モックが Plan Mode の提示に含まれていたか**（実装後に作られていないか＝§4.7） | 実装後なら「中」 |
| **7-6** | **★モックが作り込まれすぎていないか**（`M24-overview` §1.5＝**作り込みそのものが実装の約束に見える**） | 過剰なら「低」 |
| **7-7** | **★件数を本文へ書いた箇所に、その場で数え直したコマンドが併記されているか**（§7.5・`M24-02` §7-3） | 無いなら「中」 |
| **7-8** | **★★`make e2e` の実行直前にポートが空いていることを確認した出力が載っているか**（§2.4・`D-548`） | 無いなら「中」 |

---

## 8. 重大な問題の判定基準（完了承認を妨げる）

1. **★レシピの見せ方が 4 面で別々の実装になっている**（1-1）。
2. **★全文表示モードの値が 4 面で共有されていない**（1-2）。
3. **`internal/` に差分があり §2.3 の 3 条件の充足が示されていない**（2-1）。
4. **契約 F-1 の凍結パッケージに差分がある**（2-2）／ **`recipe_cache` の生成規則に触れている**（2-3）。
5. **`DES-005` §4.3.1 の解決順**（2-4）／ **§4.3 の並び順**（2-5）**が変わっている。**
6. **`check-*.sh` の判定ロジックを緩めている**（2-9）。
7. **新キーが台帳に未登録**（3-1）／ **`check-browser-storage-keys.sh` が赤**（3-2）／ **ヘルパ外の直接呼び**（3-3）／ **DB 永続化対象データの保管**（3-4）／ **既存キーへの相乗りで既存の読み手が壊れた**（3-6）。
8. **一覧で全文表示にしてもレシピが読めない**（4-1）。
9. **★比較 → 詳細の導線が「削除」と取り違えられる位置にある**（4-4）。
10. **★メモ 1 行目と技名 1 行サマリが読み分けられない**（4-6）。
11. **`M24-02` の成果物が壊れている**（4-10）／ **`DES-005` §4.3 系の既存テストが落ちている**（5-10）。
11b. **★「コンボを追加」ボタンを廃止している**（4-11）。
12. **★破壊確認が緑のまま「実施した」で終わっている**（6-3）／ **破壊確認 2 が赤くならず報告も無い**（6-4）。

---

## 9. 軽微な問題の判定基準（持ち越し許容）

- トグルの見た目・アイコン・置き場所。
- `SM-003` の導線の形（列ヘッダのリンク ／ 行クリック 等。**位置の安全性は 4-4 で見る**）。
- メモ 1 行目の省略の閾値。
- モックの作り込みの度合い。
- `web/e2e/support/` のヘルパの命名。

---

## 10. 質問・確認事項のフォーマット

**「何が」「どの根拠で」「どう違うか」の 3 点を書く。** 推測を断定形で書かないこと。

---

## 11. レビュー完了の判定

**重大 0 件になるまで往復する。★上限は 2 回**（`CLAUDE.md` §9 停止規律）。**上限に達したら未解消項目を `followup-backlog` §J へ必須 5 フィールドで記録し、直ちに停止する。**

### 11.1 ★本チェックリスト自身の検査（**発行前に設計担当が行う**）

- [x] **指示書の版数と一致しているか**（v1.0.0 ⇄ v1.0.0）
- [x] **§0.3 が「既に解消していた項目を実装していない」を欠陥と判定しない形になっているか**
- [x] **★★§0.3-2 / §0.3-4 が撤回済みであることを反映しているか**（**確認事項 2 件が決着した**＝`D-565`）
- [x] **§0.3 が「崩れが全文表示モードでは直らず、機序の報告で終わった」を欠陥と判定しない形になっているか**
- [x] **破壊確認の観点が「壊す対象が実在したか」（`M-78`）と「その壊し方で観測が動くか」（playbook §4.32）の両方を含んでいるか**
- [x] **★実査で決まる面を、断定形で書いていないか**（`M-77`）
- [x] **★「4 面が 1 つの規則を通っているか」が重大判定に入っているか**（本サブの主目的）
- [x] **ブラウザストレージの観点が `CLAUDE.md` §10.X の 4 点を網羅しているか**
- [x] **★件数の記載が指示書と一致しているか**（実査 **14 件** ／ 破壊確認 **4 件** ／ E2E **5 ケース** ／ §7.4.1 **8 条件** ／ モック **4 つ** ／ 適用範囲 **4 面**）

---

*以上、M24-03 レビューチェックリスト **v1.1.0**。*
