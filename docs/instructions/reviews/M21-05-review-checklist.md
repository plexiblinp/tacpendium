# M21-05 レビューチェックリスト

| 項目 | 内容 |
|------|------|
| 文書ID | M21-05-REVIEW-CHECKLIST |
| バージョン | 1.0.0 |
| 対象 | `docs/instructions/M21-05-keyboard-input.md` v1.0.0 |
| 対の CHANGE | `docs/change-notes/CHANGE-110-notification.md` |
| 作成者・作成日 | 設計卓（Claude Code・武装中）/ 2026-08-14 |

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| 1.0.0 | 2026-08-14 | 初版（指示書 v1.0.0 と対）。 |

---

## 0. 読む順（★ここから始める）

1. **§1.5 を最初に見る**（**判定層・調停・配送チャネルを分岐させていないか**）。**分岐は動いてしまうため、テストでは落ちない**
2. **§1.4**（テキスト入力欄との衝突）。**落とすと利用者は原因を言語化できない**
3. **§1.1**（既定を持たない全キー登録制）
4. **§4**（テスト。とくに破壊確認）
5. 残りは順不同

> **★本サブは「もう 1 つの入力デバイスを足す」だけに見える。** **実質の論点は、既にある 3 つの土台**〔判定層・受け手の調停・操作の配送〕**へ、新しい経路を作らずに乗れているかである。**

---

## 1. 設計意図との一致

### 1.1 既定を持たない全キー登録制（§4.1）

| # | 確認 |
|---|---|
| 1 | **キー割当の既定プロファイルが無い**（**「とりあえず WASD を入れておく」形になっていない**） |
| 2 | **★未登録の状態で入力面に立つと、登録が要ることが分かる**（§5 (a)） |
| 3 | **★それが「失敗」ではなく「状態」として出ている**（**エラー表示になっていない**） |
| 4 | **「対応キーボード一覧」を作っていない** |

> **★項目 2 は開発者の手元では通らない経路である**（登録済みの状態でテストするため）。**⇒ テストの有無で判定すること。** **手動確認の報告だけで通さない。**

### 1.2 操作 3 種の直接割当（§4.2）

| # | 確認 |
|---|---|
| 1 | **削除 ／ 保存 ／ 修飾の 3 つが、それぞれ 1 つのキーへ直接割り当たっている** |
| 2 | **前置きボタン方式を採っていない** |
| 3 | **★操作の種別が `M21-04` と同じ 3 値である**（**新しい列挙を作っていない**＝`E-76`） |
| 4 | **★操作用のキーが押下集合に入っていない**（§5 (e)。**`M21-04` と同型の壊れ方**） |
| 5 | **登録対象の件数と内訳が完了報告にある** |

### 1.3 設定できないキー（§4.3）

| # | 確認 |
|---|---|
| 1 | **ファンクションキー・Windows キー等が登録できない** |
| 2 | **★拒否の理由が利用者へ出ている**（**無反応になっていない**） |
| 3 | **★「除外」と「未登録」が別の表示になっている**（`E-84`。§5 (c)） |
| 4 | **除外したキーの列挙と、判断の根拠が完了報告にある** |

### 1.4 テキスト入力欄との衝突（§4.4・**最重要の 2 本目**）

| # | 確認 |
|---|---|
| 1 | **編集可能な要素にフォーカスがある間、技入力が発火しない** |
| 2 | **★操作キーも同じ扱いである**（**メモ欄を打っている最中に保存が走らない**） |
| 3 | **★判定が「編集可能な要素にフォーカスがあるか」で行われている。** **要素の種類を列挙して分岐していない**（**列挙は必ず取りこぼす**） |
| 4 | **テストがあり、破壊確認済みである**（§5 (d)） |

> **★落とすと何が起きるか。** **文字を打つたびにステップが入り、しかも文字は正しく入る。** **⇒ 動作を見ても原因が分からない。** **利用者は「勝手にステップが増える」としか言えない。**

### 1.5 ★経路を増やしていないか（§4.5・§4.6・**最重要の 1 本目**）

| # | 確認 |
|---|---|
| 1 | **判定層に「キーボードのときは」という分岐が無い**（`§4.5-4`。**入ると `M21-02` の as-built が 2 通りになる**） |
| 2 | **判定窓の値・アルゴリズムが不変である**（`§2.2-1`。**diff で示されている**） |
| 3 | **新しい調停を作っていない**（`§4.6-1`） |
| 4 | **新しい配送チャネルを作っていない**（`§4.6-2`） |
| 5 | **★供給元が 2 つになる形をどう解いたかが完了報告にある**（`§4.6-3`） |
| 6 | **排他になっていない**（`§4.6-4`。**「キーボードモード」を作っていない**）。§5 (i) |
| 7 | **キーリピートが新しい押下として扱われていない**（`§4.5-3`。§5 (f)） |

> **★分岐は動いてしまう。** **キーボードでもパッドでも正しく入力できるため、テストも手動確認も通る。** **露見するのは、次に判定を直す担当が片方だけ直したときである。** **⇒ 「動いているから良い」で通さないこと。**

### 1.6 ブラウザストレージ台帳（§4.7）

| # | 確認 |
|---|---|
| 1 | **`web/CLAUDE.md` §1 の台帳が更新されている** |
| 2 | **★followup `browser-storage-ledger-calibration-targets` が同じ手番で解消されている**（**`M21-04` の 13 → 14 が用途欄に現れる**） |
| 3 | **`bash scripts/check-browser-storage-keys.sh` が緑** |
| 4 | **台帳未記載のキーを使っていない**（CLAUDE.md §10.X。**禁止事項**） |

---

## 2. 契約・凍結インターフェース

| # | 確認 |
|---|---|
| 1 | **`internal/` 全体の diff 0**（契約 F-1）。 **★【2026-08-14 errata・版据置】この書き方は契約の誤読である**（**M-65**）——**F-1 は `recipe_cache` の読み書き契約であって `internal/` 全体の凍結ではない。** **本サブの判定結果は変わらない。** **次のサブへ写さないこと**（正しい書き方は `M21-06` 指示書 §2.2-7） |
| 2 | **`internal/moveindex/` ／ `move_commands` の diff 0**（契約 F-3） |
| 3 | **`migrations/` の diff 0**（**disk 末尾を着手時に実査した記録つき**） |
| 4 | **`scripts/` の diff 0**（**D-335**。**`M20-06` と並走しうる**） |
| 5 | **`docs/design/` ／ `docs/change-notes/` の diff 0** |
| 6 | **Gamepad 側の登録導線・既定プロファイル・正規化が不変**（`§2.2-5`。**キーボードのために書き換えていない**） |
| 7 | **`DES-005` §6.2 の押下集合の扱いが不変**（`§2.2-6`） |

> **★`§2.2` は挙動の凍結であり、失効した記述の是正を妨げない**（**D-361**）。**とくに「キーボード経路は `M21-05` が持つ」という未来形は、本サブが乗った時点で失効する**（`§4.8-2`）。**是正を「変更しないと書いてある」だけの理由で差し戻さないこと。**

---

## 3. 実装品質

| # | 確認 |
|---|---|
| 1 | `any` / `@ts-ignore` / `eslint-disable` / `console.log` の追加 0 件 |
| 2 | 関数コンポーネント ＋ Hooks のみ（CLAUDE.md §4） |
| 3 | 列挙的文字列定数がリテラルで散在していない |
| 4 | **除外キーの集合が 1 か所にある**（**複数箇所へ写されていない**＝`E-76`） |
| 5 | `TODO` コメントが `// TODO(<対応予定>): <内容>` 形式である |
| 6 | 既存 test-id を変更・削除していない（**追加は可**。命名は `DES-005` §6.8） |

---

## 4. テスト

| # | 確認 |
|---|---|
| 1 | **§5 の (a)〜(i) が実装されている** |
| 2 | **★(d)(e)(f) の破壊確認が行われ、「どのテストが赤くなったか」まで報告されている** |
| 3 | **★破壊確認で赤にならなかった項目について、「テストが弱い」ではなく「別の機構が代わりに守っている」を先に疑った形跡がある**（`SUPP-001` §5.5 (10)） |
| 4 | **(g) が「同じ入力列に対して同じステップ列が出る」形である**（**判定の同一性を主張している**） |
| 5 | `pnpm test` 全緑 ／ `pnpm lint` exit 0 ／ `go test ./...` FAIL 0 ／ `make e2e` 全緑 |
| 6 | **E2E spec を足す場合、グローバル資源を触るものは資源ごとに 1 ファイルへ寄せている**（**D-362**） |

---

## 5. ドキュメント

| # | 確認 |
|---|---|
| 1 | **`docs/progress/progress-log.md` へ 1 行追記されている**（CLAUDE.md §8。**必須**） |
| 2 | **完了報告 `docs/progress/M21-05-completion-report.md` に §7.5 の 8 項目がある** |
| 3 | **§7.5-8**（**開発者の実機確認手順**）**がある。★機材はキーボードのみであることが書かれている** |
| 4 | **`web/CLAUDE.md` §1 が更新されている**（`§4.7`） |

---

## 6. 否定形確認（§4.8）

| # | 確認 |
|---|---|
| 1 | **2 軸で回している**——**(a) 識別子 ／ (b) 前提を述べている散文**（**D-361**） |
| 2 | **★走査コマンドそのものが完了報告に残っている** |
| 3 | **§4.8 の 4 項目それぞれについて結果がある** |
| 4 | **★項目 4 は「失効していない」が正解である**——**`M21-05` は M21 の中では直列のままである。** **`M20-06` との並列は別マイルストーンであり、`M21-overview` §5.2 の対象ではない** |

---

## 7. 停止規律

| # | 確認 |
|---|---|
| 1 | **§9.4 の停止条件に当たった場合、`followup-backlog` §J へ必須 5 フィールド付きで記録され、停止している** |
| 2 | **再レビュー往復が 2 回を超えていない** |
| 3 | **記録して停止した場合、それを「失敗」として扱っていない**（**正規の完了形式である**） |

---

## 8. 差し戻し事由（重大）

| # | 事由 |
|---|---|
| **重大 1** | **★判定層に「キーボードのときは」という分岐がある**（`§4.5-4`） |
| **重大 2** | **★判定窓の値・アルゴリズムを変えた**（`§2.2-1`） |
| **重大 3** | **★新しい調停 ／ 新しい配送チャネルを作った**（`§4.6-1`・`§4.6-2`） |
| **重大 4** | **★操作の種別を `M21-04` と別に定義した**（`§4.2-3`） |
| **重大 5** | **★操作用のキーが押下集合に入っている**（`§4.2-4`） |
| **重大 6** | **★テキスト入力欄にフォーカスがある間に技入力または操作が発火する**（`§4.4`） |
| **重大 7** | **★編集可能かの判定を、要素の種類の列挙で行っている**（`§4.4-2`） |
| **重大 8** | **既定のキー割当を用意した**（`§4.1-1`） |
| **重大 9** | **前置きボタン方式を採った**（`§1.3-1`） |
| **重大 10** | **除外キーの拒否が無反応である**（`§4.3-2`） |
| **重大 11** | **「除外」と「未登録」が同じ表示である**（`§4.3-3`） |
| **重大 12** | **台帳未記載のストレージキーを使った**（CLAUDE.md §10.X） |
| **重大 13** | **キーリピートで同じステップが並ぶ**（`§4.5-3`） |
| **重大 14** | **Gamepad とキーボードが排他になっている**（`§4.6-4`） |
| **重大 15** | **`internal/` ／ `migrations/` ／ `scripts/` ／ `docs/design/` の diff がある** |
| **重大 16** | **Gamepad 側の登録導線・既定プロファイル・正規化を書き換えた**（`§2.2-5`） |

---

## 9. ★このレビューで判定してはいけないこと

| # | 内容 | 理由 |
|---|---|---|
| 1 | **既定のキー割当を用意すべきかどうか** | **`D-370`＝開発者判断で「デフォルトは用意しない」。** **キーボードは利用者により操作設定がかなり変わる** |
| 2 | **前置きボタン方式のほうが一貫するのではないか** | **`§1.3-1`。`M21-04` が採ったのは余りボタンの制約ゆえであり、キーボードにその制約は無い** |
| 3 | **除外するキーの集合が過不足か** | **`§9.2-2` で製造へ委ねた**（**根拠が書かれていることだけを見る**） |
| 4 | **登録導線を Gamepad と同じ画面に置いたか、別に置いたか** | **`§9.2-1` で製造へ委ねた** |
| 5 | **保存先を既存キーへ相乗りさせたか、新規キーにしたか** | **`§9.2-5` で製造へ委ねた**（**台帳の追記があることだけを見る**） |
| 6 | **「登録が要る」ことの見せ方の細部** | **`§9.2-3` で製造へ委ねた**（**失敗ではなく状態として出ていることだけを見る**） |
| 7 | **`DES-005` §6.4.3 項目 4 の「供給元はアプリ全体で 1 個」と矛盾していること** | **★矛盾は設計書側にある。** **設計卓が `CHANGE-110` で是正する**（`§4.6-3`） |
| 8 | **設計書の記述が古いこと**（SF6 デフォルト準拠 等） | **本サブで逆転する。** **設計書を直すのは設計卓である** |

---

*以上、M21-05 レビューチェックリスト v1.0.0。*
