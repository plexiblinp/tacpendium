# M22-07 レビューチェックリスト

| 項目 | 内容 |
|------|------|
| 対象指示書 | `docs/instructions/M22-07-alias-dictionary-enrichment.md` **v1.4.0**（取込の別名辞書の充実・`G-14b` の残り半分） |
| 文書ID | M22-07-REVIEW |
| 対象指示書ID | M22-07 |
| レビューモデル | **高精度レビュー**（**★増やした辞書が「効かない」も「別の技として確定する」も、テストと型検査が緑のまま利用者のデータへ入る**。実モデル名は `docs/human-notes/model-allocation.md` が正本。実使用は開発者判断） |
| バージョン | 1.4.0 |
| 作成者・作成日 | 設計担当 Claude（M22 期・設計卓）/ 2026-08-15 |

> 文書ID ⇄ 実パスの正は `docs/handover/docs-map.md`（自動生成）。

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| 1.1.0 | 2026-08-15 | **指示書 v1.1.0 と対。疎通確認が製造の手番になったことを反映**（**D-398**）。 |
| 1.0.0 | 2026-08-15 | 初版（指示書 v1.0.0 と対）。 |

---

## 0. レビュー担当への前置き

### 0.1 レビュー前の準備

**必読**——指示書 `M22-07` 全文 ／ **`DES-004` §5.0・§5.4・§5.5・§5.6・§5.7** ／ `DES-003` §3.9 ／ `docs/change-notes/CHANGE-104-notification.md` ／ `docs/process/m22-contract.md` §1.2・§4・§5 ／ **`SUPP-001` §5.5**（とくに (6) (10′) (11′) **(11″)** ／ §5.5.2 ／ §5.5.3.1 ／ §5.5.8）。

**★Plan Mode の実査 8 項目（指示書 §3.3）が確定してから実装されているかを最初に確認すること。** **とくに §3.3-1（「いま何が解決できていないか」の測り方）は、確定しないまま進んだ場合、以降の数値がすべて根拠を失う。**

### 0.2 レビューの基本姿勢

**★最重要ゲートは 4 つ。** **いずれも「破っても緑になる」型である。**

1. **実測が `M20-07` の逆引きの実装そのもので行われていること**（`SUPP-001` §5.5 (11″)）。**★自作の近似で数えても、もっともらしい件数が返る。** **実装と違う母集合を数えたことは、数値を見ても分からない。**
2. **3 数値が固定されていること**——**(a) 答えが変わった入力 0 ／ (b) 解決しなくなった入力 0 ／ (c) 新たに解決した入力 N**（指示書 §4.3-2）。**★(c) だけを見て承認しないこと。** **(a)(b) が測られていない実装は、利用者のレシピの表示を黙って変えうる。**
3. **衝突がキャラ単位で数えられていること**（同 §4.4）。**★プリセット単位で数えると本物の衝突が 0 件に見える**（`M20-07` の実測＝`微歩き` の 19 組）。**制約の単位（`(preset_id, character_id, alias_text)`）と照合の単位（全プリセット横断）が違う**＝教訓 **`E-127`**。
4. **`DES-004` §5 の解決順序・段・規約に diff が無いこと**（契約 **F-2**）。**★本サブは行を増やすだけである。**

### 0.3 ★重大でないもの（**誤判定しないこと**）

| # | 誤判定しやすい状態 | 実は仕様である理由 |
|---|---|---|
| N-1 | **(b) キャラ通称（「ライズ」「フレア」等）が投入されていない** | **指示書 §4.2 が意図している。** **製造は値を持っていない。** **★列挙して報告するところまでが本サブの完了形である**——**「未完了」として扱わないこと** |
| N-2 | **増やした `alias_text_en` が画面に出ない** | **指示書 §1.3-7 でスコープ外。** **followup `alias-text-en-display-and-reverse-lookup-unimplemented` (1) の担当であり、`M20-06` 以来続いている既知の未実装** |
| N-3 | **記法（`DES-004` §2 系）で拾えない表記が残っている** | **指示書 §1.3-3 でスコープ外。** **辞書ではなく規則の話である**（followup `command-hold-two-levels-indistinguishable` ／ `numeric-aerial-vs-crouching-collision`） |
| N-4 | **CHANGE 通知書が無い** | **指示書 §6.5。** **`DES-004` §5 の規則に触れないため、着手時点では CHANGE が要らない。** **★要否は as-built で判定し、該当したら完了報告へ書く運用である** |
| N-5 | **`internal/` に diff がある** | **★本サブは `internal/` 全体を凍結していない**（指示書 §2.2-2 が凍結するのは `internal/service/notation/` と `internal/service/preset/` だけ＝契約 F-5）。**生成器（`internal/seedgen/` 等）への追加は正常である。** **「契約違反」として扱わないこと**（**D-379** の型） |
| N-6 | **E2E spec が 1 本も増えていない** | **指示書 §5.2 が「増やさない見込み」としている。** **バックエンド・データ完結のため。** **非回帰の確認で足りる** |
| N-7 | **実測件数がコード・マイグレのコメントに書かれていない** | **指示書 §2.2 但し書きが禁じている**（`SUPP-001` §5.5.3.1 ／ 教訓 `E-136`）。**件数の正本は契約テスト 1 か所である。** **★書いてある方が違反** |
| N-7′ | **開発者の実機確認が行われていない** | **★本サブは開発者の手動確認を挟まない**（指示書 §7.1＝**D-398**）。**バックエンド・データ完結であり、開発者が画面で見て判定できるものが無い。** **⇒ 見るのは「製造が示した実出力が完了報告にあるか」である** |
| N-8 | **指示書の §4.5（`recipe_cache`）と §4.9（否定形確認）の番号** | **指示書 §4.9 冒頭に理由が明記されている。** **詳細仕様が §4.6 まで伸びたため寄せたもので、抜け番ではない** |

### 0.4 報告フォーマット

- 各節「OK / 重大（§9）/ 軽微（§10）/ 質問（§11）」。

---

## 1. 設計・パターンとの照合（最重要）

- [ ] **★「増やす前の基準値」が実測されており、母集合が明記されている**（指示書 §4.1-1・§4.1-3）。**「何を・どの単位で・どの基準時点で数えたか」が併記されているか**（教訓 `E-93` / `E-16`）。
- [ ] **★測定に使ったのが `M20-07` の逆引きの実装そのものである**（同 §4.1-2・`SUPP-001` §5.5 (11″)）。**自作の近似・テスト専用の再実装になっていないか。**
- [ ] **★増やす行が実測から逆算されている。思いつきで増やしていない**（同 §1.2-2）。
- [ ] **(a) 機械的に導ける別名 と (b) ドメイン知識が要る別名 が 2 分類されている**（同 §4.2）。**★件数を合わせるために行が落とされていないか**（**D-357** の型）。
- [ ] **(b) が全数列挙されて報告されている**（同 §4.2）。**★「主なもの」で打ち切られていないか。**
- [ ] **(a) について「導ける」が実査で確かめられている**（同 §4.2 末尾）。**導けなかった分が (b) へ回されているか。**
- [ ] **増やした行が既存の `preset_aliases` の値を書き換えていない**（同 §2.2-6）。
- [ ] **投入が生成器を通っている。マイグレへ直接 INSERT を書いて終わりにしていない**（同 §4.6・`DES-004` §5.7 の第 2 点）。
- [ ] **`DES-004` §5.4 の「seed 波ごとの再適用責務」が満たされている**（同 §4.6）。**将来のキャラ追加時にも同じ規則が当たるか。**
- [ ] **生成器の出力形式を変えたなら、旧形式が named option として保存され、適用済みの生成物が byte 一致で再生成できる**（`SUPP-001` §5.5.8 の世代ゲート）。

## 2. データ・API 契約・スキーマの不変（非破壊性）

- [ ] **★`DES-004` §5.0・§5.1〜§5.6 に相当する解決ロジックへ diff が無い**（契約 **F-2**）。**新しい段・新しい正規化が足されていないか**（指示書 §1.3-2）。
- [ ] **`internal/service/notation/` ／ `internal/service/preset/` の diff が 0**（契約 F-5。**実測値が完了報告にあるか**）。
- [ ] **`web/src/` 配下の diff が 0**（指示書 §2.2-4。**★`M22-06` ／ `M21-07` との非干渉**）。
- [ ] **`docs/design/` の diff が 0**（**製造は設計書本体を編集しない**＝`CLAUDE.md` §8）。
- [ ] **適用済みマイグレが改変されていない**（指示書 §2.2-3）。**★正本の是正が要った場合、`SUPP-001` §5.5.4 (6) の三点更新（CSV 正本の是正 → golden 再生成 → 追随マイグレ）のどれを採ったかが完了報告にあるか。**
- [ ] **`preset_aliases` の UNIQUE 制約 2 本に変更が無い**（`DES-003` §3.9）。
- [ ] **★`character_id` が全行で埋まっている**（指示書 §4.4-4・`DES-004` §5.7 の第 1 点）。**nullable ゆえ入れ忘れても落ちない**——**テストで固定されているかを見ること。**
- [ ] **消費したマイグレ連番が完了報告に書かれている**（指示書 §7.3）。**★自採番していないか**（§3.3-8・**D-293**）。

## 3. `recipe_cache` への影響

- [ ] **既存の再計算経路に乗るかどうかが実査されている**（指示書 §4.5-2）。
- [ ] **乗らない場合、止めて報告されている**（同 §9.3-5）。**★契約 F-5 に触れるため、製造の判断で進めてはならない。**
- [ ] **「増やしたのに表示が変わらない」「知らないうちに表示が変わった」のどちらになるかが完了報告に書かれている**（同 §4.5-3）。

## 4. テストの妥当性（ケース数で確認。指示書 §5 ↔ §7 DoD）

- [ ] **§5.1 の 8 項目がすべてある。**
- [ ] **★3 数値が契約テストで固定されている**——**(a) 0 ／ (b) 0 ／ (c) N**（§5.1-1）。**★(a) か (b) が 0 でないまま進んでいないか**（§9.3-4 の停止条件）。
- [ ] **★測定が実装の関数そのもので行われている**（§5.1-2）。**テスト側に照合の再実装が置かれていないか。**
- [ ] **衝突検査がキャラ単位である**（§5.1-3）。**★プリセット単位のままなら重大**（§9-3）。
- [ ] **★投入した行と投入しなかった行が対で固定されている**（§5.1-4・`SUPP-001` §5.5.2 (3)）。**件数だけでは、規則を広く当てすぎても検出できない。**
- [ ] **マイグレ契約テストの比較区間が自サブに閉じている**（§5.1-6・同 (1)(2)）。**始端＝自サブの直前・終端＝自サブの最終連番。`m.Up()` を使っていないか。**
- [ ] **down のスコープ条件が up の反転になっていない**（§5.1-7・同 (5)）。
- [ ] **★破壊確認が行われている**——**増やした行を 1 つ削ると (c) が減ること**（§5.1-8）。
- [ ] **★赤にならなかった場合、「何が代わりに守っていたか」が特定され、同じ層に契約テストが置き直されている**（`SUPP-001` §5.5 (10′)）。
- [ ] **★完了報告のテスト結果が、コマンド自身の出力（スイート名・pass/fail の件数）で書かれているか**（教訓 **E-125**）。**「exit 0」「全緑」だけを根拠にしていないか**——**依存が未インストールでも終了コード 0 を返す経路がある。**

## 5. 設計意図との整合（精神の確認）

- [ ] **★「照合を直してから、増やす価値のある辞書を測る」という順序が守られているか**（指示書 §1.1・`CHANGE-104`）。**測らずに増やしていないか**——**それは本サブの目的そのものに反する**（§9.3-3）。
- [ ] **★「この表記も拾いたい」を規則側で解こうとしていないか**（同 §1.3-2・§9.3-1）。**辞書の話と規則の話が混ざっていないか。**
- [ ] **★衝突組は両方落ちる**（`DES-004` §5.5）。**増やした行が既存と衝突して既存まで解決しなくなる経路が検査されているか**（指示書 §4.3-4）。**★これが (b) が 0 でなくなる主な経路である。**

## 6. コード品質・規約遵守

- [ ] 曖昧語依存でない（playbook §4.1）。定数化・命名一貫。
- [ ] `fmt.Println` を本番コードに残していない（`CLAUDE.md` §10）。
- [ ] **列挙的文字列定数がパッケージ定数化されている**（`CLAUDE.md` §4）。`bash scripts/check-enum-sync.sh` が回されているか（指示書 §7.3）。
- [ ] **★実測件数がコメント・マイグレのヘッダへ書かれていない**（指示書 §2.2 但し書き ／ `E-136`）。**★適用済みマイグレのヘッダは改変できないため、失効しても直せない。**

## 7. 既存挙動の温存（非破壊性）

- [ ] **既存の逆引きの答えが変わっていない**（§4 の (a) 0 件で担保）。
- [ ] **★製造が取込ヘルパー経由で 2 件以上を示し、実出力を完了報告へ転記している**（指示書 §7.1＝**D-398**）——**(a) 新たに解決するようになった別名 ／ (b) 従来から解決していた別名が同じ答えのままであること。** **★(b) を落とすと「増えた」だけが見え、「壊れていない」が見えない。** **★開発者の手動確認は本サブでは求めていない。**
- [ ] **`make e2e` が非回帰である**（同 §5.2）。**★着手前から落ちていた場合、その旨が §3.3-7 の実査として記録されているか。**

## 8. ドキュメント・進捗ログ

- [ ] 完了報告に **指示書 §7.5 の 11 項目**がすべてある。
- [ ] **§6.5 の CHANGE 要否の判定が書かれている**（**該当なしなら「該当なし」と明記されているか**）。
- [ ] **★(a) と (b) が書き分けられているか**（指示書 §4.9-2）。**「全部入った」と読まれる書き方になっていないか。**
- [ ] **撤回・変更した定義の残骸が残っていないか。** 指示書 §4.9 の走査結果が完了報告にあるか。**★走査コマンドそのものが残っているか。**
- [ ] 走査対象に **3 系統すべて**が含まれているか——**本番コード／テスト資産／設計文書・指示書・overview**。
- [ ] **★陽性対照が置かれているか**（`CHANGE-104` の「辞書の充実は含めない」は必ず当たる）。**「0 件だった」で終わっていないか**（教訓 `E-84`）。
- [ ] **★日本語の散文を否定文字クラスで走査するとき `LC_ALL=C.UTF-8` が付いているか**（`SUPP-001` §5.5 (11′)）。
- [ ] **★当たったファイルの全文が目視されているか。**
- [ ] **★並列相手との突合が行われているか**（教訓 `E-121`）。**`M22-06` ／ 直列本線の完了済みサブとの交差。とくにマイグレ連番の払い出し順。** **並列相手が居なかったなら「居なかった」と明記されているか。**
- [ ] **★完了報告に「■ 併せて更新が要るもの」の節があるか**（教訓 `E-114`）。**該当が無い場合も「なし」と書かれているか。**
- [ ] **`docs/progress/progress-log.md` へ追記されているか**（`CLAUDE.md` §8）。
- [ ] **★followup の更新候補が設計伝達レポート §4 へ書かれているか**（**製造は `followup-backlog.md` を編集しない**＝**D-382**）。

---

## 9. 重大な問題の判定基準（完了承認を妨げる）

1. **★実測が実装の関数そのもの以外で行われている**（`SUPP-001` §5.5 (11″)）。**もっともらしい件数が返るため、数値を見ても気づけない。**
2. **★(a) 答えが変わった入力 か (b) 解決しなくなった入力 が測られていない、または 0 でないまま進んでいる**（指示書 §4.3-3・§9.3-4）。
3. **★衝突をプリセット単位で数えている**（`E-127`。**本物の衝突が 0 件に見える**）。
4. **`DES-004` §5 の解決順序・段・規約に diff がある**（契約 **F-2**）。
5. **`internal/service/notation/` ／ `internal/service/preset/` ／ `web/src/` ／ `docs/design/` に diff がある。**
6. **既存の `preset_aliases` の行の値を書き換えている**（指示書 §2.2-6・§9.3-2）。
7. **`character_id` が空の行がある**（`DES-004` §5.7 の第 1 点。**nullable ゆえ落ちない**）。
8. **マイグレ連番を自採番している**（**D-293**）。**または disk 末尾と board の記録の不一致を報告せずに進めている。**
9. **投入が生成器を通っておらず、マイグレへ直接 INSERT されている**（`DES-004` §5.7 の第 2 点。**★次の seed 波で再適用されない**）。
10. **破壊確認（§5.1-8）が行われていない、または赤にならなかった項目を「テストを強くした」だけで済ませている。**
11. **(b) の候補が全数列挙されず、「主なもの」で打ち切られている**（**設計卓が開発者へ諮れない**）。

## 10. 軽微な問題の判定基準（持ち越し許容）

- テストのファイル配置・命名（指示書 §9.2-1）。
- 生成器の内部の実装形（出力が `DES-004` §5.7 の規約どおりなら＝同 §9.2-2）。
- 失効した docblock・コメントの残り（**★ただし本サブが触った箇所に限る**）。
- (b) の候補の並び順・分類の粒度。

## 11. 質問・確認事項のフォーマット

「指示書 §X.X / `DES-004` §5.X / `SUPP-001` §5.5 (N) に対し実装が Z。意図確認したい」の形で根拠節を併記する。

## 12. レビュー完了の判定

§1〜§8 が全て OK、§9 重大ゼロ（**特に §9-1〜§9-3**）、§0.1 の Plan Mode 実査 8 項目が揃っている。§10 軽微は持ち越し可。

### 12.1 ★本チェックリスト自身の検査（**発行前に設計担当が行う**）

- [x] **本書と対の指示書の間に、節番号の食い違いが無い。**
  → **指示書は詳細仕様が §4.1〜§4.6 まで伸びたため、否定形確認をテンプレの §4.5 から §4.9 へ寄せてある**（指示書 §4.9 冒頭に理由あり）。**本書は否定形確認を一貫して「指示書 §4.9」と書き、`recipe_cache` 側を「指示書 §4.5-2」のように項番付きで書き分けた。** **あわせて指示書は §6.5（CHANGE の要否）を §6 と §7 の間に持つ**——本書 §8 と §0.3 N-4 から参照している。
- [x] **指示書が禁じていない事項を「重大」として求めている項目が無い。**
  → **§9-5 は `internal/service/notation/` ／ `internal/service/preset/` に限っており、`internal/` 全体を求めていない**（指示書 §2.2-2 と §0.3 N-5 で明示的に打ち消してある）。**★生成器への追加は正常である**（**D-379** の型を避けた）。
  → **§9-11 は「(b) を投入していないこと」を重大としていない**（§0.3 N-1）。**求めているのは全数列挙である。**
- [x] **達成不能な条件を重大として求めていない。**
  → **§9-1〜§9-3・§9-7 はいずれも §5.1 の必須テスト（1・2・3・5・8）で検証できる。**
  → **★「(b) を投入すること」は求めていない**——**製造は値を持っていない**（指示書 §4.2）。
  → **★「新たに解決した入力 (c) が N 件以上であること」は求めていない**——**実測の結果 (c) が小さいこともありうる。** **本サブの成果は「測って逆算した」ことであって、件数の大きさではない。**

---

*以上、M22-07 レビューチェックリスト v1.0.0。配置 `docs/instructions/reviews/M22-07-review-checklist.md`。指示書 v1.1.0 と対。**実装の関数そのもので測ったこと**・**3 数値が固定されていること**・**衝突をキャラ単位で数えたこと**・**`DES-004` §5 に diff が無いこと**を最重要ゲートとする。*
