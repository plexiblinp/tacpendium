# M35-01 レビューチェックリスト（タグまわりの是正 3 件）

| 項目 | 内容 |
|------|------|
| 文書ID | M35-01-REVIEW-CHECKLIST |
| バージョン | **1.0.0** |
| 作成日 | 2026-09-08 |
| 対象指示書 | `docs/instructions/M35-01-tag-fixes.md` **v1.0.0** |
| 発行 | 設計卓（`D-785`） |
| 使い方 | `/review_plan` の入力。**★本書は指示書の完了条件を写したものではない。⇒ 「緑でも壊れているもの」を探すための束である** |

---

## 0. ★★レビュー担当への前置き

**★★本サブは「今は壊れていないもの」を直す。⇒ そこが他のサブと違う。**

| 段 | 直す前の状態 | レビューの目 |
|---|---|---|
| **段 1**（スコープ試験） | **実装は正しい可能性が高いが、誰も確かめていない** | **★試験が「何も見ていない形」になっていないか** |
| **段 2**（DTO の camelCase 化） | **★★両側が揃っているので壊れていない** | **★★片側だけ直していないか。⇒ 直す手番でそれをやると本末転倒である** |
| **段 3**（未設定へ戻せない） | **実装を 1 行も変えない** | **★報告に機序が書かれているか。⇒ 書かないと消える** |

**★★段 3 はコミットを持たない。⇒ 「差分が無い＝何もしていない」と読まないこと。**

---

## 1. 束 A — 段 1（**★★破壊確認が本体**）

| # | 見ること | 落ちる形 |
|---|---|---|
| **★★A-1** | **試験が「他人のタグを消そうとして弾かれること」を主張しているか** | **★★「自分のタグを消せる」ことを確かめても意味が無い。⇒ 向きを間違えた試験は、実装が壊れていても緑になる** |
| **★★A-2** | **破壊確認が実測で示されているか。⇒ 実装から `AND user_id = ?` を外して赤くなること** | **★★外して緑のままなら、その試験は何も見ていない。⇒ 実測が無ければ、そこで 1 件** |
| **★A-3** | **`internal/api/tag/scope_test.go:128` の `TestUpdate_CannotTouchAnotherUsersTag` と同じ形になっているか** | **形が違うと、次に読む人が 2 つの流儀を比べることになる** |
| **★★A-4** | **実装を変えていないか。⇒ 変えたなら、それ自体が報告に挙がっているか** | **★★実装を変えたということは「実装が絞っていなかった」ということである。⇒ 黙って直すと、重大な事実が試験の陰に隠れる** |

---

## 2. 束 B — 段 2（**★★本サブ最大の危険**）

| # | 見ること | 落ちる形 |
|---|---|---|
| **★★B-1** | **4 か所が 1 コミットで直っているか**〔`internal/api/tag/handler.go:163-164` ／ `web/src/types/tag.ts:23-25` ／ `web/src/features/tag/api/tagApi.ts:23` ／ `existing_tag_id` の削除〕 | **★★分けると、その間の状態が壊れている。⇒ 本件が登録された理由そのものである** |
| **★★B-2** | **4 か所以外に参照が無いことを走査で示しているか** | **★★「3 ファイルだと聞いた」では足りない。⇒ 実測で 0 件を示すこと。★1 件残ると、そこだけ `undefined` になる** |
| **★★B-3** | **`usageCount` が画面まで届くことを主張する試験が在るか** | **★★型検査は歯止めにならない**（両側を同時に変えれば型は通る）**。⇒ 値が読めることを見る試験でなければ、B-1 が守られたことを証明できない** |
| **★B-4** | **`existing_tag_id` が消えているか。⇒ 代わりを新設していないか** | **誰も使っていないので消すだけである**（指示書 §3-5） |
| **★★B-5** | **`force_delete_query` も直っているか** | **★`usage_count` だけ直して満足するのが最も起きやすい形である。⇒ 2 つある** |

---

## 3. 束 C — 段 3（**★★差分が無いので見落としやすい**）

| # | 見ること | 落ちる形 |
|---|---|---|
| **★★C-1** | **機序 3 段が報告に書かれているか**〔`\|\| undefined` → `JSON.stringify` がキーを落とす → `*string` の nil が「変更なし」〕 | **★書かないと消える。⇒ そして次の担当が「バグだ」と読んで直しに行く。★それが登録理由である** |
| **★★C-2** | **「`null` を送っても救えない」理由が書かれているか**（Go の `encoding/json` は省略と明示的 null を区別できない） | **★これが無いと、次の担当が「`null` を送れば済む」と読む** |
| **★C-3** | **設計伝達レポート §4 に CHANGE の請求が書かれているか**（指示書 §6-9） | **★★請求が無いと、設計書に「消せない」が書かれないまま終わる。⇒ 本サブの目的が未達になる** |
| **★C-4** | **`TagManagementPage.tsx:53-54` `:61-62` の `?? undefined` が no-op であることを確かめているか** | **★消すかコメントを付けるかは判断してよいが、「見ていない」は不可** |

---

## 4. 束 D — 射程（**★越えていないか**）

| # | 見ること |
|---|---|
| **★★D-1** | **`category` / `color` を未設定へ戻せるようにしていないか**（指示書 §3-2）**。⇒ 開発者が「いらない」と判断した**（2026-09-08） |
| **★D-2** | **タグの画面・文言を変えていないか**（指示書 §3-1） |
| **★D-3** | **`CLAUDE.md` を書き換えていないか**（規約の側は既に正しい） |
| **★D-4** | **`docs/design/` を編集していないか**（`CLAUDE.md` §8） |
| **★★D-5** | **同乗候補**（`tag-val-t01-check-then-act-race`）**に手を出したなら、段 1〜3 が終わってから別コミットになっているか** | **★必須ではない。⇒ 先にやっていたら順序違反である** |

---

## 5. 束 E — 検査と報告

| # | 見ること | 落ちる形 |
|---|---|---|
| **★★E-1** | **常設検査を回した時点以降にファイルを足していないか。⇒ 最後に回しているか**（指示書 §5-7） | **★先例＝`M30-01` が回した後にテストを足して「違反なし」と報告した**（`M-145` の隣） |
| **★★E-2** | **完了報告に「まだやっていないこと」を断定で書いていないか** | **★先例＝`M30-01` が `progress-log` への索引行を「追記した」と書きながら未追記だった**（`D-510`） |
| **★E-3** | **`bash scripts/check-md-emphasis.sh <完了報告>` を通しているか**（`D-775`） | **`docs/progress/` は常時走査の対象外である** |
| **★E-4** | **コミットが 3 つ以下に分かれているか**（段 1 ／ 段 2 ／ 同乗分） | **段 3 はコミットを持たない。⇒ 4 つ在ったら、何が混ざったかを見る** |

---

## 6. ★★「緑でも壊れているもの」を探す観点（**本書の値打ち**）

| # | 観点 |
|---|---|
| **★★1** | **★段 2 のテストを、DTO と同じ手番で書き換えていないか。⇒ 両方を `usageCount` に揃えれば、値が届いていなくても型は通る。★「画面まで届く」を見る試験でなければ意味が無い** |
| **★★2** | **★段 1 の試験は、実装が正しいので必ず緑になる。⇒ 緑であることは何の証明でもない。★証明になるのは破壊確認だけである** |
| **★★3** | **★段 3 は差分がゼロである。⇒ レビューは「何もしていない」と読みがちだが、成果物は報告の文章そのものである。★§3 を報告本文で確かめること** |
| **★★4** | **★`force_delete_query` の取りこぼし。⇒ `usage_count` は目立つが、もう 1 つある。★両方の走査結果を見ること** |

---

## 7. 参照

| パス | 何を見るか |
|------|-----------|
| `docs/instructions/M35-01-tag-fixes.md` | 指示書 v1.0.0（**★§4 の危険 4 件を先に読む**） |
| `docs/handover/followup-backlog.md` | **§CC の 3 行 ＋ `tag-val-t01-check-then-act-race`** |
| `CLAUDE.md` | **§4**（JSON タグの規約） |
| `docs/process/parallel-board.md` | **`D-579`** ／ **`D-593`**（同型の check-then-act の先例）／ **`M-145`** ／ **`D-510`** |

---

*以上、M35-01 レビューチェックリスト **v1.0.0**。* **★★本書の中心は束 B と観点 2 である。** **★段 2 は「今は壊れていないもの」を直す。⇒ 直す手番で片側だけ直すと、本末転倒になる。** **★段 1 は必ず緑になる。⇒ 証明になるのは破壊確認だけである。**
