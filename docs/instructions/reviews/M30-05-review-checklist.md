# M30-05 レビューチェックリスト（ファミリー行の表示名に残る強度語）

| 項目 | 内容 |
|------|------|
| 文書ID | M30-05-REVIEW-CHECKLIST |
| バージョン | **1.0.0**（2026-09-12・発行） |
| 対象指示書 | `docs/instructions/M30-05-family-label-strength-word.md` **v1.1.0** |
| 作成者 | 設計担当 Claude（設計卓） |
| 位置づけ | **レビュー担当の主入力**（`D-736`＝チェックリストは着手承認の手番で設計卓が起こす） |

---

## 0. ★★レビュー担当への前置き

**本サブは「表示名を直す」だけに見えて、実は*実査サブ*である。⇒ 段 1（全数を起こして 2 群に分ける）が本体であり、実装はその帰結にすぎない。**

**★★★最も重い不合格は「(A) 群が落ちた」だけを見て通すことである。⇒ 落としてはいけない (B) 群**（派生元の強度）**が残っていることを、同じ強さで確かめること。**

**★★数字はすべて「実測か算出か」を見ること**（計測点 `M-157`）**。⇒ `M30-03` の 49 行は写しであり、本サブは数え直すはずである。**

---

## 1. 束 A — ★★★並列上の制約（**最初に見ること**）

| # | 観点 |
|---|---|
| **A-1** | **★★★`M30-04` が着地した後のコードに載っているか。** **⇒ 本サブと `M30-04` は同じ `web/src/features/combo/inputResolution.ts` を触る。★`M30-04` 未マージの上に載せると衝突する** |
| **A-2** | **★★`M30-04` が末尾へ回した行**（`is_derived = true` のファミリー）**の並びが変わっていないか**（指示書 §2.3-5）**。⇒ 表示名を直すだけであり、並びに触れてはならない** |
| **A-3** | **★★`web/src/features/combo/moveSurfacing.ts` に差分が無いか。** **⇒ `M31-06` が同ファイルを持っている。★本サブの射程外である** |
| **A-4** | **★★共有テスト `moveSurfacing.roster.test.ts` を触ったなら、何をどう変えたかが報告に在るか。** **⇒ `M31-06` も同テストを触る見込みである。★触った事実が報告に無いと、マージ時に黙って片方が消える** |

---

## 2. 束 B — ★★★段 1 の実査が信用できるか（**本サブの本体**）

| # | 観点 |
|---|---|
| **B-1** | **★★★全数を実物で数え直しているか。** **⇒ `M30-03` の「49 行」をそのまま使っていたら不合格である**（指示書 §2.1-1。**★写しを実測と呼ばない**＝計測点 `M-157`） |
| **B-2** | **★★★(A) 群**（その技自身の強度＝落とすのが正しい）**と (B) 群**（派生元の強度＝落とすと壊れる）**の件数が両方出ているか。⇒ 合計が全数と一致するか** |
| **B-3** | **★★「判別できない」行を、判別できないと書いているか。⇒ 埋めていないか**（指示書 §2.1-3）**。★ここを推測で埋めると方式の選択そのものが誤る** |
| **B-4** | **★数値の隣に「実測」「算出」が書き分けられているか**（計測点 `M-157`） |
| **B-5** | **★★(B) 群の判別根拠が示されているか。⇒ 「派生元の強度である」をどう判定したのかが読めるか。★判定の手続きが書かれていないなら、次の人が再現できない** |

---

## 3. 束 C — ★★方式の選択が根拠を持つか

| # | 観点 |
|---|---|
| **C-1** | **★★方式**（(a) 例外辞書 ／ (b) 位置規則 ／ (c) データ側 ／ (a)+(b) 混成）**の選定理由が、段 1 の実数に基づいているか**（指示書 §2.2）**。⇒ 「(B) が少数だから辞書」のように、数から導けているか** |
| **C-2** | **★★★案 (c) を採ったなら、そこで止めて設計卓へマイグレ番号を請求したか**（指示書 §2.2 の但し書き）**。⇒ 自採番していたら不合格である**（`D-293`） |
| **C-3** | **★★例外辞書を採ったなら、辞書に載っていない行が現れたときの既定が「落とさない」か**（指示書 §4.4）**。⇒ 既定が「落とす」だと (B) 型を静かに壊す。★壊すより残すほうがましである** |
| **C-4** | **★辞書を採ったなら、載る行数が報告に在るか**（指示書 §2.2 の但し書き） |

---

## 4. 束 D — ★★★壊していないか

| # | 観点 |
|---|---|
| **D-1** | **★★★(A) 群が落ちたことと、(B) 群が残ったことが、両方とも実測で示されているか**（指示書 §2.3-1）**。⇒ 片方だけでは足りない** |
| **D-2** | **★★★陽性対照が在るか＝入れる*前*に (A) 群の表示名へ強度語が在ったことを測っているか**（指示書 §2.3-2）**。⇒ 「無い」だけを測ると、元から無かった行と区別がつかない** |
| **D-3** | **★★陽性対照が実装と独立な軸から作られているか**（`M30-04` 教訓 §7-4）**。⇒ 実装の処理を読み直して再現した対照は、実装が間違っていても同じように間違えるため対照にならない** |
| **D-4** | **★★★ファミリーの識別子（`family`）が 1 件も変わっていないか**（指示書 §2.3-4）**。⇒ 変えると保存済みのコンボが壊れうる。★これが本サブで最も高い代償である** |
| **D-5** | **★ファミリー行の総数が 372 のまま変わっていないか**（指示書 §2.3-3） |

---

## 5. 束 E — ★検査

| # | 観点 |
|---|---|
| **E-1** | **`cd web && pnpm test` が緑か。★`moveSurfacing.roster.test.ts` が緑のままか**（`DES-005` §6.1＝**この 1 本を消さないこと**） |
| **E-2** | **`pnpm tsc --noEmit` / `pnpm lint` が緑か。★`eslint-disable` を足していないか** |
| **E-3** | **★★`make e2e` を*全数*回しているか**（`SUPP-001` §5.5.7 (i)）**。⇒ 表示名は E2E の期待値に入っている可能性が高い** |
| **E-4** | **`check-import-order.sh` がベースラインから増えていないか。★レーン内で下げていないか**（`D-388`。**★実行時メッセージが「下げろ」と言うが従わないこと**＝followup `import-order-baseline-message-contradicts-header`） |
| **E-5** | **`check-enum-sync.sh` が増えていないか**（辞書を定数で持つなら関係しうる） |
| **E-6** | **`check-artifact-integrity.sh` を 1 本目に回しているか** |

---

## 6. ★★見なくてよいもの

| # | 何を | なぜ |
|---|---|---|
| 1 | **並び順**（derived を末尾へ） | **`M30-04` の成果である。⇒ 本サブは変えない**（束 A-2 で不変を確認するだけ） |
| 2 | **`setup_only` の入力面からの除外** | **`M31-06` の射程** |
| 3 | **入力の可否の述語**（`isOnSpecialTab` / `isControllerSurfaced`） | **表示名は可否に関係しない** |
| 4 | **CHANGE の起票** | **★製造は自採番しない**（`D-293`）**。⇒ 請求は報告 §6 へ書く** |

---

*以上、M30-05 レビューチェックリスト **v1.0.0**。* **★★束 B が本体である。⇒ 実装より先に、段 1 の実査が信用できるかを見ること。** **★★もう 1 つは D-4 である**——**`family` が変わると保存済みのコンボが壊れる。⇒ 表示名の変更が識別子に漏れていないことを、実測で確かめること。**
