# M36-01 レビューチェックリスト（配布アーカイブの組み立てとチェックサム）

| 項目 | 内容 |
|------|------|
| 文書ID | M36-01-REVIEW-CHECKLIST |
| バージョン | **1.3.0**（2026-09-15・**★A-6b を足した**＝`D-888`〔`dist-readme.txt` を全文書き直していないか〕） ／ **1.2.0**（2026-09-15・**★★P-4 を是正した**＝`D-887`〔**`dist-readme.txt` は射程内である。⇒ 本項は広すぎた**〕**／ §Q を新設**〔macOS の名前の非対称〕） ／ **1.1.0**（2026-09-15・**★`M32-03` との並列に伴う 2 点を反映**＝`D-886`〔**`images/` の画像不在を不合格にしない** ／ **`manual/` の中身をファイル名・件数で固定していないか**〕） ／ **1.0.0**（2026-09-13・発行） |
| 対象指示書 | `docs/instructions/M36-01-release-archive-and-checksums.md` **v1.3.0** |
| 作成者 | 設計担当 Claude（設計卓） |
| 位置づけ | **レビュー担当の主入力**（`D-736`） |

---

## 0. ★★レビュー担当への前置き

**★★★本サブは「無いものを作る」サブである。⇒ 配布物を組む仕組みはこれまで 1 つも存在しなかった。**

**★★だから「動いた」で通しやすい。⇒ zip はできる。★問題は*中身が正しいか*である。**

| # | 本サブ固有の不合格 |
|---|---|
| **1** | **★★★破壊確認が無い**（中身を抜いても検査が落ちない） |
| **2** | **★★★ライセンス関係ファイルを勝手に入れた / 落とした** |
| **3** | **★★nightly のワークフローを書き換えた** |
| **4** | **★`manual/` が 3 OS のどれかに入っていない** |
| **5** | **★成果物をコミットした** |

---

## 1. 束 A — ★★★中身が正しいこと（**本サブの本体**）

| # | 観点 |
|---|---|
| **A-1** | **★★★破壊確認が在るか＝中身を 1 つ抜くと検査が落ちること。⇒ 3 点**〔`exe` / `README.txt` / `manual/`〕**それぞれで見たか** |
| **A-2** | **★★★落ちないなら、その検査は何も見ていない。⇒ 「検査を足した」と「検査が効いている」は別である** |
| **A-3** | **★★`manual/` が空でないことを実物で確かめたか。⇒ ディレクトリの存在だけで通していないか** |
| **A-4** | **★★`manual/images/` が入っているか。⇒ 本文だけ入れて画像を落としていないか** |
| **A-5** | **★★★3 OS すべてに `manual/` が入っているか**（`D-855`）**。⇒ 非公式対応**〔macOS / Linux〕**で落としていないか** |
| **A-6** | **★`README.txt` の文面が生成物と一致するか。⇒ 「本 zip の中身は 2 点だけ」のままになっていないか** |
| **★★★A-6b** | **`dist-readme.txt` を*全文書き直していない*か**（**★★2026-09-15 追加＝`D-888`**）**。⇒ `M32-03` が既に同ファイルを変更している。★全文を書き直すと向こうが足した行が黙って消える。** **★★消えても `check-dist-readme.sh` は緑になる**〔同検査が見るのは*正本と生成物の一致*であって*内容の充足*ではない〕**。⇒ 機械では捕まらない。★`git diff` で*足した行だけ*になっているかを目で見ること** |
| **A-7** | **★`exe` 名が `tacpendium-windows-amd64.exe` か**（`CHANGE-191`）**。⇒ 食い違いを勝手に直していないか** |

---

## 2. 束 B — ★★★ライセンス関係ファイル（**判断を上げたか**）

| # | 観点 |
|---|---|
| **B-1** | **★★★勝手に入れていないか。⇒ 既定は「現状のまま入れない」である** |
| **B-2** | **★★★勝手に落としてもいないか。⇒ どちらも実装が下してよい判断ではない** |
| **B-3** | **★★段 1-5 で数えた全数が報告に在るか**（`REUSE.toml` が指すもの） |
| **B-4** | **★★同梱する / しない を*選べる形*になっているか。⇒ ファイル一覧が 1 か所にあり、足すのが 1 行で済むか** |
| **B-5** | **★★★判断事項が設計伝達レポート §4 に上がっているか。⇒ 「後で考える」で流していないか** |

---

## 3. 束 C — ★★境界（**触っていないもの**）

| # | 観点 |
|---|---|
| **C-1** | **★★★nightly のワークフローが 1 バイトも変わっていないか**（`CHANGE-129`。**★契機が違う＝定時 / タグ**） |
| **C-2** | **★★CI からリリースを打つ経路を作っていないか。⇒ 射程 6 は `M36-02` である** |
| **C-3** | **★attestation / SBOM / タグ署名に手を出していないか**（射程 3〜5） |
| **C-4** | **★コード署名**（`B1`）**／ AV 申請**（`B2`）**に手を出していないか** |
| **C-5** | **★公開リポジトリの同期に手を出していないか**（`P-46` の射程） |
| **C-6** | **★成果物をコミットしていないか。⇒ `.gitignore` に入っているか** |
| **C-7** | **★★★`docs/handover/followup-backlog.md` を 1 文字も編集していないか**（`D-838`） |
| **C-8** | **★設計書本体を書き換えていないか**（`CLAUDE.md` §8） |

---

## 4. 束 D — ★実査

| # | 観点 |
|---|---|
| **D-1** | **★★段 1 の 5 点が報告に在るか** |
| **D-2** | **★★★3**〔リリースを打つ手順の不在〕**が*否定形の確認*として書かれているか**（`E-117`）**。⇒ 「無いはず」で済ませていないか** |
| **D-3** | **★`Makefile` の全ターゲットと `.github/workflows/` の全ワークフローが列挙されているか** |
| **D-4** | **★`docs/usermanual/` の実体**〔本文 ／ `images/` の枚数〕**が数えられているか** |
| **D-5** | **★数値の隣に「実測」「算出」が書き分けられているか**（計測点 `M-157`） |
| **D-6** | **★★着手前の版ゲート 5 点を通したか**（指示書 §0.7）**。⇒ とくに 3＝`M32-02` の着地** |

---

## 5. 束 E — ★チェックサムと形

| # | 観点 |
|---|---|
| **E-1** | **★SHA-256 が 3 OS ぶん出るか** |
| **E-2** | **★★実際に検証した記録が在るか。⇒ 出しただけで終わっていないか** |
| **E-3** | **★利用者が検証するコマンドが `README.txt` か説明書に 1 行在るか。⇒ 使い方が無いと意味が無い** |
| **E-4** | **★★組み立てが CI から呼べる形か**（指示書 §7-3）**。⇒ `M36-02` が呼ぶ** |
| **E-5** | **★コマンド 1 本で 3 OS ぶんが出るか。⇒ OS ごとに手順が割れていないか** |

---

## 6. 束 F — ★検査

| # | 観点 |
|---|---|
| **F-1** | **`go test ./...` ／ `pnpm test` ／ `make e2e`（全数）が緑か** |
| **F-2** | **`check-doc-refs.sh` ／ `check-migration-license.sh` が緑か** |
| **F-3** | **★`docs/progress/progress-log.md` へ 1 行追記したか**（`CLAUDE.md` §8） |
| **F-4** | **★CHANGE 原稿が設計伝達レポート §4 に在るか。⇒ 自採番していないか**（`D-293`） |

---

## 7. ★★見なくてよいもの

| # | 何を | なぜ |
|---|---|---|
| 1 | **来歴が検証可能か** | **★射程 3・`M36-02` である** |
| 2 | **リリースが CI から出るか** | **★射程 6・`M36-02` である** |
| 3 | **ライセンス関係ファイルを入れるべきか** | **★★開発者の判断である。⇒ *選べる形になっているか*と*上げたか*だけを見る** |
| 4 | **実際にリリースが打たれたか** | **★開発者の手番** |
| 5 | **CHANGE の起票** | **★製造は自採番しない**（`D-293`） |

---

## 8. ★★対照値（**⇒ 数え直させる**）

| 対象 | 値 |
|---|---:|
| **配布物の OS**（Windows zip ／ macOS tar.gz ／ Linux tar.gz） | **3** |
| **1 アーカイブの中身**（`exe` ／ `README.txt` ／ `manual/`） | **3 点** |
| **★nightly の現在の中身**（`exe` ／ `README.txt`。**⇒ 本サブが 3 点にする対象ではない**） | **2 点** |
| **★zip に入っていないライセンス関係ファイル**（`LICENSE` / `DATA-LICENSE.md` / `NOTICE` / `REUSE.toml`） | **4** |
| 本サブの射程（射程 1 ＋ 2） | 2 件 |
| `M36` の射程の全数 | 6 件 |

**★★★「nightly の 2 点」を「配布物の仕様」と読まないこと。⇒ 仕様は `DES-002` §11.2 の 3 点である。★nightly は配布物ではない**（`CHANGE-129`）**。**


## ★★【2026-09-15 追加＝`D-886`】`M32-03` との並列

| # | 見ること |
|---|---|
| **★★★P-1** | **`manual/` の中身をファイル名の一覧やファイル数で固定していないか。⇒ `M32-03` が同ディレクトリへ新しいファイルを 1 本足す**〔クイックスタート〕**ため、固定していると着地で赤になる。★検査は*あるべきものが在るか*で見ること** |
| **★★P-2** | **破壊確認**（§5-3）**で抜くものが「あるべきもの」になっているか。⇒ 数を 1 つ減らして落ちる形にしていないか** |
| **★P-3** | **`images/` に画像ファイルが無いことを不合格にしていないか。⇒ 撮影は開発者の手番であり本サブの後である**（版ゲート 3b） |
| **★★P-4** | **★★★2026-09-15 是正＝本項は広すぎた**（計測点 `M-186`）**。⇒ 触ってはならないのは*説明書の本文と画像*であって、`docs/usermanual/` 全体ではない。** **★編集してよい＝`docs/usermanual/dist-readme.txt`**〔`README.txt` の正本。**A-6 が修正を要求しており本サブの射程である**〕**。** **★編集してはならない＝`tacpendium-readme.html`**〔説明書の本文〕**／ `images/`**〔撮影待ち〕**／ クイックスタート**〔`M32-03` が作る〕**。⇒ これらは `M32-03` の面である** |


## ★★【2026-09-15 追加＝`D-887`】macOS の名前の非対称

| # | 見ること |
|---|---|
| **★★★Q-1** | **アーカイブ名が `tacpendium-macos-arm64.tar.gz` になっているか**（`DES-002` §11.2 のとおり）**。⇒ 勝手に `darwin` へ揃えていないか** |
| **★★Q-2** | **中のバイナリ名が `tacpendium-darwin-arm64` のままか**（`Makefile` が出す名前）**。⇒ 勝手に `macos` へ改名していないか。★版ゲート #4 の「勝手に改名しない」は macOS 側にも効く** |
| **★★★Q-3** | **非対称が残ることを完了報告へ書いているか。⇒ 実測で書くこと**〔アーカイブ名 ／ 中のバイナリ名 ／ `README.txt` の記載〕**。★本サブは非対称を*作った*のではなく*可視化した*。⇒ 黙って揃えると、設計卓が判断する機会が消える** |
| **★Q-4** | **CHANGE 請求として設計伝達レポートへ出しているか。⇒ 自採番していないか**（`D-293`） |

---

*以上、M36-01 レビューチェックリスト **v1.3.0**。* **★★★束 A-1 を最初に見ること**——**破壊確認が無ければ、zip ができたことしか分かっていない。** **★★次に重いのは B-1 / B-2 である**——**ライセンスは、入れるのも落とすのも実装の判断ではない。**
