# M31-03 レビューチェックリスト（一覧の列の並び ＋ 詳細への行内リンクの位置）

| 項目 | 内容 |
|------|------|
| 文書ID | M31-03-REVIEW-CHECKLIST |
| バージョン | **1.0.0** |
| 作成日 | 2026-09-08 |
| 対象指示書 | `docs/instructions/M31-03-combo-list-columns-and-row-link.md` **v1.1.0** |
| 発行 | 設計卓（`D-783`） |
| 使い方 | `/review_plan` の入力。**★本書は指示書の完了条件を写したものではない。⇒ 「緑でも壊れているもの」を探すための束である** |

---

## 0. ★★レビュー担当への前置き

**★★本サブは 2 件を 1 本の指示書で扱うが、性質がまったく違う。⇒ 同じ目で見ないこと。**

| 件 | 性質 | レビューの目 |
|---|---|---|
| **行内リンクの位置** | **明確な是正**（元々は始動状況に在ったものがヒット種別へ移っている） | **「戻したこと」ではなく「2 か所から張られていないこと」を見る** |
| **列の並び** | **案が無い**（開発者の逐語＝「まだ案はありません」） | **「並びが良いか」は見ない。⇒ 見るのは保存済みの設定への当たり方である** |

**★★コミットが 2 件に分かれていること自体が完了条件である**（指示書 §6-9）**。⇒ 分かれていなければ、そこで 1 件。**

---

## 1. 束 A — 段 1 の実査（**★数と、見つからなかったことの区別**）

| # | 見ること | 落ちる形 |
|---|---|---|
| **★★A-1** | **実査 5 件に結果があるか。★1 と 2 は数で、3 は「見つかった / 見つからなかった」で書かれているか** | **「確認した」だけで数が無い。⇒ 数が無いと、次の担当が数え直すことになる** |
| **★★A-2** | **行内リンクがどの手番で移ったかが実測で示されているか** | **★★これが本サブ最大の落とし穴である**（指示書 §4.2）**。⇒ 「元々こうだった」を確かめずに戻すと、意図した移動を退行させる** |
| **★A-3** | **「意図した移動だった」と判明した場合、止めているか** | **止めずに戻していたら、そこで 1 件** |
| **★★A-4** | **リンクが 2 列から張られていないことを、走査で示しているか** | **「片方を消した」だけでは、もう片方が残っていないことを示せない**（指示書 §5-2 の逐語）**。⇒ 目視の報告は不可** |

---

## 2. 束 B — 保存済みの列設定（**★★本サブで唯一の重い面**）

| # | 見ること | 落ちる形 |
|---|---|---|
| **★★B-1** | **`combo-list-columns-v1` の扱いの案が添えられているか**（指示書 §6-4） | **無ければ、利用者の手元で見た目が変わるか変わらないかが黙って起きる** |
| **★★B-2** | **破壊確認＝保存済みの列設定を持つ状態を作り、選ばれた案どおりの挙動になることを実測しているか** | **★★「新規の利用者では正しい」だけでは通らない。⇒ 保存済みの状態こそが対象である** |
| **★★B-3** | **キーをバージョンアップした場合、`web/CLAUDE.md` §1 の台帳へ足し `check-browser-storage-keys.sh` が緑か** | **台帳に無いキーを使うのは `CLAUDE.md` §10.X 違反である。⇒ 検査が拾う** |
| **★B-4** | **旧キーの後始末が決まっているか**（消すのか、残すのか） | **決めずに新キーを足すと、古い値が永久に残る** |

---

## 3. 束 C — 波及（**★偏りを見る**）

| # | 見ること | 落ちる形 |
|---|---|---|
| **★★C-1** | **破壊確認で、段 1-2 の区分から 1 件ずつ選んでいるか。★`§5.19b` の専用画面を含めているか**（指示書 §5-3） | **★★「一覧を 1 面だけ見た」で終わっているのが最も多い形である。⇒ 共通部品であり波及先は一覧を出す全画面である** |
| **★C-2** | **キーボードで詳細へ到達できることを実測しているか** | **リンクを別の列へ移すと、順送りの停止点が変わりうる** |
| **★C-3** | **`M31-02` が入れた `text-drag-capture` と衝突していないか** | **同じ共通部品の族であり直列にした理由がここにある**（`D-774`） |

---

## 4. 束 D — 射程（**★越えていないか**）

| # | 見ること |
|---|---|
| **★D-1** | **指示書 §3「やらないこと」を越えていないか。⇒ 越えているなら、指摘してから越えているか**（`D-782` の一般形＝**開発者の後発の指示が上位だが、黙って越えるのは別である**） |
| **★★D-2** | **列の並びを、開発者の回答を得ずに入れていないか**（指示書 §6-5）**。⇒ 段 4 は回答が前提である** |
| **★D-3** | **`ComboTable` / `ComboTableRow` 以外へ差分が漏れていないか** |

---

## 5. 束 E — 検査と報告

| # | 見ること | 落ちる形 |
|---|---|---|
| **★★E-1** | **検査の結果を「出力で」判定しているか。⇒ 回した時点以降にファイルを足していないか** | **★先例＝`M30-01` が `check-import-order.sh` を「違反なし」と報告したが、回したのは新規テストを作る前だった**（`M-145` の隣。**教訓 3**）**。⇒ 最後にもう一度回すこと** |
| **★★E-2** | **完了報告に「まだやっていないこと」を断定で書いていないか** | **★先例＝`M30-01` が `progress-log` への索引行を「追記した」と書きながら未追記だった**（`D-510` の一般形）**。⇒ レビューは実走して確かめること** |
| **★E-3** | **`bash scripts/check-md-emphasis.sh <完了報告>` を通しているか**（`CLAUDE.md` §8） | **★`docs/progress/` は常時走査の対象外である。⇒ 書いた本人が見る形になった**（`D-775`） |
| **★E-4** | **設計伝達レポート §4 に `DES-005` §5.4 への CHANGE の請求が書かれているか**（指示書 §6-11） | **★★列の並びは `DES-005` §5.4 の契約である。⇒ 請求が無いと設計書だけが古くなる** |

---

## 6. ★★「緑でも壊れている」を探す観点（**本書の値打ち**）

| # | 観点 |
|---|---|
| **★★1** | **★リンクを「移した」のか「増やした」のか。⇒ テストが「始動状況から飛べる」だけを見ていると、ヒット種別にも残っている状態が緑になる。★走査で 0 件を示させること** |
| **★★2** | **★保存済みの設定を持つ利用者は、テストの既定値では再現しない。⇒ `localStorage` を実際に汚してから見る試験でなければ、B-2 は満たさない** |
| **★★3** | **★列の並びの案が「現状と並べて」示されているか**（指示書 §6-3）**。⇒ 案だけ出して現状を書かないと、開発者が比較できない** |
| **★★4** | **★実測の母集団を疑う**（`M-145` の一般形）**。⇒ 「一覧の列」を数えるとき、どの画面のどの定義を数えたかが書かれているか。★列の定義が 1 か所とは限らない** |

---

## 7. 参照

| パス | 何を見るか |
|------|-----------|
| `docs/instructions/M31-03-combo-list-columns-and-row-link.md` | 指示書 v1.1.0（**★§4 の危険 4 件を先に読む**） |
| `docs/design/05-screen-design.md` | **§5.4**（一覧の列）／ **§5.19b**（専用画面） |
| `web/CLAUDE.md` §1 | **ブラウザストレージの台帳**（`combo-list-columns-v1`） |
| `docs/process/parallel-board.md` | **`D-774`**（本サブの起票と直列の理由）／ **`M-145`**（実測の母集団）／ **`D-510`**（未実施を断定で書かない） |

---

*以上、M31-03 レビューチェックリスト **v1.0.0**。* **★★本書の中心は束 B である。⇒ 列の並びは「良し悪し」ではなく「既に保存されている設定への当たり方」で見る。** **★もう 1 つは束 A-2 である**——**「元々こうだった」を確かめずに戻すと、意図した移動を退行させる。**
