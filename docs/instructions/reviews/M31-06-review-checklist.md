# M31-06 レビューチェックリスト（`setup_only` を入力面 2 面から外す）

| 項目 | 内容 |
|------|------|
| 文書ID | M31-06-REVIEW-CHECKLIST |
| バージョン | **1.0.0**（2026-09-12・発行） |
| 対象指示書 | `docs/instructions/M31-06-setup-only-input-exclusion.md` **v1.2.0** |
| 作成者 | 設計担当 Claude（設計卓） |
| 位置づけ | **レビュー担当の主入力**（`D-736`＝チェックリストは着手承認の手番で設計卓が起こす） |

---

## 0. ★★レビュー担当への前置き

**本サブは「外す」サブである。⇒ *外れたこと* より *外れすぎていないこと* のほうが見落とされやすい。**

**★★★セットプレイのレシピ入力は、コンボ登録と同じ部品を使っている。⇒ 部品の中で落とすと、セットプレイからも消える。★これが本サブ最大の事故の形である。**

**★★機構は 1 本である**（`isInputExcluded()` を拡張する＝`D-810`）**。⇒ 2 本目を作っていたら、それだけで不合格である。**

---

## 1. 束 A — ★★★並列上の制約（**最初に見ること**）

| # | 観点 |
|---|---|
| **A-1** | **★★★`web/src/features/combo/inputResolution.ts` に差分が無いか。** **⇒ `M30-05` が同ファイルを持っている。★本サブの射程外である** |
| **A-2** | **★★共有テスト `moveSurfacing.roster.test.ts` を触ったなら、何をどう変えたかが報告に在るか。** **⇒ 本サブは未分類の件数を動かすので、ほぼ確実に触る。★`M30-05` も同テストを触る見込みであり、触った事実が報告に無いとマージ時に黙って片方が消える** |
| **A-3** | **★`internal/repository/move/divergence_test.go` 以外の Go 側に差分が無いか**（DTO の同期が要らなかった場合。束 C-1 参照） |

---

## 2. 束 B — ★★★機構を 2 本作っていないか（**`D-810` の核心**）

| # | 観点 |
|---|---|
| **B-1** | **★★★`isInputExcluded()` を*拡張*しているか。新しい関数・新しい定数を作っていないか**（指示書 §0.4 / §2.2-1）**。⇒ `M31-04` が作った判定の入口は 1 本であり、本サブはそこへ理由を足すだけである** |
| **B-2** | **★理由**（静的な code 列挙 ／ `setup_only`）**が判定の中で分かれているか。⇒ 「なぜ外れたか」が呼び出し側から読めるか** |
| **B-3** | **★★`INPUT_EXCLUDED_MOVE_CODES`**（`drive_reversal`）**の既存の振る舞いが 1 つも変わっていないか** |
| **B-4** | **★★`omittedCount` の数え方を変えていないか**（指示書 §2.1-4）**。⇒ `M31-04` は除外分を数えない形にした。★揃っているか** |

---

## 3. 束 C — ★★★外れすぎていないか（**本サブ最大の事故の形**）

| # | 観点 |
|---|---|
| **C-1** | **★★★セットプレイのレシピ入力から `setup_only` が 1 件も減っていないか**（指示書 §2.2-3）**。⇒ 実測で示されているか。★「減らないはず」では足りない** |
| **C-2** | **★★セットプレイ側が減らないことを、どう*保証*しているか**（指示書 §2.1-2）**。⇒ テストで床が張られているか。★次に誰かが `isInputExcluded()` を触ったとき、セットプレイが静かに壊れないか** |
| **C-3** | **★★★`drive_reversal` がセットプレイ側に出るようになっていないか**（指示書 §2.1-3）**。⇒ あちらは両面で外れているのが正しい。★本サブが「面」を導入したせいで漏れ出す形が在りうる** |
| **C-4** | **★★★コンボ一覧・コンボ詳細・比較・既存レシピの表示と再編集を 1 か所も触っていないか**（指示書 §2.2-4）**。⇒ 既に保存されたレシピに `setup_only` が入っていたら、それは表示も再編集もできなければならない** |
| **C-5** | **★`setup_only` の*保存*を禁止していないか**（指示書 §2.2-6）**。⇒ 物理入力・取込・既存レシピは通ること** |
| **C-6** | **★引っ越し取込は対象外である**（`DES-005` §6.1・2026-09-10 開発者判断）**。⇒ ここを塞いでいたら射程外である** |

---

## 4. 束 D — ★★外れているか

| # | 観点 |
|---|---|
| **D-1** | **★★仮想コントローラの 8 タブから外れているか。★★未分類タブにも出ていないか**（指示書 §2.2-1）**。⇒ `isControllerSurfaced` を false にするだけでは未分類に残る。★`M31-04` が `unclassified` 分岐へ効かせた経路に乗っているか** |
| **D-2** | **★★全技一覧プルダウンから、省略トグルの ON / OFF *どちらでも* 外れているか**（指示書 §2.2-2） |
| **D-3** | **★★陽性対照が在るか＝入れる*前*に `setup_only` が入力面に出ていたことを測っているか。⇒ 「無い」だけを測ると、元から無かったのと区別がつかない** |
| **D-4** | **★対象件数が実測で出ているか。★「実測」「算出」が書き分けられているか**（計測点 `M-157`） |

---

## 5. 束 E — ★DTO と検査

| # | 観点 |
|---|---|
| **E-1** | **★★`setupOnly` は既に `GET /api/moves` の応答に出ている**（**`M30-04` の実 DTO ダンプで確認済み＝`"setupOnly": false` が実在する**）**。⇒ バックエンド側の同期追加は要らないはずである。★足していたら、なぜ要ったのかの説明が在るか**（指示書 §2.2-5 は「出ていなければ」の条件付きである） |
| **E-2** | **★TS 側の `Move` に `setupOnly` が在るか。無ければ足したか。★足したなら `ComboSummary` / `ComboDetail` / `Combo` の 3 分岐に漏れが無いか**（`web/CLAUDE.md` §2） |
| **E-3** | **`cd web && pnpm test` / `pnpm tsc --noEmit` / `pnpm lint` が緑か** |
| **E-4** | **★★`make e2e` を*全数*回しているか**（`SUPP-001` §5.5.7 (i)）**。⇒ 入力面の母集団が変わるので、件数を固定した spec と「最後の技を選ぶ」spec に効く。★的撃ちでは足りない** |
| **E-5** | **`check-import-order.sh` がベースラインから増えていないか。★レーン内で下げていないか**（`D-388`） |
| **E-6** | **`check-enum-sync.sh` / `check-artifact-integrity.sh`** |

---

## 6. ★★見なくてよいもの

| # | 何を | なぜ |
|---|---|---|
| 1 | **ファミリー行の表示名** | **`M30-05` の射程** |
| 2 | **ファミリー行の並び順** | **`M30-04` の成果。⇒ 本サブは触らない** |
| 3 | **`custom_states` の `setup_only` との関係** | **★別物である。⇒ 本サブは `moves.setup_only` だけを見る** |
| 4 | **CHANGE の起票** | **★製造は自採番しない**（`D-293`）**。⇒ 請求は報告 §6 へ** |

---

*以上、M31-06 レビューチェックリスト **v1.0.0**。* **★★束 C が本体である。⇒ 外れたことより、外れすぎていないことを見ること。** **★★もう 1 つは B-1 である**——**`D-810` が投入前に潰した手戻りがまさにこれであり、機構が 2 本になったら本サブの値打ちは消える。**
