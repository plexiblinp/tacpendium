# M33-02 レビューチェックリスト（9 群の生成と同一性の検証）

| 項目 | 内容 |
|------|------|
| 文書ID | M33-02-REVIEW-CHECKLIST |
| バージョン | **1.0.0**（2026-09-13・発行） |
| 対象指示書 | `docs/instructions/M33-02-generate-and-verify-groups.md` **v1.2.0** |
| 作成者 | 設計担当 Claude（設計卓） |
| 位置づけ | **レビュー担当の主入力**（`D-736`） |

---

## 0. ★★レビュー担当への前置き

**★★★本サブは「失敗してよい」サブである。⇒ 一致しなかったことを正直に出すのが正しい完了形である。**

**★したがって「全一致しました」という報告こそ、最も強く疑うこと。**

| # | 本サブ固有の不合格 |
|---|---|
| **1** | **★★★差を合わせ込んで一致させた**（指示書 §0.1）**。⇒ 何が違ったのかが消える** |
| **2** | **★★層 A の群に `_data_` が付いている**。⇒ 機械検査は緑のまま `CC-BY-SA-4.0` へ移る。**★戻せない** |
| **3** | **★比較が実は何も見ていない**（破壊確認が無い） |
| **4** | **★`sqlite_sequence` を落としている** |

---

## 1. 束 A — ★★★同一性の検証（**本サブの本体**）

| # | 観点 |
|---|---|
| **A-1** | **★★★比較が 4 面すべてあるか**〔正規化スキーマ ／ 全行 ／ `sqlite_sequence` ／ 件数〕**。⇒ 1 面の一致で「全一致」と書いていないか** |
| **A-2** | **★★★正規化スキーマの比較が規約 (18) の取り方か**——`pragma table_info` / `foreign_key_list` / `index_list`。**★`sqlite_master.sql` のテキストで比較していないか** |
| **A-3** | **★★★ハッシュを「値だけ」で引き継いでいないか**（規約 (18)）**。⇒ `M33-01` §5.2.1 のスクリプト本体を使ったか。★独自の書式で算出して「食い違った」と報告していないか** |
| **A-4** | **★★★§5-4 の破壊確認があるか＝9 群のどれか 1 本から 1 行を落とすと比較が*落ちる*こと。⇒ 無ければ「比較が何も見ていない」可能性を排除できない** |
| **A-5** | **★★★差が出たなら、そのまま書かれているか。⇒ 合わせ込んでいないか**（指示書 §0.1）**。★「一致するまで直した」形跡が無いか** |
| **A-6** | **★旧最終状態と新 baseline が*独立に*構築されているか。⇒ 本番の `modernc.org/sqlite` ＋ `golang-migrate` で** |
| **A-7** | **★全 21 表の件数が表ごとに出ているか** |

---

## 2. 束 B — ★★★ライセンスの層

| # | 観点 |
|---|---|
| **B-1** | **★★★層 A の 4 群**（`S01` / `S02a` / `S06a` / `S07`）**に `_data_` が*付いていない*か。⇒ 付いていたら、`games` / `presets` / `users` / `tags` が `CC-BY-SA-4.0` へ移る** |
| **B-2** | **★★層 B の 5 群**（`S02b` / `S03` / `S04` / `S05` / `S06b`）**に `_data_` が付いているか** |
| **B-3** | **★★★§5-5 の破壊確認があるか＝層 A の 1 本に `_data_` を付けると `check-migration-license.sh` が*層 B へ解決する*こと。⇒ 検査がこの向きを見ないことを実測で確かめたか** |
| **B-4** | **★`check-migration-license.sh` の緑を*根拠にしていない*か**（指示書 §4.2）**。⇒ 表ごとの割当を目で確かめた記録が在るか** |
| **B-5** | **★`000018` / `000074` が層で分割されているか**——`000018` は DDL 部が `S01`・`raw_data` の最終値が `S03` ／ `000074` は `ALTER` が `S01`・`character_id` の値が `S06b` |

---

## 3. 束 C — ★★★`sqlite_sequence`

| # | 観点 |
|---|---|
| **C-1** | **★★★`characters` の `seq` が 34 で再現されているか**（行 31 に対して）**。⇒ `000009` を廃止した分の 3 が失われていないか** |
| **C-2** | **★★`combos` / `combo_oki_options` の `seq=0` の行が在るか。⇒ 「空の表は飛ばす」と実装していないか** |
| **C-3** | **★`preset_aliases` の差 179 が合っているか。⇒ `M33-01` の機序は*推定*である。★機序を前提にせず値で合わせたか** |
| **C-4** | **★`sqlite_sequence` の比較が §5-3 として独立に実測されているか** |

---

## 4. 束 D — ★★生成の中身

| # | 観点 |
|---|---|
| **D-1** | **★★★新系列の連番が `000001` から始まっているか**（指示書 §0.2）**。⇒ `000114` 等、既存の続きを取っていないか** |
| **D-2** | **★廃止 8 本が書かれていないか**〔`000009` / `000010` / `000011` / `000022` / `000028` / `000029` / `000081` / `000103`〕 |
| **D-3** | **★投入順が `M33-01` §4.3 のトポロジカル順か。⇒ 複合 FK `combo_setup_results` → `combo_setups` が後か** |
| **D-4** | **★自己参照 3 本が「NULL で入れてから UPDATE」の 2 パスか。⇒ 遅延制約を使っていないか** |
| **D-5** | **★`foreign_key_check` 0 件 ／ `integrity_check` が `ok`** |
| **D-6** | **★空の DB へ最初から適用して通るか**（`version` と `dirty=0`） |

---

## 5. 束 E — ★★越境と前提

| # | 観点 |
|---|---|
| **E-1** | **★★★並列 0 本で走ったか**（指示書 §0.3）**。⇒ 着手時の実測が報告に在るか** |
| **E-2** | **★★凍結点 `5576126` からの増分を数え、対応表へ追記したか。⇒ 増分 0 なら 0 と書いてあるか** |
| **E-3** | **★★`migrations/legacy/` を作っていないか ／ 歴史テストを移していないか**（`M33-03` の射程） |
| **E-4** | **★golden / seedgen / CSV 同期テストに手を出していないか**（同上） |
| **E-5** | **★`users` / `tags` の分け方を決めていないか**（`M33-03` の入力） |
| **E-6** | **★設計書本体を書き換えていないか**（`CLAUDE.md` §8） |
| **E-7** | **★★★`docs/handover/followup-backlog.md` を 1 文字も編集していないか**（`D-838`） |
| **E-8** | **★★着手前の版ゲート 6 点を通したか**（指示書 §0.5）**。⇒ とくに 2＝`M33-overview` が v2.6.0 以上か**（**★7 群のままなら古い**） |
| **E-9** | **★開発者の手元 DB に触れていないか** |

---

## 6. 束 F — ★報告の形

| # | 観点 |
|---|---|
| **F-1** | **★★★設計伝達レポート §4 の最初の 1 行に「全一致したか」が書かれているか**（指示書 §6-11）**。⇒ これが `M33-03` の可否を決める** |
| **F-2** | **★★`go test ./...` で落ちたものが*列挙*されているか。⇒ 歴史テストは落ちうる。★「隠さない」が要件である** |
| **F-3** | **★数値の隣に「実測」「算出」が書き分けられているか**（計測点 `M-157`） |
| **F-4** | **★`M33-03` へ渡す材料が更新されているか**（21 表の投入順 ／ `sqlite_sequence` の扱い ／ 数えるための式） |

---

## 7. ★★見なくてよいもの

| # | 何を | なぜ |
|---|---|---|
| 1 | **歴史テストが落ちること** | **★`M33-03` が移す。⇒ 本サブでは落ちうる。★ただし列挙は要る**（F-2） |
| 2 | **`users` / `tags` の seed 判別述語** | **★`followup` の `m33-users-tags-seed-vs-user-predicate-undefined`。⇒ `M33-03` の入力** |
| 3 | **`m.Down()` / `Migrate(0)` の保証範囲** | **★`followup` の `three-point-update-down-below-golden-version`** |
| 4 | **開発者の手元 DB の移行手順** | **★`M33-03` の射程** |
| 5 | **`SUPP-001` §2.7 の本数** | **★9 群になったら書き直す。⇒ 受理の手番で設計卓が行う** |
| 6 | **CHANGE の起票** | **★製造は自採番しない**（`D-293`） |

---

## 8. ★★対照値（**`M33-01` の実測。⇒ ずれたら報告させる**）

| 対象 | `M33-01` の実測（凍結点 `5576126`） |
|---|---|
| `*.up.sql` の本数 | **111 本**（`000001`〜`000112`・欠番 `000012` の 1 件） |
| アプリスキーマ | **42 オブジェクト**（表 21 ＋ 索引 21） |
| `schema_migrations` | `version=112` / `dirty=0` |
| `characters` | **行 31 / `seq` 34** |
| 廃止候補 | **8 本** |
| 層 A に解決する `GAME_TABLES` 書き込み | **`000018` / `000074` の 2 本**（凍結表の据置きで緑） |

---

*以上、M33-02 レビューチェックリスト **v1.1.0**。* **★★★束 A-5 を最初に見ること**——**「全一致しました」という報告は、合わせ込みの結果かもしれない。⇒ 差を出すのが本サブの正しい完了形である。** **★★次に重いのは B-1 / B-3 である**——**層 A に `_data_` が付いても機械検査は緑であり、そして戻せない。**
