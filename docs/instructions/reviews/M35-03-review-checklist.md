# M35-03 レビューチェックリスト（ラッシュ版 4 件の `original_move_code`）

| 項目 | 内容 |
|------|------|
| 文書ID | M35-03-REVIEW-CHECKLIST |
| バージョン | **1.0.0** |
| 作成日 | 2026-09-10 |
| 対象指示書 | `docs/instructions/M35-03-rush-original-move-code-typo.md` **v1.0.0** |
| 発行 | 設計卓（`D-816`。**★事後発行**——同サブは 2026-09-10 に投入済みであった） |
| 使い方 | `/review_plan` の入力。**★本書は指示書の完了条件を写したものではない。⇒ 「緑でも壊れているもの」を探すための束である** |

---

## 0. ★★レビュー担当への前置き

**★★★本サブ最大の危険は「`M35-02` と同じ」と読むことである。⇒ 壊れている列が違い**〔`ryu`＝`move_code` ／ 本件＝`original_move_code`〕**、`original_move_id` が NULL であり、入力面には出ている。★同じなのは「三点更新」という形だけである。**

---

## 1. 束 A — ★★★既存 DB が直ることの証拠

| # | 見ること | 落ちる形 |
|---|---|---|
| **★★★A-1** | **`moves.original_move_id` が 4 行とも非 NULL になることが、既存 DB の経路**（マイグレ）**と新規 DB の経路**（seed）**の両方で示されているか** | **★★★`original_move_code` を CSV で直しただけでは既存 DB の `original_move_id` は埋まらない。⇒ 新規 DB は seed で埋まるので、テストは緑のまま通りうる。★`M35-02` 教訓 §7-4 と同じ族である** |
| **★★★A-2** | **陽性対照＝`preset_aliases` の `numeric` / `srk` が、是正前は 4 件とも 0 行、是正後は 1 行ずつになることが測られているか** | **★★これが「別名が作れるようになった」唯一の証拠である。⇒ 「1 行ある」だけでは、元からあったのか分からない** |
| **★★A-3** | **対照 `rush_power_stomps_1hits`**（`zangief`）**が 1 行のまま変わっていないか** | **★変わっていたら、生成器の別の枝に触れている** |
| **★★A-4** | **セットプレイ自動提案の候補に 4 技が入ることが示されているか**（`internal/service/setplay/service.go` の `OriginalMoveID == nil` 分岐） | **★実害 1 件目である。⇒ 直っていないなら、本サブの目的の半分が未達である** |

---

## 2. 束 B — ★★★2 つの軸を混ぜていないか

| # | 見ること | 落ちる形 |
|---|---|---|
| **★★★B-1** | **軸 A**（`original_move_code` が dangling）**と 軸 B**（入力面に出ない）**を別々に数え、集合として突き合わせているか** | **★★★`M35-02` が 1 度混ぜた。⇒ 件数が 4 で一致したが、別々の 4 件だった。★件数の一致は同一性の証拠にならない** |
| **★★B-2** | **軸 B**（`alex` 2 / `jamie` 1 / `zangief` 1）**に手が入っていないか** | **★射程外である。⇒ `HitBoxLayout` から到達できないという別の族** |
| **★★B-3** | **`move_code` の差分が 0 であるか** | **★4 件とも正しい。⇒ 触っていたら是正の向きを取り違えている** |

---

## 3. 束 C — ★★★golden `000026` と規約 (16)

| # | 見ること | 落ちる形 |
|---|---|---|
| **★★★C-1** | **段 1 で「`000026` の版を終端にしている契約テスト」を `grep` した結果が示されているか**（`SUPP-001` §5.5.4 規約 (16) の手順） | **★★落ちてから気づくと、緩和なのか追随なのかを落ち着いて判断できない。★`CHANGE-178` が初めて実地に効く場面である** |
| **★★★C-2** | **該当テストが在って追随したなら、歯止め 4 つを守っているか**〔(a) その golden を再生成した手番でのみ ／ (b) 要素数を減らさず対のまま ／ (c) 先に赤を実測 ／ (d) 見出しコメントを役割で書き直す〕 | **★★(a) が最も重い。⇒ `000026` 以外の版のテストに触れていたら違反である** |
| **★★C-3** | **他 28 キャラの golden 差分が 0 バイトであるか** | **★動いていたら、CSV の 4 セル以外を触っている** |

---

## 4. 束 D — ★★マイグレの層と `preset_aliases` の `INSERT`

| # | 見ること | 落ちる形 |
|---|---|---|
| **★★★D-1** | **ファイル名が `000111_data_*.sql` か。★破壊確認**（`_data_` を外すと赤）**が実測されているか** | **★★★層 B**（`CC-BY-SA-4.0`）**である。⇒ `_data_` を入れないと `REUSE.toml` の既定**（層 A＝`AGPL`）**へ静かに落ちる。★誰も赤くならない** |
| **★★D-2** | **番号が `000111` か。⇒ 自採番していないか**（`D-293`） | **★`D-812` で払い出した番号である** |
| **★★★D-3** | **`preset_aliases` へ `INSERT` するとき `character_id` を落としていないか** | **★★★`000074` で追加された列である。⇒ NULL でも `INSERT` は通り alias は表示にも出るので、動作では気づけない。★`000075` の UNIQUE 索引 2 本の構成列でもある**（`M31-04` 教訓 §7-1 と同型） |
| **★★D-4** | **`down` に `preset_aliases` の `DELETE` が含まれているか** | **★`M35-02` は 1 対 1 置換だったので `down` も置換で済んだ。⇒ 本サブは行が増えるため `DELETE` が要る** |

---

## 5. 束 E — ★共通

| # | 見ること |
|---|---|
| **★★E-1** | **`internal/seedgen/` の差分が 0 か**（**★生成器は正しく振る舞っている**） |
| **★E-2** | **`bash scripts/check-import-order.sh` がベースラインどおりか**（`D-388`） |
| **★E-3** | **完了報告が `bash scripts/check-md-emphasis.sh <file>` を通っているか**（`D-775`） |
| **★E-4** | **`docs/progress/progress-log.md` へ索引行が 1 行足されているか**（`CLAUDE.md` §8） |
| **★★E-5** | **`rush-desert-storm-1hits-naming`**（`lily` の `name_ja`）**に手が入っていないか**（`D-305`＝データ補正は行わない） |
| **★★E-6** | **「推測:〜と仮定した」が要る箇所に書かれているか**（`CLAUDE.md` §9-3） |

---

*以上、M35-03 レビューチェックリスト **v1.0.0**。* **★★本書が最も見たいのは束 A-1 と束 B-1 である**——**前者は「新規 DB では緑だが既存 DB が直っていない」を通す形、後者は `M35-02` が実際に踏んだ形だからである。**
