# M38-03 レビューチェックリスト（説明書の画像貼り込み ＋ macOS の名前寄せ ＋ 検査の表載せ）

| 項目 | 内容 |
|------|------|
| 文書ID | M38-03-REVIEW-CHECKLIST |
| バージョン | **1.1.0**（2026-09-18・**★母集団の動き方を是正**＝指示書 v1.1.0 に対応。**⇒ `M38-02` は図を 1 枚ではなく*章ごと*減らした**） ／ **1.0.0**（2026-09-17・発行） |
| 対象指示書 | `docs/instructions/M38-03-manual-images-and-macos-naming.md` **v1.1.0** |
| 作成者 | 設計担当 Claude（設計卓） |
| 位置づけ | **レビュー担当の主入力**（`D-736`） |

---

## 0. ★★レビュー担当への前置き

**★★★本サブは射程が 3 つあり、互いに無関係である。⇒ §A ／ §B ／ §C を*別々に*判定すること。★1 つの差し戻しで他の 2 つを巻き添えにしない。**

| # | 本サブ固有の不合格 |
|---|---|
| **★★★1** | **図の枚数を、現物ではなく指示書や過去の報告から引いている**（**★★`M38-02` が `ch14` を*章ごと*消しており、`M32-03` 完了報告の一覧は章 1 つぶん古い**＝計測点 `M-187` と同じ型） |
| **★★2** | **`img` の `src` が空を指したまま `todo` が外れている。⇒ リンク切れの図を出す説明書ができる** |
| **★★★3** | **スクリーンショットを Claude 側で作っている。⇒ 撮影は開発者の手番である** |
| **★★4** | **`macos` / `darwin` を機械的に全置換している。⇒ `GOOS=darwin` のようなツールチェーンの値まで動く** |
| **★5** | **`CLAUDE.md` の行を、スクリプトを読まずに指示書の要約から書いている**（`E-76`） |

---

## A. ★★★射程 A — 説明書の画像貼り込み

| # | 見ること |
|---|---|
| **★★★A-1** | **段 1-1 の `img` の全数が*件数*で報告に在るか。⇒ 現物から数えたと読めるか** |
| **★★★A-2** | **`img` の `src` が指す先が全数実在するか。⇒ 数えた全数と貼った数が突き合わせてあるか** |
| **★★A-3** | **`figure` の `todo` が 0 件か。⇒ 残っているなら章・枚数・理由が報告に在るか** |
| **★★A-4** | **外したのが `todo` クラス*だけ*か。⇒ `figure` や `figcaption` を消していないか** |
| **★★★A-5** | **画像を*作って*いないか。⇒ 足りないときに止まって請求したか** |
| **★A-6** | **本文を書き換えていないか。⇒ 本サブは貼り込みだけである** |
| **★A-7** | **「現在準備中です」が 0 件のままか**（`M32-02` 完了条件 7.5 ／ `M32-03` §2.4-1） |
| **★A-8** | **クイックスタート側**（`qs-` 接頭辞）**も母集団に入っているか。⇒ 本編だけ数えていないか** |
| **★★A-9** | **章数の表記**（「全 21 章」）**を触っていないか。⇒ `M38-02` が 4 面とも追随済みである** |

---

## B. ★★★射程 B — macOS の名前寄せ

| # | 見ること |
|---|---|
| **★★★B-1** | **寄せた向きが (a)＝アーカイブ名を `darwin` 側へ、か**（`D-893`）**。⇒ (b)〔バイナリ名を `macos` へ〕になっていないか** |
| **★★★B-2** | **`scripts/release-targets.sh` の 1 か所で済んでいるか。⇒ 2 か所目に名前を書いていないか**（`CHANGE-213`＝**書くと必ずドリフトする**） |
| **★★B-3** | **段 1-4 / 1-5 の全数が件数で報告に在るか。⇒ 「1 行だった」と断定する前に数えたか** |
| **★★★B-4** | **全置換していないか。⇒ 1 件ずつ「アーカイブ名として書かれているか」を判定したか** |
| **★★B-5** | **`check-release-archive.sh` が緑か。⇒ 出力で判定しているか** |
| **★B-6** | **`make release-archives` の出力が報告に貼ってあり、アーカイブ名が `darwin` 側か** |
| **★★B-7** | **`DES-002` §11.1 と §11.2 の amd64 の食い違いを触っていないか。⇒ 未決である**（`des002-macos-arch-mismatch`） |
| **★B-8** | **`DES-002` 本体を書き換えていないか。⇒ 原稿だけか**（`CLAUDE.md` §8） |

---

## C. ★★射程 C — `CLAUDE.md` §8 の表

| # | 見ること |
|---|---|
| **★★★C-1** | **足したのが 1 行か。⇒ 表の他の行を触っていないか** |
| **★★C-2** | **「何を見るか」がスクリプトの実装から書かれているか。⇒ 指示書の要約を写していないか**（`E-76`） |
| **★★★C-3** | **`check-doc-refs.sh` が緑か。⇒ 出力で判定しているか**（**★これが本射程の実証そのものである**） |

---

## D. ★射程（**3 つとも共通**）

| # | 見ること |
|---|---|
| **★D-1** | **`docs/usermanual/dist-readme.txt` を触っていないか。⇒ `M36-01` の成果である**（`D-887`） |
| **★D-2** | **`docs/design/` 本体・`followup-backlog.md` の差分が 0 行か** |
| **★D-3** | **マイグレを 1 本も作っていないか**（**★本サブは 0 本である**） |
| **★★D-4** | **`M38-01` / `M38-02` の面を触っていないか。⇒ どちらも着地済みのはずである** |

---

## E. ★報告

| # | 見ること |
|---|---|
| **★★★E-1** | **報告が射程 A ／ B ／ C の 3 つに分かれているか**（指示書 §0.1） |
| **E-2** | **完了報告 ＋ `progress-log` の索引行が在るか** |
| **★★E-3** | **CHANGE 原稿が設計伝達レポートの §1 と §6 に在るか。⇒ §4 ではない**（計測点 `M-183`）**。★射程 B のぶん 1 本だけである** |
| **★★★E-4** | **完了報告を書いた*後に*、報告を入力に取る検査を回し直したか** |
| **★E-5** | **全数テストの出力をファイルへ落とし、`EXIT=` と `FAIL` の件数を貼っているか** |
| **★E-6** | **着手前の版ゲート 6 点を通したことが報告に在るか**（指示書 §0.3） |

---

## ★★不合格の扱い

**★「高」の指摘を不採用にするときだけ開発者確認が要る。★再レビュー往復の上限は 2 回である**（`CLAUDE.md` §9）**。**

**★★★差し戻しは射程ごとに切ること。⇒ 射程 B の指摘で射程 A の成果を止めない。**

---

*以上、M38-03 レビューチェックリスト **v1.0.0**。* **★★★§A-1 を最初に見ること**——**図の枚数を現物から数えたかどうかが、本サブの成否をほぼ決める。⇒ `M38-02` が着手前に母集団を 1 つ動かしている。**
