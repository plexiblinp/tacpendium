# M23-05 レビューチェックリスト

| 項目 | 内容 |
|------|------|
| 対象指示書 | `docs/instructions/M23-05-deleted-row-duplicate-collision.md` v1.0.0（削除済み行と再登録の衝突） |
| 文書ID | M23-05-REVIEW |
| 対象指示書ID | M23-05 |
| レビューモデル | 高精度レビュー（実モデル名は `docs/human-notes/model-allocation.md` が正本。実使用は開発者判断） |
| バージョン | 1.1.0 |
| 作成者・作成日 | 設計担当 Claude（設計卓／M23 起票期）/ 2026-08-22 |

> 文書ID ⇄ 実パスの正は `docs/handover/docs-map.md`（自動生成）。

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| 1.1.0 | 2026-08-23 | **★§1 / §9 / §0.3 を訂正した**（**D-517**）——**`VAL-R03` が旧行を除いていないことは重大ではない。それが正しい。** **★指示書 §4.2 の旧版の誤りを、本書がそのまま重大の判定基準へ写していた。** |
| 1.0.0 | 2026-08-22 | 初版（指示書 v1.0.0 と対）。**最重要ゲート＝「`VAL-C02` の述語が動いていないこと」と「`PUT` で `VAL-C14` が走らないこと」。** |

---

## 0. レビュー担当への前置き

### 0.1 レビュー前の準備

1. **★指示書 §1.2 を先に読む**——**`deleted_at IS NULL` は手落ちではない。外すと `PUT` が壊れる。** **本サブの誤りはほぼすべてここに集まる。**
2. **`DES-006` §2.3 の「編集による再登録時の扱い」を読む**（述語が在る理由の一次資料）。
3. **完了報告に Plan Mode の実査 8 件（指示書 §3.3）と破壊確認 2 件（§5.5）が揃っていることを先に確認する。**

### 0.2 レビューの基本姿勢

- **★★本サブは「重複判定を直す」作業ではない。** **既存の判定はそのままにして、その外側へ落とさない検証を足す。** **「`VAL-C02` が削除済み行を見ていない」を欠陥として指摘しないこと。**
- **★「出ること」より「出すぎないこと」を先に見る。** 誤検知する警告は、無視される警告になる。
- **★母集団を 1 つ間違えると `PUT` のたびに警告が出る形になる。** **母集団の 4 つの述語を 1 件ずつ突き合わせること。**

### 0.3 ★重大でないもの（**誤判定しないこと**）

| # | 誤判定しやすい状態 | 実は仕様である理由 |
|---|---|---|
| N-1 | **`VAL-C02` が削除済み行を見ていないまま** | **指示書 §1.2 / §2.2-2。★外すと `PUT` が自分の消した旧行に衝突して、あらゆる編集が重複エラーで落ちる。** `DES-006` §2.3 が理由を明記している |
| N-2 | **`VAL-C14` が `PUT` で走らない** | **指示書 §4.5-1。★意図的である。** `PUT` は自分で旧行を作るため、走らせると毎回発火する |
| N-3 | **`VAL-C14` が旧行（`superseded_by_combo_id IS NOT NULL`）を見ていない** | **指示書 §4.2。★意図的である。** 旧行はゴミ箱の一覧に出ず、利用者が復元できない。**対処できない警告は無視される警告になる** |
| **N-3b** | **★★`VAL-R03` が旧行を除いていない** | **2026-08-23 訂正＝D-517。★これが正しい。** **`VAL-R03` の母集団は生存行であり、そこに居る旧行は画面に見えている生きたコンボである。** **除くと、見えている重複を黙って見逃す**（偽陰性）。**★指示書 §4.2 の旧版が「`VAL-C14` と `VAL-R03`」と書いていたが、誤りだった** |
| N-4 | **仮登録（`is_draft = 1`）の削除済み行を見ていない** | **指示書 §3.3-4 / §1.4-5。「仮登録と本登録の間では重複判定は行わない」は既定の方針である**（`DES-006` §2.3） |
| N-5 | **materialize 経路で警告が出ない** | **指示書 §1.4-4。★警告を返す先が無い**（内部呼び出し）。**`DES-006` §2.3 の既知の穴の記述は変えない** |
| N-6 | **`PATCH` で警告が出ない** | **指示書 §1.4-7。`PATCH` は重複判定キーを変えられないため、重複が新しく生まれない** |
| N-7 | **`CalcRecipeHash` が 3 か所に別実装で存在したまま** | **指示書 §2.2 / §3.3-3。★統合は本サブの射程ではない**（`M23-RESEARCH-01` H-3 の既知事項） |
| N-8 | **「重複している既存コンボへのリンク」の UI が無い** | **指示書 §1.4-3。`M23-06` / `M23-07`。★本サブは `details` に材料を載せるところまで** |
| N-9 | **重複を解消する導線が無い** | **指示書 §1.4-2。`M23-06` / `M23-07`** |
| N-10 | **警告が出ても登録・復元が成功する** | **指示書 §4.1-1。★これが原則である**（`DES-006` §13.1） |

### 0.4 報告フォーマット

- 各節「OK / 重大（§9）/ 軽微（§10）/ 質問（§11）」。

---

## 1. 設計・パターンとの照合（最重要）

- [ ] **★★`VAL-C02` の述語・母集団・判定内容が動いていない**（指示書 §1.2 / §2.2-2）。**diff で確認すること。**
- [ ] **★★`VAL-S04` の判定内容が動いていない**（同 §2.2-2）。
- [ ] **★★`VAL-C14` が `POST /api/combos` の経路でだけ走る。`PUT` では走らない**（同 §4.5-1）。**テスト §5.1-4 が主張していること。**
- [ ] **★`VAL-C14` の母集団から `superseded_by_combo_id IS NOT NULL` が除かれている**（同 §4.2）。 **★★`VAL-R03` では除かない**（2026-08-23 訂正＝**D-517**。**旧行を除くのは削除済みを見る母集団だけである。生存行に居る旧行は画面に見えている生きたコンボであり、除くと見えている重複を黙って見逃す**）。
- [ ] **★「経路で絞る」と「印で除く」の両方が入っている**（同 §4.2 の末尾）。**★印だけでは `PUT` を守れない**——**印は 2 文目に書かれるため検証時にまだ付いていない可能性がある。**
- [ ] **`VAL-R03` / `VAL-R04` が復元対象自身を母集団に入れていない**（同 §4.3）。
- [ ] **`is_draft = 0` が 4 件すべてで保たれている**（同 §3.3-4）。
- [ ] **判定キーの定義が `VAL-C02` / `VAL-S04` と同じものを使っている**（同 §4.1-2。**★別定義を作っていないこと**）。
- [ ] **`NULL` 同士が一致として扱われている**（同 §4.4）。
- [ ] **レシピの比較が既存の 2 段（SQL で絞る → ハッシュで比較）を踏襲している**（同 §4.4 の末尾。**★`group_concat` の近似で済ませていないこと**）。
- [ ] **4 件とも登録・復元を止めていない**（同 §4.1-1）。

## 2. データ・API 契約・スキーマの不変（非破壊性）

- [ ] **スキーマを変えていない。マイグレを 1 本も足していない**（指示書 §4.7）。
- [ ] **`M23-04` が作った `warnings` の器を再利用している。作り直していない**（同 §4.6）。
- [ ] **`details` の形が `M23-04` に揃っている**（`combos`＝`{id, memo}` ／ `setups`＝`{id, name}`。同 §4.6）。
- [ ] **警告 0 件のとき `warnings` キー自体が出ない**（`M23-04` と同じ）。
- [ ] **`400 validation_failed` ＋ `details.validations` の形を変えていない**（同 §2.2）。
- [ ] **`PUT` / `PATCH` / materialize の応答形と挙動が変わっていない**（同 §2.2）。

## 3. フロントエンドの動作仕様

- [ ] **登録側と復元側で文面が違う**（指示書 §4.6。**★利用者にできることが違う**）。
- [ ] **モーダルになっていない**（同 §4.6 の末尾）。
- [ ] **文言が翻訳キー経由である。ja / en の両方が揃っている。**
- [ ] **★フロントテストが実 `ja.json` を引く `t` で書かれている**（同 §5.3。**キー返しモックでは文面の主張が空振りする**＝`M23-04` 教訓 2）。

## 4. テストの妥当性（ケース数で確認。指示書 §5 ↔ §7 DoD）

- [ ] **サービス層 12 件が揃っている**（指示書 §5.1）。
- [ ] **★誤検知を防ぐ側のテストが在る**——**§5.1-2（レシピが違う）／ §5.1-3（旧行）／ §5.1-4（`PUT`）／ §5.1-5（仮登録）／ §5.1-7（復元対象自身）／ §5.1-9（別の親コンボ）。** **★このうち 1 つでも欠けていたら §9 である。**
- [ ] **★★破壊確認 2 件が実施され、コマンドと出力が完了報告に在る**（同 §5.5）。**★「テストが在る」と「テストが効く」は別である。**
- [ ] **`NULL` を含む判定キーのテストが在る**（同 §5.1-12）。
- [ ] **ハンドラ層 3 件**（同 §5.2）。
- [ ] **E2E が 1 本足されている**（同 §5.4）。**既存 spec の命名と使い捨て DB の前提に合っている。**
- [ ] **テスト結果が件数付きで報告されている**（同 §7.2。**★終了コードだけを根拠にしていない**＝`E-125`）。

## 5. 設計意図との整合（精神の確認）

- [ ] **★「落とさずに知らせる」が 4 件すべてで守られている**（指示書 §4.1-1）。
- [ ] **★登録側と復元側の役割の違いが実装に出ている**（同 §1.3。**登録側は重複を減らす／復元側はもう並んだことを告げる**）。
- [ ] **★否定形確認 4 件が完了報告に在る**（同 §4.8）。**「変えなかった」ではなく「この理由で変えないと判断した」の形になっている。**
- [ ] **★`M23-04` §1.6-2（登録経路へ `warnings` を遡らない）との関係が報告に書かれている**（同 §4.6。**★設計書間の矛盾ではなく、前サブの射程を守る線を本サブが解禁したものである**）。

## 6. コード品質・規約遵守

- [ ] **`CLAUDE.md` §4 の Go / TypeScript 規約。** JSON タグ camelCase。
- [ ] **エラーの wrap（`%w`）。**
- [ ] **VAL コードが定数化され、リテラルが散在していない。**
- [ ] **`console.log` / `fmt.Println` が本番コードに残っていない。**
- [ ] **新しい問い合わせが既存のものをコピーして増やしただけになっていないか**（**★共通化できる部分は共通化する。ただし `VAL-C02` の既存問い合わせには触れない**）。

## 7. 既存挙動の温存（非破壊性）

- [ ] **既存の Go テストが全て緑である**（件数付き）。**★とくに `VAL-C02` / `VAL-S04` / `PUT` / materialize のテスト。**
- [ ] **既存のフロントテストが全て緑である**（件数付き）。
- [ ] **`M23-01` / `M23-02` / `M23-03` / `M23-04` が足したテストが緑のままである。**
- [ ] **`make e2e` が緑である。**

## 8. ドキュメント・進捗ログ

- [ ] **`docs/progress/progress-log.md` へ 1 行追記されている**（`CLAUDE.md` §8）。
- [ ] **★レビュー結果を参照する記述が、レビュー後に埋められている**（指示書 §7.4。**`M23-04` 教訓 3**）。
- [ ] **完了報告に指示書 §7.5 の 6 点が揃っている。**
- [ ] **★設計書本体（`DES-006` / `DES-002` / `DES-005`）が編集されていない**（反映は設計卓が `CHANGE-125` で行う）。
- [ ] **`followup-backlog.md` へ書いた場合、§J だけを触っている**（**D-382**）。

---

## 9. 重大な問題の判定基準（完了承認を妨げる）

- **★★`VAL-C02` / `VAL-S04` の述語・母集団・判定内容を変えている**（指示書 §1.2）。**これが最重要ゲートである。** **`PUT` が壊れる。**
- **★★`VAL-C14` が `PUT` で走る**（同 §4.5-1）。**編集のたびに警告が出る。**
- **`VAL-C14` の母集団に旧行（`superseded_by_combo_id IS NOT NULL`）が入っている**（同 §4.2）。 **★★`VAL-R03` の母集団に旧行が入っていることは重大ではない。それが正しい**（2026-08-23 訂正＝**D-517**）。
- **「経路で絞る」が無く「印で除く」だけになっている**（同 §4.2 の末尾。**印の書き込み順序に依存する形である**）。
- **`VAL-R03` / `VAL-R04` が復元対象自身を数えている**（同 §4.3）。
- **誤検知を防ぐテスト 6 件（§5.1-2 / -3 / -4 / -5 / -7 / -9）のいずれかが欠けている。**
- **★破壊確認 2 件が実施されていない、または出力が報告に無い**（同 §5.5）。
- **警告で登録・復元が中止されている**（同 §4.1-1）。
- **レシピの比較を SQL の近似（`group_concat` 等）で済ませている**（同 §4.4）。
- **`is_draft = 0` が落ちている**（同 §3.3-4）。
- **スキーマ変更・マイグレの追加がある**（同 §4.7）。
- **`M23-04` の `warnings` の器を作り直している**（同 §4.6）。
- **スコープ外 7 件（同 §1.4）のいずれかへ踏み込んでいる。**
- **指示書 §7.5 の 6 件のいずれかが完了報告に無い。**

## 10. 軽微な問題の判定基準（持ち越し許容）

- 新しい問い合わせ関数の名前・置き場・粒度。
- 警告文言の日本語・英語の文面（**`DES-006` §12 が開発者との協同で決めるとしている**）。
- `details` に載せる件数の上限の具体値（**★上限を切るなら「他 N 件」を伝えていれば足りる**）。
- テストのデータ組み立ての作法。
- 4 つの問い合わせのあいだの共通化の粒度。

## 11. 質問・確認事項のフォーマット

- 「指示書 §X.X / `DES-00X` §X / `M23-RESEARCH-01` 報告 §X に対し実装が Z。意図確認したい」の形で根拠節併記。

## 12. レビュー完了の判定

- §1〜§8 が全て OK、§9 重大ゼロ（**特に「`VAL-C02` が動いていないこと」と「`PUT` で `VAL-C14` が走らないこと」と「破壊確認 2 件」**）、§0.1 の Plan Mode 実査 8 件が揃っている。§10 軽微は持ち越し可。

### 12.1 ★本チェックリスト自身の検査（**発行前に設計担当が行う**）

- [ ] **本書と対の指示書の間に、節番号の食い違いが無い。**
  → 本書が参照する指示書の節は §1.2 / §1.3 / §1.4 / §2.2 / §3.3 / §4.1 / §4.2 / §4.3 / §4.4 / §4.5 / §4.6 / §4.7 / §4.8 / §5 / §7 であり、**指示書側に実在する**。
- [ ] **指示書が禁じていない事項を「重大」として求めている項目が無い。**
  → §9 の各項目は指示書 §1.4 / §2.2 / §4 の禁止・固定と 1 対 1 で対応する。
- [ ] **達成不能な条件を重大として求めていない。**
  → **「`VAL-C02` が削除済み行も見ること」を求めていない**（§0.3 N-1。**★外すと `PUT` が壊れる**）。
  → **「materialize でも警告が出ること」を求めていない**（§0.3 N-5。**返す先が無い**）。
  → **「重複を解消する導線が在ること」を求めていない**（§0.3 N-9。**`M23-06` / `M23-07`**）。
  → **「`CalcRecipeHash` が 1 つに統合されていること」を求めていない**（§0.3 N-7。**射程外**）。

---

*以上、M23-05 レビューチェックリスト v1.1.0。配置 `docs/instructions/reviews/M23-05-review-checklist.md`。指示書 v1.1.0 と対。「`VAL-C02` が動いていないこと」と「`PUT` で `VAL-C14` が走らないこと」を最重要ゲートとする。*
