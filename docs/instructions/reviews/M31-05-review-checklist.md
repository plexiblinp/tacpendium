# M31-05 レビューチェックリスト（各キャラの設置系 `custom_states` を足す）

| 項目 | 内容 |
|------|------|
| 文書ID | M31-05-REVIEW-CHECKLIST |
| バージョン | **1.1.0**（2026-09-10・**★マイグレ番号の払い出しを反映＝`D-808`**） |
| 作成日 | 2026-09-10 |
| 対象指示書 | `docs/instructions/M31-05-custom-states-placement-expansion.md` **v1.3.0** |
| 発行 | 設計卓（`D-807`） |
| 使い方 | `/review_plan` の入力。**★本書は指示書の完了条件を写したものではない。⇒ 「緑でも壊れているもの」を探すための束である** |

---

## 0. ★★レビュー担当への前置き

**★★本サブは 9 キャラぶんの `custom_states` を入れる。⇒ 1 件でも取り違えると利用者の入力が狂う。**

**★★★段 1 の運用が 2026-09-10 に変わった**（`D-807`）**＝案 (vi) が暫定採択されており、実測が否定したときだけ止めて返す。⇒ 「開発者の回答を得ずに段 2 へ進んだ」ことは違反ではない。★ただし実数と実測が報告に無ければ違反である。**

---

## 1. 束 A — ★★★段 1 の実測（**暫定採択を検算する束**）

| # | 見ること | 落ちる形 |
|---|---|---|
| **★★★A-1** | **案 (ii) と案 (vi) それぞれの state 数の実数が在るか** | **★★★暫定採択の根拠が「9 体ぶんで 50 前後になる」という設計卓の見積もりである。⇒ 実数がこれと大きく違うなら、判断そのものが変わる** |
| **★★★A-2** | **実査 5**（同じファミリーの変種を同時に 2 つ以上設置できるキャラが居るか）**の結果が在るか** | **★★★居るなら 1 つの値では表せない。⇒ そのキャラだけ案 (ii) にしたか。★候補はキンバリーの細工手裏剣** |
| **★★A-3** | **実査 6**（`start_min` / `end` が変種の値にどう効くか）**の結果が在るか** | **★「始動時は弱が置いてある、終了時は強が置いてある」が表現として要るか。⇒ 要るのに単一値にしていたら壊れている** |
| **★★★A-4** | **`options` を足しても `sun_crest` が 1 行も変わらないことが測られているか** | **★★★これが案 (vi) の前提である**（`options` を持たない `level` は今までどおり数値入力）**。⇒ 崩れたら止めて返すはずだった** |
| **★★A-5** | **案 (iii)**（`type: composite`）**を再提案していないか** | **★★2026-09-10 に撤回済みである**（`D-806`）**。⇒ `migrations/000017` で削除され `isSupportedState` がスキップする** |

---

## 2. 束 B — ★★★キャラ固有状態タブの照合（**指示書 §4.2 が「足したのにタブに出ない」と名指しした点**）

| # | 見ること | 落ちる形 |
|---|---|---|
| **★★★B-1** | **9 体それぞれでタブが空でないことが、陽性対照とセットで示されているか** | **★★★「足す前は空（または少ない）」を測ってから足したか。⇒ 足した後だけ見ると、元から出ていた分と区別できない** |
| **★★★B-2** | **語幹と `move.code` の照合が実測で確かめられているか**（`CHANGE-170` §2.5） | **★★★`yoga_arch_set` の語幹が `yoga_arch_light` に当たるか。⇒ 当たらないと、足したのにタブに出ない。★検査は緑のままである** |
| **★★B-3** | **案 (ii) を採ったキャラが居るなら、そのキャラの語幹が変種ごとに分かれていないか** | **★`shuriken_bomb_light` の語幹は `_medium` に当たらない。⇒ 3 つの state を作らないとタブに全部載らない** |

---

## 3. 束 C — ★★★マイグレの層と番号

| # | 見ること | 落ちる形 |
|---|---|---|
| **★★★C-1** | **ファイル名に `_data_` が入っているか**（`NNNNNN_data_*.sql`＝`CHANGE-158`） | **★★★`custom_states` は SF6 の事実であり層 B**（`CC-BY-SA-4.0`）**である。⇒ `_data_` を入れないと `REUSE.toml` の既定**（層 A＝`AGPL`）**へ静かに落ちる。★誰も赤くならない** |
| **★★C-2** | **`scripts/check-migration-license.sh` を回して緑であることが示されているか** | **★★目視では層を確かめたことにならない。★`M28-05` が `000106` でここを踏んでいる** |
| **★★C-3** | **番号が `000110` か。⇒ 自採番していないか**（`D-293`） | **★★`D-808` で払い出した番号である。⇒ 1 本に固定であり、足りないなら再請求する。★投入時点では未払い出しだった**（計測点 `M-154`）**。⇒ 製造が段 2 で止まって請求していたら、それは正しい動きである** |
| **★★C-4** | **`down` が書かれているか**（`SUPP-001` §5.5.4） | **★`down` が無いと歴史テストが往復できない** |

---

## 4. 束 D — ★★★足す中身（**開発者裁定が既に在るもの**）

| # | 見ること | 落ちる形 |
|---|---|---|
| **★★★D-1** | **既存の state が 1 つも消えていないか** | **★★★キンバリー `shuriken_bomb_stock` とブランカ `blanka_chan_bomb` は「残弾」であり、今回足す「設置済み」とは別物である**（§0.3）**。⇒ 名前が似ているので上書きされやすい。★製造が 1 度外している** |
| **★★D-2** | **`aki` に入れていないか** | **★対象外である**（開発者の逐語＝「いらない事がわかった」） |
| **★★D-3** | **`subject` が「その状態が誰に付いているか」で決まっているか**（`CHANGE-154`） | **★「設置技かどうか」で決めていたら、同じ是正を 3 度目に踏んでいる** |
| **★★D-4** | **state 名に技名の装飾を持ち込んでいないか**（`CHANGE-169` §2.4） | **★ジュリの「[風破]」を付けていないか** |
| **★★D-5** | **ヤスミンが `moves` を増やしていないか**（§0.2-3） | **★state 側で弱中強を持つ。⇒ `moves` を増やすと開発者裁定に反する** |
| **★D-6** | **舞の焔版**（`flame_*` の 5 件）**が入っているか**（§0.2-2） | **★落ちやすい** |

---

## 5. 束 E — ★★CHANGE と設計書

| # | 見ること | 落ちる形 |
|---|---|---|
| **★★★E-1** | **案 (vi) を採ったなら、`DES-003` §3.2 へ `options` を足す CHANGE が請求されているか** | **★★★契約の追加である。⇒ 設計書に届かないと、次に `custom_states` を触る人が `options` を知らずに壊す** |
| **★★E-2** | **CHANGE を自採番していないか**（`D-293`） | **★番号は設計卓が払い出す** |
| **★E-3** | **`isSupportedState` に手が入っているか** | **★案 (vi) では触らないはずである。⇒ 触っていたら、`level` 以外を通そうとしている** |

---

## 6. 束 F — ★共通

| # | 見ること |
|---|---|
| **★F-1** | **`bash scripts/check-import-order.sh` がベースラインどおりか**（`D-388`） |
| **★F-2** | **`bash scripts/check-enum-sync.sh` が増えていないか** |
| **★F-3** | **完了報告が `bash scripts/check-md-emphasis.sh <file>` を通っているか**（`D-775`） |
| **★F-4** | **`docs/progress/progress-log.md` へ索引行が 1 行足されているか**（`CLAUDE.md` §8） |
| **★★F-5** | **`character_data/*.csv` を触っていないか** |
| **★★F-6** | **「推測:〜と仮定した」が要る箇所に書かれているか**（`CLAUDE.md` §9-3） |

---

*以上、M31-05 レビューチェックリスト **v1.1.0**。* **★★本書が最も見たいのは束 A-4 と束 B-2 である**——**前者は暫定採択の前提そのものであり、後者は「足したのにタブに出ない」を検査が緑のまま通す形だからである。**
