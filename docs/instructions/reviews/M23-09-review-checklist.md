# M23-09 レビューチェックリスト

| 項目 | 内容 |
|------|------|
| 対象指示書 | `docs/instructions/M23-09-pre-save-duplicate-dialog.md` v1.1.0（登録前の重複ダイアログ） |
| 文書ID | M23-09-REVIEW |
| 対象指示書ID | M23-09 |
| レビューモデル | 高精度レビュー（実モデル名は `docs/human-notes/model-allocation.md` が正本。実使用は開発者判断） |
| バージョン | 1.0.1 |
| 作成者・作成日 | 設計担当 Claude（設計卓／M23 起票期）/ 2026-08-23 |

> 文書ID ⇄ 実パスの正は `docs/handover/docs-map.md`（自動生成）。

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| 1.0.1 | 2026-08-23 | **★対象指示書の版数を v1.1.0 へ追随した**（**D-524**＝並列性欄の訂正）。**★レビューの内容は変えていない。** |
| 1.0.0 | 2026-08-23 | 初版（指示書 v1.1.0 と対）。**最重要ゲート＝「復元を押す前に入力が失われることが伝わること」と「文言と行動の対応表が在ること」。** |

---

## 0. レビュー担当への前置き

### 0.1 レビュー前の準備

1. **★指示書 §1.1 / §1.3 を先に読む**——**本サブは `M23-05` の後始末ではなく、その目的を初めて成立させるサブである。** **`M23-05` は「文面が実装のしないことを約束する」形で失敗した。**
2. **`DES-006` §11.2 の版不一致の規律を読む**（§4.4 の根拠）。**`DES-006` §7 の `VAL-N02`** も読む（§4.2-2 の先例）。
3. **完了報告に Plan Mode の実査 10 件（指示書 §3.3）・文言と行動の対応表（§4.9）・破壊確認 2 件（§5.3）が揃っていることを先に確認する。**

### 0.2 レビューの基本姿勢

- **★★文言を「読んで意味が通るか」で判定しないこと。** **「この文を読んだ利用者が次に取れる行動が、実装に在るか」で判定する。** **`M23-05` の文面は意味が通っていたのに、実装のしないことを約束していた。**
- **★本サブは判定ロジックを書かない。** `M23-05` の処理を呼ぶだけである。**「判定が甘い」を指摘する前に、それが `M23-05` の判定そのものでないかを確かめる。**
- **★モーダルを出していることを §11.1 違反として指摘しないこと**（§0.3 N-1）。

### 0.3 ★重大でないもの（**誤判定しないこと**）

| # | 誤判定しやすい状態 | 実は仕様である理由 |
|---|---|---|
| N-1 | **WARNING なのにモーダル（ダイアログ）を出している** | **指示書 §4.2-2。★逸脱ではない。** **区別は「警告の表示」か「操作の分岐」かであり、選ばないと次へ進めないものはモーダルが正しい。** **先例＝`VAL-N02`**（`DES-006` §7。WARNING で明示的な同意ダイアログ） |
| N-2 | **「両方入れる」の選択肢が無い** | **指示書 §1.4-1。★設計卓の裁定**（**D-518**）。**ボタン 1 つで重複を作れるようにすると、重複を減らすための導線が重複を増やす** |
| N-3 | **生きた重複ではダイアログが出ない** | **指示書 §4.1-4 / §1.4-3。★既に ERROR で止まっている**（`VAL-C02` / `VAL-S04`） |
| N-4 | **入力中にダイアログが出ない** | **指示書 §4.2-1 / §1.4-4。★入力のたびにモーダルが出る** |
| N-5 | **「新しく作る」の後に保存後トーストが出ない** | **指示書 §4.5。★意図である。** ダイアログで告げたことを二重に告げない |
| N-6 | **サーバが `VAL-C14` / `VAL-S07` の警告を返し続けている** | **指示書 §4.5-1 / §2.2。★サーバの契約は変えない。抑制は画面側で行う** |
| N-7 | **復元後に編集画面へ行かず、詳細画面へ行く** | **指示書 §4.3-3 / §1.4-8。★復元したものと、いま入力した内容は違いうる** |
| N-8 | **`VAL-C02` / `VAL-S04` の判定・応答が変わっていない** | **指示書 §1.4-2 / §2.2。★触らない** |
| N-9 | **保存前チェックが失敗したとき、ダイアログを出さずに保存へ進む** | **指示書 §4.2-3。★意図である。** チェックは付加価値であり、登録という主目的を巻き添えにしない |
| N-10 | **`PUT` / `PATCH` / CSV 取込 / materialize / コンボ同時登録でダイアログが出ない** | **指示書 §1.4-6。★射程外である** |

### 0.4 報告フォーマット

- 各節「OK / 重大（§9）/ 軽微（§10）/ 質問（§11）」。

---

## 1. 設計・パターンとの照合（最重要）

- [ ] **★★「復元する」を押す前に、入力内容が保存されないことが画面に書かれている**（指示書 §4.4）。**★押した後に伝えるのでは遅い。**
- [ ] **★★出すすべての文言について、§4.9 の自己検査（文言 → 利用者が次に取れる行動 → 実装に在るか）が完了報告に在る。**
- [ ] **保存ボタン押下でダイアログが出る。入力中には出ない**（同 §4.2-1）。
- [ ] **選択肢が「復元する」「新しく作る」＋キャンセルの 3 つである。「両方入れる」が無い**（同 §4.3-1）。
- [ ] **どれが該当したのかが、人が読める文字列で見える**（同 §4.1-3 / §4.3-2）。**★id だけになっていないこと。**
- [ ] **該当が複数のとき、どれを復元するか選べる**（同 §4.3-2）。**★「1 件目を復元」になっていないこと。**
- [ ] **★`M23-05` の判定処理を呼んでいる。書き直していない**（同 §2.2 / §3.3-1）。
- [ ] **保存前チェックの母集団が `VAL-C14` / `VAL-S07` と一致している**（同 §4.1-1 / §4.1-2）。
- [ ] **生きた側と削除済み側が別のキーで返っている**（同 §4.1-1）。
- [ ] **保存前チェックが失敗しても保存が止まらない**（同 §4.2-3）。
- [ ] **「新しく作る」の後に保存後トーストが出ない。ダイアログを出さなかった場合は出る**（同 §4.5-2。**★抑制しすぎていないこと**）。
- [ ] **サーバへ「確認済み」フラグを送っていない**（同 §4.5-1）。

## 2. データ・API 契約・スキーマの不変（非破壊性）

- [ ] **スキーマを変えていない。マイグレを 1 本も足していない**（指示書 §4.7）。
- [ ] **既存の保存前チェックの応答のキーと意味が変わっていない**（同 §4.1-1）。**追加だけであること。**
- [ ] **`POST /api/combos` / `POST /api/combos/{comboId}/setups` の成功応答の形が変わっていない**（同 §2.2）。
- [ ] **`VAL-C02` / `VAL-S04` の判定内容と応答が変わっていない**（同 §1.4-2）。**`409 duplicate_setup` を含む。**
- [ ] **`M23-05` の 4 つの判定の内容・母集団・経路の絞り込みが変わっていない**（同 §2.2）。
- [ ] **復元 API を変えていない。**

## 3. 画面の仕様

- [ ] **★キャンセルで入力内容が失われない**（指示書 §4.6-2）。
- [ ] **ダイアログ表示中に保存ボタンを二重に押せない**（同 §4.6-1 / §4.6-3）。
- [ ] **文面が翻訳キー経由である。ja / en の両方が揃っている**（同 §4.6-4）。
- [ ] **★`M23-05` が入れた文面の回帰ガードが消えていない**（同 §3.3-8 / §4.5-3）。**保存後トーストの文面が変わっていないこと。**
- [ ] **★文面が「失敗」「エラー」と読める形になっていない**（`DES-006` §11.2 の 4 点。**★重複が在ることは失敗ではない**）。

## 4. テストの妥当性（ケース数で確認。指示書 §5 ↔ §7 DoD）

- [ ] **サーバ 7 件・フロント 12 件が揃っている**（指示書 §5.1 / §5.2）。
- [ ] **★★§5.2-4（「復元する」を押す前に入力が失われることが書かれている）が在る。** **★これが最重要ゲートのテストである。**
- [ ] **★§5.2-9（ダイアログを出さずに保存したらトーストが出る）が在る**（**★抑制しすぎたことを検出できる唯一のテストである**）。
- [ ] **★誤検知を防ぐ側のテストが在る**——**§5.2-2（生きた重複だけならダイアログを出さない）／ §5.2-3（一致が無ければ出さない）／ §5.1-3（旧行と仮登録を返さない）／ §5.1-6（別の親コンボを返さない）。**
- [ ] **★★破壊確認 2 件が実施され、コマンドと出力が完了報告に在る**（同 §5.3）。
- [ ] **★フロントテストが実 `ja.json` を引く `t` で書かれている**（同 §5.2 の末尾。**キー返しモックでは §5.2-4 の主張が空振りする**＝`M23-04` 教訓 2）。
- [ ] **E2E 2 本が足されている**（同 §5.4）。**★「復元する」で新しいコンボが作られていないことを主張していること。**
- [ ] **テスト結果が件数付きで報告されている**（同 §7.2。**★終了コードだけを根拠にしていない**＝`E-125`）。

## 5. 設計意図との整合（精神の確認）

- [ ] **★★§4.9 の文言と行動の対応表が、実際に対応を検証している**（「文言を並べただけ」になっていないこと）。
- [ ] **★否定形確認 3 件が完了報告に理由付きで在る**（指示書 §4.8）。
- [ ] **★「重複が在ること」が失敗として扱われていない**（`DES-006` §11.2 の版不一致の規律と同じ精神）。
- [ ] **★入力を保持したまま選択させている**（同 §4.4。**`DES-006` §11.2 の 4 点**）。

## 6. コード品質・規約遵守

- [ ] **`CLAUDE.md` §4 の Go / TypeScript 規約。** JSON タグ camelCase。
- [ ] **`console.log` / `fmt.Println` が本番コードに残っていない。**
- [ ] **`any` を使っていない。**
- [ ] **`TODO` が `// TODO(<対応予定>): <内容>` 形式である。**

## 7. 既存挙動の温存（非破壊性）

- [ ] **既存の Go テストが全て緑である**（件数付き）。
- [ ] **既存のフロントテストが全て緑である**（件数付き）。
- [ ] **`M23-01`〜`M23-06` / `M23-08` が足したテストが緑のままである**（指示書 §5.5）。
- [ ] **`make e2e` が緑である。**
- [ ] **ゴミ箱画面が変わっていない**（指示書 §1.4-7）。

## 8. ドキュメント・進捗ログ

- [ ] **`docs/progress/progress-log.md` へ 1 行追記されている**（`CLAUDE.md` §8）。
- [ ] **★レビュー結果を参照する記述が、レビュー後に埋められている**（指示書 §7.4。**`M23-04` 教訓 3**）。
- [ ] **完了報告に指示書 §7.5 の 8 点が揃っている。**
- [ ] **★保存前チェックの応答の逐語形とダイアログの文面の逐語が在る**（同 §7.5-5 / §7.5-6。**★`CHANGE-141` の反映がこれ待ちである**）。
- [ ] **★設計書本体（`DES-002` / `DES-005` / `DES-006`）が編集されていない**（反映は設計卓が `CHANGE-141` で行う）。
- [ ] **`followup-backlog.md` へ書いた場合、§J だけを触っている**（**D-382**）。

---

## 9. 重大な問題の判定基準（完了承認を妨げる）

- **★★「復元する」を押す前に、入力内容が保存されないことが伝わらない**（指示書 §4.4）。**これが最重要ゲートである。** **★利用者の未保存の作業が黙って消える。**
- **★★§4.9 の文言と行動の対応表が完了報告に無い**（同 §7.5-2）。**★`M23-05` が失敗した箇所であり、本サブの主目的の検証である。**
- **★§5.2-4 のテストが無い。**
- **★§5.2-9 のテストが無い**（抑制しすぎたことを検出できない）。
- **誤検知を防ぐテスト（§5.2-2 / §5.2-3 / §5.1-3 / §5.1-6）のいずれかが欠けている。**
- **★破壊確認 2 件が実施されていない、または出力が報告に無い**（同 §5.3）。
- **「両方入れる」の選択肢を出している**（同 §1.4-1。**★設計卓の裁定に反する**）。
- **入力中にダイアログを出している**（同 §4.2-1）。
- **`M23-05` の判定を書き直している**（同 §2.2）。
- **保存前チェックの母集団が `VAL-C14` / `VAL-S07` と食い違っている**（同 §4.1）。
- **サーバへ「確認済み」フラグを送っている**（同 §4.5-1）。
- **保存前チェックの失敗で保存が止まる**（同 §4.2-3）。
- **キャンセルで入力内容が失われる**（同 §4.6-2）。
- **どれが該当したのかが id でしか見えない**（同 §4.1-3）。
- **該当が複数のときに選ばせず「1 件目を復元」している**（同 §4.3-2）。
- **`VAL-C02` / `VAL-S04` の判定・応答を変えている**（同 §1.4-2）。
- **`M23-05` の文面の回帰ガードを消している**（同 §4.5-3）。
- **スキーマ変更・マイグレの追加がある**（同 §4.7）。
- **スコープ外 8 件（同 §1.4）のいずれかへ踏み込んでいる。**
- **指示書 §7.5 の 8 件のいずれかが完了報告に無い。**

## 10. 軽微な問題の判定基準（持ち越し許容）

- 保存前チェックの応答のキー名と入れ子の形（**★生きた側と削除済み側が分かれていれば足りる**）。
- セットプレイ側の新設経路の名前（**★コンボ側に倣っていれば足りる**）。
- ダイアログの文面の言い回し（**★§4.9 の自己検査を通っていれば足りる**）。
- 該当が複数のときの一覧の見せ方（**★選ばせる形になっていれば足りる**）。
- 復元後の遷移先の細部。
- テストのデータ組み立ての作法。

## 11. 質問・確認事項のフォーマット

- 「指示書 §X.X / `DES-00X` §X / `m23-05-completion-report` §X に対し実装が Z。意図確認したい」の形で根拠節併記。

## 12. レビュー完了の判定

- §1〜§8 が全て OK、§9 重大ゼロ（**特に「復元を押す前に入力が失われることが伝わること」「文言と行動の対応表」「§5.2-4 / §5.2-9」「破壊確認 2 件」**）、§0.1 の Plan Mode 実査 10 件が揃っている。§10 軽微は持ち越し可。

### 12.1 ★本チェックリスト自身の検査（**発行前に設計担当が行う**）

- [ ] **本書と対の指示書の間に、節番号の食い違いが無い。**
  → 本書が参照する指示書の節は §1.1 / §1.3 / §1.4 / §2.2 / §3.3 / §4.1 / §4.2 / §4.3 / §4.4 / §4.5 / §4.6 / §4.7 / §4.8 / §4.9 / §5 / §7 であり、**指示書側に実在する**。
- [ ] **指示書が禁じていない事項を「重大」として求めている項目が無い。**
  → §9 の各項目は指示書 §1.4 / §2.2 / §4 の禁止・固定と 1 対 1 で対応する。
- [ ] **達成不能な条件を重大として求めていない。**
  → **「WARNING をモーダルで出さないこと」を求めていない**（§0.3 N-1。**★先例が `VAL-N02` に在る**）。
  → **「両方入れるの選択肢が在ること」を求めていない**（§0.3 N-2。**設計卓の裁定**）。
  → **「生きた重複でもダイアログが出ること」を求めていない**（§0.3 N-3。**既に ERROR で止まっている**）。
  → **「サーバが警告を返さなくなること」を求めていない**（§0.3 N-6。**抑制は画面側**）。

---

*以上、M23-09 レビューチェックリスト v1.0.1。配置 `docs/instructions/reviews/M23-09-review-checklist.md`。指示書 v1.1.0 と対。「復元を押す前に入力が失われることが伝わること」と「文言と行動の対応表」を最重要ゲートとする。*
