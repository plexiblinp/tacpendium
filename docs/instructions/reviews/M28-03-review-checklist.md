# M28-03 レビューチェックリスト: ツールチェーンと依存の版上げ

| 項目 | 内容 |
|------|------|
| 文書ID | M28-03-review-checklist |
| バージョン | **1.0.0** |
| 対象指示書 | `docs/instructions/M28-03-toolchain-and-dependency-bump.md` **v1.0.0** |
| 推奨モデル | **標準レビュー**（**★判断は少ない。★★ただし「緑になったか」の判定を誤らないこと**） |
| 作成者・作成日 | 設計担当 Claude（設計卓・フェーズ4 期）/ 2026-09-06 |

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| 1.0.0 | 2026-09-06 | 初版（**`D-736`**）。**★投入の合図と同じ手番で起こした**（`m27-03-review-checklist-not-created` の是正＝`D-734`） |

---

## 0. レビュー担当への前置き

### 0.1 レビュー前の準備

1. 指示書 **v1.0.0** を全文読む。**★とくに §0.2（`go test` が見ないもの 3 つ）／§0.3（版数の食い違い）／§2.7（コミットの割り方）／§4（やらないこと 12 項）。**
2. 完了報告 `docs/progress/M28-03-completion-report.md` を読む。
3. **★`CLAUDE.md` §6（依存ライブラリ追加ポリシー）を読む。⇒ `indirect → direct` 昇格も §6 に当たる**（2026-08-14 開発者裁定）。
4. **★`docs/handover/code-facts.md` を引く。想定で書かない。**

### 0.2 レビューの基本姿勢

コードを変更せず、指摘のみを行う。

### 0.3 ★★重大でないもの（**誤判定しないこと**）

| # | これは欠陥ではない |
|---|---|
| **★★1** | **`go.mod` の `go` ディレクティブが上がっていないこと。⇒ これが正しい**（指示書 §2.2-1・§4-2）。**★上げたければ §7-1 へ返すのが正しい経路である。** |
| **★★2** | **メジャー版上げをしていないこと**（指示書 §2.3-2 / §2.4-3 / §4-3）。**★見送った理由が報告に在れば正しい。** |
| **★★3** | **devContainer をリビルドしていないこと。★開発者の手番である**（指示書 §2.5-2 / §4-11）**。⇒ 製造の環境でリビルドしても、開発者の環境で同じものが再現する保証がない。** |
| **★★4** | **`check-md-emphasis.sh` / `check-import-order.sh` のベースラインが動いていないこと。★本サブ由来でないドリフトを本サブの手番で床にしないのが正しい**（`M28-01` の先例＝`followup` `md-emphasis-baseline-per-lane-drift`）。 |
| **★5** | **`.claude/` が未編集であること**（開発者のルールファイル。指示書 §4-7。**★報告だけが射程**）。 |
| **★6** | **`docs/design/` が未編集であること**（`CLAUDE.md` §8。指示書 §4-8。**★`DES-001` の「Go 1.22+」に手が要るなら設計卓へ請求するのが正しい**）。 |
| **★7** | **`followup-backlog.md` が未編集であること**（`D-382`。**§J 停止時記録だけは例外**）。 |
| **★8** | **CHANGE 番号を自採番していないこと**（`D-293`）。 |
| **★9** | **`.devcontainer/init-firewall.sh` の許可リストが広がっていないこと。★広げる必要が出たら止まって報告するのが正しい**（指示書 §2.5-4 / §4-12）。 |

### 0.4 ★★本サブに固有の危険（**最優先で見る**）

**危険は 3 つある。★★いずれも「緑に見えるまま通る」型である。**

| # | 危険 | なぜ検出しにくいか |
|---|---|---|
| **★★1** | **`go test ./...` だけで緑と判定する** | **クロスビルドを見ない。★`modernc.org/sqlite` は純 Go 実装で Go の版に敏感な部類であり、`go test` は通るが `make build-all` で落ちる面が実在する。`nightly-crossbuild.yml` が在るのはそのためである** |
| **★★2** | **版数を 1 か所だけ上げる** | **★設計卓が読める範囲で 4 か所すべてが違う値だった**〔`go.mod`＝`go 1.26.4` ／ `CLAUDE.md` §2＝「1.26.2 以上」 ／ `README.md`＝「1.22 以上(devContainer は 1.26.2)」 ／ `DES-001`＝「Go 1.22+」〕**。⇒ 1 か所だけ直すと食い違いが増える。★★どの検査も捕まえない** |
| **★★3** | **1 コミットに全部混ぜる** | **★赤が出たときに「ツールチェーンか、Go 依存か、フロント依存か」が切り分けられなくなる。⇒ 動いているうちは誰も困らないので、指摘されないまま通る** |

### 0.5 報告フォーマット

`docs/progress/m28-03-review.md`。指摘ごとに **重大 / 高 / 中 / 低** と **根拠のファイル・行** を書く。

---

## 1. 束 A — 母数と版数の同期（最重要）

| # | 見ること |
|---|---|
| **★★1** | **版数を書いている場所の全数が報告に在るか。★`.devcontainer/`（`Dockerfile` / `devcontainer.json` / `devcontainer-lock.json`）と `.github/workflows/`（`nightly-crossbuild.yml` / `pr-checks.yml`）を見ているか**（指示書 §2.1-1）。 |
| **★★2** | **食い違いの一覧が、直す前の姿として報告に在るか**（指示書 §2.1-2）。**★「直したので今は揃っている」だけでは足りない**——**どこがどう食い違っていたかが、次に同じ作業をする人の材料になる。** |
| **★★3** | **★★挙げた場所が全部直っているか。⇒ 一覧に在るのに直っていない行が無いか。** |
| **★4** | **フロント側の版数**（Node ／ `packageManager` の pnpm 版）**も見ているか**（指示書 §2.1-3）。 |
| **★5** | **`AGENTS.md` / `.codex/` / `.agents/` の Codex 派生物に版数が入っていないか**（指示書 §6-3。**★入っていたら `/sync_codex_config` の射程に触れる**）。 |
| **★6** | **`.claude/` に版数の直書きが在れば報告されているか**（指示書 §2.1-4。**★編集していないことが正しい**）。 |

## 2. 束 B — 上げ幅の線引き

| # | 見ること |
|---|---|
| **★★1** | **★ツールチェーンだけを上げたか。`go.mod` の `go` ディレクティブが動いていないか**（指示書 §2.2-1）。**★動いていたら重大。** |
| **★★2** | **上げ先が「同じ minor の最新 patch」か。★minor を跨いでいるなら、跨いだ理由と開発者の承認が報告に在るか**（指示書 §2.2-2）。 |
| **★★3** | **★★どのセキュリティ修正が入るのかが報告に書かれているか**（指示書 §2.2-3）。**⇒ 「新しいから上げた」で終わっていないか。★本サブの目的は「利用者へ届くバイナリに入る修正を特定すること」である**〔単一バイナリ配布であり、ビルドに使った Go の版がそのまま利用者へ届く〕**。** |
| **★★4** | **Go 依存が patch / minor までに収まっているか。★メジャーが混じっていないか**（指示書 §2.3-2）。 |
| **★★5** | **★新規依存が足されていないか。★`indirect → direct` 昇格が起きていないか**（`CLAUDE.md` §6。指示書 §2.3-3 / §4-4）。**★★`go.mod` の `// indirect` が外れた行が無いかを直接見ること。** |
| **★6** | **`go.sum` の差分が「`go mod tidy` の結果」と「意図して上げた分」に分けて報告されているか**（指示書 §2.3-5）。 |
| **★7** | **フロント側で `npm` / `yarn` / `npx` が使われていないか。★`package-lock.json` / `yarn.lock` が生まれていないか**（`CLAUDE.md` §11。指示書 §2.4-1 / §2.4-2）。 |

## 3. 束 C — ★★検証（本サブの中心）

| # | 見ること |
|---|---|
| **★★1** | **`go test ./...` が緑か。** |
| **★★2** | **★★`make build-all` が 3 OS すべてで緑か**（指示書 §2.6-2）。**⇒ 1 つでも落ちていたら緑ではない。★★報告に 3 つの結果が個別に書かれているか。「build-all 緑」の 1 行だけなら不十分である。** |
| **★★3** | **`make e2e` が全数で緑か**（指示書 §2.6-3）。**★`make e2e-only` で代用していないか。** |
| **★★4** | **`cd web && pnpm test` が緑か。** |
| **★★5** | **常設検査が緑か。★`check-artifact-integrity.sh` を 1 本目に回しているか**（`CLAUDE.md` §8）。 |
| **★★6** | **★★落ちたテストを skip / disable / quarantine していないか**（指示書 §2.6-6 / §4-5）。**⇒ 落ちたら止まって報告するのが正しい。★これは重大である。** |

## 4. 束 D — コミットの割り方

| # | 見ること |
|---|---|
| **★★1** | **指示書 §2.7 の 5 段に割れているか**〔版数の同期 ／ Go ツールチェーン ／ Go 依存 ／ フロント依存 ／ devContainer・CI〕**。** |
| **★★2** | **★★2〜5 がそれぞれ単独で revert できる形か。⇒ 1 つのコミットに 2 段が混じっていないか。** |
| **★3** | **逆に、意味の無い粒度まで割れていないか**（例＝依存 1 本ごとに 1 コミット）。 |

## 5. 既存の破壊（非破壊性）

| # | 見ること |
|---|---|
| **★★1** | **`M28-01` のリネームが巻き戻っていないか。★`combomgr` が新しく現れた行が無いか**（**`combomgr-importer` / `autopilot-combomgr` は別プロジェクトであり正しい**）。 |
| **★★2** | **`.devcontainer/devcontainer-lock.json` の差分が報告に明示されているか**（指示書 §2.5-3。**★lock の更新はサプライチェーンの変更である**）。 |
| **★3** | **既存の `config.toml.example` / `Makefile` / `embed_*.go` の挙動が変わっていないか。** |
| **★4** | **`README.md` / `CLAUDE.md` の版数以外の記述が巻き込まれていないか**（**★一括置換の巻き添え**）。 |

## 6. ドキュメント・進捗ログ

| # | 見ること |
|---|---|
| **★★1** | **報告に §2.1 の母数（一覧）が在るか。** |
| **★★2** | **上げなかったものと、その理由が在るか**（指示書 §3-3）。 |
| **★★3** | **`/app_build_check` の総合判定がそのまま載っているか**（指示書 §2.3-1 / §3-5）。**⇒ 判定や深刻度を上書き・要約していないか。** |
| **★4** | **`docs/design/` に反映が要る箇所が一覧になっているか**（`DES-001` の「Go 1.22+」）。 |
| **★5** | **`docs/progress/progress-log.md` の `### M28-03` へ追記されているか**（`CLAUDE.md` §8）。 |

## 7. 破壊確認（**実際に壊して、検出できることを見る**）

**★レビュー担当が自分で 1 つ試すこと。★★「テストが在る」ではなく「壊したら赤くなる」を見る。**

| # | 壊し方 | 期待 |
|---|---|---|
| **★★1** | **`go.mod` の依存 1 本を意図的に古い版へ戻す** | **`go test` か `make build-all` のどちらかが落ちる。⇒ どちらも緑のままなら、その依存は実質検証されていない。★その事実を報告する** |
| **★2** | **`CLAUDE.md` §2 の版数だけを別の値へ書き換える** | **★★どの機械検査も赤くならないはずである。⇒ それを確認して報告に書く**（**版数の同期を守る機械検査は存在しない**＝本サブの構造的な弱点） |

---

## 8. 重大な問題の判定基準（完了承認を妨げる）

| # | 重大とするもの |
|---|---|
| **★★1** | **`make build-all` の 3 OS のいずれかが落ちている、または結果が報告に無い。** |
| **★★2** | **落ちたテストを skip / disable / quarantine して緑にしている。** |
| **★★3** | **`go.mod` の `go` ディレクティブを、承認なく上げている。** |
| **★★4** | **新規依存を足している、または `indirect → direct` 昇格をしている**（`CLAUDE.md` §6）。 |
| **★★5** | **メジャー版上げを、承認なく行っている。** |
| **★6** | **`make e2e` が全数で回っていない。** |
| **★7** | **`.devcontainer/init-firewall.sh` の許可リストを単独で広げている。** |

## 9. 軽微な問題の判定基準（持ち越し許容）

| # | 軽微とするもの |
|---|---|
| 1 | **コミットの粒度が §2.7 と細部で違う**（**★段が混じっていたら重大ではなく「高」**）。 |
| 2 | **報告の書き方の不足**（一覧が在るが読みにくい等）。 |
| 3 | **Codex 派生物の版数の扱い**（`/sync_codex_config` の射程）。 |

## 10. レビュー完了の判定

1. **束 A〜D と §5・§6 のすべてに目を通した。**
2. **§7 の破壊確認を最低 1 つ実施した。**
3. **重大が 0 件である**（在れば差し戻し）。
4. **報告 `docs/progress/m28-03-review.md` を書いた。**

### 10.1 ★本チェックリスト自身の検査（発行前に設計担当が行う）

- [x] 指示書 v1.0.0 の「やらないこと」12 項が、§0.3 の「欠陥ではない」と矛盾していない
- [x] §0.4 の危険 3 つが、指示書 §0.2 / §0.3 / §0.4 と対応している
- [x] §7 の破壊確認が、機械検査で代替できないものになっている

---

*以上、M28-03 レビューチェックリスト **v1.0.0**。* **★★本サブでレビューが最も見落としやすいのは「`go test` が緑だから終わった」である**（§0.4-1）。**★クロスビルドと E2E を通して初めて緑である。**
