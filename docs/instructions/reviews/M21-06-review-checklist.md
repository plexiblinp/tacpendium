# M21-06 レビューチェックリスト

| 項目 | 内容 |
|------|------|
| 文書ID | M21-06-REVIEW-CHECKLIST |
| バージョン | 1.1.0 |
| 対象 | `docs/instructions/M21-06-command-motion-input.md` v1.1.0 |
| 対の CHANGE | `docs/change-notes/CHANGE-109-notification.md` |
| 作成者・作成日 | 設計卓（Claude Code・武装中）/ 2026-08-14 |

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| **1.1.0** | **2026-08-14** | **指示書 v1.1.0 と対**（**D-379**）。**★§1.6 を新設**（モーション用の読み取り経路の 7 制約）。**★§2-1 を差し替え**——**旧項目「`internal/` 全体の diff 0（契約 F-1）」は契約の誤読であった**（**M-65**）。**そのまま判定すると、裁定どおりの実装が差し戻される**（**D-323** の同型）。**★重大 17 を差し替え・重大 21〜22 を追加。** **★§9 へ 2 件追加。** |
| 1.0.0 | 2026-08-14 | 初版（指示書 v1.0.0 と対）。 |

---

## 0. 読む順（★ここから始める）

1. **§1.0 を最初に見る**（**改名が単独のコミットで、挙動の diff を伴っていないか**）
2. **§1.4**（既存の 3 経路を 1 つも変えていないか）
3. **§1.2**（解決の規則。とくに**最長一致**）
4. **§4**（テスト。とくに破壊確認）
5. 残りは順不同

> **★本サブは M21 の最後であり、既存の 4 サブすべての上に乗る。** **⇒ 実質の論点は「新しい状態を足しながら、下の 4 層を 1 つも動かしていないか」である。**

---

## 1. 設計意図との一致

### 1.0 ★改名が分離されているか（§4.0）

| # | 確認 |
|---|---|
| 1 | **改名が単独のコミットで行われている**（本サブの中身と混ざっていない） |
| 2 | **挙動の diff を伴っていない**（**diff で示されている**） |
| 3 | **周辺の識別子も同じ手番で改まっている**（**名前が 2 通りになっていない**） |

### 1.1 ★モードの骨格（§4.1）

| # | 確認 |
|---|---|
| 1 | **操作の種別が 3 値 → 4 値になり、`M21-04` 側と `M21-05` 側の両方へ同時に足されている**（`§2.3`）。**★列挙が 1 か所で、両側が同じものを参照している**（`E-76`） |
| 2 | **★モードが時間で切れない**（`§4.1-3`。§5 (g)）。**判定窓に乗っていない** |
| 3 | **★モード中に方向がステップにならない**（`§4.1-4`。§5 (a)） |
| 4 | **確定の契機が攻撃ボタンである**（`§4.1-5`） |
| 5 | **溜まっている列が画面に出ている**（`§4.1-6`） |
| 6 | **モードから抜けられ、溜めた列が黙って消えない**（`§4.1-7`。`E-84`） |
| 7 | **モードに入っていない間は今までどおりである**（`§4.1-8`。§5 (h)） |
| 8 | **Gamepad とキーボードのどちらからでも入れる**（`§4.1-9`。§5 (j)） |

> **★`D-370` の「3 値。増やさない」を根拠に 4 値目を差し戻さないこと。** **同記述は `M21-05` 時点の as-built であって恒久の上限ではない**（§9-1）。

### 1.2 ★解決の規則（§4.2）

| # | 確認 |
|---|---|
| 1 | **前方一致である**（コマンドの方向列が、溜まった列の先頭に一致する） |
| 2 | **末尾の余りを無視する**（§5 (c)） |
| 3 | **★最長一致である**（§5 (d)）。**★これが無いと、コマンドが他方の先頭に含まれる技が短いほうへ解決される** |
| 4 | **同じ長さで複数残るなら解決しない**（§5 (e)） |
| 5 | **先頭のゴミは解決しない**（§5 (f)） |
| 6 | **出口が `move_code` である**（契約 F-3 の死守 3） |
| 7 | **解決できなかった入力が捨てられていない**（§5 (k)） |
| 8 | **★`§4.2-3` で意図と違う技になる組の有無が完了報告にある**（**実データで確かめた結果**） |

### 1.3 契約 F-3（§4.3）

| # | 確認 |
|---|---|
| 1 | **波形・軌跡・タイミングを解釈していない**（**押された方向の列を索引と突き合わせるだけ**） |
| 2 | **`move_commands` ／ `internal/moveindex/` の diff 0**（**読むだけ**） |
| 3 | **★「モーション解析はしない」と衝突しない理由が完了報告に書かれている**（契約が明記を求めている） |

### 1.4 ★既存の 3 経路を 1 つも変えていないか（§4.4・**最重要**）

| # | 確認 |
|---|---|
| 1 | **判定窓・アルゴリズムの diff が無い**（`§2.2-1`） |
| 2 | **★「ステップにしない」を判定層の分岐で実現していない**（`§4.4-1`。**供給する側で止めている**） |
| 3 | **新しい調停・配送チャネル・合流層を作っていない**（`§4.4-2`・`§4.4-3`） |
| 4 | **モードの状態が合流の後段にある**（`§4.4-3`） |
| 5 | **§6.5.1 の 3 つの守りが不変で、モード中も効いている**（`§4.4-4`。§5 (i)） |
| 6 | **押下集合の扱いが不変**（`§2.2-5`。**操作用の論理ボタンを判定経路へ流していない**） |

> **★判定層に分岐が入っても動いてしまう。** **モードでもモードでなくても正しく入力でき、テストも手動確認も通る。** **露見するのは、次に判定を直す担当が片方だけ直したときである。**

### 1.5 スコープ外を踏んでいないか（§1.3）

| # | 確認 |
|---|---|
| 1 | **仮想コントローラ側でモーションを実演させていない**（**死守 2 は仮想側に掛かり続ける**） |
| 2 | **曖昧一致・編集距離・入力ミスの補正を入れていない** |
| 3 | **`command` 記法を拡張していない** |
| 4 | **ステップの並び替えを作っていない** |
| 5 | **契約が凍結しているパッケージの diff 0**（**§2-1**。**★「`internal/` 全体」ではない**＝**M-65**） |


### 1.6 ★モーション用の読み取り経路（§4.6・**2026-08-14 追加**）

| # | 確認 |
|---|---|
| 1 | **★既存の解決表の絞り込みを広げていない**（`§4.6-1`。**広げると仮想コントローラの一様フォールバックが変わる**） |
| 2 | **★既存の応答が 1 バイトも変わっていない**（`§4.6-2`。**diff またはテストで示されている**） |
| 3 | **キャラ選択時に 1 回取得する形である**（`§4.6-3`。**入力ごとに往復していない**） |
| 4 | **読むだけである**（`§4.6-4`。契約 F-3） |
| 5 | **前方一致・最長一致はフロントが行っている**（`§4.6-5`。**バックエンドは表を返すだけ**） |
| 6 | **★取得に失敗しても入力が壊れない**（`§4.6-6`。**モードに入れないだけで既存の入力経路は動く**） |
| 7 | **バックエンド分が単独のコミットで分かれている**（`§4.6-7`） |

> **★`§4.6` を「契約違反」として差し戻さないこと**（§9-10）。**契約は `internal/service/inputresolve/` を凍結していない。**

---

## 2. 契約・凍結インターフェース

| # | 確認 |
|---|---|
| 1 | **★【2026-08-14 是正＝D-379】契約が凍結しているパッケージの diff 0**——**`internal/service/notation/` ／ `internal/service/preset/` ／ `internal/service/punishfinder/` ／ `internal/service/setplay/`**。**★旧項目「`internal/` 全体の diff 0（契約 F-1）」は契約の誤読であった**（**M-65**）——**F-1 は `recipe_cache` の読み書き契約であって `internal/` 全体の凍結ではない** |
| **1′** | **★`internal/` の変更が `§4.6` の読み取り経路のみである**（**単独コミットで分離されている**） |
| 2 | **`internal/moveindex/` ／ `move_commands` の diff 0**（契約 F-3。**読むだけ**） |
| 3 | **`migrations/` の diff 0**（**disk 末尾を着手時に実査した記録つき**） |
| 4 | **`scripts/` の diff 0**（**D-335**。**`M20-07` と並走しうる**） |
| 5 | **`docs/design/` ／ `docs/change-notes/` の diff 0** |
| 6 | **`bash scripts/check-browser-storage-keys.sh` が緑**（**新規キーは台帳へ追記してから使う**） |

> **★`§2.2` は挙動の凍結であり、失効した記述の是正を妨げない**（**D-361**）。**とくに「必殺技の方向キー連続入力は `M21-06` が持つ」という未来形は、本サブが持った時点で失効する**（`§4.5-2`）。

---

## 3. 実装品質

| # | 確認 |
|---|---|
| 1 | `any` / `@ts-ignore` / `eslint-disable` / `console.log` の追加 0 件 |
| 2 | 関数コンポーネント ＋ Hooks のみ（CLAUDE.md §4） |
| 3 | 列挙的文字列定数がリテラルで散在していない |
| 4 | **★操作の種別の列挙が 1 か所にある**（`E-76`） |
| 5 | 既存 test-id を変更・削除していない（**追加は可**。命名は `DES-005` §6.8） |
| 6 | `TODO` コメントが `// TODO(<対応予定>): <内容>` 形式である |

---

## 4. テスト

| # | 確認 |
|---|---|
| 1 | **§5 の (a)〜(k) が実装されている** |
| 2 | **★(a)(d)(g) の破壊確認が行われ、「どのテストが赤くなったか」まで報告されている** |
| 3 | **★赤にならなかった項目について、「別の機構が代わりに守っている」を先に疑い、守っていたものを特定してから、ガードと同じ層に契約テストを置いた形跡がある**（**D-375**） |
| 4 | **★E2E に対照実験がある**——**「モードでなければステップが増える」ことまで主張している**（**D-375**。**これが無いと、モードに入れていなくても緑になる**） |
| 5 | **(g) が「長い間隔を空けても解決する」形である**（**要件そのもの**） |
| 6 | `pnpm test` 全緑 ／ `pnpm lint` exit 0 ／ `go test ./...` FAIL 0 ／ `make e2e` 全緑 |

---

## 5. ドキュメント

| # | 確認 |
|---|---|
| 1 | **`docs/progress/progress-log.md` へ 1 行追記されている**（CLAUDE.md §8。**必須**） |
| 2 | **完了報告に §7.5 の 8 項目がある** |
| 3 | **§7.5-3**（**操作の種別を 4 値へ増やした箇所の全列挙**）**がある**。**★「両側へ同時に足した」の証跡である** |
| 4 | **§7.5-8**（**実機確認手順**）**がある。★機材は実機コントローラ ＋ キーボードである** |

---

## 6. 否定形確認（§4.5）

| # | 確認 |
|---|---|
| 1 | **2 軸で回している**——**(a) 識別子 ／ (b) 前提を述べている散文**（**D-361**） |
| 2 | **★走査の 4 条件を満たしている**（**D-375**）——**`LC_ALL=C.UTF-8` ／ 行跨ぎまたはファイル単位 ／ 数値パターン ／ 陽性対照** |
| 3 | **★陽性対照が当たったことが報告されている** |
| 4 | **§4.5 の 4 項目それぞれについて結果がある** |
| 5 | **★項目 4 は「§6.1 の記述そのものは失効しない」が正解である**——**死守 2 は仮想側に掛かり続ける。** **失効したと判定していたら誤りである** |

---

## 7. 停止規律

| # | 確認 |
|---|---|
| 1 | **§9.4 の停止条件に当たった場合、`followup-backlog` §J へ必須 5 フィールド付きで記録され、停止している** |
| 2 | **再レビュー往復が 2 回を超えていない** |
| 3 | **記録して停止した場合、それを「失敗」として扱っていない**（**正規の完了形式である**） |

---

## 8. 差し戻し事由（重大）

| # | 事由 |
|---|---|
| **重大 1** | **★判定層に「モードのときは」という分岐がある**（`§4.4-1`） |
| **重大 2** | **★判定窓・アルゴリズムを変えた**（`§2.2-1`） |
| **重大 3** | **★新しい調停 ／ 配送チャネル ／ 合流層を作った**（`§4.4-2`・`§4.4-3`） |
| **重大 4** | **★操作の種別を片側だけに足した**（`§2.3`） |
| **重大 5** | **操作の種別の列挙が 2 か所にある**（`E-76`） |
| **重大 6** | **★モードが時間で切れる**（`§4.1-3`） |
| **重大 7** | **★モード中に方向がステップになる**（`§4.1-4`） |
| **重大 8** | **モードから抜けられない、または溜めた列が黙って消える**（`§4.1-7`） |
| **重大 9** | **モードに入っていない間の挙動が変わった**（`§4.1-8`） |
| **重大 10** | **★最長一致になっていない**（`§4.2-3`） |
| **重大 11** | **同じ長さで複数残る場合に、どれかを選んで確定した**（`§4.2-4`） |
| **重大 12** | **先頭のゴミを補正した**（`§4.2-5`） |
| **重大 13** | **解決できなかった入力を黙って捨てた**（`§4.2-7`） |
| **重大 14** | **波形・軌跡・タイミングを解釈した**（契約 F-3） |
| **重大 15** | **`move_commands` ／ `internal/moveindex/` へ書き込んだ・形を変えた**（契約 F-3） |
| **重大 16** | **仮想側でモーションを実演させた**（`§1.3-1`） |
| **重大 17** | **★契約が凍結しているパッケージの diff がある**（`notation` ／ `preset` ／ `punishfinder` ／ `setplay`）**、または `migrations/` ／ `scripts/` ／ `docs/design/` の diff がある** |
| **重大 18** | **§6.5.1 の 3 つの守りが不変でない**（`§4.4-4`） |
| **重大 19** | **改名が本サブの中身と同じコミットに混ざっている、または挙動の diff を伴っている**（`§4.0`） |
| **重大 20** | **台帳未記載のストレージキーを使った**（CLAUDE.md §10.X） |
| **重大 21** | **★既存の解決表の絞り込みを広げた**（`§4.6-1`） |
| **重大 22** | **★既存の応答が変わった**（`§4.6-2`）**、または取得失敗で既存の入力経路が壊れる**（`§4.6-6`） |

---

## 9. ★このレビューで判定してはいけないこと

| # | 内容 | 理由 |
|---|---|---|
| 1 | **操作の種別が 4 値になったこと** | **`D-370` の「3 値。増やさない」は `M21-05` 時点の as-built であって恒久の上限ではない**（**D-377**）。**守るべきは「1 か所で定義し、両側が同じものを参照する」ことのほうである** |
| 2 | **入力ミスを補正すべきかどうか** | **開発者判断＝解決しない**（`§4.2-5`） |
| 3 | **モードを時間で切るべきかどうか** | **要件そのものが「時間で切れない」である**（`§4.1-3`） |
| 4 | **モードの抜け方の細部** | **`§9.2-1` で製造へ委ねた**（**溜めた列が黙って消えないことだけを見る**） |
| 5 | **溜まっている列の見せ方の細部** | **`§9.2-2` で製造へ委ねた** |
| 6 | **モード中に操作キーが来たときの扱い** | **`§9.2-3` で製造へ委ねた**（**どちらへ倒しても、溜めた列が黙って消えないことだけを見る**） |
| 7 | **`DES-005` §6.1 の死守 2（仮想側で実演させない）と矛盾すること** | **★矛盾しない。** **死守 2 は仮想側に掛かり、本サブは物理側である** |
| 8 | **契約 F-3 の「モーション解析はしない」と矛盾すること** | **★矛盾しない**（`§4.3`）。**契約 §5 自身が「衝突して見えるが衝突しない」と書いている** |
| **10** | **★`§4.6` の読み取り経路の新設が契約違反かどうか** | **【2026-08-14 追加＝D-379】違反ではない。** **契約 F-1 は `recipe_cache` の読み書き契約であり、`internal/service/inputresolve/` はどの条項でも凍結されていない**（**M-65**） |
| **11** | **★バックエンドを触ったこと自体** | **`§2.3` の例外条項で許容した。** **見るのは「範囲が読む経路 1 本に限られているか」と「単独コミットで分かれているか」だけである** |
| 9 | **設計書の文案** | **製造が文案を作らないのが正しい** |

---

*以上、M21-06 レビューチェックリスト v1.1.0。*
