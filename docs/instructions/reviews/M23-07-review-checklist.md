# M23-07 レビューチェックリスト

| 項目 | 内容 |
|------|------|
| 対象指示書 | `docs/instructions/M23-07-trash-missing-paths.md` v1.1.0（ゴミ箱の未達の導線） |
| 文書ID | M23-07-REVIEW |
| 対象指示書ID | M23-07 |
| レビューモデル | 高精度レビュー（実モデル名は `docs/human-notes/model-allocation.md` が正本。実使用は開発者判断） |
| バージョン | 1.0.1 |
| 作成者・作成日 | 設計担当 Claude（設計卓／M23 起票期）/ 2026-08-23 |

> 文書ID ⇄ 実パスの正は `docs/handover/docs-map.md`（自動生成）。

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| 1.0.1 | 2026-08-23 | **★対象指示書の版数を v1.1.0 へ追随した**（**D-524**＝並列性欄の訂正）。**★レビューの内容は変えていない。** |
| 1.0.0 | 2026-08-23 | 初版（指示書 v1.1.0 と対）。**最重要ゲート＝「セットプレイの削除がコンボの削除 API を叩かないこと」と「画面操作だけでセットプレイをゴミ箱へ入れて戻せること」。** |

---

## 0. レビュー担当への前置き

### 0.1 レビュー前の準備

1. **★指示書 §1.1 / §1.3 を先に読む**——**本サブは配線が片方しか無い箇所を 3 つつなぐサブである。** **★`M23-06` がルート列を消した裁定と、本サブが詳細でレシピを出す判断は矛盾しない**（一覧は識別、詳細は確認）。
2. **`DES-005` §5.15 を読む**（**★列構成は `CHANGE-127` で確定済み。本サブはそれを前提にする**）。
3. **完了報告に Plan Mode の実査 10 件（指示書 §3.3）と破壊確認 3 件（§5.3）が揃っていることを先に確認する。**

### 0.2 レビューの基本姿勢

- **★★「セットプレイの削除がコンボを消しうるか」を最初に見る。** **取り違えると利用者のコンボが消える。**
- **★本サブは画面の作り込みが多い。「もっと親切にできる」を指摘しない。** **指示書が定めた導線が通っているかを見る。**
- **★詳細でレシピを出していることを `M23-06` の裁定への違反として指摘しないこと**（§0.3 N-1）。

### 0.3 ★重大でないもの（**誤判定しないこと**）

| # | 誤判定しやすい状態 | 実は仕様である理由 |
|---|---|---|
| N-1 | **読み取り専用詳細にレシピが出る**（一覧のルート列は消してあるのに） | **指示書 §1.3 / §4.2-4。★矛盾ではない。** **一覧は識別、詳細は確認であり、目的が違う。** **★完全削除は不可逆である** |
| N-2 | **`GET /api/combos/{id}` が削除済みを返さないまま** | **指示書 §4.2-2。★意図である。** **求めている集合が違えば入口も別に要る**（`M23-06` の教訓） |
| N-3 | **`FindByIDAllowDeleted` に述語が足されていない** | **★`M23-03` が意図的に塞がなかった箇所である。** **本サブのために残された道具でもある** |
| N-4 | **削除済みコンボを編集できない** | **指示書 §1.5-1 / §4.2-3。★読み取り専用である。編集したければ復元する** |
| N-5 | **ゴミ箱のセットプレイ行がクリックで遷移しない** | **指示書 §4.2-5。★セットプレイは親コンボ経由でしか画面に出ないため、単独の詳細という概念が無い**（`VAL-S05`） |
| N-6 | **紐付けを解除しても完全削除が自動で再実行されない** | **指示書 §4.3-3。★意図である。** 解除の結果を見てから決められるようにするため |
| N-7 | **読み取り専用取得が `recipe_cache` へ書き戻さない** | **指示書 §4.2-4-1。★`M23-06` が固定した不変である** |
| N-8 | **ゴミ箱以外の画面に日本語直書きが残っている** | **指示書 §1.5-8 / §4.4-1。★射程はゴミ箱画面だけである** |
| N-9 | **コンボ同時登録で `VAL-S07` が出ないまま** | **指示書 §1.5-7。`M23-CLOSE`** |
| N-10 | **サーバ側の削除・完全削除が直っていない**（CASCADE / Tx 位置 / 削除日時） | **指示書 §1.5-6。`M23-08`** |

### 0.4 報告フォーマット

- 各節「OK / 重大（§9）/ 軽微（§10）/ 質問（§11）」。

---

## 1. 設計・パターンとの照合（最重要）

- [ ] **★★セットプレイの論理削除の導線が、セットプレイの API を叩く。コンボの削除 API を叩かない**（指示書 §4.1）。
- [ ] **★★画面操作だけでセットプレイをゴミ箱へ入れ、戻せる**（同 §7.1-1）。**★これが本サブの主目的である。**
- [ ] **論理削除の前に確認が出る。参照しているコンボの件数が出る**（同 §4.1-2）。
- [ ] **削除後のトーストからゴミ箱へ行ける**（同 §4.1-3）。
- [ ] **ゴミ箱のコンボ行をクリックすると読み取り専用詳細が開く**（同 §4.2）。
- [ ] **★遷移リンクの全数が処理されている**（同 §3.3-8。**★行全体と列内で行き先が違わないこと**）。
- [ ] **★読み取り専用取得が新設の経路である。`GET /api/combos/{id}` にフラグが足されていない**（同 §4.2-2）。
- [ ] **★`FindByIDAllowDeleted` を使っている。新しく取得処理を作っていない**（同 §4.2-2）。
- [ ] **読み取り専用詳細に編集・削除の操作が出ない**（同 §4.2-3）。**★押せないボタンを並べていないこと。**
- [ ] **レシピが `combo_steps` から組み立てられている。`recipe_cache` へ書き戻していない**（同 §4.2-4）。
- [ ] **★レシピ解決に失敗しても詳細そのものが落ちない**（同 §4.2-4-2）。
- [ ] **ゴミ箱のセットプレイ行がクリックで遷移しない。遷移しないことが見た目で分かる**（同 §4.2-5）。
- [ ] **完全削除を拒まれたとき、参照しているコンボが見える。新しくデータを取りに行っていない**（同 §4.3-1）。
- [ ] **★解除が「紐付けを外す」であって「コンボを消す」ではないことが文言で伝わる**（同 §4.3-2）。
- [ ] **紐付け解除の後、完全削除が自動で再実行されない**（同 §4.3-3）。
- [ ] **既存の紐付け解除の API を使っている。新しい API を作っていない**（同 §4.3）。

## 2. データ・API 契約・スキーマの不変（非破壊性）

- [ ] **スキーマを変えていない。マイグレを 1 本も足していない**（指示書 §4.6）。
- [ ] **`GET /api/combos/{id}` の挙動が変わっていない**（同 §2.2）。
- [ ] **削除・復元・完全削除の副作用が変わっていない**（同 §2.2）。
- [ ] **`M23-06` が確定した列構成が変わっていない**（同 §2.2。**★列を増減していないこと**）。
- [ ] **`DELETE /api/setups/{id}` の挙動が変わっていない**（同 §1.5-3。**★API は既に在り、呼ぶ画面が無かっただけである**）。
- [ ] **`VAL-*` の判定内容が変わっていない**（同 §2.2）。

## 3. 画面の仕様

- [ ] **ゴミ箱画面に日本語直書きが残っていない**（指示書 §4.4-1。**★全数が §3.3-9 の実測と一致していること**）。
- [ ] **ja / en の両方が揃っている。**
- [ ] **一括操作バーが両方の表から届く**（同 §4.4-2）。
- [ ] **★選択が残っている間だけ操作ボタンが出る。結果の表示は選択が空になっても残る**（同 §4.4-2。**★`M23-06` が確定した規律の退行防止**）。
- [ ] **★複数の警告を連結するときの区切りと終端が翻訳キーで持たれている**（`DES-006` §11.3）。

## 4. テストの妥当性（ケース数で確認。指示書 §5 ↔ §7 DoD）

- [ ] **サーバ 7 件・フロント 11 件が揃っている**（指示書 §5.1 / §5.2）。
- [ ] **★★§5.2-1（セットプレイの削除が正しい API を叩く）が在る。** **★最重要ゲートのテストである。**
- [ ] **★★破壊確認 3 件が実施され、コマンドと出力が完了報告に在る**（同 §5.3）。
- [ ] **★破壊確認 1 の fixture で、コンボ id とセットプレイ id が別の値である**（同 §5.3-1。**★`M23-06` は同型の破壊確認が id 衝突で空振りした**）。
- [ ] **§5.1-2（`GET /api/combos/{id}` が削除済みを返さない）が在る**（★退行の防止）。
- [ ] **§5.1-6（`recipe_cache` へ書き戻さない）が在る。**
- [ ] **§5.2-11（結果表示の規律）が在る**（★`M23-06` の退行防止）。
- [ ] **★フロントテストが実 `ja.json` を引く `t` で書かれている**（同 §5.2-10。**キー返しモックでは i18n 化の主張が空振りする**＝`M23-04` 教訓 2）。
- [ ] **E2E 3 本が足されている**（同 §5.4）。**★とくに「セットプレイを画面から削除 → ゴミ箱 → 復元」は `M23-02` 以来 1 度も通せなかった経路である。**
- [ ] **テスト結果が件数付きで報告されている**（同 §7.2。**★終了コードだけを根拠にしていない**＝`E-125`）。

## 5. 設計意図との整合（精神の確認）

- [ ] **★否定形確認 3 件が完了報告に理由付きで在る**（指示書 §4.7）。
- [ ] **★★実装コード内の `M23-07` への言及が、本サブの完了に合わせて処理されている**（同 §4.5）。**★1 件ずつ判断されていること**（消したのか、送り先を書いたのか）。
- [ ] **★「一覧は識別、詳細は確認」の区別が実装に出ている**（同 §1.3）。
- [ ] **★セットプレイの削除がコンボの削除と混ざらない作りになっている**（同 §4.1-1）。

## 6. コード品質・規約遵守

- [ ] **`CLAUDE.md` §4 の Go / TypeScript 規約。** JSON タグ camelCase。
- [ ] **`console.log` / `fmt.Println` が本番コードに残っていない。**
- [ ] **`any` を使っていない。**
- [ ] **`TODO` が `// TODO(<対応予定>): <内容>` 形式である。**

## 7. 既存挙動の温存（非破壊性）

- [ ] **既存の Go テストが全て緑である**（件数付き）。
- [ ] **既存のフロントテストが全て緑である**（件数付き）。
- [ ] **`M23-01`〜`M23-06` が足したテストが緑のままである**（指示書 §5.5）。
- [ ] **`make e2e` が緑である。**
- [ ] **ゴミ箱以外の画面が壊れていない**（**★セットプレイの削除の導線を足した画面が、既存の機能を失っていないこと**）。

## 8. ドキュメント・進捗ログ

- [ ] **`docs/progress/progress-log.md` へ 1 行追記されている**（`CLAUDE.md` §8）。
- [ ] **★レビュー結果を参照する記述が、レビュー後に埋められている**（指示書 §7.4。**`M23-04` 教訓 3**）。
- [ ] **完了報告に指示書 §7.5 の 8 点が揃っている。**
- [ ] **★読み取り専用取得の経路名と応答の逐語形／導線の置き場とその理由／バーの置き場とその理由が在る**（同 §7.5-2 〜 4。**★`CHANGE-128` の反映がこれ待ちである**）。
- [ ] **★設計書本体（`DES-005` / `DES-002`）が編集されていない**（反映は設計卓が `CHANGE-128` で行う）。
- [ ] **`followup-backlog.md` へ書いた場合、§J だけを触っている**（**D-382**）。

---

## 9. 重大な問題の判定基準（完了承認を妨げる）

- **★★セットプレイの削除の導線が、コンボの削除 API を叩きうる**（指示書 §4.1）。**これが最重要ゲートである。** **★利用者のコンボが消える。**
- **★★画面操作だけではセットプレイをゴミ箱へ入れられないまま**（同 §7.1-1）。**★本サブの主目的が達成されていない。**
- **★§5.2-1 のテストが無い。**
- **★破壊確認 3 件が実施されていない、または出力が報告に無い**（同 §5.3）。
- **★破壊確認 1 の fixture でコンボ id とセットプレイ id が同じ値である**（同 §5.3-1。**★テストが空振りする**）。
- **ゴミ箱の行をクリックすると依然として 404 になる**（同 §4.2）。
- **遷移リンクの一部だけが処理されている**（同 §3.3-8）。
- **`GET /api/combos/{id}` にフラグが足されている**（同 §4.2-2）。
- **`FindByIDAllowDeleted` に述語が足されている**（**★`M23-03` の裁定を壊す**）。
- **読み取り専用詳細から編集・削除ができる**（同 §4.2-3）。
- **読み取り専用取得が `recipe_cache` へ書き戻している**（同 §4.2-4-1）。
- **レシピ解決の失敗で詳細そのものが落ちる**（同 §4.2-4-2）。
- **紐付け解除の後に完全削除が自動で再実行される**（同 §4.3-3）。
- **紐付け解除のために新しい API を作っている**（同 §4.3）。
- **`M23-06` が確定した列構成を変えている**（同 §2.2）。
- **`M23-06` が確定した結果表示の規律が退行している**（同 §4.4-2）。
- **スキーマ変更・マイグレの追加がある**（同 §4.6）。
- **スコープ外 8 件（同 §1.5）のいずれかへ踏み込んでいる。**
- **指示書 §7.5 の 8 件のいずれかが完了報告に無い。**

## 10. 軽微な問題の判定基準（持ち越し許容）

- セットプレイの削除の導線の置き場（**★理由が報告に在れば足りる**）。
- 読み取り専用詳細の URL の形と画面の作り。
- 一括操作バーの置き場（**★両方の表から届いていれば足りる**）。
- 文言の言い回し。
- テストのデータ組み立ての作法。

## 11. 質問・確認事項のフォーマット

- 「指示書 §X.X / `DES-00X` §X / `m23-06-completion-report` §X に対し実装が Z。意図確認したい」の形で根拠節併記。

## 12. レビュー完了の判定

- §1〜§8 が全て OK、§9 重大ゼロ（**特に「セットプレイの削除の宛先」「画面操作だけで入れて戻せること」「破壊確認 3 件と fixture の id」**）、§0.1 の Plan Mode 実査 10 件が揃っている。§10 軽微は持ち越し可。

### 12.1 ★本チェックリスト自身の検査（**発行前に設計担当が行う**）

- [ ] **本書と対の指示書の間に、節番号の食い違いが無い。**
  → 本書が参照する指示書の節は §1.1 / §1.3 / §1.5 / §2.2 / §3.3 / §4.1 / §4.2 / §4.3 / §4.4 / §4.5 / §4.6 / §4.7 / §5 / §7 であり、**指示書側に実在する**。
- [ ] **指示書が禁じていない事項を「重大」として求めている項目が無い。**
  → §9 の各項目は指示書 §1.5 / §2.2 / §4 の禁止・固定と 1 対 1 で対応する。
- [ ] **達成不能な条件を重大として求めていない。**
  → **「一覧にもレシピが出ること」を求めていない**（§0.3 N-1。**★`M23-06` がルート列を消した**）。
  → **「`GET /api/combos/{id}` が削除済みを返すこと」を求めていない**（§0.3 N-2。**別の入口を作る**）。
  → **「セットプレイの詳細画面が在ること」を求めていない**（§0.3 N-5。**単独の詳細という概念が無い**）。
  → **「削除済みコンボを編集できること」を求めていない**（§0.3 N-4。**読み取り専用**）。

---

*以上、M23-07 レビューチェックリスト v1.0.1。配置 `docs/instructions/reviews/M23-07-review-checklist.md`。指示書 v1.1.0 と対。「セットプレイの削除の宛先」と「画面操作だけで入れて戻せること」を最重要ゲートとする。*
