# M37-03 レビューチェックリスト（一覧・マイコンボ・ゴミ箱の 3 件）

| 項目 | 内容 |
|------|------|
| 文書ID | M37-03-REVIEW-CHECKLIST |
| バージョン | **1.0.0**（2026-09-13・発行） |
| 対象指示書 | `docs/instructions/M37-03-list-mycombo-trash-fixes.md` **v1.0.0** |
| 作成者 | 設計担当 Claude（設計卓） |
| 位置づけ | **レビュー担当の主入力**（`D-736`） |

---

## 0. ★★レビュー担当への前置き

**★★★本サブは 3 件の*別々の*要求である。⇒ 「まとめて実装しました」で通さないこと。**

**★完了報告が 3 件を別々に判定していなければ、それだけで差し戻す理由になる**（`M37-overview` §1.2＝「別々の要求と受入条件は消さない」）**。**

**★★重心は `B15`（ヒット種別の追加）である。⇒ 列挙値を 1 つ足す作業は、1 か所忘れても*動いてしまう*。**

---

## 1. 束 A — ★★★`B15`（**本サブの重心**）

| # | 観点 |
|---|---|
| **A-1** | **★★★出発点が*実測*か。⇒ `DES-003` §3.2 の 3 値**（`normal` / `counter` / `punish_counter`）**をそのまま出発点にしていないか。★`M27-01` 以降に増えている可能性が高い** |
| **A-2** | **★★設計書と実測が食い違ったなら、その事実が報告に在るか。⇒ 食い違いは成果である** |
| **A-3** | **★★★同期がすべて満たされているか**〔Go 定数 ／ TS 定数 ／ ラベル **ja と en の両方** ／ フィルタの選択肢 ／ CSV の受け口と検証 ／ `CHECK` 制約〕**。★1 か所でも欠けたら不合格である** |
| **A-4** | **★★`bash scripts/check-enum-sync.sh` の*出力*が読まれているか。⇒ 既定は warning であり、終了コードだけでは判定できない** |
| **A-5** | **★★★§5-4 の 5 面すべてが実測されているか**〔保存 ／ 一覧 ／ フィルタ ／ 詳細 ／ CSV 往復〕**。⇒ 1 面だけの確認は不足である** |
| **A-6** | **★★★§5-5 の破壊確認が在るか＝定数の片側だけを消すと `check-enum-sync.sh` が拾うこと。⇒ 「回したら緑だった」では、機構が生きている証拠にならない** |
| **A-7** | **★既存データの `hit_type` が 1 行も変わっていないか**（差分 0 の実測） |
| **A-8** | **★コードとラベルの選定理由が在るか。⇒ 既存値の命名と揃っているか**（設計卓の案は `air_hit`。**★違う案を採ったなら理由が要る**） |

---

## 2. 束 B — ★★`B03`（マイコンボのタグ固定表示）

| # | 観点 |
|---|---|
| **B-1** | **★★★ブラウザストレージを使ったなら、`web/CLAUDE.md` §1 の台帳へキーが足されているか**（`CLAUDE.md` §10.X）**。⇒ `bash scripts/check-browser-storage-keys.sh` が緑か** |
| **B-2** | **★★保存先の選定理由が在るか。⇒ 一覧の列設定など既存の先例と揃っているか**（指示書 §2.1-4） |
| **B-3** | **★★DB を選んだなら、止まって設計卓へ請求した記録が在るか**（指示書 §2.3-3）**。⇒ 黙って列を足していないか** |
| **B-4** | **★固定したタグが、絞り込みを変えても残るか**（§5-2 の実測） |
| **B-5** | **★キーにバージョンが付いているか**（`web/CLAUDE.md` §1 のキーバージョニング）**。⇒ try-catch で囲まれているか** |

---

## 3. 束 C — ★`B01`（ゴミ箱の並び）

| # | 観点 |
|---|---|
| **C-1** | **★★「並び順」を見ているか。⇒ 「削除日時が表示されている」で閉じていないか**（指示書 §0.3） |
| **C-2** | **★既定が削除日時の降順か。⇒ 実データで 2 件以上を別時刻に削除した実測が在るか** |
| **C-3** | **★段 1 で「既に降順であった」と分かったなら、証拠付きで閉じているか**（`M37-overview` §0.2。**★実装しないのが正しい**） |
| **C-4** | **★並び替えを足さない判断をしたなら、理由が報告に在るか** |

---

## 4. 束 D — ★★越境と自採番

| # | 観点 |
|---|---|
| **D-1** | **★★★マイグレを自採番していないか**（`D-293`）**。⇒ 払い出し済みは `000113` の 1 本だけである。★2 本目を勝手に取っていないか** |
| **D-2** | **★★`000113` を消費したか / しなかったかが明記されているか**（**★`CHECK` 制約が無ければ消費しないのが正しい**） |
| **D-3** | **★`hit_type` の既存値を変えていない / 消していないか。⇒ 足すだけか** |
| **D-4** | **★設計書本体を書き換えていないか**（`CLAUDE.md` §8）**。⇒ as-built は設計伝達レポート §4 へ** |
| **D-5** | **★★★`docs/handover/followup-backlog.md` を 1 文字も編集していないか**（`D-838`） |
| **D-6** | **★`B02` / `B04`〜`B14` に手を出していないか**（`M37-02` / `M37-04` の射程） |
| **D-7** | **★一覧の横幅・列の並びに手を出していないか**（`M27-03` / `P-21` の面） |
| **D-8** | **★★着手前の版ゲートを通したか**（指示書 §0.4 の 4 点）**。⇒ とくに 3＝`ls migrations/` の最新が `000112` であったか**（計測点 `M-169`） |

---

## 5. 束 E — ★検査と説明書

| # | 観点 |
|---|---|
| **E-1** | **`bash scripts/check-enum-sync.sh` ／ `check-browser-storage-keys.sh` ／ `check-migration-license.sh`**（マイグレを足した場合）**が緑か** |
| **E-2** | **`go test ./...` / `pnpm test` / `pnpm tsc --noEmit` / `pnpm lint` が緑か。★`eslint-disable` を足していないか** |
| **E-3** | **`make e2e` を*全数*回しているか**（`SUPP-001` §5.5.7 (i)） |
| **E-4** | **`check-import-order.sh` がベースラインから増えていないか**（`D-388`） |
| **E-5** | **★説明書の追随が在るか**（指示書 §2.5）**。⇒ `M32-02` が未着地なら、変わった点の列挙が完了報告に在るか** |
| **E-6** | **★新しい `data-testid` を足したなら `docs/design/testid-convention.md` に沿っているか** |

---

## 6. ★★見なくてよいもの

| # | 何を | なぜ |
|---|---|---|
| 1 | **一覧の横幅** | **★`M27-03` / `P-21` の面** |
| 2 | **`B10` の告知の形** | **★`P-59`。⇒ `M37-02` の射程** |
| 3 | **`B06` の確定反撃候補** | **★`P-58`。⇒ `M37-04` の射程** |
| 4 | **区分表が Go と TS に二重にあること** | **★`followup` の `position-bands-duplicated-go-and-ts`** |
| 5 | **CHANGE の起票** | **★製造は自採番しない**（`D-293`）**。⇒ 請求は報告 §6 へ** |

---

## 7. ★★3 件の判定表（**★レビューはこの形で答えること**）

| ID | 完了 / 一部完了 / 未着手 | 証拠 |
|---|---|---|
| `B01` | | |
| `B03` | | |
| `B15` | | |

**★★「既に満たしていた」とする項目には、*今回の要求を満たす具体的な証拠*が要る**（`M37-overview` §0.2）**。⇒ 類似機能の過去の完了を根拠にしないこと。**

---

*以上、M37-03 レビューチェックリスト **v1.0.0**。* **★★★束 A が本体である。⇒ とくに A-3（同期の全数）と A-6（破壊確認）。★列挙値を足す作業は 1 か所忘れても動くため、テストでは出ない。** **★★次に重いのは §7 である**——**3 件を別々に判定させること。⇒ 束ねた report は、どれが満たされたか分からない。**
