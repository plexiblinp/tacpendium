# M24-05 レビューチェックリスト: セットプレイ導線・紐付け UI 統合

| 項目 | 内容 |
|------|------|
| 文書ID | M24-05-review-checklist |
| バージョン | 1.1.0 |
| 対象指示書 | `docs/instructions/M24-05-setup-navigation-and-link-ui.md` **v1.2.0** |
| 推奨モデル | **高精度レビュー**（**★2 系統の UI を消す判断が正しく検証されたかの照合が中心である**） |
| 作成者・作成日 | 設計担当 Claude（設計卓・M24 期）/ 2026-08-29 |

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| 1.1.0 | 2026-08-30 | **★★完了後の是正である**（**D-604**・計測点 **`M-95`**）。**★設計卓が指示書を v1.1.0 へ改版したとき、本書を v1.0.0 のまま残した。⇒ 本書は実装中ずっと指示書と食い違っていた。** **★製造は指示書 v1.1.0 を正として作業しており、レビューも同じ突合に到達している。⇒ 実害は出ていない。** **★★本改版は「次に読む人を誤らせない」ためのものであり、レビューをやり直すためのものではない。** **★失効した記述は消さず、§0.0 に一覧として残した**（**どこが食い違ったかが本書の価値である**）。 |
| 1.0.0 | 2026-08-29 | 初版（**D-587**） |

---

## 0.0 ★★本書 v1.0.0 は指示書 v1.1.0 と食い違っていた（**2026-08-30 是正・`M-95`**）

**★★以下の 9 点は v1.0.0 の記述であり、指示書 v1.1.0 では成立しない。** **★本文中の該当項目は残してあるが、判定には使わないこと。**

| 本書 v1.0.0 の記述 | 指示書 v1.1.0 の実態 |
|---|---|
| **「§3.3 の実査 12 件」**（1-13） | **§3.3 表は 9 行**（番号 1/2/3/6/7/8/9/11/12。**4・5・10 が欠番**）。**★指示書 §7.1-1 自身も「12 件」と書いており失効していた**（v1.2.0 で是正） |
| **「E2E 4 ケース」**（5-1） | **§5.1 は 3 ケース** |
| **「破壊確認 5 件」**（§6 見出し・6-1）／**「破壊確認 5 が赤くなっているか」**（6-2） | **§5.3 は 3 件。破壊確認 5 は存在しない** |
| 参照先 **`§3.3-4` / `-5` / `-10`**（1-7 / 1-10 / 4-3） | **v1.1.0 に存在しない**（`SM-146` とともに削られた行） |
| 参照先 **`§7.1-8`**（§0.3-6） | **v1.1.0 に存在しない**（§7.1 は 7 → 9 で 8 が欠番） |
| **1-7 / 4-2〜4-4**（`SM-146` の導線・「最後の技」の定義） | **`D-588` で射程外。本サブに該当しない** |
| **1-10**（`attack_type` の表示名の実測） | **同上**（`SM-088` の暫定案に紐づく項目。**★実装の書式は暫定案と違った**＝`DES-005` §5.9.2） |
| **5-4**（「最後の技」が非技ステップのときのテスト） | **同上** |

> **★★あわせて記録する**——**指示書 v1.1.0 が破壊確認 5 を落とした結果、本書 §8 が「最大の危険 2 件」の 1 つとして挙げていた「入力済みのセットプレイ名を自動生成で上書きしている」を見るテストが、どの層にも無くなっていた。**
>
> **⇒ 製造がレビュー取り込みで `SetplaySuggestionSection.test.tsx` へ 1 ケース足し、破壊確認で赤くなることも確かめた。**
>
> **★★一般形＝射程を削るときは、その項目に紐づく観測も一緒に消えることを見ること**（playbook §4.49）。

---

## 0. レビュー担当への前置き

### 0.1 レビュー前の準備

1. 指示書 **v1.0.0** と `CHANGE-140` 通知書を全文読む。
2. 完了報告 `docs/progress/M24-05-completion-report.md` を読む。
3. **★★`DES-005` §5.6 項目11 / 項目12 を読む**——**項目11（転用可能なセットプレイ候補＝FR011）と項目12（自動提案＝`M19-01`）は別系統である。★混同すると 1-2 を判定できない。**
4. **★`DES-005` §5.6 の「`/setups/:id` は実装しない」の文脈を読む**——**★成立条件の入力面の話であり、画面そのものの否定ではない**（指示書 §1.2）。
5. **★`DES-006` §3（VAL-S 系）を読む**（**★`VAL-S06`＝名前必須**）。
6. **★`DES-004` §2.2 を読む**（**★`move_id` が NULL の非技ステップ。1-7 の判定に要る**）。

### 0.2 レビューの基本姿勢

コードを変更せず、指摘のみを行う。

### 0.3 ★★重大でないもの（**誤判定しないこと**）

| # | これは欠陥ではない |
|---|---|
| **1** | **★★「既存から紐付け」が無くなっていること。** **指示書 §4.1 が本サブの目的そのものとしている**（memo の逐語＝**「候補表示の方を採用し紐付けボタンは消したい」**） |
| **2** | **★セットプレイ画面のレイアウトが `M24-12` の形になっていないこと**（ボタン群・順送り・1 カラム縦積み）。**指示書 §1.6-1 がスコープ外としている**（followup `setup-editor-not-on-shared-editor-parts`） |
| **3** | **★`VAL-S04` の check-then-act 競合が直っていないこと。** **指示書 §1.6-3 がスコープ外としている**（`M24-13` と同じ形の作業） |
| **4** | **★`/setups/:setupId` に成立条件（`combo_setup_results`）の入力が無いこと。** **`DES-005` §5.6 / §5.9 項目6 が「親が複数あり得るため置かない」と定めている。★★逆に足していたら「重大」**（2-6） |
| **5** | **★セットプレイ自動提案（項目12）が変わっていないこと。** **指示書 §1.6-5 が「別系統。触らない」と定めている。★★逆に触っていたら「高」**（2-5） |
| **6** | **★確認事項 1〜3 の回答が出ず、暫定案で実装されていること。** **指示書 §7.1-8 が「それは欠陥ではない」と明記している。★ただし暫定案で進めた旨の報告は要る** |
| **7** | **★「その症状は既に解消していた」として実装していない項目があること**（指示書 §9.3）。**★成立をテストで固定していることは要る** |
| **8** | **★候補 0 件のときの文言・導線の置き場・コピー先の親の決め方が設計卓の想定と違うこと**（§9.2 で委任済み）。**★選んだ理由の報告は要る** |

### 0.4 報告フォーマット

指摘は **重大 / 高 / 中 / 低** で分類し、各件に**根拠**（ファイル:行 または 出力の逐語）を添える。

---

## 1. 設計・パターンとの照合（最重要）

| # | 観点 | 判定 |
|---|---|---|
| **★★1-1** | **★★「既存から紐付け」と「転用可能なセットプレイ候補」の母集団が同じであることが実測で確かめられ、報告されているか**（§3.3-2） | **★★確かめずに消していたら「重大」**——**「役割が被っている」という前提そのものが検証されていない。⇒ 消してよい根拠が無い** |
| **1-2** | **★セットプレイ自動提案（`DES-005` §5.6 項目12）を消していないか**（**★項目11 とは別系統である**） | **消していたら「重大」**——**目的が違う機能を巻き添えにしている** |
| **1-3** | **★`LinkExistingSetupModal` の入口の全数が母数付きで報告され、すべて処理されているか**（§3.3-1） | **母数の欠落は「高」。取り残しは「高」**——**呼ばれない部品が残る** |
| **1-4** | **★候補 0 件のときにセクションが出て「候補はありません」が読めるか**（§4.1） | **セクションごと消えているなら「高」**——**紐付ける入口が画面から消え、「機能が消えた」と読まれる** |
| **1-5** | **★候補表示から紐付けられるか**（§4.1） | できないなら「重大」 |
| **1-6** | **★セットプレイのレシピ表示が `RecipeText` を通っているか。通っていない箇所の母数が報告されているか**（§3.3-6 / §4.2） | **母数の欠落は「高」。★`RecipeText` を作り替えていたら「重大」**（通す側を変える） |
| **★1-7** | **★★「最後の技」の定義が報告されているか**（§3.3-4 / §4.3）。**`move_id` が NULL の非技ステップが末尾に来た場合の扱いが決まっているか** | **★決めずに実装していたら「高」**——**`DES-004` §2.2 が「技ステップ以外は `move_id` を NULL とする」と定めており、末尾が NULL になりうる。★`SM-146` と `SM-088` で 2 つの定義を作っていたら「高」** |
| **★★1-8** | **★★セットプレイ名の自動生成が「空のときだけ」働くか。入力済みの名前を上書きしていないか**（§4.4） | **★★上書きしていたら「重大」**——**利用者の入力を壊す。★破壊確認 5 がそれを固定している** |
| **★1-9** | **★`VAL-S06`（名前必須）が撤回されていないか**（§1.6-4） | **撤回していたら「重大」**——**memo の逐語は「自動で必須項目をコードが埋める」であり、必須を外せとは言っていない** |
| **1-10** | **★`attack_type` の表示名が実測され、勝手に言い換えられていないか**（§3.3-10 / §4.4） | **言い換えていたら「高」**——**既存の表示語彙が割れる** |
| **1-11** | **★コピーがコンボ側と同じ形か。★成立条件を引き継いでいないか**（§4.5） | **作法が違うなら「中」。★成立条件を引き継いでいたら「高」**（先例＝コンボのコピーは引き継がない） |
| **1-12** | **★`SM-052` の実体が実査され、`RecipeText` で解けたか／別の原因かが報告されているか**（§3.3-7） | 報告が無いなら「高」 |
| **1-13** | **★§3.3 の実査が実施され、母数の要る項目に母数があるか**（**★★2026-08-30 是正＝v1.0.0 は「12 件」と書いていたが指示書 v1.1.0 の §3.3 表は 9 行である**＝§0.0） | 母数の欠落は「高」。実査の欠落は「高」 |
| **★1-14** | **★★`M24-12` の成果が 1 つも落ちていないか**〔離脱ガード（セットプレイ側は 3 フックが通っている） ／ 保存後の遷移も補正を通す〕（§2.2 / §3.3-11） | **落ちていたら「重大」** |

---

## 2. データ・API 契約・スキーマの不変（非破壊性）

| # | 観点 | 判定 |
|---|---|---|
| **2-1** | **`internal/` に差分があるなら、§2.3 の 3 条件が満たされ報告されているか** | **報告が無いなら「重大」** |
| **2-2** | **`DES-002` §4.2 の経路表に載る既存 API の挙動を変えていないか**（**新設のみ**） | 変えていたら「重大」 |
| **2-3** | **`internal/service/notation/` ／ `internal/service/preset/` に触れていないか**（契約 F-1） | 触れていたら「重大」 |
| **2-4** | **`DES-003` のスキーマに触れていないか。マイグレーションを消費していないか** | 触れていて報告が無いなら「重大」 |
| **2-5** | **★`DES-005` §5.6 項目12（自動提案）に触れていないか**（§1.6-5） | 触れていたら「高」 |
| **★2-6** | **★★`/setups/:setupId` へ成立条件（`combo_setup_results`）の入力を足していないか**（§1.6-2） | **★★足していたら「重大」**——**`DES-005` が「親が複数あり得るため置かない」と定めている。★選択の見落としで別のコンボの条件を書き換え、しかもエラーにならない** |
| **2-7** | **★`DES-006` の VAL-S 系の判定そのものを変えていないか**（§1.6-4） | 変えていたら「重大」 |
| **2-8** | **新規依存が追加されていないか**（`CLAUDE.md` §6） | 追加されていて提案の記録が無いなら「重大」 |

---

## 3. ブラウザストレージ（**`CLAUDE.md` §10.X**）

| # | 観点 | 判定 |
|---|---|---|
| **3-1** | **キーを新設・変更した場合、`web/CLAUDE.md` §1 の台帳へ登録されているか** | 未登録なら「重大」 |
| **3-2** | **`web/src/lib/browser-storage.ts` 経由で読み書きしているか** | 直接 API を叩いていたら「高」 |
| **3-3** | **`bash scripts/check-browser-storage-keys.sh` が緑か** | 赤なら「高」 |

---

## 4. フロントエンドの動作仕様

| # | 観点 | 判定 |
|---|---|---|
| **4-1** | **コンボ詳細に「既存から紐付け」が無いか**（§4.1） | 残っていたら「高」 |
| **4-2** | **セットプレイからコンボへ飛べるか**（§4.3） | 飛べないなら「高」 |
| **4-3** | **飛び先が §4.3 の暫定案（一覧を始動技で絞る）か、違うなら理由が報告されているか** | 理由が無いなら「中」 |
| **4-4** | **★飛び先が 0 件のときに「無い」ことが分かるか** | 分からないなら「中」 |
| **4-5** | **セットプレイ編集にコピーがあるか**（§4.5） | 無いなら「高」 |
| **4-6** | **★名前を空で保存すると自動生成された名前が入るか。★保存前に見えて直せるか**（§4.4） | **BE で埋めていて利用者が見られないなら「高」** |
| **4-7** | **★i18n の新規キーが `ja` / `en` の両方に足されているか** | 片方だけなら「中」 |

---

## 5. テストの妥当性

| # | 観点 | 判定 |
|---|---|---|
| **5-1** | **E2E が §5.1 のケース数だけ揃っているか**（**★★2026-08-30 是正＝v1.0.0 は「4 ケース」と書いていたが指示書 v1.1.0 の §5.1 は 3 ケースである**＝§0.0） | 欠けているなら「高」 |
| **★5-2** | **★★「既存から紐付けが無い」を確かめる E2E が、可視性を要求するマッチャを使っているか** | **`toHaveCount` / `toHaveAttribute` だけなら「高」**——**隠れた要素にも通る**（`M24-12` の教訓） |
| **5-3** | **純粋関数のテストが揃っているか**（名前の自動生成 ／ 「最後の技」を求める関数） | 欠けているなら「高」 |
| **★5-4** | **★「最後の技」が非技ステップのときのテストが在るか** | **無いなら「高」**——**決めた扱いが守られているかを見る唯一のテストである** |
| **5-5** | **★「既存から紐付け」を踏んでいた既存 spec の母数が報告され、候補表示の経路へ書き換えられているか**（§5.2） | **母数の欠落は「高」。★★検証内容を緩めていたら「重大」** |
| **5-6** | **★`M19-03` / `M19-07` の spec が落ちていないか** | 落ちていたら「重大」 |
| **5-7** | **★基準値が同一セッション内の背中合わせで採られているか**（playbook §4.37） | 別時点の値どうしを引き算していたら「高」 |
| **5-8** | **`getByRole` の `name` の部分一致に足をすくわれていないか**（**破壊確認が「追記」ではなく「置換」で行われているか**） | 追記で行っていたら「高」 |

---

## 6. 破壊確認（**指示書 §5.3**。**★★2026-08-30 是正＝v1.0.0 は「5 件」と書いていたが v1.1.0 の §5.3 は 3 件であり、破壊確認 5 は存在しない**＝§0.0）

| # | 観点 | 判定 |
|---|---|---|
| **6-1** | **§5.3 の全件が実施され、コマンドと出力が載っているか**（**★2026-08-30 是正＝「5 件」は失効**） | 欠けているなら「高」 |
| ~~**★★6-2**~~ | **★★【2026-08-30 失効＝§0.0】** ~~破壊確認 5（入力済みの名前も上書きする）が赤くなっているか~~ **⇒ 指示書 v1.1.0 が破壊確認 5 を落としたため存在しない。★ただし観測そのものは要る。⇒ 製造がレビュー取り込みで純粋関数テストへ 1 ケース足した** | **★★赤くならないなら「高」**——**「上書きしない」はどこにも守られていない** |
| **6-3** | **★壊す対象が実在し、その壊し方で観測が変わることが確かめられているか**（playbook §4.32） | 空振りしているなら「高」 |
| **★6-4** | **★クラスを足す形の壊し方になっていないか**（**jsdom は Tailwind を読まないため観測が変わらない**＝`M24-12` の教訓） | **クラスで壊していて空振りしているなら「高」** |
| **6-5** | **★赤くなる経路が数えられているか** | 数えていないなら「中」 |

---

## 7. ドキュメント・進捗ログ

| # | 観点 | 判定 |
|---|---|---|
| **7-1** | **`docs/progress/progress-log.md` へ追記されているか**（`CLAUDE.md` §8） | 未追記なら「高」 |
| **7-2** | **完了報告に §7.4.1 の CHANGE 要否 7 件の判定結果が載っているか** | 欠けているなら「高」 |
| **★7-3** | **★★as-built を書く節が、指示書からの転記ではなく実装かテストを見て書かれているか**（playbook §4.43） | **指示書と同じ文が並んでいて実装と食い違うなら「高」**（`M24-12` の教訓） |
| **★7-4** | **★★指示書が挙げた要求を実装しなかった箇所が、「製造判断」ではなく「契約・設計に反する独自判断」として報告されているか**（playbook §4.42） | **「判断」として書かれていたら「中」**——**レビューが「報告どおり」と読んで通過する** |
| **7-5** | **★暫定案で進めた項目（確認事項 1〜3）が、その旨とともに報告されているか** | 報告が無いなら「中」 |
| **7-6** | **★件数を本文へ書いた箇所に、その場で数え直したコマンドが併記されているか** | 併記が無いなら「中」 |
| **7-7** | **★モック HTML が提示され、変更前と変更後が並んでいるか**（§4.7） | 無いなら「中」 |
| **7-8** | **★`bash scripts/check-artifact-integrity.sh` を 1 本目に回しているか。他の検査も緑か** | 回していないなら「中」。赤を残していたら「高」 |

---

## 8. 重大な問題の判定基準（完了承認を妨げる）

- **母集団が同じことを確かめずに「既存から紐付け」を消している**（1-1）。
- **セットプレイ自動提案（項目12）を消している**（1-2）。
- **候補表示から紐付けられない**（1-5）／ **`RecipeText` を作り替えている**（1-6）。
- **★★入力済みのセットプレイ名を自動生成で上書きしている**（1-8）。
- **`VAL-S06`（名前必須）を撤回している**（1-9）。
- **`M24-12` の成果が落ちている**（1-14）。
- **`internal/` の差分に §2.3 の 3 条件の報告が無い**（2-1）／ **既存 API の挙動を変えた**（2-2）／ **契約 F-1 に触れた**（2-3）／ **スキーマに触れた**（2-4）。
- **★★`/setups/:setupId` へ成立条件の入力を足している**（2-6）。
- **VAL-S 系の判定そのものを変えている**（2-7）。
- **新規依存が提案なしで追加されている**（2-8）／ **ブラウザストレージのキーが台帳へ未登録**（3-1）。
- **E2E の書き換えで検証内容が緩められている**（5-5）／ **`M19-03` / `M19-07` の spec が落ちている**（5-6）。

---

## 9. 軽微な問題の判定基準（持ち越し許容）

- 候補 0 件のときの文言、導線の置き場と見た目、コピー先の親の決め方（§9.2 で委任済み。**★理由の報告があること**）。
- i18n の英語ロケールの文言の質（`M24-07` の手番）。
- **★ただし「持ち越す」と決めたものは followup の候補として設計伝達レポート §4 へ並べること**（**★製造は `followup-backlog` を直接編集しない**＝`D-382`。**§J だけが例外**）。

---

## 10. 質問・確認事項のフォーマット

| # | 質問 | 対象ファイル:行 | 影響 |
|---|---|---|---|

---

## 11. レビュー完了の判定

- **重大 0 件**であること。
- **高**が残る場合は、取り込みの可否を製造担当が判断し、不採用なら理由を完了報告に書くこと。
- **★再レビューの往復上限は 2 回**（`CLAUDE.md` §9 停止規律）。**★上限に達したら未解消項目を `followup-backlog` §J へ必須 5 フィールド付きで記録し、直ちに停止すること。**

### 11.1 ★本チェックリスト自身の検査（**発行前に設計担当が行う**）

- **指示書の版数と対象指示書欄が一致しているか**（`bash scripts/check-instruction-format.sh`）。
- **★★§0.3「重大でないもの」が、指示書がスコープ外・委任と定めた項目をすべて拾っているか。**
- **★§8 の重大判定が、指示書 §2.2「変更しないもの」と §7.1「機能要件」から漏れなく引かれているか。**

---

*以上、M24-05 レビューチェックリスト **v1.0.0**。* **★★本サブの最大の危険は 2 つ**——**(1) 母集団が同じことを確かめずに「既存から紐付け」を消すこと**〔**消してよい根拠が無くなる**〕**／ (2) セットプレイ名の自動生成が利用者の入力を上書きすること。** **§1-1 / §1-8 / §6-2 がその照合にあたる。**
