# M32-01 レビューチェックリスト（操作説明書の器と `README` の整理）

| 項目 | 内容 |
|------|------|
| 文書ID | M32-01-REVIEW-CHECKLIST |
| バージョン | **1.0.0**（2026-09-12・発行） |
| 対象指示書 | `docs/instructions/M32-01-usermanual-shell-and-readme.md` **v1.1.1** |
| 作成者 | 設計担当 Claude（設計卓） |
| 位置づけ | **レビュー担当の主入力**（`D-736`） |

---

## 0. ★★レビュー担当への前置き

**本サブは「文書とシェルスクリプト 1 本」に見える。⇒ その読み方が本サブ最大の落とし穴である。**

**★★★実際には配布アーカイブの必須成果物**（`README.txt`）**の生成経路に手が入る。⇒ 壊し方が 2 つあり、どちらも実装のテストでは検出できない。**

| # | 壊し方 | 何が起きるか |
|---|---|---|
| **1** | **`README.txt` を `docs/usermanual/` へ移す** | **★ビルドが壊れる。⇒ 3 つの独立した根拠が同ファイルを配布物の必須成果物と定めている**（指示書 §0.3） |
| **2** | **古い版を正本として固定する** | **★★生成のたびに `main` 側の是正が巻き戻る。⇒ 生成経路が「是正を消す機構」に化ける**（指示書 §4.2） |

**★★★最も重い不合格は「スクリプトが動いて緑だった」で通すことである。⇒ 正本と生成物が常に一致するなら、照合モードは常に緑を返す。★緑は機構が生きている証拠にならない。**

---

## 1. 束 A — ★★★配布物を壊していないか（**本サブの本体**）

| # | 観点 |
|---|---|
| **A-1** | **★★★`README.txt` がリポジトリのルートに在るか。⇒ `docs/usermanual/` へ*移して*いないか**（指示書 §3-2）**。★正本を `docs/usermanual/` 配下へ置くことと、生成物をルートへコミットすることは両立する。この区別が報告で明示されているか** |
| **A-2** | **★★`README.txt` を必須成果物と定めている 3 か所のうち、設計卓が読めていない 2 か所**（`.github/workflows/nightly-crossbuild.yml` ／ `cmd/tacpendium/main.go`）**の逐語が報告に在るか**（指示書 §2.1-2）**。⇒ 「確認した」だけでは不足である** |
| **A-3** | **★`DES-002` §11.2 のアーカイブ構成が 1 つも変わっていないか。⇒ 変わると分かったなら、止めて設計卓へ請求したか**（`D-293`） |
| **A-4** | **★CHANGE 消費 0 本・マイグレ消費 0 本が守られているか** |

---

## 2. 束 B — ★★★生成経路が「是正を消す機構」になっていないか

| # | 観点 |
|---|---|
| **B-1** | **★★★正本の出発点が `main` の最新か。⇒ 設計卓ツリーの `README.txt` を写していないか**（指示書 §4.2） |
| **B-2** | **★★実証があるか＝`windows-firewall-rule-name-changed` が述べる規則名**（`"CombMgr"` → `"Tacpendium"`）**が、生成後の `README.txt` で新しい側になっているか。⇒ この 1 行が踏み絵である** |
| **B-3** | **★★★【破壊確認】正本を 1 行変えて生成し、`README.txt` の当該行が変わることが実測で示されているか**（指示書 §5-2） |
| **B-4** | **★★★【破壊確認】同じ変更で照合モードが*落ちる*ことが実測で示されているか。⇒ 緑だけを示して通さないこと** |
| **B-5** | **★★破壊確認の対象行が「起動方法」の節の実行ファイル名の行であるか**（指示書 §5-3）**。⇒ 対象を名指しするのは、偽の証拠を作れないようにするためである**（`SUPP-001` §5.5.4 (10)・計測点 `M-139`） |
| **B-6** | **★照合モードが終了コードだけでなく出力でも判定できるか**（テンプレ §7.2） |

---

## 3. 束 C — ★★実査が信用できるか

| # | 観点 |
|---|---|
| **C-1** | **★★段 1 の実査 5 件が、コードを書く*前*に済んで報告されているか**（指示書 §2.1）**。⇒ 後追いで書いていないか** |
| **C-2** | **★★★製品名の as-built が実測で確かめられているか。⇒ `Tacpendium` へ寄せる根拠が実物から取れているか**（指示書 §4.3）**。★推測で寄せたなら「推測: 〜と仮定した」が在るか**（`CLAUDE.md` §9-3） |
| **C-3** | **★★括弧内**（`SF6 Combo Manager`）**を書き換えていないか。⇒ 別課題である**（`followup` の `product-positioning-statement-outdated`。指示書 §4.3） |
| **C-4** | **★旧名 `combmgr-friend-readme.html` を名指しする箇所の走査が*全数*であり、結果が逐語で報告に在るか**（指示書 §2.1-5 / §5-4） |
| **C-5** | **★数値の隣に「実測」「算出」が書き分けられているか**（計測点 `M-157`） |

---

## 4. 束 D — ★器としての完成度

| # | 観点 |
|---|---|
| **D-1** | **★章立てが `DES-005` §5.x の画面に対応しており、対応表が付いているか**（指示書 §2.2-1） |
| **D-2** | **★`M32-02` が書く章と、本サブで確定させる章が分かれているか。⇒ 「起動方法」は本サブ側である**（指示書 §2.2-4） |
| **D-3** | **★★本文を書いていないか。⇒ 器を作るサブである**（指示書 §0.1 / §3-1）**。★書いてしまっていたら、それは `M32-02` の先食いである** |
| **D-4** | **★`README.md` と `README.txt` の双方の冒頭に、相手を名指しした 1 行が在るか**（指示書 §2.4-1 / §5-5）**。⇒ 逐語が報告に在るか** |
| **D-5** | **★★`B6`（クラシック前提の明示）が `README` と操作説明書の器の*2 か所*に在るか**（指示書 §2.4-4）**。⇒ 片方だけなら不合格である** |
| **D-6** | **★改名先が `docs/usermanual/tacpendium-readme.html` であるか**（`D-766` で確定）**。⇒ 案を 2〜3 本出していたら、それは失効した完了条件を見ている**（指示書 v1.1.1・計測点 `M-167`） |

---

## 5. 束 E — ★検査と越境

| # | 観点 |
|---|---|
| **E-1** | **★★既存の `check-*.sh` を 1 本も書き換えていないか**（`D-335`。指示書 §2.3-5 / §3-8）**。⇒ 足すのは新規 1 本だけである** |
| **E-2** | **★生成スクリプトの名前が `scripts/` の既存命名に沿っているか**（先例＝`generate-code-facts.sh` / `generate-docs-map.sh`） |
| **E-3** | **★★`CLAUDE.md` ／ `.github/` を書き換えていないか**（指示書 §3-5）**。⇒ CI へ載せる案は*文*で出すところまでである** |
| **E-4** | **★★★`docs/handover/followup-backlog.md` を編集していないか**（**`D-838`。⇒ 同書を編集できるのは設計卓のみである。★製造は設計伝達レポート §4 へ原稿を残す**）**。★同ファイルは本サブの*読む*資料であって書く先ではない** |
| **E-5** | **★ファイルを消していないか**（`D-196`。指示書 §3-3）**。⇒ 候補と根拠を報告に出すところまでである** |
| **E-6** | **`bash scripts/check-doc-refs.sh` が緑か。★新設ファイルの参照が dead になっていないか** |
| **E-7** | **`go test ./...` ／ `cd web && pnpm test` に変化が無いか**（**★本サブは実装に触らない**） |
| **E-8** | **★★`docs/usermanual/` 配下は `check-md-emphasis.sh` の走査対象外である。⇒ 閉じない強調を、書いた本人が描画で確かめたことが報告に在るか**（指示書 §5 の注記） |

---

## 6. ★★見なくてよいもの

| # | 何を | なぜ |
|---|---|---|
| 1 | **操作説明書の本文の質** | **`M32-02` の射程。⇒ 本サブは器である** |
| 2 | **括弧内の位置づけの文の中身** | **`followup` の `product-positioning-statement-outdated`。⇒ 新しい 1 文は開発者が決める** |
| 3 | **`B1` / `B2` / `B3` / `B5`** | **★開発者の手番**（`M32-overview` §3.3） |
| 4 | **動画** | **`M32` の射程外**（`M32-overview` §4-3） |
| 5 | **照合モードを CI のどちらのリポジトリへ載せるか** | **★決着済みで、載せ方はリポジトリで分ける**（`D-766`）**。⇒ 本サブは「どちらでも動く 1 コマンド」までである** |
| 6 | **CHANGE の起票** | **★製造は自採番しない**（`D-293`）**。⇒ 請求は報告 §6 へ** |

---

*以上、M32-01 レビューチェックリスト **v1.0.0**。* **★★束 A と束 B が本体である。⇒ 「文書仕事だから軽い」で通さないこと。** **★★★とりわけ B-3 / B-4 の破壊確認 2 件は、片方だけでは足りない**——**生成物が変わることと、照合が落ちることの両方を示して初めて、機構が生きている証拠になる。**
