# M37-06 レビューチェックリスト（modifier の項目確定と表現形式）

| 項目 | 内容 |
|------|------|
| 文書ID | M37-06-REVIEW-CHECKLIST |
| バージョン | **1.1.0**（2026-09-13・**★A-6 / A-7 を足した＝`modifiers` は重複判定キーの一部である**） ／ **1.0.0**（2026-09-13・発行） |
| 対象指示書 | `docs/instructions/M37-06-modifier-set-and-display.md` **v1.1.0** |
| 作成者 | 設計担当 Claude（設計卓） |
| 位置づけ | **レビュー担当の主入力**（`D-736`） |

---

## 0. ★★レビュー担当への前置き

**★★★要否は開発者が全件決めている**（`D-873`）**。⇒ 製造が判断する余地は無い。★指示書 §2.2 の表と 1 値でも違えば不合格である。**

**★★問いは 2 つである。⇒ (i) 項目の要否と文言**〔決着済み〕**／ (ii) 表現形式**〔段 1 の実査で射程が決まる〕**。★混ぜて評価しないこと。**

| # | 本サブ固有の不合格 |
|---|---|
| **1** | **★★★削除した flag の*表示語まで*消し、既存行に `{内部識別子}` が出るようになった**（`B04` の逆行） |
| **2** | **★★★flags の全数が 14 でない** |
| **3** | **★★`link` と `no_cancel` を統合した／排他にした** |
| **4** | **★flag のコードを変えた**（`CHANGE-057`） |
| **5** | **★既存行の `modifiers` を書き換えた** |
| **6** | **★`recipeCache` が構造を持たないのに `DES-002` §4.11 を作り直しにいった** |

---

## 1. 束 A — ★★★`B04` を逆行させていないこと（**最重要**）

| # | 観点 |
|---|---|
| **A-1** | **★★★削除した 3 値**〔`just` / `neutral_jump` / `forward_jump`〕**の*表示語*が残っているか。⇒ 消していないか** |
| **A-2** | **★★★破壊確認が在るか＝削除した 3 値を持つ既存行を表示して、`{just}` のような内部識別子が出ないこと**（§5-4） |
| **A-3** | **★★★逆向きも在るか＝表示語を 1 つ外すとフォールバックが出ること。⇒ 無ければ「効いている」ことの証拠にならない** |
| **A-4** | **★14 値すべてで `{内部識別子}` が出ないか**（§5-3） |
| **A-5** | **★既存行の `modifiers` を 1 行も書き換えていないか。⇒ データ移行もマイグレもしていないか** |
| **A-6** | **★★★`recipe_hash` が既存全行で 1 バイトも変わっていないか。⇒ `modifiers` は*重複判定キーの一部*であり、書き換えると重複判定と `recipe_cache` が同時に狂う** |
| **A-7** | **★★`SUPP-001` の重複判定キーの節の古い例**〔「ジャスト入力の有無が違うコンボ」〕**を差し替える CHANGE 原稿が在るか。⇒ `just` を削除するため失効する** |

---

## 2. 束 B — ★★★flags の全数と内容

| # | 観点 |
|---|---|
| **B-1** | **★★★14 値ちょうどか。⇒ 指示書 §2.2 の表と 1 値も違わないか** |
| **B-2** | **★削除 3 値が選択肢に出ないか**〔`just` / `neutral_jump` / `forward_jump`〕 |
| **B-3** | **★追加 7 値が在るか**〔`no_cancel` / `late_cancel` / `whiff` / `cross_under` / `meaty` / `juggle_high` / `juggle_low`〕 |
| **B-4** | **★★`od_*` が 3 値のまま残っているか。⇒ 畳んでいないか** |
| **B-5** | **★`link` / `delay` / `first_hit_cancel` / `low_jump` が残っているか** |
| **B-6** | **★★`low_jump` の表示語が「低空」か。⇒ ★コードは `low_jump` のままか**（`CHANGE-057`＝内部正典として不変） |
| **B-7** | **★type 2 値**〔`parry_drive_rush` / `cancel_drive_rush`〕**が両方残っているか** |
| **B-8** | **★★type の揺らぎを機械で正規化していないか。⇒ 開発者が許容している** |
| **B-9** | **★「最速キャンセル」「連打キャンセル」「溜め」等を足していないか** |

---

## 3. 束 C — ★★`link` と `no_cancel`

| # | 観点 |
|---|---|
| **C-1** | **★★★別の flag として在るか。⇒ 統合していないか** |
| **C-2** | **★★排他になっていないか。⇒ 両方付いた行が両方描かれるか**（§5-5） |
| **C-3** | **★`DES-004` §2.3 の原稿に「`link` は*難易度*の注記であり、接続がキャンセルでないことは `no_cancel` が表す」が在るか**（§2.5）**。⇒ 無いと次の担当が同じ疑いを立てる** |
| **C-4** | **★「キャンセルの既定はキャンセルである」の 1 行が在るか**（§0.4） |
| **C-5** | **★「キャンセルした」を表す flag を作っていないか** |

---

## 4. 束 D — ★★表示語の 3 か所

| # | 観点 |
|---|---|
| **D-1** | **★★★Go ／ フロント ／ `DES-004` §2.3 の 3 か所すべてが 14 値で一致しているか**（`CHANGE-199`。**★機械検査は無い**） |
| **D-2** | **★動かした箇所数が報告に在るか**（**★算出＝11 値 × 3 か所 ＝ 33**） |
| **D-3** | **★`parry_drive_rush` の表記の割れが解消したか。⇒ どちらへ寄せたかが報告に在るか**（**★一般語は「生ラッシュ」**） |
| **D-4** | **★`juggle_high` / `juggle_low` のコード名を変えたなら、理由が報告に在るか**（§7-1） |

---

## 5. 束 E — ★★ボタン群と縦

| # | 観点 |
|---|---|
| **E-1** | **★段 1-3 の実査**〔modifier の欄が上限の対象か〕**が報告に在るか。⇒ 「対象である / ない」を事実として書いたか** |
| **E-2** | **★★対象なら、ボタン群をやめているか。⇒ `od_*` を畳んで凌ごうとしていないか**（**★12 でも 10 を超える**） |
| **E-3** | **★★★縦の前後が同じ条件で測られているか**（§2.4-3）**。⇒ `M37-02` の成果を食っていないか** |
| **E-4** | **★分類の割り当てが報告に在るか**（追加 7 値で分類が増える） |

---

## 6. 束 F — ★★表現形式（**問い (ii)**）

| # | 観点 |
|---|---|
| **F-1** | **★★★段 1-6 の実査が在るか＝`recipeCache` の JSON がステップと修飾の構造を持つか** |
| **F-2** | **★★持っていなかった場合、*止まって請求している*か。⇒ `DES-002` §4.11 を作り直しにいっていないか**（指示書 §3-1） |
| **F-3** | **★持っていた場合、一覧と詳細で同じ見え方になっているか。⇒ 片方だけ変えていないか**（§5-7） |
| **F-4** | **★波括弧を残しているか**（`D-867`）**。⇒ バッジにできない面では唯一の代替である** |

---

## 7. 束 G — ★触っていないもの

| # | 観点 |
|---|---|
| **G-1** | **★★始動技の持続当て**（`combos` の列）**に手を出していないか。⇒ 別サブである** |
| **G-2** | **★flag のコードを変えていないか**（`CHANGE-057`） |
| **G-3** | **★★スクリーンショットを撮っていないか。⇒ 開発者の手番** |
| **G-4** | **★★★`docs/handover/followup-backlog.md` を 1 文字も編集していないか**（`D-838`） |
| **G-5** | **★設計書本体を書き換えていないか**（`CLAUDE.md` §8） |
| **G-6** | **★マイグレを消費していないか。⇒ 消費したなら請求の記録が在るか** |

---

## 8. 束 H — ★実査と検査

| # | 観点 |
|---|---|
| **H-1** | **★★段 1 の 6 点が報告に在るか** |
| **H-2** | **★件数を数え直しているか。⇒ `M37-RESEARCH-01` / `M37-DESIGN-01` の値をそのまま転記していないか**（計測点 `M-157`） |
| **H-3** | **★数値の隣に「実測」「算出」が書き分けられているか** |
| **H-4** | **★★着手前の版ゲート 5 点を通したか**（§0.7）**。⇒ とくに 2＝`M37-DESIGN-01` が 1.2.0 以上** |
| **H-5** | **★★★全数の合否をパイプを挟まずに判定したか。⇒ `EXIT=0` と `FAIL` の件数が報告に貼られているか**（製造 CLI の `<!-- FULL-TEST-RUN-EXIT-CODE -->`） |
| **H-6** | **`check-enum-sync.sh` ／ `check-import-order.sh` が緑か**（**★BASELINE は 99。着手時の実測値を控えたか**） |
| **H-7** | **★`docs/usermanual/` の該当章が追随しているか**（**★スクリーンショットは除く**） |
| **H-8** | **★`progress-log.md` へ 1 行追記したか ／ CHANGE 原稿が設計伝達レポート §4 に在るか** |

---

## 9. ★★見なくてよいもの

| # | 何を | なぜ |
|---|---|---|
| 1 | **各 flag が本当に必要か** | **★★★開発者が全件決めた**（`D-873`）**。⇒ 表と一致しているかだけを見る** |
| 2 | **件数 0 の値を残す是非** | **★`D-857`＝利用者はまだいない。⇒ 不使用の証拠にならない** |
| 3 | **type の揺らぎ** | **★開発者が許容している** |
| 4 | **始動技の持続当て** | **★別サブである** |
| 5 | **CHANGE の起票** | **★製造は自採番しない**（`D-293`） |

---

## 10. ★★対照値（**⇒ 数え直させる**）

| 対象 | 値 |
|---|---:|
| 着手前の flags | 10 |
| **削除** | **3** |
| **追加** | **7** |
| **★★着地後の flags** | **★14** |
| type（不変） | 2 |
| **表示語を動かす箇所**（算出＝11 値 × 3 か所） | **33** |
| **削除 3 値を持つ実 DB の行**（`M37-RESEARCH-01` の実測） | **各 1 件** |
| ボタン化の上限（**★キーボードの数字キー**） | 10 |
| `check-import-order.sh` の BASELINE | 99 |

**★★★14 > 10 である。⇒ `od_*` を 3 → 1 に畳んでも 12 であり、やはり超える。★これがボタン群をやめる根拠である。**

---

*以上、M37-06 レビューチェックリスト **v1.1.0**。* **★★★束 A を最初に見ること**——**表示語ごと削除すると、`M37-02` が消した `{内部識別子}` が戻る。⇒ 前のサブの成果を壊す形である。** **★★次に重いのは B-1 である**——**要否は決着済みであり、14 値ちょうどでなければ不合格である。**
