# M28-05 レビューチェックリスト: terry `quick_burn_light` の `move_code` 是正

| 項目 | 内容 |
|------|------|
| 文書ID | M28-05-review-checklist |
| バージョン | **1.0.0** |
| 対象指示書 | `docs/instructions/M28-05-terry-quick-burn-code-fix.md` **v1.0.0** |
| 推奨モデル | **標準レビュー**（**★★ただし「1 行の改名だから軽い」と読ませないこと。動く golden は 4 本あり、`-check` が守るのは 1 本だけである**） |
| 作成者・作成日 | 設計担当 Claude（設計卓・フェーズ4 期）/ 2026-09-06 |

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| 1.0.0 | 2026-09-06 | 初版（`D-760`。**★投入の合図と同じ手番で起こした**＝`D-736` の運用） |

---

## 0. レビュー担当への前置き

### 0.1 レビュー前の準備

1. **指示書 v1.0.0 を通読する**（とくに §0.3「動く golden は 4 本」、§2.4 の破壊確認 3 件）。
2. **`docs/change-notes/CHANGE-163-notification.md` §1 を読む**——**★★`seedgen` の引数なし `-check` は第一波しか再生成しない。⇒ 本サブの破壊確認はこの規約の最初の適用先である。**
3. **`SUPP-001` §5.5.4 の規約 (6)〜(10) を読む**——**★とくに (6)（素の `UPDATE` は 0 行に当たって成功する）と (10)。**
4. **`migrations/000063_correct_moves_data_m1904c.up.sql` 節 A を読む**——同型の先例。

### 0.2 ★★「1 行の改名」ではない

**`sonic_break_light` → `sonic_break` の先例では動く golden は 1 本だった。⇒ 本サブは 4 本である。**

| # | golden | 固定しているテスト |
|---|---|---|
| 1 | `000026`（moves ＋ `official_ja_move` 別名） | `TestGolden_CommittedMigrationMatchesRegeneration` |
| 2 | **`000035`（command 索引）** | **`TestGolden_MoveCommandsMatchesRegeneration`** |
| 3 | `000072`（numeric alias） | `TestGolden_M2002NumericAliasesMatchesRegeneration` |
| 4 | `000073`（srk alias） | `TestGolden_M2002SRKAliasesMatchesRegeneration` |

**★★差は `is_derived` である**——**`sonic_break_light` は `is_derived=1` で索引に非搭載だったが、`quick_burn_light` は `is_derived=0` かつ `command` 非空のため索引に載っている。**

### 0.3 ★★重大でないもの（**誤判定しないこと**）

| # | 内容 |
|---|---|
| 1 | **`quick_burn_od` を改名していないこと**（**★改名しないのが正しい**。`quick_burn` / `quick_burn_od` の既存形へ合流する） |
| 2 | **`docs/` の 16 件を直していないこと**（**★歴史記録である**） |
| 3 | **guile 以外・terry 以外の同型を直していないこと**（`D-161`＝**全キャラ点検は行わない**。**見つけたら報告するだけ**） |
| 4 | **CHANGE を消費していないこと**（**★`docs/design/` の名指しは 0 件であることを `M28-04` が実査済み**） |
| 5 | **`check-md-emphasis.sh` のベースラインが動いていないこと** |

### 0.4 ★★本サブに固有の危険（**最優先で見る**）

| # | 危険 | なぜ緑のまま通りうるか |
|---|---|---|
| **★★1** | **golden を 1 本だけ再生成した** | **★★`-check` が守るのは `000026` だけである。⇒ 索引・別名の 3 本は再生成しなくても `-check` は緑を返す。★`go test ./...` まで回さないと気づけない**（`CHANGE-163` §1） |
| **★★2** | **素の `UPDATE` を書いた** | **★0 行に当たっても成功し、エラーにならない**（`SUPP-001` §5.5.4 (6)）**。⇒ 「当たったこと」を `NOT EXISTS` ガードで確かめる必要がある** |
| **★★3** | **CSV を直さずに DB だけ直した** | **★次の seed 再生成で戻る。★★戻ってもエラーは出ない** |
| **★★4** | **`WHERE` を `code` だけで絞った** | **★`moves.code` はテーブル全体では一意でない。⇒ 他キャラに同名の code が在れば巻き添えになる。★巻き添えは静かに起きる** |
| **★★5** | **マイグレ名に `data_` を付け忘れた** | **★層 A へ静かに落ちる**（`check-migration-license.sh` の検査 (4) が止める）**。★本サブのマイグレは SF6 の事実そのものを直すので層 B である** |
| **★6** | **golden を理由なく上書きした** | **★動いた理由を書かずに上書きすると、次に動いたとき誰も気づけない** |

---

## 1. 束 A — 母数

| # | 見るところ |
|---|---|
| **★★1** | **`M28-04` の実測と一致しているか**〔`character_data/` 2 ／ `migrations/` 10 ／ `internal/` 0 ／ `web/` 0 ／ `scripts/` 0 ／ `docs/` 16 ／ **`docs/design/` 0**〕**。★一致しなければ止めて報告したか** |
| **★2** | **改名先 `quick_burn` が空きであることを再確認しているか**（**★`UNIQUE (character_id, code)` の衝突が無いこと**） |
| **★3** | **terry 以外の同型を軽く見た結果が在るか**（**★見つかっても直さない。報告だけ**） |

---

## 2. 束 B — 是正

| # | 見るところ |
|---|---|
| **★★1** | **`character_data/terry.csv` が直っているか**（§0.4-3） |
| **★★2** | **DML マイグレ 1 本であること。★DDL が入っていないこと** |
| **★★3** | **`WHERE` が `character_id` ＋ `code` で絞られているか**（§0.4-4） |
| **★★4** | **`NOT EXISTS` ガードが在るか**（§0.4-2） |
| **★★5** | **★★マイグレのファイル名が `NNNNNN_data_*.sql` か**（§0.4-5。`CHANGE-158` の命名規約） |
| **★★6** | **連番が `000106` であり、自採番していないか**（`D-293`） |
| **★7** | **down の中身と、down で何が失われるかが報告に書かれているか** |
| **★8** | **`quick_burn_od` を改名していないか** |

---

## 3. 束 C — golden

| # | 見るところ |
|---|---|
| **★★1** | **★★4 本すべてが再生成されているか**（§0.4-1）**。⇒ 1 本だけ直して緑になったなら、それは残り 3 本を再生成していないだけである** |
| **★★2** | **動いた理由が報告に書かれてから更新されているか**（§0.4-6） |
| **★3** | **`go test ./...` に 4 本の golden テストが含まれ、すべて緑か** |

---

## 4. 破壊確認（**★3 件とも必須。★★とくに 2 が本サブ固有である**）

| # | 壊すもの | 期待 |
|---|---|---|
| **★★1** | **`character_data/terry.csv` を改名前へ戻して `-check`** | **`DIFF` / exit 1 になること。★terry は第一波なので引数なしで捕まる** |
| **★★2** | **`000035` の索引だけを改名前の値へ戻す** | **★★`TestGolden_MoveCommandsMatchesRegeneration` が赤になること。⇒ これが本サブ固有の確認である**（**先例には索引が無かった**）**。★1 だけで済ませていたら重大である** |
| **★★3** | **マイグレのファイル名から `data_` を外す** | **`scripts/check-migration-license.sh` が赤になること** |

**★★1 だけで済ませていないかを必ず見ること。⇒ `-check` が守るのは `000026` だけであり、索引・別名の 3 本は守らない**（`CHANGE-163` §1）。

---

## 5. テストの妥当性

| # | 見るところ |
|---|---|
| **★★1** | **`go test ./...` が緑**（**★golden 4 本のテストを含む**） |
| **★2** | **`cd web && pnpm test` / `make e2e` が緑。★★`make e2e` をリポジトリルートで実行しているか**——**`web/` で実行すると `Nothing to be done for 'e2e'.` が返り、1 本も走らないのに赤も出ない**（`M28-04` が踏んだ） |
| **★3** | **常設検査が緑か**（**★`check-artifact-integrity.sh` を 1 本目に ／ `check-migration-license.sh` も回す**） |
| **★4** | **★「常設検査は緑だった」と書いた時点が、完了時点か**（**★成果物そのものが母数に入る検査**〔`check-md-emphasis` / `check-progress-log-index` / `check-doc-inventory`〕**では、書く前に測った値は完了時の値ではない**＝`M28-04` の教訓 4） |

---

## 6. 重大な問題の判定基準（完了承認を妨げる）

| # | 内容 |
|---|---|
| **★★1** | **golden を 4 本すべて再生成していない** |
| **★★2** | **破壊確認 2（`000035` の索引）を実施していない** |
| **★★3** | **`character_data/terry.csv` を直していない**（DB だけ直した） |
| **★★4** | **`WHERE` が `code` だけで絞られている** |
| **★★5** | **`NOT EXISTS` ガードが無い** |
| **★★6** | **マイグレのファイル名が `data_` を持たない** |
| **★7** | **連番を自採番した ／ `quick_burn_od` を改名した ／ `docs/` の歴史記録を直した** |
| **★8** | **`docs/design/` を編集した ／ `followup-backlog.md` の §J 以外を編集した** |

---

## 7. 軽微な問題の判定基準（持ち越し許容）

| # | 内容 |
|---|---|
| 1 | マイグレの `-note` の文面 |
| 2 | 報告の節立て |
| 3 | **terry 以外の同型の報告が粗いこと**（**★見つけたら報告するだけでよい**） |

---

## 8. レビュー完了の判定

1. **§6 の重大が 0 件である。**
2. **§4 の破壊確認 3 件が実走されている**（**★とくに 2 は本サブ固有である**）。
3. **`go test ./...` / `cd web && pnpm test` / `make e2e`（リポジトリルートで実行）が緑。**
4. **常設検査が緑**（**★`check-migration-license.sh` を含む**）。
5. **`docs/design/` に反映が要る箇所が 0 件であること**（**★出たら設計卓へ請求されているか**）。
6. **`docs/progress/progress-log.md` へ追記されている。**

### 8.1 ★本チェックリスト自身の検査（発行前に設計担当が行う）

- [x] **対象指示書の版数と一致している**（v1.0.0）
- [x] **`bash scripts/check-instruction-format.sh` が緑**
- [x] **`bash scripts/check-md-emphasis.sh` が緑**
- [x] **★★破壊確認が「射程の全区分から 1 件ずつ」になっている**（`SUPP-001` §5.5.4 (10)。**★`M-139` の再演防止**）

---

*以上、M28-05 レビューチェックリスト **v1.0.0**。* **★★本サブでレビューが最も見落としやすいのは「`-check` が緑だから golden は守られている」と読むことである**（§0.4-1）**——`-check` が守るのは 4 本のうち 1 本だけである。⇒ §4 の破壊確認 2 が、索引の側にも観測が付いていることの唯一の証拠である。** **★★このチェックリストは `M-139` の直後に書かれている**——**前サブのチェックリストは破壊確認の対象区分を指定せず、手順を素直に実行すると偽の証拠が作れる形だった。⇒ 本書は §4 で区分を名指ししてある。**
