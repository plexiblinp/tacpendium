# M29-02 レビューチェックリスト: 入出力（CSV・バックアップ・下書き）

| 項目 | 内容 |
|------|------|
| 文書ID | M29-02-review-checklist |
| バージョン | **1.0.0** |
| 対象指示書 | `docs/instructions/M29-02-io-csv-backup-draft.md` **v1.1.0** |
| 推奨モデル | **高精度レビュー**（**★★「黙って落ちる」を鳴らす作業であり、鳴らし方を間違えると、いままで通っていた入力が通らなくなる**） |
| 作成者・作成日 | 設計担当 Claude（設計卓・フェーズ4 期）/ 2026-09-06 |

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| 1.0.0 | 2026-09-06 | 初版（`D-760`。**★投入の合図と同じ手番で起こした**＝`D-736` の運用） |

---

## 0. レビュー担当への前置き

### 0.1 レビュー前の準備

1. **指示書 v1.1.0 を通読する**（とくに §0.1「黙って落ちるを鳴らす」、§0.2「直すと通っていた入力が通らなくなる」、§0.3 の使う語の正本）。
2. **`CHANGE-161` を読む**——**★語彙の正本は `DES-005` §5.7 の手前「画面に出す語の正典（4 群）」である。⇒ 本サブが足す文言はその語を使う。**
3. **`DES-006` §2.2 と `CHANGE-139`（`M24-13`）を読む**——**★`VAL-C09` の重大度を決めるときに、過去の裁定と食い違う向きへ揃えないため。**

### 0.2 ★★本サブの中心は「成功の自動断定」を直すことである

**`FR307` は「破綻の自動断定をしない」と定めている。⇒ 本サブが直すのはその逆向きである。**

**★「上限を超えたぶんを黙って捨てて、成功したように見せる」＝アプリが勝手に「うまくいった」と断定している形である。**

### 0.3 ★★重大でないもの（**誤判定しないこと**）

| # | 内容 |
|---|---|
| **★★1** | **§2.3（バックアップ／復元）や §2.4（下書きの往復）が「特定できなかった」で終わっていること**——**指示書 §2.3-2 / §2.4-2 が明示的にその着地を許している**（`M-128` の運用）**。★推測で直しているほうが重大である** |
| 2 | **上限の値そのものを変えていないこと**（指示書 §2.1-5＝**変えないのが正しい**） |
| 3 | **`draft-gauge-default-zero` を直していないこと**（`D-723` で受容済み＝射程外） |
| 4 | **`csv-drive-damage-sign-flipped-across-versions` ／ 単一 HTML エクスポート ／ クラウドストレージ を触っていないこと**（射程外） |
| 5 | **`check-md-emphasis.sh` / `check-import-order.sh` のベースラインが動いていないこと** |

### 0.4 ★★本サブに固有の危険（**最優先で見る**）

| # | 危険 | なぜ緑のまま通りうるか |
|---|---|---|
| **★★1** | **1 か所だけ鳴らして他が黙ったまま残る** | **★上限が掛かる経路は 5 つある**〔取込 ／ 書出 ／ プレビュー ／ バックアップ ／ 復元〕**。⇒ 1 か所直せばテストは緑になり、他の 4 経路は黙ったままである** |
| **★★2** | **既存データへの影響を測らずに直した** | **★直すと「いままで通っていた入力が通らなくなる」**（指示書 §0.2）**。⇒ 既存データで上限を超えるものが在るかを先に実測していなければ、利用者の手元で初めて落ちる** |
| **★★3** | **上限の値を上げて「直した」ことにした** | **★★それは別の判断である。⇒ 本サブは「黙っている」を直すだけである** |
| **★★4** | **`VAL-C09` の重大度を、過去の裁定と食い違う向きへ揃えた** | **★`M24-13`（`CHANGE-139`）が「仮登録でもレシピは 1 ステップ以上を要する」と決めている。⇒ その向きと食い違う揃え方をすると、静かに裁定を覆すことになる** |
| **★★5** | **重大度を決める前に文言を揃えた** | **★順序が逆である**（指示書 §2.2b-4）**。⇒ 重大度が変われば文言も変わる** |
| **★★6** | **`VAL-C09` を揃えたことで、いままで通っていた CSV が通らなくなる** | **★0 ステップの行が既存データに在るかを先に実測していなければ気づけない**（指示書 §2.2b-5） |
| **★7** | **語彙が `M29-01` の統一結果と違う** | **★`M29-01` は完了している**（`CHANGE-161`）**。⇒ 新しい文言はその語を使う。★退けた 5 語は `retired-words.test.ts` が守るが、走査対象は `web/src` だけである。Go 側は自分で守る** |

---

## 1. 束 A — 行数上限の黙った切り捨て（**最重要**）

| # | 見るところ |
|---|---|
| **★★1** | **上限の値と、超えたときの挙動が実測で示されているか。★「黙って切り捨てる」が本当に黙っているかを、実際に超えさせて確かめたか** |
| **★★2** | **上限が掛かる経路が全数で挙がっているか**〔取込 ／ 書出 ／ プレビュー ／ バックアップ ／ 復元〕**。★1 つでも「調べていない」が在れば、そこは黙ったまま残る** |
| **★★3** | **★★既存データで上限を超えるものが在るかを、直す前に実測しているか**（§0.4-2）**。★在るなら報告して止まり、扱いの判断を仰いだか** |
| **★4** | **鳴らし方が Plan Mode で提案され、開発者が選んだ記録が在るか**〔(a) エラーにして止める ／ (b) 警告を出して続ける ／ (c) 件数を示して選ばせる〕 |
| **★★5** | **上限の値そのものを変えていないか**（§0.4-3） |

---

## 2. 束 B — CSV のプレビューと確定の重大度の非対称

| # | 見るところ |
|---|---|
| **★★1** | **プレビューと確定で、同じ入力に対する重大度が一致しているか。★一致させないなら、なぜ違ってよいのかが書かれているか** |
| **★★2** | **非対称が「意図された仕様」なのか「揃え忘れ」なのかを、実物で弁別しているか** |
| **★3** | **重大度の定義は `DES-006` を正本にしているか**（**★製造が新しい重大度を作っていないか**） |

---

## 3. 束 C — `VAL-C09` の重大度の非対称

| # | 見るところ |
|---|---|
| **★★1** | **CSV 取込（WARNING）と本体保存（ERROR）のどちらへ揃えたか。★その根拠が書かれているか** |
| **★★2** | **`M24-13`（`CHANGE-139`）の裁定と食い違っていないか**（§0.4-4）**。★「仮登録でもレシピは 1 ステップ以上を要する」と決まっている** |
| **★★3** | **重大度を決めてから文言を揃えているか**（§0.4-5）**。⇒ 順序が逆でないか** |
| **★★4** | **★★既存データで 0 ステップの行が在るかを先に実測しているか**（§0.4-6） |
| **★5** | **和英の文言が揃っているか。★`M29-01` の語彙の正典に従っているか** |

---

## 4. 束 D — バックアップ／復元 と 下書きの往復

| # | 見るところ |
|---|---|
| **★★1** | **★★実査して具体を出したか、「特定できなかった」と書いて止まったか。⇒ どちらでもよい。★推測で直しているなら重大である**（§0.3-1） |
| **★★2** | **`M23` の as-built（`FR601` とゴミ箱／復元）を先に読んでいるか。★巻き戻していないか** |
| **★★3** | **`M27-02b` の 3 状態（`CHANGE-153`）を先に読んでいるか。★巻き戻していないか** |
| **★4** | **`M28-01` の `internal/infra/datadir` の移行と混同していないか**（**★あれはデータディレクトリの移行であり、バックアップ／復元ではない**） |

---

## 5. テストの妥当性

| # | 見るところ |
|---|---|
| **★★1** | **★★上限を実際に超えさせるテストが在るか。⇒ 「上限未満で通る」だけのテストは、まさに事故が起きる状況を素通しする**（`M28-02a` の教訓 1 と同型） |
| **★★2** | **5 経路それぞれについて、鳴ることを主張するテストが在るか。★1 経路だけなら他は無観測である** |
| **★3** | **実 `ja.json` を引く `t` で書かれているか**（**★キーをそのまま返すモックでは文面の主張が空振りする**＝`M23-04` 教訓 2） |
| **★4** | **`make e2e` は全数で回しているか。★`make e2e-only` の絞り込みで済ませていないか**（**★文言と重大度が変わると E2E が赤くなりうる**） |
| **★5** | **★`make e2e` をリポジトリルートで実行しているか**——**`web/` で実行すると `Nothing to be done for 'e2e'.` が返り、1 本も走らないのに赤も出ない**（`M28-04` が踏んだ） |

---

## 6. 破壊確認（**★2 件とも必須**）

| # | 壊すもの | 期待 |
|---|---|---|
| **★★1** | **鳴らす実装を 1 経路だけ無効化する** | **その経路のテストが赤になること。⇒ 5 経路それぞれに観測が付いていることの陽性対照である。★1 経路を殺して全部緑なら、その経路は無観測である** |
| **★★2** | **上限をちょうど 1 件超える入力を流す** | **黙って落ちず、鳴ること。★★「上限の 2 倍」で試さないこと**——**境界の 1 件で鳴るかが要点である** |

---

## 7. 重大な問題の判定基準（完了承認を妨げる）

| # | 内容 |
|---|---|
| **★★1** | **上限が掛かる経路の全数が挙がっていない**（調べていない経路が在る） |
| **★★2** | **既存データへの影響を測らずに鳴らす実装を入れた** |
| **★★3** | **上限の値そのものを変えた** |
| **★★4** | **`VAL-C09` を `M24-13` の裁定と食い違う向きへ揃えた** |
| **★★5** | **重大度を決める前に文言を揃えた** |
| **★★6** | **§2.3 / §2.4 を推測で直した**（実査も「特定できなかった」の記録も無い） |
| **★7** | **語彙が `M29-01` の統一結果と違う**（`CHANGE-161` の正典に従っていない） |
| **★8** | **`docs/design/` を編集した ／ `followup-backlog.md` の §J 以外を編集した ／ CHANGE 番号・マイグレ連番を自採番した** |

---

## 8. 軽微な問題の判定基準（持ち越し許容）

| # | 内容 |
|---|---|
| 1 | 鳴らし方の文面の趣味 |
| 2 | **英語側の語選び**（**★日本語が決まっていれば足りる**） |
| 3 | **§2.3 / §2.4 が「特定できなかった」で終わったこと**（**★理由が報告に在る場合に限り軽微とする**） |
| 4 | 報告の節立て |

---

## 9. レビュー完了の判定

1. **§7 の重大が 0 件である。**
2. **§6 の破壊確認 2 件が実走されている**（**★とくに 1 は「5 経路それぞれに観測が付いている」ことの証拠である**）。
3. **`go test ./...` / `cd web && pnpm test` / `make e2e`（全数・リポジトリルートで実行）が緑。**
4. **常設検査が緑**（**★`check-artifact-integrity.sh` を 1 本目に回す**＝`CLAUDE.md` §8）。
5. **`docs/progress/progress-log.md` へ追記されている。**

### 9.1 ★本チェックリスト自身の検査（発行前に設計担当が行う）

- [x] **対象指示書の版数と一致している**（v1.1.0）
- [x] **`bash scripts/check-instruction-format.sh` が緑**
- [x] **`bash scripts/check-md-emphasis.sh` が緑**

---

*以上、M29-02 レビューチェックリスト **v1.0.0**。* **★★本サブでレビューが最も見落としやすいのは「1 か所鳴るようになったから直った」と読むことである**（§0.4-1）**——上限が掛かる経路は 5 つあり、1 か所直せばテストは緑になる。⇒ §6 の破壊確認 1 が、その 5 つそれぞれに観測が付いていることの唯一の証拠である。** **★★もう 1 つは「直すと、いままで通っていた入力が通らなくなる」ことである**（§0.4-2）**——既存データへの影響を先に測っていなければ、利用者の手元で初めて落ちる。**
