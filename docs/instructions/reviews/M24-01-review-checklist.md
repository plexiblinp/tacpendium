# M24-01 レビューチェックリスト

| 項目 | 内容 |
|------|------|
| 対象指示書 | `docs/instructions/M24-01-list-navigation-and-default-character.md` **v1.3.0**（一覧・ナビゲーション・既定キャラの配線） |
| 文書ID | M24-01-REVIEW |
| 対象指示書ID | M24-01 |
| レビューモデル | **高精度レビュー**（実モデル名は `docs/human-notes/model-allocation.md` が正本。実使用は開発者判断） |
| バージョン | 1.3.0 |
| 作成者・作成日 | 設計担当 Claude（設計卓・M24 期）/ 2026-08-25 |

> 文書ID ⇄ 実パスの正は `docs/handover/docs-map.md`（自動生成）。

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| **1.3.0** | **2026-08-25** | **指示書 v1.3.0 と対。確認事項がすべて決着した**（**`D-545`**）。 **★解決順は 5 段**〔段 3 が 3a（`users.main_character_id`）／ 3b（`config.toml [defaults]`）へ割れた〕。**★段 3a を実装していたら「重大」**〔開発者判断で後回しと決まっている〕。**★かつ「後から 1 行足せば差し込める形」になっているかを見る。** **★`SM-058` は承認されたが「キャラだけ」である**〔タグ・状況・仮登録トグル・ソートを持ち込んでいたら「重大」〕。 |
| **1.2.0** | **2026-08-25** | **指示書 v1.2.0 と対。** **★§8 へモック HTML の確認を追加**（**`D-543`**）——**Plan Mode の提示に含まれていたか／変更前と変更後が並んでいるか／外部依存が無いか。** **★モックの承認を受入の根拠として扱わないこと**〔実装の証拠は §4 のテストと §4.5 の破壊確認である〕。 **★`SM-002` の読み替えは開発者に確認済み**〔「利用者がタブだと思って件数を押さない」〕。 |
| **1.1.0** | **2026-08-25** | **指示書 v1.1.0 と対。** **★`DES-005` の実査で前提 3 件が覆ったことに追随**（`M-74`）——**(1) 段 2 は既存キー `combo-list-filters-v1` に相乗り**〔新規キーを作っていたら「高」〕**／(2) `SM-044` が既に満たされていれば実装しないのが正**〔実装していたら重複〕**／(3) `SM-058` は `DES-005` が明示的に除外しており、確認事項 2 の回答なしに実装したら「重大」**。 **★`SM-083` はスコープ外**（`D-541`）——**実装されていたら「重大」。** **★段 3（設定の既定キャラ）も確認事項 1 の回答待ちである。** |
| 1.0.0 | 2026-08-25 | 初版（指示書 v1.0.0 と対）。 |

---

## 0. レビュー担当への前置き

### 0.1 レビュー前の準備

- 指示書 §3.3（実査 14 件）と §11（**確認事項はすべて決着済み。記録として読む**）を先に読むこと。**本サブは「実査で前提が覆ったら止める」設計になっている。**
- **★指示書が「〜になっている」と書いた現状認識は、すべて実査項目である。** **製造がそれを実査せず前提として実装していたら指摘すること。**

### 0.2 レビューの基本姿勢

**本サブの入力は開発者の思いつきメモであり、「症状の観察」であって「機序の特定」ではない**（`D-539`）。**⇒ 「memo にそう書いてあるから正しい」を根拠として受け入れないこと。**

### 0.3 ★重大でないもの（**誤判定しないこと**）

- **`INITIAL_CHARACTER_ID` の定数そのものが残っていること**——**段 4 のフォールバックとして生き続けるのが仕様である**（指示書 §4.1-5）。**撤去されていたら逆に「高」。**
- **`?character=` のクエリ伝播が残っていること**——**切らないのが仕様である**（§4.1-4）。**切られていたら「重大」。**
- **編集画面の保存後の遷移先が変わっていないこと**——**変えないのが仕様である**（§4.4）。
- **`SM-083` が 1 バイトも実装されていないこと**——**`D-541` でスコープ外。実装されていたら「重大」**（§9-8）。
- **★段 3a（`users.main_character_id`）が未実装であること**——**開発者判断で後回しと決まっている**（指示書 §4.1-2a）。**実装されていたら「重大」**（§9-10）。
- **★マイコンボがキャラだけを維持し、タグ・状況・ソートを維持していないこと**——**それが仕様である**（指示書 §4.1-7）。
- **★`SM-044` が「既に満たされていたので実装しなかった」と報告されていること**——**実査の結果しだいで正しい状態である**（指示書 §4.1-6）。**★逆に、満たされているのに重ねて実装していたら「高」。**

### 0.4 報告フォーマット

`重大 / 高 / 中 / 低` の 4 段。**各指摘に「指示書の該当節」または「教訓 ID」を併記すること。**

---

## 1. 設計・パターンとの照合（最重要）

- [ ] **既定キャラの解決が 1 か所に集約され、指示書 §4.1-2 の順序どおりか**（**5 段のうち 3a を除く 4 段**）。**★段の順序が入れ替わっていないか。**
- [ ] **★★段 3a（`users.main_character_id`）が実装されていないか**（指示書 §4.1-2a。**開発者判断で後回し**）。**かつ、後から 1 行足せば差し込める形になっているか**——**段の列挙が 1 か所にあるか。段ごとに `if` が散っていないか。**
- [ ] **★★マイコンボが `combo-list-filters-v1` の「キャラクター」だけを読んでいるか**（指示書 §4.1-7）。**★他のキーも読んでいたら「重大」**——**`DES-005` §5.5 の「戻り時のフィルタ/ソート保持は対象外」を破る。**
- [ ] **段 3b（`config.toml [defaults] character_id`）で得た ID の実在検査があるか**（§4.1-2。**先例＝`M20-05` の `config.preset_id` 実在検査**）。**★無いと、設定が実在しないキャラを指したとき段 4 へ落ちずに壊れる。**
- [ ] **★★新しいブラウザストレージキーを作っていないか**（指示書 §4.1-3）。**`DES-005` §5.4 が既存キー `combo-list-filters-v1` を規定しており、段 2 はそこへ相乗りするのが仕様である。** **★新規キーを作っていたら「高」**——**`CHANGE-095` / `M19-06` の「新規のブラウザストレージキーを作らない」と同じ形を崩す。**
- [ ] **★`localStorage` へ移していないか**——**`DES-005` §5.4 は「セッション跨ぎの恒久ブラウザストレージ化はしない（フェーズ3）」と明記している。**
- [ ] **`combo-list-filters-v1` が `web/CLAUDE.md` §1 の台帳に登録されているかを実査し、無ければ報告しているか**（指示書 §3.3-14）。**★本サブで台帳へ勝手に足していたら「高」**——台帳への追記は CHANGE 通知書経由である。
- [ ] **ストレージが読めない・壊れているとき、例外を投げず段 3 へ落ちるか**（§4.1-3）。専用ヘルパ経由・try-catch。
- [ ] **`M23` の成果物（`/trash` 系・削除／復元の契約）に 1 バイトも触っていないか**（§2.2）。
- [ ] **`FR013` 凍結に触れていないか**——**`combos` に作成者の概念が入っていないか**（§2.2）。

## 2. データ・API 契約・スキーマの不変（非破壊性）

- [ ] **スキーマ変更が 0 件・マイグレ連番の消費が 0 本であること**（§2.2）。**★消費していたら「重大」**（設計卓の払い出しを経ていない）。
- [ ] **分岐 B を採った場合**: 例外条項 §2.3 の 3 条件をすべて満たすか——**(1) 読み取り専用 ／ (2) 既存応答へのフィールド追加のみ（新エンドポイントを作っていない） ／ (3) 逐語が設計伝達レポート §1 へ本文として書かれている**（playbook §4.29）。
- [ ] **分岐 B を採った場合**: **N+1 クエリを作っていないか**（§4.5）。**一覧の件数分だけ問い合わせを発行していないこと。**
- [ ] **分岐 A を採った場合**: **Go 側の差分が 0 バイトであること。**
- [ ] **既存 `queryKey` の綴りを rename していないか**（`M24-08` の手番。§2.2）。
- [ ] **`web/src/locales/` の既存キーを rename していないか。追加は ja / en の両方に入っているか**（parity＝playbook §4.13）。

## 3. フロントエンドの動作仕様

- [ ] **`SM-119` の解き方が指示書 §4.1-4 のとおりか**——**伝播を切らず、新規登録側に明示的な入力を置き、その初期値を解決順から取る形になっているか。** **★伝播を切っていたら `SM-044` と衝突する（「重大」）。**
- [ ] **新規登録の保存後、遷移先の一覧が「いま保存したコンボのキャラ」を対象にしているか**（§4.4）。**★これがテストで固定されているか。**
- [ ] **セットプレイ数の列が表示列カスタマイズの対象に含まれているか**（§4.5）。
- [ ] **`ComboSortControls` の一覧側の挙動が変わっていないか**（§4.3）。

## 4. テストの妥当性（ケース数で確認。指示書 §5 ↔ §7 DoD）

- [ ] **§5.1 の 1〜4（分岐 B のときは 4 も）が実在し、ケース数が完了報告に書かれているか。**
- [ ] **§5.1-1 に「段 3 が実在しないキャラを指すとき段 4 へ落ちる」「ストレージが読めないとき段 3 へ落ちる」の 2 ケースが在るか。**
- [ ] **§5.2 の E2E 3 ケース（A / B / C）が実在するか。**
- [ ] **★完了報告のテスト結果が、コマンド自身の出力（スイート名・pass/fail の件数）で書かれているか**（教訓 **E-125**）。**「全緑」「exit 0」だけを根拠にしていないか**——**依存が未インストールでも終了コード 0 を返す経路がある。件数が書かれていない報告は、そのテストが 1 件も走っていない可能性を排除できない。**
- [ ] **★★結果表示を主張するテストが、実アプリと同じ状態遷移で描画されているか**（教訓 **E-217** / **E-223**。**`M23-06` / `M23-07` の 2 サブ連続で再発**）。
  - **(a) 親のコールバックを no-op モックにしていないか。** 操作の完了で親が状態を空にし、子が `null` を返す形は、**実アプリでは 1 フレームも表示されない機能がテスト上は緑になる。**
  - **(b) 「〜が残っていない」を主張する走査型テストが、走査の前に「対象が実際に描画されたこと」を 1 行アサートしているか。**
  - **★この型はテスト・lint・型検査のいずれでも検出できない。レビューの静的読解だけが検出経路である。**
- [ ] **★E2E の判定キーが spec ごとに分かれているか**（教訓 **E-232**。**`afterEach` の後片付けでは原理的に埋められない**）。
- [ ] **★「行が作られていないこと」を一覧の件数で判定していないか**（教訓 **E-233**。一覧はページングされる）。

### 4.5 破壊確認（**指示書 §5.3・3 件**）

- [ ] **3 件すべてが実施され、コマンドと出力が完了報告に在るか。**
- [ ] **★狙ったテストだけが、狙ったアサーションで赤くなったことまで確認されているか**（教訓 **E-230**）。**★「全部赤くなった」は破壊の証拠ではない**——環境設定の漏れによる全滅と区別がつかない。
- [ ] **★赤くならなかったものについて、壊し方ではなくテストを疑っているか**（教訓 **E-218**）。

## 5. 設計意図との整合（精神の確認）

- [ ] **A 群 4 件が「1 つの規則」に収束しているか。** **★画面ごとに別々の分岐が増えていたら、本サブの目的（§1.1）を達成していない。**
- [ ] **§4.2-2 で採った「形」と、それが不変条件（利用者がタブだと思って件数を押さない）を満たす理由が完了報告に書かれているか。**
- [ ] **§4.3 で「ソート項目の集合が違う」場合に、揃えるのではなく「使える項目を渡せる形」になっているか**（§4.3。**揃えると `SM-004` が求めていない副作用が出る**）。

## 6. コード品質・規約遵守

- [ ] 曖昧語依存でない（playbook §4.1）。定数化・命名一貫。
- [ ] `console.log` / `fmt.Println` が本番コードに残っていない（CLAUDE.md §10）。不要な `eslint-disable` / `nolint` が無い。
- [ ] **★「N 件」「N か所」を書いたコメント・godoc・報告文が、実数と一致しているか**（教訓 **E-220** / **E-236**。**`M23-07` / `M23-10` の 2 サブ連続で再発**）。
  - **(a) 既存の件数コメントを機械的に +1 していないか**（`M23-07` は `10 → 11` にしたが実数は 12 だった。**元のずれが温存された**）。
  - **(b) 「〜だけである」「全 N 件」と断定した文について、異なるキーワードの走査がもう 1 本回されているか**（`M23-10` は片方の走査で 1 件落とした）。
  - **★どちらもテスト・lint・型検査で検出できない。動作は正しく、数だけが嘘になる。**
- [ ] **★`INITIAL_CHARACTER_ID` の godoc / コメントが「誰が使うか」を正しく書いているか**（教訓 **E-224**。**呼び出し元の列挙は、増えたときにしか壊れない形の記述である**）。

## 7. 既存挙動の温存（非破壊性）

- [ ] **§3.3-9 で数えた既存テストの全数が、Go / Vitest / `web/e2e/` の 3 系統で別々に報告されているか。**
- [ ] **★`combo-list-filters-v1` の既存の保持内容（タグ／状況／仮登録トグル／ソート）が増えていないか**（指示書 §4.1-3。**段 2 は読むだけであり、保存する内容を増やさない**）。
- [ ] **★列を 1 つ足したことで `colSpan` が壊れていないか**（教訓 **E-192**）。**★最も落ちやすいのは、失敗時にしか描画されない行内エラー行の `colSpan` である——動作テストにも E2E にも出ない。**
- [ ] **`AddComboToCompareModal.tsx` の挙動が変わっていないか**（参照の置換だけのはず。§4.1-5）。

## 8. ドキュメント・進捗ログ

- [ ] **`docs/progress/progress-log.md` へ索引行が追記されているか**（CLAUDE.md §8）。
- [ ] **★索引行がレビューの後に埋められているか**（教訓 **E-208**）。**★レビュー結果を参照する行がレビューより前に commit されていたら「高」**——**「結果的に当たっていた」は書いてよい理由にならない。**
- [ ] **★完了報告が「済み」と書いた成果物が、すべて実在するか**（教訓 **E-189** / **E-193** / **E-242**。**M23 期に 3 度再発した型**）。**★機械検査の緑は「書いた」の証拠にならない。**
- [ ] **★§4.10 のモック HTML が実在し、Plan Mode の提示に含まれていたか**（**`D-543`**）。**★変更前と変更後が並んでいるか**——**並んでいないと「もう直っているのか」を開発者が判定できない。** **★外部 CSS / JS / 画像に依存していないか**（外出先で開くため）。**★対象箇所が「既に解消していた」なら、モックを出さずにそう報告されているのが正しい。**
- [ ] **★モックの承認を受入の根拠として扱っていないか**——**モックは「形の妥当性」だけの材料であり、実装が正しいことの根拠にはならない**（実装の証拠は §4 のテストと §4.5 の破壊確認）。
- [ ] **§4.9 の否定形確認**——**走査コマンドの全文が完了報告に在るか。「検出した箇所」と「手当てした箇所」が別の表になっているか**（教訓 **E-193**）。**★当たったファイルの全文を見て、同じファイルの下部だけが旧のまま残っていないか**（教訓 **E-235**）。

## 9. 重大な問題の判定基準（完了承認を妨げる）

1. **スキーマ変更またはマイグレ連番の消費が発生している。**
2. **`FR013` 凍結に触れている**（`combos` に作成者の概念が入った）。
8. **★`SM-083` が実装されている**（`D-541` でスコープ外）。
9. **★マイコンボへ「キャラ以外」を持ち込んだ**（`DES-005` §5.5 の「フィルタ/ソート保持は対象外」を破る。**承認されたのはキャラだけである**）。
10. **★段 3a（`users.main_character_id`）を実装した**（開発者判断で後回しと決まっている）。
3. **`?character=` の伝播を切った**（`SM-044` と衝突する）。
4. **分岐 B を採ったのに、例外条項 §2.3 の 3 条件のいずれかを満たしていない。**
5. **`/trash` 系または M23 の削除・復元の契約に触れている。**
6. **§3.3 の実査を行わず、指示書の現状認識をそのまま前提として実装している。**
7. **破壊確認が未実施、または「赤くなった」だけで理由を読んでいない。**

## 10. 軽微な問題の判定基準（持ち越し許容）

- 文言の細部（§4.2-2 の形の好み）。
- 解決関数の命名・置き場所（§9.2 で製造へ委ねた範囲）。
- セットプレイ数の列の既定表示／非表示。

## 11. 質問・確認事項のフォーマット

**「何を / なぜ要るか / 暫定案」の 3 点で書くこと。** **★指示書 §11 の 3 件と重複する質問は立てない**（回答待ちであることだけを確認する）。

## 12. レビュー完了の判定

**重大 0 件 ＋ 高が全件処理済み**（採用または不採用の理由つき）で完了とする。

### 12.1 ★本チェックリスト自身の検査（**発行前に設計担当が行う**）

- [x] 指示書の節番号と本書の参照が一致している（§3.3 / §4.1〜§4.5 / §4.9 / §5.1〜§5.3 / §11）。
- [x] 指示書が「作らない」と書いたものを、本書が要求していない（§0.3 で明示的に打ち消してある）。
- [x] **★指示書 §11 の確認事項に依存する項目（`SM-083`）を、回答前に「未実装＝欠陥」と判定しない形にしてある**（§0.3）。

---

*以上、M24-01 レビューチェックリスト v1.3.0。*
