# M20-01 レビューチェックリスト

| 項目 | 内容 |
|------|------|
| 対象指示書 | `docs/instructions/M20-01-initial-presets-three.md` v1.0.1（初期プリセットの 3 種化） |
| 文書ID | M20-01-REVIEW |
| 対象指示書ID | M20-01 |
| レビューモデル | 高精度レビュー（**削除を含むデータ移行であり、down の忠実復元と `id` の非破壊が争点になる**。実モデル名は `docs/human-notes/model-allocation.md` が正本。実使用は開発者判断） |
| バージョン | 1.1.0 |
| 作成者・作成日 | 設計担当 Claude（M20 期・設計卓）/ 2026-08-12 |

> 文書ID ⇄ 実パスの正は `docs/handover/docs-map.md`（自動生成）。

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| 1.1.0 | 2026-08-12 | **指示書 v1.0.1 と対**（**D-320**）。**§9-6 を差し替えた**——**`srk` への付け替えは禁止に触れない**（**禁止の趣旨は「`numeric` へ書き換えて消した事実を覆い隠すこと」であり、`srk` はその形にならない**）。**§0.3 へ N-9〜N-12 を追加**（新規テストファイル ／ `srk` 存置 ／ 期待値 5 → 3 の 7 か所 ／ godoc の条件付き処置）。**§4 のテスト置き場所を `migrate_m2001_test.go` へ是正。** |
| 1.0.0 | 2026-08-12 | 初版（指示書 v1.0.0 と対）。 |

---

## 0. レビュー担当への前置き

### 0.1 レビュー前の準備

- **指示書 `M20-01` を全文読む**（特に §4.3 `id` を詰めない ／ §4.6 テストの 1 件ずつの処遇 ／ §4.7 テスト規約）。
- **`DES-004` §3.1・§3.4・§4.2・§5.3** と **`SUPP-001` §3.4・§5.5・§7.3・§7.4.1** を読む。
- **完了報告に §3.3 の実査 7 項目の結果が揃っているか**を先に見る。**揃っていなければ、実装の中身を見る前に §9 の重大として返す**——**Plan Mode 必須のサブで実査結果が無い場合、以降の判断はすべて推測の上に乗る。**

### 0.2 レビューの基本姿勢

**最重要ゲートは 3 つ。この順で見る。**

1. **`presets.id` を詰めていないこと**（§4.3）。**詰めていたら重大。**
2. **`down` が `000005_seed_presets` の投入値へ忠実に戻ること**（§4.2）。**`name` リテラルが実ファイルからの逐語コピーであること。**
3. **`migrations/000005` / `000006` / `000011` を改変していないこと**（§2.2）。

### 0.3 ★重大でないもの（**誤判定しないこと**）

| # | 誤判定しやすい状態 | 実は仕様である理由 |
|---|---|---|
| N-1 | **`presets.id` が `1` / `3` / `5` で、`2` と `4` が欠番になっている** | **指示書 §4.3 が明示的に要求している。** **`preset_aliases.preset_id` ／ `recipe_cache` の JSON キー ／ `config` の `defaults.preset_id` ／ `SUPP-001` §7.4.1 の 4 経路が `id` 値に依存しており、詰めると全部ずれる。** |
| N-2 | **`migrations/000006:9` と `000011:4` のコメントに「残り 4 プリセット」が残っている** | **指示書 §2.2 が据え置きを指示している**（**D-308**）。**適用済みマイグレは改変しない。実データではない。** |
| N-3 | **`internal/infra/migration/migrate_test.go` に `numeric` のエイリアスが 0 件であることを固定するテストが残っている** | **指示書 §4.6 #6 が「削除しないこと」を明示している。** **本サブはエイリアスを 1 行も投入しないため落ちない。** **落ちるのは M20-02 のときであり、その扱いは M20-02 の指示書が持つ**（**D-308**）。 |
| N-4 | **`BUILTIN_PRESET_CODES` が `web/src/constants/` ではなく `web/src/features/preset/types.ts` にある** | **指示書 §4.5 が移設を明示的にスコープ外としている。** **`CLAUDE.md` §4 の規約から外れているのは事実だが、移設は import 元を変え、本サブの単一の変更に別の変更を混ぜる。** **完了報告に「気づいたこと」として書かれていれば足りる。** |
| N-5 | **`config` の `preset_id` に `presets` 実在検査が無いまま残っている** | **指示書 §2.2 が「本サブでは直さない」と明示している**（**D-313**＝M20-05 が持つ）。**§3.3-4 の実査結果が報告に載っていれば足りる。** |
| N-6 | **`internal/model/recipe.go` の `DefaultPresetID = "1"` が残っている** | **同上。D-313 で M20-05 が扱うと決めた。** |
| N-7 | **`docs/` 配下の完了報告・review・change-notes に旧 3 code が残っている** | **指示書 §4.9 (b) が「歴史記録は書き換えない」と明示している。** |
| N-8 | **`preset_aliases` の行数が変わっていない** | **本サブは `preset_aliases` を 1 行も触らない**（§2.2）。**むしろ変わっていたら重大**（§9 の (4)）。 |
| N-9 | **本サブの契約テスト 10 件が `migrate_test.go` ではなく新規の `migrate_m2001_test.go` にある** | **指示書 v1.0.1 §4.7 が明示的に要求している。** **初版の `migrate_test.go` 指定が誤りだった**（`SUPP-001` §5.5 (2)「HEAD スコープの主張をサブの名前空間へ相乗りさせない」に反していた）。**既存慣習も `migrate_m1904d_test.go` 等のサブ専用ファイルである。** |
| N-10 | **`TestRepository_ListAliasesByPreset_Empty` が削除されず、引数が `srk` へ付け替えられて残っている** | **指示書 v1.0.1 §4.6.1 が明示的に要求している。** **初版の「関数ごと削除」は誤りだった**（`:157` と `:163` は同一関数で、消すと `ListAliasesByPreset` の空ケースの検査資産が失われる）。**`srk` は削除対象ではなく、いまエイリアスが 0 件である。** |
| N-11 | **`migrate_test.go` ／ `repository_test.go` ／ `handler_test.go` ／ `cache_test.go` ／ `setup_resolver_test.go` の「5」が「3」へ変わっている** | **指示書 v1.0.1 §4.6.2 が 7 か所を指定している。** **本サブが組み込みを 3 種にした以上、`5` はもう真ではない**（`SUPP-001` §5.5 (2)）。**初版の §4.6 表に無かったのは、旧 code の文字列を含まず grep に出なかったためである。** |
| N-12 | **`internal/model/preset.go` の godoc「上限 10 件」が残っている** | **指示書 v1.0.1 §4.8 は条件付きである。** **`10` を強制している実装があった場合は「触らない」が正しい処置であり、完了報告に所在と実値が書かれていれば足りる。** **強制が無かったのに残っている場合だけ §10 の軽微。** |

### 0.4 報告フォーマット

- 各節「OK / 重大（§9）/ 軽微（§10）/ 質問（§11）」。

---

## 1. 設計・パターンとの照合（最重要）

### 1.1 マイグレ up

- [ ] **削除が `code` を条件にしている**（`id` 決め打ちでない）（指示書 §4.1 (a)）。
- [ ] **`DELETE` が 1 行 1 文で書かれている**（`IN (...)` でまとめていない）（同 (b)）。
- [ ] **`numeric_ja` → `numeric` の `UPDATE` で `name` が `ナンバリング記法` になっている**（同 (c) ／ **D-300**）。
- [ ] **`is_builtin` / `base_preset_code` / `user_id` を触っていない**（同 (d)）。
- [ ] **サービス層の組み込みプリセット保護（`SUPP-001` §7.3 の 403 応答）を緩めていない**（§4.1 末尾）。

### 1.2 マイグレ down

- [ ] **`INSERT` が `id` を明示している**（指示書 §4.2 (a)）。**明示していなければ重大。**
- [ ] **`name` が `migrations/000005_seed_presets.up.sql` の逐語と一致している**（同 (b)）。**★レビュー担当が実ファイルを開いて 1 文字ずつ突き合わせること**——**括弧の全角／半角と中黒の有無が典型的な差である。**
- [ ] **`INSERT` が 6 列すべてを明示している**（同 (c)）。
- [ ] **down の条件が `code='numeric'` である**（`code='numeric_ja'` ではない＝同 (d)。**反転にすると 0 行に当たる**）。

### 1.3 定数

- [ ] **`internal/model/preset.go` が 3 定数になっている**（指示書 §4.4）。
- [ ] **`web/src/features/preset/types.ts` の `BUILTIN_PRESET_CODES` が 3 キーで、値が `official_ja_move` / `numeric` / `srk` である**（§4.5）。
- [ ] **Go 定数の値・TS 定数の値・`presets.code` の 3 者が一致している**（**C-4 が「三者完全一致」を実測しており、その状態を保っている**）。

## 2. データ・API 契約・スキーマの不変（非破壊性）

- [ ] **`presets` のスキーマ（列構成）が不変である。** **DDL 変更が 1 行も無い。**
- [ ] **`preset_aliases` / `recipe_cache` / `combos` / `combo_steps` のスキーマとデータが不変である。**
- [ ] **API 契約（`RecipeResponse.presetId` 等）が不変である。**
- [ ] **`presets.id` を振り直す `UPDATE` / `VACUUM` / テーブル再作成が無い**（§4.3）。

**マイグレのテスト規約**（`SUPP-001` §5.5 から引く）。

- [ ] **(1) 比較区間が自サブに閉じている**（始端＝本サブの直前の連番、終端＝本サブの連番）。**`m.Up()`（HEAD 終端）を使っていない。**
- [ ] **(2) HEAD スコープの主張を本サブの名前空間に置いていない。**
- [ ] **(3) 投入した行と投入しなかった行を対で固定している**（§4.7 U-2 の列挙 ／ U-6 の 0 件 ＋ 検査した全行数）。
- [ ] **(5) down のスコープ条件が up の反転になっていない**（§4.2 (d)）。

> **★「HEAD の最終状態で固定されているか」を求めないこと。** 規約 (1)(2) と衝突する。

## 3. フロントエンドの動作仕様

- [ ] **`ComboDetailRecipe.tsx` の挙動が不変である**（**`officialJaMove` のみを参照しており、本サブで変わらない見込み**＝§4.5）。**変わっている場合、理由が完了報告にあるか。**
- [ ] **プリセット選択 UI がある画面で、選択肢が 5 → 3 に減ることによる回帰が無い**（§5.3）。
- [ ] **i18n の ja / en 双方に、削除したプリセットの表示名が残っていない**（**C-5 は i18n JSON に preset code が 0 件と実測したが、表示名は別である**）。

## 4. テストの妥当性（ケース数で確認。指示書 §5 ↔ §7 DoD）

- [ ] **§4.7 のテストが 10 件実在する**（**up 7 件＝U-1〜U-7 ／ down 3 件＝DN-1〜DN-3**）。**件数が合っているか。**
- [ ] **10 件が新規ファイル `internal/infra/migration/migrate_m2001_test.go` にあり、関数名が `TestRun_M2001_*` である**（指示書 v1.0.1 §4.7）。**`migrate_test.go` へ相乗りしていないか。**
- [ ] **`TestRepository_ListAliasesByPreset_Empty` が `srk` を引く形で存置されている**（§4.6.1）。**削除されていないか。** **`numeric` へ付け替えられていないか**（§9-6）。
- [ ] **§4.6.2 の 7 か所が `5` → `3` へ更新され、完了報告に「その 5 が何の 5 か」が 1 件ずつ書かれている**（教訓 `E-16` ／ `E-93`）。
- [ ] **U-3 が `id` の欠番（2・4）を明示的に固定している**（**「3 行ある」だけでは §4.3 の非破壊条件を守れない**）。
- [ ] **U-6 の「0 件」に検査した全行数が併記されている**（教訓 `E-84`）。
- [ ] **U-7 / DN-3 が `preset_aliases` の行数不変を固定している**（**FK の `ON DELETE CASCADE` の有無を実測で固定する**＝§3.3-7）。
- [ ] **DN-2 が 5 行 × 5 列を対で固定している**（件数だけになっていない）。
- [ ] **§4.6 の 6 件が表のとおり処置されている。** **特に #2・#3 が「削除」であり「書き換え」になっていない**、**#6 が削除されていない。**

## 5. 設計意図との整合（精神の確認）

- [ ] **「3 種化」は表示の変化を伴わない**——**削除した 2 種はエイリアス空で出荷されており、選択しても `DES-004` §5.3 のフォールバックで `official_ja_move` が出ていた**（指示書 §1.1）。**実装がこの前提を壊していないか**（例: フォールバック経路に手を入れていないか）。
- [ ] **`numeric` へのリネームが「今のうちに」行われる理由**（**M20-04 で code が画面へ露出する前**＝**D-300**）と整合しているか。**露出面を先に作っていないか。**
- [ ] **本サブがスコープ外と定めた 6 項目**（指示書 §1.3）**に手を出していないか。**

## 6. コード品質・規約遵守

- [ ] 曖昧語依存でない（Playbook §4.1）。定数化・命名一貫。
- [ ] **`console.log` / `fmt.Println` が本番コードに無い**（`CLAUDE.md` §10）。
- [ ] **不要な `eslint-disable` / `nolint` が無い。**
- [ ] i18n 追加時は ja/en 両ロケール parity（Playbook §4.13）。

## 7. 既存挙動の温存（非破壊性）

- [ ] **`official_ja_move`（id=1）の行が 1 バイトも変わっていない**（`code` / `name` / `is_builtin` / `base_preset_code`）。
- [ ] **`config` の `[defaults] preset_id` の既定値が `1` のままである。**
- [ ] **既存 E2E スイートが通る。** **通らない場合、本サブが原因かの切り分けが完了報告にあるか**（**既知の flaky がある**＝ボード P-14 / P-22。**flaky を本サブの失敗として扱わない／本サブの失敗を flaky として流さない、の両方に注意する**）。

## 8. ドキュメント・進捗ログ

- [ ] 完了報告に **§3.3 の実査 7 項目の結果 ／ Plan Mode で確定した方式 ／ 変更点 ／ テストケース数 ／ 既知の制約**がある。
- [ ] **`docs/progress/progress-log.md` へ追記されている**（`CLAUDE.md` §8）。**完了報告とは別である。**
- [ ] **DES CHANGE が要なら設計担当起票前提で、製造が `DES` 本体を直接編集していない**（**本サブは `CHANGE-098` が設計卓側で起票済み**）。
- [ ] **`SUPP-001` を製造が編集していない**（指示書 §1.3＝設計卓が本サブ完了後に是正する）。
- [ ] **撤回・変更した定義の残骸が残っていないか。** 指示書 §4.9 の否定形確認 4 項目について、**走査キーワードで実際に走査した結果**が完了報告にあるか。
- [ ] 走査対象に **3 系統すべて**が含まれているか——**本番コード／テスト資産（コメント・ヘルパ名・fixture・spec 名）／設計文書・指示書・overview**。**本番コードだけを見て「直っている」と報告していないか。**
- [ ] **「見つけたが直さない」場所（§4.9 の (a)(b)(c)）が明示されているか。** **直さない記録が無いと、次の担当が同じ走査をやり直す。**
- [ ] **★並列サブの as-built との突合が行われているか**（教訓 `E-121` ／ ボード **D-284**・**D-297**）。**本サブの並列相手は M21 レーンの PoC であり、同 PoC は本体リポを 1 バイトも変更しない**（指示書 §3.4）。**その確認を実際に行った記録があるか。** **「触るファイルが違うから大丈夫」で済ませていないか。**
- [ ] **★完了報告に「■ 併せて更新が要るもの」の節があるか**（教訓 `E-114` ／ ボード **D-277**）。**該当が無い場合も「なし」と書かれているか。** 指示書 §7.4.1 の 4 行（CHANGE 番号の registry 登録 ／ 「次の番号」の写し先 4 か所 ／ 消費したマイグレ連番 ／ 版を上げた文書の参照元）。
- [ ] **`docs/human-notes/model-allocation.md` へ実使用モデルが追記されているか。**

---

## 9. 重大な問題の判定基準（完了承認を妨げる）

1. **`presets.id` を詰めている**（振り直し `UPDATE` / `VACUUM` / テーブル再作成を含む）。**§4.3 の 4 経路が壊れる。**
2. **`down` が `000005_seed_presets` の投入値へ戻らない**——`id` を省略している ／ `name` が逐語でない ／ 列を省略している ／ 条件が反転になっている。
3. **`migrations/000005` / `000006` / `000011` を改変している**（**適用済みマイグレの改変**）。
4. **`preset_aliases` の行が増減している**（**本サブは 1 行も触らない**）。
5. **`internal/infra/migration/migrate_test.go` の「エイリアス 0 件」テストを削除している**（§4.6 #6。**M20-02 の検出手段を先に壊す**）。
6. **`TestRepository_ListAliasesByPreset_Empty` を `numeric` へ付け替えている**（§4.6.1）。**`numeric` は #1・#4・#5 が既に引いており主張が重複するうえ、「`numeric_en` を消した」という事実を覆い隠す形になる。** **★`srk` への付け替えはこの禁止に触れない**（v1.1.0 で明確化。**禁止の趣旨は `numeric` への書き換えであって、付け替え一般ではない**）。**★関数ごと削除するのも不可**（v1.1.0 で反転。**指示書 v1.0.1 §4.6.1 が存置を要求している**）。
7. **§3.3 の実査 7 項目のうち、結果が報告に無いものがある**（Plan Mode 必須のサブ）。
8. **`preset_aliases` に削除対象を指す行があったのに、止めずに削除している**（§9.3-2。**実データが消える**）。
9. **マイグレ連番を自採番している**（**disk 末尾と不一致だったのに設計卓へ再請求していない**＝**D-293**）。
10. **製造が `DES-004` 本体または `SUPP-001` を編集している。**
11. **スコープ外 6 項目**（指示書 §1.3）**のいずれかに手を出している**——特に **エイリアスの投入**・**一意制約の追加**・**`recipe_cache` キーの変更**。

## 10. 軽微な問題の判定基準（持ち越し許容）

- **マイグレファイルの slug が `m20_initial_presets_three` と違う**（§9.2 で製造判断に委ねている。**理由が報告に書かれていれば可**）。
- **テスト関数名の付け方**（既存慣習に寄せていれば可）。
- **`BUILTIN_PRESET_CODES` を `web/src/constants/` へ移設していない**（**N-4**。**移設しないのが指示である**）。
- **§4.6 #2・#3 の削除に伴う未使用ヘルパの残存**（§9.2 で「消してよい」としたが必須ではない）。
- **完了報告の節構成の細部。**

## 11. 質問・確認事項のフォーマット

- 「指示書 §X.X / `DES-004` §Y / `M20-RESEARCH-01-report-web.md` の C-Z に対し実装が W。意図確認したい」の形で根拠節を併記する。

## 12. レビュー完了の判定

- §1〜§8 が全て OK、§9 重大ゼロ（**特に `id` を詰めていないこと ／ down の忠実復元 ／ 適用済みマイグレの非改変**）、§0.1 の Plan Mode 実査 7 項目が揃っている。§10 軽微は持ち越し可。

### 12.1 ★本チェックリスト自身の検査（**発行前に設計担当が行う**）

- [x] **本書と対の指示書の間に、節番号の食い違いが無い。**
  → 本書が参照する指示書の節は §1.3 / §2.2 / §3.3 / §3.4 / §4.1 / §4.2 / §4.3 / §4.5 / §4.6 / §4.7 / §4.9 / §5.3 / §7.4.1 / §9.2 / §9.3 で、**すべて指示書に実在する。** **★テンプレートは否定形確認を `## 4.5` と採番しているが、本サブの §4 は 4.1〜4.7 まで使うため衝突する。** **先例（`M19-04` §4.9）に合わせて `## 4.9` へ繰り下げた**——**衝突したまま発行すると、レビューの「§4.5」が 2 つの節のどちらを指すか決まらない。**
- [x] **指示書が禁じていない事項を「重大」として求めている項目が無い。**
  → §9 の 11 件はすべて指示書の明示的な指示に対応する（1→§4.3 / 2→§4.2 / 3→§2.2 / 4→§2.2 / 5→§4.6 #6 / 6→§4.6 #2・#3 / 7→§3.3 / 8→§9.3-2 / 9→§3.3-1 / 10→§1.3 / 11→§1.3）。
- [x] **達成不能な条件を重大として求めていない。**
  → **「旧 code の文字列がリポジトリに残っていないこと」を重大にしていない**——**down マイグレ自身が `official_ja_command` / `numeric_en` / `numeric_ja` を名指しする必要があり、達成不能である。** **§0.3 N-2 / N-7 でも残ってよい場所を明示した。**

---

*以上、M20-01 レビューチェックリスト v1.1.0。配置 `docs/instructions/reviews/M20-01-review-checklist.md`。指示書 v1.0.1 と対。`presets.id` を詰めないこと・down の忠実復元・適用済みマイグレの非改変を最重要ゲートとする。*
