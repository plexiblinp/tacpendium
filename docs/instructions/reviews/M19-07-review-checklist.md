# M19-07 レビューチェックリスト

| 項目 | 内容 |
|------|------|
| 文書ID | M19-07-REVIEW |
| 対象指示書 | `docs/instructions/M19-07-bundled-setup-verified-conditions.md` v1.0.0 |
| 対象指示書ID | M19-07 |
| **参照してよい先行資料** | **対の指示書** ／ **`m18-m19-contract.md` §2・§3.2** ／ **`SUPP-001` §5.5** ／ **`DES-005` §5.6・§5.7** ／ **`DES-006` VAL-S05**。**★他サブの指示書・チェックリストは含めない**（**D-217**） |
| レビューモデル | Sonnet 4.6（model-allocation 参照。実使用は開発者判断） |
| バージョン | **1.0.0** |
| 作成者・作成日 | 設計担当 Claude（親チャット・設計卓）/ 2026-08-08 |

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| 1.0.0 | 2026-08-08 | 初版（指示書 v1.0.0 と対）。 |

---

## §0. レビュー担当への前置き

### 0.1 このレビューが守るもの

**本サブは「既存の非対称を揃えるだけ」である。** 最重要ゲートは 3 点。

| # | ゲート |
|---|---|
| **G-1** | **同一トランザクションであること**（コンボ作成が失敗したら `combo_setup_results` も残らない） |
| **G-2** | **「対称にする」を超えていない**（`CreateSetupRequest` を変えない／`ng` や note を足さない／新しい書き込み経路を作らない） |
| **G-3** | **`copy` モードで成立条件を引き継がない** |

### 0.2 ★重大判定の基準

| # | 重大の条件 |
|---|---|
| **R-1** | **`combo_setup_results` の書き込みが、コンボ作成と別トランザクションになっている**（G-1 違反。**VAL-S05 を満たせない**） |
| **R-2** | **`CreateSetupRequest` が変更されている**（G-2 違反。本サブは `BundledSetupRequest` への追加のみ） |
| **R-3** | **`verifiedConditions` に `result` や note が追加されている**（G-2 違反。§2.2 の非対称は意図的） |
| **R-4** | **新しい書き込み経路が作られている**（既存の `internal/repository/setup/setup_results.go` を使うこと） |
| **R-5** | **`copy` モードで成立条件が引き継がれている**（G-3 違反。**引き継ぐと「元のコンボで確認した ok」が「このコンボで確認した ok」に見える**） |
| **R-6** | **`moves` に diff がある**（契約 F-1〜F-6） |
| **R-7** | **`internal/service/punishfinder/` ／ `setplay/` に diff がある**（契約 §3.2。**M19-05 と衝突する**） |
| **R-8** | **同名で中身が違う型が 2 つ作られている**（`SetupResultConditionRequest`） |
| **R-9** | **即時保存している**（`DES-005` §5.6 の v3 原則違反。**M19-01 の再発**） |

### 0.3 ★重大でないもの（**誤判定しないこと**）

| # | 誤判定しやすい状態 | 実は仕様である理由 |
|---|---|---|
| **N-1** | **`result='ng'` を入力できない** | **仕様**（指示書 §2.2）。`SetupResultConditionRequest` は `techType` / `inCorner` のみで **`ok` 固定**。**`CreateSetupRequest` と同じ非対称であり意図的** |
| **N-2** | **note を入力できない** | **仕様**（同上）。**未検証セルのメモは永続化されない**（`DES-005` §5.6 項目10） |
| **N-3** | **編集モードに入力 UI が無い** | **仕様**（同 §2.4）。**既存経路がある**（項目10 の 2×2 グリッド ＋ `UpsertSetupResult` API） |
| **N-4** | **`SetupSelectorModal` に入力 UI が無い** | **§2.5-1 の実査で「触らない」と判定されていれば仕様。** 完了報告に理由があるかを確認する |
| **N-5** | **`migrations/` に新規ファイルが無い** | **仕様。** スキーマ変更なし |
| **N-6** | **2×2 グリッドの既定が全セル未チェック** | **仕様**（同 §2.3）。**未検証で開始する** |
| **N-7** | **`copy` で成立条件だけが引き継がれない**（他は引き継がれる） | **仕様**（同 §2.4。開発者確定 2026-07-28） |

### 0.4 報告フォーマット

- 各節「OK / 重大（§0.2）/ 軽微（§8）/ 質問（§9）」。

---

## §1. 着手前（Plan Mode）の報告

- [ ] **1.1** 指示書 §3 の **8 項目すべて**が報告されている。
- [ ] **1.2** **契約の全条項が列挙され、本サブが触れるものが無いことが確認されている。**
- [ ] **1.3** **★§3-2 の突合結果**——`BundledSetupRequest` と `CreateSetupRequest` の差が **`verifiedConditions` だけ**だったか。
  → **他にも差があった場合、全数が報告され、実装に入る前に上げられているか。** **本サブの前提が崩れる。**
- [ ] **1.4** **トランザクション境界が実査で確定している**（推測で書いていない）。
- [ ] **1.5** **§2.5 の 2 件の判定**が報告されている。**「触らない」と判定したものに理由があるか。**
- [ ] **1.6** **想定と食い違った項目があった場合、実装に入る前に報告されている**（`PARENT-E21`）。

---

## §2. ★同一トランザクション（**本サブの中心**）

- [ ] **2.1** **`combo_setup_results` の書き込みが、コンボ作成と同一トランザクション内で行われている。** → 別 Tx なら **R-1**。
- [ ] **2.2** **`combo_setups` を作った後に呼ばれている**（`setup_results.go:89` の前提）。
- [ ] **2.3** **★コンボ作成が失敗したとき、`combo_setups` も `combo_setup_results` も残らない**ことがテストで固定されている。
- [ ] **2.4** **新しい書き込み経路を作っていない。** **既存の `internal/repository/setup/setup_results.go` を使っている。** → 新設なら **R-4**。
- [ ] **2.5** **即時保存していない**（ステージングしてコンボ作成時にまとめて書く）。→ 即時保存なら **R-9**。

---

## §3. 「対称にする」を超えていないか

- [ ] **3.1** **`BundledSetupRequest` への追加が `verifiedConditions` の 1 フィールドだけ。**
- [ ] **3.2** **`CreateSetupRequest` に diff 0。** → あれば **R-2**。
- [ ] **3.3** **`verifiedConditions` の要素が `techType` / `inCorner` のみ**（`result` / note を持たない）。→ 持てば **R-3**。
- [ ] **3.4** **`UpsertSetupResult` の既存経路が不変。**
- [ ] **3.5** **`SetupResultConditionRequest` が 1 つだけ**（同名で中身が違う型を 2 つ作っていない）。→ 2 つなら **R-8**。
- [ ] **3.6** **パッケージ境界を跨ぐために採った形と理由**が設計伝達レポートに書かれている。

---

## §4. `ComboEditor` の 3 モード

- [ ] **4.1** **`new` に入力 UI がある。**
- [ ] **4.2** **`copy` に入力 UI があり、成立条件が引き継がれない**（全セル未検証で開始）。→ 引き継いでいれば **R-5**。
- [ ] **4.3** **`edit` に触っていない。**
- [ ] **4.4** **`copy` で成立条件「以外」の引き継ぎが変わっていない**（N-7）。

---

## §5. フロントエンド

- [ ] **5.1** **2×2 グリッド（受け身種別 × 画面端）を使っている。** **新しい操作モデルを作っていない。**
- [ ] **5.2** **`SetupResultGrid` ／ `SetupResultEditor` の再利用可否が検討され、しなかった場合は理由が書かれている。**
- [ ] **5.3** **`SETUP_RESULT_CELLS` を使っている**（セルの組を自前で組み立てていない）。
- [ ] **5.4** **語彙が `OKI_TECH_TYPE_LABELS` と「相手が画面端」／「画面中央」の正典。** **第 3 の語彙を作っていない。**
- [ ] **5.5** **`CreateSetupInput` に載せてステージングしている。**
- [ ] **5.6** **既定が全セル未チェック。**

---

## §6. テスト

- [ ] **6.1** 指示書 §4 の **11 要件がすべて存在し、green。**
- [ ] **6.2** **★渡したセルと渡さなかったセルが対で固定されている**（`SUPP-001` §5.5.2 (3)）。
  → **「入った件数」だけでは、条件を広く当てすぎても検出できない。**
- [ ] **6.3** **`verifiedConditions` を渡さない／空のとき、`combo_setup_results` に 1 行も入らない**ことが固定されている（**未検証は行の有無で表現する**）。
- [ ] **6.4** **`ng` と note が入らない**ことが固定されている。
- [ ] **6.5** **既存のコンボ作成（`verifiedConditions` を渡さない）が変更前と同じ結果**であることが固定されている。
- [ ] **6.6** **比較区間が自サブに閉じている。** マイグレを消費しないため、**テストが HEAD 依存になっていないか。**
- [ ] **6.7** **E2E 非回帰。**

---

## §7. 否定形確認と完了条件

- [ ] **7.1** 指示書 §5 の走査（**v3 原則の v1／v2 の残骸**）が実施され、結果が完了報告にある。
- [ ] **7.2** **走査対象に 3 系統すべて**が含まれている——**本番コード／テスト資産／設計文書・指示書・overview**。
- [ ] **7.3** **除外対象が尊重されている**（`DES-005` §5.6 の v1／v2 の**撤回記録**は歴史記録として残る）。
- [ ] **7.4** **★実装側に v1 の記述が残っていた場合、報告されているか。**
  → **残っていると、後続担当やレビューが「M19-01 の再発」と誤判定して弾く。**
- [ ] **7.5** **`moves` に diff 0**（R-6）／**`internal/service/punishfinder/` ／ `setplay/` に diff 0**（R-7）／**`migrations/` に新規ファイルなし。**
- [ ] **7.6** **完了報告に指示書 §7.2 の 8 項目**が載っている。
- [ ] **7.7** **`docs/progress/progress-log.md` への追記**（**D-191**）。
- [ ] **7.8** **CHANGE の要否の判定結果**が完了報告にある。**製造が DES 本体を直接編集していない。**

---

## §8. 軽微な問題の判定基準（持ち越し許容）

- フィールド名・関数名・ファイル配置（既存の命名に合っていれば可）。
- パッケージ境界を跨ぐ手段の選択（**同名で中身が違う型を 2 つ作っていなければ可**）。
- UI のレイアウトの細部（**操作モデルと語彙が正典どおりであれば可**）。

---

## §9. 質問・確認事項のフォーマット

- 「**指示書 §X.X / `DES-005` §5.6 / `SUPP-001` §5.5 (n)** に対し実装が Z。意図確認したい」の形で根拠節を併記する。

---

## §10. 設計卓へ上げる（レビュー担当が確認する）

- [ ] **10.1** **§3-2 で `verifiedConditions` 以外の差が見つかった場合。**
- [ ] **10.2** **§2.5-1 で `SetupSelectorModal` が v3 原則に反すると判明した場合。**
- [ ] **10.3** **§5 の走査で v1／v2 の残骸が実装側に見つかった場合。**
- [ ] **10.4** **契約の条項に触れる必要が生じた場合。**
- [ ] **10.5** **マイグレが必要になった場合。**

---

## §11. ★本チェックリスト自身の検査（**発行前に設計担当が行う**）

- [x] **本書と対の指示書の間に、節番号の食い違いが無い。**（2026-08-08 確認）
- [ ] **メタ表の「対象指示書」が、配置時の実ファイル名と一致している。** → **★配置後に確認すること。**
- [x] **指示書が禁じていない事項を「重大」として求めている項目が無い。**（§0.3 に 7 件を明示）
- [x] **達成不能な条件を重大として求めていない。**（§7.3 で除外対象を明示）
- [x] **§0.3（重大でないもの）が埋まっている。**

---

## §12. レビュー完了の判定

- **§1〜§7 が全て OK、§0.2 の重大ゼロ**（**特に G-1・G-2・G-3**）、**§1 の Plan Mode 実査が揃っている。**
- **§8 軽微は持ち越し可。**

---

*以上、M19-07 レビューチェックリスト v1.0.0。配置 `docs/instructions/reviews/M19-07-review-checklist.md`。指示書 v1.0.0 と対。**「対称にするを超えないこと」**を最重要ゲートとする。*
