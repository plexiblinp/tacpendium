# M30-06 レビューチェックリスト（ファミリー行の代表名が 1 メンバーの名前になっている）

| 項目 | 内容 |
|------|------|
| 文書ID | M30-06-REVIEW-CHECKLIST |
| バージョン | **1.0.0**（2026-09-12・発行） |
| 対象指示書 | `docs/instructions/M30-06-family-label-representative-name.md` **v1.0.0** |
| 作成者 | 設計担当 Claude（設計卓） |
| 位置づけ | **レビュー担当の主入力**（`D-736`） |

---

## 0. ★★レビュー担当への前置き

**本サブは「表示名を直す」サブに見えて、実は*落としすぎないこと*が本体である。**

**★★★最も重い不合格は「対象 4 行の名前が変わった」だけを見て通すことである。⇒ 丸括弧の中の強度語を落とすと、`cannon_strike_{light,medium,heavy}_od_hooligan_combination` のような OD 兄弟 3 行が同一表示になり、区別できなくなる。★これは `M30-05` の実機確認 C が確認した形を壊す。**

**★`M30-05` の教訓 2 がそのまま効く**——**「落ちたこと」を見るテストは、落としすぎを検出できない。**

---

## 1. 束 A — ★★★落としすぎていないか（**本サブの本体**）

| # | 観点 |
|---|---|
| **A-1** | **★★★§0.3 の反例群が全数挙げられているか。⇒ cammy の OD 兄弟 3 行は既知だが、*他に無いこと*を実測で確かめたか。★「既知の 1 組だけ見た」では不足である** |
| **A-2** | **★★★反例群が、変更後も区別できるままであることが実測で示されているか。⇒ 「変わった」側の主張と対になっているか。★片方だけなら不合格である** |
| **A-3** | **★★丸括弧の中の強度語を、*表示名そのもの*から落としていないか**（指示書 §3-1）**。⇒ 落としてよいのは*代表名を作るとき*だけである。★この区別が報告で明示されているか** |
| **A-4** | **★`DES-004` §2.1 の規約**（派生元の強度は丸括弧の中）**に反していないか** |

---

## 2. 束 B — ★★★壊していないか

| # | 観点 |
|---|---|
| **B-1** | **★★★`family` 識別子の digest が前後で同一か**（`M30-05` は `b54d118…` で固定）**。⇒ 変えると保存済みのコンボが壊れうる。★本サブで最も高い代償である** |
| **B-2** | **★ファミリー行の総数 372 が不変か** |
| **B-3** | **★`is_derived` の並び 194:178 が不変か**（`M30-04` の成果） |
| **B-4** | **★★`DES-005` §6.1 の 3 形の規則が 1 つも変わっていないか**（指示書 §3-2）**。⇒ 本サブは代表名の作り方だけを変える** |
| **B-5** | **★`specialFamilyLabel.roster.test.ts` の 10 本が緑のままか**（`M30-05` が置いた床） |
| **B-6** | **★`name_ja` / `move_code` / `preset_aliases` / `migrations/` の差分が 0 か。⇒ 触ったなら、設計卓へ請求してから触ったか**（`D-293`） |

---

## 3. 束 C — ★★実査が信用できるか

| # | 観点 |
|---|---|
| **C-1** | **★★対象行を実物で数え直しているか。⇒ `M30-05` の「4 行」をそのまま使っていたら不合格である**（同サブ教訓 1＝**他所から持ってきた実測値は測り直す**） |
| **C-2** | **★強度語の位置の分布**（丸括弧の中 ／ それ以外）**が 1 行ずつ記録されているか** |
| **C-3** | **★「判別できない」行を、判別できないと書いているか。⇒ 埋めていないか** |
| **C-4** | **★数値の隣に「実測」「算出」が書き分けられているか**（計測点 `M-157`） |
| **C-5** | **★★陽性対照が在るか＝入れる*前*に対象行の代表名が強度語を含んでいたことを測っているか。⇒ 「無い」だけを測ると、元から無かった行と区別がつかない** |

---

## 4. 束 D — ★方式の選択

| # | 観点 |
|---|---|
| **D-1** | **★(a)**（代表名を作るときだけ丸括弧の中の強度語を落とす）**を先に試したか。⇒ (b) を採ったなら、(a) が成立しない理由が実測で示されているか** |
| **D-2** | **★(b) を採ったなら、設計卓へ請求してから進めたか**（指示書 §0.4 / §7） |
| **D-3** | **★選定理由が段 1 の実数に基づいているか。⇒ 「数から導けているか」** |

---

## 5. 束 E — ★並列と検査

| # | 観点 |
|---|---|
| **E-1** | **★★共有テスト**（`moveSurfacing.roster.test.ts` ／ `specialFamilyLabel.roster.test.ts`）**を触ったなら、何をどう変えたかが報告に在るか** |
| **E-2** | **★★`make e2e` を*全数*回しているか**（`SUPP-001` §5.5.7 (i)）**。★`m30-05-family-label-strength-word.spec.ts` 3 本を含めて緑か** |
| **E-3** | **`pnpm test` / `pnpm tsc --noEmit` / `pnpm lint` が緑か。★`eslint-disable` を足していないか** |
| **E-4** | **`check-import-order.sh` がベースラインから増えていないか。★レーン内で下げていないか**（`D-388`） |
| **E-5** | **★手元の `DES-005` が 2.112.0 以上・`DES-004` が 1.32.0 以上であることを着手時に確かめたか**（指示書 §1 の版ゲート ／ `playbook` §4.60） |

---

## 6. ★★見なくてよいもの

| # | 何を | なぜ |
|---|---|---|
| 1 | **【ジャスト】の命名の揺れ** | **保留 `P-56`。⇒ 裁定待ちであり本サブの射程外** |
| 2 | **`ODサンシュート`（ingrid）が残る仕組み** | **`M30-05` が実装コメントとテストで固定済み。⇒ 設計書への反映も不要と裁定した** |
| 3 | **CSV ローダの二重化** | **`followup` の `seed-roster-loader-duplicated`。⇒ `M31-06` 着地後の別手番** |
| 4 | **`moveSurfacing.roster.test.ts` 冒頭の失効した実測値** | **`followup` の `roster-test-db-row-count-stale`。⇒ 別手番** |
| 5 | **CHANGE の起票** | **★製造は自採番しない**（`D-293`）**。⇒ 請求は報告 §6 へ** |

---

*以上、M30-06 レビューチェックリスト **v1.0.0**。* **★★束 A が本体である。⇒ 「4 行の名前が変わった」で通さないこと。** **★★もう 1 つは B-1 である**——**`family` が変わると保存済みのコンボが壊れる。digest で確かめること。**
