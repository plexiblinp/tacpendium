# M28-04 レビューチェックリスト: `move_code` / `command` の是正 ＋ `startup` / `total` の 6 行

| 項目 | 内容 |
|------|------|
| 文書ID | M28-04-review-checklist |
| バージョン | **1.0.1** |
| 対象指示書 | `docs/instructions/M28-04-move-code-and-command-corrections.md` **v1.2.0** |
| 推奨モデル | **高精度レビュー**（**★★件数は少ないが、直す面が 2 つ**〔DB と CSV〕**あり、片方だけ直すと次の seed 再生成で黙って戻る**） |
| 作成者・作成日 | 設計担当 Claude（設計卓・フェーズ4 期）/ 2026-09-06 |

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| 1.0.1 | 2026-09-06 | **★★失効の注記を冒頭へ足した＝`D-759`。★§6-1 の破壊確認に本チェックリスト自身の欠陥が在った**（区分を指定していなかった＝計測点 `M-139`）**。★本文は書き換えていない**＝`D-274` (3) |
| 1.0.0 | 2026-09-06 | 初版（`D-753`。**★投入の合図と同じ手番で起こした**＝`D-736` の運用） |

---

> **★★【2026-09-06 失効＝`D-759`】本チェックリストの観点の多くは対象不存在になった。**
>
> **★`M28-04` は射程 9 行がすべて是正済みだったため、コードを 1 行も触っていない。⇒ §1〜§5 の多くと §6 の破壊確認 2 件、§7 の重大判定 1〜8 は対象が存在しない。**
>
> **★★§6-1 の破壊確認には、本チェックリスト自身の欠陥が在った**（計測点 `M-139`）**——「CSV を直さずに DB だけ直した状態を作り、seed を再生成する」とだけ書き、どの区分のキャラで試すかを書かなかった。⇒ `seedgen` の引数なし `-check` は第一波 9 キャラしか再生成せず、第一波外は壊しても緑が返る**（`CHANGE-163` §1）**。★手順を素直に実行すると「機構が守っている」の偽の証拠が作れた。**
>
> **★本文は書き換えていない**（`D-274` (3)）**。⇒ 恒久の規約は `SUPP-001` §5.5.4 (10) に在る。**

## 0. レビュー担当への前置き

### 0.1 レビュー前の準備

1. **指示書 v1.1.0 を通読する**（とくに §0.1 の「面が 2 つ」、§2.6 の 6 行、§2.5 の波及）。
2. **`SUPP-001` §2.7 を読む**（**★★2026-09-06 に「欠番」と「命名規約とライセンス」の 2 ブロックが入った**＝`CHANGE-158`）。
3. **`followup-backlog.md` の `csv-db-frame-cost-columns-drift` を読む**（**★本サブが踏みうる罠の先例**）。

### 0.2 ★★本サブの本質は「件数」ではなく「面」である

**3 件 ＋ 6 行 ＝ 9 行の値の是正にすぎない。★しかし直す面は 2 つある**——**DB（マイグレ）と `character_data/*.csv`。**

**★★CSV を直さずに DB だけ直すと、次の seed 再生成で元へ戻る。⇒ 戻ったことはエラーにならない。**

**★★これは `csv-db-frame-cost-columns-drift` の逆向きの同型である**（あちらは「`seedgen` が SQL へ出力しないため DB だけが進んだ」）。

### 0.3 ★★重大でないもの（**誤判定しないこと**）

| # | 内容 |
|---|---|
| 1 | **§2.6 の 6 行が保留のまま完了していること**（**★値は開発者が現物を見て出す。⇒ 届かなければ保留にしてよい。★書いていないことだけが違反である**＝指示書 §5-6） |
| 2 | **guile 以外の同型を直していないこと**（**★全キャラ点検は行わない方針**＝`D-161`。**見つけたら報告するだけ**） |
| 3 | **`docs/` の歴史記録に古い code が残っていること**（**★直さないのが正しい**＝`P-07` の先例） |
| 4 | **`check-md-emphasis.sh` / `check-import-order.sh` のベースラインが動いていないこと** |

### 0.4 ★★本サブに固有の危険（**最優先で見る**）

| # | 危険 | なぜ緑のまま通りうるか |
|---|---|---|
| **★★1** | **DB だけ直して CSV を直していない** | **★テストは緑になる。⇒ 次の seed 再生成で初めて戻る。★★そのときエラーは出ない** |
| **★★2** | **`UPDATE` を `code` だけで絞った** | **★`moves.code` はテーブル全体では一意でない**（`DES-003` §3.3）**。⇒ 他キャラに同名の code が在れば巻き添えになる。★巻き添えは静かに起きる** |
| **★★3** | **正しい code を製造が作った** | **★`DES-004` §2.1 の採番規則から導けないなら止まるのが正しい**（指示書 §2.2-1）**。⇒ 「それっぽい名前」を作っていたら赤** |
| **★★4** | **§2.2-3 の読みを解釈で埋めた** | **★(a) `startup_basis` が誤り ／ (b) `startup` の数値が誤り は、直す対象がまったく違う。⇒ 現物を見て開発者へ返すのが正しい** |
| **★★5** | **golden を黙って上書きした** | **★golden が動くのは正常だが、動いた理由を書かずに上書きすると、次に動いたとき誰も気づけない** |
| **★★6** | **`startup` を触ったのに波及を実査していない** | **★`moves.startup` は `M18-02` の確定反撃判定と `M19` の `S` 計算の両方が読む。⇒ 直せば両方の出力が動く。★動いた出力が全数で報告されているか** |
| **★★7** | **6 行の値を推測で埋めた** | **★どの行のどの列がいくつ誤っているかは、どこにも記録されていない。⇒ 推測できるはずがない。★埋まっていたら出所を問うこと** |

---

## 1. 束 A — 母数（**最初の成果物**）

| # | 見るところ |
|---|---|
| **★★1** | **3 件それぞれの現物の値が出ているか**（`character_data/guile.csv` と DB の**両方**） |
| **★★2** | **3 つの code のリポジトリ全体の走査結果が、件数つきで在るか**（`character_data/` ／ `migrations/` ／ `internal/` ／ `web/` ／ `docs/`） |
| **★3** | **guile 以外の同型を軽く見た結果が在るか**（**★見つかっても直さない。報告だけ**） |
| **★★4** | **§2.6 の 6 行について、値が届いたか／保留にしたかが書かれているか** |

---

## 2. 束 B — 3 件の是正

| # | 見るところ |
|---|---|
| **★★1** | **`sonic_break_light` の正しい code が `DES-004` §2.1 の採番規則から導かれているか。★導出の過程が書かれているか**（**★結論だけだと、作った語と区別がつかない**） |
| **★★2** | **`sonic_cross_2_meter_od` / `sonic_cross_3_meter_od` の `command` の交換が、`move_commands`（command 索引）へ波及しないかを実査しているか。★索引が command から引かれているなら、索引も張り替わる** |
| **★★3** | **§2.2-3 が (a) / (b) のどちらだったかが確定し、その根拠が書かれているか。★開発者の判断を仰いだ記録が在るか** |
| **★4** | **`character_data/guile.csv` を直しているか**（§0.4-1） |

---

## 3. 束 C — 6 行の是正（**★値が届いた場合のみ**）

| # | 見るところ |
|---|---|
| **★★1** | **値の出所が「開発者が出した」と書かれているか**（§0.4-7） |
| **★★2** | **6 行とも DB と CSV の両方で直っているか** |
| **★★3** | **§2.5 の波及の実査結果が全数で在るか**（**★6 行は 3 件より確実に `startup` を触る**） |
| **★★4** | **6 行が確定反撃サーチの候補に入っているかを実査したか**（`D-158` が「未確認」と書いた点。指示書 §2.6-6） |
| **★5** | **コミットが guile 3 件と分かれているか**（指示書 §3 の規約） |

---

## 4. 束 D — マイグレ

| # | 見るところ |
|---|---|
| **★★1** | **DML 1 本であること。★DDL が入っていないこと** |
| **★★2** | **`WHERE` が `character_id` ＋ `code` で絞られているか**（§0.4-2） |
| **★★3** | **★★命名規約に従っているか**（**2026-09-06 新設＝`CHANGE-158`。層 B のマイグレは `NNNNNN_data_*.sql`**）**。★本サブのマイグレは SF6 の事実そのものを直すので層 B である。⇒ `data_` を持たないと `scripts/check-migration-license.sh` の検査 (4) が赤になる** |
| **★★4** | **連番が `000106` であり、自採番していないこと**（`D-293`。**★設計卓が払い出す**） |
| **★★5** | **down が在り、何が失われるかが報告に書かれているか**（**★`startup` の是正は down で「元の誤った値」へ戻ることになる**） |
| **★6** | **`bash scripts/check-migration-license.sh` が緑であること**（**★★2026-09-06 に `M26-02` が入れた新しい検査である。⇒ 本サブが最初の適用先になる**） |

---

## 5. 束 E — 名指しと golden

| # | 見るところ |
|---|---|
| **★★1** | **テスト・E2E が当該 code を名指ししていないかを走査したか**（**★先例＝`migration-version-literals-in-tests`。名指しは静かに壊れる**） |
| **★★2** | **golden が動いたなら、動いた理由が報告に書かれてから更新されているか**（§0.4-5） |
| **★3** | **`docs/design/` に名指しが在ったか。在れば CHANGE を請求しているか**（**★製造は直さない**＝`CLAUDE.md` §8） |
| **★4** | **`docs/seed-data/` ／ 過去の完了報告の記述を直していないこと**（歴史記録） |

---

## 6. 破壊確認（**★2 件とも必須**）

| # | 壊すもの | 期待 |
|---|---|---|
| **★★1** | **CSV を直さずに DB だけ直した状態を作り、seed を再生成する** | **★元へ戻ることを実測で示すこと。⇒ 「戻る」と書くだけでは足りない。★★この確認が本サブの中心である** |
| **★★2** | **マイグレのファイル名から `data_` を外す** | **`scripts/check-migration-license.sh` が赤になること**（**★層 B の命名規約が実際に効いていることの陽性対照**） |

---

## 7. 重大な問題の判定基準（完了承認を妨げる）

| # | 内容 |
|---|---|
| **★★1** | **`character_data/*.csv` を直していない**（DB だけ直した） |
| **★★2** | **`UPDATE` が `code` だけで絞られている** |
| **★★3** | **正しい code を製造が作った**（採番規則から導いていない） |
| **★★4** | **§2.2-3 の読みを解釈で埋めた** |
| **★★5** | **`startup` を触ったのに §2.5 の波及を実査していない** |
| **★★6** | **6 行の値を推測で埋めた** |
| **★★7** | **マイグレが `data_` を持たない**（層 B の命名規約違反。`CHANGE-158`） |
| **★8** | **golden を理由なく上書きした** |
| **★9** | **連番を自採番した ／ `docs/design/` を編集した ／ `followup-backlog.md` の §J 以外を編集した** |

---

## 8. 軽微な問題の判定基準（持ち越し許容）

| # | 内容 |
|---|---|
| 1 | マイグレの `-note` の文面 |
| 2 | 報告の節立て |
| 3 | **guile 以外の同型の報告が粗いこと**（**★見つけたら報告するだけでよい**） |
| 4 | **6 行が保留のまま完了していること**（**★理由が報告に在る場合に限り軽微とする**） |

---

## 9. レビュー完了の判定

1. **§7 の重大が 0 件である。**
2. **§6 の破壊確認 2 件が実走されている**（**★とくに 1 は本サブの中心である**）。
3. **`go test ./...` / `cd web && pnpm test` / `make e2e` が緑。**
4. **常設検査が緑**（**★`check-artifact-integrity.sh` を 1 本目に回す**＝`CLAUDE.md` §8。**★`check-migration-license.sh` も回す**）。
5. **`docs/progress/progress-log.md` へ追記されている。**

### 9.1 ★本チェックリスト自身の検査（発行前に設計担当が行う）

- [x] **対象指示書の版数と一致している**（v1.1.0）
- [x] **`bash scripts/check-instruction-format.sh` が緑**
- [x] **`bash scripts/check-md-emphasis.sh` が緑**

---

*以上、M28-04 レビューチェックリスト **v1.0.1**。* **★★本サブでレビューが最も見落としやすいのは「9 行を直すだけ」と読むことである。** **★中心は「DB と CSV の両方を直したか」であり、片方だけならテストは緑のまま通り、次の seed 再生成で黙って戻る**（§0.4-1）**。⇒ §6 の破壊確認 1 を必ず実走させること。** **★★もう 1 つは、本サブが `check-migration-license.sh`**（`M26-02` が 2026-09-06 に入れた）**の最初の適用先だということである**（§4-6）**——命名規約に従っていない層 B のマイグレは、ここで初めて赤になる。**
