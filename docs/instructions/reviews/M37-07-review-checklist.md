# M37-07 レビューチェックリスト（始動技の持続当てを重複判定キーへ）

| 項目 | 内容 |
|------|------|
| 文書ID | M37-07-REVIEW-CHECKLIST |
| バージョン | **1.2.0**（2026-09-14・**★開発者裁定 2 件を反映**〔**フィルタは足す** ／ **表示語は「持続当て（始動技）」**〕） ／ **1.1.0**（2026-09-14・**★マイグレ番号を `000117` へ追随**〔`D-875`〕） ／ **1.0.0**（2026-09-13・発行） |
| 対象指示書 | `docs/instructions/M37-07-starter-meaty-key.md` **v1.2.0** |
| 作成者 | 設計担当 Claude（設計卓） |
| 位置づけ | **レビュー担当の主入力**（`D-736`） |

---

## 0. ★★レビュー担当への前置き

**★★★本サブは重複判定キーを 7 → 8 要素へ広げる。⇒ 足す先は 1 か所ではない。**

**★★漏らしたときの症状が*静か*である。⇒ 「重複でないものが重複と言われる」／「`PUT` へ行くべき編集が `PATCH` へ行く」。★どちらも型検査もテストも通る。**

| # | 本サブ固有の不合格 |
|---|---|
| **1** | **★★★重複判定キーの足し先を数え落とした**（箇所数が報告に無いものを含む） |
| **2** | **★★★`recipe_hash` が変わった** |
| **3** | **★★列が NULL 可、または 3 値になっている** |
| **4** | **★★`hit_type` に値を足した** |
| **5** | **★`combo_oki_options` を触った** |
| **6** | **★`PATCH` に載せた** |

---

## 1. 束 A — ★★★重複判定キー（**本サブの本体**）

| # | 観点 |
|---|---|
| **A-1** | **★★★段 1-1 で足し先を*数えて*いるか。⇒ 箇所数が報告に在るか。★数えずに直し始めていないか** |
| **A-2** | **★★★数えた全箇所へ足したか**〔Go の判定 ／ フロントの `extractKeyFields` / `hasKeyChanges` ／ `CheckDuplicateInput` ／ CSV の事前重複判定〕 |
| **A-3** | **★★★破壊確認が在るか＝キーから `starter_meaty` を外すと、値だけ違う 2 行が*重複と判定される*こと**（§5-2）**。⇒ 落ちないなら判定は何も見ていない** |
| **A-4** | **★★値だけが違う 2 行を*別コンボ*として登録できるか**（§5-1） |
| **A-5** | **★同じなら従来どおり重複と判定されるか** |
| **A-6** | **★論理削除済みの行が従来どおり判定対象外か** |
| **A-7** | **★DB へ UNIQUE 索引を新設していないか。⇒ 判定はアプリ層である**（`VAL-C02`） |

---

## 2. 束 B — ★★★変えてはいけないもの

| # | 観点 |
|---|---|
| **B-1** | **★★★`recipe_hash` が既存全行で 1 バイトも変わっていないか**（§5-5）**。⇒ `starter_meaty` は recipe の一部ではない** |
| **B-2** | **★★★`hit_type` に値を足していないか。⇒ 8 値のままか**（`CHANGE-198`） |
| **B-3** | **★★`combo_oki_options` の差分が 0 か**（§5-7）**。⇒ 向きが違うものである** |
| **B-4** | **★既存行へ `UPDATE` を書いていないか。⇒ 既定の `0` に任せたか** |
| **B-5** | **★modifier の `meaty` に手を出していないか。⇒ `M37-06` の射程である** |
| **B-6** | **★セットプレイとの連携を作っていないか。⇒ `SM-146` はフェーズ5 である** |

---

## 3. 束 C — ★★列の形

| # | 観点 |
|---|---|
| **C-1** | **★★★`NOT NULL DEFAULT 0` か。⇒ NULL 可にしていないか**（**★重複判定キーに入るため。`NULL != NULL` で意味の無い分岐が生まれる**） |
| **C-2** | **★★3 値**〔持続当て / 非持続当て / 不問〕**にしていないか** |
| **C-3** | **★既存の bool 列**〔`is_projectile` / `is_derived`〕**に倣った形か。⇒ `CHECK` 制約も実物を見て揃えたか** |
| **C-4** | **★列名が特定機能の用途を持ち込んでいないか。⇒ 「始動が持続当てであること」という*事実*を名前にしているか** |
| **C-5** | **★マイグレが `000117` の 1 本か。⇒ 自採番していないか**（`D-293`）**。★`000113` は欠番である** |
| **C-6** | **★`check-migration-license.sh` が緑か** |

---

## 4. 束 D — ★経路と画面

| # | 観点 |
|---|---|
| **D-1** | **★★`PATCH` に載っていないか。⇒ 識別キーだからである**（`CHANGE-195` §2.4） |
| **D-2** | **★★この列を変える編集が `PUT` へ行くか**（§5-4） |
| **D-3** | **★`POST` / `PUT` の要求 DTO と応答 DTO に在るか** |
| **D-4** | **★★CSV へ*任意列として列末尾*に足したか。⇒ ★旧 CSV**〔列が 1 つ少ない〕**が従来どおり読めるか**（§5-6） |
| **D-5** | **★編集画面の*状況*の欄に在るか。⇒ 詳細画面に出ているか** |
| **D-6** | **★★★一覧のフィルタへ*足してある*か**〔**2026-09-14 開発者裁定。⇒ 足さないのは不可になった**〕**。★表示語が「持続当て（始動技）」であるか**〔**★案の「持続当て（始動）」ではない。1 文字違う**〕 |

---

## 5. 束 E — ★★設計書の原稿

| # | 観点 |
|---|---|
| **E-1** | **★★★`SUPP-001` の重複判定キーの節を 7 → 8 要素へ広げる原稿が在るか。⇒ ★ここが正本である** |
| **E-2** | **★`DES-006` VAL-C02 ／ `DES-003` §3.3 ／ `DES-002` §4.2・§7 ／ `DES-005` §5.7・§5.6 の原稿が在るか** |
| **E-3** | **★設計書本体を書き換えていないか**（`CLAUDE.md` §8） |
| **E-4** | **★自採番していないか**（`D-293`） |

---

## 6. 束 F — ★実査と検査

| # | 観点 |
|---|---|
| **F-1** | **★★段 1 の 5 点が報告に在るか** |
| **F-2** | **★既存行の総数を数え直しているか**（**★開発者実測＝133 行〔有効 109 / 削除済み 24〕**） |
| **F-3** | **★数値の隣に「実測」「算出」が書き分けられているか** |
| **F-4** | **★★着手前の版ゲート 5 点を通したか**（§0.6）**。⇒ とくに 2＝`M37-04` の着地、3＝最新が `000115`** |
| **F-5** | **★★★全数の合否をパイプを挟まずに判定したか。⇒ `EXIT=0` と `FAIL` の件数が報告に貼られているか** |
| **F-6** | **`check-enum-sync.sh` ／ `check-import-order.sh` が緑か**（**★BASELINE は 99**） |
| **F-7** | **★★★`docs/handover/followup-backlog.md` を 1 文字も編集していないか**（`D-838`） |
| **F-8** | **★`progress-log.md` へ 1 行追記したか ／ `docs/usermanual/` の該当章が追随しているか**（**★スクリーンショットは除く**） |

---

## 7. ★★見なくてよいもの

| # | 何を | なぜ |
|---|---|---|
| 1 | **重複判定キーに入れるべきか** | **★★★開発者裁定済み**（`D-874`）**。⇒ 入れる** |
| 2 | **`hit_type` に畳めないか** | **★開発者が「ヒット種別とも別枠」と明言している** |
| 3 | **セットプレイとの連携** | **★`SM-146` はフェーズ5 である**（`D-646`） |
| 4 | **一覧のフィルタの有無** | **★★★2026-09-14 決着＝足す。⇒ 実装判断ではない。★無ければ差し戻す** |
| 5 | **CHANGE の起票** | **★製造は自採番しない** |

---

## 8. ★★対照値（**⇒ 数え直させる**）

| 対象 | 値 |
|---|---:|
| **着手前の重複判定キーの要素**〔recipe ＋ 6 列〕 | **7** |
| **★★着地後** | **★8** |
| 消費するマイグレ | **1**（`000117`） |
| **既存行**（開発者実測。有効 109 / 削除済み 24） | **133** |
| **着地後に `starter_meaty = 1` の行** | **★0** |
| `combo_oki_options` の変種（**★触らない**） | 12 |
| `hit_type` の値（**★触らない**） | 8 |

**★★★キーを増やすと「重複」が「別コンボ」になる方向にしか動かない。⇒ 既存行はすべて `0` であるから、実際には 1 件も変わらない。★それを確かめること**（§5-3）**。**

---

*以上、M37-07 レビューチェックリスト **v1.2.0**。* **★★★束 A-1 を最初に見ること**——**足し先を数えていなければ、漏れているかどうかを誰も言えない。** **★★次に重いのは B-1 である**——**`recipe_hash` が変わったら、重複判定と `recipe_cache` が同時に狂う。**
