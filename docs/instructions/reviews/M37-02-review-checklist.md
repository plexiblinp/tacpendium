# M37-02 レビューチェックリスト（仮想コントローラ UI の 10 件）

| 項目 | 内容 |
|------|------|
| 文書ID | M37-02-REVIEW-CHECKLIST |
| バージョン | **1.0.0**（2026-09-13・発行） |
| 対象指示書 | `docs/instructions/M37-02-virtual-controller-vertical.md` **v1.0.0** |
| 作成者 | 設計担当 Claude（設計卓） |
| 位置づけ | **レビュー担当の主入力**（`D-736`） |

---

## 0. ★★レビュー担当への前置き

**★★★本サブは「見た目を変えるサブ」ではない。⇒ 目的は*縦の消費*を減らすことである**（`D-857`）**。**

**★だから最初に見るのは差分ではなく*数値*である。⇒ 段 1 と段 6 の縦寸法が、同じ条件・同じキャラで 2 点そろっているか。**

| # | 本サブ固有の不合格 |
|---|---|
| **1** | **★★★縦の前後 2 点が無い**〔段 1 を飛ばした。⇒ もう取り直せない〕 |
| **2** | **★★★既存の床を黙って外した**（`D-347` 由来の 3 文の床を含む） |
| **3** | **★★`GAMEPAD_INPUT_NOTICE_POINTS` の文言を書き換えた** |
| **4** | **★★10 件のうち判定の無いものが在る**（**★8 件で閉じた形を含む**） |
| **5** | **★`B04` を新規生成だけで閉じ、`recipe_cache` に触れていない** |

---

## 1. 束 A — ★★★縦の消費（**本サブの本体**）

| # | 観点 |
|---|---|
| **A-1** | **★★★段 1 の実測が、コードを変える前に取られたか。⇒ 報告の順序と枝元コミットで確かめる** |
| **A-2** | **★★★段 1 と段 6 が*同じ条件・同じキャラ*か。⇒ 条件が違えば比較になっていない** |
| **A-3** | **★★区画ごとの内訳**〔告知 / 読取 / タブ / 通常技 / 必殺技〕**が前後で出ているか。⇒ どこが効いたか言えるか** |
| **A-4** | **★★★`B13` で横を縮めた結果、折り返して縦が*増えて*いないか**（指示書 §4.1。**★`min-h 3.25rem` が family 数だけ繰り返す構造である**） |
| **A-5** | **★実測が Playwright か。⇒ `make e2e-only P=<パターン>` を使ったか**（**★`pnpm exec playwright test` を直接叩いていないか**＝`D-599`） |
| **A-6** | **★★`B14` の実機確認を開発者へ*請求*したか。⇒ 製造が自分で「達成した」と判定していないか**（`D-855`） |
| **A-7** | **★`B09` の局所スクロールで、レシピが視界から出ない形になっているか**（指示書 §2.4） |

---

## 2. 束 B — ★★★`B10` の形

| # | 観点 |
|---|---|
| **B-1** | **★★★3 文がそのままか。⇒ 定数を 1 文字も編集していないか** |
| **B-2** | **★★初回のみ自動表示 ＋ 以降は明示操作で開く ＋ 既読フラグを localStorage、の 3 点がそろっているか** |
| **B-3** | **★★★畳んだ形が差し替えやすいか**〔ラベル 1 行 / 完全に消す の両方を 1 か所の変更で試せる〕**。⇒ 開発者は実物を見てから決める**（`P-59` (2)） |
| **B-4** | **★キー名が `gamepad-input-notice-seen-v1` か。⇒ `<feature>-seen-v1` 命名か** |
| **B-5** | **★★台帳**〔`web/CLAUDE.md` §1〕**へ 1 行足したか。⇒ `check-browser-storage-keys.sh` が緑か** |
| **B-6** | **★専用ヘルパ `web/src/lib/browser-storage.ts` 経由・try-catch か。⇒ 直接 `localStorage` を呼んでいないか** |
| **B-7** | **★2 面**〔コンボ / セットプレイ〕**で既読を共有しているか。⇒ 2 個目のキーを作っていないか** |
| **B-8** | **★★CHANGE 原稿が設計伝達レポート §4 に在るか。⇒ 自採番していないか**（`D-293`） |

---

## 3. 束 C — ★★★床（**外れていないこと**）

| # | 観点 |
|---|---|
| **C-1** | **★★★`gamepadRecipeInput.test.tsx:500`**〔告知定数が 3 件・全項目が非空・「不具合ではなく」を含む〕**が緑のままか。⇒ 落ちているなら形を取り違えている** |
| **C-2** | **★★同 `:477,490` / `:528,569` ／ `VirtualController.test.tsx:529` が緑か** |
| **C-3** | **★★★外したテスト・コメントが 1 つでも在るなら、理由が設計伝達レポート §4 に在るか**（overview §4.3＝**黙って外すのが誤りである**） |
| **C-4** | **★★`D-347` の 3 点目**〔不具合ではなく意図的な設定であること〕**が、開いたときに読めるか。⇒ 「初回も出さない」形になっていないか** |
| **C-5** | **★足したのは「2 回目以降は畳まれている」「明示操作で開ける」の*新しい*床か** |

---

## 4. 束 D — ★★`B11` の情報量

| # | 観点 |
|---|---|
| **D-1** | **★★★区画 1**〔押している入力〕**／ 2**〔確定したステップ〕**／ 5**〔解決できなかった入力〕**が残っているか。⇒ `DES-005` §6.4.3 の設計要件である** |
| **D-2** | **★★★解決できなかった入力の 6 理由が消えていないか。⇒ 「ラッシュ版は空中では出せない」を「技が無い」へ丸めていないか** |
| **D-3** | **★★区画 3 / 4 を畳んだのは*案内文*だけか。⇒ 直前操作結果・入力中の表示・履歴を消していないか** |
| **D-4** | **★★条件表示で畳んでいるか。⇒ 2 個目の既読フラグを作っていないか** |
| **D-5** | **★区画 1 / 2 / 5 を減らしたなら、CHANGE 原稿と理由が在るか。⇒ 無断で消していないか** |

---

## 5. 束 E — ★★`B04` / `B05`（**modifier**）

| # | 観点 |
|---|---|
| **E-1** | **★★★flag 10 値すべてで `{` ＋ 内部識別子 ＋ `}` が出なくなったか。⇒ 4 値だけ直して閉じていないか** |
| **E-2** | **★★★破壊確認が在るか＝表示語を 1 つ外すと再びフォールバックが出ること**（指示書 §5-3）**。⇒ 無ければ「効いた」ことの証拠にならない** |
| **E-3** | **★★★`recipe_cache` の扱いが、採った形と理由つきで報告に在るか。⇒ 「新規は直った」で閉じていないか** |
| **E-4** | **★★既存行の `recipe_cache` を書き換えたなら、それを請求した記録が在るか。⇒ 黙ってデータを更新していないか** |
| **E-5** | **★`ModifiersEditor` の type-only タイトルが内部 type を返す分岐が潰れたか** |
| **E-6** | **★`low_jump` の表示語の食い違い**〔UI「低ジャンプ」／ サーバ「最低空」〕**が解消されたか** |
| **E-7** | **★★「不足」を*設計書に在って実装に無いもの*で数えたか。⇒ `micro_walk` を不足に数えていないか**（`DES-004` §2.1 が独立 move として不採用） |
| **E-8** | **★★★「不要」を*DB 件数 0* で数えていないか。⇒ 利用者はまだいない**（`D-857`）**。★削ったなら設計書の根拠が在るか** |

---

## 6. 束 F — ★触っていないもの

| # | 観点 |
|---|---|
| **F-1** | **★`A`**〔運び量 UI〕**へ手を出していないか。⇒ `M37-01` の射程である** |
| **F-2** | **★`B01` `B02` `B03` `B06` `B15` へ手を出していないか** |
| **F-3** | **★★DB 列を足していないか・マイグレを消費していないか。⇒ 消費したなら請求の記録が在るか** |
| **F-4** | **★★スクリーンショットを撮っていないか。⇒ 開発者の手番であり `M37` の全着地後である**（overview §4.2） |
| **F-5** | **★★★`docs/handover/followup-backlog.md` を 1 文字も編集していないか**（`D-838`） |
| **F-6** | **★設計書本体を書き換えていないか**（`CLAUDE.md` §8） |
| **F-7** | **★★サブを自分で割っていないか。⇒ 大きいと判断したら請求する形になっているか**（指示書 §4.5） |

---

## 7. 束 G — ★実査と検査

| # | 観点 |
|---|---|
| **G-1** | **★★★10 件すべてに判定**〔完了 / 一部完了 / 未着手〕**が付いているか。⇒ 10 件であって 8 件ではない**（指示書 §0.1） |
| **G-2** | **★★「既に解消」とした項目に、*今回の要求を満たす具体的な証拠*が付いているか。⇒ 類似機能の過去の完了を根拠にしていないか** |
| **G-3** | **★★件数を数え直しているか。⇒ `M37-RESEARCH-01` の値をそのまま転記していないか**（計測点 `M-157`） |
| **G-4** | **★数値の隣に「実測」「算出」が書き分けられているか** |
| **G-5** | **★★着手前の版ゲート 5 点を通したか**（指示書 §0.7）**。⇒ とくに 3＝`M37-01` の着地、4＝`M32-02` の撮影が始まっていないこと** |
| **G-6** | **`go test ./...` ／ `pnpm test` ／ `make e2e`（全数）が緑か** |
| **G-7** | **`check-browser-storage-keys.sh` ／ `check-import-order.sh` ／ `check-enum-sync.sh` が緑か** |
| **G-8** | **★`docs/usermanual/` の該当章が追随しているか**（**★スクリーンショットは除く**） |
| **G-9** | **★`docs/progress/progress-log.md` へ 1 行追記したか**（`CLAUDE.md` §8） |

---

## 8. ★★見なくてよいもの

| # | 何を | なぜ |
|---|---|---|
| 1 | **畳んだときの見た目が最終形か** | **★開発者が実物を見てから決める**（`P-59` (2)）**。⇒ 両方を試せる形になっていれば足りる** |
| 2 | **`B14` を達成したか** | **★開発者の実機確認である**（`D-855`）**。⇒ 請求したことまでを見る** |
| 3 | **縦が何 px 減れば十分か** | **★閾値は置いていない。⇒ 前後 2 点と内訳が出ていれば足りる** |
| 4 | **CHANGE の起票** | **★製造は自採番しない**（`D-293`） |
| 5 | **スクリーンショットの有無** | **★撮らないことが正しい**（overview §4.2） |

---

## 9. ★★対照値（**`M37-RESEARCH-01` の実測。⇒ 数え直させる**）

| 対象 | 実測値 |
|---|---:|
| **読取欄の区画数**（**★3 ではなく 5 である**） | **5** |
| 解決できなかった入力の理由 | 6 |
| コマンド入力の理由 | 4 |
| **modifier flag の UI 定義** | **10** |
| うちサーバ側で日本語へ変換される値（**★残り 6 値がフォールバック**） | **4** |
| modifier type | 2 |
| 告知の文 | 3 |
| **床＝直接参照**（テスト 7 ファイル） | **31 行** |
| 床＝ヘルパと呼出し（4 ファイル。**★上の 31 行とは別集合**） | 43 行 |
| **`recipe_cache` に波括弧を含む行** | **combos 2 ／ setups 1** |

**★★★「31 行」はテストケース数ではない**（`M37-RESEARCH-01` §5.3 の注記）**。⇒ 行数とケース数を取り違えないこと。**

---

*以上、M37-02 レビューチェックリスト **v1.0.0**。* **★★★束 A-1 を最初に見ること**——**段 1 の実測が取られていなければ、以降の「減った」はすべて主張にすぎない。** **★★次に重いのは C-1 である**——**床が落ちているなら、`B10` の形を取り違えている。**
