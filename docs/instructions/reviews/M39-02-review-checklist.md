# M39-02 レビューチェックリスト（ラッシュ版の生成経路が実行時に `D-187` を破る穴）

| 項目 | 内容 |
|------|------|
| 文書ID | M39-02-REVIEW-CHECKLIST |
| バージョン | **1.0.0**（2026-09-19・発行） |
| 対象指示書 | `docs/instructions/M39-02-rush-variant-invariant-fix.md` **v1.0.0** |
| 作成者 | 設計担当 Claude（設計卓） |
| 位置づけ | **レビュー担当の主入力**（`D-736`） |

---

## 0. ★★レビュー担当への前置き

**★★★本サブは「テストが緑か」では判定できない。⇒ この穴は*実行時*であり、`M39-01` が置いたガードは是正の前後どちらでも緑である。**

**★★★問うのは「是正*前*に壊れることを示したか」である。⇒ 直してから「壊れていない」だけを示す報告は、何も立証していない。**

| # | 本サブ固有の不合格 |
|---|---|
| **★★★1** | **段 1 の再現の出力が無い**（**★是正前に `startup NULL` ＋ `'through'` の行ができることを示していない**） |
| **★★★2** | **`M39-01` のガードが緑のまま素通りしたことを示していない**（**★これが「実行時の穴である」の立証そのものである**） |
| **★★★3** | **破壊確認の出力が無い**（**★是正を外すと赤くなること**） |
| **★★★4** | **対照が無い**（**★元技が `startup` を持つ場合は `'through'` のまま。⇒ `'through'` を一般に外していないか**） |
| **★★★5** | **`migrations/` に差分が在る**（**★本サブの消費は 0 本。⇒ 実行時の経路の話である**） |
| **★★★6** | **件数のハードガードを置いている**（**★規約 (24)-4。⇒ その経路を 1 回通った利用者のアプリが起動しなくなる**） |
| **★★7** | **`rushEligible` を承認なしに変えている**（**★提供する技の集合が変わる**） |
| **★★★8** | **完了報告を書いた後に回し直した検査の*出力*が無い**（**★本サブは出力まで求めている**＝`M39-01` §7-1 の 2 度目） |

---

## A. ★★★再現（**段 1**）

| # | 見ること |
|---|---|
| **★★★A-1** | **§0.2 の 3 件のいずれかで実際に API を叩き、違反行ができることを*出力で*示しているか** |
| **★★A-2** | **不変条件のクエリが 0 → 1 へ増えることを示しているか** |
| **★★★A-3** | **同じ DB で `M39-01` のガードが*緑のまま*であることを示しているか。⇒ これが無いと、本サブが何を直したのか読めない** |
| **★★A-4** | **3 件の id を `move_code` で引き直したか。⇒ `1364` / `2145` / `2555` を引き写していないか**（**★2026-09-19 の実測値である**） |
| **★A-5** | **件数が 3 でなかった場合、件数と内訳を報告へ書いたか** |

---

## B. ★★★是正（**段 2**）

| # | 見ること |
|---|---|
| **★★★B-1** | **採った案**〔(a) / (b)〕**と理由が報告に在るか。⇒ (b) なら開発者の承認が在るか** |
| **★★★B-2** | **元技が `startup` を持つ場合の挙動が 1 ビットも変わっていないか。⇒ `'through'` のままか** |
| **★★★B-3** | **`'through'` のリテラルを*一般に*外していないか**（**★`M19-04` §4.5 の設計判断**） |
| **★★B-4** | **置き場**〔SQL / Go〕**の理由が在るか** |
| **★★B-5** | **`startup` 以外の列**〔`total` ／ `active` ／ `recovery` 等〕**に手を入れていないか。⇒ 射程は `startup_basis` だけである** |

---

## C. ★★★テスト（**段 3**）

| # | 見ること |
|---|---|
| **★★★C-1** | **実行時の経路に対するテストが在るか。⇒ 新規 DB の最終状態を見るテストで代用していないか** |
| **★★★C-2** | **破壊確認の出力が在るか。⇒ 是正を外して赤くなることを示しているか** |
| **★★C-3** | **対照が在るか**（§B-2 の立証） |
| **★★★C-4** | **環境変数で走行を切り替えるテストになっていないか**（**★`M39-01` §7-2 の型＝緑のまま黙って skip する**） |
| **★C-5** | **`M39-01` のガードとの*性格の違い*が、ファイル名かコメントから読めるか**〔新規 DB の最終状態 ／ 実行時の経路〕 |

---

## D. ★★既存の違反行（**段 4**）

| # | 見ること |
|---|---|
| **★★D-1** | **数えるクエリが手順として在るか** |
| **★★D-2** | **在った場合の直し方**〔`UPDATE` 1 本〕**が同じ手順に在るか** |
| **★★★D-3** | **開発者の検証 DB を触っていないか。⇒ 製造は乾式まで** |
| **★★D-4** | **新規ファイルを作っていないか。⇒ `scripts/migrate-userdata-prompt.md` へ節として足したか**（`CLAUDE.md` §10.Y） |

---

## E. ★射程

| # | 見ること |
|---|---|
| **★★★E-1** | **`migrations/` の差分が 0 行か。⇒ `ls migrations/*.up.sql` が 9 本か** |
| **★★★E-2** | **件数のハードガードを 1 つも置いていないか** |
| **★E-3** | **`docs/design/` 本体・`followup-backlog.md` の差分が 0 行か**（`CLAUDE.md` §8 ／ `D-838`） |

---

## F. ★報告

| # | 見ること |
|---|---|
| **★★F-1** | **CHANGE 原稿が設計伝達レポートの §1 と §6 に在るか。⇒ §4 ではない**（計測点 `M-183`） |
| **F-2** | **完了報告 ＋ `progress-log` の索引行が在るか** |
| **★★★F-3** | **完了報告を書いた*後に*回し直した検査の*出力*が報告に在るか。⇒ 「回した」と書いてあるだけでは足りない**（**★本サブの完了条件 14**） |
| **★F-4** | **全数テストの出力をファイルへ落とし、`EXIT=` と `FAIL` の件数を貼っているか。⇒ `pnpm test` へオプションを渡すとき `--` を付けたか**（`M39-01` §7-5） |
| **★F-5** | **着手前の版ゲート 6 点を通したことが報告に在るか。⇒ とくに `M39-overview` が v1.3.0 であること**（playbook §4.74） |

---

## ★★不合格の扱い

**★「高」の指摘を不採用にするときだけ開発者確認が要る。★再レビュー往復の上限は 2 回である**（`CLAUDE.md` §9）**。**

---

*以上、M39-02 レビューチェックリスト **v1.0.0**。* **★★★§0 の 1 と 2 を最初に見ること**——**是正前に壊れることを示していない報告は、それだけで差し戻してよい。★`M39-01` のガードは是正の前後どちらでも緑であり、緑であることは何の証明にもならない。**
