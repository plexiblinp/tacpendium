# M38-01 レビューチェックリスト（新規登録・編集の必須と値域の是正）

| 項目 | 内容 |
|------|------|
| 文書ID | M38-01-REVIEW-CHECKLIST |
| バージョン | **1.1.0**（2026-09-18・**★as-built 注記。⇒ §0-3 と B-1 の「`VAL-C15` は 4 欄」は開発者裁定で覆った**＝`D-896`） ／ **1.0.0**（2026-09-17・発行） |
| 対象指示書 | `docs/instructions/M38-01-combo-editor-required-and-domain.md` **v1.1.0** |
| 作成者 | 設計担当 Claude（設計卓） |
| 位置づけ | **レビュー担当の主入力**（`D-736`） |

---

---

> **★★★【2026-09-18 追記＝`D-896`】§0-3 と B-1 の「`VAL-C15` の欄数が 4 でなくなっている ＝ 不合格」は、開発者裁定で覆った。**
>
> **★着地は 2 欄**〔`damage` / `knockdown_advantage`〕**である**（`CHANGE-216`）**。⇒ レビューの判定にも使われていない。**
>
> **★本節は歴史記録として残す。⇒ 「いつ何が決まり、いつ覆ったか」を追えなくなるため。★機構化は `playbook` §4.72。**

## 0. ★★レビュー担当への前置き

**★★★本サブは*正体が未確定のもの*を含む。⇒ 「ヒット種別の不問」は設計書に無く、実装にだけ在る。**

| # | 本サブ固有の不合格 |
|---|---|
| **★★★1** | **段 1 の実査より先に §2.5 を直している。⇒ 正体を確かめずに消すと、消し残すか消しすぎる** |
| **★★2** | **列が増えている。⇒ NULL が「不問」を表すため、列は 1 つも足さない** |
| **★★3** | **`VAL-C15` の欄数が 4 でなくなっている。⇒ 中身が入れ替わるだけである** |
| **★★★4** | **既存の NULL 行を `normal` へ寄せたのに、重複判定キーの衝突を数えていない。⇒ `hit_type` はキーの 1 つである** |
| **★5** | **必須でないものが基本情報の上へ混ざっている** |

---

## A. ★★★段 1 の実査（**最初に見る**）

| # | 見ること |
|---|---|
| **★★★A-1** | **「不問」の正体が実測で書かれているか。⇒ 選択肢の実体の在り処 ／ 選ぶと DB へ入る値 ／ `hit_type` の既定値** |
| **★★A-2** | **`hit_type` が NULL の既存行の*件数*が在るか。⇒ 「無いはず」ではなく実測である** |
| **★★★A-3** | **NULL 行を寄せたときの重複判定キーの衝突が数えてあるか。⇒ 0 組なら 0 組と書いてある** |
| **★A-4** | **実 DB が無いなら開発者へ依頼して止まったか。⇒ 推測で埋めていないか**（先例＝`M37-07` §4-4） |
| **★A-5** | **§2.5 の (A)(B)(C) のどれを採ったかと根拠が在るか** |

---

## B. ★★必須の入れ替えと「不問」

| # | 見ること |
|---|---|
| **★★B-1** | **`VAL-C15` が 4 欄のままか。⇒ `damage` / `knockdown_advantage` / `drive_available_at_start` / `sa_available_at_start`** |
| **★★★B-2** | **外した 2 欄**〔`drive_gauge_consumed` / `sa_gauge_consumed`〕**の*値域*検査が生きているか。⇒ 必須を外すのと値域を外すのは別である** |
| **★★B-3** | **「不問」を選ぶと NULL が入るか。⇒ 列を足していないか** |
| **★★★B-4** | **placeholder に薄く「不問」が出るか**（開発者の逐語） |
| **★★B-5** | **空のままでは保存できないか。⇒ 「不問を選んだ」は保存できるか。★この 2 つが区別できていること** |
| **★B-6** | **既存行の NULL を書き換えていないか。⇒ 表示が「不問」になるだけでよい** |

---

## C. ★操作性

| # | 見ること |
|---|---|
| **★C-1** | **矢印キーの上下で数値が変わらないか。⇒ フォーカスが動くか** |
| **★★C-2** | **スピナー**〔上下ボタン〕**が残っていないか。⇒ 押せるのに効かない状態を作っていないか** |
| **★C-3** | **必須項目が基本情報の上へまとまっているか。⇒ 必須でないものが混ざっていないか** |
| **★C-4** | **採った並びと理由が報告に在るか**（実装判断であるため） |

---

## D. ★★ヒット種別

| # | 見ること |
|---|---|
| **★★D-1** | **選択肢から「不問」が消えているか**（§2.5 が (A) のとき） |
| **★D-2** | **既定が `normal` になっているか** |
| **★★★D-3** | **`hit_type` の*値域*〔8 値〕を動かしていないか。⇒ 消すのは選択肢からであって値域からではない。★そもそも値域に「不問」は無い** |
| **★★D-4** | **マイグレを使ったなら*絶対値を書く* `UPDATE` になっているか**（`SUPP-001` §5.5 規約 (20) の (b)） |
| **★D-5** | **使わなかったなら `000118` を未消費で返したことが報告に在るか** |

---

## E. ★射程

| # | 見ること |
|---|---|
| **E-1** | **列を 1 つも足していないか**（``git diff`` で確かめる） |
| **E-2** | **`M38-02` の面**〔技編集 ／ 改名〕**を触っていないか** |
| **E-3** | **`docs/design/` 本体・`followup-backlog.md` の差分が 0 行か**（`CLAUDE.md` §8 ／ `D-838`） |

---

## F. ★報告

| # | 見ること |
|---|---|
| **F-1** | **完了報告 ＋ `progress-log` の索引行が在るか** |
| **★★F-2** | **CHANGE 原稿が設計伝達レポートの §1 と §6 に在るか。⇒ §4 ではない**（計測点 `M-183`） |
| **★★★F-3** | **完了報告を書いた*後に*、報告を入力に取る検査を回し直したか。⇒ `check-progress-log-index.sh` は報告を書いた瞬間に赤へ変わりうる**（`M36-01` §7-1） |
| **★F-4** | **全数テストの出力をファイルへ落とし、`EXIT=` と `FAIL` の件数を貼っているか。⇒ `head` で切っていないか** |

---

## ★★不合格の扱い

**★「高」の指摘を不採用にするときだけ開発者確認が要る。★再レビュー往復の上限は 2 回である**（`CLAUDE.md` §9）**。⇒ 達したら未解消項目を必須 5 フィールドで設計伝達レポート §4 へ記録して停止する。**

---

*以上、M38-01 レビューチェックリスト **v1.1.0**。* **★★★§A を最初に見ること**——**段 1 の実査が済む前に §2.5 を直していたら、そこで止めてよい。⇒ 正体が未確定のまま消したものは、戻すほうが高くつく。**
