# M22-03 レビューチェックリスト

| 項目 | 内容 |
|------|------|
| 対象指示書 | `docs/instructions/M22-03-optimistic-locking.md` v1.0.0（楽観排他の実効化） |
| 文書ID | M22-03-REVIEW |
| 対象指示書ID | M22-03 |
| レビューモデル | **高精度レビュー**（**★本サブは「効いている」と「テストが緑である」が最も乖離しやすい領域である**。実モデル名は `docs/human-notes/model-allocation.md` が正本。実使用は開発者判断） |
| バージョン | 1.0.0 |
| 作成者・作成日 | 設計担当 Claude（M22 期・設計卓）/ 2026-08-15 |

> 文書ID ⇄ 実パスの正は `docs/handover/docs-map.md`（自動生成）。

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| 1.0.0 | 2026-08-15 | 初版（指示書 v1.0.0 と対。**D-406**）。 |

---

## 0. レビュー担当への前置き

### 0.1 レビュー前の準備

**必読**——指示書 `M22-03` 全文 ／ **`M22-overview` §4.2 全文**（**★判定基準の正本**） ／ **`DES-002` §6.4・§4.2** ／ `DES-003` の `combos` / `setups` の `version` ／ **`DES-005` §5.11**（**★409 の先客**） ／ `DES-006` §10・§11 ／ **`docs/change-notes/CHANGE-114-notification.md`** ／ `docs/process/m22-contract.md` §1.2・§4・§5 ／ **`M22-RESEARCH-01` 調査レポート 軸 A・軸 B・§9-B-5**。

**★Plan Mode の実査 8 項目（指示書 §3.3）が確定してから実装されているかを最初に確認すること。** とくに次の 2 つは、進め方そのものを決める。

1. **2（`version_conflict` への統一の影響範囲）** —— **全数が出ないうちに着手していないか。** 広すぎるなら §9.3-2 で止まるのが正しい。
2. **3（33 経路の判定）** —— **§1.3 の線引きに照らして 1 件ずつ判定されているか。**

### 0.2 レビューの基本姿勢

**★本サブは「作る」サブではなく「固定する」サブである。** **実測では 3 経路とも既に突き合わせている**（`M22-overview` §4.2.1）。**⇒ 利用者から見た挙動は変わらないのが正しい。**

**★最重要ゲートは 2 つ。どちらも「破っても緑になる」型である。**

1. **テストが、壊すと赤くなる形になっていること**（指示書 §4.1）。**★`409` は一意制約違反でも返る**（`DES-005` §5.11＝指示書 §1.4）。**⇒ ステータスだけを見るテストは、版の突き合わせを外しても別の理由の 409 で緑になりうる。** **★エラーコードの文字列まで固定されているか。** **★破壊確認 A / B が行われ、赤くなったか。**
2. **§1.3 の「対象外」を穴として塞いでいないこと**（同 §4.2-2）。**★成立条件・セットプレイ紐付け・確定反撃系・タグ・プリセット・`recipe_cache` は、線引きの結果として対象外である。** **⇒ 塞ぐと、検証記録を 1 行足すたびに本体編集と衝突する。** **★これは「良かれと思って」起こる型であり、しかも塞いだ側は緑になる。**

**★次点は 2 つ。**

3. **`conflict` を見ている箇所が全数更新されていること**（同 §4.3-4）。**★片方だけ直すと、競合が「不明なエラー」になる。** **★画面に何も出ないまま壊れる。**
4. **`version: 0` が既定値の差し替えで済まされていないこと**（同 §4.4-4）。**★`1` へ変えるのは「必ず失敗する」を「静かに他人の編集を上書きしうる」に変える。悪化である。**

### 0.3 ★重大でないもの（**誤判定しないこと**）

| # | 誤判定しやすい状態 | 実は仕様である理由 |
|---|---|---|
| N-1 | **成立条件（`combo_setup_results`）に楽観排他が無い** | **★`D-394` の線引きの結果である**（指示書 §1.3）。**「別の人が別の状況を検証して足す」性格の記録であり、集約に含めると検証記録を 1 行足すたびに本体編集と衝突する。** **★衝突は `(combo_id, setup_id, tech_type, in_corner)` の組単位で起きる** |
| N-2 | **セットプレイ紐付け・確定反撃系 4 表に楽観排他が無い** | **同上**（指示書 §1.3）。**紐付けの追加／削除は「そのコンボの内容」ではなく関係の記録である** |
| N-3 | **タグ・プリセットに楽観排他が無い** | **同上。** **`FR013` により作成者のみ編集可能であり、衝突するのは同一人物の 2 端末に限られる** |
| N-4 | **`recipe_cache` の更新で `version` が上がらない**（`updated_at` は進む） | **★現状どおりが正しい**（指示書 §4.5＝契約 F-5）。**実装コメントが「キャッシュは排他対象外」と明記している。** **★「`updated_at` と `version` がずれている＝バグ」として直させないこと。** **★仕様として確定させるのが本サブの仕事である**（§5.1-9 が「直っていない」ことを固定する） |
| N-5 | **論理削除・復元で `version` が上がらない** | **★現状どおり**（指示書 §1.6-5）。**削除は「編集」ではない。** **★上げると、削除のたびに他の端末の編集が弾かれる** |
| N-6 | **タグだけを送る PATCH でも `combos.version` が上がる** | **★これが集約単位である**（指示書 §2.2-1＝**D-394**）。**★「タグだけなら上げない方が親切だ」と読んで細かくさせないこと。** **`D-394` が集約を選んだ理由がこれである** |
| N-7 | **新しい `version` 列が 1 本も足されていない** | **★`D-394` で確定済み**（指示書 §1.2）。**⇒ マイグレ消費 0 が正しい。** **★「スキーマが足りない」として列の追加を求めないこと** |
| N-8 | **409 の画面での見せ方が無い** | **★`M22-04` の担当**（指示書 §1.6-3）。**本サブはサーバの契約までである** |
| N-9 | **利用者から見た挙動が変わっていない** | **★それが正しい**（指示書 §0.2 の前提）。**実測では 3 経路とも既に効いている。** **⇒ 本サブは「固定する」サブである。** **★「差分が小さい」「本番コードの差分がテストとエラーコードだけ」は欠陥ではない** |
| N-10 | **開発者の手動確認が行われていない** | **★本サブは手動確認を挟まない**（指示書 §7.1＝**D-398** のいずれにも当たらない）。**画面を作らず、利用者から見た挙動が変わらないため** |
| N-11 | **`internal/` に diff がある** | **★本サブは `internal/` 全体を凍結していない**（**D-379** の型）。**凍結は `notation` / `preset` の解決ロジック（契約 F-5）・`recipe_cache` の読み書き（同）・`M22-01` の認証（指示書 §2.2-5）だけである** |
| N-12 | **`conflict` から `version_conflict` へ変えている** | **★設計卓の裁定である**（指示書 §1.5・§4.3＝**D-406**）。**`M22-overview` の旧記述〔`M22-04` の担当〕は改訂済み。** **★「スコープ外を先取りした」として扱わないこと** |

### 0.4 報告フォーマット

- 各節「OK / 重大（§9）/ 軽微（§10）/ 質問（§11）」。

---

## 1. 最重要ゲート

- [ ] **★3 経路すべてで、版が古いときに 409 ＋ `version_conflict` が返ることが固定されている**（指示書 §4.1-1・2・§5.1-1）。**★ステータスだけでなくコード文字列まで固定されているか。**
- [ ] **★版が一致するときに通ることも固定されている**（同 §4.1-4・§5.1-2）。**★拒否側だけを固定していないか**——**「常に拒否する」実装でも緑になる。**
- [ ] **★破壊確認 A が行われ、赤くなった**——**突き合わせを外すと §5.1-1 が赤くなる**（§5.1-3）。
- [ ] **★破壊確認 B が行われ、赤くなった**——**エラーコードを `conflict` へ戻すと §5.1-1 が赤くなる**（§5.1-4）。
- [ ] **★赤にならなかった項目について、「何が代わりに守っていたか」が特定され、同じ層に契約テストが置き直されている**（`SUPP-001` §5.5 (10′)）。**「テストを強くした」だけで済ませていないか。**
- [ ] **★一意制約違反の 409 と、版不一致の 409 が区別できる**（同 §1.4・§5.1-5）。**★これが「ステータスだけを見るテストが何も守らない」理由である。**
- [ ] **★§1.3 の「対象外」に楽観排他を足していない**（同 §4.2-2）。**★成立条件・紐付け・確定反撃系・タグ・プリセット・`recipe_cache`。** **★足してあったら §9-2。**
- [ ] **★33 経路（DB へ書くもの 28 件）の判定が経路ごとに 1 行の根拠付きで示されている**（同 §4.2-1・§7.5-2）。**★「0 件だった」で終わっていないか**（`E-84`）。
- [ ] **★`conflict` を見ている箇所が全数更新されている**（同 §4.3-4・§5.1-6）。**★片方だけ残っていないか**——**残ると競合が「不明なエラー」になり、画面に何も出ないまま壊れる。**

## 2. 方式の遵守（**★製造が選び直していないか**）

- [ ] **揃える先が `version_conflict` である**（指示書 §4.3-1・3）。**★`conflict` へ揃えていないか**——**409 は一意制約違反でも使われており、総称を版不一致に割り当ててはならない**（同 §4.3-2）。
- [ ] **`setups` 系のコードを動かしていない**（同 §4.3-3）。**★片方だけ動かすのが正しい。**
- [ ] **一意制約違反の 409 のコードを触っていない**（同 §4.3-6）。
- [ ] **見せ方（画面・文言・再読込の導線）を作っていない**（同 §1.6-3）。**★`M22-04` の担当。**
- [ ] **★`version: 0` を既定値の差し替えで済ませていない**（同 §4.4-4）。**★`1` へ変えていないか**——**「必ず失敗する」が「静かに他人の編集を上書きしうる」に変わる。**
- [ ] **`initial` が不在になる経路の実在が実査され、Plan Mode で提示されている**（同 §3.3-4・§9.1-2）。**★実在しないなら型で表せないかが提示されているか。**
- [ ] **`recipe_cache` を直していない**（同 §4.5-1・契約 F-5）。**★`updated_at` を見て競合を判定する実装を足していないか**（同 §4.5-4）。

## 3. 実装の中身

- [ ] **`combos` 系と `setups` 系で固定の強度が揃っている**（指示書 §4.1-5）。**★`M22-RESEARCH-01` A-5 の非対称〔`combos` はステータスのみ・`setups` はコードまで〕が解消されているか。**
- [ ] **タグだけを送る PATCH でも `combos.version` が上がることが固定されている**（同 §5.1-8）。**★集約単位が維持されていることの網である。**
- [ ] **`recipe_cache` の更新で版が上がらないことが固定されている**（同 §5.1-9）。**★「直っていない」ことを固定するのが目的である**——**次の担当が「バグだ」と読んで直すのを防ぐ。**
- [ ] **論理削除・復元で版が上がらないままである**（同 §1.6-5）。
- [ ] **エラーメッセージの文面が変わっていても、コードは `version_conflict` で固定されている**（同 §9.2-2）。

## 4. 非破壊性（触ってはいけないもの）

- [ ] **`migrations/` ／ `character_data/` ／ `docs/design/` の diff が 0**（指示書 §2.2-6・§7.3）。**実測値が完了報告にあるか。** **★マイグレ消費 0 が正しい**（§0.3 N-7）。
- [ ] **`internal/service/notation/` ／ `internal/service/preset/` の解決ロジックの diff が 0**（契約 F-5）。
- [ ] **★`recipe_cache` の読み書きの diff が 0**（契約 F-5・§4.5-1）。
- [ ] **`web/src/features/physical-input/` ／ `gamepad/` ／ `keyboard/` の diff が 0**（契約 F-4）。
- [ ] **★`internal/api/auth/` ／ `internal/service/auth/` ／ 認証ミドルウェアの diff が 0**（同 §2.2-5）。
- [ ] **`go.mod` / `go.sum` / `package.json` / ロックファイルの diff が 0**（同 §2.3）。
- [ ] **★`combos.version` / `setups.version` の +1 の仕方を変えていない**（同 §2.2-1）。**★細かくしていないか**（§0.3 N-6）。

## 5. テストの妥当性（ケース数で確認。指示書 §5 ↔ §7 DoD）

- [ ] **§5.1 の 9 項目がすべてある。**
- [ ] **E2E の A（非回帰）と B（後から保存した側が拒否される）が置かれている**（§5.2）。
- [ ] **★E2E B が「見せ方」を見ていない**（**`M22-04` の担当**）。**拒否されることだけを見ているか。**
- [ ] **★サーバの共有状態を書き換えていない**（**D-399 (1)**）。**モックを使う場合、照合パターンの確認と対照実験が置かれているか。**
- [ ] **★完了報告のテスト結果が、コマンド自身の出力（スイート名・pass/fail の件数）で書かれているか**（教訓 **E-125**）。**「exit 0」「全緑」だけを根拠にしていないか。**

## 6. コード品質・規約遵守

- [ ] 曖昧語依存でない（playbook §4.1）。定数化・命名一貫。
- [ ] **★エラーコードがリテラル文字列として散在していない**（`CLAUDE.md` §4＝**列挙的文字列定数はパッケージ定数として定義する**）。**★本サブは 2 つのコードを 1 つに揃えるサブである。定数化されていないなら、次に同じことが起きる。**
- [ ] **JSON タグが camelCase で統一されている**（`CLAUDE.md` §4）。
- [ ] **エラーが wrap されている**（`fmt.Errorf("...: %w", err)`）。**サービス層のメソッドが第一引数に `context.Context` を取る。**
- [ ] **公開 API に godoc コメントが付いている。**
- [ ] `console.log` / `fmt.Println` を本番コードに残していない（`CLAUDE.md` §10）。
- [ ] `bash scripts/check-enum-sync.sh` ／ `bash scripts/check-browser-storage-keys.sh` が回されている（§7.3）。

## 7. 既存挙動の温存（非破壊性）

- [ ] **★利用者から見た挙動が変わっていない**（§0.3 N-9）。**既存の E2E が非回帰であるか**（§5.2-A）。
- [ ] **★版が一致する通常の編集が、いままでどおり通る**（§5.1-2）。
- [ ] **一意制約違反の 409 が、いままでどおりの形で返る**（同 §4.3-6）。

## 8. ドキュメント・進捗ログ

- [ ] 完了報告に **指示書 §7.5 の 10 項目**がすべてある。
- [ ] **★33 経路の判定結果が経路ごとに 1 行の根拠付きであるか**（§7.5-2）。**★「対象なのに上げていない」が無ければ「無かった」と明記されているか。**
- [ ] **★`conflict` を見ていた箇所の全数と更新後の状態があるか**（§7.5-4）。
- [ ] **★`recipe_cache` の非同期の as-built があるか**（§7.5-5）。**★設計卓が `DES-003` へ明記する根拠になる。欠けると反映が書けない。**
- [ ] **★`DES-002` §6.4 / §4.2 と as-built の差が全数列挙されているか**（§7.5-7）。**★設計卓が `CHANGE-114` の反映で使う。**
- [ ] **撤回・変更した定義の残骸が残っていないか。** 指示書 §4.9 の走査結果が完了報告にあるか。**★走査コマンドそのものが残っているか。**
- [ ] **★`conflict` の走査が単語境界で行われているか**（指示書 §4.9-1）。**★`version_conflict` に部分一致するため、素朴な走査では区別できない。**
- [ ] 走査対象に **3 系統すべて**が含まれているか——**本番コード／テスト資産／設計文書・指示書・overview**。
- [ ] **★陽性対照が置かれているか**（`DES-002` §4.2 の「楽観排他の 409 については本節にまだ規定が無い」は必ず当たる）。**「0 件だった」で終わっていないか**（`E-84`）。
- [ ] **★当たったファイルの全文が目視されているか。**
- [ ] **★並列相手との突合が行われているか**（教訓 `E-121`）。**並列相手が居なかったなら「居なかった」と明記されているか。**
- [ ] **★完了報告に「■ 併せて更新が要るもの」の節があるか**（教訓 `E-114`）。**該当が無い場合も「なし」と書かれているか。**
- [ ] **`docs/progress/progress-log.md` へ追記されているか**（`CLAUDE.md` §8）。
- [ ] **★followup の更新候補が設計伝達レポート §4 へ書かれているか**（**製造は `followup-backlog.md` を編集しない**＝**D-382**）。

---

## 9. 重大な問題の判定基準（完了承認を妨げる）

1. **★破壊確認 A / B が行われていない、または赤にならなかった項目を「テストを強くした」だけで済ませている**（§5.1 の 3・4）。**★本サブはテストが成果物のサブである。破壊確認が無ければ何も証明されていない。**
2. **★§1.3 の「対象外」に楽観排他を足している**（指示書 §4.2-2）。**★検証記録を 1 行足すたびに本体編集と衝突する。** **★足した側は緑になるため、テストでは見つからない。**
3. **★ステータスだけを見るテストで済ませている**（同 §4.1-2）。**★409 は一意制約違反でも返るため、突き合わせを外しても緑になりうる。**
4. **★`conflict` を見ている箇所が残っている**（同 §4.3-4）。**★競合が「不明なエラー」になり、画面に何も出ないまま壊れる。**
5. **★`version: 0` を `1` 等の既定値へ差し替えただけである**（同 §4.4-4）。**★「必ず失敗する」が「静かに他人の編集を上書きしうる」に変わる。悪化である。**
6. **★33 経路の判定が行われていない、または根拠が経路ごとに示されていない**（同 §4.2・§7.5-2）。
7. **★「対象なのに版を上げていない」経路を見つけて、止めずに自分で上げている**（同 §9.3-4）。**★上げる経路を増やすのは、他の端末の編集が弾かれる頻度を上げる判断である。**
8. **新しい `version` 列を足している ／ マイグレを消費している**（同 §1.2・§9.3-1）。**★`D-394` は「1 本も足さない」で確定している。**
9. **`recipe_cache` を直している ／ `updated_at` で競合を判定している**（同 §4.5-1・4・契約 F-5）。
10. **`migrations/` ／ `docs/design/` に diff がある。** **または `web/src/features/physical-input/` ほか（契約 F-4）／ `notation` ・ `preset` の解決ロジック（契約 F-5）に diff がある。**
11. **新規依存を追加している**（同 §2.3）。
12. **★`DES-002` §6.4 / §4.2 と as-built の差が完了報告に列挙されていない**（**設計卓が `CHANGE-114` の反映を書けない**）。
13. **★`recipe_cache` の非同期の as-built が完了報告に無い**（**`DES-003` への明記が書けない**）。
14. **`combos.version` / `setups.version` の +1 の仕方を細かくしている**（同 §2.2-1）。**★集約単位を崩している。**

## 10. 軽微な問題の判定基準（持ち越し許容）

- テストの置き場と命名（規約に反しない範囲。指示書 §9.2-1）。
- **エラーメッセージの文面**（**★コードが `version_conflict` で固定されていれば足りる**）。
- ログの文言・粒度。
- 失効した docblock・コメントの残り（**★ただし本サブが触った箇所に限る**）。
- **★「固定する側を採った」判断**（同 §9.2-3）。**採った旨と理由が完了報告に書かれていることで足りる。**

## 11. 質問・確認事項のフォーマット

「指示書 §X.X / `M22-overview` §4.2 / `DES-002` §6.4 に対し実装が Z。意図確認したい」の形で根拠節を併記する。

## 12. レビュー完了の判定

§1〜§8 が全て OK、§9 重大ゼロ（**特に §9-1〜§9-5**）、§0.1 の Plan Mode 実査 8 項目が揃っている。§10 軽微は持ち越し可。

### 12.1 ★本チェックリスト自身の検査（**発行前に設計担当が行う**）

- [x] **本書と対の指示書の間に、節番号の食い違いが無い。**
  → **指示書は詳細仕様が §4.1〜§4.5 まで伸びたため、否定形確認をテンプレの §4.5 から §4.9 へ寄せてある**（指示書 §4.9 冒頭に理由あり）。**本書は否定形確認を一貫して「指示書 §4.9」と書き、`recipe_cache` の節を「指示書 §4.5」と書き分けた。** **あわせて指示書は §6.5（CHANGE の扱い）を §6 と §7 の間に持つ**——本書 §8 から参照している。
- [x] **指示書が禁じていない事項を「重大」として求めている項目が無い。**
  → **§9-10 は凍結領域を `migrations/` ／ `docs/design/` ／ `physical-input` ほか ／ `notation`・`preset` の解決ロジックに限っており、`internal/` 全体も `web/src/` 全体も求めていない**（§0.3 N-11 で明示的に打ち消してある）。
  → **★「成立条件・紐付け・確定反撃系・タグ・プリセット・`recipe_cache` に楽観排他が無いこと」を重大として求めていない**（§0.3 N-1〜N-4）。**逆に足してあることを §9-2 で重大にしている。**
  → **★「409 の見せ方が無いこと」を重大として求めていない**（§0.3 N-8。**`M22-04` の担当**）。
- [x] **達成不能な条件を重大として求めていない。**
  → **§9-1〜§9-8 はいずれも §5.1 の必須テスト（1〜6）と完了報告の記載で検証できる。**
  → **★「利用者から見た挙動が変わっていること」は求めていない**（§0.3 N-9。**本サブは固定するサブである**）。
  → **★「スキーマが十分であること」は求めていない**（§0.3 N-7。**`D-394` で列を足さないと確定している**）。
- [x] **裁定の変更が打ち消されている。**
  → **§0.3 N-12。** **★`M22-overview` の旧記述〔エラーコードの統一は `M22-04`〕を読んだレビュー担当が「スコープ外の先取り」と判定しないよう、明示的に打ち消した**（**D-406**）。

---

*以上、M22-03 レビューチェックリスト v1.0.0。配置 `docs/instructions/reviews/M22-03-review-checklist.md`。指示書 v1.0.0 と対。最重要ゲートは 2 つ——**テストが壊すと赤くなる形になっていること**、および **線引きの「対象外」を穴として塞いでいないこと**。*
