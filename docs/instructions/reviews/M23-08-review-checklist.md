# M23-08 レビューチェックリスト

| 項目 | 内容 |
|------|------|
| 対象指示書 | `docs/instructions/M23-08-delete-server-side-corrections.md` v1.1.0（削除・完全削除のサーバ側の是正） |
| 文書ID | M23-08-REVIEW |
| 対象指示書ID | M23-08 |
| レビューモデル | 高精度レビュー（実モデル名は `docs/human-notes/model-allocation.md` が正本。実使用は開発者判断） |
| バージョン | 1.1.0 |
| 作成者・作成日 | 設計担当 Claude（設計卓／M23 起票期）/ 2026-08-22 |

> 文書ID ⇄ 実パスの正は `docs/handover/docs-map.md`（自動生成）。

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| 1.1.0 | 2026-08-23 | **★§1 / §9 / §0.3 の `N-9` を訂正した**（**D-526**）——**引き継ぎオプションは「見る」ようにするのではなく「消さない」ようにするのが正しい。** **★指示書 §4.5 の機序の取り違えを、本書がそのまま重大判定基準へ写していた。** |
| 1.0.0 | 2026-08-22 | 初版（指示書 v1.1.0 と対）。**最重要ゲート＝「子表の一覧がハードコードされていないこと」と「self-FK が NULL 化であって削除でないこと」。** |

---

## 0. レビュー担当への前置き

### 0.1 レビュー前の準備

1. **★指示書 §1.2 を先に読む**——**本サブの本体は 5 つの DELETE 文ではなく、§4.1-3 のテストである。**
2. **`architecture-patterns` §11 の `P-04` の記述を読む**（明示削除へ移す理由の一次資料）。
3. **完了報告に Plan Mode の実査 8 件（指示書 §3.3）と破壊確認 2 件（§5.2）が揃っていることを先に確認する。**

### 0.2 レビューの基本姿勢

- **★★「5 表が消えているか」だけを見て終わらないこと。** **見るべきは「6 番目の表が増えたときに落ちるか」である。**
- **★本サブは `P-04` を根治しない。** **根治されていないことを指摘しない。**
- **★画面を 1 バイトも触らないサブである。** 触っていたら §9 である。

### 0.3 ★重大でないもの（**誤判定しないこと**）

| # | 誤判定しやすい状態 | 実は仕様である理由 |
|---|---|---|
| N-1 | **`PRAGMA foreign_keys` の流し方も接続プールの設定も変わっていない** | **指示書 §1.5-1 / §4.6。★`P-04` の根治は M23 のスコープ外である**（`M23-overview` §1.3-5） |
| N-2 | **FK 句（`ON DELETE` の有無）がスキーマで変わっていない** | **指示書 §4.2-1 / §4.8。★SQLite は制約の `ALTER` を持たず、表の作り直しになる。コードで解いている** |
| N-3 | **2 度目の論理削除が成功したまま** | **指示書 §4.4-2。★これは契約である**（`DES-002` §4.2・`M23-03` が固定）。**排他を入れると「消したいのに消せない」経路が生まれる。** **直したのは日時の上書きだけである** |
| N-4 | **セットプレイ側の論理削除が 2 度目に「見つからない」を返したまま** | **指示書 §4.4-3。★非対称は意図的に残した。** **揃えるべきなのは実装ではなく性質であり、両者とも「削除日時がずれない」を満たしている** |
| N-5 | **既存の orphan が掃除されていない** | **指示書 §4.6。★データを消す変更であり開発者の判断である**（`CLAUDE.md` §10）。**件数の報告までが本サブ** |
| N-6 | **materialize 生成物が削除されずに残っている** | **指示書 §4.2-2。★NULL 化であって削除ではない。消すと利用者のデータが黙って失われる** |
| N-7 | **`setups` や他のテーブルの CASCADE 依存が残っている** | **指示書 §1.5-2。★射程は `combos` の完全削除だけである** |
| N-8 | **未使用のレシピ解決経路が撤去されていない** | **指示書 §1.5-5。★設計卓が `M23-06` へ差し戻した**（**D-513**） |
| N-9 | **引き継ぎオプションの読み側が、削除済みセットプレイを見ないまま** | **★★2026-08-23 訂正＝D-526。読み側は触らないのが正しい。** **緩めるとゴミ箱のセットプレイがコンボ詳細画面に出て、`M23-02` / `M23-03` の除外を壊す。** **直すのは書く側**〔解除する紐付けを生きた分だけに限る〕 |

### 0.4 報告フォーマット

- 各節「OK / 重大（§9）/ 軽微（§10）/ 質問（§11）」。

---

## 1. 設計・パターンとの照合（最重要）

- [ ] **★★§4.1-3 のテストが、表名をハードコードせずスキーマから列挙している**（指示書 §4.1-3）。**★ハードコードなら §9 である**——**表を足す人はテストの一覧も一緒に忘れる。**
- [ ] **★★列挙が 1 件以上あることを先に主張している**（同 §4.1-3 の末尾）。**★0 件の一覧に対する「すべて空である」は常に真である。**
- [ ] **`HardDelete` が §3.3-7 の実測一覧をすべて明示削除している**（同 §4.1-1）。**★指示書の 5 表ではなく実測の一覧であること。**
- [ ] **削除の順序が子から親である**（同 §4.1-1）。
- [ ] **既存の `combo_setup_results` / `combo_setups` と同じ書き方である**（同 §4.1-2。**★新しい書き方を作っていない**）。
- [ ] **★★self-FK が NULL 化であって削除ではない**（同 §4.2-2）。**★取り違えると利用者のデータが消える。**
- [ ] **self-FK の NULL 化が同一トランザクション内にある**（同 §4.2）。
- [ ] **完全削除の前チェックが Tx 内にある**（同 §4.3）。**`M23-02` がセットプレイ側で採った形と同じであること。**
- [ ] **論理削除の `UPDATE` に `deleted_at IS NULL` が足され、0 行でも成功を返している**（同 §4.4-2）。
- [ ] **★★引き継ぎオプションが解除する紐付けが、生きたセットプレイの分だけに限られている**（同 §4.5。**2026-08-23 訂正＝D-526**）。**★読む側は 1 か所も触っていないこと**——**緩めるとゴミ箱のセットプレイがコンボ詳細画面に出る。** **★箇所数が §3.3-6 の実測と一致していること。** 以下は訂正前の記述： **引き継ぎオプションの母集団に削除済みセットプレイが入っている**（同 §4.5）。

## 2. データ・API 契約・スキーマの不変（非破壊性）

- [ ] **スキーマを変えていない。マイグレを 1 本も足していない**（指示書 §4.8）。
- [ ] **`DELETE /api/combos/{id}` と `DELETE /api/combos/{id}/permanent` のステータス・本文が変わっていない**（同 §2.2）。
- [ ] **`PRAGMA foreign_keys` の流し方・接続プールの設定が変わっていない**（同 §1.5-1）。
- [ ] **セットプレイ側の論理削除・完全削除の挙動が変わっていない**（同 §2.2 / §4.4-3）。
- [ ] **`combo_setups` / `combo_setup_results` の既存の明示削除が書き換えられていない**（同 §1.5-7）。

## 3. 画面を触っていないこと

- [ ] **★★`web/src/` の変更が 0 バイトである**（指示書 §4.7 / §7.1-7）。**★証跡が完了報告に在ること。**
- [ ] **★これが「開発者の手動確認は不要」という判定の根拠である**（`M23-overview` §4.8）。**0 バイトでなければ、判定そのものを差し戻す必要がある**（**D-493** の教訓）。

## 4. テストの妥当性（ケース数で確認。指示書 §5 ↔ §7 DoD）

- [ ] **リポジトリ層・サービス層 9 件が揃っている**（指示書 §5.1）。
- [ ] **★★破壊確認 2 件が実施され、コマンドと出力が完了報告に在る**（同 §5.2）。
- [ ] **★破壊確認 2（列挙を空にする）で赤くなっている**（同 §5.2-2）。**★ここが緑のままなら、テストは何も守っていない。**
- [ ] **§5.1-3（生成物が削除されていないこと）が在る**（**★NULL 化と削除の取り違えを検出する唯一のテストである**）。
- [ ] **§5.1-6（2 度目の論理削除で `deleted_at` が変わらない）が在る。**
- [ ] **§5.1-9（生きたセットプレイの紐付けの挙動が変わっていない）が在る**（★退行の防止）。
- [ ] **§5.1-4 が同時実行で書けなかった場合、その旨と代替の主張の形が報告に在る**（同 §5.1-4）。
- [ ] **テスト結果が件数付きで報告されている**（同 §7.2。**★終了コードだけを根拠にしていない**＝`E-125`）。

## 5. 設計意図との整合（精神の確認）

- [ ] **★「明示削除は忘れられる形である」という代償を承知した上で採られている**（指示書 §1.2）。**実装かコメントか報告に、その認識が現れていること。**
- [ ] **★否定形確認 4 件が完了報告に在る**（同 §4.9）。
- [ ] **★実査 #1 / #2 / #8 の結果が報告に在る**（同 §4.6）。**★「根治しない」と「何が起きているか分からないままにする」は別である。**
- [ ] **★orphan が溜まっていた場合、件数が報告されている**（同 §7.5-2）。**掃除していないこと。**

## 6. コード品質・規約遵守

- [ ] **`CLAUDE.md` §4 の Go 規約。** エラーの wrap（`%w`）。
- [ ] **`fmt.Println` が本番コードに残っていない。**
- [ ] **表名が定数化されているか、スキーマから引かれている**（**★明示削除側はリテラルでもよい。テスト側がハードコードでないことが要件である**）。
- [ ] **`TODO` が `// TODO(<対応予定>): <内容>` 形式である。**

## 7. 既存挙動の温存（非破壊性）

- [ ] **既存の Go テストが全て緑である**（件数付き）。
- [ ] **`M23-01` / `M23-02` / `M23-03` / `M23-04` / `M23-05` が足したテストが緑のままである**（指示書 §5.3）。**★とくに `M23-03` が固定した削除・復元の契約テスト。**
- [ ] **`pnpm test` / `make e2e` が緑である。**

## 8. ドキュメント・進捗ログ

- [ ] **`docs/progress/progress-log.md` へ 1 行追記されている**（`CLAUDE.md` §8）。
- [ ] **★レビュー結果を参照する記述が、レビュー後に埋められている**（指示書 §7.4。**`M23-04` 教訓 3**）。
- [ ] **完了報告に指示書 §7.5 の 7 点が揃っている。**
- [ ] **★設計書本体（`DES-002` / `DES-003`）が編集されていない**（反映は設計卓が `CHANGE-126` で行う）。
- [ ] **`followup-backlog.md` へ書いた場合、§J だけを触っている**（**D-382**）。

---

## 9. 重大な問題の判定基準（完了承認を妨げる）

- **★★§4.1-3 のテストが表名をハードコードした一覧で書かれている**（指示書 §4.1-3）。**これが最重要ゲートである。** **★本サブの目的そのものを果たさない。**
- **★★列挙が 1 件以上あることの主張が無い**（同）。**走査が壊れたときに緑で通る。**
- **★★self-FK の子（materialize 生成物）が削除されている**（同 §4.2-2）。**★利用者のデータの消失である。**
- **`HardDelete` の明示削除が §3.3-7 の実測一覧を網羅していない。**
- **完全削除の前チェックが Tx の外のまま**（同 §4.3）。
- **論理削除が 2 度目に `404` を返すようになっている**（同 §4.4-2。**★契約の破壊である**）。
- **論理削除が 2 度目に `deleted_at` を上書きしたまま**（同 §4.4-2）。
- **★引き継ぎオプションが削除済みセットプレイの紐付けを消してしまう**（同 §4.5。**2026-08-23 訂正＝D-526**）。 以下は訂正前の記述： **引き継ぎオプションが削除済みセットプレイを見ないまま**（同 §4.5）。**★実装は「見る」ようにはなっていない。「消さない」ようになっている。**
- **★破壊確認 2 件が実施されていない、または出力が報告に無い**（同 §5.2）。
- **★`web/src/` に変更がある**（同 §4.7）。**★手動確認の判定を差し戻す必要が生じる。**
- **スキーマ変更・マイグレの追加・FK 句の変更がある**（同 §4.8）。
- **`PRAGMA` の流し方・接続プールを変えている**（同 §1.5-1）。
- **既存の orphan を掃除している**（同 §4.6。**★データを消す変更である**）。
- **セットプレイ側の論理削除の挙動を変えている**（同 §4.4-3）。
- **スコープ外 7 件（同 §1.5）のいずれかへ踏み込んでいる。**
- **指示書 §7.5 の 7 件のいずれかが完了報告に無い。**

## 10. 軽微な問題の判定基準（持ち越し許容）

- 明示削除を書く関数の置き場・粒度（1 本にまとめるか表ごとか）。
- §4.1-3 のテストで子表を列挙する方法の細部。
- 前チェックを Tx 内へ移すときの関数の切り方。
- テストのデータ組み立ての作法。
- 表名のリテラルと定数の使い分け（**★テスト側がハードコードでなければ足りる**）。

## 11. 質問・確認事項のフォーマット

- 「指示書 §X.X / `DES-00X` §X / `M23-RESEARCH-01` 報告 §X に対し実装が Z。意図確認したい」の形で根拠節併記。

## 12. レビュー完了の判定

- §1〜§8 が全て OK、§9 重大ゼロ（**特に「テストがハードコードでないこと」「生成物が消えていないこと」「破壊確認 2 件」「`web/src/` が 0 バイト」**）、§0.1 の Plan Mode 実査 8 件が揃っている。§10 軽微は持ち越し可。

### 12.1 ★本チェックリスト自身の検査（**発行前に設計担当が行う**）

- [ ] **本書と対の指示書の間に、節番号の食い違いが無い。**
  → 本書が参照する指示書の節は §1.2 / §1.5 / §2.2 / §3.3 / §4.1 / §4.2 / §4.3 / §4.4 / §4.5 / §4.6 / §4.7 / §4.8 / §4.9 / §5 / §7 であり、**指示書側に実在する**。
- [ ] **指示書が禁じていない事項を「重大」として求めている項目が無い。**
  → §9 の各項目は指示書 §1.5 / §2.2 / §4 の禁止・固定と 1 対 1 で対応する。
- [ ] **達成不能な条件を重大として求めていない。**
  → **「`P-04` が根治されていること」を求めていない**（§0.3 N-1。**M23 のスコープ外**）。
  → **「FK 句がスキーマで直っていること」を求めていない**（§0.3 N-2。**SQLite は制約の `ALTER` を持たない**）。
  → **「2 度目の論理削除が `404` を返すこと」を求めていない**（§0.3 N-3。**★契約に反する**）。
  → **「セットプレイ側と実装が揃っていること」を求めていない**（§0.3 N-4。**揃えるべきは性質である**）。
  → **「orphan が掃除されていること」を求めていない**（§0.3 N-5。**開発者の判断**）。

---

*以上、M23-08 レビューチェックリスト v1.1.0。配置 `docs/instructions/reviews/M23-08-review-checklist.md`。指示書 v1.1.0 と対。「テストがハードコードでないこと」と「生成物が消えていないこと」を最重要ゲートとする。*
