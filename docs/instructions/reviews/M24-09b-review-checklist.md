# M24-09b レビューチェックリスト

| 項目 | 内容 |
|------|------|
| 対象指示書 | `docs/instructions/M24-09b-test-assets-and-residual-refactor.md` **v1.1.0**（テスト資産と残リファクタ） |
| 文書ID | M24-09b-REVIEW |
| 対象指示書ID | M24-09b |
| レビューモデル | **高精度レビュー**（実モデル名は `docs/human-notes/model-allocation.md` が正本。実使用は開発者判断） |
| バージョン | 1.1.0 |
| 作成者・作成日 | 設計担当 Claude（設計卓・M24 期）/ 2026-08-25 |

> 文書ID ⇄ 実パスの正は `docs/handover/docs-map.md`（自動生成）。

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| **1.1.0** | **2026-08-25** | **指示書 v1.1.0 と対。** **★§3 へ「E2E の実測がポート衝突で汚染されていないか」を追加**（**`D-548`**）——**E2E スタックは専用ポート 47390 / 5273 を占有し、`M24-01` とそれを共有する。** **★衝突による失敗が `CO-025` の実測に混ざっていたら「重大」**〔誤った結論が恒久対策の要否をそのまま誤らせる〕。 **★§5 へ「書き換えの方式を差し替え可能にしたか／できないと報告したか」を追加**（指示書 §4.4）。 |
| 1.0.0 | 2026-08-25 | 初版（指示書 v1.0.0 と対）。 |

---

## 0. レビュー担当への前置き

### 0.1 レビュー前の準備

- 指示書 §3.3（実査 9 件）と §11（確認事項 2 件）を先に読むこと。
- **★本サブは 5 項目のうち 2 項目が「調べて報告するだけ」で正しく終わる。** **「実装していない」を欠陥と判定しないこと**（§0.3）。

### 0.2 レビューの基本姿勢

**本サブの入力は `ledger` と `followup-backlog` の記録であり、「観測の記録」であって「現在も成立する課題」の保証ではない**（指示書 §1.1）。**⇒ 「followup にそう書いてあるから正しい」を根拠として受け入れないこと。**

**★起票時点で既に 1 項目が解消していた**（`check-md-emphasis.sh` の devContainer 依存＝`IMPROVE-01` で解決済み。指示書 §1.1.1）。**同じことが他の項目で起きていないかを見ること。**

### 0.3 ★重大でないもの（**誤判定しないこと**）

- **`CO-025` が実装されていないこと**——**実測で「3 回とも 0 flaky」または「落ちる spec が毎回違う」なら、実装しないのが正しい**（指示書 §4.1）。**実測が報告されているかだけを見る。**
- **`CO-007` が「判定だけ」で終わっていること**——**それが仕様である**（指示書 §4.2）。**★マイグレーションの本文が編集されていたら「重大」。**
- **`CO-006`（inline INSERT）が後回しにされていること**——**低優先であり、理由つきで報告されていれば正しい**（指示書 §4.3）。
- **`CO-012` で「寄せなかった」と判断していること**——**2 つが似て見えるだけで別物なら、寄せないのが正しい**（指示書 §4.4。先例＝`M23-03` の `#6`）。**判定の根拠が書かれているかを見る。**
- **`followup-backlog.md` が変更されていないこと**——**製造は §J 以外を触らない**（`D-382`）。**触られていたら「高」。**

### 0.4 報告フォーマット

`重大 / 高 / 中 / 低` の 4 段。**各指摘に「指示書の該当節」または「教訓 ID」を併記すること。**

---

## 1. 設計・パターンとの照合（最重要）

- [ ] **★★適用済みマイグレーションの本文が 1 バイトも変わっていないか**（指示書 §2.2 / §4.2）。**★変わっていたら「重大」**——**適用済みマイグレーションは歴史的記録であり、書き換えると「当時どういう前提で書かれたか」が失われる**（`D-535`）。
- [ ] **スキーマ変更 0 件・マイグレ連番の消費 0 本であること。** **★消費していたら「重大」**（設計卓の払い出しを経ていない）。
- [ ] **`CalcRecipeHash` の統合先が `internal/service/notation/` / `internal/service/preset/` に入っていないか**（契約 F-1。指示書 §2.2）。**入っていたら「重大」。**
- [ ] **`canonicalModifiersJSON` と `emptyRecipeHash` も同じ手番で寄せられているか**（指示書 §4.5 手順 3）。**★片方だけだと、ハッシュ本体は 1 本なのに正規化が 3 本という、いまより分かりにくい状態になる。**
- [ ] **config のアトミック書込の性質が保たれているか**（指示書 §4.4）。**★書込中に落ちても設定ファイルが壊れない、が守りたい不変条件である。**
- [ ] **`check-md-emphasis.sh` の依存判定が緩められていないか**（指示書 §2.2）。**★「依存が無くても通る」形にしたら「重大」**——**検査が存在しないのと同じになる。**

## 2. データ・API 契約・スキーマの不変（非破壊性）

- [ ] **`DES-002` §4.2 の経路表に載る API の挙動が変わっていないか**（指示書 §9.4-1）。**変わっていたら止めて報告されているか。**
- [ ] **`CalcRecipeHash` の統合により `VAL-C02` の判定結果が変わっていないか**（指示書 §9.4-3）。**★これは「重複と判定される組が変わる」ことであり契約の変更である。** **変わっていて報告が無ければ「重大」。**
- [ ] **`/trash` 系と M23 の削除・復元の契約に触れていないか。**
- [ ] **`web/` の本番コンポーネントの挙動が変わっていないか**（指示書 §1.3）。**★テスト資産は触ってよいが、守られている挙動は変えない。**

## 3. 調査 2 件の質（`CO-025` / `CO-007`）

- [ ] **`CO-025`**: **`make e2e` を 3 回 ＋ `--workers=1` を 1 回回した実測が、回数と spec 名つきで報告されているか**（指示書 §3.3-2 / §4.1-2）。
- [ ] **★★`CO-025` の実測がポート衝突で汚染されていないか**（指示書 §2.4-1）。**`M24-01` と並列に投入されていた場合、`make e2e` を回す前にポート 47390 / 5273 が空いていたことが確かめられているか。** **★衝突による失敗を「flaky」として記録していたら「重大」**——**汚染された実測は、実測が無いより悪い。「3 spec に固定して flaky」という誤った結論が、恒久対策の要否の判断をそのまま誤らせる**（教訓 `E-230`）。
- [ ] **`CO-025`**: **「3 spec に固定」「毎回違う」「0 flaky」のどれだったかが明示され、それに対応した処置**（実装する／しない）**が採られているか**（指示書 §4.1 の表）。
- [ ] **★`CO-025`**: **タイムアウト延長を採った場合、「何を待っているのか」「その待ちが原理的に何 ms かかりうるのか」が書かれているか**（指示書 §4.1-1）。**書けていないなら「高」**——**伸ばすと「遅くなったこと」が以後見えなくなる。**
- [ ] **`CO-007`**: **`000012` の up / down の逐語が完了報告に在るか**（指示書 §4.2-1）。
- [ ] **`CO-007`**: **「down で消し残る行が構造上ありうるか」の判定と、その根拠が書かれているか**（同 §4.2-2）。
- [ ] **★`CO-007`**: **「実データを見ないと判定できない」部分が、そう明記されているか**（同 §4.2-3）。**★「実測で落ちなかった」を「落ちる経路が無い」と書いていたら「高」**（教訓 `E-237`）。
- [ ] **★`CO-007`**: **rollback 方針まで決めていないか**（指示書 §4.2。**判定が出てから設計卓が決める**）。決めていたら「中」。

## 4. テストの妥当性（ケース数で確認。指示書 §5 ↔ §7 DoD）

- [ ] **§5.1 の 1〜4 が実在し、ケース数が完了報告に書かれているか。**
- [ ] **★§5.1-1 / §5.1-2**: **統合前の 3 実装が返していた値と、統合後の 1 本が返す値が一致することを、複数の入力の組**（空レシピ ／ modifier あり ／ modifier の順序違い）**で示しているか。**
- [ ] **★指示書 §3.3-5 の実査で「3 本の出力が現在一致しているか」が測られているか**（指示書 §4.5 手順 1）。**★一致していないのに統合していたら「重大」**——**どれが正しいかは設計卓の判断である。**
- [ ] **★完了報告のテスト結果が、コマンド自身の出力（スイート名・pass/fail の件数）で書かれているか**（教訓 **E-125**）。**「全緑」「exit 0」だけを根拠にしていないか。**
- [ ] **★`CO-006` で寄せた箇所が、寄せる前と同じかそれ以上の主張を持つか**（指示書 §4.3）。**★ヘルパが既定値を埋めることで、その既定値に依存した主張が「たまたま通る」形になっていないか**（教訓 `E-218` の変種）。
- [ ] **★★結果表示を主張するテストが、実アプリと同じ状態遷移で描画されているか**（教訓 **E-217** / **E-223**）。**本サブは E2E spec を触るため、同じ穴を作りうる。**
- [ ] **★E2E の判定キーが spec ごとに分かれているか**（教訓 **E-232**）。

### 4.5 破壊確認（**指示書 §5.3・3 件**）

- [ ] **3 件すべてが実施され、コマンドと出力が完了報告に在るか。**
- [ ] **★★破壊確認 3（統合後の 1 本を壊す）が「どの経路が赤くなったか」を数えているか**（指示書 §5.3-3）。**★「テストが赤くなった」だけでは足りない**——**赤くならない経路があるなら、その経路は統合されていない。**
- [ ] **★狙ったテストだけが、狙ったアサーションで赤くなったことまで確認されているか**（教訓 **E-230**）。
- [ ] **★赤くならなかったものについて、壊し方ではなくテストを疑っているか**（教訓 **E-218**）。

## 5. 設計意図との整合（精神の確認）

- [ ] **★「実装しなかった」項目について、理由が書かれているか**（指示書 §9.3。**黙って落としていたら「高」**）。
- [ ] **`CO-012` で「寄せる／寄せない」の判定の根拠が書かれているか**（指示書 §4.4。**★「重複しているから寄せる」と決めつけていないか**＝教訓 `E-205` / `E-255`）。
- [ ] **★`CO-012` の統合で「書き換えの方式」を差し替え可能な形にしたか、または「できない」と報告しているか**（指示書 §4.4）。**★後でコメント保全（`e2e-suite-rewrites-config-toml-comments`）を扱うとき、統合した 1 本を作り直すことになるためである。** **★使われるか分からない抽象を先に作っていたら「中」**——**それ自体が負債である。**
- [ ] **★寄せた結果、層をまたいでいないか**（`internal/api/config/` と `internal/service/config/` は別の層）。**またぐ必要があると判明したら止めて報告されているか。**

## 6. コード品質・規約遵守

- [ ] 曖昧語依存でない（playbook §4.1）。定数化・命名一貫。
- [ ] `console.log` / `fmt.Println` が本番コードに残っていない（CLAUDE.md §10）。不要な `eslint-disable` / `nolint` が無い。
- [ ] **★「N 件」「N か所」を書いたコメント・godoc・報告文が、実数と一致しているか**（教訓 **E-220** / **E-236**）。
  - **(a) 既存の件数コメントを機械的に +1 していないか。**
  - **(b) 「〜だけである」「全 N 件」と断定した文について、異なるキーワードの走査がもう 1 本回されているか。**
- [ ] **★統合後の `CalcRecipeHash` の godoc が「誰が使うか」を正しく書いているか**（教訓 **E-224**。**★呼び出し元の列挙は、増えたときにしか壊れない形の記述である**）。
- [ ] **★「新規ファイルを作る」前に同名の存在を確かめているか**（教訓 **E-225**。指示書 §3.3-3）。**★変更統計に、新規ファイルのつもりのものの deletions が無いか**——**deletions がある新規ファイルは矛盾している。**

## 7. 既存挙動の温存（非破壊性）

- [ ] **§3.3-8 で数えた既存テストの全数が、Go / Vitest / `web/e2e/` の 3 系統で別々に報告されているか。**
- [ ] **§3.3-7 で記録した着手前の `go test` / `make e2e` の状態と、完了時の状態が並べて書かれているか。**
- [ ] **★`M24-01` と `web/e2e/` で交差していないか**（指示書 §3.3-1 / §2.4）。**共通 fixture を触っているなら報告されているか。**

## 8. ドキュメント・進捗ログ

- [ ] **`docs/progress/progress-log.md` へ索引行が追記されているか**（CLAUDE.md §8）。
- [ ] **★索引行がレビューの後に埋められているか**（教訓 **E-208**）。
- [ ] **★完了報告が「済み」と書いた成果物が、すべて実在するか**（教訓 **E-189** / **E-193** / **E-242**）。
- [ ] **★★`followup-backlog.md` が変更されていないか**（指示書 §2.1-6。`D-382`）。**畳むべき行が設計伝達レポート §4 へ候補として並んでいるか。**
- [ ] **§4.9 の否定形確認**——**走査コマンドの全文が完了報告に在るか。「検出した箇所」と「手当てした箇所」が別の表になっているか**（教訓 **E-193**）。**★当たったファイルの全文を見て、同じファイルの下部だけが旧のまま残っていないか**（教訓 **E-235**）。
- [ ] **★設計伝達レポート §1 に、`CalcRecipeHash` の統合先と呼び元の全数が本文として書かれているか**（指示書 §7.5）。**★参照だけでは、次に同じ面を触るサブが再び数える羽目になる。**

## 9. 重大な問題の判定基準（完了承認を妨げる）

1. **適用済みマイグレーションの本文を編集した。**
2. **スキーマ変更またはマイグレ連番の消費が発生した。**
3. **`CalcRecipeHash` の統合先が契約 F-1 のパッケージに入った。**
4. **3 実装の出力が一致していないのに、報告せず統合した。**
5. **`VAL-C02` の判定結果が変わったのに報告が無い。**
6. **`check-md-emphasis.sh` の依存判定を「依存が無くても通る」形へ緩めた。**
7. **§3.3 の実査を行わず、指示書の現状認識をそのまま前提として実装した。**
8. **破壊確認が未実施、または破壊確認 3 で「どの経路が赤くなったか」を数えていない。**

## 10. 軽微な問題の判定基準（持ち越し許容）

- inline INSERT ヘルパの命名・置き場所（指示書 §9.2 で製造へ委ねた範囲）。
- 待機のポーリング化の実装形。
- `CO-006` が時間の都合で後回しになったこと（**理由つきで報告されていれば**）。

## 11. 質問・確認事項のフォーマット

**「何を / なぜ要るか / 暫定案」の 3 点で書くこと。** **★指示書 §11 の 2 件と重複する質問は立てない。**

## 12. レビュー完了の判定

**重大 0 件 ＋ 高が全件処理済み**（採用または不採用の理由つき）で完了とする。

### 12.1 ★本チェックリスト自身の検査（**発行前に設計担当が行う**）

- [x] 指示書の節番号と本書の参照が一致している（§1.1.1 / §2.1〜§2.4 / §3.3 / §4.1〜§4.5 / §4.9 / §5.1〜§5.3 / §9.4 / §11）。
- [x] 指示書が「作らない」と書いたものを、本書が要求していない（§0.3 で明示的に打ち消してある）。
- [x] **★調査 2 件について「実装しなかったこと」を欠陥と判定しない形にしてある**（§0.3）。
- [x] **★`followup-backlog` を製造が触らない規則（`D-382`）を、§0.3 と §8 の両方に置いてある。**

---

*以上、M24-09b レビューチェックリスト v1.1.0。*
