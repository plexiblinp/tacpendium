# M21-03 レビューチェックリスト

| 項目 | 内容 |
|------|------|
| 文書ID | M21-03-REVIEW-CHECKLIST |
| バージョン | 1.2.0 |
| 対象 | `docs/instructions/M21-03-recipe-input-and-readout.md` v1.2.0 |
| 対の CHANGE | `docs/change-notes/CHANGE-111-notification.md` |
| 作成者・作成日 | 設計卓（Claude Code・武装中）/ 2026-08-13 |

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| **1.2.0** | **2026-08-13** | **指示書 v1.2.0 と対**（**D-352**）。**★§1.3 の「2 ボタン同時が OD 技へ写っている」を撤回した**——**指示書 §4.2-2 が §4.1-2 および契約 F-3 と両立していなかった。** **⇒ 同強度の P＋K は既存のシステム技へ、OD は本サブでは物理から出さない形へ差し替え。** **★旧項目のまま判定すると、正しい実装が「未実装」と判定される**（**D-323** の同型）。**§8 へ差し戻し事由 9 を追加。** |
| **1.1.0** | **2026-08-13** | **指示書 v1.1.0 と対**（**D-350**＝セットプレイ入力面も対象へ）。**§1.7 を新設＝2 面の部品化。** **§8 へ差し戻し事由 8 を追加。** **§9 へ「判定してはいけないこと」を 1 件追加。** |
| 1.0.0 | 2026-08-13 | 初版（指示書 v1.0.0 と対）。 |

---

## 0. 読む順（★ここから始める）

- **指示書 `M21-03` v1.2.0 を全文読む**（とくに **§4.1 の死守契約** ／ **§4.6 の告知** ／ **§4.9 の部品化** ／ **§9.4 の停止条件 4 件**）。
- **`CHANGE-111` 通知書を読む。**
- **`DES-005` §6.4.2 を読む**——**本サブが乗る土台であり、告知の要件はここに書かれている。**
- **★完了報告の §3.3 実査結果を、実装より先に読む。** **とくに §3.3-2**（**仮想 UI の解決経路の実体**）——**ここで「合流できたか、別経路を書いたか」が決まる。**

> **★本サブは新しい判断をほとんど持たない。** **判定は M21-02 が、解決は既存の仮想 UI が持っている。** **⇒ レビューの主眼は「新しく書いてしまっていないか」である。**

---

## 1. 設計意図との一致

### 1.1 ★出口が `move_code` である（§4.1・**最重要**）

- [ ] **モーション解析をしていない**——**方向の連続入力から技を決める箇所が 0 件**（**M21-06 の領分**）
- [ ] **解決規則を新設していない**——**段階1 の構造引き ／ 段階2 の解決表 ／ `rush_<code>` ／ 9 方向 → 3 ゾーンの縮約は、いずれも既存の実装を呼んでいる**
- [ ] **解決できない入力をステップにしていない**（**近い技を推測で選んでいない**）
- [ ] **★「同じ入力を仮想 UI で行った場合と `move_code` が一致する」テストがある**（§5 (a)）

> **★ここが重大判定の 1 本目である。** **物理入力から技を決める関数が新しく生えていたら、契約 F-3 が壊れている。** **動いていても差し戻す。**

### 1.2 告知（§4.6・**重大判定の 2 本目**）

- [ ] **3 点すべてが伝わる**——**まとまりうること ／ ゆっくり正確に ／ 不具合ではなく意図的な設定であること**
- [ ] **★3 点目が落ちていない**（**1・2 だけだと「使いにくいツール」に読める**）
- [ ] **判定窓の実値（`90` 等）を利用者向け文言へ写していない**（値は実装の 1 か所が正＝`E-76`）
- [ ] **告知の存在をテストで固定している**（§5 (f)。**文言の完全一致ではなく存在**）

> **★告知が無ければ差し戻す。** **これは「あったほうがよい」ではない**——**実機で「意図せずまとまる」が確認されており**（2026-08-13）、**知らない利用者は「壊れている」と判断する**（**D-347**）。

### 1.3 ステップの確定と削除（§4.2・**★v1.2.0 で全面差し替え**）

> **★旧項目「2 ボタン同時が OD 技へ写っている」は撤回された**（**D-352**）。**旧項目のまま判定しないこと**——**正しい実装が「未実装」と判定される**（**D-323** の同型）。

- [ ] **1 ボタンが通常の強度へ写っている**
- [ ] **★同じ強度の P＋K が既存のシステム技へ写っている**（強P＋強K → `drive_impact` ／ 中P＋中K → `drive_parry` ／ 弱P＋弱K → 投げ）
- [ ] **★その写像が「既存の入口を呼ぶ」形になっている**（§4.2-2′。**共通技の `move_code` への写像を自前で書いていない**）
- [ ] **★OD 技を物理から出していない**（§4.2-2″。**`_od` へ写す規則を書いていない**）——**書いてあったら差し戻す**
- [ ] **PP・KK・強度をまたぐ組み合わせ・3 ボタン以上が「解決できない入力」へ倒れている**（**推測で近い技を選んでいない**）
- [ ] **投げの向きを `throw_back` にした場合、その旨が報告にある**（§9.2-7。**採否そのものは指摘しない**）
- [ ] **`StepCandidate.direction` をそのまま使い、方向を再判定していない**（**M21-02 の as-built を動かしていない**）
- [ ] **物理と仮想の入力が混在でき、順序が入力順どおりである**（§5 (c)）

### 1.4 読取表示と点灯（§4.3・§4.4）

- [ ] **押されている入力・確定したステップ・解決できなかった入力の 3 つが出る**（**★解決できなかった入力を黙って捨てていない**）
- [ ] **点灯の状態源が論理ボタン層である**（**確定したステップではない**。§5 (d)）
- [ ] **test-id が `recipe-<領域>-<識別子>` に従っている**（`DES-005` §6.8）

### 1.5 §6.6 の回収（§4.5）

- [ ] **TC としてまとめるデータ構造を作っていない**（**レシピは 1 ステップ 1 行**）
- [ ] **回収した §6.6-2 / §6.6-5 の決着内容が完了報告にある**（**`CHANGE-111` の反映材料**）

### 1.6 最小可視化の撤去（§4.7）

- [ ] **M21-02 の最小可視化が本番画面から消えている**（**または残した理由が完了報告にある**）
- [ ] **読取表示と最小可視化が二重に出ていない**

### 1.7 ★2 面の部品化（§4.9・**重大判定の 3 本目**・**D-350**）

- [ ] **接続層が headless な部品になっている**（描画を持たない）
- [ ] **★`move_code` の解決が部品の中で 1 回だけ行われている**——**面ごとに解決を書いていない**
- [ ] **部品の出口が面に依存しない中立な形である**（面ごとのステップ型への写し替えは呼び出し元が行う）
- [ ] **点灯の状態も同じ部品から供給されている**（面ごとに論理ボタン層を購読していない）
- [ ] **★告知の文言が 2 か所へ書き写されていない**（`E-76`。共通化されているか 1 か所を参照している）
- [ ] **★「2 面で同じ入力が同じ `move_code` になる」テストがある**（§5 (h)）
- [ ] **セットプレイ側で落としたもの**（`modifiers` を持たない等）**が読取表示に出ている**（**黙って落としていない**）

> **★2 本書いてあったら差し戻す。** **そのときは動く。** **壊れるのは段階2 の解決表やラッシュ版の規則が更新されたときで、片方だけ追随して他方が「動いているが古い規則で解決している」状態になる。** **エラーにならない。**
>
> **★差が構造的で 1 つの部品に収まらなかった場合は、停止条件 4 が正しい対応である**（**止めて報告し、セットプレイ側を別サブへ送る**）。**「差が大きかったので 2 本書きました」は採らない。**

---

## 2. 契約・凍結インターフェース

- [ ] **`internal/service/notation/` ／ `internal/service/preset/` の diff 0**（**契約 F-1。`M20-04` と並走中であり、とくに厳格**）
- [ ] **`internal/moveindex/` ／ `move_commands` は読むだけ**（契約 F-3）
- [ ] **`internal/service/punishfinder/` ／ `internal/service/setplay/` の diff 0**
- [ ] **`migrations/` の diff 0**（**本サブは 0 本の見込み。消費したなら実査の旨と連番が完了報告にある**）
- [ ] **`scripts/` の diff 0**（**D-335**。ベースライン定数を触らない）
- [ ] **`docs/design/` の diff 0**（**製造は設計書を編集しない**）
- [ ] **M21-01・M21-02 の既存スイートが全緑**（非回帰）
- [ ] **仮想コントローラの既存テストが全緑**

---

## 3. 実装品質

- [ ] `pnpm lint` exit 0
- [ ] `console.log` の本番混入 0 件
- [ ] `any` ／ `@ts-ignore` ／ `eslint-disable` の追加 0 件
- [ ] **判定ロジックを純粋関数のまま保っている**（**M21-02 の形を崩していない**）
- [ ] **副作用層のタイマーが二重に発火していない**（§3.3-3）

---

## 4. テスト

- [ ] **§5 の (a)〜(h) が揃っている**
- [ ] **E2E を「書けなかった」と明記している**（**実機コントローラと user gesture を要するため**。**「書かなかった」と書いていない**＝`E-84`）
- [ ] **`make e2e` の結果が報告されている**（**回せなかった場合はその旨**）

---

## 5. ドキュメント

- [ ] **`docs/progress/progress-log.md` へ索引行を追記した**（`CLAUDE.md` §8）
- [ ] **★`check-progress-log-index.sh` の緑を「追記した証明」として引いていない**（**同スクリプトは「ID が本文のどこかに現れれば緑になる」と自ら明記している**＝M20-03 の教訓。**実際の追記行を目視すること**）
- [ ] **ブラウザストレージの新規キーを追加したなら台帳に載っている**（`web/CLAUDE.md` §1）

---

## 6. 否定形確認（§4.10）

- [ ] **走査の結果が「見つかった件数」と「検査した範囲」の両方で報告されている**（`E-84`）
- [ ] **最後にコードを触った時点で回している**（**サブの完了時点ではない**。実機確認 → 追補 の運用では初回の 0 件が失効する）
- [ ] **★走査キーワードの単純な出現を違反と数えていない**（**`move_code` の中の数字表記は正常である**。**残骸は「本サブが新しく書いた解決規則」だけ**）

---

## 7. 停止規律

- [ ] **再レビュー往復が 2 回を超えていない**（`CLAUDE.md` §9）
- [ ] **上限に達したら、未解消項目を `followup-backlog` §J へ必須 5 フィールドで記録して停止している**
- [ ] `bash scripts/check-stop-discipline.sh`

---

## 8. 差し戻し事由（重大）

| # | 事由 |
|---|------|
| **1** | **モーション解析を書いている**——**方向の連続入力から技を決める箇所がある**（契約 F-3。**動いていても差し戻す**） |
| **2** | **解決規則を新設している**——**段階1 / 段階2 / ラッシュ / 縮約のいずれかを自前で書き直している**（**二重管理になる**） |
| **3** | **告知が無い**（§4.6。**3 点目が落ちている場合も含む**） |
| **4** | **解決できない入力を黙って捨てている**（**画面にも報告にも出ない**） |
| **5** | **M21-02 の判定ロジックを変更している**（§2.2-7） |
| **6** | **契約 F-1 / F-3 の diff が 0 でない**（`M20-04` と並走中） |
| **7** | **`migrations/` を自採番で消費している**（**D-293**。**実査して一致しないなら再請求が正しい**） |
| **★8** | **2 面へ接続を別々に書いている**（§4.9。**`move_code` を決める箇所が 2 つある**）。**動いていても差し戻す** |
| **★9** | **OD 技を物理から出す規則を書いている**（§4.2-2″。**family を選ぶ写像は新設規則であり契約 F-3 に抵触する**＝**D-352**）。**動いていても差し戻す** |

---

## 9. ★このレビューで判定してはいけないこと

- **判定窓の値そのもの**——**M21-02 が実測から導いて決着済みである**（`DES-005` §6.4.2）。**本サブで狭める・広げるのはスコープ外。**
- **告知の文言の良し悪し**——**3 点が伝わるかだけを見る。** 表現は実装者と開発者の領分。
- **読取表示の見た目**——**§9.2 で「覆ってよい」とされている。** **設計卓の見込み（3 区画）と違っても、理由が書かれていれば指摘にしない。**
- **★OD 技が物理から出せないこと自体**——**設計どおりである**（**D-352**）。**「OD が入力できないのは不便だ」は本サブへの指摘にしない。** **既存の必殺技パネルで入れられ、物理からの確定は `M21-06` が持つ。**
- **投げの向きの扱い**（`throw_forward` 固定か方向で分けるか）——**§9.2-7 で「覆ってよい」とされている。** **報告にあるかだけを見る。**
- **セットプレイ側で落ちる情報があること自体**——**面の側の制約であり、部品の問題ではない**（§4.9 の注記）。**読取表示に出ているかだけを見る。**
- **「素早く入力するとまとまる」こと自体**——**設計どおりである**（`DES-005` §6.4.2 の「誤りの非対称性」）。**窓を狭める提案をしない。**

---

*以上、M21-03 レビューチェックリスト。*
