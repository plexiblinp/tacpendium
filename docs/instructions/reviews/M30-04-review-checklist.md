# M30-04 レビューチェックリスト（ファミリー行の派生変種を末尾へ回す）

| 項目 | 内容 |
|------|------|
| 文書ID | M30-04-REVIEW-CHECKLIST |
| バージョン | **1.0.0** |
| 作成日 | 2026-09-10 |
| 対象指示書 | `docs/instructions/M30-04-derived-variants-to-row-end.md` **v1.1.0** |
| 発行 | 設計卓（`D-816`。**★事後発行**——同サブは 2026-09-10 に投入済みであった） |
| 使い方 | `/review_plan` の入力。**★本書は指示書の完了条件を写したものではない。⇒ 「緑でも壊れているもの」を探すための束である** |

---

## 0. ★★レビュー担当への前置き

**★★費用の本体は並べ替えではない。⇒ `is_derived` をリスト DTO まで届けることである。★そこが片側だけだと `divergence_test.go` が赤くなる。**

**★★★指示書 v1.1.0 の実測値は `M30-03` 着地前のものであり古い。⇒ 実物で数え直しているかを見ること。★ファミリー行の総数は 342 → 372 になっている。**

---

## 1. 束 A — ★★★並べ替えたことの証拠

| # | 見ること | 落ちる形 |
|---|---|---|
| **★★★A-1** | **陽性対照＝入れる前は `is_derived = true` が行の先頭側に居たことを測ってから入れているか** | **★★★「末尾に居る」だけを測ると、元から末尾だったキャラで緑になる。⇒ 何も証明していない** |
| **★★★A-2** | **キャラ別の件数が、入れる前と後の両方で示されているか。★総数が変わっていないか** | **★消えていたら「削る」を実装している。⇒ 開発者は「末尾へ出す」を選んだ**（`D-801`） |
| **★★A-3** | **実測値を `M30-03` 着地後の実物から起こしているか** | **★★指示書 v1.1.0 の数値は古い**（342 → 276 の内訳は `M30-03` 前）**。⇒ 写していたら数え直していない** |
| **★★A-4** | **`is_derived = false` どうし、`is_derived = true` どうしの相対順が変わっていないか** | **★★並びは DB 挿入順である。⇒ 群の中で順序が変わると、利用者から見て別の理由で並びが動いたように見える** |

---

## 2. 束 B — ★★★DTO 拡張の同期

| # | 見ること | 落ちる形 |
|---|---|---|
| **★★★B-1** | **`model.Move` ＋ `MoveListItem` ＋ SELECT 列 ＋ divergence guard を同期して足しているか**（`followup` G-12 の型） | **★★★`internal/repository/move/divergence_test.go` が両構造体の同期を強制する。⇒ 片側だけだと赤くなる。★赤くならずに通っていたら、guard の射程外に置いている** |
| **★★B-2** | **JSON タグが camelCase**（`json:"isDerived"`）**か**（`CLAUDE.md` §4） | **★snake_case だとフロントの型と食い違う** |
| **★★B-3** | **フロントの API 型定義へも足しているか** | **★`ComboSummary` / `ComboDetail` / `Combo` の 3 分岐とは別の型だが、追加漏れが起きやすい族である**（`web/CLAUDE.md` §2） |
| **★B-4** | **`damage` を足していないか** | **★本サブでは要らない。⇒ 「非攻撃技を末尾へ」は `is_derived` で代替できる** |

---

## 3. 束 C — ★★`M30-03` の成果を壊していないか

| # | 見ること | 落ちる形 |
|---|---|---|
| **★★★C-1** | **`M30-03` が載せた `A2` 105 件が、並べ替えで消えていないか** | **★★母集団を触る変更である。⇒ 並べ替えの実装が母集団を絞る形になっていると、静かに落ちる** |
| **★★C-2** | **`moveSurfacing.roster.test.ts` の座標一致テスト**（キャラごとに `special` 総数 == 必殺技タブの座標総数）**が緑のままか** | **★★`M30-03` 以降、`isOnSpecialTab` と `deriveSpecialFamilies` の結合を守っているのはこの 1 本だけである。⇒ 消していないか** |
| **★C-3** | **「素は押せるが OD が押せない」7 組が 0 件のままか** | **★`M30-03` が 0 件にした。⇒ 戻っていないか** |

---

## 4. 束 D — ★★捨てた案を蒸し返していないか

| # | 見ること | 落ちる形 |
|---|---|---|
| **★★D-1** | **CSV の行順を並べ替えていないか ／ `moves.id` を振り直していないか** | **★★★破壊的である。⇒ `combo_steps.move_id` 等が参照している** |
| **★★D-2** | **`code` から派生変種を判定していないか** | **★取りこぼし 90 / 116・誤爆 7**（実測）**。⇒ 使えない** |
| **★★D-3** | **`is_derived` を入力面の可否に使っていないか** | **★`M30-01` §2.9 (a)。⇒ `target_combo` 117 行が入力できなくなる。★本サブは並び順にしか使わない** |
| **★D-4** | **`is_derived = true` を削る・未分類タブへ移していないか** | **★開発者は「末尾へ出す」を選んだ**（`D-801`） |

---

## 5. 束 E — ★共通

| # | 見ること |
|---|---|
| **★★E-1** | **マイグレを 1 本も足していないか**（指示書＝0 本） |
| **★E-2** | **`bash scripts/check-import-order.sh` がベースラインどおりか**（`D-388`） |
| **★E-3** | **`console.log` が残っていないか**（`CLAUDE.md` §10） |
| **★E-4** | **完了報告が `bash scripts/check-md-emphasis.sh <file>` を通っているか**（`D-775`） |
| **★E-5** | **`docs/progress/progress-log.md` へ索引行が 1 行足されているか**（`CLAUDE.md` §8） |
| **★★E-6** | **設計伝達レポートに `DES-002` §11.3**（API 契約）**と `DES-005` 第6節**（並び順）**への CHANGE の請求が書かれているか** |
| **★★E-7** | **「推測:〜と仮定した」が要る箇所に書かれているか**（`CLAUDE.md` §9-3） |

---

*以上、M30-04 レビューチェックリスト **v1.0.0**。* **★★本書が最も見たいのは束 A-1 と束 B-1 である**——**前者は「並べ替えた」を証明できない形、後者は片側だけ足すと赤くなるはずの機構が効いているかだからである。**
