# M22-06 レビューチェックリスト

| 項目 | 内容 |
|------|------|
| 対象指示書 | `docs/instructions/M22-06-connection-qr-code.md` **v2.7.0**（接続用 QR コードの**検証と穴埋め**・`FR407`） |
| 文書ID | M22-06-REVIEW |
| 対象指示書ID | M22-06 |
| レビューモデル | **高速レビュー**（実モデル名は `docs/human-notes/model-allocation.md` が正本。実使用は開発者判断） |
| バージョン | 2.4.0 |
| 作成者・作成日 | 設計担当 Claude（M22 期・設計卓）/ 2026-08-15 |

> 文書ID ⇄ 実パスの正は `docs/handover/docs-map.md`（自動生成）。

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| **2.2.0** | 2026-08-17 | **指示書 v2.4.0 と対**（**D-427**）。**★穴候補 3〔QR の内容のテキスト表示〕は撤回された**——**既に実装されている。** **⇒ 「テキスト表示を足していない」を欠陥と読まないこと。** **★穴候補 4 件のうち 2 件が「穴ではない」と判明しており、「埋めるものが 1 つも無かった」で終わる可能性がある。** **⇒ 差分が小さいことを欠陥と読まないこと。** |
| **2.1.0** | 2026-08-17 | **指示書 v2.3.0 と対**（**D-426**）。**★`CHANGE-117` が起票されたため、着手前ゲートに通知書の承認が加わった。** **★`REQ-001` `FR407` の状態記述に関する穴候補は撤回された**——**要件表の列は実装状態を表さない。** **⇒ 「`REQ-001` を直していない」を欠陥と読まないこと。** |
| **2.0.0** | 2026-08-15 | **★全面改訂（指示書 v2.0.0 と対）。QR は実装済みであると判明したため、「作ったものを見る」から「既にあるものが要件を満たし、かつ守られ続ける形になったかを見る」へ性格を変えた**（**D-393**）。**依存追加に関する項目を削除した。** |
| 1.0.0 | 2026-08-15 | 初版（指示書 v1.0.0 と対）。**★前提が誤っていた。** |

---

## 0. レビュー担当への前置き

### 0.1 レビュー前の準備

**必読**——指示書 `M22-06` **v2.0.0** 全文 ／ **`REQ-001` FR407**（§3.4）／ **`DES-005` §5.16**・§6.8 ／ `DES-002` §3.4・§4.2・**§8**（**★LAN の割り切りは §8。§12 はテスト方針である**） ／ **`SUPP-001` §2.6.2**・§5.6・§5.5 (10′) (11′) ／ `docs/process/m22-contract.md` §1.2・§4・§5 ／ **`M22-RESEARCH-01` 調査レポート §6 軸 F・§8-1**。

**★Plan Mode の実査 8 項目（指示書 §3.3）が確定してから実装されているかを最初に確認すること**（**★2026-08-18 是正＝「6 項目」と書いていた**＝**D-433**）。 **とくに §3.3-2（ボタンの `disabled` の実体）は、穴の在否を決める項目である。**

### 0.2 レビューの基本姿勢

**★本サブは「作るサブ」ではない。** **したがって「差分が小さい」ことは欠点ではない。** **見るべきは 3 点。**

1. **★破壊確認が 2 件とも行われ、赤くなったか**（指示書 §5.1 の 2・4）。**★本サブの成果はほぼこれである。** **いま満たされていることと、守られ続けることは別であり、後者はテストでしか作れない。** **「既に満たされていたので何もしていない」で終わっている実装は、本サブの目的を達していない。**
2. **★§4.3 の穴の候補 4 件の在否が、すべて確定しているか。** **「無かった」も成果である。** **★確定していない項目が残っているのが不足であって、「埋めなかった」ことは不足ではない。**
3. **★実機確認が「スマートフォンのブラウザでアプリが開いた」まで到達しているか**（同 §4.4-4）。**「QR が表示された」で止まっているなら、最重要ゲートの片方が未検証である**——**`window.location` 由来の実装でも QR の表示までは通る。**

### 0.3 ★重大でないもの（**誤判定しないこと**）

| # | 誤判定しやすい状態 | 実は仕様である理由 |
|---|---|---|
| N-1 | **差分が小さい／本番コードの差分がテストだけ** | **★本サブは実装済みのものを検証するサブである**（指示書 §1.1）。**穴が無ければ本番コードは変わらない。** **見るのは差分の量ではなくテストの中身** |
| N-2 | **認証が掛かっていない状態で QR が使える** | **指示書 §1.3-1 でスコープ外。** **認証は `M22-01` / `M22-02` が別に足す。** **`DES-002` §8 が「自宅LAN内は信頼するという割り切りを許容」と明示している** |
| N-3 | **CORS/CSRF の設定が変わっていない** | **指示書 §1.3-3 / §2.2 でスコープ外。** **`M22-05` の担当** |
| N-4 | **CHANGE 通知書が無い** | **指示書 §6.5。** **`DES-005` §5.16 が既に画面仕様を持つため、着手時点では CHANGE が要らない。** **★要否は as-built で判定し、該当したら完了報告へ書く運用である** |
| N-5 | **`/app_build_check` が WAIT である** | **★着手前から WAIT である**（`M22-RESEARCH-01` G-6＝`nanoid` high×2 ／ `postcss` high×1 ＋ moderate×1 ／ `react-router` 系 moderate×3）。**本サブが作った状態ではない。** **依存更新の判断は開発者**（`CLAUDE.md` §6）。**完了報告にその旨があるかだけを見る**（指示書 §7.5-5） |
| N-6 | **`qrcode.react` が使われている** | **★既に `dependencies` に在るものである**（`web/package.json:37`）。**本サブが足したものではない。** **「無断で依存を追加した」として扱わないこと**——**`git diff` で `package.json` / `pnpm-lock.yaml` に差分が無いことを見れば確定できる** |
| N-7 | **`SelectPrimaryLANIP` が代表 IP を 1 件しか返さない** | **既存の設計**（`SUPP-001` §2.6.2）。**指示書 §1.3-5 / §2.2-6 で変更禁止。** **★スマートフォンが別セグメントに居ると繋がらないのは既知であり、本サブの欠陥ではない** |
| N-8 | **`internal/` に diff がある** | **★本サブは `internal/` 全体を凍結していない**（指示書 §2.2-1 が凍結するのは `internal/service/notation/` と `internal/service/preset/` だけ＝契約 F-5）。**`internal/service/config/` は触ってよい。** **「契約違反」として扱わないこと**（**D-379** の型） |
| N-9 | **`REQ-001` `FR407` の「希望・フェーズ3」という記述が直っていない** | **★製造は要件を編集しない**（`CLAUDE.md` §8）。**指示書 §4.3-4 / §6.5-2 は「完了報告へ書いて設計卓へ渡す」としている。** **書いてあるかだけを見る** |

### 0.4 報告フォーマット

- 各節「OK / 重大（§9）/ 軽微（§10）/ 質問（§11）」。

---

## 1. 最重要ゲート（**ここが本サブの本体**）

- [ ] **★破壊確認 A が行われ、赤くなった**——**QR へパスワード相当の文字列を足す実装に変えるとテストが赤くなる**（指示書 §5.1-2）。
- [ ] **★破壊確認 B が行われ、赤くなった**——**`window.location` を別ホストへ差し替えても QR に渡る値が変わらない**（同 §5.1-4）。
- [ ] **★赤にならなかった項目について、「何が代わりに守っていたか」が特定され、同じ層に契約テストが置き直されている**（`SUPP-001` §5.5 (10′)）。**「テストを強くした」だけで済ませていないか。**
- [ ] **★実機確認が「スマートフォンのブラウザでアプリが開いた」まで到達している**（指示書 §4.4-4）。**「QR が表示された」で止まっていないか。**
- [ ] **★LAN 共有モードを ON にして確認している**（同 §4.4-3）。**`local` モードでは `lanUrl` が空文字であり、そもそも QR が出ない。**
- [ ] **設定を書き換えたなら元へ戻している**（同 §4.4-5）。

## 2. 設計・要件との照合

- [ ] **QR に符号化されているのが接続 URL だけである**（指示書 §4.1-1・`FR407`）。
- [ ] **パスワード・トークン・セッション情報が含まれていない**（同 §4.1-2）。**「将来のために埋めておく」形になっていない**（同 §4.1-3）。
- [ ] **接続 URL がサーバ由来である。`window.location` から組み立てていない**（同 §4.2-1）。
- [ ] **新しい IP 取得経路を作っていない**（同 §4.2-4・`E-76`）。**既存の `lanIpResolver` の注入経路を使っているか。**
- [ ] **LAN 共有モード OFF のとき、ボタンが `DES-005` §5.16 の「ON時のみ有効」と一致する状態である**（同 §4.3-1）。**★無効なだけで理由が見えない形になっていないか。**
- [ ] **「接続可能IP/ポート表示」の在否が確定している**（同 §4.3-2）。**無くて埋めたなら、`GET /api/config` の `network.primaryLanIp` を使っているか**（**サーバ側を新設していないか**）。
- [ ] **外部サービスへ問い合わせて QR を得る形になっていない**（同 §1.3-6）。

## 3. 穴の候補の処遇（指示書 §4.3）

- [ ] **★4 件それぞれについて「在った／無かった」が完了報告に書かれている**（指示書 §7.5-2）。**★1 件でも触れられていなければ不足である。**
- [ ] **埋めたものについて、埋め方が §4.3 の「埋めるか」列と整合している。**
- [ ] **埋めなかったものについて、埋めなかった理由が書かれている。**
- [ ] **`REQ-001` `FR407` の状態記述の失効（4 件目）が、完了報告へ「CHANGE が要る」の形で渡されている**（§0.3 N-9）。

## 4. データ・API 契約・依存の不変（非破壊性）

- [ ] **★`web/package.json` ／ `pnpm-lock.yaml` ／ `go.mod` ／ `go.sum` の diff が 0**（指示書 §2.2-5・§7.3）。**実測値が完了報告にあるか。**
- [ ] **`migrations/` ／ `character_data/` ／ `docs/design/` の diff が 0**（同 §2.2-3・§2.2-4）。
- [ ] **`internal/service/notation/` ／ `internal/service/preset/` の diff が 0**（契約 F-5）。
- [ ] **`web/src/features/physical-input/` ／ `gamepad/` ／ `keyboard/` の diff が 0**（契約 F-4。**`M21-07` との非干渉**）。
- [ ] **`netutil` の優先順位ロジック（`private_ip.go` の `selectByPriority`）に diff が無い**（指示書 §2.2-6）。
- [ ] **API を新設・変更した場合、それが完了報告に明記され、CHANGE が要る旨が書かれている**（指示書 §6.5-3）。

## 5. テストの妥当性（ケース数で確認。指示書 §5 ↔ §7 DoD）

- [ ] **§5.1 の 7 項目がすべてある**（**★7 は既存テストが在るなら足さない判断でよい。判断が示されているか**）。
- [ ] **既存 3 ケース（`QRCodeModal.test.tsx`）と重複していない**（指示書 §3.3-4）。
- [ ] **`lanUrl` が空文字のときにモーダルが壊れず理由が出る**（§5.1-6）。
- [ ] **E2E の A（OFF）と B（ON）が 1 組で置かれている**（§5.2）。**★B だけだと、常に有効な実装でも緑になる。**
- [ ] **spec が `config` を書き換えるなら、元の値へ復元している。**
- [ ] **新規 spec を足したなら、資源ごとに 1 ファイルへ寄せている**（**D-362**）。
- [ ] **★完了報告のテスト結果が、コマンド自身の出力（スイート名・pass/fail の件数）で書かれているか**（教訓 **E-125**）。**「exit 0」「全緑」だけを根拠にしていないか**——**`vitest: not found` が exit 0 を返した前例がある**（`M20-06`）。

## 6. コード品質・規約遵守

- [ ] 曖昧語依存でない（playbook §4.1）。定数化・命名一貫。
- [ ] **i18n を足したなら ja/en 両ロケール parity**（playbook §4.13・指示書 §9.2-2）。
- [ ] **新規 test-id が `DES-005` §6.8 の命名規約に従っている。**
- [ ] `console.log` を本番コードに残していない（`CLAUDE.md` §10）。
- [ ] **ブラウザストレージの新しいキーを使っていない**（`CLAUDE.md` §10.X）。使った場合は `web/CLAUDE.md` §1 の台帳へ追記され、CHANGE addendum が要る旨が完了報告にあるか。`bash scripts/check-browser-storage-keys.sh` が回されているか。

## 7. 既存挙動の温存（非破壊性）

- [ ] **LAN 共有モードの切替そのものが従来どおり動く**（指示書 §1.3-4・§4.5-3）。
- [ ] **設定画面の他の 5 セクションが壊れていない。**
- [ ] **`local` モードで設定画面が壊れない**（`lanUrl` が空文字になる）。

## 8. ドキュメント・進捗ログ

- [ ] 完了報告に **指示書 §7.5 の 9 項目**がすべてある。
- [ ] **§6.5 の CHANGE 要否の判定が書かれている**（**該当なしなら「該当なし」と明記されているか**）。
- [ ] **★`/app_build_check` が着手前から WAIT であることが書かれている**（指示書 §7.5-5）。**自分の変更のせいだと誤読されない形になっているか。**
- [ ] **撤回・変更した定義の残骸が残っていないか。** 指示書 §4.9 の走査結果が完了報告にあるか。**★走査コマンドそのものが残っているか。**
- [ ] 走査対象に **3 系統すべて**が含まれているか——**本番コード／テスト資産／設計文書・指示書・overview**。
- [ ] **★陽性対照が置かれているか**（`DES-005` §5.16 の QR 行は必ず当たる）。**「0 件だった」で終わっていないか**（`E-84`）。
- [ ] **★当たったファイルの全文が目視されているか。**
- [ ] **★並列相手との突合が行われているか**（教訓 `E-121`）——**`M21-07`**〔**面は違うが `web/src/` を同時に触る**〕**／ `M22-07` ／ 直列本線の完了済みサブ**。**並列相手が居なかったなら「居なかった」と明記されているか。**
- [ ] **★完了報告に「■ 併せて更新が要るもの」の節があるか**（教訓 `E-114`）。**該当が無い場合も「なし」と書かれているか。**
- [ ] **`docs/progress/progress-log.md` へ追記されているか**（`CLAUDE.md` §8）。
- [ ] **★followup の更新候補が設計伝達レポート §4 へ書かれているか**（**製造は `followup-backlog.md` を編集しない**＝**D-382**）。

---

## 9. 重大な問題の判定基準（完了承認を妨げる）

1. **★破壊確認 A / B が行われていない、または赤にならなかった項目を「テストを強くした」だけで済ませている**（指示書 §5.1 の 2・4）。**本サブの成果そのものが欠けている。**
2. **★QR にパスワード・トークン・セッション情報が含まれている**（`FR407` 違反）。
3. **★接続 URL が `window.location` から組み立てられている**（**開発者機では動いてしまい、スマートフォンでのみ失敗する**）。
4. **★実機確認が行われていない、または「QR が表示された」で止まっている**（指示書 §4.4-4）。
5. **★§4.3 の穴の候補 4 件のうち、在否が確定していないものが残っている**（**★「埋めなかった」は重大ではない。「調べていない」が重大である**）。
6. **依存を足した・替えた・上げた**（`CLAUDE.md` §6 ／ **D-382 (2)**。`web/package.json` / `pnpm-lock.yaml` / `go.mod` / `go.sum` の diff）。
7. **`migrations/` ／ `docs/design/` に diff がある。**
8. **`web/src/features/physical-input/` ほかに diff がある**（契約 F-4。**`M21-07` と交差した**）。
9. **`netutil` の優先順位ロジックを変えている**（`SUPP-001` §2.6.2 に触れる）。
10. **API を新設・変更したのに完了報告へ書いていない**（**設計卓が CHANGE を起票できない**）。

## 10. 軽微な問題の判定基準（持ち越し許容）

- モーダルのレイアウト・QR の表示サイズ。
- 無効時の理由表示の文言（**★ja/en が揃っていることは §6 で必須**）。
- 「接続可能IP/ポート表示」の並べ方・書式。
- test-id の粒度（規約に反しない範囲）。
- 失効した docblock・コメントの残り（**★ただし本サブが触った箇所に限る**）。

## 11. 質問・確認事項のフォーマット

「指示書 §X.X / `DES-005` §5.16 / `REQ-001` FR407 に対し実装が Z。意図確認したい」の形で根拠節を併記する。

## 12. レビュー完了の判定

§1〜§8 が全て OK、§9 重大ゼロ（**特に §9-1・§9-4・§9-5**）、§0.1 の Plan Mode 実査 8 項目が揃っている（**★2026-08-18 是正＝D-433**）。§10 軽微は持ち越し可。

### 12.1 ★本チェックリスト自身の検査（**発行前に設計担当が行う**）

- [x] **本書と対の指示書の間に、節番号の食い違いが無い。**
  → **指示書は詳細仕様が §4.1〜§4.5 で、否定形確認を §4.9 へ寄せてある**（指示書 §4.9 冒頭に理由あり）。**本書は否定形確認を一貫して「指示書 §4.9」と書き、詳細仕様側は「指示書 §4.5-2」のように項番付きで書き分けた。** **あわせて指示書は §6.5（CHANGE の要否）を §6 と §7 の間に持つ**——本書 §8 と §0.3 N-4 から参照している。
- [x] **指示書が禁じていない事項を「重大」として求めている項目が無い。**
  → **§9-8 は `web/src/features/physical-input/` ほかに限っており、`internal/` 全体も `web/src/` 全体も求めていない**（指示書 §2.2-1 と §0.3 N-8 で明示的に打ち消してある）。
  → **§9-6 は「足した・替えた・上げた」を禁じるのであって、`qrcode.react` を使うこと自体は正常である**（§0.3 N-6）。
- [x] **達成不能な条件を重大として求めていない。**
  → **§9-1〜§9-3 は §5.1 の必須テストで検証できる。** **§9-4 は開発者の実機確認であり、指示書 §11 が「完了はここで止まる」と明記している**——**⇒ 実機確認が未了なら「重大」ではなく「未完了」として扱い、製造の責に帰さないこと。**
  → **★「本番コードに差分があること」は求めていない**（§0.3 N-1）。**穴が無ければテストだけの差分で正しい。**
  → **★「`/app_build_check` を緑にすること」は求めていない**（§0.3 N-5）。**着手前から WAIT であり、依存更新は開発者の手番である。**

---

*以上、M22-06 レビューチェックリスト v2.4.0。配置 `docs/instructions/reviews/M22-06-review-checklist.md`。指示書 v2.7.0 と対。**本サブは作るサブではない。破壊確認 2 件が赤くなったこと・穴の候補 4 件の在否が確定したこと・実機確認がスマートフォンで接続できたところまで到達したことを最重要ゲートとする。***
