# M31-04 レビューチェックリスト（確定反撃でドライブリバーサルを扱う）

| 項目 | 内容 |
|------|------|
| 文書ID | M31-04-REVIEW-CHECKLIST |
| バージョン | **1.0.0** |
| 作成日 | 2026-09-10 |
| 対象指示書 | `docs/instructions/M31-04-drive-reversal-punish.md` **v1.0.0** |
| 発行 | 設計卓（`D-802`） |
| 使い方 | `/review_plan` の入力。**★本書は指示書の完了条件を写したものではない。⇒ 「緑でも壊れているもの」を探すための束である** |

---

## 0. ★★レビュー担当への前置き

**★★本サブは「設計が一度も扱っていない」ものを入れる。⇒ 前例が無いぶん、形の選び方そのものがレビュー対象である。**

**★★★あわせて本サブには並列上の制約が 1 つ在る＝`M35-02` が同時に走っている。⇒ あちらは `character_data/ryu.csv` を触って golden を再生成する。★本サブが CSV を触ると、マージ時に必ず衝突する。**

---

## 1. 束 A — ★★★並列上の制約（**最初に見ること**）

| # | 見ること | 落ちる形 |
|---|---|---|
| **★★★A-1** | **`character_data/*.csv` を 1 バイトも触っていないか** | **★★★触ると golden の再生成が要り、同時に走る `M35-02` とマージ時に必ず衝突する。⇒ 指示書ヘッダに注意として書いてある。★触っていたら、その理由が報告に在るか** |
| **★★A-2** | **既存 system move に倣ってマイグレで直接投入しているか**（`migrations/000025_seed_movement_system_moves_all.up.sql` の `CROSS JOIN characters`） | **★★倣っていないと、system move の持ち方が 2 通りになる。⇒ 次に触る人がどちらを正とするかで迷う**（指示書 §4.2） |
| **★A-3** | **golden の差分が 0 であることが示されているか** | **★CSV を触っていなければ 0 のはずである。⇒ 0 でないなら、どこかで CSV 経由の生成に触れている** |

---

## 2. 束 B — ★★★マイグレの層（**指示書 §4.1 が「最大の危険」と定めた点**）

| # | 見ること | 落ちる形 |
|---|---|---|
| **★★★B-1** | **ファイル名が `000109_data_*.sql` になっているか** | **★★★D リバのフレーム値は SF6 の事実であり層 B**（`CC-BY-SA-4.0`）**である。⇒ `_data_` を入れないと `REUSE.toml` の既定**（層 A＝`AGPL`）**へ静かに落ちる。★誰も赤くならない** |
| **★★B-2** | **`scripts/check-migration-license.sh` を回して緑であることが示されているか** | **★★機構が `GAME_TABLES` を見て判定する。⇒ 目視では層を確かめたことにならない。★`M28-05` が `000106` でまさにここを踏んでいる** |
| **★★B-3** | **番号が `000109` か。⇒ 自採番していないか** | **★`D-800` で払い出した番号である。★1 本に固定であり、足りないなら再請求**（`D-293`） |
| **★★B-4** | **`down` が書かれているか**（`SUPP-001` §5.5.4） | **★`down` が無いと歴史テストが往復できない** |

---

## 3. 束 C — ★★★走査に出ているか（**指示書 §4.3**）

| # | 見ること | 落ちる形 |
|---|---|---|
| **★★★C-1** | **★★陽性対照が在るか＝入れる前に「出ない」ことを実測し、入れた後に「出る」ことを実測しているか** | **★★★後だけ見ると、もともと出ていたのか本サブで出るようになったのかが分からない。⇒ 指示書 §4.3 が名指しで求めている点である** |
| **★★C-2** | **確定反撃の走査の述語を、段 1-2 で確かめてから入れているか** | **★★走査は述語で絞っている。⇒ 行を入れただけでは出ないことがありうる。★述語を確かめた記録が無ければ 1 件** |
| **★C-3** | **31 行すべてで出ることを確かめているか**（**一部のキャラだけでない**） | **★`CROSS JOIN characters` で入れると 31 行になる。⇒ 1 キャラで確かめて済ませていないか** |

---

## 4. 束 D — ★★設計判断の扱い

| # | 見ること | 落ちる形 |
|---|---|---|
| **★★★D-1** | **段 3**（ジャストパリィをどう表現するか）**を製造が独断で決めていないか** | **★★★指示書は「案を出して開発者へ回す」と定めている。⇒ 決めていたら、それ自体が契約違反であり報告に挙がっているか** |
| **★★D-2** | **`SM-098` が「要件」なのか「画面」なのかの実査結果が在るか**（指示書 §4.4） | **★★`REQ-001` に及ぶなら CHANGE の対象が変わる。⇒ 実査していなければ、CHANGE の請求が過不足になる** |
| **★★D-3** | **CHANGE を製造が自採番していないか** | **★1 本の見込みだが、番号は設計卓が払い出す**（`D-293`）**。⇒ たたき台を出すところまでが製造の手番** |
| **★D-4** | **段 4**（入力面に出すか）**の判断に理由が書かれているか** | **★出す／出さないのどちらでもよいが、理由が無いと次に触る人が読み直せない** |

---

## 5. 束 E — ★データの正しさ

| # | 見ること | 落ちる形 |
|---|---|---|
| **★★E-1** | **D リバのフレーム値の一次源が書かれているか** | **★★値そのものはレビュアーには検証できない。⇒ どこから採ったかが書かれていなければ、後から確かめられない** |
| **★★E-2** | **「一律」の値を 1 行で持っていないか**（指示書 §4.2） | **★★既存の system move は 31 行である。⇒ 1 行で持つと持ち方が 2 通りになる** |
| **★E-3** | **既存の system move を 1 行も壊していないか** | **★`CROSS JOIN characters` は既存行と衝突しうる。⇒ 追加であって置換でないことを確かめる** |

---

## 6. ★★見なくてよいもの

| # | 何を | なぜ |
|---|---|---|
| 1 | **D リバを扱うこと自体の是非** | **`SM-098` として登録済みであり、`M31` の射程 identity である** |
| 2 | **`M35-02` の中身** | **別サブである。⇒ 見るのは束 A の「衝突していないか」だけ** |
| 3 | **`data-testid` の登録** | **`CHANGE-171` が一覧運用を畳んでいる** |

---

*以上、M31-04 レビューチェックリスト **v1.0.0**。* **★★★本書で最も重いのは A-1・B-1・C-1・D-1 の 4 つである。** **★A-1 は並列運用の制約であり、破ると本サブが正しくてもマージで詰まる。** **★B-1 を落とすと SF6 の事実が `AGPL` で配られる。★C-1 が無いと「入れた」が「出る」の証拠にならない。★D-1 は設計判断を製造が飲み込んでいないかを見る点である。**
