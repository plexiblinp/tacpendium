# M20-06 レビューチェックリスト

| 項目 | 内容 |
|------|------|
| 文書ID | M20-06-REVIEW-CHECKLIST |
| バージョン | 1.2.0 |
| 対象 | `docs/instructions/M20-06-naming-canonicalization.md` v1.2.0 |
| 対の CHANGE | `docs/change-notes/CHANGE-103-notification.md` |
| 作成者・作成日 | 設計卓（Claude Code・武装中）/ 2026-08-14 |

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| **1.2.0** | **2026-08-14** | **指示書 v1.2.0 と対**（**D-373**）。**★§1.1-1 を「13 code × 2 プリセット ＝ 26 行」へ**——**旧項目は「13 件である」としか書いておらず、`srk` を落としても緑になる。** **★§1.7 を新設**（**投入しても今日は英語が表示されないことの書き分け**）。**★§2 へ「`official_ja_move` を触っていない」を追加。** **★§9 へ 2 件追加**（**表示経路の未実装 ／ 副次的発見の命名**）。**★重大 4 を差し替え・重大 17 を追加。** |
| **1.1.0** | **2026-08-14** | **指示書 v1.1.0 と対**（**D-372**）。**★§9 へ 2 件追加**——**「技名の表示が選択中プリセットを見ていないこと」と「投入先が 2 プリセットに限られること」は判定対象外である。** **★旧チェックリストのまま判定すると、`§9.4` 停止条件 3 を絞った裁定どおりの実装が差し戻される**（**D-323** の同型）。**§1.2 へ「プリセット固定」と「`move_code` 固定」の書き分けを入れた。** |
| 1.0.0 | 2026-08-14 | 初版（指示書 v1.0.0 と対）。 |

---

## 0. 読む順（★ここから始める）

1. **§1.2 を最初に見る**（**カスタムプリセット優先**）。**本サブで唯一、破っても表示が正しく見える項目である**
2. **§1.1**（値が規則どおりか）
3. **§4**（テスト。とくに破壊確認）
4. 残りは順不同

> **★本サブは「データを入れるだけ」に見える。** **だからこそ、入れ方の構造だけが実質の論点である。** **値の正しさは機械的に照合できるが、`§1.2` は照合できない。**

---

## 1. 設計意図との一致

### 1.1 13 件の値が規則どおりか（§4.2）

| # | 確認 |
|---|---|
| 1 | **★`move_code` が 13 件で、`numeric` と `srk` の両方に入っている**＝**26 行**（**★2026-08-14 更新＝D-373**。**旧項目は「13 件である」としか書いておらず、`srk` を落としても緑になった**）。**★14 件目を足していない**（`§1.3-6`） |
| **1′** | **★`official_ja_move` を触っていない**（**既存 13 行への UPDATE が無い**＝`§2.2-5`） |
| 2 | **`alias_text` が実データの `name_ja` と完全一致する**（**括弧の種類・記号を含めて。全角を半角へ直していない**） |
| 3 | **`alias_text_en` が `move_code` の整形結果と一致する**——**`_` → 半角スペース ／ トークン `od` → `OD` ／ 他は小文字のまま** |
| 4 | **★意訳が入っていない。** **`move_code` に無い語が英語側に出ていない**（**「新しい情報を作らない」が規則の本体である**） |
| 5 | **`od` を含む 3 件**（`arc_step_od` / `departure_shadow_od` / `scutum_counterattack_od`）**と、`windclad_od_...` の語中の `od` が同じ規則で処理されている** |

> **★値そのものを「こう書いたほうが分かりやすい」と指摘しないこと**（§9）。**値は `D-367` で確定しており、変更は設計卓の手番である。**

### 1.2 ★カスタムプリセット優先（§4.3・**最重要・差し戻し判定の 1 本目**）

| # | 確認 |
|---|---|
| 1 | **13 件が `preset_aliases` の行として、組み込みプリセットの `preset_id` に紐づいている** |
| 2 | **★表示経路・解決経路に「この `move_code` はこう表示する」という分岐が無い** |
| **2′** | **★【2026-08-14 追加＝D-372】「プリセット固定」を「`move_code` 固定」と混同していないか。** **★禁じているのは「この `move_code` はこう表示する」という分岐である。** **「この経路は常にこのプリセットで表示する」は別の論点であり、本サブの対象ではない**（`§1.3-8`。followup `move-name-display-preset-independent`）。**★13 件を投入しても、その経路の見え方は 1 文字も変わらない** |
| 3 | **★固定表があるとしても、それは「行を作るための入力」であって「表示時に引かれるもの」ではない**（**呼出元を実際に辿って確かめること。名前だけで判断しない**） |
| 4 | **カスタムプリセットが同じ `move_code` に別の値を持つとき、表示・逆引きのどちらもカスタム側になるテストがある**（§5 (c)(d)） |

> **★破っても 13 件以外の表示は正しく、テストも型検査も緑になる。** **露見するのは、利用者がカスタムプリセットを作り、しかもこの 13 件のどれかを編集したときだけである。** **⇒ 「動いているから良い」で通さないこと。**

### 1.3 投入が再適用の流れに乗っているか（§2.1-2）

| # | 確認 |
|---|---|
| 1 | **seed 波が増えたときに 13 件が置き去りにならない**（`M20-02` が明記した再適用責務） |
| 2 | **テストがある**（§5 (g)） |
| 3 | **★「今のデータには入っている」で終わっていない**——**次のキャラが seed された時点で欠ける形なら、それは未完である** |

### 1.4 一意制約に触れていないか（§3.3-5）

| # | 確認 |
|---|---|
| 1 | **`M20-03` の 3 制約を足しても落としてもいない**（`§2.2-3`） |
| 2 | **投入前の事前突合が計画に載っている**（**投入してから分かる形になっていない**） |
| 3 | **テストがある**（§5 (f)） |

### 1.5 `(ラッシュ)` の実査結果（§4.4）

| # | 確認 |
|---|---|
| 1 | **件数の数え方が併記されている**（**何を・どの単位で・どの基準時点で数えたか**） |
| 2 | **★残る 4 件の正体が特定されている**（**「268/272 で揺れ 0 件」だけでは報告として不足である**） |
| 3 | **★設計書の文案を作っていない**（`§4.4` の末尾。**文案を受け取ると設計卓は検証せずに写す**） |

### 1.6 連鎖・ホールドの調査（§4.6）

| # | 確認 |
|---|---|
| 1 | **3 択**（表せる／表せない／判断がつかない）**で答えている** |
| 2 | **根拠になった実例が挙がっている**（**「前例がある」だけで終わっていない**） |
| 3 | **★`character_data/` の diff が 0 である**（**調査で CSV を触っていない**） |
| 4 | **★記法を足していない**（**「表せない」と分かっても拡張しない**＝`§1.3-4`） |

### 1.7 ★投入しても今日は英語が表示されないことの書き分け（§4.9）

| # | 確認 |
|---|---|
| 1 | **完了報告が「投入した」と「表示に効いている」を書き分けている**（`E-84`） |
| 2 | **★表示経路・逆引きを本サブで実装していない**（`§4.9-1`。**`DES-004` §5 の凍結**） |
| 3 | **★「英語が表示されること」をテストにしていない**（`§4.9-4`。**通らないテストになる**） |

> **★「26 行を入れた」だけの報告を通さないこと。** **読んだ側は英語表示が動いたと読む。**

---

## 2. 契約・凍結インターフェース

| # | 確認 |
|---|---|
| 1 | **`internal/moveindex/` ／ `move_commands` の diff 0**（契約 F-3） |
| 2 | **`scripts/` の diff 0**（**D-335**） |
| 3 | **`docs/design/` ／ `docs/change-notes/` の diff 0**（**製造は設計書を編集しない**） |
| 4 | **`DES-004` §5 の解決順序 ／ §5.3 の 3 段フォールバックが不変**（`§2.2-1`・`§2.2-2`） |
| 5 | **`recipe_cache` の再計算ロジックと呼出契機が不変**（`§2.2-4`） |
| 6 | **既存の `preset_aliases` 行が書き換わっていない**（`§2.2-5`。**13 件の新規行以外**） |
| 7 | **`alias_text_en` の編集導線を足していない**（`§1.3-5`） |

> **★`§2.2` は挙動の凍結であり、失効した記述の是正を妨げない**（**D-361**）。**docblock・コメントの是正を「変更しないと書いてある」だけの理由で差し戻さないこと。** **見るのは挙動が不変であることの diff である。**

---

## 3. 実装品質

| # | 確認 |
|---|---|
| 1 | `any` / `@ts-ignore` / `eslint-disable` / `console.log` の追加 0 件 |
| 2 | エラーの wrap（`fmt.Errorf("...: %w", err)`） |
| 3 | 列挙的文字列定数がリテラルで散在していない（CLAUDE.md §4） |
| 4 | **★13 件の固定表が 1 か所にある**（**複数箇所へ写されていない**＝`E-76`） |
| 5 | `TODO` コメントが `// TODO(<対応予定>): <内容>` 形式である |

---

## 4. テスト

| # | 確認 |
|---|---|
| 1 | **§5 の (a)〜(g) が実装されている** |
| 2 | **★(c)(d) の破壊確認が行われ、「どのテストが赤くなったか」まで報告されている**（**「赤になった」だけでは不足**） |
| 3 | **★破壊確認で赤にならなかった項目について、「テストが弱い」ではなく「別の機構が代わりに守っている」を先に疑った形跡がある**（`SUPP-001` §5.5 (10)） |
| 4 | **(e) が「14 件目を足した瞬間に赤くなる」形である**（**行の不在を主張している**） |
| 5 | `go test ./...` FAIL 0 ／ `pnpm test` 全緑 ／ `pnpm lint` exit 0 ／ `make e2e` 全緑 |

---

## 5. ドキュメント

| # | 確認 |
|---|---|
| 1 | **`docs/progress/progress-log.md` へ 1 行追記されている**（CLAUDE.md §8。**必須**） |
| 2 | **完了報告 `docs/progress/M20-06-completion-report.md` に §7.5 の 6 項目がある** |
| 3 | **§7.5-2**（**投入した全値**）**が、設計卓が規則どおりかを確認できる形で載っている** |
| 4 | **`migrations/` の消費本数が報告されている**（**disk 末尾を着手時に実査した記録つき**） |

---

## 6. 否定形確認（§4.7）

| # | 確認 |
|---|---|
| 1 | **2 軸で回している**——**(a) 識別子 ／ (b) 前提を述べている散文**（**D-361**） |
| 2 | **★走査コマンドそのものが完了報告に残っている**（**件数だけでは次回の比較に使えない**） |
| 3 | **§4.7 の 4 項目それぞれについて結果がある** |
| 4 | **★項目 2 と項目 4 は「失効していない」が正解である**——**失効したと判定していたら誤りである**（`D-305` は不変 ／ (b) の補記は本サブの範囲外） |

---

## 7. 停止規律

| # | 確認 |
|---|---|
| 1 | **§9.4 の停止条件に当たった場合、`followup-backlog` §J へ必須 5 フィールド付きで記録され、停止している** |
| 2 | **再レビュー往復が 2 回を超えていない** |
| 3 | **記録して停止した場合、それを「失敗」として扱っていない**（**正規の完了形式である**） |

---

## 8. 差し戻し事由（重大）

| # | 事由 |
|---|---|
| **重大 1** | **★表示経路・解決経路にプリセット非依存の分岐がある**（`§4.3-2`） |
| **重大 2** | **★固定表が表示時に引かれている**（`§4.3-3`） |
| **重大 3** | **カスタムプリセット優先のテスト（§5 (c)(d)）が無い、または破壊確認をしていない** |
| **重大 4** | **`move_code` が 13 件でない**（**14 件目を足した ／ 減らした**）**、または `numeric` / `srk` の片方にしか入っていない**（**26 行でない**） |
| **重大 5** | **`alias_text` が `name_ja` と一致しない**（**括弧の正規化を含む**） |
| **重大 6** | **`alias_text_en` に `move_code` に無い語が入っている**（意訳） |
| **重大 7** | **一意制約を足した ／ 落とした** |
| **重大 8** | **既存の `preset_aliases` 行を書き換えた** |
| **重大 9** | **`character_data/` を触った**（`§4.6`） |
| **重大 10** | **`command` 記法を拡張した**（`§1.3-4`） |
| **重大 11** | **設計書（`docs/design/`）を編集した** |
| **重大 12** | **`internal/moveindex/` ／ `move_commands` ／ `scripts/` の diff がある** |
| **重大 13** | **命名の揺れのデータ補正を行った**（`§1.3-1`。**`D-305` は不変**） |
| **重大 14** | **再適用の流れに乗っていない**（**次の seed 波で欠ける**） |
| **重大 15** | **`alias_text_en` の編集導線を足した**（`§1.3-5`） |
| **重大 16** | **★`official_ja_move` の既存 13 行を UPDATE した**（`§2.2-5`） |
| **重大 17** | **★`alias_text_en` の表示経路 ／ 逆引きを本サブで実装した**（`§4.9-1`。**`DES-004` §5 の凍結**） |

---

## 9. ★このレビューで判定してはいけないこと

| # | 内容 | 理由 |
|---|---|---|
| 1 | **13 件の値そのものの良し悪し**（「この技名は分かりにくい」等） | **`D-367` で確定済み。変更は設計卓の手番** |
| 2 | **jp `departure_window_double_warp_od` を足すべきかどうか** | **開発者判断で対象外**（`D-367`）。**足していないことが正しい** |
| 3 | **`D-305`（命名の揺れの補正をしない）が覆ったかどうか** | **覆っていない。** **`P-34` の投入は補正ではなく新規行の投入である** |
| 4 | **`alias_text_en` を編集できるようにすべきかどうか** | **`D-368` で「組み込み専用」と割り切った** |
| 5 | **`command` 記法をどう拡張すべきか** | **`DES-004` の設計手番。本サブは調査だけを行う** |
| 6 | **設計書の文案** | **製造が文案を作らないのが正しい**（`§4.4` の末尾） |
| 7 | **固定表をどのファイル・どの層に置いたか** | **`§9.2-1` で製造へ委ねた**（`§4.3` の制約を満たす限り） |
| 8 | **投入をマイグレーションで行うか再適用で行うか** | **`§9.2-2` で製造へ委ねた**（`§2.1-2` を満たす限り） |
| **11** | **★`alias_text_en` の表示経路・逆引きが未実装であること** | **【2026-08-14 追加＝D-373】本サブの対象外。** **`M20-02` から続く状態であり、本サブが作ったものではない。** **followup へ送った** |
| **12** | **★`rush_desert_storm_1hits` の `name_ja` が規則から外れていること** | **【2026-08-14 追加＝D-373】`D-305` によりデータ補正は行わない。** **報告のみが正しい** |
| **9** | **★技名（`name_ja`）の表示が選択中プリセットを見ていないこと** | **【2026-08-14 追加＝D-372】本サブの対象外。** **`§4.3` が禁じたのは `move_code` 固定の分岐であり、これはプリセット固定である。** **followup `move-name-display-preset-independent` へ送った** |
| **10** | **★投入先が `numeric` と `srk` の 2 つに限られること** | **【2026-08-14 追加＝D-372】設計卓が確定した**（`§3.3-4`）。**公式表記のプリセットへ入れると、13 件だけ英語・他は日本語というまだらになる** |

---

*以上、M20-06 レビューチェックリスト v1.2.0。*
