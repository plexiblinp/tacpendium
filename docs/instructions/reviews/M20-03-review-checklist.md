# M20-03 レビューチェックリスト

| 項目 | 内容 |
|------|------|
| 文書ID | M20-03-REVIEW-CHECKLIST |
| バージョン | 1.0.0 |
| 対象 | `docs/instructions/M20-03-alias-uniqueness-constraint.md` v1.0.0 |
| 対の CHANGE | `docs/change-notes/CHANGE-100-notification.md` |
| 作成者・作成日 | 設計卓（Claude Code・武装中）/ 2026-08-13 |

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| 1.0.0 | 2026-08-13 | 初版（指示書 v1.0.0 と対）。 |

---

## 0. 読む順（★ここから始める）

- **指示書 `M20-03` v1.0.0 を全文読む**（特に **§1.1 の 714 の正体** ／ **§4.2 の UNIQUE 本体** ／ **§9.3 の停止条件 5 件**）。
- **`CHANGE-100` 通知書を読む。**
- **★完了報告の §3.3 実査結果を、実装より先に読む。** **とくに §3.3-2**（**違反 0 件の再確認**）——**書かれていなければ、制約が張れる根拠を誰も確認していないことになる。**
- **★破壊テストの実施記録を読む**（完了報告 §7.5 の 5）。**制約は「張った」だけでは効いているか分からない。**

---

## 1. 設計意図との一致

### 1.1 `character_id` の追加と backfill（§4.1）

- [ ] **`character_id` が `preset_aliases` に追加されている**
- [ ] **backfill 後に NULL が 0 件**（件数と、検査した全行数が報告にある）
- [ ] **★`character_id` が `moves.character_id` と全行で一致する**（§5.1 (c)。**非正規化の担保**）
- [ ] **`moves` テーブルに diff が無い**（**複製であって移動ではない**）
- [ ] **§4.1-3（`NOT NULL` の実現方法）と §4.1-4（FK）の判断が報告にあり、理由が書かれている**

### 1.2 UNIQUE 本体（§4.2）

- [ ] **`UNIQUE(preset_id, character_id, alias_text)` が張られている**
- [ ] **★`(character_id, alias_text)` に張っていない**（§4.2-2。**714 件に当たって落ちる**）
- [ ] **★既存の `UNIQUE(preset_id, move_id)` が残っている**（§4.2-3。**意味が逆向きの別制約である**）

### 1.3 部分インデックス（§4.3）

- [ ] **`UNIQUE(preset_id, character_id, alias_text_en) WHERE alias_text_en IS NOT NULL` が張られている**
- [ ] **`WHERE` 句がある**（**意図の明示。SQLite は NULL を互いに異なるものとして扱うため実害は無いが、書くこと自体が仕様である**）

### 1.4 交差の検査（§4.4）

- [ ] **同一 `(preset_id, character_id)` の中で、ある行の `alias_text` が別の行の `alias_text_en` と一致しないことがテストで固定されている**
- [ ] **★製造が `DES-004` §5 を編集していない**（**「投入は生成器を通す」の規約化は設計卓の手番**＝§4.4-2）

### 1.5 生成器の追随（§4.5）

- [ ] **生成器が `character_id` を出力へ含めている**
- [ ] **生成器のテストでそれが固定されている**（§5.1 (l)）
- [ ] **★出さない場合の危険が理解されている**——**nullable なら NULL 行が入って制約をすり抜ける。** **落ちないため気づけない**

---

## 2. 契約の非干渉（★機械的に見る）

- [ ] **`internal/service/punishfinder` ／ `setplay` の diff が 0**
- [ ] **`moves` テーブルに diff が無い**
- [ ] **M20-02 が投入した `alias_text` / `alias_text_en` の値が 1 文字も変わっていない**
- [ ] **`web/` を触っていない**（**触ったなら §9.3 の停止条件 3 に該当し、報告があるはず**）
- [ ] **適用済みマイグレを改変していない**（**改変したなら三点更新であり、どちらを採ったかが報告にある**＝§2.2）

---

## 3. テスト（§5.1）

- [ ] **(a)〜(l) が実在し、すべて通る**
- [ ] **★破壊テスト (i)(j)(k) が実在する**——**(i) 同一 `(preset_id, character_id, alias_text)` で別 `move_id` を INSERT すると失敗** ／ **(j) `alias_text_en` 側も同様** ／ **(k) `alias_text_en` が NULL の行は複数あってよい**
- [ ] **★破壊テストの実施記録が完了報告かコミットメッセージにある**（`SUPP-001` §5.5.4 (10)）
- [ ] **(c) `character_id` が `moves.character_id` と全行一致することが固定されている**
- [ ] **(h) down で投入前へ戻る**
- [ ] **比較区間が自サブに閉じている**（`SUPP-001` §5.5 (1)）。**`m.Up()` を使っていない。HEAD スコープの主張が本サブの名前空間に無い**（同 (2)）

---

## 4. 否定形確認（§4.7）

- [ ] **走査結果が表で報告されている**（対象と件数）
- [ ] **「いつ時点のものか」が明記されている**
- [ ] **0 件の項目が「0 件」と書かれている**
- [ ] **`preset_id` を含まない一意制約が残っていない**（**`preset_id` を含む形は正しい実装なので指摘しない**）

---

## 5. 報告の質

- [ ] **§3.3 の 7 項目すべてに実査結果がある**（**「確認した」ではなく、何が見えたかが書いてある**）
- [ ] **★`CHANGE-100` の反映に必要な as-built が 5 点そろっている**（§7.5 の 3＝**`character_id` の型と NULL 可否 ／ FK の有無 ／ 制約の実際の定義文 ／ 部分インデックスの `WHERE` 句 ／ 消費したマイグレ連番**）
- [ ] **§3.3-6 の実査結果がある**——**`preset_aliases` へ直接 INSERT する経路が生成器以外に実在するか**（**§4.4-2 の規約の実効性を設計卓が判断するため**）
- [ ] **backfill の件数がある**（NULL 0 件 ／ 検査した全行数）
- [ ] **`docs/progress/progress-log.md` へ索引行が追記されている**

---

## 6. ベースライン型検査

- [ ] `bash scripts/check-enum-sync.sh` がベースラインから増えていない
- [ ] **★ベースラインを緩めていない。** **緩めたなら理由と数値が完了報告にある**（**D-323** §3-4）
- [ ] **★`scripts/*.sh` のベースライン定数の diff が 0**（**D-335**）。**実際の違反を直しているぶんには問題ない。定数だけを見る**

---

## 7. 指摘しないこと（★誤検出の抑止）

| # | 見えるもの | 指摘しない理由 |
|---|---|---|
| N-1 | **`(character_id, alias_text)` に重複が 714 件ある** | **★無害である**（§1.1）。**「同じ技が複数プリセットで同じ表記になっている」だけで、指している技は同一。** **逆引きは `SELECT DISTINCT m.code` で 1 件に畳む。** **制約は `preset_id` を鍵に含むため当たらない** |
| N-2 | **衝突組（`numeric` 35 キー / `srk` 30 キー）が投入されていない** | **M20-02 の仕様**（`DES-004` §5.5）。**恒久対応は followup `numeric-aerial-vs-crouching-collision`。** **投入されていない行は制約に当たらない** |
| N-3 | **`alias_text_en` が NULL の行が大半** | **`DES-004` §5.3。** **値が入るのは移動系 2 code のみ・76 行。** **NULL は「英語表記を持たない」であって「未定義」ではない** |
| N-4 | **`character_id` が `moves` と重複して持たれている** | **★意図的な非正規化**（**D-344**）。**単一テーブルの UNIQUE で不変条件を表現するために必要。** **整合は §5.1 (c) で固定する** |
| N-5 | **`character_id` が nullable のまま** | **§4.1-3。** **SQLite の `ALTER TABLE` はテーブル再構築なしに `NOT NULL` を足せない場合がある。** **判断と理由が報告にあり、NULL 0 件がテストで固定されていれば足りる** |
| N-6 | **FK が張られていない** | **§4.1-4。** **`move_id` の FK が既にあり `character_id` はその推移的な複製である。** **判断が報告に書かれていれば十分** |
| N-7 | **交差の検査が DB 制約になっていない** | **★列を跨ぐ条件であるため UNIQUE では表現できない**（**D-344**）。**検査で守るのが正しい** |
| N-8 | **`DES-003` ／ `DES-004` が改訂されていない** | **★正しい。** **本体の改訂は as-built 確定後に設計卓が行う。** **製造が設計書を直接編集していたらそちらが重大**（§9-4） |
| N-9 | **フロントが変更されていない** | **§5.2。本サブはスキーマと生成器で閉じる** |

---

## 8. 許容してよい判断（報告に書かれていれば可）

- **§4.1-3** `NOT NULL` にするか（テーブル再構築の費用と risk を見た判断）
- **§4.1-4** FK を張るか
- **マイグレの分けかた**（§2.1 は 2 本を推奨だが覆ってよい）
- **§2.1 のパスと M20-02 の as-built の食い違い**（**既存に合わせ、食い違いが報告に書かれていれば可**）

---

## 9. 重大（★これが出たら差し戻す）

1. **★制約が `(character_id, alias_text)` に張られている**——**§4.2-2。** **714 件に当たり、マイグレが落ちる**
2. **★破壊テスト (i)(j) が存在しない**——**制約は「張った」だけでは効いているか分からない**（`SUPP-001` §5.5.4 (10)）
3. **`character_id` が `moves.character_id` と一致しない行がある**——**非正規化の整合が崩れている**
4. **製造が `DES-003` ／ `DES-004` を直接編集した**——**`CLAUDE.md` §8**
5. **`moves` テーブルを触った**——**§2.2。`character_id` は複製であって移動ではない**
6. **M20-02 が投入した `alias_text` / `alias_text_en` の値が変わった**——**本サブは制約を張るだけである**
7. **既存の `UNIQUE(preset_id, move_id)` を落とした**——**§4.2-3。意味が逆向きの別制約であり、両方要る**
8. **生成器が `character_id` を出していない**——**nullable なら NULL 行が入って制約をすり抜ける。落ちないため気づけない**
9. **★制約の違反が 0 件でないのに投入した**——**§9.3 の停止条件 1。** **止まって全件列挙するのが正しい。独断で行を消していないか**
10. **マイグレ連番を自採番した**——**D-293**
11. **`scripts/*.sh` のベースライン定数を書き換えた**——**D-335**
12. **`punishfinder` ／ `setplay` に diff がある**

---

## 10. レビュー時の自己確認（★チェックリスト自身の健全性）

- 本書が参照する指示書の節は **§1.1 / §1.3 / §2.1 / §2.2 / §3.3 / §4.1 / §4.2 / §4.3 / §4.4 / §4.5 / §4.7 / §5.1 / §5.2 / §7.5 / §9.3** である。**指示書に実在しない節を指していないかを最初に確かめること。**
- **§9 の 12 件はすべて指示書の明示的な指示に対応する**（1→§4.2-2 ／ 2→§5.1 (i)(j) ／ 3→§5.1 (c) ／ 4→`CLAUDE.md` §8 ／ 5→§2.2 ／ 6→§2.2 ／ 7→§4.2-3 ／ 8→§4.5 ／ 9→§9.3-1 ／ 10→§3.3-1 ／ 11→§7.3 ／ 12→§7.3）。**指示書に無い要求を重大に格上げしないこと。**
- **★714 件の重複を「問題」と読まないこと。** **本サブの設計は「714 は無害、0 件が守りたいもの」という理解の上に立っている**（**D-344**）。**ここを取り違えると、正しい実装が重大判定になる。**
- **★指示書が v1.0.0 のままかを確かめること**（**D-331** の実例）。

---

*以上、M20-03 レビューチェックリスト v1.0.0。配置 `docs/instructions/reviews/M20-03-review-checklist.md`。*
