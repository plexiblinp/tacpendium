# M19-04d レビューチェックリスト

| 項目 | 内容 |
|------|------|
| 文書ID | M19-04D-REVIEW |
| 対象指示書 | `docs/instructions/M19-04d-integration-catchup.md` **v1.1.0** |
| 対象指示書ID | M19-04d |
| CHANGE | **なし**（スキーマ変更ゼロ・DES 改訂なし） |
| レビューモデル | Sonnet 4.6（model-allocation。実使用は開発者判断） |
| バージョン | **1.1.3** |
| 作成者・作成日 | 親チャット（設計卓）/ 2026-08-03 |

> **文書ID ⇄ 実パスの正は `docs/handover/docs-map.md`（自動生成）。**

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| **1.1.3** | **2026-08-07** | **メタ表へ `文書ID` 行を追加＝ボード **D-203**。`docs-map` 未収録の是正**（`scripts/generate-docs-map.sh` は `文書ID` 行から §1 を抽出するため、行が無い文書は載らない）。**本文の変更なし**（反映係・設計卓の依頼 Part 3 項目 6）。**対象指示書の版は変えていない。** |
| **1.1.2** | **2026-08-03** | **§0.1・§12 の Plan Mode 着手前確認の項目数を、対象指示書 §3 の実数へ是正**（反映結果レビューの指摘）。指示書 §3 の表は **7 行**（`1・2・3・4・5・5b・6`）だが、本書は 2 か所とも 1 つ少ない値を持っていた。**★取りこぼしたのは `5b`**——v1.1.0 で「1403c（4 か所目）」対応として枝番で追加された行であり、**連番だけを数える読み方では枝番が落ちる**。**再発防止として項目を列挙する形に改めた**（数だけを書くと、次に枝番が増えたときに同じ穴が空く）。**反映係（Claude Code）による是正**。 |
| **1.1.1** | **2026-08-03** | **§0.1 の参照版を v1.0.0 → v1.1.0 へ是正**（反映係の指摘。メタ表は v1.1.0 だったが §0.1 だけ旧版のままだった＝**同じ版番号を 2 か所に持ったことによるドリフト**）。 |
| **1.1.0** | **2026-08-03** | 対象指示書を **v1.1.0** へ追従。**§3 に 1403c（4 か所目）と共有テーブルの参照元 grep を追加**。**§3.5 を新設**（HEAD 終端の残り 4 か所と、弱まる主張の評価）。 |
| 1.0.0 | 2026-08-03 | 初版。 |

---

## 0. レビュー担当への前置き

### 0.1 準備

- 必読: 指示書 M19-04d **v1.1.0** **全節**（**とくに §0 の現状〔§0.3 の `expectedMovesDelta` の参照元 2 か所・§0.4 の HEAD 終端 8 か所〕・§4.1 述語の踏襲・§4.2 区間の是正・§4.3 HEAD 終端の残り 4 か所・§5 テスト**）、`migrations/000050`（**踏襲する原型**）、`m18-m19-contract.md` **§2（F-1・F-2・F-3）**、`DES-003` **§3.3**。
- **Plan Mode 着手前確認の確認（必須）**: 指示書 §3 の **7 項目すべて**（**1・2・3・4・5・5b・6**。**`5b` を数え落とさないこと**）に提示と承認が残っているか。**とくに 3（件数の予測をキャラ別に出す）。3 が提示されずに実装へ進んでいたらその時点で重大**（§9）。

### 0.2 基本姿勢

- **本サブは新しい規則を作らない。** `000050` の述語を**第三波 6 キャラへ再適用するだけ**である。**規則を改良していたら、それは独自判断として §2 に上がっているべきである。**
- **★本サブは「テストを直す」サブでもある。** **したがって「テストが通った」ことが成功の証明にならない。** **何を検査しているかが変わっていないかを、レビューが見る。**
- **「エラーにならない失敗」を疑う。** 主要な失敗モードは、**規則を広く当てすぎて人手対象の行まで埋めること**（件数が増えるだけでエラーにならない）と、**期待値を書き換えてテストを通すこと**である。

### 0.3 報告フォーマット

- 各節ごとに「OK / 重大（§9）/ 軽微（§10）/ 質問（§11）」で報告。

---

## 1. 【最重要】投入行と非投入行が対で固定されているか

- [ ] **`startup_basis='standalone'` に変わった件数**が、**キャラ別**に固定されているか。
- [ ] **`fastest_unreachable=1` に変わった件数**が固定されているか。
- [ ] **★第三波 6 キャラの `is_derived=1` の 149 行が `startup_basis='unknown'` のまま**であることが固定されているか。**これが無ければ重大。**
- [ ] **★B 型 9 行が `fastest_unreachable=0` のまま**であることが固定されているか。
- [ ] **理由が理解されているか**——**「投入した件数」だけでは、規則を誤って広く当てても検出できない。** `is_derived=1` の行まで `standalone` にしても、**件数が増えるだけでエラーにならない。**
- [ ] **`c_viper` / `dhalsim` の 18 行が `unknown` のまま**であることが固定されているか。
- [ ] **既に値が入っていた 11 キャラの `startup_basis` / `fastest_unreachable` が 1 行も変わっていない**ことが示されているか。

## 2. 【最重要】`000050` の述語を踏襲しているか

- [ ] **`startup_basis`**——`is_derived = 0` → `'standalone'` ／ `category = 'rush_variant'` → `'through'`。**規則を変えていないか。**
- [ ] **`fastest_unreachable`**——**`code` に `jumping_` を含む（★部分一致）**。**前方一致（`LIKE 'jumping_%'` / `HasPrefix`）になっていたら重大**（`neutral_jumping_heavy_kick` を取り落とす。M18-03c で同じ 2 行を取り落とす誤りを是正した経緯がある）。
- [ ] **対象が第三波 6 キャラに限定**されているか（`character_code IN (...)` が明示されているか）。**全キャラを対象にしていないか。**
- [ ] **`AND startup_basis = 'unknown'`（および `AND fastest_unreachable = 0`）で既存値を保護**しているか（契約 F-4 (2') の (c) と同じ形＝ボード D-179）。
- [ ] **provenance コメント**が付いているか（**`000050` の規則を第三波へ追随させるものであること、および `000050` が後から INSERT された行を拾わないこと**）。
- [ ] **down が「第三波 6 キャラの、本サブが変えた行」だけ**を戻しているか。**他キャラ・他行に触れていないか。**

## 3. 【最重要】区間の是正であって、期待値の書き換えでないか

- [ ] **★4 テストの区間が指示書 §4.2 のとおり**か——**#0 `TestRun_M1403c_OtherCharsUnaffected` は v28 → v30**（終端を `m.Up()` から変更）／#1 **v42 → v48**（変更なし）／#2 **v52 → v59**（始端を変更）／#3 **v48 → v52**（終端を変更）。
- [ ] **★#0 が入っているか。** **入っていなければ重大**——`expectedMovesDelta` の削除で **1403c が新たに red になる**（参照元が 2 か所あるため。製造が 2026-08-03 に実証済み）。
- [ ] **★`expectedMovesDelta` と `parseSnapshot` の参照元の全数 grep 結果が Plan Mode に提示**されているか。**提示なしに削除していたら重大**——**定義は `migrate_m1403c_test.go` にあり `migrate_m1403d_test.go` から使われている。ファイル名から使用者を推測できない。**
- [ ] **★`m1904MovesAfter = 1028` が書き換えられていないか。** **書き換えていたら重大**——区間を閉じれば `1028` は正しい値に戻る。**書き換えは「期待値を緩める」ことである。**
- [ ] **`expectedMovesDelta{"zangief": 3}` が削除されている**か。**値を書き換えて残していないか。**
- [ ] **削除の前後で、3 テストがそれぞれ何を検査しているかが完了報告に明記**されているか。**「表を消したら通った」ではなく「区間を閉じたので表が不要になった」と書かれているか。**
- [ ] **他のテストの期待値定数を「実測値へ更新」していないか。** **していたら重大**（本サブの範囲外。区間の是正で解けないものは止まって報告すべき）。
- [ ] **`m.Up()`（HEAD 終端）が新たに導入されていないか。**
- [ ] **本サブ自身の契約テストの区間が v61 → v62 に閉じている**か（**規約を自分にも適用しているか**）。

## 3.5 【重点】HEAD 終端の残り 4 か所（指示書 §4.3）

- [ ] **`migrate_m1403c_test.go` の #1（`:61` UpContract）・#3（`:206` MovementMovesPreserved）・#4（`:286` SweepsUserRows）・#5（`:324`/`:342` DownFidelityAndReUp）の終端が `m.Migrate(30)` へ閉じられている**か。
- [ ] **4 か所それぞれについて、「閉じることで何が主張されなくなるか」が 1 行で完了報告に書かれている**か。**書かれていなければ軽微ではなく重大**——**弱まったことを認識せずに弱めていないかがここで分かる。**
- [ ] **その主張が今も要ると判断した箇所について、HEAD スコープの別テストが新設されている**か。**新設しなかった箇所は、その理由が書かれている**か。
- [ ] **新設したテストの名前に `HEAD` が含まれている**か（意図の明示）。**`TestRun_M1403c_*` の名前空間に HEAD 主張を戻していないか。**
- [ ] **「全部新設した」「全部捨てた」になっていないか。** **1 件ずつ判断した形跡があるか。**
- [ ] **`migrate_test.go:849`（`TestRun_CustomStatesSeedDownRollback`）を触っていない**か。**触っていたら軽微**（スコープ外）。**意図的に残したことが完了報告に書かれている**か。

## 4. 「触らない」ことの担保（契約 F-1・F-2・F-3）

- [ ] **`internal/service/punishfinder/` の diff が 0** か（**1 行でも変更があれば重大**）。
- [ ] **`internal/service/setplay/` の diff が 0** か。
- [ ] **`internal/seedgen` の diff が 0** か。
- [ ] **`character_data/` の diff が 0** か。
- [ ] **既存マイグレ `000001`〜`000061` の diff が 0** か（`git status --porcelain migrations/` が**新規ファイルのみ**か）。**`000039` を触っていないか**（**M19-04c の範囲**）。
- [ ] **DDL を 1 行も書いていない**か（`CREATE` / `ALTER` / `DROP` の grep）。
- [ ] **`moves.startup` / `total` / `code` / `command` を 1 行も書き換えていない**か。
- [ ] **確定反撃サーチの canary 判定値が不変**か。**変わっていたら重大。**
- [ ] **golden 7 stem が green**・**`go run ./cmd/seedgen -check` が OK** か。

## 5. 件数の予測と実測

- [ ] **Plan Mode で件数の予測がキャラ別に出ている**か。**出ていなければ重大。**
- [ ] **投入後の実測と予測の突合結果が報告**されているか。**一致したか、違ったならなぜか。**
- [ ] **Plan Mode の導出値であることが明示され、実装冒頭で実クエリと突合**されているか（M19-04b の申し送り。本環境には dev DB も SQLite CLI も無い）。
- [ ] **`rush_variant` が第三波に何件あったかが報告**されているか。
- [ ] **`rush_variant` が 0 件だった場合**、**「投入が 0 件」ではなく「対象が 0 件」を固定**しているか。**「0 件でも成功するテスト」になっていないか。**

## 6. 全体 green の確認

- [ ] **`go test ./...` 全体が green** になっているか。**3 件が解消していることの実行結果が証跡として添えられている**か（**製造が着手前に 4 か所パッチで実証済み。再現していること**）。
- [ ] **3 件以外に新たに落ちたテストが無い**か。**あったのに報告されていなければ重大。**
- [ ] `go vet ./...` / `gofmt -l internal/ cmd/` が clean か。

## 7. 連番とドキュメント

- [ ] **`ls migrations/` の実査結果が Plan Mode に提示**され、**末尾が `000061` であることが確認**されているか。**違ったのに自採番して進んでいないか**（重大）。
- [ ] **消費したマイグレ連番が完了報告に明記**されているか。
- [ ] **他にも「1 回きりの backfill が後続の行を拾えていない」箇所が見つかった場合、投入せずに一覧だけ報告**されているか。
- [ ] **製造が DES を直接編集していない**か。
- [ ] **設計伝達レポート**が 4 分類に絞られているか。
- [ ] 禁則表現・簡体字・「起き攻け」誤字・「DR」略記の grep 除去。

---

## 9. 重大な問題（完了承認を妨げる）

- **投入しなかった行（`is_derived=1` の 149 行・B 型 9 行）が固定されていない。**
- **人手対象の行まで機械 backfill で埋めた**（`is_derived=1` の行が `standalone` になっている）。
- **`fastest_unreachable` を前方一致で backfill した。**
- **`000050` の述語を「改良」した**（規則を変えた。**独自判断として §2 に上がっていない**）。
- **対象を第三波 6 キャラに限定せず、全キャラを UPDATE した。**
- **既存値を保護する条件（`AND startup_basis='unknown'` 等）が無い。**
- **★`m1904MovesAfter = 1028` を書き換えた**／**`expectedMovesDelta` の値を書き換えて残した**／**他のテストの期待値定数を実測値へ更新した。**
- **区間の是正が指示書 §4.2 のとおりでない。**
- **★`TestRun_M1403c_OtherCharsUnaffected`（#0）を閉じずに `expectedMovesDelta` を削除した**（1403c が新たに red になる）。
- **★共有テーブル（`expectedMovesDelta` / `parseSnapshot`）の参照元を全数 grep せずに削除した。**
- **§4.3 の 4 か所で、「閉じることで何が主張されなくなるか」が書かれていない**（弱めたことを認識していない）。
- **HEAD 主張を `TestRun_M1403c_*` の名前空間に戻した**（新設テストの名前に `HEAD` が無い）。
- **`m.Up()`（HEAD 終端）を新たに導入した。**
- **本サブ自身の契約テストの区間を自サブに閉じていない。**
- **`rush_variant` が 0 件の場合に「投入 0 件」を成功として固定した。**
- **Plan Mode の件数予測が無いまま実装へ進んだ。**
- **`go test ./...` が green にならないまま完了とした**／**3 件以外に落ちたテストがあるのに報告していない。**
- **DDL を書いた。**
- **`internal/service/punishfinder/` または `internal/service/setplay/` または `internal/seedgen` を変更した。**
- **`character_data/*.csv` を変更した。**
- **既存マイグレを改変した**（**とくに `000039`。これは M19-04c の範囲**）。
- **`moves.startup` / `total` / `code` / `command` の既存値を書き換えた。**
- **マイグレ末尾が想定と違ったのに自採番して進めた。**
- **確定反撃サーチの候補集合が変わった。**
- **製造が DES を直接編集した。**

## 10. 軽微な問題（持ち越し許容）

- マイグレの分割数（**1 本にまとめても、消費連番が明記されていれば可**）。
- テストファイルの配置と命名（**既存ヘルパを再利用していれば可**）。
- 完了報告での件数の書式。
- E2E の既知 flakiness による 1 件の落ち（**単独実行で green であることと判定根拠が示されていれば許容**）。

## 11. 質問・確認事項のフォーマット

- 「指示書 §X.X / 設計書 DES-00N（実パス）§Y に対し、実装が Z。意図確認したい」の形で根拠節を併記して設計担当へ。

## 12. レビュー完了の判定

- §1〜§7 が全て OK、§9 重大ゼロ、Plan Mode 7 項目（**1〜5・5b・6**。**とくに 3**）が揃っている。§10 軽微は持ち越し可。

---

*以上、M19-04d レビューチェックリスト v1.1.1。配置 `docs/instructions/reviews/M19-04d-review-checklist.md`。重点＝**投入行と非投入行を対で固定すること**・**`000050` の述語を改良せず踏襲すること**・**区間の是正であって期待値の書き換えでないこと**・**★共有テーブルの参照元を全数 grep すること**・**部分一致**。*
