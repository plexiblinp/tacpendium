# M31-01 レビューチェックリスト: セットプレイ ＋ 確定反撃 ＋ 起き攻めの連動

| 項目 | 内容 |
|------|------|
| 文書ID | M31-01-review-checklist |
| バージョン | **1.0.0** |
| 対象指示書 | `docs/instructions/M31-01-setplay-punish-and-oki.md` **v1.0.0** |
| 推奨モデル | **高精度レビュー**（**★★「9 件の UX 改善」と読ませないこと。⇒ 重いのは 1 件の欠陥と 1 件の設計判断である**） |
| 作成者・作成日 | 設計担当 Claude（設計卓・フェーズ4 期）/ 2026-09-07 |

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| 1.0.0 | 2026-09-07 | 初版（`D-773`。**★投入の合図と同じ手番で起こした**＝`D-736` の運用） |

---

## 0. レビュー担当への前置き

### 0.1 ★★本サブの重さは 9 件に均等ではない

**★★重いのは 2 つだけである。**

| # | 何が | なぜ重いか |
|---|---|---|
| **★★1** | **`materialize` の穴**（`materialize-bypasses-required-fields`） | **★UX の改善ではなく欠陥である。⇒ 確定反撃から生成した本登録コンボが、必須 4 欄が空のまま在りうる** |
| **★★2** | **`SD-006` の連動を入れると `VAL-C11` が実質死ぬ可能性** | **★鳴らなくなってもテストは緑のままである** |

**⇒ 残る 7 件は、この 2 つに比べて軽い。★レビューの重心をここへ置くこと。**

### 0.2 ★★最も見落としやすい形

**★★「連動が入って警告が鳴らなくなった」を「直った」と読むこと。**

**`SD-006`**（ノーゲージ版にチェック → DR 版へ連動）**と `VAL-C11`**（DR 版だけある → WARNING）**は向きが逆である。⇒ 連動が入ると「DR 版だけある」状態は手で作らない限り生まれない。**

**★★したがって `VAL-C11` は鳴らなくなりうる。⇒ それは「直った」ではなく「母集団が消えた」である。★製造がその区別を付けているかを見る。**

### 0.3 レビュー前の準備

- `docs/instructions/M31-01-setplay-punish-and-oki.md`（全文。**とくに §0.2 と §4**）
- `docs/design/06-validation.md` の **`VAL-C11`** ／ `VAL-C15` ／ 末尾の「`validations` と `warnings` の 2 経路」
- `docs/instructions/M31-overview.md` **§4.2〜§4.4**（`SD-006` の射程の決着と、違和感の候補）

---

## 1. 束 A — 段 1 の実測 5 件

| # | 見ること | 判定 |
|---|---|---|
| **★★1-1** | **`materialize` の穴を「壊れている」側で再現しているか**——**必須 4 欄が空の本登録コンボが実際に作れたことが示されているか** | **★★これが段 2 の陽性対照である。⇒ 無ければ「直った」の証拠が作れない** |
| **★★1-2** | **`VAL-C11` の発火が、シミー / 打撃重ね を含む全 attack_type で実測されているか** | **★1 つの attack_type だけで済ませていないか** |
| **1-3** | **`M31-overview` §2.2 の「2 件のずれ」に結果があるか** | **★見つからなかった場合は、そう書いてある形でよい**（`M-128` の運用） |
| **1-4** | **`P4M-023` が DB に及ぶかが判定されているか** | **★及ぶならマイグレを請求しているか。★自採番していないか**（`D-293`） |
| **★1-5** | **`M31-02` との交点**（`P4M-015` がキャラ選択の部品を使うか）**が実査されているか** | **★重なったら設計卓へ知らせたか** |

---

## 2. 束 B — `materialize` の穴（**★★本サブの中心**）

| # | 見ること | 判定 |
|---|---|---|
| **★★2-1** | **必須 4 欄の検証を通るようになったか** | **★段 1-1 の陽性対照と対で示されているか** |
| **★★2-2** | **既存データへの影響が実測されているか**——**必須 4 欄が空のまま在る既存行を編集・保存したときの挙動** | **★★件数 0 なら「0 件だった」と書いてある形でよい。⇒ 件数が出たなら、どう扱うかが報告にあるか** |
| **★★2-3** | **「直すと、いままで通っていた入力が通らなくなる」を測ってから直しているか** | **★★読解だけで「outcome は変わらない」と論じていないか。⇒ `M29-02` は probe を書いて実見してから進めた。同じ水準を求める** |
| **2-4** | **仮登録と本登録で扱いが違うなら、その違いが書かれているか** | **★`M24-13` の前例と食い違っていないか** |

---

## 3. 束 C — `SD-006` の連動 ＋ 起き攻めのバリデーション

| # | 見ること | 判定 |
|---|---|---|
| **★★3-1** | **連動の 3 挙動が別々のテストで示されているか**——**(a) ノーゲージ → DR に付く ／ (b) 逆向きは付かない ／ (c) 自動で付いたものを外せる** | **★1 本にまとめていないか。⇒ まとめると、どれが落ちたか分からない** |
| **★★3-2** | **「違和感」の正体が判定されているか**（候補 1＝`VAL-C11` ／ 候補 2＝2 経路 ／ どちらでもない） | **★★「どちらでもなかった」で終わっていてもよい。⇒ 無理に候補へ寄せていないか**（`M-128`） |
| **★★3-3** | **連動を入れた後の `VAL-C11` の母集団の変化が示されているか** | **★★§0.2。⇒ 「実質死んだ」なら、隠さずにそう書いてあるか** |
| **★★3-4** | **重大度と文言を変えていないか** | **★変える必要が出たなら、止めて請求しているか**（`D-293`。**`M29-02` が `VAL-C09` で同じ形を踏んでいる**） |
| **★3-5** | **射程外の 3 件**（項目数の不足 ／ 未検証の記録）**に手を出していないか** | **★`M27-02b` の 3 状態トグルで解消済みである** |

---

## 4. 束 D — セットプレイ ＋ 確定反撃 ＋ `custom_state`

| # | 見ること | 判定 |
|---|---|---|
| **4-1** | **`P4M-004` / `P4M-014`** | **★画面の是正。★`DES-005` の該当節と食い違っていないか** |
| **★4-2** | **`P4M-019`**（セットプレイ削除時の紐付け解除） | **★★論理削除であることを踏まえているか。⇒ 復元したときに紐付けが戻るか** |
| **★★4-3** | **`P4M-005`**（`setplay only` の活用）**と `SM-098`**（D リバの扱い） | **★★どちらも仕様が決まっていない。⇒ 案を出して設計卓の回答を得てから実装しているか。★勝手に決めていないか** |
| **4-4** | **`P4M-015`**（確定反撃の自キャラ既定値） | **★§1-5 の交点。⇒ `M31-02` と同じファイルへ入ったなら、知らせたか** |
| **★4-5** | **`P4M-023`**（`custom_state` 拡張） | **★セットプレイ側の要求から挙げているか。⇒ seed の都合から挙げていないか。★`M14` レーンと衝突しないか** |

---

## 5. 破壊確認（**★★2 件とも必須。★対象を名指しで見ること**）

| # | 何を | なぜ |
|---|---|---|
| **★★5-1** | **段 1-1 で作った「必須 4 欄が空の本登録コンボ」が、直した後は作れないこと** | **★★陽性対照と対になっていないと、「もともと作れなかった」と区別できない** |
| **★★5-2** | **`VAL-C11` が、連動を入れた後も鳴る経路が 1 つでも残っているか**——**残っていないなら「実質死んだ」と報告されていること** | **★★これが本サブ固有の破壊確認である。⇒ 「テストが緑」は、警告が生きている証拠にならない** |

---

## 6. テストの妥当性

| # | 見ること |
|---|---|
| 6-1 | **`go test ./...` ／ `cd web && pnpm test` を出力で判定しているか**（終了コードだけを見ていないか） |
| **★6-2** | **`make e2e-only P=<パターン>` を使っているか。⇒ `playwright` を直接叩いていないか**（`CLAUDE.md` §11） |
| **★6-3** | **連動のテストが「実装をそのまま写した」形になっていないか**——**判定式を書き写すのではなく、入力と出力で主張しているか** |
| 6-4 | **`P4M-019` の復元テストが、論理削除の実挙動に当たっているか** |

---

## 7. 重大な問題の判定基準（**完了承認を妨げる**）

| # | 内容 |
|---|---|
| **★★1** | **`materialize` の穴が塞がっていない、または陽性対照が無い** |
| **★★2** | **既存データへの影響を測らずに検証を効かせた** |
| **★★3** | **`VAL-C11` の母集団の変化が示されていない**（**⇒ 「鳴らなくなった」が「直った」として通る形になっている**） |
| **★★4** | **`VAL-C11` の重大度・文言を、請求せずに変えた** |
| **★5** | **`SM-098` / `P4M-005` を、設計卓の回答を得ずに実装した** |
| **★6** | **番号を自採番した**（CHANGE / マイグレ） |
| **★7** | **`followup-backlog.md` の本表を編集した**（`D-382`） |
| **★8** | **射程外の 3 件**（項目数・未検証の記録）**に手を出した** |

---

## 8. 軽微な問題の判定基準（**持ち越し許容**）

| # | 内容 |
|---|---|
| 1 | **`M31-overview` §2.2 の「2 件のずれ」が特定できなかった**（**★「特定できなかった」と書いてある形でよい**＝`M-128`） |
| 2 | **「違和感」がどちらの候補でもなかった**（**★同上**） |
| 3 | 画面の細部（余白・並び）の好み |
| 4 | **`P4M-023` の対象キャラが網羅されていない**（**★セットプレイ側の要求から挙がったぶんで足りる**） |

---

## 9. レビュー完了の判定

- **束 A〜D の全項目に判定が付いている**
- **★★破壊確認 2 件が実測で示されている**
- **重大の判定基準 8 件のいずれにも当たらない**
- **★指摘は「重大 / 中 / 低」で仕分けし、根拠を実測で示す**
- **★再レビューの往復は 2 回まで**（`CLAUDE.md` §9 の停止規律。**⇒ 上限に達したら `followup-backlog.md` §J へ必須 5 フィールドで記録して停止する**）

---

*以上、M31-01 レビューチェックリスト **v1.0.0**。* **★★本サブの中心は 1 件の欠陥と 1 件の設計判断である**（§0.1）**。⇒ 9 件を均等にレビューすると、重心がずれる。** **★★最も見落としやすいのは「警告が鳴らなくなった」を「直った」と読むことである**（§0.2）**——テストは緑のままである。**
