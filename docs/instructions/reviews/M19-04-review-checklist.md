# M19-04 レビューチェックリスト

| 項目 | 内容 |
|------|------|
| 文書ID | M19-04-REVIEW |
| 対象指示書 | `docs/instructions/M19-04-frame-cost-columns.md` **v1.1.0**（フレーム費用モデルの列追加） |
| 対象指示書ID | M19-04 |
| CHANGE | **CHANGE-091**（通知書 v1.1.0） |
| レビューモデル | Sonnet 4.6（model-allocation。実使用は開発者判断） |
| バージョン | **1.2.1** |
| 作成者・作成日 | 親チャット（設計卓）/ 2026-07-31 |

> **文書ID ⇄ 実パスの正は `docs/handover/docs-map.md`（自動生成）。**

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| **1.2.1** | **2026-08-07** | **メタ表へ `文書ID` 行を追加＝ボード **D-203**。`docs-map` 未収録の是正**（`scripts/generate-docs-map.sh` は `文書ID` 行から §1 を抽出するため、行が無い文書は載らない）。**本文の変更なし**（反映係・設計卓の依頼 Part 3 項目 6）。**対象指示書の版は変えていない。** |
| **1.2.0** | **2026-08-02** | **golden の stem 数を実測値へ是正**（束ねた非ブロッカー一式 §5・ボード D-131(c)）。**§4・§8.5・§9 の「golden 4 stem」を「7 stem」へ**——manon の seed で 000045 / 000046 / 000047 が追加されており、4 は 2026-07-31 時点の旧値である。**内訳は着手時に実査する**（本改訂は件数のみを是正した）。**★§9 末尾の「CustomHeader を直すと golden 4 stem が全部変わる」の 1 件も 7 stem へ是正した**（差し戻し回答パッチ PA-A11。初版パッチは「§4・§8.5・§9」と**節を名指しして列挙**したが `golden 4 stem` の全文検索をしておらず、**この 1 件が漏れていた**＝**名指しの列挙は網羅の証明ではない**・D-146）。**本ファイルに `golden 4 stem` は残っていない**（本更新履歴の引用を除く）。 |
| **1.1.0** | **2026-08-01** | 対象指示書を **v1.1.0** へ追従。**§3.5 を新設**（`chain_cancel_total` の投入＝実測資料が正本・写像の検査・「total−4」禁止）。 |
| 1.0.0 | 2026-07-31 | 初版。 |

---

## 0. レビュー担当への前置き

### 0.1 準備

- 必読: 指示書 M19-04 v1.0.0 **全節**（**とくに §2.3 停止条件・§4.4 機械 backfill・§4.9 否定形確認**）、`CHANGE-091-notification.md` v1.1.0 **§2・§3**、`m18-m19-contract.md` **§2（F-1〜F-6）**、DES-003 §3.3／§3.15／§3.17／§3.19、`architecture-patterns`（FK 強制は接続単位）。
- **Plan Mode 着手前確認の確認（必須）**: 指示書 §3.3 の **10 項目すべて**に質問書＋回答が残っているか。**とくに 1（マイグレ末尾の実査）・2（母数の再実査）・3（`characters` の実在確認）**。**3 が提示されずに実装へ進んでいたらその時点で重大**（§9）。

### 0.2 基本姿勢

- **本サブは「列を足して値を入れる」までで、値の消費は M19-05。** **新列がエンジン・API に露出していたら重大。**
- **「触らない」ことが仕様**: `punishfinder` / `setplay` / seedgen の既存生成関数 / `startup`・`total` の既存値 / `is_derived`・`is_aerial` のロジック。**触っていたら重大。**
- **「エラーにならない失敗」を疑う**: 本サブの主要な失敗モードは**例外ではなくサイレント no-op**である。**件数で固定されていない投入は、投入されていないのと同じ扱いで見る。**

### 0.3 報告フォーマット

- 各節ごとに「OK / 重大（§9）/ 軽微（§10）/ 質問（§11）」で報告。

---

## 1. 【最重要】サイレント no-op の排除

- [ ] **DDL 適用後の各新列の値の分布**が、**件数を固定するテスト**で押さえられているか。
- [ ] **機械 backfill の投入件数**（`standalone` / `through` / `fastest_unreachable=true`）が**それぞれ数で固定**されているか。**「0 件でも成功する」テストになっていないか。**
- [ ] **ザンギエフ 3 行の投入件数**が固定されているか。**`INSERT … FROM characters c WHERE c.code='zangief'` 型を使っている場合、親行が無ければ 0 行投入でマイグレは成功する**——この経路を通るなら**件数固定は必須**。
- [ ] **c_viper / dhalsim の 18 行が `startup_basis='unknown'` のまま**であることが固定されているか（機械 backfill の除外が効いているか）。
- [ ] **down 適用後に元の状態へ戻る**ことが確認されているか。

## 2. 【最重要】確定反撃サーチへの非干渉（契約 F-1）

- [ ] **`internal/service/punishfinder/` の diff が 0** か（**1 行でも変更があれば重大**）。
- [ ] canary テストで次がすべて固定されているか——**候補件数**・**3 レーン境界**・**移動 system move 9 種の除外**・**`is_aerial=1` の完全除外**・**`JumpSlack`**・**ガードタブ 0 件が正常**・**`registeredCombos`**・**レシピ行**。
- [ ] **ザンギエフ 3 行による増分がある場合、増分の内訳が明示**されているか（「数字だけ合わせた」形になっていないか）。**候補差分を再計測した証跡があるか。**
- [ ] **判定値が `const` に置かれ、テスト名に数字が埋め込まれていない**か（`Existing21PlusNeutral2` 型の命名になっていないか）。
- [ ] **`internal/service/setplay/` の diff が 0** か。

## 3. 機械 backfill の条件（重点）

- [ ] **`fastest_unreachable` が部分一致（`LIKE '%jumping_%'`）**になっているか。**前方一致（`LIKE 'jumping_%'` / `HasPrefix`）だったら重大**——`neutral_jumping_heavy_kick`（juri / ken）を取り落とす。**M18-03c で同じ 2 行を取り落とす誤りを是正した経緯がある。**
- [ ] `startup_basis = 'standalone'` の条件が **`is_derived = 0` かつ c_viper / dhalsim を除外**しているか。
- [ ] `startup_basis = 'through'` の条件が **`category = 'rush_variant'`** か。
- [ ] **既存値の上書きが起きていないか**（`startup` / `total` を 1 行も書き換えていないこと。**スナップショット比較で示されているか**）。

## 3.5 【最重要】`chain_cancel_total` の投入（実測資料からの転記）

- [ ] **値の一次源が `character_data/chain-cancel-measurements.md` の「確定値」表**か。**CSV から読んでいないか**（CSV の当該列は空欄のはず）。
- [ ] **`character_data/*.csv` の `chain_cancel_total` 列に値を転記していない**か。**転記していたら重大**（同じ値が 2 か所に存在する）。
- [ ] **技名 → `move_code` の写像表**が Plan Mode で提示され、**全行が一意に決まっている**か。**曖昧なまま当てていないか。**
- [ ] **投入範囲が seed 済み 11 キャラに限られている**か。**未 seed キャラ（エレナ / JP / ジェイミー / ルーク / ベガ / ラシード）の行を投入していない**か。
- [ ] **ロースターの非対称が正しく扱われている**か——**リリーは 4 技**（`Stand LK` を含む）／**ザンギエフは通常版・連打版の各 3 技＝6 行**。
- [ ] **投入件数が固定するテストで押さえられている**か。**写像を誤って別の技に値が入っても、それはエラーにならない。**
- [ ] **資料に無い行を「total − 4」等の規則で埋めていない**か。**埋めていたら重大**——実測資料 §1 が、**同一単発値が最大 3 通りに割れる**ことを多重検算つきで示している。

## 4. CSV 読取り層と golden（重点）

- [ ] **`internal/seedgen` の生成側（`writeMovesInsert` / `writeAliasInsert` / `rawDataSQL` / `CustomHeader` 等）が無改変**か。**触っていたら重大。**
- [ ] 変更が **`csvColumns` / `parseRow` / `MoveRow` / `testHeader` / テストの行リテラル**に限られているか（＝I/O 境界のみ）。
- [ ] **golden 7 stem が green** か（**2026-08-02 実測。「4 stem（000026 / 000030 / 000034 / 000035）」は 2026-07-31 時点の旧値**——manon の seed で 000045 / 000046 / 000047 が追加され実測は 7 stem である。**内訳は着手時に実査すること**）。**`go run ./cmd/seedgen -check` が OK** か。**この green が「変換規則に触れていない」ことの機械的証明である。**
- [ ] **`character_data/*.csv` 全ファイルのヘッダが完全同一**で、**既存 20 列の順序・値が不変**か。
- [ ] **`move_code` を整形・短縮・リネームしていない**か。
- [ ] **`zangief.csv` に行を足していない**か（**足していたら重大**＝golden が壊れる／同じ 3 行が 2 か所に存在する）。

## 5. 新表とスキーマ

- [ ] `move_derivations` が **`ON DELETE CASCADE` に依存していない**か（**`ON UPDATE CASCADE` ＋ 明示削除／明示再ポイントの併用**）。
- [ ] **親が `moves`（1 段）であることを前提にした実装**になっているか。**`combo_setup_results`（親＝`combo_setups`・1 段深い）の流儀を誤って流用していないか。**
- [ ] `moves` の識別キー変更経路の有無が **Plan Mode で実査**され、存在するならそこに乗っているか。
- [ ] 新列の型・NOT NULL・DEFAULT が指示書 §4.1 のとおりか。**`chain_cancel_total` に NOT NULL や DEFAULT 0 を付けていないか**（**非 NULL であること自体が意味を持つ**ため、0 を入れると別の意味になる）。
- [ ] **DES-006 に VAL を足していない**か（値域の正典は DES-003 の表定義）。

## 6. rush 生成経路

- [ ] `insertRushVariantSQL` に **`startup_basis = 'through'` が明示追加**されているか。**放置すると backfill 後に生成される rush 行だけが `unknown` のまま残る**（静かに壊れる型）。
- [ ] `chain_cancel_total`（NULL）と `fastest_unreachable`（false）は **DEFAULT のまま**か。
- [ ] rush 生成後の行の新列値を**固定するテスト**があるか。

## 7. 非露出（契約 F-3）

- [ ] **新列がエンジン・既存 API のどこにも露出していない**か（DTO・レスポンス・SELECT の列挙・model 構造体のシリアライズ）。
- [ ] **既存の golden・期待 SQL が壊れていない**か。
- [ ] 画面18（moves 編集）の DTO・SET 句ホワイトリストに**新列が入っていない**か。

## 8. 人手判断の一覧

- [ ] 一覧が **3 系統それぞれ**出ているか（`startup_basis` / `fastest_unreachable` の B 型 / `chain_cancel_total`）。
- [ ] **判断に必要な列が添えられている**か——`character_code` / `move_code` / `name_ja` / `category` / `is_derived` / `is_aerial` / `command` / `startup` / `total` / **親技** / `condition_ja`。**件数だけの報告になっていないか。**
- [ ] **キャラクター順・`move_code` 順で安定ソート**されているか（差分が取れる形か）。
- [ ] `chain_cancel_total` の絞り込み条件が **Plan Mode で提案され、開発者の確認を得ている**か。

## 8.5 【Phase 2 を実施した場合のみ】

- [ ] seedgen の新モードが**新規ファイル**に作られ、**既存の生成関数に触れていない**か。
- [ ] **「値を持つ行だけを列挙して UPDATE」**型か。**CSV で空欄の行を UPDATE 対象に含めていない**か。**`COALESCE` マージを使っていないか**（前例が無い）。
- [ ] **新 stem に golden** が付いているか（**note 定数・CSV 再読込・生成関数呼出・byte 比較**の 4 点）。**再生成コマンドがテスト関数のコメントに明記**されているか。
- [ ] **既存 golden 7 stem が無影響**か（**2026-08-02 実測。4 は旧値**）。

## 9. ドキュメント・否定形確認

- [ ] **§4.9 の否定形確認 4 件**が実施され、結果が報告されているか。**走査対象に 3 系統（本番コード／テスト資産／設計文書・指示書）すべて**が含まれているか。**本番コードだけを見て「直っている」と報告していないか。**
- [ ] **消費したマイグレ連番が完了報告に明記**されているか（後続サブの連番が動く）。
- [ ] **製造が DES を直接編集していない**か。
- [ ] **設計伝達レポート**が作成され、**①製造が独自に確定した実装仕様 ②契約・設計に反する独自判断 ③製造の判断 ④設計担当が未把握の残課題**に絞られているか。
- [ ] 禁則表現・簡体字・「起き攻け」誤字・「DR」略記の grep 除去。

---

## 9. 重大な問題（完了承認を妨げる）

- **投入件数を固定するテストが無い**（サイレント no-op を検出できない）。
- Plan Mode 10 項目のいずれかが未確認のまま実装（推測実装）。**とくに 1・2・3 を飛ばした。**
- **マイグレ末尾が想定と違ったのに自採番して進めた**（再請求していない）。
- **`internal/service/punishfinder/` を変更した**。**確定反撃サーチの候補集合が、ザンギエフ 3 行の増分以外の理由で変わった**。
- **`internal/service/setplay/` を変更した。**
- **`moves.startup` / `moves.total` の既存値を書き換えた。**
- **`is_derived` / `is_aerial` の値・ロジックを変更した。**
- **`internal/seedgen` の既存生成関数を変更した**。**golden 7 stem のいずれかが red のまま**。**`-check` が NG。**
- **`fastest_unreachable` を前方一致で backfill した**（`neutral_jumping_heavy_kick` の取り落とし）。
- **c_viper / dhalsim を機械 backfill から除外していない。**
- **`zangief.csv` に行を足した。**
- **`chain_cancel_total` に NOT NULL または DEFAULT 0 を付けた。**
- **新列をエンジン・API に露出させた。**
- **`move_derivations` が `ON DELETE CASCADE` に依存している**（明示処理の併用が無い）。
- **`rush` 生成経路に `startup_basis` を明示していない。**
- **人手判断の一覧が件数だけで、判断に必要な列が無い。**
- **人手が要る値を推測で埋めた**（実測・開発者記入を経ずに値を入れた）。
- **`chain_cancel_total` を「total − 4」等の規則で埋めた。**
- **`chain_cancel_total` を CSV へ転記した**（値が 2 か所に存在する）。
- **技名 → `move_code` の写像が曖昧なまま当てた**／**未 seed キャラの行を投入した。**
- **`internal/seedgen` の `CustomHeader`（生成ヘッダの投入元表記）を直した**（既知の誤りだが**直すと golden 7 stem が全部変わる**）。
- 既存マイグレを改変した。down 整合が無い。`dbtest.Setup` を破壊した。
- **製造が DES を直接編集した。**

## 10. 軽微な問題（持ち越し許容）

- マイグレの分割数（**消費連番が明記されていれば可**）。
- 一覧の書式・ファイル形式（md / csv）。
- テストファイルの配置と命名（**既存ヘルパを再利用し、新規ヘルパを作っていなければ可**）。
- **Phase 2 の未実施**（**未記入の件数と対象が明記され、後続サブへ申し送られていれば許容**）。
- E2E の既知 flakiness による 1 件の落ち（**単独実行で green であることと、その判定根拠が示されていれば許容**）。

## 11. 質問・確認事項のフォーマット

- 「指示書 §X.X / 設計書 DES-00N（実パス）§Y に対し、実装が Z。意図確認したい」の形で根拠節を併記して設計担当へ。

## 12. レビュー完了の判定

- §1〜§9 が全て OK、§9 重大ゼロ、Plan Mode 10 項目（**とくに 1・2・3**）が揃っている。§10 軽微は持ち越し可。

---

*以上、M19-04 レビューチェックリスト v1.0.0。配置 `docs/instructions/reviews/M19-04-review-checklist.md`。重点＝**サイレント no-op の排除**・**確定反撃サーチへの非干渉**・**`fastest_unreachable` の部分一致**・**seedgen 生成側の無改変（golden green が証明）**・**人手が要る値を推測で埋めていないこと**。*
