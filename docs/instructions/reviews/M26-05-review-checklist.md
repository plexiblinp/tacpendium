# M26-05 レビューチェックリスト（ウィザードの LAN 公開に警告と同意を入れる）

| 項目 | 内容 |
|------|------|
| 文書ID | M26-05-REVIEW-CHECKLIST |
| バージョン | **1.0.0**（2026-09-12・発行） |
| 対象指示書 | `docs/instructions/M26-05-wizard-lan-consent-parity.md` **v1.2.0** |
| 作成者 | 設計担当 Claude（設計卓） |
| 位置づけ | **レビュー担当の主入力**（`D-736`＝チェックリストは着手承認の手番で設計卓が起こす） |

---

## 0. ★★レビュー担当への前置き

**本サブはセキュリティのゲートを作る。⇒ 「動いている」ではなく「抜けられない」を見ること。**

**★★最も起きやすい不合格は「チェックボックスが在る」で通してしまうことである。⇒ 要件は*チェックするまで押せない*である。★実際に押してみた形跡を求めること。**

**★守りの手本は既に在る**（`web/src/features/config/LanModeConfirmDialog.tsx` / `SettingsSectionNetwork.test.tsx:106`）**。⇒ 手本と食い違っていたら、食い違いに理由が在るかを問うこと。**

---

## 1. 束 A — ★★★ゲートとして機能しているか（**最初に見ること**）

| # | 観点 |
|---|---|
| **A-1** | **★★★Step 7 の「あとにする」は、同意チェックを入れるまで押せないか。** **⇒ `disabled` が実際に効いているか。★「チェックボックスを描画した」だけで終わっていないか**（指示書 §4.1） |
| **A-2** | **★★同意なしで通り抜ける裏道が無いか。** **⇒ Enter キー、フォーム submit、戻る→進む、URL 直叩き。★1 つでも通れば不合格である** |
| **A-3** | **★★戻ってから再訪すると、同意はリセットされているか**（指示書 §4.2 ／ 手本＝`LanModeConfirmDialog.tsx:50-55`）**。⇒ 残っていると 2 度目以降は無防備である** |
| **A-4** | **★Step 5 の警告が「何をされうるか」を述べているか。** **⇒ 「アクセスできます」だけでは不合格である**（現状の `lanHint` がまさにそれで、不足と判定された） |
| **A-5** | **★Step 5 の警告が「届かない範囲」も述べているか**（別回線からは開けない）**。⇒ 恐怖だけ与えて判断材料を与えていないか** |

---

## 2. 束 B — ★★★射程を越えていないか

| # | 観点 |
|---|---|
| **B-1** | **★★★`web/src/features/config/LanModeConfirmDialog.tsx` の差分が 0 行か**（指示書 §3-2）**。⇒ 共有部品化は採らないと決めた**（`D-826`。方式 (i) 不採用） |
| **B-2** | **★同ファイルの参照元が `SettingsSectionNetwork` の 1 本のままか**（実測せよ） |
| **B-3** | **★★`totalSteps` の式（`lanEnabled ? 8 : 6`）と `LAN_ONLY_STEPS = 2` が変わっていないか**（指示書 §3-3。方式 (iii) 不採用） |
| **B-4** | **★★★パスワードを必須にしていないか。「あとにする」の導線そのものを消していないか**（`D-396` (1)。**⇒ 詰みを原理的に消すための裁定であり、覆せない**） |
| **B-5** | **★設定画面側（`SettingsSectionNetwork` / その文言）の挙動が変わっていないか**（指示書 §3-4） |
| **B-6** | **★API / スキーマ / マイグレ / `config.toml` に差分が無いか**（指示書 §3-5） |
| **B-7** | **★既存 i18n キーのリネーム・削除をしていないか**（指示書 §3-8。**足すだけのはずである**） |

---

## 3. 束 C — ★★ずれ止めが効いているか（**指示書 §2.3＝本サブで一番大事な段**）

| # | 観点 |
|---|---|
| **C-1** | **★★★`SettingsSectionNetwork.test.tsx:106` と*同じ主張*のテストが、ウィザード側にも在るか。** **⇒ これがずれ止めの本体である。★無ければ、2 か所の守りは次の改修で必ずずれる** |
| **C-2** | **★★意味が同じ文言でキーを共有しているか**（`consentBody` / `consentCheck`）**。⇒ 同じ意味なのに新しいキーを立てていないか。★立てたなら、なぜ既存で足りなかったかの説明が在るか** |
| **C-3** | **★★意味が違うところで流用していないか。** **⇒ `enableConfirmBodyNoPassword` は「この設定を ON にすると」で始まり「いまはパスワードが設定されていません」と述べる。★Step 5 ではまだ Step 7 が来ていないので後者は事実として早い。⇒ 流用していたら不合格である** |
| **C-4** | **★`ja.json` と `en.json` が同じキー集合を持つか**（指示書 §4.5） |

---

## 4. 束 D — ★★分岐が漏れていないか

| # | 観点 |
|---|---|
| **D-1** | **★★LAN 無効の経路に、警告も同意も 1 つも出ていないか**（指示書 §4.3）**。⇒ 公開していない人に「誰でも変更できます」と告げていないか** |
| **D-2** | **★★パスワードを設定して進む経路に、同意を要求していないか**（指示書 §2.2-3）**。⇒ 要求していると同意が儀式になり、読まれなくなる** |
| **D-3** | **★Step 7 が `step === 7 && lanEnabled` で出る現行の条件を保っているか** |

---

## 5. 束 E — ★テストと検査

| # | 観点 |
|---|---|
| **E-1** | **★★★ウィザードを Step 1 から通す E2E が 1 本在るか**（指示書 §2.4-3）**。⇒ 実害は部品ではなく*通り道*に在った。★単体テストだけで閉じていたら不足である** |
| **E-2** | **★`Step05Network` のテストが在るか**（着手前は 0 本であった） |
| **E-3** | **★★`make e2e` を*全数*回しているか**（`SUPP-001` §5.5.7 (i)）**。⇒ 的撃ちだけなら不足である。★既存 296 本が緑か** |
| **E-4** | **★`check-browser-storage-keys.sh` が緑か。⇒ 同意の状態をブラウザストレージへ置いていないか**（置くと A-3 のリセット要件と両立しない。`CLAUDE.md` §10.X の台帳にも無い） |
| **E-5** | **★`check-import-order.sh` がベースラインから増えていないか。★レーン内でベースラインを下げていないか**（`D-388`） |
| **E-6** | **★`eslint-disable` / `console.log` を足していないか**（`CLAUDE.md` §10） |

---

## 6. ★★見なくてよいもの

| # | 何を | なぜ |
|---|---|---|
| 1 | **設定画面側の文言の良し悪し** | **★既に開発者の目を通っている。⇒ 本サブの射程外**（指示書 §4.4） |
| 2 | **`A6` の他の実査項目** | **★開発者が「実装を直す」と裁定したのは本件だけである**（指示書 §3-7） |
| 3 | **`M26-04` の完了報告・レポートの記述** | **`D-274` (3)＝完了したサブの文書は書き換えない** |
| 4 | **CHANGE の起票要否** | **★`CHANGE-185` は設計卓が先行して反映済みである**（指示書 §0.2）**。⇒ 製造は起票しない**（`D-293`） |

---

*以上、M26-05 レビューチェックリスト **v1.0.0**。* **★★束 A が本体である。⇒ 「チェックボックスが在る」で通さないこと。** **★★もう 1 つは束 C-1 である**——**同じ守りが 2 か所になる以上、ずれ止めが無ければ本サブは次の改修で無効化される。**
