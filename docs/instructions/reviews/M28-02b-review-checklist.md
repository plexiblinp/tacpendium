# M28-02b レビューチェックリスト: ゲーム更新の影響コンボを見せる画面

| 項目 | 内容 |
|------|------|
| 文書ID | M28-02b-review-checklist |
| バージョン | **1.0.0** |
| 対象指示書 | `docs/instructions/M28-02b-game-update-impact-screen.md` **v1.2.0** |
| 推奨モデル | **標準レビュー**（**★★成果物の中心が設計とモックであり、コード量は少ないか 0 である。⇒ 見るのは「決めたか」と「決めた理由が書いてあるか」である**） |
| 作成者・作成日 | 設計担当 Claude（設計卓・フェーズ4 期）/ 2026-09-06 |

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| 1.0.0 | 2026-09-06 | 初版（`D-753`。**★投入の合図と同じ手番で起こした**＝`D-736` の運用） |

---

## 0. レビュー担当への前置き

### 0.1 レビュー前の準備

1. **指示書 v1.2.0 を通読する**（とくに §0.1 の必達成果物 3 つ、§0.5 の含み 10 件、§4 の危険 7 件）。
2. **`docs/change-notes/CHANGE-159-notification.md` を読む**——**★API の形の正本はここである。`docs/progress/` の完了報告ではない。**
3. **`docs/progress/M28-02b-mock/` の HTML を実際にブラウザで開く**（**★これが最大の成果物である**）。

### 0.2 ★★本サブは「コードが少ない」ことが正常である

**必達は設計までである**（`D-668`）。**⇒ 実装が 0 行でも、§0.1 の 3 つが揃っていれば完了である。**

**★★「実装していないこと」を欠陥として挙げないこと。** **⇒ 挙げるべきは「§2.9 のどの条件が欠けたかが書かれていないこと」である。**

### 0.3 ★★重大でないもの（**誤判定しないこと**）

| # | 内容 |
|---|---|
| 1 | **モックの見た目が粗いこと**（指示書 §2.1-3＝**作り込みすぎないこと**が明示指示である） |
| 2 | **実装へ進まなかったこと**（§0.2） |
| 3 | **`check-md-emphasis.sh` / `check-import-order.sh` のベースラインが動いていないこと**（**★自サブ由来でないドリフトを床にしない**＝`M28-01` の教訓 13） |
| 4 | **語の選び方が `M29-01` の統一結果と違うこと**（**★`M29-01` はまだ走っていない。⇒ 本サブに要るのは、自分が作る 4 面の中で語が 1 つに保たれていることだけである**） |

### 0.4 ★★本サブに固有の危険（**最優先で見る**）

| # | 危険 | なぜ緑のまま通りうるか |
|---|---|---|
| **★★1** | **「幅」を埋めずに問いへ差し戻した** | **★`D-742` で 4 問はすべて決着している。⇒ 「決められないので開発者に聞く」で返してきたら、それは仕事をしていない。★★ただし §7 の返却事項 6 件は返してよい**——**この 2 つを混同しないこと** |
| **★★2** | **「不採用に近い」を「永続しない」と読んだ** | **★実物の「不採用」は 2 つあり、どちらとも違う**（指示書 §2.5-2）**。⇒ 本サブの「確認した」は DB へ書き、進める前の値が残らない。★モックが「一時的に隠す」形になっていたら赤** |
| **★★3** | **一括の「確認した」を、API が無いのに設計した** | **★`M28-02a` は 1 件ずつしか作っていない**（`CHANGE-159` §1.1）**。⇒ 一括を採るなら経路の追加を請求しなければならない。★請求せずに一括を描いていたら赤** |
| **★★4** | **「初回は 0 件」を確かめずに「動いた」と書いた** | **★`D-725` により既存コンボの基準は最新であり、初回に出る件数は 0 件である。⇒ 画面を開いても何も出ない。★★「0 件だったから正しい」は証拠にならない** |
| **★★5** | **取得失敗を「0 件」として扱った** | **★先例＝`combo-list-setup-count-hides-fetch-failure`。⇒ ボタンが黙って消える。★`affectedByGameUpdate` は `omitempty` を付けていないので `false` は必ず載る。「キーが無い」は「判定していない」であり `false` とは別である** |

---

## 1. 束 A — 必達成果物の 3 つ（**最重要**）

| # | 見るところ |
|---|---|
| **★★1** | **モック HTML が `docs/progress/M28-02b-mock/` に在り、4 つの面を含むか**〔起動時告知 ／ 一覧のボタン ／ 専用画面 ／ コンボ詳細の 1 行〕 |
| **★★2** | **「影響可能性あり」の見せ方の案が 2〜3 並んでいるか**（指示書 §2.1-2。**★開発者が選ぶための材料である。⇒ 1 案しか無ければ赤**） |
| **★★3** | **画面設計の記述が `DES-005` へ入れられる形になっているか**（**★「こういう画面にします」ではなく「何を出し、何を出さないか」が書かれているか**） |
| **★★4** | **CHANGE 請求が在るか**（宛先と、その節に何を書くか）。**★製造が `docs/design/` を編集していないこと**（`CLAUDE.md` §8） |
| **★5** | **モックが実装の証拠として提示されていないか**（指示書 §2.1-4） |

---

## 2. 束 B — 埋めるべき「幅」3 つ

**★★`D-742` の決着には幅が 3 つ残っている。⇒ 埋めたことと、埋めた理由が報告に在るか。**

| # | 幅 | 見るところ |
|---|---|---|
| **★★1** | **告知の「消した」の保存先**（§2.2-4） | **3 候補**〔サーバ側ファイル ／ ブラウザストレージ ／ DB 列〕**から選び、理由が書かれているか。★★ブラウザストレージを採るなら、`web/CLAUDE.md` §1 の台帳への追記を先に請求しているか**（`CLAUDE.md` §10.X。**★未記載のキーを使っていたら `check-browser-storage-keys.sh` が赤**）。**★DB 列を採るならマイグレを請求しているか**（`D-293`） |
| **★★2** | **「確認した」の粒度と取り消し**（§2.5-3） | **一括を採るなら、確認ダイアログか 1 世代の保持を同時に決めているか。★★「一括を出すが取り消しは無い」で終わっていたら赤**——**誤爆したら戻せない** |
| **★★3** | **「影響可能性あり」の見せ方**（§2.7） | **★語が「破綻」になっていないか**（`FR307` 整合）**／ ★色だけで表していないか ／ ★★「影響可能性あり」と「未確認」を 2 つの語にしていないか**（**★語を割ると `M29-01` の仕事が増える**） |

---

## 3. 束 C — `M28-02a` の as-built との整合

| # | 見るところ |
|---|---|
| **★★1** | **一括の API が無いことを踏まえているか**（§0.4-3） |
| **★★2** | **楽観排他の分岐を画面に作っていないか**（**★`acknowledge-version` は `version` を受けず 409 が無い。⇒ 競合の分岐は不要である**） |
| **★★3** | **ゴミ箱の行が「確認した」にできないこと**（404）**を踏まえているか。★仮登録は対象に含む** |
| **★4** | **`updated_at` が進むことに触れているか**（**★更新日時順の一覧では並びが動く**） |
| **★★5** | **`baselineVersion` をエクスポートへ出していないか**（**★`CHANGE-159` §6 で CSV には載せないと決まっている。⇒ 出すなら列契約に及ぶ**） |
| **★6** | **コンボ詳細の 1 行が本当に 1 面かを実査したか**（**★`drive_damage` の符号表示は 3 面が 1 本を共有する先例がある**＝指示書 §2.6-2） |

---

## 4. 束 D — 実装まで進んだ場合のみ

**★進んでいなければ本束は飛ばす。⇒ 飛ばしたことを報告に書く。**

| # | 見るところ |
|---|---|
| **★★1** | **§2.9 の 3 条件がすべて満たされていたか**（**★モックの承認 ／ `M28-02a` の完了 ／ 3 つの決めごとの答え**） |
| **★★2** | **判定条件を画面側で書き直していないか**（指示書 §0.3。**★2 か所に書くと片方だけ直されて静かにずれる**） |
| **★★3** | **一覧に影響コンボの軸を常設していないか**（**★開発者が明確に否定した**＝「一覧に常に 1 軸増えると邪魔」） |
| **★4** | **`docs/progress/progress-log.md` へ追記されているか**（`CLAUDE.md` §8） |

---

## 5. テストの妥当性（**実装まで進んだ場合のみ**）

| # | 見るところ |
|---|---|
| **★★1** | **人工的に古い基準を作ってから流しているか**（**★初回は 0 件。⇒ 素のデータでは何も検証できない**） |
| **★★2** | **件数の取得失敗が `0` 件と区別されることを主張するテストが在るか** |
| **★★3** | **実 `ja.json` を引く `t` で書かれているか**（**★キーをそのまま返すモックでは文面の主張が空振りする**＝`M23-04` 教訓 2） |
| **★4** | **E2E は `make e2e-only P=<パターン>` で絞り込んでいるか**（**★`playwright` を直接叩くと `PW_EXECUTABLE_PATH` が渡らず UI を使う spec が全滅する**＝`D-599`） |

---

## 6. 破壊確認（**★実装まで進んだ場合は必須。進んでいなければ不要**）

| # | 壊すもの | 期待 |
|---|---|---|
| **★★1** | **画面を開いただけで基準が進む形にしてみる** | **★破壊確認 4 が赤になること。⇒ 進むと利用者が見る前に消える** |
| **★2** | **件数の取得を失敗させる** | **ボタンが「0 件で消える」のではなく、失敗として出るか** |

---

## 7. 重大な問題の判定基準（完了承認を妨げる）

| # | 内容 |
|---|---|
| **★★1** | **モック HTML が無い、または 4 面のいずれかが欠けている** |
| **★★2** | **「影響可能性あり」の案が 1 つしか無い**（開発者が選べない） |
| **★★3** | **§2 の「幅」3 つのいずれかが、選択も理由も無いまま残っている** |
| **★★4** | **一括の「確認した」を、API の追加を請求せずに設計している** |
| **★★5** | **「不採用に近い」を「一時的に隠すだけ」と読んで設計している** |
| **★★6** | **「初回は 0 件」を確かめずに「動作を確認した」と書いている** |
| **★7** | **`docs/design/` を編集している**（`CLAUDE.md` §8）／ **`followup-backlog.md` の §J 以外を編集している**（`D-382`） |
| **★8** | **CHANGE 番号・マイグレ連番を自採番している**（`D-293`） |

---

## 8. 軽微な問題の判定基準（持ち越し許容）

| # | 内容 |
|---|---|
| 1 | モックの配色・余白 |
| 2 | 専用画面のルート名の趣味 |
| 3 | 英語ラベルの語選び（**★日本語が決まっていれば足りる**） |
| 4 | **§7 の返却事項に対する設計卓の暫定案と、製造の案が違うこと**（**★理由が報告に在る場合に限り軽微とする**） |

---

## 9. レビュー完了の判定

1. **§7 の重大が 0 件である。**
2. **§1 の 4 つがすべて在る。**
3. **§2 の幅 3 つに、選択と理由が在る。**
4. **実装まで進んだ場合は §4 / §5 / §6 が満たされている。進んでいない場合は、§2.9 のどの条件が欠けたかが書かれている。**

### 9.1 ★本チェックリスト自身の検査（発行前に設計担当が行う）

- [x] **対象指示書の版数と一致している**（v1.2.0）
- [x] **`bash scripts/check-instruction-format.sh` が緑**
- [x] **`bash scripts/check-md-emphasis.sh` が緑**

---

*以上、M28-02b レビューチェックリスト **v1.0.0**。* **★★本サブでレビューが最も見落としやすいのは「コードが無いから見るものが無い」と読むことである。** **★見るのは「決めたか」と「決めた理由が書いてあるか」である。⇒ 幅を埋めずに問いへ差し戻した報告は、緑に見えて仕事をしていない。** **★★もう 1 つは「動かして 0 件だった」を正しさの証拠として受け取ることである**（§0.4-4）**——初回は必ず 0 件である。**
