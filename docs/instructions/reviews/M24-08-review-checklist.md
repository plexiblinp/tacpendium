# M24-08 レビューチェックリスト: `queryKey` の統一 ＋ M24 の繰越 4 件

| 項目 | 内容 |
|------|------|
| 文書ID | M24-08-review-checklist |
| バージョン | 1.0.0 |
| 対象指示書 | `docs/instructions/M24-08-querykey-and-carryover.md` **v1.0.0** |
| 推奨モデル | **高精度レビュー**（**★★契約が 5 件当たっている。しかも「静かに壊れる」形が本サブの中心である**） |
| 作成者・作成日 | 設計担当 Claude（設計卓・M24 期）/ 2026-08-30 |

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| 1.0.0 | 2026-08-30 | 初版（**D-619**） |

---

## 0. レビュー担当への前置き

### 0.1 レビュー前の準備

1. 指示書 `M24-08-querykey-and-carryover.md` **v1.0.0** を通読する。
2. `CHANGE-148` 通知書を読む。
3. **`m20-contract` §5 ／ `m21-contract` §5**（**契約 F-1 / F-2**）を読む。
4. `DES-002` §4.2 / §7.5 / §11.3 を読む。
5. **★★完了報告の「第 1 部完了時点の E2E 結果」を先に見る**（§1 の照合がそこから始まる）。

### 0.2 レビューの基本姿勢

- **コードを変更しない。指摘のみを行う。**
- **★★★本サブでは「テストが緑である」が根拠にならない場面が中心である**——**`queryKey` の形を変えて `invalidateQueries` が当たらなくなっても、テストは緑のまま通りうる。⇒ 破壊確認 2 の出力を必ず見ること。**
- **★★第 1 部と第 2 部を分けて読むこと。★第 1 部は「挙動が変わっていないこと」、第 2 部は「挙動が変わったこと」が正しい。**

### 0.3 ★★重大でないもの（**誤判定しないこと**）

| # | 誤判定しないこと |
|---|---|
| **1** | **★`queryKey` の統一先の形は製造の裁量である**（指示書 §4.1 / §9.2）。**★理由が報告されていれば足りる** |
| **2** | **★ゴミ箱のキャラ選択の既定値は製造の裁量である**（§4.2） |
| **3** | **★重複エラーの差分表示の形は製造の裁量である**（§4.4）。**★「遷移しなくても差分が読める」が満たされていれば足りる** |
| **4** | **★条件付き E**（`normal` / `counter`）**が落ちていることは、`ComboEditorBasicFields` を触らなかったという報告が伴う限り欠陥ではない**（§1.2-E） |
| **5** | **★`gofmt` をベースライン固定型にしたことは欠陥ではない**（§4.5。**★理由が報告されていれば足りる**） |
| **6** | **★`SM-016`（コンボの単位語）が扱われていないことは正しい**——**`D-618` で `M25` 以降へ送った**（§1.6-9）。**★扱っていたら指摘する** |
| **7** | **★英語ロケールの残りが手つかずであることは正しい**（§1.6-10。**開発者判断で据え置き**） |
| **8** | **★第 2 部 B で `en` の `alias_text` が Dash のままであることは正しい**（`D-615`） |

### 0.4 報告フォーマット

`docs/progress/m24-08-review.md` へ、重大 / 軽微 / 質問 の 3 区分で書く。

---

## 1. ★★★第 1 部の成果物は「E2E 非回帰」である（**最重要**）

| # | 観点 | 重大度 |
|---|---|---|
| **★★★1** | **★★★第 1 部の完了時点のコミットが在り、その時点の E2E 結果が全体の結果と別に載っているか**（指示書 §1.1 / §7.1-1）。**★無いなら、第 1 部が非回帰だったことを誰も確かめられない** | **重大** |
| **★★★2** | **★★★第 1 部で既存テストの期待値を変えていないか**（§5.2）。**★★変えているなら、それは挙動を変えた証拠である。★1 件ずつ理由を確かめること** | **重大** |
| **★★3** | **★★破壊確認 2**〔`invalidateQueries` のキーを 1 か所だけずらす〕**が赤くなったか。★★★緑のままなら、「静かにキャッシュが効かなくなる」を誰も検出できない** | **重大** |
| **4** | **★破壊確認 1**〔統一後の `queryKey` を 1 か所だけ旧の形へ戻す〕**が赤くなったか** | **重大** |
| **★★5** | **★★`invalidateQueries` / `setQueryData` も同時に変わっているか**（§4.1）。**★片方だけ変えていたら重大である** | **重大** |
| **6** | **★§3.3-1 の `queryKey` の全数が数え直され、その全数が統一されているか**（**★`code-facts` の 64 行を使っていないか**＝§1.4） | **重大** |
| **7** | **★変数参照**〔`queryKey: PRESETS_KEY` 等〕**と、配列ではない文字列定数も対象に入っているか** | **重大** |

---

## 2. ★★★契約との照合

| # | 観点 | 重大度 |
|---|---|---|
| **★★★1** | **★★★契約 F-2 の 3 面が守られているか**——**`presetId` が単一値であること ／ queryKey の構成要素として残っていること ／ `RecipeResponse.presetId` と config `[defaults] preset_id` が不変であること**（§4.1.1） | **重大** |
| **★★★2** | **★★★§3.3-2 の実査**〔F-2 の「形を変えない」がリテラルな意味かモデルの意味か〕**の答えが報告されているか。★食い違いが出たのに止めずに進んでいたら重大である**（§9.1-1） | **重大** |
| **★★3** | **★★`internal/service/notation/` / `internal/service/preset/` が変わっていないか**（契約 F-1） | **重大** |
| **★★4** | **★★★`recipe_cache` をマイグレで書き換えていないか**（§4.3。**★組み立て規則を 2 か所に持つと `E-76`**） | **重大** |
| **5** | **★`preset_aliases` の構造が変わっていないか。★変わったのは `alias_text` の値だけか**（`DES-002` §7.5） | **重大** |
| **★★6** | **★★`DES-002` §11.3 の既存の契約 5 件が 1 つも落ちていないか**（§4.5。**とくに `go test` のキャッシュ無効化とパイプの終了コード**） | **重大** |
| **7** | **★`pr-checks.yml` へ E2E の実体を足していないか**（§1.6-7。**E2E は nightly である**） | **重大** |
| **8** | **★CSV の列名・ZIP エントリ名が変わっていないか**（`DES-002` §7.6） | **重大** |
| **9** | **★`web/playwright.config.ts` の `workers` が `1` のままか** | **重大** |

---

## 3. 第 2 部 A——ゴミ箱の複数キャラ対応

| # | 観点 | 重大度 |
|---|---|---|
| **★★1** | **★★コンボ表とセットプレイ表の両方が対応しているか**（§4.2 の落としやすい点 2）。**★コンボ表だけ直してセットプレイ表を忘れる形になりやすい** | **重大** |
| **2** | **★破壊確認 3**〔キャラ選択を定数へ戻す〕**が、コンボ表とセットプレイ表の両方で赤くなったか** | **重大** |
| **3** | **★`DEFAULT_CHARACTER_ID` のハードコードと `TODO(M3+)` が消えているか** | **重大** |
| **4** | **★既存の `CharacterSelector` を使っているか。★新しい部品を作っていないか**（§4.2） | 軽微 |
| **5** | **★BE の経路が変わっていないか。★変わったなら止めて設計卓へ報告されたか**（§9.1-7） | **重大** |
| **6** | **★§3.3-6 の「キャラを渡している箇所の全数」が母数付きで報告されているか**（**★前任設計卓の「1 か所で足りる見込み」を書き写していないか**） | **重大** |

---

## 4. 第 2 部 B——地上ダッシュの表示語

| # | 観点 | 重大度 |
|---|---|---|
| **★★1** | **★★`recipe_cache` をマイグレで書き換えていないか**（§2-4 と同じ。**★最も落としやすい**） | **重大** |
| **★★2** | **★★§3.3-10 の答え**〔再構築が自動か、ボタン待ちか〕**が証拠つきで報告されているか。★「ボタン待ち」と分かったのに止めずに進んでいたら重大である**（§9.1-3） | **重大** |
| **3** | **★ja が「前方ステップ／後方ステップ」になっているか。★★en が Dash のままか**（§4.3） | **重大** |
| **4** | **★§3.3-9 の対象行の全数が母数付きで報告されているか**（**★「2 行」を書き写していないか**） | **重大** |
| **5** | **★マイグレの down が書かれているか。★消費したのは `000080` の 1 本か** | **重大** |
| **6** | **★FE 側**（`SystemRow.tsx` / `constants/punish.ts`）**を触っていないか**（**★あちらは正しい**） | **重大** |
| **7** | **★破壊確認 5**〔`alias_text` を「前ダッシュ」へ戻す〕**が赤くなったか** | 軽微 |

---

## 5. 第 2 部 C / D

| # | 観点 | 重大度 |
|---|---|---|
| **1** | **★重複エラーで「遷移しなくても差分が読める」ようになっているか**（§4.4） | **重大** |
| **2** | **★重複判定そのもの**（`VAL-C02`）**を触っていないか**（§4.4。**`M24-11` が是正済み**） | **重大** |
| **3** | **★`DES-005` §5.7 への as-built が完了報告に書かれているか** | 軽微 |
| **4** | **★`gofmt -l` が PR チェックに載っているか。★破壊確認 4 で赤くなることが示されているか** | **重大** |
| **5** | **★§3.3-15 の現在の検出数が母数付きで報告され、(a) 先に是正 か (b) ベースライン型 のどちらを選んだかと理由が書かれているか** | **重大** |

---

## 6. テストの妥当性

| # | 観点 | 重大度 |
|---|---|---|
| **★★1** | **★★テストが 1 本も減っていない・skip されていないか** | **重大** |
| **★★2** | **★★第 2 部で期待値を変えた箇所について、「変えてはいけないから固定してあった」のか「たまたま現状を写しただけ」なのかが 1 件ずつ判定されているか**（playbook §4.45 の規約 2） | **重大** |
| **★★3** | **★★§5.1-2**（invalidate が当たることを固定するテスト）**が実際に「当たらなくなったら赤くなる」形になっているか。★存在するだけでは足りない** | **重大** |
| **4** | **★破壊確認 5 件すべてに実施コマンドと出力が載っているか**（**★「赤くなった」という文章だけで済ませていないか**） | **重大** |
| **5** | **★破壊確認後の確定実行が絞られていないか**（`D-599`） | **重大** |

---

## 7. 実行と報告の作法

| # | 観点 | 重大度 |
|---|---|---|
| **★1** | **★E2E の着手前基準値を採っていないか**（`D-600` 規約 1。**★本サブは採らない**）。**★直前にマージされた `M24-07` の完了報告が全数緑を明記していることの確認が書かれているか** | 軽微 |
| **★★2** | **★★§7.3 の検査表が、1 行ずつ実際の出力から埋められているか**（`M24-09d` の教訓 2）。**★回していないものが「違反なし」と書かれていないか** | **重大** |
| **3** | **★`make e2e` が第 1 部の完了時と全体の完了時の 2 回回されているか**（§7.2） | **重大** |
| **4** | **★`pnpm build` が回されているか**（**★型検査はこちらで見る**） | **重大** |
| **5** | **★ポートの空き確認に `ss` / `netstat` / `lsof` を使っていないか**（playbook §4.37 規約 7） | 軽微 |
| **6** | **★`docs/progress/progress-log.md` へ追記されているか** | **重大** |
| **7** | **★完了報告に `git diff --numstat` が貼られているか** | 軽微 |
| **8** | **★§3.3 の実査 16 件の答えが母数付きで載っているか** | **重大** |
| **9** | **★`CHANGE-148` の番号を製造が消費していないか** | **重大** |

---

## 8. 重大な問題の判定基準（完了承認を妨げる）

- **第 1 部の完了時点の E2E 結果が別に載っていない**
- **第 1 部で既存テストの期待値を変えている**
- **破壊確認 2**（`invalidateQueries` のキーずらし）**が緑のまま**
- **`invalidateQueries` / `setQueryData` の片方だけを変えている**
- **契約 F-2 の 3 面のいずれかが崩れている。または §3.3-2 の食い違いを報告せずに進んだ**
- **`internal/service/notation/` / `internal/service/preset/` が変わっている**
- **`recipe_cache` をマイグレで書き換えている**
- **`DES-002` §11.3 の既存の契約が落ちている**
- **`pr-checks.yml` へ E2E の実体を足した**
- **ゴミ箱のセットプレイ表が対応していない**
- **`en` の `alias_text` を日本語へ変えた**
- **テストが減っている・skip されている**
- **回していない検査の結果を報告へ書いている**

---

## 9. 軽微な問題の判定基準（持ち越し許容）

- `queryKey` の統一先の形の好み
- ゴミ箱のキャラ選択の既定値
- 重複エラーの差分表示の見た目
- テストの置き場
- 条件付き E が落ちたこと（**★理由が報告されている限り**）

---

## 10. 質問・確認事項のフォーマット

| # | 対象 | 質問 | 設計卓の判断が要るか |
|---|---|---|---|

---

## 11. レビュー完了の判定

- **重大 0 件**、または**重大がすべて是正済み**であること。
- **★★★破壊確認 2 の出力が載っていること**（**★本サブで最も重要な観測である**）。
- **★★第 1 部完了時点の E2E 結果が別に載っていること。**

### 11.1 ★本チェックリスト自身の検査（**発行前に設計担当が行う**）

- **★★§0.3「重大でないもの」が、指示書がスコープ外・裁量と定めた項目をすべて拾っているか。**
- **★§8 の重大判定が、指示書 §2.2「変更しないもの」と §7.1「機能要件」から漏れなく引かれているか。**
- **★★§1「第 1 部の成果物は E2E 非回帰」が最初の節に在るか**（**本サブの構造がそこで決まるから**）。
- **★「対象指示書」欄の版数が指示書本体と一致しているか**（`check-instruction-format.sh` の検査 (3)）。

---

*以上、M24-08 レビューチェックリスト **v1.0.0**。* **★★★本サブで最も重要な観測は破壊確認 2 である**——**`invalidateQueries` のキーを 1 か所ずらして赤くなるか。★緑のままなら「静かにキャッシュが効かなくなる」を誰も検出できない。** **★★次に見るのは「第 1 部で挙動が変わっていないこと」である**——**第 1 部の成果物は E2E 非回帰であり、既存テストの期待値を変えていたら、それは挙動を変えた証拠である。** **★★契約が 5 件当たっている。とくに F-2 は `CO-009` と正面から当たり、設計卓が「モデルの契約であって表記法の凍結ではない」と裁定した。⇒ §3.3-2 の実査がその検算である。食い違いを報告せずに進んでいたら重大である。**
