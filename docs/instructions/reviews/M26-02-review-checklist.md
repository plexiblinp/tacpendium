# M26-02 レビューチェックリスト: 公開スナップショット生成の基盤 ＋ ライセンスの配置

| 項目 | 内容 |
|------|------|
| 文書ID | M26-02-review-checklist |
| バージョン | **1.0.3** |
| 対象指示書 | `docs/instructions/M26-02-public-snapshot-and-license-placement.md` **v1.3.0** |
| 推奨モデル | **高精度レビュー**（**★★除外規則は空振りしても緑になる ／ ★ライセンスの割当は戻すのが難しい向きがある**） |
| 作成者・作成日 | 設計担当 Claude（設計卓・フェーズ4 期）/ 2026-09-06 |

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| 1.0.3 | 2026-09-06 | **★★受理＝`D-746`。失効していた記述 3 件へ注記を足した**〔**欠番 3 つ → 1 つ ／ 層 A 28 → 27 ／ レビュー表の射程は `000080` まで**〕**。★観点は 1 つも変えていない。★本文は書き換えていない**＝`D-274` (3) |
| 1.0.2 | 2026-09-06 | **★対象指示書の版に追随**（**`D-743`**）。**`README.txt` の射程が「整理まで」に縮んだ。⇒ §0.3 と §4 を直した** |
| 1.0.1 | 2026-09-06 | **★対象指示書の版に追随**（**`D-742`**）。**`docs/seed-data/` の除外が確定した。⇒ §1 へ 2 行足した。★観点は不変** |
| 1.0.0 | 2026-09-06 | 初版（**`D-741`**）。**★投入の合図と同じ手番で起こした**（`m27-03-review-checklist-not-created` の是正＝`D-734`） |

---

## 0. レビュー担当への前置き

### 0.1 レビュー前の準備

1. 指示書 **v1.0.2** を全文読む。**★とくに §0.1（公開ではない）／§0.2（空振りしても緑）／§0.3（戻すのが難しい）／§4（やらないこと 9 項）。**
2. 完了報告 `docs/progress/M26-02-completion-report.md` を読む。
3. **★★`docs/progress/20260905-migration-license-assignment.md` を読む**——**レビュー表 §1（層 A 28 本）／§2（層 B 52 本）が割当の正本である。⇒ 実装がこの表と 1 対 1 で一致しているかを見る。** **★★【2026-09-06 失効＝`D-746`】§1 の 28 行は削除済みの `000012` を含むため、現存する凍結層 A は 27 本である。** **★レビュー表が覆うのは `000001`〜`000080` だけであり、`000081`〜`000103` の 23 本は覆っていなかった。⇒ as-built は `REUSE.toml` が正本である。**
4. **★`docs/human-notes/future-notes/combmgr-license-strategy-outline.md` §0（三層）を読む。**
5. **★`docs/handover/code-facts.md` を引く。想定で書かない。**

### 0.2 レビューの基本姿勢

コードを変更せず、指摘のみを行う。

### 0.3 ★★重大でないもの（**誤判定しないこと**）

| # | これは欠陥ではない |
|---|---|
| **★★1** | **公開リポジトリへ実際に落としていないこと。⇒ これが正しい**（指示書 §0.1 / §4-1）**。★本サブが作るのは「生成する仕組み」と「その入力」だけである。実施はフェーズ5 冒頭。** |
| **★★2** | **ファイルが消えていないこと。⇒ 候補を出すだけが射程である**（`D-196` 境界条件 3。指示書 §2.4-3 / §4-2）**。★仕分け済みの置き場へ移すことは可。** |
| **★★3** | **`docs` が丸ごと公開対象になっていること。⇒ これが正しい**（`D-637` (3) ＋ `M26-overview`。**アーカイブ含め全部公開する。`CLAUDE.md` / `.claude/` / `AGENTS.md` / `.codex/` も公開してよい**）。 |
| **★★4** | **REUSE 仕様の構文が「確かめられなかった」と書かれていること。⇒ これが正しい**（`M26-01` 教訓 3＝**一次情報へ到達できないなら断定しない**）**。★断定していたら、そちらが欠陥である。** |
| **★5** | **既存マイグレが 1 バイトも変わっていないこと**（`D-535`＝適用済みマイグレは凍結。指示書 §4-4）。 |
| **★6** | **`docs/design/` が未編集であること**（`CLAUDE.md` §8。**★該当箇所を報告するのが正しい**）。 |
| **★7** | **`.claude/` が未編集であること**（開発者のルールファイル）。 |
| **★8** | **`followup-backlog.md` が未編集であること**（`D-382`。**§J 停止時記録だけは例外**）。 |
| **★9** | **CHANGE 番号・マイグレ連番を自採番していないこと**（`D-293`）。 |

### 0.4 ★★本サブに固有の危険（**最優先で見る**）

**危険は 3 つある。★★1 と 2 は「緑のまま間違う」型、3 は「後から戻せない」型である。**

| # | 危険 | なぜ検出しにくいか |
|---|---|---|
| **★★1** | **除外規則が空振りしても緑になる** | **「除外した」を「除外リストに書いた」で済ませると、規則が 1 件も当たっていなくても検査は緑を返す。⇒ 陽性対照が無い検査は、存在しないのと同じである**（`E-84`） |
| **★★2** | **denylist にしてしまう** | **★新規追加が黙って通る。⇒ 公開してはならないファイルが後から増えたとき、検査が何も言わない。★許可リストなら「知らないものは出さない」に倒れる** |
| **★★3** | **ライセンスの割当を間違える** | **開発者の逐語＝「一度 AGPL 化してから CC BY SA に戻すのは難しい認識」。⇒ 割当は「決めて実装する」のではなく「表を写す」。★表に無いものが出たら止まるのが正しい** |

### 0.5 報告フォーマット

`docs/progress/m26-02-review.md`。指摘ごとに **重大 / 高 / 中 / 低** と **根拠のファイル・行** を書く。

---

## 1. 束 A — 許可リストと陽性対照（最重要）

| # | 見ること |
|---|---|
| **★★1** | **★★許可リスト方式か。denylist になっていないか**（指示書 §2.1-1）**。⇒ 「除外するもの」を列挙する形なら、それが欠陥である。** |
| **★★2** | **★★陽性対照が self-test に入っているか**（指示書 §2.1-5）**。⇒ わざと 1 件だけ除外対象を残したケースで検査が赤くなることを、実際に走らせた結果が報告に在るか。★「入れた」だけでは足りない。** |
| **★★3** | **除外の 3 群が入っているか**——**`.devcontainer/`**〔`anthropics/claude-code` 由来が全権利留保。`P-47`〕**／ 配布禁止 HTML・検証用 CSV**（`A3`）**／ `.env` / `secrets/` 系。** |
| **★★4** | **`docs/human-notes/future-notes/combmgr-features.txt` が「存在しても出さない」形で書かれているか**（指示書 §2.1-4）**。⇒ 「存在すること」を前提にした形だと、開発者が削除したときに検査が落ちる。** |
| **★★4-b** | **★★`docs/seed-data/` がディレクトリごと除外されているか**（`D-742`）**。★ファイル単位で選んでいたら「高」である**——**seed 波のたびに増えるため、後から足されたものが黙って通る。** |
| **★★4-c** | **★`target-combo-and-derived-flag-rules.md` の参照切れが報告に在るか**（`M19-DESIGN-05` / `-07` / `-08` から参照されている）**。★製造が直していないか**——**`docs/handover/` の歴史記録であり、直すのは射程外である。** |
| **★5** | **許可リストの全文が報告に在るか。★除外したものはパスで書かれているか**（**件数だけなら不十分**＝指示書 §3-1）。 |
| **★6** | **`.gitignore` の既存除外と重なる範囲が整理されているか**（指示書 §6-2）。 |

## 2. 束 B — ★★ライセンスの配置（**戻すのが難しい**）

| # | 見ること |
|---|---|
| **★★1** | **★★`migrations/` の割当が、レビュー表 §1 / §2 と 1 対 1 で一致しているか**（指示書 §2.2-3）。**⇒ 層 A 28 本 ／ 層 B 52 本。★数だけでなく、どのファイルがどちらかを突き合わせること。** **★★【2026-09-06 失効＝`D-746`】層 A は 27 本。★観点**〔ファイル単位で突き合わせる〕**は不変である。** |
| **★★2** | **★層が割れる 3 本**〔`000039` / `000017` / `000029`〕**が表のとおりに扱われているか。** |
| **★★3** | **★★表に無いものを製造が自分で割り当てていないか**（指示書 §4-5）**。⇒ 在ったら重大。止まって請うのが正しい。** |
| **★★4** | **三層の `LICENSE` が置かれているか**——**層 A＝`AGPL-3.0-or-later` ／ 層 B＝`CC-BY-SA-4.0` ／ 層 C＝`MIT`。** |
| **★★5** | **★パス単位の宣言に集約されているか。ファイル 1 本ずつ SPDX ヘッダを書いていないか**（`D-702` (f)。指示書 §2.2-2）。 |
| **★★6** | **新規マイグレの命名規約が在るか**（指示書 §2.2-4。**層 B に接頭辞**）**。⇒ 宣言がグロブ 1 行で済み、次の seed 波が何本足しても宣言を触らない形か。** |
| **★★7** | **`scripts/check-migration-license.sh` が在り、self-test を持ち、緑か**（指示書 §2.2-5）**。★`check-artifact-integrity.sh` が検出できる形か。** |
| **★8** | **`character_data/` が丸ごと層 B か。★`seed-progress.md` の 1 件だけが層 A か**（指示書 §2.2-6）。 |
| **★9** | **`DATA-LICENSE.md` に「データとしての正本は `character_data/`」の 1 行が在るか**（指示書 §2.2-7）。 |
| **★10** | **`NOTICE` / `CONTRIBUTING.md`（DCO）/ `SECURITY.md` / `SUPPORT.md` が骨子 §9 に沿っているか。★`SECURITY.md` に「LAN 内での利用を前提とし、インターネットへ直接公開しないこと」が明記されているか**（指示書 §2.2-8）。 |

## 3. 束 C — マイグレの欠番の記録

| # | 見ること |
|---|---|
| **★★1** | **`SUPP-001` §2.7 へ書く素案が報告に在るか**（**★反映は設計卓。製造は素案まで**＝指示書 §5-5）。 |
| **★★2** | ~~欠番 3 つ〔`000012` / `000081` / `000096`〕~~**が理由つきで表になっているか。★1 行で済ませていないか**（指示書 §2.3-1）。 **★★【2026-09-06 失効＝`D-746`】欠番は `000012` の 1 件だけである**（`000081` / `000096` は実在する）。**★★「3 つ」は `D-708` の予測が事実として写ったものであった**（計測点 `M-129`）。 |
| **★★3** | **★★「欠番は無害である」が併記されているか**（指示書 §2.3-3）**。⇒ `schema_migrations` は現在版数 1 行しか持たず、`Up` は存在するファイルを昇順に適用するため穴を素通りする。★書かないと次の担当が「連番を詰めよう」とする。** |
| **★★4** | **★危険なのは別のことだ、と明記されているか**（指示書 §2.3-4）**——(a) 適用済み DB に後から若い連番が現れると、その 1 本は永久にスキップされエラーにもならない ／ (b) 隣の版数を直書きしたテストは欠番を作った瞬間に落ちる。** |

## 4. 束 D — リポジトリの整理

| # | 見ること |
|---|---|
| **★★1** | **★★`README.md` / `README.txt` が「整理」までで止まっているか**（`D-743`）**。⇒ 解消そのものは `M32` へ送られた。★★`README.txt` を `docs/usermanual/` へ移していたら重大である**——**3 つの根拠が配布物の必須成果物と定めており、ビルドが壊れる。** |
| **★★2** | **★★消す変更をしていないか**（`D-196`）**。⇒ 候補と根拠を出すところまでが射程である。** |
| **★3** | **仕分け済みの置き場へ移したもの**〔`docs/handover/phase{N}/` ／ `docs/*/archive/`〕**が在れば、その一覧が報告に在るか。** |
| **★4** | **開発者の手番として残したもの**（消す候補・その根拠）**が報告に在るか**（指示書 §3-6）。 |

## 5. 既存の破壊（非破壊性）

| # | 見ること |
|---|---|
| **★★1** | **既存マイグレが 1 バイトも変わっていないか**（`D-535`）。 |
| **★★2** | **`M28-01` のリネームが巻き戻っていないか。★`combomgr` が新しく現れた行が無いか**（**`combomgr-importer` / `autopilot-combomgr` は別プロジェクトであり正しい**）。 |
| **★3** | **既存の検査スクリプトのベースラインが動いていないか。★動いたなら理由が報告に在るか。** |
| **★4** | **`docs/` からファイルが消えていないか**（§4-2）。 |

## 6. ドキュメント・進捗ログ

| # | 見ること |
|---|---|
| **★★1** | **★★「確かめられなかったもの」が「確かめられなかった」と書かれているか**（指示書 §3-7）**。⇒ とくに REUSE 仕様の構文。★断定していたら欠陥である**（`M26-01` 教訓 3）。 |
| **★2** | **`docs/design/` に反映が要る箇所が一覧になっているか。** |
| **★3** | **`docs/progress/progress-log.md` の `### M26-02` へ追記されているか**（`CLAUDE.md` §8）。 |

## 7. 破壊確認（**実際に壊して、検出できることを見る**）

**★レビュー担当が自分で 2 つ試すこと。★★「検査が在る」ではなく「壊したら赤くなる」を見る。**

| # | 壊し方 | 期待 |
|---|---|---|
| **★★1** | **除外対象のファイルを 1 件だけ許可リストへ紛れ込ませる**（または生成物へ残す） | **検査が赤くなる。⇒ 緑のままなら、陽性対照が効いていない。★★これが本サブで最も重要な破壊確認である** |
| **★★2** | **`migrations/` へ、命名規約にも宣言にも当てはまらないファイルを 1 本置く** | **`check-migration-license.sh` が赤くなる。⇒ 緑のままなら、漏れが静かに起きる** |
| **★3** | **許可リストに無い新しいファイルをリポジトリに置く** | **生成物に出ない。⇒ 出たら denylist 的に動いている** |

---

## 8. 重大な問題の判定基準（完了承認を妨げる）

| # | 重大とするもの |
|---|---|
| **★★1** | **陽性対照が無い、または走らせた結果が報告に無い**（§1-2）。 |
| **★★2** | **denylist 方式になっている**（§1-1）。 |
| **★★3** | **ライセンスの割当がレビュー表と一致しない、または表に無いものを製造が割り当てている**（§2-1 / §2-3）。 |
| **★★4** | **`check-migration-license.sh` が無い、または self-test を持たない**（§2-7）。 |
| **★★5** | **公開リポジトリへ実際に落としている**（§0.3-1 の裏返し）。 |
| **★★6** | **ファイルを消している**（`D-196`）。 |
| **★★7** | **既存マイグレを改変している**（`D-535`）。 |
| **★8** | **REUSE 仕様の構文を、一次情報の確認なく断定している**（§6-1）。 |
| **★9** | **除外の 3 群のいずれかが漏れている**（§1-3）。 |

## 9. 軽微な問題の判定基準（持ち越し許容）

| # | 軽微とするもの |
|---|---|
| 1 | **許可リストの粒度**（ディレクトリ単位かファイル単位か。**★理由が報告に在る場合に限り軽微とする**）。 |
| 2 | **`SUPP-001` §2.7 の素案の文面**（**★反映するのは設計卓なので、素案の言い回しは持ち越せる**）。 |
| 3 | **アーカイブ送りの候補の網羅性**（**★候補を出すのが射程であり、全数である必要はない**）。 |
| 4 | **報告の書き方の不足。** |

## 10. レビュー完了の判定

1. **束 A〜D と §5・§6 のすべてに目を通した。**
2. **★★§7 の破壊確認を最低 2 つ実施した**（**1 と 2 は必須**）。
3. **★★§2-1 のレビュー表との突き合わせを、数だけでなくファイル単位で行った。**
4. **重大が 0 件である**（在れば差し戻し）。
5. **報告 `docs/progress/m26-02-review.md` を書いた。**

### 10.1 ★本チェックリスト自身の検査（発行前に設計担当が行う）

- [x] 指示書 v1.0.2 の「やらないこと」9 項が、§0.3 の「欠陥ではない」と矛盾していない
- [x] §0.4 の危険 3 つが、指示書 §0.2 / §0.3 と対応している
- [x] §7 の破壊確認 1・2 が、機械検査で代替できないものになっている
- [x] `D-196`（消すのは開発者の手番）が §0.3-2 と §4-2 と §8-6 の 3 か所で一貫している

---

*以上、M26-02 レビューチェックリスト **v1.0.3**。* **★★本サブでレビューが最も見落としやすいのは「除外リストに書いてあるから除外されている」と読むことである**（§0.4-1）。**★除外規則は空振りしても緑になる。⇒ §7 の破壊確認 1 を必ず実施すること。** **★もう 1 つは「ライセンスの割当は戻すのが難しい向きがある」**（§0.4-3）**——★数が合っていても、どのファイルがどちらかが違えば意味が無い。⇒ ファイル単位で突き合わせること。**
