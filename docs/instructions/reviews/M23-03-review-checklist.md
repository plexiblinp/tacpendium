# M23-03 レビューチェックリスト

| 項目 | 内容 |
|------|------|
| 対象指示書 | `docs/instructions/M23-03-reference-side-exclusion-and-delete-contract.md` v1.0.0（参照側の除外の穴を 1 件ずつ塞ぐ） |
| 文書ID | M23-03-REVIEW |
| 対象指示書ID | M23-03 |
| レビューモデル | 高精度レビュー（実モデル名は `docs/human-notes/model-allocation.md` が正本。実使用は開発者判断） |
| バージョン | 1.0.0 |
| 作成者・作成日 | 設計担当 Claude（設計卓／M23 起票期）/ 2026-08-20 |

> 文書ID ⇄ 実パスの正は `docs/handover/docs-map.md`（自動生成）。

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| 1.0.0 | 2026-08-20 | 初版（指示書 v1.0.0 と対）。**最重要ゲート＝「6 か所を一律に扱っていないこと」。** |

---

## 0. レビュー担当への前置き

### 0.1 レビュー前の準備

1. **`docs/handover/architecture-patterns.md` §11 を先に読む**（本サブの判断の軸そのもの）。
2. 指示書 `M23-03` を通読する（**§1.2 の裁定表を頭に入れてから差分を見る**）。
3. **完了報告に Plan Mode の実査 8 件（指示書 §3.3）の結果が揃っていることを先に確認する。** **★とくに §3.3-1（`M23-02` の走査結果の引き継ぎ）が無いと、`#6` の作業対象が確定していないことになる。**

### 0.2 レビューの基本姿勢

- **★★本サブは「穴を全部塞ぐ」作業ではない。** **6 か所のうち 1 か所は意図的に残し、1 か所は関数を分ける。** **「まだ述語が無い箇所がある」を理由に指摘しないこと。**
- **★「述語が足りているか」ではなく「裁定どおりか」を見る。** 裁定は指示書 §1.2 の表にある。

### 0.3 ★重大でないもの（**誤判定しないこと**）

| # | 誤判定しやすい状態 | 実は仕様である理由 |
|---|---|---|
| N-1 | **`#1` に `deleted_at` の述語が無い** | **指示書 §4.1。★塞ぐと完全削除が壊れる。** **★これは本サブが意図的に残した唯一の箇所である** |
| N-2 | **`#6` の検証用の関数に述語が無い** | **指示書 §4.5-3。★意図的に緩くない側である。** **緩めると「ゴミ箱へ入れる → 同じレシピを作る → 復元」で重複が静かに並ぶ** |
| N-3 | **同じことをする関数が 2 つ在るように見える**（`#6` の分割後） | **指示書 §4.5-6。★2 つとも現役であり、求めている集合が違う。** **「片方は古い関数の名残」と読んで統合させないこと** |
| N-4 | **`#4` を塞いでも確定反撃サーチの結果が変わらない** | **指示書 §4.4-2。★それが期待どおりである**（過剰包含が消えるだけ）。**§5-5 は「変わらない」を主張するテストである** |
| N-5 | **コンボの論理削除が楽観排他を持たないまま** | **指示書 §4.6-2。★契約として現状を固定する側であり、実装を変えない。** **排他を入れると「消したいのに消せない」経路が生まれる** |
| N-6 | **完全削除の前チェックがトランザクションの外のまま** | **指示書 §1.5-1。`M23-06` の担当**（followup `setup-permanent-delete-precheck-tx`） |
| N-7 | **削除済みの行へもう一度削除すると削除日時が上書きされる** | **指示書 §4.6-3 / §1.5-3。`M23-06` の担当**（削除日時の見せ方は同サブが持つ） |
| N-8 | **画面側のコードに差分が無い** | **指示書 §1.5-8。★本サブはサーバ側だけで閉じる** |

### 0.4 報告フォーマット

- 各節「OK / 重大（§9）/ 軽微（§10）/ 質問（§11）」。

---

## 1. 設計・パターンとの照合（最重要）

- [ ] **★★6 か所が指示書 §1.2 の裁定どおりに処理されている。** **1 件ずつ突き合わせること**（一括で「塞がれている」と判定しない）。
- [ ] **`#1` に述語が足されていない**（指示書 §4.1-1）。
- [ ] **`#1` に「なぜ述語が無いのか」のコメントがあり、残す理由が 2 つとも書かれている**（同 §4.1-3。**完全削除の前チェック ／ ゴミ箱の行から読み取り専用詳細を出す道具**）。
- [ ] **`#2` と `#5` の両方に述語が足されている**（同 §4.2-2。**★片方だけでないこと**）。
- [ ] **`#3` に述語が足されている**（同 §4.3-1）。
- [ ] **`#4` の親側にも述語が足されている**（同 §4.4-1）。
- [ ] **`#4` のインタフェースコメントが、両側を除外する形へ書き直されている**（同 §4.4-4。**★消しただけになっていないこと**）。
- [ ] **★★`#6` が 2 つの関数に分かれている**（同 §4.5-3）。**表示用は削除済みを除外し、検証用は現状の母集団を保っていること。**
- [ ] **★`#6` の呼び出し側が全数割り当てられている**（同 §3.3-5 / §4.9-2）。**判別のつかない呼び出し側が残っていないこと。**
- [ ] **★分割後の 2 つの名前から役割が読み取れる**（同 §4.5-6）。

## 2. データ・API 契約・スキーマの不変（非破壊性）

- [ ] **スキーマを変えていない。マイグレを 1 本も足していない**（指示書 §4.7）。
- [ ] **`DELETE /api/combos/:id` の実装を変えていない**（同 §4.6-2 / §2.2-3）。
- [ ] **完全削除の前チェックの位置を変えていない**（同 §1.5-1）。
- [ ] **`M23-01` / `M23-02` が足した経路・列・テストに触れていない**（同 §2.2-7）。
- [ ] **★設計書本体（`DES-002`）を製造が直接編集していない**（`CLAUDE.md` §8。反映は `CHANGE-123` で設計卓が行う）。

## 3. フロントエンドの動作仕様

- [ ] **画面側のコードに差分が無い**（指示書 §1.5-8）。**★差分がある場合、§3.3-8 の報告が完了報告にあるか。**
- [ ] **応答から削除済みの id が消えたことで壊れた画面が無いことが、E2E で確認されている**（同 §5-9）。

## 4. テストの妥当性（ケース数で確認。指示書 §5 ↔ §7 DoD）

- [ ] **指示書 §5 の 9 件がすべて在る。**
- [ ] **★§5-1 が「`#1` が削除済みの行を返し続けること」を否定形で固定している。** **★これが無いと、後で誰かが `#1` を塞いでも気づかない。**
- [ ] **§5-3 が 2 本ある**（`#2` と `#5`。**片方だけでないこと**）。
- [ ] **§5-4 が「数えないこと」と「復元したら数えること」を対で確認している。**
- [ ] **§5-5 が「`#4` を塞いでも結果が変わらない」を主張している**（**★変わっていたら §9 の重大**）。
- [ ] **★★§5-6 と §5-7 が両方在る**（表示用は返さない ／ 検証用は返し続ける）。**★どちらか一方でも欠けていたら、関数を分けた意味を主張できていない。§9 の重大にあたる。**
- [ ] **§5-8 が「検証を緩めていないこと」を確認している。**
- [ ] **★完了報告のテスト結果が、コマンド自身の出力（スイート名・pass/fail の件数）で書かれているか**（教訓 **E-125**）。**「全緑」「exit 0」だけを根拠にしていないか。**
- [ ] **★結果キャッシュが効いた状態で「実行せずに緑」になっていないことが示されているか**（followup `stop-test-hook-lacks-count-1`。**実測で所要が 3 桁違い、終了コードはどちらも 0 になる**）。
- [ ] **E2E を実行しなかった場合、「実行しなかった」と書かれている**（「緑」と書いていない）。

## 5. 設計意図との整合（精神の確認）

- [ ] **★「参照側の穴は全部塞いだ」という記述が残っていない**（指示書 §4.9-3）。**6 か所のうち 1 か所は意図的に残っている。**
- [ ] **`#6` の検証側を緩めていない**（同 §4.5-4）。**★`M23-05` の負担を増やしていないこと。**
- [ ] **`M23-04` / `M23-05` / `M23-06` の担当範囲へ踏み込んでいない**（同 §1.5）。
- [ ] **`FR013`（所有者・監査ログを持たない）に触れていない。**

## 6. コード品質・規約遵守

- [ ] 曖昧語依存でない（Playbook §4.1）。定数化・命名一貫。
- [ ] `console.log` / `fmt.Println` が残っていない。
- [ ] **エラーが wrap されている**（`CLAUDE.md` §4）。
- [ ] **推測で進めた箇所に `// 推測: 〜と仮定した` が入っている**（`CLAUDE.md` §9-3。指示書 §9.2 の 4 件が該当しうる）。

## 7. 既存挙動の温存（非破壊性）

- [ ] **完全削除がこれまでどおり動く**（`#1` の是正漏れが起きていないこと）。
- [ ] **確定反撃サーチが壊れていない**（`#3` / `#4`。**★`materialize` の副作用と衝突していないこと**＝指示書 §3.3-3）。
- [ ] **レシピ文字列の表示が壊れていない**（`#2` / `#5`）。
- [ ] **セットプレイの編集・重複チェックがこれまでどおり動く**（`#6` の検証側）。
- [ ] **`M23-02` が是正した 2 か所を二重に触っていない**（指示書 §3.3-1）。

## 8. ドキュメント・進捗ログ

- [ ] 完了報告に **Plan Mode 確定方式・変更点・テストケース数・既知の制約** が書かれている。
- [ ] DES CHANGE が要なら設計担当起票前提で、製造が DES 本体を直接編集していない。
- [ ] **指示書 §4.9 の 3 項目について、走査キーワードで実際に走査した結果**が完了報告にあるか。**★とくに §4.9-3（「全部塞いだ」と書いていないこと）。**
- [ ] **★並列サブの as-built との突合**（教訓 `E-121`）。**★本サブは単独である**（`M23-overview` §5.2）。**完了報告に「並列相手は無い」と書かれているか。**
- [ ] **★完了報告に「■ 併せて更新が要るもの」の節があるか**（教訓 `E-114` ／ ボード **D-277**）。**該当が無い場合も「なし」と書かれているか。** **★本サブは `CHANGE-123` を消費し、マイグレは消費しない。**
- [ ] **★★指示書 §7.5 が求める 4 件が、完了報告の独立した節に置かれているか。** **1 件でも欠けていたら §9 の重大にあたる**——**設計卓は実装ソースを読めないため、報告に無いものは存在しないのと同じである。**
- [ ] **★API 契約の as-built が独立した節に置かれているか**（**D-496**）。**本サブは経路を足さないが、`DES-002` §4.2 へ書く契約が §4.6 に 4 件ある。**
- [ ] **設計伝達レポート §4 に followup の更新候補が置かれているか。** **★製造が `followup-backlog.md` を直接編集していないこと**（**D-382**）。

---

## 9. 重大な問題の判定基準（完了承認を妨げる）

- **★★最重要ゲート＝6 か所を一律に扱っていること。** **`#1` に述語が足されていたら重大である**——**完全削除が壊れる。** **`#6` が 1 つの関数のまま述語を足されていても重大である**——**検証が緩み、静かに破れる経路が生まれる。**
- **`#6` の表示用と検証用のテストが対で存在しない**（§5-6 / §5-7 のどちらか一方が欠けている）。
- **`#6` の呼び出し側に、どちらの関数を呼ぶか割り当てられていないものが残っている。**
- **`#2` / `#5` の片方だけが塞がれている**（同型の非対称を新しく作っている）。
- **`#4` を塞いだことで確定反撃サーチの結果が変わっている**（§5-5 が赤い。**★実挙動が変わるなら報告してから進める約束だった**）。
- **`#3` を塞いだことで `materialize` の副作用が壊れている。**
- **「参照側の穴は全部塞いだ」という記述が残っている**（§4.9-3）。
- **スキーマ変更・マイグレの追加がある**（指示書 §4.7）。
- **`DELETE /api/combos/:id` の実装を変えている**（同 §4.6-2）。
- **スコープ外 8 件（指示書 §1.5）のいずれかへ踏み込んでいる。**
- **指示書 §7.5 の 4 件のいずれかが完了報告に無い。**

## 10. 軽微な問題の判定基準（持ち越し許容）

- 分割後の関数名の語の選び方（**★役割が読み取れていれば足りる**）。
- コメントの文面。
- テストの命名・配置の流儀。
- `#4` のインタフェースコメントの書き直し方。

## 11. 質問・確認事項のフォーマット

- 「指示書 §X.X / `DES-00X` §X / `M23-RESEARCH-01` 報告 §X に対し実装が Z。意図確認したい」の形で根拠節併記。

## 12. レビュー完了の判定

- §1〜§8 が全て OK、§9 重大ゼロ（**特に「6 か所を一律に扱っていないこと」と「`#6` の対のテスト」**）、§0.1 の Plan Mode 実査 8 件が揃っている。§10 軽微は持ち越し可。

### 12.1 ★本チェックリスト自身の検査（**発行前に設計担当が行う**）

- [ ] **本書と対の指示書の間に、節番号の食い違いが無い。**
  → 本書が参照する指示書の節は §1.2 / §1.5 / §2.2 / §3.3 / §4.1 / §4.2 / §4.3 / §4.4 / §4.5 / §4.6 / §4.7 / §4.9 / §5 / §7 / §9 であり、**指示書側に実在する**（**★§4.9 は §4.7 との衝突を避けて寄せた番号である**）。
- [ ] **指示書が禁じていない事項を「重大」として求めている項目が無い。**
  → §9 の各項目は指示書 §1.5 / §2.2 / §4 の禁止・固定と 1 対 1 で対応する。
- [ ] **達成不能な条件を重大として求めていない。**
  → **「参照側に述語が無い箇所が 1 つも無いこと」を求めていない**（§0.3 N-1。**`#1` は意図的に残る**）。
  → **`#6` の検証側が削除済みを除外することを求めていない**（§0.3 N-2。**意図的に緩くない側である**）。
  → **完全削除の前チェックがトランザクション内にあることを求めていない**（§0.3 N-6。**`M23-06` の担当**）。

---

*以上、M23-03 レビューチェックリスト v1.0.0。配置 `docs/instructions/reviews/M23-03-review-checklist.md`。指示書 v1.0.0 と対。「6 か所を一律に扱っていないこと」を最重要ゲートとする。*
