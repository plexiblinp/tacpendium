# M33-03 レビューチェックリスト（ガードと資料の作り直し）

| 項目 | 内容 |
|------|------|
| 文書ID | M33-03-REVIEW-CHECKLIST |
| バージョン | **1.0.0**（2026-09-19・発行） |
| 対象指示書 | `docs/instructions/M33-03-guards-docs-and-migration-path.md` **v1.0.0** |
| 作成者 | 設計担当 Claude（設計卓） |
| 位置づけ | **レビュー担当の主入力**（`D-736`） |

---

## 0. ★★レビュー担当への前置き

**★★★本サブは「緑になったか」では判定できない。⇒ 158 テストは*消せば緑になる*。★問うのは「主張を捨てていないか」である。**

| # | 本サブ固有の不合格 |
|---|---|
| **★★★1** | **歴史テスト 141 本の分類の内訳が無い**（**★「141 本を処理した」は報告ではない。⇒ 消した／書き直した／諮った の本数が分かれていること**） |
| **★★★2** | **`000007` を指す 4 箇所が直っていない**（**★生きた誤参照。⇒ エラーにならないため機械では捕まらない**） |
| **★★★3** | **判断がつかない歴史テストを勝手に消している**（**⇒ 止まって諮るのが正しい**） |
| **★★4** | **ガードの破壊確認が無い**（**★塞いだ／塞がなかったのどちらでも、出力が要る**） |
| **★★5** | **移行手順が「テーブルごとの `INSERT`」になっている**（**⇒ 投入順で FK 違反になる**） |
| **★★★6** | **移行を*実データで*走らせている**（**⇒ 開発者の手番である**） |
| **★7** | **マイグレを 1 本でも作っている**（**★本サブの消費は 0 本**） |

---

## A. ★★★失効参照（**段 1**）

| # | 見ること |
|---|---|
| **★★★A-1** | **`000007` を指す 4 箇所**〔`internal/model/tag.go` ／ `internal/api/middleware/user.go` ／ `internal/service/user/service.go` ×2〕**が `000009` になっているか。⇒ 名指しで確かめる** |
| **★★A-2** | **65 箇所の 3 分類の内訳が報告に在るか**〔生きた誤参照 ／ 死んだ参照 ／ 歴史記述〕**。⇒ 合計が数えた全数と合うか** |
| **★★A-3** | **全数走査の出力をファイルへ落として数えたか。⇒ `head` で切っていないか** |
| **★A-4** | **「偶然そのまま正しい」2 箇所**（`migrations/000001` を指すもの）**が明記されているか。⇒ 次に番号が動いたら壊れる** |
| **★A-5** | **歴史記述を「旧系列の」と読めるかを 1 件ずつ見たか。⇒ まとめて (c) へ放り込んでいないか** |
| **★A-6** | **`precheck_seed_data.md:138` の扱いを決め、理由を書いたか** |

---

## B. ★★★歴史テスト 141 本（**段 2**）

| # | 見ること |
|---|---|
| **★★★B-1** | **3 分類の本数が報告に在り、合計が 141 か** |
| **★★★B-2** | **(iii)**〔判断がつかない〕**が在るなら、一覧を出して止まり、開発者へ諮ったか。⇒ 勝手に消していないか** |
| **★★★B-3** | **(i) と (ii) の分け方が「新系列でも同じことを言いたいか」で説明されているか。⇒ 本数だけでは足りない** |
| **★★B-4** | **(ii) は*書き直されている*か。⇒ 消して終わりになっていないか** |
| **★B-5** | **書き直したテストが新系列に対して*実際に通る*か。⇒ 出力で確かめているか** |

---

## C. ★★golden 17 stem（**段 3**）

| # | 見ること |
|---|---|
| **★★C-1** | **17 stem と新系列の対応表が報告に在るか。⇒ 1 対 1 とは限らない** |
| **★★★C-2** | **対応が付かない stem を勝手に捨てていないか。⇒ 報告して止まるのが正しい** |
| **★★C-3** | **`SUPP-001` §5.5 規約 (19) の 3 点セットに従ったか** |
| **★C-4** | **`cmd/seedgen/main.go` の既定 stem ／ `internal/seedgen/format.go` の 6 stem 表を新系列へ合わせたか** |
| **★★C-5** | **`internal/seedgen/` の約 35 箇所を*ここで同時に*処理したか。⇒ 2 度触っていないか** |

---

## D. ★★★ガード（**段 4**）

| # | 見ること |
|---|---|
| **★★★D-1** | **塞いだ／塞がなかったの判断と*理由*が報告に在るか。⇒ どちらでもよいが、黙って通り過ぎていないか** |
| **★★★D-2** | **破壊確認の出力が在るか。⇒ 塞いだなら「塞ぐ前の形で赤くなる」、塞がなかったなら「いまも緑で通る」** |
| **★★★D-3** | **どの規則が赤くしたかまで見ているか**（**★`M33-02` の教訓＝規則 (3) が赤くしたのを規則 (4) が効いたと読んだ実例が在る**） |
| **★★D-4** | **新系列 9 本を凍結表へ入れたままか、外したか。⇒ 決めた理由が在るか。★設計卓の推しは「外す」だが指定ではない** |
| **★D-5** | **`check-migration-license.sh:71` の例外 3 行と `SUPP-001` §2.7 の整合を見たか** |
| **★D-6** | **`--self-test` が全対照 OK か。⇒ 本検査 EXIT=0 だけで満足していないか** |

---

## E. ★★★移行手順（**段 6**）

| # | 見ること |
|---|---|
| **★★★E-1** | **FK と `sqlite_sequence` を*同時に*運んでいるか。⇒ 「テーブルを 1 つずつ `INSERT`」になっていないか** |
| **★★★E-2** | **乾式で通った*出力*が在るか。⇒ FK 違反 0 件が示されているか** |
| **★★★E-3** | **開発者の手元 DB を触っていないか。⇒ 製造は自分で作った旧系列の DB で確かめる** |
| **★★E-4** | **dump の置き場が `.gitignore` の下か。⇒ `docs/` の下に無いか**（**★`docs` は丸ごと公開される**） |
| **★★E-5** | **同一性検証*とは別に*確かめたか。⇒ 同一性検証はユーザーデータの往復を見ていない** |

---

## F. ★資料

| # | 見ること |
|---|---|
| **★F-1** | **`code-facts` を再生成し、変化量を報告へ貼ったか** |
| **★★F-2** | **再生成した差分の中に*誤り*が無いかを見たか。⇒ 「新しくなった」で終えていないか。★とくに §10 はマイグレの面である** |
| **★F-3** | **`scripts/release-targets.sh` のコメント「20 本」を 10 本へ直したか** |

---

## G. ★射程

| # | 見ること |
|---|---|
| **★★★G-1** | **マイグレを 1 本も作っていないか。⇒ `ls migrations/*.up.sql` が 9 本のままか** |
| **★★★G-2** | **`migrations/` の中身を変えていないか。⇒ 同一性検証が通った状態である** |
| **★G-3** | **`docs/design/` 本体・`followup-backlog.md` の差分が 0 行か** |
| **★★G-4** | **`docs/progress/20260906-squash-research/` を消していないか。⇒ 「消す」変更は開発者の手番**（`D-196` 境界条件 3） |

---

## H. ★報告

| # | 見ること |
|---|---|
| **★★H-1** | **CHANGE 原稿が設計伝達レポートの §1 と §6 に在るか。⇒ §4 ではない**（計測点 `M-183`） |
| **H-2** | **完了報告 ＋ `progress-log` の索引行が在るか** |
| **★★★H-3** | **完了報告を書いた*後に*、報告を入力に取る検査を回し直したか** |
| **★H-4** | **全数テストの出力をファイルへ落とし、`EXIT=` と `FAIL` の件数を貼っているか** |
| **★H-5** | **着手前の版ゲート 6 点を通したことが報告に在るか** |

---

## ★★不合格の扱い

**★「高」の指摘を不採用にするときだけ開発者確認が要る。★再レビュー往復の上限は 2 回である**（`CLAUDE.md` §9）**。**

---

*以上、M33-03 レビューチェックリスト **v1.0.0**。* **★★★§0 の 1 と 3 を最初に見ること**——**本サブは「緑になったか」では判定できない。⇒ 消しても緑になる。★分類の内訳が無い報告は、それだけで差し戻してよい。**
