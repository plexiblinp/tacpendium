# M33-01 レビューチェックリスト（凍結と実測）

| 項目 | 内容 |
|------|------|
| 文書ID | M33-01-REVIEW-CHECKLIST |
| バージョン | **1.0.0**（2026-09-13・発行） |
| 対象指示書 | `docs/instructions/M33-01-freeze-and-survey.md` **v1.1.0** |
| 作成者 | 設計担当 Claude（設計卓） |
| 位置づけ | **レビュー担当の主入力**（`D-736`） |

---

## 0. ★★レビュー担当への前置き

**★★★本サブは「作らない」サブである。⇒ 成果物は*数*である。**

**★したがって不合格の形も普通と違う。⇒ 「動いた」「緑だった」では判定できない。★見るのは「数え方」である。**

| # | 本サブ固有の不合格 |
|---|---|
| **1** | **★★★目視で数えている**（式が無い）**。⇒ 設計卓は 3 件しか挙げられず、実測は 28 ファイルだった**（指示書 §0.3）**。★これが本サブが立った理由そのものである** |
| **2** | **★★層の仕分けを自分の判断で行っている**（`check-migration-license.sh` の `GAME_TABLES` に従っていない） |
| 3 | **★調査資料を正として、実物を見ずに写している** |
| 4 | **★`migrations/` を 1 バイトでも書き換えている** |

---

## 1. 束 A — ★★★数え方（**本サブの本体**）

| # | 観点 |
|---|---|
| **A-1** | **★★★§2.3 の 3 つの一覧に、それぞれ*使った式*と*件数*が冒頭に置かれているか**（指示書 §4-3）**。⇒ 式が無い一覧は不合格である** |
| **A-2** | **★★式が実際に走ることを、レビュー担当が自分で回して確かめたか。⇒ 報告の件数と一致するか** |
| **A-3** | **★★★「`Migrate` / `Steps` で歴史時点へ移動するコード」が*全数*か。⇒ 設計卓の見立ては 3 件、既知の実測は 28 ファイルである**（`M33-overview` §4.4-4）**。★3 桁も 1 桁も疑うこと** |
| **A-4** | **★golden 群が読む旧 SQL の一覧に、読んでいる*ファイル名*まで出ているか**（指示書 §2.3-2） |
| **A-5** | **★★★層 A / 層 B の仕分けが `scripts/check-migration-license.sh` の判定に従っているか**（`GAME_TABLES` を見ているか）**。⇒ 自分で「これはゲームの事実だ」と判断していないか。★`M33-overview` §2.3 の危険**（ライセンスの層が混ざる）**の唯一の防波堤である** |
| **A-6** | **★数値の隣に「実測」「算出」が書き分けられているか**（計測点 `M-157`） |

---

## 2. 束 B — ★★★凍結点と対応表

| # | 観点 |
|---|---|
| **B-1** | **★★凍結点が commit SHA で書かれているか。⇒ 「最新」「HEAD」では後から辿れない** |
| **B-2** | **★★`migrations/` の実測と `SUPP-001` §2.7**（欠番の理由）**が突き合わされているか。⇒ 食い違いが出たら、それが最大の成果である** |
| **B-3** | **★★★対応表が*1 行 1 本*か**（指示書 §4-2）**。⇒ 「まとめて 7 群へ」では検証できない。★旧 up の全数が現れているか** |
| **B-4** | **★★調査資料と食い違った点の一覧が在るか。⇒ 0 件なら「0 件」と*書いて*あるか**（指示書 §4-6。**★空欄と 0 件は違う**） |
| **B-5** | **★★★凍結点の寿命が報告に書かれているか**（指示書 §0.5。**★2026-09-13 追加**）**。⇒ 本サブの対応表は「凍結した時点」のものであり、以後 `migrations/` に版が足されるたびに追記が要る。★「最新である」と読める書き方になっていないか** |

---

## 3. 束 C — ★★移行の下ごしらえ

| # | 観点 |
|---|---|
| **C-1** | **★★ユーザーデータ表の一覧に、判定の根拠が付いているか**（指示書 §2.4-1）**。⇒ 「利用者が作ったデータが入る表」であり、seed で埋まる表ではない** |
| **C-2** | **★★投入順が*外部キーの依存*から出されているか。⇒ 依存を持たない表から並べた順序と、その根拠が在るか。★順序を間違えると FK 違反になる** |
| **C-3** | **★`sqlite_sequence` の扱いが表ごとに 1 行ずつ書かれているか**（`AUTOINCREMENT` の有無 ／ 移行時に運ぶ必要があるか） |
| **C-4** | **★★★行数を数えて*いない*か**（指示書 §2.4-4）**。⇒ 対象の DB は開発者の手元にあり、本セッションからは触れない。★「数えるための式」を置くのが正しい** |
| **C-5** | **★★★「ユーザーデータ表は 0 行だから移行は要らない」と結論していないか**（指示書 §5-3）**。⇒ 0 行なのは調査対象のツリーであり、開発者の手元は別である** |

---

## 4. 束 D — ★基準 DB

| # | 観点 |
|---|---|
| **D-1** | **★凍結した旧系列を最後まで適用して基準 DB を作る手順が在り、実際に 1 度作って通ることが示されているか** |
| **D-2** | **★★★作った DB ファイルを*コミットしていない*か**（指示書 §2.5-2 / §5-4）**。⇒ `.gitignore` の下で作ったか。★バイナリを repo へ置くと、以後それが「正しい基準」として一人歩きする** |
| **D-3** | **★手順・使ったコマンド・得られたスキーマのハッシュが報告に在るか。⇒ `M33-02` はこのハッシュと突き合わせる** |

---

## 5. 束 E — ★★越境（**本サブは「作らない」サブである**）

| # | 観点 |
|---|---|
| **E-1** | **★★★`migrations/` の差分が 0 バイトか**（指示書 §3-1）**。⇒ 1 バイトでも書き換えたら、凍結点が意味を失う** |
| **E-2** | **★★7 群を作っていないか**（指示書 §3-2。`M33-02` の射程）**。⇒ 対応表と実体を同じ手番で作ると、対応表が「作ったもの」の説明になり検証にならない** |
| **E-3** | **★歴史テストを直していないか ／ `migrations/legacy/` を作っていないか**（指示書 §3-3。`M33-03` の射程） |
| **E-4** | **★★CHANGE 消費 0・マイグレ消費 0 か。⇒ 設計書の契約を 1 つも変えていないか**（変えたくなったら射程外である） |
| **E-5** | **★★★`docs/handover/followup-backlog.md` を 1 文字も編集していないか**（**`D-838`。⇒ 停止時記録も設計伝達レポート §4 へ原稿として書く**） |
| **E-6** | **★★★着手時の版ゲートを通したか**（指示書 §0.6。**★2026-09-13 追加**）**。⇒ 本チェックリストの存在と、`M30-07` / `M32-01` が `main` に着地していることを確かめたか**（計測点 `M-169`） |
| **E-7** | **`go test ./...` に変化が無いか。★`bash scripts/check-migration-license.sh` が緑か** |

---

## 6. ★★見なくてよいもの

| # | 何を | なぜ |
|---|---|---|
| 1 | **7 群の中身の良し悪し** | **★`M33-02` の射程。⇒ 本サブは「どこへ行くか」の表までである** |
| 2 | **`M33-02` / `M33-03` の実現可能性** | **★本サブの成果を見てから決まる** |
| 3 | **開発者の手元 DB の実データ** | **★触れない**（指示書 §2.4-4） |
| 4 | **`m.Down()` / `Migrate(0)` の保証範囲** | **★`followup` の `three-point-update-down-below-golden-version`。⇒ 別手番** |
| 5 | **`import-order` のベースライン drift** | **★`followup` §J の `import-order-baseline-101-vs-99`。⇒ 本サブ起因ではない** |
| 6 | **CHANGE の起票** | **★製造は自採番しない**（`D-293`） |

---

*以上、M33-01 レビューチェックリスト **v1.0.0**。* **★★★束 A が本体である。⇒ 「調べました」で通さないこと。★式が無い一覧は、数えていないのと同じである。** **★★次に重いのは A-5 である**——**層の仕分けを自分の判断で行うと、ライセンスの層が混ざったまま 7 群が作られる。⇒ `M33-02` で気づいても遅い。**
