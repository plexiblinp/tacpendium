# M32-02 レビューチェックリスト（操作説明書の本文 ＋ UI の粗の記録）

| 項目 | 内容 |
|------|------|
| 文書ID | M32-02-REVIEW-CHECKLIST |
| バージョン | **1.0.0**（2026-09-12・発行） |
| 対象指示書 | `docs/instructions/M32-02-usermanual-body.md` **v1.2.0** |
| 作成者 | 設計担当 Claude（設計卓） |
| 位置づけ | **レビュー担当の主入力**（`D-736`） |

---

## 0. ★★レビュー担当への前置き

**★★★本サブの成果物は 2 つある。⇒ 1 つ目だけを見て通さないこと。**

| # | 成果物 | 落とされ方 |
|---|---|---|
| 1 | **操作説明書の本文**（22 章） | **★落ちない。書けば形になる** |
| **2** | **★★★UI の粗の記録** | **★★無くても「完成して見える」。⇒ ここが本サブ最大の不合格ポイントである** |

**★`B4` を起票した理由の半分が 2 である**（`checklist` の逐語＝「説明が要る箇所＝設計が負けている箇所でもある」）**。⇒ 記録が 0 件で上がってきたら、それは「粗が無かった」ではなく「見ていない」を疑うこと。**

**★★もう 1 つの型は「網羅表に化けること」である。⇒ 全機能を列挙した章は、読み手が読まない。★指示書 §2.3-3 は「よく使う操作 3 つまで」である。**

---

## 1. 束 A — ★★★第 2 の成果物（**本サブの本体**）

| # | 観点 |
|---|---|
| **A-1** | **★★★UI の粗の記録が在るか。⇒ 0 件なら、0 件である*根拠*が書かれているか**（指示書 §4.1）**。★「特に問題なし」は根拠ではない** |
| **A-2** | **★★各件に「なぜ長くなったか」の 1 行が付いているか**（指示書 §2.4-1）**。⇒ 事象だけを並べていないか** |
| **A-3** | **★「先に別の画面を見ないと分からない」箇所が挙がっているか**（指示書 §2.4-2）**。⇒ 説明の順序と画面の順序がずれる箇所である** |
| **A-4** | **★語の揺れを見つけたなら、`M29-01` の結果を名指しして報告しているか**（指示書 §2.4-3） |
| **A-5** | **★★★仕分けを*していない*か**（指示書 §2.4-4）**。⇒ 「直す / 直さない / 判断が要る」は設計卓の手番である。★製造が仕分けていたら、それは越境である** |
| **A-6** | **★設計伝達レポート §4 へ `followup` 登録候補として並んでいるか**（指示書 §2.4-5） |
| **A-7** | **★★「3 つに絞れなかった画面」が粗の記録へ回っているか**（指示書 §4.5）**。⇒ 絞れないのは画面が負けている可能性がある。★黙って 5 つ書いていないか** |

---

## 2. 束 B — ★★数と対応（**「全部書いた」を数だけで申告させない**）

| # | 観点 |
|---|---|
| **B-1** | **★★★着手時に画面数を*数え直して*いるか**（指示書 §2.1 / §4.3）**。⇒ 設計卓の「22」をそのまま使っていたら不合格である**（`M30-05` 教訓 1＝**他所から持ってきた実測値は測り直す**） |
| **B-2** | **★★対応表が成果物に在るか**（`DES-005` の節 ⇄ 章）**。⇒ 書かない節には理由が付いているか**（計測点 `M-133` の一般形） |
| **B-3** | **★廃止済みの §5.13（エクスポート）／ §5.17（moves 取込プレビュー）を書いていないか**（指示書 §3-7） |
| **B-4** | **★`M28` 以降で節が増えていた場合、その章が足されているか。⇒ 増えていなかったなら、増えていないことが報告に在るか** |

---

## 3. 束 C — ★★★暫定表記と到達経路（**★`M32-01` からの申し送り**）

| # | 観点 |
|---|---|
| **C-1** | **★★★暫定表記「現在準備中です」が*全数*外れているか**（指示書 完了条件 7.5）**。★4 か所＝`README.md` の冒頭 blockquote ／ 同 4 ファイル役割表 ／ `docs/usermanual/dist-readme.txt` の冒頭と末尾** |
| **C-2** | **★★★`README.txt` を*直接編集していない*か。⇒ 生成物である**（`DES-002` §11.2・`CHANGE-191`）**。★正本 `docs/usermanual/dist-readme.txt` を直し、`bash scripts/check-dist-readme.sh --write` で作り直したか** |
| **C-3** | **★`bash scripts/check-dist-readme.sh`（引数なし＝照合）が緑か。⇒ 正本と生成物がずれていないか** |
| **C-4** | **★★到達経路（`P-57`）を*本サブで勝手に解決していない*か。⇒ zip へ同梱する ／ Pages を設定する等は開発者判断である。★`README` の文面で「ここにあります」と書き足すだけなら可** |

---

## 4. 束 D — ★内容の質

| # | 観点 |
|---|---|
| **D-1** | **★各章が §2.3 の 5 要素を持っているか**〔何のためにあるか ／ 動線 ／ よく使う操作 3 つまで ／ つまずきどころ ／ スクリーンショットの置き場〕 |
| **D-2** | **★★「何ができるか」ではなく「何のためか」から書かれているか**（指示書 §2.3-1）**。⇒ 機能一覧の言い換えになっていないか** |
| **D-3** | **★★`M29-01` の統一語で書かれているか。⇒ `DES-005` の本文から語を*そのまま*写していないか**（指示書 §4.2。**★同書には統一前の語が残る節がありうる**）**。★走査結果が報告に添えてあるか**（完了条件 7） |
| **D-4** | **★段ごとにチェックポイントコミットが切られているか**（`CLAUDE.md` §7・指示書 §0.3）**。⇒ 1 コミットで 22 章なら、後半が薄い可能性がある** |
| **D-5** | **★後半の段が前半と同じ密度か。⇒ 分量が主敵である**（指示書 §0.3）**。★§5.2 / §5.19b / §5.20 / §5.21 は段 5 であり、薄くなりやすい** |

---

## 5. 束 E — ★越境と検査

| # | 観点 |
|---|---|
| **E-1** | **★★★見つけた UI の粗を*直していない*か**（指示書 §3-1）**。⇒ 本サブは実装差分 0 行の見込みである。★`web/src/` に差分が在ったら理由を問うこと** |
| **E-2** | **★★★`docs/handover/followup-backlog.md` を 1 文字も編集していないか**（**`D-838`。⇒ 製造・レビュー・取り込みは同書を編集しない。★停止時記録も設計伝達レポート §4 へ原稿として書く**） |
| **E-3** | **★スクリーンショットを合成していないか ／ それらしい画像を作っていないか**（指示書 §3-5）**。⇒ 撮影は開発者の手番であり、置き場を空けるのが正しい** |
| **E-4** | **★章立て・目次・置き場を決め直していないか**（指示書 §3-3）**。⇒ `M32-01` の成果物である。変えたくなったら止めて請求する** |
| **E-5** | **★設計書本体を書き換えていないか**（`CLAUDE.md` §8） |
| **E-6** | **★★★着手時の版ゲートを通したか**（指示書 §0.4）**。⇒ 手元の `M32-01` 完了報告・器・`README` 系が `main` の着地版であることを確かめたか。★確かめずに始めていたら、それだけで成果が土台からずれうる**（計測点 `M-169`） |
| **E-7** | **`bash scripts/check-doc-refs.sh` が緑か。★新設ファイルの参照が dead になっていないか** |
| **E-8** | **`go test ./...` ／ `cd web && pnpm test` に変化が無いか**（**★本サブは実装に触らない**） |
| **E-9** | **★★`docs/usermanual/` 配下は `check-md-emphasis.sh` の走査対象外である。⇒ 閉じない強調を、書いた本人が描画で確かめたことが報告に在るか** |

---

## 6. ★★見なくてよいもの

| # | 何を | なぜ |
|---|---|---|
| 1 | **操作説明書への到達経路が無いこと** | **★保留 `P-57`。⇒ 開発者判断であり本サブの射程外**（`M32-01` レポート §4-1） |
| 2 | **スクリーンショットが 1 枚も無いこと** | **★撮影は開発者の手番。⇒ 置き場が空いていれば足りる** |
| 3 | **動画が無いこと** | **★`M32` の射程外**（`D-755`） |
| 4 | **`B1` / `B2` / `B3` / `B5`** | **★開発者の手番であり、`M32` の完了条件に含めない**（`D-757`） |
| 5 | **`B6` が 3 か所にあること** | **★`followup` の `b6-classic-only-statement-triplicated` で据え置き済み**（`D-847`） |
| 6 | **`.github/workflows/nightly-crossbuild.yml:105` のコメントが古いこと** | **★`CHANGE-191` の波及。⇒ `followup` へ登録済みで、`.github/` は本サブの射程外** |
| 7 | **CHANGE の起票** | **★製造は自採番しない**（`D-293`）**。⇒ 請求は報告 §6 へ** |

---

## 7. ★★`§5.19b` について（**着手時に確かめる項目・★答えは出ている**）

**指示書 §4.4 は「`M28-02c` が着地していなければ、その章だけを残して報告し止まること」と書いている。**

**★★★2026-09-12 時点で `M28-02c` は受理済みである**（設計卓の実査）**。⇒ 止まる条件は発動しない。★§5.19b は書いてよい。**

**★ただし画面の as-built は `M28-02c` の完了報告で確かめること。⇒ 指示書の記述ではなく、着地したものを書く。**

---

*以上、M32-02 レビューチェックリスト **v1.0.0**。* **★★★束 A が本体である。⇒ 「22 章そろっている」で通さないこと。** **★★記録が 0 件のまま上がってきたら、根拠を求めること**——**`B4` を起票した理由の半分が第 2 の成果物である。**
