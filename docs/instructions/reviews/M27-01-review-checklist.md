# M27-01 レビューチェックリスト — **★★欠番。作成されなかった**

| 項目 | 内容 |
|------|------|
| 文書ID | M27-01-review-checklist |
| バージョン | **1.0.0**（2026-09-02） |
| 対象指示書 | `docs/instructions/M27-01-hit-type-and-size-labels.md` v1.0.0 |
| 状態 | **★★欠番。本ファイルはチェックリストではなく、欠番の記録である** |
| 作成者・作成日 | 設計担当 Claude（設計卓・フェーズ4 期）/ 2026-09-02 |

---

## ★★本ファイルはチェックリストではない

**`/review_plan` などが本パスへ到達した場合、ここにレビュー観点は無い。** `M27-01` は**チェックリスト無しでレビューされ、完了・受理された**（`D-687`）。**⇒ 本ファイルは、その事実を後から辿れるようにするためだけに在る。**

**★`M27-01` は 2026-09-02 に `main` へマージ済みである。⇒ いまチェックリストを書いても使われない。書かない。**

---

## 1. なぜ欠番なのか——**コミットし忘れではなく、作成し忘れである**

**★★本ファイルは一度も存在しなかった。** 作られたが commit されなかったのではない。

**`M27-01` の指示書は `レビュー` 欄に次のように書いていた。**

> **レビュー | 必要（チェックリストは段 2 の着手承認後に設計卓が起こす）**

**⇒ 設計卓が「起こす」と約束し、その手番が来たときに起こさなかった。** 段 1（列挙の確定と画面モック）の承認後、段 2 へ進む手番でチェックリストを発行するはずだったが、発行していない。

---

## 2. それでもレビューは走った

| 項目 | 実際 |
|---|---|
| レビュー報告 | `docs/progress/m27-01-review.md`（**実在する**） |
| 指摘 | **高 6 / 中 4 / 低 3 ＝ 13 件** |
| 採否 | **全 13 件採用。「高」の不採用 0 件** |
| 往復 | **0 回** |

**★★`review_plan` はチェックリストのパスを命名規則から導出するが、無ければそのまま進む。⇒ 欠落はエラーにならない。**

**⇒ レビューは走り、指摘が出て、誰も欠落に気づかなかった。** 気づいたのは 2026-09-02、開発者が別サブ（`M27-02a`）について「レビューチェックリストは作っていない？」と尋ねたときである（計測点 **`M-117`**）。

---

## 3. ★★何が失われたか（**確定できないこと**）

**チェックリストの値打ちの中心は §0.3「重大でないもの（誤判定しないこと）」に在る**——**設計卓が意図的にスコープ外・委任と定めた項目を、レビュアーが欠陥として挙げるのを防ぐ。**

**`M27-01` には §0.3 に入るはずの項目が実際に在った。**

| # | チェックリストが在れば「欠陥ではない」と明示できた項目 |
|---|---|
| 1 | **`punishHitTypes`（確定反撃タブの振り分け）を触っていないこと**——**開発者が明示的に別サブへ回した** |
| 2 | **`大1` / `大2` の整理に踏み込んだこと**——**当初「触らない」と定めていたが、開発者が判断を供給した** |
| 3 | **指示書に無い info-mark を追加したこと**——**開発者の追加指示** |
| 4 | **既存 4 値を 1 文字も変えていないこと**——**意図どおり** |

**★★13 件のうち何件が本来 §0.3 で除外されるべきだったかは、いまから判定できない。** 全件採用・往復 0 で着地しているため実害の証拠は無いが、**「無かった」ことの証明にもならない。⇒ 分からない、が正確な記述である。**

---

## 4. 一般形——**約束したものが作られなくても、下流が静かに動き続ける**

**★★これは `M27-01` 自身が `check-enum-sync.sh` について報告したのと同じ型である**——**「守るべきものはあるが観測が無い」。**

**★今回の形はもう一段わかりにくい。「観測が無い」のではなく「約束が果たされなくても、下流が正常に見える動き方をする」。**

| # | 何が無かったか |
|---|---|
| 1 | **`reviews/` にファイルが在ることを見る機械検査が無い**（`check-instruction-format.sh` は版数と禁則表現しか見ない） |
| 2 | **`review_plan` はチェックリストの不在をエラーにしない**（パスを導出し、無ければそのまま進む） |
| 3 | **指示書の `レビュー` 欄に「必要」と書いてあっても、それを満たす成果物の存在を誰も突き合わせない** |

**⇒ 是正として、指示書の `レビュー` 欄へ「誰がいつ起こすか」を必ず書く運用にした**（`D-691`）。**★ただしこれは規約であって検査ではない。★同じことがまた起きうる。**

---

## 5. 記録

| 項目 | 内容 |
|---|---|
| 裁定 | **`D-691`**（`M27-02a` のチェックリスト起票と、欄の書き直し） ／ **`D-692`**（本ファイルと `M26-01` 側の欠番記録の作成） |
| 計測点 | **`M-117`** |
| 指摘者 | **開発者**（2026-09-02） |
| `M27-01` の状態 | **完了・受理・`main` へマージ済み**（`D-687`。`CHANGE-151` 反映済み） |

---

*以上。* **★★本ファイルはレビュー観点を持たない。`M27-01` のレビューをやり直す必要は無い。**
