# M39-01 レビューチェックリスト（`startup_basis` の不変条件の是正）

| 項目 | 内容 |
|------|------|
| 文書ID | M39-01-REVIEW-CHECKLIST |
| バージョン | **1.1.0**（2026-09-19・**★★★是正の形が案 A〔新系列への折り込み〕へ変わったことへ追随した**＝`D-910`。**★B 群を全面差し替え。★C 群**〔`down` の形〕**は案 A では効かないため差し替えた**） ／ **1.0.0**（2026-09-19・発行） |
| 対象指示書 | `docs/instructions/M39-01-startup-basis-invariant-fix.md` **v1.1.0** |
| 作成者 | 設計担当 Claude（設計卓） |
| 位置づけ | **レビュー担当の主入力**（`D-736`） |

---

## 0. ★★レビュー担当への前置き

**★★★本サブは「132 行が 0 になったか」では判定できない。⇒ 案 A では、その 132 行は*最初から存在しない*。★問うのは 2 つである＝(1) 動いたのが `startup_basis` 列の 132 セル*だけ*か ／ (2) 次の seed 波で*また*崩れないか。**

**★★もう 1 つ＝「実害は出ていない」は `M33-03` が 1 経路だけを見て出した結論である。⇒ 本サブがそれを引き写していたら、母集団は 1 経路のままである。**

| # | 本サブ固有の不合格 |
|---|---|
| **★★★1** | **`startup_basis` を読む経路の*総数*が報告に無い**（**★「全数を見た」「確かめた」は報告ではない**） |
| **★★★2** | **ガードが `TestRun_HEAD_*` へ上がっていない**（**★版固定のままなら直因が残る**） |
| **★★★3** | **段 4 の破壊確認の出力が無い**（**★1 行戻して赤くなることを示していない**） |
| **★★★4** | **`character_data/` の 6 行とマイグレが別コミットになっている**（**⇒ その間の状態が赤い**） |
| **★★★5** | **差分が `startup_basis` 列のちょうど 132 セルであることを*機械で*示していない**（**★目視で「直した」と書いていないか。⇒ 巨大な INSERT 群の中の 132 セルは目で追えない**） |
| **★★★6** | **同一性の再検証を回していない**（**★`M33-02` の「全一致」は本サブで意図的に手放す。⇒ 手放した先が「ちょうど 132 セル差」であることの立証が要る**） |
| **★★★7** | **マイグレを 1 本でも作っている**（**★案 A の消費は 0 本。⇒ 作るなら §0.6.2 のどれかに当たったときだけであり、その場合は止まって報告するのが正しい**） |
| **★★★8** | **検証 DB の作り直しの手順が無い**（**★書き換えた `000001`〜`000009` は適用済み DB では再実行されない。⇒ 開発者の手元だけ是正されないまま残る**） |
| **★★9** | **`unknown` を*減らす*方向の変更が混ざっている**（**★followup の 51 行は射程外**） |

---

## A. ★★★読む経路の全数（**段 1**）

| # | 見ること |
|---|---|
| **★★★A-1** | **Go 側とフロント側の両方を数えたか。⇒ `D-906` (5) が「フロントも含めて全部」を既に示している** |
| **★★★A-2** | **総数が報告に在るか。⇒ 数が書かれていない報告は、それだけで差し戻してよい** |
| **★★A-3** | **全数走査の出力をファイルへ落として数えたか。⇒ `head` で切っていないか** |
| **★★★A-4** | **経路ごとに「132 行が `unknown` へ動くと出力が変わるか」を判定しているか。⇒ 経路を*並べた*だけになっていないか** |
| **★★★A-5** | **出力が変わる経路が在ったなら、止まって報告したか。⇒ 黙って是正していないか** |
| **★A-6** | **`usesFirstHitStartup` の 1 本だけだったなら、そのことを*実測で*示しているか** |

---

## B. ★★★新系列への折り込み（**段 2**）

| # | 見ること |
|---|---|
| **★★★B-1** | **`ls migrations/*.up.sql` が 9 本のままか。⇒ マイグレを作っていないか** |
| **★★★B-2** | **132 行が新系列の*どのファイルの*どこに在るかを名指ししているか。⇒ ファイルごとの件数の内訳が在るか** |
| **★★★B-3** | **そのファイルが生成物か手書きかを判定したか**（`D-707` の判別述語）**。⇒ 生成物なら再生成の経路で直したか** |
| **★★★B-4** | **差分が `startup_basis` 列のちょうど 132 セルであることを*機械で*示しているか。⇒ 目視で済ませていないか** |
| **★★★B-5** | **他の列・他の行が 1 セルも動いていないか**（**★生成物の再生成では行順・空白・引用符まで動きうる**） |
| **★★B-6** | **`down` を触っていないか。⇒ 案 A では `down` の主張は動かない** |
| **★★★B-7** | **`bash scripts/check-migration-license.sh` が EXIT=0 ＋ `--self-test` が全対照 OK か。⇒ `M33-03` が規則 (4) を凍結行にも評価させている**（`D-906` (2)） |

---

## C. ★★★同一性の再検証（**§2.2.3**）

| # | 見ること |
|---|---|
| **★★★C-1** | **`M33-02` と*同じ手順*で再走させたか。⇒ 別の比較に置き換えていないか** |
| **★★★C-2** | **差分がちょうど `startup_basis` の 132 セルか。⇒ 1 セルでも超えていたら §0.6.2 の条件 3 であり、止まって報告するのが正しい** |
| **★★C-3** | **`SUPP-001` §2.7 の「全一致」の記述が失効することを、原稿として設計伝達レポートへ書いたか。⇒ 設計書本体を書き換えていないか** |
| **★★★C-4** | **案 B へ戻ったのなら、戻った理由が §0.6.2 の 3 条件のどれかで説明されているか。⇒ 「そのほうが楽だから」になっていないか** |

---

## D. ★★★`character_data/` の 6 行（**段 3**）

| # | 見ること |
|---|---|
| **★★★D-1** | **段 2 と*同じコミット*に載っているか。⇒ 分けていないか** |
| **★★D-2** | **6 行が実査されているか。⇒ 指示書の列挙を引き写していないか**（**★`M33-03` レポートの実測値である**） |
| **★★D-3** | **`TestCSVAndDBAgreeOnFrameCostColumns` ／ `TestRun_HEAD_SeedValuesMatchCSV` が緑か** |
| **★★★D-4** | **golden が動いたか動かなかったかを*実測で*判定し、報告へ書いたか。⇒ 「やらなかった」で済ませていないか。★「要らなかった」とは別である** |
| **★D-5** | **動いたなら `SUPP-001` §5.5 規約 (19) の 3 点セットに従ったか** |
| **★★★D-6** | **検証 DB の作り直しの手順が在るか。⇒ 書き換えた `000001`〜`000009` は適用済み DB では再実行されない** |
| **★★★D-7** | **開発者の検証 DB を触っていないか。⇒ 製造は手順を出すところまでである** |

---

## E. ★★★ガードの常設化（**段 4**）

| # | 見ること |
|---|---|
| **★★★E-1** | **不変条件が HEAD に対する常設テストとして置かれているか。⇒ 版数を名指ししていないか**（`D-714`） |
| **★★★E-2** | **破壊確認の出力が在るか。⇒ 1 行を `standalone` へ戻すと赤くなることを示しているか。★戻して緑のままなら、そのテストは何も見ていない** |
| **★★E-3** | **旧テスト**〔v64 固定〕**の処遇と*理由*が報告に在るか。⇒ 「緑にするため」に消していないか** |
| **★★★E-3b** | **案 A では歯止めがガードだけであることを踏まえているか**（**★折り込むと「是正した」記録が `migrations/` に 1 行も残らない**） |
| **★★E-4** | **逆向きの不変条件**〔`startup` が在るのに `unknown`〕**を巻き込んでいないか。⇒ followup の 51 行は別の性格である** |

---

## F. ★射程

| # | 見ること |
|---|---|
| **★★★F-1** | **マイグレ消費が 0 本か。⇒ `ls migrations/*.up.sql` が 9 本か** |
| **★★★F-2** | **新系列 9 本のうち、変わったのが `startup_basis` 列の 132 セルだけか**（**★★本サブは 9 本を書き換えてよい**＝`D-910` のカーブアウト。**⇒ 許されている範囲がそこまでである**） |
| **★★F-3** | **`unknown` を*減らす*方向の変更が 1 件も無いか**（**★`DES-003` §3.3 の三値目である**） |
| **★F-4** | **`docs/design/` 本体・`followup-backlog.md` の差分が 0 行か**（`CLAUDE.md` §8 ／ `D-838`） |

---

## G. ★報告

| # | 見ること |
|---|---|
| **★★G-1** | **CHANGE 原稿が設計伝達レポートの §1 と §6 に在るか。⇒ §4 ではない**（計測点 `M-183`） |
| **G-2** | **完了報告 ＋ `progress-log` の索引行が在るか** |
| **★★★G-3** | **完了報告を書いた*後に*、報告を入力に取る検査を回し直したか** |
| **★G-4** | **全数テストの出力をファイルへ落とし、`EXIT=` と `FAIL` の件数を貼っているか** |
| **★★G-5** | **着手前の版ゲート 8 点を通したことが報告に在るか。⇒ とくに「違反件数が 132 であること」の実測**（§0.5 の 4） |

---

## ★★不合格の扱い

**★「高」の指摘を不採用にするときだけ開発者確認が要る。★再レビュー往復の上限は 2 回である**（`CLAUDE.md` §9）**。**

---

*以上、M39-01 レビューチェックリスト **v1.1.0**。* **★★★§0 の 5 と 6 を最初に見ること**——**本サブは適用済みの新系列 9 本を書き換える。⇒ 許されているのは `startup_basis` 列の 132 セルだけであり、それを*機械で*示していない報告は、それだけで差し戻してよい。★もう 1 つ、歯止めは段 4 のガードだけである。**
