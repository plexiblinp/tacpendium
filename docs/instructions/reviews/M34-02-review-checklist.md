# M34-02 レビューチェックリスト（常駐ランチャと黒窓の除去）

| 項目 | 内容 |
|------|------|
| 文書ID | M34-02-REVIEW-CHECKLIST |
| バージョン | **1.0.0** |
| 作成日 | 2026-09-10 |
| 対象指示書 | `docs/instructions/M34-02-tray-residency-and-no-console.md` **v1.0.0** |
| 発行 | 設計卓（`D-802`） |
| 使い方 | `/review_plan` の入力。**★本書は指示書の完了条件を写したものではない。⇒ 「緑でも壊れているもの」を探すための束である** |

---

## 0. ★★★レビュー担当への前置き

**★★★本サブは「失敗が見えなくなる方向」の変更である。⇒ 他のサブと決定的に違うのはここである。**

**★通常のサブは、壊れると赤くなる。★本サブは、壊れると何も出なくなる。**

| 束 | 何を見るか |
|---|---|
| **A** | **★★★段の順序**（**本サブの設計そのもの**） |
| **B** | **ログと `config.toml` の基準**（段 1） |
| **C** | **失敗時の通知経路**（段 4） |
| **D** | **黒窓の除去**（段 5） |
| **E** | **`internal/desktop/` の契約を守っているか** |
| **F** | **依存とライセンス** |

---

## 1. 束 A — ★★★段の順序（**最初に見ること**）

| # | 見ること | 落ちる形 |
|---|---|---|
| **★★★A-1** | **段 1〜4 が段 5 より前に着地しているか。⇒ コミットの順序で確かめること** | **★★★段 5 を先にやると、「起動しないが何も出ない」状態で以後の手番を進めることになる。⇒ 指示書 §0.1 が「本サブで最も守ってほしい 1 行」と書いた点である。★順序が崩れていたら、それ自体が 1 件** |
| **★★A-2** | **段 1 と段 4 の破壊確認が、段 5 より前の時点で実測されているか** | **★★段 5 の後に測ると、失敗が見えないので測れない。⇒ 「後で測った」なら、その実測は信用できない** |
| **★A-3** | **各段が独立したコミットに分かれているか** | **★1 コミットに畳むと、どの段で何が壊れたかを切り分けられない** |

---

## 2. 束 B — ログと `config.toml` の基準（段 1）

| # | 見ること | 落ちる形 |
|---|---|---|
| **★★★B-1** | **★★破壊確認が在るか＝`config.toml` を意図的に別の場所へ置いて「読まれないこと」を実測しているか** | **★★★読めてしまうなら、基準が一意になっていない。⇒ この確認が無ければ、基準を変えたことの証明が 1 つも無い** |
| **★★B-2** | **採らなかった案の欠点が書かれているか**（`M34-01` 完了報告 §4.4 の 3 案） | **★「案 X を採った」だけでは、次に見直す人が同じ検討を最初からやり直す** |
| **★★★B-3** | **全 OS の既存挙動が変わらないことを試験で固定しているか** | **★★★これは Windows だけの変更ではない。⇒ Linux / macOS の既定の解決先が黙って動くと、devContainer の開発が壊れる。★試験が無ければ 1 件** |
| **★★B-4** | **`config.toml` の不在の扱いを決めた記録が在るか**（**今までどおり既定値で続行するか、知らせるか**） | **★決めずに通すと、`M34-01` が見つけた害**（**利用者の設定が黙って無視される**）**がそのまま残る。⇒ 「変えない」判断でもよいが、理由が要る** |
| **★B-5** | **既存の利用者に移行が要るかを判断しているか** | **★既存の利用者は `config.toml` を今の場所に置いている。⇒ 要るなら手順が要る** |

---

## 3. 束 C — ★★★失敗時の通知経路（段 4）

| # | 見ること | 落ちる形 |
|---|---|---|
| **★★★C-1** | **★★破壊確認が在るか＝わざと起動を失敗させて**（例＝ポートを埋める）**`MessageBox` が出ることを実測しているか** | **★★★出ないなら、この段は何もしていない。⇒ 「経路を作った」は「経路が効く」の証拠にならない。★`M34-01` が同じ形を実演している**（**サイズガードをわざと崩して門が効くことを測った**） |
| **★★C-2** | **メニュー項目の失敗も同じ経路に載っているか** | **★★黙って何も起きないのが最悪である。⇒ 起動失敗だけ塞いで、項目の失敗を落としていないか** |
| **★★C-3** | **ログへ書く経路が、標準出力が消える前に確かめられているか** | **★段 5 の後に確かめると、書けていなくても気づけない** |
| **★C-4** | **段 2 で引数エラーを「エラーにする」と選んだなら、それもこの経路に載っているか** | **★`-H=windowsgui` 下では標準エラーも消える。⇒ 載せないと、打ち間違えた引数が黙って無視される** |

---

## 4. 束 D — 黒窓の除去（段 5）

| # | 見ること | 落ちる形 |
|---|---|---|
| **★★★D-1** | **成果物が GUI サブシステムであることを機械で確かめているか**（**PE ヘッダのサブシステム値**） | **★★★「窓が出なかった」は証拠にならない。⇒ たまたま出なかっただけかもしれない。★機械の判定でなければ 1 件** |
| **★★D-2** | **`make check-windows`**（`CHANGE-173`）**が引き続き通るか** | **★`-H=windowsgui` はリンクの形を変える。⇒ ビルド検査が壊れていないか** |
| **★D-3** | **`Makefile` の `build-windows` 以外を触っていないか** | **★他のビルド経路に `-H=windowsgui` が漏れると、Linux のビルドが壊れる** |

---

## 5. 束 E — `internal/desktop/` の契約

| # | 見ること | 落ちる形 |
|---|---|---|
| **★★★E-1** | **`ErrUnsupported` を致命エラーとして扱っていないか** | **★★★契約は「呼び手はトレイ無しで動作を続ける」である**（`SUPP-001` §5.2 の注記）**。⇒ 致命にすると非 Windows でアプリが起動しなくなる。★Linux の `go test` は通るので、テストでは捕まらない** |
| **★★E-2** | **`Confirm` が非 Windows で常に false を返すことに、呼び手が詰まっていないか** | **★終了確認が常に false だと、非 Windows で終了できない形になりうる** |
| **★★★E-3** | **`ValidateURL` が残っているか**（§2.6.1 でブラウザ起動を `pkg/browser` へ寄せた場合） | **★★★これは設定由来の文字列が任意のプロトコル起動に化けるのを防ぐ唯一の関門である。⇒ 委譲のときに外すと、関門ごと消える** |
| **★E-4** | **`OpenFolder` の `os.Stat` を迂回していないか** | **★起動して待たないため、不正パスが無言で失敗する。⇒ `Stat` がその唯一の防波堤である** |
| **★E-5** | **`M34-01` が「持たせなかったもの」を勝手に足していないか**（**バルーン通知 ／ アイコンのアニメーション ／ スタートアップ登録 ／ サービス化**） | **★足すなら提案が先である。⇒ 黙って足すと射程が広がる** |

---

## 6. 束 F — 依存・ライセンス・射程

| # | 見ること | 落ちる形 |
|---|---|---|
| **★★★F-1** | **`go.mod` / `go.sum` の差分が 0 行か。⇒ 増えているなら、事前提案の記録が在るか** | **★★§2.6.1 は依存を**減らす**方向である**（自前実装 → 既存の `pkg/browser`）**。⇒ 増えていたら、それ自体が説明を要する** |
| **★★F-2** | **`scripts/build-windows.ps1` を移植したなら、`scripts/` 配下へ置いていないか** | **★★`scripts/` 配下は `REUSE.toml` の層 C**（`MIT`）**である。⇒ 層 A であるべきものが `MIT` になる。★`M34-01` が `cmd/genicon/` で回避した罠と同じである** |
| **★★F-3** | **`REUSE.toml` の差分が 0 行か** | **★層 A は既定で解決される。⇒ 触ると壊す** |
| **★★★F-4** | **`determineBindHost` / `determineBindAddr` が無改変か** | **★★★判断 2 で「`127.0.0.1` 固定にしない」と決着している。⇒ 固定すると LAN 共有が壊れる。★ブラウザで開く URL だけが `127.0.0.1` である** |
| **★F-5** | **`printStartupNotice` の文面が直っているか**（**「終了するにはこのウィンドウを閉じてください。」が失効する**） | **★常駐化した後も残っていると、そのまま利用者へ出る** |

---

## 7. ★★束 G — メニューの中身

| # | 見ること | 落ちる形 |
|---|---|---|
| **★★G-1** | **確定 4 件が揃っているか**〔ブラウザで開く ／ 設定画面をブラウザで開く ／ ログフォルダを開く ／ 終了〕 | **★★開発者が確定した中身である**（`M34-overview` §5.1.6） |
| **★★G-2** | **候補 2 件**（DB のフォルダ ／ exe のフォルダ）**の採否が、段 1 の選択と結びつけて説明されているか** | **★★ログを `%APPDATA%` へ寄せたなら DB と重なり、exe の位置へ寄せたならログフォルダの親になる。⇒ 段 1 と切り離して決めていたら、判断が空中に浮いている** |
| **★★★G-3** | **トレイ側で QR を描いていないか** | **★★★Go の QR ライブラリが増える＋Win32 で画像を描くウィンドウが要る。⇒ 「設定画面をブラウザで開く」までにする決着である。★描いていたら射程外** |
| **★G-4** | **LAN 共有が OFF のときの出し分けを作っていないか** | **★開発者の判断は「LAN の ON / OFF を気にせず設定画面を開く」である。⇒ 出し分けを作ったら射程外** |

---

## 8. ★★見なくてよいもの

| # | 何を | なぜ |
|---|---|---|
| 1 | **`internal/desktop/` の Win32 呼び出しの中身** | **`M34-01` で受理済みである。⇒ 本サブが変えたなら別だが、変えていないなら見ない** |
| 2 | **二重起動の判別のアルゴリズムの良し悪し** | **受領した実装方式に従う。⇒ 方式そのものは `D-790` で採否済み** |
| 3 | **`data-testid` の登録** | **`CHANGE-171` が一覧運用を畳んでいる。⇒ 登録義務は無い** |

---

*以上、M34-02 レビューチェックリスト **v1.0.0**。* **★★★本書で最も重いのは 束 A・C-1・D-1 の 3 つである。** **★段の順序が崩れていたら、他がすべて緑でも設計が守られていない。** **★C-1 と D-1 はどちらも「作った」を「効く」の証拠と読ませない形であり、実測が無ければそれ自体が 1 件である。**
