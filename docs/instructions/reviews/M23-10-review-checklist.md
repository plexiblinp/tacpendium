# M23-10 レビューチェックリスト

| 項目 | 内容 |
|------|------|
| 対象指示書 | `docs/instructions/M23-10-foreign-key-enforcement-root-fix.md` v1.0.0（`P-04` の根治） |
| 文書ID | M23-10-REVIEW |
| 対象指示書ID | M23-10 |
| レビューモデル | 高精度レビュー（実モデル名は `docs/human-notes/model-allocation.md` が正本。実使用は開発者判断） |
| バージョン | 1.0.0 |
| 作成者・作成日 | 設計担当 Claude（設計卓／M23 期）/ 2026-08-23 |

> 文書ID ⇄ 実パスの正は `docs/handover/docs-map.md`（自動生成）。

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| 1.0.0 | 2026-08-23 | 初版（指示書 v1.0.0 と対）。**最重要ゲート＝「回帰ゲートが『同時に掴む』形で書かれていること」と「その形でないと現行実装でも緑になることが破壊確認で示されていること」。** |

---

## 0. レビュー担当への前置き

### 0.1 レビュー前の準備

1. **★指示書 §1.2 を先に読む**——**本サブの本体は約 5 行の DSN 変更ではなく、§4.2 の回帰ゲートである。**
2. **`docs/progress/20260823-m23-08-p04-root-fix-feasibility.md` を読む**（**★実測値の一次資料。設計卓の見立てではない**）。
3. **完了報告に Plan Mode の実査 7 件（指示書 §3.3）と破壊確認 3 件（§5.2）が揃っていることを先に確認する。**

### 0.2 レビューの基本姿勢

- **★★「FK が ON になったか」だけを見て終わらないこと。** **見るべきは「明日誰かが元へ戻したときに落ちるか」である。**
- **★★とくに §5.2-2 の破壊確認を読むこと**——**「1 本ずつ取って返す形なら現行実装でも緑になる」ことが示されていないなら、回帰ゲートが要件を満たしている証拠が無い。**
- **★本サブは既存の orphan を掃除しない。** **掃除されていないことを指摘しない。**
- **★画面を 1 バイトも触らないサブである。** 触っていたら §9 である。

### 0.3 ★重大でないもの（**誤判定しないこと**）

| # | 誤判定しやすい状態 | 実は仕様である理由 |
|---|---|---|
| N-1 | **既存の orphan 19 件が掃除されていない** | **指示書 §1.5-2。★データを消す変更であり開発者の判断である**（`CLAUDE.md` §10）。**★FK=ON は供給を止めるが、溜まった分は消さない** |
| N-2 | **`combo_tags` の orphan が生む「画面からはタグを消せない」状態が残っている** | **同上。★本サブでは解消しない。** **完了報告が「解消していない」と明記していれば正しい**（明記が無ければ §10） |
| N-3 | **`tag.Delete` が CASCADE 頼みのまま（明示削除になっていない）** | **指示書 §1.5-1。★本サブは `P-04` の前提そのものを外す。根治と同じ手番で「CASCADE に頼らない箇所」を増やすのは方向が逆である**（**D-494**） |
| N-4 | **`M23-08` が入れた明示削除が撤去されていない** | **指示書 §2.2。★二重の保険として残す。撤去は本サブの判断事項ではない** |
| N-5 | **接続数の上限が設定されていない** | **指示書 §1.5-3。★FK とは独立の論点であり、混ぜると射程が膨らむ** |
| N-6 | **FK 句（`ON DELETE` の有無）がスキーマで変わっていない** | **指示書 §1.5-4。★SQLite は制約の `ALTER` を持たず、表の作り直しになる** |
| N-7 | **`journal_mode` まで接続文字列へ移っている** | **指示書 §4.1-2。★4 つとも移すのが設計卓の判断である。** **性質が違うことはコメントに書かれていれば足りる** |
| N-8 | **マイグレーションが FK=OFF のまま走る** | **指示書 §1.3-1 / §3.3-3。★マイグレーションは接続を独自に開いており、本サブの経路を通らない。これは意図である** |
| N-9 | **`PRAGMA defer_foreign_keys` が使われていない** | **指示書 §1.5-6。★逃げ道の記録であって推奨ではない。使っていたら逆に §9 である** |

### 0.4 報告フォーマット

- 各節「OK / 重大（§9）/ 軽微（§10）/ 質問（§11）」。

---

## 1. 設計・パターンとの照合（最重要）

- [ ] **★★回帰ゲートが「複数の接続を同時に保持したうえで」FK の状態を読んでいる**（指示書 §4.2-2）。**★1 本ずつ取って返す形なら §9 である**——**プールが同じ接続を使い回すため、現行実装でも緑になる＝何も守らないテストである。**
- [ ] **★★破壊確認 2（1 本ずつ取る形にすると現行実装でも緑になる）が実施され、出力が完了報告に在る**（同 §5.2-2）。**★これが無いと、上のゲートが要件を満たしている証拠が無い。**
- [ ] **★破壊確認 1（接続の開き方を現行の形へ戻すと赤くなる）が実施されている**（同 §5.2-1）。
- [ ] **接続が張られるたびに PRAGMA が適用される形になっている**（同 §4.1-1）。**プール確立後に 1 回実行する形が残っていない。**
- [ ] **★「なぜ 4 つとも接続文字列へ移すのか」がコードのコメントに書かれている**（同 §4.1-2）。**★無いと後任が 1 つずつ戻す。**
- [ ] **CASCADE が実際に発火することを、宣言ではなく挙動で主張するテストが在る**（同 §5.1-2）。

## 2. データ・API 契約・スキーマの不変（非破壊性）

- [ ] **スキーマ変更・FK 句の変更・マイグレーションの追加が無い**（指示書 §1.5-4 / §4.5）。**★`ls migrations/` の実査結果が完了報告に在る。**
- [ ] **API の応答形・ステータスが変わっていない**（同 §1.5-5）。
- [ ] **既存の orphan が 1 件も消えていない**（同 §1.5-2。**★消えていたら §9 である**）。

## 3. 画面を触っていないこと

- [ ] **★`web/src/` の変更が 0 バイトである**（指示書 §4.6）。**証跡が完了報告に在る。**

## 4. テストの妥当性（指示書 §5 ↔ §7 DoD）

- [ ] **§5.1 の 4 件がすべて在る**（**★とくに 1＝回帰ゲート、4＝パスに `?` を含めても接続できる**）。
- [ ] **§5.2 の破壊確認 3 件のコマンドと出力が完了報告に在る。**
- [ ] **`go test ./...` / `make e2e` の件数が貼られている**（**E-125**。**★終了コードだけを根拠にしていない**）。
- [ ] **`M23-01`〜`M23-08` が足したテストが緑のままである。**

## 5. 設計意図との整合（精神の確認）

- [ ] **★★本サブが「約 5 行の変更」ではなく「その 5 行が戻されたことを検出し続けるテスト」として実装されている**（指示書 §1.2）。
- [ ] **★§3.3-1（実 DB の orphan を FK=ON の接続で触ると何が起きるか）の実査結果が完了報告に在る**（**★空の DB から始まるテストはこの経路を構造的に検出できない**）。
- [ ] **★§3.3-3（マイグレーションが別経路で接続を開いていること）が実物で確かめられている**（**★同じ経路を通っていたら本サブの前提が崩れる**）。
- [ ] **§4.4 の否定形確認 5 件がすべて完了報告に在る。**

## 6. コード品質・規約遵守

- [ ] `gofmt` / lint が通っている。
- [ ] **`console.log` / `fmt.Println` が本番コードに残っていない。**
- [ ] **★「N か所」と書いたコメントを機械的に増減させていない**（指示書 §4.3 末尾。**`M23-07` 教訓 7-1**）。

## 7. 既存挙動の温存（非破壊性）

- [ ] **`M23-08` の明示削除が残っている**（指示書 §2.2）。
- [ ] **二重に消しても落ちないことを主張するテストが在る**（同 §5.1-3）。
- [ ] **接続プールの上限が設定されていない**（同 §1.5-3）。

## 8. ドキュメント・進捗ログ

- [ ] **`docs/progress/progress-log.md` へ 1 行追記されている。**
- [ ] **設計書本体が編集されていない**（**★反映は設計卓が `CHANGE-142` で行う**）。
- [ ] **完了報告 `docs/progress/m23-10-completion-report.md` に指示書 §7.5 の 8 件が揃っている。**

## 9. 重大な問題の判定基準（完了承認を妨げる）

- **★★回帰ゲートが「1 本ずつ取って返す」形で書かれている**（指示書 §4.2-2）。**これが最重要ゲートである。** **★現行実装のままでも緑になり、何も守らない。**
- **★★破壊確認 2 が実施されていない、または出力が報告に無い**（同 §5.2-2）。**★上のゲートが要件を満たしている証拠が無い。**
- **★破壊確認 1 または 3 が実施されていない、または出力が報告に無い**（同 §5.2）。
- **回帰ゲートそのものが無い**（同 §4.2）。**★DSN 化だけで終わっているなら、本サブは目的を果たしていない。**
- **★既存の orphan を掃除している**（同 §1.5-2。**★データを消す変更である**）。
- **★`PRAGMA defer_foreign_keys` を使っている**（同 §1.5-6。**★使う必要が生じたなら報告して止まる事項である**）。
- **★接続文字列に `foreign_keys` の指定が無い**（＝直っていない）。
- **★`web/src/` に変更がある**（同 §4.6）。
- **スキーマ変更・マイグレの追加・FK 句の変更がある**（同 §1.5-4 / §4.5）。
- **`M23-08` の明示削除を撤去している**（同 §2.2）。
- **接続数の上限を設定している**（同 §1.5-3）。
- **★完了報告が「`combo_tags` の実害が解消していない」を明記していない、または解消したように読める書き方をしている**（同 §7.5-4）。
- **スコープ外 6 件（同 §1.5）のいずれかへ踏み込んでいる。**
- **指示書 §7.5 の 8 件のいずれかが完了報告に無い。**

## 10. 軽微な問題の判定基準（持ち越し許容）

- パスのエスケープの方式の選び方（**★テストが 1 本在れば足りる**）。
- 回帰ゲートの置き場・ヘルパの切り方。
- 接続文字列を組み立てる関数の粒度。
- テストのデータ組み立ての作法。
- 同時に張る接続の本数（**★16 本は推奨であって要件ではない。1 本でないこと、プールが使い回せない本数であることが要件である**）。

## 11. 質問・確認事項のフォーマット

- 「指示書 §X.X / 実現可能性レポート §X に対し実装が Z。意図確認したい」の形で根拠節併記。

## 12. レビュー完了の判定

- §1〜§8 が全て OK、§9 重大ゼロ（**特に「回帰ゲートが同時に掴む形であること」「破壊確認 3 件」「orphan を消していないこと」「`web/src/` が 0 バイト」**）、§0.1 の Plan Mode 実査 7 件が揃っている。§10 軽微は持ち越し可。

### 12.1 ★本チェックリスト自身の検査（**発行前に設計担当が行う**）

- [ ] **本書と対の指示書の間に、節番号の食い違いが無い。**
  → 本書が参照する指示書の節は §1.2 / §1.3 / §1.5 / §2.2 / §3.3 / §4.1 / §4.2 / §4.3 / §4.5 / §4.6 / §5 / §7 であり、**指示書側に実在する**。
- [ ] **指示書が禁じていない事項を「重大」として求めている項目が無い。**
  → §9 の各項目は指示書 §1.5 / §2.2 / §4 / §5 の禁止・固定と 1 対 1 で対応する。
- [ ] **達成不能な条件を重大として求めていない。**
  → **「既存の orphan が掃除されていること」を求めていない**（§0.3 N-1。**開発者の判断**）。
  → **「`combo_tags` の実害が解消していること」を求めていない**（§0.3 N-2。**★掃除しない限り残る**）。
  → **「`tag.Delete` が明示削除になっていること」を求めていない**（§0.3 N-3。**方向が逆である**）。
  → **「FK 句がスキーマで直っていること」を求めていない**（§0.3 N-6。**SQLite は制約の `ALTER` を持たない**）。
  → **「マイグレーションでも FK が有効であること」を求めていない**（§0.3 N-8。**★マイグレーションは別経路であり、そこが FK=OFF であることは意図である**）。

---

*以上、M23-10 レビューチェックリスト v1.0.0。配置 `docs/instructions/reviews/M23-10-review-checklist.md`。指示書 v1.0.0 と対。「回帰ゲートが同時に掴む形であること」を最重要ゲートとする。*
