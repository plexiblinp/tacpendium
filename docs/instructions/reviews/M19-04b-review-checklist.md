# M19-04b レビューチェックリスト

| 項目 | 内容 |
|------|------|
| 文書ID | M19-04B-REVIEW |
| 対象指示書 | `docs/instructions/M19-04b-chain-cancel-and-derivations.md` **v1.1.0** |
| 対象指示書ID | M19-04b |
| CHANGE | **なし**（スキーマ変更ゼロ・DES 改訂なし。先例＝M14-03e） |
| レビューモデル | Sonnet 4.6（model-allocation。実使用は開発者判断） |
| バージョン | **1.0.1** |
| 作成者・作成日 | 親チャット（設計卓）/ 2026-08-02 |

> **文書ID ⇄ 実パスの正は `docs/handover/docs-map.md`（自動生成）。**

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| **1.0.1** | **2026-08-07** | **メタ表へ `文書ID` 行を追加＝ボード **D-203**。`docs-map` 未収録の是正**（`scripts/generate-docs-map.sh` は `文書ID` 行から §1 を抽出するため、行が無い文書は載らない）。**本文の変更なし**（反映係・設計卓の依頼 Part 3 項目 6）。**対象指示書の版は変えていない。** |
| 1.0.0 | 2026-08-02 | 初版。 |

---

## 0. レビュー担当への前置き

### 0.1 準備

- 必読: 指示書 M19-04b v1.1.0 **全節**（**とくに §0 の既済／未済の対照・§2.2 停止条件・§4.3 一覧生成・§5 テスト**）、`m18-m19-contract.md` **§2（F-1〜F-6）**、`DES-003` **§3.3・§3.20**、`M19-DESIGN-07` **§1-3・§9-1**、`character_data/chain-cancel-measurements.md` の**確定ロースター**。
- **Plan Mode 着手前確認の確認（必須）**: 指示書 §3 の **8 項目すべて**に提示と承認が残っているか。**とくに 2（技名 → `move_code` の写像表の全数）**。**2 が提示されずに実装へ進んでいたらその時点で重大**（§9）。

### 0.2 基本姿勢

- **本サブは「M19-04 でできなかったことだけ」をやる。** **DDL・本番コード・CSV に触れていたら重大。**
- **「エラーにならない失敗」を疑う。** 主要な失敗モードは**サイレント no-op**（0 件更新・0 件投入でもマイグレは成功する）と**写像の取り違え**（別の技に値が入る。これもエラーにならない）。**件数で固定されていない投入は、投入されていないのと同じ扱いで見る。**
- **★本サブには「固定してはいけない件数」がある。** §2 を必ず読むこと。**過剰に固定していたら重大。**

### 0.3 報告フォーマット

- 各節ごとに「OK / 重大（§9）/ 軽微（§10）/ 質問（§11）」で報告。

---

## 1. 【最重要】サイレント no-op の排除

- [ ] **`chain_cancel_total` の投入件数がちょうど 16 行**であることが、**件数を固定するテスト**で押さえられているか。
- [ ] **キャラ別・`move_code` 別の値**が固定されているか。**「16 行入った」だけでは、写像の誤りを検出できない。**
- [ ] **投入前に NULL だった 16 行だけが変わり、それ以外の `chain_cancel_total` が 1 行も変わっていない**ことが示されているか（**000052 で入った 37 行を含む**）。
- [ ] **`move_derivations` の投入で、`child_move_id` が zangief の連打版 3 行のいずれかである行がちょうど 9 行**であることが固定されているか。
- [ ] **`INSERT … SELECT … FROM moves WHERE …` 型を使っている場合、親行が無ければ 0 行投入でマイグレは成功する**——**この経路を通るなら件数固定は必須。**
- [ ] **down 適用後に元の状態へ戻る**ことが確認されているか（16 行が NULL・9 行が削除）。**re-up で復帰するか。**
- [ ] **down が `move_derivations` を空にしていないか**（**明示削除でなければならない**。将来ほかの行が入ったときに巻き添えにする）。

## 2. 【最重要】★固定してはいけない件数を固定していないか

- [ ] **`move_derivations` の「テーブル総数」を 9 で固定していないか。** **固定していたら重大。**
- [ ] 総数については **「9 行以上」の下限のみ**が固定されているか。
- [ ] **理由が理解されているか**——親参照は `startup_basis` の人手付与と**同一パスで付与される**設計であり（DESIGN-07 §4-1）、**人手付与が進むほど増える**。**総数を 9 に固定すると、次に親参照が入った瞬間に落ち、そのとき「期待値を緩める」誘惑が発生する。**
- [ ] **テスト名に数字が埋め込まれていない**か（`Exactly9Rows` 型の命名になっていないか）。

## 3. 【最重要】写像の一意性（`chain_cancel_total` 16 行）

- [ ] **技名 → `move_code` の写像表**が Plan Mode で提示され、**全 16 行が一意に決まっている**か。**曖昧なまま当てていないか。**
- [ ] **値の一次源が `character_data/chain-cancel-measurements.md` の「確定値」表**か。**CSV から読んでいないか**（CSV の当該列は空欄のはず）。
- [ ] **`character_data/*.csv` の `chain_cancel_total` 列に値を転記していない**か。**転記していたら重大**（同じ値が 2 か所に存在する）。
- [ ] **投入範囲が第三波 6 キャラ（m_bison / rashid / jamie / luke / marisa / jp）に限られている**か。**11 キャラ側の行を再投入していないか。**
- [ ] **ロースターの非対称が正しく扱われている**か——**ジェイミーは `Stand LP` が圏外で `Stand LK` が対象**（逆転）／**ルーク・マリーザは立ち弱攻撃が両方とも圏外**。**「4 技 × 6 キャラ = 24 行」で数えていないか。**
- [ ] **資料に無い行を「total − 4」等の規則で埋めていない**か。**埋めていたら重大**（同一の単発値が最大 3 通りに割れることが多重検算つきで示されている）。
- [ ] **1 行 1 UPDATE で、キャラクターコードと `move_code` が明示**されているか（**条件式で束ねていないか**——写像の誤りが見えなくなる）。
- [ ] **provenance コメント**（実測資料のどの行から取ったか）が各 UPDATE に付いているか。

## 4. `move_derivations` 9 行

- [ ] **3 親 × 3 子の全組み合わせ**が投入されているか。**1 対 1 の 3 行になっていないか。**
- [ ] **親＝`standing_light_punch` / `crouching_light_punch` / `crouching_light_kick`**、**子＝それぞれの `_rapid`** で正しいか。
- [ ] **実装前に、6 技のフレーム値が DESIGN-07 §1-3 と一致することを DB の実値で確認**したか（**7/3/9/18・6/2/8/15・4/3/12/18 ／ 4/3/9/15・3/2/8/12・3/3/12/17**）。**これが `move_code` の取り違えを検出する唯一の手段である。**
- [ ] **連打版 3 行の `startup_basis` が `'standalone'` である**ことを実査したか。**違っていたら止まるべき箇所である。**
- [ ] **`moves.id` をハードコードしていない**か（`character_code` と `move_code` から引いているか）。
- [ ] **親子の組み合わせが `move_code` のペアで固定**されているか（id で固定していないか）。

## 5. 【重点】一覧生成（§4.3）

- [ ] **対象キャラが第三波 6 キャラのみ**か。**既に記入済みの 11 キャラを出していないか**（二重記入を招く）。
- [ ] 抽出条件が指示書どおりか——`startup_basis`＝**`is_derived=1` かつ `category<>'rush_variant'` かつ `startup_basis='unknown'`**／**B 型**＝**`is_aerial=1` かつ `code` に `jumping_` を含まない**。
- [ ] **★`chain_cancel_total` の系統を一覧に含めていないか。** **含めていたら重大**——`IS NULL` で機械抽出すると**確定ロースター外＝圏外の行まで「未実測」として並ぶ**。**NULL には「圏外だから NULL」と「未実測だから NULL」の 2 種類があり、機械条件では区別できない。**
- [ ] **判断に必要な列がすべて添えられている**か——`character_code` / `move_code` / `name_ja` / `category` / `is_derived` / `is_aerial` / `command` / `startup` / `total` / **親技** / `condition_ja`。**件数だけの報告になっていないか。**
- [ ] **キャラクター順・`move_code` 順で安定ソート**されているか（差分が取れる形か）。
- [ ] **書式が既存の記入用コピーに揃っている**か。
- [ ] **記入欄の許容値に `unknown` が含まれ、`保留` との違いが冒頭に明記されている**か（`保留`＝未判断／`unknown`＝判断済みで語れないと確定）。
- [ ] **一覧の生成が読み取りのみ**か。**DB に書き込んでいないか。**

## 6. 「触らない」ことの担保（契約 F-1・F-2・F-3）

- [ ] **`internal/service/punishfinder/` の diff が 0** か（**1 行でも変更があれば重大**）。
- [ ] **`internal/service/setplay/` の diff が 0** か。
- [ ] **`internal/seedgen` の diff が 0** か。
- [ ] **`character_data/` の diff が 0** か。
- [ ] **既存マイグレ `000001`〜`000059` の diff が 0** か（`git status --porcelain migrations/` が**新規ファイルのみ**か）。
- [ ] **DDL を 1 行も書いていない**か。**書いていたら重大。**
- [ ] **`moves.startup` / `total` / `code` / `command` を 1 行も書き換えていない**か（**是正は M19-04c**）。
- [ ] **確定反撃サーチの canary 判定値が不変**か。**変わっていたら重大**（本サブは候補集合を動かさない）。
- [ ] **golden 7 stem が green**・**`go run ./cmd/seedgen -check` が OK** か。

## 7. 連番とドキュメント

- [ ] **`ls migrations/` の実査結果が Plan Mode に提示**され、**末尾が 000059 であることが確認**されているか。**違ったのに自採番して進んでいないか**（重大）。
- [ ] **消費したマイグレ連番が完了報告に明記**されているか（後続サブの連番が動く）。
- [ ] **マイグレが 2 本**に分かれているか（1 本にまとめていないか。**down の粒度**）。
- [ ] **§4.3 の一覧の件数（系統別・キャラ別）が完了報告に記載**されているか。
- [ ] **製造が DES を直接編集していない**か。
- [ ] **設計伝達レポート**が作成され、**①製造が独自に確定した実装仕様 ②契約・設計に反する独自判断 ③製造の判断 ④設計担当が未把握の残課題**に絞られているか。
- [ ] 禁則表現・簡体字・「起き攻け」誤字・「DR」略記の grep 除去。

---

## 9. 重大な問題（完了承認を妨げる）

- **投入件数を固定するテストが無い**（サイレント no-op を検出できない）。
- **★`move_derivations` の「テーブル総数」を 9 で固定した**（将来の親参照で落ちる）。
- **zangief スコープの 9 行を固定していない**（親の基数の取り違えを検出できない）。
- **技名 → `move_code` の写像が曖昧なまま当てられた**／**写像表が Plan Mode で提示されていない。**
- **11 キャラ側の `chain_cancel_total` を再投入した**／**第三波以外の行に値を入れた。**
- **`chain_cancel_total` を CSV へ転記した**（値が 2 か所に存在する）。
- **資料に無い行を「total − 4」等の規則で埋めた。**
- **★一覧（§4.3）に `chain_cancel_total` の系統を含めた**（圏外の行が「未実測」として並ぶ）。
- **一覧に判断に必要な列が無い**（件数だけの報告）。
- **一覧の生成が DB に書き込んでいる。**
- **DDL を書いた。**
- **`internal/service/punishfinder/` または `internal/service/setplay/` を変更した。**
- **`internal/seedgen` を変更した**／**golden 7 stem のいずれかが red**／**`-check` が NG。**
- **`character_data/*.csv` を変更した。**
- **`moves.startup` / `total` / `code` / `command` の既存値を書き換えた。**
- **マイグレ末尾が想定と違ったのに自採番して進めた**（再請求していない）。
- **down で `move_derivations` を空にした**（明示削除でない）。
- **確定反撃サーチの候補集合が変わった。**
- 既存マイグレを改変した。down 整合が無い。
- **製造が DES を直接編集した。**

## 10. 軽微な問題（持ち越し許容）

- マイグレの分割数（**消費連番が明記されていれば可**。ただし**2 本を 1 本にまとめた場合は理由の記載を求める**）。
- 一覧の書式・ファイル形式（md / csv）。
- テストファイルの配置と命名（**既存ヘルパを再利用し、新規ヘルパを作っていなければ可**）。
- E2E の既知 flakiness による 1 件の落ち（**単独実行で green であることと、その判定根拠が示されていれば許容**）。

## 11. 質問・確認事項のフォーマット

- 「指示書 §X.X / 設計書 DES-00N（実パス）§Y に対し、実装が Z。意図確認したい」の形で根拠節を併記して設計担当へ。

## 12. レビュー完了の判定

- §1〜§7 が全て OK、§9 重大ゼロ、Plan Mode 8 項目（**とくに 2**）が揃っている。§10 軽微は持ち越し可。

---

*以上、M19-04b レビューチェックリスト v1.0.0。配置 `docs/instructions/reviews/M19-04b-review-checklist.md`。重点＝**サイレント no-op の排除**・**★固定してはいけない件数を固定していないこと**・**写像の一意性**・**一覧に `chain_cancel_total` を混ぜていないこと**・**「触らない」ことの実装での担保**。*
