# M26-04 レビューチェックリスト（セキュリティ実査と公開前スキャン）

| 項目 | 内容 |
|------|------|
| 文書ID | M26-04-REVIEW-CHECKLIST |
| バージョン | **1.1.0**（2026-09-11・**★`Makefile` の条件を足した＝`D-817`**） |
| 作成日 | 2026-09-10 |
| 対象指示書 | `docs/instructions/M26-04-security-survey-and-prerelease-scan.md` **v1.2.0** |
| 発行 | 設計卓（`D-816`。**★事後発行**——同サブは 2026-09-10 に投入済みであった） |
| 使い方 | `/review_plan` の入力。**★本書は指示書の完了条件を写したものではない。⇒ 「緑でも壊れているもの」を探すための束である** |

---

## 0. ★★レビュー担当への前置き

**★★★本サブの最大の落とし穴は「走査を回して緑なら終わり」と読むことである。⇒ 道具は既知のパターンしか見ない。★検出 0 件は「パターンに当たるものが無い」であって「シークレットが無い」ではない。**

**★★本サブは公開の可否に関わる。⇒ 誤読が公開の判断を誤らせる。**

---

## 1. 束 A — ★★★`A6` の実査 3 件（**設計卓が断定していない箇所**）

| # | 見ること | 落ちる形 |
|---|---|---|
| **★★★A-1** | **(a) `DES-002` §8 の「パスワード保護を強く推奨します」の警告が実装されているか。⇒ 実装の行が 1 行引かれているか** | **★★★「設計書にそう書いてある」は実装の証拠にならない。⇒ `A6` の 3 件はまさにそれを疑っている。★「在るはず」で書いていたら不合格** |
| **★★★A-2** | **(b) 「明示的な同意ダイアログ」が実装されているか。⇒ 画面のどのコンポーネントか、無いなら「無い」と書いてあるか** | **★同上。★「無い」を書けることが本サブの品質である** |
| **★★★A-3** | **(c) `SUPP-001` の「LAN モード API 無認証一般の是正は対象外・別途判断」が、changelog の過去記録なのか現行 normative 規定なのかの判定が示されているか** | **★★★ここを取り違えると公開の判断が変わる。⇒ 「過去の記録」と「今も生きている規定」を分けているか** |
| **★★A-4** | **実査の結果が `SUPP-001` / `DES-002` §8 と食い違ったなら、CHANGE を請求しているか**（自採番していないか＝`D-293`） | **★食い違いを黙って埋めていたら、設計書が as-built と乖離したまま残る** |

---

## 2. 束 B — ★★★走査の読み方

| # | 見ること | 落ちる形 |
|---|---|---|
| **★★★B-1** | **検出 0 件を「安全である」と書いていないか。⇒ 「パターンに当たるものが無い」と書いてあるか** | **★★道具は既知のパターンしか見ない。⇒ 断定すると、次に読む人が再走査しない** |
| **★★★B-2** | **走査の母集団に、許可リストの除外分も入っているか** | **★★★`docs/seed-data/` 等は公開されないが、開発リポの履歴には残る。⇒ 「公開しないから安全」ではない** |
| **★★B-3** | **`A2`**（依存の license）**と SCANOSS**（コード片の由来）**を混ぜていないか** | **★★別物である。⇒ 混ぜるとどちらかが実質未実施になる** |
| **★★★B-4** | **検出物を削除せずに報告しているか** | **★★★削除は履歴に残る。⇒ 開発者の逐語「〇〇を消したというログを残してしまう可能性があるので…プライベートのうちに実施」。★製造が消していたら違反である** |
| **★★B-5** | **外部へ投げた問いが 1 本だけか**（Gitleaks / betterleaks の選定） | **★★本リポジトリの内容を外部サービスへ渡していないか** |

---

## 3. 束 C — ★★`A7` の実務設定

| # | 見ること | 落ちる形 |
|---|---|---|
| **★★★C-1** | **GitHub の設定を実際に変えていないか。⇒ 手順書を作るところまでで止まっているか** | **★★製造の権限外である**（`CLAUDE.md` §10＝MCP ツールは deny）**。★変えていたら越権である** |
| **★★C-2** | **`SECURITY.md` の草案で、窓口アドレスが伏せ字か** | **★★開発者が用意する**（`D-805`）**。⇒ 実アドレスを書き込んでいたら違反である** |
| **★★★C-3** | **`README.txt` / `NOTICE` を 1 バイトも触っていないか** | **★★★`M26-03` の射程である**（`D-814`）**。⇒ 両サブが同時に走っているため、触ると衝突する** |
| **★★★C-3b** | **`Makefile` を 1 バイトも触っていないか** | **★★★`M34-02` が走行中であり、同サブが `make check-windows` で `Makefile` を持っている**（`D-817`）**。⇒ 走査のターゲットを足したくなる形だが、足すのは `M34-02` の着地後の別手番である** |
| **★★C-4** | **`CONTRIBUTING.md` に (1) PR を当面受けない (2) 権利者・一般問い合わせの窓口導線 の両方が入っているか** | **★★窓口導線は `M26-03` から移した分である**（`D-814`）**。⇒ 落ちていると、どちらのサブも書かないまま残る** |
| **★C-5** | **`SECURITY.md` で応答期限を約束していないか** | **★個人 OSS であり、約束できない** |

---

## 4. 束 D — ★★射程の境界

| # | 見ること | 落ちる形 |
|---|---|---|
| **★★D-1** | **法務ポスチャの再点検に手を出していないか** | **★`M26-03` の射程である** |
| **★D-2** | **`A2` をやり直していないか** | **★`M26-01` で決着済みである** |
| **★★D-3** | **1 件見つけて直して終わりにしていないか。⇒ 何が無いかを先に全部並べているか** | **★★指示書 §0.1。⇒ 直す前に開発者が読む手番が要る** |

---

## 5. 束 E — ★共通

| # | 見ること |
|---|---|
| **★E-1** | **`bash scripts/check-doc-refs.sh` が緑か**（**★`SECURITY.md` / `CONTRIBUTING.md` を足すと参照が増える**） |
| **★E-2** | **`bash scripts/check-doc-inventory.sh` の出力を読んでいるか**（**★新種のファイルを足したら型に合っているか**＝`CLAUDE.md` §10.Y） |
| **★E-3** | **完了報告が `bash scripts/check-md-emphasis.sh <file>` を通っているか**（`D-775`） |
| **★E-4** | **`docs/progress/progress-log.md` へ索引行が 1 行足されているか**（`CLAUDE.md` §8） |
| **★★E-5** | **「推測:〜と仮定した」が要る箇所に書かれているか**（`CLAUDE.md` §9-3） |

---

*以上、M26-04 レビューチェックリスト **v1.1.0**。* **★★本書が最も見たいのは束 A と束 B-1 である**——**前者は「設計書にそう書いてある」を実装の証拠と読む形、後者は「走査した」を「安全である」と読む形だからである。⇒ どちらも報告が緑に見えたまま、公開の判断を誤らせる。**
