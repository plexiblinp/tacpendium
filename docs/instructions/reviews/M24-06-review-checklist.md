# M24-06 レビューチェックリスト: 取込・出力の UX

| 項目 | 内容 |
|------|------|
| 文書ID | M24-06-review-checklist |
| バージョン | 1.2.0 |
| 対象指示書 | `docs/instructions/M24-06-import-export-ux.md` **v1.2.0** |
| 推奨モデル | **高精度レビュー**（**★★「監査」2 件が面を守れているかの照合が中心である**） |
| 作成者・作成日 | 設計担当 Claude（設計卓・M24 期）/ 2026-08-29 |

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| 1.2.0 | 2026-08-29 | **指示書 v1.2.0 へ追随**（**D-592**）。 **★★§1-17 / §1-18 を新設＝ZIP の中のエントリ名に自動命名を及ぼしていないか**〔**`DES-002` §7.6 の契約。★「重大」に置いた**〕**／ 個別アップロードの見分け方の実査。** **★§6 の破壊確認を 5 件へ**（5 件目＝ZIP 内エントリ名にも及ぼすと E2E (4) が赤くなること）。**★E2E を 4 ケースへ。** |
| 1.1.0 | 2026-08-29 | **指示書 v1.1.0 へ追随**（**D-591**）。 **★★§0.3-4 を書き換えた**——**`SM-072` の方向は memo が「エクスポート画面の廃止（第2段）」で確定させている。⇒ 「畳まれていないこと」は欠陥ではない、ではなく「実態確認の結果しだい」である。** **★§1-6 / §1-7 を差し替え、§1-16 を新設**（**BE は弾かないことの明記**）。 |
| 1.0.0 | 2026-08-29 | 初版（**D-590**） |

---

## 0. レビュー担当への前置き

### 0.1 レビュー前の準備

1. 指示書 **v1.2.0** と `CHANGE-144` 通知書 **v1.2.0** を全文読む。
2. 完了報告 `docs/progress/M24-06-completion-report.md` を読む。
3. **★★`DES-005` §5.14 を読む**（インポート。**★入口一本化＝`CHANGE-075`/B-1**）。
4. **★`DES-005` §5.13 / §5.13a を読む**（**★`M17-05b` で「一覧のフィルタ/選択がそのまま対象」導線が既に在る**）。
5. **★`DES-005` §5.19 を読む**（**★取込ヘルパーの連携口。壊していないかの照合先**）。
6. **★`DES-006` §5 を読む**（取込の VAL）。
7. **★★`DES-002` §7.6 を読む**（**★CSV 契約と配送コンテナ。`combos.csv` / `setups.csv` のエントリ名が明示されている。1-17 の判定に要る**）。

### 0.2 レビューの基本姿勢

コードを変更せず、指摘のみを行う。

### 0.3 ★★重大でないもの（**誤判定しないこと**）

| # | これは欠陥ではない |
|---|---|
| **★★1** | **★★`SM-110`（用語）で語が 1 つも変わっていないこと。** **指示書 §1.6-1 / §4.5 が「候補を挙げるまで。語を確定させない」と定めている**（**★原文が「ただ私の感覚なので改めて検討は必要」と留保している。★`M24-07` の手番**）。**★★逆に語を変えていたら「高」**（1-8） |
| **★★2** | **★★`SM-121`（確認ダイアログ）で取込・出力以外の面が是正されていないこと。** **指示書 §1.6-4 / §4.6 が「監査は行うが是正は取込・出力の面だけ」と定めている。★★逆に他面を是正していたら「高」**（1-10） |
| **★3** | **★確認ダイアログが一律に足されていないこと。** **原文が「永続、一時的なものは除く」と自ら除外を書いている。★「動作していないのでは？と誤解しそう」はトースト・進捗表示でもよい**（§4.6） |
| **★★4** | **★★`/export/combo` が廃止されていないこと**——**ただし §3.3-5 の実態確認で「しかできないこと」が見つかった場合に限る。** **指示書 §4.3 の暫定案は「廃止する」である**（**★memo の 3 行が揃って未実施分として指している**）。**⇒ 実態確認もせず廃止していないなら「高」**（1-7） |
| **★5b** | **★★`SM-071` で BE 側が `is_draft` を弾いていないこと。** **指示書 §4.1.1 が「BE は弾かない」と明記している**（**★不変条件ではなく「うっかり混ざるのを防ぐ」ため**）。**★★逆に BE で弾いていたら §2.3 の 3 条件の報告が要る**（1-3） |
| **5** | **★「その症状は既に解消していた」として実装していない項目があること**（**★とくに `SM-071` の除外 ／ `CO-023` の StrictMode 対策**）。**指示書 §9.3 がそれを正しい結末としている。★成立をテストで固定していることは要る** |
| **6** | **★確認事項 1〜3 の回答が出ず、暫定案で実装されていること。★暫定案で進めた旨の報告は要る** |
| **7** | **★取込を実行した後のトースト（成功N/スキップN/エラーN）が変わっていないこと。** **`M17-05a` の B-5 の成果物であり、`SM-076` はプレビュー段階の話である**（§1.6-3） |
| **8** | **★0 件ファイルの文言・除外件数の見せ方・対象指定の UI の形が設計卓の想定と違うこと**（§9.2 で委任済み）。**★選んだ理由の報告は要る** |

### 0.4 報告フォーマット

指摘は **重大 / 高 / 中 / 低** で分類し、各件に**根拠**（ファイル:行 または 出力の逐語）を添える。

---

## 1. 設計・パターンとの照合（最重要）

| # | 観点 | 判定 |
|---|---|---|
| **1-1** | **★仮登録が取込から除外されるか**（§4.1） | **されないなら「重大」**——**本項の目的そのものである** |
| **★1-2** | **★★除外した件数が出ているか**（§4.1） | **★★出ていないなら「高」**——**黙って減らすと「取り込んだつもりの行が入っていない」が起きる** |
| **1-3** | **★どこで除外したか（FE / BE）が報告されているか。BE なら §2.3 の 3 条件が満たされ報告されているか** | **BE で報告が無いなら「重大」** |
| **1-4** | **★0 件ファイルのとき 0 件だと読めるか。★「0 件」の定義が報告されているか**（§4.2） | 読めないなら「高」。定義の欠落は「中」 |
| **1-5** | **★★取込を実行した後のトーストを変えていないか**（§1.6-3） | 変えていたら「高」 |
| **★★1-6** | **★★§3.3-5 の実態確認（`/export/combo` でしかできないこと）が母数付きで報告されているか**（§4.3） | **★★報告が無いなら「重大」**——**memo 自身が「実態確認後・CHANGE対象」と条件を付けている。★`CO-002` と同じ手順である**（`D-587` / `D-588`） |
| **★1-7** | **★★実態確認の結果に沿った処置になっているか**——**「無い」なら `/export/combo` が廃止されている ／ 「在る」なら廃止せず報告されている**（§4.3） | **★実態確認をせず廃止していたら「重大」。★「在る」のに廃止していたら「重大」。★「無い」のに廃止していないなら「高」** |
| **★1-7b** | **★★「しかできないこと」を §5.13a 側へ勝手に移植していないか**（§4.3） | **★移植していたら「高」**——**移植は「廃止」ではなく「機能追加」であり射程が変わる** |
| **★1-7c** | **★廃止した場合、ルート `/export/combo` ／ ヘッダのナビ項目 ／ `ComboExportPage` がすべて処理されているか**（§3.3-4 / -5） | 取り残しは「高」 |
| **★★1-17** | **★★★ZIP の中のエントリ名が `combos.csv` / `setups.csv` のまま固定されているか**（§4.4.1） | **★★及ぼしていたら「重大」**——**`DES-002` §7.6 の契約であり、「export した ZIP をそのまま受理して自動展開する」往復対称が壊れる。★破壊確認 5 がそれを固定している** |
| **★★1-18** | **★★§3.3-13 / -14 の実査結果が報告されているか**（**ZIP 内エントリ名を何で探しているか ／ 個別アップロードをファイル名で見分けているか**） | **★実査が無いなら「高」。★★「ファイル名で見分けている」と分かったのに自動命名を及ぼしていたら「重大」** |
| **★1-19** | **★ファイル名が自動生成値を初期値とした編集可能な欄になっているか**（§4.4） | **★毎回入力させる形なら「中」。★編集できない形なら「中」**——**どちらも指示書が採らないと決めた形である** |
| **★★1-16** | **★★完了報告に「BE は弾かない」ことと、§3.3-10 の連携口が同じフィルタを通るかが明記されているか**（§4.1.1） | **★明記が無いなら「高」**——**後任が「BE も守っている」と誤読すると、API 直叩きの経路が塞がっていると思い込む** |
| **★★1-8** | **★★`SM-110` で語を変えていないか**（§4.5） | **★★変えていたら「高」**——**原文が留保しており、`M24-07` が新語彙を確定させる手番である。★先に変えると `M24-07` で手戻りになる** |
| **1-9** | **★用語が全数挙げられ、候補に印が付いているか**（§3.3-8。**母数付き**） | 母数の欠落は「高」 |
| **★★1-10** | **★★`SM-121` の是正が取込・出力の面に留まっているか**（§4.6） | **★★他面を是正していたら「高」**——**指示書が明示的に切っている。★他面は報告に留める** |
| **1-11** | **★確認ダイアログの監査で「無いと事故になる」と「無くてよい」が分けられているか**（§3.3-7。**母数付き**） | 分けていないなら「高」 |
| **1-12** | **★★ファイル名が連続エクスポートで衝突しないか。★`/export` と §5.13a の両方に効いているか**（§4.4） | **衝突するなら「重大」。★片方だけなら「高」**——**作法が割れる** |
| **1-13** | **★利用者がファイル名を変えられることを奪っていないか**（§4.4） | 奪っていたら「中」 |
| **1-14** | **★`CO-023` が扱われているか**（§4.7。**★既に入っていたら成立を固定して報告**） | 扱いも報告も無いなら「中」 |
| **1-15** | **★§3.3 の実査 12 件が実施され、母数の要る項目に母数があるか**（§3.3-5 / -7 / -8 / -11） | 母数の欠落は「高」。実査の欠落は「高」 |

---

## 2. データ・API 契約・スキーマの不変（非破壊性）

| # | 観点 | 判定 |
|---|---|---|
| **2-1** | **`internal/` に差分があるなら、§2.3 の 3 条件が満たされ報告されているか** | 報告が無いなら「重大」 |
| **2-2** | **`DES-002` §4.2 の経路表に載る既存 API の挙動を変えていないか**（**新設のみ**） | 変えていたら「重大」 |
| **2-3** | **★取込の VAL（`VAL-I` 系）の判定内容を変えていないか**（§1.6-7。**★確認事項 1 で案 B を採ったときのみ**） | 変えていて報告が無いなら「重大」 |
| **2-4** | **`internal/service/notation/` ／ `internal/service/preset/` に触れていないか**（契約 F-1） | 触れていたら「重大」 |
| **2-5** | **`DES-003` のスキーマに触れていないか。マイグレーションを消費していないか** | 触れていて報告が無いなら「重大」 |
| **★2-6** | **★★`DES-005` §5.19 の連携口（取込ヘルパー → `/import` の router state）が壊れていないか**（§3.3-10） | **★★壊れていたら「重大」**——**取込ヘルパーからの導線が切れる。`CHANGE-071` の契約である** |
| **2-7** | **★出力形式の中身（CSV / PDF / PNG / クリップボード）を変えていないか**（§1.6-6） | 変えていたら「高」 |
| **2-8** | **新規依存が追加されていないか**（`CLAUDE.md` §6） | 追加されていて提案の記録が無いなら「重大」 |

---

## 3. ブラウザストレージ（**`CLAUDE.md` §10.X**）

| # | 観点 | 判定 |
|---|---|---|
| **3-1** | **キーを新設・変更した場合、`web/CLAUDE.md` §1 の台帳へ登録されているか** | 未登録なら「重大」 |
| **3-2** | **`web/src/lib/browser-storage.ts` 経由で読み書きしているか** | 直接 API を叩いていたら「高」 |
| **3-3** | **`bash scripts/check-browser-storage-keys.sh` が緑か** | 赤なら「高」 |

---

## 4. フロントエンドの動作仕様

| # | 観点 | 判定 |
|---|---|---|
| **4-1** | **確認ダイアログの是正が共有プリミティブのダイアログを通っているか**（規則9。**自作オーバーレイは物理入力抑止に参加しない**） | 自作なら「高」 |
| **4-2** | **★対象指定の選択 UI に既存部品を流用した場合、その旨が報告されているか**（§9.2） | 報告が無いなら「中」 |
| **4-3** | **★i18n の新規キーが `ja` / `en` の両方に足されているか** | 片方だけなら「中」 |
| **4-4** | **★「動作していないのでは？と誤解しそう」への手当てが、一律のダイアログになっていないか**（§4.6） | 一律なら「中」——**トースト・進捗表示でもよい** |

---

## 5. テストの妥当性

| # | 観点 | 判定 |
|---|---|---|
| **5-1** | **E2E 4 ケースが揃っているか**（§5.1） | 欠けているなら「高」 |
| **★★5-1b** | **★★E2E (4)（自動命名で出力した ZIP をそのまま取り込める）が在るか** | **★★無いなら「高」**——**往復対称の証拠がどこにも無い** |
| **★5-2** | **★★連続エクスポートで衝突しないことを見る E2E が在るか**（2 回出して名前が違うことを見ているか） | **無いなら「高」**——**1 回しか出さないテストは衝突を検出できない** |
| **5-3** | **純粋関数のテストが揃っているか**（ファイル名の組み立て ／ 仮登録フィルタ） | 欠けているなら「高」 |
| **5-4** | **★ファイル名を変えて落ちる既存 spec の母数が報告され、書き換えられているか**（§5.2。**母数付き**） | **母数の欠落は「高」。★★検証内容を緩めていたら「重大」** |
| **5-5** | **★`M17-05a` / `M17-05b` / `M17-05c` の spec が落ちていないか** | 落ちていたら「重大」 |
| **5-6** | **★基準値が同一セッション内の背中合わせで採られているか**（playbook §4.37） | 別時点の値どうしを引き算していたら「高」 |
| **5-7** | **`getByRole` の `name` の部分一致に足をすくわれていないか**（**破壊確認が「追記」ではなく「置換」で行われているか**） | 追記で行っていたら「高」 |

---

## 6. 破壊確認（**指示書 §5.3・5 件**）

| # | 観点 | 判定 |
|---|---|---|
| **6-1** | **5 件すべて実施され、コマンドと出力が載っているか** | 欠けているなら「高」 |
| **★★6-1b** | **★★破壊確認 5（ZIP 内エントリ名にも自動命名を及ぼす）が赤くなっているか** | **★★赤くならないなら「高」**——**往復対称は守られていない** |
| **★★6-2** | **★★破壊確認 4（ファイル名から日時を外す）が赤くなっているか** | **★★赤くならないなら「高」**——**衝突しないことはどこにも守られていない** |
| **6-3** | **★壊す対象が実在し、その壊し方で観測が変わることが確かめられているか**（playbook §4.32） | 空振りしているなら「高」 |
| **★6-4** | **★クラスを足す形の壊し方になっていないか**（**jsdom は Tailwind を読まない**＝`M24-12` の教訓） | クラスで壊していて空振りしているなら「高」 |
| **6-5** | **★赤くなる経路が数えられているか** | 数えていないなら「中」 |

---

## 7. ドキュメント・進捗ログ

| # | 観点 | 判定 |
|---|---|---|
| **7-1** | **`docs/progress/progress-log.md` へ追記されているか**（`CLAUDE.md` §8） | 未追記なら「高」 |
| **7-2** | **完了報告に §7.4.1 の CHANGE 要否 7 件の判定結果が載っているか** | 欠けているなら「高」 |
| **★7-3** | **★★`SM-110` の用語の候補が「用語 ／ 出ている面 ／ 言い換えの候補」の 3 列で並んでいるか**（§4.5） | 形が違っていて `M24-07` が使えないなら「中」 |
| **★7-4** | **★★「他面」で見つかった確認ダイアログの不足が報告されているか**（是正はしないが報告はする。§4.6） | 報告が無いなら「中」 |
| **★7-5** | **★★as-built を書く節が、指示書からの転記ではなく実装かテストを見て書かれているか**（playbook §4.43） | **指示書と同じ文が並んでいて実装と食い違うなら「高」** |
| **★7-6** | **★★指示書が挙げた要求を実装しなかった箇所が、「製造判断」ではなく「契約・設計に反する独自判断」として報告されているか**（playbook §4.42） | 「判断」として書かれていたら「中」 |
| **7-7** | **★暫定案で進めた項目（確認事項 1〜3）が、その旨とともに報告されているか** | 報告が無いなら「中」 |
| **7-8** | **★件数を本文へ書いた箇所に、その場で数え直したコマンドが併記されているか** | 併記が無いなら「中」 |
| **7-9** | **★モック HTML が提示され、変更前と変更後が並んでいるか**（§4.8） | 無いなら「中」 |
| **7-10** | **★`bash scripts/check-artifact-integrity.sh` を 1 本目に回しているか。他の検査も緑か** | 回していないなら「中」。赤を残していたら「高」 |

---

## 8. 重大な問題の判定基準（完了承認を妨げる）

- **仮登録が取込から除外されない**（1-1）。
- **BE で除外していて §2.3 の 3 条件の報告が無い**（1-3）。
- **★★実態確認をせず `/export/combo` を廃止した／「しかできないこと」が在るのに廃止した**（1-6 / 1-7）。
- **★★★ZIP の中のエントリ名に自動命名を及ぼした**（1-17）。**`DES-002` §7.6 の契約であり往復対称が壊れる。**
- **ファイル名が連続エクスポートで衝突する**（1-12）。
- **`internal/` の差分に §2.3 の 3 条件の報告が無い**（2-1）／ **既存 API の挙動を変えた**（2-2）。
- **取込の VAL を変えて報告が無い**（2-3）／ **契約 F-1 に触れた**（2-4）／ **スキーマに触れた**（2-5）。
- **★★`DES-005` §5.19 の連携口が壊れている**（2-6）。
- **新規依存が提案なしで追加されている**（2-8）／ **ブラウザストレージのキーが台帳へ未登録**（3-1）。
- **E2E の書き換えで検証内容が緩められている**（5-4）／ **`M17-05` 系の spec が落ちている**（5-5）。

---

## 9. 軽微な問題の判定基準（持ち越し許容）

- 0 件ファイルの文言、除外件数の見せ方、対象指定の UI の形（§9.2 で委任済み。**★理由の報告があること**）。
- i18n の英語ロケールの文言の質（`M24-07` の手番）。
- **★ただし「持ち越す」と決めたものは followup の候補として設計伝達レポート §4 へ並べること**（**★製造は `followup-backlog` を直接編集しない**＝`D-382`。**§J だけが例外**）。

---

## 10. 質問・確認事項のフォーマット

| # | 質問 | 対象ファイル:行 | 影響 |
|---|---|---|---|

---

## 11. レビュー完了の判定

- **重大 0 件**であること。
- **高**が残る場合は、取り込みの可否を製造担当が判断し、不採用なら理由を完了報告に書くこと。
- **★再レビューの往復上限は 2 回**（`CLAUDE.md` §9 停止規律）。**★上限に達したら未解消項目を `followup-backlog` §J へ必須 5 フィールド付きで記録し、直ちに停止すること。**

### 11.1 ★本チェックリスト自身の検査（**発行前に設計担当が行う**）

- **指示書の版数と対象指示書欄が一致しているか**（`bash scripts/check-instruction-format.sh`）。
- **★★§0.3「重大でないもの」が、指示書がスコープ外・委任と定めた項目をすべて拾っているか。**
- **★§8 の重大判定が、指示書 §2.2「変更しないもの」と §7.1「機能要件」から漏れなく引かれているか。**

---

*以上、M24-06 レビューチェックリスト **v1.2.0**。* **★★本サブの最大の危険は「監査」2 件が面を越えることである**〔`SM-110` で語を変える ／ `SM-121` で取込・出力以外を是正する〕。**§0.3-1 / §0.3-2 / §1-8 / §1-10 がその照合にあたる。**
