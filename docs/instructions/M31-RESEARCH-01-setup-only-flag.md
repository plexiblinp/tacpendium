# RESEARCH 指示書 M31-RESEARCH-01: `moves.setup_only` は何を出し分ける列か（`P4M-005`）

| 項目 | 内容 |
|------|------|
| 文書ID | M31-RESEARCH-01 |
| バージョン | **1.0.0**（2026-09-09） |
| 作成日 | 2026-09-09 |
| 作成者 | 設計担当 Claude（設計卓／フェーズ4 期） |
| 対象マイルストーン | **`M31`**（`α②`）。上位＝`docs/instructions/M31-overview.md` v1.6.0 |
| 種別 | **★★RESEARCH（`/research_plan`）。⇒ read-only 厳守・事実列挙のみ。★実装しない** |
| 推奨モデル | **標準**（実モデル名は `docs/human-notes/model-allocation.md`） |
| Plan Mode | **不要**（read-only） |
| レビュー | **不要**（RESEARCH レポートは設計卓が読む） |
| CHANGE 消費 | **0 本**（調査である） |
| マイグレ消費 | **0 本** |
| 前提サブ | **無し** |
| 射程 identity | **1 件**——`P4M-005`（`setup_only` フラグの活用） |
| 配置 | `docs/instructions/M31-RESEARCH-01-setup-only-flag.md` |
| 出力 | `docs/progress/M31-RESEARCH-01-report.md` |

---

## 0. この指示書の特殊性

### 0.1 ★★開発者が「専用サブを作って考えるように」と指示した

**★逐語**（2026-09-08）**＝「いったん保留でいい。専用サブを作って考えるように設計卓へ伝達してください。」**

**⇒ 本書はその「考える」サブである。★実装のサブではない。**

### 0.2 ★★前提が欠けていることが分かっている

**★★`moves.setup_only = 1` の技が実 DB に 0 件である**（`M31-01` の製造が実測）**。**

| 実測 | 内容 |
|---|---|
| **★★1** | **`moves` を `setup_only = 1` で走査して 0 件** |
| **★2** | **`internal/model/move.go:68` が「予約列」と明記している** |
| **★★3** | **`internal/service/setplay/refine_test.go:104` は逆に「filler に含める」を固定している。⇒ 出し分けの向きが実装で既に決まっている可能性がある** |

**★★したがって「フラグを活用する」を素直に実装しても、何も変わらない。⇒ 何を出し分けるかが決まっていない。**

### 0.3 ★★本書が答えるべき問いは 1 つである

**★★「`setup_only` はどの技に立てる列で、立てた技は画面のどこで扱いが変わるのか」。**

**⇒ これが決まらないと、データを入れることも実装を書くこともできない。★逆に決まれば、実装は小さい見込みである。**

**★★本書は答えを出さない。⇒ 「答えるために要る事実」を全数で並べるところまでである。**

---

## 1. 前提として読むもの

| # | 何を | どこ |
|---|---|---|
| **★★1** | **登録内容の全文** | `docs/handover/followup-backlog.md` §CD の `setup-only-flag-has-no-data` |
| **★2** | **列の定義** | `internal/model/move.go`（**★「予約列」の記述**）／ `migrations/` の当該列を作った 1 本 |
| **★★3** | **既に立っている前提** | `internal/service/setplay/refine_test.go:104`（**★「filler に含める」を固定している**） |
| 4 | **セットプレイの画面** | `DES-005` §5.7 の該当箇所 ／ §5.19 系 |
| **★5** | **同型の先例** | `docs/handover/followup-backlog.md` の `camy-od-hooligan-derivations-unregistered`（**★本件の再開条件になっている＝`D-724`**） |

---

## 2. 調べること（**★read-only。⇒ 1 行も書き換えない**）

### 2.1 ★★列の来歴

| # | 調べること |
|---|---|
| **★★1** | **`setup_only` 列はどのマイグレで作られたか。⇒ そのマイグレのコメントに意図が書かれているか** |
| **★★2** | **列を作った当時の指示書・完了報告・CHANGE に、何を出し分けるつもりだったかの記述があるか。★`grep -rn "setup_only" docs/` で全数** |
| **★3** | **「予約列」と書いたのは誰の手番か**（`internal/model/move.go:68`） |

### 2.2 ★★実装で既に決まっていること

| # | 調べること |
|---|---|
| **★★1** | **`setup_only` を読んでいるコードの全数。⇒ `grep -rn "setup_only\|SetupOnly" internal/ web/`** |
| **★★2** | **読んでいる箇所が「立っていたらどうする」と書いているか。⇒ 出し分けの向きが実装で既に決まっているなら、それが答えの一部である** |
| **★★3** | **`refine_test.go:104` の「filler に含める」は、`setup_only = 1` の技を含めるのか除くのか。⇒ 逐語で引くこと** |
| **★4** | **API 応答に載っているか**（`internal/api/` の DTO） |
| **★5** | **フロントが読んでいるか**（`web/src/types/` と使用箇所） |

### 2.3 ★★「立てるべき技」の候補

| # | 調べること |
|---|---|
| **★★1** | **`camy-od-hooligan-derivations-unregistered`**（followup）**が挙げている技は何か。⇒ 本件の再開条件になっている理由を、逐語で確かめること** |
| **★★2** | **セットプレイでしか使わない技の実例が、実データに在るか。⇒ 在るなら何件で、どういう `category` か** |
| **★3** | **`custom_states` で表せているものとの重なり。⇒ `CHANGE-169` の選定基準**（「始動技よりも先にやっておくムーブかどうか」）**と、`setup_only` の関係** |

**★★2.3-3 が本書で最も価値のある問いである。⇒ 2 つの仕組みが同じものを指しているなら、片方は要らない。**

### 2.4 ★★画面での扱い

| # | 調べること |
|---|---|
| **★★1** | **`setup_only` が立った技を「出さない」面と「出す」面の候補。⇒ 仮想コントローラ**（`M30-01` で 8 タブになった）**／ 全技一覧 ／ filler 候補 ／ セットプレイのレシピ入力** |
| **★2** | **`M30-01` の `isControllerSurfaced` が `setup_only` を見ているか。⇒ 見ていないなら、見るべきかが判断事項になる** |

---

## 3. やらないこと

| # | 何を |
|---|---|
| **★★1** | **1 行でも書き換えること。⇒ 本書は read-only である**（`/research_plan` の規律） |
| **★★2** | **答えを決めること。⇒ 「こうすべき」は書かない。事実と選択肢を並べるところまで** |
| **★3** | **データを入れること** |
| **★4** | **`setup_only` を使う実装を書くこと** |

---

## 4. ★★出力の形

**★`docs/progress/M31-RESEARCH-01-report.md` に、次の順で書くこと。**

| # | 節 |
|---|---|
| **1** | **列の来歴**（§2.1。**★「意図が書かれていない」なら、そう書く**） |
| **2** | **実装で既に決まっていること**（§2.2。**★逐語で引くこと**） |
| **3** | **立てるべき技の候補**（§2.3。**★件数と `category` を数で**） |
| **4** | **画面での扱いの候補**（§2.4） |
| **★★5** | **★★選択肢の一覧。⇒ 「この列を活かすなら、こういう決め方がありうる」を 2〜3 本。★どれを推すかは書かない** |
| **★★6** | **★★「決められないこと」の一覧。⇒ 開発者のドメイン知識が要る問いを名指しで** |

**★★母集団を 1 行書くこと**（`moves` テーブルか `character_data/*.csv` か＝`SUPP-001` §5.5.3.2 / `M-145`）**。**

---

## 5. ★★本書の落とし穴

| # | 落とし穴 |
|---|---|
| **★★1** | **★「0 件だから意味が無い列だ」と結論すること。⇒ 0 件なのは、まだ立てていないからかもしれない。★来歴を先に読むこと** |
| **★★2** | **★逆に「予約列だから将来のためのものだ」と結論すること。⇒ `internal/model/move.go:68` がそう書いているだけであり、書いた人の意図は別に確かめる** |
| **★★3** | **★`custom_states` との重なりを見落とすこと**（§2.3-3）**。⇒ `CHANGE-169` で「始動技よりも先にやっておくムーブ」という基準が明文化されたばかりであり、`setup_only` と同じものを指している可能性がある** |
| **★4** | **★推測で埋めること。⇒ 分からないことは「分からない」と書く**（`CLAUDE.md` §9-3） |

---

## 6. 参照ドキュメント

| ID | パス | 参照する箇所 |
|----|------|------------|
| — | `docs/handover/followup-backlog.md` | **§CD の `setup-only-flag-has-no-data`** ／ **`camy-od-hooligan-derivations-unregistered`** |
| — | `docs/change-notes/CHANGE-169-notification.md` | **§2**（**`custom_states` の選定基準。★§2.3-3 の比較対象**） |
| — | `docs/change-notes/CHANGE-170-notification.md` | **`M30-01` の as-built**（仮想コントローラの 8 タブと `isControllerSurfaced`） |
| DES-005 | `docs/design/05-screen-design.md` | **§5.7** ／ **§5.19 系**（セットプレイ） |
| SUPP-001 | `docs/design/supp-001-detailed-design.md` | **§5.5.3.2**（実測の母集団） |
| — | `docs/process/parallel-board.md` | **`D-724`**（再開条件）／ **`D-781`**（開発者の保留裁定）／ **`D-788`**（本書の起票） |

---

*以上、RESEARCH 指示書 M31-RESEARCH-01 **v1.0.0**。* **★★本書は答えを出さない。⇒ 「答えるために要る事実」を全数で並べるところまでである。** **★最も価値のある問いは §2.3-3 である**——**`custom_states` と `setup_only` が同じものを指しているなら、片方は要らない。⇒ そこを見ずに列を活かすと、同じ概念が 2 つの仕組みで表される。**
