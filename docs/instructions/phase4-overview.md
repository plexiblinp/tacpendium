# phase4-overview: フェーズ4（初回全体公開までにやるべきこと）マイルストーン構成

| 項目 | 内容 |
|------|------|
| 文書種別 | 補足資料（overview。CHANGE 通知書対象外・自由改訂） |
| バージョン | **1.27.0**（2026-09-20・**★★★`M40` を起票した**＝`D-919`・開発者承認。**⇒ SCANOSS の初回スキャンが終わり、是正が 2 サブになった。★実装サブは 2 本**〔`M40-01` ∥ `M40-02`〕**である**） ／ **1.26.0**（2026-09-20・**★★★公開ゲート `A` の残件 4 件が followup に残っていたことを反映した**＝`D-918`。**⇒ 前版の「残るのは開発者の手番だけ」は誤りであった**〔計測点 `M-210`〕**。★実体は `shadcn-ui-vendored-without-attribution` ほか 3 件であり、`M40` としてまとめることを提案している**） ／ **1.25.0**（2026-09-20・**★★★`M39-02` を受理し `M39` をクローズした**＝`D-916`。**⇒ 実装サブは 0 本になった。★残るのは開発者の手番だけであり、手順は `docs/process/public-release-runbook.md` v1.0.0 が正本である**〔`D-917`・開発者承認〕） ／ **1.24.0**（2026-09-20・**★★★`M36-02` を受理した**＝`D-914`**。⇒ 実装サブで残るのは `M39-02` の 1 本だけである。★★★ただし `M36` は閉じない**〔リリース経路が一度も実走していない〕**。★★公開リポ `tacpendium` の開設は*フェーズ4 の最後の手番*であり、そこで 4 件をまとめて処理する**＝`D-915`） ／ **1.23.0**（2026-09-19・**★★★`M39-02` を起票した**＝`D-913`・開発者承認。**⇒ リリースまでに残るのは `M36-02`**〔走行中〕**と `M39-02`**〔未投入・並列可〕**の 2 本である**） ／ **1.22.0**（2026-09-19・**★★★`M39-01` を受理した**＝`D-911`。**⇒ `CHANGE-229`〜`232` を反映。★リリースまでに残るのは `M36-02`**〔走行中〕**と、承認待ちの `M39-02`**（`D-912`）**である**） ／ **1.21.0**（2026-09-19・**★★★`M39` を新設した**＝`D-909`〔`d187-startup-basis-invariant-violated-at-head` の是正。**★開発者の承認済み**〕**。⇒ 同じ手番で `M36-02` が投入された。★リリースまでに残るのは `M39-01` の 1 本である。★`M33` は開き直さない**〔先例＝`D-892`〕） ／ **1.20.0**（2026-09-19・**★★★`M33-03` を受理し `M33` をクローズした**＝`D-907`。**⇒ リリースまでに残るのは `M36-02` の 1 本だけである。★`go test ./...` は緑へ戻った**） ／ **1.19.0**（2026-09-19・**★★`M33-03` を起票した**＝`D-903`。**⇒ 未起票が 0 になった。★残るのは `M33-03` → `M36-02` の 2 本であり、どちらも三点セット発行済み**） ／ **1.18.0**（2026-09-19・**★★★`M33-02` を受理した**＝`D-902`。**⇒ 比較は全一致。★残るのは `M33-03`**〔未起票〕**→ `M36-02` の 2 本。★次のマイグレ連番は `000010` へ入れ替わった**） ／ **1.17.0**（2026-09-19・**★★`M38-03` を受理した**＝`D-901`。**⇒ `M38` はクローズ。★残るのは `M33-02`**〔走行中〕**→ `M33-03` → `M36-02` の 3 本**） ／ **1.16.0**（2026-09-19・**★★`M33-02` の並列条件を緩めた**＝`D-900`〔**「並列 0 本」→「走行中のサブが `migrations/` を触らないこと」**〕**。⇒ `M38-03` と並列に置ける。★投入順は直列から並列へ**） ／ **1.15.0**（2026-09-19・**★★★投入順を改めた**＝`D-899`。**⇒ `M33` はリリース*前*に完走する。★前版の「`M33` 族はリリースを止めない」は決定に効く意味で誤りであった**〔潰せる窓は利用者の DB が版を持たないうちだけであり、公開すると閉じる〕） ／ **1.14.0**（2026-09-19・**★★`M36-02` を起票した**＝`D-898`。**⇒ §3.1 の「未起票」が 2 → 1 になった。★残る未起票は `M33-03` のみであり、同サブは `M33-02` の結果待ちである**） ／ **1.13.0**（2026-09-17・**★★`M38` を §3.1 の区分へ足した**＝`D-892`〔**撮影で出たリリース前要望の第 2 束。起票済み・未投入 2 本**〕） ／ **1.12.0**（2026-09-14・**★★`M32-03` を §3.1 の区分へ足した**＝`D-882`〔起票済み・未投入〕**。★`M32-02` の欄へ「改稿は `M32-03` が引き取る」を明記**） ／ **1.11.0**（2026-09-14・**★★§3.1 の「開発者の手番」区分を是正した**＝`D-879`〔**`M32-02` は走行済みかつ未マージであり、本区分では言い表せていなかった**＝計測点 `M-182`〕） ／ **1.10.0**（2026-09-14・**★★§3.1 の開発者の手番欄へ撮影の床の精査を書いた**＝`D-878`〔**`M37-07` に依存するのは 22 画面のうち 3 つだけである**〕**。★`B02` はフェーズ5 送りで決着**） ／ **1.9.0**（2026-09-13・**★★§3.1 の現在地を全面更新した**＝`D-869`。**★`M37` の 6 サブと `M33-01` の着地を反映し、順路を 1 本に描いた**〔`M37-04` ∥ `M37-05` → `M32-02` → `M33-02` または `M36-01`〕） ／ **1.8.0**（2026-09-13・**★★★残件の母集団に「受理済み・未起票」を足した**〔計測点 `M-170`＝`A` と `B01`〜`B15` の束を数え漏らしていた〕**／ `M36` の承認と `M37` の提案を反映**＝`D-853`） ／ **1.7.0**（2026-09-12・**★★§3.1 の残数を数え直した＝`D-845`。⇒ 2 日で 11 サブが受理され、15 本が 5 本まで縮んだ。★あわせてマイルストーン単位の状態表を足した**） ／ **1.6.0**（2026-09-10・**★★★実装サブの残数を数え直した＝`D-803` / `D-807` / `D-812`。⇒ §3 の直後へ「残数の現在地」を置いた。★ledger 行の数は 1 行も変えていない**〔**別の軸である**〕**。★あわせて `M14` の完了を反映した**＝`D-811`） ／ 1.5.0（2026-09-08・**★★`M35`**（公開前の是正束）**を追加した＝`D-783`。⇒ 公開前と決めた項目が面ごとにばらけて 4 件在り、うち 3 件は同じ面**（タグ）**である。★1 本ずつ立てると起票と受理の手番が 4 回要る**） ／ 1.4.0（2026-09-08・**★★`M34` を追加した＝`D-782`**〔Windows での起動・常駐方式。**黒いコンソール窓を出さない。開発者の直接要求**〕**。★投入は `M32` の前である**——**起動方式が変わると説明書に書く手順が丸ごと変わるため**） ／ 1.3.0（2026-09-07・**★★`M33` を追加した＝`D-774`**〔マイグレーションの統合。**開発者の直接要求。リリース前に本数を減らす**〕**。★本書はもともと `M25`〜`M32` で書かれている**） ／ 1.2.0（2026-09-02。**★★フェーズ4 に特有の運用 2 件を §8.5 へ新設＝`D-675` / `D-676`**） ／ 1.1.0（2026-09-02。**★★開発者承認済み**。確認事項 4 件の決着を反映＝`D-653`） ／ 1.0.0（2026-09-02・起票） |
| 作成日 | 2026-09-02 |
| 作成者 | 設計担当 Claude（設計卓・フェーズ4 期） |
| 位置づけ | **フェーズ4 のマイルストーン分割（`M25`〜`M32`）の正本。** 本書がサブ構成・全件処遇・CHANGE 見込みの正本になる。**★`REQ-001` §7 の改訂は本書の承認と同じ手番で行う**（`D-634` (6)） |
| 前提 | `docs/progress/M25-RESEARCH-01-report.md`（3 層 ledger 309 行・**本書の数の一次源**）／ `docs/human-notes/combmgr-prerelease-checklist.md`（公開ゲート A 9 件・B 6 件の定義）／ `docs/human-notes/phase4-memo.txt`（`α②` の源泉）／ ボード `D-634` / `D-637` / `D-643`〜`D-651` |
| 配置 | `docs/instructions/phase4-overview.md`（active＝直下。フェーズ4 終了時に `docs/instructions/phase4/` へアーカイブ） |

---

## 0. 本書の確度

- **★★本書は 2026-09-02 に開発者承認された**（確認事項 1＝「OK」）。**⇒ `REQ-001` §7 の改訂（`CHANGE-150`）は同日に反映済みである**（`D-654`）。
- **数の確度**——**`§7` の内訳は `M25-RESEARCH-01` の ledger から機械的に数えたものであり、設計卓が独立に検算している。** ただし **ledger の母集団は「M24 返却 ＋ `phase4-memo` ＋ `Memo_Someday` ＋ `followup` ＋ `checklist` ＋ `future-notes`」であり、`REQ-001` §7 が現に持つフェーズ4 の 3 項目そのものは母集団に入っていない**（§2.3）。**⇒ 行数だけでフェーズ4 の射程を語ってはならない。**
- **マイルストーンの粒度の確度**——**`M25` / `M26` / `M32` は根拠が到達条件に直結しており固い。`M27`〜`M31` は `α②` を面で割ったものであり、着手時に境界が動きうる。**
- **★★本書と各 MS overview が列挙した identity は「射程の上限」であって「必達のリスト」ではない**（§8.5.1・`D-675`）。**⇒ 「やらない」に落とす判断は指示書作成時点または Plan Mode で行う。**
- **★設計卓は実装ソースを読んでいない。** 本書に現れる実装の事実は `code-facts.md` ／ `M25-RESEARCH-01` の実査 ／ 完了報告からのみ引いている。**「在る」も「無い」も断定していない箇所は、サブ起票時の実査項目にする。**

---

## 1. 目的・位置づけ

**フェーズ4 の終わりが初回全体公開である。**

開発者の逐語（2026-09-02・`D-634`）——**「リリースまでにやるべき事はまだ残っている。それをやるのがフェーズ4。本当に後回しでいいのがフェーズ5。future-notes はフェーズ6 以降のイメージ」**。

**★現行 `REQ-001` §7 のフェーズ4 定義（「外部データ追従・拡張」＝`FR702` / `NFR407` / `ISSUE-007` の 3 項目）は、この意図と一致していない。** ⇒ 本書はフェーズ4 を作り直し、あわせてフェーズ5 / フェーズ6 を新設する（§6）。

### 1.1 フェーズの境界

| フェーズ | 定義 | 終わり |
|---|---|---|
| **フェーズ4** | **初回全体公開までにやるべきこと。** 公開ゲート A ／ 摩擦低減 B ／ `α①`〜`α③` ／ `FR702` | **初回全体公開** |
| **フェーズ5** | **本当に後回しでよいもの。** 開発プロセス・内部品質・検査・テスト・リファクタ ／ 機能拡張 ／ i18n の訳文品質 ／ 利用者向けでない文書整備 | 公開後に順次 |
| **フェーズ6 以降** | **構想。** `future-notes` の各設計メモ | 未定 |

---

## 2. スコープ

### 2.1 到達条件（`checklist` が定義した A / B ＋ 開発者が埋めた `α`）

| 群 | 中身 | 件数 |
|---|---|---:|
| **A 公開ゲート** | ライセンス三層 / 依存棚卸し / データ来歴 / 法務ポスチャ / 正式名 / LAN 認証 / 段階的公開の実務 / devContainer 除外 / SCANOSS | **9** |
| **B 摩擦低減** | コード署名 / AV 誤検知 / データ鮮度 / 操作説明書 / 宣伝記事 / クラシック前提の明示 | **6** |
| **`α①`** | 残キャラクター挿入の完遂 | 残 **14 体**〔**★2026-09-02 是正＝`D-655`。未登録 12 ＋ 仮登録 2**（`c_viper` / `dhalsim`）〕 |
| **`α②`** | `M24` で直しきれなかった「明らかに品質が低い部分」の是正 | ledger **91 行** |
| **`α③`** | 公開用の別リポジトリ化の準備 | ledger **8 行** |

**★`α` の中身は開発者回答で埋まっている**（`D-634` (1)）。**「A+B+α」という書き方自体が禁則表現だった**（計測点 `M-106`）。

### 2.2 射程外（フェーズ5 / フェーズ6 以降へ送るもの）

| 送り先 | 中身 | 件数 |
|---|---|---:|
| **フェーズ5** | `α②` の判定規則 (4)〜(7) に当たるもの／`SM-029` ダークモード（`D-643`）／`CO-026` 英語ロケール整備（`D-634` (9)）／`SM-016` 単位語（`D-634` (10)）／playbook の蒸留機構（`D-650`）／マイグレのベースライン化の再評価（`D-651`） | **102** |
| **フェーズ6 以降** | `future-notes` の 4 機能〔メモ（一人 SNS）／他プレイヤー比較／VOD 分析／HTML エクスポート〕と表裏の `checklist` §3.5 | **20** |

**★4 機能は「未定」である**（開発者の逐語＝「未定だが恐らくフェーズ6 以降」）。**⇒ `REQ-001` §7 のフェーズ6 定義に確定項目として載せない**（`D-647`）。

### 2.3 ★★ledger の母集団に入っていないもの

**`M25-RESEARCH-01` の母集団は 6 つの源泉であり、`REQ-001` §7 が現に持つフェーズ4 の 3 項目そのものは入っていない。** ⇒ 次の 3 件は行数ではなくマイルストーンとして扱う。

| 項目 | 処遇 | 根拠 |
|---|---|---|
| **`FR702`（ゲームアップデート追従）** | **フェーズ4。`M28` の本体** | `D-634` (2)。**★ledger には「`FR702` が要求するスキーマ変更」（`CL-4-3`）しか無い。実装本体は母集団の外である** |
| **`ISSUE-007`（モダン操作対応）** | **後付け。ただし公開前に「クラシック前提」を明示する**（`B6`＝`M32`） | `D-634` (2) |
| **`NFR407`（他ゲーム対応の基盤）** | **★「後回し」ではなく意思の撤回。** `REQ-001` から「非対応」として残し、フェーズ定義からは外す | `D-634` (2)。開発者の逐語＝「そもそも別ゲー対応はやるつもりがなくなっている」 |

---

## 3. マイルストーン構成（`M25`〜`M32` ＋ `M14` 独立レーン）

| MS | 名前 | ledger 行 | 主な根拠 |
|---|---|---:|---|
| **M25** | **フェーズ4 の再定義**（スコープ確定・`REQ-001` §7 改訂） | — | `M25-RESEARCH-01`（完了）＋ `CHANGE-150` |
| **M26** | **公開ゲート A**（法務・ライセンス・セキュリティ・公開リポジトリ生成）。**★サブ構成の正本＝`docs/instructions/M26-overview.md` v1.2.0**〔**★`M26-01` は受理済み**＝`D-671`〕〔**4 サブ。★`A5` の 3 行は `M28` で実行するため本 MS が閉じるのは 17 行**〕 | **20** | `A1`〜`A9` 12 ＋ `α③` 8 |
| **M27** | **`α②` 画面品質**（エディタ・一覧・詳細）。**★サブ構成の正本＝`docs/instructions/M27-overview.md` v1.4.0**〔**4 サブ**〕 | **42**（**★★raw 行数。名寄せ後は 33 identity、主題の見直しで 10 件が `M31` / `M30` へ**〔`D-667`〕**、`代表 ID` 欄の突合でさらに 4 件が `M30` へ**〔`D-686`〕**。⇒ `M27` が負うのは 19**） | `α②` |
| **M28** | **★破壊的変更の窓**（正式名リネーム ＋ `FR702` ＋ 運び量のスキーマ）。**★サブ構成の正本＝`docs/instructions/M28-overview.md` v1.4.0**〔2 サブ。**★`M28-02` は `M28-02a`（必達）と `M28-02b`（設計まで）に分かれる**＝`D-668`〕 | **4** ＋ 母集団外 | `A5` 3 ＋ `CL-4-3` 1 ＋ `FR702` 本体 |
| **M29** | **`α②` 用語・ラベルの統一と入出力**（CSV／エクスポート／バックアップ） | **29** | `α②` |
| **M30** | **`α②` 仮想コントローラ** | **11 ＋ 1**（**★`SD-021` を `M27` から移した**＝`D-667`） | `α②` |
| **M31** | **`α②` セットプレイ・確定反撃・共通部品** | **★★12 見込み**（**★`M27` から移した 9 のうち 6 は `M31` が既に代表 ID を持っており名寄せで畳まれる。⇒ 純増は 3。★さらに `P4M-012` / `P4M-013`〔共通画面部品〕は `M27-02a` が所有するため引く**＝`D-686`。**★確定は `M31-overview` 起票時**） | `α②` |
| **★★M33** | **★★マイグレーションの統合**（リリース前のベースライン化） | **—** | **★★【2026-09-07 追加＝`D-774`】開発者の直接要求である。⇒ ledger 由来ではない。★正本は `docs/instructions/M33-overview.md`。★★投入は `M32` の後・境界セッションの前**（`migrations/` 全体を触るため並列 0 本でしか入れられない）**。★★危険 4 件**〔既存 DB の起動 ／ 版数を名指ししたテスト ／ ライセンスの層 ／ golden のガード〕**は同書 §2** |
| **★★M34** | **★★Windows での起動・常駐方式**（**黒いコンソール窓を出さない**） | **—** | **★★【2026-09-08 追加＝`D-782`】開発者の直接要求である。⇒ ledger 由来ではない。★逐語＝「本アプリは起動すると、コンソールの黒窓が開くのですが、一般向けではないです。」** **★★見た目の粗ではない。⇒ 黒窓が出るソフトは一般の利用者に「開発用の何か」と読まれ、使う前に止まる。★閉じるとアプリが落ちるので、「閉じてはいけない窓」を説明書で補うことになる。** **★★投入は `M32` の前。⇒ 起動方式が変わると説明書に書く手順が丸ごと変わるためである。** **★新規依存 0 件の実装方式を受領済み**（`docs/progress/M34-RESEARCH-01-report.md`）**。★正本＝`docs/instructions/M34-overview.md`。** |
| **★★M35** | **★★公開前の是正束**（タグまわり ＋ データ 1 件） | **—** | **★★【2026-09-08 追加＝`D-783`】followup 由来である。⇒ ledger 由来ではない。** **★射程 4 件＝`tag-delete-user-scope-untested`**〔**他人のタグを消せないことが 1 本も検証されていない**〕**／ `tag-error-detail-dto-snake-case`**〔**両側が揃った規約違反。今は壊れていないから危ない**〕**／ `tag-category-color-cannot-be-cleared`**〔**開発者の判断＝戻す処理は不要。⇒ 明文化だけ**〕**／ `ryu-axe-kick-code-mismatch`**〔**入力できない技が 1 件ある**〕**。** **★★束ねた理由＝3 件が同じ面であり、1 本ずつ立てると同じファイル群を 3 回触ることになる。** **★`M35-01`**（タグ 3 件）**は起票済み。★`M35-02`**（`ryu`）**は `M33` より前に入れること。★正本＝`docs/instructions/M35-overview.md`。** |
| **M32** | **B 摩擦低減**（署名・AV・データ鮮度・操作説明書・宣伝・クラシック前提の明示） | **13** | `B1`〜`B6` ＋ `FN-02` |
| **M14** | **`α①` 残キャラ seed 完遂**（★独立レーンのまま。ただし公開前に必ず完遂）。**★指示書＝`docs/instructions/M14-03f-fourth-wave-seed.md` v1.0.0** | **9** | `α①`。`D-634` (7) |

**合計 ＝ 20 ＋ 42 ＋ 4 ＋ 29 ＋ 11 ＋ 9 ＋ 13 ＋ 9 ＝ 137。** **★ledger のフェーズ4 は 133 行であり、差 4 は `A5` 3 行と `CL-4-3` 1 行を `M26` と `M28` で二重に数えたためである**（`A5` は A ゲートに属し、実行は `M28` の破壊窓で行う）。**⇒ 重複を除いた実数は 133 で一致する。**

### 3.1 ★★★実装サブの残数（**2026-09-13 現在。⇒ 本節だけが動く。ledger 行の数は動かない**）

> **★★★【2026-09-13 全面更新＝`D-869`】下の 2 つの表は 2026-09-12 時点であり、`M37` の 6 サブと `M33-01` の着地を含んでいない。⇒ 現在地は本ブロックである。**
>
> **★★数え方は 2 母集団のまま**（`M-170` の対処）**＝(1) 起票済みのサブ ／ (2) 受理済みだが未起票の要求。**
>
> **★★★【2026-09-19 現在地＝`D-898`】下の表は*積み上げ形式*であり、行ごとに追記した時点の状態を保っている。⇒ 個々の行は古い。★いまの状態は本ブロックが正本である。**
>
> | いまの状態 | 本数 | 内訳 |
> |---|---:|---|
> | **走行中** | **0** | — |
> | **★受理済み・MS 未クローズ** | **1** | **`M36-02`**（**★2026-09-20 受理＝`D-914`。⇒ ★`M36` は閉じない**——**リリース経路が一度も実走しておらず、実走できるのは公開リポ開設後だけである**〔`D-915`〕） |
> | **★受理済み** | **1** | **`M39-02`**（**★2026-09-20 受理＝`D-916`。⇒ `M39` はクローズ。★マイグレ消費 0 本**） |
> | **★★起票済み・未投入** | **2** | **`M40-01`**（**★★帰属と宣言＝shadcn の MIT 帰属 ／ 三層の第三者素材の軸**〔**本命**〕**／ `name_ja` ／ `REUSE.toml` の暫定解除 ／ evidence の公開範囲**） ／ **`M40-02`**（**★★5 ファイルの書き換えと、その*判定*。⇒ 追補報告は 5 件とも「候補」と書いている**）**。★2 サブは並列可＝交点 0** |
> | **★受理済み** | **1** | **`M39-01`**（**★2026-09-19 受理＝`D-911`。⇒ マイグレ消費 0 本**） |
> | **★未起票** | **0** | — |
> | **★着地済み**（本表の古い行が「未投入」と書いているもの） | **9** | `M37` 全 8 サブ ／ `M32-02` ／ `M32-03` ／ `M36-01` ／ **`M38-01`** ／ **`M38-02`** ／ **`M38-03`**（**★`M38` はクローズ**） ／ **`M33-02`** ／ **`M33-03`**（**★2026-09-19 受理・`D-907`。⇒ `M33` はクローズ**） |
>
> **★★★【2026-09-19 更新③＝`D-901`】`M38-03` が着地し、`M38` はクローズした。⇒ リリースまでに残るのは 3 本である。**
>
> ```
> M36-02（起票済み・未投入） ─→ リリース
> ```
>
> **★★★【2026-09-19 更新⑦＝`D-909`】`M39` を新設した。⇒ リリースまでに残るのは 2 本である。**
>
> ```
> M39-01（受理済み）─→ M39-02（受理済み・M39 クローズ）─┐
>                                                        │
> M40-01 ∥ M40-02（起票済み・未投入・並列可）───────────┤
>                                                        │
> M36-02（受理済み）─────────────────────────────────────┼─→ 公開リポ tacpendium 開設〔開発者〕
>                                                  │     ├ 初回スナップショット同期
> SCANOSS 初回〔完了〕→ 是正後に再スキャン〔開発者・Codex〕──┘     ├ rc タグでリリース経路を1度通す
>                                                        ├ gh attestation verify の出力を返す ⇒ M36 クローズ
>                                                        └ private vulnerability reporting を有効化
>                                                                          ↓
>                                                                       リリース
> ```
>
> **★★`M39` は `d187-startup-basis-invariant-violated-at-head` の是正である**〔`startup IS NULL` なのに `startup_basis` が `unknown` でない 132 行 ＋ `character_data/` 6 行 ＋ ガードの `TestRun_HEAD_*` 化〕**。⇒ マイグレ `000010` を 1 本消費する。**
>
> **★★★順序はどちらでもよい。⇒ 開発者は同じ手番で `M36-02` を投入した。★設計卓が当初「`M39-01` を先へ」と推した理由**〔attestation と SBOM の付け直しが無駄〕**は成立していなかった**（計測点 `M-207`）**——どちらも CI がリリース時に生成する*機構*である。** **★★交点は SBOM のライセンス層 1 点だけであり、`000010_data_*.up.sql` は `REUSE.toml` の層 B グロブ `migrations/*_data_*.sql` に既に当たる。⇒ 交点 0 である。**
>
> **★★`M33` を開き直さなかった理由＝同 MS は 3 サブとも着地してクローズしており**〔`D-907`〕**、かつ `M33-03` の射程条件は「マイグレ消費 0 本」であった。⇒ 先例は `D-892`。**
>
> **★★★【2026-09-19 更新⑥＝`D-907`】`M33` がクローズした。⇒ 3 段とも着地し、`go test ./...` は FAIL 158 → FAIL 0 へ戻った。★リリースまでに残るのは `M36-02` の 1 本だけである。**
>
> **★★`M36-02` の前提は 3 本とも満ちた**〔`M36-01` ／ `M38-03` ／ `M33-03`〕**。⇒ 版ゲート 5b の「`go test ./...` が EXIT=0」も満たしている。★いつでも投入できる。**
>
> **★★★【2026-09-19 更新⑤＝`D-903`】`M33-03` を起票した。⇒ 未起票が 0 になり、残る 2 本はどちらも三点セット発行済みである。★いつでも投入できる。**
>
> **★★★【2026-09-19 更新④＝`D-902`】`M33-02` を受理した。⇒ 比較は全一致であり `M33-03` へ進める。★次のマイグレ連番は `000010` である。**
>
> **★★★`go test ./...` は `M33-03` の着地まで赤である**〔158 テスト＝版数直書きの歴史テスト 141 本 ＋ 旧 SQL の byte 比較 golden 17 本〕**。⇒ 移設が `M33-03` の射程だからである。★その間にリリースを打たない。**
>
> **★`M36-02` を `M33-03` の後に置く理由＝同サブは attestation と SBOM を作る。⇒ スキーマの形が確定してから来歴を付けるほうが、作り直しが要らない**（`D-899`）**。**
>
> **★★以下は `D-900` の記述（並列の根拠は生きている）：** リリースまでに要るのは 4 本であり、うち 2 本はいま並列で走れる。
>
> ```
> M38-03 ────────┐
>                ├─→ M33-03 ─→ M36-02 ─→ リリース
> M33-02 ────────┘
> ```
>
> **★★`M33-02` の並列条件を「並列 0 本」から「★走行中のサブが `migrations/` を触らないこと」へ緩めた**（`D-900`）**。⇒ 開発者の指摘であり、実査したところそのとおりであった。**
>
> **★★★根拠＝要件の理由は「ここから `migrations/` を触るため」である。⇒ 理由が `migrations/` を名指ししている以上、`migrations/` を触らないサブは該当しない。★`M38-03` のマイグレ消費は 0 本であり、触る面は `docs/usermanual/` ／ `scripts/release-targets.sh` の 1 行 ／ `CLAUDE.md` §8 の 1 行。⇒ 交点は 0 である。**
>
> **★★危険の実体は*ドリフト*である**——**`M33-02` は「旧 111 本が作るスキーマ」と「新 9 本が作るスキーマ」を突き合わせる。⇒ 走行中にマイグレが増えると比べる相手が動く。★見るべきは「マイグレを足すか」であって「走っているか」ではない。**
>
> **★マージ順はどちらでもよい。⇒ `M38-03` の差分は `migrations/` を 1 バイトも触らないため、先にマージしても潰した 111 本は戻らない。**
>
> **★★以下は `D-899` の記述（順序の理由は生きている）：** リリースまでに要るのは 4 本である。⇒ `M38-03` → `M33-02` → `M33-03` → `M36-02` → リリース。
>
> **★★前版は「`M38-03` → `M36-02` の 2 本。⇒ `M33` 族はリリースを止めない」と書いていた。★狭い意味では正しいが、決定に効く軸を落としていた**（計測点 `M-200`）**。**
>
> **★★★`M33` を先に置く理由＝潰せる窓が公開で閉じるからである。** `golang-migrate` は既存 DB の `schema_migrations` に適用済みの版を持ち、**その版のファイルが消えると `no migration found for version N` で abort する**〔実測の先例＝`M28-05`〕**。⇒ いまこれが効く DB は開発者の手元の 1 つだけであり、潰すコストはほぼゼロである。★公開すると、以後は (a) 旧 111 本を残す**〔綺麗にならない〕**か (b) 移行の道具を配る**〔重い〕**しか無い。**
>
> **★★★`M33` を `M36-02` より前に置く理由がもう 1 つある**——**`M36-02` は attestation と SBOM を作る。⇒ スキーマの形が確定してから来歴を付けるほうが、作り直しが要らない。**
>
> **★★`M33-02` は「並列 0 本」が要件である。⇒ `M38-03` の着地直後の枠が最も早い。★前提は他に満ちている**〔`M33-01` 受理済み ／ `M31` 族・`M32`・`M37` は全着地〕**。**
>
> **★★計測点 `M-182` の対処はまだ入っていない**——**本表は「走ったが未マージ」を表せない形のままである。⇒ 次に本節を書き換える手番で直すこと。**
>
> | 区分 | 本数 | 内訳 |
> |---|---:|---|
> | **走行中** | **2** | **`M37-04`**（確定反撃候補の初段発生）**／ `M37-05`**（始動位置のマス数の不変条件）**。⇒ 2026-09-13 並列投入。★面が交わらない** |
> | **投入可・未起動** | **0** | — |
> | **★★起票済み・未投入** | **2** | **`M33-02`**（**★★「並列 0 本」が要件。⇒ `M32-02` の着地と `M37` の全着地も要る**＝`D-868`）**／ `M36-01`**（**★`M32-02` の着地が版ゲート**） |
> | **★★起票済み・未投入**（**2026-09-14 追加＝`D-882`**） | **1** | **`M32-03`**（**操作説明書を `M37` の着地へ追随させる。★前提＝`M32-02` の着地 ＋ `M37-07` の着地**） |
> | **★★起票済み・未投入**（**2026-09-17 追加＝`D-892`**） | **2** | **`M38-01`** ／ **`M38-02`**（**★撮影で出たリリース前要望の第 2 束。⇒ 並列可。★三点セット発行済み**） |
> | **★未起票** | **1** | **`M33-03`**（`M33-02` の結果を受け取ってから）**。★★★`M36-02` は 2026-09-19 に起票した**（`D-898`）**。⇒ 三点セット発行済み・未投入。★前提＝`M36-01`**〔着地済み〕**＋ `M38-03`**〔門が説明書の `img` を見るため〕 |
> | **★★開発者の手番**〔**★★★2026-09-14 是正＝`D-879`。正しくは「走行済みであり、開発者の撮影の手番で止まっている。未マージ」である。⇒ 本区分は「開発者の手番」としか書けず、走行済みかつ未マージであることを言い表せていなかった**＝計測点 `M-182`〕 | **1** | **`M32-02`**（レビュー ＋ スクリーンショット撮影）。 **★★★2026-09-14 更新＝`M32-03` を新設した**〔`D-882`・開発者承認〕**。⇒ 改稿 8 章は同サブが引き取る。★`M32-02` は撮影を待たずに閉じる**（`D-880`）**。★撮影は `M32-03` の着地後である。** **★★★2026-09-14 更新＝`D-879`。本文は `M37` の大半より前に書かれており、改稿が要る章が 8 ある**〔表は `M32-overview` §3.2 末尾。**★§5.7 が最も重く `M37` の 5 サブが触る。★§5.20 は見た目がほぼ同じまま中身が変わっており最も見落としやすい**〕**。★残る 14 章は `M37` の影響を受けていない。** **★★2026-09-14 精査＝`D-878`。旧記述の「撮影は `M37` の全着地後」は安全側に広すぎた。⇒ `M37-07` が触るのは 22 画面のうち 3 つだけである**〔**§5.7** ／ **§5.6** ／ **§5.4**（フィルタへ足すかは実装判断）〕**。⇒ 19 画面は先行して撮れる。** **★★撮影は「マージ済みのビルド」で行うこと。⇒ §5.20 は `M37-04` を、バッジ系 5 章は `M37-06` をマージしていないと古い画面が写る。** **★★★旧記述「`M32-02` 本体の順序は動かさない」は `D-879` で失効した。⇒ 本文はもう書かれている。** |
> | **★★受理済み・未起票の要求** | **2 束** | **modifier の表現形式**（`modifier-display-differs-editor-and-viewer`）**／ modifier の項目の要否と文言**（`modifier-set-and-wording-final-review`。**★開発者が次マイルストーンで実施すると明言**） |
> | **改善レーン** | **9** | **後回し**（`D-802`。**★取り下げではない**） |
>
> **★★★順路は 1 本しかない。**
>
> ```
> M37-04 ∥ M37-05  →  M32-02〔開発者〕  →  M33-02 または M36-01  →  M33-03 / M36-02
> ```
>
> **★★`M33-02` は「並列 0 本」が要件であるため、`M36-01` と並列に置けない。⇒ どちらを先にするかは開発者の判断である。★設計卓の推奨＝`M33-02` を先。理由は、同サブの前提**〔凍結点からのマイグレ増分を対応表へ追記する〕**が待つほど重くなるのに対し、`M36-01` の前提は時間で劣化しないためである。**
>
> **★★★`M37` は 6 サブとも起票済みであり、うち 4 サブが着地した。⇒ `M37-01` / `M37-02` / `M37-03` / `M37-RESEARCH-01`。★`M37-03` は 3 件中 `B01` の 1 件だけが着地**〔`B03` は除外、`B15` は差戻〕**。**

---

#### 【2026-09-12 時点の記述（**★歴史として残す**）】

**★★★上表の「ledger 行」と、下の「実装サブの本数」は別の軸である。⇒ 混ぜないこと。★ledger 行は identity の数であり、サブは作業の単位である。**

**★★開発者の見立てとずれていたため数え直した**（2026-09-10・`D-803`）**＝「走行中 3 ＋ マイルストーン 4」ではない。**

| 区分 | 本数 | 内訳 |
|---|---:|---|
| **走行中** | **1** | `M30-06`（2026-09-12 起動） |
| **投入可・未起動** | **1** | `M32-01`（**★チェックリスト v1.0.0 発行済み ＋ 指示書 v1.1.1**＝`D-844`。**`M30-06` と面が互いに素で並列可**） |
| **起票済み未投入** | **3** | `M30-07`（**`M30-06` の着地後。★順序固定＝ファミリー総数 372 → 369**）／ `M33-01`（**`M30-07` の着地後。★同サブが `000112` を消費し凍結点を動かすため**）／ `M32-02`（**`M32-01` の着地後。★フェーズ4 の最後**） |
| **未起票** | **2** | `M33-02` ／ `M33-03`（**`M33-01` の対応表を受け取ってから起票する**＝`M33-overview` §状態） |
| **改善レーン** | **9** | **後回し**（`D-802`。**★開発者が進捗優先を選んだ。⇒ 取り下げではない**）**。★★本数は `D-802` 時点の値であり、2026-09-12 の chore レーン**（派生資料の再生成 ＋ 教訓のバッチ反映）**が上記 9 に含まれる項目を消化したかどうかは設計卓から確かめられない。⇒ 数は据え置く。★実査が要る** |

**★★★【2026-09-13 是正＝`D-853` / 計測点 `M-170`】上表は母集団を「起票済みのサブ」に取っていた。⇒ *受理済みだが未起票の要求*が別に在り、数え漏らしていた。**

| 区分 | 本数 | 内訳 |
|---|---:|---|
| **★★受理済み・未起票**（**新設**） | **2 束** | **`A`**（運び量・始動位置の 3 方式入力 UI。**★`D-758` のフェーズ5 割付から差し戻し**）**／ `B01`〜`B15`**（2026-09-10 追記の 15 項目。**★`D-816` が「仕分けは未実施」と記録して以降、消化記録が無い**）**。⇒ `M37` として起票することを提案し承認待ち** |
| **★★承認済み・未起票** | **1** | **`M36`**（リリースパッケージング）**。⇒ `M36-overview` v1.0.0 を起票済み。★サブ 2 本は未起票** |

**★★残件の母集団は 2 つ取ること。⇒ (1) 起票済みのサブ ／ (2) 受理済みだが未起票の要求。★(2) は board の裁定行と `followup` の「仕分け未実施」から拾う。**

---

**★★★2026-09-10 → 2026-09-12 の 2 日で 11 サブが受理された。⇒ 走行中 5 / 未投入 7 / 未起票 3 の 15 本が、5 本まで縮んだ。**

| マイルストーン | 状態 |
|---|---|
| **`M26`** | **★5 サブとも完了**（`D-833`）**。★★★ただし「公開ゲート A の穴は塞がった」は誤りであった**（2026-09-20 是正＝`D-918` / 計測点 `M-210`）**。⇒ 同 MS が産んだ followup 9 件のうち 7 件が未着手であり、うち `shadcn-ui-vendored-without-attribution` は「公開前に必達」と明記されている。★MS のクローズと、その MS が産んだ残件の解消は別物である。⇒ 実体 4 件は `M40` としてまとめることを提案している**（承認待ち） |
| **`M27`** | **★4 サブとも完了**（`M27-03` 受理済み） |
| **`M28`** | **★7 サブとも完了** |
| **`M29`** | **★2 サブとも完了** |
| **`M30`** | **★`01`〜`05` 完了。⇒ 残りは `06`**（走行中）**と `07`**（投入待ち） |
| **`M31`** | **★`RESEARCH-01` ＋ `01`〜`06` すべて完了**（`D-837`） |
| **`M32`** | **★`01` 投入可 ／ `02` は `01` の後。⇒ フェーズ4 の最後である** |
| **`M33`** | **★`01` は `M30-07` の着地後。`02` / `03` は未起票** |
| **`M34`** | **★2 サブとも完了**（`D-834`） |
| **`M35`** | **★3 サブとも完了**（`D-822`。**孤児 4 件の是正サブ＝`M35-03` は着地済み。⇒ 旧表の「未起票 3」から外れた**） |
| **`M14`** | **★完了**（`D-811`） |

**★★★見落としやすいのは `M26-03` / `M26-04` である。⇒ どちらも公開ゲート A の残りであり、`M26-overview` §4 の依存図に載っている。★`M26-04` は「公開前スキャン」であり、公開直前のゲートそのものである。**

**★★`M14` 独立レーンは完了した**（2026-09-10 開発者報告＝`D-811`）**。⇒ 上表からは外れる。★ただし「いったん完了」であり、`custom_states` への追加が入る可能性はゼロではない。**

**★★★これに加えて、リリース後まもなく大きめの SF6 アップデートが入る見込みである**〔1 キャラ追加 ＋ バトルバランス調整〕**。⇒ フェーズ4 の残数には入らないが、`M32`**（データ鮮度）**の射程に効く。★`followup` の `sf6-major-update-after-release`。**

**★★本節は数が動くたびに書き換えること。⇒ ボード §3.4 の裁定が正本であり、本節はその写しである。★食い違ったらボードが正しい。**

---

### M25 — フェーズ4 の再定義

- **`M25-RESEARCH-01`（仕分け RESEARCH）は完了・受理済み**（`D-642`）。成果物＝3 層 ledger 309 行。
- **残るのは `CHANGE-150`＝`REQ-001` §7 の改訂 1 本**（§6）。**本書の承認と同じ手番で行う**（`D-634` (6)）。
- **★`as-built` に相当するものは「開発者が承認した本書」である。先例＝`CHANGE-020`。**

### M26 — 公開ゲート A（法務・ライセンス・セキュリティ・公開リポジトリ生成）

- **A ゲート 12 行 ＋ `α③` 8 行。**
- **★`α③` は `D-637` で設計が固まっている**——**一方向生成**〔`公開リポ = f(tag, 許可リスト)`〕**／ 許可リスト方式**〔denylist ではない。先例＝`check-doc-inventory.sh` / `pre-push-guard.sh` / `design-desk-guard.sh`〕**／ 検査に positive control を置く ／ 公開リポは状態を持たないのでドリフトしない。** ⇒ `M26` は設計ではなく実装である。
- **★`A6`（LAN 認証）は as-built が実査済み**（`M25-RESEARCH-01` §4.2）——**本番ルート 71 本。`password_enabled=true` のとき認証不要は 5 本で残り 66 本を保護。★既定の `false` では middleware が全 API を意図的に素通しする。** ⇒ **「簡易ログイン機能が無い」意味では解消済み、「LAN API は常に認証必須」の意味では未解消。** **★★2026-09-02 是正＝`D-653`**——**「どちらの状態で公開するか」は既に決まっている。`DES-005` §5.1 の初回起動ウィザードが LAN 共有とパスワードを明示的に選ばせ、`DES-002` §8 が「パスワード未設定での LAN 公開」に警告と明示的な同意を課している。⇒ `M26` に残るのは「その警告と同意が実装されているか」の実査である**（§開発者への確認事項 2 の撤回）。**★侵入テストは行っていない。**
- **★`A5`（正式名）はここに属するが、実行は `M28` の破壊窓で行う**（§4）。
- **★`Gitleaks`（または `betterleaks`）を公開前に回す**は `α③` の一部として `M26` に含む（開発者要求・2026-09-02）。

### M27 — `α②` 画面品質（エディタ・一覧・詳細）

- **★★【2026-09-02 是正】42 行は ledger の raw 行数である。名寄せ後は 33 identity、うち 10 件は主題が `M31` / `M30` であり移した。⇒ `M27` が負うのは 23 identity である**（`M27-overview` §2＝`D-668`）。**フェーズ4 で最大であることは変わらない。**
- 中身は **`α②` のうち「利用者が触る面で、機能ができないか壊れて見える」もの**——エディタのバリデーションエラーが UI から到達不能／タブのバッジが誤誘導／戻るリンクの固定／保存失敗時にメッセージが空、等。
- **★★サブは 4 本である**（`M27-overview` v1.3.0）——**`M27-01` ヒット種別と相手サイズ ／ `M27-02a` 到達性（保存の動線・候補選択） ／ `M27-02b` 入力仕様の 3 状態と強度 ／ `M27-03` 一覧・詳細。★`M27-02` は起票時に 2 本へ割れた**（`D-683`）。 以下は起票時の記述： **★サブは 5〜6 本になる見込み。** 面で割るか、症状の型（「できない」／「壊れて見える」／「誤情報を出す」）で割るかは着手時に決める。

### M28 — ★破壊的変更の窓（正式名リネーム ＋ `FR702`）

- **`checklist` §4 の「破壊的変更の窓（1 回にまとめる）」を実体化したマイルストーンである。**
- **含むもの**——**(1) `A5` 正式名リネーム**〔module path・既定データディレクトリ・定数・ドキュメント。**`checklist` は「実質マイルストーン 1 本分」と書いている**〕**／(2) `FR702` が要求するスキーマ変更（`CL-4-3`）／(3) `FR702` 本体の実装。**
- **★含まないもの**——**マイグレーションのベースライン化。`D-648` で「実施しない」と確定した**〔実測 1.27%。`checklist` 自身の「無視できるならやらない」に当たる〕。**⇒ `checklist` §4 が挙げた 3 点のうち 1 点が落ちたため、窓は 2 点で構成する。**
- **★`FR702` の方向性は既に固まっている**（`checklist` §5.1＝**検証はユーザー任せ。アプリは影響可能性のコンボだけを出す。破綻の自動断定はしない**）。**着手時にゼロから議論しない。**
- **★このマイルストーンだけは「先送りするほど破壊面が単調増加する」性質を持つ**（`checklist` `A5`）。**⇒ `M27` の完了を待たずに着手してよい。**

### M29 — `α②` 用語・ラベルの統一と入出力

- **29 行。** 語彙の割れ〔空レシピのラベルが 4 種／コピーの言い回しが 3 種／SA ゲージの単位「本」の有無／「ステップ」の二義／相手状態の語彙〕と、入出力〔CSV の取込・書出・プレビューと確定の重大度の非対称・行数上限の黙った切り捨て・バックアップ／復元・下書きの往復〕。
- **★語彙の統一は 1 サブにまとめる。** 面ごとに直すと、直した先で再び割れる。

### M30 — `α②` 仮想コントローラ

- **★★【2026-09-02 更新＝`D-686`】11 行 ＋ `M27` から 4 件**〔`SD-010` ターゲットコンボタブ ／ `SD-011` 非表示技・キャラ別技タブ ／ `SD-019` 強度なし技の選択 ／ `SD-022` ホールド／ジャスト版の表示〕。**★4 件はいずれも `M30` 側の代表 ID**〔`P4M-007` / `P4M-008` / `P4M-016` / `P4M-018`〕**と同一識別子であり、名寄せで畳まれる。⇒ identity は増えない。★増えるのは「`M27` に置いていた片割れが消える」ことである。**
- **独立したクラスタであり、他のマイルストーンと依存しない。**
- **★`virtual-controller-key-layout-not-like-real-pad` は `要判断` として据え置いた**（好みが決め手）。**⇒ 着手前に開発者へ 1 行確認する。**

### M31 — `α②` セットプレイ・確定反撃・共通部品

- **★★【2026-09-02 是正＝`D-686`】9 行ではない。** **`M27` から移した 9 件のうち 6 件は `M31` が既に `代表 ID` を持っており、名寄せで畳まれる**〔`SD-001`⇄`P4M-004` ／ `SD-003`⇄`P4M-005` ／ `SD-023`⇄`P4M-019` ／ `SD-017`⇄`P4M-015` ／ `SD-018`⇄`P4M-014` ／ `SD-004`⇄`P4M-023`〕。**⇒ 純増は as-built 由来の 3 件だけである。★さらに `P4M-012` / `P4M-013`〔共通画面部品＝キャラ選択の Enter とドラッグ判定〕は `M27-02a` が部品ごと所有するため `M31` から引く。** **⇒ 見込み 12 前後。★確定は `M31-overview` の起票時に行う。**
- 起き攻めまわり〔`SD-006`＝ドライブラッシュなしにチェックを付けたら DR 版にも自動で付く／凡例の注記〕、確定反撃、共通画面部品。
- **★`SD-006` は開発者が「修正自体は単純な見込みだが、いったん直すときに再整理はしたい」と述べている**（`D-649`）。**⇒ 「再整理」の射程を起票前に決める。設計卓の見立ては「起き攻めまわりを 1 サブでまとめて触る」である。**

### M32 — B 摩擦低減

- **13 行。** コード署名（`B1`）→ AV 誤検知申請（`B2`）の順は固定。データ鮮度（`B3`）は `M14` の完遂に依存する。
- **★`B4`（操作説明書）はフェーズ4 である**——**`α②` の判定規則 (7) は「文書整備のうち利用者向けでないもの」をフェーズ5 に送るが、`usermanual` は利用者向けであり例外扱いにした**（`D-646`）。**★`checklist` の逐語＝「説明が要る箇所＝設計が負けている箇所でもある。説明書を作る過程で UI の粗が出たら記録すること」。⇒ `M32` は `M27`〜`M31` の後に置く。**
- **★★`FN-02`**〔`future-notes/combmgr-features.txt`〕**は公開物から外すことに決した**（`D-656`）。**⇒ `B6`（クラシック前提の明示）は本ファイルでは満たせない。★`README` と `B4` 操作説明書で満たす。★`M32` の前提が 1 つ変わった。**

### M14（独立レーン） — `α①` 残キャラ seed 完遂

- **★★残り 14 体である**（**2026-09-02 是正＝`D-655`。一次源＝`docs/progress/M14-RESEARCH-03-report.md` §2.1**）——**`characters` に行が無い 12 体**〔`aki` `akuma` `alex` `blanka` `cammy` `chun_li` `dee_jay` `e_honda` `ed` `elena` `sagat` `yasmine`〕**＋ 移動 9 種のみの仮登録 2 体**〔`c_viper` / `dhalsim`〕。
- **★`character_data/*.csv` は 31 ファイルで、31 キャラすべてが `precheck=済`。⇒ 手入力待ちは 0 である。**
- **★★第四波を 1 波でやる場合のマイグレは 13〜16 本の見込み**（`000081` から `000093`〜`000096`）。**★本数はキャラ数に比例せず段の数で決まる。⇒ 波を分割すると段の数が波の数だけ掛かる**〔第二波は 1 キャラで 6 本〕。
- **`M14-RESEARCH-03` は完了・受理した**（`D-655`。報告＝`docs/progress/M14-RESEARCH-03-report.md`）。**★設計卓が数えた「5 種類」は実測 13 段だった。⇒ `M14-overview` §3 の「5〜6 段」を 1.4.0 で是正した。**
- **★独立レーンのまま。ただし公開前に必ず完遂する**（`D-634` (7)）。**⇒ `M14` の完遂が公開ゲートに入った。**

---

## 4. 依存関係・進行順序

```
M25（本書の承認 ＋ REQ-001 §7 改訂）
  │
  ├─→ M26（公開ゲート A）… A2 → A1 の順は固定。許可リスト生成が A3/A7/A8/α③ の確認基盤
  │      │
  │      └─→ M28（破壊窓: A5 正式名 ＋ FR702）… ★M27 の完了を待たない
  │
  ├─→ M27（α② 画面品質）─→ M29（用語・入出力）
  │      └─→ M31（セットプレイ・確定反撃・共通部品）
  │
  ├─→ M30（仮想コントローラ）… 独立
  │
  └─→ M14（α① 残キャラ）… 独立レーン
         └─→ M32（B 摩擦低減）… B3 データ鮮度が M14 に依存。B1 → B2 の順は固定
                                 ★M27〜M31 の後（B4 が UI の粗を拾うため）
```

**確認された依存**（`M25-RESEARCH-01` §5.2）——`A2`→`A1` ／ `A6` を先に実査 ／ `A5` と `FR702` schema は同一の破壊窓 ／ `B1`→`B2` ／ 許可リスト生成が `A3`/`A7`/`A8`/`α③` の基盤 ／ `α①` は `M14-RESEARCH-03` の inventory に先行される。

**★`checklist` §6 の 10 段との矛盾 1 件を、本書が解消する**——**§6 は「C 機能は公開後」とするが、`D-634` は `FR702` と `α②` を公開前のフェーズ4 に含める。⇒ 開発者の新しい判断を採る**（`M25-RESEARCH-01` §4.5 が指摘した矛盾 (1)）。

---

## 5. CHANGE 見込み・採番

- **払い出し済みブロック ＝ `150`〜`161` の 12 本**（`D-639`）。**次に採番するのは `150`。★`149` は流用しない**（`D-626`）。
- **★写し先は 2 か所である**——`change-number-registry` §1 ＋ ボード §2.1。**フェーズ4 は並列運用が無いため契約ファイルを作らず、`D-277` の「4 か所」は 2 か所と読み替える**（`D-639`）。
- **★★12 本では足りない見込みである。** サブ数の見込みは **`M25` 1 ／ `M26` 4 ／ `M27` 6 ／ `M28` 3 ／ `M29` 4 ／ `M30` 2 ／ `M31` 2 ／ `M32` 5 ＝ 27 本**。**`M24` は 13 サブで 11 本を消費した**（比 0.85）。**⇒ 単純比では 23 本前後になる。** **★ただし `α②` は as-built の是正が多く、設計書を動かさないサブは CHANGE を消費しない。⇒ 見込みは 16〜20 本。**
- **⇒ 追加ブロックを 1〜2 回請求する。本書では請求しない**（`D-639` の「使い切ったら追加ブロックを請求する」に従う。先例＝`144`〜`149`）。
- **次に払い出すマイグレ連番 ＝ `000081`。** **★消費が見込まれるのは `M28`（`FR702` スキーマ）と `M14`（seed 波）だけである。**

### 5.1 推奨モデル

`SUPP-001` §6.1 の方針を継承する。**UX・正しさ・破壊的マイグレ・新ドメインの実装は高精度実装 ＋ Plan Mode 必須**、read-only 調査は推奨モデルを下げてよい。実モデル名は `docs/human-notes/model-allocation.md` が正本。

**★`M28` は Plan Mode 必須**（`CLAUDE.md` §9＝スキーマ・API 契約・データ移行を伴う変更）。

---

## 6. `REQ-001` §7 の改訂方針（`CHANGE-150`）

**手順は `D-634` (6) が定めている**——**通知書 ＋ registry 登録 → 本書の承認 → `REQ-001` §7 改訂 ＋ `change-report`。**

| # | 改訂内容 |
|---|---|
| **1** | **フェーズ4 の定義を差し替える**——現行「外部データ追従・拡張」＝`FR702` / `NFR407` / `ISSUE-007` の 3 項目 → **「初回全体公開までにやるべきこと」＝公開ゲート A ／ 摩擦低減 B ／ `α①`〜`α③` ／ `FR702`** |
| **2** | **フェーズ5 を新設する**——「本当に後回しでよいもの」 |
| **3** | **フェーズ6 以降を新設する**——「構想」。**★`future-notes` の 4 機能は確定項目として載せない**（`D-647`） |
| **4** | **`NFR407` を「非対応」として残し、フェーズ定義から外す**——**★撤回ではなく非対応である。要件そのものは記録として残す**（`D-634` (2)） |
| **5** | **`ISSUE-007` を「後付け」と明記し、`B6`（クラシック前提の明示）をフェーズ4 に残す** |

**★`DES` 本体は 1 文字も触らない。** 改訂は `REQ-001` §7 のみで、`as-built` 相当（＝承認された本書）が確定してから行う。

---

## 7. 数の根拠（ledger 309 行の内訳）

| `disposition` | 件数 | 出所 |
|---|---:|---|
| **フェーズ4** | **133** | 96（`M25-RESEARCH-01` 実行数）＋ 35（`D-646` 規則適用）＋ `FN-02` ＋ `SD-006` |
| **フェーズ5** | **102** | 24 ＋ `SM-029` ＋ 76（規則適用）＋ `SD-007` |
| **フェーズ6 以降** | **20** | 11 ＋ 9（`D-647`） |
| **判断済み・再開しない** | **43** | 35 ＋ 5（既に解消）＋ `CL-4-2` ＋ `SD-002` ＋ `SD-008` |
| **`要判断`** | **11** | 10（規則 (8) 据置）＋ `SD-005`。**★`D-651` で `SD-005` はフェーズ5、`combos-orphan-…` は解消済みへ動いた。⇒ 実質 9** |
| **合計** | **309** | **★保存則が成立する** |

**★`要判断` は 143 → 実質 9 になった**（`D-645` / `D-646` / `D-651`）。

### 7.1 `α②` 91 行の面ごとの内訳

| 面 | 件数 | 行き先 |
|---|---:|---|
| エディタ・一覧・詳細 | **42 raw ／ 33 identity ／ ★★`M27` が負うのは 19** | `M27`（**★2026-09-02 に 2 度動いた**——**まず主題の見直しで 10 件が `M31` / `M30` へ**〔`D-667`〕**、次に `代表 ID` 欄の機械的な突合でさらに 4 件が `M30` へ**〔`D-686`＝`SD-010` / `SD-011` / `SD-019` / `SD-022`〕） |
| 用語・ラベル・入出力 | **29** | `M29` |
| — | — | **★★【2026-09-06 追随注記＝`D-756`】上の「29」は内訳が引けない**（計測点 `M-127`＝ledger の `触る面` 列にこの値を再現する組み合わせが無い）**。** **★`M29-01`（語彙）の実査は 24 か所である**〔群 1 空レシピ 6 ／ 群 2 コピー 6 ／ 群 3 SA ゲージ 9 ／ 群 4 相手状態 7。i18n キー 15 ＋ 直書き 9〕**。** **★★29 に合わせにいっていない。⇒ 合わせようとしたら、それこそが誤りである**（`M29-01` チェックリスト §0.2）**。** **★総数が 29 になるかは `M29-02`（入出力）の実査を待つ。⇒ そこで確定させ、合わなければ本行の「29」のほうを直す。** |
| 仮想コントローラ | **11** | `M30` |
| セットプレイ・確定反撃・共通部品 | **9** | `M31` |
| **合計** | **91** | |

---

## 8. ★フェーズを分割しないことの再判定（`D-634` (3) の条件）

**`D-634` (3) は「フェーズは分割しない」と決めたが、条件つきだった**——開発者の逐語＝**「分けなくてマイルストーンの数がどうなるか次第」**。**⇒ 仕分け RESEARCH が MS 本数を出した時点で 1 度だけ再判定する、としてあった。**

**【再判定の結果＝分割しない。当初の判断を維持する】**

- **本数は `M25`〜`M32` の 8 本 ＋ `M14` 独立レーン。**
- **★フェーズ3 は `M13`〜`M24` の 12 本だった**〔`phase3-overview` §3 を数えた。**★`CLAUDE.md` §1 は「M13〜M23」と書いているが、同 §3 は `M24` まで持つ。⇒ `CLAUDE.md` の記述が 1 本少ない**〕。**⇒ 8 本は過剰ではない。**
- **★分割すると「フェーズ5＝本当に後回しでいいもの」の座が埋まる**（`D-634` (3)）。**開発者の当初の逐語に忠実な形は、分割しないことである。**
- **★開発者は「分けなくてもいいタイミングで設計卓を分けたり、phase 指導担当が挟まることを嫌気」とも述べている。⇒ 運用上も分割の利が無い。**

---

## 8.5 ★★フェーズ4 に特有の運用 2 件（2026-09-02 追加）

### 8.5.1 ★★overview の項目は「射程の上限」であって「必達のリスト」ではない（`D-675`）

**開発者の逐語（2026-09-02）＝「ひょっとしたら、一部やらないという結論に着地する事項もあるかもしれず、そこは指示書作成時点やプランモードで決めたいです。」**

| # | 帰結 |
|---|---|
| **1** | **各 overview が列挙した identity は、そのマイルストーンで「扱いうる」ものの上限である。⇒ 全部やることを約束していない** |
| **2** | **「やらない」に落とす判断は、指示書作成時点または Plan Mode で行う。★設計卓が overview の段階で先に落とす必要はない** |
| **3** | **★落としたら記録する**——**指示書の「やらないこと」へ 1 行、または完了報告の残課題へ 1 行。★「触れなかった」と「やらないと決めた」を区別する** |
| **4** | **★これは `D-646` の判定規則を弱めない。** 規則はフェーズの振り分けであり、**マイルストーン内で「今回はやらない」に落とすのは別の判断である** |

**★★なぜこれを書くか**——**設計卓は overview に列挙したものを「決めた」と扱いがちである。⇒ 実際には「候補として並べた」段階のものが混ざる。★区別を書いておかないと、製造が「全部やらなければならない」と読む。**

### 8.5.2 ★★「決定材料サブ」という型（`D-676`）

**開発者の逐語（2026-09-02）＝「`M26-01` は実装がない工程だが、Research の指示書ではなかったので、成果物レポートが何になるかよくわからなくなるなど、少し混乱がありました。phase4 はリリース準備で他にも似た工程はあると思うので、教訓としてください。」**

**★★設計卓の指示書設計の欠陥である**（計測点 `M-114`）。

#### 何が起きたか

**`M26-01` は「実装差分 0・出力は文書 2 本」だったが、通常サブの型で書き、出力を `M26-01-report.md` にした。** ⇒ **RESEARCH でも通常サブでもない第三の型**になり、次の 3 つが決まらなかった。

| # | 決まらなかったこと |
|---|---|
| 1 | **完了報告はどれか**（`*-completion-report.md` が存在しない） |
| 2 | **レビューをやるのか**（ヘッダは「不要」だが `/implement_plan_full` は Phase B を含む） |
| 3 | **設計伝達レポートの必須源泉が欠ける** |

#### ★★型を定義する

**フェーズ4 は「リリース準備」であり、調査でも実装でもない「決めるための文書を作る」工程が複数ある。⇒ 型を 1 つ置く。**

| 項目 | 決め |
|---|---|
| **実装差分** | **0**（コードを変えない） |
| **★★出力** | **★必ず `docs/progress/M{N}-{NN}-completion-report.md` を含める。** **★全数表など大きな成果物は `M{N}-{NN}-report.md` へ分けてよいが、完了報告は別に必ず作る** |
| **レビュー** | **★「事実性に絞って」実施する**——**対象は 数・母数・引用の正確さ。設計判断の是非は見ない**（`M26-01` で開発者が実際に選んだ形＝`D-671`） |
| **Plan Mode** | **内容次第。★判断を含むなら必須** |
| **RESEARCH との違い** | **RESEARCH は「判断しない」。決定材料サブは「1 か所だけ判断する」ことがある**〔`M26-01` は層の割当〕 |
| **CHANGE** | **★本サブでは起票しない。成果物の承認後に起票する**（`M26-01` §3.3 の形） |

**★★「完了報告を必ず作る」がこの型の核心である。** **理由＝`check-progress-log-index.sh` は `*completion-report*.md` しか走査しないため、`-report.md` だけだと索引行が落ちても永久に緑になる**（`followup` `report-class-subs-outside-index-check`。**該当は 17 件**）。

#### 該当する見込みのサブ

| サブ | なぜ該当するか |
|---|---|
| **`M26-01`**（済） | 依存棚卸し ＋ 三層のパス割当 |
| **`M26-03`** | 法務ポスチャの再点検 ＋ データ来歴の切り分け（**調査と記録が主**） |
| **`M27-01` の前段** | **画面モックでヒット種別の名前を決める**（`D-674`） |
| **`M28-02a`** | **`FR702` のスキーマとバックエンドの決定**（`D-668`） |

**★該当するかどうかは起票時に判定する。★迷ったら通常サブの型を採り、完了報告を作る。**

---

## 9. 参照

| ID | パス |
|---|---|
| 仕分け RESEARCH 報告 | `docs/progress/M25-RESEARCH-01-report.md`（**★本書の数の一次源**） |
| 公開前チェックリスト | `docs/human-notes/combmgr-prerelease-checklist.md`（A / B の定義・破壊窓・確定済みの前提） |
| `α②` の源泉 | `docs/human-notes/phase4-memo.txt` ／ `docs/human-notes/Memo_Someday.txt` |
| 追跡表 | `docs/handover/followup-backlog.md`（**★`割付` 欄が `D-646` の適用結果を持つ**） |
| 状態の正本 | `docs/process/parallel-board.md`（`D-634` / `D-637` / `D-639` / `D-643`〜`D-651`） |
| 採番 | `docs/handover/change-number-registry.md` §1 |
| `M14` レーン | `docs/instructions/M14-overview.md` ／ `docs/instructions/M14-RESEARCH-03-seed-wave-work-inventory.md` v1.0.1 |

---

## 開発者への確認事項（**★2026-09-02 に 4 件すべて決着した＝`D-653`**）

| # | 内容 | 決着 |
|---|---|---|
| **1** | **§3 のマイルストーン構成の承認** | **★承認**（開発者の逐語＝「1.OK」）。**⇒ `CHANGE-150` を起票し同日反映した**（`D-654`） |
| **2** | ~~`A6`（LAN 認証）をどちらの状態で公開するか~~ | **★★撤回する。設計卓の誤りである**（計測点 **`M-111`**）。**⇒ 詳細は下記 §開発者への確認事項 2 の撤回** |
| **3** | **`FN-02` は書き直すか、公開物から外すか** | **★★外す**（`D-656`。開発者の逐語＝「外してください」「完全に陳腐化しているので、そのうち私が消します」）。**⇒ `M26-02` の許可リストから外す。★`B6` は `README` と `B4` 操作説明書で満たす** |
| **4** | **`M28`（破壊窓）を `M27` より先に通してよいか** | **★設計卓に一任**（開発者の逐語＝「特にこだわりはないのであなたの決めた順に任せる」）。**⇒ `M26` の `A5` 準備が済み次第、`M27` と並べずに `M28` を先に通す**（§4 の順序どおり） |

### ★★確認事項 2 の撤回（`M-111`）

**設計卓は「`A6` をどちらの状態で公開するか」を開発者判断として立てたが、答えは既に設計書にあった。**

| 出所 | 逐語 |
|---|---|
| `DES-005` §5.1 | **初回起動ウィザードの Step 5＝LAN 共有モード設定**〔有効／無効。**初期値は無効**〕**／ Step 7＝パスワード設定**〔**任意。LAN 共有有効時のみ表示**〕 |
| `DES-002` §8 | **LAN 共有モード ON へ切り替える際、パスワード未設定なら「パスワード保護を強く推奨します」と警告し、パスワード設定へ誘導する** |
| `DES-002` §8 | **設定せずに続行を選んだ場合は明示的な同意ダイアログ**〔「パスワードなしで LAN 公開します。よろしいですか」〕**で承認させる** |
| `DES-002` §8 | **防御責任＝LAN の電波が届く位置からの悪意あるアクセスは、ルーターの Wi-Fi 認証とファイアウォールが担う** |

**⇒ 設計卓が推奨した「初回起動時に明示的に選ばせる」は、`M22` 期に既に決まって実装されている。★問い自体が成立していなかった。**

**★原因**——**`M25-RESEARCH-01` §4.2 は middleware の as-built**〔`password_enabled=false` のとき全 API を素通しする〕**を正しく報告していた。設計卓はそれだけを読み、「では既定をどうするか」という問いに変換した。★同じ節が「設計書は LAN を信頼ネットワークとして『パスワードは任意』とする」とも書いていたのに、そちらを引かなかった。**

**⇒ playbook §4.58**〔設計書は実在の証拠にならない〕**の裏返しの誤りである**——**実装の as-built だけを見て、設計書に答えが在るかを確かめなかった。**

**★`A6` に残る作業**（`M26` の実査項目にする。**★設計卓は実装を読めないため断定しない**）——

| # | 実査すること |
|---|---|
| 1 | **`DES-002` §8 の「強く推奨します」の警告が実装されているか** |
| 2 | **同「明示的な同意ダイアログ」が実装されているか** |
| 3 | **`SUPP-001` の「LAN モード API 無認証一般の是正は対象外・別途判断」が changelog の過去記録であることを、現行 normative 記述と突き合わせて確認する** |

### `FN-02` とは何か

**`docs/human-notes/future-notes/combmgr-features.txt`（30 行）＝対外向けの機能一覧である。** 中身は 2 節。

- **「■ 今できること」15 項目**——コンボ管理／セットプレイ管理／表記の切り替え／カスタムプリセット／横並び比較／共有／抜粋登録／**画面上の仮想コントローラで入力**／**クラシック / モダン両対応**／重複コンボの自動検知／タグ整理／PC・スマホ対応／インストール不要／動作が軽快
- **「■ これからの予定」8 項目**——**物理コントローラ・キーボードでの直接入力**／**セットプレイの提案**／対戦統計の記録／対戦統計の画像取り込み／一人 SNS 風の知識管理／連携技の記録／**確定反撃の記録**／目標（課題）管理

**★★陳腐化しているのは「モダン両対応」の 1 行だけではない。**

| # | 陳腐化 |
|---|---|
| **1** | **「クラシック / モダン両対応」＝過大表示。** モダンは後付けであり、`B6` は「クラシック前提」を明示することを求めている |
| **2** | **★「これからの予定」に、既に実装したものが並んでいる**——**セットプレイの提案は `M19` でクローズ済み ／ 確定反撃の記録は `M18` でクローズ済み ／ 物理コントローラ・キーボードでの直接入力は `M21` で実装が進んでいる**（ボード §1.1 / §1.2 / §1.5） |
| **3** | **「一人 SNS 風の知識管理」は本体外へ出た**（Recurfold＝独立アプリ。`checklist` §3.5）。**⇒ 本体の「予定」として書くと誤解を招く** |

**⇒ 「モダンの 1 行を直す」では足りず、2 節とも書き直しになる。**

**★設計卓の推奨は「公開物から外す」である。** 理由——**対外向けの機能一覧は `README` と操作説明書（`B4`）が担うべきであり、同じ役割の資料を 2 つ持つと再び陳腐化する。★実際、本ファイルは少なくとも `M18` / `M19` / `M21` の 3 マイルストーンぶん追随していない。**

---

*以上*
