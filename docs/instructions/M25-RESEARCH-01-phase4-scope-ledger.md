# 指示書 M25-RESEARCH-01: フェーズ4 スコープ再編のための 3 層 ledger

| 項目 | 内容 |
|------|------|
| 文書ID | M25-RESEARCH-01 |
| バージョン | **1.1.0** |
| 種別 | 調査指示書（read-only。実装・マイグレ作成/編集・コード変更・seed 変更・ファイル移動を一切行わない） |
| 対象 | 製造担当 Claude Code |
| モデル | 高速実装（read-only 調査。grep / view 中心の事実列挙。実モデル名は `docs/human-notes/model-allocation.md` が正本。実使用は開発者判断） |
| レビュー | 不要（read-only。実装物が無いため機械レビュー対象なし） |
| 作成者・作成日 | 設計担当 Claude（親チャット・設計卓・フェーズ4 期）/ 2026-09-02 |
| CHANGE | **消費なし**（設計書本体を変更しない。先例＝`M24-RESEARCH-01`） |
| マイグレ | **消費なし** |
| 前提 | `docs/handover/phase3-to-phase4-handover.md` §0 / §3 ／ ボード `D-634`（スコープ再編の前提 10 件） |
| 主参照 | `docs/progress/M24-RESEARCH-01-report.md` §2.1 / §2.2 ／ `docs/human-notes/combmgr-prerelease-checklist.md` ／ `docs/handover/followup-backlog.md` ／ `docs/human-notes/phase4-memo.txt` ／ `docs/handover/m24-close-report.md` §2 / §3-1 |

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| **1.1.0** | **2026-09-02** | **開発者への確認事項 2 件が承認された**（`D-640`）。**(1) `dbtest.Setup` の実測（G-4）を含める ／ (2) `A6` の実査は暫定案の深さ。** **⇒ 本指示書に開発者判断待ちは 0 件になった。投入可能である。** |
| 1.0.0 | 2026-09-02 | 初版。フェーズ4 / フェーズ5 / フェーズ6 以降の 3 層 ledger を作る。母集団は 7 つの源泉に散っており、確定させるところから始める。 |

---

## 0. この指示書の特殊性

### 0.1 製造担当として起動し、調査だけ実施して閉じる

製造担当セッションを起動し、**§4 の事実列挙 → §5 のレポート作成 → クローズ**まで行う。**実装は本調査の対象外**であり、後続のサブが別指示書で扱う。

### 0.2 read-only 厳守

**許可**: `view` / `grep` / `git log` / `git show`（`docs/` 対象）/ `find` / `ls` / `wc` / SQL の `SELECT`。

**書き込みが許されるのは次の 2 つだけである。**

1. **§2.1 の調査レポート**（`docs/progress/M25-RESEARCH-01-report.md`）の作成。
2. **`docs/progress/progress-log.md` への索引行の追記**（`CLAUDE.md` §8 の常設要求）。

**禁止**: 上記 2 つ以外の `docs/` 配下のファイル変更 ／ 本体リポのソース・マイグレ・seed・テストの変更（1 バイトも変更しない）／ `docs/design/` 配下の変更 ／ 採番（CHANGE・マイグレ連番）。**採番が必要になったら請求すること。自採番しない。**

> **★`docs/handover/followup-backlog.md` を編集してはならない**（`D-382`）。**本表を書けるのは設計卓だけである。** 登録が要る項目は §5 の「本表への登録候補」節へ並べる。**「登録した」ではなく「登録を依頼する」と書くこと**（playbook §4.20）。

> **★`docs/human-notes/phase4-memo.txt` と `docs/human-notes/combmgr-prerelease-checklist.md` を削除しない。** 両ファイルの冒頭に「phase4 始まったら消す」と書かれているが、**消すのは開発者の手番である**（`D-196` 境界条件 3）。**`phase4-overview` が確定するまでは一次材料である。**

### 0.3 判断・提案を含めない（judgement-free）

**事実列挙のみ。** 「ここを直すべき」「この機能は要らない」等の**設計結論を含めない。**

**★ただし `disposition` の付与は本調査の成果物であり、判断ではなく分類である。** 分類には**必ず根拠を併記する**（§4.C）。**根拠を書けない行は `要判断` に倒す**（§4.C-4）。

次の 2 つは判断ではないため記録すること——**(1) 想定外の発見 ／ (2) 明らかな矛盾の事実指摘。**

### 0.4 本調査の主目的

| 軸 | 主目的 |
|---|---|
| **A** | **母集団を確定する。** 7 つの源泉に散っている項目を 1 つの表へ集める |
| **B** | **名寄せする。** 同じ要求が複数の源泉に現れる分を 1 行へ畳む |
| **C** | **`disposition` を付ける。** フェーズ4 / フェーズ5 / フェーズ6 以降 / 要判断 の 4 値 |
| **D** | **到達条件と突き合わせる。** 公開ゲート（A 9 件 ＋ B 6 件 ＋ α 3 件）に対する充足状況 |
| **E** | **除外を適用する。** 「やらないと決めた」ものを未着手として扱わない |
| **F** | **継承を適用する。** 既に行き先が確定している 4 件を再判定しない |
| **G** | **マイルストーン構成の材料を出す。** フェーズ4 を分割するかの再判定に要る本数 |

**★調査対象外との混同を避けること。** 本調査は**分類**までであり、**マイルストーンへの割付・サブ分割・実装順の決定は含まない**（それらは `phase4-overview` が行う）。

---

## 1. 背景と目的

### 1.1 背景

フェーズ3 は 2026-08-31 に締めた（`D-626` / `D-627`）。**フェーズ4 は「スコープ再編」から始まる。**

**★★現行 `REQ-001` §7 のフェーズ4 定義と、開発者が意図するフェーズ4 は別物である。**

| | 現行 `REQ-001` §7 | 開発者が意図するもの |
|---|---|---|
| 名前 | 外部データ追従・拡張 | **リリースまでにやるべき事** |
| 中身 | `FR702` / `NFR407` / `ISSUE-007` の 3 項目だけ | **本調査が確定させる** |

**⇒ 作業は「現行フェーズ4 に足す」ことではなく、「フェーズ4 を作り直し、フェーズ5・フェーズ6 を新設する」ことである。**

**★★母集団が 7 つの源泉に散っており、しかも一次源の集計値が自分の列挙と食い違っている**（§3.2-1）。**⇒ 母集団の確定を最初の一手にする。**

**★先例＝`M24-RESEARCH-01`。** 同 RESEARCH が二系統 ledger（160 行 ＋ 26 項目）を作ったからこそ、`M24-overview` のサブ分割 16 本が成立した。**overview を書きながら同時に仕分けると、必ずどちらかが粗くなる。**

### 1.2 目的

**§4 の各軸を、実ファイルの `view` / `grep` で確認し、§5 の様式で報告する。**

**成果物は「3 層それぞれの ledger」である**——フェーズ4 ／ フェーズ5 ／ フェーズ6 以降。**各行に `disposition` と根拠を付ける。**

### 1.3 この調査でやらないこと

- **実装・マイグレ・seed・コードの変更を一切しない。**
- **マイルストーンへの割付・サブ分割・実装順を決めない**（`phase4-overview` の仕事）。
- **`REQ-001` §7 を変更しない**（`CHANGE-150` 系で設計卓が行う）。
- **`followup-backlog.md` を編集しない**（`D-382`）。
- **`docs/human-notes/` のファイルを削除しない**（`D-196` 境界条件 3）。

---

## 2. 成果物

### 2.1 作成するファイル

- **`docs/progress/M25-RESEARCH-01-report.md`**（調査報告。§5 の様式）

### 2.2 変更しないもの

全ソース ／ 全マイグレ ／ 全 seed ／ `docs/design/` 配下 ／ `docs/handover/followup-backlog.md` ／ `docs/handover/` と `docs/process/` の全ファイル ／ `docs/human-notes/` の全ファイル ／ テスト（read-only）。

**★例外は `docs/progress/progress-log.md` への索引行の追記のみ**（§0.2）。

---

## 3. 前提条件

### 3.1 必読ドキュメント

| # | パス | 読む範囲 |
|---|---|---|
| 1 | `docs/handover/phase3-to-phase4-handover.md` | **全文**（233 行）。§0 が食い違い、§2 が確定済みの主要判断 12 件、§3 が仕分けの材料 |
| 2 | `docs/human-notes/combmgr-prerelease-checklist.md` | **全文**（183 行）。A 公開ゲート 9 件 ／ B 摩擦低減 6 件 ／ §3 フェーズ4 のスコープ ／ §4 破壊的変更の窓 ／ §5 確定済みの前提 ／ §6 順序の推奨 |
| 3 | `docs/progress/M24-RESEARCH-01-report.md` | **§0.3（数えた範囲）・§2.1（disposition の集計）・§2.2（ledger 本体 160 行）・§3.1（CO ledger 26 項目）・§4.3（`要判断` 36 件）** |
| 4 | `docs/handover/m24-close-report.md` | **§2（未達のまま出荷すると決めたもの 5 件）・§3-1（割付先が消えていた 4 件）** |
| 5 | `docs/handover/followup-backlog.md` | **§AF 〜 §BA**（M24 期 ＋ 改善レーン ＋ 開発者の追加要求）。**★§J 停止時記録も読むこと** |
| 6 | `docs/human-notes/phase4-memo.txt` | **全文**（75 行） |
| 7 | `docs/human-notes/Memo_Someday.txt` | **末尾の未マーカー領域**（§4.B の名寄せに要る） |
| 8 | `docs/human-notes/future-notes/INDEX.md` | **全文 ＋ そこから辿る各メモ** |
| 9 | `docs/process/parallel-board.md` | **§3.4 の `D-610` / `D-626` / `D-627` / `D-634` / `D-635` / `D-637`**（フェーズ4 の前提）・**§4 保留事項**（`P-46` / `P-47` を含む） |
| 10 | `docs/design/requirements.md`（`REQ-001`） | **§7 フェーズ定義**（現状の確認。**変更しない**） |

### 3.2 前提事実（設計担当が確認済み ＝ 調査の出発点）

> **★以下は設計卓が `docs/` 側で確認した事実である。★実装に関する事実は 1 件も含まない**（設計卓は武装により実装ソースを読まない）。**各項目は実ファイルで裏を取ってから使うこと。**

**3.2-1 ★★一次源の集計値が、自分の列挙と食い違っている（最重要）**

`M24-RESEARCH-01` 報告 §2.1 の「`返却` 52 件の返却先内訳」の表について、**設計卓が 2026-09-02 に列挙された ID を数え直したところ、2 行で件数と列挙が食い違っていた。**

| 返却先 | 表が書いている件数 | 設計卓が ID を数えた値 | 差 |
|---|---:|---:|---:|
| **フェーズ4** | **30** | **29** | **−1** |
| **M14 / update wave** | **12** | **14** | **+2** |
| 正式リリース前後 | 5 | 5（**うち `SM-029` は「※フェーズ4 と重複計上せず」の注記つき**） | 0 |
| M23 | 1 | 1 | 0 |
| 利用者フィードバック運用 | 2 | 2 | 0 |
| 方針（future-notes）／将来課題 | 2 | 2 | 0 |
| **合計** | **52** | **53** | **+1** |

**★★件数の合計 52 は `disposition` 集計表の `返却` 52 と一致するが、列挙側の合計は 53 になる。⇒ どちらかが誤っている。**

**★正本は §2.2 の ledger 本体（160 行）である**（集計表ではない）。**⇒ §4.A-1 で ledger 本体から数え直すこと。**

**★★この照合を最初の一手にすること。** 母集団がずれたまま ledger を作ると、**以後どのマイルストーンのクローズでも 1 件足りない状態が続く。**

**3.2-2 到達条件（`D-634` (1)・開発者確定）**

**正式リリースの到達条件 ＝ `checklist` の A 公開ゲート 9 件 ＋ B 摩擦低減 6 件 ＋ α 3 件。**

α の中身は次の 3 つである（開発者回答 2026-09-02）。

| α | 内容 |
|---|---|
| **α①** | **残キャラクターの挿入の完遂**（`M14` レーン。**★独立レーンだが公開前に必ず完遂させる**＝`D-634` (7)） |
| **α②** | **M24 で直しきれなかった「明らかに品質が低い部分」の是正**（源泉 6） |
| **α③** | **公開用の別リポジトリ化の準備**（源泉 2 の「返却（正式リリース前後）」のうち A にも B にも入っていない分。「不要ファイルの除去」も同じ枠） |

**3.2-3 現行 `REQ-001` §7 のフェーズ4 の 3 項目の行き先（`D-634` (2) / `D-635` (B)・開発者確定）**

| 項目 | 行き先 |
|---|---|
| **`FR702`**（ゲームアップデート追従） | **フェーズ4。★他の 2 つと同じ束で落とさないこと。** 理由＝機能ではなく信頼。**追従の仕組みが要求するスキーマは公開前に確定させるのが安い**（`checklist` §3.1） |
| **`ISSUE-007`**（モダン操作対応） | **後付けでよい。★ただし公開前に「クラシック前提」を明示することは `B6` としてフェーズ4 に残す** |
| **`NFR407`**（他ゲーム対応の基盤） | **★意思の撤回。** 開発者の逐語＝「そもそも別ゲー対応はやるつもりがなくなっている」。**⇒ `REQ-001` に「非対応」と明記して残し、フェーズ定義からだけ外す** |

**3.2-4 フェーズの分割は現時点で「しない」（`D-634` (3)・条件つき）**

**フェーズ4 の終わりが初回全体公開である。** **★ただし条件つきである**——開発者の逐語＝「分けなくてマイルストーンの数がどうなるか次第」。**⇒ 本調査が MS 本数の材料を出した時点で、設計卓が 1 度だけ再判定する**（§4.G）。

**3.2-5 `docs/human-notes/phase4-memo.txt` の 3 章の射程（`D-635` (C)・開発者確定）**

| 章 | 射程 |
|---|---|
| **① Phase4 前にやりたい事** | **教訓の件だけが対象。** 他はもう終わった（改善レーンで申し送りになった分が該当） |
| **② Phase 4 でやりたい事** | **全部** |
| **③ その他リリース前に直したいところ** | **ほぼ全部。★ただし切り分けで今後の課題へ戻す可能性がある**（開発者の逐語＝「設計をしっかり考えずに書いたものもあるため」） |

**★同ファイルは一時メモであり、正式な資料へ移植され次第 開発者が消す。★`Memo_Someday.txt` からの抜粋だが AI 整理前のベースであり、現在の同ファイルと一致しているとは限らない。⇒ 名寄せは「逐語一致」を前提にしないこと**（§4.B）。

---

## 4. 調査項目

### A. 母集団の確定

- **A-1**: **★★最初の一手。** `M24-RESEARCH-01` 報告 **§2.2 の ledger 本体（160 行）**から、**`disposition` が `返却` かつ返却先が「フェーズ4」の行を全数抽出**し、**ID を列挙して件数を数える。** **§2.1 の集計表の値（30）と、§2.2 から数えた値のどちらが正しいかを判定し、根拠（走査コマンドと出力）を報告する。** **★同じことを「M14 / update wave」行についても行う**（表は 12、設計卓の数えは 14）。**★食い違いの原因が「重複計上の扱い」（`SM-029` の注記）で説明できるかも判定する。**
- **A-2**: **源泉 2**——同 §2.1 の「返却（正式リリース前後）」5 件を列挙する（コード署名 ／ AV 誤検知対策 ／ 別リポジトリ化 ／ 不要ファイルの除去 ／ 正式名称）。**★`SM-029` を源泉 1 と二重に数えないこと。**
- **A-3**: **源泉 3**——`followup-backlog.md` の **§AF 〜 §BA** の全行を抽出する。**★`§AI` からではない**（`§AF` = `M24-01` 由来 ／ `§AG` = `CHANGE-131` 由来 ／ `§AH` = `M24-09b` 由来 も M24 期である）。**★あわせて §J 停止時記録の未解決行も母集団に入れる。**
- **A-4**: **源泉 4**——`docs/human-notes/future-notes/` を `INDEX.md` から辿り、**構想メモを 1 件ずつ列挙**する。
- **A-5**: **源泉 5**——`combmgr-prerelease-checklist.md` から、**A 9 件 ／ B 6 件 ／ §3.4（フェーズ3 から送られた 3 件）／ §3.5（内定機能 4 件）／ §4 破壊的変更の窓の候補 3 件**を抽出する。
- **A-6**: **源泉 6**——`docs/human-notes/phase4-memo.txt` の全行を抽出する（**UI・データ課題 22 件 ＋ 冒頭「Phase4 前にやりたい事」3 件**）。**★同ファイルの項目は追跡台帳に 1 件も登録されていない。⇒ ledger に載せないとフェーズ4 の終了時点で失われる。**
- **A-7**: **源泉 7**——`m24-close-report.md` **§3-1** の 4 件（`SM-051` / `SM-080` / `SM-098` / `SM-149`）。**★★これらは源泉 1〜6 のどこからも拾えない**——`SM-080` / `SM-098` / `SM-149` は源泉 1 の SM 行に含まれず、followup 上の追跡先は源泉 3 の範囲外にあり、`SM-098` は followup に自分の行すら持たない。**⇒ 本項を立てないと 4 件が母集団に入らないまま終わる。**
- **A-8**: **補**——`docs/human-notes/Memo_Someday.txt` の**末尾の未マーカー領域**を抽出する（§4.B の名寄せに要る）。**★本文の散文中の「M23」は旧番号である**（現行の M23 ではない）。
- **A-9**: **A-1 〜 A-8 の合計を出す。** **★「何を・どの単位で・どの基準時点で数えたか」を必ず併記する**（`E-93` / `E-16`）。**★走査コマンドをそのまま報告へ貼る**（コマンドを併記できない件数は、まだ数えていない件数である＝`E-268`）。

### B. 名寄せ

- **B-1**: **`phase4-memo.txt` の 22 件と `Memo_Someday.txt` 末尾の重複を突き合わせる。** **★設計卓が受け取った申し送りでは「22 件のうち 19 件が逐語で重複」とされているが、`D-635` (C) により `phase4-memo.txt` は AI 整理前の `Memo_Someday` がベースであり、現在の同ファイルと一致しているとは限らない。⇒ 逐語一致を前提にせず、内容で突き合わせること。** **一致 ／ 部分一致 ／ 片方にしかない、の 3 分類で報告する。**
- **B-2**: **源泉 1（SM 行）と源泉 3（followup）の重なりを突き合わせる。** `M24-RESEARCH-01` §3.2 が「系統 1 と系統 2 の重なり」を既に持っているので、**まずそれを引くこと**（作り直さない）。
- **B-3**: **源泉 5（checklist）と源泉 4（future-notes）の重なりを突き合わせる。** **★★ここに解くべき矛盾が 1 件ある**——**開発者は future-notes を「フェーズ6 以降のイメージ」と言っているが、`checklist` §3.5 は同群の一部**〔メモ／一人 SNS・他プレイヤー比較・VOD 分析・HTML エクスポート〕**について「M24 まで埋まったのでフェーズ4 が既定の置き場」と書いている。** とくに **HTML エクスポートは「`M17` への形式追加（段階 A は AI 無関係で軽い）」、比較機能は「R1/R2 の sign-off が済めば実装可能」**としている。**⇒ 事実として両方を並べ、どちらが新しい記述かを日付で判定して報告する。決めるのは設計卓と開発者である。**
- **B-4**: **名寄せ後の母集団の件数を出す。** **★A-9 の合計との差（＝重複で畳んだ数）を明示する。**

### C. `disposition` の付与

- **C-1**: **各行へ次の 4 値のいずれかを付ける。**

  | `disposition` | 意味 |
  |---|---|
  | **`フェーズ4`** | **リリースまでにやるべき事。** 到達条件（3.2-2）の A / B / α のいずれかに紐づく |
  | **`フェーズ5`** | **本当に後回しでよいもの。** 公開後に着手する |
  | **`フェーズ6 以降`** | 構想段階。`future-notes` の多く |
  | **`要判断`** | **上記のどれかを根拠をもって選べないもの** |

- **C-2**: **各行に根拠を必ず併記する。** 根拠の形は「**到達条件のどれに紐づくか**」（`A1`〜`A9` / `B1`〜`B6` / `α①`〜`α③`）**または**「**公開の可否に影響しない理由**」のいずれかである。
- **C-3**: **`フェーズ4` に倒す行は、到達条件のどれに紐づくかを必ず書く。** **★紐づけられない行を `フェーズ4` に倒してはならない**——**それが `checklist` §0 の警告「フェーズ4（機能）を公開の前提にすると公開が遠のく」に当たる形である。**
- **C-4**: **★★`要判断` を 0 件にしない。** **根拠を書けない行は `要判断` に倒すこと。** **⇒ 無理に振り分けると、根拠の無い分類が正本になる**（`M24-RESEARCH-01` §0.3 と同じ規律）。**★`要判断` が 0 件で返ってきた場合、設計卓は「振り分けを急いだ」と読む。**
- **C-5**: **`要判断` の各行について「何が分かれば判定できるか」を 1 行で書く。** 宛先（開発者のドメイン知識 ／ 実測 ／ 先行タスクの完了）も書く。

### D. 到達条件との突合

- **D-1**: **`checklist` の A 9 件 ＋ B 6 件について、現在の状態を 1 件ずつ確認する。** **★`checklist` 本文の「状態」列は 2026-08-19 時点の値であり、既に失効しているものがある**（例＝`A5` の商標クリアランスは 2026-09-02 に完了と確認された＝`D-636`）。**⇒ 状態列を鵜呑みにせず、実ファイル・ボード §3.4・followup で裏を取ること。**
- **D-2**: **★★`A6`（LAN モード API の無認証問題）は「要調査・最優先」と書かれている。** **`M22`（協調基盤＝簡易ログイン・楽観排他・CORS/CSRF）で解消したはずだが as-built の実査が要る。** **⇒ 実装を実査し、`SUPP-001` に残る「LAN モード API 無認証一般の是正は対象外・別途判断」の記述が現在も有効かを判定する。** **★これは本調査で唯一「実装ソースを読む」項目である。** 公開すれば他人が自分の LAN で立てるため、**ここが空いていると公開が脆弱性の配布になる。**
- **D-3**: **α①（残キャラの挿入）の現在地を確認する。** `M14-RESEARCH-03` 指示書（`docs/instructions/M14-RESEARCH-03-seed-wave-work-inventory.md`・**起票済み・未投入**）を読み、**seed 済みキャラ数と残数を実データで数える。** **★数が合っていない**——handover §4-1 の記録では **seed 済み 17 ＋ 開発者の列挙 12 ＝ 29 に対し全 31 で、差 2 が未説明**である（`yasmine` が列挙に無い ＋ 不明 1）。**⇒ 実データで名指しすること。**
- **D-4**: **α③（別リポジトリ化の準備）に紐づく項目を列挙する。** ボード `P-46` / `P-47` と followup `public-repo-operating-model` / `devcontainer-publication-decision` / `public-snapshot-exclusion-rules` / `public-docs-full-disclosure` / `secret-scan-before-release` が既に登録されている。**⇒ 重複行を作らず、既存スラッグを参照すること**（`E-251`）。
- **D-5**: **`checklist` §6「順序の推奨」10 段と、C で付けた `disposition` の整合を確認する。** **★矛盾を検出した場合は、事実として報告する**（順序を決め直さない）。

### E. 除外の適用

- **E-1**: **★★`m24-close-report.md` §2「未達のまま出荷すると決めたもの」5 件を、`disposition` = `判断済み・再開しない` として ledger に載せる。** **未着手として扱わないこと。**

  | # | 何を | 決めた人 |
  |---|---|---|
  | 1 | 英語 UI を未完成のままリリースする（`CO-026`） | 開発者 |
  | 2 | `SM-016`（コンボの 1 工程を指す単位語）の統一を見送る | 開発者 |
  | 3 | **`DES-005` §5.16 の「中断耐性あり」を撤回する** | 設計卓（`D-620`） |
  | 4 | 一括バーに「紐付け解除の導線」を置かない | 開発者 |
  | 5 | `queryKey` の 3 箇所を平坦化しない | 設計卓（`D-620`） |

- **E-2**: **★★とくに #3 は「followup も立てない」と明記された項目であり、源泉 1〜6 のどこにも現れない。** **⇒ `DES-005` を引いたときに撤回済みと気づけないと、再構築の中断再開機構をフェーズ4 の要件として起票してしまう。** **本項が唯一の防波堤である。**
- **E-3**: **`CO-026` と `SM-016` は 2026-09-02 に開発者が改めて確定させた**（`D-634` (9) / (10)）——**`CO-026` はフェーズ4 では復活させない（リリース後に徐々に整理）／`SM-016` は後回し。** **⇒ `disposition` は `フェーズ5` とし、E-1 の「判断済み」と併記する。**

### F. 継承の適用

- **F-1**: **★★`m24-close-report.md` §3-1 の 4 件は、M24 期の設計卓が行き先を確定した。★再判定しないこと。**

  | ID | 内容 | 確定した行き先 |
  |---|---|---|
  | **`SM-051`** | キャラクター増加時の importer 側の見直し | **`M14` レーン**（`M14-RESEARCH-03` の入力） |
  | **`SM-080`** | 仮想コントローラのボタン名見直し | **フェーズ4**（仮想コントローラの面） |
  | **`SM-149`** | 仮想コントローラの移動ボタンと技ボタンが縦並びで直感的でない | **フェーズ4**（`SM-080` と同一クラスタ） |
  | **`CO-020`** | 同部品（`HitBoxLayout.tsx`）の i18n 未対応 | **フェーズ4**（同上） |

- **F-2**: **★★`SM-080` / `SM-149` / `CO-020` は同じ共通部品を触る 3 件であり、1 面としてまとめて扱うほうが安い、というのが確定した判断である。** **⇒ ledger では 3 行を別々に持ちつつ、「同一クラスタ」であることを備考へ書く。** **バラバラのサブにすると同じファイルを 3 回触ることになる。**
- **F-3**: **`SM-098`（確定反撃での D リバの扱い）＝扱うことが確定した**（`D-634` (8)）。**★開発者のドメイン知識**——**フレーム上は全キャラ同じ数値。ただし届く／届かないがある。** **⇒ `disposition` = `フェーズ4`。要件整理が要る旨を備考へ書く。★M18（確定反撃）の指示書・完了報告のいずれにも「リバーサル」の記載がゼロであることを実査で確認すること。**
- **F-4**: **`F12-3` / `E-1`（セットプレイ紐付け 2 系統の UI 統合）は 2026-09-02 に閉じた**（`D-635` (A)）。**⇒ ledger では `判断済み` とし、追跡は `setup-link-by-ka-search`（followup §AS）へ一本化する。★同項はフェーズ4 から後回しでよい**（開発者判断）。**⇒ `disposition` = `フェーズ5`。**

### G. マイルストーン構成の材料

- **G-1**: **`disposition` = `フェーズ4` の全行を、「触る面」で分類する。** 面の例＝法務・配布 ／ セキュリティ ／ データ（seed・スキーマ）／ 画面（一覧・詳細・編集・確定反撃・仮想コントローラ 等）／ リポジトリ運用・ドキュメント。**★1 行が複数の面に跨る場合は全部書く。**
- **G-2**: **面ごとの行数を集計する。** **★これが「フェーズ4 を分割するか」の再判定の材料になる**（3.2-4）。
- **G-3**: **依存関係を事実として列挙する。** 「X を先にやらないと Y が決まらない」という関係のみ。**★順序を決めない。** 例＝`checklist` §4 が「リネーム・スキーマ確定・マイグレーション整理は互いに干渉する」と書いている。
- **G-4**: **★`checklist` §4.1 が求めている測定を実施する**——**`dbtest.Setup` 1 回の所要時間と、テスト全体に占める割合。** **★同節は「マイグレーションを整理すべきかは、初回起動ではなくテスト時間の問題であり、測ってから決める」と明記している。⇒ 実測値を出すこと。判定はしない。**

---

## 5. 報告様式（`docs/progress/M25-RESEARCH-01-report.md`）

### §0 冒頭に必ず置くもの

- **§0.1** baseline commit（`git rev-parse HEAD`）と実施日。
- **§0.2** **走査に使ったコマンドの全文**（コピーして再実行できる形）。**★パイプで切り詰めない**（`E-284`）。
- **§0.3** **本報告が数えた範囲**（`E-93`）——何を・どの単位で・どの基準時点で数えたか。

### §1 母集団（軸 A）

- **§1.1** **★★A-1 の照合結果を最初に置く。** `M24-RESEARCH-01` §2.1 の集計値と §2.2 の実数のどちらが正しいか、根拠つきで。
- **§1.2** 源泉 1〜7 ＋ 補 の内訳（件数と ID の列挙）。
- **§1.3** 合計（A-9）。

### §2 名寄せ（軸 B）

- **§2.1** 重複の一覧（一致 ／ 部分一致 ／ 片方にしかない）。
- **§2.2** 名寄せ後の件数と、畳んだ数。
- **§2.3** **★B-3 の矛盾**（future-notes vs `checklist` §3.5）を事実として並べる。

### §3 3 層 ledger（軸 C・E・F）

**★★本報告の本体である。** 次の列を持つ 1 つの表にする（層ごとに分けず、`disposition` 列で分ける）。

| 列 | 内容 |
|---|---|
| `ID` | 安定 ID（既存の `SM-` / `CO-` / followup スラッグ / `checklist` の `A1`〜`B6` を使う。**★無いものだけ新規に採番する**） |
| `identity` | 本文の先頭 40 字 |
| `locator` | 出所（ファイル:行 または 節） |
| `源泉` | 1〜7 ／ 補 |
| `disposition` | `フェーズ4` / `フェーズ5` / `フェーズ6 以降` / `要判断` / `判断済み・再開しない` |
| `根拠` | **必須。** 到達条件のどれに紐づくか、または公開に影響しない理由 |
| `触る面` | G-1 の分類 |
| `代表 ID` | 名寄せで畳んだ場合の代表行 |
| `備考` | 同一クラスタ・依存・注意点 |

### §4 到達条件との突合（軸 D）

- **§4.1** A 9 件 ＋ B 6 件の現在の状態（**★`checklist` の状態列ではなく実査値**）。
- **§4.2** **★★`A6`（LAN 無認証）の as-built 実査結果。** 本報告で最も重い項目である。
- **§4.3** α① の残キャラ数（**差 2 の説明を含む**）。
- **§4.4** α③ に紐づく既存スラッグの一覧。
- **§4.5** `checklist` §6 の順序推奨との整合（矛盾を検出した場合は事実として記す）。

### §5 マイルストーン構成の材料（軸 G）

- **§5.1** 面ごとの行数。
- **§5.2** 依存関係の列挙。
- **§5.3** `dbtest.Setup` の実測値。

### §6 `要判断` の一覧

**各行に「何が分かれば判定できるか」と宛先を書く。**

### §7 `followup-backlog` 本表への登録候補

**★★製造は本表を編集しない**（`D-382`）。**「登録を依頼する」と書くこと**（playbook §4.20）。**★既存スラッグがあるものは新規に作らず、既存を参照する**（`E-251`）。

### §8 想定外の発見・矛盾の事実指摘

**§0.3 の例外。判断ではなく事実として書く。**

### §9 教訓（`retrospective-log` 行き）

**★本節の宛先は設計担当によるバッチ反映であり、親は読まなくてよい。**

---

## 6. 完了条件（Definition of Done）

1. **§4 の全軸（A〜G）を実値で報告した。**
2. **★A-1 の照合が済み、母集団の件数に根拠がある。**
3. **★`要判断` が 0 件でない**（0 件なら振り分けを急いだ疑いがある＝C-4）。
4. **★`disposition` 未付与が 0 件である。**
5. **★`フェーズ4` に倒した全行に、到達条件（`A1`〜`A9` / `B1`〜`B6` / `α①`〜`α③`）との紐づけが書かれている。**
6. **★`m24-close-report` §2 の 5 件が `判断済み・再開しない` として載っている**（未着手として扱っていない）。
7. **★`m24-close-report` §3-1 の 4 件が継承され、再判定されていない。**
8. **★`D-635` (A) で閉じた `F12-3` / `E-1` が「完了」として扱われず、`setup-link-by-ka-search` へ一本化されている。**
9. **read-only を逸脱していない**（§0.2 の書き込み 2 件を除き、変更ファイル 0）。
10. **`docs/progress/progress-log.md` へ索引行を追記した**（`CLAUDE.md` §8）。
11. **★件数を書いた箇所すべてに走査コマンドが併記されている**（`E-268`）。
12. **★報告書を書いたあとに `bash scripts/check-md-emphasis.sh` を回した**（`E-274`＝報告書を書くと赤に転じることがある）。

---

## 7. 参照ドキュメント

- `docs/handover/phase3-to-phase4-handover.md`（`PHASE3-TO-PHASE4-HANDOVER` v1.1.0）
- `docs/handover/m24-close-report.md`（`M24-CLOSE-REPORT` v1.1.0）§2 / §3-1
- `docs/progress/M24-RESEARCH-01-report.md` §0.3 / §2.1 / §2.2 / §3.1 / §3.2 / §4.3
- `docs/human-notes/combmgr-prerelease-checklist.md`（2026-08-19）
- `docs/human-notes/phase4-memo.txt` ／ `docs/human-notes/Memo_Someday.txt` ／ `docs/human-notes/future-notes/INDEX.md`
- `docs/handover/followup-backlog.md` §AF 〜 §BA ／ §J
- `docs/process/parallel-board.md` §3.4（`D-610` / `D-626` / `D-627` / `D-634` / `D-635` / `D-637`）／ §4（`P-46` / `P-47`）
- `docs/design/requirements.md`（`REQ-001` v2.25.0）§7
- `docs/instructions/M14-RESEARCH-03-seed-wave-work-inventory.md` v1.0.0（**未投入**）
- `docs/handover/retrospective-digest.md`（**指示書執筆時の必読。本調査でも §5 / §7 を参照する**）

---

## 開発者への確認事項

**★★2026-09-02 に 2 件とも承認された（`D-640`）。⇒ 本指示書に開発者判断待ちは 0 件である。以下は記録として残す**（`E-100`＝決定だけでなく検討の経緯を残す）。

1. ~~**`dbtest.Setup` の実測（G-4）を本調査に含めてよいか**~~ **★★【2026-09-02 承認＝含めてよい】**
   何を: `checklist` §4.1 が求める「`dbtest.Setup` 1 回の所要と、テスト全体に占める割合」の測定。
   なぜ: 他の軸が純粋な文書調査であるのに対し、本項だけはテストの実行を伴う。**所要時間が読めない**（M24 期の実測でテスト全体は 100 分規模）。
   **確定した扱い**: **含める。★測定は 1 回だけとし、再現性の確認は求めない。** **★★時間が伸びる場合は本項だけを落として報告し、残りを完成させること**（`要判断` として §6 へ回す）。**⇒ 本項が原因で他の軸が未完のまま終わることを許さない。**

2. ~~**`A6`（LAN 無認証）の実査（D-2）の深さ**~~ **★★【2026-09-02 承認＝暫定案どおり】**
   何を: 「解消したか」をどこまで確かめるか。
   なぜ: **本調査で唯一、実装ソースを読む項目である。** 設計卓は実装を読めないため深さを指定できない。
   **確定した深さ**: **(a) `SUPP-001` の当該記述が現在も有効かの判定 ＋ (b) 認証を要求しない API 経路の全数列挙**まで。**★★侵入テストや脆弱性の実証はしない**（本調査の射程外）。**★(b) は「認証を要求しない経路が 0 件である」で終わらせず、数えた母数と走査コマンドを併記すること**（`E-84`＝「0 件」は「対象が無い」と「検査が対象を見ていない」の 2 通りに読める）。

---

*以上、M25-RESEARCH-01 調査指示書 v1.1.0。配置 `docs/instructions/M25-RESEARCH-01-phase4-scope-ledger.md`。本調査の report が `phase4-overview` のスコープ確定入力となる。*
