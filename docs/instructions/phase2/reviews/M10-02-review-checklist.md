# M10-02 レビューチェックリスト v1.0.0

| 項目 | 内容 |
|------|------|
| 対象指示書 | `docs/instructions/M10-02-instruction.md` v1.0.0（リュウ固定依存 UX の画面横断解消＝A-2。既定キャラの文脈追従。フロント完結） |
| 対象指示書ID | M10-02 |
| レビューモデル | Sonnet 4.6（model-allocation） |
| バージョン | 1.0.1（指示書 v1.0.1 と対。新規登録導線＝一覧のみ・E2E-A'・一覧情報バー defect 修正の確認を反映） |
| 作成者・作成日 | 設計担当 Claude（フェーズ2 本流スパイン・M10 担当）/ 2026-06-18 |
| 関連 CHANGE | CHANGE-039（DES-005 v2.21.0 §4.3/§5.7/§5.8 既定キャラクターの文脈追従。反映済み） |

## 更新履歴

| バージョン | 更新日 | 更新内容 |
|-----------|--------|----------|
| 1.0.0 | 2026-06-18 | 初版（指示書 v1.0.0 と対。Plan Mode survey 確定後） |
| 1.0.1 | 2026-06-18 | 実装整合（指示書 v1.0.1 と対）。新規登録導線＝一覧のみ（マイコンボ導線なし）・E2E-A'（一覧起点）・一覧情報バーのリュウ固定 defect 修正（2-2）の確認を反映 |

---

## 0. レビュー担当への前置き

### 0.1 レビュー前の準備
- 必読: `docs/instructions/M10-02-instruction.md` v1.0.0、`docs/design/05-screen-design.md`（DES-005 **v2.21.0**）§4.3／§5.7「新規モードの既定キャラクター」／§5.8「コンボ追加モーダルの既定キャラクター」、`docs/change-notes/CHANGE-039-M10-02-default-character-context.md`、`docs/handover/code-facts.md`（commit ff0620b）§1/§3/§6。
- **前提ゲート**: (1) M10-01 完了承認済み。(2) **CHANGE-039 反映済み**（DES-005 v2.21.0）。(3) Plan Mode §3.4 の 3 項目（伝播機構／ComboEditor 初期値受け口／ComparePage→モーダル経路）の実 view 最終確認結果が完了報告にあるか。

### 0.2 基本姿勢
- 機械チェック（§1〜§4・§6・§7）+ 設計意図（§5）。
- **フロント完結**。BE・スキーマ非改変を重点確認（§2・§7）。
- code-facts は参考。最終根拠は実コード（retrospective-digest §1 パターンA）。

### 0.3 報告フォーマット
- 各節「OK / 重大（§9）/ 軽微（§10）/ 質問（§11）」。

---

## 1. 設計書本体との照合（最重要）

### 1.1 新規登録の既定キャラの文脈追従（指示書 §4.1、DES-005 §5.7「新規モードの既定キャラクター」）
- [ ] 一覧（ComboListPage）で選択中のキャラから「新規登録」した場合、ComboEditor 新規モードの既定がその文脈キャラになるか。**マイコンボには新規登録導線が無く・追加していない**か（導線はホーム/フッター/一覧の3箇所）。
- [ ] ホーム／フッターの汎用「新規登録」導線など文脈の無い遷移では `INITIAL_CHARACTER_ID` 既定（M10-01 挙動）か。
- [ ] `ComboEditor` に `initialCharacterId?` を追加し、不在時は `INITIAL_CHARACTER_ID`、編集モードでは無視（`initial.characterId` 固定）になっているか。
- [ ] （2-2 追認）一覧（ComboListPage）の「現在選択中キャラクター情報バー」がリュウ固定（`comboList.characterRyu`）でなく `filters.characterId` に対応する名前を動的表示（`useCharacterName`、アバター＝名前先頭1文字）になっているか。DES-005 §5.4「動的表示」に整合。

### 1.2 コンボ追加モーダルの既定キャラ（指示書 §4.2、DES-005 §5.8）
- [ ] `ComparePage` が比較リスト先頭コンボの characterId（空なら `INITIAL_CHARACTER_ID`）を `AddComboToCompareModal` の `defaultCharacterId` として渡すか。
- [ ] モーダルの既定キャラがリュウ固定でなく `defaultCharacterId` 駆動になり、内部 `CharacterSelector` の手動切替が維持されているか。

### 1.3 伝播機構（指示書 §4.3）
- [ ] 文脈キャラ伝播が `?character=`（または既存実装に合わせた nav state）で行われ、ComboEditor／モーダルは「初期値を外から受ける」形にとどまり内部選択ロジックが不変か。

---

## 2. API・スキーマ整合性
- [ ] 登録系 API（`POST`/`PUT`/`check-duplicate`）の既存契約が不変か。**バックエンド・スキーマに変更を加えていないか**（フロント完結）。

---

## 3. フロントエンドの動作仕様
- [ ] 文脈ありの新規登録／コンボ追加モーダルで既定が文脈キャラ、文脈なしで `INITIAL_CHARACTER_ID`。
- [ ] `initialCharacterId`／`defaultCharacterId` 不在時に従来挙動（INITIAL）になる後方互換。
- [ ] M10-01 の ComboEditor 内部挙動（選択化・CHANGE-036 切替）・モーダル内手動セレクタが不変か。

---

## 4. テストの妥当性（ケース数で確認）
- [ ] component/unit: (1) 新規登録の文脈追従、(2) 文脈なしで INITIAL、(3) モーダル既定が先頭コンボのキャラ、(4) 比較リスト空で INITIAL、(5) プロップ不在時の後方互換、の5ケース。
- [ ] E2E: A'（一覧起点）一覧でダルシム→新規登録既定がダルシム／B フッター新規登録→INITIAL／C 比較でダルシム→追加モーダル既定がダルシム／D 既存 spec 非回帰。（当初 A のマイコンボ起点は導線が無く非該当）

---

## 5. 設計意図との整合
- [ ] **フロント完結**（API・スキーマ非改変）。
- [ ] **初期値を外から渡すのみ**（M10-01 内部ロジック・モーダル手動セレクタ不変＝後方互換）。
- [ ] **文脈なしは INITIAL_CHARACTER_ID にフォールバック**（DES-005 §4.3/§5.7/§5.8）。
- [ ] スコープは新規登録既定＋コンボ追加モーダル既定の2点。**選択モード（useSelectMode）には手を入れていない**（スコープ外）。
- [ ] situation のキャラ固有状態を機能化していない（M11、指示書 §4.11）。

---

## 6. コード品質・規約
- [ ] queryKey 規約（code-facts §2、`["characters",{gameId}]` 等）に整合。既定キャラの取得・伝播が既存パターンに沿うか。
- [ ] `?character=` 等のパラメータ命名・型が既存ルーティング規約に整合。JSON/型 camelCase。

---

## 7. 既存挙動の温存（非破壊性）
- [ ] 編集モードのキャラ固定表示が不変。
- [ ] 閲覧系の `useCharacters`/`CharacterSelector` 既存呼び出し元が不変。
- [ ] 選択モード（`useSelectMode`）が不変。
- [ ] 登録系 API の既存契約・スキーマが不変。
- [ ] 既存 E2E（M10-01／閲覧系キャラ動的化／combo-crud）が通過。

---

## 8. ドキュメント
- [ ] DES-005 §4.3/§5.7/§5.8 の CHANGE-039 追記は**設計担当が対応済み**（DES-005 v2.21.0）。製造担当が DES を直接編集していないか。
- [ ] 完了報告に Plan Mode 最終 view 確認（§3.4 3項目）・推測内容・テストケース数を含むか。

---

## 9. 重大な問題の判定基準（完了承認を妨げる）
- 前提ゲート（CHANGE-039 反映 / Plan Mode 3項目）未充足のまま実装。
- 新規登録・コンボ追加モーダルの既定がリュウ固定のまま（文脈追従が機能しない）。
- 文脈なしの遷移で既定が `INITIAL_CHARACTER_ID` にフォールバックしない（または不在プロップで従来挙動が壊れる）。
- バックエンド・スキーマに変更を加えている（フロント完結のはず）。
- M10-01 の ComboEditor 内部挙動・編集モードのキャラ固定・閲覧系に回帰がある。
- 選択モードや situation（custom_states）に手を入れている（スコープ外）。

## 10. 軽微な問題の判定基準（持ち越し許容）
- 伝播パラメータ名・既定キャラ取得の細部。モーダル既定の表示細部。

## 11. 質問・確認事項のフォーマット
- 「指示書 §X.X / DES-005 §4.3（§5.7／§5.8）に対し実装が Z。意図確認したい」の形で根拠節併記。

## 12. レビュー完了の判定
- §1〜§8 が全て OK、§9 重大ゼロ、§0.1 の前提ゲート + Plan Mode 3項目が揃っている。§10 軽微は持ち越し可。

---

*以上、M10-02 レビューチェックリスト v1.0.0*
