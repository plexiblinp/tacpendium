# 指示書 M27-02b: 入力仕様の 3 状態（必須・任意・未検証）

| 項目 | 内容 |
|------|------|
| 文書ID | M27-02b |
| バージョン | **1.2.0**（2026-09-03。**★★投入前の更新＝`D-696`**。**§4 を全面更新**〔`M14-03f` と並列で走らせる ／ **★今回はマイグレが実際の交点である** ／ マージ順の条件〕**／ §0.6 を新設**〔段 1 と段 2 のゲート〕**／ 上位・参照の版を追随**） ／ **1.1.0**（2026-09-02。**★★射程を縮めた＝`D-686`。`SD-019` / `SD-022` は `M30`（仮想コントローラ）の識別子と同一であることが ledger の `代表 ID` 欄で確定したため移した**） ／ 1.0.0（2026-09-02・起票） |
| 作成日 | 2026-09-02 |
| 作成者 | 設計担当 Claude（設計卓／フェーズ4 期） |
| 対象マイルストーン | **`M27`**（`α②` 画面品質）。上位＝`docs/instructions/M27-overview.md` v1.6.0 |
| 推奨モデル | **高精度実装**（**★スキーマ変更の可能性 ＋ 既存データの移行**。実モデル名は `docs/human-notes/model-allocation.md` が正本） |
| Plan Mode | **必須**（`CLAUDE.md` §9＝スキーマ・データ移行を伴う変更） |
| レビュー | **必要**（**★チェックリストは段 1 の承認後に設計卓が起こす**——**射程が段 1 の結論で決まるため、先に書くと外れる**＝`D-691`） |
| CHANGE 消費 | **1〜2 本の見込み**（**★段 1 の承認後に設計卓が起票する。★製造は自採番しない**） |
| マイグレ消費 | **0〜1 本**（**★起き攻めの 3 状態化が DB に及ぶ場合のみ。★設計卓へ請求する**＝`D-293`） |
| 配置 | `docs/instructions/M27-02b-required-fields-and-strength-display.md` |
| 出力 | **`docs/progress/M27-02b-completion-report.md`**（**★必須**） |

---

## 0. この指示書の特殊性

### 0.1 ★★最初の一手は「開発者へ問うて止まる」である

**本サブの中心は「どの欄を必須にするか」であり、これは開発者しか決められない。**

| # | identity | 内容 |
|---|---|---|
| 1 | `P4M-009` | **ダメージ等の必須入力化** |
| 2 | `P4M-010` | **必須・任意ラベル** |
| 3 | `P4M-011` | **起き攻めの「未検証」と「false」の区別** |
| 4 | `P4M-024` | **基本情報が始動状態であることを表示** |
| 5 | `SD-020` | **OD 強度組合せを既定非表示**（**★下記 §0.3 の注意あり**） |

**★★1 / 2 / 3 は同じ族である**——**「必須／任意／未検証」の 3 状態をどう見せ、どう持つか。⇒ 1 つの規則を 3 か所へ通す**（`M27-overview` §3 M27-02）。

**★★開発者判断を設計卓や製造が代われない**（`roles-and-routing` ハード列＝**正解がなく好みが決め手 ／ 開発者のドメイン知識が要る**）。**逐語そのものが「リストアップしたい」であり、リストはまだ無い。**

> **`P4M-009` の逐語**（`phase4-memo.txt:33`）
> **「ダメージは必須入力にしたい。他にもこれはやっぱり入力ないとダメだなというメタデータはリストアップしたい。」**

### 0.2 ★★Plan Mode でやること

| 手番 | やること |
|---|---|
| **(1)** | **現状を数える**（§2.1.1）——**編集画面の全欄と、いま必須／任意のどちらか、DB の NOT NULL 状態、実データの未入力率** |
| **(2)** | **★★「必須にする候補」を、根拠付きで並べる**——**★設計卓が選ぶのではなく、開発者が選べる形にする。⇒ 欄ごとに「必須にすると既存の何件が保存できなくなるか」を添えること** |
| **★★(3)** | **★開発者へ提示して止まる。★答えを待たずに実装へ進まない** |
| (4) | **承認後、実装する** |

**★★(2) の「既存の何件が引っかかるか」が本サブの値打ちである。** **必須化は「決めれば済む」に見えて、既存データが通らなくなる。⇒ 数を出さずに問うと、開発者は答えられない。★これは `M28-overview` §3.2 で「持ち場を決めよ」の前に「何が届くのか」を問い損ねたのと同じ形である。**

### 0.3 ★★`SD-020` は `M30` へ移る可能性がある

**逐語＝「登録編集系画面、OD(弱中)、OD(中強)、OD(弱強)の概念は全キャラある訳ではないので、デフォルト非表示にしたい。簡単に出せるようにはしたい。」**

**★逐語は「登録編集系画面」と言っており、本サブに置いた。★ただし主題は「技選択がどの強度を出すか」であり、`M30`（仮想コントローラ）の `SD-019` / `SD-021` / `SD-022` と同じ族である見込みがある。**

**★★実査で「OD 強度組合せを描いているのが仮想コントローラの部品である」と判明したら、直さずに設計卓へ知らせること。⇒ `M30` へ移す。**

**★★ledger の `代表 ID` 欄は `SD-020` に相手を与えていない**（`—`）。**⇒ 同一識別子の証拠は無い。だから移していない。★「同じ族に見える」は移す根拠にならない**（`D-686` の教訓）。

### 0.4 ★★`SD-019` / `SD-022` は本サブから外れた（`D-686`）

**★★v1.0.0 は「強度の見せ方」として `SD-019`（強度なし技の選択）と `SD-022`（ホールド／ジャスト版の表示）を持っていた。⇒ 2026-09-02 に `M30` へ移した。**

**理由＝ledger の `代表 ID` 欄で、両者が `M30` の識別子と同一であることが確定した。**

| 本サブが持っていた行 | 代表 ID | 代表の「触る面」 |
|---|---|---|
| `SD-019` 強度なし技の選択 | **`P4M-016`** | **画面: 仮想コントローラ** |
| `SD-022` ホールド／ジャスト版の表示 | **`P4M-018`** | **画面: 仮想コントローラ** |

**⇒ 同じ 1 件が `M27` と `M30` に分かれて入っていた。★片方だけ直すと、もう片方のマイルストーンが同じものを直しに来る。**

**★v1.0.0 が「UI ではなくデータの問題かもしれない」「`SD-021` と同じ基準に効く可能性がある」と書いていた見立ては当たっていた。★ただし根拠は推測であり、ledger の列に答えが在った。**

### 0.5 ★★完了報告を必ず作る

**出力＝`docs/progress/M27-02b-completion-report.md`。★ファイル名は必ずこれ**（理由＝`check-progress-log-index.sh` は `*completion-report*.md` しか走査せず、別名だと索引行が落ちても永久に緑になる）。

**★段 1 の成果物を別ファイル**（`docs/progress/M27-02b-required-fields-proposal.md` 等）**に置いてもよいが、完了報告は別に必ず作ること。**

### 0.6 ★★段 1 と段 2 のゲート（**2026-09-03 追加＝`D-696`**）

**★★本サブは `M14-03f`（第四波 seed）と並列で走る。⇒ 段の切れ目でゲートが要る。**

| 段 | 内容 | 並列の可否 | マイグレ |
|---|---|---|---|
| **段 1** | **現状を数える → 必須化候補を根拠付きで並べる → 開発者へ提示して止まる** | **★★並列可。実装差分 0 であり `migrations/` に触らない** | **0 本** |
| **段 2** | **実装**（ラベル ／ 起き攻めの 3 状態化 ／ 始動状態表示 ／ OD 既定非表示） | **★★段 2 へ入る前に設計卓へ知らせること** | **0〜1 本**（**★`P4M-011` の 3 状態化が DB に及ぶ場合のみ**） |

**★★段 2 の前に知らせる理由**——**マイグレの要否が段 1 の結論で決まるためである。** **`M14-03f` が `migrations/` を連番で消費中であり上限を切っていない**（`D-689`）。**⇒ 要否が分かった時点で設計卓が連番を払い出す。★`ls migrations/` を見て自分で決めないこと**——**別レーンが消費した番号は本ツリーに現れない**（`M27-02a` の実測。**作業ツリーの最大は `000081` のままだが、正本は `000092` である**）。

**★★段 2 のうちマイグレを要するのは `P4M-011`（起き攻めの 3 状態化）だけである。** **他の 4 件**〔`P4M-009` 必須化 ／ `P4M-010` ラベル ／ `P4M-024` 始動状態表示 ／ `SD-020` OD 既定非表示〕**は UI であり `migrations/` に触らない。⇒ 知らせた後、マイグレ待ちの間に他 4 件を進めてよい。**

---

## 1. 前提として読むもの

| 対象 | 何を読むか |
|---|---|
| `docs/instructions/M27-overview.md` v1.6.0 | **★§3 M27-02 ／ §2.3（`代表 ID` による境界の是正） ／ §6 実査項目 6** |
| `docs/instructions/M27-02a-reachability-save-path-and-selectors.md` | **★★同じ画面を触る。⇒ やらないことの境界を確かめる** |
| `docs/handover/code-facts.md` | **★引く。想定で書かない**（**2026-09-01 生成・鮮度は最新**） |
| `docs/design/06-validation.md` | **★必須／任意の現行定義。⇒ 本サブの主戦場** |
| `docs/design/05-screen-design.md` **2.91.0** | 編集画面の欄構成・ラベル。**★★`CHANGE-152`（`M27-02a`）で §5.7 が動いた**——**タブ見出しのエラーバッジの振り分け表 ／ タグ欄のキーボード操作 ／ 相手の大きさの info-mark**（`CHANGE-151`）。**⇒ 本サブの「必須・任意ラベル」はこの上に載る。★先に読むこと** |
| `docs/design/02-architecture.md` **1.74.0** | **★§4.2 の `validation_failed` の 3 系統**（`CHANGE-152`）。**★必須化は検証エラーを増やす。⇒ どの系統で返るかを知らずに設計しない** |
| `docs/design/06-validation.md` **1.40.0** | **★§3 冒頭＝`setups[*]` は UI から到達できる唯一の系統である**（`CHANGE-152`）。**★★「到達できる検証エラーが 1 件も無い」はもう成り立たない。⇒ 必須化で増える検証エラーは画面に出る** |
| `docs/design/03-data-model.md` | **★NOT NULL / nullable の現状**（**起き攻めの 3 状態化に効く**） |
| `docs/handover/followup-backlog.md` | `editor-optional-fields-input-effort`（**★解消済み。⇒ 「欄が多い」は `M24-12` で別の形で手当てされている**） |
| `docs/human-notes/phase4-memo.txt` `:33-35` `:73` | **`P4M-009` / `P4M-010` / `P4M-011` / `P4M-024` の逐語** |

---

## 2. やること

### 2.1 段 1 — 必須にする欄を決めるための材料を出す

#### 2.1.1 現状を数える

| # | 数えること |
|---|---|
| **★★1** | **編集画面の全欄の一覧と、それぞれが必須か任意か。★母数を出す** |
| **★★2** | **各欄の DB 側の状態**（NOT NULL か nullable か、既定値が在るか）。**★画面の「必須」と DB の NOT NULL は一致しているとは限らない。⇒ ずれている欄を名指しする** |
| **★★3** | **★既存データの未入力率**——**欄ごとに「いま空である件数 ／ 全件数」。⇒ これが (2) の判断材料である** |
| **4** | **必須／任意がどこで宣言されているか**（zod か ／ Go 側か ／ 両方か）。**★両方なら、どちらが正であるかを報告に書く** |

#### 2.1.2 必須化の候補を、根拠付きで並べる（`P4M-009`）

**★★欄ごとに次を添えること。★添えずに一覧だけ出さない。**

| # | 添えるもの |
|---|---|
| **★★1** | **必須にすると保存できなくなる既存データの件数**（§2.1.1 の 3） |
| **★★2** | **★その欄が「本来いつも埋まるもの」か「埋まらないことに意味があるもの」か。⇒ 後者は必須化してはいけない**〔例＝起き攻めは「検証していない」に意味が在る＝`P4M-011`〕 |
| **3** | **必須化した場合、CSV 取込・インポートが通らなくなる経路が在るか** |
| **★4** | **★設計卓の推奨は書いてよい。ただし「開発者が選ぶ」形を崩さないこと** |

#### 2.1.3 ★★開発者へ提示して止まる

**★成果物を提示し、承認を待つ。★承認前に段 2 へ進まない。**

---

### 2.2 段 2 — 必須・任意ラベル（`P4M-010`）

**逐語＝「何が必須で、何が任意か区別できるようなラベル表示」**

| # | やること |
|---|---|
| **★★1** | **★1 つの規則を全欄へ通すこと。⇒ 欄ごとに違う見せ方をしない**（`M27-overview` §3 M27-02＝「1 つの規則を 3 か所へ通す」） |
| **★★2** | **★「未検証」を含む 3 状態が表現できる規則にすること**（`P4M-011` と同じ規則で扱う） |
| **3** | **★ラベルの文言は定数化する**（`CLAUDE.md` §4 マジックストリング）。**i18n の対象になるかを報告に書く** |
| **4** | **`M24-12` で入った入力補助**（ボタン群・数字キー）**と衝突しないこと** |

### 2.3 段 2 — 起き攻めの「未入力（未検証）」と「検証済み false」（`P4M-011`）

**逐語＝「起き攻め状況について、必須項目とするか、未入力（未検証）か検証済みでfalseかを扱いたい」**

**★★これは表示の話ではなくデータの話である。⇒ 2 状態を 3 状態にする。**

| # | やること |
|---|---|
| **★★1** | **現在の持ち方を確かめる**〔boolean か ／ nullable boolean か ／ 別の型か〕。**★既に nullable なら「表現できるのに使っていない」だけかもしれない。⇒ その場合スキーマ変更は要らない** |
| **★★2** | **3 状態の持ち方を提案する。★選択肢と、それぞれの移行コストを並べること**〔nullable boolean ／ 3 値の列挙 ／ 別列で「検証済みか」を持つ〕 |
| **★★3** | **★既存データはどの状態になるか。⇒ 「いま false のものは、未検証なのか検証済み false なのか区別できない」——★この不可逆性を報告に明記すること。開発者が知らずに移行すると情報が失われる** |
| **★★4** | **スキーマを変えるなら開発者の個別承認が要る**（`CLAUDE.md` §10）。**マイグレは設計卓へ請求する** |
| **5** | **★起き攻めの欄は 12 個ある**（`M24-12` の実測）。**⇒ 12 個すべてが同じ扱いかを確かめる** |

### 2.4 段 2 — 基本情報が始動状態であることを表示（`P4M-024`）

**逐語＝「基本情報が始動時の状態を表す事を画面に表示」**

| # | やること |
|---|---|
| **★1** | **★表示だけである。仕様は動かさない** |
| **2** | **★どこに出すか**〔節見出し ／ 補助テキスト ／ `InfoMark`〕**を決めて提案する。`InfoMark` は既存部品である**（`code-facts`） |
| **3** | **★「始動時の状態」という語が、既に画面のどこかで別の言い回しになっていないかを確かめる。⇒ 語を揃える**（**`M29` が用語統一を持つ。★本サブで語を作らない**） |

### 2.5 段 2 — OD 強度組合せを既定非表示（`SD-020`）

**★★着手前に §0.3 を読むこと。⇒ 仮想コントローラの部品が描いていると判明したら、直さずに設計卓へ知らせる。**

| # | やること |
|---|---|
| **★★1** | **★「全キャラある訳ではない」——★キャラごとに在る／無いをデータから判定できるなら、既定非表示ではなく「在るキャラだけ出す」が正しい。⇒ どちらが可能かを確かめてから提案すること** |
| **★★2** | **判定できないなら既定非表示 ＋ 展開。★「簡単に出せる」の逐語を満たすこと**（**`CollapsibleFieldset` は既存部品である**＝`code-facts`） |
| **3** | **★既存データで OD 強度組合せを使っているコンボが在るなら、そのコンボの編集では既定で開くこと。★畳んだせいで入っている値が見えなくなるのは後退である** |

---

## 3. やらないこと

| # | やらないこと | なぜ |
|---|---|---|
| **★★1** | **`M27-02a` の 6 件**〔保存の動線 3 件 ／ 候補選択 3 件〕 | **★別サブ。⇒ 同じ画面だが触る箇所が違う** |
| **★★2** | **★「必須にする欄」を設計卓や製造が決めること** | **★★開発者の判断である**（§0.1） |
| **★★3** | **`SD-019` 強度なし技の選択 ／ `SD-022` ホールド／ジャスト版の表示 ／ `SD-021` 仮想コントローラの技表示基準** | **★★`M30` の射程である**（§0.4＝`D-686`）。**⇒ 気づきは報告に書き、直さない** |
| **★★4** | **`M14` の seed データの是正** | **★`M14-03f` が `character_data/` ／ `internal/seedgen/` ／ `migrations/` で走っている**（`D-678`） |
| **★5** | **エディタの全面テコ入れ**（`P-44`） | **★保留中** |
| **★6** | **用語・ラベルの統一**（`M29`） | **★語を作らない。既存の語に合わせる** |
| **★7** | **`docs/design/` の直接編集** | **★CHANGE たたき台を報告 §で出す**（`CLAUDE.md` §8） |

---

## 4. 順序と衝突（**★★2026-09-03 全面更新＝`D-696`**）

### 4.1 現在の並び

```
M27-01（ヒット種別・相手サイズ）… ★完了・受理（D-687）
M27-02a（到達性）…               ★完了・受理（D-694）
     ↓
M14-03f（第四波 seed）… ★走行中          ┐
M27-02b（本書）… ★並列で投入してよい      ┘ 段 1 は面が交わらない
     ↓
M28-01（正式名リネーム）… ★単独で走らせる
     ↓
M27-03（一覧・詳細）… 未起票
```

### 4.2 ★★`M14-03f` と並列でよい理由（**ただし段 1 に限る**）

| レーン | 触る面 |
|---|---|
| **`M14-03f`** | `migrations/` ／ `character_data/` ／ `internal/seedgen/` |
| **`M27-02b` 段 1** | **★無し**（実装差分 0） |
| **`M27-02b` 段 2** | `web/src/` ／ `internal/`（`seedgen` を除く） ／ **★`migrations/` に触る可能性がある** |

**⇒ 段 1 は完全に交わらない。段 2 は `migrations/` で交わりうる。★だから §0.6 のゲートを置いた。**

**★`internal/seedgen/` には 1 行も触らないこと**（`internal/api` / `internal/service` は射程内）。

### 4.3 ★★今回はマイグレが実際の交点である（**`M27-02a` と違う点**）

**`M27-02a` はマイグレ 0 本の見込みで、実際に 0 本だった。⇒ 交点が発生しなかった。**

**★★本サブは `P4M-011` の 3 状態化が DB に及ぶ可能性があり、そのときは交点が実在する。**

| # | 守ること |
|---|---|
| **★★1** | **連番は設計卓へ請求する。自採番しない**（`D-293`）。**★段 2 の前に要否を知らせる**（§0.6） |
| **★★2** | **★★マージ順を守ること＝`M14-03f` が先である。** **`M14-03f` は `000082` から連番で消費中であり、本サブがその上の番号を取る。⇒ 本サブが先に `main` へ入ると、`main` の連番に穴が開く** |
| **★★3** | **★穴が開くと何が起きるか＝`golang-migrate` は適用済みの最大版数を記録する。⇒ 後から低い版数のファイルが現れても適用されない。★配布先の DB にだけ欠落が残り、エラーにならない**（ボード §2.2 が最重要リスクとして記録している事象） |
| **4** | **★マージ順を製造が決めるのではない。⇒ 完了報告に「本サブはマイグレを N 本消費した。`M14-03f` の後にマージすること」を明記し、開発者へ渡す** |

### 4.4 ★着手時に確かめること

| # | 確かめること |
|---|---|
| **★★1** | **`M27-01` と `M27-02a` が `main` へ入っているか。★入っていないなら設計卓へ知らせる** |
| **★★2** | **`M14-03f` が走行中であること。⇒ `migrations/` ／ `character_data/` ／ `internal/seedgen/` に触らない**（段 1 では触る用事が無い） |
| **3** | **`M28-01` は未投入である。⇒ モジュールパスは旧のままである** |

---

## 5. 完了報告

**出力＝`docs/progress/M27-02b-completion-report.md`。**

| § | 中身 |
|---|---|
| **1** | **5 件それぞれの「いまも再現するか」の判定**（`M27-overview` §6 実査項目 6） |
| **2** | **★★段 1 の必須化候補の一覧と、欄ごとの「引っかかる既存データの件数」**（§2.1.2）。**★開発者の選択の結果も書く** |
| **3** | **★起き攻めの 3 状態化の方式と、既存データがどう移行されるか**（§2.3 の 3 の不可逆性を含む） |
| **4** | **★`SD-020` が仮想コントローラ由来だったか**（§0.3）。**★そうなら、直さずに止めた事実** |
| **5** | **CHANGE たたき台**（`DES-006` ／ `DES-005` ／ `DES-003`） |
| **6** | **残課題**。**★§J へ直接書けるのは製造だけ**（`D-382`）。**他の表への登録候補は本報告 §へ書き、設計卓が畳む** |
| **7** | **★教訓**（出たもの。無ければ「無し」と書く） |
| **★★8** | **★マイグレを消費したなら本数と、「`M14-03f` の後にマージすること」を明記する**（§4.3-4）。**★消費 0 本なら「0 本」と書く。★空欄にしない** |

**★`docs/progress/progress-log.md` への索引行の追記は必須**（`CLAUDE.md` §8）。

---

## 6. ★設計卓が確かめていないこと

| # | 確かめていないこと | 影響 |
|---|---|---|
| **★★1** | **起き攻めの現在の持ち方が boolean か nullable か** | **★★スキーマ変更が要るかどうかが変わる。⇒ §2.3 の 1 が最初の一手である** |
| **★★2** | **`SD-020` の OD 強度組合せを、どの部品が描いているか** | **★★射程が `M30` へ移るかどうかが変わる**（§0.3） |
| **★★2b** | **`M27-02a` が `DES-005` §5.7 へ足したタブ振り分けとタグ欄の記述が、本サブの「必須・任意ラベル」と衝突しないか** | **★同じ節に載る。⇒ 1 つの規則を全欄へ通すとき、既存の記述と読み合わせること**（`CHANGE-152`） |
| **★★3** | **編集画面の欄の全数と、いまの必須／任意の内訳** | **★段 1 の母数そのもの** |
| **4** | **5 件のうち何件が既に解消しているか** | **★射程が縮む可能性。⇒ 縮んだら報告に書き、余った手番を勝手に他へ使わない** |

---

## 7. 参照

| ID | パス |
|---|---|
| 上位 | `docs/instructions/M27-overview.md` §3 M27-02 ／ §2.3 |
| 対 | `docs/instructions/M27-02a-reachability-save-path-and-selectors.md` |
| フェーズ | `docs/instructions/phase4-overview.md` §8.5 |
| ledger | `docs/progress/M25-RESEARCH-01-report.md` §3（**★`代表 ID` 欄が識別子の正本である**） |
| 源泉 | `docs/human-notes/phase4-memo.txt` `:33-35` `:73` ／ `docs/human-notes/Memo_Someday.txt` `:409` |
| 機械的事実 | `docs/handover/code-facts.md`（**2026-09-01 生成・最新**） |

---

*以上*
