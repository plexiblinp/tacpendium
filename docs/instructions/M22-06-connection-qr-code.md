# 指示書 M22-06: 接続用 QR コードの検証と穴埋め（`FR407`）

| 項目 | 内容 |
|------|------|
| 文書ID | M22-06 |
| バージョン | 2.7.0 |
| 推奨モデル | **高速実装**（**★本サブは作るサブではなく、既にあるものが要件を満たしているかを確かめるサブである**。判断軸は playbook §7.1、実モデル名は `docs/human-notes/model-allocation.md` が正本） |
| Plan Mode | **必須**（**§3.3 の実査 8 項目。とくに「どこが既に満たされていて、どこが穴か」を先に確定させる**） |
| 機械レビュー | 必須（別チェックリスト: `M22-06-review-checklist.md`） |
| 参照してよい先行資料 | **`M22-RESEARCH-01` 調査レポート §6（軸 F）・§8-1** ／ **`SUPP-001` §2.6.2・§5.6**（**★他サブの指示書・チェックリストは含めない**＝**D-217**）。**設計書は §3.1 で名指ししたもの** |
| 並列性 | **★worktree 並列可**（`m22-contract` §3.3）。**`M21-07` が `web/src/features/physical-input/` を触るが、本サブは触らない** |
| CHANGE | **`CHANGE-117`**（**★2026-08-17 承認済み**＝**D-428**。**⇒ 着手前ゲートは通過している**。**★確認事項 0 件**＝**D-427**） |
| 依存 | **なし**（**★`M22-RESEARCH-01` は完了済み。本サブはその実測を出発点とする**）。**★M22 の直列本線（`M22-01`〜`M22-05`）には依存しない** |
| 想定所要時間 | 60〜120 分（**★実装済みのため、v1.0.0 の見積り 120〜180 分から縮小した**） |
| 作成者・作成日 | 設計担当 Claude（M22 期・設計卓）/ 2026-08-15 |

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| **2.7.0** | 2026-08-18 | **★§3.3 の項目数の参照を「6 項目」→「8 項目」へ是正**（**D-433**。`M22-06` 設計伝達レポート §4-7）。**⇒ レビュー側が「6 項目」で足切りすると §3.3-7〔`lan` モードで起動できるか〕と §3.3-8〔`make e2e` の前提〕が検査されないまま通る。** **★節の項目数を本文へ書き写している箇所は、増補のたびに全数を拾うこと**（`E-118` と同型）。 |
| **2.6.0** | 2026-08-17 | **§4.4.1 を新設＝過去の手動確認を援用してよい条件を 3 つに固定した**（**D-429**）。**⇒ 完了報告 4 へ「実施したのか援用したのか」の明記を要求。** **★`M22-05` の確認は「着いた後」の検証であり、`M22-06` の「着くまで」とは別命題である。** |
| **2.5.0** | 2026-08-17 | **`CHANGE-117` が承認された**（**D-428**）。**⇒ メタ表の CHANGE 欄を承認済みへ。着手前ゲートは通過している。** |
| **2.4.0** | 2026-08-17 | **§4.3 の穴候補 3 を撤回した**（**D-427**）。**★開発者の回答「設定画面の QR コードの下に URL を出す、という意味なら既に出している」**——**穴ではなかった。** **⇒ 「作るか」ではなく「as-built を報告する」へ変わった**〔どこに出ているか ／ コピーできるか〕。**★穴候補 2〔接続可能IP/ポート表示〕と同じものである可能性があり、別物か同じかを確定させる。** **★`CHANGE-117` の確認事項は 0 件になった。** |\n| **2.3.0** | 2026-08-17 | **`CHANGE-117` を起票した**（**D-426**）。**⇒ メタ表へ CHANGE 欄を追加し、§6.5 を「通知書を持って着手する」形へ改めた。** **★あわせて §4.3 の穴候補 4 を撤回した**——**「`FR407` の状態記述が失効している」は設計卓の誤りである。** **`REQ-001` の要件表の列は `優先度 / フェーズ` であり実装状態を表さない**〔`FR501` は `M22-01` で実装済みだがいまも「重要 / 3」〕。**⇒ 製造にこの仮説を追わせない。** |
| **2.2.0** | 2026-08-17 | **`M22-05` の as-built へ追随した**（**D-425**）。**★§3.3 へ実査 1 項目を追加し 8 項目へ**——**クラウド実行環境では `lan` モードの起動自体ができない**〔非ループバックが `192.0.2.2`＝TEST-NET-1 の 1 本のみで、私用アドレス判定が**正しく**弾く〕。**⇒ QR は代表 LAN IP を埋め込む機能であり、中身を機械で実測できない。** **★「動かないので QR が壊れている」と結論させないための実査項目である。** |
| **2.1.0** | 2026-08-16 | **先行サブの as-built へ追随した**（**D-424**）。**★実害の是正ではなく、着手時に踏む前提の更新である。** **(1) §3.3 へ実査 1 項目を追加し 7 項目へ**〔**`M22-08` が同じ `web/src/features/config/` を触った**。ファイルは分かれているが節構成が変わった〕。**(2) §2.1 の i18n 行を実測へ**〔**1 ロケール 1 ファイル**＝`web/src/locales/{ja,en}.json`。**末尾へ追記しない**＝**D-416**〕。**(3) test-id の台帳が 2 か所であることを明記**〔`DES-005` §6.8 ＋ `docs/design/testid-convention.md`〕。**(4) §7.5 の突合相手へ `M22-08` を追加。** |
| **2.0.0** | 2026-08-15 | **★全面改訂。`M22-RESEARCH-01` §8-1 / F-4 が「QR は実装済み・配線済み」と実測したため、「実装する」から「検証して穴を埋める」へ性格を変えた**（**D-393**）。**依存の追加を巡る停止条件と開発者確認事項は消滅した**（`qrcode.react@4.2.0` は既に `dependencies` に在る）。 |
| 1.0.0 | 2026-08-15 | 初版（**★前提が誤っていた。「QR は未実装」として書かれていた**）。 |

---

## 1. 背景と目的

### 1.1 背景 — **★本サブは「作る」サブではない**

**`M22-RESEARCH-01`（2026-08-15・HEAD `8af2ca6`）が、QR は既に実装され設定画面へ配線されていることを実測した。** **逐語は同レポート §6 F-4 ／ §8-1。**

| 層 | 実測された実体 |
|---|---|
| 依存 | **`"qrcode.react": "^4.2.0"`**（`web/package.json:37` の `dependencies`。`pnpm-lock.yaml` も解決済み） |
| モーダル | `web/src/features/config/QRCodeModal.tsx`（58 行。`:4` `import { QRCodeSVG } from "qrcode.react";`、`:45` `<QRCodeSVG value={url} size={200} />`） |
| 配線 | `web/src/features/config/SettingsSectionNetwork.tsx:6`（import）／`:16`（`showQr` の state）／**`:96-101`（表示ボタン）**／**`:109-110`（`{showQr && config.network.lanUrl && (<QRCodeModal url={config.network.lanUrl} ... />)}`）** |
| i18n | `qrModal.title` ／ `qrModal.description` ／ `qrModal.close` ／ `settings.network.showQr` |
| テスト | `web/src/features/config/QRCodeModal.test.tsx`（3 ケース） |
| サーバ側の供給 | `internal/service/config/service.go:312-326` `resolveNetwork()` → `NetworkDTO{PrimaryLanIp, LanUrl}`（`internal/api/config/dto.go:47-50`）。**`Server.Mode != "lan"` のとき両フィールドとも空文字** |
| IP の決定 | `netutil.SelectPrimaryLANIP()` を `main.go:279` で config サービスへ**関数として注入**（`service.lanIpResolver`） |

**⇒ 本サブの仕事は 3 つに絞られる。**

1. **要件を満たしているかを確かめる**（§4.1・§4.2）。
2. **穴が見つかったら埋める**（§4.3）。
3. **★次に誰かが壊したときに赤くなる状態を作る**（§5.1。**いま満たされていることと、それが守られ続けることは別である**）。

> **★v1.0.0 の前提は誤っていた。** 同版は「QR を描く手段が在るか」を実査項目に置き、依存の追加を停止条件にし、開発者確認事項を 1 件持っていた。**いずれも実体を持たない側の推測であった**（**`E-117` の型。本レーン 3 件目**）。**本版はその推測をすべて実測へ置き換えてある。**

### 1.2 目的

完了時に、次の状態が達成されている。

1. **QR に符号化されている文字列が接続 URL だけであることが、テストで固定されている**（**★いま満たされているだけでは足りない**）。
2. **接続 URL がサーバ由来であることが、テストで固定されている**（同上）。
3. **LAN 共有モードが OFF のとき、ボタンの状態が `DES-005` §5.16 の「ON時のみ有効」と一致している**（**★ここが最有力の穴である**＝§4.3-1）。
4. **`DES-005` §5.16 が併記する「接続可能IP/ポート表示」の在否が確定している。**
5. **★開発者がスマートフォンの実機で読み取り、接続できることを確認している**（**`FR407` の要求そのもの**）。
6. **上記のうち as-built が設計書と食い違う箇所が、完了報告に列挙されている**（§6.5）。

### 1.3 このマイルストーンで作らないもの（スコープ外）

| # | 項目 | 理由 |
|---|------|------|
| 1 | **認証・パスワード**（QR にトークンを載せる等） | **`M22-01` / `M22-02` の担当。** **★`FR407` は「QR には接続 URL のみを含めパスワードは含めない」と本文で定めている**——**理由も「撮影漏洩リスク回避」と書かれている**（§4.1-2） |
| 2 | **楽観排他** | `M22-03` / `M22-04` の担当 |
| 3 | **CORS/CSRF の変更** | `M22-05` の担当。**★契約 F-3 により平文 HTTP のまま** |
| 4 | **LAN 共有モードの切替そのものの挙動** | 既存。**本サブは QR の面だけを触る** |
| 5 | **`SelectPrimaryLANIP` の優先順位の変更** | **既存**（`SUPP-001` §2.6.2 が「QRコードIP選定 = 代表IP決定ロジック」として設計参照を持つ）。**★変えたくなったら §9.3-3 で止まる** |
| 6 | **外部サービスへ問い合わせて QR を得る形への変更** | **`DES-002` §8 の LAN 前提に反する。** **★既に `qrcode.react` によるクライアント側描画である。変えない** |
| 7 | **QR ライブラリの差し替え・追加** | **`CLAUDE.md` §6。** **★既に入っているものを使う。別のものへ替えない** |
| 8 | **`docs/design/` 本体の編集** | **製造は設計書本体を編集しない**（`CLAUDE.md` §8） |

**★スコープ外を重ねた帰結**（playbook §4.11）: **(1) により、LAN 共有モードが ON でパスワード未設定のとき、QR を読んだ端末は無認証で接続できる。** **⇒ これは穴ではなく `DES-002` §8「自宅LAN内は信頼するという割り切りを許容」であり、認証は `M22-01` が別に足す。** **本サブで先取りしない。**

---

## 2. 成果物

### 2.1 作成/修正するファイル

| ファイル | 内容 |
|---|---|
| `web/src/features/config/QRCodeModal.tsx` ／ `SettingsSectionNetwork.tsx` | **as-built 次第**（§4.3 の穴が見つかった場合のみ） |
| `web/src/features/config/QRCodeModal.test.tsx` ／ `SettingsSectionNetwork.test.tsx` | **★必ず触る**（§5.1 の回帰ゲート。**既存 3 ケースに足す**） |
| `internal/service/config/` ／ `internal/api/config/` | **as-built 次第**（§4.2 の穴が見つかった場合のみ） |
| `web/e2e/` 配下 | §5.2 |
| i18n（**★実パスを実査すること**） | 文言を足した場合のみ。**ja/en 両ロケール parity**（playbook §4.13）。**★`M22-04` の実査では `web/src/locales/ja.json` ／ `en.json` の「1 ロケール 1 ファイル」であり、名前空間分割ではない**（2026-08-16 追記＝**D-424**）。**★キーはファイルの末尾へ追記せず、既存の名前空間ブロックの並びの中へ入れること**（**D-416**）——**`ja.json` / `en.json` は元から名前順に並んでいないため、意味の近い位置へ ja/en 同位置で入れる。** **★末尾追記は、同じ 2 ファイルを触る他サブとテキスト衝突する** |
| `docs/progress/m22-06-completion-report.md` | 完了報告 |
| `docs/progress/progress-log.md` | 追記（§7.4） |

> **★上記は「予見できた範囲」であって禁止列ではない。** 一覧に無いファイルでも §2.2 の禁止列に当たらないなら触ってよい。**ただし触った理由を完了報告に書くこと。**

### 2.2 変更しないもの

| # | 対象 | 内容 |
|---|------|------|
| 1 | **`internal/service/notation/` ／ `internal/service/preset/`** | **契約 F-5。** **★これ以外の `internal/` 配下は凍結されていない**（**D-379** の型を避ける）。**`internal/service/config/` は触ってよい** |
| 2 | **`web/src/features/physical-input/` ／ `gamepad/` ／ `keyboard/`** | **契約 F-4。** **★`M21-07` が同じ期間に触っている**（`E-121`） |
| 3 | **`migrations/` ／ `character_data/`** | **本サブは DB に触れない。** **★`M22-07` がマイグレを消費しうる** |
| 4 | **`docs/design/` 配下すべて** | **製造は設計書本体を編集しない**（`CLAUDE.md` §8） |
| 5 | **`web/package.json` ／ `pnpm-lock.yaml` ／ `go.mod` ／ `go.sum`** | **★依存を足さない・替えない・上げない**（`CLAUDE.md` §6 ／ **D-382 (2)**）。**`qrcode.react@4.2.0` は既に在る。そのまま使う** |
| 6 | **`netutil` の優先順位ロジック**（`private_ip.go:150-183` `selectByPriority`） | §1.3-5。**★変えたくなったら §9.3-3 で止まる** |

> **★本表は挙動の凍結であって、バイト列の凍結ではない。** **失効した記述（docblock・コメント）の是正は妨げない**（**ただし新しい仕様・意図・将来計画をコメントへ書き足さないこと**）。**是正した箇所は完了報告に列挙し、挙動が不変であることを diff で示すこと。**

### 2.3 例外条項

**該当なし。** **依存を足す必要が生じたら、それは本サブのスコープ外である**（§9.3-2）。

---

## 3. 前提条件

> 文書ID ⇄ 実パスの正は `docs/handover/docs-map.md`（自動生成）。

### 3.1 必読ドキュメント

| 文書 | 節 | 用途 |
|------|-----|------|
| `REQ-001` `docs/design/requirements.md` | **§3.4 `FR407`**（**希望・3**） | **★要件の本文。「接続 URL のみ・パスワードを含めない」と理由が書かれている** |
| `DES-005` `docs/design/05-screen-design.md` | **§5.16**（設定画面のネットワークセクション）・§6.8（test-id 命名規約。**★入力面以外への射程は 2026-08-16 に明記された**＝`CHANGE-115`。**★台帳は `DES-005` §6.8 と `docs/design/testid-convention.md` の 2 か所にあり、新規付与時は両方へ書く**） | **★本サブの正典。QR ボタンの仕様は既にここに全文で在る** |
| `DES-002` `docs/design/02-architecture.md` | §3.4・§4.2・**§8**（認証・ユーザー管理。**★「自宅LAN内は信頼するという割り切りを許容」と「防御責任の整理」はここに在る。§12 はテスト方針であって LAN の話ではない**） | LAN 構成・脅威モデル |
| `SUPP-001` `docs/design/supp-001-detailed-design.md` | **§2.6.2**（QRコードIP選定＝代表IP決定ロジック）・**§5.6**（起動時案内・センシティブ情報）・§5.5 (10′) (11′) | **★IP 選定の設計根拠** |
| **`M22-RESEARCH-01` 調査レポート** `docs/progress/M22-RESEARCH-01-report.md`（**★実パスは §3.3-1 で確認する**） | **§6 軸 F 全文**・**§8-1** | **★本サブの出発点。実測の逐語** |
| `docs/process/m22-contract.md` | §1.2・§4・§5 | スコープ・触る領域・凍結（**とくに F-3・F-4・F-5**） |
| `docs/instructions/M22-overview.md` | §2.2・§5.0・§5.1 | 優先度・並列投入の運用・E-14 交差表 |
| `docs/handover/retrospective-digest.md` | §1・§5 | 全体 |

### 3.2 参照不要

**★本書は自己完結している。他サブの指示書・レビューチェックリストを参照しないこと**（**D-217**）。**先行サブの実務知見が要る場合は `SUPP-001` §5.5 を引く。**

**本サブで読まなくてよいもの**——`DES-003`（DB に触れない） ／ `DES-004`（記法に触れない） ／ `REQ-001` の認証系 FR（`M22-01` / `M22-02` の担当）。

### 3.3 着手前の確認（**Plan Mode 必須**）

**次の 8 項目を実査し、Plan Mode で提示すること。** **★`M22-RESEARCH-01` は 2026-08-15・HEAD `8af2ca6` 時点のスナップショットである**（`E-56`）。**同レポート作成以降の変化を `git log` で確認してから使うこと。**

1. **`M22-RESEARCH-01` レポートの実パス**と、**同レポート §6 F-4 / §8-1 の記述が HEAD で今も成り立つか**（`git log` で当該ファイル群の変化を確認）。
2. **`SettingsSectionNetwork.tsx:96-101` の表示ボタンの実体**——**★`disabled` 属性が付いているか。付いているなら条件は何か。** **★ここが §4.3-1 の穴の在否を決める。**
3. **`DES-005` §5.16 が併記する「接続可能IP/ポート表示」が画面に在るか**（§4.3-2）。
4. **`QRCodeModal.test.tsx` の既存 3 ケースが何を主張しているか**（**★§5.1 で足すものと重複させないため**）。
5. **`config.network.lanUrl` が空文字のときの画面の挙動**（`local` モード時。`resolveNetwork()` が両フィールドを空にする）。
6. **★`web/src/features/config/` の現況**（2026-08-16 追加＝**D-424**）——**`M22-08` が同じディレクトリへログアウトと利用者の改名を足した**（`SettingsSectionUser.tsx` ／ `UserManagement.tsx`）。**★本サブが触るのは `SettingsSectionNetwork.tsx` ／ `QRCodeModal.tsx` であり、ファイルは分かれている。** **⇒ 交差しない見込みだが、設定画面の節構成が変わっているため実査すること。** **★`DES-005` §5.16 も改訂されている**（v2.70.0〔第81版〕。**ネットワーク節は無傷**）。
7. **★`lan` モードでサーバを起動できるか**（2026-08-17 追加＝**D-425**）——**★クラウド実行環境では起動自体ができない可能性が高い。** **非ループバックアドレスが `192.0.2.2`（RFC 5737 の TEST-NET-1）1 本しか無く、私用アドレス判定が正しく RFC1918 外として弾くため、代表 LAN IP が見つからず起動が中止される**（`M22-05` の実測＝followup `cloud-env-cannot-start-lan-mode`）。**★これは実装の欠陥ではなく仕様どおりの挙動である。** **⇒ QR コードは代表 LAN IP を埋め込む機能であり、同じ制約に当たる。** **★起動できない場合、QR の中身は機械で実測できない。** **その旨を Plan Mode で提示し、実機確認へ回す範囲を先に決めること**（§7.1）。**★「動かないので QR が壊れている」と結論しないこと。**
8. **`make e2e` がクリーンな状態で完走するか**（**★落ちる場合は、自分の変更を疑う前にこの前提を確認すること**。**★あわせて `web/node_modules` と `pip install markdown-it-py` が要る**＝followup `e2e-requires-pnpm-install-on-clean-clone`）。

---

## 4. 詳細仕様

### 4.1 QR の内容は接続 URL だけである（**最重要**）

| # | 内容 |
|---|---|
| 1 | **QR に符号化されるのは `http://<IP>:<Port>` の形の接続 URL だけである**（`FR407` 本文）。**実測では `QRCodeModal` に渡るのが `config.network.lanUrl` の 1 値のみであり、既に満たされている** |
| 2 | **★パスワード・トークン・セッション情報を含めない。** **`FR407` は理由まで書いている**——**撮影漏洩リスク回避。** **QR は写真に写る。写った時点で、写した人以外にも渡りうる** |
| 3 | **★「将来のために埋めておく」形にしない。** **クエリ文字列・フラグメント・任意のパラメータを足さない** |
| 4 | **★破っても動く。** **QR は読めるし接続もできる。露見するのは漏れたあとである。** **⇒ §5.1 の破壊確認でだけ検出できる** |

### 4.2 接続 URL はサーバ由来である（**最重要**）

| # | 内容 |
|---|---|
| 1 | **接続 URL はサーバが決めた値を使う。`window.location` から組み立てない** |
| 2 | **★実測では既に満たされている**——`internal/service/config/service.go:312-326` `resolveNetwork()` が `netutil.SelectPrimaryLANIP()` の結果から `LanUrl` を作り、`GET /api/config` の `network.lanUrl` として返している |
| 3 | **★破ったときの症状が特殊である。** **設定画面はホスト機のブラウザで `localhost` を開いて操作する。** **`window.location` から組み立てると `http://localhost:<Port>` の QR ができ、画面上は正しく表示され、読み取りもできる。** **⇒ 繋がらないのはスマートフォンで試したときだけであり、開発者機では最後まで気づけない** |
| 4 | **★新しい IP 取得経路を作らない**（`E-76`＝**同じ情報が 2 か所にあると必ずドリフトする**）。**既存の `lanIpResolver` の注入経路を使う** |
| 5 | **★`SelectPrimaryLANIP` は代表 IP を 1 件だけ返す**（優先度 4 段。`private_ip.go:150-183`）。**⇒ スマートフォンがその IP と違うセグメントに居ると繋がらない。** **これは既知の設計であり、本サブで変えない**（§1.3-5）。**ただし §4.3-2 の「接続可能IP/ポート表示」が在れば利用者は他の候補を見られる** |

### 4.3 穴の候補（**★実査で在否を決める。無いなら「無かった」と書く**）

| # | 候補 | 根拠 | 埋めるか |
|---|---|---|---|
| 1 | **★LAN 共有モード OFF のとき、表示ボタンが押せてしまう** | **`DES-005` §5.16 は「LAN共有モードON時のみ有効」と定めている。** **実測ではモーダル側が `{showQr && config.network.lanUrl && ...}` で守られているが、ボタン（`:96-101`）の `disabled` は確認されていない。** **⇒ ボタンが押せてモーダルが出ないなら、それは「無反応なボタン」であり、利用者は理由を言語化できない** | **埋める。** **★無効にするだけでなく、理由が見える形にする**（`DES-005` §5.16 の「ON時のみ有効」を満たす最小形＋利用者が次に何をすればよいか分かる 1 行） |
| 2 | **「接続可能IP/ポート表示」が無い** | **`DES-005` §5.16 は QR ボタンと並べてこれも定めている。** **実測（`M22-RESEARCH-01` F-1）は当該セクションの要素として挙げていない** | **在否を確定し、無いなら埋める。** **★`GET /api/config` の `network.primaryLanIp` が既に在るため、サーバ側の新設は要らない見込み** |
| 3 | **~~QR の内容がテキストでも見えない~~** | **★2026-08-17 に穴ではないと判明した**（**D-427**。**開発者「設定画面の QR コードの下に URL を出す、という意味なら既に出している」**）。**⇒ 実装されている** | **★「作るか」ではなく「as-built を報告する」へ変わった。** **どこに出ているか**〔QR モーダルの中か ／ ネットワーク節か ／ 両方か〕**／ 選択してコピーできるかを実査し、完了報告へ書くこと**（`CHANGE-117` §2-c が反映先を押さえている）。**★下記 2〔接続可能IP/ポート表示〕と同じものである可能性がある**——**別物か同じかを確定すること。** 以下は撤回前の記述： **★在否を確認する。無い場合、入れるかは Plan Mode で提示して判断を仰ぐ**（**必須ではない**） |
| 4 | **~~`REQ-001` `FR407` の状態記述が失効している~~** | **★2026-08-17 撤回**（**D-426**）。**設計卓の誤りだった。** **`REQ-001` の要件表の列は `ID / 要件 / 優先度 / フェーズ` であり、実装状態を表す列を持たない。** **⇒ 「希望」は優先度、「3」はフェーズであって、実装済みかどうかとは無関係である** | **★追わないこと。** **実証——`FR501`（簡易ログイン）は `M22-01` で実装済みだが、いまも「重要 / 3」のままである。** **`FR406` も同様。** **⇒ 実装しても列は動かないのが本書の運用である** |

**★上表の「候補」は仮説である。** **実査で「既に満たされていた」なら、それを完了報告へ書くのが成果である**——**埋めるものが無かったことは失敗ではない。**

### 4.4 実機確認が本サブの本体である

**`FR407` が想定している利用の流れは「スマートフォンのカメラを起動 → QR を読み取り → ブラウザが開いて本アプリへ繋がる」である**（**★正確な文言は `REQ-001` §3.4 の本文を実査すること**）。**⇒ この一連が通ることを、開発者が実機で確認して初めて要件が満たされる。**

| # | 内容 |
|---|---|
| 1 | **★手順を完了報告へ書く。** **貼り付ける内容・実行するコマンドは**フェンス付きコードブロック**で書くこと**（**★Markdown 表のセルへ置かない**＝`E-126`。**`\|` のエスケープごとコピーされ、パーサが黙ってスキップする**） |
| 2 | **画面の起動は `make run-server` ＋ `make run-web` の 2 プロセス**、または `make build` した配布バイナリ |
| 3 | **★LAN 共有モードを ON にする必要がある**（`local` モードでは `lanUrl` が空文字になる） |
| 4 | **★確認するのは「QR が表示された」ではなく「スマートフォンのブラウザでアプリが開いた」である**（**表示だけなら `window.location` 由来でも通ってしまう**＝§4.2-3） |
| 5 | **★設定を書き換えたら元へ戻すこと**（`PUT /api/config` は `config.toml` を書く） |
| 6 | **★`M22-05` の実機確認を援用してよいのは、下の 3 条件を全部満たすときだけである**（2026-08-17 追加＝**D-429**）。**⇒ 満たさない項目は `M22-06` のビルドで実施し直す** |

#### 4.4.1 ★過去の手動確認を援用してよい条件（**D-429**）

| # | 条件 | `M22-05` → `M22-06` での成否 |
|---|---|---|
| 1 | **同一のビルドで確認されている**（**主張したい経路のコードに 1 バイトも手が入っていない**） | **★本サブが QR 周り**〔ボタンの表示条件・モーダル・URL 生成・接続 URL の文字表示〕**に 1 行でも触れたら崩れる。** **⇒ 差分ゼロで終わった場合にだけ成立しうる** |
| 2 | **確認項目が完了報告に逐語で残っている**（**「動いた」ではなく「何をして何が起きたか」**） | **★`M22-05` の完了報告に「QR をカメラで読んで開いた」と書かれているかを実査すること。** **書かれていなければ援用できない**——**憶えていることと、辿れる形で残っていることは別である** |
| 3 | **★その確認が、いま主張したい命題を実際に検証している**（**別の目的で行った確認を後から流用しない**） | **★ここが最も危うい。** **`M22-05` の確認命題は「スマートフォンから開いて 1 つ保存できる」＝CORS が締まりすぎていないこと、すなわち「着いた後」の検証である。** **`M22-06` の命題は「QR が埋め込んだ URL がアプリへ着く」＝「着くまで」の検証である。** **⇒ 別の命題であり、`M22-05` は開き方を問うていない**（**URL を手で打って開いても `M22-05` は成立する**） |

**★条件 3 を満たさない項目は、`M22-05` が緑でも未検証である。** **`M22-06` §4.2-3 が言う「表示だけなら `window.location` 由来でも通ってしまう」欠陥は、まさに「着くまで」でしか露見しない。**

**⇒ 開発者が「`M22-05` のとき QR を読んで開いた」と述べた場合は、その言を完了報告へ逐語で書くこと**（**★口頭で消えると、次の担当が同じ問いを立てる**）。

### 4.5 変えないことの確認

| # | 内容 |
|---|---|
| 1 | **★依存を足さない・替えない・上げない**（§2.2-5）。**`qrcode.react@4.2.0` をそのまま使う** |
| 2 | **★`/app_build_check` は現時点で WAIT である**（`M22-RESEARCH-01` G-6＝`nanoid` high×2 ／ `postcss` high×1 ＋ moderate×1 ／ `react-router` 系 moderate×3）。**⇒ これは本サブが作った状態ではなく、着手前から在る。** **本サブで直さない**（**依存更新の判断は開発者**＝`CLAUDE.md` §6）。**★着手前から WAIT であることを完了報告へ書き、自分の変更のせいだと誤読されないようにすること** |
| 3 | **LAN 共有モードの切替そのものが従来どおり動く**（§1.3-4） |

---

## 4.9 否定形確認（撤回した仕様の残骸の全文走査）

> **★番号について**——指示書テンプレはこの節を §4.5 に置くが、**本サブは詳細仕様の小節が §4.5 まで伸びたため §4.9 へ寄せた**（**§4.5 が 2 つ在ると、参照した側がどちらを指すのか判別できない**）。**テンプレ側の番号は変えていない。**

**本サブは既存の定義を撤回しない。** **ただし「QR は未実装である」旨の記述が失効する。**

| # | 撤回・変更した定義 | 走査キーワード | 走査対象 |
|---|---|---|---|
| 1 | **「QR コード表示は未実装である」旨の記述**（**★実物は既にある**） | `FR407` ／ `QR` ／ `qrcode` ／ `未実装` ／ `notImplemented`（**★散文で前提を述べている記述も探す**＝**D-361**） | 本番コード／テスト資産／設計文書・指示書・overview |
| 2 | **「QR ライブラリの追加が要る」旨の記述**（**本サブの v1.0.0 指示書がその代表である**） | `qrcode.react` ／ `依存` ＋ `QR` | 同上 |

**走査の作法**（`SUPP-001` §5.5 (11′) ／ 指示書テンプレ §4.5）:

1. **日本語の散文を否定文字クラス（`[^。]` 等）で走査するときは `LC_ALL=C.UTF-8` を付ける。**
2. **行を跨ぐ文脈（`-A1 -B1`）またはファイル単位（`grep -rl`）でも回す。**
3. **★陽性対照を 1 つ混ぜる**（**`DES-005` §5.16 の QR 行は必ず当たる**）。**「0 件だった」は「無い」ではなく「走査が壊れている」かもしれない**（`E-84`）。
4. **★当たったファイルは全文を目で見る。**
5. **走査コマンドそのものを完了報告へ残す。**

---

## 5. テスト要件

### 5.1 必須テスト（Vitest ＋ Go）

**★本サブの中心はここである。** **「いま満たされている」ことと「守られ続ける」ことは別であり、後者はテストでしか作れない。**

| # | 確認項目 | 層 |
|---|---|---|
| 1 | **QR に渡る値が接続 URL の 1 値だけである**（§4.1-1） | Vitest（`QRCodeModal`） |
| 2 | **★破壊確認 A**——**QR へパスワード相当の文字列を足す実装に変えると、テストが赤くなる**（§4.1-2） | Vitest |
| 3 | **接続 URL が `GET /api/config` の `network.lanUrl` 由来である**（§4.2-1） | Vitest（`SettingsSectionNetwork`） |
| 4 | **★破壊確認 B**——**`window.location` を別ホストへ差し替えても、QR に渡る値が変わらない**（§4.2-3） | Vitest |
| 5 | **LAN 共有モード OFF のとき、ボタンが `DES-005` §5.16 のとおりの状態である**（§4.3-1） | Vitest |
| 6 | **`lanUrl` が空文字のとき、モーダルが壊れず理由が出る**（§3.3-5） | Vitest |
| 7 | **`resolveNetwork()` が `Mode != "lan"` のとき空の `NetworkInfo` を返す**（**★既存テストが在るなら足さない。在否を §3.3 で確認**） | Go |

> **★赤にならなかった項目については、「何が代わりに守っていたか」を特定し、同じ層に契約テストを置き直すこと**（`SUPP-001` §5.5 (10′)）。**「テストを強くした」で済ませない。**

### 5.2 E2E シナリオ（Playwright）

**★1 組で置くこと。片方だけだと常に有効な実装でも緑になる。**

| # | シナリオ |
|---|---|
| A | **LAN 共有モード OFF で設定画面を開き、QR ボタンが `DES-005` §5.16 のとおりの状態である** |
| B | **LAN 共有モード ON で設定画面を開き、ボタンを押すとモーダルが開く** |

- **★spec が `config` を書き換えるなら、元の値へ復元すること。**
- **★資源ごとに 1 ファイルへ寄せること**（**D-362**）。**設定画面を触る既存 spec が在るなら、そちらへ足す。** `/add_e2e_spec` に従う。

---

## 6. レビュー観点（別ファイル参照）

機械レビューは `M22-06-review-checklist.md` に従う。

---

## 6.5 ★CHANGE の要否（**as-built 次第**）

> **★2026-08-17 改訂**（**D-426**）——**`CHANGE-117` を起票した。** **⇒ 本サブは通知書を持って着手する。** **予約枠 `117` は消費済みになった。**

**★`CHANGE-117` が反映先として押さえているのは 4 点である**（同 §2）。**⇒ 下表に該当したら、その中身を完了報告へ書けばよい。新規起票は要らない。**

| # | `CHANGE-117` が押さえている反映先 |
|---|---|
| a | **§5.16 の「LAN共有モードON時のみ有効」の実体**（`disabled` かモーダル側のガードか、両方か） |
| b | **「接続可能IP/ポート表示」の在否**（**★無いなら「設計にあって実装が無い」ことを明示する**） |
| c | **QR の内容をテキストでも見せるか**（**開発者への確認事項**。§4.3-3 と対） |
| d | **代表 LAN IP が CORS の許可 Origin と同じ関数で決まること**（`SUPP-001` §2.6.2 ／ `DES-002` §4.4） |

**⇒ 上記以外で CHANGE が要るのは、次のいずれかが起きたときだけである。**

| # | 起きたら CHANGE が要る |
|---|---|
| 1 | **`DES-005` §5.16 の記述と as-built が食い違った**（**★食い違ったら DES を as-built に合わせる。指示書に合わせない**） |
| 2 | **~~`REQ-001` `FR407` の状態記述の失効~~**（**★2026-08-17 撤回＝D-426**。§4.3-4）。**★`REQ-001` は改訂しない。** **要件表の列は実装状態を表さない**（**`CHANGE-117` §3 に実証あり**）。 以下は撤回前の記述： **「希望・フェーズ3」と書かれた要件の実物が既に在る。** **⇒ 要件表を直すかは設計卓・開発者の判断であり、製造は完了報告へ書くだけ** |
| 3 | **API を新設・変更した**（`GET /api/config` の応答形など） |
| 4 | **`SUPP-001` §2.6.2（QRコードIP選定）の記述と実体が食い違った** |

**★該当したら、完了報告へ「CHANGE が要る」と書いて設計卓へ渡すこと。製造は起票しない。**

**★予約枠 `117` は 2026-08-17 に消費した**（**D-426**）。**⇒ 新規採番は不要である。**

---

## 7. 完了条件（Definition of Done）

### 7.1 機能要件

- §1.2 の 6 項目がすべて達成されている。
- **★§4.3 の穴の候補 4 件について、在否がすべて確定している**（**「無かった」も成果である**）。
- **★開発者の実機確認が済んでいる**（§4.4）。**手順は完了報告へ、貼り付ける内容はフェンス付きコードブロックで書くこと**（`E-126`）。
  - **★マイグレが当たったかは `logs/combomgr.log`（JSON）で見る。** コンソールには起動バナーしか出ない。

### 7.2 自己テスト結果（製造担当の責任範囲）

- `go test ./...` ／ `cd web && pnpm test` ／ `make e2e`。
- **★テストの結果は、コマンド自身の出力（スイート名・pass/fail の件数）を完了報告へ転記すること。** **終了コードだけを根拠に「緑」と書かない**（`E-125`。**`vitest: not found` が exit 0 として報告され「フロントは緑」と誤報告された前例がある**＝`M20-06`）。

### 7.3 品質チェック

- **`migrations/` ／ `character_data/` ／ `docs/design/` の diff が 0**（§2.2-3・§2.2-4）。**実測値を完了報告へ書くこと。**
- **`internal/service/notation/` ／ `internal/service/preset/` の diff が 0**（契約 F-5）。
- **`web/src/features/physical-input/` ／ `gamepad/` ／ `keyboard/` の diff が 0**（契約 F-4。**`M21-07` との非干渉**）。
- **★`web/package.json` ／ `pnpm-lock.yaml` ／ `go.mod` ／ `go.sum` の diff が 0**（§2.2-5）。
- `bash scripts/check-browser-storage-keys.sh`（**★新しいストレージキーを使っていないこと**）。

### 7.4 ドキュメント

- **`docs/progress/progress-log.md` への追記**（製造担当が行う。`CLAUDE.md` §8・playbook §11.1〜§11.3）。**完了報告・設計伝達レポートとは別である**——`progress-log.md` は**結果ログのフル版**（時系列・横断）であり、**個別ログ（サブ単位の詳細）と抽出版（設計伝達レポート）とは読者も粒度も用途も違う。**
- **§6.5 の 4 条件のいずれかに該当したら、完了報告へ「CHANGE が要る」と書く。** **製造は `DES-005` / `REQ-001` / `SUPP-001` を編集しない。**

### 7.5 完了報告

`docs/progress/m22-06-completion-report.md`。**次を必ず含める。**

1. **§3.3 の実査 8 項目の結果**（**★とくに 2＝ボタンの `disabled` の実体**）。**★2026-08-18 是正＝「6 項目」と書いていたが §3.3 の実体は 8 項目である**（**D-433**。**v2.1.0 で 7 へ、v2.2.0 で 8 へ増補した際に参照側が追随していなかった**）。
2. **★§4.3 の穴の候補 4 件それぞれの在否と、埋めたか埋めなかったか。**
3. **★破壊確認 A / B の結果**（§5.1 の 2・4）。**赤にならなかった項目については、何が代わりに守っていたかと、置き直した契約テストの位置。**
4. **実機確認の手順と結果**（§4.4。**★「QR が表示された」ではなく「スマートフォンのブラウザでアプリが開いた」まで**）。**★各項目について「`M22-06` のビルドで実施した」のか「`M22-05` の確認を援用した」のかを明記すること**（**§4.4.1 の 3 条件＝D-429**。**援用する場合は 3 条件のどれをどう満たすかを書く**）。
5. **★`/app_build_check` が着手前から WAIT であること**（§4.5-2）。**自分の変更のせいではないと分かる形で書く。**
6. **否定形確認の走査コマンドと結果**（§4.9）。
7. **§6.5 の CHANGE 要否の判定**（**該当なしなら「該当なし」と明記**）。
8. **★並列相手との突合**（`E-121`）——**`M21-07`**〔**設定画面と物理入力は面が違うが、`web/src/` を同時に触る**〕**／ `M22-07` ／ ★`M22-08`**〔**同じ `web/src/features/config/` を触った**。2026-08-16 追加＝**D-424**〕**／ 直列本線のうち完了済みサブ**。**並列相手が居なかったなら「居なかった」と明記する。**
9. **★「■ 併せて更新が要るもの」**（該当が無くても「なし」と書く）——**CHANGE の要否 ／ 版を上げた文書の参照元 ／ 設計書と食い違った箇所。**

---

## 8. 参照ドキュメント

| 文書（実パス） | 節 | 用途 |
|------|-----|------|
| `REQ-001` `docs/design/requirements.md` | §3.4 `FR407` | 要件の本文と理由 |
| `DES-005` `docs/design/05-screen-design.md` | §5.16・§6.8 | 画面仕様・test-id 命名規約 |
| `DES-002` `docs/design/02-architecture.md` | §3.4・§4.2・**§8** | LAN 構成・脅威モデル（**★LAN の割り切りは §8。§12 はテスト方針である**） |
| `SUPP-001` `docs/design/supp-001-detailed-design.md` | §2.6.2・§5.6・§5.5 (10′) (11′) | IP 選定・起動時案内・破壊確認と走査 |
| `docs/progress/M22-RESEARCH-01-report.md` | §6 軸 F・§8-1 | **★本サブの出発点** |
| `docs/process/m22-contract.md` | §1.2・§4・§5 | スコープ・触る領域・凍結 |

---

## 9. 注意事項・判断に迷ったら

### 9.1 推測で進めてはいけない事項

1. **ボタンの `disabled` の実体**（§3.3-2）。**★実査する。**
2. **「接続可能IP/ポート表示」の在否**（§3.3-3）。
3. **既存 3 テストが何を主張しているか**（§3.3-4）。**★重複させない。**
4. **`REQ-001` の状態記述をどうするか**（§4.3-4）。**★製造は要件を編集しない。**

### 9.2 推測で進めてよい事項（その旨を明示）

1. **テストのファイル配置・命名**（既存の慣行に揃える）。
2. **無効時の理由表示の文言**（**★ja/en 両方を揃えること**＝playbook §4.13）。
3. **失効した docblock・コメントの是正**（§2.2 の但し書き。**判断が割れるなら触らずに報告**）。

### 9.3 不明事項発見時の対応

| # | 停止条件 |
|---|---|
| 1 | **★QR へパスワード・トークンを載せる必要が生じた**（§4.1-2）。**⇒ `FR407` 違反である。止めて報告する** |
| 2 | **★依存を足す・替える・上げる必要が生じた**（§2.2-5）。**⇒ `CLAUDE.md` §6 ／ D-382 (2)。止めて報告する。** **候補と、`node_modules` の `LICENSE` ファイルの実体から読んだライセンス本文を添えること**（**★記憶で「MIT である」と書かない**——**誤りが直接ライセンス違反になる**） |
| 3 | **`SelectPrimaryLANIP` の優先順位を変える必要が生じた**（§1.3-5・§2.2-6）。**⇒ `SUPP-001` §2.6.2 の設計に触れる。止めて報告する** |
| 4 | **`M22-RESEARCH-01` の実測（§1.1 の表）が HEAD で成り立たない**（§3.3-1）。**⇒ 何がどう変わっていたかを報告して、Plan Mode をやり直す** |
| 5 | **本サブが「高速実装」の枠を超えて重くなった**（**★優先度は `FR407`＝希望・3 であり、M22 の中で最も低い**）。**⇒ 止めて報告し、どこまでやったかを書く** |
| 6 | **`make e2e` が着手前から落ちている**（§3.3-6）。**⇒ 自分の変更を疑う前に前提を確認し、落ちたままなら報告する** |

### 9.4 Plan Mode の提示項目

**§3.3 の 8 項目の実査結果 ＋ §4.3 の穴の候補 4 件それぞれの在否の見込み ＋ §5.1 で足すテストの一覧**を提示すること（**★2026-08-18 是正＝D-433**）。

---

## 10. 完了後の次ステップ

1. **設計伝達レポートの生成**（`/design_handover_report`）。**★followup の更新候補は §4 へ書く**——**製造は `followup-backlog.md` を編集しない**（**D-382**）。
2. **§6.5 に該当したら、設計卓が `CHANGE-117` を起票し三点セットを出す。**
3. **設計卓が followup `qr-already-implemented-m22-06-scope` を畳む。**
4. `docs/human-notes/model-allocation.md` へ本サブを追記（設計卓）。

---

## 11. 開発者への確認事項

**全て確定済み。** 本指示書に開発者判断待ちの項目は無い。

- **★v1.0.0 が持っていた「QR 依存の追加可否」は消滅した**——**`qrcode.react@4.2.0` は既に `dependencies` に在り、追加は不要である**（`M22-RESEARCH-01` F-4）。
- **★実機確認は開発者の手番である**（§4.4）。**PC ＋ スマートフォンの 2 台が要る。** **⇒ 着手はブロックされないが、完了はここで止まる。**

---

*以上、M22-06 製造指示書 v2.7.0。配置 `docs/instructions/M22-06-connection-qr-code.md`。**作るサブではなく、既にあるものが `FR407` を満たしているかを確かめ、守られ続ける形にするサブである。***
