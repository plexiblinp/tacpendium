# 指示書 M38-02: 技編集をメニューから外し、「引っ越し取込」を「他から引っ越し」へ改名する

| 項目 | 内容 |
|------|------|
| 文書ID | M38-02 |
| バージョン | **1.2.0**（2026-09-18・**★★★as-built 注記を 2 件足した。⇒ 本文は 1 文字も変えていない**〔§0.1 / §0.2 の「逐語」が失効したこと ／ §2.2a が*実行されなかった*こと〕**。★完了したサブの指示書は歴史記録である。⇒ 書き換えず、何がどうなったかを冒頭に置く**＝`D-895`） ／ **1.1.0**（2026-09-17・**★★説明書の技編集の章の扱いを 1 件足した**〔開発者逐語＝「指示書（技編集周り）も直させていいですか？直してもらった方が自然です」〕**。⇒ §2.2-5 と §2.2a が新設。★段 3**〔改名〕**と §1〜§0 は 1 文字も変えていない**） ／ **1.0.0**（2026-09-17・発行） |
| 作成日 | 2026-09-17 |
| 作成者 | 設計担当 Claude（設計卓） |
| 対象マイルストーン | **`M38`**（撮影で出たリリース前要望の第 2 束）。上位＝`docs/instructions/M38-overview.md` v1.0.0 |
| 推奨モデル | **標準実装**（実モデル名は `docs/human-notes/model-allocation.md`） |
| Plan Mode | **推奨**（**★改名の足し先を先に数えて見せること**） |
| レビュー | **必要**（**★チェックリスト＝`docs/instructions/reviews/M38-02-review-checklist.md` v1.2.0**） |
| CHANGE 消費 | **1〜2 本の見込み**（`DES-005` §5.16 / §5.18 / §5.19 ＋ §2 の画面一覧。**★自採番しない**＝`D-293`） |
| マイグレ消費 | **0 本** |
| 前提サブ | **なし**（**★`M38-01` とは並列可**＝`M38-overview` §3.3） |
| 射程 identity | **2 件**——技編集の導線除去〔**★説明書の当該章を含む**＝§2.2a〕 ／ 改名 |
| 配置 | `docs/instructions/M38-02-moves-grid-hide-and-rename.md` |
| 出力 | `docs/progress/M38-02-completion-report.md` |

---

## ★★★as-built（**2026-09-18 追記＝`D-895`。本サブは完了済みである**）

> **★本節は歴史記録の注記である。⇒ 以降の本文は発行時のまま 1 文字も変えていない。**

| # | 発行時の記述 | 実際にどうなったか |
|---|---|---|
| **★★★1** | **§0.1 / §0.2 が「開発者の逐語」として記録した*理由***〔外す理由＝フレームが特殊で開発者自身が直したい ／ 残す理由＝フェーズ5 で setplay only 画面へリメイクする〕 | **★★失効した。⇒ 2026-09-17 に開発者が訂正した**〔外す理由＝本画面は技データ是正の手段ではない。是正はマイグレーションで行う ／ 残す理由＝**消すコストが高かったから**〕**。★★★正本は `DES-005` §5.18 である**（`CHANGE-214`）**。★仕様は 1 文字も動いていない。動いたのは理由だけである** |
| **★★★2** | **§2.2a**〔説明書の技編集の章＝**章は消さない・本文を書き換える・図の置き場を外す**〕 | **★★実行されなかった。⇒ 製造は v1.0.0 で走っており、本節が届かなかった。★代わりに開発者が同じ問いを直接裁定し、*章ごと削除*で決着した**（逐語＝「あまり見せたくない機能について言及を残すのは少し嫌です」）**。⇒ 説明書は 22 章 → 21 章になり、章数の 4 か所はすべて追随済みである。★★設計卓の「章を消すと 4 か所に波及するから消さない」という射程判断は、実測では*安かった*** |

**★★★2 の帰結＝`M38-03` の母集団が「図 1 枚ぶん」ではなく「章 1 つぶん」動いた。⇒ `M38-03` v1.1.0 で是正済みである。**

---

## 0. この指示書の特殊性

### 0.1 ★★★技編集は「消す」のではない。**導線を外すだけである**

**★開発者の逐語**（2026-09-17）**＝「いったんはメニューから消すだけでいい。コードはまだ使うので消さない。なお URL 直打ちでの遷移は残っていても良い」。**

| やること | やらないこと |
|---|---|
| **設定画面から技編集への導線を外す** | **★コードを消すこと** |
| — | **★ルートを消すこと**（**⇒ URL 直打ちで着ける状態を残す**） |
| — | **★テストを消すこと** |

**★★★理由＝将来「setplay only の技だけを作成・編集する画面」へリメイクする**（開発者の逐語）**。⇒ `setup_only` の付与口そのものであり、フェーズ5 の射程である**（`followup` の `setup-only-column-has-no-input-path`）**。**

**★★いま消す理由は別に在る**——**一部の技のフレームが特殊**〔`through` 等〕**であり、初期はフレームの誤りを開発者自身が直したい**（逐語）**。⇒ 利用者に触らせない。**

### 0.2 ★★これは「過去の裁定の復元」ではない。**新しい判断である**

**★`followup` の `M14-c` は「配布 DB を全キャラ手入力データ投入済みで同梱（**技編集は温存**）」「DES-005 画面17 削除・**画面18 温存**」と記録している。⇒ 温存が決まっていた。**

**★★消されたのは画面 17**〔moves 取込プレビュー〕**であり `CHANGE-055` / `M14-02` である。**

**★★★2026-09-17 の開発者裁定がこれを上書きした**（`D-892`）**。⇒ 指示書がその経緯を書いているのは、次に読む人が「昔から消す予定だった」と誤読しないためである。**

### 0.3 ★★改名の足し先は**数えてから**直す

**★「引っ越し取込」は `CHANGE-147` / `M24-07` で「取込ヘルパー」から改名された経緯がある。⇒ 2 度目の改名である。**

**★★★足し先を先に全数走査すること。⇒ ナビ ／ 画面見出し ／ 動線の文言 ／ 説明書 ／ テスト。★1 か所落とすと「2 つの呼び名がある画面」になる**（`followup` の `import-screen-has-two-names` と同じ型）**。**

### 0.4 ★★着手前の版ゲート（**`M-169` の対処**）

| # | 確かめること | 満たさないとき |
|---|---|---|
| **1** | `docs/instructions/reviews/M38-02-review-checklist.md` が存在すること | **★止まって設計卓へ請求する** |
| **2** | **`DES-005` §5.19 の節名が「引っ越し取込」であること** | **★食い違ったら止まる。⇒ 既に誰かが直している** |
| **3** | **`DES-005` §5.18 が「moves 編集グリッド」として在ること** | **★同上** |
| **4** | **`docs/usermanual/` に説明書の本文が在ること**（**★改名の足し先である**） | **★無ければ `M32-03` が未着地である。⇒ 止まって請求する** |
| **5** | `git log --oneline -5` で枝元を報告へ書く | — |

---

## 1. 前提として読むもの

| # | 資料 | どこを |
|---|---|---|
| **★★1** | `docs/design/05-screen-design.md` | **§5.16**（設定。導線の在り処） ／ **§5.18**（moves 編集グリッド） ／ **§5.19**（引っ越し取込） ／ **§2**（画面一覧） ／ §3（画面遷移図） |
| **★2** | `docs/handover/followup-backlog.md` | `setup-only-column-has-no-input-path` ／ `import-screen-has-two-names` ／ `preset-vs-moves-grid-role-split-not-discoverable` ／ `punish-unknown-damage-fix-requires-moves-grid` |
| **★★3** | `web/src/locales/ja.json` | **★表示語の正本である。⇒ 改名はここから引く**（計測点は `M32-03` §7-2） |
| **★★4** | **`docs/usermanual/tacpendium-readme.html`** | **★★技編集の章**（§2.2a） ／ **「引っ越し取込」が出る章**（§2.3-4）**。⇒ 本サブは両方へ届く** |
| 5 | `docs/change-notes/CHANGE-147-notification.md` | **1 度目の改名の経緯** |

---

## 2. 段取り

### 2.1 ★★★段 1 — 実査（**★コードを書く前に済ませ、報告へ書く**）

| # | 確かめること | なぜ |
|---|---|---|
| **★★1** | **「引っ越し取込」の出現箇所の全数**（ナビ ／ 画面見出し ／ 動線の文言 ／ 説明書 ／ テスト ／ E2E） | **★§0.3。⇒ 1 か所落とすと 2 つの呼び名になる** |
| **★★★2** | **技編集への導線の全数**（**★設定画面だけとは限らない**） | **★`followup` の `punish-unknown-damage-fix-requires-moves-grid` が「確定反撃サーチに導線が無い」と書いている。⇒ *無い*ことの確認でもある** |
| **★3** | **技編集のルートと、それを踏むテスト・E2E の全数** | **★ルートは残す。⇒ テストが落ちないことを先に見る** |
| **★★4** | **説明書の 2 面**——(a) **技編集の章**（§2.2a） ／ (b) **「引っ越し取込」が出る章**（§2.3-4）**。★どちらも*中身*で特定し、件数で報告へ書く** | **★改名と導線除去の両方が届く。⇒ (a) は図の置き場の数も数える**（§2.2a-4） |

**★走査は `--include` を絞らないこと。⇒ 同じ語が Go とフロントと説明書の 3 面に居る**（`M37-07` §7-1 の教訓）**。★出力をファイルへ全量落として `wc -l` で数える。⇒ `head` で切らない。**

### 2.2 ★★段 2 — 技編集の導線除去（**射程 1**）

| # | やること |
|---|---|
| **★★1** | **設定画面から技編集への導線を外す** |
| **★★★2** | **ルート・コード・テストは残す。⇒ URL 直打ちで着ける** |
| **★3** | **導線を踏んでいた E2E が在れば、URL 直打ちへ寄せる。⇒ spec を消さない** |
| **★★4** | **「なぜ外したか」をコードのコメントへ 1 行残す。⇒ 次の担当が「使われていないから消そう」と判断しないため** |
| **★★★5** | **説明書の技編集の章を、導線が無い状態に合わせて直す**（**★やり方は §2.2a**） |

#### 2.2a ★★★説明書の技編集の章の扱い（**v1.1.0 で新設**）

**★★開発者の逐語**（2026-09-17）**＝「指示書（技編集周り）も直させていいですか？直してもらった方が自然です」。**

**★★★理由＝画面から導線が消えるのに説明書が「設定から開けます」と書いたままだと、説明書と画面が食い違う。⇒ `M32-02` が `UI の粗` として記録した `import-parent-resolution-mark-contradicts-result` と同じ型である。**

| # | やること | なぜ |
|---|---|---|
| **★★★1** | **章を消さないこと。⇒ 見出しは残す** | **★★章数「22」が 4 か所に散在している**（`followup` の `usermanual-chapter-count-22-scattered`）**。⇒ 章を 1 つ減らすと 4 か所とも直す必要があり、本サブの射程を越える** |
| **★★2** | **本文を、いまの状態へ書き換える**〔メニューからは開けないこと ／ 将来 setplay only の技を作る画面へ作り直す予定であること〕 | **★§0.1。⇒ 「消した」とは書かない。コードもルートも残っている** |
| **★★★3** | **その章の図の置き場**（`figure` の `todo`）**を、置き場ごと外す** | **★★導線が無い画面は撮れない。⇒ 置き場を残すと、撮影サブの「`img` の全数が実在するか」が永久に赤のままになる** |
| **★★4** | **図を減らした結果の最終の枚数と場所を、完了報告へ一覧で書く** | **★★★撮影サブ**（`M38-03`）**の母集団が本サブで動く。⇒ 書かないと向こうが古い枚数で数える**（計測点 `M-187` と同じ型） |
| **★★5** | **「現在準備中です」という語を使わないこと** | **★`M32-02` 完了条件 7.5 が全数を外し、`M32-03` §2.4-1 が 0 件であることを走査している。⇒ 復活させると両方の成果を戻す** |
| **★6** | **章の位置は*中身*で特定する。⇒ 章番号を当てにしない** | **★§2.1-4 の「`ch16` 近辺」は目安である。★実物を読んで決めること**（計測点 `M-188`＝**設計卓は `docs/usermanual/` の現物を持っていない**） |

**★★本章は「導線除去」の一部である。⇒ 段 3**〔改名〕**とは別の面であり、同じ章に両方が届くとは限らない。★段 1-4 で (a) と (b) を分けて数える理由がこれである。**

### 2.3 ★★段 3 — 改名（**射程 2**）

| # | やること |
|---|---|
| **★★1** | **「引っ越し取込」を「他から引っ越し」へ。⇒ 段 1-1 で数えた全数へ当てる** |
| **★★★2** | **表示語は `web/src/locales/ja.json` から引く。⇒ 設計書の節名を写さない**（`M32-03` §7-2 の教訓＝**`CHANGE` 通知書の本文は表示語の正本ではない**） |
| **★3** | **ルート**（`/import/combo/helper`）**は変えない。⇒ 改名は表示語だけである** |
| **★★4** | **説明書の該当章も直す。⇒ `docs/usermanual/` は本サブの射程内である**（**★`M38-01` は同ディレクトリを触らない**） |

### 2.4 ★段 4 — 設計書の原稿

**★CHANGE 原稿を設計伝達レポートの §1 と §6 へ書く。⇒ §4 ではない**（計測点 `M-183`）**。★設計書本体は書き換えない**（`CLAUDE.md` §8）**。**

**★★`DES-005` §2 の画面一覧にも「引っ越し取込」が在る。⇒ 原稿に含めること**（`followup` の `des005-screen-inventory-table-missing-rows` と同じ面）**。**

---

## 3. やらないこと

| # | やらないこと | なぜ |
|---|---|---|
| **★★★1** | **技編集のコード・ルート・テストを消すこと** | **★§0.1。⇒ フェーズ5 でリメイクする** |
| **★★2** | **`/import/combo/helper` のルートを変えること** | **★改名は表示語だけである。⇒ ルートを変えるとブックマークが切れる** |
| **★3** | **`setup_only` の付与口を作ること** | **★フェーズ5 の射程である** |
| **★★4** | **`M38-01` の面を触ること** | **★新規登録・編集は向こうの射程である** |
| **★5** | **`docs/usermanual/dist-readme.txt` を触ること** | **★`M36-01` の手番である**（`D-887`） |
| **★★6** | **説明書の技編集の章を丸ごと消すこと** | **★★§2.2a-1。⇒ 章数「22」が 4 か所に散在しており、減らすと 4 か所とも直すことになる** |
| **★7** | **説明書の図を、技編集の章*以外*で増減させること** | **★★撮影サブ**（`M38-03`）**の母集団が動く。⇒ 動かすのは §2.2a-3 の 1 枚だけに留める** |
| **★8** | **`docs/design/` 本体を編集すること** | **`CLAUDE.md` §8** |

---

## 4. 危険（**★起きやすい順**）

### 4.1 ★★★最大の危険＝**改名を 1 か所落とすこと**

**★`followup` の `import-screen-has-two-names` が同じ型を記録している**〔ナビ「取込」と `h1`「取込(CSV)」〕**。⇒ 2 度目の改名でそれを増やさない。**

**★★段 1-1 で数えた全数と、直した数を報告で突き合わせること。**

### 4.2 ★★2 番目＝**技編集を消してしまうこと**

**★§0.1。⇒ 外すのは*導線*だけである。★コードもルートもテストも残す。**

**★★★次の担当が消しにいかないよう、コードへ理由を 1 行残すこと**（§2.2-4）**。**

### 4.3 ★★3 番目＝**表示語を設計書から写すこと**

**★`M32-03` §7-2 の教訓＝`CHANGE` 通知書の本文は*設計の説明*であり、表示語の正本ではない。⇒ `ja.json` を引く。**

### 4.4 ★4 番目＝**E2E の spec を消すこと**

**★導線が消えると spec が落ちる。⇒ 消すのではなく URL 直打ちへ寄せる**（§2.2-3）**。★spec を消すと、ルートが生きていることを誰も見なくなる。**

### 4.5 ★★5 番目＝**説明書の図の枚数を、報告に書かずに動かすこと**（**v1.1.0 で追加**）

**★★§2.2a-3 で置き場が 1 つ減る。⇒ 撮影サブ**（`M38-03`）**は「説明書に在る `img` の全数」を母集団に取る。**

**★★★計測点 `M-187` が同じ型である**——**`M32-03` の母集団を*前サブの執筆時点*から引いたため 5 件取りこぼし、うち 2 件は現に誤っていた。⇒ 母集団は*着地*から引く。★そのために、本サブは最終の枚数を報告へ書く義務を負う**（§2.2a-4）**。**

---

## 5. テスト

| # | 見るもの |
|---|---|
| **★★1** | **「引っ越し取込」が 0 件であること**（**★段 1-1 で数えた全数に対して**） |
| **★★2** | **技編集のルートが URL 直打ちで着けること** |
| **★3** | **設定画面に技編集への導線が無いこと** |
| **★★4** | **技編集のテストが落ちていないこと**（**★コードは残っている**） |
| **★★4b** | **説明書の技編集の章に、`todo` クラスの図の置き場が残っていないこと**（§2.2a-3）**。★あわせて「現在準備中です」が 0 件であること**（§2.2a-5） |
| 5 | **`go test ./...` ／ `cd web && pnpm test` ／ `make e2e` を全数**（**★出力をファイルへ全量落とし、`EXIT=` と `FAIL` の件数を報告へ貼ること**） |
| 6 | `bash scripts/check-md-emphasis.sh <触った .md>` ／ `bash scripts/check-doc-refs.sh` |

---

## 6. 完了条件

| # | 条件 |
|---|---|
| **★★★1** | **段 1 の実査 4 点が報告に在る。⇒ とくに 1 と 2 は全数の件数で書かれている** |
| **★★2** | **「引っ越し取込」の出現が 0 件。⇒ 数えた全数と直した数が突き合わせてある** |
| **★★★3** | **技編集のコード・ルート・テストが残っている**（``git diff`` で示す） |
| **★4** | **設定画面から導線が消えている** |
| **★★5** | **コードに「なぜ外したか」の 1 行が在る** |
| **★★6** | **説明書の「引っ越し取込」の章が直っている**（§2.3-4） |
| **★★★6b** | **説明書の技編集の章が直っている**（§2.2a）**。⇒ 見出しは残り ／ 本文がいまの状態を書き ／ 図の置き場が外れている** |
| **★★6c** | **説明書の図の最終の枚数と場所が、完了報告に一覧で在る**（§2.2a-4）**。⇒ 着手前の枚数との差も書く** |
| 7 | **§5 の検査がすべて緑**（**★出力で判定する**） |
| **★8** | **`docs/progress/M38-02-completion-report.md` を書き、`progress-log.md` へ索引行を 1 行足した** |
| **★★9** | **設計伝達レポートを出した**。**★CHANGE 原稿は §1 と §6 へ**（計測点 `M-183`） |
| **★10** | **着手前の版ゲート 5 点を通した**（§0.4） |
| **★★11** | **完了報告を書いた*後に*、報告を入力に取る検査を回し直した**（**★製造 CLI の当該節**） |

---

## 7. ★開発者への確認事項

**★全て確定済みである**（2026-09-17・`D-892`）**。**

| # | 事項 | 確定内容 |
|---|---|---|
| 1 | 技編集を消す範囲 | **★メニューから消すだけ。⇒ コードは残す。★URL 直打ちは残ってよい** |
| 2 | 新しい名前 | **★「他から引っ越し」** |
| 3 | 将来の形 | **★setplay only の技だけを作成・編集する画面へリメイク。⇒ フェーズ5** |

---

## 8. 参照ドキュメント

| ID | ファイルパス | どこを |
|----|-------------|--------|
| M38-overview | `docs/instructions/M38-overview.md` | §2（**前提が覆った 3 件**） ／ §4（フェーズ5 送り） |
| DES-005 | `docs/design/05-screen-design.md` | §2 ／ §3 ／ §5.16 ／ §5.18 ／ §5.19 |
| followup | `docs/handover/followup-backlog.md` | `setup-only-column-has-no-input-path` ／ `import-screen-has-two-names` |
| CHANGE-147 | `docs/change-notes/CHANGE-147-notification.md` | 1 度目の改名 |
| ボード | `docs/process/parallel-board.md` | `D-891` / `D-892` |

---

*以上、指示書 M38-02 **v1.2.0**。* **★★★§0.1 を最初に読むこと**——**技編集は「消す」のではなく「導線を外す」だけである。⇒ コードもルートもテストも残す。★フェーズ5 でリメイクする。**
