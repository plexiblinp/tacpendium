# 指示書 M14-RESEARCH-03: seed 波 1 回あたりの工程の全数（第四波の着手前調査）

| 項目 | 内容 |
|------|------|
| 文書ID | M14-RESEARCH-03 |
| バージョン | **1.0.1**（2026-09-02。**★投入前 errata。★調べることは 1 件も変えていない**） |
| 作成日 | 2026-08-30 |
| 作成者 | 設計担当 Claude（設計卓／M24 期） |
| 対象マイルストーン | **M14**（公式データ配布是正・配布 DB 同梱）。**★M24 / M25 のサブではない**（`D-610`） |
| 推奨モデル | **高精度実装**（**★判断は行わない。★網羅性と母数の正確さが成果物の質を決める**。実モデル名は `docs/human-notes/model-allocation.md` が正本） |
| Plan Mode | **不要**（read-only・judgement-free） |
| レビュー | **不要**（実装差分が 0 のため） |
| CHANGE 消費 | **なし** |
| マイグレ消費 | **なし**（**★★次に払い出す番号は `000081` のまま**〔**2026-09-02 是正。起票時は `000080` と書いていたが、その後 `M24-08` が `000080` を消費した**——**地上ダッシュの表示語の是正。DML であって DDL ではない**＝`D-620` / `D-631`〕） |
| 配置 | `docs/instructions/M14-RESEARCH-03-seed-wave-work-inventory.md` |
| 出力 | `docs/progress/M14-RESEARCH-03-report.md` |

---

## 0. この指示書の特殊性

### 0.1 製造担当として起動し、調査だけ実施して閉じる

**★★コードを 1 行も変えない。CSV を作らない。マイグレを書かない。**

**★★判断もしない。** 「どう直すべきか」「どの順で投入すべきか」は書かない。**事実と母数だけを列挙する。**

### 0.2 ★★なぜこの調査が要るのか

**残り 12 キャラ**（開発者の列挙。§2.1 で数え直す）**の seed 波を第四波として実施する。⇒ しかし「1 波あたりに何をするのか」の全数が、どこにも 1 か所にまとまっていない。**

**★設計卓が followup と契約書から数えられたのは 5 種類である**——**(1) `moves.is_projectile` の backfill ／ (2) 移動 system move の `total` の backfill ／ (3) `custom_states` の `show_delta` 付与 ／ (4) M20 のエイリアス生成規則の再適用 ／ (5) 移動 9 種 seed（`000025`）が当たっていないキャラを遡って埋める。**

**★★しかしこれが全数である保証は無い。** **★最後の seed 波（第三波・`M14-03e`）は 2026-08-01 に終わっており、その後 `M18` / `M19` / `M20` / `M21` / `M22` / `M23` / `M24` の 7 マイルストーンが走っている。⇒ その間にスキーマと生成規則へ何が積まれたかを、設計卓は数えていない。**

**⇒ 本調査の主題は「seed 波 1 回あたりに現在必要な工程の全数」である。**（`M-89` の同型を避ける＝**「N 個ある」と書かれた列挙は、その N を数え直す**）

### 0.3 ★★母数の作法

**★★「N 個ある」と書かれた列挙は、その N を数え直すこと**（playbook §4.40）。

**★★あわせて「列挙そのものが全数か」を疑うこと**（`M24-09d` の教訓＝**数が合っていても、数える対象の集合が指示書で狭められていることがある**）。

**★本書が挙げる件数はすべて「設計卓が数えたもの」であり、実物で裏を取っていない。**

---

## 1. 前提として読むもの

| 対象 | 何を読むか |
|---|---|
| `docs/handover/followup-backlog.md` | **★「未 seed 波の必須前提」群 ／ 「残キャラ網羅表」 ／ `M14-03-is-projectile-backfill` ／ `M14-03-dash-total-backfill` ／ `M14-03b-custom-states-stock` ／ `combo-csv-io-ken` ／ `pre-dist-e2e-clean` ／ `character-down-migrations-orphan-user-combos` ／ `move-derivations-explicit-delete-on-reseed`** |
| `docs/process/m20-contract.md` | **★§1.1-3（エイリアスの生成規則は seed 波が再適用できる形で作る） ／ 除外表（残り 14 キャラの seed 波は M14 の所管）** |
| `docs/instructions/M14-03d-second-wave-manon.md` ／ `M14-03e-third-wave-code-quality.md` | **★過去 2 波の実際の工程。★これが最良の一次資料である** |
| `docs/instructions/M14-overview.md` §3 | **★「seed 波は前提マイグレ 2 本を含む 5〜6 段」注記** |
| `internal/seedgen/` | **★現在の生成物** |
| `migrations/` | **★過去 2 波が消費した連番と、その内容** |
| `character_data/` | **★CSV の現在のファイル数と顔ぶれ** |
| `docs/handover/code-facts.md` | **★引く。想定で書かない** |

---

## 2. 調べること

### 2.1 ★★残キャラの全数と顔ぶれ（**★数が合っていない**）

> **★★★【2026-09-02 是正②＝`D-655`】以下の errata は実測で否定された。読むなら `docs/progress/M14-RESEARCH-03-report.md` §2.1 を正本とすること。**
>
> **実測＝`characters` テーブルは 19 行**〔31 行ではない〕**／そのうち 2 行**（`c_viper` / `dhalsim`）**は移動 system move 9 種のみを持つ仮登録**〔攻撃技 0・`total` 全 NULL〕**／本 seed 済みは 17 キャラ・残りは 14 キャラ**〔未登録 12 ＋ 仮登録 2〕**／`character_data/*.csv` は 31 ファイル。**
>
> **⇒ ボード §1.3 の「全 31 / seed 済み 17 / 残り 14」は 3 つとも合っていた。★誤っていたのは設計卓の errata のほうである**（計測点 `M-112`）。**機序＝`M25-RESEARCH-01` §4.3 の「seed 済み 19」は `COUNT(moves)>0` 判定であり、仮登録 2 体を算入していた。設計卓はそれを「`characters` が 31 行」と読み替えて要約した。**
>
> **★★救ったのは本節の実査項目である**——**「数え直すこと」を残していたため、製造が母数から数え直して誤りを検出した。⇒ `M-108` と同じ形で救われた。**
>
> 以下は 2026-09-02 の errata（**誤り。経緯として残す**）：
>
> **★★【2026-09-02 errata】本節の問いは、起票後に `M25-RESEARCH-01` が先に答えている。**
>
> **一次源＝`docs/progress/M25-RESEARCH-01-report.md` §4.3。実測の結論は次のとおり。**
>
> - **`characters` は 31 行。`moves` が 1 行以上ある `character_id` は 19。⇒ 残り 12。**
> - **seed 済み 19**＝`ryu` `ken` `ingrid` `c_viper` `dhalsim` `terry` `guile` `lily` `kimberly` `juri` `mai` `zangief` `manon` `m_bison` `rashid` `jamie` `luke` `marisa` `jp`
> - **未 seed 12**＝`aki` `akuma` `alex` `blanka` `cammy` `chun_li` `dee_jay` `e_honda` `ed` `elena` `sagat` `yasmine`
> - **★「差 2」の正体**——**旧記録の「seed 済み 17」は `manon` と `c_viper` / `dhalsim` の扱いを数え違えていた。★開発者の列挙 12 は `blanka` と `yasmine` が抜ける一方で、既に seed 済みの `c_viper` / `dhalsim` を含んでいた。⇒ 合っていなかったのは引き算ではなく集合の中身である。**
>
> **⇒ 本節は「数え直して差を説明する」から「上記と一致するかを確かめる」へ変わる。** **★★それでも数え直すこと**——**`M25-RESEARCH-01` の baseline 以後に `dhalsim` の CSV が main へマージされている**（開発者・2026-09-02）。**⇒ §2.1-4 の CSV ファイル数は動いている可能性が高い。**
>
> **★以下の表の「17 / 14」は起票時の設計卓の把握であり、上記により失効している。★数え直しの出発点としてのみ残す。**

**★★設計卓が把握している数字が食い違っている。⇒ 差を説明すること。**

| 出所 | 数 |
|---|---|
| ボード §1.3 | **全 31 キャラ ／ seed 済み 17 ／ 残り 14**（2026-08-01 実績。**★キャラ数のみ 2026-08-07 に同期＝`D-213`**） |
| followup「残キャラ網羅表」 | **seed 済み 17 の顔ぶれ**＝**第一波 9**〔terry / guile / lily / ingrid / kimberly / juri / ken / mai / zangief〕**＋ ryu ＋ manon ＋ 第三波 6**〔m_bison / rashid / jamie / luke / marisa / jp〕 |
| **開発者の列挙（2026-08-30）** | **12**——**aki / akuma / cammy / chun_li / e_honda / ed / elena / alex / dee_jay / sagat / c_viper / dhalsim**（**★`c_viper` / `dhalsim` は入力中でまもなく完了の見込み**） |

**⇒ 17 ＋ 12 ＝ 29。全 31 との差 2 が説明できない。**

| # | 確かめること |
|---|---|
| **★★1** | **`characters` テーブルに現在何行あるか**（**母数**）。**そのコードの全数を列挙すること** |
| **★★2** | **`moves` が 1 行以上ある `character_id` の全数**（＝**実際に seed 済みのキャラ**）。**★「17」を数え直す** |
| **★★3** | **上記 2 の差集合＝未 seed キャラの全数と顔ぶれ。★開発者の列挙 12 との差を明示すること** |
| **4** | **`character_data/*.csv` の現在のファイル数と、対応するキャラコード**（**★「17 ファイル」を数え直す**） |
| **★5** | **`yasmine` が `characters` に在るか。moves は在るか**（**★2026-08-04 のアップデートで追加されたキャラ**） |
| **★6** | **差の 2 キャラが何であるか。`characters` に在って開発者の列挙にも seed 済みにも無いものを名指しすること** |

### 2.2 ★★seed 波 1 回あたりの工程の全数（**本調査の主題**）

| # | 確かめること |
|---|---|
| **★★1** | **過去 2 波が実際に消費したマイグレの連番と、1 本ずつの内容**（`M14-03d`＝`000043`〜`000048` ／ `M14-03e`＝`000053`〜`000059` と設計卓は把握している。**★数え直すこと**）。**★「前提マイグレ 2 本 ＋ 本体」という段構成が現在も同じか** |
| **★★2** | **`internal/seedgen` が現在生成するものの全数**（**★CSV → SQL の変換で何が出るか。★過去 2 波の時点から増えたものは何か**） |
| **★★3** | **`moves.is_projectile` の backfill が現在も要るか。要るなら何を見て値を決めるか**（followup が「★必須」と書いている。**★`internal/seedgen` が生成するようになっていれば不要**） |
| **★★4** | **移動 system move の `total` の backfill が現在も要るか**（同上。**★`dash_forward` / `dash_back` / `jump_*` の 5 code**） |
| **5** | **`custom_states` の `show_delta` 付与が要るキャラが、残キャラの中に何人いるか**（**母数**。followup は「int custom_state def を持つ 4 キャラ（Mai/Lily/Juri/Kimberly）」と書いているが、**その 4 人はすべて seed 済みである。⇒ 残キャラに該当者がいるかは別に数えること**） |
| **★★6** | **★★M20 のエイリアス生成規則の再適用に何が要るか**（`m20-contract` §1.1-3＝**「生成規則は seed 波が再適用できる形で作る」**）。**★プリセットは何本あり、そのうち seed 波ごとに再適用が要るのはどれか。★再適用の手順は在るか（スクリプト／マイグレ／手作業のどれか）** |
| **★★7** | **移動 9 種 seed（`000025`）が当たっていないキャラを遡って埋める工程が現在も要るか**（followup＝**「同マイグレは適用時点で存在した 12 キャラにしか INSERT していない」**）。**★第二波・第三波はこれをどう処理したか** |
| **8** | **`move_derivations` の明示削除責務**（followup `move-derivations-explicit-delete-on-reseed`）**が seed 波の down マイグレに要るか** |
| **★★9** | **★★上記以外に、`M18` / `M19` / `M20` / `M21` / `M22` / `M23` / `M24` の期間に積まれた「seed 波で埋める必要のある列・テーブル」が在るか**（**母数付き。★これが本調査の核心である**）。**★探し方＝`moves` / `characters` / `custom_states` / `move_derivations` および関連テーブルの列を、第三波の時点（`000059`）と現在（`000080`）で差分を取る**〔**★2026-09-02 是正。起票時は「現在（`000079`）」と書いていた**。**★disk 末尾は実査すること**——**設計卓は `migrations/` を読めない**〕 |
| **10** | **各工程が「1 波につき 1 回」なのか「1 キャラにつき 1 回」なのかの別**（**★所要の見積りが桁で変わる**） |

### 2.3 ★失効している前提が無いか

| # | 確かめること |
|---|---|
| **★★1** | **followup `combo-csv-io-ken` の前提が失効していないか**——**同項は「`web/e2e/combo-csv-io.spec.ts` は ken の moves 件数 >0 を断定するが ken の moves seed は存在しない」と書いている。★しかし ken は第一波 9 に入っている。⇒ 記述が失効している疑いが強い。実物で確かめること** |
| **2** | **followup `character-down-migrations-orphan-user-combos`**（`000024.down` / `000053.down` が利用者の作ったコンボを孤児にする）**が、第四波の down にも当てはまるか** |
| **3** | **followup「未 seed 波の必須前提」群のうち、既に解消しているものが在るか**（**★1 件ずつ判定すること。★解消していたらそう報告する**） |
| **★4** | **`c_viper` / `dhalsim` の「攻撃技 0 の仮登録」が現在も在るか。UNIQUE 衝突で起動不能になる経路が現在も成立するか**〔**★★2026-09-02 追記＝`M25-RESEARCH-01` §4.3 は両者を「seed 済み 19」に数えている。⇒ 仮登録ではなくなっている疑いが強い。★実物で確かめること。★成立しないなら「必ず最終波」の制約は失効しており、その旨を報告すること**〕（ボード §1.3＝**「必ず最終波」**）。**★成立しないなら、最終波の制約は失効している** |
| **5** | **`precheck_seed_data` スキル**（`.claude/commands/precheck_seed_data.md`）**が参照するファイルが現在も実在するか**（**★同スキルは `internal/seedgen/generate_m2002.go` 等を参照している。★`check-doc-refs.sh` は武装中の設計卓では判定できず SKIP になっている**） |
| **6** | **`pre-dist-e2e-clean`**（配布前にクリーン DB で全 E2E を回す）**が、第四波の完了条件に含まれるべきか判断する材料**（**★判断はしない。事実だけ出す**） |

### 2.4 見積りの材料

| # | 確かめること |
|---|---|
| **1** | **過去 2 波の実所要**（**★完了報告に記録が在れば引く。無ければ「記録が無い」と書く**） |
| **2** | **1 キャラあたりの moves 件数の中央値と最大値**（seed 済み 17 キャラの実測） |
| **3** | **第四波を 1 波でやる場合のマイグレ本数の見込み**（**★過去 2 波の 1 キャラあたり本数から算出。★「見込み」であることを明記すること**） |
| **★4** | **1 波を何キャラで切るかの材料**——**マイグレ 1 本あたりの行数の上限が実務上在るか ／ 1 波の適用に要する時間 ／ down の複雑さがキャラ数に比例するか** |

---

## 3. 出力

`docs/progress/M14-RESEARCH-03-report.md` へ、上記 §2.1〜§2.4 の順に**事実だけ**を書く。

| # | 書き方の規約 |
|---|---|
| **★★1** | **すべての件数に母数を付ける。★「N 件」とだけ書かない** |
| **★★2** | **「無い」と書くときは、どう探したかを書く**（playbook §4.35＝**「無いこと」を示す検索は、探す語を誤ると簡単に 0 件になる**） |
| **★★3** | **本書が挙げた数字**〔31 / 17 / 14 / 12 / 5 種類 / 000043〜000048 / 000053〜000059 / 17 ファイル〕**は、1 つずつ数え直した結果を併記すること。★合っていたら「合っていた」と書く** |
| **4** | **判断を書かない。★「〜すべき」「〜が望ましい」を書かない** |
| **5** | **★確かめられなかったものは「確かめられなかった」と書く。★推測で埋めない** |
| **6** | **実行したコマンドと出力を貼る。★パイプで切り詰めない** |

---

## 4. やらないこと

| # | やらないこと |
|---|---|
| **★★1** | **コードの変更**（**★差分 0 が完了条件である**） |
| **★★2** | **CSV の作成・修正**（**★手入力 CSV は開発者の作業である**） |
| **★★3** | **マイグレの作成**（**★次に払い出す番号は `000081` のまま動かさない**〔2026-09-02 是正〕） |
| **★4** | **判断・提案**（**★どの順で投入するか、何波に割るかは設計卓と開発者が決める**） |
| **5** | **`docs/design/` の編集**（`CLAUDE.md` §8） |
| **6** | **`followup-backlog.md` の編集**（`D-382`。**★候補として報告するだけ**） |

---

## 5. 完了条件

1. **§2.1〜§2.4 のすべてに答えが在る**（**★確かめられなかったものは「確かめられなかった」と書く**）。
2. **★★§2.1-6 の「差の 2 キャラ」が名指しされている**（**★できなければ、なぜできないかを書く**）。
3. **★★§2.2-9（`M18` 以降に積まれたもの）が母数付きで報告されている**（**★本調査の核心**）。
4. **★§2.3 の失効判定が 1 件ずつ行われている。**
5. **すべての件数に母数が付いている。**
6. **`git diff` が空である**（**★差分 0**）。
7. **`docs/progress/progress-log.md` へ追記されている**（`CLAUDE.md` §8）。

---

## 6. 参照ドキュメント

| ID | パス |
|---|---|
| followup | `docs/handover/followup-backlog.md`（**★「未 seed 波の必須前提」群**） |
| m20-contract | `docs/process/m20-contract.md` §1.1-3 ／ 除外表 |
| M14-overview | `docs/instructions/M14-overview.md` §3 |
| 過去 2 波の指示書 | `docs/instructions/M14-03d-second-wave-manon.md` ／ `M14-03e-third-wave-code-quality.md` |
| code-facts | `docs/handover/code-facts.md` |
| CLAUDE.md | §5 / §7 / §8 / §10 |

---

## 7. 開発者への確認事項

**★無し。** 本調査は事実の列挙のみであり、判断を含まない。

**★★ただし §2.1-6 の「差の 2 キャラ」は、開発者しか知らない事情**〔未リリース ／ フレームデータ未公開 等〕**で説明が付く可能性がある。⇒ 実物で説明が付かなければ、そう書いて設計卓へ返すこと。**

---

*以上*
