# 指示書 M36-01: 配布アーカイブの組み立てとチェックサム（射程 1 ＋ 2）

| 項目 | 内容 |
|------|------|
| 文書ID | M36-01 |
| バージョン | **1.3.0**（2026-09-15・**★§4.1c へ 1 ブロック追記**＝`D-888`〔**`M32-03` が `dist-readme.txt` を既に変更済みである。⇒ 上書きせず差分で足すこと**〕**。★射程・段取り・完了条件は不変**） ／ **1.2.0**（2026-09-15・**★★Plan Mode の問い 2 件へ答えた**＝`D-887`〔**危険 4.1c＝`dist-readme.txt` は射程内である** ／ **危険 4.1d＝macOS の名前の非対称は揃えない**〕**。★射程・段取り・完了条件は 1 文字も変えていない**） ／ **1.1.0**（2026-09-15・**★★投入可と判定するにあたり 2 点を明確化した**＝`D-886`〔**版ゲート 3b＝`images/` はディレクトリが在れば足りる** ／ **危険 4.1b＝`manual/` の中身をファイル名・件数で固定しない**〕**。★射程・段取り・完了条件は 1 文字も変えていない**） ／ **1.0.0**（2026-09-13・起票） |
| 作成日 | 2026-09-13 |
| 作成者 | 設計担当 Claude（設計卓） |
| 対象マイルストーン | **`M36`**（リリースパッケージング）。上位＝`docs/instructions/M36-overview.md` v1.2.0 |
| 推奨モデル | **標準実装**（実モデル名は `docs/human-notes/model-allocation.md`） |
| Plan Mode | **必須**（`CLAUDE.md` §9＝**配布物の契約に触れる**） |
| レビュー | **必要**（**★チェックリスト＝`docs/instructions/reviews/M36-01-review-checklist.md` v1.0.0・2026-09-13 発行済み**） |
| CHANGE 消費 | **1 本の見込み**（`DES-002` §11.2。**★自採番しない**＝`D-293`） |
| マイグレ消費 | **★0 本。⇒ 消費する形になったら止めて請求する** |
| 前提サブ | **`M32-02` の着地**（**★`manual/` の中身が空だと同梱の意味が薄い**） |
| 射程 identity | **2 件**——射程 1（zip / tar.gz の組み立て）／ 射程 2（SHA-256 チェックサム） |
| 配置 | `docs/instructions/M36-01-release-archive-and-checksums.md` |
| 出力 | `docs/progress/M36-01-completion-report.md` |

---

## 0. この指示書の特殊性

### 0.1 ★★★組む仕組みは 1 つも存在しない

**★開発者の逐語**（2026-09-12）**＝「リリースの zip はいまは作っていないです。GitHub にも自動で作るような仕組みはないはずです」。**

**⇒ `DES-002` §11.2 が定める配布物の構成は、いままで一度も実体になっていない。★本サブが最初の 1 本である。**

**★★だから「既存の仕組みを直す」ではなく「無いものを作る」。⇒ ただし §0.2 の否定確認を先に通すこと。**

### 0.2 ★★★「無い」と断定する前に探す

**★`E-117`。⇒ 開発者は「無いはず」と述べており、断定していない。**

**★★段 1 で 3 点を実物で確かめること**〔`Makefile` の全ターゲット ／ `.github/workflows/` の全ワークフローと契機 ／ リリースを打つ手順の不在〕**。⇒ 在ったら、それを土台にする。**

### 0.3 ★★★「組んだ」と「検証できる」は別である（**overview §4.1**）

**★zip を作るだけなら数行で済む。⇒ 本 MS の値打ちは、`B1`**〔コード署名〕**を諦めた代わりに「このバイナリはこのコミットから作られた」を*検証可能にする*ことにある。**

**★★本サブが担うのはその手前の 2 段＝*中身が正しいこと*と*壊れていないこと*である。⇒ 来歴の署名は `M36-02`。**

**★★★だから破壊確認が完了条件に入る**〔§5-3〕**——中身を 1 つ抜くと検査が落ちること。⇒ 「組めた」だけで通さない。**

### 0.4 ★★同梱物は「2 点」ではない

**★実物＝`.github/workflows/nightly-crossbuild.yml` は「中身 ＝ `tacpendium.exe` ＋ `README.txt`」と書いている**（`followup` の `release-zip-missing-license-files`）**。⇒ 2 点だけである。**

**★★`DES-002` §11.2 は 3 点を定める**〔`exe` ／ `README.txt` ／ `manual/`〕**。⇒ 本サブは §11.2 に合わせる。**

**★★★ライセンス関係のファイルは*判断が要る*。⇒ 勝手に入れない・勝手に落とさない**〔§2.4〕**。**

### 0.5 ★★nightly の artifact を配布物へ格上げしない（**overview §4.3**）

**★`CHANGE-129` は「CI の artifact は配布物ではない」と定めている。**

**★★契機が違う**〔nightly＝定時 ／ リリース＝タグ〕**。⇒ 混ぜると `CHANGE-129` の改訂が要る。★本サブは nightly を書き換えない。**

**★★★ただし本サブは CI からリリースを打つところまでは行かない**〔射程 6＝`M36-02`〕**。⇒ 本サブの成果は「コマンド 1 本で組める」ことである。**

### 0.6 ★★3 OS すべてを作る

**★開発者裁定**（`D-855`・逐語＝「3.作る」）**。⇒ Windows の zip ／ macOS・Linux の tar.gz。★非公式対応でも `manual/` は入れる**（`DES-002` §11.2）**。**

### 0.7 ★★着手前の版ゲート（**`M-169` の対処**）

| # | 確かめること | 満たさないとき |
|---|---|---|
| **1** | `docs/instructions/reviews/M36-01-review-checklist.md` が存在すること | **★止まって設計卓へ請求する** |
| **2** | **`DES-002` が 1.93.0 以上であること**（**★§11.2 に `manual/` と `CHANGE-192` が在るか**） | **★同上。⇒ 無ければ古い** |
| **3** | **`M32-02` が着地していること**（**★`docs/usermanual/` に本文が在るか**） | **★★止まって請求する。⇒ 空の `manual/` を同梱しても意味が無い** |
| **★★3b** | **★★★2026-09-15 明確化＝`images/` は*ディレクトリが在れば足りる*。⇒ 画像ファイルは揃っていなくてよい**（`D-886`） | **★★撮影は開発者の手番であり `M32-03` の後である。⇒ 画像の不在を理由に止まらないこと。★本サブの成果は「コマンド 1 本で組める」ことであってリリースそのものではない** |
| **4** | **`exe` の名前が `tacpendium-windows-amd64.exe` であること**（`CHANGE-191`） | **★食い違ったら報告する。⇒ 勝手に改名しない** |
| **5** | `git log --oneline -5` で枝元を報告へ書く | — |

---

## 1. 前提として読むもの

| # | 資料 | 読む箇所 |
|---|---|---|
| **★★★1** | `docs/design/02-architecture.md` | **§11.2**（**★配布物の構成。本サブが実体にする当のもの**）／ §11.3（CI 運用方針）／ §11.1（サポート OS） |
| **★★2** | `docs/instructions/M36-overview.md` v1.2.0 | **§1**（射程 6 件）／ **§2**（着手前の実査 4 点）／ **§4**（危険 4 件）／ §6（完了の観点） |
| **★3** | `docs/change-notes/CHANGE-192-notification.md` | **§3**（**★zip を誰が組むかの実査項目。本サブがその答えである**） |
| 4 | `docs/change-notes/CHANGE-191-notification.md` | `exe` 名の統一 |
| 5 | `docs/process/parallel-board.md` | **`D-850`**（`P-57`＝zip 同梱）／ **`D-851`**（仕組みの不在 ＋ `B1` 後回し）／ **`D-855`**（3 OS 作る）／ `D-293` |
| 6 | `docs/handover/followup-backlog.md` | **`release-zip-missing-license-files`**（**★§2.4 の出発点**）／ `release-packaging-does-not-exist` ／ `usermanual-no-reachable-path` |
| 7 | `docs/human-notes/future-notes/combmgr-license-strategy-outline.md` | **§10.6**（射程 2〜6 の出所） |

---

## 2. やること

### 2.1 ★★★段 1 — 実査（**★コードを書く前に済ませ、報告へ書く**）

| # | 確かめること | なぜ |
|---|---|---|
| **1** | **`Makefile` の全ターゲット**（とくにクロスビルドの出力先とファイル名） | **★射程 1 の土台。⇒ 実物で確かめる** |
| **2** | **`.github/workflows/` の全ワークフローと契機** | **★nightly のほかに何が在るか。⇒ `CHANGE-129` との境界を引くため** |
| **3** | **★★リリースを打つ手順が本当にどこにも無いこと**（**否定形の確認**） | **`E-117`。⇒ 在ったら土台にする** |
| **4** | **`docs/usermanual/` の実体**（本文 ／ `images/` の枚数） | **★同梱する対象そのもの。⇒ 空なら §0.7 のゲート 3 で止まる** |
| **5** | **リポジトリ直下のライセンス関係ファイルの全数**（`REUSE.toml` が指すもの） | **★§2.4 の判断材料。⇒ 数えるだけで、入れるかは決めない** |

**★数値の隣に「実測」「算出」を書き分けること**（計測点 `M-157`）**。**

### 2.2 ★★★段 2 — 組み立て（**射程 1**）

| # | やること |
|---|---|
| **1** | **★コマンド 1 本で 3 OS ぶんを組めるようにする**（`Makefile` のターゲット 1 本を想定。**★形は実装判断でよい**） |
| **2** | **★Windows は `.zip`、macOS / Linux は `.tar.gz`**（`DES-002` §11.2） |
| **3** | **★中身は `exe`**〔または各 OS の実行ファイル〕**＋ `README.txt` ＋ `manual/`**〔本文 ＋ `images/`〕 |
| **4** | **★★`manual/` は 3 OS すべてに入れる。⇒ 非公式対応でも落とさない** |
| **5** | **★組み立ての出力先を 1 か所に決める。⇒ `.gitignore` へ入れる**（**★成果物をコミットしない**） |
| **6** | **★★nightly のワークフローを書き換えない**（§0.5） |

**★★★`README.txt` の文面は実態と一致させること。⇒ 現在の文面は「本 zip の中身は 2 点だけ」と書いている**（`followup`）**。★3 点になるなら直す。⇒ 直したことを報告へ書く。**

### 2.3 ★★段 3 — チェックサム（**射程 2**）

| # | やること |
|---|---|
| **1** | **★各アーカイブの SHA-256 を出す** |
| **2** | **★形式は 1 つに決める**（**★`sha256sum` で検証できる形が素直。⇒ 採った形と理由を報告へ書く**） |
| **3** | **★★利用者が検証するコマンドを `README.txt` か説明書に 1 行書く。⇒ 出すだけで使い方が無いと意味が無い** |

### 2.4 ★★★段 4 — ライセンス関係ファイルの同梱（**★判断を仰ぐ段である**）

**★★★ここは実装で決めないこと。⇒ 現状は zip に `LICENSE` / `DATA-LICENSE.md` / `NOTICE` / `REUSE.toml` が 1 つも入っていない。**

**★★本体を `AGPL-3.0-or-later` にしたことで**（`CHANGE-157`）**、同梱物を増やすかの判断が新たに要るようになった。⇒ 設計卓も製造も法務を断定していない。**

| # | やること |
|---|---|
| **1** | **★段 1-5 で数えた全数を報告へ出す** |
| **2** | **★★同梱する / しない を*選べる形*に作る。⇒ ファイル一覧を 1 か所に持ち、足すのが 1 行で済むようにする** |
| **3** | **★★★既定は「現状のまま入れない」。⇒ 勝手に入れない** |
| **4** | **★設計伝達レポート §4 へ「入れるべきか」を判断事項として上げる** |

**★理由＝法務の断定は開発者の手番である**（`roles-and-routing` のハード列）**。⇒ ただし後から入れられない作りにしないこと。**

### 2.5 ★段 5 — 設計書の追随

**★`DES-002` §11.2 は「zip / tar.gz を組む仕組みは存在しない」と書いている。⇒ 本サブが着地するとその一文が失効する。**

**★★CHANGE 原稿を設計伝達レポート §4 へ書くこと。⇒ 設計書本体は書き換えない**（`CLAUDE.md` §8）**。★自採番しない**（`D-293`）**。**

---

## 3. やらないこと

| # | 内容 | 理由 |
|---|---|---|
| **1** | **★★★CI からリリースを打つこと**（射程 6） | **`M36-02` の射程。⇒ 本サブは「コマンド 1 本で組める」までである** |
| **2** | **★★attestation / SBOM / タグ署名**（射程 3〜5） | **同上** |
| **3** | **★★nightly のワークフローを書き換えること** | **§0.5。⇒ `CHANGE-129` の改訂が要る形になる** |
| **4** | **★★★ライセンス関係ファイルを勝手に入れる / 落とすこと** | **§2.4。⇒ 法務の断定は開発者の手番である** |
| **5** | **★コード署名**（`B1`） | **★後回しで決着**（`D-851`） |
| **6** | **★AV ベンダーへの誤検知申請**（`B2`） | **★開発者の手番** |
| **7** | **★公開リポジトリの同期スクリプト** | **`P-46` の射程。⇒ ソースの話であり配布物の話ではない** |
| **8** | **★実際にリリースを打つこと** | **★開発者の手番** |
| **9** | **★成果物をコミットすること** | **§2.2-5。⇒ `.gitignore` へ入れる** |
| **10** | **★★★`docs/handover/followup-backlog.md` を編集すること** | **`D-838`。⇒ 停止時記録も設計伝達レポート §4 へ原稿として書く** |
| **11** | **★設計書本体を書き換えること** | **`CLAUDE.md` §8** |

---

## 4. ★★危険（**先に読むこと**）

### 4.1 ★★★最大の危険＝「組めた」で閉じること

**★zip ができることと、中身が正しいことは別である。⇒ `manual/` が空でも、`README.txt` が古くても、zip は問題なくできる。**

**★★★破壊確認を置くこと**〔§5-3〕**——中身を 1 つ抜くと検査が落ちること。⇒ 落ちないなら、その検査は何も見ていない。**

### 4.1b ★★★【2026-09-15 追加＝`D-886`】`manual/` の中身をファイル名で固定すること

**★★`M32-03` が同じ `docs/usermanual/` を*編集*している**（走行中）**。⇒ 本サブは同ディレクトリを*読む*だけであり書き込みは衝突しない。★しかし検査は衝突しうる。**

**★★★`M32-03` は同ディレクトリへ*新しいファイルを 1 本足す***〔クイックスタート〕**。⇒ 図の置き場も増える。★本サブの検査が「`manual/` に在るファイル名の一覧」や「ファイル数」を固定すると、`M32-03` の着地で赤になる。**

| やること | |
|---|---|
| **同梱** | **ディレクトリごと入れる。⇒ ファイルを 1 つずつ列挙しない** |
| **検査** | **★*あるべきものが在るか*で見る**〔本文の HTML が在る ／ `images/` が在る〕**。⇒ *それしか無いか*では見ない** |
| **破壊確認**（§5-3） | **★抜くのは「あるべきもの」にすること。⇒ 数を 1 つ減らして落ちることを見る形にしない** |

**★★これは `M32-03` のためだけの配慮ではない。⇒ 説明書は今後も章が増減する。★数で縛ると、そのたびに本サブの検査が落ちる。**

### 4.1c ★★★【2026-09-15 追加＝`D-887`】`docs/usermanual/` の**どこまでが射程か**

**★★危険 §4.1b と混同しないこと。⇒ 同ディレクトリは*一様ではない*。**

| ファイル | 射程 | なぜ |
|---|---|---|
| **`dist-readme.txt`** | **★★射程内。編集する** | **`README.txt` の正本である。⇒ 「本 zip の中身は 2 点だけ」が同梱物 3 点で失効している**〔§0.4〕**。★チェックリスト A-6 が修正を要求している** |
| **`tacpendium-readme.html`** | **★射程外。読むだけ** | 説明書の本文。**`M32-03` の面である** |
| **`images/`** | **★射程外** | 撮影待ち。**開発者の手番** |
| **クイックスタート** | **★射程外** | **`M32-03` が作る。⇒ 着手時には存在しないことがある** |

**★★★`README.txt` は生成物である。⇒ 直すのは正本**〔`dist-readme.txt`〕**であって生成物ではない。★生成して `scripts/check-dist-readme.sh` で照合すること。**

**★★`dist-readme.txt` は `M32-03` も触りうる面である。⇒ 本サブの手番とした**（`D-887`）**。**

> **★★★【2026-09-15 追記＝`D-888`】`M32-03` は*既に同ファイルを変更している***（開発者報告）**。⇒ 「本サブが最初に触る」という前提は成り立たない。**
>
> **★★★着手時に現物を読むこと。⇒ A-6 が求めているのは「`README.txt` の文面が生成物と一致するか」であって「自分が直したか」ではない。★既に直っている行は、そのままでよい。**
>
> **★★上書きしないこと。⇒ 足す差分だけを当てる。★全文を書き直すと `M32-03` が足した行**〔クイックスタートへの導線など〕**が黙って消える。**
>
> **★★★消えても検査は緑になる。⇒ `check-dist-readme.sh` が見るのは*正本と生成物の一致*であって*内容の充足*ではない。★だから機械では捕まらない。**
>
> **★マージ時のコンフリクトは開発者が解消する**（開発者裁定 2026-09-15）**。⇒ 本サブはコンフリクトを避けるために遠慮しなくてよい。★避けるべきは*上書きによる消失*だけである。**

### 4.1d ★★★【2026-09-15 追加＝`D-887`】macOS の名前の非対称は**揃えない**

| 面 | 名前 | 出所 |
|---|---|---|
| **アーカイブ** | **`tacpendium-macos-arm64.tar.gz`** | `DES-002` §11.2 |
| **中のバイナリ** | **`tacpendium-darwin-arm64`** | `Makefile:74`（`-o dist/tacpendium-darwin-arm64`） |
| **`README.txt` の記載** | **`tacpendium-darwin-arm64`** | `README.txt:14` |

**★★★どちらにも揃えないこと。⇒ 設計書のとおりのアーカイブ名で組み、中のバイナリは `Makefile` が出す名前のまま入れる。**

**★理由 2 つ＝(1) 本サブの射程は「組み立て」であって「改名」ではない**〔版ゲート #4 の「勝手に改名しない」と同じ趣旨が macOS 側にも効く〕**／ (2) 配布物の名前は `CHANGE-191` で設計書に固定された面であり、動かすには CHANGE が要る。**

**★★★ただし黙って通さないこと。⇒ 非対称を完了報告へ*実測で*書き、CHANGE 請求として設計伝達レポートへ出す**〔§6〕**。★本サブは非対称を*作った*のではなく*可視化した*。⇒ 黙って揃えると、設計卓が判断する機会が消える。**

**★設計卓の把握**（2026-09-15 実査）**＝`Makefile` は 3 つとも GOOS 名で出しており**〔windows / darwin / linux〕**内部では一貫している。⇒ `windows` と `linux` は GOOS と利用者向けの語がたまたま一致し、macOS だけが露出した。★原因は「命名規則が 2 つ並んでいる」ことである。**

### 4.2 ★★2 番目＝`CHANGE-129` との境界を越えること

**★nightly の artifact を配布物へ格上げしない。⇒ 契機が違う**〔定時 / タグ〕**。**

**★★迷ったら nightly を触らないほうを採る。⇒ 触る必要が出たら止めて請求する。**

### 4.3 ★★3 番目＝ライセンスの判断を実装で下すこと

**★§2.4。⇒ 「入れておけば安全だろう」も「いまのままでいいだろう」も、どちらも実装が下してよい判断ではない。**

**★★★選べる形に作って、判断を上げること。**

### 4.4 ★4 番目＝`exe` 名を勝手に変えること

**★`CHANGE-191` が `tacpendium-windows-amd64.exe` に統一している。⇒ 食い違っていても勝手に直さず、報告する。**

### 4.5 ★★5 番目＝GitHub の機能を記憶で書くこと（**overview §4.4**）

**★本サブは射程 3〜6 に触れないため当面は影響しないが、`Makefile` から CI へ寄せる形を採るなら効く。**

**★★★具体的な設定は*現時点の公式ドキュメント*で確かめること。⇒ `/ask_external_ai` を使う手がある。**

---

## 5. テスト

| # | 対象 | 内容 |
|---|---|---|
| **5-1** | **★★組み立て** | **3 OS ぶんが 1 コマンドで出ること。⇒ それぞれの中身の一覧を報告へ出す** |
| **5-2** | **★★中身の検査** | **`exe` ／ `README.txt` ／ `manual/` 本文 ／ `manual/images/` が在ること。★`manual/` が空でないこと** |
| **5-3** | **★★★破壊確認** | **中身を 1 つ抜くと §5-2 の検査が落ちること。⇒ 3 点それぞれで見る** |
| **5-4** | **★チェックサム** | **出た値が `sha256sum` 系のコマンドで検証できること。⇒ 実際に検証した記録を報告へ** |
| **5-5** | **★★非退行** | **nightly のワークフローが 1 バイトも変わっていないこと**（§0.5） |
| **5-6** | 全数 | **`go test ./...` ／ `pnpm test` ／ `make e2e`（全数）** |
| **5-7** | **★機械検査** | **`bash scripts/check-doc-refs.sh` ／ `bash scripts/check-migration-license.sh`**（**★ライセンス面に触るため**） |

---

## 6. 完了条件

| # | 条件 |
|---|---|
| **1** | **★★★`DES-002` §11.2 の構成どおりの zip / tar.gz が、コマンド 1 本で 3 OS ぶん作れる** |
| **2** | **★★中身が実物で確かめられている**（`manual/` が空でない ／ `README.txt` が生成物と一致する） |
| **3** | **★★★破壊確認が在る＝中身を 1 つ抜くと検査が落ちる**（§5-3） |
| **4** | **★チェックサムが出て、実際に検証した記録が在る** |
| **5** | **★★段 1 の実査 5 点が報告に在る。⇒ とくに 3**〔リリース手順の不在〕**は否定形の確認として書かれている** |
| **6** | **★★★ライセンス関係ファイルの全数と、同梱の判断事項が設計伝達レポート §4 に在る**（§2.4。**★勝手に入れていない**） |
| **7** | **★nightly のワークフローが 1 バイトも変わっていない** |
| **8** | **★成果物をコミットしていない**（`.gitignore`） |
| **9** | **★★着手前の版ゲート 5 点を通した**（§0.7） |
| **10** | **★`docs/progress/progress-log.md` へ 1 行追記した**（`CLAUDE.md` §8） |
| **11** | **★CHANGE 原稿を設計伝達レポート §4 へ書いた。⇒ 自採番していない**（`D-293`） |
| **12** | **★完了報告を `bash scripts/check-md-emphasis.sh <file>` で見た**（`D-775`） |

---

## 7. ★開発者への確認事項

| # | 事項 | 設計卓の暫定案 |
|---|---|---|
| **1** | **ライセンス関係ファイルを zip へ入れるか**（§2.4） | **★★★設計卓は断定しない。⇒ 法務の判断であり開発者の手番である**（`roles-and-routing` のハード列）**。★製造は「選べる形」に作り、全数を出して判断を仰ぐ** |
| **2** | **チェックサムの公開の仕方** | **★★案＝アーカイブと同じ場所に `.sha256` を並べる。⇒ リリースから CI へ寄せるのは `M36-02` であるため、本サブは*生成できる*ところまででよい** |
| **3** | **組み立ての置き場**（`Makefile` か スクリプトか） | **★実装判断でよい。⇒ 採った形と理由が報告に在れば足りる。★★ただし `M36-02` が CI から呼ぶため、CI から呼べる形にしておくこと** |

---

## 8. 参照ドキュメント

| ID | パス | 使う箇所 |
|----|------|---------|
| DES-002 | `docs/design/02-architecture.md` | **§11.2 ／ §11.3 ／ §11.1** |
| overview | `docs/instructions/M36-overview.md` | **§1 ／ §2 ／ §4 ／ §6** |
| CHANGE-192 | `docs/change-notes/CHANGE-192-notification.md` | **§3** |
| CHANGE-191 | `docs/change-notes/CHANGE-191-notification.md` | `exe` 名 |
| ボード | `docs/process/parallel-board.md` | **`D-850` / `D-851` / `D-855` / `D-293` / `CHANGE-129`** |
| followup | `docs/handover/followup-backlog.md` | **`release-zip-missing-license-files`** ほか |
| — | `docs/human-notes/future-notes/combmgr-license-strategy-outline.md` | §10.6 |
| 規約 | `CLAUDE.md` | §8 / §9 / §10 / §11 |

---

*以上、指示書 M36-01 **v1.3.0**。* **★★★§4.1 を最初に読むこと**——**zip ができることと、中身が正しいことは別である。⇒ 破壊確認が無ければ、その検査は何も見ていない。** **★★次に重いのは §2.4 である**——**ライセンスの判断を実装で下さないこと。**
