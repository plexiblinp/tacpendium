# 指示書 M24-08: `queryKey` の統一 ＋ M24 の繰越 4 件

| 項目 | 内容 |
|------|------|
| 文書ID | M24-08 |
| バージョン | 1.0.0 |
| 推奨モデル | **高精度実装**（**★★FE データ層の全域**〔84 箇所 / 39 ファイル〕**を単独で触る。しかも契約が 5 件当たっている**。実モデル名は `docs/human-notes/model-allocation.md` が正本） |
| Plan Mode | **必須**（**★★実査 16 件を Plan Mode に含める**。CLAUDE.md §9＝**全体に影響するリファクタ ＋ データ移行を伴う変更**） |
| 機械レビュー | 必須（別チェックリスト: `M24-08-review-checklist.md`） |
| 参照してよい先行資料 | `DES-002` §4.2 / §7.5 / §11.3 ／ `DES-005` §5.7 / §5.15 ／ `m20-contract` §5 ／ `m21-contract` §5 ／ `docs/handover/followup-backlog.md` §AU / §AV / §AW ／ `docs/handover/code-facts.md`（**★★§2-2 の `queryKey` 64 行は誤り。実測 84 箇所**＝followup `code-facts-querykey-undercount`） ／ ledger `M24-RESEARCH-01` §5.2（**★他サブの指示書・チェックリストは含めない**＝D-217） |
| 並列性 | **★★並列にしない**（`M24-overview` §5＝**repo 全体の FE データ層を単独所有する期間が要る**。**★宣言できない**〔全域に触る〕） |
| 依存 | **★★M23 の全サブ ＋ M24 の全レーンの完了**（**済**。`M24-overview` §5 の先行条件） |
| CHANGE | **`CHANGE-148` v1.0.0**（**設計卓が起票済み。製造は番号を消費しない**）。影響設計書＝**`DES-002` §4.2 / §11.3**（主対象） ／ **`DES-005` §5.15**（主対象） ／ **`DES-002` §7.5**（条件付き） ／ **`DES-005` §5.7**（条件付き） |
| マイグレ | **★★1 本を消費する見込み**（`preset_aliases.alias_text` の UPDATE）。**★次に払い出す番号は `000080`** |
| 想定所要時間 | 420〜540 分（**★★第 1 部だけで 300〜360 分の見込み**） |
| 作成者・作成日 | 設計担当 Claude（設計卓・M24 期）/ 2026-08-30 |

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| 1.0.0 | 2026-08-30 | 初版（**D-619**） |

---

## 1. 背景と目的

### 1.1 ★★本サブは 2 部構成である。**順序を守ること**

| 部 | 内容 | 成果物 |
|---|---|---|
| **★★第 1 部** | **`CO-009`＝`queryKey` の object・flat 統一**（**84 箇所 / 39 ファイル**） | **★★E2E 非回帰。挙動を 1 つも変えない** |
| **第 2 部** | **M24 の繰越 4 件**（§1.2） | **★挙動を変える** |

**★★第 1 部を完了させ、E2E 非回帰を確定させてから第 2 部へ入ること。**

**★理由＝リファクタと機能変更を混ぜると、E2E が赤くなったときに「リファクタが壊したのか、機能変更が壊したのか」が切り分けられない。** **★`CO-009` の成果物そのものが「E2E 非回帰」であるため、同時に E2E の期待値を変える作業を入れると成果物が測れなくなる。**

**★★第 1 部の完了時点で 1 度コミットを切り、その時点の E2E 結果を完了報告へ別に載せること**（`CLAUDE.md` §7 のチェックポイントコミット）。

### 1.2 第 2 部の 4 件（＋条件付き 1 件）

| # | 内容 | 出所 |
|---|---|---|
| **★★A** | **ゴミ箱を複数キャラ対応にする** | `D-611` / `D-617` ／ followup §AU `trash-scoped-to-single-character` |
| **★B** | **地上ダッシュの表示語を直す**（`preset_aliases.alias_text`） | `D-615` ／ followup §AV `ground-dash-label-split-two-ways` |
| **C** | **`SM-068`（＝`SM-074`）登録時の重複エラーの表示改善** | `D-608` ／ ledger（**★`CO-010`＝`useCheckDuplicate` の TanStack 化と実装面が同一**） |
| **D** | **`gofmt -l` を PR チェックへ足す** | followup §AW `gofmt-not-in-pr-checks` |
| **★条件付き E** | **`normal` / `counter` の表示語の割れを畳む** | followup §AW `combo-labels-normal-counter-split`。**★エディタ**（`ComboEditorBasicFields` の直書き）**を触らずには畳めない。⇒ 第 1 部で同ファイルを触るなら同時に。触らないなら落として報告する** |

### 1.3 ★★★本サブの最大の危険——**契約が 5 件当たっている**

**★★起票前に契約を持つ節を走査した**（playbook §4.51）。**★過去最多である。**

| # | 契約 | なぜ当たるか | 扱い |
|---|---|---|---|
| **★★★1** | **契約 F-2**（`m20-contract` §5 / `m21-contract` §5。**`RecipeResponse.presetId` ／ queryKey `["combo", comboId, "recipe", presetId]` ／ config `[defaults] preset_id` の形を変えない**＝`D-291`） | **★★★`CO-009` はまさに queryKey の形を変えるリファクタである。⇒ 正面から当たる** | **§4.1.1 で解いた**（`D-619`） |
| **★★2** | **契約 F-1**（`recipe_cache` の読み書き ／ `internal/service/notation/` / `internal/service/preset/`） | **★第 2 部 B が `alias_text` を更新し、`recipe_cache` の再構築が要る** | **`D-614` で条件つき解除済み。§4.3 の条件を守ること** |
| **★★3** | **`DES-002` §11.3 の「契約（★落とすと検査が意味を失うもの）」** | **★第 2 部 D が PR 検査へ 1 行足す** | **§4.5。★既存の契約 5 件を 1 つも落とさないこと** |
| **4** | **`DES-002` §4.2 の経路表** | **★第 2 部 A がゴミ箱の一覧の呼び方を変える**（定数 → 選択値） | **§4.2。★経路は変わらない見込み。★変わったら報告** |
| **5** | **`DES-002` §7.5**（**`preset_aliases` がプリセット別に表示名を保持する**） | **★第 2 部 B が `alias_text` の値を直す** | **§4.3。★構造は変えない。値だけ直す** |

### 1.4 ★★`code-facts.md` の数を使わないこと

**`code-facts.md` §2-2 は本番コードの `queryKey` を 64 行として掲載しているが、実測は 84 箇所である**（followup `code-facts-querykey-undercount`）。

**★差の内訳＝(a) 変数参照 15 箇所**〔`queryKey: PRESETS_KEY` 等〕**／(b) `PUNISH_FINDER_KEY` / `PUNISH_LIST_KEY` の const 定義 2 件**〔**実体は配列ではなく文字列定数**で抽出パターンに合致しない〕。

**⇒ `code-facts` で見積もると 20 箇所を取りこぼす。★§3.3-1 で数え直すこと。**

### 1.5 ★★起票時に設計卓が確かめていないこと

**★設計卓は武装中であり、実装ソースを読んでいない。以下はすべて ledger と followup の記述である。**

| 対象 | 分からないこと |
|---|---|
| `queryKey` | **「84 箇所 / 39 ファイル」**——**N を数え直す**（playbook §4.40）。**★★あわせて「列挙そのものが全数か」を疑うこと**（`M24-09d` の教訓） |
| **契約 F-2** | **★★「形を変えない」がリテラルな意味で使われているか。⇒ §3.3-2 で `D-291` の周辺を実査すること** |
| ゴミ箱 | **★キャラを渡している箇所が FE の 1 か所で足りるか**（前任設計卓の見込み）。**★セットプレイ側も同じ画面である** |
| `alias_text` | **★`recipe_cache` が起動時に自動で再構築されるか、利用者がボタンを押すまで古いままか**（`D-615`） |
| `SM-068` | **`useCheckDuplicate` の現在の形。`CO-010`（TanStack 化）がどこまで済んでいるか** |

### 1.6 ★★このサブで作らないもの（スコープ外）

| # | 作らないもの | 理由 |
|---|---|---|
| **★★1** | **第 1 部での挙動の変更** | **★成果物が E2E 非回帰である。★1 つでも挙動を変えると測れなくなる** |
| **★★2** | **`presetId` が単一値であるというモデルの変更** | **契約 F-2 の本体**（§4.1.1）。**★`D-291`＝カスタムは横断 1 枚** |
| **★★3** | **`recipe_cache` をマイグレで書き換えること** | **`D-615`。★組み立てはアプリ側のロジックであり、SQL で再現すると規則が 2 か所になる**（`E-76`） |
| **★★4** | **`internal/service/notation/` / `internal/service/preset/` の変更** | **契約 F-1**（`D-614` の条件 3＝**触るなら `CHANGE` を要する**）。**★第 2 部 B は `alias_text` の値だけを直す** |
| **5** | **`preset_aliases` の構造の変更** | **`DES-002` §7.5。★値だけ直す** |
| **6** | **CSV の列名・ZIP エントリ名の変更** | **`DES-002` §7.6 の契約**（`M24-07` が `column_contract_test.go` で守っている） |
| **7** | **`pr-checks.yml` へ E2E を足すこと** | **★E2E は nightly である**（`CHANGE-145`。`DES-002` §11.3） |
| **8** | **`web/playwright.config.ts` の `workers` の変更** | `SUPP-001` §5.5・`D-597` |
| **9** | **`SM-016`（コンボの単位語）の実装** | **★`D-618` で `M25` 以降へ送った**（followup §AW `combo-step-unit-word-deferred`） |
| **10** | **英語ロケールの残り 122 ファイル / 1,409 行** | **★`CO-026` の手番**（`D-618`。**開発者判断で据え置き**） |

### 1.7 目的

**FE データ層の `queryKey` を 1 つの作法へ揃える。あわせて M24 の繰越 4 件を畳み、M24 を実装として閉じる。**

---

## 2. 成果物

### 2.1 作成/修正するファイル

| ファイル | 内容 | 部 |
|---|---|---|
| **FE の `queryKey` 全数**（**★§3.3-1 で数える。ledger は 84 箇所 / 39 ファイル**） | **object・flat の統一** | **1** |
| ゴミ箱の一覧を呼ぶ FE | **キャラを選べるようにする**（**★セットプレイ側も同じ画面**） | 2-A |
| `migrations/000080_*.up.sql` / `.down.sql` | **`preset_aliases.alias_text` の UPDATE**（前ダッシュ → 前方ステップ ／ 後ろダッシュ → 後方ステップ） | 2-B |
| `useCheckDuplicate` 周辺 | **`CO-010` の TanStack 化 ＋ `SM-068` の差分表示** | 2-C |
| `.github/workflows/pr-checks.yml` | **`gofmt -l` を 1 行**（`go vet` ジョブに相乗り可） | 2-D |
| **テスト**（§5.1） | queryKey の作法の固定 ／ ゴミ箱のキャラ切替 ／ `alias_text` の値 ／ `gofmt` の検出 | 1・2 |
| `docs/progress/progress-log.md` | **★追記**（CLAUDE.md §8） | — |

### 2.2 変更しないもの（原則）

| 対象 | 何を守るか | 根拠 |
|---|---|---|
| **`presetId` が単一値であるというモデル** | **契約 F-2** | `m20-contract` §5 ／ `D-291` |
| **`RecipeResponse.presetId` ／ config `[defaults] preset_id`** | **契約 F-2 の残り 2 面** | 同上 |
| **`recipe_cache` の組み立てロジック** | **契約 F-1** | `D-614` / `D-615` |
| **`internal/service/notation/` / `internal/service/preset/`** | **契約 F-1** | 同上 |
| **`preset_aliases` の構造** | **表示名の保持方式** | `DES-002` §7.5 |
| **CSV の列名 ／ ZIP エントリ名** | **往復契約** | `DES-002` §7.6 |
| **`DES-002` §11.3 の既存の契約 5 件** | **CI の検査が意味を失わないこと** | §11.3 |
| **`web/playwright.config.ts` の `workers`** | 直列実行 | `SUPP-001` §5.5 |
| **★第 1 部における挙動のすべて** | **E2E 非回帰** | §1.6-1 |

### 2.3 例外条項

**★第 2 部 E（`normal` / `counter`）は条件付きである**——**第 1 部で `ComboEditorBasicFields` を触るなら同時に畳んでよい。触らないなら落として報告すること。**

**★`internal/api` / `internal/repository` に触ることは可**（第 2 部 C で必要になりうる）。**★ただし `internal/service/notation/` / `internal/service/preset/` は不可。**

**★触ったら報告すること**（§7.4.1）。

### 2.4 ★並列性

**★★並列にしない。** `M24-overview` §5＝**repo 全体の FE データ層を単独所有する期間が要る。★宣言できない**（全域に触る）。

---

## 3. 前提条件

### 3.1 必読ドキュメント

- `CLAUDE.md` §4 / §5 / §7 / §8 / §10
- **`m20-contract` §5 ／ `m21-contract` §5**（**★★契約 F-1 / F-2。本サブの核心**）
- `DES-002` §4.2（**★ゴミ箱の一覧の as-built。★2026-08-30 に errata 是正済み**） / §7.5 / §11.3
- `DES-005` §5.7 / §5.15
- `docs/handover/followup-backlog.md` **§AU / §AV / §AW**（**★本サブの出所**）
- `docs/handover/code-facts.md`（**★★§2-2 の `queryKey` 64 行は誤り**＝§1.4）

### 3.2 参照不要

- 他サブの指示書・レビューチェックリスト（`D-217`）
- `docs/postmortem/`

### 3.3 ★★着手前の確認（Plan Mode 必須）

**★以下はすべて実査項目である。§1.5 のとおり、設計卓が現物で裏を取ったものは 1 つも無い。**

**★★「N 個ある」と書かれた列挙は、その N を数え直すこと**（playbook §4.40）。**★★あわせて「列挙そのものが全数か」を疑うこと**（`M24-09d` の教訓）。

| # | 確かめること | なぜ要るか |
|---|---|---|
| **★★1** | **`queryKey` の全数とファイル数**（**母数付き。★「84 箇所 / 39 ファイル」を数え直す**）。**★★変数参照**〔`queryKey: PRESETS_KEY` 等〕**と、配列ではない文字列定数も数に入れること** | **★★§1.4。★`code-facts` の 64 行で見積もると 20 箇所を取りこぼす** |
| **★★★2** | **★★★契約 F-2 の「形を変えない」がリテラルな意味かモデルの意味か**（**★`D-291` の周辺と `m20-contract` §1 を読む**）。**★`RecipeResponse.presetId` と config `[defaults] preset_id` の現在の形も併せて報告すること** | **★★★§4.1.1 の裁定が正しいかの検算である。★食い違ったら止めて設計卓へ報告する**（§9.1-1） |
| **3** | **★現在の `queryKey` の作法が何通りあるか**（**母数付き**）。**★統一先をどれにするかの材料** | **★§4.1。★「統一する」は「どれへ」を決めないと始まらない** |
| **4** | **★`queryKey` を持つ `invalidateQueries` / `setQueryData` の全数**（**母数付き**） | **★★キーの形を変えると invalidate 側も同時に変わる。★片方だけ変えると静かにキャッシュが効かなくなる** |
| **★★5** | **★★キーの形を変えたときに「静かに壊れる」経路が在るか**——**invalidate が当たらなくなっても、テストは緑のまま通りうる** | **★★本サブの最大の危険である。⇒ §5.1-2 の観測をどう作るかの材料** |
| **6** | **★ゴミ箱がキャラを渡している箇所の全数**（**母数付き。★コンボ表とセットプレイ表の両方**） | **★★§4.2。★前任設計卓は「FE 側の 1 か所で足りる見込み」と書いているが、その N を数え直すこと** |
| **7** | **★ゴミ箱の `queryKey` に `characterId` が入っているか**（followup は `["combos","trash",characterId]` と書いている）**と、`["combos"]` の invalidate の全数** | **★★第 1 部と第 2 部 A が同じ 1 か所で交わる。⇒ 順序が効く** |
| **8** | **★ゴミ箱でキャラを選ぶ UI をどこへ置くか。既存の `CharacterSelector` を使えるか** | **★§4.2。★新しい部品を作らない** |
| **★★9** | **★★`preset_aliases.alias_text` の現在の値と、地上ダッシュに当たる行の全数**（**母数付き**）。**★`migrations/000006` 以降に同じ値を触ったマイグレが在るか** | **★★§4.3。★「2 行」と決めつけないこと** |
| **★★10** | **★★`alias_text` を更新した後、`recipe_cache` が起動時に自動で再構築されるか、利用者がボタンを押すまで古い文字列が残るか**（**★どちらか断定できる証拠を出すこと**） | **★★§4.3。★★後者なら「配布後に直らない」ことを意味する。⇒ 止めて設計卓へ報告する**（§9.1-3） |
| **11** | **★`en` 側の `alias_text` が Dash のままか**（**★`D-615`＝en は Dash が正しい**） | **★★日本語だけを直す。en を巻き込まない** |
| **12** | **★`useCheckDuplicate` の現在の形と、`CO-010`（TanStack 化）がどこまで済んでいるか** | **★§4.4。★済んでいるなら `SM-068` の差分表示だけになる** |
| **13** | **★重複エラーの現在の文言と、差分が読めない理由**（memo＝**「既存コンボ（#40）と重複: ケン・立ち強K / …」**） | **★§4.4。★何を足せば差分が読めるか** |
| **14** | **★`pr-checks.yml` の現在の job 構成と、`go vet` ジョブの形** | **★§4.5。★★E2E の実体を足さないこと**（§1.6-7） |
| **15** | **★`gofmt -l` が現在いくつ検出するか**（**母数付き**）。**★`internal/infra/migration/migrate_m2402_test.go` は `M24-02` 由来で非適合のまま残っていると報告されている** | **★★足した瞬間に赤になるなら、先に是正するか、ベースライン型にするかを決める必要がある**（§4.5） |
| **16** | **★`ComboEditorBasicFields` を第 1 部で触るか**（**★条件付き E の分岐**） | **★§1.2-E。★触らないなら落として報告する** |

---

## 4. 詳細仕様

### 4.1 ★★第 1 部——`CO-009` `queryKey` の object・flat 統一

| 論点 | 決めたこと |
|---|---|
| **★★成果物** | **★★E2E 非回帰。挙動を 1 つも変えない**（§1.1） |
| **統一先** | **★製造が決めてよい**（§3.3-3 の実査で現在の作法を数えた上で）。**⇒ 選んだ形と理由を報告すること** |
| **★★範囲** | **★§3.3-1 で数えた全数。★`code-facts` の 64 行を使わない**（§1.4） |
| **★★invalidate 側** | **★★キーの形を変えたら `invalidateQueries` / `setQueryData` も同時に変える。★片方だけ変えると静かにキャッシュが効かなくなる**（§3.3-4） |
| **★変数参照** | **★`queryKey: PRESETS_KEY` のような変数参照も対象である。★定数の側を直せば足りるものと、呼び出し側も要るものを分けること** |
| **★完了時** | **★★1 度コミットを切り、その時点の E2E 結果を完了報告へ別に載せること**（§1.1） |

#### 4.1.1 ★★★契約 F-2 の読み方——**設計卓の裁定**（`D-619`）

**★★F-2 の逐語**（`m20-contract` §5 ／ `m21-contract` §5）:

> **`preset_id` 単一値の契約**｜`RecipeResponse.presetId` ／ queryKey `["combo", comboId, "recipe", presetId]` ／ config `[defaults] preset_id` の**形を変えない**（**D-291**）｜プリセットの解決が (キャラ, ユーザー) → preset へ変わると、**3 面が同時に壊れる**

**★★設計卓の裁定＝F-2 が守っているのは「`presetId` が単一値である」というモデルである。★queryKey の表記法**〔配列 か object か〕**の凍結ではない。**

**★根拠 2 つ**——**(1) `D-291` は「カスタムは横断 1 枚」であり、プリセットのデータモデルの決定である ／ (2) F-2 の「崩れたときの症状」欄が「プリセットの解決が (キャラ, ユーザー) → preset へ変わると 3 面が同時に壊れる」と書いており、危険として名指ししているのはモデルの変更である。**

**★★⇒ 表記法を統一してよい。ただし条件 3 件:**

| # | 条件 |
|---|---|
| **★★1** | **`presetId` が queryKey の構成要素として残ること。★落とさない** |
| **★★2** | **3 面**〔`RecipeResponse.presetId` ／ queryKey ／ config `[defaults] preset_id`〕**の対応が読める形であること。★他の 2 面は触らない** |
| **★★3** | **§3.3-2 の実査で「形を変えない」がリテラルな意味で使われていた証拠が出たら、止めて設計卓へ報告する**（§9.1-1） |

**★★これは `D-614`**〔契約は作られた目的とともに読む〕**の 2 度目の適用である**（playbook §4.56）。

### 4.2 第 2 部 A——**ゴミ箱を複数キャラ対応にする**

**★★経緯**（`D-617`）——**キャラは `DEFAULT_CHARACTER_ID = 1` にハードコードされており、`// TODO(M3+): 複数キャラ対応時に `CharacterSelector` へ差し替える` が付いている。**

**★`M23-RESEARCH-01` が発見し `M23-06` を行き先に指名したが、同サブの入力 5 件に入らず followup にも入らなかった**（計測点 `M-102`）。

| 論点 | 決めたこと |
|---|---|
| **★★サーバ** | **★★既に対応している。`character_id` は変数であり、画面が定数を渡しているだけである**（`DES-002` §4.2）。**⇒ 経路は変わらない見込み。★変わったら報告すること** |
| **UI** | **★既存の `CharacterSelector` を使う。★新しい部品を作らない**（§3.3-8） |
| **★★落としやすい点 1** | **★★`DES-002` §4.2 の注記は 2026-08-30 に errata 是正済みである**（`D-617`）。**★「キャラクターを固定して呼ぶ」という旧記述はもう無い。⇒ 「設計と違う」と読んで戻さないこと** |
| **★★落としやすい点 2** | **★★セットプレイ側のゴミ箱も同じ画面である。★コンボ表だけ直してセットプレイ表を忘れる形になりやすい**（§3.3-6） |
| **★queryKey** | **★★ゴミ箱の queryKey に `characterId` が入っている**（followup＝`["combos","trash",characterId]`）。**⇒ 第 1 部と同じ 1 か所で交わる。★第 1 部を先に終えること** |
| **既定値** | **★製造が決めてよい**（既定表示キャラを使うか、全キャラを出すか）。**⇒ 選んだ形と理由を報告すること** |

### 4.3 第 2 部 B——**地上ダッシュの表示語を直す**

**★★開発者裁定（2026-08-30・`D-615`）＝インゲームの正しい日本語表記は「ステップ」であり、`alias_text` 側の「ダッシュ」が誤りである**〔英語の Dash が正しいことに釣られた過去の指示の残骸〕。

| 論点 | 決めたこと |
|---|---|
| **対象** | **プリセット `official_ja_move` の `preset_aliases.alias_text`**（`migrations/000006`）。**★「前ダッシュ」→「前方ステップ」／「後ろダッシュ」→「後方ステップ」** |
| **★★母数** | **★★「2 行」と決めつけないこと**（§3.3-9）。**★`000006` 以降に同じ値を触ったマイグレが在るかも確かめる** |
| **★★en** | **★★en は Dash のままが正しい。日本語だけを直す**（§3.3-11） |
| **形** | **★マイグレ 1 本**（`000080`）**の UPDATE。★down も書くこと** |
| **★★★`recipe_cache`** | **★★★マイグレで書き換えないこと**（`D-615`）。**★理由＝組み立てはアプリ側のロジックであり、SQL で再現すると同じ規則を 2 か所に持つことになる**（`E-76`）。**⇒ 既存の再構築機構に任せる**（`DES-005` §5.16 の「レシピキャッシュ再構築ボタン」） |
| **★★分岐** | **★★§3.3-10 で「利用者がボタンを押すまで古いままである」と分かったら、止めて設計卓へ報告すること**（§9.1-3）。**★それは「配布後に直らない」ことを意味する** |
| **FE 側** | **★★触らない。`SystemRow.tsx` / `constants/punish.ts` の「前方ステップ／後方ステップ」は正しい**（`D-615`） |

### 4.4 第 2 部 C——**`SM-068`（＝`SM-074`）登録時の重複エラーの表示改善**

**★memo の逐語＝「重複時のエラーがちょっと不自然。既存コンボ（#40）と重複: ケン・立ち強K / …」／「コンボ登録時に被った時の表示を見直したい。遷移しなくてもどこが差分だったかわかるようにしたい」。**

| 論点 | 決めたこと |
|---|---|
| **★実装面** | **`useCheckDuplicate`**（**★`CO-010`＝TanStack 化と実装面が同一。ledger が「束ねる余地がある」と書いている**）。**⇒ §3.3-12 で `CO-010` の進捗を確かめ、済んでいなければ同時に畳んでよい** |
| **★★形** | **★製造が決めてよい。★ただし「遷移しなくても差分が読める」ことが要求である**（memo の逐語）。**⇒ 何を足せば差分が読めるかを §3.3-13 で確かめてから決める** |
| **★スコープ** | **★重複判定そのもの**（`VAL-C02`）**は触らない**（`M24-11` が check-then-act を是正済み）。**★表示だけ** |
| **★`DES-005` §5.7** | **★表示を変えるなら §5.7 への反映が要る。⇒ 完了報告へ as-built を書くこと** |

### 4.5 第 2 部 D——**`gofmt -l` を PR チェックへ足す**

**★★これは `M24-07` の最大の発見と同じ型である**——**「守るべきものはあるが、PR で観測されていない」**（followup §AW）。

| 論点 | 決めたこと |
|---|---|
| **形** | **`.github/workflows/pr-checks.yml` の `go vet` ジョブへ相乗りしてよい** |
| **★★既存の契約** | **★★`DES-002` §11.3 の契約 5 件を 1 つも落とさないこと**（**とくに (1) `go test` のキャッシュ無効化 ／ (2) パイプの終了コード**） |
| **★★E2E** | **★★`pr-checks.yml` へ E2E を足さないこと**（§1.6-7。**E2E は nightly である**＝`CHANGE-145`） |
| **★★赤の扱い** | **★★§3.3-15 で現在の検出数を数えること。★足した瞬間に赤になるなら、(a) 先に是正する か (b) ベースライン固定型にする のどちらかを選び、理由を報告する。★★`internal/infra/migration/migrate_m2402_test.go` は `M24-02` 由来で非適合のまま残っていると報告されている** |
| **★観測** | **★破壊確認 4 で「整形を崩すと PR チェックが赤くなる」ことを見せること**（§5.3） |

### 4.6 ★実施順

| 順 | 内容 | 理由 |
|---|---|---|
| **1** | **§3.3 の実査 16 件** | **★★§3.3-2**〔契約 F-2 の読み方〕**が本サブの分岐点である** |
| **★★2** | **§4.1 第 1 部**（`queryKey` の統一）→ **完了時にコミット ＋ E2E 結果を別に記録** | **★★成果物が E2E 非回帰である。★ここで挙動を変えない** |
| **3** | **§4.2 第 2 部 A**（ゴミ箱） | **★第 1 部と同じ queryKey に触るため直後に置く** |
| **4** | **§4.5 第 2 部 D**（`gofmt`） | **★最も軽く、FE と無関係** |
| **5** | **§4.3 第 2 部 B**（地上ダッシュ） | **★マイグレを伴う。★止まる分岐が在る**（§3.3-10） |
| **6** | **§4.4 第 2 部 C**（重複エラー） | **★最も形が決まっていない** |
| **7** | **§1.2-E**（条件付き） | **★触るなら最後** |

---

## 5. テスト要件

### 5.1 追加するテスト

| # | 内容 | 部 |
|---|---|---|
| **★★1** | **`queryKey` の作法を固定するテスト**（**★選んだ統一先の形を、機械で確かめられる形にする**） | 1 |
| **★★★2** | **★★★invalidate が当たることを固定するテスト**（**★§3.3-5 の実査を踏まえて形を決める**）。**★★「キーの形を変えて invalidate が当たらなくなっても緑のまま」を防ぐ観測である** | 1 |
| **3** | **★契約 F-2 の 3 面の対応を固定するテスト**（**`presetId` が queryKey の構成要素として残ること**） | 1 |
| **4** | **ゴミ箱でキャラを切り替えると一覧が変わること**（**★コンボ表とセットプレイ表の両方**） | 2-A |
| **5** | **`preset_aliases.alias_text` の値**（**★ja は「前方ステップ」／ en は Dash のまま**） | 2-B |
| **6** | **重複エラーで差分が読めること** | 2-C |

### 5.2 既存テストの扱い

**★★第 1 部ではテストの期待値を変えないこと。** **★挙動が変わらないなら、テストも変わらないはずである。★変えたくなったら、それは挙動を変えた証拠である。**

**★★1 本も消さない・skip しない。**

**★第 2 部で期待値を変えるときは、「変えてはいけないから固定してあった」のか「たまたま現状を写しただけ」なのかを 1 件ずつ判定すること**（playbook §4.45 の規約 2）。

### 5.3 ★★破壊確認（**5 件・必須**）

**★★「壊す対象がその観測に含まれているか」を、期待を書く前に 1 件ずつ確かめること**（playbook §4.32）。

| # | 壊し方 | 期待 |
|---|---|---|
| **★★★1** | **統一後の `queryKey` を 1 か所だけ旧の形へ戻す** | **★★§5.1-1 が赤くなる。★緑のままなら作法を守る観測が無い** |
| **★★★2** | **`invalidateQueries` のキーを 1 か所だけずらす**（**★形は変えず、値だけ変える**） | **★★★§5.1-2 が赤くなる。★★★これが本サブで最も重要な破壊確認である**——**緑のままなら「静かにキャッシュが効かなくなる」を誰も検出できない** |
| **3** | **ゴミ箱のキャラ選択を定数へ戻す** | **§5.1-4 が赤くなる**（**★コンボ表とセットプレイ表の両方で**） |
| **4** | **Go のファイルを 1 つ整形崩れにする** | **★PR チェックの `gofmt` が赤くなる**（§4.5） |
| **5** | **`alias_text` を「前ダッシュ」へ戻す** | **§5.1-5 が赤くなる** |

**★★破壊確認は絞り込み実行でよい**（`make e2e-only P=<パターン>`）。**★破壊確認後の確定実行は絞らないこと。**

---

## 6. レビュー観点（別ファイル参照）

`docs/instructions/reviews/M24-08-review-checklist.md` を参照。

---

## 7. 完了条件（Definition of Done）

### 7.1 機能要件

1. **★★第 1 部が完了した時点のコミットが在り、その時点の E2E 結果が完了報告へ別に載っている**（§1.1 / §4.1）。
2. **★★第 1 部で挙動が 1 つも変わっていない**（**★E2E 非回帰。★既存テストの期待値を変えていない**＝§5.2）。
3. **★★§3.3-1 の `queryKey` の全数が数え直され、その全数が統一されている**（**★`code-facts` の 64 行を使っていない**）。
4. **★★`invalidateQueries` / `setQueryData` も同時に変わっている**（§4.1）。**★破壊確認 2 が赤くなっている。**
5. **★★契約 F-2 の 3 面が守られている**（`presetId` が単一値・queryKey の構成要素として残る・他の 2 面は不変）。**★§3.3-2 の答えが報告されている。**
6. **★ゴミ箱でキャラを選べる**（**★コンボ表とセットプレイ表の両方**）。
7. **★`preset_aliases.alias_text` の ja が「前方ステップ／後方ステップ」になっている。★en は Dash のまま。★`recipe_cache` をマイグレで書き換えていない。**
8. **★★§3.3-10 の答えが報告されている**（**再構築が自動か、ボタン待ちか**）。
9. **★重複エラーで差分が読める**（§4.4）。
10. **★`gofmt -l` が PR チェックに載っている。★`DES-002` §11.3 の既存の契約 5 件が 1 つも落ちていない。★E2E の実体を足していない。**
11. **★★`internal/service/notation/` / `internal/service/preset/` が変わっていない**（契約 F-1）。
12. **★CSV の列名・ZIP エントリ名が変わっていない。**
13. **★破壊確認 5 件が実施され、コマンドと出力が載っている。**
14. **★テストが 1 本も減っていない・skip されていない。**

### 7.2 自己テスト結果

- `go test ./... -count=1`
- `cd web && pnpm test`
- **★`pnpm build`**（**★型検査はこちらで見る**＝`web/CLAUDE.md` §3）
- `make e2e`（**★★第 1 部の完了時と、全体の完了時の 2 回**）
- **破壊確認 5 件**（§5.3）。**実施コマンドと出力を載せること。**

**★★E2E の着手前基準値は採らないこと**（`D-600` 規約 1）。**★直前にマージされたのは `M24-07` の 1 本であり、その完了報告が全数緑を明記している。⇒ その確認だけを行い、完了報告では件数の増減を主張せず「全数緑（N 件）」とだけ書くこと。**

**★★ポートの空き確認に `ss` / `netstat` / `lsof` を使わないこと**（playbook §4.37 規約 7）。

**★★母数は「指示書に書いてあるから使える」ものではない**（playbook §4.37）。**⇒ 比較は同一セッション内の背中合わせで採ること。**

### 7.3 品質チェック

- `bash scripts/check-artifact-integrity.sh`（**★1 本目に回す**）
- `bash scripts/check-md-emphasis.sh` ／ `bash scripts/check-doc-refs.sh` ／ `bash scripts/check-progress-log-index.sh` ／ `bash scripts/check-enum-sync.sh` ／ `bash scripts/check-browser-storage-keys.sh`
- **★★検査表は 1 行ずつ実際の出力から埋めること**（`M24-09d` の教訓 2）。**★回していないものは書かない。**

### 7.4 ドキュメント

- **`docs/progress/progress-log.md` への追記**（製造担当が行う。`CLAUDE.md` §8）。

#### 7.4.1 ★CHANGE 要否の判定（**as-built で判定する**）

**`CHANGE-148` は起票済みである。⇒ 製造は番号を消費しない。**

**★as-built が指示書と違ったら、違ったほうを完了報告へ書くこと**（playbook §4.43）。

**★★とくに次の 3 つは設計卓が断定できていない**——**(a) 契約 F-2 の読み方 ／ (b) `recipe_cache` の再構築が自動か ／ (c) `gofmt` が足した瞬間に赤になるか。**

### 7.5 完了報告

- **★★§3.3 の実査 16 件の答えを、母数付きで載せること。**
- **★★第 1 部の完了時点の E2E 結果を、全体の結果と別に載せること。**
- **★統一先に選んだ形とその理由**（§4.1）。
- **★ゴミ箱の既定値に選んだ形とその理由**（§4.2）。
- **★落とした項目**（条件付き E ／ その他）**とその理由。**
- 完了報告に `git diff --numstat` を貼ること（教訓 `E-225`）。

---

## 8. 参照ドキュメント

| ID | パス | 使いどころ |
|---|---|---|
| m20-contract §5 / m21-contract §5 | `docs/process/m20-contract.md` / `m21-contract.md` | **★★契約 F-1 / F-2** |
| DES-002 §4.2 | `docs/design/02-architecture.md` | **★ゴミ箱の一覧の as-built**（2026-08-30 errata 是正済み） |
| DES-002 §7.5 | 同上 | **★`preset_aliases` が表示名を保持する** |
| DES-002 §11.3 | 同上 | **★★CI 運用の契約 5 件** |
| DES-005 §5.7 / §5.15 | `docs/design/05-screen-design.md` | 重複エラー ／ ゴミ箱 |
| CHANGE-148 | `docs/change-notes/CHANGE-148-notification.md` | 本サブの通知書 |
| followup §AU / §AV / §AW | `docs/handover/followup-backlog.md` | **★本サブの出所** |
| code-facts | `docs/handover/code-facts.md` | **★★§2-2 の `queryKey` 64 行は誤り**（§1.4） |

---

## 9. 注意事項・判断に迷ったら

### 9.1 推測で進めてはいけない事項

| # | 事項 | 対応 |
|---|---|---|
| **★★★1** | **契約 F-2 の「形を変えない」がリテラルな意味で使われていた証拠が出た** | **★★★止めて設計卓へ報告する。★§4.1.1 の裁定を検算させるための実査である** |
| **★★2** | **`recipe_cache` の組み立てロジックを触らないと解けない** | **★★止めて設計卓へ報告する**（契約 F-1・`D-614` の条件 3） |
| **★★3** | **`alias_text` を直しても、利用者がボタンを押すまで `recipe_cache` が古いままである** | **★★止めて設計卓へ報告する。★それは「配布後に直らない」ことを意味する** |
| **4** | **第 1 部で挙動を変えないと統一できない箇所が在る** | **★止めて設計卓へ報告する。★勝手に挙動を変えない** |
| **5** | **`gofmt` を足すと既存ファイルが大量に赤くなる** | **★§4.5 の (a)(b) から選び、理由を報告してよい。★止まらない** |
| **6** | **`DES-002` §11.3 の既存の契約を落とさないと `gofmt` を足せない** | **★★止めて設計卓へ報告する** |
| **7** | **ゴミ箱のキャラ対応に BE の変更が要ると分かった** | **★止めて設計卓へ報告する**（`DES-002` §4.2 の経路表に触れ、`CHANGE` の射程が変わる） |

### 9.2 推測で進めてよい事項（その旨を明示）

- `queryKey` の統一先の形（**★理由を報告すること**）
- ゴミ箱のキャラ選択の既定値（**★同上**）
- 重複エラーの差分表示の形（**★同上**）
- テストの置き場
- `gofmt` を足す位置（`go vet` ジョブへの相乗りか、別ジョブか）

**⇒ いずれも「推測: 〜と仮定した」をコード内コメントまたは完了報告に明示すること**（`CLAUDE.md` §9）。

### 9.3 不明事項発見時の対応

`CLAUDE.md` §9 に従う。**原則は「止めて聞く」ではなく「進めて報告する」。** **★§9.1 の 7 件はハード列扱いである。**

### 9.4 ★CHANGE 要否

**`CHANGE-148` が起票済みである**（§7.4.1）。**★製造は番号を消費しない。**

---

## 10. 完了後の次ステップ

- 設計伝達レポート（`design_handover_report`）を生成する。
- **★次は `M24-CLOSE` である**（二系統 ledger の消化状況の確定とクローズ）。**★M24 の実装サブは本サブが最後である。**

---

## 11. 開発者への確認事項

| # | 事項 | 設計卓の暫定案 | 止まるか |
|---|---|---|---|
| **★1** | **★本サブを 1 本で行くか、第 1 部と第 2 部で 2 サブに割るか**（**★想定 420〜540 分は M24 で最大である**） | **★暫定案＝1 本・2 部構成。★理由＝第 1 部と第 2 部 A が同じ queryKey に触るため、割ると 2 回触ることになる。★サブ分割を 17 本へ増やさない** | **★止まらない**（暫定案で進める） |
| **2** | **★条件付き E**（`normal` / `counter`）**を落としてよいか** | **★暫定案＝第 1 部で `ComboEditorBasicFields` を触らないなら落とす** | **★止まらない**（暫定案で進める） |
| **3** | **★`gofmt` が足した瞬間に赤になる場合、先に是正するかベースライン型にするか** | **★暫定案＝製造が選び、理由を報告する** | **★止まらない**（暫定案で進める） |

---

*以上、指示書 M24-08 **v1.0.0**。* **★★本サブは 2 部構成であり、順序が成果物を決める**——**第 1 部の成果物は「E2E 非回帰」であり、そこで挙動を変えると測れなくなる。★第 1 部を完了させコミットを切ってから第 2 部へ入ること。** **★★最大の危険は「キーの形を変えて invalidate が当たらなくなっても、テストは緑のまま通りうる」ことである**（§3.3-5 / §5.1-2 / 破壊確認 2）。**⇒ 破壊確認 2 が緑のままなら、静かに壊れる経路を誰も検出できない。** **★★契約が 5 件当たっている**（§1.3。**過去最多**）。**とくに契約 F-2 は `CO-009` と正面から当たる。⇒ §4.1.1 で「モデルの契約であって表記法の凍結ではない」と裁定したが、★★§3.3-2 でその裁定を検算させること。食い違ったら止めること。** **★`code-facts` の `queryKey` 64 行は誤りである。実測 84 箇所であり、20 箇所を取りこぼす。**
