# M33-overview: マイグレーションの統合（**リリース前のベースライン化**）

| 項目 | 内容 |
|------|------|
| 文書種別 | 補足資料（overview。CHANGE 通知書対象外・自由改訂） |
| バージョン | **2.10.0**（2026-09-19・**★★★`M33-03` を受理し `M33` をクローズした**＝`D-907`。**⇒ 3 段とも着地。★`go test ./...` が FAIL 158 → FAIL 0。★あわせて §5 段 3 の射程の記述を指示書 v1.0.0 §2.2 へ寄せて畳んだ**） ／ **2.9.0**（2026-09-19・**★★★`M33-03` の三点セットを発行した**＝`D-903`。**⇒ `M33-02` の受理**〔`D-902`〕**で §6.2 段 3 の条件が満ちた。★射程 identity は 6 件。★移行手順は「開発者が走らせる」を切り出した**） ／ **2.8.0**（2026-09-19・**★★段 2 の並列条件を緩めた**＝`D-900`〔**「並列 0 本」→「走行中のサブが `migrations/` を触らないこと」**〕） ／ **2.7.0**（2026-09-13・**★★`M33-02` の三点セットを発行した**＝`D-860`〔指示書 v1.0.0 ／ チェックリスト v1.0.0〕**。★新系列は `000001` から始まる**〔既存の連番体系の外。設計卓は番号を払い出さない〕） ／ **2.6.0**（2026-09-13・**★★`M33-01` を受理＝`D-858`。⇒ 7 群 → 9 群**〔`S02` / `S06` が層をまたいでいた〕**／ 廃止 7 本 → 8 本 ／ 難所の数値を実測へ更新**〔28 → 35 ／ golden 4 → 17 stem〕） ／ **2.5.0**（2026-09-09・**★★`M33-01` を起票した＝`D-792`**） ／ 2.4.0（2026-09-09・**★★判断 1 が決着した＝`D-791`**〔逐語＝「開発repoも7群へ」。**⇒ §4.4 の案 (a)**〕**。⇒ 判断 4 件がすべて決着し、`M33-01` を起票できる**） ／ 2.3.0（2026-09-09・**★★開発者が §4.1 の判断 4 件のうち 3 件へ回答した＝`D-790`**〔判断 2 / 3 / 4〕**。⇒ 判断 2 の回答が新しい要求を 1 件生んだ＝§4.3**〕 ／ 2.2.0（2026-09-08・**★★調査資料の配置が完了したので §7 の参照先を差し替えた＝`D-785`**） ／ 2.1.0（2026-09-07・**★★`REUSE.toml` の本文を受領し、§2.3 へ危険を 2 件足した＝`D-777`。⇒ 危険 (3) は 1.0.0 / 2.0.0 が見ていたより重い**） ／ 2.0.0（2026-09-07・**★★外部 AI の調査結果 3 点を受領して全面改訂＝`D-775`。⇒ 統合方式が具体化した**） ／ 1.0.0（2026-09-07・起票＝`D-774`） |
| 作成日 | 2026-09-07 |
| 作成者 | 設計担当 Claude（設計卓／フェーズ4 期） |
| 対象マイルストーン | **`M33`**（**★フェーズ4 の追加。`phase4-overview` は `M25`〜`M32` で書かれており、本 MS は後から足した**） |
| 位置づけ | **`M33` のサブ構成・射程・危険の正本** |
| 由来 | **開発者の直接要求**（2026-09-07。逐語＝「**やっぱり可能なら migration の本数を減らして綺麗にした状態でリリースをしたいです。一本どこかで改善用のマイルストーンを用意してもらえますか？ どういう風に統合すれば上手くいくかはコードを読める外部 AI の担当とともに検討済みです**」） |
| 状態 | **★★`M33-01` 起票済み**（`docs/instructions/M33-01-freeze-and-survey.md` v1.0.0・2026-09-09・`D-792`）**。★`M33-02` / `M33-03` は未起票**（**★`M33-01` の対応表を受け取ってから起票する**＝§5）**。** **★★§4.1 の判断 4 件はすべて決着し、開発者判断待ちは 0 件である。** **★★投入には順序の条件が 1 つある**——**`migrations/` へ版を足す 3 本**（`M31-04` / `M31-05` / `M35-02`）**の着地後**（`M33-01` §0.4）**。** **★あわせて §4.3**（検証環境内のデータ移行）**が `M33-01` / `M33-03` の射程へ入った** |
| CHANGE 見込み | **★★2〜4 本**（`SUPP-001` §2.7・§3・§5.5 ／ `DES-003` ／ `DES-002` ／ `DES-004`。**★調査 §3 の対処表 B 区分がこの数を押し上げた**） |
| マイグレ見込み | **★★本 MS は「マイグレを減らす」ものであり、増やさない見込み。⇒ ただしベースラインの実体は新しいファイルになる**（§3） |
| 配置 | `docs/instructions/M33-overview.md` |

---

## 0. ★★本書の位置づけと、いま欠けているもの

**★★開発者は既に統合方式を検討済みである**（外部 AI の担当と）**。⇒ 設計卓はその案をまだ受け取っていない。**

**⇒ 本書は「案を受け取る前に、設計卓が押さえられる制約と危険」を並べたものである。★サブ分割は案を見てから確定する。**

**★★本書の値打ちは §2 の危険 4 件にある。⇒ どの統合方式を採るにせよ、この 4 件は必ず当たる。**

---

## 0.5 ★★【2026-09-07 全面改訂＝`D-775`】外部 AI の調査結果を受領した

**★開発者から 3 点を受け取った**〔`01-squash-proposal.md` ／ `01-migration-map.md` ／ `02-live-document-impact.md`〕**。⇒ 統合方式が具体化した。**

### 0.5.1 ★★受領内容の要点

| # | 内容 |
|---|---|
| **★★1** | **技術的に実現可能である**——**旧 up 全 104 本をインメモリ SQLite へ適用し、その最終状態を 7 群へ再構成して、最終スキーマ・全テーブルの全行・`sqlite_sequence` の一致と FK 違反 0・`integrity_check` 正常を確認済み** |
| **★★2** | **7 群案**〔S01 スキーマ ／ S02 games・characters ／ S03 moves ／ S04 move_commands ／ S05 move_derivations ／ S06 presets・aliases ／ S07 初期 users・tags〕**。★S01〜S07 は論理 ID であり、払い出した番号ではない** |
| **★★3** | **独立処理を廃止できるのは 8 本である**〔2026-09-13 是正＝`D-858`・`M33-01` §4-3。旧記述は 7 本〕**。対象＝`000009` / `000010` / `000011` / `000022` / `000028` / `000029` / `000081` / `000103`。★★★`000009` の投入行は 1 行も生存しない**〔guile は `000024` ／ jamie は `000053` ／ aki は `000082` が最終行の出所。削除は `000017` の 1 本だけ〕**。 ★★★ただし「廃止できる」と「書かなくてよい」は別である。⇒ `characters` は `AUTOINCREMENT` であり、行が消えても消費した id は `sqlite_sequence` に残る。★実測＝行 31 / `seq` 34 であり、差の 3 がまさに `000009` の 3 行である。⇒ `S02b` は `sqlite_sequence` を合わせる責任を持つ。** ⇒ 残る 97 組は「潰す」対象である |
| **★★4** | **難所 5 件＝seedgen / golden ／ 歴史的なマイグレテスト **35 ファイル**〔2026-09-13 実測＝`M33-01` §4-5。旧記述 28 は調査の式がディレクトリ限定だったため。込みで 38 ／ repo 全体で 40〕 ／ seed の再生成元 ／ 旧 DB との系列切替 ／ 生きた資料** |
| **★★5** | **外部 AI の見立て＝「公開を遅らせる必須作業ではなく、AI 枠に余裕があるとき分離環境で実装・検証し、十分に完成したら採用する整備候補」** |

### 0.5.2 ★★設計卓の危険 4 件は、すべて調査でも確認された

| 本書 §2 の危険 | 調査での対応箇所 | 判定 |
|---|---|---|
| **(1) 既存 DB が起動しなくなる** | 提案 §5「既存 DB と新系列は番号表だけで繋がらない」 | **★★一致。⇒ 調査は「旧 `schema_migrations.version` を書き換えるだけの処置は提案しない」とまで踏み込んでいる** |
| **(2) 版数を名指ししたテストが壊れる** | 難所 2 ／ 影響 §4「`Migrate`/`Steps` で歴史時点へ移動するコードが **35 ファイル**」（**★2026-09-13 実測へ更新＝`M33-01` §4-5**） | **★★一致。⇒ 設計卓は 3 件しか挙げられなかったが、実測は 28 ファイルである** |
| **(3) ライセンスの層が混ざる** | **★★調査資料に無い** | **★★設計卓の独自の指摘である。⇒ §0.5.3 で 7 群案へ当てた** |
| **(4) golden 群が対象を失う** | 難所 1 ／ 影響 §4「旧 SQL を `os.ReadFile` し生成結果と byte 比較」 | **★★一致。⇒ 調査は「旧ファイル名の置換だけでは内容不一致」と述べている** |

**⇒ 4 件のうち 3 件は独立に一致した。★(3) だけが設計卓側にしか無い。**

### 0.5.3 ★★【設計卓の追加】7 群案は、層の分離を偶然満たしている

**★★`scripts/check-migration-license.sh` は `GAME_TABLES` を見て層を判定し、`REUSE.toml` は `migrations/*_data_*.sql` のグロブで層 B へ解決する**（`CHANGE-158`）**。⇒ 7 群を層で仕分けると次になる。**

| 群 | 内容 | 層 | 命名 |
|---|---|---|---|
| **S01** | 最終スキーマ | **A**（`AGPL-3.0-or-later`） | `NNNNNN_*.sql` |
| **S02a** | **games**（**★2026-09-13 分割＝`D-858`**） | **★★A**（`AGPL-3.0-or-later`） | `NNNNNN_*.sql` |
| **S02b** | characters・custom_states | **B**（`CC-BY-SA-4.0`） | **`NNNNNN_data_*.sql`** |
| **S03** | moves | **B** | **`NNNNNN_data_*.sql`** |
| **S04** | move_commands | **B** | **`NNNNNN_data_*.sql`** |
| **S05** | move_derivations | **B** | **`NNNNNN_data_*.sql`** |
| **S06a** | **presets**（**★2026-09-13 分割＝`D-858`**） | **★★A** | `NNNNNN_*.sql` |
| **S06b** | preset_aliases | **B** | **`NNNNNN_data_*.sql`** |
| **★★S07** | **初期 users・tags** | **★★A** | `NNNNNN_*.sql` |

**★★S07 が層 A である理由＝`users` / `tags` は SF6 の事実ではない。⇒ `GAME_TABLES` に入らない。★予約タグ 3 件は本アプリの設計物である。**

**★★★【2026-09-13 是正＝`D-858`・`M33-01` §4-1】7 群 → 9 群へ増えた。⇒ `S02` と `S06` がライセンスの層をまたいでいた。** **★機序＝`games` と `presets` は `GAME_TABLES` に無い。⇒ 設計卓自身が `S07` を層 A とした論法を、`games` / `presets` へ当てていなかった。** **★★★機械検査はこの向きを検出しない。** 規則 (4) は「`_data_` を持つなら層 B へ解決すること」しか見ず、**どの表へ書くかを見ない。⇒ `NNNNNN_data_seed_games_characters.up.sql` は緑になる。★`games` が静かに `CC-BY-SA-4.0` へ移る。** **★★戻せない。** 開発者の逐語「一度 AGPL 化してから CC BY SA に戻すのは難しい認識」の裏返しであり、**一度 CC-BY-SA で配ったものを AGPL へ戻すのも同じく難しい。** **★★9 群でも圧縮は十分である＝111 本 → 9 本。⇒ 「7 本」という数そのものは目的ではない。** **★★★あわせて `000018` / `000074` も分割する。** 両者は「凍結表の据置きだけで緑になっている 2 本」であり、`M33` が 111 本を潰した瞬間に据置きが消えて規則 (4) の判定対象になる。**★`000018` は DDL 部が `S01` で層 A ／ `raw_data` の最終値が `S03` で層 B。★`000074` は `ALTER` が `S01` で層 A ／ `character_id` の値が `S06b` で層 B。**

**★★したがって 9 群案は「層 A が 4 本・層 B が 5 本」に分かれる**〔2026-09-13 是正＝`D-858`。旧記述は「7 群案は層 A が 2 本・層 B が 5 本」〕**。以下は旧記述： したがって 7 群案は「層 A が 2 本・層 B が 5 本」に自然に分かれる。⇒ 設計卓が §2.3 で述べた「統合の下限は 2 本」を満たしている。★1 本へ潰す案を採らなかったことが、結果として正しかった。**

**★★ただし帰結が 1 つある**——**`S02b` / `S03` / `S04` / `S05` / `S06b` のファイル名に `_data_` を必ず入れること**〔2026-09-13 是正＝`D-858`。旧記述は「S02〜S06」〕**。★`S02a` / `S06a` は層 A であり `_data_` を入れない。⇒ 入れないと層 A（AGPL）へ静かに落ちる**（`M28-05` が `000106` で踏んだのと同じ形）**。★着手時にこの命名を検査で確かめさせる。**

### 0.5.4 ★★設計卓の 1.0.0 の誤りを 1 件直す

**★1.0.0 §1-3 は「105 本の見込み」と書いた。⇒ 調査の実測は 104 組**（`000001`〜`000105`、欠番 `000012`）**であった。**

**★★ただし調査の基準は 2026-09-06 であり、その後 `M28-05` が `000106` を消費した。⇒ 現在は 105 組 / 210 ファイルである。**

**★★一般形＝「実測」と書かれた数にも基準日が要る。⇒ 基準日の後に動く面の数は、受け取った側が足し直す。**

---


---

## 1. 現在地（**★★数は要実査**）

| # | 事実 | 出所 |
|---|---|---|
| 1 | **次に払い出す連番＝`000107`** | ボード §2.2（`D-771`） |
| 2 | **恒久の欠番は `000012` の 1 件だけ** | `D-746`（**`000081` / `000096` は統合後に埋まった**） |
| **★3** | **⇒ 実ファイルは `000001`〜`000106` から `000012` を除いた 105 本の見込み。★`up` / `down` の対で 210 ファイル** | **★★設計卓の算出であり実測ではない**（武装中で `migrations/` を読めない）**。⇒ 着手時に数え直すこと** |
| 4 | **適用済みマイグレーション本文は編集しない**（`D-535`） | **★★本 MS はその例外になる。⇒ §4-1** |

---

## 2. ★★危険 4 件（**どの統合方式を採っても当たる**）

### 2.1 ★★【最重要】既存の利用者の DB が起動しなくなる

**★`golang-migrate` は `schema_migrations` に「いまどの版まで適用したか」を持つ。⇒ その版のファイルが無くなると落ちる。**

**★★実測の先例が在る**（`M28-05`・2026-09-07）**——`main` の `migrations/` は `000105` までしかなく、`000106` へ進んだ DB では `no migration found for version 106` で abort した。**

**⇒ 逆向きも同じである。105 本を数本へ潰すと、`schema_migrations` に `106` を持つ既存 DB は、その版のファイルが無いため落ちる。**

| # | 帰結 |
|---|---|
| **★★1** | **★★本 MS の中心はここである。⇒ 「利用者の手元の DB をどうするか」を決めない統合方式は成立しない** |
| **★★2** | **★リリース前なので「利用者はまだ居ない」と読みたくなる。⇒ しかし先行リリースの利用者が居る**（`docs/handover/followup-backlog.md` §A＝friend FB は 2026-06-28 の先行リリース初日に始まっている）**。★実質的な利用者は現在 1 人であるとの記録も在る**（`windows-firewall-rule-name-changed`）**。⇒ 「0 人」ではない** |
| **★★3** | **開発者自身の dev DB も同じ扱いになる。⇒ 統合の手番で、開発者の手元の DB が起動しなくなる。** **★★【2026-09-09 追記＝`D-790`】ここが本 MS で実際に効く唯一の DB である。⇒ 開発者は「その DB のデータを検証環境内で移行したい」と回答した**（§4.3）**。★「保全しない」の回答と両立する**——**配布物には載せないが、開発者の手元では引き継ぎたい、という意味である** |

### 2.2 ★★【重い】版数を名指ししたテストが壊れる

**★実測の先例が在る**（`D-714`・2026-09-05）**——`000012` を 1 本消しただけで `TestRun_MovesFrameColumnsDownRollback` が落ちた。逐語＝`down to v12: no migration found for version 12: read down for version 12 migrations: file does not exist`。**

**★★欠番そのものは `Up` では無害である。⇒ 壊れたのは「目標版数を直書きしたテスト」である。**

**★★設計卓が知っている名指しの実例:**

| サブ | 名指ししている版数 |
|---|---|
| **`M14-03f`** | `v91` / `v95` / `v99` / `v101`（`TestRun_M1403f_*Stage_*`） |
| **`M28-05`** | `106`（`m2805Terminus`） |
| **`M19-04c`** | `migrate_m1904c_test.go`（**★版数を持つ見込み。要実査**） |

**⇒ 105 本を潰すと、この族が全部当たる。★数は上の 3 件では済まない見込みである。**

**★★手順の先例が在る**（`D-709`）**——「実査 4」＝捨てブランチで実際に消して `go test ./...` と `make e2e` を回す。★机上の `grep` より実行のほうが確実である。**

### 2.3 ★★【重い】ライセンスの層が混ざる

**★★本プロジェクトは三層で配る**〔A＝`AGPL-3.0-or-later` ／ B＝`CC-BY-SA-4.0`（SF6 の事実）／ C＝`MIT`〕**。正本は `REUSE.toml`。**

**★★`REUSE.toml` は `migrations/*_data_*.sql` のグロブで層 B へ解決する**（`M28-05` の実測）**。⇒ 層 A と層 B は「ファイル名で」分かれている。**

**★★したがって「全部を 1 本に潰す」は採れない。⇒ 層 A のスキーマと層 B のデータが同じファイルに混ざり、ライセンスの帰属が壊れる。**

| # | 帰結 |
|---|---|
| **★★1** | **統合は「層ごとに 1 本ずつ」が下限である。⇒ 最低 2 本**〔スキーマ＝層 A ／ seed データ＝層 B〕 |
| **★2** | **`CHANGE-158` の命名規約**（層 B は `NNNNNN_data_*.sql`）**が、統合後のファイルにもそのまま効く** |
| **★3** | **`scripts/check-migration-license.sh` が `GAME_TABLES` を見て判定する。⇒ 統合後のファイルが検査を通ることを確かめること** |

#### 2.3.1 ★★【2026-09-07 追加＝`D-777`】`REUSE.toml` の本文を読んで、危険が 2 件増えた

**★★開発者から `REUSE.toml` の本文を受け取った**（2026-09-07）**。⇒ 2.0.0 までの記述は「`migrations/*_data_*.sql` のグロブがある」までしか掴んでいなかった。★実際にはもう 1 つ表がある。**

| # | 何が起きるか |
|---|---|
| **★★A** | **`REUSE.toml` の (4) は、既存マイグレ 72 本を層 B として 1 本ずつ列挙した凍結表である。⇒ `migrations/000003_*.sql` のような名指しが 72 行並んでいる。** **★★統合すると、この 72 行はすべて対象を失う。⇒ 死んだグロブになる。** **★同表の直上コメントは「適用済みマイグレは凍結されている**（`D-535`）**。⇒ この行は以後変わらない」と宣言している。★★つまり `D-535` の例外の射程は `migrations/` だけでなく `REUSE.toml` にも及ぶ。** |
| **★★B** | **旧系列を `migrations/legacy/` へ退避すると、72 本が黙って層 A**（`AGPL`）**へ落ちる。** **★★理由＝`REUSE.toml` 自身が書いているグロブの規則である**〔逐語＝「`*` は `/` を跨がない。`**` は跨ぐ」〕**。⇒ `migrations/000003_*.sql` は `migrations/legacy/000003_....sql` に当たらない。** **★★同じことが (3) の `migrations/*_data_*.sql` にも起きる。⇒ 退避した層 B のマイグレは 1 本残らず層 A になる。** **★★そして既定は `path = "**"` の層 A であるため、誰も赤くならないまま落ちる。** |

**★★B は §4.1-4**（旧 SQL・golden・歴史テストの保存方針）**の設計卓案に直接当たる。⇒ 「`migrations/legacy/` へ退避する」を採るなら、`REUSE.toml` の書き換えが必ず伴う。★退避と宣言は同じ手番で行うこと。**

**★★`precedence = "override"` が全表に付いている。⇒ ファイル内の SPDX ヘッダは宣言に勝てない。★したがって「退避したファイルの先頭にヘッダを書いて凌ぐ」という逃げ道は無い。**

**★★帰結＝`M33` は `REUSE.toml` を書き換える作業を必ず含む。⇒ `M33-03` の射程に入れること。★ライセンスの宣言を変えるのは開発者の判断である**（`CLAUDE.md` §10「セキュリティ関連の自己判断」に準じる）**。**

### 2.4 ★★【重い】golden 群が対象を失う

**★`cmd/seedgen` が生成した golden が `go test ./...` の `TestGolden_*MatchesRegeneration` 群で守られている。⇒ 設計卓が把握しているだけで `000026` / `000035` / `000072` / `000073` の 4 本**（`M28-05` の実測）**。**

**★★★【2026-09-13 実測へ更新＝`M33-01` §4-5】golden は 4 本ではなく 17 stem ＝ 34 SQL ファイルである。** 設計卓の把握が `000026` / `000035` / `000072` / `000073` の 4 本に留まっていた。**★`M33-03` の書き換え量が 4 倍以上になる。⇒ 同サブの見積りをやり直すこと。**

**★★統合するとこれらのファイルが消える。⇒ golden の対象が無くなり、「seed が生成規則と一致していること」の担保が外れる。**

**★★これは `SUPP-001` §5.5.4 (10) が「恒久のガードは `go test ./...` の golden 群である」と定めた、まさにそのガードである。⇒ 統合方式は、このガードをどう作り直すかを含んでいる必要がある。**

---

## 3. ★★受け取った案は、この 6 点にどう答えているか

**★1.0.0 は「案を受け取ったら次の 6 点が答えられているかを見る」と書いた。⇒ 受け取ったので、当てた。**

| # | 項目 | 調査の答え | 判定 |
|---|---|---|---|
| **★★1** | **既存 DB をどうするか** | 提案 §5 が 3 方針を並べる〔公開前に開発 DB を作り直す ／ 旧 DB を移行する ／ 開発側は旧系列で公開側だけ統合する〕**。★調査は「初回公開前なら最も単純」と述べるが、選んでいない** | **★★未決**（§4-1） |
| **★★2** | **層 A と層 B を分けたままにするか** | **★★調査資料に無い。⇒ 設計卓が §0.5.3 で 7 群へ当てた** | **★★決着**（S01・S07 が層 A、S02〜S06 が層 B。**命名を守れば分かれる**） |
| **★★3** | **golden の作り直し方** | 難所 1 として挙がる。**★「旧 SQL を `os.ReadFile` し生成結果と byte 比較する形なので、ファイル名の置換だけでは内容不一致になる」まで踏み込んでいる。⇒ ただし作り直し方は決めていない** | **★★未決**（§4-3） |
| **★★4** | **版数を名指しするテストの扱い** | **★★実測がある＝`Migrate`/`Steps` で歴史時点へ移動するコードが 28 ファイル。★方針もある＝「歴史テストと現行契約テストを分ける」**（提案 §6-5） | **★方針あり・実施は未**（`M33-03`） |
| **★5** | **統合後の連番の起点** | 提案 §2 ＝ **「新規専用系列を 1 から採番するなら `000001`〜`000007` 相当だが、既存系列と同じ番号を使う可否は系列切替設計に依存する」** | **★1 に従属する。⇒ 1 が決まれば決まる** |
| **★6** | **`down` を残すか** | 提案 §5 末 ＝ **「新 `down` は旧 104 本の `down` を連結せず、新 7 群の逆順で設計する」**。**★ただし今回は生成していない** | **★方針あり・生成は未**（`M33-02`） |

**★★6 点の内訳＝決着 1・方針あり 2・未決 3。⇒ 未決 3 件がそのまま §4 の判断になる。**

---

## 4. ★★開発者の判断が要る点（**★2.0.0 で 2 件が片づき、4 件が残った**）

### 4.0 ★★片づいた 2 件

| 旧 # | 事項 | 決着 |
|---|---|---|
| **1.0.0 §4-1** | **`D-535`**（適用済みマイグレの凍結）**の例外として本 MS を立ててよいか** | **★★開発者が承認した**（2026-09-07・逐語＝「例外としていいです」）**。⇒ `D-775` に例外として記録する** |
| **1.0.0 §4-4** | **`M33` として立ててよいか** | **★★開発者が承認した**（逐語＝「`M33` を立ててください」）**。⇒ `phase4-overview` 1.3.0 が `M25`〜`M33` へ改訂済み** |

### 4.1 ★★残る 4 件

| # | 事項 | 設計卓の案 | なぜ開発者へ回すか |
|---|---|---|---|
| **★★1**（**★★決着＝2026-09-09・`D-791`。⇒ §4.4 の案 (a)＝開発 repo も 7 群へ。★開発者の逐語＝「開発repoも7群へ」。★設計卓の推しと一致した**） | **開発 repo も新系列へ移すか。⇒ それとも公開スナップショットだけを統合するか** | **★★設計卓は「開発側も移す」を推す。⇒ 二系列を維持すると、以後の追加マイグレを 2 か所へ入れ続けることになり、本 MS の目的**（本数を減らして綺麗にする）**が半分しか達成されない。** **★★2026-09-09 に決定的な理由が 1 つ増えた＝二系列にすると、利用者が実際に走らせる 7 本を開発側が一度も走らせないことになる**（§4.4-3）**。** | **★★`docs/seed-data/README.md` の非公開 repo / 公開 repo の分離方針に直結する。⇒ 設計卓の射程を超える。** **★★ただし判断 2 の回答が、実質的に本件を先に選んでいる**（§4.3-4）**。⇒ 「検証環境内でのデータ移行がしたい」は、開発側の DB が新系列へ移ることを前提にしないと成立しない** |
| **★★2** | **開発 DB のどのデータを保全するか** | **★★設計卓は「保全しない」を推す。⇒ 調査の実測では `combos` / `setups` ほかユーザーデータ表は全て 0 行であり、保全すべき実データが存在しない**（提案 §1） | **★★決着**（2026-09-09・`D-790`）**＝「保全しない」。★ただし条件が 1 つ付いた。⇒ §4.3 を見ること。** **★開発者の逐語＝「保全しない。コンボもタグもメチャクチャなテストデータなのでユーザーが困る。ただし今後もテスト用にデータは使うので検証環境内でのデータ移行はしたい。」** **★★調査の「全て 0 行」は調査対象のツリーの話であり、開発者の手元の DB には実データが在ることが確定した** |
| **★★3**（**★★決着＝2026-09-09・`D-790`。⇒ 設計卓の案どおり「凍結した旧系列から一度だけ基準 DB を作り、それを正とする」**） | **新 baseline の再生成元と生成責務** | **★★設計卓は「凍結した旧系列から一度だけ基準 DB を作り、それを正とする」を推す**（調査 §4 の推奨手順と同じ）**。⇒ CSV から作り直す案は成立しない。理由＝`csv_db_sync_test.go` と `internal/seedgen/model.go` に CSV 非収録行が明示されている**（移動 279 行・連打版 4 行ほか） | **★★「今後の差分更新を CSV / seedgen のどちらの責務にするか」を含む。⇒ データ運用の方針であり、ハード列である** |
| **★★4**（**★★決着＝2026-09-09・`D-790`。⇒ 設計卓の案どおり `migrations/legacy/`**〔仮〕**へ退避する**） | **旧 SQL・golden・歴史テストを保存するか、退役させるか** | **★★設計卓は「旧系列を `migrations/legacy/`**（仮）**へ退避し、歴史テストはそちらを見る形へ移す」を推す。⇒ 消すと `D-535` の趣旨**（当時の前提の保存）**が丸ごと失われる。★本 MS が破ってよいのは「配布系列から外すこと」までであり、「歴史を消すこと」ではない** | **★★`D-196`＝消す変更は開発者の手番である。⇒ 退避先の名前も配布物の形に影響する** |

### 4.3 ★★【2026-09-09 新設＝`D-790`】「保全しない」には条件が 1 つ付いた

**★★開発者の逐語＝「保全しない。コンボもタグもメチャクチャなテストデータなのでユーザーが困る。ただし今後もテスト用にデータは使うので検証環境内でのデータ移行はしたい。」**

**★★設計卓の案は「保全しない」を「移行の手段も要らない」と読んでいた。⇒ それは誤りである。** **★正しくは「配布物には載せないが、開発者の手元では引き継ぎたい」である。**

| # | 帰結 |
|---|---|
| **★★1** | **新 baseline はユーザーデータを持たない。⇒ 7 群のうち `S07` が持つのは初期 `users` / `tags` だけであり、`combos` / `setups` / ユーザーが作ったタグは 1 行も入らない。★ここは案のままである** |
| **★★2** | **★★別に「検証環境内の移行手順」が要る。⇒ 旧系列の DB から、新 baseline で作った空の DB へ、ユーザーデータ表だけを移す手順である。★配布物に載せない開発者向けの道具である** |
| **★★3** | **★★手順が生む dump ファイルは「メチャクチャなテストデータ」そのものである。⇒ 公開スナップショットへ混ざってはならない。★置き場を `.gitignore` の下に固定すること**（**`docs/` の下へ置かないこと**——**`docs` は丸ごと公開される**＝`D-637` (3)） |
| **★★4** | **★★本件は判断 1 を実質的に選んでいる。⇒ 開発側が旧系列のままなら、移行すべきものが無い。★したがって「開発 repo も新系列へ移す」が前提である** |
| **★★5** | **★★実測できるのは開発者だけである。⇒ 対象の DB は開発者の手元にあり、設計卓も製造も行数を数えられない。★`M33-01` は「数えるスクリプトと手順」までを作り、実行は開発者の手番になる** |

**★★危険が 1 件ある＝dump と restore は ID 参照を跨ぐ。⇒ `combos` / `tags` / `setups` の外部キーと `sqlite_sequence` を同時に運ばないと、投入順で FK 違反になる。★「テーブルを 1 つずつ `INSERT` する」だけの手順にしないこと。**

**★★もう 1 件＝移行が通ることは、`M33-02` の同一性検証とは別に確かめる必要がある。⇒ 同一性検証はスキーマとシードの一致を見るものであり、ユーザーデータの往復は見ていない。**

---

### 4.4 ★★【2026-09-09 新設＝`D-790`。★★同日に決着した＝`D-791`】判断 1 は何を選ぶ問いか

> **★★決着＝案 (a)。⇒ 開発 repo の `migrations/` も 7 群へ置き換える**（開発者の逐語＝「開発repoも7群へ」・2026-09-09）**。**
>
> **★★本節は決着後も残す。⇒ 「なぜ (a) を選んだか」が `M33-01`〜`M33-03` の設計判断の前提になるためである。★とくに §4.4.4-1**（開発者の手元の DB が起動しなくなる）**は §4.3 の移行手順が必須であることの根拠である。**

**★★開発者から「これは何の話か？詳しく」と差し戻された。⇒ 表が「何を選ぶか」だけを書き、「そもそも何が問題か」を書いていなかった。★設計卓の書き方の誤りである。**

#### 4.4.1 ★★前提——公開は「別リポジトリを新しく作る」形である

**★`docs/seed-data/README.md` の逐語＝「`combomgr` は仮名（コードネーム）。完成時に正式名称の別リポジトリを新規作成し、不要・問題のあるファイルを削除した上で公開・OSS 化する予定。」**

**⇒ つまり公開の手番には、もともと「スナップショットを作る工程」が存在する。★`docs/seed-data/` を落とすのは、その工程の仕事である。**

#### 4.4.2 ★★問いの中身

**★★「マイグレーションの統合を、その工程に乗せるのか、それとも開発 repo そのものでやるのか」である。**

| 案 | 何をするか | 開発 repo の `migrations/` |
|---|---|---|
| **★★(a)** | **開発 repo の `migrations/` を 7 群へ置き換える。⇒ 公開スナップショットは今までどおり「ファイルを落とすだけ」の工程のままである** | **7 本になる** |
| **★★(b)** | **開発 repo は 105 本のまま維持し、スナップショットを作るときだけ 7 群へ差し替える** | **105 本のまま** |

#### 4.4.3 ★★(b) を採ると何が起きるか（**★設計卓が (a) を推す理由**）

| # | 帰結 |
|---|---|
| **★★1** | **★★利用者が実際に走らせる 7 本を、開発側が一度も走らせないことになる。⇒ CI もローカルも 105 本のほうを走らせ続ける。★同一性を確かめるのはスナップショットを作った瞬間の 1 回だけであり、その後に足したマイグレは誰も検証しない** |
| **★★2** | **★★以後の追加マイグレを 2 か所へ入れ続けることになる。⇒ そして追加は続く**——**`docs/seed-data/README.md` が「SF6 のアップデートで新キャラが追加されるたびに手入力からの seed 作成が発生する」と明記している** |
| **★★3** | **スナップショット工程の性質が変わる。⇒ 今は「ファイルを除外する」工程だが、(b) にすると「ファイルを差し替える」工程になる。★除外の漏れは目で見えるが、差し替えの誤りは見えない** |
| **★★4** | **本 MS の目的**（開発者の逐語＝「migration の本数を減らして綺麗にした状態でリリースをしたい」）**は (b) でも達成される。⇒ 公開物は 7 本になる。★ここだけを見ると (b) でもよい** |

#### 4.4.4 ★★(a) を採ると何が起きるか

| # | 帰結 |
|---|---|
| **★★1** | **開発者の手元の DB が旧系列のままでは起動しなくなる。⇒ §4.3 の移行手順が必須になる。★これが (a) の代償である** |
| **★★2** | **歴史テスト 28 ファイルが旧系列を見る形へ移る**（判断 4 で決着済み）**。⇒ `migrations/legacy/` へ退避する** |
| **★★3** | **系列は 1 つになる。⇒ 以後の追加は 1 か所であり、CI が走らせるものと利用者が走らせるものが一致する** |

**★★設計卓は (a) を推す。⇒ 理由は §4.4.3-1 の 1 点で足りる**——**利用者が走らせるものを開発側が走らせない構成は、検証したことにならない。**

**★★そして判断 2 の回答**（検証環境内でのデータ移行がしたい）**は、(a) を前提にしないと意味を持たない。⇒ 実質的に選ばれている。★確認だけを求める。**

---

### 4.2 ★★もう 1 件、本書の位置づけそのものに食い違いがある

| 立場 | 何と言っているか |
|---|---|
| **外部 AI の見立て** | **「公開を遅らせる必須作業ではなく、AI 枠に余裕があるとき分離環境で実装・検証し、十分に完成したら採用する整備候補」** |
| **開発者の要求** | **「やっぱり可能なら migration の本数を減らして綺麗にした状態でリリースをしたい」** |
| **1.0.0 の設計卓** | **`M32` の後・境界セッションの前。⇒ 事実上「必須」として置いた** |

**★★設計卓は 2.0.0 で外部 AI 側へ寄せる。⇒ 理由は §6 に書いた。**

---

## 5. ★★サブ構成（**★2.0.0 で 3 段に確定した。⇒ ただし起票は §4 の 4 件が決まってから**）

**★1.0.0 は 2 段の仮案だった。⇒ 調査で方式が具体化したので 3 段になる。**

| サブ | 射程 | 成果物 | なぜ分けるか |
|---|---|---|---|
| **`M33-01`** | **凍結と実測**——**公開基準のコミットを凍結し、旧 104 組**（現在は 105 組）**の対応表を作る。★あわせて 3 つの一覧を実測で作る**〔`Migrate`/`Steps` を呼ぶ 28 ファイル ／ golden 群が読む旧 SQL の一覧 ／ 層 A・層 B の仕分け〕 | **★対応表 3 種 ＋ 基準 DB**（凍結した旧系列から 1 度だけ作る）**＋ ★★§4.3 の移行手順の下ごしらえ**〔**ユーザーデータ表の一覧と FK の依存順を実測で出す。★行数を数えるのは開発者の手番である**〕 | **★★数える前に実施すると途中で止まる。⇒ `M30-01` と同じ形**（実測が最初の成果物） |
| **`M33-02`** | **7 群の生成と同一性の検証**——**S01〜S07 を生成し、本番の `modernc.org/sqlite` ＋ `golang-migrate` で旧最終状態と新 baseline を独立構築して比較する**〔全 DDL・全行・ID・`sqlite_sequence`・FK・NULL・JSON・CHECK・一意性〕**。★新 `down` も 7 群の逆順で作る** | **★`migrations/` の新系列 7 本 ＋ 比較の記録** | **★★ここが本体である。⇒ 比較が通らないうちは次へ進めない** |
| **`M33-03`** | **★★★【2026-09-19 畳み込み＝`D-907`】ガードと資料の作り直し。⇒ 歴史テストは分類して消すか新系列へ書き直す。★「旧系列側へ移す」という置き場は存在しない〔`M33-02` は `migrations/legacy/` を作っていない＝実測〕。⇒ 指示書 `M33-03` v1.0.0 §2.2 が正本である。★以下は起票前の記述：** ガードと資料の作り直し——golden / seedgen / CSV 同期テスト / dbtest テンプレート経路を整え、歴史テスト 28 ファイルを旧系列側へ移す。★生きた資料の更新と `code-facts` の再生成まで | **★テスト群 ＋ 資料の改訂 ＋ CHANGE 2〜4 本 ＋ ★★§4.3 の移行手順**〔**開発者向け。★成果物の置き場は `.gitignore` の下**〕 | **★★`M33-02` が緑になるまで、何を直せばよいかが確定しない** |

**★★並列にはできない。⇒ `migrations/` 全体を触るため、他のサブが 1 本でも走っていると危険である**（`D-335` と同じ理由が、より強く効く）**。**

**★★3 段とも同じセッションでは終わらない見込みである。⇒ `M33-01` の対応表を受け取ってから `M33-02` を起票する。**

---

## 6. ★★投入の時機（**★2.0.0 で改めた**）

**★1.0.0 は「`M32` の後・境界セッションの前」と書いた。⇒ 2.0.0 は「分離ブランチで先に作り、条件を満たしたときだけリリース前に採用する」へ改める。**

### 6.1 ★★なぜ改めたか

| # | 理由 |
|---|---|
| **★★1** | **完了条件が 6 つあり、そのうち 3 つ**（比較の全一致 ／ 系列切替の検証 ／ 歴史テストの分離）**は「やってみないと通るか分からない」形である。⇒ リリースの直列上に置くと、通らなかったときリリースが止まる** |
| **★★2** | **外部 AI の見立てと一致する**（§4.2）**。⇒ 「十分に完成したら採用する」という形を採るなら、通らなかった場合に旧系列のまま出せる** |
| **★★3** | **旧系列のままリリースしても、利用者に不都合は無い。⇒ 105 本という本数は開発側の見通しの問題であり、動作の問題ではない** |
| **★★4** | **`M31-01` が `P4M-023`**（`custom_state` 拡張）**でマイグレを消費しうる。⇒ 対象が動いている間に統合すると、`M33-01` の対応表が古くなる** |

### 6.2 ★★改めた形

| 段 | いつ | 何をする |
|---|---|---|
| **★1** | **`M32` と並行してよい** | **`M33-01`**（実測と対応表）**。★`migrations/` を書き換えないので、並列の危険が無い** |
| **★★2** | **`M31` 族と `M32` がすべて着地した後** | **`M33-02`**（生成と検証）**。★ここから `migrations/` を触る。⇒ 走行中のサブが `migrations/` を触らないこと** 〔**★★★【2026-09-19 改訂＝`D-900`】旧＝「並列 0 本が要る」。⇒ 理由が `migrations/` を名指ししている以上、`migrations/` を触らないサブは該当しない。★危険の実体はドリフト〔比べる相手が動くこと〕であり、見るべきは「マイグレを足すか」であって「走っているか」ではない。★詳細は `M33-02` 指示書 v1.2.0 §0.3**〕 |
| **★3**（**★★★2026-09-19 着地＝`D-907`**） | **`M33-02` の比較が全一致した場合のみ** | **`M33-03`**（ガードと資料）**。⇒ 一致しなければ、ここへ進まずに旧系列のままリリースする。** **★★★【2026-09-19 更新＝`D-903`】条件は満ちた。⇒ 全一致であり `D-902` で受理した。★三点セット発行済み＝`docs/instructions/M33-03-guards-docs-and-migration-path.md` v1.0.0 ＋ チェックリスト v1.0.0。★★本サブの着地まで `go test ./...` は赤である**〔158 テスト〕**。⇒ その間にリリースを打たない** |
| **★★4** | **境界セッション** | **★採用したかどうかにかかわらず開く。⇒ `M33` の可否は境界セッションの前提ではない** |

**★★したがって「綺麗にした状態でリリースしたい」という要求は満たしにいくが、リリースの可否をこの MS に握らせない。⇒ 開発者の逐語も「可能なら」である。**

---

## 7. 参照ドキュメント

| ID | パス | 参照する箇所 |
|----|------|------------|
| — | `docs/progress/20260906-squash-research/` | **★★受領した調査資料の全文**（14 点）**。⇒ 本 MS の素材である。★正本は `README.md` の読む順番に従うこと。** **★設計卓が暫定で置いた `M33-RESEARCH-01-report*.md` の 3 本は重複であり、2026-09-08 に削除済みである**（`D-776` / `D-784`） |
| — | `docs/process/parallel-board.md` | **`D-535`**（適用済みマイグレの凍結）／ **`D-709`**（削除の直前に実査 4）／ **`D-714`**（版数を名指ししたテストが壊れた実測）／ `D-746`（欠番は `000012` の 1 件）／ `D-771`（次は `000107`） |
| SUPP-001 | `docs/design/supp-001-detailed-design.md` | **§2.7**（マイグレの欠番と命名）／ **§5.5.4**（追随マイグレの規約。**とくに (6) (8) (10)**） |
| DES-002 | `docs/design/02-architecture.md` | §11.2（配布物の構成） |
| — | `docs/change-notes/CHANGE-158-notification.md` | **層 B の命名規約**（`NNNNNN_data_*.sql`） |
| — | `REUSE.toml` | **★層の解決**（`migrations/*_data_*.sql` のグロブ） |
| — | `scripts/check-migration-license.sh` | `GAME_TABLES` による判定 |
| — | `docs/instructions/phase4-overview.md` | §3（**★`M33` を足すなら本書の改訂が要る**） |

---

*以上、M33-overview **v2.5.0**。* **★★本書は 2.0.0 で外部 AI の調査を取り込み、2.3.0〜2.4.0 で開発者の判断 4 件をすべて受けた。⇒ 開発者判断待ちは 0 件であり、`M33-01` を起票できる。** **★★起票の手番で落としてはならないのは §4.3 である**——**「保全しない」と「検証環境内でのデータ移行はしたい」は両立する要求であり、片方だけを読むと手順が丸ごと消える。** **★★値打ちは §2 の危険 4 件にある**——**どの統合方式を採っても、既存 DB の起動 ／ 版数を名指ししたテスト ／ ライセンスの層 ／ golden のガードの 4 つには必ず当たる。** **★★とくに §2.3 は「全部を 1 本に潰す」を最初から否定する**——**層 A と層 B はファイル名で分かれており、混ぜるとライセンスの帰属が壊れる。⇒ 統合の下限は 2 本である。**
