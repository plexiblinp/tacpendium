# 指示書 M37-02: 仮想コントローラ UI の 10 件（`B04` `B05` `B07`〜`B14`）

| 項目 | 内容 |
|------|------|
| 文書ID | M37-02 |
| バージョン | **1.0.0**（2026-09-13・起票） |
| 作成日 | 2026-09-13 |
| 作成者 | 設計担当 Claude（設計卓） |
| 対象マイルストーン | **`M37`**（リリース前要望の束）。上位＝`docs/instructions/M37-overview.md` v1.4.0 |
| 推奨モデル | **標準実装**（実モデル名は `docs/human-notes/model-allocation.md`） |
| Plan Mode | **必須**（`CLAUDE.md` §9＝**全体に影響する UI 再構成を含む**） |
| レビュー | **必要**（**★チェックリスト＝`docs/instructions/reviews/M37-02-review-checklist.md` v1.0.0・2026-09-13 発行済み**） |
| CHANGE 消費 | **2〜3 本の見込み**（`DES-005` §6.4.3 ／ §6.1 または §5.7 ／ `web/CLAUDE.md` §1 台帳。**★自採番しない**＝`D-293`） |
| マイグレ消費 | **★無しの見込み**（**`B10` はブラウザ保持であり DB 列を足さない**）**。★消費する形になったら止めて請求する** |
| 前提サブ | **`M37-RESEARCH-01`**（完了済み）**／ `M37-01` の着地**（**★同じ `web/src/features/combo/` 面へ触れる**） |
| 射程 identity | **10 件**——`B04` `B05` `B07` `B08` `B09` `B10` `B11` `B12` `B13` `B14` |
| 配置 | `docs/instructions/M37-02-virtual-controller-vertical.md` |
| 出力 | `docs/progress/M37-02-completion-report.md` |

---

## 0. この指示書の特殊性

### 0.1 ★★★本サブは 10 件である。**8 件ではない**

**★`M37-overview` v1.3.0 までの §1.2 は群 (i) の見出しに「★8 件」と書いていた。⇒ 誤りである。**

**★★実際は `B04` `B05` ＋ `B07`〜`B14` の 8 件＝合わせて 10 件である**〔`B07`〜`B14` だけを数えて `B04` / `B05` を落としていた。**設計卓の計数誤り＝計測点 `M-172`。⇒ overview は v1.4.0 で是正済み**〕**。**

**⇒ 報告では 10 件すべてに判定を付けること**（overview §6-1）**。★8 件で閉じたら不合格である。**

### 0.2 ★★★`B08` `B09` `B10` `B13` `B14` は 1 つの目的である＝**縦の消費**

**★★★`D-857` の帰結＝`B10` と `B14` は同じ目的だと判明した。⇒ 別々に最適化しない。**

| ID | 表面の要求 | ★同じ目的への寄与 |
|---|---|---|
| `B08` | 必殺技の強度選択を右側へ | **★技数の多いキャラの縦を減らす** |
| `B09` | 必殺技部分を縦スクロール可能なグループへ | **★ページ全体の縦を、区画内の局所スクロールへ移す** |
| `B10` | クイック入力と入力説明欄を縮小 | **★★常時表示をやめて縦を空ける**〔§0.3〕 |
| `B13` | 強度ボタンの横幅を縮める | **★1 行に収まる数を増やして行数を減らす** |
| `B14` | 全体のボタンを小さくする | **★★★親の目的＝縦スクロールせずに全レシピを入力したい** |

**★★★`B14` は手段ではなく*達成判定*である**（`D-855`・開発者の逐語＝「2.私がチェックする」）**。⇒ 完了は開発者の実機確認で決まる。★「小さくした」と自己申告して閉じない。**

**★★★だから段 1 で先に測る**〔§2.1〕**。⇒ 変えてからでは「前」が取れない。★「減った」は前後 2 点が無いと言えない。**

### 0.3 ★★★`B10` の形は決着済みである。**設計を選び直さない**

**★★開発者の回答**（2026-09-13・逐語）**＝「問題点は文章量というよりも常にメッセージが表示される事。初回だけ読めば後は充分な文言がずっと表示されて、画面の縦を消費するのを問題視している」。**

**⇒ 要求の軸は*文字数*ではなく*縦の消費*である**（設計卓が一度読み違えた＝計測点 `M-171`）**。★3 文を短く書き直す案は要求を満たさない。**

**★★★採る形＝`DES-005` §5.9 の「制約の告知」と同じ型である。**

| # | 決まっていること | 出所 |
|---|---|---|
| **1** | **初回のみ自動表示する** | `DES-005` §5.9 の先例 ／ 開発者回答「1 は OK」 |
| **2** | **以降は明示操作で開く** | 同上 |
| **3** | **既読フラグはブラウザに保持する。⇒ DB 列を足さない** | 同上。**★先例＝`setplay-notice-seen-v1` / `onboarding-seen-v1`** |
| **4** | **★★文言 3 点はそのまま保つ。⇒ 圧縮しない・削らない** | **★`D-347` が守っているのは 3 点目である**〔§4.2〕 |
| **5** | **★キー名＝`gamepad-input-notice-seen-v1`**（`<feature>-seen-v1` 命名） | `web/CLAUDE.md` §1 の命名規約 |

**★★★`D-347` に反しない。** 同裁定が要求したのは「利用者に事前に伝える場所も決める」ことであって常設ではない。**実装コメント自身が「常設の注記にした〔§9.2-2 で形は自由〕」と書いている。⇒ 形は自由である。**

**★★決まっていないのは 1 点だけ＝畳んだときの見た目である**（開発者の逐語＝「2 は実物を見てから決めたいです。1 行の空きが縦スクロール量以前に不自然な空白に見える可能性もあるため」）**。**

**⇒ 着手を止めない。★★畳んだ形を*差し替えやすく*作ること**——**「ラベル 1 行を残す」と「完全に消して別の場所から開く」の両方を、1 か所の変更で試せる形にする。⇒ 実物を出してから開発者が決める。**

### 0.4 ★★★床はほとんど外れない。**外そうとしたら形を取り違えている合図である**

**★`M37-RESEARCH-01` §5.3 の実測＝直接参照はテスト 7 ファイル 31 行、ヘルパと呼出しが 4 ファイル 43 行。**

**★★★§0.3 の形を採ると、これらの床の大半は*そのまま通る*。**

| 床 | 通るか | なぜ |
|---|---|---|
| `gamepadRecipeInput.test.tsx:500`〔告知定数が 3 件・全項目が非空・「不具合ではなく」を含む〕 | **★★通る** | **★定数は変えない。⇒ 変えるのは*いつ出すか*である** |
| 同 `:477,490`〔未接続で非表示・接続時に表示〕 | **★通る** | **★観測できているときだけ出す条件は変えない**（`DES-005` §6.4.3） |
| 同 `:528,569`〔2 面の読取欄・告知が 2 面で同文〕 | **★通る** | **★2 面共有をやめない** |
| `VirtualController.test.tsx:529`〔provider なしなら告知・読取欄なし〕 | **★通る** | — |

**⇒ 足すのは「2 回目以降は畳まれている」「明示操作で開ける」の新しい床である。★引くのではない。**

**★★★床を外す必要が出たら、それは形を取り違えたか、`D-347` の射程へ踏み込んだかのどちらかである。⇒ 止めて設計伝達レポート §4 へ理由を書くこと**（overview §4.3＝**「床を外すこと自体は正しい。⇒ 黙って外すのが誤りである」**）**。**

### 0.5 ★★★`B04` はフロントだけでは終わらない

**★`M37-RESEARCH-01` §3.2 / §3.3 の実測＝サーバ生成のレシピ文字列は flag 全 10 値を `{}` で囲む。⇒ 日本語変換は 4 値だけであり、残り 6 値は `{` ＋ 内部識別子 ＋ `}` のフォールバックである。**

```text
combo 78: 立ち弱P > 立ち弱K {first_hit_cancel} {od_mh} > 立ち中K > 立ち中P {forward_jump} {neutral_jump} > 立ち強P {ジャスト} {ディレイ}
```

**★★★これが `B04`〔生の `{}` 等で囲んだ表現をそのまま出さない〕の実体である。⇒ バッジ側は既に日本語ラベルであり、漏れているのは*文字列生成の経路*である。**

**★生成元＝`internal/service/notation/resolver.go:21` の `flagText` / `applyFlags`。⇒ Go 側である。**

**★★★危険が 1 つ付く＝`recipe_cache`。** **実 DB に波括弧を含む保存文字列が combos 2 行・setups 1 行ある**（§3.3 の実測）**。⇒ 生成規則を変えても、キャッシュ済みの文字列は変わらない。★再生成の経路を段 1 で実査すること**〔§4.3〕**。**

### 0.6 ★★★`B05` / `B11` の「要否」は調査対象外である

**★`M37-RESEARCH-01` §6-3 の逐語＝「各 modifier の『必要/不要』や情報欄の『無いと困る/あると良い』。要否は調査対象外の判断」。**

**⇒ 数は出ている。★決まっていないのは*捨てる基準*である。★★本書が基準を与える**〔§2.5 / §2.6〕**。⇒ 製造が好みで選ばない。**

### 0.7 ★★着手前の版ゲート（**`M-169` の対処**）

| # | 確かめること | 満たさないとき |
|---|---|---|
| **1** | `docs/instructions/reviews/M37-02-review-checklist.md` が存在すること | **★止まって設計卓へ請求する** |
| **2** | `docs/progress/M37-RESEARCH-01-report.md` が `main` に在ること | **★本書はこれを土台にしている** |
| **3** | **`M37-01` が着地していること** | **★同じ `web/src/features/combo/` 面へ触れる。⇒ 走行中なら止まって請求する** |
| **4** | **`M32-02` のスクリーンショット撮影が*始まっていない*こと** | **★★本サブが着地すると撮り直しになる**（overview §4.2。**開発者が 2026-09-13 に停止済み**）**。⇒ 始まっていたら止めて知らせる** |
| **5** | `git log --oneline -5` で枝元を報告へ書く | — |

---

## 1. 前提として読むもの

| # | 資料 | 読む箇所 |
|---|---|---|
| **★★★1** | `docs/progress/M37-RESEARCH-01-report.md` | **§3 全体**（`B05` の実査）／ **§5 全体**（`B11` / `B10` / 床 / 縦寸法）／ **付録C**（床の全行） |
| **★★2** | `docs/design/05-screen-design.md` | **§6.4.3**（**★読取表示の要件。⇒ 3 区画は設計要件である**）／ §6.1（タブ構成）／ §5.7（コンボ登録・編集）／ **§5.9**（**★`B10` が倣う先例**） |
| **★3** | `docs/process/parallel-board.md` | **`P-59` の行**（`B10` の決着の全文）／ **`D-857`** ／ **`M-171`** ／ `D-347` の位置づけ |
| 4 | `docs/instructions/M37-overview.md` v1.4.0 | §1.2（10 件の受入条件）／ **§1.3**（`B10` の 3 文の逐語）／ **§4.1**（見た目で閉じない）／ §4.2 ／ §4.3 |
| 5 | `web/CLAUDE.md` | **§1**（**★ブラウザストレージ台帳。⇒ キーを足す先**） |
| 6 | `CLAUDE.md` | §10.X（ブラウザストレージの許容範囲） |
| 7 | `docs/design/04-notation-spec.md` | §2.3（modifier の flag / type の定義） |

---

## 2. やること

### 2.1 ★★★段 1 — 先に測る（**★コードを 1 行も変える前に済ませる**）

**★★★これを飛ばすと `B14` の達成を主張できない。⇒ 前後 2 点が要る。**

| # | 測るもの | 方法 |
|---|---|---|
| **1** | **★★コンボ登録・編集面の、仮想コントローラ区画の縦寸法**（px） | **★Playwright で実測する**（`make e2e-only P=<パターン>`。**★`pnpm exec playwright test` を直接叩かない**＝`CLAUDE.md` §11 / `D-599`） |
| **2** | **★技数の多いキャラと少ないキャラの 2 通り** | **★どのキャラを使ったかを報告に書く。⇒ ファミリー行数が縦を支配する**（`M37-RESEARCH-01` §5.4） |
| **3** | **★区画ごとの内訳**〔告知 / 読取 / タブ / 通常技 / 必殺技〕 | **★どこが効いたかを後で言えるようにする** |
| **4** | **`recipe_cache` の再生成経路の有無** | **★§0.5 の危険。⇒ 在る / 無い を事実として書く** |
| **5** | **10 件それぞれの現状**〔完了 / 一部 / 未着手〕 | **★overview §6-1。⇒ 「既に解消」には今回の要求を満たす証拠を付ける** |

**★★件数は数え直すこと**（計測点 `M-157`）**。⇒ `M37-RESEARCH-01` の値をそのまま転記しない。★数値の隣に「実測」「算出」を書き分ける。**

### 2.2 ★★★段 2 — `B10`（**告知を初回のみにする**）

**★形は §0.3 で決まっている。⇒ 選び直さない。**

| # | やること |
|---|---|
| **1** | **★★`GAMEPAD_INPUT_NOTICE_POINTS` の 3 文はそのまま残す。⇒ 定数を編集しない** |
| **2** | **★初回のみ自動表示し、以降は畳む。明示操作で開けるようにする** |
| **3** | **★既読フラグを `gamepad-input-notice-seen-v1` で保持する。⇒ 専用ヘルパ `web/src/lib/browser-storage.ts` 経由・try-catch**（`web/CLAUDE.md` §1） |
| **4** | **★★台帳へ 1 行足す**（`web/CLAUDE.md` §1。用途／設計書根拠／localStorage の別／実装状態）**。⇒ 同時に設計伝達レポート §4 へ CHANGE 原稿を書く。★自採番しない** |
| **5** | **★★★畳んだ形を差し替えやすく作る**〔ラベル 1 行を残す / 完全に消す の両方を 1 か所の変更で試せる〕**。⇒ 実物を出して開発者が決める** |
| **6** | **★2 面で同じ挙動にする**〔コンボ ／ セットプレイ〕**。⇒ 既読も 2 面で共有する。★面ごとに 2 個目のキーを作らない** |

### 2.3 ★★段 3 — `B11`（**読取欄の情報量**）

**★★★捨てる基準を与える＝「状態が変わらない案内文」は畳んでよい。「入力の結果」は畳まない。**

| 区画 | 判定 | 根拠 |
|---|---|---|
| 1 押している入力 | **★残す**〔設計要件〕 | **`DES-005` §6.4.3「3 つを出す」** |
| 2 確定したステップ | **★残す**〔設計要件〕 | 同上 |
| 5 解決できなかった入力 | **★★★残す。⇒ 3 つ目は名指しの要件である** | **同上「★3 つ目が要件である。黙って捨てると『入力されなかった』と『解決できなかった』を区別できない」** |
| 3 ショートカット | **★★未登録のときの案内文は畳んでよい**〔`未登録（コントローラ設定で…）`〕**。⇒ 直前操作結果は残す** | **★案内文は状態が変わらない。⇒ `B10` と同じ性質である** |
| 4 コマンド技入力 | **★★コマンド表を取得できていないときの案内文は畳んでよい。⇒ 入力中の表示と履歴は残す** | 同上 |

**★★★区画 1 / 2 / 5 を減らすには CHANGE が要る。⇒ 勝手に消さない。★減らしたいと判断したら、理由を設計伝達レポート §4 へ書いて請求する。**

**★★2 個目のストレージキーを作らないこと。⇒ 区画 3 / 4 は*条件表示*で畳む**〔登録済みか・コマンド表を持つか〕**。★既読フラグではない。**

### 2.4 ★★段 4 — `B08` / `B09` / `B13`（**必殺技パネルと強度ボタン**）

| # | ID | やること |
|---|---|---|
| **1** | `B08` | **★強度選択を右側へ配置する。⇒ 縦積みを横並びへ移す** |
| **2** | `B09` | **★必殺技部分に高さ上限と局所スクロールを置く**（**★`M37-RESEARCH-01` §5.4 の実測＝`SpecialMovePanel` に `overflow-y` 指定は 0 件**） |
| **3** | `B13` | **★強度ボタン**〔弱・中・強・OD〕**の横幅を縮める。⇒ ★★折り返して縦が増えていないことを段 6 で測り直す**〔§4.1〕 |

**★★`B09` の局所スクロールは `B14`〔縦スクロールせずに入力したい〕と矛盾しない。⇒ ページ全体の往復を、区画内の短い移動へ移す。★ただし「レシピが見えたまま入力できる」ことが目的であるから、区画を固定してレシピが視界から出ない形にすること。**

### 2.5 ★★段 5 — `B04` / `B05`（**modifier**）

| # | ID | やること |
|---|---|---|
| **1** | `B04` | **★★★フォールバックの `{内部識別子}` を無くす**〔§0.5〕**。⇒ 10 値すべてに表示語を与える。★`internal/service/notation/resolver.go` の `flagText`。★★`recipe_cache` の扱いを段 1 の実査に従って決め、決めた形と理由を報告へ書く** |
| **2** | `B04` | **★バッジ側の表現も揃える。⇒ `ModifiersEditor` の type-only タイトルが内部 type を返す分岐を潰す**（`M37-RESEARCH-01` §3.3） |
| **3** | `B05` | **★★表示語の食い違いを解消する。⇒ 実測で `low_jump` は UI「低ジャンプ」／ サーバ文字列「最低空」である**（§3.4） |
| **4** | `B05` | **★★★「不足しているもの」の判定＝*設計書に在って実装に無いもの*だけを不足と数える。⇒ 実測の集合差は 0 値である**（§3.4）**。★`micro_walk` は `DES-004` §2.1 が独立 move として不採用にしている。⇒ 不足に数えない** |
| **5** | `B05` | **★★★「不要なもの」の判定＝*DB 件数 0* を根拠にしない**（§3.3 の実測＝「DB 件数 0 を『画面に出ていない』とは数えない」）**。⇒ 利用者はまだいない**（`D-857`）**。★削るなら設計書の根拠が要る。⇒ 無ければ削らずに残す** |

**★★★結論として `B05` は「消す」より「揃える」が本体になる見込みである。⇒ ただしこれは見込みであって指示ではない。★段 1 の実査で違う事実が出たら、それを書いて請求すること。**

### 2.6 ★段 6 — `B07` / `B12` / `B14`（**配置と達成判定**）

| # | ID | やること |
|---|---|---|
| **1** | `B07` | **★ラッシュ版のボタン位置を通常技と特殊技で統一する** |
| **2** | `B12` | **★長いレシピを縦に表示する。⇒ `...` で要約せず全体を確認できるようにする。★どの部品の省略なのかを段 1 で特定してから触る** |
| **3** | `B14` | **★★★段 1 と同じ条件で測り直す。⇒ 区画ごとの前後を表で出す** |
| **4** | `B14` | **★★★開発者へ実機確認を請求する**〔`D-855`〕**。⇒ 「縦スクロールせずに全レシピを入力できたか」。★製造は判定しない** |

### 2.7 ★段 7 — 説明書の追随（**overview §4.2**）

**★`docs/usermanual/` の該当章を追随させる。⇒ 効くのは §5.7 中心である。**

**★★スクリーンショットは撮らない**（**開発者の手番。⇒ `M37` の全着地後**）**。**

---

## 3. やらないこと

| # | 内容 | 理由 |
|---|---|---|
| **1** | **★★★`DES-005` §6.4.3 の 3 区画**〔押している入力 / 確定したステップ / 解決できなかった入力〕**を消すこと** | **★設計要件である。⇒ CHANGE 無しに触らない**（§2.3） |
| **2** | **★★★`GAMEPAD_INPUT_NOTICE_POINTS` の文言を書き換えること** | **★形の決着は「いつ出すか」であって「何を書くか」ではない**（§0.3） |
| **3** | **★★床を黙って外すこと** | **overview §4.3。⇒ 外す必要が出たら理由を書いて請求する**（§0.4） |
| **4** | **★★DB 列を足すこと・マイグレを消費すること** | **`B10` はブラウザ保持で足りる。⇒ 消費する形になったら止めて請求する** |
| **5** | **★2 個目以降のストレージキーを作ること** | **§2.3。⇒ 台帳が膨らむ。★区画 3 / 4 は条件表示で畳む** |
| **6** | **★`A`**〔運び量 UI〕**へ手を出すこと** | **`M37-01` の射程である** |
| **7** | **★`B01` `B02` `B03` `B06` `B15` へ手を出すこと** | **`M37-03` / `M37-04` の射程である** |
| **8** | **★★スクリーンショットを撮ること** | **開発者の手番。⇒ `M37` の全着地後**（overview §4.2） |
| **9** | **★★★`docs/handover/followup-backlog.md` を編集すること** | **`D-838`。⇒ 停止時記録も設計伝達レポート §4 へ原稿として書く** |
| **10** | **★設計書本体を書き換えること** | **`CLAUDE.md` §8。⇒ CHANGE 原稿を設計伝達レポート §4 へ書く** |

---

## 4. ★★危険（**先に読むこと**）

### 4.1 ★★★最大の危険＝**横を縮めて縦が増えること**

**★`B13`〔強度ボタンの横幅を縮める〕と `B14`〔縦を減らす〕は、素朴にやると衝突する。⇒ 横を詰めるとラベルが折り返し、行の高さが増える。**

**★★実測の裏付け＝`ControllerButton:59` の move サイズは `min-h 3.25rem` であり、上下 `py-2` を持つ**（`M37-RESEARCH-01` §5.4）**。⇒ 最低高が family 数だけ繰り返される構造である。**

**⇒ ★★★段 6 で測り直すまで `B13` を完了扱いにしない。★「横は縮んだ」は達成ではない。**

### 4.2 ★★2 番目＝**`D-347` の射程へ踏み込むこと**

**★`D-347` が守っているのは 3 点目**〔これは不具合ではなく、入力を取りこぼさないための意図的な設定である〕**であり、実装コメント `GamepadInputNotice:6` が「★3 点目を落とさないこと」と名指ししている。**

**★★★畳むことは落とすことではない。⇒ 明示操作で開ける限り、伝える場所は在る。**

**★★しかし「初回も出さない」「3 点目だけ消す」へ寄せると `D-347` に反する。⇒ そこへ寄りかけたら止めて請求すること。**

### 4.3 ★★★3 番目＝**`recipe_cache` を置き去りにすること**

**★★`B04` で生成規則を変えると、新しく作る文字列だけが変わる。⇒ 保存済みの `recipe_cache` は古い `{内部識別子}` を持ち続ける**（実測＝combos 2 行・setups 1 行）**。**

**★★★「新規は直った」で閉じない。⇒ 段 1 で再生成経路を実査し、採った形と理由を報告へ書くこと。**

**★★★データを黙って書き換えないこと。⇒ 既存行の `recipe_cache` を更新する形を採るなら、それは設計判断であるから設計伝達レポート §4 へ書いて請求する。**

### 4.4 ★★4 番目＝**10 件を 1 つの完了条件で閉じること**

**★★★別々の要求と受入条件は消さない**（overview §1.2 の冒頭）**。⇒ 束ねたのは「同じ面を触る」ためであり、要求を 1 つに畳むためではない。**

**⇒ 報告は 10 行の表で出すこと。★「仮想コントローラを改善した」では不合格である。**

### 4.5 ★5 番目＝**本サブが大きくなりすぎること**

**★★本サブは `M37` 最大である。⇒ 段 4**〔必殺技パネル〕**が想定より大きいと分かったら、黙って広げずに止めて分割を請求すること。**

**★★★製造は自分でサブを割らない。⇒ サブ分割は開発者の承認事項である**（`D-855`）**。**

---

## 5. テスト

| # | 対象 | 内容 |
|---|---|---|
| **5-1** | **★★`B10` の新しい床** | **初回は展開・2 回目以降は畳まれる ／ 明示操作で開ける ／ 既読フラグが 2 面で共有される** |
| **5-2** | **★★★既存の床が通ること** | **`gamepadRecipeInput.test.tsx:500` を含む 7 ファイル 31 行が緑のままであること**（§0.4。**★落ちたら形を取り違えている**） |
| **5-3** | **★★`B04` の破壊確認** | **★flag 10 値すべてで `{` ＋ 内部識別子 ＋ `}` が出ないこと。⇒ ★★逆向きも見る＝表示語を 1 つ外すと再びフォールバックが出ること** |
| **5-4** | **★★`B11` の非退行** | **区画 1 / 2 / 5 が従来どおり出ること。⇒ 解決できなかった入力の 6 理由が消えていないこと** |
| **5-5** | **★★★縦の実測** | **段 1 と段 6 を同じ条件で測る**（Playwright。**★`make e2e-only P=<パターン>` を使う**＝`D-599`） |
| **5-6** | 全数 | **`go test ./...` ／ `pnpm test` ／ `make e2e`（全数）** |
| **5-7** | **★機械検査** | **`bash scripts/check-browser-storage-keys.sh`**（**★★台帳へ足す前は赤になる。⇒ 足してから緑を確認する**）**／ `bash scripts/check-import-order.sh` ／ `bash scripts/check-enum-sync.sh`** |

---

## 6. 完了条件

| # | 条件 |
|---|---|
| **1** | **★★★10 件それぞれに判定**〔完了 / 一部完了 / 未着手〕**が付き、「既に解消」には*今回の要求を満たす証拠*が付いている**（overview §6-1 / §6-2） |
| **2** | **★★★段 1 と段 6 の縦寸法が、同じ条件・同じキャラで前後 2 点そろっている。⇒ 区画ごとの内訳が出ている** |
| **3** | **★★`B14` の実機確認を開発者へ請求済みである**（`D-855`。**★製造は判定しない。⇒ 請求したことが完了条件であり、返答を待って止まるのではない**） |
| **4** | **★★`B10` が §0.3 の 5 点をすべて満たしている。⇒ 文言 3 点が変わっていない** |
| **5** | **★★★畳んだ形が差し替えやすく作られている**〔ラベル 1 行 / 完全に消す の両方を試せる〕 |
| **6** | **★台帳**〔`web/CLAUDE.md` §1〕**へ 1 キー追加され、`check-browser-storage-keys.sh` が緑である** |
| **7** | **★★`B04` の `recipe_cache` の扱いが、採った形と理由つきで報告に在る** |
| **8** | **★既存の床 7 ファイル 31 行が緑である。⇒ 外したものが在るなら、理由が設計伝達レポート §4 に在る** |
| **9** | **★`docs/usermanual/` の該当章が追随している**（§2.7。**★スクリーンショットは除く**） |
| **10** | **★`docs/progress/progress-log.md` へ 1 行追記した**（`CLAUDE.md` §8） |
| **11** | **★CHANGE 原稿を設計伝達レポート §4 へ書いた。⇒ 自採番していない**（`D-293`） |
| **12** | **★完了報告を `bash scripts/check-md-emphasis.sh <file>` で見た**（`D-775`） |

---

## 7. ★開発者への確認事項

| # | 事項 | 設計卓の暫定案 |
|---|---|---|
| **1** | **畳んだときの見た目**〔`P-59` (2)〕 | **★★★実物を見てから決める**（2026-09-13 開発者回答）**。⇒ 本サブは両方を試せる形で作り、実物を出す。★製造から請求すること** |
| **2** | **`B14` の達成判定** | **★★開発者の実機で「縦スクロールせずに全レシピを入力できたか」を 1 回確認する**（`D-855`）**。⇒ 寸法の数値では判定しない** |
| **3** | **`B05` で削る modifier が出た場合** | **★★設計卓の案＝削らない**（§2.5-5）**。⇒ 利用者がいないため DB 件数 0 は不使用の証拠にならない。★削るべき根拠が出たら請求すること** |

---

## 8. 参照ドキュメント

| ID | パス | 使う箇所 |
|----|------|---------|
| **M37-RESEARCH-01 報告** | `docs/progress/M37-RESEARCH-01-report.md` | **★★§3 / §5 / 付録C** |
| DES-005 | `docs/design/05-screen-design.md` | **§6.4.3 / §6.1 / §5.7 / §5.9** |
| DES-004 | `docs/design/04-notation-spec.md` | §2.1 / §2.3 |
| overview | `docs/instructions/M37-overview.md` | §1.2 / §1.3 / §4.1 / §4.2 / §4.3 / §6 |
| ボード | `docs/process/parallel-board.md` | **`P-59` / `D-857` / `M-171` / `D-347`** |
| 台帳 | `web/CLAUDE.md` | **§1** |
| 規約 | `CLAUDE.md` | §8 / §9 / §10.X / §11 |

---

*以上、指示書 M37-02 **v1.0.0**。* **★★★段 1 で先に測ること**——**変えてからでは「前」が取れず、`B14` の達成を誰も主張できなくなる。** **★★次に重いのは §0.4 である**——**床を外そうとしたら、それは形を取り違えている合図である。**
