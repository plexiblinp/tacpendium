# 指示書 M27-01: ヒット種別の追加と区分名の整理

| 項目 | 内容 |
|------|------|
| 文書ID | M27-01 |
| バージョン | **1.0.0**（2026-09-02） |
| 作成日 | 2026-09-02 |
| 作成者 | 設計担当 Claude（設計卓／フェーズ4 期） |
| 対象マイルストーン | **`M27`**（`α②` 画面品質）。上位＝`docs/instructions/M27-overview.md` v1.2.0 |
| 推奨モデル | **高精度実装**（**★列挙定数の両側同期 ＋ 絞り込み・比較・エクスポートへの波及**。実モデル名は `docs/human-notes/model-allocation.md` が正本） |
| Plan Mode | **必須**（`CLAUDE.md` §9＝スキーマ・API 契約に及ぶ変更） |
| レビュー | **必要**（チェックリストは段 2 の着手承認後に設計卓が起こす） |
| CHANGE 消費 | **1 本の見込み**（**★段 1 の承認後に設計卓が起票する。★製造は自採番しない**） |
| マイグレ消費 | **0〜1 本**（**★相手サイズの DB 値を変える場合のみ DML 1 本。★設計卓へ請求する**＝`D-293`） |
| 配置 | `docs/instructions/M27-01-hit-type-and-size-labels.md` |
| 出力 | **`docs/progress/M27-01-completion-report.md`**（**★必須**。§3.3） |

---

## 0. この指示書の特殊性

### 0.1 ★★決定材料の段と実装の段が 1 本に同居する

**本サブは「決定材料サブ」**（`phase4-overview` §8.5.2・`D-676`）**の性格を前段に持ち、後段は通常の実装である。**

| 段 | 中身 | 性格 |
|---|---|---|
| **段 1** | **★ヒット種別の列挙を確定させ、画面モックで名前を決める** | **決定材料**（実装差分 0） |
| **段 2** | **列挙定数を `internal/model/` と `web/src/constants/` の両側へ足す** | 実装 |
| **段 3** | **表示・絞り込み・比較・エクスポートへ通す** | 実装 |

**★★1 本にした理由**——**段 1 と段 2 は同じ列を触り、段 1 の結論がそのまま段 2 の入力になる。⇒ 分けると引き継ぎのコストが工程の中身より大きい。**

### 0.2 ★★段 1 の成果物を出したら止まる

**★段 1 の成果物**〔列挙の確定案 ＋ 画面モック〕**を提示し、開発者の承認を待つこと。★承認前に段 2 へ進まない。**

**理由 2 つ。**

| # | 理由 |
|---|---|
| **1** | **★★列挙が確定していない**（`D-674`）。**開発者の逐語＝「ひょっとしたら他にも思いつくかもしれません。実際のマイルストーンまでにはこれも整理します。」⇒ 本サブの段 1 がその「整理」である** |
| **2** | **★名前が未定である**（開発者の逐語＝「壁やられは表記揺れがあるが……正式な名前は画面モックなどを出しつつ決める」）。**⇒ 名前を決めてから定数を書く。逆順にすると定数のリネームが要る** |

### 0.3 ★★完了報告を必ず作る

**出力は `docs/progress/M27-01-completion-report.md` である**（`D-676`）。

**★理由＝`check-progress-log-index.sh` は `*completion-report*.md` しか走査しないため、別名だと索引行が落ちても永久に緑になる**（followup `report-class-subs-outside-index-check`。**★該当は 17 件あり、`M26-01` で実際に問題になった**）。

**★段 1 の成果物を別ファイル**（`docs/progress/M27-01-hit-type-proposal.md` 等）**に置いてもよいが、完了報告は別に必ず作ること。**

---

## 1. 前提として読むもの

| 対象 | 何を読むか |
|---|---|
| `docs/instructions/M27-overview.md` v1.2.0 | **★§3.1.1（ヒット種別・現時点で 3 種）／§3.1.2（相手サイズ）／§3.1.3（重い理由 4 つ）** |
| `docs/handover/code-facts.md` | **★引く。想定で書かない** |
| `CLAUDE.md` §4 | **★列挙的文字列定数はパッケージ定数として定義し、フロント側と同期する** |
| `docs/design/03-data-model.md` | `hit_type` の現行定義 |
| `docs/design/05-screen-design.md` | 表示・絞り込みの現行仕様 |
| `docs/design/06-validation.md` | 該当するバリデーション |
| `docs/handover/followup-backlog.md` | **`opponent-size-medium-to-standard` ／ `opponent-size-large-variants-defer` ／ `csv-import-opponent-size-whitelist-stale`** |

---

## 2. やること

### 2.1 段 1 — ヒット種別の列挙を確定させる（**★最初の一手**）

#### 2.1.1 現状を数える

| # | 確かめること |
|---|---|
| **★★1** | **`hit_type` の現在の値の全数と一覧**（**★母数**）。**`M18-01` の 4 値化以後に増えた値が在るか** |
| **★★2** | **`internal/model/` と `web/src/constants/` の現在の同期状態**（`scripts/check-enum-sync.sh` を回す） |
| **★3** | **`hit_type` を読む箇所の全数**〔一覧の絞り込み ／ 比較 ／ エクスポート ／ バリデーション ／ E2E〕。**★区分ごとの母数を出すこと** |
| **★4** | **既存の値の表示名が、どこで決まっているか**（定数か ／ i18n か ／ 直書きか） |

#### 2.1.2 ★★列挙の確定案を出す

**現時点で開発者から挙がっているのは 3 種である。★これは確定ではない。**

| # | 挙がっているもの |
|---|---|
| 1 | **ドライブインパクトがヒットし、壁やられが発生した** |
| 2 | **ドライブインパクトをガードし、壁やられが発生した** |
| 3 | **ドライブインパクトがパニッシュカウンターした** |

**★★やること**——**上記 3 種を起点に、次を添えて確定案を出す。**

| # | 添えるもの |
|---|---|
| **★★1** | **既存の値との重なりが無いか。★「これは既存の X で表現できるのではないか」を 1 件ずつ判定する** |
| **★★2** | **★源泉 3 行が挙げていた他の候補**〔`SM-033` / `SM-106` / `SM-116`。**ガードクラッシュ ／ ジャストパリィ反撃 ／ インパクト壁やられ ／ パニッシュカウンター 等**〕**を並べ、「今回入れるか／入れないか」を開発者が選べる形にする。★設計卓が採否を決めていない** |
| **★3** | **値の識別子案**（`snake_case`）**と表示名案**（日本語）**を分けて出す。★識別子は後から変えにくい** |
| **★4** | **★足すことで壊れるものが無いか**——**既存データの `hit_type` は変えない。⇒ 「足すだけ」で済むかを確かめる** |

#### 2.1.3 画面モック

**★★どこに出るかを 1 面ずつ示すこと。**

| # | 面 |
|---|---|
| 1 | **コンボ登録・編集のヒット種別の選択** |
| 2 | **一覧の絞り込み**（**★`M27-03` の `P4M-021`「ヒット種別フィルタ短縮」と同じ面である。⇒ 短縮後の見た目も併せて示す**） |
| 3 | **コンボ詳細・比較の表示** |

**★モックの形式は問わない**〔テキストの画面案 ／ 既存画面のスクリーンショットへの注記 ／ HTML〕。**★「名前がどう見えるか」が分かればよい。**

#### 2.1.4 ★★段 1 を出したら止まる

**成果物を提示し、開発者の承認を待つ。★承認前に段 2 へ進まない**（§0.2）。

### 2.2 段 1 — 相手のサイズの区分名

**開発者の逐語＝「相手のサイズは適当に中、大1、大2としていたが、中は標準にしたい。」**

| # | やること |
|---|---|
| **★★1** | **現在の値の全数と、値がどこで決まっているか**（`internal/model/` ／ `web/src/constants/` ／ CSV 取込のホワイトリスト ／ DB の実データ） |
| **★★2** | **「中」→「標準」が、DB の値の変更なのか表示名だけの変更なのかを判定して提案する。★どちらが安いかではなく、どちらが正しいかで判断すること** |
| **★3** | **DB の値を変えるなら**——**DML マイグレ 1 本 ＋ CSV 取込のホワイトリストの更新**（`csv-import-opponent-size-whitelist-stale` と同じ列）**＋ 既存データの移行。⇒ 影響の全数を出す** |
| **★★4** | **「大1」「大2」の整理は本サブに含めない**（followup `opponent-size-large-variants-defer`＝`要判断`）。**★同じ列を触るが判断が済んでおらず、一緒に直そうとすると止まる。⇒ 触らない** |

### 2.3 段 2 — 列挙定数を両側へ足す（**★段 1 の承認後**）

| # | やること |
|---|---|
| **★★1** | **`internal/model/` へパッケージ定数として定義する**（`CLAUDE.md` §4） |
| **★★2** | **`web/src/constants/` へ対応する定数を作成し、画面側から参照する。★リテラル文字列を画面側へ散在させない** |
| **★★3** | **`scripts/check-enum-sync.sh` を回し、ベースラインが動いたら理由を報告に書いてから更新する** |
| **★4** | **相手サイズも同じ手番で同期する。★2 系統ぶんである** |

### 2.4 段 3 — 表示・絞り込み・比較・エクスポートへ通す

| # | やること |
|---|---|
| **★★1** | **§2.1.1-3 で数えた全箇所へ通す。★数えた母数と、通した箇所の数が一致することを報告に書く** |
| **★★2** | **絞り込みは `M27-03` の `P4M-021`（ヒット種別フィルタ短縮）と同じ列を触る。⇒ 本サブでは「新しい値が絞り込みに出る」ところまで。★短縮の設計は `M27-03` の担当である** |
| **★3** | **エクスポート**（CSV）**の列契約に触れるか判定する。★触れるなら報告して止まる**（`csv-column-contract-unobserved-in-pr`） |
| **★4** | **テストを足す**——**新しい値が保存でき、絞り込みで拾え、往復で失われないこと** |

---

## 3. 出力

### 3.1 段 1 の成果物

**`docs/progress/M27-01-hit-type-proposal.md`**（**★名前は任意。完了報告とは別ファイルにする**）。

| # | 中身 |
|---|---|
| 1 | **§2.1.1 の現状の全数**（母数つき） |
| 2 | **§2.1.2 の列挙の確定案**（**★源泉 3 行の他候補も並べる**） |
| 3 | **§2.1.3 の画面モック** |
| 4 | **§2.2 の相手サイズの判定と提案** |

### 3.2 完了報告（**★必須**）

**`docs/progress/M27-01-completion-report.md`**（`D-676`）。

| # | 書き方の規約 |
|---|---|
| **★★1** | **すべての件数に母数を付ける** |
| **★★2** | **段 1 で開発者が確定させた列挙を、逐語で書く。★「承認された」とだけ書かない** |
| **★★3** | **§2.1.1-3 で数えた箇所の数と、実際に通した箇所の数を並べて書く。★一致しないなら理由を書く** |
| **★★4** | **★やらないと決めた項目が在れば、その 1 行を書く**（`phase4-overview` §8.5.1・`D-675`）。**★「触れなかった」と「やらないと決めた」を区別する** |
| **★5** | **`check-enum-sync.sh` のベースラインが動いたか。動いたなら理由** |
| **★6** | **確かめられなかったものは「確かめられなかった」と書く** |

**★`docs/progress/progress-log.md` へ追記すること**（`CLAUDE.md` §8）。

---

## 4. やらないこと

| # | やらないこと |
|---|---|
| **★★1** | **段 1 の承認を待たずに段 2 へ進むこと**（§0.2） |
| **★★2** | **「大1」「大2」の整理**（§2.2-4。**★`要判断` である**） |
| **★★3** | **`M27-02` / `M27-03` の項目**（**★`P4M-021` のフィルタ短縮の設計は `M27-03`。本サブは「新しい値が出る」ところまで**） |
| **★★4** | **既存の `hit_type` の値の変更・削除**（**★足すだけ。★既存データを動かさない**） |
| **★★5** | **`docs/design/` の編集**（`CLAUDE.md` §8。**★該当箇所を報告するだけ**） |
| **★★6** | **CHANGE 番号・マイグレ連番の自採番**（**★設計卓へ請求する**＝`D-293`。**★`M14-03f` と `M28-01` も同じ帯を使う見込みであり衝突しうる**） |
| **★7** | **`followup-backlog.md` の編集**（`D-382`） |
| **★8** | **新しいツールの導入**（**★定義は `M26-overview` §5.2。★射程内＝既存ツールの別サブコマンド。★迷ったら止まる側に倒す**＝`D-671`） |

---

## 5. 完了条件

1. **★★段 1 の成果物が在り、開発者の承認を得ている。**
2. **★段 1 で確定した列挙が、完了報告に逐語で書かれている。**
3. **`internal/model/` と `web/src/constants/` の両側に定数が在り、`scripts/check-enum-sync.sh` が緑。**
4. **★§2.1.1-3 で数えた全箇所へ通っている**（**★数えた母数と通した数を並べて書くこと**）。
5. **相手サイズの「中」→「標準」が反映されている**（**★DB 値を変えたなら CSV 取込のホワイトリストも同期していること**）。
6. **新しい値が保存でき、絞り込みで拾え、往復で失われないテストが在る。**
7. **`go test ./...` が緑。`cd web && pnpm test` が緑。`make e2e` が緑。**
8. **検査 7 本が緑**（`check-artifact-integrity` / `check-md-emphasis` / `check-instruction-format` / `check-doc-refs` / `check-stop-discipline` / `check-progress-log-index` / `check-doc-inventory`）。
9. **`docs/progress/M27-01-completion-report.md` が在り、`progress-log.md` へ追記されている。**

---

## 6. 参照ドキュメント

| ID | パス |
|---|---|
| 上位 overview | `docs/instructions/M27-overview.md` v1.2.0 |
| ledger | `docs/progress/M25-RESEARCH-01-report.md` §3 |
| 源泉 | `docs/human-notes/phase4-memo.txt`（`P4M-025`）／ `docs/human-notes/Memo_Someday.txt` |
| 列挙定数の同期 | `CLAUDE.md` §4 ／ `scripts/check-enum-sync.sh` |
| 決定材料サブの型 | `docs/instructions/phase4-overview.md` §8.5.2 |
| 「射程の上限」 | 同 §8.5.1 |
| code-facts | `docs/handover/code-facts.md` |

---

## 7. 開発者への確認事項

**★段 1 の成果物と同時に返すこと。★本サブの着手前に必要なものは無い。**

| # | 何を確認するか |
|---|---|
| **★★1** | **ヒット種別の列挙の確定**（**★現時点で 3 種。★源泉 3 行の他候補を並べたうえで、今回入れるものを選んでもらう**） |
| **★★2** | **各値の表示名**（**★画面モックを見たうえで**） |
| **★3** | **相手サイズの「中」→「標準」が、DB の値の変更か表示名だけの変更か**（**★製造が判定して提案し、開発者が決める**） |

---

*以上*
