# M27-overview: `α②` 画面品質（エディタ・一覧・詳細）

| 項目 | 内容 |
|------|------|
| 文書種別 | 補足資料（overview。CHANGE 通知書対象外・自由改訂） |
| バージョン | **1.6.0**（2026-09-03。**★★`M27-02a` は完了・受理した＝`D-694`。`CHANGE-152` 反映済み**。**★`SD-015` / `SD-016` は開発者裁定で対応不要になった**〔6 件 → 実施 4 件〕） ／ **1.5.0**（2026-09-02。**★★`M27-01` は完了・受理した＝`D-687`**。**`hit_type` 8 値 ／ `opponent_size` 4 値。`CHANGE-151` 反映済み。★§3.1.1 の「列挙は確定していない」は決着した**） ／ **1.4.0**（2026-09-02。**★★`代表 ID` 欄による機械的な突合で、境界のずれがもう 4 件見つかった＝`D-686`**。**⇒ `M27` が負うのは 23 → 19 identity。`SD-010` / `SD-011` / `SD-019` / `SD-022` は `M30` へ**） ／ **1.3.0**（2026-09-02。**★★`M27-02` を `M27-02a` / `M27-02b` の 2 本へ割り、両方の指示書を起票した＝`D-683`**。**⇒ `M27` は 3 サブから 4 サブになった**） ／ **1.2.0**（2026-09-02。**★★ヒット種別は「2 種で確定」ではなかった＝`D-674`**。**★列挙は `M27-01` 起票時に開発者が確定させる**） ／ 1.1.0（2026-09-02。**★★開発者承認済み**。確認事項 4 件の決着を反映＝`D-669`。**★`M27-01` の射程が縮み、新規 2 件が入った**） ／ 1.0.0（2026-09-02・起票） |
| 作成日 | 2026-09-02 |
| 作成者 | 設計担当 Claude（設計卓／フェーズ4 期） |
| 対象マイルストーン | **`M27`**（フェーズ4 で最大）。上位＝`docs/instructions/phase4-overview.md` |
| 位置づけ | **`M27` のサブ構成・全件処遇・CHANGE 見込みの正本** |
| 前提 | `docs/progress/M25-RESEARCH-01-report.md` §3 ledger ／ ボード `D-646`（`α②` の判定規則）／ `docs/human-notes/phase4-memo.txt`（`P4M-*` の源泉）／ `docs/human-notes/Memo_Someday.txt`（`SD-*` の源泉） |
| 配置 | `docs/instructions/M27-overview.md`（active＝直下。フェーズ4 終了時に `docs/instructions/phase4/` へアーカイブ） |

---

## 0. 本書の確度

- **★★本書は 2026-09-02 に開発者承認された**（確認事項 2＝「承認」）。
- **★★数を 2 つ是正した**（§2）——**(1) `phase4-overview` の「42 行」は ledger の raw 行数であり、名寄せ後は 33 identity である ／(2) 面での機械分割に境界のずれがあり、セットプレイ・確定反撃の 9 件と仮想コントローラの 1 件は `M27` ではなく `M31` / `M30` の担当である。**
- **⇒ `M27` が実際に負うのは 23 identity である。**
- **★設計卓は実装ソースを読んでいない。** 各項目が「現在どうなっているか」は起票時に製造が実査する。**★本書は「何を直すか」の分類までしか書かない。**

---

## 1. 目的

**`α②`（`M24` で直しきれなかった「明らかに品質が低い部分」の是正）のうち、`D-646` の判定規則 (1)〜(3) に当たるもの——利用者が触る面で機能が「できない」か「壊れて見える」もの——を、エディタ・一覧・詳細の 3 面で潰す。**

**★フェーズ4 で最大のマイルストーンである。**

---

## 2. ★★数の是正

### 2.1 `42 行` は raw 行数である

**ledger は監査可能性のため raw 行を消さず、`代表 ID` で結んでいる**（`M25-RESEARCH-01` §2.2）。**⇒ 同じ内容が `phase4-memo`（`P4M-*`）と `Memo_Someday`（`SD-*`）の両方に在ると 2 行になる。**

**設計卓が `代表 ID` 欄で畳んだ結果——42 raw 行 → 33 identity。** 畳んだのは 7 組。

| 代表 ID | 畳んだ行 |
|---|---|
| `P4M-009` | `SD-012`（ダメージ等の必須入力化） |
| `P4M-010` | `SD-013`（必須・任意ラベル） |
| `P4M-011` | `SD-014`（起き攻めの未検証／false 区別） |
| `P4M-020` | `SD-024`（一覧の始動技欄を除去） |
| `P4M-021` | `SD-025`（ヒット種別フィルタ短縮） |
| `P4M-022` | `SD-026`（始動技フィルタ） |
| **`P4M-025`** | **`SM-033` / `SM-106` / `SM-116`**（ヒット種別・状況の拡充。**★4 行が 1 件である**） |

### 2.2 ★★面での機械分割に境界のずれがあった

**`phase4-overview` §7.1 は `α②` 91 行を「触る面」の文字列で 4 群へ割った。⇒ 面が `画面・データ` の行がすべて `M27` に入ったが、その中にセットプレイ・確定反撃・仮想コントローラの項目が混ざっていた。**

**★`M31`（セットプレイ・確定反撃・共通部品）と `M30`（仮想コントローラ）が、同じ主題の行を別々に持っている状態だった。**

| 移す先 | 件数 | 中身 |
|---|---:|---|
| **`M31`** | **9** | `SD-001` セットプレイ編集の戻るボタン位置 ／ `SD-003` setplay only フラグの活用 ／ `SD-004` 複雑な状態遷移キャラのセットプレイ対応 ／ `SD-023` セットプレイ削除時の紐付け解除 ／ `SD-017` 確定反撃の自キャラ既定値 ／ `SD-018` 確定反撃マイリストの区分整理 ／ `setup-trash-restore-bypasses-leave-guard` ／ `setup-unsaved-steps-cannot-use-recipetext` ／ `m23-06-unnamed-setup-unreachable-by-api` |
| **`M30`** | **1** | `SD-021` 仮想コントローラの技表示基準 |

**⇒ `M27` が負うのは 33 − 10 ＝ 23 identity である。**

**★★これは分割の失敗ではなく、機械分割の限界である**——**「触る面」の文字列は `M25-RESEARCH-01` が付けたものであり、主題ではなく画面名の粒度で付いている。⇒ 面で割ったら、主題で見直す。本書がその見直しである。**

**★`phase4-overview` §3 / §7.1 の数を本書の承認と同じ手番で同期すること。**

### 2.3 ★★境界のずれは、もう 4 件あった（**2026-09-02 追加＝`D-686`**）

**★★§2.2 の見直しは足りていなかった。** §2.2 は **主題を読み直して** 10 件を `M31` / `M30` へ移したが、**ledger が持っている `代表 ID` 欄を機械的に突き合わせていなかった。**

**★★`代表 ID` 欄は名寄せの結果そのものである**（`M25-RESEARCH-01` §2.2）。**⇒ 「同じ 1 件がどの行と結ばれているか」は、判断ではなくデータで分かる。**

**突き合わせの手順**——**`SD-*` の各行の `代表 ID` を引き、その代表行の「触る面」を見る。⇒ 面が `M27` の外なら、その識別子は 2 つのマイルストーンへ分かれて入っている。**

| `M27` 側の行 | 代表 ID | 代表の「触る面」 | 処遇 |
|---|---|---|---|
| `SD-001` / `SD-003` / `SD-023` | `P4M-004` / `P4M-005` / `P4M-019` | セットプレイ | **★§2.2 で `M31` へ移済み** |
| `SD-017` / `SD-018` | `P4M-015` / `P4M-014` | 確定反撃 | **★§2.2 で `M31` へ移済み** |
| `SD-004` | `P4M-023` | **データ・seed** | **★§2.2 で `M31` へ移済み。★ただし代表の面は seed であり、データ側の手当てが要る**（§2.3.1） |
| `SD-021` | `P4M-017` | 仮想コントローラ | **★§2.2 で `M30` へ移済み** |
| **`SD-010`** | **`P4M-007`** | **画面: 仮想コントローラ** | **★★見落とし。⇒ `M30` へ移す** |
| **`SD-011`** | **`P4M-008`** | **画面: 仮想コントローラ** | **★★見落とし。⇒ `M30` へ移す** |
| **`SD-019`** | **`P4M-016`** | **画面: 仮想コントローラ** | **★★見落とし。⇒ `M30` へ移す** |
| **`SD-022`** | **`P4M-018`** | **画面: 仮想コントローラ** | **★★見落とし。⇒ `M30` へ移す** |
| `SD-015` / `SD-016` | `P4M-012` / `P4M-013` | **共通画面部品** | **★★例外として `M27-02a` に残す**（§2.3.2） |
| `SD-009` | `P4M-006` | 画面・文書 | **★`M27-03` に残す**（§2.3.3） |

**⇒ `M27` が負うのは 23 − 4 ＝ 19 identity である。**

#### 2.3.1 `SD-004` の代表は seed である

**`P4M-023`（各キャラ `custom_state` 拡張）の面は `データ・seed` である。⇒ `M31` で「複雑な状態遷移キャラのセットプレイ対応」を扱うとき、画面だけでは足りず seed データの手当てが要る。★`M31` の起票時に `M14` との関係を確かめること。**

#### 2.3.2 ★★`SD-015` / `SD-016` は例外として `M27-02a` に残す

**代表の面は `共通画面部品` であり、機械的には `M31` である。⇒ それでも残す。理由 3 つ。**

| # | 理由 |
|---|---|
| **★★1** | **`tag-field-keyboard-unreachable` は ledger の行ではなく `M24` の as-built 由来であり、`代表 ID` を持たない。★その 1 件と `SD-015` / `SD-016` が同じ部品の話である見込みが高い**（追跡行が `SearchableSelect` の `handleListKeyDown` を名指ししている）。**⇒ 3 件を分けると、1 つの部品の改修が 2 つのマイルストーンに割れる。★それは §2.2 が正した失敗そのものである** |
| **2** | **`M27-02a` のほうが早く走る。⇒ 開発者が報告した操作の痛みが先に消える** |
| **3** | **`M31` は結果に乗ればよい**——**部品は `M27-02a` が所有し、`M31` は利用側の追随だけを見る。★所有を明示することで、二重に触ることを防ぐ** |

**★★`M31` は `P4M-012` / `P4M-013` を持たない。⇒ `M31` の起票時に引き算すること。**

#### 2.3.3 `SD-009` は `M27-03` に残す

**代表 `P4M-006` の面は `画面・文書` であり、`M29`（用語・ラベル）に見える。⇒ 残す。**

**理由＝開発者の逐語は「はい。表示だけです。画面上でわかりにくいというだけの指摘でした」であり、詳細画面の表示の話である。★`M29` の仕事は「複数画面で割れた語を統一すること」であり、本件は割れていない。★ただし文言を作るときは `M29` の語彙に合わせること。**

#### 2.3.4 ★★なぜ §2.2 で拾えなかったか

**★★§2.2 は「面で割ったら、主題で見直す」を掲げ、実際に主題を読み直した。⇒ 読み直しは効いた（10 件が動いた）。**

**★★しかし読み直しは `M27` から外へ出すものだけを見ており、`代表 ID` という機械的な手掛かりを使わなかった。⇒ 判断で 7 件を拾い、4 件を落とした。**

**★★一般形＝データで分かることを、判断でやらない。** **ledger は監査可能性のために `代表 ID` 欄を持っている。⇒ その欄は「名寄せの記録」であると同時に「境界の検算」に使える。★先に機械で突き合わせ、残ったものを主題で判断する。順序が逆だった。**

**★★もう 1 つ**——**設計卓は 2026-09-02 に `M27-02a` / `M27-02b` を起票し、`M27-02b` の中で「`SD-019` / `SD-022` は `SD-021` と同じ基準に効く可能性がある」と自分で書いていた。⇒ 見立ては当たっていたが、根拠を推測で置いた。★答えは同じ資料の隣の列に在った**（計測点 `M-116`）。

---

---

## 3. サブ構成（**★★4 本・19 identity**。**`M27-02` は 2026-09-02 に 2 本へ割れた＝`D-683`**）

### M27-01 — ヒット種別の追加と区分名の整理（**★★2026-09-02 完了・受理＝`D-687`**）

**★★確定した列挙**——**`hit_type` 8 値**〔既存 4〔`normal` / `counter` / `punish_counter` / `just_parry_punish_counter`〕**＋ `drive_impact_wall_splat_hit` / `drive_impact_wall_splat_block` / `drive_impact_punish_counter` / `stun`**〕**／ `opponent_size` 4 値**〔`standard`（`medium` から改名・DML `000081`） / `large`（新設） / `large1`（大1・ザンギエフ等） / `large2`（大2・マリーザ等）〕。

**★★入れないと決めた 2 値を記録する**（`D-675`）——**ガードクラッシュ ／ インパクトヒット（壁やられ無し）**。**候補として並べたうえで開発者が選ばなかった。⇒ 「触れなかった」ではなく「やらないと決めた」である。**

**★★`大1` / `大2` の整理も同梱された**——**本書 §3.1.2 は「本サブに含めない」としていたが、開発者が判断を供給したため禁止の前提が消滅した**（`D-687`。**★禁止が守っていた利益は「未確定のまま踏み込まない」であって「永久に触らない」ではない**）。

**★★本サブが確定反撃へ残した 1 件**——**`drive_impact_punish_counter` は materialize の対象外に入れたが、タブ分け（`punishHitTypes`）は触っていない。⇒ 引き続き「区分を判定できない反撃」に出る。★開発者が明示的に別サブへ回した部分であり、未完了ではない。⇒ `M31`（確定反撃）が (a) どのタブに置くか (b)「始動技がインパクトのときだけ使う」制約をどこで掛けるか を決める**（followup `punish-list-di-punish-counter-tab-placement`）。

**★★以下は起票時の記述である**（`D-669` / `D-674`。**列挙の確定と名前の決定は本サブの段 1 で行われ、決着した**）。


#### 3.1.1 ★★ヒット種別の追加は 2 種だけである

**★★【2026-09-02 是正＝`D-674`】「2 種だけ」は確定ではなかった。**

**開発者の追加の逐語＝「ヒット種別に、ドライブインパクトがパニッシュカウンターした。という専用区分も必要でした。ひょっとしたら他にも思いつくかもしれません。実際のマイルストーンまでにはこれも整理します。」**

**⇒ 現時点で挙がっているのは 3 種である。★これも確定ではない。**

| # | 挙がっているもの | 出所 |
|---|---|---|
| **1** | **ドライブインパクトがヒットし、壁やられが発生した** | 2026-09-02 回答 |
| **2** | **ドライブインパクトをガードし、壁やられが発生した** | 2026-09-02 回答 |
| **3** | **ドライブインパクトがパニッシュカウンターした** | **★2026-09-02 追加** |

**★★列挙の確定は `M27-01` の起票時である。⇒ 本書では確定させない。**

**★★一般形＝開発者の「〜でいい」は、その時点で思い出せた列挙であって確定ではない**（計測点 `M-114` の隣接）。**⇒ 列挙を受け取ったら「これで全部か」を起票時にもう一度問う。★とくにドメイン知識に依存する列挙で効く。**

**★設計卓が本書 v1.0.0 で挙げた 8 候補のうち「パニッシュカウンター」は当たっていた**——**ただし当時の書き方は「パニッシュカウンター」であり、開発者が挙げたのは「ドライブインパクトがパニッシュカウンターした」という DI 限定の区分である。⇒ 語が同じでも指すものが違う。★候補リストは無駄ではなかったが、そのままでは採れない。**

**★★正式な名前は未定である。** 開発者の逐語＝**「壁やられは表記揺れがあるが……正式な名前は画面モックなどを出しつつ決める。」**

**⇒ 起票時に画面モックを出して名前を決める。★本書では名前を確定させない。**

**★★設計卓が本書 v1.0.0 で挙げた 8 候補は採らない。** 〔ガードクラッシュ ／ ジャストパリィ反撃 ／ パニッシュカウンター ／ DI 等〕**は源泉 3 行**（`SM-033` / `SM-106` / `SM-116`）**の和集合として並べたものだったが、開発者の判断は 2 種のみである。**

**★射程は当初の 8 候補より小さいが、確定していない。** **⇒ 列挙値が数個増えるだけであり、既存の `hit_type` 4 値との関係は「足すだけ」で済む見込みである。★件数が動いても構造は変わらない。**

#### 3.1.2 相手のサイズの区分名（**★新規・`D-669`**）

**開発者の逐語＝「相手のサイズは適当に中、大1、大2としていたが、中は標準にしたい。」**

**⇒ 「中」→「標準」。★`M27-01` に同梱する。理由＝ヒット種別と同じ「列挙定数の両側同期」の手番だからである。**

**★★「大1」「大2」の整理は本サブに含めない。** 開発者の逐語＝**「大1、大2をどう整理するかは検討中で後日課題としたい。」** **⇒ followup `opponent-size-large-variants-defer` として `要判断` に置いた。★同じ列を触るが判断が済んでおらず、一緒に直そうとすると止まる。**

**★DB の値を変えるのか表示名だけを変えるのかで射程が変わる**〔値を変えるなら DML マイグレ 1 本 ＋ CSV 取込のホワイトリストへ波及〕。**⇒ 起票時に決める。**

#### 3.1.3 ★★本サブが重い理由（射程縮小後）

| # | 理由 | 変化 |
|---|---|---|
| **1** | **★列挙的文字列定数である。⇒ `internal/model/` と `web/src/constants/` の両側を同期する**（`CLAUDE.md` §4。検査＝`scripts/check-enum-sync.sh`） | **不変。★ヒット種別と相手サイズの 2 系統ぶん** |
| 2 | 既存データへの影響 | **★軽くなった。** ヒット種別は「足すだけ」。相手サイズは値を変えるなら DML 1 本 |
| **3** | **★絞り込み・比較・エクスポートに波及する**（`P4M-021` が同じ列を触る） | **不変** |
| **4** | **★★名前が未定である**（`3.1.1`） | **★新しい制約。⇒ 画面モックを出す手番が本サブの中に要る** |

**★★`3.1.4` の帰結**——**本サブは「実装」ではなく「画面モックで名前を決める → 定数を両側へ足す → 表示と絞り込みへ通す」の 3 段になる。**

### M27-02 — コンボ編集の入力品質（13 identity。**★★2026-09-02 に `M27-02a` / `M27-02b` の 2 本へ割った＝`D-683`**）

**★★割った。** 本書 v1.2.0 は「起票時に 2 本へ割れる」と述べ、割り方の候補を **(a) 保存の動線 ／(b) 入力の見え方** としていた。**⇒ 起票時に 13 件を主題で見直したところ、候補とは違う線が出た。**

| サブ | 主題 | identity | 件数 | 状態 |
|---|---|---|---:|---|
| **`M27-02a`**（**★★完了・受理＝`D-694`**。**`SD-015` / `SD-016` は開発者裁定で対応不要**〔逐語＝「キャラ欄については対象外でいい」／「そもそもそんな操作はする必要はない事に気が付いた」〕**。⇒ 実施は 4 件**） | **★利用者の操作と情報が、届くべき先へ届かない** | `SD-015` ／ `SD-016` ／ `editor-validation-errors-unreachable-from-ui` ／ `editor-tab-badge-misroutes-setups-errors` ／ `combo-put-error-message-empty` ／ `tag-field-keyboard-unreachable` | **6** | **★★投入可能**（`docs/instructions/M27-02a-reachability-save-path-and-selectors.md` v1.0.0）。**開発者判断を含まない** |
| **`M27-02b`** | **★入力仕様の 3 状態（必須・任意・未検証）** | `P4M-009` ／ `P4M-010` ／ `P4M-011` ／ `P4M-024` ／ `SD-020` | **5**（**★★`SD-019` / `SD-022` は `M30` へ移した＝`D-686`。§2.3**） | **★起票済み**（`docs/instructions/M27-02b-required-fields-and-strength-display.md` **v1.1.0**）。**★★段 1 の最初の一手が「開発者へ問うて止まる」である** |

**★★なぜ候補 (a)/(b) の線を採らなかったか。** **(a) 保存の動線は 3 件しかなく、残り 10 件が「入力の見え方」へ寄る。⇒ 3 対 10 になり、割った意味が薄い。**

**⇒ 主題で見直したら、別の線が出た。**

| 見えた束 | identity | 気づき |
|---|---|---|
| **★★候補選択に操作が届かない** | `SD-015`（Enter が反応しない） ／ `SD-016`（ドラッグ判定） ／ `tag-field-keyboard-unreachable` | **★★3 件とも候補選択の部品の話である見込みが高い。** `tag-field-*` の追跡行が**直し方を名指ししている**——**「`SearchableSelect` の `handleListKeyDown` を `TagSelector` へ広げる形が素直」**。**★開発者自身も疑っていた**（逐語＝「（一覧、ゴミ箱とか、全体の共通？部品）」） |
| **★保存が通らない理由が届かない** | 10 / 11 / 12 | **★本書 v1.2.0 が既に「同じ経路」と述べていた** |
| **★入力仕様の決めごと** | `P4M-009` / `P4M-010` / `P4M-011` / `P4M-024` | **★★開発者の答えが要る**——逐語＝**「他にもこれはやっぱり入力ないとダメだなというメタデータはリストアップしたい」**。**リストはまだ無い** |
| **★強度の見せ方** | `SD-019` / `SD-020` / `SD-022`（**★★`SD-019` / `SD-022` は 2026-09-02 に `M30` へ移した＝`D-686`。⇒ 本サブに残るのは `SD-020` だけである**） | **★★UI ではなくデータの問題かもしれない。** 逐語＝**「舞のホールド花蝶扇では起きない」**——**キャラで起きたり起きなかったりする。⇒ データの差である可能性** |

**★★割り方の決め手は「開発者の答えを待つか否か」である。** **入力仕様の決めごとは開発者しか決められない**（`roles-and-routing` ハード列）。**⇒ 混ぜると、答えを待つ間に到達性の 6 件まで止まる。★到達性の 6 件は誰の判断も要らず、いま進められる。**

**★★強度の 3 件には衝突の芽がある**——**データ側だと判明した場合、直す先は `character_data/` ／ `internal/seedgen/` ／ `migrations/` であり、`M14-03f` と同じ面である**（`D-678`）。**⇒ `M27-02b` の指示書は「データ側だと判明したら実装へ進まず設計卓へ知らせる」を明記している。**

**★★`editor-validation-errors-unreachable-from-ui` の (a)/(b)/(c) は設計卓が決めた＝(a) そのまま残す**（`D-684`）。**追跡行が「設計判断であり製造は判断しない」としていた 1 件であり、開発者判断ではなく設計卓の持ち場である。⇒ 起票時に決めて指示書へ書いた。**

#### 元の 13 identity

| # | identity | 内容 |
|---|---|---|
| 1 | `P4M-009` | **ダメージ等の必須入力化** |
| 2 | `P4M-010` | **必須・任意ラベル** |
| 3 | `P4M-011` | **起き攻めの「未検証」と「false」の区別** |
| 4 | `P4M-024` | **基本情報が始動状態であることを表示** |
| 5 | `SD-019` | **強度なし技の選択** |
| 6 | `SD-020` | **OD 強度組合せを既定非表示** |
| 7 | `SD-015` | **キャラ選択後 Enter が反応しない** |
| 8 | `SD-016` | **キャラ選択のドラッグ判定** |
| 9 | `SD-022` | **ホールド／ジャスト版の表示** |
| 10 | `editor-validation-errors-unreachable-from-ui` | **★バリデーションエラーが UI から到達不能。⇒ 保存できないのに理由が分からない** |
| 11 | `editor-tab-badge-misroutes-setups-errors` | **タブのバッジが誤誘導する** |
| 12 | `combo-put-error-message-empty` | **保存失敗時にメッセージが空** |
| 13 | `tag-field-keyboard-unreachable` | **タグ欄にキーボードで到達できない** |

**★★10 / 11 / 12 は同じ経路である**——**「保存が通らないときに、利用者が理由へたどり着けるか」。⇒ 3 件をまとめて 1 つの動線として設計すること。★バラバラに直すと、直した先で別の行き止まりが残る。**

**★1 / 2 / 3 も同じ族である**——**「必須／任意／未検証」の 3 状態をどう見せるか。⇒ 1 つの規則を 3 か所へ通す。**

**★★本サブは 13 件と多い。起票時に 2 本へ割れる**〔**(a) 保存の動線 ／(b) 入力の見え方**〕。**★割るかどうかは起票時に決める。**

### M27-03 — 一覧・詳細（**★★7 identity**。**`SD-010` / `SD-011` は 2026-09-02 に `M30` へ移した＝`D-686`。§2.3**）

**★★下表の 4（`SD-010` ターゲットコンボタブ）と 5（`SD-011` 非表示技・キャラ別技タブ）は `M27` の射程外になった。**
**代表 ID `P4M-007` / `P4M-008` の面が `画面: 仮想コントローラ` であり、逐語も技を出すタブの話である**（`Memo_Someday.txt:379-381`＝「SAの隣に何らかの理由で表示していない技（強化版必殺技等）だけを出すタブが欲しい」）。**⇒ コンボ一覧の話ではない。★`M30` が持つ。**


| # | identity | 内容 |
|---|---|---|
| 1 | `P4M-020` | **一覧の始動技欄を除去** |
| 2 | `P4M-021` | **ヒット種別フィルタ短縮**（**★`M27-01` と同じ列を触る**） |
| 3 | `P4M-022` | **始動技フィルタ** |
| 4 | `SD-010` | **ターゲットコンボタブ** |
| 5 | `SD-011` | **非表示技・キャラ別技タブ** |
| 6 | `SD-009` | **ドライブダメージの符号説明と表示**（**★逐語＝「正負がどっちを表すかいまは良く分からないので、表示を見直したい」**） |
| 7 | `combo-list-setup-count-hides-fetch-failure` | **★取得失敗が件数表示に隠れる。⇒ 黙って 0 件に見える** |
| 8 | `combo-detail-back-link-is-hardcoded` | **戻り先が固定されている** |
| 9 | `tag-count-usage-inflated-by-orphan-combo-tags` | **★タグの使用数が orphan 行で水増しされる。⇒ 画面は「使用 0 件」なのに削除は 409 で拒まれ、UI からは抜け道が無い** |

**★★1 と 3 は逆向きに見えるが両立する**——**`P4M-020` は一覧の「欄」を除き、`P4M-022` は「絞り込み」に始動技を足す。⇒ 表示と絞り込みは別である。起票時に混同しないこと。**

**★9 は `P-04`（FK 強制の接続単位問題）の「今日の実害」として記録されている行である**（followup）。**★根治は `M23-10` / `CHANGE-142` で着地済みだが、既に溜まった orphan 行は消えない。⇒ 本サブで扱うのは「数え方」であって「掃除」ではない。掃除は開発者の手番**（`D-196` 境界条件 3。**★孤立行 19 件は 2026-08-24 に掃除済み**＝`D-651`）。

---

## 4. 依存関係・順序

```
M28-01（正式名リネーム）… ★モジュールパスを変えるため全 import に触る。最初に通す
     ↓
M27-01（ヒット種別の拡充）… ★データモデルに効く
     │
     ├─→ M27-03（一覧・詳細）… P4M-021 が M27-01 と同じ列を触る
     │
     └─→ M27-02a（到達性: 保存の動線・候補選択）… ★開発者判断を含まない
              ↓
          M27-02b（入力仕様の 3 状態・強度）… ★段 1 で開発者へ問うて止まる

★M27-02a / M27-02b / M27-03 は互いに独立。ただしフェーズ4 は並列運用をしないため順に通す。
★M27-02a を M27-02b より先に置くのは、02b が開発者の答えを待つためである。
```

**★`M28`（破壊窓）との関係**——**`M28-01`（正式名リネーム）は全面に触るため、`M27` と並べると衝突が増える。⇒ `M28-01` を先に通す**（`phase4-overview` §4・開発者一任＝`D-653`）。

---

## 5. CHANGE 見込み・採番

| サブ | CHANGE 見込み | 根拠 |
|---|---|---|
| **M27-01** | **1 本。★ほぼ確実に要る** | **`DES-003`（`hit_type` の列挙値）／`DES-005`（表示）／`DES-006`（バリデーション）に及ぶ** |
| **M27-02a** | **1 本** | **`DES-005`（保存の動線・タブの指し示し・キーボード操作）／`DES-006`（`VAL-C02` の扱い）／`DES-002`（`PUT` のエラー応答）** |
| **M27-02b** | **1〜2 本** | **`DES-006`（必須化）／`DES-005`（必須・任意ラベル・強度の見せ方）／`DES-003`（起き攻めの 3 状態化）／`DES-004`（強度の内部表現）** |
| **M27-03** | **1 本** | **`DES-005`（一覧の列・絞り込み・詳細）** |

**⇒ `M27` の見込みは 4〜5 本**（**★2026-09-02 に `M27-02` が 2 本へ割れたぶん 1 本増えた＝`D-683`**）**。★`M26-01` が `151` を、`M28` が `152` 以降を使う見込みであり、`M27` はその次になる。★先付けしない**（playbook §4.15）。

**★マイグレ消費 0〜1 本**（**`M27-01` が `hit_type` の値を足すなら DML で 1 本**）。**★スキーマ変更は開発者の個別承認事項**（`CLAUDE.md` §10）。

### 5.1 推奨モデル

- **`M27-01`**——**高精度実装 ＋ Plan Mode 必須**（**★列挙定数の両側同期 ＋ 既存データへの影響**）
- **`M27-02a`**——**高精度実装 ＋ Plan Mode 必須**（**★共通部品 `SearchableSelect` の改修が利用側の全数へ波及する**＝`CLAUDE.md` §9「全体に影響するリファクタリング」）
- **`M27-02b`**——**高精度実装 ＋ Plan Mode 必須**（**★スキーマ・データ移行を伴う**＝`CLAUDE.md` §9）
- **`M27-03`**——**高精度実装**（**★Plan Mode は起票時に判断する**）

---

## 6. ★設計卓が確かめていないこと（実査項目）

| # | 実査すること | なぜ |
|---|---|---|
| 1 | **`hit_type` の現在の値と、`M18-01` の 4 値化以後に増えた値** | **★`M27-01` の起点** |
| 2 | **`internal/model/` と `web/src/constants/` のヒット種別定数の現在の同期状態**（`scripts/check-enum-sync.sh`） | **★両側同期の規約**（`CLAUDE.md` §4） |
| 3 | **保存失敗時のエラーが、どこまで UI に届いているか**（`M27-02` の 10 / 11 / 12） | **★3 件が同じ経路かを確かめる** |
| 4 | **一覧の「始動技欄」と「始動技フィルタ」が現在それぞれ在るか** | **★除去と追加が逆向きに見えるため** |
| 5 | **`tag` の `CountUsage` の現在の述語** | **★`M23-10` / `CHANGE-142` 以後の状態** |
| 6 | **`P4M-*` / `SD-*` の各項目が「まだ直っていない」ことの確認** | **★★`D-646` の適用で 126 行中 5 行が「既に解消していた」。⇒ 同じことが起きる** |

**★★実査項目 6 が最も重い。** **`M25-RESEARCH-01` §6 の 126 行を規則で畳んだとき、4% が既に直っていた。⇒ `M27` でも「もう直っていないか」を最初に見ること。**

---

## 7. 開発者への確認事項（**★2026-09-02 に 4 件すべて決着した＝`D-669`**）

| # | 内容 | 決着 |
|---|---|---|
| **1** | `M27-01` で足すヒット種別の一覧 | **★★確定していない。** **2026-09-02 の回答は「壁やられ二種の追加でいい」だったが、同日中に「ドライブインパクトがパニッシュカウンターした」が追加され、開発者の逐語＝「ひょっとしたら他にも思いつくかもしれません。実際のマイルストーンまでにはこれも整理します」。** **⇒ 現時点で 3 種。★確定は `M27-01` の起票時**（`D-674`。§3.1.1）。 **★あわせて「相手のサイズ」の「中」→「標準」が入った。「大1」「大2」の整理は後日課題**（§3.1.2） |
| **2** | 3 サブ構成の承認 | **★承認** |
| **3** | 境界の是正（`M31` へ 9・`M30` へ 1） | **★★開発者から「これは何の話でしたか？」。⇒ 設計卓の説明不足である**（計測点 `M-113`）。**移す 10 件を §2.2 に名指しで書いてあったが、返信では件数しか示さなかった。⇒ 下記に何がどこへ動くかを再掲する** |
| **4** | `SD-009`（ドライブダメージの符号） | **★表示だけ**（開発者の逐語＝「はい。表示だけです。画面上でわかりにくいというだけの指摘でした」）。**⇒ 仕様は動かさない** |

### ★★確認事項 3 の再掲——何がどこへ動くか

**`M27` が持っていた 33 identity のうち 10 件は、主題が `M31` / `M30` である。⇒ そちらへ移す。**

| 移す先 | identity | 内容 |
|---|---|---|
| **`M31`**（セットプレイ・確定反撃・共通部品） | `SD-001` | セットプレイ編集の戻るボタン位置 |
| | `SD-003` | setplay only フラグの活用 |
| | `SD-004` | 複雑な状態遷移キャラのセットプレイ対応 |
| | `SD-023` | セットプレイ削除時の紐付け解除 |
| | `SD-017` | 確定反撃の自キャラ既定値 |
| | `SD-018` | 確定反撃マイリストの区分整理 |
| | `setup-trash-restore-bypasses-leave-guard` | ゴミ箱からの復元が離脱ガードを迂回する |
| | `setup-unsaved-steps-cannot-use-recipetext` | 未保存ステップでレシピテキストが使えない |
| | `m23-06-unnamed-setup-unreachable-by-api` | 名前が空のセットプレイに API から到達できない |
| **`M30`**（仮想コントローラ） | `SD-021` | 仮想コントローラの技表示基準 |

**★なぜ動かすのか**——**`M25-RESEARCH-01` が付けた「触る面」の文字列で機械的に割った結果、この 10 件が `M27` に入っていた。★「触る面」は主題ではなく画面名の粒度で付いている。⇒ セットプレイの項目が `M27`（エディタ・一覧・詳細）と `M31`（セットプレイ）に分かれて入っていた。**

**★動かさないと何が起きるか**——**同じ主題を 2 つのマイルストーンが別々に触る。⇒ `M27` で直した前提の上に `M31` が積む形になり、どちらの担当も全体を見られない。**

## 8. 参照

| ID | パス |
|---|---|
| 上位 | `docs/instructions/phase4-overview.md` §M27 |
| ledger | `docs/progress/M25-RESEARCH-01-report.md` §3 |
| `α②` の判定規則 | ボード `D-646` |
| 源泉 | `docs/human-notes/phase4-memo.txt`（`P4M-*`）／ `docs/human-notes/Memo_Someday.txt`（`SD-*`） |
| 追跡表 | `docs/handover/followup-backlog.md`（`割付` 欄が `D-646` の適用結果を持つ） |
| 列挙定数の同期 | `CLAUDE.md` §4 ／ `scripts/check-enum-sync.sh` |

---

*以上*
