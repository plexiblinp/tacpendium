# 指示書 M35-01: タグまわりの是正 3 件（**公開前**）

| 項目 | 内容 |
|------|------|
| 文書ID | M35-01 |
| バージョン | **1.0.0**（2026-09-08） |
| 作成日 | 2026-09-08 |
| 作成者 | 設計担当 Claude（設計卓／フェーズ4 期） |
| 対象マイルストーン | **`M35`**（公開前の是正束）。上位＝`docs/instructions/M35-overview.md` v1.0.0 |
| 推奨モデル | **標準実装**（**★★ただし §2.2 は BE / FE を同時に触る**。実モデル名は `docs/human-notes/model-allocation.md`） |
| Plan Mode | **不要**（**★スキーマ・API 契約・データ移行を伴わない**＝`CLAUDE.md` §9。**★ただし §2.2 は 3 ファイルを 1 コミットで直す必要がある。⇒ 着手前に対象を全数挙げること**） |
| レビュー | **必要**（チェックリストは着手承認の手番で設計卓が起こす＝`D-736`） |
| CHANGE 消費 | **1 本の見込み**（`DES-002` の API 契約 ／ `DES-006`。**★起票は設計卓。自採番しない**＝`D-293`） |
| マイグレ消費 | **0 本** |
| 前提サブ | **無し**（面が交わらない） |
| 射程 identity | **3 件 ＋ 同乗候補 1 件**（§1） |
| 配置 | `docs/instructions/M35-01-tag-fixes.md` |
| 出力 | `docs/progress/M35-01-completion-report.md` |

---

## 0. この指示書の特殊性

### 0.1 ★★3 件の性質がそれぞれ違う。⇒ 混ぜないこと

| 段 | 件 | 性質 | コミット |
|---|---|---|---|
| **段 1** | **`Delete` のスコープテスト** | **テストを 1 本足すだけ。実装は触らない** | **1 本目** |
| **段 2** | **DTO の camelCase 化** | **★★BE / FE の 3 ファイルを 1 コミットで直す**（分けると、その間の状態が壊れている） | **2 本目** |
| **段 3** | **「未設定へ戻せない」の明文化** | **★実装を 1 行も変えない。⇒ 設計伝達レポートで CHANGE を請求するだけ** | **コミット無し**（報告のみ） |

**★★コミットを 3 つに分けること**（段 3 はコミットを持たない）**。⇒ 段 2 が赤くなったとき、段 1 まで巻き添えにしない。**

### 0.2 ★★段 2 は「今は壊れていない」ものを直す

**★★`CLAUDE.md` §4 は「JSON タグは camelCase で統一」「DB カラム名のスネークケースをフロントに持ち込まない」を規約化している。★本経路だけが両側とも snake_case で通っている。**

**★★今は両側が揃っているので壊れていない。だから登録された。⇒ 規約に気づいた誰かが片側だけ直すと、型検査もテストも通ったまま「使用件数が表示されない」形で静かに壊れる。**

**★★したがって本サブで片側だけ直すのは、最も避けるべき形である。⇒ §4.1。**

---

## 1. 前提として読むもの

| # | 何を | どこ |
|---|---|---|
| 1 | **本 MS の全体像** | `docs/instructions/M35-overview.md` v1.0.0 |
| **★★2** | **登録内容の全文** | `docs/handover/followup-backlog.md` §CC の 3 行〔`tag-delete-user-scope-untested` ／ `tag-error-detail-dto-snake-case` ／ `tag-category-color-cannot-be-cleared`〕 |
| **★3** | **JSON タグの規約** | `CLAUDE.md` §4（Go ／ TypeScript の両方） |
| 4 | **タグの検証** | `DES-006` の `VAL-T01` / `VAL-T03` |
| **★5** | **同型の先例** | `docs/process/parallel-board.md` の **`D-579`**（`VAL-C02` の check-then-act）／ **`D-593`**（`VAL-S04` の同型） |

---

## 2. やること

### 2.1 段 1 — `Delete` の利用者スコープを試験する

**★★他人のタグを消せないことを主張する試験を 1 本足す。**

| # | やること |
|---|---|
| **★★1** | **`internal/api/tag/scope_test.go` へ `TestDelete_CannotTouchAnotherUsersTag`（仮）を足す。⇒ 同ファイルの `TestUpdate_CannotTouchAnotherUsersTag`（`:128`）と同じ形にすること** |
| **★★2** | **【破壊確認】実装から `AND user_id = ?` を外すと赤くなることを実測する。⇒ 外して緑のままなら、その試験は何も見ていない**（§4.2） |
| **★3** | **`scope_test.go:126` のコメント（「タグの Update / Delete は既に `WHERE id = ? AND user_id = ?` で絞っている」）が、これで両方とも試験に裏づけられる状態になったことを確かめる** |

**★実装は 1 行も変えない見込みである。⇒ 変える必要が出たら、それ自体が報告に値する**（**実装が絞っていなかったということになる**）**。**

### 2.2 段 2 — エラー詳細 DTO を camelCase へ揃える（**★★3 ファイルを 1 コミットで**）

| # | 対象 | 変えること |
|---|---|---|
| **★★1** | `internal/api/tag/handler.go:163-164` | **`"usage_count"` → `"usageCount"` ／ `"force_delete_query"` → `"forceDeleteQuery"`** |
| **★★2** | `web/src/types/tag.ts:23-25` | **`TagErrorDetail` の同名フィールドを camelCase へ** |
| **★★3** | `web/src/features/tag/api/tagApi.ts:23` | **`details?.usage_count` → `details?.usageCount`** |
| **★★4** | `web/src/types/tag.ts:23` | **`existing_tag_id` を削除する。⇒ バックエンドは 1 か所も返しておらず**（`grep -rn "existing_tag_id" --include='*.go'` が 0 件）**、フロントも型定義の 1 行以外に参照が無い** |

**★★着手前に、上の 4 か所以外に参照が無いことを走査で示すこと。⇒ 「3 ファイルだと聞いた」では足りない。★実測で 0 件を示す。**

**★★テストが要る＝`usageCount` が画面まで届くことを 1 本で主張すること。⇒ 型検査は通ってしまうので、型検査は歯止めにならない。**

### 2.3 段 3 — 「未設定へ戻せない」を明文化する（**★実装を変えない**）

**★★開発者の判断**（2026-09-08）**＝「未設定へ戻す処理はいらない」。⇒ 実装は現状のままでよい。**

**★機序を報告に書くこと**（設計卓が CHANGE に写す）**。**

| 段 | 実装 | 挙動 |
|---|---|---|
| 1 | `web/src/features/tag/hooks/useTagFormDialog.ts:57-58` の `values.category \|\| undefined` | **空文字を `undefined` にする** |
| 2 | `JSON.stringify` | **`undefined` のキーを落とす** |
| 3 | `internal/model/tag.go:67-71` の `*string` ＋ `internal/repository/tag/repository.go:165-172` の動的 SET | **nil を「変更なし」としてスキップする** |

**★★`null` を送っても救えないことも書くこと。⇒ Go の `encoding/json` は `null` をポインタの nil へ落とすため、「省略」と「明示的な null」を区別できない。**

**★★あわせて `web/src/features/tag/components/TagManagementPage.tsx:53-54` `:61-62` の `values.category ?? undefined` が no-op であることを確かめる**（呼び元が既に `|| undefined` で正規化済み）**。⇒ 消すか、コメントを付けるかは判断してよい。★`??` は空文字を透過するため、フック側の正規化が将来外れると挙動が静かに変わる。**

### 2.4 ★同乗候補（**やってもよい。必須ではない**）

**★`tag-val-t01-check-then-act-race`**（followup §CC）**＝`internal/service/tag/service.go:69-78` / `:87-96` の check-then-act 競合。⇒ 同型は `M24-11`（`VAL-C02`）と `M24-13`（`VAL-S04`）が `BEGIN IMMEDIATE` ＋ 判定を Tx の内側へ移して塞いでいる。**

**★★同じ形を当てられるなら当ててよい。⇒ ただし段 1〜3 が終わってから、別コミットで。★時間が足りなければ触らないこと**（割付はフェーズ5 である）**。**

---

## 3. やらないこと

| # | 何を |
|---|---|
| **★★1** | **タグの画面・文言を変えること。⇒ 本サブは契約と試験だけである** |
| **★★2** | **`category` / `color` を未設定へ戻せるようにすること。⇒ 開発者が「いらない」と判断した**（2026-09-08） |
| **★3** | **`CLAUDE.md` を書き換えること。⇒ 規約の側は既に正しい** |
| **★4** | **`docs/design/` を編集すること**（`CLAUDE.md` §8）**。⇒ CHANGE の請求は設計伝達レポート §4 へ書く** |
| **★5** | **`existing_tag_id` の代わりを新設すること。⇒ 誰も使っていないので消すだけである** |

---

## 4. ★★危険（**先に読むこと**）

### 4.1 ★★最大の危険＝段 2 で片側だけ直すこと

**★★本件が登録された理由そのものである。⇒ 片側だけ直すと、型検査もテストも通ったまま「使用件数が表示されない」形で静かに壊れる。**

**★★対処＝3 ファイルを 1 コミットで直し、そのコミットの中で「画面まで届く」試験を 1 本通すこと。**

### 4.2 ★★2 番目＝スコープ試験が何も見ていない形

**★「自分のタグを消せる」ことを確かめても意味が無い。⇒ 見るのは「他人のタグを消そうとして弾かれること」である。**

**★★破壊確認を必ず採ること。⇒ 実装から `AND user_id = ?` を外して赤くなることを実測する。★外して緑のままなら、その試験は何も見ていない。**

### 4.3 ★3 番目＝段 3 を「何もしない」で終わらせること

**★実装を変えないので、報告に書かないと消える。⇒ そして次の担当が「バグだ」と読んで直しに行く。★それが登録理由である。**

**★★設計伝達レポート §4 へ、機序 3 段と「`null` でも救えない」を書くこと。⇒ 設計卓が CHANGE に写す。**

### 4.4 ★4 番目＝同乗候補に時間を使いすぎること

**★§2.4 は必須ではない。⇒ 段 1〜3 が終わっていない状態で触らないこと。**

---

## 5. テスト

| # | 何を | 判定 |
|---|---|---|
| **★★1** | **他人のタグを `Delete` できないこと** | **★破壊確認とセットで**（§4.2） |
| **★★2** | **`usageCount` が画面まで届くこと** | **★型検査は歯止めにならない。⇒ 実際に値が読めることを主張する** |
| **★3** | **`existing_tag_id` を消しても何も壊れないこと** | **★走査で参照 0 件を示す** |
| 4 | **`go test ./...`** | **★出力で判定する** |
| 5 | **`cd web && pnpm test`** | **★出力で判定する** |
| **★6** | **`make e2e-only P=<パターン>`**（**★`playwright` を直接叩かない**＝`CLAUDE.md` §11） | **★タグを含む spec を回す** |
| **★★7** | **常設検査**（1 本目は `check-artifact-integrity.sh`） | **★★回すのは最後にすること。⇒ 途中で回して「違反なし」と書き、その後にファイルを足すと嘘になる**（`M-145` の隣・`M30-01` の教訓 3） |

---

## 6. 完了条件

| # | 条件 |
|---|---|
| **★★1** | **段 1 の試験が在り、破壊確認が実測で示されている** |
| **★★2** | **段 2 の 3 ファイルが 1 コミットで直っている。⇒ 4 か所以外に参照が無いことが走査で示されている** |
| **★★3** | **`existing_tag_id` が消えている** |
| **★★4** | **段 3 の機序が報告に書かれている**（3 段 ＋ `null` でも救えない理由） |
| **★5** | **コミットが 3 つ以下に分かれている**（段 1 ／ 段 2 ／ 同乗分） |
| 6 | **§5 の検査がすべて緑**（**★出力で判定。★最後に回す**） |
| **★7** | **`docs/progress/M35-01-completion-report.md` を書き、`docs/progress/progress-log.md` へ索引行を 1 行足す**（`CLAUDE.md` §8） |
| **★★8** | **完了報告を `bash scripts/check-md-emphasis.sh <file>` に通す**（`D-775`。**★`docs/progress/` は常時走査の対象外である**） |
| **★★9** | **設計伝達レポートを出す**（`/design_handover_report`）**。★§4 に段 3 の CHANGE の請求を書くこと** |

---

## 7. ★開発者への確認事項

**★★無い。⇒ 3 件とも 2026-09-08 に開発者の判断が出ている。**

| 件 | 開発者の判断 |
|---|---|
| **未設定へ戻す処理** | **「いらない」**（2026-09-08） |
| **DTO の命名** | **「はい」**（camelCase へ両側同時に直す） |

---

## 8. 参照ドキュメント

| ID | パス | 参照する箇所 |
|----|------|------------|
| — | `docs/handover/followup-backlog.md` | **§CC の 3 行 ＋ `tag-val-t01-check-then-act-race`** |
| — | `CLAUDE.md` | **§4**（JSON タグの規約）／ **§8**（設計書を編集しない）／ **§11**（E2E の回し方） |
| DES-006 | `docs/design/06-validation.md` | **`VAL-T01` / `VAL-T03`** |
| — | `docs/process/parallel-board.md` | **`D-579`** ／ **`D-593`**（同型の check-then-act の先例）／ **`D-780`**（本件の受領）／ **`D-783`**（本サブの起票） |

---

*以上、指示書 M35-01 **v1.0.0**。* **★★本サブの落とし穴は「小さな 3 件」と読むことである。** **★段 2 は「今は壊れていないもの」を直す。⇒ 直す手番で片側だけ直すと、本末転倒になる。** **★段 3 は実装を 1 行も変えない。⇒ 報告に書かないと消え、次の担当が同じ判断を繰り返す。**
