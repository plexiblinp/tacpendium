# 指示書 M27-03: 一覧・詳細（欄の除去・フィルタ・符号の見せ方・戻り先）

| 項目 | 内容 |
|------|------|
| 文書ID | M27-03 |
| バージョン | **1.2.0**（2026-09-05・**★★確認事項が 0 件になった＝`D-725`。`§2.5` は (b) 画面側だけで確定。⇒ 投入可**） ／ 1.1.0（2026-09-05） ／ 1.0.0（2026-09-05） |
| 作成日 | 2026-09-05 |
| 作成者 | 設計担当 Claude（設計卓／フェーズ4 期） |
| 対象マイルストーン | **`M27`**（入力と表示の品質）。上位＝`docs/instructions/M27-overview.md` v1.2.0 |
| 推奨モデル | **標準実装**（**★共通部品の改修は無い。★ただし `M27-01` が触った列へ 2 度目に入る**） |
| Plan Mode | **必須**（`CLAUDE.md` §9＝画面に触れる変更が複数面へ及ぶ） |
| レビュー | **必要**（チェックリストは着手承認後に設計卓が起こす） |
| CHANGE 消費 | **★★1 本**（`DES-005` のみ。**★`DES-002` §4.2 の契約は変えないことが確定した**＝`D-725`）。**★起票は設計卓。自採番しない**（`D-293`） |
| マイグレ消費 | **なし の見込み**（**★次に払い出す番号は `000103`**。**★要ると判明したら報告して止まる**） |
| 前提 | **`M27-01` / `M27-02a` / `M27-02b` は完了・受理・マージ済み**（`D-687` / `D-694` / `D-704` / `D-711`） |

## 更新履歴

| 版 | 更新日 | 更新内容 |
|----|--------|----------|
| 1.2.0 | 2026-09-05 | **★★`§2.5` が (b)「画面側だけ」で確定**（**`D-725`**）。**⇒ 確認事項 0 件・`DES-002` §4.2 の契約変更は無し・CHANGE は `DES-005` の 1 本。★投入可になった** |
| 1.1.0 | 2026-09-05 | **★開発者回答 3 件を反映**（**`D-722`**）。**§2.2 は「案を出させる」から「形の確定」へ ／ §2.4 は暫定案の採用 ／ §7 の残りは 1 件**（`A-4`） |
| 1.0.0 | 2026-09-05 | 初版（**`D-719`**）。**★`M27-overview` の 9 項目のうち射程は 6 項目である**（§0.1） |

---

## 0. この指示書の特殊性

### 0.1 ★★射程は 9 項目ではなく 6 項目である

**`M27-overview` §M27-03 の表は 9 行あるが、うち 3 行は本サブの射程ではない。**

| # | identity | 状態 |
|---|---|---|
| 4 | `SD-010`（ターゲットコンボタブ） | **★`M30` へ移した**（`D-686`。代表の面が `画面: 仮想コントローラ`） |
| 5 | `SD-011`（非表示技・キャラ別技タブ） | **★`M30` へ移した**（`D-686`。同上） |
| 9 | `tag-count-usage-inflated-by-orphan-combo-tags` | **★★既に解消している**（2026-08-24・`D-535`。**orphan の掃除により削除時の判定が水増ししなくなった。供給側は `M23-10` が止めた**） |

**⇒ 本サブがやるのは下表の 6 項目である。**

### 0.2 ★★1 と 3 は逆向きに見えるが両立する

**`P4M-020` は一覧の「欄」を除き、`P4M-022` は「絞り込み」に始動技を足す。⇒ 表示と絞り込みは別である。**

**★混同して「始動技を消すのに始動技で絞るのはおかしい」と判断しないこと。** 開発者の逐語がそれぞれ別に在る（§2.1 / §2.3）。

### 0.3 ★`M27-01` が触った列へ 2 度目に入る

**`P4M-021`（ヒット種別フィルタ短縮）は `hit_type` を触る。同列は `M27-01` が 8 値化したばかりである**（`CHANGE-151`）。

**★★値の集合は変えない。変えるのは見せ方だけである**（開発者の逐語＝「**中身を変えずに**短くできないか？」）。

---

## 1. 前提として読むもの

| 対象 | 何を読むか |
|---|---|
| `docs/instructions/M27-overview.md` v1.2.0 §M27-03 | **9 行の表と §2.3（`M30` への移送）／§2.3.3（`SD-009` を残す理由）** |
| `docs/design/05-screen-design.md` | **§5.5（一覧）／§5.6（詳細）／§5.7（登録・編集。`M27-02b` が追記した必須・任意の印）** |
| `docs/design/06-validation.md` | **§2.7（`hit_type` の 8 値）** |
| `docs/handover/followup-backlog.md` | **`combo-list-setup-count-hides-fetch-failure` ／ `combo-detail-back-link-is-hardcoded`**（**★本表は編集しない**＝`D-382`） |
| `docs/handover/code-facts.md` | **★引く。想定で書かない** |
| ボード `D-686` / `D-535` / `D-646` | 射程の移送 ／ タグ水増しの解消 ／ フェーズ4 の割付規則 |

---

## 2. やること

### 2.1 `P4M-020` — 一覧の始動技欄を除去

**開発者の逐語**＝「**コンボ一覧、レシピが綺麗に表示されているので、始動技を始動状況欄に出す必要はなくなった。消したい。**」

| # | 内容 |
|---|---|
| 1 | **一覧の「始動状況」欄から始動技の表示を除く。** |
| **★2** | **★除くのは「表示」だけである。** **`starter_move_id` のデータ・API 応答・絞り込みには触らない**（§2.3 が絞り込みで使う）。 |
| **★3** | **詳細画面（§5.6）は変えない。** 逐語は一覧の話である。 |

### 2.2 `P4M-021` — ヒット種別フィルタの短縮

**開発者の逐語**＝「**コンボ一覧のフィルターで、ヒット種別が非常に横長。中身を変えずに短くできないか？**」

**★★形は確定している**（2026-09-05 開発者裁定＝`D-722`）。**逐語＝「イメージとしては固定のサイズ＋短縮表記を表示。プルダウンメニュー内ではフルの長さを表示。」**

| # | 内容 |
|---|---|
| **★★1** | **値の集合は変えない**（`M27-01` が定めた 8 値。`DES-006` §2.7）。**変えるのは見せ方だけである。** |
| **★★2** | **閉じているときは「固定のサイズ ＋ 短縮表記」。開いた一覧の中はフルの長さ。** ⇒ **横幅が値の長さで動かなくなる。** |
| **★★3** | **短縮表記の対応表を作ること。★8 値すべてに 1 対 1 で当てる**（`DES-006` §2.7 の 8 値）。**★案を出して開発者の確認を得てから実装する**——**語の選定はユーザー体験に関わる**（`roles-and-routing` ハード列）。 |
| **★4** | **★現在の描画がプルダウンかボタン群かを実査すること**（§6-2）。**開発者の逐語は「プルダウンメニュー内では」を前提にしている。⇒ ボタン群であったなら、この形は当てはまらない。止まって報告する。** |
| **★5** | **`M24-12` のボタン化の線引きに触れる**（`DES-005` §5.7＝「単一選択の欄は、選択肢が 10 以下ならボタン群」）。**★フィルタは単一選択ではない点に注意。⇒ 同じ規則をそのまま当てない。** |

### 2.3 `P4M-022` — 始動技フィルタを足す

**開発者の逐語**＝「**コンボ一覧のフィルターで、始動技も指定したい。**」

| # | 内容 |
|---|---|
| 1 | **一覧の絞り込みに始動技を足す。** |
| **★2** | **キャラ選択との関係を決めること**——**始動技はキャラに属する。⇒ キャラ未選択のときに何を出すかを決めて報告する**（全キャラ分を出す ／ 出さない ／ キャラを先に選ばせる）。**★製造の判断でよいが、選んだ理由を報告に書く。** |
| **★3** | **既存の絞り込み部品を使う。新しい部品を作らない**（`M27-02a` が `SearchableSelect` の波及を 15 コントロール / 13 ファイルと実測している）。 |

### 2.4 `SD-009` — ドライブダメージの符号の説明と表示

**開発者の逐語**（2 か所）＝「**ドライブダメージでマイナスを許容している理由をどこかへ記載したい。コンボ中に相手のドライブゲージが回復していくパターンもあるので、それを表現したかった。実際は 1 回復するくらいで、6 回復はありえないが。**」／「**正負がどっちを表すかいまは良く分からないので、表示を見直したい。**」

| # | 内容 |
|---|---|
| **★★1** | **★これは表示の課題である**（`M27-overview` §2.3.3。開発者の逐語＝「**はい。表示だけです。画面上でわかりにくいというだけの指摘でした**」）。**⇒ 値域も検証も変えない**（`DES-006` `VAL-C13`＝`-6`〜`6`）。 |
| **★★2** | **どちらが「削り」でどちらが「回復」かが画面から読めるようにする。★案を 2〜3 出して開発者に選ばせる**（2026-09-05 開発者裁定＝`D-722`。逐語＝「案を出してもらって私が決める」）。 |
| **★★3** | **マイナスを許容する理由は、画面のヘルプ ＋ `DES-003` の列の説明の両方に書く**（2026-09-05 開発者裁定＝`D-722`）。**★`docs/design/` は製造が編集しない。⇒ `DES-003` 側は該当箇所を報告するだけ。設計卓が CHANGE で反映する。** |
| **★4** | **実測を添えること**——**現在のデータで `drive_damage` が負の行が何件あるか。** |

### 2.5 `combo-list-setup-count-hides-fetch-failure` — 取得失敗が 0 件に見える

| # | 内容 |
|---|---|
| **★★1** | **一覧の「セットプレイ数」列は、バッチ取得に失敗しても全行 `0` になる。⇒ 「本当に 0 件」と区別できない。** ハンドラが warn ログだけで握り `setups` を空にするため。 |
| **★★2** | **区別するには応答へ「取得失敗」を載せる必要があり、`DES-002` §4.2 の契約変更にあたる**（`M24-01` の分岐 B。**同サブは分岐 A で着地したので触っていない**）。 |
| **★★3** | **★★(b)「画面側だけで『取得できなかった』を出す」で確定した**（2026-09-05 開発者裁定＝`D-725`。逐語＝「**画面側でいいかと思っています**」）**。⇒ `DES-002` §4.2 の契約は変えない。案出しも Plan Mode での停止も要らない。** **★★実装の要点は 1 つ**——**バッチ取得が失敗したことを画面が知れる形にすること。★現状はハンドラが warn ログだけで握って `setups` を空にするため、画面から見ると「本当に 0 件」と区別がつかない。⇒ 契約を変えずに区別する経路が実際にあるかを Plan Mode で確かめ、無ければ止まって報告すること。** |
| **★4** | **実害の程度を報告に書く**——**列は既定表示であり、`0` と出た行を利用者が「セットプレイ未登録」と読む。** |

### 2.6 `combo-detail-back-link-is-hardcoded` — 戻り先が固定されている

| # | 内容 |
|---|---|
| **★1** | **`ComboDetailPage` の「戻る」が `<Link to="/combos">` のハードコードであり、どこから来ても一覧へ戻る。** 「比較 → 詳細 → 戻る → 一覧」で違和感が出る（開発者の手動確認・2026-08-26）。 |
| **★★2** | **★`navigate(-1)` 系へ替えるなら、直接 URL で開いた場合の戻り先を決めること**〔履歴が無い場合〕**。⇒ 「履歴が在れば戻る、無ければ一覧」の形にするのが素直だが、判断は製造でよい。理由を報告に書く。** |
| **★3** | **先例が 3 箇所ある**（`ComboEditor` ／ `SetupEditorPage`）。**★新しい流儀を作らない。** |
| **★4** | **`M24-03` はこのファイルを触っていない**〔差分は memo prop を渡す 4 行のみ〕。**⇒ 既存の挙動であり、`SM-003`（比較 → 詳細の導線）が初めて露出させた。** |

---

## 3. 出力

`docs/progress/M27-03-completion-report.md`。

| # | 書き方の規約 |
|---|---|
| **★★1** | **6 項目それぞれについて「何を変えたか」と「何を変えなかったか」** |
| **★★2** | **§2.5 でどちらの案を採ったか、と その理由** |
| **★3** | **`drive_damage` が負の行の実測件数**（§2.4-4） |
| **★4** | **始動技フィルタのキャラ未選択時の挙動と、選んだ理由**（§2.3-2） |
| **★5** | **`docs/design/` に反映が要る箇所の一覧**（**★製造は直さない**） |
| **★6** | **確かめられなかったものは「確かめられなかった」と書く** |

**★`docs/progress/progress-log.md` へ追記すること**（`CLAUDE.md` §8）。

---

## 4. やらないこと

| # | やらないこと |
|---|---|
| **★★1** | **`hit_type` の値の集合を変えること**（`M27-01` が定めた 8 値。§2.2-1） |
| **★★2** | **`drive_damage` の値域・検証を変えること**（`VAL-C13`＝`-6`〜`6`。§2.4-1） |
| **★★3** | **`starter_move_id` のデータ・API・絞り込みを消すこと**（§2.1-2。**消すのは一覧の表示だけ**） |
| **★★4** | **`DES-002` §4.2 の契約を変えること**（§2.5-3。**★★(b) で確定したため、契約は変えない**＝`D-725`） |
| **★★5** | **`SD-010` / `SD-011` に手を付けること**（**`M30` の射程**＝`D-686`） |
| **★6** | **タグの使用数の水増し**（**★既に解消している**＝`D-535`） |
| **★7** | **`docs/design/` の編集**（`CLAUDE.md` §8） |
| **★8** | **`followup-backlog.md` の編集**（`D-382`） |
| **★9** | **CHANGE 番号・マイグレ連番の自採番**（`D-293`） |
| **★10** | **新しい共通部品を作ること**（§2.3-3） |

---

## 5. 完了条件

1. **6 項目すべてに着地がある**（実装した ／ 案を出して止まった、のいずれか）。
2. **§2.2 と §2.4 は開発者の判断を得てから実装している**（§7）。
3. **§2.5 は (b) で実装されている**（**★契約を変えていない**）**。⇒ 契約を変えずに区別できないと分かった場合のみ、止まって報告する。**
4. **`go test ./...` / `cd web && pnpm test` / `make e2e` が緑。**
5. **常設検査が緑**（`check-artifact-integrity.sh` を 1 本目に回す）。
6. **`docs/design/` に反映が要る箇所が一覧になっている**（**製造は直さない**）。
7. **`docs/progress/progress-log.md` へ追記されている。**

---

## 6. ★設計卓が確かめていないこと（実査項目）

| # | 確かめること |
|---|---|
| **★1** | **一覧の「始動状況」欄の現在の中身**（始動技のほかに何が出ているか） |
| **★★2** | **ヒット種別フィルタの現在の描画がプルダウンかボタン群か**（**★★開発者の指定した形は「プルダウン」を前提にしている。⇒ ボタン群だったら形が当てはまらない。止まって報告すること**）。**あわせて 8 値化で横幅がどれだけ増えたか** |
| **★3** | **`drive_damage` が負の行の件数** |
| **★4** | **`ComboDetailPage` の「戻る」の実装と、`navigate(-1)` を使っている 3 箇所の流儀** |
| **★5** | **一覧のセットプレイ数のバッチ取得の失敗経路**（warn ログの位置） |

---

## 7. 開発者への確認事項

**★★4 件すべて決着した**（`D-722` / `D-725`）。**⇒ 投入前に決めることは残っていない。**

| # | 確認事項 | 状態 |
|---|---|---|
| ~~1~~ | ~~ヒット種別フィルタの短縮の形~~ | **★決着＝固定サイズ ＋ 短縮表記。開いた一覧の中はフルの長さ**（§2.2）。**★短縮表記の語は案を出して確認を得る** |
| ~~2~~ | ~~ドライブダメージの符号の見せ方~~ | **★決着＝案を 2〜3 出して開発者が選ぶ**（§2.4-2） |
| ~~3~~ | ~~マイナスを許容する理由をどこへ書くか~~ | **★決着＝画面のヘルプ ＋ `DES-003` の列の説明の両方**（§2.4-3） |
| ~~4~~ | ~~`combo-list-setup-count-hides-fetch-failure` は (a) 契約変更 か (b) 画面側だけ か~~ | **★★決着＝(b) 画面側だけ**（`D-725`。逐語＝「**画面側でいいかと思っています**」）**。⇒ `DES-002` §4.2 は変えない**（§2.5-3） |

**★実装の中で開発者へ返すものが 2 つ残る**〔**確認事項ではなく、実装の途中で出す案である**〕**——短縮表記の語**（§2.2-3）**と、ドライブダメージの符号の見せ方**（§2.4-2）**。**

---

*以上、M27-03 指示書 **v1.2.0**。* **★★本サブの落とし穴は「射程が 9 項目に見えること」である**（§0.1）。**★3 項目は既に他所へ移ったか解消している。⇒ 9 件を実装しようとすると `M30` の面へ入る。**
