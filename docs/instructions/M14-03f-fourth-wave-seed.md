# 指示書 M14-03f: 第四波 seed（残り 14 キャラ）

| 項目 | 内容 |
|------|------|
| 文書ID | M14-03f |
| バージョン | **1.1.0**（2026-09-02。**★★既存行の追従 2 件を射程へ加えた＝`D-693`**〔既存キャラの `custom_states` 未設定 ／ `kimberly` の最大値の追従〕。**★開発者が §0.3-2 の「判定材料が無い」を埋めた**） ／ **1.0.1**（2026-09-02。**★レビューチェックリストを追発行し `レビュー` 欄を更新＝`D-692`。★本文の指示は 1 文字も変えていない**） ／ 1.0.0（2026-09-02・起票） |
| 作成日 | 2026-09-02 |
| 作成者 | 設計担当 Claude（設計卓／フェーズ4 期） |
| 対象マイルストーン | **`M14`**（公式データ配布是正・配布 DB 同梱）。**★独立レーン。★ただし公開前に必ず完遂する**（`D-634` (7)） |
| 推奨モデル | **高精度実装**（**★データの投入であり、取り残しがエラーにならない**。実モデル名は `docs/human-notes/model-allocation.md` が正本） |
| Plan Mode | **必須**（`CLAUDE.md` §9＝スキーマ・データ移行を伴う変更。**★★さらに §0.2 の判定を Plan Mode の最初の一手にする**） |
| レビュー | **必要**（データ投入。**★チェックリスト＝`docs/instructions/reviews/M14-03f-review-checklist.md` v1.0.0。★★走行中に追発行した**＝`D-692`。**当初「着手承認後に起こす」と書いていたが起こしていなかった**） |
| CHANGE 消費 | **なし の見込み**（**★設計書を動かさない。動かす必要が出たら報告して止まる**） |
| マイグレ消費 | **13〜16 本の見込み**（**★★着手時に設計卓へ連番を請求すること。自採番しない**＝`D-293`） |
| 配置 | `docs/instructions/M14-03f-fourth-wave-seed.md` |
| 出力 | `docs/progress/M14-03f-completion-report.md` |

---

## 0. この指示書の特殊性

### 0.1 ★★本サブは「5〜6 段」ではない

**`M14-overview` §3 は長く「前提マイグレ 2 本を含む 5〜6 段」と書いていたが、これは 2026-09-02 に是正した**（`D-655`。同書 1.4.0）。

**実測＝13 段。条件付きでさらに 0〜3 段。**（一次源＝`docs/progress/M14-RESEARCH-03-report.md` §2.4-3）

**★★本数はキャラ数に比例しない。段の数で決まる**——第二波は **1 キャラで 6 本**、第三波は **6 キャラで 9 本**だった。**⇒ 波を分割すると段の数が波の数だけ掛かる。**

### 0.2 ★★Plan Mode の最初の一手＝波の切り方を決める材料を出す

**`M14-RESEARCH-03` §2.3-4 は次を判定できなかった**（生成物の作成が要り、RESEARCH の射程外だったため）。

> **`c_viper` / `dhalsim` の「UNIQUE 衝突で起動不能」経路が現在も成立するか。**

**★この判定が波の切り方を決める。**

| 判定 | 波の切り方 | マイグレ本数 |
|---|---|---:|
| **成立しない** | **14 キャラを 1 波** | **13〜16 本** |
| **成立する** | **12 キャラ ＋ 最終波 2 キャラ**（`c_viper` / `dhalsim`） | **★段が 2 回掛かる。26 本前後** |

**★★差が大きい。⇒ 判定を先にする。**

**判定の手順**（**★Plan Mode の中で行い、結果を提示して承認を待つ**）:

1. `c_viper` / `dhalsim` を含む形で `seedgen` を回し、生成物をスクラッチ領域に作る（**★リポジトリへコミットしない**）。
2. **`000075_m20_preset_aliases_unique` を含む全マイグレを適用したクリーン DB へ、生成物を適用してみる。**
3. **UNIQUE 制約に衝突するか。衝突するなら、どの制約のどの行か。**
4. **★衝突しないなら「必ず最終波」の制約は失効している。⇒ そう報告する。**

**★★判定結果を出したら止まり、波の切り方の承認を待つこと**（**波の切り方は設計卓と開発者が決める**＝`M14-RESEARCH-03` §4-4）。

### 0.3 ★★開発者の手番が 2 件ある。揃っていなければその段を落とす

| # | 前提 | 無いとどうなるか |
|---|---|---|
| **1** | **移動 5 code の `total` の実測値**（`dash_forward` / `dash_back` / `jump_*`）**を 14 キャラぶん受領していること** | **★★段「移動 `total` の backfill」を投入できない。⇒ 該当 70 行が NULL のまま残る** |
| **2** | **`yasmine` の `custom_states` 定義**（種別・値域・増減） | **★段「`custom_states`」を投入できない** |
| **★★2b** | **【2026-09-02 追加＝`D-693`】既存キャラのうち `custom_states` が要るキャラと、その定義** | **★★本欄はもともと「`yasmine` 以外の 11 キャラに `custom_states` が要るかは、リポジトリ内に判定材料が無い」**（`M14-RESEARCH-03` §5-5）**と書いていた。⇒ 2026-09-02、開発者がその判定材料を供給した**〔逐語＝**「比較的最近挿入したキャラクターは設定をしていなかった」**〕。**⇒ 「要るのに入っていない」が確定した。★詳細は §2.2b** |

**★★揃っていなければ、その段だけを落として残りを完成させること。★止まらない。** **⇒ 落とした段は完了報告 §残課題へ「未投入の段」として名指しで書く**（**★「あとで」と書かない。何が何行 NULL のまま残っているかを書く**）。

### 0.4 ★★「1 回きりの backfill は後から INSERT される行を拾わない」

**`migrations/000062` のヘッダが 2026-08-02 に書き残していた一般化**（逐語）:

> **一般化: 「1 回きりの backfill マイグレ」は、その後に INSERT される行を拾えない。取り残しはエラーにならず、unknown / false のまま静かに残る。……→ 今後の seed 波は、既存の backfill マイグレの内容を自波の行にも適用すること。**

**★★これが本サブの最大の落とし穴である。★★取り残してもテストは緑になる。**

**⇒ §5 の完了条件に「各 backfill 列について、第四波の行で NULL / 既定値のまま残っているものが 0 件であること」を数えさせる項目を置いた。**

---

## 1. 前提として読むもの

| 対象 | 何を読むか |
|---|---|
| **`docs/progress/M14-RESEARCH-03-report.md`** | **★★本サブの一次源。§2.1（母数）／§2.2（13 段の全数）／§2.2-6（エイリアス再適用）／§2.2-9（`M18` 以降の積み増し）／§2.3（失効判定）／§2.4-3（本数の見込み）** |
| `character_data/seed-progress.md` | **★「seed 波ごとに再適用する規則」節に seedgen 5 コマンドが bash で書かれている。★ただし同節は 7 項目を含まない**（§2.2-13 参照。**下記 §2.4**） |
| `docs/instructions/M14-03e-third-wave-code-quality.md` | **★第三波の実際の工程。最良の一次資料** |
| `docs/instructions/M14-overview.md` §3 | **★1.4.0 で 13〜16 段へ是正済み** |
| `docs/process/m20-contract.md` §1.1-3 | エイリアス生成規則の再適用 |
| `docs/handover/followup-backlog.md` §BD | **★本サブ由来の 9 行。うち 5 行が本サブの工程である** |
| `docs/handover/code-facts.md` | **★引く。想定で書かない** |

---

## 2. やること

### 2.1 対象キャラ

**★母数から数え直すこと**（`M14-RESEARCH-03` の値は 2026-09-01 時点である）。

| 区分 | 数 | 顔ぶれ |
|---|---:|---|
| **`characters` に行が無い** | **12** | `aki` `akuma` `alex` `blanka` `cammy` `chun_li` `dee_jay` `e_honda` `ed` `elena` `sagat` `yasmine` |
| **移動 9 種のみの仮登録** | **2** | `c_viper` / `dhalsim`（**★攻撃技 0・`total` 全 NULL**） |
| **合計** | **14** | |

**★`character_data/*.csv` は 31 ファイルで、31 キャラすべてが `seed-progress.md` で `precheck=済`。⇒ 手入力待ちは 0 である**（2026-09-01 実測）。**★数え直すこと。**

**★★仮登録 2 体は `characters` の行が既に在る。⇒ 段 1（`characters` 行の投入）の対象から外れ、段 2 以降だけが要る。この差を段の設計に反映すること。**

### 2.2 段の全数（13 段 ＋ 条件付き 0〜3 段）

**★一次源＝`M14-RESEARCH-03` §2.4-3。★各段について、第四波での要否を 1 つずつ判定してから着手すること。**

| 段 | 内容 | 備考 |
|---:|---|---|
| 1 | **`characters` 行の投入** | **★仮登録 2 体は対象外** |
| 2 | **移動 system move 9 種の投入** | **★`000025` は遡って埋めない。⇒ 波ごとに要る** |
| 3 | **`moves` 本体 ＋ `official_ja_move` エイリアス**（seedgen） | **★同じファイルに出る** |
| 4 | **`is_derived` backfill**（seedgen `-mode derived-backfill`） | |
| 5 | **`move_commands`**（seedgen `-mode move-commands`） | |
| 6 | **`is_projectile` backfill** | **★seedgen は出力しない。⇒ 別段** |
| 7 | **移動 5 code の `total` backfill** | **★★開発者の実測値受領が前提**（§0.3-1） |
| 8 | **`chain_cancel_total`** | **★`elena` の 2 行が確定して存在する**（`M14-RESEARCH-03` §4-5。**確定値 55 件・18 キャラ に対し DB は 53 件・17 キャラ**） |
| 9 | **frame_cost** | |
| 10 | **エイリアス `numeric`**（seedgen `-mode aliases -preset numeric`） | **★`-movement-chars` にその波で増えたキャラを渡す** |
| 11 | **エイリアス `srk`**（同 `-preset srk`） | 同上 |
| 12 | **`P-34` の `numeric`**（`-noinput-only`） | **★★`seed-progress.md` の手順に無い**（§2.4） |
| 13 | **`P-34` の `srk`**（同） | 同上 |
| **+1** | **`custom_states`** | **★★条件付き。`yasmine` の定義が要る**（§0.3-2） |
| **+1** | **`fastest_unreachable` の人手判断分** | 条件付き |
| **+1** | **`move_derivations`** | **★条件付き。`M14-RESEARCH-03` §4-7＝seed 波は一度も投入していない** |

### 2.2b ★★既存行の追従（**2026-09-02 追加＝`D-693`**・2 件）

**★★本節は「第四波の投入」ではない。⇒ §2.2 の 13 段とは別に数えること。混ぜると、どちらも数が合わなくなる。**

**開発者の逐語（2026-09-02）**——**「custom_states の実測を伝達中に、比較的最近挿入したキャラクターは設定をしていなかった。kimberly はアップデートで最大値が更新されていたのだが、反映していなかった。」**

**★★2 件は性質が違う。⇒ 分けて扱うこと。**

| # | 件 | 性質 | 前提 |
|---|---|---|---|
| **A** | **既存キャラの `custom_states` 未設定** | **★deferred が積み上がったもの**（§2.2b.1） | **★キャラごとの定義が要る**（ドメイン知識） |
| **B** | **`kimberly` の最大値の追従** | **★外部データの変化への追従**（§2.2b.2） | **★値 1 つ**（開発者が供給） |

#### 2.2b.1 A — 既存キャラの `custom_states` 未設定

**★★これは新しく壊れたのではない。設計書が 2026-08-25 から記録していた状態である。**

> **`DES-003` §3.2 の逐語**——**「上の例のうち、seed に定義が入っているものと入っていないものがある（2026-08-25 実測＝19 キャラ中 11 キャラの `custom_states` が SQL NULL。`M14-03d` が定義の投入をスコープ外としたためである）。★★「入っていない」は「扱わない」を意味しない。」**

**★★⇒ 今回の発見は「知らなかったこと」ではなく、「記録されていたのに誰も手番を割り当てていなかったこと」である。**（同型＝`D-465` の「セル内 `|` の未エスケープは別コミットで直す」が 13 日直らなかった件）。

**★★あわせて `seed-progress.md` の穴が効いている**——**§2.4-7 が示すとおり、同書の「seed 波ごとに再適用する規則」節に `custom_states` は含まれていない。⇒ 波を重ねるたびに落ち続ける構造だった。★完了条件 7 の補完は「あったらいいもの」ではなく、実害が出ている穴の是正である。**

| # | やること |
|---|---|
| **★★1** | **母数を出す。`characters` の全行について `custom_states` が NULL か否かを数え、NULL のキャラを名指しすること。★第四波で増える 14 体を含めた投入後の母数も出すこと**（**`DES-003` の「19 キャラ中 11 キャラ」は 2026-08-25 の実測であり、第四波で母数が変わる**） |
| **★★2** | **NULL のキャラを 3 つに仕分ける**——**(a) 定義が既に在る**〔設計書・CSV・過去のマイグレのいずれかに具体値が在る〕**／(b) 定義が無く、開発者の供給が要る ／(c) そもそも `custom_states` が要らないキャラ**。**★仕分けの根拠を 1 キャラずつ書くこと** |
| **★★3** | **(a) だけを投入する。★(b) は名指しして止める。★(c) は「要らない」と判定した根拠を書く** |
| **★★4** | **★★(b) の定義を推測で作らないこと**（§4-6 と同じ禁止）。**★`DES-003` §3.2 は「型・発動条件の具体的な列挙値は実装時に開発者と協同で決定する」と定めている。⇒ 製造が決める事項ではない** |
| **★5** | **(b) が 1 件でも在るなら、その一覧を開発者へ提示すること。★本サブの中で供給されれば投入してよい。★供給されなければ落として名指しで報告する**（§0.3 の「落とした段」の扱いと同じ） |

#### 2.2b.2 B — `kimberly` の最大値の追従

**開発者の逐語＝「kimberly はアップデートで最大値が更新されていたのだが、反映していなかった。」**

| # | やること |
|---|---|
| **★★1** | **現在値と正しい値の両方を報告に書くこと。★「更新した」だけでは、後から何が変わったか分からない** |
| **★★2** | **★A と同じ段にしないこと。** **A は「入っていない」、B は「入っているが古い」であり、直し方も検算のしかたも違う** |
| **★3** | **`custom_states` の JSON 構造は `DES-003` §3.2 が正本である**（`type: level` の値域の持ち方）。**★構造を変えないこと。変える必要が出たら止めて報告する** |
| **★★4** | **★同じことが他キャラにも起きていないかを見ること**——**外部アップデートで値が変わるのは `kimberly` 固有の現象ではない。⇒ `custom_states` を持つ全キャラについて、値が現行の公式データと合っているかを確かめ、母数と不一致の数を報告に書くこと。★不一致が在っても直すのは開発者が値を供給したものだけ** |

#### 2.2b.3 ★★射程の境界

| # | 境界 |
|---|---|
| **★★1** | **本節は `custom_states` に限る。⇒ 他の既存行の是正へ広げないこと**（**★「ついでに」の連鎖を止める。★見つけたものは報告に書き、直さない**） |
| **★★2** | **★第四波の 14 体の `custom_states` は §2.2 の条件付き段（`yasmine` の定義が要る）であり、本節ではない。⇒ 数を分けて出すこと** |
| **★3** | **マイグレは §2.2 の連番の続きを使う**（`000092` 以降）。**★上限は切っていない。★本数が増えることは完了報告で報告する**（`D-689`） |
| **★4** | **CHANGE は不要の見込み**（**データであり設計書を動かさない**）。**★ただし `DES-003` §3.2 の「19 キャラ中 11 キャラが NULL」は本サブの完了で失効する。⇒ 新しい実測値を報告に書くこと。設計書は設計卓が追随する** |

---

### 2.3 ★★backfill の取り残しを作らない（§0.4）

| # | やること |
|---|---|
| **★★1** | **既存の backfill マイグレを 1 本ずつ読み、その内容を第四波の行にも適用すること**（`000062` ヘッダの一般化）。**★「既存の backfill」の全数を先に出すこと** |
| **★★2** | **段 6 / 7 / 8 / 9 は、いずれも「1 回きりの backfill」の性格を持つ。⇒ 第四波の行に当たっているかを投入後に数えること** |
| **★3** | **数え方**——**第四波で INSERT した `moves` の行のうち、各列が NULL / 既定値のまま残っている行数。★0 件でないなら、意図した 0 でないものを名指しすること** |

### 2.4 ★★`seed-progress.md` の手順に無いもの（**手順どおりだと落ちる**）

**`M14-RESEARCH-03` §4-6 / §2.2-13 の実測**——**同書の「seed 波ごとに再適用する規則」節は seedgen 5 コマンドを持つが、次を含まない。**

| # | 含まれていないもの |
|---|---|
| 1 | **`characters` 行の投入** |
| 2 | **移動 9 種の投入** |
| 3 | **`is_projectile`** |
| 4 | **移動 `total`** |
| 5 | **`chain_cancel_total`** |
| 6 | **frame_cost** |
| 7 | **`custom_states`** |
| **★★8** | **`P-34` の 2 本**（`000076` / `000077`・`M20-06`・`-noinput-only`）。**★同マイグレのヘッダは同じ再適用要求を持つ。⇒ 手順どおりに実行すると落ちる。★落ちてもエラーにならない** |

**★★本サブの中で `seed-progress.md` の当該節を上記 8 件で補うこと**（followup `seed-progress-procedure-misses-p34-aliases`）。**⇒ 次の波が同じ穴に落ちないようにする。**

### 2.5 down マイグレーション

| # | やること |
|---|---|
| **★★1** | **`characters` を削除する down は、利用者の作ったコンボを孤児にする**（followup `character-down-migrations-orphan-user-combos`）。**★第一波・第三波の down はどちらも `DELETE FROM characters` の 1 文だけである**（`M14-RESEARCH-03` §2.3-2 の実測）。**⇒ 同型で書くと同じ経路になる** |
| **★★2** | **`docs/process/architecture-patterns.md` §11 の規約に従うこと**（**★2026-08-25・`D-552` で設計卓が置いた。★中身は本指示書では引いていない。実物を読むこと**） |
| **★3** | **`move_derivations` の明示削除責務**（followup `move-derivations-explicit-delete-on-reseed`）。**★第三波の down は `move_derivations` に触れていない。⇒ 第四波でどうするかを決めて、決めた内容を down のヘッダに書くこと** |

---

## 3. 出力

`docs/progress/M14-03f-completion-report.md`。

| # | 書き方の規約 |
|---|---|
| **★★1** | **すべての件数に母数を付ける** |
| **★★2** | **§2.2 の段ごとに「投入した／落とした／不要だった」を 1 段ずつ書くこと。★落とした段は理由と、何が何行 NULL のまま残っているかを書く** |
| **★★3** | **§0.2 の判定結果**（UNIQUE 衝突経路の成立可否）**を書くこと。★「必ず最終波」の制約が失効しているかの結論を明示する** |
| **★★4** | **§2.3 の取り残し件数を列ごとに書くこと。★0 件なら 0 件と書く** |
| **★5** | **消費したマイグレ連番の全数と、1 本ずつの内容** |
| **★6** | **`seed-progress.md` を補った内容**（§2.4） |
| **★7** | **確かめられなかったものは「確かめられなかった」と書く。★推測で埋めない** |

**★`docs/progress/progress-log.md` へ追記すること**（`CLAUDE.md` §8）。

---

## 4. やらないこと

| # | やらないこと |
|---|---|
| **★★1** | **CSV の作成・修正**（**★手入力 CSV は開発者の作業である**） |
| **★★2** | **マイグレ連番の自採番**（**★着手時に設計卓へ請求する**＝`D-293`。**★`M28` も `000081` 以降を使う見込みがあり、衝突しうる**） |
| **★★3** | **`docs/design/` の編集**（`CLAUDE.md` §8） |
| **★★4** | **`followup-backlog.md` の編集**（`D-382`。**★候補として報告するだけ**） |
| **★★5** | **波の切り方の独自決定**（**★§0.2 の判定結果を出して止まる**） |
| **★★6** | **前提が揃わない段の「推測での投入」**（**★実測値が無い `total` を推測で埋めない**） |
| **★★8** | **【2026-09-02 追加＝`D-693`】`custom_states` 以外の既存行の是正**（§2.2b.3-1）。**★「ついでに」の連鎖を止める。★見つけたものは報告に書き、直さない** |
| **★★9** | **【2026-09-02 追加＝`D-693`】`custom_states` の定義を推測で作ること**（§2.2b.1-4）。**★`DES-003` §3.2 が「開発者と協同で決定する」と定めている** |
| **★7** | **`c_viper` / `dhalsim` の仮登録行の削除**（**★既存行であり、利用者データが紐づいている可能性がある。★更新で足すこと**） |

---

## 5. 完了条件

1. **§0.2 の判定結果が報告に在り、波の切り方が承認されている。**
2. **§2.2 の 13 段（＋条件付き）が 1 段ずつ「投入した／落とした／不要だった」で埋まっている。**
3. **★★§2.3 の取り残しが列ごとに数えられており、意図しない残りが 0 件である。**
4. **★対象キャラの `moves` が 1 行以上入っている**（**★母数＝対象キャラ数。★仮登録 2 体も攻撃技を持つようになっていること**）。
5. **★`elena` の `chain_cancel_total` 2 行が入っている**（**★確定した工程である**）。
6. **エイリアスが 3 プリセット ＋ `P-34` の 2 本すべてに再適用されている**（**★`preset_aliases` の行数を、投入前後で数えて差を書くこと**）。
7. **★`seed-progress.md` の手順が §2.4 の 8 件で補われている。**
8. **down マイグレが §2.5 の 3 点を満たし、その判断が down のヘッダに書かれている。**
9. **`go test ./...` が緑。E2E は `make e2e` が緑**（**★`pre-dist-e2e-clean`＝クリーン DB での全 E2E は本サブの範囲外。★別途扱う**）。
10. **`docs/progress/progress-log.md` へ追記されている。**
11. **★★【2026-09-02 追加＝`D-693`】既存キャラの `custom_states` について、母数（全キャラ数）・NULL の数・(a)/(b)/(c) の仕分けの数がすべて報告に在る**（§2.2b.1）。**★(b) が在るなら名指しされている。★数が無い記述は完了条件を満たさない。**
12. **★【2026-09-02 追加＝`D-693`】`kimberly` の最大値について、現在値と正しい値の両方が報告に在る**（§2.2b.2）。**★あわせて `custom_states` を持つ全キャラの値が公式データと合っているかの母数と不一致の数が在る。**
13. **★【2026-09-02 追加＝`D-693`】§2.2b の投入と §2.2 の第四波の投入が、報告の中で別々に数えられている**（§2.2b.3-2）。**★混ぜて 1 つの数にしていない。**

---

## 6. 参照ドキュメント

| ID | パス |
|---|---|
| **一次源** | **`docs/progress/M14-RESEARCH-03-report.md`** |
| 手順 | `character_data/seed-progress.md`（**★§2.4 の 8 件が欠けている**） |
| 過去の波 | `docs/instructions/M14-03d-second-wave-manon.md` ／ `M14-03e-third-wave-code-quality.md` |
| overview | `docs/instructions/M14-overview.md` 1.4.0 §3 |
| 契約 | `docs/process/m20-contract.md` §1.1-3 |
| down の規約 | `docs/process/architecture-patterns.md` §11（**★実物を読むこと**） |
| followup | `docs/handover/followup-backlog.md` §BD |
| CLAUDE.md | §5 / §7 / §8 / §9 / §10 |

---

## 7. 開発者への確認事項

**★着手前に 2 件そろっている必要がある**（§0.3）。**★そろっていなければ、その段を落として残りを完成させる。止まらない。**

| # | 何が要るか | 無いとどうなるか |
|---|---|---|
| **1** | **移動 5 code の `total` の実測値・14 キャラぶん** | **段 7 を投入できない。該当 70 行が NULL のまま残る** |
| **2** | **`yasmine` の `custom_states` 定義**（種別・値域・増減） | **段「`custom_states`」を投入できない。★`yasmine` 以外の 11 キャラに `custom_states` が要るかは、リポジトリ内に判定材料が無い** |

**★★あわせて 1 件、着手時に設計卓へ請求すること**——**マイグレ連番の払い出し**（13〜16 本）。**★自採番しない。★`M28` も `000081` 以降を使う見込みがあり、衝突しうる。**

---

*以上*
