# M37 overview — リリース前要望の束（`A` 運び量 UI ＋ `B01`〜`B15`）

| 項目 | 内容 |
|------|------|
| 文書ID | M37-OVERVIEW |
| バージョン | **1.14.0**（2026-09-14・**★★`M37-07` を受理した**＝`D-881`。**⇒ 重複判定キーが 7 → 8 要素になった。★★★`M37` は 8 サブとも受理済みであり、`B02`**〔フェーズ5 送り〕**を残して閉じた**） ／ **1.13.0**（2026-09-14・**★★`M37-04` を受理した**＝`D-877`。**⇒ `B06` は解消した**〔確定反撃サーチが初段の発生で判定するようになった〕**。★`B02` は請求のまま開発者の判断を待つ。★★8 サブのうち 7 サブが受理済みであり、残るのは `M37-07` 1 本である**） ／ **1.12.0**（2026-09-14・**★★`M37-06` が着地した**＝`D-876`。**⇒ `B04` は解消した**〔一覧・詳細・比較・セットプレイの修飾がバッジで描かれる〕**。★8 サブのうち 7 サブが着地**） ／ **1.11.0**（2026-09-14・**★★`M37-04` へ `000116` を追加払い出し**〔3 本目〕**。⇒ `M37-07` を `000117` へ動かした**＝`D-875`。**★着地順と番号順を一致させるため**） ／ **1.10.0**（2026-09-13・**★★`M37-07` を新設し三点セットを発行した**＝`D-874`〔始動技の持続当てを重複判定キーへ。**マイグレ `000116` 払い出し**〕**。★あわせて `M37-06` を v1.1.0 へ**〔**`M37-04` と並列可へ緩和**〕） ／ **1.9.0**（2026-09-13・**★★`M37-06` を新設し三点セットを発行した**＝`D-873`。**★`B04` / `B05` の未了分＝modifier の項目確定と表現形式。★flags は 10 → 14 値へ**〔削除 3 / 追加 7〕） ／ **1.8.0**（2026-09-13・**★★`M37-05` が着地した**＝`D-871`。**⇒ `M37` は 6 サブすべてが着地した**〔`M37-04` は走行中〕**。★不変条件は片方向であることが判明した**＝計測点 `M-177`） ／ **1.7.0**（2026-09-13・**★★`M37-02` が着地した＝`D-867`。⇒ `M37` は全 6 サブが着地した**。**★★★`B04` は「一部完了」である**〔語彙は揃ったが、編集画面のバッジと閲覧面の平文という*表現形式*が揃っていない。**⇒ これが開発者の要望の本体であった**〕） ／ **1.6.0**（2026-09-13・**★★`M37-03` が着地した＝ただし 3 件中 1 件だけである**〔`B01` 着地 ／ **`B03` は開発者裁定で本 MS から除外** ／ **`B15` は push 後に開発者裁定で差戻**〕**＝`D-866`。★`hit_type` は 8 値のままである**） ／ **1.5.0**（2026-09-13・**★★サブが 6 本になった＝`M37-05` を新設し、三点セットを発行した**＝`D-865`。**★開発者承認済み**〔逐語＝「M37-05 の新設は承認します」〕**／ 由来は `P-60` の決着**＝`D-864`） ／ **1.4.0**（2026-09-13・**★★`M37-02` の三点セットを発行した**＝`D-862`。**★★★群 (i) の件数を是正した＝8 件ではなく 10 件である**〔`B04` / `B05` を数え落としていた。設計卓の計数誤り＝計測点 `M-172`〕） ／ **1.3.0**（2026-09-13・**★★`M37-04` の三点セットを発行した**＝`D-861`。**★列名を `starter_startup` から `first_hit_startup` へ差し替えた**〔`M19-DESIGN-07` §7 の列追加審査〕**／ マイグレ `000114` 払い出し**） ／ **1.2.0**（2026-09-13・**★★`M37-01` / `M37-03` の三点セットを発行した**＝`D-859`。**★あわせて `M33-02` との順序を固定**〔`M37-03` → `M33-02`〕） ／ **1.1.0**（2026-09-13・**★★§7 の確認事項が決着＝`D-855`**〔サブ分割 5 本承認 ／ `B14` は開発者が実機でチェック ／ `B06` は開発者の仮説を RESEARCH で検証〕**／ `M37-RESEARCH-01` 指示書 v1.0.0 を発行**） ／ **1.0.0**（2026-09-13・起票） |
| 作成者 | 設計担当 Claude（設計卓） |
| 位置づけ | **`M37` のサブ構成の正本** |
| 承認 | **★開発者承認済み**（2026-09-13・逐語＝「M37 承認します」） |
| 由来 | `D-853`（リリース前要望の束を受理）／ 開発者依頼の Codex 調査レポート（`tmp/20260912-phase4-user-requests-design-handover.md`）／ `Memo_Someday.txt` 424〜466 行 |
| フェーズ | **フェーズ4**（**★開発者の明示的な意向＝「リリース前にやりたかった事」**） |

---

## 0. この overview の特殊性

### 0.1 ★★★「後日でもいい」の過去記述を、延期の根拠にしない

**★開発者の逐語**（2026-09-12）**＝「今回のセッションで会話した件はリリース前（＝フェーズ4内）でやりたかった事なので、設計卓へ伝達したい」。**

**⇒ 過去に `B02` / `B03` へ付いていた「後日でもいい」を、今回の意向より優先してフェーズ5送りの根拠にしない**（レポート §1）**。★「後日でもいい」の過去記述自体は歴史として残す。**

**★★あわせて `D-815` の「『あると良い』は全部フェーズ5」という一般的な推奨も、今回の 16 項目には当てない。⇒ 個別の延期承認を示すものではない**（レポート §5）**。**

### 0.2 ★★★全件を新規実装するという意味ではない

**★レポート §1 の逐語＝「全件を新規実装するという意味でもない。既存実装と照合し、満たされている要件は証拠付きで閉じ、残る分を具体的なフェーズ4のサブへ割り付けてほしい」。**

**⇒ 各サブの段 1 は*実査*である。★「満たされている」と閉じるときは、今回の要求を満たす具体的な証拠を付けること**（レポート §6-2）**。**

**★★★類似機能の過去の完了を、そのまま本要求の完了根拠にしないこと**（レポート §4 の共通判定）**。**

### 0.3 ★★★設計卓は実装を読めない

**★武装中のため `web/src/` `internal/` を作業ツリーに持たない。⇒ `B05` / `B06` / `B11` は「いま何が出ているか」を数えないと射程が決まらない。**

**⇒ `M37-RESEARCH-01` を先に 1 本置く**（§3）**。★これが本 overview の構成上の要である。**

### 0.4 ★★ID は照合用である

**★`A` / `B01`〜`B15` はレポート内の照合用 ID であり、正式課題 ID ではない。⇒ 一次源は `docs/human-notes/Memo_Someday.txt` 424〜466 行付近。**

**★★`D-816` は「16 項目」と記録しているが、実際に列挙された箇条書きは 15 件である。⇒ 記録上の数をそのまま課題件数にしないこと**（レポート §5）**。★本 overview は `B01`〜`B15` の 15 件 ＋ `A` の 1 件で数える。**

---

## 1. 射程

### 1.1 `A` — 運び量・始動位置の 3 方式入力 UI

| 項目 | 内容 |
|---|---|
| **要求** | コンボの運び量を記録し、トレーニングモードの床の目盛り（マス数）やパーセントで入力・確認できること。始動位置の入力も画面へ反映する |
| **確定済み設計** | **`D-730` / `D-731`**（2026-09-05）**＝床の物差しは 0〜160 マス ／ 始動位置は 7 区分 ／ 従来の区分選択・マス数・パーセントの 3 方式を連動させる** |
| **★★不変条件** | **始動位置と運び量は*別の値*である。⇒ 運び量から始動位置を導出しない。★区分を持つのは始動位置であり、運び量は区分へ丸めない** |
| **実装済み** | **DB の `start_position_mass` / `carry_distance_mass`（`000105`）／ API ／ CSV ／ フロントの型・スキーマ** |
| **★★未実装** | **入力・表示コンポーネントへの配線。⇒ 3 方式の連動 UI が画面に無い** |
| **経緯** | **`M28-02a` が「画面は `M28-02b`」とし、`M28-02b` の射程に入っていなかった。⇒ 計測点 `M-137` が「射程を送ったつもりで着地させていなかった」と認定。★その後 `D-758` がフェーズ5 へ移した** |
| **★★★差し戻し** | **今回の意向によりフェーズ4 へ戻す**（`D-853`）**。★`D-668`「画面実装を後にしてよい」はゲームアップデート追従の文脈であり、運び量 UI の個別の延期承認ではない**（レポート §3 の実査） |

**★あわせて扱うもの**（レポート §6-4）**＝`combo-carry-distance-record` の状態欄を実態へ**（未着手 → 基盤完了・画面未完）**／ `duplicate-precheck-ignores-position-mass` ／ `position-bands-duplicated-go-and-ts`。**

### 1.2 `B01`〜`B15` — 面ごとの分類

**★★★別々の要求と受入条件は消さない**（レポート §6-5）**。⇒ 束ねるのは「同じ面を触る」ためであり、要求を 1 つに畳むためではない。**

| 群 | ID | 要求 | ★受理時に保持する条件 |
|---|---|---|---|
| **(i) 仮想コントローラ UI**（**★★10 件**。**★★★旧記載の「8 件」は誤りである**〔`B07`〜`B14` の 8 件だけを数え、`B04` / `B05` を落としていた。2026-09-13 是正＝計測点 `M-172`〕） | `B04` | modifier を整ったデザインで表示 | **★★★【2026-09-14＝完了】**（`M37-06`・`D-876`）**＝一覧・詳細・比較・セットプレイの修飾が編集画面と同じバッジで描かれるようになった。⇒ ★波括弧は廃止していない**〔ホバー・エクスポート・コピーは生文字列のまま〕**。★修飾の位置が面によって違うのは*意図した非対称*である。** 以下は 2026-09-13 の記述： **【一部完了】語彙は揃った**〔`M37-02` が flag 10 値すべてに表示語を与え、`{内部識別子}` は出なくなった〕**。⇒ 残るのは*表現形式*である**〔編集画面は青バッジ ／ 一覧・詳細は波括弧付き平文〕**。★開発者の逐語＝「新規登録、編集の段階で modifier をつけた時の見た目と一致させたい」。⇒ `followup` §DF の `modifier-display-differs-editor-and-viewer` へ送る**（`D-867`）**。★★波括弧そのものは残す**〔平文には色が無く、波括弧が修飾を示す唯一の代替である〕**。** 以下は起票時の記述： **★生の `{}` 等で囲んだ表現をそのまま出さない**〔**★★この言い換えが原文から離れていた**＝計測点 `M-175`〕 |
| | `B05` | modifier の整理 | **★★現在出ているものを実査し、必要なもの・不足しているものを探す。⇒ 見た目の変更だけで閉じない** |
| | `B07` | ラッシュ版のボタン位置を統一 | 通常技と特殊技で配置が不統一 |
| | `B08` | 必殺技の強度選択を右側へ配置 | **★技数の多いキャラで縦スクロールが多くなる問題を減らす** |
| | `B09` | 必殺技部分を縦スクロール可能なグループへ | **★仮想コントローラ上部・下部のレシピを確認するためのページ上下往復を減らす** |
| | `B10` | クイック入力と入力説明欄を縮小 | **★★3 文は*削除*要求である。⇒ 折りたたんで残すという要求ではない**（§1.3 に逐語） |
| | `B11` | 仮想コントローラの説明・情報量を整理 | **★★「押している入力〜解決できなかった入力」のうち*必要な情報を見定めて*減らす** |
| | `B12` | 長い仮想コントローラのレシピを縦に表示 | **★`...` で要約せず確認できるようにする** |
| | `B13` | 強度ボタンの横幅を縮める | 弱・中・強・OD を並べる |
| | `B14` | 仮想コントローラ全体のボタンを小さくする | **★★目的は「縦スクロールせずに全レシピを入力したい」である。⇒ 単なる寸法変更で達成判定しない** |
| **(ii) 一覧・マイコンボ・ゴミ箱** | `B01` | ゴミ箱を削除日時で並べる | **★削除日時の*表示*や、二重削除時の日時保持とは別の要求である。⇒ 並び順への対応を確認する** |
| | `B03` | マイコンボで特定タグを固定表示する設定 | **★★★【2026-09-13 開発者裁定＝本 MS から除外】**（逐語＝「再検討してみたが、あまり有用な機能に思えないので、本マイルストーンから除外し」）**。⇒ 実装しない。★除外の裁定が出る前に実査は完了していた。⇒ 「難しいから落とした」ではない。** 以下は起票時の記述： 過去は「後日でもいい」。**★今回の意向を優先**〔**★★履歴として両方を残す**＝`D-853`〕 |
| | `B15` | ヒット種別に空中ヒットを追加 | **★★★【2026-09-13 開発者裁定＝差戻】**（逐語＝「空中ヒットについては区分としては不要でした。……ヒット区分はいらなかったです」。**★全文は `D-866` / 計測点 `M-174`**）**。⇒ 実装・レビュー・取り込み・push のあとに差し戻した。★`hit_type` は 8 値のままである。★★`opponent_stance` の `airborne`＝「空中」と重複していた**〔設計卓が隣の軸と照合していなかった＝計測点 `M-174`〕**。** 以下は起票時の記述： **★過去の壁やられ 2 種追加とは*別の*追加要求である**〔**★★照合の向きが狭かった**——**同じ軸の過去の追加とは照合したが、隣の軸とは照合していない**〕 |
| **(iii) 確定反撃** | `B02` | バーンアウト中の確定反撃にも対応 | 過去は「後日でもいい」。**★今回の意向を優先** |
| | `B06` | ターゲットコンボの確定反撃候補を確認・是正 | **★★例＝「不破三連撃(2発止め) その場 発生5F」。⇒ 確定反撃に使うのは*2 発止めのフレームではなく初段の発生*である。★データ修正による是正も検討する** |

### 1.3 ★★`B10` の削除対象（**開発者の逐語**）

```
素早く入力すると、別々のつもりの入力が 1 ステップにまとまることがあります。
ゆっくり正確に入力してください。
これは不具合ではなく、入力を取りこぼさないための意図的な設定です。
```

**★レポートの限定的な確認では、上記は `web/src/features/gamepad/components/GamepadInputNotice.tsx` に残存し、`B11` の 3 区画は `GamepadRecipeReadout.tsx` に残る。**

**★★★既存コメント・テストには旧仕様を維持する要求があるため、変更指示では「今回の要求による旧仕様の更新」を明確にすること**（レポート §4）**。⇒ 床を黙って外さない。**

---

## 2. ★★射程外

| # | 内容 | 理由 |
|---|---|---|
| 1 | **`Memo_Someday.txt` の 424〜466 行以外の項目** | **★今回の意向の対象ではない。⇒ 従来どおりの仕分けに従う** |
| 2 | **操作説明書の本文** | **`M32-02` の射程。★ただし §4.2 の追随は本 MS の完了条件に入る** |
| 3 | **モダン操作対応**（`ISSUE-007`） | **★フェーズ4 の射程外**（`D-634` (2)）**。⇒ 明示だけ**（`B6`＝`M32`） |

---

## 3. サブ分割案（**★★5 サブ。開発者の承認事項**）

| サブ | 射程 | 性質 | 前提 |
|---|---|---|---|
| **`M37-RESEARCH-01`** | **`B05` / `B06` / `B11` の実査 ＋ `A` の配線状況の実査** | **★read-only。⇒ 事実列挙のみ。★設計卓が読めない面を数える** | **無し。★★最初に走る。★指示書 v1.0.0 発行済み**（2026-09-13・`D-855`） |
| **`M37-01`** | **`A`**（運び量・始動位置の 3 方式入力 UI） | **★★確定済み設計が在る**（`D-730` / `D-731`）**。⇒ 方式の選定は不要。★実測で射程は*配線だけ*と判明した** | **★三点セット発行済み**（2026-09-13・`D-859`。指示書 v1.0.0 ／ チェックリスト v1.0.0）**。⇒ 投入可** |
| **`M37-02`** | **(i) 仮想コントローラ UI ＝ `B04` `B05` `B07`〜`B14`** | **★★★着地した**（2026-09-13・`D-867`）**。⇒ `B04` だけが「一部完了」であり、他 9 件は完了。★★縦に最も効いたのは*寸法*ではなく、一覧への高さ上限と見出しの技名補間の撤去であった** | **★三点セット発行済み**（2026-09-13・`D-862`。指示書 v1.0.0 ／ チェックリスト v1.0.0 ／ **マイグレ消費なしの見込み**）**。⇒ `M37-01` の着地後に投入する**〔**★同じ `web/src/features/combo/` 面。★`M37-04` とは並列に置ける**〕 |
| **`M37-03`** | **(ii) 一覧・マイコンボ・ゴミ箱 ＝ `B01` `B03` `B15`** | **★★★着地は `B01` の 1 件だけである**〔`B03` 除外 ／ `B15` 差戻〕**。⇒ 束ねた 3 件の結末が別々になった好例である** | **★三点セット発行済み**（2026-09-13・`D-859`。指示書 v1.0.0 ／ チェックリスト v1.0.0 ／ マイグレ `000113` 払い出し済み）**。⇒ 投入可。★★ただし `M33-02` より前に着地させること**〔`B15` がマイグレを消費しうるため〕 |
| **`M37-06`** | **`B04` / `B05` の未了分**（modifier の項目確定と表現形式） | **★★問いは 2 つ。⇒ 要否は開発者が全件決着済み。★flags は 10 → 14 値** | **★★★着地した**（2026-09-14・`D-876`。`CHANGE-201` / `202` / `203` 反映済み ／ **マイグレ消費 0**）**。⇒ ★`B04` は解消した。★★`recipe_cache` は構造を持たなかったが、文字列の中の `{表示語}` を描画時に解釈する形で成立した**〔`DES-002` §4.11 には触れていない〕 |
| **`M37-07`** | **始動技の持続当て**（重複判定キーへ加える） | **★★★重複判定キーが 7 → 8 要素になった。⇒ `hit_type` と同じ立ち位置。★`combo_oki_options` の打撃重ねとは向きが違う** | **★★★受理した**（2026-09-14・`D-881`）**。** **★消費＝マイグレ `000117`**〔層 A・列追加〕**／ CHANGE 3 本**〔`207` / `208` / `209`〕**。** **★★表示語は「持続当て（始動技）」。⇒ 設計卓案の「持続当て（始動）」から 1 文字増えた。★一覧のフィルタへも足した**〔開発者裁定＝`D-880`。**3 状態 `<select>`。列は増やしていない**〕**。** **★★★もう 1 つ重いものが出た＝`SUPP-001` §2.2 が約束していた「リスト定数を 1 箇所修正すれば切り替わる構造」が実装に 1 度も存在しなかった。⇒ 記述を実態へ書き換え、テストで固定する案を followup へ送った** |
| **`M37-05`** | **`P-60`**（始動位置のマス数の不変条件） | **★★`D-864` の決着を実装に落とすだけ。⇒ 穴は `PATCH` 経路 1 本であった** | **★★★着地した**（2026-09-13・`D-871`。`CHANGE-200` 反映済み ／ **マイグレ消費 0**）**。⇒ ★不変条件は*片方向*であることが判明した**〔逆向きは保証していない＝計測点 `M-177`〕 |
| **`M37-04`** | **(iii) 確定反撃 ＝ `B02` `B06`** | **★★`B06` はデータ是正では*ない*と実測で判明した**〔`M37-RESEARCH-01`・`D-857`〕**。⇒ 誤っているのは判定であり、`startup` の値ではない** | **★★★受理した**（2026-09-14・`D-877`）**。⇒ `B06` は解消した。** **★★`B02` は射程が大きいため製造が請求し、そのまま開発者へ回した。⇒ 本サブでは着手していない。** **★消費＝マイグレ 3 本**〔`000114`＝層 A・列追加 ／ `000115`＝層 B・空中限定 TC への是正 ／ `000116`＝層 B・`first_hit_startup` の backfill 106 行〕**／ CHANGE 3 本**〔`204` / `205` / `206`〕**。** **★★★指示書の「やらないこと」を 2 つ越えたが追認した**〔`D-877` §2-4。**開発者指示による。★`D-857`**〔手入力は開発者の手番〕**を同じ開発者が変更した**〕**。⇒ §0.2 の設計**〔初段を同定しない。値を直接持つ〕**は維持されている。★同定ロジックは実装に 1 行も入っていない。** **★挙動＝17 行が恒久的に落ち、88 行が緩い有利フレームで出る。canary の新規出現 `code` は 0 件** |

### 3.0 ★★`M37-05` は後から足したサブである（**2026-09-13・`D-865`**）

**★★起票時の 5 本には無い。⇒ `M37-01` の実装中に判明した `P-60` から出た**〔`PATCH` と `PUT` で「マス数を空にする」の結果が違う〕**。**

**★★★開発者が (ii)「代表値で埋める」を選び**（`D-864`）**、サブの新設も承認した。⇒ 決着した形は 1 行である＝`start_position_mass IS NULL` ⇔ `position = 不問`。**

**★射程は小さい。⇒ `POST` / `PUT` では既に成り立っており、穴は `PATCH` 経路だけである。★運び量は対象外**〔区分を持たないため代表値という概念が無い〕**。**

### 3.0b ★★`M37-06` は 2 本目の後付けサブである（**2026-09-13・`D-873`**）

**★`B04`**〔modifier を整ったデザインで表示〕**と `B05`**〔modifier の整理〕**の*未了分*である。⇒ `M37-02` が語彙を揃えたところまでで止まっていた。**

**★★問いは 2 つ＝(i) 項目の要否と文言**〔**開発者が全件決着**＝`D-873`〕**／ (ii) 表現形式**〔バッジ vs 波括弧付き平文〕**。**

**★★★flags は 10 → 14 値になる**〔削除 3＝`just` / `neutral_jump` / `forward_jump` ／ 追加 7＝`no_cancel` / `late_cancel` / `whiff` / `cross_under` / `meaty` / `juggle_high` / `juggle_low`〕**。⇒ 材料は `docs/handover/M37-DESIGN-01-...-coverage.md` v1.2.0。**

**★★撮影より前に実施する**（開発者裁定）**。⇒ 画面が変わるため。**

### 3.1 ★★順序と並列

```
M37-RESEARCH-01
   ├→ M37-01（A）
   ├→ M37-02（仮想コントローラ・最大）
   └→ M37-04（確定反撃）
M37-03（並列可・前提なし）
M37-05（P-60。M37-02 と M37-03 の着地後。★M37-04 とは並列可）
M37-06（B04 / B05 の未了分。★M37-04 と並列可 → M32-02 の撮影より前）
M37-07（始動技の持続当て。M37-04 の着地後。★M37-06 とは並列可）
```

**★★★【2026-09-14 現在地＝`D-881`】8 サブとも受理済みである。⇒ `M37` は `B02`**〔フェーズ5 送り＝`D-878`〕**を残して閉じた。★残る手番は開発者の main マージだけである。**

**★★受理は着地ではない。⇒ 設計卓が出せるのは受理までであり、main への反映は開発者の手番である。**

**★★`M37-01` と `M37-02` はどちらも `web/src/features/combo/` 系に触る見込みである。⇒ 並列にしない。★面の実測は `M37-RESEARCH-01` が出す。**

**★★★【2026-09-13 是正＝`D-866`】`B15` はマイグレを消費しなかった。⇒ `combos.hit_type` に `CHECK` 制約が無いためであり、しかも `B15` 自体が差し戻された。★払い出した `000113` は欠番とする。** **★`B06`**（`M37-04`）**は `000114` を消費する見込みのまま。★`M33-02` / `M33-03` は本 MS の全着地後である**（`D-853`）**。** 以下は起票時の記述： **★★★`B15` と `B06` はマイグレを消費する見込みである。⇒ `M33-01` の凍結点が動く。**

---

## 4. ★★危険

### 4.1 ★★★「見た目を変えた」で閉じること

**★`B05` / `B11` / `B14` は、いずれも*目的*が別に書かれている。**

| ID | 表面の要求 | ★実際の目的 |
|---|---|---|
| `B05` | modifier の整理 | **★必要なもの・不足しているものを探す。⇒ 実査が本体** |
| `B11` | 情報量を整理 | **★必要な情報を*見定めて*減らす。⇒ 減らす前に要否を判定する** |
| `B14` | ボタンを小さくする | **★★縦スクロールせずに全レシピを入力したい。⇒ 寸法だけ変えて目的を満たさない形がありうる** |

**⇒ 完了条件は*目的*で書くこと。★「小さくした」ではなく「縦スクロールせずに入力できた」である。**

### 4.2 ★★★操作説明書が陳腐化する

**★★`M32-02`（操作説明書の本文）は本 MS より先に走っている。⇒ 本 MS が着地すると 6 章が古くなる。**

| `DES-005` の節 | 効くもの |
|---|---|
| §5.4 コンボ一覧 | `B04` `B05` `B15` |
| §5.5 マイコンボ | `B03` |
| §5.6 コンボ詳細 | `B04` `B05` |
| **§5.7 コンボ登録・編集** | **★★`A` ＋ `B04` `B05` `B07`〜`B15`。⇒ 集中する最大の章** |
| §5.15 ゴミ箱 | `B01` |
| §5.20 確定反撃サーチ | `B02` `B06` |

**⇒ 各サブの完了条件へ「`docs/usermanual/` の該当章を追随させる」を入れること。★`followup` の `usermanual-body-goes-stale-if-ui-changes-after`。**

**★★スクリーンショットの撮影は本 MS の全着地後である**（2026-09-13 開発者が停止済み）**。**

### 4.3 ★★旧仕様を維持する床を、黙って外さないこと

**★`B10` の 3 文には、既存コメント・テストが旧仕様を維持する要求を持っている**（レポート §4）**。⇒ 削除するときは「今回の要求で旧仕様が更新された」ことを指示書と報告で明示すること。**

**★★★床を外すこと自体は正しい。⇒ 黙って外すのが誤りである。**

### 4.4 ★`B06` は「正しさ」の問題である

**★レポート §4 の注記＝「`B06` のゲーム上の正しい数値、全ターゲットコンボのデータ、各画面の現行挙動を全件検証したわけではない。これは対応先があるかの追跡調査である」。**

**⇒ `M37-RESEARCH-01` の結果に応じて是正方式を決める。★実装の分岐なのかデータの誤りなのかを先に切り分けること。**

---

## 5. CHANGE 見込み

| # | 対象 | 見込み |
|---|---|---|
| 1 | **`DES-005` §5.7** | **★1〜2 本**（`A` の 3 方式 UI ／ 仮想コントローラの配置） |
| 2 | **`DES-005` §5.4 / §5.5 / §5.15 / §5.20** | **★条件付き** |
| 3 | **`REQ-001` / `DES-003`** | **★`B15`（ヒット種別の追加）で 1 本の見込み** |
| 4 | **`DES-006`** | **★条件付き**（`B15` のバリデーション） |

**★マイグレ消費＝`B15` と `B06` で 1〜2 本の見込み。⇒ 自採番しない**（`D-293`）**。**

---

## 6. 完了の観点

| # | 観点 |
|---|---|
| **1** | **★★`A` ＋ `B01`〜`B15` の 16 件すべてに、完了 / 一部完了 / 未着手の判定が付いている**（レポート §6-2） |
| **2** | **★★★「既に解消」とする項目には、*今回の要求を満たす具体的な証拠*が付いている。⇒ 類似機能の過去の完了を根拠にしない** |
| **3** | **★残る項目の送り先の指示書に、要件と完了条件が記載されたことまで確認されている**（レポート §6-3。**★「次のサブ」「画面側」「後日」のみで閉じない**） |
| **4** | **★`combo-carry-distance-record` の状態欄が実態に合っている** |
| **5** | **★★`docs/usermanual/` の該当 6 章が追随している**（§4.2） |
| **6** | **★フェーズ4 内に扱えない項目が出たら、対象と理由を開発者へ返している**（レポート §6-7。**★過去の一般的な延期方針のみでフェーズ5 へ戻さない**） |

---

## 7. ★開発者への確認事項

| # | 事項 | 設計卓の暫定案 |
|---|---|---|
| ~~**1**~~ | **サブ分割 5 本の承認**（§3） | **★★【2026-09-13 決着＝承認】**（`D-855`・開発者の逐語＝「1.承認」）**。⇒ RESEARCH 1 ＋ 実装 4 で確定。★`M37-RESEARCH-01` 指示書 v1.0.0 を発行済み。** 以下は起票時の案： **★★案＝RESEARCH 1 ＋ 実装 4。⇒ `B05` / `B06` / `B11` は実査しないと射程が決まらない** |
| ~~**2**~~ | **`B14` の受入条件をどう測るか** | **★★【2026-09-13 決着＝開発者がチェックする】**（`D-855`・開発者の逐語＝「2.私がチェックする」）**。⇒ 「縦スクロールせずに全レシピを入力できたか」を開発者の実機で確認する。★寸法の数値では判定しない。** **★★製造への含意＝`B14` の完了条件は「開発者の実機確認が返ること」である。⇒ 自分で「小さくした」と申告して閉じない。** 以下は起票時の案： **★★案＝「開発者の実機で、縦スクロールせずに全レシピを入力できたか」を 1 回確認する。⇒ 寸法の数値では判定しない。★人の手番である** |
| **3** | **`B06` の是正方式** | **★★★【2026-09-13 開発者から仮説が示された】**（`D-855`・逐語＝「ターゲットコンボの 2 段目以降の発生 F に、1 段目の物ではなく、2 段目の発生 F を入れる方式にしていたため候補にでているものと思われる」）**。⇒ `M37-RESEARCH-01` §2.3 でこの仮説の真偽を実測させる。★仮説は事実として扱わない。** **★★★件数が射程を決める＝同じ形の行が 1 件なら個別是正、100 件なら方式の是正である。⇒ 数えるまで方式を決めない。** 以下は起票時の案： **★★`M37-RESEARCH-01` の結果を見てから決める。⇒ 実装の分岐かデータの誤りかで変わる** |

---

## 8. 参照ドキュメント

| ID | パス | 参照する箇所 |
|----|------|------------|
| — | `tmp/20260912-phase4-user-requests-design-handover.md` | **★一次の手交資料**（**恒久正本ではない**）**。§3**（`A`）**／ §4**（`B01`〜`B15`）**／ §5**（受理経緯）**／ §6**（依頼事項） |
| — | `docs/human-notes/Memo_Someday.txt` | **424〜466 行付近**（`B01`〜`B15` の一次源） |
| DES-005 | `docs/design/05-screen-design.md` | **§5.7**（★最大の面）／ §5.4 / §5.5 / §5.6 / §5.15 / §5.20 ／ **§6**（仮想コントローラの論理構成） |
| DES-003 | `docs/design/03-data-model.md` | **`B15`（ヒット種別）／ `A`（`start_position_mass` / `carry_distance_mass`）** |
| — | `docs/process/parallel-board.md` | `D-730` / `D-731`（3 方式の確定）／ `D-758`（フェーズ5 割付・**★差し戻す**）／ `D-668` ／ `D-815` / `D-816` ／ `M-137` ／ **`D-853`**（受理）／ **`M-170`** |
| — | `docs/handover/followup-backlog.md` | **§DB**（本 MS の追跡）／ `combo-carry-distance-record` ／ `duplicate-precheck-ignores-position-mass` ／ `position-bands-duplicated-go-and-ts` |

---

*以上、M37 overview **v1.14.0**。* **★★本 MS の芯は §0.2 である**——**全件を新規実装するのではない。⇒ 満たされているものは証拠付きで閉じ、残りを割り付ける。★その「証拠」を省くと、`M-170` と同じ**（**受理したのに消化記録が無い**）**状態が再演する。**
