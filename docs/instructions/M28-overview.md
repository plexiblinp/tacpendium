# M28-overview: ★破壊的変更の窓（正式名リネーム ＋ `FR702` ゲームアップデート追従）

| 項目 | 内容 |
|------|------|
| 文書種別 | 補足資料（overview。CHANGE 通知書対象外・自由改訂） |
| バージョン | **1.12.0**（2026-09-06。**★★`M28-05` を新設し、`M28-04` のマイグレ消費を 1 → 0 へ是正した＝`D-759`。⇒ `M28` は 6 サブになった**） ／ 1.11.0（2026-09-06。**★★`M28-02c` を新設した＝`D-758`。⇒ `M28` は 5 サブになった。★理由＝`FR702` のバックエンドが未完であることが `M28-02b` の実査で分かった**） ／ 1.10.0（2026-09-06。**★★`M28-02b` を起票した＝`D-745`。⇒ `M28` の 4 サブがすべて起票済みになった**） ／ 1.9.0（2026-09-06。**★★`M28-02a` のマイグレ消費が 2 本で確定した＝`D-738`。⇒ `M28-04` は `000106` 以降**） ／ 1.8.0（2026-09-05。**★★`M28-04` を起票した＝`D-733`**） ／ 1.7.0（2026-09-05。**★★§3.6 の確定事項 2 を改訂し 4 を失効させた＝`D-731`。⇒ 始動位置と運び量は別の列で、正本はマス数 1 本**） ／ 1.6.0（2026-09-05。**★★4 サブが承認された＝`D-727`。あわせて `§3.2.7` の「初回の扱い」が決着し、`§3.3` の順序の懸念も消えた。⇒ `M28-02a` が起票可になった**） ／ 1.5.0（2026-09-05。**★★サブが 2 → 4 本になった＝`D-726`**） ／ 1.4.0（2026-09-02。**★★保留の射程が精緻化され、窓が 3 点になった＝`D-668`**） ／ 1.3.0（2026-09-02。**★★`M28-02`（`FR702`）を保留にした＝`D-666`**。**期限＝初回全体公開まで**） ／ 1.2.0（2026-09-02。**★★開発者承認済み**。確認事項 ①②④⑤ の決着を反映し、③ の判断材料を全面的に立て直した＝`D-664`） ／ 1.0.0（2026-09-02・起票） |
| 作成日 | 2026-09-02 |
| 作成者 | 設計担当 Claude（設計卓／フェーズ4 期） |
| 対象マイルストーン | **`M28`**（フェーズ4）。上位＝`docs/instructions/phase4-overview.md` v1.1.0 |
| 位置づけ | **`M28` のサブ構成・全件処遇・CHANGE 見込みの正本** |
| 前提 | `phase4-overview` §M28 ／ `docs/human-notes/combmgr-prerelease-checklist.md` §4（破壊的変更の窓）・§5.1（`FR702` の確定済み方向性）／ `REQ-001` `FR702` / `FR701` / `FR703` / `FR704` ／ ボード `D-462` / `D-470` / `D-474` / `D-636`（正式名）／ `D-648`（ベースライン化は不実施）／ `D-655`（`M14` の実測） |
| 配置 | `docs/instructions/M28-overview.md`（active＝直下。フェーズ4 終了時に `docs/instructions/phase4/` へアーカイブ） |

---

## 0. 本書の確度

- **★★本書は 2026-09-02 に開発者承認された**（確認事項 ⑤＝「承認」）。**⇒ `M28-01` の指示書を起票済みである。**
- **★★【2026-09-02 保留＝`D-666`】`M28-02`（`FR702`）は保留である。** **開発者の逐語＝「バージョンアップ対応については、いったん保留は可能ですか？……並列で色々と進めているいま決めるには負担が大きい。」** **⇒ `M28-01`（正式名リネーム）だけを通す。★`REQ-001` §7 は動かさない**（`FR702` はフェーズ4 のまま）。**★保留の期限は「初回全体公開まで」である**（§3.4）。 **★あわせて、設計卓が並べた判断材料に軸が 2 本欠けていた**（§3.2.8）。
- **★★③（基準バージョンの持ち場）だけが未決である。** **開発者の逐語＝「これはじっくり検討したいのでもう一度詳しく判断材料を並べてください。使い勝手やコストなどを考えつつ決めます。」⇒ §3.2 を全面的に立て直した**（**★設計卓の前回の推奨理由の誤りを §3.2.0 で訂正した**）。
- **★設計卓は実装ソースを読んでいない。** 実装の事実は `code-facts.md` ／ `M25-RESEARCH-01` ／ `M14-RESEARCH-03` ／ 完了報告からのみ引いている。**断定していない箇所は §6 の実査項目にした。**
- **★★`FR702` の設計判断は 1 点だけ残っている**（§3.2 の「基準バージョンの持ち場」）。**それ以外は `checklist` §5.1 で確定済みであり、着手時にゼロから議論しない。**

---

## 1. 目的

**`checklist` §4 の「★破壊的変更の窓（1 回にまとめる）」を実体化する。**

**★なぜ窓にまとめるのか**——`checklist` `A5` の逐語＝**「先送りするほど破壊面が単調増加する」**。M22 で認証・`users`・資格情報が乗り、M23 でゴミ箱／復元が乗った。**⇒ 待つほど触る面が増える。**

**★★【2026-09-02 更新＝`D-668`】窓は 3 点である。** **正式名リネーム ／ `FR702` のスキーマ ／ ★運び量の記録のスキーマ**（`combo-carry-distance-record`）。**★マイグレのベースライン化が `D-648` で落ちた一方、運び量が新しく入った。** 以下は前回更新時の記述： **★★窓の中身は 3 点から 2 点になった。** `checklist` §4 は **正式名リネーム ／ `FR702` のスキーマ ／ マイグレーションのベースライン化** の 3 点を挙げていたが、**ベースライン化は `D-648` で「実施しない」と確定した**（実測 1.27%。`checklist` 自身の「無視できるならやらない」に当たる）。**⇒ 窓は 2 点で構成する。**

---

## 2. 対象

| ledger ID | 根拠 | 内容 |
|---|---|---|
| `A5` | A5 | 正式名の決定と反映 |
| `SM-053` / `SM-157` | A5 | 正式名称の決定と反映 |
| `CL-4-1` | A5 | 正式名リネーム |
| `CL-4-3` | B3 | `FR702` が要求するスキーマ変更 |
| **（母集団外）** | — | **★`FR702` 本体の実装** |

**★★`FR702` の実装本体は ledger に 1 行も無い。** `M25-RESEARCH-01` の母集団は 6 源泉であり、**`REQ-001` §7 が現に持っていたフェーズ4 の 3 項目そのものは入っていない**（`D-652`）。**⇒ 行数で射程を測ると `FR702` が丸ごと落ちる。本 MS はそれを拾うために立てた。**

---

## 3. サブ構成（**★★4 本**。2026-09-05 に 2 → 4 へ＝`D-726`）

> **★★【2026-09-05 追加＝`D-726`】`M28-03` と `M28-04` を足した。**
>
> **★2 本とも「破壊的変更の窓」の族である。**
>
> | サブ | 何を足したか | なぜ `M28` か |
> |---|---|---|
> | **`M28-03`** | **ツールチェーンと依存の版上げ** | **★開発者の提案**（2026-09-05）**——`M28-01` で devContainer に手が入るのを機に Go のセキュリティ版上げをしたい。★★ただし `M28-01`（母数 2,852 件のリネーム）と同じコミットに混ぜない**〔**赤が出たときに切り分けられなくなる**〕**。⇒ 別サブにした** |
> | **`M28-04`** | **`move_code` / `command` の是正**（board `P-17`） | **★`move_code` のリネームは配布 DB の識別子が変わる＝まさに破壊的変更である。★「ついで」の窓は無くなった**〔逐語＝「**もう `character_data` は挿入し終えたので、何かのついでに、という形はとれない**」〕**。⇒ 単独の運び手が要る** |
>
> **★★`M28-03` へ `M28-04` を相乗りさせない。** **面が違い、赤が出たときの切り分けができなくなる**〔`M28-01` と混ぜない理由と同じ〕**。**



### M28-01 — 正式名リネーム（`A5`）

**★名称は実質確定している。**

| # | 事実 | 出所 |
|---|---|---|
| 1 | **開発者決定＝`Tacpendium` または `Tactendium`**（語源＝tactics + compendium）。**理由＝「コンボとセットプレイ以外にも拡張予定なので combo から離れる」** | `D-462` |
| 2 | **2 綴りのうち `Tacpendium` を推す。** **判定を分けたのは SEO の 1 点**——**`Tactendium` は `Tactium`**〔実在の IT 企業・CRM 製品〕**と文字列・分野の双方が近く、タイプミス・検索補正・サジェストで誘導されうる** | `D-470` / `D-474` |
| 3 | **商標クリアランスは 5 点すべて完了**〔J-PlatPat / USPTO / EUIPO ／ ドメイン WHOIS ／ GitHub・npm・Homebrew・Microsoft Store・Mac App Store・Steam・Google Play ／ SEO ／ 発音・意味〕 | `D-636` |

**⇒ 残るのは反映だけである。**

| # | 反映先（**★すべて起票時に実査すること**） |
|---|---|
| 1 | **Go の module path**（`go.mod` の `module` 行と、それを参照する全 import） |
| 2 | **既定データディレクトリ**（**★★2026-09-02 決着＝`D-664`。★自動移行する**。詳細＝§3.1.1） |
| 3 | **定数・識別子**（`CLAUDE.md` §4＝マジックストリングは定数化されているはず。**★実査**） |
| 4 | **ドキュメント**（`README` ／ `docs/` ／ `CLAUDE.md` ／ `AGENTS.md`） |
| 5 | **ビルド成果物名・`Makefile`・`.github/` のワークフロー** |
| 6 | **`config.toml.example`** |

**★★一括置換をさせない。** followup `bulk-rename-must-exclude-check-scripts` の逐語——**「`sed -i 's/combomgr/<新名>/g' scripts/*.sh` を回すと `check-*.sh` まで巻き込む」。**

#### 3.1.1 ★★既定データディレクトリの移行（`D-664`・確定）

**開発者の逐語（2026-09-02）＝「実質的な利用者は私以外いまはいない。過去に提供した友人も、当時バージョンのクオリティからもう使っていないので、破壊は別だが単なる移行は止めずにやりたい。」**

**⇒ 自動移行する。★設計卓が推していた「移さず案内する」は採らない。**

**★★ただし「破壊は別」を守る。⇒ 移行の形を次に固定する。**

| # | 規則 |
|---|---|
| **★★1** | **旧ディレクトリを消さない。** **★コピーしてから旧を `<旧名>.migrated-<日付>` へリネームして残す。⇒ 消すのは開発者の手番**（`D-196` 境界条件 3） |
| **★★2** | **コピー後に検証してから旧を退避する。** **検証＝新しい場所の DB が開けること ＋ 主要テーブルの行数が旧と一致すること。★一致しなければ何も動かさずに止まり、利用者へ理由を出す** |
| **★3** | **WAL / SHM を取り残さない**（`*.db-wal` / `*.db-shm`）。**★DB を開いたまま copy しない** |
| **★4** | **`config.toml` も移す。★中の絶対パスが旧を指していたら書き換える** |
| **★5** | **移行したことを起動ログと画面に 1 度だけ出す。★黙って動かさない** |
| **★6** | **二重起動・移行途中での中断に耐えること。★中断したら旧が正本のまま残る形にする** |

**★★これは `CLAUDE.md` §10「データの不可逆な破壊の防止」に当たる作業である。⇒ `M28-01` は Plan Mode 必須であり、移行の手順を計画で提示させること。**

**★★あわせて「歴史記録は書き換えない」を守る**——**`docs/change-notes/` の完了済み通知書・反映レポート、`docs/handover/session-prompts/`、`docs/progress/` の完了報告は当時の名前のまま残す**（`E-111` 分類 (2)）。**⇒ 置換の対象範囲を先に決めることが本サブの最初の一手である。**

### M28-02 — `FR702` ゲームアップデート追従

**★★方向性は `checklist` §5.1 で確定済みである。着手時にゼロから議論しない。**

| # | 確定している設計 | 逐語の要点 |
|---|---|---|
| 1 | **検証はユーザー任せ。アプリは影響可能性のコンボだけを出す** | **破綻の自動断定はしない**（`FR307` 整合） |
| 2 | **旧案（アップデート時に全コンボへ一律タグ）は捨てた** | **move 由来の的絞りへ洗練済み** |
| 3 | **move 単位・明示マーカー・バージョン文字列** | **マーカーを立てるのは配信者（開発者）。★列に持たない性質変更**〔空中追撃回数の増加等〕**も「その move が変わった」で表現でき、列の有無に依存しない** |
| 4 | **フラグではなくバージョン文字列にする** | **★利用者が必ずしもアップデートに追従しないため**（飛ばし更新） |
| 5 | **影響コンボは保存せず導出する** | マーカーの立った move を含むコンボ＝影響可能性 |

#### 3.2 ★★残っている設計判断は 1 点だけ——基準バージョンの持ち場

`checklist` §5.1 の逐語——**「利用者側に『基準バージョン』を持たせて差分導出まで行う方向（有力案）。基準バージョンの持ち場（アプリ設定／コンボ単位の記録時バージョン）と差分導出の具体はフェーズ4 で確定」。**

##### 3.2.0 ★★設計卓の前回の推奨理由は誤っていた（訂正）

**本書 v1.0.0 は推奨理由の 2 つ目に「飛ばし更新に耐えるのは (b) だけである」と書いたが、これは誤りである。**

**★(a) でも飛ばし更新は扱える。** 判定は **「基準 < マーカー」の比較**であり、基準が 3 バージョン古くても比較は成立し、差分は正しく大きく出る。**⇒ 飛ばし更新は案の選択軸にならない。**

**★★正しい選択軸は「部分消化できるか」である**（§3.2.2 の軸 3）。**⇒ 以下で軸を立て直す。**

##### 3.2.1 前提（`checklist` §5.1 で確定済み・議論しない）

| # | 確定事項 |
|---|---|
| 1 | **マーカーは move に立てる**（コンボにではない）。**立てるのは配信者（開発者）** |
| 2 | **マーカーはバージョン文字列**（フラグではない） |
| 3 | **影響コンボは保存せず導出する** |
| 4 | **アプリは「影響可能性」を出すだけ。破綻の自動断定はしない**（`FR307` 整合） |

**★マーカーは move につき 1 本で足りる。** 飛ばし更新で 2 回変わっていても、**「最後に変わったバージョン」だけ持てば「基準 < 最終変更」で判定できる**。**⇒ この点は全案に共通であり、選択軸にならない。**

##### 3.2.2 ★★比較軸 8 本

| 軸 | 何を見るか |
|---|---|
| **1 スキーマコスト** | 増える列の本数・マイグレ本数 |
| **2 実装コスト** | 判定クエリと UI の量 |
| **★★3 部分消化できるか** | **影響コンボを 1 件ずつ「確認した」にできるか。★これが最大の軸である** |
| **4 初回に何件出るか** | 導入直後の見え方 |
| **5 誤判定の向き** | **安全側**（余分に出す）**か危険側**（出し漏らす）**か** |
| **6 「保存せず導出」との整合** | `checklist` の確定事項 3 |
| **7 利用者から見た分かりやすさ** | 何を操作すると何が起きるか |
| **8 将来の拡張** | キャラ追加・計算式変更・新システム追加に耐えるか |

##### 3.2.3 案 (a) アプリ設定に基準を 1 つ持つ

**持ち場**＝`config.toml` の `[app] baseline_version`、または設定テーブル 1 行。

| 軸 | 評価 |
|---|---|
| 1 スキーマ | **★最小。`moves` のマーカー列 1 本だけ。マイグレ 1 本** |
| 2 実装 | **最小。判定は `WHERE moves.changed_in > :baseline` の 1 本。UI は「基準バージョンの表示」＋「確認済みにする」ボタン 1 つ** |
| **★★3 部分消化** | **★★できない。** **基準を進めると、まだ見ていないコンボも一斉に「確認済み」になる。⇒ 1 回のアップデートで出た影響コンボを全部見終わるまで基準を進められない** |
| 4 初回 | **0 件から始められる**（導入時の基準を「現在の最新」にすればよい） |
| 5 誤判定 | **★進めた瞬間に危険側へ倒れる**（見ていないコンボが確認済みになる） |
| 6 導出 | **整合する**（保存するのは基準 1 件だけ） |
| 7 分かりやすさ | **★高い。「今の基準はこれ」「確認したので進める」の 2 語で説明できる** |
| 8 拡張 | **キャラ追加には効かない**（新キャラの move にマーカーは立たない。もっとも新キャラは既存コンボに影響しない） |

##### 3.2.4 案 (b) コンボ単位で記録時バージョンを持つ

**持ち場**＝`combos` に `baseline_version TEXT NULL`。登録・更新時に当時の最新を書く。

| 軸 | 評価 |
|---|---|
| 1 スキーマ | **列 2 本**（`moves` のマーカー ＋ `combos` の基準）。**マイグレ 1〜2 本** |
| 2 実装 | **中。判定は `moves.changed_in > combos.baseline_version` の結合。UI は一覧の絞り込み ＋ コンボ詳細の「確認した」ボタン** |
| **★★3 部分消化** | **★★できる。1 件ずつ潰せる。放置しても増えるだけで壊れない** |
| 4 初回 | **★★既存コンボは NULL＝不明。⇒ 初回に全件出る。★緩和策は「列を足した時点の最新を一括で入れる」だが、これは「無事の自動断定」になる** |
| 5 誤判定 | **★安全側。** NULL を「影響可能性あり」に倒せば、出し漏らしが起きない |
| 6 導出 | **整合する**（保存するのはコンボ 1 件につき 1 文字列） |
| 7 分かりやすさ | **中。「このコンボはどの時点の前提か」を利用者が意識する必要がある** |
| 8 拡張 | **★(a) と同じ** |

**★★`CSV` / エクスポートへの波及がある**——**`combos` に列が増えると、CSV の列契約（`DES-006` / `csv-column-contract-unobserved-in-pr`）に触れる。⇒ 出力するかしないかを決める必要がある。★設計卓の見立ては「出力しない」**〔`materialized_from_combo_id` / `superseded_by_combo_id` と同じ扱い。**DB 管理列であって利用者の入力ではない**〕。

##### 3.2.5 案 (c) (a) ＋ (b) の両方

| 軸 | 評価 |
|---|---|
| 1 スキーマ | **列 2 本 ＋ 設定 1 件** |
| 2 実装 | **★大。どちらを優先するかの規則が要り、規則が増えると説明が要る** |
| 3 部分消化 | できる |
| 7 分かりやすさ | **★★低い。「全体の基準」と「コンボの基準」が食い違ったときに何が起きるかを利用者が予測できない** |

**⇒ 採らない。★二重管理はドリフト源である**（`D-637` で同じ理由により一方向生成を選んだのと同じ判断）。

##### 3.2.6 ★★案 (e) 列を足さず `combos.updated_at` を基準に使う（**新規・本書 v1.2.0 で追加**）

**マーカーを「バージョン文字列」ではなく「変更日時」で持ち、`combos.updated_at < moves.changed_at` で判定する。**

| 軸 | 評価 |
|---|---|
| 1 スキーマ | **★★列 1 本だけ**（`moves` のマーカー）。**`combos` は既存列を使う。⇒ 最小** |
| 2 実装 | **小。判定は結合 1 本** |
| 3 部分消化 | **★★できる（ように見える）が、実際は破綻する。下記** |
| 4 初回 | 0 件から始められる |
| **★★5 誤判定** | **★★危険側。★「コンボのメモを 1 文字直しただけ」で `updated_at` が進み、確認していないのに確認済みになる。★これは静かに起きる** |
| 6 導出 | 整合する |
| 7 分かりやすさ | **★低い。「編集したら確認済みになる」を利用者が予測できない** |
| 8 拡張 | 日時なので飛ばし更新も自然に扱える |

**⇒ 採らない。★★理由は軸 5 の 1 点である**——**「編集したら確認済みになる」は、`checklist` の「破綻の自動断定はしない」と正反対の「無事の自動断定」であり、しかも利用者に見えない形で起きる。**

**★ただし列 0 本という利点は本物である。⇒ 記録として残す。** **★もし将来「確認した」という明示操作を持たない方針に変わるなら、(e) が最も安い。**

##### 3.2.7 ★★設計卓の推奨＝(b)。ただし初回の扱いを 1 つ決める必要がある

**推奨は (b) コンボ単位。決め手は軸 3（部分消化）である。**

| # | 理由 |
|---|---|
| **1** | **★★部分消化できるのは (b) と (c) だけであり、(c) は二重管理で落ちる。⇒ 実質 (b) 一択である** |
| **2** | **`checklist` が旧案（全コンボへ一律タグ）を捨てた理由は「的絞り」である。(a) は「全コンボが同じ基準」という点で旧案の性質を残している** |
| **3** | **軸 5（誤判定の向き）で唯一 安全側に倒せる。** NULL を「影響可能性あり」に扱えば出し漏らしが起きない |

**★★【2026-09-05 決着＝`D-725`】残っていた 1 点＝既存コンボの初期値は (b-2) に決まった。**

**開発者の逐語＝「マイグレについては正式に出してからちゃんと記録でいい。本アプリは完成までに一度大型のアップデートを挟んでいますが、ユーザー目線では、現状バージョンが第一弾として提供されるので、気にしないため。」**

**⇒ 「初回公開より前」は存在しないものとして扱う。★過去のマイグレへ遡ってゲームバージョンを付ける作業は要らない。★既存コンボには列を足した時点の最新を入れる。⇒ 初回に出る件数は 0 件。**

**★★これで `M28-02a` の設計から一番重い部分が落ちた**——**(b-1) を採ると初回に全件が出るため、「全件出たものをどう捌かせるか」という画面側の設計が必要になっていた。**

以下は決着前の記述：**★★決める必要が残るのは 1 点だけ——既存コンボの初期値である。**

| 案 | 初期値 | 初回に出る件数 | 性質 |
|---|---|---|---|
| **(b-1)** | **NULL（不明）** | **★全件** | **★安全側。「分からないものは出す」。`FR307` と同じ向き** |
| **(b-2)** | **列を足した時点の最新バージョン** | **0 件** | **★「無事の自動断定」。★ただし実害は限定的である**〔**現時点の実質的な利用者は開発者 1 人であり、過去に提供した友人は当時バージョンの品質からもう使っていない**——開発者の逐語・2026-09-02〕 |

**★設計卓の推奨は (b-2) である。** **理由＝現時点の母数が 1 人であり、「全件が影響可能性あり」で始まると初回の一覧が使い物にならない一方、失うものが実質ゼロだからである。★ただし公開後に増える利用者には (b-1) の性質が要る。⇒ 列を足すマイグレの中で一括更新し、以後に登録されるコンボは登録時の最新を書く。**

**★★これは「今回だけの一括更新」であり、`M14-RESEARCH-03` §4-4 の一般化**〔1 回きりの backfill は後から INSERT される行を拾わない〕**にそのまま当たる。⇒ 登録経路が基準を書くことを、同じ手番で必ず実装すること。片方だけだと静かに壊れる。**

##### 3.2.8 ★★設計卓が並べた軸には 2 本欠けていた（`D-666`）

**開発者が保留の理由として挙げたのは、設計卓が §3.2.2 の 8 軸に入れていなかった 2 つである。**

| # | 欠けていた軸 | なぜ効くか |
|---|---|---|
| **★★9** | **バージョンアップ時に利用者はどう更新するのか** | **★★`FR702` の前段である。** **本アプリは配布 DB を同梱した単一バイナリである。⇒ 「新しいゲームバージョンのデータ」が利用者に届く経路そのものが未設計であり、そこが決まらないと「基準バージョン」が何を指すかも決まらない**〔バイナリごと差し替えるのか ／ DB だけ差し替えるのか ／ 差分を取り込むのか〕 |
| **★★10** | **開発者（配信者）のアップデート対応のしやすさ** | **★マーカーを立てるのは配信者である**（`checklist` §5.1）。**⇒ SF6 のアップデートのたびに、開発者が「どの move が変わったか」を判定してマーカーを立てる作業が発生する。★その手間が案によって変わらないかを見ていなかった** |

**★★軸 9 が最も重い。** **`checklist` §5.1 は「基準バージョンの持ち場」を論点として立てているが、その前提である「データがどう届くか」には触れていない。⇒ 設計卓もそれを引き継いで、届き方を所与として扱っていた。**

**★★これは playbook §4.48 の族である**〔任意の 2 節が両立しないことがある〕**の隣接形——**両立しないのではなく、**前提が書かれていない**。**⇒ 一般形＝「持ち場を決めよ」と書かれていたら、その前に「何が届くのか」を問う。**

#### 3.4 ★★`M28-02` は保留である（`D-666`）

| # | 決めたこと |
|---|---|
| **1** | **`M28-02`（`FR702`）に着手しない。** **★`M28-01`（正式名リネーム）は予定どおり通す**（依存しない） |
| **2** | **★`REQ-001` §7 は動かさない。** `FR702` はフェーズ4 のままである。**⇒ `CHANGE` を消費しない。★フェーズ定義を 2 度動かすほうが高くつく** |
| **3** | **★★保留の期限は「初回全体公開まで」である。** **理由＝`checklist` §3.1 の逐語**〔**「利用者がいる状態での破壊的変更は極力減らしたい。追従の仕組みが要求するスキーマは、公開前に確定させておくのが安い」**〕。**⇒ 公開後にスキーマを足すと、利用者がいる状態での破壊的変更になる** |
| **4** | **★窓が 2 回開くことは、公開前なら許容できる。** **理由＝現時点の実質的な利用者は開発者 1 人である**（`D-664`）。**⇒ `checklist` §4 の「1 回にまとめる」は、利用者がいる状態を前提にした規約である** |
| **5** | **★保留は `P-50` としてボードに載せる。** **⇒ 忘れない仕組みを持たせる。★「あとで」で終わらせない** |

**★★次に議論するときの入力は 10 軸である**（§3.2.2 の 8 本 ＋ §3.2.8 の 2 本）。**★軸 9（データがどう届くか）を先に決めると、他の軸の評価が変わる可能性がある。⇒ 軸 9 から議論すること。**

#### 3.5 ★★保留の射程が精緻化された（`D-668`）

**開発者の補足（2026-09-02）の逐語＝「次のアップデートはまだ先（＝実際にユーザーが即日必要とする機能ではない）ので、Phase4 での必達はバックエンド、DB 構造などの決定。画面などのユーザーが触れる部分はイメージ（設計）まで出来れば実装は後でも良い。」**

**⇒ 保留の意味が「先送り」から「段を分ける」へ変わった。**

| 段 | 中身 | フェーズ4 での扱い |
|---|---|---|
| **`M28-02a`** | **★★スキーマとバックエンドの決定**〔基準バージョンの持ち場 ／ `moves` のマーカー列 ／ 判定の導出ロジック〕 | **★★必達。** **理由＝`checklist` §3.1 の逐語「追従の仕組みが要求するスキーマは、公開前に確定させておくのが安い」。★公開後にスキーマを足すと利用者がいる状態での破壊的変更になる** |
| **`M28-02b`** | **画面（影響コンボの一覧・「確認した」操作・基準バージョンの表示）** | **★設計（イメージ）まで。実装は後でよい。★★2026-09-06 起票済み＝`D-745`**〔`docs/instructions/M28-02b-game-update-impact-screen.md` v1.0.0。**4 つの設計判断は `D-742` で決着済み**〕 |
| **★★`M28-02c`** | **`FR702` のバックエンド追補 ＋ 画面の実装**（**2026-09-06 新設＝`D-758`**） | **★★必達である**——**`D-668` の「フェーズ4 での必達はバックエンド」に当たる。⇒ `affectedMoves` ／ 告知の経路 2 本 ／ 判定式を共有した `COUNT` ／ `games.current_data_version` の露出 はすべてバックエンドである。** **★★本サブが在る理由＝設計卓の見立てが誤っていた**（計測点 `M-136`）**。⇒ `M28-02b` の指示書は「専用画面は既存の 1 本で作れる」と書いたが、絞り込みについては正しく、目的の達成については成立しない。** **★指示書＝`docs/instructions/M28-02c-game-update-backend-remainder.md` v1.0.0。★設計の正本は `CHANGE-162`。** |
| **★★`M28-05`** | **terry `quick_burn_light` の `move_code` 是正**（**2026-09-06 新設＝`D-759`**） | **★★`M28` の窓のうちに片付ける必要がある**——**`move_code` のリネームは配布 DB の識別子が変わる**（`D-725` の族）**。⇒ 公開後には動かせない。** **★開発者のインゲーム確認で「強度は存在しない」＝誤りが確定した。★`sonic_break_light` → `sonic_break` と同型である。** **★★前例より影響範囲が広い**——**`quick_burn_light` は `is_derived=0` かつ `command` 非空のため索引に載っており、動く golden は 4 本である。** **★指示書＝`docs/instructions/M28-05-terry-quick-burn-code-fix.md` v1.0.0。★マイグレは `M28-04` が返した `000106`。** |

**★★これで `P-50` の性格が変わる**——**「決めない」ではなく「フェーズ4 の中で決める。ただし今この手番では決めない」である。⇒ 期限は初回全体公開のままだが、決める対象は `M28-02a` に限られる。**

#### 3.6 ★★運び量の記録が窓に入った（`D-668`・新規）

**開発者の逐語（2026-09-02）＝「コンボ始動時の位置状況に加え、運び量も記録に追加したい。またここで DB 変更が発生する可能性がある。いまのイメージだとパーセンテージでユーザー目測値も入れれるようにしたい。重複キーとしては、既存の位置状況を使い、閾値で自動で割り当てるイメージを持っている。」**

**★★スキーマ変更を伴うため `M28` の窓に載せる**（`checklist` §4＝破壊的変更を 1 回にまとめる）。**★画面は `FR702` と同じく後でよい。**

| # | 確定していること |
|---|---|
| 1 | **運び量を記録に追加する** |
| 2 | ~~パーセンテージで持つ。利用者の目測値も入れられる~~ **★★【2026-09-05 改訂＝`D-731`】保存する正本は「マス数」（0〜160 の整数）1 本である。★パーセントと区分は表示・入力のための変換であり保存しない**〔**理由＝`1% ＝ 1.6 マス` であり、マス → % → マス の往復で値が動く**〕**。★利用者の目測値を入れられる点は変わらない** |
| 3 | **`DuplicateKey`（重複キー）には既存の位置状況を使う。★運び量そのものは重複キーに入れない** |
| ~~4~~ | ~~位置状況は、運び量の値と閾値の比較で機械的に決まる~~ **★★【2026-09-05 失効＝`D-731`】始動位置と運び量は別の列である。★開発者の逐語＝「そもそも運び量と今の話は別。コンボを始める前の立ち位置です。なお必ずしも画面端にぴったり送るコンボとは限らないので、運び量は別の列として欲しいです。」⇒ 始動位置は 3 つの入力方式で直接入力する。運び量から導出しない** |

**★★設計上の含みが 1 つある。⇒ 起票前に決める必要がある。**

**項目 4 は「位置状況が入力ではなく導出値になる」ことを意味する。** **⇒ 既存の位置状況の入力欄をどうするかが決まっていない。**

| 案 | 位置状況の入力欄 | 効き方 | 難点 |
|---|---|---|---|
| **(i)** | **残す**（運び量とは独立に入力できる） | 既存データと既存操作がそのまま生きる | **★2 つの値が食い違いうる。どちらを `DuplicateKey` に使うかで結果が変わる** |
| **(ii)** | **読み取り専用にする**（運び量から導出して表示） | **★食い違いが起きない** | **★運び量を入れないと位置状況が決まらない。⇒ 既存データの移行が要る** |
| **(iii)** | **消す** | 最も単純 | **★既存の位置状況の値を捨てることになる。★`DuplicateKey` の定義が変わり、既存コンボの重複判定が動く** |

**★設計卓の見立ては (ii) だが、(ii) は「運び量が未入力の既存コンボ」の扱いを決める必要がある**〔**位置状況を残して運び量を NULL にするのか ／ 運び量を逆算するのか**〕。**⇒ `M28-02a` と同じ手番で開発者と決める。**

**★★`DES-003`（データモデル）／`DES-006`（`DuplicateKey` とバリデーション）に及ぶ。⇒ CHANGE を 1 本消費する見込み。**

#### 3.3 ★★`M14` 第四波との順序（**見落としやすい**）

**`M14-RESEARCH-03` §4-4 が拾った `000062` のヘッダの一般化**——逐語＝**「1 回きりの backfill マイグレは、その後に INSERT される行を拾えない。取り残しはエラーにならず、unknown / false のまま静かに残る。」**

**★★`FR702` のマーカー列は同じ形をしている。**

| 順序 | 何が起きるか |
|---|---|
| **`M28` が先 → `M14` 第四波が後** | **★第四波で INSERT される 14 キャラの `moves` 行が、マーカー列の初期値をどう持つかを第四波側が知らなければならない。⇒ 第四波の指示書に 1 段増える** |
| **`M14` 第四波が先 → `M28` が後** | **★マーカー列の追加時に全キャラが揃っている。⇒ backfill が 1 回で済む** |

**★★【2026-09-05 解消】`M14-03f`（第四波）は完了・受理済みである**（`D-707`）**。⇒ 本項の順序の懸念は消えた。マーカー列の追加時に全 31 キャラが揃っており、backfill は 1 回で済む。**

以下は解消前の記述：**⇒ 設計卓の見立て＝`M14` 第四波を先に通すほうが段が減る。★ただし `M14` は独立レーンであり、開発者の手入力の進みで決まる**（移動 5 code の `total` の実測値受領 14 キャラぶんが前提＝`M14-RESEARCH-03` §2.2-4）。**⇒ 待てないなら `M28` を先に通し、第四波の指示書へ 1 段足す。★どちらでも成立する。順序を選べることを明示しておく。**

---

## 4. 依存関係・順序

```
M26-01（三層のパス割当）… ★M28-01 と独立。並べてよいが、フェーズ4 は並列運用をしないため順に通す
     │
     └─→ M28-01（正式名リネーム）… ★先送りするほど破壊面が単調増加する。⇒ M27 より先に通す（開発者一任＝D-653）
              │
              ├─→ M28-03（ツールチェーンと依存の版上げ）… ★M28-01 が devContainer / CI を tacpendium へ書き換えた「後」。
              │        ★M28-02 とは独立。順序の制約なし
              │
              ├─→ M28-04（move_code / command の是正）… ★M28-01 とも M28-02 とも独立。
              │        ★ただし公開前が安い（配布 DB の識別子が変わるため）
              │
              └─→ M28-02（FR702）… ★同じ窓に置く。リネームで動いた module path の上にスキーマを足す

M14 第四波 …（独立レーン）★M28-02 より先に終わっていると backfill が 1 回で済む（§3.3）
```

**★`M28-01` と `M28-02` を同じ窓に置く理由**——**どちらも配布物と DB に触れる。分けると利用者から見て破壊が 2 回起きる**（`D-560` が「並ぶ文字列を決める作業と並び順を決める作業は同じ 1 手番でやるべき」と判断したのと同じ形）。

---

## 5. CHANGE 見込み・採番

| サブ | CHANGE 見込み | マイグレ | 根拠 |
|---|---|---|---|
| **M28-01** | **0〜1 本** | **0 本** | **★設計書に「combomgr」という文字列が残っていれば改訂が要る。★起票時に実査する。★既定データディレクトリの移行方針を決めるなら `SUPP-001` に及ぶ** |
| **M28-02a** | **1〜2 本** | **★★2 本＝`000104` ＋ `000105`**（**2026-09-06 確定＝`D-738`**） | **`REQ-001` `FR702` の明確化 ／ `DES-003`（列追加）／ `DES-002`（導出の経路）／ `DES-005`（影響コンボの見せ方）／ `DES-006`（バリデーション）に及ぶ見込み**。**★★連番は `000103` 以降**（2026-09-05 是正。**旧記載 `000081`〜 は失効**＝`D-707`） |
| **★★M28-02b** | **1 本＝`CHANGE-162`**（**消費済み**） | **0 本の見込み**（**★§2.2-4 の記録先を DB に置く案を採るなら `000106` 以降を請求する**） | **`DES-005`＝新画面 ＋ 一覧のボタン ＋ 詳細の 1 行 ／ `DES-002`＝経路を足す場合。★★必達は設計まで**（§3.5） |
| **★★M28-02c** | **0〜1 本** | **0 本の見込み** | **★契約は `CHANGE-162` で先に書いてある。⇒ 実装が契約どおりなら CHANGE は不要。ずれたら請求する** |
| **★★M28-05** | **0 本** | **1 本＝`000106`** | **★terry `quick_burn_light` → `quick_burn`。★`docs/design/` の名指しは 0 件であることを `M28-04` が実査済み。⇒ CHANGE 不要。★★動く golden は 4 本である** |
| **★★M28-03** | **0〜1 本** | **0 本** | **★`DES-001` の技術スタック表が「Go 1.22+」と書いている。⇒ 上げるなら改訂が要る。★下限表記として意図的なら不要である**〔起票時の実査項目〕 |
| **★★M28-04** | **0〜1 本** | **★★0 本**（**2026-09-06 是正＝`D-759`。★`M28-04` は射程 9 行がすべて是正済みだったため 1 本も消費せず、`000106` を未消費のまま返した**） | **★`move_code` の是正は DML マイグレ 1 本。★設計書が guile の当該 code を名指ししているかを起票時に実査し、名指ししていたら改訂を請求する** |

**⇒ `M28` の見込みは 1〜3 本。次に採番するのは `151`**（**★`M26-01` が `151` を使う予定であり、`M28` は `152` 以降になる見込み。★先付けしない**＝playbook §4.15）。

**★★スキーマ変更は開発者の個別承認事項である**（`CLAUDE.md` §10）。**⇒ `M28-02` の起票時に承認を取る。**

### 5.1 推奨モデル

- **`M28-01`**——**高精度実装 ＋ Plan Mode 必須**（**★置換の対象範囲を誤ると検査スクリプトと歴史記録を壊す**）
- **`M28-02`**——**高精度実装 ＋ Plan Mode 必須**（`CLAUDE.md` §9＝スキーマ・API 契約・データ移行を伴う変更）
- **★`M28-03`**——**標準実装 ＋ Plan Mode 必須**（**★判断は少ないが、「緑になったか」の判定を誤らないこと**＝`go test` だけでは足りない）
- **★`M28-04`**——**標準実装 ＋ Plan Mode 必須**（**★配布 DB の識別子が変わる。★既存コンボが当該 `move_id` を参照していないかの実査が要る**）

---

## 6. ★設計卓が確かめていないこと（実査項目）

| # | 実査すること | なぜ |
|---|---|---|
| 1 | **`go.mod` の `module` 行の現在値** | **★リネームの起点** |
| 2 | **`combomgr` という文字列が実装・設定・ドキュメントの何か所に在るか**（**★区分ごとの母数**） | **★一括置換をさせないため、区分を先に出す** |
| 3 | **既定データディレクトリの現在値と決定経路**（`config.toml` ／ 既定値のコード） | **★利用者のデータの置き場。移行の要否がここで決まる** |
| 4 | **`moves` に「バージョン」を表す列が既に在るか** | **★無いことを前提に設計している。★在れば設計が変わる** |
| 5 | **`combos` に登録時刻以外の「時点」を表す列が在るか** | **★(b) 案の前提** |
| 6 | **`scripts/check-*.sh` が `combomgr` を含むか**（followup `bulk-rename-must-exclude-check-scripts`） | **★除外対象の確定** |

---

## 7. 開発者への確認事項（**★2026-09-02 に ①②④⑤ が決着した＝`D-664`**）


> **★★【2026-09-05 追加＝`D-726`】サブ構成の再承認が要る。**
>
> | # | 確認事項 | 状態 |
> |---|---|---|
> | ~~A~~ | ~~サブ構成 2 本 → 4 本の承認~~ | **★★決着＝承認**（2026-09-05・`D-727`。逐語＝「**A：再承認**」）**。⇒ `M28` は 4 サブで確定した** |
> | ~~B~~ | ~~`M28-03` / `M28-04` の投入順~~ | **★★決着＝`M28-03` → `M28-04`**（`D-727`）**。★★v1.5.0 の書き方が悪く、何と何の順序か読めなかった**〔開発者の問い＝「`M28-03` とはどういう事ですか？`M27-03` より先がよかった？」〕**。⇒ 本項は `M28-03` と `M28-04` の 2 本の間の順序であり、`M27-03` とも `M28-02` とも無関係である。★★教訓＝順序を書くときは「何と何の」を必ず書く** |

> **★★`M28` の 4 サブの順序関係**（`D-727` で確定）
>
> | 組 | 順序 |
> |---|---|
> | `M28-01` → `M28-03` | **★あり。** `M28-01` が devContainer / CI を `tacpendium` へ書き換えた後に版を上げる |
> | `M28-03` → `M28-04` | **★あり。** `M28-04` はマイグレを 1 本消費し `M28-03` はしない。⇒ 環境を固めてからデータに触る |
> | `M28-02` と他の 3 本 | **★★無し。独立である。** `M28-02` の制約は「`M28-01` の後」だけである。`M28-03` / `M28-04` とは前後どちらでもよい |
> | `M27-03` と `M28` の全サブ | **★★無し。触る面が違う。** `M27-03` は 2026-09-05 に `M28-01` と並列投入された |

| # | 内容 | 決着 |
|---|---|---|
| **①** | 正式名は `Tacpendium` か | **★確定**（開発者の逐語＝「①はい」）。**⇒ `M28-01` は `Tacpendium` で進める** |
| **②** | 既定データディレクトリが変わったときの既存データの扱い | **★★自動移行する**（逐語＝「破壊は別だが単なる移行は止めずにやりたい」）。**★設計卓の推奨（移さず案内する）は採らない。⇒ 移行の形を §3.1.1 に固定した** |
| **③** | `FR702` の基準バージョンの持ち場 | **★未決。** 逐語＝「じっくり検討したいのでもう一度詳しく判断材料を並べてください」。**⇒ §3.2 を全面的に立て直した**（§3.2.0 で設計卓の前回の推奨理由の誤りを訂正） |
| **④** | `M14` 第四波と `M28-02` のどちらを先か | **★`M14` を先にできる見込み**（逐語＝「M14、着手前には揃う見込み」）。**⇒ backfill が 1 回で済む形になる**（§3.3） |
| **⑤** | 2 サブ構成の承認 | **★承認**。**⇒ `M28-01` の指示書を起票した** |

### ★★③ について、いま開発者が決めることは 2 つである

**§3.2 に 5 案（(a) / (b) / (c) / (e) ＋ 却下した (d)）と比較軸 8 本を並べた。★決めるのは次の 2 点だけである。**

| # | 決めること | 選択肢 | 設計卓の推奨 |
|---|---|---|---|
| **1** | **持ち場** | **(a) アプリ設定に 1 つ ／ (b) コンボ単位 ／ (c) 両方 ／ (e) `updated_at` を流用** | **(b)。決め手は「部分消化できるか」の 1 軸**（§3.2.7） |
| **2** | **(b) を採る場合、既存コンボの初期値** | **(b-1) NULL＝不明（初回に全件出る・安全側） ／ (b-2) 列を足した時点の最新（初回 0 件・「無事の自動断定」）** | **(b-2)。★現時点の実質的な利用者が 1 人であり、失うものが実質ゼロだからである** |

**★★①〜⑤ とは別に、`M28-02` の起票時にスキーマ変更の個別承認が要る**（`CLAUDE.md` §10）。**⇒ ③ が決まった時点で改めて 1 行いただく。**

## 8. 参照

| ID | パス |
|---|---|
| 上位 | `docs/instructions/phase4-overview.md` v1.1.0 §M28 |
| 破壊窓の定義 | `docs/human-notes/combmgr-prerelease-checklist.md` §4 |
| `FR702` の確定済み方向性 | 同 §5.1（**★着手時にゼロから議論しない**） |
| 要件本文 | `REQ-001` `FR701` / `FR702` / `FR703` / `FR704` |
| 正式名 | ボード `D-462` / `D-470` / `D-474` / `D-636` ／ `docs/handover/session-prompts/20260820-app-name-trademark-check.md` |
| ベースライン化の不実施 | ボード `D-648` |
| `M14` との順序 | `docs/progress/M14-RESEARCH-03-report.md` §4-4（`000062` の一般化）／ §2.2-4 |
| 一括置換の禁止 | `docs/handover/followup-backlog.md` `bulk-rename-must-exclude-check-scripts` |

---

*以上*
