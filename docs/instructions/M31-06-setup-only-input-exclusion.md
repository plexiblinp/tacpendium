# 指示書 M31-06: `setup_only` の技をコンボの入力面から外す経路を作る（`P4M-005` のフェーズ4 分）

| 項目 | 内容 |
|------|------|
| 文書ID | M31-06 |
| バージョン | **1.2.0**（2026-09-12・**★★§2.2-5 の条件分岐が 1 つ潰れた＝`setupOnly` は既に `GET /api/moves` に出ている**〔`M30-04` の実 DTO ダンプで確認〕**。⇒ バックエンド側の同期追加は要らない。★★あわせて `M30-05` との並列上の注意を足した＝`D-827`。★チェックリスト v1.0.0 を同時発行**） ／ 1.1.0（2026-09-10・**★★★`M31-04` が同じ 2 面に `INPUT_EXCLUDED_MOVE_CODES` / `isInputExcluded()` を新設していた＝`D-810`。⇒ 新設ではなく既存機構を拡張する形へ改めた。★段 1 の設計判断が入れ替わった**） ／ 1.0.0（2026-09-10） |
| 作成日 | 2026-09-10 |
| 作成者 | 設計担当 Claude（設計卓／フェーズ4 期） |
| 対象マイルストーン | **`M31`**（`α②`）。上位＝`docs/instructions/M31-overview.md` v1.10.0 |
| 推奨モデル | **高精度実装 ＋ Plan Mode 必須**（**★★コンボとセットプレイが同じ入力部品を共有しており、片方だけに効かせる必要がある**。実モデル名は `docs/human-notes/model-allocation.md`） |
| Plan Mode | **必須**（`CLAUDE.md` §9＝**共有部品の挙動を分岐させる**） |
| レビュー | **必要**（チェックリストは着手承認の手番で設計卓が起こす＝`D-736`） |
| CHANGE 消費 | **1 本の見込み**（`DES-003` §3.3 の `setup_only` の記述を as-built へ寄せる ＋ `DES-005` の入力面の規則。**★起票は設計卓。自採番しない**＝`D-293`） |
| マイグレ消費 | **0 本**（**★★データを 1 行も足さない**＝§0.1） |
| 前提サブ | **★★★`M31-04` の完了とマージ**（**★同サブが `INPUT_EXCLUDED_MOVE_CODES` / `isInputExcluded()` を新設し、本サブが外す 2 面にちょうど効かせている**＝§0.4）**。★`M31-RESEARCH-01` の調査結果の上にも立つ**（完了・受理済み＝`D-793`） |
| 射程 identity | **1 件**——`P4M-005` の**フェーズ4 分のみ**（**★フラグの付与とデータ投入はフェーズ5**＝§0.1） |
| 配置 | `docs/instructions/M31-06-setup-only-input-exclusion.md` |
| 出力 | `docs/progress/M31-06-completion-report.md` |

---

## 0. この指示書の特殊性

### 0.1 ★★★本サブは「データが 0 件のうちに経路だけ作る」ものである

**★`moves.setup_only = 1` の技は現在 0 件である**（`M31-RESEARCH-01` の実測）**。**

**★★したがって本サブを入れても、画面の見え方は 1 か所も変わらない。⇒ これが狙いである。**

| 段 | いつ | 何を |
|---|---|---|
| **★★フェーズ4**（本サブ） | **今** | **`setup_only = 1` の技を、コンボの入力面から外す経路を作る。⇒ データが 0 件なので、既存の入力は 1 つも変わらない** |
| **★★フェーズ5** | 後 | **どの `move_code` に立てるか ／ 付与の口をどこに置くか。★開発者のドメイン知識が要る** |

**★★★先に経路を作る理由＝フラグを立ててから経路を作ると、立てた瞬間に画面が変わる。⇒ 退行の危険が最大の順序になる。★逆にすれば、危険が最小の手番で経路を入れられる。**

**★★★開発者の逐語**（2026-09-09）**＝「後回しで良い。phase5 以降。やるとしたらフラグが立っているmoveはコンボ登録時の仮想コントローラ、おとび、すべての技プルダウンから除外し、セットプレイだけで出すという経路を先につけておくこと。フラグの設定や技登録はphase5」。**

### 0.2 ★★★外すのは入力面だけである。⇒ 一覧・詳細では出す

**★★★開発者の逐語**（2026-09-10）**＝「なお `setup_only` の技が始動技になることはあるので、一覧や詳細では出すことは記録をお願いします」。**

| 面 | 本サブでどうするか |
|---|---|
| **★★コンボ登録の仮想コントローラ** | **外す** |
| **★★コンボ登録の全技一覧プルダウン** | **外す** |
| **★★セットプレイのレシピ入力** | **★出す**（**⇒ ここが唯一の入り口になる**） |
| **★★★コンボ一覧・コンボ詳細・比較** | **★★★出す。⇒ 1 か所も触らない** |
| **★★既存レシピの表示・再編集** | **★★出す。⇒ 1 か所も触らない** |
| **★filler / target の提案候補** | **★触らない**（**⇒ 表示制御と提案候補制御は独立である**＝`M31-RESEARCH-01` §4） |
| **★取込での解決** | **★触らない** |

**★★★これを落とすと、その技を使ったコンボが一覧で読めなくなる。⇒ 「隠す」を表示面まで広げると、保存済みのコンボが壊れて見える。★`M30-02` が「素は押せるが OD が押せない」を 7 組作ったのと同じ形の事故である。**

**★★★「表示を省くこと」と「保存を禁止すること」は別である**（`M31-RESEARCH-01` §4 の逐語）**。⇒ 本サブは保存を禁止しない。★既に保存されているコンボも、これから物理入力や取込で入るものも、そのまま通す。**

### 0.3 ★開発者の逐語の読み取りを 1 件、確認事項に回す

**★逐語の「おとび」は「および」の打ち間違いと読んだ。⇒ したがって外す面は 2 つである**〔仮想コントローラ ／ 全技プルダウン〕**。**

**★★設計卓は当初これを 3 つ目の面**（「帯」）**として読み、`D-804` / `D-805` へ「3 つ」と記録した。⇒ 是正済みである**（`D-807` / 計測点 `M-153`）**。★コンボ登録に「帯」という入力面は無い**（`DES-005` の「ヘッダ帯」はキャラクター選択であり、技の入力面ではない）**。**

**⇒ 読み取りが違っていたら §7-1 で申し出ること。**

### 0.4 ★★★【2026-09-10 追加＝`D-810`】機構は既に在る。⇒ 新設せず、拡張すること

**★★★`M31-04`**（確定反撃の D リバ）**が、本サブが外そうとしている 2 面にちょうど効く機構を新設していた。⇒ 本サブは 2 本目を作ってはならない。**

| 何が | どこに |
|---|---|
| **`INPUT_EXCLUDED_MOVE_CODES` / `isInputExcluded()`** | `web/src/features/combo/moveSurfacing.ts` |
| **仮想コントローラのタブ**（**★未分類を含む**） | `surfaceBuckets` の `unclassified` 分岐 |
| **全技一覧のプルダウン** | `useControllerInputOmission` の `selectableMoves`（**★トグルの状態に関係なく常に落とす。★`omittedCount` には数えない**） |

**⇒ 本サブがやるのは「`isInputExcluded()` の判定に `setup_only` を足すこと」である。★新しい関数も新しい定数も作らない。**

**★★★これで §4.2 の落とし穴が 1 つ解けた＝「`isControllerSurfaced` を `false` にするだけでは未分類に残る」は `M31-04` が既に解いている**（`unclassified` 分岐へ効かせた）**。⇒ 同じ経路に乗ればよい。**

**★★軸が 2 つ並ぶことは避けられない＝`INPUT_EXCLUDED_MOVE_CODES` は静的な `code` 列挙**（フロント定数）**であり、`setup_only` はデータ駆動のフラグである。⇒ 由来が違う。**

**★★★しかし適用点は 1 つにすること。⇒ `isInputExcluded()` の中で「静的な列挙に載っているか」と「`setup_only` が立っているか」を論理和にする。★入口が 2 つに割れると、次に触る人がどちらを正とするかで迷う**（`followup` の `input-excluded-codes-vs-setup-only-overlap` がこれを記録している）**。**

**★★セットプレイ側の扱いは変わらない＝`M31-04` の除外は `drive_reversal` の 1 件であり、セットプレイで出す必要が無い技だった。⇒ 本サブは「コンボ側では落とすがセットプレイ側では出す」を初めて要求する。★段 1 の設計判断はここへ移った**（§2.1）**。**

---

### 0.6 ★★★`setupOnly` は既に API に出ている（**2026-09-12 追記＝`D-827`。⇒ §2.2-5 の「出ていなければ」は成立しない**）

**★★§2.2-5 は「現在 `MoveResponse` に出ているかを実物で確かめ、出ていなければ DTO の同期追加が要る」と条件付きで書いている。⇒ 確かめた。出ている。**

**★根拠＝`M30-04` が使い捨て DB へ本番同様のマイグレを全適用してハンドラを叩き、得た JSON をそのまま貼った実 DTO ダンプ**（同サブ設計伝達レポート §1-1）**に `"setupOnly": false` が実在する。**

**⇒ バックエンド側の 4 点同期**（`model.Move` ／ `MoveListItem` ／ SELECT 列 ／ divergence guard）**は要らない。★`M30-04` が `is_derived` でやった作業を、本サブが `setup_only` で繰り返す必要は無い。**

**★★ただし TS 側は別である。⇒ `web/src/features/moves/types.ts` の `Move` に `setupOnly` が在るかは実物で確かめること。無ければ足す。★足すなら `ComboSummary` / `ComboDetail` / `Combo` の 3 分岐に漏れが無いこと**（`web/CLAUDE.md` §2）**。**

### 0.7 ★★★並列上の注意（**2026-09-12 追記＝`D-827`**）

**★★`M30-05` と同時に走る。⇒ 触る実装ファイルは互いに素である。**

| サブ | 実装で触るファイル |
|---|---|
| **本サブ（`M31-06`）** | `web/src/features/combo/moveSurfacing.ts` ／ `internal/repository/move/divergence_test.go` |
| **`M30-05`** | `web/src/features/combo/inputResolution.ts` |

**⇒ 実装の衝突は起きない。★ただし共有テスト `web/src/features/combo/moveSurfacing.roster.test.ts` は両方が触りうる。**

**★★本サブは未分類の件数を動かすので、ほぼ確実に触る。⇒ 何をどう変えたかを報告に書くこと。★書かないと、マージのときに黙って片方の変更が消える。**

> **★同テストは `DES-005` §6.1 が「★★この 1 本を消さないこと」と名指ししている。⇒ 期待値を緩める形での解決は採らないこと。**

---

## 1. 前提として読むもの

| # | 何を | どこ |
|---|---|---|
| **★★1** | **実測済みの面と行番号** | **`docs/progress/M31-RESEARCH-01-report.md` §4**（**★★本サブが触る箇所はすべてここに実測がある**） |
| **★★2** | **共有部品の構造** | **同 §4 の 3 行目**（**★★セットプレイのレシピ入力は、コンボと同じ `moves` 取得・省略フック・`VirtualController` を使う**） |
| **★★3** | **列の定義** | `DES-003` §3.3（`moves.setup_only`）／ `docs/change-notes/phase2/CHANGE-022-moves-frame-data-and-csv-contract.md` §2（**★「コンボ登録 UI の技選択から除外する」と既に書いてある**） |
| **★4** | **過去の確定** | `docs/instructions/M19-01-setplay-suggestion-engine-and-ui.md` §4.5（2026-07-24 開発者確定＝「ユーザー登録のセットプレイ専用 move」「コンボ登録等の技選択から隠すための行区分」） |
| **★★★5** | **既存機構の実物** | **`web/src/features/combo/moveSurfacing.ts` の `INPUT_EXCLUDED_MOVE_CODES` / `isInputExcluded()`**（**★`M31-04` が新設**）**／ `useControllerInputOmission` の `selectableMoves` ／ `surfaceBuckets` の `unclassified` 分岐。★★読まずに新設しないこと** |
| **★★6** | **等式が崩れたこと** | **`M31-04` 設計伝達レポート §1-5**（**★★「未分類タブ ＝ `isControllerSurfaced` の裏返し 1 本」は崩れた。⇒ 未分類 ＝ 裏返し ∧ 非除外である**）**／ `CHANGE-176`** |
| **★★7** | **未決のまま残る事項** | **`M31-RESEARCH-01` §6**（**★★本サブが埋めるのは「非表示の範囲」の 1 行だけである。⇒ 他の 5 行はフェーズ5 に残る**） |

---

## 2. やること

### 2.1 ★★★段 1 — コンボ側とセットプレイ側をどこで分けるかを決める（**★本サブ最大の設計判断**）

**★★★【2026-09-10 改版＝`D-810`】判断の中身が入れ替わった。⇒ 「どこに機構を置くか」はもう決まっている**（§0.4＝`isInputExcluded()` を拡張する）**。★残る判断は「コンボ側とセットプレイ側をどう分けるか」だけである。**

**★★問題＝セットプレイのレシピ入力は、コンボ登録と同じ部品を使っている。⇒ 部品の中で `setup_only` を落とすと、セットプレイからも消える。**

**★★★`M31-04` はこの問題を持っていなかった。⇒ 同サブが除外した `drive_reversal` は、セットプレイで出す必要も無い技だったからである。★「コンボ側では落とすがセットプレイ側では出す」を要求するのは本サブが初めてである。**

**★実測**（`M31-RESEARCH-01` §4）:

| 面 | 実測された箇所 |
|---|---|
| コンボの仮想コントローラ 8 タブ | `RecipeBuilder:196` から `moves` を渡す。`isControllerSurfaced:180` は 7 面の論理和で `setupOnly` を見ない |
| コンボの全技一覧プルダウン | `RecipeBuilder:88` ／ `useControllerInputOmission:75`。既定は全件。ON は分類済み技を省く |
| セットプレイのレシピ入力 | `SetupRecipeEditor:85,115,197`。**★同じ `moves` 取得・省略フック・`VirtualController` を使う** |

**★★★行番号はレポート作成時点のものである。⇒ そのまま信じず、実物で位置を取り直すこと。**

**★★【2026-09-10 解決済み＝`D-810`】この落とし穴は `M31-04` が既に解いている。⇒ 同サブが `unclassified` 分岐と `selectableMoves` の両方へ効かせた。★同じ経路に乗ればよい。**

> **★以下は経緯である。** 落とし穴の実測＝`isControllerSurfaced` を `false` にするだけでは足りない。**「未分類」は 7 面の論理和の否定なので、`false` にすると未分類タブと全技一覧に残る**（`M31-RESEARCH-01` §4 の 2 行目）。

**★段 1 の成果物**:

| # | 何を |
|---|---|
| **★★1** | **分けかたの案。⇒ `isInputExcluded()` へ「面」を渡すか、セットプレイ側の呼び出しでだけ除外を外すか、どちらかを選び、理由を書くこと** |
| **★★2** | **その案で、セットプレイ側が 1 件も減らないことをどう保証するか** |
| **★★★3** | **`M31-04` の `drive_reversal` が、本サブの変更でセットプレイ側に出るようになっていないこと。⇒ あちらは両面で外れているのが正しい。★「面で分ける」を入れると、静的除外まで面で分かれてしまう** |
| **★4** | **`omittedCount` の数え方を変えていないこと**（`M31-04` は除外分を数えない形にした） |

**★★実装詳細は製造の裁量である**（`CLAUDE.md` §9-2）**。⇒ 検討した案と選定理由を報告に書くこと。★止めて聞かなくてよい。**

### 2.2 段 2 — 経路を入れる

| # | やること |
|---|---|
| **★★★1** | **`isInputExcluded()` の判定に `setup_only` を足す**（**★新しい関数も新しい定数も作らない**＝§0.4）**。⇒ これだけで仮想コントローラ 8 タブ**（未分類を含む）**と全技一覧プルダウンの両方に効くはずである。★実物で確かめること** |
| **★★2** | **全技一覧プルダウンで、省略トグルの ON / OFF どちらでも外れること。★`omittedCount` には数えないこと**（`M31-04` の形に揃える） |
| **★★3** | **セットプレイのレシピ入力では出す。⇒ 1 件も減らさない** |
| **★★★4** | **コンボ一覧・コンボ詳細・比較・既存レシピの表示と再編集は 1 か所も触らない** |
| **★★5** | **`setup_only` をフロントが読めるようにする。⇒ 現在 `MoveResponse` に出ているかを実物で確かめ、出ていなければ DTO の同期追加が要る**（**★`internal/repository/move/divergence_test.go` が両構造体の同期を強制する**＝`followup` G-12 と同型） |
| **★6** | **`setup_only` の保存は禁止しない。⇒ 物理入力・取込・既存レシピは通す** |

### 2.3 ★★★段 3 — 陽性対照を作って測る（**★データが 0 件なので、入れただけでは何も測れない**）

**★★★本サブ最大の検査上の困難＝`setup_only = 1` が 0 件なので、経路を入れても入れなくてもテストは同じ結果になる。⇒ 「入れた」を「効く」の証拠と読ませないこと。**

| # | やること |
|---|---|
| **★★★1** | **テストの中だけで `setup_only = 1` の技を 1 件でっち上げ、コンボ側の 2 面から消え、セットプレイ側に残ることを測る。⇒ これが唯一の効き目の証拠である** |
| **★★★2** | **同じでっち上げで、コンボ一覧・詳細に出ることを測る**（§0.2 の事故の陽性対照） |
| **★★3** | **でっち上げは `migrations/` へ入れない。⇒ テストの中だけで作ること。★本番データは 0 件のままである** |
| **★★4** | **既存の技が 1 件も消えていないことを、面ごとの件数で測る。⇒ 入れる前と後で同数であること**（`setup_only = 1` が 0 件だから同数になるはずである） |

---

## 3. やらないこと

| # | 何を | なぜ |
|---|---|---|
| **★★1** | **`setup_only` を立てる。⇒ どの `move_code` にも立てない** | **フェーズ5 である**（開発者裁定・`D-804`） |
| **★★2** | **付与の口を作る**（`PATCH` の項目 ／ CSV 列の入力） | **同上。★誰がどの経路で付与するかは未決**（`M31-RESEARCH-01` §6） |
| **★★3** | **キャミィ 9 件の親の分けかた** | **同上。★強度別フレームの実測が要る** |
| **★★4** | **filler / target の提案候補を絞る** | **表示制御と提案候補制御は独立である**（`M31-RESEARCH-01` §4）**。★現行テストの期待値と食い違う** |
| **★★5** | **`custom_states` との対応づけ** | **状態 code の一致をフラグへの機械変換規則にしない**（`M31-RESEARCH-01` §5 選択肢 C） |
| **★6** | **`M19-DESIGN-02` の残存案・照会中資料の整理** | **設計卓の手番である**（`M31-RESEARCH-01` §6 の最終行） |

---

## 4. 危険な箇所

### 4.1 ★★★共有部品に入れると、セットプレイからも消える

**⇒ 本サブが「経路を作る」意味そのものが消える。★セットプレイだけで出すことが目的である。**

**★段 3-1 の陽性対照が、これを唯一検出できる形である。**

### 4.2 ★★★機構を 2 本作ること

**⇒ `M31-04` が `INPUT_EXCLUDED_MOVE_CODES` / `isInputExcluded()` を既に持っている**（§0.4）**。★2 本目を作ると、次に触る人がどちらを正とするかで迷う。⇒ `followup` の `input-excluded-codes-vs-setup-only-overlap` が記録している。**

**★★あわせて旧版の危険を 1 つ引き継ぐ＝`isControllerSurfaced` を `false` にするだけでは未分類タブと全技一覧に残る。⇒ ただし `M31-04` の経路に乗れば起きない。★乗らずに自前で書くと踏む。**

### 4.3 ★★★表示面まで広げると、保存済みのコンボが壊れて見える

**⇒ `setup_only` の技は始動技になりうる**（開発者・2026-09-10）**。★一覧で技名が消えたコンボは、利用者から見れば壊れている。**

### 4.4 ★★データが 0 件なので、検査は緑のまま通る

**⇒ 段 3 の陽性対照を作らなければ、経路が 1 行も効いていなくてもテストは緑である。★これは `M35-01` の「破壊確認が成立しなかった」と同じ族である。**

---

## 5. 検査

| # | コマンド | 判定 |
|---|---|---|
| 1 | `go test ./...` | 緑 |
| 2 | `cd web && pnpm test` | 緑 |
| 3 | `make e2e-only P=combo-crud` | 緑（**★既存の入力が壊れていないこと**） |
| **★4** | `bash scripts/check-import-order.sh` | **ベースラインどおり**（**★`D-388`＝レーンごとに下げない**） |
| 5 | `bash scripts/check-enum-sync.sh` | 増えていないこと |
| **★6** | `bash scripts/check-md-emphasis.sh docs/progress/M31-06-completion-report.md` | **0 行**（`D-775`） |

---

## 6. 完了条件

| # | 条件 |
|---|---|
| **★★1** | **段 1 の案と選定理由が報告に在る** |
| **★★★2** | **`isInputExcluded()` を拡張しており、2 本目の機構を作っていない**（§0.4）**。⇒ コンボの仮想コントローラ**（未分類を含む）**と全技一覧の 2 面から `setup_only = 1` が外れる** |
| **★★3** | **セットプレイのレシピ入力では出る** |
| **★★★4** | **段 3-1 の陽性対照**（でっち上げ 1 件で消えることと残ることを測る）**が在る** |
| **★★★5** | **段 3-2 の陽性対照**（一覧・詳細に出る）**が在る** |
| **★★6** | **既存の技が 1 件も消えていないことが、面ごとの件数で示されている** |
| **★★★7** | **`M31-04` の `drive_reversal` が、両面で外れたままであることが示されている**（**★セットプレイ側に出るようになっていないこと**） |
| **★7** | **`migrations/` を 1 本も足していない。⇒ `setup_only = 1` の本番データは 0 件のままである** |
| 8 | **§5 の検査がすべて緑**（**★出力で判定。★最後に回す**） |
| **★9** | **`docs/progress/M31-06-completion-report.md` を書き、`docs/progress/progress-log.md` へ索引行を 1 行足す**（`CLAUDE.md` §8） |
| **★★10** | **完了報告を `bash scripts/check-md-emphasis.sh <file>` に通す**（`D-775`） |
| **★★11** | **設計伝達レポートを出す**（`/design_handover_report`）**。★DTO へ `setup_only` を足したなら `DES-002` §11.3 への CHANGE の請求を書くこと** |

---

## 7. ★開発者への確認事項

| # | 事項 | 段 |
|---|---|---|
| **★1** | **逐語の「おとび」を「および」と読んだ。⇒ 外す面は 2 つ**〔仮想コントローラ ／ 全技プルダウン〕**でよいか。★3 つ目の面を指していたなら教えてほしい** | **段 1** |

**★★これ以外は聞かなくてよい。⇒ §0.1 / §0.2 は開発者裁定で決着している**（`D-804` / `D-805`）**。**

---

## 8. 参照ドキュメント

| ID | パス | 参照する箇所 |
|----|------|------------|
| — | `docs/progress/M31-RESEARCH-01-report.md` | **★★§4**（面と行番号）／ **§5**（選択肢）／ **§6**（未決のまま残るもの） |
| DES-003 | `docs/design/03-data-model.md` | **§3.3**（`moves.setup_only`） |
| DES-005 | `docs/design/05-screen-design.md` | **§5.7**（コンボ登録）／ **§5.9**（セットプレイ登録・編集。**★§5.19 ではない**＝計測点 `M-147`） |
| — | `docs/change-notes/phase2/CHANGE-022-moves-frame-data-and-csv-contract.md` | **§2**（列定義） |
| — | `docs/handover/followup-backlog.md` | **`setup-only-flag-has-no-data`** ／ **`input-excluded-codes-vs-setup-only-overlap`**（**★★本サブが解く行**）／ **G-12**（**★DTO 同期追加の型**） |
| — | `docs/change-notes/CHANGE-176-notification.md` | **★★`M31-04` の as-built**（**未分類 ＝ 裏返し ∧ 非除外**） |
| — | `docs/process/parallel-board.md` | **`D-804`**（2 段に分けた裁定）／ **`D-805`**（表示面では出す）／ **`D-807`**（起票） |

---

*以上、指示書 M31-06 **v1.2.0**。* **★★本サブの落とし穴は「隠すだけ」と読むことである。** **★隠す先が 2 面あり、そのうち片方は共有部品の向こう側にセットプレイが居る。** **★★もう 1 つは §4.4 である**——**データが 0 件なので、経路が 1 行も効いていなくても検査は緑のまま通る。⇒ 陽性対照を作らないと、何も測れていない。**
