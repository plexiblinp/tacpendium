# 指示書 M37-03: 一覧・マイコンボ・ゴミ箱の 3 件（`B01` / `B03` / `B15`）

| 項目 | 内容 |
|------|------|
| 文書ID | M37-03 |
| バージョン | **1.0.0**（2026-09-13・起票） |
| 作成日 | 2026-09-13 |
| 作成者 | 設計担当 Claude（設計卓） |
| 対象マイルストーン | **`M37`**（リリース前要望の束）。上位＝`docs/instructions/M37-overview.md` v1.1.0 |
| 推奨モデル | **標準実装**（実モデル名は `docs/human-notes/model-allocation.md`） |
| Plan Mode | **必須**（`CLAUDE.md` §9＝**`B15` が列挙値とスキーマ制約に触る**） |
| レビュー | **必要**（**★チェックリスト＝`docs/instructions/reviews/M37-03-review-checklist.md` v1.0.0・2026-09-13 発行済み**） |
| CHANGE 消費 | **1〜2 本の見込み**（`DES-003` / `DES-005` / `DES-006`。**★自採番しない**＝`D-293`） |
| マイグレ消費 | **★`000113` を払い出し済み**（`B15` 用・**1 本の見込み**）**。★足りなければ止めて請求する。自採番しない** |
| 前提サブ | **無し**（**★`M37` の中で唯一、保留にも RESEARCH にも依存しない**） |
| 射程 identity | **3 件**——`B01`（ゴミ箱の並び）／ `B03`（マイコンボのタグ固定表示）／ `B15`（ヒット種別に空中ヒット） |
| 配置 | `docs/instructions/M37-03-list-mycombo-trash-fixes.md` |
| 出力 | `docs/progress/M37-03-completion-report.md` |

---

## 0. この指示書の特殊性

### 0.1 ★★3 件は面が違う。**束ねたのは「どれも小さい」からである**

**★★★別々の要求であり、受入条件も別である**（`M37-overview` §1.2）**。⇒ 1 つに畳まないこと。**

| ID | 触る面 | `DES-005` |
|---|---|---|
| `B01` | ゴミ箱の並び順 | §5.15 |
| `B03` | マイコンボのタグ固定表示 | §5.5 |
| `B15` | ヒット種別の値の追加 | §5.7（登録）／ §5.4（フィルタ） |

**★段に割り、段ごとにチェックポイントコミットを切ること**（`CLAUDE.md` §7）**。**

### 0.2 ★★★`B15` が本サブの重心である

**★列挙値を 1 つ足す作業は、触る面が広い。⇒ `CLAUDE.md` §4 の「列挙的文字列定数の同期」が効く。**

| 同期先 |
|---|
| `internal/model/` の定数 |
| `web/src/constants/` の対応定数 |
| ラベル（ja / en の両方） |
| フィルタの選択肢 |
| CSV の受け口と検証 |
| **★DB の `CHECK` 制約（在れば）⇒ マイグレ** |
| **★`DES-003` §3.2 の値域 ／ `DES-005` の表示順とラベル ／ `DES-006` の検証** |

**★★`bash scripts/check-enum-sync.sh` を回すこと。⇒ 既定は warning だが、本サブは列挙を足す当のサブである。**

### 0.3 ★★`B01` は「表示」ではなく「並び順」である

**★`M37-overview` §1.2 の逐語＝「削除日時の*表示*や、二重削除時の日時保持とは*別の*要求である。⇒ 並び順への対応を確認する」。**

**⇒ 「削除日時が出ているから満たしている」と読まないこと。★並び替えられるか、既定が削除日時降順か、を見る。**

### 0.4 ★★★着手前の版ゲート（**`M-169` の対処**）

| # | 確かめること | 満たさないとき |
|---|---|---|
| **1** | `docs/instructions/reviews/M37-03-review-checklist.md` が存在すること | **★止まって設計卓へ請求する** |
| **2** | `docs/instructions/M37-overview.md` が `main` に在り v1.1.0 以上であること | **★同上** |
| **3** | **`ls migrations/` の最新が `000112` であること**（**★`000113` を払い出した前提**） | **★食い違ったら自採番せず再請求**（`D-293`） |
| **4** | `git log --oneline -5` で枝元を報告へ書く | — |

---

## 1. 前提として読むもの

| # | 資料 | 読む箇所 |
|---|---|---|
| **★★1** | `docs/instructions/M37-overview.md` v1.1.0 | **§0.2**（全件を新規実装するのではない）／ **§1.2**（3 件の受入条件）／ §4.1（目的で書く）／ §4.2（説明書の追随） |
| **★2** | `docs/design/03-data-model.md` | **§3.2 `combos.hit_type` の行**（**★値域。⇒ 段 1 で実測と突き合わせる**） |
| 3 | `docs/design/05-screen-design.md` | **§5.4**（フィルタ）／ **§5.5**（マイコンボ）／ **§5.7**（登録）／ **§5.15**（ゴミ箱） |
| 4 | `docs/design/06-validation.md` | ヒット種別の検証 |
| **★5** | `CLAUDE.md` §4 | **列挙的文字列定数の同期**（Go ⇄ TS） |
| **★6** | `web/CLAUDE.md` §1 | **★ブラウザストレージの台帳**（`B03` の保存先で使う） |
| 7 | `docs/process/parallel-board.md` | `D-853`（受理）／ `D-293`（自採番しない）／ `CHANGE-151`（`M27-01` のヒット種別） |

---

## 2. やること

### 2.1 ★★★段 1 — 実査（**★コードを書く前に済ませ、報告へ書く**）

| # | 確かめること | なぜ |
|---|---|---|
| **★★★1** | **`hit_type` の現在の値の全数**（**Go 定数 ／ TS 定数 ／ ラベル ja/en ／ DB の `CHECK` 制約の有無 ／ 実 DB の distinct 値**） | **★★`DES-003` §3.2 は 3 値**（`normal` / `counter` / `punish_counter`）**と書いているが、`M27-01` 以降に増えている可能性が高い。⇒ 設計書と実測が食い違ったら、それが成果である** |
| **★★2** | **`hit_type` に `CHECK` 制約が在るか** | **★在ればマイグレが要る。⇒ 無ければ `000113` は消費しない** |
| **★3** | **ゴミ箱の現在の並び順**（既定と、並び替えの有無） | **★`B01` は「並び順」である**（§0.3） |
| **★★4** | **一覧の列設定など、既存の「画面の設定」の保存先** | **★★`B03` の保存先を先例に合わせるため。⇒ `web/CLAUDE.md` §1 の台帳を引くこと** |
| 5 | **マイコンボのタグ表示の現在の作り** | `B03` の土台 |

### 2.2 ★`B01` — ゴミ箱を削除日時で並べる

| # | やること |
|---|---|
| 1 | **既定の並びを削除日時の降順にする**（**★新しく消したものが上**） |
| **★2** | **段 1 の実査で「既に降順である」と分かったら、実装せず*証拠付きで*閉じる**（`M37-overview` §0.2） |
| 3 | **並び替えを足すかは実装判断でよい。⇒ 足さないなら理由を報告に書く** |

### 2.3 ★★`B03` — マイコンボで特定タグを固定表示する設定

| # | やること |
|---|---|
| **★★1** | **保存先を段 1 の先例に合わせる。⇒ 一覧の列設定と同じ面である** |
| **★★★2** | **ブラウザストレージを使うなら、`web/CLAUDE.md` §1 の台帳へキーを足すこと**（**★台帳未記載のキーを使わない**＝`CLAUDE.md` §10.X）**。⇒ `bash scripts/check-browser-storage-keys.sh` が緑になること** |
| **★3** | **DB へ持たせる必要があると判断したら、止まって設計卓へ請求する**（**★マイグレを自採番しない**） |
| 4 | **固定したタグが、絞り込みを変えても表示され続けること** |

> **★★設計卓の見立て＝ブラウザストレージで足りる。** 一覧の列設定と同じ「画面の見せ方の設定」であり、`§10.X` の許容用途（**設計書本体で明記された UI 状態保持**）に収まる。**⇒ ただし台帳への追記は必須であり、CHANGE 経由で通すこと。**
>
> **★実装判断で DB を選ぶこともありうる。⇒ その場合は止まって請求する。★理由は「マイグレを消費するから」ではなく「利用者データの持ち方は設計事項だから」である。**

### 2.4 ★★★`B15` — ヒット種別に空中ヒットを追加

| # | やること |
|---|---|
| **★★1** | **段 1 の実測を出発点にする。⇒ `DES-003` の 3 値を出発点にしない** |
| **★★★2** | **値のコードと日本語・英語ラベルを決めて報告に書く**（**★設計卓の案＝コードは `air_hit`。⇒ 既存値の命名と揃うかを実測で確かめること**） |
| **★★3** | **`CLAUDE.md` §4 の同期をすべて満たす**（§0.2 の表） |
| **★4** | **`CHECK` 制約が在るならマイグレ `000113` で足す。⇒ 無ければ消費しない**（**★消費しなかったことを報告に書く**） |
| **★5** | **既存データへの影響 0 を示す**（**★値を足すだけであり、既存行の `hit_type` は変わらない**） |
| 6 | **フィルタの選択肢にも出る** |

> **★★★`M37-overview` §1.2 の逐語＝「過去の壁やられ 2 種追加とは*別の*追加要求である」。⇒ 「似た追加を前にやった」を完了根拠にしないこと。**

### 2.5 ★段 5 — 説明書の追随（**`M37-overview` §4.2**）

**★`docs/usermanual/` の §5.4 / §5.5 / §5.7 / §5.15 に対応する章へ追随させること。⇒ `M32-02` が未着地なら、変わった点を完了報告に列挙して渡す。**

---

## 3. やらないこと

| # | 内容 | 理由 |
|---|---|---|
| **★★1** | **`hit_type` の既存値を変える ／ 消す** | **★足すだけである。⇒ 変えたくなったら止めて請求する** |
| **★★2** | **マイグレを自採番する** | `D-293`。**★払い出し済みは `000113` の 1 本だけ** |
| 3 | **一覧の横幅・列の並び** | **★`M27-03` / `P-21` の面。⇒ 射程外** |
| **★4** | **`B02` / `B04` 〜 `B14` に手を出す** | **★他サブの射程**（`M37-02` / `M37-04`） |
| **★5** | **設計書本体を書き換える** | `CLAUDE.md` §8。**⇒ as-built は設計伝達レポート §4 へ** |
| **★★6** | **`followup-backlog.md` の編集** | **`D-838`。⇒ 停止時記録も設計伝達レポート §4 へ原稿として書く** |
| 7 | **台帳未記載のブラウザストレージキーを使う** | `CLAUDE.md` §10.X |

---

## 4. ★★危険（**先に読むこと**）

### 4.1 ★★★最大の危険＝`DES-003` の 3 値を出発点にすること

**★同書 §3.2 の `hit_type` の行は「`normal` / `counter` / `punish_counter`」と書き、「将来的にガード破壊系の追加を検討」と続けている。⇒ この記述が今日も正しいとは限らない。**

**★★`M27-01`（`CHANGE-151`）がヒット種別に触れており、`followup` の `P-21` は「`M27-01` でヒット種別が 8 値になり横幅が伸びた」と述べている。⇒ 設計卓は実装を読めないため、どちらが正かを確かめていない。**

**⇒ 段 1 で実測すること。★食い違ったら、それが本サブの成果の 1 つである。**

### 4.2 ★★2 番目＝同期の漏れ

**★列挙値を足す作業は、1 か所忘れても動いてしまう。⇒ ラベルが英語だけ抜ける、フィルタに出ない、CSV が受け付けない、といった形で出る。**

**★★`bash scripts/check-enum-sync.sh` を回すこと。⇒ 既定は warning なので、出力を読むこと。★終了コードだけ見ない。**

### 4.3 ★★3 番目＝`B01` を「表示」で閉じること

**§0.3。⇒ 削除日時が画面に出ていることと、並んでいることは別である。**

### 4.4 ★★★4 番目＝3 件を 1 つの完了条件で閉じること

**★`M37-overview` §1.2 が「別々の要求と受入条件は消さない」と定めている。⇒ 完了報告は 3 件を*別々に*判定すること。**

**★★「まとめて実装しました」では、どれが満たされたか分からない。**

---

## 5. テスト

| # | 確かめること | 判定 |
|---|---|---|
| **★★1** | **`B01`＝削除日時の降順であること**（**★実データで。⇒ 2 件以上を別時刻に削除して並びを見る**） | **★出力そのもので判定する** |
| **★★2** | **`B03`＝固定したタグが、絞り込みを変えても残ること** | |
| **★★★3** | **`B03`＝ブラウザストレージを使ったなら `bash scripts/check-browser-storage-keys.sh` が緑** | **★台帳未記載キーの検出** |
| **★★★4** | **`B15`＝新しい値で保存・一覧・フィルタ・詳細・CSV 往復ができること**（**★5 面すべて**） | **★1 面だけの確認は不足である** |
| **★★5** | **`B15`＝【破壊確認】定数の片側だけを消すと `check-enum-sync.sh` が拾うこと** | **★★同期の歯止めが生きていることの確認。⇒ 「回したら緑だった」では足りない** |
| **★6** | **`B15`＝既存データの `hit_type` が 1 行も変わっていないこと** | **★差分 0 の実測** |
| 7 | **`bash scripts/check-enum-sync.sh`**（**★出力を読む**）／ `check-browser-storage-keys.sh` ／ `check-migration-license.sh`（マイグレを足した場合） | |
| 8 | **`go test ./...` / `pnpm test` / `pnpm tsc --noEmit` / `pnpm lint` / `make e2e`（全数）** | |

---

## 6. 完了条件

| # | 条件 |
|---|---|
| **★★1** | **§2.1 の実査 5 件が完了報告にある**（**★1 と 2 は式と件数で**） |
| **★★★2** | **3 件が*別々に*判定されている**〔完了 / 一部完了 / 未着手〕**。⇒ 「既に満たしていた」とするなら証拠が付いている**（`M37-overview` §0.2） |
| 3 | **`B01`＝§5-1 の実測がある** |
| **★4** | **`B03`＝保存先の選定理由がある。⇒ 台帳へ足したなら §5-3 が緑** |
| **★★5** | **`B15`＝§5-4 の 5 面すべてと、§5-5 の破壊確認がある** |
| **★6** | **マイグレ＝`000113` を消費したか / しなかったかが明記されている** |
| **★7** | **説明書の追随**（§2.5） |
| 8 | **§5 の検査がすべて緑**（**★出力で判定する**） |
| **★9** | **`docs/progress/M37-03-completion-report.md` ＋ `progress-log.md` への索引行**（`CLAUDE.md` §8） |
| **★★10** | **設計伝達レポート**（`/design_handover_report`）**。★§4 に `DES-003` §3.2 の実測との食い違いと、`B15` の as-built を書くこと** |

---

## 7. ★開発者への確認事項

| # | 事項 | 状態 |
|---|---|---|
| **1** | **空中ヒットのコードとラベル** | **★★設計卓の案＝コード `air_hit` ／ ラベル「空中ヒット」。⇒ 段 1 の実測で既存値の命名と揃うかを確かめ、揃わないと判断したときは、案を出して止まること** |
| **2** | **`B03` の保存先** | **★★設計卓の見立て＝ブラウザストレージ**（§2.3 の注記）**。⇒ DB を選ぶなら止まって請求する** |

**★上記 2 件とも「進めてよいが、判断が変わったら止まる」形である**（`CLAUDE.md` §9）**。⇒ 着手前の待ちは無い。**

---

## 8. 参照ドキュメント

| ID | パス | 参照する箇所 |
|----|------|------------|
| — | `docs/instructions/M37-overview.md` v1.1.0 | §0.2 ／ §1.2 ／ §4.1 ／ §4.2 |
| DES-003 | `docs/design/03-data-model.md` | **§3.2 `combos.hit_type`**（**★実測と突き合わせる対象**） |
| DES-005 | `docs/design/05-screen-design.md` | §5.4 ／ §5.5 ／ §5.7 ／ §5.15 |
| DES-006 | `docs/design/06-validation.md` | ヒット種別の検証 |
| — | `CLAUDE.md` | **§4**（列挙定数の同期）／ **§10.X**（ブラウザストレージ） |
| — | `web/CLAUDE.md` | **§1**（ストレージ台帳） |
| — | `docs/process/parallel-board.md` | `D-853` ／ `D-293` ／ `CHANGE-151`（`M27-01`） |

---

*以上、指示書 M37-03 **v1.0.0**。* **★★本サブの芯は §2.1-1 である**——**`hit_type` の現在値を実測してから始めること。⇒ `DES-003` の 3 値を出発点にすると、足す位置も同期先も間違える。** **★★★次に重いのは §4.4 である**——**3 件は別々の要求である。⇒ 1 つの完了条件で閉じると、どれが満たされたか分からなくなる。**
