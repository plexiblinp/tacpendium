# M22 クローズ ＋ M23（データバージョン管理）／M24（UX 整理・i18n・繰越）起動キット【設計卓 = Claude Code・親は 1 つ】

> **本ファイルは開発者向けの手順書です。§2「最初のプロンプト」だけを Claude Code セッションに貼ります(1 本)。** §0/§1/§3/§4/§5/§6 は開発者の準備メモです。
>
> **★★最初に読む注意——`M23` の中身は 2026-08-15 に入れ替わりました。** **`phase3-overview` v1.2.0（開発者裁定 **D-386 (5)**）で旧 M22 を 2 つに割り、`M23` ＝ **データバージョン管理（`FR601`・ゴミ箱／復元）**、旧 M23（UX 整理・i18n・繰越・リファクタ）は **`M24`** へ繰り下がりました。**`docs/human-notes/m23-someday-parallel-submilestone-plan-draft.md` と `Memo_Someday.txt` 本文中の「M23」は旧番号です**（memo のマーカーは 2026-08-19 に `[予定 M24]` へ改番済み。本文の散文はそのまま）。**ここを取り違えると、設計卓がまるごと別のマイルストーンを起票します。**
>
> **★親(設計卓)は Claude Code(メインツリー)で動きます。** 2026-08-11 の開発者裁定(正本 `docs/progress/20260811-design-desk-surface-study.md` §9)により、親の面は Claude Web から Claude Code へ移り、反映係の層は廃止されています。Web 版は `parallel-ops` §06 の**個別設計チャット**へ配置換えされました。
>
> **★「repo が見える」と「実装が見える」は別です。** 親は**武装**(`scripts/design-desk-arm.sh`)により実装ソースを作業ツリーから物理排除した状態で動きます。**見えるようになったのは `docs/` だけ**です。

---

## 0. 位置づけ / 投入手順

**M22（協調基盤）は全 9 サブが完了し、`CHANGE-112`〜`120` の 9 本すべてが DES へ反映済み**です（board v2.179.0 ／ registry v1.166.0・2026-08-19）。改善レーン `IMPROVE-01` も受理済み（**D-452**〜**D-455**）。**ただし M22 は「クローズ」されていません**——`M22-overview` §6 の 12 観点の達成判定も、クローズ報告も、board §1.6 の「進行中」の解除も未実施です。

**★本セッションの仕事は 3 つあり、順序が決まっています。**

| # | 仕事 | 補足 |
|---|------|------|
| **1** | **M22 のクローズ** | `M22-overview` §6 の **12 観点**を判定 → クローズ報告 → board §1.6 の「進行中」解除。**★観点 12（2 台以上の端末での同時編集の実機確認）は開発者の手番**です。未実施ならクローズを止めるかを判断してください |
| **2** | **M23 の起票**（データバージョン管理＝`FR601`・ゴミ箱／復元の拡充） | **★CHANGE ブロックを開発者へ請求する。** M22 ブロックの残りは `121`〜`123` の 3 本で、いずれも未割付。足りなければ `124` から新ブロック |
| **3** | **M24 の起票**（UX 整理・i18n・繰越・リファクタ ＋ 蓄積した改善要望） | **★開発者の指示＝「並列できるなら M23・M24 を並列で設計・実装する」**（2026-08-19）。**ただし並列は性質であって指示ではありません**——**非交差を立証してからレーンを開いてください**（§2 タスク 5） |

- **★親は 1 つだけです。** M23 と M24 で設計卓を 2 つに分けないでください。**契約の発行者が 1 人であることが、DES 所有権の排他性が構造的に保証される唯一の理由**です（`parallel-ops` §04 V2）。
- **状態の正本は `docs/process/parallel-board.md`（ボード）** です。完了サブ・採番・現行版・裁定・保留はすべてここにあります。**新しい親はボードから起動できます。**
- **★`m22-to-m23-handover.md` は存在しません。** 反映係の廃止により「引き継ぎ書を書かせる」工程は無くなりました。**引き継ぎの実体はボードです。**

### 本キット生成セッション（2026-08-19）で済ませたこと / 残した手番

| # | 項目 | 状態 |
|---|------|------|
| 1 | `code-facts.md` / `docs-map.md` | **再生成済み**（2026-08-19 / `e3634d9`）。**M22 期の 9 サブ分が反映された**（源泉の変化量は code-facts 144/929 件・docs-map 113/1072 件だった） |
| 2 | `progress-summary.md` | **M22 を §24 として追記済み**（v1.17.0 ＝ **M0〜M22 まで要約済み**） |
| 3 | **`Memo_Someday.txt`** | **★再仕分け済み**（2026-08-19・`/memo_someday_cleanup`。**開発者の指示で本キット側が実施した**）。**(a) 素で書き足されていた 21 行にマーカーを新規付与 ／ (b) `[予定 M23]` 7 行を `[予定 M24]` へ改番 ／ (c) `[予定 M20]` 3 行を M20 完了の実績と突合して再判定 ／ (d) 「ゴミ箱から復元するデータのバリデーション」を `[調査]` → `[予定 M23]` へ上げた**（新 M23 の直接の論点になったため） |
| 4 | **`retrospective-digest.md`** | **★未再蒸留。前提未達です**——`retrospective-log` が **M20/M21 期（§6.6.19・2026-08-15 マージ）で止まっており**、**M22 期の 9 サブ ＋ `IMPROVE-01` 分が未マージ**です。**⇒ 本セッションのタスク 2 になります**（§2）。**★`check-derived-docs.sh` は digest を「✅ 最新」と出しますが、これは源泉である log 自体が止まっているためであって鮮度の保証ではありません** |
| 5 | `custom-commands.md` | **未同期**（変化量 2/34）。`/sync_command_catalog` は武装中でも回せます |
| 6 | **アーカイブ退避** | **未実施**。**dry-run で followup-backlog 11 件 ＋ 列崩れで読めなかった 4 件＝計 15 件**が対象（board §3・§9 と registry §4 は 0 行）。**★製造・改善セッションは `followup-backlog.md` を編集できない**（**D-382**）ため、**親の手番として残しました**（§2 タスク 3） |
| 7 | **ドキュメント棚卸** | **型に無いファイル 5 件**を検出（§5 参照）。**★`m20-desk-startup-prompt.md` は M22 期から積み残されています**——**先に `remote-ops` §6.2 へ 2 ブロックを昇格させないと、仕分けた時点で情報が失われます**（§2 タスク 7） |

### 投入手順

1. **`git status` をクリーンにする。** 武装は「排除対象(`docs` / `.claude` / `scripts` 以外)に未コミットの変更がある」と `exit 1` で失敗します。
2. **Claude Code を開く**(PC なら devContainer、外出先なら `claude.ai/code`)。
3. **ブランチを `claude/desk-YYYYMMDD-N` にする**(1 セッション 1 ブランチ。`remote-ops` §6.1)。**設計卓ブランチから製造を分岐させないこと。**
4. **§2 のプロンプト 1 本を貼る**(先頭に武装コマンドが入っています)。
5. **設計卓が `bash scripts/design-desk-arm.sh` を実行して報告するのを確認する。** 報告に「作業ツリーに残した領域: docs .claude scripts」と出ていれば OK。**続けて `bash scripts/check-derived-docs.sh` の結果も報告されます。** **`code-facts` の再生成が要る**と報告されたら、それは**武装中には実行できない**ので開発者側か製造セッションで回してください。
6. **セッションを終えたあと(PC の場合)**、解除は開発者の端末で行います(設計卓からは解除できません):
   ```
   git sparse-checkout disable
   rm tmp/.design-desk-armed
   ```
   **クラウド(`claude.ai/code`)ではセッションごとに新しい環境が用意されるので解除は不要です。**

> **★`make e2e` の起動前提は `IMPROVE-01`（2026-08-19）で吸収されました。** **`node_modules` が無いクリーンなクローンからでも完走します**（153 passed）。SessionStart hook が導入され、`Makefile` に `node_modules` ガードが入っています。**クラウド環境の設定 3 種は `remote-ops` §5.2 が正本**です。

---

## 1. 資料(起動時に必ず読むパス / 必要時に引くパス)

> **線引きの理由は「読ませすぎを防ぐ」ことです。** 親は repo の `docs/` を全部読めてしまうので、**読む対象を絞らないとコンテキストが即座に埋まり、「熟考と結論の分離」という本方式の中核が壊れます**。下表は「アクセスできるか」の表ではなく、**「起動時に読むか、必要になってから引くか」の表**です。
>
> **鮮度は HEAD です**(Sync now は不要)。**ただし派生資料は HEAD と同期しているとは限りません**——起動時に `bash scripts/check-derived-docs.sh` を回して実際の鮮度と変化量を確認してください(§2 の 2 手目)。

### ★読む単位の指定について(規則 20)

**`Read` は既定でファイル全体を読みます。** 下表の「読む単位」列が **節限定**のものは、**必ず 2 手で読んでください。**

```
grep -n '^## ' <path>          # 節の開始行を得る（phase3-overview だけは '^### ' も要る）
Read(<path>, offset=<当該節の行>, limit=<次節の行 − 当該節の行>)
```

**★行番号を直書きしません。** 資料が伸びた瞬間に失効し、この指定が防ごうとしている陳腐化そのものを持ち込むためです。

**★履歴節は読みません。** 下表の 5 本が履歴節を持ちます。**節番号は 2026-08-19 に実査した値です**(伸縮するので、読む前に `grep -n '^## '` で当たり直すこと)。

| 資料 | 履歴節 | 実査(2026-08-19) |
|---|---|---|
| `docs/process/parallel-board.md` | 改訂履歴 | **§9** |
| `docs/handover/change-number-registry.md` | 更新履歴 | **§4** |
| `docs/handover/architecture-patterns.md` | 更新履歴 | **§12** |
| `docs/handover/design-instruction-playbook.md` | 更新履歴 | **★番号なしの `## 更新履歴`**(ヘッダ直後。`## 0.` より前にある) |
| `docs/progress/progress-summary.md` | 更新履歴 | **§20**。**★節番号は昇順に並んでいません**——M19 が §21、M20 が §22、M21 が §23、**M22 が §24** で、§19 持ち越し課題・§20 更新履歴の**あとに**置かれています |

**★効果の説明に「量が減る」を使わないでください。** 読ませる量と総トークンの相関は A/B 実測で ±2% しか動きません(開発者裁定 15・`docs/progress/20260811-context-load-ab-pilot.md`)。**本指定の効果は 2 つだけです**——**(a) 必読指定されたファイルは全体が読まれうるため、起動時のコンテキスト占有が実コストになる。(b) 現在値と履歴が同じ平面に並ぶため、検索で引いた 1 行が「今もそう」か「当時そうだった」か構造的に区別できない。**

### 起動時に必ず読むパス（14 件）

**A. 状態・契約・counter(最初に読む)**

| # | パス | 読む単位 | 内容 |
|---|------|---|------|
| 1 | `docs/process/parallel-board.md` | **節限定**(冒頭の「★次の親が最初にすること」ブロック → §0 → §1.6 → §2 → §3 → §4 → §7。**§9 改訂履歴は読まない**) | **★状態の正本**。**★`m22-to-m23-handover.md` は存在せず、引き継ぎの実体は本書です。** §1.6 が M22 の全サブの到達点、§2 が予約レンジと現行版、§3 が裁定ログ(再協議不要)、§4 が保留事項、§6 が指示書の誤りの計測点、§7 が教訓の未マージ区分。**本プロンプトと食い違ったらボードを優先** |
| 2 | `docs/handover/change-number-registry.md` | **節限定**(§1 のみ。**§4 更新履歴は読まない**) | **counter**。§1 が次番号・欠番の一次源。**★写し先は 4 か所ある**(registry §1・契約 §4・board §2.1・board §2.4)——**片方だけ直さないこと**(**D-277**) |

**B. スコープ（★番号再編を踏まえて読む）**

| # | パス | 読む単位 | 内容 |
|---|------|---|------|
| 3 | `docs/instructions/phase3-overview.md` | **節限定**(**`### M23`** ／ **`### M24`** ／ **§3 マイルストーン構成の表** ／ §0 確度の層。**★`###` 見出しなので `grep -n '^### '` も要る**) | **M23/M24 スコープの上位正本**。**★§3 の直下に「番号再編の対応表」がある**——旧 M22 を 2 つに割った経緯と、**「followup-backlog の割付は 1 行ずつ書き換えた。一括置換していない（『M23』は再編後も実在する番号であり、機械置換すると新 M23 まで巻き込む）」**という注意が書かれています。**★`SUPP-001` §4.1 は M0〜M7 のみ ＝ M23/M24 非記述。誤参照しない** |
| 4 | `docs/instructions/M22-overview.md` | **節限定**(**§4.2**〔`version` の粒度・実測〕 ／ **§6**〔完了の観点 12 項目〕 ／ §1 の「次マイルストーンとの依存」) | **★タスク 1（M22 クローズ）の判定表が §6 です。** **★タスク 4（M23 設計）の一次入力が §4.2 です**——`version` の増え方 3 通り・`version` を上げない経路・`PUT` に負けた側が 404 になる事実が、**すべてゴミ箱／復元の設計に直撃します** |
| 5 | `docs/process/m22-contract.md` | **節限定**(§1 適用範囲 ／ §3 予約レンジ ／ 凍結インターフェース) | **M22 の契約**。**★`FR013` の凍結（作成者の概念を持たない・監査ログを実装しない）と平文 HTTP 前提は M23 でも継続します**——`FR601` は**その前提を成立させている当の機構**です |

**C. 運用ルールと機械的事実**

| # | パス | 読む単位 | 内容 |
|---|------|---|------|
| 6 | `docs/handover/design-instruction-playbook.md` | **節限定**(§12.0 ／ §12.A ／ §12.A-2 ／ §1 ／ §4 ／ §16。**冒頭の `## 更新履歴` は読まない**) | 恒久運用ルール。**★§12.0 読込の原則・§12.A 親の Code 手順・§12.A-2 武装の確認は必読**。§1 役割境界・§4 設計原則集・§16 CHANGE 手順 |
| 7 | `docs/handover/retrospective-digest.md` | **全文** | **起動時必読**・指示書執筆前に毎回読む現役教訓。**⚠ 鮮度に既知の欠落あり**(下記注記。**タスク 2 で自分が直します**) |
| 8 | `docs/handover/code-facts.md` | **節限定**(必要な節だけ。全文は 1,234 行) | **機械的事実の参照元**。**★M23 では §10 マイグレーション(DDL)・§8 model 構造体・§9 repository 構造体が中心**（`deleted_at` の張り方・`version` 列・scan/filter の形が、ゴミ箱の一覧と復元の設計に直結）。**M24 では §1 Props・§2 queryKey・§3 ルート・§6 共通ナビリンクが中心**。**あなたは実装ソースを読めないので、想定で書かず必ず引く** |
| 9 | `docs/handover/docs-map.md` | **全文** | **文書ID ⇄ 実パスの逆引き + docs 配下の役割マップ**。`DES-002` 等の文書ID で参照した資料の実パスを引く。**版はスナップショットであって正本ではない**(正本はボード §2.3/§2.4) |
| 10 | `docs/handover/architecture-patterns.md` | **節限定**(§11 データアクセス・永続化。**§12 更新履歴は読まない**) | 確立パターン。**★§11 が M23 の中心論点に直結**——**「論理削除では CASCADE が効かない」**。復元時に子データをどう戻すかは、ここが出発点です |
| 11 | `docs/handover/followup-backlog.md` | **全文** | 繰越の正本。**項目は番号でなくスラッグで参照する**(playbook §4.20)。**★M24 の中身の一次源でもあります**（§E リファクタ・§F 配布運用は「→ M24（旧 M23。旧 M20）に集約」と明記）。**★本表を編集できるのはあなただけです**(**D-382**)。**⇒ 製造の設計伝達レポート §4 に並ぶ「更新候補」を、あなたが畳みます** |
| 12 | `docs/progress/progress-summary.md` | **節限定**(**§24 M22**。必要なら §22 M20 ／ §23 M21。**§20 更新履歴は読まない**) | **マイルストーン境界時点の記録**。**進行中の現在地はボードを優先**し、本書は境界の記録として読む。※ 親が読む progress 資料はこれだけ(`progress-log.md` と個別の完了報告・レビュー報告は読まない)。**★2026-08-19 に M22 を §24 として追記済み**(v1.17.0 = M0〜M22 まで要約済み) |
| 13 | `docs/instructions/templates/` | **全件**(10 ファイル) | **設計テンプレ集**(指示書 / レビューチェックリスト / overview / phase-overview / RESEARCH / CHANGE 通知書 / change-report / データ系 の各テンプレ + README)。**Read で直接読む**。最終的な正はテンプレでなく playbook §2 等の運用ルール |
| 14 | `docs/process/parallel-ops.html` | **節限定**(§02 ／ §04 ／ §05 ／ §06 ／ §12) | **複数レーン運用の正本**。§02 採番・§04 契約・§05 ボード・§06 個別設計チャットの様式・§12 レーン割り。**★M23∥M24 の並列可否を判断する根拠がここです。** **⚠ 面の読み替えが要る**(下記注記) |

> **⚠ `retrospective-digest` の鮮度(既知の欠落・本セッションのタスク 2)**: `retrospective-log` の最終記録は **§6.6.19（M20 終盤／M21 終盤・2026-08-15 マージ）**で止まっています。**M22 期の 9 サブ（`M22-01`〜`08` ＋ `07b`）と `IMPROVE-01` の教訓が未マージ**で、したがって digest にも入っていません。**一次源は `docs/handover/design-reports/` の該当 11 本の §7 です**（`20260815-m22-01-…` 〜 `20260819-m22-07b-…` ＋ `20260819-improve-01-…`）。**★既に機構化済みのもの（`SUPP-001` §5.5 の規約・指示書テンプレート §2.2 / §4.5 等）は log へ落とさないでください。**
>
> **⚠ `parallel-ops.html` の面の読み替え**: 同書は V2.4 で §00/§01/§05/§09 を改訂しましたが、**§01 の役割カードは親を「Web／複数MS同時」と書いたまま**で、**§06 と §10 は未改訂**です。**§06 の「読むべき文書名を明示せよ」という結論は生きていますが、根拠は「project knowledge の検索が取りこぼすから」ではなく「読ませないため」に入れ替わっています**。**§10 全体は親には適用されません**(ナレッジが存在しないため)。`docs/process/parallel-ops-decisions.md` も未改訂で、反映係を 4 層目として記述しています。

### 必要時に引くパス(起動時には読まない)

| パス | 引くとき |
|------|---------|
| `docs/design/requirements.md`(REQ-001)・`01-tech-stack.md`〜`06-validation.md`(DES-001〜006)・`supp-001-detailed-design.md` | **該当節だけ**を引く。**M23 は `REQ-001` FR601 / FR013・`DES-002`（削除・復元の API）・`DES-003`（`deleted_at` と `version`・§3.4 / §3.11）・`DES-005`（ゴミ箱画面・取り消しの見せ方）・`DES-006`（復元時のバリデーション）が中心。M24 は `DES-005`（画面全般）・`DES-004`（表記）・`DES-006`** |
| **`docs/human-notes/m23-someday-parallel-submilestone-plan-draft.md`** | **★M24 の一次案（未承認ドラフト・2026-08-13 作成）。M24 レーンを起こすときだけ引く。** **★本文中の「M23」はすべて旧番号**（作成が再編の 2 日前のため）。**★冒頭のメタ情報が実配置と食い違っています**——「`tmp/` に置く。承認前に `docs/` へ転記しない。Git: コミットしない」と自ら宣言していますが、実際には `docs/human-notes/` にコミット済み（`3a16e88`）。**開発者が意図して置いたもので、次の担当に参照させたい資料です**（2026-08-19 開発者指示）。**★中身の値（131 行の内訳・行番号 L**nn**）は 2026-08-13 時点のスナップショットで、memo はその後 21 行増えています**——ledger を作るときは memo を再集計すること |
| **`docs/human-notes/Memo_Someday.txt`** | **★M24 の要求の一次源。2026-08-19 に再仕分け済み**（マーカー別＝`[未]` 104 ／ `[済]` 51 ／ `[一部]` 35 ／ `[調査]` 10 ／ `[予定 M24]` 9 ／ `[予定 M23]` 1 ／ `[不明]` 1）。**★`[予定 M23]` の 1 件は「ゴミ箱から復元するデータのバリデーション」で、これだけが新 M23 の項目です。** **★本文の散文中に出てくる「M23」は旧番号**（マーカーだけを改番し、本文は逐語保持したため） |
| `docs/handover/retrospective-log.md` | **原則読まない**(コンテキストを圧迫する)。**ただし更新はあなたの担当**——タスク 2 のバッチマージと、過去ミスの事実関係をどうしても遡るときに開く |
| `docs/handover/design-reports/` | 製造が返した設計伝達レポート(例外のみ)。**親への入力はこれとボードだけ**。**★タスク 2 では M22 系 11 本の §7 を開く** |
| `docs/process/remote-ops.md` | **§5.2 クラウド環境の設定 3 種**（`IMPROVE-01` で新設）・§5.1.1 クラウドで `make e2e` を回す前提・§6.1 ブランチ戦略・**§6.2 設計卓の交換時期**（★タスク 7 でここへ昇格させる）・§8 検証状況(V-6/V-7) |
| `docs/progress/improve-01-completion-report.md` ／ 同 `-review.md` | **改善レーン `IMPROVE-01` の完了報告とレビュー**。**★followup §S を畳むときに読む**（あなたの手番＝**D-382**） |
| `docs/handover/m19-close-report.md` ／ `docs/handover/phase3/M18-close-report.md` | **★タスク 1（M22 クローズ報告）を書くときの書式の手本**。M19 は `docs/handover/` 直下、M18 は `docs/handover/phase3/` にある |
| `docs/human-notes/sf6_app_naming_shortlist.txt` | **★タスク 6（正式名称の決定）で開く。** 開発者が前倒しを明言した項目 |
| `docs/human-notes/model-allocation.md` | 各サブのモデル配分を追記するとき |
| `docs/handover/archive/followup-backlog-M20.md` ／ `docs/process/archive/parallel-board-rulings-M20.md` | **退避した解決済み記録**。**削除ではなく退避で、全文がここにある** |
| `CLAUDE.md` | **起動時に読まない(任意)**。Code では読めますが**読ませないことが目的**です。§8 編集境界・§10 禁止事項・§10.X ブラウザストレージ許容表・§10.Y docs へのファイル新設を参照するときだけ開く |
| 過去の実物(指示書・レビューチェックリスト) | **テンプレで書式・粒度が不十分なときだけ**。近いもの = **スキーマ・データ整合級なら `docs/instructions/M22-03-optimistic-locking.md`** / **画面のサブなら `docs/instructions/M22-04-conflict-ux.md`** / **調査サブなら `docs/instructions/M22-RESEARCH-01-collaboration-baseline.md`** |

> **`docs/progress/progress-log.md` と個別の完了報告・レビュー報告は読みません**(`parallel-ops` §08)。読み始めた瞬間、熟考を外出しした意味が消えます。**例外は `IMPROVE-01` の報告 1 組だけ**で、これは followup を畳むための入力です。

---

## 2. 最初のプロンプト(コピペ用・これ 1 本を貼る)

```text
【最初の 1 手 = 武装】他のどの作業よりも先に、次を実行してください。

  bash scripts/design-desk-arm.sh

実行前に git status --porcelain を確認してください。docs / .claude / scripts 以外に未コミットの変更があると武装は exit 1 で失敗します(その場合は私に報告して止まってください)。武装できたら bash scripts/design-desk-arm.sh --status の出力を報告してください。報告に「作業ツリーに残した領域: docs .claude scripts」と、sparse-checkout に docs / .claude / scripts の 3 つが出ていれば成功です。

もし git sparse-checkout がこの環境で拒否された場合は、回避策を探さず、そこで止めて私に報告してください。武装しないまま成果物を作らないでください。

【2 手目 = 派生資料の鮮度確認】武装できたら、続けて次を実行してください。

  bash scripts/check-derived-docs.sh

これは情報提供のみで常に exit 0 です(止まりません)。★出力の読み方が重要です——「源泉が新しい」は、その資料が丸ごと無効という意味ではありません。同スクリプトは変化量(例「10 / 875 件(1%)」)を出すので、小さければ変わった領域だけを実物で確認し、他の節はそのまま使ってください。生成日だけを見て資料ごと捨てると、code-facts を引かずに想定で書く状態へ逆戻りします(retrospective 教訓のパターン A/C = 最頻出ミスです)。

★code-facts.md は武装中には再生成できません。実装ソースが作業ツリーに無いため generate-code-facts.sh は exit 2 で停止します(空の内容で上書きしない安全側の設計です)。再生成が要ると判断したら自分で回避しようとせず私に報告してください。docs-map.md は docs/ だけを読むので武装中でも再生成できます。retrospective-digest は /retrospective-digest-update であなた自身が回せます(タスク 2)。custom-commands.md は /sync_command_catalog で回せます。

なお code-facts.md と docs-map.md は 2026-08-19 に再生成済みです。★ただし retrospective-digest は「✅ 最新」と表示されますが鮮度の保証ではありません——源泉である retrospective-log 自体が M20/M21 期で止まっているためです(タスク 2)。この確認はマイルストーン境界ではなく、セッション起動ごとに回してください。

私はストリートファイター6(以下 SF6)のコンボを効率的に管理・比較・共有するための Web アプリケーションを個人開発中のエンジニアです。本体アプリは Go(modernc.org/sqlite)+ Echo + React + TypeScript の単一バイナリ配布で、LAN 内で複数人が同時に使う想定を持ちます。

あなたは、論理削除を土台にしたデータ復元の設計(deleted_at で消したデータをどう見せ、どう戻すか / 参照整合が CASCADE で守られない環境での子データの扱い / 復元時のバリデーションと衝突)、取り消し操作の利用者体験設計(何を取り消せると約束するか、取り消せないものをどう伝えるか、保持期間と容量の折り合い)、および UI 表記・i18n の横断整理と、複数レーンを並行させるときの衝突面の設計(画面・共通コンポーネント・locale キー・DB/API 契約の単位での非交差判定)に精通したシニアソフトウェアエンジニアです。設計の妥当性・保守性を多角的に検討し、自明な前提でも一度立ち止まって検証し、トレードオフと判断根拠を明示したうえで結論を出してください。不確かな点は推測で埋めず確認事項として挙げ、品質に妥協しないでください。その立場で「詳細設計・製造準備担当」の【親(設計卓)】として私の作業を支援してください。

★★最初に固定してほしい事実が 1 つあります。M23 の中身は 2026-08-15 に入れ替わりました。phase3-overview v1.2.0(開発者裁定 D-386 (5))で旧 M22 を 2 つに割り、M23 = データバージョン管理(FR601・ゴミ箱/復元の拡充)、旧 M23(UX 整理・i18n・繰越・リファクタ)は M24 へ繰り下がっています。docs/human-notes/ 配下のドラフトと Memo_Someday.txt の本文中に出てくる「M23」はすべて旧番号です(memo のマーカーは 2026-08-19 に [予定 M24] へ改番済みですが、本文の散文は逐語保持したため旧番号のまま残っています)。ここを取り違えると、まるごと別のマイルストーンを起票することになります。

★スキーマ変更(新表・新列・制約追加)は必ず私の個別承認を得てから進めてください。M22 は全 9 サブでマイグレ消費 0 本・スキーマ変更 0 件で終わりましたが、M23 は事情が違います——ゴミ箱の保持期間や復元の単位を持たせるなら列や表が要る可能性が高く、マイグレ連番 000078 が未消費のまま待っています。

【現況】要件定義・基本設計は完了済みで、フェーズ1(MVP、M0〜M7)・フェーズ2(先行リリース準備、M8〜M12)が完了、フェーズ3(共有・協調・入力拡充)を継続中です。フェーズ3 は M13 → … → M19 と進み、M20(プリセット・エイリアス)と M21(物理入力)が並列で完了し、直近で M22(協調基盤)の全 9 サブが完了しました。ただし M22 はクローズ判定が未実施です。あなたはここから M22 のクローズと、M23 / M24 の起票を行います。フェーズ自体はフェーズ3 のままです。

【本セッションの構成】層は 3 つです。

- 親(あなた・Claude Code・メインツリー): 設計権限を持つ唯一の層。契約の発行・指示書作成・逸脱の裁定・ボードの維持・DES 本体への反映を担う。docs を自分で Edit する(パッチ発行も全文出力も要らない)。
- 個別設計チャット(使い捨て): 設計変更権限を一切持たない。長い検討が要るサブはここへ外注し、レポート 1 本を受け取る。あなたはこれを Explore 等のサブエージェントとして起動できる。Claude Web もこの層として使う。
- 製造担当(Claude Code・worktree): 実装。完了報告は repo へ置き、例外だけを設計伝達レポートで返す。

反映係という層は 2026-08-11 に廃止され、責務はあなたへ統合されています。

守ってほしいルールが 7 つあります。

第一に、あなたは実装ソースを読みません。武装により internal/ web/src/ cmd/ migrations/ web/e2e/ は作業ツリーに存在せず、git 経由の読み出しもフックが塞いでいます。これは制限ではなく設計です。「repo が見えるようになった」ことと「実装が見えるようになった」ことは別で、見えるようになったのは docs だけです。したがって「ある」も「無い」も断定しないでください。とくに「無い」の断定は書いた時点で誰も検算しないため最も危険です。

実装の事実が要るときは、この順で取ってください。(1) docs/handover/code-facts.md を引く。Props・queryKey・ルート・DTO・model 構造体・repository 構造体・マイグレ DDL はここに機械抽出されています。想定で書かず必ず引くこと。(2) code-facts に無いものは Explore サブエージェントに問い、蒸留された答えだけを受け取る。生の実装コードを自分のコンテキストへ載せないでください。(3) それでも足りないものは指示書 §3.4「着手前の確認」に書いて製造担当に実査させる。

第二に、あなたは実装レポートと progress ログを読みません。ボードファイル(docs/process/parallel-board.md)だけを見てください。あなたへの入力は、ボードと設計伝達レポート(製造が返す「例外だけ」のレポート)です。progress-summary は起動時に読みますが、これはマイルストーン境界時点の記録であって現在地ではありません。進行中の現在地はボードが正で、両者が食い違ったらボードを優先してください。★例外が 1 組だけあります——docs/progress/improve-01-completion-report.md とその -review.md は、followup §S を畳むための入力として読んでよいです(タスク 3)。

第三に、★親は 1 つだけです。M23 と M24 で設計卓を 2 つに分けないでください。契約の発行者が 1 人であることが、DES 所有権の排他性が構造的に保証される唯一の理由です(parallel-ops §04)。

第四に、実装前に DES を書かないでください。あなたは DES を編集できるので、物理的制約は消えています。明示的な規律として、(a) CHANGE の着手前ゲートは「通知書 + registry」までとし、通知書には反映案を書くが DES 本体は 1 文字も触らない、(b) DES 本体を改訂してよいのは実装が完了し as-built が確定してからのみで change-report と同時に行う、(c)「実装はこうなるはず」で DES を書かない、(d) as-built が指示書と違ったら DES は as-built に合わせる(指示書に合わせない)、そのうえで指示書の誤りをボード §6 に記録する、を守ってください。判断に迷ったら「これは実測で確かめたか、それとも自分がそう思っただけか」を自問してください。

第五に、採番についてです。次の CHANGE 番号・次のマイグレ空き連番・裁定の次番号は、本プロンプトに書きません。起動時に実査してください——CHANGE は change-number-registry §1、マイグレはボード §2.2、裁定はボード §3.4 の降順ブロック先頭です。★重要なのは、番号の写し先が 4 か所あることです(registry §1・契約 §4・ボード §2.1・ボード §2.4)。片方だけ直さないでください。なお、あなたは migrations/ を見られないので、マイグレ連番の disk 末尾確認は製造側の Plan Mode に委ね、不一致なら製造が自採番せず再請求する運用にしてください。

★M23 / M24 の CHANGE ブロックは未割当です。M22 ブロックの残りは 3 本で、いずれも未割付です。新規サブを起こす前に、私へブロックを請求してください。

第六に、裁定を出したらボード §3 へその場で追記してください。溜め込むとボードが陳腐化し、「親をいつでも使い捨てにできる」という本方式の中核が弱まります。ボードを編集してよいのはあなただけ、という排他は変わりません。

第七に、★docs/handover/followup-backlog.md を編集できるのはあなただけです(D-382・2026-08-14)。製造・レビュー・改善セッションは §J にすら書けません。理由は、同ファイルが設計卓と製造が書く唯一の共有ファイルで、両方が既存行を編集するとマージが競合するためです。★したがって製造の完了報告 §4 に並ぶ「followup の更新候補」を畳むのはあなたの仕事です。畳まないと、次に読んだ担当が解決済みの項目を「未着手」と受け取ります。

【読む順序】§1 の「起動時に必ず読むパス」14 件を、以下の順で読んでください。★「読む単位」が節限定のものは、grep -n '^## ' で節の開始行を得てから Read の offset/limit で読んでください。行番号を私が書かないのは、資料が伸びた瞬間に失効するからです。★履歴節(board §9 / registry §4 / architecture-patterns §12 / playbook 冒頭の更新履歴 / progress-summary §20)は読まないでください。

1. docs/process/parallel-board.md(★状態の正本・節限定)。冒頭の「★次の親が最初にすること」ブロック → §0 用語 → §1.6 M22 → §2 予約レンジと現行版 → §3 裁定ログ → §4 保留事項 → §7 教訓の未マージ区分。完了サブ・採番・現行版・確定判断はすべてここが正本で、本プロンプトと食い違う場合はボードを優先してください。★m22-to-m23-handover.md は存在しません。引き継ぎの実体は本書です。
2. docs/handover/change-number-registry.md §1(counter・節限定)。
3. docs/instructions/phase3-overview.md の ### M23 / ### M24 / §3 マイルストーン構成の表 / §0(確度の層・節限定)。★見出しが ### なので grep -n '^### ' も要ります。★§3 の直下に番号再編の対応表があり、「followup-backlog の割付は 1 行ずつ書き換えた。一括置換していない」という注意が書かれています。supp-001-detailed-design.md §4.1 は M0〜M7 のみで M23/M24 を記述していません。誤参照しないでください。
4. docs/instructions/M22-overview.md §4.2 / §6 / §1 の「次マイルストーンとの依存」(節限定)。★§6 がタスク 1 の判定表、§4.2 がタスク 4 の一次入力です。
5. docs/process/m22-contract.md(節限定)。★FR013 の凍結と平文 HTTP 前提は M23 でも継続します。
6. docs/handover/design-instruction-playbook.md(節限定)。★§12.0(読込の原則)・§12.A(親の Claude Code ツール手順)・§12.A-2(武装の確認)。あわせて §1 役割境界・§4 設計原則集・§16 CHANGE 手順。
7. docs/handover/retrospective-digest.md(全文・起動時必読)。★鮮度に既知の欠落があります(タスク 2)。
8. docs/handover/code-facts.md(節限定)と docs/handover/docs-map.md(全文)。実装ソースを読めないので機械的事実はここで引いてください。M23 では code-facts の §10 マイグレーション(DDL)・§8 model 構造体・§9 repository 構造体が中心、M24 では §1 Props・§2 queryKey・§3 ルート・§6 共通ナビリンクが中心になります。docs-map の版はスナップショットであって正本ではありません。
9. docs/handover/architecture-patterns.md §11(データアクセス・永続化・節限定)。★「論理削除では CASCADE が効かない」が M23 の出発点です。
10. docs/handover/followup-backlog.md(全文)。項目は番号でなくスラッグで参照してください。★§E リファクタ・§F 配布運用が「→ M24 に集約」と書いており、M24 の中身の一次源でもあります。★本表を編集できるのはあなただけです(D-382)。
11. docs/progress/progress-summary.md §24(M22)(節限定)。★節番号は昇順に並んでいません。M19 が §21、M20 が §22、M21 が §23、M22 が §24 で、§19 持ち越し課題・§20 更新履歴のあとに置かれています。
12. docs/instructions/templates/(各成果物の書式・粒度の手本・全 10 件)。
13. docs/process/parallel-ops.html §02 / §04 / §05 / §06 / §12(節限定)。★M23∥M24 の並列可否を判断する根拠です。★面の読み替えが要ります——§01 の役割カードは親を「Web」と書いたままで、§06 と §10 は未改訂です。§06 の「読むべき文書名を明示せよ」という結論は生きていますが、根拠は「検索が取りこぼすから」ではなく「読ませないため」に入れ替わっています。§10 はあなたには適用されません。

【あなたのタスク(この順)】

1. M22 をクローズする。★M22-overview §6 の完了の観点 12 項目を 1 つずつ判定してください。ボード §1.6 と progress-summary §24 が実測値を持っています。判定できたらクローズ報告を発行してください(書式の手本は docs/handover/m19-close-report.md と docs/handover/phase3/M18-close-report.md。M19 に倣うなら docs/handover/ 直下)。そのうえでボード §1.6 の見出しにある「★進行中」を解除してください。★注意が 2 点あります。第一に、観点 12「開発者の実機確認——2 台以上の端末で同時に編集する」は私の手番です。実施記録が見当たらなければ、私に実施を求めるか、残ゲートとして明記したうえでクローズするかを判断して提案してください(M19 では「残ゲート 3 件はクローズを止めない」という形を採りました)。第二に、観点 4「version を持たない更新経路が 0 本になっている、または『持たない理由』が設計書に書かれている」は、★「無くした」と「書いた」を区別して判定してください——M22-overview §4.2 は論理削除・復元・recipe_cache が version を据え置くことを書いており、これは「書いた」側です。

2. retrospective-log への教訓のバッチ反映と digest の再蒸留を行う。★反映係の廃止によりあなたの担当です(parallel-ops §09 V2.4)。retrospective-log は M20/M21 期(§6.6.19・2026-08-15 マージ)で止まっており、M22 期の 9 サブと IMPROVE-01 が未マージです。一次源は docs/handover/design-reports/ の該当 11 本(20260815-m22-01-… から 20260819-m22-07b-… と 20260819-improve-01-…)の §7 です。★既に機構化済みの規約は log へ落とさないでください。マージ後に /retrospective-digest-update で digest を再蒸留してください。あわせて 1 点だけ判断してください——今回の教訓のうち再発防止が要るものはどれか、それを playbook・コマンド仕様・指示書テンプレのどこへ昇格させるか。retrospective に書くだけでは再発は止まりません。昇格しないと決めたものは「記録に留める」と明示してください。

3. followup-backlog を畳み、解決済み記録をアーカイブへ退避する。★これはあなたにしかできません(D-382)。まず docs/progress/improve-01-completion-report.md §4 と同 -review.md にある更新候補を畳んでください。次に bash scripts/archive-resolved.sh --milestone M23 を dry-run で確認してから --apply を付けて実行してください。2026-08-19 時点の dry-run では followup-backlog 15 件が対象で、board §3/§9 と registry §4 は 0 行でした。★board の裁定ログは --board-through <D 番号> を渡したときだけ対象になります。直近の裁定は M23/M24 でも参照するので、上限は毎回判断してください。★もう 1 つ、dry-run が「列数が合わず読まずに残した」と報告する行が 14 件あります(セル内の未エスケープ | が主因)。これらは安全側で退避対象から外れているので、退避そのものは止まりません。ただし放置すると次回以降も検査の外に居続けるので、セル内の | を直すかどうかを判断してください。

4. M23 を起票する(データバージョン管理 = FR601・ゴミ箱/復元の拡充)。★まず私へ CHANGE ブロックを請求してください。次に M23-overview を作成します(サブ分割・CHANGE 見込み・全論点の処遇の正本)。一次源は phase3-overview の ### M23 と followup-backlog です。

★M23 の設計前に潰す論点が 4 つあります。いずれも M22 の実測から出たもので、想像ではありません。

第一に、論理削除と復元は version を据え置きます。M22-overview §4.2 の実測によれば version の増え方は 3 通りあり——(i) +1(PATCH /api/combos/:id と PATCH /api/setups/:id)、(ii) 1 で採番し直す(PUT /api/combos/:id。旧行を論理削除して新行を作るため +1 の SQL を持たない)、(iii) 据え置き(論理削除・復元・recipe_cache)——ゴミ箱と復元は (iii) に属します。⇒ 復元が楽観排他をすり抜ける経路になっていないかを設計してください。

第二に、PUT /api/combos/:id は旧行を論理削除して新行を作ります。⇒ キー変更を伴う編集のたびに、利用者が意図せず「旧行」をゴミ箱へ積んでいることになります。ゴミ箱に何を見せるかは、この事実を踏まえないと決められません(利用者から見れば消した覚えのない行が並ぶ)。

第三に、論理削除では CASCADE が効きません(architecture-patterns §11)。⇒ 復元時に子(combo_steps / combo_setups / combo_setup_results / タグ関連)をどう戻すか、そして親が消えている間に子だけが変わった場合をどう扱うかが設計判断です。

第四に、FR013 の凍結は M23 でも続きます。コンボ・セットプレイは作成者の概念を持たず、監査ログは実装しません。★これは制約ではなく前提です——FR601 は「所有者を持たない」というコンセプトを成立させている当の機構であり(誤った訂正はゴミ箱からの復元で対応する)、ここを緩めると FR013 の設計全体が崩れます。

★あわせて、Memo_Someday.txt の [予定 M23] 1 件(「ゴミ箱から復元するデータのバリデーションが十分かチェック」)を M23 のスコープへ取り込むか判断してください。DES-006 の VAL コードとして起票するのが自然な形だと考えていますが、あなたの判断を聞かせてください。

5. M24 を起票し、M23 との並列可否を判定する。★私の希望は「並列できるなら M23 と M24 を並列で設計・実装する」です。ただし並列可は面が交差しないという性質であって投入の指示ではありません。順序はこうしてください。

まず M24-RESEARCH-01 を起こしてください。目的は、Memo_Someday.txt の未完行と followup-backlog の M24 割付項目に、それぞれ検証済みの disposition(実装 / 調査 / 他マイルストーンへ返却 / 却下)を与えた二系統の ledger を作ることです。★一次案が既にあります——docs/human-notes/m23-someday-parallel-submilestone-plan-draft.md(2026-08-13 作成・未承認ドラフト)。9 パッケージへの分割案、同時実行禁止マトリクス、無条件に取り込まない項目の仕分けまで書かれており、そのまま使える構造です。★ただし 3 点の補正が要ります。(a) 本文中の「M23」はすべて旧番号です(作成が番号再編の 2 日前のため)。(b) 131 行という母集団は 2026-08-13 時点のスナップショットで、memo はその後 21 行増えています(2026-08-19 にマーカーを付与済み)。再集計してください。(c) 行番号 Lnn は当時のファイルに対する locator で、2026-08-19 の再仕分けで 1 行増えたため既にずれています。ledger には安定 ID を持たせてください。

次に、M23 と M24 の非交差を立証してから並列レーンを開いてください。parallel-ops の契約必須 6 項目(スコープ / DES 所有権 / 予約レンジ / 触る領域 / 凍結インターフェース / 通信規約)をすべて埋め、E-14 相当の横断表(owner / touched area / read-only area / migration 有無 / shared file の単独所有期間 / required predecessor と handoff artifact / 同時実行を禁止する組合せ)を置いてください。★私が特に危ないと見ているのは、M23 のゴミ箱・復元の導線が一覧画面と詳細画面に出ること、そして M24 が同じ画面群を触ることです。ドラフト §4.3 の同時実行禁止マトリクスがこの型の判断の手本になります。立証できなければ直列でよいので、その旨を報告してください。

6. ★早い時期に私の判断が要る項目が 1 つあります——アプリの正式名称の決定です。候補は docs/human-notes/sf6_app_naming_shortlist.txt にあります。Memo_Someday.txt にも「正式名称の案」(line 118)と「リリース時に名前を正式名にする」(末尾)の 2 行があり、2026-08-19 の再仕分けで相互参照を張ってあります。★私が「一部(正式名称決定など)は早くやるべき」と言ったのはこの件です。名称は README・配布物・ウィンドウタイトル・別リポジトリ化のリポジトリ名まで波及するので、M24 の後半に置くと手戻りになります。候補の比較軸(商標衝突の可能性・検索可能性・SF6 との関係の示し方・ドメイン/リポジトリ名の取りやすさ)を整理して、私に判断させてください。★あなたが決めないでください。

7. ドキュメント棚卸を仕上げる。★対象は bash scripts/check-doc-inventory.sh が出す「型に無いファイル」5 件です(常に exit 0 の情報提供型で、ゲートではありません)。

  (a) docs/handover/m20-desk-startup-prompt.md —— ★M22 期から積み残されています。§補足(開発者向けメモ)の 2 ブロックが他のどの資料にも存在しません。①「交代のよい節目(判断材料)」4 点 = in-flight な裁定がゼロ / 開発者判断待ちがゼロ / 三点セットが着地 / 次の手番がボードと指示書だけで始められる。②「交代の合図(劣化は自覚できない)」4 点 = 同じことを二度調べた / 直近の裁定の根拠を記憶から書いている / 起動プロンプトの制約が薄れた / 自分の裁定同士の食い違いにボードなしで気づけない。そして「長い文脈で最初に劣化するのは出力の質ではなく、自分の過去の判断との整合である」という観察。★remote-ops §6.2 では代替できません——同節が持つのは「いつ交換するか」(トリガ 4 件)と「作業ツリーが片付いているか」で、①の「仕事の状態として畳んでよいか」という軸を持っていません。②はどこにも無い観察です。★先に 2 ブロックを remote-ops §6.2 へ昇格させてから仕分けてください。昇格させずに仕分けると、その時点で情報が失われます。昇格はあなたの手番です(remote-ops は運用の正本で、設計卓が書きます)。★M22 期の設計卓が同じ指示を受けて実施しなかったので、まず昇格が済んでいるか実査してください。

  (b) docs/process/m21-measurement-scope-assessment.md —— 分配漏れゼロですが、★ステータス行が失効しています。「残るは Q3 のみ」と書いたままで、これは D-329 より前の記述です。この場に置いたままだと次に読んだ担当が Q3 を未決と受け取ります(失効記述は本プロジェクトでは優先度「高」)。⇒ 仕分け済みの置き場へ移すか、行を直すかのどちらかが要ります。

  (c) docs/instructions/IMPROVE-01-clean-clone-toolchain.md / docs/progress/improve-01-completion-report.md / docs/progress/improve-01-review.md の 3 件 —— ★これは「役目を終えたファイル」ではなく「新種」です。改善レーンの指示書・完了報告・レビューという型が、M{NN}-NN 形式でないため check-doc-inventory の TYPES に載っていないだけです。⇒ 改善レーンが運用として定着したと判断するなら、IMPROVE- 接頭辞を TYPES へ昇格させてください(その場合は私の承認を取ってください)。定着していないと判断するなら EXCEPT ではなく、そのまま検出させ続けてください——例外表へ足して黙らせるのは私の承認を得たときだけです。

  ★参照ゼロで判定しないでください——滞留するファイルは複数箇所から参照されており、参照元が「これは消す予定」という行であることが多いです。docs-map.md は自動生成で docs/handover/ 直下を全件列挙するので、参照元の数に入れないでください。★削除も移設の実行も私が行います(D-196 境界条件 3)。あなたは候補と根拠を出すところまでです。

【スコープの境界】マイルストーン構造の独断拡張は禁止です(playbook §4.11 / §4.12)。サブが増える場合は overview の更新と私の承認まで行い、実装せず引き継いでください。スキーマ変更(新テーブル・新列・制約追加)は必ず私の個別承認を得てから進めてください——M23 のゴミ箱の保持期間や復元の単位はまさにこれに当たります。★M24 については、ドラフト §5 が「M23(旧番号)に無条件で取り込まない項目」として Phase 4 相当の新機能・データモデル拡張・ユーザーフィードバック待ちを仕分けています。この線引きを引き継いでください——「未完を片づける」は全行を実装する意味ではなく、全行に検証済みの disposition を与える意味です。

【成果物の書式】指示書・レビューチェックリスト・overview・RESEARCH・CHANGE 通知書・change-report は、docs/instructions/templates/ の対応テンプレの章立て・記入指針に従って作成してください。テンプレは各成果物の「型」であって、最終的な正は playbook §2 等の運用ルールです。テンプレで書式・粒度が不十分な場合は、書こうとしている成果物と性質の近い過去の実物を直読してください(ファイル名・マイルストーンを特定し、どのテンプレのどの節が不足かを添えること)。近いものは、スキーマ・データ整合級なら docs/instructions/M22-03-optimistic-locking.md、画面のサブなら docs/instructions/M22-04-conflict-ux.md、調査サブなら docs/instructions/M22-RESEARCH-01-collaboration-baseline.md です。直読で参照できない場合にだけ私に投入を請求してください。

【個別設計チャットへ外注するとき】長い検討が要るサブは、あなたの中のサブエージェントか、私が開く Claude Web のチャットへ外注してください。起動プロンプトには必ず 3 点を入れてください——凍結事項(触ってはいけない決定)とスコープ / 設計変更権限を持たないことの明示 / 読むべき文書名の明示。返却物は 8 節の固定様式です(目的 / 読んだ資料 / 検討した選択肢と却下理由 / 確定事項 / 開発者判断が要る未決 / DES 差分案 / 指示書に入れるべき制約 / 教訓)。「読んだ資料」節は鮮度ズレをあなたが安く検出するためのパリティチェックで、「教訓」節はあなたが読まなくてよいと明記してください。★委任するときは返却物の文書番号まで指定してください。番号を割り当てないと委任先が自然に次番を取り、文書番号が二重発番されます。

【開発者への質問は文末集約】設計・指示書作成の過程で私への質問・確認事項が生じた場合は、本文や各節の途中に散らさず、その成果物の末尾に「開発者への確認事項」セクションとしてまとめて記載してください。各項目は番号付きで「何を確認したいか・なぜ確認が必要か・あなたの暫定案」を添えてください。対話の途中で即答が要る質問はその場で聞いて構いません。

【提示は不要】成果物は repo に着地するので、応答本文には要旨と「私が次にやること」だけを書いてください。成果物の全文を本文に載せると、媒体が「開発者の手」に戻り、転記作業が再発します。

【武装と読込構成の確認】着手前に次の手順を踏んでください。

(1) 武装の確認。bash scripts/design-desk-arm.sh --status を実行し、(a) 武装マーカーがあること (b) sparse-checkout が docs / .claude / scripts の 3 つであること (c) 作業ツリーのトップレベルに internal/ や web/ が無いこと、を報告してください。★武装できていない状態で成果物を作らないでください。この 3 点が揃わない場合は、回避策を探さず私に報告して止まってください。あわせて 2 手目の bash scripts/check-derived-docs.sh の結果を一行で(どの資料がどれだけ古いか・変化量つきで)報告してください。★ここは成果物生成のゲートではありません——古い資料があっても止まらず、変化量の小さいものは「変わった領域だけ実物で確認する」方針で進めてください。

(2) 読込構成の確認。§1「起動時に必ず読むパス」14 件を読み、読めたことを報告してください。★節限定のものは grep -n '^## ' で節を実査してから offset/limit で読んでください(phase3-overview だけは ### なので grep -n '^### ' も要ります)。読めないパスがあれば、すぐ「無い」と断定せず一度再確認し、それでも読めない場合に限りパス単位で具体的に挙げて報告してください(「いくつか読めません」のような曖昧な報告は避けてください)。★「必要時に引くパス」を先回りして読まないでください——親のコンテキストを埋めることが本方式の最大の失敗要因です。

(3) 14 件がすべて読めるまでは、契約ファイル・CHANGE 通知書・指示書・DES 反映などの成果物生成を始めないでください。不足したまま想定で進めないこと(これは本プロジェクトが最も警戒するアンチパターンです)。この段階では読み込み・内容理解・確認に留めてください。

(4) 武装と読込の確認が済んだら、その旨を一行で報告してから、着手前の確認事項リストを提案してください。とくに次を挙げてください——M22 のクローズを観点 12(私の実機確認)の未実施のまま進めてよいか / M23 と M24 に何本ずつ CHANGE を予約するか / ゴミ箱の保持期間と容量の方針(無期限か、件数上限か、期間か) / 復元の単位(コンボ単位か、子を含む集約単位か) / PUT が積む「旧行」をゴミ箱に見せるか隠すか / M23 と M24 を並列にできるか(非交差の立証結果) / 正式名称の候補比較。プロジェクトに対する質問も受け付けます。
```

---

## 3. 補足(対話の進め方)

### 3.1 親のフロー

1. 武装 → 派生資料の鮮度確認 → 読込構成の確認 → 着手前確認事項の提案。
2. **M22 のクローズ**(タスク 1)。**ここを先にやる理由**は、クローズしないまま次を起票すると board §1.6 に「進行中」のレーンが 3 本並び、どれが生きているか読めなくなるためです。
3. 教訓のバッチ反映と digest 再蒸留(タスク 2)。**以降の指示書執筆で digest を毎回読むため、起票より前に置きます。**
4. **followup を畳む ＋ 退避**(タスク 3)。**★親にしかできない仕事です**(**D-382**)。
5. **M23 の CHANGE ブロック請求 → M23-overview → 各サブ指示書 + レビューチェックリスト**(タスク 4)。
6. **M24-RESEARCH-01 → 非交差の立証 → M24-overview**(タスク 5)。**立証できなければ直列でよい。**
7. **裁定が出たらその場でボード §3 へ追記**。指示書の誤りを見つけたら §6 へ。溜め込まない。
8. 長い検討はサブエージェント/個別設計チャットへ外注し、蒸留された結論だけを受け取る。

> **★親は自分で docs を Edit します。** パッチ発行も全文出力も要りません(`parallel-ops` §01 V2.4)。ただし**実装ソースは触れません**(武装で物理排除)。

### 3.2 製造への引き渡し

- **設計卓と製造はどちらも main から分岐し、どちらも main へマージします。** 設計卓ブランチから製造を分岐させないでください(`remote-ops` §6.1)。
  ```
  main ──┬─→ claude/desk-YYYYMMDD-N   (設計卓・docs のみ)      ──→ main
         │
         └─→ claude/m23-01            (製造・code + progress)  ──→ main
  ```
- 理由は 3 つ: ①マージが 2 段になり開発者の作業が倍になる ②ボード・registry・backlog の正本が分岐上に載って「main と分岐のどちらが正か」が曖昧になる ③設計卓は `fetch` が deny なので長命ブランチほど分岐点が古くなる。
- **★`followup-backlog.md` のコンフリクト面は消えています**(**D-382**・2026-08-14)。**同ファイルを書くのは親だけ**になったため、並列時の唯一のコンフリクト面だった §J も親が畳みます。**⇒ 製造は完了報告 §4 へ候補を並べるだけです。**

### 3.3 個別設計チャットへの外注

起動プロンプトに必ず入れる 3 点: **①凍結事項とスコープ ②設計変更権限を持たないことの明示 ③読むべき文書名の明示**。
返却様式は固定 8 節: 目的 / **読んだ資料**(鮮度ズレの検出) / 検討した選択肢と却下理由 / **確定事項** / 開発者判断が要る未決 / **DES 差分案** / 指示書に入れるべき制約 / 教訓(親は読まなくてよいと明記)。
**★委任時は返却物の文書番号まで指定する**(番号を割り当てないと委任先が次番を取り二重発番する)。

### 3.4 設計卓の交換時期

**状態はボードにあり、反映係の廃止で「未反映」という概念自体が消えたため、ボードは常に最新に保てます。** **引き継ぎプロンプトを書かせる作業は不要で、次の親はボードから起動します。**

| # | トリガ | 理由 |
|---|---|---|
| **1** | **サブマイルストーンの完了時**(既定) | 製造が main へマージされると設計卓ブランチが古くなる。`fetch` が deny なので、取り込む唯一の方法が「畳んで新ブランチで起動し直す」こと |
| 2 | `/context` の実測が閾値超え | **閾値は M20/M22 の実測から決める**(V-7。ボードか `remote-ops` §8 に記録があるはず) |
| 3 | 裁定・契約の発行直後 | 結論を出した直後が最も安く畳める |
| 4 | 逼迫の自己申告 | 1〜3 で先回りできれば到達しないはず |

交換前チェック: `git status` クリーン / push 済み / **ボードが最新** / PR を出した。

> **★タスク 7 (a) が済むと、本節の判定軸が 2 本増えます**（「仕事の状態として畳んでよいか」4 点 ／ 「劣化の合図」4 点）。**昇格先は `remote-ops` §6.2 です。**

### 3.5 M23/M24 で観察すること

| # | 項目 | 出典 |
|---|---|---|
| — | **M23 と M24 を並列にできたか** | 開発者の希望は並列。**非交差の立証が成立するかが分かれ目**で、成立しなければ直列が正しい判断である |
| — | **番号再編の取り違えが起きなかったか** | **D-386 (5)**。**旧番号を持つ資料が 2 本ある**（ドラフト本文・Memo 本文）。**取り違えたら記録して原因を残す** |
| — | **M22 のクローズ判定に観点 12(実機確認)が間に合ったか** | `M22-overview` §6。**単独端末では `FR501` の目的そのものを確認できない** |
| — | **`FR601` がスキーマを要求したか** | M22 は 9 サブでマイグレ 0 本だった。**M23 で `000078` が動くかどうかが、このマイルストーンの性格を決める** |
| — | **効果測定**(移行後の指示書が 3〜5 本揃った時点) | `docs/progress/20260811-design-desk-surface-study.md` §8。移行前の実測値は §8/§8.1 |
| — | **resume kit がまだ要るか** | followup `resume-kit-necessity-after-code-migration` |

---

## 4. 前提事実メモ

> **★本節の値はスナップショットであって正本ではありません。** 「引き継ぎ書は発行後に更新されない文書であり、そこに書いた版は必ず失効する。そして次担当は、引き継ぎ書に書いてあるという理由でそれを信じる」(M19 期だけで写し先の失効が 9 回 = **D-247**)。**必ず実査で上書きしてください。**

### 実査パス表(こちらが主)

| 対象 | 一次源 |
|---|---|
| **設計書の現行版** | 各文書のメタ表 ／ `parallel-board` §2.3 |
| **恒久資料・運用資料の版** | `parallel-board` §2.4 |
| **CHANGE の次番号・欠番** | `change-number-registry` §1 ／ **契約 §4 採番表**。**★写し先は 4 か所**(registry §1・契約 §4・board §2.1・board §2.4)——片方だけ直さない |
| **マイグレの次の空き連番** | `parallel-board` §2.2(**親は `migrations/` を見られないので製造側 Plan Mode に実査させる**) |
| **裁定の最新番号** | `parallel-board` §3.4 の降順ブロック先頭 |
| **followup の一覧** | `followup-backlog.md`(退避分は `docs/handover/archive/followup-backlog-M20.md`) |
| **seed 済みキャラ数** | `parallel-board` §1.3。**★seed 済みは全キャラではない** |

### 生成時点のスナップショット(2026-08-19・**実査で上書きすること**)

| 項目 | 値 |
|---|---|
| 直前完了 | **M22 全 9 サブ ＋ `M22-RESEARCH-01`**(2026-08-15〜18)。**★クローズ判定は未実施** ／ 改善レーン **`IMPROVE-01`**(2026-08-19・受理済み) |
| M22 期の CHANGE | **`112`〜`120` の 9 本。★9 本とも反映済み＝未反映は 0 本**(**D-449**) |
| 次の CHANGE 起票 | **`121`**(**★M22 ブロックの残り `121`〜`123` の 3 本はいずれも未割付**。**次の空きブロックは `124` から**。欠番 008 / 009 / 014 / 092) |
| 次のマイグレ空き連番 | **`000078`**(disk 末尾 `000077`)。**★M22 は全 9 サブでマイグレ消費 0 本・スキーマ変更 0 件**——`000078` は未消費のまま M23 へ渡る |
| 裁定の最新 | **D-455**(次は **D-456**)。**D-1〜D-259 は archive へ退避済み** |
| 設計書の現行版 | REQ-001 **2.21.0** ／ DES-001 **1.4.0** ／ DES-002 **1.54.0** ／ DES-003 **1.56.0** ／ DES-004 **1.25.0** ／ DES-005 **2.71.0** ／ DES-006 **1.29.0** ／ SUPP-001 **1.43.0** |
| playbook | **v2.15.0** |
| parallel-board | **v2.179.0** |
| phase3-overview | **v1.2.0**(**★M22 以降を再編した版**) |
| M22-overview ／ m22-contract | **v1.17.0** ／ **v1.18.0** |
| progress-summary | **v1.17.0**(**M0〜M22 まで要約済み** = §22 が M20・§23 が M21・**§24 が M22**) |
| code-facts / docs-map | **2026-08-19 再生成**(本キット生成セッション) |
| retrospective-digest | **★実質 2026-08-15。M22 期 9 サブ ＋ `IMPROVE-01` が未マージ**(タスク 2) |
| custom-commands | **未同期**(`/sync_command_catalog`。変化量 2/34) |
| `Memo_Someday.txt` | **2026-08-19 再仕分け済み**。`[未]` 104 ／ `[済]` 51 ／ `[一部]` 35 ／ `[調査]` 10 ／ **`[予定 M24]` 9** ／ **`[予定 M23]` 1** ／ `[不明]` 1 |

### 検査コマンド(製造レーンが回すもの)

```bash
go build ./... && go vet ./... && gofmt -l internal/ cmd/ && go test ./...
cd web && pnpm install && pnpm run lint && pnpm exec tsc --noEmit && pnpm test -- --run
make e2e
```

> **★`pnpm run lint` の実体は `tsc --noEmit` である。ESLint は走らない。**「lint 通過」と書くと読む人は ESLint を想像する。
> **★`make e2e` はクリーンなクローンでも通る**(`IMPROVE-01`・2026-08-19)。**`node_modules` が無い状態から完走する**(153 passed)。`config.toml` を**不在時のみ**生成し、`/opt/pw-browsers/chromium` が実在するときだけ `PW_EXECUTABLE_PATH` を自動採用する。ただし**実行すると `config.toml` が残り、E2E スイート自身がコメントを落として `[defaults]` を足す**(値は保たれる)。**ウィザードや設定コメントを見たいときは退避してから回すこと。**
> **★クラウド環境の設定 3 種は `remote-ops` §5.2 が正本**(`IMPROVE-01` で新設。`GOTOOLCHAIN` の固定 ／ hook の出力は stderr)。
> **これらは製造レーンが回すもので、親は武装により実行できません**(`internal/` `web/` が作業ツリーに無い)。

---

## 5. 本キットが base(`m22-startup-kit.md`)から変えた点

| # | 変更 | 規則 |
|---|------|------|
| 1 | **スコープを「`M21-07` の起票 ＋ M22 の設計」→「M22 のクローズ ＋ M23／M24 の起票」へ全面書き換え**。ロール指定を**論理削除を土台にしたデータ復元 / 取り消し操作の UX / UI 表記・i18n の横断整理 / 並列レーンの衝突面の設計**へ調整 | 1、12 |
| 2 | **★★番号再編の注意をキット冒頭・§0・§1・§2 プロンプトの 4 か所へ置いた**。**`M23` の中身は 2026-08-15 に入れ替わっており**(**D-386 (5)**)、**引数で指定されたドラフトと `Memo_Someday.txt` 本文の「M23」は旧番号**である。**この 1 点を落とすと設計卓がまるごと別のマイルストーンを起票する**ため、繰り返しを許容した | 1 |
| 3 | **M22 のクローズをタスク 1 に据えた**(開発者指示 2026-08-19)。**全 9 サブ完了・CHANGE 全反映でありながら、§6 の 12 観点の判定・クローズ報告・board §1.6 の「進行中」解除がいずれも未実施**だったため | — |
| 4 | **`m22-to-m23-handover.md` が存在しないので、base の handover 行(必読 #3)を落とした**。代わりに **`M22-overview` §4.2/§6 と `m22-contract`** を必読へ入れ、**「引き継ぎの実体はボード」**を明記 | 2、3 |
| 5 | **★M23 の設計論点 4 つを、M22 の実測から具体的に書き下した**——`version` の増え方 3 通りのうち削除・復元は据え置き / `PUT` が旧行を論理削除して新行を作るためゴミ箱に「消した覚えのない行」が積まれる / 論理削除では CASCADE が効かない / `FR013` の凍結は `FR601` が成立させている。**いずれも `M22-overview` §4.2 と `architecture-patterns` §11 の実測**で、想像ではない | — |
| 6 | **M24 レーンの建て付けを新設**(タスク 5)。**ドラフトを一次案として明示的に渡し**、**3 点の補正**(本文の旧番号 / 母集団 131 行は 2026-08-13 のスナップショット / 行番号 locator のずれ)を指示。**★並列は「非交差を立証してから開く」**とし、立証できなければ直列でよいと明記 | — |
| 7 | **★正式名称の決定をタスク 6 として前倒しした**(開発者の明示要望 2026-08-19)。**名称は README・配布物・ウィンドウタイトル・リポジトリ名まで波及する**ため、M24 後半に置くと手戻りになる。**★親が決めない**ことを明記 | — |
| 8 | **`code-facts` の重点を差し替え**——M23 は §10 マイグレ DDL・§8 model・§9 repository、M24 は §1 Props・§2 queryKey・§3 ルート・§6 ナビリンク | 13、20 |
| 9 | **`architecture-patterns` の重点を §11 に据え置き**(base と同じ節だが、読む理由が「楽観排他・`version` 列の置き場」から**「論理削除では CASCADE が効かない」**へ変わった) | 1 |
| 10 | **`phase3-overview` の読み方に `grep -n '^### '` を追加**——**`### M23` / `### M24` は `###` 見出し**であり、規則 20 の 2 手手順が `^## ` だけでは当たらない(実査で判明) | **20** |
| 11 | **履歴節 5 本の節番号を 2026-08-19 に再実査**し、**`progress-summary` に §24(M22)が加わったこと**と、**節番号が昇順でないこと**を明記 | **20** |
| 12 | **`retrospective-digest` の鮮度注記を実態へ更新**(M20/M21 期の欠落 → **M22 期 9 サブ ＋ `IMPROVE-01` の欠落**)。**★`check-derived-docs.sh` が「✅ 最新」と出す理由**(源泉である log 自体が止まっている)**を書き添えた**——書かないと次の親が「digest は最新だ」と誤読する | 14 |
| 13 | **必要時に引くパスへ 4 件を追加**——ドラフト ／ `Memo_Someday.txt` ／ `improve-01` の報告 1 組 ／ `sf6_app_naming_shortlist.txt`。**M19 期の資料**(`m19-close-report` は「クローズ報告の書式の手本」として役割を変えて残置) | — |
| 14 | **§4 のスナップショットを 2026-08-19 実測へ全面更新**。**`progress-summary` の鮮度は実査して書き直した**(M0〜M22・v1.17.0)。**base の値を引き写していない** | 18、8 |
| 15 | **`make e2e` の現況を `IMPROVE-01` 後へ更新**(クリーンなクローン・`node_modules` 不在から完走)。**`remote-ops` §5.2 の新設**を反映 | — |
| 16 | **アーカイブ退避を親のタスク 3 へ引き渡した**——**製造・改善セッションは `followup-backlog.md` を編集できない**(**D-382**)。dry-run の実測値(15 件)と、**列崩れで読めなかった 14 行**の存在を添えた | — |
| 17 | **★タスク 7 (a) に「M22 期の設計卓が同じ指示を受けて実施しなかった」ことを明記**——`m20-desk-startup-prompt.md` の昇格は M22 キットが指示済みだが未実施。**まず昇格が済んでいるか実査させる**形にした | — |
| 18 | **タスク 7 (c) を「役目を終えたファイル」ではなく「新種」として整理し直した**——`IMPROVE-` 系 3 件は TYPES 未登録なだけ。**★EXCEPT へ足して黙らせるのは開発者の承認を得たときだけ**を再掲 | — |
| 19 | **武装 ／ `check-derived-docs` ／ テンプレ直読 2 箇所 ／ CLAUDE.md 任意 ／ `progress-summary` 必読 ／ 親は 1 つ、は base のまま維持** | 7、13、15、16、17、18、19 |

---

## 6. 改訂履歴

| 日付 | 内容 |
|------|------|
| 2026-08-19 | **新規作成**(`/next_milestone_kit M23`)。base = `m22-startup-kit.md`。**守備範囲は M22 のクローズ ＋ M23(データバージョン管理＝`FR601`)／M24(UX 整理・i18n・繰越)の起票**で、**親は 1 つ**。**★本キットの最大の論点は番号再編だった**——引数で指定されたドラフト `m23-someday-parallel-submilestone-plan-draft.md`(2026-08-13)は**番号再編の 2 日前に作られており、中身は現行の M24 相当**である(**D-386 (5)**・`phase3-overview` v1.2.0)。**開発者に確認して「M24 送りでよい。ただし並列できるなら M23・M24 を並列で」と回答を得た**ため、**M23／M24 の並列起票キットとして構成した**。**派生資料リフレッシュ**: `code-facts` / `docs-map` を再生成(2026-08-19 / `e3634d9`)。**Step 3-0b(digest 再蒸留)はスキップ**——`retrospective-log` が M20/M21 期(§6.6.19)で止まっており前提未達(**M22 期 9 サブ ＋ `IMPROVE-01` が未マージ**)。**親のタスク 2 として引き渡した**。**Step 3-0c(progress-summary)は実施**——M22 を §24 として追記(v1.17.0)。**Step 3-0f(アーカイブ退避)は dry-run のみ**——`--apply` は `followup-backlog.md` を書き換えるため **D-382** に抵触する。**⇒ 親のタスク 3 として引き渡した**(dry-run 実測 = followup 15 件 / board §3・§9 と registry §4 は 0 行)。**★あわせて `Memo_Someday.txt` の再仕分けを本キット側で実施した**(開発者指示。`/memo_someday_cleanup`。21 行へマーカー新規付与 ／ `[予定 M23]` 7 行を `[予定 M24]` へ改番 ／ `[予定 M20]` 3 行を再判定 ／ ゴミ箱復元のバリデーションを `[予定 M23]` へ)。**★正式名称の決定を早期の開発者判断事項としてタスク 6 に据えた**(開発者の明示要望) |

---

*以上。開発者は `git status` をクリーンにし、`claude/desk-YYYYMMDD-N` ブランチで Claude Code を開いて §2 のプロンプト 1 本を貼って起動する。*
