# CHANGE-205: 確定反撃サーチの走査規則へ「初段の発生で判定する行」が加わる（`DES-005` §5.20 ／ `DES-002` §4）

| 項目 | 内容 |
|------|------|
| 文書番号 | CHANGE-205 |
| バージョン | **v1.0.0**（2026-09-14・起票と同時に反映） |
| 由来 | `M37-04` 設計伝達レポート §1-3 / §1-4 ／ 受理は `D-877` |
| 対象設計書 | `DES-005` §5.20（走査規則） ／ `DES-002` §4（`GET /api/punish-finder` の応答） |
| 反映状態 | **反映済み**（`DES-005` 2.120.0 ／ `DES-002` 1.95.0） |
| スキーマ変更 | **0**（列そのものは CHANGE-204） |
| **★★★重要** | **除外述語は 4 条件である。⇒ 3 条件ではない** |

---

## 1. 変更内容

### 1.1 走査規則が 1 層深くなった

| 群 | 行数 | どの列で判定するか |
|---|---|---|
| **A** | **9** | **どちらの列も使わない**〔候補から外れる〕 |
| **B** | **109** | **`first_hit_startup`** |
| **C** | **8** | **`startup`**〔従来どおり〕 |

**★★★除外述語は 4 条件である。⇒ `M37-04` の指示書 §2.3 / A-1 は「3 つ」と書いていたが、§0.4 / §5-4 は 4 条件を前提にしていた**（計測点 `M-180`）**。★正は 4 条件のほうである。**

### 1.2 ★`starters[].startup` の意味が変わった

**★従来＝「単独で出したときの発生」。⇒ 今後は「判定に使った発生」である。**

**★★★フィールド名は変えていない。⇒ 名前だけを見ると変化に気づけないため、本行を `DES-002` §4 へ置いた。**

---

## 2. ★★★画面間の食い違いは*すでに実在する*

**★技一覧は `startup` を出し、確定反撃サーチは `first_hit_startup` を使う。⇒ 同じ技に 2 つの数が並ぶ。**

**★★レポート §1-4 は「着地時点の実挙動＝応答は 1 バイトも変わらない」と書いていたが、これは追補3 より前の記述である。⇒ `000116` が 106 行を埋めた 2026-09-14 の時点で、食い違いは現に発生している。**

**⇒ 「将来そうなる」ではなく「もうそうなっている」として読むこと。**

---

## 3. ★挙動の帰結（実測）

| | |
|---|---|
| 恒久的に候補から落ちた行 | **17** |
| 着手前より緩い有利フレームで出るようになった行 | **88**〔記録値より初段のほうが速いため〕 |
| canary で新規出現した `code` | **0 件** |
| 始動技の延べ出現 | **+54,462** |

---

## 4. 反映

| 対象 | 版 | 箇所 |
|---|---|---|
| `DES-005` | **2.120.0** | **§5.20**（走査規則の層と 4 条件の除外述語） |
| `DES-002` | **1.95.0** | **§4**（`nodes[].starters[].startup` の意味） |

---

*以上、CHANGE-205 **v1.0.0**。* **★★除外述語は 4 条件である。★食い違いは既に実在する。**
