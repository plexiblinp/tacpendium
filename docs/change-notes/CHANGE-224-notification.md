# CHANGE-224 設計変更通知書 — `REUSE.toml` の連番グロブ表を撤去（層 B は命名規約だけで決まる）

| 項目 | 内容 |
|------|------|
| CHANGE 番号 | **224** |
| バージョン | **1.0.0**（2026-09-19・起票＋同時反映） |
| 起票日 | 2026-09-19 |
| 起票者 | 設計担当 Claude（設計卓） |
| 由来 | **`M33-02`**（9 群の生成と同一性の検証）の設計伝達レポート |
| 根拠裁定 | **`D-902`** |
| 改訂した設計書 | **`DES-001` 1.8.0**（§5.1） ／ **`SUPP-001` 1.75.0**（§2.7 ライセンス項 2） |
| スキーマ変更 | **なし** ／ マイグレ消費 **0 本**（**★新系列は既存の連番体系の外である**） ／ API 経路 **0 件** ／ 新規依存 **0 件** |

---

## 1. ★★★何が起きていたか — **ライセンスの誤解決が実際に発生した**

**★新系列へ置き換えた瞬間、`000009_seed_initial_users_tags.*`**〔`users` / `tags` ＝ **層 A であるべき**〕**が `CC-BY-SA-4.0` へ*違反 0 件のまま*解決していた**（実測）**。**

**★★★指示書 `M33-02` §4.2 が「戻せない」と名指しした事故が、既に発生していた。⇒ 放置して次段へ渡す選択肢が無かった。**

---

## 2. ★★機序は 2 つの重なりである

| # | 何が |
|---|---|
| **★★1** | **`REUSE.toml` の連番グロブ `migrations/000009_*.sql` が命名規約 `migrations/*_data_*.sql` より*後ろ*に在った。⇒ REUSE 3.3**〔同一ファイル内では*最後に一致した*表だけを使う〕**により命名規約を上書きした** |
| **★★2** | **`check-migration-license.sh` の旧凍結表も `000009` を層 B としていた。⇒ 規則 (3) が黙った** |

**★★★機械検査が 2 つとも黙る形であった。⇒ 片方だけなら赤くなった。**

---

## 3. ★★★9 連番のどの並べ替えでも解消しない

| | 層 A | 層 B |
|---|---|---|
| **旧凍結表の枠**（`000001`〜`000009`） | **5** | **4** |
| **9 群が必要とする数** | **4** | **5** |

**⇒ 枠が合わない。★番号の付け替えでは逃げられない。**

---

## 4. 何を変え、何を変えていないか

| | 内容 |
|---|---|
| **★変えた** | **`REUSE.toml` の 72 連番グロブの表 (4) を撤去**（**★コメントとして経緯を残してある**） ／ **`check-migration-license.sh` の凍結表を新系列へ差し替え** |
| **★★変えていない** | **規則 (1)〜(4) のロジック**（1 行も） ／ **層の割当**（1 つも） ／ **`CLAUDE.md` §6 の禁止ライセンス列**（**★inbound の規定は不変**） |

**★実測（是正後）＝`--self-test` 11 対照すべて OK ／ 本検査 EXIT=0 ／ `--list` 18 行すべて意図どおり**〔層 A 8 ファイル / 層 B 10 ファイル〕**。**

---

## 5. ★射程の前借りであることを記録する

**★`M33-overview` §5 は「ガードの作り直し」を `M33-03` の射程としている。⇒ 本件はその前借りである。**

**★★★ただし製造の独断ではない。⇒ 2026-09-19 の着手前の照会で開発者が「本サブで `REUSE.toml` と凍結表も直す」と裁定している。**

**★★前借りが正しかった理由＝事故が既に発生しており、放置して次段へ渡すと「ライセンスが誤ったまま `M33-03` の作業が積み上がる」。⇒ 前借りのコストより小さい。**

---

## 6. ★残る穴（**`M33-03` の入力**）

**★`check-migration-license.sh` の規則 (4) は `_data_` を持つファイルについて「どの表へ書くか」を見ない。⇒ 凍結表に無い新しい連番で `users` へ書くファイルに `_data_` を付けると、`CC-BY-SA-4.0` へ解決したうえで検査が*緑*になる**（一時ファイルで実測。EXIT=0）**。**

**★★凍結表の差し替えでは塞がらない。⇒ 規則 (4) 側の変更が要る**（`followup` の `migration-license-check-blind-to-write-target-when-data-named`）**。**

**★★★あわせて＝本サブは新系列 9 本を*全数*凍結表へ入れたため、現ツリーでは規則 (4) が 1 本も評価されない**（空回り）**。⇒ 規則 (4) を守る唯一の経路は `--self-test` の陽性対照 6 形になった。**

---

*以上、CHANGE-224。* **★★★芯は §2 である**——**2 つの機械検査が*重なって*黙った。⇒ 片方だけなら捕まえた。★「検査が 2 本ある」は「2 倍安全」ではない。同じ表を共有していると、同時に外れる。**
