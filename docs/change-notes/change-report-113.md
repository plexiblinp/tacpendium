# change-report-113: `CHANGE-113` 反映レポート（M22-02 ログイン画面・ユーザー選択・ウィザードのパスワード UI）

| 項目 | 内容 |
|------|------|
| 対象 CHANGE | **CHANGE-113**（通知書 `docs/change-notes/CHANGE-113-notification.md`。**2026-08-15 承認**＝**D-401**） |
| サブマイルストーン | **M22-02**（M22 直列本線の 2 本目） |
| 反映日 | 2026-08-16 |
| 反映者 | 設計担当 Claude（M22 期・設計卓） |
| 一次源 | **M22-02 設計伝達レポート**（2026-08-16） ／ 完了報告 `docs/progress/m22-02-completion-report.md` ／ 手動確認ラウンドの詳細レポート `docs/handover/design-reports/20260816-m22-02-manual-check-design-exceptions.md` |
| 実装 | ブランチ `claude/m22-02-implementation-plan-85qmqo` / `dcf6558`〜`1b16068` の 11 コミット（基点 `ee06d48`） |

---

## 1. 反映した設計書

| 文書 | 版遷移 | 反映箇所 |
|---|---|---|
| **`DES-005`** `docs/design/05-screen-design.md` | **v2.67.0 → v2.68.0**（第79版） | **§2 へ画面 22〔ログイン〕 ／ §2.1 新設〔共通ヘッダの利用者表示〕 ／ §5.1 を 8 段へ ＋ Step 7 の要件 5 点 ＋ Step 4 の是正 ／ §5.3 の撤回と 4 点 ／ §5.16 のパスワード変更 3 点 ＋ ユーザー編集の現況 ／ §5.13・§5.14・§5.19 へ 1 行 ／ §5.22 新設** |
| **`DES-002`** `docs/design/02-architecture.md` | **v1.50.0 → v1.51.0** | **§3.4 の記載要求を改訂**（**★スコープ拡張。§3 参照**） ／ **§8 へ「ユーザー選択の as-built」6 項目と「初回設定と復旧の補足」2 項目** ／ **§8.1 へ 17 番〔画面が見るのは実効値である〕** |
| **`DES-003`** `docs/design/03-data-model.md` | **v1.54.0 → v1.55.0** | **§3.7 へ「利用者スコープの契約」2 項目 ＋ 対で扱う理由 ＋ 列を足さない理由 ＋ 出力への帰結**（**★スキーマは 1 バイトも変えていない**） |
| **`DES-006`** `docs/design/06-validation.md` | **v1.26.0 → v1.27.0** | **§7 のタイトルの射程を是正 ／ `VAL-N02` の残骸を除去 ／ `VAL-N08` / `VAL-N09` を追加**（利用者名の空・重複） |
| **`REQ-001`** `docs/design/requirements.md` | **v2.20.0 → v2.21.0** | **`FR013` 後半の射程を明確化**（**★スコープ拡張。通知書 §2.4**） |
| **`SUPP-001`** `docs/design/supp-001-detailed-design.md` | **v1.40.0 → v1.41.0** | **§3.5 の生成契機を「利用者を作成したとき」へ ／ §5.3 へ「横断関心の置き場」5 項目**（**★スコープ拡張。通知書 §2.5**） |

**★他の設計書は変更なし**——`DES-001` ／ `DES-004`。

---

## 2. 起票内容と as-built の差（**★4 件。設計伝達レポート §2 の全数。いずれも `D-407` で受理した**）

| # | 起票・設計書 | as-built | 反映先 |
|---|---|---|---|
| **2-1** | **`DES-002` §3.4**「接続可能になるIPアドレスとポート番号」と「このポートが外部に公開されていないことを念のためご確認ください」を確認ダイアログへ記載する | **★どちらも出していない**（`port` prop ごと撤去） | **`DES-002` §3.4 を改訂。** **★受理した理由**——**本 CHANGE §6.1 の文言方針〔開発者の明示的な指摘「『ルーターでポートを外部公開しないでください』は仕組みを理解していない人には分からない」〕と、指示書 §4.6-5 の禁止が、古い明文と正面から衝突していた。** **⇒ 新しい要求が優先する。** **★ただし `NFR105` の目的は落ちていない**——**§3.9 の注記が引き受け、IP とポートの表示は設定画面のネットワーク欄に残っている。** **⇒ 情報は失われず、ダイアログから外しただけである** |
| **2-2** | **`DES-005` §5.16**「ユーザー追加・**編集**」 | **★改名 UI が未実装**（`PATCH /api/users/:id` は在るが画面から呼んでいない） | **`DES-005` §5.16 へ現況を明記。** **★項目名は変えない**——**設計としては正しい。** **⇒ followup `user-rename-ui-not-wired` が追う。** **★API が在るため配線だけで済む** |
| **2-3** | **指示書 §4.5-2**「受け側は変えない。サービス層・リポジトリ層は既に `userID` を受け取っている」 | **★コンボ側の受け口を追加した**（`ReplaceTagAssociations` ／ `combosvc.Get` ／ `ListFilter.UserID` ／ `UpdateMetadataInput.UserID`） | **★設計書の反映は不要**（実装内部の粒度）。**⇒ 指示書の誤りとしてボード §6 へ記録した**（**`M-67`**）。**★前提が真だったのはタグとプリセットだけであり、`D-405` の案 B は受け口の追加を必須とする。** **★しかもチェックリストがその前提を検査項目へ昇格させ、正しい実装を違反と読む検査になっていた** |
| **2-4** | **`DES-005` §2**（共通ヘッダに利用者表示は無い） | **★ヘッダに利用者表示を追加**（2 人以上のときだけ・押すと選び直し） | **`DES-005` §2.1 を新設。** **★開発者が実機確認で直接指示したため `CLAUDE.md` §10 に触れない。** **★表示条件と押下時の動作まで書いた**——**書かないと次の担当が「常に出す」に直して 1 人運用の画面を変えてしまう** |

### 2.1 起票時に無かった as-built（**設計伝達レポート §1。設計書へ落としたもの**）

| # | 内容 | 反映先 |
|---|---|---|
| 1 | **横断関心（401 の判断・利用者の供給）は `lib/` の fetch ラッパ 1 か所に置く** | **`SUPP-001` §5.3。** **★「1 か所」が成立する層は `fetchJSON` ではない**——**API 呼び出しが 3 系統に散っており、`fetchJSON` にだけ足すと 2 系統を取りこぼす** |
| 2 | **ログイン画面・ユーザー選択画面は URL を持たない条件表示** | **`DES-005` §5.22 ／ §2 の備考。** **★選択を保持しない設計と、済んだ画面へ戻さない要求を同時に満たす唯一の形だった** |
| 3 | **利用者の指定が無いときの既定は `users.id` の最小値。解決に失敗しても要求を止めない** | **`DES-002` §8。** **★「認証ではない」ことの帰結として、無指定を拒否しない** |
| 4 | **利用者の指定は要求ヘッダで運ぶ。詐称は脅威モデルに入っていない** | **同上。** **★書かないと次の担当が署名を足そうとする** |
| 5 | **初回設定は有効化まで行う。順序が要る** | **同上。** **★API は 2 本だが利用者から見れば 1 操作である** |
| 6 | **★忘れたときの復旧には検証子の削除も要る** | **同上 ＋ `DES-005` §5.16。** **★`password_enabled = false` に戻すだけでは新しいパスワードを決められない**——**検証子が残ると変更フォームが忘れた当のパスワードを要求する** |
| 7 | **既定タグ 3 件を利用者作成と同一トランザクションで生成** | **`SUPP-001` §3.5 ／ `REQ-001` `FR013`** |
| 8 | **ウィザードは 8 段（LAN 無効時 6 段）** | **`DES-005` §5.1** |
| 9 | **`combo_tags` の置換は利用者スコープである** | **`DES-003` §3.7** |
| 10 | **「パスワードが分からないとき」に絶対パスもコピーボタンも出さない** | **`DES-005` §5.16。** **★この案内が要る人はログイン画面に居て、設定画面へ入れない** |

---

## 3. ★起票時のスコープを 3 文書ぶん広げた（**黙って広げていない**＝playbook §4.18）

**起票時のメタ表は「`REQ-001` 不変 ／ `DES-003` 変更なし」と宣言していた。** **★3 段階で広がった。**

| 段階 | 文書 | 経緯 |
|---|---|---|
| 1 | **`REQ-001` `FR013`** | **2026-08-15＝`D-402`**（通知書 §2.4）。**製造の実査で `FR013` 後半の逐語と as-built が食い違うことが判明した** |
| 2 | **`DES-003` §3.7** | **2026-08-15＝`D-404` / `D-405`**（通知書 §2-k・§2-q）。**★スキーマは 1 バイトも変えていない**——**表定義に現れない契約を書いただけである** |
| 3 | **`DES-002` §3.4 ／ `SUPP-001` §3.6・§5.3** | **2026-08-16＝`D-407`**（通知書 §2.5）。**as-built の確定で判明した** |

**★新規採番は不要**（本数は 1 本のまま＝**D-323** の先例）。**先例＝`CHANGE-083` / `CHANGE-087` / `CHANGE-101`（`D-354`） ／ `CHANGE-112` §3。**

### 3.1 ★反映時に見つけた 2 件（**本レポートで宣言する**）

| # | 内容 | 扱い |
|---|---|---|
| 1 | **★`SUPP-001` の該当節は §3.6 ではなく §3.5 だった**（設計伝達レポート §1-6 と通知書 §2-r は §3.6 と書いていた） | **★§3.5「初期タグと色」が正しい。** **§3.6 は「初期データ投入方式」であり別物である。** **⇒ §3.5 を直した。** **★節番号の取り違えは本レーンで繰り返し起きている**（`M-46` / `M-37` の型） |
| 2 | **★`VAL` 番号が `CHANGE-119` と衝突しかけた** | **`CHANGE-119`（`M22-08`・2026-08-16 起票）が `VAL-N05`〜`N07` を予約済みだった。** **⇒ 本反映は `VAL-N08` / `N09` を取った。** **★`DES-006` §7 に「番号は起票の順であって、反映の順ではない」と注記した。** **★危なかった**——**反映の順で採ると、承認済みの `CHANGE-119` を書き換えることになっていた** |

### 3.2 ★`CHANGE-119` §2-f を先取りした

**`DES-006` §7 のタイトル「LAN共有モード切替時のバリデーション」の射程の是正は、`CHANGE-119` §2-f が担当と宣言していた。**

**⇒ 本反映で先に行った。理由**——**本反映が `VAL-N08` / `N09`〔利用者名の検証〕を足すと、その時点で節タイトルの射程が破れるためである。** **⇒ 破れた状態を `M22-08` の完了まで残す方が悪い。**

**★`CHANGE-119` §2-f は反映時に「済み」として扱う。**

---

## 4. 検証の証跡（製造レポートの実測）

| 対象 | 結果 |
|---|---|
| `go test ./...` | **52 ok / FAIL 0** |
| `pnpm test` | **158 files / 1471 tests** |
| `make e2e` | **129 passed**（基線 114） |
| `tsc --noEmit` | 緑 |
| **破壊確認** | **A**（`passwordRequired` を生値へ差し替え）／ **B**（`userID` の供給元を定数へ戻す）／ **C**（`combo_tags` の置換を全消しへ戻す）——**3 通りとも赤。復旧後の diff 0 を実測** |
| 凍結領域 | `docs/design/` ／ `migrations/` ／ `character_data/` ／ `internal/api/auth/` ／ `internal/service/auth/` ／ `internal/api/middleware/auth.go` ／ `internal/service/notation/` ／ `web/src/features/{physical-input,gamepad,keyboard}/` ／ `go.mod` ／ `go.sum` ／ `package.json` ／ `pnpm-lock.yaml` ／ `internal/api/middleware/cors.go` の **diff 0 を実測** |
| `internal/service/preset/` | **本番コード diff 0**。テスト 1 ファイル 3 行のみ（`comboSvc.Get` の署名追従）。`authorizeMutation` は不変 |
| `combo_tags` のスキーマ | **不変**（2 列のまま） |
| マイグレ消費 | **0 本**（disk 末尾 `000077`。**次に払い出す番号は `000078`**） |
| 新規 CHANGE 採番 | **0 本**（本 CHANGE を消費しただけ） |
| ブラウザストレージ | **新規キー 0 件**（選択を保持しないため） |
| レビュー | **往復 1 回**（上限 2 回・未到達）。**「高」5 件すべて採用・不採用 0 件** |
| 停止時記録（`followup-backlog` §J） | **0 件** |

---

## 5. 併せて行った同期

| # | 対象 | 内容 |
|---|------|------|
| 1 | `docs/handover/change-number-registry.md` | **`113` を「反映済み」へ**。**★新規採番は無い**（`M22-02` は CHANGE を消費していない）ため「次の番号」の更新も不要 |
| 2 | `docs/instructions/M22-overview.md` | **M22-02 行を完了へ**（`D-407` で実施済み） |
| 3 | `docs/handover/followup-backlog.md` | **4 件を解決済みへ ／ 1 件を縮小 ／ 7 件を新規登録**（`D-407` で実施済み） |
| 4 | `docs/process/parallel-board.md` | **裁定 `D-407`** ／ **指示書の誤り `M-67`** ／ §1.6 ／ §2.4 |
| 5 | **`CHANGE-119`** | **§2-f を「本反映で済み」として扱う**（§3.2） |
| 6 | **`CHANGE-114`** | **★`DES-002` / `DES-003` の版の見込みを繰り上げる**——**本反映で `DES-002` は v1.51.0、`DES-003` は v1.55.0 になった。** **⇒ `CHANGE-114` の反映先は v1.52.0 / v1.56.0 になる** |

---

## 6. ★残した課題

| # | 内容 | 割付 |
|---|---|---|
| 1 | **`migrations/000007` のヘッダが「M6 着手時にウィザード経由生成へ切り替える」と宣言したまま残る** | **★適用済みマイグレのため直せない。** **`SUPP-001` §3.5 が実体を持つ。** **⇒ 恒久的に取り残される**（`M22-01` 横断課題 2 と同じ対象） |
| 2 | **`web/src/features/user/UserSelectScreen.tsx` の注記「`DES-005` §5.3 の現行記述は as-built と食い違う。`CHANGE-113` で改訂される」が失効した** | **★本反映で改訂した。** **⇒ 次に同ファイルを触るサブが落とすこと**（設計伝達レポート §4-14） |
| 3 | **パスワード入力のバリデーションが実質ゼロである** | **`M22-08` / `CHANGE-119`**（**D-408** / **D-409** / **D-410**） |

---

*以上、change-report-113。配置 `docs/change-notes/change-report-113.md`。`CHANGE-113` は本レポートをもって反映完了。*
