# CHANGE-234 設計変更通知書 — 配布物に来歴が付いた（attestation / SBOM / タグ署名の as-built）

| 項目 | 内容 |
|------|------|
| CHANGE 番号 | **234** |
| バージョン | **1.0.0**（2026-09-20・起票＋同時反映） |
| 起票日 | 2026-09-20 |
| 起票者 | 設計担当 Claude（設計卓） |
| 由来 | **`M36-02`** の設計伝達レポート **§1-2 / §3-3 / §3-4 / §6 原稿 B** |
| 根拠裁定 | **`D-914`** |
| 改訂した設計書 | **`DES-002` 1.100.0**（§11.2 の配布物の来歴） |
| スキーマ変更 | **なし** ／ マイグレ消費 **0 本** ／ API 経路 **0 件** ／ 新規依存 **0 件** |

---

## 1. ★★`CHANGE-213` が予告していた箇所が埋まった

**★同 §2.2 は「SHA-256 が示すのは壊れていないことだけである。⇒ attestation は射程 3＝`M36-02`」と書いていた。**

| 面 | as-built |
|---|---|
| **ビルド来歴** | **`actions/attest@v4` が配布アーカイブ 3 本へ SLSA provenance を付ける** |
| **SBOM** | **`anchore/sbom-action@v0`**（syft・Apache-2.0）**が `spdx-json` を出し、同じ `actions/attest@v4` で attest する。★リリース資産としても添付** |
| **利用者側の検証** | **`gh attestation verify <archive> --repo <owner>/<repo>`** |
| **タグ署名** | **`git verify-tag` ＋ `.github/allowed_signers`**（**★鍵は未登録**） |

---

## 2. ★★★検証手順は 2 か所に出るが、正本は 1 つである

**★アーカイブ内の `README.txt` と、リリースページ本文。⇒ `scripts/generate-release-notes.sh` が `docs/usermanual/dist-readme.txt` の SHA-256 の節を*抽出*する。**

**★★手で 2 か所に書かない**（`E-76`）**。⇒ これが `followup` の `sha256-verify-steps-unreadable-at-verify-time` の解消である。**

---

## 3. ★★★SBOM の母集団は「ソースツリー全体」であり、配布物ではない

**★配布物そのものを走査していない。⇒ できないからである。本体は `-tags=embed_web` でフロントのビルド成果物をバイナリへ embed するため、出来上がった実行ファイルを syft で走査しても拾えるのは Go モジュールだけである。**

**★★★したがって本 SBOM は配布物より*広い***〔devDependencies ／ `web/e2e` ／ 開発用スクリプトも入る〕**。⇒ 「入っていないものまで載る」側の誤差であり、「入っているのに載らない」側ではない。★供給網の確認という用途では安全側に倒れる。**

**★★代償を明記することが本節の目的である。⇒ 書かないと、次に読む人が「配布物の SBOM である」と読む。**

---

## 4. ★★限界として残るもの

**★★★コード署名**（`B1`）**は付いていない。⇒ SmartScreen の警告は消えない。**

**★attestation が代替するのは「出どころ」であって「OS が警告を出さないこと」ではない。⇒ リリース本文にも明記済みである。**

---

## 5. ★★書き分けの注意

**★「CI は組まない」は nightly について依然として真である。⇒ nightly は 1 バイトも変わっていない。**

**★★変わったのは「*リリースという別の契機*が組むようになった」ことである。⇒ 混ぜて書くと `CHANGE-129` の境界が消える。**

---

## 6. ★★`permissions` から `artifact-metadata` を外した（**★実走で未確認**）

**★`actions/attest@v4` の README は 3 つを挙げるが、本サブは 2 つにした**〔`id-token` / `attestations`〕**。**

| # | 根拠 |
|---|---|
| **1** | **README 自身が `artifact-metadata` を「artifact storage record を作るため」と説明し、それは `push-to-registry` が true のときのものである。⇒ 本サブはコンテナを push しない** |
| **2** | **アクション自身の CI が `contents: read` ＋ `attestations: write` ＋ `id-token: write` だけで動いている**（レビュアーも独立に追認） |

**★★★ただし実走で確かめていない。⇒ rc タグの実走で権限不足が出たら、理由ごと足す。★その旨はワークフローのコメントに在る。**

---

## 7. 影響

**★実装差分は 0 件。**

---

*以上、CHANGE-234 **v1.0.0**。*
