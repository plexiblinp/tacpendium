# change-report-021: CHANGE-021 設計書本体反映完了レポート

| 項目 | 内容 |
|------|------|
| レポート ID | change-report-021 |
| 対応 CHANGE 通知書 | CHANGE-021(FR702 文言修正 + FR704 新設) |
| 反映完了日 | 2026-06-07 |
| 反映担当 | 整理工程担当 Claude |
| 承認者 | 開発者(CHANGE-021 通知書ドラフトを 2026-06-07 に承認) |

---

## 1. 反映対象ファイル一覧

| ファイル | 旧バージョン | 新バージョン | 反映ステータス |
|---------|------------|------------|--------------|
| `docs/design/requirements.md`(REQ-001) | v2.14.0 | v2.15.0 | 反映完了 |

CHANGE-021 §4.1 影響範囲表で「あり」と判定されたファイルは REQ-001 のみ。CHANGE-020(v2.13.0 → v2.14.0)を前提に、同一整理工程内で連続して v2.14.0 → v2.15.0 へ改訂。

---

## 2. 反映内容(ファイル別の修正内容表)

### 2.1 REQ-001(requirements.md)

| 箇所 | 旧内容 | 新内容 |
|------|--------|--------|
| メタ表バージョン行 / ステータス行 | v2.14.0 | v2.15.0(第25版、CHANGE-021 反映を明記) |
| FR702(更新追従) | 更新追従を「外部ツール経由」で行う旨の記述 | 更新追従は**アプリ側の CSV インポート(FR704)で実施**する旨に文言修正。FR701 取込ツールが出力した CSV をアプリが取り込む経路に統一 |
| FR704(新設) | (なし) | **新設**: FR701 取込ツールが出力する moves / キャラクターデータ CSV をアプリが取り込む機能。**フェーズ2**。コンボ CSV 入出力(FR405、フェーズ3)とは別系統。FR703 の直後に挿入 |

FR704 の CSV フォーマット・moves スキーマの正式仕様は、フェーズ2 着手時の別 CHANGE で formalize する(本改訂では FR の存在と位置づけのみを規定)。

---

## 3. 修正されなかったが影響範囲表に挙がっていたファイル(漏れ検知)

CHANGE-021 §4.1 影響範囲表で「なし」と判定された設計書本体(漏れ検知のため明示列挙):

| ファイル | 影響判定 | 反映ステータス |
|---------|--------|--------------|
| DES-001(01-tech-stack.md) | なし | 変更なし(判定通り) |
| DES-002(02-architecture.md) | なし | 変更なし(判定通り) |
| DES-003(03-data-model.md) | なし | 変更なし(判定通り) |
| DES-004(04-notation-spec.md) | なし | 変更なし(判定通り) |
| DES-005(05-screen-design.md) | なし | 変更なし(判定通り) |
| DES-006(06-validation.md) | なし | 変更なし(判定通り) |

CSV フォーマット詳細・moves スキーマの formalize はフェーズ2 着手時の別 CHANGE に持ち越すため、DES-003(データモデル)/ DES-006(バリデーション)への波及は本 CHANGE では発生しない。

---

## 4. セルフチェック結果(playbook §16.4.2)

### 4.1 用語・参照の grep 確認

- REQ-001 の FR 一覧に FR704 が FR703 の直後へ追加され、実装フェーズ列 = フェーズ2 であることを確認
- FR702 の本文に「外部ツール経由」の旧記述が残存していないことを確認
- FR704 と FR405(コンボ CSV)の混同を避ける注記が入っていることを確認

### 4.2 バージョン文字列の整合確認

- REQ-001 メタ表バージョン行 = v2.15.0、ステータス行 = 第25版(CHANGE-021 反映)に更新済み

### 4.3 影響範囲表との照合

CHANGE-021 §4.1 の「あり」判定 1 件(REQ-001)のみ反映、「なし」判定 6 件は変更なし。漏れ・過剰反映なし。

---

## 5. 関連ファイルの更新(後続作業)

| ファイル | 旧バージョン | 新バージョン | 更新内容 |
|---------|------------|------------|--------|
| change-number-registry.md | v1.8.0 | v1.9.0 | CHANGE-020 と合算で 1 エントリ。§1 で 021 を「使用済み」に追加、§3 に REQ-001 v2.14.0 → v2.15.0 行を追加(change-report-020 §5.1 と同一の v1.9.0 改訂に含む) |

---

## 6. 開発者へのお願い

リポジトリへの反映時:

1. `docs/design/requirements.md` を v2.15.0 改訂版(CHANGE-020 + CHANGE-021 反映済み)で上書き
2. `docs/change-notes/CHANGE-021-fr702-fix-fr704-import.md`(通知書本体)を追加
3. `docs/change-notes/change-report-021.md`(本レポート)を追加
4. change-number-registry v1.9.0(CHANGE-020 と共通)を反映済みであることを確認

---

*以上、change-report-021 v1.0*
