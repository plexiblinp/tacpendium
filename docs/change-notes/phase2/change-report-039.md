# change-report-039: CHANGE-039 反映完了レポート

| 項目 | 内容 |
|------|------|
| 対象 CHANGE | CHANGE-039（M10-02 A-2 既定キャラクターの文脈追従） |
| 反映日 | 2026-06-18 |
| 反映担当 | 設計担当 Claude（フェーズ2 本流スパイン・M10 担当） |
| 承認 | 開発者（Plan Mode 2 問とも選択肢1で確定） |

---

## 1. 修正されたファイル一覧（旧版 → 新版）

| ファイル | 種別 | 旧版 → 新版 |
|----------|------|-------------|
| 05-screen-design.md（DES-005） | 設計書本体（CHANGE 管理） | v2.20.0 → **v2.21.0** |
| change-number-registry.md | 補足資料（自由改訂） | v1.25.0 → **v1.26.0** |
| CHANGE-039-M10-02-default-character-context.md | CHANGE 通知書本体 | 新規（承認済み・反映済み） |

## 2. ファイル別の修正内容

| ファイル | 節 | 修正内容 |
|----------|-----|----------|
| DES-005 | ヘッダ | バージョン 2.20.0 → 2.21.0。ステータス行に「第32版（CHANGE-039反映：…既定キャラクターの文脈追従…M10-02 A-2）」を前置 |
| DES-005 | §4.3 共通要素 | キャラクター選択プルダウンの既定説明を文脈追従へ拡張（文脈ありは当該キャラ既定・文脈なしはデフォルト表示キャラ） |
| DES-005 | §5.7 コンボ登録・編集 | 「新規モードの既定キャラクター」節を追加（一覧/マイコンボ選択キャラ既定・文脈なし INITIAL_CHARACTER_ID・伝播・編集固定不変） |
| DES-005 | §5.8 コンボ比較 | 「コンボ追加モーダルの既定キャラクター」節を追加（比較リスト先頭コンボのキャラ既定・空なら INITIAL・手動切替可） |
| change-number-registry.md | §1 / §3 / §4 | §1 に 039（反映済み）・次回 040。§3 に 1 行（DES-005 v2.20.0→v2.21.0）。§4 に v1.26.0 |

## 3. 影響範囲表に挙がったが修正しなかったもの（漏れ検知）

| 対象 | 修正不要の理由 |
|------|----------------|
| DES-002（API 契約） | 登録系 API は character_id 既存（code-facts §7-2）。文脈キャラ伝播はフロント完結で API 変更不要 |
| DES-005 §5.7 編集モードのキャラ固定 | 不変（文脈追従は新規モードのみ。編集は initial.characterId 固定） |
| ComboEditor 内部選択ロジック / モーダル内セレクタ | 不変（初期値を外から渡すのみ＝後方互換）。実装は M10-02 で実施 |

## 4. 移行影響・後方互換

- データ移行不要。プロップ不在時は従来挙動（INITIAL_CHARACTER_ID）で後方互換。挙動拡張のみ。

## 4b. 追補：実装整合 errata（2026-06-18、DES-005 v2.21.0 → v2.21.1）

- M10-02 実装 survey で、新規登録導線（`/combos/new` 非コピー）は**ホーム／フッター／一覧の3箇所のみ・マイコンボには存在しない**ことを確認（開発者確定＝設けない）。
- これに伴い DES-005 §4.3／§5.7 の新規登録の文脈追従元を「一覧／マイコンボ」→「**一覧（ComboListPage）のみ**」に是正（v2.21.1）。新規 CHANGE は起こさず CHANGE-039 の errata。§5.8 は変更なし。
- registry を v1.27.0 に更新（§3 DES-005 行を v2.21.1、§4 に履歴）。E2E は一覧起点（A'）で通過。

## 5. セルフチェック結果（playbook §16.4.2）

- DES-005 ヘッダ版（2.21.0）・ステータス（第32版）整合を確認。
- §4.3／§5.7／§5.8 の3箇所で「文脈ありは文脈キャラ・文脈なしは INITIAL_CHARACTER_ID（デフォルト表示キャラ）」の規定がぶれていないか目視突合。
- registry の 039 使用済み／040 次回／§3 1 行／§4 v1.26.0 の相互整合を確認。
- 対の M10-02 指示書・レビューチェックリストを v1.0.0 へ同時確定（retrospective-digest §7：指示書改訂時はチェックリストも追従）。

---

*以上、change-report-039 v1.0.0。リポジトリ正規パスへの実反映・git 操作は開発者の責任範囲。*
