# CHANGE-144 反映レポート（change-report-144）

| 項目 | 内容 |
|------|------|
| 対象 CHANGE | **CHANGE-144**（M24-06・取込・出力の UX） |
| 反映日 | 2026-08-30 |
| 反映者 | 設計担当 Claude（設計卓／M24 期） |
| 裁定 | **D-605**（受理） |
| 出所 | `M24-06` 設計伝達レポート（2026-08-30）／ 完了報告 ／ レビュー（重大 0 / 高 3 / 中 6 / 低 4・採用 12・不採用 1） |
| 改訂した設計書 | **`DES-005` 2.87.0（第98版）** ／ **`DES-002` 1.69.0（第67版）** |
| 消費 | **スキーマ変更 0 ／ マイグレ 0 本**（次は `000080`）**／ 新規依存 0 ／ ブラウザストレージ新キー 0 ／ `internal/` の差分 0 行** |

---

## 1. 契約違反の独自判断は **0 件**

`internal/` の差分 0 行。`DES-002` §4.2 の経路表は新設も変更もなし。**ZIP の中のエントリ名は 1 文字も変えていない**（`DES-002` §7.6 の契約）。

**★§3-5 の「逸脱」1 件は逸脱ではない。** 指示書 §4.4 は適用面を「`/export` と §5.13a の両方（片方だけだと作法が割れる）」と定めたが、**`/export` は §4.3 の判断で廃止され、面が存在しなくなった。** **⇒ 「片方だけに入れた」のではなく「片方が消えた」であり、作法は割れていない。** 受理する。

---

## 2. 反映した内容

### 2.1 `DES-005` §5.13——**エクスポート画面の廃止**

**`SM-072` の第 2 段。⇒ 出力の入口は §5.13a のダイアログ 1 本になった。**

**★★製造の手順を評価する**——**指示書 §4.3 の暫定案は「廃止する」だったが、§9.1 が「しかできないことが在るのに廃止する」を禁じていた。⇒ 実態確認で 4 件（母数 12）見つかった時点で停止し、開発者へ判断材料を提示して逐語「廃止する (memo の方向)」を得た。** **★これが `CO-002` に課した「消す前に母集団を数える」の正しい実行である。**

**失われた機能 2 件**〔(a) キャラ横断の一括出力 ／ (b) 任意コンボ ID の直接指定〕**を、どちらも代替なしと明記して残した。** **★(a) は `CHANGE-146`（`FR401` の見直し）の発端である。**

**★本節にしか無かった欠陥 1 件も記録した**——**視覚出力へ `videoPath` / `imagePath` を通していた**（`CHANGE-073` の意図に反する）。**ダイアログ側は元から除去していた。⇒ 実害は廃止で消えたが、非対称の記録として残す。**

**★廃止前の本文は残した。** **§5.13a が「§5.13 の選択可能項目」等を参照しているため、仕様の正本として要る。**

### 2.2 `DES-005` §5.13a——**ファイル名欄の追加**

書式・「対象の種別」の決め方・同一秒の連番・自動値を採り直す条件・ZIP エントリ名へ及ぼさないことを、**すべて as-built で書いた**（製造の §1-1〜§1-4）。

**★★とくに 2 点は設計卓が決めていなかった部分である**——**(1) 「対象の種別」をワイヤ値ではなく利用者から見た対象で決める**〔ワイヤ値は WYSIWYG 契約により常に `selected` であり、そのまま名前にすると「フィルタ結果を出したのに `selected`」になる〕**／(2) 同一秒の連番**〔`D-592` の書式は秒までしか持たず、書式だけでは連続出力が衝突する。**⇒ 衝突しないことは書式ではなく払い出し口が守っている**〕。

### 2.3 `DES-002` §7.6——**往復対称の限界**

**往復対称は本登録について成立し、仮登録については成立しない**（`SM-071` の帰結）。**★書かないと後任が「往復が壊れている」と起票する。**

**★あわせて ZIP エントリ名の定数が 2 パッケージに独立して定義されていることを記録した**——**片方だけ変えれば赤くなるが、両方を同時に変えると緑のまま契約が壊れる。**

---

## 3. 反映しなかったもの

| # | 項目 | 理由 |
|---|---|---|
| 1 | **`DES-006`** | **★取込 VAL そのものは変わっていない**（`SM-071` の除外は FE 側。`D-590` で確定済み） |
| 2 | **`DES-002` §4.2** | **★経路の新設・変更なし** |
| 3 | **`SM-110` の用語 20 語** | **★`M24-07` の手番**（製造は 1 語も変えていない） |

---

## 4. 設計卓の誤り 1 件（**計測点 `M-101`**）

**指示書 v1.2.0 ／ チェックリスト v1.2.0 ／ `CHANGE-144` v1.2.0 が一貫して「`DES-006` §5（取込 VAL）」と書いていたが、§5 はタグ編集であり取込 VAL（`VAL-I01`〜`I10`）は §6 である。**

**★製造は §6 を正として作業した**（正しい）。**★3 文書とも設計卓が書いたものであり、3 回とも同じ誤りを写した。**

**★機序＝1 文書目で誤り、そのまま 2・3 文書目へ転記した。⇒ 三点セットは「同じ誤りを 3 倍にする」構造でもある。**

**★本レポートで是正するのは記録のみである**（3 文書は完了済み。`D-274 (3)`）。**⇒ `DES-006` 本体は無変更であり、実装への影響は無かった。**

---

## 5. プロセスの申し送り 5 件（製造 §5）

| # | 内容 | 行き先 |
|---|---|---|
| **5-1** | **ポート確認コマンド `(ss -ltnp \|\| netstat -ltnp) \| grep …` が、この環境では `ss` も `netstat` も無いため常に「空き」と答える** | **playbook §4.37 へ具体形を書いた**（`python3` の `socket.connect_ex`）。**★★同コマンドは設計卓の成果物**〔指示書・チェックリスト・`.claude/commands/`〕**を全数走査しても 1 件も出てこない。⇒ 出所を設計卓側で特定できていない** |
| **5-2** | **「時刻に依存する性質」の観測は E2E ではなく純粋関数側に置く** | **playbook §4.49 の隣接。★破壊確認 4b で E2E は 1 回目赤・retry 緑になり、決定的に守っていたのはクロックを注入した純粋関数テストだった** |
| **5-3** | **破壊確認は「単純な早期 return」では作れない場合がある** | **★`M24-05` の同じ記録が実際に役に立った。⇒ playbook へ既出**（§4.32 の系） |
| **5-4** | **破壊確認の結果を `head` で切らない** | **★§7.2 の「採取コマンドにパイプで切り詰めを入れない」は破壊確認の観測にも効く。⇒ 指示書テンプレへ明記する** |
| **★5-5** | **レビュー担当は製造セッションの会話ログを参照できない** | **★★playbook §4.52 を新設した**（開発者判断を得たら、完了報告に「いつ・どの問いに・逐語で何と答えられたか」を独立した節として残させる） |

---

*以上、CHANGE-144 反映レポート。* **★★`M24-06` は受理した**（`D-605`）。**契約違反の独自判断 0 件。** **★最大の判断は `/export/combo` の廃止であり、製造は「実態確認 → 停止 → 開発者判断」の順を守った。** **★設計卓の誤りは 1 件**（`M-101`＝`DES-006` の節番号を 3 文書へ写した）。**★`FR401` の見直しは `CHANGE-146` として別に起票・反映した。**
