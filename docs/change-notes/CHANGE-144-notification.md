# CHANGE-144 通知書: 取込・出力の UX

| 項目 | 内容 |
|------|------|
| 通知書ID | CHANGE-144 |
| サブマイルストーン | **M24-06**（取込・出力の UX） |
| 起票日 | 2026-08-29 |
| 起票者 | 設計担当 Claude（設計卓／M24 期） |
| 承認者 | （実装・レビュー完了後に開発者が記入。commit hash 群） |
| ステータス | **起票済（着手前ゲート通過）／未反映。★★2026-08-29 に v1.2.0 へ改訂**（**D-591** / **D-592**。§8）。 **★反映は `M24-06` の as-built 確定後、change-report と同時に行う** |
| 影響設計書 | **`DES-005` §5.13 / §5.13a / §5.14**（主対象） ／ **★`DES-002` §7.6**（**2026-08-29 追加＝ZIP 内エントリ名の契約を明文で守る。★変更ではなく確認**） ／ **`DES-006` §5**（条件付き＝取込の VAL） ／ **`DES-002` §4.2**（条件付き＝§4） |
| 関連 | 指示書 `docs/instructions/M24-06-import-export-ux.md` **v1.2.0** ／ チェックリスト `docs/instructions/reviews/M24-06-review-checklist.md` **v1.2.0** ／ `M24-overview` v1.12.0 §3 ／ ledger `M24-RESEARCH-01` §2 / §3 |
| ブロック | **★★M24 本体ブロック `129`〜`140` は使い切った**（`D-587`）。**本 CHANGE が追加ブロック `144`〜`149`（`D-575` 払い出し）の先頭 `144` を消費する。残りは `145`〜`149` の 5 本** |
| スキーマ変更 | **なし の見込み。新規依存 0。マイグレ消費 0 本の見込み**（**★消費するなら次は `000080`**） |

---

## 1. 変更の概要

**取込・出力まわりの「使いにくい」を潰す。ledger 7 項目。**

| クラスタ | ID | memo の要求（要旨） | 面 |
|---|---|---|---|
| **A. 取込の入口**（★本サブの核心の 1 つ） | **`SM-071`** | **インポートで仮登録を除外したい** | `/import` |
| | **`SM-076`** | **0 件ファイルのとき何も出ない** | `/import` |
| **B. 出力の対象指定** | **`SM-072`**（＝`SM-073` / `SM-077`） | **キャラ ID 入力・タグ ID 入力・コンボ ID 指定が使いにくすぎる** | `/export` |
| **C. 出力の後始末** | **`SM-075`** | **ファイル名に気を使う。自動で対象＋日付を付けたい** | `/export`・§5.13a |
| **D. 用語** | **`SM-110`** | **「エクスポート」「インポート」等がエンジニア向けの表現ではないか** | 横断 |
| **E. 確認ダイアログの監査** | **`SM-121`** | **色々な操作に確認ダイアログが不足していないかチェックが必要** | 横断 |
| **F. 技術的負債** | **`CO-023`**（＝`G-15`） | **`M17-04` 実装上の脆さ**（`ComboImportPage` の StrictMode 対策） | `/import` |

---

## 2. 変更の内容

### 2.1 ★★`SM-071`——**取込で仮登録を除外する。★どこで除外するかが分岐点**

**memo の逐語（line 154）＝「インポート機能、仮登録は除外したい」** ／ **【2026-08-11 要確認を解消】「M17-05 の対象外だった＝未対応のまま。取込プレビューには『仮登録』の表示列があるだけで除外 UI・フィルタは無い」**

| 案 | 内容 | 設計卓の評価 |
|---|---|---|
| **A** | **FE で除外**（取込プレビューでフィルタし、送らない） | **★`DES-002` §4.2 に触れない。★★ただし「除外した」ことが利用者に見えるかは UI 次第** |
| **B** | **BE で除外**（取込 API が `is_draft` の行を弾く） | **★★`DES-006` §5 の取込 VAL に触れる。★取込結果の件数の意味が変わる**（スキップ扱い） |
| **C** | **利用者が選べる**（既定＝除外、チェックで含める） | **★★memo は「除外したい」であり、選択肢が欲しいとは書いていない** |

**★設計卓の暫定案＝A**（**★FE で除外し、除外件数を取込結果に出す**）。**★理由＝`DES-002` §4.2 に触れず、「何が除外されたか」が利用者に見える。** **★★§7 確認事項 1。**

**★★ledger が「取込 API（仮登録の除外を BE で行うか FE で行うか）」を API 契約の論点として立てている**（`M24-RESEARCH-01` §5）。**⇒ 設計卓の独断で決めない。**

### 2.2 ★`SM-076`——**0 件ファイルのとき何も出ない**

**memo の逐語（line 162）＝「インポート、0件ファイル時に何もでない」** ／ **【2026-08-11】「取込を実行した後のトースト（成功N/スキップN/エラーN）は B-5 で入った。ただし本件の『0件ファイル時』＝プレビュー段階の話であり未対応」**

**⇒ ★取込を実行する前（プレビュー段階）で 0 件だったときに、何も起きないように見える状態を直す。**

**★★注記が「プレビュー段階の話」と切り分けている。⇒ 実行後のトーストを触らないこと。**

### 2.3 ★★`SM-072`（＝`SM-073` / `SM-077`）——**出力の対象指定**

**memo の逐語（line 155）＝「エクスポート機能、現状のキャラID入力、タグID入力、コンボID指定は使いにくすぎるので修正が必要」** ／ **【2026-07-21】「M17-05 棚卸し A-3（対象選択の改善＋エクスポート画面の段階廃止）。A-1（一覧/マイコンボからの選択エクスポート）は M17-05b 完了（2026-07-18）＝一覧のフィルタ/選択がそのまま対象になる」**

**memo の逐語（line 157・`SM-073`）＝「エクスポート画面、マイコンボタグ(マイコンボ)で絞り込んで出力だけで指定はちょっとわかりにくいかも。キャラ指定も任意で可能としたい」**

**★★注記に「エクスポート画面の段階廃止」という語がある。⇒ 「直す」と「畳む」のどちらなのかが決まっていない。**

| 案 | 内容 | 設計卓の評価 |
|---|---|---|
| **A** | **`/export` の対象指定を直す**（ID 入力をやめ、選択 UI にする） | **★★`DES-005` §5.13 が残る** |
| **B** | **`/export` を畳み、§5.13a のダイアログへ一本化する** | **★★`M17-05b` で「一覧のフィルタ/選択がそのまま対象になる」導線が既に入っている。⇒ 役割が被っている可能性がある**（**`CO-002` と同じ形**） |

**★設計卓の暫定案＝A**（**★畳むのは射程が大きく、`DES-005` §5.13 の廃止は別の判断である**）。**★★§7 確認事項 2。**

**★★`CO-002` の先例が効く**——**「役割が被っているから片方を消す」は、消す前に母集団が同じことを確かめる必要がある**（`D-587` / `D-588`）。**⇒ B を採るなら、`/export` でしかできないことが無いかを先に数える。**

### 2.4 ★`SM-075`——**ファイル名の自動命名**

**memo の逐語（line 160）＝「エクスポート時のファイル名に気を使う。自動でエクスポート対象＋日付とか付ける？いまのままだと連続エクスポート時に名前を一々買えないといけないのでしんどい」**（**★「買えない」は「変えられない」の打ち間違いと読める**）

**★要求は明確である**——**連続エクスポートでファイル名が衝突しない形にする。**

**★命名の規則は §7 確認事項 3。** **★暫定案＝`{対象の要約}_{YYYYMMDD-HHmmss}.{拡張子}`。**

### 2.5 ★★`SM-110` / `SM-121`——**横断の 2 件。★どちらも「監査」である**

| ID | memo の逐語 | 本サブでの扱い |
|---|---|---|
| **`SM-110`**（line 253） | **「エクスポート、インポート等、エンジニア向けかもしれない表現を見直し。PCで仕事をする人には一般的かもしれないが、そうではない人向けには怪しい気がしてきた。ただ私の感覚なので改めて検討は必要。」** | **★★`M24-07`（用語・i18n）へ送る。★本サブでは「候補を挙げる」までにする** |
| **`SM-121`**（line 308） | **「色々な操作に確認ダイアログが不足していないかチェックが必要。削除、更新、非表示（永続、一時的なものは除く）、インポート、エクスポート、取込等。何も出ないと事故や、動作していないのでは？と誤解しそうな部分は対応が必要」** | **★★監査は本サブで行う**（取込・出力の面に限る）。**★是正は「取込・出力の面のもの」だけ。他面は報告に留める** |

**★★`SM-110` の原文は「ただ私の感覚なので改めて検討は必要」と自ら留保している。⇒ 設計卓が語を決めない。**

**★★`SM-121` は「色々な操作」＝全画面が対象だが、本サブの面は取込・出力である。⇒ 面を絞らないと `M24-06` が全画面監査になる。**

### 2.6 ★`CO-023`（＝`G-15`）——**`M17-04` 実装上の脆さ**

**followup §G の逐語＝「連携プレビューの StrictMode 対策＝`ComboImportPage` で `setTimeout(0)`＋cleanup＋ref＋`previewM.data` 購読（dev の二重マウントで `useMutation` の per-call `onSuccess`/observer が落ちて `isPending` が固まる既知事象への標準対処・E2E で機能検証）」**

**★★ledger が「ファイルが同一（`ComboImportPage`）。要求は別」と書いている**（`SM-071` と）。**⇒ 同じファイルを触るので同じサブで扱う。**

**★★`CO-023` の割付は「M24 or 改善レーン」・据え置きである。⇒ 本サブで扱うかは §7 確認事項 4。**

---

## 3. 影響範囲

| 文書 | 節 | 変更の見込み |
|---|---|---|
| **`DES-005`** | **§5.14** | **★★主対象。仮登録の除外 ／ 0 件ファイルの見せ方** |
| **`DES-005`** | **§5.13 / §5.13a** | **★対象指定 ／ ファイル名の自動命名** |
| `DES-006` | §5 | **★条件付き**（**確認事項 1 で案 B を採った場合＝取込 VAL に触れる**） |
| `DES-002` | §4.2 | **★条件付き**（§4） |
| `DES-003` / `DES-004` | — | **変更なしの見込み** |

---

## 4. ★`DES-002` §4.2 に触れる可能性

**★確認事項 1 で案 B（BE で除外）を採った場合、取込 API の挙動が変わる。**

**★★案 A（FE で除外）なら触れない。⇒ 暫定案は A である。**

**★実査で確かめ、変わるなら設計卓へ報告すること**（指示書 §7.4.1-1）。

---

## 5. リスク

| # | リスク | 手当て |
|---|---|---|
| **★★1** | **★★`SM-121`（確認ダイアログの監査）が全画面へ広がる** | **★★面を「取込・出力」に絞った**（§2.5）。**★他面で見つけたものは是正せず報告させる**（指示書 §1.6-4） |
| **★2** | **★`SM-072` で `/export` を畳むと射程が跳ね上がる** | **★暫定案は「直す」である**（§2.3）。**★★畳むなら `CO-002` と同じ手順が要る**——**消す前に母集団が同じことを確かめる**（`D-587` / `D-588` の先例） |
| **3** | **`SM-110` で語を決めてしまう** | **★★原文が「私の感覚なので改めて検討は必要」と留保している。⇒ 候補を挙げるまでにする**（§2.5） |
| **4** | **仮登録の除外を BE でやると取込結果の件数の意味が変わる** | **★§2.1。★確認事項 1** |
| **★5** | **★★設計卓は実装ソースを読めない。上記の面の指定はすべて実査項目である** | **指示書 §3.3。★「ある」も「無い」も断定していない** |

---

## 6. 代替案（検討したが採らなかった）

| 案 | 内容 | 却下の理由 |
|---|---|---|
| **A** | **`SM-110` の語を本サブで確定させる** | **★原文が留保している。★★`M24-07`（用語・i18n）が新語彙を確定させる手番である**（`M24-overview` §3＝**UI レーン完了後に一括整合**） |
| **B** | **`SM-121` の監査を全画面へ広げる** | **★本サブの面ではない。★全画面監査は別サブ規模である** |
| **C** | **`CO-023` を改善レーンへ送る** | **★★`SM-071` と同じファイル（`ComboImportPage`）を触る。⇒ 同じサブで扱うほうが安い。★ただし確認事項 4 で確かめる** |

---

## 7. 開発者への確認事項

| # | 問い | 設計卓の暫定案 | 止まるか |
|---|---|---|---|
| **1** | **★★取込での仮登録の除外を FE でやるか BE でやるか** | **★暫定案＝FE（案 A）。除外件数を取込結果に出す。★理由＝`DES-002` §4.2 に触れず、何が除外されたかが見える** | **★`SM-071` だけ止まる** |
| **2** | **★★`SM-072` は「`/export` の対象指定を直す」か「`/export` を畳んで §5.13a のダイアログへ一本化する」か**（**★memo の注記に「エクスポート画面の段階廃止」という語がある**） | **★暫定案＝直す（案 A）。★畳むのは射程が大きく、`DES-005` §5.13 の廃止は別の判断である** | **★`SM-072` だけ止まる** |
| **3** | **★エクスポートのファイル名の自動命名の規則** | **★暫定案＝`{対象の要約}_{YYYYMMDD-HHmmss}.{拡張子}`。★区切りと日付書式は好みが決め手である** | **★`SM-075` だけ止まる** |
| **4** | **★`CO-023`（`G-15`・`M17-04` 実装上の脆さ）を本サブで扱うか**（**割付は「M24 or 改善レーン」・据え置き**） | **★暫定案＝本サブで扱う。★`SM-071` と同じファイル（`ComboImportPage`）を触るため** | **★止まらない**（暫定案で進める） |

---

## 8. ★★2026-08-29 の改訂（**v1.2.0**・`D-591` / `D-592`）

### 8.1 ★★`SM-072` の方向は memo が確定させていた（**設計卓の読み取りの誤り＝`M-93`**）

**v1.0.0 は「直すか畳むかが決まっていない」と書いた。★誤りである。**

**★memo の 3 行が揃って「エクスポート画面の廃止」を未実施分として指している。**

| 行 | 逐語（2026-08-11 注記） |
|---|---|
| **line 155**（`SM-072`） | **「M17 はクローズしたが第2段は未実行 — `ComboExportPage` は router(`/export/combo`)・ヘッダのナビ項目とも健在で、ID 入力の経路は残っている。★★迂回路ができただけで本件の解消ではない」** |
| **line 157**（`SM-073`） | **「一覧/マイコンボからの選択エクスポートで実質的な代替はできたが、★エクスポート画面本体は未改修のまま残存」** |
| **line 163**（`SM-077`） | **「未実施＝CSV 契約（`DES-002` §7.6）は不変、ファイル名、★エクスポート画面の除去」** |

**★★機序＝memo の行が長く、設計卓は前半（2026-07-21 注記）だけを読んで判断した。同じ行の後半に答えが書いてあった。**

**⇒ `M-82`（ledger の要約列が原文を切っていた）の変種である。今度は設計卓自身の読み取りが原文を切った。**

**★暫定案を「廃止する」へ差し替えた。★★ただし memo 自身が「実態確認後・CHANGE対象」と条件を付けている。⇒ 順序は「実態確認 → 廃止 → CHANGE」である。**

**★実態確認＝`/export/combo` でしかできないことが無いかを母数付きで数えること**（`CO-002` と同じ手順＝`D-587` / `D-588`）。

**★★`DES-005` §5.13 の節の廃止は設計卓が反映時に行う。製造は設計書を編集しない。**

### 8.2 ★★`SM-071`——**BE は弾かない。これを明記する**

**開発者の問い＝「何らかの原因でFE突破して入ってしまうなんてことはありませんよね？」**

**★★答え＝突破されうる。** **FE で除外する形は「送らない」だけであり、BE は `is_draft` の行を弾かない。**

| 経路 | 入るか |
|---|---|
| 通常の取込画面 | **★入らない** |
| **★API を直に叩く** | **★★入る** |
| **★FE のフィルタの条件漏れ** | **★★入る**（**⇒ 純粋関数テストで固定する**） |
| **★★取込ヘルパーからの連携** | **★同じプレビュー画面を通るなら入らない。⇒ 実査項目 §3.3-10 で確かめる** |

**★★害の形＝仮登録として DB に行が増えるだけである。データは壊れず、利用者は削除できる。**

**⇒ この形を採るのは「不変条件を守る」ためではなく「うっかり混ざるのを防ぐ」ためである**（開発者裁定 2026-08-29）。

> **★★`M24-11` / `M24-13` と混同しないこと。** **あちらは不変条件の話であり、FE で塞いでも BE が守らないと意味がない**（`VAL-C02` の check-then-act ／ `VAL-C09` の適用範囲）。**本項は不変条件ではない。**

**★「絶対に入れない」が要件になったら BE で弾く形（案 B）であり `DES-006` §5 に触れる。⇒ そのときは止めて報告させる。**

---

### 8.3 ★★★ファイル名の自動命名が取込を壊しうる面を 1 つ見つけた（**D-592**）

**開発者の問い＝「ファイル名は import には無関係だったはずですが、名称変更によって取り込めなくなったりしないかは調査したいです。」**

**★★`DES-002` §7.6 の逐語＝「export はコンボ CSV（`combos.csv`）+ セットプレイ CSV（`setups.csv`）の 2 ファイルを ZIP 1 個に同梱して配送する。import は 2 CSV の個別アップロードに加え、★export した ZIP をそのまま受理して自動展開する（往復対称）。」**

**⇒ ★★名前が関わる層は 2 つある。**

| 層 | 自動命名を及ぼすか |
|---|---|
| **外側**（ダウンロードされるファイルの名前） | **★及ぼす。これが `SM-075` の対象である** |
| **★★内側**（ZIP の中のエントリ名＝`combos.csv` / `setups.csv`） | **★★★及ぼしてはいけない。`DES-002` §7.6 の契約であり、往復対称が壊れる** |

**★★これが本項の最大の落とし穴である。「ファイル名を自動生成する」を素直に実装すると ZIP の中身まで改名しかねない。**

**★実査 2 件を足した**（§3.3-13 / -14）——**ZIP 内エントリ名を何で探しているか ／ 個別アップロードで combos / setups をファイル名で見分けているか。**

**★★(b) の見込み＝入力欄で見分けている**（`DES-005` §5.14 が欄を 2 つに分け、送信前ガードも `comboFile` の有無で判定すると書いている）。**★ただし設計卓は現物を読めない。名前で見分けていたら止めて報告させる。**

**★往復の証拠として E2E (4)（自動命名で出力した ZIP をそのまま取り込める）と破壊確認 5 件目を対にした。**

### 8.4 ★確認事項 3 の決着（**D-592**）

| 論点 | 決めたこと |
|---|---|
| **規則** | **`combos_{対象の種別}_{件数}_{YYYYMMDD-HHmmss}.{拡張子}`**（例＝`combos_filtered_42_20260829-143512.zip`） |
| **★件数を入れる理由** | **★連続出力で「新しいほう」は日時で分かるが「目的のほう」が分からない** |
| **★★利用者が変えられる形** | **★★自動生成値を初期値として入れた「編集可能な欄」。★「毎回入力させる」でも「後で OS 上で変えてもらう」でもない** |
| **★その形を採る理由** | **★★ブラウザのダウンロードは既定で「ダウンロードフォルダへ即保存」であり、「名前を付けて保存」が出るかは利用者の設定に依る。⇒ 「後で変えてもらう」は結局ファイルを探して改名する作業になり、memo の「一々変えないといけないのでしんどい」がそのまま残る** |
| **★作法の一致** | **`SM-088`（セットプレイ名）と同じ形である**——**空なら自動で埋め、入力済みなら尊重する** |

---

*以上、CHANGE-144 通知書 **v1.2.0**。* **★v1.1.0 からの変更＝ファイル名の自動命名が ZIP 内エントリ名へ及ぶと往復対称が壊れることを明文化し、確認事項 3 を決着させた**（§8.3 / §8.4・`D-592`）。**★番号は消費していない。** **★v1.0.0 からの変更＝`SM-072` の方向を「廃止（第2段）」へ訂正し、`SM-071` の「BE は弾かない」を明記した**（§8・`D-591`）。**★番号は消費していない。** **`M24-06`（取込・出力の UX）。スキーマ変更なしの見込み・新規依存 0・マイグレ消費 0 本の見込み。** **★★本 CHANGE が追加ブロックの先頭 `144` を消費する。**
