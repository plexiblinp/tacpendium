# CHANGE-103 通知書: `(ラッシュ)` 命名を `DES-004` §3.2 へ正典化し、`alias_text_en` を組み込みプリセット専用として明文化する

| 項目 | 内容 |
|------|------|
| 通知書ID | CHANGE-103 |
| サブマイルストーン | M20-06 |
| 起票日 | 2026-08-14 |
| 起票者 | 設計卓（Claude Code・武装中） |
| 承認者 | **開発者**（**2026-08-14 承認**。**着手前ゲート通過。確認事項なし**） |
| ステータス | **★反映済み（2026-08-14）**——**`DES-003` v1.52.0〔第54版〕 ／ `DES-004` v1.21.0〔第22版〕 ／ `DES-005` v2.64.0〔第75版〕 ＋ `SUPP-001` v1.36.0 へ反映し、`docs/change-notes/change-report-103.md` を出した。** **★反映先が起票時の 2 文書 4 節から 3 文書 8 節へ増えた**〔`DES-003` §3.9 ／ `DES-004` §3.2.1・§5.3・§5.4・§5.6・§5.7 ／ `DES-005` §5.11〕。**★(d) の条件は満たされた**〔76 → 102 行〕**が、実測件数は設計書へ書かず原則だけを残した**〔正本は契約テスト〕。**マイグレ 2 本を消費**〔`000076` / `000077`〕 |
| 影響設計書 | **`DES-004`**（**§3.2**＝`(ラッシュ)` 接尾辞の正典化 ／ **§5**＝`alias_text_en` の対象範囲〔**as-built 次第**〕） ／ **`DES-005`**（**§5.11**＝`alias_text_en` を編集も表示もしない理由） |
| 関連 | `M20-overview` v1.6.0 §3 ／ 指示書 `M20-06` v1.0.0 ／ ボード **D-289**・**D-290**・**D-305**・**D-307**・**D-317**・**D-337**・**D-367**・**D-368** ／ 先行 **CHANGE-099**（M20-02）・**CHANGE-100**（M20-03）・**CHANGE-102**（M20-05） |

> **★本通知書は着手前ゲートであった。** **【2026-08-14 反映完了】改訂は `change-report-103` と同時に行った。** **⇒ 以降、反映内容の正本は `change-report-103` と各設計書の本体であり、本通知書は起票時点の記録である。**

---

## 1. 変更の概要

### 1.1 何を確定させるか

**3 つある。**

| # | 何を | どこへ |
|---|---|---|
| 1 | **`(ラッシュ)` 接尾辞が実データの唯一の形であること** | `DES-004` §3.2 |
| 2 | **`alias_text_en` が組み込みプリセット専用の列であること**（**理由つき**） | `DES-005` §5.11 ／ `DES-004` §5 |
| 3 | **`alias_text_en` を持つ行の対象範囲が広がったこと** | `DES-004` §5（**as-built 次第**） |

### 1.2 CHANGE 対象と対象外の切り分け

| 対象か | 内容 |
|---|---|
| **対象** | 上記 3 点（**設計書本体の記述**） |
| **対象外** | **`P-34` 13 件の値そのもの**——**これはデータであり、設計書本体には載らない。** **正本はボード **D-367** と `M20-06` 指示書 §4.2 である** |
| **対象外** | **命名の揺れのデータ補正**（**`D-305`。揺れ 0 件で対象が存在しない**） |
| **対象外** | **`P-34` の (b) 13 件の CSV 補記**（**起票先未定**） |
| **対象外** | **`command` 記法の拡張**（**前例が無ければ別 CHANGE。本サブは調査だけ**） |

> **★`P-34` の値決めが設計書本体に載らないことを明記する。** **書かないと、次の担当が「13 件の値がどこにも設計書として無い」と読んで `DES-004` へ列挙を写す。** **⇒ 列挙が 2 か所になり、データを足したときに片方だけ古くなる**（`E-76`）。

---

## 2. 変更の内容（`DES-004` ／ `DES-005`）

| # | 節 | 変更内容 |
|---|---|---|
| **a** | **`DES-004` §3.2** | **`rush_variant` のエイリアスが「元技エイリアス ＋ `(ラッシュ)`」の 1 形であることを明記する。** **★件数と、その形から外れる行の扱いは as-built 次第**（**`D-305` の実測は 272 件中 268 件。★残り 4 件の理由を設計卓は知らない**＝指示書 §3.3-8 で実査させる） |
| **b** | **`DES-005` §5.11** | **`alias_text_en` を「編集も表示もしない」と書いた行へ、理由を足す**——**★カスタムプリセットでは `alias_text` そのものが利用者の言語であり、さらに「英語版」を持たせる意味が無い**（**D-368**・開発者判断）。**★as-built の記録から設計判断へ格上げする。** **書かないと、次の担当が「編集できないのは実装の手抜きだ」と読んで導線を足す** |
| **c** | **`DES-004` §5** | **`alias_text_en` が組み込みプリセット専用であることを明記する**（**b と対。同じ判断を解決順序の側からも書く**） |
| **d** | **`DES-004` §5** | **`alias_text_en` を持つ行の対象範囲を as-built へ**（**★`D-317` は「移動系 9 code × 19 キャラ ＋ 派生 28 件」と見積もっていたが、`P-34` は 13 件になり、既存の非 NULL 行の実数も未確認である**＝指示書 §3.3-6 で実査させる）。**★増える場合のみ改訂する** |


### 2.1 ★【2026-08-14 追補＝D-373】製造の実査で反映内容が 3 件増えた

| # | 節 | 追加する内容 |
|---|---|---|
| **e** | **`DES-004` §3.2** | **正典化の文言を 2 段に書き分ける**——**「末尾が半角 `(ラッシュ)` である」は 272/272 で成立する。** **しかし「元技エイリアス ＋ `(ラッシュ)`」は 271/272 である**〔lily `rush_desert_storm_1hits` が「デザートストーム(ラッシュ)」で、規則どおりなら「デザートストーム(単発)(ラッシュ)」〕。**★1 段で書くと、どちらかが嘘になる。** **`D-305` によりデータは補正しないため、例外は例外として書く**〔followup `rush-desert-storm-1hits-naming`〕 |
| **f** | **`DES-004` §5** | **★`D-317` の (2)〔表示ロケール分岐〕と (3)〔逆引きは両方を引く〕が未実装であることを明示する**（**製造の実査＝Go / web とも実装 0 件 ／ 逆引きは `alias_text` のみ**）。**★設計書が「そうする」と書いたまま実装が無いと、次の担当は「実装済み」と読む。** **⇒ `architecture-patterns` §9.2 で「未実装」を明示したのと同じ扱いにする**〔followup `alias-text-en-display-and-reverse-lookup-unimplemented`〕 |
| **g** | **`DES-004` §5** | **★「解決順序をどの表示経路に適用するか」を書く**——**レシピ表記の解決は `preset_id` ごとに引く。技名〔`name_ja`〕の同定用ラベルは公式表記のプリセットを固定で引く。** **★実装は 3 か所で一貫しているが、設計書が書いていないため誰も誤りと言えなかった**〔製造が停止条件に当たるかを照会してきた＝**M-61** の一因〕 |

> **★あわせて `D-305` の「268/272」の正体が判明した**——**命名の揺れではなく、`original_move_code` の dangling 参照である**〔`_1hits` とすべきところが `_1`。ingrid 2 ／ lily 1 ／ mai 1〕。**⇒ 「揺れ 0 件」と「268/272」は軸が違い両立する。** **★ただし実害がありうるため followup へ送った**〔`rush-variant-dangling-original-move-code`〕。

---

## 3. 確定した設計判断

| # | 判断 | 根拠 |
|---|---|---|
| 1 | **`P-34` の対象は 27 件ではなく 13 件である** | **D-367**。**開発者のドメイン知識で分類が 18/4/5 → 9/13/5 に動いた**（marisa の OD スクトゥム派生 4 件・rashid の三角飛び・manon のランヴェルセ フェイント 4 件は**いずれも入力を伴う**）。**⇒ (a) 9 件のうち 8 件 ＋ 第 3 枠 5 件** |
| 2 | **値の規則は 2 本**——**日本語＝`name_ja` の複製 ／ 英語＝`move_code` の人間可読化** | **D-367**。**新しい情報を作らないため `D-289`〔英語の公式技名を持たない〕に触れない** |
| 3 | **日本語側も埋めるのは `alias_text` が `NOT NULL` だからである** | `DES-003` §3.9。**「英語だけ入れて日本語はフォールバックへ任せる」ができない。** **⇒ この 13 行の目的は英語表示であって、日本語表示を変えることではない** |
| 4 | **jp `departure_window_double_warp_od` は行を作らない** | **D-367**・開発者判断（**エッジケースでありフォールバックでよい**） |
| 5 | **★値はデータとして入れ、コードに特例を書かない**（**カスタムプリセット優先**） | **D-367**・開発者要求。**表示経路に分岐を置くと、カスタムを選んでいても組み込みの値が出る** |
| 6 | **`alias_text_en` は組み込みプリセット専用** | **D-368**・開発者判断 |
| 7 | **`P-34` の投入先は `M20-06` である**（**新サブを立てない**） | **D-368**。**`M20-06` 単体が軽すぎ、面も同じで、CHANGE 番号も節約できる** |
| 8 | **`D-305` は撤回されていない** | **命名の揺れの補正は行わない。** **`P-34` の投入は補正ではなく新規行の投入である** |
| 9 | **第 3 枠 5 件は `D-337` の衝突を起こさない見込み** | **公式名が基底の移動技と異なるため**（`【強化】` を含む）。**★設計卓の推論であり、指示書 §3.3-5 で実査させる** |

---

## 4. 影響範囲・移行影響・リスク

| 種別 | 内容 |
|---|---|
| **スキーマ** | **変更なし**（`alias_text_en` は **CHANGE-099 / M20-02** で追加済み。**列を足さない**） |
| **マイグレ** | **★消費 2 本の見込み**（**`000076` / `000077`**。**2026-08-14 の製造の実査＝disk 末尾 `000075`**）。**確定は反映時。** 以下は起票時の記述： **消費本数は as-built 次第**（**投入をマイグレーションで行うか再適用で行うかは製造へ委ねた**＝指示書 §9.2-2）。**disk 末尾は着手時に実査させる** |
| **移行影響** | **既存の `preset_aliases` 行は書き換えない。** **13 件の新規行のみ** |
| **リスク** | **★カスタムプリセット優先を破る**（**本サブで最も落ちやすい**）。**破っても 13 件以外の表示は正しく、テストも型検査も緑になる。** **露見するのは利用者がカスタムプリセットを作り、この 13 件のどれかを編集したときだけである。** **⇒ テストで固定する**（指示書 §5 (c)(d)）。**チェックリストの重大 1〜3** |
| **リスク** | **★一意制約との衝突**。**投入してから分かるのでは遅い** ⇒ **指示書 §3.3-5 で事前突合させ、§9.4 の停止条件 2 に立てた** |
| **リスク** | **★14 件目を足す**（jp のエッジケース）。**「27 件のうち 14 件が (a) だ」と読むと足してしまう** ⇒ **指示書 §5 (e) が行の不在を主張する** |
| **リスク** | **★`D-305` が覆ったと読まれる**。**同じサブが「補正しない」と「新規行を入れる」を同時に持つため** ⇒ **指示書 §1.1 の注記 ／ §4.7-2 ／ チェックリストの重大 13・§9-3** |
| **リスク** | **★製造が設計書の文案を作る**。**受け取ると設計卓は検証せずに写す** ⇒ **指示書 §4.4 の末尾 ／ チェックリスト §1.5-3** |

---

## 5. 直列化・後続

- **CHANGE 番号** — **103 を消費する。** **M20 ブロックの残りは 104〜105 の 2 本。**
- **マイグレ連番** — **消費本数は as-built 次第。** **反映時に確定する。**
- **`M20-07`** が後続（取込の逆引き活用 ＋ ハッジ分割 ＋ 逆引き前段の正規化）。**★本サブが `alias_text_en` の行を増やすため、逆引きの母数が変わる。**
- **`P-34` の (b) 13 件の CSV 補記** の起票先は、**本サブ §4.6 の調査結果を見て決める。**
- **★`M20-07` と並列投入しない**——**どちらも `preset_aliases` の逆引き側を触る。**

---

## 6. 開発者への確認事項

**なし。** **`D-367`（値の規則・13 件の集合）／ `D-368`（投入先・英語列の割り切り・調査の立て方）で決着している。**

**★承認をいただきたいのは着手前ゲートとしての本通知書のみである。**

---

*以上、CHANGE-103 通知書。*
