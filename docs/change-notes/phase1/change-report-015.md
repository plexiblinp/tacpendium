# change-report-015: CHANGE-015 設計書本体反映完了レポート

| 項目 | 内容 |
|------|------|
| レポート ID | change-report-015 |
| 対応 CHANGE 通知書 | CHANGE-015(DES-002 §4.2 から `POST /api/combos/compare` 削除) |
| 反映完了日 | 2026-05-23 |
| 反映担当 | 詳細設計・製造準備担当 Claude(M5 期間担当) |
| 承認者 | 開発者(CHANGE-015 通知書ドラフトを 2026-05-23 に承認) |

---

## 1. 反映対象ファイル一覧

| ファイル | 旧バージョン | 新バージョン | 反映ステータス |
|---------|------------|------------|--------------|
| `docs/design/02-architecture.md`(DES-002) | v1.7.0 | v1.8.0 | 反映完了 |

CHANGE-015 §4.1 影響範囲表で「あり」と判定されたファイルは DES-002 のみ。他設計書本体への波及なし。

---

## 2. 反映内容(ファイル別の修正内容表)

### 2.1 DES-002(02-architecture.md)

| 箇所 | 旧内容 | 新内容 |
|------|--------|--------|
| メタ表バージョン行 | `\| バージョン \| 1.7.0 \|` | `\| バージョン \| 1.8.0 \|` |
| §4.2 主要エンドポイント表 第 11 行(原本 140 行目) | `\| POST \| \`/api/combos/compare\` \| 複数コンボの比較データ取得 \|` | (行削除) |

その他の変更なし。§4.2 表の他の行、§4.1 / §4.3 / §4.4 等の他節は変更されていない。

### 2.2 反映後の §4.2 表の前後コンテキスト(検証用)

```markdown
| DELETE | `/api/combos/{id}` | コンボ削除 |
| GET | `/api/presets` | 表記プリセット一覧取得 |
| POST | `/api/presets` | カスタムプリセット作成 |
```

`DELETE /api/combos/{id}` 行の直後が `GET /api/presets` 行に直接遷移している(以前は間に `POST /api/combos/compare` 行が存在した)。表構造は崩れていない。

---

## 3. 修正されなかったが影響範囲表に挙がっていたファイル(漏れ検知)

CHANGE-015 §4.1 影響範囲表で「なし」と判定されたファイル(漏れ検知のため明示列挙):

| ファイル | 影響判定 | 反映ステータス |
|---------|--------|--------------|
| REQ-001(requirements.md) | なし | 変更なし(判定通り) |
| DES-001(01-tech-stack.md) | なし | 変更なし(判定通り) |
| DES-003(03-data-model.md) | なし | 変更なし(判定通り) |
| DES-004(04-notation-spec.md) | なし | 変更なし(判定通り) |
| DES-005(05-screen-design.md) | なし | 変更なし(判定通り) |
| DES-006(06-validation.md) | なし | 変更なし(判定通り) |
| SUPP-001(supp-001-detailed-design.md) | なし | 変更なし(判定通り) |
| architecture-patterns.md | なし | 変更なし(判定通り) |
| design-instruction-playbook.md | なし | 変更なし(判定通り) |
| CLAUDE.md | なし | 変更なし(判定通り) |

---

## 4. セルフチェック結果(playbook §16.4.2)

### 4.1 用語の grep 確認(設計書本体全体)

`docs/design/` 配下全ファイルで `/api/combos/compare` または `combos/compare` の言及を grep した結果:

```
(該当なし)
```

設計書本体・SUPP-001 配下に旧用語の残存はなし。

### 4.2 改訂対象ファイル名・節タイトル・バージョン文字列の整合確認

- DES-002 メタ表のバージョン行: `| バージョン | 1.8.0 |`(更新完了)
- DES-002 §4.2 主要エンドポイント表: 旧 12 行 → 新 11 行(`POST /api/combos/compare` 1 行削除)
- DES-002 の他節(§4.1 / §4.3 / §4.4 / §5〜§14)は変更なし

### 4.3 影響範囲表との照合

CHANGE-015 §4.1 影響範囲表の「あり」判定 1 件(DES-002)のみ反映、「なし」判定 10 件のファイルは変更されていない。漏れ・過剰反映なし。

---

## 5. 関連ファイルの更新予定(後続作業)

| ファイル | 旧バージョン | 新バージョン | 更新内容 |
|---------|------------|------------|--------|
| change-number-registry.md | v1.3.0 | v1.4.0 | §1 番号運用表で CHANGE-015 を「使用済み」に追加、§3 改訂表に DES-002 v1.7.0 → v1.8.0 行を追加 |

本レポート提示と同時に change-number-registry v1.4.0 改訂版も `/mnt/user-data/outputs/` に提示する。

---

## 6. 開発者へのお願い

リポジトリへの反映時:

1. `docs/design/02-architecture.md` を v1.8.0 改訂版で上書き
2. `docs/change-notes/CHANGE-015-remove-compare-endpoint.md`(通知書本体)を追加
3. `docs/change-notes/change-report-015.md`(本レポート)を追加
4. `docs/handover/change-number-registry.md` を v1.4.0 改訂版で上書き

リポジトリ反映後、設計担当 Claude は M5-overview ドラフト作成に進む。

---

*以上、change-report-015 v1.0*
