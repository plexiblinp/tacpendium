# CHANGE-235 設計変更通知書 — ラッシュ版の `startup_basis` は元技の `startup` の有無で決まる

| 項目 | 内容 |
|------|------|
| CHANGE 番号 | **235** |
| バージョン | **1.0.0**（2026-09-20・起票＋同時反映） |
| 起票日 | 2026-09-20 |
| 起票者 | 設計担当 Claude（設計卓） |
| 由来 | **`M39-02`**（ラッシュ版の生成経路が実行時に `D-187` を破る穴）の設計伝達レポート **§1-1 / §6** |
| 根拠裁定 | **`D-916`**（本サブの受理） |
| 改訂した設計書 | **`DES-003` 1.82.0**（§3.3 の `startup_basis` 欄） |
| スキーマ変更 | **なし** ／ マイグレ消費 **0 本** ／ API 経路 **0 件** ／ 新規依存 **0 件** |

---

## 1. ★★失効した記述（**前半だけ**）

**★旧記述の逐語＝「rush 生成経路（`insertRushVariantSQL`）は `'through'` を明示して INSERT する——DEFAULT に委ねると、backfill 後に生成される rush 行だけが `unknown` のまま残り、型エラーにも実行時エラーにもならない。」**

| 部分 | 扱い |
|---|---|
| **★前半**（`'through'` を明示して INSERT する） | **★★失効。⇒ 元技が `startup` を持たない場合は `'unknown'` である** |
| **★後半**（DEFAULT に委ねない理由） | **★失効していない。⇒ そのまま残す** |

---

## 2. ★★★新しい述語

> **rush 生成経路の `startup_basis` は、*元技の `startup` の有無*で決まる。**
>
> - **元技が `startup` を持つなら `'through'`**〔ラッシュ版の `startup` は元技のコピーであり、その値は通し値である〕
> - **持たないなら `'unknown'`**〔本節 機械付与の 3 規約 (i) の帰結＝本列は*格納されている `startup` の由来*を表すのであって、値が無い行に由来は無い〕
>
> **★どちらの場合も列は明示列挙し、値を明示的に与える。DEFAULT へ委ねない。**

---

## 3. ★★★これは `M19-04` §4.5 の覆しではない

**★同判断は「ラッシュ版は通し値である」を述べたものであり、*通し値の元が無い*場合を扱っていなかった。⇒ 未処理のケースを埋めただけである。**

---

## 4. ★★変わらないもの（**★念のため明示する**）

| # | 項目 | 状態 |
|---|---|---|
| 1 | **三値**（`standalone` / `through` / `unknown`）**の述語** | **不変** |
| 2 | **機械付与 (ii)「`through` と `standalone` が重なったら `through` が勝つ」** | **不変**（**★本件は `standalone` との衝突ではなく、`startup` の*不在*の話である**） |
| 3 | **機械付与 (iii)「seed 波ごとに再適用する責務は seed 波側にある」** | **不変** |
| 4 | **seed 済み rush 行 514 件の値** | **不変**（**★全件 `startup` が非 NULL**＝実測。**レビューが `migrations/000004` の `VALUES` タプル単位の静的解析で独立に追認**） |
| 5 | `GET /api/moves` への露出 | **不変**（引き続き非露出） |

---

## 5. ★`SUPP-001` への追加は不要である

**★規約 (24)**〔不変条件のガードは版ではなく HEAD に置く〕**は `M39-01` が既に置いており、`M39-02` はその (24)-3**〔ガードが見ているのは新規 DB の最終状態である〕**の*実例*である。⇒ 規約そのものに足すものが無い。**

---

## 6. 影響

**★実装差分は 0 件。⇒ 本通知書は `M39-02` の as-built を設計書へ載せるものである。**

---

*以上、CHANGE-235 **v1.0.0**。*
