# CHANGE-199: 仮想コントローラの as-built（`DES-005` §6.4 / §6.4.3 / §6.8）＋ modifier 表示語の二重化の注記（`DES-004` §2.3）

| 項目 | 内容 |
|------|------|
| 文書番号 | CHANGE-199 |
| バージョン | **v1.0.0**（2026-09-13・起票と同時に反映） |
| 由来 | `M37-02` 設計伝達レポート §1-1 / §1-2 / §1-3 / §4-1 / §4-2 ／ 受理は `D-867` |
| 対象設計書 | `DES-005` §6.4 ／ §6.4.3 ／ §6.8 ／ `DES-004` §2.3 |
| 反映状態 | **反映済み**（`DES-005` 2.117.0 ／ `DES-004` 1.34.0） |
| スキーマ変更 | **0**（**★マイグレ消費 0 本・API 経路の新設 0**） |
| 新規依存 | **0** |
| **台帳** | **★`web/CLAUDE.md` §1 **#12** `gamepad-input-notice-seen-v1` は `M37-02` が追記済み。⇒ 本 CHANGE がその番号を与える**（`CLAUDE.md` §10.X） |

---

## 1. `DES-005` §6.4.3 — 告知は「初回のみ自動表示」になった

**★同節は着手前まで「告知と読取表示は、機体を観測できているときだけ出す」までしか書いておらず、*表示回数*の規定を持たなかった。**

| 項目 | as-built |
|---|---|
| **回数** | **★初回のみ自動表示する。⇒ 以降は畳み、明示操作で開く** |
| **既読フラグ** | **ブラウザ保持**〔`gamepad-input-notice-seen-v1`。**★DB 列を足さない**〕 |
| **★★文言** | **`GAMEPAD_INPUT_NOTICE_POINTS` の 3 点は 1 文字も変えない**（`D-347`。**★実測＝diff 上ゼロ**） |
| **★2 面** | **コンボ / セットプレイで既読と開閉を共有する。⇒ 面ごとに 2 個目のキーを作らない** |
| **畳んだ形** | **★2 通りを 1 行で切り替えられる**（`P-59` (2)＝開発者が実物を見て決める）**。⇒ どちらの形でも「明示操作で開ける」が成り立つ** |

**★型は §5.9 の「制約の告知」と同一である。**

**★★2 面共有の実装上の理由は本書に既に在る＝§6.4.3 項目 4「入力面は同時に複数マウントされうる」。⇒ 読み手が同時に立つため、状態を外部で持つ形になる。**

**★★★保存できない環境では初回から畳まれる**（`M37-02` レビュー 低-9）**。⇒ 意図したトレードオフである**——**既読を書けないまま自動表示すると毎回出ることになり、`B10` の目的**〔常時表示をやめる〕**を満たさない。★`D-347` は「事前に伝える場所を決める」ことを要求しており、明示操作の導線が残る限り満たされる。**

---

## 2. `DES-005` §6.4 — 仮想コントローラの配置（as-built）

| # | 確定した形 |
|---|---|
| **1** | **`sm` 以上で左右 2 カラム**〔左＝ファミリー一覧 ／ 右＝種類・強度・OD 組合せ〕 |
| **2** | **★★ファミリー一覧に高さ上限と局所スクロール。⇒ 上限は*固定寸法*（`rem`）で置く** |
| **3** | **同じ扱いを直接指定系の 5 タブ**〔特殊技 / ターゲットコンボ / SA・CA / 未掲載 / キャラ固有状態〕**へも及ぼす** |
| **4** | **強度は「なし・弱・中・強・OD」が 1 行** |
| **5** | **ラッシュ版トグルは枠の中・末尾**〔通常技・特殊技で統一〕 |
| **6** | **レシピのサマリ行は sticky であり、明示操作で全文を縦に展開できる** |
| **7** | **★★★見出しは技名を補間しない。⇒ 技名は上に 1 回だけ `truncate` で出し、見出しは静的な短ラベル**〔「種類」「強度」〕 |
| **8** | **強度色は維持する**〔弱＝水色 / 中＝黄 / 強＝赤 / OD＝灰。**★開発者の指示**〕 |

### 2.1 ★★★上限をビューポート相対（`vh`）にしない

**★理由＝モバイルでは URL バーの開閉に合わせて `vh` が変わり、操作中にパネルが伸縮してボタンが指の下で動く。**

**★★`B09` の逐語は「区画を*固定*して」であり、ビューポート相対は固定ではない。**

### 2.2 ★★★見出しの技名補間を外した理由

**★着手前の見出しは「{技名} の強度」「{技名} の種類」であった。⇒ 右カラム幅**〔192px 級〕**では「【ライトニングビースト】ローリングアタック の強度」が 4 行に折り返し、見出し 2 本だけで約 128px を消費する。**

**★★★左右分割の利得を単独で食い潰していた。⇒ 縦を縮める要望に対し、最も効いたのは*寸法*ではなく*一覧への高さ上限*と*この補間の撤去*である。**

**★旧 i18n キー 2 本は削除した。⇒ 旧文言を assert する床は 0 件であった**（実測）**。**

---

## 3. `DES-005` §6.8 — 走査に使う接頭辞の下へ非ボタン要素を置かない

**★★★テストは `[data-testid^="recipe-common-"]` のような*接頭辞走査*でボタンを数える。⇒ 同じ接頭辞を持つ非ボタン要素**〔スクロール容器など〕**を足すと、床は静かに 1 件多く数える。**

**★実測＝`M37-02` がスクロール容器へ `<接頭辞>-scroll` を付け、並び順の床が落ちた。★同型の走査はリポジトリに 5 か所在る。**

**⇒ 計測用・構造用の test-id は別の名前空間**〔`recipe-scroll-*` 等〕**へ置く。★`M37-02` はそう逃がして回避した。**

---

## 4. `DES-004` §2.3 — flag の表示語は 3 か所に在る

**★★★flag を増やすとき、表示語を直す先は 3 か所である。⇒ 片側だけ直しても、表示は壊れるがテストも型検査も緑のままである。**

| # | 場所 |
|---|---|
| 1 | **Go の `flagText`**（サーバ生成のレシピ文字列） |
| 2 | **フロントの modifier ラベル表**（編集画面のバッジ） |
| 3 | **本節**（`DES-004` §2.3） |

**★★機械検査は無い＝`scripts/check-enum-sync.sh` は flags を見ていない。⇒ フロント側の定義が `web/src/constants/` ではなく `features/combo/` に在るためである**〔`.claude/rules/enum-sync.md` 規則 2 に反する配置〕**。**

**★定数の移設は波及が読めないため本 CHANGE ではやらない。⇒ `followup` §DF の `modifier-flag-labels-triplicated-without-check` へ回す。**

---

## 5. ★★本 CHANGE がやらないこと

| # | 内容 | 理由 |
|---|---|---|
| **1** | **波括弧の撤去** | **★`B04` の本体は別件になった**（`D-867`）**。⇒ 開発者の要望は「編集画面の見た目と一致させたい」であり、波括弧の有無ではない。★`followup` §DF の `modifier-display-differs-editor-and-viewer`** |
| **2** | **一覧でのバッジ化** | **同上。⇒ 一覧 API が `steps` を返さない**（`DES-002` §4.11 の N+1 回避）**ことが制約である** |
| **3** | **modifier の項目の要否・文言の確定** | **★開発者が次マイルストーンで実施すると明言した。⇒ `followup` §DF の `modifier-set-and-wording-final-review`** |
| **4** | **狭い幅の実機確認** | **★開発者判断で省略**（逐語＝「スマホ確認は省略します。おまけ機能に近いため」）**。⇒ 省略は判断であって漏れではない** |

---

## 6. 反映

| 対象 | 版 | 箇所 |
|---|---|---|
| `DES-005` | **2.117.0** | **§6.4.3 ／ §6.4 ／ §6.8** |
| `DES-004` | **1.34.0** | **§2.3** |
| `web/CLAUDE.md` §1 | **#12**（`M37-02` が追記済み） | `gamepad-input-notice-seen-v1` |

---

*以上、CHANGE-199 **v1.0.0**。* **★★★縦を縮める要望に対し、最も効いたのは寸法ではなかった**——**一覧への高さ上限**（`O(件数)` → `O(1)`）**と、見出しの技名補間の撤去である。**
