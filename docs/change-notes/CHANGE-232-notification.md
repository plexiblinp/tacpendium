# CHANGE-232 設計変更通知書 — 生成物のマイグレを直すときの 2 点（規約 (25)）

| 項目 | 内容 |
|------|------|
| CHANGE 番号 | **232** |
| バージョン | **1.0.0**（2026-09-19・起票＋同時反映） |
| 起票日 | 2026-09-19 |
| 起票者 | 設計担当 Claude（設計卓） |
| 由来 | **`M39-01`** の設計伝達レポート **§1-1 / §1-2** |
| 根拠裁定 | **`D-911`** |
| 改訂した設計書 | **`SUPP-001` 1.77.0**（§5.5 規約 (25) を新設） |
| スキーマ変更 | **なし** ／ マイグレ消費 **0 本** ／ API 経路 **0 件** ／ 新規依存 **0 件** |

---

## 1. ★★★循環を断つのは順序である

**★指示書 §2.2.1-3 は「生成器の入力が DB そのものなら循環する ⇒ 止まって報告する」としていた。⇒ 製造は循環を*断って*進めた。★その手順を規約にする。**

| # | 順序 |
|---|---|
| **★★★1** | **旧系列を凍結点から取り出し、記録済みの手順で DB を組む。⇒ 正規化ハッシュが記録値と一致することを先に示す** |
| **★★★2** | **★是正を当てずに再生成し、コミット済みのファイルと byte 一致することを示す。⇒ これが生成器の忠実性の立証である** |
| **★★★3** | **立証が通ってから、DB の複製へ是正を当てて再生成する。⇒ 入力が正しいので循環しない** |
| **★★4** | **立証が通らないなら外科的編集へ倒す。⇒ 立証の有無が手段を決める** |

---

## 2. ★★★列ごとの構造化差分（**`git diff --stat` では足りない**）

**★★1 行の中で 2 セル動いても、増減数は同じである。⇒ 「132 insertions / 132 deletions」は「132 セルが動いた」を意味しない。**

**★式＝`VALUES` タプルを、引用符とエスケープ `''` を尊重して `,` で分割し、列ごとに突き合わせる。**

**★出すのは 4 つ＝行タプル数 ／ 行タプル以外の行の一致 ／ 構造差**〔インデント・末尾記号・列数〕**／ セル差分の総数と列別内訳と値の遷移。**

**★★`M39-01` の実際＝行タプル 3057 / 3057 ／ それ以外 561 行完全一致 ／ 構造差 0 件 ／ セル差分 132 件がすべて `startup_basis` 列で `'standalone'` → `'unknown'`。**

---

## 3. ★なぜ規約にするか

**★★規約 (23) の窓を使うたびに、この 2 点が要る。⇒ 窓だけを規約にして手順を書かないと、次に使う人が循環で止まる。**

---

## 4. 影響

**★実装差分は 0 件。**

---

*以上、CHANGE-232 **v1.0.0**。*
