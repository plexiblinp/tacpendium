# change-report-050: CHANGE-050 設計書本体 反映完了レポート

| 項目 | 内容 |
|------|------|
| 対象 CHANGE | CHANGE-050（M13-01 = コンボ CSV エクスポート/インポート契約の正典化） |
| 反映日 | 2026-06-28 |
| 反映担当 | 設計担当 Claude（フェーズ3 キックオフ担当） |
| 起票文書 | `docs/change-notes/CHANGE-050-M13-01-combo-csv-export-import-contract.md` |
| 背景 | M13-01 着手前。M13-RESEARCH-01-report を受け、未記載だったコンボ CSV カラム契約と CSV 式注入無害化を補う。 |

---

## 1. 反映したファイル（旧→新）

| 設計書 | 旧 → 新 |
|--------|---------|
| DES-002 アーキテクチャ | v1.24.0 → **v1.25.0** |
| DES-006 バリデーション | v1.13.0 → **v1.14.0** |

DES-005 は変更なし（§5.13/§5.14 が既に完備＝漏れ検知の結果、追記不要）。

## 2. 修正概要

### DES-002（v1.25.0）
- **§7.6 新設「コンボ CSV エクスポート/インポート フォーマット（FR401/405）」**: 意味単位（code ベース・DB 管理列除外）・コンボ CSV `local_id` ＋ セットプレイ CSV `parent_combo_local_id`・`drive_damage` REAL・起き攻め 6 列 NULL 可・`starter_move_id` 非出力（import 再導出）・tags 名前埋込・重複動作（ユーザ選択）・無害化/検証は DES-006 §6。FR704（§7.5）とは別系統と明記。

### DES-006（v1.14.0）
- **VAL-I10 新設**: CSV 式注入無害化（先頭 `= + - @` を式評価しない・表示エスケープ・再 export 時 `'` 接頭辞）。CSV インジェクション（DDE/数式注入）対策。

## 3. 影響範囲に挙がったが「変更しなかった」項目（漏れ検知）

| 項目 | 判断 | 根拠 |
|------|------|------|
| DES-005 §5.13/§5.14 | **変更なし** | 出力形式・CSV 別ファイル（local_id/parent_combo_local_id）・重複時動作選択（スキップ/上書き/新規）・行チェックボックス・NFR103 が既に規定済み。M13-01 の CSV 限定は overview/指示書の段階分割で DES 変更不要 |
| DES-003 | **変更なし** | drive_damage REAL（CHANGE-046）・起き攻め 6 列 NULL 可・starter_move_id・combos/setups モデルは既定義。§7.6 はこれらを参照 |
| DES-004 | **変更なし** | CSV カラムは notation 対象外（move_code は §2.1 既定義を参照） |
| import 検証 VAL-I01〜09 | **変更なし** | 既存（サイズ/UTF-8/行数1000/カラム/型/code 存在/URL/サマリ）。コンボ CSV にそのまま適用。新設は VAL-I10 のみ |

## 4. 整合性チェック結果

- VAL 番号: DES-006 §6 で VAL-I 最大＝I09 → **I10** を新設（連番整合）。VAL-C/VAL-S 系には変更なし。
- drive_damage REAL・起き攻め 6 列 NULL 可・starter_move_id の扱いが DES-002 §7.6 と DES-003（既定義）・DES-006 VAL-C02/C03・VAL-C13 で一貫。
- コンボ CSV 別ファイル（local_id/parent_combo_local_id）が DES-002 §7.6・DES-005 §5.13/§5.14・DES-006 §6 で一貫。
- FR704（§7.5・moves・ローカルファイル文脈）と FR401/405（§7.6・ユーザデータ・NFR103 文脈）の別系統性を §7.6 と §7.4 で明記し混同を防止。

## 5. 派生・後続

| 文書 | 対応 |
|------|------|
| change-number-registry | §1 で 050 使用済み・次番号 051、§3 に 2 行（DES-002 v1.25.0 / DES-006 v1.14.0）、版ログ追記 |
| 指示書 M13-01 | §3.1/§8 が参照する「DES-002 §7.x コンボ CSV 契約」は本反映で **DES-002 §7.6** に確定（指示書の §7.x 表記は §7.6 を指す。追補不要） |
| レビューチェックリスト M13-01 | §0.1/§1 の DES 参照（DES-002 §7.x・DES-006 VAL-I）は本反映後の DES-002 §7.6・DES-006 VAL-I01〜I10 を正とする |
| docs-map | DES-002/DES-006 のバージョンは次回再生成で追従 |

## 6. 残ゲート（開発者）

- commit/push は開発者（設計担当はローカルで DES-002/DES-006 改訂結果 + 通知書 + 本レポートを提示するまで）。
- §7.6 の置き場（§7 配下 vs 別章）・VAL-I10 の粒度は通知書 §7 で確認事項として提示（異論なければ現状で確定）。

---

*以上、change-report-050。配置 `docs/change-notes/change-report-050.md`。*
