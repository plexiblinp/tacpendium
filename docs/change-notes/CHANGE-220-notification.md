# CHANGE-220 設計変更通知書 — macOS のアーカイブ名を `darwin` へ寄せ、`DES-002` §11.2 の旧値 2 件を是正する

| 項目 | 内容 |
|------|------|
| CHANGE 番号 | **220** |
| バージョン | **1.0.0**（2026-09-19・起票＋同時反映） |
| 起票日 | 2026-09-19 |
| 起票者 | 設計担当 Claude（設計卓） |
| 由来 | **`M38-03`** の設計伝達レポート §1-1 / §6 |
| 根拠裁定 | **`D-893`**（案 (a)・開発者裁定・2026-09-17） ／ **`D-901`** |
| 改訂した設計書 | **`DES-002` 1.99.0**（§11.2） |
| スキーマ変更 | **なし** ／ マイグレ **0 本** ／ API 経路 **0 件** ／ 新規依存 **0 件** |
| 実装の変更 | **★0 行**（**`M38-03` が実装済み。本 CHANGE は設計書側の反映である**） |

---

## 1. 何が変わったか

| # | 箇所 | 前 | 後 |
|---|---|---|---|
| **★★1** | **§11.2 の配布物構成** | `tacpendium-macos-arm64.tar.gz` | **`tacpendium-darwin-arm64.tar.gz`** |
| **★2** | **同節「操作説明書」** | 全 **22** 章 | **全 21 章**（`M38-02` が技編集の章を削除） |
| **★3** | **同節「PNG の枚数」** | **27** 枚 | **29 枚**（本編 28 ＋ クイックスタート 1。**★着地時の実測**） |
| **★★4** | **同節へ 1 ブロック追加** | — | **名前の揃え方 ＋ 正本の在り処 ＋ 写しが 1 か所だけ残ること** |

---

## 2. ★★★案 (a) を採った理由（**`D-893` の再掲**）

| 案 | 内容 | 触る面 |
|---|---|---|
| **★(a)（採用）** | **アーカイブ名を `darwin` 側へ寄せる** | **★`scripts/release-targets.sh` の 1 行** |
| (b) | バイナリ名を `macos` へ寄せる | **★`CHANGE-191` で固めた面 ＋ `windows_gui_link_test.go` ＋ nightly の 3 つ** |

**★★★正本が 1 か所に寄っていたから (a) が安かった**（`CHANGE-213`）**。⇒ `M36-01` が「構成の正本は `scripts/release-targets.sh` の 1 か所である」と決めたときに予告していた利益が、ここで現に出た。**

---

## 3. ★★★検査自身が 2 か所目を持っていた

**★`M38-03` の実装で分かったこと＝`scripts/check-release-archive.sh` の陰性対照が、旧アーカイブ名を*リテラルで*ハードコードしていた。⇒ 正本を 1 行直した瞬間に自己検査が赤になった**（着手基点の版で実証＝EXIT=1）**。**

**★★「正本は 1 か所」と書いた検査自身が 2 か所目を持っていた。⇒ `M38-03` は対照を正本の配列から引く形へ直した。**

**★★★本節を設計書へ残すのは、次に同じことをする人のためである。⇒ 正本の値を改名するときは、*自己検査と fixture のリテラルを先に `grep` する*。**

---

## 4. ★★名前を写している場所は 1 か所だけ残る

**★配布 `README.txt` の SHA-256 の案内 1 行**〔正本＝`docs/usermanual/dist-readme.txt`〕**である。⇒ ここは機械で結ばれていない。**

**★★★次にアーカイブ名を動かすと、`check-dist-readme.sh`**〔正本と生成物の一致だけを見る〕**も `check-release-archive.sh`**〔「`README` の文面が実態と合うかは見ない」と自ら限界を書く〕**も気づかない。⇒ `followup` の `dist-readme-archive-name-unlinked` として登録した。**

**★今回の改名で実際に手直しが要った。⇒ 写しが在ることの*証拠*である。**

---

## 5. ★据え置いたもの

**★§11.1**〔amd64 / arm64 の両方を「非公式対応」と書く〕**と §11.2**〔arm64 の 1 点しか挙げない〕**の食い違いは触っていない**（`followup` の `des002-macos-arch-mismatch`）**。⇒ 本 CHANGE は arm64 の 1 点の改名だけを扱う。**

---

## 6. 影響しないもの

- **`Makefile`**（`GOOS=darwin GOARCH=arm64 … -o dist/tacpendium-darwin-arm64`。**★元から `darwin` であり不変**）
- **`scripts/release-targets.sh` の 2〜4 列目**（中の実行ファイル名 ／ 形式 ／ ソース。不変）
- **`cmd/tacpendium/windows_gui_link_test.go`**（**★案 (b) が触る面**。不変）
- **nightly の実行内容**（**★コメントだけを正本参照へ書き換えた。⇒ `for f in dist/…` の 3 点は 1 文字も変えていない**）
- **スキーマ・マイグレーション・API 経路・DTO**（すべて 0）

---

*以上、CHANGE-220。* **★★★芯は §3 である**——**「正本は 1 か所」と宣言した仕組みでも、*検査の側*が写しを持っていることがある。⇒ 正本を動かして初めて分かる。★動かす前に `grep` する。**
