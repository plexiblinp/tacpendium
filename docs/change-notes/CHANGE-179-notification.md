# CHANGE-179: 必殺技ファミリーの導出規則が「強度語は `code` のどこに在ってもよい」へ広がった（`M30-03`）

| 項目 | 内容 |
|------|------|
| CHANGE 番号 | **179** |
| バージョン | **1.0.0**（2026-09-10） |
| 起票日 | 2026-09-10 |
| 起票者 | 設計担当 Claude（設計卓／フェーズ4 期） |
| 由来 | **`M30-03` 設計伝達レポート §4-1 / §6**。受理＝ボード **`D-813`** |
| 対象設計書 | **`DES-004` §2.1** ／ **`DES-005` 第6節** |
| 反映 | **起票と同時に反映**（本書と同じ手番） |

---

## 1. なぜ要るか

**★★`CHANGE-166` の帰結文**（強度接尾辞を持たない `move_code` はファミリー UI に載らない）**は、`CHANGE-175`（`M30-02`）で `A1` 186 件について失効していた。⇒ `A2` 105 件については「不変である」と書いてあった。**

**★★★`M30-03` で `A2` も失効した。⇒ したがって `CHANGE-166` の規則は全体として置き換わる。★片方だけ失効した状態の記述が残ると、次に読む人が「A2 は載らない」と読む。**

---

## 2. 変更内容

### 2.1 ★★`DES-004` §2.1 — 導出規則は 3 段である

| 段 | 条件 | 結果 |
|---|---|---|
| **1** | **末尾が `_od` / `_light` / `_medium` / `_heavy`** | `{ family, strength }` |
| **★★2**（**本 CHANGE で新設**） | **強度語が `code` の途中に在る** | **`{ family, strength }`** |
| **3** | 強度語がどこにも無い | `{ family: code, strength: "none" }` |

**★★段 2 の中身＝`SPECIAL_STRENGTH_SUFFIX` の並び**（**`od` → `light` → `medium` → `heavy`**）**で最初に見つかった強度語を 1 トークンだけ抜き、残りを `_` で連結してファミリーとする。**

**★実例＝`lightning_beast_light_rolling_attack` → ファミリー `lightning_beast_rolling_attack` ／ 強度 `light`。⇒ 同じキャラの `lightning_beast_od_rolling_attack` も同じファミリーへ落ちる。**

**★★★規則 1 が規則 2 より常に先である。⇒ これが「既存の判定が 1 件も動かない」の成立条件である。★順序を入れ替えると、末尾接尾辞を持つ 733 件の判定が変わりうる。**

**★★優先順位を `od` 先にした根拠**（`M30-03` §3-2）**：実データで位置順と結果が変わるのは 3 件だけである**〔`cammy/fatal_leg_twister_{light,medium,heavy}_od_hooligan_combination`〕**。⇒ 両側に支持する兄弟が実在するため同点ではない。★2026-09-10 の開発者の実機確認で `od` 先を確定した。**

**★優先順位は `moveSurfacing.roster.test.ts` が名指しで固定している**（3 件の全数 ＋ 規則 1 で決まる 12 件）**。⇒ 覆すときはそのテストを書き換える。**

### 2.2 ★★★`CHANGE-166` の帰結文は全体として失効した

**⇒ `A1` 186 件**（`CHANGE-175`・`M30-02`）**に続き、`A2` 105 件も載るようになった。★「強度接尾辞を持たない `move_code` はファミリー UI に載らない」は、もはやどの枝にも当てはまらない。**

### 2.3 `DES-005` 第6節 — 未分類タブの母集団から `category='special'` が消えた

| 観測 | 着手時点 | 完了時点 |
|---|---:|---:|
| 必殺技タブに載らない `special` | 105（＝`A2` 全数） | **0** |
| 未分類（実 DB・実設定） | 87 | **41** |
| **ファミリー行の総数** | 342 | **★372** |

**★★タブ構成 8 枚と並び順は不変である。**

**★★`isOnSpecialTab` は `category === 'special'` の 1 条件になった。⇒ `parseSpecialCode` が全域になり `null` を返さなくなったためである。**

**★★★副作用が 1 つある＝`isOnSpecialTab` と `deriveSpecialFamilies` が同じ述語を共有しなくなった。⇒ 前者は `category` だけ、後者は `parseSpecialCode` の全域性が前提である。★両者がずれても型検査は通る。**

**⇒ 実データ規模でこの結合を守っているのは `moveSurfacing.roster.test.ts` の座標一致テスト**（キャラごとに `special` 総数 == 必殺技タブの座標総数）**だけである。★この 1 本を消さないこと。**

---

## 3. 変更しないもの

| 対象 | 理由 |
|---|---|
| **規則 1 と規則 3** | **差分なし。⇒ 規則 2 を両者の間へ挿入しただけである** |
| **変種軸**（`SPECIAL_VARIANT_ORDER` / `splitSpecialVariant`） | **`M30-02` のまま** |
| **OD 4 種と活性条件** | **`M30-02` のまま** |
| **タブ構成 8 枚と並び順** | **不変** |
| **`internal/` / `cmd/` / `migrations/`** | **1 行も触っていない。⇒ API 契約もスキーマも不変** |

---

## 4. 影響

- **★ファミリー行の総数が 342 → 372 に増えた。⇒ `M30-04`**（ファミリー行の母集団と並び）**の指示書に書いた着手前の実測値は古くなった。★同書 v1.1.0 で注記済み**（`D-813`）
- **★★表示名に強度語が残る行が 31 → 49 に増えた。⇒ `M30-05` の射程である**（`D-813` で起票）
- **★`followup` の 2 行が解決した＝`special-family-ui-misses-state-variants`**（両枝）**／ `special-family-plain-loads-but-od-does-not`**（7 組が 0 件）

---

*以上、CHANGE-179 **v1.0.0**。* **★★本書の値打ちは §2.2 である**——**`CHANGE-166` が片方だけ失効した状態で止まっていると、次に読む人が「`A2` は載らない」と読む。⇒ 両枝とも失効したことを 1 行で書く。**
