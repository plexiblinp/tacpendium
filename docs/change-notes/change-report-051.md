# change-report-051: CHANGE-051 設計書本体 反映完了レポート

| 項目 | 内容 |
|------|------|
| 対象 CHANGE | CHANGE-051（M13-01 実装反映＝重複動作段階導入・import 行選択粒度・配送/セットプレイ CSV 明確化） |
| 反映日 | 2026-06-28 |
| 反映担当 | 設計担当 Claude（製造担当起票の 051 を確定・関連明確化を集約） |
| 起票文書 | `docs/change-notes/CHANGE-051-M13-01-import-duplicate-action-scope.md` |
| 背景 | M13-01 実装・レビュー完了。製造→設計 伝達メモ A〜H のうち DES 反映を要する A/B/C/D を反映。E/G/H は是認（DES 変更不要）、F は別途（チェックリスト commit）。 |

---

## 1. 反映したファイル（旧→新）

| 設計書 | 旧 → 新 |
|--------|---------|
| DES-002 アーキテクチャ | v1.25.0 → **v1.26.0** |
| DES-005 画面設計 | v2.26.0 → **v2.27.0** |

## 2. 修正概要

### DES-002（v1.26.0）— §7.6
- セットプレイ CSV の正準カラムを **4 列（parent_combo_local_id/name/description/recipe）**に確定列挙（D）。
- 配送コンテナ＝2 CSV を **ZIP 1 個に同梱**（in-memory・FS 非書込）・import は ZIP も受理（往復対称・「別ファイル」は ZIP 内で維持）（C）。

### DES-005（v2.27.0）— §5.14
- 重複時動作の**実装フェーズ注記**＝初期実装は skip（既定）＋新規追加の 2 択、上書きは段階導入（3 択は端状態維持）（A）。
- 行選択粒度＝**チェックボックスはコンボ行単位・セットプレイは親追従**（独立 setup チェックボックスなし・○/× 参照表示）（B）。

## 3. 影響範囲に挙がったが「変更しなかった」項目（漏れ検知）

| 項目 | 判断 | 根拠 |
|------|------|------|
| DES-006 VAL-I10 | **変更なし** | 自由文セル（memo/setup name/description）限定の式注入無害化は VAL-I10 の正しい実装解釈（数値・JSON セルへ `'` 付与は往復同一性を壊すため非適用）。要件テキストの変更不要（伝達メモ E/参考） |
| DES-003 | **変更なし** | setups（name/description/steps）は既定義。§7.6 の 4 列はこれを参照 |
| DES-004 | **変更なし** | CSV 列・配送は notation 対象外 |
| タグ import 同定キー（G） | **DES 追加せず** | name(+user)同定は単一ユーザー前提で実害小。明文規定を設けるほどでない（現実装が事実上の契約） |
| preview 親解決の楽観表示（H） | **DES 追加せず** | preview は advisory・commit が正確。DES-005 §5.14 のプレビュー警告粒度の範囲内 |

## 4. 整合性チェック結果

- DES-002 §7.6 の配送コンテナ（ZIP）と DES-005 §5.13/§5.14 の「別ファイル」が矛盾しないこと（ZIP は配送コンテナ・契約は別ファイル維持）を §7.6 に明記。
- DES-005 §5.14 の 2 択（実装）と 3 択（端状態）の関係を「実装フェーズ」注記で明示。followup-backlog F13-1（上書き繰り延べ）と一貫。
- セットプレイ CSV 4 列が DES-003 setups モデル（name/description/steps・situation 非保持）と整合。
- VAL 番号・他 VAL に影響なし（DES-006 不変）。

## 5. 派生・後続

| 文書 | 対応 |
|------|------|
| change-number-registry | §1 で 051 を起票/未反映 → **反映済み**、次番号 052。§3 に 2 行（DES-002 v1.26.0 / DES-005 v2.27.0）、§4 に版ログ追記 |
| M13-01 レビューチェックリスト（伝達メモ F） | `docs/instructions/phase3/reviews/M13-01-review-checklist.md` v1.0.1 は**設計担当が作成済み・リポジトリ未配置**＝commit 漏れ。運用（別チェックリスト方式）は維持。開発者に commit を依頼（通知書 §7-2） |
| 指示書テンプレート | 「機械レビュー: 別チェックリスト」記述は維持（F が運用変更でないため変更不要） |
| docs-map | DES-002/DES-005 のバージョンは次回再生成で追従 |

## 6. 残ゲート（開発者）

- commit/push は開発者（設計担当は DES-002/DES-005 改訂結果＋通知書＋本レポート＋registry を提示するまで）。**M13-01 レビューチェックリスト v1.0.1 の commit**（伝達メモ F）も併せて。
- 通知書 §7 の確認 2 件（セットプレイ CSV 列の確定・チェックリスト運用維持）。

---

*以上、change-report-051。配置 `docs/change-notes/change-report-051.md`。*
