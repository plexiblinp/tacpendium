# change-report-075: CHANGE-075 設計書本体 反映レポート

| 項目 | 内容 |
|------|------|
| 対象 CHANGE | CHANGE-075（import 体験再設計 B-1〜B-6・M17-05a） |
| 反映日 | 2026-07-18 |
| 反映担当 | 設計担当 Claude（M17 期・セッション3） |
| 起票文書 | `docs/change-notes/CHANGE-075-notification.md` |
| ステータス | **反映（DES 適用済み・commit は開発者）** |
| 背景 | M17-05a 製造完了・レビュー済み・開発者エスカレーション 4 点確定（2026-07-18）。伝達 `20260718-M17-05a-design-handover.md` §1〜§5 を反映判断の入力とした。**スキーマ変更ゼロ・BE 検証ロジック/VAL-S04/CSV 契約 不変** |

---

## 1. 反映したファイル（旧→新）

| 設計書 | 旧 → 新 |
|--------|---------|
| DES-005 画面設計書 | v2.45.0 → **v2.46.0**（**§5.14** 入口一本化・表示名化・local_id 非表示・重複 2 択・VAL 日本語化・親解決乖離注記） |
| DES-006 バリデーション設計書 | v1.19.0 → **v1.20.0**（**§6** 重複時の挙動の正典を新設＝2 択・setups 重複判定を VAL-S04 整合へ・graceful skip・register 分離・上書き構造予約） |

**REQ-001 / DES-001 / DES-002 / DES-003 / DES-004 / SUPP-001 は変更なし**。

## 2. 修正概要

- **DES-006 §6（新設ブロック）**: 重複時 2 択（skip／setups_only・**add 廃止**）／**setups 重複判定＝レシピ一致で skip（名前非依存＝VAL-S04 整合）**／graceful skip は VAL-S04 単独時のみ・他エラー併発は failed 優先／`recipe_hash` 非流用／VAL エラーは FE 表示層で日本語化（BE 不変・VAL-C02 は畳むが既存 ID 残す）／file レベルは現状英語（将来 BE 小改修）／上書きは構造予約のみ UI 非表示。
- **DES-005 §5.14**: 入口一本化（setups 従属＋送信前ガード）／プレビュー表示名化（既存機構流用）／local_id 非表示（CSV 列維持・取込結果は行番号・親列は解決名）／重複 2 択＋上書き構造予約／VAL 日本語化／**親解決○/× は dupAction 非依存＝実行結果と乖離しうる注記**。

## 3. 影響範囲に挙がったが「変更しなかった」項目（漏れ検知）

| 項目 | 判断 | 根拠 |
|------|------|------|
| **VAL-S04 本体・BE 検証ロジック** | **変更なし** | graceful skip は VAL-S04 の結果を skip に翻訳＝判定は不変（伝達 §4）。|
| **CSV 契約・`local_id` 列・スキーマ・`csvcore`** | **変更なし** | B-4 は表示のみ。|
| **`DuplicateKey`／`recipe_hash`／`RecomputeComboCache`** | **変更なし** | 非流用・`TestImportSetupsOnlyDoesNotTouchCombo` で固定。|
| **BE エラーメッセージ・VAL コード体系** | **変更なし** | B-2 は FE 写像（register 分離・CHANGE-066）。|
| **M17-04 連携口・`intake/`** | **変更なし** | `git diff` 空＝差分ゼロ（指示書 v1.0.1 §5.2 回帰ゲート通過）。|
| **DES-002 §4.2（BE 新 API）** | **変更なし** | `setups_only` は既存ハンドラ内の親解決。新 API なし。|

## 4. 整合性チェック結果

- **設計の実物確認漏れを是正**: 指示書 §5.1 の 3 ケースが VAL-S04（既存不変条件）と矛盾していた点を、DES-006 §6 で「レシピ一致で skip（名前非依存）」に正典化し補正。
- **register 分離を維持**: B-2 は FE 写像・BE 不変。
- **M17-04 連携口を壊していない**: 回帰ゲート通過・`intake/` 差分ゼロ。
- **節番号**: DES-005 §5.14／DES-006 §6 に追記・改訂のみ（新設節なし）。DES-005 §5.x（§5.13a 含む）・DES-006 §番号を全件列挙し重複・欠落なしを確認（**確認出力を切らずに**＝playbook §5.4.1）。
- **後方互換**: 旧 `add` は既定 skip へ倒す（安全側）。

## 5. 派生・後続

| 対象 | 内容 |
|------|------|
| **change-number-registry** | **v1.67.0**: 075 を **反映**へ。次 **076** |
| **followup** | file レベルエラーの日本語化（BE 小改修・§3-2）／親解決○ と default-skip commit の乖離の恒久対応（§3-3）／画面 chrome の一括 i18n 化（§3-4）。低・繰越 |
| **retrospective 一次資料** | **F-6** を記録（既存の不変条件〔VAL-S04〕を確認せず「振る舞いの分岐」を指示書に書いた＝F-1 と同型・製造エスカレーションで是正） |
| **M17 の残り** | **M17-05d**（指示書 v1.0.0 作成済＝投入可）。**05a 完了で M17-05 系は 05d のみ** |
| **開発者確認 2 件** | 通知書 §7（file レベル英語表示の当面許容・親解決乖離の注記対応） |

---

*以上、change-report-075。配置 `docs/change-notes/change-report-075.md`。三点セット（通知書＋改訂 DES-005 v2.46.0／DES-006 v1.20.0＋本レポート）完了。commit/push は開発者。*
