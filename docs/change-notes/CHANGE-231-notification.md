# CHANGE-231 設計変更通知書 — 不変条件のガードは版ではなく HEAD に対して置く（規約 (24)）

| 項目 | 内容 |
|------|------|
| CHANGE 番号 | **231** |
| バージョン | **1.0.0**（2026-09-19・起票＋同時反映） |
| 起票日 | 2026-09-19 |
| 起票者 | 設計担当 Claude（設計卓） |
| 由来 | **`M39-01`** の設計伝達レポート **§6-3**（指示書 §2.5 の見込みどおり） |
| 根拠裁定 | **`D-911`** |
| 改訂した設計書 | **`SUPP-001` 1.77.0**（§5.5 規約 (24) を新設） |
| スキーマ変更 | **なし** ／ マイグレ消費 **0 本** ／ API 経路 **0 件** ／ 新規依存 **0 件** |

---

## 1. ★★★2 度目だから規約にする

**★版数に固定したガードは、その版では成立していたことしか言わない。⇒ 後続の波で崩れても誰も気づかない。**

| 回 | 実測 |
|---|---|
| **1 度目** | **`D-714`＝版数を名指ししたテストが、マイグレ統合で一斉に壊れた** |
| **★★★2 度目** | **`D-187` の不変条件が v64 固定であったため、*132 行*の違反を 1 か月以上見逃した**（`M39-01`） |

---

## 2. ★★置き方（**4 点**）

| # | 置き方 |
|---|---|
| **★★★1** | **`TestRun_HEAD_*` 族へ置く。⇒ 版数を名指ししない** |
| **★★★2** | **破壊確認を対で置く。⇒ 1 行だけ故意に違反させると赤くなることを出力で示す** |
| **★★★3** | **★ガードが見ているのは「新規 DB の最終状態」であることを自覚する。⇒ *実行時に*破る経路は捕まらない** |
| **★★4** | **件数のハードガードをマイグレへ置かない。⇒ 実行時に増えうる違反を起動時に止めるとアプリが起動しなくなる** |

---

## 3. ★★★3 と 4 は `M39-01` が実測で足した

**★`M39-01` は段 1 で「`startup_basis` を*書く*経路」まで数え、ラッシュ版の生成経路が実行時に同じ不変条件を破れることを見つけた**〔HEAD に該当元技が 3 件実在〕**。⇒ followup `rush-variant-can-violate-d187-at-runtime`。**

**★★★だから 4 が要る。⇒ 件数を起動時に固定すると、その経路を 1 回通った利用者のアプリが起動しなくなる。★件数はヘッダコメントと常設テストで担保する。**

---

## 4. 影響

**★実装差分は 0 件。⇒ `M39-01` が既にこの形で置いている。**

---

*以上、CHANGE-231 **v1.0.0**。*
