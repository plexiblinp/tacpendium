# CHANGE-183: `SUPP-001` §5.5.9「ゲーム側アップデートに追従するときの配布物のデータ来歴の判定」を新設する（`M26-03`）

| 項目 | 内容 |
|------|------|
| CHANGE 番号 | **183** |
| バージョン | **1.0.0**（2026-09-11） |
| 起票日 | 2026-09-11 |
| 起票者 | 設計担当 Claude（設計卓／フェーズ4 期） |
| 由来 | **`M26-03` 設計伝達レポート §1-1 / §6**。置き場の裁定＝**`D-819`** / **`D-820`** / **`D-821`**。反映＝**`D-824`** |
| 対象設計書 | **`SUPP-001` §5.5.9**（新設）／ **§5.5.5** ／ **§5.5.6**（相互参照） |
| 反映 | **起票と同時に反映**（本書と同じ手番） |

---

## 1. なぜ要るか

**★★★SF6 のアップデートは繰り返し来る**（`followup` の `sf6-major-update-after-release`。**開発者の初出情報＝リリース数週間後に 1 キャラ追加 ＋ バトルバランス調整**）**。**

**⇒ 「配布物のうち何がカプコン由来か」の判定は一度きりではない。★結論だけ書くと、次の波で全数を最初から起こすことになる。**

---

## 2. 置き場の決まりかた（`D-819` 〜 `D-821`）

**★製造は 3 案を出した**〔A＝完了報告内 ／ B＝`docs/process/` に新設 ／ C＝`scripts/` に生成スクリプト〕**が、設計卓は 4 つ目を採った。**

| # | 決め手 |
|---|---|
| **★★★1** | **`SUPP-001` §5.5.5 が同型の運用手順を既に持ち、その末尾で「★記載先はここであり、`DES-003` §3.3 ではない。§3.3 は列の定義を書く場所であり、運用手順を書くと同じ情報が 2 か所に増える」と置き場の規約まで書いている** |
| **★★2** | **§5.5.6**（アプリ独自の格納・計測規約）**が「書かれていないと、次のアップデートで毎回再発明される」という同じ動機を持つ。⇒ 3 つ目として並ぶ** |
| **★3** | **新規ファイルを作らないので `CLAUDE.md` §10.Y の承認も `check-doc-inventory.sh` の例外表への追加も要らない** |
| **★★4** | **次のアップデートで `startup_basis` を引く担当は §5.5.5 を開く。⇒ その隣に在る。★案 B は「`followup` から索引を張る」という人の手番に依存する** |

**★★★番号は §5.5.9 である**（`D-820`）**。⇒ `D-819` は「§5.5.7」としていたが、§5.5.7 / §5.5.8 は既に埋まっていた。★製造が採番せず差し戻した。**

**★★挿入位置は §5.5.8 の直後**（2 つ目の §5.5.4 の手前）**である**（`D-821` 規則 B）**。⇒ 本書の節の並び順は番号順ではないため、位置を明示しないと末尾へ付く。**

---

## 3. 変更内容

### 3.1 §5.5.9 を新設（**76 行**）

| 節 | 内容 |
|---|---|
| **(a)** | **引き金 4 種**〔CSV の更新 ／ キャラ追加 ／ `docs/` に調査レポートが増えた ／ 許可リストの変更〕 |
| **(b)** | **母集団は `check-public-snapshot.sh --list` の実出力から起こす。★完了報告の写しを使わない** |
| **(c)** | **3 階層で降りる**〔グロブ単位 → 混在領域だけファイル単位 → CSV は列単位〕**＋ 走査は 2 本立て**〔全体は狭い網 ／ **自分が足した行だけは広い網**〕 |
| **★★★(d)** | **判断の分かれ目 9 件。⇒ ここが機械化できない部分であり、本節の値打ちである** |
| **(e)** | **前回値との差だけを見る。★件数には、その回の完了報告とレビュー報告そのものが含まれる** |
| **(f)** | **何を書き残すか。★公式の生値そのものは書き残さない** |

**★★★(d) の 1 が最も重い＝列の名前で判断しないこと。** `startup` のような公式にもある列名でも、**本プロジェクトでは実測が正であり、公式と系統的に食い違う。**

**★★(d) の 2＝`command` は情報内容が公式由来で、表記は本アプリのトークン語彙である。⇒ 「情報」と「表記」に分けないと、どちらかが嘘になる**（`D-823` / 計測点 `M-160` の是正と同じ整理）**。**

**★★★(d) の 9＝走査の母集団に自分の文書が入る。⇒ 「公式値が公開物に在るか」を調べる文書がその値を引用すれば、自分が該当件数を 1 つ増やす。★`M26-03` は完了報告とレビュー報告の両方で実際に踏んだ。**

### 3.2 §5.5.5 / §5.5.6 と相互参照を張った

**★3 つは同じ族である。⇒ アップデートに追従する手番では 3 つとも開くこと。**

---

## 4. 変更しないもの

| 対象 | 理由 |
|---|---|
| **`DATA-LICENSE.md` §4 ／ `REUSE.toml`** | **前者は層とデータの正本、後者はパス単位の宣言を書く場所である。⇒ 運用手順を書くと同じ情報が 2 か所に増える**（§5.5.9 末尾に明記） |
| **既存の §5.5.1 〜 §5.5.8** | **1 文字も触っていない**（§5.5.5 / §5.5.6 への相互参照の引用行を除く） |
| **2 つ目の §5.5.4** | **`CHANGE-181` の注記のまま** |

---

## 5. 影響

- **★`followup` の `sf6-major-update-after-release` の引き先は既に §5.5.9 である**（`D-820` で張り替え済み）
- **★★`M32`**（データ鮮度）**の投入時に本節が引かれる。⇒ 「更新の手順」を持つかの判断材料になる**
- **★§5.5.9 の (d) の 1 は `docs/seed-data/` の 2 本を参照するが、同ディレクトリは公開スナップショットから除外されている。⇒ 公開版では参照切れになる。★節自身がそれを明記しているので直さない**（`M26-02` の `target-combo-and-derived-flag-rules.md` と同じ扱い）

---

*以上、CHANGE-183 **v1.0.0**。* **★★本節の値打ちは (d) の 9 件である。⇒ (a)〜(c) と (e)(f) は手順だが、(d) は「機械化できないから人が判断する」箇所を名指ししている。★手順だけなら `scripts/` で足りた。⇒ (d) が在るからここに置く値打ちがある。**
