# change-report-105: `CHANGE-105` 反映レポート（M21-07 モーダル表示中の物理入力の遮断）

| 項目 | 内容 |
|------|------|
| 対象 CHANGE | **CHANGE-105**（通知書 `docs/change-notes/CHANGE-105-notification.md`。**2026-08-15 承認**＝**D-391**） |
| サブマイルストーン | **M21-07**（M21 レーンの最終サブ） |
| 反映日 | 2026-08-15 |
| 反映者 | 設計担当 Claude（M22 期・設計卓） |
| 一次源 | M21-07 設計伝達レポート（2026-08-15） ／ 完了報告 `docs/progress/M21-07-completion-report.md` ／ レビュー `docs/progress/m21-07-review.md` |
| 実装 | ブランチ `claude/m21-07-implementation-plan-w1mpnp` / `2ca934c`〜`bb5b769` の 5 コミット（基点 `8af2ca6`） |

---

## 1. 反映した設計書

| 文書 | 版遷移 | 反映箇所 |
|---|---|---|
| **`DES-005`** `docs/design/05-screen-design.md` | **v2.66.0 → v2.67.0**（第78版） | **§6.4.3 項目 4″ を追加** ／ **§6.4.3「画面上ボタンの点灯連動」へ 1 項目** ／ **§6.4.4 骨格表へ項目 8** ／ **§6.5.1 へ相互参照 1 行** |

**★他の設計書は変更なし**（起票時の見込みどおり）——`REQ-001` ／ `DES-001` ／ `DES-002` ／ `DES-003` ／ `DES-004` ／ `DES-006` ／ `SUPP-001`。**製造レポート §5 が「API を 1 本も呼び足していない・変えていない」ことを実測している**（フロントエンド完結）。

---

## 2. 起票内容と as-built の差（**★4 件。いずれも起票時に無かったものであり、指示書に反する判断ではない**）

| # | 起票 | as-built | 反映 |
|---|---|---|---|
| **2-1** | §2-a「軸は 1 つだけ置き、供給元ごとに置かない」 | **軸は 1 本だが、置き先の関数は 2 つ**（押下集合の合流点 ／ 操作の配送点）。**操作は押下集合を通らないため**（`DES-005` §6.5 が「操作用のキーが押下集合へ混ざらない」ことを要件として持つ） | **§6.4.3 項目 4″ へ書き分けた。** **★「1 か所」を「1 つの関数」と読むと操作の経路だけ穴が開き、しかも動作を見ても気づきにくい**（削除・保存が「たまに効く」形になる） |
| **2-2** | §2-b「観測は止めず、作用だけを止める」 | **点灯（画面上ボタンの点灯連動）も出さない。** **★開発者の実機確認（2026-08-15）で確定した**——**初版は「点灯は観測の表示なので出し続ける」と解釈していた** | **§6.4.3 の点灯連動へ 1 項目。** **★「観測を止めない」と「点灯を出す」は別の話であることも併記した**——**混同すると、次の担当が「観測を止めるな」を根拠に点灯を戻す** |
| **2-3** | §2-c「開いている間の入力を溜めて、閉じた瞬間にまとめて流さない」 | **開いた時点で既に進行中だった判定窓は破棄する**（起票は触れていなかった） | **§6.4.3 項目 4″ へ 1 文。** **★理由まで書いた**——**サンプルは入力の変化時にしか来ないため、マウスでモーダルを開いただけでは 1 サンプルも来ず、毎サンプルの抑止分岐だけでは間に合わない** |
| **2-4** | §2-d「モードから出るかは as-built」 | **モードからは抜けないが、前置き（ショートカット）からは抜ける** | **§6.4.4 項目 8 へ非対称とその理由を書いた。** **★「モードは残す」だけを書くと、前置きも残すのが正しいと読まれる** |

---

## 3. 起票どおりに反映したもの

| # | 内容 |
|---|---|
| 3-1 | **§6.5.1 は相互参照 1 行のみ。4 つ目の守りとして足さない**（**D-386 (6)**）。**理由も書いた**——**同節はキーボード経路に固有の守りを集めた節であり、モーダルの遮断は供給元を問わない合流層の話である。混ぜると次の担当が「キーボードだけの話」と読んでコントローラ側に穴を開ける** |
| 3-2 | **判定の手段（セレクタ・属性名）は書かない**（**D-380**）。**書くと実装の識別子が契約になる** |

**★ただし 3-2 には性質が伴うことを併記した**（製造レポート §1-2）——**手段が「参加が要る仕組み」である場合、網羅性は自動では保証されない。** **共通の部品を通らない自作の重なりは、自分で申告しない限り数えられない。** **⇒ 参加漏れを検出する仕組みまで含めて 1 組と考える。**

**★これは実際に穴として現れた**——**製造の網羅性の主張が「共有プリミティブを import するファイルの数」に基づいており、モーダルの数ではなかった**（レビュー高-1 で検出）。**自作オーバーレイが 2 件あり、うち 1 件は入力面と同居して欠陥がそのまま残っていた。**

---

## 4. 検証の証跡（製造レポート §5 の実測）

| 対象 | 結果 |
|---|---|
| `pnpm test` | **1412 passed / 151 files 全緑** |
| `pnpm lint` | exit 0 |
| `go test ./...` | **FAIL 0**（49 パッケージ） |
| `make e2e` | **107 passed / flaky 0** |
| `internal/` ／ `migrations/` ／ `character_data/` ／ `scripts/` ／ `docs/design/` | **diff 0 を実測**（基点 `8af2ca6` との比較で出力なし） |
| 判定層（`stepDetection.ts` / `recipeInputResolution.ts` / `normalize.ts`） | **diff 0**（判定窓・写像・解決規則は不変） |
| 供給元（`useKeyboardInput.ts` / `useGamepadPolling.ts` / `useStepDetection.ts`） | **diff 0**（**★軸の条件が供給元へ入っていない**＝最重要ゲートの実測） |
| 新規 test-id | **0 件** |
| マイグレ消費 | **0 本**（disk 末尾 `000077`。**次に払い出す番号は `000078`**） |
| 開発者の実機確認 | **手順 A〜D 実施済み。点灯の是正後も再確認済み** |
| レビュー | **往復 1 回**（上限 2 回・未到達）。**指摘 10 系統中 9 件採用**（不採用 1 件＝低-2 は followup へ。**「高」の不採用 0 件**） |
| 停止時記録（`followup-backlog` §J） | **0 件** |

---

## 5. 併せて行った同期

| # | 対象 | 内容 |
|---|---|---|
| 1 | `docs/handover/change-number-registry.md` | **`105` を「反映済み」へ** |
| 2 | `docs/instructions/M21-overview.md` | **M21-07 行を完了へ。⇒ M21 レーンは全 7 サブ完了** |
| 3 | `docs/handover/followup-backlog.md` | **`physical-input-active-while-modal-open` をクローズ** ／ **新規 3 件を §M として登録**（製造レポート §4-1） |
| 4 | `docs/process/parallel-board.md` | **裁定 D-397** ／ §1.6 ／ §2.4 |
| 5 | `docs/human-notes/model-allocation.md` | **本サブの追記は設計卓の手番として残る** |

---

## 6. 記録に値する経緯

**★指示書 §10-3 の前提が実物と違っていた**（製造レポート §4-2）。**同項は「`physical-input-active-while-modal-open` は §K と §J の 2 か所に重複行があり、統合も設計卓の手番である」と書いていたが、実物には §K の 1 行しか無かった。** **⇒ 統合は既に済んでおり、閉じるのは 1 行だけでよかった。**

**★これは設計卓が「重複がある」と断定した誤りである**（`E-117` の型）。**本レーンで 4 件目**（`D-390` の監査ログ ／ `D-392` の `FR407` 優先度 ／ `D-393` の QR 未実装 ／ 本件）。**⇒ 現物を持たない側が、もっともらしい再構成を根拠として書く経路が繰り返し出ている。**

---

*以上、change-report-105。配置 `docs/change-notes/change-report-105.md`。`CHANGE-105` は本レポートをもって反映完了。*
