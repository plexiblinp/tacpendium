# CHANGE-174 設計変更通知書: コンボ一覧の列の並びと行内リンクの位置（`M31-03`）

| 項目 | 内容 |
|------|------|
| 文書ID | CHANGE-174 |
| バージョン | **1.0.0**（2026-09-09） |
| 起票日 | 2026-09-09 |
| 起票者 | 設計担当 Claude（設計卓／フェーズ4 期） |
| 由来 | **`M31-03`**（一覧の列の並び ＋ 詳細への行内リンクの位置）**の設計伝達レポート §6 / §1-2 / §4** |
| 対象設計書 | **`DES-005`** §5.4（コンボ一覧）／ **`SUPP-001`** §5.3 相当（フロント側の注記） |
| 状態 | **★★起票と同時に反映する**（`D-796`） |
| マイグレ消費 | **0 本** |
| 新規依存 | **0 件** |

---

## 0. ★★本書の 6 件はすべて同じ型である

**★★「実装が変わったのに記述が旧のまま」。⇒ 動作は正しいままなのでテストも `tsc` も緑になり、人が読む以外に見つける経路が無い。**

**★★★実際に `M31-03` の製造が自分で踏んだ**（本書 §1-6）**——E2E の期待値を書くとき「ステータス列はマイコンボ専用」と思い込み、テストが赤くなって初めて気づいた。⇒ 次の担当も同じところで数え直す。**

---

## 1. ★★`DES-005` §5.4 の 6 件

### 1.1 「アクション」欄——ルート列は詳細へのリンクである

| 現在 | あるべき姿 |
|---|---|
| **「ルートのホバー/タップ → 全文表示」** | **★ルート列は詳細へのリンクである。⇒ タップは遷移。★全文はホバー**（`title`）**のみ** |

**★経緯＝`M27-03` が「始動状況」1 列を 3 列へ割った結果、行内リンクがヒット種別の列に載っていた。⇒ 開発者の裁定でルート列へ移した**（2026-09-09）**。**

### 1.2 「レスポンシブ」欄（スマホ）——同型

| 現在 | あるべき姿 |
|---|---|
| **「ルートはタップで全文モーダル」** | **★同上。⇒ タップは遷移である** |

**★★§1.1 と §1.2 は同型である。⇒ 片方だけ直すともう片方が残る。**

### 1.3 「各コンボの表示項目（横並び）」の並び——**案 C**

| 現在 | あるべき姿（**案 C**） |
|---|---|
| 展開アイコン → 状況 3 列 → ダメージ → ルート → タグ → 登録状態 → 備考 | **展開アイコン → ルート → ダメージ → ヒット種別 → 始動位置 → 相手の状態 → タグ → 登録状態 → 備考 → セットプレイ数** |

**★開発者が段 3 のモックを見て選んだ**（2026-09-09）**。**

### 1.4 「表示項目の『既定』」——旧順 ＋ 存在しない列名

| 現在 | あるべき姿 |
|---|---|
| **「既定で出るのは 始動状況 ／ ダメージ ／ ルート ／ タグ ／ 登録状態」** | **★★「始動状況」は存在しない。⇒ 3 列**（ヒット種別 / 始動位置 / 相手の状態）**である。★順は案 C へ** |

### 1.5 「セットプレイ展開時の表示例」の ASCII 図（2 行）——同型

| 現在 | あるべき姿 |
|---|---|
| `[始動状況/ダメージ/ルート/タグ/備考]` | **★案 C の順へ。★「始動状況」を残さない** |

### 1.6 ★★「ステータス」列の記述が無い

**★コンボ一覧にもステータス列が出る**（`ComboListPage` が `onStatusChange` を渡す）**。⇒ 本書が §5.4 へ請求する唯一の手番であるため、一緒に出す。**

**★★`M31-03` が作った差ではない。⇒ 元から書かれていなかった。**

---

## 2. ★★`SUPP-001` へ 1 行——列の並びの正本は 3 か所に分かれている

**★★`COLUMN_DEFINITIONS` が持つのは表示列メニューの並びと `colSpan` の計数だけである。⇒ 表の並びは `ComboTable` の見出し JSX と `ComboTableRow` のセル JSX に別々にハードコードされている。**

**★★★1 つだけ直すと見出しとセルがずれるが、`tsc` もテストも検出しない。**

**⇒ 恒久の規約＝一覧の列の並びを変えるときは 3 か所を同時に直すこと。**

**★`M31-03` が `ComboTable.test.tsx` へ 2 つの検査を足した**〔**3 か所が一致すること ／ 検査の母集団が全列であること**（**列を足して検査から漏れるのを防ぐ番人**）〕**。★ただし検査の存在を知らないと「3 か所ある」ことに気づけない。⇒ だから設計書に 1 行要る。**

---

## 3. ★本書が変えないもの

| # | 何を | なぜ |
|---|---|---|
| **★★1** | **ブラウザストレージのキー `combo-list-columns-v1`** | **★★バージョンを上げていない。⇒ 保存値は `ColumnVisibility`**（boolean 9 個の JSON オブジェクト）**だけであり、並びの情報を持たない。★台帳も不変**（`check-browser-storage-keys.sh` 緑） |
| **★★2** | **`ColumnVisibility` の型・キー名・`DEFAULT_COLUMN_VISIBILITY` の値** | **列の増減が無い** |
| **★3** | **行アクションの出し分け**（`showCopy`） | **`CHANGE-164` §3.1 が確定させたばかりである** |
| **★★4** | **行内リンクの実装構造**（`DETAIL_LINK_COLUMN` 定数） | **★実装の内部構造であり契約ではない。⇒ 設計書へ書かない** |
| **★5** | **レシピが空のときの行内リンクのアクセシブル名** | **★§4 の判断。⇒ 現在の挙動はテストで固定済みであり、契約化しない** |

---

## 4. 反映先の一覧

| 設計書 | 節 | 内容 |
|---|---|---|
| **`DES-005`** | **§5.4** | **§1 の 6 件** |
| **`SUPP-001`** | **フロント側の注記** | **§2 の 1 行** |

---

*以上、CHANGE-174 **v1.0.0**。* **★★本書の 6 件はすべて「動作は正しいのに記述が旧のまま」の型である。⇒ テストも `tsc` も緑になるため、人が読む以外に検出経路が無い。** **★★最も重いのは §1.4 と §1.5 である**——**「始動状況」という列は `M27-03` 以降 存在しないのに、設計書は 3 か所でその名を使い続けていた。⇒ 存在しない列名を読んだ担当は、無い列を探すことになる。**
