# parallel-board 裁定ログ アーカイブ（M22 境界）

> **本文は書き換えない**——裁定行は当時どう判断したかの記録である（D-57／D-259）。現役の裁定は `docs/process/parallel-board.md` §3。

<!-- 退避: M22 境界 -->

| # | 日付 | 裁定 |
|---|---|---|
| D-352 | 2026-08-13 | **【`M21-03` §4.2-2 は §4.1-2 および契約 F-3 と両立しない。製造の (1)(2) を受理して差し替える。設計卓の誤りである】**（**製造が実装中に検出**。**M-49 / M-51 に続く「同じ指示書の中で 2 つの節が両立しない」型の 3 件目**） **★結論＝製造の暫定案 (1)(2) をそのまま採る。別の意図は無かった。** **★§4.2-2 の「2 ボタン同時は OD 技（`_od`）」は、設計卓が実装の入口を確認せずに書いた。** **格闘ゲームの通念としては正しい**〔SF6 で OD は同強度 2 ボタン同時である〕**が、通念が正しいことと、その写像の入口が実装に在ることは別である。** **実際には OD の `move_code` は `<family>_od` であり、family が決まらないと確定しない。** **既存の解決経路は方向テンキー ＋ LP/MP/HP/LK/MK/HK の `token_key` しか引かず OD のキーを持たず、必殺技の OD は利用者がパネルでファミリーを選んで初めて確定する。** **⇒ 成立させるには「物理入力からファミリーを選ぶ」新しい解決規則が要り、§4.1-2 の新設禁止と契約 F-3 に正面から抵触する。** **★製造の判断は正しい。** **動かせる形**〔独断で family 選択を書く〕**を採らず、止めて上げたことも正しい。** **★受理した形**——**(1) 同じ強度の P＋K を既存のシステム技へ写す**〔強P＋強K → `drive_impact` ／ 中P＋中K → `drive_parry` ／ 弱P＋弱K → 投げ〕**／(2) PP・KK・強度をまたぐ組み合わせ・3 ボタン以上は「解決できない入力」として読取表示に出す。** **★(1) が新設規則にならない理由を明文化した**——**仮想 UI の共通技エリア**〔`DES-005` §6.4 の SystemRow〕**に同じ操作のボタンが実在し、そこから `move_code` への写像は既存である。** **物理入力は「その操作を押した」ことを決めるだけであり、実機のボタン割当と同じ形でもある。** **⇒ §4.1 へ規約 5 を新設した**——**★「物理入力が決めるのは、仮想 UI のどの操作に当たるかまでである。操作 → `move_code` は既存が持つ」。** **これが本サブの境界線の正確な言い方であり、初版の「どのボタンが押されたか」より 1 段はっきりしている。** **★OD の扱いを確定した**——**本サブでは物理から出さない。** **既存の必殺技パネル**〔仮想〕**で入れる**〔混在は §4.2-6 が保証している〕。**物理からの確定は `M21-06` が最初に解ける場所になる**——**family を決める情報は方向の連続入力から来るのであり、それはモーション解析そのものである。** **⇒ `M21-06` の入力材料として §10 へ申し送った。** **★設計卓の誤りの型を記録する**〔**M-55**〕——**`E-117` の同型である。** **現物を持たない側が、通念を根拠に具体名**（`_od`）**を書いた。** **具体名を書くと、実行者は「既存にあるはず」と読んで探し、無いと分かった時点で 2 つの節のどちらに従うかを独断で決めることになる。** **⇒ 機構化**——**指示書テンプレート §4 の記入指針へ入れた。** **「既存の実装を呼ぶ」と書いた節と、具体的な写像を指定する節が同居したら、その写像の入口が実在するかを起票時に確認する。** **確認できないなら §3.3 へ実査項目として置き、無かった場合の停止条件を対で書く。** **★本サブへも同じ手当てを入れた**——**§3.3-4 を「共通技の既存の写像がどこにあるか」へ書き換え、停止条件 5**〔**既存の入口が無いなら自分で書かず止めて報告する**〕**を新設した。** **★発行物の改訂**——**指示書 v1.1.0 → v1.2.0** ／ **チェックリスト v1.1.0 → v1.2.0**〔**§1.3 を全面差し替え。旧項目「2 ボタン同時が OD 技へ写っている」のまま判定すると、正しい実装が「未実装」と判定される**＝**D-323** の同型〕 ／ **`CHANGE-111` §2 へ g を追加。** **★投げの向きは `throw_forward` を既定とし、方向が後方のときの `throw_back` を採ってよい**〔§9.2-7。**実機のボタン割当と同じ形であり推測ではない**〕。 |
| D-351 | 2026-08-13 | **【`M20-04` の指示書は実質改訂を要しない。ただし非干渉宣言だけは片側で足りない】**（開発者質問「セットプレイの入力面も対象にしたいが、`M20-04` の指示書は修正対象にはならない形ですか？」） **★結論＝仕様・スコープ・成果物は 1 文字も変わらない。** **`M20-04` はもともと `web/src/features/setup/` を触らない**〔触るのは `internal/service/preset/`・`internal/repository/preset/`・`internal/api/preset/`・`web/src/pages/` の新規 2 面・`web/src/features/preset/`・router と Header〕。**`D-350` が広げたのは `M21-03` の側だけであり、面は依然として交差しない。** **★しかし 1 か所だけ足りなかった**——**非干渉宣言である。** **`M20-04` の §2.2-5 と §7.3 は `web/src/features/gamepad/` の diff 0 しか宣言していなかった。** **`M21-03` が `web/src/features/setup/` にも触るようになった以上、こちらも宣言しないと、交差の検出が片側だけになる。** **⇒ `web/src/features/setup/` を足した**〔指示書 v1.0.0 → v1.0.1 ／ チェックリスト v1.0.0 → v1.0.1〕。 **★一般則**——**並列サブの一方の触る面が広がったら、他方の「触らない」の列も同じ手番で広げる。** **diff 0 の宣言は対称でないと機能しない**——**片側だけが宣言していると、宣言していない側が踏んだときに誰も検出しない。** **★これは `E-14` 交差表の運用そのものである**〔`M20-overview` §5.1 ／ 教訓 **E-121**。**同時に発行した指示書は、他の並列サブが後から広がることを知らない**〕。**交差表は「編成時に 1 回書く表」ではなく、片側が動くたびに引き直す表である。** **★`D-349` の並列可否の判断は変わらない**〔面は交差しない〕。**変わったのは列挙の内容だけである。** |
| D-350 | 2026-08-13 | **【`M21-03` はセットプレイの入力面も対象とする。ただし接続層を共有部品にすることを条件とする。`CHANGE-101` / `CHANGE-111` は承認された】**（開発者判断 2026-08-13＝「セットプレイの入力面も対象にしたいのですが、共通部品化などによる対応は厳しいですか？」／「セットプレイ入力面の件以外は承認します」） **★結論＝広げる。設計卓の推奨**〔レシピ面のみ〕**は採られなかった。開発者の判断のほうが正しい。** **理由 2 点**——**(1) 「コントローラで入力できる面とできない面がある」状態は利用者から見える**〔設計卓自身が §11 の確認事項でハード列と認めていた〕／**(2) 後から広げるほうが高くつく。** **1 面ぶんの接続を書いてから 2 面目を足すと、先に書いたものが「面に癒着した形」になっており、剥がす作業が増える。** **最初から部品にするほうが安い。** **★共通部品化は「厳しくない」——ただし条件を付けた。** **`code-facts` §3 の実測では 2 面の props が違う**〔レシピ側 `steps: Step[]` ＋ `moves` ＋ `movesLoading` ／ セットプレイ側 `steps: SetupStepInput[]`〕。**⇒ 型は違うが、違うのは「面が持つステップの形」であって「入力から `move_code` を決める過程」ではない。** **⇒ 接続層を headless な部品**〔描画を持たず、入力 → 確定したステップ候補だけを供給する〕**にし、面ごとのステップ型への写し替えは呼び出し元が行う形にした**〔指示書 §4.9〕。 **★条件を付けた理由は、部品化しないと死守契約の検証点が 2 つに割れるからである。** **面ごとに接続を書くと `move_code` を決める箇所が 2 つになる。** **そのときは動く。** **壊れるのは段階2 の解決表やラッシュ版の規則が更新されたときであり、片方だけ追随して他方が「動いているが古い規則で解決している」状態になる。** **エラーにならない。** **⇒ 契約 F-3 が守られているかを、以後だれも 1 か所では確認できなくなる。** **★逃げ道を 1 つ用意した**〔停止条件 4〕——**2 面の差が構造的で 1 つの部品に収まらないと分かったら、面ごとに 2 本書くのではなく止めて報告し、セットプレイ側を別サブへ送る。** **「差が大きかったので 2 本書きました」は採らない。** **★差の実査を §3.3-9 に足した**〔ステップの型 ／ `modifiers` を持つか ／ 選べる技に制限があるか ／ `onChange` の形〕。 **★告知にも同じ規律を掛けた**——**同じ文言を 2 か所へ書き写さない**〔`E-76`〕。 **★発行物の改訂**——**指示書 `M21-03` v1.0.0 → v1.1.0** ／ **レビューチェックリスト v1.0.0 → v1.1.0**〔§1.7 新設・重大 8 追加〕 ／ **`CHANGE-111`**〔承認者欄・§2 へ e′ を追加・§3 の判断 4 を反転・§4 へリスク 1 行〕。**想定所要時間を 150〜210 分 → 180〜240 分へ。** **★`CHANGE-101`**（`M20-04`）**も承認された。** **§6 の注記への回答＝「M20-04 用の DB で起動するので問題ないはず」。** **⇒ 実機確認で既存データが壊れる risk は無い。** **★ただし停止条件 1 は残した**——**同条件が問うているのはデータの安全ではなく「作れるが表示できない状態で完成としてよいか」であり、専用 DB でも同じことが起きる**〔`recipe_cache` は新しいプリセットのエントリを持たない〕。**実査で壊れないと分かればそのまま進む。判断が要るのは壊れた場合だけである。** **⇒ 両サブとも投入可。** |
| D-349 | 2026-08-13 | **【`M20-04` と `M21-03` の三点セットを起票した。並列投入してよい。CHANGE の見積りが 2 本とも外れており、両 overview を是正した】**（開発者指示 2026-08-13＝「M20-04 と M21-03 の起票は許可します」） **★発行物 6 点**——**`M20-04`**〔指示書 `M20-04-preset-management-ui.md` v1.0.0 ／ レビューチェックリスト v1.0.0 ／ **`CHANGE-101` 通知書**〕／**`M21-03`**〔指示書 `M21-03-recipe-input-and-readout.md` v1.0.0 ／ レビューチェックリスト v1.0.0 ／ **`CHANGE-111` 通知書**〕。**registry v1.117.0 ／ `m20-contract` v1.6.0 ／ `m21-contract` v1.6.0 ／ `M20-overview` v1.3.0 ／ `M21-overview` v1.2.0。** **★(1) 並列可否＝可。面を突き合わせた。** **`M20-04` が触るのは `internal/service/preset/`〔新設〕・`internal/repository/preset/`・`internal/api/preset/`・`web/src/pages/` の新規 2 面・`web/src/features/preset/`・router と Header。** **`M21-03` が触るのは `web/src/features/gamepad/` とレシピ入力面。** **【2026-08-13 errata】★本行の列挙は当日中に失効した**——**`D-350` により `M21-03` はセットプレイの入力面**（`web/src/features/setup/`）**にも触る。** **並列可否の結論は変わらない**〔`M20-04` は同ディレクトリを触らない〕**が、非干渉宣言を対称にする是正が要った**〔**D-351**〕。 **⇒ `internal/` は交差しない**〔`M21-03` は契約 F-1 で `service/preset` を触れない〕。**`web/` も features 配下が別である。** **★交差しうる唯一の点は `web/` の共有部分**〔router / Header〕**だが、`M21-03` はルートを足さない見込みである。** **両指示書の §7.3 に相互の diff 0 を入れた。** **★マイグレ連番の競合も無い**——**両サブとも 0 本の見込み**〔`M20-04` は `presets` / `preset_aliases` とも既存の列で足り、`M21-03` は非スキーマ〕。**次に払い出す番号は `000076`。** **★共有ファイルの衝突点は `D-334` と同じ 2 つ**〔`progress-log` の索引行は両方残す ／ ベースライン定数は **D-335** により両サブとも触らない〕。**`DES-005` は両サブが触るが節が排他である**〔`M20-04`＝§5.10・§5.11 ／ `M21-03`＝§6.4・§6.4.2・§6.6。**契約 §2 の節単位排他**〕。 **★(2) CHANGE の見積りが 2 本とも外れた。** **`M21-03`**——**`M21-overview` は「なし〜軽微（`CHANGE-107` に同梱しうる）」としていたが、107 は同日に反映済みになっており、反映済みの CHANGE へ後から内容を足すと change-report と設計書の対応が崩れる。** **⇒ 新規に 111 を払い出し、M21 ブロックの余裕は 0 本に戻った**〔**M-54**〕。**★一般則**——**「後続へ同梱しうる」という見積りは、同梱先が閉じる前に後続が来ることを前提にしている。** **並列レーンでは先行サブのほうが早く閉じるため、この前提は壊れやすい。** **`M20-04`**——**`DES-005` ＋ `DES-002` ＋ `DES-006` の 3 文書に及ぶが 1 本に束ねた**〔**D-323**。**CHANGE の単位は文書ではなく変更である**〕。**overview の見込み「DES-005（1 本）＋ DES-002（1 本）」は 2 本を想定していたが、1 本で足りる。** **⇒ M20 ブロックの残りは 102〜105 の 4 本。** **★(3) `M20-overview` §3 の M20-05 の行が不正確だった**〔**M-53**〕——**「preset サービス層の新設」を M20-05 に置いていたが、層そのものは書き込み経路を持つ `M20-04` が作らざるを得ない。** **M20-05 が足すのはその層へのトリガの配線である。** **⇒ 両サブの境界を `M20-04` 指示書 §4.1 に表で書き、overview 本文も正した**〔**D-274** の分類 (1)＝**生きた設計文書は本文を正本へ揃える**〕。**★放置していたら製造が「層はまだ無いはずだが自分が作ってよいのか」で止まっていた**〔**E-105** の型＝**発行側が書かないと適用側が不要に止まる**〕。 **★(4) 両サブの最重要ゲートを記録する。** **`M20-04`＝`character_id` を必ず入れること**〔**nullable ゆえ入れ忘れても落ちず、制約に当たらない行ができる**。**M20-03 の申し送り §4-3 をチェックリストの重大 1 に置いた**〕**／保護はサービス層で行うこと**〔UI だけだと API 直叩きで壊れる〕**／削除は CASCADE に頼らないこと**〔**P-04** により発火しない〕。**`M21-03`＝出口を `move_code` に保つこと**〔契約 F-3 の死守契約。**物理側に独自の解決経路が生えても動いてしまうため、テストでも lint でも捕まらない。** **⇒ 「同じ入力を仮想 UI で行った場合と `move_code` が一致する」テストで固定させた**〕**／告知を必ず置くこと**〔**D-347** の帰結。**落ちても動く**〕。 **★(5) `M20-04` には着手後に開発者判断が要る点が 1 つある**〔停止条件 1＝**カスタムプリセットを作れるが `recipe_cache` が無いために表示が壊れる場合**。**実査の結果を見ないと立てられないため着手前ゲートに含めていない**＝`E-55`〕。**`M21-03` には確認事項が 1 件ある**〔**セットプレイの入力面を対象外としてよいか**。**利用者から見える差であるためハード列**〕。 |
| D-348 | 2026-08-13 | **【M20-03 の §2-1・§2-2 を受理する。指示書 §2.2 に第三の分岐が無かったのは設計卓の書き落としである。あわせて `CHANGE-100` の三点セットを反映した】**（設計伝達レポート `20260813-m20-03-design-exceptions.md` の処理） **★§2-1＝受理する。** **生成器の出力形式に「旧形式」を named option として残した形を、正式な型として承認する。** **★開発者は Plan Mode で他案の実測値**〔testdata 凍結は約 570 KB の複製〕**を見た上でこれを選んでいる**（U-3）。**設計卓も同じ結論である**——**却下側**〔golden の退役〕**は、適用済みマイグレ 6 本の手編集ドリフト検出を永久に失う。** **golden の存在理由そのものが「生成器の出力とコミット済みマイグレが byte 一致すること」であり、そこを外すと残るのは名前だけになる。** **★そして設計卓の書き落としを認める。** **指示書 §2.2 の但し書きは「本サブが正本を是正する場合」の三点更新しか分岐を持たず、しかも「本サブは正本を是正しない見込み」と明記していた。** **見込み自体は正しかった**——**CSV は 1 バイトも触られていない。** **落ちていたのは第三のケースである**——**正本が無傷のまま、生成器の出力形式が変わることだけで golden が落ちる。** **⇒ 指示書テンプレート §2.2 の記入指針へ第三分岐を足した**〔**旧形式を named option で保存する** ／ **判別はフラグではなく出力先から自動判別する**——**フラグは付け忘れた実行が適用済みファイルを新形式で上書きする** ／ **採らない形＝golden 退役・testdata 凍結・差分無視の比較**〕。**★これは `D-336` の再演である**——**同じ §2.2 が、2 サブ連続で分岐不足を露呈した。** **1 度目は「正本を是正する場合」、2 度目は「出力形式を変える場合」。** **⇒ 生成物を持つコードを触るサブでは、`§2.1` の一覧作成時に「この変更で出力が変わるものを全部挙げよ」を回すことも同じ記入指針へ入れた**〔**生成器・CLI・golden・doc コメント・失敗メッセージは 1 組である。** **M20-03 は CLI**〔`cmd/seedgen -check`〕**を落としたまま `go test ./...` が全緑だった**——**CLI のゲートはテストから呼ばれない**〕。 **★§2-2＝受理する。線を 2 本引く。** **(1) §2.1 の成果物一覧は「予見できた範囲」であって禁止列ではない。** **禁止列は §2.2 である。** **⇒ 一覧に無くても禁止列に当たらないなら触ってよく、事前記録は要らない。理由を完了報告に書けば足りる**〔製造の処理は正しい〕。**(2) `character_data/seed-progress.md` への書き込みは、列によって分ける。** **`seed_imported` 等の「状態を表す欄」は開発者の手番であり製造は触らない**〔**M20-02 §3-1 の裁定を維持する**〕。**一方、手順本文は、自分の変更がその手順を失効させた場合にかぎり製造が直してよい。** **★理由**——**失効させた本人が直すのが最も安く、他の誰も失効に気づけない。** **本件では衝突の失敗の形が「静かな欠落」から「適用時 UNIQUE 違反」へ変わっており、無改訂だと次の新キャラ波で開発者が誤った期待を持つ。** **★`internal/seedgen/generate_m1702_test.go`**〔M17-02 の資産〕**も同型で、失敗メッセージが適用済みマイグレの上書きへ誘導する状態だった。触って正しい。** **★一般則**——**「このメッセージに素直に従った人が何をするか」を、書かれた時点ではなく現在の状態で読み直す。** **M14 期に書かれた時点では正しかった。** **★三点セットを反映した**——**改訂 `DES-003` 1.50.0 → 1.51.0**〔第53版。**§3.9 へ `character_id`**〔**nullable・既定値なし・FK なし**〕**＋ UNIQUE 制約 3 本の表**〔**既存 `(preset_id, move_id)`＝1 技 1 表記 ／ 新設＝2 技 1 表記の禁止 ／ 部分インデックス**。**向きが逆の別制約であり両方要る**〕**＋ 索引として張る理由**〔**SQLite に `ALTER TABLE ADD CONSTRAINT` が無く、テーブル再構築は既存 DDL の写し落とし事故を持ち込む**〕**＋ 714 件の正体 ＋ 逆引き 1:N の書き分け**〕 ／ **改訂 `DES-004` 1.18.0 → 1.19.0**〔第20版。**§5.7 新設＝投入経路の規約 4 点**〕 ＋ **`change-report-100.md`** ＋ **registry v1.116.0** ＋ **`m20-contract` v1.5.0** ＋ **`SUPP-001` v1.33.0**〔§5.5.4 (12)＝索引名の接頭辞 `ux_`。**§4-2 の followup 起票は不要になった**〕。 **★§1-2 が最も重い as-built である**——**`preset_aliases` へ書く本番コードの経路は実測 0 件だった**〔リポジトリ層は読み取り 6 メソッドのみ〕。**⇒ 「投入は生成器を通す」の規約は、守られているのではなく、まだ一度も試されていない。** **通知書 §2.2 が「as-built を見てから文言を確定する」と保留したのは正しかった。** **⇒ 規約を 1 点から 4 点へ拡張した**——**とくに「`character_id` を必ず入れる」が要る。** **「生成器を通す」だけでは M20-04 が作る新経路を縛れず、`character_id` を落とした行は落ちない**〔**nullable ゆえ制約に当たらないだけである**〕。 **★§4-1＝本書 §2.2 の連番が失効していた。是正した**〔次に払い出す番号 `000074` → **`000076`**、disk 末尾 `000073` → **`000075`**〕。**M20-02 に続き 2 サブ連続で製造のレポートが検出している**〔**D-337** と同型〕。**⇒ 消費したサブの完了時に必ず動く欄であり、レポート経由で戻す設計が 2 回とも正しく働いた。** **★§4-3＝`M20-04` の指示書に「`preset_aliases` へ INSERT する際は `character_id` を必ず入れる」を明示要求として書く**〔起票は待機中＝**D-346**。**規約側だけだと読み落とされる**〕。**★§4-4＝衝突組と `P-34` の投入時は事前に違反検査を回す**〔`DES-004` §5.7-4 へ規約として書いた〕。**★§4-5＝`markdown-it-py` はクラウド実行環境のイメージに入っていない。両レーンのマージ後に扱う**〔followup `markdown-it-py-missing-in-cloud-env`。**開発者は devContainer の修正を後回しにする方針**〕。 |
| D-347 | 2026-08-13 | **【M21-02 の §2-1 を受理する。「その時点」を定義しなかったのは設計卓の書き落としである。あわせて `CHANGE-107` の三点セットを反映した】**（設計伝達レポート `20260813-m21-02-design-exceptions.md` の処理。**製造が唯一「親の裁定を要する」として上げた項目**） **★結論＝受理する。差し戻さない。** **as-built＝方向は窓の起点で 1 度だけ引くのではなく、窓の内側で観測しつづけ、最後の非ニュートラルを採る。** **根拠 3 点**——**(1) `D-324` の実測が直接支持する**〔**押し始めのズレは中央値 8.30 ms・最大 45.80 ms。レバーレスでは方向も物理ボタンであり、ズレは方向にも等しく効く。** **起点固定では `6` と強Pを同時に押して `6` が数 ms 遅れただけで「ニュートラル ＋ 強P」として確定する**〕／**(2) 指示書 §4.1-2 に抵触しない**〔**方向は依然として集合に加わらず、方向の変化だけではステップが生まれない。** **「まとめる対象にしていない」は保たれている。** **レビュー担当も独立に同じ判断に達している**〕／**(3) 実機確認で受理側の挙動が正しく出ている**〔開発者・2026-08-13〕。 **★そして設計卓の書き落としを認める。** **指示書 §4.1-2 は「その時点の方向 ＋ まとまったボタン集合」と書いて「その時点」を定義しなかった。** **⇒ 実装が定義せざるを得ず、初版が「窓の起点」を採って欠陥が入った**〔レビュー指摘 1（高）が検出〕。**★一般則**——**時間窓を持つ仕様は、窓の長さ・起点・境界の 3 点は書かれやすいが、窓の内側で変化する状態をどの時点で採るかが落ちやすい。** **「その時点」「現在の」「直後の」のように時点を指す語は必ず定義する。** **★§1 の 4 点も DES へ落とした**〔通知書 §2 へ h〜k として追加〕——**h 判定はデジタル**（`pressed`）**でアナログ値を使わない**〔**使うと「どこから押下とみなすか」という第 2 の閾値が要る**〕／**i 境界は半開区間 `[t0, t0 + W)`** ／**★j 下限は「実測最大のうち大きいほう」＝ 83.30 ms**〔**押し始め 45.80 と離し始め 83.30 の 2 つがあり、判定に押し始めを使う以上、押し始めの最大値を採るのが自然に見える。** **そして自然に見える読みのほうが silent な失敗へ向かう**——**窓を狭める誤りだけは利用者が気づけない**〕／**k `closedAt` は「窓が閉じた時刻」であって「閉じたことに気づいた時刻」ではない**〔M21-03 がステップ間隔の算出に使う〕。 **★§4-2 を採り、§2-c へ「silent でないことは、気づかれることを保証しない」を足した。** **§2-c の論理**〔読取表示があるから広すぎる誤りも画面に出る ⇒ 保守的に広く置いてよい〕**は正しかった**——**実機で実際に画面へ出た。** **足りなかったのは、出ていることと利用者がそれを予期していることは別だという 1 点である。** **⇒ 一般則**——**「画面に出るから安全」を根拠に閾値を緩めたら、同じ手番で「利用者にそれを事前に伝える場所」も決める。** **決めないと根拠だけが残って告知が落ちる。** **★§4-1 の告知要求は `M21-03` の入力材料として `DES-005` §6.4.2 へ書いた**〔まとまりうること ／ ゆっくり正確に入力すること ／ **不具合ではなく取りこぼしを防ぐための意図的な設定であること**の 3 点。**本サブの最小可視化は撤去前提であり告知の置き場にできない**〕。**★窓は狭めない**〔開発者判断＝「コンボの入力アプリという意味で、ゲームより緩い感度にするのは悪くない」〕。 **★§4-3 の承認者欄を確定させた**——**着手前ゲートは「`M20-03` と `M21-02` は並列投入します」の指示をもって通過している**〔**D-346**〕。**通知書の欄が起票時のまま残っていたため、三点セット発行と同じ手番で埋めた**〔**D-277** ＝**起票と登録を別の手番にしない**の同型。**欄の空白は「承認されていない」と読まれる**〕。 **★三点セットを反映した**——**改訂 `DES-005` 2.59.0 → 2.60.0**〔第71版。**§6.4 を as-built へ**〔**旧「時間窓（例：16ms）」は失効**〕／**§6.4.2 新設**〔判定の骨格 9 項目 ／ 誤りの非対称性 ／ SOCD ／ チャタリング ／ 未検証の範囲〕／**§6.6 から「OD 技などの同時押しの入力補助」を回収し、残り 4 件へ担当サブを明記**〕 ＋ **`change-report-107.md`** ＋ **registry v1.115.0** ＋ **`m21-contract` v1.5.0**。**マイグレ消費 0 本・スキーマ変更なし。** **★`DES-006` は起票時の判断どおり触っていない。** **★§4-4・§4-5 は `M21-03` の起票時の入力材料として持ち越す**〔**`StepCandidate` は SOCD 解決済みの `direction` しか持たず、M21-06 が生の上下左右を要するなら `NormalizeResult` まで配管を戻す必要がある** ／ **M21-03 の起票は開発者指示により待機中**＝**D-346**〕。**★§4-6 の 2 件は本サブ起因ではなく、両レーンのマージ後に設計卓が処理する**〔`check-browser-storage-keys.sh` の stderr 構文エラー ／ **`markdown-it-py` がクラウド実行環境に入っておらず `check-md-emphasis.sh` が実行できない**——**`.devcontainer/Dockerfile` へは導入済みだが効いていない。** **⇒ `check-artifact-integrity.sh` が違反 1 件を報告しつづける**〕。**★§4-8＝レビュー低指摘の繰越なし。往復 1 回・重大 0 件・13 件全採用で、停止規律の上限に未達。** |
| D-346 | 2026-08-13 | **【`M20-03` と `M21-02` は並列投入してよい。設計卓の手番を凍結し、状態を repo へ落とす】**（開発者指示 2026-08-13＝「M20-03 と M21-02 は並列投入します」／「M20-04 と M21-03 の起票は待機してください」／「PR を出して再度 main マージをします」） **★(1) 並列可否＝可。面を突き合わせた。** **`M20-03` が触るのは `migrations/` 2 本 ／ `internal/seedgen/` の生成器 ／ `internal/infra/migration/migrate_m2003_test.go` ／ 既存テスト**〔**`web/` は触らない見込み**＝指示書 §5.2〕。**`M21-02` が触るのは `web/src/features/gamepad/` 配下とそのテストだけ**〔**`migrations/` も `internal/` も触らない**＝`m21-contract` §4.2〕。**⇒ コード面は交差しない。** **★マイグレ連番の競合も無い**——**`M21-02` は 0 本である。** **`M20-03` が `000074` 以降を 2 本消費する見込みだが、`M21-02` は払い出しに参加しない。** **★共有ファイルの衝突点は `D-334` と同じ 2 つ**——**(1) `docs/progress/progress-log.md` の索引行**〔両方残す。片方を捨てない〕／**(2) ベースライン定数**〔**D-335** により両サブとも触らない。両指示書の §7.3 に明記済み〕。**`DES-005` は今回どちらも触らない**〔`M20-03` は `DES-003` ＋ `DES-004`、`M21-02` は `DES-005` §6.4 だが**本体の改訂は as-built 後**であり、製造は設計書を触らない〕。 **★(2) `M20-04` と `M21-03` の起票は待機とする。** **理由は開発者指示だが、設計卓の見立てとも一致する**——**`M20-04` は `M20-03` の as-built〔制約の実際の定義文〕に乗り、`M21-03` は `M21-02` の判定の公開インターフェースに乗る。** **先行起案すると差し戻しが増える。** **★(3) ベースライン定数の実測値を記録する**〔**D-335** の一括更新に要る〕——**`check-md-emphasis.sh`：実測 373 / 定数 375**〔**設計卓が本セッションで旧記述を置き換えた際に 2 行減った**〕／**`check-instruction-format.sh`：実測 74 / 定数 74**〔増減なし〕／**`check-enum-sync.sh`：定数 24・実測は warning 型のため別途**。**⇒ 両レーンのマージ後に `BASELINE_BROKEN` を 373 へ下げる。それまで触らない。** **★(4) `docs-map` を再生成した**〔6% ドリフト・57/914 件。**武装中でも再生成できる唯一の派生資料である**——`docs/` のみを読むため〕。**`code-facts` は武装中には再生成できない**〔実装ソースが作業ツリーに無く `generate-code-facts.sh` が exit 2 で停止する〕。**`custom-commands` は 21% ドリフトだが `/sync_command_catalog` の管轄で、設計卓の手番ではない。** |
| D-345 | 2026-08-13 | **【`M20-03` の三点セットを起票した。714 の読み違えを再発させない書き方にした】**（**D-344** の実施。**設計卓の手番**） **発行物 3 点**——**指示書 `docs/instructions/M20-03-alias-uniqueness-constraint.md` v1.0.0**〔**高精度実装・Plan Mode 必須・着手前実査 7 項目**〕／**レビューチェックリスト v1.0.0** ／**通知書 `docs/change-notes/CHANGE-100-notification.md`**〔起票・未反映〕。**registry v1.114.0 ／ `m20-contract` v1.4.0。** **★指示書 §1.1 とチェックリスト N-1 の両方に「714 の正体」を書いた。** **理由＝設計卓自身が読み違えた事実が直近にある**〔**D-343** → **D-344**〕。**レビュアーが同じ読み違えをすると、正しい実装が重大判定になる。** **⇒ チェックリスト §10 へ「714 件の重複を『問題』と読まないこと」を明記した。** **★破壊テストを重大 2 に置いた**〔`SUPP-001` §5.5.4 (10)〕——**一意制約は「張った」だけでは効いているか分からない典型である。** **同一 `(preset_id, character_id, alias_text)` で別 `move_id` を INSERT して失敗することを確認させる。** **★非正規化の担保をテストで固定させた**——**`character_id` が `moves.character_id` と全行で一致すること。** **複製は二重管理であり、固定しないとずれても気づけない。** **★生成器が `character_id` を出さない場合の危険を明記した**——**nullable なら NULL 行が入って制約をすり抜ける。落ちないため気づけない。** **`NOT NULL` で落ちるほうが安全である、という非対称を指示書 §4.5 に書いた。** **★`NOT NULL` の実現方法は製造の判断に委ねた**——**SQLite は `ALTER TABLE ADD COLUMN` で既定値なしの `NOT NULL` を許さず、テーブル再構築は既存 FK と参照側へ波及する。** **費用と risk は参照側の実物を見ないと判断できないため、§3.3-3 / §3.3-4 で実査させる。** **★停止条件 1 を「違反が 0 件でなければ止まる」にした**——**M20-02 の実測は 2026-08-13 時点であり、制約は張った瞬間に落ちる。** **独断で行を消させない。** |
| D-344 | 2026-08-13 | **【`D-343` の分析に誤りがあった。単一テーブルの UNIQUE で表現できる。案 (A+) を採り、`character_id` の追加を承認済みとする】**（開発者質問「重複したのは何ですか？」→ 説明の過程で設計卓が自分の誤りを発見。**開発者回答＝「A+案でお願いします」**） **★まず 714 の正体を確定する**——**「同じ技が、複数のプリセットで同じ表記になっている」件数である。** **例**〔ryu〕——**`preset=numeric, character=ryu, alias_text=236LP → hadoken_light`** と **`preset=srk, character=ryu, alias_text=236LP → hadoken_light`**。**`(character_id, alias_text)` で見ると 2 行あるが、指している技は同一である。** **★なぜこうなるか**——**`srk` は `numeric` の機械変換ではなく、通常技だけ接頭辞が変わり**〔`5`→`st.` / `2`→`cr.`〕**必殺技は数字のままである。** **⇒ 必殺技・SA が 2 プリセットで同値になり、それがそのまま重複キーになる。** **★対して #3**〔同一キャラ内で 2 つの「別の」技が同表記〕**は全プリセットとも 0 件である。** **M20-02 が衝突組を両方落としたため**〔`numeric` 35 キー / `srk` 30 キーを非投入〕。**⇒ 714 は無害な重複、0 件が守りたい不変条件。** **★★設計卓の誤りを訂正する。** **`D-343` は「守りたい不変条件は `moves` を跨ぐため単一テーブルの UNIQUE では表現できない」と書いたが、誤りである。** **`UNIQUE(preset_id, character_id, alias_text)` が、まさにその不変条件そのものである。** **⇒ 鍵に `preset_id` を入れると 714 の重複は消える**〔重複はすべてプリセットを跨いで発生しているため〕。**残る違反は「同一プリセット・同一キャラで 2 つの技が同表記」だけであり、それが #3＝実測 0 件である。** **★つまり #3 の測定は、この制約の違反件数を測ったものと同じだった。** **設計卓は #2 の 714 だけを見て「張れない」と判断し、鍵に `preset_id` を入れる形を検討していなかった。** **`D-343` の「(A) は 714 件に当たるため運用が前提」という記述も誤りである。** **`D-343` の本文は書き換えない**〔**D-57** ／ **D-259**〕。**本裁定が差し替える。** **⇒ 採択＝案 (A+)**——**`UNIQUE(preset_id, character_id, alias_text)`** ＋ **部分インデックス `UNIQUE(preset_id, character_id, alias_text_en) WHERE alias_text_en IS NOT NULL`**〔#4 も実測 0 件〕。**どちらも現状の違反は 0 件である。** **★スキーマ変更を承認済みとする**——**`preset_aliases` へ `character_id` を追加する**〔`moves` からの非正規化〕。**開発者回答「A+案でお願いします」を承認として扱う**〔契約 §4.1・F-6 のスキーマ承認ゲートを通過〕。 **★DB では守れないものが 1 つ残る**——**「ある技の `alias_text` が、別の技の `alias_text_en` と一致する」交差**〔**D-317** が想定した形〕。**列を跨ぐ条件であるため UNIQUE では表現できず、検査に残る。** **現状 0 件だが、`P-34` の 27 件を投入すると `alias_text_en` が増えるため、そのとき効いてくる。** **★非正規化のコスト**——**`character_id` は `moves` からの複製であり二重管理になる。** **実務上、技のキャラが変わることはないため risk は低いが、backfill マイグレ 1 本と `moves` との整合を固定するテストが要る。** **⇒ `P-38` 解消。`M20-03` の三点セットを起票する**〔**D-345**〕。 |
| D-343 | 2026-08-13 | **【M20-03 の一意制約は「単純 UNIQUE を張る」では成立しない。制約の形そのものを設計し直す必要がある】**（`CHANGE-099` §4 の判断 3 の帰結。**開発者判断が要る**＝**P-38**） **★M20-03 は当初「`preset_aliases` への `character_id` 追加 ＋ `alias_text` 一意制約」として予約されていた**〔契約 §4.1・F-6。**未承認のスキーマ変更**〕。**M20-02 の実測がこの前提を崩した。** **★実測**——**`(character_id, alias_text)` の重複が 714 件ある。** **⇒ 単純 UNIQUE は張れない。** **ただしこれは障害ではない**——**逆引きは `SELECT DISTINCT m.code` であり、`numeric` と `srk` が同値になる特殊技**〔`236LP` 等〕**は 1 件に畳まれる。** **壊れるのは「別 move が同表記になったとき」だけで、それは 0 件である**〔`count(DISTINCT moves.code) > 1` の組が全プリセットで 0〕。 **★つまり「守りたい不変条件」と「張れる制約」がずれている。** **守りたいのは「同一キャラ内で 2 つの技が同表記にならない」であり、`(character_id, alias_text)` の一意性ではない。** **前者は `moves` を跨ぐ条件であるため、単一テーブルの UNIQUE では表現できない。** **⇒ 採りうる形を 3 つ立てた。開発者判断が要る**〔**P-38**〕——**(A) 部分 UNIQUE インデックス**〔`preset_aliases` に `character_id` を非正規化で持ち、`(character_id, alias_text)` へ張る。**714 件に当たるため、投入側で重複を作らない運用が前提になる**〕／**(B) 制約を張らず検査で守る**〔投入マイグレとテストで `count(DISTINCT moves.code) > 1` が 0 であることを固定する。**M20-02 が既に実装している形**〕／**(C) `alias_text` と `alias_text_en` の合併集合で一意にする**〔**D-317** の想定。**2 列の交差まで見る必要があり最も重い**〕。 **★設計卓の推奨は (B) である。** **理由 3 点**——**(1) 守りたい不変条件をそのまま表現できる唯一の形である**〔他の 2 案は「近いもの」を制約にしている〕／**(2) スキーマ変更が要らない**〔`character_id` の非正規化は `moves` との二重管理を生み、`moves.character_id` が変わったときに追随が要る〕／**(3) M20-02 が既に実装しており、実測 0 件を確認済みである。** **★(B) の弱点も書く**——**DB レベルで防げないため、`preset_aliases` へ直接 INSERT する経路ができたときに守られない。** **⇒ 採るなら「投入は生成器を通す」を `DES-004` §5 へ規約として書く必要がある。** **★スキーマ変更の承認ゲートは生きている**〔契約 §4.1〕。**(A) / (C) を採るなら `character_id` 列の追加について開発者の個別承認が要る。** **(B) なら不要。** |
| D-342 | 2026-08-13 | **【`M21-02` の三点セットを起票した。判定窓の「値」ではなく「決めかた」を渡す】**（**D-341** の後続。**設計卓の手番**） **発行物 3 点**——**指示書 `docs/instructions/M21-02-simultaneous-press-and-chatter.md` v1.0.0**〔**高精度実装・Plan Mode 必須・着手前実査 8 項目**〕／**レビューチェックリスト v1.0.0** ／**通知書 `docs/change-notes/CHANGE-107-notification.md`**〔起票・未反映〕。**registry v1.113.0。** **★設計の要点は「閾値を設計卓が決めない」ことである。** **判定法 (α) 単一の広い窓 / (β) 実行時の自己校正 の選択も、チャタリング対策の要否も、SOCD の規則も、すべて実測を根拠に製造が決めて報告する形にした。** **測る前に判定法を決めない**〔**D-294** ／ `E-123`〕。 **★代わりに渡したのは条件である**——**窓は `D-324` の最大値を下回らせない**〔押し始め 45.80 ms ／ 離し始め 83.30 ms〕／**BT は未計測と明記する**／**窓を定数で埋め込まず 1 か所から差し替えられる形にする**／**フレーム数で置かない**〔60 Hz 環境で 1/4 になる〕。 **★`CHANGE-107` では判定窓の値そのものを設計書に固定値で書かない。導出の根拠と下限を書く。** **理由＝値だけを書くと次の担当が根拠を失ったまま数値だけ引き継ぐ**——**`DES-004` §3.4 のサンプル表が現に固定点として引かれた前例がある**〔**D-339**〕。**同じ事故を先回りで塞いだ。** **★`DES-006` は触らないと決めた。** **契約 §2 は「同時押し・チャタリングの入力バリデーション〔要否は PoC 後に判断〕」を M21 の所有節に挙げていたが、本件は入力層の集約の話であってバリデーション仕様ではない**——**受理するかしないかの判定ではなく、複数の入力を 1 ステップへ写す規則だからである。** **出口は既存の `move_code` であり検証仕様は変わらない。** **★silent な失敗の唯一の経路を特定した**——**窓が狭すぎることだけである。** **広すぎる誤りも狭すぎる誤りも読取表示**〔`FR106`〕**があるかぎり画面に出るが、「実測の最大値を下回る窓」だけは利用者が気づけない形で入力を落とす。** **⇒ チェックリストの重大 1 に置き、テスト §5.1 (f) で定数を直接読んで固定させる。** |
| D-341 | 2026-08-13 | **【`CHANGE-099` と `CHANGE-106` の三点セットを反映した。M20-02 と M21-01 は設計卓側も完了】**（**D-339** ／ **D-340** の実施。**開発者から実データ 2 行を受領**＝`jumping_heavy_punch` = `j.HP` ／ `sa1_shinku_hadoken` = `236236P (SA1)`） **★反映した設計書は 4 本**——**`DES-003` 1.49.0 → 1.50.0**〔第52版。§3.9 へ `alias_text_en TEXT` ／ §3.3 へ `numeric` / `srk` の波ごと再適用の相互参照〕 ／ **`DES-004` 1.17.0 → 1.18.0**〔第19版。**§3.4 サンプル表を実データ 6 行へ置換** ／ **§3.4.1 層順の原理** ／ **§3.4.2 SA / CA 注記** ／ **§5.4 seed 波ごとの再適用責務** ／ **§5.5 衝突組は両方落とす** ／ **§5.6 逆引きへの副作用**〕 ／ **`REQ-001` 2.18.0 → 2.19.0**〔第29版。**FR102 を物理側へ移し「重要・フェーズ3」へ** ／ §16 対応表 ／ 技術前提の入力デバイス行を 2 行へ分割〕 ／ **`DES-005` 2.58.0 → 2.59.0**〔第70版。§6.1 注記 ／ §6.2 の実個数 21 ／ **§6.3.1 標準配置の既定 ＋ 上書き** ／ **§6.3.2 登録対象 13 件** ／ §6.4.1 の保持対象 ／ **§6.8 test-id 命名規約**〕。 **★change-report 2 本と registry v1.112.0 を出し、通知書 2 本を「反映済み」へ。** **`SUPP-001` §3.4 の失効も同じ手番で是正した**〔自由改訂・CHANGE 対象外。**「実データ投入済みは `official_ja_move` のみ」は 3 プリセットとも投入済みへ**〕。 **★`M20-02` と `M21-01` は、製造側・設計卓側とも完了した。** **CHANGE の残りは M20 が 100〜105 の 6 本、M21 が 107〜111 の 5 本。** **★`check-md-emphasis.sh` が 375 → 373 へ 2 行減った。定数は動かしていない**〔**D-335**。**両レーンのマージ後に一括更新する**〕。 **★次の手番は `M21-02` の三点セット起票**〔同時押し判定・チャタリングの決着・`CHANGE-107`〕。**`M21-01` の論理ボタン層の 4 つの性質**〔`states` を「押されているものだけ」と読めない ／ `cardinals` に SOCD 相殺前が併記されている ／ `pressed` と `value` の両方 ／ 同時押しはまとめていない〕**を前提として引く**〔**D-338**〕。 |
| D-340 | 2026-08-13 | **【`DES-005` §6.3 のレイアウト名は案 (α)＝提示ラベルとして残す。両 CHANGE の確認事項が全部決着した】**（開発者回答 2026-08-13＝「推奨 (α) で良いです」） **★レイアウト名**〔レバーレス／アケコン／PS5パッド／キーボード〕**を `DES-005` §6.3 に残す。** **理由＝`REQ-001` FR102 が 4 レイアウト名を要件として名指ししており、`DES-005` から消すと要件との対応が切れる。** **★必ず 1 文を併記する**——**「レイアウト名は利用者への提示ラベルであって、実装の分岐条件ではない。`M21-01` の as-built は機体をレイアウト名で分岐しておらず、`mapping` と利用者のキャリブレーションだけで解決する」。** **現に分岐に使っていないことを書かないと、次の担当が分岐を作りうる**〔製造の §4-2 が「書いたまま実装が無い状態」を指摘したのと逆向きの事故〕。 **⇒ `M21-04` / `M21-05` がレイアウト種別の選択保持を実装する際は、台帳 #2 `virtual-controller-layout-v1` の取消線を外して使う**〔製造の §4-1〕。 **★これで `CHANGE-099` と `CHANGE-106` の確認事項は全件決着した。反映のブロッカーはゼロである。** **⇒ 設計卓の次の手番は両 CHANGE の三点セット反映**〔改訂 `DES-003` §3.9 ＋ 改訂 `DES-004` §3.4・§5 ＋ 改訂 `REQ-001` FR102 ＋ 改訂 `DES-005` §6.1〜§6.4.1 ＋ `change-report-099` ＋ `change-report-106` ＋ registry〕。 **★ただし着手前に 2 つの入力が要る**——**(1) `CHANGE-099` の実データ 2 行の確認**〔`jumping_heavy_punch` と `sa1_shinku_hadoken` の 2 プリセット分。**設計卓は実装ソースを読めない**＝**E-117**〕／**(2) 両サブを main へマージした後の `docs/handover/followup-backlog.md`**〔**製造が §E へ追記し、設計卓が §K へ 3 件足しており、両者が同じファイルを触っている。** **設計卓のブランチは製造の追記を持っていない。** **取得系の git 操作は `CLAUDE.md` §10 で deny されているため、設計卓は自力で取れない**〕。 |
| D-339 | 2026-08-13 | **【`CHANGE-099` を承認。サンプル表は案 (α)＝実データで置き換える】**（開発者回答 2026-08-13＝「通知書は承認します」／「サンプル表は負債になりかねないので (α) で」） **★設計卓の推奨 (β) は採られなかった。開発者の判断が正しい。** **(β) は「注記を強める」だけであり、`M20-02` の指示書 §5.1 が現に注記を読み飛ばして固定点として引いた事実を消せない。** **注記で守る仕組みは、同じ失敗を 1 度許した時点で信用を失っている。** **★設計卓は「代表を選ぶ根拠が無い」を理由に (β) を推したが、根拠なら実在する**——**層 A / 層 B の両方と SA 注記の書式を 1 つずつ含めればよい。** **⇒ (α) の具体形を確定した**〔`CHANGE-099` §7 の確認事項 2〕——**6 行**〔`standing_light_punch` ／ `crouching_light_kick` ／ **`jumping_heavy_punch`**〔**新規追加。`command` が立ち技と同一であることを示す代表行であり、`D-331` の層順の原理が可視になる**〕／ `hadoken_light` ／ `shoryuken_heavy` ／ **`sa1_shinku_hadoken`**〔**`sa1` から差し替え。`236236P (SA1)`**〕〕。 **★`parry_drive_rush` / `cancel_drive_rush` は表から外し、直後の注記へ移す**——**表に置くと「エイリアスとして生成されるはず」という誤読が復活する。** **★反映前に実査が要る**——**設計卓は実装ソースを読めない。** **上表は設計伝達レポートと `DES-004` §3.4 の現行値から構成したものであり、生成結果そのものを見ていない。** **⇒ `change-report-099` を出す前に `jumping_heavy_punch` と `sa1_shinku_hadoken` の 2 プリセット分が実データと一致するかを製造または開発者に確認する**〔**E-117**〕。 |
| D-338 | 2026-08-13 | **【M21-01 の §2-1 を受理する。`D-328` の趣旨は「決め打ちをやめること」ではなく「持っていない機体でも動くこと」だった】**（設計伝達レポート §2-1。**製造が唯一「親の裁定を要する」として上げた項目**） **★結論＝受理する。差し戻さない。** **★そして設計卓の誤りを認める。** **`D-328` と指示書 §1.1 は「所有していない機体に『対応した』と言える唯一の形は、機体ごとの決め打ちをやめることである」と書いたが、これは手段を目的として書いたものである。** **目的は「持っていない機体でも動くこと」であり、決め打ちをやめるのはその手段の 1 つにすぎない。** **★`mapping === "standard"` は機体ごとの決め打ちではない**——**W3C Standard Gamepad の仕様であり、機体を名指ししていない。** **⇒ 「機体ごとの決め打ち」に当たらない。** **機種名ハードコードは 0 件のまま維持されている。** **★指示書 §4.2-3「`mapping === "standard"` を必須条件にしない」も守られている**——**必須条件**〔無いと動かない〕**ではなく、既定の適用条件になっている。** **標準でない機体は従来どおりキャリブレーションへ倒れる。** **★抵触しない形が実装で保たれていることを製造が 4 点で示した**〔正規化は `mapping` を読まない ／ 標準でない機体を弾かない ／ 既定が外れても上書きできる ／ 鍵は機種名でない〕。**設計卓はこれを是認する。** **★決定的なのは製造の §7-1 の教訓である**——**「『決め打ちをやめる』系の設計判断は、やめた結果として誰かが払うコストを併記しないと、コストが見えないまま完成する」。** **本サブは全利用者へ初回 10 件の登録を課す形で完成し、指示書・レビュー・製造のいずれもこれを問題視しなかった。** **問題を指摘したのは実機で 1 回触った開発者である**〔「このコントローラ設定は毎回させるものですか？」〕。 **★PoC のデータ**〔2 機種とも `standard`〕**は最初からあった。見落としたのは判断であって情報ではない。** **⇒ `CHANGE-106` §2.2 の反映内容を差し替えた**——**f を「決め打ち表の撤回 → キャリブレーション一本」から「標準配置の既定 ＋ キャリブレーションによる上書き」へ** ／ **e を「14」から個数**〔**実個数 21・実装 25・差分 +4**〕**へ** ／ **g を「種類」から「対応表」へ**〔保持キー `gamepad-profiles-v1`・`profileKey = "${browserKey}::${padId}"`〕 ／ **f-2 を新設**〔**登録対象 13 件と「斜めは導出」**〕 ／ **g-2 を新設**〔**`recipe-<領域>-<識別子>` の test-id 規約そのものの明文化**。**実査の結果リポジトリのどこにも文書化されていなかった**〕。 **★製造の依頼 2 点目**〔否定形確認で「`"standard"` の分岐は全面禁止ではない」を書き分ける〕**も採った。** **指示書テンプレート §4.5 の記入指針へ一般則を入れた**——**走査キーワードは「撤回した仕様の残骸を探す網」であって、その文字列の全面禁止ではない。各行に「何が残っていたら残骸か」を 1 文で添えること。** **あわせて §7-2 の教訓**〔**否定形確認はサブの完了時点ではなく最後にコードを触った時点で回す**。**実機確認 → 追補 の運用では初回の「0 件」が失効する**〕**も同じ記入指針へ入れた。** **★`M21-02` / `M21-03` の指示書を書くときは、レポート §1-6 の 4 点を前提として引く**〔**`states` を「押されているものだけ」と読めない**——方向は割当があれば常に 1 つ入る ／ **SOCD 相殺前の生の上下左右を `cardinals` で併記している** ／ `pressed` と `value` の両方が来る ／ 同時押しはまとめていない〕。 **★開発者への確認を 1 件立てた**——**`DES-005` §6.3 でレイアウト名をどう扱うか**〔`CHANGE-106` §7 の確認事項 3。**as-built は分岐にも提示にも使っていない**〕。 |
| D-337 | 2026-08-13 | **【M20-02 の申し送りを処理した。`check-md-emphasis.sh` の +1 の正体は M-50 であり、既に解消済みである】**（設計伝達レポート §4-1・§4-3・§4-4・§4-5 の処理） **★§4-1＝本書 §2.2 の連番が失効していた。是正した。** **「次に払い出す番号」を `000070` → `000074`、disk 末尾を `000068` → `000073` へ。** **M20-02 が `000070`〜`000073` の 4 本を消費している。** **★放置すると次のサブが `000070` を再払い出しして衝突するところだった**〔**D-120**〕。**製造ブランチ上では共有直列リソースに触れないため、レポート経由で戻る設計が正しく働いた。** **★§4-4＝`check-md-emphasis.sh` の「376 対 375」の +1 は、設計卓が同日 `M-50` として特定し解消済みである。** **実体は `M{N}-RESEARCH-{NN}-{slug}` テンプレート §157 の閉じない強調**〔`**「{{後続サブ}} …要決定事項」**を`＝**閉じ `**` の直前が `」` で直後が `を`**〕。**製造の分析**〔「本サブ起因ではない。検査が動かない間に main へ入った既存の回帰である」〕**は完全に正しい**——**設計卓自身の commit が入れたものだった。** **⇒ 現在は 375 対 375 で緑である。製造へ返すこと。** **★製造の指摘で設計卓の認識が 1 つ変わった**——**`D-327` は「スクリプトが依存欠落時に緑を返す」欠陥を直したが、`markdown-it-py` がそもそも devContainer にも開発者機にも入っていなかったという別の事実は把握していなかった。** **⇒ `check-artifact-integrity.sh`〔★1 本目に回す検査〕が恒常的に赤だった期間がある。** **開発者承認で `.devcontainer/Dockerfile` へ導入済み**〔裁定 5〕。 **★§4-4 の依頼「ベースライン更新時に `--list` の出力も保存する」は採る。ただし実施は後回しにする。** **理由＝`D-335` で並走中は `scripts/*.sh` を触らないと決めたばかりであり、同じファイルへ仕組みを足すのは矛盾する。** **⇒ `followup-backlog` へ `baseline-list-snapshot` として登録し、両レーンのマージ後に設計卓が一括で実施する。** **★依頼の理由は正しい**——**件数だけでは増えたときにどの行が新規か特定できない。** **今回は設計卓が同日に直した記憶があったため特定できたが、それは再現性のある手段ではない。** **★§4-3＝`P-34` の対象を 28 → 27 件へ縮小した。** **`lily/windclad_od_condor_spire` は「値を決める対象」ではなく入力漏れであり、`notes_tool` 列が値を裏付けた。** **`D-315` が `D-312` と同じ「入力漏れ」型の混入を予告しており、そのとおりに 1 件出た。** **★`rashid` の `buffed_*` 5 件が移動系 9 code と同じ入力を持つという指摘は、`P-34` の値決めに直接効く。** **★§4-5 L-6＝製造ブランチ上の指示書が v1.0.0 のままである件は、設計卓が直せない**〔別ブランチ〕。**repo 側は v1.2.0 であり、レビューは repo 側を参照する。** **`D-331` で既に決着済みの運用であり、追加の手当ては要らない。** |
| D-336 | 2026-08-13 | **【`000035` / `000057` の再生成を是認する。「適用済みマイグレは改変しない」と三点更新は上下ではなく適用対象が違う】**（設計伝達レポート §2-1。**製造が唯一「親の裁定を要する」として上げた項目**） **★結論＝製造の判断 (A) を是認する。独断を咎めない。** **理由は「取りうる案が (A) しか無かった」ことにある。** **指示書 §4.2 (i) が OD 技 4 件の CSV 補記を明示的に要求しており、CSV は `000035` / `000057` の正本である。** **⇒ 補記すると golden が落ちる。** **(B) 放置は `D-308` が禁じる「既知の失敗」を作る ／ (C) 期待値を緩めるのは `SUPP-001` が明示的に禁じる ／ (D) 補記しないのは §4.2 (i) 違反。** **⇒ 論理的に (A) 以外が無い。** **★指示書が自分の中で矛盾していたのであって、製造が規約を破ったのではない。** **★両者は上下関係ではない。適用対象が違う。** **判別の述語を確定する**——**「そのマイグレは生成物か、手書きか」。** **(a) 手書きのマイグレ → 改変しない。** **守るのは「配布済み DB とマイグレ履歴の整合」である。** **(b) 正本から機械生成されるマイグレ**〔seedgen の出力・golden〕**→ `SUPP-001` §5.5.4 (6) の三点更新を採る。** **理由＝正本を是正した時点で、改変しないほうが「正本と生成物の乖離」を作る。** **⇒ 守るべき整合が逆向きに壊れる。** **★`SUPP-001` §5.5.4 (6) は三点更新を「CSV 正本の是正 → golden 再生成 → 追随マイグレ」と定義しており、追随マイグレが 3 点目である**〔実査 2026-08-13〕。**製造の `000071` はまさにそれであり、件数で固定せず往復で固定するという同項の要求も満たしている。** **★同 (9) が決定的である**——**「案の比較でこれを見落としやすい。『既存マイグレを改変しない案』と『改変する案』を比べるとき、どちらの案も中間版コホートに欠陥を残す」。** **⇒ `SUPP-001` は「改変する案」を正当な選択肢として明示的に想定している。** **「改変しない」を絶対の原則として書いた指示書のほうが、上位資料と食い違っていた。** **★`000071` の down を no-op にした判断も是認する。** **新規 DB では再生成済みの `000035` / `000057` が同じ 4 行を入れており、down で DELETE を書くと巻き添えにする。掃除は `000035` / `000057` の down が行う。** **これは (6) の「追随マイグレは新規 DB では 0 行に当たる」の帰結そのものである。** **★テンプレートへ但し書きを入れた**〔製造の依頼どおり。`M{N}-{NN}-{slug}.template.md` §2.2 の記入指針〕——**「適用済みマイグレは改変しない」を裸で書かせない。述語と、三点更新を採る場合の定型文を添えた。** **★但し書きが無いと、次のデータ系サブでも製造が同じ独断を強いられる**〔製造の指摘のとおり〕。 **★これは `M-52` として §6 へ計上する**——**指示書 `M20-02` §2.2 と §4.12 (a) が `SUPP-001` §5.5.4 (6) と正面から衝突していた。** **`M-51`**〔同指示書 §4.3 と §4.4 の内部矛盾〕**に続いて同じ指示書で 2 件目であり、いずれも「上位資料または自分自身と両立しない条項」である。** |
| D-335 | 2026-08-13 | **【並走中はベースライン定数を動かさない。報告を求めるだけでは穴が残る】**（**D-334** の衝突点 (2) の具体化。**開発者からの問い 2026-08-13**＝「これは何でしたか？ M21-01 が後から動くので、M20-02 側に報告を求めたらいい？」） **★実体は 3 つの定数である**〔実査 2026-08-13〕——**`scripts/check-md-emphasis.sh` の `BASELINE_BROKEN=375`** ／ **`scripts/check-instruction-format.sh` の `BASELINE_FORBIDDEN=74`** ／ **`scripts/check-enum-sync.sh` の `BASELINE_SCATTER=24`。** **3 本とも「違反が減ったら定数を下げてよい」というラチェット型で、スクリプト自身が下げるよう勧める出力を出す**〔例＝`BASELINE_BROKEN を N へ更新してよい`〕。 **★報告を求めるのは正しいが、それだけでは足りない。** **開発者の案は「M20-02 側に報告を求める」だが、これは既に `D-323` §3-4 で要求済みであり、本当の危険を塞がない。** **★本当の危険は衝突ではなく、黙って緩むことである。** **同じ 1 行を両ブランチが書き換えると git は衝突を出すが、衝突は見えるので直せる。** **見えないのはこちらである**——**M20-02 が 375 → 370 へ下げて先にマージし、その後 M21-01**〔ベースライン 375 のまま分岐している〕**がマージされたとき、解決を誤ると 375 が復活する。** **検査は緑のままで、ラチェットだけが 5 行ぶん緩む。** **`E-84` の同型**〔**「緑」と「基準が緩んだ緑」が同じ顔で出る**〕。 **⇒ 裁定＝並走中は `scripts/*.sh` のベースライン定数を 1 つも書き換えない。** **★実際の違反を直すのは自由である。触らないのは定数だけ。** **違反を減らしたら「現在 N / ベースライン M」の実測値を完了報告に書くに留める。** **定数の更新は両レーンのマージ後に設計卓が一括で行う。** **★この形にすると、報告は「あったら嬉しい情報」から「唯一の入力」へ変わる**——**設計卓が一括更新するとき、両サブの実測値が要るためである。** **⇒ 報告を求めるのは正しい。ただし「相手に報告させる」ではなく「両方に報告させ、更新は誰もしない」が正しい形である。** **★費用はゼロに近い。** **定数を下げるのは記録であって修正ではなく、遅れても実害が無い。** **一方で黙って緩むのは検出できない。** **⇒ 非対称なので、安いほうに倒す。** **★写し先**——**`M21-01` 指示書 v1.0.2 §7.3** ／ **同レビューチェックリスト v1.0.2 §6・§9-11** ／ 本表。 **★`M20-02` は会話経由で伝える**〔**同サブは Code on the web の制約で途中の指示書更新ができない**＝開発者の申告 2026-08-13。**指示書へは書けないため、差分連絡に含める**〕。 |
| D-334 | 2026-08-13 | **【`M21-01` と `M20-02` は並列実行してよい。コード面の交差はゼロである。ただし共有ファイルの衝突点が 3 つある】**（**開発者からの可否確認 2026-08-13**。**E-14 の交差確認は設計卓の担保事項**） **★結論＝並列可。** **面を実査して突き合わせた。** **`M20-02` §2.1 が触るのは `migrations/` 4 本 ／ `character_data/*.csv` 3 ファイル ／ 生成器 ／ `internal/infra/migration/migrate_m2002_test.go` ／ 既存テストの追随 ／ 完了報告**——**`web/` を 1 ファイルも触らない。** **`M21-01` §2.1 が触るのは `web/src/features/<入力系>/` ／ `web/src/lib/browser-storage.ts` 経由の保存 ／ テスト**——**`migrations/` も `internal/` も触らない**〔契約 §4.2＝スキーマ変更の予定なし〕。 **⇒ コード面は交差しない。** **★契約の柵も両側から効いている**——**`m20-contract` §4.1 が触る `web/` は `web/src/features/preset/` に限られ、`M21-01` の入力系とはディレクトリが違う。** **`m21-contract` F-1 が `notation` ／ `preset` を「触らない」と定め、`m20-contract` F-1 が同条項を写している。** **`punishfinder` ／ `setplay` は両レーンとも diff 0。** **★ただし「触るファイルが違うから大丈夫」で終わらせない**〔本書 §5.2 の警句〕。**実際に衝突する共有ファイルが 3 つある。** **(1) `docs/progress/progress-log.md`**——**両サブとも完了時に索引行を追記する**〔`CLAUDE.md` §8 の必須事項〕。**同一ファイルへ別ブランチから追記するため、マージ衝突が起きる公算が高い。** **⇒ 解消は自明**〔両方の行を残す〕**だが、片方を捨てないこと。** **(2) ベースライン型検査の数値**——**`scripts/check-enum-sync.sh` ／ `check-md-emphasis.sh` のベースラインを両サブが同時に動かすと衝突する。** **⇒ 動かした側は完了報告に数値と理由を書く**〔**D-323** §3-4〕。**後からマージする側は、自分の変更後に再測して整合させる。** **(3) `DES-005`**——**本日は交差しない**〔`M20-02` の CHANGE は `DES-004` ＋ `DES-003`、`M21-01` の `CHANGE-106` は `REQ-001` ＋ `DES-005` §6.2・§6.3・§6.4.1〕。**★交差するのは将来の `M20-04`**〔管理 UI の新画面〕**× `M21-01` の `DES-005` §6 である。** **`m20-contract` §2 が「`DES-005` は両レーンが触る唯一の文書。節単位で排他する」と定めており、節が違うため排他は成立する。** **⇒ ただし DES 本体の改訂はどちらも as-built 確定後であるため、反映の順序を設計卓が管理する。** **先に反映する側が「相手の節に隣接する記述」を足すときは設計卓へ上げること。** **★開発者手番の競合も無い**——**`M20-02` の開発者手番はゼロ**〔本書 §1.4。OD 技 4 件の値は **D-318** で確定済み〕／**`M21-01` は実機確認 3 件で 15〜25 分。** **⇒ 待ち行列にならない。** **★マイグレ連番の競合も無い**——**`M21-01` は 0 本である。** **`M20-02` が 4 本を消費するが、`M21-01` は払い出しに参加しない。** **⇒ 統合順序の制約が生じない。** |
| D-333 | 2026-08-13 | **【`CHANGE-106` が承認された。`FR102` の優先度・フェーズ列は案 (β)。M21-01 は投入可】**（**D-332** の確認事項 2 件の決着。**開発者回答 2026-08-13**＝1「(β)採用」／2「承認」） **★(1) `FR102` の優先度・フェーズ列は「必須・フェーズ1」→「重要・フェーズ3」へ揃える。** **理由＝`FR102` の内容そのものが物理入力の話になる以上、`FR105`〜`FR108` と同じ区分に置くのが実態に合う。** **★ただし格下げではない。** **⇒ `REQ-001` §16 対応表の追記で「達成済みの部分」と「M21 へ移した部分」を書き分けること**——**仮想側は M15-03 でカテゴリタブ式の direct-selection として達成済みであり、M21 へ移したのは物理側の 4 レイアウトだけである。** **区分の変更だけを見ると要件を下げたように読めるため、対応表で明示する。** **★(2) `CHANGE-106` を承認。** **⇒ 着手前ゲートを通過した。`M21-01` は投入可である。** **★ステータスは「起票（反映案）」のままである**——**承認は着手のゲートであって反映ではない。** **`REQ-001` ／ `DES-005` の本体は M21-01 の as-built 確定後に、`change-report-106` と同時に改訂する**〔設計卓の運用規則 4(b)〕。**registry も「起票済・承認済（未反映）」であって「反映済」ではない。** **★写し先を更新した**——`CHANGE-106` 通知書〔承認者欄・§2.1 a・§2.1 c・§4 の判断 5・§7〕／`change-number-registry` v1.110.0／指示書 `M21-01` v1.0.1〔§11 の状態〕／レビューチェックリスト v1.0.1〔対象版の追随〕／本表。 **★指示書は v1.0.1 だが仕様は 1 文字も変えていない**——**確認事項の決着を書いただけである。** **製造は基点 commit を `0a0167c` 以降にすること**〔**D-323** の再演防止〕。 |
| D-332 | 2026-08-13 | **【`M21-01` の三点セットを起票した。`REQ-001` と `DES-005` を 1 本の CHANGE へ束ね、ブロックの余裕ゼロを解消した】**（**P-35** の後続。**設計卓の手番**） **発行物 3 点**——**指示書 `docs/instructions/M21-01-gamepad-input-foundation.md` v1.0.0**〔**高精度実装・Plan Mode 必須・着手前実査 9 項目**〕／**レビューチェックリスト `docs/instructions/reviews/M21-01-review-checklist.md` v1.0.0** ／ **通知書 `docs/change-notes/CHANGE-106-notification.md`**〔**起票・未反映**〕。**registry 登録済**〔v1.109.0〕。 **★`CHANGE-106` は 2 文書にまたがる。当初 107 を予定していた `DES-005` 分を束ねた。** **理由**——**「4 レイアウトの対応範囲を物理側へ移し、決め打ちマッピングをキャリブレーション方式へ改める」は 1 つの設計変更であり、波及先が 2 文書に及ぶだけである。CHANGE の単位は文書ではなく変更である**〔**D-323** ／ 先例 **CHANGE-006** が `REQ-001` ／ `DES-002` ／ `DES-003` ／ `DES-005` にまたがる〕。 **★分けると、片方だけ承認・片方だけ差し戻しという状態が作れてしまう。** **FR102 を移す判断と、移した先をキャリブレーション方式にする判断は、一方だけ採れない。** **⇒ 副次的に `M21-overview` の確認事項 3 が解消した**——**見込みが 6 本から 5 本になり、ブロック 106〜111 に 1 本の余裕ができた。** **以降を 1 つ繰り上げた**〔02→107 ／ 04→108 ／ 06→109 ／ 05→110〕。 **★契約 §2 の誤りを 1 件是正した**——**同節は `REQ-001` を「所有しない」と書いており `CHANGE-106` と衝突していた。** **発生したのは FR 新設ではなく既存 `FR102` の改訂であり、所有範囲は `FR102` ＋ §16 対応表の該当行 ＋ 技術前提の入力デバイス行に限る。** **`FR105`〜`FR108` は既存で FR 新設不要という部分は生きている。** **★指示書の設計上の要点 3 つ**——**(1) 正規化を純粋関数にすることを必須にした**〔**`navigator` にも `window` にも触らない**。**⇒ Firefox 形の入力をテストで食わせられる。** **開発者の Firefox 実機確認は M21 完了時の 1 回だけであり**〔**D-329**〕**担保がこのテストしか無いため、§9 の重大 1・3 に置いた**〕／**(2) 未知の機体を弾かないことを重大扱いにした**〔**§4.2-8**。**弾くと「持っていない機体で動かす」という設計が丸ごと成立しなくなる**〕／**(3) 「認識していません」と「まだ 1 度も押されていません」を別の文言にすることを要求した**〔**§4.4-4**。**前者は障害、後者は正常な初期状態である**＝**E-84** の同型〕。 **★ブラウザストレージは台帳の実査ゲートを置いた**〔§3.3-8。**設計卓は `web/` を読めないため「載っている」と断定しない。** **未記載なら `CHANGE-106` の addendum で追記してから使う**＝`CLAUDE.md` §10.X〕。 **★開発者への確認は 2 件**〔通知書 §7＝**`FR102` の優先度・フェーズ列をどう扱うか**〔**推奨は (β) 重要・フェーズ3 へ揃える。ただし「格下げではない」ことを §16 対応表で明示する**〕**と、通知書そのものの承認**〕。**★確認事項 1 が未決でも着手できる**——**列の値は文書上の扱いであり実装に影響しないため。反映時までに決まっていればよい。** |
| D-331 | 2026-08-13 | **【M20-02 の差し戻しに回答する。層順は行の性質で入れ替わる。固定点は 7 行ではなく 4 行。`numeric` の注記を追認する】**（**製造の全数実測による差し戻し 1 件 ＋ 追認依頼 2 件**。**指示書を v1.1.0 化して反映済み**＝**commit `3396599`**） **★(1) §4.3 の層順は誤りだった。是正する。** **実測**——**`character_data/*.csv` の `command` 列は地上／空中を表現せず、前後の別は `move_code` 側が持っている**〔`standing_light_punch` と `jumping_light_punch` がともに `p_l` → `token_key` `LP`〕。**⇒ 層 A を一律に先へ置くと 3 つが同時に壊れる**〔サンプル表 `5LP` に不一致 ／ 構造引き表 `j.HP` に不一致 ／ §4.11 #3 の衝突が 6 キー × 17 キャラ ＝ 102 キー増え §9.3-5 で両方とも投入不可〕。 **⇒ 是正＝`move_code` が `standing_` / `crouching_` / `jumping_` で始まる行は層 B を先に当てる**〔開発者裁定 2026-08-13 ＋ 設計卓の追認〕。 **★ただし「3 接頭辞の例外」として書かない。原理を 1 文で書いた**——**`command` が表記を決める行では層 A が正、`move_code` の構造が表記を決める行では層 B が正である。** **段階1 の構造名を持つ行は定義上「構造が表記を決める」側であり、先頭一致はその操作的な判定にすぎない。** **⇒ 迷う行が出たらこの 1 文へ戻らせる**〔**例外の列挙は必ず漏れる。原理なら漏れても判断できる**〕。 **★境界 3 件を実測させる**〔`rush_standing_light_punch` 型の合成 code は先頭一致で捕まらない ／ 3 接頭辞を持つが `token_key` が構造引きと食い違う行で**捨てた層 A の値と件数を報告させる**〔**黙って捨てない**〕 ／ 3 接頭辞を持たないが `command` が地上／空中を区別できない行は**同じ病理が別の場所にある可能性**。**0 件なら「0 件」と書く**＝**E-84**〕。 **★残存衝突 35 キーは全件列挙して報告させる。独断で片方を捨てさせない**〔§9.3-5〕。**★(3) の注記適用後に測り直させる**——**SA3 と CA の同一コマンド組が 17 キャラにあるため、35 からさらに減る。** **★(2) `DES-004` §3.4 のサンプル表は固定点として 4 行しか使えない。製造の判断に同意する。** **`sa1` は実在する `move_code` ではなく**〔ryu の実体は `sa1_shinku_hadoken`〕**かつ CSV の `command` は強度指定なしの `236236P` であるため `236236HP` を作れない** ／ **`parry_drive_rush` と `cancel_drive_rush` は `modifiers.type` の非技ステップで `moves` 行ではなく、`move_id` を鍵とする `preset_aliases` に入らない**〔**指示書 §4.4 が自認していた。自認しておきながら §5.1 で 7 行の一致を求めた設計卓の誤り**〕。 **⇒ §5.1 (c) を 7 行 14 件 → 4 行 8 件へ是正。不可 3 行は理由付きで報告させる**〔**黙って落とさない**＝**E-84**〕。 **★`sa1` の強度について実査を 1 件足した**——**`236236P` は「SA は任意のパンチで出る」という仕様の正確な反映であって `D-312` 型の入力漏れではない公算が高い。** **判別は安い**——**SA 行の `command` が全数で強度指定を持たないなら意図した規約、一部だけ持つなら持たない側がデータ不備。** **設計卓は現物を見られない**〔**E-117**〕**が、形の不揃いは見える。この形の指摘は `D-316` で実際に誤りを 1 件拾っている。** **★サンプル表そのものの是正は M20-02 の CHANGE**〔`DES-004`〕**へ含める。ただし DES 本体の改訂は as-built 確定後**〔**CHANGE-098 は反映済みであり、そこへ後から足さない**〕。 **★(3) `numeric` への `(SA1)` / `(SA2)` / `(SA3)` / `(CA)` 注記を追認する。** **理由 3 点**——**(a) 注記が無いと `numeric` で SA3 と CA が同一文字列になり §9.3-5 で両方とも投入不可になる。注記を足さないほうが情報を失う** ／ **(b) `srk` と同じ注記体系であり新しい種類のものではない** ／ **(c) `M20-03` の一意制約を通しやすくする方向に働く。** **★条件 4 点**〔**書式を `srk` と揃える**〔半角丸括弧・大文字。**`srk` の現況を実査し食い違ったら `srk` に合わせる。本サブは `srk` の書式を変える場ではない**〕／**`move_code` から機械抽出し、正規表現と適合・不適合の行数を報告させる**〔**技名からの推測で付けない**〕／**衝突する組だけに絞るか SA/CA 全行に一律かを実測で決めさせる**〔**設計卓の見込みは一律。衝突の有無で表記が変わると利用者から見て一貫しない。覆ってよい**〕／**逆引きへの波及を報告させる**〕。 **★逆引きへの波及を設計卓が拾った**——**注記を `alias_text` に含めると、利用者が `236236K` と入力したときに一致しなくなる**〔`alias_text` が `236236K(SA3)` になるため〕。**⇒ 逆引きの前段で注記を除去する正規化が要り、これは `M20-07`**〔**D-307** の逆引き前段の正規化〕**が持つ。** **M20-02 は「M20-07 に正規化が 1 件増える」ことを完了報告へ明記するに留め、実装しない**〔**スコープ外の先取りをしない**〕。 **★製造はブランチを `3396599` 以降へ更新すること**〔**D-323** の再演防止。**裁定は出しただけでは効かない。ファイルに入って、かつ相手のブランチに届いて初めて効く**〕。 |
| D-330 | 2026-08-13 | **【M21-overview の確認事項 3 件が承認された。M21-05 は直列とする】**（**開発者回答 2026-08-13**＝1 承認 ／ 2 直列 ／ 3 了解） **★(1) サブ分割・連番・実装順を承認**——**`M21-overview` を **v1.0.0** とし、6 サブ構成を確定する**〔playbook §4.12〕。**以後の変更は overview を改訂して再度承認を求める。独断で統合・分割しない。** **★(2) `M21-05`**〔キーボード〕**は直列とする**——**04 の後。** **⇒ 本 MS に並列サブは無くなり、§5.2 の E-14 交差表は「対象なし」へ変わる。** **★節は消さない**〔**節の欠落と、対象が無いことは違う**＝テンプレートの指示〕。**交差表は「直列化により対象なし」と明記して残す。** **★交差の管理費を払わない選択であり、並走の利得**〔実機確認の集約〕**が機材の違いで小さいという設計卓の見立てが採られた。** **★(3) CHANGE ブロックの余裕ゼロを了解**——**106〜111 の 6 本で進める。** **`M21-03` を「なし〜軽微」と見積もり `CHANGE-108` へ同梱できるかを M21-03 の着手時に判定する。枯れたら 112 以降を請求する。** |
| D-329 | 2026-08-13 | **【Firefox は案 (ii) を採る。作りはするが DoD には入れない】**（**P-37** の決着。**開発者回答 2026-08-13**＝「(ii)案」） **⇒ Firefox でも動くように作るが、動くことを M21 の完了条件にはしない。** **実機確認は M21 の完了時に 1 回だけ**〔Firefox × 2 機種 ＝ 2 組・**総額 +10〜20 分**〕。 **★これは Firefox を切り捨てる裁定ではない。** **案 (i) と作りは同じであり、違うのは Firefox が壊れていたときに M21 を止めるかどうかだけである。** **⇒ 不具合が出たら followup へ回し、M21 の完了を妨げない。** **★具体的な実装条件 4 点**〔いずれも**アケコン対応で同じ分岐が要るため Firefox 固有の作業ではない**〕——**(1) `mapping === "standard"` を必須条件にせず通常経路として扱う** ／ **(2) 方向入力を `buttons` と `axes` の両方から読む** ／ **(3) `axes` の本数を固定で仮定しない** ／ **(4) 正規化関数のテストへ Firefox 形の入力を含める**〔**ユニット相当で足り実機不要。製造だけで完結する**〕。 **★後付けを禁じる。** **Chrome 前提で書いてから広げると index を仮定した箇所を探し回ることになる**——**最初からそう書けば増分はほぼ無い。** **⇒ 入力サブの指示書に「最初から」と明記する。** **★出しかたの条件＝「Firefox 未検証」と書く。「Firefox 対応」と書かない**〔**E-84**〕。**完了時の 1 回の確認で問題が出たら、そこで初めて開発者判断を仰ぐ。** **★これで `P-35`**〔M21-overview の編成〕**のブロッカーはゼロになった。** |
| D-328 | 2026-08-13 | **【開発者回答で P-36 が決着した。案 (C) を採る。ただしアケコンは非所有、BT は計測不能であり、その 2 つが同じ 1 つの設計を要求する】**（**D-326** の回収。**開発者回答 2026-08-13**＝Q1「C。ただし持っているのはレバーレスとパッドの二種類だけ」／Q2「持っていない」／Q4・Q5「Bluetooth 接続のコントローラを持っていないので計測できない」） **一次源＝`docs/process/m21-measurement-scope-assessment.md` v1.1.0。** **★Q1＝案 (C) で確定**——**ゲームパッド系 3 種を 1 つのプロファイル層で扱い、キーボードは独立サブ。** **⇒ `REQ-001` の改訂が付随する**〔**FR102 を「物理側で 4 種」へ移す**。**CHANGE は M21 ブロックの 106 から。着手前ゲートは通知書 ＋ registry 登録までで、本体の改訂は as-built 確定後**〕。 **★Q4・Q5＝BT の計測は要求しない。要求しても実行できない。** **⇒ ここで前提が 1 つ増えた**——**Gamepad API には接続方式を示すフィールドが無い**〔`id` にベンダ名や製品名が出ることはあるが、USB と BT を判別する標準の手段は無い〕。 **⇒ これは「測れなかった」の意味を変える。** **仮に測れていても、実行時に接続方式で分岐することはできなかった。** **したがって採りうる形は最初から 2 つしかない**——**(α) 接続方式を問わない単一の広い窓** ／ **(β) 実行時の自己校正**〔`Gamepad.timestamp` の更新間隔と rAF 間隔から実効サンプル周期を推定する。**有望だが `timestamp` の意味づけにブラウザ差がありうるため未検証**〕。 **★D-326 の懸念**〔USB 限定の閾値が BT で silent に落ちる〕**は、想定より軽い。** **理由＝本アプリは記録ツールであって対戦ではなく、`FR106` が「正確にゆっくり入力できるよう」読取表示を要求している。** **⇒ 窓が広すぎる誤り**〔別入力が 1 ステップにまとまる〕**も狭すぎる誤り**〔同時押しが 2 ステップに割れる〕**も画面に出る。** **`m21-contract` §1.3 が警戒した「黙って落とす」形は、読取表示があるかぎり成立しにくい。** **⇒ 実装サブへ渡す条件を 4 点に固定した**〔**(1) 窓は USB の実測から導き `D-324` の最大値を下回らせない**〔押し始め 45.80 ms ／ 離し始め 83.30 ms。**どちらを使うかは実装サブが決める**〕／**(2) BT は未計測と明記し「BT でも動く」と書かない**〔**E-84**〕／**(3) 窓を定数で埋め込まず 1 か所から差し替えられる形にする**／**(4) (β) が成立するかを実装サブの着手前に実査する**。**測る前に判定法を決めない**＝**D-294** ／ **E-123** は、ここでも生きている〕。 **★Q2＝アケコン非所有。ここで設計が 1 つ決まる。** **所有していない機体に「対応した」と言える唯一の形は、機体ごとの決め打ちをやめることである。** **⇒ キャリブレーション／リマップの導線が load-bearing になる**——**「この機体の LP はどのボタンか」を利用者に 1 度押させて覚える形にすれば、我々がその機体を持っていなくても成立する。** **★同じ導線が 3 つの未知を同時に吸収する**——**アケコン**〔非所有〕／**Firefox**〔未計測・index がずれる〕／**将来の未知の機体**。**⇒ 案 (C) を選んだ時点で実質的に不可避であり、編成では早い位置に置く。** **★出しかたの条件＝アケコンは「レバーレス・パッドと同じ経路で動く見込みだが実機未検証」と明示する。「アケコン対応」と書かない**〔**E-84**〕。 **★残るのは Q3 のみ**——**`P-37` として立て直した**〔判断材料は assessment §8.1〕。 |
| D-327 | 2026-08-13 | **【`check-md-emphasis.sh` が依存欠落のとき緑を返していた。D-319 の要求に真っ向から反する欠陥である】**（設計卓が自分の成果物の検査に使おうとして踏んだ。**運用・検査系**） **事象**——**`markdown-it-py` 未導入の環境で本検査を回すと、`ERROR: … 検査を実行できませんでした(未実行)` と出したうえで、続けて `現在 0 行 / ベースライン 375 行` ／ `OK ベースラインから 375 行減少。本スクリプトの BASELINE_BROKEN を 0 へ更新してよい。` ／ `結果: 違反なし` を印字し、`exit 0` を返していた。** **★従っていればベースラインが 375 → 0 へ消えていた。** **原因**——**`run_check()` は `rc=9` を見て `exit 2` していたが、この関数は `OUT=$(run_check …)` の形で呼ばれており、`exit` が死ぬのはコマンド置換のサブシェルだけである。** **親は素通りし、`CUR=${CUR:-0}` が空文字を `0` に落として「全件減少」へ化けさせていた。** **⇒ `D-319` が「未導入なら exit 2 で未実行を返し、緑を返さない」と明記した条項が、実装では成立していなかった。** **★教訓 `E-84` そのものである**〔**「0 件」と「検査していない」を同じ顔で出さない**〕。**同教訓を守るために書かれた検査自身が、同教訓を破っていた。** **★`check-artifact-integrity.sh` は「`check-md-emphasis.sh` の自己検査が通る」と緑を出していたが、自己検査に依存欠落の対照が無かったため素通りしていた。** **⇒ 是正 3 点**——**(1) `run_check` は `exit` ではなく `return` し、呼び出し側が `RC` を見て伝播する** ／ **(2) `__COUNT__` 行が得られない場合を「0 件」ではなく「未実行」として `exit 2`** ／ **(3) 自己検査へ陽性対照をもう 1 本追加した**〔`MD_EMPHASIS_FORCE_NO_DEP=1` で依存欠落の経路だけを踏ませ、**非ゼロ終了かつ `結果: 違反なし` を出さないこと**を主張する。**回帰検査であり、検査を緩める方向には働かない**〕。 **★あわせて実データ 1 件を是正した**——**`M{N}-RESEARCH-{NN}-{slug}.template.md` §157 に閉じない強調があり**〔`**「{{後続サブ}} …要決定事項」**を` ＝**閉じ `**` の直前が `」` で直後が `を`**〕、**これが HEAD 時点でベースライン 375 に対し実測 376 だった 1 行の実体である。** **`M-50` として §6 へ記録した。** |
| D-326 | 2026-08-13 | **【M21 の追加計測の要否を判定する。追加計測は overview のブロッカーではない。編成を止めているのは `REQ-001` FR102 の未追随である】**（**P-35** の前段。**開発者の指示 2026-08-13**＝「M21-overview を書く前に、追加計測が要るかを判断させてください」） **一次源＝`docs/process/m21-measurement-scope-assessment.md` v1.0.0**〔判定の根拠・外部情報の出典を含む〕。**本行は結論だけを持つ。** **★結論 1＝Firefox は計測不要。** **Chrome と構造的に違うことは公表挙動で既に確定しており**〔`mapping` が `standard` にならない場合がある ／ トリガーと D-pad が `axes` 側に出る ／ `axes` 要素が 1 つ多く index が全軸ずれる ／ 同一機種が Chrome 17 ボタン 4 軸・Firefox 11 ボタン 8 軸〕、**測って初めて分かる話ではない。** **かつ帰結は Chrome 単独の実測から既に出ている**——**D-324 のアナログ値の機種差**〔パッド button 7 は 12 段階・レバーレスは 0/1〕**が、既に「`mapping` と index の固定を前提にしない機種プロファイル層」を要求している。** **⇒ Firefox はこの結論を補強するだけで、編成を変えない。** **★結論 2＝Bluetooth は要る。ただし overview の前ではない。** **BT HID の既定ポーリングは 125 Hz**〔8 ms 刻み〕**で、USB の 1〜4 ms に対し 5〜15 ms を加算し、2.4 GHz 帯の干渉で再送由来のスパイクを出す。** **⇒ D-324 の同時押し分布**〔中央値 8.30 ms ／ p95 20.90・33.30 ms ／ 最大 20.90・45.80 ms〕**と同じオーダーであり、効くのは量子化ではなくテールである。** **`m21-contract` §1.3 が名指しした「動いているように見えて一部の入力を黙って落とす」がまさにこの形。** **⇒ 置き場所は「判定窓を決めるサブの段 2」とする**——**計測が変えるのはサブの中の値であってサブの分割ではないため、overview を待たせない。** **人手作業をサブの継ぎ目に置く**〔教訓 **E-55**〕。**最小仕様は assessment §3.4**〔**既存ハーネスを再演・既存 2 機種のみ・変える変数は接続方式だけ・軸は B と C の 2 本・成果物は分布のみで閾値は書かせない**＝**D-294** を踏襲〕。 **★結論 3＝アケコンとキーボードは計測の問題ではない。** **アケコンはレバーが `buttons` と `axes` のどちらに出るかが HID 実装とブラウザで変わるため断定しない**〔**E-117**〕**が、これは結論 1 で既に必要とした同じプロファイル層が吸収する。単独の追加計測は要らない。** **実機が無ければ「プロファイル未検証」と明示して出し、「対応した」と書かない**〔**E-84**〕。 **キーボードは Gamepad API ですらない**〔KeyboardEvent・イベント駆動・チャタリングは OS 側で除去済み〕——**PoC の分布を 1 つも流用できないが、同時に FR108 の論点も存在しない。計測不要・独立サブ。** **★そして本当の論点は計測ではなかった。** **`REQ-001` FR102**〔**必須・フェーズ1**〕**が「仮想コントローラはレバーレス／アケコン／PS5パッド／キーボードの 4 種類に対応する」と書いたまま残っており、`DES-005` §6.1 は「現行の仮想はカテゴリタブ式であって 4 レイアウトではない・§6.2〜§6.5 は物理入力 M21 の将来仕様として温存する」と書いている。** **`CHANGE-057` は同温存を確認事項として開発者に諮り承認を得ているが、`change-report-057` は「REQ-001 変更なし」と記録しており、REQ 側が追随していない。** **⇒ 4 レイアウトは「あったらいい将来仕様」ではなく、必須・フェーズ1 の要件の本体が重要・フェーズ3 の M21 に預けられた状態である。** **同型が FR103 にもある**〔**必須・フェーズ1** が「物理コントローラ〔ゲームパッド・キーボード〕を通じて」と書く一方、技術前提は「物理コントローラ・キーボードはフェーズ3」〕。 **⇒ M21 が 4 レイアウトのうち何種を負うかでサブの本数と DoD の重さが変わり、これは計測では出ない。** **設計卓は独断しない**〔`CLAUDE.md` §8 の設計書間の矛盾 ／ §10 のマイルストーン構造の独断拡張の禁止〕——**`P-36` として開発者へ上げた。** **★設計卓の推奨は案 (C)**〔**ゲームパッド系 3 種を 1 つのプロファイル層で扱い、キーボードは独立サブ**〕。**理由 2 点**——**(a) プロファイル層は案 (A) でも必要であるため、層の費用は変わらず「プロファイルを何枚書くか」だけが変わる** ／ **(b) 案 (B) が重いのは 4 種を 1 つの入力系として扱おうとするからで、Gamepad 系 3 種とキーボード 1 種に割れば後者が前者の DoD を汚さない。** **★いずれを採っても `REQ-001` の改訂が要り、CHANGE は M21 ブロックの 106 から払い出す。** |
| D-324 | 2026-08-12 | **【M21 PoC の結果を確定する。M21 は成立する。閾値は決めない】**（**D-294** の回収。`M21-RESEARCH-01` 報告） **★問い 1＝経路 (a)(b) で Gamepad が取れるか → 取れる**〔実測・2 機種。**経路 (a)**＝使い捨てハーネス `http://localhost:47380` ／ **経路 (b)**＝本体アプリ `http://localhost:47319`。**`Gamepad.id`・`mapping`・ボタン数が両経路で一致**〕。 **⇒ M21 は成立する。** **★ただし全経路で user gesture が必須である**——**入力 UI を開いた直後は `navigator.getGamepads()` が空を返し、利用者が最初の 1 回を押すまでコントローラは見えない。** **これは M21 の入力 UI の設計に直接効く**〔「認識しています／していません」の表示と、最初の 1 回を促す導線が要る〕。 **★経路 (c) は「未測定」であって「取れなかった」ではない**〔`E-84`〕——**開発者が試行したが LAN 経由でページに到達せず、Gamepad API を一度も実行していない。** **到達しなかった理由の推定**〔未検証〕＝**devContainer は Docker ブリッジ上にあり、VS Code のポート転送は既定でホストの localhost にのみバインドする。** **測るなら配信元を Windows 側へ変える必要がある。** **D-309 によりブロッカーではない。** **★問い 2＝同時押しのズレ幅**〔**分布のみ。閾値は決めない**＝**D-294**〕——**中央値 8.30 ms で 2 機種一致 ／ p95 は レバーレス 20.90 ms・パッド 33.30 ms ／ 最大は 20.90 ms・45.80 ms ／ 標本 各 90。** **p99 は標本 90 件では nearest-rank の要件 100 件に満たないため出していない**〔指示書 §4.5.4 の予告どおり〕。 **★押し始めと離し始めで分布が違う**——**離し始めが一貫して大きい**〔中央値 12.50 ms・最大 50.00 ms／83.30 ms。**2 機種・両方向で同じ向き**〕。**どちらを判定に使うかは M21 本実装で決める。** **★フレーム数で判定窓を置いてはならない**——**計測環境は約 238 Hz**〔rAF 間隔中央値 4.20 ms〕**であり、60 Hz 環境では同じ実時間が約 1/4 のフレーム数になる。ミリ秒が一次で、フレーム数は本環境の換算値である。** **★問い 3＝チャタリング → 本計測条件では観測されなかった**——**立ち上がり総数 50 ＝ 指示回数 50 ＝ 画面カウンタ 50 が 2 機種とも完全一致**〔**チャタリングが起きていれば立ち上がり総数が押下回数を上回る**〕。**1〜32 ms の帯に 3 指標**〔立ち上がり間隔・押下時間・離間時間〕**とも 0 件。** **⇒ `E-41` 型の「対策が不要」という第三の結論の候補である。** **★ただし 2 機種のみであり一般化しない。** **★新しい論点が 1 つ出た＝アナログ値**——**パッドのトリガー〔button 7〕は押し込みの途中経過を 12 段階で返すが、レバーレスは同じ button 7 でも `[0, 1]` の 2 値のみである。** **⇒ `pressed`〔デジタル〕だけを見るか `value`〔アナログ〕も見るかで、機種による挙動差が出る面がある。** **D-4 が「機種で分かれる蓋然性が高いので合算するな」と指示した事象が、実際にここで現れた。** **★測っていない範囲を本実装の前提にしない**——**ブラウザは Chrome 151 のみ ／ 接続方式は USB のみ ／ 機種は 2 種のみ**〔Firefox・Edge・Bluetooth・アケコン・キーボードは未測定。`DES-005` §6.2〜§6.5 は 4 レイアウトを将来仕様として温存している〕。 **⇒ 次は M21-overview の編成である**〔**P-35**〕。 |
| D-325 | 2026-08-12 | **【配布バイナリが既存 DB のある環境で起動できない。M21 の範囲外だが配布に直結するため追跡する】**（`M21-RESEARCH-01` §4.1 の想定外の発見） **事象**——**`make build-windows` で作った `combomgr-windows-amd64.exe` を Windows で起動すると、マイグレーション `000026` が `UNIQUE constraint failed: moves.character_id, moves.code` で失敗して起動できない。** **2 回目以降は golang-migrate が dirty フラグを立てるため即停止する**〔`Dirty database version 26`〕。 **★マイグレーション自体は正常である**〔実測。devContainer でまっさらな DB へ全適用したところ `schema_migrations = (68, 0)`・`moves` 1,653 行・`characters` 19 行で正常完了〕。 **⇒ 原因は `%APPDATA%\combomgr\combomgr.db` に残っていた旧ビルドの DB である。** **★開発者判断 2026-08-12＝当該 DB は削除してよい**〔保全すべきデータは無かった。**削除は開発者が行う**——`CLAUDE.md` §10 により Claude Code は `*.db` を直接削除せず、加えて当該ファイルは Windows 側でコンテナから到達できない〕。 **★ただしこの判断は事象を消さない。** **観測されたのは「旧ビルドの DB が残っている環境では、新しい配布バイナリが起動できない」という挙動であり、今回のインスタンスが不要だったことは別の事実である。** **⇒ `DES-002` §11.2〔配布〕に直結する。** **followup へ `distribution-existing-db-migration-failure` として登録し、ボード P-13〔配布の制約〕と束ねる。** **★M21 のスコープには入れない**——**PoC は原因究明も修正も行っていない**〔スコープ外〕。**配布の解禁前に潰す必要がある。** |
| D-323 | 2026-08-12 | **【M20-01 の設計伝達レポートに回答する。§2-1 は「反映しなかった」ではなく「反映したが届かなかった」である】**（製造の判断依頼 1 件 ＋ 判断要請 1 件 ＋ 申し送り 6 件） **★§2-1 への答え＝案 (1) は既に実施済みである。** **指示書は v1.0.1、レビューチェックリストは v1.1.0 で、A-1〜A-4 はすべてファイルに入っている**〔commit **`51f9090`**。§4.6.1＝`srk` へ付け替えて存置 ／ §4.6.2＝期待値 7 か所 ／ §4.7＝`migrate_m2001_test.go` ／ §4.8＝godoc の条件分岐 ／ チェックリスト §9-6 の差替えと N-9〜N-12〕。 **⇒ 製造が v1.0.0 を見ていたのは、実装ブランチが `c1f37ee` を基点にしており、その後の設計卓の commit を取り込んでいなかったためである**〔`c1f37ee` → `248def8` → **`51f9090`** の順で、反映は着手の 2 コミット後に載った〕。 **★したがって失敗の型が違う。** **製造の §7-4 は「裁定は出したがファイルに入らなかった」と読んでいるが、実際は「同じ手番でファイルへ入れたが、別ブランチの相手には届かなかった」である。** **帰結は製造の記述どおりで正しい**——**レビュアーへ裁定を明示的に渡さなければ、正しい実装が §9-6 の重大と誤判定されるところだった。** **⇒ 機構化の内容も変わる。** **「同じ手番でファイルへ入れる」だけでは足りない。回答の中に反映 commit のハッシュを書き、ブランチの更新を明示的に指示する。** **★これは本移行で初めて出た型である**——**M18/M19 期は設計卓が Web 版で repo を持たず、裁定は必ずチャット経由で届いた。設計卓が Code へ移って repo へ書けるようになったことの副作用である。** **設計卓は「書いた」で完了と感じるが、相手のブランチには存在しない。** **★§4-1 への答え＝`CHANGE-098` のスコープを 4 文書へ拡大する。** **理由＝「組み込み 5 → 3」は 1 つの設計変更であり、波及先が複数文書に及ぶだけである。CHANGE の単位は文書ではなく変更である**〔先例＝`CHANGE-006` が `REQ-001` ／ `DES-002` ／ `DES-003` ／ `DES-005` にまたがる〕。 **⇒ 対象＝`DES-004` §3.1・§3.4・§4.2 ＋ **§8**〔新規追加〕 ／ `DES-006` **VAL-P05 とその根拠段落**〔新規追加。**M20-04 のプリセット作成機能が直接参照する検証仕様であり優先度が高い**〕 ／ `DES-003` **§6**〔新規追加〕。** **`SUPP-001` §3.4・§7.3・§7.5.5 は自由改訂資料であるため CHANGE 対象外**〔設計卓が直接改訂する〕。 **★`SUPP-001` §7.5.5 には第 2 の誤りがある**——**同行は「組み込みプリセットのエイリアス編集は許容」と書いており、`D-290`〔編集不可を維持〕と正面から衝突する。** **上限の数と同時に是正する。** **★申し送り 6 件の処遇**——**§1-1**〔削除・改称サブの走査軸 2 本立て〕**と §1-2**〔非破壊条件のミューテーション裏取り。**M19-04d に続く 2 例目**〕**は `SUPP-001` §5.5 へ規約として追記する** ／ **§1-3**〔`PRAGMA foreign_keys` が効いていない実測〕**は `SUPP-001` §7.3 へ併記し、M20-04 の指示書で「CASCADE に頼らない」を明示する** ／ **§3-4**〔削除サブはベースライン型検査のラチェットを緩める〕**は指示書テンプレの §7 DoD へ常設項目として入れる**〔**設計卓自身も同日 `check-md-emphasis.sh` のベースラインを 377 → 375 へ下げており、同型である**〕 ／ **§4-3**〔`recipe_cache` に id `2` / `4` のキーが残る〕**は M20-05 の前提として §1.4 へ記録** ／ **§4-4**〔プリセット作成・削除 API が未実装で VAL-P05 の強制も 0 件〕**は M20-04 の指示書で「§7.3 の 403 保護 ＋ VAL-P05 の上限検査 ＋ `config.preset_id` の実在検査〔D-313〕」を 3 点セットで扱う** ／ **§4-5**〔クラウド実行環境で E2E が完走しない〕**は `docs/process/remote-ops.md` へ切り分け手順を明文化する** ／ **§4-6**〔`BUILTIN_PRESET_CODES` の位置〕**は `followup-backlog` へ `builtin-preset-codes-location` として登録し M20-04 へ割り付ける。** **★§4-2 は設計卓の片側ドリフトである**——**`architecture-patterns.md` の「残り 4 プリセット」を、設計卓は `followup-backlog` 側だけ直して同書を直さなかった。** **`E-118` そのものであり、指摘のとおり是正する。** |
| D-322 | 2026-08-12 | **【`alias_text_en` の列追加を M20-02 に置くことを開発者が確認した】**（**D-321** の置き場所の確定） **開発者確認 2026-08-12**＝**「素直にそこでいい」**。 **⇒ `preset_aliases` への `alias_text_en TEXT NULL` の追加は M20-02 が持つ。M20-03 は制約だけを持つ。** **★理由は 1 つで足りる**——**列が無ければ 30 種を入れられない。** **M20-03 の順序制約〔生成の後に制約〕は「制約」に掛かるものであり、列追加には掛からない。** **★これは列そのものの再承認ではない**——**列の採否は D-317 で承認扱いにしてあり、本裁定が確認したのは「どのサブに置くか」である。** **D-317 が置いた「異議があれば M20-03 の着手前に止められる」という退路は、そのまま残す。** **★記録済みの写し先は 5 か所**〔`M20-02` 指示書 §2.1・§4.1 ／ 同レビューチェックリスト §2・§9-7 ／ `M20-overview` §3 の M20-02 欄 ／ `m20-contract` §4.1 ／ 本表 D-321〕。**サブの分割・連番・実装順は変えていないため D-301 の承認範囲内である。** |
| D-321 | 2026-08-12 | **【D-314 と D-317 の関係を確定する。`alias_text_en` を持つのは 30 種だけで、D-317 の行数見積りは過大だった】**（**D-314** ／ **D-317** の突合。**設計卓の自己是正**） **2 つの裁定が同じ対象に別の答えを出したまま並んでいた**——**D-314 は `micro_forward`＝`micro6` / `micro.f` と決め、D-317 は「記法が語を持たない行に `alias_text_en` を持たせる」と決めた。** **後者を採ると前者の造語は要らなくなる。** **⇒ D-317 が優先する。** **`micro6` ／ `micro4` ／ `micro.f` ／ `micro.b` は使わない**〔**開発者が違和感を述べた当の造語であり、(C) を採った目的がそれを消すことだった**〕。 **★`alias_text_en` を持つのは「記法自身が語を持たない」行だけである。** **移動系 9 code のうち 7 code は記法自身の語彙であり、言語に依存しない**——**`forward`＝`6` / `f` ／ `back`＝`4` / `b` ／ `dash_forward`＝`66` / `dash` ／ `dash_back`＝`44` / `backdash` ／ `jump_neutral`＝`8` / `nj` ／ `jump_forward`＝`9` / `fj` ／ `jump_back`＝`7` / `bj`。** **この 7 code に `alias_text_en` は要らない**〔D-314 のこの 7 行はそのまま生きる〕。 **★en を持つのは 2 ＋ 28 ＝ 30 種**——**`micro_forward` ／ `micro_back` の 2 code と、P-34 の派生 28 件。** **`micro_forward`＝`alias_text` `微歩き` / `alias_text_en` `microwalk`、`micro_back`＝`微下がり` / `back microwalk`**〔**設計卓の提案。開発者が上書きしてよい**〕。**28 件の値は列挙後に決める**〔**P-34**〕。 **★D-317 が書いた行数は過大だった。是正する。** **旧記載「移動系 171 ＋ 派生 28 ＝ 199 行 × 2 プリセット ＝ 約 398 行」は、移動系 9 code すべてが en を要する前提だった。** **正しくは `(2 code × 19 キャラ ＝ 38 行) ＋ 28 行 ＝ 66 行、2 プリセットで 132 行。値の種類は 1 プリセットあたり 30 種。** **D-317 の本文は書き換えない**〔**D-57** ／ **D-259**〕。**本裁定が数を差し替える。** **★列を足すサブは M20-02 とする。** **理由**——**列が無ければ 30 種を入れられない。** **M20-03 が持つのは「制約」であり、順序制約〔生成の後に制約〕は列追加には掛からない。** **サブの分割は変えないため D-301 の承認範囲内である**〔`M20-overview` §3 の M20-02 欄へ追記する〕。 **★`alias_text` を空にしてフォールバックさせる案は採らない**——**行が無ければ `alias_text_en` を掛ける先が無い。** **`numeric` / `srk` にも `微歩き` の行を明示的に持たせ、そこへ en を添える。** **`official_ja_move` と値が重複するが、それは意図した重複である。** |
| D-320 | 2026-08-12 | **【M20-01 の Plan Mode 判断依頼 2 件と参考 2 件に回答する。指示書は v1.0.1・チェックリストは v1.1.0】**（**製造の実査 3 件がいずれも設計卓の誤りを拾った**） **Q1〔`TestRepository_ListAliasesByPreset_Empty` の処遇〕＝案 (B) を採る**——**引数を `srk` へ付け替えて関数を存置する。** **★(B) はチェックリスト §9-6 の禁止に触れない。** **同項の趣旨は「`numeric_en` を `numeric` へ書き換えて、消したという事実を覆い隠すこと」の禁止であり、付け替え一般の禁止ではない。** **`numeric` は `#1`・`#4`・`#5` が既に引いており主張が重複するが、`srk` は重複しない。** **★(A)〔関数ごと削除〕を採らない理由**——**`ListAliasesByPreset` の「空プリセットで 0 件」という検査資産が失われる。`#1` は別メソッド `FindAlias` であり代替にならない。** **初版が「削除」と書いたのは `:157` と `:163` を独立した 2 件と読んだためで、実物は 1 関数だった**〔**M-43**〕。 **★(C)〔削除して `numeric` の別テストを新設〕も採らない**——**`numeric` を引く時点で §9-6 の形になり、かつ新規追加は本サブの変更に別の変更を混ぜる。** **★`srk` は M20-02 でエイリアスが入ると落ちる。それは意図した仕掛けである**〔`#6` と同型＝**D-308**。**「まだ空である」という事実を守る資産**〕。 **Q2〔契約テスト 10 件の置き場所〕＝案 (A) を採る**——**新規 `internal/infra/migration/migrate_m2001_test.go` に `TestRun_M2001_*`。** **★これは §2.1 からの逸脱ではなく、§2.1 の記載が誤りである**〔**M-42**〕。**`SUPP-001` §5.5 (2) が「HEAD スコープの主張をサブの名前空間へ相乗りさせない」と定めており、`migrate_test.go` は HEAD スコープの主張を含む。** **設計卓は同規約を必読に挙げ、チェックリスト §2 へ書き写しておきながら §2.1 でそれに反した。** **`migrate_test.go` 自体は触る**〔§4.6 `#6` の code 書き換えと §4.6.2 `#7` の期待値更新のため〕。 **参考 1〔期待値 7 か所の 5 → 3〕＝製造判断に同意する。** **`SUPP-001` §5.5 (2) のとおり、HEAD スコープのテストが落ちたら期待値を更新するのが正しい対応である**〔本サブが実際に組み込みを 3 種にした以上、`5` はもう真ではない〕。 **★条件を 1 つ付ける**——**1 件ずつ「その 5 は何の 5 か」を確認して完了報告に書くこと**〔**E-16** ／ **E-93**。**`recipe_cache` のエントリ数 5 が「プリセット数 5」を意味するのか別の 5 なのかは行を見ないと決まらない。意味が違うものが混じっていたら、その行だけ更新せずに報告する**〕。**指示書 §4.6.2 として表に載せた**〔**M-44**〕。 **参考 2〔`internal/model/preset.go` の godoc「上限 10 件」〕＝製造の前提を是正する。** **「`SUPP-001` §7.3 は設計卓の手番」を、この godoc へ広げてはならない。** **設計卓が持つのは設計資料であって、実装ファイルの godoc ではない。本サブは同じファイルを既に編集している。** **⇒ 条件付きで分岐する**——**`10` を強制している実装が無ければ godoc を直す**〔「組み込み 3 ＋ カスタム 5 ＝ 8」。**`CLAUDE.md` §4 は公開 API の godoc を必須としており、失効した数を残すほうが規約違反側である。§4.9 の否定形確認 4 に自分で引っかかりもする**〕／**定数・検査が `10` を強制していれば触らない**〔**上限の変更は挙動変更でありスコープ外**。所在と実値を報告し、設計卓が §7.3 と同じ手番で扱う〕。**指示書 §4.8 として新設した。** **★godoc を直した場合、`SUPP-001` §7.3 と一時的に食い違う。想定内である**〔設計卓は as-built を見てから §7.3 を直す〕。 **★本裁定の意味**——**Plan Mode の実査 3 件がいずれも設計卓の誤りを拾った**〔M-42／M-43／M-44〕。**3 件とも「設計卓が現物を持たないこと」に由来し、3 件とも製造が実物を見た瞬間に出た。** **§3.3 を 7 項目に増やし Plan Mode を必須にした判断は、この形で回収された。** |
| D-319 | 2026-08-12 | **【`check-doc-refs.sh` は武装中の判定不能を SKIP として分離する。閉じない強調の常設検査を新設する】**（運用・検査系。**開発者裁定 2026-08-12**） **(1) `check-doc-refs.sh` の 6 件**——**ALLOW 表へは入れない**〔**恒久免除にすると、本当に消えたときに気づけなくなる**〕。**「武装中 かつ 参照先のトップ階層ディレクトリが作業ツリーに無い」ものだけを SKIP〔未検査〕として分離し、NG に数えない。** **未検査の件数を明示して報告する**〔**「0 件」と「検査していない」を同じ顔で出さない**＝教訓 **E-84**〕。**武装していないセッションでは従来どおり NG になる。** **自己検査に陽性 1・陰性 2 を足した**〔武装中 web/ 不在 → SKIP ／ 非武装 → SKIP にしない ／ 武装中でも docs/ は実在 → SKIP にしない〕。 **(2) `scripts/check-md-emphasis.sh` を新設した**——**Markdown の閉じない強調**〔`**A（B）**は` の形。**閉じ `**` の直前が全角約物・直後が文字だと right-flanking を満たさず、強調が閉じずに後続へ太字が漏れる**〕**を、CommonMark 実装で 1 行ずつ描画して判定する。** **★偶数性チェックでは原理的に検出できない型である**〔**`**` の個数は偶数のまま壊れる**。**D-258 ／ D-266 ／ D-287 が積み上げてきた近似の限界**〕。**設計卓の自作近似は parallel-board を「22 件」と誤報し、実レンダラでは 7 行だった**〔コードスパン内の `**` と、全角コロン・全角スラッシュ・長音記号の誤判定を含んでいた〕。 **★ベースライン固定型**〔**2026-08-12 実測 377 行**。対象＝`docs/process` ／ `docs/handover` ／ `docs/instructions` ／ `docs/design` ／ `docs/change-notes`。**`archive/` ・ `phase1/` ・ `phase2/` ・ `docs/progress/` は除外**＝歴史記録〕。 **★`markdown-it-py` を要する。未導入なら exit 2 で「未実行」を返し、緑を返さない**〔同じく **E-84**〕。**`CLAUDE.md` §8 の検査表へ 1 行追加した。** |
| D-318 | 2026-08-12 | **【OD 技 `departure_od` の値を是正する。`sonic_blade_od` は確認済みで是正不要】**（**D-316** の確定） **開発者回答 2026-08-12。** **(1) `jp` / `departure_od` は `d dl l plus p_h` ではなく `d dl l plus p p` が正しい**〔記載ミス。**D-316 が指摘した「1 件だけボタンが 1 個」という構造の違いが、実際に誤りだった**〕。 **⇒ 4 件は次で確定する**——**`guile` / `sonic_blade_od`＝`d dl l plus p p` ／ `jp` / `triglav_od`＝`d d plus p p` ／ `jp` / `departure_od`＝`d dl l plus p p` ／ `lily` / `condor_spire_od`＝`d dr r plus k k`。** **4 件とも 2 ボタン同時押しで揃った。** **(2) `sonic_blade_od`〔`d dl l plus p p`＝236PP〕と派生 `sonic_cross1_od`〔`r plus p p`＝6PP〕のモーション差は、どちらも正しい**〔開発者確認〕。**是正不要。** **★D-316 の本文は書き換えない**〔**D-57** ／ **D-259**〕。**本裁定が値を差し替える。** **★M20-02 の指示書には本裁定の 4 値を書く。** **★構造の違いを問うたことが誤りを 1 件拾った**——**「4 件のうち 1 件だけ形が違う」は、内容を知らなくても言える指摘である。** **設計卓は実データを見られないが、形の不揃いは見える。** |
| D-317 | 2026-08-12 | **【記法が語を持たない技の日英両対応は (C) を採る。`preset_aliases` に英語列を 1 本足す】**（**P-33** の決着・**スキーマ承認ゲート**） **開発者判断 2026-08-12**＝**「B か C。おすすめの方法でよい。副作用の 1 は気にしないが、2 と 3 は気になる」**。 **⇒ 設計卓は (C) を採る。** **理由は開発者が気にした 2 点そのものである**——**(B) 表示時の置換表は辞書をフロントに置くため、DB に英語が存在しない。** **したがって逆引きは片側しか効かず**〔副作用 2〕、**共有した英語文字列を相手が取り込めない**〔副作用 3〕。**(C) は同じ辞書を DB に置くだけで、置き場所が違うだけで両方が解ける。** **★副作用 1〔部分一致の誤爆〕は (C) では起きない**——**文字列置換ではなく行単位の選択になるため。** **開発者は 1 を気にしないと述べたが、(C) を採ると 1 自体が消える。** **★具体形 4 点。** **(1) `preset_aliases` に `alias_text_en TEXT NULL` を 1 列追加する**〔**新列＝スキーマ承認ゲート**。**NULL 可のため既存 1,653 行は無改変**〕。**埋めるのは記法が語を持たない行だけ**——**移動系 9 code × 19 キャラ ＝ 171 行 ＋ 派生 28 件 ＝ 199 行、これが `numeric` と `srk` の 2 プリセット分で約 398 行。値の種類は 1 プリセットあたり 37 通り。** **(2) 解決順序**——**表示ロケールが en かつ `alias_text_en` が非 NULL ならそれを使い、それ以外は `alias_text` を使う**〔`DES-004` §5 の改訂〕。**§5.3 の 3 段フォールバックは不変である**〔**`alias_text_en` が NULL なのは「英語表記を持たない」であって、エイリアス未定義ではない**〕。 **(3) 逆引き**——**取込ヘルパーは `alias_text` と `alias_text_en` の両方を引く**〔`DES-004` §5 冒頭注記の改訂。**これで英語表示を見た利用者のメモも解決する**〕。 **★これは M20-03 を重くする**——**一意にすべき単位が 2 列の合併集合になる。** **`(character_id, alias_text)` だけでなく `(character_id, alias_text_en)` と、両者の交差**〔ある技の `alias_text` が別の技の `alias_text_en` と一致する形〕**も見る必要がある。** **(4) `recipe_cache`**——**表示文字列がロケールに依存するため、キーに locale を含める**〔`SUPP-001` §7 の改訂〕。**M20-05 が D-313 でキー導出を触るため、同じ手番で入れるのが最も安い**〔**E-110** の型〕。**費用は D-303 の実測から見て問題にならない**——**53 エンティティ × 3 プリセット × 2 ロケール ＝ 318 エントリで、eager の判断は変わらない。** **★開発者の「B か C、おすすめでよい」を (C) への承認として扱う。** **ただし新列であるため、異議があれば M20-03 の着手前に止められる。** **★D-289「英語技名は持たない」とは衝突しない**——**同裁定の理由は「31 キャラ 1,653 行の再収集になる」ことであり、本件は 37 通りである。** |
| D-316 | 2026-08-12 | **【OD 技 4 件の `command` 値を開発者から受領した。CSV 補記と seed 反映は M20-02 が持つ】**（**D-312** の続き） **開発者提供 2026-08-12。逐語で記録する**〔**一次源はゲーム内表示であり、repo のどこにも無い値である**〕。 **`guile` / `sonic_blade_od`＝`d dl l plus p p` ／ `jp` / `triglav_od`＝`d d plus p p` ／ `jp` / `departure_od`＝`d dl l plus p_h` ／ `lily` / `condor_spire_od`＝`d dr r plus k k`。** **★語彙は既存のトークン語彙に適合している**〔`TOOL-002` §9.9 ／ `docs/seed-data/official-data-edge-cases.md` §3.4 の実測出現表に `d` `dl` `dr` `l` `r` `plus` `p` `k` `p_h` がすべて載る〕。**新しいトークンは 1 つも含まれない。** **★開発者へ確認したい点が 2 件ある**〔**普段は機械入力の箇所を手で入れたため、と開発者自身が述べている**〕——**(1) `departure_od` だけボタンが 1 個で強度指定つき**〔`p_h`〕**であり、他の 3 件は 2 ボタン**〔`p p` ／ `k k`〕**である。OD 技は通常 2 ボタン同時押しであるため、`p_h` が意図どおりかを確認したい。** **(2) `sonic_blade_od` を `d dl l plus p p`〔236PP〕としたが、その派生である `guile` / `sonic_cross1_od` は `r plus p p`〔6PP〕として既に登録されている**〔同 §3.3〕。**派生と元技でモーションが違うこと自体はありうるが、突き合わせておきたい。** **★作業の置き場所は M20-02 とする。** **理由**——**(a) CSV へ入れるだけでは既に seed 済みの DB は変わらず、反映には backfill マイグレか seed 波の再走が要る**〔**D-312**〕。**(b) M20-02 は「seed 波ごとに再適用できる規則」を作るサブであり**〔`M20-overview` §4.4〕**同じ機構に乗る。** **⇒ M20-02 の指示書に 3 段で書く**——**(i) CSV 4 行への挿入**〔上の値を逐語で。**挿入前に §3.3 で現況が空欄であることを実査**〕／**(ii) 挿入値の検算**〔**トークン語彙に適合するか ／ 同キャラの同系統技と矛盾しないか ／ `move_commands` の `token_key` へ正規化したとき既存キーと衝突しないか**。**`UNIQUE(character_id, token_key)` は意図的に無いため衝突しても投入は通る**＝`DES-003` §3.9。**通ってしまうからこそ検算が要る**〕／**(iii) seed への反映確認**〔**`move_commands` に 4 行が載ったか ／ `internal/moveindex` の索引に載ったか ／ `DES-002` §7.5 の skip 判定を通過するか**。**4 件は `is_derived=false` であるため `is_derived` の門では落ちない**＝A-4 の実測〕。 **★M20-01 のブロッカーではない。** |
| D-315 | 2026-08-12 | **【コマンドを持たない技には第 3 の類がある。28 件。まだ何も決めていない】**（**開発者指摘 2026-08-12** ／ **D-310**・**D-311** の欠落の補完） **指摘＝「移動系以外にも、何も押さないことで派生する `special` 等があるはずで、それもフォールバックか」。** **⇒ 実在する。設計卓は 2 類しか数えていなかった。** **★件数は既存の実測から確定する**〔web レポート A-3〕——**CSV の `command` 空欄は 304 行で、内訳は `rush_variant` 272 ／ `special` 26 ／ `unique` 6。** **`is_derived=false` は 4 行で、4 件とも `special`**〔**D-312** の OD 技〕。 **⇒ 「`is_derived=true` かつ `command` 空欄かつ `rush_variant` でない」行は 22 ＋ 6 ＝ 28 件である。** **検算＝272 ＋ 28 ＝ 300 ＝ `is_derived=true` の空欄総数と一致する。** **★この 28 件は D-310〔移動系 171 行〕でも D-311〔`rush_variant` 272 行〕でも扱っていない。フォールバックさせるとも決めていない。** **★決める前に列挙する**——**レポートは件数までしか出しておらず、28 行の中身は誰も見ていない。** **母数が未知のまま規則を決めない**〔**D-302** と同じ型〕。 **⇒ M20-02 の §3.4 で 28 行を全件列挙させる**〔`character_code` ／ `move_code` ／ `category` ／ `name_ja` ／ 派生元の有無〕。**設計卓は `character_data/` を見られない**〔武装〕**ため、実査は製造の手番に置く。** **★設計卓の見込み**〔決定ではない〕——**`rush_variant` と同じ「元技の表記 ＋ 派生を示す語」の合成形になる公算が高い**〔**D-311**〕。**`move_derivations`**〔`CHANGE-091` の新表〕**が派生元を持つ行は合成でき、持たない行だけがフォールバックに落ちる。** **★「入力がある派生」と混同しない**——**`is_derived=true` で `command` が埋まっている 281 件は、索引に載らないだけで表記は生成できる**〔`internal/moveindex` が `is_derived` で索引対象外にしているのは「単独で入力できない」ためであって、コマンド文字列が無いからではない〕。**28 件はそれとは別の集合である。** **★P-30 の解消は 3 類のうち 2 類とデータ不備で閉じており、第 3 類が残っていた**——**P-34 として起こす。** |
| D-314 | 2026-08-12 | **【D-310 の表を差し替える。`numeric` / `srk` から日本語を 1 語も出さない】**（**D-310** の改訂・**開発者指摘 2026-08-12**） **指摘＝「フォールバック時に日英どちらでも不自然なく表示する方法はどうするつもりか」。** **⇒ 前回の説明は答えになっていなかった。訂正する。** **D-310 の案は `micro_forward` / `micro_back` を `official_ja_move` へフォールバックさせるもので、英語圏の利用者には日本語 2 語**〔`微歩き` ／ `微下がり`〕**が出る。** **「2 語だけに減る」「記法に無いからという理由が説明できる」は、不自然さを消す議論ではない。** **★改訂案＝この 2 code にも記法側の表記を与える。** **`micro_forward`＝`micro6`〔numeric〕／ `micro.f`〔srk〕 ／ `micro_back`＝`micro4`〔numeric〕／ `micro.b`〔srk〕。** **⇒ 日本語へのフォールバックは 0 件になる。** **★記号を発明していない。語を添えているだけである**——**根拠 2 点。** **(1) `numeric` は既にラテン文字トークンを含む**〔`DES-004` §3.4 のサンプル表＝`5LP` ／ `2LK` ／ `DR` ／ `cDR`〕。**`micro6` は新しい種類のものではない。** **(2) SRK 記法は「接頭辞 ＋ `.`」の語彙体系である**〔`st.` ／ `cr.` ／ `j.`〕。**`micro.f` はその体系に収まる。** **D-310 が `srk` に `dash` ／ `backdash` を置くことを既に認めており、それと同じ性質である。** **★あわせて srk のジャンプ 3 件を改める**——**D-310 の `j.` ／ `j.f` ／ `j.b` は不可。** **SRK では `j.` は空中攻撃の接頭辞であり**〔`j.HP` 等〕**単独で移動ジャンプに使うと衝突する。** **⇒ `jump_neutral`＝`nj` ／ `jump_forward`＝`fj` ／ `jump_back`＝`bj` へ改める。** **★確定形の 9 行**〔`numeric` / `srk` の順〕——**`forward`＝`6` / `f` ／ `back`＝`4` / `b` ／ `micro_forward`＝`micro6` / `micro.f` ／ `micro_back`＝`micro4` / `micro.b` ／ `dash_forward`＝`66` / `dash` ／ `dash_back`＝`44` / `backdash` ／ `jump_neutral`＝`8` / `nj` ／ `jump_forward`＝`9` / `fj` ／ `jump_back`＝`7` / `bj`。** **★D-310 の本文は書き換えない**〔**D-57** ／ **D-259**〕。**本裁定が D-310 の表を差し替える。** **文字列の最終確認は引き続き P-33。** |
| D-313 | 2026-08-12 | **【`recipe_cache` の読み出しキーは既定プリセット設定に追随させる。2 つのハードコードを 1 つへ寄せる】**（**P-32** の決着） **開発者判断 2026-08-12**＝**「既定プリセットに追随すべき」**。 **⇒ web レポート C-3 §4 の案 2 を採る**〔`recipe_cache` のキーを `config.Defaults.PresetID` から取る〕。**案 3〔現状維持〕は採らない。** **★現況**〔実測 C-3(e)〕——**`config.Default().Defaults.PresetID = 1`〔`int64`〕と `model.DefaultPresetID = "1"`〔`string`・`recipe_cache` JSON のキー〕が同じ概念を 2 か所で独立に持ち、連動していない。** **`PUT /api/config` で `defaults.preset_id` を 2 に変えても、取り出すキーは `"1"` のままである。** **★追随させると新しい状態が 1 つ生まれる**——**切り替え先のプリセットに `recipe_cache` エントリが無い場合。** **`recipe_cache` はプリセット別のエントリを持つ JSON であり、`RecomputePresetCache` は「そのプリセットが触られたとき」に走る**〔`SUPP-001` §7.1〕。**エイリアスを一度も触っていないプリセットへ切り替えると、キーが存在しない。** **⇒ 設計卓の指定＝「切替時に当該プリセット分を再計算する」。** **eager と整合し**〔**D-303**＝有効 `combos` 39 行・有効 `setups` 14 行の計 53 エンティティ・215 move step の実測で eager 確定〕、**費用は 1 プリセット分の再計算 1 回である。** **miss 時に計算して保存する形〔lazy〕は採らない**——**D-292 が退けた「stale をどう検出するか」という状態を、ここで作り直すことになる。** **★持つサブは M20-05** 〔preset サービス層の新設 ＋ `recipe_cache` 再計算の接続〕。**M20-04〔管理 UI〕が既定プリセットの切替導線を持つなら、切替経路から M20-05 のサービスを呼ぶ形になる。** **★あわせて範囲検査の穴も直す**——**C-3(d)＝`internal/config/config.go` の `Defaults.PresetID < 1` 検査と `internal/service/config/service.go` の同型検査は、`presets` に当該 id が実在するかを見ていない。** **削除行〔`official_ja_command`＝id 2 ／ `numeric_en`＝id 4〕を指す値を受理してしまう**——**M20-01 の削除と同じ手番で効くため、M20-01 の §3.3 で実査させ、実在検査の追加そのものは M20-05 が持つ。** |
| D-312 | 2026-08-12 | **【OD 技 4 件の `command` 欠落は CSV が起点である。seed は値を落としていない】**（**P-31** の 3 問目） **開発者見立て 2026-08-12**＝**「CSV 入力漏れと思われる。CSV だけが悪いのか seed まで問題があるのか要調査」**。 **⇒ 調査は `M20-RESEARCH-01` が既に済ませていた。** **web レポート A-3 §1 と §1-b の実測**——**(1) 4 件は `character_data/*.csv` の時点で `command` 空欄である**〔`guile.csv:54` `sonic_blade_od` ／ `jp.csv:33` `triglav_od` ／ `jp.csv:41` `departure_od` ／ `lily.csv:38` `condor_spire_od`。**4 件とも `is_derived=false` の `special` で `notes` も空**〕／**(2) CSV にあって DB に無い行は 0 件 / 走査 1,479 行。** **⇒ seed は CSV の行を落としていない。** **検算も閉じている**〔`DB 対応無し(is_derived=0) 175 ＝ CSV 空欄非派生 4 ＋ 移動系 system 171`〕。 **⇒ 「CSV だけが悪いのか」の答えは Yes である。** **seed は空欄を忠実に運んでいるだけで、この 4 件に関して seed 側に別の欠陥は無い。** **★ただし「直す経路」は別問題として残る**——**CSV を埋めても、既に seed 済みの DB は自動では変わらない。** 反映には **(i) `move_commands` への backfill マイグレ**、または **(ii) seedgen の再走 ＋ 新しい seed 波**のいずれかが要る。**どちらを採るかは M20-02 が決める**〔**同サブが「seed 波ごとに再適用できる規則」を作る**＝`M20-overview` §4.4。**同じ機構に乗せる**〕。 **★正しいコマンド値は開発者しか持たない**〔一次源はゲーム内表示〕。**⇒ M20-02 の着手前に開発者が 4 件のコマンドを CSV へ入れる**——**人手作業をサブの継ぎ目に置く**〔教訓 **E-55**〕。 **★母数への帰結が大きい**——**D-302 の母数 (e)＝4 件は全件がデータ不備であった。** **⇒ 「コマンドを持たない技に、コマンド由来プリセットで何を入れるか」という規則は、現行データでは要らない**〔教訓 **E-41**＝**「決める必要がない」という第三の結論**〕。**ただし規則そのものを消してはならない**——**新キャラ CSV の投入で再発しうる。** **⇒ M20-02 は `DES-004` §5.3 の 3 段フォールバックが既定として働くことを確認するに留め、専用規則を新設しない。** |
| D-311 | 2026-08-12 | **【`rush_variant` のコマンド由来表記は「`DR` ＋ 連結子 ＋ 元技のコマンド表記」の合成形を採る】**（**P-31** の 2 問目） **開発者判断 2026-08-12**＝**`DR > 2LP`**。 **⇒ web レポート E-1 §4 の案 2 を採る**〔元技のコマンド ＋ ラッシュ表記を合成する〕。**案 1〔`rush_variant` を対象外にする〕は採らない。** **★合成できることは実測で裏取り済み**——**`rush_variant` 272 件のうち 268 件が `original_move_id` 非 NULL**〔web レポート E-1〕。**残る 4 件は元技を引けない**〔ingrid 2・lily 1・mai 1。**4 件とも `move_code` が `_1hits` で終わる**＝**D-305**〕。 **⇒ D-305 が留保していた条件が解けた。** **同裁定は「M20-06 で扱うか followup へ送るかは、M20-02 の生成規則が `original_move_id` に依存するか否かで決まる」と書いていた。** **本裁定で依存することが確定したため、4 件の処遇は M20-02 が持つ。** **★`DR` は新語彙ではない**——**`DES-004` §3.4 のサンプル表が `DR`／`cDR` を `numeric` / `srk` 両方の表記として既に持っている。** **★連結子と同じ記号を 1 ステップの中に含むことになる**——**`DES-004` §4.2 は `numeric` / `srk` とも連結子を `>` と定めており、`DR > 2LP` はステップ間の連結子と見分けが付かない。** **表示はそれでよいが、逆引きはステップ境界で切ってから完全一致で引くため、切り方を決めないと `DR` と `2LP` に割れる。** **⇒ M20-02 が「1 ステップの中の `>`」と「ステップ間の `>`」の扱いを明示する。** **★`official_ja_move` 側は変えない**——**実測で 268 件が「元技エイリアス ＋ `(ラッシュ)`」の 1 形に揃っている**〔**D-305**〕。 |
| D-310 | 2026-08-12 | **【移動系 9 code はフォールバックさせない。記法が持っている表記を素で書き、記法が持たない 2 code だけ元の名前へ落とす】**（**P-31** の 1 問目・**設計卓の提案**。**文字列の確定は P-33**） **開発者の当初案**＝**「`numeric` / `srk` では移動系のエイリアスを書かず `official_ja_move` へフォールバックさせたい。ただし英語圏から見ると不自然なので、より良い案があれば」**〔2026-08-12〕。 **★当初案は動作としては成立する**——**`DES-004` §5.3 の 2 段目が `official_ja_move` での解決を定めており、`前ジャンプ` が出る。** **⇒ それでも設計卓はフォールバックさせない案を推す。理由 3 点。** **(1) テンキー記法と SRK 記法は、そもそも移動を書くための記法である。** **テンキー記法の記号体系は方向入力の番号そのものであり、`66`〔前ダッシュ〕・`9`〔前ジャンプ〕は記法が最初から持っている表記である。** **技名を借りるのは、記法が持っている表現を捨てて、持っていない表現で埋める形になる。** **(2) 逆引きの語彙が増える。** **`DES-004` §5 冒頭注記＝取込ヘルパーは全プリセット横断・`alias_text` 完全一致・キャラスコープで引く。** **移動系は `move_commands` にも載らない**〔**実測＝移動系 system 171 行は DB-only であり CSV に元々無い**＝web レポート A-3〕**ため、`66` や `9` と書かれた発信者のメモは現状 1 件も解決しない。** **エイリアスを書けばそのまま解決語彙になる。** **フォールバックでは増えない**〔`official_ja_move` の `前ジャンプ` は既に引ける〕。 **(3) コストが小さい。** **対象は 9 code × 19 キャラ ＝ 171 行**〔同実測〕**で、規則は 9 エントリの写像 1 枚である。機械生成できる。** **★設計卓の提案表**——**`forward`＝`6` / `f` ／ `back`＝`4` / `b` ／ `dash_forward`＝`66` / `dash` ／ `dash_back`＝`44` / `backdash` ／ `jump_neutral`＝`8` / `j.` ／ `jump_forward`＝`9` / `j.f` ／ `jump_back`＝`7` / `j.b`**〔各セルは `numeric` / `srk` の順〕。 **★`micro_forward`〔微歩き〕と `micro_back`〔微下がり〕の 2 code だけは書かず、フォールバックさせる。** **両記法にこの 2 つを表す表記が存在しないためである。** **素直に書くと `forward` と同じ `6` になり、2 対 4 code が同一表記へ潰れる。** **潰すと実害が 2 つ**——**(i) 利用者が入力した区別が表示から消える**〔**`DES-004` §2.1 は 1 入力 ＝ 1 move の原則でこの 2 つをわざわざ別 move にしている**〕／**(ii) 逆引きが「複数件 → 未解決」に倒れる。** **⇒ 記法に無いものを発明せず、元の名前のまま出す。** **英語圏の利用者が日本語を見るのはこの 2 語だけになり、しかも「記法にそもそも無いから元の名前で書いている」という説明可能な理由を持つ。** **記法を発明しないのは `DES-004` §2.4 の死守契約「公式表記のみ」と同じ側の判断である。** **★ジャンプ攻撃との二重表記は起きない**——**`DES-004` §2.2 (c) が「ジャンプ → 攻撃は空中技 ＋ `{forward_jump}` flag で表現し、純移動でないジャンプ move は省略する」と定めているため、`jump_forward` が出るのは純移動のときだけである。** **★P-30 との関係**——**移動系 171 件は D-302 が母数 (e) から除いた集合そのものである。** **「別扱い」の中身が本裁定で確定した**〔除外ではなく、明示的に書く〕。 |
| D-309 | 2026-08-12 | **【スマホへのコントローラ接続は M21 では対応しない。計測機種はレバーレスとパッドの 2 種】** **開発者判断 2026-08-12**。 **⇒ M21 の入力 UI は PC 経路を前提とする。** **★PoC の経路 (c) の位置づけが変わる**——**「スマホ ＋ 物理コントローラ」の実測は不要**になり、**残るのは「非 localhost〔`http://<LAN IP>`〕で Gamepad が取れるか」という確認だけ**である。**これはブロッカーではない**〔対応しないと決めた以上、取れなくても M21 は成立する〕**が、測っておく価値はある**——**LAN 越しに別の PC からアクセスする経路が塞がるかどうかが分かる**。**機材が無ければ「未測定」でよい。** **★A-5〔スマホへの接続方式〕は削除する**——**対応しないと決めたものを測らせない**〔調査項目を減らすのは、開発者の手番を減らすことと同義である〕。 **★機種が 2 種あることは分布の一般性に直接効く**〔**D-4**＝**分布の一般性は機種数で決まる**〕。**レバーレスとパッドは入力機構が根本的に違う**〔メカニカルスイッチ ／ メンブレンまたはマイクロスイッチ〕**ため、チャタリングの実波形が機種で分かれる蓋然性が高い。両方で測り、機種ごとに分けて報告させる。** |
| D-308 | 2026-08-12 | **【「エイリアス 0 件であること」を固定した既存テストの扱いを M20-02 の指示書へ明記する】** **web レポート C-5 の実測**——**`numeric_ja` / `numeric_en` にエイリアスが 0 件であることを固定しているテストが少なくとも 3 か所ある**〔`internal/infra/migration/migrate_test.go:928`「numeric_ja alias count = 0 (empty as designed)」／`internal/repository/preset/repository_test.go:157`・`:163`「expected 0 aliases for numeric_en」〕。**M20-02 がエイリアスを投入すると、これらは落ちる。** **⇒ これは `E-23` の型である**——**出力を固定する資産は、変更と同時に突合する対象であり、「既知の失敗」として流してはならない。** **★M20-02 の指示書へ、対象テストの所在と「更新するのか削除するのか」を書く。** **`numeric_en` は D-299 で出さないと決めたため、当該テストは削除対象になる見込みである**〔ただし**実装時に実査すること**〕。 **★あわせて `migrations/000006` と `000011` のコメントが「残り 4 プリセット」と書いている**〔同レポート〕——**実データではないため据え置いてよいが、D-288／D-299 で 3 種になった以上、内容は失効している。** |
| D-307 | 2026-08-12 | **【逆引きの完全一致は、不可視文字ではなく全角／半角の混在で壊れる。正規化を前段に置く】** **web レポート B-4 の実測**——**不可視文字〔前後空白・全角空白・改行・TAB・NBSP・ゼロ幅〕は 0 件 / 1,653 行**だったが、**NFKC 正規化形と一致しない行が 36 件**あり、**全角括弧 `（` を含む行が 33 件、うち 12 件は同一文字列に半角と全角の両方を含む**〔実例＝`ソーラーフレア(Lv1)（前方）`〕。 **★指示書 B-4 が意図した「完全一致の逆引きを壊す要因」は、不可視文字ではなくこちらとして実在していた。** **利用者が全て半角で入力しても一致しない。** **⇒ 逆引きの前段に正規化を置く**〔設計卓の判断。**データ側 36 行を書き換える案は採らない**——**`alias_text` は技の表示名そのものであり、表示を変えずに照合だけを緩めるほうが影響が小さい**。**`official_ja_move` は編集不可＝D-290 とも整合する**〕。 **★正規化の具体形は M20-07 の指示書で確定する**〔NFKC か、括弧だけを揃えるか。**過剰な正規化は別の技を同一視しうるため、範囲を実測で決める**〕。 **★これは `G-11`／`G-14b` の実効性に直結する**——**辞書を充実させても、照合が入力とすれ違えば決定論は働かない。** |
| D-306 | 2026-08-12 | **【`alias_text` の一意制約はキャラ軸を含める。投入は生成の後に置く】**（**M20-03** のスキーマ設計・**P-29 の後継論点**） **web レポート B-1／B-2 の実測**——**`(preset_id, alias_text)` は 60 グループ・783 行が重複**〔**すべて `official_ja_move` 内で、中身は「キャラ違いの同名技」**。`前ジャンプ` が 19 キャラ分など〕。**`(moves.character_id, alias_text)` の重複は 0 グループ / 走査 1,653 行。** **⇒ キャラ軸を含めない一意制約は現行データと両立しない**〔783 行のうち 723 行が入らない〕。**キャラ軸を含めれば現行 1,653 行は 1 行も壊れない。** **★ただし「0 件」の解釈に限界がある**〔レポート B-2 §2 が自ら明記〕——**エイリアスが入っているプリセットは `official_ja_move` の 1 種だけであり、「プリセット横断」の横断が実質 1 プリセット分しかない。** **⇒ 0 件は「衝突が起きない構造だから」ではなく「衝突が起きうる状況がまだ無いから」である。** **★したがって投入順序は変えない**——**M20-02〔生成〕の後に M20-03〔制約〕**。**制約を先に入れると、生成した瞬間に落ちる**〔`c_viper` / `dhalsim` が UNIQUE 衝突で起動不能になる経路と同型＝§1.3〕。**D-301 が確定した順序は、実測によって根拠が強まった。** **★スコープの最終形〔`(character_id, alias_text)` か `(preset_id, character_id, alias_text)` か〕は M20-02 の生成後に再計測してから確定する**——**逆引きが引く単位は横断であるため前者が本命だが、生成規則が横断で衝突を作るなら後者へ落とす。** |
| D-305 | 2026-08-12 | **【M20-06 の「通常投げ命名補正」は実データ上やることが無い。ラッシュ版命名も既に 1 形へ統一されている】** **web レポート E-2／E-3 の実測**——**通常投げは技データを持つ 17 キャラすべてで `前投げ` / `後ろ投げ` に揃っており、正規化前の表記〔`背負い投げ` 等〕は DB にも CSV にも 0 件**〔`DES-004` §3.2 の正規化が完全に反映済み〕。**`rush_variant` 272 件は全件エイリアス付きで、268 件が「元技エイリアス ＋ `(ラッシュ)`」の 1 形のみ。揺れ〔全角括弧・スペース・語順〕は 0 件。** **⇒ M20-06 のスコープは縮む。** **★「やることが無い」も結論である**〔**E-41** の型＝**「決める必要がない」という第三の結論があり得る**〕。**M20-06 は「命名規則の正典化」〔`(ラッシュ)` 接尾辞を `DES-004` §3.2 へ明記する〕に縮小し、データ補正は行わない。** **★残る実務は 1 件**——**`rush_variant` 272 件のうち 4 件が `original_move_id` NULL**〔ingrid 2・lily 1・mai 1。**4 件とも `move_code` が `_1hits` で終わる**〕。**`moves` DDL のコメント「ラッシュ版のみ参照」の想定から外れる。** **命名自体は 4 件とも `…(ラッシュ)` で一定であるため表示は壊れていないが、「元技エイリアス ＋ 接尾辞」を機械生成する実装は 4 件で元技を引けない。** **⇒ M20-06 で扱うか followup へ送るかは、M20-02 の生成規則が `original_move_id` に依存するか否かで決まる。** |
| D-304 | 2026-08-12 | **【M20-05 は「トリガの配線」ではない。preset サービス層そのものが存在しない】**（**スコープの見積りの是正**） **ローカルレポート D-5 の実測**——**`RecomputePresetCache` / `DeletePresetCache` は interface 宣言と実装定義があるだけで、本番の呼出箇所は 0 件。** **`internal/service/preset/` に存在するのは `doc.go` だけであり、`SUPP-001` §7.2 が呼出元として挙げる `create.go` / `update_aliases.go` / `delete.go` は 3 ファイルとも存在しない。** **⇒ M20-05 は既存サービスへトリガを足す作業ではなく、preset の作成・エイリアス更新・削除のサービス層を新設してそこから呼ぶ作業である。** **★これは `architecture-patterns` §9.2 の記述と整合する**〔「未実装なのはトリガを引く機能自体がフェーズ3 だから」〕——**同書は正しかったが、設計卓は「配線」という語でスコープを小さく見積もっていた。** **★M20-04〔管理 UI〕と M20-05〔トリガ〕の境界が動く**——**サービス層は UI から呼ばれるため、M20-04 が API とサービスを作り、M20-05 がキャッシュ再計算をそこへ接続する形になる。** **サブの分割は変えない**〔D-301 の承認どおり〕**が、overview §3 の内容欄と §5 の順序の根拠を実測へ揃える。** |
| D-303 | 2026-08-12 | **【D-292 の条件は充足した。`recipe_cache` は eager で確定する】** **ローカルレポート D-1〜D-3 の実測**——**開発 DB で有効 `combos` 39 行・有効 `setups` 14 行の計 53 エンティティ、対象は 215 move step。** **`RecomputePresetCache` は 1 プリセットにつき 53 回の compute と 53 回の UPDATE を逐次実行する。** **⇒ D-292 が置いた条件「実件数の実測で閾値を超えたら lazy を再検討する」は、超えなかった。eager を確定する。** **`SUPP-001` §7 の改訂は不要。** **★ただし実測が 2 つの別の事実を明らかにした。** **(1) 実装は `SUPP-001` §7.1 が書く「delete → insert」ではなく、インライン JSON の当該キーを行単位で UPDATE する**〔**§7.1 の記述を as-built へ是正する。自由改訂**〕。**(2) 全体を囲む transaction が無く、途中でエラーが返ると先行行の UPDATE が残る**〔**53 行の部分更新が起きうる**。**本件は「eager か lazy か」とは別の欠陥であり、M20-05 の指示書で扱う**〕。 **★「1000 件規模の同期バッチ」という `architecture-patterns` §9.2 の見積りは、現時点の実データでは 53 行だった**——**見積りを実測で置き換えた記録として残す。** |
| D-302 | 2026-08-12 | **【P-30 の問いが変わった。既存表示は壊れていない。決めるのは「生成時に何を出すか」だけである】**（**P-30** の再定義） **web レポート A-5 の実測**——**`moves` 全 1,653 行に `official_ja_move` のエイリアスが付いており、`DES-004` §5.3 のフォールバック 3 段目〔`moves.code` 素出し〕に落ちる行は 0 件。** **ローカルレポート F-1 も、保存済み `recipe_cache` 265 表示値のうち raw `moves.code` を含むものは 0 件と実測した。** **⇒ 「テンキー表記の中に内部識別子が混ざる」という当初の懸念は、現行データでは発生していない。** **★P-30 が実際に決めるのは「コマンド由来プリセットを生成するとき、コマンドを持たない技に何を入れるか」だけである。** **★母数は定義で 5 通りに割れる**〔レポート要決定事項 1〕——**(a) DB で `move_commands` 対応無し 759 ／ (b) うち `is_derived=0` 175 ／ (c) 索引非搭載 585 ／ (d) CSV の `command` 空欄 304 ／ (e) `is_derived=0` かつ移動系 `system` を除く 4**。 **★設計卓の推奨は (e) の 4 件**〔guile `sonic_blade_od` ／ jp `triglav_od` ／ jp `departure_od` ／ lily `condor_spire_od`。**4 件とも OD 技**〕。**理由 2 つ**——**(1) `is_derived=1` の 584 件は `internal/moveindex` が設計どおり索引対象外にしている行であり、「コマンドが無い」のではなく「単独で入力できない」**〔**除外の理由が違うものを同じ母数に入れない**＝**D-260** の型〕／**(2) 移動系 `system` 171 件は `DES-004` §2.1・§2.3 が system move として別扱いする前提を既に持つ。** **★開発者判断待ち**〔**P-31**〕。 |
| D-301 | 2026-08-12 | **【M20 のサブ分割 7 本と実装順を承認する】**（playbook §4.12 の承認ゲート） **開発者承認 2026-08-12**。**`M20-overview` §3 の M20-01 〜 M20-07 と、§5 の実装順を確定した**〔**01** 初期プリセットの 3 種化 ／ **02** 生成規則とエイリアス実データ ／ **03** `preset_aliases` の一意制約〔**スキーマ承認ゲート・別途承認が要る**〕 ／ **04** プリセット管理 UI とカスタムプリセット作成 ／ **05** `recipe_cache` トリガの配線と F12-2 ／ **06** B-1 命名クラスタ ／ **07** 逆引き活用と多候補ハッジの分割〕。 **★順序に構造的な制約が 2 つある**——**(1) M20-03 は M20-02 の後でしか入れられない**〔**UNIQUE を先に入れると後続 seed 波の INSERT が衝突して起動不能になる**。先例＝`c_viper` / `dhalsim` が最終波指定されている＝§1.3。**「実測 → 生成規則の確定 → 制約」の順**〕／**(2) M20-05 は M20-04 の直後**〔**トリガを引く面が無いと配線を検証できない**。`architecture-patterns` §9.2 が「未実装なのはトリガを引く機能自体がフェーズ3 だから」と記す状態そのもの〕。 **★人手作業が挟まる箇所をサブの継ぎ目にしてある**〔教訓 **E-55**〕——**P-30 の開発者判断は M20-02 の直前**、**スキーマ承認は M20-03 の直前。** **★統合の余地を 1 件残す**——**M20-02 と M20-06 はどちらも命名・表記の生成規則を触るため、M20-RESEARCH-01 の軸 A・E の結果次第で統合したほうが安いと判明しうる。** **その場合は overview を改訂して再度承認を求める**〔**独断で統合しない**＝playbook §4.12〕。 **★本承認をもって `M20-overview` は v1.0.0 とする**〔テンプレの「0.1.0（ドラフト）→ 承認後 1.0.0」に従う〕。 |
| D-300 | 2026-08-12 | **【`presets.code` を `numeric` へリネームする。カスタムプリセットの上限は 5 のまま据え置く】**（**D-299** の細部の決着） **開発者判断 2026-08-12**。 **(1) code のリネーム**——**`numeric_ja` を `numeric` へ改める**〔name も「ナンバリング記法」へ〕。**★今が最も安い理由**——**`presets.code` は M20 のプリセット管理 UI で初めて画面とフロント定数へ露出する**。**参照元がほぼ無い現時点でリネームしないと、露出後は書き換え範囲が広がる**〔**E-110** の「n 例目で切り出す」と同じ形＝**増えてから判断すると誰も着手しない**〕。**`official_ja_command` の削除〔D-288〕と同じマイグレで処理する。** **★着手前の実査 3 件**——**(a) `presets.base_preset_code` に `numeric_ja` を指す行が無いか**〔カスタムプリセットは未実装のため 0 件の見込みだが **「無い」は実査してから言う**＝**E-117**〕／**(b) `config` の `[defaults] preset_id` の指し先**〔**D-288** と同じ実査。**id 参照であって code 参照ではないため直接は壊れないが、削除行を指していれば壊れる**〕／**(c) フロント定数 `BUILTIN_PRESET_CODES` の現況**〔**`m1-05-review.md` が「定数は `numpad_ja`／`numpad_en`、DB seed は `numeric_ja`／`numeric_en`。コード値不一致。現時点は未参照のため実害なし」と記録している**。**設計卓は実装ソースを見られないため、現在も不一致かは断定しない**——**M20-RESEARCH-01 で実査する**〕。 **(2) 上限**——**`SUPP-001` §7.3 のカスタム側は 5 のまま据え置く。** **組み込みが 5 → 3 になるため、上限の内訳は「組み込み 3 ＋ カスタム 5 ＝ 8」となる**〔**§7.3 は自由改訂資料であるため、D-290 の是正〔組み込みは編集不可〕と同じ手番で直す**〕。 |
| D-299 | 2026-08-12 | **【`numeric_ja` と `numeric_en` を `numeric` 1 枚へ統合する。`numeric_en` は出さない。初期プリセットは 3 種になる】**（**P-29** の決着・**D-288** の更新） **開発者判断 2026-08-12**〔「同じようなものを出す予定だったなら numeric に統合して `numeric_en` は出さない」〕。**⇒ 条件は満たされている。** **★設計卓が全記録を実査した結果、実体としての差分は 1 つも記録されていなかった**——**`DES-004` §3.4 のサンプル表は 7 行すべて同一**〔`5LP`／`2LK`／`236LP`／`623HP`／`236236HP`／`DR`／`cDR`〕／**§4.2 の連結子はどちらも `>`**／**`000005_seed_presets` の差分は name 文字列のみ**／**エイリアスは両方とも空**〔`SUPP-001` §3.4〕。**唯一の定義文である `DES-004` §3.1 の差分「日本語基準〔L/M/H〕で統一」対「技の英語準拠」も成立していない**——**L/M/H はそれ自体が英字であり、区別の軸になっていない。** **⇒ サンプル表を書いた時点で差分を書けなかったと読める。** **★帰結**——**初期プリセットは `official_ja_move` ／ `numeric` ／ `srk` の 3 種**〔**D-288 の「4 種」を更新する。D-288 の本文は書き換えない**＝**D-57**／**D-259**〕。**followup `A-1`「残り 4 プリセットのエイリアス実データ」は「残り 2 プリセット」へ縮む**〔`numeric` ／ `srk`〕。 **★着手前に決めること 2 件**——**(1) `presets.code` を `numeric` へリネームするか、`numeric_ja` 据え置きで name だけ変えるか**〔設計卓の推奨＝**リネーム**。**M20 で code が初めて画面とフロント定数へ露出するため、参照元がほぼ無い今が最も安い**。`official_ja_command` の削除と同じマイグレで処理できる〕／**(2) `SUPP-001` §7.3 のプリセット数上限「組み込み 5 ＋ カスタム 5 ＝ 10」の内訳**〔組み込みが 3 になるため要調整。**カスタム側を 5 のままとするか**〕。 **★歴史の記録**——**M0 プロトタイプには `official_en_move`〔公式英語〕が実在した**〔`docs/progress/phase1/m0-01-review.md`〕**が、M1 の seed では仕様側の 5 種が採られて落ちている。D-289 は同じ判断の 2 回目である。** |
| D-298 | 2026-08-12 | **【コマンドのアイコン併記は、プリセットとは別機能として切り出し M23 へ送る】** **開発者着想 2026-08-12**〔競合アプリの実物評価に基づく〕。**コマンドをアイコンで表記すること自体は良かったが、プリセットの一種としては作らない。技名の横へ併記する別機能とし、全画面ではなく一部へ出す**〔どこへ出すかは今後決める＝スコープ未確定〕。 **★設計書に該当記述が無い**——設計卓が実査で確認した。**⇒ `followup-backlog` §K へ `command-icon-annotation` として起票した**〔割付 M23〕。 **★既存記述との接続 2 点**——**(1) `DES-004` §3.2 が「公式表記はパンチ・キック・方向キーを専用アイコンで表現しているが借用できないため、自前アイコンを用意するか言葉に置き換える」と定めており、アイコン資産の自前調達が本機能の前提になる**／**(2) followup §G-3〔仮想コントローラのアイコン化〕が同じアイコン資産を要求する**。**⇒ 束ねるのが経済的**〔二度作らない〕。**ただし G-3 には「方向の矢印記号は M15-03 で意図的に外したものであり、アイコンで戻すのは過去判断の反転」という論点が付いており、本項にはその論点が無い**〔新規追加のため〕。 **★データ源の制約**——**コマンドは本体 DB に列として存在せず `move_commands` の index-only である**〔`DES-002` §7.5・`code-facts` §10-2〕。**コマンドが無い技では併記できない**ため、**P-30 の洗い出しが本項の前提資料を兼ねる**。 |
| D-297 | 2026-08-12 | **【M19 期の教訓のうち、再発防止が要る 3 件の昇格先を決める。残りは記録に留める】** **`retrospective` に書くだけでは再発は止まらない**〔`parallel-ops` §09＝**retrospective は履歴／統制は起動プロンプトとコマンド仕様。役割を混ぜない**〕。**⇒ 昇格 3 件**——**(1) D-280〔現物を持たない側は「ある／無い」だけでなく内容の描写も断定しない〕→ playbook §4.22 (2) の本文へ「描写も断定に含む」を追記**〔**設計卓が Code へ移り docs が見えるようになった分、「docs が見えるから実装も見えているつもり」で悪化しうる**。§12.0 の注記と対で効かせる〕／**(2) D-277〔「次の番号」は警告文でなく常設項目にする〕→ `docs/instructions/templates/` の完了報告系テンプレと製造 CLI 仕様へ**〔**playbook に書くだけでは止まらない**＝§4.21 (5)。**registry 自身が警告文を持ちながら、その警告どおり失効した**〕／**(3) D-284〔並列サブは互いの `E-14` 交差相手である〕→ overview テンプレ §5 と review-checklist テンプレ §8 へ**〔**M20 ∥ M21 でまさに効く**〕。 **★残りは `retrospective-log` への記録に留める**——**昇格しないと決めたことを明示する**〔記録に留める判断も判断であり、書かないと次の担当が同じ棚卸しをやり直す〕。 |
| D-296 | 2026-08-12 | **【`m19-desk-status` にしか無い 2 件の分配先を決めた。削除の前提が満たされた】**（**P-24** の前提の充足） **同書 §0 が「分配されていない情報が残っていたら分配漏れである。削除の前に確認すること」と自ら定めていた分**。**設計卓の実査でも、第 4 便 §6-4 の検査結果どおり本書固有の情報は 2 件だった**。 **⇒ (1) §4-13「`progress-log.md` の未記録 10 件」**〔M17 で 4・M18 で 1・M19 で 5〕**は `followup-backlog` §J へ停止時記録の必須 5 フィールド付きで登録する**——**`progress-log` は製造が書く層であり**〔**D-191** の 4 層構造 (1)〕**設計卓も反映係も書き手ではないため、「誰が再開できるか」を持つ §J が唯一の受け皿である**。**再開条件＝開発者の判断**〔遡及記入するか、記入せず「当該期は完了報告が正本」と否定形で確定するか〕。 **⇒ (2) §8.1「引き継ぎ資料に書いてはいけないこと」は playbook へ昇格する**〔**D-204**／**D-229**／**D-242** の言及が playbook 側に 0 件であることを実査で確認済み〕。**昇格先の選定理由＝統制は実際に開かれる文書まで落ちて初めて効く**〔§4.21 (5)〕。**playbook は設計担当が成果物を作るたびに開く。** **★削除そのものは開発者の手番**〔**D-196** 境界条件 3＝「消す」変更〕。 |
| D-295 | 2026-08-12 | **【`chain-adjacency-across-units` の型 B は M20 のサブにしない。update 波と同じ束へ送る】**（**P-23** の決着・**D-267** (1) の送り先） **型 B〔同一照合値の重複を費用最小で集約する分〕は M20 のテーマ〔プリセット・命名〕と触る層が違う**——**`setplay` の提案集約であり、M20 に混ぜるとレーンの「触る領域」宣言が濁る**〔契約 §3〕。 **⇒ M20 のサブにせず、第 3 のレーンも立てず、`update 波` の束へ送る。** **理由**＝**型 B は判定法を新設せず既存の照合値の同一性だけで解けるため急がない**〔**D-267** (1)＝「隣接をどう定義するか」を決めずに済む形が既に見つかっている〕。 **★起票そのものは承認された**——**M20-overview §2.x へ「後続送り・根拠付き」として記録する**〔playbook §4.11〕。**M19 のクローズ条件でなかったことは変わらない。** |
| D-294 | 2026-08-12 | **【M21 は PoC を先行させる。PoC の成果物は「閾値」ではなく「分布」である】** **Gamepad の検証が重い**〔`parallel-ops` §12 のレーン割り条件 (b)〕。**⇒ PoC 指示書を M20-overview と同時に出し、M21 レーンの 1 本目として先行投入する。本実装の指示書は PoC の結果が出てから書く。** **★スコープは「判定仕様を決めるための実測」に閉じ、本実装を含めない**——**(i) 実行環境で Gamepad API がそもそも取れるか**〔取れないなら実機確認が全面的に開発者手番になり、M21 の進め方自体が変わる〕／**(ii) ポーリング周期と入力遅延の実測**／**(iii) 同時押しのズレ幅**〔FR107〕**とチャタリングの実波形**〔FR108〕**の採取**。 **★「閾値」を PoC の成果物にしない理由**——**測る前に判定法を決めてはならない**〔**D-267**＝「表記上の隣接で除去」案を退けたのと同型／**M19 教訓 9**＝実測が出るまで確定させない。**M19 の見込み値は繰り返し失効した**〕。**同時押し判定とチャタリング対策は、閾値を 1 つ間違えると利用者の入力が黙って落ちる種類の仕様であり、机上で選んではならない。** |
| D-293 | 2026-08-12 | **【CHANGE 番号はブロック事前割当へ移行する。M20 ＝ 098〜105 ／ M21 ＝ 106〜111】**（`parallel-ops` §02 の履行） **M18/M19 期は registry 逐次採番だったが、M20 からブロック事前割当**〔同 §02＝**資源の性質で二分する**〕。**⇒ M20 に 8 本・M21 に 6 本を割り当てる。** **根拠**——M20 の CHANGE 見込みは (a) プリセット管理 UI とカスタムプリセット〔`DES-005` 新画面 ＋ `DES-004` §6〕／(b) 初期プリセットの 4 種化〔`DES-004` §3.1・§3.4・§4.2＝**D-288**〕／(c) `alias_text` の一意制約〔`DES-003`・**承認ゲート**〕／(d) `recipe_cache` トリガの API 面〔`DES-002`〕／(e) B-1 命名クラスタ〔`DES-004`〕で **4〜6 本**。**M21 は `FR105`〜`FR108` が `REQ-001` §3.1 に既存でフェーズ3 割付のため REQ 起票が不要**〔実査 2026-08-12〕**で、`DES-005` 入力 UI と同時押し・チャタリング仕様で 2〜3 本**。 **★枯れたら追加ブロックを出す**〔同 §02。**欠番は無害＝順序依存がない**〕。 **★マイグレは両レーンとも「プレースホルダ・マージ時採番」と契約へ明記する**——**設計卓は `migrations/` を見られない**〔武装〕**ため、disk 末尾の確認は製造の Plan Mode に委ね、不一致なら製造は自採番せず再請求する。** |
| D-292 | 2026-08-12 | **【`recipe_cache` の無効化は eager を採る。`SUPP-001` §7 を改訂しない。ただし条件付きである】** **開発者判断 2026-08-12**。**`SUPP-001` §7 の既定**〔`RecomputePresetCache`＝**プリセット新規・エイリアス変更時に当該プリセットの全エントリを delete → insert**〕**のまま実装する**。**lazy を採ると §7 の設計変更にあたり、§7 改訂とパフォーマンス考慮が要る**〔`architecture-patterns` §9.2〕。 **★eager を採る理由 3 点**——**(i) `SUPP-001` は CHANGE 対象外の自由改訂資料であり、「改訂できるから改訂する」誘因に流されないため**／**(ii) プリセット数に上限がある**〔`SUPP-001` §7.3。**ただし上限の内訳は D-288 で種類数が変わったため要調整**〕／**(iii) lazy は「stale をどう検出するか」という新しい状態を作り、M20 が同時に抱える `alias_text` の一意制約と検証面が交差する。** **★ただし前提が 1 つ増えた**——**自動セットプレイ提案と確定反撃提案の追加により、プリセットが効く面は増えている**〔開発者観察 2026-08-12〕。**`RecomputePresetCache` は v1.13.0 でセットプレイの `recipe_cache` も対象に含めている**〔`SUPP-001` §7.1〕。**⇒ 「1000 件規模の同期バッチ」は当時の見積りであって現在の実測ではない。着手前に実件数を実測し、体感が閾値を超えるなら lazy を再検討する**という条件付きで確定する。**実測は M20-RESEARCH-01 が持つ。** |
| D-291 | 2026-08-12 | **【カスタムプリセットは横断 1 枚とする。「キャラ単位」は UI とコピー時の実体化で満たす】** **開発者要件 2026-08-12**＝**ユーザーから見てキャラ単位で作れること／内部は問わない／ベースの選択と自キャラ分の編集が簡単ならよい**。 **⇒ `presets` にキャラ軸の列を足さない。** **理由 2 つ**——**(1) alias 行は `move_id → moves.character_id` で必ず 1 キャラに属するため、データ側は既にキャラ単位である**〔`code-facts` §10-2。**`preset_aliases` は `character_id` を持たないが `moves` が持つ**〕。**編集面をキャラで絞れば要件を満たす**／**(2) プリセットをキャラ別行にすると「いまどのプリセットで表示するか」の解決が `preset_id` 単一値から (キャラ, ユーザー) → preset へ変わり、`RecipeResponse.presetId` ／ queryKey `["combo", comboId, "recipe", presetId]` ／ config `[defaults] preset_id` まで波及する**〔いずれも `code-facts` §2-2・§5・§7 で実在確認〕**＝M20 のスコープが倍になる。** **★「ベースはキャラごとに違ってよい」はスキーマ追加ゼロで成立する**——**コピー時にエイリアスを実体化すれば、「あるキャラは SRK から・別のキャラはテンキーから」が 1 枚の中で成立する**〔`DES-004` §6.1 が「コピー操作で作成」、§6.3 が `base_preset_code` を**追跡用**と定めている〕。**`base_preset_code` はフォールバック用に 1 つ持つだけでよい。** **★副作用を記録する**——**実体化はベース側が空の技に対しては成立しない。コマンドが無い技の表記**〔**P-30**〕**と同じ穴に落ちる。** |
| D-290 | 2026-08-12 | **【`official_ja_move` は編集不可を維持する。`SUPP-001` §7.3 の記述が誤りである】** **開発者判断 2026-08-12**。**`DES-004` §3.1「組み込み 5 プリセットはいずれも編集不可とし、カスタマイズしたい場合はコピーを作成する」と `SUPP-001` §7.3「組み込みプリセットのエイリアス編集は可能」が正面衝突していた**〔設計卓が実査で検出。**`CLAUDE.md` §8 により設計卓は解釈を選ばず開発者へ回した**〕。**⇒ `DES-004` §3.1 が正。** **★理由が構造にある**——**`moves` に技の表示名カラムは無く、表示名は `preset_aliases` がプリセット別に保持する**〔`DES-002` §7.5 の注記・`code-facts` §8 で `model.Move` に name 列が無いことを確認〕。**したがって `official_ja_move` を編集可にすると、技名そのものが利用者ごとに変わる。** **★`SUPP-001` は CHANGE 対象外の自由改訂資料であるため、是正は M20 の作業として設計卓が行う。** **★これで B-1 命名クラスタの「`official_ja_move` 編集可否」は決着した**〔`followup-backlog` §B〕。 |
| D-289 | 2026-08-12 | **【英語公式技名は持たない。`DES-002` §7.5 の否定形の記録を維持する】** **開発者判断 2026-08-12**。**`DES-002` §7.5 は「`name_en` 列は CSV に含めない」と明記し、理由を「英語公式技名の格納先プリセットが無く、英語ロケールはフェーズ3以降のため」としていた。** **⇒ 反転しない。** **理由＝英語版では技名でコンボを書いている例があまり無い**〔開発者の実地観察〕。 **★帰結 2 点**——**(1) 「公式英語」プリセットは作らない**／**(2) 反転しない判断であるため、完成済みの手入力 CSV へ英語技名を後入れする人手作業は発生しない**〔**発生していたら 31 キャラ分の再収集になっていた**〕。 **★ただし未決が 1 件残った**——**`numeric_en` の差分の根拠は `DES-004` §3.1 では「技の英語準拠」であり、本裁定でそのデータを持たないと決めた時点で拠り所を失う**〔**P-29**〕。**設計卓が実査したところ `DES-004` §3.4 のサンプル表は 7 行すべてで `numeric_ja` と `numeric_en` が同一である。** |
| D-288 | 2026-08-12 | **【初期プリセットを 4 種へ再整理する。`official_ja_command` は出さない】** **開発者判断 2026-08-12**。**正典は `official_ja_move` ／ `official_ja_command` ／ `numeric_ja` ／ `numeric_en` ／ `srk` の 5 種だった**〔`DES-004` §3.1・§3.4・§4.2 ／ `SUPP-001` §3.4〕**が、`official_ja_command`〔公式のコマンド表記〕は需要を感じないため出さない**。**⇒ 初期プリセットは `official_ja_move` ／ `numeric_ja` ／ `numeric_en` ／ `srk` の 4 種**〔**`numeric_en` の去就は P-29**〕。 **★`presets` の行は `000005_seed_presets` で 5 種とも投入済みであるため、廃止は「行を消す」変更である**〔`code-facts` §10-2〕。**現時点で実ユーザーは不在であり**〔`retrospective-log` §1 パターン G の発動条件・playbook §4.17 の緩和〕**忠実復元 down を前倒しで払わずに済むため、消すなら今が最も安い。** **★エイリアスは空で出荷されている**〔`SUPP-001` §3.4＝**実データ投入済みは `official_ja_move` のみ**〕**ため、廃止による表示の変化は無い**〔選択していてもフォールバックで `official_ja_move` が出ていた〕。 **★着手前に実査すること**——**`config` の `[defaults] preset_id` が当該行を指す経路の有無**〔`code-facts` §5 に `DefaultsConfig.PresetID` が実在する〕。**指しているなら付け替えを同じマイグレに含める。** |
| D-287 | 2026-08-10 | **【`**` の検査は偶数性だけでは足りない。flanking 判定が要る】**（**D-258**／**D-266** の追補） **反映係の実測により、`**` の個数が偶数でありながら描画が崩れる型が確認された。** **CommonMark では閉じ側の `**` が right-flanking の条件を満たさないと閉じられず、日本語の全角約物が直後に来る形でこれが起きる。** **⇒ 偶数検査を捨てず、flanking 検査を足す**——**偶数検査は「対応が取れていない」を、flanking 検査は「対応は取れているが閉じられない」を見ており、別の欠陥である。** **★書き手側の作法**——**強調の閉じ `**` の直後に全角の約物を置かない。約物は強調の内側へ入れる。** **★設計卓が M19 期にこの形を大量に書いた。規律を出す側が最大の生産者だった。** **既存分は board 内に複数行あり本レーンの前後で増減していない＝既存欠陥。1 行単位では直せない**〔閉じ側だけ足すとセル後半の孤立した `**` が字面で出る〕**ため、系統的な是正として followup `emphasis-flanking-violations` へ送る。** **【2026-08-10 errata・反映係】★本行の「書き手側の作法」は実測と食い違う。** **描画が壊れるのは「閉じ `**` の直後に全角約物がある」形ではない。** **壊れるのは「閉じ `**` の直前が全角約物で、直後が非約物・非空白」の形である**〔CommonMark の right-flanking 条件〕。**実例**＝`**SIGPIPE（終了コード 141）**で落ち、` ／ `**強調。**次の平文`。**いずれも閉じられず、後続へ太字が漏れる。** **逆に `**強調**）` `**強調**〕` `**強調**。` は閉じの直前が非約物のため正しく閉じる**〔第 5 便 §7-4 の確認結果。**本裁定の `new` 自身と `progress-summary` §21 の `new` に計 21 か所あるが、描画の破綻は 0 だった**〕。**⇒ 作法は「約物を強調の内側の末尾に置かない」と読み替えること。** **裁定の趣旨〔偶数検査に flanking 検査を足す〕は変わらない。** **裁定本文は書き換えていない**〔**D-259**／**D-57**〕。 |
| D-286 | 2026-08-10 | **【派生成果物が生成不能なとき、その生成器の修復は事実追従の前提の修復として委任に含まれる】**（**D-196**／**D-285** の境界の明確化） **反映係が `scripts/generate-docs-map.sh` を改変した**〔`set -o pipefail` と `grep -q` の組み合わせによる SIGPIPE 誤判定の是正〕。**生成器のソース改変は D-285 の 4 類型に無く、D-196 が委任するのは派生成果物の「再生成」であって「生成器の改変」ではない。** **反映係は当てたうえで報告した。** **⇒ 追認する。戻すと `docs-map` が生成不能のまま固定される。** **★ただし D-285 の類型には足さない**——**「生成器が壊れているから直した」は毎回通ってしまう形であり、ロジックの改変は挙動を変えうる。** **今回は挙動が変わらないことを反映係が確認しているが、それは確認の結果であって類型として一般化できる性質ではない。** **★一般化するのは「生成器の改変」ではなく「事実追従の前提の修復」である**——**D-196 が委任しているのは事実追従であり、その前提そのものが壊れている状態の修復は委任に含まれると読んでよい。** **条件 3 つ**＝**(1) 派生成果物が生成不能である／(2) 挙動が変わらないことを確認する／(3) 確認の方法を報告に書く。** |
| D-285 | 2026-08-10 | **【反映レーンの委任範囲を 4 類型へ広げる】**（**D-196** の拡張） **反映係からの申し出**〔第 3 便適用報告 §5〕——**軽微・一意に定まる・新しい判断を含まない事項でも毎回パッチの往復が要り、設計卓の手番が積み上がっていた。** **⇒ 次の 4 類型を「当てて事後報告」へ委任する**——**(a) 参照先の誤りの是正**／**(b) 既に裁定・番号で参照されている followup の起票**／**(c) Markdown の構造破壊の修復**／**(d) メタ表の `文書ID` 行の追加**。 **★共通の条件 3 つ**——**(A) 事後報告に「何を根拠に一意と判断したか」を書く**〔**「一意に定まる」は判断であり、判断は誤りうる。設計卓自身が 2 日で 9 件の誤りを出している**〕／**(B) 一意に定まらないと判断したものは当てずに上げる**〔**委任は「止まらなくてよい」ではない**〕／**(C)「消す」変更を伴わない**〔**D-196** 境界条件 3〕。 **★裁定本文と、設計卓が発行した `new` の逐語は引き続き対象外**〔**D-57**〕。 **★(a) には追加条件がある**——**参照先が実在することだけで一意と判断しないこと。** **`DES-005` の `(FR307)` は参照先が実在したうえで内容が違っており、しかも正しい指し先は当時存在せず新設が要った。** **(i) 参照先が実在し (ii) 内容が文脈と一致する場合にのみ当てる。** **★(b) は登録内容を参照元の裁定の逐語から起こす。反映係が内容を創作しない。** **★委任の根拠**——**反映係は 3 便を通じて、当てるべきでないものを 3 件止めて差し戻し、いずれも設計卓の誤りだった。** **止める判断の質が示されている。** |
| D-284 | 2026-08-10 | **【`E-14` の交差相手に「同一 MS 内の並列サブ」を含める】**（**E-14** の適用範囲の拡張） **`M19-overview` §3 の交差表は `E-14` 突合ゲートの交差相手を M18-03 だけと定め、同一 MS 内の並列サブを数えていなかった。** **M19-05／06／07 は 2026-08-08 に 3 本まとめて発行され同時に投入されたため、3 本の指示書とチェックリストはいずれも「他の 2 本が完了した後の as-built」を知らない。** **これは `E-14`〔先に書いた指示書は書いた時点の as-built で凍る〕が述べている状況そのものである。** **★実際に起きていた**——**M19-07 の追補〔2026-08-10〕が採用パネルを 2×2 グリッドへ差し替えた。この面は M19-01／02 の領域であり、M19-05 が同じ機能領域を触った後、既に完了・マージ済みだった。** **M19-05 の指示書とチェックリストはこの差し替えを知らない。** **★実害はゼロだった**〔既存テスト 26 件が無改変で green。payload はセルの順序まで従来と同一〕——**しかしそれは結果であって、検出の仕組みが働いた結果ではない。** **⇒ 並列サブを編成するときは、各サブの完了時に「他の並列サブの as-built が、自分の指示書・チェックリストの前提を崩していないか」を照合する項目を置く。** **★M19 で実害が出なかったのは 3 本が触る面がほとんど交差しなかったためである**——**`M19-06` の R-5 と `M19-07` の R-7 は互いの領域ではなく `punishfinder`／`setplay` を守っており、06 と 07 のあいだには相互の検出項目が無かった。** **★交差の有無を「触るファイルが違うから大丈夫」で済ませない。** 面が交差しないことは編成時の見込みであり、追補や開発者要望で後から交差しうる。 |
| D-283 | 2026-08-10 | **【「現物 → 台帳」方向の実査は、除外の判断を記録しないと毎回同じ結果を出す】**（**D-257** の追補） **`M19-overview` §7 の (b) 方向の実査が初めて実行され、未収録が 10 件検出された。** **うち本書自身は循環であり意図的な除外である**〔マニフェストが自分を追うと、版が上がるたびに自分の行を直すことになる〕。 **⇒ 収録しないと決めた分を、否定形として §7 の収録基準へ記録する。** **記録しないと、次の (b) 実査で同じものが再検出され、そのたびに同じ判断をやり直すことになる。** **★これは `M19-overview` §8「確定済みで再検討しない事項」と同じ形である**——**否定形の記録は、検査を安くするための投資であって、単なる記録ではない。** **★(a) 方向〔載っている行 → 現物〕は drift を検出し、(b) 方向〔現物 → 載っている行〕は漏れを検出する。前者は毎回ゼロに収束するが、後者は除外を書かないかぎり収束しない。** |
| D-282 | 2026-08-10 | **【転記待ちのプレースホルダは、通常の約物と区別できる形にする】** **設計卓は転記待ちの値を隅付き括弧で書き、同時に通常の丸括弧としても同じ記号を使っていた。** **その結果「適用後確認＝当該記号が 0 件」が字義どおりには成立せず、反映係が「87 行に残るがすべて通常の用法である」と判定して通した。** **★判定が正しかったのは反映係が中身を読んだからであり、機械検査は機能していなかった。** **⇒ 転記待ちは機械で一意に拾える形にする**〔例＝`TODO:` を内側に置く〕。 **★一般則**——**検査の対象を、本文で使う記号と同じにしない。** **「0 件であること」を確認項目に置くなら、その文字列が本文に現れない保証をセットで作る。**〔**playbook §4.22 (6) の一般化**〕 |
| D-281 | 2026-08-10 | **【版を上げたら、パッチ自身の中の参照も全数 grep する】**（**D-247** の自己適用） **設計卓が `board-rulings-D260-D266-patch` を v1.2.0 で 6 行 → 7 行へ広げたとき、H1 タイトル〔「D-260〜D-265」〕・§2 の「6 行を挿入」・末尾の締め〔v1.0.0〕の 3 か所を直し損ねた。** **メタ表だけが v1.2.0 だった。** **反映係が「v1.2.0 の同期漏れ」と判断して 7 行として当て、申し送った。** **⇒ D-247 の対象は適用先の文書だけではない。パッチ自身も対象である。** **★パッチの版を上げたら、H1 タイトル・項目数・末尾の締め・メタ表を突き合わせること。** **★危険の質**——**メタ表と本文が食い違うパッチは、当てる側に「どちらが正か」の判断を強いる。** **当てる側は現物を持つが、発行者の意図は持たない。** |
| D-280 | 2026-08-10 | **【現物を持たない側は、「ある／無い」だけでなく「内容の描写」も断定しない】**（**D-243** の射程の拡張） **2026-08-10 の適用報告が、設計卓の同型の誤りを 3 件検出した。** **(1) 「gap 全種別を M19-05 後の版で測った値が無い」**——**実際には完了報告に存在し、meaty と同一版・同一ハーネス・同一対象で測られていた。設計卓は当該完了報告を保持していない。** **開発者に不要な再測定を依頼しかけた。** **(2) 「`DES-002` §4.2 の当該セルには既存の 13 のフィルタ・ソート・ページングの仕様が同居している」**——**実際は概要表の短いセルだった**〔操作は一意に定まったが根拠が違う〕。 **(3) 「`M19-overview` §6 は 12 項目」**——**実査値は 11**〔**D-249** の自己違反〕。 **★3 件は同じ形である**——**「無い」と書かなくても、内容を具体的に描写すれば同じ断定になる。** **★描写のほうが危険である**——**存在の断定は「無い」と書くので目に付くが、描写は根拠として書かれるため、操作が正しく当たっているかぎり誰も検算しない。そして後任は根拠を信じて、別の場所へ同じ判断を適用する。** **⇒ 現物を保持していない箇所の描写は「実査して確認すること」の形で書く。** **★とくに「〜が無い」を根拠にブロッカーを立てるときは、その不在を確認した手段を併記する。** |
| D-279 | 2026-08-10 | **【用語「採用ダイアログ」を「採用パネル」へ改める。実体はモーダルではない】** **`DES-005` §5.6 項目12 の当該 UI は、モーダルではなくインラインで展開するパネルである。** **★実害が出た**——**この呼称が原因で、開発者が別のモーダル〔既存セットプレイの紐付け。成立条件をそもそも持たない〕のことと解釈し、いったん誤った判断をしかけた。** **⇒ 改める。** **★ただし「採用」という操作の語彙は変えない**——**項目12 の操作は「採用」で正しく、そちらを変えると i18n キーと利用者の理解に触れる。** **改めるのは UI 形態の呼称〔ダイアログ → パネル〕だけである。** **★一般則**——**UI の呼称は「何をする面か」と「どう表示される面か」の 2 つを含む。** **後者を実体と違えて書くと、面そのものの取り違えを生む。** **全数 grep して揃えること**〔**D-247**。**名指しを網羅と読まない**＝**D-146**〕。 |
| D-278 | 2026-08-10 | **【「同じ X を再利用」とだけ書くと、参照先が合っていれば満たしたと読まれる】** **`DES-005` §5.6 項目12 は M19-03 の時点から「UI は項目10 と**同じ 2×2 グリッド**を再利用し、新しい操作モデルを作らない」と書いていた。** **しかし M19-03 の実装はチェックボックスの横並びであり、2×2 という構造が画面に現れていなかった。** **M19-07 はその実装を流用したため、設計書と実装の乖離が次のサブへ伝播した。** **2026-08-10 に開発者の目視で発覚し、全 4 面を揃えた。** **⇒ 裁定 2 点**——**(1) 記述の側に「同じ」の実体を書き足す**〔テーブル構造・列と行の割り当て・状態アイコン・凡例の有無〕／**(2) ★設計書に「既存の X と同じ」と書くときは、X の実体を 1 段だけ具体化する。** **参照先の名前だけでは、参照先自身が記述どおりでない場合に伝播が止まらない。** **★あわせて 2 点を記録する**——**(a) UI の構造は自動テストにもレビュー報告にも映らない**〔レビューチェックリストは「2×2 グリッドを使っている」を項目として持っていたが、**参照先の確認で通ってしまった**。開発者の目視でのみ捕まった〕／**(b) 設計卓は 2026-08-09 に「差し替えない」を追認したが、その根拠〔表記揺れは起きない〕は「両面とも横並びで見た目が同じ」という失効しうる前提に依っていた。** **グリッド化した時点で前提が失効し、製造が再提示したのは正しい。** |
| D-277 | 2026-08-10 | **【「次の番号」の運用は、警告文ではなく完了報告テンプレの常設項目にする】**（**設計卓の運用ミスの記録**） **`change-number-registry` は「★この行は連番を消費するサブが完了するたびに古くなる」「サブの完了報告と対で更新する運用が要る」と**自ら警告していた**が、M19-06 が CHANGE-095 を消費した時点で警告どおり失効した**〔§1 表に 095 の行が無く、契約 §4 も 094 のまま〕。 **★原因は設計卓にある**——**CHANGE-095 の通知書を発行しながら、registry §1 への「採番済」登録と契約 §4 の同期をまとめ適用のパッチ束へ送った。** **playbook §4.15 は「番号は起票時に採番して registry へ登録する」と定めており、起票と登録を切り離した時点で失効が確定していた。** **★M19-05 では製造が起票と同時に registry を更新しており、運用としてはそちらが正しく回っていた**〔設計卓は当初これを「通常の経路ではない」と評したが、経路の話と運用の話は別である〕。 **⇒ 裁定 2 点**——**(1) 完了報告テンプレに「併せて更新が要るもの」を常設項目として置く**〔M19-07 レポート §8 が実例として置いており、それが今回の失効を検出した。**形をテンプレへ引き上げる**〕／**(2) ★CHANGE 番号は起票と同じ手番で registry へ登録する。分けない。並列運用のあいだは設計卓が一元して払い出す**〔playbook §21.1〕。 **★契約 §3.2 の注記「本欄と §4 採番表は同じ数字である。片方だけ直さないこと」と同型の事故でもある**——**今回は registry と契約の 2 か所が同時に取り残された。** **【2026-08-10 errata】★本行の「registry と契約の 2 か所」は、当日に取り残されていた箇所の数であって、「次番号」の写し先の総数ではない。** **反映係の実査により、写し先は 4 か所である**〔registry §1 ／ 契約 §4 ／ board §2.1 ／ board §2.4〕。**現在値と総数は実査すること**（**D-259**＝**訂正値も裁定文へ書かない**）。 **★本裁定の趣旨は変わらない**——**起票と registry 登録を同じ手番で行う。** **むしろ写し先が 4 か所あることは、分けてはならない理由を強める。** |
| D-276 | 2026-08-10 | **【黙って無視される API 契約は、実装が直るまでのあいだ契約に書いて観測可能にする】** **`PUT /api/combos/:id`〔キー変更編集〕は `POST /api/combos` と同じリクエストボディを取るが、`setups` 全体を読まない。** **エラーにも警告にもならず単に無視される**〔M19-07 のレビューで判明。**本サブが作った挙動ではなく、`setups` 自体が以前から同じ扱い**。画面は編集時に `setups` を送らないため実害は無い〕。 **⇒ 裁定**——**`DES-002` に「PUT は `setups` を読まない」を明記し、弾く実装は followup `combo-put-ignores-setups` へ送る。** **★`verifiedConditions` に限定して書かないこと**——**無視されるのは `setups` 全体であり、限定して書くと「他のフィールドは書ける」と読まれる。** **★M19-07 の立場から特に必要である**——**本サブは「編集モードには入力 UI を出さない」を明示的な仕様として選んだ。** **UI が無いこと〔意図〕と API が無視すること〔取りこぼし〕は別の事実であり、分けて書かないと「編集では成立条件を扱わない設計だ」と丸められる。** **★一般則**——**成功応答を返しながら黙って捨てる経路が最も悪い。** 直せないあいだは契約に書く。 |
| D-275 | 2026-08-10 | **【契約には「あるべき姿」を書き、現状の取りこぼしは既知の乖離として併記する】** **値域外の受け身種別に対する応答が経路で食い違っていた**——**同梱経路〔`POST /api/combos`〕は 400、単独作成経路〔`POST /api/combos/{comboId}/setups`〕は 500**〔M19-03 からの取りこぼし。エラー写像を通っていない〕。 **⇒ 裁定**——**`DES-002` の契約は 400 とし、「単独作成経路は現状 500 を返す既知の取りこぼしがある」を併記する。揃える作業は followup `setup-create-invalid-value-status` が持つ。** **★契約に 500 と書かない理由 2 点**——**(1) 500 は「サーバの不具合」を意味するが実際は利用者の入力誤りであり、監視の側から見ると偽の障害として上がる**／**(2) ★契約に現状の取りこぼしを正として書くと、揃える作業に収束先が無くなる。** **★製造が本サブで直さなかった判断は正しい**——**指示書が「既存経路が不変であること」を求めており、応答ステータスの変更はこれに抵触する。** |
| D-274 | 2026-08-10 | **【古い記述の扱いは 3 分類である。一方の結論をもう一方へ持ち込まない】**（**D-250**／**D-259**／**D-268** の統合） **2026-08-09〜10 の 2 日で、同じ「古い記述をどうするか」に 3 通りの答えを出した。混同されるので分類を固定する。** **★(1) 生きた設計文書**〔`DES-*` / `M19-DESIGN-*`〕＝**本文を正本へ揃える。注記だけ置いて旧本文を残さない**〔**D-250**。**これから実装する人が読む指示であり、旧本文を残すとそれが実装される**〕。**★(2) board の裁定行**＝**本文を書き換えない。errata 注記を添える**〔**D-57**／**D-259**。**当時どう判断したかの記録であり、書き換えると記録が消える**〕。**★(3) 完了済みサブの指示書**＝**本文を書き換えない。撤回マーカーを 1 行置く**〔**当時どう指示したかの記録**であり、**実装指示としてはもう読まれない。しかし検索には当たる**ため、マーカーが無いと後任が現行仕様と誤読する〕。 **★分類の軸は 2 つ**——**「これから実装されるか」と「記録としての価値があるか」。** **完了済み指示書は「実装されない」かつ「記録価値がある」——裁定行と同じ側に立つ。** **適用例**＝`M19-03` 指示書の v1 文言 2 か所に撤回マーカーを置いた〔(3)〕／`M19-引き継ぎキット` の「中央へ請求済み」は本文を正した〔**現在地の記述であり記録ではない**＝(1)〕。 |
| D-273 | 2026-08-10 | **【api パッケージ間の DTO 型参照を認める。ただし 3 例目で共有パッケージへ切り出す】** **M19-07 が `internal/api/combo` から `internal/api/setup` の DTO 型を import した**〔**本プロジェクトで初めての api → api 参照**。`grep` で既存 0 件〕。**製造の実査**＝循環しない〔参照される側はテストを含め一切 import していない〕／同層参照であり `DES-002` §2 の「依存方向は上から下への一方向」に反しない／`api/combo` は既に `service/setup` を import しており setup ドメインへの依存自体は新規でない。 **⇒ 認める。★条件 3 つ**——**(a) 参照してよいのは DTO 型だけ**〔handler・ミドルウェア・サービス取得を api パッケージ間で呼ばない〕／**(b) 逆向きの依存を作らない**〔生じた時点でこの形は破綻している〕／**(c) ★3 例目が出たら共有 DTO パッケージへ切り出す。2 例までは import で許容する**。 **★採用理由**——**型を 1 つに保つ最も安い手段である。** type alias は名前を 2 つにして「別型では？」の疑いを生み、共有パッケージへの移動は既存参照の全書き換えを伴い、同名同構造の再定義は**同名で中身が違う型**を生む。 **★(c) を先に決める理由**——**1 例では構造を決められないが、3 例あれば形が見える。** **決めておかないと、5 例 10 例と増えてから判断することになり、そのときには書き換え範囲が大きすぎて誰も着手しない。** **`architecture-patterns.md` へ追記する**〔同書 §0 の定めにより設計担当が追記する〕。 |
| D-272 | 2026-08-09 | **【値域の要素数から導出した定数は、「導出のままであること」をテストで固定する】** **M19-06 の `unverified` 判定が「対象セルのうち未記録が 1 つ以上」の形になり、対象セル数が受け身種別の値域の要素数に結び付いた**〔従来の形にはこの結合が無かった〕。**製造は値域の正典スライスから導出することで現在の正しさを確保した。** **⇒ 追認する。★ただし「導出のままであること」をテストで固定させる。** **理由**——**`len()` 導出は「今」の保証であって「将来も導出のまま」の保証ではない。** **後任がリテラルへ戻しても、値域が増えるまで誰も気づかない。** **そして増えたときに壊れるのは絞り込みの結果であって、コンパイルでも既存テストでもない。静かに壊れる。** **★固定の仕方**——**`len()` を呼んでいることをテストするのではなく、値域を 1 要素増やした状態での挙動を固定する**〔**実装の形ではなく性質を固定する**〕。 **★あわせて**——**画面端が 2 値であることは計算にハードコードされている見込みである。現状は増えないため実害は無いが同じ形の結合であり、ハードコードなら実装コメントに理由を残すこと**〔「2 は `bool` だからである」と書けば、型が変わったときに検索で当たる〕。 |
| D-271 | 2026-08-09 | **【M19-06 の軸の「不問」は各セルの OR とする。3 値は排他でなくなる】**（**開発者裁定 2026-08-09 の追認と、その帰結の正典化**） **軸を指定しないときの意味に 3 案あり、開発者が「各セルで問うた答えの OR」を採った**〔他案では 3 値が単調でなく、**「全て」が最も狭い**という直感に反する結果が実データで観測された〕。 **⇒ 変わったものを 4 点、正典へ明記する**——**(a) 軸の「全て」は対象セルそれぞれで問うた答えの OR である**〔セルを問わず 1 回だけ問う形ではない〕／**(b) `unverified` は「未記録のセルが 1 つ以上残っている」である**〔**「1 セルも記録が無い」ではない**〕／**(c) ★3 値は排他ではない**〔画面端では成立し画面中央では不成立のコンボは、成立にも不成立にも該当する〕／**(d) ★「不成立」の含意する「成立が 1 つも無い」はセル内に限定される**〔コンボ全体ではない〕。 **★(c)(d) を書かないと必ず再燃する**——**指示書は「不成立は成立が 1 つも無いことを含意する」と書き、対のチェックリストがそれを仕様として重大判定から外していた。** **書かないと後任とレビューが「不成立に出たコンボが成立にも出るのは実装の誤り」と読む。** **★検算の形も変わった**——**「3 値の合計＝セットプレイを持つコンボ数」は成立しない。** **代わりの不変条件は単調性**〔個別指定の結果 ⊆ 「全て」の結果〕。 **★ラベル「未検証」は「手つかず」を想起させるが意味は「埋め残しがある」である。文言の再検討は followup へ送り、M19 では変えない。** |
| D-270 | 2026-08-09 | **【論理削除されたセットプレイの行は数えない。正典は画面ではなくデータ定義の側に置く】** **セットプレイの論理削除は削除時刻を立てるだけで紐付けの行を消さないため、成立条件の行が残る。** 一方**画面は論理削除を除いて見せている。** **⇒ 数えると「画面上セットプレイが無いコンボが未検証に出る」「削除済みセットプレイの成立で拾われる」という食い違いが起きる。** **⇒ 3 述語すべてから除外する**〔開発者裁定 2026-08-09〕。 **★反映先を `DES-005`〔画面〕ではなく `DES-003`〔データ定義〕とする**——**これは「画面がどう絞り込むか」ではなく「何を検証結果として数えるか」の定義である。** **`DES-003` は既に「未検証は行の有無で表現する」という同じ型の規則を持っており、同じ場所に置くのが自然。** **画面側に置くと、将来べつの面が同じ表を引くときに辿れない。** **★設計卓の書き落としの記録**——**指示書は「論理削除されたコンボ」だけを規定し、「論理削除されたセットプレイ」に触れていなかった。** **製造が実装実査で拾った。** **⇒ 一般則**——**指示書が X について規定していたら、「X と同じ語で呼ばれる別の対象が実装に無いか」を 1 回探す。** |
| D-269 | 2026-08-09 | **【D-262 の「仕様が無い」の射程を限定する】**（**D-262** の追補） **M19-05 の完了報告が指摘したとおり、`M19-DESIGN-02` §3 は「R6 の整合性検証も role 非依存に適用する」と書いており、R6 という要件自体は存在する。** **D-262 が「無い」と言ったのは、`M19-DESIGN-07` / `-08`〔＝M19-05 指示書の参照可資料〕における警告の仕様——何を検出し、何を、どこに表示するか——であって、要件の不存在ではない。** **★D-262 の判断は変わらない**——**参照可資料に仕様が無い以上、M19-05 で実装させれば製造が仕様を書くことになる。外した判断は正しい。** **⇒ 規律**——**「根拠が無い」と書くときは、どの資料の範囲で無いのかを同じ文に書くこと。** **範囲を書かないと、台帳へ入った時点で「どこにも無い」と読まれる。** **★これは D-256〔「基準が書かれていない」と判断する前に対象の節を全文読む〕の対**である——**D-256 は読む側の規律、本裁定は書く側の規律。** **followup `r6-parent-step-consistency-warning` の登録内容にも射程を書くこと。** |
| D-268 | 2026-08-09 | **【設計文書の本文是正と、board の裁定本文の扱いは作法が逆である。理由も逆である】** **同日に D-259〔裁定本文は書き換えず errata 注記〕を出しながら、`M19-DESIGN-08` の本文書き換えを指示したため、矛盾に見える。境界を明示する。** **★board の裁定行＝本文を書き換えない**〔**D-57**。**当時どう判断したかの記録であり、書き換えると記録が消える**〕。**★設計文書の本文＝正本へ揃える。注記だけ置いて旧本文を残さない**〔**D-250**。**これから実装する人が読む指示であり、旧本文を残すとそれが実装される**——**実装者はコードブロックをコピーし、注記は読まない**〕。 **⇒ 一方の結論をもう一方へ持ち込まないこと。**〔**本裁定は後に D-274 で 3 分類へ拡張された**〕 |
| D-267 | 2026-08-09 | **【別の繋ぎ単位に属するチェーン員が隣接する解は、1 つの問題ではなく 2 つである】** **M19-05 の完了報告が「除去の判定法が設計正本に無い」として裁定を求めた。** **判定法が書けなかったのは、構造の違う 2 つを 1 つの問題として扱っていたためである**〔レポート自身の分岐と一致する〕。 **★型 A＝連打版が別行として存在するキャラ**〔技 ID の列が違う ⇒ レシピの照合値も違う ⇒ **別レシピ**。壊れた版は実機で入力できない〕。**★型 B＝連打版行が無いキャラ**〔**同じ技 ID の列に費用計算が 2 通り**。照合値は同一 ⇒ **同じレシピの重複**〕。 **★原則は決着している**——**`M19-DESIGN-07` §9-3「セットプレイは最速原則」により、隣接するチェーン員は必ずチェーンする。** **単発として計上した解は最速原則に反しており、提案の対象外である。** **残っているのは「どう除去するか」だけで、そこが型によって違う。** **⇒ 裁定**——**(1) 型 B は同一の照合値を持つ提案が複数出たとき、費用が最小のものだけを残して集約する。判定法を新設しない**〔使うのは照合値の同一性という既存の概念だけであり、「隣接をどう定義するか」を決めずに済む。**チェーンは費用が小さいため、実機で成立するのは最小側**〕／**(2) 型 A は M19 で閉じない**〔**案「表記上の隣接で除去」は並べ替えれば成立する解を落とす**——**これは「隣接の判定」ではなく「表示順の決定」の問題であり、問題の所在が変わる。測る前に判定法を決めてはならない**。母数も小さい。followup `chain-adjacency-across-units`〕。 **★型 B の実装は `setplay` を触るため、M19-06 の完了後に別サブとして起こす。** |
| D-266 | 2026-08-09 | **【強調記号の偶数検査は、コードスパンを除外して数える】**（**D-258** の追補・**D-248 の逆側**） **D-258 が定めた「変更した行で `**` の個数が偶数か」の検査は、行がコードスパンでリテラルの `**` を含むと偽陽性を返す**〔`**` は 1 個として数えられるが、**CommonMark ではコードスパンが強調より先に解析されるため、この `**` は強調に参加しない**〕。 **★実例**——**D-258 の検査項目を本文に書いた行そのものが、自分の検査に引っかかる。** 2026-08-09 に設計卓が発行した 3 本のパッチで、いずれも当該行 1 行だけが検出され、**コードスパンを除いて数え直すと崩れは 0 だった。** **⇒ 規律**——**検査はコードスパンを除去してから `**` を数える。** **★D-248 と逆であることに注意する**——**GFM は表のセル分割をインライン解析より先に行うため、パイプはバックティックで保護されない**〔**D-248**〕。**一方、強調はインライン解析の中でコードスパンに負けるため保護される。** **「バックティックで守られるか」は対象によって答えが違う。一方の結論をもう一方へ持ち込まないこと。** **★あわせて `E-84` の履行**——**検査は「見つかった件数」だけでなく「検査した行数」も出すこと。** |
| D-265 | 2026-08-09 | **【TP-02（`chain_cancel_total` の追加投入）は M19 で閉じない。update 波と同波へ送る】**（**D-206** の決着） **M19-05 の Plan Mode 実査で、`move_derivations` の親のうち子を 2 つ以上持つ行の全数が出た**〔件数は followup が基準時点つきで持つ。**本行に書かない**＝**D-259**〕。**★指示書 §3.1 (ii) が名指しした m_bison `shadow_rise` 系 4 行が実測の親リストに一致した**——**材料はそろっている。閉じる先が決まっていなかっただけである。** **⇒ 送り先の裁定 3 点**——**(1) 未実測分は開発者の人手実測が律速であり、`m19-desk-status` §3.2-2 の update 実測と同じパスに載せるほうが安い**〔`E-74`＝**ブロックしないものは束ねる**〕／**(2) 実測の総数は「上限側の母数」である**——**(a)「空振りでも派生できる」の判定に一次源の記入欄が要るため、実測とセットでないと閉じられない**／**(3) `M19-overview` §6（完了の観点）に含まれず、M19 のクローズを止めない**。**★あわせて `M19-DESIGN-09` §4-3 が「これは TP-02 と同じ資料に効く」と述べており、update 波の設計と同じ資料を触る。** **followup `tp02-chain-cancel-total-remaining` へ登録。** |
| D-264 | 2026-08-09 | **【移動 system move は `move_derivations` の親に取れる。ただし判定軸は `category` ではない】**（**D-233** の決着） **M19-05 の Plan Mode 実査が 3 点を確定させた。** **(1) 根拠**——**`M19-DESIGN-07` §2-1 の分類 1（表記用親の cost 0）は親の `total` を読まない。読むのは子の通し `startup` のみである。** **したがって親が移動 move でも分類 1 は成立する。** 親自身の `total` が効くのは、その親を単独 filler として使う分類 4 の場合だけで、それは別の判定である。 **(2) ★判定軸に `category` を使わない**——**親候補は混在している**〔`ken/quick_dash` は `category='unique'`・rashid の前方ステップは `category='system'`〕。**軸は「`move_derivations` に親として登録されているか」であって `category` ではない。** **(3) ★ただし M19-05 では投入しない**——`move_derivations` への行追加はデータ変更であり、指示書 §0.2「解除しないもの」と §1.2-1 に反する。**投入すれば golden と canary が同時に動き、段階の切り分けが壊れる。投入は update 波（または専用サブ）。** **★投入時の必須確認**——**ゲート 2 は `startup_basis IN ('standalone','unknown') AND EXISTS(child)` である。「〜から最速で」系は通し値であり `through` になる系統のため、親参照を付けてもゲート 2 は発火しない。** **しかし `standalone` / `unknown` の行に親参照を付けると、その行は filler 候補から静かに落ちる。** **投入時に basis を全数実査し、`through` でない行があれば投入せず報告すること**〔**rashid の 3 行の basis を設計卓は実査していない**〕。 **★GC-2 の既知の限界**——**golden は合成 fixture で親参照を与えるため 5/5 を達成するが、実データには `ken/thunder_kick` の親参照が無く同じ前置は起きない。** **完了報告には「golden の green は実データでの動作を保証していない」ことまで書かせる**〔書かないと後任が「golden が通っているから実装できている」と読む＝`E-84` と同型〕。**followup `gc2-thunder-kick-parent-reference`。** |
| D-263 | 2026-08-09 | **【契約 F-3・F-5 が M19-05 の解除表から漏れていた。D-198 の再演である】** **★設計卓の誤りの記録**——M19-05 指示書 §0.2 は解除条項を (a)(b)(c) の 3 本しか挙げていなかったが、**本サブが必ず触れる条項が 2 本あった**〔製造が契約の全数列挙との突合で検出〕。**⇒ 扱いを分ける**——**(1) F-3〔新列のエンジン・API 非露出〕は解除する**〔**契約本文が「消費は M19-05」と自ら明記しており、解除は契約が予定していたものである。指示書が列挙し損ねただけ**〕／**(2) F-5〔`is_derived` / `is_aerial` の現行セマンティクス〕は解除しない。「参照の変更は F-5 の凍結対象外である」ことを確認するにとどめる**〔**F-5 の文言は「値やロジックを触らない」であり、target ゲートの参照先を変えることは値もロジックも変えない**。**意味は M19 では変えない**＝**D-222**〕。 **★これは D-198〔F-2 の解除が必然的に F-1 を含んでいた〕と同じ型である**——**発行側が凍結条項を列挙し損ね、製造が突合で拾った。** **指示書 §0.2 の注記「発行側が列挙し損ねる前提で確認する」が設計どおりに働いた。** **⇒ 規律**——**設計伝達レポート §2 では「触れたが凍結対象外」を「触れていない」と書き分けること。** 後者と書くと、後任が参照の変更に気づけない。 |
| D-262 | 2026-08-09 | **【R6 警告は M19-05 から外す。参照可資料を着手後に広げない】** **M19-05 §2.6 の 4 項目のうち R6 警告だけが、参照してよい先行資料の中に仕様を持たない**〔他の 3 項目は `M19-DESIGN-07` §3 と §2-1 に根拠がある〕。**★ただし「根拠が無い」ではない**——**`M19-DESIGN-07` §3 の親参照の行に「R6 整合検査に使用」という言及が実在する。無いのは警告の仕様〔何を検出し、何を、どこに表示するか〕である。** **⇒ 正しくは「名前はあるが仕様が無い」**〔**D-256** の型＝**「基準が書かれていない」と判断する前に対象の節を全文読む**。**全文を読んだことで、より正確な状態が確定した**〕。 **⇒ 裁定**——**R6 警告を M19-05 のスコープから外し、参照可資料に `M19-DESIGN-01` / `-02` を追加しない。** **理由 3 点**＝**(1) 参照可資料を着手後に広げるとサブの範囲が着手後に広がる**〔playbook §4.12「サブの段階 2 に大きな作業を予約しない」と同型。**仕様が無いものを実装させると、製造が仕様を書くことになる**〕／**(2) `M19-DESIGN-01` / `-02` は支援担当期の設計依頼書系であり現役の参照先ではない**〔**D-217**〕／**(3) R6 警告は非ブロッキング警告で提案結果を変えず、`M19-overview` §6 のどの項目にも当たらない**。**followup `r6-parent-step-consistency-warning`**——**仕様の確定から始めることを登録内容に含める。** |
| D-261 | 2026-08-09 | **【D-131(b) の前提が実装と食い違っていた。`punishfinder` テストの前方一致は是正しない】** **★裁定の撤回**——D-131(b) は「`punishfinder` のテストが `HasPrefix` を使っており `neutral_jumping_heavy_kick` を取り落とす。本番コードは部分一致で正しく、テストだけが前方一致のまま残っている」としていた。**M19-05 の Plan Mode 実査によれば、当該 `HasPrefix` は「従来 prefix 方式」を意図的に再現した対照群であり、同テストは案 C の増分が名指しの 2 件であることを検証している。** **⇒ 部分一致へ「是正」すると対照群と本命が同値になり、canary と増分検証がまとめて無効化される。** **指示書 §4.2-10 の要件〔取り落とさないこと〕は既存テストで満たされており、直す対象が無い。** **⇒ 指示書 §2.5 と `M19-DESIGN-08` §3.3 の当該記述を撤回する。** **★完了報告には「無い」を断定させない**〔playbook §4.21(6)〕——**本番コードが部分一致であることの逐語 ／ 取り落としを固定している既存テストの所在 ／ `HasPrefix` の grep 結果を「見つかった件数」と「検査した件数」の両方で**示させる〔`E-84`＝**「0 件」は検査が動いていない可能性を含む**〕。 |
| D-260 | 2026-08-09 | **【段階 C を M19-05 から外す。除外述語は setplay には正しく、`punishfinder` には転用できない】** **★設計卓の誤りの記録**——`M19-DESIGN-08` §3.1 の注記「setplay 側と punishfinder 側で同じ述語を使うこと」が誤りだった。**2 つの側は別の問いを立てている**——**setplay の filler 候補は「自分がこの技を単独で入力できるか」**〔できない ⇒ 単独 filler にできない ⇒ **除外が正しい**〕／**`punishfinder` の相手技候補は「相手がこの技を出しうるか」**〔親から出せば出しうる ⇒ **除外は実在する反撃機会を落とす偽陰性**〕。**M19-05 の Plan Mode 実査で、述語に一致する行が成立レーンの実技を含むことが判明した**〔ken 迅雷脚派生・m_bison ジャストパリィ時。件数は完了報告と followup が持つ＝**D-259**〕。 **⇒ 裁定**——**段階 C を M19-05 のスコープから外し、A・B のみとする。`internal/service/punishfinder/` に diff 0 を完了条件へ加える。** **★段階 A のゲート 2 による除外はそのまま実装する**——**あちらは setplay 側であり、除外されるべき行が除外されている。** **★理由 2〔測定〕**——**`M19-DESIGN-08` §4 の「P-16 の緩和と TP-01 の除外はどちらも候補集合の件数を動かす」も誤りである。P-16 が動かすのは setplay の filler 候補集合〔測定＝golden〕、TP-01 が動かすのは `punishfinder` の相手技候補集合〔測定＝canary〕であり、2 つの別の集合を 1 つと数えていた。** **測定対象が別である以上、段階 C を別へ出しても「2 回目の変更が 1 回目と混ざる」現象は起きない。** **★案「`AND damage IS NULL` を足す」は採らない**——**対症であり、理由が誤ったまま結果だけが合う。** `damage IS NULL` は「手動確認レーンに落ちる条件」であって「除外すべき条件」ではなく、**zangief 連打版の damage が update 波で実測されれば静かに効かなくなる。** **距離依存の飛び道具は手動確認レーンに正しく居る行である。** **⇒ TP-01 が本当に解きたかったのは「手動確認レーンのノイズ」で、その原因は damage が測れていないことであり、「単独で出せない」こととは無関係だった。** **再設計は実測とセットで followup `punishfinder-standalone-exclusion-redesign` へ。M19 のクローズ条件に含めない。** **★契約 §3.2 と F-1 の解除は取り消さない**——**「解除したが使わなかった」ことを完了報告に明記させる**〔解除の記録を消すと、後任が「なぜ開けたのか」を追えない〕。**★スコープの変更につき開発者の承認を得た**〔**2026-08-09**。playbook §4.12。先例＝**D-245**〕。 |
