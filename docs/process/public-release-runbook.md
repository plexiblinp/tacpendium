# 公開リリース手順書（public-release-runbook）

| 項目 | 内容 |
|------|------|
| 文書ID | PUBLIC-RELEASE-RUNBOOK |
| バージョン | **1.2.0**（2026-09-21・private staging の実施結果とPR無効化を反映） |
| 位置づけ | **公開リポジトリ `tacpendium` への公開と、リリースを打つ手順の正本** |
| 承認 | **★開発者承認済み**（2026-09-20・`D-917`。**⇒ `CLAUDE.md` §10.Y の「型に無い恒久ファイル」の承認**） |
| 実行者 | **★★開発者**。**⇒ Claude ができるのは手順の更新と、走らせる前の確認までである** |
| 更新責任 | 設計卓（リリース経路が変わったとき） |
| 恒久である理由 | **★リリースは 1 回きりではない。⇒ 公開の数週間後に SF6 の大型アップデートが来ることが分かっている**（`followup` の `sf6-major-update-after-release`）**。★その都度この手順を引く** |

---

## 0. ★★本書の読み方

**★★★本書は「順番に上から実行する」形である。⇒ §2 が初回公開、§3 が 2 回目以降。**

**★★飛ばしてよい段は §3 に明記してある。⇒ 初回は §2 を 1 つも飛ばさないこと。**

---

## 1. ★★★全体像（**リポジトリは 2 つある**）

| リポジトリ | 可視性 | 役割 |
|---|---|---|
| **`combomgr`** | **非公開** | **開発。⇒ 設計書・進捗・指示書・作業ブランチのすべてが在る** |
| **`tacpendium`** | **準備中は private、内容確認後に public** | **配布。⇒ 公開スナップショットとリリースだけが在る** |

**★★★生成は一方向である**（`D-637`）**。⇒ 公開側へ内容が渡るのは、開発者が同期を実行したときだけである。**

**★★★リリースのワークフローは*公開リポジトリでのみ*走る**（`if: github.event.repository.private == false`）**。⇒ 理由は 2 つ。**

| # | 理由 |
|---|---|
| **1** | **attestation は private / internal リポジトリでは取れない**（GitHub Enterprise Cloud を除く） |
| **2** | **開発リポへのタグは公開スナップショット生成の正規の契機である。⇒ ガードが無いと、生成のたびリリース区分が赤くなる** |

**★★これが「誤って公開するつもりのなかったバージョンが公開される」ことを構造で塞いでいる**（`CHANGE-233` §3）**。⇒ 非公開の開発リポでは、タグを打っても job がそもそも起動しない。**

---

## 2. ★★★初回公開の手順

### 2.A 事前（**★公開リポの開設を待たなくてよい**）

| # | やること | 済 |
|---|---|---|
| **★★★A-0** | **`followup-backlog.md` を「未着手 ＋ 公開前」で機械走査し、残件が 0 であることを確かめる。⇒ マイルストーンの状態だけを見て「残りは無い」と言わないこと** | ☐ |
| **A-1** | **SCANOSS を実行する。⇒ 公開する中身を決める作業であり、公開リポの存在に依存しない。★★★完了条件は終了コード 0 ではない**〔§2.A.1〕 | ☑（2026-09-21・固定 SHA `51915cf93349deaa1b4a63122fd64c89b2b6d2da`。再スキャンと全 match の disposition 完了） |
| **A-2** | **SSH 署名鍵を作り、公開鍵を `.github/allowed_signers` へ登録する。⇒ 台帳は `ALLOW .github/**` によりスナップショットで運ばれる。★先にやるほど 2.C が早く通る** | ☑（2026-09-21・principal `tacpendium-release`、`namespaces="git"`） |

**★★★A-0 が無かったために、2026-09-20 に公開ゲート `A` の残件 4 件が取り残されていたことが分かった**（`D-918` / 計測点 `M-210`）**。⇒ 見つけたのは A-1 の実行中であり、偶然である。★マイルストーンが閉じたことは、その MS が産んだ残件が解消したことを意味しない。**

**★★A-2 を飛ばすと 2.C-1 で必ず落ちる。⇒ それは*正しい*挙動である**（`DES-002` §11.3 の契約 9＝「タグ署名の検証は、鍵が未登録なら明示的に落ちる」）**。★落ちたら鍵を登録して打ち直すこと。**

#### ★★★A-1 の完了条件（**SCANOSS**）

**★★★終了コード 0 では完了にしない**（`M26-04` 追補報告 §7.2）**。⇒ 次のすべてが記録されて初めて `A9` / `A-1` を完了とする。**

| # | 記録すること |
|---|---|
| **★★★1** | **対象 SHA と scope**〔`cmd` / `internal` / `web/src`〕**が固定されていること。⇒ 再スキャン中に branch head が進んでも対象を変えない** |
| **★★★2** | **是正対象ファイルの一致が消えたこと。⇒ 消さないと決めたものは、その理由と収束の説明** |
| **★★3** | **shadcn/ui の一致が、帰属済みの既知 vendored source として確認されていること** |
| **★★★4** | **新規の file / snippet match が増えていないこと。⇒ 初回の件数と比較する** |
| **★★5** | **残存 match の disposition が初回と同じ基準で記録されていること** |
| **★6** | **使用した SCANOSS CLI / server / KB の版。⇒ CLI 版が変わったなら初回との差として明記** |

**★★初回 WFP や結果 JSON を上書きしないこと。⇒ 初回と最終の差分表を残すためである。**

### 2.A.2 ★★★来歴の問い合わせを受けたとき

**★★★公開リポジトリはリリースごとの snapshot であり、非公開リポの履歴をそのまま公開しない。⇒ 公開リポだけを見た第三者には、先に公開されていた類似コードを後から取り込んだように見える場合がある。**

**★★この見かけ上の時系列と、実際の開発時系列を混同しないこと。**

| # | 保持する証跡（**★非公開リポ側**） |
|---|---|
| **1** | **対象ファイルの初出 commit SHA と author date** |
| **2** | **当該 commit の変更統計および milestone 名** |
| **3** | **同時に追加された設計・指示・レビュー・進捗記録** |
| **4** | **初回 SCANOSS の chronology CSV と、固定した検査対象 SHA** |

**★★★これらを公開リポジトリへ常時掲載したり、非公開履歴全体を開示したりはしない。⇒ 具体的な問い合わせを受けた場合に、必要なファイル・期間・一致箇所に*限定して*提示できる状態を維持する。★提示方法と範囲は、問い合わせ内容と非公開情報への影響を確認して個別に決める。**

**★★★「日付が先なら自動的に独自実装と証明できる」という意味ではない。⇒ 日付、機能と同時に入った履歴、プロジェクト固有契約、実コード差分を組み合わせて説明するための監査証跡保全方針である。**

**★★★本方針と `ALLOW docs/**` が食い違っている**（2026-09-20 に設計卓が検出＝`D-919`）**。⇒ evidence をどこまで公開するかは `M40-01` 段 5 で候補と根拠を出し、開発者が決める。**


### 2.B 公開リポジトリの開設

| # | やること | 済 |
|---|---|---|
| **B-1** | **`tacpendium` を private で作成する。⇒ 内容確認前の snapshot を公開状態に置かない** | ☑（2026-09-21。public で作成後、初回同期前に private へ変更） |
| **B-2** | **公開側の Issues / Discussions / Projects / Wiki / Pull requests を無効化する。⇒ 公開側は配布専用であり、開発・文書管理の第二正本を作らない** | ☑（2026-09-21） |
| **B-3** | **公開スナップショットを生成して private の `tacpendium` へ一方向同期し、`main` を作る**（`bash scripts/check-public-snapshot.sh` で ALLOW / DENY の網羅を確かめてから） | ☑（2026-09-21・公開側初回commit `4ab389f`） |
| **B-4** | **`main` の保護を設定する。⇒ 初回同期前は対象ブランチが存在しないため、B-3 の後に行う** | **設定済み・未強制**（2026-09-21。`protect-main`、対象＝default branch、削除・force push禁止、linear history必須。private中はプラン制約で強制されないためB-6で確認） |
| **B-5** | **private のまま snapshot の内容と設定を確認し、確認後に public へ変更する** | ☐ |
| **★★B-6** | **public 化直後に `main` の保護・ruleset を再確認し、無効なら再有効化する。⇒ 可視性変更で push ruleset が無効化されうるため** | ☐ |
| **★★B-7** | **private vulnerability reporting と Dependabot alerts を有効化する。⇒ public リポジトリでも既定は off の opt-in である**（`followup` の `private-vuln-reporting-not-enabled`） | ☐ |

**★★★B-7 は「public 化した手番で有効化し、忘れると気づけない」型である。⇒ 忘れても何も赤くならない。★だから本書に在る。**

**★★private staging のCI実走（2026-09-21）**：初回pushの `PR checks` 3ジョブ（go vet + go build／go test／web test）と、手動起動した `Nightly` 2ジョブ（cross-build／E2E）はすべて緑。Pull requestsを無効化しても、`PR checks` は `main` pushと手動起動で使い続ける。

**★★★公開側を直接編集しない。** 初回も 2 回目以降も、`combomgr` の確定 ref と許可リストから snapshot を生成し、一方向同期する。private staging は公開前の検査場所であって、第二の開発リポジトリではない。

**★★公開側のコミットメッセージへ、非公開 `combomgr` の commit SHA を書かない。** 公開側は `Initial public snapshot` または `Release <public-version>` とし、生成元との対応関係は非公開側の記録だけに保持する（§2.A.2）。

### 2.C ★★★実走確認（**★本番タグの前に必ず 1 度通すこと**）

| # | やること | 済 |
|---|---|---|
| **★★★C-1** | **公開リポ側で*署名付き注釈タグ*（rc）を打つ。⇒ 例＝`v0.1.0-rc.1`** | ☐ |
| **★★C-2** | **リリースのワークフローが通ることを見る。⇒ タグ署名の検証 → 門 → クロスビルド → 配布アーカイブ → SBOM → attestation → リリース本文 → 作成** | ☐ |
| **★★★C-3** | **`gh attestation verify <archive> --repo <owner>/<repo>` を配布アーカイブ 3 本に対して実行し、出力を残す** | ☐ |

**★★★C-3 が通った時点で `M36` がクローズする**（`followup` §J の `m36-02-attestation-verify-output-pending`）**。⇒ それまで `M36` は「仕組みは在るが効くか確かめていない」状態である**（`D-890`）**。**

**★★C で `artifact-metadata` の権限不足が出たら、理由ごと `release.yml` の `permissions` へ足すこと。⇒ 外した判断は実走で未確認である**（`CHANGE-234` §6。**★その旨はワークフローのコメントに在る**）**。**

### 2.D 本番リリース

| # | やること | 済 |
|---|---|---|
| **D-1** | **公開リポ側で*署名付き注釈タグ*（`v*`）を打つ** | ☐ |
| **D-2** | **ワークフローが組み終わるのを待つ。⇒ 手元のマシンからは何もアップロードしない** | ☐ |
| **★D-3** | **配布物を 1 本ダウンロードし、SHA-256 と attestation を*利用者の立場で*検証する。⇒ 手順はリリースページ本文とアーカイブ内の `README.txt` に在る** | ☐ |

---

## 3. ★★2 回目以降（**データ更新リリース等**）

| 段 | 扱い |
|---|---|
| **2.A** | **★不要**（鍵は登録済み。SCANOSS は依存を足したときだけ） |
| **2.B** | **★不要** |
| **2.C** | **★★リリース経路**（`release.yml` ／ `scripts/release-*` ／ 門）**を触ったときだけ要る。⇒ 触っていないなら飛ばしてよい** |
| **2.D** | **★毎回要る** |

**★★★あわせて、公開スナップショットの同期は毎回要る。⇒ 公開リポに内容が渡るのは同期したときだけである**（§1）**。**

---

## 4. ★★詰まったときの見どころ

| 症状 | 見るところ |
|---|---|
| **タグ署名の検証で落ちる** | **★`.github/allowed_signers` に公開鍵が在るか。⇒ 無ければ落ちるのが*正しい*挙動である**（契約 9） |
| **attestation が取れない** | **★リポジトリが private になっていないか。⇒ private / internal では取れない**（GHEC を除く） |
| **リリース区分がそもそも起動しない** | **★開発リポ `combomgr` でタグを打っていないか。⇒ public 限定ガードが効いている** |
| **門で落ちる** | **★`docs/usermanual/` の図が欠けている。⇒ 源泉とアーカイブの両方を見る**（`M36-02` のレビュー 中-4） |
| **配布 `README.txt` の名前で落ちる** | **★`scripts/release-targets.sh` の 1 列目と `docs/usermanual/dist-readme.txt` がずれている** |

---

## 5. ★★★やってはいけないこと

| # | やらないこと | なぜ |
|---|---|---|
| **★★★1** | **開発リポ `combomgr` でリリースを打とうとすること** | **★public 限定ガードで起動しない。⇒ 起動させるためにガードを外さないこと** |
| **★★★2** | **手元のマシンから配布物をアップロードすること** | **★リリース成果物は CI から出す**（2026-09-12 開発者判断・`license-strategy-outline` §10.6） |
| **★★★3** | **初回に §2.C を飛ばして本番タグを打つこと** | **★一度も通していない経路で本番を打つことになる。⇒ `M36-02` が一貫して避けてきた形である**（`D-890`） |
| **★★4** | **タグ署名の検証を素通りさせること** | **★署名していないものを署名済みとして配ることになる**（契約 9） |
| **★5** | **公開リポ側で直接編集すること** | **★生成は一方向である**（`D-637`）**。⇒ 次の同期で黙って巻き戻る** |

---

## 6. 参照

| ID | パス | 参照する箇所 |
|----|------|------------|
| DES-002 | `docs/design/02-architecture.md` | **§11.2**（配布物の構成と来歴） ／ **§11.3**（CI の 3 区分・契約 9） |
| — | `docs/change-notes/CHANGE-233-notification.md` | **CI が 3 本立てになった理由と、自動公開の安全性の根拠** |
| — | `docs/change-notes/CHANGE-234-notification.md` | **来歴の as-built。★SBOM の母集団が配布物より広いこと** |
| — | `docs/handover/followup-backlog.md` | **§J の `m36-02-attestation-verify-output-pending` ／ `release-rc-run-belongs-to-public-opening`** ／ `private-vuln-reporting-not-enabled` |
| — | `docs/process/parallel-board.md` | `D-637`（一方向生成） ／ `D-890` ／ `D-914` ／ `D-915` ／ `D-917`（本書の新設） |
| — | `docs/progress/M36-02-completion-report.md` | **§7**（実走の具体の手順） |
| — | `docs/instructions/M36-overview.md` | 完了の判定 |

---

*以上、公開リリース手順書 **v1.2.0**。* **★★★初回は §2 を 1 つも飛ばさないこと。⇒ とくに §2.C は「一度も通していない経路で本番を打たない」ためだけに在る。**
