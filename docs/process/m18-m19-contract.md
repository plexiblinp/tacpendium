# M18/M19 残タスク 軽量契約

| 項目 | 内容 |
|------|------|
| 文書ID | M18-M19-CONTRACT |
| バージョン | **v1.9.0**（2026-08-10。**§4 の CHANGE 採番表を「次番号＝098」へ同期**〔**D-277**。**094＝M19-05／095＝M19-06／096＝M19-07／097＝M19 クローズ の 4 本が反映済**。**★本欄と `change-number-registry` §1 は同じ数字である。片方だけ直さないこと**——**2026-08-09 に両方が同時に取り残されていた**〕。**旧記載 v1.8.0**＝2026-08-08。**§2 F-4 (2') の (c) を「形」ではなく「目的」で書き直した**〔`pending5-resolution-patch` CF-01 ／ **D-231**。**目的は「保護すべき値を上書きしないこと」であり、形の指定ではない**。既定の形は `WHERE <当該列> IS NULL` ／ 既定値だが、**機械規則が付けた誤値を是正する UPDATE では保護すべき値が存在しない**ため (i) 遷移元の値を `WHERE` に明示するか (ii) 無条件とし冪等であることを設計伝達レポートに明記する。**注記の blockquote はセル内に入れられないため F-4 の表の直後へ置いた**〕。**★あわせて §1.1 の Phase 2 行の `fastest_unreachable` の件数記述を是正**〔**D-207／D-236**。「11 キャラ分の `true` は 1 行のみ」「第三波は未記入」はいずれも失効しており、**同じ失効値の 4 か所目**——他の 3 か所〔`DES-003` §3.3 ／ `M19-DESIGN-07` §4-3・§9-5〕は 2026-08-08 に是正済みだった。**パッチは名指ししていないが、正本が明示されている数字の同期として当てた**〕。**v1.7.0**＝2026-08-08。**§2 F-1 を D-199 の新文言へ追随**〔「意味・値・使われ方を変えない」→「**意味と使われ方**を変えない。値の是正は (a)(b)(c) を満たす場合に限り許す」〕／**§4 の CHANGE 番号を 092 → 094**／**§4 のマイグレ連番を `000064` → `000069`**。**v1.6.0**＝2026-08-04。**§1.1 Phase 2 行の母数を 308 行へ確定**〔ボード **D-200**。309→308・11 キャラ内訳 160→159。`000063` が guile 1 行の `startup_basis` を確定させ `保留` 1 行が対象外に。正本＝`docs/progress/m19-04c-completion-report.md`〕。v1.5.0＝2026-08-03。**§1.1 Phase 2 行へ D-187 の是正スコープを同梱**＝`startup` が NULL の 153 行を `unknown` へ戻す作業〔既 seed 11 キャラの移動 move 99 ＋ 第三波 54〕。**ボードの Phase 2 行には 2026-08-03 に記載済みだったが契約側へ写っていなかった**〔反映結果レビューの指摘・**重大**〕。v1.4.0＝2026-08-03。**§1.1 Phase 2 行の母数を実態へ同期**＝`startup_basis` 309 行／`fastest_unreachable` 候補 16 行〔申し送り①への回答〕。v1.3.0＝2026-08-03。**§2 F-4 の (2) を「CSV を一次源とする backfill」へ限定し (2') を新設**＝D-179。v1.2.0＝2026-08-02。§1 スコープを M19 クローズまでへ拡張・F-4 を新形へ改訂・採番の失効値を是正・**§3.3 の反映先指定の誤りを是正**・**§4 を 092／000060 へ**） |
| 種別 | 補足資料（自由改訂・CHANGE 通知書対象外） |
| 適用範囲 | **【2026-08-02 改訂】M18/M19 の残タスク ＋ M19 のクローズまで**（M18-03c／M19-03 の DES 反映〔**完了**〕／M19-04〔**完了**〕／M19-04b／Phase 2／M19-05／M19 後続サブ／M19 クローズ）。**残り 13 キャラの seed 波は範囲外**（優先度引き下げ・ボード D-159） |
| 位置づけ | `docs/process/parallel-ops-decisions.md` §3 のフル 6 項目版ではなく、**残タスク分の軽量版**（`docs/process/m18-19-midstream-migration.md` §2 の指定＝「遡って書く価値が薄い。凍結インターフェース＋触る領域のみで足りる」） |
| 状態の正本 | `docs/process/parallel-board.md`（本書は契約＝**ズレ検出の基準**。状態はボード） |
| フル契約の適用 | **M20 から**（ブロック事前割当・DES 所有権の排他・通信規約を含む 6 項目） |

> **本書の使い方**: 認識ズレの是正を会話でなく**本書との diff** で行う。実装や設計が本書に反したら、実装を直す前に**契約を改訂するか、逸脱を裁定する**。
>
> **なぜ軽量でよいか**: M18/M19 は統合 1 チャット（親）が全レーンを持つため、**DES 所有権の排他を他レーンと調停する必要がない**。残るのは「M19-04 が M18 の完成物を壊さないこと」の担保だけである。

---

## 1. スコープ

### 1.1 作るもの

| # | 対象 | 内容 |
|---|---|---|
| 1 | **CHANGE-090 の三点セット** | M18-03c の DES 本体反映＋`change-report-090`＋registry 更新 → **M18 クローズ** |
| 2 | **CHANGE-087 の三点セット** | M19-03 の DES 本体反映＋`change-report-087`＋registry 更新 |
| 3 | **M19-04** | `moves` 新列 3 本＋新表 1 本＋機械 backfill＋人手 backfill＋ザンギエフ連打版 3 行の登録。**【2026-08-02 完了】**CHANGE-091・マイグレ 000049〜000052 |
| 4 | **M19-04b** | 第三波 `chain_cancel_total` 16 行 ＋ `move_derivations` 9 行（**データのみ・DDL なし**）。**【2026-08-02 追加】** |
| 5 | **Phase 2**（M19-04 の残り） | 人手判断の CSV 転記（`startup_basis` **308 行**〔seed 済み 11 キャラ **159** ＋ 第三波 6 キャラ 149。**2026-08-04 に 309／160 から確定**＝`000063` が guile 1 行を確定させ `保留` 1 行が対象外に。ボード **D-200**〕 ＋ `fastest_unreachable` **候補 16 行**〔同 7 ＋ 9〕。**【2026-08-08 是正・D-207／D-236】人手判断は 16 行とも完了しており、内訳は 11 キャラが `true` 4 行 ／ `非攻撃技` 3 行、第三波 6 キャラが `true` 4 行 ／ `非攻撃技` 5 行である**〔旧記載「11 キャラ分の `true` は 1 行のみ」「第三波 6 キャラ分は未記入」はいずれも失効。**同じ失効値が `DES-003` §3.3 ／ `M19-DESIGN-07` §4-3・§9-5 にもあり、2026-08-08 に是正済み。本欄はパッチが名指ししていない 4 か所目**〕）＋ **`startup` が NULL の 153 行を `unknown` へ戻す是正**〔既 seed 11 キャラの移動 move **99 行** ＋ 第三波 **54 行**。**ボード D-187**——`startup_basis` は「格納されている `startup` の由来」を表す列であり、**値が無い行に由来は無い**。付与対象は行単位であって、`000050` がキャラ単位で除外できたのは偶然。**CSV も golden も変わらない**（当該列は CSV に存在するが全行空欄）。**M19-04c には入れない**〕＋ seedgen 新モードによる backfill マイグレ生成。**ブロッカー＝CSV 往復の byte-identical 検証**（ボード D-135）。**【2026-08-02 追加】** |
| 6 | **M19-05** | 消費側（through 解禁・チェーン費用・golden）。**保留 P-16／P-18 の決着を含む**。**【2026-08-02 追加＝旧 §1.2 から移動】** |
| 7 | **M19 後続サブ** | 成立条件の絞り込み ＋ コンボ新規登録時の入力（M19-overview §3）。**【2026-08-02 追加＝旧 §1.2 から移動】** |
| 8 | **M19 クローズ** | REQ-001 の FR 新設 1 本（ボード D-28）＋ M19 クローズ報告 |

### 1.2 作らないもの

- ~~**M19-05**（消費側）~~ ／ ~~**M19 後続サブ**~~ → **【2026-08-02 改訂・ボード D-160】本スコープ内へ移した**（§1.1 の 6・7）。**旧記述は 2026-07-30 時点で「M18/M19 の残タスク＝087／090 の DES 反映と M19-04」を対象に書かれたものであり、実態と食い違っていた。** 開発者判断（2026-08-02）＝**本チャットで M19 のクローズまで行う**
- **残り 13 キャラの seed 波（M14）**。**【2026-08-02 追加・ボード D-159】優先度を下げる**——アプリ完成前にゲーム側のアップデートが入る見込みのため。**★配布 blocker は解除されない**（全 30 キャラ充足が条件＝D-43。**後回しにすることと blocker を外すことは別**）。**M20 以降の担当へ引き継ぐ**
- **M20 以降**。別チャット
- **M18-03c でサブがさらに増える場合の実装**。overview の更新と開発者承認（playbook §4.12）まで行い、**実装は次へ引き継ぐ**（開発者判断 2026-07-27）

---

## 2. 凍結インターフェース（★M19-04 が壊してはいけないもの）

**M18 は全サブ実装完了済みである。M19-04 は `moves` に触るため、M18 の完成物と接触面を持つ。**
以下は **M19-04 の承認前提**であり、崩れたら**再承認が要る**（開発者承認 2026-07-27・ボード §3.3）。

| # | 凍結対象 | 内容 | 崩れたときの症状 |
|---|---|---|---|
| **F-1** | **確定反撃サーチの走査列** | `moves.is_projectile` / `moves.is_aerial` / `moves.on_block` / `moves.recovery` の**意味と使われ方**を変えない。**値の是正は、(a) 是正の一次源が明示され (b) canary の全数差分が説明でき (c) 変更した列と行を設計伝達レポートに明示する場合に限り許す**（**D-199**・2026-08-04 開発者承認） | 確定反撃の候補集合が静かに変わる。`is_projectile` は飛び道具除外の**唯一の判定源**であり、誤りは finder の誤走査に直結する |
| **F-2** | **`moves.startup` / `moves.total` の既存値** | **1 行も書き換えない**。M19-04 が足すのは新列だけ | M18-02 の確定反撃判定と M19 の `S = Σ(filler.total) + target.startup` が同時に狂う |
| **F-3** | **新列のエンジン・API 非露出** | `startup_basis` / `chain_cancel_total` / `fastest_unreachable` / `move_derivations` は **M19-04 の時点でエンジン・既存 API に露出させない**（消費は M19-05） | golden・期待 SQL が壊れる（承認時に「影響ゼロ」と評価した前提が崩れる） |
| **F-4** | **CSV → 既存 DB への値の流入経路** | **【2026-07-31 改訂・D-75】旧形「CSV 再取込が新列の人手付与値を上書き・NULL 化しない」は、発生条件そのものが存在しないため撤回した**（RESEARCH-04 C-2＝**既存 DB への CSV 再取込機構は現行コードに無い**）。**新しい不変条件は 2 つ**——**(1) CSV → 既存 DB へ値が流れ込む経路を新設しない。** **(2) ★CSV を一次源とする backfill マイグレを生成するときは、CSV で値が空の行を UPDATE 対象に含めない**（既存 backfill 3 例と同型。**`COALESCE` マージの前例は無いので採らない**）。**(2') CSV 以外を一次源とする転記マイグレ（実測資料など）は (2) の対象外である。** ただし次の 3 点をすべて満たすこと——**(a) 一次源を provenance コメントで明示する (b) 投入する値が常に非 NULL である (c) 既存値を保護する**——**目的は「保護すべき値を上書きしないこと」であり、形の指定ではない。** 既定の形は `WHERE <当該列> IS NULL` / 既定値。**ただし機械規則が付けた誤値を是正する UPDATE では、保護すべき値が存在しない。** その場合は **(i) 遷移元の値を `WHERE` に明示する**（`AND <列> = '<機械付与値>'`）か、**(ii) 無条件とし、冪等であることと保護すべき値が無いことを設計伝達レポートに明記する。** **【2026-08-08 改訂・D-231】詳細は表の直後の注記**。**先例＝`000060`**（`chain_cancel_total` 16 行。一次源＝`character_data/chain-cancel-measurements.md`） | (1) を破ると、seed 再投入のたびに人手付与値が消える経路が生まれる。(2) を破ると、**空欄の行が既存値を NULL で上書きする**（エラーにならない） |
| **F-5** | **`is_derived` / `is_aerial` の現行セマンティクス** | 値やロジックを**触らない**。DES-003 の**説明文言の是正のみ**行う | M18 の確定反撃走査へ波及する |
| **F-6** | **`combos` の INSERT 列と `model.Combo`** | M18-03b が `MaterializedFromComboID` を追加し INSERT 列を 22 → 23 へ増やした。**M19 側が `model.Combo` を読む場合は影響範囲に入る** | 列数のズレで setplay 側の scan が壊れる |

> **【2026-08-08 改訂・D-231】F-4 (2') の (c)「既存値の保護」は、形ではなく目的で読む。** M19 Phase 2 で、**形どおりにすると目的を達成できない箇所が 2 つ出た**（`000066` の `startup_basis` 書き直し 8 文＝**保護すべき人手値が存在せず、機械規則が付けた誤値を上書きするのが目的**だったため無条件 `UPDATE`／`000067` の kimberly `elbow_drop`＝`AND startup_basis = 'standalone'` で**保護ではなく「機械付与値からの遷移」を明示**）。**形で書いた規約は、形が合わない事例が出た瞬間に「違反」と「正しい判断」の区別がつかなくなる**（playbook §4.21 (1)＝**原則は性質で書く**）。
>
> **★契約側だけが古い形で残るのは、DF-04 で F-1 に起きたのと同じ形である**——**Phase 2 は既に新しい読み方で実装しており、契約が追随していなかった。**

> **F-1〜F-4 は開発者が M19-04 を承認したときの前提そのものである。** 承認時点でコンテキストが逼迫していたとの開発者コメントがあるため、**着手前に 4 点を再確認してよい**（再計算の手間より質を優先）。

---

## 3. 触る領域

### 3.1 スキーマ

| 対象 | 変更 | サブ |
|---|---|---|
| `moves.startup_basis` | **新列**。TEXT enum `standalone` / `through` / `unknown`・NOT NULL・既存行は `unknown` 起点 | M19-04 |
| `moves.chain_cancel_total` | **新列**。INTEGER・NULL 可。**非 NULL であること自体がチェーングループ員であることを兼ねる**（別フラグを作らない） | M19-04 |
| `moves.fastest_unreachable` | **新列**。BOOLEAN・NOT NULL DEFAULT false | M19-04 |
| `move_derivations` | **新表**。`child_move_id` / `parent_move_id`・PK(child, parent) | M19-04 |
| ザンギエフ連打版 3 行 | `moves` への**新規行登録**。**着手時に個別承認**（保留 P-02） | M19-04 |

**CHANGE-087 / CHANGE-090 はいずれもスキーマを変更しない**（087 のマイグレ 000042 は既に disk 着地済み、090 は DDL なし）。

### 3.2 コード

| 領域 | 触る | 備考 |
|---|---|---|
| `internal/service/punishfinder/` | **触らない**（M19-04） | F-1 の担保。走査列に非干渉であることを**実装で担保**する |
| `internal/service/setplay/` | 触る（M19-05 以降） | M19-04 は新列を露出させないため実質未接触 |
| `migrations/` | 触る（M19-04＝完了） | **【2026-08-02 是正】M19-04 は 000049〜000052 の 4 本を消費して完了した**（旧記載「次は 000043 から」は 2026-07-30 時点の値）。**次の空き番号は 000069**（**2026-08-08 同期**。000043〜000048＝M14-03d／000053〜000059＝M14-03e／**000060・000061＝M19-04b**／**000062＝M19-04d**／**000063＝M19-04c**／**`000064`〜`000068`＝M19 Phase 2**〔**D-230**。①`000064`／②`000065`／追補 `000066`〜`000068`〕。旧記載 000064 は 2026-08-04 時点の値、その前の 000060 は 2026-08-02 時点の値）。**★本欄と §4 採番表は同じ数字である。片方だけ直さないこと**——2026-08-08 に §4 のみを先に直し、本欄が `000064` のまま取り残された。**着手時に `ls migrations/` を実査して払い出す**運用は継続する |
| `character_data/*.csv` | **P-01 の決定次第** | CSV に置くなら F-4 の担保が別途要る |

### 3.3 設計書（CHANGE ごとの反映先）

| CHANGE | 反映先 |
|---|---|
| **090**（M18-03c） | DES-005 §5.20・§5.21・§5.7／DES-002 §4.2／DES-003 §3.3（errata と差分のみ）・§3.15・§3.17 |
| **087**（M19-03） | DES-003（§3.13 の後に `combo_setup_results`）／DES-005 §5.6 項目 10・12／**DES-006 は変更なし**（VAL を足さない＝ボード D-27） |
| **M19-04**（CHANGE-091・**反映済**） | DES-003（新列 3・**新表は §3.20 で末尾採番**／**`is_derived`・`is_aerial` の説明文言の是正**）。**★旧記載「DES-004（`is_derived` 説明文言の是正）」は誤りだった**——`is_derived` の列説明は **DES-003 §3.3** にあり（2026-07-26 errata③ の場所）、DES-004 §2.4 は索引除外の条件として引いているだけである。**DES-004 側の変更は §2.1 への追記のみ**（`move_code` の長さ規約・変種サフィックス一覧＝091 の範囲外の同梱）。**先例＝CHANGE-068 の「DES-003 §3.3」→ 正は §3.4**。**REQ-001 の FR 新設は M19 クローズ時に 1 本**（ボード D-28） |

---

## 4. 採番

> **★【2026-08-12】本節の採番表は歴史記録である。以後は同期しない。**
> **M18/M19 はクローズ済みであり、M20 からブロック事前割当へ移行した**（`parallel-ops` §02 ／ ボード **D-293**）。
> **現行の採番は `change-number-registry` §1 と、各レーンの契約 §3**（`docs/process/m20-contract.md` ／ `m21-contract.md`）**が正本である。**
> **理由**——**同じ情報を 2 か所に持つと必ずドリフトする。「合わせる」の前に「片方を消せないか」を検討する**（教訓 **E-76**）。
> **本節は M18/M19 期にどう採番したかの記録として残す。下記の数値を現行値として引かないこと。**


| 資源 | M18/M19 期の方式 | 次の値 |
|---|---|---|
| **CHANGE 番号** | **registry 採番のまま**（ブロック事前割当は M20 から） | **次番号＝098**（**094＝M19-05 消費側・反映済** ／ **095＝M19-06 成立条件フィルタ・反映済** ／ **096＝M19-07 コンボ同梱セットプレイの成立条件・反映済** ／ **097＝M19 クローズ・`REQ-001` FR 新設・反映済** ／ 092＝欠番〔D-239〕）。 **★本欄と `change-number-registry` §1 は同じ数字である。片方だけ直さないこと**（**2026-08-09 に両方が同時に取り残された**＝**D-277**） |
| **マイグレ連番** | **着手時に `ls migrations/` を実査して払い出す**。**予約帯を作らない** | **000069**（**2026-08-08 同期**。**Phase 2 が `000064`〜`000068` の 5 本を消費した**＝**D-230**／一次源＝`docs/progress/m19-phase2-completion-report.md`。旧記載 000064 は 2026-08-04 時点の値。**000060／000061＝M19-04b・000062＝M19-04d・000063＝M19-04c**。**`golang-migrate` は連番の穴を許容するが、払い出した番号より小さい番号を後から作ると、その 1 本が永久にスキップされ、しかもエラーにならない**＝D-119／D-120） |
| 指示書番号（`M19-NN`） | **マイルストーンローカル＝自採番**（採番の一元管理対象外） | — |

**マージ順**: 2026-07-30 実査で全リモートブランチの最大マイグレ番号は **000042**、`origin/main` が既に 000042 を含む。**未マージのマイグレはゼロ**のため、「番号順とマージ順のズレで配布先 DB にだけ穴が開く」事象は現時点で発生条件を満たさない。**M19-04 で新規にマイグレを作ったら、マージ前に本節を再確認する。**

---

## 5. 通信規約

| 対象 | 置き場 |
|---|---|
| 状態の正本 | `docs/process/parallel-board.md` |
| 設計伝達レポート（製造 → 親・**例外のみ**） | `docs/handover/design-reports/`（**既存維持**。`docs/design-reports/` への移行は M20 で再検討＝保留 P-05） |
| 完了報告 | `docs/progress/<id>-completion-report.md` |
| 教訓（書き捨て） | `docs/handover/M19-lessons-retrospective-merge.md` 等。**`retrospective-log.md` の直接編集は反映係のみ** |
| チャット間リレー | **不要**（統合 1 チャットのため消滅） |

- **progress ログは親・個別設計チャットへ渡さない。** 唯一の入力は設計伝達レポートとする。例外の参照経路は塞がないが、**構造的に隠蔽はしない**。
- **`followup-backlog` の項目は番号でなくスラッグで参照する**（playbook §4.20）。番号は動きうる。

---

## 6. 改訂履歴

| 版 | 日付 | 内容 |
|---|---|---|
| 1.8.0 | 2026-08-08 | **§2 F-4 (2') の (c)「既存値の保護」を目的ベースへ改訂**（`pending5-resolution-patch` CF-01 ／ **D-231**）。**旧＝「`WHERE <当該列> IS NULL` で既存値を保護する」という形の指定。新＝目的（保護すべき値を上書きしないこと）を先に置き、機械規則が付けた誤値を是正する UPDATE では (i) 遷移元の値を `WHERE` に明示する／(ii) 無条件とし冪等であることを設計伝達レポートに明記する、の 2 形を許す。** **★Phase 2 は既に新しい読み方で実装しており、契約側だけが古い形で残っていた**——**DF-04 で F-1 に起きたのと同じ形**。**★注記の blockquote は表セル内に入れられないため F-4 の表の直後へ置いた**（判断が入るため報告する）。**★あわせて §1.1 の Phase 2 行の `fastest_unreachable` の件数記述を是正**（**D-207／D-236**。「11 キャラ分の `true` は 1 行のみ」「第三波は未記入」はいずれも失効。**同じ失効値の 4 か所目で、他の 3 か所は 2026-08-08 に是正済みだった**。**パッチは名指ししていないが、正本が明示されている数字の同期として当てた＝D-196**）。反映係（Claude Code）による改訂 |
| 1.7.0 | 2026-08-08 | **§2 F-1 を D-199 の新文言へ追随**（`phase2-doc-fixup-patch` DF-04 ／ 訂正指示 §3）。**「意味・値・使われ方を変えない」→「意味と使われ方を変えない。値の是正は、(a) 是正の一次源が明示され (b) canary の全数差分が説明でき (c) 変更した列と行を設計伝達レポートに明示する場合に限り許す」**（D-199・2026-08-04 開発者承認）。**★列名の `moves.` 接頭辞は現物のまま保持した**（訂正指示 §3 の明示指定）。**Phase 2 は既にこの新文言を正として実装しており、契約側が未追随だった。** あわせて **§4 の「CHANGE 番号／次の値」を 092 → `094`**（DF-05 ／ 訂正指示 §2.1。092＝欠番〔**D-239**〕・093＝述語拡張を消費済み）／**§4 の「マイグレ連番／次の値」を `000064` → `000069`**（**Phase 2 が `000064`〜`000068` の 5 本を消費**＝**D-230**。一次源＝`docs/progress/m19-phase2-completion-report.md`）。**★訂正指示 §2.2 は「契約はマイグレ連番の数値を持たない」として指示を撤回していたが、現物 §4 は `000064` を保持していた**——前提誤りであることを報告 A で設計卓へ上げたうえ、**開発者裁定（2026-08-08）により同期した**。反映係（Claude Code）による改訂＝D-196 |
| 1.6.0 | 2026-08-04 | **§1.1 Phase 2 行の母数を 308 行へ確定**（設計卓の依頼・項目 8。**反映係による事実追従**＝D-196）。`startup_basis` **309 → 308 行**〔11 キャラ内訳 **160 → 159**〕。**出所＝ボード D-200 ／ 正本 `docs/progress/m19-04c-completion-report.md`**——`000063`（M19-04c）が `('guile','sonic_cross_2_meter_od')` の `startup_basis` を `unknown` → `through` へ確定させ、**`保留` だった 1 行が Phase 2 の対象から外れた**。**★減るのはその 1 行だけである**——改名した他の 2 行は `startup_basis` を確定させていないため記入値を持ったまま Phase 2 に残る。あわせて **§3.2・§4 の「次のマイグレ連番」を 000060 → `000064` へ同期**（**同じ数字が動いたら全出現箇所を grep する**＝v1.4.0 の改訂履歴が自ら書いている失敗パターン）。**§2 F-4 の `000060` は先例の参照であり、かつ凍結インターフェースのため触っていない**（D-196 境界条件 4）。反映係（Claude Code）による改訂 |
| 1.5.0 | 2026-08-03 | **§1.1 Phase 2 行へ D-187 の是正スコープを同梱**（反映結果レビューの指摘・**重大**）。`startup` が NULL の **153 行**〔既 seed 11 キャラの移動 move **99 行** ＋ 第三波 **54 行**〕を `unknown` へ戻す作業が Phase 2 の射程であることを明記。**★これは新規の裁定ではなく写し漏れの是正である**——D-187（2026-08-03）で決着済みで、`parallel-board.md` の Phase 2 行にも `DES-003 §3.3` の機械付与 3 規約 (i) にも既に載っていたが、**本書の Phase 2 行だけが母数の同期（v1.4.0）の際に取り残されていた**。**同じ対象を語る行が複数文書にあるとき、片方だけを直すと残りが静かに古くなる**（v1.4.0 と同型の漏れ）。反映係（Claude Code）による改訂 |
| 1.4.0 | 2026-08-03 | **§1.1 Phase 2 行の母数を実態へ同期**（反映係の申し送り①への回答パッチ・**重大**）。`startup_basis` **160 行 → 309 行**〔seed 済み 11 キャラ 160 ＋ 第三波 6 キャラ 149〕、`fastest_unreachable` **1 行 → 候補 16 行**〔同 7 ＋ 9〕。**★「1 行」は消さずスコープを明示する形へ改めた**——**その 1 行は 11 キャラ分の確定値であり、第三波 6 キャラ分の記入が済めば増え得る**。**数字そのものより「何の数字か」が落ちると、次に読む人が母数を取り違える**。**発行側の漏れ**＝束 第 2 版で `M19-DESIGN-07` の母数だけを直し、同じ数字を持つ本書と DES-003 を数えていなかった（**母数のような複数文書に散る数字は、更新のたびに全数 grep する**＝`PARENT-E26` の裏側）。反映係（Claude Code）による改訂 |
| 1.3.0 | 2026-08-03 | **束ねた非ブロッカー一式 第 2 版（2026-08-03）の §2 を反映**（ボード **D-179**）。**§2 F-4 の (2) を「CSV を一次源とする backfill マイグレ」に限定し、(2') を新設**——**CSV 以外を一次源とする転記マイグレ（実測資料など）は (2) の対象外**とし、代わりに **(a) 一次源の provenance コメント (b) 投入値が常に非 NULL (c) `WHERE <当該列> IS NULL` による既存値保護**の 3 点を課す。**先例＝`000060`**（`chain_cancel_total` 16 行・一次源は `character_data/chain-cancel-measurements.md`）。**★条文が広すぎたのは設計卓の誤りである**——字面どおりに適用すると **D-99**（`chain_cancel_total` は CSV に転記しない）と組み合わさって**この列がどの経路でも投入できなくなる**（製造が M19-04b §2 として正しく上申）。反映係（Claude Code）による改訂 |
| 1.2.0 | 2026-08-02 | **v1.1.0 で保留していた PA-03・PA-04 を反映**（CHANGE-091 の DES-003 反映が着地し「反映済」が事実になったため）。(1) **§4 の次番号を CHANGE **092**／マイグレ **000060** へ**（`golang-migrate` は連番の穴を許容するが、**払い出した番号より小さい番号を後から作るとその 1 本が永久にスキップされ、しかもエラーにならない**＝D-119／D-120）。(2) **§3.3 の反映先指定の誤りを是正**——旧記載「DES-004（`is_derived` 説明文言の是正）」は誤りで、**列説明は DES-003 §3.3 にあり、DES-004 §2.4 は索引除外の条件として引いているだけ**である。**DES-004 側の変更は §2.1 への追記のみ**（091 の範囲外の同梱）。先例＝CHANGE-068。反映係（Claude Code）による改訂 |
| 1.1.0 | 2026-08-02 | **束ねた非ブロッカー一式（2026-08-02）の §2・§10 を反映。** (1) **§2 の F-4 を新形へ改訂**（旧形「CSV 再取込が人手付与値を上書き・NULL 化しない」は**発生条件そのものが存在しない**ため撤回＝既存 DB への CSV 再取込機構は現行コードに無い〔RESEARCH-04 C-2〕。新不変条件は「CSV → 既存 DB の流入経路を新設しない」「backfill マイグレは CSV 空欄行を UPDATE 対象に含めない」の 2 つ＝D-75／D-128(b)）。(2) **§3.2 の採番の失効値を是正**（「次は 000043 から」→ M19-04 は 000049〜000052 を消費して完了・次の空きは 000060）。(3) **§1.1 に M19-04b／Phase 2／M19-05／M19 後続サブ／M19 クローズの 5 行を追加**し、**§1.2 から M19-05・後続サブを外した**（開発者判断＝本チャットで M19 クローズまで行う＝D-160）。**残り 13 キャラの seed 波を §1.2 へ追加**（優先度引き下げ・**配布 blocker は解除しない**＝D-159／D-43）。(4) **メタ表の適用範囲を M19 クローズまでへ改訂**。**★同パッチの PA-03（§4 の次番号表）・PA-04（§3.3 の反映先指定の是正）は当てていない**——いずれも「CHANGE-091＝反映済」を宣言する記述であり、**CHANGE-091 の DES-003 反映が未実施**（反映パッチ PA-01 の挿入位置がパッチ内で矛盾・設計卓へ差し戻し中）のため事実に反する。**したがって §3.3 の誤った反映先指定と §4 の失効した次番号は、本版の時点では未是正のまま残っていた**（→ **v1.2.0 で是正済み**）。 反映係（Claude Code）による改訂 |
| 1.0.0 | 2026-07-30 | 新設。M18/M19 の残タスク（CHANGE-090／CHANGE-087 の三点セット・M19-04）に対する軽量契約。凍結インターフェース F-1〜F-6 は M19-04 の開発者承認前提（2026-07-27）を契約化したもの |

---

*以上。ズレを見つけたら会話でなく本書との diff で是正する。*
