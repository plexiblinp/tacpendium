# フェーズ2 完了 → フェーズ3 引き継ぎ(phase2-to-phase3-handover)

| 項目 | 内容 |
|------|------|
| 文書種別 | マイルストーン/フェーズ引き継ぎ書(設計担当 Claude 作成) |
| バージョン | 1.0.0 |
| 作成日 | 2026-06-26 |
| 作成者 | 設計担当 Claude(フェーズ2 本流スパイン・M12 担当) |
| 宛先 | **フェーズ3(共有・協調・入力拡充)最初の設計担当 Claude**(別チャットで起動予定) |
| 引き継ぎ形態 | **整理工程を挟まない直接引き継ぎ**(私 → フェーズ3 最初の担当)。フェーズ1 は M7 → 整理工程 → フェーズ2 の 2 本構成だったが、今回はマイルストーン再編が不要のため **本書 1 本に統合** |
| 前提資料 | retrospective-log v1.0.41 / followup-backlog(2026-06-26 転記版)/ M12-overview / phase2-overview / m7-phase1-completion-handover(様式参考)/ phase2-kickoff-handover(様式参考) |

> **読み方**: §1 完了状態 → §2 確定判断 → §3 残作業カタログ(フェーズ3 で効くもの)→ §4 プロセス申し送り → §5 キックオフ(最初の一手)→ §6 現況一覧。詳細な生きた課題リストは **followup-backlog が正**(本書 §3 はキックオフ用スナップショット)。番号より項目の中身(何を・なぜ・どこを触るか)が正。

---

## 1. フェーズ2 完了状態(検証済み)

フェーズ2「先行リリース準備」は **M8〜M12 の 5 大マイルストーンで完了**。到達点 = 先行リリース実施点。

| 大MS | 達成 | 主な CHANGE |
|------|------|------|
| M8 | moves フレームデータ基盤(発生/持続/硬直/硬直差等 + CSV 契約 + 技名正規化)+ Playwright E2E 基盤 | 022/023/024 |
| M9 | 公式データ取込パイプライン(FR701 ツール → FR704 アプリ取込 → FR703 手動修正) | m9 群 |
| M10 | クラシック5体の登録 UI 解禁(ComboEditor キャラ固定解消) | m10 群 |
| M11 | custom_states 開始時状態(付与・表示・PATCH presence-detection 一般化) | 040〜043 |
| M12 | 先行リリース仕上げ(残フェーズ2 全件処遇 + UX/表記/正しさ + seed 整理 + B-7 + 統合 E2E + 配布判定) | 044〜048 |

- **クラシック5体**(ryu/ken/ingrid/c_viper/dhalsim)が UI から登録・閲覧でき、公式取込データ(新形 move_code)が投入済み。
- **custom_states 開始時状態**(ryu=denjin_charge / ingrid=sun_crest / c_viper=limit_decoupler)が付与・表示可能。
- **配布 DB がクリーン初期状態**(M12-05): 耐久 seed 36 件除去・ajg(aki/jamie/guile)除去・ryu seed 新形化・旧形 move_code 一掃。仮想コントローラが ryu + 取込5体で解決(B-7 クローズ)。
- **統合 E2E**(M12-06): E-1 回帰穴(多項目メタデータの 1 項目編集 → 他項目保持 + クリア)を自動 spec 化。先行リリース配布判定チェックリスト(開発者実施)を整備。

### 1.1 設計書本体の現行版(フェーズ3 着手時の基準)
- REQ-001 v2.15.0 / DES-001 v1.3.0 / DES-002 v1.23.0 / DES-003 v1.22.0 / **DES-004 v1.6.3**(CHANGE-048 で dash 系追記)/ DES-005 v2.26.0 / DES-006 v1.13.0 / SUPP-001 v1.24.0。
- マイグレーションは **000017 まで**(000017 = ajg 除去 + ryu code 新形化)。
- CHANGE 最新 = **048**、次採番 = **049**(欠番 008/009/014)。

### 1.2 補足資料の現行版
- playbook v1.11.0 / architecture-patterns(最新)/ change-number-registry v1.36.0 / model-allocation v1.21.0 / **retrospective-log v1.0.41**(§6.6.5 = M12 反省確定)/ followup-backlog(2026-06-26 転記版、§F12 = M12 繰越)/ retrospective-digest(最新)。
- **`m12-retrospective-notes.md` は転記完了につき破棄可**(§1→retrospective-log §6.6.5、§2→followup-backlog §F12 へ転記済み)。
- progress-summary は **開発者が M12 完了時に更新予定**(設計担当タスク外、開発者方針)。

---

## 2. フェーズ2 で確定した主要判断(フェーズ3 が前提にすべき)

- **先行リリース対象 = クラシック操作5体**(ryu/ken/ingrid/c_viper/dhalsim)。aki/jamie/guile は M12-05 で除去(対象外)。ブランカ等の追加は別スコープ(キャラ追加 = M10 型の登録 + FR701 CSV + custom_states 整備)。
- **custom_states は開始時状態のみ**。消費(コンボ中でどう減るか等)は非モデル化 = notes 管理(フェーズ2 スコープ確定)。フェーズ3 で消費をモデル化するなら DES-003/004/006 追記(CHANGE 見込み)。
- **PATCH メタデータは presence-detection 単一トライステート**(CHANGE-043: キー不在=不変更 / null=NULL クリア / 値=更新)。E-1 がその回帰ネット。
- **move_code 正典 = 新形**(DES-004 §2.1。通常技 `standing_*`/`crouching_*`/`jumping_*`、投げ `throw_forward`/`throw_back`、`drive_parry`。移動 `dash_*`/`micro_*`/`jump_*` も system move として §2.1 に列挙=CHANGE-048)。
- **drive_damage = REAL/-6〜6 小数**(CHANGE-046、VAL-C13 + UI 担保・CHECK 不使用)。
- **起き攻め 6 BOOLEAN は正典語彙統一**(重ね/シミー・その場/後ろ受け身・ドライブラッシュ)・有利フレーム `+nF`(R-1)。
- **始動技は自動推定一本化**(レシピ先頭導出)・VAL-C03 は BE 維持(C-12)。
- **キャラ選択は共通 `CharacterSelector`**(一覧フィルタのみ native 維持=役割区別)(N-29)。

---

## 3. フェーズ3 残作業カタログ(機能領域別・番号非依存)

> 補助タグ: `[定義済み未実装]`=設計書に規定あり未実装 / `[要設計]`=実装前に設計が要る / `[CHANGE見込み]`=設計書本体改訂が要りそう / `[意図送り]`=意図的に送った。**living 版は followup-backlog**(本カタログはキックオフ用スナップショット)。

### A. 入力拡充・プリセット(フェーズ3 の本丸の一部)
- **A-1. 残り 4 プリセットのエイリアス実データ整備** `[定義済み未実装]`(numeric_ja / srk 等。現状は official_ja_move のみ実データ、他は日本語フォールバック)。
- **A-2. プリセット管理 UI / カスタムプリセット作成(DES-004 §6)** `[定義済み未実装]`。
- **A-3. recipe_cache の無効化トリガ(SUPP-001 §7)** `[定義済み未実装]` `[要設計]` `[A-1/A-2 着手時に必須]`: §7 は「プリセット作成/エイリアス編集時に当該プリセットの recipe_cache 全エントリ再計算(即時)」を既定済みだがトリガ未配線。**6.3-2 レシピ表記ゆれ + recipe_cache(followup-backlog F12-2)と連動**。M12-2 教訓(保存済みキャッシュ + 全表示箇所を全数調査)を前提に着手。

### B. custom_states 消費(フェーズ3 の設計対象)
- **B-1. custom_states の消費・参照のモデル化** `[定義済み未実装]` `[要設計]` `[CHANGE見込み]`: 毒/酔いレベル等が実際に機能する仕組み(ステップ/状況/レシピ/検証連動)。DES-003 §3.2 は定義構造のみで消費セマンティクス未規定 → DES-003/004/006 追記。
- **B-2. ajg 再追加時の jamie `drunk_level` 正典乖離**(followup-backlog F12-5、将来): composite vs 単純 level/integer の設計判断。dist CSV 無し。

### C. 共有・協調(フェーズ3 = REQ-001 §7 の本旨)
- 先行リリースの反応を踏まえた共有・協調・入力拡充機能。**先行リリースのフィードバック取込方針**を開発者と確定して具体化(§5.3)。

### D. UI / レスポンシブ / i18n
- **D-1. 本格スマホ UI** `[意図送り]`(カード/アコーディオン/ボトムシート/スワイプ。DES-005 §5.6/§5.8/§5.9/§5.10/§7)。
- **D-2. 英語ロケール整備** `[意図送り]`(official 英語技名プリセット新設・英語 UI。custom_states 英語名・SA ゲージ等の整合)。
- **D-3. 表示細部の i18n/表記**(followup-backlog F12-2): SA ゲージ「(0〜3本)」日本語のみ(純 i18n・要対応)/ 情報バー頭文字 i18n(B-9)/ G-3 タグ表示崩れ(残存崩れ修正 + タグフィルタのドロップダウン化)。

### E. UX 整理
- **E-1. セットプレイ紐付け 2 系統の UI 統合(6.5-1)** `[要設計]`(followup-backlog F12-3): 「既存から紐付け」と「転用候補」が C-08 後に機能重複。**開発者要望 = 編集の『既存から紐付け』ボタンを削除し候補表示へ一本化**(前ターン D-9)を統合方針として反映。CHANGE 見込み(DES-005 §5.6/§5.7)。
- **E-2. 設定「デフォルト表示キャラ」配線漏れ(§4-4)** `[CHANGE見込み]`(followup-backlog F12-1): 一覧/マイコンボ/新規登録/比較が `INITIAL_CHARACTER_ID=1` 固定で `config.defaults.characterId` 不参照。**修正サブ + CHANGE**。優先順位 URL/セッション文脈(CHANGE-039)> 設定既定 > リュウ。マイコンボ遷移時のキャラ維持(D-2)も同基盤で同梱候補。
- **E-3. dirty 確認ダイアログ**(followup-backlog F12-3): DES-005 §5.7 L368 は確認ダイアログを規定するが実装は `navigate(-1)` 直行。(i) 実装に dirty 確認を入れる /(ii) DES 規定を緩める の設計判断。

### F. E2E / テスト拡充
- **F-1. メタデータ入力フィールドの安定 test-id 整備**(followup-backlog F12-4): damage/drive/sa/knockdown/memo/起き攻め6 に test-id が無く、フォーム値の保持/クリアの UI 完結・型横断検証が test-id 整備前提(FE 変更)。
- **F-2. ラッシュ技の昇格/presence-detection E2E**(followup-backlog F12-4): M12-06 はラッシュ技を踏んでいない。ラッシュ仕様見直し時に回帰 E2E。
- **F-3. 過去マイルストーンの i18n 網羅再検証**(retrospective-log §7.1 メタ候補): コンテキスト圧迫 × モデル × MS 後半の三重要因による i18n 漏れの遡及確認。

### G. リファクタ / 技術的負債(機能影響なし・全て `[意図送り]` フェーズ3)
- recipeCache の API 露出(比較画面用)/ queryKey の object/flat 統一 / useCheckDuplicate の TanStack Query 化 / SetupSummary 型の所在集約(m7 F-1)。
- config 保存の TOML 全再エンコード(コメント喪失)+ アトミック書き込み重複(m7 F-2)。
- 検証エラー fetch の共通 lib 昇格(setup/combo の `requestJSON` 重複)(m7 F-3)。
- RecipeBuilder console.warn(P-01、m7 F-4)。
- テスト「非ryu + custom_states + moves」の inline INSERT パターン化(followup-backlog F12-6)/ 000012 down.sql の orphan リスク(既存・編集禁止)。

### H. 配布 / 運用(先行リリースの反応で優先度確定)
- LAN 切替時の IP 事前提示(m7 E-1)/ クロスビルド mac/Linux 実機起動(m7 E-2)/ GitHub Actions CI(m7 E-3)/ 依存ライブラリ棚卸し(m7 E-4 pkg/browser)/ LAN の FW/AV 既知制約(m7 E-5)。
- **友人リリース最終チェックの裏取り**(別途 `combmgr-friend-release-checklist.html`。開発者確認 2026-06-26 反映):
  - **配布形態の決定**: クラシック5体のうち **ryu のみ seed(000004 を M12-05 で新形化・温存)、ken/ingrid/c_viper/dhalsim の moves は FR704 取込(dist CSV)由来**。配布 DB を「取込済みで渡す」か「友人が初回取込する」かを決める。後者なら起動手順に**取込手順 + 配布 CSV** を同梱しないと ryu 以外で技が出ない。
  - **ゴミ箱/復元**: 復旧手段あり=問題なし(開発者確定)。**追加: 本アプリ外への破壊的 FS 操作が無いことの最終チェック**(`os.Remove`/`RemoveAll`/`Rename` を全数 grep し、データ/設定ディレクトリ配下に封じ込められパストラバーサル/絶対パス直書きが無いことを確認)= **read-only 調査の候補**。
  - **同時操作(楽観排他/WAL)**: 友人は独立環境・単一利用前提のため**残課題据え置き**(H 群・followup-backlog)。LAN 同時書込運用はしない前提。
  - **エクスポート = 未実装**(CHANGE-005 は設計書本体の改訂記録であり実装ではない。前任〔本書作成者〕が「実装済み」と誤認したのを訂正)。先行リリースは「**データ引き継ぎ不可の可能性**」を友人へ明示 + **DB ファイルのコピー**を保険として案内 + Known issues に 1 行(対応より説明)。フェーズ3 でデータ移行が要るなら **エクスポート実装**(`[定義済み未実装]`・スキーマ非依存=意味単位での書き出しが望ましい)。
  - **dirty 離脱確認**: followup-backlog F12-3 / 本書 E-3(継続論点)。

---

## 4. retrospective / プロセス申し送り

### 4.1 確立した運用(継承)
- **三役分離**: 設計担当(本書の担当)/ 製造担当 Claude Code / レビュー担当 Claude Code。設計担当は指示書 + レビューチェックリスト作成・Q&A・CHANGE 起票・handover/playbook 改訂提案を担う(playbook §1)。
- **CHANGE 運用**: 対象は **REQ-001 + DES-001〜006 のみ**。補足資料(SUPP/playbook/handover/retrospective/architecture-patterns/指示書/チェックリスト)は自由改訂(バージョン bump)。「念のため起票」は §16.4.4 違反。
- **着手前確認(§3.4)+ Plan Mode**: 設計担当のセルフチェック限界を製造の実態確認 + 多段フィードバック(Plan Mode 質問/完了報告/E2E/開発者確認)が補う。M12 期間はこれが前提誤り・内部矛盾を繰り返し捕捉した(機構が機能)。
- **本キット限りの実験運用**(playbook 恒久ではない): (a) 開発者への確認は成果物末尾に集約、(b) 出力前チェック(Python 簡体字 simp set + 禁則表現 grep = playbook §4.2 の言い換え対象を機械検出)。フェーズ3 で継続するかは開発者判断。

### 4.2 M12 期間の教訓(retrospective-log §6.6.5 確定)
- パターン A(既存実装/実態の確認漏れ)の網を広げる事例が中心: 表記の grep 全数(M12-1)/ 表示トークン × recipe_cache × 全表示画面(M12-2)/ UI 構造の現状全列挙(M12-3)/ 除去キャラの id/code 依存 grep(M12-6)。
- **FK=OFF × CASCADE の内部矛盾**(M12-5): 破壊的マイグレ指示書で「FK=OFF」と「子は CASCADE 連鎖」を同居させた設計担当ミス。製造が明示 DELETE で是正。→ 「マイグレ接続 FK=OFF のため子行は明示 DELETE」を破壊的マイグレ指示書テンプレに定型文化(digest §5 反映候補)。
- **追記マイグレの grep ゲート**(M12-7): 案A では DoD「旧形ゼロ」を「最終 DB 状態」と明記する。
- 教訓は retrospective-digest へ繰り込み。新規 §1 パターン追加は不要。

### 4.3 番号・体系
- 4 フェーズ体系の正本 = REQ-001 §7。CHANGE 次採番 049(欠番 008/009/014)。VAL 採番は設計担当が DES-006 の最大番号の次で確定。

---

## 5. フェーズ3 キックオフ担当への引き継ぎ(最重要)

### 5.1 最初の作業
1. 本書 §5.2 のドキュメントを読み、現況(フェーズ2 完了・先行リリース実施点)を把握。
2. **フェーズ3 のゴールを REQ-001 §7 から定義**(共有・協調・入力拡充)し、開発者と §5.3 を協議。
3. **フェーズ3 overview を起票**(マイルストーン分割・全件処遇・CHANGE 見込み)。phase2-overview / M12-overview が様式の前例。

### 5.2 最初に読むドキュメント(順序)
playbook(§1 役割・§4.10〜4.12・§16・§17.4)→ **本書** → docs-map(ID⇄実パス)→ **followup-backlog**(§F12 = フェーズ3 繰越)→ retrospective-digest(指示書執筆前に毎回)→ REQ-001 §7 → phase2-overview → code-facts(想定で書かず引く)。

### 5.3 着手前に開発者へ確認すべき事項
1. **フェーズ3 のスコープ優先順位**: 共有・協調 / 入力拡充(プリセット A 群)/ custom_states 消費 / UX 整理(E 群)/ 英語ロケール(D-2)のどれを先に置くか。
2. **先行リリースの反応フィードバックの取込方針**: 友人リリースで得た要望/不具合をフェーズ3 のどのマイルストーンへ割り付けるか。
3. **繰越課題の割り付け**: §4-4 配線漏れ(E-2)・セットプレイ紐付け統合(E-1)・データ保護裏取り(H)等を、フェーズ3 のどこで拾うか。

### 5.4 運用ルールの継承
- CHANGE 対象 = REQ + DES-001〜006 のみ / 出力前チェック(簡体字・禁則)/ 確認事項末尾集約(本キット実験)/ 教訓メモは破棄済み(retrospective-log §6.6.5 + followup-backlog §F12 が正)。
- git/commit/push・実機 E2E・配布判定は開発者ゲート(playbook §1.3/§1.4)。

---

## 6. ドキュメント現況一覧

| 種類 | ファイル | 現況 |
|------|---------|------|
| 設計書本体 | DES-001〜006 / REQ-001 / SUPP-001 | §1.1 の版。マイグレ 000017 まで |
| 恒久運用 | playbook v1.11.0 / retrospective-digest / architecture-patterns / change-number-registry v1.36.0 | 最新 |
| 反省 | retrospective-log v1.0.41 | §6.6.5 = M12 確定 |
| 計画 | followup-backlog(2026-06-26)/ model-allocation v1.21.0 | §F12 = フェーズ3 繰越 |
| 機械的事実 | code-facts(`5acf478`、§10 マイグレ含む) | 最新。フェーズ3 着手時に再生成推奨 |
| 破棄予定 | m12-retrospective-notes | 転記完了 = 破棄可 |
| 進捗 | progress-summary | 開発者が M12 完了時更新予定 |

---

## 開発者への確認事項

1. **本引き継ぎ書の確定とフェーズ3 キックオフの起動**
   何を: 本書を「フェーズ2完了→フェーズ3」引き継ぎの正本として確定し、フェーズ3 最初の担当へ渡してよいか。
   なぜ: 整理工程を挟まない直接引き継ぎのため、本書が唯一の継承資料になる。
   暫定案: 本書を確定。フェーズ3 担当は §5 の手順で起動。

2. **`m12-retrospective-notes.md` の破棄**
   何を: 転記完了(§1→retrospective-log §6.6.5、§2→followup-backlog §F12)につき破棄してよいか。
   なぜ: 一時メモ(tmp 配下)で、二重管理を避けるため。
   暫定案: 破棄。以後の正は retrospective-log + followup-backlog。

3. **先行リリースのフィードバック取込のフェーズ3 への組み込み**
   何を: 友人リリースの反応(要望/不具合/データ保護・配布形態の確認点〔§3-H〕)を、フェーズ3 のどの段で扱うか。
   なぜ: フェーズ3 の本旨(共有・協調)と先行リリース反応の取込は優先順位が絡み、開発者判断(§1.4)。
   暫定案: フェーズ3 キックオフ担当が overview 起票時に、先行リリース反応を最初のインプットとして整理し、§5.3 で協議。

---

*以上、フェーズ2完了→フェーズ3 引き継ぎ書 v1.0.0。配置 `docs/handover/phase2-to-phase3-handover.md`。整理工程を挟まない直接引き継ぎのため本書 1 本に統合。詳細な生きた課題は followup-backlog が正。*
