# 投入プロンプト: フェーズ4 境界の非武装セッション（2026-09-07 作成・`D-767`）

| 項目 | 内容 |
|------|------|
| 文書種別 | セッション投入プロンプト（`docs/handover/session-prompts/` の型） |
| バージョン | **1.2.0**（2026-09-07・**★★実測項目を 1 件追加＝`D-769`**〔本番ビルドが CI で走っているか〕） ／ 1.1.0（2026-09-07・**★★段 3**〔改善レーン C1〜C3〕**を本書から切り出した＝`D-768`。⇒ 別書 `20260907-improvement-lane-c1-c3.md` が持つ**） ／ 1.0.0（2026-09-07・`D-767`） |
| 由来 | **`P-53`**（開発者の逐語＝「A2:たてる」）。設計卓の推し＝**「リリース用」ではなく「非武装・境界セッション」として定義する** |
| いつ投入するか | **★★フェーズ4 の締めの後、`next_phase_kit`（フェーズ5 キックオフ資料）の生成より前** |
| 使い方 | **★下の `---- ここから ----` から `---- ここまで ----` までを、新しいセッションへそのまま貼る** |
| **★★本書でないもの** | **改善レーン C1〜C3**（`check-md-emphasis.sh` の案 A ほか）**は本書ではない。⇒ `docs/handover/session-prompts/20260907-improvement-lane-c1-c3.md` が持ち、走行中 3 本のマージ直後に回す**（`D-768`） |
| **★★本書はリリースを実行しない** | **★リリースそのもの**〔コード署名の取得 ／ AV ベンダーへの申請 ／ GitHub Releases への公開 ／ 宣伝記事の執筆〕**は開発者の作業であり、Claude は代われない。⇒ 本書は「リリースできる状態かを確かめ、Claude 側の残りを片付ける」ところまでである** |

---

## 0. ★★このセッションが要る理由（**開発者向けの説明。貼る対象ではない**）

**★設計卓は武装している**（`scripts/design-desk-arm.sh` が `internal/` / `web/src/` / `cmd/` / `migrations/` / `.devcontainer/` / `.github/` を作業ツリーから外す）**。あわせて `git fetch` が `deny` にある。⇒ 設計卓は `main` の新しい状態を構造的に見られない**（`D-697` / `M-118` / `M-119`）**。**

**★★その結果、「実物を見ないと決まらないこと」が溜まる。⇒ 2026-09-07 時点で 5 件ある**（下の §2。**★当初 6 件としていたが、うち 1 件**〔ベースラインの測り直し〕**は改善レーン C3 が持つ。⇒ 設計卓の当初の割付が誤っていた＝`D-768`**）**。**

**★★本セッションは、その溜まりを境界で 1 度だけ流すためのものである。⇒ 常設のレーンにしない。**

---

## 1. ★★守らせる条件 3 つ（**外すと 2 つめの設計卓になる**）

| # | 条件 | 理由 |
|---|---|---|
| **★★1** | **`docs/process/parallel-board.md` へ書かない** | **★ボードの書き手は設計卓だけである。⇒ 2 人が既存行を編集するとマージが競合する**（`E-121` / `D-247`）**。★出力は完了報告 1 本とし、設計卓が畳む**（`D-382` と同じ形） |
| **★★2** | **番号を自採番しない**（CHANGE / マイグレ / 裁定 / 計測点 / 保留） | `D-293`。**⇒ 要るときは設計卓へ請求する** |
| **★★3** | **期間を限る**（**境界の 1 手番**） | **★居座ると設計卓と役割が重なる。⇒ 溜まりを流し終えたら閉じる** |

---

## 2. ★★このセッションでしかできない実測 6 件（**2026-09-07 時点。★着手時に増減を確かめること**）

| # | 何を | 出所 |
|---|---|---|
| 1 | **`dependency-pin-ops` §2 の台帳を実物で埋める** | `docs/process/dependency-pin-ops.md` §2・§6-3 |
| 2 | **`B3`（データ鮮度）を現物で 1 度確かめる** | `M32-overview` §2.4 |
| 3 | **`code-facts.md` を再生成する** | **★設計卓では `generate-code-facts.sh` が exit 2 で止まる**（`CLAUDE.md` §8） |
| ~~4~~ | ~~`check-md-emphasis.sh` のベースラインを測り直す~~ | **★★【2026-09-07 移動＝`D-768`】改善レーン C3 が持つ**——**同レーンも非武装であり、測り直せる。⇒ 本書へ置く理由が無かった。★設計卓の当初の割付が誤っていた** |
| 5 | **`README.txt` を必須成果物と定めている 2 か所の逐語を取る** | `M32-01` §0.3 の表の 2 と 3（`.github/workflows/nightly-crossbuild.yml` ／ `cmd/tacpendium/main.go`） |
| 6 | **`docs/human-notes/future-notes/combmgr-features.txt` が `main` に在るかを確かめる** | `D-656` は削除済みと述べるが、設計卓ツリーには残っている（`D-765`） |
| **★★7** | **★★本番ビルド**（タグなし）**が CI で走っているかを確かめる** | **`D-769`。⇒ `M28-02c` 以降、E2E は debug ビルドを回す。★本番ビルドが壊れたことを検出する経路が別に要る。★設計卓は `.github/` を読めない** |

---

## 3. ★★同じ手番に乗せるもの 2 群

| 群 | 内容 |
|---|---|
| **A. 公開ゲート** | **`B1`**（コード署名）／ **`B2`**（AV 誤検知対策）／ **`B3`**（データ鮮度）／ **`B5`**（宣伝ブログ）。**★どれも開発者の作業であり、Claude は代われない。⇒ 本セッションは状態を確かめて一覧にするところまで** |
| **B. プロセス見直し** | **`process-review-cadence-undefined`**（`followup` §BT）**。★契機を「フェーズ境界」に置いたため、まさにここに落ちる。⇒ 陳腐化したルール・フック・検査の掃除** |

---

## 4. ★★投入プロンプト本体

---- ここから ----

あなたはこのリポジトリの「境界セッション」です。フェーズ4 の締めと
フェーズ5 のキックオフの間で、1 手番だけ動きます。

## あなたの立ち位置

- **武装しないこと。** `scripts/design-desk-arm.sh` を実行しないでください。
  実装ディレクトリが作業ツリーに在る状態で作業します。
- あなたは設計卓ではありません。**`docs/process/parallel-board.md` を編集しないでください。**
  書き手は設計卓だけです。2 人が編集するとマージが競合します。
- **番号を自採番しないでください**（CHANGE / マイグレーション連番 / 裁定 D-番号 /
  計測点 M-番号 / 保留 P-番号）。要るときは報告に「請求」と書いてください。
- 出力は **完了報告 1 本**です。設計卓がそれを読んで畳みます。

## 最初に読むもの

1. `CLAUDE.md`（プロジェクト指針。特に §8 の常設検査の表と §9 停止規律）
2. `docs/process/parallel-board.md`（**読むだけ。書かない**）
   — 冒頭の「次の親が最初にすること」と「採番の現在地」
3. `docs/handover/followup-backlog.md` §BT / §BU
4. `docs/process/dependency-pin-ops.md`（全文）
5. `docs/instructions/M32-overview.md` §2.4 / §3.3

## やること（**★段ごとにコミットを切る**）

### 段 1 — 実測 7 件

1. **`dependency-pin-ops` §2 の台帳を実物で埋める。**
   `web/package.json` の `pnpm.overrides` と `go.mod` の `replace` / `toolchain` を
   実際に読み、表の「固定した版」と「状態」を埋めてください。
   **推測で埋めないこと。** 同書 §4 の段 1（固定を外して 1 度動かす）まで
   やれるものはやり、やれないものは理由を書いてください。
2. **`B3`（データ鮮度）の確認。** `games.current_data_version` が `2026.08.03.01` で
   あること、`characters` が 31 件であることを実物で確かめてください。
3. **`code-facts.md` を再生成する。** `bash scripts/generate-code-facts.sh` を回し、
   差分を報告してください（設計卓では exit 2 で止まるため、ここでしかできません）。
4. **`check-md-emphasis.sh` のベースラインを測り直す。**
   ただし **案 A（下の段 3）を適用した後**に測ってください。順序が逆だと意味がありません。
5. **`README.txt` を必須成果物と定めている 2 か所の逐語を取る。**
   `.github/workflows/nightly-crossbuild.yml` と `cmd/tacpendium/main.go` の
   該当箇所を引用してください。
6. **`docs/human-notes/future-notes/combmgr-features.txt` が在るかを確かめる。**
   在るなら、削除済みという記録（`D-656`）と食い違います。報告してください。
7. **本番ビルド（タグなし）が CI で走っているかを確かめる。**
   `.github/workflows/` を読み、`go build ./...`（タグなし）に相当する job が
   あるかを報告してください。**M28-02c 以降、E2E は debug ビルドを回します。**
   **本番ビルドが壊れたことを検出する経路が無いなら、それも報告してください。**

### 段 2 — 公開ゲートの状態を一覧にする

`docs/human-notes/combmgr-prerelease-checklist.md` の `A` 群と `B` 群について、
**いま何が済んでいて何が残っているか**を、実物を見て一覧にしてください。
**★`B1` / `B2` / `B5` は開発者の作業です。あなたは状態を確かめるだけです。**

### 段 3 — （**★★本書から外した**）

**★★改善レーン C1〜C3 は本書ではない。⇒ `docs/handover/session-prompts/20260907-improvement-lane-c1-c3.md` が持ち、走行中 3 本のマージ直後に回す**（`D-768`）**。**

**★本セッションが立つ頃には、既に入っているはずである。⇒ 入っていなければ、入っていないことを報告してください。★あなたが代わりに入れないでください**（**別の手番の射程です**）**。**

### 段 4 — プロセス見直し（**陳腐化の掃除**）

`followup-backlog.md` §BT の `process-review-cadence-undefined` が本段です。

**候補を挙げるところまでをやり、消す判断は開発者へ回してください**（`D-196`）。

見る先:
- `CLAUDE.md`（失効した記述。過去に「M0 / M1 の全指示書」「フェーズ1完了」が
  失効したまま残った実例があります）
- `.claude/settings.json` の `allow` / `deny`（使われていない規則）
- `.claude/hooks/`（役目を終えたフック）
- `.claude/commands/`（`old/` へ退避すべきもの）
- `scripts/`（呼ばれていない検査、ベースラインが実態と乖離した検査）
- `docs/` 直下（`bash scripts/check-doc-inventory.sh` の出力を起点に）

**★★「同じ型の計測点が 3 回以上出ている規約」も見てください。**
`docs/process/parallel-board.md` §6 の計測点表に `M-126` / `M-133` / `M-140` / `M-141` が
あり、いずれも「読めない対象について、読めたかのように書く」型です。
**4 件出ています。⇒ 規約側で防げるかを検討し、案を報告してください。**

## 完了条件

- 段 1 の 7 件すべてに結果がある（**測れなかったものは、測れなかった理由を書く**）
- 段 2 の一覧がある
- **段 3 は無い。⇒ 改善レーン C1〜C3 が既に入っていることだけを確かめ、入っていなければ報告する**
- **検査がすべて緑**（**★`bash scripts/check-artifact-integrity.sh` を最初に回す**）
- 段 4 の候補一覧がある（**消していない。候補と根拠だけ**）
- **完了報告を `docs/progress/` へ 1 本**書き、`docs/progress/progress-log.md` へ索引行を 1 行足す
- **設計卓への請求事項**（番号の払い出し、判断を仰ぐ事項）を報告の末尾にまとめる

## やらないこと

- `docs/process/parallel-board.md` の編集
- 番号の自採番
- ファイルの削除（**候補を出すところまで。消すのは開発者**）
- 設計書本体（`docs/design/`）の編集
- `M31` / `M32` などのサブの実装

---- ここまで ----

---

## 5. 参照

| パス | 何を見るか |
|------|-----------|
| `docs/process/parallel-board.md` | `P-53`（本セッションの起票）／ `D-767`（決着）／ `D-761`（案 A）／ `D-764` / `D-766` |
| `docs/handover/followup-backlog.md` | §BT（3 件）／ §BU（board §9 の版番号衝突） |
| `docs/process/dependency-pin-ops.md` | §2（台帳）／ §3.2（ツールチェインの契機）／ §6-3 |
| `docs/process/remote-ops.md` | §6（push の境界） |

---

*以上、投入プロンプト **v1.2.0**。* **★★本セッションの値打ちは「設計卓が見られないものを見る」ことだけにある。⇒ 見た結果を設計卓へ返すところまでが役目であり、判断まで持ち込まない。** **★★居座らせないこと**——**常設のレーンになると、ボードの書き手が 2 人になる。**
