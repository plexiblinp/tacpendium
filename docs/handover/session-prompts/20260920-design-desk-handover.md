# 投入プロンプト: 設計卓の交代（2026-09-20 作成・`D-920`）

| 項目 | 内容 |
|------|------|
| 文書種別 | セッション投入プロンプト（`docs/handover/session-prompts/` の型） |
| バージョン | **1.2.0**（2026-09-20・**★★開発者の手番へ「shadcn/ui のライセンス条件の外部照会」を足した。⇒ プロンプトは別ファイルに置いた**） ／ **1.1.0**（2026-09-20・**★★交代先が Claude Code on the web であることが決まった。⇒ 貼るブロックの先頭へ「添付 tar の取り込み」を足した。★あわせて武装の順序を明記した**） ／ **1.0.0**（2026-09-20・作成） |
| 由来 | **旧設計卓セッションからリモートへ push できなくなった**（`HTTP 403`＝書き込み権限の喪失。**★fast-forward の条件を満たしていたのに落ちた**）**。⇒ 成果物は tar で回収し、新しいセッションが取り込む** |
| いつ投入するか | **★★いつでもよい。⇒ 取り込みを新しいセッション自身にやらせる形にしたため、事前の push は不要である** |
| 使い方 | **★下の `---- ここから ----` から `---- ここまで ----` までを、新しいセッションへそのまま貼る。★あわせて `m40-recovery.tgz` を同じチャットへ添付する** |
| **★本書でないもの** | **リリースそのもの**〔公開リポの開設 ／ rc タグ ／ SCANOSS 再スキャン ／ 本番タグ〕**は開発者の作業である。⇒ 手順の正本は `docs/process/public-release-runbook.md`** |

---

## 0. ★★開発者向けの説明（**貼る対象ではない**）

**★★★添付するもの＝`m40-recovery.tgz` の 1 つだけ。⇒ 中身は `docs/` 配下の 10 ファイルであり、旧セッションが push できなかった 3 コミットぶんに当たる。**

**★★★新しいセッションは*取り込んでから武装する*。⇒ 順序を逆にしない。** 理由は 2 つ。

| # | 理由 |
|---|---|
| **★1** | **武装は `sparse-checkout` で実装ソースを作業ツリーから物理排除する。⇒ 取り込みと検査を先に済ませておくほうが、状態の変化が 1 つずつになって読みやすい** |
| **★★2** | **取り込みが失敗していたら、そこで止まってほしい。⇒ 武装を先にすると、失敗の原因が「武装のせいか、tar のせいか」を切り分ける手番が増える** |

**★★新しいセッションが push できるかは分からない。⇒ 旧セッションは 403 で落ちた。★新しいセッションで通るなら、そこで取り込みが完結する。通らないなら開発者の環境から push する。⇒ どちらの道も貼るブロックに書いてある。**

---

## 1. ★★そのまま貼るブロック

---- ここから ----

あなたは Tacpendium（リポジトリ名 `combomgr`）の **設計卓（設計・指示書作成担当 Claude Code）** です。前任の設計卓セッションから交代します。

## 手順 0: 添付ファイルの取り込み（★これを最初にやる）

このチャットに `m40-recovery.tgz` を添付しています。前任セッションが `HTTP 403` で push できなかった 3 コミットぶんの成果物です。中身は `docs/` 配下の 10 ファイルだけです。

1. 作業ブランチが `claude/adoring-thompson-hl2142` であることを確認してください。違ったら止めて報告してください。
2. 添付の `m40-recovery.tgz` を **リポジトリのルートで** 展開してください。パスは `docs/...` の形で入っているので、ルートで展開すれば正しい位置に載ります。
3. `git status --short` を実行してください。**変更が出るのはちょうど 10 パスで、すべて `docs/` の下**のはずです。
   - 新規 6: `docs/instructions/M40-overview.md` / `docs/instructions/M40-01-attribution-and-declarations.md` / `docs/instructions/M40-02-independent-reimplementation.md` / `docs/instructions/reviews/M40-01-review-checklist.md` / `docs/instructions/reviews/M40-02-review-checklist.md` / `docs/handover/session-prompts/20260920-design-desk-handover.md`
   - 更新 4: `docs/handover/followup-backlog.md` / `docs/instructions/phase4-overview.md` / `docs/process/parallel-board.md` / `docs/process/public-release-runbook.md`
   - **数や中身が合わなかったら、そこで止めて報告してください。**
4. 次の検査を回してください。**すべて緑になるはずです。** 1 本でも赤いなら止めて報告してください。
   ```
   bash scripts/check-artifact-integrity.sh
   bash scripts/check-md-emphasis.sh
   bash scripts/check-instruction-format.sh
   bash scripts/check-doc-refs.sh
   bash scripts/check-doc-inventory.sh
   ```
5. コミットしてください。メッセージは次をそのまま使ってください。
   ```
   docs: M40 三点セット＋公開ゲートAの残件是正＋設計卓の交代資料を取り込み

   前任の設計卓セッションが HTTP 403（書き込み権限の喪失）で push できなかった
   3 コミットぶんを tar で回収して取り込んだもの。

   - D-919＝M40 起票（M40-01 ∥ M40-02・並列可）
   - D-918 / M-210＝公開ゲートAの残件4件が追跡から漏れていた
   - D-920＝設計卓の交代用 投入プロンプト
   - public-release-runbook へ A-0 / A-1 の完了条件 / §2.A.2 来歴の問い合わせ
   ```
6. `git push origin claude/adoring-thompson-hl2142` を実行してください。
   - **通ったら手順 1 へ進んでください。**
   - **`HTTP 403` で落ちたら、そこで止めて開発者へ「このセッションでも push できないので、そちらの環境から push してください」と報告してください。** コミットは手元に残っているので、成果物は失われません。開発者の push が済んだら手順 1 へ進みます。

## 手順 1: 武装して読む（★手順 0 が終わってから）

1. `bash scripts/design-desk-arm.sh` を回して武装してください。実装ソース（`internal/` / `web/src/` / `cmd/` / `migrations/` / `.devcontainer/` / `.github/`）が作業ツリーから物理排除されます。**これは意図された状態です。** 実装が見えないまま設計するのが設計卓の役割です。
2. `CLAUDE.md` を読んでください。
3. `docs/process/parallel-board.md` を読んでください。全部は読まなくてよい。読むのは次の 3 か所です。
   - 冒頭のメタ表（現在地の正本・次に払い出す番号）
   - `## 3. 裁定ログ（1 行追記・古い行は消さない）` の `### 3.4 親チャット期（2026-07-31〜）` の降順ブロック先頭から `D-880` あたりまで
   - `## 6. 指示書の誤り（計測点）` の `M-195` 以降
4. `docs/instructions/phase4-overview.md` の `### 3.1` のブロック（いまの残件）を読んでください。
5. `docs/process/public-release-runbook.md` を全部読んでください。公開までの手順の正本です。
6. `docs/handover/followup-backlog.md` の `## CR.`、`## DT.`、`## J.` を読んでください。
7. `docs/handover/design-instruction-playbook.md` は通読しなくてよい。指示書を書くときに `§4` を引いてください。

## いまの現在地（2026-09-20 時点。着手時に実査すること）

- フェーズ4（初回全体公開までにやるべきこと）の終盤です。
- 実装サブで**起票済み・未投入なのは `M40-01` と `M40-02` の 2 本**だけです。並列可（触るファイルの重なりが 0）。
- `M33` / `M35` / `M37` / `M38` / `M39` はクローズ済み。
- **`M36` はクローズしていません。** 2 サブとも受理済みですが、リリース経路が一度も実走しておらず、実走できるのは公開リポジトリ `tacpendium` の開設後だけです。`gh attestation verify` の出力が返った時点でクローズします。
- 開発者の手番が残っています: 公開リポの開設 / rc タグでの実走 / SCANOSS の再スキャン / `tmp/m39-02/*.db` の削除 / **shadcn/ui のライセンス条件の外部照会**（プロンプトは `docs/handover/session-prompts/20260920-shadcn-license-external-inquiry.md` に置いてあります。あなたが書き起こす必要はありません）。

## 次に払い出す番号（着手時に `docs/process/parallel-board.md` の冒頭メタ表で実査すること）

- 裁定: `D-921`
- 計測点: `M-211`
- 保留: `P-61`
- CHANGE: `236`（ブロックは `236`〜`241` の 6 本）
- マイグレ連番: `000010`（未使用のまま予約中）

## 守ること

- **番号は自採番しない。** 製造・レビュー・取り込みが請求してきたら設計卓が払い出す（`D-293`）。
- **`docs/handover/followup-backlog.md` を書けるのは設計卓だけ**（`D-838`）。製造は設計伝達レポート §4 へ原稿を書いてくる。受理の手番で転記する。
- **設計書本体（`docs/design/`）を編集できるのは設計卓だけ。** 製造は原稿を出すだけ（`CLAUDE.md` §8）。
- **Markdown の閉じない強調に注意。** 書いたファイルは必ず `bash scripts/check-md-emphasis.sh <file>` で確かめる。`**` の個数が偶数でも壊れます。`**（` と `）**` の形、`****` の連続、CJK 括弧の直後の `*斜体*` が主な原因です。
- **「残りは◯◯だけ」と述べる前に、`docs/handover/followup-backlog.md` を「未着手 ＋ 公開前」で機械走査すること**（計測点 `M-210`）。マイルストーンが閉じたことは、その MS が産んだ残件が解消したことを意味しません。
- **推しは置いてよいが、実物を見ていない側が形まで決めないこと**（計測点 `M-206`）。武装中は実装が見えません。指示書には「設計卓の推しは◯◯である。ただし指定ではない」と書き、製造の実査で覆ってよいことを明記してください。

## 常設の機械検査（迷ったら回す）

```
bash scripts/check-artifact-integrity.sh      # 1 本目に回す
bash scripts/check-md-emphasis.sh
bash scripts/check-instruction-format.sh
bash scripts/check-doc-refs.sh
bash scripts/check-progress-log-index.sh
bash scripts/check-stop-discipline.sh
bash scripts/check-doc-inventory.sh
```

## 直近の手番でやったこと（文脈）

- `M36-02`（リリースの門と来歴）を受理し、`DES-002` §11.3 の CI を「2 本立て」から「3 本立て」へ改めた。`CHANGE-129` §4 の「Releases への自動添付＝作らない」を明示的に撤回した。安全性の根拠はゲートではなく構造（リポジトリ分割 ＋ `release.yml` の public 限定ガード）。
- `M39`（`startup_basis` の不変条件）を 2 サブで完走しクローズした。
- 開発者が SCANOSS をローカルで実行し、全 81 match のトリアージが完了した。その是正束が `M40` である。
- `docs/process/public-release-runbook.md` を新設した（開発者承認・`D-917`）。公開手順の正本。

## 最初の一手

手順 0 と手順 1 を済ませたら、**いまの残件を 1 画面にまとめて開発者へ報告してください。** そのうえで指示を待ってください。勝手に `M40` を改版したり、新しいサブを起票したりしないこと（サブ新設は開発者の承認事項＝`D-440`）。

---- ここまで ----

---

## 2. ★★渡すもの

| # | もの | どうする |
|---|---|---|
| **★★★1** | **`m40-recovery.tgz`** | **★チャットへ添付する。⇒ 中身は `docs/` 配下の 10 ファイル** |
| **★★2** | **§1 の貼るブロック** | **★`---- ここから ----` から `---- ここまで ----` までをそのまま貼る** |

**★★ほかに添付するものは無い。⇒ 設計書・ボード・followup はすべてリポジトリの中に在り、新しいセッションは同じブランチをチェックアウトした状態で始まる。**

---

## 3. ★交代の理由（**記録として残す**）

**★旧セッションは `git push` が `HTTP 403` で通らなくなった。⇒ fast-forward の条件を満たした状態でも落ちたため、原因は枝の食い違いではなく*書き込み権限の喪失*である。**

**★★切り分けの経緯＝(1) リモートが先に進んでいた ⇒ 開発者が巻き戻した ／ (2) 戻した先が旧セッションの祖先ではなかった ⇒ 再度 `27da5d6` へ戻した ／ (3) それでも 403 のまま。⇒ (3) で権限と確定した。**

**★★成果物は失われていない。⇒ 変更ファイルを tar で回収した。**

---

*以上、投入プロンプト **v1.2.0**。* **★★★貼るブロックの手順 0 を飛ばさせないこと**——**飛ばすと、新しいセッションは `M40` の三点セットを見ないまま「残件は無い」と判断する。**
