# 投入プロンプト: 公開支援担当への再開通知（2026-09-21 作成・`D-926`）

| 項目 | 内容 |
|------|------|
| 文書種別 | セッション投入プロンプト（`docs/handover/session-prompts/` の型） |
| バージョン | **1.0.0**（2026-09-21・作成） |
| 由来 | **2026-09-21 の公開前準備セッション報告** ＝「公開前ブロッカー 1 件（`restore-real-user-premise-before-public-release`）は設計卓の反映待ち」で止まっていた |
| 宛先 | **公開支援担当**（公開リポジトリの開設・同期・リリース実走を担う Claude Code セッション） |
| いつ投入するか | **★★設計卓の枝を `main` へマージした後**（§0 の前提を参照） |
| 使い方 | **★下の `---- ここから ----` から `---- ここまで ----` までを、公開支援担当のセッションへそのまま貼る** |
| **★本書でないもの** | **リリース手順の正本ではない。⇒ 正本は `docs/process/public-release-runbook.md`** |

---

## 0. ★★★開発者向けの説明（**貼る対象ではない**）

**★★★貼る前に 1 つだけ満たすこと＝設計卓の枝を `main` へマージしておく。**

**⇒ 公開支援担当は `main` を見る。★設計卓の反映が届いていない状態で貼ると、受け手は「ブロッカーがまだ在る」と読んで止まる。**

**★★貼るブロックの中に*受け手自身が照合できる 1 行*を入れてある。⇒ `public-release-runbook` が `1.4.0` 以降かどうかを見る形にした。★マージが済んでいなければ受け手がその場で気づき、開発者へ返す。**

### 0.1 ★★なぜ「再開してよい」と一言で言わないか

**★★★止まっていた理由は 1 件だけではなかった。⇒ 設計卓の反映**（4 面の復元）**に加えて、*設計卓の `A-0` 走査にバグが在った*ことが分かった。**

**⇒ 受け手は「11 件」という数字を前提に作業していた可能性がある。★真値は 14 件であった。⇒ その訂正を渡さないと、受け手の手元の理解が古いまま進む。**

### 0.2 ★公開支援担当は `roles-and-routing.md` に未登録である

**★2026-09-21 実査＝同書の登場人物は 開発者 / 相談役（全体・個別）/ 設計担当 / 調査担当 / 製造担当 / レビュー担当 / 臨時レビュー担当 / startkit担当 であり、公開支援担当の行が無い。**

**⇒ 本書はその穴を埋めるものではない。★役割として定着するなら、別の手番で `roles-and-routing.md` へ登録すること。⇒ followup へは登録していない**（公開を止めないため。**開発者の判断で起こす**）**。**

---

## 1. ★★そのまま貼るブロック

---- ここから ----

公開前ブロッカーの解消が終わりました。公開作業を再開してください。

## 0. 最初に確かめること（★1 コマンドだけ）

リポジトリのルートで次を実行してください。

```
grep -c '1.4.0' docs/process/public-release-runbook.md
```

- **1 以上が返ったら**、設計卓の反映が届いています。⇒ 先へ進んでください。
- **0 が返ったら**、まだ届いていません。**そこで止めて開発者へ「設計卓の枝が main へマージされていないようです」と報告してください。**

## 1. 何が変わったか

### 1.1 ★公開前ブロッカーは 0 件になりました

`docs/handover/followup-backlog.md` の `restore-real-user-premise-before-public-release` を **完了**にしました。全体公開の直前という発動トリガが来たため、2026-08-09 に置かれていた「実ユーザーは存在しない」前提の緩和を、4 面すべてで解除しています。

| # | 解除した場所 |
|---|---|
| 1 | `docs/handover/design-instruction-playbook.md` の `### 4.17 破壊的マイグレの定型観点(M17-6 反省、v1.13.0 で追加)` 冒頭の発動条件 |
| 2 | 同ファイルの `#### 4.17.3 down は忠実復元 ＋ グラウンドトゥルース検証` にあった「全体公開までは no-op down を許容する」 |
| 3 | `docs/handover/retrospective-log.md` の `### パターン G: 配布済みソフトウェアの影響範囲を dev 内に閉じて考える(2026-08-09 に §7.2 から昇格)` の発動条件 |
| 4 | `docs/handover/retrospective-digest.md` の破壊的マイグレの項 ＋ 冒頭の 2026-08-09 追記 |

緩和の記述は削除せず、引用として残してあります（歴史記録のため）。`design-instruction-playbook.md` は `2.54.0` になりました。

**以後、破壊的マイグレは「実ユーザーの DB が存在する」「自分が作っていない行が存在する」を既定の前提として扱ってください。忠実復元 down ＋ グラウンドトゥルース検証が既定へ戻っています。**

### 1.2 ★★★設計卓が渡していた `A-0` の件数が間違っていました

設計卓は `A-0`（`followup-backlog.md` の「未着手 ＋ 公開前」走査）の結果を **11 件**と 3 度報告していましたが、**真値は 14 件**でした。

原因は、走査が**5 列の表の行しか見ていなかった**ことです。`docs/handover/followup-backlog.md` の表は一様ではなく、実測で 5 列 870 行 / 3 列 60 行 / 6 列 48 行 / 4 列 39 行 / 2 列 14 行 / 7 列 5 行 / 10 列 2 行でした。

取りこぼしていた 3 行のうち 2 行は開発者が独立に見つけたものです。**手元に「11 件」という数字が残っていたら捨ててください。**

走査をやり直す場合は、**列数を決め打ちしないでください**。状態セルの位置は列数で変わります（3 列なら 3 番目、4 列以上なら 4 番目）。行全体に「未着手」と「公開前」が出るかで見るほうが安全です（取りこぼすより過検出のほうがましです）。**走査したら、見つけた件数より先に「何行を見たか」を出してください。**

### 1.3 ★現在の `A-0` 結果

修正した走査での実測は、母数 1,027 行に対して **6 件**です。

| スラッグ | 扱い |
|---|---|
| `process-review-cadence-undefined` | 公開を止めない |
| `toolchain-upgrade-trigger-undefined` | 方針の文書化のみ。公開を止めない |
| `trashlist-detail-link-on-nonexistent-column` | フェーズ5 送り（割付済み） |
| `sf6-major-update-after-release` | 公開後の運用 |
| `b02-burnout-punish-finder` | フェーズ5（開発者裁定済み） |
| `progress-docs-carry-private-repo-shas` | `docs/process/public-release-runbook.md` の 見出し「### 2.A.3 ★★★ `docs/progress/**` に載る非公開リポの SHA と日付」 に掛け金あり |

**確定ブロッカーは 0 件です。**

### 1.4 ★台帳を 11 行同期しました

あなたの報告の「完了済みだが台帳が古いもの」は、**1 件を除いて**該当行を更新しました。

**除いた 1 件は `ci-crossbuild-e2e-not-on-pr` です。** この行の主張は「**PR では** 1 度も走らない」であり、Nightly の実走が緑だったことは「Nightly が壊れていない」ことしか示しません。別の事実なので畳みませんでした。公開ブロッカーではないので、公開後の CI 設計として残ります。

実査で 2 件、報告と違う結果が出ました。

- **`README.md` の「ライセンス: 未定」は既に解消済み**でした。同ファイルの `## ライセンス` は三層構成を宣言しています。⇒ 畳みました。
- **SF6 データ鮮度確認は「台帳が古い」のではなく「台帳に行が無い」でした。**⇒ `docs/handover/followup-backlog.md` の 見出し「## DW. 公開前準備セッション（2026-09-21 開発者報告）で出た項目（2026-09-21 追加・ `D-926` ）」 へ新規登録しました。

### 1.5 ★手順書が 2 か所増えています

`docs/process/public-release-runbook.md` は `1.4.0` 以降です。前回あなたが見た版から次が変わりました。

- `### 2.A 事前（**★公開リポの開設を待たなくてよい**）` の `A-0` 行に、走査方法の注意を書き足しました（1.2 の内容）。
- `### 2.C ★★★実走確認（**★本番タグの前に必ず 1 度通すこと**）` に **`C-4`** を足しました。長い日本語ラベル「コンボ開始時の◯◯ゲージ残量」をスマートフォン幅とエクスポート画像 / PDF で目視する項目です。**公開ブロッカーではありませんが、rc を触る手番なら追加費用がほぼ無いので相乗りさせました。** 静的解析では崩れなしを確認済みなので、見て崩れていなければそのまま閉じてください。

## 2. 次にやること

`docs/process/public-release-runbook.md` の `## 2. ★★★初回公開の手順` に従ってください。`2.A`（`A-0` / `A-1` / `A-2`）は完了しています。

**次は `### 2.B 公開リポジトリの開設` の `B-1`＝公開リポジトリを public にすることです。**

**★★public にした直後、同じ手番で続けて 3 つやってください。** 忘れても何も赤くなりません。

1. `protect-main` ruleset が `main` へ強制適用されたことを確認する（private の間は現在のプラン上 ruleset が強制されない旨の警告が出ていたため）
2. Private vulnerability reporting を有効化する
3. Dependabot alerts を有効化する

そのあと `### 2.C ★★★実走確認（**★本番タグの前に必ず 1 度通すこと**）` へ進んでください。`C-3` の `gh attestation verify` が 3 アーカイブとも通った時点で `M36` がクローズします。

## 3. 守ること

- **リリース成果物は CI から出します。手元のマシンから配布物をアップロードしないでください。**
- **公開リポジトリ側で直接編集しないでください。** 生成は一方向であり、次の同期で巻き戻ります。
- **`2.C` を飛ばして本番タグを打たないでください。** 一度も通していない経路で本番を打つことになります。
- 詰まったら `docs/process/public-release-runbook.md` の `## 4. ★★詰まったときの見どころ` と `## 5. ★★★やってはいけないこと` を見てください。

## 4. 番号が要るとき

CHANGE 番号・裁定番号・計測点・マイグレ連番は**自分で採番しないでください**。設計卓へ請求してください。現時点で次に払い出せるのは CHANGE `237`（残り `237`〜`241` の 5 本）です。

`docs/handover/followup-backlog.md` は設計卓だけが編集します。**あなたが見つけた残件は、報告に書いて設計卓へ渡してください。**

---- ここまで ----

---

## 2. ★★渡すもの

| # | もの | どうする |
|---|---|---|
| **★★★1** | **§1 の貼るブロック** | **★`---- ここから ----` から `---- ここまで ----` までをそのまま貼る** |

**★★ほかに添付するものは無い。⇒ 手順書も台帳も設計書もリポジトリの中に在り、受け手は同じ `main` を見る。**

---

## 3. ★書き方の規約への適合（**`docs/handover/session-prompts/README.md` の `## 1.1 書き方の規約（★★「そのまま貼る」ブロックは自己完結させる）`**）

| # | 規約 | 本書での扱い |
|---|---|---|
| **(i)** | ブロックの中で「上記ファイル」「前述の」「先ほどの」と書かない | **★守った。⇒ ブロック内はすべてファイルパスで指している** |
| **(ii)** | 外の節を指すならファイルパスと*実際の見出し文字列*を書く。見出しは `grep` で確かめる | **★守った。⇒ `public-release-runbook` / `playbook` / `retrospective-log` / `followup-backlog` の見出しはすべて 2026-09-21 に `grep` で実査した逐語である** |
| **(iii)** | 書き終えたらブロックだけを読み直して「これだけで始められるか」を確かめる | **★守った。⇒ そのうえで冒頭に*自己照合できる 1 コマンド*を置いた**（`grep -c '1.4.0'`）**。★前提が崩れていたら受け手がその場で止まる** |

---

*以上、投入プロンプト **v1.0.0**。* **★★★本書の要点は「再開してよい」だけではない。⇒ *設計卓が渡していた数字が間違っていた*ことを併せて渡すことである。★それを渡さないと、受け手の手元に「11 件」という古い理解が残る。**
