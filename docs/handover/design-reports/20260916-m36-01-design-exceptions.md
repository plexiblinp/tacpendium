# M36-01 設計伝達レポート(例外レポート)

| 項目 | 内容 |
|------|------|
| 対象 | **親チャット(設計卓)** |
| 発信 | 製造担当 Claude Code ／ 2026-09-16 |
| 指示書 | `docs/instructions/M36-01-release-archive-and-checksums.md` **v1.2.0**(**★開発者が製造チャットへ直接提示した実体。⇒ リポジトリの `docs/instructions/` 配下は v1.1.0 のままである**＝§4-7) |
| 実装コミット | ブランチ `claude/zealous-pascal-0crwcd`。**実装と報告の本体は `1cfbc07` → `13ae77a` の 8 コミット**。**★以後は本レポート自身を直すコミットが加わる。⇒ 現在値は `git rev-list --count 1cfbc07..HEAD` で数えること**(**固定の数を書くと、それを直したコミットが増えて即座に古くなる**＝§7-4 の当のもの)。**push 済み ⇒ 設計卓が HEAD で読める**。PR [#228](https://github.com/plexiblinp/combomgr/pull/228)(**★以後の push で更新される**) |
| 関連 | 完了報告 `docs/progress/M36-01-completion-report.md` ／ レビュー `docs/progress/m36-01-review.md` |
| **レビュー集計** | **高 4 / 中 5 / 低 4 ＝ 全 13 件・採用 13 / 不採用 0・「高」の不採用 0 件・再レビュー往復 0 回(上限 2)** |
| マイグレ消費 | **0 本**(`migrations/` の差分 0 バイト) |
| 新規依存 | **0 件**(`go.mod` / `web/package.json` の差分 0 バイト) |

本書は **①独自確定仕様 ②契約違反の独自判断 ③製造判断 ④残課題** に絞る。指示書どおりの部分は割愛する。

**★★§2(契約違反の独自判断)は「なし」である。** 受理/却下の裁定を要する項目は無い。

**★★★最重要は §1-1 と §4-2 である。** **§1-1 は `DES-002` §11.2 の「組む仕組みは存在しない」を失効させる as-built であり、§4-2 は「組めることは配布してよいことではない」という、いまどこにも門が無い状態の申し送りである。**

---

## §1 製造が独自に確定した実装仕様(DES 反映が要るもの)

### 1-1 ★★★配布物を組む経路が実在するようになった(`DES-002` §11.2)

**★着手前は存在しなかった。** 段 1 の否定形確認で、`goreleaser` / `softprops` / `actions/create-release` / `ncipollo` / `gh release` / `on: push: tags:` / `on: release:` / 成果物向けの `sha256sum` / 成果物を固める `zip`・`tar -c` の **9 種を走査して全 0 件**であった。両ワークフローとも `permissions: contents: read` であり、**リリースを作る権限すら無い**。

#### 入口(1 コマンド)

```
make release-archives        # Makefile:101。build-all を前提に持つ
bash scripts/build-release-archives.sh   # dist/ の既存バイナリから組む(CI 用。M36-02 が呼ぶ)
```

#### 出力先

**`dist/release/`。** `.gitignore` の `/dist/` が既にカバーしており、**成果物はコミットしない**(実測＝`git check-ignore` が zip と `.sha256` の両方に当たる)。

#### アーカイブ 3 本の中身(**実測の全量**)

```
tacpendium-windows-amd64.zip     (zipinfo -1)
  tacpendium-windows-amd64.exe
  README.txt
  LICENSE
  DATA-LICENSE.md
  NOTICE
  REUSE.toml
  LICENSES/
  LICENSES/AGPL-3.0-or-later.txt
  LICENSES/CC-BY-SA-4.0.txt
  LICENSES/MIT.txt
  manual/
  manual/tacpendium-readme.html
  manual/images/
  manual/images/.gitkeep

  ※ macOS / Linux の tar.gz も同じ中身（実行ファイル名だけが
     tacpendium-darwin-arm64 / tacpendium-linux-amd64 に変わる）。
     ★エントリは "./" 付きで格納される（検査側で正規化している）。
```

**★★★ライセンス関係ファイル 7 件は 2026-09-16 の開発者判断で同梱した**（§3-1 の #4）。**⇒ `DES-002` §11.2 の zip 構成図は 3 点のままであり、これは*新しい*反映事項である**（§6 (a)）。

**★`LICENSES/` はディレクトリ名ごと保っている。⇒ basename で平坦化しない**——**`NOTICE:4-6` が「ライセンス本文は `LICENSE` … と `LICENSES/` 配下 … を参照すること」と書いており、平坦化すると同梱した `NOTICE` の参照先がアーカイブ内に存在しなくなるためである。**

**★`manual/tacpendium-readme.html` は 3 本とも 102,234 バイト**(正本と同一)。**★`docs/usermanual/dist-readme.txt` は 3 本とも入っていない**——説明書の中身ではなく `README.txt` の正本であり、生成物のほうを同梱しているためである。

#### チェックサムの形式(**採った形と理由**)

**`<アーカイブ名>.sha256` を各アーカイブの隣に 1 本ずつ置く**(§7-2 の設計卓案どおり)。中身は `sha256sum` 互換の 1 行で、**パスは basename で書く**。**⇒ そのディレクトリで `sha256sum -c` がそのまま通る。利用者に追加の道具を要求しない。**

#### ★★★構成の正本は `scripts/release-targets.sh` 1 か所である

**アーカイブ名 / 中の実行ファイル名 / 形式 / `dist/` 内のソース**を 1 つの表に持ち、**組み立て(`build-release-archives.sh`)と検査(`check-release-archive.sh`)の両方が `source` する**。**★`Makefile` には名前を書いていない**——書くと 3 か所目になり、必ずドリフトする(`E-76`)。

**⇒ macOS の名前を寄せるときも、この 1 行を直せばどちらへでも寄せられる**(§4-1)。

#### ★★★持たせなかったもの(**誤読を防ぐため明記する**)

| 持たせなかったもの | どこの射程か |
|---|---|
| **CI からリリースを打つ経路** | 射程 6＝`M36-02`。**新規 3 本に `gh release` / `actions/*` / タグ契機は 1 件も無い** |
| **artifact attestation(来歴の署名)** | 射程 3＝`M36-02`。**★SHA-256 が示すのは「壊れていないこと」だけであり「このバイナリがこのコミットから作られたこと」ではない**。この区別は `build-release-archives.sh` の「限界」節と `README.txt` の利用者向け文面の両方に書いてある |
| **SBOM ／ タグ署名** | 射程 4〜5＝`M36-02` |
| **nightly の改変** | **`.github/` の差分は 0 バイト**(`CHANGE-129` の境界を保つ) |
| **リリースを実際に打つこと** | **開発者の手番**(指示書 §3-8) |

### 1-2 ★★`README.txt` の同梱物記述が 2 点 → 3 点になった(`DES-002` §11.2 の `README.txt` 生成経路)

**★直したのは正本 `docs/usermanual/dist-readme.txt` であり、生成物 `README.txt` ではない**(`CHANGE-191` §2.4)。`bash scripts/check-dist-readme.sh --write` で生成し、引数なしで照合して EXIT=0 を確認した。

| 箇所 | 着手前(逐語) | 着手後 |
|---|---|---|
| `dist-readme.txt:154` | **「※ 本 zip に入っているのは実行ファイルと本 README.txt だけです。」** | **★2 段階で直した。** (1) 「実行ファイル・本 README.txt・操作説明書(manual/ フォルダ)の 3 点です。上記のライセンス関係のファイルは含まれていません。」 → (2) **2026-09-16 のライセンス同梱(§4-5)で後半が真逆になったため**、「ライセンスは三層構成です…関係するファイルは本アーカイブに同梱しています。見るものは次のとおりです。／ アプリ本体のライセンス本文 ... `LICENSE` ／ ゲームデータについての説明 ... `DATA-LICENSE.md` ／ 告知(帰属・撤退ポリシー) ... `NOTICE` ／ どのファイルがどの層かの正本 ... `REUSE.toml` ／ 各ライセンスの本文 ... `LICENSES/` フォルダ」 |
| `:7` ／ `:172-173` | 説明書の所在が `docs/usermanual/...`(公開リポジトリ) | **同梱された `manual/tacpendium-readme.html`** |
| **新設** | — | **「■ ダウンロードしたファイルの破損確認(SHA-256)」**。Windows は `Get-FileHash -Algorithm SHA256`、macOS は `shasum -a 256 -c`、Linux は `sha256sum -c`。**★末尾に「これは『ファイルが壊れていないこと』の確認であり『配布元が正しいこと』の証明ではない」と明記した** |

**★指示書 §0.4 と `followup` が引く「本 zip の中身は 2 点だけ」は要約である。⇒ 実ファイルの逐語は上表のとおりであった**(実測)。

### 1-3 ★破壊確認を `check-artifact-integrity.sh` の `--self-test` 規約へ載せた

**★指示書 §5-3 は「中身を 1 つ抜くと検査が落ちること」を求めているが、*どう置くか*は書いていない。⇒ 製造が形を決めた。**

`scripts/check-artifact-integrity.sh` は **`scripts/check-*.sh` が `--self-test` を持ち、それが通ることを機械強制している**(`SELFTEST_ALLOW` 以外は必須)。**⇒ そこへ載せたことで、本サブの破壊確認は今回限りのデモではなく以後すべての手番で回る。**

**対照 10 件(陰性 5 / 陽性 5)**: 完全な zip ／ 完全な tar.gz ／ **`manual/` に余分 2 本を足しても緑**(件数で固定していない証拠) ／ **`.gitkeep` すら無い真に空の `images/` でも緑**(版ゲート 3b が `.gitkeep` に依存していない証拠) ／ チェックサム一致 ／ exe・`README.txt`・説明書本文・`images/` ディレクトリをそれぞれ抜くと赤 ／ チェックサム不一致で赤。

**★★合格の名乗りには文言の規約がある**——`check-artifact-integrity.sh` は **`自己検査: 合格`** という文字列を拾う。**⇒ 対照が 9 件すべて通っていても、この文言が無いと同検査は赤になる**(実際に 1 回赤になった。§7-2)。

---

## §2 契約・設計に反する独自判断(★受理/却下の裁定が要る)

**なし。**

> **★念のため 4 点を明示する**(いずれも契約どおりであり、違反ではない)。
> - **アーカイブ名は `DES-002` §11.2 のまま**(`tacpendium-macos-arm64.tar.gz` を含む)。
> - **中の実行ファイル名は `Makefile` の出力のまま**(`CHANGE-191` の `exe` 名と一致。macOS は `tacpendium-darwin-arm64`)。**⇒ どちらへも揃えていない**(`D-887` の裁定どおり。§3-1)。
> - **`docs/usermanual/dist-readme.txt` の編集は `D-887` で射程内と裁定済み**である。**⇒ チェックリスト v1.1.0 の `P-4`(「`docs/usermanual/` を編集したら射程外」)は v1.2.0 で是正されており、その是正版に従った。**
> - **`.github/` ／ `docs/design/` ／ `docs/handover/followup-backlog.md` の差分はいずれも 0 バイト**(§5)。

---

## §3 製造の判断

### 3-1 開発者へ確認して確定した点

| # | 事項 | 確定内容(`D-887`・2026-09-15) |
|---|---|---|
| 1 | **`README.txt` の文面修正は射程内か** | **射程内。直すこと。⇒ チェックリスト `P-4` が広すぎたのが誤りで、`A-6` が正しい。** `docs/usermanual/` は一様ではなくファイル単位で射程が決まる＝`dist-readme.txt` は編集する ／ `tacpendium-readme.html` は読むだけ ／ `images/` は触らない ／ クイックスタートは触らない(`M32-03` の面) |
| 2 | **macOS の名前の非対称をどうするか** | **揃えない。設計書どおりに組む。⇒ アーカイブは `macos`、中のバイナリは `darwin` のまま。★黙って通さず、実測で報告して CHANGE 請求へ回すこと** |
| 4 | **ライセンス関係ファイルを同梱するか**(2026-09-16) | **★★★同梱する。範囲は全 7 件・パス保持。⇒ 指示書 §2.4 が判断を開発者へ上げる形にしていたものが決着した。★製造は判断が下りるまで 1 件も入れていない**(§4-5) |
| 3 | **コミットの範囲**(2026-09-16) | **コミットしないのは組み立ての成果物だけ**(zip / tar.gz / `.sha256`)。**⇒ 作業一式は通常どおりコミットする。★着手時に前者を*作業一式への禁止*と読み違えた**(§7-3) |

### 3-2 推測で進めた点(実装判断)

指示書 §7-3 は「組み立ての置き場(`Makefile` か スクリプトか)は**実装判断でよい**。採った形と理由が報告に在れば足りる」と定めている。

| # | 判断 | 理由 |
|---|---|---|
| 1 | **`Makefile` にターゲット 1 本、実体はスクリプト** | (a) 既存のクロスビルド(`build-all`)と同じ入口に揃うので、利用者が探す場所が 1 か所で済む (b) 実体をスクリプトにすると CI から `bash scripts/...` で直接呼べ、`make` の前提(`ensure-web-deps` 等)を巻き込まずに済む(E-4／`M36-02` が呼ぶ) |
| 2 | **`manual/` はディレクトリごとコピーし、`dist-readme.txt` 1 本だけを除く** | 危険 §4.1b が「ファイルを 1 つずつ列挙しない」と定めている。**★除く 1 本は説明書の中身ではなく `README.txt` の正本であり、同梱すると生成物と二重になる** |
| 3 | **検査は「あるべきものが在るか」だけを見る。`images/` はディレクトリの存在のみ** | 版ゲート 3b ／ `P-1`。**★ファイル数もファイル名の一覧も見ていない。⇒ `M32-03` のクイックスタート着地で赤にならない**(陰性対照 C が毎回証明する) |
| 4 | **ライセンス同梱を `RELEASE_EXTRA_FILES` の「選べる形」にした** | §2.4-2。**着手時の既定は空＝入れない。⇒ 判断が下りた 2026-09-16 に 7 件を足した**(§3-1 の #4)。**★1 行足せば同梱でき、検査も自動で付いてくる**(レビュー 中-5 で検査側にも足した)。**★パスは保持する**(当初は basename 平坦化だったが、`NOTICE` の「`LICENSES/` 配下」が壊れるため同梱時に改めた) |
| 5 | **組み立ての最後に検査を必ず呼ぶ** | **「組めた」が常に「中身を検査した」を含む形にするため**(指示書 §4.1)。あわせて**組む前に `check-dist-readme.sh` を回し**、ドリフトした `README.txt` が 1 度も配布物へ入らないようにした |
| 6 | **tar は決定論に寄せた**(`--sort=name --owner=0 --group=0 --numeric-owner --mtime=@0` ＋ `gzip -n`) | 環境差を減らすため。**★ただしバイト単位の再現性は主張していない**(zip は Info-ZIP の格納順・属性に依存が残る)。「限界」節に明記した |

---

## §4 設計担当が未把握の残課題・申し送り

### ★★★4-1 CHANGE 請求 2 件(**自採番していない**＝`D-293`)

§6 にたたき台を置いた。**(a) `DES-002` §11.2 の「組む仕組みは存在しない」の失効 ／ (b) macOS の名前の非対称。**

### ★★★4-2 「組める」ことは「配布してよい」ことではない(**いま門がどこにも無い**)

**★現時点で `make release-archives` を打つと、`<img>` 27 箇所がすべてリンク切れの説明書が入ったアーカイブ 3 本が、検査全緑で出来上がる**(`docs/usermanual/images/` は実測 0 枚)。

**★★版ゲート 3b がこれを不合格にしないのは正しい**(撮影は開発者の手番であり本サブの後)**。⇒ しかしその帰結として、「撮影前の成果物を配布してはならない」を止める仕組みが、機械にも手順にも 1 つも無い状態になった。**

| 面 | 状態 |
|---|---|
| 本サブが保証する | **コマンド 1 本で組めること** |
| 本サブが保証しない | **組んだものを配布してよいこと。⇒ 画像 0 枚でも緑である** |
| 門が無い場所 | **実際にリリースを打つ手番**(開発者。指示書 §3-8 で射程外) |

**⇒ 門を置くかは設計卓の判断。★`M36-02` が CI からリリースを打つ形を作るなら、そこが自然な置き場になる。**

### ★★4-3 SHA-256 の検証手順が、検証したい時点では読めない(**`M36-02` への従属**)

**★手順は `README.txt` に足した**(`E-3` は満たしている)**。⇒ しかしその `README.txt` はアーカイブの中にある。★ダウンロード直後に照合したい利用者は、まだ展開していない。**

**⇒ 同じ 3 行が*リリースページ側*にも要る。★`M36-02` がリリース本文を組むときに載せること。**

### ★4-4 `README.txt` 末尾の「リポジトリを扱う人向けの注記」が、本サブで初めて配布物へ載る

`CHANGE-191` が新設した生成注記(「本ファイルは `docs/usermanual/dist-readme.txt` から生成されています。修正はそちらを直し `--write` で作り直してください」)は、**当時 `README.txt` がリポジトリ内にしか無かったため宛先と読者が一致していた**。**⇒ 本サブが同梱したことで、内部の生成手順書が利用者の手元へ届く形になった。**

**★実害は小さい**(「利用者の方は読み飛ばしてください」と断ってある)**。★落とす場合の注意＝落とすと `check-dist-readme.sh` の照合が壊れる**(同スクリプトは「正本の全文 ＋ 生成注記」を生成物と定義している)**。⇒ 素直な形は「組み立て時に末尾を落とす」ではなく「注記の文面を配布物に載ってよいものにする」ほうである。** **★`CHANGE-191` を起こした面の判断。**

### ★★★4-5 【決着】ライセンス関係ファイルを同梱した(**2026-09-16 開発者判断**)

**★指示書 §2.4 は「既定は入れない。判断は開発者へ上げる」と定めていた。⇒ 判断が下りたので実施した。★実装が判断を下してはいない。**

| 項目 | 内容 |
|---|---|
| **判断** | **同梱する。範囲は全 7 件・パス保持** |
| **入れたもの** | `LICENSE` ／ `DATA-LICENSE.md` ／ `NOTICE` ／ `REUSE.toml` ／ `LICENSES/AGPL-3.0-or-later.txt` ／ `LICENSES/CC-BY-SA-4.0.txt` ／ `LICENSES/MIT.txt` |
| **根拠(7 件が 1 セットである理由)** | **`NOTICE:4-6` の逐語＝「本ファイルは Tacpendium の配布物に付随する告知である。ライセンス本文は `LICENSE`(アプリ本体＝AGPL-3.0-or-later)と `LICENSES/` 配下、パス単位の割当は `REUSE.toml` を参照すること。データ層の説明は `DATA-LICENSE.md` にある。」⇒ `NOTICE` 自身が「配布物に付随する」と名乗り、4 つすべてを名指ししている。★部分的に入れると、同梱した `NOTICE` の参照先が欠ける** |
| **層 B の本文が要る実体** | **`embed_migrations.go` の `//go:embed migrations/*.sql` により、`CC-BY-SA-4.0` のゲームデータのマイグレ 20 本(`*_data_*.sql`)が配布バイナリへ実際に埋め込まれる** |
| **パスを保つ理由** | **`NOTICE` が「`LICENSES/` 配下」と書いているため。⇒ basename で平坦化すると `LICENSES/MIT.txt` が `MIT.txt` として直下に落ち、参照先が消える** |
| **承知のうえの重複** | **`LICENSE` と `LICENSES/AGPL-3.0-or-later.txt` は md5 一致で完全同一**(34,020 B × 2)。**⇒ 34KB は 12MB のアーカイブの 0.3% であり、リポジトリと同じ形を保つ利益のほうが大きい**(開発者判断) |

**★★★設計卓への依頼が 2 つある。**

1. **`DES-002` §11.2 の zip 構成図は 3 点のままである。⇒ 7 件が増えたことの反映が要る**(§6 (a) に足した)。
2. **★未確定のまま配布物へ載るものが 2 つある**——**`NOTICE` の連絡先は伏せ字であり、記載の公開リポジトリ URL `https://github.com/plexiblinp/tacpendium` は未開設である**(`NOTICE` 内に明記)。**⇒ 同梱したことで、この 2 つが利用者の手元へ届くようになった。★公開の手番までに差し替えが要る。**

**★★減らすときの注意(次の担当へ)**——**先に `NOTICE` の参照先が欠けないかを見ること。⇒ 「重複しているから `LICENSE` を落とす」のような一見素直な削減が、同梱した `NOTICE` を壊す。★法務の断定は開発者の手番である。⇒ 実装で減らさない。**

### ★★4-6 `followup-backlog.md` の状態更新の依頼(**設計卓の手番**＝`D-838`)

**★製造は同ファイルを 1 文字も編集していない。** 下記は新規登録ではなく**既存行の更新**である。

| スラッグ | 更新後の状態 | 根拠 |
|---|---|---|
| **`release-packaging-does-not-exist`** | **射程 (1) zip / tar.gz の組み立て と (2) SHA-256 は満たされた。⇒ (3)〜(6)(attestation / SBOM / タグ署名 / CI からのリリース)は未着手のままであり、行は閉じない** | 本レポート §1-1 ／ `Makefile:101` ／ `scripts/build-release-archives.sh` |
| **`usermanual-no-reachable-path`** | **同梱が実現した。⇒ 閉じられる見込み。★実際にリリースが打たれるまで待つなら開いたままでよい** | §1-1 の中身の全量(3 本とも `manual/` を含む) |
| **`readme-exe-name-ci-comment-unsynced`** | **失効の根拠が 1 つ増えた。⇒ `nightly-crossbuild.yml:105-107` のコメントは `exe` 名だけでなく「中身は 2 点」という*点数*の面でも古くなった**(配布物は 3 点になった) | `DES-002` §11.2 ／ §1-2 |
| **`release-zip-missing-license-files`** | **★★解消した。⇒ 2026-09-16 の開発者判断で 7 件すべてを同梱した**(`LICENSE` / `DATA-LICENSE.md` / `NOTICE` / `REUSE.toml` ＋ `LICENSES/` 配下 3 本)。**★あわせて根拠の文面も古くなっている**(同行は nightly の逐語「中身 = tacpendium.exe + README.txt」を引用しているが、配布物は実行ファイル ＋ `README.txt` ＋ `manual/` ＋ ライセンス 7 件になった) | §4-5 ／ 本レポート §1-1 の中身の全量 |

### ★4-7 指示書・チェックリスト・設計書の記載の食い違い 3 件(**いずれも勝手に直していない**)

| # | 事項 | 実測 |
|---|---|---|
| 1 | **指示書 §4.1d の表が `README.txt:14` と書く** | **実測は `README.txt:19`**(`dist-readme.txt:19` と同じ行)。**★内容(どの名前がどこに在るか)は正しい。行番号だけがずれている** |
| 2 | **チェックリスト §8 対照値「ライセンス関係ファイル 4」** | **ルート直下だけを数えれば 4 で正しい。⇒ ただし `B-3` は「`REUSE.toml` が指すもの」と書いており、素直に従うと 7 になる。★対照値に数え方を添えると取り違えが消える** |
| 3 | **`DES-002` §11.2 ／ `CHANGE-192` §2.1 の「22 枚の PNG」** | **実測は `<img>` 27 箇所**(`<figure>` も 27)。**★章数 22 と図数 27 の混同。⇒ 本サブの判断(同梱の形)には影響しないが、data URI 案を落とした理由の数字である** |

**★あわせて**: **リポジトリの `docs/instructions/M36-01-*.md` と `reviews/M36-01-review-checklist.md` は v1.1.0 のままである**。適用した v1.2.0 は開発者が製造チャットへ直接提示した実体であり、**リポジトリへの反映は設計卓の手番**である。**★Phase B のレビュアーへは v1.2.0 の実体パスを明示して渡した**(渡さないと、是正前の広い `P-4` で `dist-readme.txt` の編集が射程外と誤判定される)。

### ★4-8 `CLAUDE.md` §8 の常設機械検査表への掲載(**製造が編集する面ではない**)

**★新設の `scripts/check-release-archive.sh` は同表に載っていない。** **★機械的な強制はすでに効いている**(`check-artifact-integrity.sh` が `--self-test` を拾う。実測＝`OK check-release-archive.sh の自己検査が通る`)。**⇒ 表へ載せるかは「人が迷ったときに回す先」として案内するかの判断である。**

**★先例＝`check-derived-docs.sh` は表に載っていなかったため回すきっかけが失われ、派生資料が長期間止まって設計卓が資料を使わなくなる実害が出た**(`CLAUDE.md` §8 の注記)。

### ★4-9 レビュー低指摘の繰越

**なし。全 13 件を採用した**(不採用 0 件)。**⇒ 「高」の不採用も 0 件であり、Phase C 安全弁のエスカレーションは発生していない。**

### ★4-10 停止時記録(`§J` 行の原稿)

**なし。** 未解消のまま停止した項目は 0 件であり、再レビューの往復上限(2 回)にも達していない(往復 0 回)。

**★本レポート §4-2 〜 §4-8 は「製造が解けずに止まった」ものではなく、はじめから他者の手番として設計された事項である。⇒ §J ではなく上記の請求・申し送りとして上げる。**

---

## §5 参考(触れていない=不変の証跡)

- `git diff 1cfbc07 -- .github/` → **0 行**(nightly / pr-checks とも 1 バイトも変えていない＝`CHANGE-129` の境界)
- `git diff 1cfbc07 -- docs/design/` → **0 行**(設計書本体は未編集＝`CLAUDE.md` §8)
- `git diff 1cfbc07 -- docs/handover/followup-backlog.md` → **0 行**(`D-838`)
- `docs/usermanual/tacpendium-readme.html` ／ `docs/usermanual/images/` → `git status` に現れない(`M32-03` の面)
- `migrations/` ／ `go.mod` ／ `go.sum` ／ `web/package.json` → 差分 0 バイト
- `internal/` ／ `cmd/` ／ `web/src/` → 差分 0 バイト(Go / TS のコードを 1 行も変えていない)
- `go test ./...` EXIT=0(`--- FAIL` 0 件・`ok` 60) ／ `pnpm test` EXIT=0(234 ファイル・2950 テスト)
- `make e2e` EXIT=0 ／ **352 passed**——**`M37-07`(2026-09-14)の実測と一致**(`E-125`＝「緑だが 1 件も走っていない」の排除)
- 常設検査 9 本すべて EXIT=0(`check-artifact-integrity.sh` を 1 本目に回した)

---

## §6 CHANGE 起票のたたき台(設計担当向けチェックリスト)

**★番号は起票時に `change-number-registry` で採番すること。製造は自採番していない**(`D-293`)。**★マイグレ消費 0 本・スキーマ変更 0。**

### (a) `DES-002` §11.2 — 「組む仕組みは存在しない」が失効した

| 項目 | 内容 |
|---|---|
| 対象 | `docs/design/02-architecture.md` §11.2 の注記ブロック(`:1506` / `:1508`) |
| **失効した逐語** | **「★★★zip / tar.gz を組む仕組みは存在しない(2026-09-12 開発者回答で確定)。⇒ 本節が定める配布物の構成は、いまだ一度も実現されていない。★設計と as-built の乖離である。」** ／ 同 **「★`Makefile` にも zip ターゲットは無い見込みである(開発者回答)。」** |
| 反映する as-built | 本レポート §1-1 の全文(入口 / 出力先 / 中身の全量 / チェックサムの形式 / 正本の所在 / 持たせなかったもの) |
| **★★zip 構成図の更新も要る** | **同節の構成図は「`exe` ＋ `README.txt` ＋ `manual/`」の 3 点のままである。⇒ 2026-09-16 の開発者判断で**`LICENSE` ／ `DATA-LICENSE.md` ／ `NOTICE` ／ `REUSE.toml` ／ `LICENSES/` 配下 3 本**の 7 件が加わった**(§4-5)。**★`LICENSES/` はディレクトリ名ごと保つ。⇒ 図でもそう描くこと** |
| **失効しない部分** | **「★CI は組まない」は真のままである**(nightly は 1 バイトも変わっていない)。**★「CI からリリースを打つ」は依然として未実装である**(射程 6＝`M36-02`) |

### (b) `DES-002` §11.2 — macOS の名前の非対称(**判断が要る**)

| 項目 | 内容 |
|---|---|
| 事象 | アーカイブ名は **`tacpendium-macos-arm64.tar.gz`**(§11.2)、中のバイナリと `README.txt` の記載は **`tacpendium-darwin-arm64`**(`Makefile:74` ／ `README.txt:19`) |
| **数** | **`macos` と書いてあるのは設計書の 1 か所だけであり、実装と配布文書の 2 か所は `darwin` で揃っている**(実測) |
| 原因 | **命名規則が 2 つ並んでいる。** `Makefile` は 3 つとも GOOS 名で出す(`windows` / `darwin` / `linux`)。**`windows` と `linux` は利用者向けの語とたまたま一致して隠れ、macOS だけが露出した** |
| 選択肢 | **(a)** 設計書のアーカイブ名を `darwin` へ寄せる ／ **(b)** `Makefile` の出力名を `macos` へ寄せる(**★`CHANGE-191` で固定された面であり、`cmd/tacpendium/windows_gui_link_test.go` や nightly の `Verify artifacts` にも波及する**) ／ **(c)** 非対称のまま明文化する |
| 実装側の備え | **`scripts/release-targets.sh` の表を 1 行直せば、どちらへでも寄せられる**(アーカイブ名と中のバイナリ名が同じ行に並んでいる) |

---

## §7 教訓(retrospective 行き)

> **★親は本節を読まなくてよい。** 宛先は `retrospective-log` へのバッチ反映であり、実施者は設計担当である(反映担当は 2026-08-11 に廃止)。

### 7-1 ★★★検査を回した時点と報告を書いた時点がずれると、「緑」が偽になる

**★実際に起きたこと**——`check-progress-log-index.sh` を回して **EXIT=0 / 112 件**を得て、その値を完了報告 §7.3 の表へ書いた。**⇒ その後に完了報告そのものを作成したことで、同検査は「対応する `progress-log` の追記が無い」を検出して赤(EXIT=1)へ変わった。製造はそれに気づかず、赤い検査を緑と書いた表を残した。**

**★★さらに完了条件 10 の判定を「満たす」とし、根拠欄に「Phase D で実施」と*未来形*で書いた。⇒ `D-510` とまったく同型である。**

**★★★機構化の案**——**完了報告そのものを入力に取る検査**(`check-progress-log-index.sh` ／ `check-completion-report-md-emphasis.sh` ／ `check-doc-inventory.sh`)**は、完了報告を書き終えた後にもう一度回すこと。⇒ 製造 CLI の Phase D へ「報告を書いた後に回し直す」を明記するのが安い。**

**★人が読む以外に見つける経路が無い**——`go test` も lint も型検査も文書を見ない。**本件を検出したのは、レビュアーが報告を読むだけで済ませず自分で検査を回したからである。**

### 7-2 ★★破壊確認は `--self-test` 規約へ載せると常設になる(**ただし文言まで合わせる必要がある**)

**★`check-artifact-integrity.sh` は `scripts/check-*.sh` が `--self-test` を持ち、それが通ることを機械強制している。⇒ 破壊確認をここへ載せると、一過性のデモではなく以後すべての手番で回る。**

**★★ただし合格の名乗りに文言の規約がある**(`自己検査: 合格` という文字列を拾う)**。⇒ 対照 9 件がすべて通っていたのに、同検査は赤を返した。**

**★★★これは同検査の存在理由そのものが働いた実例である**——**「他の検査が緑でも、その緑が信用できるとは限らない」。⇒ 新しい `check-*.sh` を足す手番では、`--self-test` を書いただけでは足りない。**

### 7-3 ★★★禁止の範囲を広く取り違えると、成果物の中に失効記述が生まれる

**★実際に起きたこと**——開発者指示「本ブランチに対するコミットはしないでください」を**作業一式への禁止**と読んだ。**⇒ 実際の意図は「組み立ての成果物(zip / tar.gz / `.sha256`)をコミットしない」であり、それらは `.gitignore` の `/dist/` で既にカバーされていた。**

**★★被害は「コミットしなかったこと」ではない。⇒ 完了報告・`progress-log`・レビュー報告の 3 か所へ「本サブは 1 度もコミットしていない」と*書いた*ことである。★コミットした瞬間に、3 つとも失効記述になった。**

**★★★一般形＝禁止の範囲が読み取れないときは、広く取る前に確かめること。⇒ 広く取るのは「安全側」に見えるが、その前提を*文書へ書き込む*と安全ではなくなる。** **★とくに `progress-log` は横断インデックスであり、次のサブと設計卓が前提としてそのままコピーする**(`CLAUDE.md` §8)**。⇒ 誤った断定は「済んだこと」として下流へ複製される。**

**★機構化は難しい**(自然言語の指示の範囲を機械で判定できない)**。⇒ せめて「禁止を前提にした記述を報告へ書くときは、その範囲を逐語で引用する」だけでも、読み違いが後から見える。**

### 7-4 ★★★「着手前 → 着手後」の表は、**「着手後」欄そのものが後から失効する**

**★本サブで 2 度踏んだ。**

| # | 実例 |
|---|---|
| **1** | **`dist-readme.txt:154` の同梱物の記述が 2 段階で変わった。** 着手前＝「※ 本 zip に入っているのは実行ファイルと本 README.txt だけです。」→ **(a)**「…操作説明書(manual/ フォルダ)の 3 点です。**上記のライセンス関係のファイルは含まれていません。**」→ **(b)** ライセンス同梱(§4-5)で**後半が真逆**になり、層ごとの案内へ。**⇒ (a) を「着手後」として書いた表が、(b) の手番で偽になった。★完了報告 §3.3 と本書 §1-2 の 2 か所で同時に起きた** |
| **2** | **本書ヘッダ表の「実装コミット」。** 作成時点(`62403f4`)は 5 本で正しかったが、**その後 3 本増えて 8 本になった。⇒ 中身は §4-5 等で追随させたのに、数だけが取り残された** |

**★★機序＝「着手後」は*その時点の観測*であるが、表の形が「確定した結論」に見える。⇒ 次の手番が同じ箇所を触っても、表を見直す動機が生まれない。** **★とくに数を書いた欄**〔件数・コミット数・行数・対照の本数〕**は、作業が伸びるたびに静かに古くなる。**

**★★`7-1` との違い**——**`7-1` は「検査の緑が後から赤になる」、本項は「報告の記述が後から偽になる」。⇒ どちらも*時点*の問題だが、前者は機械が教えてくれ、後者は人が読む以外に経路が無い**(`D-250` と同型)。

**★★★機構化の案 2 つ。**

1. **同じ箇所を 2 度直したら、表を「着手前 → 着手後」ではなく*変遷*として書き直す。** **⇒ 本サブは実際にそう直した**(完了報告 §3.3 ／ 本書 §1-2 とも「2 段階で直した」の形にした)。**★「着手後」という語が単一の終着点を含意しているのが、そもそもの罠である。**
2. **数を書いた欄に、その数を得たコマンドを添える。** **⇒ `git rev-list --count 1cfbc07..HEAD` と書いてあれば、次の担当が 1 行で数え直せる。★数だけを書くと、数え直す手段が失われる。**

**★★★本項を書いた手番で、まさにこの罠が実演された。** ヘッダ表を「5 → 8 コミット」へ直したところ、**その修正コミット自身が 9 本目になり、書いた瞬間に 8 が古くなった。** **⇒ 自分を含む数は、書けば必ず古くなる。★無限後退である。**

**⇒ 採った形＝「基準ハッシュまでの数」を固定値として書き、現在値は数え直しコマンドに委ねる。**「実装と報告の本体は `1cfbc07` → `13ae77a` の 8 コミット。以後は本レポート自身を直すコミットが加わるので `git rev-list --count` で数えること」。**★これは永久に真である**——`13ae77a` 時点の数は動かない。**★一般形＝*自己を含みうる数*は、基準点を添えて書くか、書かずにコマンドを渡すこと。**
