# M22-07 設計伝達レポート（取込の別名辞書の充実 — `G-14b` の残り半分）

| 項目 | 内容 |
|------|------|
| 対象 | **親チャット（設計卓）** |
| 発信 | 製造担当 Claude Code / 2026-08-18 |
| 指示書 | `docs/instructions/M22-07-alias-dictionary-enrichment.md` **v1.4.0** ／ チェックリスト `docs/instructions/reviews/M22-07-review-checklist.md` |
| CHANGE / マイグレ | **`CHANGE-118`**（設計卓が起票・**承認済み**＝`D-436`・**未反映**）。**製造は新規採番なし** ／ **マイグレ消費 0 本**（`ls migrations/` の最大は **`000077`** のまま＝**次に払い出す番号は `000078`** で不変） |
| 実装コミット | ブランチ `claude/m22-07-implementation-plan-ue2d6h` / `206fece`〜**`78ac328`** の **4 本**。基点 `2f472be`。**全数 push 済み** |
| 関連 | 完了報告 `docs/progress/m22-07-completion-report.md` ／ レビュー `docs/progress/m22-07-review.md`（**末尾に自動トリアージの採否記録あり**） |
| 作成条件 | **実装直後の同一セッション内で生成**（Plan Mode の実査 8 項目・レビュー往復 1 回とその取り込み・**開発者との裁定 3 件**・**裁定材料として本セッションで行った追加実査**まで含む） |

本レポートは **①製造が独自に確定した実装仕様 ②契約・設計に反する独自判断 ③製造の判断 ④設計担当が未把握の残課題** に絞る。指示書どおりの部分（§3.3 の実査を行ったこと自体・§4.9 否定形確認の実施そのもの・§5.1 の各項目）は割愛した。

**★★最重要は §2-1 と §4-1。**

- **§2-1** は **`CHANGE-118` の承認済みの前提（「★スキーマ変更 0 件。既存の `preset_aliases` へ行を増やすだけである」）が成立しなかった**という報告である。**親の受理／却下の裁定を要する。**
- **§4-1** は **投入方式の裁定材料**である。**開発者の指示により、本サブでは裁定せず本レポートへ出す形にした**（2026-08-18）。**★A / B / C に加えて、本セッションの追加実査で出てきた第 4 案 E がある。E はどの文書にも記録が無く、本レポートに載せなければ消える。**

**★§2（契約・設計に反する独自判断）は 1 件ある。** 開発者裁定を得て閉じてはいるが、**承認済み CHANGE の前提が崩れたまま `M22-07` を閉じた**という事実は設計卓の裁定事項であるため、§3 へ混ぜず §2 に挙げた。

**★本サブの結論——行を 1 行も投入していない。** `preset_aliases` は **`UNIQUE (preset_id, move_id)`**（`DES-003` §3.9 制約①「1 技 1 表記」）を持ち、SA / CA の **96 技は全数が `official_ja_move` に別名を持つ**ため、`SA1` を 2 本目の別名行として足せない。**成果は「測った・分類した・報告した」までである。** `go test ./...` **52 ok / FAIL 0** ／ `pnpm test` **167 files / 1664 tests** ／ `make e2e` **149 passed**（2 回目・flaky 0）。**レビューは往復 1 回（上限 2 回・未到達）・高 1 / 中 4 / 低 3 で、不採用 0 件。停止時記録（`followup-backlog` §J）1 件。**

---

## §1 製造が独自に確定した実装仕様（DES 反映が要るもの）

> **★本節は `CHANGE-118` §2 の a〜e が製造へ求めた成果物そのものである。** 同節は「行を増やす作法は、まだどこにも書かれていない」として 5 点の反映先を予告している。**⇒ 以下がその実測値である。**
>
> **★実測件数は設計書へ書かないこと**（`D-374` ／ `E-136`）。**正本は本サブの契約テスト 2 本**（`internal/aliasindex/m2207_baseline_test.go` ／ `internal/seedgen/classify_m2207_test.go`）。本節の数値は反映の判断材料であって、設計書へ転記する値ではない。

### 1-1 逆引きと表示は、既に SQL の経路が分かれている（**★どの文書にも書かれていない**）

`internal/repository/preset/queries.go` の実査。

| 経路 | SQL | 本番の消費者 |
|---|---|---|
| **表示**（内部表現 → 表記） | `findAliasSQL`（`queries.go:18`） | `internal/service/notation`（`resolveMoveStep`）→ `recipe_cache` |
| **逆引き**（表記 → 内部表現） | `listAliasEntriesByCharacterSQL`（`queries.go:47`） | **`internal/service/intake` のみ**（`service.go:92`。実査で本番消費者は 1 つだけ） |

**⇒ 逆引き側だけを触る改修は、表示にも `recipe_cache` にも構造的に触れない。**

**⇒ `DES-003` §3.9 または `DES-004` §5.0 へ「逆引きの読み口は表示の読み口と別であり、逆引き側の変更は表示へ波及しない」を明文化してほしい。** **★これを書かないと、次の担当が「辞書を触ると表示が変わる」と前提して、実際には不要な凍結を自分に課す。** 本サブの §4-1 の裁定にも直接効く。

### 1-2 `preset_aliases` は (preset, character) 内で「技 ⇄ 表記」の双方向一意である

制約①（`UNIQUE (preset_id, move_id)`＝1 技 1 表記・`migrations/000001_init_schema.up.sql`）と
制約②（`ux_preset_aliases_preset_char_alias`＝2 技 1 表記の禁止・`000075`）が**向きの違う一対**を成す。

**⇒ 同義語（1 技に複数の表記）を保持できない。** `DES-003` §3.9 は制約①②③を正しく列挙しているが、**「だから同義語を持てない」という帰結までは書いていない。**

**⇒ `DES-004` §5.7 へ「`preset_aliases` は 1 技 1 表記であり、別名の同義語を保持できない。同義語が要る場合は別の器が要る」を明文化してほしい。** **★これを書かないと、次に辞書を増やす担当が本サブと同じ実査をやり直す**（`CHANGE-117` §6-5 と同じ理由）。

### 1-3 (a) 機械的に導ける別名と (b) ドメイン知識が要る別名の切り分け（`CHANGE-118` §2-a）

**★「導ける」は仮説だったが、実査で成立した。**

| 種別 | 導出源 | 実測 |
|---|---|---|
| **(a) SA 番号** | **`move_code`**（`moves` の属性ではない） | `super_art` ＋ `critical_art` の **96 行全数**から導けた。導けなかった行 **0** |
| **(b) 技通称** | **導けない** | 候補は数えられる（§3-2 の規則で **747 技**）。**製造が値を持つのは 0 技** |

- **導出は既存の `saPattern`**（`internal/seedgen/generate_m2002.go:348`・`D-5`）**をそのまま使い回した。再実装していない。**
- **★状態接頭辞つきの 10 行も拾える**（`denjin_charge_sa1_` / `flame_sa1_` / `flame_sa2_` / `fuha_sa1_` / `windclad_sa2_`）。**この 10 行を落とす実装にすると、衝突組が衝突していないように見え、投入できない組を投入できると誤判定する**（`D-357` の型）。契約テストが「10 行すべてが衝突組に居ること」まで固定している。

**⇒ `DES-004` §5.7 へ「SA 番号は `move_code` から導く。技名（`name_ja`）からは推測しない」を明文化してほしい**（`D-5` の裁定はコード内注記にしか無い）。

### 1-4 値の正本の在り処は「散っている」（`CHANGE-118` §2-b）

| 層 | 値の出どころ |
|---|---|
| 層 A | **CSV の `command`** |
| 層 B | **`move_code` の構造** |
| 層 C-1（移動系） | **生成器コード内の `movementAlias` マップ** |
| 層 C-3（何も押さずに派生） | **集合は生成器コード内・値は CSV の `name_ja` ＋ `humanizeMoveCode(move_code)`** |

**⇒ `DES-004` §5.7 の 3 は生成器 2 本を名指ししているが、値がどこから来るかを書いていない。上表を書いてほしい。** **★「散っている」ことそのものを書くこと**——1 か所に見えると、次の担当が CSV だけを直して層 C-1 が古いまま残る。

### 1-5 `CHANGE-118` §2-c / §2-d は「既に書かれていた」「食い違いは無かった」

**★確かめた事実として残す**（`CHANGE-117` §6-5 と同じ扱い。**§6-4 が「穴が 1 つも無かった場合でも反映は行う」と定めている**）。

| # | 反映先 | 実査結果 |
|---|---|---|
| **c** | 衝突は照合の単位＝キャラ単位で数える | **★既に書かれていた。** `DES-004` §5.5 の 2026-08-14 追記が「照合側の衝突検査はキャラ単位で行う。プリセット単位で数えてはならない」と定めている。**実装側も既にその単位を持つ**（`aliasindex.StrictCollisions()` / `RelaxedCollisions()`）。**⇒ 追記は要らない** |
| **d** | `DES-003` §3.9 の記述と実体 | **★食い違いは無かった。** 制約①②③とも記述どおり実在する。**むしろ記述が正しかったために本サブが止まった** |

---

## §2 契約・設計に反する独自判断（★独立カテゴリ・最優先）

### 2-1 承認済み `CHANGE-118` の前提が成立せず、完了条件を未達のまま `M22-07` を閉じた

| 項目 | 内容 |
|---|---|
| **何に反したか** | **`CHANGE-118` メタ表**「**★スキーマ変更 0 件**（新テーブル・新列・制約追加なし。**★既存の `preset_aliases` へ行を増やすだけである**）」 ／ **指示書 §1.2 の完了条件 3**（増やした行が投入されている）**と 4**（増やした前後の 3 数値）|
| **なぜそう判断したか** | **制約①により行を 1 行も投入できない**（§1-2）。投入方式は **A / B / C のいずれも `DES-004` / `DES-003` の改訂を伴う設計判断**であり、**指示書 §1.3-1・§1.3-2 がスコープ外と明示している**。⇒ 製造が選ぶ事項ではない |
| **実装がどうなっているか** | **投入 0 行。** 3 数値は (a) 0 / (b) 0 / (c) 0（`TestBaseline_ThreeCountsOfThisSub`）。`preset_aliases` の差分 0・マイグレ消費 0 本 |
| **開発者裁定** | **2026-08-18・Plan Mode 提示時に選択肢 D（測定・分類・報告まで）を採ることで合意済み。** ただし、「承認済み CHANGE の前提が崩れたまま閉じてよいか」の裁定は**設計卓の手番**として残る |

**⇒ 親の仕事は次の二択である。**

1. **受理する**——`CHANGE-118` を改訂し（メタ表のスキーマ変更 0 件・§3 リスク表に「そもそも投入先が無い」を追加）、`M22-07` を「測定・分類サブ」として完了と宣言する。投入は別サブへ切り出す。
2. **差し戻す**——投入方式を先に裁定し、`M22-07` の追補として投入まで行わせる。

**★製造の見立ては 1 である。** 測定と分類は裁定に依存せず完了しており、**投入は方式が決まらないと 1 行も書けない**ため、同じサブに残しても進まない。

**★あわせて**——`CHANGE-118` §3 のリスク表 1〜6 は**いずれも「投入した後」のリスク**であり、「投入先が無い」というリスクを持っていない。改訂時に足すこと。

---

## §3 製造の判断

### 3-1 開発者へ確認して確定した点

| # | 事項 | 確定内容 | 日付 |
|---|---|---|---|
| 1 | **制約①により投入できないと判明した後の進め方** | **選択肢 D**＝測定・分類・報告までで閉じ、投入方式の裁定は設計卓へ返す | 2026-08-18 |
| 2 | **`web/node_modules` 不在で `pnpm test` / `make e2e` が回らない**（`CHANGE-118` §3-5 ／ `D-435` の既知事項） | **`pnpm install` して回す**（当座を凌ぐ＝指示書 §3.3-7） | 2026-08-18 |
| 3 | **投入方式の裁定をどこで行うか** | **本サブでは裁定しない。設計伝達レポートへ材料を出し、固まったら追補として製造が実装する** | 2026-08-18 |

### 3-2 推測で進めた点（その旨を明示した箇所）

| # | 事項 | 採った推測と理由 |
|---|---|---|
| 1 | **(b) 候補の母集合の切り方** | **指示書 §4.2 は「(b) の候補を全数列挙せよ」と言うが、何を候補と数えるかを定義していない。** ⇒ **固有名を持つ 5 category**（`special` / `super_art` / `critical_art` / `unique` / `drive_impact`）**＝ 747 技**を候補と定義した。**通称は固有名の短縮形であるため、系統的な表記しか持たない category（`normal` / `throw` / `system` / `target_combo` / `rush_variant`）を外した。** **★`rush_variant` を外したのは「元技のエイリアス ＋ `(ラッシュ)`」という派生形だからであり**（`DES-004` §3.2.1）、**元技側に通称が付けばそちらへ従う。** **⇒ この切り方が妥当かは設計卓の判断を仰ぐ** |
| 2 | **母集合 X の単位** | 当初は「(キャラ, SA 番号) の 68 組」で測ったが、レビュー中-2（正規表現の写し）の是正として**全キャラ × `SA1`/`SA2`/`SA3`/`CA` の 76 通り**へ変えた。**当該キャラが持たない SA 番号も引く**——持たない番号も確定してはならないため、母集合としてむしろ広い。**分類の件数（68 / 53 / 15）は `internal/seedgen` 側の単独の正本にした**（件数の正本を 1 か所に置く＝`SUPP-001` §5.5.3.1） |
| 3 | **テストのファイル配置・命名** | 指示書 §9.2-1 の裁量範囲。`internal/aliasindex/m2207_baseline_test.go`（基準値）／ `internal/seedgen/classify_m2207_test.go`（分類）。**後者はレビュー低-1 を受けて `generate_m2207_test.go` から改名**した（`generate_*.go` の対と紛れないため） |
| 4 | **`markdown-it-py` の当座導入** | `check-artifact-integrity.sh` が `check-md-emphasis.sh --self-test` の依存欠落で赤になるため `pip install` した（`D-435` の「当座を凌ぐ」に沿う）。**恒久策は改善レーンの手番であり、本サブでは何も恒久化していない** |

---

## §4 設計担当が未把握の残課題・申し送り

### 4-1 ★投入方式の裁定材料（**A / B / C に加えて第 4 案 E がある**）

> **★E は本セッションの追加実査で出てきた案であり、完了報告にもレビューにも記録が無い。** 指示書 §1.3-2 が `M22-07` のスコープ外としていたため当時は検討できなかったが、**裁定の場では選択肢である。**

**測定結果（裁定の入力）**——(a) SA 番号＝**68 組**〔一意 **53** / 同一キャラ内で多義 **15**〕 ／ (b) 技通称＝**747 技・値は 0**。

| | **A** 4 枚目のプリセット | **B** 逆引き専用テーブル | **C** 制約①の撤回 | **E** 逆引きに SA 番号の段を足す |
|---|---|---|---|---|
| やること | `presets` へ 1 行 ＋ `preset_aliases` へ 53 行 | 新テーブル ＋ `listAliasEntriesByCharacterSQL` に UNION | テーブル再構築 ＋ 代表行の列 ＋ 表示規約 | `aliasindex` に第 3 段（`move_code` から SA 番号を導く） |
| スキーマ変更 | なし | **新テーブル 1**（開発者承認事項） | **再構築 ＋ 新列**（最重） | **なし** |
| マイグレ | 1 本以上 | 1 本 | 2 本以上 | **0 本** |
| 表示への影響 | **★選ぶと `SA1` が表記として出る** | **なし** | **★不定になるため規約が要る** | **なし** |
| 多義 15 組 | **落とす**（§5.5） | **落とす**（§5.5） | 落とす | **★候補として人へ返す** |
| 次のキャラ波 | **人が再投入** | **人が再投入** | 人が再投入 | **★自動**（規則のため） |
| (b) に効くか | 効く | **効く** | 効く | **★効かない** |
| `DES-004` 改訂 | §3.1 | §5.0（辞書源が 2 つ） | §5 ＋ `DES-003` §3.9 | §5.0（第 3 段。**規約は §5.0.2 に既にある**） |

**★A 案の実査**——`presets` に「選ばせない」手段が無い。

```
CREATE TABLE presets (id, user_id, code, name, base_preset_code, is_builtin)
```

`is_selectable` に相当する列は無く、`listAllPresetsSQL`（`queries.go:3`）は**絞り込みなしで全件返す**。⇒ プリセットを 1 枚足すと、**設定画面の既定プリセット選択（`PresetSwitcher`）・プリセット一覧・コピー元候補のすべてに出る。** 選ぶと**その 53 技だけ `SA1` と表示され、残りは §5.3 のフォールバックで日本語技名が出る**混在表記になる。**★「プリセット＝表記の流儀」という意味論も崩れる**——SA 番号は流儀ではない。

**★C 案の実査**——波及先は 4 つある（完了報告 §5 は 1 つ目しか書いていない）。

| 箇所 | 制約①撤回後 |
|---|---|
| `findAliasSQL`（`queries.go:18`） | **`ORDER BY` も `LIMIT` も無い** ⇒ 表示が不定になる |
| `updateAliasTextSQL`（`queries.go:147`） | `WHERE preset_id=? AND move_id=?` が**複数行を黙って更新する** |
| `listAliasDetailsSQL`（`queries.go:111`。M20-04 編集グリッド） | **行が重複し、`ref` の LEFT JOIN 側でも増殖する** |
| スキーマ | **SQLite はテーブル定義の `UNIQUE` を落とせず、テーブル再構築が要る**（`DES-003` §3.9 が「再構築は写し落とし事故を持ち込む」として明示的に避けた当のもの） |

**★E 案の中身**——`aliasindex.Build` が受け取る `model.AliasEntry` は **`MoveCode` を持っている**（`internal/model/preset.go:76`）。⇒ **既存の `saPattern` と同じ規則で `SA1 → {move_code…}` の索引をその場で組める。データも行もマイグレも要らない。**

- **SA / CA の 96 技は全数が `official_ja_move` に別名を持つ**ため、**96 技すべてがエントリとして索引に入っている**。⇒ 68 組すべてを規則で引ける。
- **多義 15 組は「複数件 → 未確定 ＋ 候補提示」へ倒れる**（`DES-004` §5.0.2 規約 1「どの段でも 1 件のときだけ確定する」）。**A / B / C が 15 組を捨てるのに対し、E は人に選ばせる。**
- **`DES-004` §5.0.2 は「照合に段を足してよい」と既に定めている**（段ごとに入力側／辞書側を書き分け、段ごとに検査を置くこと）。**新しい規約を作るのではなく、既にある規約に従う形になる。**
- **★弱点**——**(b) 技通称には効かない**（導けないため）。**逆引きの決定論の経路に段が 1 つ増える**ため、§5.0.2 規約 3 に従い**第 3 段が第 1 段・第 2 段の答えを変えないこと**を固定する必要がある（**前 2 段が 0 件のときだけ引く形にすれば構造的に変わらない**）。

**★製造の推奨**（決めるのは開発者・設計卓）——**(a) は E、(b) は B。両者は排他ではなく、解くべき問題が違う。** E を単独で先に入れれば `G-14b` の SA 番号側が閉じ、**次のキャラ波でも自動で効く**（§4.6 が警告した「人が再投入しないと新キャラだけ空になる」問題が (a) について消える）。**(b) は値も粒度も未確定であり急がない**（§4-2）。

### 4-2 (b) 技通称は「値を決める」だけでは足りない（**粒度の論点**）

疎通確認で観測した実例。guile の公式別名は **`弱ソニックブーム`** であり、利用者が書く **`ソニックブーム`** は解決しない。**かつ強度を含まないため、通称を与えても一意には定まらない**（弱／中／強／OD の 4 技に当たる）。

**⇒ (b) は「ライズ＝○○」と値を決めるだけでなく、「どの粒度の技へ結びつけるか」を決める必要がある。** 開発者へ諮る際は、この論点を併せて示すこと。

### 4-3 `M22-06`（QR）の実施要否が資料から判別できない（**M22 が閉じるかに直結**）

- `docs/progress/` に **m22-06 の完了報告もレビュー報告も無い**
- `docs/handover/design-reports/` にも **M22-06 の分が無い**（他サブは全数ある）
- 一方 **`CHANGE-117` は反映済み**（`D-433`）で「QR は実装済み・配線済みであった／穴は 1 件も無く、本番コードの差分は 0 バイトだった」と記録されている
- ボードの行は **「★投入可」のまま**

**⇒ `CHANGE-118` §6-1 は「`M22-07` は M22 の最後である。完了をもって M22 は閉じる」と書いているが、M22-06 が未実施なら閉じない。** 設計卓が検証を済ませて製造サブ自体が不要になったのか、まだ回っていないのかを確定すること。

### 4-4 `followup-backlog` の更新候補（**★製造は本表を編集しない**＝`D-382`）

| スラッグ | 何が起きるか | 根拠 | 新規/更新 |
|---|---|---|---|
| **`G-14b`** | ② 照合側は `M20-07` で完了済み。**辞書の充実は「投入方式の裁定待ち」であり、起票先未定ではなく裁定待ちへ状態が進んだ** | 本レポート §2-1・§4-1 ／ `docs/progress/m22-07-completion-report.md` §7 | **更新**（状態を「★半分完了」→「② 完了 ／ 辞書の充実は投入方式の裁定待ち（A/B/C/E）」へ） |
| **`alias-dictionary-enrichment-blocked-by-one-alias-per-move`** | `preset_aliases` の制約①により同義語を投入できず `M22-07` が停止した | `followup-backlog` **§J に製造が登録済み**（必須 5 フィールド） | **索引行のみ**（本文は §J が持つ。複製しないこと） |
| **`character-nickname-aliases-need-per-wave-human-input`** | **粒度の論点が新たに判明した**——値を決めても、どの強度の技へ結びつけるかが決まらない | 本レポート §4-2 | **更新**（粒度の論点を追記） |
| **`e2e-shared-global-resource-parallel`** | 一括実行時にだけ再現性なく flaky が出る。**本サブで 6 サブ連続の観測**（`M22-01`〜`M22-05` に続く） | 完了報告 §14（1 回目 148 passed + 1 flaky / 2 回目 149 passed） | **更新**（連続回数） |
| **（`D-435` の環境恒久策）** | `markdown-it-py` 未導入・`web/node_modules` 不在。**`markdown-it-py` は 6 回目の再発** | 完了報告 §15 の注記 | **更新**（`D-435` は「`M22-07` の後・`M23` の前」と定めており、**いまその「後」に来た**） |

### 4-5 反映の残り（設計卓の手番）

- **`change-report-118.md` が未作成**（`CHANGE-118` は承認済み・**未反映**）
- **`progress-summary` は M21 までしか無い**（`/update_progress_summary M22` が未実行）
- **ボード §2.2 と registry §1 は更新不要**（**マイグレ消費 0 本**・**新規 CHANGE 採番なし**）

---

## §5 参考（触れていない＝不変の証跡）

- `git diff 2f472be -- web/src` **空**（`DES-005` 面へ非波及）
- `git diff 2f472be -- docs/design` **空**（製造は設計書本体を編集しない＝`CLAUDE.md` §8）
- `git diff 2f472be -- internal/service/notation` **空**（契約 F-5）
- `git diff 2f472be -- internal/service/preset` **空**（契約 F-5）
- `git diff 2f472be -- migrations` **空**（連番 0 本消費・`000078` 未消費）
- `preset_aliases` の行は 1 行も追加・変更・削除していない（§2.2-6）
- 不変を主張する既存テスト: `TestRealData_StrictStageHasNoCollisions`（第 1 段の同一視 0 組）／ `TestRealData_RelaxedStageCollisionsAreOnlyCAvsSA3`（第 2 段は CA vs SA3 のみ）
- 新設した基準値テスト: `TestBaseline_Y_ExistingNotationsAllResolve`（既存表記が全数 1 件で確定）／ `TestBaseline_Y_RelaxedCollisionBaseline`（第 2 段の衝突組数を固定）
- 本サブの差分は **Go のテスト 2 本 ＋ `docs/` のみ。本番コード 0 バイト。**

---

## §6 CHANGE 起票のたたき台（設計担当向けチェックリスト）

**★新規採番は不要である。** 本サブが要求するのは **`CHANGE-118` の改訂**であり、新しい番号ではない。

| # | 対象 | 内容 |
|---|---|---|
| 1 | **`CHANGE-118` メタ表** | 「★スキーマ変更 0 件（既存の `preset_aliases` へ行を増やすだけである）」を**成立しなかった旨へ改める**（§2-1） |
| 2 | **`CHANGE-118` §3 リスク表** | 「**そもそも投入先が無い**」を足す。現行の 1〜6 はいずれも「投入した後」のリスクである |
| 3 | **`DES-004` §5.7** | §1-2（1 技 1 表記＝同義語を持てない）／ §1-3（SA 番号は `move_code` から導く）／ §1-4（値の正本は散っている） |
| 4 | **`DES-003` §3.9 または `DES-004` §5.0** | §1-1（逆引きの読み口は表示と別） |
| 5 | **`CHANGE-118` §2-c / §2-d** | **「既に書かれていた」「食い違いは無かった」を反映レポートへ書く**（§6-4 が求める形） |
| 6 | **`DES-004` §5.4 の再適用表** | **★本サブ分は載せるものが無い**（投入 0 行）。**裁定後に (a) を投入するなら新しい行が要る**——層 A の注記（`236236P (SA1)` の `(SA1)`）とは別物であり、既存行には収まらない。**★(a) と (b) は行を分けること**（`CHANGE-118` §2-f） |

**★投入方式を裁定したら、その方式について新しい CHANGE が要る。** E 案なら `DES-004` §5.0 へ第 3 段を追記する形になり、**§5.0.2（段を足すときの規約）が既にあるため、新しい規約を作る必要は無い。**

**マイグレ連番**——**本サブの消費は 0 本。** 着手時に `ls migrations/` を実査して disk 末尾が **`000077`** であることを確認し、ボード記録「次の空き＝`000078`」と一致したため再請求していない（`D-293` の条件を満たす）。**`000078` は未消費のまま次のサブへ渡る。**

---

## §7 教訓（retrospective 行き）

> **★親チャットは本節を読まなくてよい。** 宛先は `retrospective-log` へのバッチ反映であり、実施者は設計担当（反映担当は 2026-08-11 に廃止・責務は設計担当へ統合）。

1. **件数を散文・コメントにだけ書くと、検証されないまま複製される**（`E-136` の実例が本サブで発生した）。本サブは空き枠の件数と「空いた理由」をテストのヘッダコメントにだけ書き、assert していなかった。**コメントは実データと突き合わされないため、誤った単一原因の説明が 5 か所へ複製された**（完了報告・progress-log・followup §J・テスト 2 本）。**⇒ 件数は正本を 1 か所（契約テストの `want` 定数）に置き、散文へ書かない。** 機構化: **「散文に件数を書いたら、その件数を assert するテストがあるか」を自問する。**

2. **「制約が塞いでいる」と「規約が禁じている」を区別せずに書くと、次の裁定の前提が狂う。** 本サブは結論（入れてよい行は 0 行）が正しかったため、根拠の誤りが**テストでも lint でも型検査でも赤くならなかった**。**発見経路はレビュアーの独立実査だけだった。** **⇒ 「できない」と書くときは、できないのが機構によるものか規約によるものかを必ず書き分ける。** 採るべき対策が変わる（機構なら回避策があり、規約なら裁定が要る）。

3. **followup に調べた結果があっても、それを使うべきサブの起票時に突合されないと意味がない。** 制約①は **`M17-04` の実スキーマ確認として `followup-backlog` の `G-14b` 行に既に記録されていた**（「UNIQUE は `(preset_id, move_id)` のみ」）。にもかかわらず `CHANGE-118` は「行を増やすだけ」を前提に起票・承認された。**⇒ 「調べていなかった」型ではなく「調べた結果が引かれなかった」型である。** 機構化: **CHANGE 起票時に、対象テーブル・対象領域について followup に既存の実測記録が無いかを 1 回引く。**

4. **スコープ外の指定は「検討してはいけない」ではなく「このサブでは決めない」である。** 本サブの指示書 §1.3-2 が「新しい段の追加」をスコープ外としたため、**製造は E 案（規則で解く）を検討対象から外していた。** しかし裁定の場では E が最有力候補になりうる。**⇒ スコープ外で切った選択肢のうち、裁定に影響しうるものは設計伝達レポートへ書き出す。** 書かなければ、次の担当は「A / B / C しか無い」と読む。
