# M24-05 設計伝達レポート（例外レポート）

| 項目 | 内容 |
|------|------|
| 対象 | **親チャット（設計卓）** |
| 発信 | 製造担当 Claude Code / 2026-08-30 |
| 指示書 | `docs/instructions/M24-05-setup-navigation-and-link-ui.md` **v1.1.0** ／ チェックリスト **v1.0.0**（**★版ずれあり＝§2-1**） |
| CHANGE | **`CHANGE-140` v1.1.0**（設計卓が起票済み。**製造は番号を消費していない**） |
| 実装コミット | ブランチ `claude/m24-05-implementation-plan-fpa2rp` / **着手基点 `ad42c4a`** |
| 関連 | 完了報告 `docs/progress/M24-05-completion-report.md` ／ レビュー `docs/progress/m24-05-review.md`（**重大 0 / 高 4 / 中 8 / 低 5**） ／ モック `docs/progress/M24-05-mock/m24-05-link-ui.html` |
| 作成条件 | **実装直後の同一セッションで生成。** 開発者とのやり取りで確定した判断（§3-1）を含む |
| 実機確認 | **2026-08-30 に開発者が実施。2 件出た**——**§4-10**（「レシピ全文表示」ラベルが実態を表していない。`M24-03` 由来） ／ **§1-4 の訂正**（製造の説明が不正確で検証を妨げた） |

本レポートは **①独自確定仕様 ②契約違反の独自判断 ③製造判断 ④残課題** に絞る。**指示書どおりの部分は割愛する。**

**★★最重要は §4-1（編集画面の紐付けを「KA 入力による検索」へ作り替えたいという開発者の要求。本サブでは着手していない）と §2-1（チェックリスト v1.0.0 が指示書 v1.1.0 と食い違ったまま残っている）である。**

---

## §1 製造が独自に確定した実装仕様（DES 反映が要るもの）

### 1-1 ★★`SM-088` の自動命名の規則（**`DES-005` §5.9 の as-built。設計卓の暫定案とは一致しない**）

**指示書 §4.4 が「実測した規則を逐語で報告すること」と求めた項目。**

| 項目 | 実測値 |
|---|---|
| 正本 | i18n キー `setplay.adoptNameFormat`（**純粋関数ではなくロケール文字列が書式の正本である**） |
| ja | **`"{{move}} 持続{{n}}F目重ね"`** |
| en | **`"{{move}} meaty on active frame {{n}}"`** |
| `{{move}}` | 提案 steps の**単純な末尾要素**の技名（`nameJa ?? code`）。**en ロケールでも技名は日本語**（書式だけが英語） |
| `{{n}}` | 起き上がりに重なる持続フレーム番号 |
| 区切り | **半角スペース 1 個。`・` は使わない** |
| 「〇〇重ね」 | **`重ね` の 1 語のみ。`attack_type` の表示語彙〔投げ重ね / シミー / 打撃重ね〕は使っていない** |
| 適用経路 | **「自動提案からの採用」1 本のみ。** 通常の新規登録・編集には自動生成が無い |
| 上書き | **しない。** `setName` は採用ボタン押下の 1 か所だけで走る |
| モード差 | **無し。** gap（あえて重ねない）でも同じ書式 |

**⇒ `CHANGE-140` §7 確認事項 2 の暫定案 `{attack_type の表示名}・{最後の技の表示名}`（例「打撃重ね・弱P」）は実装と一致しない。実装側を正とした。**

**★通常の新規登録・編集に自動生成が無いことは仕様どおりである**——開発者の逐語が「**自動提案時に**…イチイチ決めないといけないのが良くなかった」と経路を限定しており、本サブのやり取りでも「現状のまま」と確定した。

### 1-2 ★候補 0 件のときの見せ方（**`DES-005` §5.6 項目11 の該当行が失効する**）

**同項目の逐語「候補が0件の場合は本セクション自体を非表示」は、本サブで失効した。**

as-built＝**候補 0 件でもセクションを出し、見出し「転用可能なセットプレイ候補（0 件）」と本文「候補はありません」を表示する**（`D-588`）。i18n キーは `comboDetail.setups.candidatesHeading` / `comboDetail.setups.candidatesEmpty`（**ja / en 両方に新設**）。

**★見出しキーは本サブ以前、ja / en どちらにも存在せず `defaultValue` で描かれていた。**

### 1-3 ★`SM-011` コピーの as-built（**`DES-005` §5.9 へ**）

| 論点 | as-built |
|---|---|
| 作法 | **コンボ側と同型の query 経由**＝`/combos/{parentComboId}/setups/new?copyFrom={setupId}` |
| 導線の位置 | `/setups/:setupId`（編集モード）のヘッダ |
| 親コンボの決め方 | **コピー元の先頭の親。不在なら導線を出さない**（`DES-005` §5.9 が「ほかの人の内容を見る」で定めた既存規則の再利用。`VAL-S05` が親必須のため） |
| 引き継ぐもの | **名前・説明・レシピの 3 つだけ** |
| 引き継がないもの | **成立条件**（`DES-005` §5.6「全セル未検証で開始」） |
| タイトル | `セットプレイ登録（コピー元: #N）` |

**★★1 点、設計卓が把握していないはずの挙動がある**——**コピー直後にレシピを変えずに保存すると `VAL-S04` で止まる。** コピー先の親はコピー元の先頭の親であり、そこには同一レシピのセットプレイが既に紐付いているためである（`VAL-S04` は名前を見ない）。**これは仕様どおりの挙動であり、memo の逐語「途中までレシピが同じ場合がある」とも整合する**（利用者はコピー後にレシピへ手を入れる）。**⇒ E2E に対照ケースとして固定した**（`(6) 対照: コピー後にレシピを変えずに保存すると VAL-S04 で止まる`）。**★`DES-005` §5.9 へ 1 行残すのが望ましい**——書かないと後任が「コピーが壊れている」と起票する。

### 1-4 ★`SM-052` の実体（**`CHANGE-140` §2.2 の見立てが機序と違った**）

**`CHANGE-140` §2.2 と指示書 §4.2 は `SM-052` を followup `setup-recipe-display-not-through-shared-component` と同根とし、「`RecipeText` へ通せば解ける」と見立てていた。⇒ 機序が違った。**

実体は **modifiers 要約表示の 2 重実装**（コンボ側 `StepRow` の `renderModifiersSummary` ／ セットプレイ側 `SetupRecipeEditor` のインライン）。食い違いは 5 点——flags を内部コードのまま出す／notes を本文ごと行内展開する／1 個の span に `", "` 連結する／`modifiers.type` を内部コードで出す／**型だけのステップ（ドライブラッシュ類）で、主ラベルが日本語で出しているのと同じ情報を灰バッジに内部コードで二重に出す**。**★★2026-08-30 訂正**——初版は 5 点目を「中身が空でも灰バッジが残る」と書いたが、**完全に空の修飾は画面操作では作れない**（`ModifiersEditor` が中身ゼロなら `undefined` を返す）。**開発者の実機確認で「検証できない」と指摘されて判明した。** **★再現手順**＝セットプレイ編集 → 技プルダウン → 区分「共通システム」→ 「パリィドライブラッシュ」→ 「ステップを追加」。

**⇒ `ModifiersSummary` へ共通化して解決した**（開発者の指示による）。**★`ModifiersEditor`（編集ダイアログ）は元から共有されており、割れていたのは要約表示だけだった。**

---

## §2 契約・設計に反する独自判断（**★親の裁定が要る**）

### 2-1 ★★チェックリスト v1.0.0 が指示書 v1.1.0 と食い違ったまま残っている（**設計卓の手番**）

**製造は指示書 v1.1.0 を正として作業した。** レビューも同じ突合に到達し「版ずれの解決は妥当」と判定している。**★ただしチェックリスト本体は未改版であり、次に読む人を誤らせる。**

| チェックリスト v1.0.0 の記述 | 指示書 v1.1.0 の実態 |
|---|---|
| 「§3.3 の実査 **12 件**」（1-13） | **§3.3 表は 9 行**（番号 1/2/3/6/7/8/9/11/12。4・5・10 が欠番）。**★指示書 §7.1-1 自身も「12 件」と書いており失効している** |
| 「E2E **4 ケース**」（5-1） | §5.1 は **3 ケース** |
| 「破壊確認 **5 件**」（6-1）・「破壊確認 5 が赤くなっているか」（6-2） | §5.3 は **3 件**。**破壊確認 5 は存在しない** |
| 参照先 `§3.3-4` / `-5` / `-10`（1-7 / 1-10 / 4-3） | **v1.1.0 に存在しない**（`SM-146` とともに削られた行） |
| 参照先 `§7.1-8`（§0.3-6） | **v1.1.0 に存在しない**（§7.1 は 7 → 9 で 8 が欠番） |
| 1-7 / 4-2〜4-4（`SM-146` の導線・「最後の技」の定義） | **`D-588` で射程外。本サブに該当しない** |
| 1-10（`attack_type` の表示名の実測） | **同上**（`SM-088` の暫定案に紐づく項目） |
| 5-4（「最後の技」が非技ステップのときのテスト） | **同上** |

**⇒ 依頼: チェックリストを v1.1.0 対象へ改版する。** **★あわせて `scripts/check-instruction-format.sh` に「チェックリストの『対象指示書』欄の版数と、指示書本体の版数の一致」を足すかを検討されたい**——同検査は現在**指示書内の版数一致しか見ておらず、この版ずれを緑のまま通した**。

### 2-2 ★`DES-005` §5.6 項目12（自動提案）のファイルに触れた（**指示書 §2.2 が「触らない」と定めている**）

**触ったのは `SetplaySuggestionSection.tsx` の 2 か所のみ**——(a) 名前生成のインライン式を `buildAdoptedSetupName` の呼び出しへ置換 (b) 不要になった `const target` を削除。

**★提案エンジン・条件パネル・ソート・採用／不採用・制約告知・保存経路は 1 行も変えていない。挙動は不変である。**

**触った理由**: 指示書 §5.1 が「`SM-088` の成立を固定する**純粋関数テスト**」を、§5.3 の破壊確認 3 が「自動生成を止めると純粋関数テストが赤くなる」ことを求めており、**インラインのままではどちらも書けない**（＝「既に成立している」ことが 1 つも固定されない）。**⇒ §7.4.1-6 の「触ったら報告」に従い報告する。**

**★1 点だけ挙動差がある**（レビュー 低-2）: 旧実装は `steps` が空なら例外になる形、新実装は `""` を返す。型と生成経路から空にはならないため実害は無いが、「1 文字も変えていない」ではない。

---

## §3 製造の判断

### 3-1 開発者へ確認して確定した点（**本サブのやり取り・逐語**）

| # | 問い | 開発者の回答（逐語） |
|---|---|---|
| 1 | 「既存から紐付け」相当のモーダルが 2 系統ある。どちらを消すか | **「詳細は消す。編集については、KA入力して既存セットプレイを検索して紐づけるような形にしたい。その前段階として削除するかは任せる。編集については申し送りとして設計卓へ完了時のレポートで伝達。」** |
| 2 | `SM-088` を通常登録・編集へも広げるか | **「現状のまま。仕様通りなので新規登録編集にないことは報告不要。」** |
| 3 | `SM-052` を実際に直すか（modifiers 要約の共通化） | **「共通化して直す（推奨）」** |

### 3-2 推測で進めた点（**指示書 §9.2 の委任範囲**）

- **候補 0 件のときの文言＝「候補はありません」**（趣旨が伝わることを基準に選んだ）。
- **コピー先の親＝コピー元の先頭の親**（§9.2 の暫定案どおり。かつ `DES-005` §5.9 の既存規則）。
- **E2E ヘルパの命名とシグネチャ**（`web/e2e/support/setup-link.ts`）。

### 3-3 ★`RecipeText` の `fullView` を共有トグルへ繋がなかった

**面ごとの固定値で渡した**（`SetupAccordionItem` のみ `true`、他 4 面は `false`）。理由＝`recipe-full-view-v1` は `DES-005` が「**コンボの 4 面が 1 つの値を共有する**」と定めた鍵であり（`web/CLAUDE.md` §1 台帳 #11）、**読み手を増やすには台帳と設計書の変更を伴う**。§4.2 が求めたのは表示の統一であってトグルの拡張ではない。**⇒ ブラウザストレージのキーは新設も変更もしていない。**

**★★ただし利用者に見える変化が 1 つある**（レビュー 中-2）——`SetupAccordionItem` は `fullView` 固定 `true` としたため、レシピが **`1.` `2.` の番号付きで縦へ展開される**表示になった。従来は「`>` 区切りの 1 本の文字列を折り返す」表示だった。**`DES-005` §5.6 項目10 の「全文〔複数行折返し〕表示」には反しないが、モックには含めていない面である。★開発者の目視確認では見えない。**

### 3-4 ★`SetupEditorPage` へ i18n キーを足さなかった

同画面は「セットプレイ登録」「保存」「キャンセル」まで含めて**全面がハードコードの日本語**であり、新規の 2 文字列だけ i18n にすると同じ画面に 2 系統が混ざる。**⇒ `M24-07`（i18n）の手番として渡す**（§1.6-8）。

### 3-5 ★`mode` を 3 値にしなかった（コピー）

コンボ側は `"new" | "edit" | "copy"` だが、`SetupEditorPage` には `mode === "create"` の分岐が **6 か所**ある。3 値にすると `M19-07` / `M22-04` / `M23-09` / `M24-04` が積んだ分岐すべてに波及するため、**フラグを 1 本足すだけにして既存の分岐を 1 つも動かしていない。**

---

## §4 設計担当が未把握の残課題・申し送り

### 4-1 ★★編集画面の紐付けを「KA 入力による検索」へ作り替えたい（**開発者の新規要求。本サブでは着手していない**）

**開発者の逐語＝「編集については、KA入力して既存セットプレイを検索して紐づけるような形にしたい。その前段階として削除するかは任せる。」**

**現状**（実査）:

| | 詳細画面 | コンボ登録・編集エディタ |
|---|---|---|
| ボタン文言 | 「既存から紐付け」**（本サブで撤去）** | 「既存のセットプレイを紐付け」**（残した）** |
| モーダル | `LinkExistingSetupModal`**（削除済み）** | `SetupSelectorModal` |
| API | `GET /api/combos/{comboId}/setup-candidates` | `GET /api/setups/candidates?characterId=&knockdownAdvantage=` |
| 母集団 | 同一キャラ ＋ 同一 KA ＋ **当該コンボ未紐付け** | 同一キャラ ＋ 同一 KA。**「当該コンボ未紐付け」が効かない**（`excludeComboID=0` で呼ぶため） |
| 同じ面に候補表示があるか | **在った** | **無い** |

**★残した理由**——**代替の形（KA を入力して検索する）がまだ無く、今消すとコンボ登録・編集の途中で既存セットプレイを紐付ける手段がゼロになる。** 母集団も別であるため、指示書 §4.1 の「消す前提の検証」（母集団が同じこと）もエディタ側については満たしていない。

**⇒ 依頼: 新機能として起票されたい。** **★`DES-002` §4.2 の経路が要る見込みである**（KA を入力条件にした検索は、現行の `GET /api/setups/candidates` に `knockdownAdvantage` を任意入力として渡せば足りる可能性があるが、**「当該コンボ未紐付け」を効かせるなら `comboId` を受ける新パラメータが要る**）。

### 4-2 ★★`internal/service/setup` の記述と実装が食い違っている（**本サブでは触らないのが正しい**）

**`internal/service/setup/validate.go` のコメントは「レシピ未変更でも実行する」と書いているが、呼び手 `service.go` は `if input.Steps != nil` の内側でしか `ValidateSetupUpdate` を呼んでいない。**

**⇒ `PATCH /api/setups/{id}` に `steps` を含めず `name: ""` だけを送ると `VAL-S06`（名前必須）が走らない。** 画面は常に `steps` を同送するため UI からは踏めない。**レビュー担当も独立に追試して事実であることを確認している。**

**★これは「撤回済み・失効した記述がコード上に残っている」型そのものである。** `internal/` の VAL であり指示書 §1.6-4 / §2.2 / §9.1 が触ることを禁じているため直していない。**⇒ 割付先の判断を仰ぐ。**

### 4-3 ★`SetupEditorPage` の `handleTrashRestore` が離脱ガードを 1 つも通していない

素の `navigate()` を使っており、`M24-04`(CO-003) の 3 フックのいずれも経由しない（M23-09 由来の既存挙動）。**本サブが落としたものではない。**

### 4-4 ★`RecipeText` 未経由が 6 か所残る（**通せない理由がある**）

| 群 | 面 | 理由 |
|---|---|---|
| 名前の代替 | `CompareTable` / `TrashSetupListRow` / `TrashBulkActions` | レシピではなく「名前が無いときの代替表示」。`getSetupDisplayName` の**文字数切り詰め規則**（CSS truncate ではない）に従う |
| 未保存の steps | `SetupInputRow` / `SetupRecipeEditor` | `recipe_cache` 由来の文字列がまだ存在しない |
| 同上 | `SetplaySuggestionSection` | 同上 ＋ **`DES-005` §5.6 項目12＝別系統** |

**★母数の単位に注意**（`M24-13` 教訓 6 の作法）——**上記はセットプレイ側の数である。** コンボ側にも未経由の面が 2 つ残る（`HomePage` / `TrashComboDetailPage`。レビュー 低-4 の指摘）。

**⇒ followup `setup-recipe-display-not-through-shared-component` は「セットプレイ側の本文表示」については閉じてよい**（5 か所すべて通した）。**★ただしスラッグの文言どおりに全面が通ったわけではないため、閉じ方は設計卓の判断で。**

### 4-5 ★空表示の文言が 4 種に割れている

`"（レシピなし）"`（定数 `RECIPE_EMPTY_LABEL`）／ `"(レシピ未入力)"`（`SetupInputRow`）／ `"ステップがありません"`（`SetupRecipeEditor`）／ `t("trash.setup.unnamed")`。**本サブで面側の直書きは 0 件になった**が、C 群の 2 種は残る。

### 4-6 ★コピーの作法が 3 通りある

コンボ＝query（`?copyFrom=`）／ プリセット＝ダイアログ（`PresetCopyDialog`）／ セットプレイ＝**本サブで query に揃えた**。**本サブは「新しい作法を作らない」を守っただけで、プリセット側の統一は射程外。**

### 4-7 ★★指示書の改版で観測が 1 つ消えた（**記録**）

**チェックリスト §8 は「入力済みのセットプレイ名を自動生成で上書きしている」を最大の危険 2 件のうち 1 件として挙げていた**が、指示書 v1.1.0 が破壊確認 5 を落とした結果、**その性質を見るテストがどの層にも無くなっていた**（レビュー 中-4）。

**⇒ 本サブのレビュー取り込みで `SetplaySuggestionSection.test.tsx` へ 1 ケース足し、破壊確認で赤くなることも確かめた。** **★射程を削るときは「その項目に紐づく観測」も一緒に消えることを見ること。**

### 4-8 ★記録の訂正 2 件

1. **離脱ガード 3 フックの出所は `M24-12` ではなく `M24-04`(CO-003)**（`1e41985`）。指示書 §3.3-11 と followup `setup-editor-not-on-shared-editor-parts` が「`M24-12` の実測」と書いている。**`M24-12` のコミットは `SetupEditorPage.tsx` を 1 度も変更していない。**
2. **followup `setup-recipe-display-not-through-shared-component` の「5 か所」は実測 12 か所だった**（`SetupRegistrationSection` が漏れていた）。

### 4-9 ★`M24-07`（i18n）へ渡すもの

- **`SetupEditorPage` が全面ハードコードの日本語**（タイトル・保存・キャンセル・読み込み中・エラー文言・「コピー」）。
- **`SetupCandidateList` の「このコンボにも紐付ける」「N ステップ」「セットプレイ #N」もハードコード。**
- **`SetupAccordionItem` の「レシピ」見出しもハードコード。**
- **★`comboCommon.recipeView.label`（「レシピ全文表示」／ en `Full recipe`）は §4-10 の判断待ち。** 文言だけ直すか、語彙ごと決め直すかで扱いが変わる。

### 4-10 ★★`DES-005` §5.4 の「通常は末尾省略」というモデルが、コンボ詳細では成立していない（**開発者の実機確認・2026-08-30**）

**開発者の逐語＝「『レシピ全文表示』ボタンについて。レシピは元から全文表示されており、これは縦に表示するボタンなので、ラベル名が実際の機能を表していない。」**

**★★出所は `M24-03` / `CHANGE-134`（`e6e35f0`）であり、`M24-05` は挙動を変えていない。** `RecipeText.tsx` の差分は**コメントのみ**であることを実測で確認した（コメント行を除いた差分が空）。

**機序＝`compactClassName` が面ごとに非対称である**（実測）:

| 面 | `compactClassName` | トグル OFF のとき |
|---|---|---|
| 一覧 `ComboTableRow` ／ 比較 `CompareTable` | `max-w-xs` | **幅で切られて `…` になる** ⇒ 「全文表示」が実態と合う |
| 比較の追加ダイアログ `AddComboToCompareModal` | `sm:max-w-[380px]` | 同上 |
| **コンボ詳細 `ComboDetailRecipe`** | **`whitespace-pre-wrap break-words`** | **幅の制約が無く、折り返して全部読める** |

**⇒ コンボ詳細では OFF でも全文が読めるため、このトグルが実際に切り替えているのは「ステップごとに改行して連番（`1.` `2.`）を振るか」である。** 加えて同面は `surfaceDefault={true}` であり、**初回から ON** である。

**★★これは「ラベルの誤り」ではなく「設計書のモデルと実装の非対称」である。** `DES-005` §5.4 は `CHANGE-134` で **「通常は末尾省略、トグルで全文表示モードへ切替」**と確定しており、**「全文表示」はその語彙そのものである**。⇒ **ラベルだけを直すと設計書と乖離するため、語彙ごと決め直す判断が要る。**

**★ホバーの説明文は実態を補っている**——`comboCommon.recipeView.hint`＝「レシピを省略表示と全文表示(ステップごとに改行)で切り替えます。設定は一覧・詳細・比較・比較の追加ダイアログで共有されます」。**括弧内だけが本当の軸を述べている。**

**選択肢**:

| 案 | 内容 | 評価 |
|---|---|---|
| **A** | **ラベルと `DES-005` §5.4 の語彙を実態へ寄せる**（例「レシピを縦に展開」） | **★語彙を 1 か所で決め直せる。** i18n 文言のため `M24-07` と束ねられる |
| **B** | **コンボ詳細の `compactClassName` を他面と揃えて幅で切る** | **設計書のモデルには忠実になるが、詳細でレシピが読めなくなる。** `SM-022`（詳細で縦に読みたい）と逆行するため**非推奨** |

**★製造は直していない。** `M24-03` の成果物であり本サブの射程外であること、語彙が `DES-005` §5.4 に記録されており変更に `CHANGE` が要ることによる（**開発者判断＝申し送り**）。

---

## §5 参考（触れていない＝不変の証跡）

- `internal/` / `cmd/` / `migrations/` の差分 **0**（`git diff --stat ad42c4a -- internal/ cmd/ migrations/` が空）。**契約 F-1（`internal/service/notation/` / `internal/service/preset/`）も無変更。**
- **マイグレ消費 0 本**（最新は `000079`。**次に払い出す番号は `000080` のまま**）。**新規依存 0。**
- **ブラウザストレージのキーは新設・変更なし**（`check-browser-storage-keys.sh` 緑）。
- **`VAL-S01`〜`S07` の判定は 1 文字も変えていない**（`VAL-S06` も撤回していない）。
- **`/setups/:setupId` へ成立条件の入力を足していない。**
- **`SM-146` は 1 行も触れていない**（導線・「最後の技」・一覧の始動技フィルター・持続当ての扱い）。
- **`M24-12` / `M24-04` の成果は落ちていない**（離脱ガード 3 フック健在。レビューが独立に確認）。

---

## §6 CHANGE 起票のたたき台（設計担当向け）

**`CHANGE-140` の反映で扱う項目:**

1. **`DES-005` §5.6 項目11** — 「候補が0件の場合は本セクション自体を非表示」を**失効させ**、0 件でもセクションを出す形へ書き換える（§1-2）。**あわせて「`LinkExistingSetupModal` の候補抽出も…」の段（M12-03/6.5-1）と「将来的に UI 統合を検討する余地がある（現状は両方残置）」を、撤去済みとして書き換える。**
2. **`DES-005` §5.6 の「セットプレイ編集画面（`/setups/:id`）は実装しない」の曖昧さを是正**（指示書 §1.2 が予告した項目。**成立条件の入力面の話であることを本文で明示する**）。
3. **`DES-005` §5.9** — `SM-011` コピーの as-built（§1-3）＋ **コピー直後に無変更で保存すると `VAL-S04` で止まること**を 1 行。
4. **`DES-005` §5.9** — `SM-088` の自動命名の規則（§1-1）を as-built として記載。**★設計卓の暫定案とは違う。**
5. **`DES-006` VAL-S06** — 「空なら埋める」がどこで起きるか（**自動提案の採用時にフロントで埋める。通常登録・編集では埋めない**）の注記。**判定そのものは不変。**
6. **`DES-002` §4.2** — **本サブでは変更なし。**§4-1 の新機能を起票するなら、そのときに扱う。

**★スキーマ変更なし・マイグレ消費 0・新規依存 0・ブラウザストレージ新キー 0。**

---

## §7 教訓（**★親は本節を読まなくてよい**。宛先＝`retrospective-log` へのバッチ反映）

1. **★★「N 個ある」は本当に数え直すと違う。3 件当たった。** 指示書 §7.1-1 の「実査 12 件」（正 9 件）／ followup と `RecipeText` ヘッダの「未経由 5 か所」（正 12 か所）／ 完了報告自身の「新規追加 6 ファイル」（列挙は 9 件・レビュー 中-6 で指摘された）。**★3 件目は、その規律を書いている本人が自分の報告書で破った形である。**
2. **★★「工程順としてまだ無いものを過去形で書かない」を 3 か所で破った**（レビュー 高-1 / 高-3）。`progress-log` へ「追記した」・設計伝達レポートへ「渡した」「申し送った」と書いた時点で、**どちらも存在していなかった**。**★完了報告は実装の直後に書き上げたくなるが、後工程の成果物に触れる節は最後に埋めること。**
3. **★★検査は「報告書を書いたあと」にもう一度回す。** `check-md-emphasis.sh` は実装完了時点で緑だったが、**報告書を書いたことで赤に転じた**（増えた 3 行はすべて報告書自身）。`M24-13` 教訓 11「数値は書く前に採る」の裏返しで、**「書いたあとに採り直す」も要る。**
4. **★テスト名は「守られている」という記述として機能する。** E2E (5) は「コピー先で保存すると別のセットプレイとして増える」と名乗りながら、**コピー導線を押しても保存もしていなかった**（レビュー 高-4）。**動作は正しいままなのでテストも lint も型検査も緑になる。**
5. **★★「共通部品へ通した」だけでは守られない。** 新たに通した 5 か所のうち 3 か所は、**直書きへ差し戻しても全テストが緑のまま**だった（レビュー 中-1）。`data-testid` の存在を 1 行確かめるだけで固定できる。
6. **★見た目が変わる変更は、モックに含めた面だけを見ていると落ちる。** `SetupAccordionItem` が番号付き縦展開になった件は、**モックにも as-built にも無く、開発者の目視確認でも見えない**（`M24-13` 教訓 10 と同型）。
7. **★E2E の使い捨て DB は spec をまたいで共有される。** 候補の母集団は「同一キャラ ＋ 同一 KA」で決まるため、**KA を使い回すと他テストのセットプレイが混ざる**。初回実行で実際に落ちた。**⇒ 母集団に効く軸はテストごとに変えること。**
8. **★破壊確認は「型が通る壊し方」を選ぶ。** `buildAdoptedSetupName` を単純な早期 return で壊したところ**未使用引数でビルドが落ち**、「テストが赤い」ではなく「webServer が起動しない」になった（`M24-13` 教訓 7 の隣接形）。**引数を使ったまま結果を捨てる形へ変えて観測が成立した。**
9. **★memo の見立てを実装の根拠にしない。** `SM-052` は指示書・`CHANGE` の両方が「`RecipeText` で解ける」と書いていたが、実体は別だった。**⇒ 症状の記述と機序は別に確かめること。**

---

*以上、M24-05 設計伝達レポート。* **★★最重要は §4-1（編集画面の紐付けを KA 入力による検索へ作り替えたいという開発者の要求）と §2-1（チェックリストの版ずれ）。** **★あわせて §4-10（`DES-005` §5.4 のモデルが詳細画面で成立していない）は開発者の実機確認で出たものであり、語彙の決め直しが要る。** **★§1-1 の `SM-088` の規則は設計卓の暫定案と一致しないため、`DES-005` §5.9 へは実測値を書かれたい。**
