# M37-07 設計伝達レポート(例外レポート)

| 項目 | 内容 |
|------|------|
| 対象 | **親チャット(設計卓)** |
| 発信 | 製造担当 Claude Code ／ 2026-09-14 |
| 指示書 | `docs/instructions/M37-07-starter-meaty-key.md` **v1.2.0** |
| 実装コミット | ブランチ `claude/tender-newton-csy9b2` ／ 着手基点 `6db1eb9` |
| 関連 | 完了報告 `docs/progress/M37-07-completion-report.md` ／ レビュー `docs/progress/m37-07-review.md` |
| **レビュー集計** | **高 3 / 中 4 / 低 3 ＝ 全 10 件・採用 7 / 不採用 3・「高」の不採用 0 件・再レビュー往復 0 回(上限 2)** |
| マイグレ | **1 本消費＝`000117`**(払い出し済み。**自採番していない**＝`D-293`) |
| CHANGE | **請求のみ。製造は自採番しない**(`D-293`)。⇒ §6 |

本書は **①独自確定仕様 ②契約違反の独自判断 ③製造判断 ④残課題** に絞る。指示書どおりの部分は割愛する。

**★★★最重要は §1-1 である**——**`SUPP-001` §2.2 の重複判定キーを 7 → 8 要素へ広げる原稿。⇒ ここが正本である。**

**★★次に重いのは §4-1 である**——**同じ §2.2 が約束している「リスト定数を 1 箇所修正すれば切り替わる構造」が、実装には存在しない。⇒ 約束のほうを実態へ合わせるか、実装を約束へ合わせるかの判断が要る。**

---

## §1 製造が独自に確定した実装仕様(DES 反映が要るもの)

### 1-1 ★★★`SUPP-001` §2.2 の重複判定キーを **7 → 8 要素**へ広げる(**CHANGE 原稿・ここが正本**)

**現行(逐語)**

```
- コンボの重複判定キーは以下の「完全一致」で行う(VAL-C02)
  - `character_id`、`recipe(combo_stepsの順序込みmove_id列+modifiersの完全一致)`、
    `starter_move_id`、`position`、`opponent_stance`、`hit_type`、`opponent_size`
```

**★改訂原稿**

```
- コンボの重複判定キーは以下の「完全一致」で行う(VAL-C02)
  - `character_id`、`recipe(combo_stepsの順序込みmove_id列+modifiersの完全一致)`、
    `starter_move_id`、`position`、`opponent_stance`、`hit_type`、`opponent_size`、
    `starter_meaty`
- **★`starter_meaty` は「そのコンボの始動技を持続当てしたか」(M37-07・開発者裁定 `D-874`)。**
  `INTEGER NOT NULL DEFAULT 0` の bool であり、**NULL を取らない**
  ——重複判定キーに入る列で NULL を許すと、SQL の `NULL != NULL` により
  「不明どうし」が別コンボへ分かれる意味の無い分岐が生まれるためである。
  **★同じ理由で 3 値(持続当て / 非持続当て / 不問)にしない。**
- **★★`hit_type` とは別枠である**(開発者の逐語＝「始動技用としては別枠として欲しい。
  ヒット種別とも別枠」)。持続当ては*ヒットの種類*ではなく*当て方*であり、
  「持続当てのカウンターヒット」がありうる。⇒ 1 つの列に畳めない。
- **★★`combo_oki_options` の打撃重ね(`oki_strike_meaty_*`・`CHANGE-063`)とは向きが違う。**
  あちらは「このコンボの*後に*どんな起き攻めができるか」(出る側)、本列は
  「このコンボの*始動が*持続当てか」(入る側)である。
- **★`starter_meaty` は recipe の一部ではない。⇒ `recipe_hash` は 1 バイトも変わらない。**
```

**★★あわせて同節末尾のコード例(`DuplicateCheckKeys` のスケッチ)へ `"starter_meaty"` を足す。⇒ ただし §4-1 を読むこと**——**同スケッチが謳う「1 箇所で切り替わる構造」は実装に存在しない。**

### 1-2 ★★`DES-003` §3.3 の `combos` 列一覧へ 1 行(**CHANGE 原稿**)

```
| starter_meaty | INTEGER | NOT NULL DEFAULT 0 | 始動技を持続当てしたか(bool 0/1)。
  M37-07・マイグレ 000117。★重複判定キー(VAL-C02)の 8 つ目である。
  ★recipe_hash の対象外(列の側であり recipe の一部ではない)。
  ★CHECK 制約は持たない —— 既存の bool 列(is_draft / oki_verified /
    moves.is_derived / moves.is_projectile)がいずれも持たない流儀に揃えた。 |
```

**★`combo_oki_options` の節は 1 バイトも変えない**(§0.3 の「向きが違う」)。

### 1-3 ★★`DES-006` VAL-C02 §2.3 へ 1 項(**CHANGE 原稿**)

§2.3「重複判定のロジック」の条件列挙へ次を足す。

```
- `starter_meaty` が同じ(M37-07)
  **★本列は NOT NULL であり、他の 5 列のような「NULL 同士は一致」の扱いを持たない。**
  ⇒ 判定 SQL の述語は常に `starter_meaty = ?` である。
```

**★`deleted_at IS NULL` / `is_draft = 0` の固定条件は 1 文字も変えていない。**
**★`VAL-C14` / `VAL-S07` / `VAL-R03` / `VAL-R04` の判定内容も変えていない**——本列はキーの器
(`duplicateKeyPredicates`)を共有しているため、4 件へ自動的に及ぶ。

### 1-4 ★★`DES-002` §4.2 / §7 へ(**CHANGE 原稿**・API 逐語つき)

**★経路表に載る API を変えたため、逐語を本文へ埋める**(本テンプレ §1 の扱い・`D-496`)。
**⇒ 「完了報告 §X が正本」と参照だけにしない**——設計卓は完了報告を読まず、製造ブランチの
完了報告は設計卓の作業ツリーに無い。

#### (1) 応答の実 DTO(**`json.MarshalIndent` の実行出力**。転記ではない)

`ComboResponse`(`POST` 201 / `GET` 200 / `PATCH` 200 / `PUT` 201)——**新キーは `starterMeaty` の 1 つ**:

```json
{
  "id": 42,
  "characterId": 1,
  "isDraft": false,
  "starterMoveId": 100,
  "position": "mid_screen",
  "opponentStance": "standing",
  "hitType": "normal",
  "opponentSize": "standard",
  "starterMeaty": true,
  "okiVerified": false,
  "stepCount": 2,
  "version": 1
}
```

`DuplicateInfoResponse`(`POST /api/combos/check-duplicate` の `duplicates[]`)——
**★`starterMeaty: false` が*キーごと出ている*ことが要点である**:

```json
{
  "id": 7,
  "characterId": 1,
  "starterMoveId": 100,
  "position": "mid_screen",
  "opponentStance": "standing",
  "hitType": "normal",
  "opponentSize": "standard",
  "starterMeaty": false,
  "stepCount": 2,
  "memo": null
}
```

#### (2) エラー契約の全行(状況 → ステータス ＋ コード)

| 経路 | 状況 | 応答 |
|---|---|---|
| `POST` / `PUT /api/combos` | **`starterMeaty` まで一致する重複**(VAL-C02) | **400 `validation_failed`**(★409 ではない。`DES-006` §11.2 (3)) |
| `POST` / `PUT` | 本文が JSON として壊れている | 400 `invalid_request` |
| `GET /api/combos` | **`starter_meaty` が真偽値として読めない** | **400 `invalid_query_param`**(先例＝`affected_by_game_update` / `setup_in_corner`) |
| `POST /api/combos/check-duplicate` | 本文が壊れている ／ `characterId` 欠落 | 400 `invalid_request` |
| 同 | サービス層の失敗 | 500 `internal_error` |

**★本サブは新しいエラーコードを 1 つも作っていない。** `starter_meaty` 固有の検証(値域等)は
**存在しない**——列が `NOT NULL` の bool であり、JSON の型検査以外に検査すべき値域が無い。

#### (3) 母集団の述語(その経路が何を返し、何を返さないか)

- **重複判定の母集団は 1 文字も変えていない**＝`is_draft = 0` ＋ `deleted_at IS NULL`
  (生きた側)／ `deleted_at IS NOT NULL` ＋ `superseded_by_combo_id IS NULL`(ゴミ箱側)。
  **⇒ 足したのは絞り込みの述語 1 本(`starter_meaty = ?`)だけである。**
- **★他の 5 列と違い `col IS NULL` の分岐を持たない**——`NOT NULL` であり NULL 一致の扱いが要らない。
- `GET /api/combos` の `starter_meaty` は**指定したときだけ効く**。未指定＝絞り込みなし(両方返る)。

#### (4) ★★持たせなかった分岐(**読む側の誤読を防ぐために要る情報**)

| 何を持たせなかったか | なぜ |
|---|---|
| **`PATCH /api/combos/:id` に受け口を作らなかった** | **識別キーだからである**(`CHANGE-195` §2.4＝「本経路が触るのは識別キーが変わらない編集だけ」)。**⇒ この欄を変える編集は `PUT` へ行く。★`UpdateMetadataRequest` に `starterMeaty` のフィールドは存在しない**(`TestStarterMeaty_NotOnPatch` が reflect で主張・陽性対照つき) |
| **一覧クエリの値域外を「絞り込まない」に倒さなかった** | 倒すと typo が全件返却に見える。**⇒ 400 で落とす** |
| **「不問」の第 3 値を作らなかった** | 列が `NOT NULL DEFAULT 0` の 2 値である。**⇒ 3 値にすると重複判定キーに意味の無い分岐が入る**(指示書 §0.5)。**★一覧フィルタの「指定なし」はフィルタの状態であって列の値ではない** |
| **DB へ UNIQUE 索引を新設しなかった** | 判定はアプリ層である(`VAL-C02`) |

#### (5) §7 CSV 契約

**任意列として列末尾へ `starter_meaty` を追加。⇒ 全 36 → 37 列 ／ 必須 22(不変) ／ 任意 14 → 15。**
**★必須列が動いていないことが「旧 CSV の取込を壊していない」ことの観測である**
(`TestStarterMeatyBackwardCompatImport` が列を 1 つ削った CSV で主張)。
**★欠損セル＝`nil`＝`false`(通常始動)として読む**(`oki_verified` と同じ扱い)。

### 1-5 ★`DES-005` §5.7 / §5.6 / §5.4 へ(**CHANGE 原稿**)

| 節 | 原稿 |
|---|---|
| **§5.7 編集画面(状況の欄)** | ヒット種別の隣へ「**持続当て（始動技）**」を追加。**選択肢は はい / いいえ の 2 値**(★「不問」を置かない＝列が `NOT NULL DEFAULT 0` であるため)。形は `OptionButtonGroup mode="single"`(隣接する状況欄と同じ) |
| **§5.6 詳細画面** | 状況の `<dl>` へ「持続当て（始動技）」を追加(5 項目目) |
| **§5.4 一覧** | **フィルタへ 1 軸追加**(指定なし / はい / いいえ の 3 状態 `<select>`)。**★一覧の*列*は増やしていない**——`ColumnVisibility` は 9 のままである |

**★★表示語は「持続当て（始動技）」である**(開発者裁定の逐語)。**設計卓案の「持続当て（始動）」ではない。⇒ 1 文字多い。写し間違えないこと。**

**★step の modifier「持続当て」は 1 文字も変えていない**(`M37-06` の射程)。**語に「（始動技）」を付けたのは、両者が同じ画面に出て紛れるためである。**

---

## §2 契約・設計に反する独自判断(★親の裁定が要る)

### 2-1 ★★指示書 §2.5 は「CHANGE 原稿を設計伝達レポート **§4** へ書く」と定めているが、**§1 へ書いた**

**★本レポートの既存の型では、§4 は「設計担当が未把握の残課題・申し送り」であり、DES 反映が要る確定仕様は §1、CHANGE の起票たたき台は §6 である**(先例＝`M37-06` / `M37-05` / `M34-02` のいずれも同じ配置)。

**⇒ 指示書 §2.5 の「§4」は本レポートの節番号と食い違っている。★原稿を失わないため、§1(本体)・§4(索引)・§6(たたき台)の 3 か所から辿れる形にした。**

**★★停止規律の §J 原稿は指示書どおり §4 である**(`CLAUDE.md` §9 / `D-838`)。**⇒ そちらは動かしていない。**

---

## §3 製造の判断

### 3-1 開発者へ確認して確定した点(2026-09-14)

| # | 事項 | 回答 |
|---|---|---|
| 1 | 列名 `starter_meaty`(指示書 §7-1) | **そのまま採用** |
| 2 | 編集画面の選択肢の文言 | **「はい / いいえ」** |

### 3-2 製造の裁量で決めた点

| # | 決めたこと | 理由 |
|---|---|---|
| 1 | **`CHECK` 制約を付けない** | 実物の bool 列 4 本(`is_draft` / `oki_verified` / `moves.is_derived` / `moves.is_projectile`)が**いずれも持たない**。`CHECK` は値域列だけの流儀である(指示書 §2.2-3「実物を見て揃える」の答え) |
| 2 | **述語を `addNullable` に通さない** | 本列は `NOT NULL` であり NULL 一致の分岐が存在しない。⇒ 常に `starter_meaty = ?` |
| 3 | **フロントで `nullableEqual` を使わない** | `undefined` と `false` を同値にしないと、**保存済みの行を読み直しただけで `PUT` へ流れ、旧行を捨てて id を採番し直す** |
| 4 | **Materialize の FR301 探索へは「埋めるだけ」** | 同探索が差し替えるのは `hit_type` だけであり、生成物は基底と同じ始動である。⇒ `base.StarterMeaty` をそのまま渡す(条件を広げても狭めてもいない) |
| 5 | **一覧フィルタを 3 状態 `<select>` にした** | 横幅のため。本行は `P-21` / `M27-03` の面に近く、ボタン群を増やすと最も先に折り返す。**★「指定なし」はフィルタの状態であって列の値ではない** |
| 6 | **一覧の*列*は増やさない** | 指示書はフィルタを求めており列は求めていない。先例＝`M37-01`(列を足さずに詳細へ出した) |
| 7 | **選択肢の語を `ja.json` 由来にした** | 既存の `FLAG_STATE_OPTIONS` は「はい」「いいえ」を直書きしている。流用すると同じ語が 2 か所に在る状態(`E-76`)になり、`label-keys.test.ts` の網にも入らない |
| 8 | **CSV 取込プレビューの列ラベルを足さない** | `comboImport.col.*` は 37 列中 18 列だけの部分的な写像であり、直近に足った任意列 3 つ(`oki_verified` / `start_position_mass` / `carry_distance_mass`)も登録されていない。⇒ 先例に揃えた |
| 9 | **CSV の列契約テストを同じ手番で更新した** | 同テストの godoc 自身が「列を足すときは本テストの期待値も同じ手番で更新すること」と定めている |

### 3-3 推測で進めた点

**なし。** 未決事項を抱えたまま進めた箇所は無い。

---

## §4 設計担当が未把握の残課題・申し送り

### 4-1 ★★★`SUPP-001` §2.2 が約束している構造が、実装に存在しない(**判断が要る**)

同節の逐語：

> 重複判定で比較するフィールドは、サービス層で**リスト定数として定義**する。将来「modifiers を比較から
> 外す」等の変更が必要になった場合、**リスト定数を 1 箇所修正するだけで挙動を切り替えられる構造**とする。

**★実測(2026-09-14・`grep -rn "DuplicateCheckFields" --include=*.go .`)＝定義行のみの 1 件。⇒ 判定ロジックからも、自身のテストからも参照されていない。**

**★★本サブでは値だけを 8 要素へ更新し、godoc へ「これは正本の写しであってスイッチではない」ことと実際の足し先の一覧を書き足した。⇒ 配線し直すのは射程外と判断した**(設計書の記述を実装へ強制する構造変更になる)。

**★★★判断が要るのは次の 2 択である。**

| 案 | 中身 | 代償 |
|---|---|---|
| **A** | **`SUPP-001` の記述を実態へ合わせる**(「キーは複数箇所に分かれて持たれている。増減するときは全数を直す」と書き換える) | 安いが、**次に足す担当も 30 箇所を数え直すことになる** |
| **B** | **実装を記述へ合わせる**(判定の各所を 1 本の定義から導く) | 高い。**層独立性のため `repository` と `validation` で意図的に二重定義している**(`M1-03` 機械レビュー・高優先の対応)ため、素直には畳めない |

**★どちらにせよ本サブの射程外である。⇒ 設計卓の手番。**

### 4-2 ★★指示書・チェックリストの失効記述 3 件(**製造はどちらも編集しない**)

| # | 箇所 | 記述 | 実測 |
|---|---|---|---|
| 1 | 指示書 **§2.1 段 1-2** | 「`ls migrations/` の最新は **`000115`** のはず」 | **`000116`** |
| 2 | チェックリスト **F-4** | 「3＝最新が **`000115`**」 | **`000116`** |
| **3** | **指示書 §2.1 段 1-4 ／ チェックリスト §8 対照値** | 「**既存行 133**〔有効 109 / 削除済み 24〕」 | **総数 144 / 本登録・未削除 111**(2026-09-14・開発者が実 DB で実測) |

**★1 / 2 の原因**＝v1.1.0 が `§0.6 #3` だけを `000116` へ更新し、この 2 か所が追随していない。
**⇒ 版ゲートは `§0.6 #3` を正として通した。**

**★★3 の原因**＝**対照値は `M37-05`(2026-09-13)時点の開発者実測であり、1 日のあいだに DB が増えた。**
**⇒ 結論は 1 つも変わらない**(全行 `0`・重複 0 組)。
**★ただしチェックリスト §8 は「⇒ 数え直させる」ための対照値である。**
**⇒ 古い値のままだと、次に数え直す担当が「合わない」と誤って止まる。**

### 4-3 ★既存 followup の関連(**製造は `followup-backlog.md` を編集できない**＝`D-838`)

`csv-import-precheck-duplicate-uses-unnormalized-position` —— CSV 取込の事前重複判定が
`normalizePositionAndMass` を通らない生の `position` を使う既知の穴。
**本サブは同じ関数へ `starter_meaty` を足したが、この穴は直していない。**
指示書 §4.1 が「本サブでその穴を直す必要は無いが、足す先には含まれる」と明示している。
**⇒ 行の更新は設計卓の手番。**

### 4-4 ★★開発者の手動確認は**完了した**(2026-09-14)——**全 5 項目合格**

**★製造の手番では原理的に取れない観測を、開発者が 5 項目実施し、いずれも問題なしであった。**

| # | 何を | 結果 |
|---|---|---|
| **B-1** | 実 DB(列定義 / 既存行 / 重複監査) | **合格**(下記) |
| **B-2** | **★★一覧フィルタの横幅**(`P-21` / `M27-03` の面) | **合格** |
| **B-3** | 編集画面(欄の位置・2 値・数字キー・順送り・**modifier「持続当て」との紛れ**) | **合格** |
| **B-4** | `PUT` / `PATCH` の振り分け(実挙動) | **合格** |
| **B-5** | CSV の往復(実データ / **旧 CSV**) | **合格** |

**★★★B-2 は指示書 §2.4-5 が名指しで開発者判断を求めていた唯一の項目である。**
同節は「**一覧のフィルタへ足す**」を裁定として確定させたうえで、
「**`P-21` / `M27-03` の面に近づくことは承知のうえの決定である。**」
「**⇒ 横幅が苦しくなったら、外すのではなく止めて報告すること**」と条件を付けていた。
**⇒ その分岐には入らなかった。**
**承知のうえで受け入れたリスクが実機で問題にならなかった、という結果を設計卓へ返す。**

**★★B-3 の「紛れ」は表示語の存在理由そのものである**——step の modifier「持続当て」と
状況欄の「持続当て（始動技）」が同一画面に出る。
**⇒ 開発者裁定で「（始動技）」を足した判断が、実機で機能したことの確認である。**

#### B-1 の詳細(実 DB)


**★製造の作業ツリーには DB ファイルが 1 つも無い**(クリーンクローンであり `combos` は利用者データ)。
**⇒ 製造の手番では原理的に取れないため、開発者が dev DB を読み取り専用で実測した。**

| 対象 | 実測 |
|---|---:|
| 列定義 | `32\|starter_meaty\|INTEGER\|1\|0\|0` ＝ **`NOT NULL` / `DEFAULT 0`** |
| `combos` の総数 | **144**(うち `<> 0` が **0** ／ `IS NULL` が **0**) |
| 本登録・未削除 | **111** |
| 識別キー ＋ `recipe_hash` が一致する組 | **0 組 / 0 件** |

**⇒ 「既存行はすべて `0` で一様であり、重複判定の結果は 1 件も変わらない」が実データで確かめられた。**

**★★ただし「0 組」の読み方を書き分ける。** これは**重複が無いことの確認**であって、
**監査の突合せロジックが `starter_meaty` を正しく織り込んでいることの証明ではない**
(比較対象の組が 1 つも無いため)。
**⇒ 積極的に示されたのは「新列を含む `SELECT` が実 DB で通り、111 行を読めた」ことである。**
**★突合せの正しさは Go のテスト側が担う。**

**★行数が指示書・チェックリストの対照値(133 / 109)と食い違う件は §4-2 # 3 を参照。**

### 4-5 ★レビュー低指摘の繰越(**不採用 2 件を設計卓へ回す**)

| # | 事項 | なぜ本サブで直さなかったか |
|---|---|---|
| **1** | **出力(PDF / PNG / クリップボード)に `starter_meaty` が出ない** | `web/src/features/combo-io/export-model.ts` の `situationValue` は position / stance / hitType / opponentSize の 4 つだけを連結する。**★本欄は重複判定キーであるため、持続当て違いの 2 本を並べて出力すると見分けが付かない。⇒ 実害は認める。** **★しかし先例＝`start_position_mass` / `carry_distance_mass`(`M28-02a`)も同関数に入っていない。⇒ 本欄だけ入れると流儀が割れる。** 指示書 §2.4 も出力までは求めていない。**⇒ 3 列まとめて判断するのが妥当であり、設計卓の手番。** |
| **2** | **停止しない `playwright test` が残る運用上の穴** | レビュー担当が着手したとき、ポート 47390 / 5273 を占有したままの `playwright test` プロセス(バックエンド・vite preview・chromium を含む)が残っており、最初の `make e2e-only` が `is already used` で落ちた。**★成果物の欠陥ではなく実行環境の運用上の穴である。** **★取り込みの手番で残留プロセスとポート占有が無いことを確認し `web/test-results/` を掃除したが、恒久の対処(`make e2e` の後始末)は本サブの射程外。** |

### 4-6 §J 停止時記録の原稿

**★未解消の項目は無い。⇒ §J へ足す行は無い。**

**★★根拠＝レビュー指摘 10 件のうち「高」3 件・「中」4 件をすべて採用して直し、「低」3 件は理由を記録して不採用とした。「高」の不採用は 0 件であり、開発者エスカレーションは発生していない。再レビュー往復は 0 回(上限 2)。**


---

## §5 参考(触れていない＝不変の証跡)

| 対象 | 証跡 |
|---|---|
| **`recipe_hash`** | `internal/recipehash/` の差分 **0 行**。`TestStarterMeaty_RecipeHashUnchanged` が同一レシピのハッシュ一致を主張 |
| **`hit_type`** | **8 値のまま**。値を 1 つも足していない |
| **`combo_oki_options`** | **12 変種のまま**。変更行のうち同表・`oki_strike_meaty_*` を含むものは**コメントの言及のみ**で、実コードは `gofmt` の空白再整列 1 行だけ(`git diff -w` で 0 行) |
| **modifier の `meaty`** | `labels.ts` / `notation/resolver.go` とも差分 0 行(`M37-06` の射程) |
| **`PATCH`(`UpdateMetadataRequest`)** | 受け口なし。`TestStarterMeaty_NotOnPatch` が reflect で主張(陽性対照つき) |
| **`docs/design/` 本体** | 差分 0 行(`CLAUDE.md` §8) |
| **`docs/handover/followup-backlog.md`** | 差分 0 行(`D-838`) |
| **必須 CSV 列** | **22 のまま**(旧 CSV の取込を壊していないことの観測) |

---

## §6 CHANGE 起票のたたき台(設計担当向け)

**★製造は自採番しない**(`D-293`)。**★ボード L123 時点の残枠＝`207`〜`211` の 5 本。**

**★★マイグレは `000117` の 1 本を消費した**(`combos` へ `starter_meaty` を足す列追加)。
**⇒ 設計卓が払い出し済みのものを使っており、製造は自採番していない**(`D-293`)。
**★着手前に `ls migrations/` を実査し、最新が `000116` であることを確認してから使った**
(版ゲート §0.6 # 3)。**★`000113` は欠番である**(`D-866`)。**⇒ 予約帯は作っていない。**
**★ボード §2.2「次に払い出す番号」は着手前から `000118` であり、動かす必要は無い。**

| # | 影響設計書 | 中身 | 本レポートの節 |
|---|---|---|---|
| **1** | **`SUPP-001` §2.2**(正本) ＋ `DES-006` §2.3 VAL-C02 | **重複判定キーを 7 → 8 要素へ** | **§1-1 / §1-3** |
| **2** | `DES-003` §3.3 | `combos` へ `starter_meaty` 列(マイグレ `000117`) | §1-2 |
| **3** | `DES-002` §4.2・§7 ／ `DES-005` §5.7・§5.6・§5.4 | API 経路(POST / PUT / check-duplicate / 一覧クエリ。**★PATCH には載せない**)・CSV 任意列・画面 3 面 | §1-4 / §1-5 |

**★指示書の見込みは「2〜3 本」であり、上の束ね方は 3 本である。⇒ 束ね直しは設計卓の裁量。**

---

## §7 教訓(retrospective 行き)

**★★製造が自分で踏んだ穴である。⇒ レビューが 3 件すべてを「高」で検出した。**

| # | 何を踏んだか |
|---|---|
| **1** | **★★★足し先を数えるための `grep` を `\| head -20` へ通した。** 20 行で切られ、`internal/service/validation/duplicate.go` 以降が視界に入っていなかった。**しかも `--include=*.go` しか掛けておらず、フロント(`*.tsx`)は一度も走査していない。⇒ 失効記述 11 箇所を取りこぼし、レビューが検出した。** **★★`M37-05` の `go test \| head -20` と*同型*である**——**製造 CLI の `<!-- FULL-TEST-RUN-EXIT-CODE -->` は「全数テストの合否」を対象に書かれているが、真の一般形は「*全数を数えるための出力*を `head` で切らない」である。⇒ 対象がテスト出力から `grep` へ移っただけで同じ穴に落ちた。** |
| **2** | **★★「テストが緑」を足し先を数え切った証拠として扱った。** `existing_duplicates_audit_test.go` はキーを独立に再実装しており、`TACPENDIUM_AUDIT_DB` 未設定なら skip されるため **`go test ./...` は緑のまま**であった。**⇒ 環境変数で走行が切り替わるテストは、その環境でしか観測できない。★計画段階では「追随が要る」と一覧に挙げていたのに、実装で落とした。** |
| **3** | **★既存ラベルを部分文字列として含む新ラベルを足した。** 「持続当て（**始動技**）」が「始動技」を含み、E2E の `getByText("始動技")` が 2 要素に当たった。**⇒ 一般形＝新ラベルが既存ラベルの上位文字列になるときは、`web/e2e/` の部分一致ロケータを全数走査すること。★フロントの Vitest 側は Testing Library が既定で完全一致のため無事だった(言語ではなくライブラリの既定に救われた)。**

---

*以上、M37-07 設計伝達レポート。* **★★§1-1 が正本である。** **★★§4-1 は本サブの外の判断を求めている**——**`SUPP-001` が約束する構造が実装に無い。** **★レビュー取り込みまで完了しており、「高」指摘の不採用は 0 件である。**
