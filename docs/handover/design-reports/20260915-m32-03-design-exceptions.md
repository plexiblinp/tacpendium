# M32-03 設計伝達レポート(例外レポート)

| 項目 | 内容 |
|------|------|
| 対象 | **親チャット(設計卓)** |
| 発信 | 製造担当 Claude Code ／ 2026-09-15 |
| 指示書 | `docs/instructions/M32-03-usermanual-revision-after-m37.md` **v1.1.0** |
| 実装コミット | ブランチ `claude/wizardly-lamport-bz9z7t` ／ 着手基点 **`96b01d6`** ／ 7 コミット |
| 関連 | 完了報告 `docs/progress/M32-03-completion-report.md` ／ レビュー `docs/progress/m32-03-review.md` |
| **レビュー集計** | **高 2 / 中 7 / 低 5 ＝ 全 14 件・採用 9 / 不採用 5・「高」の不採用 0 件・再レビュー往復 0 回(上限 2)** |
| マイグレ | **0 本** |
| CHANGE | **請求のみ。製造は自採番しない**(`D-293`)。⇒ §6 |

本書は **①独自確定仕様 ②契約違反の独自判断 ③製造判断 ④残課題** に絞る。指示書どおりの部分は割愛する。

**★★★最重要は §2-1 である**——**指示書 §2.2 の対応表が前提とした `CHANGE` の範囲が、実際より狭かった。⇒ 製造は範囲を自分で引き直し、表に無い 2 本を取り込んだ。この判断の受理/却下が要る。**

**★★次に重いのは §2-3 である**——**指示書 §2.5.3 のクイックスタートの上限 3 条件は、算術上、同時に満たせない。⇒ 条件のどれを緩めるかの裁定が要る。**

**★★★§3-1 の 4 件は、いずれもリポジトリ上に裁定行が無い**(本セッションの対話で確定した)**。⇒ 受理の手番で記録をお願いしたい**(レビューも「不明事項 1」として同じ点を挙げている)。

---

## §1 製造が独自に確定した実装仕様(DES 反映が要るもの)

**★API 経路の新設・変更は 0 件である**(実装差分 0 行)。**⇒ `DES-002` §4.2 の as-built 逐語埋め込み(本テンプレ §1 の規則)は該当しない。**

### 1-1 ★★`CHANGE-192` §2 の配布アーカイブ構成に**クイックスタートが無い**(**CHANGE 原稿・ここが正本**)

**現行(逐語・`CHANGE-192` §2)**

```
tacpendium-windows-amd64.zip    （公式サポート）
├── tacpendium-windows-amd64.exe
├── README.txt（起動方法・注意事項）
└── manual/
    ├── tacpendium-readme.html （操作説明書）
    └── images/                 （章ごとのスクリーンショット）
```

**本サブが `docs/usermanual/tacpendium-quickstart.html` を新設した。⇒ 同図の `manual/` に 1 ファイル足りない。**

**★★反映依頼**——`DES-002` §11.2 のアーカイブ構成へ次の 1 行を足してほしい。

```
└── manual/
    ├── tacpendium-readme.html      （操作説明書・全 22 章）
    ├── tacpendium-quickstart.html  （クイックスタート・★本サブで新設）
    └── images/
```

**★★★製造は本文側で先回りしていない**(レビュー 高-2 の取り込み)**。⇒ 配布 `README.txt` が名指しするのは `manual/tacpendium-readme.html` だけであり、クイックスタートへは*その冒頭のリンク経由*で送る形にした。⇒ 構成図が更新されないまま Releases を切っても dead reference は生じない。**

**★あわせて `.github/workflows/nightly-crossbuild.yml:105` のコメントが失効している**(逐語＝「`tacpendium-windows-amd64.zip`（中身 = tacpendium.exe + README.txt）」)**。⇒ `CHANGE-192` 後の構成と食い違う。★製造は CI を触っていない**(指示書 §3-6)**。**

### 1-2 クイックスタートの置き場と命名(**確定させた**)

| 項目 | 確定値 | 根拠 |
|---|---|---|
| パス | **`docs/usermanual/tacpendium-quickstart.html`** | `M32-01` が定めた `tacpendium-<種別>.html` の流儀(`tacpendium-readme.html` と対) |
| 図の接頭辞 | **`qs-`**(実例 `images/qs-first-combo-1.png`) | 本編の `ch<NN>-<slug>-<n>.png` と衝突させないため。指示書 §2.5.6 が「別の接頭辞を提案してよい」としている |
| 導線 | 本編の**前付け「本書の読み方」**から 1 行リンク | 開発者裁定(§3-1)。**★22 章の本文には 1 行も触れていない** |

**⇒ `docs/usermanual/` の置き場規約を書いた文書があれば、上の 2 行(命名・図の接頭辞)を明文化してほしい。**

---

## §2 契約・設計に反する独自判断(★親の裁定が要る)

### 2-1 ★★★指示書 §2.2 の対応表が前提とした `CHANGE` の範囲が、実際より狭かった

| | |
|---|---|
| **指示書の明文** | §0.1＝「当てる差分は列挙できる。⇒ **`CHANGE-195`〜`209` の 15 本**であり、どの章に当たるかは §2.2 の表に置いた」。§1-5 が一次源として **8 本**(`196`/`197`/`199`/`200`/`201`/`203`/`205`/`209`)を名指しする |
| **実測** | **`M32-02` 本文執筆後に着地したのは `CHANGE-192`〜`211` の 20 本である** |
| **境界の引きかた** | 本文の最終コミット `8df45ab`(2026-09-12 18:03)以降に**新規追加**された通知書を `git log --diff-filter=A -- docs/change-notes/` で列挙した |
| **製造の判断** | **範囲を自分で引き直し、残る 12 本を全数実読して判定表に落とした**(完了報告 §3.1)**。影響ありと判定した 2 本を取り込んだ**——**`CHANGE-192`**(§1-1) と **`CHANGE-207`**(下記 2-2) |

**★これは指示書 §4.5「設計卓が出した対応表を信じきるな」の想定内の動作ではある。⇒ ただし「表に 1 行足りない」ではなく「*前提とした母集団が違った*」ため、§3 ではなく §2 へ挙げる。**

**⇒ 裁定をお願いしたい**——(a) 範囲の引き直しを受理し §2.2 の表を更新する ／ (b) 取り込んだ 2 本を差し戻す。

### 2-2 ★★★`ch08` の記述が**現に誤っていた**——重複判定キーが 6 つのまま

| | |
|---|---|
| **本文(着手前)** | 「レシピ・始動技・始動位置・相手の状態・ヒット種別・相手の大きさ——**この 6 つ**のどれかを変えて保存すると、元のコンボはゴミ箱へ入り、新しいコンボとして登録されます」 |
| **実測** | `internal/service/combo/duplicate_keys.go:33-42` の `DuplicateCheckFields` は **8 要素**。`character_id` を除くと利用者から見て **7 つ** |
| **由来** | `CHANGE-207` / `CHANGE-209`(`M37-07`・`D-874`)が `starter_meaty` を足した |
| **実装** | **「持続当て（始動技）」を足して「7 つ」へ是正した**(`ch08`) |

**★★指示書 §2.2 の表は `ch08` に `CHANGE-209` を割り当てているが、「大事 box の項数」は名指ししていない。⇒ 表の粒度では拾えない失効記述であった。**

**★レビューが独立に裏取り済み**(逐語＝「本文の 7 語と 1 対 1 で一致」)**。**

### 2-3 ★★★指示書 §2.5.3 の上限 3 条件は**算術上、同時に満たせない**

| 条件 | 状態 |
|---|---|
| 節 5 つまで | **満たす**(5 節) |
| 各節 3 行程度 ＋ 図 1 枚まで | **満たす**(最長の節で約 5 行、図は芯の節に 1 枚) |
| **全体が 1 画面に収まること** | **★満たさない。概算 約 1,389px**(ノート PC のビューポート高 ≒ 800〜900px) |

**★算術上の下限**＝ 5 節 ×(3 行 × 26.6px ＋ 見出し・余白 70px)＋ ヘッダ 約 150px ＋ フッタ 約 90px ＝ **図が 0 枚でも約 990px**。

**⇒ 「節 5 つ」「各節 3 行」「図 1 枚」を満たしたまま 1 画面へ収めることはできない。**

**★製造は分量を詰めた**(レビュー 中-4 の取り込み。概算 2,000px 超 → 約 1,389px)**が、これ以上は指示書が求める節そのものを落とすことになる**(芯の節 3 は チェックリスト `I-3` が保護し、残る 4 節はいずれも道順の一部)**。**

**⇒ 裁定をお願いしたい**——(a)「1 画面」を「1 スクロールで読み通せる長さ」の意味に改める ／ (b) 図を落とす(約 1,130px) ／ (c) 節を 3 つへ減らす。

**★★概算は CSS の寸法からの計算であり実測ではない**(製造もレビューも描画していない)**。⇒ 開発者の目視で確定してほしい。ただし上の「図 0 枚でも約 990px」は描画に依らない。**

### 2-4 ★★指示書 §3-2「`M37` の影響を受けていない 14 章を触らない」に対して、`ch03` の 1 文を変更した

| | |
|---|---|
| **何に反したか** | 指示書 §3 やらないこと **2**(「`M37` の影響を受けていない 14 章を触ること」) |
| **なぜ** | **開発者の明示指示**(§3-1)**。**`ch03:263` の「**ウィンドウをタスクトレイへしまってある状態から**」は、`M34-02` のトレイ常駐化により**しまうウィンドウが存在しない**ため現に誤りである |
| **実装** | `docs/usermanual/tacpendium-readme.html:266`。**1 文に限定した**。章ごとの差分を機械判定し、14 章のうち差分があるのは `ch03` だけであることを完了報告 §8.2 に示した |

**★`DES-005` に「タスクトレイへしまう」に相当する記述は 0 件である**(`grep` 実測)**。⇒ 設計書側の是正は不要。誤りは説明書本文にだけ在った。**

### 2-5 ★指示書 §2.5.5 が実在するとした資料が、**参照先を失っている**

**指示書の逐語**＝「★`docs/usermanual/combmgr-friend-readme.html` が実在する。⇒ 『短く読めます』『気づいたこと・不満は何でも言ってください（怒りません）』の調子で 4〜5 節。」

**実測**——同ファイルは **`M32-01`(`582106f`)で `tacpendium-readme.html` へ改名され、中身は `M32-02` が全面改稿した。⇒ 現在は存在しない。**

**★★製造の対応**——**git 履歴(`582106f^`)から旧本文を引いて声だけを写した。⇒ 内容は重ねていない**(チェックリスト `I-5`)**。**

**★同じ逐語が §2.5.4-1「黒い窓の注意は同書に在る。⇒ 書き直さず参照する」にも乗っており、そちらは §2-6 の問題になった。**

### 2-6 ★★指示書 §2.5.4 の節案 2 件が失効していた

| # | 指示書の案 | 実測 | 対応 |
|---|---|---|---|
| **1** | 節 1＝「★黒い窓の注意は `combmgr-friend-readme.html` に在る。⇒ 書き直さず参照する」 | **黒窓はもう出ない**(`M34-02` / `CHANGE-187`。`make build-windows` の `-H=windowsgui`)**。かつ参照先が無い**(§2-5) | **開発者の明示指示により、トレイ常駐と終了導線を書いた**(§3-1) |
| **2** | 節 2＝「**キャラクターを取り込む**。★リュウ以外は取込が要る。⇒ ここで詰まる人が多い」 | **キャラクター・技は `migrations/` の seed で投入済み**(`000003_seed_characters` 以降。`character_data/*.csv` は 31 本)**。⇒ 初回に取込は要らない。22 章にもその画面は無い**(`ch16` は**コンボの CSV 取込**であって別物) | **節 2 を「最初に聞かれることに答える」(初回起動ウィザードへ送る)へ差し替えた** |

**★レビューが 2 を独立に裏取り済み。**

---

## §3 製造の判断

### 3-1 開発者へ確認して確定した点

**★★★4 件とも本セッションの対話で確定しており、リポジトリ上に裁定行が無い。⇒ 受理の手番で記録をお願いしたい**(レビューの「不明事項 1」と同じ指摘)**。**

| # | 事項 | 確定内容 |
|---|---|---|
| 1 | 指示書 §7-1＝`ch20` の「発生」の食い違いに説明書で触れるか | **触れない**(設計卓の暫定案どおり)。⇒ `followup` の `punish-scan-response-startup-means-first-hit` と同じ面として §4 へ回す |
| **2** | **黒窓** | **「そもそも黒い窓は現在出さないようになっているので食い違いがあり修正が必要。`M34-02` で通知領域へのトレイ常駐を実装とともに黒枠除去された。逆にトレイ常駐の件を載せないといけない」**(開発者の逐語) |
| 3 | クイックスタートの導線 | **本編の前付け「本書の読み方」へ 1 行リンクを足す**(★22 章の本文には触れない) |
| **4** | **`M32-02` 作成後・マージ前に先行した 9 件への注意** | **`M30-07` / `M33-01` / `M37-01`〜`M37-07`。⇒ 製造はこれを起点に `CHANGE` の母集団を機械的に引き直した**(§2-1) |

### 3-2 推測で進めた点

| # | 判断 | 理由 |
|---|---|---|
| 1 | **修飾のバッジの説明を `ch05` にだけ置き、`ch06`/`ch07`/`ch10`/`ch11` では繰り返さない** | 読む面 5 つで見た目は同じであり、**読者が最初に出会うのが `ch05`** である(本書は「使い始める順」に並ぶ)。5 か所へ書くと同期先が 5 つになる(`E-76`)。**★レビュー 中-5(ii) が「`ch07`/`ch11` に送りが無い」と指摘したが不採用とした**——送りを足すと `ch06`/`ch10` にも同じ理由で要ることになる。**⇒ §4-4 の `UI の粗` 候補として回す** |
| 2 | **`ch17` の並び順を「直す」ではなく「足す」** | 着手前の本文に**旧い並び順の記述は 1 行も無かった**(チェックリスト `A-5` は旧記述の残存を見るので、そのままでも通る)。**⇒ よく使う操作が「戻す」であり「さっき消したもの」に辿り着けることが実益であるため足した** |
| 3 | **`CHANGE-199` のゲームパッド告知(コンボ/セットプレイの 2 面で既読と開閉を共有)を `ch11` ではなく `ch08` に収めた** | 指示書 §2.2 の表は `ch11` に `CHANGE-203` しか割り当てていない。**⇒ 1 か所に収めるほうが同期先が増えない。`ch11` は差分 0 とした** |
| 4 | **`ch05` の撮影の置き場を増やさない** | フィルタが 1 軸増えたが、2 枚目が既に「絞り込み欄を開いた状態」であり同じ枠に収まる |

---

## §4 設計担当が未把握の残課題・申し送り

> **★`followup-backlog.md` は 1 文字も編集していない**(`D-838`)**。⇒ 以下は設計卓が §J・本表へ転記するための原稿である。**

### 4-1 ★★`CHANGE-187` §5 の「`M32` への申し送り 2 件」が `M32-03` 指示書へ届いていなかった

| 項目 | 内容 |
|---|---|
| **スラッグ** | `change187-m32-handoff-not-picked-up` |
| **何が起きるか** | `CHANGE-187` §5 は「**`M32`(説明書)への申し送りが 2 件**——移行手順(ショートカットの作業フォルダを変えていた利用者は `config.toml` を exe の隣へ) ／ **「終了は通知領域のアイコンから」**。⇒ `M32` の起票時に拾う」と明記しているが、**`M32-03` 指示書 v1.1.0 はどちらも拾っていない** |
| **根拠** | `docs/change-notes/CHANGE-187-notification.md` §5 ／ `docs/instructions/M32-03-usermanual-revision-after-m37.md` 全文(逐語検索で `CHANGE-187` の言及 0 件) |
| **状態** | **終了導線は本サブで書いた**(開発者指示)**。★移行手順は書いていない**——射程外であり、**該当する利用者が実在するかは開発者の情報である** |
| **割付の候補** | **開発者**(移行手順の要否判断) ／ 不要と判断されれば畳む |
| **新規/更新** | **新規登録** |

### 4-2 ★★`ch03` / `ch04` にトレイ由来の追随余地が残る

| 項目 | 内容 |
|---|---|
| **スラッグ** | `usermanual-password-recovery-lacks-tray-route` |
| **何が起きるか** | `CHANGE-187` §2(f) は簡易パスワードの復旧導線として**トレイの「設定ファイルのフォルダを開く」**を用意したが、`ch03`(設定)・`ch04`(ログイン)は `config.toml` を「開いて直す」としか書いていない。**⇒ 利用者が `config.toml` の置き場へ辿り着く手順が本文に無い** |
| **根拠** | `docs/usermanual/tacpendium-readme.html` の `ch03` / `ch04` ／ `cmd/tacpendium/tray.go:37`(`trayLabelOpenConfigDir`) |
| **★過大に書かない** | **唯一の手段ではない**——`config.toml` は `AppBaseDir()`(配布では exe の隣)にあり、**exe のフォルダからも辿れる** |
| **割付の候補** | **`M32` の後続、または説明書の次の改稿**。★本サブでは 14 章不干渉のため当てていない |
| **新規/更新** | **新規登録** |

### 4-3 ★`ch20` の「発生」の食い違い(**裁定＝触れない**)

| 項目 | 内容 |
|---|---|
| **スラッグ** | `punish-scan-response-startup-means-first-hit`(**既存**) |
| **更新後の状態** | **【2026-09-15・`M32-03`】説明書では触れないことを開発者が裁定した。⇒ 本文に候補の数も「発生」の説明も無いことを実測で確認済み**(「発生」0 件 ／「候補」4 件はいずれも件数を述べていない)**。★`DES-005` §5.20 も「除外を利用者へ見せる表現は持たせていない」と明記しており整合する。⇒ 説明書側の残課題としては閉じる。実装側の食い違いは本行に残る** |
| **新規/更新** | **既存行の更新**(状態欄へ 1 行) |

### 4-4 ★`UI の粗` の追記候補 4 件(`D-382`)

> **★`M32-02` 完了報告 §4 の 28 件は 1 バイトも上書きしていない。以下は*追記*の候補である。**

| # | 章 | 粗 | 型 |
|---|---|---|---|
| **F-1** | `ch05` | **修飾の札と波括弧付き文字列が混ざる状態を、利用者が「キャッシュ再構築を押す」以外に解消できない。⇒ 画面側に「古い行が残っています」を示す手掛かりが 1 つも無く、説明書が `note` で肩代わりしている** | `M32-02` §4.5「本文が弁解を負った箇所」 |
| **F-2** | `ch08` | **「持続当て（始動技）」と修飾の「持続当て」が同じ画面に出るのに、画面上で区別が付かない。⇒ 括弧書きの 1 語だけが手掛かりであり、説明書が `caution` を 1 つ立てる必要があった** | 同上 |
| **F-3** | `ch08` | **入力方式の選択が保存もブラウザ保持もされない**(`CHANGE-196`)**。⇒ 毎回「通常入力」へ戻る。マス目で入れ続ける利用者は毎回 1 回多く押す** | 同上 |
| **F-4** | `ch07` / `ch11` | **修飾のバッジの説明が `ch05` にしか無く、`ch07`/`ch11` から辿れない**(レビュー 中-5(ii)。**製造は不採用としたが粗としては実在する**) | `M32-02` §4.2「先に別の画面を見ないと分からない」 |

### 4-5 ★指示書・チェックリストの失効記述 2 件(**是正は設計卓の手番**)

| # | 場所 | 現記述 | 実測 |
|---|---|---|---|
| 1 | 指示書 §2.3-1 | 撮影枚数の出所＝「`M32-02` レポート **§1-1**」 | 実際は **§3.4**。**★枚数 27 そのものは正しい** |
| 2 | 指示書 §2.5.5 / §2.5.4-1 | 「`docs/usermanual/combmgr-friend-readme.html` が実在する」 | **`M32-01`(`582106f`)で改名され、中身も `M32-02` が全面改稿した。⇒ 参照先を失っている**(§2-5) |

### 4-6 ★レビュー指摘のうち不採用 5 件の繰越

**★★「高」の不採用は 0 件である。⇒ 開発者確認を要する棄却は発生していない。**

| 指摘 | 優先度 | 不採用の理由 |
|---|---|---|
| 中-5(ii) `ch07`/`ch11` に `ch05` への送りが無い | 中 | 本書は「使い始める順」に並び `ch05` が前。送りを足すと `ch06`/`ch10` にも要ることになり 1 か所に収めた意味が薄れる。**⇒ §4-4 F-4 として粗に回した** |
| 低-1 章数「22」が 4 か所に散在 | 低 | `M32-02` から引き継いだ形。直すと**触っていない前付け・フッタ・14 章側へ差分が及ぶ** |
| 低-2 CSS トークンの二重化 | 低 | クイックスタートは配布物の中で**単独でも開ける必要がある**。共通 CSS を起こすのは `M32-01` が決めた置き場の構造の変更に当たり指示書 §3-3 で禁じられている |
| 低-3 退役 modifier 3 値への言及なし | 低 | `CHANGE-201` のとおり **3 値は選択肢から外れており利用者が新しく付けられない**。書くと「選べる」と誤読される |
| 低-5 `ch08` に修飾位置の非対称を 1 文 | 低 | チェックリスト `B-2` が「揃っていない」「不統一」と**書かないこと**を求めており、**言及する文を足すほどその線を踏む危険が上がる** |

---

## §5 参考(触れていない＝不変の証跡)

- **実装コード・`migrations/`・`docs/design/` 本体の差分 0 行**(`git diff --stat 96b01d6` は 5 ファイルのみ・すべて `docs/usermanual/` と `README.txt` と `docs/progress/`)
- **`go test` / `pnpm test` / `make e2e` は回していない**(指示書 §5・チェックリスト `C-4`。実装差分 0 行)
- **14 章のうち差分があるのは `ch03` のみ**(章ごとの機械判定。完了報告 §8.2)
- **8 章のうち `ch06` / `ch10` / `ch11` は差分 0**(「修飾」「波括弧」「バッジ」いずれも本文に 0 件)
- **`M32-02` の `UI の粗` 28 件は 1 バイトも触っていない**(`docs/progress/M32-02-completion-report.md` が変更統計に現れない)
- **`followup-backlog.md` の差分 0 行**(`D-838`)
- **`README.txt` は直接編集せず `check-dist-readme.sh --write` で再生成**(`D-755` 生成方式 (b))
- **新規ファイルの deletions 0**(`tacpendium-quickstart.html` は `+127 / -0`。教訓 `E-225`)
- 常設検査 `check-artifact-integrity` / `check-doc-refs` / `check-doc-inventory` / `check-dist-readme` / `check-stop-discipline` / `check-progress-log-index` がすべて **EXIT=0**
- `check-md-emphasis.sh` をファイル引数モードで新規 2 本へ実行し **0 行**(`D-775`)

---

## §6 CHANGE 起票のたたき台(設計担当向け)

**★番号は起票時に `change-number-registry` で採番してほしい。製造は自採番していない**(`D-293`)**。**

| # | 対象 | 内容 | 出所 |
|---|---|---|---|
| **1** | **`DES-002` §11.2**(配布アーカイブ構成) | **`manual/` へ `tacpendium-quickstart.html` を足す。**★あわせて `CHANGE-192` §2 の構成図も同じ形にする | §1-1 |
| 2 | `docs/usermanual/` の置き場規約(該当文書があれば) | クイックスタートの命名 `tacpendium-<種別>.html` と、図の接頭辞 `qs-` を明文化 | §1-2 |

**★マイグレは 0 本消費**(新規作成なし)**。**

**★★`.github/workflows/nightly-crossbuild.yml:105` のコメントが `CHANGE-192` 後の構成と食い違っている**(逐語＝「中身 = tacpendium.exe + README.txt」)**。⇒ 実装/CI 側の是正であり、上の 1 と同じ手番で拾えると取りこぼしが減る。★製造は CI を触っていない。**

---

## §7 教訓(retrospective 行き)

> **★親は本節を読まなくてよい。** 宛先は `retrospective-log` へのバッチ反映(実施者は設計担当)。

### 7-1 ★★★「追随サブ」は、対応表を作る前に**母集団を機械的に引く**

**★`M32-03` の指示書は `CHANGE-195`〜`209` の 15 本を母集団とし、8 本を名指しした。⇒ 実際に前サブの本文執筆後に着地したのは `CHANGE-192`〜`211` の 20 本であった。**

**★★★「前サブの完了」と「前サブの本文執筆」は別の時点である。** `M32-02` は 2026-09-12 に本文を書き、マージまでに 9 サブが先行した。**⇒ 指示書は「前提サブ＝`M32-02` の着地」と書いたが、追随が要るのは*着地*までの差分ではなく*執筆*以降の差分である。**

**⇒ 機構化**：追随サブの指示書を書く前に次を回す。**表を人手で作る前にこれを引くこと。**

```bash
git log --format="%h|%ad|%s" --date=short --diff-filter=A --name-only <前サブの本文最終コミット>..HEAD -- docs/change-notes/
```

**★一般形＝「差分を当てるサブ」では、*当てる差分の一覧*より先に*差分の境界*を確定させる。**

### 7-2 ★★★一次源が `CHANGE` でも、**画面に出る語は実装を引く**

**★製造は `ch05` の絞り込みの中立ラベルを `CHANGE-209` §4 から写した。⇒ 同節の「指定なし」は*状態の説明語*であって表示語ではなく、実装は `comboList.filter.all` ＝「全て」であった**(レビュー 高-1)**。**

**★★しかも同じ `ch05` の 4 段落下で、同じ中立ラベルを「全て」と書いていた。⇒ 本文内で 2 通りになっており、自分の書いた 2 段落を並べて読めば気づけた。**

**⇒ 機構化**：**利用者向け文書で「」に入れる語は、`web/src/locales/ja.json` を引いて確定する。** `CHANGE` 通知書の本文は*設計の説明*であり、表示語の正本ではない。

**★同型が 2 件あった**——「レシピキャッシュ再構築」(正＝`settings.data.cacheRebuild` ＝「キャッシュ再構築」。「レシピキャッシュ…」は完了 toast の文言)**。⇒ 1 件ではなくパターンである。**

### 7-3 ★★UI の**挙動**も、書く前に実装を読む

**★製造は `CHANGE-200` の「保存前の補完と `onBlur`」から「区分だけを選んで数字の欄を空のままにしておくと…」と書いた。⇒ `ComboEditorBasicFields.tsx:655-672` は通常入力モードで数値欄を描かず(区分ボタンのみ)、その状態は画面上に存在しない**。

**★★`onBlur` は `MassPercentInput` にしか渡っておらず、区分を押した場合は `setPositionBand` が*その場で*代表値を入れる。⇒ 「欄から離れたときに入る」は別の経路の話であった。**

**⇒ 一般形＝`CHANGE` 通知書は「何が変わったか」を書くが、「利用者にどう見えるか」は書かない。⇒ 見えかたを書くなら描画側のコードを読む。**

### 7-4 ★★上限が複数あるときは、**同時に満たせるかを先に算術で確かめる**

**★指示書 §2.5.3 は「節 5 つ」「各節 3 行 ＋ 図 1 枚」「1 画面」を課したが、下限を計算すると図 0 枚でも約 990px であり、最後の条件を満たせない。⇒ 製造は書き上げてからレビュー指摘で気づいた。**

**⇒ 機構化**：**分量の上限が 2 つ以上ある指示書を受けたら、着手前に下限を計算して矛盾が無いか確かめ、あれば計画段階で報告する。**(本サブは計画段階で「節 5 つ・図 1 枚」を決めており、**そこで掛け算していれば分かった**。)

### 7-5 ★失効した参照は、**改名の手番では消えない**

**★`M32-01` が `combmgr-friend-readme.html` を改名し、`M32-02` が中身を全面改稿した。⇒ `M32-03` 指示書(2026-09-14 発行)は、その 2 サブの*後*に書かれながら、旧ファイルが「実在する」と書いていた。**

**★★`followup` の `usermanual-filename-still-combmgr` は 2026-09-12 に「解消＝畳む」と記録され、「旧名を名指しする箇所は生きている参照が 0 件」と結論している。⇒ *その後に書かれた文書*が旧名を復活させた。**

**⇒ 一般形＝「参照 0 件」は*その時点*の事実であり、以後の新規文書には効かない。⇒ 畳んだ `followup` 行の読み替えは `playbook` §4.63 に恒久化されているが、指示書の書き手がそれを引かなければ同じことが起きる。**

---

*以上*
