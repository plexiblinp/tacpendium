# M24-09c 設計伝達レポート（例外レポート）

| 項目 | 内容 |
|------|------|
| **対象** | **親チャット（設計卓）** |
| 発信 | 製造担当 Claude Code / 2026-08-26 |
| 指示書 | `docs/instructions/M24-09c-e2e-suite-stabilization.md` **v1.2.0** |
| 実装コミット | ブランチ `claude/m24-09c-implement-plan-lb6g5r` ／ **7 コミット**（`8d44c58` 〜 **`83eca89`**。着手基点 `c8c7330`）。**`claude/` 名前空間のため push 済み**（main への反映は開発者のマージ） |
| **★開発者の実機確認** | **2026-08-27 に取得済み。180 passed / flaky 0 / failed 0 / 3 分以内。受容可能と判定された**（**§16**） |
| 関連 | 完了報告 `docs/progress/M24-09c-completion-report.md` ／ レビュー `docs/progress/m24-09c-review.md`（重大 0 / 高 2 / 中 5 / 低 4・**往復 0 回**で完了） |
| CHANGE | **消費 0 件**（§7.4.1 の 6 条件を 1 件ずつ判定した結果） ／ **マイグレ消費 0 本** ／ **新規依存 0 件** |

本レポートは **①独自確定仕様 ②契約違反の独自判断 ③製造判断 ④残課題** に絞る。指示書どおりの部分は割愛する。

> **★★最重要は §4-1（本番コードのバグ）である。** **`VAL-C02` が防ぐはずの状態が実際に作れた。** 決定論的な再現手順つき。**`POST /api/combos` は `DES-002` §4.2 の経路表の API であり、CHANGE 起票の要否は設計卓の手番である。**
>
> **★§2 に 2 件ある**（決定論 spec の本数が指示書明文と違う ／ 指示書 §1.1 の分類が実測と違う）。**受理／却下の裁定を要する。**

---

## §1 製造が独自に確定した実装仕様（DES 反映が要るもの）

> **★本サブは `DES-002` §4.2 の経路表に載る API を 1 本も新設・変更していない**（`internal/` の差分 **0 バイト**）。**⇒ playbook §4.29 の API 逐語ルールは対象外。**

### §1-1 ★E2E スイートの走らせ方を「直列」に確定した（`SUPP-001` §5.5 の射程）

**`web/playwright.config.ts` に `workers: 1` を追加した。** 以後、`make e2e` は **`Running 180 tests using 1 worker`** で走る。

**★確定した内容（`SUPP-001` §5.5 に明文が無いため、製造が確定させた）:**

| 項目 | 確定値 | 根拠 |
|---|---|---|
| **worker 数** | **1（固定）** | 従来は未指定＝Playwright 既定 `"50%"`＝`Math.max(1, floor(os.cpus().length * 0.5))`（`playwright/lib/common/index.js:587` / `:678-682`）。**⇒ CPU コア数依存であり環境ごとに並列度が違っていた** |
| **使い捨て DB** | **1 スイート 1 個・全 worker 共有のまま**（変更なし） | worker ごとの DB 分離は採らなかった（§3-2 参照）。**⇒ `CLAUDE.md` §5 の「使い捨て DB ＋ 専用ポート」の記述は失効しない** |
| **専用ポート** | **47390 / 5273 のまま**（変更なし） | `web/playwright.config.ts:36-37` |
| **実行時間** | **既定（2 worker）の約 1.54 倍**（248s → 386s） | 起動は約 5 秒で worker 数に依らず一定。伸びるのはテスト実行部分だけ |

**⇒ `SUPP-001` §5.5 へ「E2E は直列（`workers: 1`）で走る」と明文化するかどうかは設計卓の判断である。** **★製造の判定は「現状は同節が worker 数に一切触れていないため、失効する記述は無い＝CHANGE 不要」**。**明文化する方針なら CHANGE が要る。**

### §1-2 ★「`起動時間 + テスト実行時間 × N`」は上限であって推定値ではない

**指示書 §4.2.1 が示した伸び幅の式は、実測と合わなかった。**

| | 式による予測（N=2） | 実測 |
|---|---|---|
| 直列化の倍率 | **2 倍** | **1.54 倍** |

**★理由＝`fullyParallel: false` は「ファイル単位」でしか並列化しないため、長いファイルが律速して並列度を使い切れない。** **⇒ worker 数が多い環境ほど、直列化の代償は N 倍より小さくなる。** **★同じ式を次のサブで使う場合は「上限」と読むこと。**

---

## §2 契約・設計に反する独自判断（★受理／却下の裁定を要する）

### §2-1 ★★決定論 spec を「1 本」ではなく「2 本 ＋ 共通ヘルパ 1 本」にした

**指示書は 4 か所で「1 本」と書いている**——§1.3-2（`:95`）／ §4.3（`:304`）／ §5.1（`:343`＝「★これが本サブで唯一追加する spec である」）／ §7.1-3（`:385`）。

**★しかし同じ §4.3（`:310`）が、その spec の形をこう指定している**（逐語）:

> **「2 つの spec が同じデータ空間を奪い合う」ことを再現する形であること。**

**★★この 2 つは同時に満たせない。** `fullyParallel: false` が直列化するのは**ファイルの中**であり、**1 ファイルの中の 2 テストは決して同時に走らない。** **⇒ 1 ファイルでは「ファイルをまたいだ干渉」を再現できない**——再現できるのは「同じテストを 2 回走らせる」形（＝冪等性）だけであり、**それはチェックリスト 4-2 が「重大」と判定する形**である。

**⇒ 製造の判断**: **形の要求（§4.3 `:310`・チェックリスト 4-2）を優先し、本数の明文（4 か所）に反した。**

| 実装 | パス |
|---|---|
| probe A | `web/e2e/aa-interference-probe-a.spec.ts`（35 行・本体 3 行） |
| probe B | `web/e2e/aa-interference-probe-b.spec.ts`（35 行・本体 3 行） |
| 共通ヘルパ | `web/e2e/support/interference-probe.ts`（194 行。**`D-553` が運用の型として承認済みの置き場**） |

**★増えたテストケースは 2 件**（178 → 180）。**シナリオ spec は 1 本も足していない**（§1.3-2 の趣旨は守っている）。

**⇒ 設計卓へ**: **受理いただけるなら、指示書テンプレート側で「決定論 spec は 2 本 1 組になる」を既定として書いておくと、次に同型のサブを立てるとき本数の明文と形の要求が衝突しない。**

### §2-2 ★★指示書 §1.1 の分類が実測と食い違っていた（`D-557` の前提が 1 つ崩れる）

**指示書 §1.1（`:58-61`）の表は `M24-09b` の観測を `flaky` と分類している。** **★しかし `M24-09b-completion-report.md:101` の逐語は「両方とも retry #1 でも赤」である**——**§4.1.1 が固定した定義では `fail` に当たる。**

**⇒ `D-557` が立てた見立て**（逐語＝**「同じ現象を違う語で数えていた可能性がある」**「**開発者は `fail` を、製造 2 サブは `flaky` を見ており**」）**は、少なくとも `M24-09b` については成り立たない。クラウド実行環境でも `fail` は出ている。**

**★これは §4.1.1（3 語の定義を固定した節）の動機そのものに関わるため、§3 ではなく §2 に置く。** **⇒ 指示書 §1.1 の表の補正は設計卓の手番。**

---

## §3 製造の判断

### §3-1 開発者へ確認して確定した点

| # | 問い | 開発者の回答 | 反映 |
|---|---|---|---|
| 1 | `make e2e` を 10 回以上回すため E2E ポートを占有してよいか（§2.4・`D-548`） | **占有してよい** | 実測 1〜4 ＋ 完了証明 ＋ 破壊確認を直列に実行した。**実測はすべて他レーンと重ねていない** |
| 2 | 同じ症状の未着手記録が 8 件ではなく 9 件あった。9 件目をどう扱うか | **射程に含める** | §4.4 の結末表を **9 行**にした。**followup 本体は編集せず**、束ねへの追加は本レポート §4-2 で候補として上げる（`D-382`） |
| 3 | 開発者ローカルの CPU コア数（伸び幅の予測に使う） | **「調べ方を教えてください。devContainer なので物理コア数とは一致しないはず」** | 調べ方を提示した（§4-6）。**値は未取得のため、伸び幅の予測は本レポートに載せていない** |

### §3-2 推測で進めた点（指示書 §9.2 が推測を許容している範囲）

| # | 推測 | 内容と根拠 |
|---|---|---|
| 1 | **決定論 spec の具体的な形** | **「同じ識別キーの本登録コンボを 2 ファイルが取り合う」形にした。** 識別キーは `characterId + starterMoveId + position + opponentStance + hitType + opponentSize` の 6 項で **DB 全体で一意**（`internal/service/combo/service.go:1020` `duplicateKeyOf`）。**★`m23-05` と `m23-09` が実際にこの衝突を避けるためキーをずらしている**（`m23-09` spec 冒頭の注記）ので、**人工的な干渉ではなく実在の型を再現している** |
| 2 | **対象キャラに `zangief` を選んだ** | **全 51 spec を走査して、どの spec も触っていないことを確認した**（既存 spec は全て明示 `code` 指定であり、インデックス選択は 0 件）。seed 済みでもある。**⇒ probe が他 spec を巻き添えにせず、他 spec も probe を壊さない** |
| 3 | **ファイル名を `aa-` 始まりにした** | Playwright はファイルをパス順に worker へ割り当てるため、**先頭 2 本にしておくと worker が 2 つ以上のとき必ず同時に走り出す。** **★「本 2 本より前へソートされる名前を作らないこと」を spec のコメントへ明記した**（ASCII 順で数字・大文字は小文字より前） |
| 4 | **`PROBE_HOLD_MS = 3000`** | 前置き（キャラ解決＋始動技の決定）は実測 1 秒未満であり、**3 秒はその 3 倍以上の余白**。**★これを超えるほど環境が遅くなると偽陰性（並列なのに緑）が起きうる旨と、その場合は本値を上げる旨をコメントへ残した** |
| 5 | **`test.describe.configure({ retries: 0 })` を probe へ入れた** | **レビュー高-2 の指摘による。** スイート既定は `retries: 1` であり、**retry を許すと `workers` を戻す変更が入っても「1 回目は赤・retry で緑」＝ flaky 扱いで終了コードが 0 になる**＝検出器が黙って効かなくなる |

---

## §4 設計担当が未把握の残課題・申し送り

> **★`followup-backlog.md` の本表は製造が編集しない**（`D-382`）。**⇒ 設計卓が迷わず登録できる形で書く。**

### §4-1 ★★【新規登録】本番コードのバグ — `VAL-C02` の check-then-act 競合

| 項目 | 内容 |
|---|---|
| **スラッグ候補** | `combo-val-c02-check-then-act-race` |
| **何が起きるか** | **同一識別キーの本登録コンボが 2 件同時に存在する。** `VAL-C02`（「同一キャラ・同一レシピ・同一状況のコンボが既に存在します」）が防ぐはずの状態である |
| **再現条件** | `web/playwright.config.ts` の `workers: 1` を外し、`pnpm e2e e2e/aa-interference-probe-a.spec.ts e2e/aa-interference-probe-b.spec.ts --workers=2 --retries=0` を繰り返す。**10 回中 1 回の頻度で、両方の `POST /api/combos` が 201 を返し、双方が `duplicates` を 2 件観測する**（`Expected: 1 / Received: 2`） |
| **根拠（機序）** | **検証が `BeginTx` の前に走る**（`internal/service/combo/service.go:253-279`）。**加えて 6 項識別キーに対する UNIQUE 制約が `migrations/` に存在しない**（レビュー担当が独立に全走査して確認）。**⇒ 2 つの要求が同時に判定を通過し、双方が INSERT できる** |
| **なぜ今まで踏まなかったか** | **単独利用では同時要求が起きない。★ただし `M22` が LAN・多人数モードを入れたため、2 クライアントから到達しうる** |
| **なぜ製造が直さなかったか** | **指示書 §2.3 の 3 条件のうち条件 2 が不成立。** `POST /api/combos` は **`DES-002` §4.2 の経路表に載る API** であり、**CHANGE が要る**＝止めて報告（§1.3-3 / §9.3） |
| **割付の候補** | **`M24-CLOSE` では早い。** 多人数モードの信頼性に関わるため、**独立サブか `M25` 相当**を推す。**★設計判断は設計卓の手番** |
| **あわせて見ていただきたい** | **`DES-006` の `VAL-C02` の記述**。**判定を「保証」と読める書き方があるなら、並行要求下では保証されないことになる** |

### §4-2 ★★【既存行の更新】束ねが 8 件ではなく 9 件である

| 項目 | 内容 |
|---|---|
| **対象スラッグ** | 束ね行 `e2e-flake-seven-records-have-no-bundle-target`（`followup-backlog.md:800`） |
| **更新内容** | **`e2e-shared-global-resource-parallel`（`:348`）を束ねの 9 件目として加える** |
| **根拠** | **同じ症状・未着手・5 サブ連続で観測**。**★`docs/` 配下 26 ファイルから参照されており、9 件中で最多である。** **★★同行は恒久策の候補として `workers: 1` を自ら名指ししていた**（逐語＝「**残るのは恒久策の選択——`workers: 1` ／ 資源ごとの排他フィクスチャ ／ 上限テストの相対化**」） |
| **更新後の状態** | **本サブで解消見込み**（完了報告 §6 の 9 行目）。**★畳むときは束ね行と 9 件すべてを同時に**（片方だけ残さない＝指示書 §10） |

### §4-3 ★【既存行の更新】束ね行 `:800` の内部矛盾と、独立行を持たない 1 件

| 項目 | 内容 |
|---|---|
| **(a)** | **束ね行 `:800` の割付列が `★開発者の承認待ち`（`P-41`）のまま**だが、内容列の冒頭は `【2026-08-25 決着・D-554】…承認した`。**`docs/process/parallel-board.md:712` では `P-41` は解消済み**（取り消し線＋`【2026-08-25 解消・D-554】`）。**⇒ 同一セル内で状態が食い違っている** |
| **(b)** | **9 件のうち `e2e-flaky-one-per-run-persists` だけが followup に独立行を持たない**（`grep -c` = 1＝束ね行の中に埋め込まれているのみ）。**⇒ 畳むときに取りこぼしやすい** |

### §4-4 ★★【新規登録】`CLAUDE.md` §5 の E2E 記述が実態と食い違っている

| 項目 | 内容 |
|---|---|
| **スラッグ候補** | `claude-md-e2e-claims-no-dev-impact` |
| **何が起きるか** | **`CLAUDE.md:158`** は E2E を「使い捨て DB + 専用ポート…の独立スタックで実行され、**dev DB・dev サーバに影響しない**」と説明しているが、**E2E スイート自身が `PUT /api/config` を踏み、リポジトリ直下の `config.toml`（dev と同じ実ファイル）を再シリアライズしてコメントを落とす** |
| **根拠** | **`Makefile:97-101` が自認している**（逐語＝「**さらに E2E スイート自身が `PUT /api/config` を踏んで config.toml を再シリアライズするため、コメントが全部落ちて `[defaults]` が追加される**」）。書き戻し先は `cmd/combomgr/main.go:81` の `configPath = "config.toml"`。**51 spec 中 10 本が `PUT /api/config` を撃つ** |
| **★注記** | **本サブの変更が原因ではない。既存の齟齬である。** **★`CLAUDE.md` は開発者のファイルであり、製造も設計卓も編集しない**（指示書 §7.4.1 条件 6 の定めどおり報告のみ） |
| **★あわせて** | **「使い捨て DB ＋ 専用ポート」の記述自体は本サブ後も真である**（worker ごとの DB 分離は採らなかったため失効しない） |

### §4-5 ★【新規登録】`retries: 1` が無注釈で、それを事後正当化する記述が 25 ファイルに生えている

| 項目 | 内容 |
|---|---|
| **スラッグ候補** | `e2e-retry-absorption-normalized-in-acceptance-criteria` |
| **何が起きるか** | **「落ちても retry で通るから正常」と次の担当が読む経路が残っている** |
| **根拠（母集合を宣言して走査）** | **母集合 1,241 ファイル**（`docs/` 1,185 ＋ `web/e2e/*.ts` 55 ＋ `Makefile` 1）。**「flaky は仕様／正常／想定内」の明言 2 件 ／「retry で吸収」趣旨 25 ファイル。** **★★うち 2 件は「制度化された場所」に入っている**——`docs/instructions/phase3/M17-03-stage2-ui.md:182`（**受入条件**）と `docs/instructions/phase3/reviews/M17-03-review-checklist.md:81`（**レビュー合格基準**） |
| **★書き換えていない理由** | **完了済みマイルストーンの指示書は当時の記録として残す**（`D-535`）。**挙がったことだけを報告する** |
| **★源** | **`web/playwright.config.ts:78` の `retries: 1` には理由コメントが 1 行も無い。** 25 件はすべてこの無注釈の設定を事後的に正当化する形で書かれている。**本サブでは `workers: 1` の側に理由を書いた** |

### §4-6 ★★【開発者の手番】ローカルでの `make e2e` 1 回（**製造の手が届かない**）

> **★★【2026-08-27 決着】開発者の実機確認が取れた。§16 を参照。** **本項は解消済みである。**
> 以下は 2026-08-26 時点の記述として保持する。

**`M24-overview` §4.4 の原理**（**機械が判定できないものだけが開発者の手番**）**に照らすと、本サブは `M24-09b` と同類で「実機確認は不要」である**（本番コード 0 バイト・画面変更なし）。**★ただし 1 件だけ (b) 機械に手が無い ＋ (c) 環境差 に該当する。**

**★★`D-557` の動機は開発者ローカルの `fail` だったが、製造は 4 コア / 2 worker のクラウド環境しか持たず、そこでは対策前でも 5 回中 flake 0 件だった。⇒ 製造は開発者が見ている症状を一度も再現していない。**

**さらに `os.cpus()` は `/proc/cpuinfo` 由来でホストの論理 CPU 数を返し、cgroup の CPU quota を見ない。** **⇒ devContainer に CPU 上限があるとオーバーサブスクライブが起き、Playwright の 30s timeout を突き抜けやすくなる（＝干渉とは独立の第 2 の要因）。これは未検証である。**

**見るところ 3 つ**: (1) 1 行目が `Running 180 tests using 1 worker` か (2) 緑になり「個別にやり直す」が消えたか (3) 総実行時間が許容できるか（**ローカルは worker 数が多いぶん伸び幅はクラウドの 1.54 倍より大きい**）。

**★コア数は `node -e "console.log(require('os').cpus().length)"` と `nproc` / `cat /sys/fs/cgroup/cpu.max` を突き合わせる**（**食い違えば第 2 の要因が実在したことになる**）。**★`workers: 1` を入れた後は `make e2e` の 1 行目からコア数は読めない**（常に `using 1 worker` になるため）。

### §4-7 ★未解消のまま残す 2 件（`M24-CLOSE` で行き先を確認）

| スラッグ | なぜ未解消か |
|---|---|
| `e2e-sqlite-write-contention`(a) | **並列書き込みが消える点は説明が付くが、`busy_timeout` は `M23-10` で既に是正済み**（`internal/infra/db/db.go:38` が DSN で 5000 を指定）。**⇒ 本記録が書かれた当時の前提は既に変わっており、残りが何なのか特定できていない** |
| `e2e-flaky-combo-post-500` | **`POST /api/combos` の 500 は干渉だけでは説明しにくい**（チェックリスト 7-3 が警告する型）。**当時サーバ側ログが採取されておらず原因は未断定。⇒ 再現したらまずサーバログを採ること。★ただし §4-1 のバグが同じ経路の 500 を説明しうるので、両者の関係は要調査** |

---

## §5 参考（触れていない＝不変の証跡）

- `git diff --stat c8c7330..HEAD` ＝ **7 ファイル / 992 insertions / 0 deletions**（**新規ファイルに deletions 無し**＝`E-225`）。
- **禁止面の変更ファイル数 0**: `web/src` ／ `internal` ／ `cmd` ／ `migrations` ／ `.github` ／ `CLAUDE.md` ／ `.claude/commands`。
- 専用ポート不変: `web/playwright.config.ts:36-37`（`47390` / `5273`）。
- 既存 spec **51 本は 1 本も書き換えていない**（判定基準 1 を 0 本で満たした）。
- `go test ./... -count=1` **FAIL 0 件** ／ `pnpm test -- --run` **184 ファイル / 1,881 テスト passed**（着手前と同一）。
- `check-artifact-integrity.sh` / `check-md-emphasis.sh` / `check-doc-refs.sh` / `check-progress-log-index.sh` **すべて exit 0**。

---

## §6 CHANGE 起票のたたき台（設計担当向けチェックリスト）

**本サブの消費は 0 件である。** 以下は**設計卓の判断次第で起票が要る**もの。

| # | 起票が要る条件 | 対象 | 製造の判定 |
|---|---|---|---|
| 1 | **`VAL-C02` の check-then-act 競合を是正する方針を採る場合** | `DES-002` §4.2（`POST /api/combos`）／ `DES-006`（`VAL-C02`）／ 場合により `DES-003`（UNIQUE 制約の追加） | **§4-1。製造は直していない。要否は設計卓** |
| 2 | **「E2E は直列（`workers: 1`）で走る」を明文化する場合** | `SUPP-001` §5.5 | **現状は同節が worker 数に触れておらず失効する記述が無いため「不要」と判定した**（§1-1） |

- **★番号は起票時に registry で採番**（`docs/handover/change-number-registry.md` §1）。
- **マイグレは新規作成していない**（消費 0 本）。**⇒ 連番の払い出しは無い。**

---

## §7 教訓（retrospective 行き）

> **★親は本節を読まなくてよい。** 宛先は `retrospective-log` へのバッチ反映で、実施者は設計担当。

1. **★★計測器そのものが壊れていないかを、使う前に対照実験で確かめる。** 本サブのポート確認は `ss -ltn | grep` で書いたが、**環境に `ss` が無く、grep が常に空振りして「空き」と誤答していた。** **⇒ 5 回分の証跡が無効になった。** **一般形＝「無いこと」を報告する検査は、対象を実際に置いて「有ることを検知できる」ことを先に示す**（`D-375` の対照実験の要求を、テストだけでなく**計測スクリプト**へも広げる）。**★`ss` / `lsof` があると仮定しないこと。**
2. **★★撤回は本文とコードコメントを同じ手番で直す。** 完了報告 §10.1 で撤回した主張が、**取り込み前まで `playwright.config.ts` のコメントに事実として残っていた**（レビュー高-1 が検出）。**コメントは実験より前に書き、実験で予測が外れた後に直し忘れた。** **⇒ 「予測を書いたコメント」は、その予測を検証した時点で必ず読み返す。**
3. **★★回帰の検出器には `retries: 0` を宣言する。** スイート既定が `retries: 1` のままだと、**検出器が捉えるべき変更が入っても「1 回目は赤・retry で緑」＝ flaky 扱いで終了コード 0 になり、検出器が黙って効かなくなる。** **⇒ 「落ちたら赤で止まる」ことを構造的に保証してから、検出器だと名乗る。**
4. **★★flake 対策の成否は「緑の回数」で判定できない。** 本サブは**対策前も既定 worker で 3/3 緑**であり、5 回連続緑は必要条件にすぎなかった。**⇒ 確率を決定論へ翻訳する**（並列 10/10 赤・直列 10/10 緑）**まで、根拠は揃わない。**
5. **★「速いか」を比べるときは、成功した 1 回の時間ではなく目的に到達するまでの時間を比べる**（`D-557` が既に言語化）。**★あわせて「`起動時間 + テスト実行時間 × N`」のような式は上限であって推定値ではない**（実測 1.54 倍 vs 式 2 倍）。**並列化の効率は分割の粒度で決まる。**
6. **★指示書の「本数」と「形」が衝突することがある。** §4.3 は「spec を 1 本」と「2 つの spec が同じデータ空間を奪い合う形」を同時に要求していたが、**`fullyParallel: false` の下では 1 ファイルで後者を満たせない。** **⇒ 決定論的な干渉 spec は 2 本 1 組になる。テンプレート側の既定にしておくと衝突しない。**
7. **★見立てを「証拠」として報告へ書く前に、1 件ずつ裏を取る。** 本サブは Plan Mode で挙げた干渉の証拠 3 件が**すべて実測・実装確認で否定された**（`VAL-S04` はコンボ単位 ／ `memo` はラベルであり検索キーではない ／ `defaults.presetId` の衝突は 5 回とも緑）。**⇒ 裏取り前の見立ては「仮説」と明示し、報告本文へ断定形で載せない。**

---

# 追補（2026-08-27 ／ 指示書 **v1.4.0** ・ `CHANGE-135` **v1.1.0** ・ 裁定 **`D-569`** 後）

| 項目 | 内容 |
|------|------|
| 実装コミット | 段 1 + 段 2 ＝ `777ee1f`（push 済み）／ **段 3 ＝ 本追補と同じ手番のコミット** |
| 指示書の版 | **リポジトリ内は v1.2.0 のまま**。v1.3.0 / v1.4.0 と `CHANGE-135` は**別ブランチで反映**されるため、**製造は参照のみ**（開発者の明示指示） |
| 実測の正本 | 完了報告 `docs/progress/M24-09c-completion-report.md` **§16〜§24** |

## §8 裁定 `D-569` への追従（**やったこと**）

| 裁定 | 追従 |
|---|---|
| **`embed_web` 撤回・`vite preview` 採用** | `web/vite.config.ts` に `preview.proxy` を 1 か所、`web/playwright.config.ts` の `webServer[1].command` を `pnpm build && pnpm exec vite preview` へ。**構成もポートも変えていない** |
| **`resolveApiTarget()` を再利用（第 2 の解決規則を書かない）** | 同ファイル内の既存関数をそのまま呼んだ |
| **`cmd/` は 1 か所のまま・`launchBrowser` に触らない（13-6b 重大）** | **`c8c7330` の定義と逐語一致（13 行）。1 文字も差分が無い**ことを `diff` で示した（完了報告 §20） |
| **§12.1.3-4 撤回（13-2 の判定が反転）** | `readDevBackendPort()` は **dev の `config.toml` を読み続ける**。揃えていない |
| **CI に触らない（§6-3・§12.9）** | `.github` の差分 **0 ファイル** |
| **`SUPP-001` を製造が編集しない（§6-2）** | `docs/design/` の差分 **0 ファイル** |
| **`VAL-C02` を本サブで直さない（§12.6）** | 直していない |
| **`e2e-does-not-exercise-production-static-serving` を本サブで直さない（§7）** | 直していない |

## §9 ★裁定 §8 の宿題への回答 —— 「分からない」

**386〜399 秒 → 307〜309 秒（約 20%）の原因は、本サブの変更に帰属できない。**

| 切り分け（どちらも `pnpm dev` ・ `workers: 1` ・ 180 テスト・同一セッション） | 実測 |
|---|---|
| 段 1 のみ（段 2 の spec 書き換えを一時的に外した） | 180 passed / **399.8 秒** |
| 段 1 ＋ 段 2 | 180 passed / **402.4 秒** |

**差 2.6 秒（0.6%）。⇒ 段 2 は要因ではない。** 段 1 は起動時の設定ファイル読取 1 回が変わるだけで**機序が無い**。

**★あわせて、指示書が与えた母数 307〜309 秒そのものが今日は再現しなかった**（今日の `pnpm dev` は 402 秒）。
**同じ環境が別時点で 307〜309 と 400 を出しており、環境側の変動幅のほうが差より大きい。**

**⇒ 推測を断定形では書かない。「分からない」が回答である。**

## §10 ★同一セッション内の背中合わせ比較（**これだけが根拠になる**）

| 配信方式（同一 HEAD・`workers: 1`・180 テスト） | 実測 |
|---|---|
| `pnpm dev` | **402 秒** |
| `pnpm build` + `vite preview` | **223 / 225 / 253 秒**（一発緑 **3 / 3**） |

**約 1.6〜1.8 倍速。** `pnpm build` の上乗せは **20〜27 秒**。

> **★以前この卓へ出した「`vite preview` 156〜159 秒」「`embed_web` 156〜158 秒」は再現しなかった。** 当時の測り方の記録が残っていない。**⇒ 速度を根拠に使うときは本節の値を引いていただきたい。**
> **★ただし裁定 `D-569` は速度ではなく影響範囲で決めており、この訂正は裁定の前提を崩さない。**

## §11 【既存行の更新】`SUPP-001` §4.5 の齟齬は **2 件**（**§6-2 で受理済み。設計卓の手番**）

| # | 記述（`docs/design/supp-001-detailed-design.md`） | 実装 |
|---|---|---|
| 1 | `:573`「dual webServer array + **`reuseExistingServer: true`**（既存サーバ再利用と自動起動を両立）」 | `web/playwright.config.ts:138` / `:161` で **両方 `false`** |
| 2 | `:572`「**`resolveBackendPort()`** が起動時に `config.toml` を読み、**バックエンドの実ポートを動的取得**」 | 関数名は **`readDevBackendPort()`**。役割は **worktree のポートオフセット決定**であり、バックエンドのポートは **`COMBOMGR_PORT`** で明示的に渡す |

**★2 は `D-569` §12.1.3-4 の撤回と対になる。** 「`config.toml` を読む」こと自体は**残す**のが正しい（`scripts/wt-new.sh` の worktree ごとのポート分離がそれに依存する）。**直すべきは「何のために読むか」の説明である。**

## §12 【新規登録の候補】`vite preview` 化に伴って生じた申し送り

> **★`followup-backlog.md` の本表への登録は設計卓の手番**（`D-382`）。**製造は §J にしか書けない**ため、ここへ候補として置く。

| # | 候補 | 中身 |
|---|---|---|
| 1 | **`e2e-does-not-exercise-production-static-serving`**（裁定 §7 で既に起票済み） | `vite preview` は Go の embed による**本番の静的配信経路を通らない**。**その経路は E2E で一切検査されないままである**（`embed_web` を撤回した代償）。**★本サブでは直さない** |
| 2 | **★【新規】E2E のビルドは `web/playwright.config.ts` にしか無い** | `pnpm build` を `Makefile` へ移すと **`cd web && pnpm e2e` を直接叩いた経路が古い `dist` を検査し、しかも緑で通る**。**破壊確認 3-(b) で再現済み**（完了報告 §19.3）。**移設を禁じる注記を `Makefile` とハーネスの両方へ置いたが、機械検査は無い** |
| 3 | **★【新規】`getByRole` の `name` は既定で部分一致である** | 破壊確認の対象に**文字を追記する**形の検査は**動かない**。**本サブで実際に空振りした**（完了報告 §19.3・1 回目）。`/add_e2e_spec` か testid 規約側へ 1 行足す価値がある |

## §13 ★教訓（追補分・`retrospective-log` 行き）

8. **★★「製造の環境では踏めない」ことが構造的に 2 度起きた。** ① **事実 A** ＝ クリーンな clone には `config.toml` が無いので、dev 設定による汚染を踏めない。② **`launchBrowser`** ＝ クラウドでは `xdg-open` が失敗し warn ログだけになる。**⇒ どちらも「3 回緑だった」ことが「問題が無い」ことの証拠になっていなかった。★緑は環境の証明にしかならない。**
9. **★★破壊確認は「壊した対象が実在するか」だけでなく「壊すと観測が動くか」まで示す。** `確定反撃サーチ` の**末尾に 1 文字足した**破壊確認が **180 passed** で返り、**危うく「反映されている」と読み違えるところだった**。原因は `getByRole` の `name` が**既定で部分一致**であること。**⇒ 置換でやり直して初めて対照になった**（`M-78` ／ playbook §4.32 の一般形を、UI 文字列の破壊確認へも広げる）。
10. **★比較の基準は、自分が触る前ではなく「比べたい変化の直前」に取る。** 破壊確認 1 の副作用（E2E → dev の汚染）を、**自分の `sed` より前のバックアップと比べてしまい**、実行後の差分を誰が書いたか分けられなくなった。**★本サブで 2 度目である**（1 度目は `config.toml` の md5）。
11. **★母数は「指示書に書いてあるから使える」ものではない。** 与えられた母数 307〜309 秒が**今日は再現せず 402 秒だった**。**⇒ 別時点の値どうしを引き算しない。同一セッション内の背中合わせだけを根拠にする。**

---

## §14 追補分のレビュー結果（**重大 0 / 高 3 / 中 3 / 低 3 ・ 9 件すべて採用 ・ 往復 0 回**）

詳細と採否は `docs/progress/m24-09c-review.md` の「追補分のレビュー（2026-08-27）」と、その下の製造トリアージ表。
**「高」の不採用は 0 件のため、開発者エスカレーションは発火していない。**

**★設計卓に関係する 2 件だけここへ再掲する。**

### §14-1 ★★裁定 `D-569` の理由付けに 1 件、成り立たないものがあった（レビュー M-8）

裁定が `embed_web` の代償として挙げた 11 か所のうち、**「React dev→prod で StrictMode の二重実行が消える」は `vite preview` でも同じく起きる。**
`vite preview` が配るのは `vite build` の成果物であり、React は production モードで動くためである。

**⇒ これは 2 案を分ける論点ではない。** **採った案でも失う検査である。**

**★裁定の結論（`vite preview` を採る）は変わらない。** 分ける論点として残るのは
① **`launchBrowser` の運用障害** ＝ 抑止手段が無く、抑止には本番コードの 3 か所目が要る。
② **静的要求が全ミドルウェアを通り、ページ遷移ごとに余計な DB 参照とアクセスログが出ること**。

**★製造は裁定の理由付けをそのまま報告へ書き写しており、1 件ずつ裏を取っていなかった**（本サブ §7 教訓 7 の再演）。**完了報告 §17.2 に訂正を入れ、§23 の代償表へ計上した。**

### §14-2 【既存行の更新】`CHANGE-135` の射程は **`SUPP-001` §4.5 へ広がる**（13-12。レビュー M-9）

| 節 | 判定 |
|---|---|
| **§5.8**（設定ファイルの配置場所） | **既に射程内。広がらない** |
| **§4.5**（E2E 基盤の実装メモ） | **★広がる。** 配信方式が **dev サーバ → `vite preview`（ビルド済み `dist`）** に変わり、**同節が扱う対象そのものが変わる**。**§11 の齟齬 2 件と同じ節である** |
| **§5.5**（テスト最低ライン） | **広がらない**（無効化しない）。**ただし §13 条件 5 の宿題＝「E2E は直列で走る」を明文化するかは未決のまま。段 3 で足すなら「フロントはビルド済み `dist` を配る」も同じ手番になる** |

**★どちらも製造は編集していない**（§6-2 受理済み・13-11）。

---

## §15 追補分の教訓（**§13 の続き。`retrospective-log` 行き**）

12. **★★「撤回した」は、本文・コード注記・索引のすべてを同じ手番で直して初めて成立する。** 完了報告で「再現しなかった」と申告した数字が、**同じコミットで入れたコード注記に「実測・同一環境」の断定形で残っていた**（レビュー H-5）。**★本サブで 2 度目である**——1 度目（取り込み前の高-1）で**この教訓を自分で言語化した直後に、自分でもう一度破った。** **⇒ 「撤回」を書いたら、その値の出現箇所を `grep` で全数当たること。記憶で「直した」と判断しない。**
13. **★裁定であっても、報告へ書き写す前に 1 件ずつ裏を取る**（§14-1）。**★これは §13 教訓 7（見立てを証拠として書く前に裏を取る）の対象が「自分の見立て」から「上流から降りてきた理由付け」へ広がった形である。** **上流の文書は、正しさの根拠にはなるが、事実の検証を免除しない。**
14. **★索引（`progress-log`）の既存行は、後の作業が偽にしても機械検査に掛からない。** `check-progress-log-index.sh` は**追記の欠落**を見るが、**既存行の陳腐化は見ない**（レビュー H-4）。**⇒ 追補で前提が変わったら、索引の当該行を名指しで訂正する手番を明示的に取ること。**
15. **★実機確認を依頼するときは、手順の 1 行目に「HEAD の確認」を置く。** 開発者の 1 回目の実機確認が赤になった原因は**実装ではなく pull 漏れ**だった（§16-3）。**★製造のブランチは `claude/` 名前空間で push されるが、開発者のローカルが自動で追随するわけではない。** **⇒ 「`git log --oneline -3` で `<hash>` が出ることを確認してから回してください」を依頼文へ入れること。** **★あわせて、赤の報告を受けたときは「手順の問題」ではなく「自分の実装の穴」を第一候補に置き、自分の主張の前提（本件では `config.toml.example` に `[defaults]` が無いこと）を実査してから切り分けを返すこと。**


---

## §16 ★★【決着】開発者の実機確認（2026-08-27）—— **§4-6 が解消した**

**★これが本サブで唯一、製造の環境では構造的に取れなかった証拠である。**

| 項目 | 内容 |
|---|---|
| 条件 | **画面から既定キャラを「ガイル」・既定プリセットを「2」に設定した状態**のまま `make e2e` |
| 結果 | **180 passed。`flaky` 0 件 ／ `failed` 0 件** |
| 実行時間 | **3 分以内** |
| 開発者の判定 | **「これぐらいなら許容可能」** |

### §16-1 何が証明されたか

1. **事実 A の根治**（13-5 が「根治の唯一の証明」と定める項目）。**既定キャラが `ryu` 以外のまま全件緑になった。**
   **★これは以前 35 件がまとめて落ちた条件そのものである**（当時は juri）。
2. **`defaults.presetId` も同様に切り離されている。** **プリセット 2 は本サブで一度も試していない条件**だったが影響が出なかった。
3. **実行時間が受容範囲であること。** **★これは製造が決められない項目だった**（`roles-and-routing` のハード列＝好みが決め手）。
   **★開発者機（24 コア）は製造のクラウド環境（223〜253 秒）より速い。** **⇒ `workers: 1` の代償は、`vite preview` 化によって開発者の受容線を下回った。**

### §16-2 ★製造の 3 回緑は、この証拠の代わりにならなかった

**クリーンな clone には `config.toml` が無く、`config.toml.example` に `[defaults]` も無いため、製造は事実 A を踏めない。**
**⇒ 製造がどれだけ緑を積んでも「dev の設定が E2E を汚さない」ことの証明にはならず、開発者機で回して初めて取れた。**

**★§13 教訓 8 の「緑は環境の証明にしかならない」が、そのまま実演された形である。**

### §16-3 ★手順上の落とし穴が 1 件あった（**教訓 15 として計上**）

**1 回目の実機確認は赤だった。** 原因は**実装ではなく、ローカルが `777ee1f` 以降を pull できていなかったこと**である。
**⇒ 段 1 の仕組み（`COMBOMGR_CONFIG_PATH`）自体がまだ手元に無い状態で回していた。**

**★製造は「手順が悪かったのではないか」と問われた時点で、自分の実装の穴を第一候補として扱い、`config.toml.example` に `[defaults]` が無いことを実査したうえで、切り分けコマンド 4 本（HEAD ／ 段 1 の有無 ／ 段 3 の有無 ／ 実行直後の専用 config の中身）を返した。**
**⇒ 1 往復で原因が確定した。**

---

## §17 ★`DES-002` §4.2 の経路表について（**追補では新設・変更なし**）

**追補（段 1〜段 3）は API 経路を 1 本も新設・変更していない。**

- 段 1 が足したのは **環境変数 `COMBOMGR_CONFIG_PATH`** であり、**HTTP 経路ではない**。
- 段 2 は **E2E spec のみ**の変更（`web/e2e/` 配下）。
- 段 3 は **E2E ハーネスとフロントのビルド設定のみ**（`web/playwright.config.ts` ／ `web/vite.config.ts` ／ `Makefile` の注記）。

**⇒ 本節に逐語で埋めるべき DTO・エラー契約・母集団の述語は無い。**

**★ただし `VAL-C02` の競合（§4-1）は `POST /api/combos` の話であり、経路表の API である。** **本サブでは直していない**（§12.6 で射程外・別サブへ）。**as-built の反映が要るかどうかは、そのサブの手番になる。**

