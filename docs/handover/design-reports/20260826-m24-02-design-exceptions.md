# M24-02 設計伝達レポート（フィルタ・タグ・キャラ選択 — 並び順は指示書の分岐表に反して実装した）

| 項目 | 内容 |
|------|------|
| 対象 | **親チャット（設計卓）** |
| 発信 | 製造担当 Claude Code / **2026-08-26** |
| 指示書 | **v1.2.0**（開発者から本セッション中に差分で受領） ／ チェックリスト **v1.2.0**。**★リポジトリ内の `docs/instructions/M24-02-filter-tag-character-selection.md` と `reviews/M24-02-review-checklist.md` は v1.1.0 のままである**（§4-4） |
| CHANGE / マイグレ | **`CHANGE-133`**（設計卓が起票済み。**製造は CHANGE 番号を 1 件も消費していない**） ／ **★マイグレ 1 本消費＝`000079`**（§6） |
| 実装コミット | ブランチ `claude/m24-02-version-check-n2gk7m` / `8e0704c` 〜 **`d84afb4`** の **16 本**。基点 **`428b707`**（PR#110 マージ後の main）。**うち 2 本は開発者確認を受けた追補**〔`1c35272` 追補1・`d84afb4` 追補2〕。**★全数 push 済み**（`claude/` 名前空間。正史 main への反映は開発者のマージ） |
| 関連 | 完了報告 `docs/progress/M24-02-completion-report.md` ／ レビュー `docs/progress/m24-02-review.md`（**末尾に自動トリアージの採否記録あり**） ／ モック `docs/progress/M24-02-mock/m24-02-screens.html` |
| **★手動確認** | **★★実施済み（2026-08-26・開発者）。全項目通過**。 **4 項目**＝ドロップダウンの見え方〔**問題なし**〕／キーボード操作〔**確認項目の説明が足りず伝わっていなかった。手順を完了報告 §13.1 へ補った**〕／要約ピル〔**★2 件の要望が出て追補 2 本で対応。再確認も問題なし**〕／キャラの並びと `舞` の表示〔**期待どおり・インゲームと一致**〕。 **★要望の内容**＝(1) ピルを見出し行の隣へ〔逐語「少しでも縦の枠を確保するのが目的」〕 ／ (2) ピルを `{軸名}: {値}` へ揃える。**あわせて (3)「隠す」を従来どおり行の右端へ**（追補2） |
| 作成条件 | **実装直後の同一セッション内で生成**。実査 16 件 ／ レビュー往復 **1 回**（重大 0・高 6・中 7・低 4／**高の不採用 0 件＝エスカレーションの発火なし**）／ 破壊確認 **4 件** |

本レポートは **①独自確定仕様 ②契約違反の独自判断 ③製造判断 ④残課題** に絞る。
指示書どおりの部分（タグフィルタのドロップダウン化・折りたたみ・タグ管理の検索・`CHANGE-130` の残ゲート・分岐 A のフロント実装）は割愛した。

**★★最重要は §2-1 と §2-2。どちらも設計卓の裁定が要る。**

- **§2-1 = 指示書 §4.3.1 の分岐表に反して並び順を実装した**（漢字 1 件でも実装した）。**根拠は開発者裁定である。**
- **§2-2 = その結果として §4.3.1 の「英語なら a〜z」を満たしていない。** **ラベルが ja 固定であるあいだ、同要件は原理的に満たせない。**

---

## §1 製造が独自に確定した実装仕様（DES 反映が要るもの）

**API 経路の新設・変更は無い**（分岐 A。`internal/` の本番 Go コード差分 0 バイト）。**⇒ `DES-002` §4.2 は射程外。** 以下はフロントと seed の as-built である。

### 1-1. `DES-005` §4.3 の「コンボ一覧の絞り込みフィルタのみ native `<select>` を維持」が失効した

**逐語（現行 `DES-005` §4.3）**:
> 全系統（マイコンボ/新規登録-比較追加モーダル/設定・初回/技マスタ編集）が本共通プルダウン〔`CharacterSelector`〕を用いる＝M12-04/N-29 で統一完了。**コンボ一覧の絞り込みフィルタのみ native `<select>` を維持**（隣接フィルタ群との一貫性優先・コンテキスト選択とは別役割）

**as-built**:

| 面 | 変更前 | 変更後 |
|---|---|---|
| 一覧のヘッダ帯 | **独自の native `<select>`**（`ComboListPage.tsx:184-210`。`M24-01` がフィルタ欄から移した） | **共有 `CharacterSelector`**（`ComboListPage.tsx:186`） |
| 取込ヘルパー step1 | **独自の native `<select>`（値が `code`）** | **共有 `CharacterSelector`**（`IntakeHelperPage.tsx:174`。`id ⇄ code` の変換は呼び出し側） |
| 状況フィルタ 7 項目 | native `<select>` | **native `<select>` のまま**（変えていない） |

**⇒ `DES-005` §4.3 へ次を明文化してほしい**: **キャラ選択は全面が共有 `CharacterSelector` を通る**（native `<select>` を維持する面は無い）。**「隣接フィルタ群との一貫性」で残していた例外は、`M24-01` がキャラ選択をフィルタ欄の外へ出した時点で前提を失っている。**

### 1-2. キャラ選択部品の as-built（`DES-005` §4.3 へ）

| 確定した点 | 根拠 |
|---|---|
| **形＝`Popover` ＋ 検索 `Input` ＋ `<ul role="listbox">`** の手組みコンボボックス。**Radix `Select` はやめた** | `web/src/components/SearchableSelect.tsx` |
| **検索対象は `name_ja` / `name_en` / `code` の 3 つ** | `CharacterSelector.tsx:59`。E2E が `code` 検索で 1 件に絞れることを主張（`m24-02-*.spec.ts`） |
| **★並べ替えのキーは `name_ja` 固定**（ロケールで変えない） | `web/src/lib/character-sort.ts`。**§2-2 を参照** |
| **★`↑` `↓` での候補移動は自前実装**（Radix `Select` から `Popover` へ移すと消えるため） | `SearchableSelect.tsx` の `handleListKeyDown`。テスト 4 件 |
| **★`role="combobox"` の `aria-controls` は Radix が Popover content〔`role="dialog"`〕を指す。中の `<ul role="listbox">` とは別要素である** | ARIA 1.2 のコンボボックスパターンとは厳密には一致しない。直すなら独自実装が要る |
| **適用は 13 コントロール / 11 ファイル** | `grep -rn "<CharacterSelector" web/src --include=*.tsx \| grep -v test` |

### 1-3. 一覧のフィルタ欄の as-built（`DES-005` §5.4 へ）

| 確定した点 | 根拠 |
|---|---|
| **タグフィルタ＝ドロップダウン ＋ 検索欄 ＋ 選択件数バッジ。** 閉じた状態でも件数が見え、ドロップダウン内に解除行がある | `ComboListFilters.tsx` の `SearchableSelect`（`data-testid="combo-list-tag-filter"`） |
| **フィルタ欄は折りたためる。既定は開いた状態。** 部品は新規登録と同じ `CollapsibleFieldset` | 同上 |
| **★閉じていても「効いている」ことが分かる**——有効な軸をピルで常時表示（`summary` が開閉に関わらず出る既存仕様を使い、新機構を作っていない） | `data-testid="combo-list-filter-summary"` |
| **★ピルの書式は `{軸名}: {値}`**（開発者確認 2026-08-26 の要望）。**値のラベルは各 `<select>` の `<option>` と同一の式から採る**（第 2 の語彙を作らない） | 完了報告 §13.2 |
| **★ピルは見出し行と同じ行に置く**（`CollapsibleFieldset` の任意 prop `summaryPlacement="inline"`）。**常用域で 1 行ぶん（24px）縦が縮む** | 完了報告 §13.2 の実測表 |
| **★★inline のヘッダ行は「見出し / 要約 / トグル」の 3 分割で、トグル（「隠す ⌄」）は行の右端**（追補2）。**見出しがボタンの外へ出るため、トグルへ `aria-labelledby={見出しid + トグル文言id}` を付けてアクセシブル名を組み立てている**——**無いとトグルの名前が「隠す」だけになり、どの節のトグルかが読み上げから落ちる** | `CollapsibleFieldset.tsx` の inline 分岐 ／ 完了報告 §13.2 |
| **★挙動の差分 1 件**——**見出し文字のクリックでは開閉しない**（押下領域はトグル部分のみ）。**開発者確認で現状のままでよいと判定された** | 完了報告 §13.2 末尾 |
| **★ソートも要約に出す**（`sort !== DEFAULT_SORT_FIELD` のとき別枠のピル）。**折りたたむとソートの `select` も畳まれるため** | `data-testid="combo-list-filter-summary-sort"`。**レビュー 中-1 で追加** |
| **開閉状態は localStorage `combo-list-filters-collapsed-v1` で保持**（台帳 #10 へ登録済み） | `useFilterPanelCollapsed.ts` |

**⇒ `DES-005` §5.4 へ上記 5 点を明文化してほしい。**

### 1-4. タグ管理画面の as-built（`DES-005` §5.12 へ）

**検索欄 1 つ（タグ名のみ対象）＋「N 件 / 全 M 件」の件数 ＋ 0 件の 2 状態の区別**（そもそも無い／絞り込みで 0 件）。
**⇒ §5.12 の表示項目に「検索欄」と「絞り込み結果の件数」を足してほしい。**

### 1-5. ★キャラ表示名の突合（`docs/seed-data/check-criteria.md` §7 に規約を新設した）

**現行 19 体を `name_ja` 単位で突合した。19 ＝ 不一致 1 ＋ 一致 16 ＋ 不明 2。**

| 分類 | 対象 |
|---|---|
| **不一致 1** | **`mai`：`不知火舞` → `舞`**（`000079` で是正） |
| **不明 2** | **`ingrid` / `c_viper`**——**SF6 の playable キャラではなく（`000014` の classic5 取込由来）、インゲーム表示が存在しない** |
| **別枠で 1 列** | **`m_bison` の `name_en`（`M. Bison`）**——ピリオド後の空白の有無を確認できない。**`name_ja`（`ベガ`）は一致** |

**★`combomgr-importer/dist/` は作業ツリーに無く、公式データとの機械突合はできなかった**（製造も設計卓と同じ制約下にある）。
**★表示名の出所は migrations だけである**——`character_data/*.csv` はキャラ表示名の列を持たず（`name_ja` は**技名**）、取込経路 `internal/repository/character/upsert.go` の `UpsertByCode` は `ON CONFLICT DO NOTHING` で既存の表示名を上書きしない。**⇒ migration で直せば次の取込で戻らない。**

**⇒ 不明 3 件の裁定をお願いしたい**（§4-1）。

---

## §2 契約・設計に反する独自判断（★最優先・裁定が要る）

### 2-1. ★★指示書 §4.3.1 の分岐表に反して並び順を実装した

| 項目 | 内容 |
|---|---|
| **何に反したか** | **指示書 v1.2.0 §4.3.1** の分岐表——「**`name_ja` に漢字を含むキャラが 1 件以上 ⇒ 並び順は実装せず、件数と該当キャラを報告する**」。**チェックリスト v1.2.0 §0.3-2 / §4-7 も、未実装を欠陥と判定しない前提で書かれている** |
| **実測** | **漢字を含むのは 1 件（`mai`）。** ⇒ 字句どおりなら **未実装が正しい結末**だった |
| **なぜそう判断したか** | **開発者裁定（2026-08-26・本セッション。逐語）**＝**「例外1件を許容して実装。」／「漢字キャラは一番下に並べていいです。」** |
| **実装がどうなっているか** | `web/src/lib/character-sort.ts`（`sortCharactersByLocale`） ／ `CharacterSelector.tsx:55` が表示時に適用 |
| **★禁じられた回避はしていない** | **読み仮名のマップをフロントへ持たせていない。読み仮名の列も足していない。** どちらも設計卓の判断である旨は指示書どおり |
| **★受容挙動は固定済み** | `character-sort.ts` の冒頭ブロック ＋ `character-sort.test.ts` の 2 件（漢字は末尾 ／ ラテン文字は先頭）。**後任が「バグ」と読んで自前の読み仮名マップを足すのを防ぐため** |

**★あわせて、指示書が挙げていない第 2 の落とし穴を報告する**——**`name_ja` がラテン文字で始まるキャラは仮名より前に来る**（実測 2 件＝`c_viper` = `C.ヴァイパー` / `jp` = `JP`）。`Intl.Collator("ja")` の実測:

```
C.ヴァイパー / JP / イングリッド / ガイル / キンバリー / ケン / ザンギエフ / ジェイミー /
ジュリ / ダルシム / テリー / ベガ / マノン / マリーザ / ラシード / リュウ / リリー / ルーク / 舞
```

**⇒ 受理して §4.3.1 の分岐表を改訂するか、却下して並び順を外すかの裁定をお願いしたい。**

### 2-2. ★★§4.3.1 の「英語なら a〜z の昇順」は満たしていない

| 項目 | 内容 |
|---|---|
| **何に反したか** | **指示書 v1.2.0 §4.3.1**——「**日本語なら あ〜ん の昇順（`name_ja`） / 英語なら a〜z の昇順（`name_en`）**」「**i18n の現在の言語に従う**」 |
| **なぜ満たせないか** | **`CharacterSelector` は言語に関わらず `name_ja` を表示する。** **これは M24-02 以前からの振る舞いであり、`name_en` を表示している画面は 1 つも無い**（`grep -rn "nameEn" web/src` の本番ヒットは型定義・照合・検索対象のみ） |
| **初回実装で何が起きたか** | **§4.3.1 の字句どおり `name_en` をキーにした。⇒ 英語 UI では「日本語名のリストが、見えない英語名の順に並ぶ」＝利用者の目には無順序に見える状態になった**（レビュー 高-5 が検出） |
| **どう着地したか** | **開発者裁定（2026-08-26）＝「表示している値で並べる」。** ⇒ `sortCharactersByLocale` から**ロケール引数を外し、`name_ja` 固定**にした（引数に取ると「表示と並びがずれる」余地が残るため）。汎用 `sortByLocale` は primitive として残しテストも維持している |
| **モックの扱い** | **「変更後・英語」列を「参考図（実装していない）」へ改めた**——**開発者はモックで形を承認するため、承認した形と as-built が違う状態を残さない** |

**⇒ 裁定をお願いしたい。二択である。**
1. **§4.3.1 の「英語なら a〜z」を撤回する**（ラベルが ja 固定であることを前提に据える）
2. **ラベルもロケールに従わせる**（英語 UI では `name_en` を表示する）。**★`M24-07`〔用語・i18n〕の射程に接する。** エディタ・ウィザード・設定など**固定 ja の面**にも及ぶため、`DES-005` §5.13 の i18n サーフェス境界の再確認が要る

---

## §3 製造の判断

### 3-1. 開発者へ確認して確定した点

| # | 論点 | 開発者回答（逐語 or 要旨） |
|---|---|---|
| 1 | 並び順を実装するか（漢字 1 件） | **「例外1件を許容して実装。」「漢字キャラは一番下に並べていいです。」** |
| 2 | キャラ選択の適用範囲 | **「実質全数に揃える」** |
| 3 | `000079` で直す範囲 | **「mai の 1 件だけ直す」**（不明 3 件は保留・推測で直さない） |
| 4 | 英語ロケールの並び（§2-2） | **「表示している値で並べる」** |
| 5 | モックの形 | **「モックの形で構いません」**（実装前に承認を得てからコードに触った） |

### 3-2. 推測で進めた点

| # | 判断 | 理由 |
|---|---|---|
| 1 | **ドロップダウンの土台に `Popover` を選んだ**（`ColumnVisibilityMenu` と同じ `DropdownMenu` ではない） | **Radix `DropdownMenu.Content` は typeahead を持ち、中にテキスト入力を置くと検索語の打鍵を奪う。抑止すると矢印キー移動も死ぬ。** 検索欄付きの実績は付与側 `TagSelector` にある。**`cmdk` は未導入で、入れると新規依存の提案になる**（`CLAUDE.md` §6）。**⇒ 新規依存 0 件。** 見た目・作法は `ColumnVisibilityMenu` に揃えた |
| 2 | **折りたたみ状態に新キーを作った**（既存キーへ相乗りしない） | **`combo-list-filters-v1` の格納値は「生の URL クエリ文字列」**であり、相乗りさせると開閉状態が URL に載る。**`combo-list-columns-v1` は `ColumnVisibility`（7 boolean）の型そのもの。** 台帳 #10 へ登録済み・脚注に理由あり |
| 3 | **`activeFilterKeys` を `useComboListFilters` から公開した** | `hasActiveFilters` と要約表示の源を 1 つにするため（`E-76`。実際 M19-06 で 3 軸が増えている）。**ソートは足していない**——同関数は「フィルタを解除」で消える軸の集合でもあり意味が違うため、要約側で別枠にした |
| 4 | **`CollapsibleFieldset` へ制御モード等の任意 prop を足した** | 開閉状態の保持に必要。**既存 6 か所の呼び出しは 1 行も変えずに従来どおり動く** |
| 5 | **タグ管理画面の新規文言 4 本を locale へ入れなかった** | **同画面は元から i18n を通っていない**（`t()` 呼び出し 0）。**新規文言だけ `t()` にすると画面内で 2 系統が混ざる。** ⇒ 既存に合わせた。**画面ごと i18n へ載せるのは `M24-07` の手番**（§4-3） |
| 6 | **`ComboExportPage` の「キャラクター ID」欄は揃えなかった** | **ID を手打ちさせる欄であり「選ばせる UI」ではない。** 候補を並べていないので「増えると使えなくなる」機序に当たらない（§4-2） |

---

## §4 設計担当が未把握の残課題・申し送り

**★`followup-backlog.md` の本表は製造が編集しない**（`D-382`）。以下は設計卓が畳むための候補である。

### 4-1. `character-display-name-unverified-three`（**新規登録**）

- **何が起きるか**: **キャラ表示名の突合で 3 件が「不明／要確認」のまま残っている。** 次に seed を投入する担当が同じ判断を再度迫られる。
- **根拠**: 完了報告 §5 ／ `docs/seed-data/check-criteria.md` §7。**`ingrid` / `c_viper` は SF6 の playable キャラではなくインゲーム表示が存在しない**（`migrations/000014` の classic5 取込由来）。**`m_bison` の `name_en`（`M. Bison`）はピリオド後の空白の有無が確認できない。**
- **割付の候補と理由**: **開発者のドメイン確認**（`roles-and-routing` のハード列＝ドメイン知識）。**または `combomgr-importer/dist/` を参照できるセッション**。
- **新規登録／既存更新**: **新規登録。**

### 4-2. `combo-export-character-id-is-raw-input`（**新規登録**）

- **何が起きるか**: **エクスポート画面がキャラを「ID の手打ち」で指定させる。** 他の 13 面が検索付きコンボボックスに揃った結果、**この面だけが取り残されている。**
- **根拠**: `web/src/pages/ComboExportPage.tsx:202-211`（`<Label htmlFor="characterId">キャラクター ID</Label>` ＋ 数値 `<Input>`。`range === "filter"` のとき表示）。**レビュー 中-2 が検出。**
- **割付の候補と理由**: **`M24-07` ではなく別サブ。** 揃えるなら「ID 直打ちをやめて選択にする」という**画面仕様の変更**であり、**`DES-005` §5.13 の表示項目に触れる**（本サブの §2.1 一覧の外）。
- **新規登録／既存更新**: **新規登録。**

### 4-3. `tag-management-page-not-i18n`（**新規登録**、または既存の i18n 行の更新）

- **何が起きるか**: **タグ管理画面が丸ごと非 i18n である**（`t()` 呼び出し 0）。**本サブが足した 4 本も直書きにした**（画面内で 2 系統が混ざるのを避けたため）。
- **根拠**: `TagManagementPage.tsx:93,95,101` ／ `TagListTable.tsx:46,53`。該当 4 本＝`タグ名で検索`（placeholder / aria-label）／`{n} 件 / 全 {m} 件`／`「…」に一致するタグはありません。検索条件を変えてください。`／`検索を解除`。
- **割付の候補と理由**: **`M24-07`（用語・i18n）。** **★`M23-01` の「ゴミ箱画面は i18n を通っておらず生の日本語文字列である」（`D-487`）と同じ型である。** **⇒ 既に `M24-07` へ同種の行があるなら、そちらへ合流させるほうがよい。**
- **新規登録／既存更新**: **★要確認**（同型の行が既にあるなら更新）。

### 4-4. `m24-02-instruction-set-stale-in-repo`（**新規登録**・**設計卓の手番**）

- **何が起きるか**: **リポジトリ内の三点セットが v1.1.0 のままである。** **⇒ リポジトリだけを読む後任には §4.8（マイグレ `000079`）が存在せず、§4.3.1 の分岐表が as-built と逆に読める。**
- **根拠**: `docs/instructions/M24-02-filter-tag-character-selection.md:6` ／ `docs/instructions/reviews/M24-02-review-checklist.md`。**v1.2.0 は本セッション中に開発者から差分で受領した**（アップロード）。
- **あわせて `CHANGE-133` の失効 1 行**: `docs/change-notes/CHANGE-133-notification.md:15` の「**スキーマ変更 なし。マイグレ消費 0 本。新規依存 0。**」——**`000079` を 1 本消費した。スキーマ変更 0 列・新規依存 0 は正しい。**
- **★さらに版数の写し漏れが 6 件**（v1.1.0 時点で検出）: `CHANGE-133:12` の「関連」が指示書・チェックリストとも v1.0.0 ／ チェックリスト §0.1-1・§12.1 が v1.0.0 ／ チェックリスト §5-2 が「E2E 3 ケース」（実物 4）／ 指示書 §7.1-1・チェックリスト §1-3 が「実査 11 件」（実物 16）／ 指示書 §7.2 が「破壊確認 3 件」（実物 4）。
- **割付の候補と理由**: **設計卓**（製造は指示書・チェックリスト・CHANGE の版を上げない）。
- **新規登録／既存更新**: **新規登録。**

### 4-6. `filter-value-labels-hardcoded-ja`（**新規登録**、または既存の i18n 行の更新）

- **何が起きるか**: **一覧フィルタの 4 軸（`position` / `hitType` / `opponentStance` / `setupTechType`）のラベル定数がハードコード日本語**であり、**英語 UI でも日本語が出る。** `<select>` の `<option>` でも要約ピルでも同じ。
- **根拠**: `web/src/constants/combo-list.ts`（`POSITION_LABELS` / `HIT_TYPE_LABELS` / `OPPONENT_STANCE_LABELS`） ／ `web/src/constants/oki.ts`（`OKI_TECH_TYPE_LABELS`）。**どちらのファイルにも `useTranslation` / `t(` の import が無い。**
- **★本サブでは直していない**——ピルだけ英語化すると画面内で 2 系統が混ざる（`option` は日本語・ピルは英語）。
- **★方針変更を伴う**: `constants/oki.ts` はヘッダに「**本ファイルが起き攻めラベルの単一の正典**。従来 editor・出力・詳細/比較(i18n) に散在していたラベルを集約し、表記揺れを防ぐ」と明記しており、**i18n 化はこの集約方針の見直しになる。**
- **割付の候補と理由**: **`M24-07`（用語・i18n）。** **★`§4-3`（タグ管理画面が非 i18n）・`M23-01` の「ゴミ箱画面が非 i18n」（`D-487`）と同じ型であり、まとめて扱うのが経済的。**
- **新規登録／既存更新**: **★要確認**（同型の行が既にあるなら更新）。

### 4-7. `filter-summary-pills-wrap-at-full-filters`（**新規登録・低**）

- **何が起きるか**: **8 軸すべてを効かせた状態では、`{軸名}: {値}` のピルが 2 行へ折り返し、`summaryPlacement="inline"` の縦の節約が相殺される**（実測: 開 195 → 197px ＝ **+2px**）。**常用域（0〜2 軸）では −24px。**
- **根拠**: 完了報告 §13.2 の実測表（Chromium）。
- **★要望 1（縦を節約）と要望 2（値を出す）のトレードオフである。** 値を出すぶんピルが長くなる。
- **割付の候補と理由**: **将来（未割付）。** 直すなら「折り返し時に "+N 件" へ畳む」等だが、**8 軸同時指定は稀な状態**であり急がない。**開発者の判断待ち**（完了報告 §13.2 に「気になる場合は連絡いただきたい」と書いた）。
- **新規登録／既存更新**: **新規登録。**

### 4-5. `searchable-select-aria-controls-points-to-dialog`（**新規登録・低**）

- **何が起きるか**: **`role="combobox"` の `aria-controls` が `<ul role="listbox">` ではなく Popover content（`role="dialog"`）を指す。** ARIA 1.2 のコンボボックスパターンと厳密には一致しない。
- **根拠**: `web/src/components/SearchableSelect.tsx` の `PopoverTrigger asChild`。**Radix `PopoverTrigger` の仕様であり、直すなら独自実装が要る。** レビュー 低-4。
- **割付の候補と理由**: **将来（未割付）。** **13 コントロール全部に効く**ため、直すなら支援技術での実測とセットで。
- **新規登録／既存更新**: **新規登録。**

---

## §5 参考（触れていない＝不変の証跡）

- `docs/design/` の差分 0 バイト（`git diff --stat 428b707 -- docs/design/` が空）
- `internal/` の本番 Go コード差分 0 バイト（差分は新規テスト `migrate_m2402_test.go` +89/-0 のみ）／ `cmd/` 差分 0 バイト
- `internal/service/notation/` ／ `internal/service/preset/` 差分なし（契約 F-1 の凍結パッケージ）
- `/trash` 系 差分なし（M23 の成果物）
- `web/src/features/combo/components/ColumnVisibilityMenu.tsx` 差分 0 バイト（形式を借りただけ）
- `web/src/features/tag/components/TagSelector.tsx` 差分 0 バイト（付与側の振る舞い不変）
- `migrations/` の既存ファイル差分 0 バイト（`000079` の新規 2 件のみ・`D-535`）
- `characters.code` 不変・技名不変を主張するテスト: `TestRun_M2402_CodeAndMoveNamesUntouched`
- `DES-005` §5.5 の除外を守るテスト: `MyComboPage.test.tsx`「★キャラ以外は持ち込まない」（破壊確認 3 で赤を確認）
- `DES-005` §4.3.1 の解決順を守るテスト: `defaultCharacter.test.ts`「段の列挙は 1 か所にある」（不変）
- `queryKey` の綴り不変（`web/src` の差分に `queryKey` の行が 1 行も無い）
- 新規 16 ファイルすべて deletions 0（`git diff --diff-filter=A --numstat 428b707`）

---

## §6 CHANGE 起票のたたき台（設計担当向けチェックリスト）

**本サブは `CHANGE-133` の射程内であり、製造は CHANGE 番号を 1 件も消費していない。** 以下は反映時の確認項目。

| # | 項目 | 状態 |
|---|---|---|
| 1 | **`DES-005` §4.3** — native `<select>` 維持の例外が失効（§1-1） | **要反映** |
| 2 | **`DES-005` §4.3** — キャラ選択部品の as-built（§1-2） | **要反映** |
| 3 | **`DES-005` §5.4** — フィルタ欄の as-built 5 点（§1-3） | **要反映** |
| 4 | **`DES-005` §5.12** — タグ管理の検索と件数（§1-4） | **要反映** |
| 5 | **`DES-005` §5.4** — ブラウザストレージキーの新設（`combo-list-filters-collapsed-v1`）。**指示書 §7.4.1-6 が `CHANGE-133` の射程内と明記** | **要反映** |
| 6 | **§4.3.1 の分岐表**（§2-1）と**英語ロケールの要件**（§2-2） | **★裁定が要る** |
| 7 | **`CHANGE-133`:15 の「マイグレ消費 0 本」** | **★失効。要是正** |
| 8 | `DES-002` §4.2 / `DES-003` のスキーマ / `DES-006` の VAL / `SUPP-001` §5.2・§5.5 | **いずれも射程外**（分岐 A・DDL 0 列・VAL 不変・パッケージ増減なし・テスト方針不変） |

**★マイグレ連番**: **`000079` を 1 本消費した。**
**着手時に `ls migrations/` を実査し、disk 末尾＝`000078`、ボード §2.2 の「次に払い出す番号」＝`000079` と一致することを確認してから払い出した**（`D-293` の再請求条件に当たらなかった）。
**⇒ ボード §2.2 の「次に払い出す番号」を `000080` へ、「disk 末尾」を `000079` へ更新してほしい。**
（**★この行は連番を消費するサブが完了するたびに古くなり、過去に 2 度失効している**——`D-479` の写し漏れ＝`E-114` の同型）

---

## §7 教訓（`retrospective-log` 行き）

**★親は本節を読まなくてよい。** 宛先は `retrospective-log` へのバッチ反映で、実施者は設計担当。

| # | 教訓 |
|---|---|
| 1 | **「壊す対象が実在するか」だけでは破壊確認として足りない。「その壊し方で本当に観測が変わるか」も要る。** 破壊 3 は 1 度目の壊し方〔`tag_ids` を読むだけ〕では 33 件すべて緑のままだった——読むだけでは送信するリクエストが変わらないため。**⇒ 計測点 `M-78` に 1 行足す。** |
| 2 | **基準値は「変更を入れる前」に、切らずに採ること。** 本サブは着手前の `go test` を `\| tail -60` で切って採取し、`make e2e` はモック提示を優先して採らなかった。**⇒ どちらも「着手前との対照」を主張できなくなった。** **★採取コマンドにパイプで切り詰めを入れないこと。** |
| 3 | **「N 件だけである」は判定の結果から数えること。** 本サブは「14 コントロール / 13 ファイル」と書いたが実測は 13 / 11 で、**表の小計とも合っていなかった。しかも同じ誤りをソースコメントに固定していた。** **★`E-220` / `E-236` の再演であり、`M24-01` 横断課題 6 と同型。** **⇒ 件数を本文へ書くときは、その場で数え直したコマンドを併記する。** |
| 4 | **native `<select>` を button 系の部品へ替える変更は、`toHaveValue` ベースの assert を全部壊す。** E2E は「Not an input element」で落ちる。**⇒ 共有部品の DOM 種別を変えるときは、`selectOption` / `toHaveValue` の全数を先に grep すること**（本サブは 4 spec / 10 ケースが落ちた）。 |
| 5 | **Radix `DropdownMenu` の中にテキスト入力は置けない**（Content の typeahead が打鍵を奪い、抑止すると矢印キー移動も死ぬ）。**⇒ 検索欄付きドロップダウンは `Popover` ＋ `Input` ＋ `<ul>` が本リポジトリの型である。** **★ただし `Popover` へ移すと矢印キー移動が消えるため、自前で実装する必要がある**（本サブはレビュー指摘で気づいた）。 |
| 6 | **並べ替えのキーは「画面に出している値」にすること。** 表示は `name_ja` 固定なのに `name_en` でソートすると、**利用者の目には無順序に見える**。**★「仕様に書いてあるとおりに実装した」だけでは、利用者から見た結果が仕様の意図を満たすとは限らない。** |
| 7 | **表示名の不一致は「見つかった 1 件」で終わらせない。** 同じ不一致が `M19-04` の完了報告に既に記録されていたが、`character_code` で回避され followup にも ledger にも上がらなかった。**⇒ 回避で済ませた不一致は、回避した本人が記録しないと消える。** |
| 9 | **★「見た目の要望」は 1 往復で終わらないことがある。** 追補1 で要約ピルを見出し行の隣へ移した結果、**「隠す」が右端から見出しの直後へ移動した**。要望は「縦を節約する」であり、**位置の副作用までは要望に書かれていない**。**⇒ レイアウトを動かす変更は、動かした要素だけでなく「同じ行にある他の要素がどこへ行くか」も併せて提示すること。** |
| 10 | **★共有部品の見た目を条件で変えるときは、既定側を 1 行も動かさないこと。** `CollapsibleFieldset` は登録画面 6 節と一覧フィルタ欄で共有されている。**任意 prop（`summaryPlacement`）を足して既定を従来値にしたため、追補1・追補2 のどちらでも登録画面は無変更で済んだ**（テストで固定）。**⇒ 共有部品の改造は「新しい枝を足す」形にする。既存の枝を書き換えない。** |
| 8 | **テストのフィクスチャが「既定状態」を表しているか確かめること。** 本サブのフィルタテストは `sort: "default"` を既定として使っていたが、**`"default"` は「始動状況順」という別の並び順の値**であり、既定は `updated_at` だった。**名前が紛らわしい定数は、フィクスチャ側で取り違えられる。** |

---

*以上、M24-02 設計伝達レポート。*
