# 設計伝達レポート（例外レポート） — `M40-01`（帰属と宣言の是正 ／ 三層への来歴の軸）

| 項目 | 内容 |
|------|------|
| 対象 | **親チャット（設計卓）** |
| 発信 | 製造担当 Claude Code ／ 2026-09-20 |
| 指示書 | `docs/instructions/M40-01-attribution-and-declarations.md` **v1.1.0** |
| チェックリスト | `docs/instructions/reviews/M40-01-review-checklist.md` **v1.1.0** |
| 実装コミット | ブランチ `claude/focused-cannon-arp5uy` ／ 枝元 `c788714` から **6 コミット**（push 済み。**⇒ 設計卓は HEAD で読める**） |
| 関連 | 完了報告 `docs/progress/M40-01-completion-report.md` ／ レビュー `docs/progress/m40-01-review.md` |
| **レビュー集計** | **高 4 / 中 5 / 低 5 ＝ 全 14 件・採用 14 / 不採用 0・「高」の不採用 0 件・再レビュー往復 0 回（上限 2）** |
| CHANGE | **自採番 0 本。⇒ 原稿 2 本を §1 と §6 へ置く** |
| マイグレ | **0 本消費** |

---

## 導入

本書は **①独自確定仕様 ②契約違反の独自判断 ③製造判断 ④残課題** に絞る。指示書どおりの部分は割愛する。

**★最重要は §1-1（三層への来歴の軸＝`DES-001` §5 の改訂原稿）である。**

**★★§4-1（改変 4 件の outbound）は 2026-09-20 に開発者裁定が返り、決着した。⇒ 受理時に判断を要する項目は残っていない。**

**★§2（契約違反の独自判断）は 0 件である。**

---

## §1 製造が独自に確定した実装仕様（DES 反映が要るもの）

### §1-1 ★★★【CHANGE 原稿・1 本】三層へ直交する「来歴の軸」を足す（`DES-001` §5）

**確定の経緯＝開発者確定（2026-09-20 Plan Mode）＝(a) 軸を足す。** 指示書 §7-3 が「構造の拡張は開発者の承認事項」としていた項目である。

**★実測（`REUSE.toml` の全ブロックを当てはめた）。** 三層は `SPDX-FileCopyrightText` を `plexiblinp` 系に固定しており、**第三者が著作権者であるブロックが 1 つも無かった**。したがって vendored な第三者素材は「置き場が無い」のではなく **どこへ落ちても著作権者の宣言が偽になる**。

| 対象 | 改訂前の解決先 | 宣言されていた著作権者 |
|---|---|---|
| `web/src/components/ui/**`（21 件）＋ `web/src/lib/utils.ts` | ブロック(1) `path = "**"` → 層 A `AGPL-3.0-or-later` | `2026 plexiblinp` |
| `docs/seed-data/**`（8 件） | ブロック(3) → 層 B `CC-BY-SA-4.0` | `2026 plexiblinp and the Tacpendium contributors` |

**⇒ `DES-001` §5 へ次を明文化してほしい。**

**(a) §5.1 の層 B 行から `docs/seed-data/**` を外す。** 現行の §5.1 は層 B に `character_data/**` ／ `docs/seed-data/**` ／ `migrations/*_data_*.sql` ／ `internal/seedgen/testdata/**` を挙げているが、**実装は `docs/seed-data/**` を層 B から抜き、来歴の軸（T2）へ移した**（根拠＝`M26-03` 完了報告 §4.1 / §4.3 の再判定＝3 件（＋境界 1 件）に第三者素材が現存する）。

**(b) §5.5 として来歴の軸を新設する。原稿は次のとおり。**

> ### 5.5 ★★来歴の軸（第三者が著作権者であるもの）
>
> **三層は「何の内容か」で切る軸である。** これと**直交する軸として「誰の著作物か（来歴）」を置く**。`SPDX-FileCopyrightText` が `plexiblinp` 以外になるファイルは、**層の割当より先に来歴で宣言する。**
>
> | 種別 | 何を置くか | 宣言の形 |
> |---|---|---|
> | **T1** | **第三者の許諾の下で同梱・再配布するもの** | 上流のライセンスと著作権者をそのまま宣言する（例: shadcn/ui ＝ `MIT` / `Copyright (c) 2023 shadcn`） |
> | **T2** | **第三者素材を含むが配布しないもの** | `LicenseRef-` で「配らない範囲」を宣言し、公開スナップショットの `DENY` と同じことを言わせる |
>
> **★割当の正本は `REUSE.toml` の (6) である**（§5.1 と同じ建て付け。本節は規則と軸の意味を持ち、ファイルの列挙は持たない）。
>
> **★本節は層の定義にも、層へのパスの振り分けにも触れない。** 三層 A / B / C の意味は 1 つも変わらない。
>
> **★T1 は inbound の規定（`SUPP-001` §5.7 の禁止ライセンス表）とは無関係である。** 禁止列は 1 行も変えていない。

**根拠**: `REUSE.toml`(6) ／ `NOTICE` §6 ／ `scripts/check-third-party-attribution.sh` ／ 完了報告 §2.1〜§2.5。

### §1-2 ★★【CHANGE 原稿・1 本】`NOTICE` の構成に §6 を足した（vendored 第三者ソース）

**`NOTICE` §5 は `go.mod` / `web/package.json` を源泉とする依存の棚卸しであり、コピーして持ち込んだソースは構造上そこに載らない。** §5 の源泉を広げると §5 の意味が壊れるため、**別節として §6 を立てた。**

§6 が持つもの＝`Copyright (c) 2023 shadcn` の逐語 ／ **MIT の許諾・免責条項の全文**（インライン） ／ 対象パスと件数 ／ 取得経路 ／ 改変 4 件の明示 ／ 上流 URL と参照日 ／ Radix との切り分け ／ 射程外（`export-image` の自プロジェクト由来コピー）の明示。あわせて §2 へ `name_ja` の取得経路を 1 段落足した。

**★なぜ `NOTICE` か**＝`scripts/release-targets.sh` の `RELEASE_EXTRA_FILES` に既に入っており、`check-release-archive.sh` が**配布アーカイブ内での存在を機械で確かめている**。⇒ MIT が求める「コピーに含める」が**配布物の側で担保される**。**実アーカイブ 3 本で実物確認済み**（完了報告 §11）。

**⇒ `DES-001` §5 か `DES-002` §11.2 のどちらに書くかは設計卓の判断。** 実装としては `NOTICE` の節構成が 5 → 6 へ増えた事実だけが差分である。

### §1-3 `LicenseRef-Tacpendium-NotForDistribution` を新設した

`LICENSES/LicenseRef-Tacpendium-NotForDistribution.txt`（新規・53 行）。**REUSE 仕様 3.3 §License Files が本文ファイルを要求する**（逐語は完了報告 §2.5-1）。`scripts/release-targets.sh` の `RELEASE_EXTRA_FILES` にも足した（`REUSE.toml` が配布物に同梱され新識別子を参照するため、本文が無いと**配布物の中で宣言が宙に浮く**）。

**★否定的発見を 1 つ記録した**——**REUSE には「リポジトリに在るが配布しない」を表す仕組みが無い**（FAQ #exclude-file の逐語＝"you cannot exclude files from REUSE compliance testing"）。**⇒ REUSE の答えは「除外」ではなく「注釈」であり、T2 はその形に従っている。**

---

## §2 契約・設計に反する独自判断

**なし。**

ライセンス条件そのものは 1 つも自己判断していない（`CLAUDE.md` §10）——条件は開発者の外部照会（`docs/progress/20260920-shadcn-license-external-inquiry-report.md`）に、置き場と形は Plan Mode での開発者確定に、仕様の逐語は一次情報に依っている。`CLAUDE.md` §6 の禁止ライセンス列も 1 行も触っていない。

---

## §3 製造の判断

### §3-1 開発者へ確認して確定した点（★すべて Plan Mode・2026-09-20）

| # | 事項 | 確定 |
|---|---|---|
| 1 | 三層の形（指示書 §7-3） | **(a) 来歴の軸を足す**（T1 ＋ T2。⇒ 射程 4 の「暫定」も同じ手番で外す） |
| 2 | 帰属の置き場（§7-2） | **`NOTICE` へ新節。★あわせて他 OSS の先例を調べること**（外部 AI に任せてよい） |
| 3 | evidence の公開範囲（§7-1） | **`scan-review-chronology-initial.csv` だけ `DENY`。** 他 3 ファイルは公開 |
| 4 | 追補報告の扱い | **非公開リポの SHA 3 件だけ伏せる**（本文・件数・日付・判定・差分統計は落とさない） |
| 5 | `LICENSES/MIT.txt`（★射程外） | **著作権表示なしのテンプレートへ戻す**（REUSE FAQ #license-templates ＋ 実在の REUSE 準拠リポ 5 本の実測を提示したうえで） |

### §3-2 製造の裁量で決めた点

| # | 判断 | 理由 |
|---|---|---|
| 1 | `NOTICE` §6 に MIT 条項を**全文インライン**で持たせた | `LICENSES/MIT.txt` はテンプレートであり「誰の MIT か」が本文から決まらない。**⇒ 取り違えさせないため。** 先例も実在する（Kubernetes ／ btcp-ui） |
| 2 | `precedence = "override"` を維持した | `D-702` (f)＝パス単位の宣言に集約する方針。**★同型の先例 `SecPal/frontend`**（AGPL ＋ REUSE ＋ shadcn vendored）**は `aggregate` を使うが、あれはファイル内 SPDX ヘッダとの併用が前提であり本 repo の方針と噛み合わない** |
| 3 | 改変 4 件の著作権者を**並記**した | 派生物であるため（`["2023 shadcn", "2026 plexiblinp"]`） |
| 4 | 機械検査を**実装した**（指示書 §2.1-5 は「実装してもよいし見送ってもよい」） | `D-777` の穴に対する唯一の機械的な答えであるため。**⇒ チェックリスト B-3 の問いにも同じもので答えた** |
| 5 | 検査の登録先を `README.md` 主とした | `check-migration-license.sh` / `check-public-snapshot.sh` と同じ場所。`CLAUDE.md` §8 には製造担当の導線として 1 行だけ |

### §3-3 推測で進めた点

**なし。** 指示書の射程 5 件すべてについて、判断の根拠は実査・一次情報・開発者確定のいずれかに在る。

---

## §4 設計担当が未把握の残課題・申し送り

### §4-1 ★★【裁定済み】改変 4 件の outbound が `AGPL-3.0-or-later` から `MIT` へ変わった

| スラッグ | `shadcn-modified-files-outbound-license` |
|---|---|
| **★★★裁定（2026-09-20・開発者）** | **現状維持＝22 件を 1 表で `MIT`。⇒ 改変 4 件も同じ表に入れ、本体側の改変分も MIT で提供する。** |
| **何が起きたか** | `web/src/components/ui/**` 21 件 ＋ `web/src/lib/utils.ts` の宣言が、ブロック(1) 既定の `AGPL-3.0-or-later` / © `2026 plexiblinp` から、(6-T1) の `MIT` / © `["2023 shadcn", "2026 plexiblinp"]` へ変わった。**⇒ 受け取った人がこの 22 ファイルを MIT の条件で使える。** |
| **根拠** | `REUSE.toml` (6-T1) ／ 完了報告 §5b ／ レビュー 高-4 |
| **★切り分け** | **上流そのままの 18 件**（`ui/` 17 件 ＋ `utils.ts`）**は、宣言が上流の事実へ一致しただけであり手放したものは無い。⇒ むしろ変更前のほうが偽であった。★裁定の対象は改変 4 件**（`dialog.tsx` / `alert-dialog.tsx` / `sheet.tsx` / `sonner.tsx`）**だけであった。** |
| **★なぜ製造が決めなかったか** | **開発者確定「(a) 軸を足す」の T1 定義は「上流のライセンスと著作権者をそのまま宣言する」であり、*改変分を MIT で出すこと*までを名指しで承認したものではなかった。⇒ `roles-and-routing` のハード列（公開・法務）に当たるため返した。** |
| **★採らなかった代案（検討済み）** | (2) 改変 4 件だけ別表で `MIT AND AGPL-3.0-or-later`——**事実としては最も正確だが、受け取る側は両方の条件を満たす必要があり実質 (3) と同じ拘束になる。⇒ 正確さと引き換えに読む側の負担だけが増える。** ／ (3) 改変 4 件は層 A に残し `SPDX-FileCopyrightText` にだけ shadcn を並記——**改変分を手放さずに済むが、「shadcn 由来なのに MIT で取り出せない 4 件」が生まれ、来歴の軸の説明が 1 段複雑になる。** |
| **★裁定を支えた実測** | **手放す実体が小さい。⇒ `dialog` / `alert-dialog` / `sheet` はそれぞれ*呼び出し側の 2 行*（`import` 1 行 ＋ `<ModalPresenceMarker />` 1 個）、`sonner` はアイコンとテーマの設定である。★★`ModalPresenceMarker` の*実装本体*（`web/src/lib/modal-presence.ts`）は (6-T1) のグロブに当たらず層 A（AGPL）のまま残る。⇒ MIT で出るのは呼び出し側だけであり、ロジックは手放していない。** |
| **★★取り消せないこと** | **一度 MIT で配ったものは、後から AGPL へ戻しても既に受け取った人には効かない。⇒ `REUSE.toml` の (6-T1) コメントへこの旨を書き、次に同表を触る人が判断の重みを知れるようにした。**（同じ原則の先例＝同ファイル (4) の撤去コメント） |
| **新規/更新** | **★登録不要（裁定済み）。** **⇒ `followup-backlog.md` へは登録しない——決着しており「再開に必要な条件」が無いため。★記録は本節と `REUSE.toml` (6-T1) のコメントに在る。** |

### §4-2 ★★【既存行の更新】`three-layer-lacks-third-party-axis`

| スラッグ | `three-layer-lacks-third-party-axis` |
|---|---|
| **更新後の状態** | **★★解決済み（2026-09-20・`M40-01`）。⇒ (a) 軸を足す形で決着した**（`REUSE.toml` (6)。原稿は本レポート §1-1） |
| **★閉じるときに残すこと** | **選択肢が 2 文書で食い違っていた**——followup 本文は「(a) 軸を足す ／ (b) 除外で切る（現状は (b)）」、チェックリスト B-2 は「(a) ／ (b) 層を足す ／ (c) 宣言だけ」。**⇒ 指示書 v1.1.0 の 3 択に従い (a) を採った。★followup 側の「(b) 除外で切る」にも答えておくこと——除外は公開範囲の話であって、著作権者の宣言が偽である問題は解けない。** |
| **★名指しの 3 母集団のうち触ったのは 1 つだけである** | (i) `web/src/components/ui/` 21 件＝**本サブで解決** ／ (ii) `LICENSES/` 配下＝**REUSE 仕様上 Covered File ではなく宣言の対象外**（`REUSE.toml:20-21` の逐語。`M26-03` 完了報告 §5.2 X-1 と同じ判定） ／ (iii) `docs/progress/` の L-1 / L-2 ＝**`a3-exclusion-cut-by-location-not-content` として 2026-09-11 に処置済み**（`D-823`）。**⇒ 3 つとも決着している。** |
| **新規/更新** | **既存行の更新** |

### §4-3 ★★【既存行の更新】残り 3 スラッグ

| スラッグ | 更新後の状態 |
|---|---|
| `shadcn-ui-vendored-without-attribution` | **★★解決済み（2026-09-20・`M40-01`）。** 帰属は `NOTICE` §6 ＋ `REUSE.toml` (6-T1)。**⇒ 配布アーカイブ 3 本すべてに入ることを実物で確認済み**（完了報告 §11） |
| `reuse-toml-seed-data-declaration-provisional` | **★★解決済み（2026-09-20・`M40-01`）。** `M26-03` の推奨案 B を採り `LicenseRef-Tacpendium-NotForDistribution` へ。**⇒ `public-snapshot-manifest.txt:26` の注記も再判定結果へ追随させた** |
| `name-ja-provenance-not-documented` | **★解決済み（2026-09-20・`M40-01`）。** 正本は `NOTICE` §2。`DATA-LICENSE.md` §5 は参照 1 行のみ（`E-76`） |

### §4-4 ★★【新規登録】`docs/change-notes/CHANGE-226-notification.md` が存在しない

| スラッグ | `change-226-notification-missing` |
|---|---|
| **何が起きるか** | 指示書 `M40-01` §1-6 / §9 とレビューチェックリスト E-3 が `docs/change-notes/CHANGE-226-notification.md` を参照しているが、**実体が無い**。中身は `docs/handover/change-number-registry.md:203` ／ `DES-001`（`docs/design/01-tech-stack.md:9,241`） ／ `SUPP-001`（`docs/design/supp-001-detailed-design.md:391,1472`）に在る |
| **根拠** | `ls docs/change-notes/` ＝ `change-report-133.md` 〜 `change-report-152.md` ＋ `phase1/` `phase2/` のみ。`grep -rn CHANGE-226` の全件が上記 |
| **★機械では出ない** | `check-doc-refs.sh` の走査範囲は「`CLAUDE.md` ／ `web/CLAUDE.md` ／ `.claude/rules/*.md` ＋ `.claude/commands/*.md`」の 37 ファイルであり、**指示書とレビューチェックリストは含まれない** |
| **割付の候補** | **設計卓**（通知書を起こすか、参照側を `change-number-registry.md:203` へ張り替えるか） |
| **新規/更新** | **新規登録** |

### §4-5 ★★【新規登録】`docs/progress/**` 全体に短縮 SHA が日常的に引用されている

| スラッグ | `progress-docs-carry-private-repo-shas` |
|---|---|
| **何が起きるか** | `docs/progress/**` は `ALLOW` であり、完了報告・レビュー報告は**日常的に非公開リポの短縮 SHA を引用している**（本サブの完了報告も指示書 §0.5-6 により枝元 commit を書いている）。**⇒ 本サブが入れたファイル単位の `DENY` は chronology のような*塊*には効くが、この面には効かない** |
| **★あわせて** | 開発者確定は「SHA 3 件だけ伏せる」であり、**追補報告に残る非公開リポの*日付***（`M26-04-scanoss-followup-report.md` の §5.4 / §5.5 / §6 等）**は伏せていない。** 同報告 §7.1 が保持対象に挙げるのは「初出 commit SHA **と author date**」の両方である |
| **根拠** | `scripts/public-snapshot-manifest.txt:104` の `ALLOW docs/**` ／ 完了報告 §6.4 |
| **割付の候補** | **開発者の判断**（公開前に方針を決めるなら別の手番。`public-release-runbook` §2.A へ 1 行足すのが安い） |
| **新規/更新** | **新規登録** |

### §4-6 ★【新規登録】`M26-02` の判断が本サブで反転した

| スラッグ | `licenses-mit-txt-copyright-line-reverted` |
|---|---|
| **何が起きるか** | `docs/progress/M26-02-completion-report.md:81` に「`LICENSES/MIT.txt` … **著作権行の `<year> <copyright holders>` を埋めた**」と在る。**⇒ 本サブ（完了報告 §5）でその判断が反転し、テンプレートへ戻した** |
| **根拠** | REUSE FAQ #license-templates の逐語 ＋ 実在の REUSE 準拠リポ 5 本の実測（完了報告 §2.5-3）。開発者確定 2026-09-20 |
| **★なぜ登録するか** | **歴史記録は書き換えない**（`D-274` (3)）**が、反転を辿れる経路が無いと、次に `M26-02` を読んだ人が現行と食い違う記述を前提にする** |
| **割付の候補** | **設計卓**（1 行の注記で足りる） |
| **新規/更新** | **新規登録** |

### §4-7 ★レビュー低指摘の繰越

**なし。** 低 5 件はすべて本サブ内で採用・対応済み（完了報告 §13.1）。**不採用は 0 件であり、「高」の不採用も 0 件である。⇒ 開発者確認を要する棄却は発生していない。**

### §4-8 ★停止時記録（**§J 行の原稿**）

**なし。** **★未解消のまま停止した項目は 0 件である。** 再レビュー往復も 0 回で上限（2 回）に達していない。**⇒ `docs/handover/followup-backlog.md` は 1 文字も編集していない**（`D-838`）。

---

## §5 参考（触れていない＝不変の証跡）

- `web/src/` のコード: 1 行も触っていない（`M40-02` の面）。`git diff --stat c788714 HEAD` に `web/src/` の行が無い
- `docs/design/` 本体: 1 行も触っていない（`CLAUDE.md` §8）
- `CLAUDE.md` §6 の禁止ライセンス列: 1 行も触っていない（差分は §8 の検査表 1 行のみ）
- `migrations/`: 1 バイトも触っていない（マイグレ消費 0 本）
- `docs/handover/followup-backlog.md`: 1 文字も触っていない（`D-838`）
- `go test ./...` EXIT=0 ／ `grep -cE "^--- FAIL"` = 0
- `cd web && pnpm test` EXIT=0 ／ 234 ファイル・2974 件 passed
- `check-migration-license.sh` は `migrations/` と `internal/seedgen/testdata/` のみを走査するため `REUSE.toml` の (6) 追加の影響を受けない（EXIT=0 ＋ `--self-test` EXIT=0）
- `make e2e` は回していない（`web/src/` も Go の実装も触らないため。完了報告 §8 に理由を明記）

---

## §6 CHANGE 起票のたたき台（設計担当向けチェックリスト）

**番号は起票時に `docs/handover/change-number-registry.md` で採番する**（★製造は自採番していない＝`D-293`）。**マイグレは 0 本消費である。**

| # | 対象 | 改訂内容 | 根拠 |
|---|---|---|---|
| **1** | **`DES-001` §5**（`docs/design/01-tech-stack.md`） | **(a) §5.1 の層 B 行から `docs/seed-data/**` を外す ／ (b) §5.5 として「来歴の軸（T1 / T2）」を新設する。★原稿は §1-1 に在る** | `REUSE.toml` (6) ／ `M26-03` 完了報告 §4.1 / §4.3 ／ 開発者確定 2026-09-20 |
| **2** | **`NOTICE` の構成**（反映先は `DES-001` §5 か `DES-002` §11.2。**⇒ 設計卓の判断**） | **§6「同梱する第三者ソース（vendored）」を新設した ／ §2 へ `name_ja` の取得経路を足した。** 節構成が 5 → 6 へ | `NOTICE` §2 / §6 ／ 外部照会結果 ／ 開発者確定 2026-09-20 |

**★3 本目の要否は設計卓の判断。** evidence の公開範囲（`scripts/public-snapshot-manifest.txt` の `DENY` 1 行追加）は同ファイルが正本であり `docs/design/` に差分が出ないため、**製造は CHANGE 対象と見ていない**（先例＝`D-777` の「`docs/design/` に差分が出ないので CHANGE は不要と見る」）。

---

## §7 教訓（`retrospective-log` 行き）

**★親（設計卓）は本節を読まなくてよい。** 宛先は `retrospective-log` へのバッチ反映であり、実施者は設計担当である。

### §7-1 ★★★「検査を書いた」は「検査が効く」の証拠にならない

**★実際に起きたこと**（`M40-01`・レビュー 高-2 / 高-3）**＝`D-777` の穴**（グロブで解決する宣言は、ファイルを動かすと黙って外れ、外れた先が既定なので検査は緑のまま）**を塞ぐために新しい検査を書いた。⇒ その検査は、`REUSE.toml` から当の宣言ブロックを丸ごと削除しても EXIT=0 であった。**

**★★このとき、次のすべてが緑であった。**

| 何が緑だったか |
|---|
| 新検査の本体（全 7 規則が OK） |
| 新検査の自己検査（陰性 2 / 陽性 5 がすべて期待どおり） |
| `check-artifact-integrity.sh`（「`scripts/check-*.sh` が `--self-test` を持ち、それが通ること」を見る検査） |

**⇒ 「対照を持っている」ことと「対照が効いている」ことは別である。** 本件の陽性対照は*終了コードだけ*を見ていたため、**1 つの対照が 2 つの規則で同時に赤くなり、片方の規則を壊しても合格を返した。**

**★★★一般形＝新しい検査を書いたら、*守りたい事故そのものを実際に起こして* 赤くなることを確かめること。** 対照の設計を眺めても分からない。**実リポジトリでの変異でしか分からない。**

**★あわせて、陽性対照は「どの規則が鳴ったか」まで照合すること。** 終了コードだけの照合は、**規則が増えるほど隔離が崩れる**（本件は規則 7 個の時点で既に崩れていた）。

**★本件を見つけた経路は 1 つだけである**——**レビュアーが完了報告の「緑」を信用せず、自分で検査を壊して回した**（`M36-01` §7-1 と同型の姿勢）。**⇒ 製造の報告に貼られた出力は、レビューの入力であって結論ではない。**

### §7-2 ★★自分が直した事実に、自分の別の記述が追随しないことがある

**★実際に起きたこと**（レビュー 高-1）**＝`LICENSES/MIT.txt` をテンプレートへ戻した*後に*、`REUSE.toml` のコメントが同ファイルの旧内容を逐語で引いたまま残っていた。**

**★同じ手番で自分が変えたものである。⇒ それでも追随を落とした。** 原因は、**その引用が「なぜ来歴の軸が要るか」の論拠として書かれており、論拠を書いた時点では真であった**こと。**⇒ 後から前提を変えたとき、論拠を書き直す動きにならなかった。**

**★一般形＝「Aを根拠にBを書いた」後にAを変えたら、Bを `grep` で全数拾うこと**（`E-118` と同型）。**本件は `grep -rn "MIT.txt"` で 1 回で出た。**

### §7-3 ★先例調査は「やっていないこと」も結論である

**★開発者の指示で他 OSS の先例を調べた。⇒ 広く使われている AGPL/OSS 製品 9 本**〔`documenso` ／ `openstatus` ／ `twenty` ／ `formbricks` ／ `dub` ／ `unkey` ／ `teable` ／ `glasskube/distr` ／ `cal.com`〕**が shadcn を vendored していながら、帰属の成果物を 1 つも持っていなかった。**

**★これを「だから要らない」と読まないこと。** 指示書 §0.1 の逐語＝**直す理由は「帰属が要るから」であって「スキャナが黙るから」ではない。⇒ 「他がやっているから」でもない。**

**★★ただし調べた価値はあった。** 同型の先例 1 本（`SecPal/frontend`＝AGPL ＋ REUSE ＋ shadcn vendored）から `precedence` の選び方を、REUSE FAQ から `LICENSES/<SPDX>.txt` の書き方を、それぞれ**採るか採らないかの根拠込みで**決められた。**⇒ 調査の成果は「真似すること」ではなく「選んだ理由を持てること」である。**

---

*以上、`M40-01` 設計伝達レポート。* **★★★§2 は 0 件であり、§4-1 の裁定も 2026-09-20 に返って決着した。⇒ 受理時に残るのは §6 の CHANGE 原稿 2 本の採番と反映、および §4-2 〜 §4-6 の followup 更新だけである。**
