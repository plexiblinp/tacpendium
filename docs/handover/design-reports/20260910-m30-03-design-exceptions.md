# 設計伝達レポート: M30-03（必殺技ファミリーの導き方を「`code` のどこかにある強度語」へ広げる）

| 項目 | 内容 |
|------|------|
| 作業ID | M30-03 |
| 対象指示書 | `docs/instructions/M30-03-special-family-code-anywhere.md` **v1.0.0** ／ チェックリスト `docs/instructions/reviews/M30-03-review-checklist.md` **v1.0.0** |
| CHANGE | **消費 0 本。★製造は番号を消費していない**（`D-293`）**。⇒ 起票のたたき台は §4-1** |
| マイグレ | **消費 0 本**（指示書どおり。`move_code` を 1 文字も変えていない） |
| 作成日 | 2026-09-10 |
| 実装コミット | `claude/m30-03-implementation-plan-ugc02r`（本レポート自身を除く）。差分＝**11 ファイル**（`web/` の 9 ファイル ＋ `docs/progress/` の 2 本）。**新規ファイル 0 本** |
| 源泉 | 完了報告 `docs/progress/M30-03-completion-report.md` ／ レビュー `docs/progress/m30-03-review.md`（**高 3 / 中 4 / 低 3 ＝ 全 10 件。★高の不採用 0 件・再レビュー往復 0 回**）／ 開発者の裁定 1 件（2026-09-10「進めて良い」）／ **★開発者の実機確認 5 項目**（2026-09-10。**A・B・D・E は問題なし ／ C は「直したい」＝§3-1**）／ 実装直後の同一セッションで生成 |
| 宛先 | 設計卓（親チャット） |

**本レポートは ①独自確定仕様 ②契約違反の独自判断 ③製造判断 ④残課題 に絞る。指示書どおりの部分は割愛する。**

**★★最重要は §2-1**（**`M30-01` の副次指標が実測ではなく引き算だった。⇒ 計測点の扱いに裁定が要る**）**と §4-4**（**2026-09-08 開発者判断の「案 C」が、現行データに対して効く行を持たなくなった**）**である。**

**★★★あわせて §4-3 に開発者の方針が入った**（2026-09-10 の実機確認）**——「ファミリー行の表示名に強度語が残る」件は直す方向で、★汎用アルゴリズムに固執せず例外辞書でもよい、という許可が出ている。⇒ 割付先の判断を請う。**

**★規則 2 の優先順位**（旧 §3-1）**は開発者の実機確認で確定した。⇒ 裁定不要**（§3-1 の B ／ §3-2）**。**

> **★設計卓は `docs/progress/` を読まない**（2026-08-11 開発者裁定①）**。⇒ 本レポートは参照だけを書かず、判断に要る中身をここへ埋めてある。**

> **★`DES-002` §4.2 の経路表に載る API の新設・変更は 0 件である。** 本サブは `internal/` `cmd/` `migrations/` を **1 行も触っていない**。⇒ **本レポートに API 契約の逐語は無い。**

---

## 0. 本サブで実際に起きたこと（4 行で）

1. **`parseSpecialCode` を案 (i) で広げた** —— 「末尾の強度接尾辞」から「`code` のどこかにある強度語」へ。**A2 の 105 件が必殺技タブへ載り、既存の判定は 1 件も動いていない**（§1-1）。
2. **同関数は全域になった**（`null` を返さなくなった）**。⇒ `isOnSpecialTab` は `category === 'special'` の 1 条件になり、未分類タブに必殺技は 1 件も残らない**（§1-2）。
3. **`M30-01` の申告のうち副次指標 1 件が実測と食い違った** —— 「未分類 251 → 146」は**測った値ではなく引き算**であった（§2-1）。**指示書 §2.1 に従い実装前に停止・報告し、開発者の裁定を得て続行した。**
4. **`followup` §CL の「素は押せるが OD が押せない」7 組が 0 件になった**（§4-2 の消込対象）。
5. **★実機確認で 1 件だけ「直したい」が出た** —— **ファミリー行の表示名に強度語が残る形**（§4-3）**。★開発者から「例外辞書的な対応でもよい」という方針が示されている。⇒ 割付の判断が要る。**

---

## 1. 製造が独自に確定した実装仕様（DES 反映が要るもの）

### 1-1 必殺技ファミリーの導出規則が 3 段のまま中身だけ変わった

| 段 | 条件 | 着手時点 | **完了時点** |
|---|---|---|---|
| 1 | 末尾が `_od` / `_light` / `_medium` / `_heavy` | `{ family, strength }` | **不変** |
| 2 | **強度語が `code` の途中に在る** | **`null`** | **★`{ family, strength }`（新設）** |
| 3 | 強度語がどこにも無い | `{ family: code, strength: "none" }` | **不変** |

**段 2 の中身**: **`SPECIAL_STRENGTH_SUFFIX` の並び**（`od` → `light` → `medium` → `heavy`）**で最初に見つかった強度語を 1 つだけ抜き、残りを `_` で連結してファミリーとする。**

例＝`lightning_beast_light_rolling_attack` → ファミリー `lightning_beast_rolling_attack` ／ 強度 `light`。**同じキャラの `lightning_beast_od_rolling_attack` も同じファミリーへ落ちる。**

**★★規則 1 が規則 2 より先であることが「既存の判定が 1 件も動かない」の成立条件である**（§3-2）**。**

**⇒ `DES-004` §2.1 ／ `DES-005` 第6節 へ、「強度接尾辞を持たない `move_code` はファミリー UI に載らない」**（`CHANGE-166`）**が A2 についても失効したことを明文化してほしい。★これで A1 / A2 の両枝とも失効し、`CHANGE-166` の規則は全体として置き換わる。**

**波及の実測**（母集団＝`moves` テーブル 3026 行 / 31 キャラ。`character_data/*.csv` ではない＝`SUPP-001` §5.5.3.2）:

| 観測 | 着手時点 | 完了時点 |
|---|---:|---:|
| 必殺技タブに載らない `special` | 105（＝A2 全数） | **0** |
| 未分類（CSV 基準・固有状態タブ無し） | 144 | **39** |
| 未分類（**実 DB・実設定**） | 87 | **41** |
| 未分類（実 DB）jamie / mai / blanka / manon / cammy / m_bison | 2 / 1 / 2 / 0 / 13 / 9 | **2 / 1 / 2 / 0 / 1 / 0** |
| **「素は押せるが OD が押せない」組** | **7** | **0** |
| **ファミリー行の総数** | 342 | **372** |

### 1-2 `parseSpecialCode` が全域になり、`isOnSpecialTab` が 1 条件になった

**着手時点の型は `{...} | null` だった。⇒ `null` を返す枝が消えたので型からも外した。**

**★`| null` を残さなかった理由**: 常に真である `parseSpecialCode(...) !== null` は「まだ何かを弾いている」と読ませるが、**弾いていない。** 呼び出し側は 2 か所（`isOnSpecialTab` / `deriveSpecialFamilies`）に閉じている。

**★★副作用が 1 つある**（レビュー指摘）**。`isOnSpecialTab` と `deriveSpecialFamilies` が同じ述語を共有しなくなった**——前者は `category` だけ、後者は `parseSpecialCode` の全域性が前提。**両者がずれても型検査は通る。⇒ 実データ規模でこの結合を守っているのは、`moveSurfacing.roster.test.ts` の座標一致テスト**（キャラごとに `special` 総数 == 必殺技タブの座標総数）**である。⇒ この 1 本を消さないこと。**

---

## 2. 契約違反・前提の崩れ（**裁定が要る**）

### 2-1 ★★`M30-01` の副次指標「未分類 251 → 146」は測った値ではなかった

**★指示書 §0.1 が「実測が既に在る」として掲げた 5 つのうち、1 つが実測ではなかった。**

| 指標 | `M30-01` の申告 | **本サブの実測** | 突合 |
|---|---|---|---|
| 新たに載る | 105 | **105** | 一致 |
| ファミリー衝突 | 0 | **0** | 一致 |
| 誤検出 | 0 | **0** | 一致 |
| 既存の判定が変わる技 | 0 | **0** | 一致 |
| **副次：未分類** | **251 → 146** | **87 → 41** | **★再現せず** |
| （内訳）A1 / A2 | 186 / 105 | **187** / 105 | **A1 が +1** |

**★★「146」が出ない理由＝A2 105 件のうち未分類タブに居るのは 46 件だけで、残る 59 件は既にキャラ固有状態タブに載っている。** A2 はすべてキャラ固有状態の強化版必殺技であるため、**案 C**（固有状態タブを掲載済みに数える・2026-09-08 開発者判断）**の下では当然そうなる。⇒ 251 − 105 の引き算は、既に外れている 59 件を二重に引いている。**

**★A1 の +1 は説明がつく**——`M30-02` のマイグレ `000107` が `terry/round_wave_heavy` を `round_wave` へ是正し、接尾辞形が 1 件 A1 へ移った（実測で `terry/round_wave` が A1 に居ることを確認済み）。**⇒ 裁定不要。**

**★★★裁定を請う点**: **`M-145`**（`M30-01` の母集団取り違え）**は「母集団を間違えた」として記録されているが、本件は別型である**——**母集団は正しく、「実測」と題した表の中に測った値と計算した値が混ざっていた。★どちらであるかは表からは読めない。⇒ 計測点として `M-145` の隣へ足すか、同一件として畳むかの判断を請う。**

**★決定的な 4 指標はすべて再現したため、案 (i) の設計判断そのものは崩れていない。開発者の裁定**（2026-09-10）**＝「進めて良い」。⇒ 本サブはそれに従って続行した。**

---

## 3. 製造判断

### 3-1 開発者へ確認して確定した点（**★実機確認 2026-09-10**）

**★実装完了後、開発者が dev サーバで 5 項目を確認した。⇒ 結果を確定として記録する。**

| # | 確認したこと | 結果 |
|---|---|---|
| **A** | 逐語で報告された技が押せるか（`ryu` の `[電刃錬気]波動拳` / `[電刃錬気]波掌撃` の OD ／ `blanka` のライトニングビースト系の弱中強＋OD ／ `mai` の `[焔版]花蝶扇` のホールド変種 ／ `jamie` の非退行） | **問題なし** |
| **★B** | **規則 2 の優先順位（`od` 先）の見え方**＝`cammy` の「フェイタルレッグツイスター(◯ODフーリガン…)」が **3 行に割れ、各行 OD だけが押せる**形 | **★★問題なし（＝`od` 先を採用で確定）。⇒ §3-2 の製造判断は開発者の裁定を得た** |
| **C** | ファミリー行の表示名に強度語が残る形の見え方 | **★気になる。直したい**（⇒ §4-3。**方針の逐語も同節に記録**） |
| **D** | 未分類タブの件数（jamie 2 / mai 1 / blanka 2 / manon 0 / cammy 1 / m_bison 0） | **問題なし** |
| **E** | 積んで保存し、開き直して復元されるか | **問題なし** |

**⇒ B の確定により、§3-2 は「覆せる形で残す製造判断」から「開発者が確認して確定した判断」へ変わった。★設計卓の裁定は不要である。**

### 3-2 ★規則 2 の優先順位を `od` 先にした（**★開発者の実機確認で確定済み＝§3-1 の B**）

**★実データで位置順と結果が変わるのは 3 件だけである**——`cammy/fatal_leg_twister_{light,medium,heavy}_od_hooligan_combination`（他 1021 件は一致。**製造とレビューが独立に再現した**）。

**★★対象 3 件は、技自身に強度が無く `light` も `od` も派生元フーリガン側を指す形である。⇒ 意味の上ではどちらを採っても同点ではなく、両側に支持する兄弟が実在する。**

| 支持する側 | 兄弟 | 畳まれ方 |
|---|---|---|
| **`od` 先**（採用） | `cannon_strike_<強度>_od_hooligan_combination_od`（**規則 1 で決まる**） | 派生元の強度ごとに 3 ファミリー、中身は OD だけ |
| **位置順** | `fatal_leg_twister_{light,medium,heavy}` ／ `reverse_edge_<強度>_hooligan_combination` ／ `cannon_strike_<強度>_hooligan_combination` | 1 ファミリー・弱中強。**強度ボタンの意味が「派生元の強度」である点は対象 3 件と同じ** |

**★採用側の兄弟には性質の違いが 1 つある**——`cannon_strike_..._od` の**末尾**の `od` は**技自身の強度**（ODキャノンストライク）であり、完全な同型ではない。

**★実測の差＝cammy のファミリー行が 2 行**（位置順 395 ／ `od` 先 397・変種畳み前の生ファミリー数）**。**

**★★★これはユーザーが見る形の違いであり、`roles-and-routing` のハード列「ユーザー体験に影響する選択」に当たる。⇒ 製造では決めきれないため実機で開発者に見てもらい、`od` 先で「問題なし」の確定を得た**（2026-09-10・§3-1 の B）**。★設計卓の裁定は不要である。⇒ ただし `M30-04`**（ファミリー行の母集団と並び）**が前提として複製するため、判断の根拠として本節を残す。**

**★優先順位は `moveSurfacing.roster.test.ts` が名指しで固定してある**（3 件の全数 ＋ 規則 1 で決まる 12 件）**。⇒ 覆すときはそのテストを書き換える。**

---

## 4. 残課題（**設計卓へ**）

### 4-1 ★CHANGE の請求（**指示書が明示的に求めている**）

| 対象 | 何を |
|---|---|
| **`DES-004` §2.1** | **必殺技ファミリーの導出規則**（`<技名>_<強度>` / `<技名>_od`）**に、「強度語は `code` のどこに在ってもよい」を反映してほしい。** ★あわせて `CHANGE-166`（強度接尾辞を持たない `move_code` は載らない）が **A1・A2 の両枝とも失効した**ことを明文化してほしい |
| **`DES-005` 第6節** | **入力面の現行**——未分類タブの母集団から `category='special'` が消えたこと（実測 0 件）。★タブ構成は 8 枚のまま不変 |

### 4-2 ★`followup` の更新（**製造は §J しか書けない**＝`D-382`。**⇒ 設計卓が畳む**）

**★以下は「既存行の更新」である**（新規登録ではない）**。設計卓がそのまま反映できる形で書く。**

| スラッグ | 何が起きたか | 根拠 | 更新後の状態 |
|---|---|---|---|
| **§CE `special-family-ui-misses-state-variants`** | **A2 枝（実測 105 件）が必殺技タブへ載った。A1 枝（実測 187 件）は `M30-02` で解消済み。⇒ 登録内容の両枝とも解消した** | `web/src/features/combo/inputResolution.ts` の `parseSpecialCode` ／ テスト `moveSurfacing.roster.test.ts`「★★special は 1 件も未分類に残らない」（`special` 総数 1024 も併せて主張） | **解決済み（`M30-02` ＋ `M30-03`）** |
| **§CL `special-family-plain-loads-but-od-does-not`** | **「素は押せるが OD が押せない」7 組が 0 件になった** | テスト `moveSurfacing.roster.test.ts`「★★『素は押せるが OD が押せない』7 組が、素と OD で同じファミリーへ合流している」（**7 組を名指しで全数**。破壊確認で赤になることを確認済み） | **解決済み（`M30-03`）。★ただし主張の取り方は作り直してある＝§4-5** |

### 4-3 ★★ファミリー行の表示名に強度語が残る（**★開発者が「直したい」と表明。割付の判断を請う**）

**★実測**（母集団＝`moves` テーブル 3026 行に `deriveSpecialFamilies` を通した出力・変種畳み後）:

| 測ったもの | 前 → 後 |
|---|---|
| ファミリー行の総数 | **342 → 372** |
| **うち表示名に強度語が残る行** | **31 → 49**（+18） |

例＝`lightning_beast_rolling_attack` の表示名が「【ライトニングビースト】弱ローリングアタック」のまま並ぶ（**弱中強 ＋ OD が押せるファミリーなのに名前が「弱」を含む**）。

**★原因**: `stripStrengthLabel` が**接頭形と末尾形しか落とさない**。`【…】` や `[…]` の**直後**に来る強度語は落ちない。**★`M30-02` も同じ限界を指摘していた**（`【ホールド】弱グラディウス`）**。**

**★★★機械的に落とすと壊れる形が在る。⇒ これが本サブで直さなかった理由である。**

- `【ライトニングビースト】弱ローリングアタック` の「弱」は**その技自身の強度である。⇒ 落とすのが正しい。**
- `キャノンストライク（弱フーリガンコンビネーション派生）` の「弱」は**派生元の強度である。⇒ 落とすと意味が壊れる。**

**⇒ 判別規則の設計が要る。**

#### ★★★開発者の方針（**2026-09-10 実機確認・逐語**）

> 「気になる。直したい。方式は後で決めるが、こういう無理に直すと壊れるが気になるものは、**汎用的なアルゴリズムではなく、例外辞書的なもので強引に対応することも考えて良い**と思っている。」

**★★これは本件に閉じない一般方針の表明である。⇒ 「汎用規則で解けないから見送る」を既定にしない、という許可が出た。**

**★帰結が 2 つある。**

1. **本件は「直す」方向で確定した。⇒ 見送りの申し送りではない。★決めるべきは方式ではなく割付先である。**
2. **例外辞書が選択肢に入るなら、本件は `M30-04` の設計判断を待たなくてよい。⇒ 独立したサブとして切れる**（規則の設計が要らないため）**。**

**★★方式の選択肢**（**製造の見立て。決めるのは設計卓・開発者である**）:

| 案 | 内容 | 規模の見立て |
|---|---|---|
| **(a) 例外辞書** | **強度語を落とす／落とさないを `move_code`（またはファミリー識別子）単位の表で持つ** | **★対象は 49 行。★開発者が明示的に許可した方式** |
| (b) 位置規則 | 先頭のブラケット装飾（`【…】` / `[…]`）**の直後**に来る強度語だけを接頭形として落とす | **★実データでは (a) より少ない記述で済みうるが、`(弱…派生)` 型を確実に除外できるかの実査が要る** |
| (c) データ側 | `official_ja_move` の `alias_text` を直す | **★`preset_aliases` の更新であり、マイグレが要る** |

**★割付の候補**: **`M30-04`**（ファミリー行の母集団と並びを扱うサブであり同じ面を触る）**か、独立の新サブ。⇒ 上記 2 のとおり、例外辞書を採るなら独立でも成立する。**

**★`followup` へ新規登録する場合のたたき台**（設計卓が畳む）:

| 欄 | 内容 |
|---|---|
| **スラッグ（案）** | `special-family-label-keeps-strength-word` |
| **何が起きるか** | ファミリー行の表示名に強度語が残る（**実測 31 → 49 行 / 全 372 行**）。弱中強 ＋ OD が押せるファミリーなのに名前が「弱…」で始まる |
| **根拠** | `web/src/features/combo/inputResolution.ts` の `stripStrengthLabel`（接頭形と末尾形しか落とさない）。実例＝`lightning_beast_rolling_attack` ＝「【ライトニングビースト】弱ローリングアタック」 |
| **★機械的に直せない理由** | `【…】弱X` の「弱」は技自身の強度（落とすのが正しい）だが、`X（弱Y派生）` の「弱」は**派生元の強度**（落とすと壊れる）。**⇒ 判別規則の設計が要る** |
| **★開発者の意向** | **「直したい」と表明済み**（2026-09-10 実機確認）**。★汎用アルゴリズムに固執せず例外辞書でもよい、との許可あり**（逐語は上記） |
| **割付の候補と理由** | **`M30-04`**（同じ面を触る）**か独立の新サブ。★例外辞書を採るなら規則の設計が要らないため、`M30-04` を待たずに切れる** |
| **新規/更新** | **新規登録。★状態は「未着手・直す方向で確定」**（「見送り」ではない） |

### 4-4 ★★★案 C が現行データに対して効く行を持たなくなった（**設計判断の前提の変化**）

**2026-09-08 開発者判断の「案 C」＝キャラ固有状態タブを『掲載済み』に数える**（同じ技が未分類にも固有状態にも出る重なりを消すため。当時の実測で 45 件 / 5 キャラ）**。**

**★★本サブの後、実 seed に「未分類に残り、かつ状態 code を含む」行は 1 件も無い**（実測 0 件）**。⇒ 案 C が実データに対して効く行が 0 件になった。**

**★帰結**: `moveSurfacing.test.ts` の「★状態 code を渡すと、その技は未分類へ落ちない」は、**合成 code**（`flame_air_throw`）**でしか動かせなくなった。⇒ 本サブではそのように差し替えてある**（理由をコメントに明記）**。**

**★★規則そのものを残すか畳むかは設計判断であり、製造の手番ではない。⇒ 裁定を請う。** なお**規則を残すことには意味がある**と考える——`M30-04` / `M31-06` が未分類の母集団を動かせば、再び効く行が現れうるためである。

### 4-5 ★レビュー 高-2 の記録（**完了条件が空振りしていた**）

**★指示書ヘッダが完了条件として名指しした「7 組 → 0 件」のテストは、着手時の形では恒真だった。**

**着手時の形**＝「`isOnSpecialTab(兄弟)` が偽である組を数え、0 件を主張する」。**本サブで `isOnSpecialTab` が `category === 'special'` の 1 条件になったため、この式は成立しえない**（兄弟は同じキャラの `moves` から `code` 一致で引くので `category` は必ず `special`）。

**★実測**（レビューと製造が独立に再現）: **兄弟判定に到達する組 102 ／ そのうち `category !== 'special'` の兄弟 0。⇒ `parseSpecialCode` を着手時点へ巻き戻しても緑のままだった。**

**★★是正**＝主張を**述語からファミリーの座標へ移した。** 7 組を**名指しで全数**残し、各組について**素と OD が同じファミリー・同じ変種の中に、別々の強度として在ること**を主張する。**破壊確認（規則 2 を丸ごと外す）で赤になることを確認済み。**

**★★★申し送り**: **「規則を簡約したら、その規則を使っていたテストが恒真になっていないか」を見る観点が要る。⇒ 型検査もカバレッジも緑のままであり、人が読む以外に検出経路が無い。★本件は「完了条件として名指しされた主張」ですら空振りしうることの実例である。**

### 4-6 ★`check-import-order.sh` の実行時メッセージと冒頭コメントが食い違う（**改善レーンの候補**）

**★実行時は「BASELINE を下げること」と出るが、冒頭コメントは「レーンごとに測って下げてはならない」と明示している**（並走レーンぶんが見えずマージ時にずれる。`check-md-emphasis.sh` が 2026-08-14 に踏んだ型）**。本サブは一度下げてコメントを読んで戻した。⇒ 実行時メッセージ側に「レーンでは下げない」の一言が要る。**

### 4-7 ★A1 は引き続き射程外

**A1**（実測 **187 件**）**は `M30-02` で `strength:"none"` として既に載っている。⇒ 本サブは 1 件も触っていない**（指示書 §3-1 のとおり）**。**

---

## 5. 参考（触れていない＝不変の証跡）

- `internal/` ／ `cmd/` ／ `migrations/` — `git diff` 空（**API 契約もスキーマも不変。`DES-002` §4.2 の経路表に載る変更 0 件**）
- `web/src/features/combo/moveSurfacing.ts` — **述語の追加・削除なし**（`isOnSpecialTab` の式が 1 条件へ縮んだのみ。面の枚数・優先順は不変）
- `parseSpecialCode` の**規則 1 と規則 3** — 差分なし（規則 2 を両者の間へ挿入した。§1-1）
- `SPECIAL_VARIANT_ORDER` ／ `splitSpecialVariant` — 差分なし（変種軸は `M30-02` のまま）
- `OD_VARIANT_ORDER` ／ `isOdVariantApplicable` — 差分なし（OD 4 種と活性条件は `M30-02` のまま）
- タブ構成 8 枚と並び順 — 不変。`m30-01-controller-surfacing.spec.ts` の「タブは 8 枚で…」が固定
- 既存 `data-testid` — 新設 0 件（E2E は既存 testid だけを引いている）
- `web/CLAUDE.md` §1 ブラウザストレージ台帳 — 追記なし（新しいキーを使っていない）
- `.claude/rules/enum-sync.md` の列挙定数 — 追記なし（バックエンドの列挙を足していない）
- `go.mod` / `web/package.json` — 差分 0 行（**新規依存 0 件**）
- `scripts/check-import-order.sh` — **最終的に差分なし**（一度下げて戻した＝§4-6）
- 新規ファイル — **0 本**（変更 9 ファイルはすべて既存。教訓 `E-225` の観点で `git diff --stat` を読んだ）

---

## 6. CHANGE 起票のたたき台（設計担当向けチェックリスト）

> **番号は起票時に `docs/handover/change-number-registry.md` §1 で採番すること。★製造は 1 本も消費していない**（`D-293`）**。**
>
> **★マイグレは 0 本消費である。⇒ 連番は動いていない**（本サブ完了時点で `migrations/` の最大は `M30-02` が払い出した `000107` のまま）**。**

| # | 対象 | 内容 | 出所 |
|---|---|---|---|
| 1 | **`DES-004` §2.1** | **必殺技ファミリーの導出規則が「強度語は `code` のどこに在ってもよい」へ広がった。** 規則 1（末尾接尾辞）が規則 2（途中）より常に先であること、規則 2 は強度語を **1 トークンだけ**抜くこと、優先順は `od` → `light` → `medium` → `heavy` であること | §1-1 ／ §3-2 |
| 2 | **`DES-004` §2.1 ／ `DES-005` 第6節** | **`CHANGE-166`**（強度接尾辞を持たない `move_code` はファミリー UI に載らない）**が A1・A2 の両枝とも失効した。⇒ 規則が全体として置き換わる** | §1-1 |
| 3 | **`DES-005` 第6節** | **as-built: 未分類タブの母集団から `category='special'` が消えた**（実測 0 件）。**★タブ構成 8 枚は不変** | §1-1 ／ §1-2 |
| 4 | **`M30-overview`（見込みの更新）** | **本サブの CHANGE 消費は 1 本の見込みだったが、製造は 0 本消費**（起票は設計卓）。**マイグレも見込みどおり 0 本** | ヘッダ表 |

---

## 7. 教訓（retrospective-log 行き・**★親は本節を読まなくてよい**）

> **宛先は `docs/handover/retrospective-log.md` へのバッチ反映であり、実施者は設計担当である。**

1. **★★★述語を簡約したら、その述語を使っていたテストが恒真になっていないかを見る。** 本サブは `isOnSpecialTab` を `category==='special'` の 1 条件へ縮めた結果、**指示書ヘッダが完了条件として名指しした「7 組 → 0 件」のテストが恒真になった**（実測＝兄弟判定に到達する組 102 / うち `category!=='special'` が 0。**実装を着手時点へ巻き戻しても緑のままだった**）。**⇒ テストも lint も型検査もカバレッジも緑であり、人が読む以外に検出経路が無い。★「完了条件として名指しされた主張」ですら空振りしうる。**
2. **★★「破壊確認をした」は「すべての主張を破壊確認した」ではない。** 本サブは破壊確認を 2 本実施していたが、**そのどちらもが上記のテストを対象にしていなかったため検出できなかった**（レビューが検出した）。**⇒ 破壊確認の対象は、指示書が完了条件として名指しした主張から先に選ぶこと。**
3. **★★★「実測」と題した表の中に、測った値と計算した値を混ぜない。** `M30-01` の「未分類 251 → 146」は **251 − 105 の引き算**であり、案 C で既に外れている 59 件を二重に引いていた。**⇒ どちらであるかは表からは読めない。★`M-145`（母集団の取り違え）とは別型である**——**母集団は正しく、算出の経路が実測でなかった。⇒ 数値の隣に「測った／計算した」を書く。**
4. **★★他人の欠陥として書いた作法は、自分にも当てはめる。** 本サブは完了報告 §6.1 で `M30-01` の上記を批判しておきながら、**同じ報告の §6.2 で「327 → 348」を母集団抜きで書いた**（レビュー 中-3 が検出。正しくは総数 342 → 372 で、327/348 は「表示名を持つ行」に絞った値だった）。**⇒ 母集団を 1 行書く規則は、指摘する側にも効く。**
5. **★★「全数直した」と書けるのは、全数を数えたときだけである。** 本サブは完了報告に「撤回済みの記述をこの手番で全数直した」と書いたが、**数えずに書いており、本番ファイルに 1 か所残っていた**（レビュー 高-1。`VirtualController.tsx` の未分類タブの注記）。**⇒ 「全数」と書く前に `grep` する。書いた後でもよいが、書きっぱなしにしない。**
6. **★★同じ形の兄弟が両方向に在るときは、片方だけを根拠に据えない。** 規則 2 の優先順位（`od` 先）の根拠として `cannon_strike_*_od` を挙げたが、**同じキャラ内に位置順を支持する兄弟群も実在した**（レビュー 中-1）。**⇒ 「同点だから一貫性で決めた」と書く前に、反対向きの実例を探す。★実データで 3 件にしか効かなくても、ユーザーが見る形が変わるならハード列である。**
7. **★実行時メッセージと冒頭コメントが食い違う検査スクリプトが在る。** `check-import-order.sh` は実行時に「BASELINE を下げること」と出すが、**冒頭コメントはレーンからの引き下げを禁じている**（§4-6）。**⇒ スクリプトの指示に従う前に、そのスクリプト自身の前提を読む。★本サブは一度下げて戻した。**
8. **★★「汎用規則で解けない」を見送りの既定にしない。** 本サブは表示名の強度語について「機械的に落とすと `(弱…派生)` 型が壊れる ⇒ 判別規則の設計が要る ⇒ 本サブの射程外」と畳んだが、**開発者は実機確認で「直したい」と述べ、あわせて「汎用的なアルゴリズムではなく、例外辞書的なもので強引に対応することも考えて良い」と明示した**（2026-09-10・逐語は §4-3）**。⇒ 対象が数十行に収まるなら、規則を諦めて表で持つ選択肢が最初から在る。★「規則が書けない」と「直せない」は別である。**
9. **★1 回の緑は flaky の反証にならない。** `make e2e` 全数を 2 回測り、**1 回目は flaky 0 件、2 回目に `M30-02` の申し送りどおり `m31-02-tag-field-drag.spec.ts:49` が flaky になった。⇒ 1 回目の時点で「直った」と書かなくてよかった**（報告に「1 回の緑は反証にならない」と書いてあった）。**★申し送られた flaky は、複数回測ってから判断する。**

---

*以上、設計伝達レポート M30-03。*
