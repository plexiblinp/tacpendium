# 設計伝達レポート: M26-03（法務ポスチャの再点検とデータ来歴の切り分け・`A4` ＋ `A3`）

| 項目 | 内容 |
|------|------|
| 作業ID | M26-03 |
| 対象指示書 | `docs/instructions/M26-03-legal-posture-recheck.md` **v1.1.0** ／ チェックリスト `docs/instructions/reviews/M26-03-review-checklist.md` **v1.0.0** |
| **★版の食い違い** | **設計卓が発行した指示書 `v1.2.0` / `v1.3.0` とチェックリスト `v1.1.0` / `v1.2.0` は、製造の作業ツリーに無い**（設計卓ブランチ `claude/design-desk-d697-log-shj48l` 上。`CLAUDE.md` §7 により製造は `fetch` できない）**。⇒ 追走 2 回はリレー文の指示本文を指示として扱った**（リレー文が但し書きでそれを許している） |
| CHANGE | **消費 0 本。★製造は番号を消費していない**（`D-293`）**。⇒ たたき台は §6。宛先は `SUPP-001` 1 本**（§5.5.9 の新設） |
| マイグレ | **消費 0 本。**`ls migrations/` の末尾は `000110` のままで、**次に払い出す番号は動かない** |
| 作成日 | 2026-09-11 |
| 実装コミット | `claude/trusting-brown-7stp5c`・`2e35bbd..09bd42d`（**8 コミット・push 済み**）。差分＝**6 ファイル / +1327 / −7** |
| 源泉 | 完了報告 `docs/progress/M26-03-completion-report.md` ／ レビュー `docs/progress/m26-03-review.md`（**初回 高 2 / 中 7 / 低 4 ＋ 再レビュー 高 3 / 中 2 / 低 3 ＝ 21 件・高の不採用 0 件・往復 2 回＝上限到達**）／ **開発者裁定 4 回**（判定粒度・手順の置き場・外部照会 ／ 節番号の差し戻し ／ 裁定番号と束 B ／ ③④と本人確認）／ **設計卓の裁定 3 本**（`D-819` / `D-820` / `D-821`）／ 実装直後の同一セッションで生成 |
| 宛先 | 設計卓（親チャット） |

**本レポートは ①独自確定仕様 ②契約違反の独自判断 ③製造判断 ④残課題 に絞る。指示書どおりの部分は割愛する。**

**★★最重要は §2-1 と §1-1 である。** 前者は**指示書の例文が一次記録と逆向きで、製造が指示書に従わなかった**件、後者は**受理時にそのまま `SUPP-001` へ移す原稿**である。

---

## §1 製造が独自に確定した実装仕様（設計書反映が要るもの）

### 1-1 ★★`SUPP-001` §5.5.9 の原稿（**受理時にそのまま移す**）

**`D-819` ＋ `D-820` ＋ `D-821` により、判定手順の正本は `SUPP-001` の新節 §5.5.9 と決まった。⇒ 製造は原稿を完了報告 §7.A に置いた。**

| 項目 | 値 |
|---|---|
| 原稿の在り処 | `docs/progress/M26-03-completion-report.md` §7.A（**HTML コメントのマーカーで挟んである**。`awk` で範囲抽出できる） |
| **★挿入位置** | **§5.5.8 の直後**（2 つ目の §5.5.4 の手前）**。⇒ `SUPP-001` の節の並び順は番号順ではない。位置を明示しないと末尾へ付く**（`D-821` 規則 B） |
| 長さ | **76 行**（`D-820` の 60〜80 に収まる） |
| 節名 | `5.5.9 ゲーム側アップデートに追従するときの配布物のデータ来歴の判定`（**§5.5.5 と語頭を揃えた**） |
| 構成 | `(a)` 引き金 4 種 ／ `(b)` 母集団 ／ `(c)` 3 階層と 2 本立ての走査 ／ **`(d)` 判断の分かれ目 9 件**（本体）／ `(e)` 差の見方 ／ `(f)` 何を書き残すか ／ 末尾に「置き場の規約」 |

**★原稿は切り出して成立する形にしてある。** 完了報告内の相対参照は **0 件**（`grep '本書'` で確認）。`§4.2` / `§1.3` / `§4` はすべて文書名で修飾済み。**⇒ 移した瞬間に「本書」が `SUPP-001` を指す事故は起きない。**

**★原稿の先頭に置いた引用行は、反映時に落としてよい。**（見出しの直後にあるため、落とし忘れても §5.5.9 の内側に収まる）

### 1-2 窓口導線の as-built（`README.txt` ／ `NOTICE` §3）

**`D-805` の決着（専用アドレスを用意する）に従い、導線 2 本を差し替えた。★`CONTRIBUTING.md` は触っていない**（`D-814` により `M26-04` の射程）。

| ファイル | 内容 |
|---|---|
| `README.txt:138-147` | 権利者の申し出とデータ出所の問い合わせの窓口を 1 項追加。**撤退ポリシーは `NOTICE` を参照させる**（本文を再掲していない） |
| `NOTICE:32-43` | §3 の回し先を `SECURITY.md` から専用窓口へ。**アドレスは伏せ字**（`<権利者・一般問い合わせ窓口のアドレス>`） |

**★共通の性質 3 つ**——**(1) 返答の期限を約束しない ／ (2) DMCA 経路を案内しない**（`D-805` で推奨撤回済み）**／ (3) 脆弱性は本窓口ではない旨を明記して `SECURITY.md` と分離。**

**★★2026-09-11 に開発者の判断で 1 行を追加した**（下記 §3-1 の 7）——**「お申し出の内容を確かめるため、いただいた連絡先へ折り返し照会することがある。⇒ これは取り下げの条件ではない。」**
**⇒ `NOTICE` §3 の撤退ポリシーは無条件のままである。★条件付きにすると `framedata-legal` §4.3 が「本件の実質的な盾」と呼ぶ撤退ラインが弱まるため、約束と手続きを分けた。**

**⇒ `DES` 反映は不要**（配布物の文面であり設計書の記述対象ではない）。**★ただし `NOTICE` §3 の as-built が変わったことは記録が要る。**

---

## §2 契約・設計に反する独自判断（**★設計卓の裁定が要る**）

### 2-1 ★★★指示書の例文に従わなかった（**最重要**）

**何に反したか**: 指示書 `M26-03` **§2.3-3 / §4.3** ／ チェックリスト v1.0.0 **束 A-2**。

**逐語**——「**フレーム値は事実、`command` は開発者の補完**」。**⇒ 列ごとに来歴が違う例として挙げられている。**

**★リポジトリ内の一次記録は、この 2 つを逆向きに述べている。**

| | 指示書の例 | 一次記録 |
|---|---|---|
| フレーム値 | 「事実」＝カプコン由来と読める | **`docs/seed-data/moves-input-background.md:51`＝「公式データを `character_data` へ転記しない。開発者が実機を見て手入力する」／ `check-criteria.md:64` `:66`＝公式値との系統的な差は意図的** |
| `command` | 「開発者の補完」＝開発者由来と読める | **`docs/progress/M14-RESEARCH-02-report.md` §C-3-4＝「`command` は公式と完全一致（差分 0）」／ `character_data/command-correction-history.md:3`＝「公式データとの突合で補完した全件記録」** |

**実装（判定）がどうなっているか**: **製造は指示書の例文に従わず、一次記録の逐語に従って判定した**（完了報告 §3.4 ／ §3.3 の 24 列表）。
**⇒ 第三の整理を出した＝「情報」と「表記」に分ければ両立する。** `command` は**情報内容が公式由来で、表記は本アプリのトークン語彙**である（`M14-RESEARCH-02-report.md:44`＝トークン語彙は importer の `README.md` を正とした）。

**なぜそう判断したか**: **指示書の例文に従うと、24 列の判定表が逆向きに埋まる。⇒ 層 B に入れるべきものの判断が狂う。★一次記録は複数あり、いずれも同じ向きである。**

**★設計卓の手番**: **指示書 §2.3-3 / §4.3 と、チェックリストの束 `A-2` の両方に同じ逆向きの例が写っている。⇒ どちらも直す手番が要る。★製造はどちらも編集していない。**

### 2-2 他サブの歴史記録を書き換えた（**開発者裁定による**）

**何に反したか**: 一般原則「**完了報告・レビュー報告は歴史記録として書き換えない**」（`D-274` (3)）／ `progress-log.md` の「**追記のみ・既存の過去節は編集しない**」。

**何をしたか**: **`docs/progress/M14-RESEARCH-02-report.md` §C-3-7 の表と `progress-log.md` の該当行から、公式の生フレーム値・ダメージ値を落とした**（§3-1 の 5）。

**なぜ**: **2026-09-11 の開発者判断**（案 (ii)＝該当箇所だけ要約・抽象化）。**⇒ 製造が独自に決めたのではない。**

**★どうなっているか**: **落とした事実と理由をその場に注記してある**（黙って書き換えていない）。**件数・技・列・是正の向きは落としていない。⇒ 表の主張は値を引かずに成立する。**

**⇒ 設計卓の手番**: **この処置を是とするか。★是とするなら、`D-742` の除外が「場所」で切られていて「内容」では切れていなかったこと**（§4 の `A3` の適用範囲）**を裁定として記録してほしい。**

---

## §3 製造の判断

### 3-1 開発者へ確認して確定した点

| # | 事項 | 確定内容 |
|---|---|---|
| 1 | **来歴判定の粒度**（母集団 2954 件を全数ファイル単位で見るか） | **階層判定**——グロブ単位 → 混在領域だけファイル単位 → CSV は列単位 |
| 2 | **判定手順の恒久的な置き場** | **設計卓へリレー。⇒ `D-819` / `D-820` / `D-821` で `SUPP-001` §5.5.9 と確定**（製造の案 A / B / C はいずれも不採用） |
| 3 | **外部一次情報の確認** | **`/ask_external_ai` のプロンプトを 1 本生成して開発者が実施。★製造は外部へ 1 バイトも送っていない** |
| 4 | **裁定番号の引き方** | 当初は `D-819` ＋ `D-821` の併記（**検証できない番号は引かない**）。**⇒ 束 B v1.2.0 の逐語で `D-820` が検証できたため 3 本の併記へ改めた** |
| 5 | **公開される 2 ファイルの公式生値** | **案 (ii)＝該当箇所だけ要約・抽象化**（§2-2） |
| 6 | **`name_ja` の整理** | **開発者が製造の当初の整理を訂正した**（§3-2 の 2 を参照） |
| 7 | **窓口での本人確認** | **文面案を採用。⇒ 折り返し照会は手続きであって取り下げの条件ではない**（§1-2） |

### 3-2 推測で進めた点・訂正された点

| # | 事項 | 扱い |
|---|---|---|
| 1 | **「三層の割当そのもの」の解釈**（指示書 §3-2 の禁止範囲） | **推測: 層の定義とパスの層への振り分けを指し、除外範囲の表現形式は含まない、と仮定した**（完了報告 §4.3 に明示）。**⇒ そのうえで `REUSE.toml` は 1 バイトも触っていない** |
| 2 | **★`name_ja` の来歴の整理**（製造の当初案は**誤りであった**） | **製造は「独自表記 ／ 公式表記」の軸で整理した。⇒ 2026-09-11 に開発者が訂正した**——**分かれ目は取得経路である。固有技名はインゲームで表示される公然の事実の観測であって、公式サイトの技表からの転記ではない。★フレーム値と同じ原則が列をまたいで通っている**（完了報告 §1.6） |

### 3-3 ★製造が自分で見つけて直したもの（**再発防止の形まで落とした**）

| # | 何が起きたか | 是正 |
|---|---|---|
| 1 | **完了報告自身が、除外の根拠を書くために公式の生値を引き写していた**（初回レビュー 高-2） | 転記を落とし、**顛末を原稿の分かれ目 9 として残した** |
| 2 | **★レビュー報告書自身も同じ欠陥を持っていた**（追走で発見） | 同上。**★そのとき走査は 1 行しか拾わなかった**——`docs/seed-data/` 由来の引用は「実測値（公式 N）」「`NN→NN`」の形で、英語の列名も 2 桁以上の数値も伴わないため狭い網を素通りする |
| 3 | **★走査そのものが使い物にならないことが分かった** | 網を広げると**150 件超**に当たり（版数の矢印に埋もれる）、合否に使えない。**⇒ 2 本立てへ改めた**〔全体は狭い網 ／ **自分が足した行だけは広い網**〕**。★結論は「走査を直せば済む」ではない。機械で決められないので、書き方の側で守るしかない** |
| 4 | `§8-10` が `DRAFT` マーカーの文字列を逐語で書いており、**`grep` での原稿抽出が 2 件に当たり範囲指定が壊れた** | 逐語をやめ、理由つきで注記した |

---

## §4 設計担当が未把握の残課題・申し送り

**★`followup-backlog.md` の本表は製造が編集していない**（`D-382`）。**⇒ 設計卓が畳めるよう、スラッグ・根拠・割付の候補・新規か更新かを揃える。**

| # | スラッグ（候補） | 何が起きるか | 根拠 | 割付の候補と理由 | 登録の別 |
|---|---|---|---|---|---|
| **1** | **`a3-exclusion-cut-by-location-not-content`** | **`D-742` の除外は「場所」で切られており「内容」では切れていない。** 同じ性格の公式生値が、除外されていない `docs/progress/` の 2 ファイルに在った | 完了報告 §4.2（`M14-RESEARCH-02-report.md` §C-3-7 ／ `progress-log.md` の該当行） | **★2026-09-11 に案 (ii) で処置済み**（§2-2）。**⇒ 残るのは「`A3` の適用範囲を内容で切る」という一般則の裁定である** | **新規登録**（**状態＝処置済み・一般則は未裁定**） |
| **2** | **`three-layer-lacks-third-party-axis`** | **三層は「SF6 の事実か否か」の軸しか持たず、「第三者素材か否か」の軸が無い。** 見つかった逆向き 3 件はすべて後者で、**三層のどこへ置いても解けない** | 完了報告 §5.2 ／ §2.3（`LICENSES/` 4 件 ／ `web/src/components/ui/` 21 件 ／ 上記 2 ファイル） | **公開ゲート `A` の設計。⇒ 軸を足すか、除外で切るか。★現状は除外で切る運用であり、それで足りるなら軸は要らない** | **新規登録** |
| **3** | **`reuse-toml-seed-data-declaration-provisional`** | **`REUSE.toml:73-77` が `docs/seed-data/**` の層 B 宣言を「暫定」と自認し、本サブでの再判定を名指ししていた。⇒ 再判定の結果、3 件（＋境界 1 件）に第三者素材が現存すると確認した** | 完了報告 §4.1 ／ §4.3 | **★製造の推奨は案 B**（`LicenseRef-` 形式で「配らない範囲」を宣言に書く）**。⇒ CHANGE の要否は設計卓の判断。★`REUSE.toml` は触っていない** | **新規登録** |
| **4** | **`rights-holder-contact-channel-missing`** | **既存行が `D-814` を反映しておらず**、「導線 3 本」「`M26-03` v1.0.0」のまま。**⇒ 実際は導線 2 本を差し替え済みで、`CONTRIBUTING.md` は `M26-04` の射程** | `followup-backlog.md:1399` ／ 完了報告 §6 | **既存行の更新。⇒ 「導線 2 本を差し替え済み・アドレスは伏せ字・実アドレスの差し替えは公開の手番」へ** | **★既存行の更新** |
| **5** | **`csv-column-contract-23-vs-24-stale`** | **「CSV は 23 列」という失効した記述が 2 か所に残っている**（実体は 24 列＝`CHANGE-159`） | `.claude/commands/precheck_seed_data.md:40` ／ **★`internal/seedgen/model.go:21`**（同じ `var csvColumns` の要素は 24 個） | **改善レーン。★後者は本番コードのコメントで、`go test` / `go vet` / `tsc` が一切検出しない**（`D-250` と同型）**。★正本側**（`SUPP-001` §3.2 ／ `DES-003`）**は正しい** | **新規登録** |
| **6** | **`shadcn-ui-vendored-without-attribution`** | **`web/src/components/ui/` の 21 件が shadcn/ui 由来で、帰属表示がどこにも無い。★`NOTICE` §5 は `go.mod` / `package.json` を源泉とするため、コピーして持ち込んだソースは構造上載らない** | 完了報告 §2.3 の `X-2`（`web/components.json` の `$schema` 宣言） | **`M26-01` 系または `M26-04`。★ライセンス条件の確認が先**（外部照会が要る＝`U-3`）**。許諾ではなく帰属の問題である可能性が高い** | **新規登録** |
| **7** | **`battlelog-decision-host-mismatch`** | **`combmgr-battlelog-source-decision.md:27` は `cid.capcom.com` の robots 拒否を根拠に挙げているが、Buckler's Boot Camp は `www.streetfighter.com` 側の別サービスである** | 完了報告 §1.4.2（**2026-09-11 の外部照会**） | **★結論は変わらない**——同書 `:55` が「条文確認は補強であって決定の前提ではない」と自ら書いている。**⇒ 開発者が次に同書を開くとき根拠のホストを 1 行直せば足りる** | **新規登録** |
| **8** | **`legal-decision-docs-have-no-decision-date`** | **法務決定文書 2 本がどちらも判断日を持たない。⇒ 「前提が変わっていないか」を確かめる基準日が文書から決まらない** | 完了報告 §1.3-A（`framedata-legal:119` ／ `battlelog-source:3`） | **開発者**（開発者のファイル）**。⇒ 次に開くときヘッダへ日付を 1 行** | **新規登録** |
| **9** | **`name-ja-provenance-not-documented`** | **`name_ja` の取得経路が `DATA-LICENSE.md` にも `NOTICE` にも書かれていない。⇒ フレーム値と同じ原則が通っているのに明文が無い** | 完了報告 §1.6（**2026-09-11 開発者訂正**） | **★`NOTICE` §2 が既に「手入力と観測に基づく事実情報」と書いており、`name_ja` もそこに含まれる旨を 1 句足せば足りる。★「独自表記 ／ 公式表記」の軸で書かないこと** | **新規登録** |
| **10** | **`a4-premises-change-unverifiable`** | **外部照会で現在値の一部は確認できたが、「直近 1 年で変わったか」は 4 件すべて不明のままである。⇒ 「変わっていない」とは書けない** | 完了報告 §1.4.1〜1.4.3 ／ §3.5 の `U-4` | **★残る不明 3 つ**＝利用者規約の本文（**403** で未取得）／ 4 件の変化の有無 ／ Buckler 側の robots。**⇒ 公開前に再度確認するかは開発者の判断** | **新規登録** |

**★`followup-backlog.md` §J（停止時記録）への登録は 0 件である。** 往復上限（2 回）に達したが、**未解消の指摘が 0 件のため記録すべき項目が無い**（`CLAUDE.md` §9）。

**★「判定できない」6 件（`U-1`〜`U-6`）は完了報告 §3.5 に表で残してある。⇒ うち `U-1` / `U-2` / `U-6` は公式 dist（`combomgr-importer/`）を接続した瞬間にまとめて閉じる。**

---

## §5 参考（触れていない＝不変の証跡）

- `git diff --stat 2e35bbd -- docs/design/` が**空**（`CLAUDE.md` §8。原稿を置いただけで反映はしていない）
- 同 `-- character_data/` が**空**（CSV 31 本・md 4 本とも 0 バイト差分）
- 同 `-- docs/human-notes/future-notes/` が**空**（法務決定文書 2 本は読むだけ）
- 同 `-- CONTRIBUTING.md` が**空**（`D-814`＝`M26-04` の射程）
- 同 `-- REUSE.toml` が**空**（三層の割当は変えていない）
- 同 `-- SECURITY.md` が**空**（脆弱性の窓口は `M26-04` の射程）
- 同 `-- docs/process/ scripts/` が**空**（新規ファイル 0 件）
- `bash scripts/check-migration-license.sh` ＝ `結果: 違反なし`（凍結表 層 A 30 / 層 B 72・218 件の解決は A 64 / B 154）
- `bash scripts/check-public-snapshot.sh` ＝ `結果: 違反なし`
- `go test ./...` ＝ 緑（**Go コードを 1 行も触っていない**）
- `E-225` の確認＝新規 3 ファイルはいずれも **deletions 0**。全体の deletions は `NOTICE` の差し替え 1 行のみ

---

## §6 CHANGE 起票のたたき台（設計担当向けチェックリスト）

**★番号は起票時に registry で採番する。製造は消費していない**（`D-293`）。**⇒ `D-821` の追伸は `CHANGE-182` を見込みとしていた。**

| # | 項目 | 内容 |
|---|---|---|
| 1 | **宛先文書** | **`SUPP-001` 1 本のみ**（§5.5.9 の新設） |
| 2 | **反映内容** | 完了報告 §7.A の原稿（**76 行**）を **§5.5.8 の直後**へ挿入。**★先頭の引用行は落としてよい** |
| 3 | **相互参照** | **§5.5.5 ／ §5.5.6 と相互参照を張る**（`D-819` の決め手が「同じ動機の節が既に在る」ことであったため） |
| 4 | **索引の張り替え** | `followup-backlog` の `sf6-major-update-after-release` の引き先を **§5.5.9** へ（**★`D-821` の追伸は §5.5.7 としていたが、同番号は既存節である**） |
| 5 | **スキーマ変更** | **なし** |
| 6 | **マイグレ消費** | **0 本。**`ls migrations/` の末尾は `000110` のまま。**⇒ 次に払い出す番号は動かない** |
| 7 | **新規依存** | **なし**（`go.mod` / `package.json` とも差分 0 行） |
| 8 | **★あわせて裁定がほしいもの** | **§2-1**（指示書とチェックリストの例文の是正）／ **§2-2**（歴史記録の書き換えの是非と `A3` の適用範囲の一般則） |

---

## §7 教訓（`retrospective-log` 行き・**設計卓は本節を読まなくてよい**）

**宛先は `retrospective-log` へのバッチ反映であり、実施者は設計担当である。**

| # | 教訓 |
|---|---|
| 1 | **「公式値が公開物に在るか」を調べる文書は、その値を引用しないで書ける形を先に考える。** 走査の母集団には、いま書いている自分の文書が入る。**⇒ 本サブでは完了報告とレビュー報告の両方が、指摘した当の欠陥を再生産した。** 根拠は行番号・件数・値の形で示せる |
| 2 | **★正規表現による走査は網であって証明ではない。** 狭い網は取りこぼし（英語の列名を伴わない引用・1 桁の数値）、広い網は版数の矢印に埋もれて合否に使えない（実測 150 件超）。**⇒ 2 本立てにする**〔全体は狭い網 ／ **自分が足した行だけは広い網**〕**。★機械で決められないものは、書き方の側で守る** |
| 3 | **★節を組み替えるときは、その節が抱えていた「取り込み済みの指摘」を数え直す。** 本サブは §7 を組み替えた際に、採用済みの注記を 1 件消したまま、取り込み表には「採用」と残していた。**⇒ 取り込んだ記録と成果物が食い違う。★機械検査は 1 つも鳴らない** |
| 4 | **★裁定に書かれた番号は、実物で検算してから使う。** `D-819` は `SUPP-001` §5.5.7 の新設を指示したが、同番号は既に埋まっていた（§5.5.8 も）。**⇒ 写していれば既存節の上へ新節が乗った。★節番号は外部から名指しされうるため、繰り下げも採れない** |
| 5 | **★「別の文書へ移す原稿」は、切り出したときに成立するかで読み直す。** 完了報告の中では正しく読める相対参照（「本書の §N」）が、移した瞬間に別文書を指す。**⇒ 移す前提の文書は、参照を全数外部参照へ書き換える。★`grep` で機械確認できる** |
| 6 | **一次記録が指示書と食い違うときは、一次記録に従って進めて報告する。** 本サブでは指示書の例文が逆向きで、従えば 24 列の判定表が逆に埋まるところだった。**⇒ 止めるのではなく、実測と逐語を並べて返す**（`CLAUDE.md` §9 の「進めて報告する」） |
| 7 | **マーカーで範囲抽出させる文書は、マーカーの文字列を本文に逐語で書かない。** `grep` が 2 件に当たり範囲指定が壊れる。**⇒ 本サブの検証で実際に踏んだ** |

---

*以上、`M26-03` 設計伝達レポート。* **★受理時に要る手番は 2 つ**——**(1) §1-1 の原稿を `SUPP-001` §5.5.8 の直後へ反映し `CHANGE` を採番する ／ (2) §2-1 と §2-2 の裁定。**
