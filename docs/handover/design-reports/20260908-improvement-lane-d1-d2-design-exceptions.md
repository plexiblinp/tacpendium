# 設計伝達レポート（例外レポート） — 改善レーン D1 / D2

| 項目 | 内容 |
|---|---|
| **対象** | **親チャット（設計卓）** |
| **発信** | 製造担当 Claude Code（2026-09-09 生成。作業日 2026-09-08 付の ID） |
| **指示書** | **無し**（改善レーンは投入プロンプト直接。`docs/instructions/` に対応ファイルは存在しない） |
| **実装コミット** | ブランチ `claude/improvement-lane-d1-d2-g18pqg`（**push 済み。設計卓は HEAD で読める**） ／ 起点 `a6a072f` ／ **実装 2 コミット**（`1d9f350` 実装 ／ `d7f5687` 完了報告・索引行）＋ **本レポート自身**（`5a470cc`。以後の微修正はその後続） |
| **関連** | 完了報告 `docs/progress/20260908-improvement-lane-d1-d2-completion-report.md` ／ **レビュー報告は無し**（本手番はレビュー工程を持たない） |
| **源泉** | 完了報告 ＋ **実装直後の同一セッション内で生成**（セッション中の実測を反映済み） |

本書は **①独自確定仕様 ②契約違反の独自判断 ③製造判断 ④残課題**に絞る。指示どおりの部分は割愛する。

**★最重要は §2-1 と §4-1 である。**
**§2 に項目が 2 件ある**（受理／却下の裁定が要る）。
**★§4-1 は「設計卓が過去に出した資料 3 つが揃って事実と違っていた」件であり、訂正は設計卓の手番である。**

---

## §1 製造が独自に確定した実装仕様（DES 反映が要るもの）

**なし。**

本手番は `scripts/` ／ `.claude/` ／ `web/e2e/` のコメントのみを触っており、
**`internal/` ／ `cmd/` ／ `migrations/` ／ `web/src/` の差分は 0 バイト**である。
**⇒ `DES-002` §4.2 の経路表に載る API の新設・変更は無い**（新設 DTO・エラー契約・母集団の述語のいずれも発生していない）。
CHANGE 0 本・マイグレ 0 本・新規依存 0。

---

## §2 契約・設計に反する独自判断（★裁定が要る・2 件）

### §2-1 ★★指示は「新しい流儀を作らないこと」を求めたが、**2 ブロック構成**にした

**何に反したか**: 投入プロンプト D1-2 の逐語 —
**「形は `check-progress-log-index.sh` の `CLI_FILES` ブロックと同じにしてください。新しい流儀を作らないでください」**。

**なぜそう判断したか**: **前提が事実と違ったためである**（詳細は §4-1）。
指示は対象を 5 本と想定していたが、**マーカーは 4 本にしか無く、指示書テンプレートには一度も入っていない。**
同時に「やらないこと」が **マーカーそのものの増減**を禁じている。
**⇒ 単一ブロックのままでは、backlog が求めた 5 つ目の口（テンプレート §7.4）が素手のまま残る。**

**実装がどうなっているか**: `scripts/check-completion-report-md-emphasis.sh`

| ブロック | 対象 | 判定 | パス |
|---|---|---|---|
| 1 | 製造 CLI **4 本** | マーカー `<!-- COMPLETION-REPORT-MD-EMPHASIS -->` の存在 | `:75-80`（`CLI_FILES`） |
| 2 | 指示書テンプレート | **本文パターン** `scripts/check-md-emphasis.sh <path>` の存在 | `:83-84` |

**★「新しい流儀」ではないと判断した根拠**: `check-stop-discipline.sh:34-35` が既に
**ファイル種別ごとに別パターン**を持つ（`SECTION_MARKER` は `followup-backlog.md`、`TEMPLATE_MARKER` は CLI 4 本）。
**⇒ 同じ体系の 3 本目として揃えた。** ブロック 1 の形は指示どおり `CLI_FILES` ブロックの写しである。

**★弱点を自覚している**: 本文パターンは**文言に依存し、マーカーより壊れやすい**。
スクリプト冒頭の「限界」節と、赤くなったときの `note`（`:242`）に
**「言い換えただけなら `TEMPLATE_RULE_PATTERN` を追随させること」** を明記した。

**⇒ 親への請求（二択）**:

- **(a) 受理**: 現状の 2 ブロック構成を残す。**テンプレート §7.4 を書き換えるときは `TEMPLATE_RULE_PATTERN` の追随が要る**という制約が恒久的に付く
- **(b) テンプレートへマーカーを足す**: ブロック 2 をブロック 1 へ統合でき、**文言依存が消える**（強い）。**★製造は足していない**——「やらないこと」がマーカーの増減を禁じているため

**★製造の推奨は (b) である。** ただしマーカーの増減は本手番の射程外として実行していない。

### §2-2 指示の名指しは 2 か所だが、**8 か所**を直した

**何に反したか**: 投入プロンプト D2 は 2 か所（`add_e2e_spec.md:91` ／ `m12-06-presence-detection.spec.ts:17-19`）を名指しし、
追加分については **「見つかったものは §3-1 の判断に従ってください」** としていた。
**★しかし投入プロンプトに §3-1 は含まれていなかった**（プロンプトの節構成は 前提 / 最初に読むもの / D1 / D2 / 検査 / 完了条件 / やらないこと）。
**D1 側の「§3-3 の判断があります」も同様に参照先が届いていない。**

**なぜそう判断したか**: **完了条件 4 が「走査で同型が残っていないことが示されている」を求めている。**
**⇒ 名指しの 2 か所だけでは条件を満たせない。**
`CLAUDE.md` §9 共通原則 1（**止めて聞くではなく進めて報告する**）に従い、
ハード列に当たらない実装詳細として自己判断で進め、本書で報告する。

**実装がどうなっているか**: 雛形 1 か所 ＋ spec 7 本 = **8 か所**。いずれもコメントのみで挙動に触れない。
**`make e2e-only` で全 11 テスト緑**（`m12-06` の 3 本 ＋ 触った 5 spec の 8 本）。

**⇒ 親への請求**: **§3-1 / §3-3 の逐語が製造へ届いていない。**
改善レーンの投入プロンプトが外部の節を参照する形なら、**その節も一緒に投入する**必要がある。

---

## §3 製造の判断

### §3-1 開発者へ確認して確定した点

**なし**（本手番中に開発者とのやり取りは発生していない）。

### §3-2 推測で進めた点

| # | 推測した内容 | 根拠 | 実装 |
|---|---|---|---|
| 1 | **テンプレートへマーカーを足さない** | 「やらないこと」の **「マーカーそのものを 5 本から増減させること」**。実数が 4 本でも、足せば増加になる | §2-1 のブロック 2 |
| 2 | **`review_plan.md` を `CLI_FILES` に含める** | md-emphasis の手順は**レビュー報告書も `docs/progress/` へ新規ファイルを作る**ため同ファイルに実在する（`review_plan.md:216`）。`check-progress-log-index.sh` が 3 本なのは索引行がレビュー工程に無いからで、性格が違う | `CLI_FILES` は 4 本 |
| 3 | **`check-artifact-integrity.sh` を編集しない** | 同スクリプト `:259` が `find scripts -maxdepth 1 -name 'check-*.sh'` の **glob で自動発見**する。**推測ではなく件数の増加（14 → 15）で実測確認した** | 差分なし |
| 4 | **`CLAUDE.md` §8 の表で `check-progress-log-index.sh` の直後へ置く** | マーカー存在検査の 3 本を隣り合わせにするため | `CLAUDE.md:251` |

### §3-3 ★陽性対照の後始末に `cp` ＋ `sha256sum` を使った

`git checkout` / `git restore` は `.claude/settings.json` の `deny` により実行できない。
**⇒ 事前にセッション scratchpad へ退避し、`cp` で戻して `sha256sum -c` と `git diff --stat` の両方で確認した。**
**2 本とも 1 バイトも変わっていない**（`.claude/commands/incorporate_plan.md` ／ 指示書テンプレート）。

---

## §4 設計担当が未把握の残課題・申し送り

### §4-1 ★★先行資料 3 つが揃って事実と違う（**設計卓の手番**）

**何が起きているか**: **`<!-- COMPLETION-REPORT-MD-EMPHASIS -->` は 4 本にしか無い。5 本ではない。**

**根拠（再現条件）**:

```
$ grep -rn "COMPLETION-REPORT-MD-EMPHASIS" . --exclude-dir=.git
  → 実装ファイルの該当は製造 CLI 4 本のみ
     implement_plan.md:201 / implement_plan_full.md:337 / incorporate_plan.md:150 / review_plan.md:216

$ git log -S 'COMPLETION-REPORT-MD-EMPHASIS' --oneline -- 'docs/instructions/templates/M{N}-{NN}-{slug}.template.md'
  → 0 件
```

**`-S` は当該文字列の出現数が変化したコミットを拾う。⇒ 0 件は「入ったことも消えたこともない」を意味する。**
テンプレート §7.4 に実際に在るのは **手順の本文と記入指針コメントだけ**である。

**誤っている記述の一覧**:

| 資料 | 誤っている記述 | 種別 |
|---|---|---|
| `docs/handover/followup-backlog.md` `completion-report-md-emphasis-marker-unchecked` | **「★ただし対象は 5 本である〔製造 CLI 4 本 ＋ 指示書テンプレート §7.4〕」** | **既存行の更新** |
| `docs/progress/20260907-improvement-lane-c1-c3-completion-report.md` | 表 5 行目に **「○」**、直後に **「節にはマーカーを置き」** | **歴史記録**（`D-274` (3) により書き換えない） |
| `docs/handover/design-reports/20260907-improvement-lane-c1-c3-design-exceptions.md` §1-3 | 見出し **「マーカーを 5 本 ＋ テンプレートへ置いた」** ＋ 表 5 行目 | **歴史記録**（同上） |

**★製造は `followup-backlog.md` を 1 文字も編集していない**（`D-382`。書けるのは §J のみ）。
**⇒ 本表の訂正は設計卓の手番である。**

**★一般形（`retrospective-log` 行きの候補）**: **「置いた」という報告は、置かれたことの証拠にならない。**
`check-artifact-integrity.sh` の冒頭が同じ趣旨を書いている
（引き継ぎ書が「すべて `--self-test` を持つ」と書いていたが、走らせたら 1 本は持っていなかった）。
**⇒ 今回もその再発である。1 回 `grep` すれば分かる誤りが、3 つの資料へ複製されていた。**

### §4-2 `followup-backlog.md` の状態更新（**設計卓の畳み込みを請う**）

| スラッグ | 新規/更新 | 更新後の状態 | 根拠 |
|---|---|---|---|
| `completion-report-md-emphasis-marker-unchecked` | **更新** | **解消**（`scripts/check-completion-report-md-emphasis.sh` 新設） ＋ **§4-1 の事実誤認の訂正** | 完了報告 §1 / §2 |
| `add-e2e-spec-template-stale-ports` | **更新** | **解消**（雛形 1 か所 ＋ spec 7 本を是正。走査で同型 0 件） | 完了報告 §3 |
| `claude-md-e2e-isolation-claim-false-both-ways` | **更新** | **★(2) と ★(3) が解消**。**★(1)（`testid-convention.md`）は §4-3 のとおり残る** | 完了報告 §3 / §6-2 |

### §4-3 ★`docs/design/testid-convention.md:62` に旧ポートの原文が残る（**設計卓の手番**）

**何が起きるか**: `CHANGE-171`（2026-09-08）が `:55` へ **「旧記述は失効した」** という打消しバナーを置いたが、
**`:62` の原文はそのまま残っている**。逐語 —
**「ポート 47318(backend)と 5173(vite)が利用可能であること: `reuseExistingServer: true` のため、起動済みなら再利用し、未起動なら Playwright が自動起動する。」**

**⇒ バナーと原文が同居しており、`:62` だけを読んだ人は旧前提を信じる。**
実態は `reuseExistingServer: false`（`web/playwright.config.ts:152` / `:179`）であり、**再利用は起こらない。**

**★`docs/design/` は編集禁止のため製造は触っていない。**

### §4-4 ★`.claude/commands/add_e2e_spec.md` §3-3 が、畳んだはずの運用を今も指示している（**新規登録の候補**）

| 欄 | 内容 |
|---|---|
| **スラッグ（案）** | `add-e2e-spec-testid-inventory-step-stale` |
| **何が起きるか** | 同 CLI §3-3 が **「`docs/design/testid-convention.md` の『付与済み一覧』を Edit で更新」** を指示している。しかし **`CHANGE-171` がその一覧運用を畳んだ**（`D-782` で開発者承認）。現行の同書は **「本表は全数ではない」「新規付与時に本表を更新する義務は無い」** と明記する。**⇒ CLI が設計書に反する手順を指示しており、このコマンドを使うたびに複製される** |
| **根拠** | `.claude/commands/add_e2e_spec.md` §3-3（`3-2` の直後） ／ `docs/design/testid-convention.md:66` |
| **割付の候補と理由** | **`CHANGE-171` を起票した側**（設計卓）。§3-3 を**削る**のか、**「一覧は畳んだので更新不要」と書き換える**のかは、畳んだ側の意図に属する |
| **新規/更新** | **新規登録** |

**★D2 とまったく同型である**（陳腐化した雛形が、使うたびに陳腐化を複製する）。
**旧ポートの件ではないため本手番の射程外とし、直していない。**

### §4-5 `make e2e-only P=<正規表現>` に `|` を渡せない（**新規登録の候補・軽微**）

| 欄 | 内容 |
|---|---|
| **スラッグ（案）** | `make-e2e-only-p-unquoted` |
| **何が起きるか** | `Makefile:179` が `cd web && pnpm e2e $(P)` と**クォートせずに展開**するため、`P="a\|b"` がシェルのパイプとして解釈され **`Error 127` ＋ `EPIPE`** で落ちる。**★エラーが Node のスタックトレースで出るため「テストが落ちた」と誤読しうる** |
| **再現条件** | `make e2e-only P="character-default\|combo-custom-states"` → `make: *** [Makefile:174: e2e-only] Error 127`。**回避は空白区切り**（`P="a b c"`。本手番はこれで 8 テスト緑） |
| **割付の候補と理由** | **開発者**（`Makefile` は開発者のファイル）。**★直すか、使い方の例へ「`\|` は使えない」を 1 行足すかの二択**。実害は小さい |
| **新規/更新** | **新規登録** |

### §4-6 情報提供型の検査が出した 2 件（**いずれも本手番と無関係**）

- **`check-import-order.sh` が「ベースラインより 1 ファイル少ない → BASELINE を 100 へ下げること」と出す。**
  **★本手番は `web/src/` を 1 ファイルも触っていない**（`git diff --name-only a6a072f..HEAD -- web/src` = **0 件**。同検査の走査範囲は `web/src/` のみ）。
  **⇒ 先行サブが下げた分である。ベースラインを下げるのは本手番の射程外とした**——
  **原因を作っていない手番が他の検査の床を動かすと、誰がいつ下げたのかが追えなくなる。**
- **`check-derived-docs.sh` が 3 件を「陳腐化疑い」と出す**（`code-facts` 18% ／ `docs-map` 11% ／ `custom-commands` 38%）。
  **★3 件とも本手番より前から ⚠ である。** `custom-commands` の源泉最終コミットは本手番のコミットだが
  （`.claude/commands/add_e2e_spec.md` を触ったため）、**コマンドの新設・削除はしていないので掲載内容は変わっていない。**

### §4-7 停止時記録（§J）

**なし。** 上限（再レビュー往復・タイムボックス・終了指示）に達しておらず、**射程の全項目を完了した。**
**⇒ `followup-backlog.md` §J への追記は行っていない。**

---

## §5 参考（触れていない = 不変の証跡）

- `git diff --stat a6a072f..HEAD -- internal/ cmd/ migrations/ web/src/` → **空**（アプリ本体は 0 バイト）
- `scripts/check-md-emphasis.sh` → **差分なし**（走査範囲・`BASELINE_BROKEN` を触っていない＝「やらないこと」）
- `scripts/check-artifact-integrity.sh` → **差分なし**（glob 自動発見のため編集不要。§3-2 #3）
- `docs/design/` → **差分なし**（編集禁止。§4-3 で請求）
- `docs/handover/followup-backlog.md` → **差分なし**（`D-382`。§4-2 で請求）
- 陽性対照で一時改変した 2 ファイル → `sha256sum -c` で **OK**、`git diff --stat` で **空**
- `make e2e-only P=m12-06` → **3 passed** ／ 触った他 5 spec → **8 passed**
- 常設検査 14 本 ＋ 完了報告のファイル引数モード → **全 15 本 EXIT=0**（★最後に実施）

---

## §6 CHANGE 起票のたたき台（設計担当向けチェックリスト）

**CHANGE は不要と判断した。**

- `DES-002` §4.2 の経路表に載る API の挙動変更 → **なし**
- `DES-003` のスキーマ・論理削除の契約 → **触れていない**
- `DES-006` の VAL の判定結果 → **変わらない**
- **`SUPP-001` §5.2 のパッケージ構成の増減** → **なし**（`internal/` 差分 0）
- **`SUPP-001` §5.8 の設定ファイルの決定事項** → **変わらない**
- **マイグレーション** → **新規作成なし**（連番の払い出しをしていない）

**★ただし §4-3 は `docs/design/testid-convention.md` の改訂を要する**（`:62` の原文が `:55` のバナーと矛盾）。
**⇒ `CHANGE-171` の追補として畳むか、新規 CHANGE を起票するかは設計卓の判断。番号は起票時に registry で採番。**

---

## §7 教訓（`retrospective-log` 行き・**親は読まなくてよい**）

> 本節の宛先は `docs/handover/retrospective-log.md` へのバッチ反映であり、**実施者は設計担当**である。

1. **「置いた」という報告は、置かれたことの証拠にならない。**
   **★成果物の設置を報告するときは、設置箇所を数えた `grep` の出力そのものを残す。**
   本件は 3 つの資料が揃って「5 本」「○」と書いていたが、実数は 4 本だった（§4-1）。
   **`check-artifact-integrity.sh` が生まれた原因とまったく同じ形の再発である。**

2. **★陳腐化した雛形の複製を数えるとき、雛形の全行が同じ語を持つとは限らない。**
   本件は `grep "47318\|5173"` で 6 か所が出たが、**旧ポートを持たず「製造環境はブラウザ未導入のため未実行」だけを持つ形が 2 か所**あり、
   その式では出なかった。**⇒ 雛形が複数行なら、行ごとに別の式で数える。**

3. **★検査を新設するとき、「登録が要るか」を先に実測する。**
   本件は `check-artifact-integrity.sh` への追加を求められたが、
   同スクリプトは `check-*.sh` の **glob で自動発見**しており編集不要だった（件数 14 → 15 で確認）。
   **⇒ 「追加してください」を額面どおり受けて手で登録すると、二重管理を作る。**

4. **★陽性対照の後始末は、`git` が使えない前提で設計する。**
   `git checkout` / `git restore` は `deny` である。**⇒ 事前に退避し、`sha256sum -c` と `git diff` の両方で戻りを確かめる。**

5. **★投入プロンプトが外部の節（`§3-1` / `§3-3`）を参照するなら、その節も一緒に投入する。**
   本件は 2 か所で参照先が届いておらず、`CLAUDE.md` §9 の一般原則で代替した（§2-2）。

---

*以上*
