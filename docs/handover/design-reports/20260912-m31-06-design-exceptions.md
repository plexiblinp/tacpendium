# 設計伝達レポート: M31-06（`setup_only` の技をコンボの入力面から外す経路・`P4M-005` のフェーズ4 分）

| 項目 | 内容 |
|------|------|
| 作業ID | M31-06 |
| 対象指示書 | `docs/instructions/M31-06-setup-only-input-exclusion.md` **v1.2.0** ／ チェックリスト `docs/instructions/reviews/M31-06-review-checklist.md` v1.0.0 |
| CHANGE | **未起票。★製造は番号を消費していない**（`D-293`）**。⇒ 請求は 4 件。たたき台は §6。★ボード実測では次の番号は `186`**（残り `186`〜`191`） |
| マイグレ | **0 本消費**（指示書 §2.3-3 / 完了条件 7）**。⇒ ボード §2.2 の「次に払い出す番号 ＝ `000112`」は動いていない** |
| 作成日 | 2026-09-12 |
| 実装コミット | `claude/keen-dirac-8hckcz`・着手基点 `aa0be70` → `6142935`（**5 コミット・push 済み**）。**実装差分**（`docs/` 除く）＝**11 ファイル / +679 / −63** ／ docs 3 ファイル / +629 / −0。**★`E-225` の観点＝この手番で新規作成したファイルは完了報告とレビュー報告の 2 本だけであり、どちらも `+` のみ。実装側に新規ファイルは 1 つも無く、deletions 63 行はすべて既存ファイルの編集で説明がつく** |
| 源泉 | 完了報告 `docs/progress/M31-06-completion-report.md` ／ レビュー `docs/progress/m31-06-review.md`（**高 4 / 中 3 / 低 4・10 件採用 / 1 件不採用・★「高」の不採用 0 件・往復 0 回**）／ **開発者の判断 2 件**（2026-09-12。§3-1）／ 実装と同一セッションで生成 |
| 宛先 | 設計卓（親チャット） |

**本レポートは ①独自確定仕様 ②契約違反の独自判断 ③製造判断 ④残課題 に絞る。指示書どおりの部分は割愛する。**

> **★★★最重要は §2-1 と §1-1 である。⇒ どちらも「指示書・設計書の明文が実装事実と食い違っていた」型であり、親の裁定が要る。**
>
> **★★API 経路の新設・変更は無い**（`DES-002` §4.2 の経路表に触れていない。**Go 差分は `internal/model/move.go` のコメント 1 か所だけである**）**。⇒ §1 の逐語埋め込み規則は本サブでは発動しない。**
>
> **★★★本サブは「データが 0 件のうちに経路だけ作る」ものである。⇒ 画面の見え方は 1 か所も変わっていない。** 実 DB の `moves.setup_only = 1` は **0 件のまま**である。**★したがって「入れた」は「効く」の証拠にならない。効き目の証拠は陽性対照 22 本と破壊確認 5 通りである**（§2-1）。
>
> **★実機確認は未了である**（§4-7）**。⇒ 手順は完了報告 §7.2 に残した。フェーズ5 では必須になる。**

---

## §1 製造が独自に確定した実装仕様（DES 反映が要るもの）

### §1-1 ★★★「入力面の除外」の適用点が `unclassified` 分岐から**母集団**へ移った（`DES-005` §6 の 2 つの表が失効する）

**着手時点の実測** —— `isInputExcluded()` を通っていたのは **2 か所だけ**であった。

| 面 | 通るか |
|---|---|
| 全技一覧プルダウン | 通る（`useControllerInputOmission` の `selectableMoves`） |
| 未分類タブ | 通る（`surfaceBuckets` の `unclassified` 分岐） |
| 特殊技 ／ SA ／ ターゲットコンボ ／ 共通技 ／ キャラ固有状態 の 5 タブ | **★通らない**（各バケットは同関数を呼んでいない） |
| 通常技タブ | **★通らない。★そもそもバケットを持たない**——`HitBoxLayout` が押下のたびに `resolveDirectionalInput` で解決する |
| 必殺技タブ | **★通らない。★同じくバケットを持たない**——`SpecialMovePanel` が `deriveSpecialFamilies(moves)` を呼ぶ |

**⇒ `surfaceBuckets` の先頭で 1 回だけ母集団を絞る形へ引き上げた**（`web/src/features/combo/moveSurfacing.ts` の `surfaceBuckets`）。

```ts
const inputMoves = moves.filter((m) => !isInputExcluded(m, context));
```

**★判定は依然 `isInputExcluded` の 1 本しかない。⇒ 2 本目を作っていない**（`D-810`）。**★「押し下げ」ではなく「引き上げ」である**——旧注記が勧めていた「各バケットの push 直前へ足す」は採らなかった。**押し下げると判定が 7 か所へ散り、しかもバケットを持たない 2 タブには依然として効かない。**

#### ⇒ `DES-005` §6 の**2 つの表**が as-built と食い違う

| 箇所 | 現行の記述 | as-built |
|---|---|---|
| **`:2009` / `:2013`** | 「実体＝… ⇒ **2 面へ効く**」／ 表の 1 行目「仮想コントローラのタブ（★未分類を含む）＝ `surfaceBuckets` の **`unclassified` 分岐**」 | **★適用点は `unclassified` 分岐ではなく母集団である。⇒ 8 タブすべてに効く。★2 行目**（プルダウン ＝ `selectableMoves`）**は真のままである** |
| **`:2155-2163`** | 8 タブの「母集団（述語の逐語）」表。タブ 1 / 2 / 4 / 5 に **「（不変）」** と書かれ、タブ 6 は **「`isControllerSurfaced()` が false を返す行そのもの」** | **★どのタブの母集団も「その述語 ∧ 非除外（面）」になった。⇒ 「そのもの」「不変」はいずれも不正確である** |
| **`:2020`** | 「**現在の除外は 1 件だけである**（`drive_reversal`）。⇒ `setup_only` を同じ機構へ足すのは `M31-06` の射程である」 | **★足した。⇒ 同文は役目を終えた。★機構は 1 本のままである** |

**★★`CHANGE-176` §2.5 の等式そのものは崩れていない。**

```
未分類 ＝ isControllerSurfaced の裏返し ∧ 非除外
```

**⇒ 変わったのは `∧ 非除外` の位置**（`unclassified` 分岐 → 母集団）**と、その `非除外` が面を持つようになったこと**である。

> **★★★なぜ `M31-04` で表面化しなかったか。** `drive_reversal` は `category='system'` で**どのタブにも載らない code だった**。**⇒ `setup_only` は任意の category に立ちうる。確実に踏む。** **★旧注記自身が「載る形の code を足すと、未分類とプルダウンからは消えるのにタブには出続ける。型検査もテストも緑のままである」と予告していた状態である。**

### §1-2 ★★除外の判定が「面」を持つようになった（`DES-005` §5.9 の一文が `setup_only` について偽になる）

**問題**——セットプレイのレシピ入力は、コンボ登録と **同じ `VirtualController`・同じ `useControllerInputOmission`** を共有している。**⇒ 部品の中で落とすとセットプレイからも消える。**

```ts
export type RecipeInputContext = "combo" | "setup";

export function isInputExcluded(move: Move, context: RecipeInputContext): boolean {
  if (INPUT_EXCLUDED_MOVE_CODES.has(move.code)) return true;   // 理由1: 両面で外す
  return context === "combo" && move.setupOnly;                // 理由2: コンボ側だけ外す
}
```

| 理由 | 由来 | 面での効き方 |
|---|---|---|
| `INPUT_EXCLUDED_MOVE_CODES` に載る code | 静的なフロント定数 | **両面**で外れる |
| `move.setupOnly` | データ駆動のフラグ | **コンボ側だけ**外れる |

**⇒ `DES-005` §5.9 の表示項目 4「レシピ入力領域（コンボ登録と同じ仮想コントローラ使用）」は、`setup_only` について偽になった。★同じ部品だが、同じ母集団ではない。**

> **★案 (ii)**〔セットプレイ側の呼び出しでだけ除外を外す〕**を落とした理由は 1 点である**——**静的除外まで面で分かれ、`drive_reversal` がセットプレイ側へ漏れる**（チェックリスト C-3 が警告していた形）。

### §1-3 ★`setup_only` の as-built 全数（`DES-003` §3.3 を「予約列」から実際の役割へ）

| 面 | 扱い |
|---|---|
| コンボ登録の仮想コントローラ **8 タブ**（未分類を含む） | **外す** |
| コンボ登録の全技一覧プルダウン（**トグル ON / OFF どちらでも**） | **外す** |
| セットプレイのレシピ入力 | **★出す。⇒ ここが唯一の入り口である** |
| コンボ一覧 ／ 詳細 ／ 比較 ／ 既存レシピの表示・再編集 | **★出す。1 か所も触っていない** |
| 物理入力（パッド ／ キーボード） | **★通す。⇒ 塞ぎ忘れではなく対象外と決めた結果である**（指示書 §2.2-6 ／ チェックリスト C-5） |
| 引っ越し取込 | **★通す。触っていない**（`DES-005` §6 `:2018` の開発者逐語どおり） |
| 保存 | **★禁止しない**（指示書 §2.2-6） |
| 付与の口 | **★無い。`UpdateMoveRequest` に項目なし ／ CSV に列なし。⇒ フェーズ5** |

**★`internal/model/move.go` の「予約列」コメントは本サブで是正した**（§2-2）。**★判定はフロント 1 か所であり、サーバは値を配るだけで出し分けの分岐を持たない。**

### §1-4 ★`SurfaceBuckets` に `inputMoves` を足した

**通常技タブと必殺技タブは*バケットを持たない***（前者は押下時に解決、後者はファミリーへ畳む）。**⇒ その 2 タブが他の 6 面と同じ母集団を引くための口が要った。**

```ts
export interface SurfaceBuckets {
  inputMoves: Move[];   // ★入力面へ出す母集団（isInputExcluded で落とした後）
  unique / superArt / targetCombo / common / characterState / unclassified: Move[];
}
```

**★呼び手が自前で filter し直さないこと。絞り込みの適用点は `surfaceBuckets` の 1 か所である。**

### §1-5 ★`omittedCount` は「常時除外を数えない」性質を保つため走査対象を変えた

**`M31-04` が `CHANGE-176` §2.5 に明記した性質＝「常時除外はトグルの効き目ではないので `omittedCount` には数えない」。⇒ 数え方の方針は変えていない。**

- 旧実装は `moves` を走査していたが、**`drive_reversal` が非掲載だったため偶然 0 件しか差が出ていなかった**。
- **★`setup_only` は掲載される形**（例 `category='special'`）**に立ちうる。⇒ 旧実装のままだと数え過ぎる。**
- **★さらにレビュー 高-2 を受けて母集団も揃えた**（§2-3 の注記ではなく §4-2 の副作用に関わる。詳細は下記）。

**★★走査対象だけでなく判定材料（母集団）も `selectableMoves` へ揃えた。⇒ 理由は `surfaceBuckets` と同じ材料を使うためである。** 揃っていないと、**段階2 の解決表が `setup_only` の技を指しているとき**、`surfaceBuckets` 側は段階1 へフォールバックして別の通常技を「押せる」状態にする一方、フック側は `entries` が当たるため同じ技を「押せない」と判定し、**`CHANGE-176` §2.5 の等式が静かに崩れる**。**★データが 0 件のうちは再現しない。**

---

## §2 契約・設計に反する独自判断（★親の裁定が要る）

### §2-1 ★★★指示書 §2.2-1 の明文に反して構造を変えた

**指示書の逐語**——「`isInputExcluded()` の判定に `setup_only` を足す（★新しい関数も新しい定数も作らない＝§0.4）。**⇒ これだけで仮想コントローラ 8 タブ**（未分類を含む）**と全技一覧プルダウンの両方に効くはずである。★実物で確かめること**」。

**⇒ 確かめた。効かない**（§1-1 の実測表）。**⇒ `surfaceBuckets` の構造を変えた。**

| 何に反したか | 指示書 §2.2-1 の「これだけで効くはず」 ／ `moveSurfacing.ts` の旧注記「効かせるなら各バケットの push 直前へ足す」 |
|---|---|
| なぜそう判断したか | **押し下げでは判定が 7 か所へ散り、しかもバケットを持たない通常技タブ・必殺技タブには依然として効かない。⇒ 完了条件 2「コンボの仮想コントローラ（未分類を含む）と全技一覧の 2 面から `setup_only = 1` が外れる」を満たせない** |
| 実装がどうなっているか | `web/src/features/combo/moveSurfacing.ts` の `surfaceBuckets` 先頭で 1 回絞る ＋ `SurfaceBuckets.inputMoves` を `VirtualController` が 2 タブへ配る |

**★★★テスト側から独立に裏取りしてある**——**破壊確認 (2)＝`surfaceBuckets` を旧構造**（母集団を絞らず `unclassified` 分岐の中だけで落とす＝**指示書 §2.2-1 の想定そのもの**）**へ戻すと 6 本が赤になる。**

**★指示書 §0.4 の「新設せず拡張する」という核心は守っている**（新しい関数も新しい定数も 0 件・`INPUT_EXCLUDED_MOVE_CODES` は 1 文字も変えていない）。**⇒ 反したのは「適用点をどこに置くか」だけである。**

**⇒ 親の裁定事項**: 受理して `DES-005` §6 と指示書の記述を as-built へ寄せるか、差し戻すか。**★`M18-03b` の「実装が正しく指示書が不足」と同型と見ている。**

### §2-2 ★★チェックリスト A-3 から意図的に逸脱した（Go 差分 1 か所）

| 何に反したか | チェックリスト **A-3**「`internal/repository/move/divergence_test.go` 以外の Go 差分が無いこと」 |
|---|---|
| なぜそう判断したか | **`internal/model/move.go` の `SetupOnly` に付いた「予約列」コメントが、本サブの投入で失効する**（列が実際に使われ始めるため）。**★レビュー優先度較正が「失効した記述がコード上に残っている」を『高』と定めており、`go test` / `go vet` / `tsc` のどれも検出しない型である**（`D-250` と同型）。**★followup `setup-only-flag-has-no-data` も「コメントの是正が要る」と記録済みであった** |
| 実装がどうなっているか | `internal/model/move.go`（**+15 / −1。コメントのみ。フィールド定義・タグ・型は不変**）。**★`divergence_test.go` には差分 0 である** |

**★★開発者裁定を得ている**（2026-09-12。§3-1 の 2）**が、その事実は完了報告 §11 にしか無かった**（レビュー 中-3）。**⇒ 親が読む面へ出すのが本項の目的である。**

### §2-3 ★`surfaceBuckets` の第 4 引数にだけ既定値を残し、型の一貫性を意図的に崩した

| 何に反したか | 本サブ自身が §1-2 で置いた方針「**面を宣言しない呼び出しを型検査で止める**」 |
|---|---|
| なぜそう判断したか | **共有テスト `web/src/features/combo/moveSurfacing.roster.test.ts` が `surfaceBuckets` を 6 か所で呼ぶ**（`:146` `:154` `:219` `:393` `:399` `:440`）**。必須にするとその 6 か所を書き換えることになり、`M30-05` と同じファイルを奪い合う**（指示書 §0.7 ／ チェックリスト A-2） |
| 実装がどうなっているか | `isInputExcluded` ／ `useControllerInputOmission` ／ `VirtualController` の `context` は**必須**。**`surfaceBuckets` の第 4 引数だけ既定 `"combo"`**。**★本番の呼び元は `VirtualController` 1 つだけで、そこは必ず明示して渡す。⇒ 既定に頼る経路は本番に存在しない** |

**★暫定である。⇒ `M30-05` のマージ後に外す手番が要る**（§4-1）。**★放置すると「既定へ落ちた面で `setup_only` が静かに消える」入口が残る**——**本サブ最大の事故の形そのものである。**

---

## §3 製造の判断

### §3-1 開発者へ確認して確定した点

| # | 事項 | 回答（2026-09-12） |
|---|---|---|
| 1 | **指示書 §7-1**。逐語の「おとび」を「および」と読み、外す面は 2 つ〔コンボ登録の仮想コントローラ ／ コンボ登録の全技一覧プルダウン〕でよいか | **★2 面でよい**（`D-807` / 計測点 `M-153` の是正どおり。3 つ目の面は無い） |
| 2 | `internal/model/move.go` の「予約列」コメント（失効する）を本サブで直すか、チェックリスト A-3 を守って改善レーンへ回すか | **★本サブで直す。⇒ §2-2** |

### §3-2 推測で進めた点

**なし。⇒ 推測で進めた箇所は 0 件である。**

### §3-3 ★レビュー推奨の「置き場」だけ変えた 1 件（★内容は採用している）

**レビューは 高-1 と 中-2 について「`followup-backlog.md` §J へ 1 行残すこと」を推奨した。⇒ 内容は採用したが、置き場を本レポート §4 にした。**

1. **§J は停止時記録の面であり、改善候補の置き場ではない。** **★`M30-04` で同じ提案が出て、製造がレポート §4 経由へ回した先例が在る**（`followup-backlog.md` の `import-order-baseline-message-contradicts-header` 行の出所欄に経緯が書かれている）。
2. **本サブに停止時記録は 1 件も無い**（未解消 0 件 ／ 往復 0 回）。**⇒ §J へ書くべきものが無い。**
3. **★`D-828`**（2026-09-12）**——設計卓が `followup-backlog.md` を大きく編集中に製造がブランチ内で §J を編集し、開発者が差し戻して設計卓へ回した事例である。⇒ 同じ日に同じことをしない。**

**★★したがって本サブは `docs/handover/followup-backlog.md` を 1 文字も編集していない**（`D-382`）。

---

## §4 設計担当が未把握の残課題・申し送り

### §4-1 `surface-buckets-context-default-is-provisional`（**新規登録**）

- **何が起きるか**: `surfaceBuckets` の第 4 引数 `context` の既定値 `"combo"` に新しい呼び元が落ちると、**その面から `setup_only` が静かに消える**。型検査もテストも緑のまま。
- **根拠**: `web/src/features/combo/moveSurfacing.ts` の `surfaceBuckets` シグネチャ ／ 依存している呼び元は `moveSurfacing.roster.test.ts` の 6 か所（`:146` `:154` `:219` `:393` `:399` `:440`）。
- **割付の候補と理由**: **改善レーン**（`M30-05` のマージ後）。**★同ファイルを奪い合わないための暫定なので、衝突要因が消えた時点で外せる。**
- **新規登録**。

### §4-2 `setup-only-exclusion-repoints-stage2-resolution`（**新規登録**）

- **何が起きるか**: **除外はボタンを無効化せず、同じボタンの解決先を別の技へ差し替える。** 段階2 の解決表 `entries` が `setup_only` の技を指していると、コンボ側では母集団に居ないため `resolveDirectionalInput` が段階1 へフォールバックし、**別の通常技が解決される**。
- **根拠**: `web/src/features/combo/inputResolutionStage2.ts` の `resolveDirectionalInput`（`entries` 参照 → 見つからなければ段階1）／ 再現テスト＝`web/src/features/combo/hooks/useControllerInputOmission.test.ts` の「★前提の確認: この解決表では両面で『押せる技』が食い違う」。**★中段ゾーンは 4 / 5 / 6 の 3 方向が `neutral` へ縮約されるため、3 つとも差し替えないと差が出ない**（実測して直した）。
- **割付の候補と理由**: **フェーズ5**（付与口の設計）。**★フラグを立てた瞬間に入力の意味が変わる面であり、「どの `move_code` に立てるか」を決める側が知っていなければならない。**
- **新規登録**。

### §4-3 `setup-only-flag-granularity-splits-special-families`（**新規登録**）

- **何が起きるか**: **除外の単位は「行」であって「ファミリー」ではない。** `deriveSpecialFamilies` の段 2 は `hasFamily`（絞った母集団に基底ファミリーが在るか）で変種を畳むため、**親の行だけに `setup_only` が立ち変種の行に立っていない組では、変種が独立ファミリーとして立つ**。
- **根拠**: `web/src/features/combo/inputResolution.ts` の `deriveSpecialFamilies` 段 2（`splitSpecialVariant(family, hasFamily)`）。**★本サブは同ファイルに差分 0 である**（チェックリスト A-1）**——引数に渡す母集団が変わったことによる帰結である。**
- **割付の候補と理由**: **フェーズ5**（付与口の設計）。**★★`M30-02` が解消した「素は押せるが OD が押せない」7 組と同型の非対称が、フラグ付与で作られうる。⇒ 「どの粒度で立てるか」の設計制約である。**
- **新規登録**。

### §4-4 `input-excluded-codes-vs-setup-only-overlap`（**既存行の更新 → 解消**）

- **更新後の状態**: **★解消。⇒ 畳んでよい。** 同行は担当欄が `M31-06`・状態「**対処済み・着地待ち**」であり、**本サブが着地した**。
- **根拠**: 適用点は `isInputExcluded` の 1 本のまま、理由が 2 つに分かれた形で着地した（§1-2）。**★同行が予告していた「軸が 2 つ並ぶことは避けられない。揃えるのは入口だけでよい」のとおりになっている。**

### §4-5 `setup-only-flag-has-no-data`（**既存行の更新 → 半分解消**）

- **更新後の状態**: **★半分解消。**
  - **解消した分**: 「`internal/model/move.go` の『予約列』コメントのほうが失効している。⇒ コメントの是正が要る」——**本サブで是正した**（§2-2）。
  - **残る分**: 同行が挙げる 3 つの未決のうち **(1) どの `move_code` に立てるか (3) 誰がどの経路で立てるか の 2 つ**。**★(2)「非表示の範囲はどこまでか」は本サブで決着した**（§1-3 の面の表）。
- **割付**: 残りは **フェーズ5**（指示書 §3-1 / §3-2）。

### §4-6 `import-order-baseline-101-vs-99`（**既存行の更新**）

- **更新後の状態**: **★本サブで 4 サブ連続の申し送りになった**（`M35-02` ／ `M31-04` ／ `M35-03` ／ 本サブ）。実測 99・ベースライン 101。
- **根拠**: `bash scripts/check-import-order.sh` が「ベースラインより 2 ファイル少ない → BASELINE を 99 へ下げること」と出すが、**`D-388` がレーンごとに下げることを禁じている。⇒ 下げていない。**
- **割付の候補と理由**: **改善レーン**。**★どのサブの由来でもないため、誰の手番でもないまま床だけが高い。** 関連＝`import-order-baseline-message-contradicts-header`（同じ矛盾のスクリプト側の是正）。

### §4-7 ★★実機確認が未了である（**申し送り。★フェーズ5 では必須になる**）

- **何が起きているか**: **`setup_only` には付与口が 1 つも無く**、実 DB は **0 件**である。**⇒ 通常操作では画面で何も観測できない。**
- **本サブで固めた範囲**: 陽性対照 **22 本**（コンボ側で消える ／ セットプレイ側に残る を対にして測る）＋ **破壊確認 5 通り**（いずれも当該テストが赤になることを実測）。**⇒ 機械的には固めてある。**
- **レビューの制約事項も同旨**: 「`setup_only = 1` の実データは 0 件であるため、本レビューの 高-2 / 中-2 はいずれも**コード上の推論**であり、画面での再現は行っていない。フェーズ5 でフラグを立てる手番では、実データでの再確認が要る」。
- **★手順は完了報告 §7.2 に残した**（使い捨て DB＝`scripts/dev-throwaway-db.sh` を使い、実 dev DB には触れない形）。**⇒ フェーズ5 の起票時に同節を引くこと。**
- **★★【2026-09-12 追補】開発者の問い合わせを受けて「配線」まで自動テストで塞いだ**（完了報告 §4.5）**。⇒ 着手時点では「どの配列をどのタブへ渡しているか」が測られておらず、`<HitBoxLayout>` / `<SpecialMovePanel>` を生の `moves` へ戻しても、`SetupRecipeEditor` の `context="setup"` を `"combo"` へ倒しても、全テストが緑のまま通る状態だった**（実データが 0 件だと `inputMoves === moves` になるため）**。★破壊確認 3 通りで赤になることを実測して塞いだ。⇒ 完了条件 2 / 3 がタブの面で満たされた。**
- **★★それでも実機確認は未了のままである。⇒ 本サブの検査はすべて合成 fixture であり、実 DB の実データで見た者は居ない。**

### §4-8 ★`M31` の残りは本サブ 1 本だった（**申し送り**）

- `M31-overview` v1.12.0 の逐語＝「**`M31` の残りは `M31-06` の 1 本である**」。**⇒ 本サブの受理をもって `M31` はクローズ判定に入る。★判定は親の手番である。**

---

## §5 参考（触れていない＝不変の証跡）

- `web/src/features/combo/inputResolution.ts`: 差分 0（チェックリスト A-1。`M30-05` と衝突しない）
- `web/src/features/combo/moveSurfacing.roster.test.ts`: 差分 0（A-2。**★指示書 §0.7 の「ほぼ確実に触る」という見込みは外れた**）
- `internal/repository/move/divergence_test.go`: 差分 0（A-3）
- `migrations/`: 差分 0（完了条件 7。**★実 DB の `setup_only = 1` は 0 件のまま**）
- `INPUT_EXCLUDED_MOVE_CODES` の中身: 不変（`drive_reversal` 1 件のまま）
- `COMMON_MOVE_CODES`: 13 不変（`toHaveLength(13)` が固定）
- `isControllerSurfaced` の式: **不変**（母集団を絞る形を採ったため本体を触っていない）
- `usePhysicalRecipeInput` / `GamepadRecipeReadout`: 差分 0（C-5。物理入力は通す）
- `IntakeHelperPage` / `internal/service/intake`: 差分 0（C-6）
- `RecipeText` / `ComboTableRow` / `ComboDetailRecipe` / `CompareTable`: 差分 0（C-4）
- `internal/service/setplay/`: 差分 0（filler / target の提案候補を絞っていない）
- `go.mod` / `go.sum` / `web/package.json`: 差分 0（新規依存 0 件・ピン留め 0 件）
- `web/CLAUDE.md` §1 台帳: 新規キー 0 件（`check-browser-storage-keys.sh` 緑）
- `docs/handover/followup-backlog.md`: **差分 0**（`D-382` / `D-828`。§3-3）
- 常設検査 8 本すべて違反なし（1 本目に `check-artifact-integrity.sh`）

---

## §6 CHANGE 起票のたたき台（設計担当向けチェックリスト）

> **★番号は起票時に `docs/handover/change-number-registry.md` §1 で採番すること。⇒ 製造は 1 本も消費していない**（`D-293`）**。★ボード §2.1 の実測では次は `186`**（残り `186`〜`191` の 6 本）**。**

**★マイグレは 0 本消費である。⇒ ボード §2.2 の「次に払い出す番号 ＝ `000112`」は動いていない。**

| # | 反映先 | 内容 | 由来 |
|---|---|---|---|
| **★★★1** | **`DES-005` §6**（`:2009` / `:2013` ／ **`:2155-2163`** ／ `:2020`） | **★適用点が `unclassified` 分岐から母集団へ移った。⇒ 8 タブすべてに効く。★8 タブの母集団表の「（不変）」「そのもの」はいずれも「その述語 ∧ 非除外（面）」へ。★`:2020` の「現在の除外は 1 件だけ」は役目を終えた** | §1-1 |
| **★★2** | **`DES-005` §5.9**（表示項目 4） | **★「レシピ入力領域（コンボ登録と同じ仮想コントローラ使用）」が `setup_only` について偽になった。⇒ 同じ部品だが同じ母集団ではない。★§5.7 側にも同じ旨の追記が要る** | §1-2 |
| **★3** | **`DES-003` §3.3**（`moves.setup_only`） | **★「予約列」ではなくなった。⇒ as-built へ**（外す面 2 ／ 表示面では出す ／ 物理入力・取込は対象外 ／ 保存は禁止しない ／ 付与口は無い） | §1-3 |
| **4** | **`CHANGE-176` §2.5 の追随** | **★等式そのものは不変。⇒ `∧ 非除外` の位置が移り、`非除外` が面を持つようになったことの追補** | §1-1 |
| — | **`REQ-001`** | **★請求しない**（要件面の変更は無い） | — |

---

## §7 教訓（retrospective 行き・**親は読まなくてよい**）

> **本節の宛先は `retrospective-log` へのバッチ反映であり、実施者は設計担当**（反映担当は 2026-08-11 に廃止）**。**

### §7-1 ★★★指示書が「これだけで効くはず」と書いていたら、効くことを実測してから乗る

**指示書 §2.2-1 は「★実物で確かめること」まで書いていた。⇒ 書いてあったから確かめた、のではない。** **★確かめずに乗っても全部緑で通る状況だった**（データ 0 件）**。⇒ 指示書どおりに実装して緑を報告するほうが楽であり、誰も気づかない。** **★「〜のはずである」は仮説の表明であって、検査の結果ではない。⇒ 仮説と読める語**〔はず ／ と思われる ／ 想定される〕**が指示書に在ったら、そこが実測の起点である。**

### §7-2 ★★★データが 0 件の「経路だけ先に作る」サブは、破壊確認が唯一の証拠である

**⇒ 入れても入れなくてもテストは同じ結果になる。⇒ 「入れた」を「効く」の証拠と読ませない。** **★本サブは 5 通り壊して赤を見た。★とくに「指示書が想定していた実装へ戻すと赤になる」形は、指示書の前提の誤りをテスト側から独立に裏取りする。⇒ 自分の主張の根拠を、自分の文章の外へ出せる。**

### §7-3 ★★★型の床は「渡し忘れ」しか止めない。「倒す」は止めない

**⇒ 必須引数にしても、`"setup"` を `"combo"` へ*書き換えた*場合は型検査を通る。** **★本サブは着手時点でそれが全検査緑だった**（レビュー 高-2 派生で床を足した）**。⇒ 「型で守った」と書くときは、守れるのが渡し忘れだけであることを併記する。★値の取り違えを守るのはテストである。**

### §7-4 ★★同じ述語を 2 面で共有するとき、揃えるのは走査対象ではなく判定材料である

**⇒ `isControllerSurfaced` を、片方は絞った母集団で、片方は生の母集団で呼んでいた。** **★走査対象**（どの move について聞くか）**を揃えても、母集団**（何を材料に答えるか）**が違えば答えが違う。⇒ 「規則を 2 か所へ書かない」は、引数まで含めて初めて成立する。**

### §7-5 ★★自分が書いた注記が自分の実装と矛盾していないかを、同じ手番で読み返す

**⇒ 「既定値を置いていない。どちらの既定も事故を作る」と書いた型 doc の 200 行下に、既定値を持つ関数を自分で置いていた**（レビュー 高-1）**。★注記は実装より先に書くため、実装が動いた時点で置き去りになる。⇒ 手番の最後に、その手番で書いた注記だけをもう一度読む。**

---

*以上、M31-06 設計伝達レポート。*
