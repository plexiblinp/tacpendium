# 設計伝達レポート: M30-02（仮想コントローラの技の選び方と見た目）

| 項目 | 内容 |
|------|------|
| 作業ID | M30-02 |
| 対象指示書 | `docs/instructions/M30-02-controller-selection-and-labels.md` **v1.0.0** ／ チェックリスト `docs/instructions/reviews/M30-02-review-checklist.md` **v1.0.0** |
| CHANGE | **消費 0 本。★製造は番号を消費していない**（`D-293`）**。⇒ 起票のたたき台は §6** |
| マイグレ | **★★消費 1 本＝`000107`**（`data_correct_terry_round_wave_code`）。**着手時点の宣言は「0 本」であった。⇒ §2-1 で裁定を請う。★連番は着手時に `ls migrations/` で実査して払い出した**（最大は `000106` だった） |
| 作成日 | 2026-09-09 |
| 実装コミット | `claude/m30-02-implementation-plan-2wa8l9`・`4c0dcb2..d64b904`（**14 コミット・push 済み**。本レポート自身を除く）。差分＝**37 ファイル・+2633 / −227**（うち `web/` は 21 ファイル・+1544 / −215） |
| 源泉 | 完了報告 `docs/progress/M30-02-completion-report.md` **v2.0.0** ／ レビュー `docs/progress/m30-02-review.md`（**高 3 / 中 5 / 低 5 ＝ 全 13 件。高の不採用 0 件・再レビュー往復 0 回**）／ **開発者との確定 9 件**（2026-09-09）／ **開発者の実機確認 9 項目**（2026-09-09）／ 実装直後の同一セッションで生成 |
| 宛先 | 設計卓（親チャット） |

**本レポートは ①独自確定仕様 ②契約違反の独自判断 ③製造判断 ④残課題 に絞る。指示書どおりの部分は割愛する。**

**★★最重要は §2-1**（**マイグレを 1 本消費した — ヘッダ宣言「0 本」に反する。開発者が明示指示したが、`M30-overview` の見込みも動く**）**と §2-2**（**`CHANGE-166` が明文化した規則を A1 186 件について失効させた — 指示書 §4-1「『何を出すか』の規則を変えること」に該当する**）**である。★どちらも開発者判断に基づく実施であり、受理して `DES` と `overview` を更新するか、却下して差し戻すかの裁定が要る。**

> **★設計卓は `docs/progress/` を読まない**（2026-08-11 開発者裁定①）**。⇒ 本レポートは参照だけを書かず、判断に要る中身をここへ埋めてある。**

> **★`DES-002` §4.2 の経路表に載る API の新設・変更は 0 件である。** 本サブは `internal/api/` `internal/service/` `cmd/` を **1 行も触っていない**（`internal/` の差分は `infra/migration/` のテスト 1 本のみ）。⇒ **本レポートに API 契約の逐語は無い。**

---

## 0. 本サブで実際に起きたこと（4 行で）

1. **必殺技ファミリー UI に 2 つの軸を足した** —— **「強度なし」**（`P4M-016`。A1 186 件が新たに載る）と **「変種」**（`P4M-018`。ホールド／最大ホールド／ジャストを基底へ畳む）。**⇒ どちらも `CHANGE-166` / `DES-004` §2.1 の規則の変更である**（§1-1 / §1-2）。
2. **`SD-020` の調査で「OD 非活性で強度組合せだけ活性」が実在した** —— **修飾フラグ側は、そのステップが OD 技かを一切見ていなかった。⇒ 開発者判断で射程を広げ、活性条件を新設した**（§1-3）。
3. **`P4M-016` (b)「テリーだけ強が押せる」の原因はデータであった** —— **開発者のインゲーム確認を得て `round_wave_heavy` → `round_wave` を是正した**（マイグレ `000107`。§2-1）。
4. **★本サブは 1 つ非対称を作った** —— **A1 の素が載る一方その OD は A2 のままで、「素は押せるが OD が押せない」形が 7 組できた**（§4-2）。**`M30-03` が閉じる。**

---

## 1. 製造が独自に確定した実装仕様（DES 反映が要るもの）

### 1-1 必殺技ファミリー UI に「強度なし」ができた（`P4M-016`・開発者判断 案 A）

**`web/src/features/combo/inputResolution.ts` の `parseSpecialCode` を 3 段にした。**

| 段 | 条件 | 戻り値 | 母集団（`moves` テーブル実測・`category='special'` 1024 行） |
|---|---|---|---|
| 1 | 末尾が `_od` / `_light` / `_medium` / `_heavy` | `{ family, strength }`（従来どおり） | 733 |
| 2 | **強度語が `code` の途中に在る**（`_` で割ったトークンに含む） | **`null`（従来どおり）** | **105（A2）** |
| 3 | 強度語がどこにも無い | **`{ family: code, strength: "none" }`（新設）** | **186（A1）** |

- **★強度語の集合は既存の接尾辞表から導いている。書き起こしていない**（`SPECIAL_STRENGTH_TOKENS`）。
- UI: 強度行の先頭へ **「強度なし通常版」**（文言は開発者確定）。`recipe-special-strength-none`。**★弱中強の行は、そのいずれかが在るときだけ描く**（強度なししか持たないファミリーで常に押せない 3 つを並べない）。
- **★代表名の優先順に `"none"` を `"light"` の次へ置いた** —— 強度を持たない技の `name_ja` は強度語を含まないため最も素直な代表名になる。

**⇒ `DES-004` §2.1 ／ `DES-005` §5.7 へ、「強度接尾辞を持たない `move_code` はファミリー UI に載らない」（`CHANGE-166`）が **A1 について失効した**ことを明文化してほしい。★A2 については不変である**（`M30-03` の射程）。

**波及の実測**（意図した変化。テストは新しい実測へ書き直した）:

| 観測 | 着手時点 | 完了時点 |
|---|---:|---:|
| 未分類（CSV 基準・固有状態タブ無し） | 330 | **144** |
| その `special` 内訳 | 291 | **105**（＝A2 のみ） |
| 未分類（**実 DB**）jamie / mai / blanka / manon | 11 / 3 / 12 / 0 | **2 / 1 / 2 / 0** |
| **`_od` だけが押せる形** | **95** | **0** |

### 1-2 必殺技ファミリーに「変種」の軸ができた（`P4M-018`・開発者判断 案 B）

**`SpecialVariant = "plain" | "holding" | "max_holding" | "perfect"`。** ファミリー行と強度行の**間**に変種行を挟む（`recipe-special-variant-{plain|holding|max-holding|perfect}`）。

| 規則 | 内容 |
|---|---|
| 畳む対象 | `<基底>_holding` / `_max_holding` / `_perfect` |
| **照合順序** | **`_max_holding` → `_holding` → `_perfect`。★`_max_holding` は `_holding` でも終わる。順序を誤ると「最大ホールド」が「ホールド」として畳まれ、静かに 1 変種消える** |
| 畳む条件 | **剥がした基底が同じキャラの必殺技ファミリーとして実在するときだけ。★実在しなければ独立ファミリーのまま残す**（seed 実測では孤児 0 件だが、基底が消えた変種を到達不能にしない） |
| 射程 | **`category='special'` のみ**（開発者の逐語＝「必殺技以外は対象外。通常技や特殊技など」）。通常技・特殊技の `_holding` 17 件には 1 行も触れていない |
| 代表名 | **plain から採る**（変種の `name_ja` は「【ホールド】弱グラディウス」の形で、装飾も強度語も残るため） |
| 変種行の表示 | 変種が 1 つのファミリーでは行ごと出さない |
| ファミリー切替 | **変種は通常へ戻す**（押した覚えのない変種で確定させない） |

**実測**: 変種マーカーで終わるファミリーは **着手時点の規則で 19 本 ／ `P4M-016` 適用後は 22 本**（基底が A1 の 3 本が合流して初めて畳める）。**孤児 0 件。⇒ 畳んだ結果、変種を持つファミリーは 18 本。**

**★★確定するステップは着手時点とまったく同じである** —— ホールド版は従来どおり自分の `move_id` で入り、修飾フラグは足していない。**⇒ レシピ表示・保存・比較・エクスポートは 1 行も変わらない。**

**★契約の削除が 1 件ある** —— **`recipe-special-family-{holding 系 19 本}` が消えた**（変種として畳まれたため）。**名指ししているテストは 0 件**（E2E は `recipe-special-family-hadoken` のみ）。

**⇒ `DES-004` §2.1 ／ `DES-005` §5.7 へ、ファミリーが「基底 × 変種 × 強度」の 3 軸になったことを明文化してほしい。**

### 1-3 OD 強度組合せ（`od_lm` / `od_mh` / `od_lh`）に活性条件ができた（`SD-020`・開発者判断で射程拡大）

**★★指示書 §2.5 の調査結果＝「OD 非活性で強度組合せだけ活性」は片面で常に実在していた。**

| 面 | 着手時点 | 完了時点 |
|---|---|---|
| 仮想コントローラ側（`SpecialMovePanel`） | **実在しない**（`{odId != null && …}` の内側で、構造的に起こらない） | 既定非表示・展開で 3 つとも出す |
| **修飾フラグ側**（`MODIFIER_FLAGS`） | **★★常に実在していた。** そのステップの move が OD かを**一切見ていない。しゃがみ弱 P にも「OD(弱中)」を付けられた** | **必殺技の OD でなければ節ごと出さない** |

**活性条件**＝`isOdVariantApplicable(move)`：`category === "special"` かつ **`code` を `_` で割ったトークンに `od` を含む**。

- **★末尾の `_od` だけを見てはいけない**（レビュー 高-2）。**`od` が `code` の途中に在る OD 必殺技が seed 全 31 キャラで 37 行実在する**（`mai/flame_od_kachousen` ／ `ryu/denjin_charge_od_hadoken` ／ `ingrid/od_sun_shot_light` 等）。**末尾だけで判定すると、着手時点にできた入力ができなくなる＝機能後退である。**
- **★実測で偽陽性・巻き添えはゼロ**: `od` トークンを持つ `special` で `name_ja` に "OD" を含まない行 **0** ／ `_od` 終わりで "OD" を含まない行 **0** ／ `special` 以外で `od` トークンを持つ行 **0**。
- **★★既存データの保護**: 既に値が入っているステップでは**非適用でも節を出し、自動展開し、外せるようにする。⇒ 抑止しているのは新規付与だけである。**
- **★抑止は確定させる関数の側にも要る**（レビュー 高-3）。画面の `disabled` は「既存を外せるようにする」ために `checked` を除外しており、**「OD 技で選ぶ → 通常技へ変える → 追加」で素通りした。⇒ `RecipeBuilder.handleAdd` で追加時にも濾す。**
- **★`OD_VARIANT_ORDER` の値の集合は変えていない**（指示書 §4-4）。変えたのは「いつ出すか」だけである。

**⇒ `DES-005` §5.7 ／ `SUPP-001` §3.3.1（`modifiers.flags`）へ、(a) 既定非表示 (b) 活性条件 (c) 既存値は消さない、の 3 点を明文化してほしい。**

### 1-4 通常技タブの配置と、タブ行の折り返し（`SM-149` ／ 実機確認 A①）

- **`SM-149`（開発者判断 案 X′）**: 左＝方向パッド（3×3） ／ 右＝攻撃ボタン（2×3）＋ **その下に空く 1 行ぶんへラッシュトグル**。**⇒ 縦が着手時点の約半分。★ボタンの大きさは 1 つも変えていない**（逐語「サイズ感はいい」）。狭い画面（`sm` 未満）は縦へ折り返す。**★DOM 上の順序が「方向 → ラッシュ → 攻撃」から「方向 → 攻撃 → ラッシュ」へ変わる**（Tab 順が変わる）。
- **`SM-080`（開発者判断 案 A）**: **現行維持。** `controller.direction.*.label` は ja / en とも 1 値も変えていない。**★「未着手」ではなく「案を 3 つ出して開発者が現行維持を選んだ」である**（`followup` `G-2` は 2026-07-21 から止まっていた）。
- **★タブ行の折り返しが箱からはみ出していた**（開発者の実機確認）。**原因＝共通部品 `web/src/components/ui/tabs.tsx` の `TabsList` が `h-10`（高さ 40px 固定）を持つこと。`M30-01` がタブを 4 → 8 枚にしたとき `flex-wrap` だけを足し、高さを開放していなかった。⇒ `M30-01` 由来であり `SM-149` とは無関係。** 共通部品は触らず**呼び出し側で `h-auto flex-wrap justify-start gap-1` を上書き**した（`h-10` は 1 行に収まる他画面のタブが依存する既定であり、変えると波及先が読み切れない）。

### 1-5 `data-testid` の増減（**★`docs/design/testid-convention.md` への登録義務は無い**）

**★`CHANGE-171`（`D-782`）が「付与済み一覧」の運用を畳んでおり、同書は「新規付与時に本表を更新する義務は無い」「実装が正本である」と明記している。⇒ 本項は情報提供である。**

| 増 | `recipe-special-strength-none` ／ `recipe-special-variant-{plain,holding,max-holding,perfect}` ／ `recipe-special-od-variants` ＋ `-toggle` ／ `recipe-od-variant-section-{inline,dialog}` ＋ `recipe-od-variant-toggle-{inline,dialog}` |
|---|---|
| **減** | **`recipe-special-family-{holding 系 19 本}`**（変種として畳まれた） |

**★同書が今も残す価値を認める「判断が要った付与」は 2 件ある** —— `recipe-special-variant-max-holding`（**列挙値 `max_holding` をそのまま埋めず、ケバブケース規約へ合わせた。新設 id で未配布のうちが唯一の機会だった**） ／ `recipe-od-variant-section-{inline,dialog}`（**2 面が同じ部品を描くため id を割った。割らないと同時表示で Playwright の strict mode と RTL の `getByTestId` が落ちる**）。**足すか否かは設計卓の任意である。**

**★既存 `data-testid` と aria 名は 1 つも変えていない。** aria 名は `VirtualController.test.tsx` のセレクタであり、**指示書が挙げていない契約である**（完了報告 §1.4 で棚卸しに足した）。

---

## 2. 契約・設計に反する独自判断（**★受理／却下の裁定が要る**）

### 2-1 マイグレを 1 本消費した（**ヘッダ宣言「マイグレ消費 0 本」に反する**）

| 項目 | 内容 |
|---|---|
| **何に反したか** | 指示書ヘッダ「マイグレ消費 **0 本**（★★`SD-020` は (b) を採ったためデータ属性を足さない＝`D-722`）」 ／ `M30-overview` v1.4.0 ヘッダ「マイグレ見込み **0 本**」 |
| **なぜそう判断したか** | **`P4M-016` (b) の決着条件が揃ったため。** 指示書 §2.2-2 は「原因が分かるまで直さない」と定め、§7-1 相当として開発者へ返すことを求めていた。**⇒ 2026-09-09 に開発者のインゲーム確認を得た**（逐語＝「ラウンドウェイブの強度はなしです。`round_wave` へ是正するべきです」）。**★あわせて本サブ内で切ることも開発者が明示的に指示した。⇒ 自採番ではない**（`D-293`） |
| **実装がどうなっているか** | `migrations/000107_data_correct_terry_round_wave_code.{up,down}.sql` ／ `character_data/terry.csv:27` ／ **golden 4 本の up・down 計 10 か所**（`000026` は `moves` seed と `name_ja` の 2 か所 ／ `000035` ／ `000072` ／ `000073`） ／ `internal/infra/migration/migrate_m3002_test.go`（v107 の最終状態・`down` → re-up の往復・紐付きの生存） |
| **連番の払い出し** | **着手時に `ls migrations/` で実査した。最大は `000106` だった。⇒ `000107`** |

**根拠（2 つ揃っている）**:

1. **開発者のインゲーム確認**（2026-09-09）。
2. **★★リポジトリ内の傍証** —— `character_data/command-correction-history.md` の terry の行は、`round_wave_heavy` に対する **「対応 dist code」欄に `round_wave` と記録している**（`M14-03b` 投入前チェック・2026-07-13）。**⇒ 公式データ側の識別子は最初から `round_wave` であり、本アプリ側だけが `_heavy` を持っていた。★同ファイルは `quick_burn` についてまったく同じ形の注記を持つ。**
3. **データ側の裏付け** —— terry の `power_wave` は弱・中・OD しか持たない（236HP が別技のラウンドウェイブであるため）。`name_ja` も「ラウンドウェイブ」で強度語を持たない。

**先例と同型**: `sonic_break_light → sonic_break`（`M19-04c` / `000063` 節 A） ／ `quick_burn_light → quick_burn`（`M28-05` / `000106`）。**いずれも一次源は開発者のインゲーム確認である。**

**⇒ 設計卓へ**: **受理するなら `M30-overview` の「マイグレ見込み 0 本」とボード §2.2 の「次に払い出す番号」を更新してほしい。★実査値＝本サブ完了時点で `migrations/` の最大は `000107`。⇒ 次に払い出すのは `000108` である。**

### 2-2 `CHANGE-166` が明文化した規則を A1 186 件について失効させた

| 項目 | 内容 |
|---|---|
| **何に反したか** | 指示書 §4-1「**『何を出すか』の規則を変えること**（`M30-01` が決めた。★食い違いを見つけたら報告して止まる）」。**`parseSpecialCode` の 3 段化は `isOnSpecialTab` の述語を通じて 186 件を未分類から必殺技タブへ移す** |
| **なぜそう判断したか** | **`P4M-016` の逐語**（「強度のない技のノーマル版が選択できない」）**に応えるには、この規則を変える以外の道が無い。** 指示書 §2.2-4 が「**選択できるようにする形を Plan Mode で提案する**」と求めており、**2026-09-09 に開発者が案 A を選んだ**（ボタン名「強度なし通常版」も開発者確定） |
| **実装がどうなっているか** | `web/src/features/combo/inputResolution.ts`（`parseSpecialCode` の 3 段目） ／ `moveSurfacing.ts` の `isOnSpecialTab` は**式を 1 文字も変えていない**（注記のみ更新）。**⇒ 規則は 1 か所に留まっている** |

**★これは `M30-01` §6-1 の請求**（「ファミリー UI の拡張。開発者判断＝拡張するが方法は改めて検討し設計卓へ請求する」）**に対する、A1 側の実施である。⇒ 請求の一部が本サブで消化された。**

**⇒ 設計卓へ**: **受理するなら `CHANGE-166` の帰結文と `DES-004` §2.1 / `DES-005` §5.7 を §1-1 のとおり更新してほしい。★A2 の 105 件については規則は不変であり、`M30-03` が扱う。**

---

## 3. 製造の判断

### 3-1 開発者へ確認して確定した点（**9 件・すべて 2026-09-09**）

| # | 論点 | 確定 |
|---|---|---|
| 1 | `P4M-018` の直し方 | **案 B**（強度の外からホールドを消し、変種として選ばせる）。**ジャスト・最大ホールドも射程。ただし必殺技以外は対象外** |
| 2 | `P4M-016` の形とボタン名 | **案 A**。文言は **「強度なし通常版」** |
| 3 | `SD-020` の射程 | **広げる。活性条件も直す** |
| 4 | `SM-080` ボタン名 | **案 A＝現行維持**（案を 3 つ提示した） |
| 5 | `SM-149` 配置 | **案 X′**（横並び ＋ 空きにラッシュトグル） |
| 6 | ラウンドウェイブ | **強度なし。`round_wave` へ是正すべき。★本サブでマイグレを切る** |
| 7 | 「素は押せるが OD が押せない」7 組 | **`M30-03` に委ねる** |
| 8 | ⑤ 魔身（ファミリー行の密度） | **別サブへ回す** |
| 9 | OD 節の見た目 | **案 A**（全技一覧トグルと同じ枠付きボタン） |

### 3-2 推測で進めた点（指示書 §9 / `roles-and-routing` 共通原則 3）

| # | 判断 | 理由 |
|---|---|---|
| 1 | 変種マーカーの照合を**長い接尾辞から**行う | `_max_holding` は `_holding` でも終わる。**順序を誤ると 1 変種が静かに消える**（テストで固定した） |
| 2 | **孤児は畳まない**（基底が実在するときだけ畳む） | seed 実測では孤児 0 件だが、**基底が消えた変種を畳むとファミリー行から消えて到達不能になる** |
| 3 | 代表名は **plain から採る** | 変種の `name_ja` は装飾（`【ホールド】`）と強度語を含む |
| 4 | 弱中強の行は**そのいずれかが在るときだけ描く** | 強度なししか持たないファミリーで、常に押せない 3 つを並べても意味が無い |
| 5 | `TabsList` の高さは**呼び出し側で上書き**（共通部品を触らない） | `h-10` は 1 行に収まる他画面のタブが依存する既定であり、**変えると波及先が読み切れない** |
| 6 | OD 節の折りたたみは **`CollapsibleFieldset` を使わず素の `<button>`** | 同部品は登録画面の 5 節とコンボ一覧のフィルタ欄が使っており、**見た目を変えると 6 面へ波及する** |

---

## 4. 設計担当が未把握の残課題・申し送り

> **★`followup-backlog.md` の本表は製造が編集していない**（`D-382`）。**⇒ 下記は設計卓が迷わず追加できる形で書いてある。**

### 4-1 【新規登録】`special-family-derived-variants-crowd-the-row`（⑤ 魔身）

| 項目 | 内容 |
|---|---|
| **何が起きるか** | **`P4M-016` によりファミリー総数が 273 → 342 に増えた。** 3 列グリッドに並ぶ数はキャラ最大 **21**（`jamie`） ／ 20（`akuma`） ／ 17（`ingrid` / `blanka`）。**開発者の逐語**（2026-09-09）＝「魔身がたくさん並んでいるのは確かに煩雑。魔身（酔い+1）以外の魔身は未分類とかへ入れられたらベスト。また魔身は攻撃技ではないので、必殺技の最後に置きたい。（これは DB 登録順?）」 |
| **並び順の答え** | **DB 登録順である。** `deriveSpecialFamilies` は `moves` 配列の初出順を保持し、その配列は `moves.id` 昇順＝seed CSV の行順。**⇒ 並びを決める規則は実質無い** |
| **根拠（実測）** | **「（酔い+1）以外」は `is_derived=true` と完全に一致する** —— `character_data/jamie.csv` の魔身 7 行のうち base だけが `false`、残り 6 行が `true`（全行 `damage=0` ／ `command` も同一の `d d plus p`）。**`is_derived=true` の A1 をファミリー行から外すと 342 → 276**（着手前 273）。jamie 21 → 15 ／ blanka 17 → 8 ／ dhalsim 16 → 7 ／ akuma 20 → 15 |
| **★★実装できなかった理由（3 つとも実測）** | **(1) `is_derived` はフロントへ届いていない** —— `internal/api/move/dto.go` の `MoveResponse` にも `web/src/features/moves/types.ts` の `Move` にも無い。**⇒ リスト DTO の拡張が要る。** **(2) `code` だけの代理判定は使えない** —— 「同キャラに自分の接頭辞となる別の A1 が在る」で代用できるか測ったところ**取りこぼし 90 / 116・誤爆 7**（`akuma` の百鬼系は `demon_dash` 派生だが接頭辞で拾えない）。**(3) 「非攻撃技を末尾へ」も `damage = 0` が要るが、`damage` もリスト DTO に無い**（`MoveDetailResponse` のみ）。`on_hit` / `on_block` は攻撃技でも空になるため代理にならない |
| **★★設計上の衝突** | **`M30-01` §2.9 が「`is_derived` を入力面の可否に使わない」を 2 つの理由で決めている** —— (a) `M19-DESIGN-08` §0 の述語は filler 候補から**除く**側であり裏返すと `target_combo` 117 行が入力できなくなる ／ (b) `is_derived` は既に 3 義を兼ねており、**第 4 の用途は同書 §7 の列追加審査規約が禁じる形**。**⇒ 覆すのは設計判断である** |
| **割付の候補** | **`M30` の新サブ（`M30-04` 相当）か、フェーズ4 の別サブ。★`M30-03` とは別である**（あちらは A2 のファミリー導出であり、本件は「ファミリー行の母集団と並び」） |
| **状態** | **未着手（新規登録）**。開発者判断（2026-09-09）＝別サブへ回す |

### 4-2 【新規登録】`special-family-plain-loads-but-od-does-not`（7 組の非対称）

| 項目 | 内容 |
|---|---|
| **何が起きるか** | **本サブが作った非対称である。** A1 の素が必殺技タブに載る一方、その OD は A2 のままで載らない。**⇒ 「素は押せるが OD が押せない」形が 7 組。** 開発者の実機確認で顕在化した（逐語＝「`[電刃錬気]OD波動拳` が必殺技タブのボタンから入力できないがこれは既存バグ?」） |
| **全数** | `ryu/denjin_charge_hadoken` ／ `ryu/denjin_charge_hashogeki` ／ `blanka/lightning_beast_electric_thunder` ／ 同 `_holding` ／ `lily/windclad_condor_dive` ／ `m_bison/mine_set_devil_reverse` ／ `mai/flame_musasabi_no_mai` |
| **★入力手段は 1 つも消えていない** | 6 組はキャラ固有状態タブから押せる（`denjin_charge` ／ `lightning_beast` ／ `windclad` ／ `flame`〔`flame_stock` の語幹〕）、`m_bison` の 1 組は未分類タブに残る（状態 code `psycho_mine_is_set` の語幹 `psycho_mine` が `mine_set_*` と一致しないため）。**⇒ 一貫性と見つけやすさの問題であり、機能の欠落ではない** |
| **根拠** | `web/src/features/combo/moveSurfacing.roster.test.ts` の「★★『素は押せるが OD が押せない』組が 7 件ある」が全数を固定している |
| **割付** | **`M30-03`**（開発者判断 2026-09-09）。**★同サブが A2 を必殺技タブへ載せれば 0 件になる。⇒ 上記テストが赤で知らせる** |
| **状態** | 未着手 |

### 4-3 【新規登録】`m31-02-tag-field-drag-flaky-in-full-run`

| 項目 | 内容 |
|---|---|
| **何が起きるか** | `web/e2e/m31-02-tag-field-drag.spec.ts:49` が**全数実行のときだけ**揺れる |
| **実測（2026-09-09）** | **単独実行 5 回はすべて 6 passed（30/30） ／ 全数実行 2 回はどちらも flaky**（1 回目＝「下へ」 ／ 2 回目＝「右へ」「下へ」） |
| **本サブ由来ではない根拠** | (a) 本サブの差分は `TagSelector` ／ `web/src/lib/text-drag-capture.ts` ／ `SearchableSelect` に **1 行も触れていない** ／ (b) **本追補より前の全数実行**（コミット `2335d59` 時点）**で既に同じ spec が flaky だった** |
| **★「無関係」とは言い切れない点** | 同 spec は `/combos/new` を使い、**レシピ面は `hidden` で常時マウントされている**（`web/src/features/combo/components/ComboEditor.tsx:1060-1067`）。**⇒ 本サブの変更も同じページに乗る。`display:none` なのでレイアウト箱を持たず座標はずれないが、描画の重さには効きうる** |
| **次に見るなら** | 同 spec は座標ドラッグ（`popoverBox` ＋ `EDGE`）で測っており、**4 方向のうち「右」「下」だけが揺れる。`M31-02` 完了報告の横断課題 4 が「原因ではなく回避を入れた」と自ら書いており、揺れはその回避の境界に在る可能性がある** |
| **割付の候補** | **`M31-02` の後続手番**。**★「flake」で片付けないこと** |
| **状態** | 未着手 |

### 4-4 【既存行の更新】`od-variant-active-without-od` → **解消**

**★調査完了。修飾フラグ側で常に実在していた。射程を広げて是正済み**（§1-3）。**⇒ 状態を「解消（2026-09-09・`M30-02`）」へ。**

### 4-5 【既存行の更新】`sd-020-od-variants-two-surfaces` → **解消**

**★2 面とも直した**（§1-3）。**⇒ 状態を「解消（2026-09-09・`M30-02`）」へ。**

### 4-6 【新規登録】`add-e2e-spec-cli-points-at-retired-testid-ledger`

**`.claude/commands/add_e2e_spec.md` §3-3 が、`testid-convention.md` の「付与済み一覧」を Edit で更新せよと今も指示している。★`CHANGE-171`（`D-782`）がその一覧運用を畳んでおり、同書は「新規付与時に本表を更新する義務は無い」と明記している。⇒ CLI が設計書に反する手順を複製している。** **★2026-09-08 の改善レーン D1/D2 が同じ食い違いを既に申し送っており、2 サブ連続で踏んだ**（`docs/progress/progress-log.md` の `20260908-improvement-lane-d1-d2` 横断課題 5）。**割付＝起票側（CLI の手番）。**

### 4-7 【記録のみ】その他

- **`scripts/check-import-order.sh` の `BASELINE`**: 本サブで 101 → 100 になったが**下げていない**。**★同スクリプト冒頭が「本定数は作業ツリー全体の総数である。レーンごとに測って下げてはならない」と明記している**（並走レーンが足したぶんが見えず、マージした瞬間にずれる）。**⇒ `main` へマージ後、改善レーンか開発者の手番。**
- **`DisclosureToggleButton` へ寄せていない既存 2 か所**: `RecipeBuilder` / `SetupRecipeEditor` の `recipe-pulldown-toggle` は着手時点の記述のまま。**⇒ 見た目を変えるときは 3 か所を同じ手番で動かすこと**（部品の godoc に明記済み）。寄せる整理は別の手番の候補。
- **レビュー低-3（import 順）を不採用にした**: `SpecialMovePanel.tsx` / `HitBoxLayout.tsx` の `react-i18next` が相対 import の後ろに在るのは**着手時点からの持ち越し**でベースライン内。**本サブの差分に無関係な整形を混ぜない**（指摘者も同じ結論）。

---

## 5. 参考（触れていない＝不変の証跡）

- `web/src/components/ui/tabs.tsx` — `git diff` 空（共通部品は触っていない。§1-4）
- `web/src/features/combo/components/CollapsibleFieldset.tsx` — `git diff` 空（6 面への波及なし。§3-2 の 6）
- `web/src/features/combo/optionButtons.ts` ／ `components/OptionButtonGroup.tsx` — `git diff` 空（`M24-12` の数字キーの割当は不変）
- `web/src/features/combo/moveSurfacing.ts` — **ロジックの差分 0**（変更は注記のみ。規則は `parseSpecialCode` の 1 か所に留まる）
- `inputResolution.ts` の `OD_VARIANT_ORDER` — 値の集合不変（指示書 §4-4）
- 既存 `data-testid` と aria 名 — 不変。`VirtualController.test.tsx` の「★★`M24-12` の入力の骨格に触れていない」が方向 9 種・攻撃 6 種の全数と aria 名 2 件で固定
- `internal/api/` ／ `internal/service/` ／ `cmd/` — `git diff` 空（API 契約の変更なし）
- `web/CLAUDE.md` §1 ブラウザストレージ台帳 — 追記なし（新設の UI 状態はすべて component-local の `useState`）

---

## 6. CHANGE 起票のたたき台（設計担当向けチェックリスト）

> **番号は起票時に `docs/handover/change-number-registry.md` §1 で採番すること。★製造は 1 本も消費していない。**

| # | 対象 | 内容 | 出所 |
|---|---|---|---|
| 1 | **`DES-004` §2.1 ／ `DES-005` §5.7** | **必殺技ファミリー UI が「強度なし」を持つ**（A1 186 件）。**`CHANGE-166` の帰結文は A1 について失効**（A2 105 件については不変） | §1-1 ／ §2-2 |
| 2 | **`DES-004` §2.1 ／ `DES-005` §5.7** | **ファミリーが「基底 × 変種 × 強度」の 3 軸になった**（変種＝通常 / ホールド / 最大ホールド / ジャスト）。畳む条件・照合順序・射程（`special` のみ）を含む | §1-2 |
| 3 | **`DES-005` §5.7 ／ `SUPP-001` §3.3.1** | **OD 強度組合せに (a) 既定非表示 (b) 活性条件 (c) 既存値は消さない、が付いた** | §1-3 |
| 4 | **`DES-005` §5.7** | **as-built: 通常技タブが横並びになった**（左＝方向パッド / 右＝攻撃 ＋ ラッシュトグル。ボタンの大きさは不変） | §1-4 |
| 5 | **`M30-overview` v1.4.0 ／ `docs/process/parallel-board.md` §2.2** | **マイグレ見込みの更新**（0 本 → 1 本消費）。**★実査値＝本サブ完了時点で `migrations/` の最大は `000107`。⇒ 次に払い出すのは `000108`** | §2-1 |
| 6 | **`DES-004` §2.1（データ）** | **`terry` の `move_code` 是正**（`round_wave_heavy` → `round_wave`）。**マイグレ `000107` で実施済み。★連番は着手時に実査して払い出した** | §2-1 |

**★`docs/design/testid-convention.md` への登録は義務ではない**（`CHANGE-171` / `D-782`）。判断が要った付与 2 件を §1-5 に挙げてあるので、**足すか否かは設計卓の任意である。**

---

## 7. 教訓（retrospective-log 行き・**★親は本節を読まなくてよい**）

> **宛先は `docs/handover/retrospective-log.md` へのバッチ反映であり、実施者は設計担当である。**

1. **★★新設した「禁止」は、禁止する集合の全数を実データで数えてからでないと安全でない。** 本サブの `isOdVariantApplicable` は `_od` 末尾だけを見ており、**`od` が `code` の途中に在る OD 必殺技 37 行を弾いた＝機能後退**（レビュー 高-2 が検出）。**★自分のテストは代表 1 件（`hadoken_od`）しか見ておらず、この形が 1 件も入っていなかった。⇒ 「何を許すか」を書いたら、その述語を実データへ当てて母集団を数える。**
2. **★★「片側だけ解消した」を「解消した」と書かない。** `P4M-016` は「`_od` だけ押せる 95 件」を 0 にしたが、**同時に逆向きの「素は押せるが OD が押せない」を 7 組作った。⇒ 完了報告は前者しか書いていなかった**（開発者の実機確認で顕在化）。**⇒ 対称な形が生まれていないかを、同じ手番で測ること。**
3. **★★画面の `disabled` は入口の 1 つでしかない。** 「既存データを外せるようにする」ための緩和（`disabled = !applicable && !checked`）が、**選び直しの経路では抑止をすり抜けた**（レビュー 高-3）。**⇒ 確定させる関数（`handleAdd`）の側にも同じ条件を置くまで、抑止は成立しない。★テストが「選ぶ → 判定を見る」で止まっていると、この順序は通らない。**
4. **★★共通部品の既定は、呼び出し側の新しい使い方と衝突しうる。** `ui/tabs.tsx` の `TabsList` は `h-10`（高さ固定）を持ち、`M30-01` がタブを 4 → 8 枚にして `flex-wrap` を足したとき**高さを開放していなかった。⇒ 狭幅で折り返した 2 行目以降が箱の外へ出た。★共通部品の既定を上書きする形で使うときは、その既定が何を前提にしているかを読むこと。**
5. **★jsdom はレイアウトを持たない。⇒ 「はみ出す」型の欠陥は単体テストでは測れない。** 判定はクラスで行うほかなく、**見た目そのものはスクリーンショットで確かめる必要がある。★その限界をテスト本文に書くこと**（書かないと、後任が「テストが在るから守られている」と読む）。
6. **★過去の投入前チェックの記録が、後のデータ誤りの傍証になる。** `character_data/command-correction-history.md` は「対応 dist code」欄を持っており、**`round_wave` も `quick_burn` も、公式側の識別子と本アプリ側の `move_code` が食い違っていることが 2026-07-13 の時点で表に載っていた。⇒ `move_code` の誤りを疑ったら、まず同ファイルの「対応 dist code」欄を引く。**
7. **★CLI の文言をそのまま引き写すと、失効した前提がそのまま利用者へ出る。** `/add_e2e_spec` §3-3 が畳まれた「付与済み一覧」運用を今も指示しており、**2026-09-08 の改善レーンに続き 2 サブ連続で踏んだ**（§4-6）。**⇒ CLI が設計書を参照している箇所は、設計書が変わった手番で `grep` して全数追随させる**（教訓 `E-118` と同型）。
