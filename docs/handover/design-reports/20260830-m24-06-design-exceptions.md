# 設計伝達レポート: M24-06（取込・出力の UX）

| 項目 | 内容 |
|------|------|
| 作業ID | M24-06 |
| 対象指示書 | `docs/instructions/M24-06-import-export-ux.md` v1.2.0 |
| CHANGE | `CHANGE-144` v1.2.0（**製造は番号を消費していない**） |
| 作成日 | 2026-08-30 |
| 源泉 | 完了報告 `docs/progress/M24-06-completion-report.md` ／ レビュー報告 `docs/progress/m24-06-review.md`（重大 0 / 高 3 / 中 6 / 低 4・採用 12・不採用 1） |
| 宛先 | 設計卓（親チャット） |

---

## 1. 独自確定仕様（設計書に無い／設計書より細かい決めごと）

| # | 決めたこと | 根拠・理由 |
|---|---|---|
| **1-1** | **ファイル名の「同一秒の連番」** — `combos_{slug}_{件数}_{YYYYMMDD-HHmmss}` に加え、**同じ秒に 2 回以上出したら `_2` `_3` を足す** | **`D-592` の書式は秒までしか持たない。** 書式だけでは同一秒の連続出力が衝突し、§4.4 の核心（「連続エクスポートで衝突しない形にすること」）を満たさない。§5.1 が純粋関数テストへ「連続で呼んで衝突しないこと」を課していることとも整合する。**⇒ 衝突しないことは書式ではなく払い出し口が守っている** |
| **1-2** | **名前に載る「対象の種別」は `isSelectionActive` で決める** — 選択中なら `selected`、選択ゼロ（現フィルタ結果）なら `filtered` | ワイヤ値は §5.13a の WYSIWYG 契約により**常に `range="selected"`** だが、**利用者から見た対象は 2 種類ある。** ワイヤ値をそのまま名前にすると「フィルタ結果を出したのに `selected`」になる |
| **1-3** | **ファイル名欄が持つのは「拡張子を除いたベース名」** | ダイアログは**複数形式を同時に出せる**（`CHANGE-072`/A-2）。1 つの名前に複数の拡張子が付く形にしないと、形式ごとに欄が要る |
| **1-4** | **欄の自動値を採り直す条件** — ①ダイアログを開いたとき ②対象・件数が変わったとき ③出力を実行したとき。**ただし利用者が書き換えていない間だけ** | `D-592` は「自動生成値を初期値とした編集可能な欄」までを決め、**いつ採り直すかを決めていない。** 採り直さないと、開いたまま 2 回出したときに同じ名前になる |
| **1-5** | **「0 件」の定義** — `comboFileError` が無く、取り込めるコンボ行が 0 行。**空ファイルとヘッダ行だけの CSV は両方ここに落ちる。全行エラーは 0 件ではない**（行は返るので既存の表が理由を説明する） | 指示書 §4.2 が「どれを 0 件とみなすかを報告すること」と委任していた |
| **1-6** | **取込実行の確認ダイアログの文言は固定 ja** | `ComboImportPage` は可視文言をすべて直書きしており（`t()` の直接呼び出しは 0 件）、**`DES-005` §5.19 が import/export 系を固定 ja の流儀と明記している。** 新しい流儀を作らない |
| **1-7** | **仮登録の行は「消さずに残して disabled にする」** | 指示書 §4.1 は「除外した件数を出すこと／黙って減らさない」までを決めている。**行ごと消すと「自分が書いた行がどこへ行ったか」が読めない。** 既存の「取り込めない行は disabled」の作法にも合わせた |

---

## 2. 契約違反の独自判断（設計卓の確認が要るもの）

**★該当なし（0 件）。**

- `internal/` の差分は **0 行**（`git diff --stat 87e4e73 -- internal cmd migrations go.mod go.sum` が空）。§2.3 の例外条項は発動していない。
- `DES-002` §4.2 の経路表に載る API は新設も変更もしていない。
- **ZIP の中のエントリ名（`combos.csv` / `setups.csv`）は 1 文字も変えていない**（`DES-002` §7.6 の契約）。

---

## 3. 製造判断（設計書の範囲内だが、記録が要るもの）

| # | 判断 | 内容 |
|---|---|---|
| **3-1** | **★`/export/combo` の廃止は「実態確認 → 停止 → 開発者判断 → 廃止」の順で行った** | 指示書 §4.3 の暫定案は「廃止する」だが、§9.1 は「しかできないことが在るのに廃止する」を禁じている。**実態確認で 4 件（母数 12）見つかったため停止し、開発者へ判断材料を提示して逐語「廃止する (memo の方向)」を得た。** ⇒ §11 確認事項 2 の分岐は「止まる」側に入り、**決着した** |
| **3-2** | **`CO-023`（`G-15`）は実装していない** | 着手時点で 4 点すべて入っていた（`ComboImportPage.tsx:58-70` / `:72-94` / `:100`）。**⇒ 成立をテストで固定した**（指示書 §9.3 の正しい結末）。**着手時点で `ComboImportPage` のコンポーネントテストは 1 本も無かった** |
| **3-3** | **`SM-110` は 1 語も変えていない** | 候補 20 語を「用語 ／ 出ている面 ／ 言い換えの候補」の 3 列で完了報告 §2-C に並べた。**`M24-07` の手番** |
| **3-4** | **`SM-121` の是正は 1 件だけ** | 監査の母数 6（取込・出力の面）。「無いと事故になる」は**取込実行の 1 件のみ**。他 5 件は「無くてよい」と判定した（読み取りのみ／再取得できる／遷移先が確認段） |
| **★3-5** | **★逸脱として報告する 1 件** | 指示書 §4.4 は「適用面＝`/export` と §5.13a の両方（片方だけだと作法が割れる）」と定めているが、**`/export` は §4.3 の判断により廃止したため面が存在しなくなった。** 適用先はダイアログのみ。**「片方だけに入れた」のではなく「片方が消えた」結果であり、作法は割れていない** |
| **3-6** | **`combo-csv-io.spec.ts` の取り込む行を仮登録から本登録へ変えた** | 従来 仮登録を使っていた理由は `VAL-C02` を避けて再実行を冪等にすることだったが、**`SM-071` により仮登録は 1 行も取り込めなくなった**ため成立しない。前後で fixture を掃除する形へ変えた。**spec ヘッダの説明も同時に直した**（本文だけ直すと失効記述になる） |

---

## 4. 残課題（設計卓の手番・`followup-backlog` への候補）

**★製造は `followup-backlog` を直接編集しない（`D-382`）。以下は候補である。**

| # | 候補 | 内容 |
|---|---|---|
| **★4-1** | **`DES-006` の節番号の誤り（3 文書）** | 指示書 v1.2.0・チェックリスト v1.2.0・`CHANGE-144` v1.2.0 が一貫して「`DES-006` §5（取込 VAL）」と書くが、**§5 はタグ編集**であり取込 VAL（`VAL-I01`〜`I10`）は **§6** である。**⇒ §6 を正として作業した。3 文書とも設計卓の手番** |
| **★★4-2** | **`DES-005` §5.13 の廃止と §5.13a への追記**（`CHANGE-144` の反映） | `/export/combo` を廃止したため **§5.13（エクスポート画面）は節ごと失効する。** あわせて **§5.13a へファイル名欄が増えた**（自動生成値を初期値とする編集可能な欄）。**★節の廃止は設計卓の判断であり、製造は設計書を編集していない**（§7.4.1 の #4 に該当） |
| **★★4-3** | **廃止で失われた機能 2 件の記録** | **(a) キャラ横断の一括出力（代替なし）。** `useResolvedCharacterId` は必ず 1 キャラへ解決するため、UI に「全キャラ」という状態が存在しない。**`FR401` が CSV を「バックアップ・データ移行用途」と位置づけていることと直結する。** **(b) 任意コンボ ID の直接指定（代替なし）。** **★どちらも開発者が「残す価値なし」と判断して廃止したものであり欠陥ではない。** ただし**将来「全キャラのバックアップが要る」となったら、戻す先はダイアログではなく別の手段**（DB ファイルのコピー、またはバックアップ機能の実装）である |
| **★4-4** | **仮登録は往復で復元できない** | エクスポートした CSV は `is_draft=true` を保持するが、**取込側が `SM-071` で除外する**ため取り込み直せない。**⇒ `DES-002` §7.6 の「往復対称」は本登録について成立し、仮登録については成立しない。** `SM-071` の直接の帰結であり欠陥ではないが、**設計書に 1 行残さないと後任が「往復が壊れている」と起票する** |
| **★4-5** | **`SM-110` の用語候補 20 語を `M24-07` へ渡す** | 完了報告 §2-C の 3 列表。**とくに「インポート」と「取込」がアプリ内で 2 本立てになっている**のは、語を選ぶ前に統一の要否を決める必要がある |
| **★4-6** | **ZIP エントリ名の定数が 2 パッケージに独立して定義されている** | `internal/service/comboio/types.go:11-13`（export 側）と `internal/api/comboio/handler.go:42-43`（import 側）。**片方だけ変えれば Go テストも E2E も赤くなるが、両方を同時に変えると緑のまま `DES-002` §7.6 の往復対称が壊れる。** 一本化は `internal/` に触るため本サブでは行っていない |
| **★4-7** | **CSV エクスポートの 1000 件の静かな打ち切り** | `export.go:28-32` は上限到達を `slog.WarnContext` に出すだけで**利用者へ通知しない**（コメントも「利用者通知は将来サブで検討」）。**`FR401` の「バックアップ用途」との齟齬** |
| **★4-8** | **アプリ内バックアップ／リストアが未実装のまま画面に出ている** | `SettingsSectionData.tsx:22-45` の 3 ボタンが `disabled` ＋「今後実装予定」。**`/export/combo` の廃止判断の前提になった事実** |
| **★4-9** | **`CO-023`（`G-15`）は閉じきっていない** | (a) の router 層リファクタと (b) の「候補:」先頭提示は本サブの射程外。**⇒ followup を閉じるかは設計卓の判断** |
| **4-10** | **他面の確認ダイアログの不足 1 件** | `GamepadCalibrationDialog.tsx:348` に `window.confirm` が残存（`architecture-patterns` §1.2.4 が禁止と明記）。**是正は本サブの面ではない** |
| **4-11** | **`/export/combo` にだけ在った欠陥 1 件（廃止で消滅）** | 視覚出力（PDF/PNG/クリップボード）へ `videoPath` / `imagePath` を通していた（`CHANGE-073` の意図に反する）。**ダイアログ側は `run-export.ts:70` で除去していた。** 実害は廃止で消えたが、非対称の記録として残す |

**★`4-3` ／ `4-7` ／ `4-8` は下の §4.1（`FR401` の見直し案）の根拠である。** 単独の残課題としても読めるが、**3 件は同じ 1 つの構造を別の角度から見たものである。**

### 4.1 ★★`FR401` の見直し案（**開発者の指示・2026-08-30**）

**★発端**: 開発者が本サブの実機確認（3 件とも通過）のあとに **「`FR401` 側の見直しをしたい」** と判断した。きっかけは §4-3——**`/export/combo` の廃止で「全キャラ横断の一括出力」が無くなった**のに、`REQ-001` `FR401` は CSV を **「バックアップ・データ移行用途」** と定義したままだったことである。

**★本節は改訂案である。製造は `REQ-001` を編集していない**（`CLAUDE.md` §8）。

#### (1) 調べて分かった構造——**バックアップの FR が 1 つも無い**

| # | 事実 | 出所 |
|---|---|---|
| **1** | **`REQ-001` で「バックアップ」に触れるのは `FR401` の括弧書き 1 か所だけである。** バックアップの機能要件は存在しない | `requirements.md:170`。`バックアップ｜リストア｜復元` の全ヒットは `FR013` ／ `FR401` ／ 廃止済み `FR603` の 3 行のみ |
| **2** | **実際のバックアップ機構は `NFR201`（誤操作等で全データが消失する事態を防止）由来で、設計だけが先行している** | `DES-002` §6.3＝DB 配置の安全化・危険操作前の `VACUUM INTO` スナップショット 10 世代・**手動バックアップ／リストア機能（設定画面から）** ／ `DES-006` §8＝`VAL-B01`〜`B03` ／ `DES-005` §5.16 の 3 ボタン |
| **3** | **その手動バックアップ／リストアは未実装のまま `disabled` で画面に出ている** | `SettingsSectionData.tsx:22-45`（ツールチップ「今後実装予定」） |
| **4** | **CSV は「原状復帰の手段」として成立しない**（本サブの実測 4 点） | ①**1000 行で静かに打ち切る**（`export.go:18` ＝ `csvcore/rules.go:8`。通知は `slog.WarnContext` のみ＝`export.go:28-32`） ②**DB 管理列を落とす**（`DES-002` §7.6・設計どおり） ③**タグは操作した利用者のものだけ**（`CHANGE-113`） ④**全キャラ横断の出力手段が無くなった**（§4-3） |
| **5** | **仮登録は往復で復元できない** | §4-4（`SM-071` の帰結） |

**⇒ `FR401` の「バックアップ」という語が、`NFR201` → `DES-002` §6.3 が担うべき責務を肩代わりしている。** トレーサビリティが**括弧書き 1 語**に乗っている状態である。

**★`REQ-001` §9.1 の課題対応表は、既に `FR401`〜`FR406` を「データ共有・エクスポート」と呼んでおり、バックアップとは呼んでいない。** 下記の改訂案はこの既存の呼び方と整合する。

#### (2) 改訂案の逐語（before / after）

**before**（`requirements.md:170` の現行）:

> | FR401 | CSV等の形式でコンボデータをエクスポートできる（バックアップ・データ移行用途）。コンボとセットプレイは別CSVファイルとして出力し、インポート時の別ファイル方式と対称な形式とする | 重要 | 3 |

**after**（案）:

> | FR401 | CSV等の形式でコンボデータをエクスポートできる（**データ移行・共有用途**）。コンボとセットプレイは別CSVファイルとして出力し、インポート時の別ファイル方式と対称な形式とする。**★全データの保全（バックアップ）は本要件の射程ではない**——出力は行数上限（1000 行）で打ち切られ、DB 管理列を持たず、タグは操作した利用者のものだけを載せるため、原状復帰の手段として成立しない。**バックアップの方針は `NFR201` ／ `DES-002` §6.3 が持つ** | 重要 | 3 |

**★「バックアップ」の語を黙って消すのではなく、「射程ではない」と明示して残すこと**を勧める。**消すだけだと、後任が「なぜ CSV でバックアップしないのか」を再発見するところからやり直す**（撤回の記録を残さないと同じ要望が再登場する＝`D-482` の趣旨）。

#### (3) ★設計卓が決めること（**製造は決めない**）

| # | 決めること | 製造の見立て |
|---|---|---|
| **(a)** | **バックアップに FR を新設するか。** それとも `NFR201` の非機能要件のままとし、`DES-002` §6.3 を実現方式の正本と明記するか | **★製造の推奨は「今回は新設しない」。** 理由＝`NFR201` は既に「全データが消失する事態を防止すること」を要求しており、`DES-002` §6.3 がその実現方式を持っている。**⇒ 穴は「要件が無いこと」ではなく「`FR401` が別の責務を名乗っていること」であり、`FR401` を直せば閉じる。** ただし**設定画面に機能ボタンが出ている以上、機能要件として立てたくなる余地は残る**（下記 (c)） |
| **(b)** | (a) で新設する場合、**`REQ-001` §9.1 の範囲表記を機械的に広げないこと** | **★同節が自ら警告している**——「FR を新設したときは、本表の範囲表記を機械的に広げないこと。広げると『その FR が当該課題のために在る』という誤読を生む」 |
| **(c)** | **`FR401` から「バックアップ」を外すと、設定画面の `disabled` 3 ボタンが「何の要件に紐づくか」が改めて問われる** | 現状の紐づけは `NFR201` → `DES-002` §6.3 のみ。**★ボタンの実装・削除は本サブの射程外**であり、要件が決まる前に画面を動かしていない |

#### (4) 波及の見込み

| 対象 | 影響 |
|---|---|
| `REQ-001` `FR401` | **本体**（上記 after） |
| `REQ-001` §9.1 | **触らない見込み**。既に「データ共有・エクスポート」と呼んでおり、改訂案と整合する |
| `DES-002` §7.6 | **触らない見込み**。CSV 契約そのものは不変（本サブでも 1 文字も変えていない） |
| `DES-002` §6.3 ／ `DES-006` §8 ／ `DES-005` §5.16 | **(a) の答え次第。** 新設しないなら「`NFR201` の実現方式である」ことを 1 行明記するだけで足りる見込み |


---

## 5. プロセスの申し送り（playbook / 指示書テンプレートへの候補）

| # | 内容 |
|---|---|
| **★★5-1** | **★指示書 §7.2 が例示するポート確認コマンドが、この環境では常に空振りする。** `(ss -ltnp \|\| netstat -ltnp) \| grep -E '47390\|5273' \|\| echo "空き"` は、**`ss` も `netstat` も存在しない**ため grep が必ず空になり、**ポートが埋まっていても「空き」と答える**（`M24-09c` / `D-570` と同じ機序）。**⇒ 対照実験（既知の listener を立てて USED と答えるか）を通してから使うこと。** 本サブは python の `socket.connect_ex` へ差し替えた。**★同じ例示が他サブの指示書にも載っているため、同型の空振りが起きうる** |
| **★★5-2** | **★「時刻に依存する性質」の観測は E2E ではなく純粋関数側に置くこと。** 破壊確認 4b（衝突回避だけを外す）で **E2E は 1 回目が赤・retry が緑**になった。2 回の出力が同じ秒に落ちたときだけ衝突するためである。**決定的に守っているのはクロックを注入した純粋関数テストだった** |
| **★5-3** | **★破壊確認は「単純な早期 return」では作れない場合がある。** `noUnusedParameters` / `TS18047` でビルドが落ち、**「テストが赤い」ではなく「起動しない」**になる（`make e2e` は `pnpm build` を通るため webServer が上がらない）。**⇒ 引数を使ったまま結果を捨てる／条件を never true にする形で壊すこと。** `M24-05` の完了報告に同じ記録があり、実際に役に立った |
| **★5-4** | **★破壊確認の結果を `head` で切らないこと。** 本サブでは破壊確認 5 の `go test ./internal/...` を `head -20` で切ったため「Go は全緑のまま」と誤読しかけた（**実際は `internal/service/comboio` の 4 テストが赤**）。**採取コマンドにパイプで切り詰めを入れない**という §7.2 の規約は、破壊確認の観測にも効く |
| **★5-5** | **★レビュー担当は製造セッションの会話ログを参照できない。** 本サブは開発者判断（`/export/combo` の廃止）を Plan Mode 内で得たが、レビューは「逐語の痕跡が完了報告の引用以外に無く検証できない」と指摘した（**正当な指摘である**）。**⇒ 開発者判断を得た場合は、完了報告に「いつ・どの問いに対して・逐語で何と答えられたか」を独立した節として残すと、レビューが判定できる** |

---

*以上、M24-06 設計伝達レポート。* **★★契約違反の独自判断は 0 件。** **★最大の判断は `/export/combo` の廃止であり、実態確認 → 停止 → 開発者判断 の順で行った。** **★★`DES-005` §5.13 の廃止は設計卓の手番である（4-2）。**
