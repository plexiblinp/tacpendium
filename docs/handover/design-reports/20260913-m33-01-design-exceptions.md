# M33-01 設計伝達レポート（例外レポート）

| 項目 | 内容 |
|---|---|
| 対象 | **親チャット（設計卓）** |
| 発信 | 製造担当 Claude Code ／ 2026-09-13 |
| 指示書 | `docs/instructions/M33-01-freeze-and-survey.md` **v1.1.0** ／ チェックリスト **v1.0.0** |
| 実装コミット | ブランチ `claude/upbeat-galileo-ejyts1` ／ **3 コミット**（`db3572a` `c92891b` `5167ada`）**push 済み** |
| 関連 | 完了報告 `docs/progress/M33-01-completion-report.md` ／ レビュー `docs/progress/m33-01-review.md` |
| 作成条件 | 実装直後の同一セッション |
| **レビュー集計** | **高 4 / 中 5 / 低 3 ＝ 全 12 件・採用 12 / 不採用 0・「高」の不採用 0 件・再レビュー往復 0 回（上限 2）** |
| 実機確認 | **不要**（**★実装差分が 0 である**——成果物は `docs/progress/` の 3 ファイルのみで、UI・API・DB スキーマの振る舞いが 1 つも変わっていない。**⇒ 実機で見る対象が存在しない**） |
| 凍結点 | **`5576126`**（**★以後 `M33-02` / `M33-03` はこの SHA を基準に語ること**） |

**本書は①独自確定仕様 ②契約違反の独自判断 ③製造判断 ④残課題に絞る。指示書どおりの部分は割愛する。**

**★★★最重要は §4-1 である**——**`M33` の 7 群のうち `S02` と `S06` が、ライセンスの層をまたいでいる。⇒ `M33-02` の着手前に裁定が要る。★機械検査はこの向きを検出しないため、放置すると緑のまま層が混ざる。**

**★★次に重いのは §4-2 である**——**`000018` / `000074` は「凍結表の据置きだけで緑になっている 2 本」であり、`M33` が 111 本を潰した瞬間に据置きが消えて判定対象になる。⇒ §4-1 の裁定に含める必要がある。**

**★§1-1 は `M33-02` が直接使う仕様である**（スキーマ同一性の固定方法）**。⇒ ここを取り違えると、正しい実装が赤くなる。**

---

## §1 製造が独自に確定した実装仕様（DES 反映が要るもの）

### 1-1 ★★★スキーマの同一性は「正規化ハッシュ ＋ レシピ」で固定する（**生テキストでは固定できない**）

**指示書 §2.5-3 は「得られたスキーマのハッシュを報告へ書く」としか言っておらず、取り方を定めていない。⇒ 製造が定義した。**

**★★素朴に `sqlite_master.sql` を連結してハッシュすると `M33-02` で必ず食い違う。** 基準 DB の生テキストは、旧系列が 111 本かけて積み上げた文字列そのものだからである（すべて実測）。

| 表 | 生テキストの実態 |
|---|---|
| `combos` | **`CREATE TABLE "combos" (`** ——**表名が引用符付き**。`000016` / `000019` の表再構築で `new_combos` → `RENAME TO combos` した跡 |
| `moves` | **`ALTER TABLE ADD COLUMN` の追記が 1 行に連なった形**で保存されている |
| `games` | 元の閉じ括弧の後ろへ `, current_data_version TEXT NOT NULL DEFAULT '2026.08.03.01' CHECK (…))` が追記された形 |
| **13 表** | **旧 SQL の `--` コメントを本文として含む**（`characters` / `moves` / `combos` ほか） |

**⇒ `M33-02` が `S01` を素直な `CREATE TABLE` 群として書き直すと、意味が完全に同じでも一致しない。**

**★★★調査資料の `schema_exact_equal=True` を「手書きの `S01` でも一致する」と読まないこと。** 同値は `research.py:102,115` が**累積 DB の `sqlite_master.sql` をそのまま別 DB で実行し直して**得たものであり、**手書きの一致は一度も検証されていない**（レビュー担当が独立に確認）。

**⇒ 採った形＝`pragma table_info` / `foreign_key_list` / `index_list` から構造だけを取り、テキストの見てくれを捨てる。**

| 正本（`M33-02` が突き合わせる） | 値 |
|---|---|
| 正規化スキーマ（21 表・250 行） | **`9b87a96e67042850603e692fc3a3599e9002bdec3ac8a3f71ba6b3a2809ad77e`** |
| `sqlite_sequence` | **`694011b0fe4e87f8098b220eb3a9b1a414ea5df0cc1136208738d2aefe48fb69`** |

**★★★値だけを引き継がないこと。ハッシュは「レシピ」と対でしか意味を持たない。**
**実例＝レビュー担当が独立に算出した正規化ハッシュは `8a86d298…`、`sqlite_sequence` は `4343fdcc…` であり、本報告の値と違う。★どちらも間違っていない**——**連結の書式が違うだけである。⇒ `M33-02` へ渡すのはスクリプト本体である**（完了報告 §5.2.1 に全文を載せた）。

- 根拠＝完了報告 §5.2.1 / §5.2.2 ／ 調査 `README.md:40`（`evidence-final-schema.sql` を仕様の正典にするなという禁止）
- **⇒ `SUPP-001` §5.5.4 へ規約として明文化してほしい**（現在の最大は `(17)` 起票見込み＝`M30-07` 由来。**番号は設計卓が採る**）。文面のたたき台：

> **(N) 累積 DB と手書きマイグレの同一性を「`sqlite_master.sql` のテキスト」で固定しない。** 適用済みの DB には表再構築の引用符（`CREATE TABLE "combos"`）・`ALTER ADD COLUMN` の追記の連なり・旧コメントが残っており、**意味が同じでも手書きの DDL とは一致しない。⇒ `pragma` 経由の正規化投影を突き合わせること。★正規化ハッシュは「取り方のスクリプト」と対で引き継ぐ**（書式が違えば値も違う）。

### 1-2 ★基準 DB の作り方（**`sqlite3` CLI がこの環境に無い**）

**指示書 §2.5-1 は「手順を書き、実際に 1 度作って通ることを示す」としか言っていない。⇒ 経路を製造が確定した。**

- **`which sqlite3` → 見つからない**（実測。レビュー担当も独立に確認）
- **⇒ 新規依存を足さず**（`CLAUDE.md` §6）**、既存の `scripts/dev-throwaway-db.sh` と同じ経路 ＋ `python3` 標準ライブラリで済ませた**
- `TACPENDIUM_DB_PATH=scratch-m33-01.db`（**`.gitignore:30` の `*.db`**）／ `TACPENDIUM_CONFIG_PATH` を**作業ツリー外**へ逃がして `config.toml` を作らせない
- 結果＝`schema_migrations` `version=112` / `dirty=0` ／ アプリスキーマ **42 オブジェクト**（表 21 ＋ 索引 21。**調査資料 `verification.md:7-9` と一致**）／ `foreign_key_check` 0 ／ `integrity_check ok`

**⇒ `M33-02` は同じ手順で作り直す。手順の全文は完了報告 §5.1。**

### 1-3 ★★ユーザーデータ表は「二分」できない —— **hybrid が 3 表ある**

**指示書 §2.4-1 は「ユーザーデータ表の一覧」（seed で埋まる表との二分）を求めている。⇒ 実測すると二分できない表が 3 つあった。**

| 表 | なぜ二分できないか | 分ける述語 |
|---|---|---|
| `presets` | 組込 3 件（`user_id IS NULL` / `is_builtin=1`）＋ **利用者が足す行** | **`is_builtin` / `user_id` で分かれる**（実測） |
| `users` | 初期ユーザー 1 件 ＋ **利用者が足す行** | **★未確定**（列に seed/利用者の別を示すものが無い） |
| `tags` | 予約タグ 3 件 ＋ **利用者が作るタグ** | **★未確定**（`category` 列は在るが、予約かどうかの判定に使えるかは未検証） |

**⇒ `M33-03`（移行手順）は「seed 由来と利用者由来を分ける述語」を決める必要がある。★`users` / `tags` の 2 表は本サブでは確定していない。**

- 根拠＝完了報告 §4.2 (b) ／ 基準 DB の `pragma table_info`（`users` = `id,name,password_hash,main_character_id,created_at` ／ `tags` = `id,user_id,name,category,color`）

### 1-4 ★投入順は 21 表・自己参照 3 本・複合 FK 1 本（**FK 依存から算出**）

**指示書 §2.4-2 が求めた「依存を持たない表から並べた順序」を算出した。⇒ `M33-03` の移行手順が直接使う。**

- **自己参照 3 本**（`moves.original_move_id` / `combos.materialized_from_combo_id` / `combos.superseded_by_combo_id`）**はいずれも nullable。⇒ 「NULL で入れてから UPDATE」の 2 パスで解ける。★遅延制約は要らない**
- **唯一の複合 FK ＝ `combo_setup_results` → `combo_setups(combo_id, setup_id)`。⇒ 必ず `combo_setups` の後**
- **★マイグレ接続は `foreign_keys=OFF`・アプリ接続は `ON`**（`internal/infra/db/db.go`）**。⇒ 移行スクリプトを FK=ON で書くなら順序が要る**
- 全 21 表の順序は完了報告 §4.3。**レビュー担当が基準 DB の `pragma foreign_key_list` で全 21 表を独立に照合し、依存先の完全一致とトポロジカル順の妥当性を確認済み**

### 1-5 ★API 経路の新設・変更 —— **なし**

**`DES-002` §4.2 の経路表に載る API を 1 つも触っていない**（コード差分 0）。**⇒ 本節の逐語埋め込み規則（DTO・エラー契約・母集団の述語・持たせなかった分岐）は非該当。**

---

## §2 契約・設計に反する独自判断

**なし。**

**★念のため 2 件について「該当しない」判断の根拠を残す**（親が §4 と取り違えないため）。

| 一見 §2 に見えるもの | なぜ §2 でないか |
|---|---|
| **廃止候補を 7 本 → 8 本にした**（`000009` を追加） | **指示書 §2.2-2 は 7 本を名指しするが、同 §2.2-4 が「調査資料の対応表と食い違った行を数える。★食い違いは価値のある成果であり、隠さない」と明記している。⇒ 指示どおりである** |
| **7 群の層の割当に反する指摘を出した**（§4-1） | **実装が別の判断を下したのではない**——**実装物が 1 つも無い**（`migrations/` 差分 0 バイト）。**製造は事実を出しただけで、裁定は親へ回している。⇒ §4 が正しい置き場である。★ただし親の裁定が要る点では §2 と同じ重さであり、冒頭で最重要に指名した** |

---

## §3 製造の判断

### 3-1 開発者へ確認して確定した点

| # | 論点 | 確定 |
|---|---|---|
| **1** | **対応表 111 行の作り方**（調査資料は 104 行しか無く、`000106`〜`000112` の 7 行は新規判定が要る） | **「式で導出 → 差分だけ精査」を採用**（開発者選択）。**⇒ 各 up の「書き込む表 / DDL する表」を機械抽出して `S01`〜`S07` へ割り当て、調査 CSV と突き合わせ、食い違った行・廃止 7 本・新規 7 行だけを実物の SQL で精査した。★指示書 §0.3「全項目を式で数える」に最も忠実な形** |
| **2** | **基準 DB をこのセッションで実際に構築してよいか**（`sqlite3` CLI 不在のため経路が非自明） | **「実行する」を採用**（開発者選択）。**⇒ §1-2 の経路で 1 度作り、`version=112`/`dirty=0` とハッシュを得た。★束 D-1 が「実際に 1 度作って通ることを示す」を求めているため、書くだけでは未達だった** |

### 3-2 推測で進めた点

| # | 箇所 | 推測の内容と明示のしかた |
|---|---|---|
| **1** | **`preset_aliases` の `sqlite_sequence` 差 179 の機序** | **`moves` と同値の 179 である理由を「削除された技 1 件につき official_ja の alias が 1 件消えるため」と推定した**（`000072` 以降の numeric / srk エイリアスは削除より*後*に足されており、削除時点では official_ja しか無かった）。**★完了報告 §4.4 に「この因果は推定であり実測していない」と明記した。★差 179 という数そのものは実測である** |
| **2** | **`000018` / `000070` の吸収先の粒度** | **`000018` は `S01,S03`**（`UPDATE moves SET raw_data = json_remove(…)` が `moves` の最終値を形づくるため）、**`000070` は `S01`**（`ALTER TABLE … ADD COLUMN` 1 文のみでデータ 0 行）**と判定し、調査資料と食い違う旨を出した。★どちらが正かは決めていない**——**`000070` は「分類の粒度の違いであって誤りではない」と書いた** |

**★指示書 §2.1-3 / §3-5 が「食い違いはそのまま報告し、どちらが正かを本サブで決めない」と定めているため、上記 2 件とも判断を親へ残している。**

---

## §4 設計担当が未把握の残課題・申し送り

### 4-1 ★★★`S02` と `S06` はライセンスの層をまたいでいる（**`M33-02` 着手前に裁定が要る**）

**`M33-overview` §0.5.3 は `S02`〜`S06` を一律 **層 B**（`CC-BY-SA-4.0`）としている。⇒ `GAME_TABLES` の基準で検算すると、2 群が層をまたぐ。**

```
GAME_TABLES = ("characters", "moves", "move_commands", "move_derivations",
               "preset_aliases", "custom_states")     ← scripts/check-migration-license.sh:102-103
```

| 群 | **層 A 側の表**（`GAME_TABLES` に無い） | 層 B 側の表 |
|---|---|---|
| **S02** | **`games`** | `characters` / `custom_states` |
| **S06** | **`presets`** | `preset_aliases` |

**★これは製造が「ゲームデータかどうか」を自分で判断したものではない。⇒ 設計卓自身が `S07` を層 A とした論法**（同 §0.5.3「`users` / `tags` は SF6 の事実ではない。⇒ `GAME_TABLES` に入らない」）**を `games` / `presets` へそのまま当てただけである。**

**実測の裏づけ**——`000002_seed_games`（`games`）／ `000005_seed_presets`・`000069_m20_initial_presets_three`（`presets`）**は 3 本とも層 A に解決している**（`check-migration-license.sh --list`）。

> **★★★機械検査はこの向きを検出しない。**
> 規則 (4) は **(a) `_data_` を持つなら層 B へ解決すること ／ (b) `_data_` を持たないなら層 A へ解決し、かつ `GAME_TABLES` へ書かないこと** である。
> **⇒ `NNNNNN_data_seed_games_characters.up.sql` は (a) を満たすため*緑になる*。★どの表へ書くかを見ない。**
> **⇒ `M33-02` が `S02` / `S06` を素直に 1 本ずつ作ると、`games` と `presets`**（アプリの設計物であって SF6 の事実ではない）**が `CC-BY-SA-4.0` へ静かに移る。**
> **★開発者逐語の前例＝「一度 AGPL 化してから CC BY SA に戻すのは難しい認識」**（同スクリプトのヘッダ）**。向きは逆だが、戻しにくさは同じである。**

**⇒ 判断を求める**: **(a) `S02` / `S06` を層ごとに分割する**（例＝`S02a` games=層 A ／ `S02b` characters・custom_states=層 B）／ **(b) `games` / `presets` を層 B へ移すことを是とする**／ **(c) 現状のまま進める**（その場合、層が混ざることを明示的に受容した記録が要る）。

- 根拠＝完了報告 §2.8 ／ `M33-overview` §0.5.3（`:58-72`）／ `scripts/check-migration-license.sh:102-103` `:22-27`

### 4-2 ★★★`000018` / `000074` は「凍結表の据置きだけで緑になっている 2 本」（**§4-1 の裁定に含めること**）

**全 111 本の up のうち、`GAME_TABLES` へ書きながら層 A に解決しているのは 2 本だけである**（実測）。

| 旧番号 | 書き込む表 | 実体 |
|---|---|---|
| **`000018`** | **`moves`** | `UPDATE moves SET raw_data = json_remove(…)` |
| **`000074`** | **`preset_aliases`** | `UPDATE preset_aliases SET character_id = (SELECT m.character_id FROM moves m …)` |

**★★赤くならない理由＝両方とも凍結表に在るからである。** 規則 (3) は凍結表の行を「表どおりか」だけで判定し、**規則 (4)（`_data_` と `GAME_TABLES` を見る本体）は凍結表に*無い*ファイルにしか当たらない。**

**⇒ `M33` は 111 本を 7 本へ潰す作業であり、その瞬間に凍結表の据置きが全部消える。⇒ この 2 本の効果は、規則 (4) の判定を受ける新しい群のどこかへ入る。**

**★分割すれば整合する**——`000018` は DDL 部が `S01`（層 A）／ `raw_data` の最終値が `S03`（層 B）、`000074` は `ALTER` が `S01`（層 A）／ `character_id` の値が `S06`（層 B）。**⇒ 分割しなければ層をまたぐ。**

- **★本件はレビュー（高-4）が発見した。** 製造の初版は緩い突合（多群行を素通しする条件）で 3 件しか出しておらず、この 2 本が見えていなかった。**⇒ 厳密条件では 5 行**（`000002` / `000005` / `000018` / `000069` / `000074`）。
- 根拠＝完了報告 §2.8（式10 / 式10b）／ `migrations/000018_cleanup_moves_unobservable_columns.up.sql` ／ `migrations/000074_m20_preset_aliases_add_character_id.up.sql`

### 4-3 ★★`000009` は調査資料が挙げていない 8 本目の廃止候補（**ただし純粋な no-op ではない**）

**`000009_seed_characters_aki_jamie_guile` の投入行は 1 行も生存しない。⇒ 調査資料は `S02` に分類しており、廃止候補に挙げていない。**

| キャラ | `000009` で投入 | `000017` で削除 | **最終行の出所** |
|---|---|---|---|
| guile | ✅ | ✅ | **`000024`**（第一波） |
| jamie | ✅ | ✅ | **`000053`**（第三波） |
| aki | ✅ | ✅ | **`000082`**（第四波） |

**★裏づけ（レビュー担当も独立に再現）**——`000009` の書き込みは `INSERT INTO characters` **1 文のみ**、`DELETE FROM characters` は全 111 本のうち **`000017` の 1 本だけ**。

> **★★★ただし「廃止できる」と「書かなくてよい」は別である。**
> **`characters` は `AUTOINCREMENT` であり、行が消えても消費した id は `sqlite_sequence` に残る。**
> **実測（基準 DB）＝`characters` は 行 31 / `seq` 34。★差の 3 が、まさに `000009` の 3 行である。**
> **⇒ `M33-02` が `S02` を作るとき `000009` を単に「無かったこと」にすると、調査資料の完了条件 3（`sequence_equal`）が落ちる。**

**⇒ `M33-overview` の「独立処理を廃止できる 7 本」を 8 本へ改めるかの判定をお願いする。**

### 4-4 ★`SUPP-001` §2.7 の本数と範囲が失効している

| 項目 | `SUPP-001` §2.7（2026-09-06 実測） | 実測（2026-09-13） |
|---|---|---|
| `*.up.sql` の本数 | **102 本** | **111 本** |
| 連番の範囲 | `000001`〜`000103` | `000001`〜`000112` |
| **欠番** | **`000012` の 1 件のみ** | **`000012` の 1 件のみ（一致）** |

**★機序は判明している**——同節の実測日以降に `000104`〜`000112` の 9 本が足された（`000104`/`000105` は `M28-02a`＝`40b6cfc`・PR #155 で**実測と同日マージ**のため範囲外になった）。

**⇒ as-built 化をお願いする。★欠番が `000012` の 1 件だけである点は今日も成立しており、そこは変えなくてよい。**（製造は設計書を直接編集しない＝`CLAUDE.md` §8）

### 4-5 ★★調査資料の数は「基準日つき」で読むこと（**2 件が実測とずれている**）

| 資料の記載 | 実測（2026-09-13） | 機序 |
|---|---|---|
| **`Migrate`/`Steps` ＝ 28 ファイル** | **35**（調査と同一の式）／ **38**（`.Up(` `.Version(` 込み）／ **40**（repo 全体） | **調査の式が `internal/infra/migration/*.go` に*ディレクトリ限定*である**（`research.py:151`）**。★増えた 7 本は新マイグレ 7 本と 1 対 1・消失 0。★40 のうち 2 本は `internal/infra/datadir` の同名 `Migrate` であり別サブシステム** |
| **golden ＝ 4 本**（`M33-overview:163`） | **17 stem ＝ 34 SQL ファイル** | 設計卓の把握が `000026`/`000035`/`000072`/`000073` の 4 本に留まっていた。**⇒ `M33-03` の書き換え量が 4 倍以上になる** |

**★`.Force(` と `.Down(` はリポジトリ全体で 0 件。★マイグレ総数・最大版数を assert するテストは存在しない**（いずれも実測）。**⇒ 統合で壊れる経路はそのぶん少ない。**

### 4-6 ★`combos` / `combo_oki_options` は 0 行でも `sqlite_sequence` に `seq=0` の行を持つ

**行数が 0 なので「触っていない表」に見えるが、`sqlite_sequence` には行が在る**（`combos` は `000016`/`000019` の表再構築、`combo_oki_options` は `000021` の backfill が 0 行の `INSERT` を走らせたため）。

**⇒ `M33-02` が「空の表は飛ばす」と実装すると `sequence_equal` で静かにずれる。** 実測でしか出ない型の知見であるため申し送る。

### 4-7 レビュー低指摘の繰越 —— **なし**

**★不採用は 0 件である**（全 12 件を採用）。**⇒ 繰り越した指摘は無い。「高」の不採用も 0 件のため、開発者確認は発生していない。**

### 4-8 ★★`followup-backlog.md` §J 行の原稿（**設計卓が受理の手番で転記する**）

**★本ファイルは 1 文字も編集していない**（`D-838`。`git log 5576126..HEAD -- docs/handover/followup-backlog.md` が 0 件＝レビュー担当も確認済み）。

#### 原稿 1（新規登録）

| フィールド | 内容 |
|---|---|
| **ID（スラッグ）** | `m33-groups-s02-s06-straddle-license-layers` |
| **発生元** | `M33-01`（完了報告 §2.8 ／ 本レポート §4-1・§4-2） ／ レビュー `docs/progress/m33-01-review.md`（高-4） |
| **未解消の理由** | **`M33-overview` §0.5.3 は S02〜S06 を一律 層 B とするが、`GAME_TABLES` の基準では `games`（S02）と `presets`（S06）は 層 A である。⇒ 7 群の定義そのものを変える判断であり、製造の裁量を超える**（指示書 §3-5＝契約を変えたくなったら射程外）**。★あわせて `000018` / `000074`（凍結表の据置きだけで緑になっている 2 本）の割付も同じ裁定に含まれる** |
| **再開に必要な条件** | **設計卓が (a) S02 / S06 を層ごとに分割する ／ (b) `games` / `presets` を層 B へ移すことを是とする ／ (c) 現状のまま受容する のいずれかを裁定すること。★`M33-02` の着手前** |
| **記録日・状態** | 2026-09-13 ／ **未着手**（本サブの射程は「出すこと」まで） |
| **割付の候補と理由** | **`M33-02` の直前**。同サブが 7 群の実体を作る手番であり、それより後では作り直しになる |

#### 原稿 2（新規登録）

| フィールド | 内容 |
|---|---|
| **ID（スラッグ）** | `m33-000009-eighth-retirement-candidate` |
| **発生元** | `M33-01`（完了報告 §2.6 ／ 本レポート §4-3） |
| **未解消の理由** | **調査資料が挙げた廃止 7 本に `000009` が入っていない。実測では生存行 0 であり 8 本目に当たる。★ただし `sqlite_sequence` の 3 を再現する必要があり、単純な削除では `sequence_equal` が落ちる。⇒ 対応表の更新は設計卓の受理事項** |
| **再開に必要な条件** | **設計卓が `M33-overview` の「廃止 7 本」を 8 本へ改めるかを判定すること。⇒ `M33-02` の入力になる** |
| **記録日・状態** | 2026-09-13 ／ **未着手** |
| **割付の候補と理由** | **`M33-02` の直前**（原稿 1 と同じ手番で片付く） |

#### 原稿 3（新規登録）

| フィールド | 内容 |
|---|---|
| **ID（スラッグ）** | `supp001-s27-migration-count-as-built` |
| **発生元** | `M33-01`（完了報告 §1.5 ／ 本レポート §4-4） |
| **未解消の理由** | **`SUPP-001` §2.7 の「`migrations/*.up.sql` は 102 本、`000001`〜`000103`」が失効している**（実測 111 本・〜`000112`）**。★製造は設計書を直接編集しない**（`CLAUDE.md` §8） |
| **再開に必要な条件** | **設計卓が as-built 化すること。★欠番が `000012` の 1 件だけである点は今日も成立しており、そこは変えなくてよい** |
| **記録日・状態** | 2026-09-13 ／ **未着手** |
| **割付の候補と理由** | **設計卓の手番でいつでも可**。`M33` の進行を待たない（本数は今後も増えるが、`M33-02` で 7 本へ潰れるため、**潰した後に一度で直すのが安い**という判断もありうる） |

---

## §5 参考（触れていない＝不変の証跡）

- `git diff --stat 5576126 -- migrations/` が空（**`migrations/` の差分 0 バイト**）
- `git diff --numstat 5576126` は `docs/progress/` の 3 ファイルのみ・**deletions 全て 0**（教訓 `E-225` の観点）
- `internal/` `cmd/` `web/` の差分 0（コード・テストを 1 バイトも触っていない）
- `go test ./...` ＝ **ok 60 / no test files 9 / 全 69 パッケージ / FAIL 0**（exit 0）
- `bash scripts/check-artifact-integrity.sh` 違反なし（**1 本目に実行**）
- `bash scripts/check-migration-license.sh` 違反なし（exit 0）
- `bash scripts/check-progress-log-index.sh` 違反なし（Phase D の追記後）
- `bash scripts/check-md-emphasis.sh <本サブの新規 2 ファイル>` 検出 0 行
- `bash scripts/check-stop-discipline.sh` 違反なし
- `git log 5576126..HEAD -- docs/handover/followup-backlog.md` が 0 件（`D-838` 準拠）
- `migrations/legacy/` は存在しない（`M33-03` の射程を侵していない）
- CHANGE 0 本・マイグレ 0 本・自採番なし（`D-293`）
- `scratch-m33-01.db` は `.gitignore:30` の `*.db`（`git status --porcelain --untracked-files=all` に現れない）

---

## §6 CHANGE 起票のたたき台（設計担当向けチェックリスト）

**★本サブの CHANGE 消費は 0 本・マイグレ消費は 0 本である**（自採番なし）。**⇒ 下記は「親が起票を検討する候補」であり、製造が番号を取ったものではない。**

| # | 対象文書 | 変更の中身 | 前提 |
|---|---|---|---|
| 1 | **`M33-overview` §0.5.3** | **7 群の層の割当**（`S02` / `S06` が層をまたぐ件の裁定結果を反映） | **§4-1 の裁定が先** |
| 2 | **`M33-overview`**（廃止 7 本の記述） | **`000009` を加えて 8 本にするか**。あわせて「`sqlite_sequence` の 3 は再現が要る」の注記 | **§4-3 の判定が先** |
| 3 | **`SUPP-001` §2.7** | **本数 102 → 111 ／ 範囲 `000103` → `000112` の as-built 化**（**★欠番 `000012` 1 件は据置き**） | 単独で可（§4-4） |
| 4 | **`SUPP-001` §5.5.4** | **規約追加＝「スキーマの同一性を `sqlite_master.sql` のテキストで固定しない。`pragma` 経由の正規化投影を、取り方のスクリプトと対で引き継ぐ」**（文面のたたき台は §1-1） | 単独で可 |
| 5 | **`M33-overview` §4.4-4 ／ §2**（危険の記述） | **`Migrate`/`Steps` の 28 → 35/38/40 ／ golden の 4 本 → 17 stem** への更新 | 単独で可（§4-5） |

**★マイグレ連番は 1 本も消費していない。⇒ ボード §2.2 の「次に払い出す番号 ＝ `000113`」は動かない**（`ls migrations/` の実測最大 `000112` と整合）。

**★番号は起票時に registry で採番すること。**

---

## §7 教訓（retrospective 行き）

> **★親は本節を読まなくてよい。** 宛先は `retrospective-log` へのバッチ反映であり、実施者は設計担当（反映担当は 2026-08-11 に廃止）。

### 7-1 ★★★件数を数える対象は、`tail` / `head` を通していない全出力であること

**実際に踏んだ**——`go test ./...` をバックグラウンドで **`2>&1 | tail -40`** の形で回し、保存された出力が末尾 40 行しか無いのに、そこから `grep -c '^ok '` して「**ok 37**」と報告した。**実測は ok 60 / no test files 9 / 全 69 である。**

**★しかも「実測」ラベルを付けていた。** 本サブは「成果は数である」と自ら宣言した回であり、**評価軸そのものに触れる誤りだった。**

**一般化＝出力を切る指定（`tail` / `head` / `-n`）と、その出力を母集団にする集計は同居させない。** 切るのは人が読むためであり、数えるためではない。**⇒ 集計する予定がある実行は、全出力をファイルへ落としてから数える。**

### 7-2 ★★★「まだやっていないこと」を完了形で書かない（**`D-510` と同型の再発**）

**完了報告 §7 の常設項目「■ 併せて更新が要るもの」に、`progress-log.md` への索引行を「**追記済み**（Phase D）」と書いた。⇒ 執筆時点で未実施であり、`check-progress-log-index.sh` が現に違反 1 件を返していた。**

**★`D-510`（`M23-04`）とまったく同型である**——あちらは「レビュー『高』指摘の不採用は 0 件」をレビュー実施前に断定した。**製造 CLI は `<!-- COMPLETION-REPORT-WRITE-TIMING -->` でこの型を名指しで戒めており、私はその節を読んだうえで別の欄で同じことをした。**

**一般化＝戒めが効くのは「名指しされた欄」だけである。** `D-510` の対処は「レビュー結果を参照する欄」に限定して書かれていたため、**「Phase D の結果を参照する欄」には効かなかった。**
**⇒ 対処を書くときは、事故った欄ではなく*事故の型*（＝自分の後工程の結果を先に書く）で書くこと。**
**⇒ 実務的な歯止め＝「済」「完了」「追記済み」と書いた欄は、その場で検査を回して緑を確かめるか、書かない。**

### 7-3 ★★期待値が「集合」になる突合は、集合が広がるほど素通しする

**実際に踏んだ**——層の交差検証で「`S01`/`S07` → A、`S02`..`S06` → B として期待値を作り、実測層と比較する」と実装した。**`S01,S03` のような多群行は期待値が `{A, B}` になり、実測層が何であっても通る。⇒ 食い違い 3 件と報告したが、厳密条件では 5 行だった**（レビュー 高-4 が発見）。

**★取り落とした 2 行が、本サブで最も重い事実だった**（`000018` / `000074` ＝凍結表の据置きだけで緑になっている 2 本）。

**一般化＝「期待値の集合に実測が含まれるか」で書くと、集合が広がるほど検査が緩む。⇒ 「B であるべき条件が 1 つでも成り立つなら B でなければならない」という*必要条件*の形で書くこと。**

### 7-4 ★★ハッシュは「レシピ」と対でしか意味を持たない

**正規化ハッシュを算出したところ、レビュー担当が独立に算出した値と違った**（`9b87a96e…` ／ `8a86d298…`）**。★どちらも間違っていない——連結の書式が違うだけである。**

**一般化＝「後続サブが突き合わせるハッシュ」を残すときは、値ではなく*算出スクリプトそのもの*を成果物にする。** 値だけを申し送ると、受け取った側が独自の書式で算出して食い違い、**「実装が間違っている」と誤診する。**

### 7-5 ★調査資料の数は、式と基準日を伴わないと再現できない

**調査資料の「28 ファイル」は、式が `internal/infra/migration/*.go` に*ディレクトリ限定*であることを本文に書いていなかった**（`research.py:151` を読んで初めて分かる）**。⇒ 「今日は 35」と「repo 全体なら 40」のどちらが同じ数え方なのかを、読む側が判定できない。**

**一般化＝件数を資料へ書くときは「式・範囲・基準日」を同じ行に置く。** 本サブはこれを守り、`35 / 38 / 40` を三段で出して差分ファイル名まで載せた。**⇒ レビュー担当が 20 式以上を再実行して一致を確認できたのは、この形にしたためである。**

---

*以上、`M33-01` 設計伝達レポート。* **★★裁定が要るのは §4-1（＋ §4-2）である。⇒ `M33-02` の着手前。** **★§4-3 / §4-4 は判定のみで足りる。** **★本サブは実装差分 0 であり、実機確認は不要である。**
