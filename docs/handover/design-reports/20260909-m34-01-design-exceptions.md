# M34-01 設計伝達レポート（例外レポート）

| 項目 | 内容 |
|------|------|
| 対象 | **親チャット（設計卓）** |
| 発信 | 製造担当 Claude Code ／ 2026-09-09 |
| 指示書 | `docs/instructions/M34-01-desktop-port-and-os-split.md` v1.0.0（上位＝`M34-overview` v1.3.0） |
| 実装コミット | ブランチ `claude/m34-01-autopilot-combomgr-readonly-rm8mpk` ／ **9 コミット**（`026aa32` → `bd8d4f9`）。**push 済み。⇒ HEAD で読める** |
| 関連 | 完了報告 `docs/progress/M34-01-completion-report.md` ／ レビュー `docs/progress/m34-01-review.md` |
| 作成条件 | 実装直後の同一セッション内。**開発者の動作確認結果も反映済み** |

本レポートは **①独自確定仕様 ②契約違反の独自判断 ③製造判断 ④残課題** に絞る。指示書どおりに実施した部分（実査 4 件の実施・移植・ビルドタグでの OS 分離）は割愛する。

**★最重要は §1-1（新しい面の新設と公開 API）と §2-1（射程外の変更を含むこと）である。** 前者は `SUPP-001` の反映が要り、後者はブランチの射程宣言に関わる。

---

## §1 製造が独自に確定した実装仕様（DES 反映が要るもの）

### 1-1 ★★`internal/desktop/` は `internal/` の既存 5 層のどれでもない新しい面である

指示書 §2.2-1 が「命名は実装時に確定してよいが、既存の層のどれでもない新しい面であることを報告に書くこと」と求めていた件。**`internal/desktop/` で確定した。**

**★設計卓は実装ソースを読めないため、公開 API を逐語で置く。** 参照だけを書くと反映できない。

| 種別 | シンボル | 意味 |
|---|---|---|
| 型 | `TrayMenu{ Title, Tooltip string; Items []TrayItem }` | 通知領域アイコンと右クリックメニューの記述 |
| 型 | `TrayItem{ Label string; OnClick func(); Default bool }` | メニュー 1 項目。`Default` は左ダブルクリックでも走る項目 |
| 変数 | `ErrUnsupported` | 非 Windows で `RunTray` が返す。**呼び手は「トレイ無しで動作を続ける」と扱う契約であり、致命エラーにしない** |
| 関数 | `RunTray(TrayMenu) error` | メッセージループに入る（ブロックする） |
| 関数 | `StopTray()` | 上記を終わらせ、アイコンを削除する |
| 関数 | `OpenURL(string) error` | 既定ブラウザで開く。`ValidateURL` を内部で通す |
| 関数 | `OpenFolder(string) error` | ファイルマネージャで開く。**開く前に `os.Stat` する**（起動して待たないため、不正パスが無言で失敗するのを防ぐ） |
| 関数 | `ValidateURL(string) error` | **http/https 以外とホスト無しを拒む。** 設定由来の文字列が任意のプロトコル起動に化けるのを防ぐ |
| 関数 | `Alert(title, text string)` | Windows は `MessageBoxW`、他 OS は stderr |
| 関数 | `Confirm(title, text string) bool` | Windows は `MessageBoxW`(YesNo)、**他 OS は常に false** |

**★持たせなかったもの（読む側が誤読しないために書く）**——バルーン通知 ／ アイコンのアニメーション ／ 二重起動判別 ／ スタートアップ登録 ／ Windows サービス化は**いずれも無い**。二重起動判別は `M34-02` の射程である。

**★ビルド制約の形**（本リポジトリで**初めての GOOS ファイル名制約**。着手前は `*_windows.go` が 0 件で、OS 分岐は全て `runtime.GOOS` の実行時 switch だった）:

| ファイル | 制約 |
|---|---|
| `desktop.go` / `desktop_test.go` | 無し（全 GOOS。純粋関数のみを置き、Linux でテストできる形にした） |
| `desktop_windows.go` / `tray_windows.go` | `//go:build windows` |
| `desktop_other.go` | `//go:build !windows` |
| `tray_sizes_windows_amd64.go` | ファイル名制約（windows かつ amd64）。構造体サイズのコンパイル時ガード |

**⇒ `SUPP-001` §5.2（Go 側ディレクトリ構成）へ `internal/desktop/`（デスクトップ統合。Windows 専用実装＋他 OS スタブ）と `cmd/genicon/`（アイコン生成器。開発時のみ）を明文化してほしい。** 先例＝`internal/recipehash/` を §5.2 へ足した `CHANGE-132`。**★`cmd/` は現状 `tacpendium` しか載っていない**（`seedgen` も未記載）。

### 1-2 ★★アイコン生成器の置き場を `cmd/` にした（`scripts/` ではない）

移植元は `scripts/genicon/` に置いていたが、**`REUSE.toml` の層 C は `scripts/**` を含む。⇒ そのまま置くと移植物が `MIT` になり、層 A（`AGPL-3.0-or-later`）であるべきものが崩れる。**

`cmd/**` は層 C のどの列にも当たらず既定の `path = "**"` にのみ一致するため、層 A に落ちる。**`REUSE.toml` は 1 行も変えていない**（`M34-overview` §3.2.3 の要求どおり）。

**⇒ 反映は不要だが、`M34-02` が `scripts/build-windows.ps1` を移植するときに同じ判断が要る**（§4-2）。

### 1-3 ★★`M34-overview` §5.1.7 と指示書 §2.4 の実測値が陳腐化していた

| 対象 | 設計卓の記載 | **実測値** |
|---|---|---|
| DB | `%APPDATA%/combomgr/combomgr.db` | **`%APPDATA%/tacpendium/tacpendium.db`**（`internal/infra/db/db.go:191`） |
| ログ | `logs/combomgr.log` | **`logs/tacpendium.log`**（`internal/config/config.go:57`） |

`M28-01` の改名を反映し損ねたものである。**設計書間の矛盾ではないため止めずに進めた**（`CLAUDE.md` §8 の停止条件に当たらない）。

**⇒ `M34-overview` §5.1.7 と `M34-01` §2.4 の表を `tacpendium` へ直してほしい。★「DB とログは別の場所である」という結論そのものは変わらない。**

### 1-4 ★★`M34-overview` §5.1.6 の候補 2 件へ、実査から回答できる

| 項目 | 回答 |
|---|---|
| **5「DB のフォルダを開く」** | **現状では意味がある。** DB は `%APPDATA%/tacpendium/`（絶対）、ログは CWD 配下であり重ならない。**★ただし §4-3 の是正で案 B（ログも `%APPDATA%` へ）を採ると重なる。⇒ そのときは項目 1 つで足りる** |
| **6「exe のフォルダを開く」** | **現状ではログフォルダの親ではない。** ログの基準はプロセスのカレントディレクトリであり、exe の位置とは無関係である。**⇒ §5.1.6 が「親ではないと分かったら、そのとき足すこと」と書いた条件に当たる** |

---

## §2 契約・設計に反する独自判断

### 2-1 ★★ブランチが `M34-01` の射程外の変更を 1 件含む（開発者裁定による）

**何に反したか**——指示書ヘッダ「前提サブ」欄の射程宣言（**触るのは `cmd/` と `internal/desktop/`〔新設〕とビルド設定である**）。

**何をしたか**——`internal/infra/migration/migrate_m1403f_test.go` と `migrate_m2703_test.go` へ `gofmt -w` を当てた（コミット `d51ecd3`。**独立コミットに分けてある**）。

**なぜ**——両ファイルは Go 1.19 以降の doc コメント整形規則に非適合で、**着手基点 `026aa32` の時点から `pr-checks.yml` の `gofmt` ステップが赤だった**（同ステップは出力が非空なら明示的に落とす実装）。最終更新は `a36df63`＝`M27-03`。放置すると赤が既定になり、**本物の `gofmt` 違反が混ざっても気づけなくなる**（`M24-08` が 27 ファイルを静かに規約外にしたのと同型）。

**★製造の独断ではない。2026-09-09 に開発者へ選択肢を提示し「このブランチで直す」と裁定を得た**（§3-1）。**⇒ 親の裁定が要るのは「この差分を `M34-01` の成果として扱ってよいか」であり、実装の是非ではない。**

変更は機械的整形のみで、**コード行の差分は 0 件**（`git diff` からコメント行を除くと空になることを確認）。`M27-03` の設計判断には触れていない。

---

## §3 製造の判断

### 3-1 開発者へ確認して確定した点

| # | 事項 | 確定 |
|---|---|---|
| 1 | **ログ・設定の相対パス基準の是正をどこでやるか** | **`M34-01` は実測と提案まで。実装は `M34-02`**（「ログフォルダを開く」と同時）。指示書 §2.4-3 が自己判断を許していたが、**全 OS の既存挙動が変わるため開発者へ返した** |
| 2 | **アイコン生成器も移植するか** | **移植する。** 開発者から「ライセンス的に大丈夫か」と確認を受け、根拠 4 点（著作権者同一 ／ 第三者断片 0 件の実査 ／ アイコンは Go コードで描いた自作物 ／ `cmd/` なら層 A のまま）を提示して承認を得た |
| 3 | **Windows 専用コードのコンパイル検査をどこに置くか** | **`Makefile` の `check-windows` ＋ `pr-checks.yml` の両方。** CI 分数への影響を懸念材料として提示した上での判断 |
| 4 | **着手前から赤い `gofmt` をどうするか** | **このブランチで直す**（§2-1） |

### 3-2 推測で進めた点

| # | 推測 | 確認方法 |
|---|---|---|
| 1 | **ウィンドウクラス名を `MovestoolTrayWindow` → `TacpendiumTrayWindow` へ改名した。** 移植元 `INTAKE.md` は「移植時に変えるのは `TrayMenu` の文言と `-ldflags` と no-arg 分岐だけ」と書いて改名に触れていないが、**ウィンドウクラス名は同一 OS 上で他アプリと衝突しうる識別子**である | `M34-02` の実機確認でトレイが出れば足りる |
| 2 | **アイコンの絵柄は移植元のものをそのまま持ち込んだ。** `INTAKE.md` は「本体では差し替える」としているが、**差し替えはトレイが実際に出る `M34-02` の判断**であり、本サブでは `//go:embed` がコンパイルできることだけが要件である | `cmd/genicon/main.go` を書き換えて再生成すればよい |

---

## §4 設計担当が未把握の残課題・申し送り

**★`followup-backlog.md` の本表は製造が書けない（`D-382`）。⇒ 設計卓が畳めるよう、スラッグ・内容・根拠・割付候補・新規/更新の別を揃えて置く。**

### 4-1 `browser-launch-duplication`（新規登録）

- **何が起きるか**: 既定ブラウザを開く経路が 2 本になった。既存の `github.com/pkg/browser`（BSD-2-Clause・`go.mod` の direct 依存）を `cmd/tacpendium/main.go:637` の `launchBrowser` が使う一方、新設の `desktop.OpenURL` は `rundll32` を自前で叩く別実装である。
- **根拠**: `cmd/tacpendium/main.go:637` ／ `internal/desktop/desktop.go` の `browserCommand`。
- **★重要**: 移植元のコメントが挙げる「`cmd /c start` だと黒窓が一瞬出る」という選定理由は、**本体では自前実装を正当化しない**——`pkg/browser` の Windows 実装は `ShellExecute` であって黒窓を出さない。**移植元が外部依存 0 本のリポジトリだったという前提が効いている。**
- **割付候補**: `M34-02`（配線を作る手番でどちらかへ寄せる）。**両方残すと `CLAUDE.md` §6「重複機能ライブラリは導入しない」から外れる。**

### 4-2 `porting-target-layer-c-collision`（新規登録）

- **何が起きるか**: `scripts/**` は `REUSE.toml` の層 C（`MIT`）である。**移植物をそのまま `scripts/` へ置くと層 A であるべきものが `MIT` になる。**
- **根拠**: `REUSE.toml` 表(2) の `path` に `"scripts/**"` がある。本サブは同じ罠を `cmd/genicon/` への配置で回避した（§1-2）。
- **割付候補**: `M34-02`（`scripts/build-windows.ps1` の移植時に同じ判断が要る）。

### 4-3 `log-path-cwd-relative`（新規登録）

- **何が起きるか**: `config.toml` と `logs/tacpendium.log` が**プロセスのカレントディレクトリ基準**で解決される。`os.Executable()` はリポジトリ全体で **0 件**。常駐化で起動経路が増える（ダブルクリック ／ ショートカットの「作業フォルダ」 ／ 将来のスタートアップ登録）と出先が動く。
- **★指示書が想定した以上に重い帰結が 1 件ある**: 「ログフォルダを開く」が壊れるだけでなく、**`config.toml` も見つからなくなる。** `internal/config/config.go:147` は**不在をエラーにせず既定値で続行する**ため、**利用者の設定（ポート・LAN 共有モード・パスワード）が黙って無視される。★黒窓が無ければ気づく手掛かりも無い。**
- **根拠**: `internal/config/config.go:57` / `:147` / `:389-412` / `:425-437` ／ `cmd/tacpendium/main.go:86-99` ／ `internal/infra/log/log.go:24-49`。
- **割付候補**: `M34-02`（開発者裁定・§3-1）。案 3 つと影響範囲は完了報告 §4.4。

### 4-4 `startup-notice-text-stale-on-tray`（新規登録）

- **何が起きるか**: `printStartupNotice`（`cmd/tacpendium/main.go:610-631`）の末尾が「終了するにはこのウィンドウを閉じてください。」であり、**常駐化すると文面が失効する。** 同関数と `launchBrowser` は `WebEmbedded` でガードされており、タグ無しビルドではそもそも走らない。
- **割付候補**: `M34-02`。

### 4-5 `cli-arg-parsing-is-a-spec-change`（新規登録）

- **何が起きるか**: `cmd/tacpendium` は現在 `flag` も `os.Args` も持たず、**余分な引数を黙って無視する。** `M34-02` が「引数なし＝常駐モード」のために `flag.Parse()` を入れると、**それまで無視されていた文字列が意味を持つ**（仕様変更）。
- **根拠**: `cmd/tacpendium/main.go:247-252`。
- **割付候補**: `M34-02`。

### 4-6 `clean-clone-missing-toolchain-deps`（**既存行の更新**）

- **更新後の状態**: **クラウド実行環境（Claude Code on the web）に `markdown-it-py` が入っていない**事例を追加。素の状態では **1 本目に回す `check-artifact-integrity.sh` が赤**になり（`check-md-emphasis.sh --self-test` が「未実行」を返すため）、**`D-775` の閉じない強調チェックも回せない**。`pip install markdown-it-py` で解消した。
- **★既存スラッグと同型**（clean clone に道具が足りない）。新規スラッグを起こすか既存へ足すかは設計卓の判断。

### 4-7 レビュー低指摘の繰越

**繰越は 1 件のみ**（低-5・**不採用**）。`make test` から `check-windows` を呼ぶ案。**同ターゲットは `test-go` と `test-web` の合成であり「テストを走らせる」入口である。⇒ クロス GOOS のビルド検査を混ぜるとターゲットの意味が変わる。** レビュー自身も「実行時間との兼ね合いがあるため必須にはしない」としていた。門は `pr-checks.yml`（自動・全 PR）に置いた。**★他の 13 件は採用済み。「高」の不採用は 0 件である。**

---

## §5 参考（触れていない＝不変の証跡）

- `git diff 026aa32 -- go.mod go.sum` → **0 行**（新規依存 0 件。`golang.org/x/sys` は indirect のまま据え置き）
- `git diff 026aa32 -- REUSE.toml` → **0 行**（層 A へ既定で落ちることを確認）
- `git diff 026aa32 -- scripts/public-snapshot-manifest.txt` → **0 行**（`ALLOW cmd/**` と `ALLOW internal/**` が既存）
- `git diff 026aa32 -- docs/design/` → **0 行**（CHANGE 消費 0 本）
- `git diff 026aa32 -- migrations/` → **0 行**（マイグレ消費 0 本）
- `determineBindHost` / `determineBindAddr`（`cmd/tacpendium/main.go:675-690`）→ 無改変（待ち受けの `127.0.0.1` 固定はしていない）
- `Makefile` の `build-windows` → 無改変（`-H=windowsgui` は付けていない）
- `cmd/` から `internal/desktop` の import → **0 件**（トレイは結線していない。⇒ **Go のリンカは同パッケージを配布 exe に含めない**）
- `go run ./cmd/genicon` と `internal/desktop/icon.ico` の `cmp` → バイト同一

---

## §6 CHANGE 起票のたたき台（設計担当向けチェックリスト）

- [ ] **`SUPP-001` §5.2** へ `internal/desktop/`（デスクトップ統合。Windows 専用実装＋他 OS スタブ。公開 API は §1-1）と `cmd/genicon/`（開発時のみの生成器）を追記。**★`cmd/seedgen` も未記載なので同時に拾えるか確認**
- [ ] **`SUPP-001` のビルド規約**（または `DES-001` 配布方針）へ **`make check-windows`** を明文化。**★理由も併記してほしい**——PR CI は Linux のみで回るため、これが無いと Windows 専用ファイルは PR で 1 度もコンパイルされない
- [ ] **`M34-overview` §5.1.7 と `M34-01` §2.4** の実測値を `combomgr` → `tacpendium` へ是正（§1-3）
- [ ] **`M34-overview` §5.1.6** の候補 2 件へ §1-4 の回答を反映
- [ ] **`DES-002`** への反映は**不要**（HTTP 経路の新設・変更は 0 件。待ち受けにも触れていない）
- [ ] **マイグレ連番の消費は 0 本。⇒ ボード §2.2 の「次に払い出す番号」は動かない**
- [ ] **CHANGE 番号は起票時に registry で採番**（本サブは 0 本消費のため未払い出し）
- [ ] **★`M34-02` の起票前に §4-3（ログの相対パス）を読むこと。** 「ログフォルダを開く」はログの居場所が一意でないと作れない

---

## §7 教訓（`retrospective-log` 行き）

**★親チャットは本節を読まなくてよい。** 宛先は `retrospective-log` へのバッチ反映であり、実施者は設計担当である。

1. **移植は「移植元の記述が本体では失効する」型の欠陥が最も出やすい面である。** 本サブのレビュー「高」4 件は**全てこの型**だった（`--addr` 参照 ／ workspace JSON と CSV の置き場 ／ 撤回済みの「データフォルダ」 ／ アイコンの「UI と同じアクセント色」）。**動作は正しく、テストも lint も型検査も緑のまま通る。⇒ 人が読む以外に検出経路が無い。** **どう防ぐか＝移植を含むサブでは、レビュアーへ渡す観点にこの型を名指しで書く**（本サブは実際に書いたので 4 件とも捕まった）。
2. **検査を足したら陽性対照まで測る**（教訓 `E-84` の再確認）。本サブは `tray_sizes_windows_amd64.go` のサイズガードをわざと崩し、`make check-windows` が赤・`go build ./...`（Linux）が緑のままであることを実測した。**⇒ 「門を置いた」は「門が効く」の証拠にならない。**
3. **★「リポジトリ全体を grep して 0 件」は母数を書かないと再現できない。** 本サブのライセンス実査は追跡ファイルに対して 0 件だったが、開発者がローカルで `find` を実行すると `node_modules/` 配下が多数ヒットし、**報告が誤りに見えた**（実際は git 管理外かつ別プロジェクトの依存で母数外）。**どう防ぐか＝実査の記録には母数（`git ls-files` か作業ツリー全体か）と再現コマンドを書く。** `find` / `grep -r` は未追跡物を拾うため再現しない。
4. **★レビュー指摘の取り込みは、1 件の指摘が複数箇所を名指ししていないか確認する。** 本サブは「高-4」が `cmd/genicon` の**パッケージ doc と色定義コメントの両方**を挙げていたのに、doc 側だけを直して色定義側を落とした（開発者の指摘で発覚）。**どう防ぐか＝指摘文中のファイル名・シンボル名を全数拾ってから着手する。**

---

*以上、`M34-01` 設計伝達レポート。*
