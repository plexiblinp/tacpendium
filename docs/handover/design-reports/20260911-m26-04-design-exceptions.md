# 設計伝達レポート: M26-04（セキュリティ実査と公開前スキャン／`A6` / `A9` / `A7`）

| 項目 | 内容 |
|------|------|
| 作業ID | M26-04 |
| 対象指示書 | `docs/instructions/M26-04-security-survey-and-prerelease-scan.md` **v1.1.0** ／ チェックリスト `docs/instructions/reviews/M26-04-review-checklist.md` v1.0.0 |
| CHANGE | **未起票。★製造は番号を消費していない**＝`D-293`。**請求 1 本のたたき台は §6。宛先は `SUPP-001` §5.8 と `DES-002` §8** |
| マイグレ | **消費 0 本**。ボード §2.2 の「次に払い出す番号」は動かない |
| 作成日 | 2026-09-11 |
| 実装コミット | `claude/lucid-thompson-fv1hlc`・`2e35bbd..f08c0ba`（**4 コミット**）。差分＝**5 ファイル / +1006 / −2**。**★push 済み**（`claude/` 名前空間） |
| 源泉 | 完了報告 `docs/progress/M26-04-completion-report.md` ／ レビュー `docs/progress/m26-04-review.md`（**高 3 / 中 5 / 低 4・全件採用・高の不採用 0 件・再レビュー往復 0 回**）／ **開発者裁定 2 回**（2026-09-11・Plan Mode で 4 点 ＋ 外部確認の回答）／ 実装直後の同一セッションで生成 |
| 宛先 | 設計卓（親チャット） |

**本レポートは ①独自確定仕様 ②契約違反の独自判断 ③製造判断 ④残課題 に絞る。指示書どおりの部分は割愛する。**

**★★★最重要は §2-1（指示書の完了条件 3 と 5 に不達があり、受理/差し戻しの裁定が要る）と §1-1（`A6` の as-built が `DES-002` §8 と食い違っており、`SUPP-001:1495` は失効している）である。**

> **★★設計卓は `docs/progress/` を読まない**＝2026-08-11 開発者裁定①。**したがって本レポートは参照だけを書かず、反映に要る中身をここへ埋めてある。**

---

## 1. 独自確定仕様（設計書に無い／設計書より細かい決めごと）

### ★★★§1-1 `A6` の as-built — **設計書 3 か所と食い違っている**

**`D-653` が「設計卓は実装を読めないため断定していない」とした 3 件を、実物の行で判定した。**

| # | 設計書の記述 | as-built | 判定 |
|---|---|---|---|
| **(a)** | `DES-002:1160`「**パスワード保護を強く推奨します**」と警告し、パスワード設定へ誘導する | **警告と誘導は在る**（`web/src/features/config/LanModeConfirmDialog.tsx:60-71` の `step === "warn"` 分岐 ／ 誘導ボタン `:78-80` `lan-confirm-set-password`）。**★ただし「強く推奨します」という文字列は実装に 0 件**（`grep -rn "強く推奨" internal/ cmd/ web/src/` の実測）。実装の最強の表現は `web/src/locales/ja.json:651` の **「パスワードを決める（おすすめ）」** | **機能は充足・文言は不一致** |
| **(b)** | `DES-002:1161`「明示的な同意ダイアログ」／ `:118`「同意は明示的な操作で得る（チェックボックス等。押すだけで通さない）」 | **在る。** 同 `LanModeConfirmDialog.tsx:97-127` の `step === "consent"`、`:109-113` の `Checkbox`（`lan-consent-check`）、**`:118-121` の `disabled={!consented}`**。`:50-55` で開き直すたびに同意をリセット | **充足** |
| **(c)** | `SUPP-001:1495`「LAN モードの API 無認証一般の是正（`Security.PasswordEnabled` 実配線）は本制約の対象外」 | **★前半が失効している。** `PasswordEnabled` は **M22-01 で配線済み**——`internal/service/auth/service.go:74`（`return sec.PasswordEnabled && sec.PasswordHash != ""`）／ `internal/api/middleware/auth.go:62-64`（`if !validator.Enabled() { return next(c) }`）／ `cmd/tacpendium/main.go:470`（`e.Use(mw.Auth(authService))` のグローバル登録） | **失効** |

**★★★(c) の判定は「過去記録か現行規定か」の二択では表せない。** `:1495` は**前半（実配線が未了という含意）が失効し、後半（LAN で無認証の経路が既定で残る）は事実として現存する**行である。

**★後半が現存する根拠**: **`Server.Mode` と `Security.PasswordEnabled` を突き合わせるコードは実装に 0 件**（`grep -rn 'Mode == "lan"' internal/ cmd/` の全ヒットは bind ／ CORS ／ LAN IP 解決 ／ 起動案内のみ）。既定は `internal/config/config.go:131` の `PasswordEnabled: false`。**⇒ LAN でも既定なら `/api/*` は無認証で通る。**

**★★ただしこれは事故ではない。** `DES-002:1225`（§8.1 項 16）逐語＝「**`password_enabled` は LAN 共有モードの前提条件にしない**（「必須化はしない」の帰結）」。**⇒ 設計が明示的に選んだ状態である。**

**⇒ 反映依頼（3 件）**:

1. **`SUPP-001` §5.8 の `:1495` を書き直してほしい。** 前半の失効を消し、後半を「`DES-002:1225` が選んだ設計であって未了の是正ではない」と書き分けること。**★この 2 つが 1 行に同居しているため、読む人が「未了の是正が残っている」と受け取り、公開を止める判断になりうる。**
2. **`DES-002` §8 へ as-built 注記を足してほしい**——**警告と同意ダイアログは設定画面経路にしか無い**（§1-2）。
3. **`DES-002:1160` の文言指定をどうするか裁定してほしい**（§3-2 の推測を参照）。

### ★★§1-2 `A6` の不足 — **警告と同意は「設定画面経路」にしか無い**

**指示書 §0.1 が「見つけた不足を先に全部並べること」を求めているため、直さずに並べた。**

**`DES-002:1160` が求めるのは 2 要素**（「警告する」と「パスワード設定へ誘導する」）**であり、`:1161` の同意ダイアログを足すと 3 要素になる。⇒ 経路ごとに割ると次のとおり。**

| 経路 | ① 警告 | ② パスワード設定への誘導 | ③ 同意ダイアログ |
|---|---|---|---|
| **設定画面**（`web/src/features/config/SettingsSectionNetwork.tsx`） | **在る** | **在る** | **在る** |
| **初回起動ウィザード**（`web/src/features/wizard/Step05Network.tsx`・全 44 行） | **無い** | **★在る** | **無い** |

**★「完全に無防備」ではない。** `web/src/pages/WizardPage.tsx:116-117` が `lanEnabled` のとき `Step07Password` を段として挿入する（`web/src/features/wizard/Step07Password.tsx:13` の逐語コメント＝「LAN 共有を有効にしたときだけ出す（`DES-005` §5.1 の『任意、LAN共有有効時のみ表示』）」）。**⇒ 欠けるのは ① 警告と ③ 同意ダイアログの 2 つである。**

**★`LanModeConfirmDialog` の参照元は実測で 1 本だけ**（`SettingsSectionNetwork.tsx:8` の import と `:123` の描画）。

**★設計書側は経路を限定していない**——`DES-002:101`（「LAN共有モード有効化時には画面上で警告を表示し」）／ `:1160` ／ `:1161` のいずれも「設定画面で」とは書いていない。

**⇒ 裁定が要る（§2-2 で二択として挙げる）。**

### §1-3 API 経路 — **該当なし**

**本サブは API を 1 本も新設・変更していない。** コード差分は 0 行（`go.mod` / `go.sum` / `web/` に差分なし）。**⇒ `DES-002` §4.2 の経路表への反映は不要。**

---

## 2. 契約・設計に反する独自判断（★親の裁定が要る）

### ★★★§2-1 指示書の完了条件に **2 件の不達**がある — **受理/差し戻しの裁定をお願いしたい**

**どちらも開発者裁定または環境制約によるものであり、製造が独断で外したものではない。★ただし「`M26-04` を完了として受理してよいか」は親の裁定事項である。**

| # | 完了条件 | 状態 | 理由 |
|---|---|---|---|
| **1** | **★3**「静的解析・SCANOSS・シークレット走査の **3 つを回し**、結果が示されている」 | **★2/3 のみ。SCANOSS は未実施** | **開発者裁定（2026-09-11）＝「止まって手順書のみ」。** SCANOSS CLI は既定で fingerprint を `api.scanoss.com` へ送るため、**指示書 §3-5「本リポジトリの内容を外部サービスへ渡す」（やらないこと）と抵触しうる。** `D-671`「迷ったら止まる側に倒す」に従い製造が止め、開発者が追認した。**⇒ 手順書は完了報告 §4.2。実行は開発者の手番** |
| **2** | **★★5**「走査の母集団に、許可リストの除外分も入っていることが示されている」＋ `secret-scan-before-release` の **(b) 全履歴** | **★(a) は完了・(b) は未完了** | **★★クラウド実行環境は浅いクローンである**（`.git/shallow` 実在・到達可能な最古は **2026-09-02**・435 revs。`gitleaks` が走査したのは 367 コミット）。**⇒ 2026-09-02 より前の履歴に原理的に到達できない。** (a) 現在のツリーは**追跡 2983 件（公開 2954 ＋ 除外 29）を全数走査済み**であり、母集団の条件は満たしている |

**★★★これは `M26` 全体に効く制約である。** **クラウド実行で回す限り、どのサブも全履歴に到達できない。⇒ 「公開前に全履歴を走査した」という状態は、開発者のローカル（full clone）でしか作れない。** 手順書は完了報告 §3.5。

**★静的解析にも 1 件の実行不可がある**（不達には数えていない。指示書は道具を指定していないため）——**`golangci-lint` はクラウド環境で実行不可**。`/usr/local/bin/golangci-lint` は v2.5.0（go1.25.1 ビルド）であり、逐語＝`the Go language version (go1.25) used to build golangci-lint is lower than the targeted Go version (1.26.8)`。**`D-659` の「到達できなければ『実行不可』と書いて止まり、回避策を独自に作らせない」に従い、`go` ディレクティブを下げる等の回避はしていない。**

### ★★§2-2 `A6` の不足を **直さずに返した** — 二択のどちらかへ倒してほしい

§1-2 の不足について、**指示書 §0.1**（「実査して記録するサブであり、直すサブではない」「直す前に、何が無いかを先に全部並べること」）**に従い、直していない。**

**⇒ 親の裁定が要る。★どちらにも倒れないまま残さないこと。**

| 選択 | 帰結 |
|---|---|
| **(A) 実装を直す** | **別サブ。** フロント実装 ＋ Vitest ＋ E2E が要り、`M26-04` の性格と規模が変わる |
| **(B) 直さない** | **★`DES-002` §8 へ as-built 注記の CHANGE を請求する必要がある**（§6-2）。`:101` / `:1160` / `:1161` は経路を限定していないため、**放置すると設計書が as-built と食い違ったまま残る** |

**★指示書ヘッダの「`DES-002` §8 の as-built 追記が要る可能性」はこれを指している。**

### ★★§2-3 指示書に無い道具を 2 本導入した（**開発者承認済み**）

**`D-659` は「新しい実行ファイルの導入は報告して止まる」と定めており、製造は止めて承認を得た。⇒ 独断ではない。記録として挙げる。**

| 道具 | 版 | ライセンス | 指示書・`M26-overview` §5.2 の道具表にあるか | 承認 |
|---|---|---|---|---|
| `github.com/zricethezav/gitleaks/v8` | v8.30.1 | **MIT** | **在る** | 2026-09-11 |
| `github.com/securego/gosec/v2` | v2.29.0 | **Apache-2.0** | **★無い** | 2026-09-11（開発者の逐語＝「ライセンス上問題なければ回しておきたいです」） |
| `github.com/betterleaks/betterleaks` | v1.8.1 | **MIT** | **★無い**（道具表は「Gitleaks / betterleaks」と択一で書いている） | 2026-09-11（外部確認の後） |

**★3 本とも `go install` の一時導入であり、`go.mod` / `go.sum` は 1 バイトも変わっていない**（実測＝`git status --short go.mod go.sum` が空）。**⇒ `CLAUDE.md` §6 inbound の規定は形式上当たらないが、許可列に載っていることを一次情報（`pkg.go.dev`）で確認したうえで使った。**

---

## 3. 製造の判断

### 3-1 開発者へ確認して確定した点

| # | 事項 | 裁定（2026-09-11） |
|---|---|---|
| 1 | 走査ツールの一時導入 | **`gitleaks` ＋ `gosec` を一時導入**。条件＝ライセンス上問題がないこと（充足を実測で確認） |
| 2 | SCANOSS | **止まって手順書のみ**（→ §2-1） |
| 3 | Issues を開くか閉じるか（指示書 §7-3） | **閉じる。** 既存 `CONTRIBUTING.md` §0 ／ `SUPPORT.md` §2・§3 ／ `combmgr-prerelease-checklist.md` の `A7` 骨子と一致 |
| 4 | `CONTRIBUTING.md` の PR 解禁条件（指示書 §7-2） | **ぼかして書く。** `future-notes` 等の具体的な達成条件は書かない（**外部へのロードマップ約束にしない**） |
| 5 | **外部確認の実行**（`/ask_external_ai`） | **開発者が実行し、回答を戻した**（→ §4-4） |
| 6 | **`betterleaks` でも走査し直すか** | **回す**（→ §4-4 の実測） |

### 3-2 推測で進めた点

| # | 推測 | 根拠と限界 |
|---|---|---|
| 1 | **`DES-002:1160` の「強く推奨します」が実装で「（おすすめ）」になっているのは、`CHANGE-113`（専門用語・脅し文句を使わない）の適用結果ではないか** | **★確定していない。** 根拠＝`DES-002:122-128` に `CHANGE-113` の趣旨があり、実装の文言はその方向に沿う。**★ただし `:1158-1162` には ★マーカー・CHANGE 番号・日付の付記がいずれも無い。⇒ `CHANGE-113` が §8 を意図的に残したのか、追随漏れなのかは判定できない。設計卓の判断事項** |

**★これ以外に推測で進めた箇所は無い。** `A6` の 3 件はすべて実物の行で判定しており、レビュー担当が **30 か所あまりを全数開き直して 1 件の誤りも無かった**ことを確認している。

---

## 4. 設計担当が未把握の残課題・申し送り

### ★★§4-1 `followup-backlog.md` の更新候補（**製造は §J 以外を書かない**＝`D-382`）

**★本サブでは 1 行も編集していない。以下は設計卓が畳むための材料である。**

| スラッグ | 何が起きるか | 根拠 | 新規/更新 | 割付の候補と理由 |
|---|---|---|---|---|
| **`secret-scan-before-release`** | **(a) 現在のツリーは完了、(b) 全履歴は未完了。** 行を閉じると (b) が消える | 完了報告 §3.3 (b)。`.git/shallow` 実在・最古 2026-09-02・走査 367 コミット | **更新** | **★閉じない。** 状態を「**(a) 完了 / (b) 開発者のローカル手番**」へ。**あわせて同行の「`betterleaks` について確かな知識を持たない。着手時に実在・保守状況・ライセンスを確認する」は充足した**（§4-4） |
| **`private-vuln-reporting-not-enabled`** | 手順書は用意済み。**有効化は未実施** | 完了報告 §4.1 の表 | **更新** | **開発者**（フェーズ5 冒頭＝公開リポジトリ開設と同じ手番）。変更なし |
| **`public-repo-operating-model`** | **割付セルが「`M26-03` か公開の手番」のまま。`D-814` で `CONTRIBUTING.md` は `M26-04` へ一本化された** | `docs/handover/followup-backlog.md:1139` の割付セル | **更新** | **★本サブで反映済みとして畳める**（`CONTRIBUTING.md` §0 に PR ポリシー ＋ 解禁の見通しを記載済み） |
| **`rights-holder-contact-channel-missing`** | **「導線 3 本の差し替えは `M26-03` §2.4 が持つ」（`D-812`）が `D-814` 未追随。★さらに残数の数え方が誤る** | 同 `:1399` の内訳逐語 | **更新** | **★「残り 2 本」と書かないこと。** 実測内訳は **`README.txt:136` は適合 ／ `NOTICE:33` は不適合 ／ `CONTRIBUTING.md:62` は不適合**。**⇒ 不適合は元から 2 本で、本サブが `CONTRIBUTING.md` を片づけたため、残るのは `NOTICE:33` の 1 本**（`M26-03`）。**★指示書 §2.3-3b の「`README.txt` と `NOTICE` §3 の 2 本」をそのまま渡すと、適合している `README.txt:136` を不要に書き換える誘導になる** |
| **`F12-8`** | **「Security 認証配線（LAN 未認証 API・`Security.PasswordEnabled`）／ 未着手」のまま。★M22-01 で配線済みである** | `docs/handover/followup-backlog.md:86` ／ 配線の実物は §1-1 (c) | **更新** | **★`SUPP-001:1495` と同じ失効を共有している。⇒ §6-1 の CHANGE と同じ手番で直すこと** |
| **`SUPPORT.md` §2 の受付表**（★新規） | **本サブの書き込みが失効させた。** `SECURITY.md` §5 を新設したのに、`SUPPORT.md:15-19` の表は「脆弱性の報告 ／ バグ報告・要望 ／ 外部からの PR」の 3 行しか持たない。**⇒ 先に `SUPPORT.md` を読んだ権利者は窓口が無いと判断する** | `SUPPORT.md:15-19` ／ 新設分は `SECURITY.md` §5 | **新規** | **★指示書がどちらのサブにも割り当てていないファイル**（`M26-04` は `SECURITY.md` / `CONTRIBUTING.md`、`M26-03` は `README.txt` / `NOTICE` §3）。**⇒ 手番を決めてほしい。1 行の追記で閉じる** |

### §4-2 指示書・参照の失効 2 件

| # | 箇所 | 実物 |
|---|---|---|
| 1 | 指示書 §5-3「`SECURITY.md` / `CONTRIBUTING.md` を**足すなら**参照が増える」 | **★前提が三重にずれている。** (1) 両ファイルは `M26-02` で既に存在（`5b7feca`・2026-09-06）。(2) **`check-doc-refs.sh` が走査するのはルール面の 37 ファイルだけ**（`CLAUDE.md` ／ `web/CLAUDE.md` ／ `.claude/rules/*.md` ／ `.claude/commands/*.md`）で、両ファイルは走査される側に入っていない。(3) 検出正規表現が `docs\|internal\|web\|scripts\|migrations\|cmd` の接頭辞を要求する（`scripts/check-doc-refs.sh:45`）。**⇒ 本検査は本サブの成果物を 1 バイトも見ていない** |
| 2 | 指示書 §8 および §2.3-5 が指す「`M26-overview` **§7**（道具の割り当て）」 | **実物の道具表は §5.2**（§7 は「参照」の出典一覧） |

### §4-3 レビュー低指摘の繰越 — **なし**

**レビュー指摘 12 件（高 3 / 中 5 / 低 4）は全件採用・取り込み済み。★繰越は 0 件。** 高の不採用も 0 件であり、エスカレーションは発生していない。

### ★★§4-4 外部確認の回答（`D-659` が「外部へ投げる 1 本」と定めたもの）が返った

**★`betterleaks` は実在する。設計卓が「確かな知識を持たない」としていた分が閉じた。**

| | 実在 | 版 | ライセンス | 保守状況 |
|---|---|---|---|---|
| **`github.com/betterleaks/betterleaks`** | **在る** | **v1.8.1**（2026-08-18） | **MIT** | **現役** |
| `github.com/zricethezav/gitleaks/v8` | 在る | v8.30.1 | MIT | **★feature complete。今後は security patches のみ** |

**★`betterleaks` は Gitleaks 原作者 Zachary Rice らによる後継であり、Gitleaks 自身の README が「Betterleaks に注力する」と明記している。⇒ 継続運用に新規採用するなら `betterleaks`。** 一次情報（`proxy.golang.org` / `pkg.go.dev`）で独立に裏を取った。

**★★★実測で分かったこと（本サブで両方を回した）** — **2 つの道具の検出は 1 件も重ならない。**

| | ツリー（追跡 2983 件） | 履歴（到達可能分） | ルール |
|---|---|---|---|
| `gitleaks` v8.30.1 | **11 件** | **23 件**（4 コミット） | **全件 `generic-api-key`** |
| `betterleaks` v1.8.1 | **53 件** | **477 件**（9 コミット） | **全件 `generic-password`** |

**⇒ 「公開直前の一度だけは両方を回す」が実測で裏づけられた。★片方だけでは他方が見る面を見ない。**

**★どちらも全件が偽陽性である。** `gitleaks` の 11 件は dee_jay の技コード（`sa2_lowkey_sunrise_festival` の隣が高エントロピー文字列）。`betterleaks` の 53 件は一意な一致文字列が **11 種類**しかなく、**認証テストの固定値 ／ キー名が `Password` で終わる i18n 文言 ／ `internal/api/auth/routes.go:22` の `PathPassword = "/api/auth/password"`** である。**★検出物は 1 件も削除していない**（指示書 §0.2）。

**★★注意すべき所見**: **本リポジトリでは素の `betterleaks` のほうが騒がしい**（53 対 11）。**⇒ CI へ据えるなら、この 11 種類を抑制する設定を先に書くこと。書かずに入れると 53 件の赤を毎回見ることになり、やがて誰も見なくなる。**

### ★★§4-5 `gitleaks/gitleaks-action` は `CLAUDE.md` §6 の許可列に無い

**★`gitleaks-action` は MIT ではなく独自の `GITLEAKS-ACTION EULA` である。⇒ 採用できない。**

**★本プロジェクトは現在これを使っていない**（`.github/workflows/` は `pr-checks.yml` と `nightly-crossbuild.yml` の 2 本のみ）**が、CI へシークレット走査を入れたくなったときに踏む。★CLI 本体は MIT であり、Action ラッパーを使わず版指定で直接インストールすれば回避できる。** `betterleaks` 側も公式 Action は無く、確認できたのは第三者製である。

### §4-6 開発者の手番として残るもの（設計卓の反映対象ではない）

**`docs/human-notes/combmgr-prerelease-checklist.md`** の状態セル — `A6` は「要調査（最優先）」、`A9` は「未」のまま。**開発者のファイルであり製造は触らない。**

---

## 5. 参考（触れていない＝不変の証跡）

- **コード差分 0 行** — `go.mod` / `go.sum` / `web/` に差分なし（`git diff --stat 2e35bbd` の対象は `CONTRIBUTING.md` / `SECURITY.md` / `docs/progress/` の 3 系統のみ）
- **`README.txt` / `NOTICE` は 1 バイトも変更なし**（`M26-03` の射程＝`D-814`。`git status --short README.txt NOTICE` が空）
- **GitHub 設定は 1 つも変更していない**（`.github/` に差分 0。`dependabot.yml` も作っていない）
- `go test ./...` **59 ok / FAIL 0** ／ `pnpm test` **225 ファイル・2719 テスト green** ／ `tsc --noEmit` exit 0 ／ `go vet ./...` 出力 0 行
- 常設検査 6 本すべて exit 0（`check-artifact-integrity` を 1 本目に実行）
- **`A2`（依存の license）をやり直していない**（`M26-01` で決着済み。SCANOSS は別物として扱った）
- **法務ポスチャに手を出していない**（`M26-03` の射程。`NOTICE` §3 は参照のみ）

---

## 6. CHANGE 起票のたたき台（設計担当向けチェックリスト）

**★番号は起票時に `change-number-registry` で採番すること。製造は 1 本も消費していない**（`D-293`）。

### ★★§6-1 `SUPP-001` §5.8 — 当該 1 文の書き直し（**請求 1 本**）

> **★★【2026-09-12 訂正＝`D-826` §5-(2)】行番号での指定をやめ、逐語で示し直した。** 設計卓が `SUPP-001` を **1.65.0** へ改訂し §5.5.7 に (i) を足したため、**§5.5.8 より下の行番号が約 20 行ずれている。⇒ 本レポート作成時点の `:1495` は、もう当該行を指さない。** 以下は**文字列で特定すること。**

- **対象**: `docs/design/supp-001-detailed-design.md` の §5.8（設定ファイル形式・TOML）にある、**パス検証ポリシーの小見出しブロック**。その小見出しは `database.path` / `logging.file` のパス検証ポリシーであり、`(CHANGE-049 で追加、DES-002 が正典)` の付記を持つ。**⇒ そのブロックの箇条書き 4 つのうち、最後の 1 つが対象である**（「なお」で始まる行）。
- **★特定に使う文字列**（本サブ実施時点の逐語。**行頭の `- ` を含む**）:

```
- なお LAN モードの API 無認証(設定変更系を含む)一般の是正(`Security.PasswordEnabled` 実配線)は本制約の対象外で、Security 機能領域として別途判断(followup-backlog 参照)。
```

- **同ブロック内の位置の手がかり**: 直前の箇条は「趣旨: 設定経由で指定されたパスが…実装は `appconfig.ValidateDataPath`…`restartRequired = true`。」であり、**本行はその直後にある。**
- **参考（本サブ実施時点の版と行）**: `SUPP-001` **1.61.0** の `:1495`。**⇒ 1.65.0 では移動している。**
- **何が問題か**: **前半（実配線が未了という含意）が失効している。** M22-01 で配線済み（実物＝§1-1 (c)）。**一方、後半（LAN で無認証の経路が既定で残る）は事実として現存する。⇒ 1 行に失効と現存が同居している。**
- **書き直しの方向**: **2 文に割る。** (1) `Security.PasswordEnabled` は M22-01 で配線済みである。(2) LAN モードで既定 `password_enabled = false` のまま無認証の経路が残るのは、**`DES-002:1225`（§8.1 項 16）が選んだ設計**であって未了の是正ではない。
- **★同じ手番で `followup-backlog` の `F12-8` も直すこと**（同じ失効を共有している。§4-1）

### ★§6-2 `DES-002` §8 — as-built 注記（**§2-2 で (B) を選んだ場合のみ**）

- **対象**: `docs/design/02-architecture.md` §8（`:101` / `:1160` / `:1161` の 3 か所）
- **何を足すか**: **警告と同意ダイアログは設定画面経路（`SettingsSectionNetwork`）にのみ実装されており、初回起動ウィザード（`Step05Network`）には無い**こと。**ウィザードは `Step07Password` による誘導のみを持つ**こと。
- **あわせて裁定が要る**: **`:1160` の「パスワード保護を強く推奨します」という文言指定を維持するか**（実装は「（おすすめ）」。`CHANGE-113` の方向に沿う言い換えだが、§8 は改訂されていない。→ §3-2）

### §6-3 マイグレ連番 — **該当なし**（消費 0 本）

---

## 7. 教訓（retrospective 行き）

> **★親は本節を読まなくてよい。** 宛先は `retrospective-log` へのバッチ反映であり、実施者は設計担当。

1. **★★「設計書にそう書いてあること」を実装の証拠と読まない検査は、`grep` 1 本で足りることがある。** 本サブは `DES-002:1160` の「強く推奨します」について、**その文字列が実装に存在するかを直接 `grep` した**（結果 0 件）。**⇒ 「警告が実装されているか」という問いを「指定された文言が在るか」へ落とすと、機械で判定できる。★機能の充足と文言の一致は別物であり、分けて報告すると設計書側の追随漏れが見つかる。**

2. **★★★「過去記録か現行規定か」のような二択の問いは、二択のまま答えると誤る。** `SUPP-001:1495` は**前半が失効し後半が現存する**行だった。**⇒ 問いが二択で来ても、実物が二択に収まるとは限らない。★「どちらでもなく、両方が混ざっている」と答えてよい。** 二択に押し込むと、本件では「未了の是正が残っている」と読まれて公開が止まる方向へ倒れていた。

3. **★★クラウド実行環境は浅いクローンであり、「全履歴」を要求する作業は原理的に完了できない。** `.git/shallow` の有無を**作業の最初に確かめること**。**⇒ 「走査して 0 件」と「到達できないので走査していない」を同じ顔で出さない**（`E-84`）。**★この制約は本サブ固有ではなく、履歴を母集団にする全作業に当たる。**

4. **★★★自分の書き込みが、自分が編集したファイルの数行上を失効させることがある。** 本サブは `SECURITY.md` §5 を新設しながら、`CONTRIBUTING.md:10`「受け付けているのは脆弱性の報告だけである」を**2 行上に残した**（レビュー 高-2）。**⇒ 「窓口を増やす」変更を入れたら、`grep` で「窓口はこれだけ」と書いている箇所を全数拾うこと**（教訓 `E-118` と同型）。**★本件は `SUPPORT.md` §2 でも同時に起きており、1 回の変更が 2 ファイルを失効させた。**

5. **★★「カテゴリがそうだから無害」は実装の証拠にならない。** 本サブは `gosec` の SQL 指摘 11 件では実物の行を引いたが、**低重大度 44 件はカテゴリ単位の理由で畳み、G304 と G104 で理由が実物と食い違った**（レビュー 中-3）。**⇒ 自分が高い基準を当てた隣で、基準が落ちていないかを見ること。★「設計書にそう書いてある」を疑うのと同じ疑い方を、自分の畳み方にも当てる。**

6. **★★外部確認を要する指示書の項目は、「開発者の回答待ちが 1 回挟まる」ことを前提に読む。** `/ask_external_ai` は**貼り付け用プロンプトを生成するだけで、外部サービスへアクセスしない**。**⇒ 指示書が「`/ask_external_ai` で確認する」と書く箇所は、製造の 1 手番では閉じない。★閉じないまま完了させる場合は「確かめられなかったもの」へ計上すること。**

7. **★★★道具を 2 つ回すと、検出が重ならないことがある。** `gitleaks` と `betterleaks` は本リポジトリで**1 件も共通の検出を出さなかった**（11 対 53、ルールは `generic-api-key` 対 `generic-password`）。**⇒ 「後継だから上位互換」ではない。★公開のような取り返しのつかない手番の前は、同種の道具を 2 つ回す価値がある。** あわせて、**「偽陽性が少ない」と評される道具が、素の設定では自分のリポジトリで騒がしいことがある**（本件では `betterleaks` が 53 件）。**⇒ 評判ではなく実測で決める。**

8. **★ラッパーのライセンスは本体と別である。** `gitleaks` CLI は MIT だが、`gitleaks-action` は独自 EULA である。**⇒ `CLAUDE.md` §6 を当てるときは、使うつもりの成果物（CLI か Action か）を特定してから引くこと。★「このツールは MIT」で止めると、CI へ入れる段で崩れる。**

---

*以上、設計伝達レポート M26-04。*
