# 設計伝達レポート（例外レポート） — `M38-02`（技編集の導線除去 ＋「引っ越し取込」→「他から引っ越し」改名）

| 項目 | 内容 |
|---|---|
| **対象** | **親チャット（設計卓）** |
| **発信** | 製造担当 Claude Code（2026-09-17 初版 ／ **2026-09-18 改訂＝§2-3 / §2-4 / §7-5 を追加**） |
| **指示書** | `docs/instructions/M38-02-moves-grid-hide-and-rename.md` **v1.0.0** ／ 上位 `M38-overview` v1.0.0 |
| **実装コミット** | ブランチ `claude/clever-darwin-87l5gp`（**push 済み。設計卓は HEAD で読める**） ／ **6 コミット**（`23e5bee` 実装 ／ `2b3e03d` 完了報告 ／ `457d235` レビュー取り込み ／ `b9c9a95` 本レポート初版 ／ **`4eca404` 開発者裁定 2 件**〔理由の差し替え・説明書 `ch14` 削除〕 ／ **`367aa5f` 自己是正**〔`dist-readme.txt` の禁止は前提が失効済みだった〕） |
| **関連** | 完了報告 `docs/progress/M38-02-completion-report.md` ／ レビュー `docs/progress/m38-02-review.md` |
| **チェックリスト** | `docs/instructions/reviews/M38-02-review-checklist.md` **v1.0.0**（**着手前に実在を確認した**＝指示書 §0.4 の版ゲート 5 点すべて通過） |
| **レビュー集計** | **高 4 / 中 4 / 低 2 ＝ 全 10 件・採用 6 / 送付 2 / 不採用 2・「高」の不採用 0 件・再レビュー往復 0 回（上限 2）** |
| **源泉** | 完了報告 ＋ レビュー報告書 ＋ **開発者との同一セッション内のやり取り**（着手前の裁定 4 件） |
| **CHANGE / マイグレ / 依存** | **CHANGE 0 本**（★自採番しない＝`D-293`。原稿は §6） ／ **マイグレ 0 本** ／ **新規依存 0 件** ／ **`docs/design/` 差分 0 行 ／ `followup-backlog.md` 差分 0 行** |

本書は **①独自確定仕様 ②契約違反の独自判断 ③製造判断 ④残課題**に絞る。指示書どおりの部分は割愛する。

**★★★§2 に 4 件ある。いずれも親の裁定が要る**——**(1)** 指示書 §0.1 が名指しした導線の在り処が as-built と食い違っていた ／ **(2)** 指示書 §2.3-2 / §4.3 が命じた「表示語は `ja.json` から引く」が本画面では実行不能であった ／ **(3)** 指示書 §0.1 / §0.2 が「開発者の逐語」として記録した理由が、後日の開発者の訂正で差し替わった ／ **(4)** 説明書 `ch14` を削除し `dist-readme.txt` を触った（指示書 §3-5 の禁止。★ただしその前提は既に失効していた）。**★(3)(4) は 2026-09-18 の追加分である。**

**★最重要は §2-3 と §4-3 である。** §2-3 は**設計書へ刻む理由が差し替わった**件であり（**★放置すると `DES-005` §5.18 に誤った理由が入る**）、§4-3 は**同じ型の取りこぼしが本サブで 3 度目に近づいている**ことの警告である。**★あわせて §2-1**（`DES-005` §3 と実装の乖離が `M38-02` 以前から在った）**と §2-4**（指示書が「他サブの手番」を理由に禁じた面は、起票時点で既にその手番が閉じていた）**は、どちらも「指示書・設計書の記述が現状より古かった」型である。**

---

## §1 製造が独自に確定した実装仕様（DES 反映が要るもの）

### §1-1 技編集の導線は **`Header.tsx` のメインナビ 1 件のみ**であった（**全数の実測値**）

**指示書 §2.1-2 が「★設定画面だけとは限らない」として全数走査を求めた結果。**

| 場所 | 実測 |
|---|---|
| **`web/src/components/Header.tsx:37`** | **★唯一の導線。** `NAV_LINKS` の 1 エントリであり、**PC ナビ（`:79`）とモバイル Sheet（`:125`）が同じ配列を描く**ため、1 行の削除で両面から消える |
| `SettingsPage.tsx` ／ `features/config/SettingsSection*.tsx` | **0 件**（画面外への `Link` は `/presets` のみ＝`SettingsSectionPresetLink.tsx`） |
| `Footer.tsx`（モバイル下部ナビ 4 項目） | **0 件** |
| 確定反撃サーチ ／ その他の全画面 | **0 件** |

**⇒ `DES-005` §5.16（設定）の項目一覧に技編集の行を足す必要は無い。元から無く、実装にも無かった。**

**⇒ `DES-005` §5.18 へ「本画面への導線は持たない（`M38-02`・`D-892`）。ルートは存置し URL 直打ちで到達する」を明文化してほしい。**

### §1-2 **本画面の表示語は `ja.json` に 1 件も無く、正本はコード 2 箇所である**

| 実測 | 値 |
|---|---|
| `web/src/locales/ja.json` の intake / helper 系キー | **0 件**（`en.json` も 0 件） |
| 表示語の実体 | **`web/src/components/Header.tsx:36`**（ナビ `label`） ／ **`web/src/pages/IntakeHelperPage.tsx:159`**（`h1`） |
| 根拠 | `DES-005` §5.19 の性質欄「**i18n キーは追加しない**（import/export 系の**固定 ja** 流儀＝§5.13 の i18n 境界注記）」 |

**⇒ 実装は設計どおりである。★食い違っていたのは指示書の側である**（§2-2）。

**⇒ `DES-005` §5.19 の当該行は正しいが、「i18n キーが無い ＝ 表示語の正本はコードである」という帰結が §5.19 に書かれていない。⇒ 1 行足してほしい。** 本サブの改名でも、次の改名でも、**まずここを読む人が `ja.json` を探して空振りする**（実際に指示書がそう書いた）。

### §1-3 API 経路・DTO・エラー契約

**なし。** 本サブは **API 経路の新設・変更を 1 件も行っていない**（`GET/PATCH /api/moves`・`POST /api/moves/:id/rush-variant`・`POST /api/intake/resolve`・`POST /api/intake/csv` はいずれも無変更）。**⇒ `DES-002` §4.2 への反映は不要である。**

---

## §2 契約・設計に反する独自判断（★親の裁定が要る）

### §2-1 **`DES-005` §3 と as-built が食い違っていた。⇒ 指示書 §0.1 の前提が成立しなかった**

| 項目 | 内容 |
|---|---|
| **何に反したか** | **`docs/design/05-screen-design.md:131`**（§3 画面遷移図の注記）＝「moves 編集グリッド（画面18、FR703）への導線は**主要ナビゲーションには含めず**、『設定』（画面16）配下のデータ管理導線として配置する（管理的補正機能のため主要ナビと分離）…（`CHANGE-033`・`CHANGE-055`）」 |
| **as-built** | **メインナビに置かれていた**（`web/src/components/Header.tsx:37`）。**設定画面には 1 件も無い**（§1-1） |
| **いつからか** | **★M38-02 以前から**。本サブが作った乖離ではない |
| **実装がどうなっているか** | **`Header.tsx:37` を削除した。⇒ 結果として「主要ナビには含めない」という `DES-005` §3 の要求を*いま初めて満たした*状態になっている**（ただし設定配下へ移したのではなく、**導線ゼロ**である） |
| **なぜ止めなかったか** | **設計書*間*の矛盾ではなく、設計書と実装の乖離であるため**（`CLAUDE.md` §8 の停止条件は前者）。かつ**指示書 §2.1-2 が「設定画面だけとは限らない」と全数走査を先回りで求めていた**ため、実測に従って進めた |

**⇒ 親へ:** `DES-005` §3 `:131` の注記は**現状と二重に食い違っている**——(a) 「設定配下へ配置」は**一度も実装されていなかった**、(b) `M38-02` 以後は**どこにも配置しない**。**★(a) を「失効」として消すのか「未実装」として残すのかは設計判断である。** 製造は文面を触っていない。

### §2-2 **指示書 §2.3-2 / §4.3 の「表示語は `ja.json` から引く」は本画面では実行不能であった**

| 項目 | 内容 |
|---|---|
| **何に反したか** | 指示書 `M38-02-moves-grid-hide-and-rename.md` §2.3-2（「**表示語は `web/src/locales/ja.json` から引く**」）および §4.3（危険の 3 番目「表示語を設計書から写すこと」） |
| **なぜ実行できないか** | **`ja.json` / `en.json` に本画面のキーが 0 件だから**（§1-2）。`DES-005` §5.19 が「i18n キーは追加しない」と定めている面である |
| **実装がどうなっているか** | **コード上の直書き 2 箇所を正本として扱った**（`Header.tsx:36` / `IntakeHelperPage.tsx:159`）。**★i18n キーは新設していない**——新設すると `locales.test.ts` のキー対称と `no-japanese-in-en.test.ts` を巻き込み、射程外の変更になる |
| **趣旨は満たしているか** | **満たしている。** §4.3 の教訓（`M32-03` §7-2＝**`CHANGE` 通知書・設計書の本文は表示語の正本ではない**）の実質は「**実物を引け**」であり、実物がコードであるならコードを引くのが正しい |

**⇒ 親へ:** **指示書の欠陥として記録し、次に同型の改名指示書を書くときのテンプレを直してほしい。** 「表示語は `ja.json` から引く」ではなく「**表示語の正本を先に実査せよ（`ja.json` にキーが無い面がある）**」が正しい書き方である。**★本プロジェクトには固定 ja の面が実在する**（import / export 系＝`DES-005` §5.13 の i18n 境界注記）。

### §2-3 **★★★指示書 §0.1 / §0.2 が「開発者の逐語」として記録した理由が、後日の開発者の訂正で差し替わった**

| 項目 | 内容 |
|---|---|
| **指示書の記録**（v1.0.0 §0.1 / §0.2） | **外す理由**＝「**一部の技のフレームが特殊〔`through` 等〕であり、初期はフレームの誤りを開発者自身が直したい**」（★逐語と明記） ／ **残す理由**＝「**★★★理由＝将来「setplay only の技だけを作成・編集する画面」へリメイクする**」（★同じく逐語と明記） |
| **2026-09-17 の開発者の訂正** | 「**`/moves/edit` を残すのは消すコストが高かったからであり、フレームの誤りはマイグレ等で直す**」 |
| **★食い違い** | **(a)** 残す動機が「フェーズ5 のリメイク構想」から「**削除コスト**」へ。**(b)** 本画面は**技データ是正の手段ではない**（是正はマイグレーション）。**⇒ 指示書 §0.1 の「コードはまだ使うので消さない」も、「使う」の中身が違って読める** |
| **実装がどうなっているか** | **★訂正後の理由へ差し替えた**（開発者裁定 2026-09-17）。`web/src/components/Header.tsx:39-43` ／ `web/src/pages/MovesEditGridPage.tsx:13-23` ／ 本書 §6 の CHANGE 原稿 A |
| **なぜ §2 に挙げるか** | **指示書が「開発者の逐語」として明示的にマークした記述を、製造の判断で上書きしたためである。⇒ 逐語の扱いは本来 §2 の型である。** ★あわせて **`M38-overview` §2 の実測表と `D-892` のボード行も同じ逐語に基づいており、そちらは製造が触れない** |

**⇒ 親へ:** **`DES-005` §5.18 へ書く理由は訂正後のものでよいか**を確認してほしい。**★とくに「フェーズ5 で setplay only 画面へリメイクする」を設計書へ残すかどうか**——`followup` の `setup-only-column-has-no-input-path` は生きているが、**それが `/moves/edit` のリメイクという形を取るとは限らなくなった**。

**★★さらに `M38-02` 指示書 §0.1 / §0.2 と `M38-overview` §2 の当該行が失効している。⇒ 是正は設計卓の手番である。**

### §2-4 **説明書 `ch14` を削除し、`dist-readme.txt` を触った（指示書 §3-5 の禁止。★ただし前提は失効していた）**

| 項目 | 内容 |
|---|---|
| **何に反したか** | 指示書 §3-5「**`docs/usermanual/dist-readme.txt` を触らないこと**（`M36-01` の手番・`D-887`）」 |
| **やったこと** | 「全 22 章」→「全 21 章」を **2 箇所**（`:8` / `:223`）。`bash scripts/check-dist-readme.sh --write` で生成物 `README.txt` を再生成（照合 `EXIT=0`） |
| **なぜ** | **開発者裁定**（2026-09-17）＝「あまり見せたくない機能について言及を残すのは少し嫌です」により `ch14` を節ごと削除 ⇒ 章数が 22 → 21 になった。**★章数は `readme.html` と `dist-readme.txt` の両方に持たれており、片側だけ直すと「配布 zip の `README.txt` が『全 22 章』と名乗る一方、実物は 21 章」という食い違いが配布物の中に残る** |
| **★★★前提の失効（2026-09-18 の実査）** | **(1) `D-887`**（2026-09-15）**は `M36-01` に「`dist-readme.txt` は射程内」と許可を与えた裁定**であり、指示書 §3-5 はそれを「だから `M38-02` は触るな」へ読み替えたもの。 **(2) `D-890`**（2026-09-17）**が `M36-01` を受理した＝マージ可。⇒ 「`M36-01` の手番」は既に終わっていた。** **(3) `M36-01` の同ファイルへの変更**（`37573df` ／ `e2e4260`）**はマージ `1795065` 経由で本サブの着手基点 `7ce1393` に入っており、本サブはその上に積んだ。⇒ `D-888` が塞ごうとした「上書きによる消失」は発生していない** |

**⇒ 親へ:** **指示書 §3-5 は発行時点で既に失効していた可能性が高い**（`D-890` と `M38` 起票の `D-892` は同じ 2026-09-17）。**★次に同種の「他サブの手番だから触るな」を書くときは、その手番が閉じていないかを起票時に確かめてほしい。⇒ 製造は指示書の明文に従って一度停止し、開発者へ確認を上げる分の往復が発生する。**

---

## §3 製造の判断

### §3-1 開発者へ確認して確定した点（**着手前 4 件 ＋ ★完了報告の提出後 2 件**）

| # | 事項 | 確定内容 | 反映先 |
|---|---|---|---|
| 1 | **改名後の `h1` の文言** | **「他から引っ越し(他のアプリ・メモ・表計算から)」**（**括弧書きを残す**。ナビは括弧なし「他から引っ越し」） | `IntakeHelperPage.tsx:159` |
| 2 | **Go 側コメント `internal/model/move.go:33` を改名するか** | **直す**（表示語ではないが、画面を現在の呼び名で指しているコメントであるため） | `move.go:33` |
| 3 | **説明書 `ch14`（moves 編集グリッド章）の扱い** | **章は残し、URL は載せず「いまメニューに出していない」と書く**（章を消すと `ch15` 以降の章番号・アンカー・画像ファイル名規約へ波及し `M32` 系の撮影運用と衝突する。URL を載せると「利用者に触らせない」という §0.1 の目的と逆向きに働く） | `tacpendium-readme.html:712` |
| 4 | **既存の失効記述をどこまで直すか** | **①②③ すべて直す**（① `web/CLAUDE.md` §1 台帳 #4 の旧名 1 語 ／ ② Go の「取込ヘルパー」**19 箇所** ／ ③ `MovesEditGridPage.tsx` の `?character=` コメント） | §3-1 の下表 |
| **★5** | **`/moves/edit` を残す理由**（完了報告の提出後・2026-09-18） | **★指示書 §0.1 / §0.2 の逐語を差し替える**＝「**消すコストが高かったから残した。フレームの誤りはマイグレ等で直す**」 | `Header.tsx` ／ `MovesEditGridPage.tsx` ／ 本書 §6 の CHANGE 原稿 A（詳細は §2-3） |
| **★6** | **説明書 `ch14` の扱い**（同上） | **★3 を撤回し、章ごと削除する**＝「**あまり見せたくない機能について言及を残すのは少し嫌です**」。⇒ 節・TOC 行・`ch20` の相互参照・撮影枠を削除し「全 22 章」→「全 21 章」を 4 箇所 | `tacpendium-readme.html` ／ `dist-readme.txt` ／ `README.txt`（詳細は §2-4） |

**★4 の ①②③ は指示書に無い変更である。⇒ 実装前に確認し、裁定を得てから実行した**（`roles-and-routing` のハード列運用）。**レビューも「射程逸脱に当たらない」と判定している。**

### §3-2 推測で進めた点

**なし。** 判断が要った 4 点はすべて §3-1 のとおり開発者へ確認した。**⇒ コード内に「推測:〜と仮定した」のコメントは 1 件も無い。**

### §3-3 レビュー指摘の不採用（**2 件。いずれも「低」**）

| # | 指摘 | 不採用の理由 |
|---|---|---|
| 低-9 | `retired-words.test.ts` の `stripComments` の 2 つの弱点（行末コメント判定 `/\s\/\/.*$/` が**文字列リテラル中の `" // "` を含む行を途中で切る** ／ ブロックコメント除去の非貪欲一括置換が**文字列リテラル中の `*/` で早期に閉じうる**） | **★レビュー自身が「現状の実データでは発火していない・いま直す必要は無い」と書いている。** 発火していない正規表現を投機的に直すと、**直したこと自体が検証できない**。⇒ 次に同ファイルを触る手番へ委ねる |
| 低-10 | `moves-edit.spec.ts` がヘッダのナビ内容まで見るようになった（責務の混在） | **意図した設計である。** 導線の有無だけを見る spec を新設すると全数の実行時間が伸びる割に主張は 2 行。**★「将来ナビが変わったら落ちる」は望ましい**——技編集の導線が戻ったら知りたい。理由は spec 内のコメントに明記済み |

**★「高」の不採用は 0 件である。⇒ Phase C の安全弁（重大指摘の自動棄却時のみ開発者エスカレーション）は発動していない。**

---

## §4 設計担当が未把握の残課題・申し送り

> **★`followup-backlog.md` は 1 文字も編集していない**（`D-838` / `D-382`）。**⇒ 下記は設計卓が受理の手番で転記するための原稿である。**

### §4-1 `followup` 新規登録の候補（**1 件**）

| 項目 | 内容 |
|---|---|
| **スラッグ** | **`code-facts-hardcodes-nav-labels-and-goes-stale`** |
| **何が起きるか** | 派生資料 `docs/handover/code-facts.md` が **Header ナビの表示語とルートをハードコードした表**を持っており、ナビを変えると失効する。**★`M38-02` の時点で 2 行が失効した**——**`:501`** が `\| Header \| /import/combo/helper \| "引っ越し取込" \| active \|`、**`:502`** が `\| Header \| /moves/edit \| "技編集" \| active \|`。**★後者は「消した導線」を `active` と主張しており、前者より誤解を生む** |
| **根拠** | `docs/handover/code-facts.md:501-502`（実測） ／ 生成器は `scripts/generate-code-facts.sh` |
| **割付の候補と理由** | **派生資料レーン。** 本サブでは**触っていない**——同ファイルは生成物であり、手で直すと次回の再生成で戻る。**⇒ 再生成の要否（およびタイミング）は設計卓の判断である** |
| **新規登録か更新か** | **新規登録** |

### §4-2 `followup` 既存行の更新候補（**1 件**）

| 項目 | 内容 |
|---|---|
| **対象スラッグ** | **`import-screen-has-two-names`** |
| **更新後の状態** | 対象へ **`IntakeHelperPage`** を加える。**現状は「ナビ『取込』と `h1`『取込(CSV)』」（`ComboImportPage`）だけが登録されている** |
| **何が起きるか** | **`/import/combo/helper` も同型の非対称を持つ**——ナビ「**他から引っ越し**」に対し `h1` は「**他から引っ越し(他のアプリ・メモ・表計算から)**」 |
| **根拠** | `web/src/components/Header.tsx:36` ／ `web/src/pages/IntakeHelperPage.tsx:159` ／ 先行形は `DES-005` §2 の画面一覧 行19（`05-screen-design.md:44`）が**元から括弧込み**で載せている |
| **★重要（判定の材料）** | **本サブは同型を*増やしていない*。** 非対称は**着手前から同じ形で存在**しており、**括弧書きを残すことは開発者が 3 案から選んだ結果**である（§3-1 の 1）。**⇒ 「直すべき欠陥」ではなく「台帳に載っていない既知の形」として登録するのが適当と考える。最終判断は設計卓** |
| **新規登録か更新か** | **既存行の更新** |

### §4-3 ★★★`retired-words.test.ts` の走査根を広げるかの裁定請求（**最重要**）

| 項目 | 内容 |
|---|---|
| **スラッグ（新規登録の候補）** | **`retired-words-scan-root-limited-to-web-src`** |
| **何が起きるか** | 同検査の走査根は **`web/src` だけ**であり、**Go（`internal/` `cmd/`）・`web/e2e`・`migrations/`・`docs/` を 1 バイトも見ない**。**⇒ 退けた語がそれらの面に残っても永久に赤くならない** |
| **★実害（2 度起きている）** | **(1)** `M24-07` の「取込ヘルパー」が **Go 側 19 箇所 ＋ `migrations/` 1 箇所**に残り、**約 3 週間**誰にも気づかれなかった（`M38-02` で是正）。 **(2) ★製造自身が同じ穴に落ちた**——段 3b の走査を `web/` `internal/` `cmd/` `docs/usermanual/` の **4 箱**に切ったため `migrations/000033_create_move_commands.up.sql:10` を取りこぼし、**レビュー 高-1 が検出した** |
| **根拠** | `web/src/locales/retired-words.test.ts:36`（`const SRC_ROOT = join(process.cwd(), "src")`） ／ 同 `:44`（`*.test.ts(x)` 除外） ／ 同 `:122` の `SA ゲージ` エントリの `reason` が**同じ限界を既に逐語で告白している**（「★本検査の走査対象は web/src だけである——Go 側…と migrations のコメントも詰めへ揃えたが、そちらは本検査の網の外にある」） |
| **★★レビューの警告（そのまま伝える）** | **「本サブが『走査根は `web/src` だけである』と明記して申し送ったのは正しい対応だが、その結果として高-1 が実際に発生した。⇒ 申し送りだけでは 3 度目の同型が起きる。」** |
| **★製造が実施しなかった理由** | **走査根の変更は設計判断を要し、本サブの射程外である**——現在の `stripComments` は **TypeScript のコメント構文しか知らない**（`//` と `/* */`）。`.sql`（`--`）・`.html`（`<!-- -->`）へ広げると構文を足す必要があり、**かつ広げた瞬間に「歴史として残すべき経緯コメント」まで赤くなる**設計上の判断が要る |
| **割付の候補と理由** | **語彙レーン**（`import-screen-has-two-names` / `intake-japanese-export-katakana-asymmetry` と同じ面） |
| **新規登録か更新か** | **新規登録** |

### §4-4 `M38-01` とのマージ衝突の確認請求（**1 件**）

| 項目 | 内容 |
|---|---|
| **何が起きるか** | **`web/e2e/combo-crud.spec.ts` を `M38-01` と同時に触っている可能性がある**（両サブは `M38-overview` §3.3 により並列可） |
| **本サブの変更** | **44 行目のコメント 5 行だけ**（`exact: true` の根拠が「ヘッダーの『技編集』ナビ」であったが、そのナビを本サブが外したため失効した）。**★アサート本体・`exact: true` は 1 文字も変えていない** |
| **★製造からは確認できない** | 取り込み系の git 操作（`merge` / `fetch` 等）は `deny` であり、`M38-01` のブランチを読めない |
| **割付** | **取り込みの手番（設計卓・開発者）。⇒ 衝突しても解決は自明である** |

### §4-5 §J（停止時記録）の原稿

**なし。** 停止規律の上限（再レビュー往復 2 回 ／ タイムボックス ／ 開発者の終了指示）に達しておらず、**未解消の指摘は 0 件**である（レビュー 10 件のうち採用 6 ／ §4 へ送付 2 ／ 理由つき不採用 2＝いずれも「低」）。

---

## §5 参考（触れていない＝不変の証跡）

- `docs/design/` 配下: `git diff 7ce1393 -- docs/design/` **空**（`CLAUDE.md` §8）
- `docs/handover/followup-backlog.md`: `git diff 7ce1393 -- docs/handover/followup-backlog.md` **空**（`D-838`）
- `docs/usermanual/dist-readme.txt`: **★触った**（章数 2 箇所。指示書 §3-5 は「触るな」としていたが、**`D-890` で `M36-01` が受理済みであり前提が失効していた**。詳細は §2-4）
- `migrations/`: **新規 0 本。** 既存 1 ファイルの**コメント 1 行のみ**変更（`000033_...up.sql:10`）。スキーマ・データは不変
- ルート `/import/combo/helper`: `router.tsx:46` **無変更**（指示書 §3-2）
- ルート `/moves/edit`: `router.tsx:44` の `<Route>` 行 **無変更**（JSX コメント 2 行を上に足しただけ）
- `INTAKE_USER_RULES_STORAGE_KEY = "intake-helper-user-rules-v1"`: `user-rules-storage.ts:8` **無変更**
- `web/src/features/moves/{MoveEditGrid.tsx, api.ts, types.ts}`: `git diff` **空**
- `web/src/features/moves/MoveEditGrid.test.tsx`（**14 本**）: `git diff` **空**
- E2E `m14-03d-manon-seed` ／ `m14-03e-third-wave-seed` ／ `m31-02-character-picker-drag`: `git diff` **空**（3 本とも画面18 を踏むが、元から `page.goto` 直打ち）
- `internal/api/move/` ／ `internal/service/move*`: 実装コード **差分なし**（`repository/move/repository.go` のコメント 1 行のみ）
- 新規ファイル: **実装フェーズは 0 件**（`git diff --diff-filter=A` にコード無し）。文書のみ **3 件**（完了報告 ／ レビュー報告 ／ **本レポート**）＝**教訓 `E-225` の上書き消失なし**

---

## §6 CHANGE 起票のたたき台（設計担当向けチェックリスト）

**★番号は起票時に `docs/handover/change-number-registry.md` で採番すること。製造は自採番していない（`D-293`）。★マイグレ消費 0 本。**

### CHANGE 原稿 **A** — 技編集の導線除去（画面18 を主要ナビから外す）

| 反映先 | 書くこと |
|---|---|
| `DES-005` §2 画面一覧 **行18**（`05-screen-design.md:43`） | 備考へ「**`M38-02`（`D-892`・2026-09-17）で導線を除去。ルート `/moves/edit` は存置し URL 直打ちで到達する**」 |
| **`DES-005` §3 の注記（`:131`）** | **★§2-1 の裁定が要る。** 「設定（画面16）配下のデータ管理導線として配置する」は **(a) 一度も実装されておらず (b) `M38-02` 以後はどこにも配置しない**。**⇒ 失効として消すか、未実装の記録として残すかを決めてほしい** |
| `DES-005` §5.16（設定） | **as-built の記録**＝「**項目一覧に技編集への導線は元から無い**」（§1-1 の実測。**新たに消したのではない**） |
| `DES-005` §5.18（moves 編集グリッド） | **★「本画面への導線は持たない」を明文化。** あわせて **(i)** 外した理由（**利用者に見せないため。★技データの是正はマイグレーションで行うので、本画面はその手段ではない**）、**(ii)** **コード・ルート・テストを温存した理由（★削除コストが高かったから**であり、積極的に活かす予定があるからではない）、**(iii)** **URL 直打ちでの到達は残す**、の 3 点

> **★★★2026-09-17 の開発者の訂正により、(i)(ii) は指示書 §0.1 / §0.2 の逐語から差し替わっている。⇒ 下の §2-3 を参照。** |

**★スキーマ変更 0 ／ API 経路の新設・変更 0 ／ マイグレ 0。**

### CHANGE 原稿 **B** — 「引っ越し取込」→「他から引っ越し」への改名（**2 度目の改名**）

| 反映先 | 書くこと |
|---|---|
| **`DES-005` §2 画面一覧 行19**（`05-screen-design.md:44`） | **★ここにも旧名が在る**（`followup` の `des005-screen-inventory-table-missing-rows` と同じ面）。「**引っ越し取込(他のアプリ・メモ・表計算から)**」→「**他から引っ越し(他のアプリ・メモ・表計算から)**」。**★括弧書きは残す**（開発者が 3 案から選択＝§3-1 の 1） |
| `DES-005` §5.14 の連携口注記（`:1386`） | 「**引っ越し取込からの連携口**」→「**他から引っ越しからの連携口**」 |
| `DES-005` §5.19 の節見出し（`:1718`） | 「**5.19 引っ越し取込**」→「**5.19 他から引っ越し**」。**★改名履歴を 2 段にする**（`CHANGE-147`・`M24-07` で「取込ヘルパー」→「引っ越し取込」／ **`M38-02`・`D-892`・2026-09-17 で「他から引っ越し」**） |
| **`DES-005` §5.19 の性質欄** | **★§1-2 の 1 行を足してほしい**＝「i18n キーは追加しない」の帰結として「**表示語の正本は `Header.tsx` の `NAV_LINKS` と `IntakeHelperPage` の `h1` である（`ja.json` に本画面のキーは無い）**」 |
| `DES-005` `:2225`（「入力面」の定義） | 「**★★★「入力面」に引っ越し取込は含めない**」→「**他から引っ越し**」（2026-09-10 開発者判断の中身は不変） |
| **`DES-003`**（`03-data-model.md:350`） | `setup_only` 列の説明中の「引っ越し取込」→「他から引っ越し」 |

**★ルート `/import/combo/helper` は変えない**（ブックマークが切れるため＝指示書 §3-2）**。★スキーマ変更 0 ／ API 経路の新設・変更 0 ／ マイグレ 0。**

**★★`DES-005` 以外にも旧名が在る点に注意**——上記 `DES-003` の 1 件は、**製造の走査が `docs/design/` 全体を対象にして初めて見つかった**（`grep` を `05-screen-design.md` だけに掛けていたら落ちていた）。

---

## §7 教訓（`retrospective-log` 行き）

> **★親は本節を読まなくてよい。** 宛先は `retrospective-log` へのバッチ反映であり、実施者は設計担当（反映担当の廃止＝2026-08-11 に伴う）。

### §7-1 ★★★**「射程を箱で切る」と、箱の外が静かに落ちる**（`M38-02` 高-1）

**実際に起きたこと**＝段 3b ② の「取込ヘルパー」全数走査を **`web/` ／ `internal/` ／ `cmd/` ／ `docs/usermanual/` の 4 箱**に切った。**⇒ `migrations/` の 1 件が視界に入らず、レビューが検出した。**

**★`D-881`（`head`/`tail` で切らない）の一般形がもう 1 つ在る**——**`D-881` は「出力を切るな」であったが、本件は「入力を切るな」である。⇒ 走査の*範囲*を絞った時点で、絞った外の件数は 0 に見える。**

**⇒ 機構化**: **全数を数えるときは、まずリポジトリ全体へ掛けてから射程外を*引き算*で除外する。⇒ 最初から射程内だけを走査しない。** 本サブの §3-1 の表（「リポジトリ全体 102 → 射程 31 ／ 射程外 71」）はその形になっており、**この形だったら `migrations/` は 71 の内訳に現れて気づけた**。

### §7-2 ★★**改名は連鎖する。⇒ 「退けた語」の台帳は、既存行の `useInstead` も腐る**

**実際に起きたこと**＝`retired-words.test.ts` の `取込ヘルパー` エントリの `useInstead` が「引っ越し取込」であり、**その値自体が本サブで退けた語になった**。

**⇒ 機構化**: **禁則語を足すときは、既存エントリの `useInstead` が今も生きた語かを必ず確かめる。** 本サブでファイル冒頭にその 1 行を足した（`retired-words.test.ts:13-16`）。

**★あわせて「陰性対照」も連鎖する**——同ファイルの「採った語が 1 件以上当たる」テストは、**改名した瞬間に 0 件で落ちる唯一のテスト**であった。**⇒ 改名と同じ手番で対照も移すこと。**

### §7-3 ★★**テストの「題」が実態を偽ると、守りが無いことに誰も気づかない**（`M38-02` 中-6）

**実際に起きたこと**＝`Header.test.tsx` の 1 本目は **「NAV_LINKS の全リンクを表示」** という題でありながら、**11 本のうち 6 本しか主張していなかった。⇒ 技編集を外しても戻しても赤くならなかった。**

**★これは「失効した記述」の一種である**——動作は正しいままなので **lint も型検査も緑**であり、**題を読んだ人が「全部守られている」と誤読する。**

**⇒ 機構化**: **「全 N 件」を主張する題のテストは、配列を持って回すか、件数そのものを assert する。** 本サブは**名指しの全 11 本 ＋ `getAllByRole("link").length` の件数チェック**へ作り替えた（名指しだけだと「知らないリンクが増えた」ことに気づけないため）。

### §7-4 ★**指示書の前提が as-built と食い違ったとき、止めるか進めるかの判定**

**実際に起きたこと**＝指示書 §0.1 が「設定画面から」と名指ししたが、導線はヘッダに在った（§2-1）。

**⇒ 判定の型**: **設計書*間*の矛盾なら止める（`CLAUDE.md` §8）。設計書と*実装*の乖離なら進めて §2 で裁定を仰ぐ。** 本件は後者であり、**かつ指示書 §2.1-2 自身が「★設定画面だけとは限らない」と全数走査を求めていた**（＝指示書が乖離の可能性を織り込んでいた）。**⇒ 指示書が「実測せよ」と書いている箇所は、書かれた前提より実測が優先する。**

### §7-5 ★★★**「他サブの手番だから触るな」は、その手番が閉じた瞬間に失効する。⇒ 誰も消しに来ない**（`M38-02` §2-4）

**実際に起きたこと**＝指示書 §3-5 が「`dist-readme.txt` を触るな（`M36-01` の手番・`D-887`）」と禁じていたが、**`D-890` が `M36-01` を受理したのは `M38` 起票の `D-892` と同じ 2026-09-17 であった。⇒ 発行時点で既に前提が消えていた可能性が高い。**

**★禁止そのものは静的なテキストだが、根拠は「他サブが進行中である」という*時点依存の状態*である。⇒ 状態が変わっても文は変わらない。**

**★★製造は明文に従って停止し、開発者へ確認を上げた。⇒ 正しい振る舞いだが、往復 1 回分のコストが乗った。しかも製造は「他サブが閉じたか」を自分で確かめる手段を持たない**（取り込み系の git 操作も `mcp__*` も `deny`）**。**

**⇒ 機構化（設計卓側）**: **指示書へ「他サブの手番だから触るな」を書くときは、(a) 根拠の裁定番号 ＋ (b) 「その手番が閉じたら本項は失効する」の 1 行を必ず添える。** 本件は (a) は在ったが (b) が無く、**製造には「`D-887` が今も生きているか」を判定する材料が渡っていなかった。**

**★★あわせて製造側の教訓**——**禁止に当たったら、まず根拠の裁定を読んで*それがまだ生きているか*を確かめる。⇒ 本サブは確かめずに「裁定で上書きした」と報告し、後から自分で是正した**（`367aa5f`）**。★「指示書に反した」と「前提が消えていた」は、設計卓にとって意味がまったく違う。**


---

*以上、設計伝達レポート `M38-02`。* **★★★§2 の 2 件と §4-3 の裁定請求が本書の本体である。**
