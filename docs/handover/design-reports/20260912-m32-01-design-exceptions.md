# 設計伝達レポート（例外レポート） — `M32-01`（操作説明書の器と `README` の整理）

| 項目 | 内容 |
|---|---|
| **対象** | **親チャット（設計卓）** |
| **発信** | 製造担当 Claude Code（2026-09-12） |
| **指示書** | `docs/instructions/M32-01-usermanual-shell-and-readme.md` **v1.1.0** ／ 上位 `M32-overview` v1.2.0 |
| **実装コミット** | ブランチ `claude/charming-goodall-n3r99h`（**push 済み。設計卓は HEAD で読める**） ／ **9 コミット**（`582106f` 器 ／ `427de9f` 正本と生成経路 ／ `5ddd6e4` `README.md` ／ `6e39a7b` 是正 2 件 ／ `7b9921e` 完了報告 ／ `91893ac` レビュー取り込み ／ `24a5a22` レビュー結果と索引行 ／ `3082fee` 本レポート初版 ／ **`8b40495` 開発者裁定 2 件の反映**） |
| **関連** | 完了報告 `docs/progress/M32-01-completion-report.md` ／ レビュー `docs/progress/m32-01-review.md` |
| **★チェックリスト** | **不在**。`docs/instructions/reviews/M32-01-review-checklist.md` は起票されていなかった（`D-736` の運用では着手承認の手番で設計卓が起こす）。**⇒ レビューは指示書 §5 / §6 ＋ `M32-02` §2.1〜§2.3 を観点に代用した** |
| **レビュー集計** | **高 2 / 中 5 / 低 6 ＝ 全 13 件・採用 13 / 不採用 0・「高」の不採用 0 件・再レビュー往復 0 回（上限 2）** |
| **源泉** | 完了報告 ＋ レビュー報告書 ＋ **開発者との同一セッション内のやり取り**（着手前の 4 件の判断 ＋ **本レポート初版への裁定 2 件**。いずれも逐語を含む） |
| **CHANGE / マイグレ / 依存** | **CHANGE 0 本**（自採番しない） ／ マイグレ 0 本 ／ **新規依存 0 件**（`go.mod` / `go.sum` / `web/package.json` の差分 0 行） |

本書は **①独自確定仕様 ②契約違反の独自判断 ③製造判断 ④残課題**に絞る。指示書どおりの部分は割愛する。

**★★§2 の 2 件は、本レポートの初版を開発者が読んで裁定済みである**（2026-09-12）。**⇒ `README.txt` 側は反映した。★残るのは `DES-002` §11.2 の書き換えであり、設計卓の手番である**（§6-1）。

**★最重要は §4-1 と §4-2 である**——**操作説明書への到達経路が存在しないこと**と、**`B4` の残り（本文）を `M32-02` が書くまで両 `README` に暫定表記が乗っていること**は、どちらも公開の可否に直結する。

---

## §1 製造が独自に確定した実装仕様（DES 反映が要るもの）

### §1-1 ★★`README.txt` の生成経路の as-built（`DES-002` §11.2 へ 1 段落）

**正本と生成物の関係が新設された。`DES-002` §11.2 は `README.txt` を配布物の構成要素として挙げているだけで、それがどこから来るかを書いていない。**

| 役割 | パス |
|---|---|
| 正本 | `docs/usermanual/dist-readme.txt`（プレーンテキスト・**173 行**） |
| 生成物 | `README.txt`（ルート・**179 行**。**移していない**） |
| 生成・照合 | `scripts/check-dist-readme.sh` |

**生成規則＝正本の全文 ＋ 末尾の生成注記ブロック（6 行。179 − 173 の差がそれである）。** 注記は次の逐語で、**日付・コミットハッシュを含まない**（含めると照合が毎回赤になり、赤が常態化して検出力を失う。`cmd/seedgen/main.go` の `diffHint` 周りが同型の落とし穴を明記している）。

```
--------------------------------------------------
※ ここから下はリポジトリを扱う人向けの注記です(利用者の方は読み飛ばしてください)。
  本ファイルは docs/usermanual/dist-readme.txt から生成されています。
  修正はそちら(正本)を直し、bash scripts/check-dist-readme.sh --write で作り直して
  ください。本ファイルを直接編集すると、次の生成で巻き戻ります。
```

**⇒ `DES-002` §11.2 へ「`README.txt` は `docs/usermanual/dist-readme.txt` から生成した成果物であり、両者の一致は `scripts/check-dist-readme.sh` が検査する」を明文化してほしい。** 書かないと、**次に `README.txt` を直す担当が生成物のほうを編集し、次の `--write` で黙って巻き戻る。**

### §1-2 ★★スクリプトの契約（**引数なしが照合である**）

| モード | 動作 | 終了コード |
|---|---|---|
| **引数なし** | **照合**（正本から生成し直した内容と `README.txt` を byte 比較） | **0＝一致 / 1＝差分 / 2＝実行エラー** |
| `--write` | 正本から `README.txt` を生成 | 0 / 2 |
| `--self-test` | 陽性対照・陰性対照 | 0 / 1 / 2 |

- **★持たせなかった分岐**——**`--check` は無い**（引数なしがそれである）。**`--diff` も `--quiet` も無い。** 不明な引数はすべて **exit 2**。**⇒ 返らない分岐を実装に残していない。**
- **★既定を照合にしたのは `D-766` の帰結である**（`combomgr`＝手動ないし常設検査の表 ／ `tacpendium`＝自動毎回）。**呼び出し側を選ばないためには、引数なしの 1 コマンドで照合できる必要がある。** あわせて、既定が書き込みでないため、うっかり実行しても正本・生成物のどちらも壊れない。

### §1-3 ★操作説明書の器の構造（`M32-02` の前提になる）

`docs/usermanual/tacpendium-readme.html`（714 行）。**22 章のスケルトンのみで、本文は 1 行も無い。**

- **章順は `M32-02` §2.2 の段割りと段内の順序をそのまま採り、5 つの部へ読者向けの名を付けた。** 並べ替えていないのは、本文を段ごとにコミットする運用と章順をずらさないため。
- **章番号は CSS カウンタ**（`counter(ch, decimal-leading-zero)`）。旧版の手書き 2 桁は、章を 1 本挿すたびに以降が全部ずれる。目次側も独立したカウンタで振ってある。
- **スクリーンショットの置き場＝`docs/usermanual/images/`、命名＝`ch<NN>-<slug>-<n>.png`。** 各章に 1 枚ぶんの `<figure>` を置いてある（計 22 枚ぶん）。**★撮影と貼り込みは開発者の手番。**
- **★`.src` / `.toc-src`（`DES-005` の節番号）を出したままにしてある。** 執筆時の対応付けに要るため。**`M32-02` の完了時に「出す／隠す」を決めること**を器の CSS コメントへ残した。

**⇒ `DES-005` への反映は要らない（器は画面定義を変えない）。本節は `M32-02` の指示書を書くときの前提として読んでほしい。**

---

## §2 契約・設計に反する独自判断（★独立カテゴリ・最優先）

### §2-1 ★★`README.txt` が案内する実行ファイル名が、`DES-002` §11.2 の zip 構成と食い違っている（**製造は直していない。裁定が要る**）

| 出所 | 何と書いてあるか |
|---|---|
| **`DES-002` §11.2**（逐語） | `tacpendium-windows-amd64.zip` の中身は **`tacpendium.exe`** ＋ `README.txt` |
| **`Makefile:70`**（as-built） | 出力は **`dist/tacpendium-windows-amd64.exe`** |
| **`README.txt:18`**（起動方法） | **`tacpendium-windows-amd64.exe`**（Makefile と一致） |
| **`README.txt:94`**（ファイアウォール手順） | `program="(`**`tacpendium.exe`**` のフルパス)"`（**`DES-002` と一致し、Makefile と食い違う**） |

**★同じ 1 ファイルの中で、2 つの節が別の実行ファイル名を案内している。** `tacpendium.exe` という名の成果物を作るターゲットはリポジトリに存在しない（実査済み）。

#### ★★【2026-09-12 裁定済み】`tacpendium-windows-amd64.exe` へ統一する

**開発者の逐語＝「`make build-windows` だと `tacpendium-windows-amd64.exe` です。GitHub 上でビルドしても同じ名前になりますね？ `tacpendium-windows-amd64.exe` で統一してください」。**

**★問いには実査で答えた——なる。** `.github/workflows/nightly-crossbuild.yml` は `make build-all` を呼び、`dist/tacpendium-windows-amd64.exe` の存在を検査している。**⇒ ローカルビルドと CI ビルドは同一の名前を出す。`tacpendium.exe` は同ワークフローのコメントが `DES-002` §11.2 を引用している箇所にしか現れない。**

**★製造が反映した範囲**＝`README.txt`（正本 `docs/usermanual/dist-readme.txt`）のファイアウォール手順 1 行のみ。

```
        netsh advfirewall firewall add rule name="Tacpendium" dir=in ^
          action=allow program="(tacpendium-windows-amd64.exe のフルパス)" ^
          enable=yes profile=private
```

**★★残っているのは `DES-002` §11.2 である**（製造は設計書を編集しない＝指示書 §3-4 / `CLAUDE.md` §8）。**⇒ §6-1 の CHANGE で処理してほしい。★対象は 1 か所ではない**——`docs/design/02-architecture.md` の **`:25`（構成図の `単一バイナリ（tacpendium.exe）`）／ `:76`（シーケンス図の `participant Exe as tacpendium.exe`）／ `:1380`（§11.2 の zip 構成）** の 3 か所である（実査）。**★`:25` と `:76` は「単一バイナリである」ことを示す図であり、配布物名としての `tacpendium.exe` とは意味が違う可能性がある。⇒ 3 か所すべてを機械的に置換しないこと。**

**★★あわせて波及が 2 件ある。**

| # | 箇所 | 内容 |
|---|---|---|
| 1 | `.github/workflows/nightly-crossbuild.yml:105` | **`DES-002` §11.2 を引用したコメント**（`中身 = tacpendium.exe + README.txt`）。**⇒ `DES-002` が変われば、ここも合わせる必要がある。★製造は `.github/` を書き換えない**（指示書 §3-5） |
| 2 | `followup` `release-zip-missing-license-files` | **同じ逐語を根拠として引用している。⇒ 根拠の文面が変わる** |

### §2-2 ★`DES-002` §11.1 が求める「公式／非公式の明示」が `README.txt` に無い（**製造は直していない**）

`DES-002` §11.1 の逐語＝「**README等のドキュメントには「Windowsを公式サポートとし、macOS・Linuxは非公式対応」と明記し、期待値のずれを防ぐ**」。

ところが `README.txt` の起動方法は 3 OS を**並列に**案内しており、公式／非公式の別を一言も書いていない。**⇒ 設計書の要求が as-built で満たされていない。**

#### ★★【2026-09-12 裁定済み】いま追記する

**開発者の逐語＝「射程外ですが今のうちに追記してください」。⇒ 反映した。**

起動方法の 3 行へ区分を付け、直後に段落を 1 つ置いた（正本 `docs/usermanual/dist-readme.txt`）。

```
1. 実行ファイルをダブルクリック(または端末から実行)します。
     - Windows : tacpendium-windows-amd64.exe   (公式サポート)
     - macOS   : tacpendium-darwin-arm64        (非公式対応)
     - Linux   : tacpendium-linux-amd64         (非公式対応)
```

```
※公式サポートは Windows(amd64)のみです。macOS・Linux 版もビルドして配布して
  いますが、動作検証と問い合わせ対応は行っておらず、動作保証はありません。
  お使いいただけますが、自己責任でお願いします。
```

**★`DES-002` §11.1 の逐語**（「動作検証と問い合わせ対応のリソースは限られるため、公式サポートはWindowsのみに絞る」）**に沿わせた。⇒ 設計書側の変更は要らない**（設計書は「明記せよ」と書いており、足りなかったのは実装側である）。**★CHANGE 不要。**

**★★`DES-002` §11.1 と §11.2 の macOS アーキテクチャの食い違い**（`followup` `des002-macos-arch-mismatch`。§11.1 は amd64 / arm64 の両方を非公式対応とするが §11.2 は arm64 のみ）**には触れていない。** `README.txt` は `tacpendium-darwin-arm64` の 1 点だけを案内しており、**Makefile の実物**（`GOOS=darwin GOARCH=arm64` のみ）**と一致している。⇒ 本サブでは矛盾が顕在化しない。**

---

## §3 製造の判断

### 3-1 開発者へ確認して確定した点（2026-09-12）

**★1〜4 は着手前、5〜6 は本レポート初版を読んだ後の裁定である。**

| # | 事項 | 開発者の選択 | 効いたところ |
|---|---|---|---|
| 1 | **器の形式・分割** | **HTML 単一ファイル** | `D-742` の逐語「マニュアルなら html 化したい」と overview §3.1-3「改名して器として再利用する」に沿う。**⇒ Markdown 複数ファイル案は採らなかった** |
| 2 | **既存「お試し版」本文 7 章** | **本文を差し替える**（ファイルは残す） | overview §3.1-3「中身は書き直す前提である」と一致。**★削除ではないため `D-196` には触れない** |
| 3 | **位置づけの文** | **反映する（`README.md` / `README.txt` のみ）** | `D-767` の割付どおり。§3-2 に文面 |
| 4 | **スクリプトの名前と既定動作** | **`check-dist-readme.sh`・既定＝照合** | §1-2 |
| **5** | **実行ファイル名の食い違い**（§2-1） | **`tacpendium-windows-amd64.exe` へ統一する** | `README.txt` は反映済み。**★`DES-002` の 3 か所が残る**（§6-1） |
| **6** | **サポート方針の明示**（§2-2） | **今のうちに追記する** | `README.txt` へ反映済み。**★設計書の変更は不要** |

**★4 のうち「名前」は指示書 §2.3-2 の例示（`generate-*` が先例）から外れる。** 選定理由は 2 つ——**(a) 既定が照合であること ／ (b) `check-artifact-integrity.sh` が `scripts/check-*.sh` 全数に `--self-test` を機械的に要求し、指示書 §4.4 が警戒した「何も検出しない形」が常設で潰れること。** **⇒ 追認をお願いしたい。** 却下なら改名は安い（参照は `CLAUDE.md` 追記案と本レポートのみで、実装からは呼ばれていない）。

### 3-2 推測で進めた点

| # | 箇所 | 推測の内容 |
|---|---|---|
| **1** | **位置づけの和文定義文** | **`D-767` は英語側を `SF6 Tactics Workbench` に決着させ、和文は設計卓案「SF6 の戦術ワークベンチ」を示したうえで「開発者が別案を出したらそちらに従う」としている。⇒ 設計卓案に沿って製造が 1 文を書いた。** `README.md:3`＝「ストリートファイター6(SF6)の戦術ワークベンチ。コンボ・セットプレイ・確定反撃を記録し、整理・比較・共有するための Web アプリケーション。」／ `README.txt:9-12`＝同趣旨。**★別案があれば差し替える（正本 1 か所の修正で済む）** |
| 2 | **`B6` を 3 か所に置いた** | 指示書 §2.4-4 は「置き場は 2 か所である（`README` ／ 操作説明書）」と書いているが、**`README` が 2 つある。⇒ `README.md` / `README.txt` / 器の 3 か所へ置いた。** 理由＝公開リポジトリで最初に開かれるのは `README.md` であり、モダン操作の人がリポジトリを覗いた時点で分かる必要がある |
| 3 | **指示書 §5-4（否定形確認）の読み替え** | §4-5 に詳述 |

---

## §4 設計担当が未把握の残課題・申し送り

### §4-1 ★★★操作説明書への到達経路が存在しない（**`B4` の価値がここで止まる**）

`README.md` / `README.txt` はどちらも `docs/usermanual/tacpendium-readme.html` を名指ししている。**しかし利用者が実際に読み着く経路が無い。**

| # | 塞がっている理由 |
|---|---|
| a | **URL が書けない**——`P-46`（公開リポジトリの運用形態）が未決で、公開リポジトリの所在が確定していない |
| b | **zip に同梱されない**——`DES-002` §11.2 の構成は exe と `README.txt` のみ |
| c | **GitHub 上の `.html` はソース表示になる**——ブラウザで読める形にならない |

**⇒ 決着が要る。** 選択肢は「zip へ同梱する（`DES-002` §11.2 の改訂＝CHANGE）」「GitHub Pages 等で公開する（`P-46` に従属）」「`README.txt` へ要点を畳む（器の意味が薄れる）」あたりで、**いずれも製造の手番ではない。**

**★`M32-02` が本文を書き終えても、この経路が無ければ誰にも届かない。⇒ `M32` の完了判定（`D-757`＝`M32-01` ＋ `M32-02` で完了）に含めるかを含めて決めてほしい。**

### §4-2 ★★両 `README` に暫定表記「現在準備中です」が乗っている（**`M32-02` が外すこと**）

レビュー 高-2 の取り込みである。**器は 22 章すべてが `.todo`・画像 22 枚が未配置であり、「操作説明書にあります」と断定すると、`M32-02` の前に公開が起きた場合に案内どおり辿った利用者が空の器に着く。**（`README.txt` は公開スナップショットの `ALLOW` かつリリース zip の同梱物である。）

**★外す箇所は 4 つ**——`README.md` の冒頭 blockquote ／ 同 4 ファイル役割表 ／ `docs/usermanual/dist-readme.txt` の冒頭と末尾。**`README.txt` は生成物なので、正本を直して `--write` で作り直すこと。**

**⇒ `M32-02` の指示書へ完了条件として入れてほしい。★入れないと「準備中のまま完成した」という逆向きの失効が残る**（本プロジェクトのレビュー較正では「高」に当たる型）。

### §4-3 ★`followup-backlog.md` の登録・更新候補（**製造は 1 文字も編集していない＝`D-838`**）

| # | スラッグ | 新規／更新 | 何が起きるか | 根拠 | 割付の候補と理由 |
|---|---|---|---|---|---|
| 1 | `usermanual-filename-still-combmgr` | **更新（解消 → 畳む）** | ファイル名は `docs/usermanual/tacpendium-readme.html` になった | `582106f` ／ 完了報告 §1.5 | **畳んでよい。**`D-766` の決着どおり |
| 2 | `readme-md-and-txt-coexist` | **更新（解消 → 畳む）** | どちらが何の正本かが、両方の冒頭と `README.md` の 4 ファイル役割表から読める | 完了報告 §7 ／ `README.md` §ドキュメント | **畳んでよい** |
| 3 | `usermanual-not-written` | **更新（部分解消）** | **器だけ立った。本文は未執筆。** | 完了報告 §4 | **`M32-02` へ残す**（状態欄に「器は `M32-01` で完了」を足す形） |
| 4 | `product-positioning-statement-outdated` | **更新（部分解消）** | **`README.md` / `README.txt` の 2 か所は新しくなった。`CLAUDE.md` / `AGENTS.md` / `REQ-001` の 3 か所は古いまま** | `5ddd6e4` ／ `427de9f` | **★`D-767` の割付どおり残り 3 か所は別担当。⇒ 現時点で「2 か所だけ新しい」状態であることを状態欄へ書いてほしい** |
| 5 | **`usermanual-no-reachable-path`**（新規案） | **新規** | 操作説明書に利用者が辿り着く経路が無い（URL 不可・zip 非同梱・GitHub でソース表示） | 本書 §4-1 ／ `DES-002` §11.2 ／ `P-46` | **開発者 ＋ 設計卓。**`P-46` に従属するため、`P-46` の決着と同時に判断するのが安い |
| 6 | **`readme-exe-name-mismatch`**（新規案） | **新規** | `README.txt` 内の 2 節が別の実行ファイル名を案内し、片方は存在しない成果物名 | 本書 §2-1（`DES-002` §11.2 ／ `Makefile:70` ／ `README.txt:18` `:94`） | **開発者の裁定**（zip 内で改名するかどうかの事実確認） |
| 7 | **`readme-support-tier-not-stated`**（新規案） | **新規** | `DES-002` §11.1 が求める「Windows 公式 / macOS・Linux 非公式」の明示が `README.txt` に無い | 本書 §2-2 | **設計卓**（`M32-02` か改善レーンへ割付。正本へ 2〜3 行） |
| 8 | **`b6-classic-only-statement-triplicated`**（新規案） | **新規** | `B6` が `README.md` / `README.txt` / 器の 3 か所にあり、正本を名指しする仕掛けが無い。モダン対応時に 3 か所を同時に直す必要がある（`E-76`） | レビュー 低-4 ／ 完了報告 §8 | **設計卓**（モダン対応の検討時に一緒に見る） |
| 9 | **`roles-and-routing-stop-record-outdated`**（新規案） | **新規** | `docs/handover/roles-and-routing.md` の「停止時の記録先＝`followup-backlog.md` §J へ直接」が `D-838`（2026-09-12）で失効している | レビューが独立に検出（`m32-01-review.md` 指摘 8） | **設計卓。★本サブ由来ではないが、現役資料に失効した記述が残っている** |

### §4-4 ★`M32-overview` §1.1 の `B3` 逐語が失効している

`checklist` §2 側は `D-764` で `2026-08-03` へ是正済みだが、**`M32-overview` §1.1 の `B3` 逐語は `2026-08-04` のまま**である。**⇒ overview §5-3 が自ら「実査項目」に挙げていた食い違いであり、片側だけ直った状態が残っている。**

### §4-5 ★★指示書 §5-4（否定形確認）は達成不能である（**以後の改名サブの前提**）

指示書 §5-4 は「旧名 `combmgr-friend-readme.html` を名指しする箇所が **0 件**であること」を求めている。**★これは満たせない。**

着手時 27 件のうち **25 件は歴史記録**（`parallel-board` ／ 完了報告 ／ レビュー ／ 設計伝達レポート ／ CHANGE 通知 ／ `followup-backlog` ／ 指示書本体）であり、**本プロジェクトの運用では書き換えない**（`D-274` (3)）。

**⇒ 「生きている参照が 0 件」で判定した。** 実際に直したのは 2 件（`.claude/commands/cleanup_docs.md:251` ／ `docs/human-notes/future-notes/INDEX.md:53`）で、**ルール面・運用面のヒットは 0 件**である。

**★改名を伴うサブの否定形確認は、以後この読み替えを前提に書いてほしい。** 「0 件」と書くと、達成不能な条件が完了条件に残り、**製造が「達成した」と書くか「未達」と書くかのどちらかで嘘になる。**

### §4-6 ★レビュー低指摘の繰越

**★不採用は 0 件である。** 低 6 件はすべて採用し、うち **4 件はこの手番で直した**（`--self-test` の終了コード判定 ／ 一時領域と `trap` ／ `sed -i` の GNU 依存 ／ 未使用 CSS の使い道をコメント化）。**残る 2 件は記録として扱った**——`B6` の三重化（§4-3 の 8）と `custom-commands.md` / `roles-and-routing.md` の陳腐化（§4-3 の 9 ＋ 下記）。

**★`check-derived-docs.sh` が `docs-map`（6 / 1552 件）と `custom-commands`（14 / 34 件）を陳腐化疑いと報告する。** 前者は本サブが `docs/` へ足したため、後者は `.claude/commands/cleanup_docs.md` に触れたため源泉コミットが動いたものであり、**掲載一覧そのものに変化は無い。⇒ 再生成は設計卓・改善レーンの手番。**

### §4-7 ★停止時記録（§J 行の原稿）

**該当なし。** 停止規律の上限（再レビュー往復 2 回 ／ タイムボックス ／ 開発者の終了指示）には達しておらず、**未解消のまま停止した項目は無い**（レビュー 13 件は全件採用・往復 0 回）。

---

## §5 参考（触れていない＝不変の証跡）

- `go.mod` / `go.sum` / `web/package.json` / `web/pnpm-lock.yaml`: `git diff 6cd4ce5` 空（新規依存 0 件）
- `internal/` / `cmd/` / `web/src/` / `migrations/`: `git diff 6cd4ce5` 空（実装に 1 行も触れていない）
- `docs/design/` 配下: `git diff 6cd4ce5` 空（`DES-002` §11.2 を含め設計書は未編集）
- `.github/` / `Makefile` / `CLAUDE.md`: `git diff 6cd4ce5` 空（指示書 §3-5）
- 既存 `scripts/check-*.sh` 14 本: `git diff 6cd4ce5` 空（`D-335`。足したのは新規 1 本のみ）
- `docs/handover/followup-backlog.md`: `git diff 6cd4ce5` 空（`D-838`）
- `go test ./...` exit 0（60 パッケージ ok / FAIL 0） ／ `cd web && pnpm test` exit 0（228 ファイル・2795 件）
- 常設検査 7 本 ＋ `check-dist-readme.sh` ＋ `--self-test`: すべて exit 0（**測定時点＝本レポート手交時。開発者裁定 2 件の反映後に取り直した**）

---

## §6 CHANGE 起票のたたき台（設計担当向けチェックリスト）

**本サブは CHANGE を 1 本も消費していない。マイグレも 0 本である**（`ls migrations/` の実査値は不変）。**以下は起票の候補であり、製造は自採番していない**（`D-293`）。

**★★初版からの差分**——**開発者が §2 の 2 件を裁定した**（2026-09-12）。**⇒ 3 は不要になり、1 は「事実の確認待ち」から「起票してほしい」へ変わった。**

| # | 対象 | 内容 | 状態 |
|---|---|---|---|
| **1** | **`DES-002` §11.2** ＋ 同 `:25` `:76` | **★★裁定済み＝実行ファイル名を `tacpendium-windows-amd64.exe` へ統一する**（開発者・2026-09-12）。**⇒ `README.txt` 側は反映済み。設計書 3 か所が残っている**（§2-1）。**★`:25` / `:76` は「単一バイナリである」ことを示す図であり、配布物名と意味が違いうる。機械的に置換しないこと。★波及＝`.github/workflows/nightly-crossbuild.yml:105` のコメントと `followup` `release-zip-missing-license-files` の根拠文面** | **★起票してほしい**（判断は済んでおり、反映先の切り分けだけが残る） |
| **2** | **`DES-002` §11.2**（別件） | **`README.txt` が `docs/usermanual/dist-readme.txt` からの生成物であることと、照合コマンドを明文化する**（§1-1） | **★起票してほしい**。書かないと次の担当が生成物を直して巻き戻る |
| 3 | `DES-002` §11.1 | サポート方針の明示を `README.txt` へ足す（§2-2） | **★★不要。反映済み**（開発者裁定「今のうちに追記してください」）。設計書は既に「明記せよ」と書いており、足りなかったのは実装側だった。**⇒ 設計書の変更は無い** |

**★1 と 2 は同じ §11.2 に当たる。⇒ 1 本の CHANGE にまとめられる可能性が高い**（切り分けは設計卓の判断）。

**番号は起票時に `docs/handover/change-number-registry.md` §1 で採番すること。** 本サブは番号を 1 つも消費していないため、registry §1 ／ 契約 §4 ／ ボード §2.1 ／ ボード §2.4 の「次の番号」は**いずれも動かない**。

---

## §7 教訓（`retrospective-log` 行き）

> **★親は本節を読まなくてよい。** 宛先は `retrospective-log` へのバッチ反映であり、実施者は設計担当である。

1. **★★「完了報告の表が求める測定時点」と「実際に測った時点」がずれると、未了の条件が緑として記録される。** 本サブでは §6.5 の見出しが「Phase C の後に、すべてのファイルを足し終えてから回す」と自ら書いていたのに、実際に回したのは完了報告を書く前であり、`check-progress-log-index.sh` を「exit 0」と記録していた（実際は exit 1＝索引行が無い）。**⇒ 検査結果を表へ書くときは、測定時点を同じ行に書くこと。** 時点が書かれていない緑は、いつの緑か分からない。**★`D-510` の一般形である**——同裁定は「レビュー結果を参照する欄」を対象にしていたが、**同じことが「自分で回した検査の欄」でも起きる。**
2. **★★対照（self-test）の合否を真偽値で見ると、道具が壊れた状態を「期待どおり」と読む。** 本サブの `--self-test` は `if compare …; then ❌ else ✅` の形で、`compare` が返す 1（差分）と 2（実行エラー）を区別していなかった。**レビューが false green を実証した**（`compare` が常に 2 を返す複製で「自己検査: 合格」が出て exit 0）。**⇒ 対照は「期待する終了コードと一致するか」で見ること。** `-ne 0` は合格条件にならない。
3. **★`scripts/check-*.sh` を新設したら、`check-artifact-integrity.sh` が拾うマーカー文言（`自己検査: 合格`）を先に確かめる。** 本サブは自己検査を実装していたのに文言が違い、「宣言だけで実装が無い」と赤で検出された。**⇒ 機構は動いた。★同時に、「自己検査を書いた」という宣言が検査機構に繋がっていない状態が実在しうることの実例でもある。**
4. **★`RETURN` trap ＋ コマンド置換は片づかない。** `d="$(scratch …)"` の中で `mktemp -d` を打つと、(a) サブシェルでの変数代入は親へ戻らず (b) サブシェルでは trap が既定へ戻るため、一時ディレクトリが誰にも消されない。**実測で 5 個漏れた。⇒ 一時領域は最上位で 1 度だけ作り、`EXIT`/`INT`/`TERM` の trap を 1 つだけ置く。**
5. **★改名を伴うサブの「旧名 0 件」は達成不能である。** 歴史記録（完了報告・レビュー・board・CHANGE 通知）は書き換えない運用のため、旧名は必ず残る。**⇒ 指示書は「生きている参照が 0 件」と書くこと。** 達成不能な完了条件は、製造がどちらに書いても嘘になる（`E-84` と同型＝「0 件」と「検査していない／直せない」を同じ顔で出さない）。
6. **★設計卓が「武装中で読めない」と書いた実査項目は、製造の作業ツリーでは読めることがある。** 本サブでは `.github/` と `cmd/` の 2 か所がそれに当たり、**逐語を埋めた結果、`DES-002` §11.2 と現物の食い違い（§2-1）が新しく見つかった。⇒ 「読めない」と書かれた項目は、製造の手番で必ず埋めること。埋めると新しい食い違いが出る。**
7. **★生成物と正本のドリフト検査は、2 方向を確かめる。** 「正本を直して生成し忘れた」と「生成物を直接編集した」は別の事故であり、片方しか確かめないともう片方が素通りしていても気づけない。**本サブは 3 方向で測った**（上記 2 つ ＋ `--write` で追随すること）。
