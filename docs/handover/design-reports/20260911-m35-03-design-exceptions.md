# M35-03 設計伝達レポート（例外レポート）

| 項目 | 内容 |
|------|------|
| 対象 | **親チャット（設計卓）** |
| 発信 | 製造担当 Claude Code ／ 2026-09-11 |
| 対象指示書 | `docs/instructions/M35-03-rush-original-move-code-typo.md` **v1.0.0** ／ チェックリスト `docs/instructions/reviews/M35-03-review-checklist.md` v1.0.0（**`D-816`・★事後発行**） |
| 実装コミット | `claude/keen-mayer-y7ri0t`。着手基点 `23b8a08`。`a1a4d8d`（チェックリスト配置）／ `671f650`（CSV 正本 ＋ golden 3 本 ＋ 版固定テストの追随）／ `6678076`（追随マイグレ `000111` ＋ 契約テスト）／ `380e2d0`（破壊確認で空振りを検出し主張を移動）／ `5812603`（完了報告）／ `8684e20`（レビュー取り込み）／ `08d534c`（Phase D）／ `38c906f`（高-1 の裁定取り込み）。**push 済み**。実装差分（`docs/` 除く）＝**21 ファイル / +962 / −58** |
| ★新規ファイル | **3 本**（`internal/infra/migration/migrate_m3503_test.go` `403/0` ／ `migrations/000111_data_correct_rush_original_move_refs.up.sql` `183/0` ／ 同 `.down.sql` `91/0`）**はいずれも `+` のみ**（教訓 `E-225`） |
| 関連 | 完了報告 `docs/progress/M35-03-completion-report.md` ／ レビュー `docs/progress/m35-03-review.md` |
| CHANGE 消費 | **0 本**（見込みどおり）。**★ただし §6 に請求 2 本がある** |
| マイグレ消費 | **`000111` の 1 本**（`D-812` の払い出しどおり。自採番していない） |

---

本レポートは **①独自確定仕様 ②契約違反の独自判断 ③製造判断 ④残課題** に絞る。指示書どおりの部分（三点更新の各点・段 1 の実測値・破壊確認の一覧）は割愛する。**正本は完了報告であり、設計卓は読まない前提で書いている。**

**★最重要は §2-1 と §4-2 である。**§2-1 は**指示書の完了条件に反した判断**（開発者裁定で決着済みだが、裁定の内容そのものが契約の読み替えを含む）。§4-2 は**同じ 4 件を指す followup 行が 2 本あり、片方だけ畳むと後任が同じ調査をやり直す**という、設計卓にしか畳めない問題である。

---

## 1. 製造が独自に確定した実装仕様（DES 反映が要るもの）

### 1-1 ★★★実害は 2 件ではなく 3 件だった（`VAL-C12` 経由で保存が 400）

**指示書 §0.4 ／ followup `rush-original-move-code-typo` ／ `M35-02` 完了報告 §2.7 はいずれも「実害は 2 つ」と書いている。⇒ 3 件目が在った。**

| 経路 | 機序 |
|---|---|
| `internal/service/validation/combo.go:483-499` | `validateC12RushVariantOriginal` は、レシピ中の `rush_variant` について `FindOriginalMoveID` が `nil` を返したら `AddError`（**Error severity**） |
| `internal/service/combo/deps_adapter.go:60-74` | `moves.original_move_id` が NULL のとき `(nil, nil)` を返す |
| `internal/service/combo/service.go:368` | `result.HasError()` でトランザクションを巻き戻し、ハンドラが **400** を返す |

**★★4 技は入力面に出ていた**（`isOnUniqueTab=true`。完了報告 §2.3 の実測）**。⇒ 利用者は編集画面から選べるのに、保存すると `ラッシュ版 move id=N に対応する元技が見つかりません`で弾かれていた。★3 件の実害のうち、唯一エラーとして見えていた経路である。**

**★同分岐は着手時点でテストが 1 本も通っていなかった**——既存の `TestC12_*` 2 本はいずれも`ExistsForCharacter` 側の分岐（元技 id は解決するがキャラに存在しない）を見ている。**⇒ `origID == nil` の分岐は無検査だった。** 対の床を新設した（`internal/service/validation/combo_test.go` の `TestC12_RushVariant_OriginalIDIsNull`。分岐を外すと赤になることを破壊確認で実測）。

> **⇒ `DES-006` §（`VAL-C12` の欄）へ、次の 1 点を明文化してほしい。** **`VAL-C12` は 2 つの分岐を持つ**——(a) `original_move_id` が **NULL**（＝seed 時に元技を解決できなかった）／ (b) 解決するがそのキャラに存在しない。**(a) は「データ不整合のラッシュ版はレシピに入れられない」という利用者向けの契約であり、(b) とは原因も直し方も違う。★現行の記述はこの 2 つを書き分けていない。**
>
> **★値（`ingrid` 等の技名）は書かないこと。** seed 波ごとに陳腐化する。

### 1-2 ★★規約 (16) の `grep` 手順は golden の連番では取りこぼす

`SUPP-001` §5.5.4 規約 (16)（`CHANGE-178`・`M35-02` 新設）の末尾は「**golden を再生成する前に、その版を終端にしている契約テストを `grep` で探すこと**」とだけ書いている。**⇒ 本サブが実地の反証を得た。`CHANGE-178` が初めて実効した手番である。**

| ファイル | 終端 | 連番 `grep` で出るか | 実際に動いたか |
|---|---|---|---|
| `internal/infra/migration/migrate_m2002_test.go` | `v72` / `v73` | — | **動いた**（別名件数・4 行の主張が反転） |
| **`internal/infra/migration/rules_m1905_test.go`** | **`v68`** | **★出ない**（`000026` という文字列を 1 度も持たない） | **動いた**（`m1905TargetsBefore` 903 → 907 ／ `After` 937 → 941） |
| **`internal/aliasindex/m2207b_third_stage_test.go`** | HEAD | **★出ない** | **動いた**（`preset_aliases` 総数 7629 → 7637） |

**★`rules_m1905_test.go` が動く機序**——`m1905TargetBaseSQL`（`:364-370`）は `rush_variant` を`EXISTS(SELECT 1 FROM moves o WHERE o.id = m.original_move_id AND o.category IN ('normal','unique'))`で絞る。**基底 4 技はいずれも `unique` であり、rush 4 行は `startup>=1 / active>=1 / damage>0 / is_aerial=false` を満たす。⇒ 旧ゲートと新ゲートの両方を一様に +4 で通る。**（対の不変条件 `after == before + 57 − 23` は変わっていない＝ゲートが動いたのではない）

**★弁別に効いた道具は連番の `grep` ではなかった。** 「**その golden の再生成で「値が変わる列名」（本件は `original_move_id`）を `*_test.go` へ `grep` し、ヒットした各ファイルの終端がその golden の版以上かを突き合わせる**」である。

> **⇒ `SUPP-001` §5.5.4 (16) へ、この手順を畳んでほしい**（§6-1 に請求のたたき台を置いた）。**★落とすと、次に golden を動かす担当が同じ取りこぼしを踏む。** 連番の `grep` は「やった」という体裁を保ったまま 2 本を取り落とす——**`SUPP-001` §5.5.4 (11′) が言う「緑だが効いていない走査」と同じ形である。**

### 1-3 `preset_aliases` への後追い `INSERT` の as-built（`character_id` は必須）

`000111` は `preset_aliases` へ 8 行を `INSERT` する。**`000074` で追加された `character_id` を明示的に書いている。**

**★`000072` / `000073` 自身は `character_id` を書いていない。** 両者が `000074` より前のマイグレであり、`000074` が後から backfill するためである。**⇒ その形を後発のマイグレへ写すと NULL 行が生まれる。★NULL でも `INSERT` は通り alias は画面にも出るので、動作では気づけない**（`M31-04` 教訓 §7-1 と同型。`000109:57-59` が同じ注意を書いている）。

> **⇒ `DES-003` §3.14（`preset_aliases` の欄）へ 1 行**——**`character_id` は `000074` 以降のマイグレでは必須である**（`000075` の UNIQUE 索引 2 本の構成列でもある）。★「**`000072` / `000073` が書いていないのは歴史的事情であり、模倣してはならない**」まで書かないと、次に同じ表へ足す担当が同じ形を写す。

---

## 2. 契約・設計に反する独自判断（★親の裁定を要する）

### 2-1 ★★★指示書の完了条件 11（`internal/seedgen/` の差分 0）に反した

| 項目 | 内容 |
|---|---|
| 何に反したか | **指示書 `M35-03` 完了条件 11**「`internal/seedgen/` の差分が 0 である」／ 同 §2.2-7「`internal/seedgen/` に手を入れない」／ §3-4 |
| 実装がどうなっているか | **同ディレクトリに 3 ファイルの差分がある。** `generate_m2002_golden_test.go`（`+13/−4`）／ `generate_m2002.go`（`+14/−3`）／ `generate_m2002_test.go`（`+4/−1`） |
| なぜそう判断したか（前半＝テスト） | **`SUPP-001` §5.5.4 規約 (16) と正面から衝突するためである。** golden `000072` / `000073` を再生成すると `TestM2002_GenerationMeasurements` の実測値（`layerRush` ／ `rushNoOriginal`）が必ず動く。**同テストは `internal/seedgen/` に在る。⇒ 「差分 0」を守る唯一の方法は「golden を再生成しない」であり、三点更新の 2 点目に正面から反する。** `CLAUDE.md` §8 の参照優先順位（設計書 > 指示書）に従い、規約 (16) を採った |
| なぜそう判断したか（後半＝コメント） | **レビュー 高-1** が `generate_m2002.go:635-638` の失効コメントを指摘した（「入力ミスが 4 件あり…`original_move_id` が NULL になっている」が**現在形**であり、実測 0 件になったいま誤り）。**★製造は独断で直さず保留し、開発者へ裁定を求めた。⇒ 2026-09-11 に「修正してください」の裁定を得て是正した** |
| ★裁定の帰結 | **開発者がコメントの是正を指示したことで、完了条件 11 は「生成器の*振る舞い*」を指すと確定した。⇒ パッケージ全体の byte 差分を意味しない** |
| **★親への依頼** | **この読み替えを恒久の規約として畳むか、本サブ限りの裁定に留めるかの判断。** ⇒ **規約 (16) は「その golden を再生成した手番でのみ触ってよい」（歯止め (a)）としか書いておらず、「触ってよい対象がどのディレクトリに在ってもよいか」を述べていない。★指示書テンプレート側に「`<pkg>/` の差分 0」という完了条件を書くと、規約 (16) と機械的に衝突する。** |

**★歯止め 4 つは全部守っている**（完了報告 §7）。 (a) 触ったのは `000026` / `000072` / `000073` の版に依存するものだけで、**`migrate_m1403c_test.go`（`000030` の版・`M35-02` が (16) を初適用した現物）には 1 文字も触っていない** ／ (b) `m2002RushNoOriginal` を削除せず `m2002RushLateResolved` へ改め、要素数 4 のまま「別名 0 行」→「別名 1 行ちょうど ＋ 値まで」へ**反転**（主張は強くなっている） ／ (c) 赤を先に実測し完了報告へ貼付 ／ (d) 見出しを役割で書き直し。

**★生成物は 1 バイトも動いていない**——コメント是正後に`go run ./cmd/seedgen -check` が `OK: 生成物は既存ファイルと一致` / exit 0 を返し、`git diff --stat -- migrations/` が空であることを実測した。

---

## 3. 製造の判断

### 3-1 開発者へ確認して確定した点

| # | 事項 | 確定内容 |
|---|---|---|
| 1 | **レビューチェックリストが未発行だった**（指示書ヘッダ＝「着手承認の手番で設計卓が起こす＝`D-736`」） | **「設計卓の発行を待つ」。⇒ Phase A の実装に入らず停止した。** `D-816` で発行され、製造が `docs/instructions/reviews/` へ逐語配置した（`a1a4d8d`。加筆していない） |
| 2 | **本手番で `/design_handover_report` を回すか** | **回さない**（Phase D まで終えたところで一旦報告）。**⇒ 本レポートは開発者の再指示で別手番として作成している** |
| 3 | **レビュー 高-1（失効コメント）の扱い** | **「修正してください」**（2026-09-11）。⇒ §2-1 のとおり |
| 4 | **実機での確認**（`000111` を既存 DB へ適用） | **開発者が実施済み**（2026-09-11・「A は全て確認できました」）。★別名 4 件 ／ 保存が通ること ／ セットプレイ候補 ／ 対照の不変 |

### 3-2 推測で進めた点

指示書 §7 は「開発者への確認事項 0 件」としており、`CLAUDE.md` §9-3 に従って**コード内へ「推測:〜」と明示した**箇所が 2 つある。

| # | 何を推測したか | 根拠と、外れた場合に起きること |
|---|---|---|
| 1 | **`000111` の `preset_aliases` `INSERT` で `alias_text_en` を書かない**（NULL のまま） | 生成器の層 C-rush が同列を出力しないため、それに合わせた。**★ラッシュ合成形について明文の規定は見当たらなかった。** `000075` の部分索引は `alias_text_en IS NOT NULL` の行だけを見るので一意性には触れない。**⇒ 外れていた場合、英語表記が要る面で 4 技だけ欠ける** |
| 2 | **`down` の非対称は「往復で復帰することだけを保証する」** | `000108.down` の先例に倣った（`000063` / `000106` / `000107` と共有する既存クラス）。**★`000108` が持っていた「全降下で孤児行が残る」欠陥は本サブには当たらない**——`move_code` を 1 文字も変えていないため、`000026.down` / `000072.down` / `000073.down` の code 列挙が down の前後で当たり続ける。**⇒ この「当たらないこと」を down のヘッダへ明記した**（沈黙させない） |

---

## 4. 設計担当が未把握の残課題・申し送り

**★製造は `followup-backlog.md` 本表を編集していない**（`D-382`）。**★例外として §J（停止時記録）へ 3 行を直接登録した**（必須 5 フィールド記入済み。`CLAUDE.md` §9 の停止規律）。**下記はいずれも「設計卓が迷わず畳める形」で揃えてある。**

### 4-1 `rush-original-move-code-typo` — **既存行の更新**

| 項目 | 内容 |
|---|---|
| 対象 | `docs/handover/followup-backlog.md` **§K**（`:328`） |
| 何が起きたか | **解決した。** `M35-03` が三点更新 ＋ 2 で是正（マイグレ `000111`） |
| 根拠 | `docs/progress/M35-03-completion-report.md` ／ `migrations/000111_data_correct_rush_original_move_refs.{up,down}.sql` ／ `internal/infra/migration/migrate_m3503_test.go` |
| 更新後の状態 | 「**未着手**」→ **「解決済み（`M35-03` / `000111`・2026-09-11）」** |
| ★実 DB 確認 | **済んでいる**（開発者が 2026-09-11 に実機確認）。**⇒ `M35-02` のときと違い「実 DB 確認待ち」の但し書きは不要** |
| **★あわせて** | **§4-2 と同時に畳むこと** |

### 4-2 ★★★`rush-variant-dangling-original-move-code` — **既存行の更新（重複行）**

| 項目 | 内容 |
|---|---|
| 対象 | `docs/handover/followup-backlog.md` **§K**（`:344`）。**★`:328` と同じ 4 件を指す重複行である** |
| 現状 | 割付 **未割付** ／ 状態 **未着手** |
| 何が起きたか | **解決した。** 同じ 4 件であり、`M35-03` が是正した |
| ★なぜ重要か | **同行の本文は「実害の有無を確かめてから直す」と書いている。⇒ `M35-03` がまさにその答えを出した**（実害 3 件・完了報告 §1）**。★片方だけ畳むと、残った方を読んだ後任が「実害を確かめる」調査をやり直す** |
| 更新後の状態 | **「解決済み（`M35-03` / `000111`・2026-09-11）」。★`:328` と同じ手番で畳むこと** |
| 派生（★据え置きでよい） | `docs/handover/m21-to-m22-handover.md:187` にも同スラッグの「調査が先」の行がある。**歴史的な引継ぎ資料なので触らなくてよい**（レビューの判断に同意） |

### 4-3 `m35-03-stale-records-after-dangling-fix` — **§J に登録済み（製造が直接追記）**

| 項目 | 内容 |
|---|---|
| 対象 | `docs/handover/followup-backlog.md` **§J**（製造が 2026-09-11 に追記） |
| 状態 | **一部解決。** 開発者裁定（§2-1）で **(4) seedgen コメント** と **完了条件 11 の読み** は解決した |
| ★残っているもの | **3 件。いずれも設計卓の手番である**——(1) `docs/design/04-notation-spec.md:342` の注記（§6-2） ／ (2) §4-1 ／ (3) §4-2 |
| 設計卓への依頼 | **(1)(2)(3) を畳んだら、本 §J 行の状態を `完了` へ更新すること**（§J の作法＝削除しない） |

### 4-4 `regulation-16-grep-by-sequence-misses-column-pinned-tests` — **§J に登録済み**

§1-2 の本文がそのまま根拠である。**設計卓の判断は「`SUPP-001` §5.5.4 (16) へ手順として畳むか否か」の一択**（§6-1 に請求のたたき台）。**★実装の変更は要らない。規約の文言だけの話。** **★畳まないと決めた場合も、決めたことが記録される必要がある。**

### 4-5 `import-order-baseline-101-vs-99` — **§J に登録済み**

| 項目 | 内容 |
|---|---|
| 何が起きるか | `scripts/check-import-order.sh` の `BASELINE` が 101、実測 99。**スクリプト自身が「99 へ下げること」と出力する** |
| ★射程 | **どのサブの由来でもない。** `M35-02` ／ `M31-04` ／ 本サブの **3 サブ連続で申し送られ、誰も引き取っていない。⇒ 誰の手番でもないまま床だけが高い** |
| 割付の候補と理由 | **改善レーンに 1 手番。** 理由＝共有スクリプトであり、走行中の並列サブと同時に触ると衝突面が増える。**★下げる前に実測し直すこと**（別レーンのマージで数が動く） |

### 4-6 既存の申し送りとの重複（**新規登録は不要**）

- **`shipped-move-code-plural-suffix-violations`**（§J）——`ingrid` / `lily` / `mai` / `zangief` の`_1hits` は命名規約 R5（1 段＝`_1hit`）違反として既に登録されている。**★本サブは`move_code` を 1 文字も変えていないので、この行は動かない。** `M35-03` と混同しないこと。
- **`rush-desert-storm-1hits-naming`**——`lily` の `name_ja` が `デザートストーム(ラッシュ)`のまま（規則どおりなら `(単発)(ラッシュ)`）。**`D-305` / `D-809` により意図的に直していない。★状態は変わらない。**

---

## 5. 参考（触れていない＝不変の証跡）

- `move_code` 不可触 ── CSV 差分の 2 列目が左右で同一（機械照合）／ golden 差分も逆置換で完全一致
- **`internal/seedgen/` の生成器実装は無差分** ── `generate.go` / `csv.go` / `format.go` / `model.go` / `generate_m1702.go` / `generate_m2802a.go`
- **生成物は不変** ── コメント是正後に `seedgen -check` exit 0 ／ `git diff --stat -- migrations/` が空
- 他の seed golden 無変更 ── `000030` / `000034` / `000035` / `000045` / `000055` / `000076` / `000077` / `000084` / `000090` / `000091`
- `000026` 内で動いたキャラは `ingrid` / `lily` / `mai` の 3 体のみ（見出しで機械判定）
- 軸 B 不変 ── `moveSurfacing.roster.test.ts` の `rush_variant: 4` は動いていない（`loadCharacter` は 17 列目を読まない）
- 対照不変 ── `zangief/rush_power_stomps_1hits` は `DR > 22MK` の 1 行のまま
- セットプレイ全数 diff ── `TARGET` 4 行の追加と `COUNT` 3 行のみ。`FILLER` は 1 行も動かない
- `migrate_m1403c_test.go` 無変更（歯止め (a)。`000030` は再生成していない）
- `docs/design/` 無変更（`CLAUDE.md` §8）
- `followup-backlog.md` 本表は無変更（`D-382`。§J のみ追記）
- `go.mod` / `go.sum` / `web/package.json` / `REUSE.toml` の差分 0（新規依存なし・`000111` はパターンで層 B に解決）
- 常設検査 ── `check-artifact-integrity.sh` / `check-stop-discipline.sh` / `check-progress-log-index.sh` / `check-migration-license.sh` / `check-doc-refs.sh` / `check-doc-inventory.sh` / `check-import-order.sh` / `check-enum-sync.sh` / `check-browser-storage-keys.sh` がいずれも違反なし

---

## 6. CHANGE 起票のたたき台（設計担当向けチェックリスト）

**番号は起票時に `change-number-registry` で採番すること。**

### 6-1 請求 1 本 ── 規約 (16) の `grep` 手順を具体化する

| 項目 | 内容 |
|---|---|
| 対象 | `docs/design/supp-001-detailed-design.md` §5.5.4 規約 **(16)** の末尾（「golden を再生成する前に、その版を終端にしている契約テストを `grep` で探すこと」の段） |
| 書くこと | **連番（`000026` 等）の `grep` では足りない。⇒ その golden の再生成で*値が変わる列名*を `*_test.go` へ `grep` し、ヒットした各ファイルの終端がその golden の版以上かを突き合わせる。** 実例＝`M35-03` は `grep original_move_id --include=*_test.go` で `rules_m1905_test.go`（終端 `v68`）と `m2207b_third_stage_test.go`（HEAD）を拾った。**どちらも `000026` という文字列を持たない** |
| ★性格 | **`CHANGE-178` が初めて実効した手番の所見である。** 規約 (16) は `M35-02` が作ったが、そのとき動いた版固定テストは連番で見つかる形（`migrate_m1403c_test.go`）だけだった。**⇒ 手順の限界は 2 例目で初めて出た** |
| 書かないこと | **`original_move_id` という列名そのもの**（本件固有）。**手順の形だけを書く** |

### 6-2 請求 1 本 ── `DES-004` §3.2.1 の注記が失効した

| 項目 | 内容 |
|---|---|
| 対象 | `docs/design/04-notation-spec.md` **`:342`** の注記 |
| 現状の逐語 | 「**`original_move_code` が実在しない `move_code` を指す dangling 参照であり**（`_1hits` とすべきところが `_1`。4 件）**、元技を解決できないため突合できなかっただけである。**」 |
| 何が失効したか | **4 件は 0 件になった**（`M35-03`・2026-09-11。実測）。**⇒ 現在形の記述が誤りになる** |
| ★残すべきこと | **「接尾辞の形」と「元技参照の解決可否」は軸が違い両立する**という**弁別そのものは残す。** これが無いと、次の担当が「揺れが 4 件ある」と読んでデータ補正を始める（`D-305` は補正しないと定めている）。**⇒ 消すのではなく、いつ 0 件になったかを対で書く**（`D-775`） |
| ★あわせて確認 | 同節の「272/272」「271/272」の数え（`rush-desert-storm-1hits-naming` の例外）は**本サブで動いていない。⇒ 触らなくてよい** |
| 指示書との関係 | **指示書 §3-5 は「`DES-004` §3.2.1 の注記を直す」を *やらないこと* に挙げている**（理由＝「正しい。`M35-02` が誤っていると請求したのは誤読であった」）。**★その判断は着手時点では正しかった。⇒ 本サブの着地で状況が変わった** |

### 6-3 消費したマイグレ連番

- **`000111`** ── `000111_data_correct_rush_original_move_refs.{up,down}.sql`。**`D-812` の払い出しどおりであり、自採番していない。**
- 着手時に `migrations/` を実査し、末尾が `000109` であることを確認した（`000110` は`M31-05` の払い出しで本ツリーに無い）。
- **ボード §2.2 は既に「次に払い出すのは `000112`」と書かれている。⇒ 更新は不要。**
- 層 ── **B（`CC-BY-SA-4.0`）**。`check-migration-license.sh --list` が 2 本とも `B` と判定。**破壊確認でファイル名から `_data_` を外すと `違反 2 件` / exit 1 になることを実測した。**
- **★★マージ順の制約**（ボード §2.2 の逐語）── **`000110`（`M31-05`・走行中）→ `000111`（`M35-03`）。⇒ `M35-03` を先にマージすると、既に `v111` まで適用済みの DB へ後から若い番号が現れる。`SUPP-001` §2.7 が「危険なのは穴そのものではなく、適用済み DB に後から若い番号が現れること」と名指ししている形である。**

---

## 7. 教訓（retrospective 行き）

**★親（設計卓）は本節を読まなくてよい。** 宛先は `retrospective-log` へのバッチ反映であり、**反映担当の廃止（2026-08-11）に伴い実施者は設計担当**。

### 7-1 ★★★先行資料が数えた件数は、自分で数え直す

**指示書 §0.4 ／ followup 行 ／ `M35-02` 完了報告 §2.7 の 3 つが揃って「実害は 2 つ」と書いていた。製造はその記述を写した。⇒ 3 件目はレビューが独立に辿って見つけた**（§1-1）。

**★3 つ揃っていたのは独立した裏づけではない。** followup 行が `M20-02` の調査を写し、指示書がその followup を写し、`M35-02` が同じ followup を写しただけである。**⇒ 「複数の資料が同じことを言っている」は、源泉が 1 つなら証拠にならない。**

**機構化**——**「先行資料が数えた件数」を完了報告へ書くときは、その数を自分の実測で置き換えるか、「写しである」と明記する。** 本サブの指示書 §0.2 は「★★★件数は本書の正本ではない。⇒ 着手時に実測して全数を起こすこと」と**対象データの件数については書いていた**。**★同じ要求が「実害の件数」には掛かっていなかった。**

### 7-2 ★★破壊確認が空振りしたら、「別の機構が代わりに守っている」を先に疑う

`preset_aliases` の `character_id` を主張するテストは**書かれていて緑だった**が、`000111` の `INSERT` 列から `character_id` を落としても**赤にならなかった**。

**守っていたのは `000074` の backfill であって本サブのマイグレではなかった。**新規 DB の 8 行は golden `000072` / `000073` が入れたものであり、`000111` の `INSERT` は`NOT EXISTS` で弾かれる。**⇒ `000111` が列を落としていても `v111` 時点の値は正しいまま。**

**`SUPP-001` §5.5.4 (10′) の一例である。** 守っていたものを特定したうえで、**`000111` の `INSERT` が実際に走る唯一の経路（`down` → `re-up`）へ主張を置き直した。**

**★一般形**——**「三点更新」という型では、新規 DB で全文が 0 行に当たるため、状態テストだけでは何も守れない場面が繰り返し出る。⇒ この型では「往復」が主張の置き場である**（規約 (6) が言う「最終状態と往復で固定する」の、往復側の意味）。

### 7-3 ★機械置換は「値」と「歴史の記述」を区別しない

`migrate_test.go` の件数を `2272 → 2276` へ機械置換したとき、**同ファイル内の履歴の算術（`numeric 1258 + 1014 = 2272`。`M14-03f` の記録）も一緒に書き換えてしまった**。同じ手番で気づいて戻し、`M35-03` の `+4` は別行に分けた。

**★HEAD スコープのテストは「現在値」と「その値に至った履歴」を同じファイルに持つ。** **⇒ 数値の置換は必ず全出現を目視し、「これは現在値か、過去の記録か」を 1 件ずつ判定する。**`SUPP-001` §5.5.4 (11) の「個数側は 1 件ずつ意味を確認し、動的カウントを機械置換しない」と同じ族だが、**あちらは「動的カウント」を、こちらは「歴史の算術」を対象にしている。**

### 7-4 ★指示書の完了条件と設計書の規約は機械的に衝突しうる

完了条件 11「`internal/seedgen/` の差分が 0」と `SUPP-001` §5.5.4 (16) は**両立しない**（§2-1）。**★どちらも正しく書かれている。** 衝突は「規約 (16) が対象とするテストがたまたま同ディレクトリに在る」という配置の問題から生まれた。

**機構化**——**指示書テンプレートで「`<pkg>/` の差分 0」型の完了条件を書くときは、そのディレクトリに版固定契約テストが在るかを先に見る。** 在るなら「**生成器の実装ファイルの差分が 0**」のように、**ファイルの性格で書く。**

### 7-5 ★事後発行のチェックリストでも、待った価値はあった

本サブのチェックリスト（`D-816`）は**事後発行**であり、製造は発行を待って停止した（§3-1）。**⇒ 待ったことで得たものは「重点 5 束」である**——とくに束 A-1 （既存 DB と新規 DB の両方で示せ）は、**§7-2 の空振りを見つけた破壊確認の動機になった。**

**★「指示書の完了条件を写したものではない。緑でも壊れているものを探すための束である」というチェックリストの自己定義が、実際にそのとおり働いた事例として記録する。**
