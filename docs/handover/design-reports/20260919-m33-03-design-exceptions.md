# 設計伝達レポート（例外レポート） — `M33-03`（ガードと資料の作り直し）

| 項目 | 内容 |
|------|------|
| 作業 ID | `M33-03` |
| 指示書 | `docs/instructions/M33-03-guards-docs-and-migration-path.md` **v1.0.0** |
| 実施日 | 2026-09-19 |
| 追記 | 同日・Codex：開発者承認による検証用 DB の実移行と、開発者の手動確認完了（§3-5） |
| ブランチ | `claude/keen-hypatia-imze0q` ／ 着手基点 `9de8fa9` |
| 完了報告 | `docs/progress/M33-03-completion-report.md` |
| レビュー報告 | `docs/progress/m33-03-review.md`（高 5 / 中 7 / 低 5・**不採用 0 件**） |
| マイグレ消費 | **0 本**（`ls migrations/*.up.sql` は **9 本**のまま） |
| CHANGE 消費 | **0 本**（★自採番していない＝`D-293`。原稿は §1 / §6） |

---

## 最初に置く結論（指示書 §6-11 の要求）

**★★`go test ./...` が EXIT=1 / `--- FAIL` 158 → EXIT=0 / FAIL 0 になった。⇒ `M33` の 3 段が着地した。**

2026-09-19 追記：検証用 DB の実移行・上書きと手動確認まで完了した（§3-5）。以下の「乾式なし」は当初の製造時点の裁定を示す。今回の実移行は追加承認を得て実施したものであり、当初の乾式検証を実施済みへ読み替えるものではない。

**★★★親の手番が 4 つある。**

| # | 何を | なぜ急ぐか |
|---|---|---|
| **1** | **開発者裁定 7 件をボードへ採番する**（§3-1） | **★レビューが「採番が無く真偽を判定できない」と記録した。⇒ とくに「乾式を行わない」は完了条件 §6-7 を ✗ にしている根拠であり、採番が無いとチェックリスト E-2 の不合格に見える** |
| **2** | **`SUPP-001` §5.5.2 規約 (2) の食い違いを畳む**（§2-1） | **★新系列は区間が 1 つしか無いため、`internal/infra/migration` の*すべて*のテストが HEAD スコープになった。⇒ 規約 (2) が想定していない世界である** |
| **3** | **`REUSE.toml` の層 B 追加を as-built 化する**（§1-2 / §6-2） | **★段 3 が作った穴を塞いだ変更であり、`DES-001` §5.1 と `SUPP-001` §2.7 が失効した** |
| **4** | **`SUPP-001` §5.5 規約 (19) を追補する**（§1-1 / §6-1） | **★同規約は「対象ファイルが消えた」場面を覆っていない。⇒ 次に踏む人が再発明する** |

---

## §1 製造が独自に確定した実装仕様（DES 反映が要るもの）

### §1-1 ★★★golden の比較先を `migrations/` から `internal/seedgen/testdata/` へ移した

| 項目 | 内容 |
|---|---|
| **なぜ必要だったか** | **byte 一致の再建が構造的に不可能である。** seedgen は `INSERT ... SELECT ... CROSS JOIN` 形式・**12 列**を出すが、新 `000004_data_seed_moves` は **id 明示の `VALUES` タプル 22 列**であり、さらに自己参照 **514 行**を「NULL で入れてから `UPDATE`」の 2 パスで解いている。⇒ **seedgen にその生成能力は無い。** さらに `is_derived` backfill 系 **4 stem は対応先の SQL ファイルそのものが存在しない**（列値として `000004` に溶けた）**。⇒ stem 単位の対応が原理的に付かない** |
| **★`SUPP-001` §5.5 規約 (19) が覆っていない** | 同規約の 3 点セット〔CSV を直す ／ golden を追随 ／ **データ修正マイグレを置く**〕は「**CSV の 1 値が間違っていた**」場面の手順である。⇒ 手順 3 に相当するものが「対象ファイルが消えた」場面には無い |
| **実装がどうなっているか** | `internal/seedgen/generate_m1702_test.go` の `assertGolden` が `internal/seedgen/testdata/` を見る ／ 17 stem × up/down = **34 ファイル**を凍結 ／ 再生成は **`TACPENDIUM_UPDATE_GOLDEN=1 go test ./internal/seedgen/`**（★着手時点は各テストの doc に書かれた CLI であり、`-note` を Go の定数から**人が転記する**形だった） |
| **★★凍結が忠実であることの実測** | 34 ファイルすべてについて旧コミット済み SQL（`git show b5cd716^:migrations/<名>`）と本体を突き合わせ、**差分 0 / 34**。⇒ **ドリフトは無い**（開発者の指示「ドリフトが疑われたら報告」への回答） |
| **★失われた主張を意味で埋めた** | `TestRun_HEAD_SeedValuesMatchCSV` を新設（`csv_db_sync_test.go`）。**2743 行 × 9 列で一致**（CSV データ行の全数も 2743 ⇒ 取りこぼし 0） |

**⇒ 親の手番: `SUPP-001` §5.5 へ「golden の比較先が消えた場面」の規約を追補する**（§6-1）。

### §1-2 ★★★`REUSE.toml` の層 B へ `internal/seedgen/testdata/**` を足した（開発者裁定）

| 項目 | 内容 |
|---|---|
| **何が起きていたか** | **段 3 が作った新しい穴である。** `testdata/**` は SF6 の事実（技 code・フレーム値・日本語技名）を含む層 B の素材だが、**`REUSE.toml` の層 B グロブが 3 つだけ**〔`character_data/**` / `docs/seed-data/**` / `migrations/*_data_*.sql`〕**なので既定の `path = "**"` へ落ちて `AGPL-3.0-or-later`（層 A）へ解決していた** |
| **★公開される** | 公開スナップショットは **`ALLOW internal/**`** である（実測） |
| **★★どの検査も赤くならない** | `check-migration-license.sh` は `migrations/` だけを走査し、`check-public-snapshot.sh` は ALLOW/DENY の網羅だけを見る。**両方 EXIT=0** |
| **★★★`D-777` の一般形そのもの** | 逐語＝「**グロブで解決する宣言は、ファイルを動かした瞬間に黙って外れる。⇒ そして外れた先が既定なので、検査は緑のままである。**」 |
| **開発者裁定** | **2026-09-19・「`REUSE.toml` の層 B へ 1 行足す」。⇒ 製造の独断ではない**（★ボード採番は親の手番＝§3-1） |
| **実測（是正後）** | `internal/seedgen/testdata/**` → `CC-BY-SA-4.0`（層 B） ／ `scripts/migrate-userdata-prompt.md` → `MIT`（層 C） |

**⇒ 親の手番: `DES-001` §5.1 と `SUPP-001` §2.7 のライセンス項を as-built 化する**（§6-2）。

### §1-3 ★★ガードへ規則 (5) を新設し、規則 (4) を凍結分にも評価するようにした

| 項目 | 内容 |
|---|---|
| **規則 (4) が空回りしていた** | 着手時点は `if num in frozen: (3) else: (4)` であり、`M33-02` が新系列 9 本を*全数*凍結表へ入れたため **(4) が 1 本も評価されない**（`--list` の実測で **18/18 が「凍結」**）。⇒ 規則 (4) を守る唯一の経路が `--self-test` の陽性対照だけになっていた |
| **設計卓の推しを採らなかった** | 推し（`D-902`）は「9 本を凍結表から外して (4) へ委ねる」で、実査でも**外しても 9 本すべて (4) を通る**（緑）。**★それでも残したのは、規則 (3) が層の*値*を固定する力を持ち (4) に無いためである**（`REUSE.toml` を書き換えて層を入れ替えても (4) だけでは検出できない）。⇒ **凍結表を残したまま凍結分にも (4) を評価する形＝外す案より厳密に強い** |
| **規則 (4a) の穴** | `_data_` を持つ側で**書き込み先を一切見ていなかった**。⇒ `APP_TABLES`〔`games` / `presets` / `users` / `tags`〕への書き込み検査を追加。★followup `migration-license-check-blind-to-write-target-when-data-named` の是正 |
| **規則 (5)** | 凍結 golden が層 B へ解決すること。**★走査範囲を `migrations/` の外へ広げる唯一の規則である** |
| **副産物** | `ON UPDATE CASCADE` の `CASCADE` を表名として捕獲する偽陽性を潰した（実測: `000001` の捕獲が `['cascade','sqlite_sequence']` → `['sqlite_sequence']`） |
| **★破壊確認 3 件** | いずれも **「塞ぐ前は EXIT=0（緑）」を実測した** うえで、赤くした規則を名指し。⇒ ① `_data_` 付きで `users` へ書く → 規則(4a) ／ ② `REUSE.toml` から 1 行抜く → 規則(5) 34 件 ／ ③ 凍結分がゲームデータ表へ書く → 規則(4b)。**★3 件とも後片付け済み**（`git diff --numstat 9de8fa9 -- migrations/` は 0 ファイル） |
| **自己検査** | 陽性 9 / 陰性 2（計 **11**）→ 陽性 **12** / 陰性 **3**（計 **15**）。すべて OK |

**⇒ 親の手番: 規約として `SUPP-001` へ載せるかの判断**（§6-3）。

---

## §2 契約・設計に反する独自判断（★親の裁定が要る）

### §2-1 ★★★`SUPP-001` §5.5.2 規約 (2) と現状が食い違っている（レビュー 高-1）

| 項目 | 内容 |
|---|---|
| **何に反したか** | **規約 (2) の逐語**＝「**HEAD スコープの不変条件は、専用ファイル・専用の名前空間に置く。`migrate_head_test.go` に `TestRun_HEAD_*` として置き、`TestRun_M14xx_*` のようなサブ名の名前空間へ相乗りさせない。**」 |
| **現状** | **HEAD 終端へ移した 39 本**〔(ii) 34 ＋ (iii) 5〕**が `TestRun_M28xx_*` 等サブ名のままである**。**★★【2026-09-19 追記・母数を実測】抵触は 39 本ではなく 49 本・22 ファイルである**〔`internal/infra/migration/*_test.go` の全 77 本＝`TestRun_HEAD_*` **11** ／ `TestRun_M*` **44** ／ `TestM1905_*` **5** ／ その他 17。**★39 は私が書き直した分だけを数えた過小値であった**——**書き直していない既存のサブ名テストも、区間が 1 本になった時点で同じく HEAD スコープである**。上位は `migrate_m3105_test.go` 6 ／ `rules_m1905_test.go` 5 ／ `migrate_m3704_test.go` 5 ／ `migrate_m3007_test.go` 4 ／ `migrate_m1403f_test.go` 4〕 |
| **★なぜ起きたか（規約の前提が消えた）** | 規約 (2) は「サブ名の**契約テスト**（区間を自サブに閉じる）」と「**HEAD テスト**」の 2 種類が共存する世界を前提にしている。⇒ **`M33-02` が 111 本を 9 本へ潰した結果、区間が 1 つしか無くなり、契約テストという category そのものが消えた。★`internal/infra/migration` の*すべて*のテストが HEAD スコープである** |
| **★製造がやったこと／やらなかったこと** | **やった**＝7 ファイルへ「本ファイルの終端は HEAD である／名前空間は未解決である」を明記し、撤回済みの逐語（「HEAD を終端にしない」計 87 行）を落とした。**やらなかった**＝49 本の移設。⇒ **`CLAUDE.md` §8「設計書間の矛盾は止めて確認」により製造の独断で行わない** |
| **⇒ 親の裁定** | **(a) 49 本を `migrate_head_test.go` へ移す**（規約 (2) をそのまま守る。★ただし provenance＝どのサブが立てた主張かが名前から消える） ／ **(b) 規約 (2) を改める**（区間が 1 つの世界では「契約テスト」が無いことを明文化し、サブ名のままでよいとする） |

### §2-2 ★`M33-overview` §5 と指示書 v1.0.0 §2.2 が食い違っており、片方を選んだ

| 項目 | 内容 |
|---|---|
| **食い違い** | `M33-overview` §5 は `M33-03` の射程を「歴史テスト 28 ファイルを**旧系列側へ移す**」と書く ↔ 指示書 v1.0.0 §2.2 は「**分類して消す／書き直す**」 |
| **なぜそう判断したか** | **より新しく具体的な指示書 §2.2 を採った**（v1.0.0 は 2026-09-19 発行）。★`M33-02` は `migrations/legacy/` を作っておらず（`find migrations -mindepth 2` が空）、「旧系列側」という置き場が存在しない |
| **⇒ 親の裁定** | 畳んでおかないと、次に `M33-overview` を読む担当が迷う |

---

## §3 製造の判断

### §3-1 ★★★開発者へ確認して確定した点（**7 件・ボード採番が要る**）

**★レビューが「`parallel-board.md` に採番が無く、レビュー側からは真偽を判定できない」と記録した。⇒ 正しい観察である。**
**★7 件はいずれも本手番の対話で照会して得た回答であり、ボードへの採番は親の手番である。**

| # | 照会した内容 | 開発者の回答（逐語または要旨） |
|---|---|---|
| **1** | golden 17 stem の処遇（byte 一致が再建できない） | **「推奨案。なお仮に csv と sql のドリフトが疑われた場合は報告」** ⇒ `testdata/` へ凍結 ＋ 意味比較 1 本 |
| **2** | ガードの穴（規則 (4) の空回り ＋ `has_data` 側の盲点） | **「両方やる」** |
| **3** | **移行手順の形** | **「これは devContainer や生成物ではなく、既存の検証面に対してのものですね？ローカル上の AI にやらせるので最小限のプロンプトだけでよい」** ⇒ 続けて乾式の可否を照会し **「乾式なし・プロンプトだけ」** |
| **4** | 新系列の down 往復テストの新設 | **「新設する」** |
| **5** | 失効参照の射程（フロントを含めるか） | **「フロントも含めて全部直す」** |
| **6** | **`internal/seedgen/testdata/**` の層** | **「`REUSE.toml` の層 B へ 1 行足す」** |
| **7** | **★★`D-187` の 132 行の処遇**（発生フレームを持たない技の `startup_basis`） | **「発生フレームを持たない技はunknownへ修正」** ⇒ **`D-187` は撤回しない。132 行すべてが是正対象**（**是正は `000010` の別サブ。本サブはマイグレ消費 0 本が射程条件**） |

**★★★とくに #3 は完了条件 §6-7（移行手順が乾式で通る）を ✗ にしている根拠である。**
**⇒ 採番が無いと、チェックリスト E-2「乾式で通った*出力*が在るか」の不合格に見える。最優先で採番を。**

**★技術的には乾式は可能だった**（旧 111 本は `git show b5cd716^:migrations/<名>` から取れる）。
**⇒ できなかったのではなく、開発者の判断で行わなかった。**

### §3-2 推測で進めた点

| # | 内容 |
|---|---|
| 1 | **歴史記述 (c) の扱いを「内容を 1 文字も変えず『旧』を明示する」で一律にした。** ⇒ 指示書 §2.1-(c) は「1 件ずつ『旧系列の』と読めるかを見ること」と求めるが、判定基準は示していない。★推測: 番号の前に「旧」を付ければ機械的に「旧系列の話」と読める |
| 2 | **`.claude/commands/precheck_seed_data.md:138` を歴史注記化した。** ⇒ 実測に基づく（新 `000008` は `move_id` で持ち `move_code` を 1 件も含まないため、code を grep する裏取りは**どの技でも 0 件になり常に通る空の検査**になる）。★差し替えも `ALLOW` も不正確と判断した |
| 3 | **(ii) の書き直しで「件数の固定」を構造の主張へ置き換えた。** ⇒ `SUPP-001` §5.5.7 (i) の警告（行数固定は seed 波で全部壊れる）に従った。★1 件は主張が弱くなった（§3-3） |

### §3-3 ★製造が足した検査（指示書に無い）

| # | 検査 | なぜ |
|---|---|---|
| 1 | **`TestRun_HEAD_DownUpRoundTrip`** | **★新系列 9 本の `.down.sql` は、本テストを入れるまで 1 本もテストされていなかった。⇒ 48 本を消した穴ではなく、着手時点から空いていた穴である** |
| 2 | **`TestRun_HEAD_SeedValuesMatchCSV`** | golden の比較先を移した結果、「配布されるシードが CSV 由来である」を見るものが無くなるため |
| 3 | **`head_seed_invariants_test.go` の 7 本** | レビュー 高-3 の是正。消した (i) 102 本に含まれていた HEAD で表現できる主張を復元（§4-2） |
| 4 | **ガードの規則 (5)** | §1-3 |

### §3-4 ★主張が 1 つ弱くなったことの明示

`TestRun_M2006_P34Rows` の母集団を「非 system の全行」から「P-34 の対象集合」へ閉じた。
⇒ **着手時点は「P-34 以外の行へ `alias_text_en` が付いた」ことも検出できたが、それは見えなくなった。**
★閉じた理由＝後続の seed 波が正当に `alias_text_en` を足すため（現在 18 行）、全体で固定すると波ごとに落ちる。
**★完了報告の初版は「ガードの強さは変わらない」と書いていた。⇒ 不正確であり、レビュー 中-4 で是正した。**

---

### §3-5 追記：検証用 DB の実移行・手動確認完了（2026-09-19）

開発者が `scripts/migrate-userdata-prompt.md` の実行を依頼し、移行元を指定したうえで「バックアップした後、tacpendium.db を上書きする形」を指示した。Codex が `feature/m33-03-2` で実施した。着手時は `claude/keen-hypatia-imze0q` と先端 `84cfd162` が一致していた。

主キー比較で差異を検出したため、投入前に停止して報告した。開発者の「その方針で進めてください」を受け、次の処理を行った。

- `users` の 1 行は作成日時だけが相違。旧 `created_at` を維持した。
- `preset_aliases` は同じ ID で 5,785 行が相違したが、`(preset_id, move_id)` で対応付けると共通 7,604 行は ID 以外の全列が一致した。共通行は新 baseline の ID を使い、旧 DB にしかない 1,247 行は衝突しない新 ID で追加した。新 baseline 固有の 31 行も保持した。
- 技 ID `1065` はコードが `axe_kick_2` → `axe_kick`。参照 ID を維持し、新 baseline のコードを採用した。配布シード 5 表の行は移行せず、新 baseline と一致することを検証した。

新 baseline のコピーへ `foreign_keys=ON` で投入し、`combos` の自己参照 2 列は NULL 投入後の更新で復元した。`sqlite_sequence` は旧値以上を保証した。全 16 表の旧データが移行先に存在することを全列で照合した（別名のみ承認済みの ID 変更を除く）。移行直後の件数は次のとおり。手動確認後の現 DB 件数ではない。

| 表 | 旧 DB | 移行後 | 新規投入 |
|---|---:|---:|---:|
| `users` | 1 | 1 | 0 |
| `tags` | 7 | 7 | 4 |
| `presets` | 4 | 4 | 1 |
| `setups` | 33 | 33 | 33 |
| `combos` | 152 | 152 | 152 |
| `preset_aliases` | 8,851 | 8,882 | 1,247 |
| `combo_steps` | 875 | 875 | 875 |
| `setup_steps` | 136 | 136 | 136 |
| `combo_tags` | 56 | 56 | 56 |
| `combo_setups` | 31 | 31 | 31 |
| `combo_oki_options` | 286 | 286 | 286 |
| `combo_punishes` | 16 | 16 | 16 |
| `combo_punish_curations` | 0 | 0 | 0 |
| `combo_punish_prunings` | 0 | 0 | 0 |
| `combo_punish_starters` | 16 | 16 | 16 |
| `combo_setup_results` | 17 | 17 | 17 |

同一 seed として省略した件数は `tags` 3、`presets` 3、`preset_aliases` 7,604。`users` 1 行は省略ではなく旧作成日時への更新である。

移行元の変更がないことをバックアップとの照合で確認し、SQLite backup API でバックアップ後に元の `tacpendium.db` を上書きした。上書き後も `foreign_key_check` の返却行は `[]`（0 件）、`integrity_check` は `ok`。`schema_migrations` は新系列の `version=9, dirty=0` を保持し、旧系列の `version=117` は移していない。追加マイグレーションは 0 本。

作業スクリプト・DB・詳細 JSON は Git 管理外の `tmp/` にのみ置いた。ローカル証跡は `tmp/userdata-migration-20260919-approved/report.json` と `installation-report.json`、復元用バックアップは同ディレクトリの `before-overwrite-backup.db`。これらは公開・配布対象ではなく、復元が不要になった時点で開発者が整理する。本節に結果を記録し、ローカル証跡の永続性には依存しない。

手動確認は開発者から「全て問題なし」と報告された。依頼した確認範囲は起動、既存コンボと関連データの表示、組込・カスタムプリセットの別名表示、コンボの複製・保存・再編集・再起動後の保持、および該当データがある場合の技 ID `1065` 関連の表示。これは開発者による確認結果であり、Codex による画面操作の実測とは区別する。今回の検証用 DB 移行に関する判断待ちはない。

設計卓への申し送り：元プロンプトの「同一主キーなら seed 行」とする手順だけでは、この実 DB の別名 ID のずれを処理できなかった。今回の対応は上記の個別承認に基づく。プロンプトを再利用する場合はこの実測を踏まえること。当初の裁定 7 件（§3-1）と後続の移行承認は区別し、本追記で既存の採番待ちや他の残課題を解消扱いにはしない。

---

## §4 設計担当が未把握の残課題・申し送り

> **★`followup-backlog.md` は 1 文字も編集していない**（`D-838`）**。⇒ 下記は「§J 行の原稿」であり、設計卓が受理の手番で §J へ転記する。全件 5 フィールドを揃えてある。**

### §4-1 【§J 行の原稿・新規】`d187-startup-basis-invariant-violated-at-head`

| フィールド | 内容 |
|---|---|
| **ID（スラッグ）** | `d187-startup-basis-invariant-violated-at-head` |
| **発生元** | `M33-03`（完了報告 §4.5） ／ レビュー報告書 `docs/progress/m33-03-review.md` |
| **未解消の理由** | **`D-187` の是正が主張していた不変条件「`startup IS NULL` なら `startup_basis` は `unknown`」が、HEAD では 132 行違反している**（実測）**。⇒ 旧 `TestRun_M19P2_ManualBackfill` は v64 でしか見ていなかったため、後続の seed 波で崩れたことに誰も気づいていない。★本サブは「不変条件として成立しないので HEAD スコープへ引き上げなかった」判断をした** ／ **★★【2026-09-19 追記・内訳を実測】126 が `category='system'` の移動 move**（**14 キャラ × 9。★14 キャラは第四波そのもの**＝旧 `000084` の見出し逐語「第四波 14 キャラ（未 seed 12 + 仮登録 2）」）／ `target_combo` 4 ／ `special` 2。**原因は旧 `000065` の是正が第三波までの母集団にしか当たらず、第四波の backfill**（旧 `000089`）**が CSV の `standalone` を入れ直したことである**。**★CSV とマイグレ SQL の揺らぎではない**——**CSV 2743 行 ↔ DB の `startup_basis` 突合は不一致 0 件**、**移動 system move は 31 個の CSV すべてに 1 行も無い**（⇒ 126 行はマイグレ SQL のみが源泉）、**CSV 側の違反は 6 行だけ**（＝非 system の 6 行と完全一致）。**★同ファイルの `drive_reversal` は 31 キャラすべてが `unknown` であり、SQL 自身の慣習が `unknown` であることを示す**。**★実害は出ていない**——`usesFirstHitStartup` の述語 110 行のうち `first_hit_startup` 未記入の 4 行が上の `target_combo` 4 行と完全一致し、`candidateStartup` が `nil` を返して候補から落とす（fail-closed） |
| **再開に必要な条件** | **★★【2026-09-19 更新】方針は開発者裁定で確定した。逐語＝「発生フレームを持たない技はunknownへ修正」** ⇒ **`D-187` は撤回しない。132 行すべてが是正対象である**。残るのは実行であり、**サブ新設は開発者の承認事項**（先例＝`D-440`）。**是正サブの中身は 3 点**——**(1) `000010` の是正マイグレ 1 本で SQL 由来 126 行 ＋ CSV 由来 6 行をまとめて `unknown` へ**。**(2) `character_data/` の 6 行も同じ手番で直す**〔`blanka/blanka_chan_bomb_activated` ／ 同 `_od` ／ `chun_li/soaring_eagle_punches` ／ `dee_jay/party_in_the_air` ／ `elena/soaring_raid` ／ `elena/raptor_range`〕**。★CSV だけ先に直すと `TestCSVAndDBAgreeOnFrameCostColumns`**（`internal/infra/migration/csv_db_sync_test.go:121,143`）**が赤くなる**。**(3) `D-187` を `TestRun_HEAD_*` へ引き上げて常設ガードにする**——**ガードが v64 固定だったことが 132 行を見逃した直因である**。**★本サブでは実行できない**（**マイグレ消費 0 本が射程条件**＝指示書 G-1 / G-7 ／ チェックリスト §0 の ★7）。★母集団の取り方は `SELECT count(*) FROM moves WHERE startup IS NULL AND startup_basis <> 'unknown'` |
| **記録日・状態** | 2026-09-19 ／ **方針確定・実行未着手**（**開発者裁定あり。是正サブの起票を待つ**） |
| **新規登録か更新か** | **新規登録** |

### §4-2 【§J 行の原稿・新規】`rules-m1905-lives-in-wrong-package`

| フィールド | 内容 |
|---|---|
| **ID（スラッグ）** | `rules-m1905-lives-in-wrong-package` |
| **発生元** | `M33-03`（完了報告 §4.4） |
| **未解消の理由** | **`rules_m1905_test.go` の 5 本は `setplay.ProjectCandidates` のロジックテストであり、`internal/infra/migration` に住んでいる理由がもう無い**（マイグレの版を固定するためにそこに居た）**。⇒ 版固定が消えた以上、同ファイルのヘッダ自身の規約に従えば setplay サービスの隣が筋である。★移設は差分が大きく本サブの射程外とした** |
| **再開に必要な条件** | **`internal/service/setplay` 側へ移すかの判断。★移すなら `canarySetplay*` ヘルパ（`canary_setplay_projection_test.go`）との依存も同時に動かす必要がある** |
| **記録日・状態** | 2026-09-19 ／ **未着手** |
| **新規登録か更新か** | **新規登録** |

### §4-3 【§J 行の原稿・新規】`contract-test-category-gone-after-single-series`

| フィールド | 内容 |
|---|---|
| **ID（スラッグ）** | `contract-test-category-gone-after-single-series` |
| **発生元** | `M33-03`（§2-1） ／ レビュー報告書 高-1 |
| **未解消の理由** | **`SUPP-001` §5.5.2 規約 (2) は「サブ名の契約テスト」と「HEAD テスト」が共存する世界を前提にしているが、`M33-02` 後は区間が 1 つしか無く、`internal/infra/migration` の*すべて*のテストが HEAD スコープになった。** ⇒ **49 本・22 ファイルがサブ名の名前空間に残っている**〔全 77 本の内訳＝`TestRun_HEAD_*` 11 ／ `TestRun_M*` 44 ／ `TestM1905_*` 5 ／ その他 17。**★旧記載の 39 は書き直した分だけを数えた過小値である**〕。**★製造は設計書を直接編集しない**（`CLAUDE.md` §8） |
| **再開に必要な条件** | **(a) 49 本を `migrate_head_test.go` へ移す ／ (b) 規約 (2) を改める（区間が 1 つの世界では契約テストが無いことを明文化する）のどちらかを設計卓が決めること。★製造は 7 ファイルへ「終端は HEAD である／名前空間は未解決」を明記してある** |
| **記録日・状態** | 2026-09-19 ／ **未着手** |
| **新規登録か更新か** | **新規登録** |

### §4-4 【§J 行の原稿・新規】`m3303-developer-rulings-not-numbered-on-board`

| フィールド | 内容 |
|---|---|
| **ID（スラッグ）** | `m3303-developer-rulings-not-numbered-on-board` |
| **発生元** | `M33-03`（§3-1） ／ レビュー報告書 `不明` |
| **未解消の理由** | **本サブは開発者裁定 7 件のうえに立っているが、`parallel-board.md` に採番が無い**（最終は `D-904` で着手基点より前）**。⇒ レビュアーは「真偽を判定できない」と記録した。★とくに「乾式を行わない」は完了条件 §6-7 を ✗ にしている根拠であり、採番が無いとチェックリスト E-2 の不合格条件に当たって見える** |
| **再開に必要な条件** | **設計卓が §3-1 の 7 件を `D-905` 以降へ採番すること。★逐語は §3-1 の表に在る** |
| **記録日・状態** | 2026-09-19 ／ **未着手（★最優先）** |
| **新規登録か更新か** | **新規登録** |

### §4-5 【§J 行の原稿・新規】`stale-ref-census-formula-misses-stem-form`

| フィールド | 内容 |
|---|---|
| **ID（スラッグ）** | `stale-ref-census-formula-misses-stem-form` |
| **発生元** | `M33-03`（完了報告 §3.1） |
| **未解消の理由** | **`M33-02` 完了報告 §11 と指示書 §2.1 が使う「65 箇所」の式は、stem 形（`000026_seed_moves_first_wave`）を 20 件取りこぼす**（`\b0000[0-9]{2}\b` は `_` が語構成文字なので後端 `\b` が成立しない）**。⇒ 真の全数は 85。★落ちた中に `cmd/seedgen/main.go:142` の既定出力 stem（実コード）が含まれていた。★正しい式＝`grep -rnP '(?<![0-9])0000[0-9]{2}(?![0-9])'`** |
| **再開に必要な条件** | **今後この型の棚卸しをするときは正しい式を使うこと。★資料へ式を載せるかの判断（`check-doc-refs.sh` は本番ソースを見ないため、恒久検査にするなら別途）** |
| **記録日・状態** | 2026-09-19 ／ **記録のみ（本サブで是正済み）** |
| **新規登録か更新か** | **新規登録** |

### §4-6 【§J 行の原稿・新規】`accidentally-correct-000001-references`

| フィールド | 内容 |
|---|---|
| **ID（スラッグ）** | `accidentally-correct-000001-references` |
| **発生元** | `M33-03`（完了報告 §3.7 / §8-6） |
| **未解消の理由** | **`migrations/000001` を指す参照が「偶然そのまま正しい」箇所が 3 つある**〔`internal/repository/tag/repository.go:196` ／ `internal/seedgen/model.go:63` ／ **`web/e2e/m22-03-optimistic-locking.spec.ts:74`**（★指示書が挙げた 2 箇所に入っていない実査の追加分）〕**。⇒ 新 `000001` も最終スキーマであるため成立しているだけである。★次に連番の起点が動いたら 3 箇所とも壊れる。★同型で `scripts/generate-code-facts.sh:745` の `need ... '^\| 000001 '` も偶然通る** |
| **再開に必要な条件** | **連番の起点を動かす変更をするときに、この 4 箇所を先に潰すこと。★恒久検査にするかは設計卓の判断** |
| **記録日・状態** | 2026-09-19 ／ **記録のみ** |
| **新規登録か更新か** | **新規登録** |

### §4-7 レビュー低指摘の繰越

**なし。** **17 件すべてを採用した**（高 5 / 中 7 / 低 5・**不採用 0 件**）。**⇒ 繰越は無い。★再レビューの往復も 0 回。**

### §4-8 ★その他の申し送り

| # | 内容 |
|---|---|
| 1 | **`docs-map` は本サブでは回していない。** ⇒ 本サブが `docs/progress/` へ報告 2 本 ＋ `docs/handover/design-reports/` へ本書を足すため、**Phase D の後に回すのが正しい**（いま回しても古くなる）。★着手時点から 8% 陳腐化しており、本サブ由来ではない |
| 2 | **`tmp/` に `.go` ファイルを置くと `go build ./...` / `go vet ./...` が拾って壊れる**（本サブで実際に踏んだ）**。⇒ 移行プロンプトへ注記した。★作業用の Go ファイルはリポジトリ外のスクラッチへ置くこと** |
| 3 | **規則 (5) の走査範囲は 1 ディレクトリ固定である**（レビュー 中-6）。⇒ 「層 B の素材が `migrations/` の外へ出る」形が別の場所にも生まれたら本規則は見ない。★実査では現時点で他に同型の箇所は無い |
| 4 | **`GAME_TABLES` の `custom_states` は表ではなく `characters` の JSON 列である**（レビュー 中-7）。⇒ 実害は無いが名前だけを見ると表だと読める。★外さない理由をコメントへ書いた（偽陽性は見落ちを生まない側） |
| 5 | **`TestCSVAndDBAgreeOnFrameCostColumns` と `TestRun_HEAD_SeedValuesMatchCSV` を合わせても CSV の全列は覆えない**（レビュー 中-5 の是正）。⇒ 覆っていない 9 列をコメントへ列挙した。★「全列を覆った」と書くと次に列が増えたとき「もう見ている」と誤読される |
| 6 | **`docs/progress/20260906-squash-research/` の削除は開発者の手番**（`D-196`）。**⇒ 公開の危険は既に機構で閉じている**（`scripts/public-snapshot-manifest.txt:27` に DENY。開発者確定 2026-09-08）**。★残る理由は 6.2MB のディスクだけである** |

---

## §5 参考（触れていない＝不変の証跡）

| # | 対象 | 差分 |
|---|---|---|
| 1 | `migrations/` | **0 ファイル**（`ls migrations/*.up.sql` は **9 本**のまま） |
| 2 | `docs/design/` | **0 ファイル** |
| 3 | `docs/handover/followup-backlog.md` | **0 ファイル**（`D-838`） |
| 4 | `docs/progress/20260906-squash-research/` | **0 ファイル**（`D-196`） |
| 5 | 開発者の手元 DB | 当初の製造時点では触っていない（使ったのは `.gitignore` 済みの `tmp/m33-03/*.db`。後片付け済み）。同日の後続依頼で、バックアップ後の実移行・上書きと手動確認まで完了（§3-5） |
| 6 | `parallel-board.md` | **0 ファイル**（製造はボードを編集しない） |
| 7 | 破壊確認 3 件の後片付け | **完了**（`git diff --numstat 9de8fa9 -- migrations/` が 0 ファイル） |

**テストと検査の全数**: `go test ./...` **EXIT=0 / FAIL 0 / ok 60**（`no test files` 9 ＝ `go list` 69・検算一致） ／
`pnpm test` **234 files / 2974 tests** ／ `make e2e` **EXIT=0 / 364 passed / flaky 0** ／
常設検査 **13 本すべて EXIT=0** ／ `check-migration-license.sh --self-test` **全 15 対照 OK** ／
`go run ./cmd/seedgen -check` **EXIT=0** ／ コード上で 1 度も使われない宣言 **0 件**。

**変更統計**: `git diff --stat 9de8fa9` ＝ **160 files / +17405 / -10604**（コミット 10 本）。
**★新規 37 ファイルの deletions 合計 = 0**（`E-225`）。**削除したファイル 15**（意図的）。

---

## §6 CHANGE 起票のたたき台（設計担当向けチェックリスト）

> **番号は起票時に `docs/handover/change-number-registry.md` §1 で採番すること**（★製造は自採番しない＝`D-293`）**。**

| # | 対象 | 内容 |
|---|---|---|
| **1** | **`SUPP-001` §5.5**（規約 (19) の追補） | **「golden の比較先が消えた場面」の規約を足す。** ⇒ 規約 (19) の 3 点セットは「CSV の 1 値が間違っていた」場面の手順であり、手順 3（データ修正マイグレ）に相当するものが無い。★本サブの解＝比較先を `internal/seedgen/testdata/` へ移し、失われる主張（配布シードが CSV 由来である）を意味比較テストで埋める（§1-1） |
| **2** | **`DES-001` §5.1 ／ `SUPP-001` §2.7 ライセンス項** | **`REUSE.toml` の層 B へ `internal/seedgen/testdata/**` を足したことを as-built 化**（§1-2。開発者裁定 2026-09-19）。**★層の割当は 1 つも変えていない**（層 B ＝ SF6 の事実、で不変）**。★禁止ライセンス列にも触れていない** |
| **3** | **`SUPP-001` §5.5**（ガードの規約） | **規則 (4) を凍結分にも評価する ／ 規則 (4a) へ書き込み先検査 ／ 規則 (5) の新設**（§1-3）。★「凍結表を残したまま (4) も評価する」が設計卓の推し（外す）より厳密に強い理由も含める |
| **4** | **`SUPP-001` §5.5.2 規約 (2)** | **★★区間が 1 つの世界を想定していない**（§2-1）。⇒ **49 本・22 ファイルを移すか規約を改めるか**の裁定を先に。★裁定の結果を規約へ反映する |
| **5** | **`M33-overview` §5** | **`M33-03` の射程の記述を指示書 v1.0.0 §2.2 へ寄せて畳む**（§2-2。「旧系列側へ移す」→「分類して消す／書き直す」） |
| **6** | **`DES-002` §11 系** | **★該当なし。⇒ 本サブは API 経路を 1 本も新設・変更していない** |

**マイグレ連番について**: **本サブの消費は 0 本である。⇒ 次に払い出す番号は `000010` のまま**（`D-902`）**。**

---

## §7 教訓（`retrospective-log` 行き）

> **★親は本節を読まなくてよい。** 宛先は `retrospective-log` へのバッチ反映である。

### §7-1 ★★★自分が塞いだ穴と同じ形を、同じ手番で自分が作った

段 3 で golden の比較先を `migrations/` から `internal/seedgen/testdata/` へ移した瞬間、
**`REUSE.toml` の層 B グロブから外れて既定の層 A（AGPL）へ静かに解決していた。**
⇒ これは段 4 で塞ごうとしていた `D-777` の一般形そのものである。

**★気づけた経路は「新しいディレクトリを作ったら層を確かめる」を自分に課したことだけであった。**
検査は 2 本とも緑を返していた（`check-migration-license.sh` は `migrations/` しか見ず、
`check-public-snapshot.sh` は ALLOW/DENY の網羅しか見ない）。
**⇒ 一般形: ファイルの置き場を変える変更は、ライセンス宣言の解決を必ず再確認する。**

### §7-2 ★★段を別の手番として扱うと、前の段の成果物が次の段で失効する

段 1 で `internal/repository/preset/queries.go` と `internal/service/preset/service.go` の
コメントを整え、「seed 経路は `migrate_m2003_test.go` (g) が 0 件を固定している」を残した。
**⇒ 段 2 で自分がそのテストを削除した。**
**★レビューが見つけるまで気づかなかった。⇒ 段をまたぐ参照は、最後にもう一度走査すること。**

### §7-3 ★★未使用宣言の検出器が、doc コメントの言及を「使用」と数えていた

着手時点の検出器は「識別子の出現が 1 回以下なら未使用」としていた。
**⇒ doc コメントがその名前を書いていると 2 回になって生き残る。** 47 件を取りこぼした。
**★コメントを除いてから数えると 0 件まで落ちた。**
**⇒ 「使われているか」を数えるときは、コメントを先に落とす。**

### §7-4 ★★「緑だった」は「正しかった」ではない

`TestRun_M3503_FreshDBIsAlreadyCorrectBeforeTerminus` は 141 本の赤に入っていない＝**緑だった。**
しかしそれは `versionBefore` が黙って HEAD を返していたからであり、
「`000111` を通す前」という前提は新系列に存在しない。
**⇒ 緑のテストも、前提が成立しているかを別に問う。**

### §7-5 ★★旧テストの主張が「その時点でしか成り立っていなかった」ことがある

2 件出た。(a) `TestM1905_TargetGateSwitch` の「除外 23 行の理由は `fastest_unreachable` ただ 1 つ」は
HEAD では単独入力不可だけで落ちる行が 12 行ある ／ (b) `TestRun_M1403c_UpContract` の
「CSV + 移動 9」は system move が 11 種（移動 9 ＋ `drive_parry` ＋ `drive_reversal`）になって失効。
**⇒ 件数や「理由は 1 つ」を写した契約テストは、母数が動くと*主張ごと*古くなる。**

### §7-6 ★消すときは「その主張を誰が見るか」を先に数える

削除した (i) 102 本に、HEAD で表現できる主張が 6 種類含まれていた。
**★とくに `D-317` の交差検査は、repo 全体でガードが 0 件になっていた。**
⇒ 削除されたコメント自身が「UNIQUE では表現できず検査でしか守れない」と書いていたものである。
**⇒ 消す前に `grep` で「同じ主張を見る別のテスト」を数える。★0 件なら消せない。**

---

*以上、`M33-03` 設計伝達レポート。* **★★§3-1 の採番（とくに「乾式を行わない」）と §2-1 の裁定が、親の最優先の手番である。**
