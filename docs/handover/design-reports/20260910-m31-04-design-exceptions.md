# 設計伝達レポート: M31-04（確定反撃でドライブリバーサルを扱う・`SM-098`）

| 項目 | 内容 |
|------|------|
| 作業ID | M31-04 |
| 対象指示書 | `docs/instructions/M31-04-drive-reversal-punish.md` **v1.0.0** ／ チェックリスト `docs/instructions/reviews/M31-04-review-checklist.md` v1.0.0 |
| CHANGE | **未起票。★製造は番号を消費していない**（`D-293`）**。⇒ 本サブの請求は `DES-002` §4 への追随 1 件が主。たたき台は §6。★`REQ-001` への請求は要らない**（§1-4 の実査） |
| マイグレ | **1 本消費＝`000109`**（`000109_data_seed_drive_reversal_system_move`）**。★着手時に実査して払い出された番号である**（`D-800`。自採番していない）**。★層 B**（`CC-BY-SA-4.0`。`check-migration-license.sh --list` で up / down とも `B 新規 CC-BY-SA-4.0` を実測） |
| 作成日 | 2026-09-10 |
| 実装コミット | `claude/m31-04-implementation-plan-kmeq7e`・着手基点 `0660fa2` → `b088039`（**6 コミット・push 済み**）。実装差分（`docs/` 除く）＝**14 ファイル / +703 / −8**（**★新規 6 本〔`000109` の up・down ／ `migrate_m3104_test.go` ／ `m31_04_drive_reversal_test.go` ／ `move-code.ts` ／ `useControllerInputOmission.test.ts`〕はいずれも `+` のみ。deletions 8 行は既存 3 ファイルの編集で説明がつく**＝`E-225`） |
| 源泉 | 完了報告 `docs/progress/M31-04-completion-report.md` ／ レビュー `docs/progress/m31-04-review.md`（**高 3 / 中 3 / 低 3・全 9 件採用・★「高」の不採用 0 件・往復 0 回**）／ **開発者の判断 3 件 ＋ 実機確認 4 件**（2026-09-10。§3-1）／ `CHANGE-169` §3 ／ 実装と同一セッションで生成 |
| 宛先 | 設計卓（親チャット） |

**本レポートは ①独自確定仕様 ②契約違反の独自判断 ③製造判断 ④残課題 に絞る。指示書どおりの部分は割愛する。**

> **★★最重要は §2-1 と §2-2 である。⇒ どちらも「指示書の明文が実装事実と食い違っていた」型であり、親の裁定が要る。**
>
> **★API 経路の新設・変更は無い**（`DES-002` §4.2 の経路表に触れていない）**。⇒ §1 の逐語埋め込み規則は本サブでは発動しない。**
>
> **★★開発者の実機確認は済んでいる**（2026-09-10・§3-1 の後半）**。⇒ §4-5 は実機確認で不要になったため、候補から取り下げた。**

---

## §1 製造が独自に確定した実装仕様（DES 反映が要るもの）

### §1-1 ★★`drive_reversal` の持ち方（**列・値・一次源の全数**）

**投入したもの＝`moves` に全キャラ 1 行ずつ（sf6 キャラ 31 → 31 行）＋ `official_ja_move` alias 31 件。**

| 列 | 値 | 一次源 |
|---|---|---|
| `code` | `drive_reversal` | — |
| `category` | **`system`** | **製造判断。⇒ `drive_parry` の前例に揃えた**（全キャラ一律の共通システム技。§3-2） |
| `damage` | **500** | **`DES-002` §4**（`docs/design/02-architecture.md:1000`）**の逐語「ドライブリバーサルはダメージ 500>0 のためダメージ値では非攻撃と判別できず、名称・位置で除外する」** |
| `on_block` | **-6** | 開発者の逐語（2026-09-08） |
| `recovery` | **27** | 開発者の逐語（2026-09-08）。**★JP 後の有利フレームは `recovery` 列そのものである**（`internal/model/punish.go:18`） |
| `startup` / `active` / `total` / `on_hit` | **NULL** | **一次源が無いため入れていない**（§3-2） |
| `is_projectile` / `is_aerial` / `setup_only` / `fastest_unreachable` | 0（DEFAULT） | — |

**★`character_data/*.csv` には足していない**（意図的）**。⇒ CSV は攻撃技の手入力データであり、全キャラ一律の system move の持ち場はマイグレ側である**（`000025` / `000083` と同じ扱い）**。`csv_db_sync_test` は CSV 行を起点に突合するため DB 側にだけ在る行は母集団外である**（`checked = 2743` は CSV 由来）**。**

**⇒ `DES-003` §3.3（`moves`）へ「ドライブリバーサルは全キャラ 1 行の `category='system'` で持つ。持ち場は CSV ではなくマイグレ」を明文化してほしい。** **★`drive_parry` について同節が既に「ドライブパリィの扱い」を持っているので、その隣に 1 項として並ぶ形が自然である。**

### §1-2 ★★確定反撃の走査は「述語 1 本」ではなく「述語 ＋ 4 つのふるい」である（**指示書が把握していなかった**）

**指示書 §2.1-2 は「走査がどの述語で技を拾っているか」だけを実査項目に挙げていたが、実測すると関門は 2 層あった。**

| 層 | 実体 | 根拠 |
|---|---|---|
| **リポジトリ** | **絞っていない**（当該キャラの `moves` を全投影） | `internal/repository/punish/queries.go` の `listMovesForScanSQL` |
| **サービス（除外）** | `is_aerial=1` ／ `category='system' ∧ code ∈ movementSystemCodes`（移動 9 code）**の 2 条件だけ。⇒ system を一律には除かない** | `internal/service/punishfinder/service.go:420` `isNonPunishableTarget` |
| **サービス（4 つのふるい）** | **`damage=0` → 完全除外**（画面に出ない） ／ **`damage IS NULL` → 手動確認レーン `unknown_damage`** ／ `is_projectile=1` → 手動確認 `distance_dependent` ／ **`on_block IS NULL`（block タブ）→ 手動確認 `data_missing`** | 同 `service.go` pass 1（`:232`〜`:266`） |

**⇒ 「行を入れて `on_block` を入れる」だけでは成立レーンに出ない。`damage` を非 NULL かつ ≠0 にする必要があった。** **★これが `damage=500` の一次源を探した理由である**（§1-1）**。**

**⇒ `DES-005` §5.20（確定反撃・探す）へ、この 4 つのふるいと落ちる先（完全除外か手動確認レーンか）を表として明文化してほしい。** **★次に走査へ技を足すサブが、同じ実査からやり直すことになる。**

### §1-3 ★ジャストパリィタブからの明示除外を機構として置いた（`justParryExcludedCodes`）

**`internal/service/punishfinder/constants.go` に `justParryExcludedCodes`（現在 1 件＝`drive_reversal`）を置き、`service.go` の `just_parry` 分岐の先頭で `continue` する。**

- **成立レーンにも手動確認レーンにも出さない。⇒ 開発者の逐語は「取り扱わなくてもいい」であって「判断を人に回す」ではないため。**
- **★ブロックタブからは外していない。★除外は `code` 単位である**（`category='system'` を丸ごと外していない）**。⇒ `drive_parry` は JP タブに出続ける。この対照を `m31_04_drive_reversal_test.go` の `TestScan_JustParryExclusion_IsCodeScoped` が固定した。**
- **「27F」という事実は `moves.recovery` に残っている。⇒ 捨てると次に誰かが同じことを調べ直す。**

**⇒ `DES-005` §5.20 へ「ガード種ごとに走査から外す技が在りうる」ことを 1 行。★判断は設計卓に委ねる**（実装の内部構造とも読めるため）**。**

### §1-4 ★`SM-098` は「画面 ＋ データ」であって「要件」ではない（**`M31-overview` §5-6 の実査項目への回答**）

**`REQ-001` で確定反撃を扱っているのは `NFR406`**（`docs/design/requirements.md:370`）**＝「相手の技をガード／ジャストパリィした後の隙に当てる技と、そこから発展するコンボの記録」。**

**★D リバは「相手の技」であり、要件文を変えずに収まる。⇒ `REQ-001` への CHANGE 請求は要らない。**

**⇒ `M31-overview` §5-6 の実査項目を閉じてよい。**

### §1-5 ★入力面から明示除外する機構を新設した（`INPUT_EXCLUDED_MOVE_CODES`）＋「入力面」の定義

**`web/src/features/combo/moveSurfacing.ts` に `INPUT_EXCLUDED_MOVE_CODES` / `isInputExcluded()` を置き、2 面へ効かせた。**

| 面 | 実体 |
|---|---|
| **仮想コントローラのタブ（未分類を含む）** | `surfaceBuckets` の `unclassified` 分岐 |
| **全技一覧のプルダウン** | `useControllerInputOmission` の `selectableMoves`（**トグルの状態に関係なく常に落とす**。`omittedCount` には数えない） |

**★★共通技タブの固定 13 code には 1 文字も触っていない**（指示書 §3-2）**。テストで固定した。**

**★★「入力面」に引っ越し取込は含めない**（2026-09-10 開発者判断。§3-1-3）**。⇒ 塞ぎ忘れではなく対象外と決めた結果であることを、`moveSurfacing.ts` と `internal/model/move.go` の 2 か所へ記述として残した。**

**⇒ `DES-005` 第6節（入力面）へ「仮想コントローラに出さない技が在りうる。母集団はタブの述語 ＋ 明示除外の差である」を明文化してほしい。** **★`CHANGE-170` §2.4 が「未分類タブ ＝ `isControllerSurfaced` の裏返し 1 本」と書いているが、本サブでその等式が崩れた**——**未分類 ＝ 裏返し ∧ 非除外**である。**⇒ `CHANGE-170` §2.4 の追随が要る。**

---

## §2 契約・設計に反する独自判断（★親の裁定が要る）

### §2-1 ★★指示書 §2.3 の案 (a) の帰結が、実装事実と食い違っていた

**指示書 §2.3 は案 (a)（列に入れない）の帰結を「★データが無いので走査に出ない。⇒ 最も単純」と書いていた。**

**★実測＝そうではない。** `recovery` を NULL のまま残すと、JP タブの pass 1 は `om.Recovery == nil` の分岐（`service.go:262`）で**手動確認レーンへ `reason=data_missing` として出す**。

| 案 | 指示書の記述 | 実測 |
|---|---|---|
| **(a) 列に入れない** | **「走査に出ない」** | **★出る。JP タブの手動確認レーンに `data_missing` として並ぶ** |
| (b) 入れるが走査から外す | 「事実は残る」 | **本当に出ない**（明示除外のため） |

> **⇒ (a) は「事実を捨てたうえで画面へ出る」という組み合わせになる。⇒ 3 案の比較が成り立っていなかった。**
>
> **★製造は独断で決めていない。** 実測を添えて開発者へ 3 択で回し、**(b) の回答を得てから実装した**（`D-1` のチェックリスト項目）。
>
> **★親の裁定＝指示書 §2.3 の記述を是正するか**（本サブは完了しているので、直す価値は「同じ表を次のサブが写すかどうか」で決まる）**。**

### §2-2 ★★指示書の完了条件 §5 を超えて実装している（**段 4 を「案の提示」で止めなかった**）

**指示書 §6 の完了条件 5 は「**段 4 の案が報告に書かれている**」であり、実装までは求めていない。§2.4 も「設計卓の案＝入力面には出さない」と書くにとどまる。**

**★実測で分かったこと＝「案を書くだけ」だと、その案が実現しない。**

- **行を入れた時点で `isControllerSurfaced()` が false を返し、`drive_reversal` は自動的に未分類タブへ並ぶ**（`CHANGE-170` §2.4）**。⇒ コンボのステップとして押せるようになる。**
- **現状 `category='system'` の 10 code はすべて共通技タブの 13 に含まれており、未分類へ落ちる system move は 1 件も無い。⇒ D リバが最初の 1 件になる。**

> **⇒ 「何もしない」は「出さない」にならない。**
>
> **★製造は独断で広げていない。** 実測を添えて開発者へ 3 択（実装する ／ 案の提示のみ ／ 出す）で回し、**「出さない＝明示除外を入れる」の回答を得てから実装した**（§3-1-2）。
>
> **★親の裁定＝この超過を受理するか。⇒ 受理する場合、`M31-overview` の `SM-098` 行に「入力面の除外まで含む」を追記してほしい**（次に `P4M-005` を起票するとき、面が重なる＝§4-2）**。**

### §2-3 ★他サブのテストのベースライン定数を製造が更新した

**`internal/aliasindex/m2207b_third_stage_test.go` の `wantRows` を `7598` → `7629` へ上げた**（増分 31 ＝ D リバの alias）**。**

**★同テスト自身が「本テストが守っているのは『案 E(第 3 段)が辞書に触らない』であって『`preset_aliases` が永久に N 行である』ではない。seed 波のたびに更新する」と明記しており**（`:170`〜`:176`）**、規約どおりの更新である。** **⇒ 案 E が足していないことの直接の証拠である「素の SA 番号の行 0 行」は 0 のままである。**

**★念のため §2 に挙げる。⇒ 他サブの資産に触れた事実を親が把握していないと、後で「誰が上げたか」を追えなくなるため。**

---

## §3 製造の判断

### §3-1 開発者へ確認して確定した点

| # | 事項 | 回答（2026-09-10） | 実装への効き方 |
|---|---|---|---|
| **1** | **段 3＝ジャストパリィ（27F）の表現**（指示書 §7-1） | **案 (b) 入れるが走査から外す** | `recovery=27` を投入 ＋ `justParryExcludedCodes`（§1-3） |
| **2** | **段 4＝入力面に出すか**（指示書 §7-2） | **出さない＝明示除外を入れる** | `INPUT_EXCLUDED_MOVE_CODES`（§1-5・§2-2） |
| **3** | **レビュー 高-3＝引っ越し取込を「入力面」に含めるか** | **含めない・明記。逐語＝「ユーザーがあり得ないコンボを入れたりとかは考えなくていい」** | **実装は変えない。⇒ コード 2 か所へ「対象外と決めた結果である」と記述**（§4-3） |

**★指示書 §7 の「どちらも段 1 の報告と一緒に出すこと。⇒ 2 回に分けて聞かない」を守った。** #1 と #2 は段 1 の実査結果と同じ手番で 1 回にまとめて出した。#3 はレビュー後に新たに出た論点である。

**★★あわせて開発者の実機確認を受けている**（2026-09-10。**コード上では判定できない範囲**）**。**

| # | 確認したこと | 結果 |
|---|---|---|
| **1** | **ブロックタブに D リバが出るか。★有利フレーム `+6` が実際のゲームの体感と合っているか** | **★★合っている。⇒ `-6` を `-(on_block)` に乗せて `+6` とする既存の算出式が、この技にも当てはまることが実機で確かめられた** |
| **2** | **`+6` で出る始動技候補に、距離的に届かない技が混じっていないか** | **混じるが仕様どおりで問題なし**（走査は距離判定を持たない）**。⇒ 実用上の邪魔にはなっていない** |
| **3** | **ジャストパリィタブから本当に消えているか。★消えることが気持ち悪くないか** | **消えている。★★「気持ち悪さもなかった」。⇒ §4-5 はこれで不要になった** |
| **4** | **入力面に出ていないこと ／ 技名が生 code になっていないこと** | **どちらも良好**（未分類タブ・全技一覧プルダウンとも出ない ／ 表示は「ドライブリバーサル」） |

**★引っ越し取込（§4-3）は判断済みのため確認していない**（開発者の判断）**。⇒ 決着済み・対象外という §4-3 の記録はそのまま有効である。**

### §3-2 推測で進めた点

| # | 判断 | 根拠と、外れたときに何が起きるか |
|---|---|---|
| **1** | **「27F」＝ JP 後の有利フレーム＝ `recovery` 列** | `internal/model/punish.go:18` の「just_parry: 有利フレーム = opponent_move.recovery」。**★外れていても現在は無害である**——**JP タブからは明示除外しており、この値は誰も読まない。⇒ 除外を外した日に初めて効く** |
| **2** | **`category='system'`** | **`drive_parry` の前例**（全キャラ一律の共通システム技）**。★指示書は category を指定していなかった**（§2.1-1 が「在るなら何か」を問うだけ）**。⇒ 走査は category ではなく code で絞るため、判定への影響は無い**（§1-2） |
| **3** | **`startup` / `active` / `total` / `on_hit` を NULL のまま** | **一次源が無い。⇒ 推測値を入れると、後から「実測か推測か」が分からなくなる。★走査には不要である**（§1-2 の 4 つのふるいはこの 4 列を見ない） |
| **4** | **`0` 行と `31` 行の中間を採らなかった** | 指示書 §2.2 の設計卓案どおり。**★段 1 の実測でも変える理由は出なかった** |

---

## §4 設計担当が未把握の残課題・申し送り

> **★`followup-backlog.md` の本表は製造が編集していない**（`D-382`）**。⇒ 設計卓が畳める形で以下に置く。**

### §4-1 `drive-reversal-row-missing-on-new-character-wave`（**新規登録**）

| 項目 | 内容 |
|---|---|
| **何が起きるか** | **キャラ追加波で `000109` の追随マイグレを打たないと、そのキャラだけ確定反撃に D リバが出ない。★動作は落ちず、テストも緑である** |
| **根拠** | **移動 system move が同じ性質を持ち、実際に追随してきた**——`000025` → `000044`（マノン）→ `000054`（第三波）。**★検出する常設テストは無い**——`internal/infra/migration/migrate_m3104_test.go` の比較区間は `v107 → v109` に閉じており（`SUPP-001` §5.5 規約 (1) どおりで正しい）、`migrate_head_test.go` は FK の宙吊りしか見ない |
| **割付の候補と理由** | **★★フェーズ4（`α①`）＝次のキャラ追加波の指示書の DoD へ。⇒ 既存の `backfill-migration-does-not-catch-later-rows` が同じ一般形**（「1 回きりのマイグレは、その後に INSERT される行を拾えない」）**を持っており、本件はその「行」が「キャラ」になった版である。⇒ 同じ波の手番で一緒に扱うのが安い** |
| **新規/更新** | **新規登録。★ただし `backfill-migration-does-not-catch-later-rows` からの相互参照を張ってほしい** |

### §4-2 `input-excluded-codes-vs-setup-only-overlap`（**新規登録。★既存 `setup-only-column-has-no-input-path` の更新も要る**）

| 項目 | 内容 |
|---|---|
| **何が起きるか** | **「入力から外す」仕組みが 2 通り並ぶ。⇒ 次に触る人がどちらを正とするかで迷う** |
| **根拠** | **本サブが `INPUT_EXCLUDED_MOVE_CODES`（`web/src/features/combo/moveSurfacing.ts`）を新設した＝1 件目である。★`P4M-005`**（`setup_only` の活用）**の目的は `CHANGE-022` §2 / `DES-003` §3.3 が「コンボ登録 UI の技選択から除外」と定めており、同じ面である**（`M31-RESEARCH-01` 報告） |
| **割付の候補と理由** | **★★`P4M-005` の実装サブ（`M31-RESEARCH-01` の続き）。⇒ あちらは「どの `move_code` に立てるか／非表示の範囲／付与口」の 3 つが未決である。⇒ 本件はその「非表示の範囲」に直結する。★別々に決めると必ず二重化する** |
| **新規/更新** | **新規登録 ＋ 既存 `setup-only-column-has-no-input-path` へ「`M31-04` が FE 側に別経路を作った。範囲を決めるときに突き合わせること」を 1 行追記** |

### §4-3 `intake-face-excluded-from-input-face-by-decision`（**新規登録・★「解消しない」型の記録**）

| 項目 | 内容 |
|---|---|
| **何が起きるか** | **引っ越し取込から `drive_reversal` 入りコンボを作れる。⇒ 作った後はエディタのプルダウンに同じ技が出ないため、同じ手で作り直せない非対称が残る** |
| **根拠** | **① 逆引き＝`internal/service/intake/service.go:125` の `ax.Lookup(step.NameCandidate)` は `category` で 1 件も絞らない。★`Lookup`（`internal/aliasindex/aliasindex.go:153`）は「別名テキスト → code 集合」でキャラ横断に code を重複排除するため、31 キャラ共通の「ドライブリバーサル」は 1 件に解けて `Resolved=true` になる。② 技セレクタ＝`web/src/pages/IntakeHelperPage.tsx:73` の `moveOptions` は `useMovesByCharacter` の全件から組む** |
| **割付の候補と理由** | **★★「解消」ではなく「決着済み・対象外」として記録してほしい。⇒ 2026-09-10 開発者判断（§3-1-3）で対象外と決まっている。★記録の目的は、次に触る人が「穴だ」と読んで勝手に塞がないようにすることである**（同じ旨をコード 2 か所へも残した） |
| **新規/更新** | **新規登録・状態＝「★対象外と決定（2026-09-10）」** |

### §4-4 `check-import-order-baseline-drift`（**既存行の更新**）

| 項目 | 内容 |
|---|---|
| **更新内容** | **★実測値が 4 つ目に揃った＝`M31-04` は `99`**（`M35-01` が `99` ／ `M30-02` が `100` ／ ベースラインは `101`）**。⇒ 本サブも下げていない。★根拠は同じ**——部分的なツリーの値であり、マージした瞬間にずれる |
| **★あわせて 1 件** | **`M31-04` の Phase C で足したテストが本検査に 1 件当たった**（`useControllerInputOmission.test.ts` で `React` をサードパーティの後に置いていた）**。是正済み。⇒ ★型検査もテストも緑のまま通る型であり、本検査が唯一の検出経路だった。⇒ 本検査を緩めない理由の実例が 1 つ増えた** |
| **新規/更新** | **既存行の更新。状態は「未着手」のまま** |

### §4-5 `des-005-punish-just-parry-silent-exclusion`（**★★登録しない。実機確認で不要になった**）

| 項目 | 内容 |
|---|---|
| **もともとの懸念** | **利用者から見ると「ブロックタブに在る技が JP タブでは消える」だけであり、将来「バグでは」と再調査される芽が残る**（レビュー 低-3） |
| **★★取り下げの根拠** | **2026-09-10 の開発者の実機確認＝「消えている。気持ち悪さもなかった」**（§3-1 の実機確認 3）**。⇒ 懸念は実機で成立しなかった。** **★`DES-005` §5.20 への画面表現の追随は要らない** |
| **★残すもの** | **除外の理由はコード側に残っている**（`internal/service/punishfinder/constants.go` の `justParryExcludedCodes` 注記）**。⇒ 次に「なぜ消えるのか」を調べる人はそこへ辿り着ける** |
| **新規/更新** | **★登録しない**（`followup-backlog` へ行を足さない）**。⇒ 本レポートに取り下げの経緯だけを残す** |

### §4-6 ★★マイグレ連番の実査（**ボード §2.2 の更新。★製造はボードを編集しない＝`D-382`**）

**`000109` を消費した。⇒ ボード §2.2 の「次に払い出す番号」の更新が要る。**

**★ただし `000108` は本サブの作業ツリーに存在しない**（並列サブが消費する番号として払い出されている）**。⇒ 実査値はマージ後でないと確定しない。★本サブの契約テストは比較区間の始端に実在する `000107` を使っている**（理由はテスト本文に書いた）**。**

---

## §5 参考（触れていない＝不変の証跡）

- `character_data/*.csv`: **`git diff --stat 0660fa2` の 14 ファイルに 1 件も無い**（チェックリスト A-1。`M35-02` と衝突しない）
- `internal/seedgen/` の golden: 差分 0 バイト（A-3）
- `docs/design/`: 差分 0（指示書 §3-4）
- `go.mod` / `go.sum` / `web/package.json`: 差分 0 行（新規依存 0 件・ピン留め 0 件）
- 共通技タブの固定 13 code: `COMMON_MOVE_CODES` 不変（`moveSurfacing.test.ts` の `toHaveLength(13)` が固定）
- `isControllerSurfaced` の式: 不変（`isInputExcluded` は同関数を経由しない＝§1-5）
- 既存 system move: `drive_parry` の行数不変（`TestRun_M3104_UpContract`）
- `web/CLAUDE.md` §1 台帳: 新規キー 0 件（`check-browser-storage-keys.sh` 緑）
- 常設検査 8 本すべて違反なし（1 本目 `check-artifact-integrity.sh` を含む）

---

## §6 CHANGE 起票のたたき台（設計担当向けチェックリスト）

> **★番号は起票時に `docs/handover/change-number-registry.md` §1 で採番すること。⇒ 製造は 1 本も消費していない**（`D-293`）**。**

**★マイグレ連番＝`000109` を消費した。着手時に実査して払い出された番号である**（`D-800`）**。⇒ 予約帯は作っていない。**

| # | 反映先 | 内容 | 由来 |
|---|---|---|---|
| **★★1** | **`DES-002` §4** | **`CHANGE-169` §3 が書いた「必要が生じた」の as-built。⇒ 実際に走査対象へ入れた**（マイグレ `000109`・全キャラ 31 行）**。★取込からの除外そのものは変えていない** | §1-1 |
| **★2** | **`DES-003` §3.3** | **ドライブリバーサルの持ち方**（全キャラ 1 行の `category='system'` ／ 持ち場は CSV ではなくマイグレ）**。★`drive_parry` の項の隣へ** | §1-1 |
| **★★3** | **`DES-005` §5.20** | **走査の 4 つのふるい**（`damage=0` 完全除外 ／ `damage IS NULL` ／ `is_projectile` ／ `on_block IS NULL`）**と落ちる先の表。⇒ 次に走査へ技を足すサブが同じ実査からやり直さないため** | §1-2 |
| **4** | **`DES-005` §5.20** | **ガード種ごとに走査から外す技が在りうること**（`justParryExcludedCodes`）**。★採否は設計卓の判断。★★画面表現の追随は要らない**（実機確認で決着＝§4-5） | §1-3 |
| **★5** | **`DES-005` 第6節 ／ `CHANGE-170` §2.4 の追随** | **★「未分類タブ ＝ `isControllerSurfaced` の裏返し 1 本」が崩れた。⇒ 未分類 ＝ 裏返し ∧ 非除外である** | §1-5 |
| **6** | **`M31-overview`** | **`SM-098` 行に「入力面の除外まで含む」を追記**（§2-2 を受理する場合）**。★§5-6 の実査項目は閉じてよい**（`REQ-001` は不要＝§1-4） | §1-4 ／ §2-2 |
| — | **`REQ-001`** | **★請求しない。** `NFR406` の文言に収まる（§1-4） | §1-4 |

---

## §7 教訓（retrospective 行き・**親は読まなくてよい**）

> **本節の宛先は `retrospective-log` へのバッチ反映であり、実施者は設計担当**（反映担当は 2026-08-11 に廃止）**。**

### §7-1 ★★古いマイグレを「同型」として写すときは、その後に足された列を先に数えること

**`000025` を写して `preset_aliases.character_id` を落とした。⇒ 同マイグレは `000074`（`character_id` を非正規化で追加）より前であり、その列を持たない。**

**★NULL でも `INSERT` は通り、alias は表示にも出る。⇒ 動作では気づけない。** **★`000075` の UNIQUE 索引 2 本の構成列でもあり、NULL だと一意性が効かない。**

**★気づけた唯一の経路は `internal/repository/preset` の「`character_id` が NULL の行が 0 件」テストであった。**

**⇒ 機構化: 「前例マイグレを写す」ときは、`前例の連番 → HEAD` の間に当該テーブルへの `ALTER TABLE` が無いかを `grep` で先に数える。★「同型だから安全」は、スキーマが動いていない期間にしか成り立たない。**

### §7-2 ★★「案 X は出ない」と書かれていたら、出ないことを実測してから比較すること

**指示書 §2.3 の案 (a) は「データが無いので走査に出ない」と書いていたが、実測では手動確認レーンに出た**（§2-1）**。⇒ 3 案の比較そのものが成り立っていなかった。**

**⇒ 機構化: 設計卓が案の帰結を書いた表は、実装前に 1 行ずつ実測で確かめる。★とくに「出ない／効かない／影響しない」と書かれた行が危ない**——**肯定の記述は実装すれば分かるが、否定の記述は誰も試さないまま次の表へ写される。**

### §7-3 ★★「何もしない」が「出さない」になるとは限らない（**面の数え上げ**）

**段 4 は「入力面には出さない」だったが、行を入れた時点で未分類タブへ自動的に並ぶ**（§2-2）**。さらにレビューが 3 面目**（引っ越し取込）**を見つけた**（§4-3）**。**

**⇒ 機構化: 「出さない」を決めたら、**その技へ到達できる面を `grep` で数え上げてから**着手する。★数え上げの起点は「その技を母集団に含む述語」であり、`moves` を全件引いている箇所がそれである**（本件は 3 か所＝未分類タブ ／ 全技一覧 ／ 取込セレクタ ＋ 逆引き）**。**

### §7-4 ★テストを足した手番で、そのテストが検査に当たっていないかを見ること

**Phase C で足した `useControllerInputOmission.test.ts` が `check-import-order.sh` に 1 件当たった**（`React` をサードパーティの後に置いた）**。⇒ `tsc` もテストも緑のまま通る。**

**⇒ 機構化: 新規 `.ts` / `.tsx` を足したら、その手番で `check-import-order.sh` を回す。★`M24-08` が 27 ファイルを静かに規約外にしたのと同じ型であり、本検査が唯一の検出経路である。**

### §7-5 ★「消しても緑」を疑ったら、実際に消して赤を見ること

**レビュー 中-1 が「`selectableMoves` の filter を消しても全テスト・全 E2E が緑で通る」と指摘した。⇒ テストを足したあと、実際に filter を外して赤になることを確かめてから戻した。**

**⇒ 機構化: 「このテストは本当に守っているか」を主張するときは、変異させて赤を見る。★通ることは、守っていることの証拠にならない。**

---

*以上、設計伝達レポート M31-04。*
