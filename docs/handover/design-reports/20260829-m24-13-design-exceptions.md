# M24-13 設計伝達レポート（例外レポート）

| 項目 | 内容 |
|------|------|
| 対象 | **親チャット（設計卓）** |
| 発信 | 製造担当 Claude Code / 2026-08-29 |
| 指示書 | `docs/instructions/M24-13-draft-requires-recipe-step.md` **v1.1.0**（**★着手は v1.0.0。実装途中の 2026-08-29 に `D-588` で §4.6＝`VAL-S04` が加わった**）／ チェックリスト **v1.1.0** |
| CHANGE | **`CHANGE-139` v1.1.0**（設計卓が起票済み。**製造は番号を消費していない**） |
| 実装コミット | ブランチ `claude/m24-13-implementation-dal6rs` / **着手基点 `11bcb6a` → 8 コミット**（`322190e` まで push 済み。**本レポートのコミットのみ未 push**） |
| 関連 | 完了報告 `docs/progress/M24-13-completion-report.md` ／ レビュー `docs/progress/m24-13-review.md`（重大 0 / 高 3 / 中 4 / 低 5）・`docs/progress/m24-13-review-2.md`（重大 0 / 高 1 / 中 1 / 低 6）。**★2 回とも全件採用・不採用 0** ／ モック `docs/progress/M24-13-mock/m24-13-save-blocked.html` |
| 作成条件 | **実装直後の同一セッションで生成**。開発者とのやり取りで確定した判断（§3-1）を含む |

本レポートは **①独自確定仕様 ②契約違反の独自判断 ③製造判断 ④残課題** に絞る。**指示書どおりの部分は割愛する。**

**★★最重要は §2-1（仮登録トグルのラベルを製造が変えた）と §4-1（`SUPP-001` §2.1 と `REQ-001` FR305 の失効。どちらも影響先に挙がっていなかった）である。**

---

## §1 製造が独自に確定した実装仕様（DES 反映が要るもの）

### 1-1 ★★`POST` / `PUT /api/combos` の応答（**`DES-002` §4.2 の as-built**）

**★逐語を埋める**（設計卓は完了報告を読まない・製造ブランチの完了報告は設計卓の作業ツリーに無いため）。**以下はすべて実行結果である**（使い捨て DB ＋ `go run ./cmd/combomgr` で実測。2026-08-29）。

**(a) 仮登録・ステップ 0 本 → `400`**

```json
{"error":{"code":"validation_failed","message":"バリデーションエラーがあります",
  "details":{"validations":{"issues":[
    {"code":"VAL-C09","severity":"error","field":"steps",
     "message":"レシピが空です。少なくとも 1 ステップ必要です"}]}}}}
```

**(b) 仮登録・技が未指定のステップ 1 本 → `201`**（`{"id":1,"isDraft":true,"stepCount":1,"starterMoveCode":"","defaultRecipe":"","version":1}`。**★`validations` キーも `warnings` キーも出ない**＝警告 0 件）

**エラー契約の全行（本サブで変わる行のみ）**

| 状況 | ステータス + コード | 備考 |
|---|---|---|
| 仮登録・ステップ 0 本の `POST` | **400 `validation_failed`** ＋ `issues[].code = "VAL-C09"` | **★新設ではない。既存コードが新しい入力集合で出るようになっただけ** |
| 仮登録・ステップ 0 本の `PUT` | **400 `validation_failed`** ＋ 同上（**★`message` は空文字**＝`handler.go:443` が `Message` を設定していない。**`POST` とは非対称。本サブでは触れていない**） | 同上 |
| 仮登録・ステップ 1 本（`move_id` NULL） | **201 / 200** | **★`VAL-D02` は不変** |

**母集団の述語**——**数えるのは `len(steps)` だけである。`move_id` の中身を見ない。** `move_id` NULL には 2 つの目的があり（非技ステップの正規表現＝`DES-004` §2.2 ／ 仮登録の技未指定＝`VAL-D02`）、**どちらも 1 本として数える。**

**★持たせなかった分岐**——**新しいエラーコードは 1 件も足していない。`VAL-D04` も作っていない。** **`VAL-C09` の `field` は添字を持たない `"steps"` である**（フロントは完全一致集合 `RECIPE_TAB_FIELDS` の側で拾う。前方一致 `steps[` ではない）。

**⇒ `DES-002` §4.2 の経路表の行は変わらない。ただし「仮登録の `POST`/`PUT` が 400 を返しうる」ことは as-built として明文化してほしい。**

### 1-2 ★`VAL-S04` の適用面と、判定を tx 内へ移した形（**`DES-006` §2.3 に `VAL-S04` の記述が無い**）

`DES-006` §2.3 は **`VAL-C02` についてのみ**「判定は登録と同じ書き込みトランザクションの内側で `*sql.Tx` を使って行う」を固定し、適用面を 4 経路の表で持っている。**`VAL-S04` 側に同じ記述が無い。**

as-built:

| 数える単位 | 母数 | 内訳 |
|---|---:|---|
| **判定関数の呼出元** | **3** | `setup/service.go:246` `CreateSetup` ／ `:321` `CreateSetupInTx` ／ `:494` `UpdateSetup` |
| **★入口（API ＋ CSV 取込）** | **4** | `POST /api/combos/{comboId}/setups` ／ `PATCH /api/setups/{id}` ／ CSV 取込（`comboio/import.go:274`） ／ **`POST /api/combos` の同梱セットプレイ**（`combo/service.go:384`） |

**★followup の「3 経路」は前者としては正しい。** **⇒ `M-89`（3 と書いて実は 4）の型は、数を訂正するのではなく単位を明示することで避けた。** **⇒ 反映時はどちらの単位で書くかを決めてほしい。**

**★★着手前は 3 か所とも塞がっておらず、塞がっていない理由が 2 種類あった**——`CreateSetup` / `UpdateSetup` は**判定が `BeginTx` の前**、`CreateSetupInTx` は**tx を持ちながら判定が `*sql.DB` 直読み**（`D-360`）。**⇒ 後者は競合ではなく決定論的な穴であった**（§1-3）。

**⇒ `DES-006` §2.3 へ `VAL-S04` のぶんを足してほしい**（`VAL-C02` と同じ形。**★「同じ 1 本を呼ぶ」の規約に倣い、実装も `M24-11` の `ComboDuplicateAdapter` を踏襲して `SetupDuplicateAdapter` を置いた。2 つ目の形は作っていない**）。

### 1-3 ★★`POST /api/combos` の同梱セットプレイの応答が変わった（**201 → 400 の経路が 1 本増えた**）

**同一リクエストで同梱した 2 本目の同一レシピが、1 本目を見られていなかった。** `VAL-S04` は名前を見ない（`DES-006` §3）ため、**名前だけ違う同一レシピが 2 本とも通っていた。**

実測（同上の probe。**★実行結果である**）:

```json
{"error":{"code":"validation_failed","message":"バリデーションエラーがあります",
  "details":{"validations":{"issues":[
    {"code":"VAL-S04","severity":"error","field":"setups[1]",
     "message":"同一レシピのセットプレイが既にこのコンボに紐付いています"}]}}}}
```

- **★`field` は `setups[1]`**（`combo/service.go:389-395` が `setups[%d].%s` / `setups[%d]` へ包み直す。**本サブでは触れていない**）。
- **★CSV 取込へは波及しない**——`import.go:274` は `CreateSetup` を 1 本ずつ呼んで個別にコミットするため、2 本目は以前から見えていた。
- **★★新たに 500 が出る経路は増えていない**（指示書 §4.6 の要求）。**probe で 500 を 1 件観測したが、原因は存在しない `move_id` による FK 違反であり、同梱が 1 本でも起きる。`git diff 11bcb6a` に `InsertSteps` とその失敗処理の差分は 0 件**であり、**本サブ由来ではない。**

**⇒ `DES-002` §4.2 ／ `DES-006` §3 へ「同梱セットプレイは同一リクエスト内でも `VAL-S04` の対象である」を明文化してほしい。**

### 1-4 ★CSV 取込のプレビュー層も仮登録へ適用した（**severity は据え置き**）

`csvcore/validate.go:158` の `VAL-C09` から仮登録の除外を外した。**★severity は `WARNING` のまま**＝**取込可否は変わらない**（`Importable` は `Status != StatusError` で決まる）。

**★外さないと「プレビューは無警告 → commit で検証エラー」というズレが残る**（commit は `comboSvc.Create` を通るため必ず落ちる）。**開発者裁定 2026-08-29**（§3-1）。

**⇒ `DES-006` §5 / §6 の射程。あわせて「プレビューは WARNING・本体は ERROR」という severity の非対称が残ることを明記してほしい**（§4-5）。

### 1-5 ★保存できない理由の見せ方（**`DES-005` §5.7 の as-built**）

| 項目 | as-built |
|---|---|
| **ボタン** | **消していない**（`D-582`）。`disabled` ＋ `aria-describedby` |
| **文言** | **`レシピを 1 つ以上入力してください(仮登録でも必要です)。`**（`ComboEditor.tsx` の `SAVE_BLOCKED_EMPTY_RECIPE`）。**★「技を選べ」とは言っていない**——技が未指定のステップ 1 本でも保存できるため |
| **置き場** | **保存ボタン行の直前**（`ValidationDisplay` の直後）。**`role="status" aria-live="polite"` の器を常設し、中身だけ差し替える**（器へ `empty:hidden`。**★空の箱が `space-y-4` に 16px の余白を足すため**） |
| **★順送り** | **理由の側に `tabIndex={0}` を置いた。** **`disabled` なボタンはフォーカスを受けない**ため、ボタンに説明を持たせるとキーボードだけでは読めない |
| **条件** | **`steps.length` だけ。★タブにも仮登録トグルにも依存しない** |
| **物理コントローラ** | `RecipeBuilder` の `canSave` も同じ `canSaveNow` を見る（**「押せない条件の定義は 1 か所」の既存の規律を維持**） |

**⇒ `DES-005` §5.7 へ上表を明文化してほしい。★とくに「理由の側を順送りの停止点にした」は、次の担当が `disabled` なボタンへ説明を付け直す誘惑に対する歯止めになる。**

### 1-6 ★FE の zod は「掛かる範囲を広げる」を採った

`schema.ts` の `steps.min(1)` を **`comboFormBase` へ移し**、`comboFormSchemaPublished` / `comboFormSchemaDraft` の上書きを**両方削除**した。**★モードごとに 2 本目を書き起こしていない。** 指示書 §4.4 の 2 択のうち「**FE で先回りして塞ぐ**」側である。

**⇒ 帰結として §4-2（画面から到達できる検証エラーが 0 件になった）が生じている。**

---

## §2 契約・設計に反する独自判断（**★親の裁定が要る。2 件**）

### 2-1 ★★仮登録トグルのラベルを製造が変えた

| 論点 | 内容 |
|---|---|
| **何に反したか** | **`DES-005` §5.7 項目3** が「**★ラベルは 2 つの版で同一にする〔正典＝`ComboDraftToggleField` の `DRAFT_TOGGLE_LABEL`〕**」として **`M24-12` の as-built を記録している。** 指示書 §9.2 が委任したのは「**保存できない理由の文言**」であって、トグルのラベルではない |
| **なぜそう判断したか** | **旧ラベル＝「仮登録として保存(★レシピ未入力や重複コンボの登録等を許容)」。★これはコメントではなく利用者に見える文字列である。** 本サブの後、**同じ画面の下で保存ボタンが `disabled` になり「レシピを 1 つ以上入力してください(仮登録でも必要です)。」と出る。⇒ 画面が上下で正反対のことを言う状態**になった。**★旧文字列はテスト 2 本が固定しており、後から直すと赤くなるため「意図的に固定されている」と誤読されうる形でもあった** |
| **実装がどうなっているか** | `ComboDraftToggleField.tsx:35-36`（`DRAFT_TOGGLE_LABEL`）／ `:41`（`DRAFT_TOGGLE_NOTE`）から「レシピ未入力」を落とした。**新＝`仮登録として保存(重複コンボの登録等を許容)`。★`VAL-C02` が仮登録で走らないことは今も事実なので「重複コンボの登録等」は残した。★正典 1 本の規律は崩していない。** 追随＝`ComboEditorBasicFields.test.tsx:1231` ／ `m24-12-editor-rebuild.spec.ts:206` |
| **★見つかった経路** | **2 回目のレビュー（高-A）である。★1 回目の取り込みで「`grep` で残存 0 を確認した」と書いたが、走査したのは E2E の spec ヘッダだけで `web/src/` を見ていなかった。★モック HTML もトグルのラベルを描いていないため、開発者の目視確認でも見えなかった** |

**⇒ 受理して `DES-005` §5.7 の as-built を更新するか、文言を設計卓が決め直すかの裁定を求める。**

### 2-2 ★`M24-04`(4) の E2E で、エラーの作り方をサーバ応答のスタブへ替えた

| 論点 | 内容 |
|---|---|
| **何に反したか** | 指示書 §2.2 が「**`M24-12` の成果を 1 つも落とさない**」（うち「エラーの在り処をタブ見出しで示す」）とし、チェックリスト 5-4 が「**E2E の書き換えで検証内容を緩めていたら重大**」としている |
| **なぜそう判断したか** | 元の仕掛けは「基本情報タブに居たまま、**レシピ 0 件で保存する**」であり、**本サブがその唯一の到達経路を塞いだ。** **★あわせて、画面から到達できる検証エラーが 1 件も無くなった**（§4-2）。⇒ **命題を保つには、サーバ応答を差し替える以外に手が無かった** |
| **実装がどうなっているか** | `m24-04-editor-input-safety.spec.ts:346-` が `page.route` で `POST /api/combos` を 400 + `field:"steps[0].moveId"` に差し替える（**先例＝`m22-01` / `m22-02`**）。**★assert は元と同一**（レシピ側にバッジ 1 ／ 基本情報側は 0） |
| **★代償** | **BE の `field` 書式を実サーバの出力と突き合わせる観測が E2E から消えた。** ⇒ Go 側で補った（`TestIssueFieldFormat_RecipeSideUsesIndexedPrefix` ／ `..._RushVariantErrorUsesIndexedPrefix`）。**★後者は 2 回目のレビュー 中-A で足した**——**フロントは `severity !== "error"` を捨てるため、前方一致 `steps[` を本番で実際に通る唯一の生産者は `VAL-C12`（ERROR）だった** |

**⇒ 「観測手段を替えた」ことの受理可否と、§4-2 の位置づけの裁定を求める。**

---

## §3 製造の判断

### 3-1 開発者へ確認して確定した点

| # | 論点 | 確定内容（2026-08-29） |
|---|---|---|
| 1 | **§3.3-4 の既存データの件数**（本セッションのコンテナに DB が無く数えられない） | **開発者がローカル DB で照会 SQL を実行。★ステップ 0 本の仮登録は 0 件**（仮登録の総数 0 ／ コンボ総数 **86**）。**⇒ 既存データへの影響は無く、開発者の手番は発生しない**（`CHANGE-139` §5 リスク 1 は空振り） |
| 2 | **CSV 取込のプレビュー層も仮登録へ適用するか** | **適用する**（§1-4）。**severity は WARNING のまま据え置く** |

### 3-2 推測で進めた点（**指示書 §9.2 の委任範囲**）

| # | 事項 | 採った形 |
|---|---|---|
| 1 | 保存できない理由の**文言** | §1-5 のとおり |
| 2 | 理由の**置き場の具体** | **理由の側を順送りの停止点にした**（`tabIndex={0}`）。§1-5 |
| 3 | **FE で先回りするか、BE の応答を表示するだけにするか** | **先回りする側**（§1-6）。**理由＝既に本登録だけに掛かる検証が在り、指示書 §4.4 が「その掛かる範囲を広げる」を第一の選択肢としていた** |
| 4 | `web/e2e/support/` のヘルパの命名とシグネチャ | `minimalDraftSteps()`（API 経路）／ `addMinimalRecipeStep(page, returnTo)`（UI 経路） |
| 5 | **★E2E fixture の最小レシピに「非技ステップ」を選んだ** | **実在の技を足すと `VAL-C03` の WARNING が新たに出て、警告を数えている spec を巻き込む。**（`starter_move_id` が NULL なら C03 は early return、`move_id` が NULL なら C08 / C12 はスキップ）**⇒ fixture へ警告を 1 件も増やさない形にした。対照テストで固定してある** |

### 3-3 ★既存テストの入力を変えた（**緩和ではない**）

`TestService_Create_WithMultipleSetups_OK` ／ `TestService_Create_BundledVerifiedConditions_PerSetup` は**名前だけ違う同一レシピ**を 2 本同梱しており、§1-3 の穴に乗っていた。**⇒ fixture のレシピを分けた。★検証内容は緩めていない**——同一レシピの 2 本同梱は本来 `VAL-S04` の重複である。

---

## §4 設計担当が未把握の残課題・申し送り

**★`followup-backlog` の本表は製造が編集しない（`D-382`）。設計卓が畳めるよう、スラッグ / 内容 / 根拠 / 割付候補 / 新規か更新か を揃える。**

### 4-1 ★★`SUPP-001` §2.1 と `REQ-001` FR305 の失効（**どちらも影響先に挙がっていなかった**）

- **スラッグ**: `m24-13-stale-design-statements`（**新規登録**）
- **何が起きるか**: **仮登録の「レシピ 0 件許容」を前提にした記述が 3 か所残っている。** 後任がコピーすると撤回済みの前提が複製される。
- **根拠（実測の `grep`）**:
  - `docs/design/supp-001-detailed-design.md:93` —「仮登録コンボ(`is_draft=true`)は … **レシピ0ステップを許容する**」
  - `docs/design/supp-001-detailed-design.md:94` —「**VAL-C09(レシピ空はERROR)は本登録時のみ適用。仮登録時は WARNING にも出さない**」
  - `docs/design/requirements.md:162` — FR305 「**仮登録状態のコンボに対してはバリデーションを適用しない、または警告のみとする**」（**★ERROR の `VAL-C09` が掛かるため後段が成立しない**）
- **★指示書 §4.5 も `CHANGE-139` §2.4 も名指ししているのは `REQ-001` FR009 だけ**であり、**`SUPP-001` と FR305 は誰も挙げていなかった。**
- **割付の候補**: **設計卓が `CHANGE-139` の反映と同じ手番で処理する**（製造は `REQ-001` / `SUPP-001` を編集しない）。
- **★教訓**: **「影響設計書」の欄は起票時の見込みであって全数ではない。** §7-1 参照。

### 4-2 ★★画面から到達できる検証エラーが 1 件も無くなった

- **スラッグ**: `editor-validation-errors-unreachable-from-ui`（**新規登録**）
- **何が起きるか**: **`M24-04`（SM-148）／`M24-12` の「エラーの在り処をタブ見出しで示す」が、実装としては生きているが利用者が見る機会がほぼ無い。**
- **根拠（実測）**: メモ `maxLength={2000}`（`ComboEditorBasicFields.tsx:759`）＝zod の `max(2000)` と同値 ／ ステップメモ `maxLength={200}`（`RecipeBuilder.tsx:288`）＝zod の `max(200)` と同値 ／ 数値欄は `clampNumericString` でクランプ ／ **レシピ 0 件が残る唯一の経路だったが本サブで塞いだ**。`VAL-C02` は `field` が空で振り分け対象外（モーダルで出る）。
- **割付の候補**: **設計判断（機能の位置づけ）。M24-CLOSE か M25。** 選択肢＝(a) そのまま残す（API 直叩き・将来の BE 検証への備え） (b) UI 側の上限を緩めて到達可能にする (c) 機能を畳む。**★製造は判断しない。**
- **★関連**: §2-2（E2E の観測手段を替えた）。

### 4-3 ★`DES-006` §2.3 に `VAL-S04` の tx 内判定の記述が無い

- **スラッグ**: `des006-val-s04-tx-scoped-判定-未記載`（**新規登録**）／ 根拠＝§1-2 ／ 割付＝**`CHANGE-139` の反映と同じ手番**。

### 4-4 ★本番配線がアダプタを挟んでいることを固定する観測が無い

- **スラッグ**: `val-dup-adapter-wiring-unobserved`（**新規登録**。**★`M24-11` の `combo` 側も同じ構造であり、`VAL-S04` 固有ではない**）
- **何が起きるか**: `txScopedValidDeps` / `txScopedDeps` は、配線が `*…Adapter` でなければ **`slog.Warn` を出して黙って `*sql.DB` 直読みへ落ちる。** **⇒ `cmd/combomgr/main.go` の配線を間違えてもテストは全部緑になる**（テスト側は自前でアダプタを挟むため）。
- **根拠**: `internal/service/setup/service.go:205-217` ／ `internal/service/combo/service.go:298-315`（`M24-11`）。
- **割付の候補**: 依存組み立ての切り出しは 1 サブの射程を超える。**両側まとめて扱うサブ**が要る。

### 4-5 ★CSV 取込のプレビューと commit の severity が非対称

- **スラッグ**: `csv-preview-commit-severity-asymmetry`（**新規登録**）／ 根拠＝§1-4 ／ **「プレビューは通るが commit では落ちる」形自体は残る。揃えるべきかは設計判断。**

### 4-6 ★`VAL-S04` の適用面の数え方（**既存行の更新**）

- **対象スラッグ**: followup の `VAL-S04` の check-then-act 行（**★本サブで解消したため、更新後の状態＝「解消済み」**）。
- **更新内容**: 「3 経路」→ **「判定関数の呼出元 3 ／ 入口 4」**（§1-2）。

### 4-7 ★★エディタのタブ見出しのバッジが、同梱セットプレイのエラーを「基本情報」へ誤計上する

- **スラッグ**: `editor-tab-badge-misroutes-setups-errors`（**新規登録**）
- **何が起きるか**: **同梱セットプレイのエラー（`field = "setups[1]"` 等）が「基本情報」タブのバッジに計上される。しかし直す欄は「レシピ」タブのセットプレイ登録の節に在る。** ⇒ **利用者を、直す欄の無いタブへ誘導する。**
- **根拠（実測）**:
  - `web/src/features/combo/editorTabs.ts:47-56` — `tabOfIssueField` は `steps` / `starterMoveId`（完全一致）と `steps[` / `steps.`（前方一致）**以外を全て `return "basic"` へ落とす**。`setups[1]` はどれにも当たらない。
  - `web/src/features/combo/components/ComboEditor.tsx:1031-1041` — `SetupRegistrationSection` は**レシピパネル（`:1013`）の内側**に在る。
  - `internal/service/combo/service.go:393 / :395` — `setups[%d].%s` / `setups[%d]` を組む。**★製造は触っていない**（`git diff 11bcb6a -- internal/service/combo/service.go` は 0 バイト）。
  - **`web/src/features/combo/editorTabs.test.ts` に `setups` の文字列は 0 件**＝振り分けを見るテストが無い。
- **★★開発者の実機確認で裏取り済み**（2026-08-29）——**「基本情報タブにエラーのバッジが出る動きでした」**（逐語）。**★本項はコードの読みだけでなく、実画面で再現している。**
- **★★重大ではない**——エラーの文言そのものは `ValidationDisplay`（`ComboEditor.tsx:1045`）が**タブの外**に出すため隠れない。**壊れているのは「どのタブで直すか」の指し示しだけである。** **★ただし `M24-04`（SM-148）は、まさにその指し示しのために作られた機能である。**
- **★★M24-13 が作った欠陥ではない。** `VAL-S01` / `VAL-S02` / `VAL-S06` は以前から `setups[*]` の ERROR を出しうる。**ただし M24-13 が到達経路を 1 本増やした**——同一リクエスト内の同一レシピ 2 本（§1-3）。**⇒ 当たりやすくなった。**
- **★★裁定してほしいこと＝「`setups[*]` はレシピタブでよいか」。** **編集モードではセットプレイ登録の欄が出ない**（`mode === "new" || "copy"` のみ＝`ComboEditor.tsx:1031`）**ため、前方一致へ機械的に足すだけでは正しいとは限らない。⇒ 製造は直していない。**
- **割付の候補と理由**: **`M24-04`（SM-148）由来の別件**。**⇒ `M24-CLOSE` か `M25`。**
- **直す場合の見積り**: `RECIPE_TAB_FIELD_PREFIXES` へ 1 行 ＋ 振り分けテスト（現在 0 件） ＋ E2E 1 本。

### 4-8 ★`opponent_size` の許容値が CSV 取込だけ本体に追随していない（**開発者裁定でスコープ外**）

- **スラッグ**: `csv-import-opponent-size-whitelist-stale`（**新規登録**）
- **何が起きるか**: **CSV 取込の許容値が `small` / `medium` / `large` のままである**（`internal/service/comboio/csvcore/rules.go:53-56`）。**アプリ本体は `small` を廃し、`large` を 2 種へ分けた**（`large1` / `large2`）。**⇒ `large2` を持つコンボを書き出して取り込み直すと `VAL-ENUM`「未知の値」が出る＝往復が壊れている。**
- **★取込自体は止まらない**——`enumField`（`csvcore/validate.go`）は不一致でも**値をそのまま返し WARNING を付ける**だけである。実測（2026-08-29）:
  ```
  "warnings": ["[VAL-ENUM] opponent_size: unknown opponent_size value \"large2\"", ...]
  "importable": true
  ```
- **開発者の逐語（2026-08-29）**: 「**small 区分は存在しなかったので消し、large が二種類に分かれる事をこれで表現していましたが、CSV 側が対応できていなかったようです。これはあなたのミスでもなければスコープ外なので、対応不要ですが、申し送りにはしておいてください。**」
- **★★M24-13 とは無関係。** 本サブは `opponent_size` に 1 行も触れていない。**★開発者の実機確認（D-1）の途中で見つかった。**
- **割付の候補**: `DES-006` §6（CSV の検証方針）と `DES-003` の列説明の突合。**⇒ `M24-CLOSE` か `M25`。**

### 4-9 ★開発者の実機で「取込に失敗しました: Failed to fetch」が 1 度出たが、**双方で再現しなくなった**（記録）

- **スラッグ**: `csv-import-failed-to-fetch-unreproduced`（**新規登録**）
- **何が起きたか**: 開発者が D-1（CSV 取込）で 1 度だけ **「取込に失敗しました: Failed to fetch」** を観測した。**★これは検証エラーの表示ではなく、ブラウザの fetch そのものの失敗である。**
- **★★製造が同じ CSV で再現を試み、再現しなかった**（2026-08-29。使い捨て DB ＋ `go run ./cmd/combomgr` ＋ `curl`）。**★4 通りすべてで正常応答（HTTP 200）**:

  | 条件 | 結果 |
  |---|---|
  | 開発者の CSV そのまま（`large2` ＋ `recipe` が引用空欄） | `failed` / `検証エラー: [VAL-C09] レシピが空です…` |
  | `is_draft=false`（**M24-13 の前から在る枝**） | 同上 |
  | BOM ＋ CRLF（表計算ソフトで編集した形） | 同上 |
  | `recipe` セルに引用符が実データとして入った形 | `VAL-I05` のエラー行・取込対象外（**★このとき `VAL-C09` は出ない**） |

- **★サーバの stderr は全期間 0 バイト＝panic は一度も起きていない。**
- **★★当初の仮説は外れた**——「`middleware.Recover()` が無い（`cmd/combomgr/main.go:297-312`）ため panic が応答なしで接続を閉じている」と読んだが、**panic は起きていなかった。** **★仮説は仮説として記録するに留める。**
- **★★M24-13 の経路は正常である**ことが、開発者の追試と製造の再現の両方で確認できた——**仮登録＋レシピ空の行は「失敗行」として正しく報告される**（これは本サブが狙った振る舞いそのものである）。
- **★★開発者の環境でも再現しなくなった**（2026-08-29。逐語＝**「私の方でも再現しなくなりました。端末の不調だった可能性があります。」**）。**⇒ 製造側・開発者側の双方で再現しない。**
- **★★⇒ 環境要因であった可能性が高い。** 症状が出たのはブラウザ経由（Vite の dev プロキシ）の 1 度きりであり、**同じ入力をサーバへ直接流した 4 通りすべてが正常であった。★開発者は同時期に `opponent_size` の変更を入れており、バックエンドが preview と commit の間で再起動・停止していた可能性も残る。**
- **⇒ 対応は不要。★ただし再発したら次を採ること**: (1) **バックエンドを起動している端末の出力**（panic のスタックトレースはそこにしか出ない） (2) ブラウザの開発者ツール Network タブで**要求が送られたか / ステータス**。
- **★★「直った」とは書かない。** **原因が特定できていない以上、消えたことは直ったことではない。** 本項は**閉じるためではなく、再発したときに「一度観測されている」と分かるために残す。**

### 4-10 ★記録のみ（対応不要）

- `gofmt -l ./internal ./cmd` に **`internal/infra/migration/migrate_m2402_test.go` 1 件**が残る。**★着手前から未整形であり本サブ由来ではない**（`git show 11bcb6a:<path> | gofmt -d` で確認）。**リポジトリに gofmt の機械検査が無い**ことの記録。
- `body.warnings`（`VAL-C14`）を見る E2E assert が 0 本になった。**★本サブの命題と無関係。**

---

## §5 参考（触れていない＝不変の証跡）

- `internal/service/notation/` ／ `internal/service/preset/`（契約 F-1）: `git diff --numstat 11bcb6a` に 1 件も現れない。
- `migrations/`: 差分 0。最新は `000079`。**次に払い出す番号は `000080` のまま。**
- `go.mod` / `package.json`: 差分 0（新規依存 0）。
- `web/e2e/m24-12-editor-rebuild.spec.ts`: **ラベルの 1 行以外は無改変**（§2-1 の追随）。7 ケース全数緑。
- `VAL-D01` / `VAL-D02` / `VAL-D03`: 判定コードに差分 0。`TestC08_NullMoveIDSkipped` 不変。
- `DES-005` §5.7 の保存後の遷移先の表（4 モード）: 遷移コードに差分 0。
- `REQ-001` / `docs/design/` 配下: **製造は 1 バイトも編集していない**（`git diff --name-only 11bcb6a -- docs/design/` が 0 件）。
- ブラウザストレージ: 新キー 0（`check-browser-storage-keys.sh` 違反なし）。
- `VAL-S07` / `VAL-S02`: 差分 0。

---

## §6 CHANGE 起票のたたき台（設計担当向けチェックリスト）

**★`CHANGE-139` は起票済み。以下は「反映時に射程へ含めるか」の候補である。番号は起票時に registry で採番。**

| # | 候補 | 対象 |
|---|---|---|
| 1 | **`DES-006` §2.2 の緩和ルールへ `VAL-C09` の例外を 1 件立てる** | `CHANGE-139` §2.1（**予定どおり**） |
| 2 | **`REQ-001` FR009 へ明確化の 1 文** | `CHANGE-139` §2.4（**予定どおり**） |
| 3 | **`DES-005` §5.7 へ §1-5 の表** ＋ **§2-1 のラベル** | **★ラベルは裁定が要る** |
| 4 | **★`SUPP-001` §2.1 の 2 行 ＋ `REQ-001` FR305** | **§4-1。★射程の追加が要る** |
| 5 | **★`DES-006` §2.3 へ `VAL-S04` のぶん** ＋ **§3 へ「同梱は同一リクエスト内でも対象」** | §1-2 / §1-3。**★`D-588` で足した §4.6 のぶんであり、`CHANGE-139` v1.1.0 の §8 に対応する** |
| 6 | `DES-006` §5 / §6 へ CSV プレビューの適用 | §1-4 |
| 7 | `DES-002` §4.2 の as-built | §1-1 / §1-3。**★経路表の行そのものは不変** |

**マイグレーション: 0 本消費。★予約帯は作っていない。次は `000080`。**

---

## §7 教訓（**★親は本節を読まなくてよい**。宛先＝`retrospective-log` へのバッチ反映。実施者は設計担当）

1. **★★「影響設計書」の欄は起票時の見込みであって全数ではない。** 本サブは `SUPP-001` §2.1 の 2 行と `REQ-001` FR305 を取りこぼした（§4-1）。**⇒ 失効箇所を挙げる節は、走査語を複数の言い換えで用意して設計書を直接 `grep` すること。★1 度目の走査語（「全て適用しない」「仮登録＋レシピ／空」）では FR305 に当たらなかった。**
2. **★★失効記述の走査は「設計書」「コメント」「利用者に見える文字列」の 3 面を別々に行うこと。** 本サブは 3 面目を落とし、**トグルのラベルが画面上で保存ボタンと正反対のことを言う状態**を作った（§2-1）。**★テスト 2 本が旧文字列を固定しており、モックにも描かれていないため、開発者の目視確認でも見えなかった。**
3. **★★数値は「書く前に採る」。本サブで同型を 3 度やった。** (a) 回していない検査を完了報告へ「違反なし」と書いた (b) 取り込み後に `git diff --stat` を採り直さず、**deletions が減っていること**に気づかなかった (c) 再実行の表に実行前の見込み値を書きかけた（実測は ±0 で誤りだった）。**⇒ 報告書の数値は後任にとって「回した証拠」として機能する。**
4. **★★`BEGIN IMMEDIATE` があると「直っていなくても緑」になる。** 破壊確認 5b（**束ねだけを外す**）を足したところ、**競合テストは緑のまま**で同一 tx のテスト 1 本だけが赤くなった。**⇒ `D-360` を守っていることを見ている観測は 1 本しか無い。★指示書が求めた破壊確認だけでは、この盲点は見えなかった。**
5. **★「唯一の観測である」と書くときは、消費側が本当にそれを見ているかを確かめること。** BE の `field` 書式を固定するテストを足したが、押さえたのは `VAL-C08`（**WARNING**）であり、**フロントは `severity !== "error"` を捨てる**。⇒ 実際の唯一の生産者は `VAL-C12`（ERROR）だった。**★同じ書式文字列でも呼び出し箇所が別なら片方だけ変えられる。**
6. **★`pnpm exec tsc --noEmit -p tsconfig.json` はテストファイルを型検査しない。** 本サブで実際に取りこぼし、`tsc` が緑のまま `pnpm build` が落ちた。**`make e2e` は `pnpm build` を通るため webServer が起動せず、「赤の意味が確かめられない」状態になった**（`M24-12` 教訓 17 の隣接形）。**⇒ フロントの型検査は `pnpm build` で見る。**
7. **★母数は「見る単位」で割れることがある。** `VAL-S04` の適用面は **判定関数の呼出元 3 ／ 入口 4**。**⇒ `M-89` の型は、数を訂正するのではなく単位を明示することで避けられる。**
8. **★E2E の spec ヘッダのコメントは、本文を直しても取り残される。** 「仮登録モードで保存するためステップ入力は不要」が 4 か所残り、**直後の行が `addMinimalRecipeStep` を呼んでいる**状態になった。**⇒ 本文を直したら同じファイルのヘッダを `grep` で当たること。**
9. **★★「どの欄がどのパネルに在るか」と「エラーをどのタブへ振り分けるか」は別々のコードに書かれており、別々に育つ。** 振り分け規則が `steps` 系しか知らないまま、セットプレイ登録の節がレシピパネルへ入った（§4-7）。**⇒ パネルへ節を足したら、その節が出しうる `field` が振り分け規則に載っているかを見ること。★既定が「基本情報」へ落ちる形なので、載せ忘れても静かに間違ったタブを指す**——**テストも lint も緑のままである。**
10. **★★実機で出た症状は、同じ入力で再現できるまで原因を断定しない。** 本サブは「`Recover` ミドルウェアが無いので panic が応答なしで接続を閉じている」という**筋の通った仮説をコードから立てたが、外れた**（§4-9。panic は一度も起きていなかった）。**★仮説の質が高いほど、確かめずに書きたくなる。** **⇒ 症状の報告を受けたら、まず同じ入力を流す。1 変数ずつ戻すと、どこまでが事実でどこからが推測かが分かれる。**

---

*以上、M24-13 設計伝達レポート。* **★★裁定が要るのは §2 の 2 件（トグルのラベル ／ E2E の観測手段）と、§4-1・§4-2・§4-7 である。**
