# 設計伝達レポート: M30-01（仮想コントローラの技の出し分け）

| 項目 | 内容 |
|------|------|
| 作業ID | M30-01 |
| 対象指示書 | `docs/instructions/M30-01-controller-move-surfacing.md` **v1.1.0** ／ チェックリスト `docs/instructions/reviews/M30-01-review-checklist.md` **v1.0.0** |
| CHANGE | **消費 0 本。★製造は番号を消費していない**（`D-293`）**。⇒ 起票のたたき台は §6** |
| マイグレ | **消費 0 本。★実査値＝`migrations/` の最大は `000106`。⇒ 次に払い出すのは `000107`**（指示書ヘッダの「次は `000104` 以降」は起票時点の値であり、既に `000104`〜`000106` が消費されている。ボード §2.2 と食い違うなら設計卓が直す） |
| 作成日 | 2026-09-08 |
| 実装コミット | `claude/m30-01-implementation-plan-8lqchp`・`50235ce..612d648`（**18 コミット・push 済み**。うち 1 本は本レポート自身）。差分＝**26 ファイル・+2875 / −136**（本レポートを除く。うち `web/` は 23 ファイル・+2076 / −136） |
| 源泉 | 完了報告 `docs/progress/M30-01-completion-report.md` **v1.3.0** ／ レビュー `docs/progress/m30-01-review.md`（**重大 0 件・高 4 / 中 5 / 低 7 ＝ 全 16 件。高の不採用 0 件・再レビュー往復 0 回**）／ **開発者との確定 12 件**（2026-09-08）／ **開発者の実機確認 6 項目 ＋ 物理コントローラ**（2026-09-08）／ 実装直後の同一セッションで生成 |
| 宛先 | 設計卓（親チャット） |

**本レポートは ①独自確定仕様 ②契約違反の独自判断 ③製造判断 ④残課題 に絞る。指示書どおりの部分は割愛する。**

**★★最重要は §2-1（`DES-005` §5.7 の入力の骨格を動かした — 指示書 §4「やらないこと」に該当するが開発者が明示指示した。受理/却下の裁定が要る）と §2-2（製造が実測の母集団を取り違え、それを回帰テストで固定していた — 指示書 §2.1 の明文と異なる母集団で「実測」を名乗った）である。**

> **★設計卓は `docs/progress/` を読まない**（2026-08-11 開発者裁定①）**。⇒ 本レポートは参照だけを書かず、判断に要る中身をここへ埋めてある。**

> **★`DES-002` §4.2 の経路表に載る API の新設・変更は 0 件である。** 本サブは `internal/` `cmd/` `migrations/` を **1 行も触っていない**（差分は `web/` と `docs/progress/` のみ）。⇒ **本レポートに API 契約の逐語は無い。**

---

## 0. 本サブで実際に起きたこと（3 行で）

1. **指示書が求めた「最初の成果物＝実測」を出したが、母集団を取り違えた**（`character_data/*.csv` を数え、`moves` テーブルを数えなかった）。**⇒ 未分類 330 行と報告したが実際は 551 行で、差の 217 行は移動系 system 技だった。開発者の実機確認まで誰も検出していない**（§2-2）。
2. 出し分けの規則を **`web/src/features/combo/moveSurfacing.ts` の 1 本へ集約**し、タブを **4 → 8 枚**にした。**うち共通技タブはタブ外の常設行からの移設であり、`DES-005` §5.7 の骨格に触れる**（§2-1）。
3. **`P4M-017` の逐語「必殺技が一部しか出ていない」の実体を特定した**——**`move_code` の強度語が末尾にあるか途中にあるかで、同じ「状態版の必殺技」の扱いが割れている**（§4-1）。

---

## 1. 独自確定仕様（設計書に無い／設計書より細かい決めごと）

### ★★§1-1 仮想コントローラのタブは 8 枚になった（as-built・`DES-005` §5.7 へ反映が要る）

**並びと母集団**（`web/src/features/combo/components/VirtualController/VirtualController.tsx:250-400`）:

| # | タブ | `data-testid` | 母集団（述語の逐語） |
|---|---|---|---|
| 1 | 通常技 | `recipe-tab-normal` | 段階2 解決表 → 段階1 構造引き（不変） |
| 2 | 特殊技 | `recipe-tab-unique` | `category === "unique"`（不変） |
| 3 | **ターゲットコンボ** | `recipe-tab-target-combo` | **`category === "target_combo"` の全行。★`is_derived` で絞らない** |
| 4 | 必殺技 | `recipe-tab-special` | `category==="special"` ∧ `parseSpecialCode(code) !== null`（不変） |
| 5 | SA | `recipe-tab-super-art` | `category ∈ {super_art, critical_art}`（不変） |
| 6 | **未分類** | `recipe-tab-unclassified` | **`isControllerSurfaced()` が false を返す行そのもの** |
| 7 | **キャラ固有状態** | `recipe-tab-character-state` | **`custom_states[].code`（＋語幹）を `move_code` が含む行** |
| 8 | **共通技** | `recipe-tab-common` | **固定 13 code（§1-2）。★一番右**（開発者指示） |

**⇒ `DES-005` §5.7 へ、上表と「タブ外の常設行は削除ボタンだけになった」ことを明文化してほしい。**

**★ターゲットコンボタブが `is_derived` で絞らない理由**: `M19-DESIGN-08` §0 の述語 `NOT (category='target_combo' AND is_derived = 1)` は **filler 候補から除く側**のものであり、入力面は「出す」側である。裏返すと 126 行中 117 行が入力できず、**`SM-135`「ターゲットコンボも入力に欲しい」と真逆になる**。

### ★★§1-2 共通技タブの母集団は固定 13 code である（as-built）

`web/src/features/combo/moveSurfacing.ts` の `COMMON_MOVE_CODES`（並び順そのもの）:

```
forward / back / micro_forward / micro_back /
jump_neutral / jump_forward / jump_back /
drive_impact / drive_parry / throw_forward / throw_back /
dash_forward / dash_back
```

- **13 code すべてが実 DB に 31 キャラぶん在り、`official_ja_move` の表示名も付いている**（前入力 ／ 微歩き(前) ／ 垂直ジャンプ …）。**⇒ ラベルは `nameJa` をそのまま使う。**
- **★生ラッシュ（`parry_drive_rush`）はこの 13 に入れない。** move ではなく `modifiers.type` だからである（`DES-004` §2.1）。**タブ内に別ボタンとして置いた**（`testid` は移設前と同じ `recipe-system-parry-drive-rush`）。
- **★移設前の常設行が引いていたのは 6 code だけで、移動系 7 code（`forward` / `back` / `micro_*` / `jump_*`）はどの面にも出ていなかった**——実 DB で **217 行**（7 × 31）。**⇒ 本サブで初めて入力可能になった。**

**⇒ `DES-005` §5.7 へ「共通技タブの母集団は上記 13 code」「生ラッシュは move ではないため別ボタン」を明文化してほしい。**

### ★§1-3 「未分類」の定義は 1 本の述語の裏返しである（as-built）

`isControllerSurfaced(move, moves, entries, normalIds, stateCodes)` が **通常技 ∨ 特殊技 ∨ 必殺技 ∨ SA ∨ ターゲットコンボ ∨ 共通技 ∨ キャラ固有状態** の論理和で、**未分類タブはその否定**。**同じ述語の裏返しを `SM-100` のトグルも使う**（規則を 2 か所へ書かない＝指示書 §2.5-2）。

**★キャラ固有状態タブを「掲載済み」に数える**（2026-09-08 開発者判断・案 C）。**⇒ 未分類との重なり 45 件 / 5 キャラが消えた。**

### ★§1-4 キャラ固有状態タブの判定軸は「状態 code ＋ その語幹」である（as-built・案 A′）

**後置語 `_stock` / `_mode` / `_is_set` / `_crest` / `_charge` を落とした語幹でも `move_code` を照合する**（`moveSurfacing.ts` の `expandStateCodes`）。

**★新しい列も新しい API も足していない**（指示書 §2.2-3 / §4-5）。`Character.customStates` は既に配られている生 JSON である。

**実測の効き目**（定義を持つ 14 キャラ）: mai **0 → 26** ／ ingrid **0 → 31** ／ juri 2 → 11 ／ kimberly 0 → 6 ／ yasmine 1 → 5。

### ★§1-5 `SM-100` は切替トグル・既定 OFF・永続化なし（as-built）

- 文言は **「タブに分類済みの技を省く」**（開発者判断・案 A。`controller.panel.omitSurfacedLabel`）。
- **★保持は component-local の `useState` のみ。ブラウザストレージへ置いていない**（`web/CLAUDE.md` §1 の台帳へ新キーを足していない。`check-browser-storage-keys.sh` 緑）。
- **★ON にしても入力手段が消える技は 1 件も無い**（残る集合＝未分類タブの母集団と完全に一致）。チェックリスト §6-3 が禁じた形に当たらない。
- 対象は 2 面（コンボのレシピ入力・セットプレイのレシピ入力）。同じ部品・同じフックを使う。

---

## 2. 契約・設計に反する独自判断（★受理／却下の裁定が要る）

### ★★§2-1 `DES-005` §5.7 の入力の骨格を動かした（共通技行 → タブへの移設）

**何に反したか**: 指示書 §0.2 と §4「やらないこと」-1 が **「`M24-12` の入力の骨格に触れること」**（ボタン群の押し方・数字キーの割当・順送りの停止点）を禁じ、「**本サブが触るのは『タブに何を並べるか』であり『どう押すか』ではない**」と明記していた。**常設行をタブへ移す変更は「どう押すか」に当たる。**

**なぜそう判断したか**: **開発者が実機確認を経て明示的に指示した**（2026-09-08 の逐語＝「共通技のタブが欲しくなってきた。…なお下部の共通技は移動に伴い削除してください」）。製造は**指示書との衝突を指摘したうえで**実施している。

**実装がどうなっているか**:
- `web/src/features/combo/components/VirtualController/CommonMovePanel.tsx`（新規 63 行）＝ 共通技タブ本体
- `web/src/features/combo/components/VirtualController/SystemRow.tsx` を **削除**し `DeleteRow.tsx`（新規 30 行）へ改めた。**残るのは削除ボタンだけである**（どのタブに居ても直前のステップを消せる必要があるため、削除はタブへ入れていない）
- **★物理入力の点灯**（`M21-03` §4.4）**は `DirectSpecPanel` へ `heldCodes` を通して保った**。渡さなければ静かに消える面であり、`web/src/features/gamepad/gamepadRecipeInput.test.tsx:422` が回帰を押さえている（**開発者の実機確認で点灯を確認済み**）
- **★数字キーの割当は 1 つも消費していない**。仮想コントローラのタブは Radix `Tabs` であり数字キーを購読しない。`OptionButtonGroup` の数字キーは**その群にフォーカスがある間だけ**効き、両者は同じ画面に同居しない（前者はレシピタブ、後者は基本情報タブ）。回帰テストで固定した

**⇒ 裁定してほしいこと**: **(a) 受理して `DES-005` §5.7 を as-built へ更新する**か、**(b) 却下して常設行へ戻す**か。**★製造は (a) を前提に実装済みである。**

### ★★§2-2 指示書 §2.1 の明文と異なる母集団で「実測」を行い、それを回帰テストで固定した

**何に反したか**: 指示書 §2.1-3 は **「`moves` に在って仮想コントローラに出ない行を全数で挙げること」**と書いていた。**`moves` はテーブルである。** 製造は **`character_data/*.csv` を「seed の正本」と見なして数えた。**

**何が起きたか**:

| | 製造が数えたもの | 実際 |
|---|---|---|
| 母集団 | CSV **2743 行** | `moves` **3026 行** |
| 未分類（着手時点） | **330 行**と報告 | **551 行** |

**差の 283 行はマイグレーションが直接投入する行**で、大半は**移動系 system 技 217 行**（`000025_seed_movement_system_moves_all` 等）。**⇒ この 217 行はどの面にも出ておらず、開発者が実機で「システムの move が未分類に居る」と気づくまで検出されなかった。**

**★あわせて誤報が 1 件あった**——製造は「`dash_forward` / `dash_back` は seed に無いので押しても何も起きない」と報告したが**誤りである**。実 DB に 31 キャラぶん在り、開発者の実機確認どおり押せる。

**★★最も重いのは、誤った母集団を回帰テストで「実測の固定」として緑にしていたことである**（`moveSurfacing.roster.test.ts` 初版）。**テストが在ることは、確からしさの証明にならなかった。**

**是正**（実施済み）:
- 同テストの冒頭へ**母集団の限界を明記**し、「**共通技 13 code のうち CSV に在るのは 4 code だけ**」という主張を新設した（同型の取り違えを次に押さえる）
- **実 DB での件数は E2E が実サーバ越しに主張する**（`web/e2e/m30-01-controller-surfacing.spec.ts:130`。jamie 11 / mai 3 / blanka 12 / manon 0）
- 完了報告の先頭へ **§2.0 訂正節**を置いた

**⇒ 裁定してほしいこと**: **本件を `SUPP-001` §5.5 系の規約へ 1 行として昇格させるか**（例＝「seed の実測は `moves` テーブルに対して行う。`character_data/*.csv` は入力の一部でしかない」）。**⇒ 製造は規約を書き換える権限を持たない。**

### ★§2-3 `CHANGE-166` の帰結文 1 行が失効した

`CHANGE-166` は「強度接尾辞を持たない `move_code` は…**⇒『全技一覧から選ぶ』プルダウンからの入力になる**」と書いている。**未分類タブができたため、入力経路はプルダウンだけではなくなった。**

**★ファミリー UI に載らないという規則そのものは不変である**（本サブは `parseSpecialCode` を 1 行も変えていない）。**⇒ 帰結の 1 文だけが失効した。**

---

## 3. 製造の判断

### 3-1 開発者へ確認して確定した点（2026-09-08）

| # | 事項 | 確定内容 |
|---|---|---|
| 1 | `P4M-017` の出し分け規則 | **ファミリー UI を拡張する。ただし方法は改めて検討し、設計卓へ請求する。⇒ 本サブでは規則を 1 行も変えない** |
| 2 | `P4M-008` (b) の読み | **読み2＝キャラ固有状態の技タブ。(a) とは別の 1 枚（2 タブ）** |
| 3 | `SM-100` の既定 | **切替トグル・既定 OFF** |
| 4 | 「未掲載」の名 | **「未分類」へ改称**（逐語＝「現に掲載されているのだから未分類が適切」） |
| 5 | 未分類と固有状態の重なり | **案 C＝固有状態タブを「掲載済み」に数える** |
| 6 | 舞が拾えない件 | **案 A′＝状態 code の語幹でも照合。★救えないキャラが出たら案 D も検討する旨をレポートへ含めること**（本指示により §4-2 を書いた） |
| 7 | トグル文言 | **案 A「タブに分類済みの技を省く」** |
| 8 | 共通技タブ | **実在するものだけ移動。実在しないものは設計卓へ**（★実測の結果 14 件すべて実在した。⇒ **「実在しないもの」は 0 件**） |
| 9 | 共通技タブの位置 | **一番右**（製造は当初「通常技の次」に置いたが、実機確認を経て開発者が末尾を指示） |
| 10 | `ryu` の `axe_kick` 是正の向き | **誤っているのは `axe_kick_2` の側。`axe_kick` / `rush_axe_kick` が正しい。DB 修正は設計卓へ申し送り**（§4-3） |
| 11 | `aki` の固有状態タブが空 | **許容**（★理由は §4-2。`subject: opponent` であり救う対象が無い） |
| 12 | 実機確認 | **6 項目 ＋ 物理コントローラの点灯すべて合格** |

### 3-2 推測で進めた点（指示書 §9「進めて報告する」）

| # | 判断 | 理由 |
|---|---|---|
| 1 | **新タブ 4 枚の `data-testid` を新規採番した**（`recipe-tab-common` / `-target-combo` / `-unclassified` / `-character-state`、およびボタンの `recipe-<面>-<code>`） | **`M30-02` の射程は「既存の割当を変えること」と読んだ。** 新しい面にセレクタが無いとテストが書けない。**★既存 id は 1 つも変えていない** |
| 2 | **`hidden` → `unclassified` へ内部名・testid も揃えた** | 本サブで新設したばかりの id であり、`M30-02` が持つ既存割当ではない。**ラベルだけ「未分類」で内部が `hidden` だと失効した命名が残る** |
| 3 | **削除ボタンをタブへ入れず常設のまま残した** | 開発者の逐語は「**下部の共通技**は移動に伴い削除」であり、削除は共通技ではない。**どのタブに居ても直前のステップを消せる必要がある** |
| 4 | **`nonmove:` の既存リテラルは畳まなかった** | 各エディタ側に 4 か所ずつ散在しているが、**本サブの射程は出し分けであり、無関係な差分を増やさない** |
| 5 | **`check-import-order.sh` の `BASELINE` を下げなかった** | 現在 100 / ベースライン 101 で緑だが、**スクリプト自身が「レーンごとに測って下げてはならない」と明記している**（並走レーンの追加が見えなくなる） |

---

## 4. 設計担当が未把握の残課題・申し送り

> **`followup-backlog.md` の本表は製造が編集しない**（`D-382`）。**⇒ 設計卓が迷わず追加できる形で書く。**

### ★★§4-1 必殺技ファミリー UI の拡張（**請求 2 本**。`P4M-017` の本体）

**スラッグ**: `special-family-ui-misses-state-variants`（新規登録）

**何が起きるか**: 必殺技クイック入力パネルに載らない `category='special'` が **実 DB に 291 件**ある。**これは 2 つの別問題に割れる。**

| 枝 | 件数 | 形 | 非派生 |
|---|---:|---|---:|
| **A1** | **186** | `move_code` のどこにも強度語が無い（`tenshin` / `sonic_break` / `double_lariat`） | 70 |
| **A2** | **105** | **強度語は在るが `code` の途中に在る**（`lightning_beast_light_rolling_attack` / `flame_od_kachousen`） | 2 |

**★A2 はすべて「キャラ固有状態の強化版必殺技」である**（接頭辞 13 キャラ・12 種＝`flame` 21 / `lightning_beast` 18 / cammy 系 12 / `windclad` 10 / `perfect_timing` 9 / `mine_set` 9 / `buffed` 8 / `high_jump` / `sumo_spirit` / `bayani` 等）。**⇒ 開発者の逐語**（`P4M-008` (a)「強化版必殺技等」）**が指していた対象そのものである。**

**★★同じ「状態版の必殺技」なのに、`move_code` の書き方の違いだけで扱いが割れている**（開発者の実機確認で顕在化。逐語＝「Jamie の必殺技に、酔いレベル4の流酔拳だけ固有状態技なのに出ていました」）:

| 形 | 実例 | 現行 |
|---|---|---|
| 技名 ＋ **末尾**に強度 | `drink_level_4_freeflow_strikes_light`（jamie・12 行） | **載る** |
| **途中**に強度 ＋ 技名 | `lightning_beast_light_rolling_attack`（blanka・18 行） | **載らない** |

**取りうる形は 2 つ**:

| 案 | 内容 | 実測 |
|---|---|---|
| **(i) ファミリーの導き方を変える** | `parseSpecialCode` を「**末尾の強度接尾辞**」から「**`code` のどこかにある強度語**」へ広げる。例＝`lightning_beast_light_rolling_attack` → ファミリー `lightning_beast_rolling_attack` ／ 強度 `light` | **★実測: 新たに載る 105 件 ／ ファミリー衝突 0 件 ／ 誤検出 0 件 ／ 既存の判定が変わる技 0 件。副次で未分類が 251 → 146 行** |
| (ii) データ側の命名を揃える | 105 行の `move_code` をどちらかの形へ寄せる | マイグレ ＋ 別名 ＋ 既存レシピの追随。**`move_code` は `preset_aliases` の解決キー**（`DES-004` §2.1 の警告） |

**★製造の見立ては (i)** だが、**`DES-004` §2.1 / `DES-005` §5.7 / `CHANGE-166` の規則そのものの変更であり、`D-293` により設計卓の判断である。⇒ 本サブでは実装していない。**

**★A1（186 件）は別の判断になる**——強度の概念が無い技を 2 段 UI にどう置くか、という**新しい見せ方の設計**が要る。**⇒ (i) では救われない。**

**★(i) を入れても根っこは残る**: 「なぜ命名が 2 通りあるのか」はデータ側の問題であり、将来の整理課題として残る。

**割付の候補**: **`M30-02` ではない**（同サブの射程は testid・数字キー・強度と変種の選び方）。**新サブ、または `M30` の追加サブが要る。**

### ★§4-2 キャラ固有状態タブの軸（案 D への移行判断）

**スラッグ**: `character-state-axis-needs-explicit-prefix`（新規登録）

**何が起きるか**: 案 A′（状態 code ＋ 語幹）でも救えないキャラが **2 体**残る。

| キャラ | 状態 code | 技の形 | 結果 |
|---|---|---|---|
| **`aki`** | `poisoned` | — | **★救う対象が無い。`subject: "opponent"`（相手に付与する状態）であり、`aki` 自身の技コードに `poison` を含む行は 0 件**（実測）。**定義を持つ 14 キャラのうち `subject: opponent` はこの 1 件だけ**（`DES-003` §3.2 が名指しで例示）**。⇒ 案 D でも救われない。仕様どおりである** |
| **`m_bison`** | `psycho_mine_is_set` | `mine_set_*`（9 行） | **語順が違うため当たらない。★空ではなく 1 件は出る**（`psycho_mine_auto_detonation` が語幹 `psycho_mine` に当たる） |

**案 D**: `custom_states` の各 state に **「技コードの接頭辞」を明示的に持たせる**。**⇒ 偽陽性ゼロで意味とも一致するが、`DES-003` §3.2 の構造変更である。**

**★開発者の指示**（2026-09-08）＝「この形が増えたら案 D 等も検討する、という旨をレポートに含めること」。**⇒ 現時点では移行を推していない**（対象が実質 `m_bison` 1 体のため）。

### ★§4-3 データ是正 1 件（`ryu` の `axe_kick_2`）

**スラッグ**: `ryu-axe-kick-code-mismatch`（新規登録）

**何が起きるか**: `axe_kick_2`（unique）と `rush_axe_kick`（rush_variant）が対応せず、`resolveRushByCode` が `rush_axe_kick_2` を探して外す。**⇒ `rush_axe_kick` はどの面からも入力できない。**

**★是正の向きは開発者が確定した**（2026-09-08 の逐語＝「`axe_kick_2` 側の方が誤り。`axe_kick`、`rush_axe_kick` が正しい」）。**⇒ `axe_kick_2` → `axe_kick` のリネームであり、`rush_axe_kick` は触らない。**

**根拠**: `character_data/ryu.csv:27`（`axe_kick_2,unique,かかと落とし`）／ 同 `:84`（`rush_axe_kick,rush_variant,original_move_code=axe_kick_2`）。

**★`move_code` は `preset_aliases` の解決キーである。⇒ リネームには別名・既存レシピの追随が要る**（`M28-05` の三点更新が先例）。**開発者の指示により DB 修正は設計卓への申し送りとする。**

### §4-4 `docs/design/testid-convention.md` への登録（**製造は `docs/design/` を編集しない**）

**新規 `data-testid` 8 種**: `recipe-tab-common` ／ `recipe-tab-target-combo` ／ `recipe-tab-unclassified` ／ `recipe-tab-character-state` ／ `recipe-common-{code}` ／ `recipe-target-combo-{code}` ／ `recipe-unclassified-{code}` ／ `recipe-character-state-{code}` ／ `recipe-omit-surfaced-toggle`。

**★`/add_e2e_spec` は同ファイルの更新を求めるが、`CLAUDE.md` §8 と指示書 §4-9 が製造による `docs/design/` の編集を禁じているため回した**（§8 が優先＝`CLAUDE.md` §9 の注記）。

**★あわせて棚卸しの価値がある**（レビューの実査）: **同一覧には既存の `recipe-tab-*` / `recipe-direct-*` / `recipe-dir-*` も載っていない**（`grep "recipe-"` は 3 行のみ）。

### §4-5 `m24-12` の E2E の競合 —— **開発者の採用判断により本サブで修正した（対応済み・登録不要）**

**スラッグ**: `m24-12-back-navigation-test-races-on-home-redirect`（**★新規登録は不要**。着手前に解消したため。**経緯だけを本節に残す**）

**何が起きていたか**: `web/e2e/m24-12-editor-rebuild.spec.ts` の (3) が起点にホーム（`/`）を置き、戻る 2 回でホームへ着くことを主張していた。**しかし `web/src/App.tsx:32-34` が PC 幅（`isMobile` は `max-width: 639px`）で `/` を `/combos` へ `replace` する。⇒ E2E は Desktop Chrome（1280px）で走るため、起点のエントリはホームでなくなり、`toHaveURL(/\/$/)` が競合になっていた。**

**根拠**: `sessionStorage` へ `pushState` / `replaceState` を記録して採取した実測（`[auat] replace /combos @ /`）。**★離脱ガード自体は正しく動いている**（保存時に番人エントリを `replace` している）。**壊れていたのは目印だけである。**

**★本サブ由来ではない**: 着手基点 `50235ce` へファイルを戻して同じ 1 本を回し、同一エラーで再現することを実測した。

**適用した修正**（`web/e2e/m24-12-editor-rebuild.spec.ts:34-42, 92-96`）: 起点を `page.goto("/settings")` へ、期待を `toHaveURL(/\/settings$/)` へ。**`/settings` を選んだのはリダイレクトを持たないためであり、起点へ着いたことを `await expect(page).toHaveURL(...)` で先に確かめてから編集画面へ進む形にした。**

**実測**: `make e2e-only P=m24-12` **5/5 緑**。**★破壊確認も採った**——起点を `/` へ戻すと (3) だけが再び落ちる（`make e2e-only P="m24-12 -g 保存直後"` が 1 failed）。**⇒ 修正が効いていることの観測が在る。**

**★設計書への反映は不要**（テストの目印の付け替えであり、`App.tsx` のリダイレクト仕様は 1 行も変えていない）。**★ただし §7-6 の教訓は残す。**

### §4-6 `moveSurfacing.roster.test.ts` の母集団の限界（運用の申し送り）

**スラッグ**: `roster-test-corpus-is-csv-only`（新規登録）

同テストは `character_data/*.csv` しか読めず、**マイグレーション由来の 283 行と `characters.custom_states` を母集団に含められない**。**⇒ 件数は「CSV 由来・固有状態タブ無しの基準」である。** 実 DB・実設定での件数は E2E 側が主張する。**★seed が動く手番では 5 か所（2743 / 330 / 内訳 / 126 / 95 / キャラ別表）を数え直す。**

### §4-7 レビューで不採用にした 3 件（理由つき・記録のみ）

| 指摘 | 不採用の理由 |
|---|---|
| 中-2 トグル文言の見直し | **利用者に見える取捨であり `roles-and-routing` ハード列。⇒ 開発者へ回し、案 A で確定した**（3-1 #7） |
| 低-12 素の `<input type="checkbox">` を `Switch` へ | **`Switch`（Radix）は `role="switch"` の `<button>` であり、E2E の `check()` / `not.toBeChecked()` と Vitest の `.checked` が使えなくなる。** 既定 OFF を 3 層で主張しているため観測の素直さを優先した |
| 低-15 中間コミットが一時的に不整合 | **履歴の改変は製造の権限外**（`CLAUDE.md` §7・§10）。指摘自身が「逆順 revert では成り立つため実害は無い」と述べている |

---

## 5. 参考（触れていない＝不変の証跡）

- `internal/` `cmd/` `migrations/` `character_data/` の差分は **0 ファイル**（`git diff --name-only 50235ce HEAD -- internal cmd migrations character_data` が空）
- `docs/design/` の差分は **0 ファイル**（`CLAUDE.md` §8 / 指示書 §4-9）
- `parseSpecialCode` / `deriveSpecialFamilies`（`inputResolution.ts:106-155`）は **1 行も変えていない**
- 段階1 / 段階2 の解決（`inputResolutionStage2.ts`）は **1 行も変えていない**
- `OptionButtonGroup` / `fieldSequence` / `ComboEditorBasicFields`（`M24-12` の骨格）は **1 行も変えていない**
- 不変を守るテスト: `★M24-12 の入力の骨格に触れていないこと`（`VirtualController.test.tsx`）
- 不変を守るテスト: `★必殺技タブの述語が deriveSpecialFamilies と一致する`（`moveSurfacing.test.ts`）
- 不変を守るテスト: `マクロの投げは方向に合わせて前投げ/後ろ投げが点灯する`（`gamepadRecipeInput.test.tsx:422`。移設後も点灯が残ることを固定）
- `web/CLAUDE.md` §1 のブラウザストレージ台帳は **1 行も動いていない**（`check-browser-storage-keys.sh` 緑）
- `go test ./...` 緑 ／ `pnpm test` 219 files・2582 passed ／ `make e2e` **261 passed / 0 failed**（**§4-5 の修正後に取り直した値**）
- 常設検査 8 種すべて緑（`check-artifact-integrity.sh` を 1 本目に実行）

---

## 6. CHANGE 起票のたたき台（設計担当向けチェックリスト）

| # | 対象 | 内容 |
|---|---|---|
| 1 | **`DES-005` §5.7** | **タブ 8 枚の as-built**（§1-1 の表）／ **共通技タブの母集団 13 code と生ラッシュの別ボタン**（§1-2）／ **常設行は削除ボタンだけになった**。**★§2-1 の裁定が先** |
| 2 | **`DES-005` §5.4 / §5.15 相当** | **全技一覧に「タブに分類済みの技を省く」トグルが付いた**（既定 OFF・永続化なし・2 面） |
| 3 | **`CHANGE-166` の帰結文** | **「⇒『全技一覧から選ぶ』プルダウンからの入力になる」の 1 文が失効**（§2-3）。**★ファミリー UI の規則そのものは不変** |
| 4 | **`DES-004` §2.1 ／ `DES-005` §5.7** | **§4-1 の請求 2 本**（A1 / A2）。**★これは別サブの起票が要る規模である** |
| 5 | **`DES-003` §3.2** | **§4-2 の案 D**（`custom_states` に技コードの接頭辞を持たせる）。**★現時点では推していない** |
| 6 | **`DES-004` §2.1（データ）** | **§4-3 の `axe_kick_2` → `axe_kick` リネーム。★マイグレ 1 本 ＋ 別名・既存レシピの追随** |
| 7 | **`SUPP-001` §5.5 系** | **§2-2 の規約化**（「seed の実測は `moves` テーブルに対して行う」） |

**★番号は起票時に `docs/handover/change-number-registry.md` §1 で採番する。**
**★マイグレは本サブで 0 本消費。実査値＝最大 `000106`。⇒ 次に払い出すのは `000107`。**

---

## 7. 教訓（`retrospective-log` 行き・★親は読まなくてよい）

> 本節の宛先は `retrospective-log` へのバッチ反映であり、実施者は設計担当である。

1. **★★「実測」を名乗るときは、母集団が仕様の言う対象と同じかを最初に確かめる。** 本サブは指示書が `moves`（テーブル）と書いているのに `character_data/*.csv` を数えた。**⇒ CSV は入力の一部でしかなく、マイグレーションが直接投入する行が 283 行あった。一般形＝「seed」という語が指すものは 1 つとは限らない。数える前に、その語が実装のどこに対応するかを 1 度たどる。**
2. **★★誤った前提を回帰テストで固定すると、誤りが「検証済み」の外観をまとう。** 本サブは間違った母集団の件数を「実測の固定」として緑にした。**⇒ テストが在ることは確からしさの証明にならない。一般形＝新しく固定する数値は、それを算出した経路そのものを 1 度疑う。**
3. **★「注記」は運用にならない。** `moveSurfacing.ts` の初版は「両者の一致を検査する」と書いておきながら、その検査を書いていなかった（レビュー高-2 が検出）。**⇒ 一般形＝コメントに「〜を検査する」と書いたら、その手番で検査を実在させる。書けないなら「検査は無い」と正直に書く。**
4. **★未実施のことを完了報告へ断定で書いた**（`progress-log` への索引行を「追記した」と書きながら未追記。レビュー高-1 が実走して検出）。**★本サブは `D-510` を意識して §8 では避けたのに、§7 でやっていた。⇒ 一般形＝`D-510` の教訓は「レビュー結果を参照する欄」だけの話ではない。「まだやっていないこと」全般に効く。**
5. **★UI の要素を別の面へ移すときは、その要素が持っていた「表示だけの効果」を数え上げる。** 共通技の移設で `held`（物理入力の点灯）が消えかけた。**テストが 1 本あったため検出できたが、そのテストは testid を直す過程で書き換える対象でもあった。⇒ 一般形＝移設は「押せるか」だけでなく「見えるか」も移す。**
6. **★E2E の「起点」がアプリのリダイレクトで書き換わることがある。** `m24-12` は `page.goto("/")` を起点にしたが、PC 幅では `/` が `/combos` へ `replace` される。**⇒ 一般形＝履歴を数えるテストの起点は、アプリがそのパスに留まることを先に確かめてから選ぶ。**
7. **★開発者の実機確認は、テストが構造的に見られない層を見る。** 本サブでは **(a) 未分類に system が居る**（母集団の誤り）**(b) 舞のタブが空**（軸の限界）**(c) 必殺技タブに状態版が 1 つだけ混ざる**（命名の揺れ）の 3 件を、すべて実機確認が最初に捉えた。**⇒ 一般形＝「何が見えているか」を問う確認項目は、件数の主張より先に価値を出すことがある。**
