# 設計伝達レポート（例外レポート） — `M32-02`（操作説明書の本文 ＋ UI の粗の記録）

| 項目 | 内容 |
|---|---|
| **対象** | **親チャット（設計卓）** |
| **発信** | 製造担当 Claude Code（2026-09-12） |
| **指示書** | `docs/instructions/M32-02-usermanual-body.md` **v1.2.0** ／ 上位 `M32-overview` v1.5.0 |
| **実装コミット** | ブランチ `claude/exciting-cerf-vtmoia`（**push 済み。設計卓は HEAD で読める**） ／ **9 コミット**（`111a760` 段 1 ／ `960ac7b` 段 2 ／ `bf8d59d` 段 3 ／ `429bd43` 段 4 ／ `ef45cbd` 段 5 ／ `8df45ab` 節番号と暫定表記 ／ `a25bc64` 完了報告 ／ `4212bc5` レビュー取り込みと索引行 ／ 本レポート） |
| **関連** | 完了報告 `docs/progress/M32-02-completion-report.md` ／ レビュー `docs/progress/m32-02-review.md` |
| **チェックリスト** | `docs/instructions/reviews/M32-02-review-checklist.md` **v1.0.0**（**着手前に実在を確認した**＝指示書 §0.4 の版ゲート） |
| **レビュー集計** | **高 4 / 中 3 / 低 4 ＝ 全 11 件・採用 9 / 不採用 2・「高」の不採用 0 件・再レビュー往復 0 回（上限 2）** |
| **源泉** | 完了報告 ＋ レビュー報告書 ＋ **開発者との同一セッション内のやり取り**（着手前の裁定 4 件） |
| **CHANGE / マイグレ / 依存** | **CHANGE 0 本**（自採番しない） ／ マイグレ 0 本 ／ **新規依存 0 件** ／ **実装差分 0 行**（`web/` `internal/` `cmd/` `migrations/` の `git diff` 空） |

本書は **①独自確定仕様 ②契約違反の独自判断 ③製造判断 ④残課題**に絞る。指示書どおりの部分は割愛する。

**★★★§2 に 2 件ある。どちらも親の裁定が要る**——**ch16 の章名変更**（指示書 §3-3 の禁止に触れる。開発者裁定を根拠に実行した）と、**書き出しの件数上限について実装を正とし設計書に反する記述を本文へ書いたこと**である。

**★最重要は §2-2 と §4 である。** §2-2 は `DES-005` と実装の食い違いが**説明書という第三の面で発現した**初例であり、§4 は本サブ第 2 の成果物（UI の粗 **28 件**）そのものである。**⇒ 設計卓は完了報告を読まないため、28 件は本書にしか無い。**

---

## §1 製造が独自に確定した実装仕様（DES 反映が要るもの）

### §1-1 スクリーンショットの員数と割り当て（**27 枚**。開発者の手番の入力になる）

**開発者の裁定は「複雑な数画面だけ 2 枚に増やす」であり、どの章かは製造が決めた。**

- **選定基準＝`DES-005` の当該節の行数（実測）の上位 5 節。** 説明の分量ではなく**画面そのものの複雑さの代理指標**として採った。
- 計 **27 枚**＝22 章 × 1 枚 ＋ 下記 5 章の 2 枚目。

| 章 | 画面 | `DES-005` の節の行数 | 2 枚目に撮るもの |
|---|---|---:|---|
| 08 | コンボ登録・編集 | **378**（最大） | レシピを入力しているタブ |
| 17 | ゴミ箱 | 197 | 完全削除が拒否され、掴んでいるコンボが並んだ状態 |
| 20 | 確定反撃サーチ | 159 | 「自動判定できない相手技」のまとまりと理由ラベル |
| 07 | コンボ詳細 | 152 | 紐づくセットプレイと成立条件の 2×2 グリッド |
| 05 | コンボ一覧 | 112 | 絞り込み欄を開き、セットプレイを展開した状態 |

- 命名は `M32-01` が `.gitkeep` に定めた `ch<NN>-<slug>-<n>.png` に従った。**正本は器の各 `<figure>` の `img src`** である。
- **★撮り終えたら `figure` の `todo` クラスを外す必要がある**（未記入であることを示す枠線・背景色が付いている）。**⇒ 撮影の手番に「クラスを外す」が含まれることを、開発者へ伝えてほしい。**

**⇒ DES 反映は不要**（器は設計書に載っていない）。**★開発者の手番の入力としてここへ置く。**

### §1-2 説明書が用いる画面名の正本を**実装側**に取った

**`DES-005` の節名と実装の画面名が食い違う場合、実装を正とした。** 実際に効いたのは 1 件（ch16。§2-1）だが、**規則としては全 22 章に適用している。**

**⇒ `DES-005` §5 の節名は「設計上の呼称」であり「画面に出る語」ではない、という前提で書いた。★この前提が正しいかは親の確認を要する**——**正しければ、`M29-01` の語の統一は `DES-005` の節名までは及ばないことになる**（`retired-words.test.ts` が `docs/` を走査しないことと整合する）。

---

## §2 契約・設計に反する独自判断（**★独立カテゴリ・最優先**）

### §2-1 ★★ch16 の章名を「インポート」→「取込」へ変えた（指示書 §3-3 に触れる）

| 項目 | 内容 |
|---|---|
| **何に反したか** | **指示書 `M32-02` §3-7 の「やらないこと」表の 3 行目＝「章立て・目次・置き場を決め直す」**（**★`M32-01` の成果物である。変えたくなったら止めて設計卓へ請求する**）／ **チェックリスト E-4** |
| **なぜそう判断したか** | **「インポート」は `M24-07`（開発者判断 2026-08-30）で退けた語**であり、`web/src/locales/retired-words.test.ts` の `RETIRED` に `{word:"インポート", useInstead:"取込"}` として登録されている。**⇒ 説明書だけが退けた語で画面を呼ぶ形になっていた。** **★着手前に開発者へ提示し、「『取込』へ直す」の裁定を得た。** |
| **実装がどうなっているか** | `docs/usermanual/tacpendium-readme.html` の **4 か所**——目次の `<a href="#ch16">`、章見出しの `<h2>`、`figure` の `img alt`、`figcaption`。**画像のスラッグ `ch16-import-1.png` は変えていない**（スラッグは全 22 章とも英語で統一されており、`import` は「取込」の英語表記として正しい） |
| **★実装の現物** | ナビ「取込」（`web/src/components/Header.tsx:35`） ／ `h1`「取込(CSV)」（`web/src/pages/ComboImportPage.tsx:188`） ／ **`DES-005` §2 の画面一覧は `CHANGE-147` で「取込(CSV)」へ改名済み** |

**⇒ 親の裁定を要する 2 点**——**(a) 章名変更そのものの受理** ／ **(b) `DES-005` §5.14 の節名が「インポート」のまま残っていることの是正**（§6-1）。

### §2-2 ★★★書き出しの件数上限について、**設計書ではなく実装を正とした記述を本文へ書いた**

| 項目 | 内容 |
|---|---|
| **何に反したか** | **`DES-005` §5.13a の `CHANGE-165` 注記の逐語**＝「**書出に行数・バイト上限が無いのに、取込は 1000 行 / 10MiB で弾く**」 |
| **なぜそう判断したか** | **実装は書き出しも 1000 件で打ち切る。⇒ 設計書の逐語をそのまま本文へ写した初版が、それ自体で矛盾していた**（同じ段落に「上限がない」と「上限に当たったときの確認が出る」が並ぶ形になった）。**レビューが「高-1」として検出し、製造が実装を実物で当たって確認した。** |
| **実装の根拠** | `internal/service/comboio/export.go:18` の `const exportRowLimit = csvcore.DefaultMaxRows` ／ 同 `:136` の `if len(out) >= exportRowLimit { ... }` ／ `internal/service/comboio/csvcore/rules.go:10` の `DefaultMaxRows = 1000` ／ 打ち切り時は `res.Truncated()` が立ち、画面に `export.truncation.body`「**対象 {{total}} 件のうち {{included}} 件までしか出力できません。残りの {{dropped}} 件は出力に含まれません。**」の確認（`data-testid="export-truncation-confirm"`）が出る |
| **実装がどうなっているか** | `docs/usermanual/tacpendium-readme.html` の ch15（`:739-743`）と ch16（`:780-784`）。**「コンボは 1000 件で打ち切られる」「上限が無いのはセットプレイの行数とバイト数であり、そのぶん自分の取込が拒否しうる」の形へ**書き直した |

**★★同じ逐語が 2 か所で失効している**——**`DES-005` §5.13a の注記**と、**`internal/service/comboio/export.go:80` のコード注釈**（「★★書出には行数・バイト数の上限が無いのに、取込は上限で弾く」）。**⇒ 片方だけ直すともう片方が残る**（`E-118` と同型）。

**★★レビューが判断を保留した点（逐語）**＝「**`CHANGE-165` 注記が『書出に上限が無い』と述べている意図が、バイト数とセットプレイ行数だけを指した省略表現なのか、`exportRowLimit` を見落とした記述なのかは、本レビューでは判断できない。設計卓の確認を要する。**」**⇒ 製造も判断していない。親の裁定を要する**（§6-2）。

---

## §3 製造の判断

### §3-1 開発者へ確認して確定した点（**着手前・4 件**）

| # | 事項 | 開発者の裁定 | 製造の反映 |
|---|---|---|---|
| **1** | **`.src` / `.toc-src`（`DES-005 §5.x` の可視表示・全 44 か所）を出したままにするか隠すか。** `M32-01` が器の CSS へ「**★決めずに公開しないこと**」と申し送っていた論点 | **隠す**（CSS 1 行） | `.src, .toc-src{display:none;}` を追加。**★要素は HTML に残した**ので章 ⇄ `DES-005` の対応はソースで追える。器の CSS コメントを「決めた」記述へ書き替えた。**描画で可視 0 件を実測**（44 要素中） |
| **2** | **ch16 の章名「インポート」** | **「取込」へ直す** | §2-1 |
| **3** | **説明書の読み手の想定**（指示書 §7-2 の確認事項） | **設計卓の想定どおり**＝「SF6 は知っているが本アプリは初めて」。SF6 の用語は説明せず本アプリの用語だけを説明する | 器の `#about` の記述をそのまま使い、全 22 章で守った |
| **4** | **スクリーンショットの枚数** | **複雑な数画面だけ 2 枚に増やす** | §1-1（どの章かは製造が決めた） |

### §3-2 推測で進めた点

**なし。** 指示書・チェックリスト・開発者裁定・`DES-005` の as-built・実装ソースのいずれかで裏が取れる範囲だけを書いた。**★裏が取れない挙動は本文へ書いていない。**

---

## §4 設計担当が未把握の残課題・申し送り

> **★★★本節が本サブ第 2 の成果物である。** 指示書 §2.4-5 ／ チェックリスト A-6 が「設計伝達レポート §4 へ `followup` 登録候補として並べる」ことを求めている。
>
> **★`followup-backlog.md` は 1 文字も編集していない**（`D-838`）。**⇒ 畳むのは設計卓の手番。**
>
> **★★仕分けていない**（チェックリスト A-5）。「直す ／ 直さない ／ 判断が要る」は設計卓の手番であり、下表の「割付候補」は**どのレーンが受け取りうるかの目安**であって修正要否の判断ではない。
>
> **★計 28 件。0 件ではない。** すべて**新規登録**の候補である（既存スラッグの更新に当たるものは無い）。

### §4-1 説明が長くなった箇所（11 件）

**★共通の性質＝いずれも「画面からは観測できないこと」を文章で補っている。**

| スラッグ | 何が起きるか | 根拠 | 割付候補 |
|---|---|---|---|
| `combo-list-column-default-invisible-after-first-touch` | 表示列の既定は**一度でも列を触った環境では効かない**が、画面に「いま既定のままか」を示す手がかりが無い | `DES-005` §5.4（`useColumnVisibility` の保存値優先） | UI 改善レーン |
| `combo-list-filter-retention-scope-unreadable` | 絞り込み・並びの保持が **2 段の条件**（寄り道して戻ったときだけ／コンボ一覧だけ）で、**マイコンボは同じ表部品で見た目が同一なのに対象外** | `DES-005` §5.4（`combo-list-filters-v1`） | UI 改善レーン |
| `setup-condition-tristate-reads-as-exclusive` | 「成立 / 不成立 / 未検証」が**排他に見える語で非排他の集合**を表す。さらに「未検証＝埋め残しがある」「セットプレイ 0 件はどれにも出ない」 | `DES-005` §5.4（`CHANGE-095`） | 語彙・UI 改善レーン |
| `combo-detail-three-destructive-actions-indistinguishable` | 「ゴミ箱へ移動」「紐付け解除」「コンボ削除」が同一面に 3 つ。**`DES-005` の守り 5 点のうち 4 点は実装側の分離で、利用者が読む手がかりは「文言が違う」1 点だけ** | `DES-005` §5.6（`CHANGE-128`） | UI 改善レーン |
| `combo-editor-key-change-save-has-no-visible-cue` | **6 項目のどれかを変えると別コンボとして登録し直される**が、分岐が画面に一切現れない。**ゴミ箱に旧行が増えるという結果でしか気づけない** | `DES-005` §5.7 の保存方式 (a)/(b) | UI 改善レーン |
| `import-parent-resolution-mark-contradicts-result` | 親解決の `○` / `×` が**重複時の動作を考えに入れておらず、実際の結果と食い違う**。**⇒ 説明書が画面に反論する形になった** | `DES-005` §5.14（既知の乖離・恒久対応は繰越） | 取込レーン |
| `trash-mixes-two-origins-with-time-asymmetry` | ゴミ箱に **2 つの由来**（自分で消した／編集で積まれた）が混在しうるうえ、**`M23-01` 以前に編集した分は出たまま**という時期による非対称がある | `DES-005` §5.15（`CHANGE-121`・`D-460`） | ゴミ箱レーン |
| `game-update-two-edit-entrances-behave-differently` | **行の［編集］と［詳細］→ 編集で保存後の戻り先が違う**のに、画面に差が出ていない | `DES-005` §5.19b ／ 既存 `followup` `game-update-return-to-lost-via-detail` **と同根** | ゲーム更新レーン |
| `punish-absent-move-has-two-different-causes` | `damage = 0` は完全除外、`damage IS NULL` は手動確認レーンへ、と**行き先が違うのに画面で区別が付かない** | `DES-005` §5.20（`CHANGE-176`。**計測点 `M-155` が同じ取り違えを記録している**） | 確定反撃レーン |
| `punish-two-registration-paths-differ-in-auto-adopt` | **同じ「新規登録」に見える 2 導線で自動採用の有無が逆**。非対称は意図的だが、画面に区別を示す表示が無い | `DES-005` §5.20（`CHANGE-090`） | 確定反撃レーン |
| `punish-list-third-section-nesting-unreadable` | 第 3 セクションの入れ子が **3 段**あり、どこに何が入るかを区分名から辿れない。**別枠が「そこにしか解除手段が無いから残してある」ことは名前から読めない** | `DES-005` §5.21（`CHANGE-168`） | 確定反撃レーン |

### §4-2 「先に別の画面を見ないと分からない」箇所（6 件）

**★説明の順序（章の並び）と作業の動線が逆行する箇所である。**

| スラッグ | 何が起きるか | 根拠 | 割付候補 |
|---|---|---|---|
| `mycombo-status-not-switchable-on-its-own-screen` | マイコンボの状態はマイコンボの画面で切り替えられず、**コンボ登録・編集の「その他情報」へ**行く必要がある | `DES-005` §5.5 / §5.7 | UI 改善レーン |
| `setup-conditions-editable-only-from-parent-combo` | 成立条件は**新規登録時のみ**出る。あとから埋めるには**コンボ詳細**へ戻る | `DES-005` §5.9-6（`CHANGE-096`。`/setups/:setupId` を実装しない判断の帰結） | セットプレイレーン |
| `preset-vs-moves-grid-role-split-not-discoverable` | 「呼び名を変えたい」のか「技のデータを直したい」のかの区別が、**moves 編集グリッドを知るまで付かない** | `DES-005` §5.11 / §5.18 | UI 改善レーン |
| `punish-unknown-damage-fix-requires-moves-grid` | 「ダメージ不明」で手動確認へ回った技を直すには **moves 編集グリッド**が要るが、そこへの導線が確定反撃サーチに無い | `DES-005` §5.20 / §5.18 | 確定反撃レーン |
| `password-recovery-notice-duplicated-in-two-screens` | パスワード紛失時の復旧手順が**設定とログインの 2 か所**に要る。**★`DES-005` §5.16 が理由まで明記している**（「この案内が要る人はログイン画面に居て、設定画面へ入れない」） | `DES-005` §5.16 / §5.22 | **★設計書に理由の明記あり。据え置きの可能性が高い** |
| `game-update-badge-only-on-detail` | 「更新未確認」の印が出るのは**コンボ詳細だけ**。**★`DES-005` §5.19b が「受け入れた代償」として明記している設計判断** | `DES-005` §5.19b | **★設計書に明記あり。据え置きの可能性が高い** |

### §4-3 語が揺れている箇所（4 件・**`M29-01` / `M24-07` の結果を名指しする**）

| スラッグ | 何が起きるか | 根拠 | 割付候補 |
|---|---|---|---|
| **`des005-section-name-uses-retired-word-import`** | **`DES-005` §5.14 の節名が「インポート」のまま。⇒ 退けた語である。★§2 の画面一覧は `CHANGE-147` で改名済みで、片側だけが古い** | `DES-005` §5.14 ／ `web/src/locales/retired-words.test.ts` の `RETIRED` | **★CHANGE 請求として §6-1 に別途挙げた** |
| **`retired-words-guard-does-not-scan-docs`** | **`retired-words.test.ts` の走査対象は `web/src` だけで、`docs/` を 1 バイトも見ない。⇒ 設計書・説明書の語の統一は、構造的に人が読む以外の経路が無い** | 同ファイル自身の注記 ／ **`M29-01` 完了報告 §7 が `05-screen-design.md:558` / `:562` について同じことを書いている** | **★機構の穴。改善レーン** |
| `import-screen-has-two-names` | 同じ画面にナビ「取込」と `h1`「取込(CSV)」の 2 つの呼び名がある。**`M24-07` は括弧の有無までは揃えていない** | `Header.tsx:35` ／ `ComboImportPage.tsx:188` | 語彙レーン |
| `intake-japanese-export-katakana-asymmetry` | **「取込」は和語、「エクスポート」はカタカナのまま。** `M24-07` は取込側だけを和語へ寄せ、**`M29-01` はこの対を群として扱っていない**（4 群＝空レシピ／コピー／ゲージ／相手状態） | `DES-005` §5.13a ／ `M29-01` 完了報告 §2 | 語彙レーン |

### §4-4 「よく使う操作」を 3 つに絞れなかった画面（4 件）

**★黙って 5 つ書いていない。3 つに収め、落としたものを挙げる。**

| スラッグ | 何が起きるか | 落としたもの | 割付候補 |
|---|---|---|---|
| `settings-screen-has-six-sections` | 設定は節が 6 つあり、3 つの操作で代表させられない | データ（DB パス・バックアップ・リストア・レシピキャッシュ再構築）／ プリセット管理へのリンク ／ 詳細（初回ガイド・ログパス・バージョン） | UI 改善レーン |
| `combo-list-carries-too-many-roles` | **操作が多いのではなく、1 画面が担う役割が多い** | 表示列カスタマイズ ／ セットプレイの展開 ／ ソート ／ レシピを縦に表示するトグル ／ 行の編集・コピー・削除 | UI 改善レーン |
| `combo-editor-has-fourteen-display-items` | `DES-005` §5.7 の表示項目は 14 項目 | セットプレイ登録セクション ／ キャラ固有状態 ／ 起き攻め情報 ／ メディア 3 欄 ／ タグ ／ メモ ／ コンボ後情報 | UI 改善レーン |
| `punish-search-has-two-kinds-of-reject` | **「不採用」が 2 種類ある**（一時非表示 ／ `adopted`・`unreachable` トグル）**こと自体が粗の候補である** | 手動確認レーンからの登録 ／ 不採用 2 種 ／ 変換済みの折りたたみ。**★`DES-005` §5.21 の注記が「混同すると指示が 2 通りに読める」と述べている** | 確定反撃レーン |

### §4-5 ★★本文が弁解を負った箇所（3 件・**レビュー指摘 中-1 で足した 5 つ目の型**）

**★「これは仕様です」「不具合ではありません」「今後実装予定です」と説明書が書かざるを得なかった箇所である。⇒ 書き手が困った場所がそのまま出ている。`B4` の逐語「説明が要る箇所＝設計が負けている箇所」に最も近い型。**

| スラッグ | 本文の逐語 | **なぜ弁解が要ったか** | 割付候補 |
|---|---|---|---|
| `user-select-not-persisted-reads-as-bug` | 「選んだ人を保存していないためで、**不具合ではありません**」 | **利用者から見ると「選んだのに忘れられた」体験そのものである。設計の意図**（`DES-002` §8）**は正しいが、画面はそれを 1 つも伝えていない** | UI 改善レーン |
| `settings-backup-restore-buttons-disabled-in-release` | 「『手動バックアップ』『リストア』のボタンは、**いまは押せません**（**今後実装予定**）」 | **押せないボタンが配布物の画面に出ている。⇒ 説明書が「押せない理由」を引き受けている。★`D-607` は「設定画面に出ているボタンを未達の機能要件にしないため」と記録しているが、その判断は*画面に出し続けること*を前提にしている** | **★公開前ゲートに近い。`M32` 後の判断** |
| `tag-category-color-one-way-only` | 「カテゴリと色は、一度決めると『未設定』には戻せません。**仕様であり、不具合ではありません**」 | **一方向にしか動かせない設定が画面上ではそう見えない。⇒ 押してみるまで知る手段が無い** | UI 改善レーン |

> **★据え置いた 2 件**（レビューも据え置きが妥当としたもの）——**ch21 の「パニッシュカウンター(インパクト)」が第 3 セクションへ出る件**（`CHANGE-151` §6-1 に「欠陥ではない」の明記あり）と、**ch15 / ch16 の「載るタグは操作した利用者のもの」**（`CHANGE-113` に同様の明記あり）。**どちらも弁解ではなく既知の設計判断の説明である。**

### §4-6 そのほかの申し送り（4 件）

| # | 内容 |
|---|---|
| **★★1** | **目次末尾の 1 文が、利用者に内部の設計書パスを見せている**（逐語＝「全 22 章。`docs/design/05-screen-design.md` §5 の画面 24 節のうち、廃止・削除済みの 2 節（§5.13 エクスポート／§5.17 moves 取込プレビュー）を除いた数です。」）**。⇒ `.src` / `.toc-src` を隠した結果、ここが読み手から見える唯一の内部参照として残った。★目次は `M32-01` の成果物であり、開発者の裁定は `.src` / `.toc-src` についてのものだったため触っていない**（`E-4`。レビューもこの判断を妥当としている）**。~~⇒ 公開前に扱いを決めてほしい。~~** **★★★【2026-09-15 決着＝開発者裁定】外した。** 開発者の逐語＝**「説明書に設計書の参照は付けたくありません」（2026-09-15）**。⇒ 目次末尾の 1 文を **「全 22 章。アプリの画面ごとに 1 章です。」** へ置き換えた。**★描画で実測し、読み手に見えるテキストから `docs/design` / `DES-005` / `§5.13` / `§5.17` の痕跡が 0 件であることを確認した。** **★`.src` / `.toc-src` の 44 件は `display:none` のまま据え置き**〔2026-09-12 の裁定＝「隠す。要素は残す」。**`§5.13` / `§5.17` は同 44 件に含まれないため本裁定の射程外**〕**。** **⇒ `followup` 登録は不要である**〔スラッグ候補だった `usermanual-toc-leaks-design-doc-path` は起票しないこと〕 |
| **★★2** | **到達経路（`P-57`）は未決のままだが、本文が 22 章そろったことで優先度が上がった。** `README` が案内する先に実体が在る状態になったのに、**(a) URL が書けない ／ (b) zip に同梱されない ／ (c) GitHub 上の `.html` はソース表示になる** は変わっていない。**★あわせて器が Google Fonts を `@import` で読む**（`tacpendium-readme.html:8`）**ため、zip 同梱を採るなら「同梱物が閲覧のたびに外部へ接続する」ことの可否も同じ手番で決まる**（レビュー 低-3。オフラインでは `sans-serif` へ落ちる）**。⇒ `P-57` の既存行へこの観点を足してほしい** |
| **3** | **`DES-005` §2 の画面一覧に §5.19b と §5.13a の行が無い**（表は 22 行）**。§3 の画面遷移図にも §5.19b / §5.20 / §5.21 / §5.22 / §5.13a / §5.19 が無い**（`:111` に「主要な遷移のみ記載」の注記はある）**。⇒ 本サブの対応表は §5 の節見出しを正とした。★次に画面数を数えるサブは §2 の表を母数にしないこと。** スラッグ候補＝`des005-screen-inventory-table-missing-rows` |
| **4** | **章 ⇄ `DES-005` の対応表が、読み手の受け取る成果物の側に無い**（レビュー 中-3）**。⇒ `.toc-src` を隠した開発者裁定の直接の帰結である。★対応は HTML ソース（要素は残してある）と完了報告 §2.1 の 2 系統で追えるが、設計卓は完了報告を読まないため、本書 §1-1 と `DES-005` 側の追記が実質の突合先になる** |

### §4-7 ★停止時記録（**§J 行の原稿**）

**なし。** **★上限（再レビュー往復 2 回 / タイムボックス / 開発者の終了指示）に達していない。** 再レビュー往復 **0 回**、未解消のまま停止した項目は **0 件**、「高」指摘の不採用は **0 件**。

---

## §5 参考（触れていない＝不変の証跡）

- **実装差分 0 バイト**: `git diff --stat 6fdd018 -- web/ internal/ cmd/ migrations/` が空。
- `go test ./...` → **ok 60 / FAIL 0**（着手前と同一）。
- `cd web && pnpm test` → **229 ファイル / 2810 件すべて緑**（着手前と同一）。
- `scripts/check-dist-readme.sh` 照合 ✅ ／ `--self-test` 合格（陽性は赤・陰性は緑）。
- **`dist-readme.txt` の起動方法節 `- Windows : tacpendium-windows-amd64.exe   (公式サポート)` は変えていない**（`check-dist-readme.sh:145` の陽性対照の標的）。
- 常設検査 7 本違反なし（`check-artifact-integrity.sh` を 1 本目）。`check-progress-log-index.sh` は着手時 NG 1 件 → 索引行追記で緑。
- **`E-225` 観点**: HTML の 130 deletions はプレースホルダ 88 行 ＋ 器の CSS コメント ＋ 章名・暫定表記の置換のみ。**レビューが独立検証し「失われた行は無い」と確認**（`h3` は 22 章すべて 4 本＝計 88）。
- **新規ファイルは 3 本のみ**（完了報告 ／ レビュー報告 ／ 本書）。器・`README` 系はすべて既存ファイルの編集。
- HTML 構造: id 重複 0 ／ dead anchor 0 ／ `img alt` 空 0 ／ 描画時 JS 例外 0（900px / 390px とも横スクロールなし）。

---

## §6 CHANGE 起票のたたき台（設計担当向けチェックリスト）

**★製造は自採番していない**（`D-293`）。**番号は起票時に registry で採番すること。**

### §6-1 `DES-005` §5.14 の節名が退けた語のまま

- **対象**: `docs/design/05-screen-design.md` §5.14 の節見出し「インポート」。
- **是正の向き**: 実装とナビの語は「取込」、`h1` は「取込(CSV)」。**同書 §2 の画面一覧は `CHANGE-147` で既に「取込(CSV)」へ改名済み**であり、**片側だけが古い**。
- **★機械で捕まらない理由**: `retired-words.test.ts` の走査対象は `web/src` だけで `docs/` を見ない（§4-3）。
- **波及の確認先**: 同書の他の節・`DES-002` §4.2・`DES-006` に「インポート」が残っていないかを `grep` で全数（`E-118`）。

### §6-2 ★★`DES-005` §5.13a の `CHANGE-165` 注記と `export.go:80` のコード注釈が実装と食い違う

- **対象**: `DES-005` §5.13a の注記の逐語「**書出に行数・バイト上限が無いのに、取込は 1000 行 / 10MiB で弾く**」。**同じ逐語が `internal/service/comboio/export.go:80` のコード注釈にも在る。⇒ 2 か所。**
- **実装**: `exportRowLimit = csvcore.DefaultMaxRows = 1000`。**書き出しはコンボ 1000 件で打ち切られ、打ち切り時は確認が出る**（§2-2 に根拠一式）。**上限が無いのは「セットプレイの行数」と「バイト数」である。**
- **★先に決めることが 1 つある**（レビューが判断を保留し、製造も判断していない）——**注記が「バイト数とセットプレイ行数だけを指した省略表現」なのか、「`exportRowLimit` を見落とした記述」なのか。** 前者なら注記の書き足し、後者なら記述の是正になる。
- **★コード注釈の是正は実装の手番である**（本サブは実装差分 0 行）。**⇒ 設計書の是正と同じ CHANGE に紐づけて、実装側の手番も併記してほしい。**

### §6-3 マイグレ

**なし。** 本サブは **0 本**。`ls migrations/` は着手前後で不変。

---

## §7 教訓（retrospective 行き・**親は読まなくてよい**）

> 宛先は `docs/handover/retrospective-log.md` へのバッチ反映であり、実施者は設計担当である。

1. **★★「粗の型」は指示書が挙げた分で閉じない。** 指示書 §2.4 は 3 型（長くなった／順序が逆行する／語が揺れた）を挙げ、§4.5 が実質 4 型目（3 つに絞れなかった）を作っていた。**製造はその 4 型で網を閉じ、5 つ目——「本文が『これは壊れていません』と書かされている箇所」——を自分では思いつかなかった**（レビュー 中-1 で 3 件出た）。**⇒ 型を列挙した指示書は、列挙が網の上限として働く。★「列挙は例であって全部ではない」を指示書側に 1 行書くだけで防げる。**
2. **★★設計書の逐語を写すと、設計書の失効がそのまま新しい面へ複製される。** 本サブは `DES-005` §5.13a の注記「書出に上限が無い」を本文へ写し、**それ自体で矛盾する段落**（同じ段落に「上限に当たったときの確認が出る」が並ぶ）**を作った。⇒ 矛盾は書いた本人には見えず、レビューが実装を当たって初めて出た。★設計書から数値・上限・否定形を写すときは、写す前に実装で 1 度当たること。**
3. **★★★機械の網は「走査対象」を書いておかないと、無い所を守っていると誤解される。** `retired-words.test.ts` は `web/src` だけを走査するが、**それを知るにはファイル自身のコメントを読むしかない。⇒ `M29-01` が語を統一したあとも、設計書と説明書は一度も守られていなかった。★検査を新設するときは「何を見ないか」を検査の出力自体に出すこと**（`check-doc-refs.sh` の走査範囲表示が先例。同検査は `M26-04` / `M32-01` で 2 度「緑を健全さの証拠と読むな」と申し送られている）。
4. **★文書を書く工程でも `E-225`（新規のつもりで既存を上書き）の観点は要る。** 本サブは新規ファイルを 3 本しか作っていないが、**`git diff --stat` の deletions を読む手順は実際に効いた**——130 deletions の内訳を分解しなければ、`<h3>` が削除側に現れる差分整列の産物を「消した」と誤認しうる。**⇒ 実装サブだけの手順にしない。**
5. **★「説明書を書く」サブは、書き終えた時点では半分しか終わっていない。** 本サブの成果物 2 つのうち、**第 2**（粗の記録）**は設計卓へ届いて初めて価値になる**（レビュー 高-4 の逐語）。**★設計卓は完了報告を読まないため、粗を完了報告にだけ書くと届かない。⇒ 設計伝達レポート §4 が唯一の経路である。★この構造を指示書 §2.4-5 は 1 行で書いているが、重みが伝わりにくい。**

---

*以上、`M32-02` 設計伝達レポート。* **★★§2 の 2 件（ch16 の章名変更 ／ 書き出し上限について実装を正としたこと）は親の裁定を要する。** **★§4 の 28 件は本書にしか無い**——**設計卓は完了報告を読まないため、畳む対象は本書 §4 である。**
