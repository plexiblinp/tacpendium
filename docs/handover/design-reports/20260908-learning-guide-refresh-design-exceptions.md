# 学習ガイド実態追随 設計伝達レポート（コード側是正候補 18 件の申し送り）

| 項目 | 内容 |
|------|------|
| 対象 | **親チャット（設計卓）** |
| 発信 | **学習教材担当 Claude Code（クラウド実行）** / 2026-09-08 |
| 作業 ID | `20260908-learning-guide-refresh`（ブランチ `claude/go-react-learning-guide-update-yixm88`） |
| 指示 | **指示書は無い。** 開発者が本セッションへ直接投入した自然言語の依頼（`docs/human-notes/go-react-learning-guide` の実態追随 ＋ 新資料 1 本） |
| CHANGE / マイグレ | **CHANGE 起票 0 本** ／ **マイグレ消費 0 本**（`ls migrations/` 実査の末尾は `000106`。**次に払い出す番号は `000107`**。着手前から不変） |
| 実装コミット | 基点 `50235ce` から **8 本**（`7678202`〜`a2b8dba`）。**push 済み** |
| 変更範囲 | **`docs/human-notes/go-react-learning-guide/` の 10 ファイルのみ**（`git diff 50235ce..HEAD --name-only` の全件がこの 1 ディレクトリ配下） |
| 関連 | **完了報告・レビュー報告は存在しない**（本セッションは製造工程ではなく、レビュー工程も持たない） |
| 作成条件 | **作業直後の同一セッション内で生成**。本文の指摘はすべて本セッションで実ファイルを再確認済み |

---

## §0 このレポートの発信者について（★設計卓は本セッションの存在を知らない）

**★★本レポートの発信者は製造担当ではない。** 通常の設計伝達レポートは「指示書に沿って実装した製造担当が、指示書との差分を親へ返す」ものだが、**本セッションはその系列に属さない。** 設計卓が発注した覚えのない作業からレポートが届く形になるため、先に立ち位置を明かす。

### 0-1 何をしていたセッションか

**開発者が直接投入した依頼で、`docs/human-notes/go-react-learning-guide/`（Flask / Spring 経験者向けに、本リポジトリの実コードを教材化した HTML 9 本）を実態へ追随させていた。** 同資料は **2026-07-22 のコードスナップショット**で書かれており、以降 **79 コミット**（M27 系 / M28 系 / M29 系）が入ったため、**全ページのコード引用（パス・行番号・引用コード・説明）を現在の作業ツリーと 1 件ずつ突き合わせる**作業をした。あわせて、学習価値が高い新機能として **ゲーム更新影響判定（FR702・M28-02a / M28-02c）のウォークスルーを 1 本新設**した。

**⇒ つまり本セッションは「実装」ではなく「実コードの全数読み合わせ」をしていた。** 本レポートの中身は、**その読み合わせの副産物として検出された、コード側の失効・欠陥・非対称の一覧**である。

### 0-2 権限と制約（★コードには 1 行も触れていない）

- **開発者が本セッションに課した制約は「他の資料やコードの編集は禁止。直すべき場所を見つけたら画面上にレポートだけ出せ」であった。** 本レポートはその「レポートだけ出す」を、口頭の画面出力で終わらせず、**設計卓が受け取れる形へ移したものである**（開発者の追加指示）。
- **⇒ `internal/` `web/src/` `web/e2e/` `migrations/` `docs/design/` への差分は 0 である。** 証跡は §5。
- **本レポートは `docs/handover/followup-backlog.md` の本表を編集していない**（`CLAUDE.md` §10.Y ／ board `D-382`。**製造が直接書けるのは §J だけであり、本表への登録は本レポート §4 を設計卓が畳む形を採る**）。

### 0-3 証拠としての強さ（★これは監査ではない）

**★本一覧は網羅的なコード監査ではない。** 見ているのは **「ガイドが引用している範囲」だけ**であり、そこから外れた領域は 1 行も読んでいない。**⇒ 「ここに挙がっていない = 健全」ではない。**

いっぽう、**挙がっている 18 件はすべて本セッションで実ファイルを開いて再確認した**（下請けエージェントの申告をそのまま転記していない）。**⇒ ファイルパス・行番号・引用は信頼してよい。** ただし **「実害の大きさ」と「直すべきか」の判断は本セッションの領分ではない**ため、そこは候補として出すに留める。

### 0-4 §1〜§3 が空である理由

**本テンプレートの §1（独自確定仕様）・§2（契約違反の独自判断）・§3（製造判断）は、いずれも「実装が判断を下した」ことを報告する枠である。** **本セッションは実装をしていないので、構造上どれも該当しない。** 節は削除せず「なし」で残す（受取側が「書き忘れ」と「該当なし」を区別できるようにするため）。

**⇒ 本レポートの本体は §4 だけである。** 外出先で読む場合は §4 だけで足りる。

---

## §1 製造が独自に確定した実装仕様（DES 反映が要るもの）

**なし。**（実装をしていないため。§0-4）

---

## §2 契約・設計に反する独自判断

**なし。**（実装をしていないため。§0-4）

> **★ただし「実装が既に契約と食い違っている状態」は検出している。** それは本セッションが下した判断ではなく**既存の状態**なので、§2 ではなく §4-C に置いた。**§2 の「未処理で流れることを避ける」という趣旨は §4-C にも同じく当ててほしい。**

---

## §3 製造の判断

**なし。**（実装をしていないため。§0-4）

---

## §4 設計担当が未把握の残課題・申し送り

**★★ここが本レポートの全部である。** 検出は 18 件。**うち 3 件は `followup-backlog` に既登録の行と重なる**ため、新規登録ではなく**既存行の前提を補正する情報**として 4-A に分けた。残る 15 件が新規候補である。

**★深刻度は本セッションの見立てであり、確定ではない。**

### 4-A 既存 followup と重なるもの（★新規登録は不要。既存行の前提が不正確なので補正を請う）

#### 4-A-1 `sqlite-busy-translation-remaining-paths` の前提が 1 つ狭い ——「サービスが翻訳していても、ハンドラが落とす」経路が実在する

既存行（`followup-backlog.md:865`）は **「`SQLITE_BUSY` → 503 `database_busy` の翻訳が 5 か所にしか入っていない」「機械的に広げられる（判定関数は既に在る）」** と述べている。**⇒ 読むと「サービス層で翻訳を足せば済む」に見える。**

**★実際にはもう 1 段ある。** **`PATCH /api/combos/:id`（メタデータ更新）は、サービス層が既に翻訳しているのに、ハンドラが 503 へ写していない。**

| 層 | 実装 | 状態 |
|---|---|---|
| サービス | `internal/service/combo/service.go:650` が `busyOr(err, "begin tx")` を通す | **翻訳済み** |
| ハンドラ | `internal/api/combo/handler.go:402` の `UpdateMetadata` に `ErrDatabaseBusy` の分岐が**無い** | **500 `internal_error` へ落ちる** |

**対照**：同ファイルの他 3 経路（`handler.go:59` / `:470` / `:559`）は分岐を持つ。**⇒ 同じ混雑が POST / PUT では 503、PATCH だけ 500 になる。**

**⇒ 依頼**: 既存行の内容列へ **「サービスが翻訳済みでもハンドラが写していない経路が 1 本ある（`UpdateMetadata`）」** を追記してほしい。**この 1 行が無いと、サービス層だけを掃いて「完了」と判定される。**

#### 4-A-2 `api-error-codes-not-centralized` の射程外に、503 のリテラルが 1 件ある

既存行（`followup-backlog.md:464`）は **409 を返す 10 種**を列挙している。**★503 側にも同型が 1 件ある。**

- `internal/api/combo/handler.go:560` が **`"database_busy"` を生リテラルで書いている**。定数 `model.ErrorCodeDatabaseBusy`（`internal/model/api_error.go:40`）は既に存在し、他経路はそちらを使う。
- **★同じ経路で文言も割れている**：`handler.go:62` と `:473` は「データベースが混み合っています。**少し時間をおいて再度お試しください**」、`handler.go:560` は「**少し待って再試行してください**」。

**⇒ 依頼**: 既存行の列挙へ 503 側 1 件を足すか、「409 に限らない」と射程を広げてほしい。**文言の不統一は M29-01（i18n 用語統一）の観点でも拾える。**

#### 4-A-3 `csv-drive-damage-sign-flipped-across-versions` の隣に、**コード内コメントの失効**が 2 件残っている

既存行（`followup-backlog.md:1330`）は **CSV の値の意味が版をまたいで反転する**問題を扱う。**★これとは別に、`M27-03` の符号反転（負=削り / 正=回復）に追随していない Go のコメントが 2 件ある。** 詳細は 4-C-1。

**⇒ 依頼**: 4-C-1 を別行として起票するか、既存行の下に束ねるかを決めてほしい。**別物である**——既存行は**データの互換性**、4-C-1 は**仕様記述の失効**である。

---

### 4-B 新規候補: 実装の欠陥・非対称（★実害の見積もりが要る）

#### 4-B-1 ★★タグの重複判定に check-then-act 競合が残っている ——「同型の 3 例目」の掃き残し

**`internal/service/tag/service.go:69-78`（`CreateTag`）と `:87-96`（`UpdateTag`）は、`repo.List` で全件取って Go 側のループで同名を探し、その後に `repo.Create` / `repo.Update` を撃つ。** **トランザクションを持たず、判定と書き込みの間が開いている。**

```go
existing, err := s.repo.List(ctx, userID, "", false, nil)   // service.go:69
for _, t := range existing {
    if t.Name == input.Name { return nil, ErrTagNameDuplicate }
}
return s.repo.Create(ctx, userID, input)                     // service.go:78
```

**★★これは `M24-11` と `M24-13` が塞いだのと同じ型である。**

| 面 | 状態 | 出典 |
|---|---|---|
| コンボ（`VAL-C02`） | **解消済み**（`M24-11`。`BEGIN IMMEDIATE` ＋ 判定を Tx の内側へ） | `val-c02-check-then-act-race-on-combo-create`（`D-579`） |
| セットプレイ（`VAL-S04`） | **解消済み**（`M24-13`） | `setup-val-s04-check-then-act-race`（`D-593`） |
| **タグ（`VAL-T01`）** | **未着手・台帳に行が無い** | **本項が初出** |

**★負けたときの落ち方は判明している。** `tags` には `UNIQUE (user_id, name)` がある（`migrations/000001_init_schema.up.sql`）。**⇒ 競合に負けた側は DB のユニーク制約に当たり、`repository.Create` が生エラーを返す。** ハンドラ（`internal/api/tag/handler.go:92-100`）は `ErrTagNameDuplicate` としては拾えないので **`default` 分岐へ落ち、`409 tag_name_duplicate` ではなく `500 internal_error` になる。**

**★実害は小さいと見ている**（単一利用者のローカルアプリで、タグを同時に 2 つ作る操作が想定しにくい）。**⇒ 判断材料は「同型を 2 面で潰した以上、3 面目を残す理由があるか」であって、実害の大きさではないかもしれない。設計卓の裁定を請う。**

#### 4-B-2 ★★タグの `category` / `color` を「未設定へ戻す」手段が画面から無い

**一度カテゴリや色を付けたタグを、UI から NULL へ戻せない。** 3 層が噛み合って起きている。

| 段 | 実装 | 挙動 |
|---|---|---|
| 1. フロント | `web/src/features/tag/hooks/useTagFormDialog.ts:57-58` の `values.category \|\| undefined` | **空文字を `undefined` にする** |
| 2. 送信 | `JSON.stringify` が `undefined` のキーを落とす | **キーごと消える** |
| 3. バックエンド | `internal/model/tag.go:67-71` の `*string` ＋ `internal/repository/tag/repository.go:165-172` の動的 SET | **nil = 「変更なし」としてスキップ** |

**★`null` を送っても救えない。** Go の `encoding/json` は `null` をポインタの nil へ落とすため、**「省略」と「明示的な null」が区別できない。** **⇒ フロントの小修正では直らず、契約の決めごとが要る**（空文字を「消す」の意味に割り当てる／`Optional[T]` 相当の三値表現を入れる／消せない仕様として明文化する、のいずれか）。

**⇒ ★これは設計判断事項である。** 本セッションは案を選んでいない。

#### 4-B-3 `AlertDialogContent` にだけ高さ制限が無い（`DialogContent` との非対称）

- `web/src/components/ui/dialog.tsx:40` は `max-h-[85vh] overflow-y-auto` を持つ。
- **`web/src/components/ui/alert-dialog.tsx` は `max-h-[85vh]` を 1 件も持たない**（grep 0 件）。両者の Content のクラス列は他の部分がほぼ同一で、**差分はこの 2 クラスの有無だけ**である。

**⇒ 確認ダイアログの本文が長いとビューポートからはみ出し、ボタンへ手が届かなくなりうる。** 本文の長さは呼び手が渡す i18n 文言に依存する（例 `DeleteComboConfirm` の `message`）。**★「確認ダイアログは短文前提」という意図的な非対称なのか、`dialog.tsx` だけ直して反映し忘れたのかは、コード上のコメントからは判別できない。** 深刻度は **中（条件付き）**。

---

### 4-C 新規候補: 失効コメント・規約乖離（★動作は正しいが、読んだ人が誤る）

#### 4-C-1 ★★`M27-03` の符号反転に追随していない Go コメントが 2 件

**現行の正しい意味は「負が削り、正が回復」**（`a36df63` ／ `web/src/locales/ja.json:590`）。**以下 2 件は反転前の意味のまま残っている。**

| ファイル:行 | 現在の記述 | 問題 |
|---|---|---|
| `internal/service/comboio/csvcore/combo.go:23` | 「DriveDamage は相手に与える **D ゲージ削り量**」 | **正 = 削り**と読める。★**CSV の契約 DTO** であり、値を作る人が最初に読む場所 |
| `internal/service/validation/combo.go:309` | 「相手ドライブゲージ削り量（本数）。**回復で負値**」 | **符号規則を明示的に逆に述べている。** ★`VAL-C13` の godoc であり、**仕様として読まれる** |

**★ロジックは正しい。** `validateC13DriveDamageRange`（`validation/combo.go:311-321`）は `-6〜6` の対称な範囲検査で、符号の向きに依存しない。**⇒ 実害はコメントの誤読のみ。**

**★★ただし見逃せない点がある。** **`M27-03` のコミットメッセージ自身が「`help.driveDamage` を ja/en とも『負が削り、正が回復』へ。★失効した記述を残さない。」と宣言している。** **⇒ 宣言した当人の手番で 2 件漏れた。** 教訓は §7。

#### 4-C-2 タグのエラー詳細 DTO が snake_case のまま（`CLAUDE.md` §4 との乖離）

**`CLAUDE.md` §4 は「JSON タグは camelCase で統一」「DB カラム名のスネークケースをフロントに持ち込まない」を規約化している。** **本経路だけが両側とも snake_case で通っている。**

- バックエンド: `internal/api/tag/handler.go:163-164` が `"usage_count"` / `"force_delete_query"` を詳細マップへ入れる
- フロント: `web/src/types/tag.ts:23-25` の `TagErrorDetail` が同名で受ける
- 消費: `web/src/features/tag/api/tagApi.ts:23` が `details?.usage_count` を読む

**★★今は両側が揃っているので壊れていない。だから危ない。** **⇒ 規約に気づいた誰かが片側だけ camelCase へ直すと、型検査もテストも通ったまま「使用件数が表示されない」形で静かに壊れる。**

**⇒ 依頼**: **(a) 規約どおり camelCase へ両側同時に直す (b) 例外として明文化する** のどちらかを決めてほしい。**放置が最も危ない状態である。**

#### 4-C-3 `TagErrorDetail.existing_tag_id` は誰も送らず誰も読まない死んだフィールド

**`web/src/types/tag.ts:23` の `existing_tag_id`** について:

- **バックエンドは 1 か所も返していない**（`grep -rn "existing_tag_id" --include='*.go'` が **0 件**）
- **フロントも読んでいない**（型定義の 1 行以外に参照が無い）

**⇒ 「サーバが返しうる」と型が主張しているが、実際には返らない。** 型が実態と食い違う状態であり、**4-C-2 を直すときに同時に始末できる。**

#### 4-C-4 `ErrTagInUse` の godoc が、隣の型の説明になっている

`internal/service/tag/errors.go:15-16`:

```go
// ErrTagInUse はタグが combo_tags で使用中の場合のセンチネル(VAL-T03)。
// UsageCount フィールドで件数を確認できる。       ← ★この行
var ErrTagInUse = errors.New("tag: in use")        // ← フィールドを持たない
```

**`UsageCount` を実際に持つのは、下で定義される `TagInUseError` 型（`errors.go:19-21`）である。** **⇒ コメントの置き場が 1 つずれている。** 深刻度は低。

---

### 4-D 新規候補: 設計書・運用ファイルの陳腐化（★設計卓の持ち物）

> **★本群は `docs/design/` と `.claude/` 配下であり、製造・教材担当が独断で触れる場所ではない。** **⇒ すべて設計卓へ差し戻す。**

#### 4-D-1 ★★`docs/design/testid-convention.md` の「付与済み一覧」が実態と大きく乖離

同書は **「プロジェクト全体の `data-testid` を一元管理する。新規付与時は必ずこの一覧を更新すること」** と宣言している。

| 実測 | 値 |
|---|---|
| `web/src` の `data-testid` 出現数 | **430 件**（`grep -rn "data-testid" web/src --include='*.tsx'`） |
| 同書「付与済み一覧」の表の行数 | **21 行** |

**⇒ 一覧はもはや一元管理として機能していない。** 現用中の spec が掴む `combo-editor-damage` / `combo-editor-knockdown-advantage` / `combo-editor-drive-damage` / `export-open-button` / `game-update-banner` 等が軒並み未登録である。

**⇒ 判断が要る**: **(a) 一覧を実態へ追随させ、機械検査（`scripts/` の型）で維持する (b) 一覧という運用そのものを畳み、規約だけ残す**。**★(a) を人手で維持する運用は既に 1 度破綻している**——**同書の宣言を信じて「一覧に無い = 未付与」と読むと誤る状態が今ある。**

#### 4-D-2 同書「既知のセレクタ・ギャップ」に、**解消済みのギャップが残置**されている

同書は **「数値メタデータ入力（ダメージ / ドライブゲージ / …）に testid 無し → 数値項目を spec で直接操作できない」** をギャップとして記録し、解消方法として **「`combo-editor-{field}`（例 `combo-editor-damage`）を最小付与してこの一覧へ登録する」** を挙げている。

**★その解消方法は既に実施済みである。** `web/src/features/combo/components/ComboEditorBasicFields.tsx:372` / `:535` / `:603` に `combo-editor-damage` / `combo-editor-drive-damage` / `combo-editor-knockdown-advantage` が実装され、E2E の `fillRequiredComboFields`（`web/e2e/support/editor-input.ts:51-57`）が実際に使っている。**⇒ 解消済みのギャップが未解消として残り、一覧への登録だけが漏れた。**

#### 4-D-3 同書「spec 実行前の環境前提」が旧スタックのまま

同書は **「ポート 47318（backend）と 5173（vite）」「`reuseExistingServer: true` のため、起動済みなら再利用」** と書いている。**現行は使い捨てスタックであり、既定は 47390 / 5273 である**（`web/playwright.config.ts:13-14` / `:29`）。

**★近縁の既登録行がある**——`claude-md-e2e-isolation-claim-false-both-ways`（`followup-backlog.md:833`）は `CLAUDE.md` §5 の記述について同種の指摘をしている。**⇒ 同じ束で扱えるかもしれない。**

#### 4-D-4 `.claude/commands/add_e2e_spec.md` のテンプレートが旧ポートを複製する

`add_e2e_spec.md:91` が、新規 spec の冒頭へ貼らせるコメントの雛形として次を持っている:

```
// 前提: ウィザード完了済み DB。バックエンド(:47318) および Vite dev(:5173) が起動していること。
```

**⇒ このコマンドを使うたびに、旧ポート前提のコメントが新しい spec へ複製される。** **★実際に手修正で回避された形跡がある**（`m29-02` 系の spec は 47390 / 5273 で書かれている）。**⇒ 雛形が間違っていることを知っている人だけが直せる状態である。**

#### 4-D-5 `web/e2e/m12-06-presence-detection.spec.ts:17-19` の冒頭コメントが旧環境前提

同 spec は **「バックエンド(:47318)+ Vite dev(:5173)」** と **「Playwright ブラウザ + ライブスタックが必要。製造環境はブラウザ未導入のため未実行（実機は開発者ゲート）」** を冒頭に持つ。**現行はポートが違い、Chromium はプリインストールされ、`make e2e` で実行される。** **⇒ 4-D-4 の雛形から複製された 1 例と見られる。**

---

### 4-E 新規候補: テストの穴（★緑であることの意味が弱い箇所）

#### 4-E-1 タグの `Delete` にだけ利用者スコープのテストが無い

`internal/api/tag/scope_test.go:126` のコメントは **「★確定-a: タグの Update / Delete は既に `WHERE id = ? AND user_id = ?` で絞っている。」** と **両方に言及**している。**★しかし同ファイルにあるのは `TestUpdate_CannotTouchAnotherUsersTag`（`:128`）だけで、Delete のスコープ検証は無い**（同ファイルの `func Test...` は List / Create / Update の 3 本）。

他所の Delete テスト（`handler_test.go:279` `:291` `:304`、`service_test.go:198` `:210` `:234`）は **いずれも単一利用者の観点**であり、他人のタグを消せないことは検証していない。**⇒ コメントが主張する「確定-a」の片肺だけがテストで固定されている。**

#### 4-E-2 コンボのハンドラテストのモックが `userID` を捨てている

`internal/api/combo/handler_test.go:100-102`:

```go
func (m *mockService) Get(ctx context.Context, id, userID int64) (*model.Combo, error) {
	return m.getFn(ctx, id)          // ★userID がここで消える
}
```

**⇒ ハンドラがサービスへ渡す `userID` の値は、ハンドラ層テストでは一切検証されない。** 誤って `0` 固定で渡すバグを検出できない。

**★対照が同じファイルにある。** 直下の `GetDeleted`（`:106-111`）は **「未設定なら `getFn` へ落とさず明示的に失敗させる（Get と取り違えたテストが黙って通るのを防ぐ）」** という設計になっている。**⇒ 同じ配慮を `Get` の `userID` へ及ぼすかどうかの判断が要る。** タグ側は `scope_test.go` が実スタックで担保しているが、**コンボ側に対応するものがあるかは本セッションでは未確認である。**

#### 4-E-3 E2E ヘルパの `isVisible()` が待機しない

`web/e2e/support/editor-input.ts:52`:

```ts
if (!(await damage.isVisible())) {
    await switchEditorTab(page, "basic");
}
```

**Playwright の `isVisible()` は自動待機しない即時判定である。** エディタの描画が遅い環境では「見えていない」と誤判定してタブを余計に切り替えうる。**★このヘルパは本登録で保存する spec がほぼ全部通る場所**であり、フレークの温床になりやすい。**⇒ 待機ベース（`expect(...).toBeVisible()` 系）への置き換えを検討する価値がある。**

#### 4-E-4 `TagSelector.test.tsx` のインデックス依存セレクタ

`web/src/features/tag/components/TagSelector.test.tsx:93-95` が **`allButtons[1]`** という順序依存の要素特定をしている。同ファイルの他のテストは role / ラベルで掴んでおり、**ここだけ流儀が違う。** **⇒ ボタンの並びが変わると静かに別のボタンを押す。** 深刻度は低。

#### 4-E-5 【参考・既知】`web/e2e/` は型検査されない

`web/CLAUDE.md` §3 に記録済みの積み残し。**⇒ 新規行は不要。** 4-D-4 / 4-D-5 のような spec 内の陳腐化が機械で捕まらない背景として挙げておく。

---

### 4-F 新規候補: 低優先（cosmetic / 将来コスト）

| # | 場所 | 内容 | 深刻度 |
|---|---|---|---|
| 4-F-1 | `web/src/features/combo/components/DeleteComboConfirm.tsx:30` `:37` ／ `web/src/features/preset/components/PresetDeleteConfirmDialog.tsx:63` | **削除ボタンが `destructive` トークンではなく生の `bg-red-600` / `text-red-700` を使う。** トークンは `web/src/components/ui/button.tsx:13` に定義済みで、`AlertDialogAction` は `cn(buttonVariants(), className)` を通すため渡す道はある。**★2 箇所で同じイディオムが複製されている。** **★意図的な可能性もある**——`--destructive`（`web/src/index.css:21` ＝ `0 84.2% 60.2%`）は red-500 相当で `bg-red-600` より明るいため、「削除をより強く見せる」ための逸脱かもしれない。**⇒ 機械的な置換は推奨しない。判断が要る。** **★実害の見積もりに 1 点**——**`.dark` ブロックは既に在り**（`index.css:29`）**、`--destructive` のダーク値も定義済みである**（`:44` ＝ `0 62.8% 30.6%`）。**⇒ トークン側は追随の準備ができており、生の直接色だけが取り残される形になる** | 低（将来コスト） |
| 4-F-2 | `web/src/components/ui/alert-dialog.tsx:22` | クラス文字列内の**二重スペース**（`bg-black/80  data-[state=open]`）。対照の `dialog.tsx:23` は 1 個。描画上は無害 | 低 |
| 4-F-3 | `web/src/features/tag/components/TagManagementPage.tsx:53-54` `:61-62` | `values.category ?? undefined` が **no-op**（呼び元の `useTagFormDialog` が既に `\|\| undefined` で正規化済み）。**★`??` は空文字を透過するため、フック側の正規化が将来外れると挙動が静かに変わる** | 低 |
| 4-F-4 | `web/src/features/combo-io/components/ImportCommitConfirmDialog.tsx:54-55` ／ `web/src/features/preset/components/PresetDeleteConfirmDialog.tsx:58` `:66` | **i18n 未適用の日本語直書き**（「キャンセル」「取り込む」「削除中...」「削除」）。**M29-01 の対象外だったのか漏れなのかは未確認** | 低 |
| 4-F-5 | `internal/repository/tag/repository_test.go:121-127` | **テストコメントに未確定の設計判断が埋まっている**——「`usage_count` の表示はゴミ箱除外、削除ガード（`CountUsage`）はゴミ箱込み」という非対称について、**「セマンティクス確定は要判断として開発者に委ねる」と書かれたまま**である。**⇒ 判断待ちがテストコメントの中で滞留している。台帳へ出すか、確定して書き換えるか** | 中（滞留） |

---

## §5 参考（触れていない = 不変の証跡）

- `git diff 50235ce..HEAD --name-only` の全件が `docs/human-notes/go-react-learning-guide/` 配下（10 ファイル）。**コード・設計書・マイグレへの差分は 0。**
- `git diff 50235ce..HEAD --stat -- internal web migrations docs/design` は **空**。
- `ls migrations/` の末尾は `000106`。**着手前から不変。次に払い出す番号は `000107`。**
- `docs/handover/followup-backlog.md` は**未編集**（`D-382` により本表は製造・教材担当が触らない）。
- 本レポート以外に `docs/handover/` へ新設したファイルは無い。

---

## §6 CHANGE 起票のたたき台（設計担当向けチェックリスト）

> 番号は起票時に registry で採番。**マイグレは 1 本も消費していない。**

**★CHANGE が要りそうなのは 2 件だけと見ている。** 残りは台帳登録か、設計書の直接更新で足りる。

| 候補 | 対象 | 理由 |
|---|---|---|
| **(1) タグの部分更新で「未設定へ戻す」を表現できるようにするか** | `DES-002`（API 契約）／ `DES-006`（バリデーション） | **4-B-2。契約の決めごとが要る**（空文字に「消す」を割り当てる／三値表現を入れる／消せない仕様として明文化する）。**★フロントの小修正では直らない。** |
| **(2) タグのエラー詳細 DTO の命名を camelCase へ揃えるか、例外として明文化するか** | `CLAUDE.md` §4 ／ `DES-002` | **4-C-2。★両側同時変更になるため、CHANGE を挟まずに片側だけ直されるのが最悪。** |
| （参考）`docs/design/testid-convention.md` の扱い | `docs/design/` | **4-D-1 〜 4-D-3。CHANGE ではなく設計書の直接更新かもしれないが、「一覧運用を畳むか」は方針判断である。** |

**★台帳（`followup-backlog`）行の新設候補は 4-B-1 / 4-B-2 / 4-B-3 / 4-C-1 / 4-C-2（+4-C-3 を同項に束ねる）/ 4-D-1〜4-D-5 / 4-E-1〜4-E-4 / 4-F-5。** **既存行の補正は 4-A-1 / 4-A-2 / 4-A-3。**

---

## §7 教訓（retrospective 行き）

> **★親は本節を読まなくてよい。** 宛先は `retrospective-log` へのバッチ反映で、実施者は設計担当。

### 7-1 「値の意味を反転させる変更」では、宣言だけでは記述の失効を防げない。★語で grep する手順が要る

**`M27-03` のコミットメッセージは「★失効した記述を残さない」と明示的に宣言していた。** **それでも Go のコメント 2 件が反転前のまま残った**（4-C-1）。**★宣言した当人の手番で漏れている**ことが要点であり、注意力の問題ではない。

**一般形**: **意味を反転・再定義する変更では、「新しい語」ではなく「古い語」で全文検索する破壊確認を手順に入れる。** 本件なら `削り量` `回復で負値` で grep すれば 2 件とも出た（実際、本セッションはそれで見つけた）。**新しい語で探すと、直した箇所しか出てこない。**

### 7-2 「両側が揃った規約違反」は、規約を守ろうとした人が壊す

**4-C-2（タグのエラー詳細が両側 snake_case）は、今どこも壊れていない。** **だから残っている。** **★危険なのは、`CLAUDE.md` §4 を読んだ誰かが片側だけ直す瞬間である**——型検査もテストも通り、「使用件数が出ない」形で静かに壊れる。

**一般形**: **規約から外れた箇所は、外れたまま整合していると検出されない。** **⇒ 規約に例外を作るなら、例外であることをその場に書く**（`CLAUDE.md` の規約本文と、実装の両方に）。**書かない例外は、後から来た人が「バグだ」と判断して直す。**

### 7-3 「一元管理する」と宣言した一覧は、機械検査を伴わないと必ず腐る

**`testid-convention.md` は「新規付与時は必ずこの一覧を更新すること」と宣言していたが、実測 430 対 21 まで開いた**（4-D-1）。**★しかも同書は自分が挙げたギャップの解消にも気づいていない**（4-D-2）。

**一般形**: **本リポジトリには既に「ベースライン固定型の機械検査」という型がある**（`check-import-order.sh` / `check-browser-storage-keys.sh` / `check-enum-sync.sh`）。**⇒ 一覧を維持したいなら同じ型へ乗せる。乗せられないなら、一覧をやめて規約だけ残すほうが正直である。** **「一覧に無い = 未付与」と読める状態を放置するのが最も害が大きい。**

### 7-4 陳腐化した雛形は、使うたびに陳腐化を複製する

**`add_e2e_spec.md:91` の雛形が旧ポートを持つため、新規 spec が旧前提のコメントを持って生まれる**（4-D-4 → 4-D-5 がその実例）。**★雛形が間違っていることを知っている人だけが手修正で回避できる**という、最も再現性の低い状態になっている。

**一般形**: **自動化・雛形・スニペットは「1 回書けば終わり」ではなく、参照している事実（ポート・パス・コマンド）が動いたら追随が要る資産である。** **⇒ 環境の前提が変わる変更（本件なら E2E スタックの独立化）では、その前提を埋め込んでいる雛形を洗い出す手順を入れる。**

### 7-5 ★教材の実態追随は、副産物として実装監査になる

**本セッションは「ガイドの行番号を直す」ためにコードを全数読み合わせた。** **その結果、18 件の是正候補が出た**——**うち 1 件は既存 followup の前提が狭いことの発見**（4-A-1）**であり、通常のレビュー工程では出にくい種類である**（レビューは差分を見るが、教材の突合は**差分の無い箇所も含めて既存記述と実物を突き合わせる**ため）。

**一般形**: **説明資料と実装の突合には、レビューとは異なる検出力がある。** **⇒ 教材・設計書の実態追随を「ドキュメント作業」として後回しにすると、この検出機会も一緒に失われる。** ただし **§0-3 のとおり網羅性は無い**ので、監査の代替にはならない。**位置づけは「別の網」である。**

---

*以上*
