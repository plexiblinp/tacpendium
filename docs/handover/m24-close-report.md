# M24 クローズ報告（UX 整理・i18n・繰越・リファクタ ＋ 蓄積した改善要望）

| 項目 | 内容 |
|------|------|
| 文書ID | M24-CLOSE-REPORT |
| バージョン | **1.1.0**（2026-08-31）<br>**★本セルは現行版と日付だけを持つ。変更の内容は §改訂履歴へ書く** |
| 発行 | 設計担当 Claude（親チャット・設計卓）/ 2026-08-31 |
| 対象 | **M24 マイルストーン全体**（`M24-RESEARCH-01` ／ `M24-01`〜`M24-08` ／ `M24-09a`〜`09d` ／ `M24-11` ／ `M24-12` ／ `M24-13`。**サブ 17 本**。**`M24-10` は欠番＝`D-609`**） |
| 判定 | **★クローズする。** `M24-overview` §6「完了の観点」**13 項目のうち 10 項目が達成 ／ 2 項目が開発者判断で未達のまま出荷 ／ 1 項目が開発者の手番として残る**（§1） |
| 配置 | `docs/handover/m24-close-report.md`（**M19 / M22 / M23 と同じ直下運用**。`check-doc-inventory` の型に合致） |
| 一次源 | `docs/process/parallel-board.md` §1.8・§3.4・§6 ／ `docs/handover/change-number-registry.md` §1 ／ `docs/progress/M24-RESEARCH-01-report.md` ／ `docs/handover/design-reports/` ／ `docs/change-notes/change-report-1XX.md` |
| 併読 | **`docs/handover/phase3-to-phase4-handover.md`**（**フェーズ3 の締め。本書と同時に発行した＝`D-626`**） |

## 改訂履歴

| 版 | 更新日 | 更新内容 |
|----|--------|---------|
| **1.1.0** | **2026-08-31** | **★`M24-08` の手交後追記 1 件を反映した**（**D-629**）。**実機確認 6 が消化され、`web test (Vitest)` の summary が件数を 1 度も記録できていなかったことが分かった。** **⇒ §1 の観点 13 ／ §3-3 の 1 ／ §6 を更新。`DES-002` は 1.73.0（第71版）へ。** |
| 1.0.0 | 2026-08-31 | 初版（`D-627`） |

---

## §0. 結論

**M24 をクローズする。**

**★★本 MS の性格は「未完を片づける」だった**（`M24-overview` §1.2）。**⇒ 成果は新機能ではなく「行き先の無い項目が 0 件になったこと」で測る。**

| 指標 | 値 |
|---|---:|
| サブ本数 | **17 本**（`RESEARCH` 1 ／ 実装 15 ／ `CLOSE` 1。**`M24-10` は欠番**） |
| 完了・受理 | **17 / 17** |
| 消費した CHANGE | **17 本**（`129`〜`140` ／ `144`〜`148`。**★`141`〜`143` は M23 追加ブロック ／ `149` は未消費のまま残す**） |
| 消費したマイグレ | **2 本**（`000079`＝`M24-02` ／ `000080`＝`M24-08`。**どちらも DML**。**⇒ 次に払い出すのは `000081`**） |
| スキーマ変更 | **0 件**（`M24-overview` §1.4-4 の見込みどおり） |
| 新規依存 | **0 件** |
| 計測点（設計卓の誤り） | **`M-89`〜`M-105` の 17 件**（**★M23 期の 6 件から大幅に増えた。理由は §4**） |
| 裁定 | **`D-549`〜`D-627`** |

**★★本 MS の最大の成果は 3 つ。** **(1) 二系統 ledger（160 行 ＋ 26 項目）に行き先が付いた ／ (2) `queryKey` の正本が 1 か所に定まった**〔`CO-009`。96 サイト / 44 ファイル ＋ 検査 2 本〕**／ (3) 「守るべきものはあるが観測が無い」空白を 3 つ見つけ、うち 2 つを塞いだ**〔CSV 列名の契約 ／ `gofmt`。**残る 1 つは import 順**〕。 **★★手交後に 4 つ目が見つかった**——**`web test (Vitest)` の summary が件数を 1 度も記録できていなかった**（`D-629`）。**⇒ 塞いだのは 3 つになった。★見つけたのは機械ではなく、開発者が手動起動の結果を人の目で読んだときである。**

---

## §1. 完了の観点（`M24-overview` §6）の突合

| # | 観点 | 判定 | 根拠 |
|---|---|---|---|
| 1 | 系統 1 の全 160 行に `disposition` があり、`実装` 60 件が実装済みか理由が書かれている | **★達成（サブ単位）** | **`disposition` 未付与は着手時点で 0 件**（`M24-RESEARCH-01` §2.1）。**`実装` 60 件は 15 本の実装サブへ割り付けられ、全サブが完了・受理した。** **★★行単位の突合は行っていない**——**設計卓は武装しており実装ソースを読まない。** **確かめたのは「割り付けたサブがすべて完了・受理し、各サブの完了報告とレビューが射程の充足を主張していること」までである**（§3 の注記） |
| 2 | 系統 2 の全 26 項目が同じ判定を満たす | **★一部未達** | **`CO-009` は達成**（`M24-08`）。**★`CO-026`（英語ロケール整備）は未達のまま出荷する**（開発者判断・§2）。**★`CO-023`（`G-15`）は閉じきっていない**（followup `co023-g15-not-fully-closed`） |
| 3 | 両 ledger が揃ってクローズしている | **★達成** | 上記 1・2 の判定を両方に対して行った。**片方だけで閉じていない** |
| 4 | `要判断` 36 件のすべてに行き先がある。**とくに割付先が消えている 4 件** | **★★達成（本書で 4 件の行き先を確定した）** | **§3-1** |
| 5′ | `却下` 4 件が 1 件ずつ棚卸しされている（`D-482`） | **★達成** | **`D-542`（棚卸しの実施） ／ `D-544`（4 件の確定）。** **★一括の「4 件とも却下」で閉じていない** |
| 5 | `返却` 52 件の返却先が実在し、そこで追跡されている | **★達成** | 返却先 6 種の実在は `M24-RESEARCH-01` §2.1 が確認済み。**★本書 §3-2 で「フェーズ4 返却 30 件」の受け皿を `phase3-to-phase4-handover.md` §3 として実体化した** |
| 6 | `queryKey` の形が 1 つに揃っている（`CO-009`） | **★★達成** | `M24-08`。**正本＝`web/src/lib/query-keys.ts` の `queryKeys` ファクトリ。96 サイト / 44 ファイル ＋ 検査 2 本。** **★観点の但し書き「実測 84 箇所」も失効していた**——**実測は 92 / 43**（計測点 `M-105`） |
| 7 | `ja.json` / `en.json` の leaf キー数が一致し、英語の訳文が実データとして入っている | **★★未達のまま出荷する（開発者判断）** | **キー数は一致している**（`M24-07` 完了時 794 / 30）。**★訳文は未完成である**——**開発者の逐語＝「全画面を見た訳ではないですが、日本語が残っている部分もありました。が、まずはリリースなのでいったんはこれぐらいでいいかと思っています。」** **⇒ `CO-026` は Phase4 へ返す** |
| 8 | CHANGE の三点セットが全サブぶん着地している | **★達成** | 17 本すべてが「反映済み」。**★`change-number-registry` §1 が正本。★`CHANGE-149` は未消費のまま残る**（§5） |
| 9 | E2E が非回帰（`make e2e`） | **★達成** | `M24-08` の実測＝**226 passed × 3 回**（第 1 部完了時 ／ 全体完了時 ／ 取り込み後。**いずれも絞っていない**）。**★`M24-08` はリファクタであり、非回帰そのものが成果物である** |
| 10 | 開発者の実機確認（`M24-01`〜`M24-07` 各 1 回 ＋ `CO-016`） | **★ほぼ達成** | **`M24-01`〜`M24-08` の各サブで実施済み**（`M24-08` は 6 件）。**★`CO-016`（クロスビルド mac・Linux 実機起動）の消化は本書で確認できていない**——**設計卓が武装しており、また実機は開発者の手番である。⇒ §3-3 へ回す** |
| 11 | `Memo_Someday.txt` と ledger の関係が決まっている | **★達成** | **`D-473`**（2026-08-20・暫定案どおり確定） |
| 12 | フェーズ3 の繰越が畳まれている（followup §B / §E / §F の M24 割付行） | **★達成** | **`CO-001`〜`CO-026` として ledger 化され、§2 の判定に含まれている**（`M24-RESEARCH-01` §3.1 が §B / §E / §F / §G / §K を出所として明示） |
| 13 | 止めていた PR 検査が戻っている（`D-503`） | **★★開発者の手番として残る** | **2026-08-20 に PR の検査を必須から外した。** **★★2026-08-31 に開発者が `PR checks` を手動起動し、3 job の緑を確認した**（実機確認 6 の消化）。**⇒ 検査そのものは動いている。残るのは「必須 check として再登録する」ことだけである。** **★★この手動起動が 1 件を掘り当てた**——**`web test (Vitest)` の summary が件数を 1 度も記録できていなかった**〔`M24-09a` の新設以来〕。**製造が是正し、`DES-002` §11.3 へ契約 8 を新設した**（`D-629`）。 **★必須 check の登録はリポジトリの設定に属し、設計書には書けない**（`DES-002` §11.3） |

**★達成 10 ／ 開発者判断で未達 2**〔観点 2 の一部・観点 7〕**／ 開発者の手番 1**〔観点 13〕**。⇒ クローズを止める項目は無い。**

---

## §2. ★「未達のまま出荷する」と決めたもの（**否定形の記録**）

**★★これは失敗の記録ではない。判断の記録である。** **書かないと、次の担当が「落ちた」と読んで拾い直す。**

| # | 何を | 誰が決めたか | 逐語・根拠 |
|---|---|---|---|
| 1 | **英語 UI を未完成のままリリースする**（`CO-026`） | 開発者（2026-08-30・実機確認後） | **「全画面を見た訳ではないですが、日本語が残っている部分もありました。が、まずはリリースなのでいったんはこれぐらいでいいかと思っています。」** **★実機で確認された面＝`/setups/:id`**〔18 語〕**／ `/punish/search`**〔`CharacterSelector` のプルダウン内〕**／ プリセット編集・確定反撃・マイリスト**〔全面が直書き日本語〕。**★どれも M24 が作った回帰ではない**——**混在は元から在って M24 が深めた** |
| 2 | **`SM-016`（コンボの 1 工程を指す単位語）の統一を見送る** | 開発者（2026-08-30・モック提示後） | **「想定よりも影響箇所が多く、手動確認の手間が大きいため、今回は対応不要とします」** **★★技名「前方ステップ」**〔`D-615` で正と確定〕**と単位語「ステップ」の衝突は残った。** **★`D-616` が定めた「衝突を解くなら動かすのは単位語の側」という論点は未決である** |
| 3 | **`DES-005` §5.16 の「中断耐性あり」を撤回する** | 設計卓（`D-620`） | **再構築は冪等であり、途中で閉じてももう一度押せば足りる。** **⇒ 中断からの再開を覚えておく機構を別に持つ意味が無い。★followup も立てない**——**「やらないと決めた」ものを置くと、次の担当が未着手の宿題と読む** |
| 4 | **一括バーに「紐付け解除の導線」を置かない** | 開発者（2026-08-31） | **行部品と同じ解除 UI を一括バーへ置くと重複するため共通化のリファクタを伴う。** **利用者は理由を読んで行の「完全削除」へ移り、そこで解除する**〔`M23-07` が作った導線〕 |
| 5 | **`queryKey` の 3 箇所を平坦化しない** | 設計卓（`D-620`） | **平坦化すると matching の意味が変わり、前方一致・部分一致に依存して当たっていた invalidate が静かに外れる。** **★理由は正本ファイルの冒頭へ逐語で残してある**——**残さないと次の担当が「統一漏れ」と読んで潰す** |

---

## §3. 引き渡す宿題

### 3-1. ★★`要判断` のうち「割付先が消えていた」4 件——**本書で行き先を確定した**（`D-627`）

**★`M24-overview` §6 観点 4 が名指しした 4 件である。** **⇒ 「行き先がある」ことを本書で満たす。**

| ID | 内容 | **確定した行き先** | 理由 |
|---|---|---|---|
| **`SM-051`** | キャラクターが増えた時に簡単に更新できるよう importer 側を見直す | **★`M14` レーン**（`M14-RESEARCH-03` の入力） | **残り 14 キャラの seed 波は `M14` の所管である**（`D-610`）。**★本件はまさに「seed 波 1 回あたりの工程」の話であり、`M14-RESEARCH-03` が数えようとしているものと同じである** |
| **`SM-080`** | 仮想コントローラのボタン名見直し | **★Phase4**（仮想コントローラの面） | **共通部品 `HitBoxLayout.tsx` を触る。`CO-020`（同部品の i18n 未対応）・`SM-149` と同クラスタであり、1 面としてまとめて扱うほうが安い** |
| **`SM-098`** | 確定反撃での D リバ（リバーサル）の扱い | **★Phase4 の要件整理**（**★開発者への確認事項として名指しする**） | **★M18 は 2026-07-31 にクローズしたが本件は扱われておらず、M18 の指示書・完了報告のいずれにも「リバーサル」の記載がゼロである**（`M24-RESEARCH-01` §⑤）。**⇒ 実装の宿題ではなく「そもそも何を扱うか」が未定である。開発者のドメイン知識が要る** |
| **`SM-149`** | 仮想コントローラの移動ボタンと技ボタンが縦並びで直感的でない | **★Phase4**（`SM-080` と同一クラスタ） | followup `virtual-controller-key-layout-not-like-real-pad` として登録済み。**同じ共通部品を触る** |

### 3-2. `返却（フェーズ4）` 30 件の受け皿

**★受け皿を `phase3-to-phase4-handover.md` §3 として実体化した。** **⇒ 「返した」だけで受け皿が無い項目は 0 件である**（観点 5 の否定形確認）。

### 3-3. 開発者の手番

| # | 何を |
|---|---|
| 1 | **PR 検査を必須へ戻す**（観点 13）。**★★3 job の緑は 2026-08-31 の手動起動で確認済みである。残るのは必須 check としての再登録だけ。** **★再登録の後、`web test (Vitest)` の summary に件数が入っていることを 1 度だけ見ること**——**同欄は `M24-09a` の新設以来ずっと空だった**（`D-629`） |
| 2 | **`CO-016`（クロスビルド mac・Linux 実機起動）の消化確認**（観点 10）。**★実機は開発者の手番であり、設計卓は状態を確認できない** |
| 3 | **`docs/progress/` に滞留している資料の仕分け**（`D-196` 境界条件 3＝「消す」変更は開発者の手番） |
| 4 | **`code-facts.md` の再生成**（`check-derived-docs.sh` の変化量 173 / 1072 件＝16%）。**★設計卓は武装中で再生成できない。★Phase4 の設計卓が引くので、Phase4 に入る前に直っている意味が大きい** |
| 5 | **アプリ正式名称の商標クリアランス**（`D-474`＝`Tacpendium` を推す。**法的な確認は未**）。**★`SM-053` / `SM-157` の残り** |
| 6 | **★★`docs/progress/progress-summary.md` が 2 MS ぶん遅れている**（**最終節が `M22`・2026-08-18**）。**`M23` と `M24` の節が無い。** **★これは効く**——**`CLAUDE.md` §1 が「現在地は本ファイルに書かない。`progress-summary` を参照すること」と定めており、次のセッションはここを見て現在地を判断する。** **⇒ `/update_progress_summary` を回すこと。★設計卓は `progress-log` を読まない規律のため代行できない** |

### 3-4. ★次の設計卓へ

**`phase3-to-phase4-handover.md` §5 を読むこと。** 本書は M24 の締めであり、フェーズの引き継ぎは同書が持つ。

---

## §4. ★★設計卓の誤りの記録（**計測点 17 件・M23 期の約 3 倍**）

**`M-89`〜`M-105`。★増えた理由を、増えたこと自体より重く見るべきである。**

| 型 | 件数 | 代表 |
|---|---:|---|
| **母数・件数の失効** | **5** | `M-95`（実査件数）／`M-105`（M24-08 の 5 件）ほか。**⇒ playbook §4.59 を新設** |
| **正本の圧縮による意味の反転** | **2** | `M-93` / `M-103`。**同じ型が 2 度出た。⇒ playbook §4.57** |
| **影響設計書欄の漏れ** | **2** | `M-94` / `M-99`。**★`M-99` は「E2E は CI に載せない」という明示的な禁止契約を起票時に 1 度も参照しなかった。⇒ playbook §4.51（契約台帳）** |
| **前提の実在を確かめずに書いた** | **1** | **`M-104`。★最も重い。**「既存の再構築機構に任せる」と書いたが、その機構は `disabled` のプレースホルダで API すら無かった。**設計書の記述を実在の証拠として扱った＝設計卓の規則 1 違反。⇒ playbook §4.58** |
| **指示書内部の矛盾・節番号の誤り等** | 7 | `M-96` / `M-97` / `M-98` / `M-100` / `M-101` / `M-102` ほか |

**★★増えた理由は「品質が落ちたから」ではないと見ている。** **M24 は 17 本のサブを 1 か月で回した MS であり、母数が大きい。** **★それ以上に効いているのは、設計伝達レポートの §2（契約違反の独自判断）が定着し、製造が「指示書と違う」を隠さずに上げてくるようになったことである**——**`M24-05` / `M24-07` / `M24-08` の契約違反はいずれも受理され、うち複数は設計卓の誤りが原因だった。** **⇒ 計測点が増えたのは、見えるようになったからである。**

**★ただし `M-104` だけは性格が違う。** **これは母数の問題でも伝達の問題でもなく、設計卓が自分の規則を破った件である。** **⇒ playbook §4.58 は「実査項目を立てよ」と書いたが、本当の是正は「設計書は実在の証拠にならない」を身体で覚えることである。**

---

## §5. ★採番の締め

| 対象 | 状態 |
|---|---|
| **CHANGE** | **M24 本体ブロック `129`〜`140`（12 本）＝全消費・全反映。追加ブロック `144`〜`149`（6 本）＝`144`〜`148` の 5 本を消費・全反映。** **★★`149` は未消費のまま残す**——**M24 に使い道が無かった。★Phase4 は新ブロック `150`〜 を切ること**（`D-626`）。**`149` を Phase4 の先頭に流用しない**〔ブロックの境界が消え、どの MS の番号か辿れなくなる〕 |
| **マイグレ** | **`000079`（`M24-02`・キャラ表示名の是正） ／ `000080`（`M24-08`・地上ダッシュの表示語の是正）。どちらも DML。** **⇒ 次に払い出すのは `000081`** |
| **裁定** | **`D-549`〜`D-627`。⇒ 次は `D-628`** |
| **計測点** | **`M-89`〜`M-105`。⇒ 次は `M-106`** |

---

## §6. ★正の記録（**うまく働いたもの**）

| # | 何が |
|---|---|
| 1 | **★★設計伝達レポートの §2（契約違反の独自判断）。** **製造が「指示書の明文と違う」を理由の妥当性で §3 へ格下げせず、§2 に置き続けた。** **⇒ 設計卓の誤りが 17 件見えた。見えなければ playbook §4.51 / §4.57 / §4.58 は生まれていない** |
| 2 | **★破壊確認。** **`M24-07` §5.3-3 の破壊確認が `DES-002` §7.6 の契約に観測が無いことを暴いた**〔往復テストは両側が同じ定数を読むため列名を変えても緑〕。**★`M24-08` でも「観測を足したら、何を壊せば赤くなるかを 1 本ずつ言えるか確かめる」形が効いた** |
| 3 | **★実機確認を手交の前に挟む形**（`M24-06` の `e881e2c` が作った型）。**`M24-07` / `M24-08` で 2 度続けて効いた。** **★`M24-08` の「既存 DB の古い表記が再構築で直る」は、E2E も Go テストも構造上そこへ到達できず、実機確認でしか確かめられなかった** |
| 4 | **★指示書 §9 の分岐**〔前提が成り立たなければ手を止めて報告する〕。**`M24-08` を救った。分岐が無ければ製造は詰むか、勝手に作っていた**（§4 の `M-104`） |
| 5 | **★機械検査の増設。** `check-instruction-format.sh` へ検査 (3)〔チェックリストの版ずれ〕を追加。`check-doc-inventory.sh` の型を 15 → 18 件へ。**★「規約はあるが観測が無い」空白を 3 つ見つけ、2 つを塞いだ** |

---

*以上、M24 クローズ報告。* **★★フェーズ3 の締めは `docs/handover/phase3-to-phase4-handover.md` が持つ。本書と併読すること。**
