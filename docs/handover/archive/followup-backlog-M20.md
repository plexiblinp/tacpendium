# followup-backlog アーカイブ（M20 境界）

> 解決済みの記録を退避したもの。**現役の追跡は `docs/handover/followup-backlog.md` 側**。退避の経緯は `scripts/archive-resolved.sh` の冒頭コメントを参照。

<!-- 退避: M20 境界 -->

### 退避元: A. friend FB（2026-06-28・先行リリース初日・living）

| # | 要旨 | 種別 | 割付 | 状態 | メモ |
|---|---|---|---|---|---|
| ⑰ | 入力項目が多い→上級者モードで隠す・モード選択 | UX/新機能（モード） | ~~M15-06~~→**M15-05 で実現** | **完了** | **上級者モードの目的（項目を隠す）は M15-05 の登録画面セクション折り畳み（既定全展開・任意で畳む）で達成（2026-07-04）。別建ての上級者モードは作らない（M15-06 から除外）** |

### 退避元: B. §F12 繰越のフェーズ3 再配置（phase2 §F12 継承）

| 旧ID | 内容 | 割付 | 状態 | タグ |
|---|---|---|---|---|
| F12-4 | メタデータ入力の安定 test-id 整備（damage/drive/sa/knockdown/memo/起き攻め6＋native select 4） | **M15-01** | **完了**（2026-07-02） | 規約 `combo-editor-<field>`（M15-overview §4.7.1）。既存 spec は併設・未移行 |
| F12-7 | データ保護・エクスポート・破壊的 FS 操作監査 | M13（export・FS 監査） | **完了** | M13-01 export（意味単位）+ M13-RESEARCH-02 FS 監査クリーン（16 件全て管轄内・ユーザ入力パス流入 0・`os.RemoveAll` 不使用・DB DROP なし） |

### 退避元: C. M14（公式データ配布是正・スキーマ整理）の新規論点

| ID | 内容 | 状態 | タグ |
|---|---|---|---|
| M14-01 | moves 観測不能6列削除＋recovery 追加＋raw_data 退避5キー除去 | **完了**（実装/レビュー/E2E 済・CHANGE-054 反映。CHANGE-022 の recovery 非保持を反転・意味単位 export 往復不変を実証） | `[完了]` |
| M14-02 | 取込パイプライン段階削除（共有シンボル中立移設→movesimport 削除→画面17 除去・FR704 降格） | **完了**（実装/レビュー/E2E 済・CHANGE-055 反映。REQ-001 FR704 降格・movewarning 中立移設で逆依存解消・取込専用 repository メソッド/死蔵3ラベル除去） | `[完了]` |
| M14-02-F1 | `service/movesimport/parse.go` の `validateComboScaling`/`comboScalingKeys` dead code | **解消**（M14-02 で movesimport ごと削除） | `[完了]` 伝達メモ §3-F1 |
| M14-02-F2 | FE `import/types.ts` の `comboScaling`（＋画面17 プレビュー欄）と backend `previewRowDTO` の契約ずれ | **解消**（M14-02 で画面17・features/import 削除） | `[完了]` 伝達メモ §3-F2 |
| M14-02-F3 | 取込が退避キーを再生成する一時状態（取込実行禁止ガードレール） | **解消**（M14-02 で取込削除＝ガードレール解除） | `[完了]` 伝達メモ §2-D/§3-F3 |
| M14-03a-e2e | 画面18 E2E（`moves-edit.spec.ts`）を ryu 対象・import 非依存で再有効化 | **完了**（実装/レビュー済・2026-07-01。id 昇順先頭を決め打ち＝冪等〔rush 増殖回避〕。承認 plan の「rush 未存在フィルタ」から意図的逸脱・レビュー妥当評価済み） | `[完了]` M14-03a 報告 §3 |
| **配布完了の定義（恒久記録）** | **クラシック全 30（/31）キャラの moves が新スキーマで seed 済み ＝ 配布可能**。M14-03b が満たすまで**配布はブロック**（M14-01/02 のコード完了と配布可能は別概念）。M14 を「コード完了・配布データはローリング」と表記する根拠。忘却防止のため恒久記録 | 恒久記録（M14-03b 完了で充足） | phase3-overview v1.0.0 M14 § |
| **M14-03d（第二波＝manon）** | **完了**（2026-07-31・マイグレ **000043〜000048**）。moves 72＋移動 9＝**81**・alias 81（欠落 0）・`is_derived` 27・`move_commands` 45・移動 5 code の `total` backfill 済み。**§4.3 の判定＝【解あり】AI 補正で埋まっている**（正準形違反 0／アクセント破損 0／過長 code 0／dup 0）⇒ **第三波はアクセント由来の品質問題なしで進められる**。**次の空き連番は 000049** | **完了** | `docs/progress/m14-03d-completion-report.md` |

### 退避元: D. M13（データ共有）先行成果物の統合可否

| ID | 内容 | 状態 |
|---|---|---|
| M13-a | `combo-csv-import` の FR405 適合・統合可否 | **完了**（M13-RESEARCH-01：部分カバー→契約準拠統合） |
| M13-b | `combo-export-csv` の FR401 適合・統合可否 | **完了**（M13-RESEARCH-01。実在名は `combo-export-csv`） |
| M13-c | `combo-export-image` の FR402/403 適合・統合可否 | **完了**（M13-RESEARCH-01。実在名は `combo-export-image`。比較表/setups なし・ラスタ PDF→M13-02 で補完） |
| M13-d | FR403（PNG）/ FR404（クリップボード）の充足/欠落の特定 | **完了**（FR404 はゼロ実装＝M13-02 で新規実装） |
| M13-e | 破壊的 FS 操作の全数監査（F12-7） | **完了**（M13-RESEARCH-02：クリーン。管轄内・ユーザ入力パス流入 0・DB DROP なし） |
| M13-f | FR402/403/404（PDF/PNG/クリップボード）= **M13-02 で実装完了**。比較表既定 §5.6∪§5.8 superset・状況行に size 畳み・setups「名称: フルレシピ」 | **完了**（DES-005 §5.13 明確化＝**CHANGE-052 反映済み**） |
| M13-h | 導入依存 `html-to-image` / `pdf-lib` のライセンス確認 → **node_modules 実体で MIT 確認済**（M13-02） | 完了 |

### 退避元: H. M19（セットプレイ自動提案）課題記録（M19-01 由来・2026-07-24 中央登録）

| ID | 内容 | 状態 |
|---|---|---|
| ~~**M19-rush-variant-origin**（e）~~ | **【2026-07-26 クローズ】`rush_variant` 通し値の起点確認**。**開発者回答＝「ニュートラル起点で、ラッシュを最速キャンセルして技を出したときの**ラッシュ分＋技分**」**（M19 リレー・2026-07-26）。起き攻めはニュートラルから入るため**前提が一致**し、`rush_variant` の `startup`／`total`（元技 +11・実測値）を **filler・target の双方でそのまま使える**。**帰結**＝(1) rush を filler から除外しない判断が裏付けられた（CHANGE-086 §3.6）、(2) M19-01 完了報告 §3.7-1 の残論点も解決、(3) **`rush_variant.startup` は通し値＝親のラッシュ分を含む合算値**であることが確定し、**中央帰属の `startup_basis`（standalone／through／unknown）設計の一次源**になった | **クローズ済** |
| ~~**guile-move-code-and-command-corrections**（o）~~ | **【2026-08-03 クローズ】M19-04c（マイグレ `000063`）で是正済み。** (a) `sonic_break_light` → **`sonic_break`**。(b) OD ソニッククロス系 3 行の `command` 是正 ＋ **改名 2 件**（`sonic_cross_2_meter_od` → `perfect_timing_sonic_cross_od`／`sonic_cross_3_meter_od` → `sonic_cross_2_meter_od`）＋ 旧 3_meter_od のフレームを通し値 **15/46/11/71** へ是正し **`startup_basis='through'`** を確定。**M19-05 の着手前ゲートは解消**。フレーム値 6 行（kimberly／lily×2／mai×2／manon）も同サブで是正。**★Phase 2（人手判断の転記）は `move_code` 単独で突き合わせてはならない**——改名後の `sonic_cross_2_meter_od` は記入用コピーでは別の技を指し、`standalone` が `through` の行を上書きする（エラーにならない）。対応表は `docs/progress/m19-04c-completion-report.md` §7。<br>**〔起票時（2026-08-02・CHANGE-091／M19-04 由来・ボード D-142／保留 P-17）の記述〕** guile のデータ是正 2 件。(a) `sonic_break_light` の `move_code` に `_light` が付いているのは誤り（**強度の区別が無い技**）。(b) `sonic_cross_2_meter_od` と `sonic_cross_3_meter_od` の **`command` が入れ替わっており**、かつ **`sonic_cross_3_meter_od` は `through` のフレームであるべきところに `standalone` のフレームが入っている**。**★唯一の記録は人手判断の記入コメントである**（他に一次源が無い）。**★`startup_basis` の誤りは M19-05 の前提に直結するため、M19-05 着手前に解消が要る**（＝本サブで解消） | ~~未着手・優先度「高」~~ → **クローズ済**（2026-08-03・M19-04c） |
| **m19-03-instruction-v1-wording** | **【2026-08-09 登録・D-274 由来】`M19-03` 指示書の 2 か所に撤回マーカー無しの v1 文言が残っている**（画面名による禁止の形）。**完了済みサブの指示書は本文を書き換えず、撤回マーカーを 1 行置く** | **クローズ**（2026-08-10・`M19-07-COMPLETION-RULINGS` §8-2 の AP-02 (i) 適用で解消） |

### 退避元: I. M18（確定反撃記録）期の新規論点（2026-07-26 追加・中央登録／M18-03a 由来）

| ID | 内容 | 状態 |
|---|---|---|
| **combo-punish-carryover-on-edit**（b） | **コンボ編集（PUT）で確定反撃の採用が黙って消える**。識別キー変更を伴う編集は旧コンボを `deleted_at` で論理削除して新コンボを作るが、**`combo_punishes`／`combo_punish_curations` を新コンボへ引き継ぐコードが存在しない**（引き継ぎロジック 0 件・M18-03a 実測）。**ゴミ箱経路より編集経路の方が頻度が高く実害が大きい**。**処遇＝M18-03b で併せて決める**（03b は materialize で `combos` の INSERT と `combo_punishes` を同一トランザクションで扱うため、**採用の引き継ぎ規則を設計する自然な場所**。03b の設計骨子 §8 へ申し送り済み）。**【2026-07-30 クローズ】M18-03b で実装済**（`combo_punishes`／`combo_punish_curations` の `combo_id` を旧→新へ **FK 再ポイント**・同一 Tx・原子的）。**DES 反映済＝DES-003 §3.15**（CHANGE-089） | **クローズ済** |
| **punish-manual-registration-not-wired**（d） | **手動確認レーンの「登録」ボタンが `combo_punishes` を作らない**（DES-005 §5.20 既知の限界 2）。`/combos/new` へ遷移するが `punishContext` は**保存後のトースト文言にしか使われず**、採用を作らない。**表示側（`registeredCombos`）は M18-03a で実装済みだが、そこへ至るデータを UI から作れない**ため到達不能。**「登録」と名乗るボタンが登録しない**状態。**処遇＝M18-03c で実装**（保存成功後に `POST /api/combo-punishes` を呼ぶ）。**暫定の文言変更は行わない**（開発者再判断 2026-07-26＝1 サブ分長く残ることを**許容**）。**【2026-07-30】M18-03c で実装完了**（項目 3＝保存成功後に `POST /api/combo-punishes` を呼ぶ＋ボタン文言の整備）。**DES 反映待ち＝CHANGE-090 の三点セット**（DES-005 §5.20 既知の限界 2 を「**解消済み**」へ）。**反映時にクローズ** | **クローズ済**（CHANGE-090 反映＝2026-07-31。DES-005 §5.20 既知の限界 2 を「解消済み」へ更新） |
| **punish-hide-normal-after-materialize**（e） | **materialize 済みのノーマル版が探す画面の候補に残る**（開発者要望 2026-07-27）。**M18-03c で設計**。**論点＝「silent に消さない」思想との緊張**——探す画面は M18-02 以来「除外は消さず理由バッジ付きで別レーンに出す」を貫いており、候補を無言で消すと一貫性が崩れる。**隠す範囲は相手技コンテキスト単位が正**（materialize は `(基底, 相手技)` の組で行われるため、相手技 A の PC 版を作っても**相手技 B の候補としては基底はまだ有効**。`EXISTS(Y.materialized_from = X.id)` だけで判定すると**相手技を問わず全部隠れ過剰除外**）。PC 版削除時は EXISTS 判定なら**自動復活**するが「なぜ突然出てきたか」が分からないため**隠す側にも理由バッジ**を検討。他画面（マイコンボ・一覧・比較）では**残す**。**`counter` 始動版から作った PC 版も同様に隠す**のが一貫。**【2026-07-30】M18-03c で実装完了**（項目 2＝**単純除外ではなく「畳む」**＝「変換済み (N 件)」の折りたたみ＋「PC 版を作成済み」バッジ・判定は **`EXISTS(active Y: Y.materialized_from = X.id)`＝相手技を問わない**・**`is_draft` は問わない**）。**DES 反映待ち＝CHANGE-090 の三点セット**。**反映時にクローズ** | **クローズ済**（CHANGE-090 反映＝2026-07-31。DES-005 §5.20 に「変換済みノーマル版の畳み込み」を記載し、§5.21 の過剰除外懸念は**撤回済みの見立て**として明記） |
| **materialize-target-combo-damage**（f） | **`target_combo` が始動技のとき materialize のダメージ加算が過剰になる可能性**（M18-03b 一次受け §3-2）。`target_combo` の `damage` は**連携全体の累積値**であるため、materialize の式「**始動技ダメージ × 0.2 を合計に加算**」が「**ターゲットコンボ全体の 20%**」を加算することになる。spec §5 の意図（**始動技＝初段にのみ 1.2 倍**）と乖離しうる。**現時点で実害は出ていない**（`target_combo` を 1 手目にしたコンボが登録され、かつ materialize される条件が要る）。**ただし `combos.starter_move_id` はレシピ 1 ステップ目と常に一致するため、ユーザーが登録すれば成立する**。**要ドメイン確認**＝加算すべきは (a) ターゲットコンボ全体の 20% か (b) 初段のみの 20% か。**(b) なら `moves` に初段ダメージを持たないため現状の式では計算できず**、対処（`target_combo` 始動時は加算せず理由表示、等）が要る。**ダメージは中核データで、生成物は編集可能なため利用者が「アプリが計算した値」として信頼して使う**。**【2026-07-27 裁定】問題は `target_combo` に限らないと判明**＝**PC 補正は初段にしか乗らない**（多段技の `damage` は合計値）・**SA／CA は等倍**・**多段は機械判別できない**（ヒット数列が無い）。→ **③ SA／CA のみ加算しない**／**①②④ は概算として流し生成時メッセージで告知**／**永続注記はしない**。**初段ダメージ列・ヒット数列は新設しない**。**【2026-07-30】M18-03c で実装完了**（項目 6）。**DES 反映済＝DES-003 §3.3 errata（①〜④ の 4 分類を含む一次源）**／**DES-002 §4.2 の理由コードは反映待ち＝CHANGE-090**。**反映時にクローズ** | **クローズ済**（DES-003 §3.3 の 2026-07-27 errata で「概算として流す」と裁定済み・CHANGE-090 で実装反映済み＝2026-07-31） |

### 退避元: J. 停止時記録（未解消のまま停止した項目・stop-record）

| ID（スラッグ） | 発生元 | 未解消の理由 | 再開に必要な条件 | 記録日・状態 |
|---|---|---|---|---|
| **context-load-change-eval-pending** | 工程改善 第一波 P4-e ／ `docs/progress/20260810-prompt-rule-audit.md` §3-5・§5-1。**2026-08-10 追記＝CLAUDE.md 縮小（下記）も本項へ束ねる**（開発者裁定: 評価は一度に行う） | **未検証の context 変更が 2 件ある。** ①レビューの読込構成を「`docs/handover/` 全読込（87 ファイル・3.0MB）」→「小集合 10.7KB ＋ docs-map 経由の必要時取得」／②`CLAUDE.md` を 31,457B → 26,038B（−17.2%）に縮小し、ブラウザストレージ台帳と型分岐運用を `web/CLAUDE.md` へ移設。**どちらも通したのは機械採点のみ**（削減量・出典充足・lint 緑）で、**レビュー品質の新旧比較を通していない**。比較にはレビュー用サブエージェントの複数 trial が要るが、本セッションではサブエージェント起動が許可されていない | 開発者の実行指示。監査レポート §4-1 の評価セット（「重大指摘あり」10 本 ＋「安全な反例」7 本）を新旧条件で各 3 trial 実行し、coverage / restraint / retention / actionability を採点する。**①②を束ねて評価するため、回帰が出た場合はどちらが原因かの切り分けに追加 trial が要る**。差し戻し対象は ①＝`review_plan` と `implement_plan_full` Phase B の 2 か所、②＝`CLAUDE.md` §4・§10.X と `web/CLAUDE.md`<br>**【2026-08-11 パイロット実施済み・拡大判断待ち】** 前段の文脈を継承しない別セッションで、**M19-04b を対象に新旧各 3 trial（計 6 本の fresh subagent）**を実行した（`docs/progress/20260811-context-load-ab-pilot.md`）。結果＝**高指摘の Coverage は両条件 3/3・誤検出は両条件 0 件・Actionability に差なし**で、**回帰は検出されなかった**。副産物として **(a)** 「3,038KB → 10.7KB」は指示文上の削減量にすぎず**実消費トークンは新旧で +0.03% しか違わない**こと、**(b)** 過去報告書の**中・低指摘は正解集合として機能しない**（両条件 0/18。Coverage に使えるのは「高」のみ）こと、**(c)** 6/6 が原レビューの見落とし（`progress-log.md` への M19-04b 未追記＝実際に 04c・04d でも再発）を検出したことが判明した。**未解消の理由は残る**——実効的な正解集合が 1 件しかなく検出力が細い／Restraint 未測定／①②を束ねたまま／旧条件が `CLAUDE.md` 内容と汚染の両面で有利なバイアスを持つ（いずれも報告書 §5）<br>**【2026-08-11 M19-05 で拡大実施】** 開発者指示により **M19-05**（2,179 行・正解「高」3 件）で各 3 trial を追加実行（`docs/progress/20260811-context-load-ab-expansion.md`）。結果＝**正解「高」の再現は旧 5/9・新 8/9 で新条件が上回り**、指摘総数は旧 54 → 新 47 と減った（**精度が上がった**）。トークンは新 +1.9%・ツール呼出 +22.9%・所要 +24.6% で、**パイロット（+0.03%）と合わせて ① にトークン削減効果が無いことが 2 タスクで一致**した。**通算 旧 8/12・新 11/12 で回帰は検出されず、差し戻しの根拠は無い**。**残る未解消**＝(1) n=3 のため「新が劣らない」までが確実で「新が優れている」は暫定 (2) **Restraint 依然未測定** (3) ①②を束ねたまま (4) `web/CLAUDE.md` の補償は結局テストできていない（locales が HEAD と差分ありで `git show` 経由になり `web/` の Read が発生しない）<br>**【2026-08-11 開発者裁定・限界つきで閉じる】** E-1 を採用し、**① ② を確定採用**する。2 タスク・計 12 trial（旧 8/12・新 11/12）で回帰は検出されず、差し戻しの根拠が無いため。**上記 (1)〜(4) の限界は解消していないが、それを承知のうえで閉じる**（開発者裁定 3＝初回リリースまで継続的な労働を要する改善は Phase 4 以降）。**Restraint の未測定分は `context-load-restraint-unmeasured` へ切り出して追跡を継続する** | 2026-08-10 記録 ／ 2026-08-11 **`完了（限界つき）`** |
| **context-load-restraint-unmeasured** | 工程改善 A/B（パイロット＋拡大）／ `docs/progress/20260811-context-load-ab-pilot.md` §5-3・`20260811-context-load-ab-expansion.md` §4（E-5） | **Restraint（誤検出しない力）が 2 ラウンドとも未測定のまま残っている。** A/B は「重大指摘を落とさないか」（Coverage）を測ったが、**「指摘なしが正しい対象に、誤って指摘を出さないか」は測っていない**。正解集合外の「高」指摘が旧 7 件・新 6 件出ており、うち収束したものは実在性が高いが、**単発のものが誤検出かどうかを判定していない**。`context-load-change-eval-pending` は限界つきで閉じたため、**本項が唯一の残存追跡先**である | 実行指示。監査レポート `20260810-prompt-rule-audit.md` §1.5 の「高: なし」を明記した **7 本のうち web 非依存のもの**を対象に、新旧各 3 trial を回して「高」を出さないかを見る。**新条件が「高」を出したら人間が誤検出か判定する**（重大区分のみのスポット確認）。工数は A/B 1 ラウンドと同等<br>**【2026-08-11 開発者裁定・測定は不要】** **Restraint の測定は行わない。** 親項目（`context-load-change-eval-pending`）を限界つきで閉じた判断と同じ根拠——**回帰の兆候が無い状態で、A/B 1 ラウンド分のコストを追加で払う理由が無い**（開発者裁定 3＝初回リリースまでは承認・トリアージ・単発短時間の作業のみ）。**未測定であること自体は消さずに残す**——①②の採用根拠が「Coverage は測ったが Restraint は測っていない」ものであることを、後任が誤解しないため。**再開が必要になる条件**＝新条件下で誤検出（実在しない欠陥の「高」指摘）が実運用で目立ってきた場合。そのときは上記「再開に必要な条件」欄の手順をそのまま使える | 2026-08-11 ／ **`対応不要（開発者裁定。未測定のまま確定）`** |
| **m19-05-ruling-doc-absent** | 工程改善 A/B 拡大 ／ `docs/progress/20260811-context-load-ab-expansion.md` §1.4・§4（E-3） | **A/B の 6 trial すべてが検出し、うち 5 本が「高」に置いた指摘。** 裁定書 `M19-05-PLAN-MODE-RULINGS` v1.1.0（D-259〜D-266）が**リポジトリに存在しない**（`git grep` で当たるのは参照側 4 文書と `internal/service/setplay/service.go:216` のみ。ボード `m19-desk-status.md` の裁定は D-258 が最大）。この裁定書は指示書 §2.4／§2.5／§2.6／§4.2 の読み替えと、**完了条件「段階 A・B・C がそれぞれ別コミット」を満たさないことの正当化の唯一の根拠**である。**原レビュー（`docs/progress/m19-05-review.md`）は §制約事項に「裁定書の現物を参照していない」と書いたのみで、指摘として立てていない**——「参照できない＝完了条件の検証不能」という推論に進んでいなかった。改善担当は M19-05 の成否を判定する立場にないため、事実の提示に留める | 開発者または設計担当による所在の確認。(a) 設計卓の資産としてどこかに存在するなら参照可能な場所へ配置する／(b) 存在しないなら完了報告へ D-259〜D-266 の裁定文言を逐語転記する。**あわせて `docs/handover/m19-desk-status.md` の裁定レンジへ反映されているかを確認する**<br>**【2026-08-11 開発者回答・欠陥ではない】** 裁定書は **`tmp/m19-05-plan-mode-rulings.md`（Git 管理外）に実在する**。**Plan Mode の内容はコミットする資材ではない**という開発者方針であり、**repo に無いこと自体は欠陥ではない**（移動もコミットもしない）。開発者は「誤解を生む記載だった」と認めているが、**記載の是正は求められていないため行わない**。<br>**ただし A/B の指摘のうち下流部分は `f6fee98` 時点で真だった**（改善担当が裏取り）——同書 §10 が反映係へ指示した **followup 5 件**（`punishfinder-standalone-exclusion-redesign` / `r6-parent-step-consistency-warning` / `tp02-chain-cancel-total-remaining` / `gc2-thunder-kick-parent-reference` / `notation-expansion-multiple-parents`）は `f6fee98` 時点で **backlog に 0 件**、**D-260〜D-266 も `parallel-board.md` §3.4 に 0 件**だった。**いずれも HEAD では反映済み**（backlog 5/5・board 5/5）であり、**反映係の工程はレビュー後に正常に完了している**。**恒久対応は不要。** | 2026-08-11 ／ **`完了（欠陥なし・下流の反映は完了済み）`** |
| **m19-05-gate2-thirteen-rows-unreachable** | 工程改善 A/B 拡大 ／ `docs/progress/20260811-context-load-ab-expansion.md` §1.4・§4（E-4） | **A/B の 6 trial 中 4 本が「高」で挙げた、正解集合に無い指摘。** 「ゲート 2 で除外した 16 行のうち **13 行がどの提案経路にも現れない**」——`M19-DESIGN-07` §5-4 と指示書 §2.1 は「★該当行はチェーン合成・表記展開経由でのみ登場させる（除外して終わりにしない）」と定めるが、チェーン合成で復帰するのは `chain_cancel_total` を持つ zangief 連打版 3 行のみで、表記展開は `startup_basis == 'through'` の技にしか親を前置しない（`internal/service/setplay/service.go:697-700`）ため `standalone`／`unknown` の 13 行（guile 5・kimberly 3・ken 3・m_bison 2）は復帰経路が無い、という主張。**改善担当は裏取りしていない**（設計正本の解釈と費用規則の新設可否を含むため、製造側では閉じられない） | 設計卓の裁定。(a) 主張の真偽の確認 → (b) 真なら「`standalone`／`unknown` の子に親を前置する費用規則」を新設するか、「13 行は登場しない」を仕様として受け入れて完了報告・設計伝達レポートへ明記するかの選択。**費用規則 4 分類のいずれにも該当しない新規則になるため、製造の推測で埋めてはならない**<br>**【2026-08-11 開発者裁定・ミスとして計測しない】** **裁定 D-260 §1.2 で既に決着している**——「**したがって段階 A のゲート 2 による除外はそのまま実装してよい。これは setplay 側であり、除外されるべき行が除外されている**」。setplay 側の問いは「**自分がこの技を単独で入力できるか**」であり、`standalone`/`unknown` ＋ 子あり＝単独入力不可＝**除外が正しい**（転用できないのは punishfinder 側だけ）。**M19-05 の欠陥ではないため、A/B の Retention としても計上しない。**<br>**なぜ 4/6 が「高」で挙げたか**: この決着文（D-260 §1.2）は **Plan Mode 資材にしか無く、repo 内の資産からは到達できない**。**本件は `m19-05-ruling-doc-absent` の下流であって、独立した誤検出ではない**。<br>**残る論点（開発者判断に委ねる・改善担当からは提案しない）**: `M19-DESIGN-07` §5-4 の「チェーン合成・表記展開経由でのみ登場させる」に対し、表記展開は D-264 (4) のとおり `startup_basis='through'` の行にしか効かないため、`standalone`/`unknown` の 13 行は**構造的に表記展開では復帰しない**。これは実装の欠陥ではなく**設計書記述の射程の問題**である | 2026-08-11 ／ **`完了（裁定済み・計測対象外）`** |
| **web-claude-md-codex-invisibility** | 工程改善 CLAUDE.md 縮小 ／ 承認済みプラン §5-2 | `web/CLAUDE.md` へ移設した規則は **Codex から見えない**（`AGENTS.md` は root の `CLAUDE.md` を読む前提で、`human-notes/codex/README.md` §4 も「規約本文は CLAUDE.md だけに置く」と定める）。補償として `scripts/check-browser-storage-keys.sh` を機械強制にしたが、**lint は「書いた後」の検査であり「書く前」の抑止にはならない**。`AGENTS.md` へ 1 行のポインタを足す案は開発者裁定で採らなかった | 次のいずれか。(a) Codex で web 配下の作業を実際に行い、台帳未記載キーを lint が捕まえることを確認する（＝補償が機能する実証）／(b) 実害が出たら `AGENTS.md` へ `web/CLAUDE.md` へのポインタを 1 行追加し、あわせて `human-notes/codex/README.md` §4 の「規約本文は CLAUDE.md だけ」の記述を実態へ更新する <br>**【2026-08-11 追記】わざと発火させる実証プロンプトを `docs/handover/session-prompts/20260811-codex-storage-probe.md` へ用意した**（Codex へ貼る。キー `combomgr.audit.probe.v1` を `browser-storage.ts` 経由で追加 → `check-browser-storage-keys.sh` を実行 → 結果を報告して変更を戻す）。**通常の調査作業では `localStorage` を書かないため発火しない**ので、実作業のタイミングを待つか本プロンプトで意図的に発火させる。**赤なら補償の機能を実証して閉じる／緑なら `AGENTS.md` へポインタ 1 行を追加する** <br>**【2026-08-11 解消・補償の機能を実証】** Codex で `web/src` に `localStorage` を書かせ、`scripts/check-browser-storage-keys.sh` を実行した。**★1 回目は緑になったが、それは「違反なし」ではなく「検査が見えていなかった」**——投入プロンプトが指定したキー `combomgr.audit.probe.v1` は**ドット区切り**で、検査の抽出パターン `KEY_RE='[a-z0-9-]+-v[0-9]+'` に一致しない（台帳のキーは全て `kebab-case-v1` 形式）。**プロンプト側の誤りであって lint の誤りではない。** **⇒ 規約に沿ったキー `audit-probe-v1` を本番コード（`web/src/types/tag.ts`）へ入れて実地検証したところ、`NG 台帳に未記載のキーを本番コードが使用` で赤になった**（復元後は緑・作業ツリーもクリーン）。**lint はファイルベースなので誰が書いたかは無関係**であり、**Codex が `web/CLAUDE.md` を読めなくても書いた後に必ず捕まる**。**補償は機能している。** **★ただし条件つきで、新しい穴を `browser-storage-key-naming-blindspot` として起票した。** | 2026-08-10 記録 ／ 2026-08-11 **`完了（補償の機能を実証）`** |
| **browser-storage-key-naming-blindspot** | 工程改善 第二波 ／ `web-claude-md-codex-invisibility` の実証で判明（2026-08-11） | **`scripts/check-browser-storage-keys.sh` は「命名規約に沿ったキー」しか見ていない。** 抽出パターンは `KEY_RE='[a-z0-9-]+-v[0-9]+'` で、台帳のキー 7 件がすべて `kebab-case-v1` 形式であることに依存している。**`myKey` ／ `MY_KEY_V1` ／ `combomgr.foo.v1` のような規約外の名前でブラウザストレージを使うと、検査を完全に素通りする。** **★これが重大なのは、`CLAUDE.md` §10.X の規則が「何を保存するか」（機密情報・DB 永続化対象データの禁止）についてであって命名についてではないため**——**規約外の名前で機密情報を保存しても検出されない**。実証の 1 回目が緑になったのはまさにこの盲点で、**「緑」は「違反なし」ではなく「検査が見えていない」だった**（`environment-reliability-plan.md` §4.1 の原則の実例）。**なお本項は補償の否定ではない**——規約に沿ったキーは確実に捕まることを実地で確認済み | 着手指示のみ（改善担当で完結）。方針の候補は 2 つ。**(a) 検査を広げる**——`localStorage` / `sessionStorage` / `indexedDB` の**呼び出し箇所そのもの**を検出し、そこで使われている文字列リテラルを台帳と突合する（命名規約に依存しなくなる）。ただし `browser-storage.ts` のヘルパ経由が既定のため、**ヘルパの呼び出し引数**を見るのが実質的。**(b) 命名規約を機械強制する**——規約外の名前を使ったら赤にする。(a) のほうが本質的だが実装が重い。**着手前に「規約外の名前が現に使われていないか」を実査すること**（現状 6 件はすべて規約準拠） <br>**【2026-08-11 解消】方針 (a)（検査を広げる）を採った。** 実測でストレージへ触る経路が **3 種に限定**されていた（ヘルパ 2 本 `createLocalStorageHelper` / `createSessionStorageHelper` ＋ フック `useSessionStorage`。呼び出し 13 箇所）ため、**命名規約に依存しない検査にできた**。**(1)** ヘルパ呼び出しの**第 1 引数リテラル**を抽出して台帳と突合する経路を追加（従来の `KEY_RE` 経路は後方互換で残し**和集合**を取る）。**(2)** **ヘルパ外からの直接呼び出し**（`localStorage.setItem` 等）を検出。ALLOW は実装本体の 2 本のみ（`browser-storage.ts` ／ `useSessionStorage.ts`）——**直接呼びを許すとキーが引数に現れず突合を回避できる**ため。**(3)** self-test へ陽性対照 2 件を追加（規約外の名前／直接呼び出し）。**★実地検証済み**——`createLocalStorageHelper<string>("myKey")` を本番コードへ入れると赤、`localStorage.setItem` を直接書いても赤、復元すると緑に戻る。**旧実装ではどちらも素通りしていた。** スクリプト冒頭の「限界」も実態へ更新した | 2026-08-11 記録 ／ 2026-08-11 **`完了`** |
| **browser-storage-ledger-location-change** | 工程改善 CLAUDE.md 縮小 ／ 承認済みプラン §5-3 | ブラウザストレージ台帳を `CLAUDE.md` §10.X から `web/CLAUDE.md` §1 へ移した。§10.X は「台帳に未記載の UI 状態は CHANGE 通知書経由で**本表**へ追記」と定めており、**その「本表」の所在が変わった**。CHANGE 通知書を起票するのは設計担当だが、移設は製造側（改善担当）が行ったため、設計担当がまだ新しい所在を知らない | 設計担当への申し送りが届き、CHANGE テンプレート・playbook 側の参照先が `web/CLAUDE.md` §1 を指すこと。次回の CHANGE 起票時に確認する <br>**【2026-08-11 解消】前提が事実と違っていた。** 再開条件は「CHANGE テンプレート・playbook 側の**参照先**が `web/CLAUDE.md` §1 を指すこと」だったが、**実査したところ `docs/instructions/templates/` と `design-instruction-playbook.md` にブラウザストレージ台帳への言及は 1 件も無かった**——張り替えるべき参照が存在しなかった。**実際のリスクは別にあった**: `CLAUDE.md` §10.X は「台帳に未記載の UI 状態は CHANGE 通知書経由で台帳へ追記」と定めるが、**CHANGE を起票する設計担当は `CLAUDE.md` を起動時に受け取らない**（`next_milestone_kit` 変換規則 17 で任意化済み）ため、**設計担当が台帳の存在を知る導線が無かった**。**⇒ 参照の張り替えではなく参照の新設で解決した**——`CHANGE-{num}-notification.template.md` §5 へ記入指針を追加（UI 状態をブラウザストレージへ保持する変更なら `web/CLAUDE.md` §1 の台帳へ追記する行を含めること／機密情報・DB 永続化対象データは保持不可／検査コマンド）。 | 2026-08-10 記録 ／ 2026-08-11 **`完了`** |
| **context-load-retrieval-list-gaps** | 工程改善 評価前スクリーニング（Step 0）／ `docs/progress/20260810-prompt-rule-audit.md` §7.3 | **読込構成の変更で「必要時に取りに行く」リストに 2 資料の記載漏れがある。** 過去のレビュー報告書 51 本の実測で `retrospective-digest` は 9 件、`change-number-registry` は 6 件引用されていたが、どちらも変更後のリストで名指ししていない。旧「`docs/handover/` 全読込」には含まれていた。`docs-map` 経由で到達はできるが、**名指ししていない資料は探されない**。修正は 5 分だが、**開発者の停止指示を受けたターンだったため着手せず記録に切り替えた**（停止規律） | 着手指示のみ。`.claude/commands/review_plan.md` と `implement_plan_full.md` Phase B の「必要時に取りに行く」リストへ 2 行追加する。**A/B 評価（`context-load-change-eval-pending`）とは独立に先行してよい**——明らかな記載漏れであり評価待ちの理由がない<br>**【2026-08-11 解消】** `.claude/commands/review_plan.md` と `implement_plan_full.md` Phase B の「必要時に取りに行く」リストへ 2 行ずつ追加した（`retrospective-digest.md` の該当節／`change-number-registry.md` の該当行。**いずれも「該当節だけ開く」形にして全文読込へ戻していない**。213KB の registry には全文を開かない旨を明記）。`bash scripts/check-doc-refs.sh` exit 0 で追加パスの実在を確認 | 2026-08-10 記録 ／ 2026-08-11 `完了` |
| **audit-c1-comparison-unmeasured** | 工程改善 第二波 監査タスク C1 ／ `tmp/kaizen_before_M20/prompt-quality-improvement-audit.md` §5 | **C1 の「導入前後を評価セットのレビュー区分で比較する」を実施しないまま確定した。** C1 の機構部分（全指摘修正後に再レビュー 1 回・**往復上限 2 回を機構で強制**・未解消項目は理由付きで開発者裁定へ）は**実装済み**（停止規律＝`scripts/check-stop-discipline.sh` ＋ 4 ファイルのマーカー ＋ §J 必須 5 フィールド＝開発者裁定 1）。**未実施なのは比較測定だけ。** | **測定は行わない（2026-08-11 開発者裁定）。** 理由＝**測定結果が意思決定を変えられない**——上限 2 回は既に裁定 1 で確定しており、仮に「上限 2 回だと品質が下がる」と出て上限を上げるなら**裁定 3（初回リリースまで継続的労働を要する改善は Phase 4 以降）と衝突する**。加えて**問いの立て方がずれている**——停止規律は「完璧主義による停止不能」という**行動の問題**への対策であって、レビュー品質の最適化ではない。**未測定であること自体は消さずに残す**——C1 の採用根拠が「実測」ではなく「開発者裁定」であることを後任が誤解しないため（`context-load-restraint-unmeasured` と同じ形）。**再開が必要になる条件**＝上限 2 回によって重大指摘が実際に取りこぼされた事例が観測されたとき | 2026-08-11 ／ **`対応不要（開発者裁定。未測定のまま確定）`** |
| **audit-c3-ablation-not-run** | 工程改善 第二波 監査タスク C3 ／ `tmp/kaizen_before_M20/prompt-quality-improvement-audit.md` §5 | **C3 の③裁定層（N 回報告されたルールをアブレーション＝1 つ外して評価セットを回す）を実施しない。** **理由 1: 現時点で実行不可能**——③は「**N 回報告されたルール**」を入力に取るが、その N を数える**①自己申告層（完了報告テンプレの「ルール摩擦」欄）と②行動ログ層（フック発火の記録と月次集計）がどちらも未実装**で、入力が存在しない。**理由 2: 仮に動かしても効果を主張できる余地がほとんど無い**——非交渉条件が「**高致命度（不可逆系）ルールを、発火実績のみを根拠に削除提案しない**」と定め、ヘルメット判定も「発火ゼロ×高致命度 → 維持」としているため**外せるのは低致命度ルールだけ**。低致命度ルールを外して得られるのは主にトークン削減だが、**裁定 15 で「削減量を効果の根拠にしない」と決まっている**。**非交渉条件と裁定 15 に挟まれて、答えられる問いが残っていない** | **アブレーションは行わない（2026-08-11 開発者裁定）。** ただし **C3 の成果物のうち `deny` 全件の仕分け表だけは作る価値がある**——`.claude/settings.json` の deny を読んで**不可逆性で分類**する read-only 作業で、**ヘルメット判定の土台**になり、「発火ゼロだから消す」という誤った提案を将来防ぐ。**着手指示のみで実施できる**（改善担当で完結）。**再開が必要になる条件**＝①②が実装され、特定のルールが N 回報告されたとき <br>**【2026-08-11 完了】`deny` 全件の仕分け表を作成した**（`docs/process/deny-rules-inventory.md`）。79 件を不可逆性で分類——**高（不可逆）58 件／中（外部影響）21 件／低（局所・可逆）0 件**（検算済み・未分類と重複はいずれも 0）。**★削除候補は 1 件も無かった**——deny リストは「不可逆な操作」と「外部影響のある操作」だけで構成されており、**発火実績を根拠に削除できるものが存在しない**。**これはアブレーションを行わない判断の裏付けになる**（仮に実施しても対象になるルールが無い）。**★副次的な発見**——同じ意図のルールが複数行にわたるのは**マッチャの仕様上の必要**であって無駄ではない（`curl` とパイプと `bash` の間の空白の有無で別パターンになる／`git reset` と `git reset:*` は別／`.env` はツール別・相対パス表記別に要る）。**「同じような行が並んでいる」ことを理由に整理してはならない。減らすと穴が開く。** | 2026-08-11 ／ **`対応不要（アブレーションは行わない）。deny 仕分け表は完了`** |
| **instruction-templates-model-names** | 工程改善 第一波 監査タスク A3 ／ `docs/progress/20260810-prompt-rule-audit.md` §1.3・§4-3 | `docs/instructions/templates/`（8 件）と `design-instruction-playbook.md`（33 件）にモデル名が直書きされており、非交渉条件「モデル名を恒久ルールへ直書きしない」に抵触する。**両者は設計担当の管轄**のため改善担当は編集せず、申し送りに留めた | 設計担当による置換（役割参照「高精度実装／高速実装／高精度レビュー」＋ `docs/human-notes/model-allocation.md` 参照）。完了判定は該当 2 対象のモデル名が 0 件になること<br>**【2026-08-11 解消】開発者がテンプレート編集を改善担当へ許可**したため実施した。**★件数は §J の記載と違った**——実測は**テンプレ 5 出現・playbook 26 出現の計 31**（記載は 8＋33＝41）。**★D-274 の 3 分類で仕分けた**: **恒久ルール 8 出現を役割参照へ置換**（テンプレ 4 ファイル 6 出現＝推奨モデル欄・レビューモデル欄・RESEARCH のモデル欄・記入指針コメント ／ playbook §2.1 メタ表・§5 行数目安・§7.1 の 2 見出し・§7.3 レビュー判断）。役割名＝**高精度実装／高速実装／高精度レビュー／高速レビュー**、実モデル名の正本は `docs/human-notes/model-allocation.md`。**★playbook §7.2 配分実績表（M1〜M2）と §7.4 Max 5x 方針の 12 出現は意図的に残した**——**恒久ルールではなく「当時どう配分したか」の記録**であり、書き換えると記録が消える（**D-274 (2)**＝裁定行と同じ扱い）。**残した理由を §7.2 の直下へ明記**した。**テンプレ側は 0 件**（4 モデル名を `grep -rE` で走査して 0 件を確認） | 2026-08-10 記録 ／ 2026-08-11 **`完了`** |
| **board-backlog-resolved-archive** | 工程改善 第二波 反映担当の廃止 ／ `docs/progress/20260811-progress-doc-responsibility-rulings.md` §3A-⑥ | **解決済みの記録が設計卓へ渡す資料を膨らませている。** 反映担当の廃止（2026-08-11）により、設計担当は `parallel-board.md` ／ 本書 ／ `M{N}-overview.md` を**その回の資料として渡されて更新する**運用になった。**渡す量が直接コストになる。** 実測＝`parallel-board.md` は **517 KB / 768 行**で、**§3 裁定ログが 322 KB（62%）**・§9 改訂履歴が 51 KB（10%）＝**履歴が 72%**。現況にあたる節は約 70 KB しかない。§3 は見出し自体が「**1 行追記・古い行は消さない**」と明記しており**構造的に成長し続ける**。本書は 156 KB / 348 行で、§J が「**完了した項目も削除しない**」を明文の規則とし（完了系 7 件）、§A〜§I にも完了・解消済みを含む行が 41 件ある。**★どちらも「消さない」ことに理由がある**——§3 は当時どう判断したかの記録（D-57／D-259）、§J は停止と再開の履歴が規律の実績。**単純削除は不可で、アーカイブ先への退避が要る** | 開発者の設計指示。決めるべきは (a) アーカイブの単位（マイルストーン境界／裁定番号レンジ／状態が `完了` になってから N 日）(b) アーカイブ先のパスと、そこへの参照をどう残すか（`docs/handover/archive/` に前例あり）(c) 退避を機械化するか人手か。**着手は「古い行を渡さない」ことが実際にコストとして効き始めてからでよい**——反映を設計担当が自分で行う運用に切り替えた直後は、まず渡す量の実害を観測する <br>**【2026-08-11 機構を実装。退避の実行は M20 起動時】** 開発者裁定で **3 点が確定**——**(1) 単位＝マイルストーン境界**（既存前例 `docs/*/phase{N}/` に合わせる）**(2) 退避先＝`docs/handover/archive/followup-backlog-M{N}.md` ／ `docs/process/archive/parallel-board-rulings-M{N}.md`。M20 以降はマイルストーン単位に分ける**（確認時に全部を読まないで済むため）**(3) コマンドで自動化**（マイルストーンは 1 週間〜1 日ペースで回るため人手では追いつかない）。**実測を待たず予防的に進める**。**⇒ `scripts/archive-resolved.sh` を新設**（既定 dry-run・`--apply` で実行・`--self-test` 付き・行数保存の検証つきで失敗時は書き戻し）。**`next_milestone_kit` の Step 3-0f から呼ばれる**（dry-run → Plan Mode 提示 → 承認後 `--apply`）。**★設計が実測で変わった点**——当初は「節（＝マイルストーン）ごと退避」を想定したが、**§H（M19）は M19 クローズ済みなのに 48 行中 25 行が未解決**だったため成立しない。**行単位で状態列を見る**方式にした（列位置は節ごとに違うのでヘッダから検出）。**★排除ルート**＝`--exclude <ID,…>`（マイルストーンが想定より拡大し、`完了` 扱いだが実はまだ動いている項目を設計担当の宣言で外す）。**規則も改訂**: §0 へ「解決済み記録のアーカイブ」を新設／§J の「削除しない」に「境界では退避してよい（退避は削除ではない）」を追記／board §3 の見出し直下へアーカイブ運用の注記。**★board 裁定ログの退避は `--board-through <D 番号>` を渡したときだけ**で、**上限は毎回判断する**——dry-run で `--board-through 287` を試すと **284 行すべて**が対象になり §3 が空になる。直近の裁定（D-260〜D-287＝M19 クローズ期）は M20 でも参照されるため全部は退避しない。 **【2026-08-11 追記】M20 起動時に使う投入プロンプトを `docs/handover/session-prompts/20260811-board-archive-M20.md` へ用意した。** <br>**【2026-08-11 追記 2・優先度が下がった】** 本項の動機は「**設計卓へ渡す資料が膨らむ**」ことだったが、**親の面が Claude Code へ移った**（`code-side-design-desk-migration` 決着）ため、**「渡す」という概念自体が消えた**（親は repo を直接読む）。**残る動機は「読む量」と「grep のノイズ」だけ**になり、渡す量ほど直接的ではない。**機構は完了済みなので M20 起動時の Step 3-0f はそのまま実行してよい**が、**急ぐ理由は無くなった**。 | 2026-08-11 記録 ／ 2026-08-11 **`機構は完了（退避の実行は M20 起動時に Step 3-0f で。ただし優先度は低下）`** |
| **instruction-template-goal-orientation-proposals** | 工程改善 第二波 監査タスク B ／ `docs/progress/20260811-instruction-goal-orientation-audit.md` §5・§6 | **テンプレート改訂の提案 5 件（B-1〜B-5）が採否待ちで止まっている。** `docs/instructions/templates/` は**設計担当の管轄**のため改善担当は編集していない（先例＝本節 `instruction-templates-model-names`）。**B-1**＝`パス:行` を書くときは識別子（関数名・テスト名・定数名）を必ず併記する〔M19-04d の引用 7 件中 3 件が失効していたが、全件に識別子があったため実害が出なかった実測が根拠〕／**B-2**＝RESEARCH テンプレートへ「探索モード（PoC 可）」の任意ブロックを新設し `M19-RESEARCH-01` §0.1・§0.2 を範として収録する〔**探索モードは実務で必要になったが制度が無く、1 件ごとに手書きされ再利用されていない**〕／**B-3**＝契約 7 種のチェックリスト／**B-4**＝固定件数に「見込み」の明示と実測報告を求める／**B-5**＝In scope の独立節化。**B-1・B-2 は実測に裏付けがありコストも小さい。B-3・B-5 は「実物は指針が無くても書けている」ため効果が抜け防止に限られ、採否の判断が要る** | 設計担当によるテンプレート改訂の採否判断。**B-1・B-2 を優先して扱うことを推奨**。完了判定は、採用した提案がテンプレートの記入指針へ反映され、次の実装指示書 1 本で機能したことを確認できること（B-1 は `パス:行` の全出現に識別子が併記されているか、B-2 は PoC を含む調査でテンプレートのブロックが使われ手書きの再定義が起きていないか）<br>**【2026-08-11 解消】開発者がテンプレート編集を改善担当へ許可**し、**B-1・B-2・B-4 を採用**した。**B-1**＝実装テンプレ §4 の記入指針へ「`パス:行` は識別子を必ず併記・単独の行番号は書かない」を実測つきで追加／**B-2**＝RESEARCH テンプレ §0 へ**探索モード（PoC 可）の任意ブロック**を新設（`M19-RESEARCH-01` §0.2 の 3 点＝リポ外の使い捨て領域に限る・DB はコピーを読む・本体は 1 バイトも変えない ＋ **移行条件**＝PoC 結果をそのまま受入条件にしない ＋ **合格線は置かない**）／**B-4**＝実装テンプレ §7 へ「固定件数は確定値か見込みかを書き分け、見込みなら実測値を報告させる」を追加。**★B-3（契約 7 種のチェックリスト）と B-5（In scope の独立節化）は見送り**——いずれも「実物は指針が無くても書けている／実害が観測されていない」ため。**B-3 は §4 の記入指針が既に長く、7 項目を足すと儀式化（該当しない項目にも『なし』と書く作業）のリスクが効果を上回る**。**B-5 は 19 本でスコープ逸脱の記録が無い**。**再開条件**＝B-3 は完了報告 §2（契約違反の独自判断）に契約の書き落としが現れたとき、B-5 は M20 期に実際にスコープの迷いが出たとき | 2026-08-11 記録 ／ 2026-08-11 **`完了（B-1・B-2・B-4 を採用／B-3・B-5 は見送り）`** |
| **progress-log-responsibility-rulings-pending** | 工程改善 第二波 ／ `tmp/kaizen_before_M20/progress-log-improvement-plan.md` §2（先に行う裁定 5 件） | **開発者裁定 5 件が未取得のため、テンプレート改訂（同 §3 P1〜P3）に着手できない。** 5 件＝①「親は progress を読まない」の対象から `progress-summary` を除外するか ②親の進行中正本は `parallel-board` でよいか ③design handover §5 に必要な実装証拠の上限 ④`progress-log` は横断インデックス型でよいか ⑤各文書の古さをどの更新時刻・版数で判断するか。**作業自体は短時間だが、本セッションは末尾に A/B パイロットの拡大判断という開発者裁定を既に 1 件立てている。** 同一セッションで裁定を 2 系統出すのは、開発者裁定 3（初回リリースまでは承認・トリアージ・単発短時間のみ）を超えると判断して見送った | 開発者裁定 5 件。改善担当が実態調査つきの質問票（推奨案＋根拠つき）を作れば、開発者の所要は選択のみで 15 分程度に収まる見込み。**質問票の作成から次セッションで行う**<br>**【2026-08-11 解消・裁定 5 件を取得】** 実態調査のうえ開発者裁定を取得し、明文化まで完了した（正本 `docs/progress/20260811-progress-doc-responsibility-rulings.md`）。**①**＝`progress-summary` は「親が読まない」の対象から**除外する（起動キットの投入資材として起動時にのみ読む）**／**②**＝進行中の現在地の正本は `parallel-board` の**まま（現状追認）**／**③**＝設計伝達レポートは**節番号固定 ＋ §5 は 1 項目 1 行・15 行以内**（対応表つきのずれのみ許容）／**④**＝`progress-log` は**横断インデックス型（実態の追認）＋ 製造 CLI へ索引行の追記を組み込み**、P3 自動生成は見送り／**⑤**＝古さは**優先順位ルール 1 行**で扱い機械検査は作らない。**★④の根本原因を特定した**——`progress-log` 追記の要求は**指示書テンプレートの記入指針（D-191）にしか無く、製造 CLI 3 本には 0 件だった**（変更前 grep）。型を変えても追記漏れは直らないため、`implement_plan` §完了時 ／ `implement_plan_full` Phase D ／ `incorporate_plan` へ二重化した。**★⑤は当初推奨（git 時刻を唯一軸に）を取り下げた**——board は状態変化時・summary は境界時に更新するため「summary が古い」は正常であり、機械検査は誤警報になる。**残した申し送りは `playbook-progress-log-scope-sync` の 1 件のみ** | 2026-08-11 記録 ／ 2026-08-11 **`完了`** |
| **playbook-progress-log-scope-sync** | 工程改善 第二波 progress 資料の責務整理 ／ `docs/progress/20260811-progress-doc-responsibility-rulings.md` §3（裁定④の派生） | **裁定④と `design-instruction-playbook.md` の記述が食い違ったまま残っている。** 裁定④は `progress-log.md` を「**横断インデックス（日付・作業ID・結果・報告書リンク＋完了報告に書けない横断課題）**」と定義し、同書のメタ表を書き換えた。一方 playbook **§11.2**（完了報告セクションの構成＝実装内容のサマリ・E2E シナリオ実行結果・Plan Mode 確認結果・既知の制限事項・持ち越し課題）と **§11.4**（「**記録の正本は `progress-log.md`（全量維持）**」）は**全量記録型の定義のまま**である。**playbook は設計担当の管轄**のため改善担当は編集しなかった（先例＝本節 `instruction-templates-model-names` で、テンプレ・playbook のモデル名 41 件を同じ理由で申し送りにしている）。**矛盾を黙って残さないため、`progress-log.md` メタ表の当該行に「playbook §11.2 / §11.4 と食い違う・同期は設計担当待ち」と明記してある** | 設計担当による playbook §11.2 / §11.4 の同期。**§11.1（既知制限事項を本書へ統合し独立ファイルを作らない）と §11.3（過去の制限事項のクローズ記録）は裁定④と衝突しないため、そのまま playbook が正本でよい**。完了判定は §11.4 の「全量維持」の記述が横断インデックス型の定義へ置き換わり、`progress-log.md` メタ表の同期待ち注記を外せること<br>**【2026-08-11 解消】開発者が「改善担当が触れてもよい資料」と明示**したため改善担当が同期した。**§11.2** を「完了報告セクションの構成」から「**progress-log への索引行の構成**」へ全面改稿（形式・詳細の正本は個別完了報告・追記必須と D-191 の経緯・検査コマンドを明記。旧構成は失効と明示し、過去節は遡及改訂しないことも記載）／**§11.4** の「記録の正本は `progress-log.md`（全量維持）」を失効とし、**読まれ方**（親が読むのは `progress-summary` だけ・起動時のみ・進行中の正本は `parallel-board`・summary が古いのは正常）を追記。`progress-log.md` メタ表の同期待ち注記を外した。**★副次的に判明**: playbook は **§11 と §12 が 2 回ずつ出現する**（§11 質問・確認事項のフォーマット / §11 進捗ログ統合方針）。本項では番号の重複自体は直していない（節番号の振り直しは全文の参照に波及するため） | 2026-08-11 記録 ／ 2026-08-11 **`完了`** |
| **playbook-section12-web-tool-names** | 工程改善 第二波 playbook 全面監査 ／ `docs/progress/20260811-playbook-duplication-audit.md` §5 | **playbook §12「ツール使用パターン」が claude.ai Web 版の旧ツール名を前提にしている**（`create_file` ／ `str_replace` ／ `present_files` ／ `bash`）。開発者裁定（2026-08-11）により**設計担当は要件に応じて Web 版でも Code 版でも動く**ことになったため、**Code 版で動く設計担当にとって §12 はそのまま使えない**（ツール名も操作単位も異なる）。**単独では解かない**——§12 をどう書き分けるか（面ごとに分割するのか、面非依存の抽象へ書き換えるのか、Web 版限定と明示して残すのか）は**設計卓を Code 版へ寄せるかどうかの判断と不可分**であり、先に面の方針が決まらないと書き直しても無駄になる | **`code-side-design-desk-migration` と合流させて扱う**（同じ判断に依存する）。面の方針が決まった後で、(a) §12 を面別に分けるか (b) `create_file`／`str_replace`／`present_files` を面非依存の記述へ置換するか (c) Web 版専用と明示して残すか を決める。完了判定は、Code 版で動く設計担当が §12 を読んで実際の操作に対応づけられること <br>**【2026-08-11 完了】面の方針が決まった**（`code-side-design-desk-migration`＝親は Code、Web 版は個別設計チャット）ため、**(a) 面別に分ける**を採用して **playbook §12 を全面改訂**した（v2.12.0）。**新構成**＝**§12.0 読込の原則**（両面共通・新設。読んでよい/読まないの線、担保 3 枚、実装の事実の取り方 3 段）／**§12.A** 親（Claude Code）のツール手順（Read/Grep/Glob/Write/Edit/Bash/Agent。**提示は不要**＝成果物が repo に着地するため）／**§12.B** 旧 §12.1〜§12.5 の Web 版手順（`create_file`／`str_replace`／`present_files`／`bash`）を**個別設計チャット向けとして移設**。**★旧ツール名を削除しなかった理由**＝Web 版は今も個別設計チャットとして動くため、記述は必要である（「使えない」のではなく「宛先が変わった」）| 2026-08-11 記録 ／ **`完了（playbook v2.12.0 §12 を面別に分割）`** |
| **ai-relay-prompt-guide-adoption** | 開発者が外部 AI 作成のガイドを格納（2026-08-11）／ `tmp/kaizen_before_M20/ai_relay_prompt_engineering_guide.md`（**git 管理外**・整形済み） | **AI 間リレープロンプトの設計ガイドが改善計画に加わった。原著は外部 AI がリポジトリを見ずに書いたもので、そのまま実行すると既存の作り直しになる。** 改善担当が実物照合のうえ整形し（875 行 → 332 行）、**照合結果を §0 として追記**した。**★実測でわかったこと**——**(1) 原著 §17 の第 1 位「リレー標準フォーマット（Goal/Decisions/Scope/Constraints/AC）」は実装済み**。既存の実装指示書テンプレが **13 項目中 12 を満たす**（欠けているのは `Constraints` の独立節のみ）。**(2) 原著が警告する問題は実測ほぼゼロ**——思考強制（step by step）**0 件**・儀式的な自己検証命令 **0 件**・Few-shot は 2 ファイルのみ。**(3) Codex との関係の記述が不正確**——`.claude/commands` の本文は Skill 経由で読まれるが **`AGENTS.md` が「スラッシュコマンドとしては使えない」と明記**しており、**Model Adapter は `human-notes/codex/README.md` が正本で `/sync_codex_config` まで存在する**（原著 Rule 2 の 3 層分離は実装済み）。**(4) 原著 §14 Phase 1〜5 は `prompt-quality-improvement-plan` Stage 1〜5 とほぼ同型で、2026-08-11 に完了済み**。**(5) 裁定 15 と衝突**——原著は評価指標に「使用トークン・実行時間」を挙げ削除候補を列挙するが、**削減量は効果指標にしない**ため除外した。**(6) Rule 1（Single Source of Truth）はそのまま適用できない**——playbook 全面監査の判定は「**同期機構を持つ二重化は残す**」。**★唯一の空白＝原著 §16「モデルアップグレード時の運用」**。相当する手順を `docs/handover` ／ `docs/human-notes` ／ `.claude/commands` ／ `docs/process` で走査して **0 件**だった | **別セッションで実行**（投入プロンプト `docs/handover/session-prompts/20260811-ai-relay-prompt-guide.md`）。中心作業は **原著 §16 の手順を書くこと**で、**2026-08-11 の `instruction-templates-model-names`（恒久ルールから実モデル名を除去し役割名へ置換・実モデル名の正本を `docs/human-notes/model-allocation.md` へ一元化）の続き**にあたる。決めるべきは (a) 置き場所（`model-allocation.md` へ 1 節 ／ `docs/process/` へ独立ファイル）(b) 触らないもの・触るものの線引き (c) **回帰セットが常設で保存されているかの確認**（A/B は実タスクで 12 trial 実施したが、常設保存は未確認）。**★`Constraints` の独立節化は「制約の書き落としが実際に起きているか」で判断し、起きていなければ作らない**（テンプレ提案 B-3・B-5 を見送ったのと同じ理由＝儀式化のリスクが効果を上回る）  <br>**【2026-08-11 完了・改善担当が実施】** 開発者判断で別セッションを立てず本セッションで実行した（残差分が 1 件のみと確定したため）。**`docs/process/model-upgrade-ops.md` を新設**（v1.0.0）。**★置き場所の判断**＝`docs/human-notes/model-allocation.md` ではなく `docs/process/` にした。前者は**マイルストーンごとに追記する開発者向け記録**（v1.42.0・「完了済みの行は履歴として残す」がメンテ方針）であり、**手順を混ぜると記録と規則が同居する**（D-274 の区別）。後者は `deny-rules-inventory` ／ `remote-ops` と同じ規則層。**内容**＝(1) 触らないもの（Goal/Scope/Non-goals/Decisions/AC・playbook §7.1 の判断軸・**役割名を実モデル名へ戻さない**）／(2) 触ってよいもの（`model-allocation.md` の実モデル名・effort/thinking/verbosity・**workaround は「なぜ要るか」と「いつ消せるか」を併記**）／(3) 記録は書き換えない（playbook §7.2・§7.4・完了済み配分行）＝D-274 (2)／(4) 回帰の当て方。**★新しい回帰スイートは作らなかった**——常設セットは未保存で、**保存されているのは方法論**（A/B パイロット・拡大の 2 報告）。**新モデル後の最初の実サブで同じ形の A/B を 1 ラウンド当てる**形にした（専用タスクを作らず実作業を使う＝パイロットと同じ）。**指標から使用トークン・実行時間・削減行数を除外**（裁定 15）。加えて **§4「モデルのせいに見えて違うもの」**（指示書の欠落／検査自体の故障／Non-goals の書き落とし／機構側の誤り）を置き、**workaround を足すのは全部潰した後**と明記。**導線**＝`docs-map.md` へ登録し、playbook §7.2 の注記から相互参照。**★Constraints の独立節化は不要と確定**（照合の誤りを訂正。テンプレ §2.2＋§2.3 が該当） | 2026-08-11 記録 ／ 2026-08-11 **`完了`** |
