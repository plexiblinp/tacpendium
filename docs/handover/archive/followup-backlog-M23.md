# followup-backlog アーカイブ（M23 境界）

> 解決済みの記録を退避したもの。**現役の追跡は `docs/handover/followup-backlog.md` 側**。退避の経緯は `scripts/archive-resolved.sh` の冒頭コメントを参照。

<!-- 退避: M23 境界 -->

### 退避元: K. M20（プリセット管理・カスタムプリセット）期の新規論点（2026-08-12 追加・M20 期設計卓登録）

| スラッグ | 内容 | 割付 | 状態 | 出所 |
|---|---|---|---|---|
| `markdown-it-py-missing-in-cloud-env` | **`scripts/check-md-emphasis.sh` がクラウド実行環境（Claude Code on the web）から実行できない**——`markdown-it-py` が入っていない。**スクリプトは `D-327` の修正どおり「未実行（緑ではない）」を正しく返すが、`check-artifact-integrity.sh`〔★1 本目に回す検査〕が違反 1 件を報告しつづける。** **★`.devcontainer/Dockerfile` へは導入済みだが、クラウド実行環境には効いていない**（**製造とレビュー担当が独立に同じ状態を観測**。**M20-02 横断課題 14 と同型**）。**★設計卓の環境では実行できている**——**環境によって「回せる検査」が違う状態そのものが問題である** | **★畳んだ**（2026-08-19＝**D-452**。**⇒ `clean-clone-missing-toolchain-deps`**〔§S〕） | **★★解消**（2026-08-19・`IMPROVE-01`）。**`check-artifact-integrity.sh` が「違反なし」を返す。** **★`M22-01` から数えて再発しつづけていたが、SessionStart hook で閉じた。** 以下は前回の記述： **★時期確定・未着手**（**恒久策は開発者＝環境構築の手番**。**2026-08-13 開発者判断**＝ローカル環境にも `pip`／Python 環境が無く確認できないため後で対処する）。 **★【2026-08-15 更新】クラウドセッションで再現を確認した。** **`pip install markdown-it-py` で当該セッション内は解消する**〔M22 期の設計卓が実際にそうして検査を回した〕。**★踏むと `check-artifact-integrity.sh` が 1 本目で赤になるため、必ず入口で気づく形にはなっている**——**`check-md-emphasis.sh` 自身が「未実行として非ゼロ終了・緑を返さない」陽性対照を持っており、設計どおり働いた**（教訓 `E-134`）。**⇒ 実害は「毎回 1 手増える」ことに縮んでいる** | **M21-02 設計伝達レポート §4-6-2** ／ **M20-03 §4-5**（**2 サブが独立に観測**） ／ ボード **D-347**・**D-348** ／ **改善レーン 第 1 束 報告 §4**（3 度目の観測） |

### 退避元: L. M22（協調基盤）期の新規論点（2026-08-15 追加・`M22-RESEARCH-01` 由来・M22 期設計卓登録）

| スラッグ | 内容 | 割付 | 状態 | 出所 |
|---|---|---|---|---|
| `conflict-error-code-inconsistency` | **同一の楽観排他衝突に対し、`combos` 系が `conflict`、`setups` 系が `version_conflict` を返す。** 加えて**ハンドラ層テストの固定強度も非対称**（setups はコード文字列まで固定、combos はステータス 409 のみ）。フロントの 409 判別は `web/src/features/setup/errors.ts` が setups 側のみを持つ | **`M22-04`**（競合時の UX。**★409 の見せ方を決めるサブと同じ面である**）→ **★実際に解いたのは `M22-03`**（**D-406** で割付を変更した） | **★解消（2026-08-16・D-413）**。**`version_conflict` へ統一済み。** **★ハンドラ層テストの固定強度の非対称も解消**（`combos` / `setups` ともコード文字列まで固定）。**★総称 `conflict` ではなく `version_conflict` を正とした理由＝409 は既に一意制約違反で使われており、総称を版不一致に割り当ててはならない**（`DES-005` §5.11） | `M22-RESEARCH-01` A-3 / A-5 / §8-3 / §11-2 ／ `M22-03` 設計伝達レポート §4-8 |
| `des002-4-2-lacks-conflict-contract` | **`DES-002` §4.2 に版不一致時のステータス・エラーコード・本文の規定が無い。** 楽観排他の規定は §6.4 の方針一文のみで、**実装の 409 / `conflict` / `version_conflict` を照合できる記述が設計書に存在しない** | **`M22-03` / `M22-04` の反映時に設計卓が書き足す**（**★設計書の改訂は設計担当の手番**） | **★解消（2026-08-16・D-414）**。**`CHANGE-114` を反映し、`DES-002` §4.2 へ「版不一致時の応答」を新設・§6.4 を全面改訂した**（`DES-002` v1.52.0〔第50版〕）。**★あわせて契約の例外〔`PUT` に負けた側は 404〕も書いた** | `M22-RESEARCH-01` A-3 / §9-B-5 / §11-3 ／ `M22-03` 設計伝達レポート §4-8・§6 |
| `csrf-design-vs-implementation-gap` | **`DES-002` §4.4 が定める CSRF 対策 2 本が、いずれも実装に存在しない。** (1)「SameSite=Strict Cookie」——**Cookie を発行・検証するコードが 0 件**（`SameSite` / `Set-Cookie` / `document.cookie` の全走査 0 件）。(2)「`X-Requested-With` を要求し CORS preflight と組み合わせる」——**サーバ側で要求・検証する実装が 0 件**。フロントは `PUT /api/config` の 1 経路だけ付与し、他 35 件の非 GET ルートには付けていない。**★2026-08-15 更新＝(1) の前提が埋まった**（`M22-01`）——**`SameSite=Strict` の Cookie が発行されるようになったため、第一対策は成立可能になった。** **従来は「Cookie を発行するコードが 0 件」で前提そのものが無かった。** **(2) の `X-Requested-With` の検証は依然 0 件** | **`M22-05`**（CORS/CSRF の境界） | **★解消（2026-08-17・D-425）**。**(1) の `SameSite=Strict` Cookie は `M22-01` で成立し、(2) の `X-Requested-With` は `CHANGE-116` で撤回した**〔**「未実装」ではなく「要らないと判断した」＋理由 3 点を `DES-002` §4.4 へ書いた**〕。**⇒ 「設計書に書いてあるのに実装 0 件」の状態が消えた。** **★フロント側の付与 6 経路も撤去済み**〔起票時の実測「`PUT /api/config` の 1 経路だけ」は陳腐化しており、`M22-01` が `authApi`・`M22-02` が `userApi` へ複製していた〕 | `M22-RESEARCH-01` E-3 / §9-B-1 / §9-B-2 / §11-4 |
| `combo-editor-version-zero-fallback` | **`ComboEditor.buildPatchPayload` が `initial` 不在時に `version: 0` を送る**（`web/src/features/combo/components/ComboEditor.tsx:237` `version: initial?.version ?? 0`）。**`combos.version` は `NOT NULL DEFAULT 1` であり、`version = 0` の行が生成される経路は無い**（新規行の初期値はすべて 1） | **`M22-03`**（楽観排他の実効化。**★「version を持たない経路を塞ぐ」の隣にある問題である**） | **★解消（2026-08-16・D-413）**。**`?? 0` を消し、型で不在を表せなくした**（`buildPatchPayload` / `runPatch` の引数化）。**`ComboEditor.test.tsx` で固定済み。** **★props 型の判別可能ユニオン化には踏み込んでいない**——**編集画面の型設計へ手が入り、本サブのスコープを越えるため**（製造の判断・`M22-03` §3-2 の 1） | `M22-RESEARCH-01` A-6 / §8-4 / §11-7 ／ `M22-03` 設計伝達レポート §4-8 |
| `qr-already-implemented-m22-06-scope` | **QR コード表示は実装済み・配線済みである**（`qrcode.react@4.2.0` ／ `QRCodeModal.tsx` ／ `SettingsSectionNetwork.tsx:96-110` ／ i18n 4 キー ／ テスト 3 ケース）。**`M22-06` 指示書 v1.0.0 は「未実装」を前提に書かれており、前提が誤っていた** | **`M22-06`**（**★2026-08-15 に指示書を v2.0.0 へ全面改訂し、「検証と穴埋め」サブへ性格を変えた**＝**D-393**） | **★完了・畳んだ**（2026-08-18＝**D-433**）。**`M22-06` の実施が完了し、`CHANGE-117` として `DES-005` §5.16 へ反映済み。** **★結論は「穴は 1 件も無かった」であり、本番コードの差分は 0 バイトである。** 以下は前回の記述： **★対処済み**（指示書 v2.0.0 ／ チェックリスト v2.0.0 を発行。**残るのは製造の実施**） | `M22-RESEARCH-01` F-4 / §8-1 / §11-9 |

### 退避元: P. M22-04（競合したときの利用者体験）着手前実査由来の新規論点（2026-08-16 追加・M22 期設計卓登録）

| スラッグ | 内容 | 割付 | 状態 | 出所 |
|---|---|---|---|---|
| **`put-key-change-loser-has-no-way-to-save`** | **★キー変更編集に負けた側が、入力を抱えたまま保存する道を失う。** タブ A がキー項目を変えて保存すると旧行が論理削除され新 id で採番し直される ⇒ タブ B の保存は 404 になり、**`M22-04` v1.4.0 までの競合モーダルは「閉じる」しか導線を持たない。** 「読み込み直す」も「ほかの人の内容を見る」も指す先が無い。**★あわせて、タブ B が参照画面へ戻ると旧 id を取りに行き `HTTP 404: {"error":…}` が生のまま表示される** | **★`M22-04` の追補**（2026-08-16・**D-417**。指示書 v1.5.0 §4.7）——**足したのは 2 本**〔**この内容で新しく登録する**〔`POST /api/combos`・2 段階〕**／ コンボ一覧へ**〕。**★サーバ変更は実際に不要だった**（`internal/` diff 0）。**★生の 404 表示は面が外であり別行**（`raw-error-body-shown-in-generic-error-display`） | **★解消（2026-08-16・D-418）**。**同一セッション内で実施済み。** **破壊確認 C**〔赤 9 本 ＋ 第 2 注入で赤 4 本〕**で導線の到達可能性を固定した。** **★行き止まりを固定していたのは E2E ではなく `ConflictDialog.test.tsx` の `notFound` describe だった**——**同 describe が `onSaveAsNew` / `onGoToList` を渡さない props でレンダリングし、「閉じる」だけの絵姿を部品層で緑のまま保存していた。追補のレビューで是正済み** | `M22-04` 設計伝達レポート §2-1・§4-2 ／ **開発者の実機確認（2026-08-16）** |
