# followup-backlog アーカイブ（M22 境界）

> 解決済みの記録を退避したもの。**現役の追跡は `docs/handover/followup-backlog.md` 側**。退避の経緯は `scripts/archive-resolved.sh` の冒頭コメントを参照。

<!-- 退避: M22 境界 -->

### 退避元: K. M20（プリセット管理・カスタムプリセット）期の新規論点（2026-08-12 追加・M20 期設計卓登録）

| スラッグ | 内容 | 割付 | 状態 | 出所 |
|---|---|---|---|---|
| `browser-storage-ledger-calibration-targets` | ~~**ブラウザストレージ台帳の用途欄に、登録対象の件数・内容が現れない。** `web/CLAUDE.md` §1 は「保存する情報の内容」を用途欄に書く運用だが、`gamepad-profiles-v1` の用途欄は「機体ごとの論理ボタン → 物理ボタン対応表」のままで、M21-04 が登録対象を 13 → 14 へ増やした事実が読み取れない。~~ **★2026-08-14・`M21-05` で解消した。** #8 の用途欄へ **14 件の内訳**（方向 4 ＋ 攻撃 6 の必須 10 ＋ マクロ 3 ＋ 操作用の前置き 1）と、保持する情報（物理 index とその読み取り方）を明記し、実装欄へ「M21-04 で 13 → 14（CHANGE-108）」を入れた。**あわせて #9 `keyboard-bindings-v1`（16 件）を新設し、「#8 と #9 の件数は別々の理由で決まっており釣られて直さない」旨の脚注も足した。** | ~~`M21-05`~~ | **解消済み（`M21-05`・2026-08-14）** | M21-04 設計伝達レポート §4-3 ／ レビュー低-7 ／ 解消は `M21-05` 完了報告 §4.7・レビュー高-4 |
| `config-toml-write-back-env-asymmetry` | **`PUT /api/config` が env override 適用済みの実行時 cfg を丸ごと書き戻し、E2E 用 `server.port = 47390` / `database.path = "web/e2e/.tmp/combomgr-e2e.db"` を `config.toml` に焼き付けていた。** 2026-08-08 に dev DB へのマイグレ適用確認を 3 回空振りさせ、M20-04 で 9 本目の spec が同経路を踏むことを再確認。**【2026-08-14 根治】永続化用コピーだけをディスク値へ戻し、有効な env override 3 項目（port / DB path / log level）を除外した。** env 無しの書き戻しと初回ファイル不在も回帰テストで固定 | **改善レーン（2026-08-14）** | **完了**（`go test ./...` 全緑 ／ `make e2e` 73 passed ／ E2E 前後の `config.toml` は SHA-256・314 bytes・`cmp` で同一） | `docs/progress/progress-log.md` の 2026-08-08 課題節・2026-08-14 改善レーン索引 |

### 退避元: J. 停止時記録（未解消のまま停止した項目・stop-record）

| ID（スラッグ） | 発生元 | 未解消の理由 | 再開に必要な条件 | 記録日・状態 |
|---|---|---|---|---|
| **code-side-design-desk-migration** | 工程改善 第二波 ／ `docs/progress/20260811-progress-doc-responsibility-rulings.md` §3A-⑥ ＋ サブエージェント可否の調査 | **設計卓を Code 版へ移すかが未決のまま、反映担当だけ廃止した。** 廃止裁定は「**設計担当の面は要件に応じて Web 版・Code 版のいずれでもよい**」としており、面の選択は都度判断に委ねられている。**調査で分かった事実**＝サブエージェントによるクリーンルームレビューは **Claude Code の機能**であり、公式ブログは「terminal, VS Code, JetBrains, **the web**, desktop の全 Claude Code インターフェースで使える」と述べる一方、**claude.ai の通常チャット（現在の設計卓）での起動可否には言及が無い**。**つまり設計卓を `claude.ai/code` へ移すと、反映の直接実施とクリーンルームレビューの両方が同時に手に入る。** 一方で設計卓の運用全体（プロジェクトナレッジ・GitHub 直読・起動キット）を変えることになり、影響が大きい | **開発者との打ち合わせ**（2026-08-11 に「このセッションではなく別途進めたい」と明示・**後日課題**）。決めるべきは (a) Web 版と Code 版をどの基準で使い分けるか (b) 起動キット（`next_milestone_kit` の変換規則）を面ごとに分けるか (c) `docs/handover/design-instruction-playbook.md` の前提（Web 版・全ソース不可視）をどう扱うか。**`roles-and-routing.md` の設計担当の特記が既に「Code 版への部分移行を検討中」と書いており、本項はその継続である** <br>**【2026-08-11 追記】検討資料を `docs/handover/session-prompts/20260811-code-side-design-desk.md` へ用意した。** 選択肢 A（Code 版へ移行）／B（Web 版のまま）／C（設計は Web・反映は Code）を比較し、**推奨は A**。決め手＝**「反映は設計担当が自分で当てる」という廃止裁定が Web 版では物理的に実行できない**。 <br>**【2026-08-11 決着】三択はいずれも採らなかった。** 開発者の意向が「両面で進められるようにしたい／なるべく Code 化／仕事を洗い出してから割当を決めたい」だったため、**決定材料レポート `docs/progress/20260811-design-desk-surface-study.md` を新設**し、そちらで決着させた（同書 §9 に裁定 6 件）。**確定した内容**＝**(1) 設計担当の仕事 19 件のうち 15 件はリスク無〜低・利得 大〜最大で無条件に Code へ**（§2.2）**(2) 論点 4 件も含め全件 Code。Web 版は親をやめ `parallel-ops` §06 の「個別設計チャット」へ配置換え**（§5.2。M20+M21 並列で時間分割が成立しないため）**(3) 担保は「物理排除＋git 系遮断＋接触の記録」の 3 枚**（§3.6）——**★フックによる Read 遮断は成立しないことが実測で判明**（`Bash(cat:*)` `Bash(sed:*)` 等がパス制限なしで allow 済みのため素通りし、**クラウドでは ask も効かない**＝V-2）**(3') 別リポジトリ化は採らない**（E-33／G-5 の再演になるため。§3.7）**(4) 並列は親 1・サブエージェント N・worktree 2**（§4.5）**(5) `parallel-ops.html` の反映係記述を明記**（V2.4 で処理済み）**(6) 完了報告 §2 のベースライン計測**＝**15 本 / 合計 27 件 / 平均 1.8 件**（§8.1 で実施済み）。**★「全ソース不可視」は無料ではなかった**——補償機構を 9 件抱えていた（code-facts 1124 行・docs-map 276 行・playbook §1.4/§4.6/§4.9/§4.10・変換規則 13/15・再生成コマンド・鮮度監査 2 本）。**機構も設置済み**＝`scripts/design-desk-arm.sh`（武装・自己検査 6 項目）＋ `.claude/hooks/design-desk-guard.sh`（`PreToolUse(Bash)`・許可リスト方式・自己検査 29 ケース）＋ `check-artifact-integrity.sh` への登録。**※ `.claude/hooks/` への書き込みはハーネスの分類器に一度拒否され、開発者の明示的な許可を得て設置した**——**フックを追加する作業は開発者の許可を前提に計画すること**（次回も同じことが起きる）。**残るのは運用側の確認 2 件**＝(a) **クラウドで `git sparse-checkout` が実際に通るか**（allow/deny 非該当のため未検証。通らなければクラウドの親運用は物理排除なしになる）(b) **ガードの誤爆率**（M20 の 1 サブで観察）| 2026-08-11 ／ **`完了（運用確認 2 件は M20 起動時。surface-study §9.1・§7-B）`** |
| **m20-kit-derived-docs-line** | 工程改善 第二波 設計卓の Code 化 ／ `.claude/commands/next_milestone_kit.md` 変換規則 19 (I)（2026-08-11 新設） | **変換規則 19 (I) は「起動キット §2 の冒頭に `bash scripts/check-derived-docs.sh` を置く」を定めたが、M20 起動キットは同ルールの新設と同時並行で生成されていたため、この 1 行が入っていない。** 派生資料のメンテがマイルストーン境界に固定されていた問題（M18/M19/M14 の並列期に `retrospective-digest` 等が長期間止まり、**設計卓が「完全に陳腐化した」と誤解して資料を使わなくなった**）の再発防止がねらいで、**M20 の設計卓が起動する前に入っていないと、M20 期も同じ空白が生じる**。**同一ファイルを 2 セッションで触ると衝突する**ため、本セッションでは追記していない | **M20 起動キットの生成が完了してから**、`docs/human-notes/milestone-startup-kit/m20-startup-kit.md` の §2 冒頭（武装手順 `bash scripts/design-desk-arm.sh` の直後）へ 1 行足す: `bash scripts/check-derived-docs.sh`。**あわせて出力の読み方を 1 文添える**——「**『源泉が新しい』は資料が丸ごと無効という意味ではない。変化量（例 10/875 件）が小さければ、変わった領域だけ実物で確認し他の節はそのまま使う**」。**★`code-facts` は武装中に再生成できない**（`generate-code-facts.sh` が exit 2 で停止する）ので、要再生成なら武装していないセッションか開発者へ回す旨も添える。**担当は M20 キット生成セッション側が安全**（同一ファイルの衝突回避）<br>**【2026-08-12 完了・ただし根本原因は記録と違った】** キット本体は **M20 キット生成セッションが適用済み**（§0 手順 5 ／ §1 ／ §2 の 2 手目 ／ 末尾ゲート (1) ／ §5 変換点表 ／ §6 改訂履歴 の 6 箇所。規則 19 (I) が要求する 3 要素をすべて満たす）。**★本項は「同時並行だったので入らなかった」一過性の事故として記録されていたが、実査すると再発する構造だった**——`2a7f080` は規則 19 (I) を**規則表**へ追加したが、**Step 5「プロンプト本文の必須要素の反映確認」（脱落の再発防止チェックリスト）へは追加していなかった**。同チェックリストの規則 19 行は `(a)(b)(c)(d)` だけで **(I) が無く**、`next_phase_kit.md` に至っては**生成規則 17 自体が 1 行も無かった**（規則 7 / 15 / 16 のみ）。**⇒ キット本体を直しても M21 以降で同じ欠落が起きる状態だった。** 改善レーンで両コマンドのチェックリストへ検査行を追加して塞いだ（`next_milestone_kit` 規則 19 に **(e)**、`next_phase_kit` に**生成規則 17 の行を新設**。いずれも「コマンドが在るか」ではなく**3 要素（変化量の読み方／ゲートにしない／`code-facts` は武装中に再生成不可）が揃っているか**を見る形にした）。**★教訓**＝**規則表へ足したら、同じ変更単位で脱落防止チェックリストにも足す**。規則表だけの追加は「書いたが検査されない」状態を作る | 2026-08-11 記録 ／ **`完了（2026-08-12。キット本体＝生成セッション／生成側チェックリスト＝改善レーン）`** |
| **codex-hooks-json-not-dispatched** | 工程改善 第二波 ／ `codex-hook-firing-unverified` の測定結果（2026-08-12）／ `human-notes/codex/README.md` §5 の実測ブロック | **【2026-08-12 解決】旧 `.codex/hooks.json` はトップレベルに `hooks` envelope が無い Claude `settings.json` 形で、Codex 0.147.0 app-server は `unknown field PreToolUse, expected description or hooks` と報告していた。** 公式 `externalAgentConfig/detect` / `externalAgentConfig/import` に `.claude/settings.json` を読ませた生成物から、正しい置き場所は同じ `.codex/hooks.json`、書式は `description` と `hooks` envelope、その内側に `PreToolUse` / `PostToolUse` / `Stop` を置く形と確定した。**旧記録の「Codex に Stop は無い」も誤り**で、公式 `/hooks` と API は `Stop`（turn 終了）と `SessionEnd`（session 終了）を別イベントとして列挙し、importer も `Stop` を生成した。**★誤った理由を残す**（2026-08-12・設計卓が追記。**是正だけ書いて理由を落とすと同じ推論をやり直す**）——**旧記録は「傍証として Stop は Codex のフックイベントに存在しない（Codex は `session_end` / `subagent_stop`）」と書いていたが、これはバイナリの文字列から snake_case の列挙を拾い、それを設定ファイルの受理形式と解釈したものだった。実際にはその列挙は別モジュール（外部エージェント設定の取り込み）のものであり、設定ファイルのイベント名は Claude と同じ PascalCase（`PreToolUse` / `PostToolUse` / `Stop`）のまま正しかった。** **★一般則**——**バイナリ実査は「その語が実行ファイル内に在る」ことしか示さない。どの経路で・どのモジュールで使われるかまでは示さない。** 実際の欠落は**イベント名ではなくトップレベルの `hooks` envelope** だった（`f716d56` で判明）。**「在る」から「受理形式である」への飛躍が誤りの本体である**（`E-117` の一般化＝**現物の一部を見て全体の役割を描写した**）。正本の `.claude/hooks/*.sh` は変更・複製せず直接参照を維持した。Codex の実 `apply_patch` ペイロードは `file_path` でなく `tool_input.command` に patch パスを持つため、Codex 側だけで Add / Update File のパスを抽出して正本へ渡す adapter を加えた。matcher はツール名を独立に実測していないため付けていない | **発火まで実測済み。** 信頼許可後の `hooks/list` は project hook 5 件を enabled / trusted、matcher 全件 null、error / warning なしと報告。開発者が武装マーカーを置いた状態で新規 Codex セッションから shell の `git status --short` を実行すると、`Command blocked by PreToolUse hook: design-desk-guard: 武装マーカーがあるのに実装ソースが作業ツリーに存在します。` に続く前提崩れ説明を含む全出力で exit 2。開発者によるマーカー削除後、未整形の `tmp/hooktest/main.go` を Codex `apply_patch` で編集すると、`[品質フック] tmp/hooktest/main.go の編集後チェックで指摘:` と `gofmt: 未フォーマット (tmp/hooktest/main.go)。` が表示された。検証用 `tmp/hooktest` は削除済み。`.codex/hooks.json` の警告 description も実証済みへ更新した。**完了条件は設定保存ではなく PreToolUse / PostToolUse の実発火で満たした。** `docs/process/remote-ops.md` §6 の「Codex では層が存在しない」「Stop は存在しない」は是正が必要だが、Claude 側正本のため本作業では変更せず差分を報告する | 2026-08-12 ／ **`完了（Codex 0.147.0 実セッションで PreToolUse の exit 2 と PostToolUse の品質指摘を実測）`** |
| **preset-config-preset-id-existence** | M20-04 ／ `docs/progress/m20-04-review.md` 指摘 E（中 7） | **`SUPP-001` §7.3 が名指しした 3 点のうち 1 つが半分残っている。** 同節は「M20-04 でプリセット作成 UI を作るとき、403 保護・上限検査・**`config.preset_id` の実在確認**（**D-313**）が同時に要る」とする。M20-04 が実装したのは**削除側の拒否**（`ErrInUseByConfig` → 409 `preset_in_use_by_config`）だけで、**`config` 側が実在しない `preset_id` を指せる状態は残っている**——`internal/service/config/service.go:227` の検証は `PresetID < 1` のみで DB 上の実在を見ない。⇒ (1) `config.toml` の手編集、(2) M20-04 の経路を通らずに消えたプリセット、では依然として起動時に実在しない preset を引く。**M20-04 で実装しなかった理由**: config サービスに DB 依存（プリセットリポジトリ）を持たせる変更になり、**preset ドメインに閉じよという指示書 §2.3 と、他ドメインへ触るなら止めよという §9.4 停止条件 3 の両方に当たる**。独断で他ドメインの依存関係を変えない。 | **設計卓の裁定**——(a) `configsvc` に preset リポジトリを注入して起動時・更新時に実在検証する、(b) 起動時に不正なら既定値 `1` へフォールバックしてログを出す、(c) 本サブ範囲外として据え置く、のいずれか。**`M20-05` が `SUPP-001` §7.1・§7.2 の as-built 是正を持つため、同サブで併せて扱うのが自然**。 **★【2026-08-14 解消＝`M20-05`】** 更新時（`PUT /api/config` が 422）と起動時（組み込み既定へフォールバック ＋ WARN）の両方を実装した。**config サービスへは関数注入（`PresetExistsFunc` / `DefaultPresetChangedFunc`）で渡し、DB・preset・notation への直接依存を増やしていない**——**本項が「実装しなかった理由は正しい」と記録した制約を保ったまま解いている。** 詳細は `docs/progress/M20-05-completion-report.md` §2-3 ／ **D-363**。 | 2026-08-13 記録 ／ **`完了`（2026-08-14・M20-05）** |
