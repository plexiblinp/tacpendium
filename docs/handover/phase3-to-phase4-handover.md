# フェーズ3 → フェーズ4 引き継ぎ書

| 項目 | 内容 |
|------|------|
| 文書ID | PHASE3-TO-PHASE4-HANDOVER |
| バージョン | **1.1.0**（2026-08-31）<br>**★本セルは現行版と日付だけを持つ。変更の内容は §改訂履歴へ書く** |
| 発行 | 設計担当 Claude（親チャット・設計卓・M24 期）/ 2026-08-31 |
| 対象 | **フェーズ3 全体**（`M13`〜`M24`。**★`M14` は独立レーンとして継続中**） |
| 配置 | `docs/handover/phase3-to-phase4-handover.md`（**`phase2-to-phase3-handover.md` と同じ運用**。`check-doc-inventory` の型に合致） |
| 併読 | **`docs/handover/m24-close-report.md`**（M24 の締め。**本書と同時に発行した＝`D-626` / `D-627`**） |

## 改訂履歴

| 版 | 更新日 | 更新内容 |
|----|--------|---------|
| **1.1.0** | **2026-08-31** | **★改善レーン 第 2 束の完了を受けて更新した**（**D-630**）。**§5.1 へ手順 0**〔申し送りを拾う〕**を新設 ／ §5.4 新設**〔`next_phase_kit` 担当への注意〕。 |
| 1.0.0 | 2026-08-31 | 初版（`D-627`） |

---

## §0. ★★本書を読む前に知っておくこと

**★★フェーズ4 は「スコープ再編」から始まる。** 開発者の意図（2026-08-31・逐語の要約）は次のとおりである。

> **リリースまでにやるべき事はまだ残っている。それをやるのがフェーズ4。本当に後回しでいいのがフェーズ5。`future-notes` はフェーズ6 以降のイメージ。**

**★★ここに重要な食い違いがある。`REQ-001` §7 の現行のフェーズ4 定義は、これと別物である。**

| | 現行 `REQ-001` §7 のフェーズ4 | 開発者が意図するフェーズ4 |
|---|---|---|
| 名前 | **外部データ追従・拡張** | **リリースまでにやるべき事** |
| 中身 | **3 項目だけ**——ゲームアップデート追従（`FR702`） ／ 他ゲーム対応の基盤（`NFR407`） ／ モダン操作対応（`ISSUE-007`） | **未定**（本書 §3 が材料を出す） |

**⇒ 再編は「現行フェーズ4 に足す」形にはならない。** **現行の 3 項目はいずれも「リリースまでにやるべき事」ではないため、フェーズ5 以降へ送られる公算が高い。** **★つまり作業は「フェーズ4 を作り直し、フェーズ5・フェーズ6 を新設する」ことである。**

**★★これは `REQ-001` §7 の改訂であり CHANGE が要る。** **フェーズ4 は新しい CHANGE ブロック `150`〜 を切ること**（`D-626`）。**`149` は M24 ブロックの残りであり、流用しない。**

---

## §1. フェーズ3 完了状態（**検証済み**）

### 1.1 マイルストーン

| MS | 内容 | 状態 |
|---|---|---|
| `M13` | データ共有・データ保護（export/import） | **完了** |
| **`M14`** | 公式データ配布是正・スキーマ整理・DB 同梱 | **★★独立レーンとして継続中**（§4-1） |
| `M15` | 入力・使いやすさ向上 ＋ コマンド解決 段階1 | **完了** |
| `M16` | データモデル拡充・スキーマの継ぎ目 | **完了** |
| `M17` | 発信者の流通基盤（取込ヘルパー・メディア参照） | **完了** |
| `M18` | 確定反撃記録（別コンボ materialize） | **完了**（2026-07-31 クローズ） |
| `M19` | セットプレイ自動提案 | **完了** |
| `M20` | プリセット管理・カスタムプリセット | **完了** |
| `M21` | 物理コントローラ入力 ＋ コマンド実モーション | **完了** |
| `M22` | 協調基盤（簡易ログイン・楽観排他・QR・CORS/CSRF） | **完了** |
| `M23` | データバージョン管理（`FR601`・ゴミ箱／復元） | **完了**（2026-08-24 クローズ） |
| **`M24`** | UX 整理・i18n・繰越・リファクタ ＋ 蓄積した改善要望 | **★完了**（2026-08-31 クローズ＝`m24-close-report.md`） |

### 1.2 設計書本体の現行版（**フェーズ4 着手時の基準**）

| 文書 | 版 |
|---|---|
| `REQ-001` | **2.25.0** |
| `DES-001` | 1.4.0 |
| `DES-002` | **1.72.0** |
| `DES-003` | **1.63.0** |
| `DES-004` | **1.26.0** |
| `DES-005` | **2.89.0** |
| `DES-006` | **1.38.0** |
| `SUPP-001` | **1.49.0** |

> **★版数の正本は各文書の「バージョン」セルである**（`D-67`）。**ボード §2.3 は写しであり、過去に 3 度失効している。⇒ 疑ったら実物を見ること。**

### 1.3 採番の現在地

| 対象 | 値 |
|---|---|
| **CHANGE** | **次に採番するのは `149`**（M24 追加ブロックの残り 1 本。**★M24 に使い道が無かったため未消費のまま残した**）。**★★フェーズ4 は新ブロック `150`〜 を切ること**（`D-626`） |
| **マイグレ連番** | **次に払い出すのは `000081`**（`000080` は `M24-08` が消費） |
| **裁定** | 次は **`D-628`** |
| **計測点** | 次は **`M-106`** |

---

## §2. フェーズ3 で確定した主要判断（**フェーズ4 が前提にすべき**）

| # | 判断 | 出所 |
|---|---|---|
| 1 | **★保持は無期限であり、期限による自動完全削除は持たない** | `D-458` ／ `DES-003` §5 |
| 2 | **★`PUT`（識別キーを変える編集）が積む旧行は、ゴミ箱の既定の一覧に出さない。変更履歴ビューは作らない** | `D-460` ／ `CHANGE-121` |
| 3 | **★差分の自動提示は作らない。ただし否定しているのは「ステップの配列の対応付けを要する比較 UI」であって、コンボの差分提示一般ではない** | `D-415` ／ **射程の確定＝`D-622`** |
| 4 | **★書き込みトランザクションは `BEGIN IMMEDIATE` で開く。`SQLITE_BUSY` の応答は 503 `database_busy`** | `CHANGE-136` ／ `DES-002` §6.4 |
| 5 | **★E2E は nightly で回す。`pr-checks.yml` には載せない**（**旧契約「E2E は CI に載せない」は撤回した**） | `D-606` ／ `DES-002` §11.3 |
| 6 | **★`FR401` の射程に「全データの保全」は含まない。バックアップ／リストアの正本は `NFR201` ＋ `DES-002` §6.3 である** | `CHANGE-146` ／ `D-607` |
| 7 | **★`queryKey` の正本は `web/src/lib/query-keys.ts` の `queryKeys` ファクトリである。★3 箇所だけは平坦化していない**（前方一致・部分一致に依存して当たっている） | `CO-009` ／ `D-620` |
| 8 | **★`VAL-P01` / `VAL-P07` はキャッシュ保守を含まない**（`POST /api/presets/{id}/recipe-cache/rebuild` の 1 本だけ） | `D-621` |
| 9 | **★契約 F-1 / F-2 は「並列のためのレーン分離条項」であり全面凍結ではない。F-2 はモデルの契約であって表記法の凍結ではない** | `D-614` ／ `D-619` |
| 10 | **★残り 14 キャラの seed 波は `M14` の所管である。M24 にも M25 にも入れない** | `D-610` |
| 11 | **★アプリ正式名称は `Tacpendium` を推す。** ~~ただし法的な商標クリアランスは未実施である~~ **★★【2026-09-02 errata＝`D-636`】「未実施」は失効していた。5 点すべて完了している。** **1 商標登録**〔J-PlatPat / USPTO / EUIPO〕**＝結果なし ／ 2 ドメイン WHOIS ＝結果なし ／ 3 レジストリ・ストア名の重複**〔GitHub / npm / Homebrew / Microsoft Store / Mac App Store / Steam **＋ Google Play**〕**＝一致なし ／ 4 SEO ＝考慮済み ／ 5 発音・意味 ＝考慮済み。** **⇒ `Tacpendium` は名称として確定してよい。残るのは波及先への反映（`checklist` `A5`）だけである。** **★「やっていなかった」のではなく「結果がボードへ返っていなかった」形である**〔4・5 はネーミング支援担当 AI との間で閉じていた〕。 | `D-474` ／ **errata＝`D-635`** |
| 12 | **★「消す」変更は開発者の手番である。Claude は候補と根拠を出すところまで** | `D-196` 境界条件 3 |

---

## §3. フェーズ4 のスコープ再編に使う材料（**3 層への仕分けの入力**）

**★★本節は「フェーズ4 の残作業カタログ」ではない。仕分けの入力である。** **⇒ どれがフェーズ4 でどれがフェーズ5 かは、まだ決まっていない。**

### 3-1. 材料の在り処（**4 つに散っている**）

| # | 源泉 | 中身 | 件数 |
|---|---|---|---:|
| **1** | **`M24-RESEARCH-01` 報告 §2.1 の `返却` 内訳** | **`返却（フェーズ4）` の `SM` 行**——`SM-023`〜`SM-035` ／ `SM-042` ／ `SM-046` ／ `SM-050` ／ `SM-081` ／ `SM-087` ／ `SM-100` ／ `SM-105` ／ `SM-106` ／ `SM-116` ／ `SM-123`〜`SM-125` ／ `SM-134` ／ `SM-135` ／ `SM-138` ／ `SM-147` | **30** |
| **2** | 同 §2.1 | **`返却（正式リリース前後）`**——コード署名 ／ AV 誤検知対策 ／ 別リポジトリ化 ／ 不要ファイルの除去 ／ 正式名称 | **5** |
| **3** | `followup-backlog.md` の M24 期セクション（**§AI 〜 §AY**） | **M24 の 17 サブが積んだ新規論点**。**★`CO-026`（英語ロケール整備）／ `co023-g15-not-fully-closed` ／ `combo-step-unit-word-deferred` など、M24 が明示的に持ち越したものを含む** | 概算 **100+** |
| **4** | `docs/human-notes/future-notes/` | **開発者の構想メモ群**（AI 連携 ／ 発信者向けピボット ／ HTML エクスポート ／ 別ゲーム対応 等） | — |

### 3-2. ★★仕分けの前に決めるべきこと 2 つ

| # | 論点 | なぜ先に決めるか |
|---|---|---|
| **1** | **★「リリース」とは何を指すか。** 先行リリース（近しい関係者への試用提供）は**フェーズ2 で済んでいる**（`REQ-001` §7）。**⇒ ここで言うリリースは「正式リリース」である**と読めるが、**その到達条件が `REQ-001` に書かれていない** | **★到達条件が無いと「リリースまでにやるべき事」を判定できない。** **⇒ 3 層の仕分けが、判定基準の無い多数決になる** |
| **2** | **★現行 `REQ-001` §7 のフェーズ4 の 3 項目**〔`FR702` / `NFR407` / `ISSUE-007`〕**をどこへ送るか** | **★どれも「リリースまでに要る」とは読めない。⇒ フェーズ5 へ送る公算が高いが、`FR702`（ゲームアップデート追従）だけは性格が違う**——**SF6 は運営中のタイトルであり、アップデートが来れば既存データが古くなる。「後回しでよい」と言い切れるかは開発者の判断が要る** |

### 3-3. ★仕分けの進め方の推奨（**設計卓の提案・決めるのは開発者**）

**★★`overview` を書きながら同時に仕分けると、必ずどちらかが粗くなる。** **⇒ 仕分けを RESEARCH 1 本として先に回すことを勧める。**

- **先例＝`M24-RESEARCH-01`。** 同 RESEARCH が二系統 ledger（160 行 ＋ 26 項目）を作ったからこそ、`M24-overview` のサブ分割 16 本が成立した。
- **★RESEARCH の成果物は「3 層それぞれの ledger」である**〔フェーズ4 ／ フェーズ5 ／ フェーズ6 以降〕。**各行に `disposition` と根拠を付ける。**
- **★★判定できない行は「要判断」に倒し、0 件にしない**（`M24-RESEARCH-01` §0.3 と同じ規律）。**⇒ 無理に振り分けると、根拠の無い分類が正本になる。**

---

## §4. 引き渡す宿題

### 4-1. ★★`M14` レーン（**フェーズ4 担当が処理する**・開発者判断 2026-08-31）

| # | 何を |
|---|---|
| 1 | **`M14-RESEARCH-03` を投入する**（`docs/instructions/M14-RESEARCH-03-seed-wave-work-inventory.md` v1.0.0・**起票済み・未投入**）。**★入力の CSV が揃うのを待っている**〔`c_viper` / `dhalsim` が最後〕 |
| 2 | **★核心は §2.2-9**——**第三波（2026-08-01 以降）の 7 MS で積まれたものを `000059` ⇄ `000079` の列差分で探させる。** **⇒ 「seed 波 1 回あたりの工程の全数」は設計卓が数えた 5 種類では足りない可能性がある** |
| 3 | **★残キャラの数が合わない**——seed 済み 17 ＋ 開発者の列挙 12 ＝ 29 ／ 全 31。**差 2 が未説明である**〔`yasmine` が列挙に無い ＋ 不明 1〕。**§2.1-6 で名指しさせること** |
| 4 | **★`SM-051`（キャラ増加時の importer 側の見直し）を本レーンで拾う**（`m24-close-report.md` §3-1 で確定した行き先） |

### 4-2. ★フェーズ4 の要件整理で名指しすべきもの

| # | 何を | なぜ |
|---|---|---|
| 1 | **`SM-098`（確定反撃での D リバの扱い）** | **★M18 は 2026-07-31 にクローズしたが本件は扱われておらず、M18 の指示書・完了報告のいずれにも「リバーサル」の記載がゼロである。** **⇒ 実装の宿題ではなく「そもそも何を扱うか」が未定である。開発者のドメイン知識が要る** |
| 2 | **`SM-080` / `SM-149` / `CO-020`（仮想コントローラの面）** | **同じ共通部品**（`HitBoxLayout.tsx`）**を触る 3 件。ボタン名 ／ レイアウト ／ i18n。1 面としてまとめるほうが安い** |
| 3 | **`CO-026`（英語ロケール整備）** | **★M24 は未完成のまま出荷すると決めた**（開発者判断）。**⇒ フェーズ4 で「どこまでやるか」を決める。★全画面の訳文見直しは M24 で明示的に射程外にしてある** |
| 4 | **`SM-016`（コンボの 1 工程を指す単位語）** | **★開発者判断で M24 では見送った。技名「前方ステップ」との衝突は残っている**（`D-615` / `D-616`）。**★単独サブにする価値がある** |

### 4-3. ★開発者の手番（**`m24-close-report.md` §3-3 と同じ**）

**PR 検査を必須へ戻す ／ `CO-016` の消化確認 ／ `docs/progress/` の滞留資料の仕分け ／ `code-facts.md` の再生成 ／ 正式名称の商標クリアランス。**

> **★★あわせて `docs/progress/progress-summary.md` を 2 MS ぶん進めること**（**最終節が `M22`・2026-08-18。`M23` と `M24` の節が無い**）。
>
> **★これは効く**——**`CLAUDE.md` §1 が「現在地は本ファイルに書かない。`progress-summary` を参照すること」と定めている。**
> **⇒ フェーズ4 のキックオフ担当は、ここを見て「まだ M22 だ」と読む。**
>
> **★設計卓は代行できない**（`progress-log` を読まない規律）。**`/update_progress_summary` は製造側か開発者の手番である。**

---

## §5. フェーズ4 キックオフ担当への引き継ぎ（**最重要**）

### 5.1 ★★最初にやること（**この順で**）

0. **★★改善レーンの申し送りを拾う**（**2026-08-31 追加＝`D-630`**）。**改善レーン 第 2 束は完了したが、一部を「スコープを超える可能性がある」として申し送りにした。** **★在り処は 2 つ**——**(a) 改善レーンの完了報告**〔`docs/progress/improve-NN-completion-report.md`〕**／ (b) `followup-backlog.md` §J 停止時記録**〔**改善レーンが本表を書けるのは §J だけである**＝`D-382`〕。 **★★やること＝申し送りを 1 件ずつ読み、`followup-backlog` の本表へ畳む。** **畳めるのは設計卓だけである**（`D-382`）。**⇒ 畳まないと §J に溜まったまま、次の誰も本表から見つけられない。** **★M24 期の設計卓は本作業を行っていない**——**申し送りの中身を読んでいないため**（改善レーンの成果は別ブランチに在り、武装中の作業ツリーに来ていない）。
1. **改善担当のレーンが先に入る**（開発者判断・2026-08-31）。**★フェーズ4 の準備はその後である**——**改善の成果**〔検査の追加・プロセスの是正〕**はフェーズ4 の指示書の書き方に効くため、逆順だと `overview` を書き直すことになる。**
2. **`next_phase_kit` を生成する**（**★改善担当の後。docs が動くため**）。
3. **§3-2 の 2 つを開発者へ問う**〔リリースの到達条件 ／ 現行フェーズ4 の 3 項目の行き先〕。**★これが決まらないと 3 層の仕分けができない。**
4. **仕分けの RESEARCH を 1 本回す**（§3-3）。
5. **`phase4-overview.md` を書く。** **★`REQ-001` §7 の改訂は CHANGE ブロック `150`〜 を切って行う。**

### 5.2 ★最初に読むもの（**順序**）

| # | 文書 | なぜ |
|---|---|---|
| 1 | `CLAUDE.md` | プロジェクト指針。**★現在地は書いていない**（陳腐化するため） |
| 2 | **本書** | フェーズの締めと再編の前提 |
| 3 | `docs/handover/m24-close-report.md` | 直前の MS の締め |
| 4 | `docs/handover/roles-and-routing.md` | **誰に聞くか・何を自分で決めてよいか** |
| 5 | `docs/handover/design-instruction-playbook.md` | **★61 節ある。★4.51 / §4.57 / §4.58 / §4.59 は M24 期に生まれた新しいもので、いずれも「同じ失敗を 2 度した」記録である** |
| 6 | `docs/process/parallel-board.md` | **裁定・保留・レーン状況の正本** |
| 7 | `docs/handover/followup-backlog.md` | **★§AI 〜 §AY が M24 期。★§J は製造の領域であり、設計卓は本表だけを書く**（`D-382`） |
| 8 | `docs/handover/code-facts.md` | **★2026-08-31 時点で古い**（変化量 16%）。**★再生成されているか確認してから引くこと** |

### 5.3 ★運用ルールの継承（**落とすと事故る**）

| # | ルール |
|---|---|
| 1 | **★設計卓は武装する**（`scripts/design-desk-arm.sh`）。**実装ソースを読まない。「ある」も「無い」も断定しない。** **★事実は `code-facts.md` ／ Explore サブエージェント ／ 指示書 §3.3 実査項目の 3 つからしか取らない** |
| 2 | **★★M24 期で最も重い失敗は、この規則を設計卓自身が破ったことである**（計測点 `M-104`）。**「既存の再構築機構に任せる」と書いたが、その機構は `disabled` のプレースホルダで API すら無かった。★根拠にしたのは設計書の記述である。** **⇒ 設計書は実在の証拠にならない**（playbook §4.58） |
| 3 | **★実装前に `DES` 本体を 1 文字も触らない。** 起票時のゲートは通知書 ＋ registry 登録だけ。**改訂は as-built 確定後、change-report と同時。** |
| 4 | **★過去の裁定を上書きするなら、上書きする裁定を名指しして「撤回する」と書く。** **権限は明示的な 1 行によってのみ渡る** |
| 5 | **★採番はブロック事前割当。写し先が複数あるので、片方だけ直さない**（`D-277`） |
| 6 | **★裁定を出したらボード §3.4 へその場で追記する。** ボードを編集してよいのは設計卓だけ |
| 7 | **★成果物を書いたら検査を回す**——`check-artifact-integrity.sh`（**1 本目**） ／ `check-md-emphasis.sh` ／ `check-instruction-format.sh` ／ `check-doc-refs.sh` ／ `check-stop-discipline.sh` ／ `check-progress-log-index.sh` ／ `check-doc-inventory.sh` |
| 8 | **★停止規律。** 上限に達したら `followup-backlog` §J へ必須 5 フィールドで記録して直ちに停止する。**記録して停止することは正規の完了形式である** |

### 5.4 ★★`next_phase_kit` 担当への注意（**2026-08-31 追加＝`D-630`**）

**★本書はキットの入力である。⇒ キットが本書を要約するとき、次の 4 点は落とさないこと。**

| # | 落としてはいけないもの | なぜ |
|---|---|---|
| 1 | **§0 の食い違い**〔現行 `REQ-001` §7 のフェーズ4 定義 ≠ 開発者が意図するフェーズ4〕 | **★これを落とすと、キックオフ担当が「フェーズ4 は外部データ追従だ」と読んで、まったく違う overview を書き始める。** **本書で唯一「前提が壊れている」と告げている節である** |
| 2 | **§3-2 の未決 2 件**〔リリースの到達条件 ／ 現行 3 項目の行き先〕 | **★どちらも開発者にしか答えられない。★これを問う前に `overview` を書き始めないこと** |
| 3 | **採番**〔**CHANGE は `150`〜 の新ブロック。`149` は流用しない**〕 | **★流用するとブロックの境界が消え、どの MS の番号か辿れなくなる**（`D-626`） |
| 4 | **§5.3 の運用ルール 8 件**（とくに **1**〔設計卓は武装する〕**と 2**〔M24 期はその規則を設計卓自身が破った＝`M-104`〕） | **★キットが「読むべき文書の一覧」だけになると、規律が落ちる。★M24 期で最も高くついた失敗がここである** |

**★★キットは生成物である。生成したら、上の 4 点が本文に残っているかを目視で確かめること。** **⇒ 「生成した」と「落ちていない」は別の主張である**（playbook §4.58 と同じ型）。

**★あわせて `docs/progress/progress-summary.md` の遅れに注意する**（§4-3）。**キットが現在地を同書から引くなら、2 MS 古い値を拾う。⇒ 先に `/update_progress_summary` を回すか、キットの当該欄を本書 §1.1 で上書きすること。**

---

## §6. ★フェーズ3 の総括（**1 段引いた見方**）

**★フェーズ3 は「共有・協調・入力拡充」として始まり、途中で「発信者中心へ舵切り」した**（`CLAUDE.md` §1）。**⇒ M17（発信者の流通基盤）・M18（確定反撃）・M19（セットプレイ自動提案）はその舵切りの産物である。**

**★★そして M24 は、舵切りの代償を払う MS だった。** 12 の MS を走らせた結果、**行き先の無い項目が 160 行 ＋ 26 項目たまっていた。** M24 はそれを 1 件ずつ仕分けて片づけた。

**★★フェーズ4 が同じ状態にならないために、1 つだけ言い置く。** **M24 が高くついたのは「片づけを最後にまとめてやった」ことではなく、「片づける単位が MS 単位ではなく memo 行単位になっていた」ことである。** **`M24-RESEARCH-01` が 6 軸の調査を回してようやく母集団が確定した。**

**⇒ フェーズ4 では、各 MS のクローズ報告に「この MS へ割り付けられた行がすべて処理されたか」の節を必ず置くこと**（followup `milestone-close-does-not-close-assigned-items`。**★M18 / M22 で実際に落ちて、M24 まで宙に浮いた項目が 4 件ある**）。

---

*以上、フェーズ3 → フェーズ4 引き継ぎ書。* **★★最初の一手は「スコープ再編の前提 2 つ」を開発者へ問うことである**（§3-2）。**それより先に `overview` を書き始めないこと。**
