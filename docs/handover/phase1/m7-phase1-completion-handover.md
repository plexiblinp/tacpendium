# フェーズ 1 完了 引き継ぎ(handover)

| 項目 | 内容 |
|------|------|
| 文書 | フェーズ 1 完了に伴う引き継ぎ資料 |
| 宛先 | **次工程「ドキュメント・プロジェクト整理工程」の担当**(別チャットで起動予定) |
| 作成者・作成日 | 設計担当 Claude(M7 期間担当)、2026-06-05 |
| 前提資料 | `m7-phase1-completion-judgment.md`(フェーズ 1 完了判定)/ retrospective-log v1.0.28+ / architecture-patterns v1.0.13+ |

> **読み方の注意(重要)**
> - 本資料の第 2 層カタログの段取りタグは、整理工程(CHANGE-020)のフェーズ番号再設定後の**新番号(フェーズ1〜4)**に追従済み。新旧対応・各フェーズ定義は REQ-001 §7 を参照。**歴史記録部分(§1.1 / §1.3 一部 / 第 3 層 / 下記改称経緯)は当時の旧番号のまま据え置く**(記録保全のため)。**番号より項目の中身(何を・なぜ・どこを触るか)が正**。
> - 旧ファイル名 `m7-to-phase2-handover.md` から **`m7-phase1-completion-handover.md` に改称**した(「phase2 向け」の含意がミスリードのため。開発者構想ではフェーズ 3 = FR701 取込ツールを先行、フェーズ 2 はスキップの可能性)。
> - 本資料は**引き継ぎ(状態 + 残作業カタログ)**であり、整理工程以降の実作業(アーカイブ化・削除・スケジュール/番号再設定・要望ノート処理・playbook/CLAUDE.md 整理)そのものは行わない。それらの**前提として効く項目**のみ列挙する。

---

## 第 1 層: 引き継ぎの核

### 1.1 フェーズ 1 完成状態(検証済み)

- **コンボ管理アプリの中核機能が完成**。コンボ/セットプレイの登録・編集・一覧・詳細・比較・マイコンボ、タグ、プリセット表記、仮想コントローラ等。
- **複数キャラ対応**: リュウ + AKI + ジェイミー + ガイルの 4 キャラで動作・スキーマ耐久実証(検索/フィルタ/ソートが NFR001 100ms 内)。
- **UI 統一**: shadcn/ui ベースへ移行完了、Header 共通化 + ハンバーガー(Sheet)、PC/タブレット/スマホ実用可能レベル(NFR306、本格スマホ UI は送り)。
- **配布アーキテクチャ成立(M7-06)**: フロント embed による**単一バイナリ**(`-tags=embed_web`)、SPA フォールバック(/api 非干渉)、起動時ブラウザ自動起動 + 実ポート + 起動案内 stdout、設定画面の LAN 共有モード切替トグル。**配布構成でのスマホ LAN 実機検証済み**(同一サーバーへ Wi-Fi 接続)。Windows 単一バイナリ起動確認済み。
- **テスト**: `go test ./...` / `pnpm test`(435)全パス。スキーマ変更なし、新規 CHANGE 起票なし(フェーズ 1 完了時点で CHANGE-019 まで)。

### 1.2 整理工程が最初にやろうとしていること(把握済み・本資料の前提)

整理工程では以下が想定されている(実作業は整理工程担当の仕事、本資料は前提情報のみ提供):

- 不要ドキュメントのアーカイブ化 / 不要ファイル削除
- 作業スケジュールの再設定(優先順位の変化、**非公開の開発者要望ノート**の処理を含む)
- playbook / CLAUDE.md の整理
- **フェーズ等の番号の再設定の可能性**

今後の段取り(整理工程 Part A で確定): 整理工程(本工程・完了間近)→ **フェーズ2 先行リリース準備**(moves スキーマ → FR701 取込ツール → moves CSV インポート → キャラ選択 UI → custom_states → UX 修正)→ **先行リリース**(近しい関係者へ提供し反応を見つつ並行開発)→ **フェーズ3 共有・協調・入力拡充** → 正式リリース準備 → **正式リリース** → **フェーズ4 外部データ追従・拡張 / 機能追加**。

→ **第 2 層のカタログでは、各項目に「どのフェーズ/工程で効くか」を補助タグで付す**(例: `[フェーズ2: FR701 で必須]`)。優先順位を決める材料とするため。

### 1.3 一次資料の所在(詳細はここを参照)

| 内容 | 所在 |
|------|------|
| フェーズ 1 完了判定の逐条 | `m7-phase1-completion-judgment.md` |
| M7-04-1 完了処理 | `M7-04-1-completion-records.md` / progress-log M7-04-1 節 |
| M7-04-2 製造連絡 + E2E 課題 + 反省 | `M7-04-2-connection-memo-handling.md` / progress-log M7-04-2 節 |
| M7-05 製造連絡(L-03 集約 / バグ#6 / E-3 / クリア) | `M7-05-connection-memo-handling.md` / progress-log M7-05 節 |
| M7-06 + 追補 A-1/A-2 | progress-log M7-05/M7-06 各節 |
| 設計担当の反省・申し送り | retrospective-log v1.0.28+ §6.6(M7-16 / M7-17、M7-18 は完了処理ターン 3 で追加予定) |
| アーキテクチャ既知事項 | architecture-patterns v1.0.13+ §3(エラー)/ §9(custom_states / recipe_cache フェーズ2/3 前提) |

---

## 第 2 層: 作業項目カタログ(機能領域別 + 補助タグ、番号非依存)

> 補助タグ凡例: `[フェーズN]` / `[整理工程]` / `[先行リリース]` / `[正式リリース]` = §1.2 の段取りのどの段で効くか(整理工程 CHANGE-020 のフェーズ番号再設定後の新番号)。`[定義済み未実装]` = 設計書に規定があるが未実装。`[意図送り]` = 意図的に送った。`[要設計]` = 実装前に設計が要る。`[CHANGE見込み]` = 着手時に設計書本体改訂 = CHANGE 起票が要りそう。

### A. キャラクター / データ取込

- **A-1. ComboEditor のキャラ選択 = 他キャラ登録 UI** `[定義済み未実装]` `[フェーズ2: FR701 取込ツールの前提]`
  フェーズ 1 では UI 登録はリュウのみ(ComboEditor がリュウ固定)。閲覧系は複数キャラ動的化済み。他キャラを UI から登録するには ComboEditor のキャラ選択化 + キャラ別 moves 取得が必要。取込ツールで複数キャラが入った後に必須。(出典: M7-04-2 連携メモ B-1)
- **A-2. 新規登録/選択モードのリュウ固定 UX(E-2)** `[フェーズ2]`
  A-1 に起因する UX。キャラ選択 UI 化と同時に解消。
- **A-3. 検証用データ全クリアと 000012 耐久 seed の関係** `[整理工程で意識]` `[フェーズ2]`
  フェーズ 1 完了時に `scripts/clear-validation-data.sql` で全キャラのコンボ/セットプレイを物理削除済み(クリーンスレート、マスタ + タグ定義は保持)。**注意: マイグレーション 000012 は耐久テスト用 36 件を seed するため、新規 DB を構築すると再び 36 件が入る** → クリアスクリプトの再実行、または将来 000012 を無効化するマイグレーションが必要。耐久例を残したい場合はスクリプト末尾のリュウ限定版 WHERE コメントを使用。(出典: M7-05 連携メモ B-3)

### B. プリセット / 表記(notation)

- **B-1. 残り 4 プリセットのエイリアス実データ整備** `[定義済み未実装]` `[フェーズ3]`
  フェーズ 1 では official_ja_move(日本語)のみ実データがあり、他プリセットは選択すると日本語へフォールバック(動作は正しい)。numeric_ja / srk 等の実エイリアスデータ整備が残る。
- **B-2. プリセット管理 UI / カスタムプリセット作成(DES-004 §6)** `[定義済み未実装]` `[フェーズ3]`
- **B-3. recipe_cache の無効化(プリセット/エイリアス変更時)** `[定義済み未実装]` `[要設計]` `[B-1/B-2 着手時に必須]`
  SUPP-001 §7 が「プリセット作成時 / エイリアス編集時に当該プリセットの recipe_cache 全エントリ再計算」を**既定済み**だが、トリガ機能(プリセット管理 UI)が未実装のため未配線。B-1/B-2 着手時に §7 の再計算を実装すること。製造側で挙がった「遅延無効化(lazy)」は §7 の「即時(eager)」からの設計変更にあたり、採るなら §7 改訂 + パフォーマンス考慮が要る。(architecture-patterns §9.2)

### C. custom_states(キャラ固有状態)

- **C-1. custom_states の消費・表示・参照** `[定義済み未実装]` `[要設計]` `[CHANGE見込み]` `[フェーズ2]`
  フェーズ 1 では `characters.custom_states` は**保存 + API 返却のみ**で、combos 等から参照されず表示 UI もない。毒(AKI)・酔いレベル(ジェイミー)が実際に機能する仕組み(コンボステップ/状況/レシピ/バリデーション連動、レベルで使用可能技が変わる等)は要設計。DES-003 §3.2 は**定義構造のみ**規定し**消費セマンティクスは未規定** → DES-003/004/006 への追記(CHANGE 起票)が見込まれる。(architecture-patterns §9.1)

### D. UI / レスポンシブ / 国際化

- **D-1. 本格スマホ UI 整備** `[意図送り]` `[フェーズ3: 先行リリースで効く]`
  DES-005 §5.6 / §5.8 / §5.9 / §5.10 / §7。フェーズ 1 は「横スクロール + Header ハンバーガー + 機能動作」の実用可能レベルまで。カード形式 / アコーディオン / ボトムシート / スワイプはフェーズ 3 以降送り(M7-overview §2.9 採用案 Y)。
- **D-2. i18n 英語ロケール整備** `[意図送り]` `[正式リリースで効く]`
  フェーズ 1 は未使用キー削除 + フォールバック汎用化のみ実施。英語ロケールの実データは未整備。
- **D-3. virtual-controller-layout-v1 / 日英切替の一部画面不適応** `[意図送り]`
  継承持ち越し。詳細は m6-to-m7-handover / progress-log 参照。

### E. 配布 / 運用(フェーズ 1 で成立、残る磨き込み)

- **E-1. A-2(LAN 切替)確認ダイアログでの LAN IP 事前提示(R-1)** `[フェーズ3]`
  local→lan の確認時点では `resolveNetwork` が IP 未返却のため、ダイアログはポート明示 + 再起動後にネットワーク欄/起動案内で IP 表示。事前提示が要件なら別 API(候補 IP プレビュー)が必要 = フェーズ3。(M7-06 連携メモ R-1)
- **E-2. クロスビルド mac/Linux の実機起動確認(R-2)** `[意図送り]`
  §11.1 非公式 OS。コンパイル成立のみ確認済み、実機起動は将来。
- **E-3. GitHub Actions CI 整備(§11.3、R-3)** `[フェーズ3]`
  本タスク範囲外と確定済み。クロスビルドの自動化。
- **E-4. pkg/browser の版が約 2 年前(R-4)** `[整理工程後 or フェーズ3: 依存棚卸し]`
  DES-002 §11.1 指定で採用は正当。後続の依存ライブラリ棚卸しで代替検討。
- **E-5. LAN 利用の FW/AV 既知制約(C-2)** `[先行リリースで効く]`
  LAN 利用は inbound ファイアウォール許可が前提。**サードパーティ AV(Norton 等)が Windows FW を代替制御し、Windows 設定では解決しない場合がある**(製品ごとに異なり保証外)。README + progress-log に記載済み。architecture-patterns に既知制約として追記予定(完了処理ターン 4)。DES-002 本体には入れない(運用注意の性質、CHANGE 回避)。

### F. リファクタ / 技術的負債(機能影響なし)

- **F-1. M-1〜M-4(M5-RESEARCH-01 由来 UI 層リファクタ)** `[意図送り]` `[フェーズ3]`
  M7-05 で**全件フェーズ3 送り確定**。M-1 = recipeCache の API 露出(比較画面用)/ M-2 = queryKey の object/flat 統一 / M-3 = useCheckDuplicate の TanStack Query 化 / M-4 = SetupSummary 型の所在集約。(M7-05 連携メモ C-1)
- **F-2. config 保存の TOML 全再エンコード(コメント喪失)+ アトミック書き込み重複** `[フェーズ3]`
  `config.Save` / config service `writeAtomic` が TOML 全再エンコードでコメント・整形を喪失、かつ 2 箇所でアトミック書き込みロジック重複。コメント保持要否 + 共通基盤化はフェーズ3 リファクタで検討。(M7-05 連携メモ C-2 指摘 2)
- **F-3. 検証エラー対応 fetch の共通 lib 昇格余地** `[フェーズ3]`
  `features/setup/api/setupApi.ts` の `requestSetupJSON` が `lib/api-client.ts` の `fetchJSON` を部分再実装(構造化 ApiError 保持のため)。combo 側 `requestJSON` + `ApiError` と合わせ共通 lib へ昇格する余地。今回はフィーチャ間結合回避で setup ローカルに留めた。(M7-05 連携メモ C-2 指摘 3)
- **F-4. P-01: RecipeBuilder console.warn 既知課題** `[フェーズ3]`
  M7-06 とは無関係の既存メモ。後続で対応要。(M7-06 連携メモ R-5)

### G. 完了後の開発者責任作業(整理工程の前 or 中で)

- **G-1. `web/prototypes/` 削除** `[整理工程]`
  M0 プロトタイプ成果物。git 履歴には残る(SUPP-001 §4.1 / CLAUDE.md §3 既定)。**開発者責任**。
- **G-2. model-allocation.md の M7-04 分割 / M7-05・M7-06 = Opus 4.6 反映** `[整理工程]`
  開発者管理。整理工程で扱う想定。
- **G-3. progress-summary の M7 セクション追記** `[整理工程]`
  整理工程で扱う想定。

---

## 第 3 層: 整理工程への申し送り(プロセス面)

- **完了判定基準の網羅監査不足(教訓)**: フェーズ 1 完了判定基準(M7-overview §13 DoD)が、embed / 単一バイナリ / 設定画面 LAN トグル(いずれも DES-002 / DES-005 / NFR の定義済み要件)を**取りこぼしていた**。M7-05/M7-06 で顕在化・補完したが、**今後フェーズ完了基準を作る際は設計書本体(REQ/DES/NFR)の要件を網羅監査して導出すべき**。詳細は retrospective(完了処理ターン 3 で M7-18 として記録予定)。
- **番号再設定時の注意**: 本資料・各 connection-memo・retrospective・architecture-patterns は「フェーズ 2 / 3」を多数参照する。整理工程で番号を再設定するなら、これら補足資料の参照も追従更新が要る(設計書本体 REQ/DES は CHANGE 管理、補足資料は自由改訂)。
- **非公開の開発者要望ノート**: 整理工程で処理予定とのこと。本資料は中身を扱わない(存在のみ認識)。要望取込時に、上記カタログの `[意図送り]` 項目と優先度を突き合わせると段取りが立てやすい。
- **役割の引き継ぎ**: M7 期間の設計担当(本チャット)の作業は本 handover まで。整理工程からは別チャットで担当が起動する。

---

*以上、フェーズ 1 完了 引き継ぎ*
