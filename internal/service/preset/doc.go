// Package preset はプリセットのビジネスロジックを提供する。
//
// M1-04 でパッケージだけが用意され、M20-04 で実体(CRUD ＋ 保護 ＋ 上限検査 ＋
// コピー時のエイリアス実体化 ＋ 削除時の子行削除)が入った。
//
// ★本パッケージは preset ドメインに閉じる。他ドメインへ触る横断ヘルパを
// ここへ置かないこと(指示書 M20-04 §2.3)。
//
// ★recipe_cache の再計算は M20-05(D-360)で接続済みである。作成・エイリアス更新・
// 削除の 3 経路から notation の既存メソッドを呼ぶ。★同等の処理をここへ書かないこと——
// 同じ計算が 2 か所にあると、規則が変わったときに片方だけ追随する(E-76)。
package preset
